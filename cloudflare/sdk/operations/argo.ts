// AUTO-GENERATED by scripts/generate.ts from cloudflare/smithy. Do not edit.
import * as S from "effect/Schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import { CloudflareProtocol } from "../protocol.ts";
import { CloudflareError, CloudflareRateLimited } from "../errors.ts";

export const SmartRoutingEditRequestValue = /*@__PURE__*/ S.Literals([
  "on",
  "off",
]);

export const SmartRoutingEditRequest = /*@__PURE__*/ S.Struct({
  zone_id: S.String.pipe(T.Label()),
  value: SmartRoutingEditRequestValue,
}).pipe(
  T.Http({
    method: "PATCH",
    uri: "/zones/{zone_id}/argo/smart_routing",
    code: 200,
  }),
);

export const SmartRoutingEditResponseValue = /*@__PURE__*/ S.Literals([
  "on",
  "off",
]);

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export const SmartRoutingEditResponse = /*@__PURE__*/ S.Struct({
  id: S.String,
  editable: S.Boolean,
  value: SmartRoutingEditResponseValue,
  modified_on: S.optional(S.String),
});

export const SmartRoutingGetRequest = /*@__PURE__*/ S.Struct({
  zone_id: S.String.pipe(T.Label()),
}).pipe(
  T.Http({
    method: "GET",
    uri: "/zones/{zone_id}/argo/smart_routing",
    code: 200,
  }),
);

export const SmartRoutingGetResponseValue = /*@__PURE__*/ S.Literals([
  "on",
  "off",
]);

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export const SmartRoutingGetResponse = /*@__PURE__*/ S.Struct({
  id: S.String,
  editable: S.Boolean,
  value: SmartRoutingGetResponseValue,
  modified_on: S.optional(S.String),
});

export const TieredCachingEditRequestValue = /*@__PURE__*/ S.Literals([
  "on",
  "off",
]);

export const TieredCachingEditRequest = /*@__PURE__*/ S.Struct({
  zone_id: S.String.pipe(T.Label()),
  value: TieredCachingEditRequestValue,
}).pipe(
  T.Http({
    method: "PATCH",
    uri: "/zones/{zone_id}/argo/tiered_caching",
    code: 200,
  }),
);

export const TieredCachingEditResponseId = /*@__PURE__*/ S.Literals([
  "tiered_caching",
]);

export const TieredCachingEditResponseValue = /*@__PURE__*/ S.Literals([
  "on",
  "off",
]);

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export const TieredCachingEditResponse = /*@__PURE__*/ S.Struct({
  id: TieredCachingEditResponseId,
  editable: S.Boolean,
  value: TieredCachingEditResponseValue,
  modified_on: S.optional(S.String),
});

export const TieredCachingGetRequest = /*@__PURE__*/ S.Struct({
  zone_id: S.String.pipe(T.Label()),
}).pipe(
  T.Http({
    method: "GET",
    uri: "/zones/{zone_id}/argo/tiered_caching",
    code: 200,
  }),
);

export const TieredCachingGetResponseId = /*@__PURE__*/ S.Literals([
  "tiered_caching",
]);

export const TieredCachingGetResponseValue = /*@__PURE__*/ S.Literals([
  "on",
  "off",
]);

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export const TieredCachingGetResponse = /*@__PURE__*/ S.Struct({
  id: TieredCachingGetResponseId,
  editable: S.Boolean,
  value: TieredCachingGetResponseValue,
  modified_on: S.optional(S.String),
});

/** Configures the value of the Argo Smart Routing enablement setting. */
export const SmartRoutingEdit = /*@__PURE__*/ API.make(() => ({
  input: SmartRoutingEditRequest,
  output: SmartRoutingEditResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
}));

/** Retrieves the value of Argo Smart Routing enablement setting. */
export const SmartRoutingGet = /*@__PURE__*/ API.make(() => ({
  input: SmartRoutingGetRequest,
  output: SmartRoutingGetResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
}));

/** Tiered Cache works by dividing Cloudflare's data centers into a hierarchy of lower-tiers and upper-tiers. If content is not cached in lower-tier data centers (generally the ones closest to a visitor), the lower-tier must ask an upper-tier to see if it has the content. If the upper-tier does not have the content, only the upper-tier can ask the origin for content. This practice improves bandwidth efficiency by limiting the number of data centers that can ask the origin for content, which reduces origin load and makes websites more cost-effective to operate. Additionally, Tiered Cache concentrates connections to origin servers so they come from a small number of data centers rather than the full set of network locations. This results in fewer open connections using server resources. */
export const TieredCachingEdit = /*@__PURE__*/ API.make(() => ({
  input: TieredCachingEditRequest,
  output: TieredCachingEditResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
}));

/** Tiered Cache works by dividing Cloudflare's data centers into a hierarchy of lower-tiers and upper-tiers. If content is not cached in lower-tier data centers (generally the ones closest to a visitor), the lower-tier must ask an upper-tier to see if it has the content. If the upper-tier does not have the content, only the upper-tier can ask the origin for content. This practice improves bandwidth efficiency by limiting the number of data centers that can ask the origin for content, which reduces origin load and makes websites more cost-effective to operate. Additionally, Tiered Cache concentrates connections to origin servers so they come from a small number of data centers rather than the full set of network locations. This results in fewer open connections using server resources. */
export const TieredCachingGet = /*@__PURE__*/ API.make(() => ({
  input: TieredCachingGetRequest,
  output: TieredCachingGetResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
}));
