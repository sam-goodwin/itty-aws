// AUTO-GENERATED by scripts/generate.ts from cloudflare/smithy. Do not edit.
import * as S from "effect/Schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import { CloudflareProtocol } from "../protocol.ts";
import { CloudflareError, CloudflareRateLimited } from "../errors.ts";

export const GetRequest = /*@__PURE__*/ S.Struct({
  zone_id: S.String.pipe(T.Label()),
}).pipe(
  T.Http({
    method: "GET",
    uri: "/zones/{zone_id}/cache/origin_post_quantum_encryption",
    code: 200,
  }),
);

export const GetResponseId = /*@__PURE__*/ S.Literals(["origin_pqe"]);

export const GetResponseValue = /*@__PURE__*/ S.Literals([
  "preferred",
  "supported",
  "off",
]);

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export const GetResponse = /*@__PURE__*/ S.Struct({
  id: GetResponseId,
  editable: S.Boolean,
  value: GetResponseValue,
  modified_on: S.optional(S.String),
});

export const UpdateRequestValue = /*@__PURE__*/ S.Literals([
  "preferred",
  "supported",
  "off",
]);

export const UpdateRequest = /*@__PURE__*/ S.Struct({
  zone_id: S.String.pipe(T.Label()),
  value: UpdateRequestValue,
}).pipe(
  T.Http({
    method: "PUT",
    uri: "/zones/{zone_id}/cache/origin_post_quantum_encryption",
    code: 200,
  }),
);

export const UpdateResponseId = /*@__PURE__*/ S.Literals(["origin_pqe"]);

export const UpdateResponseValue = /*@__PURE__*/ S.Literals([
  "preferred",
  "supported",
  "off",
]);

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export const UpdateResponse = /*@__PURE__*/ S.Struct({
  id: UpdateResponseId,
  editable: S.Boolean,
  value: UpdateResponseValue,
  modified_on: S.optional(S.String),
});

/** Instructs Cloudflare to use Post-Quantum (PQ) key agreement algorithms when connecting to your origin. Preferred instructs Cloudflare to opportunistically send a Post-Quantum keyshare in the first message to the origin (for fastest connections when the origin supports and prefers PQ), supported means that PQ algorithms are advertised but only used when requested by the origin, and off means that PQ algorithms are not advertised. */
export const Get = /*@__PURE__*/ API.make(() => ({
  input: GetRequest,
  output: GetResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
}));

/** Instructs Cloudflare to use Post-Quantum (PQ) key agreement algorithms when connecting to your origin. Preferred instructs Cloudflare to opportunistically send a Post-Quantum keyshare in the first message to the origin (for fastest connections when the origin supports and prefers PQ), supported means that PQ algorithms are advertised but only used when requested by the origin, and off means that PQ algorithms are not advertised. */
export const Update = /*@__PURE__*/ API.make(() => ({
  input: UpdateRequest,
  output: UpdateResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
}));
