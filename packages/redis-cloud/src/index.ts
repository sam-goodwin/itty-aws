/**
 * @distilled.cloud/redis-cloud — Redis Cloud SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy model
 * in `.generated-specs` (written by `scripts/convert.ts` from the OpenAPI
 * spec). Everything else in this folder is hand-written.
 *
 * @example
 * ```ts
 * import * as RedisCloud from "@distilled.cloud/redis-cloud";
 *
 * const account = yield* RedisCloud.Services.redisCloud.getCurrentAccount({});
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  RedisCloudProtocol,
  type RedisCloudOpError,
  type RedisCloudOpContext,
} from "./protocol.ts";
export { paginateCursor } from "./pagination.ts";
export * as Retry from "./retry.ts";
export * as Services from "./services/index.ts";
export * from "./services/redisCloud.ts";
export {
  BadRequest,
  Conflict,
  Forbidden,
  NotFound,
  UnprocessableEntity,
} from "./services/redisCloud.ts";
