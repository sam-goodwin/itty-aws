// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/cloud.spec.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  HetznerProtocol,
  type HetznerOpError,
  type HetznerOpContext,
} from "../protocol.ts";
import { paginatePageNumber } from "../pagination.ts";
import { UnknownHetznerError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { HetznerOpError, HetznerOpContext };

/** Mode of the [Zone](#tag/zones). For more information, see [Zone Modes](#tag/zones/zone-modes). */
export type CreateZoneRequestMode = "primary" | "secondary";
export const CreateZoneRequestMode = /*@__PURE__*/ S.String;

/** User-defined labels (`key/value` pairs) for the Resource. For more information, see "[Labels](#description/labels)". */
export type CreateZoneRequestLabelsMap = { [key: string]: string | undefined };
export const CreateZoneRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateZoneRequestLabelsMap>;

/** [Transaction signature (TSIG)](https://en.wikipedia.org/wiki/TSIG) algorithm used to generate the TSIG key. */
export type CreateZoneRequestPrimaryNameserversItemTsigAlgorithm =
  | "hmac-md5"
  | "hmac-sha1"
  | "hmac-sha256";
export const CreateZoneRequestPrimaryNameserversItemTsigAlgorithm =
  /*@__PURE__*/ S.String;

/** Primary nameserver that returns [Zones](#tag/zones) via `AXFR`. Must allow queries from and may send `NOTIFY` queries to [Hetzner's secondary nameservers](https://docs.hetzner.com/dns-console/dns/general/authoritative-name-servers#secondary-dns-servers-old-name-servers-for-robot-customers). */
export interface CreateZoneRequestPrimaryNameserversItem {
  /** Public IPv4 or IPv6 address of the primary nameserver. Must be unique across all primary nameservers of this zone. */
  address: string;
  /** Port of the primary nameserver. */
  port?: number;
  /** [Transaction signature (TSIG)](https://en.wikipedia.org/wiki/TSIG) key to use for the zone transfer. Must be base64 encoded. */
  tsig_key?: string;
  /** [Transaction signature (TSIG)](https://en.wikipedia.org/wiki/TSIG) algorithm used to generate the TSIG key. */
  tsig_algorithm?:
    | CreateZoneRequestPrimaryNameserversItemTsigAlgorithm
    | (string & {});
}
export const CreateZoneRequestPrimaryNameserversItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      address: S.String,
      port: S.optional(S.Number),
      tsig_key: S.optional(S.String),
      tsig_algorithm: S.optional(
        CreateZoneRequestPrimaryNameserversItemTsigAlgorithm,
      ),
    }),
).annotate({
  identifier: "CreateZoneRequestPrimaryNameserversItem",
}) as any as S.Schema<CreateZoneRequestPrimaryNameserversItem>;

/** Primary nameservers of the [Zone](#tag/zones). Only applicable for [Zones](#tag/zones) in secondary mode. Ignored for [Zones](#tag/zones) in primary mode. */
export type CreateZoneRequestPrimaryNameserversList =
  Array<CreateZoneRequestPrimaryNameserversItem>;
export const CreateZoneRequestPrimaryNameserversList = /*@__PURE__*/ S.Array(
  CreateZoneRequestPrimaryNameserversItem,
) as any as S.Schema<CreateZoneRequestPrimaryNameserversList>;

/** Type of the [RRSet](#tag/zone-rrsets). */
export type CreateZoneRequestRrsetsItemType =
  | "A"
  | "AAAA"
  | "CAA"
  | "CNAME"
  | "DS"
  | "HINFO"
  | "HTTPS"
  | "MX"
  | "NS"
  | "PTR"
  | "RP"
  | "SOA"
  | "SRV"
  | "SVCB"
  | "TLSA"
  | "TXT";
export const CreateZoneRequestRrsetsItemType = /*@__PURE__*/ S.String;

/** Record of a [RRSet](#tag/zone-rrsets). The `value` is used to identify the record in an [RRSet](#tag/zone-rrsets). */
export interface CreateZoneRequestRrsetsItemRecordsItem {
  /** Value of the record. For details about accepted values, see the [DNS record types documentation](https://docs.hetzner.com/networking/dns/record-types/overview/). */
  value: string;
  /** Comment of the record. */
  comment?: string;
}
export const CreateZoneRequestRrsetsItemRecordsItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      value: S.String,
      comment: S.optional(S.String),
    }),
).annotate({
  identifier: "CreateZoneRequestRrsetsItemRecordsItem",
}) as any as S.Schema<CreateZoneRequestRrsetsItemRecordsItem>;

/** Records of the [RRSet](#tag/zone-rrsets). Must not be empty and must only contain distinct record values. The order of records returned in responses is not guaranteed to be consistent. */
export type CreateZoneRequestRrsetsItemRecordsList =
  Array<CreateZoneRequestRrsetsItemRecordsItem>;
export const CreateZoneRequestRrsetsItemRecordsList = /*@__PURE__*/ S.Array(
  CreateZoneRequestRrsetsItemRecordsItem,
) as any as S.Schema<CreateZoneRequestRrsetsItemRecordsList>;

/** User-defined labels (`key/value` pairs) for the Resource. For more information, see "[Labels](#description/labels)". */
export type CreateZoneRequestRrsetsItemLabelsMap = {
  [key: string]: string | undefined;
};
export const CreateZoneRequestRrsetsItemLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateZoneRequestRrsetsItemLabelsMap>;

export interface CreateZoneRequestRrsetsItem {
  /** Name of the [RRSet](#tag/zone-rrsets). The name must be in lower case, and must not end with a dot or the [Zone](#tag/zones) name. Names containing non-ASCII characters must be transcribed to [Punycode](https://wikipedia.org/wiki/Punycode) representation with ACE prefix, e.g. `xn--4bi` (✉️). For the [Zone](#tag/zones) apex, use `@`. */
  name: string;
  /** Type of the [RRSet](#tag/zone-rrsets). */
  type: CreateZoneRequestRrsetsItemType | (string & {});
  /** Time To Live (TTL) of the [RRSet](#tag/zone-rrsets). Must be in between 60s and 2147483647s. If not set, the [Zone's](#tag/zones) Default TTL is used. */
  ttl?: number | null;
  /** Records of the [RRSet](#tag/zone-rrsets). Must not be empty and must only contain distinct record values. The order of records returned in responses is not guaranteed to be consistent. */
  records: CreateZoneRequestRrsetsItemRecordsList;
  /** User-defined labels (`key/value` pairs) for the Resource. For more information, see "[Labels](#description/labels)". */
  labels?: CreateZoneRequestRrsetsItemLabelsMap;
}
export const CreateZoneRequestRrsetsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    type: CreateZoneRequestRrsetsItemType,
    ttl: S.optional(S.NullOr(S.Number)),
    records: CreateZoneRequestRrsetsItemRecordsList,
    labels: S.optional(CreateZoneRequestRrsetsItemLabelsMap),
  }),
).annotate({
  identifier: "CreateZoneRequestRrsetsItem",
}) as any as S.Schema<CreateZoneRequestRrsetsItem>;

/** [RRSets](#tag/zone-rrsets) to be added to the [Zone](#tag/zones). Only applicable for [Zones](#tag/zones) in primary mode. Ignored for [Zones](#tag/zones) in secondary mode. */
export type CreateZoneRequestRrsetsList = Array<CreateZoneRequestRrsetsItem>;
export const CreateZoneRequestRrsetsList = /*@__PURE__*/ S.Array(
  CreateZoneRequestRrsetsItem,
) as any as S.Schema<CreateZoneRequestRrsetsList>;

export interface CreateZoneRequest {
  /** Name of the [Zone](#tag/zones). All names with [well-known public suffixes](https://publicsuffix.org/) (e.g. `.de`, `.com`, `.co.uk`) are supported. Subdomains are not supported. The name must be in lower case and must not end with a dot. [Internationalized domain names](https://en.wikipedia.org/wiki/Internationalized_domain_name) must be transcribed to [Punycode](https://wikipedia.org/wiki/Punycode) representation with ACE prefix, e.g. `xn--mnchen-3ya.de` (`münchen.de`). */
  name: string;
  /** Mode of the [Zone](#tag/zones). For more information, see [Zone Modes](#tag/zones/zone-modes). */
  mode: CreateZoneRequestMode | (string & {});
  /** Default Time To Live (TTL) of the [Zone](#tag/zones). Must be in between 60s and 2147483647s. This TTL is used for [RRSets](#tag/zone-rrsets) that do not explicitly define a TTL. */
  ttl?: number;
  /** User-defined labels (`key/value` pairs) for the Resource. For more information, see "[Labels](#description/labels)". */
  labels?: CreateZoneRequestLabelsMap;
  /** Primary nameservers of the [Zone](#tag/zones). Only applicable for [Zones](#tag/zones) in secondary mode. Ignored for [Zones](#tag/zones) in primary mode. */
  primary_nameservers?: CreateZoneRequestPrimaryNameserversList;
  /** [RRSets](#tag/zone-rrsets) to be added to the [Zone](#tag/zones). Only applicable for [Zones](#tag/zones) in primary mode. Ignored for [Zones](#tag/zones) in secondary mode. */
  rrsets?: CreateZoneRequestRrsetsList;
  /** Zone file to import. Only applicable for [Zones](#tag/zones) in primary mode. Ignored for [Zones](#tag/zones) in secondary mode. If provided, `rrsets` must be empty. See [Zone file import](#tag/zones/zone-file-import) for more details. */
  zonefile?: string;
}
export const CreateZoneRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    mode: CreateZoneRequestMode,
    ttl: S.optional(S.Number),
    labels: S.optional(CreateZoneRequestLabelsMap),
    primary_nameservers: S.optional(CreateZoneRequestPrimaryNameserversList),
    rrsets: S.optional(CreateZoneRequestRrsetsList),
    zonefile: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/zones", code: 200 })),
).annotate({
  identifier: "CreateZoneRequest",
}) as any as S.Schema<CreateZoneRequest>;

/** [Transaction signature (TSIG)](https://en.wikipedia.org/wiki/TSIG) algorithm used to generate the TSIG key. */
export type ZonePrimaryPrimaryNameserversItemTsigAlgorithm =
  | "hmac-md5"
  | "hmac-sha1"
  | "hmac-sha256";
export const ZonePrimaryPrimaryNameserversItemTsigAlgorithm =
  /*@__PURE__*/ S.String;

/** Primary nameserver that returns [Zones](#tag/zones) via `AXFR`. Must allow queries from and may send `NOTIFY` queries to [Hetzner's secondary nameservers](https://docs.hetzner.com/dns-console/dns/general/authoritative-name-servers#secondary-dns-servers-old-name-servers-for-robot-customers). */
export interface ZonePrimaryPrimaryNameserversItem {
  /** Public IPv4 or IPv6 address of the primary nameserver. Must be unique across all primary nameservers of this zone. */
  address: string;
  /** Port of the primary nameserver. */
  port?: number;
  /** [Transaction signature (TSIG)](https://en.wikipedia.org/wiki/TSIG) key to use for the zone transfer. Must be base64 encoded. */
  tsig_key?: string;
  /** [Transaction signature (TSIG)](https://en.wikipedia.org/wiki/TSIG) algorithm used to generate the TSIG key. */
  tsig_algorithm?: ZonePrimaryPrimaryNameserversItemTsigAlgorithm;
}
export const ZonePrimaryPrimaryNameserversItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.String,
    port: S.optional(S.Number),
    tsig_key: S.optional(S.String),
    tsig_algorithm: S.optional(ZonePrimaryPrimaryNameserversItemTsigAlgorithm),
  }),
).annotate({
  identifier: "ZonePrimaryPrimaryNameserversItem",
}) as any as S.Schema<ZonePrimaryPrimaryNameserversItem>;

/** Primary nameservers of the [Zone](#tag/zones). Only set if [Zone](#tag/zones) is in [secondary mode](#tag/zones/zone-modes), otherwise empty. */
export type ZonePrimaryPrimaryNameserversList =
  Array<ZonePrimaryPrimaryNameserversItem>;
export const ZonePrimaryPrimaryNameserversList = /*@__PURE__*/ S.Array(
  ZonePrimaryPrimaryNameserversItem,
) as any as S.Schema<ZonePrimaryPrimaryNameserversList>;

/** User-defined labels (`key/value` pairs) for the Resource. For more information, see "[Labels](#description/labels)". */
export type ZonePrimaryLabelsMap = { [key: string]: string | undefined };
export const ZonePrimaryLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ZonePrimaryLabelsMap>;

/** Protection configuration for the Resource. */
export interface ZonePrimaryProtection {
  /** Prevent the Resource from being deleted. */
  delete: boolean;
}
export const ZonePrimaryProtection = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    delete: S.Boolean,
  }),
).annotate({
  identifier: "ZonePrimaryProtection",
}) as any as S.Schema<ZonePrimaryProtection>;

/** Status of the [Zone](#tag/zones). */
export type ZonePrimaryStatus = "ok" | "updating" | "error";
export const ZonePrimaryStatus = /*@__PURE__*/ S.String;

/** Authoritative Hetzner nameservers assigned to this [Zone](#tag/zones). */
export type ZonePrimaryAuthoritativeNameserversAssignedList = Array<string>;
export const ZonePrimaryAuthoritativeNameserversAssignedList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ZonePrimaryAuthoritativeNameserversAssignedList>;

/** Authoritative nameservers currently delegated to by the parent DNS zone. If these don't match the assigned authoritative nameservers, the DNS zone is currently not being served by Hetzner. */
export type ZonePrimaryAuthoritativeNameserversDelegatedList = Array<string>;
export const ZonePrimaryAuthoritativeNameserversDelegatedList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ZonePrimaryAuthoritativeNameserversDelegatedList>;

/** Status of the delegation. */
export type ZonePrimaryAuthoritativeNameserversDelegationStatus =
  | "valid"
  | "partially-valid"
  | "invalid"
  | "lame"
  | "unregistered"
  | "unknown";
export const ZonePrimaryAuthoritativeNameserversDelegationStatus =
  /*@__PURE__*/ S.String;

export interface ZonePrimaryAuthoritativeNameservers {
  /** Authoritative Hetzner nameservers assigned to this [Zone](#tag/zones). */
  assigned: ZonePrimaryAuthoritativeNameserversAssignedList;
  /** Authoritative nameservers currently delegated to by the parent DNS zone. If these don't match the assigned authoritative nameservers, the DNS zone is currently not being served by Hetzner. */
  delegated: ZonePrimaryAuthoritativeNameserversDelegatedList;
  /** Point in time (in [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339#section-5.6) format) when the DNS zone delegation was last checked. */
  delegation_last_check: string | null;
  /** Status of the delegation. */
  delegation_status?: ZonePrimaryAuthoritativeNameserversDelegationStatus;
}
export const ZonePrimaryAuthoritativeNameservers = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    assigned: ZonePrimaryAuthoritativeNameserversAssignedList,
    delegated: ZonePrimaryAuthoritativeNameserversDelegatedList,
    delegation_last_check: S.NullOr(S.String),
    delegation_status: S.optional(
      ZonePrimaryAuthoritativeNameserversDelegationStatus,
    ),
  }),
).annotate({
  identifier: "ZonePrimaryAuthoritativeNameservers",
}) as any as S.Schema<ZonePrimaryAuthoritativeNameservers>;

/** Registrar of the domain. */
export type ZonePrimaryRegistrar = "hetzner" | "other" | "unknown";
export const ZonePrimaryRegistrar = /*@__PURE__*/ S.String;

/** Mode of the [Zone](#tag/zones). For more information, see [Zone Modes](#tag/zones/zone-modes). */
export type ZonePrimaryMode = "primary";
export const ZonePrimaryMode = /*@__PURE__*/ S.String;

export interface ZonePrimary {
  /** ID of the [Zone](#tag/zones). */
  id: number;
  /** Name of the [Zone](#tag/zones). All names with [well-known public suffixes](https://publicsuffix.org/) (e.g. `.de`, `.com`, `.co.uk`) are supported. Subdomains are not supported. The name must be in lower case and must not end with a dot. [Internationalized domain names](https://en.wikipedia.org/wiki/Internationalized_domain_name) must be transcribed to [Punycode](https://wikipedia.org/wiki/Punycode) representation with ACE prefix, e.g. `xn--mnchen-3ya.de` (`münchen.de`). */
  name: string;
  /** Point in time when the Resource was created (in [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339#section-5.6) format). */
  created: string;
  /** Primary nameservers of the [Zone](#tag/zones). Only set if [Zone](#tag/zones) is in [secondary mode](#tag/zones/zone-modes), otherwise empty. */
  primary_nameservers?: ZonePrimaryPrimaryNameserversList;
  /** User-defined labels (`key/value` pairs) for the Resource. For more information, see "[Labels](#description/labels)". */
  labels: ZonePrimaryLabelsMap;
  /** Protection configuration for the Resource. */
  protection: ZonePrimaryProtection;
  /** Default Time To Live (TTL) of the [Zone](#tag/zones). Must be in between 60s and 2147483647s. This TTL is used for [RRSets](#tag/zone-rrsets) that do not explicitly define a TTL. */
  ttl: number;
  /** Status of the [Zone](#tag/zones). */
  status: ZonePrimaryStatus;
  /** Number of resource records (RR) within the [Zone](#tag/zones). */
  record_count: number;
  authoritative_nameservers: ZonePrimaryAuthoritativeNameservers;
  /** Registrar of the domain. */
  registrar: ZonePrimaryRegistrar;
  /** Mode of the [Zone](#tag/zones). For more information, see [Zone Modes](#tag/zones/zone-modes). */
  mode: ZonePrimaryMode;
}
export const ZonePrimary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    name: S.String,
    created: S.String,
    primary_nameservers: S.optional(ZonePrimaryPrimaryNameserversList),
    labels: ZonePrimaryLabelsMap,
    protection: ZonePrimaryProtection,
    ttl: S.Number,
    status: ZonePrimaryStatus,
    record_count: S.Number,
    authoritative_nameservers: ZonePrimaryAuthoritativeNameservers,
    registrar: ZonePrimaryRegistrar,
    mode: ZonePrimaryMode,
  }),
).annotate({ identifier: "ZonePrimary" }) as any as S.Schema<ZonePrimary>;

/** [Transaction signature (TSIG)](https://en.wikipedia.org/wiki/TSIG) algorithm used to generate the TSIG key. */
export type ZoneSecondaryPrimaryNameserversItemTsigAlgorithm =
  | "hmac-md5"
  | "hmac-sha1"
  | "hmac-sha256";
export const ZoneSecondaryPrimaryNameserversItemTsigAlgorithm =
  /*@__PURE__*/ S.String;

/** Primary nameserver that returns [Zones](#tag/zones) via `AXFR`. Must allow queries from and may send `NOTIFY` queries to [Hetzner's secondary nameservers](https://docs.hetzner.com/dns-console/dns/general/authoritative-name-servers#secondary-dns-servers-old-name-servers-for-robot-customers). */
export interface ZoneSecondaryPrimaryNameserversItem {
  /** Public IPv4 or IPv6 address of the primary nameserver. Must be unique across all primary nameservers of this zone. */
  address: string;
  /** Port of the primary nameserver. */
  port?: number;
  /** [Transaction signature (TSIG)](https://en.wikipedia.org/wiki/TSIG) key to use for the zone transfer. Must be base64 encoded. */
  tsig_key?: string;
  /** [Transaction signature (TSIG)](https://en.wikipedia.org/wiki/TSIG) algorithm used to generate the TSIG key. */
  tsig_algorithm?: ZoneSecondaryPrimaryNameserversItemTsigAlgorithm;
}
export const ZoneSecondaryPrimaryNameserversItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.String,
    port: S.optional(S.Number),
    tsig_key: S.optional(S.String),
    tsig_algorithm: S.optional(
      ZoneSecondaryPrimaryNameserversItemTsigAlgorithm,
    ),
  }),
).annotate({
  identifier: "ZoneSecondaryPrimaryNameserversItem",
}) as any as S.Schema<ZoneSecondaryPrimaryNameserversItem>;

/** Primary nameservers of the [Zone](#tag/zones). Only set if [Zone](#tag/zones) is in [secondary mode](#tag/zones/zone-modes), otherwise empty. */
export type ZoneSecondaryPrimaryNameserversList =
  Array<ZoneSecondaryPrimaryNameserversItem>;
export const ZoneSecondaryPrimaryNameserversList = /*@__PURE__*/ S.Array(
  ZoneSecondaryPrimaryNameserversItem,
) as any as S.Schema<ZoneSecondaryPrimaryNameserversList>;

/** User-defined labels (`key/value` pairs) for the Resource. For more information, see "[Labels](#description/labels)". */
export type ZoneSecondaryLabelsMap = { [key: string]: string | undefined };
export const ZoneSecondaryLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ZoneSecondaryLabelsMap>;

/** Protection configuration for the Resource. */
export type ZoneSecondaryProtection = ZonePrimaryProtection;
export const ZoneSecondaryProtection = ZonePrimaryProtection;

/** Status of the [Zone](#tag/zones). */
export type ZoneSecondaryStatus = "ok" | "updating" | "error";
export const ZoneSecondaryStatus = /*@__PURE__*/ S.String;

/** Authoritative Hetzner nameservers assigned to this [Zone](#tag/zones). */
export type ZoneSecondaryAuthoritativeNameserversAssignedList = Array<string>;
export const ZoneSecondaryAuthoritativeNameserversAssignedList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ZoneSecondaryAuthoritativeNameserversAssignedList>;

/** Authoritative nameservers currently delegated to by the parent DNS zone. If these don't match the assigned authoritative nameservers, the DNS zone is currently not being served by Hetzner. */
export type ZoneSecondaryAuthoritativeNameserversDelegatedList = Array<string>;
export const ZoneSecondaryAuthoritativeNameserversDelegatedList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ZoneSecondaryAuthoritativeNameserversDelegatedList>;

/** Status of the delegation. */
export type ZoneSecondaryAuthoritativeNameserversDelegationStatus =
  | "valid"
  | "partially-valid"
  | "invalid"
  | "lame"
  | "unregistered"
  | "unknown";
export const ZoneSecondaryAuthoritativeNameserversDelegationStatus =
  /*@__PURE__*/ S.String;

export interface ZoneSecondaryAuthoritativeNameservers {
  /** Authoritative Hetzner nameservers assigned to this [Zone](#tag/zones). */
  assigned: ZoneSecondaryAuthoritativeNameserversAssignedList;
  /** Authoritative nameservers currently delegated to by the parent DNS zone. If these don't match the assigned authoritative nameservers, the DNS zone is currently not being served by Hetzner. */
  delegated: ZoneSecondaryAuthoritativeNameserversDelegatedList;
  /** Point in time (in [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339#section-5.6) format) when the DNS zone delegation was last checked. */
  delegation_last_check: string | null;
  /** Status of the delegation. */
  delegation_status?: ZoneSecondaryAuthoritativeNameserversDelegationStatus;
}
export const ZoneSecondaryAuthoritativeNameservers = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      assigned: ZoneSecondaryAuthoritativeNameserversAssignedList,
      delegated: ZoneSecondaryAuthoritativeNameserversDelegatedList,
      delegation_last_check: S.NullOr(S.String),
      delegation_status: S.optional(
        ZoneSecondaryAuthoritativeNameserversDelegationStatus,
      ),
    }),
).annotate({
  identifier: "ZoneSecondaryAuthoritativeNameservers",
}) as any as S.Schema<ZoneSecondaryAuthoritativeNameservers>;

/** Registrar of the domain. */
export type ZoneSecondaryRegistrar = "hetzner" | "other" | "unknown";
export const ZoneSecondaryRegistrar = /*@__PURE__*/ S.String;

/** Mode of the [Zone](#tag/zones). For more information, see [Zone Modes](#tag/zones/zone-modes). */
export type ZoneSecondaryMode = "secondary";
export const ZoneSecondaryMode = /*@__PURE__*/ S.String;

export interface ZoneSecondary {
  /** ID of the [Zone](#tag/zones). */
  id: number;
  /** Name of the [Zone](#tag/zones). All names with [well-known public suffixes](https://publicsuffix.org/) (e.g. `.de`, `.com`, `.co.uk`) are supported. Subdomains are not supported. The name must be in lower case and must not end with a dot. [Internationalized domain names](https://en.wikipedia.org/wiki/Internationalized_domain_name) must be transcribed to [Punycode](https://wikipedia.org/wiki/Punycode) representation with ACE prefix, e.g. `xn--mnchen-3ya.de` (`münchen.de`). */
  name: string;
  /** Point in time when the Resource was created (in [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339#section-5.6) format). */
  created: string;
  /** Primary nameservers of the [Zone](#tag/zones). Only set if [Zone](#tag/zones) is in [secondary mode](#tag/zones/zone-modes), otherwise empty. */
  primary_nameservers?: ZoneSecondaryPrimaryNameserversList;
  /** User-defined labels (`key/value` pairs) for the Resource. For more information, see "[Labels](#description/labels)". */
  labels: ZoneSecondaryLabelsMap;
  /** Protection configuration for the Resource. */
  protection: ZonePrimaryProtection;
  /** Default Time To Live (TTL) of the [Zone](#tag/zones). Must be in between 60s and 2147483647s. This TTL is used for [RRSets](#tag/zone-rrsets) that do not explicitly define a TTL. */
  ttl: number;
  /** Status of the [Zone](#tag/zones). */
  status: ZoneSecondaryStatus;
  /** Number of resource records (RR) within the [Zone](#tag/zones). */
  record_count: number;
  authoritative_nameservers: ZoneSecondaryAuthoritativeNameservers;
  /** Registrar of the domain. */
  registrar: ZoneSecondaryRegistrar;
  /** Mode of the [Zone](#tag/zones). For more information, see [Zone Modes](#tag/zones/zone-modes). */
  mode: ZoneSecondaryMode;
}
export const ZoneSecondary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    name: S.String,
    created: S.String,
    primary_nameservers: S.optional(ZoneSecondaryPrimaryNameserversList),
    labels: ZoneSecondaryLabelsMap,
    protection: ZonePrimaryProtection,
    ttl: S.Number,
    status: ZoneSecondaryStatus,
    record_count: S.Number,
    authoritative_nameservers: ZoneSecondaryAuthoritativeNameservers,
    registrar: ZoneSecondaryRegistrar,
    mode: ZoneSecondaryMode,
  }),
).annotate({ identifier: "ZoneSecondary" }) as any as S.Schema<ZoneSecondary>;

export type CreateZoneResponseZone = ZonePrimary | ZoneSecondary;
export const CreateZoneResponseZone =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateZoneResponseZone>;

/** Status of the Action. */
export type CreateZoneResponseActionStatus = "running" | "success" | "error";
export const CreateZoneResponseActionStatus = /*@__PURE__*/ S.String;

export interface CreateZoneResponseActionResourcesItem {
  /** ID of the Resource. */
  id: number;
  /** Type of the Resource. */
  type: string;
}
export const CreateZoneResponseActionResourcesItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      id: S.Number,
      type: S.String,
    }),
).annotate({
  identifier: "CreateZoneResponseActionResourcesItem",
}) as any as S.Schema<CreateZoneResponseActionResourcesItem>;

/** Resources the Action relates to. */
export type CreateZoneResponseActionResourcesList =
  Array<CreateZoneResponseActionResourcesItem>;
export const CreateZoneResponseActionResourcesList = /*@__PURE__*/ S.Array(
  CreateZoneResponseActionResourcesItem,
) as any as S.Schema<CreateZoneResponseActionResourcesList>;

/** Error message for the Action if an error occurred, otherwise null. */
export interface CreateZoneResponseActionError {
  /** Fixed error code for machines. */
  code: string;
  /** Error message for humans. */
  message: string;
}
export const CreateZoneResponseActionError = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    code: S.String,
    message: S.String,
  }),
).annotate({
  identifier: "CreateZoneResponseActionError",
}) as any as S.Schema<CreateZoneResponseActionError>;

export interface CreateZoneResponseAction {
  /** ID of the [Action](#description/actions). */
  id: number;
  /** Command executed in the Action. */
  command: string;
  /** Status of the Action. */
  status: CreateZoneResponseActionStatus;
  /** Point in time when the Action was started (in [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339#section-5.6) format). */
  started: string;
  /** Point in time when the Action was finished (in [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339#section-5.6) format). Only set if the Action is finished otherwise null. */
  finished: string | null;
  /** Progress of the Action in percent. */
  progress: number;
  /** Resources the Action relates to. */
  resources: CreateZoneResponseActionResourcesList;
  /** Error message for the Action if an error occurred, otherwise null. */
  error: CreateZoneResponseActionError | null;
}
export const CreateZoneResponseAction = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    command: S.String,
    status: CreateZoneResponseActionStatus,
    started: S.String,
    finished: S.NullOr(S.String),
    progress: S.Number,
    resources: CreateZoneResponseActionResourcesList,
    error: S.NullOr(CreateZoneResponseActionError),
  }),
).annotate({
  identifier: "CreateZoneResponseAction",
}) as any as S.Schema<CreateZoneResponseAction>;

export interface CreateZoneResponse {
  zone: CreateZoneResponseZone;
  action: CreateZoneResponseAction;
}
export const CreateZoneResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    zone: CreateZoneResponseZone,
    action: CreateZoneResponseAction,
  }),
).annotate({
  identifier: "CreateZoneResponse",
}) as any as S.Schema<CreateZoneResponse>;

export interface DeleteZoneRequest {
  /** ID or Name of the Zone. */
  id_or_name: string;
}
export const DeleteZoneRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id_or_name: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/zones/{id_or_name}", code: 200 })),
).annotate({
  identifier: "DeleteZoneRequest",
}) as any as S.Schema<DeleteZoneRequest>;

/** Status of the Action. */
export type DeleteZoneResponseActionStatus = "running" | "success" | "error";
export const DeleteZoneResponseActionStatus = /*@__PURE__*/ S.String;

export type DeleteZoneResponseActionResourcesItem =
  CreateZoneResponseActionResourcesItem;
export const DeleteZoneResponseActionResourcesItem =
  CreateZoneResponseActionResourcesItem;

/** Resources the Action relates to. */
export type DeleteZoneResponseActionResourcesList =
  Array<CreateZoneResponseActionResourcesItem>;
export const DeleteZoneResponseActionResourcesList = /*@__PURE__*/ S.Array(
  CreateZoneResponseActionResourcesItem,
) as any as S.Schema<DeleteZoneResponseActionResourcesList>;

/** Error message for the Action if an error occurred, otherwise null. */
export type DeleteZoneResponseActionError = CreateZoneResponseActionError;
export const DeleteZoneResponseActionError = CreateZoneResponseActionError;

export interface DeleteZoneResponseAction {
  /** ID of the [Action](#description/actions). */
  id: number;
  /** Command executed in the Action. */
  command: string;
  /** Status of the Action. */
  status: DeleteZoneResponseActionStatus;
  /** Point in time when the Action was started (in [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339#section-5.6) format). */
  started: string;
  /** Point in time when the Action was finished (in [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339#section-5.6) format). Only set if the Action is finished otherwise null. */
  finished: string | null;
  /** Progress of the Action in percent. */
  progress: number;
  /** Resources the Action relates to. */
  resources: DeleteZoneResponseActionResourcesList;
  /** Error message for the Action if an error occurred, otherwise null. */
  error: CreateZoneResponseActionError | null;
}
export const DeleteZoneResponseAction = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    command: S.String,
    status: DeleteZoneResponseActionStatus,
    started: S.String,
    finished: S.NullOr(S.String),
    progress: S.Number,
    resources: DeleteZoneResponseActionResourcesList,
    error: S.NullOr(CreateZoneResponseActionError),
  }),
).annotate({
  identifier: "DeleteZoneResponseAction",
}) as any as S.Schema<DeleteZoneResponseAction>;

export interface DeleteZoneResponse {
  action: DeleteZoneResponseAction;
}
export const DeleteZoneResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: DeleteZoneResponseAction,
  }),
).annotate({
  identifier: "DeleteZoneResponse",
}) as any as S.Schema<DeleteZoneResponse>;

export interface GetZoneRequest {
  /** ID or Name of the Zone. */
  id_or_name: string;
}
export const GetZoneRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id_or_name: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/zones/{id_or_name}", code: 200 })),
).annotate({ identifier: "GetZoneRequest" }) as any as S.Schema<GetZoneRequest>;

export type GetZoneResponseZone = ZonePrimary | ZoneSecondary;
export const GetZoneResponseZone =
  /*@__PURE__*/ S.Unknown as any as S.Schema<GetZoneResponseZone>;

export interface GetZoneResponse {
  zone: GetZoneResponseZone;
}
export const GetZoneResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    zone: GetZoneResponseZone,
  }),
).annotate({
  identifier: "GetZoneResponse",
}) as any as S.Schema<GetZoneResponse>;

export interface GetZoneZonefileRequest {
  /** ID or Name of the Zone. */
  id_or_name: string;
}
export const GetZoneZonefileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id_or_name: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "GET", uri: "/zones/{id_or_name}/zonefile", code: 200 }),
  ),
).annotate({
  identifier: "GetZoneZonefileRequest",
}) as any as S.Schema<GetZoneZonefileRequest>;

export interface GetZoneZonefileResponse {
  /** Generated zone file. Example: ```dns $ORIGIN example.com. $TTL 3600 @ IN SOA hydrogen.ns.hetzner.com. dns.hetzner.com. 2024010100 86400 10800 3600000 3600 @ IN 10800 NS hydrogen.ns.hetzner.com. ; Some comment. @ IN 10800 NS oxygen.ns.hetzner.com. @ IN 10800 NS helium.ns.hetzner.de. ``` */
  zonefile: string;
}
export const GetZoneZonefileResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    zonefile: S.String,
  }),
).annotate({
  identifier: "GetZoneZonefileResponse",
}) as any as S.Schema<GetZoneZonefileResponse>;

/** Mode of the [Zone](#tag/zones). For more information, see [Zone Modes](#tag/zones/zone-modes). */
export type ListZonesRequestMode = "primary" | "secondary";
export const ListZonesRequestMode = /*@__PURE__*/ S.String;

export type ListZonesRequestSortItem =
  | "id"
  | "id:asc"
  | "id:desc"
  | "name"
  | "name:asc"
  | "name:desc"
  | "created"
  | "created:asc"
  | "created:desc";
export const ListZonesRequestSortItem = /*@__PURE__*/ S.String;

export type ListZonesRequestSortList = Array<
  ListZonesRequestSortItem | (string & {})
>;
export const ListZonesRequestSortList = /*@__PURE__*/ S.Array(
  ListZonesRequestSortItem,
) as any as S.Schema<ListZonesRequestSortList>;

export interface ListZonesRequest {
  /** Filter resources by their name. The response will only contain the resources matching exactly the specified name. */
  name?: string;
  /** Filter resources by their mode. The response will only contain the resources matching exactly the specified mode. */
  mode?: ListZonesRequestMode | (string & {});
  /** Filter resources by labels. The response will only contain resources matching the label selector. For more information, see "[Label Selector](#description/label-selector)". */
  label_selector?: string;
  /** Sort resources by field and direction. May be used multiple times. For more information, see "[Sorting](#description/sorting)". */
  sort?: ListZonesRequestSortList;
  /** Page number to return. For more information, see "[Pagination](#description/pagination)". */
  page?: number;
  /** Maximum number of entries returned per page. For more information, see "[Pagination](#description/pagination)". */
  per_page?: number;
}
export const ListZonesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String.pipe(T.Query())),
    mode: S.optional(ListZonesRequestMode.pipe(T.Query())),
    label_selector: S.optional(S.String.pipe(T.Query())),
    sort: S.optional(ListZonesRequestSortList.pipe(T.Query())),
    page: S.optional(S.Number.pipe(T.Query())),
    per_page: S.optional(S.Number.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/zones", code: 200 })),
).annotate({
  identifier: "ListZonesRequest",
}) as any as S.Schema<ListZonesRequest>;

export type ListZonesResponseZonesItem = ZonePrimary | ZoneSecondary;
export const ListZonesResponseZonesItem =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ListZonesResponseZonesItem>;

export type ListZonesResponseZonesList = Array<ListZonesResponseZonesItem>;
export const ListZonesResponseZonesList = /*@__PURE__*/ S.Array(
  ListZonesResponseZonesItem,
) as any as S.Schema<ListZonesResponseZonesList>;

/** See "[Pagination](#description/pagination)" for more information. */
export interface ListZonesResponseMetaPagination {
  /** Current page number. */
  page: number;
  /** Maximum number of entries returned per page. */
  per_page: number;
  /** Page number of the previous page. Can be null if the current page is the first one. */
  previous_page: number | null;
  /** Page number of the next page. Can be null if the current page is the last one. */
  next_page: number | null;
  /** Page number of the last page available. Can be null if the current page is the last one. */
  last_page: number | null;
  /** Total number of entries that exist for this query. Can be null if unknown. */
  total_entries: number | null;
}
export const ListZonesResponseMetaPagination = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    page: S.Number,
    per_page: S.Number,
    previous_page: S.NullOr(S.Number),
    next_page: S.NullOr(S.Number),
    last_page: S.NullOr(S.Number),
    total_entries: S.NullOr(S.Number),
  }),
).annotate({
  identifier: "ListZonesResponseMetaPagination",
}) as any as S.Schema<ListZonesResponseMetaPagination>;

export interface ListZonesResponseMeta {
  /** See "[Pagination](#description/pagination)" for more information. */
  pagination: ListZonesResponseMetaPagination;
}
export const ListZonesResponseMeta = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pagination: ListZonesResponseMetaPagination,
  }),
).annotate({
  identifier: "ListZonesResponseMeta",
}) as any as S.Schema<ListZonesResponseMeta>;

export interface ListZonesResponse {
  zones: ListZonesResponseZonesList;
  meta: ListZonesResponseMeta;
}
export const ListZonesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    zones: ListZonesResponseZonesList,
    meta: ListZonesResponseMeta,
  }),
).annotate({
  identifier: "ListZonesResponse",
}) as any as S.Schema<ListZonesResponse>;

/** User-defined labels (`key/value` pairs) for the Resource. Note that the set of [Labels](#description/labels) provided in the request will overwrite the existing one. For more information, see "[Labels](#description/labels)". */
export type UpdateZoneRequestLabelsMap = { [key: string]: string | undefined };
export const UpdateZoneRequestLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<UpdateZoneRequestLabelsMap>;

export interface UpdateZoneRequest {
  /** ID or Name of the Zone. */
  id_or_name: string;
  /** User-defined labels (`key/value` pairs) for the Resource. Note that the set of [Labels](#description/labels) provided in the request will overwrite the existing one. For more information, see "[Labels](#description/labels)". */
  labels?: UpdateZoneRequestLabelsMap;
}
export const UpdateZoneRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id_or_name: S.String.pipe(T.Label()),
    labels: S.optional(UpdateZoneRequestLabelsMap),
  }).pipe(T.Http({ method: "PUT", uri: "/zones/{id_or_name}", code: 200 })),
).annotate({
  identifier: "UpdateZoneRequest",
}) as any as S.Schema<UpdateZoneRequest>;

export type UpdateZoneResponseZone = ZonePrimary | ZoneSecondary;
export const UpdateZoneResponseZone =
  /*@__PURE__*/ S.Unknown as any as S.Schema<UpdateZoneResponseZone>;

export interface UpdateZoneResponse {
  zone: UpdateZoneResponseZone;
}
export const UpdateZoneResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    zone: UpdateZoneResponseZone,
  }),
).annotate({
  identifier: "UpdateZoneResponse",
}) as any as S.Schema<UpdateZoneResponse>;

export type CreateZoneError = HetznerOpError;
/** Create a Zone Creates a [Zone](#tag/zones). A default `SOA` and three `NS` resource records with the assigned Hetzner nameservers are created automatically. */
export const createZone: API.OperationMethod<
  CreateZoneRequest,
  CreateZoneResponse,
  CreateZoneError,
  HetznerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateZoneRequest,
  output: CreateZoneResponse,
  errors: [UnknownHetznerError],
  protocol: HetznerProtocol,
  retry: Retry.Retry,
}));

export type DeleteZoneError = HetznerOpError;
/** Delete a Zone Deletes a [Zone](#tag/zones). */
export const deleteZone: API.OperationMethod<
  DeleteZoneRequest,
  DeleteZoneResponse,
  DeleteZoneError,
  HetznerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteZoneRequest,
  output: DeleteZoneResponse,
  errors: [UnknownHetznerError],
  protocol: HetznerProtocol,
  retry: Retry.Retry,
}));

export type GetZoneError = HetznerOpError;
/** Get a Zone Returns a single [Zone](#tag/zones). */
export const getZone: API.OperationMethod<
  GetZoneRequest,
  GetZoneResponse,
  GetZoneError,
  HetznerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetZoneRequest,
  output: GetZoneResponse,
  errors: [UnknownHetznerError],
  protocol: HetznerProtocol,
  retry: Retry.Retry,
}));

export type GetZoneZonefileError = HetznerOpError;
/** Export a Zone file Returns a generated [Zone](#tag/zones) file in BIND (RFC [1034](https://datatracker.ietf.org/doc/html/rfc1034)/[1035](https://datatracker.ietf.org/doc/html/rfc1035)) format. Only applicable for [Zones](#tag/zones) in primary mode. #### Operation specific errors */
export const getZoneZonefile: API.OperationMethod<
  GetZoneZonefileRequest,
  GetZoneZonefileResponse,
  GetZoneZonefileError,
  HetznerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetZoneZonefileRequest,
  output: GetZoneZonefileResponse,
  errors: [UnknownHetznerError],
  protocol: HetznerProtocol,
  retry: Retry.Retry,
}));

export type ListZonesError = HetznerOpError;
/** List Zones Returns all [Zones](#tag/zones). Use the provided URI parameters to modify the result. */
export const listZones: API.PaginatedOperationMethod<
  ListZonesRequest,
  ListZonesResponse,
  ListZonesError,
  HetznerOpContext,
  ListZonesResponseZonesItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListZonesRequest,
    output: ListZonesResponse,
    errors: [UnknownHetznerError],
    protocol: HetznerProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "page",
      inputToken: "page",
      outputToken: "meta.pagination.next_page",
      items: "zones",
      pageSize: "per_page",
    } as const,
  }),
  paginatePageNumber,
) as any;

export type UpdateZoneError = HetznerOpError;
/** Update a Zone Updates a [Zone](#tag/zones). To modify resource record sets ([RRSets](#tag/zone-rrsets)), use the [RRSet Actions endpoints](#tag/zone-rrset-actions). */
export const updateZone: API.OperationMethod<
  UpdateZoneRequest,
  UpdateZoneResponse,
  UpdateZoneError,
  HetznerOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateZoneRequest,
  output: UpdateZoneResponse,
  errors: [UnknownHetznerError],
  protocol: HetznerProtocol,
  retry: Retry.Retry,
}));
