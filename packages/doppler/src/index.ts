/**
 * @distilled.cloud/doppler — Doppler SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy model
 * in `.generated-specs` (written by `scripts/convert.ts` from the OpenAPI
 * spec assembled from https://docs.doppler.com/llms.txt). Everything else
 * in this folder is hand-written.
 *
 * @example
 * ```ts
 * import * as Doppler from "@distilled.cloud/doppler";
 *
 * const projects = yield* Doppler.Services.doppler.projectsList({});
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  DopplerProtocol,
  type DopplerOpError,
  type DopplerOpContext,
} from "./protocol.ts";
export { paginateCursor } from "./pagination.ts";
export * as Retry from "./retry.ts";
export * as Services from "./services/index.ts";
// Operations (and their request/response types) are importable straight off
// the package root. The service-local typed error classes shadow the
// same-named shared ones from ./errors.ts — the ops actually raise the
// service-local classes.
export * from "./services/doppler.ts";
export { BadRequest, Conflict, NotFound } from "./services/doppler.ts";
