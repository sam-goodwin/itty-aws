/**
 * @distilled.cloud/customerio — Customer.io SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy models
 * in `.generated-specs` (written by `scripts/convert.ts` from the OpenAPI
 * specs). Everything else in this folder is hand-written.
 *
 * @example
 * ```ts
 * import * as Customerio from "@distilled.cloud/customerio";
 *
 * const campaigns = yield* Customerio.Services.customerio.listCampaigns({});
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  CustomerioProtocol,
  type CustomerioOpError,
  type CustomerioOpContext,
} from "./protocol.ts";
export { paginateCursor } from "./pagination.ts";
export * as Retry from "./retry.ts";
export * as Services from "./services/index.ts";
// Operations (and their request/response types) are importable straight off
// the package root. The service-local typed error classes shadow the
// same-named shared ones from ./errors.ts — the ops actually raise the
// service-local classes.
export * from "./services/customerio.ts";
export {
  BadRequest,
  Conflict,
  Forbidden,
  NotFound,
  UnprocessableEntity,
} from "./services/customerio.ts";
