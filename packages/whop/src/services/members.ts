// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface ListMemberLogsRequest {
  /** Member ID (`mber_` tag). */
  id: string;
  /** Number of log entries to return from the start of the window. */
  first?: number;
  /** Cursor to paginate forwards from. */
  after?: string;
  /** Number of log entries to return from the end of the window. */
  last?: number;
  /** Cursor to paginate backwards from. */
  before?: string;
}
export const ListMemberLogsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/members/{id}/logs", code: 200 })),
).annotate({
  identifier: "ListMemberLogsRequest",
}) as any as S.Schema<ListMemberLogsRequest>;

export interface ListMemberLogsResponseDataItemActor {
  id: string;
  name?: string | null;
  username?: string | null;
}
export const ListMemberLogsResponseDataItemActor = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.optional(S.NullOr(S.String)),
    username: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "ListMemberLogsResponseDataItemActor",
}) as any as S.Schema<ListMemberLogsResponseDataItemActor>;

export interface ListMemberLogsResponseDataItem {
  action: string | null;
  actor: ListMemberLogsResponseDataItemActor | null;
  created_at: string;
}
export const ListMemberLogsResponseDataItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: S.NullOr(S.String),
    actor: S.NullOr(ListMemberLogsResponseDataItemActor),
    created_at: S.String,
  }),
).annotate({
  identifier: "ListMemberLogsResponseDataItem",
}) as any as S.Schema<ListMemberLogsResponseDataItem>;

export type ListMemberLogsResponseDataList =
  Array<ListMemberLogsResponseDataItem>;
export const ListMemberLogsResponseDataList = /*@__PURE__*/ S.Array(
  ListMemberLogsResponseDataItem,
) as any as S.Schema<ListMemberLogsResponseDataList>;

export interface ListMemberLogsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListMemberLogsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListMemberLogsResponsePageInfo",
}) as any as S.Schema<ListMemberLogsResponsePageInfo>;

export interface ListMemberLogsResponse {
  data: ListMemberLogsResponseDataList;
  page_info: ListMemberLogsResponsePageInfo;
}
export const ListMemberLogsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListMemberLogsResponseDataList,
    page_info: ListMemberLogsResponsePageInfo,
  }),
).annotate({
  identifier: "ListMemberLogsResponse",
}) as any as S.Schema<ListMemberLogsResponse>;

export type ListMembersRequestAccessLevel = "no_access" | "admin" | "customer";
export const ListMembersRequestAccessLevel = /*@__PURE__*/ S.String;

export type ListMembersRequestStatus = "joined" | "left";
export const ListMembersRequestStatus = /*@__PURE__*/ S.String;

export type ListMembersRequestOrder =
  | "created_at"
  | "joined_at"
  | "last_accessed_at"
  | "usd_total_spent";
export const ListMembersRequestOrder = /*@__PURE__*/ S.String;

export type ListMembersRequestDirection = "asc" | "desc";
export const ListMembersRequestDirection = /*@__PURE__*/ S.String;

export interface ListMembersRequest {
  /** The account to list members for (`biz_` tag). Defaults to the account the credential acts as. */
  account_id?: string;
  /** Filter by what the member can reach on the account. */
  access_level?: ListMembersRequestAccessLevel | (string & {});
  /** Filter by whether the member is still part of the account. */
  status?: ListMembersRequestStatus | (string & {});
  /** Search members by name or username. An exact email address also matches when the credential holds the member:email:read scope. */
  query?: string;
  /** Only members who joined after this ISO 8601 timestamp. */
  created_after?: string;
  /** Only members who joined before this ISO 8601 timestamp. */
  created_before?: string;
  /** Sort field. */
  order?: ListMembersRequestOrder | (string & {});
  /** Sort direction. */
  direction?: ListMembersRequestDirection | (string & {});
  /** Number of members to return from the start of the window. */
  first?: number;
  /** Cursor to paginate forwards from. */
  after?: string;
  /** Number of members to return from the end of the window. */
  last?: number;
  /** Cursor to paginate backwards from. */
  before?: string;
}
export const ListMembersRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    access_level: S.optional(ListMembersRequestAccessLevel.pipe(T.Query())),
    status: S.optional(ListMembersRequestStatus.pipe(T.Query())),
    query: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListMembersRequestOrder.pipe(T.Query())),
    direction: S.optional(ListMembersRequestDirection.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/members", code: 200 })),
).annotate({
  identifier: "ListMembersRequest",
}) as any as S.Schema<ListMembersRequest>;

/** What the member can reach on the account: `customer` for paying members, `admin` for team members, `no_access` once every grant has lapsed. */
export type MemberAccessLevel = "no_access" | "admin" | "customer";
export const MemberAccessLevel = /*@__PURE__*/ S.String;

/** `joined` while the member is part of the account, `left` after they leave. */
export type MemberStatus = "joined" | "left";
export const MemberStatus = /*@__PURE__*/ S.String;

export interface UserProfilePicture {
  /** Avatar image URL. Always present — a generated placeholder when the user set no picture. */
  url: string;
}
export const UserProfilePicture = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
  }),
).annotate({
  identifier: "UserProfilePicture",
}) as any as S.Schema<UserProfilePicture>;

export interface UserSummary {
  /** User ID, prefixed `user_`. */
  id: string;
  /** Display name. */
  name: string | null;
  /** Avatar wrapper; its `url` is always present, using a generated placeholder when the user set no picture. */
  profile_picture: UserProfilePicture;
  /** Public username. */
  username: string;
}
export const UserSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    profile_picture: UserProfilePicture,
    username: S.String,
  }),
).annotate({ identifier: "UserSummary" }) as any as S.Schema<UserSummary>;

export interface Member {
  /** What the member can reach on the account: `customer` for paying members, `admin` for team members, `no_access` once every grant has lapsed. */
  access_level: MemberAccessLevel;
  /** The account this member belongs to, prefixed `biz_`. */
  account_id: string;
  /** When the member record was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Member ID, prefixed `mber_`. */
  id: string;
  /** When the member first joined the account, as an ISO 8601 timestamp. */
  joined_at: string;
  /** When the member last opened the account's content, as an ISO 8601 timestamp. `null` if they never have. */
  last_accessed_at: string | null;
  /** `joined` while the member is part of the account, `left` after they leave. */
  status: MemberStatus;
  /** The user behind this member. `null` when the buyer is another business rather than a person. */
  user: UserSummary | null;
}
export const Member = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    access_level: MemberAccessLevel,
    account_id: S.String,
    created_at: S.String,
    id: S.String,
    joined_at: S.String,
    last_accessed_at: S.NullOr(S.String),
    status: MemberStatus,
    user: S.NullOr(UserSummary),
  }),
).annotate({ identifier: "Member" }) as any as S.Schema<Member>;

export type ListMembersResponseDataList = Array<Member>;
export const ListMembersResponseDataList = /*@__PURE__*/ S.Array(
  Member,
) as any as S.Schema<ListMembersResponseDataList>;

export type ListMembersResponsePageInfo = ListMemberLogsResponsePageInfo;
export const ListMembersResponsePageInfo = ListMemberLogsResponsePageInfo;

export interface ListMembersResponse {
  data: ListMembersResponseDataList;
  page_info: ListMemberLogsResponsePageInfo;
}
export const ListMembersResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListMembersResponseDataList,
    page_info: ListMemberLogsResponsePageInfo,
  }),
).annotate({
  identifier: "ListMembersResponse",
}) as any as S.Schema<ListMembersResponse>;

export interface RetrieveMemberRequest {
  /** Member ID (`mber_` tag). */
  id: string;
}
export const RetrieveMemberRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/members/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveMemberRequest",
}) as any as S.Schema<RetrieveMemberRequest>;

export type ListMemberLogsError = Forbidden | NotFound | WhopOpError;
/** List Member Logs Lists activity for a member and all of their non-drafted memberships, most recent first. */
export const listMemberLogs: API.PaginatedOperationMethod<
  ListMemberLogsRequest,
  ListMemberLogsResponse,
  ListMemberLogsError,
  WhopOpContext,
  ListMemberLogsResponseDataItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListMemberLogsRequest,
    output: ListMemberLogsResponse,
    errors: [Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ListMembersError = BadRequest | Forbidden | WhopOpError;
/** List Members Lists the members of an account. A member is one buyer's relationship with the account, regardless of how many memberships they hold. */
export const listMembers: API.PaginatedOperationMethod<
  ListMembersRequest,
  ListMembersResponse,
  ListMembersError,
  WhopOpContext,
  Member
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListMembersRequest,
    output: ListMembersResponse,
    errors: [BadRequest, Forbidden],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveMemberError = Forbidden | NotFound | WhopOpError;
/** Retrieve Member Retrieves a member by ID. Accessible to the account and to the member's own user. */
export const retrieveMember: API.OperationMethod<
  RetrieveMemberRequest,
  Member,
  RetrieveMemberError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveMemberRequest,
  output: Member,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
