// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** An array of permission scopes to grant to the access token. If empty or omitted, all permissions from the authenticating credential are inherited. Must be a subset of the credential's permissions. */
export type CreateAccessTokenRequestScopedActionsList = Array<string>;
export const CreateAccessTokenRequestScopedActionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateAccessTokenRequestScopedActionsList>;

export interface CreateAccessTokenRequest {
  /** The unique identifier of the company to generate the token for, starting with 'biz_'. The API key must have permission to access this company. */
  company_id?: string | null;
  /** The expiration timestamp for the access token. Defaults to 1 hour from now, with a maximum of 3 hours. */
  expires_at?: string | null;
  /** An array of permission scopes to grant to the access token. If empty or omitted, all permissions from the authenticating credential are inherited. Must be a subset of the credential's permissions. */
  scoped_actions?: CreateAccessTokenRequestScopedActionsList | null;
  /** The unique identifier of the user to generate the token for, starting with 'user_'. The API key must have permission to access this user. */
  user_id?: string | null;
}
export const CreateAccessTokenRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    company_id: S.optional(S.NullOr(S.String)),
    expires_at: S.optional(S.NullOr(S.String)),
    scoped_actions: S.optional(
      S.NullOr(CreateAccessTokenRequestScopedActionsList),
    ),
    user_id: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "POST", uri: "/access_tokens", code: 200 })),
).annotate({
  identifier: "CreateAccessTokenRequest",
}) as any as S.Schema<CreateAccessTokenRequest>;

/** A short-lived access token used to authenticate API requests on behalf of a user. */
export interface AccessToken {
  /** The timestamp after which this access token is no longer valid and must be refreshed. */
  expires_at: string;
  /** The signed JWT access token string to include in API request Authorization headers. */
  token: string;
}
export const AccessToken = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    expires_at: S.String,
    token: S.String,
  }),
).annotate({ identifier: "AccessToken" }) as any as S.Schema<AccessToken>;

export type CreateAccessTokenError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create access token [Legacy API — https://docs.whop.com/api-reference] Create a short-lived access token for authenticating API requests. When using API key authentication, provide company_id or user_id. When using OAuth, the user is derived from the token. Use this token with Whop's web and mobile embedded components. */
export const createAccessToken: API.OperationMethod<
  CreateAccessTokenRequest,
  AccessToken,
  CreateAccessTokenError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAccessTokenRequest,
  output: AccessToken,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
