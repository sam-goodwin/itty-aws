// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** Scope the report to these ad campaigns (max 100); stats are summed across them. Mutually exclusive with `companyId`, `adGroupIds`, and `adIds`. */
export type RetrieveAdReportRequestAdCampaignIdsList = Array<string>;
export const RetrieveAdReportRequestAdCampaignIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<RetrieveAdReportRequestAdCampaignIdsList>;

/** Scope the report to these ad groups (max 100); stats are summed across them. Mutually exclusive with `companyId`, `adCampaignIds`, and `adIds`. */
export type RetrieveAdReportRequestAdGroupIdsList = Array<string>;
export const RetrieveAdReportRequestAdGroupIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<RetrieveAdReportRequestAdGroupIdsList>;

/** Scope the report to these ads (max 100); stats are summed across them. Mutually exclusive with `companyId`, `adCampaignIds`, and `adGroupIds`. */
export type RetrieveAdReportRequestAdIdsList = Array<string>;
export const RetrieveAdReportRequestAdIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<RetrieveAdReportRequestAdIdsList>;

/** Entity level to group an ad report by. */
export type AdReportBreakdownLevels = "campaign" | "ad_group" | "ad";
export const AdReportBreakdownLevels = /*@__PURE__*/ S.String;

/** Bucket size for external ad stat rows. */
export type Granularities = "hourly" | "daily" | "weekly" | "monthly";
export const Granularities = /*@__PURE__*/ S.String;

export interface RetrieveAdReportRequest {
  ad_campaign_ids?: RetrieveAdReportRequestAdCampaignIdsList;
  ad_group_ids?: RetrieveAdReportRequestAdGroupIdsList;
  ad_ids?: RetrieveAdReportRequestAdIdsList;
  breakdown?: AdReportBreakdownLevels | (string & {});
  company_id?: string;
  currency?: string;
  from: string;
  granularity?: Granularities | (string & {});
  to: string;
}
export const RetrieveAdReportRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ad_campaign_ids: S.optional(
      RetrieveAdReportRequestAdCampaignIdsList.pipe(T.Query()),
    ),
    ad_group_ids: S.optional(
      RetrieveAdReportRequestAdGroupIdsList.pipe(T.Query()),
    ),
    ad_ids: S.optional(RetrieveAdReportRequestAdIdsList.pipe(T.Query())),
    breakdown: S.optional(AdReportBreakdownLevels.pipe(T.Query())),
    company_id: S.optional(S.String.pipe(T.Query())),
    currency: S.optional(S.String.pipe(T.Query())),
    from: S.String.pipe(T.Query()),
    granularity: S.optional(Granularities.pipe(T.Query())),
    to: S.String.pipe(T.Query()),
  }).pipe(T.Http({ method: "GET", uri: "/ad_reports", code: 200 })),
).annotate({
  identifier: "RetrieveAdReportRequest",
}) as any as S.Schema<RetrieveAdReportRequest>;

/** Types of optimization results tracked from external ad platforms */
export type ResultLabelKeys =
  | "app_installs"
  | "messaging_conversations_started"
  | "post_engagement"
  | "event_responses"
  | "impressions"
  | "website_purchases"
  | "landing_page_views"
  | "leads"
  | "link_clicks"
  | "quality_calls"
  | "appointments_booked"
  | "messaging_purchases"
  | "page_likes"
  | "instagram_profile_visits"
  | "reach"
  | "reminders_set"
  | "new_subscribers"
  | "video_views"
  | "registrations"
  | "content_views"
  | "searches"
  | "adds_to_cart"
  | "adds_to_wishlist"
  | "adds_of_payment_info"
  | "checkouts_initiated"
  | "website_schedules"
  | "website_submit_applications"
  | "website_trials_started"
  | "website_subscriptions"
  | "website_contacts"
  | "website_donations"
  | "website_find_locations"
  | "website_product_customizations"
  | "custom";
export const ResultLabelKeys = /*@__PURE__*/ S.String;

/** The available currencies on the platform */
export type Currencies =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const Currencies = /*@__PURE__*/ S.String;

/** Per-bucket ad performance for an ad campaign, ad group, or ad. Bucket grain is set by the `ad_report` query's `granularity` argument. */
export interface AdReportBreakdownItemGranularityItem {
  /** The bucket's start time as a real UTC instant. `(statDate, statHour)` resolved in the ad account's reporting timezone — render this in the viewer's local timezone. */
  bucket_start: string;
  /** Clicks in this bucket. */
  clicks: number;
  /** The bucket size of this row (`hourly`, `daily`, `weekly`, or `monthly`). */
  granularity: Granularities;
  /** Impressions in this bucket. */
  impressions: number;
  /** Unique users reached in this bucket. Always `0` for hourly rows (Meta does not return reach at hourly grain). */
  reach: number;
  /** Count of the primary optimization result in this bucket. */
  result_count: number | null;
  /** The type of optimization result represented by `resultCount`. */
  result_label_key: ResultLabelKeys | null;
  /** Advertiser-defined label for the result when `resultLabelKey` is `custom`. */
  result_label_override: string | null;
  /** Charged spend in this bucket in the requested reporting currency — the amount billed including platform fees, not the platform-side net spend. */
  spend: number;
  /** Currency of the `spend` value. */
  spend_currency: Currencies;
  /** The date these stats cover (midnight UTC). For hourly rows, see `statHour` and `bucketStart`. */
  stat_date: string;
  /** Hour of the day in the ad account's reporting timezone (0-23). `null` for daily rows. */
  stat_hour: number | null;
}
export const AdReportBreakdownItemGranularityItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      bucket_start: S.String,
      clicks: S.Number,
      granularity: Granularities,
      impressions: S.Number,
      reach: S.Number,
      result_count: S.NullOr(S.Number),
      result_label_key: S.NullOr(ResultLabelKeys),
      result_label_override: S.NullOr(S.String),
      spend: S.Number,
      spend_currency: Currencies,
      stat_date: S.String,
      stat_hour: S.NullOr(S.Number),
    }),
).annotate({
  identifier: "AdReportBreakdownItemGranularityItem",
}) as any as S.Schema<AdReportBreakdownItemGranularityItem>;

/** Per-bucket time series for this entity over the date range, ordered ascending by `bucketStart`. `null` when the `granularity` arg on `adReport` is omitted; otherwise contains rows at the requested grain (`daily` or `hourly`). */
export type AdReportBreakdownItemGranularityList =
  Array<AdReportBreakdownItemGranularityItem>;
export const AdReportBreakdownItemGranularityList = /*@__PURE__*/ S.Array(
  AdReportBreakdownItemGranularityItem,
) as any as S.Schema<AdReportBreakdownItemGranularityList>;

/** Aggregate totals and rates for this entity over the date range. */
export interface AdReportBreakdownItemSummary {
  /** Click-through rate (clicks / impressions). */
  click_through_rate: number;
  /** Total clicks over the date range. */
  clicks: number;
  /** Cost per click in the requested reporting currency. */
  cost_per_click: number;
  /** Cost per thousand impressions in the requested reporting currency. */
  cost_per_mille: number | null;
  /** Spend divided by `resultCount`. Null when there are no results. */
  cost_per_result: number | null;
  /** Average number of times each reached user saw an ad. */
  frequency: number | null;
  /** Total impressions over the date range. */
  impressions: number;
  /** Unique users reached, deduplicated by the external ad platform. */
  reach: number;
  /** Count of the campaign's primary optimization result (purchases, clicks, etc.) — see `resultLabelKey`. */
  result_count: number | null;
  /** The type of optimization result represented by `resultCount`. */
  result_label_key: ResultLabelKeys | null;
  /** Advertiser-defined label for the result when `resultLabelKey` is `custom`. */
  result_label_override: string | null;
  /** Alias for `purchaseReturnOnAdSpend` — return on ad spend for purchases, as reported by the external ad platform. */
  return_on_ad_spend: number | null;
  /** Total spend over the date range in the requested reporting currency. */
  spend: number;
  /** Currency of the `spend` value. Matches the requested `currency` when set. */
  spend_currency: Currencies | null;
}
export const AdReportBreakdownItemSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    click_through_rate: S.Number,
    clicks: S.Number,
    cost_per_click: S.Number,
    cost_per_mille: S.NullOr(S.Number),
    cost_per_result: S.NullOr(S.Number),
    frequency: S.NullOr(S.Number),
    impressions: S.Number,
    reach: S.Number,
    result_count: S.NullOr(S.Number),
    result_label_key: S.NullOr(ResultLabelKeys),
    result_label_override: S.NullOr(S.String),
    return_on_ad_spend: S.NullOr(S.Number),
    spend: S.Number,
    spend_currency: S.NullOr(Currencies),
  }),
).annotate({
  identifier: "AdReportBreakdownItemSummary",
}) as any as S.Schema<AdReportBreakdownItemSummary>;

/** Per-entity ad performance row. Returned when the `breakdown` arg on `adReport` is set. */
export interface AdReportBreakdownItem {
  /** Per-bucket time series for this entity over the date range, ordered ascending by `bucketStart`. `null` when the `granularity` arg on `adReport` is omitted; otherwise contains rows at the requested grain (`daily` or `hourly`). */
  granularity: AdReportBreakdownItemGranularityList | null;
  /** Tag of the entity (ad campaign, ad group, or ad). */
  id: string;
  /** The entity level of this row — matches the `breakdown` arg. */
  level: AdReportBreakdownLevels;
  /** Display name of the entity, when available. */
  name: string | null;
  /** Aggregate totals and rates for this entity over the date range. */
  summary: AdReportBreakdownItemSummary;
}
export const AdReportBreakdownItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    granularity: S.NullOr(AdReportBreakdownItemGranularityList),
    id: S.String,
    level: AdReportBreakdownLevels,
    name: S.NullOr(S.String),
    summary: AdReportBreakdownItemSummary,
  }),
).annotate({
  identifier: "AdReportBreakdownItem",
}) as any as S.Schema<AdReportBreakdownItem>;

/** Per-entity rows over the date range. `null` when the `breakdown` arg on `adReport` is omitted; otherwise contains one row per ad campaign, ad group, or ad inside the requested scope at the requested level. */
export type AdReportBreakdownList = Array<AdReportBreakdownItem>;
export const AdReportBreakdownList = /*@__PURE__*/ S.Array(
  AdReportBreakdownItem,
) as any as S.Schema<AdReportBreakdownList>;

/** Per-bucket ad performance for an ad campaign, ad group, or ad. Bucket grain is set by the `ad_report` query's `granularity` argument. */
export type AdReportGranularityItem = AdReportBreakdownItemGranularityItem;
export const AdReportGranularityItem = AdReportBreakdownItemGranularityItem;

/** Per-bucket time series over the date range, ordered ascending by `bucketStart`. `null` when the `granularity` arg on `adReport` is omitted; otherwise contains rows at the requested grain (`daily` or `hourly`). */
export type AdReportGranularityList =
  Array<AdReportBreakdownItemGranularityItem>;
export const AdReportGranularityList = /*@__PURE__*/ S.Array(
  AdReportBreakdownItemGranularityItem,
) as any as S.Schema<AdReportGranularityList>;

/** Aggregate totals and rates over the date range. */
export type AdReportSummary = AdReportBreakdownItemSummary;
export const AdReportSummary = AdReportBreakdownItemSummary;

/** An ads performance report. Always returns a summary. The `granularity` field contains a per-bucket time series when the `granularity` arg is set; the `breakdown` field contains per-entity rows when the `breakdown` arg is set. */
export interface AdReport {
  /** Per-entity rows over the date range. `null` when the `breakdown` arg on `adReport` is omitted; otherwise contains one row per ad campaign, ad group, or ad inside the requested scope at the requested level. */
  breakdown: AdReportBreakdownList | null;
  /** Per-bucket time series over the date range, ordered ascending by `bucketStart`. `null` when the `granularity` arg on `adReport` is omitted; otherwise contains rows at the requested grain (`daily` or `hourly`). */
  granularity: AdReportGranularityList | null;
  /** Aggregate totals and rates over the date range. */
  summary: AdReportBreakdownItemSummary;
}
export const AdReport = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    breakdown: S.NullOr(AdReportBreakdownList),
    granularity: S.NullOr(AdReportGranularityList),
    summary: AdReportBreakdownItemSummary,
  }),
).annotate({ identifier: "AdReport" }) as any as S.Schema<AdReport>;

export type RetrieveAdReportError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve ad report [Legacy API — https://docs.whop.com/api-reference] Performance report for a company, ad campaigns, ad groups, or ads. Always returns aggregate `summary` totals summed across the scope. Set `granularity` to additionally get a time series, or set `breakdown` (`campaign`/`ad_group`/`ad`) to additionally get per-entity rows inside the requested scope. Exactly one of `companyId`, `adCampaignIds`, `adGroupIds`, or `adIds` must be provided. Required permissions: - `ad_campaign:stats:read` */
export const retrieveAdReport: API.OperationMethod<
  RetrieveAdReportRequest,
  AdReport,
  RetrieveAdReportError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveAdReportRequest,
  output: AdReport,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
