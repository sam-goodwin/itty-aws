// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** A JSON object of custom metadata to attach to the lead for tracking purposes. */
export type CreateLeadRequestMetadataMap = {
  [key: string]: unknown | undefined;
};
export const CreateLeadRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateLeadRequestMetadataMap>;

export interface CreateLeadRequest {
  /** The unique identifier of the company to create the lead for, starting with 'biz_'. */
  company_id: string;
  /** A JSON object of custom metadata to attach to the lead for tracking purposes. */
  metadata?: CreateLeadRequestMetadataMap | null;
  /** The unique identifier of the product the lead is interested in, starting with 'prod_'. */
  product_id?: string | null;
  /** The referral URL that brought the lead to the company, such as 'https://example.com/landing'. */
  referrer?: string | null;
  /** The unique identifier of the user to record as the lead. If authenticated as a user, that user is used automatically. */
  user_id?: string | null;
}
export const CreateLeadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    company_id: S.String,
    metadata: S.optional(S.NullOr(CreateLeadRequestMetadataMap)),
    product_id: S.optional(S.NullOr(S.String)),
    referrer: S.optional(S.NullOr(S.String)),
    user_id: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "POST", uri: "/leads", code: 200 })),
).annotate({
  identifier: "CreateLeadRequest",
}) as any as S.Schema<CreateLeadRequest>;

/** The company member record if this lead has converted into a paying customer. Null if the lead has not converted. */
export interface LeadMember {
  /** The unique identifier for the company member. */
  id: string;
}
export const LeadMember = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({ identifier: "LeadMember" }) as any as S.Schema<LeadMember>;

/** Custom key-value pairs attached to this lead. Null if no metadata was provided. */
export type LeadMetadataMap = { [key: string]: unknown | undefined };
export const LeadMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<LeadMetadataMap>;

/** The product the lead expressed interest in. Null if the lead is not associated with a specific product. */
export interface LeadProduct {
  /** The unique identifier for the product. */
  id: string;
  /** The display name of the product shown to customers on the product page and in search results. */
  title: string;
}
export const LeadProduct = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({ identifier: "LeadProduct" }) as any as S.Schema<LeadProduct>;

/** The user account associated with this lead. */
export interface LeadUser {
  /** The user's email address. Requires the member:email:read permission to access. Null if not authorized. */
  email: string | null;
  /** The unique identifier for the user. */
  id: string;
  /** The user's display name shown on their public profile. */
  name: string | null;
  /** The user's unique username shown on their public profile. */
  username: string;
}
export const LeadUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email: S.NullOr(S.String),
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({ identifier: "LeadUser" }) as any as S.Schema<LeadUser>;

/** A prospective customer who has expressed interest in a company or product but has not yet purchased. */
export interface Lead {
  /** The datetime the lead was created. */
  created_at: string;
  /** The unique identifier for the lead. */
  id: string;
  /** The company member record if this lead has converted into a paying customer. Null if the lead has not converted. */
  member: LeadMember | null;
  /** Custom key-value pairs attached to this lead. Null if no metadata was provided. */
  metadata: LeadMetadataMap | null;
  /** The product the lead expressed interest in. Null if the lead is not associated with a specific product. */
  product: LeadProduct | null;
  /** The URL of the page that referred this lead to the company. Null if no referrer was captured. */
  referrer: string | null;
  /** The datetime the lead was last updated. */
  updated_at: string;
  /** The user account associated with this lead. */
  user: LeadUser;
}
export const Lead = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    created_at: S.String,
    id: S.String,
    member: S.NullOr(LeadMember),
    metadata: S.NullOr(LeadMetadataMap),
    product: S.NullOr(LeadProduct),
    referrer: S.NullOr(S.String),
    updated_at: S.String,
    user: LeadUser,
  }),
).annotate({ identifier: "Lead" }) as any as S.Schema<Lead>;

/** Filter leads to only those associated with these specific product identifiers. */
export type ListLeadRequestProductIdsList = Array<string>;
export const ListLeadRequestProductIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListLeadRequestProductIdsList>;

export interface ListLeadRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  company_id: string;
  created_after?: string;
  created_before?: string;
  product_ids?: ListLeadRequestProductIdsList;
}
export const ListLeadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    company_id: S.String.pipe(T.Query()),
    created_after: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    product_ids: S.optional(ListLeadRequestProductIdsList.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/leads", code: 200 })),
).annotate({
  identifier: "ListLeadRequest",
}) as any as S.Schema<ListLeadRequest>;

/** The company member record if this lead has converted into a paying customer. Null if the lead has not converted. */
export type LeadListItemMember = LeadMember;
export const LeadListItemMember = LeadMember;

/** Custom key-value pairs attached to this lead. Null if no metadata was provided. */
export type LeadListItemMetadataMap = { [key: string]: unknown | undefined };
export const LeadListItemMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<LeadListItemMetadataMap>;

/** The product the lead expressed interest in. Null if the lead is not associated with a specific product. */
export type LeadListItemProduct = LeadProduct;
export const LeadListItemProduct = LeadProduct;

/** The user account associated with this lead. */
export type LeadListItemUser = LeadUser;
export const LeadListItemUser = LeadUser;

/** A prospective customer who has expressed interest in a company or product but has not yet purchased. */
export interface LeadListItem {
  /** The datetime the lead was created. */
  created_at: string;
  /** The unique identifier for the lead. */
  id: string;
  /** The company member record if this lead has converted into a paying customer. Null if the lead has not converted. */
  member: LeadMember | null;
  /** Custom key-value pairs attached to this lead. Null if no metadata was provided. */
  metadata: LeadListItemMetadataMap | null;
  /** The product the lead expressed interest in. Null if the lead is not associated with a specific product. */
  product: LeadProduct | null;
  /** The URL of the page that referred this lead to the company. Null if no referrer was captured. */
  referrer: string | null;
  /** The datetime the lead was last updated. */
  updated_at: string;
  /** The user account associated with this lead. */
  user: LeadUser;
}
export const LeadListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    created_at: S.String,
    id: S.String,
    member: S.NullOr(LeadMember),
    metadata: S.NullOr(LeadListItemMetadataMap),
    product: S.NullOr(LeadProduct),
    referrer: S.NullOr(S.String),
    updated_at: S.String,
    user: LeadUser,
  }),
).annotate({ identifier: "LeadListItem" }) as any as S.Schema<LeadListItem>;

/** A list of nodes. */
export type ListLeadResponseDataList = Array<LeadListItem>;
export const ListLeadResponseDataList = /*@__PURE__*/ S.Array(
  LeadListItem,
) as any as S.Schema<ListLeadResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListLeadResponse {
  /** A list of nodes. */
  data: ListLeadResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListLeadResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListLeadResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListLeadResponse",
}) as any as S.Schema<ListLeadResponse>;

export interface RetrieveLeadRequest {
  /** The unique identifier of the lead to retrieve. */
  id: string;
}
export const RetrieveLeadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/leads/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveLeadRequest",
}) as any as S.Schema<RetrieveLeadRequest>;

/** A JSON object of custom metadata to set on the lead, replacing any existing metadata. */
export type UpdateLeadRequestMetadataMap = {
  [key: string]: unknown | undefined;
};
export const UpdateLeadRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<UpdateLeadRequestMetadataMap>;

export interface UpdateLeadRequest {
  /** The unique identifier of the lead to update, starting with 'lead_'. */
  id: string;
  /** A JSON object of custom metadata to set on the lead, replacing any existing metadata. */
  metadata?: UpdateLeadRequestMetadataMap | null;
  /** The updated referral URL for the lead, such as 'https://example.com/landing'. */
  referrer?: string | null;
}
export const UpdateLeadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    metadata: S.optional(S.NullOr(UpdateLeadRequestMetadataMap)),
    referrer: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "PATCH", uri: "/leads/{id}", code: 200 })),
).annotate({
  identifier: "UpdateLeadRequest",
}) as any as S.Schema<UpdateLeadRequest>;

export type CreateLeadError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create lead [Legacy API — https://docs.whop.com/api-reference] Record a new lead for a company, capturing a potential customer's interest in a specific product. Required permissions: - `lead:manage` - `member:email:read` - `access_pass:basic:read` - `member:basic:read` */
export const createLead: API.OperationMethod<
  CreateLeadRequest,
  Lead,
  CreateLeadError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateLeadRequest,
  output: Lead,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListLeadError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List leads [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of leads for a company, with optional filtering by product and creation date. Required permissions: - `lead:basic:read` - `member:email:read` - `access_pass:basic:read` - `member:basic:read` */
export const listLead: API.PaginatedOperationMethod<
  ListLeadRequest,
  ListLeadResponse,
  ListLeadError,
  WhopOpContext,
  LeadListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListLeadRequest,
    output: ListLeadResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveLeadError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve lead [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing lead. Required permissions: - `lead:basic:read` - `member:email:read` - `access_pass:basic:read` - `member:basic:read` */
export const retrieveLead: API.OperationMethod<
  RetrieveLeadRequest,
  Lead,
  RetrieveLeadError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveLeadRequest,
  output: Lead,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateLeadError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update lead [Legacy API — https://docs.whop.com/api-reference] Update the metadata or referrer information on an existing lead record. Required permissions: - `lead:manage` - `member:email:read` - `access_pass:basic:read` - `member:basic:read` */
export const updateLead: API.OperationMethod<
  UpdateLeadRequest,
  Lead,
  UpdateLeadError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateLeadRequest,
  output: Lead,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
