// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface CancelBountyRequest {
  /** Bounty ID (`bnty_` tag). */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CancelBountyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/bounties/{id}/cancel", code: 200 })),
).annotate({
  identifier: "CancelBountyRequest",
}) as any as S.Schema<CancelBountyRequest>;

/** The deliverable shapes this bounty accepts. Every bounty accepts any combination of `content_url` (posted links) and `media` (uploaded files), except `data_capture` bounties, whose proof is clips recorded in the Whop app that accumulate on the attempt. */
export type BountyAcceptedDeliverableTypesItem =
  | "content_url"
  | "media"
  | "data_capture";
export const BountyAcceptedDeliverableTypesItem = /*@__PURE__*/ S.String;

export type BountyAcceptedDeliverableTypesList =
  Array<BountyAcceptedDeliverableTypesItem>;
export const BountyAcceptedDeliverableTypesList = /*@__PURE__*/ S.Array(
  BountyAcceptedDeliverableTypesItem,
) as any as S.Schema<BountyAcceptedDeliverableTypesList>;

export interface UserProfilePicture {
  /** Avatar image URL. Always present — a generated placeholder when the user set no picture. */
  url: string;
}
export const UserProfilePicture = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
  }),
).annotate({
  identifier: "UserProfilePicture",
}) as any as S.Schema<UserProfilePicture>;

export interface UserSummary {
  /** User ID, prefixed `user_`. */
  id: string;
  /** Display name. */
  name: string | null;
  /** Avatar wrapper; its `url` is always present, using a generated placeholder when the user set no picture. */
  profile_picture: UserProfilePicture;
  /** Public username. */
  username: string;
}
export const UserSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    profile_picture: UserProfilePicture,
    username: S.String,
  }),
).annotate({ identifier: "UserSummary" }) as any as S.Schema<UserSummary>;

export interface BountyActiveLivestreamFeed {
  /** User hosting the proof livestream — the worker streaming their attempt. `null` if the host account no longer exists. */
  host: UserSummary | null;
  /** Livestream feed ID. */
  id: string;
  /** Display title for the proof livestream. */
  title: string;
}
export const BountyActiveLivestreamFeed = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    host: S.NullOr(UserSummary),
    id: S.String,
    title: S.String,
  }),
).annotate({
  identifier: "BountyActiveLivestreamFeed",
}) as any as S.Schema<BountyActiveLivestreamFeed>;

export type BountyActiveProofLivestreamFeedsList =
  Array<BountyActiveLivestreamFeed>;
export const BountyActiveProofLivestreamFeedsList = /*@__PURE__*/ S.Array(
  BountyActiveLivestreamFeed,
) as any as S.Schema<BountyActiveProofLivestreamFeedsList>;

export type BountyAllowedCountryCodesList = Array<string>;
export const BountyAllowedCountryCodesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<BountyAllowedCountryCodesList>;

/** What the poster wants the work to achieve, declared once at create. `null` for bounties created before the taxonomy rolled out. */
export type BountyBusinessGoalType =
  | "clipping"
  | "post_engagement"
  | "owned_account_growth"
  | "ugc_content"
  | "local_activation"
  | "data_capture"
  | "other";
export const BountyBusinessGoalType = /*@__PURE__*/ S.String;

export interface CaptureSpecImu {
  /** Units for the device-motion channels, as a compact key=unit string. */
  device_motion_units: string;
  /** Units for the magnetometer channel. */
  magnetometer_units: string;
  /** Minimum sustained IMU sample rate in hertz for a clip to pass validation. */
  min_rate_hz: number;
  /** Target IMU sample rate in hertz. */
  target_rate_hz: number;
  /** Minimum IMU sample rate in hertz tolerated during the warmup window. */
  warmup_min_rate_hz: number;
  /** Startup window, in nanoseconds, during which the relaxed warmup rate applies. */
  warmup_ns: number;
}
export const CaptureSpecImu = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    device_motion_units: S.String,
    magnetometer_units: S.String,
    min_rate_hz: S.Number,
    target_rate_hz: S.Number,
    warmup_min_rate_hz: S.Number,
    warmup_ns: S.Number,
  }),
).annotate({ identifier: "CaptureSpecImu" }) as any as S.Schema<CaptureSpecImu>;

export type CaptureSpecRequiredMetadataFieldsList = Array<string>;
export const CaptureSpecRequiredMetadataFieldsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CaptureSpecRequiredMetadataFieldsList>;

export type CaptureSpecVideoCodecsList = Array<string>;
export const CaptureSpecVideoCodecsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CaptureSpecVideoCodecsList>;

/** How the client must configure video stabilization: `off` disables EIS so raw motion is preserved for pose extraction, `on` requires it, `any` leaves the device default. */
export type CaptureSpecVideoStabilizationMode = "off" | "on" | "any";
export const CaptureSpecVideoStabilizationMode = /*@__PURE__*/ S.String;

export interface CaptureSpecVideo {
  /** Maximum acceptable average bitrate, in megabits per second. */
  bitrate_ceiling_mbps: number;
  /** Minimum acceptable average bitrate, in megabits per second. */
  bitrate_floor_mbps: number;
  /** Recommended average bitrate to encode at, in megabits per second. */
  bitrate_target_mbps: number;
  /** Which physical lens to record with. */
  camera_lens: string;
  codecs: CaptureSpecVideoCodecsList;
  /** Whether the client must also write the camera make and model into the video container's metadata. When `false`, the capture manifest and export CSV are the metadata carrier. */
  embed_camera_metadata: boolean;
  /** Target capture frame rate. */
  fps: number;
  /** Longest stall between consecutive frames a clip may contain before the client rejects it, in milliseconds. Every frame is timestamped in the frame log, so a stall stays alignable downstream — this bounds how broken a capture may be, not how evenly it must be paced. */
  frame_gap_tolerance_ms: number;
  /** Required frame height in pixels — recorded footage must match exactly. */
  height: number;
  /** Minimum acceptable horizontal field of view, in degrees. */
  min_fov_degrees: number;
  /** Device orientation to record in. */
  orientation: string;
  /** Preferred horizontal field of view, in degrees. */
  preferred_fov_degrees: number;
  /** How the client must configure video stabilization: `off` disables EIS so raw motion is preserved for pose extraction, `on` requires it, `any` leaves the device default. */
  stabilization_mode: CaptureSpecVideoStabilizationMode;
  /** Whether hardware/software stabilization must be enabled. True exactly when stabilization_mode is `on`. */
  stabilization_required: boolean;
  /** Required frame width in pixels — recorded footage must match exactly. */
  width: number;
}
export const CaptureSpecVideo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bitrate_ceiling_mbps: S.Number,
    bitrate_floor_mbps: S.Number,
    bitrate_target_mbps: S.Number,
    camera_lens: S.String,
    codecs: CaptureSpecVideoCodecsList,
    embed_camera_metadata: S.Boolean,
    fps: S.Number,
    frame_gap_tolerance_ms: S.Number,
    height: S.Number,
    min_fov_degrees: S.Number,
    orientation: S.String,
    preferred_fov_degrees: S.Number,
    stabilization_mode: CaptureSpecVideoStabilizationMode,
    stabilization_required: S.Boolean,
    width: S.Number,
  }),
).annotate({
  identifier: "CaptureSpecVideo",
}) as any as S.Schema<CaptureSpecVideo>;

export interface CaptureSpec {
  /** The naming convention for uploaded files, built from the required metadata fields. */
  filename_pattern: string;
  /** Inertial measurement unit (IMU) recording requirements. */
  imu: CaptureSpecImu;
  /** Schema version the client must stamp on the capture manifest it uploads. */
  manifest_schema_version: number;
  /** Minimum length of a single clip, in seconds. */
  min_clip_duration_seconds: number;
  /** Total verified footage a submission must accumulate across all its clips before it can be submitted, in seconds. Always a whole number of hours. */
  min_total_verified_duration_seconds: number;
  required_metadata_fields: CaptureSpecRequiredMetadataFieldsList;
  /** Whether each clip must be one uninterrupted recording rather than stitched segments. */
  single_continuous_take: boolean;
  /** Video recording requirements. */
  video: CaptureSpecVideo;
}
export const CaptureSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    filename_pattern: S.String,
    imu: CaptureSpecImu,
    manifest_schema_version: S.Number,
    min_clip_duration_seconds: S.Number,
    min_total_verified_duration_seconds: S.Number,
    required_metadata_fields: CaptureSpecRequiredMetadataFieldsList,
    single_continuous_take: S.Boolean,
    video: CaptureSpecVideo,
  }),
).annotate({ identifier: "CaptureSpec" }) as any as S.Schema<CaptureSpec>;

/** Currency for all amounts on the bounty, as a lowercase ISO 4217 code. */
export type BountyCurrency =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "cny"
  | "kzt"
  | "awg";
export const BountyCurrency = /*@__PURE__*/ S.String;

export interface AccountSummary {
  /** Account ID, prefixed `biz_`. */
  id: string;
  /** Account display name. */
  title: string;
}
export const AccountSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({ identifier: "AccountSummary" }) as any as S.Schema<AccountSummary>;

export interface StorefrontAccount {
  /** Account ID, prefixed `biz_`. */
  id: string;
  /** Account public route identifier — the `whop.com/{route}` storefront path. */
  route: string;
  /** Account display name. */
  title: string;
}
export const StorefrontAccount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    route: S.String,
    title: S.String,
  }),
).annotate({
  identifier: "StorefrontAccount",
}) as any as S.Schema<StorefrontAccount>;

/** How often the schedule creates a new bounty. Each occurrence is a separate bounty; the original is not republished. */
export type BountyScheduledFrequency =
  | "once"
  | "hourly"
  | "daily"
  | "weekly"
  | "monthly";
export const BountyScheduledFrequency = /*@__PURE__*/ S.String;

/** Lifecycle state. `scheduled` bounties are unpublished drafts, visible to their poster and the account's authorized managers; `open` bounties accept new submissions; `closed` bounties are live but no longer accept new submissions; `completed` bounties paid out every winner slot; `canceled` bounties ended before filling their slots. */
export type BountyStatus =
  | "scheduled"
  | "open"
  | "closed"
  | "completed"
  | "canceled";
export const BountyStatus = /*@__PURE__*/ S.String;

export interface Bounty {
  accepted_deliverable_types: BountyAcceptedDeliverableTypesList;
  /** Submissions accepted so far. */
  accepted_submissions_count: number;
  /** Number of submissions that can be accepted (winner slots). */
  accepted_submissions_limit: number;
  /** How many winner slots one worker can win. Defaults to `1`. Wins plus proofs awaiting review never exceed this number, and a worker runs one attempt at a time. Cannot exceed `accepted_submissions_limit`. */
  accepted_submissions_per_user_limit: number;
  active_proof_livestream_feeds: BountyActiveProofLivestreamFeedsList;
  /** What a referrer earns per accepted submission when the worker arrived through their affiliate link, in whole currency units, at the standard platform fee rate. Taken out of the worker's post-fee reward rather than added on top. `0` when the bounty pays no affiliate share, including bounties tied to no account, which cannot record a referral. */
  affiliate_share_amount: number;
  allowed_country_codes: BountyAllowedCountryCodesList;
  /** Submissions delivered and waiting on review. A subset of `unresolved_submissions_count`, which also counts attempts still in progress. */
  awaiting_review_submissions_count: number;
  /** Total gross budget committed to the bounty: `gross_reward_amount` times `accepted_submissions_limit`. */
  budget_amount: number;
  /** What the poster wants the work to achieve, declared once at create. `null` for bounties created before the taxonomy rolled out. */
  business_goal_type: BountyBusinessGoalType | null;
  /** When cancellation was requested, as an ISO 8601 timestamp. On a `closed` bounty this means the cancel is pending: submissions are stopped and the bounty cancels once in-flight submissions resolve. On a `canceled` bounty it records when the cancellation was requested. `null` when no cancellation was ever requested. */
  cancel_requested_at: string | null;
  /** The technical contract footage must be recorded against. Present only on `data_capture` bounties; `null` for every other goal type. */
  capture_spec: CaptureSpec | null;
  /** When the bounty was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Currency for all amounts on the bounty, as a lowercase ISO 4217 code. */
  currency: BountyCurrency;
  /** Submissions reviewed and turned down. */
  denied_submissions_count: number;
  /** Full task instructions shown to workers. */
  description: string;
  /** Experience the bounty's discussion thread lives in, prefixed `exp_`. Read this — not `experience_id` — to open the thread: a platform-wide bounty has no hosting experience of its own but its discussion still lives in one. */
  discussion_experience_id: string | null;
  /** Forum feed containing the bounty's discussion thread. `null` for a bounty with no forum post. */
  discussion_feed_id: string | null;
  /** Forum post anchoring the bounty's discussion thread. Read together with `discussion_experience_id` to address the thread. `null` for a bounty with no forum post. */
  discussion_post_id: string | null;
  /** Experience the bounty is hosted in, prefixed `exp_`. `null` for platform-wide bounties; may belong to a different account than the funder. */
  experience_id: string | null;
  /** Account whose balance funds the bounty pool, or `null` when a user funds it personally. May differ from the account hosting `experience_id`. */
  funding_account: AccountSummary | null;
  /** Gross amount paid out from the bounty pool across accepted submissions — worker payouts, platform fees, and affiliate shares together. Tips and reviewer rewards are excluded. */
  gross_paid_out_amount: number;
  /** Gross bounty-pool amount allocated per accepted submission, in whole currency units. */
  gross_reward_amount: number;
  /** Account hosting the bounty's forum — the one whose `route` and `experience_id` address its discussion thread, and where its submissions dashboard lives. `null` for a platform-wide bounty with no host. May differ from `funding_account`. */
  hosting_account: StorefrontAccount | null;
  /** Bounty ID, prefixed `bnty_`. */
  id: string;
  /** What a worker is quoted per accepted submission after the platform fee, in whole currency units. The exact post-fee figure, at the standard platform fee rate — a worker who locked a different rate, or who arrived through an affiliate link, is paid a different amount. */
  net_reward_amount: number;
  /** User who posted the bounty — the account owner when created with an account API key. */
  poster: UserSummary;
  /** How often the schedule creates a new bounty. Each occurrence is a separate bounty; the original is not republished. */
  scheduled_frequency: BountyScheduledFrequency | null;
  /** When a scheduled bounty will publish, as an ISO 8601 timestamp. `null` once published, for bounties that were never scheduled, and for terminally failed drafts parked for manual rescheduling. */
  scheduled_publish_at: string | null;
  /** Unfilled winner capacity: `accepted_submissions_limit` minus `accepted_submissions_count`, clamped to zero. Not on its own a signal that the bounty accepts new claims — read `status` for that: only an `open` bounty takes new submissions. */
  spots_remaining: number;
  /** Lifecycle state. `scheduled` bounties are unpublished drafts, visible to their poster and the account's authorized managers; `open` bounties accept new submissions; `closed` bounties are live but no longer accept new submissions; `completed` bounties paid out every winner slot; `canceled` bounties ended before filling their slots. */
  status: BountyStatus;
  /** When new submissions stopped being accepted, as an ISO 8601 timestamp. Set when a cancellation is requested on a bounty with work in flight, so in-flight submissions can resolve before the bounty cancels. `null` when submissions were never stopped — including completed bounties that simply filled every winner slot. */
  submissions_closed_at: string | null;
  /** Short name of the task shown to workers. */
  title: string;
  /** Submissions still awaiting an outcome: in progress or pending review. */
  unresolved_submissions_count: number;
  /** When the bounty was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** How many winner slots the authenticated user has already won on this bounty. Read against `accepted_submissions_per_user_limit` to show a worker their remaining allowance. `0` when the request has no authenticated user. */
  viewer_accepted_submissions_count: number;
}
export const Bounty = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accepted_deliverable_types: BountyAcceptedDeliverableTypesList,
    accepted_submissions_count: S.Number,
    accepted_submissions_limit: S.Number,
    accepted_submissions_per_user_limit: S.Number,
    active_proof_livestream_feeds: BountyActiveProofLivestreamFeedsList,
    affiliate_share_amount: S.Number,
    allowed_country_codes: BountyAllowedCountryCodesList,
    awaiting_review_submissions_count: S.Number,
    budget_amount: S.Number,
    business_goal_type: S.NullOr(BountyBusinessGoalType),
    cancel_requested_at: S.NullOr(S.String),
    capture_spec: S.NullOr(CaptureSpec),
    created_at: S.String,
    currency: BountyCurrency,
    denied_submissions_count: S.Number,
    description: S.String,
    discussion_experience_id: S.NullOr(S.String),
    discussion_feed_id: S.NullOr(S.String),
    discussion_post_id: S.NullOr(S.String),
    experience_id: S.NullOr(S.String),
    funding_account: S.NullOr(AccountSummary),
    gross_paid_out_amount: S.Number,
    gross_reward_amount: S.Number,
    hosting_account: S.NullOr(StorefrontAccount),
    id: S.String,
    net_reward_amount: S.Number,
    poster: UserSummary,
    scheduled_frequency: S.NullOr(BountyScheduledFrequency),
    scheduled_publish_at: S.NullOr(S.String),
    spots_remaining: S.Number,
    status: BountyStatus,
    submissions_closed_at: S.NullOr(S.String),
    title: S.String,
    unresolved_submissions_count: S.Number,
    updated_at: S.String,
    viewer_accepted_submissions_count: S.Number,
  }),
).annotate({ identifier: "Bounty" }) as any as S.Schema<Bounty>;

/** Countries whose residents can work the bounty, as ISO 3166 alpha-2 codes. Empty means worldwide. */
export type CreateBountyRequestAllowedCountryCodesList = Array<string>;
export const CreateBountyRequestAllowedCountryCodesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateBountyRequestAllowedCountryCodesList>;

/** What the poster wants the work to achieve, declared once here. */
export type CreateBountyRequestBusinessGoalType =
  | "clipping"
  | "post_engagement"
  | "owned_account_growth"
  | "ugc_content"
  | "local_activation"
  | "data_capture"
  | "other";
export const CreateBountyRequestBusinessGoalType = /*@__PURE__*/ S.String;

/** How the recorder configures video stabilization. `off` preserves raw motion for pose extraction. */
export type CreateBountyRequestCaptureSpecStabilizationMode =
  | "off"
  | "on"
  | "any";
export const CreateBountyRequestCaptureSpecStabilizationMode =
  /*@__PURE__*/ S.String;

/** Per-bounty overrides of the served capture contract. Only accepted when `business_goal_type` is `data_capture`; omitted fields keep the platform defaults, and the resulting contract is echoed back as `capture_spec` on the bounty. */
export interface CreateBountyRequestCaptureSpec {
  /** Average bitrate the recorder encodes at, in megabits per second. Must sit within the served floor and ceiling. */
  bitrate_target_mbps?: number;
  /** Whether the recorder also writes camera make and model into the video container's metadata. */
  embed_camera_metadata?: boolean;
  /** Longest stall between consecutive frames a clip may contain before the client rejects it, in milliseconds. Unlike the recording fields this one can also be tuned after the bounty is created, since it bounds what is accepted rather than how footage is captured. */
  frame_gap_tolerance_ms?: number;
  /** Minimum length of a single clip, in seconds. */
  min_clip_duration_seconds?: number;
  /** Total verified footage a submission must accumulate across all its clips before it can be submitted, in seconds. Must be a whole number of hours between 1 and 12. Editable after create, until someone starts an attempt. */
  min_total_verified_duration_seconds?: number;
  /** How the recorder configures video stabilization. `off` preserves raw motion for pose extraction. */
  stabilization_mode?:
    | CreateBountyRequestCaptureSpecStabilizationMode
    | (string & {});
}
export const CreateBountyRequestCaptureSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bitrate_target_mbps: S.optional(S.Number),
    embed_camera_metadata: S.optional(S.Boolean),
    frame_gap_tolerance_ms: S.optional(S.Number),
    min_clip_duration_seconds: S.optional(S.Number),
    min_total_verified_duration_seconds: S.optional(S.Number),
    stabilization_mode: S.optional(
      CreateBountyRequestCaptureSpecStabilizationMode,
    ),
  }),
).annotate({
  identifier: "CreateBountyRequestCaptureSpec",
}) as any as S.Schema<CreateBountyRequestCaptureSpec>;

/** How often the schedule creates a new bounty. Each occurrence is a separate bounty. Defaults to `once`; only applies with `publish_at`. */
export type CreateBountyRequestFrequency =
  | "once"
  | "hourly"
  | "daily"
  | "weekly"
  | "monthly";
export const CreateBountyRequestFrequency = /*@__PURE__*/ S.String;

export interface CreateBountyRequest {
  /** Number of submissions that can be accepted (winner slots). Defaults to 1. The escrowed total is `gross_reward_amount` times this limit and must be at least $5. */
  accepted_submissions_limit?: number | null;
  /** How many winner slots one worker can win. Defaults to `1`. Wins plus proofs awaiting review never exceed this number, and a worker runs one attempt at a time. Cannot exceed `accepted_submissions_limit`. */
  accepted_submissions_per_user_limit?: number | null;
  /** Account whose balance funds the bounty pool (`biz_` tag). Defaults to the caller's personal balance. Requires permission to move the account's funds. */
  account_id?: string | null;
  /** Countries whose residents can work the bounty, as ISO 3166 alpha-2 codes. Empty means worldwide. */
  allowed_country_codes?: CreateBountyRequestAllowedCountryCodesList | null;
  /** What the poster wants the work to achieve, declared once here. */
  business_goal_type?: CreateBountyRequestBusinessGoalType | (string & {});
  /** Per-bounty overrides of the served capture contract. Only accepted when `business_goal_type` is `data_capture`; omitted fields keep the platform defaults, and the resulting contract is echoed back as `capture_spec` on the bounty. */
  capture_spec?: CreateBountyRequestCaptureSpec;
  /** Full task instructions shown to workers. */
  description: string;
  /** Experience to host the bounty in (`exp_` tag). Any visibility — public for an open bounty, private for an invited one. Required unless account_id is set, in which case the bounty anchors in that account's public forum. */
  experience_id?: string | null;
  /** How often the schedule creates a new bounty. Each occurrence is a separate bounty. Defaults to `once`; only applies with `publish_at`. */
  frequency?: CreateBountyRequestFrequency | (string & {});
  /** Gross bounty-pool amount (USD) escrowed per accepted submission, in whole dollars. Platform fees and affiliate shares are paid from this amount. */
  gross_reward_amount: number;
  /** ISO 8601 time to publish the bounty. When set, the bounty is created as a hidden draft and funded + published at this time instead of immediately. */
  publish_at?: string | null;
  /** IANA timezone for recurring occurrences. Required when publish_at is set. */
  publish_at_timezone?: string | null;
  /** Short name of the task shown to workers. */
  title: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateBountyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accepted_submissions_limit: S.optional(S.NullOr(S.Number)),
    accepted_submissions_per_user_limit: S.optional(S.NullOr(S.Number)),
    account_id: S.optional(S.NullOr(S.String)),
    allowed_country_codes: S.optional(
      S.NullOr(CreateBountyRequestAllowedCountryCodesList),
    ),
    business_goal_type: S.optional(CreateBountyRequestBusinessGoalType),
    capture_spec: S.optional(CreateBountyRequestCaptureSpec),
    description: S.String,
    experience_id: S.optional(S.NullOr(S.String)),
    frequency: S.optional(CreateBountyRequestFrequency),
    gross_reward_amount: S.Number,
    publish_at: S.optional(S.NullOr(S.String)),
    publish_at_timezone: S.optional(S.NullOr(S.String)),
    title: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/bounties", code: 200 })),
).annotate({
  identifier: "CreateBountyRequest",
}) as any as S.Schema<CreateBountyRequest>;

export type ListBountiesRequestStatus =
  | "scheduled"
  | "open"
  | "closed"
  | "completed"
  | "canceled";
export const ListBountiesRequestStatus = /*@__PURE__*/ S.String;

export type ListBountiesRequestBusinessGoalType =
  | "clipping"
  | "post_engagement"
  | "owned_account_growth"
  | "ugc_content"
  | "local_activation"
  | "data_capture"
  | "other";
export const ListBountiesRequestBusinessGoalType = /*@__PURE__*/ S.String;

export type ListBountiesRequestOrder =
  | "created_at"
  | "gross_paid_out_amount"
  | "gross_reward_amount";
export const ListBountiesRequestOrder = /*@__PURE__*/ S.String;

export type ListBountiesRequestDirection = "asc" | "desc";
export const ListBountiesRequestDirection = /*@__PURE__*/ S.String;

export interface ListBountiesRequest {
  /** Scope the list to this account (`biz_` tag). Requires read access to the account; account API keys may pass their own account or a connected account. */
  account_id?: string;
  /** List the bounties this user participated in (`user_` tag). Must be the authenticated user. */
  user_id?: string;
  /** Filter by lifecycle state. */
  status?: ListBountiesRequestStatus | (string & {});
  /** Filter by the poster's declared goal. Bounties created before the goal taxonomy carry no goal and never match this filter. */
  business_goal_type?: ListBountiesRequestBusinessGoalType | (string & {});
  /** Only bounties workable from this country, as an ISO 3166-1 alpha-2 code. Bounties with no country targeting are workable worldwide and always match. */
  country?: string;
  /** Only bounties posted to this forum experience, prefixed `exp_`. An unknown experience, or one outside the caller's scope, matches nothing. */
  experience_id?: string;
  /** Substring match on the bounty title or ID. */
  query?: string;
  /** Only bounties created after this ISO 8601 timestamp. */
  created_after?: string;
  /** Only bounties created before this ISO 8601 timestamp. */
  created_before?: string;
  /** Sort field. */
  order?: ListBountiesRequestOrder | (string & {});
  /** Sort direction. */
  direction?: ListBountiesRequestDirection | (string & {});
  /** Number of bounties to return from the start of the window. */
  first?: number;
  /** Cursor to paginate forwards from. */
  after?: string;
  /** Number of bounties to return from the end of the window. */
  last?: number;
  /** Cursor to paginate backwards from. */
  before?: string;
}
export const ListBountiesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
    status: S.optional(ListBountiesRequestStatus.pipe(T.Query())),
    business_goal_type: S.optional(
      ListBountiesRequestBusinessGoalType.pipe(T.Query()),
    ),
    country: S.optional(S.String.pipe(T.Query())),
    experience_id: S.optional(S.String.pipe(T.Query())),
    query: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListBountiesRequestOrder.pipe(T.Query())),
    direction: S.optional(ListBountiesRequestDirection.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/bounties", code: 200 })),
).annotate({
  identifier: "ListBountiesRequest",
}) as any as S.Schema<ListBountiesRequest>;

/** The deliverable shapes this bounty accepts. Every bounty accepts any combination of `content_url` (posted links) and `media` (uploaded files), except `data_capture` bounties, whose proof is clips recorded in the Whop app that accumulate on the attempt. */
export type BountyListItemAcceptedDeliverableTypesItem =
  | "content_url"
  | "media"
  | "data_capture";
export const BountyListItemAcceptedDeliverableTypesItem =
  /*@__PURE__*/ S.String;

export type BountyListItemAcceptedDeliverableTypesList =
  Array<BountyListItemAcceptedDeliverableTypesItem>;
export const BountyListItemAcceptedDeliverableTypesList = /*@__PURE__*/ S.Array(
  BountyListItemAcceptedDeliverableTypesItem,
) as any as S.Schema<BountyListItemAcceptedDeliverableTypesList>;

export type BountyListItemAllowedCountryCodesList = Array<string>;
export const BountyListItemAllowedCountryCodesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<BountyListItemAllowedCountryCodesList>;

/** What the poster wants the work to achieve, declared once at create. `null` for bounties created before the taxonomy rolled out. */
export type BountyListItemBusinessGoalType =
  | "clipping"
  | "post_engagement"
  | "owned_account_growth"
  | "ugc_content"
  | "local_activation"
  | "data_capture"
  | "other";
export const BountyListItemBusinessGoalType = /*@__PURE__*/ S.String;

/** How often the schedule creates a new bounty. Each occurrence is a separate bounty; the original is not republished. */
export type BountyListItemScheduledFrequency =
  | "once"
  | "hourly"
  | "daily"
  | "weekly"
  | "monthly";
export const BountyListItemScheduledFrequency = /*@__PURE__*/ S.String;

/** Lifecycle state. `scheduled` bounties are unpublished drafts, visible to their poster and the account's authorized managers; `open` bounties accept new submissions; `closed` bounties are live but no longer accept new submissions; `completed` bounties paid out every winner slot; `canceled` bounties ended before filling their slots. */
export type BountyListItemStatus =
  | "scheduled"
  | "open"
  | "closed"
  | "completed"
  | "canceled";
export const BountyListItemStatus = /*@__PURE__*/ S.String;

export interface BountyListItem {
  accepted_deliverable_types: BountyListItemAcceptedDeliverableTypesList;
  /** Submissions accepted so far. */
  accepted_submissions_count: number;
  /** Number of submissions that can be accepted (winner slots). */
  accepted_submissions_limit: number;
  /** How many winner slots one worker can win. Defaults to `1`. Wins plus proofs awaiting review never exceed this number, and a worker runs one attempt at a time. Cannot exceed `accepted_submissions_limit`. */
  accepted_submissions_per_user_limit: number;
  /** What a referrer earns per accepted submission when the worker arrived through their affiliate link, in whole currency units, at the standard platform fee rate. Taken out of the worker's post-fee reward rather than added on top. `0` when the bounty pays no affiliate share, including bounties tied to no account, which cannot record a referral. */
  affiliate_share_amount: number;
  allowed_country_codes: BountyListItemAllowedCountryCodesList;
  /** Total gross budget committed to the bounty: `gross_reward_amount` times `accepted_submissions_limit`. */
  budget_amount: number;
  /** What the poster wants the work to achieve, declared once at create. `null` for bounties created before the taxonomy rolled out. */
  business_goal_type: BountyListItemBusinessGoalType | null;
  /** When cancellation was requested, as an ISO 8601 timestamp. On a `closed` bounty this means the cancel is pending: submissions are stopped and the bounty cancels once in-flight submissions resolve. On a `canceled` bounty it records when the cancellation was requested. `null` when no cancellation was ever requested. */
  cancel_requested_at: string | null;
  /** When the bounty was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Currency for all amounts on the bounty, as a lowercase ISO 4217 code. */
  currency: string;
  /** Full task instructions shown to workers. */
  description: string;
  /** Experience the bounty's discussion thread lives in, prefixed `exp_`. Read this — not `experience_id` — to open the thread: a platform-wide bounty has no hosting experience of its own but its discussion still lives in one. */
  discussion_experience_id: string | null;
  /** Forum feed containing the bounty's discussion thread. `null` for a bounty with no forum post. */
  discussion_feed_id: string | null;
  /** Forum post anchoring the bounty's discussion thread. Read together with `discussion_experience_id` to address the thread. `null` for a bounty with no forum post. */
  discussion_post_id: string | null;
  /** Experience the bounty is hosted in, prefixed `exp_`. `null` for platform-wide bounties; may belong to a different account than the funder. */
  experience_id: string | null;
  /** Account whose balance funds the bounty pool, or `null` when a user funds it personally. May differ from the account hosting `experience_id`. */
  funding_account: AccountSummary | null;
  /** Gross amount paid out from the bounty pool across accepted submissions — worker payouts, platform fees, and affiliate shares together. Tips and reviewer rewards are excluded. */
  gross_paid_out_amount: number;
  /** Gross bounty-pool amount allocated per accepted submission, in whole currency units. */
  gross_reward_amount: number;
  /** Account hosting the bounty's forum — the one whose `route` and `experience_id` address its discussion thread, and where its submissions dashboard lives. `null` for a platform-wide bounty with no host. May differ from `funding_account`. */
  hosting_account: StorefrontAccount | null;
  /** Bounty ID, prefixed `bnty_`. */
  id: string;
  /** What a worker is quoted per accepted submission after the platform fee, in whole currency units. The exact post-fee figure, at the standard platform fee rate — a worker who locked a different rate, or who arrived through an affiliate link, is paid a different amount. */
  net_reward_amount: number;
  /** User who posted the bounty — the account owner when created with an account API key. */
  poster: UserSummary;
  /** How often the schedule creates a new bounty. Each occurrence is a separate bounty; the original is not republished. */
  scheduled_frequency: BountyListItemScheduledFrequency | null;
  /** When a scheduled bounty will publish, as an ISO 8601 timestamp. `null` once published, for bounties that were never scheduled, and for terminally failed drafts parked for manual rescheduling. */
  scheduled_publish_at: string | null;
  /** Unfilled winner capacity: `accepted_submissions_limit` minus `accepted_submissions_count`, clamped to zero. Not on its own a signal that the bounty accepts new claims — read `status` for that: only an `open` bounty takes new submissions. */
  spots_remaining: number;
  /** Lifecycle state. `scheduled` bounties are unpublished drafts, visible to their poster and the account's authorized managers; `open` bounties accept new submissions; `closed` bounties are live but no longer accept new submissions; `completed` bounties paid out every winner slot; `canceled` bounties ended before filling their slots. */
  status: BountyListItemStatus;
  /** When new submissions stopped being accepted, as an ISO 8601 timestamp. Set when a cancellation is requested on a bounty with work in flight, so in-flight submissions can resolve before the bounty cancels. `null` when submissions were never stopped — including completed bounties that simply filled every winner slot. */
  submissions_closed_at: string | null;
  /** Short name of the task shown to workers. */
  title: string;
  /** Submissions still awaiting an outcome: in progress or pending review. */
  unresolved_submissions_count: number;
  /** When the bounty was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** How many winner slots the authenticated user has already won on this bounty. Read against `accepted_submissions_per_user_limit` to show a worker their remaining allowance. `0` when the request has no authenticated user. */
  viewer_accepted_submissions_count: number;
}
export const BountyListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accepted_deliverable_types: BountyListItemAcceptedDeliverableTypesList,
    accepted_submissions_count: S.Number,
    accepted_submissions_limit: S.Number,
    accepted_submissions_per_user_limit: S.Number,
    affiliate_share_amount: S.Number,
    allowed_country_codes: BountyListItemAllowedCountryCodesList,
    budget_amount: S.Number,
    business_goal_type: S.NullOr(BountyListItemBusinessGoalType),
    cancel_requested_at: S.NullOr(S.String),
    created_at: S.String,
    currency: S.String,
    description: S.String,
    discussion_experience_id: S.NullOr(S.String),
    discussion_feed_id: S.NullOr(S.String),
    discussion_post_id: S.NullOr(S.String),
    experience_id: S.NullOr(S.String),
    funding_account: S.NullOr(AccountSummary),
    gross_paid_out_amount: S.Number,
    gross_reward_amount: S.Number,
    hosting_account: S.NullOr(StorefrontAccount),
    id: S.String,
    net_reward_amount: S.Number,
    poster: UserSummary,
    scheduled_frequency: S.NullOr(BountyListItemScheduledFrequency),
    scheduled_publish_at: S.NullOr(S.String),
    spots_remaining: S.Number,
    status: BountyListItemStatus,
    submissions_closed_at: S.NullOr(S.String),
    title: S.String,
    unresolved_submissions_count: S.Number,
    updated_at: S.String,
    viewer_accepted_submissions_count: S.Number,
  }),
).annotate({ identifier: "BountyListItem" }) as any as S.Schema<BountyListItem>;

export type ListBountiesResponseDataList = Array<BountyListItem>;
export const ListBountiesResponseDataList = /*@__PURE__*/ S.Array(
  BountyListItem,
) as any as S.Schema<ListBountiesResponseDataList>;

export interface ListBountiesResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListBountiesResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListBountiesResponsePageInfo",
}) as any as S.Schema<ListBountiesResponsePageInfo>;

export interface ListBountiesResponse {
  data: ListBountiesResponseDataList;
  page_info: ListBountiesResponsePageInfo;
}
export const ListBountiesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListBountiesResponseDataList,
    page_info: ListBountiesResponsePageInfo,
  }),
).annotate({
  identifier: "ListBountiesResponse",
}) as any as S.Schema<ListBountiesResponse>;

export type ListPublicBountySubmissionsRequestStatus =
  | "submitted"
  | "approved"
  | "denied";
export const ListPublicBountySubmissionsRequestStatus = /*@__PURE__*/ S.String;

export type ListPublicBountySubmissionsRequestOrder =
  | "created_at"
  | "updated_at";
export const ListPublicBountySubmissionsRequestOrder = /*@__PURE__*/ S.String;

export type ListPublicBountySubmissionsRequestDirection = "asc" | "desc";
export const ListPublicBountySubmissionsRequestDirection =
  /*@__PURE__*/ S.String;

export interface ListPublicBountySubmissionsRequest {
  /** The bounty whose public submissions to list (`bnty_` tag). */
  bounty_id: string;
  /** Filter by lifecycle state. */
  status?: ListPublicBountySubmissionsRequestStatus | (string & {});
  /** Only submissions created after this ISO 8601 timestamp. */
  created_after?: string;
  /** Only submissions created before this ISO 8601 timestamp. */
  created_before?: string;
  /** Sort field. */
  order?: ListPublicBountySubmissionsRequestOrder | (string & {});
  /** Sort direction. */
  direction?: ListPublicBountySubmissionsRequestDirection | (string & {});
  /** Number of submissions to return from the start of the window. */
  first?: number;
  /** Cursor to paginate forwards from. */
  after?: string;
  /** Number of submissions to return from the end of the window. */
  last?: number;
  /** Cursor to paginate backwards from. */
  before?: string;
}
export const ListPublicBountySubmissionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bounty_id: S.String.pipe(T.Label()),
    status: S.optional(
      ListPublicBountySubmissionsRequestStatus.pipe(T.Query()),
    ),
    created_after: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListPublicBountySubmissionsRequestOrder.pipe(T.Query())),
    direction: S.optional(
      ListPublicBountySubmissionsRequestDirection.pipe(T.Query()),
    ),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/bounties/{bounty_id}/submissions",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListPublicBountySubmissionsRequest",
}) as any as S.Schema<ListPublicBountySubmissionsRequest>;

/** Recording lifecycle state. */
export type BountySubmissionLivestreamFeedRecordingStatus =
  | "recording"
  | "processing"
  | "completed"
  | "failed";
export const BountySubmissionLivestreamFeedRecordingStatus =
  /*@__PURE__*/ S.String;

export interface BountySubmissionLivestreamFeed {
  /** When the proof livestream ended, as an ISO 8601 timestamp. `null` while it is still live — a feed with a `started_at` and no `ended_at` is streaming right now. */
  ended_at: string | null;
  /** Livestream feed ID. */
  id: string;
  /** Recording lifecycle state. */
  recording_status: BountySubmissionLivestreamFeedRecordingStatus | null;
  /** Playback URL for a completed proof recording, when available. */
  recording_url: string | null;
  /** When the proof livestream went live, as an ISO 8601 timestamp. `null` before it starts. */
  started_at: string | null;
  /** Current proof thumbnail URL, when available. */
  thumbnail_url: string | null;
  /** Display title for the proof livestream. */
  title: string;
}
export const BountySubmissionLivestreamFeed = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ended_at: S.NullOr(S.String),
    id: S.String,
    recording_status: S.NullOr(BountySubmissionLivestreamFeedRecordingStatus),
    recording_url: S.NullOr(S.String),
    started_at: S.NullOr(S.String),
    thumbnail_url: S.NullOr(S.String),
    title: S.String,
  }),
).annotate({
  identifier: "BountySubmissionLivestreamFeed",
}) as any as S.Schema<BountySubmissionLivestreamFeed>;

/** Lifecycle state. `submitted` submissions await review; `approved` submissions were accepted and paid; `denied` submissions were rejected. In-progress attempts never appear on the public list. */
export type PublicBountySubmissionStatus = "submitted" | "approved" | "denied";
export const PublicBountySubmissionStatus = /*@__PURE__*/ S.String;

export interface PublicBountySubmission {
  /** The bounty the work was submitted to, prefixed `bnty_`. */
  bounty_id: string;
  /** When the worker claimed the submission, as an ISO 8601 timestamp. */
  claimed_at: string | null;
  /** When the submission was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Why the submission was denied, when a presentable reason exists. Always `null` unless `status` is `denied`. */
  denial_reason: string | null;
  /** Submission ID, prefixed `btys_`. */
  id: string;
  /** Latest public proof livestream attached to the submission. */
  latest_proof_livestream_feed: BountySubmissionLivestreamFeed | null;
  /** When the submission was approved or denied, as an ISO 8601 timestamp. `null` until then. */
  resolved_at: string | null;
  /** Lifecycle state. `submitted` submissions await review; `approved` submissions were accepted and paid; `denied` submissions were rejected. In-progress attempts never appear on the public list. */
  status: PublicBountySubmissionStatus;
  /** When proof was submitted for review, as an ISO 8601 timestamp. */
  submitted_at: string | null;
  /** When the submission was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** User who submitted the work. */
  worker: UserSummary;
}
export const PublicBountySubmission = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bounty_id: S.String,
    claimed_at: S.NullOr(S.String),
    created_at: S.String,
    denial_reason: S.NullOr(S.String),
    id: S.String,
    latest_proof_livestream_feed: S.NullOr(BountySubmissionLivestreamFeed),
    resolved_at: S.NullOr(S.String),
    status: PublicBountySubmissionStatus,
    submitted_at: S.NullOr(S.String),
    updated_at: S.String,
    worker: UserSummary,
  }),
).annotate({
  identifier: "PublicBountySubmission",
}) as any as S.Schema<PublicBountySubmission>;

export type ListPublicBountySubmissionsResponseDataList =
  Array<PublicBountySubmission>;
export const ListPublicBountySubmissionsResponseDataList =
  /*@__PURE__*/ S.Array(
    PublicBountySubmission,
  ) as any as S.Schema<ListPublicBountySubmissionsResponseDataList>;

export type ListPublicBountySubmissionsResponsePageInfo =
  ListBountiesResponsePageInfo;
export const ListPublicBountySubmissionsResponsePageInfo =
  ListBountiesResponsePageInfo;

export interface ListPublicBountySubmissionsResponse {
  data: ListPublicBountySubmissionsResponseDataList;
  page_info: ListBountiesResponsePageInfo;
}
export const ListPublicBountySubmissionsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListPublicBountySubmissionsResponseDataList,
    page_info: ListBountiesResponsePageInfo,
  }),
).annotate({
  identifier: "ListPublicBountySubmissionsResponse",
}) as any as S.Schema<ListPublicBountySubmissionsResponse>;

export interface RetrieveBountyRequest {
  /** Bounty ID (`bnty_` tag). */
  id: string;
}
export const RetrieveBountyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/bounties/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveBountyRequest",
}) as any as S.Schema<RetrieveBountyRequest>;

export interface RetrievePublicBountySubmissionRequest {
  /** The bounty the submission belongs to (`bnty_` tag). */
  bounty_id: string;
  /** The submission to retrieve (`btys_` tag). */
  id: string;
}
export const RetrievePublicBountySubmissionRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      bounty_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/bounties/{bounty_id}/submissions/{id}",
        code: 200,
      }),
    ),
).annotate({
  identifier: "RetrievePublicBountySubmissionRequest",
}) as any as S.Schema<RetrievePublicBountySubmissionRequest>;

/** Replace the countries whose residents can work the bounty, as ISO 3166 alpha-2 codes. Empty means worldwide. */
export type UpdateBountyRequestAllowedCountryCodesList = Array<string>;
export const UpdateBountyRequestAllowedCountryCodesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpdateBountyRequestAllowedCountryCodesList>;

/** What the poster wants the work to achieve, declared once here. */
export type UpdateBountyRequestBusinessGoalType =
  | "clipping"
  | "post_engagement"
  | "owned_account_growth"
  | "ugc_content"
  | "local_activation"
  | "data_capture"
  | "other";
export const UpdateBountyRequestBusinessGoalType = /*@__PURE__*/ S.String;

/** Scheduled drafts only. How often the schedule creates a new bounty. */
export type UpdateBountyRequestFrequency =
  | "once"
  | "hourly"
  | "daily"
  | "weekly"
  | "monthly";
export const UpdateBountyRequestFrequency = /*@__PURE__*/ S.String;

export interface UpdateBountyRequest {
  /** Bounty ID (`bnty_` tag). */
  id: string;
  /** Scheduled drafts only. Number of submissions that can be accepted (winner slots). */
  accepted_submissions_limit?: number | null;
  /** How many winner slots one worker can win. Defaults to `1`. Wins plus proofs awaiting review never exceed this number, and a worker runs one attempt at a time. Cannot exceed `accepted_submissions_limit`. Editable while the bounty is still open with nothing under review. */
  accepted_submissions_per_user_limit?: number | null;
  /** Replace the countries whose residents can work the bounty, as ISO 3166 alpha-2 codes. Empty means worldwide. */
  allowed_country_codes?: UpdateBountyRequestAllowedCountryCodesList | null;
  /** What the poster wants the work to achieve, declared once here. */
  business_goal_type?: UpdateBountyRequestBusinessGoalType | (string & {});
  /** New full task instructions. */
  description?: string;
  /** Scheduled drafts only. How often the schedule creates a new bounty. */
  frequency?: UpdateBountyRequestFrequency | (string & {});
  /** Scheduled drafts only. Gross bounty-pool amount (USD) escrowed per accepted submission. The escrowed total (this times accepted_submissions_limit) must stay at least $5. */
  gross_reward_amount?: number | null;
  /** Scheduled drafts only. New ISO 8601 time to publish the draft. Must be in the future. */
  publish_at?: string | null;
  /** Scheduled drafts only. IANA timezone for recurring occurrences. */
  publish_at_timezone?: string | null;
  /** New short name of the task. */
  title?: string;
}
export const UpdateBountyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    accepted_submissions_limit: S.optional(S.NullOr(S.Number)),
    accepted_submissions_per_user_limit: S.optional(S.NullOr(S.Number)),
    allowed_country_codes: S.optional(
      S.NullOr(UpdateBountyRequestAllowedCountryCodesList),
    ),
    business_goal_type: S.optional(UpdateBountyRequestBusinessGoalType),
    description: S.optional(S.String),
    frequency: S.optional(UpdateBountyRequestFrequency),
    gross_reward_amount: S.optional(S.NullOr(S.Number)),
    publish_at: S.optional(S.NullOr(S.String)),
    publish_at_timezone: S.optional(S.NullOr(S.String)),
    title: S.optional(S.String),
  }).pipe(T.Http({ method: "PATCH", uri: "/bounties/{id}", code: 200 })),
).annotate({
  identifier: "UpdateBountyRequest",
}) as any as S.Schema<UpdateBountyRequest>;

export type CancelBountyError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Cancel Cancels a bounty. With no in-flight work, it cancels immediately and refunds the funder. Otherwise it stops new submissions and cancels once the in-flight work resolves and pays out. Repeating the request is a no-op. A bounty that already paid out every slot returns `400`. */
export const cancelBounty: API.OperationMethod<
  CancelBountyRequest,
  Bounty,
  CancelBountyError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CancelBountyRequest,
  output: Bounty,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreateBountyError = BadRequest | Forbidden | Conflict | WhopOpError;
/** Create Bounty Creates a bounty and escrows its reward pool. Publishes immediately, or as a scheduled draft when you set `publish_at`. */
export const createBounty: API.OperationMethod<
  CreateBountyRequest,
  Bounty,
  CreateBountyError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateBountyRequest,
  output: Bounty,
  errors: [BadRequest, Forbidden, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListBountiesError = BadRequest | Forbidden | WhopOpError;
/** List Bounties Lists bounties visible to the credential — for an account API key, the account's bounties including scheduled drafts; for a user token, the bounties the user can see and work. */
export const listBounties: API.PaginatedOperationMethod<
  ListBountiesRequest,
  ListBountiesResponse,
  ListBountiesError,
  WhopOpContext,
  BountyListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListBountiesRequest,
    output: ListBountiesResponse,
    errors: [BadRequest, Forbidden],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ListPublicBountySubmissionsError =
  | BadRequest
  | NotFound
  | WhopOpError;
/** List Public Submissions Lists a bounty's publicly visible work — submitted, approved, and denied submissions in the reduced public shape. Authentication is optional; a bounty that is not publicly visible returns `404`. */
export const listPublicBountySubmissions: API.PaginatedOperationMethod<
  ListPublicBountySubmissionsRequest,
  ListPublicBountySubmissionsResponse,
  ListPublicBountySubmissionsError,
  WhopOpContext,
  PublicBountySubmission
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListPublicBountySubmissionsRequest,
    output: ListPublicBountySubmissionsResponse,
    errors: [BadRequest, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveBountyError = NotFound | WhopOpError;
/** Retrieve Bounty Retrieves a bounty by ID. Authentication is optional: a request with no credential reads the bounty when it is publicly visible — published or completed, and not restricted to a private experience's members. Bounties outside the caller's scope, and bounties not publicly visible to an anonymous caller, return `404`. */
export const retrieveBounty: API.OperationMethod<
  RetrieveBountyRequest,
  Bounty,
  RetrieveBountyError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveBountyRequest,
  output: Bounty,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrievePublicBountySubmissionError = NotFound | WhopOpError;
/** Retrieve Public Submission Retrieves one of a bounty's publicly visible submissions in the reduced public shape — the read behind a shared proof link, whose submission is usually outside the bounty page's capped preview. Authentication is optional; a bounty that is not publicly visible, and a submission that is not publicly visible work on it, both return `404`. */
export const retrievePublicBountySubmission: API.OperationMethod<
  RetrievePublicBountySubmissionRequest,
  PublicBountySubmission,
  RetrievePublicBountySubmissionError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrievePublicBountySubmissionRequest,
  output: PublicBountySubmission,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateBountyError = NotFound | WhopOpError;
/** Update Bounty Updates a bounty. A published bounty accepts title, description, and country targeting while it is still open with nothing under review. A scheduled (not-yet-published) draft additionally accepts the reward, winner slots, and schedule. */
export const updateBounty: API.OperationMethod<
  UpdateBountyRequest,
  Bounty,
  UpdateBountyError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateBountyRequest,
  output: Bounty,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
