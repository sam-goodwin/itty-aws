// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface ActivatePromoCodeRequest {
  /** Promo code ID (`promo_` tag). */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const ActivatePromoCodeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/promo_codes/{id}/activate", code: 200 }),
  ),
).annotate({
  identifier: "ActivatePromoCodeRequest",
}) as any as S.Schema<ActivatePromoCodeRequest>;

export interface AccountSummary {
  /** Account ID, prefixed `biz_`. */
  id: string;
  /** Account display name. */
  title: string;
}
export const AccountSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({ identifier: "AccountSummary" }) as any as S.Schema<AccountSummary>;

/** Currency used for a fixed-amount discount. */
export type PromoCodeCurrency =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const PromoCodeCurrency = /*@__PURE__*/ S.String;

/** How long the discount applies. */
export type PromoCodeDuration = "forever" | "once" | "repeating";
export const PromoCodeDuration = /*@__PURE__*/ S.String;

export interface PromoCodeProduct {
  /** Product ID, prefixed `prod_`. */
  id: string;
  /** Product display name. */
  title: string;
}
export const PromoCodeProduct = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({
  identifier: "PromoCodeProduct",
}) as any as S.Schema<PromoCodeProduct>;

/** Whether the discount is percentage-based or a fixed amount. */
export type PromoCodePromoType = "percentage" | "flat_amount";
export const PromoCodePromoType = /*@__PURE__*/ S.String;

/** Promo code lifecycle status. */
export type PromoCodeStatus = "active" | "inactive" | "archived";
export const PromoCodeStatus = /*@__PURE__*/ S.String;

export interface PromoCode {
  /** Account that owns the promo code. */
  account: AccountSummary;
  /** Discount amount. Percentage discounts are represented as a decimal fraction. */
  amount_off: number;
  /** Whether the promo code is restricted to churned customers. */
  churned_users_only: boolean;
  /** Code entered at checkout. */
  code: string | null;
  /** When the promo code was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Currency used for a fixed-amount discount. */
  currency: PromoCodeCurrency;
  /** How long the discount applies. */
  duration: PromoCodeDuration;
  /** Whether the promo code applies only to existing memberships. */
  existing_memberships_only: boolean;
  /** When the promo code expires, as an ISO 8601 timestamp. */
  expires_at: string | null;
  /** Promo code ID, prefixed `promo_`. */
  id: string;
  /** Custom key-value metadata stored on the promo code. */
  metadata: unknown;
  /** Whether the promo code is restricted to new customers. */
  new_users_only: boolean;
  /** Whether each customer may redeem the promo code only once. */
  one_per_customer: boolean;
  /** Product the promo code is restricted to, or `null` when it is not product-scoped. */
  product: PromoCodeProduct | null;
  /** Billing intervals the discount applies to. */
  promo_duration_months: number | null;
  /** Whether the discount is percentage-based or a fixed amount. */
  promo_type: PromoCodePromoType;
  /** Promo code lifecycle status. */
  status: PromoCodeStatus;
  /** Maximum uses when stock is limited. */
  stock: number;
  /** Whether the promo code has no redemption limit. */
  unlimited_stock: boolean;
  /** When the promo code was updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** Memberships that used the promo code. */
  uses: number;
}
export const PromoCode = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account: AccountSummary,
    amount_off: S.Number,
    churned_users_only: S.Boolean,
    code: S.NullOr(S.String),
    created_at: S.String,
    currency: PromoCodeCurrency,
    duration: PromoCodeDuration,
    existing_memberships_only: S.Boolean,
    expires_at: S.NullOr(S.String),
    id: S.String,
    metadata: S.Unknown,
    new_users_only: S.Boolean,
    one_per_customer: S.Boolean,
    product: S.NullOr(PromoCodeProduct),
    promo_duration_months: S.NullOr(S.Number),
    promo_type: PromoCodePromoType,
    status: PromoCodeStatus,
    stock: S.Number,
    unlimited_stock: S.Boolean,
    updated_at: S.String,
    uses: S.Number,
  }),
).annotate({ identifier: "PromoCode" }) as any as S.Schema<PromoCode>;

export type CreatePromoCodeRequestBaseCurrency =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const CreatePromoCodeRequestBaseCurrency = /*@__PURE__*/ S.String;

export type CreatePromoCodeRequestPlanIdsList = Array<string>;
export const CreatePromoCodeRequestPlanIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreatePromoCodeRequestPlanIdsList>;

export type CreatePromoCodeRequestPromoType = "percentage" | "flat_amount";
export const CreatePromoCodeRequestPromoType = /*@__PURE__*/ S.String;

export interface CreatePromoCodeRequest {
  account_id: string;
  amount_off: number;
  base_currency: CreatePromoCodeRequestBaseCurrency | (string & {});
  churned_users_only?: boolean;
  code: string;
  existing_memberships_only?: boolean;
  expires_at?: string | null;
  new_users_only: boolean;
  one_per_customer?: boolean;
  plan_ids?: CreatePromoCodeRequestPlanIdsList;
  product_id?: string | null;
  promo_duration_months: number;
  promo_type: CreatePromoCodeRequestPromoType | (string & {});
  stock?: number | null;
  unlimited_stock?: boolean;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreatePromoCodeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String,
    amount_off: S.Number,
    base_currency: CreatePromoCodeRequestBaseCurrency,
    churned_users_only: S.optional(S.Boolean),
    code: S.String,
    existing_memberships_only: S.optional(S.Boolean),
    expires_at: S.optional(S.NullOr(S.String)),
    new_users_only: S.Boolean,
    one_per_customer: S.optional(S.Boolean),
    plan_ids: S.optional(CreatePromoCodeRequestPlanIdsList),
    product_id: S.optional(S.NullOr(S.String)),
    promo_duration_months: S.Number,
    promo_type: CreatePromoCodeRequestPromoType,
    stock: S.optional(S.NullOr(S.Number)),
    unlimited_stock: S.optional(S.Boolean),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/promo_codes", code: 200 })),
).annotate({
  identifier: "CreatePromoCodeRequest",
}) as any as S.Schema<CreatePromoCodeRequest>;

export interface DeactivatePromoCodeRequest {
  /** Promo code ID (`promo_` tag). */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const DeactivatePromoCodeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/promo_codes/{id}/deactivate", code: 200 }),
  ),
).annotate({
  identifier: "DeactivatePromoCodeRequest",
}) as any as S.Schema<DeactivatePromoCodeRequest>;

export interface DeletePromoCodeRequest {
  /** Promo code ID (`promo_` tag). */
  id: string;
}
export const DeletePromoCodeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/promo_codes/{id}", code: 200 })),
).annotate({
  identifier: "DeletePromoCodeRequest",
}) as any as S.Schema<DeletePromoCodeRequest>;

export interface DeletePromoCodeResponse {
  deleted: boolean;
  id: string;
}
export const DeletePromoCodeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "DeletePromoCodeResponse",
}) as any as S.Schema<DeletePromoCodeResponse>;

export type ListPromoCodesRequestStatus =
  | "active"
  | "inactive"
  | "archived"
  | "expired";
export const ListPromoCodesRequestStatus = /*@__PURE__*/ S.String;

export type ListPromoCodesRequestProductIdsList = Array<string>;
export const ListPromoCodesRequestProductIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListPromoCodesRequestProductIdsList>;

export type ListPromoCodesRequestPlanIdsList = Array<string>;
export const ListPromoCodesRequestPlanIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListPromoCodesRequestPlanIdsList>;

export type ListPromoCodesRequestOrder = "created_at";
export const ListPromoCodesRequestOrder = /*@__PURE__*/ S.String;

export type ListPromoCodesRequestDirection = "asc" | "desc";
export const ListPromoCodesRequestDirection = /*@__PURE__*/ S.String;

export interface ListPromoCodesRequest {
  /** Account whose promo codes are listed (`biz_` tag). */
  account_id: string;
  /** Promo-code status. `expired` groups inactive and archived codes. */
  status?: ListPromoCodesRequestStatus | (string & {});
  /** Only promo codes scoped to these product IDs. */
  product_ids?: ListPromoCodesRequestProductIdsList;
  /** Only promo codes scoped to these plan IDs. */
  plan_ids?: ListPromoCodesRequestPlanIdsList;
  /** Only promo codes created before this ISO 8601 timestamp. */
  created_before?: string;
  /** Only promo codes created after this ISO 8601 timestamp. */
  created_after?: string;
  /** Sort field. */
  order?: ListPromoCodesRequestOrder | (string & {});
  /** Sort direction. */
  direction?: ListPromoCodesRequestDirection | (string & {});
  /** Number of promo codes to return from the start of the window. */
  first?: number;
  /** Cursor to paginate forwards from. */
  after?: string;
  /** Number of promo codes to return from the end of the window. */
  last?: number;
  /** Cursor to paginate backwards from. */
  before?: string;
}
export const ListPromoCodesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String.pipe(T.Query()),
    status: S.optional(ListPromoCodesRequestStatus.pipe(T.Query())),
    product_ids: S.optional(
      ListPromoCodesRequestProductIdsList.pipe(T.Query()),
    ),
    plan_ids: S.optional(ListPromoCodesRequestPlanIdsList.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListPromoCodesRequestOrder.pipe(T.Query())),
    direction: S.optional(ListPromoCodesRequestDirection.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/promo_codes", code: 200 })),
).annotate({
  identifier: "ListPromoCodesRequest",
}) as any as S.Schema<ListPromoCodesRequest>;

/** Currency used for a fixed-amount discount. */
export type PromoCodeListItemCurrency =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const PromoCodeListItemCurrency = /*@__PURE__*/ S.String;

/** How long the discount applies. */
export type PromoCodeListItemDuration = "forever" | "once" | "repeating";
export const PromoCodeListItemDuration = /*@__PURE__*/ S.String;

/** Whether the discount is percentage-based or a fixed amount. */
export type PromoCodeListItemPromoType = "percentage" | "flat_amount";
export const PromoCodeListItemPromoType = /*@__PURE__*/ S.String;

/** Promo code lifecycle status. */
export type PromoCodeListItemStatus = "active" | "inactive" | "archived";
export const PromoCodeListItemStatus = /*@__PURE__*/ S.String;

export interface PromoCodeListItem {
  /** Discount amount. Percentage discounts are represented as a decimal fraction. */
  amount_off: number;
  /** Whether the promo code is restricted to churned customers. */
  churned_users_only: boolean;
  /** Code entered at checkout. */
  code: string | null;
  /** When the promo code was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Currency used for a fixed-amount discount. */
  currency: PromoCodeListItemCurrency;
  /** How long the discount applies. */
  duration: PromoCodeListItemDuration;
  /** Whether the promo code applies only to existing memberships. */
  existing_memberships_only: boolean;
  /** When the promo code expires, as an ISO 8601 timestamp. */
  expires_at: string | null;
  /** Promo code ID, prefixed `promo_`. */
  id: string;
  /** Custom key-value metadata stored on the promo code. */
  metadata: unknown;
  /** Whether the promo code is restricted to new customers. */
  new_users_only: boolean;
  /** Whether each customer may redeem the promo code only once. */
  one_per_customer: boolean;
  /** Product the promo code is restricted to, or `null` when it is not product-scoped. */
  product: PromoCodeProduct | null;
  /** Billing intervals the discount applies to. */
  promo_duration_months: number | null;
  /** Whether the discount is percentage-based or a fixed amount. */
  promo_type: PromoCodeListItemPromoType;
  /** Promo code lifecycle status. */
  status: PromoCodeListItemStatus;
  /** Maximum uses when stock is limited. */
  stock: number;
  /** Whether the promo code has no redemption limit. */
  unlimited_stock: boolean;
  /** When the promo code was updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** Memberships that used the promo code. */
  uses: number;
}
export const PromoCodeListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount_off: S.Number,
    churned_users_only: S.Boolean,
    code: S.NullOr(S.String),
    created_at: S.String,
    currency: PromoCodeListItemCurrency,
    duration: PromoCodeListItemDuration,
    existing_memberships_only: S.Boolean,
    expires_at: S.NullOr(S.String),
    id: S.String,
    metadata: S.Unknown,
    new_users_only: S.Boolean,
    one_per_customer: S.Boolean,
    product: S.NullOr(PromoCodeProduct),
    promo_duration_months: S.NullOr(S.Number),
    promo_type: PromoCodeListItemPromoType,
    status: PromoCodeListItemStatus,
    stock: S.Number,
    unlimited_stock: S.Boolean,
    updated_at: S.String,
    uses: S.Number,
  }),
).annotate({
  identifier: "PromoCodeListItem",
}) as any as S.Schema<PromoCodeListItem>;

export type ListPromoCodesResponseDataList = Array<PromoCodeListItem>;
export const ListPromoCodesResponseDataList = /*@__PURE__*/ S.Array(
  PromoCodeListItem,
) as any as S.Schema<ListPromoCodesResponseDataList>;

export interface ListPromoCodesResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListPromoCodesResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListPromoCodesResponsePageInfo",
}) as any as S.Schema<ListPromoCodesResponsePageInfo>;

export interface ListPromoCodesResponse {
  data: ListPromoCodesResponseDataList;
  page_info: ListPromoCodesResponsePageInfo;
}
export const ListPromoCodesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListPromoCodesResponseDataList,
    page_info: ListPromoCodesResponsePageInfo,
  }),
).annotate({
  identifier: "ListPromoCodesResponse",
}) as any as S.Schema<ListPromoCodesResponse>;

export interface RetrievePromoCodeRequest {
  /** Promo code ID (`promo_` tag). */
  id: string;
}
export const RetrievePromoCodeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/promo_codes/{id}", code: 200 })),
).annotate({
  identifier: "RetrievePromoCodeRequest",
}) as any as S.Schema<RetrievePromoCodeRequest>;

/** Statuses that can be applied to promo codes through update operations. */
export type PromoCodeUpdateStatus = "active" | "inactive";
export const PromoCodeUpdateStatus = /*@__PURE__*/ S.String;

export interface UpdatePromoCodeRequest {
  /** The unique identifier of the promo code to update. */
  id: string;
  /** The status to apply to the promo code. */
  status: PromoCodeUpdateStatus | (string & {});
}
export const UpdatePromoCodeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    status: PromoCodeUpdateStatus,
  }).pipe(T.Http({ method: "PATCH", uri: "/promo_codes/{id}", code: 200 })),
).annotate({
  identifier: "UpdatePromoCodeRequest",
}) as any as S.Schema<UpdatePromoCodeRequest>;

/** The company for the promo code. */
export interface LegacyPromoCodeCompany {
  /** The unique identifier for the company. */
  id: string;
  /** The written name of the company. */
  title: string;
}
export const LegacyPromoCodeCompany = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({
  identifier: "LegacyPromoCodeCompany",
}) as any as S.Schema<LegacyPromoCodeCompany>;

/** The available currencies on the platform */
export type Currencies =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const Currencies = /*@__PURE__*/ S.String;

/** The duration setting for the promo code */
export type PromoDurations = "forever" | "once" | "repeating";
export const PromoDurations = /*@__PURE__*/ S.String;

/** The product this promo code applies to */
export interface LegacyPromoCodeProduct {
  /** The unique identifier for the product. */
  id: string;
  /** The display name of the product shown to customers on the product page and in search results. */
  title: string;
}
export const LegacyPromoCodeProduct = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({
  identifier: "LegacyPromoCodeProduct",
}) as any as S.Schema<LegacyPromoCodeProduct>;

/** The type of promo code used to discount a plan */
export type PromoTypes = "percentage" | "flat_amount";
export const PromoTypes = /*@__PURE__*/ S.String;

/** Statuses for promo codes */
export type PromoCodeStatus2 = "active" | "inactive" | "archived";
export const PromoCodeStatus2 = /*@__PURE__*/ S.String;

/** A promo code applies a discount to a plan during checkout. Promo codes can be percentage-based or fixed-amount, and can have usage limits and expiration dates. */
export interface LegacyPromoCode {
  /** The discount amount. Interpretation depends on promo_type: if 'percentage', this is the percentage (e.g., 20 means 20% off); if 'flat_amount', this is dollars off (e.g., 10.00 means $10.00 off). */
  amount_off: number;
  /** Restricts promo use to only users who have churned from the company before. */
  churned_users_only: boolean;
  /** The specific code used to apply the promo at checkout. */
  code: string | null;
  /** The company for the promo code. */
  company: LegacyPromoCodeCompany;
  /** The datetime the promo code was created. */
  created_at: string;
  /** The monetary currency of the promo code. */
  currency: Currencies;
  /** The duration of the promo. */
  duration: PromoDurations | null;
  /** Restricts promo use to only be applied to already purchased memberships. */
  existing_memberships_only: boolean;
  /** The date/time of when the promo expires. */
  expires_at: string | null;
  /** The unique identifier for the promo code. */
  id: string;
  /** Restricts promo use to only users who have never purchased from the company before. */
  new_users_only: boolean;
  /** Restricts promo use to only be applied once per customer. */
  one_per_customer: boolean;
  /** The product this promo code applies to */
  product: LegacyPromoCodeProduct | null;
  /** The number of months the promo is applied for. */
  promo_duration_months: number | null;
  /** The type (% or flat amount) of the promo. */
  promo_type: PromoTypes;
  /** Indicates if the promo code is live or disabled. */
  status: PromoCodeStatus2;
  /** The quantity limit on the number of uses. */
  stock: number;
  /** Whether or not the promo code has unlimited stock. */
  unlimited_stock: boolean;
  /** The amount of times the promo codes has been used. */
  uses: number;
}
export const LegacyPromoCode = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount_off: S.Number,
    churned_users_only: S.Boolean,
    code: S.NullOr(S.String),
    company: LegacyPromoCodeCompany,
    created_at: S.String,
    currency: Currencies,
    duration: S.NullOr(PromoDurations),
    existing_memberships_only: S.Boolean,
    expires_at: S.NullOr(S.String),
    id: S.String,
    new_users_only: S.Boolean,
    one_per_customer: S.Boolean,
    product: S.NullOr(LegacyPromoCodeProduct),
    promo_duration_months: S.NullOr(S.Number),
    promo_type: PromoTypes,
    status: PromoCodeStatus2,
    stock: S.Number,
    unlimited_stock: S.Boolean,
    uses: S.Number,
  }),
).annotate({
  identifier: "LegacyPromoCode",
}) as any as S.Schema<LegacyPromoCode>;

export type ActivatePromoCodeError = BadRequest | Conflict | WhopOpError;
/** Activate Promo Code Turns an inactive promo code back on so it can be redeemed at checkout. */
export const activatePromoCode: API.OperationMethod<
  ActivatePromoCodeRequest,
  PromoCode,
  ActivatePromoCodeError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ActivatePromoCodeRequest,
  output: PromoCode,
  errors: [BadRequest, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreatePromoCodeError = Conflict | WhopOpError;
/** Create Promo Code Creates a promo code for an account. First-party sessions may attach an affiliate. */
export const createPromoCode: API.OperationMethod<
  CreatePromoCodeRequest,
  PromoCode,
  CreatePromoCodeError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreatePromoCodeRequest,
  output: PromoCode,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeactivatePromoCodeError = Conflict | WhopOpError;
/** Deactivate Promo Code Turns off an active promo code so it can no longer be redeemed at checkout. */
export const deactivatePromoCode: API.OperationMethod<
  DeactivatePromoCodeRequest,
  PromoCode,
  DeactivatePromoCodeError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeactivatePromoCodeRequest,
  output: PromoCode,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeletePromoCodeError = WhopOpError;
/** Delete Promo Code Archives a promo code so it cannot be used in future checkouts. */
export const deletePromoCode: API.OperationMethod<
  DeletePromoCodeRequest,
  DeletePromoCodeResponse,
  DeletePromoCodeError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeletePromoCodeRequest,
  output: DeletePromoCodeResponse,
  errors: [],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListPromoCodesError = BadRequest | NotFound | WhopOpError;
/** List Promo Codes Lists promo codes for an account with cursor pagination, filters, and sorting. */
export const listPromoCodes: API.PaginatedOperationMethod<
  ListPromoCodesRequest,
  ListPromoCodesResponse,
  ListPromoCodesError,
  WhopOpContext,
  PromoCodeListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListPromoCodesRequest,
    output: ListPromoCodesResponse,
    errors: [BadRequest, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrievePromoCodeError = NotFound | WhopOpError;
/** Retrieve Promo Code Retrieves a promo code by ID. */
export const retrievePromoCode: API.OperationMethod<
  RetrievePromoCodeRequest,
  PromoCode,
  RetrievePromoCodeError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrievePromoCodeRequest,
  output: PromoCode,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdatePromoCodeError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update promo code [Legacy API — https://docs.whop.com/api-reference] Update whether a promo code can be used at checkout. Required permissions: - `promo_code:update` - `access_pass:basic:read` */
export const updatePromoCode: API.OperationMethod<
  UpdatePromoCodeRequest,
  LegacyPromoCode,
  UpdatePromoCodeError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdatePromoCodeRequest,
  output: LegacyPromoCode,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
