// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface ListChatChannelRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  company_id: string;
  product_id?: string;
}
export const ListChatChannelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    company_id: S.String.pipe(T.Query()),
    product_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/chat_channels", code: 200 })),
).annotate({
  identifier: "ListChatChannelRequest",
}) as any as S.Schema<ListChatChannelRequest>;

/** A list of words that are automatically filtered from messages in this chat. */
export type ChatChannelListItemBannedWordsList = Array<string>;
export const ChatChannelListItemBannedWordsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ChatChannelListItemBannedWordsList>;

/** The experience this chat feed is attached to. */
export interface ChatChannelListItemExperience {
  /** The unique identifier for the experience. */
  id: string;
  /** The display name of this experience shown to users in the product navigation. Maximum 255 characters. */
  name: string;
}
export const ChatChannelListItemExperience = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
  }),
).annotate({
  identifier: "ChatChannelListItemExperience",
}) as any as S.Schema<ChatChannelListItemExperience>;

/** Who can post on a chat feed */
export type WhoCanPostTypes = "everyone" | "admins";
export const WhoCanPostTypes = /*@__PURE__*/ S.String;

/** Who can react on a chat feed */
export type WhoCanReactTypes = "everyone" | "no_one";
export const WhoCanReactTypes = /*@__PURE__*/ S.String;

/** A real-time chat feed attached to an experience, with configurable moderation and posting permissions. */
export interface ChatChannelListItem {
  /** Whether media uploads such as images and videos are blocked in this chat. */
  ban_media: boolean;
  /** Whether URL links are blocked from being posted in this chat. */
  ban_urls: boolean;
  /** A list of words that are automatically filtered from messages in this chat. */
  banned_words: ChatChannelListItemBannedWordsList;
  /** The experience this chat feed is attached to. */
  experience: ChatChannelListItemExperience;
  /** The unique identifier for the entity */
  id: string;
  /** The minimum number of seconds a user must wait between consecutive messages. Null if no cooldown is enforced. */
  user_posts_cooldown_seconds: number | null;
  /** The permission level controlling which users can send messages in this chat. */
  who_can_post: WhoCanPostTypes;
  /** The permission level controlling which users can add reactions in this chat. */
  who_can_react: WhoCanReactTypes;
}
export const ChatChannelListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ban_media: S.Boolean,
    ban_urls: S.Boolean,
    banned_words: ChatChannelListItemBannedWordsList,
    experience: ChatChannelListItemExperience,
    id: S.String,
    user_posts_cooldown_seconds: S.NullOr(S.Number),
    who_can_post: WhoCanPostTypes,
    who_can_react: WhoCanReactTypes,
  }),
).annotate({
  identifier: "ChatChannelListItem",
}) as any as S.Schema<ChatChannelListItem>;

/** A list of nodes. */
export type ListChatChannelResponseDataList = Array<ChatChannelListItem>;
export const ListChatChannelResponseDataList = /*@__PURE__*/ S.Array(
  ChatChannelListItem,
) as any as S.Schema<ListChatChannelResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListChatChannelResponse {
  /** A list of nodes. */
  data: ListChatChannelResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListChatChannelResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListChatChannelResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListChatChannelResponse",
}) as any as S.Schema<ListChatChannelResponse>;

export interface RetrieveChatChannelRequest {
  /** The unique identifier of the chat channel or experience to retrieve. */
  id: string;
}
export const RetrieveChatChannelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/chat_channels/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveChatChannelRequest",
}) as any as S.Schema<RetrieveChatChannelRequest>;

/** A list of words that are automatically filtered from messages in this chat. */
export type ChatChannelBannedWordsList = Array<string>;
export const ChatChannelBannedWordsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ChatChannelBannedWordsList>;

/** The experience this chat feed is attached to. */
export type ChatChannelExperience = ChatChannelListItemExperience;
export const ChatChannelExperience = ChatChannelListItemExperience;

/** A real-time chat feed attached to an experience, with configurable moderation and posting permissions. */
export interface ChatChannel {
  /** Whether media uploads such as images and videos are blocked in this chat. */
  ban_media: boolean;
  /** Whether URL links are blocked from being posted in this chat. */
  ban_urls: boolean;
  /** A list of words that are automatically filtered from messages in this chat. */
  banned_words: ChatChannelBannedWordsList;
  /** The experience this chat feed is attached to. */
  experience: ChatChannelListItemExperience;
  /** The unique identifier for the entity */
  id: string;
  /** The minimum number of seconds a user must wait between consecutive messages. Null if no cooldown is enforced. */
  user_posts_cooldown_seconds: number | null;
  /** The permission level controlling which users can send messages in this chat. */
  who_can_post: WhoCanPostTypes;
  /** The permission level controlling which users can add reactions in this chat. */
  who_can_react: WhoCanReactTypes;
}
export const ChatChannel = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ban_media: S.Boolean,
    ban_urls: S.Boolean,
    banned_words: ChatChannelBannedWordsList,
    experience: ChatChannelListItemExperience,
    id: S.String,
    user_posts_cooldown_seconds: S.NullOr(S.Number),
    who_can_post: WhoCanPostTypes,
    who_can_react: WhoCanReactTypes,
  }),
).annotate({ identifier: "ChatChannel" }) as any as S.Schema<ChatChannel>;

/** A list of words that are automatically blocked from messages in this chat channel. For example, ['spam', 'scam']. */
export type UpdateChatChannelRequestBannedWordsList = Array<string>;
export const UpdateChatChannelRequestBannedWordsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpdateChatChannelRequestBannedWordsList>;

export interface UpdateChatChannelRequest {
  /** The unique identifier of the chat channel to update. Accepts either an experience ID (e.g. 'exp_xxxxx') or a chat channel ID. */
  id: string;
  /** Whether media uploads such as images and videos are banned in this chat channel. */
  ban_media?: boolean | null;
  /** Whether URLs and links are banned from being posted in this chat channel. */
  ban_urls?: boolean | null;
  /** A list of words that are automatically blocked from messages in this chat channel. For example, ['spam', 'scam']. */
  banned_words?: UpdateChatChannelRequestBannedWordsList | null;
  /** The minimum number of seconds a user must wait between sending messages in this chat channel. */
  user_posts_cooldown_seconds?: number | null;
  /** Controls which roles are allowed to send messages in this chat channel. */
  who_can_post?: WhoCanPostTypes | (string & {}) | null;
  /** Controls which roles are allowed to add reactions to messages in this chat channel. */
  who_can_react?: WhoCanReactTypes | (string & {}) | null;
}
export const UpdateChatChannelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    ban_media: S.optional(S.NullOr(S.Boolean)),
    ban_urls: S.optional(S.NullOr(S.Boolean)),
    banned_words: S.optional(S.NullOr(UpdateChatChannelRequestBannedWordsList)),
    user_posts_cooldown_seconds: S.optional(S.NullOr(S.Number)),
    who_can_post: S.optional(S.NullOr(WhoCanPostTypes)),
    who_can_react: S.optional(S.NullOr(WhoCanReactTypes)),
  }).pipe(T.Http({ method: "PATCH", uri: "/chat_channels/{id}", code: 200 })),
).annotate({
  identifier: "UpdateChatChannelRequest",
}) as any as S.Schema<UpdateChatChannelRequest>;

export type ListChatChannelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List chat channels [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of chat channels within a specific company, with optional filtering by product. Required permissions: - `chat:read` */
export const listChatChannel: API.PaginatedOperationMethod<
  ListChatChannelRequest,
  ListChatChannelResponse,
  ListChatChannelError,
  WhopOpContext,
  ChatChannelListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListChatChannelRequest,
    output: ListChatChannelResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveChatChannelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve chat channel [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing chat channel. Required permissions: - `chat:read` */
export const retrieveChatChannel: API.OperationMethod<
  RetrieveChatChannelRequest,
  ChatChannel,
  RetrieveChatChannelError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveChatChannelRequest,
  output: ChatChannel,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateChatChannelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update chat channel [Legacy API — https://docs.whop.com/api-reference] Update moderation settings for a chat channel, such as who can post, banned words, and media restrictions. Required permissions: - `chat:moderate` */
export const updateChatChannel: API.OperationMethod<
  UpdateChatChannelRequest,
  ChatChannel,
  UpdateChatChannelError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateChatChannelRequest,
  output: ChatChannel,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
