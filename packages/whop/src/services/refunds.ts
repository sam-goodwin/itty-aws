// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** The direction of the sort. */
export type Direction = "asc" | "desc";
export const Direction = /*@__PURE__*/ S.String;

export interface ListRefundRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  payment_id?: string;
  company_id?: string;
  user_id?: string;
  direction?: Direction | (string & {});
  created_before?: string;
  created_after?: string;
}
export const ListRefundRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    payment_id: S.optional(S.String.pipe(T.Query())),
    company_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
    direction: S.optional(Direction.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/refunds", code: 200 })),
).annotate({
  identifier: "ListRefundRequest",
}) as any as S.Schema<ListRefundRequest>;

/** The available currencies on the platform */
export type Currencies =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const Currencies = /*@__PURE__*/ S.String;

/** The original payment that this refund was issued against. Null if the payment is no longer available. */
export interface RefundListItemPayment {
  /** The unique identifier for the payment. */
  id: string;
}
export const RefundListItemPayment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "RefundListItemPayment",
}) as any as S.Schema<RefundListItemPayment>;

/** The different payment providers. */
export type PaymentProviders =
  | "stripe"
  | "coinbase"
  | "paypal"
  | "apple"
  | "sezzle"
  | "splitit"
  | "platform_balance"
  | "multi_psp"
  | "adyen"
  | "claritypay"
  | "checkout_dot_com"
  | "airwallex"
  | "coinflow"
  | "sequra"
  | "dlocal"
  | "masspay"
  | "braintree";
export const PaymentProviders = /*@__PURE__*/ S.String;

/** The status of the refund reference. */
export type RefundReferenceStatuses = "available" | "pending" | "unavailable";
export const RefundReferenceStatuses = /*@__PURE__*/ S.String;

/** The type of refund reference that was made available by the payment provider. */
export type RefundReferenceTypes =
  | "acquirer_reference_number"
  | "retrieval_reference_number"
  | "system_trace_audit_number";
export const RefundReferenceTypes = /*@__PURE__*/ S.String;

/** The different statuses for a Refund object */
export type RefundStatuses =
  | "pending"
  | "requires_action"
  | "succeeded"
  | "failed"
  | "canceled";
export const RefundStatuses = /*@__PURE__*/ S.String;

/** A refund represents a full or partial reversal of a payment, including the amount, status, and payment provider. */
export interface RefundListItem {
  /** The refunded amount as a decimal in the specified currency, such as 10.43 for $10.43 USD. */
  amount: number;
  /** The datetime the refund was created. */
  created_at: string;
  /** The three-letter ISO currency code for the refunded amount. */
  currency: Currencies;
  /** The unique identifier for the refund. */
  id: string;
  /** The original payment that this refund was issued against. Null if the payment is no longer available. */
  payment: RefundListItemPayment | null;
  /** The payment provider that processed the refund. */
  provider: PaymentProviders;
  /** The timestamp when the refund was created in the payment provider's system. Null if not available from the provider. */
  provider_created_at: string | null;
  /** The availability status of the refund tracking reference from the payment processor. Null if no reference was provided. */
  reference_status: RefundReferenceStatuses | null;
  /** The type of tracking reference provided by the payment processor, such as an acquirer reference number. Null if no reference was provided. */
  reference_type: RefundReferenceTypes | null;
  /** The tracking reference value from the payment processor, used to trace the refund through banking networks. Null if no reference was provided. */
  reference_value: string | null;
  /** The current processing status of the refund, such as pending, succeeded, or failed. */
  status: RefundStatuses;
}
export const RefundListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.Number,
    created_at: S.String,
    currency: Currencies,
    id: S.String,
    payment: S.NullOr(RefundListItemPayment),
    provider: PaymentProviders,
    provider_created_at: S.NullOr(S.String),
    reference_status: S.NullOr(RefundReferenceStatuses),
    reference_type: S.NullOr(RefundReferenceTypes),
    reference_value: S.NullOr(S.String),
    status: RefundStatuses,
  }),
).annotate({ identifier: "RefundListItem" }) as any as S.Schema<RefundListItem>;

/** A list of nodes. */
export type ListRefundResponseDataList = Array<RefundListItem>;
export const ListRefundResponseDataList = /*@__PURE__*/ S.Array(
  RefundListItem,
) as any as S.Schema<ListRefundResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListRefundResponse {
  /** A list of nodes. */
  data: ListRefundResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListRefundResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListRefundResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListRefundResponse",
}) as any as S.Schema<ListRefundResponse>;

export interface RetrieveRefundRequest {
  /** The unique identifier of the refund. */
  id: string;
}
export const RetrieveRefundRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/refunds/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveRefundRequest",
}) as any as S.Schema<RetrieveRefundRequest>;

/** The reason why a specific payment was billed */
export type BillingReasons =
  | "subscription_create"
  | "subscription_cycle"
  | "subscription_update"
  | "one_time"
  | "manual"
  | "subscription";
export const BillingReasons = /*@__PURE__*/ S.String;

/** Possible card brands that a payment token can have */
export type CardBrands =
  | "mastercard"
  | "visa"
  | "amex"
  | "discover"
  | "unionpay"
  | "jcb"
  | "diners"
  | "link"
  | "troy"
  | "visadankort"
  | "visabancontact"
  | "china_union_pay"
  | "rupay"
  | "jcbrupay"
  | "elo"
  | "maestro"
  | "tarjeta_naranja"
  | "cirrus"
  | "nspk_mir"
  | "verve"
  | "ebt"
  | "private_label"
  | "local_brand"
  | "uatp"
  | "wexcard"
  | "uzcard"
  | "meeza"
  | "hrg_store_card"
  | "girocard"
  | "fuel_card"
  | "dankort"
  | "carnet"
  | "atm_card"
  | "china_union_payuzcard"
  | "codensa"
  | "cabal"
  | "hipercard"
  | "jcblankapay"
  | "cmi"
  | "aura"
  | "accel"
  | "culiance"
  | "nyce"
  | "pulse"
  | "star"
  | "unknown";
export const CardBrands = /*@__PURE__*/ S.String;

/** The member attached to this payment. */
export interface RefundPaymentMember {
  /** The unique identifier for the company member. */
  id: string;
  /** The phone number for the member, if available. */
  phone: string | null;
}
export const RefundPaymentMember = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    phone: S.NullOr(S.String),
  }),
).annotate({
  identifier: "RefundPaymentMember",
}) as any as S.Schema<RefundPaymentMember>;

/** The status of a membership */
export type MembershipStatus =
  | "trialing"
  | "active"
  | "past_due"
  | "completed"
  | "canceled"
  | "expired"
  | "unresolved"
  | "drafted"
  | "canceling";
export const MembershipStatus = /*@__PURE__*/ S.String;

/** The membership attached to this payment. */
export interface RefundPaymentMembership {
  /** The unique identifier for the membership. */
  id: string;
  /** The state of the membership. */
  status: MembershipStatus;
}
export const RefundPaymentMembership = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    status: MembershipStatus,
  }),
).annotate({
  identifier: "RefundPaymentMembership",
}) as any as S.Schema<RefundPaymentMembership>;

/** The custom metadata stored on this payment. This will be copied over to the checkout configuration for which this payment was made */
export type RefundPaymentMetadataMap = { [key: string]: unknown | undefined };
export const RefundPaymentMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<RefundPaymentMetadataMap>;

/** The different types of payment methods that can be used. */
export type PaymentMethodTypes =
  | "acss_debit"
  | "addi"
  | "affirm"
  | "afterpay_clearpay"
  | "alipay"
  | "alma"
  | "amazon_pay"
  | "apple"
  | "apple_pay"
  | "au_bank_transfer"
  | "au_becs_debit"
  | "bacs_debit"
  | "bancolombia"
  | "bancontact"
  | "bank_wire"
  | "billie"
  | "bizum"
  | "blik"
  | "boleto"
  | "bre_b"
  | "ca_bank_transfer"
  | "capchase_pay"
  | "card"
  | "card_installments_three"
  | "card_installments_six"
  | "card_installments_twelve"
  | "cashapp"
  | "claritypay"
  | "coinbase"
  | "crypto"
  | "custom"
  | "customer_balance"
  | "demo_pay"
  | "efecty"
  | "eps"
  | "eu_bank_transfer"
  | "fpx"
  | "gb_bank_transfer"
  | "giropay"
  | "google_pay"
  | "gopay"
  | "grabpay"
  | "id_bank_transfer"
  | "ideal"
  | "interac"
  | "kakao_pay"
  | "klarna"
  | "klarna_pay_now"
  | "konbini"
  | "kr_card"
  | "kr_market"
  | "kriya"
  | "kueski"
  | "link"
  | "mb_way"
  | "m_pesa"
  | "mercado_pago"
  | "mobilepay"
  | "modo"
  | "mondu"
  | "multibanco"
  | "naver_pay"
  | "nequi"
  | "netbanking"
  | "ng_bank"
  | "ng_bank_transfer"
  | "ng_card"
  | "ng_market"
  | "ng_ussd"
  | "ng_wallet"
  | "nz_bank_account"
  | "oxxo"
  | "p24"
  | "pago_efectivo"
  | "pse"
  | "pay_by_bank"
  | "payco"
  | "paynow"
  | "paypal"
  | "paypay"
  | "payto"
  | "pix"
  | "platform_balance"
  | "promptpay"
  | "qris"
  | "rapipago"
  | "rechnung"
  | "revolut_pay"
  | "samsung_pay"
  | "satispay"
  | "scalapay"
  | "sencillito"
  | "sepa_debit"
  | "sequra"
  | "servipag"
  | "sezzle"
  | "shop_pay"
  | "shopeepay"
  | "sofort"
  | "south_korea_market"
  | "spei"
  | "splitit"
  | "sunbit"
  | "swish"
  | "tamara"
  | "twint"
  | "upi"
  | "us_bank_account"
  | "us_bank_transfer"
  | "venmo"
  | "vipps"
  | "webpay"
  | "wechat_pay"
  | "yape"
  | "zip"
  | "coinflow"
  | "unknown";
export const PaymentMethodTypes = /*@__PURE__*/ S.String;

/** Custom key-value pairs stored on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys `custom_cta` and `custom_cta_url`, when set, override the product's checkout call to action for this plan. */
export type RefundPaymentPlanMetadataMap = {
  [key: string]: unknown | undefined;
};
export const RefundPaymentPlanMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<RefundPaymentPlanMetadataMap>;

/** The plan attached to this payment. */
export interface RefundPaymentPlan {
  /** The unique identifier for the plan. */
  id: string;
  /** Custom key-value pairs stored on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys `custom_cta` and `custom_cta_url`, when set, override the product's checkout call to action for this plan. */
  metadata: RefundPaymentPlanMetadataMap | null;
}
export const RefundPaymentPlan = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    metadata: S.NullOr(RefundPaymentPlanMetadataMap),
  }),
).annotate({
  identifier: "RefundPaymentPlan",
}) as any as S.Schema<RefundPaymentPlan>;

/** Custom key-value pairs stored on the product and included in payment and membership webhook payloads. Max 50 keys, 100 characters per key, 500 characters per string value. */
export type RefundPaymentProductMetadataMap = {
  [key: string]: unknown | undefined;
};
export const RefundPaymentProductMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<RefundPaymentProductMetadataMap>;

/** The product this payment was made for */
export interface RefundPaymentProduct {
  /** The unique identifier for the product. */
  id: string;
  /** Custom key-value pairs stored on the product and included in payment and membership webhook payloads. Max 50 keys, 100 characters per key, 500 characters per string value. */
  metadata: RefundPaymentProductMetadataMap | null;
}
export const RefundPaymentProduct = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    metadata: S.NullOr(RefundPaymentProductMetadataMap),
  }),
).annotate({
  identifier: "RefundPaymentProduct",
}) as any as S.Schema<RefundPaymentProduct>;

/** The type of tax inclusivity applied to the receipt, for determining whether the tax is included in the final price, or paid on top. */
export type ReceiptTaxBehaviors =
  | "exclusive"
  | "inclusive"
  | "unspecified"
  | "unable_to_collect";
export const ReceiptTaxBehaviors = /*@__PURE__*/ S.String;

/** The user that made this payment. */
export interface RefundPaymentUser {
  /** The user's email address. Requires the member:email:read permission to access. Null if not authorized. */
  email: string | null;
  /** The unique identifier for the user. */
  id: string;
  /** The user's display name shown on their public profile. */
  name: string | null;
  /** The user's unique username shown on their public profile. */
  username: string;
}
export const RefundPaymentUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email: S.NullOr(S.String),
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({
  identifier: "RefundPaymentUser",
}) as any as S.Schema<RefundPaymentUser>;

/** The original payment that this refund was issued against. Null if the payment is no longer available. */
export interface RefundPayment {
  /** The machine-readable reason this charge was created, such as initial subscription purchase, renewal cycle, or one-time payment. */
  billing_reason: BillingReasons | null;
  /** Card network reported by the processor (e.g., 'visa', 'mastercard', 'amex'). Present only when the payment method type is 'card'. */
  card_brand: CardBrands | null;
  /** The last four digits of the card used to make this payment. Null if the payment was not made with a card. */
  card_last4: string | null;
  /** The datetime the payment was created. */
  created_at: string;
  /** The three-letter ISO currency code for this payment (e.g., 'usd', 'eur'). */
  currency: Currencies;
  /** When an alert came in that this transaction will be disputed */
  dispute_alerted_at: string | null;
  /** The unique identifier for the payment. */
  id: string;
  /** The member attached to this payment. */
  member: RefundPaymentMember | null;
  /** The membership attached to this payment. */
  membership: RefundPaymentMembership | null;
  /** The custom metadata stored on this payment. This will be copied over to the checkout configuration for which this payment was made */
  metadata: RefundPaymentMetadataMap | null;
  /** The time at which this payment was successfully collected. Null if the payment has not yet succeeded. As a Unix timestamp. */
  paid_at: string | null;
  /** The type of payment instrument used for this payment (e.g., card, Cash App, iDEAL, Klarna, crypto). Null when the processor does not supply a type. */
  payment_method_type: PaymentMethodTypes | null;
  /** The plan attached to this payment. */
  plan: RefundPaymentPlan | null;
  /** The product this payment was made for */
  product: RefundPaymentProduct | null;
  /** The subtotal to show to the creator (excluding buyer fees). */
  subtotal: number | null;
  /** The calculated amount of the sales/VAT tax (if applicable). */
  tax_amount: number | null;
  /** The type of tax inclusivity applied to the payment, for determining whether the tax is included in the final price, or paid on top. */
  tax_behavior: ReceiptTaxBehaviors | null;
  /** The amount of tax that has been refunded (if applicable). */
  tax_refunded_amount: number | null;
  /** The total to show to the creator (excluding buyer fees). */
  total: number | null;
  /** The total in USD to show to the creator (excluding buyer fees). */
  usd_total: number | null;
  /** The user that made this payment. */
  user: RefundPaymentUser | null;
}
export const RefundPayment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing_reason: S.NullOr(BillingReasons),
    card_brand: S.NullOr(CardBrands),
    card_last4: S.NullOr(S.String),
    created_at: S.String,
    currency: Currencies,
    dispute_alerted_at: S.NullOr(S.String),
    id: S.String,
    member: S.NullOr(RefundPaymentMember),
    membership: S.NullOr(RefundPaymentMembership),
    metadata: S.NullOr(RefundPaymentMetadataMap),
    paid_at: S.NullOr(S.String),
    payment_method_type: S.NullOr(PaymentMethodTypes),
    plan: S.NullOr(RefundPaymentPlan),
    product: S.NullOr(RefundPaymentProduct),
    subtotal: S.NullOr(S.Number),
    tax_amount: S.NullOr(S.Number),
    tax_behavior: S.NullOr(ReceiptTaxBehaviors),
    tax_refunded_amount: S.NullOr(S.Number),
    total: S.NullOr(S.Number),
    usd_total: S.NullOr(S.Number),
    user: S.NullOr(RefundPaymentUser),
  }),
).annotate({ identifier: "RefundPayment" }) as any as S.Schema<RefundPayment>;

/** A refund represents a full or partial reversal of a payment, including the amount, status, and payment provider. */
export interface Refund {
  /** The refunded amount as a decimal in the specified currency, such as 10.43 for $10.43 USD. */
  amount: number;
  /** The datetime the refund was created. */
  created_at: string;
  /** The three-letter ISO currency code for the refunded amount. */
  currency: Currencies;
  /** The unique identifier for the refund. */
  id: string;
  /** The original payment that this refund was issued against. Null if the payment is no longer available. */
  payment: RefundPayment | null;
  /** The payment provider that processed the refund. */
  provider: PaymentProviders;
  /** The timestamp when the refund was created in the payment provider's system. Null if not available from the provider. */
  provider_created_at: string | null;
  /** The availability status of the refund tracking reference from the payment processor. Null if no reference was provided. */
  reference_status: RefundReferenceStatuses | null;
  /** The type of tracking reference provided by the payment processor, such as an acquirer reference number. Null if no reference was provided. */
  reference_type: RefundReferenceTypes | null;
  /** The tracking reference value from the payment processor, used to trace the refund through banking networks. Null if no reference was provided. */
  reference_value: string | null;
  /** The current processing status of the refund, such as pending, succeeded, or failed. */
  status: RefundStatuses;
}
export const Refund = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.Number,
    created_at: S.String,
    currency: Currencies,
    id: S.String,
    payment: S.NullOr(RefundPayment),
    provider: PaymentProviders,
    provider_created_at: S.NullOr(S.String),
    reference_status: S.NullOr(RefundReferenceStatuses),
    reference_type: S.NullOr(RefundReferenceTypes),
    reference_value: S.NullOr(S.String),
    status: RefundStatuses,
  }),
).annotate({ identifier: "Refund" }) as any as S.Schema<Refund>;

export type ListRefundError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List refunds [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of refunds, with optional filtering by payment, company, user, and creation date. Required permissions: - `payment:basic:read` */
export const listRefund: API.PaginatedOperationMethod<
  ListRefundRequest,
  ListRefundResponse,
  ListRefundError,
  WhopOpContext,
  RefundListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListRefundRequest,
    output: ListRefundResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveRefundError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve refund [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing refund. Required permissions: - `payment:basic:read` - `plan:basic:read` - `access_pass:basic:read` - `member:email:read` - `member:basic:read` - `member:phone:read` */
export const retrieveRefund: API.OperationMethod<
  RetrieveRefundRequest,
  Refund,
  RetrieveRefundError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveRefundRequest,
  output: Refund,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
