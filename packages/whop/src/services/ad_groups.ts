// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** IDs of saved audiences to exclude from delivery, prefixed `adaud_`. */
export type AdGroupAudiencesBodyExcludeList = Array<string>;
export const AdGroupAudiencesBodyExcludeList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupAudiencesBodyExcludeList>;

/** IDs of saved audiences to deliver to, prefixed `adaud_`. */
export type AdGroupAudiencesBodyIncludeList = Array<string>;
export const AdGroupAudiencesBodyIncludeList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupAudiencesBodyIncludeList>;

/** Saved audiences to deliver to or exclude. Can't be combined with demographics.automatic. */
export interface AdGroupAudiencesBody {
  /** IDs of saved audiences to exclude from delivery, prefixed `adaud_`. */
  exclude?: AdGroupAudiencesBodyExcludeList;
  /** IDs of saved audiences to deliver to, prefixed `adaud_`. */
  include?: AdGroupAudiencesBodyIncludeList;
}
export const AdGroupAudiencesBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exclude: S.optional(AdGroupAudiencesBodyExcludeList),
    include: S.optional(AdGroupAudiencesBodyIncludeList),
  }),
).annotate({
  identifier: "AdGroupAudiencesBody",
}) as any as S.Schema<AdGroupAudiencesBody>;

/** How delivery bids are set in the ad auction. Target-based strategies use `desired_cost_per_result`. */
export type CreateAdGroupRequestBidType =
  | "minimum_cost"
  | "average_target"
  | "maximum_target";
export const CreateAdGroupRequestBidType = /*@__PURE__*/ S.String;

/** Whether budget_amount is spent per day (`daily`) or over the ad group's full run (`lifetime`). */
export type CreateAdGroupRequestBudgetType = "daily" | "lifetime";
export const CreateAdGroupRequestBudgetType = /*@__PURE__*/ S.String;

export type ConversionEventCase0 =
  | "purchase"
  | "add_to_cart"
  | "initiated_checkout"
  | "add_payment_info"
  | "complete_registration"
  | "lead"
  | "content_view"
  | "search"
  | "contact"
  | "customize_product"
  | "donate"
  | "find_location"
  | "schedule"
  | "start_trial"
  | "submit_application"
  | "subscribe";
export const ConversionEventCase0 = /*@__PURE__*/ S.String;

/** The pixel event optimized for. A standard event, or any custom pixel event name. */
export type ConversionEvent = ConversionEventCase0 | string;
export const ConversionEvent =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ConversionEvent>;

/** Where the outcome being optimized for occurs, such as a website visit, social-profile visit, messaging conversation, ad interaction, or lead-form submission. The lead form itself is set on the ad. */
export type CreateAdGroupRequestConversionLocation =
  | "website"
  | "profile"
  | "instagram_and_facebook"
  | "instagram_profile"
  | "messaging"
  | "on_ad"
  | "instant_forms"
  | "instant_forms_and_messenger"
  | "website_and_instant_forms";
export const CreateAdGroupRequestConversionLocation = /*@__PURE__*/ S.String;

/** Gender to target. */
export type AdGroupDemographicsBodyGender = "all" | "male" | "female";
export const AdGroupDemographicsBodyGender = /*@__PURE__*/ S.String;

/** Age, gender, and automatic-audience targeting. */
export interface AdGroupDemographicsBody {
  /** Turn on automatic audience targeting (Advantage+ on Meta): the platform can deliver beyond the ages, genders, and detailed targeting you set, treating them as suggestions. */
  automatic?: boolean;
  /** Gender to target. */
  gender?: AdGroupDemographicsBodyGender | (string & {});
  /** Oldest age to target. */
  maximum_age?: number;
  /** Youngest age to target. */
  minimum_age?: number;
}
export const AdGroupDemographicsBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    automatic: S.optional(S.Boolean),
    gender: S.optional(AdGroupDemographicsBodyGender),
    maximum_age: S.optional(S.Number),
    minimum_age: S.optional(S.Number),
  }),
).annotate({
  identifier: "AdGroupDemographicsBody",
}) as any as S.Schema<AdGroupDemographicsBody>;

/** On ad platforms that scope behavior categories, what this one is measured on. Send back the value the targeting_options endpoint returned alongside the id. */
export type AdGroupDetailedTargetingBodyBehaviorsItemBehaviorType =
  | "video"
  | "creator"
  | "hashtag";
export const AdGroupDetailedTargetingBodyBehaviorsItemBehaviorType =
  /*@__PURE__*/ S.String;

export interface AdGroupDetailedTargetingBodyBehaviorsItem {
  /** On ad platforms that scope behavior categories, what this one is measured on. Send back the value the targeting_options endpoint returned alongside the id. */
  behavior_type?:
    | AdGroupDetailedTargetingBodyBehaviorsItemBehaviorType
    | (string & {});
  /** The ad platform's ID for the category in its targeting taxonomy. */
  id: string;
  /** Category name, such as `Frequent travelers`. */
  name?: string;
  /** On ad platforms that scope behavior categories, how many days of activity the category covers. */
  period?: number;
}
export const AdGroupDetailedTargetingBodyBehaviorsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      behavior_type: S.optional(
        AdGroupDetailedTargetingBodyBehaviorsItemBehaviorType,
      ),
      id: S.String,
      name: S.optional(S.String),
      period: S.optional(S.Number),
    }),
  ).annotate({
    identifier: "AdGroupDetailedTargetingBodyBehaviorsItem",
  }) as any as S.Schema<AdGroupDetailedTargetingBodyBehaviorsItem>;

/** Behavior categories to target, such as frequent travelers. */
export type AdGroupDetailedTargetingBodyBehaviorsList =
  Array<AdGroupDetailedTargetingBodyBehaviorsItem>;
export const AdGroupDetailedTargetingBodyBehaviorsList = /*@__PURE__*/ S.Array(
  AdGroupDetailedTargetingBodyBehaviorsItem,
) as any as S.Schema<AdGroupDetailedTargetingBodyBehaviorsList>;

/** Kind of demographic the category belongs to. */
export type AdGroupDetailedTargetingBodyDemographicsItemType =
  | "life_events"
  | "industries"
  | "income"
  | "family_statuses"
  | "work_employers"
  | "work_positions"
  | "education_schools"
  | "education_majors";
export const AdGroupDetailedTargetingBodyDemographicsItemType =
  /*@__PURE__*/ S.String;

export interface AdGroupDetailedTargetingBodyDemographicsItem {
  /** The ad platform's ID for the category in its targeting taxonomy. */
  id: string;
  /** Category name, such as `Recently moved`. */
  name?: string;
  /** Kind of demographic the category belongs to. */
  type: AdGroupDetailedTargetingBodyDemographicsItemType | (string & {});
}
export const AdGroupDetailedTargetingBodyDemographicsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      id: S.String,
      name: S.optional(S.String),
      type: AdGroupDetailedTargetingBodyDemographicsItemType,
    }),
  ).annotate({
    identifier: "AdGroupDetailedTargetingBodyDemographicsItem",
  }) as any as S.Schema<AdGroupDetailedTargetingBodyDemographicsItem>;

/** Demographic categories to target, such as life events, industries, work employers, job titles, schools, or majors. */
export type AdGroupDetailedTargetingBodyDemographicsList =
  Array<AdGroupDetailedTargetingBodyDemographicsItem>;
export const AdGroupDetailedTargetingBodyDemographicsList =
  /*@__PURE__*/ S.Array(
    AdGroupDetailedTargetingBodyDemographicsItem,
  ) as any as S.Schema<AdGroupDetailedTargetingBodyDemographicsList>;

export interface AdGroupDetailedTargetingBodyInterestsItem {
  /** The ad platform's ID for the category in its targeting taxonomy. */
  id: string;
  /** Category name, such as `Movies`. */
  name?: string;
}
export const AdGroupDetailedTargetingBodyInterestsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      id: S.String,
      name: S.optional(S.String),
    }),
  ).annotate({
    identifier: "AdGroupDetailedTargetingBodyInterestsItem",
  }) as any as S.Schema<AdGroupDetailedTargetingBodyInterestsItem>;

/** Interest categories to target, such as an interest in movies. */
export type AdGroupDetailedTargetingBodyInterestsList =
  Array<AdGroupDetailedTargetingBodyInterestsItem>;
export const AdGroupDetailedTargetingBodyInterestsList = /*@__PURE__*/ S.Array(
  AdGroupDetailedTargetingBodyInterestsItem,
) as any as S.Schema<AdGroupDetailedTargetingBodyInterestsList>;

/** Interest, behavior, and demographic targeting, using categories from the ad platform's targeting taxonomy. Entries across interests, behaviors, and demographics are OR'd together (anyone matching any entry is reached), matching Ads Manager's detailed-targeting box. At most 100 entries per section. Can't be combined with demographics.automatic, and unavailable to campaigns with special_ad_categories. */
export interface AdGroupDetailedTargetingBody {
  /** Behavior categories to target, such as frequent travelers. */
  behaviors?: AdGroupDetailedTargetingBodyBehaviorsList;
  /** Demographic categories to target, such as life events, industries, work employers, job titles, schools, or majors. */
  demographics?: AdGroupDetailedTargetingBodyDemographicsList;
  /** Interest categories to target, such as an interest in movies. */
  interests?: AdGroupDetailedTargetingBodyInterestsList;
}
export const AdGroupDetailedTargetingBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    behaviors: S.optional(AdGroupDetailedTargetingBodyBehaviorsList),
    demographics: S.optional(AdGroupDetailedTargetingBodyDemographicsList),
    interests: S.optional(AdGroupDetailedTargetingBodyInterestsList),
  }),
).annotate({
  identifier: "AdGroupDetailedTargetingBody",
}) as any as S.Schema<AdGroupDetailedTargetingBody>;

/** Operating system to target. */
export type AdGroupDevicesBodyOperatingSystemsItemOs = "ios" | "android";
export const AdGroupDevicesBodyOperatingSystemsItemOs = /*@__PURE__*/ S.String;

export interface AdGroupDevicesBodyOperatingSystemsItem {
  /** Lowest OS version to target, such as `18.0`. Omit to target any version. */
  minimum_version?: string;
  /** Operating system to target. */
  os: AdGroupDevicesBodyOperatingSystemsItemOs | (string & {});
}
export const AdGroupDevicesBodyOperatingSystemsItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      minimum_version: S.optional(S.String),
      os: AdGroupDevicesBodyOperatingSystemsItemOs,
    }),
).annotate({
  identifier: "AdGroupDevicesBodyOperatingSystemsItem",
}) as any as S.Schema<AdGroupDevicesBodyOperatingSystemsItem>;

/** Operating systems to target. Empty targets all operating systems. */
export type AdGroupDevicesBodyOperatingSystemsList =
  Array<AdGroupDevicesBodyOperatingSystemsItem>;
export const AdGroupDevicesBodyOperatingSystemsList = /*@__PURE__*/ S.Array(
  AdGroupDevicesBodyOperatingSystemsItem,
) as any as S.Schema<AdGroupDevicesBodyOperatingSystemsList>;

export type AdGroupDevicesBodyPlatformsItem = "mobile" | "desktop";
export const AdGroupDevicesBodyPlatformsItem = /*@__PURE__*/ S.String;

/** Device types to target. Empty targets all devices. */
export type AdGroupDevicesBodyPlatformsList = Array<
  AdGroupDevicesBodyPlatformsItem | (string & {})
>;
export const AdGroupDevicesBodyPlatformsList = /*@__PURE__*/ S.Array(
  AdGroupDevicesBodyPlatformsItem,
) as any as S.Schema<AdGroupDevicesBodyPlatformsList>;

/** Device platforms and operating systems to target. */
export interface AdGroupDevicesBody {
  /** Operating systems to target. Empty targets all operating systems. */
  operating_systems?: AdGroupDevicesBodyOperatingSystemsList;
  /** Device types to target. Empty targets all devices. */
  platforms?: AdGroupDevicesBodyPlatformsList;
}
export const AdGroupDevicesBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    operating_systems: S.optional(AdGroupDevicesBodyOperatingSystemsList),
    platforms: S.optional(AdGroupDevicesBodyPlatformsList),
  }),
).annotate({
  identifier: "AdGroupDevicesBody",
}) as any as S.Schema<AdGroupDevicesBody>;

/** Cap on how often one person sees ads from this ad group. Only available on campaigns with the `awareness` objective. */
export interface CreateAdGroupRequestFrequencyCap {
  /** Most times one person can be shown ads from this ad group within the window. */
  maximum_impressions?: number;
  /** Length of the rolling window, in days. */
  per_days?: number;
}
export const CreateAdGroupRequestFrequencyCap = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    maximum_impressions: S.optional(S.Number),
    per_days: S.optional(S.Number),
  }),
).annotate({
  identifier: "CreateAdGroupRequestFrequencyCap",
}) as any as S.Schema<CreateAdGroupRequestFrequencyCap>;

/** Languages to target, as ISO 639 codes such as `en` or `es`. Empty or omitted targets all languages. */
export type CreateAdGroupRequestLanguagesList = Array<string>;
export const CreateAdGroupRequestLanguagesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateAdGroupRequestLanguagesList>;

export type CreateAdGroupRequestMessageAppsItem =
  | "messenger"
  | "instagram"
  | "whatsapp";
export const CreateAdGroupRequestMessageAppsItem = /*@__PURE__*/ S.String;

/** Apps the conversation opens in. Required when setting `conversion_location` to `messaging`, and rejected unless the ad group's conversion location is `messaging`. */
export type CreateAdGroupRequestMessageAppsList = Array<
  CreateAdGroupRequestMessageAppsItem | (string & {})
>;
export const CreateAdGroupRequestMessageAppsList = /*@__PURE__*/ S.Array(
  CreateAdGroupRequestMessageAppsItem,
) as any as S.Schema<CreateAdGroupRequestMessageAppsList>;

/** The result the ad group's delivery is optimized to get the most of. */
export type CreateAdGroupRequestOptimizationGoal =
  | "conversions"
  | "link_clicks"
  | "landing_page_views"
  | "reach"
  | "impressions"
  | "engagement"
  | "conversations"
  | "video_views"
  | "two_second_views"
  | "page_likes"
  | "social_profile"
  | "ad_recall_lift"
  | "event_responses"
  | "reminders_set"
  | "lead_generation"
  | "quality_lead"
  | "value"
  | "profile_and_page_engagement";
export const CreateAdGroupRequestOptimizationGoal = /*@__PURE__*/ S.String;

export type CreateAdGroupRequestPlacementsCase0 = "automatic";
export const CreateAdGroupRequestPlacementsCase0 = /*@__PURE__*/ S.String;

/** Platform the ads run on. */
export type CreateAdGroupRequestPlacementsCase1ItemPlatform =
  | "facebook"
  | "instagram"
  | "messenger"
  | "audience_network"
  | "threads"
  | "whatsapp";
export const CreateAdGroupRequestPlacementsCase1ItemPlatform =
  /*@__PURE__*/ S.String;

/** Positions to target within the platform, such as `feed` or `story`. Omit to target all of the platform's positions. */
export type CreateAdGroupRequestPlacementsCase1ItemPositionsList =
  Array<string>;
export const CreateAdGroupRequestPlacementsCase1ItemPositionsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateAdGroupRequestPlacementsCase1ItemPositionsList>;

export interface CreateAdGroupRequestPlacementsCase1Item {
  /** Platform the ads run on. */
  platform: CreateAdGroupRequestPlacementsCase1ItemPlatform | (string & {});
  /** Positions to target within the platform, such as `feed` or `story`. Omit to target all of the platform's positions. */
  positions?: CreateAdGroupRequestPlacementsCase1ItemPositionsList;
}
export const CreateAdGroupRequestPlacementsCase1Item = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      platform: CreateAdGroupRequestPlacementsCase1ItemPlatform,
      positions: S.optional(
        CreateAdGroupRequestPlacementsCase1ItemPositionsList,
      ),
    }),
).annotate({
  identifier: "CreateAdGroupRequestPlacementsCase1Item",
}) as any as S.Schema<CreateAdGroupRequestPlacementsCase1Item>;

export type CreateAdGroupRequestPlacementsCase1List =
  Array<CreateAdGroupRequestPlacementsCase1Item>;
export const CreateAdGroupRequestPlacementsCase1List = /*@__PURE__*/ S.Array(
  CreateAdGroupRequestPlacementsCase1Item,
) as any as S.Schema<CreateAdGroupRequestPlacementsCase1List>;

/** `automatic` to let the ad platform choose placements, or the list of platforms and positions to target. Omit a platform's positions to target all of them. Valid positions per platform: - `facebook`: `feed`, `right_hand_column`, `marketplace`, `search`, `profile_feed`, `notification`, `story`, `instream_video`, `facebook_reels`, `facebook_reels_overlay`, `biz_disco_feed` - `instagram`: `stream`, `story`, `explore`, `explore_home`, `reels`, `profile_feed`, `profile_reels`, `ig_search` - `messenger`: `story` - `audience_network`: `classic`, `rewarded_video` - `threads`: `threads_stream` - `whatsapp`: `status` */
export type CreateAdGroupRequestPlacements =
  | CreateAdGroupRequestPlacementsCase0
  | CreateAdGroupRequestPlacementsCase1List;
export const CreateAdGroupRequestPlacements =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateAdGroupRequestPlacements>;

export interface AdGroupGeoLocationsBodyCitiesItem {
  /** The ad platform's key for the city in its location taxonomy. */
  key: string;
  /** City name, such as `Austin`. */
  name?: string;
}
export const AdGroupGeoLocationsBodyCitiesItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.String,
    name: S.optional(S.String),
  }),
).annotate({
  identifier: "AdGroupGeoLocationsBodyCitiesItem",
}) as any as S.Schema<AdGroupGeoLocationsBodyCitiesItem>;

/** Cities, keyed by the ad platform's location taxonomy. */
export type AdGroupGeoLocationsBodyCitiesList =
  Array<AdGroupGeoLocationsBodyCitiesItem>;
export const AdGroupGeoLocationsBodyCitiesList = /*@__PURE__*/ S.Array(
  AdGroupGeoLocationsBodyCitiesItem,
) as any as S.Schema<AdGroupGeoLocationsBodyCitiesList>;

/** Countries, as ISO 3166-1 alpha-2 codes such as `US`. */
export type AdGroupGeoLocationsBodyCountriesList = Array<string>;
export const AdGroupGeoLocationsBodyCountriesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupGeoLocationsBodyCountriesList>;

/** Multi-country groups such as `worldwide` or `europe`. Include-only — groups can't be excluded. */
export type AdGroupGeoLocationsBodyCountryGroupsList = Array<string>;
export const AdGroupGeoLocationsBodyCountryGroupsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupGeoLocationsBodyCountryGroupsList>;

/** Unit for `radius`. Defaults to `mile`. */
export type AdGroupGeoLocationsBodyCustomLocationsItemDistanceUnit =
  | "mile"
  | "kilometer";
export const AdGroupGeoLocationsBodyCustomLocationsItemDistanceUnit =
  /*@__PURE__*/ S.String;

export interface AdGroupGeoLocationsBodyCustomLocationsItem {
  /** Unit for `radius`. Defaults to `mile`. */
  distance_unit?:
    | AdGroupGeoLocationsBodyCustomLocationsItemDistanceUnit
    | (string & {});
  /** Latitude of the center point. */
  latitude: number;
  /** Longitude of the center point. */
  longitude: number;
  /** Label for the location, such as a city or address. */
  name?: string;
  /** Radius around the center point: 1-50 miles or 1-80 kilometers. */
  radius: number;
}
export const AdGroupGeoLocationsBodyCustomLocationsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      distance_unit: S.optional(
        AdGroupGeoLocationsBodyCustomLocationsItemDistanceUnit,
      ),
      latitude: S.Number,
      longitude: S.Number,
      name: S.optional(S.String),
      radius: S.Number,
    }),
  ).annotate({
    identifier: "AdGroupGeoLocationsBodyCustomLocationsItem",
  }) as any as S.Schema<AdGroupGeoLocationsBodyCustomLocationsItem>;

/** Circular areas, each a coordinate plus a radius. At most 200 across include and exclude. */
export type AdGroupGeoLocationsBodyCustomLocationsList =
  Array<AdGroupGeoLocationsBodyCustomLocationsItem>;
export const AdGroupGeoLocationsBodyCustomLocationsList = /*@__PURE__*/ S.Array(
  AdGroupGeoLocationsBodyCustomLocationsItem,
) as any as S.Schema<AdGroupGeoLocationsBodyCustomLocationsList>;

/** US states and DC, as ISO 3166-2 codes such as `US-CA`. US territories (`PR`, `GU`, `VI`, `AS`, `MP`) and everywhere outside the US are targeted through `countries`. */
export type AdGroupGeoLocationsBodyRegionsList = Array<string>;
export const AdGroupGeoLocationsBodyRegionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupGeoLocationsBodyRegionsList>;

export interface AdGroupGeoLocationsBodyZipsItemCase1 {
  /** The ad platform's key for the ZIP or postal code. */
  key: string;
}
export const AdGroupGeoLocationsBodyZipsItemCase1 = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      key: S.String,
    }),
).annotate({
  identifier: "AdGroupGeoLocationsBodyZipsItemCase1",
}) as any as S.Schema<AdGroupGeoLocationsBodyZipsItemCase1>;

export type AdGroupGeoLocationsBodyZipsItem =
  | string
  | AdGroupGeoLocationsBodyZipsItemCase1;
export const AdGroupGeoLocationsBodyZipsItem =
  /*@__PURE__*/ S.Unknown as any as S.Schema<AdGroupGeoLocationsBodyZipsItem>;

/** ZIP and postal codes, keyed by the ad platform's location taxonomy. Meta keys these `COUNTRY:CODE`, as `US:78756` — a bare code is ambiguous, because the same one exists in several countries. TikTok takes the bare code. */
export type AdGroupGeoLocationsBodyZipsList =
  Array<AdGroupGeoLocationsBodyZipsItem>;
export const AdGroupGeoLocationsBodyZipsList = /*@__PURE__*/ S.Array(
  AdGroupGeoLocationsBodyZipsItem,
) as any as S.Schema<AdGroupGeoLocationsBodyZipsList>;

export interface AdGroupGeoLocationsBody {
  /** Cities, keyed by the ad platform's location taxonomy. */
  cities?: AdGroupGeoLocationsBodyCitiesList;
  /** Countries, as ISO 3166-1 alpha-2 codes such as `US`. */
  countries?: AdGroupGeoLocationsBodyCountriesList;
  /** Multi-country groups such as `worldwide` or `europe`. Include-only — groups can't be excluded. */
  country_groups?: AdGroupGeoLocationsBodyCountryGroupsList;
  /** Circular areas, each a coordinate plus a radius. At most 200 across include and exclude. */
  custom_locations?: AdGroupGeoLocationsBodyCustomLocationsList;
  /** US states and DC, as ISO 3166-2 codes such as `US-CA`. US territories (`PR`, `GU`, `VI`, `AS`, `MP`) and everywhere outside the US are targeted through `countries`. */
  regions?: AdGroupGeoLocationsBodyRegionsList;
  /** ZIP and postal codes, keyed by the ad platform's location taxonomy. Meta keys these `COUNTRY:CODE`, as `US:78756` — a bare code is ambiguous, because the same one exists in several countries. TikTok takes the bare code. */
  zips?: AdGroupGeoLocationsBodyZipsList;
}
export const AdGroupGeoLocationsBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cities: S.optional(AdGroupGeoLocationsBodyCitiesList),
    countries: S.optional(AdGroupGeoLocationsBodyCountriesList),
    country_groups: S.optional(AdGroupGeoLocationsBodyCountryGroupsList),
    custom_locations: S.optional(AdGroupGeoLocationsBodyCustomLocationsList),
    regions: S.optional(AdGroupGeoLocationsBodyRegionsList),
    zips: S.optional(AdGroupGeoLocationsBodyZipsList),
  }),
).annotate({
  identifier: "AdGroupGeoLocationsBody",
}) as any as S.Schema<AdGroupGeoLocationsBody>;

/** Locations to target and exclude. */
export interface AdGroupRegionsBody {
  /** Locations excluded from targeting. Country groups can't be excluded. */
  exclude?: AdGroupGeoLocationsBody;
  /** Locations the ad group targets. */
  include?: AdGroupGeoLocationsBody;
}
export const AdGroupRegionsBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exclude: S.optional(AdGroupGeoLocationsBody),
    include: S.optional(AdGroupGeoLocationsBody),
  }),
).annotate({
  identifier: "AdGroupRegionsBody",
}) as any as S.Schema<AdGroupRegionsBody>;

/** Initial status (default: `active`). */
export type CreateAdGroupRequestStatus = "active" | "paused";
export const CreateAdGroupRequestStatus = /*@__PURE__*/ S.String;

export interface CreateAdGroupRequest {
  /** The ad campaign to create the ad group in, prefixed `adcamp_`. */
  ad_campaign_id: string;
  /** Saved audiences to deliver to or exclude. Can't be combined with demographics.automatic. */
  audiences?: AdGroupAudiencesBody;
  /** How delivery bids are set in the ad auction. Target-based strategies use `desired_cost_per_result`. */
  bid_type?: CreateAdGroupRequestBidType | (string & {});
  /** This ad group's budget, in the ad account's currency. Omit when the budget is set on the campaign instead. */
  budget_amount?: number;
  /** Whether budget_amount is spent per day (`daily`) or over the ad group's full run (`lifetime`). */
  budget_type?: CreateAdGroupRequestBudgetType | (string & {});
  conversion_event?: ConversionEvent | null;
  /** Where the outcome being optimized for occurs, such as a website visit, social-profile visit, messaging conversation, ad interaction, or lead-form submission. The lead form itself is set on the ad. */
  conversion_location?: CreateAdGroupRequestConversionLocation | (string & {});
  /** Age, gender, and automatic-audience targeting. */
  demographics?: AdGroupDemographicsBody;
  /** Cost per result to aim for (`average_target`) or never exceed (`maximum_target`). */
  desired_cost_per_result?: number;
  /** Interest, behavior, and demographic targeting, using categories from the ad platform's targeting taxonomy. Entries across interests, behaviors, and demographics are OR'd together (anyone matching any entry is reached), matching Ads Manager's detailed-targeting box. At most 100 entries per section. Can't be combined with demographics.automatic, and unavailable to campaigns with special_ad_categories. Send the complete intended state — a section you omit is cleared. */
  detailed_targeting?: AdGroupDetailedTargetingBody;
  /** Device platforms and operating systems to target. */
  devices?: AdGroupDevicesBody;
  /** Let the ad platform automatically mix and match this ad group's creatives and copy to find the best-performing combinations. Set at creation; can't be changed afterward. */
  dynamic_creative?: boolean;
  /** When the ad group stops delivering, as an ISO 8601 timestamp. Omit to run until paused. */
  ends_at?: string;
  /** Cap on how often one person sees ads from this ad group. Only available on campaigns with the `awareness` objective. */
  frequency_cap?: CreateAdGroupRequestFrequencyCap;
  /** Languages to target, as ISO 639 codes such as `en` or `es`. Empty or omitted targets all languages. */
  languages?: CreateAdGroupRequestLanguagesList;
  /** Apps the conversation opens in. Required when setting `conversion_location` to `messaging`, and rejected unless the ad group's conversion location is `messaging`. */
  message_apps?: CreateAdGroupRequestMessageAppsList;
  /** Minimum the ad group tries to spend each day. */
  minimum_daily_spend?: number;
  /** The result the ad group's delivery is optimized to get the most of. */
  optimization_goal?: CreateAdGroupRequestOptimizationGoal | (string & {});
  /** `automatic` to let the ad platform choose placements, or the list of platforms and positions to target. Omit a platform's positions to target all of them. Valid positions per platform: - `facebook`: `feed`, `right_hand_column`, `marketplace`, `search`, `profile_feed`, `notification`, `story`, `instream_video`, `facebook_reels`, `facebook_reels_overlay`, `biz_disco_feed` - `instagram`: `stream`, `story`, `explore`, `explore_home`, `reels`, `profile_feed`, `profile_reels`, `ig_search` - `messenger`: `story` - `audience_network`: `classic`, `rewarded_video` - `threads`: `threads_stream` - `whatsapp`: `status` */
  placements?: CreateAdGroupRequestPlacements;
  /** Locations to target and exclude. */
  regions?: AdGroupRegionsBody;
  /** When the ad group starts delivering, as an ISO 8601 timestamp. Omit to start as soon as it's active. */
  starts_at?: string;
  /** Initial status (default: `active`). */
  status?: CreateAdGroupRequestStatus | (string & {});
  /** The display name of the ad group. */
  title?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateAdGroupRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ad_campaign_id: S.String,
    audiences: S.optional(AdGroupAudiencesBody),
    bid_type: S.optional(CreateAdGroupRequestBidType),
    budget_amount: S.optional(S.Number),
    budget_type: S.optional(CreateAdGroupRequestBudgetType),
    conversion_event: S.optional(S.NullOr(ConversionEvent)),
    conversion_location: S.optional(CreateAdGroupRequestConversionLocation),
    demographics: S.optional(AdGroupDemographicsBody),
    desired_cost_per_result: S.optional(S.Number),
    detailed_targeting: S.optional(AdGroupDetailedTargetingBody),
    devices: S.optional(AdGroupDevicesBody),
    dynamic_creative: S.optional(S.Boolean),
    ends_at: S.optional(S.String),
    frequency_cap: S.optional(CreateAdGroupRequestFrequencyCap),
    languages: S.optional(CreateAdGroupRequestLanguagesList),
    message_apps: S.optional(CreateAdGroupRequestMessageAppsList),
    minimum_daily_spend: S.optional(S.Number),
    optimization_goal: S.optional(CreateAdGroupRequestOptimizationGoal),
    placements: S.optional(CreateAdGroupRequestPlacements),
    regions: S.optional(AdGroupRegionsBody),
    starts_at: S.optional(S.String),
    status: S.optional(CreateAdGroupRequestStatus),
    title: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/ad_groups", code: 200 })),
).annotate({
  identifier: "CreateAdGroupRequest",
}) as any as S.Schema<CreateAdGroupRequest>;

export interface AdEntityReference {
  /** The referenced entity's id. */
  id: string;
}
export const AdEntityReference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "AdEntityReference",
}) as any as S.Schema<AdEntityReference>;

export type AdGroupAudiencesExcludeList = Array<string>;
export const AdGroupAudiencesExcludeList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupAudiencesExcludeList>;

export type AdGroupAudiencesIncludeList = Array<string>;
export const AdGroupAudiencesIncludeList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupAudiencesIncludeList>;

export interface AdGroupAudiences {
  exclude: AdGroupAudiencesExcludeList;
  include: AdGroupAudiencesIncludeList;
}
export const AdGroupAudiences = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exclude: AdGroupAudiencesExcludeList,
    include: AdGroupAudiencesIncludeList,
  }),
).annotate({
  identifier: "AdGroupAudiences",
}) as any as S.Schema<AdGroupAudiences>;

/** How delivery bids are set in the ad auction. Target-based strategies use `desired_cost_per_result`. */
export type AdGroupBidType =
  | "minimum_cost"
  | "average_target"
  | "maximum_target";
export const AdGroupBidType = /*@__PURE__*/ S.String;

/** Whether `budget_amount` is spent per day (`daily`) or over the ad group's full run (`lifetime`). */
export type AdGroupBudgetType = "daily" | "lifetime";
export const AdGroupBudgetType = /*@__PURE__*/ S.String;

/** Where the outcome being optimized for occurs, such as a website visit, social-profile visit, messaging conversation, ad interaction, or lead-form submission. */
export type AdGroupConversionLocation =
  | "website"
  | "profile"
  | "instagram_and_facebook"
  | "instagram_profile"
  | "messaging"
  | "on_ad"
  | "instant_forms"
  | "instant_forms_and_messenger"
  | "website_and_instant_forms";
export const AdGroupConversionLocation = /*@__PURE__*/ S.String;

/** Whether ads in this ad group are delivering right now, and if not, why. When several states apply at once, the highest-precedence one is returned. */
export type AdGroupDeliveryStatus =
  | "all_ads_rejected"
  | "rejected"
  | "draft"
  | "no_ads"
  | "campaign_paused"
  | "paused"
  | "processing"
  | "issues"
  | "scheduled"
  | "completed"
  | "ads_off"
  | "learning_limited"
  | "learning"
  | "active";
export const AdGroupDeliveryStatus = /*@__PURE__*/ S.String;

/** Gender targeted. */
export type AdGroupDemographicsGender = "all" | "male" | "female";
export const AdGroupDemographicsGender = /*@__PURE__*/ S.String;

export interface AdGroupDemographics {
  /** Whether automatic audience targeting is on (Advantage+ on Meta). When `true`, the platform can deliver beyond the ages, genders, and detailed targeting you set, treating them as suggestions. */
  automatic: boolean;
  /** Gender targeted. */
  gender: AdGroupDemographicsGender;
  /** Oldest age targeted. `null` when no maximum is set. */
  maximum_age: number | null;
  /** Youngest age targeted. `null` when no minimum is set. */
  minimum_age: number | null;
}
export const AdGroupDemographics = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    automatic: S.Boolean,
    gender: AdGroupDemographicsGender,
    maximum_age: S.NullOr(S.Number),
    minimum_age: S.NullOr(S.Number),
  }),
).annotate({
  identifier: "AdGroupDemographics",
}) as any as S.Schema<AdGroupDemographics>;

/** On ad platforms that scope behavior categories, what this one is measured on. Send back the value the targeting_options endpoint returned alongside the id. Absent on platforms that don't scope them. */
export type AdGroupBehaviorCategoryBehaviorType =
  | "video"
  | "creator"
  | "hashtag";
export const AdGroupBehaviorCategoryBehaviorType = /*@__PURE__*/ S.String;

export interface AdGroupBehaviorCategory {
  /** On ad platforms that scope behavior categories, what this one is measured on. Send back the value the targeting_options endpoint returned alongside the id. Absent on platforms that don't scope them. */
  behavior_type?: AdGroupBehaviorCategoryBehaviorType;
  /** The ad platform's ID for the category in its targeting taxonomy. */
  id: string;
  /** Category name, such as `Frequent travelers`. */
  name?: string;
  /** On ad platforms that scope behavior categories, how many days of activity the category covers. Absent on platforms that don't scope them. */
  period?: number;
}
export const AdGroupBehaviorCategory = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    behavior_type: S.optional(AdGroupBehaviorCategoryBehaviorType),
    id: S.String,
    name: S.optional(S.String),
    period: S.optional(S.Number),
  }),
).annotate({
  identifier: "AdGroupBehaviorCategory",
}) as any as S.Schema<AdGroupBehaviorCategory>;

export type AdGroupDetailedTargetingBehaviorsList =
  Array<AdGroupBehaviorCategory>;
export const AdGroupDetailedTargetingBehaviorsList = /*@__PURE__*/ S.Array(
  AdGroupBehaviorCategory,
) as any as S.Schema<AdGroupDetailedTargetingBehaviorsList>;

/** Kind of demographic the category belongs to. */
export type AdGroupDemographicCategoryType =
  | "life_events"
  | "industries"
  | "income"
  | "family_statuses"
  | "work_employers"
  | "work_positions"
  | "education_schools"
  | "education_majors";
export const AdGroupDemographicCategoryType = /*@__PURE__*/ S.String;

export interface AdGroupDemographicCategory {
  /** The ad platform's ID for the category in its targeting taxonomy. */
  id: string;
  /** Category name, such as `Recently moved`. */
  name?: string;
  /** Kind of demographic the category belongs to. */
  type: AdGroupDemographicCategoryType;
}
export const AdGroupDemographicCategory = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.optional(S.String),
    type: AdGroupDemographicCategoryType,
  }),
).annotate({
  identifier: "AdGroupDemographicCategory",
}) as any as S.Schema<AdGroupDemographicCategory>;

export type AdGroupDetailedTargetingDemographicsList =
  Array<AdGroupDemographicCategory>;
export const AdGroupDetailedTargetingDemographicsList = /*@__PURE__*/ S.Array(
  AdGroupDemographicCategory,
) as any as S.Schema<AdGroupDetailedTargetingDemographicsList>;

export type AdGroupTargetingCategory =
  AdGroupDetailedTargetingBodyInterestsItem;
export const AdGroupTargetingCategory =
  AdGroupDetailedTargetingBodyInterestsItem;

export type AdGroupDetailedTargetingInterestsList =
  Array<AdGroupDetailedTargetingBodyInterestsItem>;
export const AdGroupDetailedTargetingInterestsList = /*@__PURE__*/ S.Array(
  AdGroupDetailedTargetingBodyInterestsItem,
) as any as S.Schema<AdGroupDetailedTargetingInterestsList>;

export interface AdGroupDetailedTargeting {
  behaviors: AdGroupDetailedTargetingBehaviorsList;
  demographics: AdGroupDetailedTargetingDemographicsList;
  interests: AdGroupDetailedTargetingInterestsList;
}
export const AdGroupDetailedTargeting = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    behaviors: AdGroupDetailedTargetingBehaviorsList,
    demographics: AdGroupDetailedTargetingDemographicsList,
    interests: AdGroupDetailedTargetingInterestsList,
  }),
).annotate({
  identifier: "AdGroupDetailedTargeting",
}) as any as S.Schema<AdGroupDetailedTargeting>;

/** Operating system targeted. */
export type AdGroupOperatingSystemOs = "ios" | "android";
export const AdGroupOperatingSystemOs = /*@__PURE__*/ S.String;

export interface AdGroupOperatingSystem {
  /** Lowest OS version targeted, such as `18.0`. Absent when any version qualifies. */
  minimum_version?: string;
  /** Operating system targeted. */
  os: AdGroupOperatingSystemOs;
}
export const AdGroupOperatingSystem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    minimum_version: S.optional(S.String),
    os: AdGroupOperatingSystemOs,
  }),
).annotate({
  identifier: "AdGroupOperatingSystem",
}) as any as S.Schema<AdGroupOperatingSystem>;

export type AdGroupDevicesOperatingSystemsList = Array<AdGroupOperatingSystem>;
export const AdGroupDevicesOperatingSystemsList = /*@__PURE__*/ S.Array(
  AdGroupOperatingSystem,
) as any as S.Schema<AdGroupDevicesOperatingSystemsList>;

/** Device types targeted. Empty targets all devices. */
export type AdGroupDevicesPlatformsItem = "mobile" | "desktop";
export const AdGroupDevicesPlatformsItem = /*@__PURE__*/ S.String;

export type AdGroupDevicesPlatformsList = Array<AdGroupDevicesPlatformsItem>;
export const AdGroupDevicesPlatformsList = /*@__PURE__*/ S.Array(
  AdGroupDevicesPlatformsItem,
) as any as S.Schema<AdGroupDevicesPlatformsList>;

export interface AdGroupDevices {
  operating_systems: AdGroupDevicesOperatingSystemsList;
  platforms: AdGroupDevicesPlatformsList;
}
export const AdGroupDevices = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    operating_systems: AdGroupDevicesOperatingSystemsList,
    platforms: AdGroupDevicesPlatformsList,
  }),
).annotate({ identifier: "AdGroupDevices" }) as any as S.Schema<AdGroupDevices>;

export interface AdGroupFrequencyCap {
  /** Most times one person can be shown ads from this ad group within the window. */
  maximum_impressions: number;
  /** Length of the rolling window, in days. */
  per_days: number | null;
}
export const AdGroupFrequencyCap = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    maximum_impressions: S.Number,
    per_days: S.NullOr(S.Number),
  }),
).annotate({
  identifier: "AdGroupFrequencyCap",
}) as any as S.Schema<AdGroupFrequencyCap>;

/** The type of resource the issue is attached to. */
export type AdPlatformIssueResourceType = "ad_campaign" | "ad_group" | "ad";
export const AdPlatformIssueResourceType = /*@__PURE__*/ S.String;

export interface AdPlatformIssue {
  /** Unique identifier for the issue. */
  id: string;
  /** A description of what the issue is and how it can be resolved. */
  message: string;
  /** The ID of the campaign, ad group, or ad the issue is attached to. */
  resource_id: string | null;
  /** The type of resource the issue is attached to. */
  resource_type: AdPlatformIssueResourceType;
}
export const AdPlatformIssue = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    message: S.String,
    resource_id: S.NullOr(S.String),
    resource_type: AdPlatformIssueResourceType,
  }),
).annotate({
  identifier: "AdPlatformIssue",
}) as any as S.Schema<AdPlatformIssue>;

export type AdGroupIssuesList = Array<AdPlatformIssue>;
export const AdGroupIssuesList = /*@__PURE__*/ S.Array(
  AdPlatformIssue,
) as any as S.Schema<AdGroupIssuesList>;

export type AdGroupLanguagesList = Array<string>;
export const AdGroupLanguagesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupLanguagesList>;

/** Apps the conversation opens in when `conversion_location` is `messaging`. Empty for other conversion locations. */
export type AdGroupMessageAppsItem = "messenger" | "instagram" | "whatsapp";
export const AdGroupMessageAppsItem = /*@__PURE__*/ S.String;

export type AdGroupMessageAppsList = Array<AdGroupMessageAppsItem>;
export const AdGroupMessageAppsList = /*@__PURE__*/ S.Array(
  AdGroupMessageAppsItem,
) as any as S.Schema<AdGroupMessageAppsList>;

/** The result the ad group's delivery is optimized to get the most of. */
export type AdGroupOptimizationGoal =
  | "conversions"
  | "link_clicks"
  | "landing_page_views"
  | "reach"
  | "impressions"
  | "engagement"
  | "conversations"
  | "video_views"
  | "two_second_views"
  | "page_likes"
  | "social_profile"
  | "ad_recall_lift"
  | "event_responses"
  | "reminders_set"
  | "lead_generation"
  | "quality_lead"
  | "value"
  | "profile_and_page_engagement";
export const AdGroupOptimizationGoal = /*@__PURE__*/ S.String;

/** Publisher platform where the ad is eligible to appear. */
export type AdGroupPlacementPlatform =
  | "facebook"
  | "instagram"
  | "messenger"
  | "audience_network"
  | "threads"
  | "whatsapp";
export const AdGroupPlacementPlatform = /*@__PURE__*/ S.String;

export type AdGroupPlacementPositionsList = Array<string>;
export const AdGroupPlacementPositionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupPlacementPositionsList>;

export interface AdGroupPlacement {
  /** Publisher platform where the ad is eligible to appear. */
  platform: AdGroupPlacementPlatform;
  positions: AdGroupPlacementPositionsList;
}
export const AdGroupPlacement = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    platform: AdGroupPlacementPlatform,
    positions: AdGroupPlacementPositionsList,
  }),
).annotate({
  identifier: "AdGroupPlacement",
}) as any as S.Schema<AdGroupPlacement>;

export type AdGroupPlacementsList = Array<AdGroupPlacement>;
export const AdGroupPlacementsList = /*@__PURE__*/ S.Array(
  AdGroupPlacement,
) as any as S.Schema<AdGroupPlacementsList>;

export interface AdGroupCity {
  /** The ad platform's key for the city in its location taxonomy. */
  key: string;
  /** City name, such as `Austin`. Absent when the platform doesn't return one. */
  name?: string;
}
export const AdGroupCity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.String,
    name: S.optional(S.String),
  }),
).annotate({ identifier: "AdGroupCity" }) as any as S.Schema<AdGroupCity>;

export type AdGroupGeoLocationsCitiesList = Array<AdGroupCity>;
export const AdGroupGeoLocationsCitiesList = /*@__PURE__*/ S.Array(
  AdGroupCity,
) as any as S.Schema<AdGroupGeoLocationsCitiesList>;

export type AdGroupGeoLocationsCountriesList = Array<string>;
export const AdGroupGeoLocationsCountriesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupGeoLocationsCountriesList>;

export type AdGroupGeoLocationsCountryGroupsList = Array<string>;
export const AdGroupGeoLocationsCountryGroupsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupGeoLocationsCountryGroupsList>;

/** Unit for `radius`. */
export type AdGroupCustomLocationDistanceUnit = "mile" | "kilometer";
export const AdGroupCustomLocationDistanceUnit = /*@__PURE__*/ S.String;

export interface AdGroupCustomLocation {
  /** Unit for `radius`. */
  distance_unit: AdGroupCustomLocationDistanceUnit;
  /** Latitude of the center point. */
  latitude: number;
  /** Longitude of the center point. */
  longitude: number;
  /** Label for the location, such as a city or address. Absent when the location has no label. */
  name?: string;
  /** Radius around the center point, in `distance_unit`. */
  radius: number;
}
export const AdGroupCustomLocation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    distance_unit: AdGroupCustomLocationDistanceUnit,
    latitude: S.Number,
    longitude: S.Number,
    name: S.optional(S.String),
    radius: S.Number,
  }),
).annotate({
  identifier: "AdGroupCustomLocation",
}) as any as S.Schema<AdGroupCustomLocation>;

export type AdGroupGeoLocationsCustomLocationsList =
  Array<AdGroupCustomLocation>;
export const AdGroupGeoLocationsCustomLocationsList = /*@__PURE__*/ S.Array(
  AdGroupCustomLocation,
) as any as S.Schema<AdGroupGeoLocationsCustomLocationsList>;

export type AdGroupGeoLocationsRegionsList = Array<string>;
export const AdGroupGeoLocationsRegionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupGeoLocationsRegionsList>;

export type AdGroupGeoLocationsZipsList = Array<string>;
export const AdGroupGeoLocationsZipsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdGroupGeoLocationsZipsList>;

export interface AdGroupGeoLocations {
  cities: AdGroupGeoLocationsCitiesList;
  countries: AdGroupGeoLocationsCountriesList;
  country_groups: AdGroupGeoLocationsCountryGroupsList;
  custom_locations: AdGroupGeoLocationsCustomLocationsList;
  regions: AdGroupGeoLocationsRegionsList;
  zips: AdGroupGeoLocationsZipsList;
}
export const AdGroupGeoLocations = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cities: AdGroupGeoLocationsCitiesList,
    countries: AdGroupGeoLocationsCountriesList,
    country_groups: AdGroupGeoLocationsCountryGroupsList,
    custom_locations: AdGroupGeoLocationsCustomLocationsList,
    regions: AdGroupGeoLocationsRegionsList,
    zips: AdGroupGeoLocationsZipsList,
  }),
).annotate({
  identifier: "AdGroupGeoLocations",
}) as any as S.Schema<AdGroupGeoLocations>;

export interface AdGroupRegions {
  /** Locations excluded from targeting. Country groups can't be excluded. */
  exclude: AdGroupGeoLocations;
  /** Locations the ad group targets. */
  include: AdGroupGeoLocations;
}
export const AdGroupRegions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exclude: AdGroupGeoLocations,
    include: AdGroupGeoLocations,
  }),
).annotate({ identifier: "AdGroupRegions" }) as any as S.Schema<AdGroupRegions>;

/** The Whop pixel conversion event whose attributed count represents results — the optimization goal, or the highest-volume attributed event for campaigns that budget per ad group. Null when the goal isn't a Whop-attributed event. */
export type AdGroupResultEvent =
  | "purchase"
  | "lead"
  | "schedule"
  | "submit_application"
  | "contact"
  | "complete_registration"
  | "view_content"
  | "add_to_cart"
  | "custom"
  | "messaging_conversation";
export const AdGroupResultEvent = /*@__PURE__*/ S.String;

/** Whether the ad group is enabled. `active` and `paused` are set by you; `rejected` means it failed ad review; `duplicating` is a copy still being filled in. */
export type AdGroupStatus = "active" | "paused" | "rejected" | "duplicating";
export const AdGroupStatus = /*@__PURE__*/ S.String;

export interface AdGroup {
  /** The ad campaign this ad group belongs to. */
  ad_campaign: AdEntityReference;
  /** USD value attributed to add-to-cart events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  added_to_cart_value: number;
  /** Whop pixel-attributed add-to-cart events, last-click. */
  added_to_carts: number;
  /** Saved audiences this ad group delivers to or excludes. */
  audiences: AdGroupAudiences;
  /** How delivery bids are set in the ad auction. Target-based strategies use `desired_cost_per_result`. */
  bid_type: AdGroupBidType | null;
  /** This ad group's budget, in the ad account's currency. `null` when the budget is set on the campaign instead. */
  budget_amount: number | null;
  /** Whether `budget_amount` is spent per day (`daily`) or over the ad group's full run (`lifetime`). */
  budget_type: AdGroupBudgetType | null;
  /** Clicks divided by impressions, between 0 and 1. */
  click_through_rate: number;
  /** The number of clicks. */
  clicks: number;
  /** USD value attributed to complete-registration events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  completed_registration_value: number;
  /** Whop pixel-attributed complete-registration events, last-click. */
  completed_registrations: number;
  /** USD value attributed to contact events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  contact_value: number;
  /** Whop pixel-attributed contact events, last-click. */
  contacts: number;
  conversion_event?: ConversionEvent | null;
  /** Where the outcome being optimized for occurs, such as a website visit, social-profile visit, messaging conversation, ad interaction, or lead-form submission. */
  conversion_location?: AdGroupConversionLocation | null;
  /** Spend divided by attributed add-to-cart events; null when they are not the goal and none are attributed. */
  cost_per_added_to_cart: number | null;
  /** Spend divided by clicks; 0 when there are no clicks. */
  cost_per_click: number;
  /** Spend divided by attributed complete-registration events; null when they are not the goal and none are attributed. */
  cost_per_completed_registration: number | null;
  /** Spend divided by attributed contact events; null when contacts are not the goal and none are attributed. */
  cost_per_contact: number | null;
  /** Spend divided by attributed leads; null when leads are not a goal and none are attributed. */
  cost_per_lead: number | null;
  /** Spend per 1,000 impressions; 0 when there are no impressions. */
  cost_per_mille: number;
  /** Spend divided by attributed purchases; null when purchases are not a goal and none are attributed. */
  cost_per_purchase: number | null;
  /** Spend divided by Whop pixel-attributed results; null when nothing Whop-attributable is being optimized for. */
  cost_per_result: number | null;
  /** Spend divided by attributed schedule events; null when schedules are not the goal and none are attributed. */
  cost_per_schedule: number | null;
  /** Spend divided by attributed submit-application events; null when they are not the goal and none are attributed. */
  cost_per_submitted_application: number | null;
  /** Spend divided by unique clicks; null when there are no unique clicks. */
  cost_per_unique_click: number | null;
  /** Spend divided by attributed view-content events; null when they are not the goal and none are attributed. */
  cost_per_viewed_content: number | null;
  /** When the ad group was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Whop pixel-attributed custom (merchant-defined) conversion events, last-click, across all custom event names. */
  custom_conversions: number;
  /** Whop pixel-attributed custom conversions, keyed by your event name with its last-click count as the value. Empty when no named custom events are attributed. Custom events fired without a name are counted in custom_conversions but omitted here, so these values sum to at most custom_conversions. */
  custom_event_counts: unknown;
  /** Conversion value attributed to each custom event, keyed by event name like custom_event_counts. Sums the value passed to whop.track, normalized to USD; events fired without a value contribute 0. */
  custom_event_values: unknown;
  /** Whether ads in this ad group are delivering right now, and if not, why. When several states apply at once, the highest-precedence one is returned. */
  delivery_status: AdGroupDeliveryStatus;
  /** Age, gender, and automatic-audience targeting. */
  demographics: AdGroupDemographics;
  /** Cost per result to aim for (`average_target`) or never exceed (`maximum_target`). `null` for `minimum_cost` bidding. */
  desired_cost_per_result: number | null;
  /** Interest, behavior, and demographic targeting, using categories from the ad platform's targeting taxonomy. Entries across interests, behaviors, and demographics are OR'd together (anyone matching any entry is reached), matching Ads Manager's detailed-targeting box. Can't be combined with automatic audience targeting. Special ad category campaigns are limited to approved interests. */
  detailed_targeting: AdGroupDetailedTargeting;
  /** Device platforms and operating systems targeted. */
  devices: AdGroupDevices;
  /** Whether the ad platform automatically mixes and matches this ad group's creatives and copy to find the best-performing combinations. */
  dynamic_creative?: boolean;
  /** When the ad group stops delivering, as an ISO 8601 timestamp. `null` when it runs until paused. */
  ends_at: string | null;
  /** Platform-reported impressions divided by reach. */
  frequency: number | null;
  /** Cap on how often one person sees ads from this ad group. Only available on campaigns with the `awareness` objective; `null` when uncapped. */
  frequency_cap: AdGroupFrequencyCap | null;
  /** Unique identifier for the ad group, prefixed `adgrp_`. */
  id: string;
  /** The number of impressions. */
  impressions: number;
  issues: AdGroupIssuesList;
  languages: AdGroupLanguagesList;
  /** USD value attributed to lead events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  lead_value: number;
  /** Whop pixel-attributed leads, last-click. */
  leads: number;
  message_apps?: AdGroupMessageAppsList;
  /** Minimum the ad group tries to spend each day. `null` when no floor is set. */
  minimum_daily_spend?: number | null;
  /** The result the ad group's delivery is optimized to get the most of. */
  optimization_goal: AdGroupOptimizationGoal | null;
  placements: AdGroupPlacementsList;
  /** USD value of pixel-attributed purchases. */
  purchase_value: number;
  /** Whop pixel-attributed purchases, last-click. */
  purchases: number;
  /** The number of unique people who saw this. */
  reach: number;
  /** Locations targeted and excluded. */
  regions: AdGroupRegions;
  /** The Whop pixel conversion event whose attributed count represents results — the optimization goal, or the highest-volume attributed event for campaigns that budget per ad group. Null when the goal isn't a Whop-attributed event. */
  result_event: AdGroupResultEvent | null;
  /** The merchant-defined event name when result_event is custom; null for the standard events. */
  result_event_name: string | null;
  /** The Whop pixel-attributed count behind result_event. When a campaign's ad groups optimize different goals there is no single result_event (it is null), and this is instead the sum of each ad group's own attributed results. Null when nothing Whop-attributable is being optimized for. */
  results: number | null;
  /** Purchase value divided by spend, both in USD (a currency-neutral ratio); 0 when there is no spend. */
  return_on_ad_spend: number;
  /** USD value attributed to schedule events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  schedule_value: number;
  /** Whop pixel-attributed schedule events, last-click. */
  schedules: number;
  /** The amount charged, in spend_currency. */
  spend: number;
  /** The ISO 4217 currency code of all monetary metrics. */
  spend_currency: string | null;
  /** When the ad group starts delivering, as an ISO 8601 timestamp. `null` when it starts as soon as it's active. */
  starts_at: string | null;
  /** Whether the ad group is enabled. `active` and `paused` are set by you; `rejected` means it failed ad review; `duplicating` is a copy still being filled in. */
  status: AdGroupStatus;
  /** USD value attributed to submit-application events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  submitted_application_value: number;
  /** Whop pixel-attributed submit-application events, last-click. */
  submitted_applications: number;
  /** Display name of the ad group. */
  title: string | null;
  /** Unique clicks divided by impressions, between 0 and 1. */
  unique_click_through_rate: number | null;
  /** People who clicked, reported by the Whop pixel, counted once per person. */
  unique_clicks: number;
  /** When the ad group was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** USD value attributed to view-content events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  viewed_content_value: number;
  /** Whop pixel-attributed view-content events, last-click. */
  viewed_contents: number;
}
export const AdGroup = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ad_campaign: AdEntityReference,
    added_to_cart_value: S.Number,
    added_to_carts: S.Number,
    audiences: AdGroupAudiences,
    bid_type: S.NullOr(AdGroupBidType),
    budget_amount: S.NullOr(S.Number),
    budget_type: S.NullOr(AdGroupBudgetType),
    click_through_rate: S.Number,
    clicks: S.Number,
    completed_registration_value: S.Number,
    completed_registrations: S.Number,
    contact_value: S.Number,
    contacts: S.Number,
    conversion_event: S.optional(S.NullOr(ConversionEvent)),
    conversion_location: S.optional(S.NullOr(AdGroupConversionLocation)),
    cost_per_added_to_cart: S.NullOr(S.Number),
    cost_per_click: S.Number,
    cost_per_completed_registration: S.NullOr(S.Number),
    cost_per_contact: S.NullOr(S.Number),
    cost_per_lead: S.NullOr(S.Number),
    cost_per_mille: S.Number,
    cost_per_purchase: S.NullOr(S.Number),
    cost_per_result: S.NullOr(S.Number),
    cost_per_schedule: S.NullOr(S.Number),
    cost_per_submitted_application: S.NullOr(S.Number),
    cost_per_unique_click: S.NullOr(S.Number),
    cost_per_viewed_content: S.NullOr(S.Number),
    created_at: S.String,
    custom_conversions: S.Number,
    custom_event_counts: S.Unknown,
    custom_event_values: S.Unknown,
    delivery_status: AdGroupDeliveryStatus,
    demographics: AdGroupDemographics,
    desired_cost_per_result: S.NullOr(S.Number),
    detailed_targeting: AdGroupDetailedTargeting,
    devices: AdGroupDevices,
    dynamic_creative: S.optional(S.Boolean),
    ends_at: S.NullOr(S.String),
    frequency: S.NullOr(S.Number),
    frequency_cap: S.NullOr(AdGroupFrequencyCap),
    id: S.String,
    impressions: S.Number,
    issues: AdGroupIssuesList,
    languages: AdGroupLanguagesList,
    lead_value: S.Number,
    leads: S.Number,
    message_apps: S.optional(AdGroupMessageAppsList),
    minimum_daily_spend: S.optional(S.NullOr(S.Number)),
    optimization_goal: S.NullOr(AdGroupOptimizationGoal),
    placements: AdGroupPlacementsList,
    purchase_value: S.Number,
    purchases: S.Number,
    reach: S.Number,
    regions: AdGroupRegions,
    result_event: S.NullOr(AdGroupResultEvent),
    result_event_name: S.NullOr(S.String),
    results: S.NullOr(S.Number),
    return_on_ad_spend: S.Number,
    schedule_value: S.Number,
    schedules: S.Number,
    spend: S.Number,
    spend_currency: S.NullOr(S.String),
    starts_at: S.NullOr(S.String),
    status: AdGroupStatus,
    submitted_application_value: S.Number,
    submitted_applications: S.Number,
    title: S.NullOr(S.String),
    unique_click_through_rate: S.NullOr(S.Number),
    unique_clicks: S.Number,
    updated_at: S.String,
    viewed_content_value: S.Number,
    viewed_contents: S.Number,
  }),
).annotate({ identifier: "AdGroup" }) as any as S.Schema<AdGroup>;

export interface DeleteAdGroupRequest {
  /** The ad group ID. */
  id: string;
}
export const DeleteAdGroupRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/ad_groups/{id}", code: 200 })),
).annotate({
  identifier: "DeleteAdGroupRequest",
}) as any as S.Schema<DeleteAdGroupRequest>;

export interface DeleteAdGroupResponse {
  /** Always true. */
  deleted: boolean;
  /** ID of the deleted ad group. */
  id: string;
}
export const DeleteAdGroupResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "DeleteAdGroupResponse",
}) as any as S.Schema<DeleteAdGroupResponse>;

export interface DuplicateAdGroupRequest {
  /** The ad group ID. */
  id: string;
  /** Number of copies to create (1-10). Defaults to 1. */
  count?: number;
  /** Whether the copied ads keep the original posts' engagement (likes, comments, shares). Defaults to false. */
  preserve_engagement?: boolean;
  /** Campaign to duplicate into. Defaults to the ad group's own campaign. */
  target_ad_campaign_id?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const DuplicateAdGroupRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    count: S.optional(S.Number),
    preserve_engagement: S.optional(S.Boolean),
    target_ad_campaign_id: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/ad_groups/{id}/duplicate", code: 200 }),
  ),
).annotate({
  identifier: "DuplicateAdGroupRequest",
}) as any as S.Schema<DuplicateAdGroupRequest>;

export type DuplicateAdGroupResponseDataList = Array<AdGroup>;
export const DuplicateAdGroupResponseDataList = /*@__PURE__*/ S.Array(
  AdGroup,
) as any as S.Schema<DuplicateAdGroupResponseDataList>;

export interface DuplicateAdGroupResponse {
  data: DuplicateAdGroupResponseDataList;
}
export const DuplicateAdGroupResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: DuplicateAdGroupResponseDataList,
  }),
).annotate({
  identifier: "DuplicateAdGroupResponse",
}) as any as S.Schema<DuplicateAdGroupResponse>;

/** Languages to target, as ISO 639 codes such as `en` or `es`. Empty or omitted targets all languages. */
export type EstimateAdGroupReachRequestLanguagesList = Array<string>;
export const EstimateAdGroupReachRequestLanguagesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<EstimateAdGroupReachRequestLanguagesList>;

/** The ad network the estimate runs on. */
export type EstimateAdGroupReachRequestPlatform = "meta";
export const EstimateAdGroupReachRequestPlatform = /*@__PURE__*/ S.String;

export interface EstimateAdGroupReachRequest {
  /** Account to estimate on behalf of. Defaults to the authenticated account. */
  account_id?: string;
  /** Saved audiences to deliver to or exclude. Can't be combined with demographics.automatic. */
  audiences?: AdGroupAudiencesBody;
  /** Age, gender, and automatic-audience targeting. */
  demographics?: AdGroupDemographicsBody;
  /** Interest, behavior, and demographic targeting, using categories from the ad platform's targeting taxonomy. At most 100 entries per section. */
  detailed_targeting?: AdGroupDetailedTargetingBody;
  /** Device platforms and operating systems to target. */
  devices?: AdGroupDevicesBody;
  /** Languages to target, as ISO 639 codes such as `en` or `es`. Empty or omitted targets all languages. */
  languages?: EstimateAdGroupReachRequestLanguagesList;
  /** The ad network the estimate runs on. */
  platform: EstimateAdGroupReachRequestPlatform | (string & {});
  /** Locations to target and exclude. */
  regions?: AdGroupRegionsBody;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const EstimateAdGroupReachRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    audiences: S.optional(AdGroupAudiencesBody),
    demographics: S.optional(AdGroupDemographicsBody),
    detailed_targeting: S.optional(AdGroupDetailedTargetingBody),
    devices: S.optional(AdGroupDevicesBody),
    languages: S.optional(EstimateAdGroupReachRequestLanguagesList),
    platform: EstimateAdGroupReachRequestPlatform,
    regions: S.optional(AdGroupRegionsBody),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/ad_groups/estimate_reach", code: 200 }),
  ),
).annotate({
  identifier: "EstimateAdGroupReachRequest",
}) as any as S.Schema<EstimateAdGroupReachRequest>;

export interface ReachEstimate {
  /** Low end of how many people the targeting can reach. Null when the platform couldn't produce an estimate. */
  users_lower_bound: number | null;
  /** High end of how many people the targeting can reach. Null when the platform couldn't produce an estimate. */
  users_upper_bound: number | null;
}
export const ReachEstimate = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    users_lower_bound: S.NullOr(S.Number),
    users_upper_bound: S.NullOr(S.Number),
  }),
).annotate({ identifier: "ReachEstimate" }) as any as S.Schema<ReachEstimate>;

export type ListAdGroupsRequestAdCampaignIdsList = Array<string>;
export const ListAdGroupsRequestAdCampaignIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListAdGroupsRequestAdCampaignIdsList>;

export type ListAdGroupsRequestStatus =
  | "active"
  | "paused"
  | "rejected"
  | "duplicating";
export const ListAdGroupsRequestStatus = /*@__PURE__*/ S.String;

export type ListAdGroupsRequestOrder =
  | "created_at"
  | "updated_at"
  | "spend"
  | "impressions"
  | "reach"
  | "clicks"
  | "unique_clicks"
  | "frequency"
  | "click_through_rate"
  | "results"
  | "cost_per_mille"
  | "cost_per_click"
  | "cost_per_result"
  | "return_on_ad_spend";
export const ListAdGroupsRequestOrder = /*@__PURE__*/ S.String;

export type ListAdGroupsRequestDirection = "asc" | "desc";
export const ListAdGroupsRequestDirection = /*@__PURE__*/ S.String;

export type ListAdGroupsRequestAttributionModel = "last_touch" | "first_touch";
export const ListAdGroupsRequestAttributionModel = /*@__PURE__*/ S.String;

export interface ListAdGroupsRequest {
  /** Account whose ad groups to list. Defaults to the authenticated account. */
  account_id?: string;
  /** Filter to ad groups in this campaign. */
  ad_campaign_id?: string;
  /** Filter to ad groups in these campaigns (max 100). Repeat the parameter for each id (ad_campaign_ids=a&ad_campaign_ids=b). */
  ad_campaign_ids?: ListAdGroupsRequestAdCampaignIdsList;
  /** Filter to ad groups with this status. */
  status?: ListAdGroupsRequestStatus | (string & {});
  /** Filter ad groups by a title or ID substring. */
  query?: string;
  /** The field to sort by. Defaults to created_at. Stat columns (spend, impressions, …) rank over the stats_from/stats_to window across the whole list, not just the current page. results, cost_per_result and return_on_ad_spend rank by the same Whop pixel-attributed values the response reports. */
  order?: ListAdGroupsRequestOrder | (string & {});
  /** The sort direction. Defaults to desc. */
  direction?: ListAdGroupsRequestDirection | (string & {});
  /** Only return ad groups created before this timestamp. */
  created_before?: string;
  /** Only return ad groups created after this timestamp. */
  created_after?: string;
  /** Start of the stats window. Defaults to all-time. */
  stats_from?: string;
  /** End of the stats window. Defaults to now. */
  stats_to?: string;
  /** IANA timezone (e.g. America/New_York) the stats window is interpreted in. Bare stats_from/stats_to dates resolve to day boundaries on this clock. Defaults to UTC. */
  time_zone?: string;
  /** Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise. */
  attribution_model?: ListAdGroupsRequestAttributionModel | (string & {});
  /** The number of ad groups to return. */
  first?: number;
  /** Cursor to fetch the page after (from page_info.end_cursor). */
  after?: string;
  /** The number of ad groups to return from the end of the range. */
  last?: number;
  /** Cursor to fetch the page before (from page_info.start_cursor). */
  before?: string;
}
export const ListAdGroupsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    ad_campaign_id: S.optional(S.String.pipe(T.Query())),
    ad_campaign_ids: S.optional(
      ListAdGroupsRequestAdCampaignIdsList.pipe(T.Query()),
    ),
    status: S.optional(ListAdGroupsRequestStatus.pipe(T.Query())),
    query: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListAdGroupsRequestOrder.pipe(T.Query())),
    direction: S.optional(ListAdGroupsRequestDirection.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    stats_from: S.optional(S.String.pipe(T.Query())),
    stats_to: S.optional(S.String.pipe(T.Query())),
    time_zone: S.optional(S.String.pipe(T.Query())),
    attribution_model: S.optional(
      ListAdGroupsRequestAttributionModel.pipe(T.Query()),
    ),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/ad_groups", code: 200 })),
).annotate({
  identifier: "ListAdGroupsRequest",
}) as any as S.Schema<ListAdGroupsRequest>;

export type ListAdGroupsResponseDataList = Array<AdGroup>;
export const ListAdGroupsResponseDataList = /*@__PURE__*/ S.Array(
  AdGroup,
) as any as S.Schema<ListAdGroupsResponseDataList>;

export interface ListAdGroupsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListAdGroupsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListAdGroupsResponsePageInfo",
}) as any as S.Schema<ListAdGroupsResponsePageInfo>;

export interface ListAdGroupsResponse {
  data: ListAdGroupsResponseDataList;
  page_info: ListAdGroupsResponsePageInfo;
}
export const ListAdGroupsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListAdGroupsResponseDataList,
    page_info: ListAdGroupsResponsePageInfo,
  }),
).annotate({
  identifier: "ListAdGroupsResponse",
}) as any as S.Schema<ListAdGroupsResponse>;

export interface PauseAdGroupRequest {
  /** The ad group ID. */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const PauseAdGroupRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/ad_groups/{id}/pause", code: 200 })),
).annotate({
  identifier: "PauseAdGroupRequest",
}) as any as S.Schema<PauseAdGroupRequest>;

export type RetrieveAdGroupRequestAttributionModel =
  | "last_touch"
  | "first_touch";
export const RetrieveAdGroupRequestAttributionModel = /*@__PURE__*/ S.String;

export interface RetrieveAdGroupRequest {
  /** The ad group ID. */
  id: string;
  /** Start of the stats window. */
  stats_from?: string;
  /** End of the stats window. */
  stats_to?: string;
  /** IANA timezone the stats window is interpreted in. Defaults to UTC. */
  time_zone?: string;
  /** Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise. */
  attribution_model?: RetrieveAdGroupRequestAttributionModel | (string & {});
}
export const RetrieveAdGroupRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    stats_from: S.optional(S.String.pipe(T.Query())),
    stats_to: S.optional(S.String.pipe(T.Query())),
    time_zone: S.optional(S.String.pipe(T.Query())),
    attribution_model: S.optional(
      RetrieveAdGroupRequestAttributionModel.pipe(T.Query()),
    ),
  }).pipe(T.Http({ method: "GET", uri: "/ad_groups/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveAdGroupRequest",
}) as any as S.Schema<RetrieveAdGroupRequest>;

export type SearchAdGroupTargetingOptionsRequestPlatform = "meta";
export const SearchAdGroupTargetingOptionsRequestPlatform =
  /*@__PURE__*/ S.String;

export type SearchAdGroupTargetingOptionsRequestTypesItem =
  | "interests"
  | "behaviors"
  | "life_events"
  | "industries"
  | "income"
  | "family_statuses"
  | "work_employers"
  | "work_positions"
  | "education_schools"
  | "education_majors"
  | "languages"
  | "locations";
export const SearchAdGroupTargetingOptionsRequestTypesItem =
  /*@__PURE__*/ S.String;

export type SearchAdGroupTargetingOptionsRequestTypesList = Array<
  SearchAdGroupTargetingOptionsRequestTypesItem | (string & {})
>;
export const SearchAdGroupTargetingOptionsRequestTypesList =
  /*@__PURE__*/ S.Array(
    SearchAdGroupTargetingOptionsRequestTypesItem,
  ) as any as S.Schema<SearchAdGroupTargetingOptionsRequestTypesList>;

export type SearchAdGroupTargetingOptionsRequestLocationTypesItem =
  | "country"
  | "region"
  | "city"
  | "zip"
  | "neighborhood"
  | "subcity"
  | "medium_geo_area";
export const SearchAdGroupTargetingOptionsRequestLocationTypesItem =
  /*@__PURE__*/ S.String;

export type SearchAdGroupTargetingOptionsRequestLocationTypesList = Array<
  SearchAdGroupTargetingOptionsRequestLocationTypesItem | (string & {})
>;
export const SearchAdGroupTargetingOptionsRequestLocationTypesList =
  /*@__PURE__*/ S.Array(
    SearchAdGroupTargetingOptionsRequestLocationTypesItem,
  ) as any as S.Schema<SearchAdGroupTargetingOptionsRequestLocationTypesList>;

export type SearchAdGroupTargetingOptionsRequestSpecialAdCategoriesItem =
  | "housing"
  | "employment"
  | "financial_products"
  | "politics";
export const SearchAdGroupTargetingOptionsRequestSpecialAdCategoriesItem =
  /*@__PURE__*/ S.String;

export type SearchAdGroupTargetingOptionsRequestSpecialAdCategoriesList = Array<
  SearchAdGroupTargetingOptionsRequestSpecialAdCategoriesItem | (string & {})
>;
export const SearchAdGroupTargetingOptionsRequestSpecialAdCategoriesList =
  /*@__PURE__*/ S.Array(
    SearchAdGroupTargetingOptionsRequestSpecialAdCategoriesItem,
  ) as any as S.Schema<SearchAdGroupTargetingOptionsRequestSpecialAdCategoriesList>;

export interface SearchAdGroupTargetingOptionsRequest {
  /** Account to search on behalf of. Defaults to the authenticated account. */
  account_id?: string;
  /** The ad network whose targeting taxonomy to search. */
  platform: SearchAdGroupTargetingOptionsRequestPlatform | (string & {});
  /** The search term. Blank browses the fixed lists; interests, work employers, job titles, schools, majors, and locations return nothing without one. */
  query?: string;
  /** Kinds of targeting options to search. Defaults to all of them. */
  types?: SearchAdGroupTargetingOptionsRequestTypesList;
  /** Narrow location results to these kinds of places. Only applies when `types` includes `locations`. */
  location_types?: SearchAdGroupTargetingOptionsRequestLocationTypesList;
  /** Narrow location results to one country, as an ISO 3166-1 code such as `US`. Only applies when `types` includes `locations`. */
  country?: string;
  /** Maximum number of results per requested type. */
  limit?: number;
  /** The campaign's declared special ad categories. Under `housing`, `employment`, or `financial_products` the ad platform allows interests only, drawn from a short approved list, so results are narrowed to what such a campaign can launch with and other kinds return nothing. Blank `query` browses that approved list instead of the usual fixed lists. */
  special_ad_categories?: SearchAdGroupTargetingOptionsRequestSpecialAdCategoriesList;
}
export const SearchAdGroupTargetingOptionsRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      account_id: S.optional(S.String.pipe(T.Query())),
      platform: SearchAdGroupTargetingOptionsRequestPlatform.pipe(T.Query()),
      query: S.optional(S.String.pipe(T.Query())),
      types: S.optional(
        SearchAdGroupTargetingOptionsRequestTypesList.pipe(T.Query()),
      ),
      location_types: S.optional(
        SearchAdGroupTargetingOptionsRequestLocationTypesList.pipe(T.Query()),
      ),
      country: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      special_ad_categories: S.optional(
        SearchAdGroupTargetingOptionsRequestSpecialAdCategoriesList.pipe(
          T.Query(),
        ),
      ),
    }).pipe(
      T.Http({ method: "GET", uri: "/ad_groups/targeting_options", code: 200 }),
    ),
).annotate({
  identifier: "SearchAdGroupTargetingOptionsRequest",
}) as any as S.Schema<SearchAdGroupTargetingOptionsRequest>;

/** What a behavior category is measured on, on ad platforms that scope them. Send it back on the `detailed_targeting.behaviors` entry alongside the id. Null for options that aren't scoped. */
export type DetailedTargetingOptionBehaviorType =
  | "video"
  | "creator"
  | "hashtag";
export const DetailedTargetingOptionBehaviorType = /*@__PURE__*/ S.String;

/** Which detailed-targeting field the option belongs in: `interests` and `behaviors` go in the matching `detailed_targeting` field; demographic categories (`life_events`, `industries`, `income`, `family_statuses`, `work_employers`, `work_positions`, `education_schools`, `education_majors`) go in `detailed_targeting.demographics` with this value as the entry's `type`. */
export type DetailedTargetingOptionType =
  | "interests"
  | "behaviors"
  | "life_events"
  | "industries"
  | "income"
  | "family_statuses"
  | "work_employers"
  | "work_positions"
  | "education_schools"
  | "education_majors";
export const DetailedTargetingOptionType = /*@__PURE__*/ S.String;

export interface DetailedTargetingOption {
  /** Low end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one. */
  audience_size_lower_bound: number | null;
  /** High end of the ad platform's estimate of how many people this option can reach. Null when the platform doesn't publish one. */
  audience_size_upper_bound: number | null;
  /** What a behavior category is measured on, on ad platforms that scope them. Send it back on the `detailed_targeting.behaviors` entry alongside the id. Null for options that aren't scoped. */
  behavior_type: DetailedTargetingOptionBehaviorType | null;
  /** The ad platform's description of who the option covers, when it publishes one. */
  description: string | null;
  /** The ad platform's ID for the option in its targeting taxonomy. Use it as the `id` of a `detailed_targeting` entry. */
  id: string;
  /** Display name, such as `Movies`. */
  name: string;
  /** Which detailed-targeting field the option belongs in: `interests` and `behaviors` go in the matching `detailed_targeting` field; demographic categories (`life_events`, `industries`, `income`, `family_statuses`, `work_employers`, `work_positions`, `education_schools`, `education_majors`) go in `detailed_targeting.demographics` with this value as the entry's `type`. */
  type: DetailedTargetingOptionType;
}
export const DetailedTargetingOption = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    audience_size_lower_bound: S.NullOr(S.Number),
    audience_size_upper_bound: S.NullOr(S.Number),
    behavior_type: S.NullOr(DetailedTargetingOptionBehaviorType),
    description: S.NullOr(S.String),
    id: S.String,
    name: S.String,
    type: DetailedTargetingOptionType,
  }),
).annotate({
  identifier: "DetailedTargetingOption",
}) as any as S.Schema<DetailedTargetingOption>;

/** Always `languages`. The option goes in the ad-group `languages` field. */
export type LanguageTargetingOptionType = "languages";
export const LanguageTargetingOptionType = /*@__PURE__*/ S.String;

export interface LanguageTargetingOption {
  /** ISO 639 code the ad-group `languages` field takes, such as `en`. */
  code: string;
  /** Display name, such as `English`. */
  name: string;
  /** Always `languages`. The option goes in the ad-group `languages` field. */
  type: LanguageTargetingOptionType;
}
export const LanguageTargetingOption = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    code: S.String,
    name: S.String,
    type: LanguageTargetingOptionType,
  }),
).annotate({
  identifier: "LanguageTargetingOption",
}) as any as S.Schema<LanguageTargetingOption>;

/** Granularity of the location. Which of these an ad platform reports depends on how finely it divides its location taxonomy. */
export type LocationTargetingOptionLocationType =
  | "country"
  | "region"
  | "city"
  | "zip"
  | "neighborhood"
  | "subcity"
  | "medium_geo_area"
  | "district"
  | "dma";
export const LocationTargetingOptionLocationType = /*@__PURE__*/ S.String;

/** Always `locations`. The option goes in the ad-group `regions` field. */
export type LocationTargetingOptionType = "locations";
export const LocationTargetingOptionType = /*@__PURE__*/ S.String;

export interface LocationTargetingOption {
  /** The standardized code the ad-group `regions` field takes: an ISO 3166-1 code for countries (`US`) or an ISO 3166-2 code for states and provinces (`US-CA`, `CA-ON`). Null for a location that has no standard code, such as a city or a metro area — target those by `key` in the `regions` cities list instead. */
  code: string | null;
  /** ISO 3166-1 code of the country the location sits in. */
  country_code: string | null;
  /** Name of the country the location sits in. */
  country_name: string | null;
  /** The ad platform's key for the location in its location taxonomy. Use it as the `key` of a `regions` city or zip entry. */
  key: string;
  /** Granularity of the location. Which of these an ad platform reports depends on how finely it divides its location taxonomy. */
  location_type: LocationTargetingOptionLocationType;
  /** Display name, such as `California`. */
  name: string;
  /** Name of the state or province a city sits in. Null for everything but cities. */
  region: string | null;
  /** Always `locations`. The option goes in the ad-group `regions` field. */
  type: LocationTargetingOptionType;
}
export const LocationTargetingOption = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    code: S.NullOr(S.String),
    country_code: S.NullOr(S.String),
    country_name: S.NullOr(S.String),
    key: S.String,
    location_type: LocationTargetingOptionLocationType,
    name: S.String,
    region: S.NullOr(S.String),
    type: LocationTargetingOptionType,
  }),
).annotate({
  identifier: "LocationTargetingOption",
}) as any as S.Schema<LocationTargetingOption>;

/** One result from the ad-group targeting-options search. `type` picks the shape: detailed-targeting taxonomy options carry the ad platform's `id`, languages carry an ISO 639 `code`, and locations carry the platform `key` plus geographic context. */
export type TargetingOption =
  | DetailedTargetingOption
  | LanguageTargetingOption
  | LocationTargetingOption;
export const TargetingOption =
  /*@__PURE__*/ S.Unknown as any as S.Schema<TargetingOption>;

export type SearchAdGroupTargetingOptionsResponseDataList =
  Array<TargetingOption>;
export const SearchAdGroupTargetingOptionsResponseDataList =
  /*@__PURE__*/ S.Array(
    TargetingOption,
  ) as any as S.Schema<SearchAdGroupTargetingOptionsResponseDataList>;

export interface SearchAdGroupTargetingOptionsResponse {
  data: SearchAdGroupTargetingOptionsResponseDataList;
}
export const SearchAdGroupTargetingOptionsResponse = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      data: SearchAdGroupTargetingOptionsResponseDataList,
    }),
).annotate({
  identifier: "SearchAdGroupTargetingOptionsResponse",
}) as any as S.Schema<SearchAdGroupTargetingOptionsResponse>;

export interface UnpauseAdGroupRequest {
  /** The ad group ID. */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const UnpauseAdGroupRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/ad_groups/{id}/unpause", code: 200 }),
  ),
).annotate({
  identifier: "UnpauseAdGroupRequest",
}) as any as S.Schema<UnpauseAdGroupRequest>;

/** How delivery bids are set in the ad auction. Target-based strategies use `desired_cost_per_result`. */
export type UpdateAdGroupRequestBidType =
  | "minimum_cost"
  | "average_target"
  | "maximum_target";
export const UpdateAdGroupRequestBidType = /*@__PURE__*/ S.String;

/** Whether budget_amount is spent per day (`daily`) or over the ad group's full run (`lifetime`). */
export type UpdateAdGroupRequestBudgetType = "daily" | "lifetime";
export const UpdateAdGroupRequestBudgetType = /*@__PURE__*/ S.String;

/** Where the outcome being optimized for occurs, such as a website visit, social-profile visit, messaging conversation, ad interaction, or lead-form submission. The lead form itself is set on the ad. */
export type UpdateAdGroupRequestConversionLocation =
  | "website"
  | "profile"
  | "instagram_and_facebook"
  | "instagram_profile"
  | "messaging"
  | "on_ad"
  | "instant_forms"
  | "instant_forms_and_messenger"
  | "website_and_instant_forms";
export const UpdateAdGroupRequestConversionLocation = /*@__PURE__*/ S.String;

/** Cap on how often one person sees ads from this ad group. Only available on campaigns with the `awareness` objective. */
export type UpdateAdGroupRequestFrequencyCap = CreateAdGroupRequestFrequencyCap;
export const UpdateAdGroupRequestFrequencyCap =
  CreateAdGroupRequestFrequencyCap;

/** Languages to target, as ISO 639 codes such as `en` or `es`. Empty or omitted targets all languages. */
export type UpdateAdGroupRequestLanguagesList = Array<string>;
export const UpdateAdGroupRequestLanguagesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpdateAdGroupRequestLanguagesList>;

export type UpdateAdGroupRequestMessageAppsItem =
  | "messenger"
  | "instagram"
  | "whatsapp";
export const UpdateAdGroupRequestMessageAppsItem = /*@__PURE__*/ S.String;

/** Apps the conversation opens in. Required when setting `conversion_location` to `messaging`, and rejected unless the ad group's conversion location is `messaging`. */
export type UpdateAdGroupRequestMessageAppsList = Array<
  UpdateAdGroupRequestMessageAppsItem | (string & {})
>;
export const UpdateAdGroupRequestMessageAppsList = /*@__PURE__*/ S.Array(
  UpdateAdGroupRequestMessageAppsItem,
) as any as S.Schema<UpdateAdGroupRequestMessageAppsList>;

/** The result the ad group's delivery is optimized to get the most of. */
export type UpdateAdGroupRequestOptimizationGoal =
  | "conversions"
  | "link_clicks"
  | "landing_page_views"
  | "reach"
  | "impressions"
  | "engagement"
  | "conversations"
  | "video_views"
  | "two_second_views"
  | "page_likes"
  | "social_profile"
  | "ad_recall_lift"
  | "event_responses"
  | "reminders_set"
  | "lead_generation"
  | "quality_lead"
  | "value"
  | "profile_and_page_engagement";
export const UpdateAdGroupRequestOptimizationGoal = /*@__PURE__*/ S.String;

export type UpdateAdGroupRequestPlacementsCase0 = "automatic";
export const UpdateAdGroupRequestPlacementsCase0 = /*@__PURE__*/ S.String;

/** Platform the ads run on. */
export type UpdateAdGroupRequestPlacementsCase1ItemPlatform =
  | "facebook"
  | "instagram"
  | "messenger"
  | "audience_network"
  | "threads"
  | "whatsapp";
export const UpdateAdGroupRequestPlacementsCase1ItemPlatform =
  /*@__PURE__*/ S.String;

/** Positions to target within the platform, such as `feed` or `story`. Omit to target all of the platform's positions. */
export type UpdateAdGroupRequestPlacementsCase1ItemPositionsList =
  Array<string>;
export const UpdateAdGroupRequestPlacementsCase1ItemPositionsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateAdGroupRequestPlacementsCase1ItemPositionsList>;

export interface UpdateAdGroupRequestPlacementsCase1Item {
  /** Platform the ads run on. */
  platform: UpdateAdGroupRequestPlacementsCase1ItemPlatform | (string & {});
  /** Positions to target within the platform, such as `feed` or `story`. Omit to target all of the platform's positions. */
  positions?: UpdateAdGroupRequestPlacementsCase1ItemPositionsList;
}
export const UpdateAdGroupRequestPlacementsCase1Item = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      platform: UpdateAdGroupRequestPlacementsCase1ItemPlatform,
      positions: S.optional(
        UpdateAdGroupRequestPlacementsCase1ItemPositionsList,
      ),
    }),
).annotate({
  identifier: "UpdateAdGroupRequestPlacementsCase1Item",
}) as any as S.Schema<UpdateAdGroupRequestPlacementsCase1Item>;

export type UpdateAdGroupRequestPlacementsCase1List =
  Array<UpdateAdGroupRequestPlacementsCase1Item>;
export const UpdateAdGroupRequestPlacementsCase1List = /*@__PURE__*/ S.Array(
  UpdateAdGroupRequestPlacementsCase1Item,
) as any as S.Schema<UpdateAdGroupRequestPlacementsCase1List>;

/** `automatic` to let the ad platform choose placements, or the list of platforms and positions to target. Omit a platform's positions to target all of them. Valid positions per platform: - `facebook`: `feed`, `right_hand_column`, `marketplace`, `search`, `profile_feed`, `notification`, `story`, `instream_video`, `facebook_reels`, `facebook_reels_overlay`, `biz_disco_feed` - `instagram`: `stream`, `story`, `explore`, `explore_home`, `reels`, `profile_feed`, `profile_reels`, `ig_search` - `messenger`: `story` - `audience_network`: `classic`, `rewarded_video` - `threads`: `threads_stream` - `whatsapp`: `status` */
export type UpdateAdGroupRequestPlacements =
  | UpdateAdGroupRequestPlacementsCase0
  | UpdateAdGroupRequestPlacementsCase1List;
export const UpdateAdGroupRequestPlacements =
  /*@__PURE__*/ S.Unknown as any as S.Schema<UpdateAdGroupRequestPlacements>;

/** Initial status (default: `active`). */
export type UpdateAdGroupRequestStatus = "active" | "paused";
export const UpdateAdGroupRequestStatus = /*@__PURE__*/ S.String;

export interface UpdateAdGroupRequest {
  /** The ad group ID. */
  id: string;
  /** Saved audiences to deliver to or exclude. Can't be combined with demographics.automatic. */
  audiences?: AdGroupAudiencesBody;
  /** How delivery bids are set in the ad auction. Target-based strategies use `desired_cost_per_result`. */
  bid_type?: UpdateAdGroupRequestBidType | (string & {});
  /** This ad group's budget, in the ad account's currency. Omit when the budget is set on the campaign instead. */
  budget_amount?: number;
  /** Whether budget_amount is spent per day (`daily`) or over the ad group's full run (`lifetime`). */
  budget_type?: UpdateAdGroupRequestBudgetType | (string & {});
  conversion_event?: ConversionEvent | null;
  /** Where the outcome being optimized for occurs, such as a website visit, social-profile visit, messaging conversation, ad interaction, or lead-form submission. The lead form itself is set on the ad. */
  conversion_location?: UpdateAdGroupRequestConversionLocation | (string & {});
  /** Age, gender, and automatic-audience targeting. */
  demographics?: AdGroupDemographicsBody;
  /** Cost per result to aim for (`average_target`) or never exceed (`maximum_target`). */
  desired_cost_per_result?: number;
  /** Interest, behavior, and demographic targeting, using categories from the ad platform's targeting taxonomy. Entries across interests, behaviors, and demographics are OR'd together (anyone matching any entry is reached), matching Ads Manager's detailed-targeting box. At most 100 entries per section. Can't be combined with demographics.automatic, and unavailable to campaigns with special_ad_categories. Send the complete intended state — a section you omit is cleared. */
  detailed_targeting?: AdGroupDetailedTargetingBody;
  /** Device platforms and operating systems to target. */
  devices?: AdGroupDevicesBody;
  /** When the ad group stops delivering, as an ISO 8601 timestamp. Omit to run until paused. */
  ends_at?: string;
  /** Cap on how often one person sees ads from this ad group. Only available on campaigns with the `awareness` objective. */
  frequency_cap?: CreateAdGroupRequestFrequencyCap;
  /** Languages to target, as ISO 639 codes such as `en` or `es`. Empty or omitted targets all languages. */
  languages?: UpdateAdGroupRequestLanguagesList;
  /** Apps the conversation opens in. Required when setting `conversion_location` to `messaging`, and rejected unless the ad group's conversion location is `messaging`. */
  message_apps?: UpdateAdGroupRequestMessageAppsList;
  /** Minimum the ad group tries to spend each day. */
  minimum_daily_spend?: number;
  /** The result the ad group's delivery is optimized to get the most of. */
  optimization_goal?: UpdateAdGroupRequestOptimizationGoal | (string & {});
  /** `automatic` to let the ad platform choose placements, or the list of platforms and positions to target. Omit a platform's positions to target all of them. Valid positions per platform: - `facebook`: `feed`, `right_hand_column`, `marketplace`, `search`, `profile_feed`, `notification`, `story`, `instream_video`, `facebook_reels`, `facebook_reels_overlay`, `biz_disco_feed` - `instagram`: `stream`, `story`, `explore`, `explore_home`, `reels`, `profile_feed`, `profile_reels`, `ig_search` - `messenger`: `story` - `audience_network`: `classic`, `rewarded_video` - `threads`: `threads_stream` - `whatsapp`: `status` */
  placements?: UpdateAdGroupRequestPlacements;
  /** Locations to target and exclude. */
  regions?: AdGroupRegionsBody;
  /** When the ad group starts delivering, as an ISO 8601 timestamp. Omit to start as soon as it's active. */
  starts_at?: string;
  /** Initial status (default: `active`). */
  status?: UpdateAdGroupRequestStatus | (string & {});
  /** The display name of the ad group. */
  title?: string;
}
export const UpdateAdGroupRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    audiences: S.optional(AdGroupAudiencesBody),
    bid_type: S.optional(UpdateAdGroupRequestBidType),
    budget_amount: S.optional(S.Number),
    budget_type: S.optional(UpdateAdGroupRequestBudgetType),
    conversion_event: S.optional(S.NullOr(ConversionEvent)),
    conversion_location: S.optional(UpdateAdGroupRequestConversionLocation),
    demographics: S.optional(AdGroupDemographicsBody),
    desired_cost_per_result: S.optional(S.Number),
    detailed_targeting: S.optional(AdGroupDetailedTargetingBody),
    devices: S.optional(AdGroupDevicesBody),
    ends_at: S.optional(S.String),
    frequency_cap: S.optional(CreateAdGroupRequestFrequencyCap),
    languages: S.optional(UpdateAdGroupRequestLanguagesList),
    message_apps: S.optional(UpdateAdGroupRequestMessageAppsList),
    minimum_daily_spend: S.optional(S.Number),
    optimization_goal: S.optional(UpdateAdGroupRequestOptimizationGoal),
    placements: S.optional(UpdateAdGroupRequestPlacements),
    regions: S.optional(AdGroupRegionsBody),
    starts_at: S.optional(S.String),
    status: S.optional(UpdateAdGroupRequestStatus),
    title: S.optional(S.String),
  }).pipe(T.Http({ method: "PATCH", uri: "/ad_groups/{id}", code: 200 })),
).annotate({
  identifier: "UpdateAdGroupRequest",
}) as any as S.Schema<UpdateAdGroupRequest>;

export type CreateAdGroupError = NotFound | Conflict | WhopOpError;
/** Create an Ad Group Creates an ad group (ad set) in a campaign. */
export const createAdGroup: API.OperationMethod<
  CreateAdGroupRequest,
  AdGroup,
  CreateAdGroupError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAdGroupRequest,
  output: AdGroup,
  errors: [NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteAdGroupError = NotFound | WhopOpError;
/** Delete an Ad Group Deletes an ad group. */
export const deleteAdGroup: API.OperationMethod<
  DeleteAdGroupRequest,
  DeleteAdGroupResponse,
  DeleteAdGroupError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteAdGroupRequest,
  output: DeleteAdGroupResponse,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DuplicateAdGroupError =
  | BadRequest
  | NotFound
  | Conflict
  | WhopOpError;
/** Duplicate an Ad Group Creates copies of the ad group in `duplicating` status and returns them — into its own campaign, or into target_ad_campaign_id (which must belong to the same account and be compatible with the ad group's targeting and goals); each copy transitions to its final status (matching the source's active/paused state) once duplication completes. Poll each returned ad group until it leaves `duplicating` — a copy that could not be completed is deleted and returns 404. */
export const duplicateAdGroup: API.OperationMethod<
  DuplicateAdGroupRequest,
  DuplicateAdGroupResponse,
  DuplicateAdGroupError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DuplicateAdGroupRequest,
  output: DuplicateAdGroupResponse,
  errors: [BadRequest, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type EstimateAdGroupReachError =
  | BadRequest
  | Forbidden
  | Conflict
  | WhopOpError;
/** Estimate Ad Group Reach Estimates how many people a draft targeting spec can reach, before an ad group is created. The body takes the same targeting fields as creating an ad group — `regions`, `demographics`, `detailed_targeting`, `audiences`, `languages`, and `devices` — and nothing is persisted. */
export const estimateAdGroupReach: API.OperationMethod<
  EstimateAdGroupReachRequest,
  ReachEstimate,
  EstimateAdGroupReachError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EstimateAdGroupReachRequest,
  output: ReachEstimate,
  errors: [BadRequest, Forbidden, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListAdGroupsError = WhopOpError;
/** List Ad Groups Lists ad groups for the account, newest first. */
export const listAdGroups: API.PaginatedOperationMethod<
  ListAdGroupsRequest,
  ListAdGroupsResponse,
  ListAdGroupsError,
  WhopOpContext,
  AdGroup
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListAdGroupsRequest,
    output: ListAdGroupsResponse,
    errors: [],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type PauseAdGroupError = Conflict | WhopOpError;
/** Pause an Ad Group Pauses delivery of an ad group. */
export const pauseAdGroup: API.OperationMethod<
  PauseAdGroupRequest,
  AdGroup,
  PauseAdGroupError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PauseAdGroupRequest,
  output: AdGroup,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveAdGroupError = NotFound | WhopOpError;
/** Retrieve an Ad Group Retrieves a single ad group. */
export const retrieveAdGroup: API.OperationMethod<
  RetrieveAdGroupRequest,
  AdGroup,
  RetrieveAdGroupError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveAdGroupRequest,
  output: AdGroup,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type SearchAdGroupTargetingOptionsError =
  | BadRequest
  | Forbidden
  | WhopOpError;
/** Search Targeting Options Searches the ad platform's targeting taxonomy for options to target an ad group with. Each result comes back in the exact shape the ad-group body accepts for its `type`, so it can be used in `detailed_targeting`, `regions`, or `languages` as-is. A blank `query` browses the small fixed lists (behaviors, browse demographic categories, languages); interests, work employers, job titles, schools, majors, and locations need a search term. */
export const searchAdGroupTargetingOptions: API.OperationMethod<
  SearchAdGroupTargetingOptionsRequest,
  SearchAdGroupTargetingOptionsResponse,
  SearchAdGroupTargetingOptionsError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SearchAdGroupTargetingOptionsRequest,
  output: SearchAdGroupTargetingOptionsResponse,
  errors: [BadRequest, Forbidden],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UnpauseAdGroupError = Conflict | WhopOpError;
/** Unpause an Ad Group Resumes delivery of a paused ad group. */
export const unpauseAdGroup: API.OperationMethod<
  UnpauseAdGroupRequest,
  AdGroup,
  UnpauseAdGroupError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UnpauseAdGroupRequest,
  output: AdGroup,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateAdGroupError = WhopOpError;
/** Update an Ad Group Updates an ad group's editable fields. Only the keys you send are changed. */
export const updateAdGroup: API.OperationMethod<
  UpdateAdGroupRequest,
  AdGroup,
  UpdateAdGroupError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateAdGroupRequest,
  output: AdGroup,
  errors: [],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
