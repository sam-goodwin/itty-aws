// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** The call-to-action button label. */
export type CreateProductRequestCustomCta =
  | "get_access"
  | "join"
  | "order_now"
  | "shop_now"
  | "call_now"
  | "donate_now"
  | "contact_us"
  | "sign_up"
  | "subscribe"
  | "purchase"
  | "get_offer"
  | "apply_now"
  | "complete_order";
export const CreateProductRequestCustomCta = /*@__PURE__*/ S.String;

/** The enrollment status in the global affiliate program. */
export type CreateProductRequestGlobalAffiliateStatus = "enabled" | "disabled";
export const CreateProductRequestGlobalAffiliateStatus = /*@__PURE__*/ S.String;

/** Labels used to group products into collections. Stored lowercased and de-duplicated. Maximum 20 labels, 50 characters each. */
export type CreateProductRequestLabelsList = Array<string>;
export const CreateProductRequestLabelsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateProductRequestLabelsList>;

/** The enrollment status in the member affiliate program. */
export type CreateProductRequestMemberAffiliateStatus = "enabled" | "disabled";
export const CreateProductRequestMemberAffiliateStatus = /*@__PURE__*/ S.String;

export interface CreateProductRequest {
  /** The unique identifier of the account to create this product for. */
  account_id?: string;
  /** Whether to collect a shipping address at checkout. */
  collect_shipping_address?: boolean | null;
  /** The call-to-action button label. */
  custom_cta?: CreateProductRequestCustomCta | (string & {}) | null;
  /** A URL the call-to-action button links to. */
  custom_cta_url?: string | null;
  /** Custom bank statement descriptor. Must start with WHOP*. */
  custom_statement_descriptor?: string | null;
  /** A written description displayed on the product page. */
  description?: string | null;
  /** The commission rate affiliates earn. */
  global_affiliate_percentage?: number | null;
  /** The enrollment status in the global affiliate program. */
  global_affiliate_status?:
    | CreateProductRequestGlobalAffiliateStatus
    | (string & {});
  /** A short marketing headline for the product page. */
  headline?: string | null;
  /** Labels used to group products into collections. Stored lowercased and de-duplicated. Maximum 20 labels, 50 characters each. */
  labels?: CreateProductRequestLabelsList | null;
  /** The commission rate members earn. */
  member_affiliate_percentage?: number | null;
  /** The enrollment status in the member affiliate program. */
  member_affiliate_status?:
    | CreateProductRequestMemberAffiliateStatus
    | (string & {});
  /** Custom key-value pairs to store on the product. */
  metadata?: unknown | null;
  /** The unique identifier of the tax classification code. See the available [product categories](https://docs.numeral.com/essentials/product-categories). */
  product_tax_code_id?: string | null;
  /** A URL to redirect the customer to after purchase. */
  redirect_purchase_url?: string | null;
  /** The URL slug for the product's public link. */
  route?: string | null;
  /** Whether to send an automated welcome message via support chat when a user joins this product. Defaults to true. */
  send_welcome_message?: boolean | null;
  /** The display name of the product. Maximum 80 characters. */
  title: string;
  /** Whether the product is visible to customers. */
  visibility?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateProductRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    collect_shipping_address: S.optional(S.NullOr(S.Boolean)),
    custom_cta: S.optional(S.NullOr(CreateProductRequestCustomCta)),
    custom_cta_url: S.optional(S.NullOr(S.String)),
    custom_statement_descriptor: S.optional(S.NullOr(S.String)),
    description: S.optional(S.NullOr(S.String)),
    global_affiliate_percentage: S.optional(S.NullOr(S.Number)),
    global_affiliate_status: S.optional(
      CreateProductRequestGlobalAffiliateStatus,
    ),
    headline: S.optional(S.NullOr(S.String)),
    labels: S.optional(S.NullOr(CreateProductRequestLabelsList)),
    member_affiliate_percentage: S.optional(S.NullOr(S.Number)),
    member_affiliate_status: S.optional(
      CreateProductRequestMemberAffiliateStatus,
    ),
    metadata: S.optional(S.NullOr(S.Unknown)),
    product_tax_code_id: S.optional(S.NullOr(S.String)),
    redirect_purchase_url: S.optional(S.NullOr(S.String)),
    route: S.optional(S.NullOr(S.String)),
    send_welcome_message: S.optional(S.NullOr(S.Boolean)),
    title: S.String,
    visibility: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/products", code: 200 })),
).annotate({
  identifier: "CreateProductRequest",
}) as any as S.Schema<CreateProductRequest>;

/** Call-to-action button label shown on the product purchase page. */
export type ProductCustomCta =
  | "get_access"
  | "join"
  | "order_now"
  | "shop_now"
  | "call_now"
  | "donate_now"
  | "contact_us"
  | "sign_up"
  | "subscribe"
  | "purchase"
  | "get_offer"
  | "apply_now"
  | "complete_order";
export const ProductCustomCta = /*@__PURE__*/ S.String;

export interface Money {
  /** The amount in major units, as an exact decimal string — `"10.00"` is ten dollars. A string so no float rounds it in transit. */
  amount: string;
  /** Three-letter ISO 4217 currency code, lowercase. */
  currency: string;
  /** How many decimal places the amount CARRIES — the precision the charge itself runs at. */
  decimals: number;
  /** How many decimal places to SHOW. Usually equal to `decimals`, and deliberately not always: COP is charged in centavos but written in whole pesos, so it is `2` and `0`. Format the number in your own locale using this. */
  display_decimals: number;
}
export const Money = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.String,
    currency: S.String,
    decimals: S.Number,
    display_decimals: S.Number,
  }),
).annotate({ identifier: "Money" }) as any as S.Schema<Money>;

/** Billing model for this plan: `one_time` or `renewal`. */
export type ProductPublicPlanPlanType = "renewal" | "one_time";
export const ProductPublicPlanPlanType = /*@__PURE__*/ S.String;

/** Where this plan can be seen. `visible` plans appear on the product page. */
export type ProductPublicPlanVisibility =
  | "visible"
  | "hidden"
  | "archived"
  | "quick_link";
export const ProductPublicPlanVisibility = /*@__PURE__*/ S.String;

export interface ProductPublicPlan {
  /** Number of days between recurring charges, such as 30 for monthly or 365 for annual. `null` for one-time plans. */
  billing_period: number | null;
  /** Access duration in days for expiration-based plans. `null` for plans without an expiration. */
  expiration_days: number | null;
  /** Plan ID, prefixed `plan_`. */
  id: string;
  /** What checkout charges up front. `amount` is `"0.00"` when the first charge is free, such as a trial. */
  initial_price: Money;
  /** Billing model for this plan: `one_time` or `renewal`. */
  plan_type: ProductPublicPlanPlanType;
  /** The recurring charge every `billing_period` days. `amount` is `"0.00"` for one-time plans. */
  renewal_price: Money;
  /** Plan display name shown to customers. `null` if no title has been set. */
  title: string | null;
  /** Whether the plan has unlimited stock. */
  unlimited_stock: boolean;
  /** Where this plan can be seen. `visible` plans appear on the product page. */
  visibility: ProductPublicPlanVisibility;
}
export const ProductPublicPlan = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing_period: S.NullOr(S.Number),
    expiration_days: S.NullOr(S.Number),
    id: S.String,
    initial_price: Money,
    plan_type: ProductPublicPlanPlanType,
    renewal_price: Money,
    title: S.NullOr(S.String),
    unlimited_stock: S.Boolean,
    visibility: ProductPublicPlanVisibility,
  }),
).annotate({
  identifier: "ProductPublicPlan",
}) as any as S.Schema<ProductPublicPlan>;

export interface ProductGalleryImage {
  /** Uploaded file MIME type, such as image/jpeg. */
  content_type: string | null;
  /** Gallery image ID. */
  id: string;
  /** Pre-optimized URL for rendering this image on the client. */
  url: string | null;
}
export const ProductGalleryImage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    content_type: S.NullOr(S.String),
    id: S.String,
    url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ProductGalleryImage",
}) as any as S.Schema<ProductGalleryImage>;

export type ProductGalleryImagesList = Array<ProductGalleryImage>;
export const ProductGalleryImagesList = /*@__PURE__*/ S.Array(
  ProductGalleryImage,
) as any as S.Schema<ProductGalleryImagesList>;

/** Enrollment status in the global affiliate program. */
export type ProductGlobalAffiliateStatus = "enabled" | "disabled";
export const ProductGlobalAffiliateStatus = /*@__PURE__*/ S.String;

export type ProductLabelsList = Array<string>;
export const ProductLabelsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ProductLabelsList>;

/** Listing state on the whop.com marketplace. `pending_review` means submitted and awaiting review; `live_marketplace` means approved and discoverable. */
export type ProductMarketplaceStatus =
  | "not_available"
  | "pending_review"
  | "live_marketplace";
export const ProductMarketplaceStatus = /*@__PURE__*/ S.String;

/** Enrollment status in the member affiliate program. */
export type ProductMemberAffiliateStatus = "enabled" | "disabled";
export const ProductMemberAffiliateStatus = /*@__PURE__*/ S.String;

export interface Product {
  /** Account that sells this product. */
  account: unknown | null;
  /** When the product was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Call-to-action button label shown on the product purchase page. */
  custom_cta: ProductCustomCta | null;
  /** URL the call-to-action button links to instead of checkout. */
  custom_cta_url: string | null;
  /** Custom text label on customer's bank statement. */
  custom_statement_descriptor: string | null;
  /** Buyable plan to show and check out with. The configured default when that plan is buyable, otherwise the first buyable plan in product-page order. `null` when none is buyable. */
  default_plan: ProductPublicPlan | null;
  /** Written description displayed on the product page. `null` if none is set. */
  description: string | null;
  /** External identifier stored on the product for your own reference. */
  external_identifier: string | null;
  gallery_images: ProductGalleryImagesList;
  /** Commission rate affiliates earn through the global affiliate program. */
  global_affiliate_percentage: number | null;
  /** Enrollment status in the global affiliate program. */
  global_affiliate_status: ProductGlobalAffiliateStatus | null;
  /** Short marketing headline displayed on product page. */
  headline: string | null;
  /** Product ID, prefixed `prod_`. */
  id: string;
  labels: ProductLabelsList;
  /** Listing state on the whop.com marketplace. `pending_review` means submitted and awaiting review; `live_marketplace` means approved and discoverable. */
  marketplace_status: ProductMarketplaceStatus;
  /** Commission rate members earn through the member affiliate program. */
  member_affiliate_percentage: number | null;
  /** Enrollment status in the member affiliate program. */
  member_affiliate_status: ProductMemberAffiliateStatus | null;
  /** Active memberships for this product; 0 if public member counts are disabled. */
  member_count: number;
  /** Custom key-value pairs stored on the product. */
  metadata: unknown | null;
  /** User who owns the account selling this product. */
  owner_user: unknown | null;
  /** Tax classification code for this product, or `null` if no tax code is set. */
  product_tax_code: unknown | null;
  /** Published customer reviews for this product. */
  published_reviews_count: number;
  /** URL slug for the product's public link. */
  route: string;
  /** Product display name shown to customers. */
  title: string;
  /** When the product was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** Whether the product has been verified by Whop. */
  verified: boolean;
  /** Whether the product is publicly visible, hidden, or archived. */
  visibility: string | null;
}
export const Product = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account: S.NullOr(S.Unknown),
    created_at: S.String,
    custom_cta: S.NullOr(ProductCustomCta),
    custom_cta_url: S.NullOr(S.String),
    custom_statement_descriptor: S.NullOr(S.String),
    default_plan: S.NullOr(ProductPublicPlan),
    description: S.NullOr(S.String),
    external_identifier: S.NullOr(S.String),
    gallery_images: ProductGalleryImagesList,
    global_affiliate_percentage: S.NullOr(S.Number),
    global_affiliate_status: S.NullOr(ProductGlobalAffiliateStatus),
    headline: S.NullOr(S.String),
    id: S.String,
    labels: ProductLabelsList,
    marketplace_status: ProductMarketplaceStatus,
    member_affiliate_percentage: S.NullOr(S.Number),
    member_affiliate_status: S.NullOr(ProductMemberAffiliateStatus),
    member_count: S.Number,
    metadata: S.NullOr(S.Unknown),
    owner_user: S.NullOr(S.Unknown),
    product_tax_code: S.NullOr(S.Unknown),
    published_reviews_count: S.Number,
    route: S.String,
    title: S.String,
    updated_at: S.String,
    verified: S.Boolean,
    visibility: S.NullOr(S.String),
  }),
).annotate({ identifier: "Product" }) as any as S.Schema<Product>;

export interface DeleteProductRequest {
  /** The unique identifier of the product. */
  id: string;
}
export const DeleteProductRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/products/{id}", code: 200 })),
).annotate({
  identifier: "DeleteProductRequest",
}) as any as S.Schema<DeleteProductRequest>;

export interface DeleteProductResponse {
  /** Always true. */
  deleted: boolean;
  /** ID of the deleted product. */
  id: string;
}
export const DeleteProductResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "DeleteProductResponse",
}) as any as S.Schema<DeleteProductResponse>;

export type ListProductsRequestPlanTypesItem = "renewal" | "one_time";
export const ListProductsRequestPlanTypesItem = /*@__PURE__*/ S.String;

export type ListProductsRequestPlanTypesList = Array<
  ListProductsRequestPlanTypesItem | (string & {})
>;
export const ListProductsRequestPlanTypesList = /*@__PURE__*/ S.Array(
  ListProductsRequestPlanTypesItem,
) as any as S.Schema<ListProductsRequestPlanTypesList>;

export type ListProductsRequestVisibilitiesList = Array<string>;
export const ListProductsRequestVisibilitiesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListProductsRequestVisibilitiesList>;

export type ListProductsRequestAccessPassTypesList = Array<string>;
export const ListProductsRequestAccessPassTypesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListProductsRequestAccessPassTypesList>;

export type ListProductsRequestLabelsList = Array<string>;
export const ListProductsRequestLabelsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListProductsRequestLabelsList>;

export type ListProductsRequestDirection = "asc" | "desc";
export const ListProductsRequestDirection = /*@__PURE__*/ S.String;

export interface ListProductsRequest {
  /** The unique identifier of the account to list products for. Omit to search the public marketplace. */
  account_id?: string;
  /** Ranked search against product title and headline. Omit to browse by recency. */
  query?: string;
  /** Only return marketplace products assigned to this category route, such as `trading`. */
  marketplace_category_route?: string;
  /** Filter to products with a buyable plan of these billing models, such as `one_time` or `renewal`. */
  plan_types?: ListProductsRequestPlanTypesList;
  /** Only return products whose advertised buyable plan has a displayed price of at least this amount. Recurring plans use renewal price. */
  price_minimum?: number;
  /** Only return products whose advertised buyable plan has a displayed price of at most this amount. Recurring plans use renewal price. */
  price_maximum?: number;
  /** Filter to only products matching these visibility states. Ignored on the public marketplace list, which only returns visible products. */
  visibilities?: ListProductsRequestVisibilitiesList;
  /** Filter to only products matching these types. */
  access_pass_types?: ListProductsRequestAccessPassTypesList;
  /** Filter to only products carrying all of these labels. Labels are matched lowercased. */
  labels?: ListProductsRequestLabelsList;
  /** The sort direction for results. Defaults to descending. */
  direction?: ListProductsRequestDirection | (string & {});
  /** The field to sort results by. Account lists default to `created_at`. Marketplace lists default to `discoverable_at` and accept `created_at` or `discoverable_at`. Cannot be combined with `query`. */
  order?: string;
  /** The number of products to return (default and max 100). */
  first?: number;
  /** A cursor; returns products after this position. */
  after?: string;
  /** The number of products to return from the end of the range. */
  last?: number;
  /** A cursor; returns products before this position. */
  before?: string;
  /** Only return products created after this ISO 8601 timestamp. */
  created_after?: string;
  /** Only return products created before this ISO 8601 timestamp. */
  created_before?: string;
}
export const ListProductsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    query: S.optional(S.String.pipe(T.Query())),
    marketplace_category_route: S.optional(S.String.pipe(T.Query())),
    plan_types: S.optional(ListProductsRequestPlanTypesList.pipe(T.Query())),
    price_minimum: S.optional(S.Number.pipe(T.Query())),
    price_maximum: S.optional(S.Number.pipe(T.Query())),
    visibilities: S.optional(
      ListProductsRequestVisibilitiesList.pipe(T.Query()),
    ),
    access_pass_types: S.optional(
      ListProductsRequestAccessPassTypesList.pipe(T.Query()),
    ),
    labels: S.optional(ListProductsRequestLabelsList.pipe(T.Query())),
    direction: S.optional(ListProductsRequestDirection.pipe(T.Query())),
    order: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/products", code: 200 })),
).annotate({
  identifier: "ListProductsRequest",
}) as any as S.Schema<ListProductsRequest>;

export type ProductListItemGalleryImagesList = Array<ProductGalleryImage>;
export const ProductListItemGalleryImagesList = /*@__PURE__*/ S.Array(
  ProductGalleryImage,
) as any as S.Schema<ProductListItemGalleryImagesList>;

export type ProductListItemLabelsList = Array<string>;
export const ProductListItemLabelsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ProductListItemLabelsList>;

export interface ProductListItem {
  /** Account that sells this product. */
  account: unknown | null;
  /** When the product was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Buyable plan to show and check out with. The configured default when that plan is buyable, otherwise the first buyable plan in product-page order. `null` when none is buyable. */
  default_plan: ProductPublicPlan | null;
  /** Written description displayed on the product page. `null` if none is set. */
  description: string | null;
  /** External identifier stored on the product for your own reference. */
  external_identifier: string | null;
  gallery_images: ProductListItemGalleryImagesList;
  /** Short marketing headline displayed on product page. */
  headline: string | null;
  /** Product ID, prefixed `prod_`. */
  id: string;
  labels: ProductListItemLabelsList;
  /** Active memberships for this product; 0 if public member counts are disabled. */
  member_count: number;
  /** Custom key-value pairs stored on the product. */
  metadata: unknown | null;
  /** Published customer reviews for this product. */
  published_reviews_count: number;
  /** URL slug for the product's public link. */
  route: string;
  /** Product display name shown to customers. */
  title: string;
  /** When the product was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** Whether the product has been verified by Whop. */
  verified: boolean;
  /** Whether the product is publicly visible, hidden, or archived. */
  visibility: string | null;
}
export const ProductListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account: S.NullOr(S.Unknown),
    created_at: S.String,
    default_plan: S.NullOr(ProductPublicPlan),
    description: S.NullOr(S.String),
    external_identifier: S.NullOr(S.String),
    gallery_images: ProductListItemGalleryImagesList,
    headline: S.NullOr(S.String),
    id: S.String,
    labels: ProductListItemLabelsList,
    member_count: S.Number,
    metadata: S.NullOr(S.Unknown),
    published_reviews_count: S.Number,
    route: S.String,
    title: S.String,
    updated_at: S.String,
    verified: S.Boolean,
    visibility: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ProductListItem",
}) as any as S.Schema<ProductListItem>;

export type ListProductsResponseDataList = Array<ProductListItem>;
export const ListProductsResponseDataList = /*@__PURE__*/ S.Array(
  ProductListItem,
) as any as S.Schema<ListProductsResponseDataList>;

export interface ListProductsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListProductsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListProductsResponsePageInfo",
}) as any as S.Schema<ListProductsResponsePageInfo>;

export interface ListProductsResponse {
  data: ListProductsResponseDataList;
  page_info: ListProductsResponsePageInfo;
}
export const ListProductsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListProductsResponseDataList,
    page_info: ListProductsResponsePageInfo,
  }),
).annotate({
  identifier: "ListProductsResponse",
}) as any as S.Schema<ListProductsResponse>;

export interface PublishProductRequest {
  /** The unique identifier of the product, prefixed `prod_`. */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const PublishProductRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/products/{id}/publish", code: 200 })),
).annotate({
  identifier: "PublishProductRequest",
}) as any as S.Schema<PublishProductRequest>;

export interface RetrieveProductRequest {
  /** The unique identifier of the product. */
  id: string;
}
export const RetrieveProductRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/products/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveProductRequest",
}) as any as S.Schema<RetrieveProductRequest>;

export interface UnpublishProductRequest {
  /** The unique identifier of the product, prefixed `prod_`. */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const UnpublishProductRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/products/{id}/unpublish", code: 200 }),
  ),
).annotate({
  identifier: "UnpublishProductRequest",
}) as any as S.Schema<UnpublishProductRequest>;

/** A wide image for the product, shown on the product page and on listing cards. Pass `{ id }` for an existing attachment or `{ direct_upload_id }` for a completed direct upload; `null` removes it. */
export interface UpdateProductRequestBannerImage {
  /** The signed id of a completed direct upload. */
  direct_upload_id?: string;
  /** The tag of an already-uploaded attachment. */
  id?: string;
}
export const UpdateProductRequestBannerImage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    direct_upload_id: S.optional(S.String),
    id: S.optional(S.String),
  }),
).annotate({
  identifier: "UpdateProductRequestBannerImage",
}) as any as S.Schema<UpdateProductRequestBannerImage>;

/** Labels used to group products into collections. Replaces the existing labels. Send an empty array to clear them. */
export type UpdateProductRequestLabelsList = Array<string>;
export const UpdateProductRequestLabelsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpdateProductRequestLabelsList>;

export interface UpdateProductRequest {
  /** The unique identifier of the product. */
  id: string;
  /** A wide image for the product, shown on the product page and on listing cards. Pass `{ id }` for an existing attachment or `{ direct_upload_id }` for a completed direct upload; `null` removes it. */
  banner_image?: UpdateProductRequestBannerImage | null;
  /** A written description displayed on the product page. */
  description?: string | null;
  /** A short marketing headline for the product page. */
  headline?: string | null;
  /** Labels used to group products into collections. Replaces the existing labels. Send an empty array to clear them. */
  labels?: UpdateProductRequestLabelsList | null;
  /** Custom key-value pairs to store on the product. */
  metadata?: unknown | null;
  /** The unique identifier of the tax classification code. See the available [product categories](https://docs.numeral.com/essentials/product-categories). */
  product_tax_code_id?: string | null;
  /** Whether to send an automated welcome message via support chat when a user joins this product. */
  send_welcome_message?: boolean | null;
  /** The display name of the product. */
  title?: string;
  /** Whether the product is visible to customers. */
  visibility?: string;
}
export const UpdateProductRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    banner_image: S.optional(S.NullOr(UpdateProductRequestBannerImage)),
    description: S.optional(S.NullOr(S.String)),
    headline: S.optional(S.NullOr(S.String)),
    labels: S.optional(S.NullOr(UpdateProductRequestLabelsList)),
    metadata: S.optional(S.NullOr(S.Unknown)),
    product_tax_code_id: S.optional(S.NullOr(S.String)),
    send_welcome_message: S.optional(S.NullOr(S.Boolean)),
    title: S.optional(S.String),
    visibility: S.optional(S.String),
  }).pipe(T.Http({ method: "PATCH", uri: "/products/{id}", code: 200 })),
).annotate({
  identifier: "UpdateProductRequest",
}) as any as S.Schema<UpdateProductRequest>;

export type CreateProductError = Conflict | WhopOpError;
/** Create Product Creates a new product for an account. */
export const createProduct: API.OperationMethod<
  CreateProductRequest,
  Product,
  CreateProductError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateProductRequest,
  output: Product,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteProductError = NotFound | WhopOpError;
/** Delete Product Deletes a product. Only products with no memberships, entries, reviews, or invoices can be deleted. */
export const deleteProduct: API.OperationMethod<
  DeleteProductRequest,
  DeleteProductResponse,
  DeleteProductError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteProductRequest,
  output: DeleteProductResponse,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListProductsError = BadRequest | WhopOpError;
/** List Products Returns a paginated list of products. Omit `account_id` to search the public marketplace. */
export const listProducts: API.PaginatedOperationMethod<
  ListProductsRequest,
  ListProductsResponse,
  ListProductsError,
  WhopOpContext,
  ProductListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListProductsRequest,
    output: ListProductsResponse,
    errors: [BadRequest],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type PublishProductError = NotFound | Conflict | WhopOpError;
/** Publish Product Submits a product to the whop.com marketplace for review. The product moves to `pending_review`; a Whop reviewer approves it before it goes live. */
export const publishProduct: API.OperationMethod<
  PublishProductRequest,
  Product,
  PublishProductError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PublishProductRequest,
  output: Product,
  errors: [NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveProductError = NotFound | WhopOpError;
/** Retrieve Product Retrieves a product. Public — no credentials. */
export const retrieveProduct: API.OperationMethod<
  RetrieveProductRequest,
  Product,
  RetrieveProductError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveProductRequest,
  output: Product,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UnpublishProductError = NotFound | Conflict | WhopOpError;
/** Unpublish Product Removes a product from the whop.com marketplace. The product moves to `not_available`. */
export const unpublishProduct: API.OperationMethod<
  UnpublishProductRequest,
  Product,
  UnpublishProductError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UnpublishProductRequest,
  output: Product,
  errors: [NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateProductError = NotFound | WhopOpError;
/** Update Product Updates an existing product. */
export const updateProduct: API.OperationMethod<
  UpdateProductRequest,
  Product,
  UpdateProductError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateProductRequest,
  output: Product,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
