// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface CancelBountySubmissionRequest {
  /** The bounty submission to act on (`btys_` tag). */
  id: string;
}
export const CancelBountySubmissionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "DELETE", uri: "/bounty_submissions/{id}", code: 200 }),
  ),
).annotate({
  identifier: "CancelBountySubmissionRequest",
}) as any as S.Schema<CancelBountySubmissionRequest>;

export interface CancelBountySubmissionResponse {
  /** Always true. */
  deleted: boolean;
  /** ID of the cancelled submission. */
  id: string;
}
export const CancelBountySubmissionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "CancelBountySubmissionResponse",
}) as any as S.Schema<CancelBountySubmissionResponse>;

/** IDs of uploaded files attached as work, up to 10, each prefixed `file_`. Combinable with `urls` and `caption`. */
export type CreateBountySubmissionRequestDeliverableFileIdsList = Array<string>;
export const CreateBountySubmissionRequestDeliverableFileIdsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateBountySubmissionRequestDeliverableFileIdsList>;

/** Legacy shape selector; no longer selects anything. When present it must name an inline shape (`content_url` or `media`). */
export type CreateBountySubmissionRequestDeliverableType =
  | "content_url"
  | "media";
export const CreateBountySubmissionRequestDeliverableType =
  /*@__PURE__*/ S.String;

/** Links to the posted work, up to 10. Combinable with `file_ids` and `caption`. */
export type CreateBountySubmissionRequestDeliverableUrlsList = Array<string>;
export const CreateBountySubmissionRequestDeliverableUrlsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateBountySubmissionRequestDeliverableUrlsList>;

/** The submitted work. Combine `urls`, `file_ids`, and `caption` freely; at least one link or file is required. */
export interface CreateBountySubmissionRequestDeliverable {
  /** Written context shown to reviewers alongside the work. */
  caption?: string | null;
  /** IDs of uploaded files attached as work, up to 10, each prefixed `file_`. Combinable with `urls` and `caption`. */
  file_ids?: CreateBountySubmissionRequestDeliverableFileIdsList;
  /** Legacy shape selector; no longer selects anything. When present it must name an inline shape (`content_url` or `media`). */
  type?: CreateBountySubmissionRequestDeliverableType | (string & {}) | null;
  /** Links to the posted work, up to 10. Combinable with `file_ids` and `caption`. */
  urls?: CreateBountySubmissionRequestDeliverableUrlsList;
}
export const CreateBountySubmissionRequestDeliverable = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      caption: S.optional(S.NullOr(S.String)),
      file_ids: S.optional(CreateBountySubmissionRequestDeliverableFileIdsList),
      type: S.optional(S.NullOr(CreateBountySubmissionRequestDeliverableType)),
      urls: S.optional(CreateBountySubmissionRequestDeliverableUrlsList),
    }),
).annotate({
  identifier: "CreateBountySubmissionRequestDeliverable",
}) as any as S.Schema<CreateBountySubmissionRequestDeliverable>;

/** Optional capture metadata describing where and how the footage was recorded. Persisted on the submission. On a `data_capture` bounty every field except `fov` is required whenever metadata is provided. */
export interface CreateBountySubmissionRequestMetadata {
  /** City the footage was recorded in. */
  city?: string | null;
  /** Country the footage was recorded in. */
  country?: string | null;
  /** Device the footage was recorded on. */
  device?: string | null;
  /** Horizontal field of view in degrees. */
  fov?: number | null;
  /** Identifier of the person who recorded the footage. */
  operator?: string | null;
  /** Site or venue the footage was recorded at. */
  site?: string | null;
  /** Station or position within the site. */
  station?: string | null;
}
export const CreateBountySubmissionRequestMetadata = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      city: S.optional(S.NullOr(S.String)),
      country: S.optional(S.NullOr(S.String)),
      device: S.optional(S.NullOr(S.String)),
      fov: S.optional(S.NullOr(S.Number)),
      operator: S.optional(S.NullOr(S.String)),
      site: S.optional(S.NullOr(S.String)),
      station: S.optional(S.NullOr(S.String)),
    }),
).annotate({
  identifier: "CreateBountySubmissionRequestMetadata",
}) as any as S.Schema<CreateBountySubmissionRequestMetadata>;

export interface CreateBountySubmissionRequest {
  /** Affiliate code crediting the referrer, when the worker arrived through one. */
  affiliate_code?: string | null;
  /** The bounty to submit to (`bnty_` tag). */
  bounty_id: string;
  /** The submitted work. Combine `urls`, `file_ids`, and `caption` freely; at least one link or file is required. */
  deliverable?: CreateBountySubmissionRequestDeliverable | null;
  /** Optional capture metadata describing where and how the footage was recorded. Persisted on the submission. On a `data_capture` bounty every field except `fov` is required whenever metadata is provided. */
  metadata?: CreateBountySubmissionRequestMetadata | null;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateBountySubmissionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    affiliate_code: S.optional(S.NullOr(S.String)),
    bounty_id: S.String,
    deliverable: S.optional(S.NullOr(CreateBountySubmissionRequestDeliverable)),
    metadata: S.optional(S.NullOr(CreateBountySubmissionRequestMetadata)),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/bounty_submissions", code: 200 })),
).annotate({
  identifier: "CreateBountySubmissionRequest",
}) as any as S.Schema<CreateBountySubmissionRequest>;

/** Recording and validation state. `recording` is still capturing; `verifying` is running server-side validation; `ready` passed validation and counts toward the verified-duration payout gate; `failed` did not validate. */
export type BountyCaptureClipStatus =
  | "recording"
  | "verifying"
  | "ready"
  | "failed";
export const BountyCaptureClipStatus = /*@__PURE__*/ S.String;

export interface BountyCaptureClip {
  /** The bounty submission (attempt) this clip belongs to, prefixed `btys_`. */
  bounty_submission_id: string;
  /** When the clip was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Server-validated clip duration in whole seconds. `null` until validation completes. */
  duration_seconds: number | null;
  /** Stable validation failure code. `null` unless `status` is `failed`. */
  failure_code: string | null;
  /** Human-readable validation failure reason. `null` unless `status` is `failed`. */
  failure_message: string | null;
  /** Temporary signed URL for the video frame timestamp log. Returned only on single-clip reads for an authorized viewer; `null` on list responses or until the artifact is attached. */
  frames_url: string | null;
  /** Capture clip ID, prefixed `bclip_`. */
  id: string;
  /** Temporary signed URL for the IMU (accelerometer + gyroscope) log. Returned only on single-clip reads for an authorized viewer; `null` on list responses or until the artifact is attached. */
  imu_url: string | null;
  /** Temporary signed URL for the capture manifest. Returned only on single-clip reads for an authorized viewer; `null` on list responses or until the artifact is attached. */
  manifest_url: string | null;
  /** When server-side validation completed successfully, as an ISO 8601 timestamp. `null` until then. */
  ready_at: string | null;
  /** The clip's stable order within the attempt, starting at 1. */
  sequence: number;
  /** Recording and validation state. `recording` is still capturing; `verifying` is running server-side validation; `ready` passed validation and counts toward the verified-duration payout gate; `failed` did not validate. */
  status: BountyCaptureClipStatus;
  /** When the clip was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** Temporary signed URL for the synchronized MP4 video. Returned only on single-clip reads for an authorized viewer; `null` on list responses or until the artifact is attached. */
  video_url: string | null;
}
export const BountyCaptureClip = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bounty_submission_id: S.String,
    created_at: S.String,
    duration_seconds: S.NullOr(S.Number),
    failure_code: S.NullOr(S.String),
    failure_message: S.NullOr(S.String),
    frames_url: S.NullOr(S.String),
    id: S.String,
    imu_url: S.NullOr(S.String),
    manifest_url: S.NullOr(S.String),
    ready_at: S.NullOr(S.String),
    sequence: S.Number,
    status: BountyCaptureClipStatus,
    updated_at: S.String,
    video_url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "BountyCaptureClip",
}) as any as S.Schema<BountyCaptureClip>;

export type BountySubmissionCaptureClipsList = Array<BountyCaptureClip>;
export const BountySubmissionCaptureClipsList = /*@__PURE__*/ S.Array(
  BountyCaptureClip,
) as any as S.Schema<BountySubmissionCaptureClipsList>;

/** How the work arrived when it came in through the API in one shot, informational only — read the work from `deliverable_urls`, `files`, and `capture_clips` directly. `null` for submissions whose proof is a livestream recording, including ones that attached links or files on submit. */
export type BountySubmissionDeliverableType =
  | "content_url"
  | "media"
  | "data_capture";
export const BountySubmissionDeliverableType = /*@__PURE__*/ S.String;

export type BountySubmissionDeliverableUrlsList = Array<string>;
export const BountySubmissionDeliverableUrlsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<BountySubmissionDeliverableUrlsList>;

/** Broad kind of file. */
export type BountySubmissionFileAttachmentType =
  | "image"
  | "video"
  | "audio"
  | "other";
export const BountySubmissionFileAttachmentType = /*@__PURE__*/ S.String;

export interface BountySubmissionFile {
  /** Broad kind of file. */
  attachment_type: BountySubmissionFileAttachmentType | null;
  /** MIME type of the file. */
  content_type: string | null;
  /** Name the file was uploaded with. */
  filename: string | null;
  /** File ID, prefixed `file_`. */
  id: string;
  /** Temporary download URL for the file. */
  url: string | null;
}
export const BountySubmissionFile = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    attachment_type: S.NullOr(BountySubmissionFileAttachmentType),
    content_type: S.NullOr(S.String),
    filename: S.NullOr(S.String),
    id: S.String,
    url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "BountySubmissionFile",
}) as any as S.Schema<BountySubmissionFile>;

export type BountySubmissionFilesList = Array<BountySubmissionFile>;
export const BountySubmissionFilesList = /*@__PURE__*/ S.Array(
  BountySubmissionFile,
) as any as S.Schema<BountySubmissionFilesList>;

/** Recording lifecycle state. */
export type BountySubmissionLivestreamFeedRecordingStatus =
  | "recording"
  | "processing"
  | "completed"
  | "failed";
export const BountySubmissionLivestreamFeedRecordingStatus =
  /*@__PURE__*/ S.String;

export interface BountySubmissionLivestreamFeed {
  /** When the proof livestream ended, as an ISO 8601 timestamp. `null` while it is still live — a feed with a `started_at` and no `ended_at` is streaming right now. */
  ended_at: string | null;
  /** Livestream feed ID. */
  id: string;
  /** Recording lifecycle state. */
  recording_status: BountySubmissionLivestreamFeedRecordingStatus | null;
  /** Playback URL for a completed proof recording, when available. */
  recording_url: string | null;
  /** When the proof livestream went live, as an ISO 8601 timestamp. `null` before it starts. */
  started_at: string | null;
  /** Current proof thumbnail URL, when available. */
  thumbnail_url: string | null;
  /** Display title for the proof livestream. */
  title: string;
}
export const BountySubmissionLivestreamFeed = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ended_at: S.NullOr(S.String),
    id: S.String,
    recording_status: S.NullOr(BountySubmissionLivestreamFeedRecordingStatus),
    recording_url: S.NullOr(S.String),
    started_at: S.NullOr(S.String),
    thumbnail_url: S.NullOr(S.String),
    title: S.String,
  }),
).annotate({
  identifier: "BountySubmissionLivestreamFeed",
}) as any as S.Schema<BountySubmissionLivestreamFeed>;

/** Lifecycle state. `in_progress` submissions are active attempts that have not submitted proof yet; `submitted` submissions await review; `approved` submissions were accepted and paid; `denied` submissions were rejected. `null` when the attempt ended without proof, taking it out of the public lifecycle — those attempts are absent from every public list and read. */
export type BountySubmissionStatus =
  | "in_progress"
  | "submitted"
  | "approved"
  | "denied";
export const BountySubmissionStatus = /*@__PURE__*/ S.String;

export interface UserProfilePicture {
  /** Avatar image URL. Always present — a generated placeholder when the user set no picture. */
  url: string;
}
export const UserProfilePicture = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
  }),
).annotate({
  identifier: "UserProfilePicture",
}) as any as S.Schema<UserProfilePicture>;

export interface UserSummary {
  /** User ID, prefixed `user_`. */
  id: string;
  /** Display name. */
  name: string | null;
  /** Avatar wrapper; its `url` is always present, using a generated placeholder when the user set no picture. */
  profile_picture: UserProfilePicture;
  /** Public username. */
  username: string;
}
export const UserSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    profile_picture: UserProfilePicture,
    username: S.String,
  }),
).annotate({ identifier: "UserSummary" }) as any as S.Schema<UserSummary>;

export interface BountySubmission {
  /** The bounty the work was submitted to, prefixed `bnty_`. */
  bounty_id: string;
  capture_clips: BountySubmissionCaptureClipsList | null;
  /** The vendor filename stem `Country_City_Site_Station_Operator`, derived from the capture metadata. `null` until every component is present. */
  capture_filename: string | null;
  /** Number of verified capture clips accepted for this submission so far. `0` for submissions whose deliverable doesn't accumulate clips. */
  captured_clip_count: number;
  /** Total verified duration of accepted capture clips, in whole seconds. `0` for submissions whose deliverable doesn't accumulate clips. */
  captured_duration_seconds: number;
  /** Capture metadata: city the footage was recorded in. `null` unless capture metadata was provided. */
  city: string | null;
  /** When the worker claimed the submission, as an ISO 8601 timestamp. */
  claimed_at: string | null;
  /** Written proof the worker submitted with their work. */
  content: string | null;
  /** Capture metadata: country the footage was recorded in. `null` unless capture metadata was provided. */
  country: string | null;
  /** When the submission was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** How the work arrived when it came in through the API in one shot, informational only — read the work from `deliverable_urls`, `files`, and `capture_clips` directly. `null` for submissions whose proof is a livestream recording, including ones that attached links or files on submit. */
  deliverable_type: BountySubmissionDeliverableType | null;
  deliverable_urls: BountySubmissionDeliverableUrlsList | null;
  /** Why the submission was denied, when a presentable reason exists. Always `null` unless `status` is `denied`. */
  denial_reason: string | null;
  /** Capture metadata: device the footage was recorded on. `null` unless capture metadata was provided. */
  device: string | null;
  files: BountySubmissionFilesList;
  /** Capture metadata: horizontal field of view in degrees. `null` when not reported. */
  fov: number | null;
  /** Submission ID, prefixed `btys_`. */
  id: string;
  /** Latest public proof livestream attached to the submission. */
  latest_proof_livestream_feed: BountySubmissionLivestreamFeed | null;
  /** Capture metadata: identifier of the person who recorded the footage. `null` unless capture metadata was provided. */
  operator: string | null;
  /** When the submission was approved or denied, as an ISO 8601 timestamp. `null` until then. */
  resolved_at: string | null;
  /** Capture metadata: site or venue the footage was recorded at. `null` unless capture metadata was provided. */
  site: string | null;
  /** Capture metadata: station or position within the site. `null` unless capture metadata was provided. */
  station: string | null;
  /** Lifecycle state. `in_progress` submissions are active attempts that have not submitted proof yet; `submitted` submissions await review; `approved` submissions were accepted and paid; `denied` submissions were rejected. `null` when the attempt ended without proof, taking it out of the public lifecycle — those attempts are absent from every public list and read. */
  status: BountySubmissionStatus | null;
  /** When proof was submitted for review, as an ISO 8601 timestamp. `null` while the attempt is in progress. */
  submitted_at: string | null;
  /** When the submission was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** User who submitted the work. */
  worker: UserSummary;
}
export const BountySubmission = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bounty_id: S.String,
    capture_clips: S.NullOr(BountySubmissionCaptureClipsList),
    capture_filename: S.NullOr(S.String),
    captured_clip_count: S.Number,
    captured_duration_seconds: S.Number,
    city: S.NullOr(S.String),
    claimed_at: S.NullOr(S.String),
    content: S.NullOr(S.String),
    country: S.NullOr(S.String),
    created_at: S.String,
    deliverable_type: S.NullOr(BountySubmissionDeliverableType),
    deliverable_urls: S.NullOr(BountySubmissionDeliverableUrlsList),
    denial_reason: S.NullOr(S.String),
    device: S.NullOr(S.String),
    files: BountySubmissionFilesList,
    fov: S.NullOr(S.Number),
    id: S.String,
    latest_proof_livestream_feed: S.NullOr(BountySubmissionLivestreamFeed),
    operator: S.NullOr(S.String),
    resolved_at: S.NullOr(S.String),
    site: S.NullOr(S.String),
    station: S.NullOr(S.String),
    status: S.NullOr(BountySubmissionStatus),
    submitted_at: S.NullOr(S.String),
    updated_at: S.String,
    worker: UserSummary,
  }),
).annotate({
  identifier: "BountySubmission",
}) as any as S.Schema<BountySubmission>;

export type ListBountySubmissionsRequestStatus =
  | "in_progress"
  | "submitted"
  | "approved"
  | "denied";
export const ListBountySubmissionsRequestStatus = /*@__PURE__*/ S.String;

export type ListBountySubmissionsRequestOrder = "created_at" | "updated_at";
export const ListBountySubmissionsRequestOrder = /*@__PURE__*/ S.String;

export type ListBountySubmissionsRequestDirection = "asc" | "desc";
export const ListBountySubmissionsRequestDirection = /*@__PURE__*/ S.String;

export interface ListBountySubmissionsRequest {
  /** Scope the list to submissions on this account's bounties (`biz_` tag). Requires read access to the account. */
  account_id?: string;
  /** Only submissions on this bounty (`bnty_` tag). */
  bounty_id?: string;
  /** Filter by lifecycle state. */
  status?: ListBountySubmissionsRequestStatus | (string & {});
  /** Only submissions created after this ISO 8601 timestamp. */
  created_after?: string;
  /** Only submissions created before this ISO 8601 timestamp. */
  created_before?: string;
  /** Sort field. */
  order?: ListBountySubmissionsRequestOrder | (string & {});
  /** Sort direction. */
  direction?: ListBountySubmissionsRequestDirection | (string & {});
  /** Number of submissions to return from the start of the window. */
  first?: number;
  /** Cursor to paginate forwards from. */
  after?: string;
  /** Number of submissions to return from the end of the window. */
  last?: number;
  /** Cursor to paginate backwards from. */
  before?: string;
}
export const ListBountySubmissionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    bounty_id: S.optional(S.String.pipe(T.Query())),
    status: S.optional(ListBountySubmissionsRequestStatus.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListBountySubmissionsRequestOrder.pipe(T.Query())),
    direction: S.optional(
      ListBountySubmissionsRequestDirection.pipe(T.Query()),
    ),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/bounty_submissions", code: 200 })),
).annotate({
  identifier: "ListBountySubmissionsRequest",
}) as any as S.Schema<ListBountySubmissionsRequest>;

export type ListBountySubmissionsResponseDataList = Array<BountySubmission>;
export const ListBountySubmissionsResponseDataList = /*@__PURE__*/ S.Array(
  BountySubmission,
) as any as S.Schema<ListBountySubmissionsResponseDataList>;

export interface ListBountySubmissionsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListBountySubmissionsResponsePageInfo = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      end_cursor: S.NullOr(S.String),
      has_next_page: S.Boolean,
      has_previous_page: S.Boolean,
      start_cursor: S.NullOr(S.String),
    }),
).annotate({
  identifier: "ListBountySubmissionsResponsePageInfo",
}) as any as S.Schema<ListBountySubmissionsResponsePageInfo>;

export interface ListBountySubmissionsResponse {
  data: ListBountySubmissionsResponseDataList;
  page_info: ListBountySubmissionsResponsePageInfo;
}
export const ListBountySubmissionsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListBountySubmissionsResponseDataList,
    page_info: ListBountySubmissionsResponsePageInfo,
  }),
).annotate({
  identifier: "ListBountySubmissionsResponse",
}) as any as S.Schema<ListBountySubmissionsResponse>;

export interface RetrieveBountySubmissionRequest {
  /** The bounty submission to act on (`btys_` tag). */
  id: string;
  /** Read the submission as this account (`biz_` tag), scoping the lookup to its bounties rather than the caller's own work. Requires read access to the account. Without it the lookup covers only what the credential owns — the submissions the caller authored plus those on bounties they posted. */
  account_id?: string;
}
export const RetrieveBountySubmissionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    account_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/bounty_submissions/{id}", code: 200 }),
  ),
).annotate({
  identifier: "RetrieveBountySubmissionRequest",
}) as any as S.Schema<RetrieveBountySubmissionRequest>;

/** IDs of uploaded files attached as work, up to 10, each prefixed `file_`. Combinable with `urls` and `caption`. */
export type SubmitBountySubmissionRequestDeliverableFileIdsList = Array<string>;
export const SubmitBountySubmissionRequestDeliverableFileIdsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<SubmitBountySubmissionRequestDeliverableFileIdsList>;

/** Links to the posted work, up to 10. Combinable with `file_ids` and `caption`. */
export type SubmitBountySubmissionRequestDeliverableUrlsList = Array<string>;
export const SubmitBountySubmissionRequestDeliverableUrlsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<SubmitBountySubmissionRequestDeliverableUrlsList>;

/** Work to attach to the submission. Combine `urls`, `file_ids`, and `caption` freely; all are optional. */
export interface SubmitBountySubmissionRequestDeliverable {
  /** Written context shown to reviewers alongside the work. */
  caption?: string | null;
  /** IDs of uploaded files attached as work, up to 10, each prefixed `file_`. Combinable with `urls` and `caption`. */
  file_ids?: SubmitBountySubmissionRequestDeliverableFileIdsList;
  /** Links to the posted work, up to 10. Combinable with `file_ids` and `caption`. */
  urls?: SubmitBountySubmissionRequestDeliverableUrlsList;
}
export const SubmitBountySubmissionRequestDeliverable = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      caption: S.optional(S.NullOr(S.String)),
      file_ids: S.optional(SubmitBountySubmissionRequestDeliverableFileIdsList),
      urls: S.optional(SubmitBountySubmissionRequestDeliverableUrlsList),
    }),
).annotate({
  identifier: "SubmitBountySubmissionRequestDeliverable",
}) as any as S.Schema<SubmitBountySubmissionRequestDeliverable>;

export interface SubmitBountySubmissionRequest {
  /** The claimed attempt to submit for review (`btys_` tag). */
  id: string;
  /** Work to attach to the submission. Combine `urls`, `file_ids`, and `caption` freely; all are optional. */
  deliverable?: SubmitBountySubmissionRequestDeliverable | null;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const SubmitBountySubmissionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    deliverable: S.optional(S.NullOr(SubmitBountySubmissionRequestDeliverable)),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/bounty_submissions/{id}/submit",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SubmitBountySubmissionRequest",
}) as any as S.Schema<SubmitBountySubmissionRequest>;

export type CancelBountySubmissionError = Forbidden | NotFound | WhopOpError;
/** Cancel Bounty Submission Cancels the caller's own active attempt on a bounty and discards any accumulated capture clips. Only the worker who started the attempt can cancel it — account API keys cannot. */
export const cancelBountySubmission: API.OperationMethod<
  CancelBountySubmissionRequest,
  CancelBountySubmissionResponse,
  CancelBountySubmissionError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CancelBountySubmissionRequest,
  output: CancelBountySubmissionResponse,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreateBountySubmissionError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Create Bounty Submission Creates a submission on a workforce bounty. Include a `deliverable` payload — any combination of links and uploaded files, with at least one of the two — and the submission goes straight to review; create is the only step. For `data_capture` bounties, omit the deliverable: this starts a claimed attempt whose proof accumulates server-side, and the separate submit endpoint sends it to review once complete. Requires a user credential — account API keys cannot author submissions. */
export const createBountySubmission: API.OperationMethod<
  CreateBountySubmissionRequest,
  BountySubmission,
  CreateBountySubmissionError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateBountySubmissionRequest,
  output: BountySubmission,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListBountySubmissionsError = BadRequest | Forbidden | WhopOpError;
/** List Bounty Submissions Lists bounty submissions visible to the credential — for a user token, the submissions they authored plus those on bounties they posted; for an account API key, the submissions on the account's bounties. For the anonymous view of one bounty's reviewed work, use the submissions list under the bounty instead. */
export const listBountySubmissions: API.PaginatedOperationMethod<
  ListBountySubmissionsRequest,
  ListBountySubmissionsResponse,
  ListBountySubmissionsError,
  WhopOpContext,
  BountySubmission
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListBountySubmissionsRequest,
    output: ListBountySubmissionsResponse,
    errors: [BadRequest, Forbidden],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveBountySubmissionError = NotFound | WhopOpError;
/** Retrieve Bounty Submission Retrieves one bounty submission the credential can see — one the caller authored, or one on a bounty they posted or their account owns. Reading another member's work on an account's bounty takes `account_id`, the same way the list does. */
export const retrieveBountySubmission: API.OperationMethod<
  RetrieveBountySubmissionRequest,
  BountySubmission,
  RetrieveBountySubmissionError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveBountySubmissionRequest,
  output: BountySubmission,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type SubmitBountySubmissionError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Submit Bounty Submission Submits a claimed attempt for review. A livestream attempt needs an ended proof stream and can attach an optional `deliverable` — links, files, and a caption in any combination; if the attempt already went to review when its stream ended, the payload attaches to it once, until reviewers start voting. A data capture attempt instead needs enough validated clip time and takes no payload. Only the worker who started the attempt can submit it — account API keys cannot. */
export const submitBountySubmission: API.OperationMethod<
  SubmitBountySubmissionRequest,
  BountySubmission,
  SubmitBountySubmissionError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubmitBountySubmissionRequest,
  output: BountySubmission,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
