// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** The different use cases for generating an account link. */
export type AccountLinkUseCases = "account_onboarding" | "payouts_portal";
export const AccountLinkUseCases = /*@__PURE__*/ S.String;

export interface CreateAccountLinkRequest {
  /** The unique identifier of the company to generate the link for, starting with 'biz_'. Must be a sub-merchant of the API key's company. */
  company_id: string;
  /** The URL to redirect the user to if the session expires and needs to be re-authenticated, such as 'https://example.com/refresh'. */
  refresh_url: string;
  /** The URL to redirect the user to when they want to return to your site, such as 'https://example.com/return'. */
  return_url: string;
  /** The purpose of the account link, such as hosted payouts portal or hosted KYC onboarding. */
  use_case: AccountLinkUseCases | (string & {});
}
export const CreateAccountLinkRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    company_id: S.String,
    refresh_url: S.String,
    return_url: S.String,
    use_case: AccountLinkUseCases,
  }).pipe(T.Http({ method: "POST", uri: "/account_links", code: 200 })),
).annotate({
  identifier: "CreateAccountLinkRequest",
}) as any as S.Schema<CreateAccountLinkRequest>;

/** A temporary, time-limited URL that grants a user access to an external account management page. */
export interface AccountLink {
  /** The timestamp after which this account link URL is no longer valid. */
  expires_at: string;
  /** The temporary URL to redirect the user to for account access. Expires at the time specified by expires_at. */
  url: string;
}
export const AccountLink = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    expires_at: S.String,
    url: S.String,
  }),
).annotate({ identifier: "AccountLink" }) as any as S.Schema<AccountLink>;

export type CreateAccountLinkError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create account link [Legacy API — https://docs.whop.com/api-reference] Generate a URL that directs a sub-merchant to their account portal, such as the hosted payouts dashboard or the KYC onboarding flow. */
export const createAccountLink: API.OperationMethod<
  CreateAccountLinkRequest,
  AccountLink,
  CreateAccountLinkError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAccountLinkRequest,
  output: AccountLink,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
