// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** Every value accepts `null`, so a form seeded from a previous response can round-trip unset fields unchanged. */
export interface CreateVerificationRequestBodyCase0Address {
  city?: string | null;
  /** Two-letter ISO 3166-1 country code, for example `US`, `DE`, or `GB`. */
  country?: string | null;
  /** First line of the street address. */
  line1?: string | null;
  /** Second line of the street address. */
  line2?: string | null;
  /** Postal or ZIP code. */
  postal_code?: string | null;
  /** State, province, or region code, for example `CA`. */
  state?: string | null;
}
export const CreateVerificationRequestBodyCase0Address =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      city: S.optional(S.NullOr(S.String)),
      country: S.optional(S.NullOr(S.String)),
      line1: S.optional(S.NullOr(S.String)),
      line2: S.optional(S.NullOr(S.String)),
      postal_code: S.optional(S.NullOr(S.String)),
      state: S.optional(S.NullOr(S.String)),
    }),
  ).annotate({
    identifier: "CreateVerificationRequestBodyCase0Address",
  }) as any as S.Schema<CreateVerificationRequestBodyCase0Address>;

/** Identity document being sent, when verifying with `documents`. Decides exactly which file slots to send: `ID_CARD` → `id_card_front` + `id_card_back` + `selfie`; `DRIVERS` → `drivers_front` + `drivers_back` + `selfie`; `RESIDENCE_PERMIT` → `residence_permit_front` + `residence_permit_back` + `selfie`; `PASSPORT` → `passport_front` + `selfie`. See [Identity documents](/developer/verification/identity-documents). */
export type CreateVerificationRequestBodyCase0DocumentType =
  | "ID_CARD"
  | "DRIVERS"
  | "RESIDENCE_PERMIT"
  | "PASSPORT";
export const CreateVerificationRequestBodyCase0DocumentType =
  /*@__PURE__*/ S.String;

/** Identity document files, each value the file's raw bytes base64-encoded (JPEG, PNG, or PDF, up to 5MB per file before encoding). Sending this object verifies the person from the files in this request instead of a hosted session — individual verifications only, and the request must also carry `document_type`, `first_name`, `last_name`, `date_of_birth`, `country`, `phone`, `tax_identification_number`, and an `address` with `line1`, `city`, `state`, and `postal_code`. Send every slot for your `document_type` — a missing or rejected file fails the whole request and nothing is submitted; review starts automatically once every document is accepted. See [Identity documents](/developer/verification/identity-documents) for a full walkthrough. */
export interface CreateVerificationRequestBodyCase0Documents {
  /** Back of the driver's license, base64-encoded. Required when `document_type` is `DRIVERS`. */
  drivers_back?: string;
  /** Front of the driver's license, base64-encoded. Required when `document_type` is `DRIVERS`. */
  drivers_front?: string;
  /** Back of the ID card, base64-encoded. Required when `document_type` is `ID_CARD`. */
  id_card_back?: string;
  /** Front of the ID card, base64-encoded. Required when `document_type` is `ID_CARD`. */
  id_card_front?: string;
  /** Photo page of the passport, base64-encoded. Required when `document_type` is `PASSPORT`. */
  passport_front?: string;
  /** Back of the residence permit, base64-encoded. Required when `document_type` is `RESIDENCE_PERMIT`. */
  residence_permit_back?: string;
  /** Front of the residence permit, base64-encoded. Required when `document_type` is `RESIDENCE_PERMIT`. */
  residence_permit_front?: string;
  /** Photo of the person's face, base64-encoded. Always required, with every document type. Must be JPEG or PNG. */
  selfie?: string;
}
export const CreateVerificationRequestBodyCase0Documents =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      drivers_back: S.optional(S.String),
      drivers_front: S.optional(S.String),
      id_card_back: S.optional(S.String),
      id_card_front: S.optional(S.String),
      passport_front: S.optional(S.String),
      residence_permit_back: S.optional(S.String),
      residence_permit_front: S.optional(S.String),
      selfie: S.optional(S.String),
    }),
  ).annotate({
    identifier: "CreateVerificationRequestBodyCase0Documents",
  }) as any as S.Schema<CreateVerificationRequestBodyCase0Documents>;

/** Verification type. Defaults to `individual`. */
export type CreateVerificationRequestBodyCase0Kind = "individual";
export const CreateVerificationRequestBodyCase0Kind = /*@__PURE__*/ S.String;

/** Request body for an individual (KYC) verification. Omit `kind` or set it to `individual`. KYC is required to pay out funds and is a prerequisite for Whop Card access. Accepting payments does not require verification until a business reaches $5000 in payments. Add `business_name` and `business_structure` if the individual operates under a business entity — this enables payouts to be received by a business bank account. `country` is always the individual's own country, and the supported `business_structure` values vary by it — see [Business structures](/developer/verification/business-structures). */
export interface CreateVerificationRequestBodyCase0 {
  /** Every value accepts `null`, so a form seeded from a previous response can round-trip unset fields unchanged. */
  address?: CreateVerificationRequestBodyCase0Address;
  /** Legal business name for a sole proprietor or single-member LLC. */
  business_name?: string;
  /** Entity type for sole proprietors, such as `single_member_llc`. Supported values vary by country of incorporation — see [Business structures](/developer/verification/business-structures). */
  business_structure?: string;
  /** The business ID number of the company, as appropriate for the company's country. Examples are an Employer Identification Number (EIN) in the US, a Business Number in Canada, or a Company Number in the UK. */
  business_tax_identification_number?: string;
  /** Business website URL. Whop store pages are not accepted. */
  business_website?: string;
  /** Two-letter ISO 3166-1 country code, for example `US`, `DE`, or `GB`. */
  country?: string;
  /** Formatted as `YYYY-MM-DD`. */
  date_of_birth?: string;
  /** Identity document being sent, when verifying with `documents`. Decides exactly which file slots to send: `ID_CARD` → `id_card_front` + `id_card_back` + `selfie`; `DRIVERS` → `drivers_front` + `drivers_back` + `selfie`; `RESIDENCE_PERMIT` → `residence_permit_front` + `residence_permit_back` + `selfie`; `PASSPORT` → `passport_front` + `selfie`. See [Identity documents](/developer/verification/identity-documents). */
  document_type?:
    | CreateVerificationRequestBodyCase0DocumentType
    | (string & {});
  /** Identity document files, each value the file's raw bytes base64-encoded (JPEG, PNG, or PDF, up to 5MB per file before encoding). Sending this object verifies the person from the files in this request instead of a hosted session — individual verifications only, and the request must also carry `document_type`, `first_name`, `last_name`, `date_of_birth`, `country`, `phone`, `tax_identification_number`, and an `address` with `line1`, `city`, `state`, and `postal_code`. Send every slot for your `document_type` — a missing or rejected file fails the whole request and nothing is submitted; review starts automatically once every document is accepted. See [Identity documents](/developer/verification/identity-documents) for a full walkthrough. */
  documents?: CreateVerificationRequestBodyCase0Documents;
  first_name?: string;
  /** Verification type. Defaults to `individual`. */
  kind?: CreateVerificationRequestBodyCase0Kind | (string & {});
  last_name?: string;
  phone?: string;
  /** Sumsub share token for a person a partner has already verified, generated by their Sumsub account for Whop's client ID. Reuses that verification instead of collecting anything: Whop imports the shared identity, rechecks it against its own requirements, and returns `approved` when it satisfies them, or `pending` with a `session_url` covering only what is still missing. Requires a Reusable KYC agreement between the partner's Sumsub account and Whop's. Each token works once, and cannot be combined with `documents` or `document_type`. */
  share_token?: string;
  /** The government-issued ID number of the person being verified — the individual for a KYC verification, or the business representative for a KYB verification — as appropriate for their country. Examples are a Social Security Number (SSN) in the US, or a Social Insurance Number in Canada. */
  tax_identification_number?: string;
}
export const CreateVerificationRequestBodyCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.optional(CreateVerificationRequestBodyCase0Address),
    business_name: S.optional(S.String),
    business_structure: S.optional(S.String),
    business_tax_identification_number: S.optional(S.String),
    business_website: S.optional(S.String),
    country: S.optional(S.String),
    date_of_birth: S.optional(S.String),
    document_type: S.optional(CreateVerificationRequestBodyCase0DocumentType),
    documents: S.optional(CreateVerificationRequestBodyCase0Documents),
    first_name: S.optional(S.String),
    kind: S.optional(CreateVerificationRequestBodyCase0Kind),
    last_name: S.optional(S.String),
    phone: S.optional(S.String),
    share_token: S.optional(S.String),
    tax_identification_number: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateVerificationRequestBodyCase0",
}) as any as S.Schema<CreateVerificationRequestBodyCase0>;

/** Every value accepts `null`, so a form seeded from a previous response can round-trip unset fields unchanged. */
export type CreateVerificationRequestBodyCase1Address =
  CreateVerificationRequestBodyCase0Address;
export const CreateVerificationRequestBodyCase1Address =
  CreateVerificationRequestBodyCase0Address;

/** Must be `business` to start a KYB verification. */
export type CreateVerificationRequestBodyCase1Kind = "business";
export const CreateVerificationRequestBodyCase1Kind = /*@__PURE__*/ S.String;

/** Request body for a business entity (KYB) verification. Set `kind` to `business`. KYB includes everything KYC provides and additionally unlocks financing options for payments and business Whop Cards. */
export interface CreateVerificationRequestBodyCase1 {
  /** Every value accepts `null`, so a form seeded from a previous response can round-trip unset fields unchanged. */
  address?: CreateVerificationRequestBodyCase0Address;
  /** Legal business name. */
  business_name?: string;
  /** Legal entity structure of the business, such as `private_corporation` or `sole_proprietorship`. Supported values vary by country of incorporation — see [Business structures](/developer/verification/business-structures). */
  business_structure?: string;
  /** The business ID number of the company, as appropriate for the company's country. Examples are an Employer Identification Number (EIN) in the US, a Business Number in Canada, or a Company Number in the UK. */
  business_tax_identification_number?: string;
  /** Business website URL. Whop store pages are not accepted. */
  business_website?: string;
  /** Country of incorporation as a two-letter ISO 3166-1 country code. */
  country?: string;
  /** Business representative's date of birth, formatted as `YYYY-MM-DD`. */
  date_of_birth?: string;
  /** First name of the business representative, used to prefill the verification session. */
  first_name?: string;
  /** Must be `business` to start a KYB verification. */
  kind?: CreateVerificationRequestBodyCase1Kind | (string & {});
  /** Last name of the business representative, used to prefill the verification session. */
  last_name?: string;
  /** State or region where the business is incorporated. */
  place_of_incorporation?: string;
  /** Sumsub share token for a person a partner has already verified, generated by their Sumsub account for Whop's client ID. Reuses that verification instead of collecting anything: Whop imports the shared identity, rechecks it against its own requirements, and returns `approved` when it satisfies them, or `pending` with a `session_url` covering only what is still missing. Requires a Reusable KYC agreement between the partner's Sumsub account and Whop's. Each token works once, and cannot be combined with `documents` or `document_type`. */
  share_token?: string;
  /** The government-issued ID number of the person being verified — the individual for a KYC verification, or the business representative for a KYB verification — as appropriate for their country. Examples are a Social Security Number (SSN) in the US, or a Social Insurance Number in Canada. */
  tax_identification_number?: string;
}
export const CreateVerificationRequestBodyCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.optional(CreateVerificationRequestBodyCase0Address),
    business_name: S.optional(S.String),
    business_structure: S.optional(S.String),
    business_tax_identification_number: S.optional(S.String),
    business_website: S.optional(S.String),
    country: S.optional(S.String),
    date_of_birth: S.optional(S.String),
    first_name: S.optional(S.String),
    kind: S.optional(CreateVerificationRequestBodyCase1Kind),
    last_name: S.optional(S.String),
    place_of_incorporation: S.optional(S.String),
    share_token: S.optional(S.String),
    tax_identification_number: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateVerificationRequestBodyCase1",
}) as any as S.Schema<CreateVerificationRequestBodyCase1>;

export type CreateVerificationRequestBody =
  | CreateVerificationRequestBodyCase0
  | CreateVerificationRequestBodyCase1;
export const CreateVerificationRequestBody =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateVerificationRequestBody>;

export interface CreateVerificationRequest {
  /** Account or user ID whose identity you want to verify. Use a `biz_` account ID for account verifications, or the caller's `user_` ID for personal verification. */
  account_id: string;
  body?: CreateVerificationRequestBody;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateVerificationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String.pipe(T.Query()),
    body: S.optional(CreateVerificationRequestBody.pipe(T.HttpBody())),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/verifications", code: 200 })),
).annotate({
  identifier: "CreateVerificationRequest",
}) as any as S.Schema<CreateVerificationRequest>;

/** Address on the verification profile. `null` when no address is set. */
export interface CreateVerificationResponseAddress {
  city?: string | null;
  /** Two-letter ISO 3166-1 country code, for example `US`, `DE`, or `GB`. */
  country?: string | null;
  /** First line of the street address. */
  line1?: string;
  /** Second line of the street address. */
  line2?: string | null;
  /** Postal or ZIP code. */
  postal_code?: string | null;
  /** State, province, or region code, for example `CA`. */
  state?: string | null;
}
export const CreateVerificationResponseAddress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    city: S.optional(S.NullOr(S.String)),
    country: S.optional(S.NullOr(S.String)),
    line1: S.optional(S.String),
    line2: S.optional(S.NullOr(S.String)),
    postal_code: S.optional(S.NullOr(S.String)),
    state: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "CreateVerificationResponseAddress",
}) as any as S.Schema<CreateVerificationResponseAddress>;

export type CreateVerificationResponseKind = "individual" | "business";
export const CreateVerificationResponseKind = /*@__PURE__*/ S.String;

export interface CreateVerificationResponseRequestedInformationItemErrorsItem {
  /** Stable error code. */
  code?: string;
  /** Why it was rejected. */
  reason?: string;
}
export const CreateVerificationResponseRequestedInformationItemErrorsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      code: S.optional(S.String),
      reason: S.optional(S.String),
    }),
  ).annotate({
    identifier: "CreateVerificationResponseRequestedInformationItemErrorsItem",
  }) as any as S.Schema<CreateVerificationResponseRequestedInformationItemErrorsItem>;

/** Present after a rejected submission. */
export type CreateVerificationResponseRequestedInformationItemErrorsList =
  Array<CreateVerificationResponseRequestedInformationItemErrorsItem>;
export const CreateVerificationResponseRequestedInformationItemErrorsList =
  /*@__PURE__*/ S.Array(
    CreateVerificationResponseRequestedInformationItemErrorsItem,
  ) as any as S.Schema<CreateVerificationResponseRequestedInformationItemErrorsList>;

/** The values `value` may take on a `select` item. On an `id_document` item these are the ID types accepted, and the chosen one decides which `documents` slots to send. Absent when the item has no choice to make. */
export type CreateVerificationResponseRequestedInformationItemOptionsList =
  Array<string>;
export const CreateVerificationResponseRequestedInformationItemOptionsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateVerificationResponseRequestedInformationItemOptionsList>;

export interface CreateVerificationResponseRequestedInformationItem {
  /** Present after a rejected submission. */
  errors?: CreateVerificationResponseRequestedInformationItemErrorsList;
  /** Requested information item ID, prefixed `inrqi_`. */
  id: string;
  /** Instruction to show the user. Carries the reviewer's note verbatim when there is one. */
  label: string;
  /** `true` when the item can be skipped. */
  optional?: boolean;
  /** The values `value` may take on a `select` item. On an `id_document` item these are the ID types accepted, and the chosen one decides which `documents` slots to send. Absent when the item has no choice to make. */
  options?: CreateVerificationResponseRequestedInformationItemOptionsList;
  /** What is needed: a document name such as `bank_statement`, or a field key such as `ssn` or `identity_document`. Handle unrecognized values by `type`. */
  requirement: string;
  /** What to send as the answer, so you never have to infer it: `files` (a document, as a list of its pages), `id_document` (send `documents` with the slot keys for the ID you are uploading), `text`, `date`, `phone` or `select` (send `value`), or `address` (send `address`). */
  type: string;
}
export const CreateVerificationResponseRequestedInformationItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      errors: S.optional(
        CreateVerificationResponseRequestedInformationItemErrorsList,
      ),
      id: S.String,
      label: S.String,
      optional: S.optional(S.Boolean),
      options: S.optional(
        CreateVerificationResponseRequestedInformationItemOptionsList,
      ),
      requirement: S.String,
      type: S.String,
    }),
  ).annotate({
    identifier: "CreateVerificationResponseRequestedInformationItem",
  }) as any as S.Schema<CreateVerificationResponseRequestedInformationItem>;

/** What Whop still needs before review can continue — one requirement per entry. Answer with Update Verification; nothing from the response is echoed back. Keys that don't apply are omitted. */
export type CreateVerificationResponseRequestedInformationList =
  Array<CreateVerificationResponseRequestedInformationItem>;
export const CreateVerificationResponseRequestedInformationList =
  /*@__PURE__*/ S.Array(
    CreateVerificationResponseRequestedInformationItem,
  ) as any as S.Schema<CreateVerificationResponseRequestedInformationList>;

/** `pending_upload` until the document has been relayed for review; `submitted` afterwards. */
export type CreateVerificationResponseRequiredDocumentsItemStatus =
  | "pending_upload"
  | "submitted";
export const CreateVerificationResponseRequiredDocumentsItemStatus =
  /*@__PURE__*/ S.String;

export interface CreateVerificationResponseRequiredDocumentsItem {
  /** Document slot key, such as `id_card_front`, `id_card_back`, or `selfie`. */
  document?: string;
  /** Why the previous submission was rejected, when the provider requested new documents or declined the verification. */
  rejection_reason?: string | null;
  /** `pending_upload` until the document has been relayed for review; `submitted` afterwards. */
  status?: CreateVerificationResponseRequiredDocumentsItemStatus;
}
export const CreateVerificationResponseRequiredDocumentsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      document: S.optional(S.String),
      rejection_reason: S.optional(S.NullOr(S.String)),
      status: S.optional(CreateVerificationResponseRequiredDocumentsItemStatus),
    }),
  ).annotate({
    identifier: "CreateVerificationResponseRequiredDocumentsItem",
  }) as any as S.Schema<CreateVerificationResponseRequiredDocumentsItem>;

/** Documents for a document-upload verification and their progress. Present only on verifications created by sending `documents`. `pending_upload` documents were not accepted yet — send the full set again with another Create Verification call. */
export type CreateVerificationResponseRequiredDocumentsList =
  Array<CreateVerificationResponseRequiredDocumentsItem>;
export const CreateVerificationResponseRequiredDocumentsList =
  /*@__PURE__*/ S.Array(
    CreateVerificationResponseRequiredDocumentsItem,
  ) as any as S.Schema<CreateVerificationResponseRequiredDocumentsList>;

/** Current verification state. `not_started` before any session exists; `pending` while a session needs the user's input; `processing` while the provider's automated checks run on a fresh submission; `action_required` when `requested_information` needs answers; `manual_review` while information already sent is under review — an audit answer, or a document the payout provider holds — nothing to submit, usually done within 3 business days; `approved` on success; `rejected` on failure. Call Create Verification again to start a new session. */
export type CreateVerificationResponseStatus =
  | "not_started"
  | "pending"
  | "processing"
  | "manual_review"
  | "approved"
  | "rejected"
  | "action_required";
export const CreateVerificationResponseStatus = /*@__PURE__*/ S.String;

export interface CreateVerificationResponse {
  /** Address on the verification profile. `null` when no address is set. */
  address?: CreateVerificationResponseAddress | null;
  /** Legal business name. */
  business_name?: string | null;
  /** Legal entity structure of the business, such as `private_corporation` or `sole_proprietorship`. Supported values vary by country of incorporation — see [Business structures](/developer/verification/business-structures). */
  business_structure?: string | null;
  /** Two-letter ISO 3166-1 country code, for example `US`, `DE`, or `GB`. */
  country?: string | null;
  /** When the verification profile was created, as an ISO 8601 timestamp. */
  created_at?: string;
  /** Formatted as `YYYY-MM-DD`. */
  date_of_birth?: string | null;
  /** Email address on the verification profile. */
  email?: string | null;
  first_name?: string | null;
  /** Verification profile ID, prefixed `idpf_`. */
  id?: string;
  kind?: CreateVerificationResponseKind;
  last_name?: string | null;
  /** Phone number on the verification profile. */
  phone?: string | null;
  /** What Whop still needs before review can continue — one requirement per entry. Answer with Update Verification; nothing from the response is echoed back. Keys that don't apply are omitted. */
  requested_information?: CreateVerificationResponseRequestedInformationList;
  /** Documents for a document-upload verification and their progress. Present only on verifications created by sending `documents`. `pending_upload` documents were not accepted yet — send the full set again with another Create Verification call. */
  required_documents?: CreateVerificationResponseRequiredDocumentsList;
  /** Hosted verification session URL for the user to complete identity checks. Expires 7 days after creation. */
  session_url?: string | null;
  /** Current verification state. `not_started` before any session exists; `pending` while a session needs the user's input; `processing` while the provider's automated checks run on a fresh submission; `action_required` when `requested_information` needs answers; `manual_review` while information already sent is under review — an audit answer, or a document the payout provider holds — nothing to submit, usually done within 3 business days; `approved` on success; `rejected` on failure. Call Create Verification again to start a new session. */
  status?: CreateVerificationResponseStatus;
  /** When the verification profile was last updated, as an ISO 8601 timestamp. */
  updated_at?: string;
}
export const CreateVerificationResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.optional(S.NullOr(CreateVerificationResponseAddress)),
    business_name: S.optional(S.NullOr(S.String)),
    business_structure: S.optional(S.NullOr(S.String)),
    country: S.optional(S.NullOr(S.String)),
    created_at: S.optional(S.String),
    date_of_birth: S.optional(S.NullOr(S.String)),
    email: S.optional(S.NullOr(S.String)),
    first_name: S.optional(S.NullOr(S.String)),
    id: S.optional(S.String),
    kind: S.optional(CreateVerificationResponseKind),
    last_name: S.optional(S.NullOr(S.String)),
    phone: S.optional(S.NullOr(S.String)),
    requested_information: S.optional(
      CreateVerificationResponseRequestedInformationList,
    ),
    required_documents: S.optional(
      CreateVerificationResponseRequiredDocumentsList,
    ),
    session_url: S.optional(S.NullOr(S.String)),
    status: S.optional(CreateVerificationResponseStatus),
    updated_at: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateVerificationResponse",
}) as any as S.Schema<CreateVerificationResponse>;

export interface GetVerificationRequest {
  /** Verification profile ID, prefixed `idpf_`. */
  id: string;
}
export const GetVerificationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/verifications/{id}", code: 200 })),
).annotate({
  identifier: "GetVerificationRequest",
}) as any as S.Schema<GetVerificationRequest>;

/** Address on the verification profile. `null` when no address is set. */
export type GetVerificationResponseAddress = CreateVerificationResponseAddress;
export const GetVerificationResponseAddress = CreateVerificationResponseAddress;

export type GetVerificationResponseKind = "individual" | "business";
export const GetVerificationResponseKind = /*@__PURE__*/ S.String;

export type GetVerificationResponseRequestedInformationItemErrorsItem =
  CreateVerificationResponseRequestedInformationItemErrorsItem;
export const GetVerificationResponseRequestedInformationItemErrorsItem =
  CreateVerificationResponseRequestedInformationItemErrorsItem;

/** Present after a rejected submission. */
export type GetVerificationResponseRequestedInformationItemErrorsList =
  Array<CreateVerificationResponseRequestedInformationItemErrorsItem>;
export const GetVerificationResponseRequestedInformationItemErrorsList =
  /*@__PURE__*/ S.Array(
    CreateVerificationResponseRequestedInformationItemErrorsItem,
  ) as any as S.Schema<GetVerificationResponseRequestedInformationItemErrorsList>;

/** The values `value` may take on a `select` item. On an `id_document` item these are the ID types accepted, and the chosen one decides which `documents` slots to send. Absent when the item has no choice to make. */
export type GetVerificationResponseRequestedInformationItemOptionsList =
  Array<string>;
export const GetVerificationResponseRequestedInformationItemOptionsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<GetVerificationResponseRequestedInformationItemOptionsList>;

export interface GetVerificationResponseRequestedInformationItem {
  /** Present after a rejected submission. */
  errors?: GetVerificationResponseRequestedInformationItemErrorsList;
  /** Requested information item ID, prefixed `inrqi_`. */
  id: string;
  /** Instruction to show the user. Carries the reviewer's note verbatim when there is one. */
  label: string;
  /** `true` when the item can be skipped. */
  optional?: boolean;
  /** The values `value` may take on a `select` item. On an `id_document` item these are the ID types accepted, and the chosen one decides which `documents` slots to send. Absent when the item has no choice to make. */
  options?: GetVerificationResponseRequestedInformationItemOptionsList;
  /** What is needed: a document name such as `bank_statement`, or a field key such as `ssn` or `identity_document`. Handle unrecognized values by `type`. */
  requirement: string;
  /** What to send as the answer, so you never have to infer it: `files` (a document, as a list of its pages), `id_document` (send `documents` with the slot keys for the ID you are uploading), `text`, `date`, `phone` or `select` (send `value`), or `address` (send `address`). */
  type: string;
}
export const GetVerificationResponseRequestedInformationItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      errors: S.optional(
        GetVerificationResponseRequestedInformationItemErrorsList,
      ),
      id: S.String,
      label: S.String,
      optional: S.optional(S.Boolean),
      options: S.optional(
        GetVerificationResponseRequestedInformationItemOptionsList,
      ),
      requirement: S.String,
      type: S.String,
    }),
  ).annotate({
    identifier: "GetVerificationResponseRequestedInformationItem",
  }) as any as S.Schema<GetVerificationResponseRequestedInformationItem>;

/** What Whop still needs before review can continue — one requirement per entry. Answer with Update Verification; nothing from the response is echoed back. Keys that don't apply are omitted. */
export type GetVerificationResponseRequestedInformationList =
  Array<GetVerificationResponseRequestedInformationItem>;
export const GetVerificationResponseRequestedInformationList =
  /*@__PURE__*/ S.Array(
    GetVerificationResponseRequestedInformationItem,
  ) as any as S.Schema<GetVerificationResponseRequestedInformationList>;

/** `pending_upload` until the document has been relayed for review; `submitted` afterwards. */
export type GetVerificationResponseRequiredDocumentsItemStatus =
  | "pending_upload"
  | "submitted";
export const GetVerificationResponseRequiredDocumentsItemStatus =
  /*@__PURE__*/ S.String;

export interface GetVerificationResponseRequiredDocumentsItem {
  /** Document slot key, such as `id_card_front`, `id_card_back`, or `selfie`. */
  document?: string;
  /** Why the previous submission was rejected, when the provider requested new documents or declined the verification. */
  rejection_reason?: string | null;
  /** `pending_upload` until the document has been relayed for review; `submitted` afterwards. */
  status?: GetVerificationResponseRequiredDocumentsItemStatus;
}
export const GetVerificationResponseRequiredDocumentsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      document: S.optional(S.String),
      rejection_reason: S.optional(S.NullOr(S.String)),
      status: S.optional(GetVerificationResponseRequiredDocumentsItemStatus),
    }),
  ).annotate({
    identifier: "GetVerificationResponseRequiredDocumentsItem",
  }) as any as S.Schema<GetVerificationResponseRequiredDocumentsItem>;

/** Documents for a document-upload verification and their progress. Present only on verifications created by sending `documents`. `pending_upload` documents were not accepted yet — send the full set again with another Create Verification call. */
export type GetVerificationResponseRequiredDocumentsList =
  Array<GetVerificationResponseRequiredDocumentsItem>;
export const GetVerificationResponseRequiredDocumentsList =
  /*@__PURE__*/ S.Array(
    GetVerificationResponseRequiredDocumentsItem,
  ) as any as S.Schema<GetVerificationResponseRequiredDocumentsList>;

/** Current verification state. `not_started` before any session exists; `pending` while a session needs the user's input; `processing` while the provider's automated checks run on a fresh submission; `action_required` when `requested_information` needs answers; `manual_review` while information already sent is under review — an audit answer, or a document the payout provider holds — nothing to submit, usually done within 3 business days; `approved` on success; `rejected` on failure. Call Create Verification again to start a new session. */
export type GetVerificationResponseStatus =
  | "not_started"
  | "pending"
  | "processing"
  | "manual_review"
  | "approved"
  | "rejected"
  | "action_required";
export const GetVerificationResponseStatus = /*@__PURE__*/ S.String;

export interface GetVerificationResponse {
  /** Address on the verification profile. `null` when no address is set. */
  address?: CreateVerificationResponseAddress | null;
  /** Legal business name. */
  business_name?: string | null;
  /** Legal entity structure of the business, such as `private_corporation` or `sole_proprietorship`. Supported values vary by country of incorporation — see [Business structures](/developer/verification/business-structures). */
  business_structure?: string | null;
  /** Two-letter ISO 3166-1 country code, for example `US`, `DE`, or `GB`. */
  country?: string | null;
  /** When the verification profile was created, as an ISO 8601 timestamp. */
  created_at?: string;
  /** Formatted as `YYYY-MM-DD`. */
  date_of_birth?: string | null;
  /** Email address on the verification profile. */
  email?: string | null;
  first_name?: string | null;
  /** Verification profile ID, prefixed `idpf_`. */
  id?: string;
  kind?: GetVerificationResponseKind;
  last_name?: string | null;
  /** Phone number on the verification profile. */
  phone?: string | null;
  /** What Whop still needs before review can continue — one requirement per entry. Answer with Update Verification; nothing from the response is echoed back. Keys that don't apply are omitted. */
  requested_information?: GetVerificationResponseRequestedInformationList;
  /** Documents for a document-upload verification and their progress. Present only on verifications created by sending `documents`. `pending_upload` documents were not accepted yet — send the full set again with another Create Verification call. */
  required_documents?: GetVerificationResponseRequiredDocumentsList;
  /** Hosted verification session URL for the user to complete identity checks. Expires 7 days after creation. */
  session_url?: string | null;
  /** Current verification state. `not_started` before any session exists; `pending` while a session needs the user's input; `processing` while the provider's automated checks run on a fresh submission; `action_required` when `requested_information` needs answers; `manual_review` while information already sent is under review — an audit answer, or a document the payout provider holds — nothing to submit, usually done within 3 business days; `approved` on success; `rejected` on failure. Call Create Verification again to start a new session. */
  status?: GetVerificationResponseStatus;
  /** When the verification profile was last updated, as an ISO 8601 timestamp. */
  updated_at?: string;
}
export const GetVerificationResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.optional(S.NullOr(CreateVerificationResponseAddress)),
    business_name: S.optional(S.NullOr(S.String)),
    business_structure: S.optional(S.NullOr(S.String)),
    country: S.optional(S.NullOr(S.String)),
    created_at: S.optional(S.String),
    date_of_birth: S.optional(S.NullOr(S.String)),
    email: S.optional(S.NullOr(S.String)),
    first_name: S.optional(S.NullOr(S.String)),
    id: S.optional(S.String),
    kind: S.optional(GetVerificationResponseKind),
    last_name: S.optional(S.NullOr(S.String)),
    phone: S.optional(S.NullOr(S.String)),
    requested_information: S.optional(
      GetVerificationResponseRequestedInformationList,
    ),
    required_documents: S.optional(
      GetVerificationResponseRequiredDocumentsList,
    ),
    session_url: S.optional(S.NullOr(S.String)),
    status: S.optional(GetVerificationResponseStatus),
    updated_at: S.optional(S.String),
  }),
).annotate({
  identifier: "GetVerificationResponse",
}) as any as S.Schema<GetVerificationResponse>;

export type ListVerificationsRequestOrder = "updated_at" | "created_at";
export const ListVerificationsRequestOrder = /*@__PURE__*/ S.String;

export type ListVerificationsRequestDirection = "asc" | "desc";
export const ListVerificationsRequestDirection = /*@__PURE__*/ S.String;

export interface ListVerificationsRequest {
  /** Account or user ID whose verifications you want to list. Use a `biz_` account ID, or the caller's `user_` ID for personal verifications. */
  account_id: string;
  /** Field used to sort returned verifications. */
  order?: ListVerificationsRequestOrder | (string & {});
  /** Sort direction for returned verifications. */
  direction?: ListVerificationsRequestDirection | (string & {});
}
export const ListVerificationsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String.pipe(T.Query()),
    order: S.optional(ListVerificationsRequestOrder.pipe(T.Query())),
    direction: S.optional(ListVerificationsRequestDirection.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/verifications", code: 200 })),
).annotate({
  identifier: "ListVerificationsRequest",
}) as any as S.Schema<ListVerificationsRequest>;

/** Address on the verification profile. `null` when no address is set. */
export type ListVerificationsResponseDataItemAddress =
  CreateVerificationResponseAddress;
export const ListVerificationsResponseDataItemAddress =
  CreateVerificationResponseAddress;

export type ListVerificationsResponseDataItemKind = "individual" | "business";
export const ListVerificationsResponseDataItemKind = /*@__PURE__*/ S.String;

export type ListVerificationsResponseDataItemRequestedInformationItemErrorsItem =
  CreateVerificationResponseRequestedInformationItemErrorsItem;
export const ListVerificationsResponseDataItemRequestedInformationItemErrorsItem =
  CreateVerificationResponseRequestedInformationItemErrorsItem;

/** Present after a rejected submission. */
export type ListVerificationsResponseDataItemRequestedInformationItemErrorsList =
  Array<CreateVerificationResponseRequestedInformationItemErrorsItem>;
export const ListVerificationsResponseDataItemRequestedInformationItemErrorsList =
  /*@__PURE__*/ S.Array(
    CreateVerificationResponseRequestedInformationItemErrorsItem,
  ) as any as S.Schema<ListVerificationsResponseDataItemRequestedInformationItemErrorsList>;

/** The values `value` may take on a `select` item. On an `id_document` item these are the ID types accepted, and the chosen one decides which `documents` slots to send. Absent when the item has no choice to make. */
export type ListVerificationsResponseDataItemRequestedInformationItemOptionsList =
  Array<string>;
export const ListVerificationsResponseDataItemRequestedInformationItemOptionsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ListVerificationsResponseDataItemRequestedInformationItemOptionsList>;

export interface ListVerificationsResponseDataItemRequestedInformationItem {
  /** Present after a rejected submission. */
  errors?: ListVerificationsResponseDataItemRequestedInformationItemErrorsList;
  /** Requested information item ID, prefixed `inrqi_`. */
  id: string;
  /** Instruction to show the user. Carries the reviewer's note verbatim when there is one. */
  label: string;
  /** `true` when the item can be skipped. */
  optional?: boolean;
  /** The values `value` may take on a `select` item. On an `id_document` item these are the ID types accepted, and the chosen one decides which `documents` slots to send. Absent when the item has no choice to make. */
  options?: ListVerificationsResponseDataItemRequestedInformationItemOptionsList;
  /** What is needed: a document name such as `bank_statement`, or a field key such as `ssn` or `identity_document`. Handle unrecognized values by `type`. */
  requirement: string;
  /** What to send as the answer, so you never have to infer it: `files` (a document, as a list of its pages), `id_document` (send `documents` with the slot keys for the ID you are uploading), `text`, `date`, `phone` or `select` (send `value`), or `address` (send `address`). */
  type: string;
}
export const ListVerificationsResponseDataItemRequestedInformationItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      errors: S.optional(
        ListVerificationsResponseDataItemRequestedInformationItemErrorsList,
      ),
      id: S.String,
      label: S.String,
      optional: S.optional(S.Boolean),
      options: S.optional(
        ListVerificationsResponseDataItemRequestedInformationItemOptionsList,
      ),
      requirement: S.String,
      type: S.String,
    }),
  ).annotate({
    identifier: "ListVerificationsResponseDataItemRequestedInformationItem",
  }) as any as S.Schema<ListVerificationsResponseDataItemRequestedInformationItem>;

/** What Whop still needs before review can continue — one requirement per entry. Answer with Update Verification; nothing from the response is echoed back. Keys that don't apply are omitted. */
export type ListVerificationsResponseDataItemRequestedInformationList =
  Array<ListVerificationsResponseDataItemRequestedInformationItem>;
export const ListVerificationsResponseDataItemRequestedInformationList =
  /*@__PURE__*/ S.Array(
    ListVerificationsResponseDataItemRequestedInformationItem,
  ) as any as S.Schema<ListVerificationsResponseDataItemRequestedInformationList>;

/** `pending_upload` until the document has been relayed for review; `submitted` afterwards. */
export type ListVerificationsResponseDataItemRequiredDocumentsItemStatus =
  | "pending_upload"
  | "submitted";
export const ListVerificationsResponseDataItemRequiredDocumentsItemStatus =
  /*@__PURE__*/ S.String;

export interface ListVerificationsResponseDataItemRequiredDocumentsItem {
  /** Document slot key, such as `id_card_front`, `id_card_back`, or `selfie`. */
  document?: string;
  /** Why the previous submission was rejected, when the provider requested new documents or declined the verification. */
  rejection_reason?: string | null;
  /** `pending_upload` until the document has been relayed for review; `submitted` afterwards. */
  status?: ListVerificationsResponseDataItemRequiredDocumentsItemStatus;
}
export const ListVerificationsResponseDataItemRequiredDocumentsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      document: S.optional(S.String),
      rejection_reason: S.optional(S.NullOr(S.String)),
      status: S.optional(
        ListVerificationsResponseDataItemRequiredDocumentsItemStatus,
      ),
    }),
  ).annotate({
    identifier: "ListVerificationsResponseDataItemRequiredDocumentsItem",
  }) as any as S.Schema<ListVerificationsResponseDataItemRequiredDocumentsItem>;

/** Documents for a document-upload verification and their progress. Present only on verifications created by sending `documents`. `pending_upload` documents were not accepted yet — send the full set again with another Create Verification call. */
export type ListVerificationsResponseDataItemRequiredDocumentsList =
  Array<ListVerificationsResponseDataItemRequiredDocumentsItem>;
export const ListVerificationsResponseDataItemRequiredDocumentsList =
  /*@__PURE__*/ S.Array(
    ListVerificationsResponseDataItemRequiredDocumentsItem,
  ) as any as S.Schema<ListVerificationsResponseDataItemRequiredDocumentsList>;

/** Current verification state. `not_started` before any session exists; `pending` while a session needs the user's input; `processing` while the provider's automated checks run on a fresh submission; `action_required` when `requested_information` needs answers; `manual_review` while information already sent is under review — an audit answer, or a document the payout provider holds — nothing to submit, usually done within 3 business days; `approved` on success; `rejected` on failure. Call Create Verification again to start a new session. */
export type ListVerificationsResponseDataItemStatus =
  | "not_started"
  | "pending"
  | "processing"
  | "manual_review"
  | "approved"
  | "rejected"
  | "action_required";
export const ListVerificationsResponseDataItemStatus = /*@__PURE__*/ S.String;

export interface ListVerificationsResponseDataItem {
  /** Address on the verification profile. `null` when no address is set. */
  address?: CreateVerificationResponseAddress | null;
  /** Legal business name. */
  business_name?: string | null;
  /** Legal entity structure of the business, such as `private_corporation` or `sole_proprietorship`. Supported values vary by country of incorporation — see [Business structures](/developer/verification/business-structures). */
  business_structure?: string | null;
  /** Two-letter ISO 3166-1 country code, for example `US`, `DE`, or `GB`. */
  country?: string | null;
  /** When the verification profile was created, as an ISO 8601 timestamp. */
  created_at?: string;
  /** Formatted as `YYYY-MM-DD`. */
  date_of_birth?: string | null;
  /** Email address on the verification profile. */
  email?: string | null;
  first_name?: string | null;
  /** Verification profile ID, prefixed `idpf_`. */
  id?: string;
  kind?: ListVerificationsResponseDataItemKind;
  last_name?: string | null;
  /** Phone number on the verification profile. */
  phone?: string | null;
  /** What Whop still needs before review can continue — one requirement per entry. Answer with Update Verification; nothing from the response is echoed back. Keys that don't apply are omitted. */
  requested_information?: ListVerificationsResponseDataItemRequestedInformationList;
  /** Documents for a document-upload verification and their progress. Present only on verifications created by sending `documents`. `pending_upload` documents were not accepted yet — send the full set again with another Create Verification call. */
  required_documents?: ListVerificationsResponseDataItemRequiredDocumentsList;
  /** Hosted verification session URL for the user to complete identity checks. Expires 7 days after creation. */
  session_url?: string | null;
  /** Current verification state. `not_started` before any session exists; `pending` while a session needs the user's input; `processing` while the provider's automated checks run on a fresh submission; `action_required` when `requested_information` needs answers; `manual_review` while information already sent is under review — an audit answer, or a document the payout provider holds — nothing to submit, usually done within 3 business days; `approved` on success; `rejected` on failure. Call Create Verification again to start a new session. */
  status?: ListVerificationsResponseDataItemStatus;
  /** When the verification profile was last updated, as an ISO 8601 timestamp. */
  updated_at?: string;
}
export const ListVerificationsResponseDataItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.optional(S.NullOr(CreateVerificationResponseAddress)),
    business_name: S.optional(S.NullOr(S.String)),
    business_structure: S.optional(S.NullOr(S.String)),
    country: S.optional(S.NullOr(S.String)),
    created_at: S.optional(S.String),
    date_of_birth: S.optional(S.NullOr(S.String)),
    email: S.optional(S.NullOr(S.String)),
    first_name: S.optional(S.NullOr(S.String)),
    id: S.optional(S.String),
    kind: S.optional(ListVerificationsResponseDataItemKind),
    last_name: S.optional(S.NullOr(S.String)),
    phone: S.optional(S.NullOr(S.String)),
    requested_information: S.optional(
      ListVerificationsResponseDataItemRequestedInformationList,
    ),
    required_documents: S.optional(
      ListVerificationsResponseDataItemRequiredDocumentsList,
    ),
    session_url: S.optional(S.NullOr(S.String)),
    status: S.optional(ListVerificationsResponseDataItemStatus),
    updated_at: S.optional(S.String),
  }),
).annotate({
  identifier: "ListVerificationsResponseDataItem",
}) as any as S.Schema<ListVerificationsResponseDataItem>;

export type ListVerificationsResponseDataList =
  Array<ListVerificationsResponseDataItem>;
export const ListVerificationsResponseDataList = /*@__PURE__*/ S.Array(
  ListVerificationsResponseDataItem,
) as any as S.Schema<ListVerificationsResponseDataList>;

export interface ListVerificationsResponse {
  data?: ListVerificationsResponseDataList;
}
export const ListVerificationsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: S.optional(ListVerificationsResponseDataList),
  }),
).annotate({
  identifier: "ListVerificationsResponse",
}) as any as S.Schema<ListVerificationsResponse>;

/** Personal address for the individual. */
export type UpdateVerificationRequestBodyCase0PersonalAddress =
  CreateVerificationRequestBodyCase0Address;
export const UpdateVerificationRequestBodyCase0PersonalAddress =
  CreateVerificationRequestBodyCase0Address;

/** Answer for `address` items. */
export type UpdateVerificationRequestBodyCase0RequestedInformationItemAddress =
  CreateVerificationRequestBodyCase0Address;
export const UpdateVerificationRequestBodyCase0RequestedInformationItemAddress =
  CreateVerificationRequestBodyCase0Address;

/** Answer for an `id_document` item: the same slot keys Create Verification takes, so the key names both the document and the side. Send every slot for the ID you are uploading — `PASSPORT` is `passport_front`; `ID_CARD`, `DRIVERS` and `RESIDENCE_PERMIT` take a front and a back. Each value is a direct upload ID, or a `file_`-prefixed attachment ID to reuse an uploaded document. */
export interface UpdateVerificationRequestBodyCase0RequestedInformationItemDocuments {
  /** Back of the driver's license. */
  drivers_back?: string;
  /** Front of the driver's license. */
  drivers_front?: string;
  /** Back of the ID card. */
  id_card_back?: string;
  /** Front of the ID card. */
  id_card_front?: string;
  /** Photo page of the passport. */
  passport_front?: string;
  /** Back of the residence permit. */
  residence_permit_back?: string;
  /** Front of the residence permit. */
  residence_permit_front?: string;
}
export const UpdateVerificationRequestBodyCase0RequestedInformationItemDocuments =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      drivers_back: S.optional(S.String),
      drivers_front: S.optional(S.String),
      id_card_back: S.optional(S.String),
      id_card_front: S.optional(S.String),
      passport_front: S.optional(S.String),
      residence_permit_back: S.optional(S.String),
      residence_permit_front: S.optional(S.String),
    }),
  ).annotate({
    identifier:
      "UpdateVerificationRequestBodyCase0RequestedInformationItemDocuments",
  }) as any as S.Schema<UpdateVerificationRequestBodyCase0RequestedInformationItemDocuments>;

/** Answer for a `files` item — one document, as a list of its pages, first page first. Each entry is a direct upload ID, or a `file_`-prefixed attachment ID to reuse an uploaded document. */
export type UpdateVerificationRequestBodyCase0RequestedInformationItemFilesList =
  Array<string>;
export const UpdateVerificationRequestBodyCase0RequestedInformationItemFilesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateVerificationRequestBodyCase0RequestedInformationItemFilesList>;

/** Whether `value` is raw input or a vault token. */
export type UpdateVerificationRequestBodyCase0RequestedInformationItemValueType =
  | "raw"
  | "vault_token";
export const UpdateVerificationRequestBodyCase0RequestedInformationItemValueType =
  /*@__PURE__*/ S.String;

export interface UpdateVerificationRequestBodyCase0RequestedInformationItem {
  /** Answer for `address` items. */
  address?: CreateVerificationRequestBodyCase0Address;
  /** Answer for an `id_document` item: the same slot keys Create Verification takes, so the key names both the document and the side. Send every slot for the ID you are uploading — `PASSPORT` is `passport_front`; `ID_CARD`, `DRIVERS` and `RESIDENCE_PERMIT` take a front and a back. Each value is a direct upload ID, or a `file_`-prefixed attachment ID to reuse an uploaded document. */
  documents?: UpdateVerificationRequestBodyCase0RequestedInformationItemDocuments;
  /** Answer for a `files` item — one document, as a list of its pages, first page first. Each entry is a direct upload ID, or a `file_`-prefixed attachment ID to reuse an uploaded document. */
  files?: UpdateVerificationRequestBodyCase0RequestedInformationItemFilesList;
  /** Item ID from `requested_information`. */
  id: string;
  /** Answer for `text`, `date`, `phone`, and `select` items, and the chosen document type for a `file` item that lists `options`. */
  value?: string;
  /** Whether `value` is raw input or a vault token. */
  value_type?:
    | UpdateVerificationRequestBodyCase0RequestedInformationItemValueType
    | (string & {});
}
export const UpdateVerificationRequestBodyCase0RequestedInformationItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      address: S.optional(CreateVerificationRequestBodyCase0Address),
      documents: S.optional(
        UpdateVerificationRequestBodyCase0RequestedInformationItemDocuments,
      ),
      files: S.optional(
        UpdateVerificationRequestBodyCase0RequestedInformationItemFilesList,
      ),
      id: S.String,
      value: S.optional(S.String),
      value_type: S.optional(
        UpdateVerificationRequestBodyCase0RequestedInformationItemValueType,
      ),
    }),
  ).annotate({
    identifier: "UpdateVerificationRequestBodyCase0RequestedInformationItem",
  }) as any as S.Schema<UpdateVerificationRequestBodyCase0RequestedInformationItem>;

/** Answers to items in `requested_information`. Each entry pairs the item `id` with one answer payload matching its `type`. */
export type UpdateVerificationRequestBodyCase0RequestedInformationList =
  Array<UpdateVerificationRequestBodyCase0RequestedInformationItem>;
export const UpdateVerificationRequestBodyCase0RequestedInformationList =
  /*@__PURE__*/ S.Array(
    UpdateVerificationRequestBodyCase0RequestedInformationItem,
  ) as any as S.Schema<UpdateVerificationRequestBodyCase0RequestedInformationList>;

/** Fields that can be updated on an individual (KYC) verification. At least one field is required. */
export interface UpdateVerificationRequestBodyCase0 {
  /** Legal business name for a sole proprietor or single-member LLC. */
  business_name?: string;
  /** Entity type for sole proprietors, such as `single_member_llc`. Supported values vary by country of incorporation — see [Business structures](/developer/verification/business-structures). */
  business_structure?: string;
  /** The business ID number of the company, as appropriate for the company's country. Examples are an Employer Identification Number (EIN) in the US, a Business Number in Canada, or a Company Number in the UK. */
  business_tax_identification_number?: string;
  /** Two-letter ISO 3166-1 country code, for example `US`, `DE`, or `GB`. */
  country?: string;
  /** Formatted as `YYYY-MM-DD`. */
  date_of_birth?: string;
  first_name?: string;
  last_name?: string;
  /** Personal address for the individual. */
  personal_address?: CreateVerificationRequestBodyCase0Address;
  /** Answers to items in `requested_information`. Each entry pairs the item `id` with one answer payload matching its `type`. */
  requested_information?: UpdateVerificationRequestBodyCase0RequestedInformationList;
  /** The government-issued ID number of the person being verified — the individual for a KYC verification, or the business representative for a KYB verification — as appropriate for their country. Examples are a Social Security Number (SSN) in the US, or a Social Insurance Number in Canada. */
  tax_identification_number?: string;
}
export const UpdateVerificationRequestBodyCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    business_name: S.optional(S.String),
    business_structure: S.optional(S.String),
    business_tax_identification_number: S.optional(S.String),
    country: S.optional(S.String),
    date_of_birth: S.optional(S.String),
    first_name: S.optional(S.String),
    last_name: S.optional(S.String),
    personal_address: S.optional(CreateVerificationRequestBodyCase0Address),
    requested_information: S.optional(
      UpdateVerificationRequestBodyCase0RequestedInformationList,
    ),
    tax_identification_number: S.optional(S.String),
  }),
).annotate({
  identifier: "UpdateVerificationRequestBodyCase0",
}) as any as S.Schema<UpdateVerificationRequestBodyCase0>;

/** Business address. */
export type UpdateVerificationRequestBodyCase1BusinessAddress =
  CreateVerificationRequestBodyCase0Address;
export const UpdateVerificationRequestBodyCase1BusinessAddress =
  CreateVerificationRequestBodyCase0Address;

/** Answer for `address` items. */
export type UpdateVerificationRequestBodyCase1RequestedInformationItemAddress =
  CreateVerificationRequestBodyCase0Address;
export const UpdateVerificationRequestBodyCase1RequestedInformationItemAddress =
  CreateVerificationRequestBodyCase0Address;

/** Answer for an `id_document` item: the same slot keys Create Verification takes, so the key names both the document and the side. Send every slot for the ID you are uploading — `PASSPORT` is `passport_front`; `ID_CARD`, `DRIVERS` and `RESIDENCE_PERMIT` take a front and a back. Each value is a direct upload ID, or a `file_`-prefixed attachment ID to reuse an uploaded document. */
export type UpdateVerificationRequestBodyCase1RequestedInformationItemDocuments =
  UpdateVerificationRequestBodyCase0RequestedInformationItemDocuments;
export const UpdateVerificationRequestBodyCase1RequestedInformationItemDocuments =
  UpdateVerificationRequestBodyCase0RequestedInformationItemDocuments;

/** Answer for a `files` item — one document, as a list of its pages, first page first. Each entry is a direct upload ID, or a `file_`-prefixed attachment ID to reuse an uploaded document. */
export type UpdateVerificationRequestBodyCase1RequestedInformationItemFilesList =
  Array<string>;
export const UpdateVerificationRequestBodyCase1RequestedInformationItemFilesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateVerificationRequestBodyCase1RequestedInformationItemFilesList>;

/** Whether `value` is raw input or a vault token. */
export type UpdateVerificationRequestBodyCase1RequestedInformationItemValueType =
  | "raw"
  | "vault_token";
export const UpdateVerificationRequestBodyCase1RequestedInformationItemValueType =
  /*@__PURE__*/ S.String;

export interface UpdateVerificationRequestBodyCase1RequestedInformationItem {
  /** Answer for `address` items. */
  address?: CreateVerificationRequestBodyCase0Address;
  /** Answer for an `id_document` item: the same slot keys Create Verification takes, so the key names both the document and the side. Send every slot for the ID you are uploading — `PASSPORT` is `passport_front`; `ID_CARD`, `DRIVERS` and `RESIDENCE_PERMIT` take a front and a back. Each value is a direct upload ID, or a `file_`-prefixed attachment ID to reuse an uploaded document. */
  documents?: UpdateVerificationRequestBodyCase0RequestedInformationItemDocuments;
  /** Answer for a `files` item — one document, as a list of its pages, first page first. Each entry is a direct upload ID, or a `file_`-prefixed attachment ID to reuse an uploaded document. */
  files?: UpdateVerificationRequestBodyCase1RequestedInformationItemFilesList;
  /** Item ID from `requested_information`. */
  id: string;
  /** Answer for `text`, `date`, `phone`, and `select` items, and the chosen document type for a `file` item that lists `options`. */
  value?: string;
  /** Whether `value` is raw input or a vault token. */
  value_type?:
    | UpdateVerificationRequestBodyCase1RequestedInformationItemValueType
    | (string & {});
}
export const UpdateVerificationRequestBodyCase1RequestedInformationItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      address: S.optional(CreateVerificationRequestBodyCase0Address),
      documents: S.optional(
        UpdateVerificationRequestBodyCase0RequestedInformationItemDocuments,
      ),
      files: S.optional(
        UpdateVerificationRequestBodyCase1RequestedInformationItemFilesList,
      ),
      id: S.String,
      value: S.optional(S.String),
      value_type: S.optional(
        UpdateVerificationRequestBodyCase1RequestedInformationItemValueType,
      ),
    }),
  ).annotate({
    identifier: "UpdateVerificationRequestBodyCase1RequestedInformationItem",
  }) as any as S.Schema<UpdateVerificationRequestBodyCase1RequestedInformationItem>;

/** Answers to items in `requested_information`. Each entry pairs the item `id` with one answer payload matching its `type`. */
export type UpdateVerificationRequestBodyCase1RequestedInformationList =
  Array<UpdateVerificationRequestBodyCase1RequestedInformationItem>;
export const UpdateVerificationRequestBodyCase1RequestedInformationList =
  /*@__PURE__*/ S.Array(
    UpdateVerificationRequestBodyCase1RequestedInformationItem,
  ) as any as S.Schema<UpdateVerificationRequestBodyCase1RequestedInformationList>;

/** Fields that can be updated on a business entity (KYB) verification. At least one field is required. */
export interface UpdateVerificationRequestBodyCase1 {
  /** Business address. */
  business_address?: CreateVerificationRequestBodyCase0Address;
  /** Legal business name. */
  business_name?: string;
  /** Legal entity structure of the business, such as `private_corporation` or `sole_proprietorship`. Supported values vary by country of incorporation — see [Business structures](/developer/verification/business-structures). */
  business_structure?: string;
  /** The business ID number of the company, as appropriate for the company's country. Examples are an Employer Identification Number (EIN) in the US, a Business Number in Canada, or a Company Number in the UK. */
  business_tax_identification_number?: string;
  /** Two-letter ISO 3166-1 country code, for example `US`, `DE`, or `GB`. */
  country?: string;
  /** Business representative's date of birth, formatted as `YYYY-MM-DD`. */
  date_of_birth?: string;
  /** First name of the business representative. */
  first_name?: string;
  /** Last name of the business representative. */
  last_name?: string;
  /** Answers to items in `requested_information`. Each entry pairs the item `id` with one answer payload matching its `type`. */
  requested_information?: UpdateVerificationRequestBodyCase1RequestedInformationList;
  /** The government-issued ID number of the person being verified — the individual for a KYC verification, or the business representative for a KYB verification — as appropriate for their country. Examples are a Social Security Number (SSN) in the US, or a Social Insurance Number in Canada. */
  tax_identification_number?: string;
}
export const UpdateVerificationRequestBodyCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    business_address: S.optional(CreateVerificationRequestBodyCase0Address),
    business_name: S.optional(S.String),
    business_structure: S.optional(S.String),
    business_tax_identification_number: S.optional(S.String),
    country: S.optional(S.String),
    date_of_birth: S.optional(S.String),
    first_name: S.optional(S.String),
    last_name: S.optional(S.String),
    requested_information: S.optional(
      UpdateVerificationRequestBodyCase1RequestedInformationList,
    ),
    tax_identification_number: S.optional(S.String),
  }),
).annotate({
  identifier: "UpdateVerificationRequestBodyCase1",
}) as any as S.Schema<UpdateVerificationRequestBodyCase1>;

export type UpdateVerificationRequestBody =
  | UpdateVerificationRequestBodyCase0
  | UpdateVerificationRequestBodyCase1;
export const UpdateVerificationRequestBody =
  /*@__PURE__*/ S.Unknown as any as S.Schema<UpdateVerificationRequestBody>;

export interface UpdateVerificationRequest {
  /** Verification profile ID, prefixed `idpf_`. */
  id: string;
  body: UpdateVerificationRequestBody;
}
export const UpdateVerificationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    body: UpdateVerificationRequestBody.pipe(T.HttpBody()),
  }).pipe(T.Http({ method: "PATCH", uri: "/verifications/{id}", code: 200 })),
).annotate({
  identifier: "UpdateVerificationRequest",
}) as any as S.Schema<UpdateVerificationRequest>;

/** Address on the verification profile. `null` when no address is set. */
export type UpdateVerificationResponseAddress =
  CreateVerificationResponseAddress;
export const UpdateVerificationResponseAddress =
  CreateVerificationResponseAddress;

export type UpdateVerificationResponseKind = "individual" | "business";
export const UpdateVerificationResponseKind = /*@__PURE__*/ S.String;

export type UpdateVerificationResponseRequestedInformationItemErrorsItem =
  CreateVerificationResponseRequestedInformationItemErrorsItem;
export const UpdateVerificationResponseRequestedInformationItemErrorsItem =
  CreateVerificationResponseRequestedInformationItemErrorsItem;

/** Present after a rejected submission. */
export type UpdateVerificationResponseRequestedInformationItemErrorsList =
  Array<CreateVerificationResponseRequestedInformationItemErrorsItem>;
export const UpdateVerificationResponseRequestedInformationItemErrorsList =
  /*@__PURE__*/ S.Array(
    CreateVerificationResponseRequestedInformationItemErrorsItem,
  ) as any as S.Schema<UpdateVerificationResponseRequestedInformationItemErrorsList>;

/** The values `value` may take on a `select` item. On an `id_document` item these are the ID types accepted, and the chosen one decides which `documents` slots to send. Absent when the item has no choice to make. */
export type UpdateVerificationResponseRequestedInformationItemOptionsList =
  Array<string>;
export const UpdateVerificationResponseRequestedInformationItemOptionsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateVerificationResponseRequestedInformationItemOptionsList>;

export interface UpdateVerificationResponseRequestedInformationItem {
  /** Present after a rejected submission. */
  errors?: UpdateVerificationResponseRequestedInformationItemErrorsList;
  /** Requested information item ID, prefixed `inrqi_`. */
  id: string;
  /** Instruction to show the user. Carries the reviewer's note verbatim when there is one. */
  label: string;
  /** `true` when the item can be skipped. */
  optional?: boolean;
  /** The values `value` may take on a `select` item. On an `id_document` item these are the ID types accepted, and the chosen one decides which `documents` slots to send. Absent when the item has no choice to make. */
  options?: UpdateVerificationResponseRequestedInformationItemOptionsList;
  /** What is needed: a document name such as `bank_statement`, or a field key such as `ssn` or `identity_document`. Handle unrecognized values by `type`. */
  requirement: string;
  /** What to send as the answer, so you never have to infer it: `files` (a document, as a list of its pages), `id_document` (send `documents` with the slot keys for the ID you are uploading), `text`, `date`, `phone` or `select` (send `value`), or `address` (send `address`). */
  type: string;
}
export const UpdateVerificationResponseRequestedInformationItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      errors: S.optional(
        UpdateVerificationResponseRequestedInformationItemErrorsList,
      ),
      id: S.String,
      label: S.String,
      optional: S.optional(S.Boolean),
      options: S.optional(
        UpdateVerificationResponseRequestedInformationItemOptionsList,
      ),
      requirement: S.String,
      type: S.String,
    }),
  ).annotate({
    identifier: "UpdateVerificationResponseRequestedInformationItem",
  }) as any as S.Schema<UpdateVerificationResponseRequestedInformationItem>;

/** What Whop still needs before review can continue — one requirement per entry. Answer with Update Verification; nothing from the response is echoed back. Keys that don't apply are omitted. */
export type UpdateVerificationResponseRequestedInformationList =
  Array<UpdateVerificationResponseRequestedInformationItem>;
export const UpdateVerificationResponseRequestedInformationList =
  /*@__PURE__*/ S.Array(
    UpdateVerificationResponseRequestedInformationItem,
  ) as any as S.Schema<UpdateVerificationResponseRequestedInformationList>;

/** `pending_upload` until the document has been relayed for review; `submitted` afterwards. */
export type UpdateVerificationResponseRequiredDocumentsItemStatus =
  | "pending_upload"
  | "submitted";
export const UpdateVerificationResponseRequiredDocumentsItemStatus =
  /*@__PURE__*/ S.String;

export interface UpdateVerificationResponseRequiredDocumentsItem {
  /** Document slot key, such as `id_card_front`, `id_card_back`, or `selfie`. */
  document?: string;
  /** Why the previous submission was rejected, when the provider requested new documents or declined the verification. */
  rejection_reason?: string | null;
  /** `pending_upload` until the document has been relayed for review; `submitted` afterwards. */
  status?: UpdateVerificationResponseRequiredDocumentsItemStatus;
}
export const UpdateVerificationResponseRequiredDocumentsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      document: S.optional(S.String),
      rejection_reason: S.optional(S.NullOr(S.String)),
      status: S.optional(UpdateVerificationResponseRequiredDocumentsItemStatus),
    }),
  ).annotate({
    identifier: "UpdateVerificationResponseRequiredDocumentsItem",
  }) as any as S.Schema<UpdateVerificationResponseRequiredDocumentsItem>;

/** Documents for a document-upload verification and their progress. Present only on verifications created by sending `documents`. `pending_upload` documents were not accepted yet — send the full set again with another Create Verification call. */
export type UpdateVerificationResponseRequiredDocumentsList =
  Array<UpdateVerificationResponseRequiredDocumentsItem>;
export const UpdateVerificationResponseRequiredDocumentsList =
  /*@__PURE__*/ S.Array(
    UpdateVerificationResponseRequiredDocumentsItem,
  ) as any as S.Schema<UpdateVerificationResponseRequiredDocumentsList>;

/** Current verification state. `not_started` before any session exists; `pending` while a session needs the user's input; `processing` while the provider's automated checks run on a fresh submission; `action_required` when `requested_information` needs answers; `manual_review` while information already sent is under review — an audit answer, or a document the payout provider holds — nothing to submit, usually done within 3 business days; `approved` on success; `rejected` on failure. Call Create Verification again to start a new session. */
export type UpdateVerificationResponseStatus =
  | "not_started"
  | "pending"
  | "processing"
  | "manual_review"
  | "approved"
  | "rejected"
  | "action_required";
export const UpdateVerificationResponseStatus = /*@__PURE__*/ S.String;

export interface UpdateVerificationResponse {
  /** Address on the verification profile. `null` when no address is set. */
  address?: CreateVerificationResponseAddress | null;
  /** Legal business name. */
  business_name?: string | null;
  /** Legal entity structure of the business, such as `private_corporation` or `sole_proprietorship`. Supported values vary by country of incorporation — see [Business structures](/developer/verification/business-structures). */
  business_structure?: string | null;
  /** Two-letter ISO 3166-1 country code, for example `US`, `DE`, or `GB`. */
  country?: string | null;
  /** When the verification profile was created, as an ISO 8601 timestamp. */
  created_at?: string;
  /** Formatted as `YYYY-MM-DD`. */
  date_of_birth?: string | null;
  /** Email address on the verification profile. */
  email?: string | null;
  first_name?: string | null;
  /** Verification profile ID, prefixed `idpf_`. */
  id?: string;
  kind?: UpdateVerificationResponseKind;
  last_name?: string | null;
  /** Phone number on the verification profile. */
  phone?: string | null;
  /** What Whop still needs before review can continue — one requirement per entry. Answer with Update Verification; nothing from the response is echoed back. Keys that don't apply are omitted. */
  requested_information?: UpdateVerificationResponseRequestedInformationList;
  /** Documents for a document-upload verification and their progress. Present only on verifications created by sending `documents`. `pending_upload` documents were not accepted yet — send the full set again with another Create Verification call. */
  required_documents?: UpdateVerificationResponseRequiredDocumentsList;
  /** Hosted verification session URL for the user to complete identity checks. Expires 7 days after creation. */
  session_url?: string | null;
  /** Current verification state. `not_started` before any session exists; `pending` while a session needs the user's input; `processing` while the provider's automated checks run on a fresh submission; `action_required` when `requested_information` needs answers; `manual_review` while information already sent is under review — an audit answer, or a document the payout provider holds — nothing to submit, usually done within 3 business days; `approved` on success; `rejected` on failure. Call Create Verification again to start a new session. */
  status?: UpdateVerificationResponseStatus;
  /** When the verification profile was last updated, as an ISO 8601 timestamp. */
  updated_at?: string;
}
export const UpdateVerificationResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.optional(S.NullOr(CreateVerificationResponseAddress)),
    business_name: S.optional(S.NullOr(S.String)),
    business_structure: S.optional(S.NullOr(S.String)),
    country: S.optional(S.NullOr(S.String)),
    created_at: S.optional(S.String),
    date_of_birth: S.optional(S.NullOr(S.String)),
    email: S.optional(S.NullOr(S.String)),
    first_name: S.optional(S.NullOr(S.String)),
    id: S.optional(S.String),
    kind: S.optional(UpdateVerificationResponseKind),
    last_name: S.optional(S.NullOr(S.String)),
    phone: S.optional(S.NullOr(S.String)),
    requested_information: S.optional(
      UpdateVerificationResponseRequestedInformationList,
    ),
    required_documents: S.optional(
      UpdateVerificationResponseRequiredDocumentsList,
    ),
    session_url: S.optional(S.NullOr(S.String)),
    status: S.optional(UpdateVerificationResponseStatus),
    updated_at: S.optional(S.String),
  }),
).annotate({
  identifier: "UpdateVerificationResponse",
}) as any as S.Schema<UpdateVerificationResponse>;

export type CreateVerificationError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Create Verification Starts a hosted verification session for an account or user, or returns the active session when one already exists. Any fields you include in the request body are used to prefill the session. Send `documents` (with `document_type`) to instead verify the person from identity documents included in this request — no hosted session involved. Send `share_token` to reuse a verification another Sumsub account has already completed for this person, instead of verifying them again. If the account already has an `approved` verification the request is rejected; unlink it first to start a new one. */
export const createVerification: API.OperationMethod<
  CreateVerificationRequest,
  CreateVerificationResponse,
  CreateVerificationError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateVerificationRequest,
  output: CreateVerificationResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type GetVerificationError = NotFound | WhopOpError;
/** Retrieve Verification Returns verifications for an account, including their status and any required actions. */
export const getVerification: API.OperationMethod<
  GetVerificationRequest,
  GetVerificationResponse,
  GetVerificationError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetVerificationRequest,
  output: GetVerificationResponse,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListVerificationsError = BadRequest | Forbidden | WhopOpError;
/** List Verifications Returns verifications for an account, including their status and any required actions. */
export const listVerifications: API.OperationMethod<
  ListVerificationsRequest,
  ListVerificationsResponse,
  ListVerificationsError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListVerificationsRequest,
  output: ListVerificationsResponse,
  errors: [BadRequest, Forbidden],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateVerificationError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** Update Verification Updates editable profile details or submits answers for items returned in `requested_information`. Once a verification is `approved` its profile details are locked and can no longer be edited. */
export const updateVerification: API.OperationMethod<
  UpdateVerificationRequest,
  UpdateVerificationResponse,
  UpdateVerificationError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateVerificationRequest,
  output: UpdateVerificationResponse,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
