// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export type ListNotificationBadgesRequestExperienceIdsList = Array<string>;
export const ListNotificationBadgesRequestExperienceIdsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ListNotificationBadgesRequestExperienceIdsList>;

export interface ListNotificationBadgesRequest {
  /** Only return badges for these experiences (`exp_` tags). */
  experience_ids?: ListNotificationBadgesRequestExperienceIdsList;
  /** The client's last fetched-at ISO 8601 timestamp, used to partially refresh badges after a websocket message. */
  last_fetched_at?: string;
}
export const ListNotificationBadgesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    experience_ids: S.optional(
      ListNotificationBadgesRequestExperienceIdsList.pipe(T.Query()),
    ),
    last_fetched_at: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/notifications/badges", code: 200 })),
).annotate({
  identifier: "ListNotificationBadgesRequest",
}) as any as S.Schema<ListNotificationBadgesRequest>;

export interface NotificationBadge {
  /** Account the experience belongs to, prefixed `biz_`. */
  account_id: string | null;
  /** Experience the badge counts, prefixed `exp_`. */
  experience_id: string;
  /** Whether the caller has unread notifications in this experience. */
  has_unread: boolean;
  /** Number of unread important (mention) notifications in this experience. */
  important_count: number;
  /** When the caller last viewed the experience, as an ISO 8601 timestamp. `null` when never viewed. */
  last_viewed_at: string | null;
}
export const NotificationBadge = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.NullOr(S.String),
    experience_id: S.String,
    has_unread: S.Boolean,
    important_count: S.Number,
    last_viewed_at: S.NullOr(S.String),
  }),
).annotate({
  identifier: "NotificationBadge",
}) as any as S.Schema<NotificationBadge>;

export type ListNotificationBadgesResponseDataList = Array<NotificationBadge>;
export const ListNotificationBadgesResponseDataList = /*@__PURE__*/ S.Array(
  NotificationBadge,
) as any as S.Schema<ListNotificationBadgesResponseDataList>;

export interface ListNotificationBadgesResponse {
  data: ListNotificationBadgesResponseDataList;
}
export const ListNotificationBadgesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListNotificationBadgesResponseDataList,
  }),
).annotate({
  identifier: "ListNotificationBadgesResponse",
}) as any as S.Schema<ListNotificationBadgesResponse>;

export interface ListNotificationsRequest {
  /** Only return notifications created since the user last viewed their source. */
  unread?: boolean;
  /** Only return notifications from this experience (`exp_` tag). */
  experience_id?: string;
  /** Only return team notifications for this account (`biz_` tag). */
  account_id?: string;
  /** Only return notifications that mention the user directly. */
  mentions?: boolean;
  /** The number of notifications to return (default 20, max 100). */
  first?: number;
  /** A cursor (a notification `id` from a previous page); returns notifications older than it. */
  after?: string;
}
export const ListNotificationsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    unread: S.optional(S.Boolean.pipe(T.Query())),
    experience_id: S.optional(S.String.pipe(T.Query())),
    account_id: S.optional(S.String.pipe(T.Query())),
    mentions: S.optional(S.Boolean.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/notifications", code: 200 })),
).annotate({
  identifier: "ListNotificationsRequest",
}) as any as S.Schema<ListNotificationsRequest>;

export interface NotificationAccount {
  /** Account ID, prefixed `biz_`. */
  id: string;
  /** Account logo image URL. `null` when the account has not uploaded one. */
  logo_url: string | null;
  /** URL slug of the account's store page on whop.com. */
  route: string | null;
  /** Account display name. */
  title: string;
}
export const NotificationAccount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    logo_url: S.NullOr(S.String),
    route: S.NullOr(S.String),
    title: S.String,
  }),
).annotate({
  identifier: "NotificationAccount",
}) as any as S.Schema<NotificationAccount>;

export interface NotificationExperienceApp {
  /** Icon image URL. Always present — the default app icon when none is uploaded. */
  icon_url: string;
  /** App ID, prefixed `app_`. */
  id: string;
}
export const NotificationExperienceApp = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    icon_url: S.String,
    id: S.String,
  }),
).annotate({
  identifier: "NotificationExperienceApp",
}) as any as S.Schema<NotificationExperienceApp>;

export interface NotificationExperience {
  /** App the experience belongs to. */
  app: NotificationExperienceApp | null;
  /** Experience ID, prefixed `exp_`. */
  id: string;
  /** Display name of the experience. */
  name: string | null;
}
export const NotificationExperience = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    app: S.NullOr(NotificationExperienceApp),
    id: S.String,
    name: S.NullOr(S.String),
  }),
).annotate({
  identifier: "NotificationExperience",
}) as any as S.Schema<NotificationExperience>;

export interface NotificationSender {
  /** User ID, prefixed `user_`. */
  id: string;
  /** Display name. */
  name: string | null;
  /** Public username. */
  username: string;
}
export const NotificationSender = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({
  identifier: "NotificationSender",
}) as any as S.Schema<NotificationSender>;

/** Scope of the topic: whether it applies to an account, a user, or an account's team. */
export type NotificationTopicSummaryTopicType =
  | "account"
  | "user"
  | "account_team";
export const NotificationTopicSummaryTopicType = /*@__PURE__*/ S.String;

export interface NotificationTopicSummary {
  /** Whether notifications for this topic are enabled by default when the user has not set a preference. */
  default_preference_value: boolean;
  /** Notification topic ID, prefixed `topic_`. */
  id: string;
  /** Whether this topic exclusively handles mention-based notifications. */
  is_mention: boolean;
  /** Scope of the topic: whether it applies to an account, a user, or an account's team. */
  topic_type: NotificationTopicSummaryTopicType;
}
export const NotificationTopicSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    default_preference_value: S.Boolean,
    id: S.String,
    is_mention: S.Boolean,
    topic_type: NotificationTopicSummaryTopicType,
  }),
).annotate({
  identifier: "NotificationTopicSummary",
}) as any as S.Schema<NotificationTopicSummary>;

export interface Notification {
  /** Account the notification belongs to. `null` when the notification is not associated with an account. */
  account: NotificationAccount | null;
  /** Image displayed alongside the notification — the sender's avatar, or the default notification image. */
  attachment_url: string;
  /** The body text of the notification displayed to the user. */
  content: string;
  /** When the notification was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Experience the notification is related to. `null` when not tied to a specific experience. */
  experience: NotificationExperience | null;
  /** Notification ID. Feed rows carry a composite id that doubles as the list cursor. */
  id: string;
  /** The same destination on the app's own domain, which the Whop web and mobile clients embed instead of navigating to it. Only relevant if you render Whop apps yourself. `null` when the notification carries its own `link`. */
  iframe_link: string | null;
  /** The whop.com page the notification opens, as a normal top-level navigation. This is the link to use unless you host Whop apps yourself. `null` when the notification has no click-through destination. */
  link: string | null;
  /** Whether the authenticated user was directly mentioned in this notification. */
  mentions_me: boolean;
  /** Additional path information appended to the notification's deep link, if any. */
  rest_path: string | null;
  /** User who triggered the notification. `null` when it was system-generated. */
  sender: NotificationSender | null;
  /** The title line of the notification displayed to the user. */
  subject: string;
  /** Topic category the notification belongs to, used for grouping and preference management. `null` when uncategorized. */
  topic: NotificationTopicSummary | null;
}
export const Notification = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account: S.NullOr(NotificationAccount),
    attachment_url: S.String,
    content: S.String,
    created_at: S.String,
    experience: S.NullOr(NotificationExperience),
    id: S.String,
    iframe_link: S.NullOr(S.String),
    link: S.NullOr(S.String),
    mentions_me: S.Boolean,
    rest_path: S.NullOr(S.String),
    sender: S.NullOr(NotificationSender),
    subject: S.String,
    topic: S.NullOr(NotificationTopicSummary),
  }),
).annotate({ identifier: "Notification" }) as any as S.Schema<Notification>;

export type ListNotificationsResponseDataList = Array<Notification>;
export const ListNotificationsResponseDataList = /*@__PURE__*/ S.Array(
  Notification,
) as any as S.Schema<ListNotificationsResponseDataList>;

export interface ListNotificationsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListNotificationsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListNotificationsResponsePageInfo",
}) as any as S.Schema<ListNotificationsResponsePageInfo>;

export interface ListNotificationsResponse {
  data: ListNotificationsResponseDataList;
  page_info: ListNotificationsResponsePageInfo;
}
export const ListNotificationsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListNotificationsResponseDataList,
    page_info: ListNotificationsResponsePageInfo,
  }),
).annotate({
  identifier: "ListNotificationsResponse",
}) as any as S.Schema<ListNotificationsResponse>;

export type ListNotificationTopicsRequestTopicType = "user" | "account_team";
export const ListNotificationTopicsRequestTopicType = /*@__PURE__*/ S.String;

export interface ListNotificationTopicsRequest {
  /** Only return topics of this scope: `user` (member notifications) or `account_team` (team notifications). */
  topic_type?: ListNotificationTopicsRequestTopicType | (string & {});
  /** The number of topics to return (default 20, max 100). */
  first?: number;
  /** A cursor; returns topics after this position. */
  after?: string;
}
export const ListNotificationTopicsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    topic_type: S.optional(
      ListNotificationTopicsRequestTopicType.pipe(T.Query()),
    ),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/notifications/topics", code: 200 })),
).annotate({
  identifier: "ListNotificationTopicsRequest",
}) as any as S.Schema<ListNotificationTopicsRequest>;

/** Scope of the topic: whether it applies to an account, a user, or an account's team. */
export type NotificationTopicTopicType = "account" | "user" | "account_team";
export const NotificationTopicTopicType = /*@__PURE__*/ S.String;

export interface NotificationTopic {
  /** Whether notifications for this topic are enabled by default when the user has not set a preference. */
  default_preference_value: boolean;
  /** Human-readable explanation of what notifications in this topic are about. `null` when no description has been set. */
  description: string | null;
  /** Notification topic ID, prefixed `topic_`. This is the value the notification preference endpoints take as `topic_id`. */
  id: string;
  /** Stable, human-readable name for the category, such as `new-follower`. Unlike `id`, it is the same in every environment, which makes it the value to match on in code and to read in logs. Treat it as an opaque string: the set is open and the casing is historical rather than normalized. */
  identifier: string;
  /** Whether this topic exclusively handles mention-based notifications. */
  is_mention: boolean;
  /** Display name shown in notification preference settings. */
  name: string;
  /** Scope of the topic: whether it applies to an account, a user, or an account's team. */
  topic_type: NotificationTopicTopicType;
}
export const NotificationTopic = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    default_preference_value: S.Boolean,
    description: S.NullOr(S.String),
    id: S.String,
    identifier: S.String,
    is_mention: S.Boolean,
    name: S.String,
    topic_type: NotificationTopicTopicType,
  }),
).annotate({
  identifier: "NotificationTopic",
}) as any as S.Schema<NotificationTopic>;

export type ListNotificationTopicsResponseDataList = Array<NotificationTopic>;
export const ListNotificationTopicsResponseDataList = /*@__PURE__*/ S.Array(
  NotificationTopic,
) as any as S.Schema<ListNotificationTopicsResponseDataList>;

export type ListNotificationTopicsResponsePageInfo =
  ListNotificationsResponsePageInfo;
export const ListNotificationTopicsResponsePageInfo =
  ListNotificationsResponsePageInfo;

export interface ListNotificationTopicsResponse {
  data: ListNotificationTopicsResponseDataList;
  page_info: ListNotificationsResponsePageInfo;
}
export const ListNotificationTopicsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListNotificationTopicsResponseDataList,
    page_info: ListNotificationsResponsePageInfo,
  }),
).annotate({
  identifier: "ListNotificationTopicsResponse",
}) as any as S.Schema<ListNotificationTopicsResponse>;

export interface MarkNotificationsReadRequest {
  /** Pass `true` to mark every notification read. Exactly one of `experience_id` or `all` is required. */
  all?: boolean;
  /** Experience to mark read (`exp_` tag). Exactly one of `experience_id` or `all` is required. */
  experience_id?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const MarkNotificationsReadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    all: S.optional(S.Boolean),
    experience_id: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/notifications/mark_read", code: 200 }),
  ),
).annotate({
  identifier: "MarkNotificationsReadRequest",
}) as any as S.Schema<MarkNotificationsReadRequest>;

export type MarkNotificationsReadResponseDataList = Array<NotificationBadge>;
export const MarkNotificationsReadResponseDataList = /*@__PURE__*/ S.Array(
  NotificationBadge,
) as any as S.Schema<MarkNotificationsReadResponseDataList>;

export interface MarkNotificationsReadResponse {
  data: MarkNotificationsReadResponseDataList;
}
export const MarkNotificationsReadResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: MarkNotificationsReadResponseDataList,
  }),
).annotate({
  identifier: "MarkNotificationsReadResponse",
}) as any as S.Schema<MarkNotificationsReadResponse>;

export interface RetrieveNotificationRequest {
  /** A notification `id` from List Notifications, or the id delivered with a push/websocket event. */
  id: string;
}
export const RetrieveNotificationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/notifications/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveNotificationRequest",
}) as any as S.Schema<RetrieveNotificationRequest>;

/** Optional `user_` tags narrowing the audience. When provided, only these users are notified (as a mention), provided they are in the targeted experience or account. */
export type SendNotificationRequestUserIdsList = Array<string>;
export const SendNotificationRequestUserIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SendNotificationRequestUserIdsList>;

export interface SendNotificationRequest {
  /** Account whose team members receive the notification (`biz_` tag). Exactly one of `experience_id` or `account_id` is required. */
  account_id?: string;
  /** Main body text of the notification. */
  content: string;
  /** Experience whose users receive the notification (`exp_` tag). Exactly one of `experience_id` or `account_id` is required. */
  experience_id?: string;
  /** User whose profile picture is used as the notification icon. Defaults to the experience or account avatar. */
  icon_user_id?: string | null;
  /** Path segment appended to the generated deep link that opens your app, for example `/settings/billing`. */
  rest_path?: string | null;
  /** Optional secondary line displayed below the title. */
  subtitle?: string | null;
  /** Headline text of the notification. */
  title: string;
  /** Optional `user_` tags narrowing the audience. When provided, only these users are notified (as a mention), provided they are in the targeted experience or account. */
  user_ids?: SendNotificationRequestUserIdsList;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const SendNotificationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    content: S.String,
    experience_id: S.optional(S.String),
    icon_user_id: S.optional(S.NullOr(S.String)),
    rest_path: S.optional(S.NullOr(S.String)),
    subtitle: S.optional(S.NullOr(S.String)),
    title: S.String,
    user_ids: S.optional(SendNotificationRequestUserIdsList),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/notifications", code: 200 })),
).annotate({
  identifier: "SendNotificationRequest",
}) as any as S.Schema<SendNotificationRequest>;

export interface SendNotificationResponse {
  success: boolean;
}
export const SendNotificationResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    success: S.Boolean,
  }),
).annotate({
  identifier: "SendNotificationResponse",
}) as any as S.Schema<SendNotificationResponse>;

export type ListNotificationBadgesError = Forbidden | WhopOpError;
/** List Notification Badges Lists the authenticated user's per-experience unread badge state. Requires a user credential. Returns one row per experience the user belongs to (or per requested experience). */
export const listNotificationBadges: API.OperationMethod<
  ListNotificationBadgesRequest,
  ListNotificationBadgesResponse,
  ListNotificationBadgesError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListNotificationBadgesRequest,
  output: ListNotificationBadgesResponse,
  errors: [Forbidden],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListNotificationsError = BadRequest | Forbidden | WhopOpError;
/** List Notifications Lists the authenticated user's notifications, newest first. Requires a user credential — an account API key has no notification feed. Without filters the feed spans every experience the user belongs to plus the teams they are a member of. */
export const listNotifications: API.PaginatedOperationMethod<
  ListNotificationsRequest,
  ListNotificationsResponse,
  ListNotificationsError,
  WhopOpContext,
  Notification
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListNotificationsRequest,
    output: ListNotificationsResponse,
    errors: [BadRequest, Forbidden],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ListNotificationTopicsError = BadRequest | WhopOpError;
/** List Notification Topics Lists the platform's visible notification topics — the categories users can set notification preferences on. App-created topics are internal and not returned. */
export const listNotificationTopics: API.PaginatedOperationMethod<
  ListNotificationTopicsRequest,
  ListNotificationTopicsResponse,
  ListNotificationTopicsError,
  WhopOpContext,
  NotificationTopic
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListNotificationTopicsRequest,
    output: ListNotificationTopicsResponse,
    errors: [BadRequest],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type MarkNotificationsReadError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Mark Notifications Read Marks the authenticated user's notifications as read: one experience's (`experience_id`) or everything (`all: true`) — exactly one of the two. Requires a user credential. Responds with the refreshed badge rows for the affected scope. */
export const markNotificationsRead: API.OperationMethod<
  MarkNotificationsReadRequest,
  MarkNotificationsReadResponse,
  MarkNotificationsReadError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: MarkNotificationsReadRequest,
  output: MarkNotificationsReadResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveNotificationError = NotFound | WhopOpError;
/** Retrieve Notification Retrieves a single notification by id — either an `id` returned by List Notifications, or the ephemeral id delivered with a push/websocket event. Requires a user credential. */
export const retrieveNotification: API.OperationMethod<
  RetrieveNotificationRequest,
  Notification,
  RetrieveNotificationError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveNotificationRequest,
  output: Notification,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type SendNotificationError =
  | BadRequest
  | Forbidden
  | Conflict
  | WhopOpError;
/** Send Notification Queues a notification to every user of an experience or to an account's team, processed asynchronously. Every send is attributed to an app: use an app API key, or a credential acting on behalf of an app. Narrow the audience with `user_ids` to send a mention. */
export const sendNotification: API.OperationMethod<
  SendNotificationRequest,
  SendNotificationResponse,
  SendNotificationError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SendNotificationRequest,
  output: SendNotificationResponse,
  errors: [BadRequest, Forbidden, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
