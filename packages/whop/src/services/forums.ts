// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface ListForumRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  company_id: string;
  product_id?: string;
}
export const ListForumRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    company_id: S.String.pipe(T.Query()),
    product_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/forums", code: 200 })),
).annotate({
  identifier: "ListForumRequest",
}) as any as S.Schema<ListForumRequest>;

/** Email notification preference option for a forum feed */
export type ForumEmailNotificationPreferences =
  | "all_admin_posts"
  | "only_weekly_summary"
  | "none";
export const ForumEmailNotificationPreferences = /*@__PURE__*/ S.String;

/** The parent experience that this forum belongs to. */
export interface ForumListItemExperience {
  /** The unique identifier for the experience. */
  id: string;
  /** Whether this experience is publicly visible to all users, including those without a membership. */
  is_public: boolean;
  /** The display name of this experience shown to users in the product navigation. Maximum 255 characters. */
  name: string;
}
export const ForumListItemExperience = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    is_public: S.Boolean,
    name: S.String,
  }),
).annotate({
  identifier: "ForumListItemExperience",
}) as any as S.Schema<ForumListItemExperience>;

/** Who can comment on a forum feed */
export type ForumWhoCanCommentTypes = "everyone" | "admins";
export const ForumWhoCanCommentTypes = /*@__PURE__*/ S.String;

/** Who can post on a forum feed */
export type ForumWhoCanPostTypes = "everyone" | "admins";
export const ForumWhoCanPostTypes = /*@__PURE__*/ S.String;

/** A discussion forum where members can create posts, comment, and react, belonging to an experience. */
export interface ForumListItem {
  /** The email notification setting that controls which posts trigger email alerts. One of: all_admin_posts, only_weekly_summary, none. */
  email_notification_preference: ForumEmailNotificationPreferences;
  /** The parent experience that this forum belongs to. */
  experience: ForumListItemExperience;
  /** The unique identifier for the entity */
  id: string;
  /** The permission level controlling who can comment on posts. One of: everyone, admins. */
  who_can_comment: ForumWhoCanCommentTypes;
  /** The permission level controlling who can create new posts. One of: everyone, admins. */
  who_can_post: ForumWhoCanPostTypes;
}
export const ForumListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email_notification_preference: ForumEmailNotificationPreferences,
    experience: ForumListItemExperience,
    id: S.String,
    who_can_comment: ForumWhoCanCommentTypes,
    who_can_post: ForumWhoCanPostTypes,
  }),
).annotate({ identifier: "ForumListItem" }) as any as S.Schema<ForumListItem>;

/** A list of nodes. */
export type ListForumResponseDataList = Array<ForumListItem>;
export const ListForumResponseDataList = /*@__PURE__*/ S.Array(
  ForumListItem,
) as any as S.Schema<ListForumResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListForumResponse {
  /** A list of nodes. */
  data: ListForumResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListForumResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListForumResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListForumResponse",
}) as any as S.Schema<ListForumResponse>;

export interface RetrieveForumRequest {
  /** The unique identifier of the forum or experience to retrieve. */
  id: string;
}
export const RetrieveForumRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/forums/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveForumRequest",
}) as any as S.Schema<RetrieveForumRequest>;

/** The parent experience that this forum belongs to. */
export type ForumExperience = ForumListItemExperience;
export const ForumExperience = ForumListItemExperience;

/** A discussion forum where members can create posts, comment, and react, belonging to an experience. */
export interface Forum {
  /** The email notification setting that controls which posts trigger email alerts. One of: all_admin_posts, only_weekly_summary, none. */
  email_notification_preference: ForumEmailNotificationPreferences;
  /** The parent experience that this forum belongs to. */
  experience: ForumListItemExperience;
  /** The unique identifier for the entity */
  id: string;
  /** The permission level controlling who can comment on posts. One of: everyone, admins. */
  who_can_comment: ForumWhoCanCommentTypes;
  /** The permission level controlling who can create new posts. One of: everyone, admins. */
  who_can_post: ForumWhoCanPostTypes;
}
export const Forum = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email_notification_preference: ForumEmailNotificationPreferences,
    experience: ForumListItemExperience,
    id: S.String,
    who_can_comment: ForumWhoCanCommentTypes,
    who_can_post: ForumWhoCanPostTypes,
  }),
).annotate({ identifier: "Forum" }) as any as S.Schema<Forum>;

/** A list of words that are automatically blocked from posts in this forum. For example, ['spam', 'scam']. */
export type UpdateForumRequestBannedWordsList = Array<string>;
export const UpdateForumRequestBannedWordsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpdateForumRequestBannedWordsList>;

/** The banner image displayed at the top of the forum page. Pass null to remove the existing banner. */
export interface UpdateForumRequestBannerImage {
  /** The ID of an existing file object. */
  id: string;
}
export const UpdateForumRequestBannerImage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "UpdateForumRequestBannerImage",
}) as any as S.Schema<UpdateForumRequestBannerImage>;

export interface UpdateForumRequest {
  /** The unique identifier of the forum to update. Accepts either an experience ID (e.g. 'exp_xxxxx') or a forum ID. */
  id: string;
  /** A list of words that are automatically blocked from posts in this forum. For example, ['spam', 'scam']. */
  banned_words?: UpdateForumRequestBannedWordsList | null;
  /** The banner image displayed at the top of the forum page. Pass null to remove the existing banner. */
  banner_image?: UpdateForumRequestBannerImage | null;
  /** Controls how email notifications are sent to members when new posts are created in this forum. */
  email_notification_preference?:
    | ForumEmailNotificationPreferences
    | (string & {})
    | null;
  /** Controls which roles are allowed to comment on posts in this forum. */
  who_can_comment?: ForumWhoCanCommentTypes | (string & {}) | null;
  /** Controls which roles are allowed to create new posts in this forum. */
  who_can_post?: ForumWhoCanPostTypes | (string & {}) | null;
}
export const UpdateForumRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    banned_words: S.optional(S.NullOr(UpdateForumRequestBannedWordsList)),
    banner_image: S.optional(S.NullOr(UpdateForumRequestBannerImage)),
    email_notification_preference: S.optional(
      S.NullOr(ForumEmailNotificationPreferences),
    ),
    who_can_comment: S.optional(S.NullOr(ForumWhoCanCommentTypes)),
    who_can_post: S.optional(S.NullOr(ForumWhoCanPostTypes)),
  }).pipe(T.Http({ method: "PATCH", uri: "/forums/{id}", code: 200 })),
).annotate({
  identifier: "UpdateForumRequest",
}) as any as S.Schema<UpdateForumRequest>;

export type ListForumError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List forums [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of forums within a specific company, with optional filtering by product. Required permissions: - `forum:read` */
export const listForum: API.PaginatedOperationMethod<
  ListForumRequest,
  ListForumResponse,
  ListForumError,
  WhopOpContext,
  ForumListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListForumRequest,
    output: ListForumResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveForumError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve forum [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing forum. Required permissions: - `forum:read` */
export const retrieveForum: API.OperationMethod<
  RetrieveForumRequest,
  Forum,
  RetrieveForumError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveForumRequest,
  output: Forum,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateForumError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update forum [Legacy API — https://docs.whop.com/api-reference] Update moderation and notification settings for a forum, such as who can post, who can comment, and email notification preferences. Required permissions: - `forum:moderate` */
export const updateForum: API.OperationMethod<
  UpdateForumRequest,
  Forum,
  UpdateForumError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateForumRequest,
  output: Forum,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
