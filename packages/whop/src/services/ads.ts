// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** The call-to-action button shown on the ad. */
export type CreateAdRequestCallToAction =
  | "apply_now"
  | "book_now"
  | "call_now"
  | "contact_us"
  | "download"
  | "get_directions"
  | "get_offer"
  | "get_quote"
  | "learn_more"
  | "listen_now"
  | "message_page"
  | "no_button"
  | "open_link"
  | "order_now"
  | "request_time"
  | "see_details"
  | "see_menu"
  | "send_updates"
  | "shop_now"
  | "sign_up"
  | "subscribe"
  | "watch_more";
export const CreateAdRequestCallToAction = /*@__PURE__*/ S.String;

/** The saved crop window for this creative, in source image pixels. Omit it for the original asset or for a format that has not been cropped. */
export interface CreateAdRequestCreativesItemCrop {
  height?: number;
  width?: number;
  x?: number;
  y?: number;
}
export const CreateAdRequestCreativesItemCrop = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    height: S.optional(S.Number),
    width: S.optional(S.Number),
    x: S.optional(S.Number),
    y: S.optional(S.Number),
  }),
).annotate({
  identifier: "CreateAdRequestCreativesItemCrop",
}) as any as S.Schema<CreateAdRequestCreativesItemCrop>;

export type CreateAdRequestCreativesItemFormat =
  | "square"
  | "vertical"
  | "horizontal";
export const CreateAdRequestCreativesItemFormat = /*@__PURE__*/ S.String;

export interface CreateAdRequestCreativesItem {
  /** The saved crop window for this creative, in source image pixels. Omit it for the original asset or for a format that has not been cropped. */
  crop?: CreateAdRequestCreativesItemCrop;
  format?: CreateAdRequestCreativesItemFormat | (string & {});
  /** Uploaded file ID, prefixed `file_`. */
  id?: string;
}
export const CreateAdRequestCreativesItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    crop: S.optional(CreateAdRequestCreativesItemCrop),
    format: S.optional(CreateAdRequestCreativesItemFormat),
    id: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateAdRequestCreativesItem",
}) as any as S.Schema<CreateAdRequestCreativesItem>;

/** The ad's creative assets. Each entry is an uploaded file id with an optional format; omit format for the original asset. Two or more entries with no format become a carousel (2-10 attachments), in order, sharing the ad's copy. */
export type CreateAdRequestCreativesList = Array<CreateAdRequestCreativesItem>;
export const CreateAdRequestCreativesList = /*@__PURE__*/ S.Array(
  CreateAdRequestCreativesItem,
) as any as S.Schema<CreateAdRequestCreativesList>;

/** The description variants shown on the ad. */
export type CreateAdRequestDescriptionsList = Array<string>;
export const CreateAdRequestDescriptionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateAdRequestDescriptionsList>;

/** The headline variants shown on the ad. */
export type CreateAdRequestHeadlinesList = Array<string>;
export const CreateAdRequestHeadlinesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateAdRequestHeadlinesList>;

/** Optional completion screen shown after submission; url sets the follow-up website button. */
export interface CreateAdRequestLeadFormCompletion {
  button_text?: string;
  description?: string;
  headline?: string;
  url?: string;
}
export const CreateAdRequestLeadFormCompletion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    button_text: S.optional(S.String),
    description: S.optional(S.String),
    headline: S.optional(S.String),
    url: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateAdRequestLeadFormCompletion",
}) as any as S.Schema<CreateAdRequestLeadFormCompletion>;

export interface CreateAdRequestLeadFormDisclaimerCheckboxesItem {
  checked_by_default?: boolean;
  key?: string;
  required?: boolean;
  text?: string;
}
export const CreateAdRequestLeadFormDisclaimerCheckboxesItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      checked_by_default: S.optional(S.Boolean),
      key: S.optional(S.String),
      required: S.optional(S.Boolean),
      text: S.optional(S.String),
    }),
  ).annotate({
    identifier: "CreateAdRequestLeadFormDisclaimerCheckboxesItem",
  }) as any as S.Schema<CreateAdRequestLeadFormDisclaimerCheckboxesItem>;

export type CreateAdRequestLeadFormDisclaimerCheckboxesList =
  Array<CreateAdRequestLeadFormDisclaimerCheckboxesItem>;
export const CreateAdRequestLeadFormDisclaimerCheckboxesList =
  /*@__PURE__*/ S.Array(
    CreateAdRequestLeadFormDisclaimerCheckboxesItem,
  ) as any as S.Schema<CreateAdRequestLeadFormDisclaimerCheckboxesList>;

/** Optional custom consent disclaimer with checkboxes. */
export interface CreateAdRequestLeadFormDisclaimer {
  body?: string;
  checkboxes?: CreateAdRequestLeadFormDisclaimerCheckboxesList;
  title?: string;
}
export const CreateAdRequestLeadFormDisclaimer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: S.optional(S.String),
    checkboxes: S.optional(CreateAdRequestLeadFormDisclaimerCheckboxesList),
    title: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateAdRequestLeadFormDisclaimer",
}) as any as S.Schema<CreateAdRequestLeadFormDisclaimer>;

/** more_volume (default) is quickest to submit; higher_intent adds a confirmation step. */
export type CreateAdRequestLeadFormFormType = "more_volume" | "higher_intent";
export const CreateAdRequestLeadFormFormType = /*@__PURE__*/ S.String;

/** Optional intro screen shown before the questions. */
export interface CreateAdRequestLeadFormIntro {
  description?: string;
  headline?: string;
}
export const CreateAdRequestLeadFormIntro = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.optional(S.String),
    headline: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateAdRequestLeadFormIntro",
}) as any as S.Schema<CreateAdRequestLeadFormIntro>;

/** Your privacy policy. url is required by the ad platform. */
export interface CreateAdRequestLeadFormPrivacyPolicy {
  link_text?: string;
  url?: string;
}
export const CreateAdRequestLeadFormPrivacyPolicy = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      link_text: S.optional(S.String),
      url: S.optional(S.String),
    }),
).annotate({
  identifier: "CreateAdRequestLeadFormPrivacyPolicy",
}) as any as S.Schema<CreateAdRequestLeadFormPrivacyPolicy>;

export type CreateAdRequestLeadFormQuestionsItemFormat =
  | "short_answer"
  | "multiple_choice"
  | "appointment";
export const CreateAdRequestLeadFormQuestionsItemFormat =
  /*@__PURE__*/ S.String;

export type CreateAdRequestLeadFormQuestionsItemOptionsItemLogicAction =
  | "go_to_question"
  | "submit_form"
  | "close_form";
export const CreateAdRequestLeadFormQuestionsItemOptionsItemLogicAction =
  /*@__PURE__*/ S.String;

export interface CreateAdRequestLeadFormQuestionsItemOptionsItemLogic {
  action?:
    | CreateAdRequestLeadFormQuestionsItemOptionsItemLogicAction
    | (string & {});
  target_end_page_index?: number;
  target_question_index?: number;
}
export const CreateAdRequestLeadFormQuestionsItemOptionsItemLogic =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      action: S.optional(
        CreateAdRequestLeadFormQuestionsItemOptionsItemLogicAction,
      ),
      target_end_page_index: S.optional(S.Number),
      target_question_index: S.optional(S.Number),
    }),
  ).annotate({
    identifier: "CreateAdRequestLeadFormQuestionsItemOptionsItemLogic",
  }) as any as S.Schema<CreateAdRequestLeadFormQuestionsItemOptionsItemLogic>;

export interface CreateAdRequestLeadFormQuestionsItemOptionsItem {
  key?: string;
  logic?: CreateAdRequestLeadFormQuestionsItemOptionsItemLogic;
  value?: string;
}
export const CreateAdRequestLeadFormQuestionsItemOptionsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      key: S.optional(S.String),
      logic: S.optional(CreateAdRequestLeadFormQuestionsItemOptionsItemLogic),
      value: S.optional(S.String),
    }),
  ).annotate({
    identifier: "CreateAdRequestLeadFormQuestionsItemOptionsItem",
  }) as any as S.Schema<CreateAdRequestLeadFormQuestionsItemOptionsItem>;

export type CreateAdRequestLeadFormQuestionsItemOptionsList =
  Array<CreateAdRequestLeadFormQuestionsItemOptionsItem>;
export const CreateAdRequestLeadFormQuestionsItemOptionsList =
  /*@__PURE__*/ S.Array(
    CreateAdRequestLeadFormQuestionsItemOptionsItem,
  ) as any as S.Schema<CreateAdRequestLeadFormQuestionsItemOptionsList>;

export type CreateAdRequestLeadFormQuestionsItemType =
  | "email"
  | "phone"
  | "full_name"
  | "first_name"
  | "last_name"
  | "city"
  | "state"
  | "zip"
  | "country"
  | "street_address"
  | "job_title"
  | "company_name"
  | "work_email"
  | "work_phone_number"
  | "dob"
  | "gender"
  | "marital_status"
  | "relationship_status"
  | "military_status"
  | "date_time"
  | "custom";
export const CreateAdRequestLeadFormQuestionsItemType = /*@__PURE__*/ S.String;

export interface CreateAdRequestLeadFormQuestionsItem {
  format?: CreateAdRequestLeadFormQuestionsItemFormat | (string & {});
  label?: string;
  options?: CreateAdRequestLeadFormQuestionsItemOptionsList;
  type?: CreateAdRequestLeadFormQuestionsItemType | (string & {});
}
export const CreateAdRequestLeadFormQuestionsItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      format: S.optional(CreateAdRequestLeadFormQuestionsItemFormat),
      label: S.optional(S.String),
      options: S.optional(CreateAdRequestLeadFormQuestionsItemOptionsList),
      type: S.optional(CreateAdRequestLeadFormQuestionsItemType),
    }),
).annotate({
  identifier: "CreateAdRequestLeadFormQuestionsItem",
}) as any as S.Schema<CreateAdRequestLeadFormQuestionsItem>;

/** The questions on the form. Standard prefill types need only a type; a custom question needs a label and a format (plus options for multiple_choice). Options carry an optional key and answer-routing logic. */
export type CreateAdRequestLeadFormQuestionsList =
  Array<CreateAdRequestLeadFormQuestionsItem>;
export const CreateAdRequestLeadFormQuestionsList = /*@__PURE__*/ S.Array(
  CreateAdRequestLeadFormQuestionsItem,
) as any as S.Schema<CreateAdRequestLeadFormQuestionsList>;

/** Instant lead form for the ad. Only allowed when the ad group's conversion_location is an instant-form destination (instant_forms, instant_forms_and_messenger, website_and_instant_forms). Mutually exclusive with lead_form_id. */
export interface CreateAdRequestLeadForm {
  /** Optional completion screen shown after submission; url sets the follow-up website button. */
  completion?: CreateAdRequestLeadFormCompletion;
  /** Optional custom consent disclaimer with checkboxes. */
  disclaimer?: CreateAdRequestLeadFormDisclaimer;
  /** more_volume (default) is quickest to submit; higher_intent adds a confirmation step. */
  form_type?: CreateAdRequestLeadFormFormType | (string & {});
  /** Optional intro screen shown before the questions. */
  intro?: CreateAdRequestLeadFormIntro;
  /** Internal name for the form. Auto-generated if omitted. */
  name?: string;
  /** Require SMS verification of the phone number (higher_intent forms). */
  phone_verification?: boolean;
  /** Your privacy policy. url is required by the ad platform. */
  privacy_policy?: CreateAdRequestLeadFormPrivacyPolicy;
  /** The questions on the form. Standard prefill types need only a type; a custom question needs a label and a format (plus options for multiple_choice). Options carry an optional key and answer-routing logic. */
  questions?: CreateAdRequestLeadFormQuestionsList;
}
export const CreateAdRequestLeadForm = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    completion: S.optional(CreateAdRequestLeadFormCompletion),
    disclaimer: S.optional(CreateAdRequestLeadFormDisclaimer),
    form_type: S.optional(CreateAdRequestLeadFormFormType),
    intro: S.optional(CreateAdRequestLeadFormIntro),
    name: S.optional(S.String),
    phone_verification: S.optional(S.Boolean),
    privacy_policy: S.optional(CreateAdRequestLeadFormPrivacyPolicy),
    questions: S.optional(CreateAdRequestLeadFormQuestionsList),
  }),
).annotate({
  identifier: "CreateAdRequestLeadForm",
}) as any as S.Schema<CreateAdRequestLeadForm>;

/** Click-to-message welcome copy: the greeting (message) and the ice-breaker prompt (keyword). */
export interface CreateAdRequestMessagingConfig {
  keyword?: string;
  message?: string;
}
export const CreateAdRequestMessagingConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyword: S.optional(S.String),
    message: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateAdRequestMessagingConfig",
}) as any as S.Schema<CreateAdRequestMessagingConfig>;

/** Identifies the network that owns `existing_post_id`. The source is inferred from the ID shape when omitted. */
export type CreateAdRequestPostSource = "facebook" | "instagram";
export const CreateAdRequestPostSource = /*@__PURE__*/ S.String;

/** The primary text variants shown in the ad body. */
export type CreateAdRequestPrimaryTextsList = Array<string>;
export const CreateAdRequestPrimaryTextsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateAdRequestPrimaryTextsList>;

export interface CreateAdRequestSocialAccountsItem {
  /** Social account ID, prefixed `sacc_`. */
  id?: string;
}
export const CreateAdRequestSocialAccountsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateAdRequestSocialAccountsItem",
}) as any as S.Schema<CreateAdRequestSocialAccountsItem>;

/** The social accounts the ad runs under — a connected Facebook page and, optionally, an Instagram profile. */
export type CreateAdRequestSocialAccountsList =
  Array<CreateAdRequestSocialAccountsItem>;
export const CreateAdRequestSocialAccountsList = /*@__PURE__*/ S.Array(
  CreateAdRequestSocialAccountsItem,
) as any as S.Schema<CreateAdRequestSocialAccountsList>;

export interface CreateAdRequest {
  /** An inline ad group to create (same shape as POST /ad_groups, including ad_campaign_id). Creates the ad group and the ad together. Provide this OR ad_group_id. */
  ad_group?: unknown;
  /** The existing ad group to create the ad in. Provide this OR ad_group, not both. */
  ad_group_id?: string;
  /** The call-to-action button shown on the ad. */
  call_to_action?: CreateAdRequestCallToAction | (string & {});
  /** The ad's creative assets. Each entry is an uploaded file id with an optional format; omit format for the original asset. Two or more entries with no format become a carousel (2-10 attachments), in order, sharing the ad's copy. */
  creatives?: CreateAdRequestCreativesList;
  /** The description variants shown on the ad. */
  descriptions?: CreateAdRequestDescriptionsList;
  /** Promote a post you already published instead of uploading creatives — a Facebook post or Instagram media id. Mutually exclusive with creatives. Pair with post_source. */
  existing_post_id?: string;
  /** The headline variants shown on the ad. */
  headlines?: CreateAdRequestHeadlinesList;
  /** Instant lead form for the ad. Only allowed when the ad group's conversion_location is an instant-form destination (instant_forms, instant_forms_and_messenger, website_and_instant_forms). Mutually exclusive with lead_form_id. */
  lead_form?: CreateAdRequestLeadForm;
  /** Use an existing instant form instead of creating one — the form's platform ID, from a form already on the ad's Facebook page. Only allowed when the ad group's conversion_location is an instant-form destination. Mutually exclusive with lead_form. */
  lead_form_id?: string;
  /** Click-to-message welcome copy: the greeting (message) and the ice-breaker prompt (keyword). */
  messaging_config?: CreateAdRequestMessagingConfig;
  /** Whether the ad can appear alongside other advertisers' ads in the same unit. Defaults to true. */
  multi_advertiser_ads?: boolean;
  /** Identifies the network that owns `existing_post_id`. The source is inferred from the ID shape when omitted. */
  post_source?: CreateAdRequestPostSource | (string & {});
  /** The primary text variants shown in the ad body. */
  primary_texts?: CreateAdRequestPrimaryTextsList;
  /** The social accounts the ad runs under — a connected Facebook page and, optionally, an Instagram profile. */
  social_accounts?: CreateAdRequestSocialAccountsList;
  /** The display name of the ad. */
  title?: string;
  /** The URL the ad links to. Query parameters are merged into url_parameters, so the stored URL is always bare. */
  url?: string;
  /** Query parameters to append to the destination URL, keyed by parameter name. Merged with any query string on `url`. Whop adds its own click-attribution parameters; those are reserved and rejected if you set them (utm_meta_ad_id, utm_meta_adset_id, utm_meta_campaign_id, utm_source, utm_placement, utm_medium, utm_content, utm_adset, utm_whop, wacid, wasid, waid, tw_source, tw_adid). */
  url_parameters?: unknown;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateAdRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ad_group: S.optional(S.Unknown),
    ad_group_id: S.optional(S.String),
    call_to_action: S.optional(CreateAdRequestCallToAction),
    creatives: S.optional(CreateAdRequestCreativesList),
    descriptions: S.optional(CreateAdRequestDescriptionsList),
    existing_post_id: S.optional(S.String),
    headlines: S.optional(CreateAdRequestHeadlinesList),
    lead_form: S.optional(CreateAdRequestLeadForm),
    lead_form_id: S.optional(S.String),
    messaging_config: S.optional(CreateAdRequestMessagingConfig),
    multi_advertiser_ads: S.optional(S.Boolean),
    post_source: S.optional(CreateAdRequestPostSource),
    primary_texts: S.optional(CreateAdRequestPrimaryTextsList),
    social_accounts: S.optional(CreateAdRequestSocialAccountsList),
    title: S.optional(S.String),
    url: S.optional(S.String),
    url_parameters: S.optional(S.Unknown),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/ads", code: 200 })),
).annotate({
  identifier: "CreateAdRequest",
}) as any as S.Schema<CreateAdRequest>;

export interface AdEntityReference {
  /** The referenced entity's id. */
  id: string;
}
export const AdEntityReference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "AdEntityReference",
}) as any as S.Schema<AdEntityReference>;

/** The call-to-action button shown on the ad. */
export type AdCallToAction =
  | "learn_more"
  | "shop_now"
  | "sign_up"
  | "subscribe"
  | "get_started"
  | "book_now"
  | "apply_now"
  | "contact_us"
  | "download"
  | "order_now"
  | "buy_now"
  | "get_quote"
  | "message_page"
  | "whatsapp_message"
  | "instagram_message"
  | "call_now"
  | "get_directions"
  | "send_updates"
  | "get_offer"
  | "watch_more"
  | "listen_now"
  | "play_game"
  | "open_link"
  | "no_button"
  | "get_offer_view"
  | "get_event_tickets"
  | "see_menu"
  | "request_time"
  | "event_rsvp"
  | "see_details"
  | "view_instagram_profile";
export const AdCallToAction = /*@__PURE__*/ S.String;

export interface AdCreativeCrop {
  /** Height of the crop window in source pixels. */
  height: number;
  /** Width of the crop window in source pixels. */
  width: number;
  /** Left edge of the crop window in source pixels. */
  x: number;
  /** Top edge of the crop window in source pixels. */
  y: number;
}
export const AdCreativeCrop = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    height: S.Number,
    width: S.Number,
    x: S.Number,
    y: S.Number,
  }),
).annotate({ identifier: "AdCreativeCrop" }) as any as S.Schema<AdCreativeCrop>;

/** The placement variant this asset covers, or null for the original asset. */
export type AdCreativeFormat = "square" | "vertical" | "horizontal";
export const AdCreativeFormat = /*@__PURE__*/ S.String;

export interface AdCreative {
  /** The saved crop window for this creative, in source image pixels. Null for the original asset or a format that has not been cropped. */
  crop: AdCreativeCrop | null;
  /** The placement variant this asset covers, or null for the original asset. */
  format: AdCreativeFormat | null;
  /** The creative attachment's file id. */
  id: string;
  /** The kind of asset, image or video. */
  media_type: string | null;
  /** CDN url of the asset. */
  url: string | null;
}
export const AdCreative = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    crop: S.NullOr(AdCreativeCrop),
    format: S.NullOr(AdCreativeFormat),
    id: S.String,
    media_type: S.NullOr(S.String),
    url: S.NullOr(S.String),
  }),
).annotate({ identifier: "AdCreative" }) as any as S.Schema<AdCreative>;

export type AdCreativesList = Array<AdCreative>;
export const AdCreativesList = /*@__PURE__*/ S.Array(
  AdCreative,
) as any as S.Schema<AdCreativesList>;

/** Whether the ad is delivering right now, and if not, why. When several states apply at once, the highest-precedence one is returned. */
export type AdDeliveryStatus =
  | "rejected"
  | "in_review"
  | "draft"
  | "campaign_paused"
  | "ad_group_paused"
  | "paused"
  | "processing"
  | "issues"
  | "scheduled"
  | "learning_limited"
  | "learning"
  | "active";
export const AdDeliveryStatus = /*@__PURE__*/ S.String;

export type AdDescriptionsList = Array<string>;
export const AdDescriptionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdDescriptionsList>;

export type AdHeadlinesList = Array<string>;
export const AdHeadlinesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdHeadlinesList>;

/** The type of resource the issue is attached to. */
export type AdPlatformIssueResourceType = "ad_campaign" | "ad_group" | "ad";
export const AdPlatformIssueResourceType = /*@__PURE__*/ S.String;

export interface AdPlatformIssue {
  /** Unique identifier for the issue. */
  id: string;
  /** A description of what the issue is and how it can be resolved. */
  message: string;
  /** The ID of the campaign, ad group, or ad the issue is attached to. */
  resource_id: string | null;
  /** The type of resource the issue is attached to. */
  resource_type: AdPlatformIssueResourceType;
}
export const AdPlatformIssue = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    message: S.String,
    resource_id: S.NullOr(S.String),
    resource_type: AdPlatformIssueResourceType,
  }),
).annotate({
  identifier: "AdPlatformIssue",
}) as any as S.Schema<AdPlatformIssue>;

export type AdIssuesList = Array<AdPlatformIssue>;
export const AdIssuesList = /*@__PURE__*/ S.Array(
  AdPlatformIssue,
) as any as S.Schema<AdIssuesList>;

export interface AdLeadFormCompletion {
  /** Text of the follow-up button. */
  button_text: string | null;
  /** Body text under the headline. */
  description: string | null;
  /** Headline of the completion screen. */
  headline: string | null;
  /** Website the follow-up button opens. `null` when the screen has no button. */
  url: string | null;
}
export const AdLeadFormCompletion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    button_text: S.NullOr(S.String),
    description: S.NullOr(S.String),
    headline: S.NullOr(S.String),
    url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AdLeadFormCompletion",
}) as any as S.Schema<AdLeadFormCompletion>;

export interface AdLeadFormDisclaimerCheckbox {
  /** Whether the checkbox starts ticked. */
  checked_by_default: boolean | null;
  /** Stable identifier consent responses are stored under. */
  key: string | null;
  /** Whether the checkbox must be ticked to submit the form. */
  required: boolean | null;
  /** Consent text next to the checkbox. */
  text: string;
}
export const AdLeadFormDisclaimerCheckbox = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    checked_by_default: S.NullOr(S.Boolean),
    key: S.NullOr(S.String),
    required: S.NullOr(S.Boolean),
    text: S.String,
  }),
).annotate({
  identifier: "AdLeadFormDisclaimerCheckbox",
}) as any as S.Schema<AdLeadFormDisclaimerCheckbox>;

export type AdLeadFormDisclaimerCheckboxesList =
  Array<AdLeadFormDisclaimerCheckbox>;
export const AdLeadFormDisclaimerCheckboxesList = /*@__PURE__*/ S.Array(
  AdLeadFormDisclaimerCheckbox,
) as any as S.Schema<AdLeadFormDisclaimerCheckboxesList>;

export interface AdLeadFormDisclaimer {
  /** Disclaimer text. */
  body: string | null;
  checkboxes: AdLeadFormDisclaimerCheckboxesList;
  /** Disclaimer title. */
  title: string | null;
}
export const AdLeadFormDisclaimer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: S.NullOr(S.String),
    checkboxes: AdLeadFormDisclaimerCheckboxesList,
    title: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AdLeadFormDisclaimer",
}) as any as S.Schema<AdLeadFormDisclaimer>;

/** `more_volume` is quickest to submit; `higher_intent` adds a confirmation step before submission. */
export type AdLeadFormFormType = "more_volume" | "higher_intent";
export const AdLeadFormFormType = /*@__PURE__*/ S.String;

export interface AdLeadFormIntro {
  /** Body text under the headline. */
  description: string | null;
  /** Headline of the intro screen. */
  headline: string | null;
}
export const AdLeadFormIntro = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.NullOr(S.String),
    headline: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AdLeadFormIntro",
}) as any as S.Schema<AdLeadFormIntro>;

export interface AdLeadFormPrivacyPolicy {
  /** Link text shown for the policy. `null` uses the platform default. */
  link_text: string | null;
  /** URL of your privacy policy. */
  url: string;
}
export const AdLeadFormPrivacyPolicy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    link_text: S.NullOr(S.String),
    url: S.String,
  }),
).annotate({
  identifier: "AdLeadFormPrivacyPolicy",
}) as any as S.Schema<AdLeadFormPrivacyPolicy>;

/** What happens when the choice is selected. */
export type AdLeadFormOptionLogicAction =
  | "go_to_question"
  | "submit_form"
  | "close_form";
export const AdLeadFormOptionLogicAction = /*@__PURE__*/ S.String;

export interface AdLeadFormOptionLogic {
  /** What happens when the choice is selected. */
  action: AdLeadFormOptionLogicAction;
  /** Zero-based index of the ending screen to jump to. */
  target_end_page_index?: number;
  /** Zero-based index of the question to jump to, for `go_to_question`. */
  target_question_index?: number;
}
export const AdLeadFormOptionLogic = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: AdLeadFormOptionLogicAction,
    target_end_page_index: S.optional(S.Number),
    target_question_index: S.optional(S.Number),
  }),
).annotate({
  identifier: "AdLeadFormOptionLogic",
}) as any as S.Schema<AdLeadFormOptionLogic>;

export interface AdLeadFormQuestionOption {
  /** Stable identifier the choice's answers are stored under. Absent for simple choices. */
  key?: string | null;
  /** Where the form goes when this choice is selected. Absent when the form just continues to the next question. */
  logic?: AdLeadFormOptionLogic;
  /** Choice text shown to the person. */
  value: string;
}
export const AdLeadFormQuestionOption = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.optional(S.NullOr(S.String)),
    logic: S.optional(AdLeadFormOptionLogic),
    value: S.String,
  }),
).annotate({
  identifier: "AdLeadFormQuestionOption",
}) as any as S.Schema<AdLeadFormQuestionOption>;

export type AdLeadFormQuestionOptionsList = Array<AdLeadFormQuestionOption>;
export const AdLeadFormQuestionOptionsList = /*@__PURE__*/ S.Array(
  AdLeadFormQuestionOption,
) as any as S.Schema<AdLeadFormQuestionOptionsList>;

export interface AdLeadFormQuestion {
  /** Answer format for `custom` questions: `short_answer`, `multiple_choice`, or `appointment`. Absent otherwise. */
  format?: string;
  /** Question text for `custom` questions. Absent for standard prefill questions. */
  label?: string;
  options?: AdLeadFormQuestionOptionsList;
  /** Question type: a standard prefill type such as `email`, `phone`, or `full_name`, or `custom` for your own question. */
  type: string;
}
export const AdLeadFormQuestion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    format: S.optional(S.String),
    label: S.optional(S.String),
    options: S.optional(AdLeadFormQuestionOptionsList),
    type: S.String,
  }),
).annotate({
  identifier: "AdLeadFormQuestion",
}) as any as S.Schema<AdLeadFormQuestion>;

export type AdLeadFormQuestionsList = Array<AdLeadFormQuestion>;
export const AdLeadFormQuestionsList = /*@__PURE__*/ S.Array(
  AdLeadFormQuestion,
) as any as S.Schema<AdLeadFormQuestionsList>;

export interface AdLeadForm {
  /** Screen shown after the form is submitted. `null` when the form uses the default. */
  completion: AdLeadFormCompletion | null;
  /** Custom consent disclaimer shown before submission. `null` when the form has none. */
  disclaimer: AdLeadFormDisclaimer | null;
  /** `more_volume` is quickest to submit; `higher_intent` adds a confirmation step before submission. */
  form_type: AdLeadFormFormType;
  /** Intro screen shown before the questions. `null` when the form has none. */
  intro: AdLeadFormIntro | null;
  /** Internal name of the form. */
  name: string | null;
  /** Whether the phone number must be verified by SMS before submitting. */
  phone_verification: boolean;
  /** Your privacy policy, linked from the form. `null` when unset. */
  privacy_policy: AdLeadFormPrivacyPolicy | null;
  questions: AdLeadFormQuestionsList;
}
export const AdLeadForm = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    completion: S.NullOr(AdLeadFormCompletion),
    disclaimer: S.NullOr(AdLeadFormDisclaimer),
    form_type: AdLeadFormFormType,
    intro: S.NullOr(AdLeadFormIntro),
    name: S.NullOr(S.String),
    phone_verification: S.Boolean,
    privacy_policy: S.NullOr(AdLeadFormPrivacyPolicy),
    questions: AdLeadFormQuestionsList,
  }),
).annotate({ identifier: "AdLeadForm" }) as any as S.Schema<AdLeadForm>;

export interface AdMessagingConfig {
  /** Suggested reply the person can tap to start the conversation. */
  keyword: string | null;
  /** Greeting shown when the conversation opens. */
  message: string | null;
}
export const AdMessagingConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyword: S.NullOr(S.String),
    message: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AdMessagingConfig",
}) as any as S.Schema<AdMessagingConfig>;

/** Identifies the network that owns `existing_post_id`; `null` when the ad uses uploaded creatives. */
export type AdPostSource = "facebook" | "instagram";
export const AdPostSource = /*@__PURE__*/ S.String;

export type AdPrimaryTextsList = Array<string>;
export const AdPrimaryTextsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdPrimaryTextsList>;

/** The Whop pixel conversion event whose attributed count represents results — the optimization goal, or the highest-volume attributed event for campaigns that budget per ad group. Null when the goal isn't a Whop-attributed event. */
export type AdResultEvent =
  | "purchase"
  | "lead"
  | "schedule"
  | "submit_application"
  | "contact"
  | "complete_registration"
  | "view_content"
  | "add_to_cart"
  | "custom"
  | "messaging_conversation";
export const AdResultEvent = /*@__PURE__*/ S.String;

export type AdSocialAccountsList = Array<AdEntityReference>;
export const AdSocialAccountsList = /*@__PURE__*/ S.Array(
  AdEntityReference,
) as any as S.Schema<AdSocialAccountsList>;

/** Whether the ad is enabled. `active` and `paused` are set by you; `in_review` and `rejected` come from ad review. */
export type AdStatus = "active" | "paused" | "in_review" | "rejected";
export const AdStatus = /*@__PURE__*/ S.String;

export interface Ad {
  /** The ad campaign this ad belongs to. */
  ad_campaign: AdEntityReference;
  /** The ad group this ad belongs to. */
  ad_group: AdEntityReference;
  /** USD value attributed to add-to-cart events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  added_to_cart_value: number;
  /** Whop pixel-attributed add-to-cart events, last-click. */
  added_to_carts: number;
  /** The call-to-action button shown on the ad. */
  call_to_action: AdCallToAction | null;
  /** Clicks divided by impressions, between 0 and 1. */
  click_through_rate: number;
  /** The number of clicks. */
  clicks: number;
  /** USD value attributed to complete-registration events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  completed_registration_value: number;
  /** Whop pixel-attributed complete-registration events, last-click. */
  completed_registrations: number;
  /** USD value attributed to contact events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  contact_value: number;
  /** Whop pixel-attributed contact events, last-click. */
  contacts: number;
  /** Spend divided by attributed add-to-cart events; null when they are not the goal and none are attributed. */
  cost_per_added_to_cart: number | null;
  /** Spend divided by clicks; 0 when there are no clicks. */
  cost_per_click: number;
  /** Spend divided by attributed complete-registration events; null when they are not the goal and none are attributed. */
  cost_per_completed_registration: number | null;
  /** Spend divided by attributed contact events; null when contacts are not the goal and none are attributed. */
  cost_per_contact: number | null;
  /** Spend divided by attributed leads; null when leads are not a goal and none are attributed. */
  cost_per_lead: number | null;
  /** Spend per 1,000 impressions; 0 when there are no impressions. */
  cost_per_mille: number;
  /** Spend divided by attributed purchases; null when purchases are not a goal and none are attributed. */
  cost_per_purchase: number | null;
  /** Spend divided by Whop pixel-attributed results; null when nothing Whop-attributable is being optimized for. */
  cost_per_result: number | null;
  /** Spend divided by attributed schedule events; null when schedules are not the goal and none are attributed. */
  cost_per_schedule: number | null;
  /** Spend divided by attributed submit-application events; null when they are not the goal and none are attributed. */
  cost_per_submitted_application: number | null;
  /** Spend divided by unique clicks; null when there are no unique clicks. */
  cost_per_unique_click: number | null;
  /** Spend divided by attributed view-content events; null when they are not the goal and none are attributed. */
  cost_per_viewed_content: number | null;
  /** When the ad was created, as an ISO 8601 timestamp. */
  created_at: string;
  creatives: AdCreativesList;
  /** Whop pixel-attributed custom (merchant-defined) conversion events, last-click, across all custom event names. */
  custom_conversions: number;
  /** Whop pixel-attributed custom conversions, keyed by your event name with its last-click count as the value. Empty when no named custom events are attributed. Custom events fired without a name are counted in custom_conversions but omitted here, so these values sum to at most custom_conversions. */
  custom_event_counts: unknown;
  /** Conversion value attributed to each custom event, keyed by event name like custom_event_counts. Sums the value passed to whop.track, normalized to USD; events fired without a value contribute 0. */
  custom_event_values: unknown;
  /** Whether the ad is delivering right now, and if not, why. When several states apply at once, the highest-precedence one is returned. */
  delivery_status: AdDeliveryStatus;
  descriptions: AdDescriptionsList;
  /** The post you pointed this ad at, when it promotes one you already published — a Facebook post, Instagram media, or TikTok video ID. `null` when the ad uses uploaded creatives. */
  existing_post_id: string | null;
  /** Platform-reported impressions divided by reach. */
  frequency: number | null;
  headlines: AdHeadlinesList;
  /** Unique identifier for the ad, prefixed `ad_`. */
  id: string;
  /** The number of impressions. */
  impressions: number;
  issues: AdIssuesList;
  /** The instant lead form shown when someone taps this ad. `null` when the ad group's conversion_location is not an instant-form destination. */
  lead_form?: AdLeadForm | null;
  /** The ad platform's ID for the instant form the ad uses. Set when the ad references an existing form via `lead_form_id`, or once a form built from `lead_form` has been created on the platform. */
  lead_form_id?: string | null;
  /** USD value attributed to lead events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  lead_value: number;
  /** Whop pixel-attributed leads, last-click. */
  leads: number;
  /** Welcome message for click-to-message ads, shown when the conversation opens. `null` when the ad has none. */
  messaging_config?: AdMessagingConfig | null;
  /** Whether the ad can appear alongside other advertisers' ads in the same unit. Defaults to true. */
  multi_advertiser_ads?: boolean;
  /** The post the ad network serves for this ad, as `pageID_postID` on Meta — the post Meta created for an uploaded creative, or the post being promoted. Use it to open the live post, or to promote the same post from another ad. `null` until the network has created the post. */
  post_id: string | null;
  /** Identifies the network that owns `existing_post_id`; `null` when the ad uses uploaded creatives. */
  post_source: AdPostSource | null;
  /** Preview image of the post named by `existing_post_id`. `null` for ads that use uploaded creatives, or until the post's media has been fetched from the network. */
  post_thumbnail_url: string | null;
  primary_texts: AdPrimaryTextsList;
  /** USD value of pixel-attributed purchases. */
  purchase_value: number;
  /** Whop pixel-attributed purchases, last-click. */
  purchases: number;
  /** The number of unique people who saw this. */
  reach: number;
  /** The Whop pixel conversion event whose attributed count represents results — the optimization goal, or the highest-volume attributed event for campaigns that budget per ad group. Null when the goal isn't a Whop-attributed event. */
  result_event: AdResultEvent | null;
  /** The merchant-defined event name when result_event is custom; null for the standard events. */
  result_event_name: string | null;
  /** The Whop pixel-attributed count behind result_event. When a campaign's ad groups optimize different goals there is no single result_event (it is null), and this is instead the sum of each ad group's own attributed results. Null when nothing Whop-attributable is being optimized for. */
  results: number | null;
  /** Purchase value divided by spend, both in USD (a currency-neutral ratio); 0 when there is no spend. */
  return_on_ad_spend: number;
  /** USD value attributed to schedule events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  schedule_value: number;
  /** Whop pixel-attributed schedule events, last-click. */
  schedules: number;
  social_accounts: AdSocialAccountsList;
  /** The amount charged, in spend_currency. */
  spend: number;
  /** The ISO 4217 currency code of all monetary metrics. */
  spend_currency: string | null;
  /** Whether the ad is enabled. `active` and `paused` are set by you; `in_review` and `rejected` come from ad review. */
  status: AdStatus;
  /** USD value attributed to submit-application events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  submitted_application_value: number;
  /** Whop pixel-attributed submit-application events, last-click. */
  submitted_applications: number;
  /** Display title of the ad. */
  title: string | null;
  /** Unique clicks divided by impressions, between 0 and 1. */
  unique_click_through_rate: number | null;
  /** People who clicked, reported by the Whop pixel, counted once per person. */
  unique_clicks: number;
  /** When the ad was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** The URL the ad links to, without its query string. Parameters belong in `url_parameters`; any you send on `url` are moved there. */
  url: string | null;
  /** Every query parameter appended to the URL, keyed by parameter name — including any you sent on `url` itself. Whop adds its own click-attribution parameters on top; those are reserved and rejected if you set them (utm_meta_ad_id, utm_meta_adset_id, utm_meta_campaign_id, utm_source, utm_placement, utm_medium, utm_content, utm_adset, utm_whop, wacid, wasid, waid, tw_source, tw_adid). */
  url_parameters: unknown;
  /** USD value attributed to view-content events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  viewed_content_value: number;
  /** Whop pixel-attributed view-content events, last-click. */
  viewed_contents: number;
}
export const Ad = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ad_campaign: AdEntityReference,
    ad_group: AdEntityReference,
    added_to_cart_value: S.Number,
    added_to_carts: S.Number,
    call_to_action: S.NullOr(AdCallToAction),
    click_through_rate: S.Number,
    clicks: S.Number,
    completed_registration_value: S.Number,
    completed_registrations: S.Number,
    contact_value: S.Number,
    contacts: S.Number,
    cost_per_added_to_cart: S.NullOr(S.Number),
    cost_per_click: S.Number,
    cost_per_completed_registration: S.NullOr(S.Number),
    cost_per_contact: S.NullOr(S.Number),
    cost_per_lead: S.NullOr(S.Number),
    cost_per_mille: S.Number,
    cost_per_purchase: S.NullOr(S.Number),
    cost_per_result: S.NullOr(S.Number),
    cost_per_schedule: S.NullOr(S.Number),
    cost_per_submitted_application: S.NullOr(S.Number),
    cost_per_unique_click: S.NullOr(S.Number),
    cost_per_viewed_content: S.NullOr(S.Number),
    created_at: S.String,
    creatives: AdCreativesList,
    custom_conversions: S.Number,
    custom_event_counts: S.Unknown,
    custom_event_values: S.Unknown,
    delivery_status: AdDeliveryStatus,
    descriptions: AdDescriptionsList,
    existing_post_id: S.NullOr(S.String),
    frequency: S.NullOr(S.Number),
    headlines: AdHeadlinesList,
    id: S.String,
    impressions: S.Number,
    issues: AdIssuesList,
    lead_form: S.optional(S.NullOr(AdLeadForm)),
    lead_form_id: S.optional(S.NullOr(S.String)),
    lead_value: S.Number,
    leads: S.Number,
    messaging_config: S.optional(S.NullOr(AdMessagingConfig)),
    multi_advertiser_ads: S.optional(S.Boolean),
    post_id: S.NullOr(S.String),
    post_source: S.NullOr(AdPostSource),
    post_thumbnail_url: S.NullOr(S.String),
    primary_texts: AdPrimaryTextsList,
    purchase_value: S.Number,
    purchases: S.Number,
    reach: S.Number,
    result_event: S.NullOr(AdResultEvent),
    result_event_name: S.NullOr(S.String),
    results: S.NullOr(S.Number),
    return_on_ad_spend: S.Number,
    schedule_value: S.Number,
    schedules: S.Number,
    social_accounts: AdSocialAccountsList,
    spend: S.Number,
    spend_currency: S.NullOr(S.String),
    status: AdStatus,
    submitted_application_value: S.Number,
    submitted_applications: S.Number,
    title: S.NullOr(S.String),
    unique_click_through_rate: S.NullOr(S.Number),
    unique_clicks: S.Number,
    updated_at: S.String,
    url: S.NullOr(S.String),
    url_parameters: S.Unknown,
    viewed_content_value: S.Number,
    viewed_contents: S.Number,
  }),
).annotate({ identifier: "Ad" }) as any as S.Schema<Ad>;

export interface DeleteAdRequest {
  /** The ad ID. */
  id: string;
}
export const DeleteAdRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/ads/{id}", code: 200 })),
).annotate({
  identifier: "DeleteAdRequest",
}) as any as S.Schema<DeleteAdRequest>;

export interface DeleteAdResponse {
  /** Always true. */
  deleted: boolean;
  /** ID of the deleted ad. */
  id: string;
}
export const DeleteAdResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "DeleteAdResponse",
}) as any as S.Schema<DeleteAdResponse>;

export interface DuplicateAdRequest {
  /** The ad ID. */
  id: string;
  /** Number of copies to create (1-10). Defaults to 1. */
  count?: number;
  /** Whether the copies keep the original post's engagement (likes, comments, shares). Defaults to false. */
  preserve_engagement?: boolean;
  /** Ad group to duplicate into. Defaults to the ad's own ad group. */
  target_ad_group_id?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const DuplicateAdRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    count: S.optional(S.Number),
    preserve_engagement: S.optional(S.Boolean),
    target_ad_group_id: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/ads/{id}/duplicate", code: 200 })),
).annotate({
  identifier: "DuplicateAdRequest",
}) as any as S.Schema<DuplicateAdRequest>;

export type DuplicateAdResponseDataList = Array<Ad>;
export const DuplicateAdResponseDataList = /*@__PURE__*/ S.Array(
  Ad,
) as any as S.Schema<DuplicateAdResponseDataList>;

export interface DuplicateAdResponse {
  data: DuplicateAdResponseDataList;
}
export const DuplicateAdResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: DuplicateAdResponseDataList,
  }),
).annotate({
  identifier: "DuplicateAdResponse",
}) as any as S.Schema<DuplicateAdResponse>;

export type ListAdsRequestAdCampaignIdsList = Array<string>;
export const ListAdsRequestAdCampaignIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListAdsRequestAdCampaignIdsList>;

export type ListAdsRequestAdGroupIdsList = Array<string>;
export const ListAdsRequestAdGroupIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListAdsRequestAdGroupIdsList>;

export type ListAdsRequestStatus =
  | "active"
  | "paused"
  | "in_review"
  | "rejected";
export const ListAdsRequestStatus = /*@__PURE__*/ S.String;

export type ListAdsRequestOrder =
  | "created_at"
  | "updated_at"
  | "spend"
  | "impressions"
  | "reach"
  | "clicks"
  | "unique_clicks"
  | "frequency"
  | "click_through_rate"
  | "results"
  | "cost_per_mille"
  | "cost_per_click"
  | "cost_per_result"
  | "return_on_ad_spend";
export const ListAdsRequestOrder = /*@__PURE__*/ S.String;

export type ListAdsRequestDirection = "asc" | "desc";
export const ListAdsRequestDirection = /*@__PURE__*/ S.String;

export type ListAdsRequestAttributionModel = "last_touch" | "first_touch";
export const ListAdsRequestAttributionModel = /*@__PURE__*/ S.String;

export interface ListAdsRequest {
  /** The account the ads belong to. Defaults to the account-scoped key's own account. */
  account_id?: string;
  /** Only return ads in this ad campaign. */
  ad_campaign_id?: string;
  /** Only return ads in these ad campaigns (max 100). Repeat the parameter for each id (ad_campaign_ids=a&ad_campaign_ids=b). */
  ad_campaign_ids?: ListAdsRequestAdCampaignIdsList;
  /** Only return ads in this ad group. */
  ad_group_id?: string;
  /** Only return ads in these ad groups (max 100). Repeat the parameter for each id (ad_group_ids=a&ad_group_ids=b). */
  ad_group_ids?: ListAdsRequestAdGroupIdsList;
  /** Only return ads with this status. */
  status?: ListAdsRequestStatus | (string & {});
  /** Filter ads by a title or ID substring. */
  query?: string;
  /** The field to sort by. Defaults to created_at. Stat columns (spend, impressions, …) rank over the stats_from/stats_to window across the whole list, not just the current page. results, cost_per_result and return_on_ad_spend rank by the same Whop pixel-attributed values the response reports. */
  order?: ListAdsRequestOrder | (string & {});
  /** The sort direction. Defaults to desc. */
  direction?: ListAdsRequestDirection | (string & {});
  /** Only return ads created before this timestamp. */
  created_before?: string;
  /** Only return ads created after this timestamp. */
  created_after?: string;
  /** Start of the stats window. Defaults to all-time. */
  stats_from?: string;
  /** End of the stats window. Defaults to now. */
  stats_to?: string;
  /** IANA timezone (e.g. America/New_York) the stats window is interpreted in. Bare stats_from/stats_to dates resolve to day boundaries on this clock. Defaults to UTC. */
  time_zone?: string;
  /** Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise. */
  attribution_model?: ListAdsRequestAttributionModel | (string & {});
  /** The number of ads to return. */
  first?: number;
  /** Cursor to fetch the page after (from page_info.end_cursor). */
  after?: string;
  /** The number of ads to return from the end of the range. */
  last?: number;
  /** Cursor to fetch the page before (from page_info.start_cursor). */
  before?: string;
}
export const ListAdsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    ad_campaign_id: S.optional(S.String.pipe(T.Query())),
    ad_campaign_ids: S.optional(
      ListAdsRequestAdCampaignIdsList.pipe(T.Query()),
    ),
    ad_group_id: S.optional(S.String.pipe(T.Query())),
    ad_group_ids: S.optional(ListAdsRequestAdGroupIdsList.pipe(T.Query())),
    status: S.optional(ListAdsRequestStatus.pipe(T.Query())),
    query: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListAdsRequestOrder.pipe(T.Query())),
    direction: S.optional(ListAdsRequestDirection.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    stats_from: S.optional(S.String.pipe(T.Query())),
    stats_to: S.optional(S.String.pipe(T.Query())),
    time_zone: S.optional(S.String.pipe(T.Query())),
    attribution_model: S.optional(
      ListAdsRequestAttributionModel.pipe(T.Query()),
    ),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/ads", code: 200 })),
).annotate({ identifier: "ListAdsRequest" }) as any as S.Schema<ListAdsRequest>;

export type ListAdsResponseDataList = Array<Ad>;
export const ListAdsResponseDataList = /*@__PURE__*/ S.Array(
  Ad,
) as any as S.Schema<ListAdsResponseDataList>;

export interface ListAdsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListAdsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListAdsResponsePageInfo",
}) as any as S.Schema<ListAdsResponsePageInfo>;

export interface ListAdsResponse {
  data: ListAdsResponseDataList;
  page_info: ListAdsResponsePageInfo;
}
export const ListAdsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListAdsResponseDataList,
    page_info: ListAdsResponsePageInfo,
  }),
).annotate({
  identifier: "ListAdsResponse",
}) as any as S.Schema<ListAdsResponse>;

export interface PauseAdRequest {
  /** The ad ID. */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const PauseAdRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/ads/{id}/pause", code: 200 })),
).annotate({ identifier: "PauseAdRequest" }) as any as S.Schema<PauseAdRequest>;

export type RetrieveAdRequestAttributionModel = "last_touch" | "first_touch";
export const RetrieveAdRequestAttributionModel = /*@__PURE__*/ S.String;

export interface RetrieveAdRequest {
  /** The ad ID. */
  id: string;
  /** Start of the stats window. */
  stats_from?: string;
  /** End of the stats window. */
  stats_to?: string;
  /** IANA timezone the stats window is interpreted in. Defaults to UTC. */
  time_zone?: string;
  /** Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise. */
  attribution_model?: RetrieveAdRequestAttributionModel | (string & {});
}
export const RetrieveAdRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    stats_from: S.optional(S.String.pipe(T.Query())),
    stats_to: S.optional(S.String.pipe(T.Query())),
    time_zone: S.optional(S.String.pipe(T.Query())),
    attribution_model: S.optional(
      RetrieveAdRequestAttributionModel.pipe(T.Query()),
    ),
  }).pipe(T.Http({ method: "GET", uri: "/ads/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveAdRequest",
}) as any as S.Schema<RetrieveAdRequest>;

export interface UnpauseAdRequest {
  /** The ad ID. */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const UnpauseAdRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/ads/{id}/unpause", code: 200 })),
).annotate({
  identifier: "UnpauseAdRequest",
}) as any as S.Schema<UnpauseAdRequest>;

/** The call-to-action button shown on the ad. */
export type UpdateAdRequestCallToAction =
  | "apply_now"
  | "book_now"
  | "call_now"
  | "contact_us"
  | "download"
  | "get_directions"
  | "get_offer"
  | "get_quote"
  | "learn_more"
  | "listen_now"
  | "message_page"
  | "no_button"
  | "open_link"
  | "order_now"
  | "request_time"
  | "see_details"
  | "see_menu"
  | "send_updates"
  | "shop_now"
  | "sign_up"
  | "subscribe"
  | "watch_more";
export const UpdateAdRequestCallToAction = /*@__PURE__*/ S.String;

/** The saved crop window for this creative, in source image pixels. Omit it for the original asset or for a format that has not been cropped. */
export type UpdateAdRequestCreativesItemCrop = CreateAdRequestCreativesItemCrop;
export const UpdateAdRequestCreativesItemCrop =
  CreateAdRequestCreativesItemCrop;

export type UpdateAdRequestCreativesItemFormat =
  | "square"
  | "vertical"
  | "horizontal";
export const UpdateAdRequestCreativesItemFormat = /*@__PURE__*/ S.String;

export interface UpdateAdRequestCreativesItem {
  /** The saved crop window for this creative, in source image pixels. Omit it for the original asset or for a format that has not been cropped. */
  crop?: CreateAdRequestCreativesItemCrop;
  format?: UpdateAdRequestCreativesItemFormat | (string & {});
  /** Uploaded file ID, prefixed `file_`. */
  id?: string;
}
export const UpdateAdRequestCreativesItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    crop: S.optional(CreateAdRequestCreativesItemCrop),
    format: S.optional(UpdateAdRequestCreativesItemFormat),
    id: S.optional(S.String),
  }),
).annotate({
  identifier: "UpdateAdRequestCreativesItem",
}) as any as S.Schema<UpdateAdRequestCreativesItem>;

/** The ad's creative assets. Each entry is an uploaded file id with an optional format; omit format for the original asset. Replaces a live ad's creative on the platform. Two or more entries with no format replace it with a carousel (2-10 attachments), in order, sharing the ad's copy. */
export type UpdateAdRequestCreativesList = Array<UpdateAdRequestCreativesItem>;
export const UpdateAdRequestCreativesList = /*@__PURE__*/ S.Array(
  UpdateAdRequestCreativesItem,
) as any as S.Schema<UpdateAdRequestCreativesList>;

/** The description variants shown on the ad. */
export type UpdateAdRequestDescriptionsList = Array<string>;
export const UpdateAdRequestDescriptionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpdateAdRequestDescriptionsList>;

/** The headline variants shown on the ad. */
export type UpdateAdRequestHeadlinesList = Array<string>;
export const UpdateAdRequestHeadlinesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpdateAdRequestHeadlinesList>;

/** Optional completion screen shown after submission; url sets the follow-up website button. */
export type UpdateAdRequestLeadFormCompletion =
  CreateAdRequestLeadFormCompletion;
export const UpdateAdRequestLeadFormCompletion =
  CreateAdRequestLeadFormCompletion;

export type UpdateAdRequestLeadFormDisclaimerCheckboxesItem =
  CreateAdRequestLeadFormDisclaimerCheckboxesItem;
export const UpdateAdRequestLeadFormDisclaimerCheckboxesItem =
  CreateAdRequestLeadFormDisclaimerCheckboxesItem;

export type UpdateAdRequestLeadFormDisclaimerCheckboxesList =
  Array<CreateAdRequestLeadFormDisclaimerCheckboxesItem>;
export const UpdateAdRequestLeadFormDisclaimerCheckboxesList =
  /*@__PURE__*/ S.Array(
    CreateAdRequestLeadFormDisclaimerCheckboxesItem,
  ) as any as S.Schema<UpdateAdRequestLeadFormDisclaimerCheckboxesList>;

/** Optional custom consent disclaimer with checkboxes. */
export interface UpdateAdRequestLeadFormDisclaimer {
  body?: string;
  checkboxes?: UpdateAdRequestLeadFormDisclaimerCheckboxesList;
  title?: string;
}
export const UpdateAdRequestLeadFormDisclaimer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: S.optional(S.String),
    checkboxes: S.optional(UpdateAdRequestLeadFormDisclaimerCheckboxesList),
    title: S.optional(S.String),
  }),
).annotate({
  identifier: "UpdateAdRequestLeadFormDisclaimer",
}) as any as S.Schema<UpdateAdRequestLeadFormDisclaimer>;

/** more_volume (default) is quickest to submit; higher_intent adds a confirmation step. */
export type UpdateAdRequestLeadFormFormType = "more_volume" | "higher_intent";
export const UpdateAdRequestLeadFormFormType = /*@__PURE__*/ S.String;

/** Optional intro screen shown before the questions. */
export type UpdateAdRequestLeadFormIntro = CreateAdRequestLeadFormIntro;
export const UpdateAdRequestLeadFormIntro = CreateAdRequestLeadFormIntro;

/** Your privacy policy. url is required by the ad platform. */
export type UpdateAdRequestLeadFormPrivacyPolicy =
  CreateAdRequestLeadFormPrivacyPolicy;
export const UpdateAdRequestLeadFormPrivacyPolicy =
  CreateAdRequestLeadFormPrivacyPolicy;

export type UpdateAdRequestLeadFormQuestionsItemFormat =
  | "short_answer"
  | "multiple_choice"
  | "appointment";
export const UpdateAdRequestLeadFormQuestionsItemFormat =
  /*@__PURE__*/ S.String;

export type UpdateAdRequestLeadFormQuestionsItemOptionsItemLogicAction =
  | "go_to_question"
  | "submit_form"
  | "close_form";
export const UpdateAdRequestLeadFormQuestionsItemOptionsItemLogicAction =
  /*@__PURE__*/ S.String;

export interface UpdateAdRequestLeadFormQuestionsItemOptionsItemLogic {
  action?:
    | UpdateAdRequestLeadFormQuestionsItemOptionsItemLogicAction
    | (string & {});
  target_end_page_index?: number;
  target_question_index?: number;
}
export const UpdateAdRequestLeadFormQuestionsItemOptionsItemLogic =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      action: S.optional(
        UpdateAdRequestLeadFormQuestionsItemOptionsItemLogicAction,
      ),
      target_end_page_index: S.optional(S.Number),
      target_question_index: S.optional(S.Number),
    }),
  ).annotate({
    identifier: "UpdateAdRequestLeadFormQuestionsItemOptionsItemLogic",
  }) as any as S.Schema<UpdateAdRequestLeadFormQuestionsItemOptionsItemLogic>;

export interface UpdateAdRequestLeadFormQuestionsItemOptionsItem {
  key?: string;
  logic?: UpdateAdRequestLeadFormQuestionsItemOptionsItemLogic;
  value?: string;
}
export const UpdateAdRequestLeadFormQuestionsItemOptionsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      key: S.optional(S.String),
      logic: S.optional(UpdateAdRequestLeadFormQuestionsItemOptionsItemLogic),
      value: S.optional(S.String),
    }),
  ).annotate({
    identifier: "UpdateAdRequestLeadFormQuestionsItemOptionsItem",
  }) as any as S.Schema<UpdateAdRequestLeadFormQuestionsItemOptionsItem>;

export type UpdateAdRequestLeadFormQuestionsItemOptionsList =
  Array<UpdateAdRequestLeadFormQuestionsItemOptionsItem>;
export const UpdateAdRequestLeadFormQuestionsItemOptionsList =
  /*@__PURE__*/ S.Array(
    UpdateAdRequestLeadFormQuestionsItemOptionsItem,
  ) as any as S.Schema<UpdateAdRequestLeadFormQuestionsItemOptionsList>;

export type UpdateAdRequestLeadFormQuestionsItemType =
  | "email"
  | "phone"
  | "full_name"
  | "first_name"
  | "last_name"
  | "city"
  | "state"
  | "zip"
  | "country"
  | "street_address"
  | "job_title"
  | "company_name"
  | "work_email"
  | "work_phone_number"
  | "dob"
  | "gender"
  | "marital_status"
  | "relationship_status"
  | "military_status"
  | "date_time"
  | "custom";
export const UpdateAdRequestLeadFormQuestionsItemType = /*@__PURE__*/ S.String;

export interface UpdateAdRequestLeadFormQuestionsItem {
  format?: UpdateAdRequestLeadFormQuestionsItemFormat | (string & {});
  label?: string;
  options?: UpdateAdRequestLeadFormQuestionsItemOptionsList;
  type?: UpdateAdRequestLeadFormQuestionsItemType | (string & {});
}
export const UpdateAdRequestLeadFormQuestionsItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      format: S.optional(UpdateAdRequestLeadFormQuestionsItemFormat),
      label: S.optional(S.String),
      options: S.optional(UpdateAdRequestLeadFormQuestionsItemOptionsList),
      type: S.optional(UpdateAdRequestLeadFormQuestionsItemType),
    }),
).annotate({
  identifier: "UpdateAdRequestLeadFormQuestionsItem",
}) as any as S.Schema<UpdateAdRequestLeadFormQuestionsItem>;

/** The questions on the form. Standard prefill types need only a type; a custom question needs a label and a format (plus options for multiple_choice). Options carry an optional key and answer-routing logic. */
export type UpdateAdRequestLeadFormQuestionsList =
  Array<UpdateAdRequestLeadFormQuestionsItem>;
export const UpdateAdRequestLeadFormQuestionsList = /*@__PURE__*/ S.Array(
  UpdateAdRequestLeadFormQuestionsItem,
) as any as S.Schema<UpdateAdRequestLeadFormQuestionsList>;

/** Instant lead form for the ad. Only allowed when the ad group's conversion_location is an instant-form destination (instant_forms, instant_forms_and_messenger, website_and_instant_forms). Mutually exclusive with lead_form_id. */
export interface UpdateAdRequestLeadForm {
  /** Optional completion screen shown after submission; url sets the follow-up website button. */
  completion?: CreateAdRequestLeadFormCompletion;
  /** Optional custom consent disclaimer with checkboxes. */
  disclaimer?: UpdateAdRequestLeadFormDisclaimer;
  /** more_volume (default) is quickest to submit; higher_intent adds a confirmation step. */
  form_type?: UpdateAdRequestLeadFormFormType | (string & {});
  /** Optional intro screen shown before the questions. */
  intro?: CreateAdRequestLeadFormIntro;
  /** Internal name for the form. Auto-generated if omitted. */
  name?: string;
  /** Require SMS verification of the phone number (higher_intent forms). */
  phone_verification?: boolean;
  /** Your privacy policy. url is required by the ad platform. */
  privacy_policy?: CreateAdRequestLeadFormPrivacyPolicy;
  /** The questions on the form. Standard prefill types need only a type; a custom question needs a label and a format (plus options for multiple_choice). Options carry an optional key and answer-routing logic. */
  questions?: UpdateAdRequestLeadFormQuestionsList;
}
export const UpdateAdRequestLeadForm = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    completion: S.optional(CreateAdRequestLeadFormCompletion),
    disclaimer: S.optional(UpdateAdRequestLeadFormDisclaimer),
    form_type: S.optional(UpdateAdRequestLeadFormFormType),
    intro: S.optional(CreateAdRequestLeadFormIntro),
    name: S.optional(S.String),
    phone_verification: S.optional(S.Boolean),
    privacy_policy: S.optional(CreateAdRequestLeadFormPrivacyPolicy),
    questions: S.optional(UpdateAdRequestLeadFormQuestionsList),
  }),
).annotate({
  identifier: "UpdateAdRequestLeadForm",
}) as any as S.Schema<UpdateAdRequestLeadForm>;

/** Click-to-message welcome copy: the greeting (message) and the ice-breaker prompt (keyword). */
export type UpdateAdRequestMessagingConfig = CreateAdRequestMessagingConfig;
export const UpdateAdRequestMessagingConfig = CreateAdRequestMessagingConfig;

/** Identifies the network that owns `existing_post_id`. The source is inferred from the ID shape when omitted. */
export type UpdateAdRequestPostSource = "facebook" | "instagram";
export const UpdateAdRequestPostSource = /*@__PURE__*/ S.String;

/** The primary text variants shown in the ad body. */
export type UpdateAdRequestPrimaryTextsList = Array<string>;
export const UpdateAdRequestPrimaryTextsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpdateAdRequestPrimaryTextsList>;

export type UpdateAdRequestSocialAccountsItem =
  CreateAdRequestSocialAccountsItem;
export const UpdateAdRequestSocialAccountsItem =
  CreateAdRequestSocialAccountsItem;

/** The social accounts the ad runs under — a connected Facebook page and, optionally, an Instagram profile. */
export type UpdateAdRequestSocialAccountsList =
  Array<CreateAdRequestSocialAccountsItem>;
export const UpdateAdRequestSocialAccountsList = /*@__PURE__*/ S.Array(
  CreateAdRequestSocialAccountsItem,
) as any as S.Schema<UpdateAdRequestSocialAccountsList>;

export interface UpdateAdRequest {
  /** The ad ID. */
  id: string;
  /** The call-to-action button shown on the ad. */
  call_to_action?: UpdateAdRequestCallToAction | (string & {});
  /** The ad's creative assets. Each entry is an uploaded file id with an optional format; omit format for the original asset. Replaces a live ad's creative on the platform. Two or more entries with no format replace it with a carousel (2-10 attachments), in order, sharing the ad's copy. */
  creatives?: UpdateAdRequestCreativesList;
  /** The description variants shown on the ad. */
  descriptions?: UpdateAdRequestDescriptionsList;
  /** Promote a post you already published instead of uploading creatives — a Facebook post or Instagram media id. Mutually exclusive with creatives. Pair with post_source. */
  existing_post_id?: string;
  /** The headline variants shown on the ad. */
  headlines?: UpdateAdRequestHeadlinesList;
  /** Instant lead form for the ad. Only allowed when the ad group's conversion_location is an instant-form destination (instant_forms, instant_forms_and_messenger, website_and_instant_forms). Mutually exclusive with lead_form_id. */
  lead_form?: UpdateAdRequestLeadForm;
  /** Use an existing instant form instead of creating one — the form's platform ID, from a form already on the ad's Facebook page. Only allowed when the ad group's conversion_location is an instant-form destination. Mutually exclusive with lead_form. Replaces a stored lead_form. */
  lead_form_id?: string;
  /** Click-to-message welcome copy: the greeting (message) and the ice-breaker prompt (keyword). */
  messaging_config?: CreateAdRequestMessagingConfig;
  /** Whether the ad can appear alongside other advertisers' ads in the same unit. Defaults to true. */
  multi_advertiser_ads?: boolean;
  /** Identifies the network that owns `existing_post_id`. The source is inferred from the ID shape when omitted. */
  post_source?: UpdateAdRequestPostSource | (string & {});
  /** The primary text variants shown in the ad body. */
  primary_texts?: UpdateAdRequestPrimaryTextsList;
  /** The social accounts the ad runs under — a connected Facebook page and, optionally, an Instagram profile. */
  social_accounts?: UpdateAdRequestSocialAccountsList;
  /** The display name of the ad. */
  title?: string;
  /** The URL the ad links to. Query parameters are merged into url_parameters, so the stored URL is always bare. */
  url?: string;
  /** Query parameters to append to the destination URL, keyed by parameter name. Merged with any query string on `url`. Whop adds its own click-attribution parameters; those are reserved and rejected if you set them (utm_meta_ad_id, utm_meta_adset_id, utm_meta_campaign_id, utm_source, utm_placement, utm_medium, utm_content, utm_adset, utm_whop, wacid, wasid, waid, tw_source, tw_adid). */
  url_parameters?: unknown;
}
export const UpdateAdRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    call_to_action: S.optional(UpdateAdRequestCallToAction),
    creatives: S.optional(UpdateAdRequestCreativesList),
    descriptions: S.optional(UpdateAdRequestDescriptionsList),
    existing_post_id: S.optional(S.String),
    headlines: S.optional(UpdateAdRequestHeadlinesList),
    lead_form: S.optional(UpdateAdRequestLeadForm),
    lead_form_id: S.optional(S.String),
    messaging_config: S.optional(CreateAdRequestMessagingConfig),
    multi_advertiser_ads: S.optional(S.Boolean),
    post_source: S.optional(UpdateAdRequestPostSource),
    primary_texts: S.optional(UpdateAdRequestPrimaryTextsList),
    social_accounts: S.optional(UpdateAdRequestSocialAccountsList),
    title: S.optional(S.String),
    url: S.optional(S.String),
    url_parameters: S.optional(S.Unknown),
  }).pipe(T.Http({ method: "PATCH", uri: "/ads/{id}", code: 200 })),
).annotate({
  identifier: "UpdateAdRequest",
}) as any as S.Schema<UpdateAdRequest>;

export type CreateAdError = BadRequest | NotFound | Conflict | WhopOpError;
/** Create an Ad Creates an ad in an ad group. */
export const createAd: API.OperationMethod<
  CreateAdRequest,
  Ad,
  CreateAdError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAdRequest,
  output: Ad,
  errors: [BadRequest, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteAdError = NotFound | WhopOpError;
/** Delete an Ad Deletes an ad. */
export const deleteAd: API.OperationMethod<
  DeleteAdRequest,
  DeleteAdResponse,
  DeleteAdError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteAdRequest,
  output: DeleteAdResponse,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DuplicateAdError = BadRequest | NotFound | Conflict | WhopOpError;
/** Duplicate an Ad Copies the ad into its own ad group, or into target_ad_group_id (which must belong to the same account and be compatible with the ad). Copies keep the source ad's active/paused state. */
export const duplicateAd: API.OperationMethod<
  DuplicateAdRequest,
  DuplicateAdResponse,
  DuplicateAdError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DuplicateAdRequest,
  output: DuplicateAdResponse,
  errors: [BadRequest, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListAdsError = BadRequest | WhopOpError;
/** List Ads Lists the ads for an account, with stats over the requested window. */
export const listAds: API.PaginatedOperationMethod<
  ListAdsRequest,
  ListAdsResponse,
  ListAdsError,
  WhopOpContext,
  Ad
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListAdsRequest,
    output: ListAdsResponse,
    errors: [BadRequest],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type PauseAdError = Conflict | WhopOpError;
/** Pause an Ad Pauses an active ad. */
export const pauseAd: API.OperationMethod<
  PauseAdRequest,
  Ad,
  PauseAdError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PauseAdRequest,
  output: Ad,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveAdError = NotFound | WhopOpError;
/** Retrieve an Ad Retrieves a single ad with stats over the requested window. */
export const retrieveAd: API.OperationMethod<
  RetrieveAdRequest,
  Ad,
  RetrieveAdError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveAdRequest,
  output: Ad,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UnpauseAdError = Conflict | WhopOpError;
/** Unpause an Ad Resumes a paused ad. */
export const unpauseAd: API.OperationMethod<
  UnpauseAdRequest,
  Ad,
  UnpauseAdError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UnpauseAdRequest,
  output: Ad,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateAdError = WhopOpError;
/** Update an Ad Updates an ad's editable fields. */
export const updateAd: API.OperationMethod<
  UpdateAdRequest,
  Ad,
  UpdateAdError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateAdRequest,
  output: Ad,
  errors: [],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
