// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** Source chain name or chain ID. Defaults to the source token's chain when omitted. */
export type CreateSwapRequestFromChain = string | number;
export const CreateSwapRequestFromChain =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateSwapRequestFromChain>;

/** Destination chain name or chain ID. Defaults to the destination token's chain when omitted. */
export type CreateSwapRequestToChain = string | number;
export const CreateSwapRequestToChain =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateSwapRequestToChain>;

export interface CreateSwapRequest {
  /** Business or user account ID (biz_* / user_*). */
  account_id: string;
  /** Source token amount. Required for crypto swaps. For fiat pairs: the amount of from_token to convert at the mid-market rate; omit (along with to_amount) to repay the full negative to_token balance instead. */
  amount?: string | null;
  /** Source chain name or chain ID. Defaults to the source token's chain when omitted. */
  from_chain?: CreateSwapRequestFromChain | null;
  /** Source token contract address or ticker symbol, such as "USDT". */
  from_token: string;
  /** Maximum slippage tolerance in basis points. */
  slippage_bps?: number | null;
  /** Fiat pairs only: sizes a partial repayment of the negative to_token balance, denominated in to_token. Must not exceed the debt. Mutually exclusive with amount. */
  to_amount?: string | null;
  /** Destination chain name or chain ID. Defaults to the destination token's chain when omitted. */
  to_chain?: CreateSwapRequestToChain | null;
  /** Destination token contract address or ticker symbol, such as "XAUT". */
  to_token: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateSwapRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String,
    amount: S.optional(S.NullOr(S.String)),
    from_chain: S.optional(S.NullOr(CreateSwapRequestFromChain)),
    from_token: S.String,
    slippage_bps: S.optional(S.NullOr(S.Number)),
    to_amount: S.optional(S.NullOr(S.String)),
    to_chain: S.optional(S.NullOr(CreateSwapRequestToChain)),
    to_token: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/swaps", code: 200 })),
).annotate({
  identifier: "CreateSwapRequest",
}) as any as S.Schema<CreateSwapRequest>;

/** Fiat pairs only: the source currency. */
export interface CreateSwapResponseFromToken {
  symbol?: string;
}
export const CreateSwapResponseFromToken = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    symbol: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateSwapResponseFromToken",
}) as any as S.Schema<CreateSwapResponseFromToken>;

export type CreateSwapResponseObject = "swap";
export const CreateSwapResponseObject = /*@__PURE__*/ S.String;

/** Swap status. Crypto swaps start `queued`; fiat conversions return `complete`, or `working` while a stablecoin repayment settles. */
export type CreateSwapResponseStatus =
  | "queued"
  | "working"
  | "complete"
  | "failed";
export const CreateSwapResponseStatus = /*@__PURE__*/ S.String;

/** Fiat pairs only: the destination currency. */
export type CreateSwapResponseToToken = CreateSwapResponseFromToken;
export const CreateSwapResponseToToken = CreateSwapResponseFromToken;

export interface CreateSwapResponse {
  /** Account ID that owns the wallet used for the swap. */
  account_id: string;
  /** Fiat pairs only: amount of the source currency converted. Null while a stablecoin repayment is processing. */
  amount_in?: number | null;
  /** Fiat pairs only: amount credited in the destination currency. Null while a stablecoin repayment is processing. */
  amount_out?: number | null;
  /** Expected destination token amount. */
  amount_out_expected?: string;
  /** Minimum destination amount after slippage. */
  amount_out_min?: string;
  /** Fiat pairs only: the source currency. */
  from_token?: CreateSwapResponseFromToken | null;
  /** Swap ID. Poll `GET /swaps/:id` for status. */
  id?: string;
  object: CreateSwapResponseObject;
  /** Quoted exchange rate used to create the swap. */
  rate?: string;
  /** Swap status. Crypto swaps start `queued`; fiat conversions return `complete`, or `working` while a stablecoin repayment settles. */
  status: CreateSwapResponseStatus;
  /** Destination chain for the swap. */
  to_chain?: string;
  /** Fiat pairs only: the destination currency. */
  to_token?: CreateSwapResponseFromToken | null;
}
export const CreateSwapResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String,
    amount_in: S.optional(S.NullOr(S.Number)),
    amount_out: S.optional(S.NullOr(S.Number)),
    amount_out_expected: S.optional(S.String),
    amount_out_min: S.optional(S.String),
    from_token: S.optional(S.NullOr(CreateSwapResponseFromToken)),
    id: S.optional(S.String),
    object: CreateSwapResponseObject,
    rate: S.optional(S.String),
    status: CreateSwapResponseStatus,
    to_chain: S.optional(S.String),
    to_token: S.optional(S.NullOr(CreateSwapResponseFromToken)),
  }),
).annotate({
  identifier: "CreateSwapResponse",
}) as any as S.Schema<CreateSwapResponse>;

/** Source chain name or chain ID. Defaults to the source token's chain when omitted. */
export type CreateSwapQuoteRequestFromChain = string | number;
export const CreateSwapQuoteRequestFromChain =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateSwapQuoteRequestFromChain>;

/** Metadata to include with the quote response. */
export type CreateSwapQuoteRequestMetadataMap = {
  [key: string]: unknown | undefined;
};
export const CreateSwapQuoteRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateSwapQuoteRequestMetadataMap>;

/** Destination chain name or chain ID. Defaults to the destination token's chain when omitted. */
export type CreateSwapQuoteRequestToChain = string | number;
export const CreateSwapQuoteRequestToChain =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateSwapQuoteRequestToChain>;

export interface CreateSwapQuoteRequest {
  /** Source token amount. */
  amount: string;
  /** Source wallet address used for the quote. */
  from_address?: string | null;
  /** Source chain name or chain ID. Defaults to the source token's chain when omitted. */
  from_chain?: CreateSwapQuoteRequestFromChain | null;
  /** Source token contract address or ticker symbol, such as "USDT". */
  from_token: string;
  /** Metadata to include with the quote response. */
  metadata?: CreateSwapQuoteRequestMetadataMap;
  /** Maximum slippage tolerance in basis points. */
  slippage_bps?: number | null;
  /** Destination wallet address used for the quote. */
  to_address?: string | null;
  /** Destination chain name or chain ID. Defaults to the destination token's chain when omitted. */
  to_chain?: CreateSwapQuoteRequestToChain | null;
  /** Destination token contract address or ticker symbol, such as "XAUT". */
  to_token: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateSwapQuoteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.String,
    from_address: S.optional(S.NullOr(S.String)),
    from_chain: S.optional(S.NullOr(CreateSwapQuoteRequestFromChain)),
    from_token: S.String,
    metadata: S.optional(CreateSwapQuoteRequestMetadataMap),
    slippage_bps: S.optional(S.NullOr(S.Number)),
    to_address: S.optional(S.NullOr(S.String)),
    to_chain: S.optional(S.NullOr(CreateSwapQuoteRequestToChain)),
    to_token: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/swaps/quote", code: 200 })),
).annotate({
  identifier: "CreateSwapQuoteRequest",
}) as any as S.Schema<CreateSwapQuoteRequest>;

/** Resolved source token details. */
export type CreateSwapQuoteResponseFromTokenMap = {
  [key: string]: unknown | undefined;
};
export const CreateSwapQuoteResponseFromTokenMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateSwapQuoteResponseFromTokenMap>;

/** Metadata from the request. */
export type CreateSwapQuoteResponseMetadataMap = {
  [key: string]: unknown | undefined;
};
export const CreateSwapQuoteResponseMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateSwapQuoteResponseMetadataMap>;

export type CreateSwapQuoteResponseObject = "swap_quote";
export const CreateSwapQuoteResponseObject = /*@__PURE__*/ S.String;

/** Resolved destination token details. */
export type CreateSwapQuoteResponseToTokenMap = {
  [key: string]: unknown | undefined;
};
export const CreateSwapQuoteResponseToTokenMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateSwapQuoteResponseToTokenMap>;

export interface CreateSwapQuoteResponse {
  /** Source token amount used for the quote. */
  amount_in: string;
  /** Estimated destination token amount. */
  amount_out: string;
  /** Minimum destination amount after slippage. */
  amount_out_min?: string;
  /** Estimated bridge fee for cross-chain swaps. */
  bridge_fee?: string | null;
  /** Estimated time for the swap to complete. */
  estimated_duration_seconds?: number | null;
  /** Whop fee in basis points. */
  fee_bps: number;
  /** Source wallet address used for the quote. */
  from_address?: string | null;
  /** Resolved source token details. */
  from_token: CreateSwapQuoteResponseFromTokenMap;
  /** Metadata from the request. */
  metadata: CreateSwapQuoteResponseMetadataMap;
  object: CreateSwapQuoteResponseObject;
  /** Quoted exchange rate. */
  rate: string;
  /** Whether the source token needs approval before swapping. */
  requires_token_approval?: boolean | null;
  /** Destination wallet address used for the quote. */
  to_address?: string | null;
  /** Resolved destination token details. */
  to_token: CreateSwapQuoteResponseToTokenMap;
}
export const CreateSwapQuoteResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount_in: S.String,
    amount_out: S.String,
    amount_out_min: S.optional(S.String),
    bridge_fee: S.optional(S.NullOr(S.String)),
    estimated_duration_seconds: S.optional(S.NullOr(S.Number)),
    fee_bps: S.Number,
    from_address: S.optional(S.NullOr(S.String)),
    from_token: CreateSwapQuoteResponseFromTokenMap,
    metadata: CreateSwapQuoteResponseMetadataMap,
    object: CreateSwapQuoteResponseObject,
    rate: S.String,
    requires_token_approval: S.optional(S.NullOr(S.Boolean)),
    to_address: S.optional(S.NullOr(S.String)),
    to_token: CreateSwapQuoteResponseToTokenMap,
  }),
).annotate({
  identifier: "CreateSwapQuoteResponse",
}) as any as S.Schema<CreateSwapQuoteResponse>;

export interface ListSwapsRequest {
  /** Business or user account ID (biz_* / user_*). */
  account_id: string;
}
export const ListSwapsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String.pipe(T.Query()),
  }).pipe(T.Http({ method: "GET", uri: "/swaps", code: 200 })),
).annotate({
  identifier: "ListSwapsRequest",
}) as any as S.Schema<ListSwapsRequest>;

export type ListSwapsResponseDataItemObject = "swap";
export const ListSwapsResponseDataItemObject = /*@__PURE__*/ S.String;

/** Current swap status. `complete` and `failed` are terminal. */
export type ListSwapsResponseDataItemStatus =
  | "queued"
  | "working"
  | "complete"
  | "failed";
export const ListSwapsResponseDataItemStatus = /*@__PURE__*/ S.String;

/** On-chain transaction hashes produced by the swap. */
export type ListSwapsResponseDataItemTxHashesList = Array<string>;
export const ListSwapsResponseDataItemTxHashesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListSwapsResponseDataItemTxHashesList>;

export interface ListSwapsResponseDataItem {
  /** Account ID that owns the wallet used for the swap. */
  account_id: string;
  /** Latest error returned for a failed swap. */
  error?: string | null;
  /** Swap ID. */
  id: string;
  object: ListSwapsResponseDataItemObject;
  /** Current swap status. `complete` and `failed` are terminal. */
  status: ListSwapsResponseDataItemStatus;
  /** On-chain transaction hashes produced by the swap. */
  tx_hashes: ListSwapsResponseDataItemTxHashesList;
}
export const ListSwapsResponseDataItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String,
    error: S.optional(S.NullOr(S.String)),
    id: S.String,
    object: ListSwapsResponseDataItemObject,
    status: ListSwapsResponseDataItemStatus,
    tx_hashes: ListSwapsResponseDataItemTxHashesList,
  }),
).annotate({
  identifier: "ListSwapsResponseDataItem",
}) as any as S.Schema<ListSwapsResponseDataItem>;

/** Swaps returned for this account. */
export type ListSwapsResponseDataList = Array<ListSwapsResponseDataItem>;
export const ListSwapsResponseDataList = /*@__PURE__*/ S.Array(
  ListSwapsResponseDataItem,
) as any as S.Schema<ListSwapsResponseDataList>;

export interface ListSwapsResponse {
  /** Swaps returned for this account. */
  data: ListSwapsResponseDataList;
}
export const ListSwapsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListSwapsResponseDataList,
  }),
).annotate({
  identifier: "ListSwapsResponse",
}) as any as S.Schema<ListSwapsResponse>;

export interface RetrieveSwapRequest {
  /** Swap ID returned from POST /swaps. */
  id: string;
}
export const RetrieveSwapRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/swaps/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveSwapRequest",
}) as any as S.Schema<RetrieveSwapRequest>;

export type RetrieveSwapResponseObject = "swap";
export const RetrieveSwapResponseObject = /*@__PURE__*/ S.String;

/** Current swap status. `complete` and `failed` are terminal. */
export type RetrieveSwapResponseStatus =
  | "queued"
  | "working"
  | "complete"
  | "failed";
export const RetrieveSwapResponseStatus = /*@__PURE__*/ S.String;

/** On-chain transaction hashes produced by the swap. */
export type RetrieveSwapResponseTxHashesList = Array<string>;
export const RetrieveSwapResponseTxHashesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<RetrieveSwapResponseTxHashesList>;

export interface RetrieveSwapResponse {
  /** Account ID that owns the wallet used for the swap. */
  account_id: string;
  /** Latest error returned for a failed swap. */
  error?: string | null;
  /** Swap ID. */
  id: string;
  object: RetrieveSwapResponseObject;
  /** Current swap status. `complete` and `failed` are terminal. */
  status: RetrieveSwapResponseStatus;
  /** On-chain transaction hashes produced by the swap. */
  tx_hashes: RetrieveSwapResponseTxHashesList;
}
export const RetrieveSwapResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String,
    error: S.optional(S.NullOr(S.String)),
    id: S.String,
    object: RetrieveSwapResponseObject,
    status: RetrieveSwapResponseStatus,
    tx_hashes: RetrieveSwapResponseTxHashesList,
  }),
).annotate({
  identifier: "RetrieveSwapResponse",
}) as any as S.Schema<RetrieveSwapResponse>;

export type CreateSwapError = BadRequest | Forbidden | Conflict | WhopOpError;
/** Create Swap Swaps one token for another from the account's wallet, or converts between fiat currencies in the account's ledger at the mid-market rate. Crypto swaps finish in the background — check the swap for its status. */
export const createSwap: API.OperationMethod<
  CreateSwapRequest,
  CreateSwapResponse,
  CreateSwapError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSwapRequest,
  output: CreateSwapResponse,
  errors: [BadRequest, Forbidden, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreateSwapQuoteError = BadRequest | Conflict | WhopOpError;
/** Create Swap Quote Previews the price of a swap. Fiat pairs quote the in-ledger mid-market conversion — the same rate creating the swap fills at. No funds move and nothing is saved. */
export const createSwapQuote: API.OperationMethod<
  CreateSwapQuoteRequest,
  CreateSwapQuoteResponse,
  CreateSwapQuoteError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSwapQuoteRequest,
  output: CreateSwapQuoteResponse,
  errors: [BadRequest, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListSwapsError = Forbidden | WhopOpError;
/** List Swaps Retrieve the account's completed or pending swaps — currently just the latest one. */
export const listSwaps: API.OperationMethod<
  ListSwapsRequest,
  ListSwapsResponse,
  ListSwapsError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListSwapsRequest,
  output: ListSwapsResponse,
  errors: [Forbidden],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveSwapError = BadRequest | Forbidden | NotFound | WhopOpError;
/** Retrieve Swap Retrieves a single swap and its status. */
export const retrieveSwap: API.OperationMethod<
  RetrieveSwapRequest,
  RetrieveSwapResponse,
  RetrieveSwapError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveSwapRequest,
  output: RetrieveSwapResponse,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
