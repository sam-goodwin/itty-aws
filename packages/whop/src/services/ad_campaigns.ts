// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** How delivery bids in the ad auction: `minimum_cost` gets the most results for the budget, `average_target` holds an average cost per result, `maximum_target` never bids above a cap. Only for campaigns that own the budget. */
export type CreateAdCampaignRequestBidType =
  | "minimum_cost"
  | "average_target"
  | "maximum_target";
export const CreateAdCampaignRequestBidType = /*@__PURE__*/ S.String;

/** Which level owns the budget: the whole campaign (`ad_campaign`) or each ad group individually (`ad_group`). Defaults to `ad_group`. */
export type CreateAdCampaignRequestBudgetOptimization =
  | "ad_campaign"
  | "ad_group";
export const CreateAdCampaignRequestBudgetOptimization = /*@__PURE__*/ S.String;

/** Whether the budget is spent per day (`daily`) or over the campaign's full run (`lifetime`). Defaults to `daily`. */
export type CreateAdCampaignRequestBudgetType = "daily" | "lifetime";
export const CreateAdCampaignRequestBudgetType = /*@__PURE__*/ S.String;

/** The goal the campaign optimizes toward. */
export type CreateAdCampaignRequestObjective =
  | "awareness"
  | "traffic"
  | "engagement"
  | "leads"
  | "sales";
export const CreateAdCampaignRequestObjective = /*@__PURE__*/ S.String;

/** The ad network the campaign runs on. */
export type CreateAdCampaignRequestPlatform = "meta";
export const CreateAdCampaignRequestPlatform = /*@__PURE__*/ S.String;

export type CreateAdCampaignRequestSpecialAdCategoriesItem =
  | "housing"
  | "employment"
  | "financial_products"
  | "politics";
export const CreateAdCampaignRequestSpecialAdCategoriesItem =
  /*@__PURE__*/ S.String;

/** Regulated categories the campaign falls under. Ads in these categories are subject to extra targeting restrictions. */
export type CreateAdCampaignRequestSpecialAdCategoriesList = Array<
  CreateAdCampaignRequestSpecialAdCategoriesItem | (string & {})
>;
export const CreateAdCampaignRequestSpecialAdCategoriesList =
  /*@__PURE__*/ S.Array(
    CreateAdCampaignRequestSpecialAdCategoriesItem,
  ) as any as S.Schema<CreateAdCampaignRequestSpecialAdCategoriesList>;

export interface CreateAdCampaignRequest {
  /** The account to create the campaign under. Defaults to the account-scoped key's own account. */
  account_id?: string;
  /** How delivery bids in the ad auction: `minimum_cost` gets the most results for the budget, `average_target` holds an average cost per result, `maximum_target` never bids above a cap. Only for campaigns that own the budget. */
  bid_type?: CreateAdCampaignRequestBidType | (string & {});
  /** The campaign's budget, in the ad account's currency. Required when budget_optimization is `ad_campaign`; omit when each ad group sets its own budget. */
  budget_amount?: number;
  /** Which level owns the budget: the whole campaign (`ad_campaign`) or each ad group individually (`ad_group`). Defaults to `ad_group`. */
  budget_optimization?:
    | CreateAdCampaignRequestBudgetOptimization
    | (string & {});
  /** Whether the budget is spent per day (`daily`) or over the campaign's full run (`lifetime`). Defaults to `daily`. */
  budget_type?: CreateAdCampaignRequestBudgetType | (string & {});
  /** Cost per result to aim for (`average_target`) or never exceed (`maximum_target`). Only for campaigns that own the budget. */
  desired_cost_per_result?: number;
  /** When the campaign stops delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget. */
  ends_at?: string;
  /** The goal the campaign optimizes toward. */
  objective: CreateAdCampaignRequestObjective | (string & {});
  /** The ad network the campaign runs on. */
  platform: CreateAdCampaignRequestPlatform | (string & {});
  /** Regulated categories the campaign falls under. Ads in these categories are subject to extra targeting restrictions. */
  special_ad_categories?: CreateAdCampaignRequestSpecialAdCategoriesList;
  /** When the campaign starts delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget. */
  starts_at?: string;
  /** The title of the campaign. */
  title: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateAdCampaignRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    bid_type: S.optional(CreateAdCampaignRequestBidType),
    budget_amount: S.optional(S.Number),
    budget_optimization: S.optional(CreateAdCampaignRequestBudgetOptimization),
    budget_type: S.optional(CreateAdCampaignRequestBudgetType),
    desired_cost_per_result: S.optional(S.Number),
    ends_at: S.optional(S.String),
    objective: CreateAdCampaignRequestObjective,
    platform: CreateAdCampaignRequestPlatform,
    special_ad_categories: S.optional(
      CreateAdCampaignRequestSpecialAdCategoriesList,
    ),
    starts_at: S.optional(S.String),
    title: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/ad_campaigns", code: 200 })),
).annotate({
  identifier: "CreateAdCampaignRequest",
}) as any as S.Schema<CreateAdCampaignRequest>;

/** How delivery bids in the ad auction: `minimum_cost` gets the most results for the budget, `average_target` holds an average cost per result, and `maximum_target` never bids above a cap. */
export type AdCampaignBidType =
  | "minimum_cost"
  | "average_target"
  | "maximum_target";
export const AdCampaignBidType = /*@__PURE__*/ S.String;

/** Which level owns the budget: the whole campaign (`ad_campaign`) or each ad group individually (`ad_group`). */
export type AdCampaignBudgetOptimization = "ad_campaign" | "ad_group";
export const AdCampaignBudgetOptimization = /*@__PURE__*/ S.String;

/** Whether `budget_amount` is spent per day (`daily`) or over the campaign's full run (`lifetime`). */
export type AdCampaignBudgetType = "daily" | "lifetime";
export const AdCampaignBudgetType = /*@__PURE__*/ S.String;

/** Whether the campaign's ads are delivering right now, and if not, why. When several states apply at once, the highest-precedence one is returned. */
export type AdCampaignDeliveryStatus =
  | "payment_failed"
  | "all_ads_rejected"
  | "draft"
  | "no_ad_groups"
  | "no_ads"
  | "paused"
  | "processing"
  | "issues"
  | "scheduled"
  | "completed"
  | "ad_groups_off"
  | "active";
export const AdCampaignDeliveryStatus = /*@__PURE__*/ S.String;

/** The type of resource the issue is attached to. */
export type AdPlatformIssueResourceType = "ad_campaign" | "ad_group" | "ad";
export const AdPlatformIssueResourceType = /*@__PURE__*/ S.String;

export interface AdPlatformIssue {
  /** Unique identifier for the issue. */
  id: string;
  /** A description of what the issue is and how it can be resolved. */
  message: string;
  /** The ID of the campaign, ad group, or ad the issue is attached to. */
  resource_id: string | null;
  /** The type of resource the issue is attached to. */
  resource_type: AdPlatformIssueResourceType;
}
export const AdPlatformIssue = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    message: S.String,
    resource_id: S.NullOr(S.String),
    resource_type: AdPlatformIssueResourceType,
  }),
).annotate({
  identifier: "AdPlatformIssue",
}) as any as S.Schema<AdPlatformIssue>;

export type AdCampaignIssuesList = Array<AdPlatformIssue>;
export const AdCampaignIssuesList = /*@__PURE__*/ S.Array(
  AdPlatformIssue,
) as any as S.Schema<AdCampaignIssuesList>;

/** The goal the campaign optimizes toward. */
export type AdCampaignObjective =
  | "awareness"
  | "traffic"
  | "engagement"
  | "leads"
  | "sales";
export const AdCampaignObjective = /*@__PURE__*/ S.String;

/** The ad network the campaign runs on. */
export type AdCampaignPlatform = "meta" | "tiktok";
export const AdCampaignPlatform = /*@__PURE__*/ S.String;

/** The Whop pixel conversion event whose attributed count represents results — the optimization goal, or the highest-volume attributed event for campaigns that budget per ad group. Null when the goal isn't a Whop-attributed event. */
export type AdCampaignResultEvent =
  | "purchase"
  | "lead"
  | "schedule"
  | "submit_application"
  | "contact"
  | "complete_registration"
  | "view_content"
  | "add_to_cart"
  | "custom"
  | "messaging_conversation";
export const AdCampaignResultEvent = /*@__PURE__*/ S.String;

/** Regulated categories the campaign is declared under. Ads in these categories are subject to extra targeting restrictions. Empty when none apply. */
export type AdCampaignSpecialAdCategoriesItem =
  | "housing"
  | "employment"
  | "financial_products"
  | "politics";
export const AdCampaignSpecialAdCategoriesItem = /*@__PURE__*/ S.String;

export type AdCampaignSpecialAdCategoriesList =
  Array<AdCampaignSpecialAdCategoriesItem>;
export const AdCampaignSpecialAdCategoriesList = /*@__PURE__*/ S.Array(
  AdCampaignSpecialAdCategoriesItem,
) as any as S.Schema<AdCampaignSpecialAdCategoriesList>;

/** The lifecycle status of the ad campaign. */
export type AdCampaignStatus =
  | "active"
  | "paused"
  | "inactive"
  | "stale"
  | "pending_refund"
  | "payment_failed"
  | "draft"
  | "in_review"
  | "flagged"
  | "importing"
  | "imported"
  | "duplicating";
export const AdCampaignStatus = /*@__PURE__*/ S.String;

export interface AdCampaign {
  /** USD value attributed to add-to-cart events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  added_to_cart_value: number;
  /** Whop pixel-attributed add-to-cart events, last-click. */
  added_to_carts: number;
  /** How delivery bids in the ad auction: `minimum_cost` gets the most results for the budget, `average_target` holds an average cost per result, and `maximum_target` never bids above a cap. */
  bid_type?: AdCampaignBidType | null;
  /** The campaign's budget, in the ad account's currency. `null` when each ad group sets its own budget instead. */
  budget_amount: number | null;
  /** Which level owns the budget: the whole campaign (`ad_campaign`) or each ad group individually (`ad_group`). */
  budget_optimization: AdCampaignBudgetOptimization | null;
  /** Whether `budget_amount` is spent per day (`daily`) or over the campaign's full run (`lifetime`). */
  budget_type: AdCampaignBudgetType | null;
  /** Clicks divided by impressions, between 0 and 1. */
  click_through_rate: number;
  /** The number of clicks. */
  clicks: number;
  /** USD value attributed to complete-registration events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  completed_registration_value: number;
  /** Whop pixel-attributed complete-registration events, last-click. */
  completed_registrations: number;
  /** USD value attributed to contact events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  contact_value: number;
  /** Whop pixel-attributed contact events, last-click. */
  contacts: number;
  /** Spend divided by attributed add-to-cart events; null when they are not the goal and none are attributed. */
  cost_per_added_to_cart: number | null;
  /** Spend divided by clicks; 0 when there are no clicks. */
  cost_per_click: number;
  /** Spend divided by attributed complete-registration events; null when they are not the goal and none are attributed. */
  cost_per_completed_registration: number | null;
  /** Spend divided by attributed contact events; null when contacts are not the goal and none are attributed. */
  cost_per_contact: number | null;
  /** Spend divided by attributed leads; null when leads are not a goal and none are attributed. */
  cost_per_lead: number | null;
  /** Spend per 1,000 impressions; 0 when there are no impressions. */
  cost_per_mille: number;
  /** Spend divided by attributed purchases; null when purchases are not a goal and none are attributed. */
  cost_per_purchase: number | null;
  /** Spend divided by Whop pixel-attributed results; null when nothing Whop-attributable is being optimized for. */
  cost_per_result: number | null;
  /** Spend divided by attributed schedule events; null when schedules are not the goal and none are attributed. */
  cost_per_schedule: number | null;
  /** Spend divided by attributed submit-application events; null when they are not the goal and none are attributed. */
  cost_per_submitted_application: number | null;
  /** Spend divided by unique clicks; null when there are no unique clicks. */
  cost_per_unique_click: number | null;
  /** Spend divided by attributed view-content events; null when they are not the goal and none are attributed. */
  cost_per_viewed_content: number | null;
  /** When the campaign was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Whop pixel-attributed custom (merchant-defined) conversion events, last-click, across all custom event names. */
  custom_conversions: number;
  /** Whop pixel-attributed custom conversions, keyed by your event name with its last-click count as the value. Empty when no named custom events are attributed. Custom events fired without a name are counted in custom_conversions but omitted here, so these values sum to at most custom_conversions. */
  custom_event_counts: unknown;
  /** Conversion value attributed to each custom event, keyed by event name like custom_event_counts. Sums the value passed to whop.track, normalized to USD; events fired without a value contribute 0. */
  custom_event_values: unknown;
  /** Whether the campaign's ads are delivering right now, and if not, why. When several states apply at once, the highest-precedence one is returned. */
  delivery_status: AdCampaignDeliveryStatus;
  /** Platform-reported impressions divided by reach. */
  frequency: number | null;
  /** Unique identifier for the ad campaign, prefixed `adcamp_`. */
  id: string;
  /** The number of impressions. */
  impressions: number;
  issues: AdCampaignIssuesList;
  /** USD value attributed to lead events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  lead_value: number;
  /** Whop pixel-attributed leads, last-click. */
  leads: number;
  /** The goal the campaign optimizes toward. */
  objective: AdCampaignObjective | null;
  /** The event the campaign optimizes for when a single goal is set campaign-wide. `null` when each ad group sets its own optimization_goal. */
  optimization_goal: string | null;
  /** The ad network the campaign runs on. */
  platform: AdCampaignPlatform;
  /** USD value of pixel-attributed purchases. */
  purchase_value: number;
  /** Whop pixel-attributed purchases, last-click. */
  purchases: number;
  /** The number of unique people who saw this. */
  reach: number;
  /** The Whop pixel conversion event whose attributed count represents results — the optimization goal, or the highest-volume attributed event for campaigns that budget per ad group. Null when the goal isn't a Whop-attributed event. */
  result_event: AdCampaignResultEvent | null;
  /** The merchant-defined event name when result_event is custom; null for the standard events. */
  result_event_name: string | null;
  /** The Whop pixel-attributed count behind result_event. When a campaign's ad groups optimize different goals there is no single result_event (it is null), and this is instead the sum of each ad group's own attributed results. Null when nothing Whop-attributable is being optimized for. */
  results: number | null;
  /** Purchase value divided by spend, both in USD (a currency-neutral ratio); 0 when there is no spend. */
  return_on_ad_spend: number;
  /** USD value attributed to schedule events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  schedule_value: number;
  /** Whop pixel-attributed schedule events, last-click. */
  schedules: number;
  special_ad_categories: AdCampaignSpecialAdCategoriesList;
  /** The amount charged, in spend_currency. */
  spend: number;
  /** The ISO 4217 currency code of all monetary metrics. */
  spend_currency: string | null;
  /** The lifecycle status of the ad campaign. */
  status: AdCampaignStatus;
  /** USD value attributed to submit-application events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  submitted_application_value: number;
  /** Whop pixel-attributed submit-application events, last-click. */
  submitted_applications: number;
  /** Display name of the ad campaign. */
  title: string;
  /** Unique clicks divided by impressions, between 0 and 1. */
  unique_click_through_rate: number | null;
  /** People who clicked, reported by the Whop pixel, counted once per person. */
  unique_clicks: number;
  /** When the campaign was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** USD value attributed to view-content events. Sums the value sent with each event, normalized to USD; events without a value contribute 0. */
  viewed_content_value: number;
  /** Whop pixel-attributed view-content events, last-click. */
  viewed_contents: number;
}
export const AdCampaign = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    added_to_cart_value: S.Number,
    added_to_carts: S.Number,
    bid_type: S.optional(S.NullOr(AdCampaignBidType)),
    budget_amount: S.NullOr(S.Number),
    budget_optimization: S.NullOr(AdCampaignBudgetOptimization),
    budget_type: S.NullOr(AdCampaignBudgetType),
    click_through_rate: S.Number,
    clicks: S.Number,
    completed_registration_value: S.Number,
    completed_registrations: S.Number,
    contact_value: S.Number,
    contacts: S.Number,
    cost_per_added_to_cart: S.NullOr(S.Number),
    cost_per_click: S.Number,
    cost_per_completed_registration: S.NullOr(S.Number),
    cost_per_contact: S.NullOr(S.Number),
    cost_per_lead: S.NullOr(S.Number),
    cost_per_mille: S.Number,
    cost_per_purchase: S.NullOr(S.Number),
    cost_per_result: S.NullOr(S.Number),
    cost_per_schedule: S.NullOr(S.Number),
    cost_per_submitted_application: S.NullOr(S.Number),
    cost_per_unique_click: S.NullOr(S.Number),
    cost_per_viewed_content: S.NullOr(S.Number),
    created_at: S.String,
    custom_conversions: S.Number,
    custom_event_counts: S.Unknown,
    custom_event_values: S.Unknown,
    delivery_status: AdCampaignDeliveryStatus,
    frequency: S.NullOr(S.Number),
    id: S.String,
    impressions: S.Number,
    issues: AdCampaignIssuesList,
    lead_value: S.Number,
    leads: S.Number,
    objective: S.NullOr(AdCampaignObjective),
    optimization_goal: S.NullOr(S.String),
    platform: AdCampaignPlatform,
    purchase_value: S.Number,
    purchases: S.Number,
    reach: S.Number,
    result_event: S.NullOr(AdCampaignResultEvent),
    result_event_name: S.NullOr(S.String),
    results: S.NullOr(S.Number),
    return_on_ad_spend: S.Number,
    schedule_value: S.Number,
    schedules: S.Number,
    special_ad_categories: AdCampaignSpecialAdCategoriesList,
    spend: S.Number,
    spend_currency: S.NullOr(S.String),
    status: AdCampaignStatus,
    submitted_application_value: S.Number,
    submitted_applications: S.Number,
    title: S.String,
    unique_click_through_rate: S.NullOr(S.Number),
    unique_clicks: S.Number,
    updated_at: S.String,
    viewed_content_value: S.Number,
    viewed_contents: S.Number,
  }),
).annotate({ identifier: "AdCampaign" }) as any as S.Schema<AdCampaign>;

export interface DeleteAdCampaignRequest {
  /** The ad campaign ID. */
  id: string;
}
export const DeleteAdCampaignRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/ad_campaigns/{id}", code: 200 })),
).annotate({
  identifier: "DeleteAdCampaignRequest",
}) as any as S.Schema<DeleteAdCampaignRequest>;

export interface DeleteAdCampaignResponse {
  /** Always true. */
  deleted: boolean;
  /** ID of the deleted ad campaign. */
  id: string;
}
export const DeleteAdCampaignResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "DeleteAdCampaignResponse",
}) as any as S.Schema<DeleteAdCampaignResponse>;

export interface DuplicateAdCampaignRequest {
  /** The ad campaign ID. */
  id: string;
  /** Number of copies to create (1-10). Defaults to 1. */
  count?: number;
  /** Whether the copied ads keep the original posts' engagement (likes, comments, shares). Defaults to false. */
  preserve_engagement?: boolean;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const DuplicateAdCampaignRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    count: S.optional(S.Number),
    preserve_engagement: S.optional(S.Boolean),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/ad_campaigns/{id}/duplicate", code: 200 }),
  ),
).annotate({
  identifier: "DuplicateAdCampaignRequest",
}) as any as S.Schema<DuplicateAdCampaignRequest>;

export type DuplicateAdCampaignResponseDataList = Array<AdCampaign>;
export const DuplicateAdCampaignResponseDataList = /*@__PURE__*/ S.Array(
  AdCampaign,
) as any as S.Schema<DuplicateAdCampaignResponseDataList>;

export interface DuplicateAdCampaignResponse {
  data: DuplicateAdCampaignResponseDataList;
}
export const DuplicateAdCampaignResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: DuplicateAdCampaignResponseDataList,
  }),
).annotate({
  identifier: "DuplicateAdCampaignResponse",
}) as any as S.Schema<DuplicateAdCampaignResponse>;

export type ListAdCampaignsRequestStatus =
  | "draft"
  | "active"
  | "paused"
  | "payment_failed";
export const ListAdCampaignsRequestStatus = /*@__PURE__*/ S.String;

export type ListAdCampaignsRequestOrder =
  | "created_at"
  | "updated_at"
  | "spend"
  | "impressions"
  | "reach"
  | "clicks"
  | "unique_clicks"
  | "frequency"
  | "click_through_rate"
  | "results"
  | "cost_per_mille"
  | "cost_per_click"
  | "cost_per_result"
  | "return_on_ad_spend";
export const ListAdCampaignsRequestOrder = /*@__PURE__*/ S.String;

export type ListAdCampaignsRequestDirection = "asc" | "desc";
export const ListAdCampaignsRequestDirection = /*@__PURE__*/ S.String;

export type ListAdCampaignsRequestAttributionModel =
  | "last_touch"
  | "first_touch";
export const ListAdCampaignsRequestAttributionModel = /*@__PURE__*/ S.String;

export interface ListAdCampaignsRequest {
  /** The account the campaigns belong to. Defaults to the account-scoped key's own account. */
  account_id?: string;
  /** Only return campaigns with this status. */
  status?: ListAdCampaignsRequestStatus | (string & {});
  /** Filter campaigns by a title or ID substring. */
  query?: string;
  /** The field to sort by. Defaults to created_at. Stat columns (spend, impressions, …) rank over the stats_from/stats_to window across the whole list, not just the current page. results, cost_per_result and return_on_ad_spend rank by the same Whop pixel-attributed values the response reports. */
  order?: ListAdCampaignsRequestOrder | (string & {});
  /** The sort direction. Defaults to desc. */
  direction?: ListAdCampaignsRequestDirection | (string & {});
  /** Only return campaigns created before this timestamp. */
  created_before?: string;
  /** Only return campaigns created after this timestamp. */
  created_after?: string;
  /** Start of the stats window. Defaults to all-time. */
  stats_from?: string;
  /** End of the stats window. Defaults to now. */
  stats_to?: string;
  /** IANA timezone (e.g. America/New_York) the stats window is interpreted in. Bare stats_from/stats_to dates resolve to day boundaries on this clock. Defaults to UTC. */
  time_zone?: string;
  /** Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise. */
  attribution_model?: ListAdCampaignsRequestAttributionModel | (string & {});
  /** The number of campaigns to return. */
  first?: number;
  /** Cursor to fetch the page after (from page_info.end_cursor). */
  after?: string;
  /** The number of campaigns to return from the end of the range. */
  last?: number;
  /** Cursor to fetch the page before (from page_info.start_cursor). */
  before?: string;
}
export const ListAdCampaignsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    status: S.optional(ListAdCampaignsRequestStatus.pipe(T.Query())),
    query: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListAdCampaignsRequestOrder.pipe(T.Query())),
    direction: S.optional(ListAdCampaignsRequestDirection.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    stats_from: S.optional(S.String.pipe(T.Query())),
    stats_to: S.optional(S.String.pipe(T.Query())),
    time_zone: S.optional(S.String.pipe(T.Query())),
    attribution_model: S.optional(
      ListAdCampaignsRequestAttributionModel.pipe(T.Query()),
    ),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/ad_campaigns", code: 200 })),
).annotate({
  identifier: "ListAdCampaignsRequest",
}) as any as S.Schema<ListAdCampaignsRequest>;

export type ListAdCampaignsResponseDataList = Array<AdCampaign>;
export const ListAdCampaignsResponseDataList = /*@__PURE__*/ S.Array(
  AdCampaign,
) as any as S.Schema<ListAdCampaignsResponseDataList>;

export interface ListAdCampaignsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListAdCampaignsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListAdCampaignsResponsePageInfo",
}) as any as S.Schema<ListAdCampaignsResponsePageInfo>;

export interface ListAdCampaignsResponse {
  data: ListAdCampaignsResponseDataList;
  page_info: ListAdCampaignsResponsePageInfo;
}
export const ListAdCampaignsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListAdCampaignsResponseDataList,
    page_info: ListAdCampaignsResponsePageInfo,
  }),
).annotate({
  identifier: "ListAdCampaignsResponse",
}) as any as S.Schema<ListAdCampaignsResponse>;

export interface PauseAdCampaignRequest {
  /** The ad campaign ID. */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const PauseAdCampaignRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/ad_campaigns/{id}/pause", code: 200 }),
  ),
).annotate({
  identifier: "PauseAdCampaignRequest",
}) as any as S.Schema<PauseAdCampaignRequest>;

export type RetrieveAdCampaignRequestAttributionModel =
  | "last_touch"
  | "first_touch";
export const RetrieveAdCampaignRequestAttributionModel = /*@__PURE__*/ S.String;

export interface RetrieveAdCampaignRequest {
  /** The ad campaign ID. */
  id: string;
  /** Start of the stats window. */
  stats_from?: string;
  /** End of the stats window. */
  stats_to?: string;
  /** IANA timezone the stats window is interpreted in. Defaults to UTC. */
  time_zone?: string;
  /** Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise. */
  attribution_model?: RetrieveAdCampaignRequestAttributionModel | (string & {});
}
export const RetrieveAdCampaignRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    stats_from: S.optional(S.String.pipe(T.Query())),
    stats_to: S.optional(S.String.pipe(T.Query())),
    time_zone: S.optional(S.String.pipe(T.Query())),
    attribution_model: S.optional(
      RetrieveAdCampaignRequestAttributionModel.pipe(T.Query()),
    ),
  }).pipe(T.Http({ method: "GET", uri: "/ad_campaigns/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveAdCampaignRequest",
}) as any as S.Schema<RetrieveAdCampaignRequest>;

export interface RetryAdCampaignPaymentRequest {
  /** The ad campaign ID. */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const RetryAdCampaignPaymentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/ad_campaigns/{id}/retry_payment",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RetryAdCampaignPaymentRequest",
}) as any as S.Schema<RetryAdCampaignPaymentRequest>;

export interface UnpauseAdCampaignRequest {
  /** The ad campaign ID. */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const UnpauseAdCampaignRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/ad_campaigns/{id}/unpause", code: 200 }),
  ),
).annotate({
  identifier: "UnpauseAdCampaignRequest",
}) as any as S.Schema<UnpauseAdCampaignRequest>;

/** How delivery bids in the ad auction: `minimum_cost` gets the most results for the budget, `average_target` holds an average cost per result, `maximum_target` never bids above a cap. Switching to `minimum_cost` clears the cap amounts stored on the campaign's ad groups. Only for campaigns that own the budget. */
export type UpdateAdCampaignRequestBidType =
  | "minimum_cost"
  | "average_target"
  | "maximum_target";
export const UpdateAdCampaignRequestBidType = /*@__PURE__*/ S.String;

/** Which level owns the budget: the whole campaign (`ad_campaign`) or each ad group individually (`ad_group`). Only changeable before the campaign is live on the ad network; switching to `ad_campaign` requires budget_amount in the same request, and switching to `ad_group` clears the campaign budget. */
export type UpdateAdCampaignRequestBudgetOptimization =
  | "ad_campaign"
  | "ad_group";
export const UpdateAdCampaignRequestBudgetOptimization = /*@__PURE__*/ S.String;

/** Whether `budget_amount` is spent per day (`daily`) or over the campaign's full run (`lifetime`). Only changeable while the campaign is a draft; send budget_amount in the same request so the amount lands on the new type. */
export type UpdateAdCampaignRequestBudgetType = "daily" | "lifetime";
export const UpdateAdCampaignRequestBudgetType = /*@__PURE__*/ S.String;

export type UpdateAdCampaignRequestSpecialAdCategoriesItem =
  | "housing"
  | "employment"
  | "financial_products"
  | "politics";
export const UpdateAdCampaignRequestSpecialAdCategoriesItem =
  /*@__PURE__*/ S.String;

/** Regulated categories the campaign falls under. Editable on any campaign, draft or launched; pass an empty array to clear. */
export type UpdateAdCampaignRequestSpecialAdCategoriesList = Array<
  UpdateAdCampaignRequestSpecialAdCategoriesItem | (string & {})
>;
export const UpdateAdCampaignRequestSpecialAdCategoriesList =
  /*@__PURE__*/ S.Array(
    UpdateAdCampaignRequestSpecialAdCategoriesItem,
  ) as any as S.Schema<UpdateAdCampaignRequestSpecialAdCategoriesList>;

/** Set to active to launch a draft campaign (moderates and pushes it live). Live-campaign pause and resume use the pause and unpause actions. */
export type UpdateAdCampaignRequestStatus = "active";
export const UpdateAdCampaignRequestStatus = /*@__PURE__*/ S.String;

export interface UpdateAdCampaignRequest {
  /** The ad campaign ID. */
  id: string;
  /** How delivery bids in the ad auction: `minimum_cost` gets the most results for the budget, `average_target` holds an average cost per result, `maximum_target` never bids above a cap. Switching to `minimum_cost` clears the cap amounts stored on the campaign's ad groups. Only for campaigns that own the budget. */
  bid_type?: UpdateAdCampaignRequestBidType | (string & {});
  /** The campaign budget, in the account's currency. Interpreted as daily or lifetime per the campaign's budget type, including a budget_type sent in the same request. */
  budget_amount?: number;
  /** Which level owns the budget: the whole campaign (`ad_campaign`) or each ad group individually (`ad_group`). Only changeable before the campaign is live on the ad network; switching to `ad_campaign` requires budget_amount in the same request, and switching to `ad_group` clears the campaign budget. */
  budget_optimization?:
    | UpdateAdCampaignRequestBudgetOptimization
    | (string & {});
  /** Whether `budget_amount` is spent per day (`daily`) or over the campaign's full run (`lifetime`). Only changeable while the campaign is a draft; send budget_amount in the same request so the amount lands on the new type. */
  budget_type?: UpdateAdCampaignRequestBudgetType | (string & {});
  /** When the campaign stops delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget. */
  ends_at?: string;
  /** Regulated categories the campaign falls under. Editable on any campaign, draft or launched; pass an empty array to clear. */
  special_ad_categories?: UpdateAdCampaignRequestSpecialAdCategoriesList;
  /** When the campaign starts delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget. */
  starts_at?: string;
  /** Set to active to launch a draft campaign (moderates and pushes it live). Live-campaign pause and resume use the pause and unpause actions. */
  status?: UpdateAdCampaignRequestStatus | (string & {});
  /** The name of the campaign. */
  title?: string;
}
export const UpdateAdCampaignRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    bid_type: S.optional(UpdateAdCampaignRequestBidType),
    budget_amount: S.optional(S.Number),
    budget_optimization: S.optional(UpdateAdCampaignRequestBudgetOptimization),
    budget_type: S.optional(UpdateAdCampaignRequestBudgetType),
    ends_at: S.optional(S.String),
    special_ad_categories: S.optional(
      UpdateAdCampaignRequestSpecialAdCategoriesList,
    ),
    starts_at: S.optional(S.String),
    status: S.optional(UpdateAdCampaignRequestStatus),
    title: S.optional(S.String),
  }).pipe(T.Http({ method: "PATCH", uri: "/ad_campaigns/{id}", code: 200 })),
).annotate({
  identifier: "UpdateAdCampaignRequest",
}) as any as S.Schema<UpdateAdCampaignRequest>;

export type CreateAdCampaignError = BadRequest | Conflict | WhopOpError;
/** Create an Ad Campaign Creates an ad campaign for an account. */
export const createAdCampaign: API.OperationMethod<
  CreateAdCampaignRequest,
  AdCampaign,
  CreateAdCampaignError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAdCampaignRequest,
  output: AdCampaign,
  errors: [BadRequest, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteAdCampaignError = NotFound | WhopOpError;
/** Delete an Ad Campaign Deletes an ad campaign and archives it on the ad platform (cascades to ad groups and ads). */
export const deleteAdCampaign: API.OperationMethod<
  DeleteAdCampaignRequest,
  DeleteAdCampaignResponse,
  DeleteAdCampaignError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteAdCampaignRequest,
  output: DeleteAdCampaignResponse,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DuplicateAdCampaignError =
  | BadRequest
  | NotFound
  | Conflict
  | WhopOpError;
/** Duplicate an Ad Campaign Creates copies of the campaign in `duplicating` status and returns them; each copy transitions to `draft` once duplication completes. Poll each returned campaign until it leaves `duplicating` — a copy that could not be completed is deleted and returns 404. */
export const duplicateAdCampaign: API.OperationMethod<
  DuplicateAdCampaignRequest,
  DuplicateAdCampaignResponse,
  DuplicateAdCampaignError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DuplicateAdCampaignRequest,
  output: DuplicateAdCampaignResponse,
  errors: [BadRequest, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListAdCampaignsError = BadRequest | WhopOpError;
/** List Ad Campaigns Lists the ad campaigns for an account, with stats over the requested window. */
export const listAdCampaigns: API.PaginatedOperationMethod<
  ListAdCampaignsRequest,
  ListAdCampaignsResponse,
  ListAdCampaignsError,
  WhopOpContext,
  AdCampaign
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListAdCampaignsRequest,
    output: ListAdCampaignsResponse,
    errors: [BadRequest],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type PauseAdCampaignError = Conflict | WhopOpError;
/** Pause an Ad Campaign Pauses an active ad campaign. */
export const pauseAdCampaign: API.OperationMethod<
  PauseAdCampaignRequest,
  AdCampaign,
  PauseAdCampaignError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PauseAdCampaignRequest,
  output: AdCampaign,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveAdCampaignError = NotFound | WhopOpError;
/** Retrieve an Ad Campaign Retrieves a single ad campaign with stats over the requested window. */
export const retrieveAdCampaign: API.OperationMethod<
  RetrieveAdCampaignRequest,
  AdCampaign,
  RetrieveAdCampaignError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveAdCampaignRequest,
  output: AdCampaign,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetryAdCampaignPaymentError = BadRequest | Conflict | WhopOpError;
/** Retry a Failed Ad Campaign Payment Retries billing for an ad campaign whose payment previously failed. */
export const retryAdCampaignPayment: API.OperationMethod<
  RetryAdCampaignPaymentRequest,
  AdCampaign,
  RetryAdCampaignPaymentError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetryAdCampaignPaymentRequest,
  output: AdCampaign,
  errors: [BadRequest, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UnpauseAdCampaignError = Conflict | WhopOpError;
/** Unpause an Ad Campaign Resumes a paused ad campaign. */
export const unpauseAdCampaign: API.OperationMethod<
  UnpauseAdCampaignRequest,
  AdCampaign,
  UnpauseAdCampaignError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UnpauseAdCampaignRequest,
  output: AdCampaign,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateAdCampaignError = WhopOpError;
/** Update an Ad Campaign Updates an ad campaign's editable fields (title, budget, schedule, bid strategy, special ad categories, and, before launch, budget type and budget optimization), and launches a draft campaign by setting status to active. Objective and desired cost per result are fixed at creation and cannot be changed. */
export const updateAdCampaign: API.OperationMethod<
  UpdateAdCampaignRequest,
  AdCampaign,
  UpdateAdCampaignError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateAdCampaignRequest,
  output: AdCampaign,
  errors: [],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
