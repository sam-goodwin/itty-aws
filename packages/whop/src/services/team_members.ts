// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** The system role to grant. */
export type CreateTeamMemberRequestRole =
  | "owner"
  | "admin"
  | "sales_manager"
  | "moderator"
  | "advertiser"
  | "workforce";
export const CreateTeamMemberRequestRole = /*@__PURE__*/ S.String;

export interface CreateTeamMemberRequest {
  /** Account ID, prefixed `biz_`. */
  account_id: string;
  /** Email address to invite. Mutually exclusive with `user_id`. If the email already belongs to a Whop account it is treated the same as passing that account's `user_id`; otherwise a pending invite is created for the email. */
  email?: string;
  /** The system role to grant. */
  role: CreateTeamMemberRequestRole | (string & {});
  /** The user to add to the team, prefixed `user_`. Mutually exclusive with `email`. */
  user_id?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateTeamMemberRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String,
    email: S.optional(S.String),
    role: CreateTeamMemberRequestRole,
    user_id: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/team_members", code: 200 })),
).annotate({
  identifier: "CreateTeamMemberRequest",
}) as any as S.Schema<CreateTeamMemberRequest>;

export interface TeamMemberAuthorizedRole {
  /** Custom role ID, prefixed `aurl_`. */
  id: string;
  /** Custom role name. */
  name: string;
}
export const TeamMemberAuthorizedRole = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
  }),
).annotate({
  identifier: "TeamMemberAuthorizedRole",
}) as any as S.Schema<TeamMemberAuthorizedRole>;

/** The member's role on the account. `custom` means a bespoke dashboard-managed role; the API can read but not grant it. */
export type TeamMemberRole =
  | "owner"
  | "admin"
  | "sales_manager"
  | "moderator"
  | "advertiser"
  | "app_manager"
  | "support"
  | "manager"
  | "workforce"
  | "custom";
export const TeamMemberRole = /*@__PURE__*/ S.String;

/** `joined` for accepted members, `pending` while the invite is pending. */
export type TeamMemberStatus = "joined" | "pending";
export const TeamMemberStatus = /*@__PURE__*/ S.String;

export interface UserProfilePicture {
  /** Avatar image URL. Always present — a generated placeholder when the user set no picture. */
  url: string;
}
export const UserProfilePicture = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
  }),
).annotate({
  identifier: "UserProfilePicture",
}) as any as S.Schema<UserProfilePicture>;

export interface UserSummary {
  /** User ID, prefixed `user_`. */
  id: string;
  /** Display name. */
  name: string | null;
  /** Avatar wrapper; its `url` is always present, using a generated placeholder when the user set no picture. */
  profile_picture: UserProfilePicture;
  /** Public username. */
  username: string;
}
export const UserSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    profile_picture: UserProfilePicture,
    username: S.String,
  }),
).annotate({ identifier: "UserSummary" }) as any as S.Schema<UserSummary>;

export interface TeamMember {
  /** The account this membership belongs to, prefixed `biz_`. */
  account_id: string;
  /** Custom role assigned to this member, or `null` when the member has a system role. */
  authorized_role: TeamMemberAuthorizedRole | null;
  /** When the member joined or the invite was sent, as an ISO 8601 timestamp. */
  created_at: string;
  /** The member's email address. For accepted members, `null` unless the caller holds the email read scope; for invites, the invited address. */
  email: string | null;
  /** Team member ID — `ausr_` for accepted members, `ausri_` for pending invites. */
  id: string;
  /** Whether this member is an agent (app-controlled account) rather than a human team member. Always `false` for invites. */
  is_agent: boolean;
  /** The member's role on the account. `custom` means a bespoke dashboard-managed role; the API can read but not grant it. */
  role: TeamMemberRole;
  /** `joined` for accepted members, `pending` while the invite is pending. */
  status: TeamMemberStatus;
  /** When the membership was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** The user behind this team membership. `null` for an invite sent to an email with no Whop account yet. */
  user: UserSummary | null;
}
export const TeamMember = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String,
    authorized_role: S.NullOr(TeamMemberAuthorizedRole),
    created_at: S.String,
    email: S.NullOr(S.String),
    id: S.String,
    is_agent: S.Boolean,
    role: TeamMemberRole,
    status: TeamMemberStatus,
    updated_at: S.String,
    user: S.NullOr(UserSummary),
  }),
).annotate({ identifier: "TeamMember" }) as any as S.Schema<TeamMember>;

export interface DeleteTeamMemberRequest {
  /** Team member ID — `ausr_` for accepted members, `ausri_` for pending invites. */
  id: string;
}
export const DeleteTeamMemberRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/team_members/{id}", code: 200 })),
).annotate({
  identifier: "DeleteTeamMemberRequest",
}) as any as S.Schema<DeleteTeamMemberRequest>;

export interface DeleteTeamMemberResponse {
  success: boolean;
}
export const DeleteTeamMemberResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    success: S.Boolean,
  }),
).annotate({
  identifier: "DeleteTeamMemberResponse",
}) as any as S.Schema<DeleteTeamMemberResponse>;

export type ListTeamMembersRequestStatus = "joined" | "pending";
export const ListTeamMembersRequestStatus = /*@__PURE__*/ S.String;

export type ListTeamMembersRequestRole =
  | "owner"
  | "admin"
  | "sales_manager"
  | "moderator"
  | "advertiser"
  | "app_manager"
  | "support"
  | "manager"
  | "workforce"
  | "custom";
export const ListTeamMembersRequestRole = /*@__PURE__*/ S.String;

export type ListTeamMembersRequestOrder = "created_at";
export const ListTeamMembersRequestOrder = /*@__PURE__*/ S.String;

export type ListTeamMembersRequestDirection = "asc" | "desc";
export const ListTeamMembersRequestDirection = /*@__PURE__*/ S.String;

export interface ListTeamMembersRequest {
  /** Account ID, prefixed `biz_`. */
  account_id: string;
  /** Only return members with this status: `joined` (accepted members) or `pending` (pending invites). Both are returned by default. */
  status?: ListTeamMembersRequestStatus | (string & {});
  /** Only return the membership for this user ID, prefixed `user_`. */
  user_id?: string;
  /** Only return members with this role. `custom` matches members on a dashboard-managed custom role. */
  role?: ListTeamMembersRequestRole | (string & {});
  /** Only return members added before this ISO 8601 timestamp. */
  created_before?: string;
  /** Only return members added after this ISO 8601 timestamp. */
  created_after?: string;
  /** Field used to sort members. */
  order?: ListTeamMembersRequestOrder | (string & {});
  /** Sort direction. Defaults to `desc`. */
  direction?: ListTeamMembersRequestDirection | (string & {});
  /** Number of members to return. Defaults to 20; maximum 100. */
  first?: number;
  /** Cursor for the next page of members. */
  after?: string;
  /** Number of members to return from the end of the window. */
  last?: number;
  /** Cursor to paginate backwards from. */
  before?: string;
}
export const ListTeamMembersRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String.pipe(T.Query()),
    status: S.optional(ListTeamMembersRequestStatus.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
    role: S.optional(ListTeamMembersRequestRole.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListTeamMembersRequestOrder.pipe(T.Query())),
    direction: S.optional(ListTeamMembersRequestDirection.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/team_members", code: 200 })),
).annotate({
  identifier: "ListTeamMembersRequest",
}) as any as S.Schema<ListTeamMembersRequest>;

export type ListTeamMembersResponseDataList = Array<TeamMember>;
export const ListTeamMembersResponseDataList = /*@__PURE__*/ S.Array(
  TeamMember,
) as any as S.Schema<ListTeamMembersResponseDataList>;

export interface ListTeamMembersResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListTeamMembersResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListTeamMembersResponsePageInfo",
}) as any as S.Schema<ListTeamMembersResponsePageInfo>;

export interface ListTeamMembersResponse {
  data: ListTeamMembersResponseDataList;
  page_info: ListTeamMembersResponsePageInfo;
}
export const ListTeamMembersResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListTeamMembersResponseDataList,
    page_info: ListTeamMembersResponsePageInfo,
  }),
).annotate({
  identifier: "ListTeamMembersResponse",
}) as any as S.Schema<ListTeamMembersResponse>;

export interface RetrieveTeamMemberRequest {
  /** Team member ID — `ausr_` for accepted members, `ausri_` for pending invites. */
  id: string;
}
export const RetrieveTeamMemberRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/team_members/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveTeamMemberRequest",
}) as any as S.Schema<RetrieveTeamMemberRequest>;

/** The system role to grant. */
export type UpdateTeamMemberRequestRole =
  | "owner"
  | "admin"
  | "sales_manager"
  | "moderator"
  | "advertiser"
  | "workforce";
export const UpdateTeamMemberRequestRole = /*@__PURE__*/ S.String;

export interface UpdateTeamMemberRequest {
  /** Team member ID — `ausr_` for accepted members, `ausri_` for pending invites. */
  id: string;
  /** The system role to grant. */
  role: UpdateTeamMemberRequestRole | (string & {});
}
export const UpdateTeamMemberRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    role: UpdateTeamMemberRequestRole,
  }).pipe(T.Http({ method: "PATCH", uri: "/team_members/{id}", code: 200 })),
).annotate({
  identifier: "UpdateTeamMemberRequest",
}) as any as S.Schema<UpdateTeamMemberRequest>;

export type CreateTeamMemberError = BadRequest | Conflict | WhopOpError;
/** Create Team Member Adds a member to an account's team with a system role. Identify them by exactly one of `user_id` or `email`. If the person has not yet accepted — or the email does not belong to a Whop account yet — an invitation is sent instead and the response is `202` with `{ "object": "team_member_invite", "invitation_sent": true }`. If they already have a pending invite, the request fails with a `400`. Custom roles cannot be granted via the API. Granting the `workforce` role is also allowed with the `bounty:create` scope. */
export const createTeamMember: API.OperationMethod<
  CreateTeamMemberRequest,
  TeamMember,
  CreateTeamMemberError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateTeamMemberRequest,
  output: TeamMember,
  errors: [BadRequest, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteTeamMemberError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** Delete Team Member Removes a team member from the account, or revokes a pending invite when given an `ausri_` ID. A user session may delete its own membership to leave the team without the delete scope. Removing a member on the `workforce` role is also allowed with the `bounty:create` scope. The account owner cannot be removed. */
export const deleteTeamMember: API.OperationMethod<
  DeleteTeamMemberRequest,
  DeleteTeamMemberResponse,
  DeleteTeamMemberError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteTeamMemberRequest,
  output: DeleteTeamMemberResponse,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListTeamMembersError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** List Team Members Lists an account's team members, including pending invites (`status: "pending"`, `ausri_` ids; `user` is `null` for invites sent to an email with no Whop account yet). For accepted members, `email` requires the `company:authorized_user:email:read` scope and is `null` otherwise. Listing `role=workforce` is also allowed with the `bounty:create` scope. */
export const listTeamMembers: API.PaginatedOperationMethod<
  ListTeamMembersRequest,
  ListTeamMembersResponse,
  ListTeamMembersError,
  WhopOpContext,
  TeamMember
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListTeamMembersRequest,
    output: ListTeamMembersResponse,
    errors: [BadRequest, Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveTeamMemberError = NotFound | WhopOpError;
/** Retrieve Team Member Retrieves a team member by ID. `email` requires the `company:authorized_user:email:read` scope and is `null` otherwise. */
export const retrieveTeamMember: API.OperationMethod<
  RetrieveTeamMemberRequest,
  TeamMember,
  RetrieveTeamMemberError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveTeamMemberRequest,
  output: TeamMember,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateTeamMemberError = BadRequest | NotFound | WhopOpError;
/** Update Team Member Changes a team member's system role. Requires a user session — account API keys cannot change member roles. The account owner's role cannot be changed, and you cannot change your own role. */
export const updateTeamMember: API.OperationMethod<
  UpdateTeamMemberRequest,
  TeamMember,
  UpdateTeamMemberError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateTeamMemberRequest,
  output: TeamMember,
  errors: [BadRequest, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
