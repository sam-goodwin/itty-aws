// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface CancelPayoutRequest {
  /** Payout ID, prefixed `wdrl_`, or the `cofr_` payout request ID returned by `POST /payouts` — both cancel the same payout. */
  id: string;
  /** Owning account ID, prefixed `biz_`. Provide exactly one of `account_id` or `user_id`. */
  account_id?: string;
  /** Owning user ID, prefixed `user_`. Provide exactly one of `account_id` or `user_id`. */
  user_id?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CancelPayoutRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/payouts/{id}/cancel", code: 200 })),
).annotate({
  identifier: "CancelPayoutRequest",
}) as any as S.Schema<CancelPayoutRequest>;

/** Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; `null` otherwise. */
export interface CancelPayoutResponseFailure {
  /** Classified failure code from the maintained error catalog. */
  code: string | null;
  /** The effective time of the reversal that put the funds back in the balance — `null` if they never left it or have not returned yet. Set only once the return is confirmed in the ledger; the ledger posting itself can land moments after this time. */
  funds_returned_at: string | null;
  /** Human-readable explanation of the failure. Callers holding `payout:destination:read` may receive text personalized to the destination; other callers get the generic catalog message. */
  message: string | null;
}
export const CancelPayoutResponseFailure = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    code: S.NullOr(S.String),
    funds_returned_at: S.NullOr(S.String),
    message: S.NullOr(S.String),
  }),
).annotate({
  identifier: "CancelPayoutResponseFailure",
}) as any as S.Schema<CancelPayoutResponseFailure>;

/** Who bore the payout fee: the account itself, or its parent platform. */
export type CancelPayoutResponseFeePaidBy = "self" | "platform";
export const CancelPayoutResponseFeePaidBy = /*@__PURE__*/ S.String;

/** Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters. */
export type CancelPayoutResponseMetadataMap = {
  [key: string]: string | undefined;
};
export const CancelPayoutResponseMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CancelPayoutResponseMetadataMap>;

export type CancelPayoutResponseObject = "payout";
export const CancelPayoutResponseObject = /*@__PURE__*/ S.String;

/** How the funds are delivered to the recipient. */
export type CancelPayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType =
  | "cash_pickup"
  | "bank_deposit"
  | "home_delivery"
  | "mobile_wallet"
  | "card"
  | "check"
  | "bill"
  | "cryptocurrency"
  | "unknown";
export const CancelPayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType =
  /*@__PURE__*/ S.String;

/** Supported payout method display details. */
export interface CancelPayoutResponsePayoutMethodSupportedPayoutMethod {
  /** How the funds are delivered to the recipient. */
  delivery_type: CancelPayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType;
  /** Supported payout method icon URL. */
  icon_url: string | null;
  /** Supported payout method display name. */
  payer_name: string | null;
}
export const CancelPayoutResponsePayoutMethodSupportedPayoutMethod =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      delivery_type:
        CancelPayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType,
      icon_url: S.NullOr(S.String),
      payer_name: S.NullOr(S.String),
    }),
  ).annotate({
    identifier: "CancelPayoutResponsePayoutMethodSupportedPayoutMethod",
  }) as any as S.Schema<CancelPayoutResponsePayoutMethodSupportedPayoutMethod>;

/** The saved payout method used. Requires payout:destination:read; null without it. */
export interface CancelPayoutResponsePayoutMethod {
  /** Saved payout method nickname. */
  nickname: string | null;
  /** Supported payout method display details. */
  supported_payout_method: CancelPayoutResponsePayoutMethodSupportedPayoutMethod | null;
}
export const CancelPayoutResponsePayoutMethod = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    nickname: S.NullOr(S.String),
    supported_payout_method: S.NullOr(
      CancelPayoutResponsePayoutMethodSupportedPayoutMethod,
    ),
  }),
).annotate({
  identifier: "CancelPayoutResponsePayoutMethod",
}) as any as S.Schema<CancelPayoutResponsePayoutMethod>;

/** How the payout was created. `automatic` means a scheduled auto-payout; `null` on payouts created before source tracking or through internal tooling. */
export type CancelPayoutResponseSource = "api" | "dashboard" | "automatic";
export const CancelPayoutResponseSource = /*@__PURE__*/ S.String;

/** Payout delivery speed. */
export type CancelPayoutResponseSpeed = "standard" | "instant";
export const CancelPayoutResponseSpeed = /*@__PURE__*/ S.String;

/** Current payout status. */
export type CancelPayoutResponseStatus =
  | "requested"
  | "in_review"
  | "processing"
  | "completed"
  | "reversed"
  | "canceled"
  | "failed"
  | "denied";
export const CancelPayoutResponseStatus = /*@__PURE__*/ S.String;

export interface CancelPayoutResponse {
  /** The payout amount in whole currency units, as a decimal string. */
  amount: string;
  /** When the payout was created. */
  created_at: string;
  /** Payout currency. */
  currency: string;
  /** The amount delivered in the destination currency, as a decimal string. Assigned when the payout is processed, so it is `null` before then and on payouts without a recorded conversion. */
  destination_amount: string | null;
  /** Currency the funds are delivered in, taken from the payout method when the payout is created. On a stablecoin payout it follows the settlement payout minted alongside it — the `GET /payouts` row carrying this payout's id as `payout_request_id` — and is `null` only when no settlement payout exists. */
  destination_currency: string | null;
  /** Estimated time the funds become available in the destination account. */
  estimated_arrival: string | null;
  /** Exchange rate from the payout currency to the destination currency. Assigned when the payout is processed, so it is `null` before then and on payouts without a recorded rate. */
  exchange_rate: number | null;
  /** Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; `null` otherwise. */
  failure: CancelPayoutResponseFailure | null;
  /** The fee charged for the payout, in the payout currency, as a decimal string. */
  fee_amount: string;
  /** Who bore the payout fee: the account itself, or its parent platform. */
  fee_paid_by: CancelPayoutResponseFeePaidBy;
  /** Payout ID, prefixed `wdrl_` for a payout returned by `GET /payouts` or `cofr_` for a payout request returned by `POST /payouts`. */
  id: string;
  /** Whop's markup on the provider fee, in the payout currency, as a decimal string. `"0.0"` when none applies. */
  markup_fee: string;
  /** Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters. */
  metadata: CancelPayoutResponseMetadataMap;
  /** The planned net for the destination, in the payout currency: amount minus fee_amount minus markup_fee when fee_paid_by is `self`; equal to amount when the platform covers the fees. A payout that ends denied, canceled, or failed delivered nothing — most keep the planned figure and `failure` says where the funds are, but a canceled stablecoin payout can report the settled outcome instead: `amount` carries what stayed in the balance, fees are zero because none were charged, and `net_amount` is 0 because nothing was delivered. */
  net_amount: string;
  /** Free-form notes attached by the payout creator, or `null` when none were provided. Maximum 255 characters. */
  notes: string | null;
  object: CancelPayoutResponseObject;
  /** Name of the entity processing the payout. */
  payer_name: string | null;
  /** The saved payout method used. Requires payout:destination:read; null without it. */
  payout_method: CancelPayoutResponsePayoutMethod | null;
  /** Payout request ID, prefixed `cofr_`, returned by `POST /payouts`. For a request retrieved by its own `cofr_` ID, this equals `id`. Returns `null` for payouts not created by `POST /payouts`. */
  payout_request_id: string | null;
  /** How the payout was created. `automatic` means a scheduled auto-payout; `null` on payouts created before source tracking or through internal tooling. */
  source: CancelPayoutResponseSource | null;
  /** Payout delivery speed. */
  speed: CancelPayoutResponseSpeed;
  /** Current payout status. */
  status: CancelPayoutResponseStatus;
  /** The finest machine phase under `status` — for example `awaiting_provider_acceptance` vs `in_transit` under `processing`, or the stablecoin conversion phase under `requested`. Informational vocabulary: values can be added without a version bump; `status` is the versioned contract. */
  status_detail: string;
  /** ACH trace number the recipient's bank can use to locate this payout. Assigned when the payout is submitted to the bank, so it is `null` before then and on payouts not sent over ACH. */
  trace_code: string | null;
}
export const CancelPayoutResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.String,
    created_at: S.String,
    currency: S.String,
    destination_amount: S.NullOr(S.String),
    destination_currency: S.NullOr(S.String),
    estimated_arrival: S.NullOr(S.String),
    exchange_rate: S.NullOr(S.Number),
    failure: S.NullOr(CancelPayoutResponseFailure),
    fee_amount: S.String,
    fee_paid_by: CancelPayoutResponseFeePaidBy,
    id: S.String,
    markup_fee: S.String,
    metadata: CancelPayoutResponseMetadataMap,
    net_amount: S.String,
    notes: S.NullOr(S.String),
    object: CancelPayoutResponseObject,
    payer_name: S.NullOr(S.String),
    payout_method: S.NullOr(CancelPayoutResponsePayoutMethod),
    payout_request_id: S.NullOr(S.String),
    source: S.NullOr(CancelPayoutResponseSource),
    speed: CancelPayoutResponseSpeed,
    status: CancelPayoutResponseStatus,
    status_detail: S.String,
    trace_code: S.NullOr(S.String),
  }),
).annotate({
  identifier: "CancelPayoutResponse",
}) as any as S.Schema<CancelPayoutResponse>;

/** Key-value data to attach to the payout, echoed on every read and in webhook payloads. At most 50 keys, key names up to 40 characters, string values up to 500 characters. Never store secrets or regulated personal data here — webhook bodies are retained for delivery inspection. */
export type CreatePayoutRequestMetadataMap = {
  [key: string]: string | undefined;
};
export const CreatePayoutRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreatePayoutRequestMetadataMap>;

/** How fast the funds should arrive. `instant` is only accepted when the account and payout method are eligible; otherwise the payout is rejected. */
export type CreatePayoutRequestSpeed = "standard" | "instant";
export const CreatePayoutRequestSpeed = /*@__PURE__*/ S.String;

export interface CreatePayoutRequest {
  /** Account to pay out from, prefixed `biz_`. Provide exactly one of `account_id` or `user_id`. */
  account_id?: string;
  /** Set to `true` to continue when the destination bank could not confirm the payout method account holder's name, or `false` to have the payout refused in that case so the account holder can correct the name or link their bank first. Omitting the field skips the warning gate — a client that cannot show the warning keeps its pre-gate behavior. */
  acknowledge_bank_warning?: boolean;
  /** The amount to pay out in the specified currency. */
  amount: number;
  /** The currency to pay out. Balances are held per currency and the payout draws only from the balance in this currency, so match the currency the funds arrived in — for example `cad` for an account funded by CAD transfers. Defaults to `usd`. */
  currency?: string;
  /** Key-value data to attach to the payout, echoed on every read and in webhook payloads. At most 50 keys, key names up to 40 characters, string values up to 500 characters. Never store secrets or regulated personal data here — webhook bodies are retained for delivery inspection. */
  metadata?: CreatePayoutRequestMetadataMap;
  /** Free-form notes to attach to the payout, with a maximum of 255 characters. Omit or pass `null` for no notes. */
  notes?: string | null;
  /** The saved payout method to deliver to (a potk_ identifier). */
  payout_method_id: string;
  /** Whether the parent platform covers the payout fee instead of the account being paid out. Omit to use the platform's configured fee coverage policy; pass `false` to opt out of it. `true` is only accepted for accounts that belong to a platform, and requires the platform's policy to cover this payout method's category or a caller authorized to manage the platform's child account fees. */
  platform_covers_fees?: boolean;
  /** How fast the funds should arrive. `instant` is only accepted when the account and payout method are eligible; otherwise the payout is rejected. */
  speed?: CreatePayoutRequestSpeed | (string & {});
  /** User to pay out from, prefixed `user_`. Provide exactly one of `account_id` or `user_id`. */
  user_id?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreatePayoutRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    acknowledge_bank_warning: S.optional(S.Boolean),
    amount: S.Number,
    currency: S.optional(S.String),
    metadata: S.optional(CreatePayoutRequestMetadataMap),
    notes: S.optional(S.NullOr(S.String)),
    payout_method_id: S.String,
    platform_covers_fees: S.optional(S.Boolean),
    speed: S.optional(CreatePayoutRequestSpeed),
    user_id: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/payouts", code: 200 })),
).annotate({
  identifier: "CreatePayoutRequest",
}) as any as S.Schema<CreatePayoutRequest>;

/** Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; `null` otherwise. */
export type CreatePayoutResponseFailure = CancelPayoutResponseFailure;
export const CreatePayoutResponseFailure = CancelPayoutResponseFailure;

/** Who bore the payout fee: the account itself, or its parent platform. */
export type CreatePayoutResponseFeePaidBy = "self" | "platform";
export const CreatePayoutResponseFeePaidBy = /*@__PURE__*/ S.String;

/** Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters. */
export type CreatePayoutResponseMetadataMap = {
  [key: string]: string | undefined;
};
export const CreatePayoutResponseMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreatePayoutResponseMetadataMap>;

export type CreatePayoutResponseObject = "payout";
export const CreatePayoutResponseObject = /*@__PURE__*/ S.String;

/** How the funds are delivered to the recipient. */
export type CreatePayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType =
  | "cash_pickup"
  | "bank_deposit"
  | "home_delivery"
  | "mobile_wallet"
  | "card"
  | "check"
  | "bill"
  | "cryptocurrency"
  | "unknown";
export const CreatePayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType =
  /*@__PURE__*/ S.String;

/** Supported payout method display details. */
export interface CreatePayoutResponsePayoutMethodSupportedPayoutMethod {
  /** How the funds are delivered to the recipient. */
  delivery_type: CreatePayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType;
  /** Supported payout method icon URL. */
  icon_url: string | null;
  /** Supported payout method display name. */
  payer_name: string | null;
}
export const CreatePayoutResponsePayoutMethodSupportedPayoutMethod =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      delivery_type:
        CreatePayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType,
      icon_url: S.NullOr(S.String),
      payer_name: S.NullOr(S.String),
    }),
  ).annotate({
    identifier: "CreatePayoutResponsePayoutMethodSupportedPayoutMethod",
  }) as any as S.Schema<CreatePayoutResponsePayoutMethodSupportedPayoutMethod>;

/** The saved payout method used. Requires payout:destination:read; null without it. */
export interface CreatePayoutResponsePayoutMethod {
  /** Saved payout method nickname. */
  nickname: string | null;
  /** Supported payout method display details. */
  supported_payout_method: CreatePayoutResponsePayoutMethodSupportedPayoutMethod | null;
}
export const CreatePayoutResponsePayoutMethod = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    nickname: S.NullOr(S.String),
    supported_payout_method: S.NullOr(
      CreatePayoutResponsePayoutMethodSupportedPayoutMethod,
    ),
  }),
).annotate({
  identifier: "CreatePayoutResponsePayoutMethod",
}) as any as S.Schema<CreatePayoutResponsePayoutMethod>;

/** How the payout was created. `automatic` means a scheduled auto-payout; `null` on payouts created before source tracking or through internal tooling. */
export type CreatePayoutResponseSource = "api" | "dashboard" | "automatic";
export const CreatePayoutResponseSource = /*@__PURE__*/ S.String;

/** Payout delivery speed. */
export type CreatePayoutResponseSpeed = "standard" | "instant";
export const CreatePayoutResponseSpeed = /*@__PURE__*/ S.String;

/** Current payout status, in the same vocabulary as GET /payouts. */
export type CreatePayoutResponseStatus =
  | "requested"
  | "in_review"
  | "processing"
  | "completed"
  | "reversed"
  | "canceled"
  | "failed"
  | "denied";
export const CreatePayoutResponseStatus = /*@__PURE__*/ S.String;

export interface CreatePayoutResponse {
  /** The payout amount in whole currency units, as a decimal string. */
  amount: string;
  /** When the payout was created. */
  created_at: string;
  /** Payout currency. */
  currency: string;
  /** The amount delivered in the destination currency, as a decimal string. Null until the payout settles; appears on the payout in GET /payouts once assigned. */
  destination_amount: string | null;
  /** Currency the funds are delivered in, taken from the payout method. On a stablecoin payout it follows the settlement payout minted alongside it — the `GET /payouts` row carrying this payout's id as `payout_request_id` — and is `null` only when no settlement payout exists. */
  destination_currency: string | null;
  /** Estimated time the funds become available in the destination account. Null until the payout settles. */
  estimated_arrival: string | null;
  /** Exchange rate from the payout currency to the destination currency. Null until the payout settles; appears on the payout in GET /payouts once assigned. */
  exchange_rate: number | null;
  /** Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; `null` otherwise. */
  failure: CancelPayoutResponseFailure | null;
  /** The fee charged for the payout, in the payout currency, as a decimal string. */
  fee_amount: string;
  /** Who bore the payout fee: the account itself, or its parent platform. */
  fee_paid_by: CreatePayoutResponseFeePaidBy;
  /** Payout ID, prefixed `wdrl_` — the id POST returns is the id GET /payouts lists. Conversion requests created before this version keep answering under their `cofr_` id. */
  id: string;
  /** Whop's markup on the provider fee, in the payout currency, as a decimal string. `"0.0"` when none applies. */
  markup_fee: string;
  /** Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters. */
  metadata: CreatePayoutResponseMetadataMap;
  /** The planned net for the destination, in the payout currency: amount minus fee_amount minus markup_fee when fee_paid_by is `self`; equal to amount when the platform covers the fees. A payout that ends denied, canceled, or failed delivered nothing — most keep the planned figure and `failure` says where the funds are, but a canceled stablecoin payout can report the settled outcome instead: `amount` carries what stayed in the balance, fees are zero because none were charged, and `net_amount` is 0 because nothing was delivered. */
  net_amount: string;
  /** Free-form notes attached by the payout creator, or `null` when none were provided. Maximum 255 characters. */
  notes: string | null;
  object: CreatePayoutResponseObject;
  /** Name of the entity processing the payout. Null until the payout settles. */
  payer_name: string | null;
  /** The saved payout method used. Requires payout:destination:read; null without it. */
  payout_method: CreatePayoutResponsePayoutMethod | null;
  /** For a stablecoin payout, the id of the conversion request that funds it, prefixed `cofr_`; `null` on fiat payouts. */
  payout_request_id: string | null;
  /** How the payout was created. `automatic` means a scheduled auto-payout; `null` on payouts created before source tracking or through internal tooling. */
  source: CreatePayoutResponseSource | null;
  /** Payout delivery speed. */
  speed: CreatePayoutResponseSpeed;
  /** Current payout status, in the same vocabulary as GET /payouts. */
  status: CreatePayoutResponseStatus;
  /** The finest machine phase under `status` — for example `awaiting_provider_acceptance` vs `in_transit` under `processing`, or the stablecoin conversion phase under `requested`. Informational vocabulary: values can be added without a version bump; `status` is the versioned contract. */
  status_detail: string;
  /** ACH trace number the recipient's bank can use to locate this payout. Always `null` here — it is assigned when the payout is submitted to the bank, and appears on the payout in GET /payouts once it has been sent; payouts not sent over ACH never get one. */
  trace_code: string | null;
}
export const CreatePayoutResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.String,
    created_at: S.String,
    currency: S.String,
    destination_amount: S.NullOr(S.String),
    destination_currency: S.NullOr(S.String),
    estimated_arrival: S.NullOr(S.String),
    exchange_rate: S.NullOr(S.Number),
    failure: S.NullOr(CancelPayoutResponseFailure),
    fee_amount: S.String,
    fee_paid_by: CreatePayoutResponseFeePaidBy,
    id: S.String,
    markup_fee: S.String,
    metadata: CreatePayoutResponseMetadataMap,
    net_amount: S.String,
    notes: S.NullOr(S.String),
    object: CreatePayoutResponseObject,
    payer_name: S.NullOr(S.String),
    payout_method: S.NullOr(CreatePayoutResponsePayoutMethod),
    payout_request_id: S.NullOr(S.String),
    source: S.NullOr(CreatePayoutResponseSource),
    speed: CreatePayoutResponseSpeed,
    status: CreatePayoutResponseStatus,
    status_detail: S.String,
    trace_code: S.NullOr(S.String),
  }),
).annotate({
  identifier: "CreatePayoutResponse",
}) as any as S.Schema<CreatePayoutResponse>;

/** The supported payout method's required field values, keyed by field id — list them with `GET /payouts/supported_methods?supported_payout_method_id=...`. Field ids are stable `fld_` identifiers you may hardcode; they never change for a given field. A Basis Theory token id may be passed in place of a raw value. For a U.S. bank routing-number field, a raw nine-digit value must also pass the ABA checksum. A validation failure returns the method's full required_fields schema alongside the error. Required whenever the account details are supplied directly. */
export type CreatePayoutMethodRequestFieldsMap = {
  [key: string]: string | undefined;
};
export const CreatePayoutMethodRequestFieldsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreatePayoutMethodRequestFieldsMap>;

export interface CreatePayoutMethodRequest {
  /** The account to add the payout method for, prefixed `biz_`. Provide this or `user_id`. */
  account_id?: string;
  /** Currency the supported payout method delivers payouts in. */
  destination_currency?: string;
  /** The supported payout method's required field values, keyed by field id — list them with `GET /payouts/supported_methods?supported_payout_method_id=...`. Field ids are stable `fld_` identifiers you may hardcode; they never change for a given field. A Basis Theory token id may be passed in place of a raw value. For a U.S. bank routing-number field, a raw nine-digit value must also pass the ABA checksum. A validation failure returns the method's full required_fields schema alongside the error. Required whenever the account details are supplied directly. */
  fields?: CreatePayoutMethodRequestFieldsMap;
  /** Whether to make this the account's default payout method. */
  is_default?: boolean;
  /** A label for the payout method, unique per destination. */
  nickname?: string;
  /** The supported payout method to save (a podst_ identifier from a previous listing). */
  supported_payout_method_id: string;
  /** The user to add the payout method for, prefixed `user_`. Provide this or `account_id`. */
  user_id?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreatePayoutMethodRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    destination_currency: S.optional(S.String),
    fields: S.optional(CreatePayoutMethodRequestFieldsMap),
    is_default: S.optional(S.Boolean),
    nickname: S.optional(S.String),
    supported_payout_method_id: S.String,
    user_id: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/payouts/methods", code: 200 })),
).annotate({
  identifier: "CreatePayoutMethodRequest",
}) as any as S.Schema<CreatePayoutMethodRequest>;

/** Lifecycle trust state: `checking` (verification still running), `verified` (bank confirmed ownership or a payout already completed to it), `no_data` (verification unavailable or bank returned no ownership data), `warning` (bank could not confirm the destination's owner), `broken` (payouts failed with a permanent account error), `null` (never checked). */
export type CreatePayoutMethodResponseBankVerificationState =
  | "checking"
  | "verified"
  | "no_data"
  | "warning"
  | "broken";
export const CreatePayoutMethodResponseBankVerificationState =
  /*@__PURE__*/ S.String;

export type CreatePayoutMethodResponseObject = "payout_method";
export const CreatePayoutMethodResponseObject = /*@__PURE__*/ S.String;

/** Always `created` on create — no payout has used the method yet. */
export type CreatePayoutMethodResponseStatus = "created" | "active" | "broken";
export const CreatePayoutMethodResponseStatus = /*@__PURE__*/ S.String;

/** How funds are delivered. */
export type CreatePayoutMethodResponseSupportedPayoutMethodDeliveryType =
  | "cash_pickup"
  | "bank_deposit"
  | "home_delivery"
  | "mobile_wallet"
  | "card"
  | "check"
  | "bill"
  | "cryptocurrency"
  | "unknown";
export const CreatePayoutMethodResponseSupportedPayoutMethodDeliveryType =
  /*@__PURE__*/ S.String;

export interface CreatePayoutMethodResponseSupportedPayoutMethod {
  /** ISO 3166-1 alpha-3 country the destination pays out to. */
  country_code?: string | null;
  /** How funds are delivered. */
  delivery_type: CreatePayoutMethodResponseSupportedPayoutMethodDeliveryType;
  icon_url: string | null;
  name: string | null;
  supports_instant_delivery: boolean;
  /** Whether the payer can link this method by signing in to their bank instead of typing account details. */
  supports_plaid?: boolean;
  supports_standard_delivery: boolean;
}
export const CreatePayoutMethodResponseSupportedPayoutMethod =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      country_code: S.optional(S.NullOr(S.String)),
      delivery_type:
        CreatePayoutMethodResponseSupportedPayoutMethodDeliveryType,
      icon_url: S.NullOr(S.String),
      name: S.NullOr(S.String),
      supports_instant_delivery: S.Boolean,
      supports_plaid: S.optional(S.Boolean),
      supports_standard_delivery: S.Boolean,
    }),
  ).annotate({
    identifier: "CreatePayoutMethodResponseSupportedPayoutMethod",
  }) as any as S.Schema<CreatePayoutMethodResponseSupportedPayoutMethod>;

/** Why this method is unavailable: `destination_retired` means the payout provider stopped offering the destination. Whop may automatically remap an eligible method that was not linked through Plaid to a compatible replacement; otherwise, the account owner must re-add it. `null` means no unavailability reason is known. */
export type CreatePayoutMethodResponseUnavailableReason = "destination_retired";
export const CreatePayoutMethodResponseUnavailableReason =
  /*@__PURE__*/ S.String;

export interface CreatePayoutMethodResponse {
  /** Masked identifier for the destination. */
  account_reference: string | null;
  /** Lifecycle trust state: `checking` (verification still running), `verified` (bank confirmed ownership or a payout already completed to it), `no_data` (verification unavailable or bank returned no ownership data), `warning` (bank could not confirm the destination's owner), `broken` (payouts failed with a permanent account error), `null` (never checked). */
  bank_verification_state?: CreatePayoutMethodResponseBankVerificationState | null;
  created_at: string;
  destination_currency: string;
  /** Null on create. List payout methods to retrieve arrival estimates. */
  estimated_arrival: unknown | null;
  /** Null on create. List payout methods to retrieve the configured fee terms. */
  fee_structure: unknown | null;
  /** Payout method ID, usable as payout_method_id on POST /payouts. */
  id: string;
  institution_name: string | null;
  /** Whether this method is a copy of one saved on another of the payer's accounts. */
  is_clone?: boolean;
  is_default: boolean;
  /** When the most recent completed payout was delivered to this method, as an ISO 8601 timestamp. `null` when nothing has been paid out to it yet. */
  last_paid_out_at: string | null;
  /** Whether the payer added this method by signing in to their bank rather than typing account details. */
  linked_via_plaid?: boolean;
  /** Whether the bank sign-in behind this method has expired and must be redone before it counts as linked. */
  needs_plaid_reconnect?: boolean;
  /** User-defined label for the payout method. */
  nickname: string | null;
  object: CreatePayoutMethodResponseObject;
  payer_name: string | null;
  /** Always null on create. */
  quote: unknown | null;
  /** Always `created` on create — no payout has used the method yet. */
  status: CreatePayoutMethodResponseStatus;
  /** Always `null` on create. */
  status_reason: string | null;
  supported_payout_method: CreatePayoutMethodResponseSupportedPayoutMethod | null;
  /** Why this method is unavailable: `destination_retired` means the payout provider stopped offering the destination. Whop may automatically remap an eligible method that was not linked through Plaid to a compatible replacement; otherwise, the account owner must re-add it. `null` means no unavailability reason is known. */
  unavailable_reason: CreatePayoutMethodResponseUnavailableReason | null;
}
export const CreatePayoutMethodResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_reference: S.NullOr(S.String),
    bank_verification_state: S.optional(
      S.NullOr(CreatePayoutMethodResponseBankVerificationState),
    ),
    created_at: S.String,
    destination_currency: S.String,
    estimated_arrival: S.NullOr(S.Unknown),
    fee_structure: S.NullOr(S.Unknown),
    id: S.String,
    institution_name: S.NullOr(S.String),
    is_clone: S.optional(S.Boolean),
    is_default: S.Boolean,
    last_paid_out_at: S.NullOr(S.String),
    linked_via_plaid: S.optional(S.Boolean),
    needs_plaid_reconnect: S.optional(S.Boolean),
    nickname: S.NullOr(S.String),
    object: CreatePayoutMethodResponseObject,
    payer_name: S.NullOr(S.String),
    quote: S.NullOr(S.Unknown),
    status: CreatePayoutMethodResponseStatus,
    status_reason: S.NullOr(S.String),
    supported_payout_method: S.NullOr(
      CreatePayoutMethodResponseSupportedPayoutMethod,
    ),
    unavailable_reason: S.NullOr(CreatePayoutMethodResponseUnavailableReason),
  }),
).annotate({
  identifier: "CreatePayoutMethodResponse",
}) as any as S.Schema<CreatePayoutMethodResponse>;

export interface DeletePayoutMethodRequest {
  /** Payout method ID, prefixed `potk_`. */
  id: string;
}
export const DeletePayoutMethodRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "DELETE", uri: "/payouts/methods/{id}", code: 200 }),
  ),
).annotate({
  identifier: "DeletePayoutMethodRequest",
}) as any as S.Schema<DeletePayoutMethodRequest>;

export interface DeletePayoutMethodResponse {
  /** Always `true`. */
  deleted: boolean;
  /** ID of the deleted payout method, prefixed `potk_`. */
  id: string;
}
export const DeletePayoutMethodResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "DeletePayoutMethodResponse",
}) as any as S.Schema<DeletePayoutMethodResponse>;

export type ListPayoutMethodsRequestStatus = "created" | "active" | "broken";
export const ListPayoutMethodsRequestStatus = /*@__PURE__*/ S.String;

export interface ListPayoutMethodsRequest {
  /** The owning account ID (a biz_ identifier). Provide this or user_id. */
  account_id?: string;
  /** The owning user ID (a user_ identifier). Provide this or account_id. */
  user_id?: string;
  /** Optional status filter. `created` means saved but unused, `active` means a payout through it succeeded, `broken` means the last payout failed and the method needs fixing. */
  status?: ListPayoutMethodsRequestStatus | (string & {});
  /** Optional payout amount in whole currency units, for example `250.00`. When provided, each method includes a quote with the estimated fee, amount received, and delivery date for that amount. */
  amount?: number;
  /** Currency code of the amount, for example `usd`. Only meaningful with amount or include_limits. */
  currency?: string;
  /** When true, the response also carries limits — the live per-speed payout caps the account's payout requests are validated against, in the requested currency. Requires the payout:withdrawal:read scope. */
  include_limits?: boolean;
  /** Number of payout methods to return from the start of the window. */
  first?: number;
  /** Cursor to fetch the page after (from page_info.end_cursor). */
  after?: string;
  /** Number of payout methods to return from the end of the window. */
  last?: number;
  /** Cursor to fetch the page before (from page_info.start_cursor). */
  before?: string;
}
export const ListPayoutMethodsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
    status: S.optional(ListPayoutMethodsRequestStatus.pipe(T.Query())),
    amount: S.optional(S.Number.pipe(T.Query())),
    currency: S.optional(S.String.pipe(T.Query())),
    include_limits: S.optional(S.Boolean.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/payouts/methods", code: 200 })),
).annotate({
  identifier: "ListPayoutMethodsRequest",
}) as any as S.Schema<ListPayoutMethodsRequest>;

/** Lifecycle trust state: `checking` (verification still running), `verified` (bank confirmed ownership or a payout already completed to it), `no_data` (verification unavailable or bank returned no ownership data), `warning` (bank could not confirm the destination's owner), `broken` (payouts failed with a permanent account error), `null` (never checked). */
export type ListPayoutMethodsResponseDataItemBankVerificationState =
  | "checking"
  | "verified"
  | "no_data"
  | "warning"
  | "broken";
export const ListPayoutMethodsResponseDataItemBankVerificationState =
  /*@__PURE__*/ S.String;

/** Estimated arrival times before an amount-specific quote is requested. Null when the method is not currently eligible. */
export interface ListPayoutMethodsResponseDataItemEstimatedArrival {
  /** Estimated instant-delivery arrival, or null when unavailable. */
  instant: string | null;
  /** Estimated standard-delivery arrival, or null when unavailable. */
  standard: string | null;
}
export const ListPayoutMethodsResponseDataItemEstimatedArrival =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      instant: S.NullOr(S.String),
      standard: S.NullOr(S.String),
    }),
  ).annotate({
    identifier: "ListPayoutMethodsResponseDataItemEstimatedArrival",
  }) as any as S.Schema<ListPayoutMethodsResponseDataItemEstimatedArrival>;

/** Configured fee terms for this payout method. Null when the method is not currently eligible. An amount-specific quote remains authoritative. */
export interface ListPayoutMethodsResponseDataItemFeeStructure {
  /** Currency code of fixed_amount. */
  currency: string;
  /** Fixed fee charged, denominated in `currency`. */
  fixed_amount: number;
  /** Percentage of the payout amount charged as a fee. */
  percentage: number;
}
export const ListPayoutMethodsResponseDataItemFeeStructure =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      currency: S.String,
      fixed_amount: S.Number,
      percentage: S.Number,
    }),
  ).annotate({
    identifier: "ListPayoutMethodsResponseDataItemFeeStructure",
  }) as any as S.Schema<ListPayoutMethodsResponseDataItemFeeStructure>;

export type ListPayoutMethodsResponseDataItemObject = "payout_method";
export const ListPayoutMethodsResponseDataItemObject = /*@__PURE__*/ S.String;

/** Instant-delivery estimate. Null if the method does not support instant delivery, instant delivery is unavailable for the account, or the amount does not cover the fee. */
export interface ListPayoutMethodsResponseDataItemQuoteInstant {
  /** Total fee charged, in the payout currency. */
  fee: number;
  /** Amount delivered after fees, in the payout currency. */
  total_received: number;
}
export const ListPayoutMethodsResponseDataItemQuoteInstant =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      fee: S.Number,
      total_received: S.Number,
    }),
  ).annotate({
    identifier: "ListPayoutMethodsResponseDataItemQuoteInstant",
  }) as any as S.Schema<ListPayoutMethodsResponseDataItemQuoteInstant>;

/** Standard-delivery estimate. Null if the method does not support standard delivery, or the amount does not cover the fee. */
export type ListPayoutMethodsResponseDataItemQuoteStandard =
  ListPayoutMethodsResponseDataItemQuoteInstant;
export const ListPayoutMethodsResponseDataItemQuoteStandard =
  ListPayoutMethodsResponseDataItemQuoteInstant;

/** Fee and delivery estimate for paying out the requested amount through this method. Null unless an amount was provided, or when the estimate is unavailable. */
export interface ListPayoutMethodsResponseDataItemQuote {
  /** The payout amount the quote is for. */
  amount: number;
  /** Currency of the quoted amount. */
  currency: string;
  /** Exchange rate from the payout currency to the destination currency. */
  exchange_rate: number;
  /** Instant-delivery estimate. Null if the method does not support instant delivery, instant delivery is unavailable for the account, or the amount does not cover the fee. */
  instant: ListPayoutMethodsResponseDataItemQuoteInstant | null;
  /** Maximum payout amount for this method, in the payout currency. */
  max_limit: number | null;
  /** Minimum payout amount for this method, in the payout currency. */
  min_limit: number;
  /** Standard-delivery estimate. Null if the method does not support standard delivery, or the amount does not cover the fee. */
  standard: ListPayoutMethodsResponseDataItemQuoteInstant | null;
}
export const ListPayoutMethodsResponseDataItemQuote = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      amount: S.Number,
      currency: S.String,
      exchange_rate: S.Number,
      instant: S.NullOr(ListPayoutMethodsResponseDataItemQuoteInstant),
      max_limit: S.NullOr(S.Number),
      min_limit: S.Number,
      standard: S.NullOr(ListPayoutMethodsResponseDataItemQuoteInstant),
    }),
).annotate({
  identifier: "ListPayoutMethodsResponseDataItemQuote",
}) as any as S.Schema<ListPayoutMethodsResponseDataItemQuote>;

/** Lifecycle status: `created` means saved but unused, `active` means a payout succeeded through it, `broken` means a payout failure disabled it; a later successful payout returns it to `active`. */
export type ListPayoutMethodsResponseDataItemStatus =
  | "created"
  | "active"
  | "broken";
export const ListPayoutMethodsResponseDataItemStatus = /*@__PURE__*/ S.String;

/** How funds are delivered. */
export type ListPayoutMethodsResponseDataItemSupportedPayoutMethodDeliveryType =
  | "cash_pickup"
  | "bank_deposit"
  | "home_delivery"
  | "mobile_wallet"
  | "card"
  | "check"
  | "bill"
  | "cryptocurrency"
  | "unknown";
export const ListPayoutMethodsResponseDataItemSupportedPayoutMethodDeliveryType =
  /*@__PURE__*/ S.String;

/** The supported payout method this saved method was created from. */
export interface ListPayoutMethodsResponseDataItemSupportedPayoutMethod {
  /** ISO 3166-1 alpha-3 country the destination pays out to. */
  country_code: string | null;
  /** How funds are delivered. */
  delivery_type: ListPayoutMethodsResponseDataItemSupportedPayoutMethodDeliveryType;
  /** Supported payout method icon URL. */
  icon_url: string | null;
  /** Supported payout method display name. */
  name: string | null;
  supports_instant_delivery: boolean;
  /** Whether the payer can link this method by signing in to their bank instead of typing account details. */
  supports_plaid: boolean;
  supports_standard_delivery: boolean;
}
export const ListPayoutMethodsResponseDataItemSupportedPayoutMethod =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      country_code: S.NullOr(S.String),
      delivery_type:
        ListPayoutMethodsResponseDataItemSupportedPayoutMethodDeliveryType,
      icon_url: S.NullOr(S.String),
      name: S.NullOr(S.String),
      supports_instant_delivery: S.Boolean,
      supports_plaid: S.Boolean,
      supports_standard_delivery: S.Boolean,
    }),
  ).annotate({
    identifier: "ListPayoutMethodsResponseDataItemSupportedPayoutMethod",
  }) as any as S.Schema<ListPayoutMethodsResponseDataItemSupportedPayoutMethod>;

/** Why this method is unavailable: `destination_retired` means the payout provider stopped offering the destination. Whop may automatically remap an eligible method that was not linked through Plaid to a compatible replacement; otherwise, the account owner must re-add it. `null` means no unavailability reason is known. */
export type ListPayoutMethodsResponseDataItemUnavailableReason =
  "destination_retired";
export const ListPayoutMethodsResponseDataItemUnavailableReason =
  /*@__PURE__*/ S.String;

export interface ListPayoutMethodsResponseDataItem {
  /** Masked identifier for the destination, such as the last four digits of a bank account. */
  account_reference: string | null;
  /** Lifecycle trust state: `checking` (verification still running), `verified` (bank confirmed ownership or a payout already completed to it), `no_data` (verification unavailable or bank returned no ownership data), `warning` (bank could not confirm the destination's owner), `broken` (payouts failed with a permanent account error), `null` (never checked). */
  bank_verification_state: ListPayoutMethodsResponseDataItemBankVerificationState | null;
  /** When the payout method was added. */
  created_at: string;
  /** Currency payouts are delivered in for this method. */
  destination_currency: string;
  /** Estimated arrival times before an amount-specific quote is requested. Null when the method is not currently eligible. */
  estimated_arrival: ListPayoutMethodsResponseDataItemEstimatedArrival | null;
  /** Configured fee terms for this payout method. Null when the method is not currently eligible. An amount-specific quote remains authoritative. */
  fee_structure: ListPayoutMethodsResponseDataItemFeeStructure | null;
  /** Payout method ID. */
  id: string;
  /** Name of the bank or institution receiving payouts. */
  institution_name: string | null;
  /** Whether this method is a copy of one saved on another of the payer's accounts. */
  is_clone: boolean;
  /** Whether this is the default payout method for the account. */
  is_default: boolean;
  /** When the most recent completed payout was delivered to this method, as an ISO 8601 timestamp. `null` when nothing has been paid out to it yet. */
  last_paid_out_at: string | null;
  /** Whether the payer added this method by signing in to their bank rather than typing account details. */
  linked_via_plaid: boolean;
  /** Whether the bank sign-in behind this method has expired and must be redone before it counts as linked. */
  needs_plaid_reconnect: boolean;
  /** User-defined label for the payout method. */
  nickname: string | null;
  object: ListPayoutMethodsResponseDataItemObject;
  /** Display name of the payout rail, such as `ACH Bank Deposit`. */
  payer_name: string | null;
  /** Fee and delivery estimate for paying out the requested amount through this method. Null unless an amount was provided, or when the estimate is unavailable. */
  quote: ListPayoutMethodsResponseDataItemQuote | null;
  /** Lifecycle status: `created` means saved but unused, `active` means a payout succeeded through it, `broken` means a payout failure disabled it; a later successful payout returns it to `active`. */
  status: ListPayoutMethodsResponseDataItemStatus;
  /** Machine-readable code for why the method is `broken` — the newest disabling failure recorded through it, whether a payout error or a pre-payout rejection. `null` unless the method is broken, or when it was disabled without a recorded failure. */
  status_reason: string | null;
  /** The supported payout method this saved method was created from. */
  supported_payout_method: ListPayoutMethodsResponseDataItemSupportedPayoutMethod | null;
  /** Why this method is unavailable: `destination_retired` means the payout provider stopped offering the destination. Whop may automatically remap an eligible method that was not linked through Plaid to a compatible replacement; otherwise, the account owner must re-add it. `null` means no unavailability reason is known. */
  unavailable_reason: ListPayoutMethodsResponseDataItemUnavailableReason | null;
}
export const ListPayoutMethodsResponseDataItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_reference: S.NullOr(S.String),
    bank_verification_state: S.NullOr(
      ListPayoutMethodsResponseDataItemBankVerificationState,
    ),
    created_at: S.String,
    destination_currency: S.String,
    estimated_arrival: S.NullOr(
      ListPayoutMethodsResponseDataItemEstimatedArrival,
    ),
    fee_structure: S.NullOr(ListPayoutMethodsResponseDataItemFeeStructure),
    id: S.String,
    institution_name: S.NullOr(S.String),
    is_clone: S.Boolean,
    is_default: S.Boolean,
    last_paid_out_at: S.NullOr(S.String),
    linked_via_plaid: S.Boolean,
    needs_plaid_reconnect: S.Boolean,
    nickname: S.NullOr(S.String),
    object: ListPayoutMethodsResponseDataItemObject,
    payer_name: S.NullOr(S.String),
    quote: S.NullOr(ListPayoutMethodsResponseDataItemQuote),
    status: ListPayoutMethodsResponseDataItemStatus,
    status_reason: S.NullOr(S.String),
    supported_payout_method: S.NullOr(
      ListPayoutMethodsResponseDataItemSupportedPayoutMethod,
    ),
    unavailable_reason: S.NullOr(
      ListPayoutMethodsResponseDataItemUnavailableReason,
    ),
  }),
).annotate({
  identifier: "ListPayoutMethodsResponseDataItem",
}) as any as S.Schema<ListPayoutMethodsResponseDataItem>;

export type ListPayoutMethodsResponseDataList =
  Array<ListPayoutMethodsResponseDataItem>;
export const ListPayoutMethodsResponseDataList = /*@__PURE__*/ S.Array(
  ListPayoutMethodsResponseDataItem,
) as any as S.Schema<ListPayoutMethodsResponseDataList>;

/** Why instant payouts are unavailable for this account, or null when they are available. */
export type ListPayoutMethodsResponseLimitsInstantErrorCode =
  | "amount_too_high"
  | "account_daily_limit_reached"
  | "restricted_account"
  | "feature_disabled";
export const ListPayoutMethodsResponseLimitsInstantErrorCode =
  /*@__PURE__*/ S.String;

/** Caps for instant-speed payouts, which additionally draw on pending funds. */
export interface ListPayoutMethodsResponseLimitsInstant {
  /** How much of the account's daily instant allowance is left, in the requested currency. Null when the account is exempt from the daily cap, which is not the same as 0. */
  daily_amount_remaining: number | null;
  /** Why instant payouts are unavailable for this account, or null when they are available. */
  error_code: ListPayoutMethodsResponseLimitsInstantErrorCode | null;
  /** Human-readable form of error_code, or null when instant payouts are available. */
  error_message: string | null;
  /** The maximum amount an instant payout can move right now, in whole currency units. Already bounded by the remaining daily instant allowance; 0 while an eligibility error blocks instant payouts. */
  max_amount: number;
  /** When the daily instant allowance resets, or null when the account is exempt from the daily cap. */
  resets_at: string | null;
}
export const ListPayoutMethodsResponseLimitsInstant = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      daily_amount_remaining: S.NullOr(S.Number),
      error_code: S.NullOr(ListPayoutMethodsResponseLimitsInstantErrorCode),
      error_message: S.NullOr(S.String),
      max_amount: S.Number,
      resets_at: S.NullOr(S.String),
    }),
).annotate({
  identifier: "ListPayoutMethodsResponseLimitsInstant",
}) as any as S.Schema<ListPayoutMethodsResponseLimitsInstant>;

export type ListPayoutMethodsResponseLimitsObject = "payout_limit";
export const ListPayoutMethodsResponseLimitsObject = /*@__PURE__*/ S.String;

/** Caps for standard-speed payouts, which draw on settled funds only. */
export interface ListPayoutMethodsResponseLimitsStandard {
  /** The maximum amount a standard payout can move right now, in whole currency units. */
  max_amount: number;
}
export const ListPayoutMethodsResponseLimitsStandard = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      max_amount: S.Number,
    }),
).annotate({
  identifier: "ListPayoutMethodsResponseLimitsStandard",
}) as any as S.Schema<ListPayoutMethodsResponseLimitsStandard>;

/** The live per-speed payout caps for the account in the requested currency — the numbers a payout request is validated against at submit time, so clients can cap an amount input at a value the request will accept. Only present when include_limits is true. */
export interface ListPayoutMethodsResponseLimits {
  /** The currency the caps are denominated in. */
  currency: string;
  /** Caps for instant-speed payouts, which additionally draw on pending funds. */
  instant: ListPayoutMethodsResponseLimitsInstant;
  object: ListPayoutMethodsResponseLimitsObject;
  /** Caps for standard-speed payouts, which draw on settled funds only. */
  standard: ListPayoutMethodsResponseLimitsStandard;
}
export const ListPayoutMethodsResponseLimits = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    currency: S.String,
    instant: ListPayoutMethodsResponseLimitsInstant,
    object: ListPayoutMethodsResponseLimitsObject,
    standard: ListPayoutMethodsResponseLimitsStandard,
  }),
).annotate({
  identifier: "ListPayoutMethodsResponseLimits",
}) as any as S.Schema<ListPayoutMethodsResponseLimits>;

export interface ListPayoutMethodsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListPayoutMethodsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListPayoutMethodsResponsePageInfo",
}) as any as S.Schema<ListPayoutMethodsResponsePageInfo>;

export interface ListPayoutMethodsResponse {
  data: ListPayoutMethodsResponseDataList;
  /** The live per-speed payout caps for the account in the requested currency — the numbers a payout request is validated against at submit time, so clients can cap an amount input at a value the request will accept. Only present when include_limits is true. */
  limits?: ListPayoutMethodsResponseLimits;
  page_info: ListPayoutMethodsResponsePageInfo;
}
export const ListPayoutMethodsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListPayoutMethodsResponseDataList,
    limits: S.optional(ListPayoutMethodsResponseLimits),
    page_info: ListPayoutMethodsResponsePageInfo,
  }),
).annotate({
  identifier: "ListPayoutMethodsResponse",
}) as any as S.Schema<ListPayoutMethodsResponse>;

export type ListPayoutsRequestStatus =
  | "requested"
  | "in_review"
  | "processing"
  | "completed"
  | "reversed"
  | "canceled"
  | "failed"
  | "denied";
export const ListPayoutsRequestStatus = /*@__PURE__*/ S.String;

export type ListPayoutsRequestSource = "api" | "dashboard" | "automatic";
export const ListPayoutsRequestSource = /*@__PURE__*/ S.String;

export interface ListPayoutsRequest {
  /** The owning account ID (a biz_ identifier). Provide this or user_id. */
  account_id?: string;
  /** The owning user ID (a user_ identifier). Provide this or account_id. */
  user_id?: string;
  /** Optional currency code filter, for example `usd`. */
  currency?: string;
  /** Filter to payouts whose `status` reads this word, matching exactly what this version displays — `reversed` finds settled payouts the bank later returned. Requires Api-Version-Date 2026-08-21 or later. */
  status?: ListPayoutsRequestStatus | (string & {});
  /** Filter by how the payout was created. Payouts created before source tracking or through internal tooling carry no source and never match. */
  source?: ListPayoutsRequestSource | (string & {});
  /** Filter to payouts sent to one saved payout method (a pytk_ identifier). An unknown id matches nothing. */
  payout_method_id?: string;
  /** Only payouts created before this ISO 8601 time (exclusive). */
  created_before?: string;
  /** Only payouts created at or after this ISO 8601 time (inclusive). */
  created_after?: string;
  /** Number of payouts to return from the start of the window. */
  first?: number;
  /** Cursor to fetch the page after (from page_info.end_cursor). */
  after?: string;
  /** Number of payouts to return from the end of the window. */
  last?: number;
  /** Cursor to fetch the page before (from page_info.start_cursor). */
  before?: string;
}
export const ListPayoutsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
    currency: S.optional(S.String.pipe(T.Query())),
    status: S.optional(ListPayoutsRequestStatus.pipe(T.Query())),
    source: S.optional(ListPayoutsRequestSource.pipe(T.Query())),
    payout_method_id: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/payouts", code: 200 })),
).annotate({
  identifier: "ListPayoutsRequest",
}) as any as S.Schema<ListPayoutsRequest>;

/** Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; `null` otherwise. */
export type ListPayoutsResponseDataItemFailure = CancelPayoutResponseFailure;
export const ListPayoutsResponseDataItemFailure = CancelPayoutResponseFailure;

/** Who bore the payout fee: the account itself, or its parent platform. */
export type ListPayoutsResponseDataItemFeePaidBy = "self" | "platform";
export const ListPayoutsResponseDataItemFeePaidBy = /*@__PURE__*/ S.String;

/** Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters. */
export type ListPayoutsResponseDataItemMetadataMap = {
  [key: string]: string | undefined;
};
export const ListPayoutsResponseDataItemMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ListPayoutsResponseDataItemMetadataMap>;

export type ListPayoutsResponseDataItemObject = "payout";
export const ListPayoutsResponseDataItemObject = /*@__PURE__*/ S.String;

/** How the funds are delivered to the recipient. */
export type ListPayoutsResponseDataItemPayoutMethodSupportedPayoutMethodDeliveryType =
  | "cash_pickup"
  | "bank_deposit"
  | "home_delivery"
  | "mobile_wallet"
  | "card"
  | "check"
  | "bill"
  | "cryptocurrency"
  | "unknown";
export const ListPayoutsResponseDataItemPayoutMethodSupportedPayoutMethodDeliveryType =
  /*@__PURE__*/ S.String;

/** Supported payout method display details. */
export interface ListPayoutsResponseDataItemPayoutMethodSupportedPayoutMethod {
  /** How the funds are delivered to the recipient. */
  delivery_type: ListPayoutsResponseDataItemPayoutMethodSupportedPayoutMethodDeliveryType;
  /** Supported payout method icon URL. */
  icon_url: string | null;
  /** Supported payout method display name. */
  payer_name: string | null;
}
export const ListPayoutsResponseDataItemPayoutMethodSupportedPayoutMethod =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      delivery_type:
        ListPayoutsResponseDataItemPayoutMethodSupportedPayoutMethodDeliveryType,
      icon_url: S.NullOr(S.String),
      payer_name: S.NullOr(S.String),
    }),
  ).annotate({
    identifier: "ListPayoutsResponseDataItemPayoutMethodSupportedPayoutMethod",
  }) as any as S.Schema<ListPayoutsResponseDataItemPayoutMethodSupportedPayoutMethod>;

/** The saved payout method used. Requires payout:destination:read; null without it. */
export interface ListPayoutsResponseDataItemPayoutMethod {
  /** Saved payout method nickname. */
  nickname: string | null;
  /** Supported payout method display details. */
  supported_payout_method: ListPayoutsResponseDataItemPayoutMethodSupportedPayoutMethod | null;
}
export const ListPayoutsResponseDataItemPayoutMethod = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      nickname: S.NullOr(S.String),
      supported_payout_method: S.NullOr(
        ListPayoutsResponseDataItemPayoutMethodSupportedPayoutMethod,
      ),
    }),
).annotate({
  identifier: "ListPayoutsResponseDataItemPayoutMethod",
}) as any as S.Schema<ListPayoutsResponseDataItemPayoutMethod>;

/** How the payout was created. `automatic` means a scheduled auto-payout; `null` on payouts created before source tracking or through internal tooling. */
export type ListPayoutsResponseDataItemSource =
  | "api"
  | "dashboard"
  | "automatic";
export const ListPayoutsResponseDataItemSource = /*@__PURE__*/ S.String;

/** Payout delivery speed. */
export type ListPayoutsResponseDataItemSpeed = "standard" | "instant";
export const ListPayoutsResponseDataItemSpeed = /*@__PURE__*/ S.String;

/** Current payout status. */
export type ListPayoutsResponseDataItemStatus =
  | "requested"
  | "in_review"
  | "processing"
  | "completed"
  | "reversed"
  | "canceled"
  | "failed"
  | "denied";
export const ListPayoutsResponseDataItemStatus = /*@__PURE__*/ S.String;

export interface ListPayoutsResponseDataItem {
  /** The payout amount in whole currency units, as a decimal string. */
  amount: string;
  /** When the payout was created. */
  created_at: string;
  /** Payout currency. */
  currency: string;
  /** The amount delivered in the destination currency, as a decimal string. Assigned when the payout is processed, so it is `null` before then and on payouts without a recorded conversion. */
  destination_amount: string | null;
  /** Currency the funds are delivered in, taken from the payout method when the payout is created. On a stablecoin payout it follows the settlement payout minted alongside it — the `GET /payouts` row carrying this payout's id as `payout_request_id` — and is `null` only when no settlement payout exists. */
  destination_currency: string | null;
  /** Estimated time the funds become available in the destination account. */
  estimated_arrival: string | null;
  /** Exchange rate from the payout currency to the destination currency. Assigned when the payout is processed, so it is `null` before then and on payouts without a recorded rate. */
  exchange_rate: number | null;
  /** Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; `null` otherwise. */
  failure: CancelPayoutResponseFailure | null;
  /** The fee charged for the payout, in the payout currency, as a decimal string. */
  fee_amount: string;
  /** Who bore the payout fee: the account itself, or its parent platform. */
  fee_paid_by: ListPayoutsResponseDataItemFeePaidBy;
  /** Payout ID, prefixed `wdrl_`. */
  id: string;
  /** Whop's markup on the provider fee, in the payout currency, as a decimal string. `"0.0"` when none applies. */
  markup_fee: string;
  /** Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters. */
  metadata: ListPayoutsResponseDataItemMetadataMap;
  /** The planned net for the destination, in the payout currency: amount minus fee_amount minus markup_fee when fee_paid_by is `self`; equal to amount when the platform covers the fees. A payout that ends denied, canceled, or failed delivered nothing — most keep the planned figure and `failure` says where the funds are, but a canceled stablecoin payout can report the settled outcome instead: `amount` carries what stayed in the balance, fees are zero because none were charged, and `net_amount` is 0 because nothing was delivered. */
  net_amount: string;
  /** Free-form notes attached by the payout creator, or `null` when none were provided. Maximum 255 characters. */
  notes: string | null;
  object: ListPayoutsResponseDataItemObject;
  /** Name of the entity processing the payout. */
  payer_name: string | null;
  /** The saved payout method used. Requires payout:destination:read; null without it. */
  payout_method: ListPayoutsResponseDataItemPayoutMethod | null;
  /** Payout request ID, prefixed `cofr_`, returned by `POST /payouts`. Match it to the settled payout in `GET /payouts`. Returns `null` for payouts not created by `POST /payouts`. */
  payout_request_id: string | null;
  /** How the payout was created. `automatic` means a scheduled auto-payout; `null` on payouts created before source tracking or through internal tooling. */
  source: ListPayoutsResponseDataItemSource | null;
  /** Payout delivery speed. */
  speed: ListPayoutsResponseDataItemSpeed;
  /** Current payout status. */
  status: ListPayoutsResponseDataItemStatus;
  /** The finest machine phase under `status` — for example `awaiting_provider_acceptance` vs `in_transit` under `processing`, or the stablecoin conversion phase under `requested`. Informational vocabulary: values can be added without a version bump; `status` is the versioned contract. */
  status_detail: string;
  /** ACH trace number the recipient's bank can use to locate this payout. Assigned when the payout is submitted to the bank, so it is `null` before then and on payouts not sent over ACH. */
  trace_code: string | null;
}
export const ListPayoutsResponseDataItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.String,
    created_at: S.String,
    currency: S.String,
    destination_amount: S.NullOr(S.String),
    destination_currency: S.NullOr(S.String),
    estimated_arrival: S.NullOr(S.String),
    exchange_rate: S.NullOr(S.Number),
    failure: S.NullOr(CancelPayoutResponseFailure),
    fee_amount: S.String,
    fee_paid_by: ListPayoutsResponseDataItemFeePaidBy,
    id: S.String,
    markup_fee: S.String,
    metadata: ListPayoutsResponseDataItemMetadataMap,
    net_amount: S.String,
    notes: S.NullOr(S.String),
    object: ListPayoutsResponseDataItemObject,
    payer_name: S.NullOr(S.String),
    payout_method: S.NullOr(ListPayoutsResponseDataItemPayoutMethod),
    payout_request_id: S.NullOr(S.String),
    source: S.NullOr(ListPayoutsResponseDataItemSource),
    speed: ListPayoutsResponseDataItemSpeed,
    status: ListPayoutsResponseDataItemStatus,
    status_detail: S.String,
    trace_code: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListPayoutsResponseDataItem",
}) as any as S.Schema<ListPayoutsResponseDataItem>;

export type ListPayoutsResponseDataList = Array<ListPayoutsResponseDataItem>;
export const ListPayoutsResponseDataList = /*@__PURE__*/ S.Array(
  ListPayoutsResponseDataItem,
) as any as S.Schema<ListPayoutsResponseDataList>;

export type ListPayoutsResponsePageInfo = ListPayoutMethodsResponsePageInfo;
export const ListPayoutsResponsePageInfo = ListPayoutMethodsResponsePageInfo;

export interface ListPayoutsResponse {
  data: ListPayoutsResponseDataList;
  page_info: ListPayoutMethodsResponsePageInfo;
}
export const ListPayoutsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListPayoutsResponseDataList,
    page_info: ListPayoutMethodsResponsePageInfo,
  }),
).annotate({
  identifier: "ListPayoutsResponse",
}) as any as S.Schema<ListPayoutsResponse>;

export interface ListSupportedPayoutMethodsRequest {
  /** The owning account ID (a biz_ identifier). Provide this or user_id. */
  account_id?: string;
  /** The owning user ID (a user_ identifier). Provide this or account_id. */
  user_id?: string;
  /** ISO 3166-1 alpha-2 country code for the bank account or wallet, such as `US`. Defaults to the country of supported_payout_method_id when one is given, otherwise the payout account's country. */
  country?: string;
  /** Optional payout amount in whole currency units, for example `250.00`. When provided, each destination includes per-currency fee and delivery quotes. */
  amount?: number;
  /** Currency code of the amount, for example `usd`. Only meaningful with amount. */
  currency?: string;
  /** Narrows the list to one supported payout method (a podst_ identifier) and includes the required_fields needed to save it as a payout method. */
  supported_payout_method_id?: string;
  /** Currency the supported payout method would deliver payouts in. Only meaningful with supported_payout_method_id; required fields vary by destination currency. */
  destination_currency?: string;
  /** Number of supported payout methods to return from the start of the window. */
  first?: number;
  /** Cursor to fetch the page after (from page_info.end_cursor). */
  after?: string;
  /** Number of supported payout methods to return from the end of the window. */
  last?: number;
  /** Cursor to fetch the page before (from page_info.start_cursor). */
  before?: string;
}
export const ListSupportedPayoutMethodsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
    country: S.optional(S.String.pipe(T.Query())),
    amount: S.optional(S.Number.pipe(T.Query())),
    currency: S.optional(S.String.pipe(T.Query())),
    supported_payout_method_id: S.optional(S.String.pipe(T.Query())),
    destination_currency: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/payouts/supported_methods", code: 200 }),
  ),
).annotate({
  identifier: "ListSupportedPayoutMethodsRequest",
}) as any as S.Schema<ListSupportedPayoutMethodsRequest>;

/** How funds are delivered, for example `bank_deposit`. */
export type ListSupportedPayoutMethodsResponseDataItemDeliveryType =
  | "cash_pickup"
  | "bank_deposit"
  | "home_delivery"
  | "mobile_wallet"
  | "card"
  | "check"
  | "bill"
  | "cryptocurrency";
export const ListSupportedPayoutMethodsResponseDataItemDeliveryType =
  /*@__PURE__*/ S.String;

export type ListSupportedPayoutMethodsResponseDataItemObject =
  "supported_payout_method";
export const ListSupportedPayoutMethodsResponseDataItemObject =
  /*@__PURE__*/ S.String;

/** Instant-delivery estimate. Null if unsupported, unavailable for the account, or the amount does not cover the fee. */
export interface ListSupportedPayoutMethodsResponseDataItemQuotesItemInstant {
  estimated_arrival: string;
  fee: number;
  total_received: number;
}
export const ListSupportedPayoutMethodsResponseDataItemQuotesItemInstant =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      estimated_arrival: S.String,
      fee: S.Number,
      total_received: S.Number,
    }),
  ).annotate({
    identifier: "ListSupportedPayoutMethodsResponseDataItemQuotesItemInstant",
  }) as any as S.Schema<ListSupportedPayoutMethodsResponseDataItemQuotesItemInstant>;

/** Standard-delivery estimate. Null if unsupported or the amount does not cover the fee. */
export type ListSupportedPayoutMethodsResponseDataItemQuotesItemStandard =
  ListSupportedPayoutMethodsResponseDataItemQuotesItemInstant;
export const ListSupportedPayoutMethodsResponseDataItemQuotesItemStandard =
  ListSupportedPayoutMethodsResponseDataItemQuotesItemInstant;

export interface ListSupportedPayoutMethodsResponseDataItemQuotesItem {
  /** The payout amount the quote is for. */
  amount: number;
  /** Currency of the quoted amount. */
  currency: string;
  /** Currency the funds are delivered in. */
  destination_currency: string;
  /** Exchange rate from the payout currency to the destination currency. */
  exchange_rate: number;
  /** Instant-delivery estimate. Null if unsupported, unavailable for the account, or the amount does not cover the fee. */
  instant: ListSupportedPayoutMethodsResponseDataItemQuotesItemInstant | null;
  /** Maximum payout amount, in the payout currency. */
  max_limit: number | null;
  /** Minimum payout amount, in the payout currency. */
  min_limit: number;
  /** Standard-delivery estimate. Null if unsupported or the amount does not cover the fee. */
  standard: ListSupportedPayoutMethodsResponseDataItemQuotesItemInstant | null;
}
export const ListSupportedPayoutMethodsResponseDataItemQuotesItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      amount: S.Number,
      currency: S.String,
      destination_currency: S.String,
      exchange_rate: S.Number,
      instant: S.NullOr(
        ListSupportedPayoutMethodsResponseDataItemQuotesItemInstant,
      ),
      max_limit: S.NullOr(S.Number),
      min_limit: S.Number,
      standard: S.NullOr(
        ListSupportedPayoutMethodsResponseDataItemQuotesItemInstant,
      ),
    }),
  ).annotate({
    identifier: "ListSupportedPayoutMethodsResponseDataItemQuotesItem",
  }) as any as S.Schema<ListSupportedPayoutMethodsResponseDataItemQuotesItem>;

/** Fee and delivery estimates for the requested amount, one per destination currency. Null unless an amount was provided. */
export type ListSupportedPayoutMethodsResponseDataItemQuotesList =
  Array<ListSupportedPayoutMethodsResponseDataItemQuotesItem>;
export const ListSupportedPayoutMethodsResponseDataItemQuotesList =
  /*@__PURE__*/ S.Array(
    ListSupportedPayoutMethodsResponseDataItemQuotesItem,
  ) as any as S.Schema<ListSupportedPayoutMethodsResponseDataItemQuotesList>;

export type ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItemObject =
  "required_field";
export const ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItemObject =
  /*@__PURE__*/ S.String;

/** Allowed values for options fields. */
export type ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItemOptionsList =
  Array<string>;
export const ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItemOptionsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItemOptionsList>;

export interface ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItem {
  /** Stable field ID (`fld_` + the semantic type), used as the field key when creating the payout method. Safe to hardcode — it never changes for a given field. */
  id: string;
  /** How to collect the value: `text`, `options`, or `date`. */
  input_type: string;
  /** Human-readable field name. */
  label: string;
  object: ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItemObject;
  /** Allowed values for options fields. */
  options: ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItemOptionsList | null;
  /** Example value. */
  placeholder: string | null;
  /** Whether the field must be provided. */
  required: boolean;
  /** Whether the value is vaulted in transit and never stored raw. */
  sensitive: boolean;
  /** Semantic field type, for example `bank_account_number` or `swift`. */
  type: string;
  /** Regex the value must match. Null for options fields. */
  validation: string | null;
}
export const ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      id: S.String,
      input_type: S.String,
      label: S.String,
      object:
        ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItemObject,
      options: S.NullOr(
        ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItemOptionsList,
      ),
      placeholder: S.NullOr(S.String),
      required: S.Boolean,
      sensitive: S.Boolean,
      type: S.String,
      validation: S.NullOr(S.String),
    }),
  ).annotate({
    identifier: "ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItem",
  }) as any as S.Schema<ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItem>;

/** Fields to collect before saving this supported payout method. Present only when supported_payout_method_id narrows the request to one method. */
export type ListSupportedPayoutMethodsResponseDataItemRequiredFieldsList =
  Array<ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItem>;
export const ListSupportedPayoutMethodsResponseDataItemRequiredFieldsList =
  /*@__PURE__*/ S.Array(
    ListSupportedPayoutMethodsResponseDataItemRequiredFieldsItem,
  ) as any as S.Schema<ListSupportedPayoutMethodsResponseDataItemRequiredFieldsList>;

export interface ListSupportedPayoutMethodsResponseDataItem {
  /** How funds are delivered, for example `bank_deposit`. */
  delivery_type: ListSupportedPayoutMethodsResponseDataItemDeliveryType;
  /** Supported payout method icon URL. */
  icon_url: string | null;
  /** Supported payout method ID. */
  id: string;
  /** Supported payout method display name. */
  name: string | null;
  object: ListSupportedPayoutMethodsResponseDataItemObject;
  /** Fee and delivery estimates for the requested amount, one per destination currency. Null unless an amount was provided. */
  quotes: ListSupportedPayoutMethodsResponseDataItemQuotesList | null;
  /** Fields to collect before saving this supported payout method. Present only when supported_payout_method_id narrows the request to one method. */
  required_fields: ListSupportedPayoutMethodsResponseDataItemRequiredFieldsList | null;
  supports_instant_delivery: boolean;
  supports_standard_delivery: boolean;
}
export const ListSupportedPayoutMethodsResponseDataItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      delivery_type: ListSupportedPayoutMethodsResponseDataItemDeliveryType,
      icon_url: S.NullOr(S.String),
      id: S.String,
      name: S.NullOr(S.String),
      object: ListSupportedPayoutMethodsResponseDataItemObject,
      quotes: S.NullOr(ListSupportedPayoutMethodsResponseDataItemQuotesList),
      required_fields: S.NullOr(
        ListSupportedPayoutMethodsResponseDataItemRequiredFieldsList,
      ),
      supports_instant_delivery: S.Boolean,
      supports_standard_delivery: S.Boolean,
    }),
  ).annotate({
    identifier: "ListSupportedPayoutMethodsResponseDataItem",
  }) as any as S.Schema<ListSupportedPayoutMethodsResponseDataItem>;

export type ListSupportedPayoutMethodsResponseDataList =
  Array<ListSupportedPayoutMethodsResponseDataItem>;
export const ListSupportedPayoutMethodsResponseDataList = /*@__PURE__*/ S.Array(
  ListSupportedPayoutMethodsResponseDataItem,
) as any as S.Schema<ListSupportedPayoutMethodsResponseDataList>;

export type ListSupportedPayoutMethodsResponsePageInfo =
  ListPayoutMethodsResponsePageInfo;
export const ListSupportedPayoutMethodsResponsePageInfo =
  ListPayoutMethodsResponsePageInfo;

export interface ListSupportedPayoutMethodsResponse {
  data: ListSupportedPayoutMethodsResponseDataList;
  page_info: ListPayoutMethodsResponsePageInfo;
}
export const ListSupportedPayoutMethodsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListSupportedPayoutMethodsResponseDataList,
    page_info: ListPayoutMethodsResponsePageInfo,
  }),
).annotate({
  identifier: "ListSupportedPayoutMethodsResponse",
}) as any as S.Schema<ListSupportedPayoutMethodsResponse>;

export interface RetrievePayoutRequest {
  /** Payout ID, prefixed `wdrl_` for a payout returned by `GET /payouts` or `cofr_` for the payout request returned by `POST /payouts`. */
  id: string;
  /** Owning account ID, prefixed `biz_`. Provide exactly one of `account_id` or `user_id`. */
  account_id?: string;
  /** Owning user ID, prefixed `user_`. Provide exactly one of `account_id` or `user_id`. */
  user_id?: string;
}
export const RetrievePayoutRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/payouts/{id}", code: 200 })),
).annotate({
  identifier: "RetrievePayoutRequest",
}) as any as S.Schema<RetrievePayoutRequest>;

/** Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; `null` otherwise. */
export type RetrievePayoutResponseFailure = CancelPayoutResponseFailure;
export const RetrievePayoutResponseFailure = CancelPayoutResponseFailure;

/** Who bore the payout fee: the account itself, or its parent platform. */
export type RetrievePayoutResponseFeePaidBy = "self" | "platform";
export const RetrievePayoutResponseFeePaidBy = /*@__PURE__*/ S.String;

/** Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters. */
export type RetrievePayoutResponseMetadataMap = {
  [key: string]: string | undefined;
};
export const RetrievePayoutResponseMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<RetrievePayoutResponseMetadataMap>;

export type RetrievePayoutResponseObject = "payout";
export const RetrievePayoutResponseObject = /*@__PURE__*/ S.String;

/** How the funds are delivered to the recipient. */
export type RetrievePayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType =
  | "cash_pickup"
  | "bank_deposit"
  | "home_delivery"
  | "mobile_wallet"
  | "card"
  | "check"
  | "bill"
  | "cryptocurrency"
  | "unknown";
export const RetrievePayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType =
  /*@__PURE__*/ S.String;

/** Supported payout method display details. */
export interface RetrievePayoutResponsePayoutMethodSupportedPayoutMethod {
  /** How the funds are delivered to the recipient. */
  delivery_type: RetrievePayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType;
  /** Supported payout method icon URL. */
  icon_url: string | null;
  /** Supported payout method display name. */
  payer_name: string | null;
}
export const RetrievePayoutResponsePayoutMethodSupportedPayoutMethod =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      delivery_type:
        RetrievePayoutResponsePayoutMethodSupportedPayoutMethodDeliveryType,
      icon_url: S.NullOr(S.String),
      payer_name: S.NullOr(S.String),
    }),
  ).annotate({
    identifier: "RetrievePayoutResponsePayoutMethodSupportedPayoutMethod",
  }) as any as S.Schema<RetrievePayoutResponsePayoutMethodSupportedPayoutMethod>;

/** The saved payout method used. Requires payout:destination:read; null without it. */
export interface RetrievePayoutResponsePayoutMethod {
  /** Saved payout method nickname. */
  nickname: string | null;
  /** Supported payout method display details. */
  supported_payout_method: RetrievePayoutResponsePayoutMethodSupportedPayoutMethod | null;
}
export const RetrievePayoutResponsePayoutMethod = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    nickname: S.NullOr(S.String),
    supported_payout_method: S.NullOr(
      RetrievePayoutResponsePayoutMethodSupportedPayoutMethod,
    ),
  }),
).annotate({
  identifier: "RetrievePayoutResponsePayoutMethod",
}) as any as S.Schema<RetrievePayoutResponsePayoutMethod>;

/** How the payout was created. `automatic` means a scheduled auto-payout; `null` on payouts created before source tracking or through internal tooling. */
export type RetrievePayoutResponseSource = "api" | "dashboard" | "automatic";
export const RetrievePayoutResponseSource = /*@__PURE__*/ S.String;

/** Payout delivery speed. */
export type RetrievePayoutResponseSpeed = "standard" | "instant";
export const RetrievePayoutResponseSpeed = /*@__PURE__*/ S.String;

/** Current payout status. */
export type RetrievePayoutResponseStatus =
  | "requested"
  | "in_review"
  | "processing"
  | "completed"
  | "reversed"
  | "canceled"
  | "failed"
  | "denied";
export const RetrievePayoutResponseStatus = /*@__PURE__*/ S.String;

export interface RetrievePayoutResponse {
  /** The payout amount in whole currency units, as a decimal string. */
  amount: string;
  /** When the payout was created. */
  created_at: string;
  /** Payout currency. */
  currency: string;
  /** The amount delivered in the destination currency, as a decimal string. Assigned when the payout is processed, so it is `null` before then and on payouts without a recorded conversion. */
  destination_amount: string | null;
  /** Currency the funds are delivered in, taken from the payout method when the payout is created. On a stablecoin payout it follows the settlement payout minted alongside it — the `GET /payouts` row carrying this payout's id as `payout_request_id` — and is `null` only when no settlement payout exists. */
  destination_currency: string | null;
  /** Estimated time the funds become available in the destination account. */
  estimated_arrival: string | null;
  /** Exchange rate from the payout currency to the destination currency. Assigned when the payout is processed, so it is `null` before then and on payouts without a recorded rate. */
  exchange_rate: number | null;
  /** Why the payout ended without paying, or why it reversed after settlement. Present on failed, canceled, denied, and reversed payouts; `null` otherwise. */
  failure: CancelPayoutResponseFailure | null;
  /** The fee charged for the payout, in the payout currency, as a decimal string. */
  fee_amount: string;
  /** Who bore the payout fee: the account itself, or its parent platform. */
  fee_paid_by: RetrievePayoutResponseFeePaidBy;
  /** Payout ID, prefixed `wdrl_` for a payout returned by `GET /payouts` or `cofr_` for a payout request returned by `POST /payouts`. */
  id: string;
  /** Whop's markup on the provider fee, in the payout currency, as a decimal string. `"0.0"` when none applies. */
  markup_fee: string;
  /** Key-value data attached at creation and echoed on every read. At most 50 keys, key names up to 40 characters, string values up to 500 characters. */
  metadata: RetrievePayoutResponseMetadataMap;
  /** The planned net for the destination, in the payout currency: amount minus fee_amount minus markup_fee when fee_paid_by is `self`; equal to amount when the platform covers the fees. A payout that ends denied, canceled, or failed delivered nothing — most keep the planned figure and `failure` says where the funds are, but a canceled stablecoin payout can report the settled outcome instead: `amount` carries what stayed in the balance, fees are zero because none were charged, and `net_amount` is 0 because nothing was delivered. */
  net_amount: string;
  /** Free-form notes attached by the payout creator, or `null` when none were provided. Maximum 255 characters. */
  notes: string | null;
  object: RetrievePayoutResponseObject;
  /** Name of the entity processing the payout. */
  payer_name: string | null;
  /** The saved payout method used. Requires payout:destination:read; null without it. */
  payout_method: RetrievePayoutResponsePayoutMethod | null;
  /** Payout request ID, prefixed `cofr_`, returned by `POST /payouts`. For a request retrieved by its own `cofr_` ID, this equals `id`. Returns `null` for payouts not created by `POST /payouts`. */
  payout_request_id: string | null;
  /** How the payout was created. `automatic` means a scheduled auto-payout; `null` on payouts created before source tracking or through internal tooling. */
  source: RetrievePayoutResponseSource | null;
  /** Payout delivery speed. */
  speed: RetrievePayoutResponseSpeed;
  /** Current payout status. */
  status: RetrievePayoutResponseStatus;
  /** The finest machine phase under `status` — for example `awaiting_provider_acceptance` vs `in_transit` under `processing`, or the stablecoin conversion phase under `requested`. Informational vocabulary: values can be added without a version bump; `status` is the versioned contract. */
  status_detail: string;
  /** ACH trace number the recipient's bank can use to locate this payout. Assigned when the payout is submitted to the bank, so it is `null` before then and on payouts not sent over ACH. */
  trace_code: string | null;
}
export const RetrievePayoutResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.String,
    created_at: S.String,
    currency: S.String,
    destination_amount: S.NullOr(S.String),
    destination_currency: S.NullOr(S.String),
    estimated_arrival: S.NullOr(S.String),
    exchange_rate: S.NullOr(S.Number),
    failure: S.NullOr(CancelPayoutResponseFailure),
    fee_amount: S.String,
    fee_paid_by: RetrievePayoutResponseFeePaidBy,
    id: S.String,
    markup_fee: S.String,
    metadata: RetrievePayoutResponseMetadataMap,
    net_amount: S.String,
    notes: S.NullOr(S.String),
    object: RetrievePayoutResponseObject,
    payer_name: S.NullOr(S.String),
    payout_method: S.NullOr(RetrievePayoutResponsePayoutMethod),
    payout_request_id: S.NullOr(S.String),
    source: S.NullOr(RetrievePayoutResponseSource),
    speed: RetrievePayoutResponseSpeed,
    status: RetrievePayoutResponseStatus,
    status_detail: S.String,
    trace_code: S.NullOr(S.String),
  }),
).annotate({
  identifier: "RetrievePayoutResponse",
}) as any as S.Schema<RetrievePayoutResponse>;

export interface UpdatePayoutMethodRequest {
  /** Payout method ID, prefixed `potk_`. */
  id: string;
  /** New label for the payout method, with at least one non-whitespace character and a maximum of 100 characters. */
  nickname: string;
}
export const UpdatePayoutMethodRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    nickname: S.String,
  }).pipe(T.Http({ method: "PATCH", uri: "/payouts/methods/{id}", code: 200 })),
).annotate({
  identifier: "UpdatePayoutMethodRequest",
}) as any as S.Schema<UpdatePayoutMethodRequest>;

/** Lifecycle trust state: `checking` (verification still running), `verified` (bank confirmed ownership or a payout already completed to it), `no_data` (verification unavailable or bank returned no ownership data), `warning` (bank could not confirm the destination's owner), `broken` (payouts failed with a permanent account error), `null` (never checked). */
export type UpdatePayoutMethodResponseBankVerificationState =
  | "checking"
  | "verified"
  | "no_data"
  | "warning"
  | "broken";
export const UpdatePayoutMethodResponseBankVerificationState =
  /*@__PURE__*/ S.String;

export type UpdatePayoutMethodResponseObject = "payout_method";
export const UpdatePayoutMethodResponseObject = /*@__PURE__*/ S.String;

/** `created` — saved, no payout has completed through it yet. `active` — a payout through it completed. `broken` — a payout through it failed with an error attributable to the method, and it no longer accepts payouts; a later successful payout returns it to `active`. */
export type UpdatePayoutMethodResponseStatus = "created" | "active" | "broken";
export const UpdatePayoutMethodResponseStatus = /*@__PURE__*/ S.String;

/** How funds are delivered. */
export type UpdatePayoutMethodResponseSupportedPayoutMethodDeliveryType =
  | "cash_pickup"
  | "bank_deposit"
  | "home_delivery"
  | "mobile_wallet"
  | "card"
  | "check"
  | "bill"
  | "cryptocurrency"
  | "unknown";
export const UpdatePayoutMethodResponseSupportedPayoutMethodDeliveryType =
  /*@__PURE__*/ S.String;

export interface UpdatePayoutMethodResponseSupportedPayoutMethod {
  /** ISO 3166-1 alpha-3 country the destination pays out to. */
  country_code: string | null;
  /** How funds are delivered. */
  delivery_type: UpdatePayoutMethodResponseSupportedPayoutMethodDeliveryType;
  icon_url: string | null;
  name: string | null;
  supports_instant_delivery: boolean;
  /** Whether the payer can link this method by signing in to their bank instead of typing account details. */
  supports_plaid: boolean;
  supports_standard_delivery: boolean;
}
export const UpdatePayoutMethodResponseSupportedPayoutMethod =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      country_code: S.NullOr(S.String),
      delivery_type:
        UpdatePayoutMethodResponseSupportedPayoutMethodDeliveryType,
      icon_url: S.NullOr(S.String),
      name: S.NullOr(S.String),
      supports_instant_delivery: S.Boolean,
      supports_plaid: S.Boolean,
      supports_standard_delivery: S.Boolean,
    }),
  ).annotate({
    identifier: "UpdatePayoutMethodResponseSupportedPayoutMethod",
  }) as any as S.Schema<UpdatePayoutMethodResponseSupportedPayoutMethod>;

/** Why this method is unavailable: `destination_retired` means the payout provider stopped offering the destination. Whop may automatically remap an eligible method that was not linked through Plaid to a compatible replacement; otherwise, the account owner must re-add it. `null` means no unavailability reason is known. */
export type UpdatePayoutMethodResponseUnavailableReason = "destination_retired";
export const UpdatePayoutMethodResponseUnavailableReason =
  /*@__PURE__*/ S.String;

export interface UpdatePayoutMethodResponse {
  /** Masked identifier for the destination. */
  account_reference: string | null;
  /** Lifecycle trust state: `checking` (verification still running), `verified` (bank confirmed ownership or a payout already completed to it), `no_data` (verification unavailable or bank returned no ownership data), `warning` (bank could not confirm the destination's owner), `broken` (payouts failed with a permanent account error), `null` (never checked). */
  bank_verification_state: UpdatePayoutMethodResponseBankVerificationState | null;
  created_at: string;
  destination_currency: string;
  /** `null` after an update. List payout methods to retrieve arrival estimates. */
  estimated_arrival: unknown | null;
  /** `null` after an update. List payout methods to retrieve the configured fee terms. */
  fee_structure: unknown | null;
  /** Payout method ID, prefixed `potk_`. */
  id: string;
  institution_name: string | null;
  /** Whether this method is a copy of one saved on another of the payer's accounts. */
  is_clone: boolean;
  is_default: boolean;
  /** When the most recent completed payout was delivered to this method, as an ISO 8601 timestamp. `null` when nothing has been paid out to it yet. */
  last_paid_out_at: string | null;
  /** Whether the payer added this method by signing in to their bank rather than typing account details. */
  linked_via_plaid: boolean;
  /** Whether the bank sign-in behind this method has expired and must be redone before it counts as linked. */
  needs_plaid_reconnect: boolean;
  /** User-defined label for the payout method. */
  nickname: string | null;
  object: UpdatePayoutMethodResponseObject;
  payer_name: string | null;
  /** Always `null` after an update. */
  quote: unknown | null;
  /** `created` — saved, no payout has completed through it yet. `active` — a payout through it completed. `broken` — a payout through it failed with an error attributable to the method, and it no longer accepts payouts; a later successful payout returns it to `active`. */
  status: UpdatePayoutMethodResponseStatus;
  /** Machine-readable code for why the method is `broken` — the newest failure recorded through it, whether a payout error or a pre-payout rejection. `null` unless the method is broken, or when it was disabled without a recorded failure. */
  status_reason: string | null;
  supported_payout_method: UpdatePayoutMethodResponseSupportedPayoutMethod | null;
  /** Why this method is unavailable: `destination_retired` means the payout provider stopped offering the destination. Whop may automatically remap an eligible method that was not linked through Plaid to a compatible replacement; otherwise, the account owner must re-add it. `null` means no unavailability reason is known. */
  unavailable_reason: UpdatePayoutMethodResponseUnavailableReason | null;
}
export const UpdatePayoutMethodResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_reference: S.NullOr(S.String),
    bank_verification_state: S.NullOr(
      UpdatePayoutMethodResponseBankVerificationState,
    ),
    created_at: S.String,
    destination_currency: S.String,
    estimated_arrival: S.NullOr(S.Unknown),
    fee_structure: S.NullOr(S.Unknown),
    id: S.String,
    institution_name: S.NullOr(S.String),
    is_clone: S.Boolean,
    is_default: S.Boolean,
    last_paid_out_at: S.NullOr(S.String),
    linked_via_plaid: S.Boolean,
    needs_plaid_reconnect: S.Boolean,
    nickname: S.NullOr(S.String),
    object: UpdatePayoutMethodResponseObject,
    payer_name: S.NullOr(S.String),
    quote: S.NullOr(S.Unknown),
    status: UpdatePayoutMethodResponseStatus,
    status_reason: S.NullOr(S.String),
    supported_payout_method: S.NullOr(
      UpdatePayoutMethodResponseSupportedPayoutMethod,
    ),
    unavailable_reason: S.NullOr(UpdatePayoutMethodResponseUnavailableReason),
  }),
).annotate({
  identifier: "UpdatePayoutMethodResponse",
}) as any as S.Schema<UpdatePayoutMethodResponse>;

export type CancelPayoutError = Forbidden | NotFound | Conflict | WhopOpError;
/** Cancel Payout Cancels a payout that is still in review and returns the funds, fees included, to the balance. A payout can be canceled while its status is `in_review`. A `requested` payout is still being prepared (its funds may be converting) and answers 409 until it reaches review; from `processing` on, the money is on its way and the answer is 409 with error type `not_cancelable`. Canceling a payout that is already canceled succeeds and returns it unchanged. */
export const cancelPayout: API.OperationMethod<
  CancelPayoutRequest,
  CancelPayoutResponse,
  CancelPayoutError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CancelPayoutRequest,
  output: CancelPayoutResponse,
  errors: [Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreatePayoutError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Create Payout Sends money from an account or user balance to a saved payout method for that owner. */
export const createPayout: API.OperationMethod<
  CreatePayoutRequest,
  CreatePayoutResponse,
  CreatePayoutError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreatePayoutRequest,
  output: CreatePayoutResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreatePayoutMethodError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Create Saved Payout Method Saves a new place an account or user can pay out to. Sensitive details are vaulted in transit and never stored raw. */
export const createPayoutMethod: API.OperationMethod<
  CreatePayoutMethodRequest,
  CreatePayoutMethodResponse,
  CreatePayoutMethodError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreatePayoutMethodRequest,
  output: CreatePayoutMethodResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeletePayoutMethodError = Forbidden | NotFound | WhopOpError;
/** Delete Saved Payout Method Deletes a saved payout method so it can no longer receive payouts. */
export const deletePayoutMethod: API.OperationMethod<
  DeletePayoutMethodRequest,
  DeletePayoutMethodResponse,
  DeletePayoutMethodError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeletePayoutMethodRequest,
  output: DeletePayoutMethodResponse,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListPayoutMethodsError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** List Saved Payout Methods Lists the bank accounts, wallets, and crypto addresses an account or user can pay out to, newest first. */
export const listPayoutMethods: API.PaginatedOperationMethod<
  ListPayoutMethodsRequest,
  ListPayoutMethodsResponse,
  ListPayoutMethodsError,
  WhopOpContext,
  ListPayoutMethodsResponseDataItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListPayoutMethodsRequest,
    output: ListPayoutMethodsResponse,
    errors: [BadRequest, Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ListPayoutsError = BadRequest | Forbidden | NotFound | WhopOpError;
/** List Payouts Lists an account's or user's payouts, newest first. */
export const listPayouts: API.PaginatedOperationMethod<
  ListPayoutsRequest,
  ListPayoutsResponse,
  ListPayoutsError,
  WhopOpContext,
  ListPayoutsResponseDataItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListPayoutsRequest,
    output: ListPayoutsResponse,
    errors: [BadRequest, Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ListSupportedPayoutMethodsError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** List Supported Payout Methods Lists the payout methods an account or user is eligible to add. */
export const listSupportedPayoutMethods: API.PaginatedOperationMethod<
  ListSupportedPayoutMethodsRequest,
  ListSupportedPayoutMethodsResponse,
  ListSupportedPayoutMethodsError,
  WhopOpContext,
  ListSupportedPayoutMethodsResponseDataItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListSupportedPayoutMethodsRequest,
    output: ListSupportedPayoutMethodsResponse,
    errors: [BadRequest, Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrievePayoutError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** Retrieve Payout Fetches one payout by its `wdrl_` ID, or by the `cofr_` conversion request ID a stablecoin payout carries as `payout_request_id` — both ids answer with the same payout object. */
export const retrievePayout: API.OperationMethod<
  RetrievePayoutRequest,
  RetrievePayoutResponse,
  RetrievePayoutError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrievePayoutRequest,
  output: RetrievePayoutResponse,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdatePayoutMethodError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** Rename Saved Payout Method Changes the label used to identify a saved payout method. */
export const updatePayoutMethod: API.OperationMethod<
  UpdatePayoutMethodRequest,
  UpdatePayoutMethodResponse,
  UpdatePayoutMethodError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdatePayoutMethodRequest,
  output: UpdatePayoutMethodResponse,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
