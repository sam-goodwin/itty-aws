// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface CompleteFileRequestMultipartPartsItem {
  /** The `ETag` response header from the part's upload. */
  etag: string;
  /** The 1-based index of the part. */
  part_number: number;
}
export const CompleteFileRequestMultipartPartsItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      etag: S.String,
      part_number: S.Number,
    }),
).annotate({
  identifier: "CompleteFileRequestMultipartPartsItem",
}) as any as S.Schema<CompleteFileRequestMultipartPartsItem>;

/** Every uploaded part, in order. */
export type CompleteFileRequestMultipartPartsList =
  Array<CompleteFileRequestMultipartPartsItem>;
export const CompleteFileRequestMultipartPartsList = /*@__PURE__*/ S.Array(
  CompleteFileRequestMultipartPartsItem,
) as any as S.Schema<CompleteFileRequestMultipartPartsList>;

export interface CompleteFileRequest {
  /** The unique identifier of the file, prefixed `file_`. */
  id: string;
  /** Every uploaded part, in order. */
  multipart_parts: CompleteFileRequestMultipartPartsList;
  /** The ID of the multipart upload, returned by Create File. */
  multipart_upload_id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CompleteFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    multipart_parts: CompleteFileRequestMultipartPartsList,
    multipart_upload_id: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/files/{id}/complete", code: 200 })),
).annotate({
  identifier: "CompleteFileRequest",
}) as any as S.Schema<CompleteFileRequest>;

export interface FileMultipartUrl {
  /** The 1-based index of this part within the multipart upload. */
  part_number: number;
  /** The presigned URL to PUT this part's bytes to. */
  url: string;
}
export const FileMultipartUrl = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    part_number: S.Number,
    url: S.String,
  }),
).annotate({
  identifier: "FileMultipartUrl",
}) as any as S.Schema<FileMultipartUrl>;

export type FileMultipartUploadUrlsList = Array<FileMultipartUrl>;
export const FileMultipartUploadUrlsList = /*@__PURE__*/ S.Array(
  FileMultipartUrl,
) as any as S.Schema<FileMultipartUploadUrlsList>;

/** Where the file is in its upload lifecycle. */
export type FileUploadStatus = "pending" | "processing" | "ready" | "failed";
export const FileUploadStatus = /*@__PURE__*/ S.String;

/** `public` files are served via an unsigned CDN URL; `private` files via a signed, expiring URL. */
export type FileVisibility = "public" | "private";
export const FileVisibility = /*@__PURE__*/ S.String;

export interface File {
  /** The file's MIME type, e.g. `application/pdf`. */
  content_type: string | null;
  /** When the file was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** The original filename, including its extension. */
  filename: string | null;
  /** The file's ID, prefixed `file_`. */
  id: string;
  /** The byte size each part (except the last) must be. Present only on create, and only for multipart uploads. */
  multipart_chunk_size?: number | null;
  /** The ID of the multipart upload, passed back to `complete`. Present only on create, and only for multipart uploads. */
  multipart_upload_id?: string | null;
  multipart_upload_urls?: FileMultipartUploadUrlsList | null;
  /** The type of this object, always `file`. */
  object: string;
  /** The file size in bytes. `null` until the upload has finished. */
  size: number | null;
  /** Headers to send with the upload PUT. Present only on create. */
  upload_headers?: unknown;
  /** Where the file is in its upload lifecycle. */
  upload_status: FileUploadStatus;
  /** Presigned URL to PUT the file's bytes to. Present only on create, and only for single-part uploads. */
  upload_url?: string | null;
  /** A URL to download the file: a permanent CDN URL for public files, a signed expiring URL for private ones. `null` until the upload has finished. */
  url: string | null;
  /** `public` files are served via an unsigned CDN URL; `private` files via a signed, expiring URL. */
  visibility: FileVisibility;
}
export const File = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    content_type: S.NullOr(S.String),
    created_at: S.String,
    filename: S.NullOr(S.String),
    id: S.String,
    multipart_chunk_size: S.optional(S.NullOr(S.Number)),
    multipart_upload_id: S.optional(S.NullOr(S.String)),
    multipart_upload_urls: S.optional(S.NullOr(FileMultipartUploadUrlsList)),
    object: S.String,
    size: S.NullOr(S.Number),
    upload_headers: S.optional(S.Unknown),
    upload_status: FileUploadStatus,
    upload_url: S.optional(S.NullOr(S.String)),
    url: S.NullOr(S.String),
    visibility: FileVisibility,
  }),
).annotate({ identifier: "File" }) as any as S.Schema<File>;

/** `public` files are served via an unsigned CDN URL — use for assets anyone may see. `private` files are served via a signed, expiring URL — use for sensitive documents. Defaults to `private`. */
export type CreateFileRequestVisibility = "public" | "private";
export const CreateFileRequestVisibility = /*@__PURE__*/ S.String;

export interface CreateFileRequest {
  /** The file's size in bytes. Required when `multipart` is `true`. Multipart uploads support at most 10,000 parts of 5MB each (about 50 GB). */
  byte_size?: number;
  /** The name of the file including its extension, e.g. `terms.pdf`. */
  filename: string;
  /** Upload the file in 5MB parts. Required for files larger than 5GB; useful above ~100MB. The file must be larger than 5MB. */
  multipart?: boolean;
  /** `public` files are served via an unsigned CDN URL — use for assets anyone may see. `private` files are served via a signed, expiring URL — use for sensitive documents. Defaults to `private`. */
  visibility?: CreateFileRequestVisibility | (string & {});
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    byte_size: S.optional(S.Number),
    filename: S.String,
    multipart: S.optional(S.Boolean),
    visibility: S.optional(CreateFileRequestVisibility),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/files", code: 200 })),
).annotate({
  identifier: "CreateFileRequest",
}) as any as S.Schema<CreateFileRequest>;

export type ListFilesRequestFileIdsList = Array<string>;
export const ListFilesRequestFileIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListFilesRequestFileIdsList>;

export type ListFilesRequestOrder = "created_at";
export const ListFilesRequestOrder = /*@__PURE__*/ S.String;

export type ListFilesRequestDirection = "asc" | "desc";
export const ListFilesRequestDirection = /*@__PURE__*/ S.String;

export interface ListFilesRequest {
  /** The files to return, each prefixed `file_`. Repeat the parameter to pass several, up to 250 per request. Batches of up to 100 answer in one page by default; larger batches page at up to 100 per response. */
  file_ids: ListFilesRequestFileIdsList;
  /** The field to sort by. */
  order?: ListFilesRequestOrder | (string & {});
  /** The sort direction. */
  direction?: ListFilesRequestDirection | (string & {});
  /** The number of files to return. */
  first?: number;
  /** A cursor; returns files after this position. */
  after?: string;
  /** The number of files to return from the end of the range. */
  last?: number;
  /** A cursor; returns files before this position. */
  before?: string;
}
export const ListFilesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    file_ids: ListFilesRequestFileIdsList.pipe(T.Query()),
    order: S.optional(ListFilesRequestOrder.pipe(T.Query())),
    direction: S.optional(ListFilesRequestDirection.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/files", code: 200 })),
).annotate({
  identifier: "ListFilesRequest",
}) as any as S.Schema<ListFilesRequest>;

export type ListFilesResponseDataList = Array<File>;
export const ListFilesResponseDataList = /*@__PURE__*/ S.Array(
  File,
) as any as S.Schema<ListFilesResponseDataList>;

export interface ListFilesResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListFilesResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListFilesResponsePageInfo",
}) as any as S.Schema<ListFilesResponsePageInfo>;

export interface ListFilesResponse {
  data: ListFilesResponseDataList;
  page_info: ListFilesResponsePageInfo;
}
export const ListFilesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListFilesResponseDataList,
    page_info: ListFilesResponsePageInfo,
  }),
).annotate({
  identifier: "ListFilesResponse",
}) as any as S.Schema<ListFilesResponse>;

export interface RetrieveFileRequest {
  /** The unique identifier of the file, prefixed `file_`. */
  id: string;
}
export const RetrieveFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/files/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveFileRequest",
}) as any as S.Schema<RetrieveFileRequest>;

export type CompleteFileError = BadRequest | NotFound | Conflict | WhopOpError;
/** Complete File Multipart Upload Assembles the parts of a multipart upload after every part has been PUT to its presigned URL. Pass the `multipart_upload_id` from Create File and each part's `ETag` response header. */
export const completeFile: API.OperationMethod<
  CompleteFileRequest,
  File,
  CompleteFileError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CompleteFileRequest,
  output: File,
  errors: [BadRequest, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreateFileError = BadRequest | Conflict | WhopOpError;
/** Create File Creates a file and returns a presigned destination to upload its bytes to. PUT the bytes to `upload_url` (single-part), or to each of `multipart_upload_urls` and then call Complete File Multipart Upload. Once the bytes land the file becomes `ready`, and its ID can be attached wherever a file is accepted — account legal documents, dispute evidence documents. */
export const createFile: API.OperationMethod<
  CreateFileRequest,
  File,
  CreateFileError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateFileRequest,
  output: File,
  errors: [BadRequest, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListFilesError = BadRequest | WhopOpError;
/** List Files Returns the files with the given IDs, newest first — fetch a batch in one request instead of retrieving each file individually. Only files you created are returned; IDs that do not exist, or that another credential created, are omitted. A request for up to 100 IDs answers in a single page by default; a larger batch pages at up to 100 files per response — follow `page_info` with the same `file_ids` to walk the rest. */
export const listFiles: API.PaginatedOperationMethod<
  ListFilesRequest,
  ListFilesResponse,
  ListFilesError,
  WhopOpContext,
  File
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListFilesRequest,
    output: ListFilesResponse,
    errors: [BadRequest],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveFileError = NotFound | WhopOpError;
/** Retrieve File Retrieves a file you uploaded — poll it after uploading the bytes to see `upload_status` become `ready`. Only the creator can retrieve a file this way; a file attached to another resource is read through that resource. */
export const retrieveFile: API.OperationMethod<
  RetrieveFileRequest,
  File,
  RetrieveFileError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveFileRequest,
  output: File,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
