// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface RetrieveLedgerAccountRequest {
  /** The identifier to look up the ledger account. Accepts a user ID ('user_xxx'), company ID ('biz_xxx'), or ledger account ID ('ldgr_xxx'). */
  id: string;
}
export const RetrieveLedgerAccountRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/ledger_accounts/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveLedgerAccountRequest",
}) as any as S.Schema<RetrieveLedgerAccountRequest>;

/** The available currencies on the platform */
export type Currencies =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const Currencies = /*@__PURE__*/ S.String;

/** A cached balance for a LedgerAccount in respect to a currency. */
export interface LedgerAccountBalancesItem {
  /** The amount of the balance. */
  balance: number;
  /** The currency of the balance. */
  currency: Currencies;
  /** The amount of the balance that is pending. */
  pending_balance: number;
  /** The amount of the balance that is reserved. */
  reserve_balance: number;
}
export const LedgerAccountBalancesItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    balance: S.Number,
    currency: Currencies,
    pending_balance: S.Number,
    reserve_balance: S.Number,
  }),
).annotate({
  identifier: "LedgerAccountBalancesItem",
}) as any as S.Schema<LedgerAccountBalancesItem>;

/** The balances associated with the account. */
export type LedgerAccountBalancesList = Array<LedgerAccountBalancesItem>;
export const LedgerAccountBalancesList = /*@__PURE__*/ S.Array(
  LedgerAccountBalancesItem,
) as any as S.Schema<LedgerAccountBalancesList>;

/** The types of ledgers that can be created. */
export type LedgerTypes = "primary" | "pool";
export const LedgerTypes = /*@__PURE__*/ S.String;

/** A user account on Whop. Contains profile information, identity details, and social connections. */
export interface LedgerAccountOwnerCase0 {
  /** The unique identifier for the user. */
  id: string;
  /** The user's display name shown on their public profile. */
  name: string | null;
  /** The typename of this object */
  typename: string;
  /** The user's unique username shown on their public profile. */
  username: string;
}
export const LedgerAccountOwnerCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    typename: S.String,
    username: S.String,
  }),
).annotate({
  identifier: "LedgerAccountOwnerCase0",
}) as any as S.Schema<LedgerAccountOwnerCase0>;

/** A company is a seller on Whop. Companies own products, manage members, and receive payouts. */
export interface LedgerAccountOwnerCase1 {
  /** The unique identifier for the company. */
  id: string;
  /** URL slug for the account's store page, e.g. `pickaxe` in whop.com/pickaxe. */
  route: string;
  /** The display name of the company shown to customers. */
  title: string;
  /** The typename of this object */
  typename: string;
}
export const LedgerAccountOwnerCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    route: S.String,
    title: S.String,
    typename: S.String,
  }),
).annotate({
  identifier: "LedgerAccountOwnerCase1",
}) as any as S.Schema<LedgerAccountOwnerCase1>;

/** The owner of the ledger account. */
export type LedgerAccountOwner =
  | LedgerAccountOwnerCase0
  | LedgerAccountOwnerCase1;
export const LedgerAccountOwner =
  /*@__PURE__*/ S.Unknown as any as S.Schema<LedgerAccountOwner>;

/** The different approval statuses an account can have. */
export type PaymentsApprovalStatuses =
  | "pending"
  | "approved"
  | "monitoring"
  | "rejected";
export const PaymentsApprovalStatuses = /*@__PURE__*/ S.String;

/** The physical address associated with this payout account */
export interface LedgerAccountPayoutAccountDetailsAddress {
  /** The city of the address. */
  city: string | null;
  /** The country of the address. */
  country: string | null;
  /** The line 1 of the address. */
  line1: string | null;
  /** The line 2 of the address. */
  line2: string | null;
  /** The postal code of the address. */
  postal_code: string | null;
  /** The state of the address. */
  state: string | null;
}
export const LedgerAccountPayoutAccountDetailsAddress = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      city: S.NullOr(S.String),
      country: S.NullOr(S.String),
      line1: S.NullOr(S.String),
      line2: S.NullOr(S.String),
      postal_code: S.NullOr(S.String),
      state: S.NullOr(S.String),
    }),
).annotate({
  identifier: "LedgerAccountPayoutAccountDetailsAddress",
}) as any as S.Schema<LedgerAccountPayoutAccountDetailsAddress>;

/** The business representative for this payout account */
export interface LedgerAccountPayoutAccountDetailsBusinessRepresentative {
  /** The date of birth of the business representative in ISO 8601 format (YYYY-MM-DD). */
  date_of_birth: string | null;
  /** The first name of the business representative. */
  first_name: string | null;
  /** The last name of the business representative. */
  last_name: string | null;
  /** The middle name of the business representative. */
  middle_name: string | null;
}
export const LedgerAccountPayoutAccountDetailsBusinessRepresentative =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      date_of_birth: S.NullOr(S.String),
      first_name: S.NullOr(S.String),
      last_name: S.NullOr(S.String),
      middle_name: S.NullOr(S.String),
    }),
  ).annotate({
    identifier: "LedgerAccountPayoutAccountDetailsBusinessRepresentative",
  }) as any as S.Schema<LedgerAccountPayoutAccountDetailsBusinessRepresentative>;

/** An error code for a verification attempt. */
export type VerificationErrorCodes =
  | "abandoned"
  | "consent_declined"
  | "country_not_supported"
  | "device_not_supported"
  | "document_expired"
  | "document_type_not_supported"
  | "document_unverified_other"
  | "email_unverified_other"
  | "email_verification_declined"
  | "id_number_insufficient_document_data"
  | "id_number_mismatch"
  | "id_number_unverified_other"
  | "phone_unverified_other"
  | "phone_verification_declined"
  | "selfie_document_missing_photo"
  | "selfie_face_mismatch"
  | "selfie_manipulated"
  | "selfie_unverified_other"
  | "under_supported_age";
export const VerificationErrorCodes = /*@__PURE__*/ S.String;

/** A status for a verification. */
export type VerificationStatuses =
  | "requires_input"
  | "processing"
  | "verified"
  | "canceled"
  | "created"
  | "started"
  | "submitted"
  | "approved"
  | "declined"
  | "resubmission_requested"
  | "expired"
  | "abandoned"
  | "review"
  | "action_required"
  | "manual_review";
export const VerificationStatuses = /*@__PURE__*/ S.String;

/** The latest verification for the connected account. */
export interface LedgerAccountPayoutAccountDetailsLatestVerification {
  /** The numeric id of the verification record. */
  id: string;
  /** The most recent error code returned during verification. Null if no error has occurred. */
  last_error_code: VerificationErrorCodes | null;
  /** A human-readable explanation of the most recent verification error. Null if no error has occurred. */
  last_error_reason: string | null;
  /** The current status of this verification session. */
  status: VerificationStatuses;
}
export const LedgerAccountPayoutAccountDetailsLatestVerification =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      id: S.String,
      last_error_code: S.NullOr(VerificationErrorCodes),
      last_error_reason: S.NullOr(S.String),
      status: VerificationStatuses,
    }),
  ).annotate({
    identifier: "LedgerAccountPayoutAccountDetailsLatestVerification",
  }) as any as S.Schema<LedgerAccountPayoutAccountDetailsLatestVerification>;

/** The granular calculated statuses reflecting payout account KYC and payout readiness. */
export type PayoutAccountCalculatedStatuses =
  | "connected"
  | "disabled"
  | "action_required"
  | "pending_verification"
  | "verification_failed"
  | "manual_review"
  | "denied"
  | "not_started"
  | "blocked_by_parent";
export const PayoutAccountCalculatedStatuses = /*@__PURE__*/ S.String;

/** The payout account associated with the LedgerAccount, if any. */
export interface LedgerAccountPayoutAccountDetails {
  /** The physical address associated with this payout account */
  address: LedgerAccountPayoutAccountDetailsAddress | null;
  /** The company's legal name */
  business_name: string | null;
  /** The business representative for this payout account */
  business_representative: LedgerAccountPayoutAccountDetailsBusinessRepresentative | null;
  /** The email address of the representative */
  email: string | null;
  /** The unique identifier for the payout account. */
  id: string;
  /** The latest verification for the connected account. */
  latest_verification: LedgerAccountPayoutAccountDetailsLatestVerification | null;
  /** The business representative's phone */
  phone: string | null;
  /** The granular calculated status of the payout account reflecting its current KYC and payout readiness state. */
  status: PayoutAccountCalculatedStatuses | null;
}
export const LedgerAccountPayoutAccountDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.NullOr(LedgerAccountPayoutAccountDetailsAddress),
    business_name: S.NullOr(S.String),
    business_representative: S.NullOr(
      LedgerAccountPayoutAccountDetailsBusinessRepresentative,
    ),
    email: S.NullOr(S.String),
    id: S.String,
    latest_verification: S.NullOr(
      LedgerAccountPayoutAccountDetailsLatestVerification,
    ),
    phone: S.NullOr(S.String),
    status: S.NullOr(PayoutAccountCalculatedStatuses),
  }),
).annotate({
  identifier: "LedgerAccountPayoutAccountDetails",
}) as any as S.Schema<LedgerAccountPayoutAccountDetails>;

/** The balance cache associated with the account by currency. */
export interface LedgerAccountTreasuryBalance {
  /** The amount of the balance. */
  balance: number;
  /** The balance converted to USD. */
  balance_usd: number;
  /** The currency of the balance. */
  currency: Currencies;
  /** The amount of the balance that is pending. */
  pending_balance: number;
  /** The amount of the balance that is reserved. */
  reserve_balance: number;
  /** The amount of the balance that is withdrawable. */
  total_withdrawable_balance: number;
}
export const LedgerAccountTreasuryBalance = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    balance: S.Number,
    balance_usd: S.Number,
    currency: Currencies,
    pending_balance: S.Number,
    reserve_balance: S.Number,
    total_withdrawable_balance: S.Number,
  }),
).annotate({
  identifier: "LedgerAccountTreasuryBalance",
}) as any as S.Schema<LedgerAccountTreasuryBalance>;

/** A ledger account represents a financial account on Whop that can hold many balances. */
export interface LedgerAccount {
  /** The balances associated with the account. */
  balances: LedgerAccountBalancesList;
  /** The unique identifier for the ledger account. */
  id: string;
  /** The type of ledger account. */
  ledger_type: LedgerTypes;
  /** The owner of the ledger account. */
  owner: LedgerAccountOwner;
  /** The status of payments approval for the ledger account. */
  payments_approval_status: PaymentsApprovalStatuses | null;
  /** The payout account associated with the LedgerAccount, if any. */
  payout_account_details: LedgerAccountPayoutAccountDetails | null;
  /** The settlement batch most recently posted to this account's available balance, at midnight UTC. Every payment settling in that batch carries the same `settlement_time_at`. */
  settlement_time_at: string | null;
  /** The fee for transfers, if applicable. */
  transfer_fee: number | null;
  /** The balance cache associated with the account by currency. */
  treasury_balance: LedgerAccountTreasuryBalance | null;
}
export const LedgerAccount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    balances: LedgerAccountBalancesList,
    id: S.String,
    ledger_type: LedgerTypes,
    owner: LedgerAccountOwner,
    payments_approval_status: S.NullOr(PaymentsApprovalStatuses),
    payout_account_details: S.NullOr(LedgerAccountPayoutAccountDetails),
    settlement_time_at: S.NullOr(S.String),
    transfer_fee: S.NullOr(S.Number),
    treasury_balance: S.NullOr(LedgerAccountTreasuryBalance),
  }),
).annotate({ identifier: "LedgerAccount" }) as any as S.Schema<LedgerAccount>;

export type RetrieveLedgerAccountError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve ledger account [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing ledger account. Required permissions: - `company:balance:read` - `payout:account:read` */
export const retrieveLedgerAccount: API.OperationMethod<
  RetrieveLedgerAccountRequest,
  LedgerAccount,
  RetrieveLedgerAccountError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveLedgerAccountRequest,
  output: LedgerAccount,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
