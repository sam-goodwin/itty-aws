// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface AttachExperienceRequest {
  /** The unique identifier of the experience to attach. */
  id: string;
  /** The unique identifier of the product to attach the experience to. */
  product_id: string;
}
export const AttachExperienceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    product_id: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/experiences/{id}/attach", code: 200 }),
  ),
).annotate({
  identifier: "AttachExperienceRequest",
}) as any as S.Schema<AttachExperienceRequest>;

/** The icon image for this app, displayed on the app store, product pages, checkout, and as the default icon for experiences using this app. */
export interface ExperienceAppIcon {
  /** A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps. */
  url: string | null;
}
export const ExperienceAppIcon = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ExperienceAppIcon",
}) as any as S.Schema<ExperienceAppIcon>;

/** The app that powers this experience, defining its interface and behavior. */
export interface ExperienceApp {
  /** The icon image for this app, displayed on the app store, product pages, checkout, and as the default icon for experiences using this app. */
  icon: ExperienceAppIcon | null;
  /** The unique identifier for the app. */
  id: string;
  /** The display name of this app shown on the app store and in experience navigation. Maximum 30 characters. */
  name: string;
}
export const ExperienceApp = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    icon: S.NullOr(ExperienceAppIcon),
    id: S.String,
    name: S.String,
  }),
).annotate({ identifier: "ExperienceApp" }) as any as S.Schema<ExperienceApp>;

/** The company that owns this experience. */
export interface ExperienceCompany {
  /** The unique identifier for the company. */
  id: string;
  /** URL slug for the account's store page, e.g. `pickaxe` in whop.com/pickaxe. */
  route: string;
  /** The display name of the company shown to customers. */
  title: string;
}
export const ExperienceCompany = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    route: S.String,
    title: S.String,
  }),
).annotate({
  identifier: "ExperienceCompany",
}) as any as S.Schema<ExperienceCompany>;

/** The custom logo image for this experience. Null if no custom logo has been uploaded. */
export type ExperienceImage = ExperienceAppIcon;
export const ExperienceImage = ExperienceAppIcon;

/** A product is a digital good or service sold on Whop. Products contain plans for pricing and experiences for content delivery. */
export interface ExperienceProductsItem {
  /** The unique identifier for the product. */
  id: string;
  /** URL slug in the product's public link, e.g. `pickaxe-analytics` in whop.com/company/pickaxe-analytics. */
  route: string;
  /** The display name of the product shown to customers on the product page and in search results. */
  title: string;
}
export const ExperienceProductsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    route: S.String,
    title: S.String,
  }),
).annotate({
  identifier: "ExperienceProductsItem",
}) as any as S.Schema<ExperienceProductsItem>;

/** The list of products this experience is attached to, which determines which customers have access. Empty if the experience is only visible to authorized company team members. */
export type ExperienceProductsList = Array<ExperienceProductsItem>;
export const ExperienceProductsList = /*@__PURE__*/ S.Array(
  ExperienceProductsItem,
) as any as S.Schema<ExperienceProductsList>;

/** An experience is a feature or content module within a product, such as a chat, course, or custom app. */
export interface Experience {
  /** The app that powers this experience, defining its interface and behavior. */
  app: ExperienceApp;
  /** The company that owns this experience. */
  company: ExperienceCompany;
  /** The datetime the experience was created. */
  created_at: string;
  /** The unique identifier for the experience. */
  id: string;
  /** The custom logo image for this experience. Null if no custom logo has been uploaded. */
  image: ExperienceAppIcon | null;
  /** Whether this experience is publicly visible to all users, including those without a membership. */
  is_public: boolean;
  /** The display name of this experience shown to users in the product navigation. Maximum 255 characters. */
  name: string;
  /** The sort position of this experience within its section. Lower values appear first. Null if no position has been set. */
  order: string | null;
  /** The list of products this experience is attached to, which determines which customers have access. Empty if the experience is only visible to authorized company team members. */
  products: ExperienceProductsList;
}
export const Experience = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    app: ExperienceApp,
    company: ExperienceCompany,
    created_at: S.String,
    id: S.String,
    image: S.NullOr(ExperienceAppIcon),
    is_public: S.Boolean,
    name: S.String,
    order: S.NullOr(S.String),
    products: ExperienceProductsList,
  }),
).annotate({ identifier: "Experience" }) as any as S.Schema<Experience>;

/** A logo image displayed alongside the experience name. */
export interface CreateExperienceRequestLogo {
  /** The ID of an existing file object. */
  id: string;
}
export const CreateExperienceRequestLogo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "CreateExperienceRequestLogo",
}) as any as S.Schema<CreateExperienceRequestLogo>;

export interface CreateExperienceRequest {
  /** The unique identifier of the app that powers this experience. */
  app_id: string;
  /** The unique identifier of the company to create this experience for. */
  company_id: string;
  /** Whether the experience is publicly accessible without a membership. */
  is_public?: boolean | null;
  /** A logo image displayed alongside the experience name. */
  logo?: CreateExperienceRequestLogo | null;
  /** The display name of the experience. Defaults to the app's name if not provided. */
  name?: string | null;
  /** Whether Whop app notifications are enabled for this experience. Webhooks still fire. */
  notifications_enabled?: boolean | null;
  /** The unique identifier of the section to place the experience in. */
  section_id?: string | null;
}
export const CreateExperienceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    app_id: S.String,
    company_id: S.String,
    is_public: S.optional(S.NullOr(S.Boolean)),
    logo: S.optional(S.NullOr(CreateExperienceRequestLogo)),
    name: S.optional(S.NullOr(S.String)),
    notifications_enabled: S.optional(S.NullOr(S.Boolean)),
    section_id: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "POST", uri: "/experiences", code: 200 })),
).annotate({
  identifier: "CreateExperienceRequest",
}) as any as S.Schema<CreateExperienceRequest>;

export interface DeleteExperienceRequest {
  /** The unique identifier of the experience to delete. */
  id: string;
}
export const DeleteExperienceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/experiences/{id}", code: 200 })),
).annotate({
  identifier: "DeleteExperienceRequest",
}) as any as S.Schema<DeleteExperienceRequest>;

export type DeleteExperienceResponse = boolean;
export const DeleteExperienceResponse = /*@__PURE__*/ S.suspend(() =>
  S.Boolean.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "DeleteExperienceResponse",
}) as any as S.Schema<DeleteExperienceResponse>;

export interface DetachExperienceRequest {
  /** The unique identifier of the experience to detach. */
  id: string;
  /** The unique identifier of the product to detach the experience from. */
  product_id: string;
}
export const DetachExperienceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    product_id: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/experiences/{id}/detach", code: 200 }),
  ),
).annotate({
  identifier: "DetachExperienceRequest",
}) as any as S.Schema<DetachExperienceRequest>;

export interface DuplicateExperienceRequest {
  /** The unique identifier of the experience to duplicate. */
  id: string;
  /** The display name for the duplicated experience. Defaults to the original experience's name. */
  name?: string | null;
}
export const DuplicateExperienceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    name: S.optional(S.NullOr(S.String)),
  }).pipe(
    T.Http({ method: "POST", uri: "/experiences/{id}/duplicate", code: 200 }),
  ),
).annotate({
  identifier: "DuplicateExperienceRequest",
}) as any as S.Schema<DuplicateExperienceRequest>;

export interface ListExperienceRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  company_id: string;
  product_id?: string;
  app_id?: string;
  created_before?: string;
  created_after?: string;
}
export const ListExperienceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    company_id: S.String.pipe(T.Query()),
    product_id: S.optional(S.String.pipe(T.Query())),
    app_id: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/experiences", code: 200 })),
).annotate({
  identifier: "ListExperienceRequest",
}) as any as S.Schema<ListExperienceRequest>;

/** The icon image for this app, displayed on the app store, product pages, checkout, and as the default icon for experiences using this app. */
export type ExperienceListItemAppIcon = ExperienceAppIcon;
export const ExperienceListItemAppIcon = ExperienceAppIcon;

/** The app that powers this experience, defining its interface and behavior. */
export type ExperienceListItemApp = ExperienceApp;
export const ExperienceListItemApp = ExperienceApp;

/** The company that owns this experience. */
export type ExperienceListItemCompany = ExperienceCompany;
export const ExperienceListItemCompany = ExperienceCompany;

/** The custom logo image for this experience. Null if no custom logo has been uploaded. */
export type ExperienceListItemImage = ExperienceAppIcon;
export const ExperienceListItemImage = ExperienceAppIcon;

/** An experience is a feature or content module within a product, such as a chat, course, or custom app. */
export interface ExperienceListItem {
  /** The app that powers this experience, defining its interface and behavior. */
  app: ExperienceApp;
  /** The company that owns this experience. */
  company: ExperienceCompany;
  /** The datetime the experience was created. */
  created_at: string;
  /** The unique identifier for the experience. */
  id: string;
  /** The custom logo image for this experience. Null if no custom logo has been uploaded. */
  image: ExperienceAppIcon | null;
  /** Whether this experience is publicly visible to all users, including those without a membership. */
  is_public: boolean;
  /** The display name of this experience shown to users in the product navigation. Maximum 255 characters. */
  name: string;
  /** The sort position of this experience within its section. Lower values appear first. Null if no position has been set. */
  order: string | null;
}
export const ExperienceListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    app: ExperienceApp,
    company: ExperienceCompany,
    created_at: S.String,
    id: S.String,
    image: S.NullOr(ExperienceAppIcon),
    is_public: S.Boolean,
    name: S.String,
    order: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ExperienceListItem",
}) as any as S.Schema<ExperienceListItem>;

/** A list of nodes. */
export type ListExperienceResponseDataList = Array<ExperienceListItem>;
export const ListExperienceResponseDataList = /*@__PURE__*/ S.Array(
  ExperienceListItem,
) as any as S.Schema<ListExperienceResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListExperienceResponse {
  /** A list of nodes. */
  data: ListExperienceResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListExperienceResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListExperienceResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListExperienceResponse",
}) as any as S.Schema<ListExperienceResponse>;

export interface RetrieveExperienceRequest {
  /** The unique identifier of the experience. */
  id: string;
}
export const RetrieveExperienceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/experiences/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveExperienceRequest",
}) as any as S.Schema<RetrieveExperienceRequest>;

/** The different access levels for experiences (PUBLIC IS NEVER USED ANYMORE). */
export type ExperienceAccessLevels = "public" | "private";
export const ExperienceAccessLevels = /*@__PURE__*/ S.String;

/** A logo image displayed alongside the experience name. */
export type UpdateExperienceRequestLogo = CreateExperienceRequestLogo;
export const UpdateExperienceRequestLogo = CreateExperienceRequestLogo;

export interface UpdateExperienceRequest {
  /** The unique identifier of the experience to update. */
  id: string;
  /** The access level of the experience. */
  access_level?: ExperienceAccessLevels | (string & {}) | null;
  /** Whether the experience is publicly accessible without a membership. */
  is_public?: boolean | null;
  /** A logo image displayed alongside the experience name. */
  logo?: CreateExperienceRequestLogo | null;
  /** The display name of the experience. */
  name?: string | null;
  /** Whether Whop app notifications are enabled for this experience. Webhooks still fire. */
  notifications_enabled?: boolean | null;
  /** The position of the experience within its section for display ordering. */
  order?: string | null;
  /** The unique identifier of the section to move the experience into. */
  section_id?: string | null;
}
export const UpdateExperienceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    access_level: S.optional(S.NullOr(ExperienceAccessLevels)),
    is_public: S.optional(S.NullOr(S.Boolean)),
    logo: S.optional(S.NullOr(CreateExperienceRequestLogo)),
    name: S.optional(S.NullOr(S.String)),
    notifications_enabled: S.optional(S.NullOr(S.Boolean)),
    order: S.optional(S.NullOr(S.String)),
    section_id: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "PATCH", uri: "/experiences/{id}", code: 200 })),
).annotate({
  identifier: "UpdateExperienceRequest",
}) as any as S.Schema<UpdateExperienceRequest>;

export type AttachExperienceError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Attach experience [Legacy API — https://docs.whop.com/api-reference] Attach an experience to a product, making it accessible to the product's customers. Required permissions: - `experience:attach` */
export const attachExperience: API.OperationMethod<
  AttachExperienceRequest,
  Experience,
  AttachExperienceError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AttachExperienceRequest,
  output: Experience,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreateExperienceError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create experience [Legacy API — https://docs.whop.com/api-reference] Required permissions: - `experience:create` */
export const createExperience: API.OperationMethod<
  CreateExperienceRequest,
  Experience,
  CreateExperienceError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateExperienceRequest,
  output: Experience,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteExperienceError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Delete experience [Legacy API — https://docs.whop.com/api-reference] Required permissions: - `experience:delete` */
export const deleteExperience: API.OperationMethod<
  DeleteExperienceRequest,
  DeleteExperienceResponse,
  DeleteExperienceError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteExperienceRequest,
  output: DeleteExperienceResponse,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DetachExperienceError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Detach experience [Legacy API — https://docs.whop.com/api-reference] Detach an experience from a product, removing customer access to it through that product. Required permissions: - `experience:detach` */
export const detachExperience: API.OperationMethod<
  DetachExperienceRequest,
  Experience,
  DetachExperienceError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DetachExperienceRequest,
  output: Experience,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DuplicateExperienceError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Duplicate experience [Legacy API — https://docs.whop.com/api-reference] Duplicates an existing experience. The name will be copied, unless provided. The new experience will be attached to the same products as the original experience. If duplicating a Forum or Chat experience, the new experience will have the same settings as the original experience, e.g. who can post, who can comment, etc. No content, e.g. posts, messages, lessons from within the original experience will be copied. Required permissions: - `experience:create` */
export const duplicateExperience: API.OperationMethod<
  DuplicateExperienceRequest,
  Experience,
  DuplicateExperienceError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DuplicateExperienceRequest,
  output: Experience,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListExperienceError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List experiences [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of experiences belonging to a company, with optional filtering by product and app. */
export const listExperience: API.PaginatedOperationMethod<
  ListExperienceRequest,
  ListExperienceResponse,
  ListExperienceError,
  WhopOpContext,
  ExperienceListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListExperienceRequest,
    output: ListExperienceResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveExperienceError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve experience [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing experience. */
export const retrieveExperience: API.OperationMethod<
  RetrieveExperienceRequest,
  Experience,
  RetrieveExperienceError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveExperienceRequest,
  output: Experience,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateExperienceError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update experience [Legacy API — https://docs.whop.com/api-reference] Required permissions: - `experience:update` */
export const updateExperience: API.OperationMethod<
  UpdateExperienceRequest,
  Experience,
  UpdateExperienceError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateExperienceRequest,
  output: Experience,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
