// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** The platform to connect the social account on. Supported options are `meta_business` and `tiktok`. */
export type ConnectSocialAccountRequestPlatform = "meta_business" | "tiktok";
export const ConnectSocialAccountRequestPlatform = /*@__PURE__*/ S.String;

export type ConnectSocialAccountRequestScopesItem = "advertise";
export const ConnectSocialAccountRequestScopesItem = /*@__PURE__*/ S.String;

/** Capabilities to grant for the connected social account. Use `advertise` when connecting a Meta Business or TikTok account for ads. */
export type ConnectSocialAccountRequestScopesList = Array<
  ConnectSocialAccountRequestScopesItem | (string & {})
>;
export const ConnectSocialAccountRequestScopesList = /*@__PURE__*/ S.Array(
  ConnectSocialAccountRequestScopesItem,
) as any as S.Schema<ConnectSocialAccountRequestScopesList>;

export interface ConnectSocialAccountRequest {
  /** The Account (biz_ identifier) to connect the social account for. An account-scoped API key may omit this to default to its own account. */
  account_id?: string;
  /** The platform to connect the social account on. Supported options are `meta_business` and `tiktok`. */
  platform: ConnectSocialAccountRequestPlatform | (string & {});
  /** The Whop URL to redirect the user to after they finish connecting. */
  redirect_url?: string;
  /** Capabilities to grant for the connected social account. Use `advertise` when connecting a Meta Business or TikTok account for ads. */
  scopes?: ConnectSocialAccountRequestScopesList;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const ConnectSocialAccountRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    platform: ConnectSocialAccountRequestPlatform,
    redirect_url: S.optional(S.String),
    scopes: S.optional(ConnectSocialAccountRequestScopesList),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/social_accounts/connect", code: 200 }),
  ),
).annotate({
  identifier: "ConnectSocialAccountRequest",
}) as any as S.Schema<ConnectSocialAccountRequest>;

export interface ConnectSocialAccountResponse {
  /** The OAuth authorization URL to redirect the user to. */
  authorize_url: string;
}
export const ConnectSocialAccountResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    authorize_url: S.String,
  }),
).annotate({
  identifier: "ConnectSocialAccountResponse",
}) as any as S.Schema<ConnectSocialAccountResponse>;

/** The platform to create the social account on. `facebook` requires the account's `banner_image`, `logo`, and `description`; configure them with [Update Account](/api-reference/beta/accounts/update-account). */
export type CreateSocialAccountRequestPlatform = "facebook";
export const CreateSocialAccountRequestPlatform = /*@__PURE__*/ S.String;

export interface CreateSocialAccountRequest {
  /** The Account (biz_ identifier) to create the social account for. An account-scoped API key may omit this to default to its own account. Account API keys cannot update their own account's branding through Update Account; use a user-authenticated path. */
  account_id?: string;
  /** The platform to create the social account on. `facebook` requires the account's `banner_image`, `logo`, and `description`; configure them with [Update Account](/api-reference/beta/accounts/update-account). */
  platform: CreateSocialAccountRequestPlatform | (string & {});
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateSocialAccountRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    platform: CreateSocialAccountRequestPlatform,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/social_accounts", code: 200 })),
).annotate({
  identifier: "CreateSocialAccountRequest",
}) as any as S.Schema<CreateSocialAccountRequest>;

/** The platform the parent social account exists on. */
export type SocialAccountParentPlatform =
  | "x"
  | "instagram"
  | "youtube"
  | "tiktok"
  | "facebook"
  | "discord"
  | "telegram";
export const SocialAccountParentPlatform = /*@__PURE__*/ S.String;

export interface SocialAccountParent {
  /** The platform-specific ID for the parent social account. */
  external_id: string | null;
  /** Social account ID, prefixed `sacc_`. */
  id: string;
  /** The display name of the parent social account on the platform. */
  name: string | null;
  /** The platform the parent social account exists on. */
  platform: SocialAccountParentPlatform;
  /** The URL where the profile picture of the parent social account can be accessed. */
  profile_picture_url: string | null;
  /** The username of the parent social account on the platform. */
  username: string | null;
  /** Whether the parent social account is verified on the platform. */
  verified: boolean;
}
export const SocialAccountParent = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    external_id: S.NullOr(S.String),
    id: S.String,
    name: S.NullOr(S.String),
    platform: SocialAccountParentPlatform,
    profile_picture_url: S.NullOr(S.String),
    username: S.NullOr(S.String),
    verified: S.Boolean,
  }),
).annotate({
  identifier: "SocialAccountParent",
}) as any as S.Schema<SocialAccountParent>;

/** The platform the social account exists on. */
export type SocialAccountPlatform =
  | "x"
  | "instagram"
  | "youtube"
  | "tiktok"
  | "facebook"
  | "discord"
  | "telegram";
export const SocialAccountPlatform = /*@__PURE__*/ S.String;

export type SocialAccountScopesList = Array<string>;
export const SocialAccountScopesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SocialAccountScopesList>;

export interface SocialAccount {
  /** Why this social account currently can't be used for advertising — a failed share or a Meta-side restriction. Null when the account is healthy. */
  error: string | null;
  /** The platform-specific ID for this social account. */
  external_id: string | null;
  /** Unique identifier for the social account. */
  id: string;
  /** The display name of the social account on the platform. */
  name: string | null;
  /** The social account this one belongs to on the platform, such as the Facebook page that owns an Instagram account. Null when the social account stands on its own. */
  parent_social_account: SocialAccountParent | null;
  /** The platform the social account exists on. */
  platform: SocialAccountPlatform;
  /** The URL where the profile picture of the social account can be accessed. */
  profile_picture_url: string | null;
  scopes: SocialAccountScopesList;
  /** The URL where the social account can be accessed on the platform. Null while a Whop-owned page is still being provisioned. */
  url: string | null;
  /** The username of the social account on the platform. Null while a Whop-owned page is still being provisioned. */
  username: string | null;
  /** Whether the social account is verified on the platform. */
  verified: boolean;
}
export const SocialAccount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    error: S.NullOr(S.String),
    external_id: S.NullOr(S.String),
    id: S.String,
    name: S.NullOr(S.String),
    parent_social_account: S.NullOr(SocialAccountParent),
    platform: SocialAccountPlatform,
    profile_picture_url: S.NullOr(S.String),
    scopes: SocialAccountScopesList,
    url: S.NullOr(S.String),
    username: S.NullOr(S.String),
    verified: S.Boolean,
  }),
).annotate({ identifier: "SocialAccount" }) as any as S.Schema<SocialAccount>;

export interface DeleteSocialAccountRequest {
  /** The ID of the social account to disconnect. */
  id: string;
  /** The Account that the social account is connected to. Provide either this or user_id. */
  account_id?: string;
  /** The User that the social account is connected to. Provide either this or account_id. */
  user_id?: string;
}
export const DeleteSocialAccountRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "DELETE", uri: "/social_accounts/{id}", code: 200 }),
  ),
).annotate({
  identifier: "DeleteSocialAccountRequest",
}) as any as S.Schema<DeleteSocialAccountRequest>;

export interface DeleteSocialAccountResponse {
  /** Always true. */
  deleted: boolean;
  /** ID of the disconnected social account. */
  id: string;
}
export const DeleteSocialAccountResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "DeleteSocialAccountResponse",
}) as any as S.Schema<DeleteSocialAccountResponse>;

export interface ListSocialAccountLeadFormsRequest {
  /** The social account (a sacc_ identifier) whose lead forms to list. */
  id: string;
  /** The Account (a biz_ identifier) the social account is connected to. */
  account_id: string;
}
export const ListSocialAccountLeadFormsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    account_id: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/social_accounts/{id}/lead_forms",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListSocialAccountLeadFormsRequest",
}) as any as S.Schema<ListSocialAccountLeadFormsRequest>;

export interface AdLeadFormCompletion {
  /** Text of the follow-up button. */
  button_text: string | null;
  /** Body text under the headline. */
  description: string | null;
  /** Headline of the completion screen. */
  headline: string | null;
  /** Website the follow-up button opens. `null` when the screen has no button. */
  url: string | null;
}
export const AdLeadFormCompletion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    button_text: S.NullOr(S.String),
    description: S.NullOr(S.String),
    headline: S.NullOr(S.String),
    url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AdLeadFormCompletion",
}) as any as S.Schema<AdLeadFormCompletion>;

export interface AdLeadFormDisclaimerCheckbox {
  /** Whether the checkbox starts ticked. */
  checked_by_default: boolean | null;
  /** Stable identifier consent responses are stored under. */
  key: string | null;
  /** Whether the checkbox must be ticked to submit the form. */
  required: boolean | null;
  /** Consent text next to the checkbox. */
  text: string;
}
export const AdLeadFormDisclaimerCheckbox = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    checked_by_default: S.NullOr(S.Boolean),
    key: S.NullOr(S.String),
    required: S.NullOr(S.Boolean),
    text: S.String,
  }),
).annotate({
  identifier: "AdLeadFormDisclaimerCheckbox",
}) as any as S.Schema<AdLeadFormDisclaimerCheckbox>;

export type AdLeadFormDisclaimerCheckboxesList =
  Array<AdLeadFormDisclaimerCheckbox>;
export const AdLeadFormDisclaimerCheckboxesList = /*@__PURE__*/ S.Array(
  AdLeadFormDisclaimerCheckbox,
) as any as S.Schema<AdLeadFormDisclaimerCheckboxesList>;

export interface AdLeadFormDisclaimer {
  /** Disclaimer text. */
  body: string | null;
  checkboxes: AdLeadFormDisclaimerCheckboxesList;
  /** Disclaimer title. */
  title: string | null;
}
export const AdLeadFormDisclaimer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: S.NullOr(S.String),
    checkboxes: AdLeadFormDisclaimerCheckboxesList,
    title: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AdLeadFormDisclaimer",
}) as any as S.Schema<AdLeadFormDisclaimer>;

/** `more_volume` is quickest to submit; `higher_intent` adds a confirmation step before submission. */
export type SocialAccountLeadFormFormType = "more_volume" | "higher_intent";
export const SocialAccountLeadFormFormType = /*@__PURE__*/ S.String;

export interface AdLeadFormIntro {
  /** Body text under the headline. */
  description: string | null;
  /** Headline of the intro screen. */
  headline: string | null;
}
export const AdLeadFormIntro = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.NullOr(S.String),
    headline: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AdLeadFormIntro",
}) as any as S.Schema<AdLeadFormIntro>;

export type SocialAccountLeadFormQuestionLabelsList = Array<string>;
export const SocialAccountLeadFormQuestionLabelsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SocialAccountLeadFormQuestionLabelsList>;

/** What happens when the choice is selected. */
export type AdLeadFormOptionLogicAction =
  | "go_to_question"
  | "submit_form"
  | "close_form";
export const AdLeadFormOptionLogicAction = /*@__PURE__*/ S.String;

export interface AdLeadFormOptionLogic {
  /** What happens when the choice is selected. */
  action: AdLeadFormOptionLogicAction;
  /** Zero-based index of the ending screen to jump to. */
  target_end_page_index?: number;
  /** Zero-based index of the question to jump to, for `go_to_question`. */
  target_question_index?: number;
}
export const AdLeadFormOptionLogic = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: AdLeadFormOptionLogicAction,
    target_end_page_index: S.optional(S.Number),
    target_question_index: S.optional(S.Number),
  }),
).annotate({
  identifier: "AdLeadFormOptionLogic",
}) as any as S.Schema<AdLeadFormOptionLogic>;

export interface AdLeadFormQuestionOption {
  /** Stable identifier the choice's answers are stored under. Absent for simple choices. */
  key?: string | null;
  /** Where the form goes when this choice is selected. Absent when the form just continues to the next question. */
  logic?: AdLeadFormOptionLogic;
  /** Choice text shown to the person. */
  value: string;
}
export const AdLeadFormQuestionOption = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.optional(S.NullOr(S.String)),
    logic: S.optional(AdLeadFormOptionLogic),
    value: S.String,
  }),
).annotate({
  identifier: "AdLeadFormQuestionOption",
}) as any as S.Schema<AdLeadFormQuestionOption>;

export type AdLeadFormQuestionOptionsList = Array<AdLeadFormQuestionOption>;
export const AdLeadFormQuestionOptionsList = /*@__PURE__*/ S.Array(
  AdLeadFormQuestionOption,
) as any as S.Schema<AdLeadFormQuestionOptionsList>;

export interface AdLeadFormQuestion {
  /** Answer format for `custom` questions: `short_answer`, `multiple_choice`, or `appointment`. Absent otherwise. */
  format?: string;
  /** Question text for `custom` questions. Absent for standard prefill questions. */
  label?: string;
  options?: AdLeadFormQuestionOptionsList;
  /** Question type: a standard prefill type such as `email`, `phone`, or `full_name`, or `custom` for your own question. */
  type: string;
}
export const AdLeadFormQuestion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    format: S.optional(S.String),
    label: S.optional(S.String),
    options: S.optional(AdLeadFormQuestionOptionsList),
    type: S.String,
  }),
).annotate({
  identifier: "AdLeadFormQuestion",
}) as any as S.Schema<AdLeadFormQuestion>;

export type SocialAccountLeadFormQuestionsList = Array<AdLeadFormQuestion>;
export const SocialAccountLeadFormQuestionsList = /*@__PURE__*/ S.Array(
  AdLeadFormQuestion,
) as any as S.Schema<SocialAccountLeadFormQuestionsList>;

export interface SocialAccountLeadForm {
  /** Screen shown after the form is submitted. `null` when the form has none. */
  completion: AdLeadFormCompletion | null;
  /** When the form was created, as an ISO 8601 timestamp. */
  created_at: string | null;
  /** Custom consent disclaimer shown before submission. `null` when the form has none. */
  disclaimer: AdLeadFormDisclaimer | null;
  /** `more_volume` is quickest to submit; `higher_intent` adds a confirmation step before submission. */
  form_type: SocialAccountLeadFormFormType;
  /** The ad platform's identifier for the form. Use it as lead_gen_form_id on an ad to reuse the form. */
  id: string;
  /** Intro screen shown before the questions. `null` when the form has none. */
  intro: AdLeadFormIntro | null;
  /** Language the form is shown in, such as en_US. */
  locale: string | null;
  /** Advertiser-facing form name. */
  name: string | null;
  /** Custom link text for the privacy policy. `null` when the default is used. */
  privacy_policy_link_text: string | null;
  /** Privacy policy URL configured on the form. */
  privacy_policy_url: string | null;
  question_labels: SocialAccountLeadFormQuestionLabelsList;
  questions: SocialAccountLeadFormQuestionsList;
}
export const SocialAccountLeadForm = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    completion: S.NullOr(AdLeadFormCompletion),
    created_at: S.NullOr(S.String),
    disclaimer: S.NullOr(AdLeadFormDisclaimer),
    form_type: SocialAccountLeadFormFormType,
    id: S.String,
    intro: S.NullOr(AdLeadFormIntro),
    locale: S.NullOr(S.String),
    name: S.NullOr(S.String),
    privacy_policy_link_text: S.NullOr(S.String),
    privacy_policy_url: S.NullOr(S.String),
    question_labels: SocialAccountLeadFormQuestionLabelsList,
    questions: SocialAccountLeadFormQuestionsList,
  }),
).annotate({
  identifier: "SocialAccountLeadForm",
}) as any as S.Schema<SocialAccountLeadForm>;

export type ListSocialAccountLeadFormsResponseDataList =
  Array<SocialAccountLeadForm>;
export const ListSocialAccountLeadFormsResponseDataList = /*@__PURE__*/ S.Array(
  SocialAccountLeadForm,
) as any as S.Schema<ListSocialAccountLeadFormsResponseDataList>;

export interface ListSocialAccountLeadFormsResponse {
  data: ListSocialAccountLeadFormsResponseDataList;
}
export const ListSocialAccountLeadFormsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListSocialAccountLeadFormsResponseDataList,
  }),
).annotate({
  identifier: "ListSocialAccountLeadFormsResponse",
}) as any as S.Schema<ListSocialAccountLeadFormsResponse>;

export interface ListSocialAccountPostsRequest {
  /** The social account (a sacc_ identifier) whose posts to list. */
  id: string;
  /** The Account (a biz_ identifier) the social account is connected to. */
  account_id: string;
  /** Return only the single post with this platform id, instead of the full list. */
  post_id?: string;
  /** The number of posts to return. */
  first?: number;
  /** Cursor to fetch the page after (from page_info.end_cursor). */
  after?: string;
}
export const ListSocialAccountPostsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    account_id: S.String.pipe(T.Query()),
    post_id: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/social_accounts/{id}/posts", code: 200 }),
  ),
).annotate({
  identifier: "ListSocialAccountPostsRequest",
}) as any as S.Schema<ListSocialAccountPostsRequest>;

/** The post's call-to-action button, for example shop_now (Facebook only; null for Instagram and TikTok). */
export type SocialAccountPostCallToAction =
  | "learn_more"
  | "shop_now"
  | "sign_up"
  | "subscribe"
  | "get_started"
  | "book_now"
  | "apply_now"
  | "contact_us"
  | "download"
  | "order_now"
  | "buy_now"
  | "get_quote"
  | "message_page"
  | "whatsapp_message"
  | "instagram_message"
  | "call_now"
  | "get_directions"
  | "send_updates"
  | "get_offer"
  | "watch_more"
  | "listen_now"
  | "play_game"
  | "open_link"
  | "no_button"
  | "get_offer_view"
  | "get_event_tickets"
  | "see_menu"
  | "request_time"
  | "event_rsvp"
  | "see_details"
  | "view_instagram_profile";
export const SocialAccountPostCallToAction = /*@__PURE__*/ S.String;

/** Ways this post can't be used on an ad. `lead_form_ineligible`: post can't be used in lead form ads. `promotion_ineligible`: the platform won't promote this post at all — always present when that's true, alongside a reason code when one is identified. `copyrighted_music`: post uses music the platform does not allow for ads. `messenger_destination`, `instagram_destination`, `whatsapp_destination`: the post's own button opens that chat app, so the post can only run in an ad group that sends people to the same one — always accompanied by `lead_form_ineligible`. Empty when the post has no restrictions. */
export type SocialAccountPostRestrictionsItem =
  | "lead_form_ineligible"
  | "promotion_ineligible"
  | "copyrighted_music"
  | "messenger_destination"
  | "instagram_destination"
  | "whatsapp_destination";
export const SocialAccountPostRestrictionsItem = /*@__PURE__*/ S.String;

export type SocialAccountPostRestrictionsList =
  Array<SocialAccountPostRestrictionsItem>;
export const SocialAccountPostRestrictionsList = /*@__PURE__*/ S.Array(
  SocialAccountPostRestrictionsItem,
) as any as S.Schema<SocialAccountPostRestrictionsList>;

export interface SocialAccountPost {
  /** The post's call-to-action button, for example shop_now (Facebook only; null for Instagram and TikTok). */
  call_to_action: SocialAccountPostCallToAction | null;
  /** The URL the post's call-to-action drives to (Facebook only; null for Instagram and TikTok). */
  destination_url: string | null;
  /** An iframe-embeddable URL for previewing the post inline (the platform's player or post embed). For TikTok this is the only preview, since media_url is null; for Facebook and Instagram it supplements media_url. Null when no public embed is available. */
  embed_url: string | null;
  /** The platform's own identifier for the post or media. Use it to reference the post on an ad. */
  id: string;
  /** The URL of the post's media — the image for image posts, the playable video file for video posts. Null for TikTok, which exposes no raw file (use embed_url). Meta URLs are signed and expire after roughly 24 hours, so don't store them. */
  media_url: string | null;
  restrictions: SocialAccountPostRestrictionsList;
  /** Poster image for video posts (always set for TikTok, which is video-only); null for image posts, where media_url is already the image. */
  thumbnail_url: string | null;
}
export const SocialAccountPost = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    call_to_action: S.NullOr(SocialAccountPostCallToAction),
    destination_url: S.NullOr(S.String),
    embed_url: S.NullOr(S.String),
    id: S.String,
    media_url: S.NullOr(S.String),
    restrictions: SocialAccountPostRestrictionsList,
    thumbnail_url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "SocialAccountPost",
}) as any as S.Schema<SocialAccountPost>;

export type ListSocialAccountPostsResponseDataList = Array<SocialAccountPost>;
export const ListSocialAccountPostsResponseDataList = /*@__PURE__*/ S.Array(
  SocialAccountPost,
) as any as S.Schema<ListSocialAccountPostsResponseDataList>;

export interface ListSocialAccountPostsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
}
export const ListSocialAccountPostsResponsePageInfo = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      end_cursor: S.NullOr(S.String),
      has_next_page: S.Boolean,
    }),
).annotate({
  identifier: "ListSocialAccountPostsResponsePageInfo",
}) as any as S.Schema<ListSocialAccountPostsResponsePageInfo>;

export interface ListSocialAccountPostsResponse {
  data: ListSocialAccountPostsResponseDataList;
  page_info: ListSocialAccountPostsResponsePageInfo;
}
export const ListSocialAccountPostsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListSocialAccountPostsResponseDataList,
    page_info: ListSocialAccountPostsResponsePageInfo,
  }),
).annotate({
  identifier: "ListSocialAccountPostsResponse",
}) as any as S.Schema<ListSocialAccountPostsResponse>;

export type ListSocialAccountsRequestPlatform =
  | "x"
  | "instagram"
  | "youtube"
  | "tiktok"
  | "facebook"
  | "discord"
  | "telegram";
export const ListSocialAccountsRequestPlatform = /*@__PURE__*/ S.String;

export type ListSocialAccountsRequestScopesItem = "advertise";
export const ListSocialAccountsRequestScopesItem = /*@__PURE__*/ S.String;

export type ListSocialAccountsRequestScopesList = Array<
  ListSocialAccountsRequestScopesItem | (string & {})
>;
export const ListSocialAccountsRequestScopesList = /*@__PURE__*/ S.Array(
  ListSocialAccountsRequestScopesItem,
) as any as S.Schema<ListSocialAccountsRequestScopesList>;

export type ListSocialAccountsRequestOrder = "display_order" | "created_at";
export const ListSocialAccountsRequestOrder = /*@__PURE__*/ S.String;

export type ListSocialAccountsRequestDirection = "asc" | "desc";
export const ListSocialAccountsRequestDirection = /*@__PURE__*/ S.String;

export interface ListSocialAccountsRequest {
  /** The Account that the social accounts are connected to. Provide either this or user_id. */
  account_id?: string;
  /** The User that the social accounts are connected to. Provide either this or account_id. */
  user_id?: string;
  /** Only return social accounts for the platform that is specified. */
  platform?: ListSocialAccountsRequestPlatform | (string & {});
  /** Only return social accounts that are verified on the platform. */
  verified?: boolean;
  /** Only return social accounts that have these scopes. */
  scopes?: ListSocialAccountsRequestScopesList;
  /** The number of social accounts to return. */
  first?: number;
  /** Cursor to fetch the page after (from page_info.end_cursor). */
  after?: string;
  /** The number of social accounts to return from the end of the range. */
  last?: number;
  /** Cursor to fetch the page before (from page_info.start_cursor). */
  before?: string;
  /** The field to sort social accounts by. */
  order?: ListSocialAccountsRequestOrder | (string & {});
  /** Sort direction. */
  direction?: ListSocialAccountsRequestDirection | (string & {});
}
export const ListSocialAccountsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
    platform: S.optional(ListSocialAccountsRequestPlatform.pipe(T.Query())),
    verified: S.optional(S.Boolean.pipe(T.Query())),
    scopes: S.optional(ListSocialAccountsRequestScopesList.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListSocialAccountsRequestOrder.pipe(T.Query())),
    direction: S.optional(ListSocialAccountsRequestDirection.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/social_accounts", code: 200 })),
).annotate({
  identifier: "ListSocialAccountsRequest",
}) as any as S.Schema<ListSocialAccountsRequest>;

export type ListSocialAccountsResponseDataList = Array<SocialAccount>;
export const ListSocialAccountsResponseDataList = /*@__PURE__*/ S.Array(
  SocialAccount,
) as any as S.Schema<ListSocialAccountsResponseDataList>;

export interface ListSocialAccountsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListSocialAccountsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListSocialAccountsResponsePageInfo",
}) as any as S.Schema<ListSocialAccountsResponsePageInfo>;

export interface ListSocialAccountsResponse {
  data: ListSocialAccountsResponseDataList;
  page_info: ListSocialAccountsResponsePageInfo;
}
export const ListSocialAccountsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListSocialAccountsResponseDataList,
    page_info: ListSocialAccountsResponsePageInfo,
  }),
).annotate({
  identifier: "ListSocialAccountsResponse",
}) as any as S.Schema<ListSocialAccountsResponse>;

export type ConnectSocialAccountError = BadRequest | Conflict | WhopOpError;
/** Connect a Social Account Starts an OAuth connection flow and returns an authorize_url where the user can connect a social account. */
export const connectSocialAccount: API.OperationMethod<
  ConnectSocialAccountRequest,
  ConnectSocialAccountResponse,
  ConnectSocialAccountError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ConnectSocialAccountRequest,
  output: ConnectSocialAccountResponse,
  errors: [BadRequest, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreateSocialAccountError = BadRequest | Conflict | WhopOpError;
/** Create a Social Account Creates or returns a Whop-managed Facebook page for an account. */
export const createSocialAccount: API.OperationMethod<
  CreateSocialAccountRequest,
  SocialAccount,
  CreateSocialAccountError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSocialAccountRequest,
  output: SocialAccount,
  errors: [BadRequest, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteSocialAccountError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Delete a Social Account Disconnects a social account from an account or user without deleting the underlying platform account. */
export const deleteSocialAccount: API.OperationMethod<
  DeleteSocialAccountRequest,
  DeleteSocialAccountResponse,
  DeleteSocialAccountError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteSocialAccountRequest,
  output: DeleteSocialAccountResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListSocialAccountLeadFormsError =
  | BadRequest
  | NotFound
  | WhopOpError;
/** List Social Account Lead Forms Lists the active lead (instant) forms that already exist on a connected Facebook page, so an ad can reuse one as its `lead_gen_form_id` instead of authoring a new form. Every active form comes back in a single response — the list is not paginated. */
export const listSocialAccountLeadForms: API.OperationMethod<
  ListSocialAccountLeadFormsRequest,
  ListSocialAccountLeadFormsResponse,
  ListSocialAccountLeadFormsError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListSocialAccountLeadFormsRequest,
  output: ListSocialAccountLeadFormsResponse,
  errors: [BadRequest, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListSocialAccountPostsError = BadRequest | NotFound | WhopOpError;
/** List Social Account Posts Lists the existing posts of a connected Facebook page, Instagram account, or TikTok account. */
export const listSocialAccountPosts: API.PaginatedOperationMethod<
  ListSocialAccountPostsRequest,
  ListSocialAccountPostsResponse,
  ListSocialAccountPostsError,
  WhopOpContext,
  SocialAccountPost
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListSocialAccountPostsRequest,
    output: ListSocialAccountPostsResponse,
    errors: [BadRequest, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ListSocialAccountsError = BadRequest | WhopOpError;
/** List Social Accounts Lists the social accounts linked to an account or user. */
export const listSocialAccounts: API.PaginatedOperationMethod<
  ListSocialAccountsRequest,
  ListSocialAccountsResponse,
  ListSocialAccountsError,
  WhopOpContext,
  SocialAccount
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListSocialAccountsRequest,
    output: ListSocialAccountsResponse,
    errors: [BadRequest],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;
