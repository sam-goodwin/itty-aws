// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface CreateSupportChannelRequest {
  /** The unique identifier of the company to create the support channel in. */
  company_id: string;
  /** Optional custom display name for the support channel. */
  custom_name?: string | null;
  /** Whether Whop app notifications are enabled for this support channel. Webhooks still fire. */
  notifications_enabled?: boolean | null;
  /** The user ID (e.g. 'user_xxxxx') or username of the customer to open a support channel for. */
  user_id: string;
}
export const CreateSupportChannelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    company_id: S.String,
    custom_name: S.optional(S.NullOr(S.String)),
    notifications_enabled: S.optional(S.NullOr(S.Boolean)),
    user_id: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/support_channels", code: 200 })),
).annotate({
  identifier: "CreateSupportChannelRequest",
}) as any as S.Schema<CreateSupportChannelRequest>;

/** The customer who initiated this support conversation. Null if this is not a support chat. */
export interface SupportChannelCustomerUser {
  /** The unique identifier for the user. */
  id: string;
  /** The user's display name shown on their public profile. */
  name: string | null;
  /** The user's unique username shown on their public profile. */
  username: string;
}
export const SupportChannelCustomerUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({
  identifier: "SupportChannelCustomerUser",
}) as any as S.Schema<SupportChannelCustomerUser>;

/** A messaging channel that can be a one-on-one DM, group chat, company support conversation, or platform-level direct message. */
export interface SupportChannel {
  /** The unique identifier of the company associated with this channel. Null if this is not a support or company-scoped conversation. */
  company_id: string | null;
  /** A custom display name assigned to this channel by the user. Null if no custom name has been set. */
  custom_name: string | null;
  /** The customer who initiated this support conversation. Null if this is not a support chat. */
  customer_user: SupportChannelCustomerUser | null;
  /** The unique identifier for the entity */
  id: string;
  /** The timestamp when the most recent message was sent in this channel. Null if no messages have been sent. */
  last_message_at: string | null;
  /** The timestamp when the linked support ticket was marked as resolved. Null if unresolved or not a support chat. */
  resolved_at: string | null;
}
export const SupportChannel = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    company_id: S.NullOr(S.String),
    custom_name: S.NullOr(S.String),
    customer_user: S.NullOr(SupportChannelCustomerUser),
    id: S.String,
    last_message_at: S.NullOr(S.String),
    resolved_at: S.NullOr(S.String),
  }),
).annotate({ identifier: "SupportChannel" }) as any as S.Schema<SupportChannel>;

/** The perspective to filter support channels by. */
export type SupportChannelView = "all" | "admin" | "customer";
export const SupportChannelView = /*@__PURE__*/ S.String;

/** The direction of the sort. */
export type Direction = "asc" | "desc";
export const Direction = /*@__PURE__*/ S.String;

/** Sort options for message channels */
export type MessageChannelOrder = "created_at" | "last_post_sent_at";
export const MessageChannelOrder = /*@__PURE__*/ S.String;

export interface ListSupportChannelRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  company_id?: string;
  view?: SupportChannelView | (string & {});
  open?: boolean;
  direction?: Direction | (string & {});
  order?: MessageChannelOrder | (string & {});
}
export const ListSupportChannelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    company_id: S.optional(S.String.pipe(T.Query())),
    view: S.optional(SupportChannelView.pipe(T.Query())),
    open: S.optional(S.Boolean.pipe(T.Query())),
    direction: S.optional(Direction.pipe(T.Query())),
    order: S.optional(MessageChannelOrder.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/support_channels", code: 200 })),
).annotate({
  identifier: "ListSupportChannelRequest",
}) as any as S.Schema<ListSupportChannelRequest>;

/** The customer who initiated this support conversation. Null if this is not a support chat. */
export type SupportChannelListItemCustomerUser = SupportChannelCustomerUser;
export const SupportChannelListItemCustomerUser = SupportChannelCustomerUser;

/** A messaging channel that can be a one-on-one DM, group chat, company support conversation, or platform-level direct message. */
export interface SupportChannelListItem {
  /** The unique identifier of the company associated with this channel. Null if this is not a support or company-scoped conversation. */
  company_id: string | null;
  /** A custom display name assigned to this channel by the user. Null if no custom name has been set. */
  custom_name: string | null;
  /** The customer who initiated this support conversation. Null if this is not a support chat. */
  customer_user: SupportChannelCustomerUser | null;
  /** The unique identifier for the entity */
  id: string;
  /** The timestamp when the most recent message was sent in this channel. Null if no messages have been sent. */
  last_message_at: string | null;
  /** The timestamp when the linked support ticket was marked as resolved. Null if unresolved or not a support chat. */
  resolved_at: string | null;
}
export const SupportChannelListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    company_id: S.NullOr(S.String),
    custom_name: S.NullOr(S.String),
    customer_user: S.NullOr(SupportChannelCustomerUser),
    id: S.String,
    last_message_at: S.NullOr(S.String),
    resolved_at: S.NullOr(S.String),
  }),
).annotate({
  identifier: "SupportChannelListItem",
}) as any as S.Schema<SupportChannelListItem>;

/** A list of nodes. */
export type ListSupportChannelResponseDataList = Array<SupportChannelListItem>;
export const ListSupportChannelResponseDataList = /*@__PURE__*/ S.Array(
  SupportChannelListItem,
) as any as S.Schema<ListSupportChannelResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListSupportChannelResponse {
  /** A list of nodes. */
  data: ListSupportChannelResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListSupportChannelResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListSupportChannelResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListSupportChannelResponse",
}) as any as S.Schema<ListSupportChannelResponse>;

export interface RetrieveSupportChannelRequest {
  /** The unique identifier of the support channel to retrieve. */
  id: string;
}
export const RetrieveSupportChannelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/support_channels/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveSupportChannelRequest",
}) as any as S.Schema<RetrieveSupportChannelRequest>;

export type CreateSupportChannelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create support channel [Legacy API — https://docs.whop.com/api-reference] Open a new support channel between a company team member and a customer. Returns the existing channel if one already exists for that user. Required permissions: - `support_chat:create` */
export const createSupportChannel: API.OperationMethod<
  CreateSupportChannelRequest,
  SupportChannel,
  CreateSupportChannelError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSupportChannelRequest,
  output: SupportChannel,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListSupportChannelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List support channels [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of support channels for a specific company, with optional filtering by resolution status and custom sorting. Required permissions: - `support_chat:read` */
export const listSupportChannel: API.PaginatedOperationMethod<
  ListSupportChannelRequest,
  ListSupportChannelResponse,
  ListSupportChannelError,
  WhopOpContext,
  SupportChannelListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListSupportChannelRequest,
    output: ListSupportChannelResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveSupportChannelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve support channel [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing support channel. Required permissions: - `support_chat:read` */
export const retrieveSupportChannel: API.OperationMethod<
  RetrieveSupportChannelRequest,
  SupportChannel,
  RetrieveSupportChannelError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveSupportChannelRequest,
  output: SupportChannel,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
