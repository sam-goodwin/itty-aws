// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** The types of fee markups that can be configured */
export type FeeMarkupTypes =
  | "crypto_withdrawal_markup"
  | "rtp_withdrawal_markup"
  | "next_day_bank_withdrawal_markup"
  | "bank_wire_withdrawal_markup"
  | "digital_wallet_withdrawal_markup";
export const FeeMarkupTypes = /*@__PURE__*/ S.String;

/** Custom key-value metadata to attach to this fee markup. */
export type CreateFeeMarkupRequestMetadataMap = {
  [key: string]: unknown | undefined;
};
export const CreateFeeMarkupRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateFeeMarkupRequestMetadataMap>;

export interface CreateFeeMarkupRequest {
  /** The unique identifier of the company to create or update the fee markup for. */
  company_id: string;
  /** The type of fee this markup applies to, such as processing or platform fees. */
  fee_type: FeeMarkupTypes | (string & {});
  /** The fixed fee amount in USD to charge per transaction. Must be between 0 and 50. */
  fixed_fee_usd?: number | null;
  /** Custom key-value metadata to attach to this fee markup. */
  metadata?: CreateFeeMarkupRequestMetadataMap | null;
  /** Internal notes about this fee markup for record-keeping purposes. */
  notes?: string | null;
  /** The percentage fee to charge per transaction. Must be between 0 and 25. */
  percentage_fee?: number | null;
}
export const CreateFeeMarkupRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    company_id: S.String,
    fee_type: FeeMarkupTypes,
    fixed_fee_usd: S.optional(S.NullOr(S.Number)),
    metadata: S.optional(S.NullOr(CreateFeeMarkupRequestMetadataMap)),
    notes: S.optional(S.NullOr(S.String)),
    percentage_fee: S.optional(S.NullOr(S.Number)),
  }).pipe(T.Http({ method: "POST", uri: "/fee_markups", code: 200 })),
).annotate({
  identifier: "CreateFeeMarkupRequest",
}) as any as S.Schema<CreateFeeMarkupRequest>;

/** A fee markup configuration that defines additional charges applied to transactions for a platform's connected accounts. */
export interface FeeMarkup {
  /** The datetime the fee markup was created. */
  created_at: string;
  /** The category of fee this markup applies to. */
  fee_type: FeeMarkupTypes;
  /** A flat fee charged per transaction, in USD. Ranges from 0 to 50. Null if no fixed fee is configured. */
  fixed_fee_usd: number | null;
  /** The unique identifier for the fee markup. */
  id: string;
  /** Internal notes about this fee markup, visible only to administrators. Null if no notes have been added. */
  notes: string | null;
  /** A percentage-based fee charged per transaction. Ranges from 0 to 25. Null if no percentage fee is configured. */
  percentage_fee: number | null;
  /** The datetime the fee markup was last updated. */
  updated_at: string;
}
export const FeeMarkup = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    created_at: S.String,
    fee_type: FeeMarkupTypes,
    fixed_fee_usd: S.NullOr(S.Number),
    id: S.String,
    notes: S.NullOr(S.String),
    percentage_fee: S.NullOr(S.Number),
    updated_at: S.String,
  }),
).annotate({ identifier: "FeeMarkup" }) as any as S.Schema<FeeMarkup>;

export interface DeleteFeeMarkupRequest {
  /** The unique identifier of the fee markup to delete. */
  id: string;
}
export const DeleteFeeMarkupRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/fee_markups/{id}", code: 200 })),
).annotate({
  identifier: "DeleteFeeMarkupRequest",
}) as any as S.Schema<DeleteFeeMarkupRequest>;

export type DeleteFeeMarkupResponse = boolean;
export const DeleteFeeMarkupResponse = /*@__PURE__*/ S.suspend(() =>
  S.Boolean.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "DeleteFeeMarkupResponse",
}) as any as S.Schema<DeleteFeeMarkupResponse>;

export interface ListFeeMarkupRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  company_id: string;
}
export const ListFeeMarkupRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    company_id: S.String.pipe(T.Query()),
  }).pipe(T.Http({ method: "GET", uri: "/fee_markups", code: 200 })),
).annotate({
  identifier: "ListFeeMarkupRequest",
}) as any as S.Schema<ListFeeMarkupRequest>;

/** A fee markup configuration that defines additional charges applied to transactions for a platform's connected accounts. */
export interface FeeMarkupListItem {
  /** The datetime the fee markup was created. */
  created_at: string;
  /** The category of fee this markup applies to. */
  fee_type: FeeMarkupTypes;
  /** A flat fee charged per transaction, in USD. Ranges from 0 to 50. Null if no fixed fee is configured. */
  fixed_fee_usd: number | null;
  /** The unique identifier for the fee markup. */
  id: string;
  /** Internal notes about this fee markup, visible only to administrators. Null if no notes have been added. */
  notes: string | null;
  /** A percentage-based fee charged per transaction. Ranges from 0 to 25. Null if no percentage fee is configured. */
  percentage_fee: number | null;
  /** The datetime the fee markup was last updated. */
  updated_at: string;
}
export const FeeMarkupListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    created_at: S.String,
    fee_type: FeeMarkupTypes,
    fixed_fee_usd: S.NullOr(S.Number),
    id: S.String,
    notes: S.NullOr(S.String),
    percentage_fee: S.NullOr(S.Number),
    updated_at: S.String,
  }),
).annotate({
  identifier: "FeeMarkupListItem",
}) as any as S.Schema<FeeMarkupListItem>;

/** A list of nodes. */
export type ListFeeMarkupResponseDataList = Array<FeeMarkupListItem>;
export const ListFeeMarkupResponseDataList = /*@__PURE__*/ S.Array(
  FeeMarkupListItem,
) as any as S.Schema<ListFeeMarkupResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListFeeMarkupResponse {
  /** A list of nodes. */
  data: ListFeeMarkupResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListFeeMarkupResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListFeeMarkupResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListFeeMarkupResponse",
}) as any as S.Schema<ListFeeMarkupResponse>;

export type CreateFeeMarkupError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create fee markup [Legacy API — https://docs.whop.com/api-reference] Create or update a fee markup for a company. If a markup for the specified fee type already exists, it will be updated with the new values. Required permissions: - `company:update_child_fees` */
export const createFeeMarkup: API.OperationMethod<
  CreateFeeMarkupRequest,
  FeeMarkup,
  CreateFeeMarkupError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateFeeMarkupRequest,
  output: FeeMarkup,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteFeeMarkupError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Delete fee markup [Legacy API — https://docs.whop.com/api-reference] Delete a fee markup configuration for a company. This removes the custom fee override and reverts to the parent company's default fees. Required permissions: - `company:update_child_fees` */
export const deleteFeeMarkup: API.OperationMethod<
  DeleteFeeMarkupRequest,
  DeleteFeeMarkupResponse,
  DeleteFeeMarkupError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteFeeMarkupRequest,
  output: DeleteFeeMarkupResponse,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListFeeMarkupError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List fee markups [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of fee markups configured for a company. If the company is a platform account, returns the platform default markups. Required permissions: - `company:update_child_fees` */
export const listFeeMarkup: API.PaginatedOperationMethod<
  ListFeeMarkupRequest,
  ListFeeMarkupResponse,
  ListFeeMarkupError,
  WhopOpContext,
  FeeMarkupListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListFeeMarkupRequest,
    output: ListFeeMarkupResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;
