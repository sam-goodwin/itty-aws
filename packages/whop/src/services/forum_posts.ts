// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** Input for an attachment */
export interface CreateForumPostRequestAttachmentsItem {
  /** The ID of an existing file object. */
  id: string;
}
export const CreateForumPostRequestAttachmentsItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      id: S.String,
    }),
).annotate({
  identifier: "CreateForumPostRequestAttachmentsItem",
}) as any as S.Schema<CreateForumPostRequestAttachmentsItem>;

/** A list of file attachments to include with the post, such as images or videos. */
export type CreateForumPostRequestAttachmentsList =
  Array<CreateForumPostRequestAttachmentsItem>;
export const CreateForumPostRequestAttachmentsList = /*@__PURE__*/ S.Array(
  CreateForumPostRequestAttachmentsItem,
) as any as S.Schema<CreateForumPostRequestAttachmentsList>;

/** The available currencies on the platform */
export type Currencies =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const Currencies = /*@__PURE__*/ S.String;

/** Input type for a single poll option */
export interface CreateForumPostRequestPollOptionsItem {
  /** Sequential ID for the poll option (starting from '1') */
  id: string;
  /** The text of the poll option */
  text: string;
}
export const CreateForumPostRequestPollOptionsItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      id: S.String,
      text: S.String,
    }),
).annotate({
  identifier: "CreateForumPostRequestPollOptionsItem",
}) as any as S.Schema<CreateForumPostRequestPollOptionsItem>;

/** The options for the poll. Must have sequential IDs starting from 1 */
export type CreateForumPostRequestPollOptionsList =
  Array<CreateForumPostRequestPollOptionsItem>;
export const CreateForumPostRequestPollOptionsList = /*@__PURE__*/ S.Array(
  CreateForumPostRequestPollOptionsItem,
) as any as S.Schema<CreateForumPostRequestPollOptionsList>;

/** A poll to attach to this post, allowing members to vote on options. */
export interface CreateForumPostRequestPoll {
  /** The options for the poll. Must have sequential IDs starting from 1 */
  options: CreateForumPostRequestPollOptionsList;
}
export const CreateForumPostRequestPoll = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    options: CreateForumPostRequestPollOptionsList,
  }),
).annotate({
  identifier: "CreateForumPostRequestPoll",
}) as any as S.Schema<CreateForumPostRequestPoll>;

/** The visibility types for forum posts */
export type ForumPostVisibilityTypes = "members_only" | "globally_visible";
export const ForumPostVisibilityTypes = /*@__PURE__*/ S.String;

export interface CreateForumPostRequest {
  /** A list of file attachments to include with the post, such as images or videos. */
  attachments?: CreateForumPostRequestAttachmentsList | null;
  /** The unique identifier of the company whose public forum to post in. Required when experience_id is 'public'. For example, 'biz_xxxxx'. */
  company_id?: string | null;
  /** The main body of the post in Markdown format. For example, 'Check out this **update**'. Hidden if the post is paywalled and the viewer has not purchased access. */
  content?: string | null;
  /** The unique identifier of the experience to create this post in. For example, 'exp_xxxxx'. Pass 'public' along with company_id to automatically use the company's public forum. */
  experience_id: string;
  /** Whether to send this post as a mention notification to all users in the experience who have mentions enabled. */
  is_mention?: boolean | null;
  /** The unique identifier of the parent post to comment on. Omit this field to create a top-level post. */
  parent_id?: string | null;
  /** The price to unlock this post in the specified paywall currency. For example, 5.00 for $5.00. When set, users must purchase access to view the post content. */
  paywall_amount?: number | null;
  /** The currency for the paywall price on this post. When set along with paywall_amount, users must purchase access to view the post content. */
  paywall_currency?: Currencies | (string & {}) | null;
  /** Whether this post should be pinned to the top of the forum. */
  pinned?: boolean | null;
  /** A poll to attach to this post, allowing members to vote on options. */
  poll?: CreateForumPostRequestPoll | null;
  /** The rich content of the post in Tiptap JSON format. When provided, takes priority over the markdown content field for rendering. */
  rich_content?: string | null;
  /** The title of the post, displayed prominently at the top. Required for paywalled posts as it remains visible to non-purchasers. */
  title?: string | null;
  /** Controls who can see this forum post, such as members only or public. */
  visibility?: ForumPostVisibilityTypes | (string & {}) | null;
}
export const CreateForumPostRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    attachments: S.optional(S.NullOr(CreateForumPostRequestAttachmentsList)),
    company_id: S.optional(S.NullOr(S.String)),
    content: S.optional(S.NullOr(S.String)),
    experience_id: S.String,
    is_mention: S.optional(S.NullOr(S.Boolean)),
    parent_id: S.optional(S.NullOr(S.String)),
    paywall_amount: S.optional(S.NullOr(S.Number)),
    paywall_currency: S.optional(S.NullOr(Currencies)),
    pinned: S.optional(S.NullOr(S.Boolean)),
    poll: S.optional(S.NullOr(CreateForumPostRequestPoll)),
    rich_content: S.optional(S.NullOr(S.String)),
    title: S.optional(S.NullOr(S.String)),
    visibility: S.optional(S.NullOr(ForumPostVisibilityTypes)),
  }).pipe(T.Http({ method: "POST", uri: "/forum_posts", code: 200 })),
).annotate({
  identifier: "CreateForumPostRequest",
}) as any as S.Schema<CreateForumPostRequest>;

/** Represents an image attachment */
export interface ForumPostAttachmentsItem {
  /** Uploaded file MIME type, such as image/jpeg, video/mp4, or audio/mpeg. */
  content_type: string | null;
  /** The original filename of the uploaded attachment, including its file extension. */
  filename: string | null;
  /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
  id: string;
  /** A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps. */
  url: string | null;
}
export const ForumPostAttachmentsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    content_type: S.NullOr(S.String),
    filename: S.NullOr(S.String),
    id: S.String,
    url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ForumPostAttachmentsItem",
}) as any as S.Schema<ForumPostAttachmentsItem>;

/** All file attachments on this post, such as images, documents, and videos. */
export type ForumPostAttachmentsList = Array<ForumPostAttachmentsItem>;
export const ForumPostAttachmentsList = /*@__PURE__*/ S.Array(
  ForumPostAttachmentsItem,
) as any as S.Schema<ForumPostAttachmentsList>;

/** The user who authored this forum post. */
export interface ForumPostUser {
  /** The unique identifier for the user. */
  id: string;
  /** The user's display name shown on their public profile. */
  name: string | null;
  /** The user's unique username shown on their public profile. */
  username: string;
}
export const ForumPostUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({ identifier: "ForumPostUser" }) as any as S.Schema<ForumPostUser>;

/** A post or comment in a forum feed, supporting rich text, attachments, polls, and reactions. */
export interface ForumPost {
  /** All file attachments on this post, such as images, documents, and videos. */
  attachments: ForumPostAttachmentsList;
  /** The total number of direct comments on this post. */
  comment_count: number;
  /** The body of the forum post in Markdown format. Null if the post is paywalled and the current user does not have access. */
  content: string | null;
  /** The time this post was created, as a Unix timestamp. */
  created_at: string;
  /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
  id: string;
  /** Whether this post has been edited after its initial creation. */
  is_edited: boolean;
  /** Whether this post is pinned to the top of the forum feed. */
  is_pinned: boolean;
  /** Whether the author of this post is an admin of the company that owns the forum. */
  is_poster_admin: boolean;
  /** The total number of like reactions this post has received. */
  like_count: number | null;
  /** The unique identifier of the parent post. Null if this is a top-level post. */
  parent_id: string | null;
  /** The headline of the forum post. Null if the post has no title. */
  title: string | null;
  /** The time this post was last updated, as a Unix timestamp. */
  updated_at: string;
  /** The user who authored this forum post. */
  user: ForumPostUser;
  /** The total number of times this post has been viewed by users. */
  view_count: number | null;
}
export const ForumPost = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    attachments: ForumPostAttachmentsList,
    comment_count: S.Number,
    content: S.NullOr(S.String),
    created_at: S.String,
    id: S.String,
    is_edited: S.Boolean,
    is_pinned: S.Boolean,
    is_poster_admin: S.Boolean,
    like_count: S.NullOr(S.Number),
    parent_id: S.NullOr(S.String),
    title: S.NullOr(S.String),
    updated_at: S.String,
    user: ForumPostUser,
    view_count: S.NullOr(S.Number),
  }),
).annotate({ identifier: "ForumPost" }) as any as S.Schema<ForumPost>;

export interface ListForumPostRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  experience_id: string;
  include_bounty_anchors?: boolean;
  parent_id?: string;
  pinned?: boolean;
}
export const ListForumPostRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    experience_id: S.String.pipe(T.Query()),
    include_bounty_anchors: S.optional(S.Boolean.pipe(T.Query())),
    parent_id: S.optional(S.String.pipe(T.Query())),
    pinned: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/forum_posts", code: 200 })),
).annotate({
  identifier: "ListForumPostRequest",
}) as any as S.Schema<ListForumPostRequest>;

/** Represents an image attachment */
export type ForumPostListItemAttachmentsItem = ForumPostAttachmentsItem;
export const ForumPostListItemAttachmentsItem = ForumPostAttachmentsItem;

/** All file attachments on this post, such as images, documents, and videos. */
export type ForumPostListItemAttachmentsList = Array<ForumPostAttachmentsItem>;
export const ForumPostListItemAttachmentsList = /*@__PURE__*/ S.Array(
  ForumPostAttachmentsItem,
) as any as S.Schema<ForumPostListItemAttachmentsList>;

/** The user who authored this forum post. */
export type ForumPostListItemUser = ForumPostUser;
export const ForumPostListItemUser = ForumPostUser;

/** A post or comment in a forum feed, supporting rich text, attachments, polls, and reactions. */
export interface ForumPostListItem {
  /** All file attachments on this post, such as images, documents, and videos. */
  attachments: ForumPostListItemAttachmentsList;
  /** The total number of direct comments on this post. */
  comment_count: number;
  /** The body of the forum post in Markdown format. Null if the post is paywalled and the current user does not have access. */
  content: string | null;
  /** The time this post was created, as a Unix timestamp. */
  created_at: string;
  /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
  id: string;
  /** Whether this post has been edited after its initial creation. */
  is_edited: boolean;
  /** Whether this post is pinned to the top of the forum feed. */
  is_pinned: boolean;
  /** Whether the author of this post is an admin of the company that owns the forum. */
  is_poster_admin: boolean;
  /** The total number of like reactions this post has received. */
  like_count: number | null;
  /** The unique identifier of the parent post. Null if this is a top-level post. */
  parent_id: string | null;
  /** The headline of the forum post. Null if the post has no title. */
  title: string | null;
  /** The time this post was last updated, as a Unix timestamp. */
  updated_at: string;
  /** The user who authored this forum post. */
  user: ForumPostUser;
  /** The total number of times this post has been viewed by users. */
  view_count: number | null;
}
export const ForumPostListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    attachments: ForumPostListItemAttachmentsList,
    comment_count: S.Number,
    content: S.NullOr(S.String),
    created_at: S.String,
    id: S.String,
    is_edited: S.Boolean,
    is_pinned: S.Boolean,
    is_poster_admin: S.Boolean,
    like_count: S.NullOr(S.Number),
    parent_id: S.NullOr(S.String),
    title: S.NullOr(S.String),
    updated_at: S.String,
    user: ForumPostUser,
    view_count: S.NullOr(S.Number),
  }),
).annotate({
  identifier: "ForumPostListItem",
}) as any as S.Schema<ForumPostListItem>;

/** A list of nodes. */
export type ListForumPostResponseDataList = Array<ForumPostListItem>;
export const ListForumPostResponseDataList = /*@__PURE__*/ S.Array(
  ForumPostListItem,
) as any as S.Schema<ListForumPostResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListForumPostResponse {
  /** A list of nodes. */
  data: ListForumPostResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListForumPostResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListForumPostResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListForumPostResponse",
}) as any as S.Schema<ListForumPostResponse>;

export interface RetrieveForumPostRequest {
  /** The unique identifier of the forum post to retrieve. */
  id: string;
}
export const RetrieveForumPostRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/forum_posts/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveForumPostRequest",
}) as any as S.Schema<RetrieveForumPostRequest>;

/** Input for an attachment */
export type UpdateForumPostRequestAttachmentsItem =
  CreateForumPostRequestAttachmentsItem;
export const UpdateForumPostRequestAttachmentsItem =
  CreateForumPostRequestAttachmentsItem;

/** A replacement list of file attachments for this post, such as images or videos. */
export type UpdateForumPostRequestAttachmentsList =
  Array<CreateForumPostRequestAttachmentsItem>;
export const UpdateForumPostRequestAttachmentsList = /*@__PURE__*/ S.Array(
  CreateForumPostRequestAttachmentsItem,
) as any as S.Schema<UpdateForumPostRequestAttachmentsList>;

export interface UpdateForumPostRequest {
  /** The unique identifier of the forum post to update. */
  id: string;
  /** A replacement list of file attachments for this post, such as images or videos. */
  attachments?: UpdateForumPostRequestAttachmentsList | null;
  /** The updated body of the post in Markdown format. For example, 'Check out this **update**'. Hidden if the post is paywalled and the viewer has not purchased access. */
  content?: string | null;
  /** Whether this post should be pinned to the top of the forum. Only top-level posts can be pinned, not comments. */
  is_pinned?: boolean | null;
  /** The updated title of the post, displayed prominently at the top. Required for paywalled posts as it remains visible to non-purchasers. */
  title?: string | null;
  /** Controls who can see this forum post, such as members only or public. */
  visibility?: ForumPostVisibilityTypes | (string & {}) | null;
}
export const UpdateForumPostRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    attachments: S.optional(S.NullOr(UpdateForumPostRequestAttachmentsList)),
    content: S.optional(S.NullOr(S.String)),
    is_pinned: S.optional(S.NullOr(S.Boolean)),
    title: S.optional(S.NullOr(S.String)),
    visibility: S.optional(S.NullOr(ForumPostVisibilityTypes)),
  }).pipe(T.Http({ method: "PATCH", uri: "/forum_posts/{id}", code: 200 })),
).annotate({
  identifier: "UpdateForumPostRequest",
}) as any as S.Schema<UpdateForumPostRequest>;

export type CreateForumPostError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create forum post [Legacy API — https://docs.whop.com/api-reference] Create a new forum post or comment within an experience. Supports text content, attachments, polls, paywalling, and pinning. Pass experience_id 'public' with a company_id to post to a company's public forum. Required permissions: - `forum:post:create` */
export const createForumPost: API.OperationMethod<
  CreateForumPostRequest,
  ForumPost,
  CreateForumPostError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateForumPostRequest,
  output: ForumPost,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListForumPostError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List forum posts [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of forum posts within a specific experience, with optional filtering by parent post or pinned status. Required permissions: - `forum:read` */
export const listForumPost: API.PaginatedOperationMethod<
  ListForumPostRequest,
  ListForumPostResponse,
  ListForumPostError,
  WhopOpContext,
  ForumPostListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListForumPostRequest,
    output: ListForumPostResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveForumPostError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve forum post [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing forum post. Required permissions: - `forum:read` */
export const retrieveForumPost: API.OperationMethod<
  RetrieveForumPostRequest,
  ForumPost,
  RetrieveForumPostError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveForumPostRequest,
  output: ForumPost,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateForumPostError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update forum post [Legacy API — https://docs.whop.com/api-reference] Edit the content, attachments, pinned status, or visibility of an existing forum post or comment. */
export const updateForumPost: API.OperationMethod<
  UpdateForumPostRequest,
  ForumPost,
  UpdateForumPostError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateForumPostRequest,
  output: ForumPost,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
