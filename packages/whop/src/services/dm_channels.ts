// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** The list of user identifiers to include in the DM channel. Each entry can be an email, username, or user ID (e.g. 'user_xxxxx'). */
export type CreateDmChannelRequestWithUserIdsList = Array<string>;
export const CreateDmChannelRequestWithUserIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateDmChannelRequestWithUserIdsList>;

export interface CreateDmChannelRequest {
  /** The unique identifier of the company to scope this DM channel to. When set, the channel is visible only within that company context. */
  company_id?: string | null;
  /** A custom display name for the DM channel. For example, 'Project Discussion'. */
  custom_name?: string | null;
  /** Whether Whop app notifications are enabled for this direct message channel. Webhooks still fire. */
  notifications_enabled?: boolean | null;
  /** The list of user identifiers to include in the DM channel. Each entry can be an email, username, or user ID (e.g. 'user_xxxxx'). */
  with_user_ids: CreateDmChannelRequestWithUserIdsList;
}
export const CreateDmChannelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    company_id: S.optional(S.NullOr(S.String)),
    custom_name: S.optional(S.NullOr(S.String)),
    notifications_enabled: S.optional(S.NullOr(S.Boolean)),
    with_user_ids: CreateDmChannelRequestWithUserIdsList,
  }).pipe(T.Http({ method: "POST", uri: "/dm_channels", code: 200 })),
).annotate({
  identifier: "CreateDmChannelRequest",
}) as any as S.Schema<CreateDmChannelRequest>;

/** A messaging channel that can be a one-on-one DM, group chat, company support conversation, or platform-level direct message. */
export interface DmChannel {
  /** The time the entity was created (in milliseconds since Unix epoch) */
  created_at: string;
  /** The unique identifier for the entity */
  id: string;
  /** The timestamp when the most recent message was sent in this channel. Null if no messages have been sent. */
  last_message_at: string | null;
  /** A custom display name assigned to this channel by the user. Null if no custom name has been set. */
  name: string | null;
}
export const DmChannel = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    created_at: S.String,
    id: S.String,
    last_message_at: S.NullOr(S.String),
    name: S.NullOr(S.String),
  }),
).annotate({ identifier: "DmChannel" }) as any as S.Schema<DmChannel>;

export interface DeleteDmChannelRequest {
  /** The unique identifier of the DM channel to delete. */
  id: string;
}
export const DeleteDmChannelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/dm_channels/{id}", code: 200 })),
).annotate({
  identifier: "DeleteDmChannelRequest",
}) as any as S.Schema<DeleteDmChannelRequest>;

export type DeleteDmChannelResponse = boolean;
export const DeleteDmChannelResponse = /*@__PURE__*/ S.suspend(() =>
  S.Boolean.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "DeleteDmChannelResponse",
}) as any as S.Schema<DeleteDmChannelResponse>;

export interface ListDmChannelRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  company_id?: string;
}
export const ListDmChannelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    company_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/dm_channels", code: 200 })),
).annotate({
  identifier: "ListDmChannelRequest",
}) as any as S.Schema<ListDmChannelRequest>;

/** A messaging channel that can be a one-on-one DM, group chat, company support conversation, or platform-level direct message. */
export interface DmChannelListItem {
  /** The time the entity was created (in milliseconds since Unix epoch) */
  created_at: string;
  /** The unique identifier for the entity */
  id: string;
  /** The timestamp when the most recent message was sent in this channel. Null if no messages have been sent. */
  last_message_at: string | null;
  /** A custom display name assigned to this channel by the user. Null if no custom name has been set. */
  name: string | null;
}
export const DmChannelListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    created_at: S.String,
    id: S.String,
    last_message_at: S.NullOr(S.String),
    name: S.NullOr(S.String),
  }),
).annotate({
  identifier: "DmChannelListItem",
}) as any as S.Schema<DmChannelListItem>;

/** A list of nodes. */
export type ListDmChannelResponseDataList = Array<DmChannelListItem>;
export const ListDmChannelResponseDataList = /*@__PURE__*/ S.Array(
  DmChannelListItem,
) as any as S.Schema<ListDmChannelResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListDmChannelResponse {
  /** A list of nodes. */
  data: ListDmChannelResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListDmChannelResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListDmChannelResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListDmChannelResponse",
}) as any as S.Schema<ListDmChannelResponse>;

export interface RetrieveDmChannelRequest {
  /** The unique identifier of the DM channel to retrieve. */
  id: string;
}
export const RetrieveDmChannelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/dm_channels/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveDmChannelRequest",
}) as any as S.Schema<RetrieveDmChannelRequest>;

export interface UpdateDmChannelRequest {
  /** The unique identifier of the DM channel to update. */
  id: string;
  /** A new custom display name for the DM channel. For example, 'Project Discussion'. */
  custom_name?: string | null;
}
export const UpdateDmChannelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    custom_name: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "PATCH", uri: "/dm_channels/{id}", code: 200 })),
).annotate({
  identifier: "UpdateDmChannelRequest",
}) as any as S.Schema<UpdateDmChannelRequest>;

export type CreateDmChannelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create dm channel [Legacy API — https://docs.whop.com/api-reference] Create a new DM channel between two or more users, optionally scoped to a specific company. Returns the existing channel if one already exists. Required permissions: - `dms:channel:manage` */
export const createDmChannel: API.OperationMethod<
  CreateDmChannelRequest,
  DmChannel,
  CreateDmChannelError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateDmChannelRequest,
  output: DmChannel,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteDmChannelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Delete dm channel [Legacy API — https://docs.whop.com/api-reference] Permanently delete a DM channel and all of its messages. Only an admin of the channel can perform this action. Required permissions (one of): - `dms:channel:manage` - `support_chat:create` */
export const deleteDmChannel: API.OperationMethod<
  DeleteDmChannelRequest,
  DeleteDmChannelResponse,
  DeleteDmChannelError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteDmChannelRequest,
  output: DeleteDmChannelResponse,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListDmChannelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List dm channels [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of DM channels for the currently authenticated user, sorted by most recently active. Required permissions: - `dms:read` */
export const listDmChannel: API.PaginatedOperationMethod<
  ListDmChannelRequest,
  ListDmChannelResponse,
  ListDmChannelError,
  WhopOpContext,
  DmChannelListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListDmChannelRequest,
    output: ListDmChannelResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveDmChannelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve dm channel [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing DM channel. Required permissions (one of): - `dms:read` - `support_chat:read` */
export const retrieveDmChannel: API.OperationMethod<
  RetrieveDmChannelRequest,
  DmChannel,
  RetrieveDmChannelError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveDmChannelRequest,
  output: DmChannel,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateDmChannelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update dm channel [Legacy API — https://docs.whop.com/api-reference] Update the settings of an existing DM channel, such as its display name. Only an admin of the channel can perform this action. Required permissions (one of): - `dms:channel:manage` - `support_chat:create` */
export const updateDmChannel: API.OperationMethod<
  UpdateDmChannelRequest,
  DmChannel,
  UpdateDmChannelError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateDmChannelRequest,
  output: DmChannel,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
