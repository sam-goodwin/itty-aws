// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** The type of app to create. Defaults to `b2c_app`. */
export type CreateAppRequestAppType =
  | "b2b_app"
  | "b2c_app"
  | "company_app"
  | "component"
  | "website";
export const CreateAppRequestAppType = /*@__PURE__*/ S.String;

/** The icon image for the app in PNG, JPEG, or GIF format, referencing an uploaded file: `{ id }` for an existing attachment or `{ direct_upload_id }` for a new direct upload. */
export interface CreateAppRequestIcon {
  /** The signed id of a completed direct upload. */
  direct_upload_id?: string;
  /** The tag of an already-uploaded attachment. */
  id?: string;
}
export const CreateAppRequestIcon = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    direct_upload_id: S.optional(S.String),
    id: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateAppRequestIcon",
}) as any as S.Schema<CreateAppRequestIcon>;

/** The whitelisted OAuth callback URLs that users are redirected to after authorizing the app. */
export type CreateAppRequestRedirectUrisList = Array<string>;
export const CreateAppRequestRedirectUrisList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateAppRequestRedirectUrisList>;

export interface CreateAppRequest {
  /** The account to create the app for (`biz_` tag). Defaults to the account behind the presented credential. */
  account_id?: string;
  /** The type of app to create. Defaults to `b2c_app`. */
  app_type?: CreateAppRequestAppType | (string & {});
  /** The base production URL where the app is hosted, such as `https://myapp.example.com`. */
  base_url?: string | null;
  /** The icon image for the app in PNG, JPEG, or GIF format, referencing an uploaded file: `{ id }` for an existing attachment or `{ direct_upload_id }` for a new direct upload. */
  icon?: CreateAppRequestIcon;
  /** The display name for the app, shown to users on the app store and product pages. */
  name: string;
  /** The whitelisted OAuth callback URLs that users are redirected to after authorizing the app. */
  redirect_uris?: CreateAppRequestRedirectUrisList;
  /** The subdomain route where the app's hosted web builds are served, such as `myapp` for myapp.whop.app. */
  route?: string | null;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateAppRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    app_type: S.optional(CreateAppRequestAppType),
    base_url: S.optional(S.NullOr(S.String)),
    icon: S.optional(CreateAppRequestIcon),
    name: S.String,
    redirect_uris: S.optional(CreateAppRequestRedirectUrisList),
    route: S.optional(S.NullOr(S.String)),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/apps", code: 200 })),
).annotate({
  identifier: "CreateAppRequest",
}) as any as S.Schema<CreateAppRequest>;

export interface AccountParent {
  /** Account ID, prefixed `biz_`. */
  id: string;
  /** Account logo image URL. */
  logo_url: string | null;
  /** Account public route identifier. */
  route: string;
  /** Account display name. */
  title: string;
}
export const AccountParent = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    logo_url: S.NullOr(S.String),
    route: S.String,
    title: S.String,
  }),
).annotate({ identifier: "AccountParent" }) as any as S.Schema<AccountParent>;

export interface AppApiKey {
  /** When the key was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** The key's secret token, sent as a bearer token to authenticate requests on the app's behalf. */
  token: string;
}
export const AppApiKey = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    created_at: S.String,
    token: S.String,
  }),
).annotate({ identifier: "AppApiKey" }) as any as S.Schema<AppApiKey>;

/** The type of end-user the app is built for. */
export type AppAppType =
  | "b2b_app"
  | "b2c_app"
  | "company_app"
  | "component"
  | "website";
export const AppAppType = /*@__PURE__*/ S.String;

export interface AppBannerImage {
  /** Banner image URL, taken from the app's product listing. */
  url: string;
}
export const AppBannerImage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
  }),
).annotate({ identifier: "AppBannerImage" }) as any as S.Schema<AppBannerImage>;

export type AppBusinessesCreatedLogoUrlsList = Array<string>;
export const AppBusinessesCreatedLogoUrlsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AppBusinessesCreatedLogoUrlsList>;

export interface AppCreator {
  /** User ID, prefixed `user_`. */
  id: string;
  /** Display name. */
  name: string | null;
  /** Public username. */
  username: string;
}
export const AppCreator = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({ identifier: "AppCreator" }) as any as S.Schema<AppCreator>;

export interface AppDefaultApiKey {
  /** API key ID, prefixed `apik_`. */
  id: string;
  /** Human-readable name identifying the API key, or `null` when none was set. */
  name: string | null;
  /** Masked version of the secret key, so the key can be recognized without revealing the full secret. */
  obfuscated_secret_key: string | Redacted.Redacted<string>;
  /** The full secret used to authenticate requests. `null` unless the caller could have created the key themselves. */
  secret_key: string | Redacted.Redacted<string> | null;
}
export const AppDefaultApiKey = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    obfuscated_secret_key: S.String.pipe(T.SensitiveValue({})),
    secret_key: S.NullOr(S.String).pipe(T.SensitiveValue({})),
  }),
).annotate({
  identifier: "AppDefaultApiKey",
}) as any as S.Schema<AppDefaultApiKey>;

/** The stage a running deployment has reached, or `null` when none is running. Later phases dominate the wall clock: `process_archive` waits on the upload pipeline and `promote` waits for the build to go live. */
export type AppDeploymentPhase =
  | "install"
  | "build"
  | "typecheck"
  | "upload_build"
  | "upload_source"
  | "process_archive"
  | "create_build"
  | "promote";
export const AppDeploymentPhase = /*@__PURE__*/ S.String;

/** Whether the app has anything to publish, and what a publish in flight is doing. `unpublished` means publishing would ship something new; `no_source` means the sandbox holds no copy of this app, so there is nothing to publish from. */
export type AppDeploymentStatus =
  | "published"
  | "unpublished"
  | "publishing"
  | "failed"
  | "no_source";
export const AppDeploymentStatus = /*@__PURE__*/ S.String;

export interface AppDeployment {
  /** The app being deployed, prefixed `app_`. */
  app_id: string;
  /** The build the deployment produced, prefixed `abld_`, or `null` until it succeeds. */
  build_id: string | null;
  /** Whether the running or last deployment uploaded a build without making it live. */
  draft: boolean;
  /** Why the deployment failed, or `null` when it did not. */
  error: string | null;
  /** How long this deployment is expected to take in total, estimated from previous runs. */
  estimated_duration_ms: number | null;
  /** How much longer the deployment is expected to take. Held above zero until it actually finishes. */
  estimated_remaining_ms: number | null;
  /** When the deployment ended, in milliseconds since the epoch, or `null` while it is still running. */
  finished_at: number | null;
  /** The stage a running deployment has reached, or `null` when none is running. Later phases dominate the wall clock: `process_archive` waits on the upload pipeline and `promote` waits for the build to go live. */
  phase: AppDeploymentPhase | null;
  /** Fraction of the deployment estimated to be complete, from 0 to 1. Stops just short of 1 until the run ends. */
  progress: number | null;
  /** When the deployment began, in milliseconds since the epoch, or `null` when none has run. */
  started_at: number | null;
  /** Whether the app has anything to publish, and what a publish in flight is doing. `unpublished` means publishing would ship something new; `no_source` means the sandbox holds no copy of this app, so there is nothing to publish from. */
  status: AppDeploymentStatus;
  /** Where the deployed site is served, or `null` unless the deployment went live. */
  url: string | null;
}
export const AppDeployment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    app_id: S.String,
    build_id: S.NullOr(S.String),
    draft: S.Boolean,
    error: S.NullOr(S.String),
    estimated_duration_ms: S.NullOr(S.Number),
    estimated_remaining_ms: S.NullOr(S.Number),
    finished_at: S.NullOr(S.Number),
    phase: S.NullOr(AppDeploymentPhase),
    progress: S.NullOr(S.Number),
    started_at: S.NullOr(S.Number),
    status: AppDeploymentStatus,
    url: S.NullOr(S.String),
  }),
).annotate({ identifier: "AppDeployment" }) as any as S.Schema<AppDeployment>;

/** Whop Elements the app's production web build mounts, as `<namespace>.<element>` keys (sub-controller children take a third segment, e.g. `payments.cardFields.cardNumber`). A bare namespace means the build reaches that namespace but the individual elements could not be resolved. Empty when the build mounts none, when it has not been scanned yet, or when the app has no production web build. */
export type AppElementsUsedItem =
  | "ads"
  | "ads.billing-setup"
  | "ads.campaign-creator"
  | "ads.reporting"
  | "ads.reporting.chart"
  | "ads.reporting.table"
  | "checkout"
  | "checkout.checkout"
  | "checkout.expressCheckout"
  | "payments"
  | "payments.address"
  | "payments.branding"
  | "payments.card"
  | "payments.cardFields"
  | "payments.cardFields.cardCvc"
  | "payments.cardFields.cardExpiry"
  | "payments.cardFields.cardNumber"
  | "payments.email"
  | "payments.payment"
  | "payments.taxId"
  | "tracking"
  | "tracking.events"
  | "tracking.people"
  | "wallet"
  | "wallet.activity"
  | "wallet.balances"
  | "wallet.balances.balance"
  | "wallet.balances.list"
  | "wallet.cards"
  | "wallet.deposit"
  | "wallet.send"
  | "wallet.withdraw"
  | "websites"
  | "websites.pixel-setup"
  | "websites.websites";
export const AppElementsUsedItem = /*@__PURE__*/ S.String;

export type AppElementsUsedList = Array<AppElementsUsedItem>;
export const AppElementsUsedList = /*@__PURE__*/ S.Array(
  AppElementsUsedItem,
) as any as S.Schema<AppElementsUsedList>;

export interface AppIcon {
  /** Icon image URL. Always present — the default app icon when none is uploaded. */
  url: string;
}
export const AppIcon = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
  }),
).annotate({ identifier: "AppIcon" }) as any as S.Schema<AppIcon>;

/** Approval status of the app's product listing on the Whop app store, or `null` when the app has no associated product. */
export type AppMarketplaceStatus =
  | "not_available"
  | "pending_review"
  | "live_marketplace";
export const AppMarketplaceStatus = /*@__PURE__*/ S.String;

/** How the app authenticates at the OAuth token endpoint. */
export type AppOauthClientType = "public" | "confidential";
export const AppOauthClientType = /*@__PURE__*/ S.String;

/** The build's review status. */
export type AppProductionBuildStatus =
  | "draft"
  | "pending"
  | "approved"
  | "rejected";
export const AppProductionBuildStatus = /*@__PURE__*/ S.String;

export interface AppProductionBuild {
  /** Client-generated checksum of the build file, used to verify file integrity. */
  checksum: string | null;
  /** URL to download the uploaded build artifact. */
  file_url: string | null;
  /** App build ID, prefixed `abld_`. */
  id: string;
  /** URL to download the compressed source code archive that produced this build, or `null` when the build was uploaded without a source archive. */
  source_url: string | null;
  /** The build's review status. */
  status: AppProductionBuildStatus;
}
export const AppProductionBuild = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    checksum: S.NullOr(S.String),
    file_url: S.NullOr(S.String),
    id: S.String,
    source_url: S.NullOr(S.String),
    status: AppProductionBuildStatus,
  }),
).annotate({
  identifier: "AppProductionBuild",
}) as any as S.Schema<AppProductionBuild>;

export type AppRedirectUrisList = Array<string>;
export const AppRedirectUrisList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AppRedirectUrisList>;

export interface AppRequestedPermissionAction {
  /** The permission action's identifier, for example `company:basic:read`. */
  action: string;
  /** Human-readable name of the action. */
  name: string;
}
export const AppRequestedPermissionAction = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: S.String,
    name: S.String,
  }),
).annotate({
  identifier: "AppRequestedPermissionAction",
}) as any as S.Schema<AppRequestedPermissionAction>;

export interface AppRequestedPermission {
  /** Whether the app requires the permission to be granted on install, as opposed to requesting it optionally. */
  is_required: boolean;
  /** The developer's explanation of why the app needs the permission, or `null` when none was provided. */
  justification: string | null;
  /** The permission action the app requests. */
  permission_action: AppRequestedPermissionAction;
}
export const AppRequestedPermission = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    is_required: S.Boolean,
    justification: S.NullOr(S.String),
    permission_action: AppRequestedPermissionAction,
  }),
).annotate({
  identifier: "AppRequestedPermission",
}) as any as S.Schema<AppRequestedPermission>;

export type AppRequestedPermissionsList = Array<AppRequestedPermission>;
export const AppRequestedPermissionsList = /*@__PURE__*/ S.Array(
  AppRequestedPermission,
) as any as S.Schema<AppRequestedPermissionsList>;

/** Scopes the app asks a user to grant when they authorize it, shown on the consent screen. */
export type AppRequiredScopesItem = "read_user";
export const AppRequiredScopesItem = /*@__PURE__*/ S.String;

export type AppRequiredScopesList = Array<AppRequiredScopesItem>;
export const AppRequiredScopesList = /*@__PURE__*/ S.Array(
  AppRequiredScopesItem,
) as any as S.Schema<AppRequiredScopesList>;

/** Visibility on the Whop app store: `live` is publicly discoverable, `unlisted` is accessible only via direct link, `hidden` is not visible anywhere. */
export type AppStatus = "live" | "unlisted" | "hidden";
export const AppStatus = /*@__PURE__*/ S.String;

export interface App {
  /** The account that owns the app. */
  account: AccountParent;
  /** Legacy app API key used to authenticate requests on the app's behalf. `null` when no key exists or the caller lacks the `developer:manage_api_key` permission. */
  api_key: AppApiKey | null;
  /** Detailed description shown on the app store's in-depth app page, or `null` when none has been set. */
  app_store_description: string | null;
  /** The type of end-user the app is built for. */
  app_type: AppAppType;
  /** Banner image from the app's product listing, or `null` when none is uploaded. */
  banner_image: AppBannerImage | null;
  /** The production base URL where the app is hosted. `null` if no base URL is configured, if the caller lacks the `developer:basic:read` permission on the app's account, or on list responses, which never expose it. */
  base_url: string | null;
  /** Website businesses created from this app as a template. */
  businesses_created_count: number;
  businesses_created_logo_urls: AppBusinessesCreatedLogoUrlsList;
  /** The user who owns the publishing account. */
  creator: AppCreator;
  /** URL path for the account dashboard view, or `null` when not configured. */
  dashboard_path: string | null;
  /** The app's default API key. `null` when the app has no default key or the caller lacks the `developer:manage_api_key` permission; `secret_key` is additionally `null` unless the caller could have created the key themselves. */
  default_api_key: AppDefaultApiKey | null;
  /** What the app has left to publish, and how a publish in flight is going — `status` is only ever `unpublished`, `publishing`, or `failed` here. `null` means there is nothing to report: the app is fully published, there is no working copy to publish from, or the caller cannot deploy this app. Tell those apart from the app's own `production_web_build`. */
  deployment: AppDeployment | null;
  /** Short description shown in listings and search results, or `null` if none has been set. */
  description: string | null;
  /** URL path for the discover view, or `null` when not configured. */
  discover_path: string | null;
  /** Subdomain identifier for the app's proxied URL, forming https://{domain_id}.apps.whop.com. */
  domain_id: string;
  elements_used: AppElementsUsedList;
  /** URL path for the member-facing hub view, or `null` when not configured. */
  experience_path: string | null;
  /** Full URL where the app's hosted web build is served, or `null` if no route is claimed. */
  hosted_url: string | null;
  /** The app's icon. Falls back to the default app icon when none is uploaded. */
  icon: AppIcon;
  /** App ID, prefixed `app_`. */
  id: string;
  /** Approval status of the app's product listing on the Whop app store, or `null` when the app has no associated product. */
  marketplace_status: AppMarketplaceStatus | null;
  /** Display name shown on the app store and in experience navigation. */
  name: string;
  /** How the app authenticates at the OAuth token endpoint. */
  oauth_client_type: AppOauthClientType;
  /** URL path to the app's OpenAPI spec file, or `null` when not configured. */
  openapi_path: string | null;
  /** Full origin URL of the app's proxied domain, for example https://ab1c2d3e4f.apps.whop.com. */
  origin: string | null;
  /** ID of the app's product listing on the Whop app store, or `null` when the app has no associated product. */
  product_id: string | null;
  /** The approved build currently served on Android, or `null` when none is deployed. */
  production_android_build: AppProductionBuild | null;
  /** The approved build currently served on iOS, or `null` when none is deployed. */
  production_ios_build: AppProductionBuild | null;
  /** The approved build currently served on web, or `null` when none is deployed. */
  production_web_build: AppProductionBuild | null;
  redirect_uris: AppRedirectUrisList;
  requested_permissions: AppRequestedPermissionsList;
  required_scopes: AppRequiredScopesList;
  /** Claimed subdomain route where hosted web builds are served (`myapp` for myapp.whop.app), or `null` if no route is claimed. */
  route: string | null;
  /** The app's production secrets as an object of string values, injected into the hosted server runtime. `null` when the caller lacks the `developer:update_app` permission. */
  secrets: unknown | null;
  /** URL path to the app's skills directory, or `null` when not configured. */
  skills_path: string | null;
  /** Visibility on the Whop app store: `live` is publicly discoverable, `unlisted` is accessible only via direct link, `hidden` is not visible anywhere. */
  status: AppStatus;
  /** Whether the app has been verified by Whop and is eligible for the featured apps section. */
  verified: boolean;
}
export const App = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account: AccountParent,
    api_key: S.NullOr(AppApiKey),
    app_store_description: S.NullOr(S.String),
    app_type: AppAppType,
    banner_image: S.NullOr(AppBannerImage),
    base_url: S.NullOr(S.String),
    businesses_created_count: S.Number,
    businesses_created_logo_urls: AppBusinessesCreatedLogoUrlsList,
    creator: AppCreator,
    dashboard_path: S.NullOr(S.String),
    default_api_key: S.NullOr(AppDefaultApiKey),
    deployment: S.NullOr(AppDeployment),
    description: S.NullOr(S.String),
    discover_path: S.NullOr(S.String),
    domain_id: S.String,
    elements_used: AppElementsUsedList,
    experience_path: S.NullOr(S.String),
    hosted_url: S.NullOr(S.String),
    icon: AppIcon,
    id: S.String,
    marketplace_status: S.NullOr(AppMarketplaceStatus),
    name: S.String,
    oauth_client_type: AppOauthClientType,
    openapi_path: S.NullOr(S.String),
    origin: S.NullOr(S.String),
    product_id: S.NullOr(S.String),
    production_android_build: S.NullOr(AppProductionBuild),
    production_ios_build: S.NullOr(AppProductionBuild),
    production_web_build: S.NullOr(AppProductionBuild),
    redirect_uris: AppRedirectUrisList,
    requested_permissions: AppRequestedPermissionsList,
    required_scopes: AppRequiredScopesList,
    route: S.NullOr(S.String),
    secrets: S.NullOr(S.Unknown),
    skills_path: S.NullOr(S.String),
    status: AppStatus,
    verified: S.Boolean,
  }),
).annotate({ identifier: "App" }) as any as S.Schema<App>;

export interface DeleteAppRequest {
  /** App ID (prefixed `app_`), the app's claimed route, or its proxy domain id. */
  id: string;
}
export const DeleteAppRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/apps/{id}", code: 200 })),
).annotate({
  identifier: "DeleteAppRequest",
}) as any as S.Schema<DeleteAppRequest>;

export interface DeleteAppResponse {
  /** Always true. */
  deleted: boolean;
  /** ID of the deleted app. */
  id: string;
}
export const DeleteAppResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "DeleteAppResponse",
}) as any as S.Schema<DeleteAppResponse>;

export interface DeployAppRequest {
  /** The app to deploy, prefixed `app_`. */
  id: string;
  /** Upload the build without making it live. Defaults to `false`, which deploys and promotes in one step. */
  draft?: boolean;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const DeployAppRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    draft: S.optional(S.Boolean),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/apps/{id}/deploy", code: 200 })),
).annotate({
  identifier: "DeployAppRequest",
}) as any as S.Schema<DeployAppRequest>;

export type ListAppLogsRequestLevel =
  | "log"
  | "debug"
  | "info"
  | "warn"
  | "error";
export const ListAppLogsRequestLevel = /*@__PURE__*/ S.String;

export interface ListAppLogsRequest {
  /** The ID of the app, which will look like app_*************. */
  id: string;
  /** Only return logs from this build. */
  app_build_id?: string;
  /** Only return console lines of this level. */
  level?: ListAppLogsRequestLevel | (string & {});
  /** Only return logs whose message contains this text (case-insensitive). */
  query?: string;
  /** Start of the time window as an ISO 8601 timestamp. Defaults to 7 days before created_before. */
  created_after?: string;
  /** End of the time window as an ISO 8601 timestamp. Defaults to now. */
  created_before?: string;
  /** The number of log lines to return (max 500). */
  first?: number;
  /** A cursor for fetching logs after a previous page. */
  after?: string;
  /** A cursor for fetching logs before a later page. */
  before?: string;
}
export const ListAppLogsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    app_build_id: S.optional(S.String.pipe(T.Query())),
    level: S.optional(ListAppLogsRequestLevel.pipe(T.Query())),
    query: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/apps/{id}/logs", code: 200 })),
).annotate({
  identifier: "ListAppLogsRequest",
}) as any as S.Schema<ListAppLogsRequest>;

export type ListAppLogsResponseDataItemLevel =
  | "log"
  | "debug"
  | "info"
  | "warn"
  | "error";
export const ListAppLogsResponseDataItemLevel = /*@__PURE__*/ S.String;

export type ListAppLogsResponseDataItemSource =
  | "console"
  | "exception"
  | "request";
export const ListAppLogsResponseDataItemSource = /*@__PURE__*/ S.String;

export interface ListAppLogsResponseDataItem {
  app_build_id: string;
  app_id: string;
  cpu_time_ms?: number;
  created_at: string;
  level: ListAppLogsResponseDataItemLevel;
  message: string;
  outcome?: string | null;
  request_id: string;
  request_method?: string | null;
  request_path?: string | null;
  response_status?: number | null;
  source: ListAppLogsResponseDataItemSource;
  stack?: string | null;
  truncated?: boolean;
  wall_time_ms?: number;
}
export const ListAppLogsResponseDataItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    app_build_id: S.String,
    app_id: S.String,
    cpu_time_ms: S.optional(S.Number),
    created_at: S.String,
    level: ListAppLogsResponseDataItemLevel,
    message: S.String,
    outcome: S.optional(S.NullOr(S.String)),
    request_id: S.String,
    request_method: S.optional(S.NullOr(S.String)),
    request_path: S.optional(S.NullOr(S.String)),
    response_status: S.optional(S.NullOr(S.Number)),
    source: ListAppLogsResponseDataItemSource,
    stack: S.optional(S.NullOr(S.String)),
    truncated: S.optional(S.Boolean),
    wall_time_ms: S.optional(S.Number),
  }),
).annotate({
  identifier: "ListAppLogsResponseDataItem",
}) as any as S.Schema<ListAppLogsResponseDataItem>;

export type ListAppLogsResponseDataList = Array<ListAppLogsResponseDataItem>;
export const ListAppLogsResponseDataList = /*@__PURE__*/ S.Array(
  ListAppLogsResponseDataItem,
) as any as S.Schema<ListAppLogsResponseDataList>;

export interface ListAppLogsResponsePageInfo {
  end_cursor?: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor?: string | null;
}
export const ListAppLogsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.optional(S.NullOr(S.String)),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "ListAppLogsResponsePageInfo",
}) as any as S.Schema<ListAppLogsResponsePageInfo>;

export interface ListAppLogsResponse {
  data: ListAppLogsResponseDataList;
  page_info: ListAppLogsResponsePageInfo;
}
export const ListAppLogsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListAppLogsResponseDataList,
    page_info: ListAppLogsResponsePageInfo,
  }),
).annotate({
  identifier: "ListAppLogsResponse",
}) as any as S.Schema<ListAppLogsResponse>;

export type ListAppsRequestAppType =
  | "b2b_app"
  | "b2c_app"
  | "company_app"
  | "component"
  | "website";
export const ListAppsRequestAppType = /*@__PURE__*/ S.String;

export type ListAppsRequestViewType =
  | "hub"
  | "discover"
  | "dash"
  | "dashboard"
  | "analytics"
  | "skills"
  | "openapi";
export const ListAppsRequestViewType = /*@__PURE__*/ S.String;

export type ListAppsRequestOrder =
  | "created_at"
  | "discoverable_at"
  | "template_usage"
  | "total_installs_last_30_days"
  | "total_installs_last_7_days";
export const ListAppsRequestOrder = /*@__PURE__*/ S.String;

export type ListAppsRequestDirection = "asc" | "desc";
export const ListAppsRequestDirection = /*@__PURE__*/ S.String;

export interface ListAppsRequest {
  /** Only return apps created by this account (`biz_` tag). With developer access to the account this includes its unlisted and hidden apps. */
  account_id?: string;
  /** Filter apps by the type of end-user they are built for. Apps of type `website` are left out unless you ask for them by name. */
  app_type?: ListAppsRequestAppType | (string & {});
  /** Only return apps supporting this view type, such as `dashboard` or `hub`. */
  view_type?: ListAppsRequestViewType | (string & {});
  /** Only return apps whose Whop verification status matches this value. Omit this filter to include every verification status the caller can see. */
  verified?: boolean;
  /** Legacy compatibility filter. Use `verified` for field equality. `true` returns verified apps; clients pinned before `2026-08-25-2` retain the earlier public website discovery behavior. */
  verified_apps_only?: boolean;
  /** Only return apps Whop recommends (or, with `false`, only those it does not), independently of verification status. */
  recommended?: boolean;
  /** A search string matched against app names. */
  query?: string;
  /** The field to sort apps by. Defaults to discoverable_at, showing the most recently published apps first. `template_usage` ranks Whop-verified apps first, then apps with a banner image, then by how many apps were created from each app as a template. */
  order?: ListAppsRequestOrder | (string & {});
  /** Sort direction. */
  direction?: ListAppsRequestDirection | (string & {});
  /** The number of apps to return (default 20, max 100). */
  first?: number;
  /** A cursor; returns apps after this position. */
  after?: string;
  /** The number of apps to return from the end of the range. */
  last?: number;
  /** A cursor; returns apps before this position. */
  before?: string;
}
export const ListAppsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    app_type: S.optional(ListAppsRequestAppType.pipe(T.Query())),
    view_type: S.optional(ListAppsRequestViewType.pipe(T.Query())),
    verified: S.optional(S.Boolean.pipe(T.Query())),
    verified_apps_only: S.optional(S.Boolean.pipe(T.Query())),
    recommended: S.optional(S.Boolean.pipe(T.Query())),
    query: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListAppsRequestOrder.pipe(T.Query())),
    direction: S.optional(ListAppsRequestDirection.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/apps", code: 200 })),
).annotate({
  identifier: "ListAppsRequest",
}) as any as S.Schema<ListAppsRequest>;

/** The type of end-user the app is built for. */
export type AppListItemAppType =
  | "b2b_app"
  | "b2c_app"
  | "company_app"
  | "component"
  | "website";
export const AppListItemAppType = /*@__PURE__*/ S.String;

export type AppListItemBusinessesCreatedLogoUrlsList = Array<string>;
export const AppListItemBusinessesCreatedLogoUrlsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AppListItemBusinessesCreatedLogoUrlsList>;

/** Visibility on the Whop app store: `live` is publicly discoverable, `unlisted` is accessible only via direct link, `hidden` is not visible anywhere. */
export type AppListItemStatus = "live" | "unlisted" | "hidden";
export const AppListItemStatus = /*@__PURE__*/ S.String;

export interface AppListItem {
  /** The account that owns the app. */
  account: AccountParent;
  /** The type of end-user the app is built for. */
  app_type: AppListItemAppType;
  /** Banner image from the app's product listing, or `null` when none is uploaded. */
  banner_image: AppBannerImage | null;
  /** The production base URL where the app is hosted. `null` if no base URL is configured, if the caller lacks the `developer:basic:read` permission on the app's account, or on list responses, which never expose it. */
  base_url: string | null;
  /** Number of businesses created from this app as a template. */
  businesses_created_count: number;
  businesses_created_logo_urls: AppListItemBusinessesCreatedLogoUrlsList;
  /** The user who owns the publishing account. */
  creator: AppCreator;
  /** URL path for the account dashboard view, or `null` when not configured. */
  dashboard_path: string | null;
  /** Short description shown in listings and search results, or `null` if none has been set. */
  description: string | null;
  /** URL path for the discover view, or `null` when not configured. */
  discover_path: string | null;
  /** Subdomain identifier for the app's proxied URL, forming https://{domain_id}.apps.whop.com. */
  domain_id: string;
  /** URL path for the member-facing hub view, or `null` when not configured. */
  experience_path: string | null;
  /** Full URL where the app's hosted web build is served, or `null` if no route is claimed. */
  hosted_url: string | null;
  /** The app's icon. Falls back to the default app icon when none is uploaded. */
  icon: AppIcon;
  /** App ID, prefixed `app_`. */
  id: string;
  /** Display name shown on the app store and in experience navigation. */
  name: string;
  /** URL path to the app's OpenAPI spec file, or `null` when not configured. */
  openapi_path: string | null;
  /** Full origin URL of the app's proxied domain, for example https://ab1c2d3e4f.apps.whop.com. */
  origin: string | null;
  /** Claimed subdomain route where hosted web builds are served (`myapp` for myapp.whop.app), or `null` if no route is claimed. */
  route: string | null;
  /** URL path to the app's skills directory, or `null` when not configured. */
  skills_path: string | null;
  /** Visibility on the Whop app store: `live` is publicly discoverable, `unlisted` is accessible only via direct link, `hidden` is not visible anywhere. */
  status: AppListItemStatus;
  /** Whether the app has been verified by Whop and is eligible for the featured apps section. */
  verified: boolean;
}
export const AppListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account: AccountParent,
    app_type: AppListItemAppType,
    banner_image: S.NullOr(AppBannerImage),
    base_url: S.NullOr(S.String),
    businesses_created_count: S.Number,
    businesses_created_logo_urls: AppListItemBusinessesCreatedLogoUrlsList,
    creator: AppCreator,
    dashboard_path: S.NullOr(S.String),
    description: S.NullOr(S.String),
    discover_path: S.NullOr(S.String),
    domain_id: S.String,
    experience_path: S.NullOr(S.String),
    hosted_url: S.NullOr(S.String),
    icon: AppIcon,
    id: S.String,
    name: S.String,
    openapi_path: S.NullOr(S.String),
    origin: S.NullOr(S.String),
    route: S.NullOr(S.String),
    skills_path: S.NullOr(S.String),
    status: AppListItemStatus,
    verified: S.Boolean,
  }),
).annotate({ identifier: "AppListItem" }) as any as S.Schema<AppListItem>;

export type ListAppsResponseDataList = Array<AppListItem>;
export const ListAppsResponseDataList = /*@__PURE__*/ S.Array(
  AppListItem,
) as any as S.Schema<ListAppsResponseDataList>;

export interface ListAppsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListAppsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListAppsResponsePageInfo",
}) as any as S.Schema<ListAppsResponsePageInfo>;

export interface ListAppsResponse {
  data: ListAppsResponseDataList;
  page_info: ListAppsResponsePageInfo;
}
export const ListAppsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListAppsResponseDataList,
    page_info: ListAppsResponsePageInfo,
  }),
).annotate({
  identifier: "ListAppsResponse",
}) as any as S.Schema<ListAppsResponse>;

export interface RetrieveAppRequest {
  /** App ID (prefixed `app_`), the app's claimed route, or its proxy domain id. */
  id: string;
}
export const RetrieveAppRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/apps/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveAppRequest",
}) as any as S.Schema<RetrieveAppRequest>;

/** The type of end-user the app is built for. Cannot be changed on an app whose type is already `website`. */
export type UpdateAppRequestAppType =
  | "b2b_app"
  | "b2c_app"
  | "company_app"
  | "component"
  | "website";
export const UpdateAppRequestAppType = /*@__PURE__*/ S.String;

/** The icon image for the app in PNG, JPEG, or GIF format, referencing an uploaded file: `{ id }` for an existing attachment or `{ direct_upload_id }` for a new direct upload. */
export type UpdateAppRequestIcon = CreateAppRequestIcon;
export const UpdateAppRequestIcon = CreateAppRequestIcon;

/** How the app authenticates at the OAuth token endpoint. */
export type UpdateAppRequestOauthClientType = "public" | "confidential";
export const UpdateAppRequestOauthClientType = /*@__PURE__*/ S.String;

/** The whitelisted OAuth callback URLs users are redirected to after authorizing the app. */
export type UpdateAppRequestRedirectUrisList = Array<string>;
export const UpdateAppRequestRedirectUrisList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpdateAppRequestRedirectUrisList>;

/** The OAuth scopes the app requests from users when they install it. */
export type UpdateAppRequestRequiredScopesList = Array<string>;
export const UpdateAppRequestRequiredScopesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpdateAppRequestRequiredScopesList>;

/** Controls whether the app is published on Whop discovery or accessible only through its direct link. Publishing requires a name, icon, and description. */
export type UpdateAppRequestStatus = "live" | "unlisted" | "hidden";
export const UpdateAppRequestStatus = /*@__PURE__*/ S.String;

export interface UpdateAppRequest {
  /** App ID (prefixed `app_`), the app's claimed route, or its proxy domain id. */
  id: string;
  /** The detailed description shown on the app store's in-depth app view page. */
  app_store_description?: string;
  /** The type of end-user the app is built for. Cannot be changed on an app whose type is already `website`. */
  app_type?: UpdateAppRequestAppType | (string & {});
  /** The base production URL where the app is hosted. Set to `null` to take the app proxy offline. */
  base_url?: string | null;
  /** The URL path for the account dashboard view. */
  dashboard_path?: string | null;
  /** A short description of the app shown in listings and search results. */
  description?: string;
  /** The URL path for the discover view. */
  discover_path?: string | null;
  /** The URL path for the member-facing hub view, such as `/experiences/[experienceId]`. */
  experience_path?: string | null;
  /** The icon image for the app in PNG, JPEG, or GIF format, referencing an uploaded file: `{ id }` for an existing attachment or `{ direct_upload_id }` for a new direct upload. */
  icon?: CreateAppRequestIcon;
  /** The display name for the app, shown to users on the app store and product pages. */
  name?: string;
  /** How the app authenticates at the OAuth token endpoint. */
  oauth_client_type?: UpdateAppRequestOauthClientType | (string & {});
  /** The URL path to the app's OpenAPI spec file (requires the ai_chat capability). */
  openapi_path?: string | null;
  /** The app build (`abld_` tag) to serve as the Android production build, or `null` to unassign it. Same rules as `production_web_build_id`. */
  production_android_build_id?: string | null;
  /** The app build (`abld_` tag) to serve as the iOS production build, or `null` to unassign it. Same rules as `production_web_build_id`. */
  production_ios_build_id?: string | null;
  /** The app build (`abld_` tag) to serve as the web production build, or `null` to unassign it. The build must belong to this app, target web, and be in the draft or approved status; a draft build is queued for approval and takes over once approved. Requires the `developer:manage_builds` scope. */
  production_web_build_id?: string | null;
  /** The whitelisted OAuth callback URLs users are redirected to after authorizing the app. */
  redirect_uris?: UpdateAppRequestRedirectUrisList;
  /** The OAuth scopes the app requests from users when they install it. */
  required_scopes?: UpdateAppRequestRequiredScopesList;
  /** The subdomain route where the app's hosted web builds are served. */
  route?: string;
  /** Secrets to add or overwrite on the app, as an object of string values. Keys not included are left untouched; pass null or an empty string as the value to delete a secret. Encrypted at rest and injected into the app's hosted server runtime. */
  secrets?: unknown;
  /** The URL path to the app's skills directory (requires the ai_chat capability). */
  skills_path?: string | null;
  /** Controls whether the app is published on Whop discovery or accessible only through its direct link. Publishing requires a name, icon, and description. */
  status?: UpdateAppRequestStatus | (string & {});
}
export const UpdateAppRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    app_store_description: S.optional(S.String),
    app_type: S.optional(UpdateAppRequestAppType),
    base_url: S.optional(S.NullOr(S.String)),
    dashboard_path: S.optional(S.NullOr(S.String)),
    description: S.optional(S.String),
    discover_path: S.optional(S.NullOr(S.String)),
    experience_path: S.optional(S.NullOr(S.String)),
    icon: S.optional(CreateAppRequestIcon),
    name: S.optional(S.String),
    oauth_client_type: S.optional(UpdateAppRequestOauthClientType),
    openapi_path: S.optional(S.NullOr(S.String)),
    production_android_build_id: S.optional(S.NullOr(S.String)),
    production_ios_build_id: S.optional(S.NullOr(S.String)),
    production_web_build_id: S.optional(S.NullOr(S.String)),
    redirect_uris: S.optional(UpdateAppRequestRedirectUrisList),
    required_scopes: S.optional(UpdateAppRequestRequiredScopesList),
    route: S.optional(S.String),
    secrets: S.optional(S.Unknown),
    skills_path: S.optional(S.NullOr(S.String)),
    status: S.optional(UpdateAppRequestStatus),
  }).pipe(T.Http({ method: "PATCH", uri: "/apps/{id}", code: 200 })),
).annotate({
  identifier: "UpdateAppRequest",
}) as any as S.Schema<UpdateAppRequest>;

export interface UpdateAppPermissionsRequestRequestedPermissionsItem {
  /** The permission action, for example `company:basic:read`. */
  action: string;
  /** Whether installing the app requires granting this permission. */
  is_required: boolean;
  /** Why the app needs this permission (20-512 characters), shown to the installing user. */
  justification: string;
}
export const UpdateAppPermissionsRequestRequestedPermissionsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      action: S.String,
      is_required: S.Boolean,
      justification: S.String,
    }),
  ).annotate({
    identifier: "UpdateAppPermissionsRequestRequestedPermissionsItem",
  }) as any as S.Schema<UpdateAppPermissionsRequestRequestedPermissionsItem>;

/** The full set of permissions the app requests on install; permissions not listed are removed. */
export type UpdateAppPermissionsRequestRequestedPermissionsList =
  Array<UpdateAppPermissionsRequestRequestedPermissionsItem>;
export const UpdateAppPermissionsRequestRequestedPermissionsList =
  /*@__PURE__*/ S.Array(
    UpdateAppPermissionsRequestRequestedPermissionsItem,
  ) as any as S.Schema<UpdateAppPermissionsRequestRequestedPermissionsList>;

export interface UpdateAppPermissionsRequest {
  /** App ID, prefixed `app_`. */
  id: string;
  /** The full set of permissions the app requests on install; permissions not listed are removed. */
  requested_permissions: UpdateAppPermissionsRequestRequestedPermissionsList;
}
export const UpdateAppPermissionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    requested_permissions: UpdateAppPermissionsRequestRequestedPermissionsList,
  }).pipe(
    T.Http({ method: "PATCH", uri: "/apps/{id}/permissions", code: 200 }),
  ),
).annotate({
  identifier: "UpdateAppPermissionsRequest",
}) as any as S.Schema<UpdateAppPermissionsRequest>;

/** Input for creating a requested permission */
export interface UpdatePermissionsAppRequestRequestedPermissionsItem {
  /** The action that the app will request off of users when a user installs the app. */
  action: string;
  /** Whether the action is required for the app to function. */
  is_required: boolean;
  /** The reason for requesting the action. */
  justification: string;
}
export const UpdatePermissionsAppRequestRequestedPermissionsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      action: S.String,
      is_required: S.Boolean,
      justification: S.String,
    }),
  ).annotate({
    identifier: "UpdatePermissionsAppRequestRequestedPermissionsItem",
  }) as any as S.Schema<UpdatePermissionsAppRequestRequestedPermissionsItem>;

/** The permissions that the app will request off of users when a user installs the app. */
export type UpdatePermissionsAppRequestRequestedPermissionsList =
  Array<UpdatePermissionsAppRequestRequestedPermissionsItem>;
export const UpdatePermissionsAppRequestRequestedPermissionsList =
  /*@__PURE__*/ S.Array(
    UpdatePermissionsAppRequestRequestedPermissionsItem,
  ) as any as S.Schema<UpdatePermissionsAppRequestRequestedPermissionsList>;

export interface UpdatePermissionsAppRequest {
  /** The ID of the app the permission requirements are being updated for */
  app_id: string;
  /** The permissions that the app will request off of users when a user installs the app. */
  requested_permissions: UpdatePermissionsAppRequestRequestedPermissionsList;
}
export const UpdatePermissionsAppRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    app_id: S.String.pipe(T.Label()),
    requested_permissions: UpdatePermissionsAppRequestRequestedPermissionsList,
  }).pipe(
    T.Http({ method: "PATCH", uri: "/apps/{app_id}/permissions", code: 200 }),
  ),
).annotate({
  identifier: "UpdatePermissionsAppRequest",
}) as any as S.Schema<UpdatePermissionsAppRequest>;

export type UpdatePermissionsAppResponse = boolean;
export const UpdatePermissionsAppResponse = /*@__PURE__*/ S.suspend(() =>
  S.Boolean.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "UpdatePermissionsAppResponse",
}) as any as S.Schema<UpdatePermissionsAppResponse>;

export type CreateAppError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Create App Registers a new app on the Whop developer platform. Apps provide custom experiences that can be added to products. */
export const createApp: API.OperationMethod<
  CreateAppRequest,
  App,
  CreateAppError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAppRequest,
  output: App,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteAppError = Forbidden | NotFound | WhopOpError;
/** Delete App Deletes an app. The app stops resolving within seconds — a website's site stops serving, and any claimed subdomain is reserved for a month before it can be claimed again. */
export const deleteApp: API.OperationMethod<
  DeleteAppRequest,
  DeleteAppResponse,
  DeleteAppError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteAppRequest,
  output: DeleteAppResponse,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeployAppError = NotFound | Conflict | WhopOpError;
/** Deploy App Builds the app's current source and ships it. Returns the run it started, so the caller can render progress from this response and then follow it on the app's `deployment` field. Only one deployment runs per app at a time — calling this while one is in flight reports that run rather than starting a second, and calling it with nothing to publish reports that instead of starting one. */
export const deployApp: API.OperationMethod<
  DeployAppRequest,
  AppDeployment,
  DeployAppError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeployAppRequest,
  output: AppDeployment,
  errors: [NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListAppLogsError = Forbidden | NotFound | WhopOpError;
/** List App Logs Lists a hosted app's server runtime logs, most recent first: console output, uncaught exceptions, and failed-request summaries captured on whop.app hosting. Logs are retained for 7 days. */
export const listAppLogs: API.PaginatedOperationMethod<
  ListAppLogsRequest,
  ListAppLogsResponse,
  ListAppLogsError,
  WhopOpContext,
  ListAppLogsResponseDataItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListAppLogsRequest,
    output: ListAppLogsResponse,
    errors: [Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ListAppsError = BadRequest | WhopOpError;
/** List Apps Lists apps on the Whop platform: the app store's live apps, or — with `account_id` and developer access to that account — every app the account owns. Requires authentication except for Whop's public app and website discovery lists. Public website discovery includes built official blueprints (verified apps with a product) and built, live community blueprints that Whop recommends. */
export const listApps: API.PaginatedOperationMethod<
  ListAppsRequest,
  ListAppsResponse,
  ListAppsError,
  WhopOpContext,
  AppListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListAppsRequest,
    output: ListAppsResponse,
    errors: [BadRequest],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveAppError = NotFound | WhopOpError;
/** Retrieve App Retrieves an app by ID, claimed route, or proxy domain id. Credential fields (api_key, default_api_key, secrets) render `null` unless the caller has the corresponding developer permission on the owning account. */
export const retrieveApp: API.OperationMethod<
  RetrieveAppRequest,
  App,
  RetrieveAppError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveAppRequest,
  output: App,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateAppError = BadRequest | Forbidden | NotFound | WhopOpError;
/** Update App Updates the settings, metadata, or status of an app. Fields that are omitted keep their current value. */
export const updateApp: API.OperationMethod<
  UpdateAppRequest,
  App,
  UpdateAppError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateAppRequest,
  output: App,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateAppPermissionsError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** Update App Permissions Replaces the set of permissions the app requests from users when they install it. Requires a user session: the `developer:update_app_authorization` scope cannot be delegated to API keys. Sensitive permissions require step-up verification. */
export const updateAppPermissions: API.OperationMethod<
  UpdateAppPermissionsRequest,
  App,
  UpdateAppPermissionsError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateAppPermissionsRequest,
  output: App,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdatePermissionsAppError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update requested permissions [Legacy API — https://docs.whop.com/api-reference] Updates the permission requirements for an app Required permissions: - `developer:update_app_authorization` */
export const updatePermissionsApp: API.OperationMethod<
  UpdatePermissionsAppRequest,
  UpdatePermissionsAppResponse,
  UpdatePermissionsAppError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdatePermissionsAppRequest,
  output: UpdatePermissionsAppResponse,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
