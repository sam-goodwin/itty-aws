// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface CreateShipmentRequest {
  /** The unique identifier of the account, prefixed `biz_`. */
  account_id?: string;
  /** The payment to attach the shipment to, prefixed `pay_`. */
  payment_id: string;
  /** The carrier-assigned tracking number. */
  tracking_number: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateShipmentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    payment_id: S.String,
    tracking_number: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/shipments", code: 200 })),
).annotate({
  identifier: "CreateShipmentRequest",
}) as any as S.Schema<CreateShipmentRequest>;

/** Delivery status this carrier scan maps to. */
export type ShipmentCheckpointStatus =
  | "unknown"
  | "pre_transit"
  | "in_transit"
  | "out_for_delivery"
  | "delivered"
  | "available_for_pickup"
  | "return_to_sender"
  | "failure"
  | "cancelled"
  | "error";
export const ShipmentCheckpointStatus = /*@__PURE__*/ S.String;

export interface ShipmentCheckpoint {
  /** Where the carrier recorded the scan, such as `PHILADELPHIA, PA`. Null when the carrier sent none. */
  location: string | null;
  /** Carrier's description of the scan, such as `Departed USPS Regional Facility`. Null when the carrier sent none. */
  message: string | null;
  /** Delivery status this carrier scan maps to. */
  status: ShipmentCheckpointStatus;
  /** When the carrier recorded the scan, as an ISO 8601 timestamp. Null when the carrier sent no scan time. */
  timestamp: string | null;
}
export const ShipmentCheckpoint = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    location: S.NullOr(S.String),
    message: S.NullOr(S.String),
    status: ShipmentCheckpointStatus,
    timestamp: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ShipmentCheckpoint",
}) as any as S.Schema<ShipmentCheckpoint>;

export type ShipmentCheckpointsList = Array<ShipmentCheckpoint>;
export const ShipmentCheckpointsList = /*@__PURE__*/ S.Array(
  ShipmentCheckpoint,
) as any as S.Schema<ShipmentCheckpointsList>;

/** The current delivery status of this shipment. */
export type ShipmentStatus =
  | "unknown"
  | "pre_transit"
  | "in_transit"
  | "out_for_delivery"
  | "delivered"
  | "available_for_pickup"
  | "return_to_sender"
  | "failure"
  | "cancelled"
  | "error";
export const ShipmentStatus = /*@__PURE__*/ S.String;

export interface Shipment {
  /** The account that owns this shipment, prefixed `biz_`. */
  account_id: string;
  /** The shipping carrier detected for this shipment. Null until a tracking update identifies it. */
  carrier: string | null;
  checkpoints: ShipmentCheckpointsList;
  /** The datetime the shipment was created (ISO 8601). */
  created_at: string;
  /** Shipment ID, prefixed `ship_`. */
  id: string;
  /** The payment this shipment fulfills, prefixed `pay_`. */
  payment_id: string;
  /** The current delivery status of this shipment. */
  status: ShipmentStatus;
  /** The carrier-assigned tracking number used to look up shipment progress. */
  tracking_number: string;
  /** A customer-facing URL to track this shipment's progress. */
  tracking_url: string;
  /** The datetime the shipment was last updated (ISO 8601). */
  updated_at: string;
}
export const Shipment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String,
    carrier: S.NullOr(S.String),
    checkpoints: ShipmentCheckpointsList,
    created_at: S.String,
    id: S.String,
    payment_id: S.String,
    status: ShipmentStatus,
    tracking_number: S.String,
    tracking_url: S.String,
    updated_at: S.String,
  }),
).annotate({ identifier: "Shipment" }) as any as S.Schema<Shipment>;

export type ListShipmentsRequestStatus =
  | "unknown"
  | "pre_transit"
  | "in_transit"
  | "out_for_delivery"
  | "delivered"
  | "available_for_pickup"
  | "return_to_sender"
  | "failure"
  | "cancelled"
  | "error";
export const ListShipmentsRequestStatus = /*@__PURE__*/ S.String;

export type ListShipmentsRequestPaymentIdList = Array<string>;
export const ListShipmentsRequestPaymentIdList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListShipmentsRequestPaymentIdList>;

export type ListShipmentsRequestOrder = "created_at";
export const ListShipmentsRequestOrder = /*@__PURE__*/ S.String;

export type ListShipmentsRequestDirection = "asc" | "desc";
export const ListShipmentsRequestDirection = /*@__PURE__*/ S.String;

export interface ListShipmentsRequest {
  /** The account to list shipments for. Defaults to the acting account. */
  account_id?: string;
  /** Filter to shipments with this delivery status. */
  status?: ListShipmentsRequestStatus | (string & {});
  /** Only shipments fulfilling these payments, each prefixed `pay_`. Repeat the parameter to pass several, up to 100 per request — one paginated list covers all of them. */
  payment_id?: ListShipmentsRequestPaymentIdList;
  /** Return shipments created before this ISO 8601 timestamp. */
  created_before?: string;
  /** Return shipments created after this ISO 8601 timestamp. */
  created_after?: string;
  /** The field to sort by. */
  order?: ListShipmentsRequestOrder | (string & {});
  /** The sort direction. */
  direction?: ListShipmentsRequestDirection | (string & {});
  /** The number of shipments to return. */
  first?: number;
  /** A cursor; returns shipments after this position. */
  after?: string;
  /** The number of shipments to return from the end of the range. */
  last?: number;
  /** A cursor; returns shipments before this position. */
  before?: string;
}
export const ListShipmentsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    status: S.optional(ListShipmentsRequestStatus.pipe(T.Query())),
    payment_id: S.optional(ListShipmentsRequestPaymentIdList.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListShipmentsRequestOrder.pipe(T.Query())),
    direction: S.optional(ListShipmentsRequestDirection.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/shipments", code: 200 })),
).annotate({
  identifier: "ListShipmentsRequest",
}) as any as S.Schema<ListShipmentsRequest>;

export type ListShipmentsResponseDataList = Array<Shipment>;
export const ListShipmentsResponseDataList = /*@__PURE__*/ S.Array(
  Shipment,
) as any as S.Schema<ListShipmentsResponseDataList>;

export interface ListShipmentsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListShipmentsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListShipmentsResponsePageInfo",
}) as any as S.Schema<ListShipmentsResponsePageInfo>;

export interface ListShipmentsResponse {
  data: ListShipmentsResponseDataList;
  page_info: ListShipmentsResponsePageInfo;
}
export const ListShipmentsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListShipmentsResponseDataList,
    page_info: ListShipmentsResponsePageInfo,
  }),
).annotate({
  identifier: "ListShipmentsResponse",
}) as any as S.Schema<ListShipmentsResponse>;

export interface RetrieveShipmentRequest {
  /** The shipment id (`ship_`), or the payment id (`pay_`) it fulfills. */
  id: string;
}
export const RetrieveShipmentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/shipments/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveShipmentRequest",
}) as any as S.Schema<RetrieveShipmentRequest>;

export interface UpdateShipmentRequest {
  /** The shipment id (`ship_`), or the payment id (`pay_`) it fulfills. */
  id: string;
  /** The new carrier-assigned tracking number. */
  tracking_number: string;
}
export const UpdateShipmentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    tracking_number: S.String,
  }).pipe(T.Http({ method: "PATCH", uri: "/shipments/{id}", code: 200 })),
).annotate({
  identifier: "UpdateShipmentRequest",
}) as any as S.Schema<UpdateShipmentRequest>;

export type CreateShipmentError =
  | BadRequest
  | Forbidden
  | Conflict
  | WhopOpError;
/** Create Shipment Attaches a carrier tracking number to a payment and begins tracking it. */
export const createShipment: API.OperationMethod<
  CreateShipmentRequest,
  Shipment,
  CreateShipmentError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateShipmentRequest,
  output: Shipment,
  errors: [BadRequest, Forbidden, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListShipmentsError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** List Shipments Returns a paginated list of shipments for an account. */
export const listShipments: API.PaginatedOperationMethod<
  ListShipmentsRequest,
  ListShipmentsResponse,
  ListShipmentsError,
  WhopOpContext,
  Shipment
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListShipmentsRequest,
    output: ListShipmentsResponse,
    errors: [BadRequest, Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveShipmentError = Forbidden | NotFound | WhopOpError;
/** Retrieve Shipment Retrieves a shipment by its id, or by the payment id it fulfills. */
export const retrieveShipment: API.OperationMethod<
  RetrieveShipmentRequest,
  Shipment,
  RetrieveShipmentError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveShipmentRequest,
  output: Shipment,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateShipmentError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** Update Shipment Updates a shipment's tracking number and re-tracks it with the carrier. */
export const updateShipment: API.OperationMethod<
  UpdateShipmentRequest,
  Shipment,
  UpdateShipmentError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateShipmentRequest,
  output: Shipment,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
