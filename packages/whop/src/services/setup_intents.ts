// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** The available currencies on the platform */
export type Currencies =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const Currencies = /*@__PURE__*/ S.String;

/** Custom metadata to attach to the setup intent. */
export type CreateSetupIntentRequestBodyCase0MetadataMap = {
  [key: string]: unknown | undefined;
};
export const CreateSetupIntentRequestBodyCase0MetadataMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<CreateSetupIntentRequestBodyCase0MetadataMap>;

/** Autogenerated input type of CreateSetupIntent */
export interface CreateSetupIntentRequestBodyCase0 {
  /** The ID of the company to save the payment method for. */
  company_id: string;
  /** A confirmation token ID (ctok_) describing a payment method the buyer just supplied. Provide this or payment_method_id, not both. */
  confirmation_token: string;
  /** The currency the saved payment method will be used with. Controls which currency-specific payment methods are available. Defaults to usd. */
  currency?: Currencies | (string & {}) | null;
  /** Overrides the buyer email carried on the confirmation token, resolving or creating the Whop user the method belongs to. Ignored when the caller IS the buyer or the confirmation token was created by a signed-in buyer, and unless confirmation_token is provided. */
  email?: string | null;
  /** Custom metadata to attach to the setup intent. */
  metadata?: CreateSetupIntentRequestBodyCase0MetadataMap | null;
  /** Where the buyer continues after completing an off-site step. Must be an absolute https URL without credentials (http is allowed for localhost), at most 2,048 characters. */
  return_url?: string | null;
}
export const CreateSetupIntentRequestBodyCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    company_id: S.String,
    confirmation_token: S.String,
    currency: S.optional(S.NullOr(Currencies)),
    email: S.optional(S.NullOr(S.String)),
    metadata: S.optional(
      S.NullOr(CreateSetupIntentRequestBodyCase0MetadataMap),
    ),
    return_url: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "CreateSetupIntentRequestBodyCase0",
}) as any as S.Schema<CreateSetupIntentRequestBodyCase0>;

/** Custom metadata to attach to the setup intent. */
export type CreateSetupIntentRequestBodyCase1MetadataMap = {
  [key: string]: unknown | undefined;
};
export const CreateSetupIntentRequestBodyCase1MetadataMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<CreateSetupIntentRequestBodyCase1MetadataMap>;

/** Autogenerated input type of CreateSetupIntent */
export interface CreateSetupIntentRequestBodyCase1 {
  /** The ID of the company to save the payment method for. */
  company_id: string;
  /** The currency the saved payment method will be used with. Controls which currency-specific payment methods are available. Defaults to usd. */
  currency?: Currencies | (string & {}) | null;
  /** Overrides the buyer email carried on the confirmation token, resolving or creating the Whop user the method belongs to. Ignored when the caller IS the buyer or the confirmation token was created by a signed-in buyer, and unless confirmation_token is provided. */
  email?: string | null;
  /** Custom metadata to attach to the setup intent. */
  metadata?: CreateSetupIntentRequestBodyCase1MetadataMap | null;
  /** An existing payment method (payt_) to re-verify and save. Provide this or confirmation_token, not both. */
  payment_method_id: string;
  /** Where the buyer continues after completing an off-site step. Must be an absolute https URL without credentials (http is allowed for localhost), at most 2,048 characters. */
  return_url?: string | null;
}
export const CreateSetupIntentRequestBodyCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    company_id: S.String,
    currency: S.optional(S.NullOr(Currencies)),
    email: S.optional(S.NullOr(S.String)),
    metadata: S.optional(
      S.NullOr(CreateSetupIntentRequestBodyCase1MetadataMap),
    ),
    payment_method_id: S.String,
    return_url: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "CreateSetupIntentRequestBodyCase1",
}) as any as S.Schema<CreateSetupIntentRequestBodyCase1>;

/** Parameters for CreateSetupIntent */
export type CreateSetupIntentRequestBody =
  | CreateSetupIntentRequestBodyCase0
  | CreateSetupIntentRequestBodyCase1;
export const CreateSetupIntentRequestBody =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateSetupIntentRequestBody>;

export interface CreateSetupIntentRequest {
  body: CreateSetupIntentRequestBody;
}
export const CreateSetupIntentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: CreateSetupIntentRequestBody.pipe(T.HttpBody()),
  }).pipe(T.Http({ method: "POST", uri: "/setup_intents", code: 200 })),
).annotate({
  identifier: "CreateSetupIntentRequest",
}) as any as S.Schema<CreateSetupIntentRequest>;

/** The checkout session configuration associated with this setup intent. Null if no checkout session was used. */
export interface CreateSetupIntentResponseCheckoutConfiguration {
  /** The unique identifier for the checkout session. */
  id: string;
}
export const CreateSetupIntentResponseCheckoutConfiguration =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      id: S.String,
    }),
  ).annotate({
    identifier: "CreateSetupIntentResponseCheckoutConfiguration",
  }) as any as S.Schema<CreateSetupIntentResponseCheckoutConfiguration>;

/** The company that initiated this setup intent. Null if the company has been deleted. */
export interface CreateSetupIntentResponseCompany {
  /** The unique identifier for the company. */
  id: string;
}
export const CreateSetupIntentResponseCompany = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "CreateSetupIntentResponseCompany",
}) as any as S.Schema<CreateSetupIntentResponseCompany>;

/** The user for this member, if any. */
export interface CreateSetupIntentResponseMemberUser {
  /** The digital mailing address of the user. */
  email: string | null;
  /** The unique identifier for the company member user. */
  id: string;
  /** The user's full name. */
  name: string | null;
  /** The whop username. */
  username: string;
}
export const CreateSetupIntentResponseMemberUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email: S.NullOr(S.String),
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({
  identifier: "CreateSetupIntentResponseMemberUser",
}) as any as S.Schema<CreateSetupIntentResponseMemberUser>;

/** The company member associated with this setup intent. Null if the user is not a member. */
export interface CreateSetupIntentResponseMember {
  /** The unique identifier for the company member. */
  id: string;
  /** The user for this member, if any. */
  user: CreateSetupIntentResponseMemberUser | null;
}
export const CreateSetupIntentResponseMember = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    user: S.NullOr(CreateSetupIntentResponseMemberUser),
  }),
).annotate({
  identifier: "CreateSetupIntentResponseMember",
}) as any as S.Schema<CreateSetupIntentResponseMember>;

/** Custom key-value pairs attached to this setup intent. Null if no metadata was provided. */
export type CreateSetupIntentResponseMetadataMap = {
  [key: string]: unknown | undefined;
};
export const CreateSetupIntentResponseMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateSetupIntentResponseMetadataMap>;

/** Possible card brands that a payment token can have */
export type CardBrands =
  | "mastercard"
  | "visa"
  | "amex"
  | "discover"
  | "unionpay"
  | "jcb"
  | "diners"
  | "link"
  | "troy"
  | "visadankort"
  | "visabancontact"
  | "china_union_pay"
  | "rupay"
  | "jcbrupay"
  | "elo"
  | "maestro"
  | "tarjeta_naranja"
  | "cirrus"
  | "nspk_mir"
  | "verve"
  | "ebt"
  | "private_label"
  | "local_brand"
  | "uatp"
  | "wexcard"
  | "uzcard"
  | "meeza"
  | "hrg_store_card"
  | "girocard"
  | "fuel_card"
  | "dankort"
  | "carnet"
  | "atm_card"
  | "china_union_payuzcard"
  | "codensa"
  | "cabal"
  | "hipercard"
  | "jcblankapay"
  | "cmi"
  | "aura"
  | "accel"
  | "culiance"
  | "nyce"
  | "pulse"
  | "star"
  | "unknown";
export const CardBrands = /*@__PURE__*/ S.String;

/** The card data associated with the payment method, if its a debit or credit card. */
export interface CreateSetupIntentResponsePaymentMethodCard {
  /** The card network (e.g., visa, mastercard, amex). Null if the brand could not be determined. */
  brand: CardBrands | null;
  /** The two-digit expiration month of the card (1-12). Null if not available. */
  exp_month: number | null;
  /** The two-digit expiration year of the card (e.g., 27 for 2027). Null if not available. */
  exp_year: number | null;
  /** The last four digits of the card number. Null if not available. */
  last4: string | null;
}
export const CreateSetupIntentResponsePaymentMethodCard =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      brand: S.NullOr(CardBrands),
      exp_month: S.NullOr(S.Number),
      exp_year: S.NullOr(S.Number),
      last4: S.NullOr(S.String),
    }),
  ).annotate({
    identifier: "CreateSetupIntentResponsePaymentMethodCard",
  }) as any as S.Schema<CreateSetupIntentResponsePaymentMethodCard>;

/** The mailing address associated with the payment method's user */
export interface CreateSetupIntentResponsePaymentMethodMailingAddress {
  /** The city of the address. */
  city: string | null;
  /** The country of the address. */
  country: string | null;
  /** The line 1 of the address. */
  line1: string | null;
  /** The line 2 of the address. */
  line2: string | null;
  /** The name of the customer. */
  name: string | null;
  /** The postal code of the address. */
  postal_code: string | null;
  /** The state of the address. */
  state: string | null;
}
export const CreateSetupIntentResponsePaymentMethodMailingAddress =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      city: S.NullOr(S.String),
      country: S.NullOr(S.String),
      line1: S.NullOr(S.String),
      line2: S.NullOr(S.String),
      name: S.NullOr(S.String),
      postal_code: S.NullOr(S.String),
      state: S.NullOr(S.String),
    }),
  ).annotate({
    identifier: "CreateSetupIntentResponsePaymentMethodMailingAddress",
  }) as any as S.Schema<CreateSetupIntentResponsePaymentMethodMailingAddress>;

/** The different types of payment methods that can be used. */
export type PaymentMethodTypes =
  | "acss_debit"
  | "addi"
  | "affirm"
  | "afterpay_clearpay"
  | "alipay"
  | "alma"
  | "amazon_pay"
  | "apple"
  | "apple_pay"
  | "au_bank_transfer"
  | "au_becs_debit"
  | "bacs_debit"
  | "bancolombia"
  | "bancontact"
  | "bank_wire"
  | "billie"
  | "bizum"
  | "blik"
  | "boleto"
  | "bre_b"
  | "ca_bank_transfer"
  | "capchase_pay"
  | "card"
  | "card_installments_three"
  | "card_installments_six"
  | "card_installments_twelve"
  | "cashapp"
  | "claritypay"
  | "coinbase"
  | "crypto"
  | "custom"
  | "customer_balance"
  | "demo_pay"
  | "efecty"
  | "eps"
  | "eu_bank_transfer"
  | "fpx"
  | "gb_bank_transfer"
  | "giropay"
  | "google_pay"
  | "gopay"
  | "grabpay"
  | "id_bank_transfer"
  | "ideal"
  | "interac"
  | "kakao_pay"
  | "klarna"
  | "klarna_pay_now"
  | "konbini"
  | "kr_card"
  | "kr_market"
  | "kriya"
  | "kueski"
  | "link"
  | "mb_way"
  | "m_pesa"
  | "mercado_pago"
  | "mobilepay"
  | "modo"
  | "mondu"
  | "multibanco"
  | "naver_pay"
  | "nequi"
  | "netbanking"
  | "ng_bank"
  | "ng_bank_transfer"
  | "ng_card"
  | "ng_market"
  | "ng_ussd"
  | "ng_wallet"
  | "nz_bank_account"
  | "oxxo"
  | "p24"
  | "pago_efectivo"
  | "pse"
  | "pay_by_bank"
  | "payco"
  | "paynow"
  | "paypal"
  | "paypay"
  | "payto"
  | "pix"
  | "platform_balance"
  | "promptpay"
  | "qris"
  | "rapipago"
  | "rechnung"
  | "revolut_pay"
  | "samsung_pay"
  | "satispay"
  | "scalapay"
  | "sencillito"
  | "sepa_debit"
  | "sequra"
  | "servipag"
  | "sezzle"
  | "shop_pay"
  | "shopeepay"
  | "sofort"
  | "south_korea_market"
  | "spei"
  | "splitit"
  | "sunbit"
  | "swish"
  | "tamara"
  | "twint"
  | "upi"
  | "us_bank_account"
  | "us_bank_transfer"
  | "venmo"
  | "vipps"
  | "webpay"
  | "wechat_pay"
  | "yape"
  | "zip"
  | "coinflow"
  | "unknown";
export const PaymentMethodTypes = /*@__PURE__*/ S.String;

/** The saved payment method created by this setup intent. Null if the setup has not completed successfully. */
export interface CreateSetupIntentResponsePaymentMethod {
  /** The card data associated with the payment method, if its a debit or credit card. */
  card: CreateSetupIntentResponsePaymentMethodCard | null;
  /** The datetime the payment token was created. */
  created_at: string;
  /** The unique identifier for the payment token. */
  id: string;
  /** The mailing address associated with the payment method's user */
  mailing_address: CreateSetupIntentResponsePaymentMethodMailingAddress | null;
  /** The payment method type of the payment method */
  payment_method_type: PaymentMethodTypes;
}
export const CreateSetupIntentResponsePaymentMethod = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      card: S.NullOr(CreateSetupIntentResponsePaymentMethodCard),
      created_at: S.String,
      id: S.String,
      mailing_address: S.NullOr(
        CreateSetupIntentResponsePaymentMethodMailingAddress,
      ),
      payment_method_type: PaymentMethodTypes,
    }),
).annotate({
  identifier: "CreateSetupIntentResponsePaymentMethod",
}) as any as S.Schema<CreateSetupIntentResponsePaymentMethod>;

/** The status of the setup intent. */
export type SetupIntentStatuses =
  | "processing"
  | "succeeded"
  | "canceled"
  | "requires_action";
export const SetupIntentStatuses = /*@__PURE__*/ S.String;

export interface CreateSetupIntentResponse {
  /** The checkout session configuration associated with this setup intent. Null if no checkout session was used. */
  checkout_configuration: CreateSetupIntentResponseCheckoutConfiguration | null;
  /** The credential the buyer's surface presents to poll this setup intent and set its return URL. Returned when the setup is created and still has something to finish — hold on to it, because a later read will not repeat it. Null when the setup settled on the spot and there is nothing left to authorize. It unlocks this setup intent and nothing else; treat it like a password for that one attempt. */
  client_secret: string | Redacted.Redacted<string> | null;
  /** The company that initiated this setup intent. Null if the company has been deleted. */
  company: CreateSetupIntentResponseCompany | null;
  /** The datetime the setup intent was created. */
  created_at: string;
  /** A human-readable error message explaining why the setup intent failed. Null if no error occurred. */
  error_message: string | null;
  /** The unique identifier for the setup intent. */
  id: string;
  /** The company member associated with this setup intent. Null if the user is not a member. */
  member: CreateSetupIntentResponseMember | null;
  /** Custom key-value pairs attached to this setup intent. Null if no metadata was provided. */
  metadata: CreateSetupIntentResponseMetadataMap | null;
  /** The saved payment method created by this setup intent. Null if the setup has not completed successfully. */
  payment_method: CreateSetupIntentResponsePaymentMethod | null;
  /** The current status of the setup intent. */
  status: SetupIntentStatuses;
  /** Whether 3D Secure authentication was completed when this payment method was set up. */
  three_ds_verified: boolean;
}
export const CreateSetupIntentResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    checkout_configuration: S.NullOr(
      CreateSetupIntentResponseCheckoutConfiguration,
    ),
    client_secret: S.NullOr(S.String).pipe(T.SensitiveValue({})),
    company: S.NullOr(CreateSetupIntentResponseCompany),
    created_at: S.String,
    error_message: S.NullOr(S.String),
    id: S.String,
    member: S.NullOr(CreateSetupIntentResponseMember),
    metadata: S.NullOr(CreateSetupIntentResponseMetadataMap),
    payment_method: S.NullOr(CreateSetupIntentResponsePaymentMethod),
    status: SetupIntentStatuses,
    three_ds_verified: S.Boolean,
  }),
).annotate({
  identifier: "CreateSetupIntentResponse",
}) as any as S.Schema<CreateSetupIntentResponse>;

/** The direction of the sort. */
export type Direction = "asc" | "desc";
export const Direction = /*@__PURE__*/ S.String;

export interface ListSetupIntentRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  company_id: string;
  direction?: Direction | (string & {});
  created_before?: string;
  created_after?: string;
}
export const ListSetupIntentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    company_id: S.String.pipe(T.Query()),
    direction: S.optional(Direction.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/setup_intents", code: 200 })),
).annotate({
  identifier: "ListSetupIntentRequest",
}) as any as S.Schema<ListSetupIntentRequest>;

/** The checkout session configuration associated with this setup intent. Null if no checkout session was used. */
export type SetupIntentListItemCheckoutConfiguration =
  CreateSetupIntentResponseCheckoutConfiguration;
export const SetupIntentListItemCheckoutConfiguration =
  CreateSetupIntentResponseCheckoutConfiguration;

/** The company that initiated this setup intent. Null if the company has been deleted. */
export type SetupIntentListItemCompany = CreateSetupIntentResponseCompany;
export const SetupIntentListItemCompany = CreateSetupIntentResponseCompany;

/** The user for this member, if any. */
export type SetupIntentListItemMemberUser = CreateSetupIntentResponseMemberUser;
export const SetupIntentListItemMemberUser =
  CreateSetupIntentResponseMemberUser;

/** The company member associated with this setup intent. Null if the user is not a member. */
export type SetupIntentListItemMember = CreateSetupIntentResponseMember;
export const SetupIntentListItemMember = CreateSetupIntentResponseMember;

/** Custom key-value pairs attached to this setup intent. Null if no metadata was provided. */
export type SetupIntentListItemMetadataMap = {
  [key: string]: unknown | undefined;
};
export const SetupIntentListItemMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<SetupIntentListItemMetadataMap>;

/** The card data associated with the payment method, if its a debit or credit card. */
export type SetupIntentListItemPaymentMethodCard =
  CreateSetupIntentResponsePaymentMethodCard;
export const SetupIntentListItemPaymentMethodCard =
  CreateSetupIntentResponsePaymentMethodCard;

/** The mailing address associated with the payment method's user */
export type SetupIntentListItemPaymentMethodMailingAddress =
  CreateSetupIntentResponsePaymentMethodMailingAddress;
export const SetupIntentListItemPaymentMethodMailingAddress =
  CreateSetupIntentResponsePaymentMethodMailingAddress;

/** The saved payment method created by this setup intent. Null if the setup has not completed successfully. */
export type SetupIntentListItemPaymentMethod =
  CreateSetupIntentResponsePaymentMethod;
export const SetupIntentListItemPaymentMethod =
  CreateSetupIntentResponsePaymentMethod;

/** A setup intent allows a user to save a payment method for future use without making an immediate purchase. */
export interface SetupIntentListItem {
  /** The checkout session configuration associated with this setup intent. Null if no checkout session was used. */
  checkout_configuration: CreateSetupIntentResponseCheckoutConfiguration | null;
  /** The company that initiated this setup intent. Null if the company has been deleted. */
  company: CreateSetupIntentResponseCompany | null;
  /** The datetime the setup intent was created. */
  created_at: string;
  /** A human-readable error message explaining why the setup intent failed. Null if no error occurred. */
  error_message: string | null;
  /** The unique identifier for the setup intent. */
  id: string;
  /** The company member associated with this setup intent. Null if the user is not a member. */
  member: CreateSetupIntentResponseMember | null;
  /** Custom key-value pairs attached to this setup intent. Null if no metadata was provided. */
  metadata: SetupIntentListItemMetadataMap | null;
  /** The saved payment method created by this setup intent. Null if the setup has not completed successfully. */
  payment_method: CreateSetupIntentResponsePaymentMethod | null;
  /** The current status of the setup intent. */
  status: SetupIntentStatuses;
}
export const SetupIntentListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    checkout_configuration: S.NullOr(
      CreateSetupIntentResponseCheckoutConfiguration,
    ),
    company: S.NullOr(CreateSetupIntentResponseCompany),
    created_at: S.String,
    error_message: S.NullOr(S.String),
    id: S.String,
    member: S.NullOr(CreateSetupIntentResponseMember),
    metadata: S.NullOr(SetupIntentListItemMetadataMap),
    payment_method: S.NullOr(CreateSetupIntentResponsePaymentMethod),
    status: SetupIntentStatuses,
  }),
).annotate({
  identifier: "SetupIntentListItem",
}) as any as S.Schema<SetupIntentListItem>;

/** A list of nodes. */
export type ListSetupIntentResponseDataList = Array<SetupIntentListItem>;
export const ListSetupIntentResponseDataList = /*@__PURE__*/ S.Array(
  SetupIntentListItem,
) as any as S.Schema<ListSetupIntentResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListSetupIntentResponse {
  /** A list of nodes. */
  data: ListSetupIntentResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListSetupIntentResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListSetupIntentResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListSetupIntentResponse",
}) as any as S.Schema<ListSetupIntentResponse>;

export interface RetrieveSetupIntentRequest {
  /** The unique identifier of the setup intent. */
  id: string;
}
export const RetrieveSetupIntentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/setup_intents/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveSetupIntentRequest",
}) as any as S.Schema<RetrieveSetupIntentRequest>;

/** The checkout session configuration associated with this setup intent. Null if no checkout session was used. */
export type SetupIntentCheckoutConfiguration =
  CreateSetupIntentResponseCheckoutConfiguration;
export const SetupIntentCheckoutConfiguration =
  CreateSetupIntentResponseCheckoutConfiguration;

/** The company that initiated this setup intent. Null if the company has been deleted. */
export type SetupIntentCompany = CreateSetupIntentResponseCompany;
export const SetupIntentCompany = CreateSetupIntentResponseCompany;

/** The user for this member, if any. */
export type SetupIntentMemberUser = CreateSetupIntentResponseMemberUser;
export const SetupIntentMemberUser = CreateSetupIntentResponseMemberUser;

/** The company member associated with this setup intent. Null if the user is not a member. */
export type SetupIntentMember = CreateSetupIntentResponseMember;
export const SetupIntentMember = CreateSetupIntentResponseMember;

/** Custom key-value pairs attached to this setup intent. Null if no metadata was provided. */
export type SetupIntentMetadataMap = { [key: string]: unknown | undefined };
export const SetupIntentMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<SetupIntentMetadataMap>;

/** The card data associated with the payment method, if its a debit or credit card. */
export type SetupIntentPaymentMethodCard =
  CreateSetupIntentResponsePaymentMethodCard;
export const SetupIntentPaymentMethodCard =
  CreateSetupIntentResponsePaymentMethodCard;

/** The mailing address associated with the payment method's user */
export type SetupIntentPaymentMethodMailingAddress =
  CreateSetupIntentResponsePaymentMethodMailingAddress;
export const SetupIntentPaymentMethodMailingAddress =
  CreateSetupIntentResponsePaymentMethodMailingAddress;

/** The saved payment method created by this setup intent. Null if the setup has not completed successfully. */
export type SetupIntentPaymentMethod = CreateSetupIntentResponsePaymentMethod;
export const SetupIntentPaymentMethod = CreateSetupIntentResponsePaymentMethod;

/** A setup intent allows a user to save a payment method for future use without making an immediate purchase. */
export interface SetupIntent {
  /** The checkout session configuration associated with this setup intent. Null if no checkout session was used. */
  checkout_configuration: CreateSetupIntentResponseCheckoutConfiguration | null;
  /** The company that initiated this setup intent. Null if the company has been deleted. */
  company: CreateSetupIntentResponseCompany | null;
  /** The datetime the setup intent was created. */
  created_at: string;
  /** A human-readable error message explaining why the setup intent failed. Null if no error occurred. */
  error_message: string | null;
  /** The unique identifier for the setup intent. */
  id: string;
  /** The company member associated with this setup intent. Null if the user is not a member. */
  member: CreateSetupIntentResponseMember | null;
  /** Custom key-value pairs attached to this setup intent. Null if no metadata was provided. */
  metadata: SetupIntentMetadataMap | null;
  /** The saved payment method created by this setup intent. Null if the setup has not completed successfully. */
  payment_method: CreateSetupIntentResponsePaymentMethod | null;
  /** The current status of the setup intent. */
  status: SetupIntentStatuses;
  /** Whether 3D Secure authentication was completed when this payment method was set up. */
  three_ds_verified: boolean;
}
export const SetupIntent = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    checkout_configuration: S.NullOr(
      CreateSetupIntentResponseCheckoutConfiguration,
    ),
    company: S.NullOr(CreateSetupIntentResponseCompany),
    created_at: S.String,
    error_message: S.NullOr(S.String),
    id: S.String,
    member: S.NullOr(CreateSetupIntentResponseMember),
    metadata: S.NullOr(SetupIntentMetadataMap),
    payment_method: S.NullOr(CreateSetupIntentResponsePaymentMethod),
    status: SetupIntentStatuses,
    three_ds_verified: S.Boolean,
  }),
).annotate({ identifier: "SetupIntent" }) as any as S.Schema<SetupIntent>;

export interface RetrieveStatusSetupIntentRequest {
  /** The unique identifier of the setup intent. */
  setup_intent_id: string;
}
export const RetrieveStatusSetupIntentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    setup_intent_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/setup_intents/{setup_intent_id}/status",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RetrieveStatusSetupIntentRequest",
}) as any as S.Schema<RetrieveStatusSetupIntentRequest>;

export interface SetupLastSetupError {
  /** A machine-readable classification of the failure, e.g. `enrollment_declined`. Absent when the buyer simply abandoned the setup. */
  code: string | null;
  /** A human-readable explanation of the failure. */
  message: string | null;
}
export const SetupLastSetupError = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    code: S.NullOr(S.String),
    message: S.NullOr(S.String),
  }),
).annotate({
  identifier: "SetupLastSetupError",
}) as any as S.Schema<SetupLastSetupError>;

export interface PaymentNextActionRedirectData {
  /** The widest the provider's page lays out usefully, in CSS pixels — cap a frame or dialog presenting it at this width. `null` when the page fills whatever width it is given. */
  frame_max_width: number | null;
  /** The provider's page for this payment, as an absolute URL — take the buyer there. */
  url: string;
}
export const PaymentNextActionRedirectData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    frame_max_width: S.NullOr(S.Number),
    url: S.String,
  }),
).annotate({
  identifier: "PaymentNextActionRedirectData",
}) as any as S.Schema<PaymentNextActionRedirectData>;

/** Where this step can be presented: `inline` inside your own page, `full_page` as a top-level navigation. Pick whichever listed mode suits your surface. */
export type PaymentNextActionRedirectRenderItem = "inline" | "full_page";
export const PaymentNextActionRedirectRenderItem = /*@__PURE__*/ S.String;

export type PaymentNextActionRedirectRenderList =
  Array<PaymentNextActionRedirectRenderItem>;
export const PaymentNextActionRedirectRenderList = /*@__PURE__*/ S.Array(
  PaymentNextActionRedirectRenderItem,
) as any as S.Schema<PaymentNextActionRedirectRenderList>;

/** Always `redirect`: send the buyer to `data.url`. The provider hands them back to `return_url` when they are done. */
export type PaymentNextActionRedirectType = "redirect";
export const PaymentNextActionRedirectType = /*@__PURE__*/ S.String;

export interface PaymentNextActionRedirect {
  /** Where to send the buyer. */
  data: PaymentNextActionRedirectData;
  render: PaymentNextActionRedirectRenderList;
  /** Always `redirect`: send the buyer to `data.url`. The provider hands them back to `return_url` when they are done. */
  type: PaymentNextActionRedirectType;
}
export const PaymentNextActionRedirect = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: PaymentNextActionRedirectData,
    render: PaymentNextActionRedirectRenderList,
    type: PaymentNextActionRedirectType,
  }),
).annotate({
  identifier: "PaymentNextActionRedirect",
}) as any as S.Schema<PaymentNextActionRedirect>;

/** Always `voucher`: a code the buyer pays in person, at a convenience store or bank counter. */
export type PaymentVoucherInstructionsKind = "voucher";
export const PaymentVoucherInstructionsKind = /*@__PURE__*/ S.String;

export interface Money {
  /** The amount in major units, as an exact decimal string — `"10.00"` is ten dollars. A string so no float rounds it in transit. */
  amount: string;
  /** Three-letter ISO 4217 currency code, lowercase. */
  currency: string;
  /** How many decimal places the amount CARRIES — the precision the charge itself runs at. */
  decimals: number;
  /** How many decimal places to SHOW. Usually equal to `decimals`, and deliberately not always: COP is charged in centavos but written in whole pesos, so it is `2` and `0`. Format the number in your own locale using this. */
  display_decimals: number;
}
export const Money = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.String,
    currency: S.String,
    decimals: S.Number,
    display_decimals: S.Number,
  }),
).annotate({ identifier: "Money" }) as any as S.Schema<Money>;

export interface PaymentVoucher {
  /** Exactly what the buyer must pay, in the charged currency. */
  amount?: Money;
  /** The barcode's contents, when the voucher carries one — render it in the symbology named by `barcode_format`. */
  barcode?: string;
  /** The symbology `barcode` is encoded in, such as `CODE_128`. */
  barcode_format?: string;
  /** Who the payment is made out to. */
  company_name?: string;
  /** A hosted page with the complete, printable instructions. If you would rather not render the details yourself, send the buyer here. */
  document_url?: string;
  /** When the voucher stops being payable, as an ISO 8601 timestamp. */
  expires_at?: string;
  /** URL of that network's logo. */
  provider_logo?: string;
  /** The network the buyer pays at, such as OXXO. */
  provider_name?: string;
  /** The voucher's number — what the buyer reads out or types at the counter to pay. */
  reference?: string;
}
export const PaymentVoucher = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.optional(Money),
    barcode: S.optional(S.String),
    barcode_format: S.optional(S.String),
    company_name: S.optional(S.String),
    document_url: S.optional(S.String),
    expires_at: S.optional(S.String),
    provider_logo: S.optional(S.String),
    provider_name: S.optional(S.String),
    reference: S.optional(S.String),
  }),
).annotate({ identifier: "PaymentVoucher" }) as any as S.Schema<PaymentVoucher>;

export interface PaymentVoucherInstructions {
  /** Always `voucher`: a code the buyer pays in person, at a convenience store or bank counter. */
  kind: PaymentVoucherInstructionsKind;
  /** The voucher to show. */
  voucher: PaymentVoucher;
}
export const PaymentVoucherInstructions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    kind: PaymentVoucherInstructionsKind,
    voucher: PaymentVoucher,
  }),
).annotate({
  identifier: "PaymentVoucherInstructions",
}) as any as S.Schema<PaymentVoucherInstructions>;

/** Always `qr`: a code the buyer scans with their banking app. */
export type PaymentQrInstructionsKind = "qr";
export const PaymentQrInstructionsKind = /*@__PURE__*/ S.String;

export interface PaymentQr {
  /** Exactly what the buyer must pay, in the charged currency. */
  amount?: Money;
  /** A hosted page with the complete, printable instructions. If you would rather not render the details yourself, send the buyer here. */
  document_url?: string;
  /** When the code stops being payable, as an ISO 8601 timestamp. */
  expires_at?: string;
  /** An account key the buyer can pay to directly (Colombia's Bre-B llave), for apps that take a key instead of a scan. */
  key?: string;
  /** The QR code's contents, ready to render as a scannable image — `qr_format` says how it is encoded. */
  qr_code?: string;
  /** How `qr_code` is encoded. */
  qr_format?: string;
}
export const PaymentQr = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.optional(Money),
    document_url: S.optional(S.String),
    expires_at: S.optional(S.String),
    key: S.optional(S.String),
    qr_code: S.optional(S.String),
    qr_format: S.optional(S.String),
  }),
).annotate({ identifier: "PaymentQr" }) as any as S.Schema<PaymentQr>;

export interface PaymentQrInstructions {
  /** Always `qr`: a code the buyer scans with their banking app. */
  kind: PaymentQrInstructionsKind;
  /** The code to show. */
  qr: PaymentQr;
}
export const PaymentQrInstructions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    kind: PaymentQrInstructionsKind,
    qr: PaymentQr,
  }),
).annotate({
  identifier: "PaymentQrInstructions",
}) as any as S.Schema<PaymentQrInstructions>;

export interface PaymentBankTransfer {
  /** The account to send to, in the local scheme's format — `account_number_label` says what to call it. */
  account_number?: string;
  /** What to call `account_number` when showing it, in the local scheme's own terms — `CLABE` in Mexico, for example. */
  account_number_label?: string;
  /** Exactly what the buyer must send, in the charged currency. */
  amount?: Money;
  /** The kind of account receiving the transfer, such as a checking account, in the local system's own vocabulary. */
  bank_account_type?: string;
  /** The receiving bank's address. */
  bank_address?: string;
  /** The receiving branch, where the local system routes by branch. */
  bank_branch?: string;
  /** The receiving bank's code in the local clearing system. */
  bank_code?: string;
  /** The receiving bank's name. */
  bank_name?: string;
  /** The account holder's tax or identity document number, where the local system needs it to send. */
  beneficiary_document?: string;
  /** What kind of document `beneficiary_document` is, in the local system's own vocabulary. */
  beneficiary_document_type?: string;
  /** Who the account belongs to — the name the buyer's bank may ask them to confirm. */
  beneficiary_name?: string;
  /** A hosted page with the complete, printable instructions. If you would rather not render the details yourself, send the buyer here. */
  document_url?: string;
  /** When these details stop being payable, as an ISO 8601 timestamp. */
  expires_at?: string;
  /** The rail's own step-by-step payment text, when it supplies one. */
  instructions?: string;
  /** The reference the buyer must attach to the transfer so it can be matched to this payment. */
  reference?: string;
  /** The receiving bank's routing number for US transfers. */
  routing_number?: string;
  /** A second account number, where the rail publishes the same destination in more than one format. */
  secondary_account_number?: string;
  /** What to call `secondary_account_number` when showing it. */
  secondary_account_number_label?: string;
}
export const PaymentBankTransfer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_number: S.optional(S.String),
    account_number_label: S.optional(S.String),
    amount: S.optional(Money),
    bank_account_type: S.optional(S.String),
    bank_address: S.optional(S.String),
    bank_branch: S.optional(S.String),
    bank_code: S.optional(S.String),
    bank_name: S.optional(S.String),
    beneficiary_document: S.optional(S.String),
    beneficiary_document_type: S.optional(S.String),
    beneficiary_name: S.optional(S.String),
    document_url: S.optional(S.String),
    expires_at: S.optional(S.String),
    instructions: S.optional(S.String),
    reference: S.optional(S.String),
    routing_number: S.optional(S.String),
    secondary_account_number: S.optional(S.String),
    secondary_account_number_label: S.optional(S.String),
  }),
).annotate({
  identifier: "PaymentBankTransfer",
}) as any as S.Schema<PaymentBankTransfer>;

/** Always `bank_transfer`: account details the buyer sends money to from their own bank. */
export type PaymentBankTransferInstructionsKind = "bank_transfer";
export const PaymentBankTransferInstructionsKind = /*@__PURE__*/ S.String;

export interface PaymentBankTransferInstructions {
  /** The account details to show. */
  bank_transfer: PaymentBankTransfer;
  /** Always `bank_transfer`: account details the buyer sends money to from their own bank. */
  kind: PaymentBankTransferInstructionsKind;
}
export const PaymentBankTransferInstructions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bank_transfer: PaymentBankTransfer,
    kind: PaymentBankTransferInstructionsKind,
  }),
).annotate({
  identifier: "PaymentBankTransferInstructions",
}) as any as S.Schema<PaymentBankTransferInstructions>;

/** What to show the buyer so they can pay. `kind` picks the shape and the details sit under the key named for it, so switching on `kind` gives you exactly that kind's payload. Every detail field is optional — the rails behind these methods publish them unevenly — but a kind that arrives with `document_url` can always fall back to sending the buyer to that hosted copy of the instructions. */
export type PaymentInstructions =
  | PaymentVoucherInstructions
  | PaymentQrInstructions
  | PaymentBankTransferInstructions;
export const PaymentInstructions =
  /*@__PURE__*/ S.Unknown as any as S.Schema<PaymentInstructions>;

/** Where this step can be presented: `inline` inside your own page, `full_page` as a top-level navigation. Pick whichever listed mode suits your surface. */
export type PaymentNextActionDisplayInstructionsRenderItem =
  | "inline"
  | "full_page";
export const PaymentNextActionDisplayInstructionsRenderItem =
  /*@__PURE__*/ S.String;

export type PaymentNextActionDisplayInstructionsRenderList =
  Array<PaymentNextActionDisplayInstructionsRenderItem>;
export const PaymentNextActionDisplayInstructionsRenderList =
  /*@__PURE__*/ S.Array(
    PaymentNextActionDisplayInstructionsRenderItem,
  ) as any as S.Schema<PaymentNextActionDisplayInstructionsRenderList>;

/** Always `display_instructions`: show the buyer `data` — what to pay, where, and by when. The payment completes once they pay and the rail confirms it, so keep polling `status`. */
export type PaymentNextActionDisplayInstructionsType = "display_instructions";
export const PaymentNextActionDisplayInstructionsType = /*@__PURE__*/ S.String;

export interface PaymentNextActionDisplayInstructions {
  /** The instructions. `kind` picks the shape, and the details sit under the key named for it: `voucher`, `qr`, or `bank_transfer`. */
  data: PaymentInstructions;
  render: PaymentNextActionDisplayInstructionsRenderList;
  /** Always `display_instructions`: show the buyer `data` — what to pay, where, and by when. The payment completes once they pay and the rail confirms it, so keep polling `status`. */
  type: PaymentNextActionDisplayInstructionsType;
}
export const PaymentNextActionDisplayInstructions = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      data: PaymentInstructions,
      render: PaymentNextActionDisplayInstructionsRenderList,
      type: PaymentNextActionDisplayInstructionsType,
    }),
).annotate({
  identifier: "PaymentNextActionDisplayInstructions",
}) as any as S.Schema<PaymentNextActionDisplayInstructions>;

export interface PaymentNextActionAwaitConfirmationData {
  /** When the confirmation window closes, as an ISO 8601 timestamp. A payment still unconfirmed by then will not succeed — watch `status` for the failed attempt. */
  expires_at: string;
}
export const PaymentNextActionAwaitConfirmationData = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      expires_at: S.String,
    }),
).annotate({
  identifier: "PaymentNextActionAwaitConfirmationData",
}) as any as S.Schema<PaymentNextActionAwaitConfirmationData>;

/** Where this step can be presented: `inline` inside your own page, `full_page` as a top-level navigation. Pick whichever listed mode suits your surface. */
export type PaymentNextActionAwaitConfirmationRenderItem =
  | "inline"
  | "full_page";
export const PaymentNextActionAwaitConfirmationRenderItem =
  /*@__PURE__*/ S.String;

export type PaymentNextActionAwaitConfirmationRenderList =
  Array<PaymentNextActionAwaitConfirmationRenderItem>;
export const PaymentNextActionAwaitConfirmationRenderList =
  /*@__PURE__*/ S.Array(
    PaymentNextActionAwaitConfirmationRenderItem,
  ) as any as S.Schema<PaymentNextActionAwaitConfirmationRenderList>;

/** Always `await_confirmation`: nothing to show — the buyer has done their part and the rail settles out of band. Poll `status` until it moves. */
export type PaymentNextActionAwaitConfirmationType = "await_confirmation";
export const PaymentNextActionAwaitConfirmationType = /*@__PURE__*/ S.String;

export interface PaymentNextActionAwaitConfirmation {
  /** How long the wait can last. */
  data: PaymentNextActionAwaitConfirmationData;
  render: PaymentNextActionAwaitConfirmationRenderList;
  /** Always `await_confirmation`: nothing to show — the buyer has done their part and the rail settles out of band. Poll `status` until it moves. */
  type: PaymentNextActionAwaitConfirmationType;
}
export const PaymentNextActionAwaitConfirmation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: PaymentNextActionAwaitConfirmationData,
    render: PaymentNextActionAwaitConfirmationRenderList,
    type: PaymentNextActionAwaitConfirmationType,
  }),
).annotate({
  identifier: "PaymentNextActionAwaitConfirmation",
}) as any as S.Schema<PaymentNextActionAwaitConfirmation>;

/** What the buyer must do to finish. `type` picks the shape and each type carries only its own `data`, so switching on `type` gives you exactly that step's payload. */
export type PaymentNextAction =
  | PaymentNextActionRedirect
  | PaymentNextActionDisplayInstructions
  | PaymentNextActionAwaitConfirmation;
export const PaymentNextAction =
  /*@__PURE__*/ S.Unknown as any as S.Schema<PaymentNextAction>;

/** How far the setup has got. **A 200 means we answered, not that the method was saved — always branch on this.** `requires_action` — the buyer has a step outstanding; see `next_action`. `processing` — the buyer has done their part and the processor is deciding. `succeeded` — the payment method is saved, and only this one means saved. `canceled` — abandoned or refused; see `last_setup_error` to tell which. */
export type SetupStatusStatus =
  | "processing"
  | "succeeded"
  | "canceled"
  | "requires_action";
export const SetupStatusStatus = /*@__PURE__*/ S.String;

export interface SetupStatus {
  /** The setup this status describes, prefixed `sint_`. */
  id: string;
  /** Why the setup ended where it did, or `null` when nothing has failed. Present on `canceled` — a buyer who abandoned carries no code, one refused by the provider does. */
  last_setup_error: SetupLastSetupError | null;
  /** What the buyer must do next while `status` is `requires_action`, otherwise `null`. `type` picks the shape and each variant carries only its own `data`, so switching on `type` gives you exactly that step's payload. */
  next_action: PaymentNextAction | null;
  /** Always `setup_status`. */
  object: string;
  /** Where to send the buyer once the setup reaches a resting state, or `null` to leave them where they are. */
  return_url: string | null;
  /** How far the setup has got. **A 200 means we answered, not that the method was saved — always branch on this.** `requires_action` — the buyer has a step outstanding; see `next_action`. `processing` — the buyer has done their part and the processor is deciding. `succeeded` — the payment method is saved, and only this one means saved. `canceled` — abandoned or refused; see `last_setup_error` to tell which. */
  status: SetupStatusStatus;
}
export const SetupStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    last_setup_error: S.NullOr(SetupLastSetupError),
    next_action: S.NullOr(PaymentNextAction),
    object: S.String,
    return_url: S.NullOr(S.String),
    status: SetupStatusStatus,
  }),
).annotate({ identifier: "SetupStatus" }) as any as S.Schema<SetupStatus>;

export interface UpdateReturnUrlSetupIntentRequest {
  /** The unique identifier of the setup intent. */
  setup_intent_id: string;
  /** Where the buyer continues after completing an off-site step. Must be an absolute https URL without credentials (http is allowed for localhost), at most 2,048 characters. */
  return_url: string;
}
export const UpdateReturnUrlSetupIntentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    setup_intent_id: S.String.pipe(T.Label()),
    return_url: S.String,
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/setup_intents/{setup_intent_id}/return_url",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateReturnUrlSetupIntentRequest",
}) as any as S.Schema<UpdateReturnUrlSetupIntentRequest>;

export type CreateSetupIntentError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create setup intent [Legacy API — https://docs.whop.com/api-reference] Save a buyer's payment method for later without charging it. Provide a confirmation token for a method the buyer just supplied, or an existing payment method to re-verify. The buyer may still have a step to complete — 3D Secure, a hosted enrollment, linking a bank account — so poll the setup intent's status endpoint for what to do next. Required permissions: - `payment:charge` - `member:basic:read` - `member:email:read` */
export const createSetupIntent: API.OperationMethod<
  CreateSetupIntentRequest,
  CreateSetupIntentResponse,
  CreateSetupIntentError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSetupIntentRequest,
  output: CreateSetupIntentResponse,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListSetupIntentError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List setup intents [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of setup intents for a company, with optional filtering by creation date. A setup intent securely collects and stores a member's payment method for future use without charging them immediately. Required permissions: - `payment:setup_intent:read` - `member:basic:read` - `member:email:read` */
export const listSetupIntent: API.PaginatedOperationMethod<
  ListSetupIntentRequest,
  ListSetupIntentResponse,
  ListSetupIntentError,
  WhopOpContext,
  SetupIntentListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListSetupIntentRequest,
    output: ListSetupIntentResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveSetupIntentError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve setup intent [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing setup intent. Required permissions: - `payment:setup_intent:read` - `member:basic:read` - `member:email:read` */
export const retrieveSetupIntent: API.OperationMethod<
  RetrieveSetupIntentRequest,
  SetupIntent,
  RetrieveSetupIntentError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveSetupIntentRequest,
  output: SetupIntent,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveStatusSetupIntentError = Forbidden | NotFound | WhopOpError;
/** Retrieve setup status Retrieves how far a setup has got and what the buyer must do next, if anything. Collection runs in the background, so poll this rather than reading the create response. Accepts either a secret key or the setup's own `client_secret`, so the surface collecting the payment method can poll it directly. */
export const retrieveStatusSetupIntent: API.OperationMethod<
  RetrieveStatusSetupIntentRequest,
  SetupStatus,
  RetrieveStatusSetupIntentError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveStatusSetupIntentRequest,
  output: SetupStatus,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateReturnUrlSetupIntentError =
  | BadRequest
  | Forbidden
  | WhopOpError;
/** Update setup return URL Changes where the buyer lands after completing an off-site step, up until they return. Accepts either a secret key or the setup's own `client_secret`, so the surface that knows the final destination can set it. */
export const updateReturnUrlSetupIntent: API.OperationMethod<
  UpdateReturnUrlSetupIntentRequest,
  SetupStatus,
  UpdateReturnUrlSetupIntentError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateReturnUrlSetupIntentRequest,
  output: SetupStatus,
  errors: [BadRequest, Forbidden],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
