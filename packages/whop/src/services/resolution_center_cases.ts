// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface AcceptResolutionCenterCaseRequestAttachmentsItem {
  direct_upload_id?: string;
  id?: string;
}
export const AcceptResolutionCenterCaseRequestAttachmentsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      direct_upload_id: S.optional(S.String),
      id: S.optional(S.String),
    }),
  ).annotate({
    identifier: "AcceptResolutionCenterCaseRequestAttachmentsItem",
  }) as any as S.Schema<AcceptResolutionCenterCaseRequestAttachmentsItem>;

/** Up to 3 evidence files, by existing file `id` or `direct_upload_id`. */
export type AcceptResolutionCenterCaseRequestAttachmentsList =
  Array<AcceptResolutionCenterCaseRequestAttachmentsItem>;
export const AcceptResolutionCenterCaseRequestAttachmentsList =
  /*@__PURE__*/ S.Array(
    AcceptResolutionCenterCaseRequestAttachmentsItem,
  ) as any as S.Schema<AcceptResolutionCenterCaseRequestAttachmentsList>;

export interface AcceptResolutionCenterCaseRequest {
  /** The resolution center case ID (`reso_` tag). */
  id: string;
  /** Up to 3 evidence files, by existing file `id` or `direct_upload_id`. */
  attachments?: AcceptResolutionCenterCaseRequestAttachmentsList;
  /** An optional note to the customer, recorded on the case timeline. */
  message?: string;
  /** Whether to also terminate the customer's membership. */
  terminate_membership?: boolean;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const AcceptResolutionCenterCaseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    attachments: S.optional(AcceptResolutionCenterCaseRequestAttachmentsList),
    message: S.optional(S.String),
    terminate_membership: S.optional(S.Boolean),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/resolution_center_cases/{id}/accept",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AcceptResolutionCenterCaseRequest",
}) as any as S.Schema<AcceptResolutionCenterCaseRequest>;

export interface AccountSummary {
  /** Account ID, prefixed `biz_`. */
  id: string;
  /** Account display name. */
  title: string;
}
export const AccountSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({ identifier: "AccountSummary" }) as any as S.Schema<AccountSummary>;

/** What you can do to this case right now, named for the endpoint that does it. Resolved for the calling credential, so a merchant and a customer reading the same case see their own options. */
export type ResolutionCenterCaseAvailableActionsItem =
  | "accept"
  | "deny"
  | "request_info"
  | "reply"
  | "appeal"
  | "withdraw";
export const ResolutionCenterCaseAvailableActionsItem = /*@__PURE__*/ S.String;

export type ResolutionCenterCaseAvailableActionsList =
  Array<ResolutionCenterCaseAvailableActionsItem>;
export const ResolutionCenterCaseAvailableActionsList = /*@__PURE__*/ S.Array(
  ResolutionCenterCaseAvailableActionsItem,
) as any as S.Schema<ResolutionCenterCaseAvailableActionsList>;

export interface ResolutionBuyer {
  /** The customer's email address. Requires the `member:email:read` scope; `null` without it. */
  email: string | null;
  /** The customer's member row on the account, prefixed `mem_`. */
  member_id: string | null;
  /** The customer's display name. */
  name: string | null;
  /** The customer's user ID, prefixed `user_`. */
  user_id: string | null;
  /** The customer's Whop username. */
  username: string | null;
}
export const ResolutionBuyer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email: S.NullOr(S.String),
    member_id: S.NullOr(S.String),
    name: S.NullOr(S.String),
    user_id: S.NullOr(S.String),
    username: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ResolutionBuyer",
}) as any as S.Schema<ResolutionBuyer>;

/** Who prevailed on the claim. `null` until the case closes. Read `refund` for whether any money actually moved. */
export type ResolutionCenterCaseOutcome =
  | "customer_won"
  | "merchant_won"
  | "withdrawn";
export const ResolutionCenterCaseOutcome = /*@__PURE__*/ S.String;

export interface ResolutionPayment {
  /** Card brand, when the customer paid by card. */
  card_brand: string | null;
  /** Last four digits of the card, when the customer paid by card. */
  card_last4: string | null;
  /** When the payment was made, as an ISO 8601 timestamp. */
  created_at: string;
  /** Payment ID, prefixed `pay_`. */
  id: string;
  /** How the customer paid, such as `card` or `paypal`. */
  payment_method_type: string | null;
}
export const ResolutionPayment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    card_brand: S.NullOr(S.String),
    card_last4: S.NullOr(S.String),
    created_at: S.String,
    id: S.String,
    payment_method_type: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ResolutionPayment",
}) as any as S.Schema<ResolutionPayment>;

/** What the customer says went wrong. Shares the `/disputes` vocabulary, so a case that later becomes a chargeback reports the same complaint. */
export type ResolutionCenterCaseReason =
  | "fraudulent"
  | "product_not_received"
  | "not_as_described"
  | "product_unacceptable"
  | "subscription_canceled";
export const ResolutionCenterCaseReason = /*@__PURE__*/ S.String;

/** Whether money moved and off whose balance: `none`, `merchant`, or `platform` (Whop refunded the customer and the merchant kept the funds). Independent of `outcome` — a case the merchant won can still carry a platform refund. `null` while the case is open, and on older closed cases that predate this being recorded. */
export type ResolutionCenterCaseRefund = "none" | "merchant" | "platform";
export const ResolutionCenterCaseRefund = /*@__PURE__*/ S.String;

/** Who the case is waiting on. `awaiting_merchant` and `awaiting_customer` name the side that owes a response, `under_review` means Whop is deciding, and `closed` means it is settled — read `outcome` for how. */
export type ResolutionCenterCaseStatus =
  | "awaiting_merchant"
  | "awaiting_customer"
  | "under_review"
  | "closed";
export const ResolutionCenterCaseStatus = /*@__PURE__*/ S.String;

export interface ResolutionCenterCase {
  /** The account the case was filed against. */
  account: AccountSummary | null;
  /** The amount in question, in whole units of `currency`. */
  amount: number;
  available_actions: ResolutionCenterCaseAvailableActionsList;
  /** The customer who opened the case. */
  buyer: ResolutionBuyer;
  /** When the case was opened, as an ISO 8601 timestamp. */
  created_at: string;
  /** Three-letter ISO currency code of the amount. */
  currency: string | null;
  /** Whether the customer has appealed a decision on this case. */
  customer_appealed: boolean;
  /** Whether Whop is involved — either reviewing the case, or waiting on the side named by `status` for something it asked for while reviewing. */
  escalated: boolean;
  /** Resolution center case ID, prefixed `reso_`. */
  id: string;
  /** Who prevailed on the claim. `null` until the case closes. Read `refund` for whether any money actually moved. */
  outcome: ResolutionCenterCaseOutcome | null;
  /** The payment the case was opened against. */
  payment: ResolutionPayment;
  /** The plan the payment was made on, prefixed `plan_`. */
  plan_id: string | null;
  /** The product the payment was for, prefixed `prod_`. */
  product_id: string | null;
  /** What the customer says went wrong. Shares the `/disputes` vocabulary, so a case that later becomes a chargeback reports the same complaint. */
  reason: ResolutionCenterCaseReason;
  /** Whether money moved and off whose balance: `none`, `merchant`, or `platform` (Whop refunded the customer and the merchant kept the funds). Independent of `outcome` — a case the merchant won can still carry a platform refund. `null` while the case is open, and on older closed cases that predate this being recorded. */
  refund: ResolutionCenterCaseRefund | null;
  /** When the next response is due, as an ISO 8601 timestamp. */
  response_due_at: string | null;
  /** Who the case is waiting on. `awaiting_merchant` and `awaiting_customer` name the side that owes a response, `under_review` means Whop is deciding, and `closed` means it is settled — read `outcome` for how. */
  status: ResolutionCenterCaseStatus;
  /** When the case was last changed, as an ISO 8601 timestamp. */
  updated_at: string;
}
export const ResolutionCenterCase = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account: S.NullOr(AccountSummary),
    amount: S.Number,
    available_actions: ResolutionCenterCaseAvailableActionsList,
    buyer: ResolutionBuyer,
    created_at: S.String,
    currency: S.NullOr(S.String),
    customer_appealed: S.Boolean,
    escalated: S.Boolean,
    id: S.String,
    outcome: S.NullOr(ResolutionCenterCaseOutcome),
    payment: ResolutionPayment,
    plan_id: S.NullOr(S.String),
    product_id: S.NullOr(S.String),
    reason: ResolutionCenterCaseReason,
    refund: S.NullOr(ResolutionCenterCaseRefund),
    response_due_at: S.NullOr(S.String),
    status: ResolutionCenterCaseStatus,
    updated_at: S.String,
  }),
).annotate({
  identifier: "ResolutionCenterCase",
}) as any as S.Schema<ResolutionCenterCase>;

export type AppealResolutionCenterCaseRequestAttachmentsItem =
  AcceptResolutionCenterCaseRequestAttachmentsItem;
export const AppealResolutionCenterCaseRequestAttachmentsItem =
  AcceptResolutionCenterCaseRequestAttachmentsItem;

/** Up to 3 evidence files, by existing file `id` or `direct_upload_id`. */
export type AppealResolutionCenterCaseRequestAttachmentsList =
  Array<AcceptResolutionCenterCaseRequestAttachmentsItem>;
export const AppealResolutionCenterCaseRequestAttachmentsList =
  /*@__PURE__*/ S.Array(
    AcceptResolutionCenterCaseRequestAttachmentsItem,
  ) as any as S.Schema<AppealResolutionCenterCaseRequestAttachmentsList>;

export interface AppealResolutionCenterCaseRequest {
  /** The resolution center case ID (`reso_` tag). */
  id: string;
  /** Up to 3 evidence files, by existing file `id` or `direct_upload_id`. */
  attachments?: AppealResolutionCenterCaseRequestAttachmentsList;
  /** Why you are appealing the decision. */
  message: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const AppealResolutionCenterCaseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    attachments: S.optional(AppealResolutionCenterCaseRequestAttachmentsList),
    message: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/resolution_center_cases/{id}/appeal",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AppealResolutionCenterCaseRequest",
}) as any as S.Schema<AppealResolutionCenterCaseRequest>;

export type CreateResolutionCenterCaseRequestAttachmentsItem =
  AcceptResolutionCenterCaseRequestAttachmentsItem;
export const CreateResolutionCenterCaseRequestAttachmentsItem =
  AcceptResolutionCenterCaseRequestAttachmentsItem;

export type CreateResolutionCenterCaseRequestAttachmentsList =
  Array<AcceptResolutionCenterCaseRequestAttachmentsItem>;
export const CreateResolutionCenterCaseRequestAttachmentsList =
  /*@__PURE__*/ S.Array(
    AcceptResolutionCenterCaseRequestAttachmentsItem,
  ) as any as S.Schema<CreateResolutionCenterCaseRequestAttachmentsList>;

/** What went wrong. Uses the same vocabulary as `/disputes`. */
export type CreateResolutionCenterCaseRequestReason =
  | "fraudulent"
  | "product_not_received"
  | "not_as_described"
  | "product_unacceptable"
  | "subscription_canceled";
export const CreateResolutionCenterCaseRequestReason = /*@__PURE__*/ S.String;

export interface CreateResolutionCenterCaseRequest {
  attachments?: CreateResolutionCenterCaseRequestAttachmentsList;
  /** The customer's explanation. */
  message: string;
  /** What went wrong. Uses the same vocabulary as `/disputes`. */
  reason: CreateResolutionCenterCaseRequestReason | (string & {});
  /** The payment to open the case against (`pay_` tag). */
  receipt_id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateResolutionCenterCaseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    attachments: S.optional(CreateResolutionCenterCaseRequestAttachmentsList),
    message: S.String,
    reason: CreateResolutionCenterCaseRequestReason,
    receipt_id: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/resolution_center_cases", code: 200 }),
  ),
).annotate({
  identifier: "CreateResolutionCenterCaseRequest",
}) as any as S.Schema<CreateResolutionCenterCaseRequest>;

export type DenyResolutionCenterCaseRequestAttachmentsItem =
  AcceptResolutionCenterCaseRequestAttachmentsItem;
export const DenyResolutionCenterCaseRequestAttachmentsItem =
  AcceptResolutionCenterCaseRequestAttachmentsItem;

/** Up to 3 evidence files, by existing file `id` or `direct_upload_id`. */
export type DenyResolutionCenterCaseRequestAttachmentsList =
  Array<AcceptResolutionCenterCaseRequestAttachmentsItem>;
export const DenyResolutionCenterCaseRequestAttachmentsList =
  /*@__PURE__*/ S.Array(
    AcceptResolutionCenterCaseRequestAttachmentsItem,
  ) as any as S.Schema<DenyResolutionCenterCaseRequestAttachmentsList>;

export interface DenyResolutionCenterCaseRequest {
  /** The resolution center case ID (`reso_` tag). */
  id: string;
  /** Up to 3 evidence files, by existing file `id` or `direct_upload_id`. */
  attachments?: DenyResolutionCenterCaseRequestAttachmentsList;
  /** Why the claim is being denied. Shown to the customer. */
  message: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const DenyResolutionCenterCaseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    attachments: S.optional(DenyResolutionCenterCaseRequestAttachmentsList),
    message: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/resolution_center_cases/{id}/deny",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DenyResolutionCenterCaseRequest",
}) as any as S.Schema<DenyResolutionCenterCaseRequest>;

export interface ListResolutionCenterCaseEventsRequest {
  /** The resolution center case ID (`reso_` tag). */
  id: string;
  /** The number of events to return (default 20, max 100). */
  first?: number;
  /** A cursor; returns events after this position. */
  after?: string;
  /** The number of events to return from the end of the range. */
  last?: number;
  /** A cursor; returns events before this position. */
  before?: string;
}
export const ListResolutionCenterCaseEventsRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      id: S.String.pipe(T.Label()),
      first: S.optional(S.Number.pipe(T.Query())),
      after: S.optional(S.String.pipe(T.Query())),
      last: S.optional(S.Number.pipe(T.Query())),
      before: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/resolution_center_cases/{id}/events",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ListResolutionCenterCaseEventsRequest",
}) as any as S.Schema<ListResolutionCenterCaseEventsRequest>;

/** The action recorded in this event. */
export type ResolutionEventAction =
  | "created"
  | "responded"
  | "accepted"
  | "denied"
  | "appealed"
  | "withdrew"
  | "requested_more_info"
  | "escalated"
  | "dispute_opened"
  | "dispute_customer_won"
  | "dispute_merchant_won";
export const ResolutionEventAction = /*@__PURE__*/ S.String;

export interface ResolutionAttachment {
  /** The file's MIME type. */
  content_type: string | null;
  /** The original file name. */
  filename: string | null;
  /** Unique identifier for the attachment, prefixed `att_`. */
  id: string;
  /** A URL to view or download the file. */
  url: string | null;
}
export const ResolutionAttachment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    content_type: S.NullOr(S.String),
    filename: S.NullOr(S.String),
    id: S.String,
    url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ResolutionAttachment",
}) as any as S.Schema<ResolutionAttachment>;

export type ResolutionEventAttachmentsList = Array<ResolutionAttachment>;
export const ResolutionEventAttachmentsList = /*@__PURE__*/ S.Array(
  ResolutionAttachment,
) as any as S.Schema<ResolutionEventAttachmentsList>;

/** The party that performed the action. */
export type ResolutionEventReporterType =
  | "merchant"
  | "customer"
  | "platform"
  | "system";
export const ResolutionEventReporterType = /*@__PURE__*/ S.String;

export interface ResolutionEvent {
  /** The action recorded in this event. */
  action: ResolutionEventAction;
  attachments: ResolutionEventAttachmentsList;
  /** When the event occurred, as an ISO 8601 timestamp. */
  created_at: string;
  /** The message body or additional context provided with the event. */
  details: string | null;
  /** Unique identifier for the event, prefixed `revt_`. */
  id: string;
  /** The party that performed the action. */
  reporter_type: ResolutionEventReporterType;
  /** Whether the customer can see this event in the timeline. */
  viewable_by_customer: boolean;
  /** Whether the merchant can see this event in the timeline. */
  viewable_by_merchant: boolean;
}
export const ResolutionEvent = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: ResolutionEventAction,
    attachments: ResolutionEventAttachmentsList,
    created_at: S.String,
    details: S.NullOr(S.String),
    id: S.String,
    reporter_type: ResolutionEventReporterType,
    viewable_by_customer: S.Boolean,
    viewable_by_merchant: S.Boolean,
  }),
).annotate({
  identifier: "ResolutionEvent",
}) as any as S.Schema<ResolutionEvent>;

export type ListResolutionCenterCaseEventsResponseDataList =
  Array<ResolutionEvent>;
export const ListResolutionCenterCaseEventsResponseDataList =
  /*@__PURE__*/ S.Array(
    ResolutionEvent,
  ) as any as S.Schema<ListResolutionCenterCaseEventsResponseDataList>;

export interface ListResolutionCenterCaseEventsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListResolutionCenterCaseEventsResponsePageInfo =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      end_cursor: S.NullOr(S.String),
      has_next_page: S.Boolean,
      has_previous_page: S.Boolean,
      start_cursor: S.NullOr(S.String),
    }),
  ).annotate({
    identifier: "ListResolutionCenterCaseEventsResponsePageInfo",
  }) as any as S.Schema<ListResolutionCenterCaseEventsResponsePageInfo>;

export interface ListResolutionCenterCaseEventsResponse {
  data: ListResolutionCenterCaseEventsResponseDataList;
  page_info: ListResolutionCenterCaseEventsResponsePageInfo;
}
export const ListResolutionCenterCaseEventsResponse = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      data: ListResolutionCenterCaseEventsResponseDataList,
      page_info: ListResolutionCenterCaseEventsResponsePageInfo,
    }),
).annotate({
  identifier: "ListResolutionCenterCaseEventsResponse",
}) as any as S.Schema<ListResolutionCenterCaseEventsResponse>;

export type ListResolutionCenterCasesRequestOrder =
  | "created_at"
  | "response_due_at";
export const ListResolutionCenterCasesRequestOrder = /*@__PURE__*/ S.String;

export type ListResolutionCenterCasesRequestDirection = "asc" | "desc";
export const ListResolutionCenterCasesRequestDirection = /*@__PURE__*/ S.String;

export type ListResolutionCenterCasesRequestStatusItem =
  | "awaiting_merchant"
  | "awaiting_customer"
  | "under_review"
  | "closed";
export const ListResolutionCenterCasesRequestStatusItem =
  /*@__PURE__*/ S.String;

export type ListResolutionCenterCasesRequestStatusList = Array<
  ListResolutionCenterCasesRequestStatusItem | (string & {})
>;
export const ListResolutionCenterCasesRequestStatusList = /*@__PURE__*/ S.Array(
  ListResolutionCenterCasesRequestStatusItem,
) as any as S.Schema<ListResolutionCenterCasesRequestStatusList>;

export type ListResolutionCenterCasesRequestReasonItem =
  | "fraudulent"
  | "product_not_received"
  | "not_as_described"
  | "product_unacceptable"
  | "subscription_canceled";
export const ListResolutionCenterCasesRequestReasonItem =
  /*@__PURE__*/ S.String;

export type ListResolutionCenterCasesRequestReasonList = Array<
  ListResolutionCenterCasesRequestReasonItem | (string & {})
>;
export const ListResolutionCenterCasesRequestReasonList = /*@__PURE__*/ S.Array(
  ListResolutionCenterCasesRequestReasonItem,
) as any as S.Schema<ListResolutionCenterCasesRequestReasonList>;

export type ListResolutionCenterCasesRequestOutcomeItem =
  | "customer_won"
  | "merchant_won"
  | "withdrawn";
export const ListResolutionCenterCasesRequestOutcomeItem =
  /*@__PURE__*/ S.String;

export type ListResolutionCenterCasesRequestOutcomeList = Array<
  ListResolutionCenterCasesRequestOutcomeItem | (string & {})
>;
export const ListResolutionCenterCasesRequestOutcomeList =
  /*@__PURE__*/ S.Array(
    ListResolutionCenterCasesRequestOutcomeItem,
  ) as any as S.Schema<ListResolutionCenterCasesRequestOutcomeList>;

export interface ListResolutionCenterCasesRequest {
  /** Only cases filed against this account (`biz_` tag). With read access to the account this lists its whole queue; without, only the cases you opened against it. */
  account_id?: string;
  /** Only cases opened by this customer — a `user_` tag, or `me` for the calling user. It narrows what you can already read, so `me` lists the cases you opened without the ones on accounts you are a team member of. */
  user_id?: string;
  /** The number of cases to return (default 20, max 100). */
  first?: number;
  /** A cursor; returns cases after this position. */
  after?: string;
  /** The number of cases to return from the end of the range. */
  last?: number;
  /** A cursor; returns cases before this position. */
  before?: string;
  /** The field to sort cases by. */
  order?: ListResolutionCenterCasesRequestOrder | (string & {});
  /** Sort direction. */
  direction?: ListResolutionCenterCasesRequestDirection | (string & {});
  /** Only cases in these statuses. Repeat the parameter to pass several — one paginated list covers all of them. */
  status?: ListResolutionCenterCasesRequestStatusList;
  /** Only cases opened for these reasons. Repeat the parameter to pass several. */
  reason?: ListResolutionCenterCasesRequestReasonList;
  /** Only closed cases that ended these ways. Repeat the parameter to pass several. */
  outcome?: ListResolutionCenterCasesRequestOutcomeList;
  /** Only cases created before this ISO 8601 timestamp. */
  created_before?: string;
  /** Only cases created after this ISO 8601 timestamp. */
  created_after?: string;
}
export const ListResolutionCenterCasesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListResolutionCenterCasesRequestOrder.pipe(T.Query())),
    direction: S.optional(
      ListResolutionCenterCasesRequestDirection.pipe(T.Query()),
    ),
    status: S.optional(
      ListResolutionCenterCasesRequestStatusList.pipe(T.Query()),
    ),
    reason: S.optional(
      ListResolutionCenterCasesRequestReasonList.pipe(T.Query()),
    ),
    outcome: S.optional(
      ListResolutionCenterCasesRequestOutcomeList.pipe(T.Query()),
    ),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/resolution_center_cases", code: 200 }),
  ),
).annotate({
  identifier: "ListResolutionCenterCasesRequest",
}) as any as S.Schema<ListResolutionCenterCasesRequest>;

export type ListResolutionCenterCasesResponseDataList =
  Array<ResolutionCenterCase>;
export const ListResolutionCenterCasesResponseDataList = /*@__PURE__*/ S.Array(
  ResolutionCenterCase,
) as any as S.Schema<ListResolutionCenterCasesResponseDataList>;

export type ListResolutionCenterCasesResponsePageInfo =
  ListResolutionCenterCaseEventsResponsePageInfo;
export const ListResolutionCenterCasesResponsePageInfo =
  ListResolutionCenterCaseEventsResponsePageInfo;

export interface ListResolutionCenterCasesResponse {
  data: ListResolutionCenterCasesResponseDataList;
  page_info: ListResolutionCenterCaseEventsResponsePageInfo;
}
export const ListResolutionCenterCasesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListResolutionCenterCasesResponseDataList,
    page_info: ListResolutionCenterCaseEventsResponsePageInfo,
  }),
).annotate({
  identifier: "ListResolutionCenterCasesResponse",
}) as any as S.Schema<ListResolutionCenterCasesResponse>;

export type ReplyToResolutionCenterCaseRequestAttachmentsItem =
  AcceptResolutionCenterCaseRequestAttachmentsItem;
export const ReplyToResolutionCenterCaseRequestAttachmentsItem =
  AcceptResolutionCenterCaseRequestAttachmentsItem;

/** Up to 3 evidence files, by existing file `id` or `direct_upload_id`. */
export type ReplyToResolutionCenterCaseRequestAttachmentsList =
  Array<AcceptResolutionCenterCaseRequestAttachmentsItem>;
export const ReplyToResolutionCenterCaseRequestAttachmentsList =
  /*@__PURE__*/ S.Array(
    AcceptResolutionCenterCaseRequestAttachmentsItem,
  ) as any as S.Schema<ReplyToResolutionCenterCaseRequestAttachmentsList>;

export interface ReplyToResolutionCenterCaseRequest {
  /** The resolution center case ID (`reso_` tag). */
  id: string;
  /** Up to 3 evidence files, by existing file `id` or `direct_upload_id`. */
  attachments?: ReplyToResolutionCenterCaseRequestAttachmentsList;
  /** The reply to add to the case. */
  message: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const ReplyToResolutionCenterCaseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    attachments: S.optional(ReplyToResolutionCenterCaseRequestAttachmentsList),
    message: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/resolution_center_cases/{id}/reply",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ReplyToResolutionCenterCaseRequest",
}) as any as S.Schema<ReplyToResolutionCenterCaseRequest>;

export type RequestResolutionCenterCaseInfoRequestAttachmentsItem =
  AcceptResolutionCenterCaseRequestAttachmentsItem;
export const RequestResolutionCenterCaseInfoRequestAttachmentsItem =
  AcceptResolutionCenterCaseRequestAttachmentsItem;

/** Up to 3 evidence files, by existing file `id` or `direct_upload_id`. */
export type RequestResolutionCenterCaseInfoRequestAttachmentsList =
  Array<AcceptResolutionCenterCaseRequestAttachmentsItem>;
export const RequestResolutionCenterCaseInfoRequestAttachmentsList =
  /*@__PURE__*/ S.Array(
    AcceptResolutionCenterCaseRequestAttachmentsItem,
  ) as any as S.Schema<RequestResolutionCenterCaseInfoRequestAttachmentsList>;

export interface RequestResolutionCenterCaseInfoRequest {
  /** The resolution center case ID (`reso_` tag). */
  id: string;
  /** Up to 3 evidence files, by existing file `id` or `direct_upload_id`. */
  attachments?: RequestResolutionCenterCaseInfoRequestAttachmentsList;
  /** What you need from the customer. */
  message?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const RequestResolutionCenterCaseInfoRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      id: S.String.pipe(T.Label()),
      attachments: S.optional(
        RequestResolutionCenterCaseInfoRequestAttachmentsList,
      ),
      message: S.optional(S.String),
      idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/resolution_center_cases/{id}/request_info",
        code: 200,
      }),
    ),
).annotate({
  identifier: "RequestResolutionCenterCaseInfoRequest",
}) as any as S.Schema<RequestResolutionCenterCaseInfoRequest>;

export interface RetrieveResolutionCenterCaseRequest {
  /** The resolution center case ID (`reso_` tag). */
  id: string;
}
export const RetrieveResolutionCenterCaseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "GET", uri: "/resolution_center_cases/{id}", code: 200 }),
  ),
).annotate({
  identifier: "RetrieveResolutionCenterCaseRequest",
}) as any as S.Schema<RetrieveResolutionCenterCaseRequest>;

export type RetrieveResolutionCenterCaseSummaryRequestGroupsItem =
  | "status"
  | "reason"
  | "outcome";
export const RetrieveResolutionCenterCaseSummaryRequestGroupsItem =
  /*@__PURE__*/ S.String;

export type RetrieveResolutionCenterCaseSummaryRequestGroupsList = Array<
  RetrieveResolutionCenterCaseSummaryRequestGroupsItem | (string & {})
>;
export const RetrieveResolutionCenterCaseSummaryRequestGroupsList =
  /*@__PURE__*/ S.Array(
    RetrieveResolutionCenterCaseSummaryRequestGroupsItem,
  ) as any as S.Schema<RetrieveResolutionCenterCaseSummaryRequestGroupsList>;

export type RetrieveResolutionCenterCaseSummaryRequestStatusItem =
  | "awaiting_merchant"
  | "awaiting_customer"
  | "under_review"
  | "closed";
export const RetrieveResolutionCenterCaseSummaryRequestStatusItem =
  /*@__PURE__*/ S.String;

export type RetrieveResolutionCenterCaseSummaryRequestStatusList = Array<
  RetrieveResolutionCenterCaseSummaryRequestStatusItem | (string & {})
>;
export const RetrieveResolutionCenterCaseSummaryRequestStatusList =
  /*@__PURE__*/ S.Array(
    RetrieveResolutionCenterCaseSummaryRequestStatusItem,
  ) as any as S.Schema<RetrieveResolutionCenterCaseSummaryRequestStatusList>;

export type RetrieveResolutionCenterCaseSummaryRequestReasonItem =
  | "fraudulent"
  | "product_not_received"
  | "not_as_described"
  | "product_unacceptable"
  | "subscription_canceled";
export const RetrieveResolutionCenterCaseSummaryRequestReasonItem =
  /*@__PURE__*/ S.String;

export type RetrieveResolutionCenterCaseSummaryRequestReasonList = Array<
  RetrieveResolutionCenterCaseSummaryRequestReasonItem | (string & {})
>;
export const RetrieveResolutionCenterCaseSummaryRequestReasonList =
  /*@__PURE__*/ S.Array(
    RetrieveResolutionCenterCaseSummaryRequestReasonItem,
  ) as any as S.Schema<RetrieveResolutionCenterCaseSummaryRequestReasonList>;

export type RetrieveResolutionCenterCaseSummaryRequestOutcomeItem =
  | "customer_won"
  | "merchant_won"
  | "withdrawn";
export const RetrieveResolutionCenterCaseSummaryRequestOutcomeItem =
  /*@__PURE__*/ S.String;

export type RetrieveResolutionCenterCaseSummaryRequestOutcomeList = Array<
  RetrieveResolutionCenterCaseSummaryRequestOutcomeItem | (string & {})
>;
export const RetrieveResolutionCenterCaseSummaryRequestOutcomeList =
  /*@__PURE__*/ S.Array(
    RetrieveResolutionCenterCaseSummaryRequestOutcomeItem,
  ) as any as S.Schema<RetrieveResolutionCenterCaseSummaryRequestOutcomeList>;

export interface RetrieveResolutionCenterCaseSummaryRequest {
  /** Which breakdowns to return, keyed by these names under `groups`. Repeat the parameter to ask for several; omit it for all of them. */
  groups?: RetrieveResolutionCenterCaseSummaryRequestGroupsList;
  /** The account to summarize cases for (`biz_` tag). */
  account_id?: string;
  /** Only cases opened by this customer — a `user_` tag, or `me` for the calling user. */
  user_id?: string;
  /** Only cases in these statuses. */
  status?: RetrieveResolutionCenterCaseSummaryRequestStatusList;
  /** Only cases opened for these reasons. */
  reason?: RetrieveResolutionCenterCaseSummaryRequestReasonList;
  /** Only closed cases that ended these ways. */
  outcome?: RetrieveResolutionCenterCaseSummaryRequestOutcomeList;
  /** Only count cases created before this ISO 8601 timestamp. */
  created_before?: string;
  /** Only count cases created after this ISO 8601 timestamp. */
  created_after?: string;
}
export const RetrieveResolutionCenterCaseSummaryRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      groups: S.optional(
        RetrieveResolutionCenterCaseSummaryRequestGroupsList.pipe(T.Query()),
      ),
      account_id: S.optional(S.String.pipe(T.Query())),
      user_id: S.optional(S.String.pipe(T.Query())),
      status: S.optional(
        RetrieveResolutionCenterCaseSummaryRequestStatusList.pipe(T.Query()),
      ),
      reason: S.optional(
        RetrieveResolutionCenterCaseSummaryRequestReasonList.pipe(T.Query()),
      ),
      outcome: S.optional(
        RetrieveResolutionCenterCaseSummaryRequestOutcomeList.pipe(T.Query()),
      ),
      created_before: S.optional(S.String.pipe(T.Query())),
      created_after: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/resolution_center_cases/summary",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "RetrieveResolutionCenterCaseSummaryRequest",
  }) as any as S.Schema<RetrieveResolutionCenterCaseSummaryRequest>;

/** How many of the matching cases ended each way. Every outcome is present, including those with a count of zero; open cases are counted in none of them. */
export interface RetrieveResolutionCenterCaseSummaryResponseGroupsOutcome {
  customer_won: number;
  merchant_won: number;
  withdrawn: number;
}
export const RetrieveResolutionCenterCaseSummaryResponseGroupsOutcome =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      customer_won: S.Number,
      merchant_won: S.Number,
      withdrawn: S.Number,
    }),
  ).annotate({
    identifier: "RetrieveResolutionCenterCaseSummaryResponseGroupsOutcome",
  }) as any as S.Schema<RetrieveResolutionCenterCaseSummaryResponseGroupsOutcome>;

/** How many of the matching cases were opened for each reason. Every reason is present, including those with a count of zero. */
export interface RetrieveResolutionCenterCaseSummaryResponseGroupsReason {
  fraudulent: number;
  not_as_described: number;
  product_not_received: number;
  product_unacceptable: number;
  subscription_canceled: number;
}
export const RetrieveResolutionCenterCaseSummaryResponseGroupsReason =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      fraudulent: S.Number,
      not_as_described: S.Number,
      product_not_received: S.Number,
      product_unacceptable: S.Number,
      subscription_canceled: S.Number,
    }),
  ).annotate({
    identifier: "RetrieveResolutionCenterCaseSummaryResponseGroupsReason",
  }) as any as S.Schema<RetrieveResolutionCenterCaseSummaryResponseGroupsReason>;

/** How many of the matching cases are in each status. Every status is present, including those with a count of zero. */
export interface RetrieveResolutionCenterCaseSummaryResponseGroupsStatus {
  awaiting_customer: number;
  awaiting_merchant: number;
  closed: number;
  under_review: number;
}
export const RetrieveResolutionCenterCaseSummaryResponseGroupsStatus =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      awaiting_customer: S.Number,
      awaiting_merchant: S.Number,
      closed: S.Number,
      under_review: S.Number,
    }),
  ).annotate({
    identifier: "RetrieveResolutionCenterCaseSummaryResponseGroupsStatus",
  }) as any as S.Schema<RetrieveResolutionCenterCaseSummaryResponseGroupsStatus>;

/** One entry per requested breakdown, keyed by the field it groups on. A field you did not ask for is absent. */
export interface RetrieveResolutionCenterCaseSummaryResponseGroups {
  /** How many of the matching cases ended each way. Every outcome is present, including those with a count of zero; open cases are counted in none of them. */
  outcome?: RetrieveResolutionCenterCaseSummaryResponseGroupsOutcome;
  /** How many of the matching cases were opened for each reason. Every reason is present, including those with a count of zero. */
  reason?: RetrieveResolutionCenterCaseSummaryResponseGroupsReason;
  /** How many of the matching cases are in each status. Every status is present, including those with a count of zero. */
  status?: RetrieveResolutionCenterCaseSummaryResponseGroupsStatus;
}
export const RetrieveResolutionCenterCaseSummaryResponseGroups =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      outcome: S.optional(
        RetrieveResolutionCenterCaseSummaryResponseGroupsOutcome,
      ),
      reason: S.optional(
        RetrieveResolutionCenterCaseSummaryResponseGroupsReason,
      ),
      status: S.optional(
        RetrieveResolutionCenterCaseSummaryResponseGroupsStatus,
      ),
    }),
  ).annotate({
    identifier: "RetrieveResolutionCenterCaseSummaryResponseGroups",
  }) as any as S.Schema<RetrieveResolutionCenterCaseSummaryResponseGroups>;

export interface RetrieveResolutionCenterCaseSummaryResponse {
  /** One entry per requested breakdown, keyed by the field it groups on. A field you did not ask for is absent. */
  groups: RetrieveResolutionCenterCaseSummaryResponseGroups;
  /** How many cases match the filters. */
  total: number;
}
export const RetrieveResolutionCenterCaseSummaryResponse =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      groups: RetrieveResolutionCenterCaseSummaryResponseGroups,
      total: S.Number,
    }),
  ).annotate({
    identifier: "RetrieveResolutionCenterCaseSummaryResponse",
  }) as any as S.Schema<RetrieveResolutionCenterCaseSummaryResponse>;

export interface WithdrawResolutionCenterCaseRequest {
  /** The resolution center case ID (`reso_` tag). */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const WithdrawResolutionCenterCaseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/resolution_center_cases/{id}/withdraw",
      code: 200,
    }),
  ),
).annotate({
  identifier: "WithdrawResolutionCenterCaseRequest",
}) as any as S.Schema<WithdrawResolutionCenterCaseRequest>;

export type AcceptResolutionCenterCaseError =
  | Forbidden
  | Conflict
  | WhopOpError;
/** Accept a Case Accepts the case in the customer's favor, as the merchant: refunds the payment in full and closes the case. */
export const acceptResolutionCenterCase: API.OperationMethod<
  AcceptResolutionCenterCaseRequest,
  ResolutionCenterCase,
  AcceptResolutionCenterCaseError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AcceptResolutionCenterCaseRequest,
  output: ResolutionCenterCase,
  errors: [Forbidden, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type AppealResolutionCenterCaseError = Conflict | WhopOpError;
/** Appeal a Case Appeals a decision, as the customer, on a case that closed in the merchant's favor. Escalates the case to Whop for platform review. A case can be appealed once. */
export const appealResolutionCenterCase: API.OperationMethod<
  AppealResolutionCenterCaseRequest,
  ResolutionCenterCase,
  AppealResolutionCenterCaseError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AppealResolutionCenterCaseRequest,
  output: ResolutionCenterCase,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreateResolutionCenterCaseError = Conflict | WhopOpError;
/** Open a Case as the Customer Opens a case, as the customer, against one of your own payments. Provide the payment (`receipt_id`), the `reason`, and a `message`. */
export const createResolutionCenterCase: API.OperationMethod<
  CreateResolutionCenterCaseRequest,
  ResolutionCenterCase,
  CreateResolutionCenterCaseError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateResolutionCenterCaseRequest,
  output: ResolutionCenterCase,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DenyResolutionCenterCaseError = Conflict | WhopOpError;
/** Deny a Case Denies the case, as the merchant: rejects the claim and closes the case with no refund. */
export const denyResolutionCenterCase: API.OperationMethod<
  DenyResolutionCenterCaseRequest,
  ResolutionCenterCase,
  DenyResolutionCenterCaseError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DenyResolutionCenterCaseRequest,
  output: ResolutionCenterCase,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListResolutionCenterCaseEventsError = NotFound | WhopOpError;
/** List Resolution Center Case Events Lists the case timeline, newest first. Events the viewer is not allowed to see are omitted — a customer reads the customer-visible timeline, the merchant reads the full one. */
export const listResolutionCenterCaseEvents: API.PaginatedOperationMethod<
  ListResolutionCenterCaseEventsRequest,
  ListResolutionCenterCaseEventsResponse,
  ListResolutionCenterCaseEventsError,
  WhopOpContext,
  ResolutionEvent
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListResolutionCenterCaseEventsRequest,
    output: ListResolutionCenterCaseEventsResponse,
    errors: [NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ListResolutionCenterCasesError = Forbidden | NotFound | WhopOpError;
/** List Resolution Center Cases Lists resolution center cases. Without `account_id` you get every case you can read — the ones you opened as a buyer and every account you are a team member of; the filters narrow that list. */
export const listResolutionCenterCases: API.PaginatedOperationMethod<
  ListResolutionCenterCasesRequest,
  ListResolutionCenterCasesResponse,
  ListResolutionCenterCasesError,
  WhopOpContext,
  ResolutionCenterCase
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListResolutionCenterCasesRequest,
    output: ListResolutionCenterCasesResponse,
    errors: [Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ReplyToResolutionCenterCaseError = Conflict | WhopOpError;
/** Reply to a Case Replies to an open request for information on the case. As the merchant this answers Whop's request (valid while the case awaits your information); as the customer it provides the information requested from you. The actor is resolved from the credential. */
export const replyToResolutionCenterCase: API.OperationMethod<
  ReplyToResolutionCenterCaseRequest,
  ResolutionCenterCase,
  ReplyToResolutionCenterCaseError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReplyToResolutionCenterCaseRequest,
  output: ResolutionCenterCase,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RequestResolutionCenterCaseInfoError = Conflict | WhopOpError;
/** Request Information from the Customer Asks the customer for more information, as the merchant. Allowed up to 3 times per case before you must accept or deny it. */
export const requestResolutionCenterCaseInfo: API.OperationMethod<
  RequestResolutionCenterCaseInfoRequest,
  ResolutionCenterCase,
  RequestResolutionCenterCaseInfoError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RequestResolutionCenterCaseInfoRequest,
  output: ResolutionCenterCase,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveResolutionCenterCaseError = NotFound | WhopOpError;
/** Retrieve Resolution Center Case Retrieves a single resolution center case with its full event timeline. */
export const retrieveResolutionCenterCase: API.OperationMethod<
  RetrieveResolutionCenterCaseRequest,
  ResolutionCenterCase,
  RetrieveResolutionCenterCaseError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveResolutionCenterCaseRequest,
  output: ResolutionCenterCase,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveResolutionCenterCaseSummaryError = BadRequest | WhopOpError;
/** Retrieve Resolution Center Case Summary Aggregates the same cases `GET /resolution_center_cases` lists, using the same filters. Use it to build status tabs and issue filters without paging the whole list. */
export const retrieveResolutionCenterCaseSummary: API.OperationMethod<
  RetrieveResolutionCenterCaseSummaryRequest,
  RetrieveResolutionCenterCaseSummaryResponse,
  RetrieveResolutionCenterCaseSummaryError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveResolutionCenterCaseSummaryRequest,
  output: RetrieveResolutionCenterCaseSummaryResponse,
  errors: [BadRequest],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type WithdrawResolutionCenterCaseError = Conflict | WhopOpError;
/** Withdraw a Case Withdraws (cancels) a case you opened, as the customer. Only possible while the case is still open. */
export const withdrawResolutionCenterCase: API.OperationMethod<
  WithdrawResolutionCenterCaseRequest,
  ResolutionCenterCase,
  WithdrawResolutionCenterCaseError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WithdrawResolutionCenterCaseRequest,
  output: ResolutionCenterCase,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
