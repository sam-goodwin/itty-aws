// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** Buyer billing address used for tax calculation. Provide either `address.country` or `ip_address`; include state and postal code when available for more accurate results. */
export interface CalculatePlanTaxRequestAddress {
  /** City name. */
  city?: string | null;
  /** ISO 3166-1 alpha-2 country code, such as `US`, `DE`, or `GB`. */
  country: string;
  /** First line of the street address. */
  line1?: string | null;
  /** Second line of the street address. */
  line2?: string | null;
  /** Postal or ZIP code. */
  postal_code?: string | null;
  /** State, province, or region code, such as `CA`. */
  state?: string | null;
}
export const CalculatePlanTaxRequestAddress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    city: S.optional(S.NullOr(S.String)),
    country: S.String,
    line1: S.optional(S.NullOr(S.String)),
    line2: S.optional(S.NullOr(S.String)),
    postal_code: S.optional(S.NullOr(S.String)),
    state: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "CalculatePlanTaxRequestAddress",
}) as any as S.Schema<CalculatePlanTaxRequestAddress>;

/** Tax ID type, such as `eu_vat` for an EU VAT number. */
export type CalculatePlanTaxRequestTaxIdsItemType =
  | "ad_nrt"
  | "ao_tin"
  | "ar_cuit"
  | "al_tin"
  | "am_tin"
  | "aw_tin"
  | "au_abn"
  | "au_arn"
  | "eu_vat"
  | "az_tin"
  | "bs_tin"
  | "bh_vat"
  | "bd_bin"
  | "bb_tin"
  | "by_tin"
  | "bj_ifu"
  | "bo_tin"
  | "ba_tin"
  | "br_cnpj"
  | "br_cpf"
  | "bg_uic"
  | "bf_ifu"
  | "kh_tin"
  | "cm_niu"
  | "ca_bn"
  | "ca_gst_hst"
  | "ca_pst_bc"
  | "ca_pst_mb"
  | "ca_pst_sk"
  | "ca_qst"
  | "cv_nif"
  | "cl_tin"
  | "cn_tin"
  | "co_nit"
  | "cd_nif"
  | "cr_tin"
  | "hr_oib"
  | "do_rcn"
  | "ec_ruc"
  | "eg_tin"
  | "sv_nit"
  | "et_tin"
  | "eu_oss_vat"
  | "ge_vat"
  | "gh_tin"
  | "de_stn"
  | "gb_vat"
  | "gn_nif"
  | "hk_br"
  | "hu_tin"
  | "is_vat"
  | "in_gst"
  | "id_npwp"
  | "il_vat"
  | "jp_cn"
  | "jp_rn"
  | "jp_trn"
  | "kz_bin"
  | "ke_pin"
  | "kg_tin"
  | "la_tin"
  | "li_uid"
  | "li_vat"
  | "my_frp"
  | "my_itn"
  | "my_sst"
  | "mr_nif"
  | "mx_rfc"
  | "md_vat"
  | "me_pib"
  | "ma_vat"
  | "np_pan"
  | "nz_gst"
  | "ng_tin"
  | "mk_vat"
  | "no_vat"
  | "no_voec"
  | "om_vat"
  | "pe_ruc"
  | "ph_tin"
  | "ro_tin"
  | "ru_inn"
  | "ru_kpp"
  | "sa_vat"
  | "sn_ninea"
  | "rs_pib"
  | "sg_gst"
  | "sg_uen"
  | "si_tin"
  | "za_vat"
  | "kr_brn"
  | "es_cif"
  | "ch_uid"
  | "ch_vat"
  | "tw_vat"
  | "tj_tin"
  | "tz_vat"
  | "th_vat"
  | "tr_tin"
  | "ug_tin"
  | "ua_vat"
  | "ae_trn"
  | "us_ein"
  | "uy_ruc"
  | "uz_tin"
  | "uz_vat"
  | "ve_rif"
  | "vn_tin"
  | "zm_tin"
  | "zw_tin"
  | "sr_fin";
export const CalculatePlanTaxRequestTaxIdsItemType = /*@__PURE__*/ S.String;

export interface CalculatePlanTaxRequestTaxIdsItem {
  /** Tax ID type, such as `eu_vat` for an EU VAT number. */
  type?: CalculatePlanTaxRequestTaxIdsItemType | (string & {});
  /** Tax ID value, for example `DE123456789`. */
  value?: string;
}
export const CalculatePlanTaxRequestTaxIdsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.optional(CalculatePlanTaxRequestTaxIdsItemType),
    value: S.optional(S.String),
  }),
).annotate({
  identifier: "CalculatePlanTaxRequestTaxIdsItem",
}) as any as S.Schema<CalculatePlanTaxRequestTaxIdsItem>;

/** Optional buyer tax ID for B2B exemptions. At most one entry is supported. */
export type CalculatePlanTaxRequestTaxIdsList =
  Array<CalculatePlanTaxRequestTaxIdsItem>;
export const CalculatePlanTaxRequestTaxIdsList = /*@__PURE__*/ S.Array(
  CalculatePlanTaxRequestTaxIdsItem,
) as any as S.Schema<CalculatePlanTaxRequestTaxIdsList>;

export interface CalculatePlanTaxRequest {
  /** Plan ID, prefixed `plan_`. */
  id: string;
  /** Buyer billing address used for tax calculation. Provide either `address.country` or `ip_address`; include state and postal code when available for more accurate results. */
  address?: CalculatePlanTaxRequestAddress | null;
  /** Buyer IP address used to infer location when no billing address is provided. */
  ip_address?: string;
  /** Optional buyer tax ID for B2B exemptions. At most one entry is supported. */
  tax_ids?: CalculatePlanTaxRequestTaxIdsList | null;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CalculatePlanTaxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    address: S.optional(S.NullOr(CalculatePlanTaxRequestAddress)),
    ip_address: S.optional(S.String),
    tax_ids: S.optional(S.NullOr(CalculatePlanTaxRequestTaxIdsList)),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/plans/{id}/calculate_tax", code: 200 }),
  ),
).annotate({
  identifier: "CalculatePlanTaxRequest",
}) as any as S.Schema<CalculatePlanTaxRequest>;

/** Whether Whop calculated tax for this preview. `not_calculated` means no tax could be determined, so `tax_amount` is 0 and `total` equals `subtotal`. */
export type CalculatePlanTaxResponseStatus = "calculated" | "not_calculated";
export const CalculatePlanTaxResponseStatus = /*@__PURE__*/ S.String;

/** Whether tax is added on top of the plan price or already included in it for this buyer's location. */
export type CalculatePlanTaxResponseTaxBehavior = "exclusive" | "inclusive";
export const CalculatePlanTaxResponseTaxBehavior = /*@__PURE__*/ S.String;

export interface CalculatePlanTaxResponse {
  /** Three-letter ISO 4217 currency code for the returned amounts. */
  currency: string;
  /** Whether Whop calculated tax for this preview. `not_calculated` means no tax could be determined, so `tax_amount` is 0 and `total` equals `subtotal`. */
  status: CalculatePlanTaxResponseStatus;
  /** Plan price in the currency's smallest unit, for example cents. For exclusive tax, this is the pre-tax amount; for inclusive tax, it already includes tax and equals the total. */
  subtotal: number;
  /** Calculated tax amount in the currency's smallest unit. For exclusive tax, this is added on top of the subtotal; for inclusive tax, it is the portion of the subtotal that is tax. */
  tax_amount: number;
  /** Whether tax is added on top of the plan price or already included in it for this buyer's location. */
  tax_behavior: CalculatePlanTaxResponseTaxBehavior;
  /** Amount the buyer would pay in the currency's smallest unit. */
  total: number;
}
export const CalculatePlanTaxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    currency: S.String,
    status: CalculatePlanTaxResponseStatus,
    subtotal: S.Number,
    tax_amount: S.Number,
    tax_behavior: CalculatePlanTaxResponseTaxBehavior,
    total: S.Number,
  }),
).annotate({
  identifier: "CalculatePlanTaxResponse",
}) as any as S.Schema<CalculatePlanTaxResponse>;

/** The type of the custom field. */
export type CreatePlanRequestCustomFieldsItemFieldType = "text";
export const CreatePlanRequestCustomFieldsItemFieldType =
  /*@__PURE__*/ S.String;

export interface CreatePlanRequestCustomFieldsItem {
  /** The type of the custom field. */
  field_type?: CreatePlanRequestCustomFieldsItemFieldType | (string & {});
  /** The ID of the custom field (if being updated). */
  id?: string;
  /** The name of the custom field. */
  name?: string;
  /** The order of the field. */
  order?: number;
  /** An example response displayed in the input field. */
  placeholder?: string | null;
  /** Whether or not the field is required. */
  required?: boolean;
}
export const CreatePlanRequestCustomFieldsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    field_type: S.optional(CreatePlanRequestCustomFieldsItemFieldType),
    id: S.optional(S.String),
    name: S.optional(S.String),
    order: S.optional(S.Number),
    placeholder: S.optional(S.NullOr(S.String)),
    required: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "CreatePlanRequestCustomFieldsItem",
}) as any as S.Schema<CreatePlanRequestCustomFieldsItem>;

/** An array of custom field definitions to collect from customers at checkout. Omitting this field clears existing custom fields. */
export type CreatePlanRequestCustomFieldsList =
  Array<CreatePlanRequestCustomFieldsItem>;
export const CreatePlanRequestCustomFieldsList = /*@__PURE__*/ S.Array(
  CreatePlanRequestCustomFieldsItem,
) as any as S.Schema<CreatePlanRequestCustomFieldsList>;

/** An image displayed on the product page to represent this plan. */
export interface CreatePlanRequestImage {
  direct_upload_id?: string;
  id?: string;
}
export const CreatePlanRequestImage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    direct_upload_id: S.optional(S.String),
    id: S.optional(S.String),
  }),
).annotate({
  identifier: "CreatePlanRequestImage",
}) as any as S.Schema<CreatePlanRequestImage>;

export type CreatePlanRequestPaymentMethodConfigurationDisabledList =
  Array<string>;
export const CreatePlanRequestPaymentMethodConfigurationDisabledList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreatePlanRequestPaymentMethodConfigurationDisabledList>;

export type CreatePlanRequestPaymentMethodConfigurationEnabledList =
  Array<string>;
export const CreatePlanRequestPaymentMethodConfigurationEnabledList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreatePlanRequestPaymentMethodConfigurationEnabledList>;

/** Explicit payment method configuration for the plan. When not provided, the account's defaults apply. */
export interface CreatePlanRequestPaymentMethodConfiguration {
  disabled?: CreatePlanRequestPaymentMethodConfigurationDisabledList;
  enabled?: CreatePlanRequestPaymentMethodConfigurationEnabledList;
  include_platform_defaults?: boolean;
}
export const CreatePlanRequestPaymentMethodConfiguration =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      disabled: S.optional(
        CreatePlanRequestPaymentMethodConfigurationDisabledList,
      ),
      enabled: S.optional(
        CreatePlanRequestPaymentMethodConfigurationEnabledList,
      ),
      include_platform_defaults: S.optional(S.Boolean),
    }),
  ).annotate({
    identifier: "CreatePlanRequestPaymentMethodConfiguration",
  }) as any as S.Schema<CreatePlanRequestPaymentMethodConfiguration>;

/** 3D Secure behavior for this plan. Send `null` to inherit the account default. */
export type CreatePlanRequestThreeDsLevel =
  | "mandate_challenge"
  | "frictionless";
export const CreatePlanRequestThreeDsLevel = /*@__PURE__*/ S.String;

export interface CreatePlanRequest {
  /** The unique identifier of the account to create this plan for. Defaults to the caller's account. */
  account_id?: string;
  /** Whether this plan accepts local currency payments via adaptive pricing. */
  adaptive_pricing_enabled?: boolean | null;
  /** Recurring billing interval in days, such as 30 for monthly or 365 for annual. */
  billing_period?: number | null;
  /** Checkout styling overrides for this plan. */
  checkout_styling?: unknown | null;
  /** The three-letter ISO currency code for the plan's pricing. Defaults to USD. */
  currency?: string;
  /** An array of custom field definitions to collect from customers at checkout. Omitting this field clears existing custom fields. */
  custom_fields?: CreatePlanRequestCustomFieldsList | null;
  /** A text description of the plan displayed to customers on the product page. */
  description?: string | null;
  /** Access duration in days before the membership expires. */
  expiration_days?: number | null;
  /** An image displayed on the product page to represent this plan. */
  image?: CreatePlanRequestImage | null;
  /** Initial amount charged in the plan's currency, e.g. 10.43 for $10.43. */
  initial_price?: number | null;
  /** Private notes visible only to the account owner. Not shown to customers. */
  internal_notes?: string | null;
  /** Custom key-value pairs to store on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys `custom_cta` (a checkout call-to-action button label — one of the product custom CTA values, e.g. `subscribe`, `get_offer`) and `custom_cta_url` (a URL the button links to; web or `tel:`) override the product's call to action for this plan and are validated on save. */
  metadata?: unknown | null;
  /** Override the default tax classification for this specific plan. */
  override_tax_type?: string;
  /** Explicit payment method configuration for the plan. When not provided, the account's defaults apply. */
  payment_method_configuration?: CreatePlanRequestPaymentMethodConfiguration | null;
  /** Plan billing type, such as `one_time` or `renewal`. */
  plan_type?: string;
  /** The unique identifier of the product to attach this plan to. */
  product_id?: string;
  /** Sales method for this plan. */
  release_method?: string;
  /** The amount charged each billing period for recurring plans, in the plan's currency. */
  renewal_price?: number | null;
  /** Installment payments required before the subscription pauses. */
  split_pay_required_payments?: number | null;
  /** The maximum number of units available for purchase. Ignored when unlimited_stock is true. */
  stock?: number | null;
  /** 3D Secure behavior for this plan. Send `null` to inherit the account default. */
  three_ds_level?: CreatePlanRequestThreeDsLevel | (string & {}) | null;
  /** The display name of the plan shown to customers on the product page. */
  title?: string | null;
  /** Free trial duration before the first recurring charge. */
  trial_period_days?: number | null;
  /** Whether the plan has unlimited stock. When true, the stock field is ignored. */
  unlimited_stock?: boolean | null;
  /** Whether the plan is visible to customers or hidden from public view. */
  visibility?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreatePlanRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    adaptive_pricing_enabled: S.optional(S.NullOr(S.Boolean)),
    billing_period: S.optional(S.NullOr(S.Number)),
    checkout_styling: S.optional(S.NullOr(S.Unknown)),
    currency: S.optional(S.String),
    custom_fields: S.optional(S.NullOr(CreatePlanRequestCustomFieldsList)),
    description: S.optional(S.NullOr(S.String)),
    expiration_days: S.optional(S.NullOr(S.Number)),
    image: S.optional(S.NullOr(CreatePlanRequestImage)),
    initial_price: S.optional(S.NullOr(S.Number)),
    internal_notes: S.optional(S.NullOr(S.String)),
    metadata: S.optional(S.NullOr(S.Unknown)),
    override_tax_type: S.optional(S.String),
    payment_method_configuration: S.optional(
      S.NullOr(CreatePlanRequestPaymentMethodConfiguration),
    ),
    plan_type: S.optional(S.String),
    product_id: S.optional(S.String),
    release_method: S.optional(S.String),
    renewal_price: S.optional(S.NullOr(S.Number)),
    split_pay_required_payments: S.optional(S.NullOr(S.Number)),
    stock: S.optional(S.NullOr(S.Number)),
    three_ds_level: S.optional(S.NullOr(CreatePlanRequestThreeDsLevel)),
    title: S.optional(S.NullOr(S.String)),
    trial_period_days: S.optional(S.NullOr(S.Number)),
    unlimited_stock: S.optional(S.NullOr(S.Boolean)),
    visibility: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/plans", code: 200 })),
).annotate({
  identifier: "CreatePlanRequest",
}) as any as S.Schema<CreatePlanRequest>;

export interface AccountSummary {
  /** Account ID, prefixed `biz_`. */
  id: string;
  /** Account display name. */
  title: string;
}
export const AccountSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({ identifier: "AccountSummary" }) as any as S.Schema<AccountSummary>;

/** Three-letter ISO currency code for this plan's prices. */
export type PlanCurrency =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const PlanCurrency = /*@__PURE__*/ S.String;

/** Custom field input type. */
export type PlanCustomFieldFieldType = "text";
export const PlanCustomFieldFieldType = /*@__PURE__*/ S.String;

export interface PlanCustomField {
  /** Custom field input type. */
  field_type: PlanCustomFieldFieldType;
  /** Custom field ID, prefixed `field_`. */
  id: string;
  /** Field label shown to customer at checkout. */
  name: string;
  /** Field position on checkout form. */
  order: number;
  /** Placeholder text shown in the empty field. `null` if none is set. */
  placeholder: string | null;
  /** Whether the customer must complete this field to check out. */
  required: boolean;
}
export const PlanCustomField = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    field_type: PlanCustomFieldFieldType,
    id: S.String,
    name: S.String,
    order: S.Number,
    placeholder: S.NullOr(S.String),
    required: S.Boolean,
  }),
).annotate({
  identifier: "PlanCustomField",
}) as any as S.Schema<PlanCustomField>;

export type PlanCustomFieldsList = Array<PlanCustomField>;
export const PlanCustomFieldsList = /*@__PURE__*/ S.Array(
  PlanCustomField,
) as any as S.Schema<PlanCustomFieldsList>;

export type CheckoutSessionPaymentMethodConfigurationDisabledList =
  Array<string>;
export const CheckoutSessionPaymentMethodConfigurationDisabledList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CheckoutSessionPaymentMethodConfigurationDisabledList>;

export type CheckoutSessionPaymentMethodConfigurationEnabledList =
  Array<string>;
export const CheckoutSessionPaymentMethodConfigurationEnabledList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CheckoutSessionPaymentMethodConfigurationEnabledList>;

export interface CheckoutSessionPaymentMethodConfiguration {
  disabled: CheckoutSessionPaymentMethodConfigurationDisabledList;
  enabled: CheckoutSessionPaymentMethodConfigurationEnabledList;
  /** Whether Whop's default set is the starting point. When `false`, only `enabled` is offered. */
  include_platform_defaults: boolean;
}
export const CheckoutSessionPaymentMethodConfiguration =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      disabled: CheckoutSessionPaymentMethodConfigurationDisabledList,
      enabled: CheckoutSessionPaymentMethodConfigurationEnabledList,
      include_platform_defaults: S.Boolean,
    }),
  ).annotate({
    identifier: "CheckoutSessionPaymentMethodConfiguration",
  }) as any as S.Schema<CheckoutSessionPaymentMethodConfiguration>;

/** Billing model for this plan. */
export type PlanPlanType = "renewal" | "one_time";
export const PlanPlanType = /*@__PURE__*/ S.String;

/** Sales method for this plan. */
export type PlanReleaseMethod = "buy_now" | "waitlist";
export const PlanReleaseMethod = /*@__PURE__*/ S.String;

/** How tax is handled for this plan, including whether tax is included in the price, added at checkout, or not configured. */
export type PlanTaxType = "inclusive" | "exclusive" | "unspecified";
export const PlanTaxType = /*@__PURE__*/ S.String;

/** 3D Secure behavior for this plan; `null` inherits the account default. */
export type PlanThreeDsLevel = "mandate_challenge" | "frictionless";
export const PlanThreeDsLevel = /*@__PURE__*/ S.String;

/** Controls where this plan can be seen. When `hidden`, the plan is reachable only by its direct link. */
export type PlanVisibility = "visible" | "hidden" | "archived" | "quick_link";
export const PlanVisibility = /*@__PURE__*/ S.String;

export interface Plan {
  /** Account that sells this plan; `null` for standalone invoice plans. */
  account: AccountSummary | null;
  /** Whether adaptive pricing is enabled for this plan. Raw setting — does not check processor compatibility or feature flags. */
  adaptive_pricing_enabled: boolean;
  /** Number of days between recurring charges, such as 30 for monthly or 365 for annual. `null` for one-time plans. */
  billing_period: number | null;
  /** Billing intervals the cancellation discount applies to (`0` forever, `1` first payment, or a month count). `null` when none is offered or the actor lacks the `plan:basic:read` scope. */
  cancel_discount_intervals: number | null;
  /** Cancellation discount as a whole-number percentage. `null` when none is offered or the actor lacks the `plan:basic:read` scope. */
  cancel_discount_percentage: number | null;
  /** Plan-level checkout styling (`background_color`, `button_color`, `font_family`, `border_style`); `null` inherits the account default. */
  checkout_styling: unknown | null;
  /** Whether tax is collected on purchases of this plan, based on the account's tax configuration. */
  collect_tax: boolean;
  /** When the plan was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Three-letter ISO currency code for this plan's prices. */
  currency: PlanCurrency;
  custom_fields: PlanCustomFieldsList;
  /** Whether the plan can be deleted (it has no memberships or waitlist entries). `null` unless the actor has the `plan:basic:read` scope on the plan's account. */
  deletable: boolean | null;
  /** Customer-visible plan description. Maximum 1000 characters. `null` if no description is set. */
  description: string | null;
  /** The configuration governing a checkout for this plan, resolved through every layer (the plan's own and the account's) — the shape a session's `payment_method_configuration` carries. Apply it over the payment method types catalogue for the offerable set. `null` means platform defaults; `payment_method_configuration` stays the plan's own editable override. */
  effective_payment_method_configuration: CheckoutSessionPaymentMethodConfiguration | null;
  /** Access duration in days for expiration-based plans, such as 365 for a one-year pass. `null` for plans without an expiration. */
  expiration_days: number | null;
  /** Human-readable price for display (currency + interval), e.g. "$10 / month". */
  formatted_price: string;
  /** Plan ID, prefixed `plan_`. */
  id: string;
  /** Pricing-tier image (`url`, `blurhash`) shown on the product page; `null` when no image is set. */
  image: unknown | null;
  /** Initial purchase price in plan currency. */
  initial_price: number;
  /** Private notes not shown to customers. `null` unless the actor has the `plan:basic:read` scope on the plan's account. */
  internal_notes: string | null;
  /** Invoice this plan was generated for; `null` unless created for an invoice. */
  invoice: unknown | null;
  /** Active memberships through this plan. `null` unless the actor has the `plan:basic:read` scope on the plan's account. */
  member_count: number | null;
  /** Custom key-value pairs stored on the plan. Included in webhook payloads for payment and membership events. Maximum 50 keys, 100 characters per key, 500 characters per value. The reserved keys `custom_cta` and `custom_cta_url`, when set, override the product's checkout call to action for this plan. */
  metadata: unknown | null;
  /** Whether a cancellation discount is offered. `null` unless the actor has the `plan:basic:read` scope on the plan's account. */
  offer_cancel_discount: boolean | null;
  /** Payment method configuration (`enabled`, `disabled`, `include_platform_defaults`); `null` when plan uses default settings. */
  payment_method_configuration: unknown | null;
  /** Billing model for this plan. */
  plan_type: PlanPlanType;
  /** Product this plan belongs to; `null` for standalone plans. */
  product: unknown | null;
  /** URL where customers can purchase this plan directly. */
  purchase_url: string;
  /** Sales method for this plan. */
  release_method: PlanReleaseMethod;
  /** Recurring price charged every billing period. */
  renewal_price: number;
  /** Installment payments required before the subscription pauses. Must be greater than 1. `null` if split pay is not configured. */
  split_pay_required_payments: number | null;
  /** Units available for purchase. `null` unless the actor has the `plan:basic:read` scope on the plan's account. */
  stock: number | null;
  /** Original initial price shown with a strikethrough, in the plan's currency. `null` when no strikethrough is set. */
  strike_through_initial_price: number | null;
  /** Original renewal price shown with a strikethrough, in the plan's currency. `null` when no strikethrough is set. */
  strike_through_renewal_price: number | null;
  /** How tax is handled for this plan, including whether tax is included in the price, added at checkout, or not configured. */
  tax_type: PlanTaxType;
  /** 3D Secure behavior for this plan; `null` inherits the account default. */
  three_ds_level: PlanThreeDsLevel | null;
  /** Plan display name shown to customers. Maximum 30 characters. `null` if no title has been set. */
  title: string | null;
  /** Free trial days before the first renewal charge. `null` if no trial is configured or the user has already used a trial for this plan. */
  trial_period_days: number | null;
  /** Whether the plan has unlimited stock. When `true`, the `stock` field is ignored; waitlist plans always report `true`. */
  unlimited_stock: boolean;
  /** When the plan was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** Controls where this plan can be seen. When `hidden`, the plan is reachable only by its direct link. */
  visibility: PlanVisibility;
}
export const Plan = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account: S.NullOr(AccountSummary),
    adaptive_pricing_enabled: S.Boolean,
    billing_period: S.NullOr(S.Number),
    cancel_discount_intervals: S.NullOr(S.Number),
    cancel_discount_percentage: S.NullOr(S.Number),
    checkout_styling: S.NullOr(S.Unknown),
    collect_tax: S.Boolean,
    created_at: S.String,
    currency: PlanCurrency,
    custom_fields: PlanCustomFieldsList,
    deletable: S.NullOr(S.Boolean),
    description: S.NullOr(S.String),
    effective_payment_method_configuration: S.NullOr(
      CheckoutSessionPaymentMethodConfiguration,
    ),
    expiration_days: S.NullOr(S.Number),
    formatted_price: S.String,
    id: S.String,
    image: S.NullOr(S.Unknown),
    initial_price: S.Number,
    internal_notes: S.NullOr(S.String),
    invoice: S.NullOr(S.Unknown),
    member_count: S.NullOr(S.Number),
    metadata: S.NullOr(S.Unknown),
    offer_cancel_discount: S.NullOr(S.Boolean),
    payment_method_configuration: S.NullOr(S.Unknown),
    plan_type: PlanPlanType,
    product: S.NullOr(S.Unknown),
    purchase_url: S.String,
    release_method: PlanReleaseMethod,
    renewal_price: S.Number,
    split_pay_required_payments: S.NullOr(S.Number),
    stock: S.NullOr(S.Number),
    strike_through_initial_price: S.NullOr(S.Number),
    strike_through_renewal_price: S.NullOr(S.Number),
    tax_type: PlanTaxType,
    three_ds_level: S.NullOr(PlanThreeDsLevel),
    title: S.NullOr(S.String),
    trial_period_days: S.NullOr(S.Number),
    unlimited_stock: S.Boolean,
    updated_at: S.String,
    visibility: PlanVisibility,
  }),
).annotate({ identifier: "Plan" }) as any as S.Schema<Plan>;

export interface DeletePlanRequest {
  /** Plan ID, prefixed `plan_`. */
  id: string;
}
export const DeletePlanRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/plans/{id}", code: 200 })),
).annotate({
  identifier: "DeletePlanRequest",
}) as any as S.Schema<DeletePlanRequest>;

export interface DeletePlanResponse {
  /** Always true. */
  deleted: boolean;
  /** ID of the deleted plan. */
  id: string;
}
export const DeletePlanResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "DeletePlanResponse",
}) as any as S.Schema<DeletePlanResponse>;

export type ListPlansRequestDirection = "asc" | "desc";
export const ListPlansRequestDirection = /*@__PURE__*/ S.String;

export type ListPlansRequestOrder =
  | "id"
  | "active_members_count"
  | "created_at"
  | "internal_notes"
  | "expiration_days";
export const ListPlansRequestOrder = /*@__PURE__*/ S.String;

export type ListPlansRequestReleaseMethodsList = Array<string>;
export const ListPlansRequestReleaseMethodsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListPlansRequestReleaseMethodsList>;

export type ListPlansRequestVisibilitiesList = Array<string>;
export const ListPlansRequestVisibilitiesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListPlansRequestVisibilitiesList>;

export type ListPlansRequestPlanTypesList = Array<string>;
export const ListPlansRequestPlanTypesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListPlansRequestPlanTypesList>;

export type ListPlansRequestProductIdsList = Array<string>;
export const ListPlansRequestProductIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListPlansRequestProductIdsList>;

export interface ListPlansRequest {
  /** The unique identifier of the account to list plans for. Required unless `product_ids` is provided for a public product-plan read. */
  account_id?: string;
  /** The sort direction for results. Defaults to descending. */
  direction?: ListPlansRequestDirection | (string & {});
  /** The field to sort results by. Defaults to created_at. */
  order?: ListPlansRequestOrder | (string & {});
  /** Filter to only plans matching these release methods. */
  release_methods?: ListPlansRequestReleaseMethodsList;
  /** Filter to only plans matching these visibility states. */
  visibilities?: ListPlansRequestVisibilitiesList;
  /** Filter to only plans matching these billing types. */
  plan_types?: ListPlansRequestPlanTypesList;
  /** Filter to only plans belonging to these product identifiers. When `account_id` is omitted, this is required and the response is publicly readable: only visible, non-invoice plans are returned. */
  product_ids?: ListPlansRequestProductIdsList;
  /** Only return plans created before this timestamp. */
  created_before?: string;
  /** Only return plans created after this timestamp. */
  created_after?: string;
  /** The number of plans to return (default and max 100). */
  first?: number;
  /** A cursor; returns plans after this position. */
  after?: string;
  /** The number of plans to return from the end of the range. */
  last?: number;
  /** A cursor; returns plans before this position. */
  before?: string;
}
export const ListPlansRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    direction: S.optional(ListPlansRequestDirection.pipe(T.Query())),
    order: S.optional(ListPlansRequestOrder.pipe(T.Query())),
    release_methods: S.optional(
      ListPlansRequestReleaseMethodsList.pipe(T.Query()),
    ),
    visibilities: S.optional(ListPlansRequestVisibilitiesList.pipe(T.Query())),
    plan_types: S.optional(ListPlansRequestPlanTypesList.pipe(T.Query())),
    product_ids: S.optional(ListPlansRequestProductIdsList.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/plans", code: 200 })),
).annotate({
  identifier: "ListPlansRequest",
}) as any as S.Schema<ListPlansRequest>;

export type PlanListItemCustomFieldsList = Array<PlanCustomField>;
export const PlanListItemCustomFieldsList = /*@__PURE__*/ S.Array(
  PlanCustomField,
) as any as S.Schema<PlanListItemCustomFieldsList>;

/** Billing model for this plan. */
export type PlanListItemPlanType = "renewal" | "one_time";
export const PlanListItemPlanType = /*@__PURE__*/ S.String;

/** Sales method for this plan. */
export type PlanListItemReleaseMethod = "buy_now" | "waitlist";
export const PlanListItemReleaseMethod = /*@__PURE__*/ S.String;

/** 3D Secure behavior for this plan; `null` inherits the account default. */
export type PlanListItemThreeDsLevel = "mandate_challenge" | "frictionless";
export const PlanListItemThreeDsLevel = /*@__PURE__*/ S.String;

/** Controls where this plan can be seen. When `hidden`, the plan is reachable only by its direct link. */
export type PlanListItemVisibility =
  | "visible"
  | "hidden"
  | "archived"
  | "quick_link";
export const PlanListItemVisibility = /*@__PURE__*/ S.String;

export interface PlanListItem {
  /** Account that sells this plan; `null` for standalone invoice plans. */
  account: AccountSummary | null;
  /** Whether adaptive pricing is enabled for this plan. Raw setting — does not check processor compatibility or feature flags. */
  adaptive_pricing_enabled: boolean;
  /** Number of days between recurring charges, such as 30 for monthly or 365 for annual. `null` for one-time plans. */
  billing_period: number | null;
  /** Billing intervals the cancellation discount applies to (`0` forever, `1` first payment, or a month count). `null` when none is offered or the actor lacks the `plan:basic:read` scope. */
  cancel_discount_intervals: number | null;
  /** Cancellation discount as a whole-number percentage. `null` when none is offered or the actor lacks the `plan:basic:read` scope. */
  cancel_discount_percentage: number | null;
  /** Plan-level checkout styling (`background_color`, `button_color`, `font_family`, `border_style`); `null` inherits the account default. */
  checkout_styling: unknown | null;
  /** When the plan was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Three-letter ISO currency code for this plan's prices. */
  currency: string;
  custom_fields: PlanListItemCustomFieldsList;
  /** Customer-visible plan description. Maximum 1000 characters. `null` if no description is set. */
  description: string | null;
  /** Access duration in days for expiration-based plans, such as 365 for a one-year pass. `null` for plans without an expiration. */
  expiration_days: number | null;
  /** Human-readable price for display (currency + interval), e.g. "$10 / month". */
  formatted_price: string;
  /** Plan ID, prefixed `plan_`. */
  id: string;
  /** Pricing-tier image (`url`, `blurhash`) shown on the product page; `null` when no image is set. */
  image: unknown | null;
  /** Initial purchase price in plan currency. */
  initial_price: number;
  /** Private notes not shown to customers. `null` unless the actor has the `plan:basic:read` scope on the plan's account. */
  internal_notes: string | null;
  /** Invoice this plan was generated for; `null` unless created for an invoice. */
  invoice: unknown | null;
  /** Active memberships through this plan. `null` unless the actor has the `plan:basic:read` scope on the plan's account. */
  member_count: number | null;
  /** Custom key-value pairs stored on the plan. Included in webhook payloads for payment and membership events. Maximum 50 keys, 100 characters per key, 500 characters per value. The reserved keys `custom_cta` and `custom_cta_url`, when set, override the product's checkout call to action for this plan. */
  metadata: unknown | null;
  /** Whether a cancellation discount is offered. `null` unless the actor has the `plan:basic:read` scope on the plan's account. */
  offer_cancel_discount: boolean | null;
  /** Payment method configuration (`enabled`, `disabled`, `include_platform_defaults`); `null` when plan uses default settings. */
  payment_method_configuration: unknown | null;
  /** Billing model for this plan. */
  plan_type: PlanListItemPlanType;
  /** Product this plan belongs to; `null` for standalone plans. */
  product: unknown | null;
  /** URL where customers can purchase this plan directly. */
  purchase_url: string;
  /** Sales method for this plan. */
  release_method: PlanListItemReleaseMethod;
  /** Recurring price charged every billing period. */
  renewal_price: number;
  /** Installment payments required before the subscription pauses. Must be greater than 1. `null` if split pay is not configured. */
  split_pay_required_payments: number | null;
  /** Units available for purchase. `null` unless the actor has the `plan:basic:read` scope on the plan's account. */
  stock: number | null;
  /** Original initial price shown with a strikethrough, in the plan's currency. `null` when no strikethrough is set. */
  strike_through_initial_price: number | null;
  /** Original renewal price shown with a strikethrough, in the plan's currency. `null` when no strikethrough is set. */
  strike_through_renewal_price: number | null;
  /** 3D Secure behavior for this plan; `null` inherits the account default. */
  three_ds_level: PlanListItemThreeDsLevel | null;
  /** Plan display name shown to customers. Maximum 30 characters. `null` if no title has been set. */
  title: string | null;
  /** Free trial days before the first renewal charge. `null` if no trial is configured or the user has already used a trial for this plan. */
  trial_period_days: number | null;
  /** Whether the plan has unlimited stock. When `true`, the `stock` field is ignored; waitlist plans always report `true`. */
  unlimited_stock: boolean;
  /** When the plan was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
  /** Controls where this plan can be seen. When `hidden`, the plan is reachable only by its direct link. */
  visibility: PlanListItemVisibility;
}
export const PlanListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account: S.NullOr(AccountSummary),
    adaptive_pricing_enabled: S.Boolean,
    billing_period: S.NullOr(S.Number),
    cancel_discount_intervals: S.NullOr(S.Number),
    cancel_discount_percentage: S.NullOr(S.Number),
    checkout_styling: S.NullOr(S.Unknown),
    created_at: S.String,
    currency: S.String,
    custom_fields: PlanListItemCustomFieldsList,
    description: S.NullOr(S.String),
    expiration_days: S.NullOr(S.Number),
    formatted_price: S.String,
    id: S.String,
    image: S.NullOr(S.Unknown),
    initial_price: S.Number,
    internal_notes: S.NullOr(S.String),
    invoice: S.NullOr(S.Unknown),
    member_count: S.NullOr(S.Number),
    metadata: S.NullOr(S.Unknown),
    offer_cancel_discount: S.NullOr(S.Boolean),
    payment_method_configuration: S.NullOr(S.Unknown),
    plan_type: PlanListItemPlanType,
    product: S.NullOr(S.Unknown),
    purchase_url: S.String,
    release_method: PlanListItemReleaseMethod,
    renewal_price: S.Number,
    split_pay_required_payments: S.NullOr(S.Number),
    stock: S.NullOr(S.Number),
    strike_through_initial_price: S.NullOr(S.Number),
    strike_through_renewal_price: S.NullOr(S.Number),
    three_ds_level: S.NullOr(PlanListItemThreeDsLevel),
    title: S.NullOr(S.String),
    trial_period_days: S.NullOr(S.Number),
    unlimited_stock: S.Boolean,
    updated_at: S.String,
    visibility: PlanListItemVisibility,
  }),
).annotate({ identifier: "PlanListItem" }) as any as S.Schema<PlanListItem>;

export type ListPlansResponseDataList = Array<PlanListItem>;
export const ListPlansResponseDataList = /*@__PURE__*/ S.Array(
  PlanListItem,
) as any as S.Schema<ListPlansResponseDataList>;

export interface ListPlansResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListPlansResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListPlansResponsePageInfo",
}) as any as S.Schema<ListPlansResponsePageInfo>;

export interface ListPlansResponse {
  data: ListPlansResponseDataList;
  page_info: ListPlansResponsePageInfo;
}
export const ListPlansResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListPlansResponseDataList,
    page_info: ListPlansResponsePageInfo,
  }),
).annotate({
  identifier: "ListPlansResponse",
}) as any as S.Schema<ListPlansResponse>;

export interface RetrievePlanRequest {
  /** Plan ID, prefixed `plan_`. */
  id: string;
}
export const RetrievePlanRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/plans/{id}", code: 200 })),
).annotate({
  identifier: "RetrievePlanRequest",
}) as any as S.Schema<RetrievePlanRequest>;

/** The type of the custom field. */
export type UpdatePlanRequestCustomFieldsItemFieldType = "text";
export const UpdatePlanRequestCustomFieldsItemFieldType =
  /*@__PURE__*/ S.String;

export interface UpdatePlanRequestCustomFieldsItem {
  /** The type of the custom field. */
  field_type?: UpdatePlanRequestCustomFieldsItemFieldType | (string & {});
  /** The ID of the custom field (if being updated). */
  id?: string;
  /** The name of the custom field. */
  name?: string;
  /** The order of the field. */
  order?: number;
  /** An example response displayed in the input field. */
  placeholder?: string | null;
  /** Whether or not the field is required. */
  required?: boolean;
}
export const UpdatePlanRequestCustomFieldsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    field_type: S.optional(UpdatePlanRequestCustomFieldsItemFieldType),
    id: S.optional(S.String),
    name: S.optional(S.String),
    order: S.optional(S.Number),
    placeholder: S.optional(S.NullOr(S.String)),
    required: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "UpdatePlanRequestCustomFieldsItem",
}) as any as S.Schema<UpdatePlanRequestCustomFieldsItem>;

/** An array of custom field definitions to collect from customers at checkout. Omitting this field clears existing custom fields. */
export type UpdatePlanRequestCustomFieldsList =
  Array<UpdatePlanRequestCustomFieldsItem>;
export const UpdatePlanRequestCustomFieldsList = /*@__PURE__*/ S.Array(
  UpdatePlanRequestCustomFieldsItem,
) as any as S.Schema<UpdatePlanRequestCustomFieldsList>;

/** An image displayed on the product page to represent this plan. */
export type UpdatePlanRequestImage = CreatePlanRequestImage;
export const UpdatePlanRequestImage = CreatePlanRequestImage;

export type UpdatePlanRequestPaymentMethodConfigurationDisabledList =
  Array<string>;
export const UpdatePlanRequestPaymentMethodConfigurationDisabledList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdatePlanRequestPaymentMethodConfigurationDisabledList>;

export type UpdatePlanRequestPaymentMethodConfigurationEnabledList =
  Array<string>;
export const UpdatePlanRequestPaymentMethodConfigurationEnabledList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdatePlanRequestPaymentMethodConfigurationEnabledList>;

/** Explicit payment method configuration for the plan. When not provided, the account's defaults apply. */
export interface UpdatePlanRequestPaymentMethodConfiguration {
  disabled?: UpdatePlanRequestPaymentMethodConfigurationDisabledList;
  enabled?: UpdatePlanRequestPaymentMethodConfigurationEnabledList;
  include_platform_defaults?: boolean;
}
export const UpdatePlanRequestPaymentMethodConfiguration =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      disabled: S.optional(
        UpdatePlanRequestPaymentMethodConfigurationDisabledList,
      ),
      enabled: S.optional(
        UpdatePlanRequestPaymentMethodConfigurationEnabledList,
      ),
      include_platform_defaults: S.optional(S.Boolean),
    }),
  ).annotate({
    identifier: "UpdatePlanRequestPaymentMethodConfiguration",
  }) as any as S.Schema<UpdatePlanRequestPaymentMethodConfiguration>;

/** 3D Secure behavior for this plan. Send `null` to inherit the account default. */
export type UpdatePlanRequestThreeDsLevel =
  | "mandate_challenge"
  | "frictionless";
export const UpdatePlanRequestThreeDsLevel = /*@__PURE__*/ S.String;

export interface UpdatePlanRequest {
  /** Plan ID, prefixed `plan_`. */
  id: string;
  /** Whether this plan accepts local currency payments via adaptive pricing. */
  adaptive_pricing_enabled?: boolean | null;
  /** Recurring billing interval in days, such as 30 for monthly or 365 for annual. */
  billing_period?: number | null;
  /** How many renewals the retention discount applies to. Required when `offer_cancel_discount` is true. */
  cancel_discount_intervals?: number | null;
  /** Percentage taken off each discounted renewal. Required when `offer_cancel_discount` is true. */
  cancel_discount_percentage?: number | null;
  /** Checkout styling overrides for this plan. */
  checkout_styling?: unknown | null;
  /** The three-letter ISO currency code for the plan's pricing. Defaults to USD. */
  currency?: string;
  /** An array of custom field definitions to collect from customers at checkout. Omitting this field clears existing custom fields. */
  custom_fields?: UpdatePlanRequestCustomFieldsList | null;
  /** A text description of the plan displayed to customers on the product page. */
  description?: string | null;
  /** Access duration in days before the membership expires. */
  expiration_days?: number | null;
  /** An image displayed on the product page to represent this plan. */
  image?: CreatePlanRequestImage | null;
  /** Initial amount charged in the plan's currency, e.g. 10.43 for $10.43. */
  initial_price?: number | null;
  /** Private notes visible only to the account owner. Not shown to customers. */
  internal_notes?: string | null;
  /** Custom key-value pairs to store on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys `custom_cta` (a checkout call-to-action button label — one of the product custom CTA values, e.g. `subscribe`, `get_offer`) and `custom_cta_url` (a URL the button links to; web or `tel:`) override the product's call to action for this plan and are validated on save. */
  metadata?: unknown | null;
  /** Whether to offer a retention discount when a customer attempts to cancel. */
  offer_cancel_discount?: boolean | null;
  /** Override the default tax classification for this specific plan. */
  override_tax_type?: string;
  /** Explicit payment method configuration for the plan. When not provided, the account's defaults apply. */
  payment_method_configuration?: UpdatePlanRequestPaymentMethodConfiguration | null;
  /** Sales method for this plan. */
  release_method?: string;
  /** The amount charged each billing period for recurring plans, in the plan's currency. */
  renewal_price?: number | null;
  /** The maximum number of units available for purchase. Ignored when unlimited_stock is true. */
  stock?: number | null;
  /** A comparison price displayed with a strikethrough for the initial price. */
  strike_through_initial_price?: number | null;
  /** A comparison price displayed with a strikethrough for the renewal price. */
  strike_through_renewal_price?: number | null;
  /** 3D Secure behavior for this plan. Send `null` to inherit the account default. */
  three_ds_level?: UpdatePlanRequestThreeDsLevel | (string & {}) | null;
  /** The display name of the plan shown to customers on the product page. */
  title?: string | null;
  /** Free trial duration before the first recurring charge. */
  trial_period_days?: number | null;
  /** Whether the plan has unlimited stock. When true, the stock field is ignored. */
  unlimited_stock?: boolean | null;
  /** Whether the plan is visible to customers or hidden from public view. */
  visibility?: string;
}
export const UpdatePlanRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    adaptive_pricing_enabled: S.optional(S.NullOr(S.Boolean)),
    billing_period: S.optional(S.NullOr(S.Number)),
    cancel_discount_intervals: S.optional(S.NullOr(S.Number)),
    cancel_discount_percentage: S.optional(S.NullOr(S.Number)),
    checkout_styling: S.optional(S.NullOr(S.Unknown)),
    currency: S.optional(S.String),
    custom_fields: S.optional(S.NullOr(UpdatePlanRequestCustomFieldsList)),
    description: S.optional(S.NullOr(S.String)),
    expiration_days: S.optional(S.NullOr(S.Number)),
    image: S.optional(S.NullOr(CreatePlanRequestImage)),
    initial_price: S.optional(S.NullOr(S.Number)),
    internal_notes: S.optional(S.NullOr(S.String)),
    metadata: S.optional(S.NullOr(S.Unknown)),
    offer_cancel_discount: S.optional(S.NullOr(S.Boolean)),
    override_tax_type: S.optional(S.String),
    payment_method_configuration: S.optional(
      S.NullOr(UpdatePlanRequestPaymentMethodConfiguration),
    ),
    release_method: S.optional(S.String),
    renewal_price: S.optional(S.NullOr(S.Number)),
    stock: S.optional(S.NullOr(S.Number)),
    strike_through_initial_price: S.optional(S.NullOr(S.Number)),
    strike_through_renewal_price: S.optional(S.NullOr(S.Number)),
    three_ds_level: S.optional(S.NullOr(UpdatePlanRequestThreeDsLevel)),
    title: S.optional(S.NullOr(S.String)),
    trial_period_days: S.optional(S.NullOr(S.Number)),
    unlimited_stock: S.optional(S.NullOr(S.Boolean)),
    visibility: S.optional(S.String),
  }).pipe(T.Http({ method: "PATCH", uri: "/plans/{id}", code: 200 })),
).annotate({
  identifier: "UpdatePlanRequest",
}) as any as S.Schema<UpdatePlanRequest>;

export type CalculatePlanTaxError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Calculate Tax Previews tax for a plan before checkout, based on the buyer's location. */
export const calculatePlanTax: API.OperationMethod<
  CalculatePlanTaxRequest,
  CalculatePlanTaxResponse,
  CalculatePlanTaxError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CalculatePlanTaxRequest,
  output: CalculatePlanTaxResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreatePlanError = BadRequest | Conflict | WhopOpError;
/** Create Plan Create a new pricing plan for a product. The plan defines the billing interval, price, and availability for customers. */
export const createPlan: API.OperationMethod<
  CreatePlanRequest,
  Plan,
  CreatePlanError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreatePlanRequest,
  output: Plan,
  errors: [BadRequest, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeletePlanError = NotFound | WhopOpError;
/** Delete Plan Permanently delete a plan from a product. Existing memberships on this plan will not be affected. */
export const deletePlan: API.OperationMethod<
  DeletePlanRequest,
  DeletePlanResponse,
  DeletePlanError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeletePlanRequest,
  output: DeletePlanResponse,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListPlansError = BadRequest | WhopOpError;
/** List Plans Returns a paginated list of plans. Omit `account_id` and pass `product_ids` to list a product's public buyable plans. */
export const listPlans: API.PaginatedOperationMethod<
  ListPlansRequest,
  ListPlansResponse,
  ListPlansError,
  WhopOpContext,
  PlanListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListPlansRequest,
    output: ListPlansResponse,
    errors: [BadRequest],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrievePlanError = NotFound | WhopOpError;
/** Retrieve Plan Retrieves the details of an existing plan. */
export const retrievePlan: API.OperationMethod<
  RetrievePlanRequest,
  Plan,
  RetrievePlanError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrievePlanRequest,
  output: Plan,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdatePlanError = WhopOpError;
/** Update Plan Update a plan's pricing, billing interval, visibility, stock, and other settings. */
export const updatePlan: API.OperationMethod<
  UpdatePlanRequest,
  Plan,
  UpdatePlanError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdatePlanRequest,
  output: Plan,
  errors: [],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
