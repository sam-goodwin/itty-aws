// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export type CreateWebhookRequestEventsItem =
  | "account.updated"
  | "invoice.created"
  | "invoice.marked_uncollectible"
  | "invoice.paid"
  | "invoice.past_due"
  | "invoice.voided"
  | "membership.activated"
  | "membership.deactivated"
  | "membership.trial_ending_soon"
  | "entry.created"
  | "entry.approved"
  | "entry.denied"
  | "entry.deleted"
  | "export.completed"
  | "export.failed"
  | "setup_intent.requires_action"
  | "setup_intent.succeeded"
  | "setup_intent.canceled"
  | "ledger_account.funds_available"
  | "swap.completed"
  | "deposit.succeeded"
  | "transfer.created"
  | "transfer.completed"
  | "transfer.failed"
  | "payout.created"
  | "payout.updated"
  | "payout.reversed"
  | "card_transaction.created"
  | "card_transaction.updated"
  | "card_transaction.completed"
  | "card_transaction.declined"
  | "card_transaction.reversed"
  | "card.created"
  | "card.updated"
  | "card.frozen"
  | "card.canceled"
  | "card_application.created"
  | "card_application.updated"
  | "card_application.approved"
  | "card_application.denied"
  | "course_lesson_interaction.completed"
  | "payout_method.created"
  | "verification.succeeded"
  | "identity_profile.approved"
  | "identity_profile.rejected"
  | "identity_profile.needs_action"
  | "identity_profile.updated"
  | "payout_account.status_updated"
  | "resolution_center_case.created"
  | "resolution_center_case.updated"
  | "resolution_center_case.decided"
  | "product.created"
  | "product.updated"
  | "product.deleted"
  | "product.published"
  | "product.unpublished"
  | "plan.created"
  | "plan.updated"
  | "plan.deleted"
  | "shipment.created"
  | "shipment.updated"
  | "member.created"
  | "ad_campaign.payment_failed"
  | "chat.message.created"
  | "chat.reaction.created"
  | "payment.created"
  | "payment.succeeded"
  | "payment.failed"
  | "payment.pending"
  | "dispute.created"
  | "dispute.updated"
  | "refund.created"
  | "refund.updated"
  | "dispute_alert.created"
  | "membership.cancel_at_period_end_changed"
  | "membership_went_valid"
  | "membership_went_invalid"
  | "membership_metadata_updated"
  | "resolution_created"
  | "resolution_updated"
  | "resolution_decided"
  | "payment_affiliate_reward_created"
  | "membership_experience_claimed"
  | "app_membership_went_valid"
  | "app_membership_went_invalid"
  | "app_payment_created"
  | "app_payment_succeeded"
  | "app_payment_failed"
  | "app_payment_pending"
  | "app_membership_cancel_at_period_end_changed"
  | "payment_created"
  | "payment_succeeded"
  | "payment_failed"
  | "payment_pending"
  | "dispute_created"
  | "dispute_updated"
  | "refund_created"
  | "refund_updated"
  | "dispute_alert_created"
  | "membership_cancel_at_period_end_changed"
  | "membership.went_valid"
  | "membership.went_invalid"
  | "membership.metadata_updated"
  | "resolution.created"
  | "resolution.updated"
  | "resolution.decided"
  | "payment.affiliate_reward_created"
  | "membership.experience_claimed"
  | "app_membership.went_valid"
  | "app_membership.went_invalid"
  | "app_payment.created"
  | "app_payment.succeeded"
  | "app_payment.failed"
  | "app_payment.pending"
  | "app_membership.cancel_at_period_end_changed";
export const CreateWebhookRequestEventsItem = /*@__PURE__*/ S.String;

/** The events to send the webhook for, in dot form (for example `payment.succeeded`). */
export type CreateWebhookRequestEventsList = Array<
  CreateWebhookRequestEventsItem | (string & {})
>;
export const CreateWebhookRequestEventsList = /*@__PURE__*/ S.Array(
  CreateWebhookRequestEventsItem,
) as any as S.Schema<CreateWebhookRequestEventsList>;

export interface CreateWebhookRequest {
  /** The dated API version (Api-Version-Date) to pin this webhook's payloads to. Omit to leave the webhook unpinned, tracking the current payload shape. */
  api_version_date?: string | null;
  /** Whether to send events for child resources. For example, if the webhook is created for an account, enabling this sends events only from its connected accounts. */
  child_resource_events?: boolean;
  /** Whether or not the webhook is enabled. Defaults to `true`. */
  enabled?: boolean;
  /** The events to send the webhook for, in dot form (for example `payment.succeeded`). */
  events?: CreateWebhookRequestEventsList;
  /** The account or app to create the webhook for. Defaults to the current account. */
  resource_id?: string | null;
  /** The URL to send the webhook to. */
  url: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateWebhookRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    api_version_date: S.optional(S.NullOr(S.String)),
    child_resource_events: S.optional(S.Boolean),
    enabled: S.optional(S.Boolean),
    events: S.optional(CreateWebhookRequestEventsList),
    resource_id: S.optional(S.NullOr(S.String)),
    url: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/webhooks", code: 200 })),
).annotate({
  identifier: "CreateWebhookRequest",
}) as any as S.Schema<CreateWebhookRequest>;

/** The API version used to format payloads sent to this webhook endpoint. */
export type WebhookApiVersion = "v1" | "v2" | "v5";
export const WebhookApiVersion = /*@__PURE__*/ S.String;

/** Why Whop disabled this webhook. `delivery_failures` means every delivery failed for 3 days straight. `null` when `disabled_at` is `null`. */
export type WebhookDisabledReason = "delivery_failures";
export const WebhookDisabledReason = /*@__PURE__*/ S.String;

/** Event types this webhook is subscribed to, in dot form (for example `payment.succeeded`). */
export type WebhookEventsItem =
  | "account.updated"
  | "invoice.created"
  | "invoice.marked_uncollectible"
  | "invoice.paid"
  | "invoice.past_due"
  | "invoice.voided"
  | "membership.activated"
  | "membership.deactivated"
  | "membership.trial_ending_soon"
  | "entry.created"
  | "entry.approved"
  | "entry.denied"
  | "entry.deleted"
  | "export.completed"
  | "export.failed"
  | "setup_intent.requires_action"
  | "setup_intent.succeeded"
  | "setup_intent.canceled"
  | "ledger_account.funds_available"
  | "swap.completed"
  | "deposit.succeeded"
  | "transfer.created"
  | "transfer.completed"
  | "transfer.failed"
  | "withdrawal.created"
  | "withdrawal.updated"
  | "withdrawal.reversed"
  | "payout.created"
  | "payout.updated"
  | "payout.reversed"
  | "card_transaction.created"
  | "card_transaction.updated"
  | "card_transaction.completed"
  | "card_transaction.declined"
  | "card_transaction.reversed"
  | "card.created"
  | "card.updated"
  | "card.frozen"
  | "card.canceled"
  | "card_application.created"
  | "card_application.updated"
  | "card_application.approved"
  | "card_application.denied"
  | "course_lesson_interaction.completed"
  | "payout_method.created"
  | "verification.succeeded"
  | "identity_profile.approved"
  | "identity_profile.rejected"
  | "identity_profile.needs_action"
  | "identity_profile.updated"
  | "payout_account.status_updated"
  | "resolution_center_case.created"
  | "resolution_center_case.updated"
  | "resolution_center_case.decided"
  | "product.created"
  | "product.updated"
  | "product.deleted"
  | "product.published"
  | "product.unpublished"
  | "plan.created"
  | "plan.updated"
  | "plan.deleted"
  | "shipment.created"
  | "shipment.updated"
  | "member.created"
  | "ad_campaign.payment_failed"
  | "chat.message.created"
  | "chat.reaction.created"
  | "payment.created"
  | "payment.succeeded"
  | "payment.failed"
  | "payment.pending"
  | "dispute.created"
  | "dispute.updated"
  | "refund.created"
  | "refund.updated"
  | "dispute_alert.created"
  | "membership.cancel_at_period_end_changed"
  | "membership.went_valid"
  | "membership.went_invalid"
  | "membership.metadata_updated"
  | "resolution.created"
  | "resolution.updated"
  | "resolution.decided"
  | "payment.affiliate_reward_created"
  | "membership.experience_claimed"
  | "app_membership.went_valid"
  | "app_membership.went_invalid"
  | "app_payment.created"
  | "app_payment.succeeded"
  | "app_payment.failed"
  | "app_payment.pending"
  | "app_membership.cancel_at_period_end_changed";
export const WebhookEventsItem = /*@__PURE__*/ S.String;

export type WebhookEventsList = Array<WebhookEventsItem>;
export const WebhookEventsList = /*@__PURE__*/ S.Array(
  WebhookEventsItem,
) as any as S.Schema<WebhookEventsList>;

/** The subset of subscribed event types that support sending test payloads, in dot form. */
export type WebhookTestableEventsItem =
  | "account.updated"
  | "invoice.created"
  | "invoice.marked_uncollectible"
  | "invoice.paid"
  | "invoice.past_due"
  | "invoice.voided"
  | "membership.activated"
  | "membership.deactivated"
  | "membership.trial_ending_soon"
  | "entry.created"
  | "entry.approved"
  | "entry.denied"
  | "entry.deleted"
  | "export.completed"
  | "export.failed"
  | "setup_intent.requires_action"
  | "setup_intent.succeeded"
  | "setup_intent.canceled"
  | "ledger_account.funds_available"
  | "swap.completed"
  | "deposit.succeeded"
  | "transfer.created"
  | "transfer.completed"
  | "transfer.failed"
  | "withdrawal.created"
  | "withdrawal.updated"
  | "withdrawal.reversed"
  | "payout.created"
  | "payout.updated"
  | "payout.reversed"
  | "card_transaction.created"
  | "card_transaction.updated"
  | "card_transaction.completed"
  | "card_transaction.declined"
  | "card_transaction.reversed"
  | "card.created"
  | "card.updated"
  | "card.frozen"
  | "card.canceled"
  | "card_application.created"
  | "card_application.updated"
  | "card_application.approved"
  | "card_application.denied"
  | "course_lesson_interaction.completed"
  | "payout_method.created"
  | "verification.succeeded"
  | "identity_profile.approved"
  | "identity_profile.rejected"
  | "identity_profile.needs_action"
  | "identity_profile.updated"
  | "payout_account.status_updated"
  | "resolution_center_case.created"
  | "resolution_center_case.updated"
  | "resolution_center_case.decided"
  | "product.created"
  | "product.updated"
  | "product.deleted"
  | "product.published"
  | "product.unpublished"
  | "plan.created"
  | "plan.updated"
  | "plan.deleted"
  | "shipment.created"
  | "shipment.updated"
  | "member.created"
  | "ad_campaign.payment_failed"
  | "chat.message.created"
  | "chat.reaction.created"
  | "payment.created"
  | "payment.succeeded"
  | "payment.failed"
  | "payment.pending"
  | "dispute.created"
  | "dispute.updated"
  | "refund.created"
  | "refund.updated"
  | "dispute_alert.created"
  | "membership.cancel_at_period_end_changed"
  | "membership.went_valid"
  | "membership.went_invalid"
  | "membership.metadata_updated"
  | "resolution.created"
  | "resolution.updated"
  | "resolution.decided"
  | "payment.affiliate_reward_created"
  | "membership.experience_claimed"
  | "app_membership.went_valid"
  | "app_membership.went_invalid"
  | "app_payment.created"
  | "app_payment.succeeded"
  | "app_payment.failed"
  | "app_payment.pending"
  | "app_membership.cancel_at_period_end_changed";
export const WebhookTestableEventsItem = /*@__PURE__*/ S.String;

export type WebhookTestableEventsList = Array<WebhookTestableEventsItem>;
export const WebhookTestableEventsList = /*@__PURE__*/ S.Array(
  WebhookTestableEventsItem,
) as any as S.Schema<WebhookTestableEventsList>;

export interface Webhook {
  /** The API version used to format payloads sent to this webhook endpoint. */
  api_version: WebhookApiVersion;
  /** The dated API version (Api-Version-Date) that v1 payloads for this endpoint are pinned to: events serialize exactly like a REST read at this version (the native serializer where the resource has one). Null when unpinned — legacy (v2/v5) webhooks, and v1 webhooks on the legacy payload shape. */
  api_version_date: string | null;
  /** Whether events are sent for child resources. For example, if the webhook is on an account, enabling this sends events only from its connected accounts. */
  child_resource_events: boolean;
  /** Number of consecutive deliveries whose first attempt to this endpoint failed since it last accepted one. Later retries of the same delivery do not increment it. Resets to `0` when a delivery succeeds or the webhook is re-enabled. */
  consecutive_failures: number;
  /** When the webhook was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** When Whop automatically disabled this webhook, as an ISO 8601 timestamp. `null` unless the webhook was disabled by Whop; a webhook you disabled yourself has `enabled: false` and a `null` `disabled_at`. */
  disabled_at: string | null;
  /** Why Whop disabled this webhook. `delivery_failures` means every delivery failed for 3 days straight. `null` when `disabled_at` is `null`. */
  disabled_reason: WebhookDisabledReason | null;
  /** Whether this webhook endpoint is currently active and receiving events. */
  enabled: boolean;
  events: WebhookEventsList;
  /** When the current failure streak began, as an ISO 8601 timestamp. Unlike `last_failure_at`, this is set on the streak's first failed attempt, so it shows an endpoint that is failing right now. `null` when the endpoint is healthy. */
  failing_since: string | null;
  /** Webhook ID, prefixed `hook_`. */
  id: string;
  /** When a delivery to this endpoint most recently failed after exhausting retries, as an ISO 8601 timestamp. `null` if no delivery has ever failed. */
  last_failure_at: string | null;
  /** ID of the resource (account or app) this webhook is attached to. */
  resource_id: string;
  testable_events: WebhookTestableEventsList;
  /** Destination URL where webhook payloads are delivered via HTTP POST. */
  url: string;
  /** Secret key used to sign webhook payloads for verification. Include this in your HMAC validation logic. Returned on the create response and to interactive dashboard sessions; `null` for API-key and OAuth callers on later reads. */
  webhook_secret: string | Redacted.Redacted<string> | null;
}
export const Webhook = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    api_version: WebhookApiVersion,
    api_version_date: S.NullOr(S.String),
    child_resource_events: S.Boolean,
    consecutive_failures: S.Number,
    created_at: S.String,
    disabled_at: S.NullOr(S.String),
    disabled_reason: S.NullOr(WebhookDisabledReason),
    enabled: S.Boolean,
    events: WebhookEventsList,
    failing_since: S.NullOr(S.String),
    id: S.String,
    last_failure_at: S.NullOr(S.String),
    resource_id: S.String,
    testable_events: WebhookTestableEventsList,
    url: S.String,
    webhook_secret: S.NullOr(S.String).pipe(T.SensitiveValue({})),
  }),
).annotate({ identifier: "Webhook" }) as any as S.Schema<Webhook>;

export interface DeleteWebhookRequest {
  /** Webhook ID, prefixed `hook_`. */
  id: string;
}
export const DeleteWebhookRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/webhooks/{id}", code: 200 })),
).annotate({
  identifier: "DeleteWebhookRequest",
}) as any as S.Schema<DeleteWebhookRequest>;

export interface DeleteWebhookResponse {
  /** Always `true`: the resource was deleted. */
  deleted: boolean;
  /** The ID of the deleted resource. */
  id: string;
}
export const DeleteWebhookResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "DeleteWebhookResponse",
}) as any as S.Schema<DeleteWebhookResponse>;

export interface DeliveriesWebhookRequest {
  /** The unique identifier of the webhook to list deliveries for. */
  webhook_id: string;
  after?: string;
  before?: string;
  first?: number;
  last?: number;
}
export const DeliveriesWebhookRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    webhook_id: S.String.pipe(T.Label()),
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/webhooks/{webhook_id}/deliveries",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeliveriesWebhookRequest",
}) as any as S.Schema<DeliveriesWebhookRequest>;

/** The request body sent to the webhook endpoint */
export type DeliveriesWebhookResponseDataItemRequestBodyMap = {
  [key: string]: unknown | undefined;
};
export const DeliveriesWebhookResponseDataItemRequestBodyMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<DeliveriesWebhookResponseDataItemRequestBodyMap>;

/** The response body received from the webhook endpoint */
export type DeliveriesWebhookResponseDataItemResponseBodyMap = {
  [key: string]: unknown | undefined;
};
export const DeliveriesWebhookResponseDataItemResponseBodyMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<DeliveriesWebhookResponseDataItemResponseBodyMap>;

/** A webhook log entry containing the request and response details */
export interface DeliveriesWebhookResponseDataItem {
  /** The request body sent to the webhook endpoint */
  request_body: DeliveriesWebhookResponseDataItemRequestBodyMap;
  /** The ID of the resource that triggered the webhook */
  resource_id: string;
  /** The response body received from the webhook endpoint */
  response_body: DeliveriesWebhookResponseDataItemResponseBodyMap;
  /** The HTTP response code received from the webhook endpoint */
  response_code: number;
  /** The timestamp when the webhook was sent */
  sent_at: string;
  /** The total time taken to send the webhook request in seconds */
  total_time: number;
}
export const DeliveriesWebhookResponseDataItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    request_body: DeliveriesWebhookResponseDataItemRequestBodyMap,
    resource_id: S.String,
    response_body: DeliveriesWebhookResponseDataItemResponseBodyMap,
    response_code: S.Number,
    sent_at: S.String,
    total_time: S.Number,
  }),
).annotate({
  identifier: "DeliveriesWebhookResponseDataItem",
}) as any as S.Schema<DeliveriesWebhookResponseDataItem>;

/** A list of nodes. */
export type DeliveriesWebhookResponseDataList =
  Array<DeliveriesWebhookResponseDataItem>;
export const DeliveriesWebhookResponseDataList = /*@__PURE__*/ S.Array(
  DeliveriesWebhookResponseDataItem,
) as any as S.Schema<DeliveriesWebhookResponseDataList>;

/** Information to aid in pagination. */
export interface DeliveriesWebhookResponsePageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
}
export const DeliveriesWebhookResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
  }),
).annotate({
  identifier: "DeliveriesWebhookResponsePageInfo",
}) as any as S.Schema<DeliveriesWebhookResponsePageInfo>;

export interface DeliveriesWebhookResponse {
  /** A list of nodes. */
  data: DeliveriesWebhookResponseDataList;
  /** Information to aid in pagination. */
  page_info: DeliveriesWebhookResponsePageInfo;
}
export const DeliveriesWebhookResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: DeliveriesWebhookResponseDataList,
    page_info: DeliveriesWebhookResponsePageInfo,
  }),
).annotate({
  identifier: "DeliveriesWebhookResponse",
}) as any as S.Schema<DeliveriesWebhookResponse>;

export interface ListWebhookDeliveriesRequest {
  /** Webhook ID, prefixed `hook_`. */
  id: string;
  /** The number of deliveries to return (default 50, max 100). */
  first?: number;
  /** A cursor; returns deliveries after this position. */
  after?: string;
}
export const ListWebhookDeliveriesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/webhooks/{id}/deliveries", code: 200 }),
  ),
).annotate({
  identifier: "ListWebhookDeliveriesRequest",
}) as any as S.Schema<ListWebhookDeliveriesRequest>;

export interface WebhookDelivery {
  /** The event type this delivery carried, for example `payment.succeeded`. */
  event: string | null;
  /** Unique identifier for this delivery attempt. Pass it to the replay endpoint to re-send this exact payload. */
  id: string;
  /** The id of the delivery attempt this one replayed. `null` for an original delivery. */
  replayed_from: string | null;
  /** The JSON event payload sent to the webhook endpoint. */
  request_body: unknown;
  /** ID of the resource that triggered the webhook. */
  resource_id: string;
  /** The endpoint's JSON response. A non-JSON response is stored as `{ error, raw_body }` with the first 100 bytes. */
  response_body: unknown | null;
  /** HTTP response code received from the webhook endpoint. */
  response_code: number;
  /** When the webhook was sent, as an ISO 8601 timestamp. */
  sent_at: string;
  /** Whether the endpoint acknowledged this attempt with a 2xx response. */
  success: boolean;
  /** Total time taken to send the webhook request, in seconds. */
  total_time: number;
}
export const WebhookDelivery = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    event: S.NullOr(S.String),
    id: S.String,
    replayed_from: S.NullOr(S.String),
    request_body: S.Unknown,
    resource_id: S.String,
    response_body: S.NullOr(S.Unknown),
    response_code: S.Number,
    sent_at: S.String,
    success: S.Boolean,
    total_time: S.Number,
  }),
).annotate({
  identifier: "WebhookDelivery",
}) as any as S.Schema<WebhookDelivery>;

export type ListWebhookDeliveriesResponseDataList = Array<WebhookDelivery>;
export const ListWebhookDeliveriesResponseDataList = /*@__PURE__*/ S.Array(
  WebhookDelivery,
) as any as S.Schema<ListWebhookDeliveriesResponseDataList>;

export interface ListWebhookDeliveriesResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListWebhookDeliveriesResponsePageInfo = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      end_cursor: S.NullOr(S.String),
      has_next_page: S.Boolean,
      has_previous_page: S.Boolean,
      start_cursor: S.NullOr(S.String),
    }),
).annotate({
  identifier: "ListWebhookDeliveriesResponsePageInfo",
}) as any as S.Schema<ListWebhookDeliveriesResponsePageInfo>;

export interface ListWebhookDeliveriesResponse {
  data: ListWebhookDeliveriesResponseDataList;
  page_info: ListWebhookDeliveriesResponsePageInfo;
}
export const ListWebhookDeliveriesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListWebhookDeliveriesResponseDataList,
    page_info: ListWebhookDeliveriesResponsePageInfo,
  }),
).annotate({
  identifier: "ListWebhookDeliveriesResponse",
}) as any as S.Schema<ListWebhookDeliveriesResponse>;

export interface ListWebhooksRequest {
  /** The unique identifier of the account to list webhooks for. */
  account_id: string;
  /** Only return webhooks attached to this app. Omit to list the account's own webhooks. */
  app_id?: string;
  /** Also return webhooks attached to the account's apps, not just the account's own. Cannot be combined with `app_id`. */
  include_app_webhooks?: boolean;
  /** Only return webhooks whose endpoint is currently failing — every delivery since the current failure streak began has been rejected. Clears as soon as a delivery succeeds. */
  has_failures?: boolean;
  /** The number of webhooks to return (default 20, max 100). */
  first?: number;
  /** A cursor; returns webhooks after this position. */
  after?: string;
  /** The number of webhooks to return from the end of the range. */
  last?: number;
  /** A cursor; returns webhooks before this position. */
  before?: string;
}
export const ListWebhooksRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String.pipe(T.Query()),
    app_id: S.optional(S.String.pipe(T.Query())),
    include_app_webhooks: S.optional(S.Boolean.pipe(T.Query())),
    has_failures: S.optional(S.Boolean.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/webhooks", code: 200 })),
).annotate({
  identifier: "ListWebhooksRequest",
}) as any as S.Schema<ListWebhooksRequest>;

/** The API version used to format payloads sent to this webhook endpoint. */
export type WebhookListItemApiVersion = "v1" | "v2" | "v5";
export const WebhookListItemApiVersion = /*@__PURE__*/ S.String;

/** Why Whop disabled this webhook. `delivery_failures` means every delivery failed for 3 days straight. `null` when `disabled_at` is `null`. */
export type WebhookListItemDisabledReason = "delivery_failures";
export const WebhookListItemDisabledReason = /*@__PURE__*/ S.String;

/** Event types this webhook is subscribed to, in dot form (for example `payment.succeeded`). */
export type WebhookListItemEventsItem =
  | "account.updated"
  | "invoice.created"
  | "invoice.marked_uncollectible"
  | "invoice.paid"
  | "invoice.past_due"
  | "invoice.voided"
  | "membership.activated"
  | "membership.deactivated"
  | "membership.trial_ending_soon"
  | "entry.created"
  | "entry.approved"
  | "entry.denied"
  | "entry.deleted"
  | "export.completed"
  | "export.failed"
  | "setup_intent.requires_action"
  | "setup_intent.succeeded"
  | "setup_intent.canceled"
  | "ledger_account.funds_available"
  | "swap.completed"
  | "deposit.succeeded"
  | "transfer.created"
  | "transfer.completed"
  | "transfer.failed"
  | "withdrawal.created"
  | "withdrawal.updated"
  | "withdrawal.reversed"
  | "payout.created"
  | "payout.updated"
  | "payout.reversed"
  | "card_transaction.created"
  | "card_transaction.updated"
  | "card_transaction.completed"
  | "card_transaction.declined"
  | "card_transaction.reversed"
  | "card.created"
  | "card.updated"
  | "card.frozen"
  | "card.canceled"
  | "card_application.created"
  | "card_application.updated"
  | "card_application.approved"
  | "card_application.denied"
  | "course_lesson_interaction.completed"
  | "payout_method.created"
  | "verification.succeeded"
  | "identity_profile.approved"
  | "identity_profile.rejected"
  | "identity_profile.needs_action"
  | "identity_profile.updated"
  | "payout_account.status_updated"
  | "resolution_center_case.created"
  | "resolution_center_case.updated"
  | "resolution_center_case.decided"
  | "product.created"
  | "product.updated"
  | "product.deleted"
  | "product.published"
  | "product.unpublished"
  | "plan.created"
  | "plan.updated"
  | "plan.deleted"
  | "shipment.created"
  | "shipment.updated"
  | "member.created"
  | "ad_campaign.payment_failed"
  | "chat.message.created"
  | "chat.reaction.created"
  | "payment.created"
  | "payment.succeeded"
  | "payment.failed"
  | "payment.pending"
  | "dispute.created"
  | "dispute.updated"
  | "refund.created"
  | "refund.updated"
  | "dispute_alert.created"
  | "membership.cancel_at_period_end_changed"
  | "membership.went_valid"
  | "membership.went_invalid"
  | "membership.metadata_updated"
  | "resolution.created"
  | "resolution.updated"
  | "resolution.decided"
  | "payment.affiliate_reward_created"
  | "membership.experience_claimed"
  | "app_membership.went_valid"
  | "app_membership.went_invalid"
  | "app_payment.created"
  | "app_payment.succeeded"
  | "app_payment.failed"
  | "app_payment.pending"
  | "app_membership.cancel_at_period_end_changed";
export const WebhookListItemEventsItem = /*@__PURE__*/ S.String;

export type WebhookListItemEventsList = Array<WebhookListItemEventsItem>;
export const WebhookListItemEventsList = /*@__PURE__*/ S.Array(
  WebhookListItemEventsItem,
) as any as S.Schema<WebhookListItemEventsList>;

export interface WebhookListItem {
  /** The API version used to format payloads sent to this webhook endpoint. */
  api_version: WebhookListItemApiVersion;
  /** The dated API version (Api-Version-Date) that v1 payloads for this endpoint are pinned to: events serialize exactly like a REST read at this version (the native serializer where the resource has one). Null when unpinned — legacy (v2/v5) webhooks, and v1 webhooks on the legacy payload shape. */
  api_version_date: string | null;
  /** Whether events are sent for child resources. For example, if the webhook is on an account, enabling this sends events only from its connected accounts. */
  child_resource_events: boolean;
  /** Number of consecutive deliveries whose first attempt to this endpoint failed since it last accepted one. Later retries of the same delivery do not increment it. Resets to `0` when a delivery succeeds or the webhook is re-enabled. */
  consecutive_failures: number;
  /** When the webhook was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** When Whop automatically disabled this webhook, as an ISO 8601 timestamp. `null` unless the webhook was disabled by Whop; a webhook you disabled yourself has `enabled: false` and a `null` `disabled_at`. */
  disabled_at: string | null;
  /** Why Whop disabled this webhook. `delivery_failures` means every delivery failed for 3 days straight. `null` when `disabled_at` is `null`. */
  disabled_reason: WebhookListItemDisabledReason | null;
  /** Whether this webhook endpoint is currently active and receiving events. */
  enabled: boolean;
  events: WebhookListItemEventsList;
  /** When the current failure streak began, as an ISO 8601 timestamp. Unlike `last_failure_at`, this is set on the streak's first failed attempt, so it shows an endpoint that is failing right now. `null` when the endpoint is healthy. */
  failing_since: string | null;
  /** Webhook ID, prefixed `hook_`. */
  id: string;
  /** When a delivery to this endpoint most recently failed after exhausting retries, as an ISO 8601 timestamp. `null` if no delivery has ever failed. */
  last_failure_at: string | null;
  /** ID of the resource (account or app) this webhook is attached to. */
  resource_id: string;
  /** Destination URL where webhook payloads are delivered via HTTP POST. */
  url: string;
  /** Secret key used to sign webhook payloads for verification. Include this in your HMAC validation logic. Returned on the create response and to interactive dashboard sessions; `null` for API-key and OAuth callers on later reads. */
  webhook_secret: string | Redacted.Redacted<string> | null;
}
export const WebhookListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    api_version: WebhookListItemApiVersion,
    api_version_date: S.NullOr(S.String),
    child_resource_events: S.Boolean,
    consecutive_failures: S.Number,
    created_at: S.String,
    disabled_at: S.NullOr(S.String),
    disabled_reason: S.NullOr(WebhookListItemDisabledReason),
    enabled: S.Boolean,
    events: WebhookListItemEventsList,
    failing_since: S.NullOr(S.String),
    id: S.String,
    last_failure_at: S.NullOr(S.String),
    resource_id: S.String,
    url: S.String,
    webhook_secret: S.NullOr(S.String).pipe(T.SensitiveValue({})),
  }),
).annotate({
  identifier: "WebhookListItem",
}) as any as S.Schema<WebhookListItem>;

export type ListWebhooksResponseDataList = Array<WebhookListItem>;
export const ListWebhooksResponseDataList = /*@__PURE__*/ S.Array(
  WebhookListItem,
) as any as S.Schema<ListWebhooksResponseDataList>;

export type ListWebhooksResponsePageInfo =
  ListWebhookDeliveriesResponsePageInfo;
export const ListWebhooksResponsePageInfo =
  ListWebhookDeliveriesResponsePageInfo;

export interface ListWebhooksResponse {
  data: ListWebhooksResponseDataList;
  page_info: ListWebhookDeliveriesResponsePageInfo;
}
export const ListWebhooksResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListWebhooksResponseDataList,
    page_info: ListWebhookDeliveriesResponsePageInfo,
  }),
).annotate({
  identifier: "ListWebhooksResponse",
}) as any as S.Schema<ListWebhooksResponse>;

/** Only replay these event types, in dot form (for example `payment.succeeded`). Omit to include every event. */
export type ReplayWebhookRequestEventsList = Array<string>;
export const ReplayWebhookRequestEventsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ReplayWebhookRequestEventsList>;

export interface ReplayWebhookRequest {
  /** Webhook ID, prefixed `hook_`. */
  id: string;
  /** Only replay these event types, in dot form (for example `payment.succeeded`). Omit to include every event. */
  events?: ReplayWebhookRequestEventsList;
  /** Only replay messages whose most recent delivery attempt in the window failed. Defaults to false. Best-effort: a message whose attempts span processing batches can still be re-sent — replays keep the original `webhook-id` by default, so consumers that deduplicate are unaffected. */
  failed_only?: boolean;
  /** Re-send each replayed message under a freshly generated `webhook-id` (in both the envelope and the signed headers) instead of its original one. Defaults to false. Use this when your endpoint deduplicates on `webhook-id` and you want it to process the replays as new messages. */
  regenerate_ids?: boolean;
  /** Start of the delivery window to replay, as an ISO 8601 timestamp. Clamped to the 30-day delivery retention. */
  sent_after: string;
  /** End of the delivery window to replay, as an ISO 8601 timestamp. Defaults to now. */
  sent_before?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const ReplayWebhookRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    events: S.optional(ReplayWebhookRequestEventsList),
    failed_only: S.optional(S.Boolean),
    regenerate_ids: S.optional(S.Boolean),
    sent_after: S.String,
    sent_before: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/webhooks/{id}/replay", code: 200 })),
).annotate({
  identifier: "ReplayWebhookRequest",
}) as any as S.Schema<ReplayWebhookRequest>;

export interface ReplayWebhookResponse {
  /** Whether the replay was accepted. Watch the webhook's delivery log for the re-sends. */
  queued: boolean;
}
export const ReplayWebhookResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    queued: S.Boolean,
  }),
).annotate({
  identifier: "ReplayWebhookResponse",
}) as any as S.Schema<ReplayWebhookResponse>;

export interface ReplayWebhookDeliveryRequest {
  /** Webhook ID, prefixed `hook_`. */
  id: string;
  /** Delivery ID, prefixed `whdel_`, from the List Deliveries endpoint. */
  delivery_id: string;
  /** Re-send the delivery under a freshly generated `webhook-id` (in both the envelope and the signed headers) instead of the original one. Defaults to false. Use this when your endpoint deduplicates on `webhook-id` and you want it to process the replay as a new message. */
  regenerate_id?: boolean;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const ReplayWebhookDeliveryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    delivery_id: S.String.pipe(T.Label()),
    regenerate_id: S.optional(S.Boolean),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/webhooks/{id}/deliveries/{delivery_id}/replay",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ReplayWebhookDeliveryRequest",
}) as any as S.Schema<ReplayWebhookDeliveryRequest>;

export interface ReplayWebhookDeliveryResponse {
  /** The body your endpoint returned for the replayed request, as raw text. Empty when the endpoint could not be reached. */
  body: string;
  /** The HTTP response code your endpoint returned for the replayed request, or 0 when it could not be reached (timeout, DNS, or connection failure). */
  status: number;
  /** Whether your endpoint acknowledged the replay with a 2xx response. */
  success: boolean;
}
export const ReplayWebhookDeliveryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: S.String,
    status: S.Number,
    success: S.Boolean,
  }),
).annotate({
  identifier: "ReplayWebhookDeliveryResponse",
}) as any as S.Schema<ReplayWebhookDeliveryResponse>;

export interface RetrieveWebhookRequest {
  /** Webhook ID, prefixed `hook_`. */
  id: string;
}
export const RetrieveWebhookRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/webhooks/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveWebhookRequest",
}) as any as S.Schema<RetrieveWebhookRequest>;

export interface TestWebhookRequest {
  /** Webhook ID, prefixed `hook_`. */
  id: string;
  /** The event to test the webhook for, in dot form (for example `payment.succeeded`). */
  event: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const TestWebhookRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    event: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/webhooks/{id}/test", code: 200 })),
).annotate({
  identifier: "TestWebhookRequest",
}) as any as S.Schema<TestWebhookRequest>;

export interface TestWebhookResponse {
  /** The body of the webhook response. */
  body: unknown;
  /** The HTTP response code of this request. */
  status: number;
  /** Whether or not the webhook test was successful. */
  success: boolean;
}
export const TestWebhookResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: S.Unknown,
    status: S.Number,
    success: S.Boolean,
  }),
).annotate({
  identifier: "TestWebhookResponse",
}) as any as S.Schema<TestWebhookResponse>;

export type UpdateWebhookRequestEventsItem =
  | "account.updated"
  | "invoice.created"
  | "invoice.marked_uncollectible"
  | "invoice.paid"
  | "invoice.past_due"
  | "invoice.voided"
  | "membership.activated"
  | "membership.deactivated"
  | "membership.trial_ending_soon"
  | "entry.created"
  | "entry.approved"
  | "entry.denied"
  | "entry.deleted"
  | "export.completed"
  | "export.failed"
  | "setup_intent.requires_action"
  | "setup_intent.succeeded"
  | "setup_intent.canceled"
  | "ledger_account.funds_available"
  | "swap.completed"
  | "deposit.succeeded"
  | "transfer.created"
  | "transfer.completed"
  | "transfer.failed"
  | "payout.created"
  | "payout.updated"
  | "payout.reversed"
  | "card_transaction.created"
  | "card_transaction.updated"
  | "card_transaction.completed"
  | "card_transaction.declined"
  | "card_transaction.reversed"
  | "card.created"
  | "card.updated"
  | "card.frozen"
  | "card.canceled"
  | "card_application.created"
  | "card_application.updated"
  | "card_application.approved"
  | "card_application.denied"
  | "course_lesson_interaction.completed"
  | "payout_method.created"
  | "verification.succeeded"
  | "identity_profile.approved"
  | "identity_profile.rejected"
  | "identity_profile.needs_action"
  | "identity_profile.updated"
  | "payout_account.status_updated"
  | "resolution_center_case.created"
  | "resolution_center_case.updated"
  | "resolution_center_case.decided"
  | "product.created"
  | "product.updated"
  | "product.deleted"
  | "product.published"
  | "product.unpublished"
  | "plan.created"
  | "plan.updated"
  | "plan.deleted"
  | "shipment.created"
  | "shipment.updated"
  | "member.created"
  | "ad_campaign.payment_failed"
  | "chat.message.created"
  | "chat.reaction.created"
  | "payment.created"
  | "payment.succeeded"
  | "payment.failed"
  | "payment.pending"
  | "dispute.created"
  | "dispute.updated"
  | "refund.created"
  | "refund.updated"
  | "dispute_alert.created"
  | "membership.cancel_at_period_end_changed"
  | "membership_went_valid"
  | "membership_went_invalid"
  | "membership_metadata_updated"
  | "resolution_created"
  | "resolution_updated"
  | "resolution_decided"
  | "payment_affiliate_reward_created"
  | "membership_experience_claimed"
  | "app_membership_went_valid"
  | "app_membership_went_invalid"
  | "app_payment_created"
  | "app_payment_succeeded"
  | "app_payment_failed"
  | "app_payment_pending"
  | "app_membership_cancel_at_period_end_changed"
  | "payment_created"
  | "payment_succeeded"
  | "payment_failed"
  | "payment_pending"
  | "dispute_created"
  | "dispute_updated"
  | "refund_created"
  | "refund_updated"
  | "dispute_alert_created"
  | "membership_cancel_at_period_end_changed"
  | "membership.went_valid"
  | "membership.went_invalid"
  | "membership.metadata_updated"
  | "resolution.created"
  | "resolution.updated"
  | "resolution.decided"
  | "payment.affiliate_reward_created"
  | "membership.experience_claimed"
  | "app_membership.went_valid"
  | "app_membership.went_invalid"
  | "app_payment.created"
  | "app_payment.succeeded"
  | "app_payment.failed"
  | "app_payment.pending"
  | "app_membership.cancel_at_period_end_changed";
export const UpdateWebhookRequestEventsItem = /*@__PURE__*/ S.String;

/** The events to send the webhook for, in dot form (for example `payment.succeeded`). */
export type UpdateWebhookRequestEventsList = Array<
  UpdateWebhookRequestEventsItem | (string & {})
>;
export const UpdateWebhookRequestEventsList = /*@__PURE__*/ S.Array(
  UpdateWebhookRequestEventsItem,
) as any as S.Schema<UpdateWebhookRequestEventsList>;

export interface UpdateWebhookRequest {
  /** Webhook ID, prefixed `hook_`. */
  id: string;
  /** The dated API version (Api-Version-Date) to pin this webhook's payloads to. Only valid for `v1` webhooks. Omit to leave the current pin unchanged, or pass `null` to unpin and track the current payload shape. */
  api_version_date?: string | null;
  /** Whether or not to send events for child resources. */
  child_resource_events?: boolean;
  /** Whether or not the webhook is enabled. */
  enabled?: boolean;
  /** The events to send the webhook for, in dot form (for example `payment.succeeded`). */
  events?: UpdateWebhookRequestEventsList;
  /** The URL to send the webhook to. */
  url?: string;
}
export const UpdateWebhookRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    api_version_date: S.optional(S.NullOr(S.String)),
    child_resource_events: S.optional(S.Boolean),
    enabled: S.optional(S.Boolean),
    events: S.optional(UpdateWebhookRequestEventsList),
    url: S.optional(S.String),
  }).pipe(T.Http({ method: "PATCH", uri: "/webhooks/{id}", code: 200 })),
).annotate({
  identifier: "UpdateWebhookRequest",
}) as any as S.Schema<UpdateWebhookRequest>;

export type CreateWebhookError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Create Webhook Creates a webhook endpoint that receives event notifications via HTTP POST. */
export const createWebhook: API.OperationMethod<
  CreateWebhookRequest,
  Webhook,
  CreateWebhookError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateWebhookRequest,
  output: Webhook,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteWebhookError = Forbidden | NotFound | WhopOpError;
/** Delete Webhook Permanently deletes a webhook endpoint. Returns `true` on success, matching the legacy proxy response. */
export const deleteWebhook: API.OperationMethod<
  DeleteWebhookRequest,
  DeleteWebhookResponse,
  DeleteWebhookError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteWebhookRequest,
  output: DeleteWebhookResponse,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeliveriesWebhookError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List deliveries [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of delivery attempts for a webhook, ordered by most recent first. Includes the request payload, response body, response code, and timing for each attempt. Required permissions: - `developer:manage_webhook` */
export const deliveriesWebhook: API.PaginatedOperationMethod<
  DeliveriesWebhookRequest,
  DeliveriesWebhookResponse,
  DeliveriesWebhookError,
  WhopOpContext,
  DeliveriesWebhookResponseDataItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: DeliveriesWebhookRequest,
    output: DeliveriesWebhookResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ListWebhookDeliveriesError = Forbidden | NotFound | WhopOpError;
/** List Deliveries Returns a paginated list of delivery attempts for a webhook, ordered by most recent first. Includes the request payload, response body, response code, and timing for each attempt. */
export const listWebhookDeliveries: API.PaginatedOperationMethod<
  ListWebhookDeliveriesRequest,
  ListWebhookDeliveriesResponse,
  ListWebhookDeliveriesError,
  WhopOpContext,
  WebhookDelivery
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListWebhookDeliveriesRequest,
    output: ListWebhookDeliveriesResponse,
    errors: [Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ListWebhooksError = BadRequest | Forbidden | NotFound | WhopOpError;
/** List Webhooks Returns a paginated list of webhook endpoints configured for an account, ordered by most recently created. */
export const listWebhooks: API.PaginatedOperationMethod<
  ListWebhooksRequest,
  ListWebhooksResponse,
  ListWebhooksError,
  WhopOpContext,
  WebhookListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListWebhooksRequest,
    output: ListWebhooksResponse,
    errors: [BadRequest, Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type ReplayWebhookError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Replay Deliveries in a Range Re-sends the webhook's past deliveries within a time window, optionally limited to specific events or to messages whose most recent delivery attempt failed. Fire and forget: nothing about the replay is stored, and each re-send appears as a new entry in the webhook's delivery log. Each matching message is re-sent once, by default with its original `webhook-id`, so consumers that deduplicate are unaffected; pass `regenerate_ids` to re-send under freshly generated ids instead. Only available for enabled webhooks on API version v1; deliveries are retained for 30 days. */
export const replayWebhook: API.OperationMethod<
  ReplayWebhookRequest,
  ReplayWebhookResponse,
  ReplayWebhookError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReplayWebhookRequest,
  output: ReplayWebhookResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ReplayWebhookDeliveryError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Replay Delivery Re-sends the exact payload of a past delivery to the webhook's current URL and returns the delivery result. By default the replay keeps the original `webhook-id`, so consumers that deduplicate on it can drop events they already processed. Pass `regenerate_id` to re-send under a freshly generated `webhook-id` instead, so a deduplicating consumer processes the replay as a new message. Only available for enabled webhooks on API version v1; deliveries are retained for 30 days. */
export const replayWebhookDelivery: API.OperationMethod<
  ReplayWebhookDeliveryRequest,
  ReplayWebhookDeliveryResponse,
  ReplayWebhookDeliveryError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReplayWebhookDeliveryRequest,
  output: ReplayWebhookDeliveryResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveWebhookError = Forbidden | NotFound | WhopOpError;
/** Retrieve Webhook Retrieves the details of an existing webhook. */
export const retrieveWebhook: API.OperationMethod<
  RetrieveWebhookRequest,
  Webhook,
  RetrieveWebhookError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveWebhookRequest,
  output: Webhook,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type TestWebhookError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Send Test Event Sends a sample payload for the given event to the webhook's URL and returns the delivery result. */
export const testWebhook: API.OperationMethod<
  TestWebhookRequest,
  TestWebhookResponse,
  TestWebhookError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: TestWebhookRequest,
  output: TestWebhookResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateWebhookError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** Update Webhook Updates a webhook endpoint's URL, subscribed events, pinned payload version, or enabled state. */
export const updateWebhook: API.OperationMethod<
  UpdateWebhookRequest,
  Webhook,
  UpdateWebhookError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateWebhookRequest,
  output: Webhook,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
