// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface CreateReactionRequest {
  /** The emoji to react with, in shortcode or unicode format. For example, ':heart:' or a unicode emoji. Ignored in forums where reactions are always likes. */
  emoji?: string | null;
  /** The unique identifier of a poll option to vote for. Only valid when the target message or post contains a poll. */
  poll_option_id?: string | null;
  /** The unique identifier of the message or forum post to react to. */
  resource_id: string;
}
export const CreateReactionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    emoji: S.optional(S.NullOr(S.String)),
    poll_option_id: S.optional(S.NullOr(S.String)),
    resource_id: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/reactions", code: 200 })),
).annotate({
  identifier: "CreateReactionRequest",
}) as any as S.Schema<CreateReactionRequest>;

/** The user who left this reaction on the post. */
export interface ReactionUser {
  /** The unique identifier for the user. */
  id: string;
  /** The user's display name shown on their public profile. */
  name: string | null;
  /** The user's unique username shown on their public profile. */
  username: string;
}
export const ReactionUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({ identifier: "ReactionUser" }) as any as S.Schema<ReactionUser>;

/** A single reaction left by a user on a feed post, such as a like or emoji. */
export interface Reaction {
  /** The emoji used for this reaction in shortcode format. Null if the reaction type is not emoji. */
  emoji: string | null;
  /** The unique identifier for the entity */
  id: string;
  /** The unique identifier of the post this reaction was left on. */
  resource_id: string;
  /** The user who left this reaction on the post. */
  user: ReactionUser;
}
export const Reaction = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    emoji: S.NullOr(S.String),
    id: S.String,
    resource_id: S.String,
    user: ReactionUser,
  }),
).annotate({ identifier: "Reaction" }) as any as S.Schema<Reaction>;

export interface DeleteReactionRequest {
  /** The unique identifier of the reaction to remove, or the identifier of the message or forum post to remove a reaction from. When passing a message or post ID, you must also provide the emoji argument. */
  id: string;
  emoji?: string;
}
export const DeleteReactionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    emoji: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "DELETE", uri: "/reactions/{id}", code: 200 })),
).annotate({
  identifier: "DeleteReactionRequest",
}) as any as S.Schema<DeleteReactionRequest>;

export type DeleteReactionResponse = boolean;
export const DeleteReactionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Boolean.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "DeleteReactionResponse",
}) as any as S.Schema<DeleteReactionResponse>;

export interface ListReactionRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  resource_id: string;
}
export const ListReactionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    resource_id: S.String.pipe(T.Query()),
  }).pipe(T.Http({ method: "GET", uri: "/reactions", code: 200 })),
).annotate({
  identifier: "ListReactionRequest",
}) as any as S.Schema<ListReactionRequest>;

/** The user who left this reaction on the post. */
export type ReactionListItemUser = ReactionUser;
export const ReactionListItemUser = ReactionUser;

/** A single reaction left by a user on a feed post, such as a like or emoji. */
export interface ReactionListItem {
  /** The emoji used for this reaction in shortcode format. Null if the reaction type is not emoji. */
  emoji: string | null;
  /** The unique identifier for the entity */
  id: string;
  /** The unique identifier of the post this reaction was left on. */
  resource_id: string;
  /** The user who left this reaction on the post. */
  user: ReactionUser;
}
export const ReactionListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    emoji: S.NullOr(S.String),
    id: S.String,
    resource_id: S.String,
    user: ReactionUser,
  }),
).annotate({
  identifier: "ReactionListItem",
}) as any as S.Schema<ReactionListItem>;

/** A list of nodes. */
export type ListReactionResponseDataList = Array<ReactionListItem>;
export const ListReactionResponseDataList = /*@__PURE__*/ S.Array(
  ReactionListItem,
) as any as S.Schema<ListReactionResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListReactionResponse {
  /** A list of nodes. */
  data: ListReactionResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListReactionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListReactionResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListReactionResponse",
}) as any as S.Schema<ListReactionResponse>;

export interface RetrieveReactionRequest {
  /** The unique identifier of the reaction to retrieve. */
  id: string;
}
export const RetrieveReactionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/reactions/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveReactionRequest",
}) as any as S.Schema<RetrieveReactionRequest>;

export type CreateReactionError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create reaction [Legacy API — https://docs.whop.com/api-reference] Add an emoji reaction or poll vote to a message or forum post. In forums, the reaction is always a like. Required permissions (one of): - `chat:read` - `dms:read` - `forum:read` - `livestream:chat:read` - `support_chat:read` */
export const createReaction: API.OperationMethod<
  CreateReactionRequest,
  Reaction,
  CreateReactionError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateReactionRequest,
  output: Reaction,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteReactionError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Delete reaction [Legacy API — https://docs.whop.com/api-reference] Remove an emoji reaction from a message or forum post. Only the reaction author or a channel admin can remove a reaction. Required permissions (one of): - `chat:read` - `dms:read` - `forum:read` - `livestream:chat:read` - `support_chat:read` */
export const deleteReaction: API.OperationMethod<
  DeleteReactionRequest,
  DeleteReactionResponse,
  DeleteReactionError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteReactionRequest,
  output: DeleteReactionResponse,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListReactionError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List reactions [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of emoji reactions on a specific message or forum post, sorted by most recent. Required permissions (one of): - `chat:read` - `dms:read` - `forum:read` - `livestream:chat:read` - `support_chat:read` */
export const listReaction: API.PaginatedOperationMethod<
  ListReactionRequest,
  ListReactionResponse,
  ListReactionError,
  WhopOpContext,
  ReactionListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListReactionRequest,
    output: ListReactionResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveReactionError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve reaction [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing reaction. Required permissions (one of): - `chat:read` - `dms:read` - `forum:read` - `livestream:chat:read` - `support_chat:read` */
export const retrieveReaction: API.OperationMethod<
  RetrieveReactionRequest,
  Reaction,
  RetrieveReactionError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveReactionRequest,
  output: Reaction,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
