// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface AddFreeDaysMembershipRequest {
  /** The unique identifier of the membership. */
  id: string;
  /** The number of free days to add (1-1095). Extends the billing period, expiration date, or Stripe trial depending on plan type. */
  free_days: number;
}
export const AddFreeDaysMembershipRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    free_days: S.Number,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/memberships/{id}/add_free_days",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AddFreeDaysMembershipRequest",
}) as any as S.Schema<AddFreeDaysMembershipRequest>;

/** The different reasons a user can choose for why they are canceling their membership. */
export type CancelOptions =
  | "too_expensive"
  | "switching"
  | "missing_features"
  | "technical_issues"
  | "bad_experience"
  | "other"
  | "testing";
export const CancelOptions = /*@__PURE__*/ S.String;

/** The state of a membership after a customer provides a cancelation reason. */
export type CancelationStatus = "won_back" | "left" | "canceling";
export const CancelationStatus = /*@__PURE__*/ S.String;

/** The company this membership belongs to. */
export interface LegacyMembershipCompany {
  /** The unique identifier for the company. */
  id: string;
  /** The display name of the company shown to customers. */
  title: string;
}
export const LegacyMembershipCompany = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({
  identifier: "LegacyMembershipCompany",
}) as any as S.Schema<LegacyMembershipCompany>;

/** The available currencies on the platform */
export type Currencies =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const Currencies = /*@__PURE__*/ S.String;

/** The response from a custom field on checkout */
export interface LegacyMembershipCustomFieldResponsesItem {
  /** The response a user gave to the specific question or field. */
  answer: string;
  /** The unique identifier for the custom field response. */
  id: string;
  /** The question asked by the custom field */
  question: string;
}
export const LegacyMembershipCustomFieldResponsesItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      answer: S.String,
      id: S.String,
      question: S.String,
    }),
).annotate({
  identifier: "LegacyMembershipCustomFieldResponsesItem",
}) as any as S.Schema<LegacyMembershipCustomFieldResponsesItem>;

/** The customer's responses to custom checkout questions configured on the product at the time of purchase. */
export type LegacyMembershipCustomFieldResponsesList =
  Array<LegacyMembershipCustomFieldResponsesItem>;
export const LegacyMembershipCustomFieldResponsesList = /*@__PURE__*/ S.Array(
  LegacyMembershipCustomFieldResponsesItem,
) as any as S.Schema<LegacyMembershipCustomFieldResponsesList>;

/** The member record linking the user to the company for this membership. Null if the member record has not been created yet. */
export interface LegacyMembershipMember {
  /** The unique identifier for the member. */
  id: string;
}
export const LegacyMembershipMember = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "LegacyMembershipMember",
}) as any as S.Schema<LegacyMembershipMember>;

/** Custom key-value pairs for the membership (commonly used for software licensing, e.g., HWID). Max 50 keys, 100 chars per key, 500 chars per string value. */
export type LegacyMembershipMetadataMap = {
  [key: string]: unknown | undefined;
};
export const LegacyMembershipMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<LegacyMembershipMetadataMap>;

/** Custom key-value pairs stored on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys `custom_cta` and `custom_cta_url`, when set, override the product's checkout call to action for this plan. */
export type LegacyMembershipPlanMetadataMap = {
  [key: string]: unknown | undefined;
};
export const LegacyMembershipPlanMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<LegacyMembershipPlanMetadataMap>;

/** The plan the customer purchased to create this membership. */
export interface LegacyMembershipPlan {
  /** The unique identifier for the plan. */
  id: string;
  /** Custom key-value pairs stored on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys `custom_cta` and `custom_cta_url`, when set, override the product's checkout call to action for this plan. */
  metadata: LegacyMembershipPlanMetadataMap | null;
}
export const LegacyMembershipPlan = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    metadata: S.NullOr(LegacyMembershipPlanMetadataMap),
  }),
).annotate({
  identifier: "LegacyMembershipPlan",
}) as any as S.Schema<LegacyMembershipPlan>;

/** Custom key-value pairs stored on the product and included in payment and membership webhook payloads. Max 50 keys, 100 characters per key, 500 characters per string value. */
export type LegacyMembershipProductMetadataMap = {
  [key: string]: unknown | undefined;
};
export const LegacyMembershipProductMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<LegacyMembershipProductMetadataMap>;

/** The product this membership grants access to. */
export interface LegacyMembershipProduct {
  /** The unique identifier for the product. */
  id: string;
  /** Custom key-value pairs stored on the product and included in payment and membership webhook payloads. Max 50 keys, 100 characters per key, 500 characters per string value. */
  metadata: LegacyMembershipProductMetadataMap | null;
  /** The display name of the product shown to customers on the product page and in search results. */
  title: string;
}
export const LegacyMembershipProduct = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    metadata: S.NullOr(LegacyMembershipProductMetadataMap),
    title: S.String,
  }),
).annotate({
  identifier: "LegacyMembershipProduct",
}) as any as S.Schema<LegacyMembershipProduct>;

/** The promotional code currently applied to this membership's billing. Null if no promo code is active. */
export interface LegacyMembershipPromoCode {
  /** The unique identifier for the promo code. */
  id: string;
}
export const LegacyMembershipPromoCode = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "LegacyMembershipPromoCode",
}) as any as S.Schema<LegacyMembershipPromoCode>;

/** The status of a membership */
export type MembershipStatus2 =
  | "trialing"
  | "active"
  | "past_due"
  | "completed"
  | "canceled"
  | "expired"
  | "unresolved"
  | "drafted"
  | "canceling";
export const MembershipStatus2 = /*@__PURE__*/ S.String;

/** The user who owns this membership. Null if the user account has been deleted. */
export interface LegacyMembershipUser {
  /** The user's email address. Requires the member:email:read permission to access. Null if not authorized. */
  email: string | null;
  /** The unique identifier for the user. */
  id: string;
  /** The user's display name shown on their public profile. */
  name: string | null;
  /** The URL of the user's profile picture. Use profilePicture for the full attachment object. */
  profile_pic: string;
  /** The user's unique username shown on their public profile. */
  username: string;
}
export const LegacyMembershipUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email: S.NullOr(S.String),
    id: S.String,
    name: S.NullOr(S.String),
    profile_pic: S.String,
    username: S.String,
  }),
).annotate({
  identifier: "LegacyMembershipUser",
}) as any as S.Schema<LegacyMembershipUser>;

/** A membership represents an active relationship between a user and a product. It tracks the user's access, billing status, and renewal schedule. */
export interface LegacyMembership {
  /** Whether this membership is set to cancel at the end of the current billing cycle. Only applies to memberships with a recurring plan. */
  cancel_at_period_end: boolean;
  /** The category selected for why the member canceled (e.g. too_expensive, switching, missing_features). */
  cancel_option: CancelOptions | null;
  /** Whether the customer is canceling, left, or was won back. Null if the membership has no cancellation reason or its cancellation state is indeterminate. */
  cancelation_status: CancelationStatus | null;
  /** The time the customer initiated cancellation of this membership. As a Unix timestamp. Null if the membership has not been canceled. */
  canceled_at: string | null;
  /** Free-text explanation provided by the customer when canceling. Null if the customer did not provide a reason. */
  cancellation_reason: string | null;
  /** The ID of the checkout session/configuration that produced this membership, if any. Use this to map memberships back to the checkout configuration that created them. */
  checkout_configuration_id: string | null;
  /** The company this membership belongs to. */
  company: LegacyMembershipCompany;
  /** The datetime the membership was created. */
  created_at: string;
  /** The three-letter ISO currency code for this membership's billing. Null if the membership is free. */
  currency: Currencies | null;
  /** The customer's responses to custom checkout questions configured on the product at the time of purchase. */
  custom_field_responses: LegacyMembershipCustomFieldResponsesList;
  /** The recurring renewal price for this membership, formatted with currency symbol and billing interval. Null if the membership is not recurring. */
  formatted_renewal_price: string | null;
  /** The unique identifier for the membership. */
  id: string;
  /** The amount the customer paid when first purchasing this membership, formatted with currency symbol. */
  initial_price_paid: string;
  /** The time the user first joined the company associated with this membership. As a Unix timestamp. Null if the member record does not exist. */
  joined_at: string | null;
  /** The software license key associated with this membership. Only present if the product includes a Whop Software Licensing experience. Null otherwise. */
  license_key: string | null;
  /** The URL where the customer can view and manage this membership, including cancellation and plan changes. Null if no member record exists. */
  manage_url: string | null;
  /** The member record linking the user to the company for this membership. Null if the member record has not been created yet. */
  member: LegacyMembershipMember | null;
  /** Custom key-value pairs for the membership (commonly used for software licensing, e.g., HWID). Max 50 keys, 100 chars per key, 500 chars per string value. */
  metadata: LegacyMembershipMetadataMap | null;
  /** Whether recurring payment collection for this membership is temporarily paused by the company. */
  payment_collection_paused: boolean;
  /** The plan the customer purchased to create this membership. */
  plan: LegacyMembershipPlan;
  /** The product this membership grants access to. */
  product: LegacyMembershipProduct;
  /** The promotional code currently applied to this membership's billing. Null if no promo code is active. */
  promo_code: LegacyMembershipPromoCode | null;
  /** The end of the current billing period for this recurring membership. As a Unix timestamp. Null if the membership is not recurring. */
  renewal_period_end: string | null;
  /** The start of the current billing period for this recurring membership. As a Unix timestamp. Null if the membership is not recurring. */
  renewal_period_start: string | null;
  /** The current lifecycle status of the membership (e.g., active, trialing, past_due, canceled, expired, completed). */
  status: MembershipStatus2;
  /** The datetime the membership was last updated. */
  updated_at: string;
  /** The user who owns this membership. Null if the user account has been deleted. */
  user: LegacyMembershipUser | null;
}
export const LegacyMembership = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cancel_at_period_end: S.Boolean,
    cancel_option: S.NullOr(CancelOptions),
    cancelation_status: S.NullOr(CancelationStatus),
    canceled_at: S.NullOr(S.String),
    cancellation_reason: S.NullOr(S.String),
    checkout_configuration_id: S.NullOr(S.String),
    company: LegacyMembershipCompany,
    created_at: S.String,
    currency: S.NullOr(Currencies),
    custom_field_responses: LegacyMembershipCustomFieldResponsesList,
    formatted_renewal_price: S.NullOr(S.String),
    id: S.String,
    initial_price_paid: S.String,
    joined_at: S.NullOr(S.String),
    license_key: S.NullOr(S.String),
    manage_url: S.NullOr(S.String),
    member: S.NullOr(LegacyMembershipMember),
    metadata: S.NullOr(LegacyMembershipMetadataMap),
    payment_collection_paused: S.Boolean,
    plan: LegacyMembershipPlan,
    product: LegacyMembershipProduct,
    promo_code: S.NullOr(LegacyMembershipPromoCode),
    renewal_period_end: S.NullOr(S.String),
    renewal_period_start: S.NullOr(S.String),
    status: MembershipStatus2,
    updated_at: S.String,
    user: S.NullOr(LegacyMembershipUser),
  }),
).annotate({
  identifier: "LegacyMembership",
}) as any as S.Schema<LegacyMembership>;

export interface CancelMembershipRequest {
  /** Membership ID (`mem_` tag). */
  id: string;
  /** `true` stops auto-renewal and keeps access until the current billing period ends. Omit or `false` revokes access immediately. */
  cancel_at_period_end?: boolean;
  /** Free-form note recording why the membership was canceled. */
  reason?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CancelMembershipRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    cancel_at_period_end: S.optional(S.Boolean),
    reason: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/memberships/{id}/cancel", code: 200 }),
  ),
).annotate({
  identifier: "CancelMembershipRequest",
}) as any as S.Schema<CancelMembershipRequest>;

export interface MembershipAccount {
  /** Account ID, prefixed `biz_`. */
  id: string;
  /** Account logo image URL. */
  logo_url: string | null;
  /** Account public route identifier — the `whop.com/{route}` storefront path. */
  route: string;
  /** Account display name. */
  title: string;
}
export const MembershipAccount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    logo_url: S.NullOr(S.String),
    route: S.String,
    title: S.String,
  }),
).annotate({
  identifier: "MembershipAccount",
}) as any as S.Schema<MembershipAccount>;

/** What the member can reach on the account: `customer` for paying members, `admin` for team members, `no_access` once every grant has lapsed. */
export type MembershipMemberAccessLevel = "no_access" | "admin" | "customer";
export const MembershipMemberAccessLevel = /*@__PURE__*/ S.String;

export interface MembershipMember {
  /** What the member can reach on the account: `customer` for paying members, `admin` for team members, `no_access` once every grant has lapsed. */
  access_level: MembershipMemberAccessLevel;
  /** When the member last opened the account's content, as an ISO 8601 timestamp. `null` if they never have. */
  last_accessed_at: string | null;
  /** The member's sort position in the buyer's own account list. `null` until they arrange it. */
  position: number | null;
}
export const MembershipMember = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    access_level: MembershipMemberAccessLevel,
    last_accessed_at: S.NullOr(S.String),
    position: S.NullOr(S.Number),
  }),
).annotate({
  identifier: "MembershipMember",
}) as any as S.Schema<MembershipMember>;

/** Billing state of the membership. `active`/`trialing` memberships grant access; `past_due` is the grace period after a failed payment; `completed` one-time purchases keep access; `canceled`/`expired` do not. */
export type MembershipStatus =
  | "trialing"
  | "active"
  | "past_due"
  | "completed"
  | "canceled"
  | "expired"
  | "unresolved";
export const MembershipStatus = /*@__PURE__*/ S.String;

export interface Membership {
  /** The account (seller) this membership belongs to. */
  account: MembershipAccount;
  /** Whether the membership is set to cancel when the current billing period ends. Only meaningful for recurring plans. */
  cancel_at_period_end: boolean;
  /** When the membership was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** When the current billing period renews, or when a non-renewing membership expires, as an ISO 8601 timestamp. `null` for one-time purchases with no expiration. */
  current_period_end: string | null;
  /** Membership ID, prefixed `mem_`. */
  id: string;
  /** The software license key for this membership. Only present when the product includes a software licensing experience. */
  license_key: string | null;
  /** The caller's member row on the account. Present only when the membership belongs to the caller; `null` on seller-side reads. */
  member: MembershipMember | null;
  /** Custom key-value pairs stored on the membership, commonly used for software licensing. */
  metadata: unknown;
  /** The plan the buyer purchased, prefixed `plan_`. */
  plan_id: string;
  /** The product this membership grants access to, prefixed `prod_`. */
  product_id: string;
  /** Billing state of the membership. `active`/`trialing` memberships grant access; `past_due` is the grace period after a failed payment; `completed` one-time purchases keep access; `canceled`/`expired` do not. */
  status: MembershipStatus;
  /** The buyer, prefixed `user_`. `null` when the buyer is another business or the membership is unclaimed. */
  user_id: string | null;
}
export const Membership = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account: MembershipAccount,
    cancel_at_period_end: S.Boolean,
    created_at: S.String,
    current_period_end: S.NullOr(S.String),
    id: S.String,
    license_key: S.NullOr(S.String),
    member: S.NullOr(MembershipMember),
    metadata: S.Unknown,
    plan_id: S.String,
    product_id: S.String,
    status: MembershipStatus,
    user_id: S.NullOr(S.String),
  }),
).annotate({ identifier: "Membership" }) as any as S.Schema<Membership>;

export interface ExtendMembershipRequest {
  /** Membership ID (`mem_` tag). */
  id: string;
  /** Number of free days to add (1-1095). */
  days: number;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const ExtendMembershipRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    days: S.Number,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/memberships/{id}/extend", code: 200 }),
  ),
).annotate({
  identifier: "ExtendMembershipRequest",
}) as any as S.Schema<ExtendMembershipRequest>;

export interface InviteMembershipRequest {
  /** Recipient email address. Mutually exclusive with `user_id`. */
  email?: string;
  /** Free plan whose membership the recipient is invited to, prefixed `plan_`. */
  plan_id: string;
  /** Recipient user ID, prefixed `user_`. Mutually exclusive with `email`. */
  user_id?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const InviteMembershipRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email: S.optional(S.String),
    plan_id: S.String,
    user_id: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/memberships/invite", code: 200 })),
).annotate({
  identifier: "InviteMembershipRequest",
}) as any as S.Schema<InviteMembershipRequest>;

export interface InviteMembershipResponse {
  invitation_sent: boolean;
}
export const InviteMembershipResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invitation_sent: S.Boolean,
  }),
).annotate({
  identifier: "InviteMembershipResponse",
}) as any as S.Schema<InviteMembershipResponse>;

export type ListMembershipsRequestStatus =
  | "active"
  | "trialing"
  | "past_due"
  | "completed"
  | "canceled"
  | "expired"
  | "canceling"
  | "paused";
export const ListMembershipsRequestStatus = /*@__PURE__*/ S.String;

export type ListMembershipsRequestOrder = "created_at";
export const ListMembershipsRequestOrder = /*@__PURE__*/ S.String;

export type ListMembershipsRequestDirection = "asc" | "desc";
export const ListMembershipsRequestDirection = /*@__PURE__*/ S.String;

export interface ListMembershipsRequest {
  /** Narrow to one account (`biz_` tag). With read access to the account this lists all of its memberships; without, only the caller's own memberships in it. */
  account_id?: string;
  /** Narrow to one user's memberships (`user_` tag, or `me` for the caller). A user outside the caller's visible set returns an empty list. */
  user_id?: string;
  /** Filter by billing state. `canceling` matches active memberships set to cancel at period end; `paused` matches memberships with payment collection paused. */
  status?: ListMembershipsRequestStatus | (string & {});
  /** Filter to memberships of this product (`prod_` tag). Repeat as product_ids[] for several. */
  product_id?: string;
  /** Filter to memberships of this plan (`plan_` tag). Repeat as plan_ids[] for several. */
  plan_id?: string;
  /** Only memberships created after this ISO 8601 timestamp. */
  created_after?: string;
  /** Only memberships created before this ISO 8601 timestamp. */
  created_before?: string;
  /** Sort field. */
  order?: ListMembershipsRequestOrder | (string & {});
  /** Sort direction. */
  direction?: ListMembershipsRequestDirection | (string & {});
  /** Number of memberships to return from the start of the window. */
  first?: number;
  /** Cursor to paginate forwards from. */
  after?: string;
  /** Number of memberships to return from the end of the window. */
  last?: number;
  /** Cursor to paginate backwards from. */
  before?: string;
}
export const ListMembershipsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
    status: S.optional(ListMembershipsRequestStatus.pipe(T.Query())),
    product_id: S.optional(S.String.pipe(T.Query())),
    plan_id: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListMembershipsRequestOrder.pipe(T.Query())),
    direction: S.optional(ListMembershipsRequestDirection.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/memberships", code: 200 })),
).annotate({
  identifier: "ListMembershipsRequest",
}) as any as S.Schema<ListMembershipsRequest>;

export type ListMembershipsResponseDataList = Array<Membership>;
export const ListMembershipsResponseDataList = /*@__PURE__*/ S.Array(
  Membership,
) as any as S.Schema<ListMembershipsResponseDataList>;

export interface ListMembershipsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListMembershipsResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListMembershipsResponsePageInfo",
}) as any as S.Schema<ListMembershipsResponsePageInfo>;

export interface ListMembershipsResponse {
  data: ListMembershipsResponseDataList;
  page_info: ListMembershipsResponsePageInfo;
}
export const ListMembershipsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListMembershipsResponseDataList,
    page_info: ListMembershipsResponsePageInfo,
  }),
).annotate({
  identifier: "ListMembershipsResponse",
}) as any as S.Schema<ListMembershipsResponse>;

export interface PauseMembershipRequest {
  /** Membership ID (`mem_` tag). */
  id: string;
  /** ISO 8601 time to automatically resume payment collection. Must be in the future; only supported for memberships billed by Whop. */
  until?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const PauseMembershipRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    until: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/memberships/{id}/pause", code: 200 }),
  ),
).annotate({
  identifier: "PauseMembershipRequest",
}) as any as S.Schema<PauseMembershipRequest>;

export interface ResumeMembershipRequest {
  /** Membership ID (`mem_` tag). */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const ResumeMembershipRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/memberships/{id}/resume", code: 200 }),
  ),
).annotate({
  identifier: "ResumeMembershipRequest",
}) as any as S.Schema<ResumeMembershipRequest>;

export interface ResyncAccessMembershipRequest {
  /** The unique identifier of the membership to resync access for. */
  id: string;
}
export const ResyncAccessMembershipRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/memberships/{id}/resync_access",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ResyncAccessMembershipRequest",
}) as any as S.Schema<ResyncAccessMembershipRequest>;

export interface RetrieveMembershipRequest {
  /** Membership ID (`mem_` tag), or a software license key. */
  id: string;
}
export const RetrieveMembershipRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/memberships/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveMembershipRequest",
}) as any as S.Schema<RetrieveMembershipRequest>;

export interface TransferMembershipRequest {
  /** Membership ID (`mem_` tag). */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const TransferMembershipRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/memberships/{id}/transfer", code: 200 }),
  ),
).annotate({
  identifier: "TransferMembershipRequest",
}) as any as S.Schema<TransferMembershipRequest>;

export interface TransferMembershipResponse {
  /** One-use URL the destination account opens to claim the membership. */
  url: string;
}
export const TransferMembershipResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
  }),
).annotate({
  identifier: "TransferMembershipResponse",
}) as any as S.Schema<TransferMembershipResponse>;

export interface UncancelMembershipRequest {
  /** The unique identifier of the membership to uncancel. */
  id: string;
}
export const UncancelMembershipRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "POST", uri: "/memberships/{id}/uncancel", code: 200 }),
  ),
).annotate({
  identifier: "UncancelMembershipRequest",
}) as any as S.Schema<UncancelMembershipRequest>;

export interface UpdateMembershipRequest {
  /** Membership ID (`mem_` tag), or a software license key. */
  id: string;
  /** `true` cancels at the end of the current billing period (the customer keeps access until then); `false` reverses a pending cancellation. */
  cancel_at_period_end?: boolean;
  /** Key-value pairs to merge into the membership's metadata. Pass an empty object to clear it. */
  metadata?: unknown;
}
export const UpdateMembershipRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    cancel_at_period_end: S.optional(S.Boolean),
    metadata: S.optional(S.Unknown),
  }).pipe(T.Http({ method: "PATCH", uri: "/memberships/{id}", code: 200 })),
).annotate({
  identifier: "UpdateMembershipRequest",
}) as any as S.Schema<UpdateMembershipRequest>;

export type AddFreeDaysMembershipError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Add free days membership [Legacy API — https://docs.whop.com/api-reference] Add free days to extend a membership's current billing period, expiration date, or Stripe trial. Required permissions: - `member:manage` - `member:email:read` - `member:basic:read` */
export const addFreeDaysMembership: API.OperationMethod<
  AddFreeDaysMembershipRequest,
  LegacyMembership,
  AddFreeDaysMembershipError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AddFreeDaysMembershipRequest,
  output: LegacyMembership,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CancelMembershipError =
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Cancel Membership Cancels a membership. Pass `cancel_at_period_end: true` to stop auto-renewal and keep access until the current billing period ends. Omit it (or pass `false`) to revoke access immediately. Buyers cannot cancel buy-now-pay-later (`splitit`, `sezzle`) or non-trial split-pay memberships. */
export const cancelMembership: API.OperationMethod<
  CancelMembershipRequest,
  Membership,
  CancelMembershipError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CancelMembershipRequest,
  output: Membership,
  errors: [Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ExtendMembershipError =
  | BadRequest
  | Forbidden
  | Conflict
  | WhopOpError;
/** Extend Membership Adds free days to a membership, extending its current billing period, expiration date, or trial depending on the plan type. */
export const extendMembership: API.OperationMethod<
  ExtendMembershipRequest,
  Membership,
  ExtendMembershipError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExtendMembershipRequest,
  output: Membership,
  errors: [BadRequest, Forbidden, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type InviteMembershipError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Invite to a Membership Sends an email inviting one recipient to join the account through a free plan. Identify the recipient by exactly one of `user_id` or `email`. The invitation is bound to that recipient; after signing in, accepting it immediately grants the membership without checkout. This Experimental endpoint is available only to accounts enabled for membership invitations. */
export const inviteMembership: API.OperationMethod<
  InviteMembershipRequest,
  InviteMembershipResponse,
  InviteMembershipError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InviteMembershipRequest,
  output: InviteMembershipResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListMembershipsError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** List Memberships Lists every membership the caller can read: an account API key its account's; a user credential their own plus those of every account they manage. `account_id` and `user_id` only narrow that list — values outside the caller's reach return fewer results, not an error. */
export const listMemberships: API.PaginatedOperationMethod<
  ListMembershipsRequest,
  ListMembershipsResponse,
  ListMembershipsError,
  WhopOpContext,
  Membership
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListMembershipsRequest,
    output: ListMembershipsResponse,
    errors: [BadRequest, Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type PauseMembershipError =
  | BadRequest
  | Forbidden
  | Conflict
  | WhopOpError;
/** Pause Membership Pauses a membership's recurring payment collection. The customer keeps access but is not charged until the membership is resumed. */
export const pauseMembership: API.OperationMethod<
  PauseMembershipRequest,
  Membership,
  PauseMembershipError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PauseMembershipRequest,
  output: Membership,
  errors: [BadRequest, Forbidden, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ResumeMembershipError =
  | BadRequest
  | Forbidden
  | Conflict
  | WhopOpError;
/** Resume Membership Resumes a previously paused membership's recurring payment collection. Billing resumes on the next cycle. */
export const resumeMembership: API.OperationMethod<
  ResumeMembershipRequest,
  Membership,
  ResumeMembershipError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ResumeMembershipRequest,
  output: Membership,
  errors: [BadRequest, Forbidden, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ResyncAccessMembershipError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Resync access membership [Legacy API — https://docs.whop.com/api-reference] Re-run access fulfillment for a membership. Recomputes the member's content access on Whop, re-validates their Discord link (re-adding them to the server and re-assigning roles if needed), and re-fulfills TradingView indicator access. Telegram access is invite-based and cannot be resynced here. The outcome is written to the membership's logs. Required permissions: - `membership:resync_access` - `member:email:read` - `member:basic:read` */
export const resyncAccessMembership: API.OperationMethod<
  ResyncAccessMembershipRequest,
  LegacyMembership,
  ResyncAccessMembershipError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ResyncAccessMembershipRequest,
  output: LegacyMembership,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveMembershipError = Forbidden | NotFound | WhopOpError;
/** Retrieve Membership Retrieves a membership by ID or license key. Accessible to the account and to the membership's own user. */
export const retrieveMembership: API.OperationMethod<
  RetrieveMembershipRequest,
  Membership,
  RetrieveMembershipError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveMembershipRequest,
  output: Membership,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type TransferMembershipError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Transfer Membership Creates a one-use transfer URL for a membership. Opening the URL while logged into a different Whop account claims the membership onto that account. The membership's buyer can generate a link for their own membership with `membership:transfer` when the product allows transfers and the membership is `trialing`, `active`, or `completed`. An account credential with `membership:update` bypasses both restrictions. */
export const transferMembership: API.OperationMethod<
  TransferMembershipRequest,
  TransferMembershipResponse,
  TransferMembershipError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: TransferMembershipRequest,
  output: TransferMembershipResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UncancelMembershipError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Uncancel membership [Legacy API — https://docs.whop.com/api-reference] Reverse a pending cancellation for a membership that was scheduled to cancel at period end. Required permissions: - `member:manage` - `member:email:read` - `member:basic:read` */
export const uncancelMembership: API.OperationMethod<
  UncancelMembershipRequest,
  LegacyMembership,
  UncancelMembershipError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UncancelMembershipRequest,
  output: LegacyMembership,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateMembershipError = BadRequest | Forbidden | WhopOpError;
/** Update Membership Updates a membership: merge metadata key-value pairs, or toggle `cancel_at_period_end` — `true` schedules the cancellation for the end of the current billing period, `false` reverses a pending one. */
export const updateMembership: API.OperationMethod<
  UpdateMembershipRequest,
  Membership,
  UpdateMembershipError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateMembershipRequest,
  output: Membership,
  errors: [BadRequest, Forbidden],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
