// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** The thumbnail image for the course in PNG, JPEG, or GIF format. */
export interface CreateCourseRequestThumbnail {
  /** The ID of an existing file object. */
  id: string;
}
export const CreateCourseRequestThumbnail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "CreateCourseRequestThumbnail",
}) as any as S.Schema<CreateCourseRequestThumbnail>;

/** The available visibilities for a course. Determines how / whether a course is visible to users. */
export type CourseVisibilities = "visible" | "hidden";
export const CourseVisibilities = /*@__PURE__*/ S.String;

export interface CreateCourseRequest {
  /** Whether the course awards students a PDF certificate after completing all lessons. */
  certificate_after_completion_enabled?: boolean | null;
  /** The unique identifier of the experience to create the course in (e.g., "exp_XXXXX"). */
  experience_id: string;
  /** The decimal order position of the course within its experience. Use fractional values (e.g., "1.5") to place between existing courses. */
  order?: string | null;
  /** Whether students must complete each lesson sequentially before advancing to the next one. */
  require_completing_lessons_in_order?: boolean | null;
  /** A short tagline displayed beneath the course title (e.g., "Master the fundamentals of design"). */
  tagline?: string | null;
  /** The thumbnail image for the course in PNG, JPEG, or GIF format. */
  thumbnail?: CreateCourseRequestThumbnail | null;
  /** The display title of the course (e.g., "Introduction to Web Development"). */
  title: string;
  /** Controls whether this course is visible to students or hidden as a draft. */
  visibility?: CourseVisibilities | (string & {}) | null;
}
export const CreateCourseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    certificate_after_completion_enabled: S.optional(S.NullOr(S.Boolean)),
    experience_id: S.String,
    order: S.optional(S.NullOr(S.String)),
    require_completing_lessons_in_order: S.optional(S.NullOr(S.Boolean)),
    tagline: S.optional(S.NullOr(S.String)),
    thumbnail: S.optional(S.NullOr(CreateCourseRequestThumbnail)),
    title: S.String,
    visibility: S.optional(S.NullOr(CourseVisibilities)),
  }).pipe(T.Http({ method: "POST", uri: "/courses", code: 200 })),
).annotate({
  identifier: "CreateCourseRequest",
}) as any as S.Schema<CreateCourseRequest>;

/** The available types for a lesson */
export type LessonTypes =
  | "text"
  | "video"
  | "pdf"
  | "multi"
  | "quiz"
  | "knowledge_check";
export const LessonTypes = /*@__PURE__*/ S.String;

/** The thumbnail image displayed on lesson cards and previews. Null if no thumbnail has been uploaded. */
export interface CourseChaptersItemLessonsItemThumbnail {
  /** A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps. */
  url: string | null;
}
export const CourseChaptersItemLessonsItemThumbnail = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      url: S.NullOr(S.String),
    }),
).annotate({
  identifier: "CourseChaptersItemLessonsItemThumbnail",
}) as any as S.Schema<CourseChaptersItemLessonsItemThumbnail>;

/** The Mux video asset for video-type lessons, used for streaming playback. Null if this lesson has no hosted video. */
export interface CourseChaptersItemLessonsItemVideoAsset {
  /** The duration of the video in seconds */
  duration_seconds: number | null;
  /** The signed playback ID of the Mux asset */
  signed_playback_id: string | null;
  /** The signed thumbnail playback token of the Mux asset */
  signed_thumbnail_playback_token: string | null;
}
export const CourseChaptersItemLessonsItemVideoAsset = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      duration_seconds: S.NullOr(S.Number),
      signed_playback_id: S.NullOr(S.String),
      signed_thumbnail_playback_token: S.NullOr(S.String),
    }),
).annotate({
  identifier: "CourseChaptersItemLessonsItemVideoAsset",
}) as any as S.Schema<CourseChaptersItemLessonsItemVideoAsset>;

/** An individual learning unit within a chapter, which can contain text, video, PDF, or assessment content. */
export interface CourseChaptersItemLessonsItem {
  /** The unique identifier for the lesson. */
  id: string;
  /** The content format of this lesson. One of: text, video, pdf, multi, quiz, knowledge_check. */
  lesson_type: LessonTypes;
  /** The sort position of this lesson within its parent chapter, starting from zero. */
  order: number;
  /** The thumbnail image displayed on lesson cards and previews. Null if no thumbnail has been uploaded. */
  thumbnail: CourseChaptersItemLessonsItemThumbnail | null;
  /** The display name of the lesson shown to students. Maximum 120 characters. */
  title: string;
  /** The Mux video asset for video-type lessons, used for streaming playback. Null if this lesson has no hosted video. */
  video_asset: CourseChaptersItemLessonsItemVideoAsset | null;
}
export const CourseChaptersItemLessonsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    lesson_type: LessonTypes,
    order: S.Number,
    thumbnail: S.NullOr(CourseChaptersItemLessonsItemThumbnail),
    title: S.String,
    video_asset: S.NullOr(CourseChaptersItemLessonsItemVideoAsset),
  }),
).annotate({
  identifier: "CourseChaptersItemLessonsItem",
}) as any as S.Schema<CourseChaptersItemLessonsItem>;

/** An ordered list of lessons in this chapter, sorted by display position. Hidden lessons are excluded for non-admin users. */
export type CourseChaptersItemLessonsList =
  Array<CourseChaptersItemLessonsItem>;
export const CourseChaptersItemLessonsList = /*@__PURE__*/ S.Array(
  CourseChaptersItemLessonsItem,
) as any as S.Schema<CourseChaptersItemLessonsList>;

/** A grouping of related lessons within a course, used to organize content into sections. */
export interface CourseChaptersItem {
  /** The unique identifier for the chapter. */
  id: string;
  /** An ordered list of lessons in this chapter, sorted by display position. Hidden lessons are excluded for non-admin users. */
  lessons: CourseChaptersItemLessonsList;
  /** The sort position of this chapter within its parent course, starting from zero. */
  order: number;
  /** The display name of the chapter shown to students. Maximum 150 characters. */
  title: string;
}
export const CourseChaptersItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    lessons: CourseChaptersItemLessonsList,
    order: S.Number,
    title: S.String,
  }),
).annotate({
  identifier: "CourseChaptersItem",
}) as any as S.Schema<CourseChaptersItem>;

/** An ordered list of all chapters in this course, sorted by their display position. */
export type CourseChaptersList = Array<CourseChaptersItem>;
export const CourseChaptersList = /*@__PURE__*/ S.Array(
  CourseChaptersItem,
) as any as S.Schema<CourseChaptersList>;

/** The available languages for a course */
export type Languages =
  | "en"
  | "es"
  | "it"
  | "pt"
  | "de"
  | "fr"
  | "pl"
  | "ru"
  | "nl"
  | "ca"
  | "tr"
  | "sv"
  | "uk"
  | "no"
  | "fi"
  | "sk"
  | "el"
  | "cs"
  | "hr"
  | "da"
  | "ro"
  | "bg";
export const Languages = /*@__PURE__*/ S.String;

/** The distinct drip schedules, in days after the course start, of lessons visible to the current user. Combine with startedAt to work out which have unlocked. Empty when the user has not started the course or no lesson is on a schedule. */
export type CourseLessonUnlockDaysList = Array<number>;
export const CourseLessonUnlockDaysList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<CourseLessonUnlockDaysList>;

/** The lesson the current user should continue from: their first incomplete lesson, or the first lesson when they have finished the course, have not started it, or can edit it. Null if the course has no lessons. */
export interface CourseResumeLesson {
  /** The unique identifier for the lesson. */
  id: string;
}
export const CourseResumeLesson = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "CourseResumeLesson",
}) as any as S.Schema<CourseResumeLesson>;

/** The thumbnail image displayed on course cards and previews. Null if no thumbnail has been uploaded. */
export interface CourseThumbnail {
  /** Uploaded file MIME type, such as image/jpeg, video/mp4, or audio/mpeg. */
  content_type: string | null;
  /** The original filename of the uploaded attachment, including its file extension. */
  filename: string | null;
  /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
  id: string;
  /** A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps. */
  optimized_url: string | null;
  /** The original source URL of the attachment, such as a direct link to S3. This should never be displayed on the client and should always be passed through an Imgproxy transformer. */
  source_url: string | null;
}
export const CourseThumbnail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    content_type: S.NullOr(S.String),
    filename: S.NullOr(S.String),
    id: S.String,
    optimized_url: S.NullOr(S.String),
    source_url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "CourseThumbnail",
}) as any as S.Schema<CourseThumbnail>;

/** A structured learning module containing chapters and lessons, belonging to an experience. */
export interface Course {
  /** Whether students receive a PDF certificate after completing all lessons in this course. Null if the setting has not been configured. */
  certificate_after_completion_enabled: boolean | null;
  /** An ordered list of all chapters in this course, sorted by their display position. */
  chapters: CourseChaptersList;
  /** The total number of chapters in this course, including chapters whose lessons are all hidden from the current user. */
  chapters_count: number;
  /** The number of lessons in this course that the current user has marked as completed. Zero when the request is not made on behalf of a user. */
  completed_lessons_count: number;
  /** The URL of the course cover image shown on preview cards. Null if no cover image has been uploaded. */
  cover_image: string | null;
  /** The datetime the course was created. */
  created_at: string;
  /** A brief summary of the course content and objectives. Null if no description has been set. */
  description: string | null;
  /** The unique identifier for the course. */
  id: string;
  /** The spoken language of the video content, used to generate accurate closed captions. One of: en, es, it, pt, de, fr, pl, ru, nl, ca, tr, sv, uk, no, fi, sk, el, cs, hr, da, ro, bg. */
  language: Languages;
  /** The creation timestamp of the most recently added lesson visible to the current user. Null if the course has no lessons. */
  latest_lesson_created_at: string | null;
  /** The distinct drip schedules, in days after the course start, of lessons visible to the current user. Combine with startedAt to work out which have unlocked. Empty when the user has not started the course or no lesson is on a schedule. */
  lesson_unlock_days: CourseLessonUnlockDaysList;
  /** The sort position of this course within its parent experience, as a decimal for flexible ordering. */
  order: string;
  /** Whether students must complete each lesson sequentially before advancing to the next one. */
  require_completing_lessons_in_order: boolean;
  /** The lesson the current user should continue from: their first incomplete lesson, or the first lesson when they have finished the course, have not started it, or can edit it. Null if the course has no lessons. */
  resume_lesson: CourseResumeLesson | null;
  /** The earliest time the current user is known to have started this course. Null if they have not started it. Drip unlock schedules are measured from this point. */
  started_at: string | null;
  /** A short marketing tagline displayed beneath the course title. Null if no tagline has been set. */
  tagline: string | null;
  /** The thumbnail image displayed on course cards and previews. Null if no thumbnail has been uploaded. */
  thumbnail: CourseThumbnail | null;
  /** The display name of the course shown to students. Null if no title has been set. */
  title: string | null;
  /** The combined duration in seconds of every hosted video across the lessons visible to the current user. */
  total_duration_seconds: number;
  /** The number of lessons in this course visible to the current user. */
  total_lessons_count: number;
  /** The datetime the course was last updated. */
  updated_at: string;
  /** The visibility setting that controls whether this course appears to students. One of: visible, hidden. */
  visibility: CourseVisibilities;
}
export const Course = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    certificate_after_completion_enabled: S.NullOr(S.Boolean),
    chapters: CourseChaptersList,
    chapters_count: S.Number,
    completed_lessons_count: S.Number,
    cover_image: S.NullOr(S.String),
    created_at: S.String,
    description: S.NullOr(S.String),
    id: S.String,
    language: Languages,
    latest_lesson_created_at: S.NullOr(S.String),
    lesson_unlock_days: CourseLessonUnlockDaysList,
    order: S.String,
    require_completing_lessons_in_order: S.Boolean,
    resume_lesson: S.NullOr(CourseResumeLesson),
    started_at: S.NullOr(S.String),
    tagline: S.NullOr(S.String),
    thumbnail: S.NullOr(CourseThumbnail),
    title: S.NullOr(S.String),
    total_duration_seconds: S.Number,
    total_lessons_count: S.Number,
    updated_at: S.String,
    visibility: CourseVisibilities,
  }),
).annotate({ identifier: "Course" }) as any as S.Schema<Course>;

export interface DeleteCourseRequest {
  /** The unique identifier of the course to delete (e.g., "course_XXXXX"). */
  id: string;
}
export const DeleteCourseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/courses/{id}", code: 200 })),
).annotate({
  identifier: "DeleteCourseRequest",
}) as any as S.Schema<DeleteCourseRequest>;

export type DeleteCourseResponse = boolean;
export const DeleteCourseResponse = /*@__PURE__*/ S.suspend(() =>
  S.Boolean.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "DeleteCourseResponse",
}) as any as S.Schema<DeleteCourseResponse>;

export interface ListCourseRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  experience_id?: string;
  company_id?: string;
}
export const ListCourseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    experience_id: S.optional(S.String.pipe(T.Query())),
    company_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/courses", code: 200 })),
).annotate({
  identifier: "ListCourseRequest",
}) as any as S.Schema<ListCourseRequest>;

/** The distinct drip schedules, in days after the course start, of lessons visible to the current user. Combine with startedAt to work out which have unlocked. Empty when the user has not started the course or no lesson is on a schedule. */
export type CourseListItemLessonUnlockDaysList = Array<number>;
export const CourseListItemLessonUnlockDaysList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<CourseListItemLessonUnlockDaysList>;

/** The lesson the current user should continue from: their first incomplete lesson, or the first lesson when they have finished the course, have not started it, or can edit it. Null if the course has no lessons. */
export type CourseListItemResumeLesson = CourseResumeLesson;
export const CourseListItemResumeLesson = CourseResumeLesson;

/** The thumbnail image displayed on course cards and previews. Null if no thumbnail has been uploaded. */
export type CourseListItemThumbnail = CourseThumbnail;
export const CourseListItemThumbnail = CourseThumbnail;

/** A structured learning module containing chapters and lessons, belonging to an experience. */
export interface CourseListItem {
  /** Whether students receive a PDF certificate after completing all lessons in this course. Null if the setting has not been configured. */
  certificate_after_completion_enabled: boolean | null;
  /** The total number of chapters in this course, including chapters whose lessons are all hidden from the current user. */
  chapters_count: number;
  /** The number of lessons in this course that the current user has marked as completed. Zero when the request is not made on behalf of a user. */
  completed_lessons_count: number;
  /** The URL of the course cover image shown on preview cards. Null if no cover image has been uploaded. */
  cover_image: string | null;
  /** The datetime the course was created. */
  created_at: string;
  /** A brief summary of the course content and objectives. Null if no description has been set. */
  description: string | null;
  /** The unique identifier for the course. */
  id: string;
  /** The spoken language of the video content, used to generate accurate closed captions. One of: en, es, it, pt, de, fr, pl, ru, nl, ca, tr, sv, uk, no, fi, sk, el, cs, hr, da, ro, bg. */
  language: Languages;
  /** The creation timestamp of the most recently added lesson visible to the current user. Null if the course has no lessons. */
  latest_lesson_created_at: string | null;
  /** The distinct drip schedules, in days after the course start, of lessons visible to the current user. Combine with startedAt to work out which have unlocked. Empty when the user has not started the course or no lesson is on a schedule. */
  lesson_unlock_days: CourseListItemLessonUnlockDaysList;
  /** The sort position of this course within its parent experience, as a decimal for flexible ordering. */
  order: string;
  /** Whether students must complete each lesson sequentially before advancing to the next one. */
  require_completing_lessons_in_order: boolean;
  /** The lesson the current user should continue from: their first incomplete lesson, or the first lesson when they have finished the course, have not started it, or can edit it. Null if the course has no lessons. */
  resume_lesson: CourseResumeLesson | null;
  /** The earliest time the current user is known to have started this course. Null if they have not started it. Drip unlock schedules are measured from this point. */
  started_at: string | null;
  /** A short marketing tagline displayed beneath the course title. Null if no tagline has been set. */
  tagline: string | null;
  /** The thumbnail image displayed on course cards and previews. Null if no thumbnail has been uploaded. */
  thumbnail: CourseThumbnail | null;
  /** The display name of the course shown to students. Null if no title has been set. */
  title: string | null;
  /** The combined duration in seconds of every hosted video across the lessons visible to the current user. */
  total_duration_seconds: number;
  /** The number of lessons in this course visible to the current user. */
  total_lessons_count: number;
  /** The datetime the course was last updated. */
  updated_at: string;
  /** The visibility setting that controls whether this course appears to students. One of: visible, hidden. */
  visibility: CourseVisibilities;
}
export const CourseListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    certificate_after_completion_enabled: S.NullOr(S.Boolean),
    chapters_count: S.Number,
    completed_lessons_count: S.Number,
    cover_image: S.NullOr(S.String),
    created_at: S.String,
    description: S.NullOr(S.String),
    id: S.String,
    language: Languages,
    latest_lesson_created_at: S.NullOr(S.String),
    lesson_unlock_days: CourseListItemLessonUnlockDaysList,
    order: S.String,
    require_completing_lessons_in_order: S.Boolean,
    resume_lesson: S.NullOr(CourseResumeLesson),
    started_at: S.NullOr(S.String),
    tagline: S.NullOr(S.String),
    thumbnail: S.NullOr(CourseThumbnail),
    title: S.NullOr(S.String),
    total_duration_seconds: S.Number,
    total_lessons_count: S.Number,
    updated_at: S.String,
    visibility: CourseVisibilities,
  }),
).annotate({ identifier: "CourseListItem" }) as any as S.Schema<CourseListItem>;

/** A list of nodes. */
export type ListCourseResponseDataList = Array<CourseListItem>;
export const ListCourseResponseDataList = /*@__PURE__*/ S.Array(
  CourseListItem,
) as any as S.Schema<ListCourseResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListCourseResponse {
  /** A list of nodes. */
  data: ListCourseResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListCourseResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListCourseResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListCourseResponse",
}) as any as S.Schema<ListCourseResponse>;

export interface RetrieveCourseRequest {
  /** The unique identifier of the course to retrieve. */
  id: string;
}
export const RetrieveCourseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/courses/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveCourseRequest",
}) as any as S.Schema<RetrieveCourseRequest>;

/** Input for updating a lesson while updating a course */
export interface UpdateCourseRequestChaptersItemLessonsItem {
  /** The ID of the chapter this lesson belongs to (for moving between chapters) */
  chapter_id: string;
  /** The ID of the lesson to update */
  id: string;
  /** The order of the lesson within its chapter */
  order: number;
  /** The title of the lesson */
  title: string;
}
export const UpdateCourseRequestChaptersItemLessonsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      chapter_id: S.String,
      id: S.String,
      order: S.Number,
      title: S.String,
    }),
  ).annotate({
    identifier: "UpdateCourseRequestChaptersItemLessonsItem",
  }) as any as S.Schema<UpdateCourseRequestChaptersItemLessonsItem>;

/** The lessons to update within this chapter */
export type UpdateCourseRequestChaptersItemLessonsList =
  Array<UpdateCourseRequestChaptersItemLessonsItem>;
export const UpdateCourseRequestChaptersItemLessonsList = /*@__PURE__*/ S.Array(
  UpdateCourseRequestChaptersItemLessonsItem,
) as any as S.Schema<UpdateCourseRequestChaptersItemLessonsList>;

/** Input for updating a chapter while updating a course */
export interface UpdateCourseRequestChaptersItem {
  /** The ID of the chapter to update */
  id: string;
  /** The lessons to update within this chapter */
  lessons?: UpdateCourseRequestChaptersItemLessonsList | null;
  /** The order of the chapter within its course */
  order: number;
  /** The title of the chapter */
  title: string;
}
export const UpdateCourseRequestChaptersItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    lessons: S.optional(S.NullOr(UpdateCourseRequestChaptersItemLessonsList)),
    order: S.Number,
    title: S.String,
  }),
).annotate({
  identifier: "UpdateCourseRequestChaptersItem",
}) as any as S.Schema<UpdateCourseRequestChaptersItem>;

/** A list of chapters with nested lessons to reorder or rename in bulk. */
export type UpdateCourseRequestChaptersList =
  Array<UpdateCourseRequestChaptersItem>;
export const UpdateCourseRequestChaptersList = /*@__PURE__*/ S.Array(
  UpdateCourseRequestChaptersItem,
) as any as S.Schema<UpdateCourseRequestChaptersList>;

/** The thumbnail image for the course in PNG, JPEG, or GIF format. */
export type UpdateCourseRequestThumbnail = CreateCourseRequestThumbnail;
export const UpdateCourseRequestThumbnail = CreateCourseRequestThumbnail;

export interface UpdateCourseRequest {
  /** The unique identifier of the course to update (e.g., "course_XXXXX"). */
  id: string;
  /** Whether the course awards students a PDF certificate after completing all lessons. */
  certificate_after_completion_enabled?: boolean | null;
  /** A list of chapters with nested lessons to reorder or rename in bulk. */
  chapters?: UpdateCourseRequestChaptersList | null;
  /** A short description of the course displayed to students on the course page. */
  description?: string | null;
  /** The primary language spoken in the video content of the course. */
  language?: Languages | (string & {}) | null;
  /** The decimal order position of the course within its experience. Use fractional values (e.g., "1.5") to place between existing courses. */
  order?: string | null;
  /** Whether students must complete each lesson sequentially before advancing to the next one. */
  require_completing_lessons_in_order?: boolean | null;
  /** A short tagline displayed beneath the course title (e.g., "Master the fundamentals of design"). */
  tagline?: string | null;
  /** The thumbnail image for the course in PNG, JPEG, or GIF format. */
  thumbnail?: CreateCourseRequestThumbnail | null;
  /** The display title of the course (e.g., "Introduction to Web Development"). */
  title?: string | null;
  /** Controls whether this course is visible to students or hidden as a draft. */
  visibility?: CourseVisibilities | (string & {}) | null;
}
export const UpdateCourseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    certificate_after_completion_enabled: S.optional(S.NullOr(S.Boolean)),
    chapters: S.optional(S.NullOr(UpdateCourseRequestChaptersList)),
    description: S.optional(S.NullOr(S.String)),
    language: S.optional(S.NullOr(Languages)),
    order: S.optional(S.NullOr(S.String)),
    require_completing_lessons_in_order: S.optional(S.NullOr(S.Boolean)),
    tagline: S.optional(S.NullOr(S.String)),
    thumbnail: S.optional(S.NullOr(CreateCourseRequestThumbnail)),
    title: S.optional(S.NullOr(S.String)),
    visibility: S.optional(S.NullOr(CourseVisibilities)),
  }).pipe(T.Http({ method: "PATCH", uri: "/courses/{id}", code: 200 })),
).annotate({
  identifier: "UpdateCourseRequest",
}) as any as S.Schema<UpdateCourseRequest>;

export type CreateCourseError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create course [Legacy API — https://docs.whop.com/api-reference] Create a new course within an experience, with optional chapters, lessons, and a certificate. Required permissions: - `courses:update` */
export const createCourse: API.OperationMethod<
  CreateCourseRequest,
  Course,
  CreateCourseError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateCourseRequest,
  output: Course,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteCourseError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Delete course [Legacy API — https://docs.whop.com/api-reference] Permanently delete a course and all of its chapters, lessons, and student progress. Required permissions: - `courses:update` */
export const deleteCourse: API.OperationMethod<
  DeleteCourseRequest,
  DeleteCourseResponse,
  DeleteCourseError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteCourseRequest,
  output: DeleteCourseResponse,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListCourseError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List courses [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of courses, filtered by either an experience or a company. Required permissions: - `courses:read` */
export const listCourse: API.PaginatedOperationMethod<
  ListCourseRequest,
  ListCourseResponse,
  ListCourseError,
  WhopOpContext,
  CourseListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListCourseRequest,
    output: ListCourseResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveCourseError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve course [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing course. Required permissions: - `courses:read` */
export const retrieveCourse: API.OperationMethod<
  RetrieveCourseRequest,
  Course,
  RetrieveCourseError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveCourseRequest,
  output: Course,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateCourseError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update course [Legacy API — https://docs.whop.com/api-reference] Update a course's title, description, visibility, thumbnail, or chapter ordering. Required permissions: - `courses:update` */
export const updateCourse: API.OperationMethod<
  UpdateCourseRequest,
  Course,
  UpdateCourseError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateCourseRequest,
  output: Course,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
