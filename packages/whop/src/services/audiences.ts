// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export interface AddAudiencePeopleRequest {
  /** Audience ID, prefixed `adaud_`. */
  id: string;
  /** The new customer CSV — a file id (`file_...`) returned by `POST /files`. Its headers must match the audience's saved column mapping. */
  file_id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const AddAudiencePeopleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    file_id: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/audiences/{id}/add_people", code: 200 }),
  ),
).annotate({
  identifier: "AddAudiencePeopleRequest",
}) as any as S.Schema<AddAudiencePeopleRequest>;

/** `custom` = a customer list (uploaded, or built from saved People filters); `lookalike` = Meta lookalike built from a custom audience. */
export type AudienceAudienceType = "custom" | "lookalike";
export const AudienceAudienceType = /*@__PURE__*/ S.String;

/** The ad platform that provided the match-rate estimate. */
export type AudienceMatchRatePlatform = "meta";
export const AudienceMatchRatePlatform = /*@__PURE__*/ S.String;

/** Availability of the estimated match rate. */
export type AudienceMatchRateStatus =
  | "calculating"
  | "available"
  | "unavailable";
export const AudienceMatchRateStatus = /*@__PURE__*/ S.String;

export interface AudienceMatchRate {
  /** Lower bound of the estimated match rate percentage. `null` until available. */
  lower_bound: number | null;
  /** The ad platform that provided the match-rate estimate. */
  platform: AudienceMatchRatePlatform;
  /** Availability of the estimated match rate. */
  status: AudienceMatchRateStatus | null;
  /** Upper bound of the estimated match rate percentage. `null` until available. */
  upper_bound: number | null;
}
export const AudienceMatchRate = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    lower_bound: S.NullOr(S.Number),
    platform: AudienceMatchRatePlatform,
    status: S.NullOr(AudienceMatchRateStatus),
    upper_bound: S.NullOr(S.Number),
  }),
).annotate({
  identifier: "AudienceMatchRate",
}) as any as S.Schema<AudienceMatchRate>;

export type AudienceMatchRatesList = Array<AudienceMatchRate>;
export const AudienceMatchRatesList = /*@__PURE__*/ S.Array(
  AudienceMatchRate,
) as any as S.Schema<AudienceMatchRatesList>;

export type AudiencePlatformAudienceIdsList = Array<string>;
export const AudiencePlatformAudienceIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AudiencePlatformAudienceIdsList>;

/** Where members come from. `csv_upload` = an uploaded customer list; `people_filter` = built from saved People filters. See `auto_refresh` for whether a `people_filter` audience keeps updating. */
export type AudienceSourceType = "csv_upload" | "people_filter";
export const AudienceSourceType = /*@__PURE__*/ S.String;

/** Current state of the audience import. `syncing` means Whop is sending matched rows to connected ad accounts. When status is `partial` or `failed`, `error_message` explains what went wrong. */
export type AudienceStatus =
  | "pending"
  | "processing"
  | "syncing"
  | "ready"
  | "partial"
  | "failed";
export const AudienceStatus = /*@__PURE__*/ S.String;

export interface Audience {
  /** `custom` = a customer list (uploaded, or built from saved People filters); `lookalike` = Meta lookalike built from a custom audience. */
  audience_type: AudienceAudienceType;
  /** Whether membership keeps updating. `true` rebuilds it from the saved filters twice a day, so people join and leave as they start and stop matching. `false` keeps whoever matched when it was built and never rebuilds. Always `false` for uploaded lists and lookalikes. */
  auto_refresh: boolean;
  /** When the audience was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Processing error message. `null` unless processing is partial or failed. */
  error_message: string | null;
  /** For audiences built from People filters: the filters that define membership, keyed exactly as `GET /people` accepts them — for example `{"os": "iOS", "country": "US"}`. `null` for uploaded lists and lookalikes. */
  filters: unknown | null;
  /** Audience ID, prefixed `adaud_`. */
  id: string;
  /** When the audience membership was last rebuilt, as an ISO 8601 timestamp. `null` until the first build completes. */
  last_refreshed_at: string | null;
  /** For lookalikes: the upper bound of the similarity band as a fraction (0.02 = top 2%). `null` for custom audiences. */
  lookalike_ratio: number | null;
  /** For lookalikes: the lower bound of the similarity band as a fraction. `null` for custom audiences and first-tier lookalikes. */
  lookalike_starting_ratio: number | null;
  match_rates: AudienceMatchRatesList;
  /** Members successfully uploaded to connected ad accounts. Always 0 for lookalikes. */
  matched_rows: number;
  /** Audience display name. */
  name: string;
  platform_audience_ids: AudiencePlatformAudienceIdsList;
  /** Members processed from the source so far. Always 0 for lookalikes. */
  processed_rows: number;
  /** Processing progress from 0 to 100. */
  progress_percent: number;
  /** For lookalikes: the audience this lookalike was built from. `null` for custom audiences. */
  source_audience_id: string | null;
  /** Where members come from. `csv_upload` = an uploaded customer list; `people_filter` = built from saved People filters. See `auto_refresh` for whether a `people_filter` audience keeps updating. */
  source_type: AudienceSourceType;
  /** Current state of the audience import. `syncing` means Whop is sending matched rows to connected ad accounts. When status is `partial` or `failed`, `error_message` explains what went wrong. */
  status: AudienceStatus;
  /** Total members detected in the source — CSV rows for uploaded lists, matching people for automatic audiences. Always 0 for lookalikes. */
  total_rows: number;
  /** When the audience was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
}
export const Audience = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    audience_type: AudienceAudienceType,
    auto_refresh: S.Boolean,
    created_at: S.String,
    error_message: S.NullOr(S.String),
    filters: S.NullOr(S.Unknown),
    id: S.String,
    last_refreshed_at: S.NullOr(S.String),
    lookalike_ratio: S.NullOr(S.Number),
    lookalike_starting_ratio: S.NullOr(S.Number),
    match_rates: AudienceMatchRatesList,
    matched_rows: S.Number,
    name: S.String,
    platform_audience_ids: AudiencePlatformAudienceIdsList,
    processed_rows: S.Number,
    progress_percent: S.Number,
    source_audience_id: S.NullOr(S.String),
    source_type: AudienceSourceType,
    status: AudienceStatus,
    total_rows: S.Number,
    updated_at: S.String,
  }),
).annotate({ identifier: "Audience" }) as any as S.Schema<Audience>;

/** What to create. Defaults to `custom` (CSV upload). */
export type CreateAudienceRequestAudienceType = "custom" | "lookalike";
export const CreateAudienceRequestAudienceType = /*@__PURE__*/ S.String;

/** Custom audiences only. Maps supported identity fields to CSV column headers. Map at least one of `email` or `phone`. */
export interface CreateAudienceRequestColumnMapping {
  /** CSV header for ISO 3166-1 alpha-2 country codes, such as `US`. */
  country?: string;
  /** CSV header for email addresses. */
  email?: string;
  /** CSV header for first names. */
  first_name?: string;
  /** CSV header for last names. */
  last_name?: string;
  /** CSV header for each customer's lifetime value — a non-negative number, currency symbols allowed. When mapped, Meta creates the audience as value-based, so lookalikes built from it favor people similar to the highest-value customers. */
  ltv?: string;
  /** CSV header for phone numbers. */
  phone?: string;
}
export const CreateAudienceRequestColumnMapping = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    country: S.optional(S.String),
    email: S.optional(S.String),
    first_name: S.optional(S.String),
    last_name: S.optional(S.String),
    ltv: S.optional(S.String),
    phone: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateAudienceRequestColumnMapping",
}) as any as S.Schema<CreateAudienceRequestColumnMapping>;

export interface CreateAudienceRequest {
  /** Account ID, prefixed `biz_`. */
  account_id: string;
  /** What to create. Defaults to `custom` (CSV upload). */
  audience_type?: CreateAudienceRequestAudienceType | (string & {});
  /** Filter audiences only, and set only at creation. `true` (the default) rebuilds membership from the filters twice a day. `false` keeps whoever matched at creation and never rebuilds. */
  auto_refresh?: boolean;
  /** Custom audiences only. Maps supported identity fields to CSV column headers. Map at least one of `email` or `phone`. */
  column_mapping?: CreateAudienceRequestColumnMapping;
  /** Lookalikes only. Number of lookalike audiences to create (1–6). */
  count?: number;
  /** Custom audiences only. The uploaded customer CSV — a file id (`file_...`) returned by `POST /files`. */
  file_id?: string;
  /** Filter audiences only. The People filters that define membership, keyed exactly as `GET /people` accepts them — for example `{"os": "iOS", "country": "US"}`. Date filters must be rolling windows — `first_seen_within_days` or `last_seen_within_days` — so the audience re-anchors on every refresh; fixed dates such as `first_seen_after` are rejected. Source values are canonical source paths (`whop:<campaign>:<group>:<ad>`, `ext:<platform>:...`, `referrer:<domain>`, `direct`), exact or with a trailing `:*` wildcard. */
  filters?: unknown;
  /** Audience display name. Required for custom audiences; lookalike names are generated from the source audience. */
  name?: string;
  /** Lookalikes only. Total similarity reach as a whole percent (1–20), sliced evenly across `count` — must be divisible by `count`. */
  percentage?: number;
  /** Lookalikes only. The ready custom audience (`adaud_`) to build from; it needs at least 100 matched people. */
  source_audience_id?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateAudienceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String,
    audience_type: S.optional(CreateAudienceRequestAudienceType),
    auto_refresh: S.optional(S.Boolean),
    column_mapping: S.optional(CreateAudienceRequestColumnMapping),
    count: S.optional(S.Number),
    file_id: S.optional(S.String),
    filters: S.optional(S.Unknown),
    name: S.optional(S.String),
    percentage: S.optional(S.Number),
    source_audience_id: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/audiences", code: 200 })),
).annotate({
  identifier: "CreateAudienceRequest",
}) as any as S.Schema<CreateAudienceRequest>;

export type CreateAudienceResponseBodyCase1DataList = Array<Audience>;
export const CreateAudienceResponseBodyCase1DataList = /*@__PURE__*/ S.Array(
  Audience,
) as any as S.Schema<CreateAudienceResponseBodyCase1DataList>;

export interface CreateAudienceResponseBodyCase1 {
  data: CreateAudienceResponseBodyCase1DataList;
}
export const CreateAudienceResponseBodyCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: CreateAudienceResponseBodyCase1DataList,
  }),
).annotate({
  identifier: "CreateAudienceResponseBodyCase1",
}) as any as S.Schema<CreateAudienceResponseBodyCase1>;

export type CreateAudienceResponseBody =
  | Audience
  | CreateAudienceResponseBodyCase1;
export const CreateAudienceResponseBody =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateAudienceResponseBody>;

export type CreateAudienceResponse = CreateAudienceResponseBody;
export const CreateAudienceResponse = /*@__PURE__*/ S.suspend(() =>
  CreateAudienceResponseBody.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "CreateAudienceResponse",
}) as any as S.Schema<CreateAudienceResponse>;

export interface DeleteAudienceRequest {
  /** Audience ID, prefixed `adaud_`. */
  id: string;
}
export const DeleteAudienceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/audiences/{id}", code: 200 })),
).annotate({
  identifier: "DeleteAudienceRequest",
}) as any as S.Schema<DeleteAudienceRequest>;

export interface DeleteAudienceResponse {
  success: boolean;
}
export const DeleteAudienceResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    success: S.Boolean,
  }),
).annotate({
  identifier: "DeleteAudienceResponse",
}) as any as S.Schema<DeleteAudienceResponse>;

export type ListAudiencesRequestAudienceType = "custom" | "lookalike";
export const ListAudiencesRequestAudienceType = /*@__PURE__*/ S.String;

export type ListAudiencesRequestSourceType = "csv_upload" | "people_filter";
export const ListAudiencesRequestSourceType = /*@__PURE__*/ S.String;

export interface ListAudiencesRequest {
  /** Account ID, prefixed `biz_`. */
  account_id: string;
  /** Audience ID, prefixed `adaud_`, used to filter the response to one audience. */
  audience_id?: string;
  /** Filter by audience type: `custom` (uploaded lists) or `lookalike`. */
  audience_type?: ListAudiencesRequestAudienceType | (string & {});
  /** Filter by member source: `csv_upload` (uploaded lists) or `people_filter` (automatic audiences built from saved People filters). */
  source_type?: ListAudiencesRequestSourceType | (string & {});
  /** Number of audiences to return. Defaults to 20; maximum 100. */
  first?: number;
  /** Cursor for the next page of audiences. */
  after?: string;
}
export const ListAudiencesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.String.pipe(T.Query()),
    audience_id: S.optional(S.String.pipe(T.Query())),
    audience_type: S.optional(ListAudiencesRequestAudienceType.pipe(T.Query())),
    source_type: S.optional(ListAudiencesRequestSourceType.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/audiences", code: 200 })),
).annotate({
  identifier: "ListAudiencesRequest",
}) as any as S.Schema<ListAudiencesRequest>;

export type ListAudiencesResponseDataList = Array<Audience>;
export const ListAudiencesResponseDataList = /*@__PURE__*/ S.Array(
  Audience,
) as any as S.Schema<ListAudiencesResponseDataList>;

export interface ListAudiencesResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListAudiencesResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListAudiencesResponsePageInfo",
}) as any as S.Schema<ListAudiencesResponsePageInfo>;

export interface ListAudiencesResponse {
  data: ListAudiencesResponseDataList;
  page_info: ListAudiencesResponsePageInfo;
}
export const ListAudiencesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListAudiencesResponseDataList,
    page_info: ListAudiencesResponsePageInfo,
  }),
).annotate({
  identifier: "ListAudiencesResponse",
}) as any as S.Schema<ListAudiencesResponse>;

export interface UpdateAudienceRequest {
  /** Audience ID, prefixed `adaud_`. */
  id: string;
  /** Replaces the People filters that define membership. The whole definition is replaced rather than merged, so send every filter you want to keep — a filter you leave out stops applying. Keys and values are the ones `GET /people` accepts, such as an `os` of `iOS` or a `country` of `US`, and at least one filter is required. Date filters must be rolling windows — `first_seen_within_days` or `last_seen_within_days` — so the audience re-anchors every time it rebuilds; fixed dates such as `first_seen_after` are rejected, as is `audience_id`. An array value holds at most 500 items, and each value at most 10 KB. Only an audience with a `source_type` of `people_filter` and `auto_refresh` of `true` accepts filters: an uploaded list has no filters to replace, and with auto refresh off the audience keeps the people it matched when it was built, so create a new audience instead. */
  filters?: unknown;
  /** New audience display name. A blank value is ignored rather than clearing the name. */
  name?: string;
}
export const UpdateAudienceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    filters: S.optional(S.Unknown),
    name: S.optional(S.String),
  }).pipe(T.Http({ method: "PATCH", uri: "/audiences/{id}", code: 200 })),
).annotate({
  identifier: "UpdateAudienceRequest",
}) as any as S.Schema<UpdateAudienceRequest>;

export type AddAudiencePeopleError = Conflict | WhopOpError;
/** Add People Adds users from a new CSV file to an existing uploaded custom audience. The file uses the audience's saved column mapping, processing happens in the background, and existing audience members remain unchanged. */
export const addAudiencePeople: API.OperationMethod<
  AddAudiencePeopleRequest,
  Audience,
  AddAudiencePeopleError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AddAudiencePeopleRequest,
  output: Audience,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreateAudienceError = Conflict | WhopOpError;
/** Create Audience Creates an audience. Default (`audience_type` omitted or `custom`): creates one audience from an uploaded customer identity CSV file (`name`, `column_mapping`, and `file_id` required) and starts processing it; responds with the audience object. With `filters`: creates an audience from saved People filters (`name` required) — membership is built from the account's People data, and `auto_refresh` decides whether it keeps tracking the filters or keeps whoever matched at creation. With `audience_type: lookalike`: creates a ladder of Meta lookalike audiences from an existing ready custom audience (`source_audience_id`, `count`, and `percentage` required) — `count` equal similarity bands slicing the top `percentage`% (3 audiences at 6% = 0–2%, 2–4%, 4–6%), each returned as its own audience in a `{ data: [...] }` envelope. */
export const createAudience: API.OperationMethod<
  CreateAudienceRequest,
  CreateAudienceResponse,
  CreateAudienceError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAudienceRequest,
  output: CreateAudienceResponse,
  errors: [Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteAudienceError = WhopOpError;
/** Delete Audience Deletes an audience so it is no longer available for targeting. */
export const deleteAudience: API.OperationMethod<
  DeleteAudienceRequest,
  DeleteAudienceResponse,
  DeleteAudienceError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteAudienceRequest,
  output: DeleteAudienceResponse,
  errors: [],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListAudiencesError = WhopOpError;
/** List Audiences Lists uploaded customer-list audiences for an account. Pass `audience_id` to return a specific audience. */
export const listAudiences: API.PaginatedOperationMethod<
  ListAudiencesRequest,
  ListAudiencesResponse,
  ListAudiencesError,
  WhopOpContext,
  Audience
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListAudiencesRequest,
    output: ListAudiencesResponse,
    errors: [],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type UpdateAudienceError = WhopOpError;
/** Update Audience Renames an audience. For an audience built from People filters that keeps itself up to date, pass `filters` to replace them, which rebuilds membership immediately. Whether an audience auto refreshes is set when it is created. */
export const updateAudience: API.OperationMethod<
  UpdateAudienceRequest,
  Audience,
  UpdateAudienceError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateAudienceRequest,
  output: Audience,
  errors: [],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
