// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface RetrievePayoutAccountRequest {
  /** The unique identifier of the payout account to retrieve. */
  id: string;
}
export const RetrievePayoutAccountRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/payout_accounts/{id}", code: 200 })),
).annotate({
  identifier: "RetrievePayoutAccountRequest",
}) as any as S.Schema<RetrievePayoutAccountRequest>;

/** The physical address associated with this payout account */
export interface PayoutAccountAddress {
  /** The city of the address. */
  city: string | null;
  /** The country of the address. */
  country: string | null;
  /** The line 1 of the address. */
  line1: string | null;
  /** The line 2 of the address. */
  line2: string | null;
  /** The postal code of the address. */
  postal_code: string | null;
  /** The state of the address. */
  state: string | null;
}
export const PayoutAccountAddress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    city: S.NullOr(S.String),
    country: S.NullOr(S.String),
    line1: S.NullOr(S.String),
    line2: S.NullOr(S.String),
    postal_code: S.NullOr(S.String),
    state: S.NullOr(S.String),
  }),
).annotate({
  identifier: "PayoutAccountAddress",
}) as any as S.Schema<PayoutAccountAddress>;

/** The business representative for this payout account */
export interface PayoutAccountBusinessRepresentative {
  /** The date of birth of the business representative in ISO 8601 format (YYYY-MM-DD). */
  date_of_birth: string | null;
  /** The first name of the business representative. */
  first_name: string | null;
  /** The last name of the business representative. */
  last_name: string | null;
  /** The middle name of the business representative. */
  middle_name: string | null;
}
export const PayoutAccountBusinessRepresentative = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    date_of_birth: S.NullOr(S.String),
    first_name: S.NullOr(S.String),
    last_name: S.NullOr(S.String),
    middle_name: S.NullOr(S.String),
  }),
).annotate({
  identifier: "PayoutAccountBusinessRepresentative",
}) as any as S.Schema<PayoutAccountBusinessRepresentative>;

/** An error code for a verification attempt. */
export type VerificationErrorCodes =
  | "abandoned"
  | "consent_declined"
  | "country_not_supported"
  | "device_not_supported"
  | "document_expired"
  | "document_type_not_supported"
  | "document_unverified_other"
  | "email_unverified_other"
  | "email_verification_declined"
  | "id_number_insufficient_document_data"
  | "id_number_mismatch"
  | "id_number_unverified_other"
  | "phone_unverified_other"
  | "phone_verification_declined"
  | "selfie_document_missing_photo"
  | "selfie_face_mismatch"
  | "selfie_manipulated"
  | "selfie_unverified_other"
  | "under_supported_age";
export const VerificationErrorCodes = /*@__PURE__*/ S.String;

/** A status for a verification. */
export type VerificationStatuses =
  | "requires_input"
  | "processing"
  | "verified"
  | "canceled"
  | "created"
  | "started"
  | "submitted"
  | "approved"
  | "declined"
  | "resubmission_requested"
  | "expired"
  | "abandoned"
  | "review"
  | "action_required"
  | "manual_review";
export const VerificationStatuses = /*@__PURE__*/ S.String;

/** The latest verification for the connected account. */
export interface PayoutAccountLatestVerification {
  /** The numeric id of the verification record. */
  id: string;
  /** The most recent error code returned during verification. Null if no error has occurred. */
  last_error_code: VerificationErrorCodes | null;
  /** A human-readable explanation of the most recent verification error. Null if no error has occurred. */
  last_error_reason: string | null;
  /** The current status of this verification session. */
  status: VerificationStatuses;
}
export const PayoutAccountLatestVerification = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    last_error_code: S.NullOr(VerificationErrorCodes),
    last_error_reason: S.NullOr(S.String),
    status: VerificationStatuses,
  }),
).annotate({
  identifier: "PayoutAccountLatestVerification",
}) as any as S.Schema<PayoutAccountLatestVerification>;

/** The granular calculated statuses reflecting payout account KYC and payout readiness. */
export type PayoutAccountCalculatedStatuses =
  | "connected"
  | "disabled"
  | "action_required"
  | "pending_verification"
  | "verification_failed"
  | "manual_review"
  | "denied"
  | "not_started"
  | "blocked_by_parent";
export const PayoutAccountCalculatedStatuses = /*@__PURE__*/ S.String;

/** An object representing an account used for payouts. */
export interface PayoutAccount {
  /** The physical address associated with this payout account */
  address: PayoutAccountAddress | null;
  /** The company's legal name */
  business_name: string | null;
  /** The business representative for this payout account */
  business_representative: PayoutAccountBusinessRepresentative | null;
  /** The email address of the representative */
  email: string | null;
  /** The unique identifier for the payout account. */
  id: string;
  /** The latest verification for the connected account. */
  latest_verification: PayoutAccountLatestVerification | null;
  /** The business representative's phone */
  phone: string | null;
  /** The granular calculated status of the payout account reflecting its current KYC and payout readiness state. */
  status: PayoutAccountCalculatedStatuses | null;
}
export const PayoutAccount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.NullOr(PayoutAccountAddress),
    business_name: S.NullOr(S.String),
    business_representative: S.NullOr(PayoutAccountBusinessRepresentative),
    email: S.NullOr(S.String),
    id: S.String,
    latest_verification: S.NullOr(PayoutAccountLatestVerification),
    phone: S.NullOr(S.String),
    status: S.NullOr(PayoutAccountCalculatedStatuses),
  }),
).annotate({ identifier: "PayoutAccount" }) as any as S.Schema<PayoutAccount>;

export type RetrievePayoutAccountError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve payout account [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing payout account. Required permissions: - `payout:account:read` */
export const retrievePayoutAccount: API.OperationMethod<
  RetrievePayoutAccountRequest,
  PayoutAccount,
  RetrievePayoutAccountError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrievePayoutAccountRequest,
  output: PayoutAccount,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
