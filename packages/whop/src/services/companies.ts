// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** Actions covered by this statement */
export type CreateApiKeyCompanyRequestPermissionsItemActionsList =
  Array<string>;
export const CreateApiKeyCompanyRequestPermissionsItemActionsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateApiKeyCompanyRequestPermissionsItemActionsList>;

/** Resource identifiers. Can look like 'biz_xxxx' or 'biz_xxx|pass_*|exp_xxx' or 'biz_xxx|app_xxx' or 'biz_xxx|pass_xxx|exp_xxx' or 'biz_xxx|pass_xxx' or 'biz_xxx|pass_*' */
export type CreateApiKeyCompanyRequestPermissionsItemResourcesList =
  Array<string>;
export const CreateApiKeyCompanyRequestPermissionsItemResourcesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateApiKeyCompanyRequestPermissionsItemResourcesList>;

/** Input for a single permissions statement */
export interface CreateApiKeyCompanyRequestPermissionsItem {
  /** Actions covered by this statement */
  actions: CreateApiKeyCompanyRequestPermissionsItemActionsList;
  /** Whether the actions are granted or denied */
  grant: boolean;
  /** Resource identifiers. Can look like 'biz_xxxx' or 'biz_xxx|pass_*|exp_xxx' or 'biz_xxx|app_xxx' or 'biz_xxx|pass_xxx|exp_xxx' or 'biz_xxx|pass_xxx' or 'biz_xxx|pass_*' */
  resources: CreateApiKeyCompanyRequestPermissionsItemResourcesList;
}
export const CreateApiKeyCompanyRequestPermissionsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      actions: CreateApiKeyCompanyRequestPermissionsItemActionsList,
      grant: S.Boolean,
      resources: CreateApiKeyCompanyRequestPermissionsItemResourcesList,
    }),
  ).annotate({
    identifier: "CreateApiKeyCompanyRequestPermissionsItem",
  }) as any as S.Schema<CreateApiKeyCompanyRequestPermissionsItem>;

/** Granular permission statements defining which actions this API key can perform. Either permissions or role must be provided. */
export type CreateApiKeyCompanyRequestPermissionsList =
  Array<CreateApiKeyCompanyRequestPermissionsItem>;
export const CreateApiKeyCompanyRequestPermissionsList = /*@__PURE__*/ S.Array(
  CreateApiKeyCompanyRequestPermissionsItem,
) as any as S.Schema<CreateApiKeyCompanyRequestPermissionsList>;

/** The different system roles that can be assigned. */
export type PermissionSystemRoles =
  | "owner"
  | "admin"
  | "moderator"
  | "sales_manager"
  | "advertiser";
export const PermissionSystemRoles = /*@__PURE__*/ S.String;

export interface CreateApiKeyCompanyRequest {
  /** The unique identifier of the parent platform company (e.g. 'biz_xxx'). */
  parent_company_id: string;
  /** The unique identifier of the connected account to create the API key for (e.g. 'biz_xxx'). */
  child_company_id: string;
  /** A human-readable name for the API key, such as 'Production API Key'. */
  name?: string | null;
  /** Granular permission statements defining which actions this API key can perform. Either permissions or role must be provided. */
  permissions?: CreateApiKeyCompanyRequestPermissionsList | null;
  /** A system role to inherit permissions from (e.g. owner, admin, moderator). Either role or permissions must be provided. */
  role?: PermissionSystemRoles | (string & {}) | null;
}
export const CreateApiKeyCompanyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    parent_company_id: S.String.pipe(T.Label()),
    child_company_id: S.String,
    name: S.optional(S.NullOr(S.String)),
    permissions: S.optional(
      S.NullOr(CreateApiKeyCompanyRequestPermissionsList),
    ),
    role: S.optional(S.NullOr(PermissionSystemRoles)),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/companies/{parent_company_id}/api_keys",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateApiKeyCompanyRequest",
}) as any as S.Schema<CreateApiKeyCompanyRequest>;

export interface CreateApiKeyCompanyResponse {
  /** The unique identifier for the authorized api key. */
  id: string;
  /** A user set name to identify an API key */
  name: string | null;
  /** The secret key used to authenticate requests. Only returned at creation time. */
  secret_key: string | Redacted.Redacted<string>;
}
export const CreateApiKeyCompanyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    secret_key: S.String.pipe(T.SensitiveValue({})),
  }),
).annotate({
  identifier: "CreateApiKeyCompanyResponse",
}) as any as S.Schema<CreateApiKeyCompanyResponse>;

/** The available countries on the platform */
export type Countries =
  | "us"
  | "ca"
  | "gb"
  | "ab"
  | "af"
  | "al"
  | "dz"
  | "as"
  | "ad"
  | "ao"
  | "ai"
  | "aq"
  | "ag"
  | "ar"
  | "am"
  | "aw"
  | "au"
  | "at"
  | "az"
  | "bs"
  | "bh"
  | "bd"
  | "bb"
  | "by"
  | "be"
  | "bz"
  | "bj"
  | "bm"
  | "bt"
  | "bo"
  | "ba"
  | "bw"
  | "br"
  | "io"
  | "vg"
  | "bn"
  | "bg"
  | "bf"
  | "bi"
  | "kh"
  | "cm"
  | "cv"
  | "ky"
  | "cf"
  | "td"
  | "cl"
  | "cn"
  | "cx"
  | "cc"
  | "co"
  | "km"
  | "cg"
  | "ck"
  | "cr"
  | "ci"
  | "hr"
  | "cu"
  | "cw"
  | "cy"
  | "cz"
  | "cd"
  | "dk"
  | "dj"
  | "dm"
  | "do"
  | "ec"
  | "eg"
  | "sv"
  | "gq"
  | "ee"
  | "et"
  | "fk"
  | "fo"
  | "fj"
  | "fi"
  | "fr"
  | "gf"
  | "pf"
  | "ga"
  | "gm"
  | "ge"
  | "de"
  | "gh"
  | "gi"
  | "gr"
  | "gl"
  | "gd"
  | "gp"
  | "gu"
  | "gt"
  | "gg"
  | "gn"
  | "gw"
  | "gy"
  | "ht"
  | "hn"
  | "hk"
  | "hu"
  | "is"
  | "in"
  | "id"
  | "iq"
  | "ie"
  | "im"
  | "il"
  | "it"
  | "jm"
  | "sj"
  | "jp"
  | "je"
  | "jo"
  | "kz"
  | "ke"
  | "ki"
  | "xk"
  | "kw"
  | "kg"
  | "la"
  | "lv"
  | "lb"
  | "ls"
  | "lr"
  | "ly"
  | "li"
  | "lt"
  | "lu"
  | "mo"
  | "mk"
  | "mg"
  | "mw"
  | "my"
  | "mv"
  | "ml"
  | "mt"
  | "mh"
  | "mq"
  | "mr"
  | "mu"
  | "yt"
  | "mx"
  | "fm"
  | "md"
  | "mc"
  | "mn"
  | "me"
  | "ms"
  | "ma"
  | "mz"
  | "mm"
  | "na"
  | "nr"
  | "np"
  | "nl"
  | "an"
  | "nc"
  | "nz"
  | "ni"
  | "ne"
  | "ng"
  | "nu"
  | "mp"
  | "no"
  | "om"
  | "pk"
  | "pw"
  | "ps"
  | "pa"
  | "pg"
  | "py"
  | "pe"
  | "ph"
  | "pn"
  | "pl"
  | "pt"
  | "pr"
  | "qa"
  | "re"
  | "ro"
  | "ru"
  | "rw"
  | "bl"
  | "sh"
  | "kn"
  | "lc"
  | "mf"
  | "pm"
  | "vc"
  | "ws"
  | "sm"
  | "st"
  | "sa"
  | "sn"
  | "rs"
  | "sc"
  | "sl"
  | "sg"
  | "sx"
  | "sk"
  | "si"
  | "sb"
  | "so"
  | "za"
  | "kr"
  | "ss"
  | "es"
  | "lk"
  | "sd"
  | "sr"
  | "sz"
  | "se"
  | "ch"
  | "tw"
  | "tj"
  | "tz"
  | "th"
  | "tl"
  | "tg"
  | "tk"
  | "to"
  | "tt"
  | "tn"
  | "tr"
  | "tm"
  | "tc"
  | "tv"
  | "vi"
  | "ug"
  | "ua"
  | "ae"
  | "um"
  | "uy"
  | "uz"
  | "vu"
  | "va"
  | "ve"
  | "vn"
  | "wf"
  | "eh"
  | "ye"
  | "zm"
  | "zw"
  | "sy";
export const Countries = /*@__PURE__*/ S.String;

/** The company's logo image. Accepts PNG, JPEG, or GIF format. */
export interface CreateCompanyRequestLogo {
  /** The ID of an existing file object. */
  id: string;
}
export const CreateCompanyRequestLogo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "CreateCompanyRequestLogo",
}) as any as S.Schema<CreateCompanyRequestLogo>;

/** A key-value JSON object of custom metadata to store on the company. */
export type CreateCompanyRequestMetadataMap = {
  [key: string]: unknown | undefined;
};
export const CreateCompanyRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateCompanyRequestMetadataMap>;

export interface CreateCompanyRequest {
  /** The country the company is located in. Defaults to the parent company's country for connected accounts, or the owner's IP-derived country. */
  country?: Countries | (string & {}) | null;
  /** A promotional pitch displayed to potential customers on the company's store page. */
  description?: string | null;
  /** The email address of the user who will own the connected account. Required when parent_company_id is provided. */
  email?: string | null;
  /** The company's logo image. Accepts PNG, JPEG, or GIF format. */
  logo?: CreateCompanyRequestLogo | null;
  /** A key-value JSON object of custom metadata to store on the company. */
  metadata?: CreateCompanyRequestMetadataMap | null;
  /** The unique identifier of the parent platform company. When provided, creates a connected account under that platform. Omit to create a company for the current user. */
  parent_company_id?: string | null;
  /** Whether Whop sends transactional emails to customers on behalf of this company. Only applies when creating a connected account. */
  send_customer_emails?: boolean | null;
  /** The display name of the company shown to customers. */
  title: string;
}
export const CreateCompanyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    country: S.optional(S.NullOr(Countries)),
    description: S.optional(S.NullOr(S.String)),
    email: S.optional(S.NullOr(S.String)),
    logo: S.optional(S.NullOr(CreateCompanyRequestLogo)),
    metadata: S.optional(S.NullOr(CreateCompanyRequestMetadataMap)),
    parent_company_id: S.optional(S.NullOr(S.String)),
    send_customer_emails: S.optional(S.NullOr(S.Boolean)),
    title: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/companies", code: 200 })),
).annotate({
  identifier: "CreateCompanyRequest",
}) as any as S.Schema<CreateCompanyRequest>;

/** The product featured for affiliates to promote on this company's affiliate page. Null if none is configured. */
export interface CompanyFeaturedAffiliateProduct {
  /** The unique identifier for the product. */
  id: string;
  /** The display name of the product shown to customers. Maximum 50 characters. */
  name: string;
}
export const CompanyFeaturedAffiliateProduct = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
  }),
).annotate({
  identifier: "CompanyFeaturedAffiliateProduct",
}) as any as S.Schema<CompanyFeaturedAffiliateProduct>;

/** The company's logo. */
export interface CompanyLogo {
  /** A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps. */
  url: string | null;
}
export const CompanyLogo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.NullOr(S.String),
  }),
).annotate({ identifier: "CompanyLogo" }) as any as S.Schema<CompanyLogo>;

/** A key-value JSON object of custom metadata for this company, managed by the platform that created the account. */
export type CompanyMetadataMap = { [key: string]: unknown | undefined };
export const CompanyMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CompanyMetadataMap>;

/** The user who owns and has full administrative control over this company. */
export interface CompanyOwnerUser {
  /** The unique identifier for the user. */
  id: string;
  /** The user's display name shown on their public profile. */
  name: string | null;
  /** The user's unique username shown on their public profile. */
  username: string;
}
export const CompanyOwnerUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({
  identifier: "CompanyOwnerUser",
}) as any as S.Schema<CompanyOwnerUser>;

/** The different websites you can have social links for */
export type SocialLinkWebsites =
  | "x"
  | "instagram"
  | "facebook"
  | "tiktok"
  | "youtube"
  | "linkedin"
  | "twitch"
  | "website"
  | "custom";
export const SocialLinkWebsites = /*@__PURE__*/ S.String;

/** A social link attached to a resource on the site. */
export interface CompanySocialLinksItem {
  /** The unique identifier for the social link. */
  id: string;
  /** The URL of the social media profile or external link. */
  url: string;
  /** The website */
  website: SocialLinkWebsites;
}
export const CompanySocialLinksItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    url: S.String,
    website: SocialLinkWebsites,
  }),
).annotate({
  identifier: "CompanySocialLinksItem",
}) as any as S.Schema<CompanySocialLinksItem>;

/** The list of social media accounts and external links associated with this company. */
export type CompanySocialLinksList = Array<CompanySocialLinksItem>;
export const CompanySocialLinksList = /*@__PURE__*/ S.Array(
  CompanySocialLinksItem,
) as any as S.Schema<CompanySocialLinksList>;

/** A company is a seller on Whop. Companies own products, manage members, and receive payouts. */
export interface Company {
  /** Guidelines and instructions provided to affiliates explaining how to promote this company's products. */
  affiliate_instructions: string | null;
  /** The datetime the company was created. */
  created_at: string;
  /** A promotional pitch written by the company creator, displayed to potential customers on the store page. */
  description: string | null;
  /** The product featured for affiliates to promote on this company's affiliate page. Null if none is configured. */
  featured_affiliate_product: CompanyFeaturedAffiliateProduct | null;
  /** The unique identifier for the company. */
  id: string;
  /** The company's logo. */
  logo: CompanyLogo | null;
  /** The total number of users who currently hold active memberships across all of this company's products. */
  member_count: number;
  /** A key-value JSON object of custom metadata for this company, managed by the platform that created the account. */
  metadata: CompanyMetadataMap | null;
  /** The user who owns and has full administrative control over this company. */
  owner_user: CompanyOwnerUser;
  /** The total number of published customer reviews across all products for this company. */
  published_reviews_count: number;
  /** URL slug for the account's store page, e.g. `pickaxe` in whop.com/pickaxe. */
  route: string;
  /** Whether Whop sends transactional emails (receipts, updates) to customers on behalf of this company. */
  send_customer_emails: boolean;
  /** The list of social media accounts and external links associated with this company. */
  social_links: CompanySocialLinksList;
  /** The target audience for the company. Null if not set. */
  target_audience: string | null;
  /** The display name of the company shown to customers. */
  title: string;
  /** The datetime the company was last updated. */
  updated_at: string;
  /** Whether this company has been verified by Whop's trust and safety team. */
  verified: boolean;
}
export const Company = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    affiliate_instructions: S.NullOr(S.String),
    created_at: S.String,
    description: S.NullOr(S.String),
    featured_affiliate_product: S.NullOr(CompanyFeaturedAffiliateProduct),
    id: S.String,
    logo: S.NullOr(CompanyLogo),
    member_count: S.Number,
    metadata: S.NullOr(CompanyMetadataMap),
    owner_user: CompanyOwnerUser,
    published_reviews_count: S.Number,
    route: S.String,
    send_customer_emails: S.Boolean,
    social_links: CompanySocialLinksList,
    target_audience: S.NullOr(S.String),
    title: S.String,
    updated_at: S.String,
    verified: S.Boolean,
  }),
).annotate({ identifier: "Company" }) as any as S.Schema<Company>;

/** The direction of the sort. */
export type Direction = "asc" | "desc";
export const Direction = /*@__PURE__*/ S.String;

export interface ListCompanyRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  parent_company_id?: string;
  direction?: Direction | (string & {});
  created_before?: string;
  created_after?: string;
}
export const ListCompanyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    parent_company_id: S.optional(S.String.pipe(T.Query())),
    direction: S.optional(Direction.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/companies", code: 200 })),
).annotate({
  identifier: "ListCompanyRequest",
}) as any as S.Schema<ListCompanyRequest>;

/** The company's logo. */
export type CompanyListItemLogo = CompanyLogo;
export const CompanyListItemLogo = CompanyLogo;

/** A key-value JSON object of custom metadata for this company, managed by the platform that created the account. */
export type CompanyListItemMetadataMap = { [key: string]: unknown | undefined };
export const CompanyListItemMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CompanyListItemMetadataMap>;

/** The user who owns and has full administrative control over this company. */
export type CompanyListItemOwnerUser = CompanyOwnerUser;
export const CompanyListItemOwnerUser = CompanyOwnerUser;

/** A company is a seller on Whop. Companies own products, manage members, and receive payouts. */
export interface CompanyListItem {
  /** The datetime the company was created. */
  created_at: string;
  /** A promotional pitch written by the company creator, displayed to potential customers on the store page. */
  description: string | null;
  /** The unique identifier for the company. */
  id: string;
  /** The company's logo. */
  logo: CompanyLogo | null;
  /** The total number of users who currently hold active memberships across all of this company's products. */
  member_count: number;
  /** A key-value JSON object of custom metadata for this company, managed by the platform that created the account. */
  metadata: CompanyListItemMetadataMap | null;
  /** The user who owns and has full administrative control over this company. */
  owner_user: CompanyOwnerUser;
  /** The total number of published customer reviews across all products for this company. */
  published_reviews_count: number;
  /** URL slug for the account's store page, e.g. `pickaxe` in whop.com/pickaxe. */
  route: string;
  /** Whether Whop sends transactional emails (receipts, updates) to customers on behalf of this company. */
  send_customer_emails: boolean;
  /** The target audience for the company. Null if not set. */
  target_audience: string | null;
  /** The display name of the company shown to customers. */
  title: string;
  /** The datetime the company was last updated. */
  updated_at: string;
  /** Whether this company has been verified by Whop's trust and safety team. */
  verified: boolean;
}
export const CompanyListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    created_at: S.String,
    description: S.NullOr(S.String),
    id: S.String,
    logo: S.NullOr(CompanyLogo),
    member_count: S.Number,
    metadata: S.NullOr(CompanyListItemMetadataMap),
    owner_user: CompanyOwnerUser,
    published_reviews_count: S.Number,
    route: S.String,
    send_customer_emails: S.Boolean,
    target_audience: S.NullOr(S.String),
    title: S.String,
    updated_at: S.String,
    verified: S.Boolean,
  }),
).annotate({
  identifier: "CompanyListItem",
}) as any as S.Schema<CompanyListItem>;

/** A list of nodes. */
export type ListCompanyResponseDataList = Array<CompanyListItem>;
export const ListCompanyResponseDataList = /*@__PURE__*/ S.Array(
  CompanyListItem,
) as any as S.Schema<ListCompanyResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListCompanyResponse {
  /** A list of nodes. */
  data: ListCompanyResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListCompanyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListCompanyResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListCompanyResponse",
}) as any as S.Schema<ListCompanyResponse>;

export interface RetrieveCompanyRequest {
  /** The unique identifier or route slug of the company. */
  id: string;
}
export const RetrieveCompanyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/companies/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveCompanyRequest",
}) as any as S.Schema<RetrieveCompanyRequest>;

/** The company's banner image. Accepts PNG or JPEG format. */
export type UpdateCompanyRequestBannerImage = CreateCompanyRequestLogo;
export const UpdateCompanyRequestBannerImage = CreateCompanyRequestLogo;

/** The company's logo image. Accepts PNG, JPEG, or GIF format. */
export type UpdateCompanyRequestLogo = CreateCompanyRequestLogo;
export const UpdateCompanyRequestLogo = CreateCompanyRequestLogo;

/** The custom image for the social link */
export type UpdateCompanyRequestSocialLinksItemImage = CreateCompanyRequestLogo;
export const UpdateCompanyRequestSocialLinksItemImage =
  CreateCompanyRequestLogo;

/** Input for creating a social link for a company */
export interface UpdateCompanyRequestSocialLinksItem {
  /** The custom image for the social link */
  image?: CreateCompanyRequestLogo | null;
  /** The order of the social link */
  order?: string | null;
  /** The title of the social link */
  title?: string | null;
  /** The URL of the social link */
  url: string;
  /** The website this link is for */
  website: SocialLinkWebsites | (string & {});
  /** The order of the website social link */
  website_order?: string | null;
}
export const UpdateCompanyRequestSocialLinksItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    image: S.optional(S.NullOr(CreateCompanyRequestLogo)),
    order: S.optional(S.NullOr(S.String)),
    title: S.optional(S.NullOr(S.String)),
    url: S.String,
    website: SocialLinkWebsites,
    website_order: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "UpdateCompanyRequestSocialLinksItem",
}) as any as S.Schema<UpdateCompanyRequestSocialLinksItem>;

/** The social media links to display on the company's store page. Pass the full list of desired social links — any existing links not included will be removed. */
export type UpdateCompanyRequestSocialLinksList =
  Array<UpdateCompanyRequestSocialLinksItem>;
export const UpdateCompanyRequestSocialLinksList = /*@__PURE__*/ S.Array(
  UpdateCompanyRequestSocialLinksItem,
) as any as S.Schema<UpdateCompanyRequestSocialLinksList>;

export interface UpdateCompanyRequest {
  /** The unique identifier of the company to update. */
  id: string;
  /** Whether prospective affiliates must submit an application before they can promote this company. */
  affiliate_application_required?: boolean | null;
  /** Guidelines and instructions shown to affiliates explaining how to promote this company's products. */
  affiliate_instructions?: string | null;
  /** The company's banner image. Accepts PNG or JPEG format. */
  banner_image?: CreateCompanyRequestLogo | null;
  /** A promotional pitch displayed to potential customers on the company's store page. */
  description?: string | null;
  /** The ID of the product to feature on this company's affiliate page. Pass null to clear. */
  featured_affiliate_product_id?: string | null;
  /** The company's logo image. Accepts PNG, JPEG, or GIF format. */
  logo?: CreateCompanyRequestLogo | null;
  /** The unique URL slug for the company's store page. Must be lowercase and can include hyphens (e.g., 'my-company'). If not provided, the route will remain unchanged. */
  route?: string | null;
  /** Whether Whop sends transactional emails (receipts, renewals, cancelations) to customers on behalf of this company. */
  send_customer_emails?: boolean | null;
  /** The social media links to display on the company's store page. Pass the full list of desired social links — any existing links not included will be removed. */
  social_links?: UpdateCompanyRequestSocialLinksList | null;
  /** The target audience for this company (e.g., 'beginner day traders aged 18-25 looking to learn options'). */
  target_audience?: string | null;
  /** The display name of the company shown to customers. */
  title?: string | null;
}
export const UpdateCompanyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    affiliate_application_required: S.optional(S.NullOr(S.Boolean)),
    affiliate_instructions: S.optional(S.NullOr(S.String)),
    banner_image: S.optional(S.NullOr(CreateCompanyRequestLogo)),
    description: S.optional(S.NullOr(S.String)),
    featured_affiliate_product_id: S.optional(S.NullOr(S.String)),
    logo: S.optional(S.NullOr(CreateCompanyRequestLogo)),
    route: S.optional(S.NullOr(S.String)),
    send_customer_emails: S.optional(S.NullOr(S.Boolean)),
    social_links: S.optional(S.NullOr(UpdateCompanyRequestSocialLinksList)),
    target_audience: S.optional(S.NullOr(S.String)),
    title: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "PATCH", uri: "/companies/{id}", code: 200 })),
).annotate({
  identifier: "UpdateCompanyRequest",
}) as any as S.Schema<UpdateCompanyRequest>;

export type CreateApiKeyCompanyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create child company API key [Legacy API — https://docs.whop.com/api-reference] Create an API key for a connected account (child company) owned by a parent company. */
export const createApiKeyCompany: API.OperationMethod<
  CreateApiKeyCompanyRequest,
  CreateApiKeyCompanyResponse,
  CreateApiKeyCompanyError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateApiKeyCompanyRequest,
  output: CreateApiKeyCompanyResponse,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type CreateCompanyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create company [Legacy API — https://docs.whop.com/api-reference] Create a new company. Pass parent_company_id to create a connected account under a platform, or omit it to create a company for the current user. Required permissions: - `company:create` - `company:basic:read` */
export const createCompany: API.OperationMethod<
  CreateCompanyRequest,
  Company,
  CreateCompanyError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateCompanyRequest,
  output: Company,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListCompanyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List companies [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of companies. When parent_company_id is provided, lists connected accounts under that platform. When omitted, lists companies the current user has access to. Required permissions: - `company:basic:read` */
export const listCompany: API.PaginatedOperationMethod<
  ListCompanyRequest,
  ListCompanyResponse,
  ListCompanyError,
  WhopOpContext,
  CompanyListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListCompanyRequest,
    output: ListCompanyResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveCompanyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve company [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing company. Required permissions: - `company:basic:read` */
export const retrieveCompany: API.OperationMethod<
  RetrieveCompanyRequest,
  Company,
  RetrieveCompanyError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveCompanyRequest,
  output: Company,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateCompanyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update company [Legacy API — https://docs.whop.com/api-reference] Update a company's title, description, logo, and other settings. Required permissions: - `company:update` - `company:basic:read` */
export const updateCompany: API.OperationMethod<
  UpdateCompanyRequest,
  Company,
  UpdateCompanyError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateCompanyRequest,
  output: Company,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
