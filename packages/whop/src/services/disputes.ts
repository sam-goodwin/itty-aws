// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export type ListDisputesRequestOrder =
  | "created_at"
  | "amount"
  | "evidence_due_at";
export const ListDisputesRequestOrder = /*@__PURE__*/ S.String;

export type ListDisputesRequestDirection = "asc" | "desc";
export const ListDisputesRequestDirection = /*@__PURE__*/ S.String;

export type ListDisputesRequestStatusItem =
  | "needs_response"
  | "under_review"
  | "won"
  | "lost"
  | "closed";
export const ListDisputesRequestStatusItem = /*@__PURE__*/ S.String;

export type ListDisputesRequestStatusList = Array<
  ListDisputesRequestStatusItem | (string & {})
>;
export const ListDisputesRequestStatusList = /*@__PURE__*/ S.Array(
  ListDisputesRequestStatusItem,
) as any as S.Schema<ListDisputesRequestStatusList>;

export interface ListDisputesRequest {
  /** Only disputes filed against this account (`biz_` tag). Omit it to cover every account you can read. */
  account_id?: string;
  /** The number of disputes to return (default 20, max 100). */
  first?: number;
  /** A cursor; returns disputes after this position. */
  after?: string;
  /** The number of disputes to return from the end of the range. */
  last?: number;
  /** A cursor; returns disputes before this position. */
  before?: string;
  /** The field to sort disputes by. */
  order?: ListDisputesRequestOrder | (string & {});
  /** Sort direction. */
  direction?: ListDisputesRequestDirection | (string & {});
  /** Only disputes in these statuses. Repeat the parameter to pass several — one paginated list covers all of them. Covers both chargebacks and inquiries at each stage. A `needs_response` dispute whose evidence deadline has passed reports and filters as `under_review` instead. */
  status?: ListDisputesRequestStatusList;
  /** Only disputes in this three-letter ISO currency. */
  currency?: string;
  /** Only disputes opened before this ISO 8601 timestamp. */
  created_before?: string;
  /** Only disputes opened after this ISO 8601 timestamp. */
  created_after?: string;
}
export const ListDisputesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListDisputesRequestOrder.pipe(T.Query())),
    direction: S.optional(ListDisputesRequestDirection.pipe(T.Query())),
    status: S.optional(ListDisputesRequestStatusList.pipe(T.Query())),
    currency: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/disputes", code: 200 })),
).annotate({
  identifier: "ListDisputesRequest",
}) as any as S.Schema<ListDisputesRequest>;

export interface DisputeBuyer {
  /** The customer's email address. Requires the `member:email:read` scope; `null` without it. */
  email: string | null;
  /** The customer's member row on the account, prefixed `mem_`. */
  member_id: string | null;
  /** The customer's display name. */
  name: string | null;
  /** The customer's user ID, prefixed `user_`. `null` for a guest checkout. */
  user_id: string | null;
  /** The customer's Whop username. */
  username: string | null;
}
export const DisputeBuyer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email: S.NullOr(S.String),
    member_id: S.NullOr(S.String),
    name: S.NullOr(S.String),
    user_id: S.NullOr(S.String),
    username: S.NullOr(S.String),
  }),
).annotate({ identifier: "DisputeBuyer" }) as any as S.Schema<DisputeBuyer>;

export interface DisputeAttachment {
  /** The uploaded file's MIME type. */
  content_type: string | null;
  /** The uploaded file's name. */
  filename: string | null;
  /** The attachment's ID. `null` for a Whop-hosted policy, which is not an uploaded file. */
  id: string | null;
  /** Whether this is Whop's own hosted policy, standing in because the seller uploaded none. Sending it back on a PATCH changes nothing. */
  platform: boolean;
  /** A URL to download the attachment. */
  url: string | null;
}
export const DisputeAttachment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    content_type: S.NullOr(S.String),
    filename: S.NullOr(S.String),
    id: S.NullOr(S.String),
    platform: S.Boolean,
    url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "DisputeAttachment",
}) as any as S.Schema<DisputeAttachment>;

/** The uploaded file's MIME type. Uploads are restricted to the types the processor accepts. */
export type DisputeEvidenceDocumentContentType =
  | "application/pdf"
  | "application/json"
  | "image/jpeg"
  | "image/png"
  | "image/webp";
export const DisputeEvidenceDocumentContentType = /*@__PURE__*/ S.String;

/** What kind of evidence the document is. */
export type DisputeEvidenceDocumentDocumentType =
  | "return_policy"
  | "shipping_policy"
  | "physical_fulfillment"
  | "customer_order_history"
  | "product_image"
  | "prior_transactions"
  | "customer_session"
  | "digital_fulfillment"
  | "subscription";
export const DisputeEvidenceDocumentDocumentType = /*@__PURE__*/ S.String;

export interface FileMultipartUrl {
  /** The 1-based index of this part within the multipart upload. */
  part_number: number;
  /** The presigned URL to PUT this part's bytes to. */
  url: string;
}
export const FileMultipartUrl = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    part_number: S.Number,
    url: S.String,
  }),
).annotate({
  identifier: "FileMultipartUrl",
}) as any as S.Schema<FileMultipartUrl>;

export type DisputeEvidenceDocumentMultipartUploadUrlsList =
  Array<FileMultipartUrl>;
export const DisputeEvidenceDocumentMultipartUploadUrlsList =
  /*@__PURE__*/ S.Array(
    FileMultipartUrl,
  ) as any as S.Schema<DisputeEvidenceDocumentMultipartUploadUrlsList>;

/** Where the file is in its upload lifecycle. */
export type DisputeEvidenceDocumentUploadStatus =
  | "pending"
  | "processing"
  | "ready"
  | "failed";
export const DisputeEvidenceDocumentUploadStatus = /*@__PURE__*/ S.String;

/** `public` files are served via an unsigned CDN URL; `private` files via a signed, expiring URL. */
export type DisputeEvidenceDocumentVisibility = "public" | "private";
export const DisputeEvidenceDocumentVisibility = /*@__PURE__*/ S.String;

export interface DisputeEvidenceDocument {
  /** The uploaded file's MIME type. Uploads are restricted to the types the processor accepts. */
  content_type: DisputeEvidenceDocumentContentType | null;
  /** When the file was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** What kind of evidence the document is. */
  document_type: DisputeEvidenceDocumentDocumentType;
  /** The original filename, including its extension. */
  filename: string | null;
  /** The file's ID, prefixed `file_`. */
  id: string;
  /** The byte size each part (except the last) must be. Present only on create, and only for multipart uploads. */
  multipart_chunk_size?: number | null;
  /** The ID of the multipart upload, passed back to `complete`. Present only on create, and only for multipart uploads. */
  multipart_upload_id?: string | null;
  multipart_upload_urls?: DisputeEvidenceDocumentMultipartUploadUrlsList | null;
  /** The type of this object, always `file`. */
  object: string;
  /** The file size in bytes. `null` until the upload has finished. */
  size: number | null;
  /** Headers to send with the upload PUT. Present only on create. */
  upload_headers?: unknown;
  /** Where the file is in its upload lifecycle. */
  upload_status: DisputeEvidenceDocumentUploadStatus;
  /** Presigned URL to PUT the file's bytes to. Present only on create, and only for single-part uploads. */
  upload_url?: string | null;
  /** A URL to download the file: a permanent CDN URL for public files, a signed expiring URL for private ones. `null` until the upload has finished. */
  url: string | null;
  /** `public` files are served via an unsigned CDN URL; `private` files via a signed, expiring URL. */
  visibility: DisputeEvidenceDocumentVisibility;
}
export const DisputeEvidenceDocument = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    content_type: S.NullOr(DisputeEvidenceDocumentContentType),
    created_at: S.String,
    document_type: DisputeEvidenceDocumentDocumentType,
    filename: S.NullOr(S.String),
    id: S.String,
    multipart_chunk_size: S.optional(S.NullOr(S.Number)),
    multipart_upload_id: S.optional(S.NullOr(S.String)),
    multipart_upload_urls: S.optional(
      S.NullOr(DisputeEvidenceDocumentMultipartUploadUrlsList),
    ),
    object: S.String,
    size: S.NullOr(S.Number),
    upload_headers: S.optional(S.Unknown),
    upload_status: DisputeEvidenceDocumentUploadStatus,
    upload_url: S.optional(S.NullOr(S.String)),
    url: S.NullOr(S.String),
    visibility: DisputeEvidenceDocumentVisibility,
  }),
).annotate({
  identifier: "DisputeEvidenceDocument",
}) as any as S.Schema<DisputeEvidenceDocument>;

export type DisputeEvidenceDocumentsList = Array<DisputeEvidenceDocument>;
export const DisputeEvidenceDocumentsList = /*@__PURE__*/ S.Array(
  DisputeEvidenceDocument,
) as any as S.Schema<DisputeEvidenceDocumentsList>;

export interface DisputeEvidence {
  /** Log of the customer's access to the product, such as sign-in or download activity. */
  access_activity_log: string | null;
  /** The billing address the customer provided at checkout. */
  billing_address: string | null;
  /** The cancellation policy document. Falls back to Whop's platform policy when the seller has not uploaded their own. */
  cancellation_policy_attachment: DisputeAttachment | null;
  /** How the cancellation policy was shown to the customer before purchase. */
  cancellation_policy_disclosure: string | null;
  /** Correspondence with the customer, or proof they used the product. */
  customer_communication_attachment: DisputeAttachment | null;
  /** The email address the customer used at checkout. */
  customer_email_address: string | null;
  /** The customer's name as given at checkout. */
  customer_name: string | null;
  documents: DisputeEvidenceDocumentsList;
  /** Any additional context for the processor reviewing the dispute. */
  notes: string | null;
  /** What the customer purchased, in the seller's own words. */
  product_description: string | null;
  /** The refund policy document. Falls back to Whop's platform policy when the seller has not uploaded their own. */
  refund_policy_attachment: DisputeAttachment | null;
  /** How the refund policy was shown to the customer before purchase. */
  refund_policy_disclosure: string | null;
  /** Why a refund was refused, when one was requested and denied. */
  refund_refusal_explanation: string | null;
  /** When the product or service was delivered. */
  service_date: string | null;
  /** Supporting evidence that does not fit the other categories. */
  uncategorized_attachment: DisputeAttachment | null;
}
export const DisputeEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    access_activity_log: S.NullOr(S.String),
    billing_address: S.NullOr(S.String),
    cancellation_policy_attachment: S.NullOr(DisputeAttachment),
    cancellation_policy_disclosure: S.NullOr(S.String),
    customer_communication_attachment: S.NullOr(DisputeAttachment),
    customer_email_address: S.NullOr(S.String),
    customer_name: S.NullOr(S.String),
    documents: DisputeEvidenceDocumentsList,
    notes: S.NullOr(S.String),
    product_description: S.NullOr(S.String),
    refund_policy_attachment: S.NullOr(DisputeAttachment),
    refund_policy_disclosure: S.NullOr(S.String),
    refund_refusal_explanation: S.NullOr(S.String),
    service_date: S.NullOr(S.String),
    uncategorized_attachment: S.NullOr(DisputeAttachment),
  }),
).annotate({
  identifier: "DisputeEvidence",
}) as any as S.Schema<DisputeEvidence>;

/** Why evidence can no longer be edited. `null` while `evidence_editable` is true. */
export type DisputeEvidenceLockedReason =
  | "submitted"
  | "response_window_closed"
  | "not_contestable";
export const DisputeEvidenceLockedReason = /*@__PURE__*/ S.String;

export interface DisputeIssuerComment {
  /** When the comment was received, as an ISO 8601 timestamp. */
  received_at: string | null;
  /** What the issuer wrote, as received. */
  text: string;
}
export const DisputeIssuerComment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    received_at: S.NullOr(S.String),
    text: S.String,
  }),
).annotate({
  identifier: "DisputeIssuerComment",
}) as any as S.Schema<DisputeIssuerComment>;

export type DisputeIssuerCommentsList = Array<DisputeIssuerComment>;
export const DisputeIssuerCommentsList = /*@__PURE__*/ S.Array(
  DisputeIssuerComment,
) as any as S.Schema<DisputeIssuerCommentsList>;

export interface PaymentInstrumentCard {
  /** The network identifier (`visa`, `amex`, …), matching `card.networks` entries and saved card payment methods. */
  brand: string;
  /** The card's last four digits, when captured. */
  last4: string | null;
}
export const PaymentInstrumentCard = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    brand: S.String,
    last4: S.NullOr(S.String),
  }),
).annotate({
  identifier: "PaymentInstrumentCard",
}) as any as S.Schema<PaymentInstrumentCard>;

export interface PaymentMethodIconFiles {
  /** Raster fallback at the shape's native size. */
  png_1x: string;
  /** Raster fallback at double density. */
  png_2x: string;
  /** Raster fallback at quadruple density. */
  png_4x: string;
  /** The vector file. Prefer this everywhere SVG renders. */
  svg: string;
}
export const PaymentMethodIconFiles = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    png_1x: S.String,
    png_2x: S.String,
    png_4x: S.String,
    svg: S.String,
  }),
).annotate({
  identifier: "PaymentMethodIconFiles",
}) as any as S.Schema<PaymentMethodIconFiles>;

export interface PaymentMethodIconVariants {
  /** The colorway for dark surfaces. */
  dark: PaymentMethodIconFiles;
  /** The colorway for light surfaces. */
  light: PaymentMethodIconFiles;
}
export const PaymentMethodIconVariants = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    dark: PaymentMethodIconFiles,
    light: PaymentMethodIconFiles,
  }),
).annotate({
  identifier: "PaymentMethodIconVariants",
}) as any as S.Schema<PaymentMethodIconVariants>;

export interface PaymentMethodIcons {
  /** The credit-card-proportioned tile (48x30). */
  card: PaymentMethodIconVariants;
  /** The square tile (32x32). */
  square: PaymentMethodIconVariants;
}
export const PaymentMethodIcons = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    card: PaymentMethodIconVariants,
    square: PaymentMethodIconVariants,
  }),
).annotate({
  identifier: "PaymentMethodIcons",
}) as any as S.Schema<PaymentMethodIcons>;

export interface PaymentInstrument {
  /** Card payments only: the card's network and last four. */
  card: PaymentInstrumentCard | null;
  /** Buyer-facing instrument name — "Visa •••• 4242" when the card surfaced, else the method's own name ("Klarna"). */
  display_name: string;
  /** The standard icon set: square and card shapes, each in light and dark colorways. */
  icons: PaymentMethodIcons;
  /** Installment methods only: how many payments the charge splits into. Data, not copy — compose and translate the label client-side. */
  installment_count: number | null;
  /** The payment method type identifier, e.g. `card`, `klarna`, `apple_pay`. */
  payment_method_type: string;
}
export const PaymentInstrument = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    card: S.NullOr(PaymentInstrumentCard),
    display_name: S.String,
    icons: PaymentMethodIcons,
    installment_count: S.NullOr(S.Number),
    payment_method_type: S.String,
  }),
).annotate({
  identifier: "PaymentInstrument",
}) as any as S.Schema<PaymentInstrument>;

export interface DisputePayment {
  /** What the customer was charged, in whole units of the payment's currency. */
  amount: number | null;
  /** Card brand, when the customer paid by card. */
  card_brand: string | null;
  /** Last four digits of the card, when the customer paid by card. */
  card_last4: string | null;
  /** When the payment was made, as an ISO 8601 timestamp. */
  created_at: string;
  /** Three-letter ISO currency code of the payment. Can differ from the dispute's currency when the processor settles in another currency. */
  currency: string | null;
  /** Payment ID, prefixed `pay_`. */
  id: string;
  /** The instrument this payment was made with, shaped for display: the method type, a buyer-facing name, the standard icon set, and the card facts when it was a card. Null when the payment names no method. */
  payment_instrument: PaymentInstrument | null;
  /** How the customer paid, such as `card` or `paypal`. */
  payment_method_type: string | null;
  /** The processor that handled the payment, such as `stripe`. */
  payment_processor: string | null;
}
export const DisputePayment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.NullOr(S.Number),
    card_brand: S.NullOr(S.String),
    card_last4: S.NullOr(S.String),
    created_at: S.String,
    currency: S.NullOr(S.String),
    id: S.String,
    payment_instrument: S.NullOr(PaymentInstrument),
    payment_method_type: S.NullOr(S.String),
    payment_processor: S.NullOr(S.String),
  }),
).annotate({ identifier: "DisputePayment" }) as any as S.Schema<DisputePayment>;

/** Why the customer says they are disputing, normalized across card networks. `other` covers a code Whop has not categorized yet — read `reason_code` for the raw value. */
export type DisputeReason =
  | "fraudulent"
  | "unrecognized"
  | "declined_authorization"
  | "product_not_received"
  | "product_unacceptable"
  | "subscription_canceled"
  | "credit_not_processed"
  | "duplicate"
  | "processing_error"
  | "documentation_request"
  | "bank_cannot_process"
  | "other";
export const DisputeReason = /*@__PURE__*/ S.String;

/** Where the dispute stands. `needs_response` is awaiting evidence, `under_review` is with the processor, `won` returned the funds to the seller, `lost` returned them to the customer, and `closed` ended without a ruling. A dispute past its `evidence_due_at` reports `under_review` — the window to respond has closed. */
export type DisputeStatus =
  | "needs_response"
  | "under_review"
  | "won"
  | "lost"
  | "closed";
export const DisputeStatus = /*@__PURE__*/ S.String;

export interface Dispute {
  /** The account the dispute was filed against, prefixed `biz_`. */
  account_id: string | null;
  /** The disputed amount, in whole units of `currency`. */
  amount: number;
  /** The customer who filed the dispute. */
  buyer: DisputeBuyer | null;
  /** When the dispute was opened, as an ISO 8601 timestamp. */
  created_at: string;
  /** Three-letter ISO currency code of the disputed amount. */
  currency: string;
  /** The evidence packet sent to the processor to contest the dispute. */
  evidence: DisputeEvidence;
  /** The deadline to submit evidence, as an ISO 8601 timestamp. Whop reserves the last 24 hours before the processor's own cutoff to forward the submission. */
  evidence_due_at: string | null;
  /** Whether `evidence` can still be changed and submitted. */
  evidence_editable: boolean;
  /** Why evidence can no longer be edited. `null` while `evidence_editable` is true. */
  evidence_locked_reason: DisputeEvidenceLockedReason | null;
  /** When the evidence was submitted to the processor, as an ISO 8601 timestamp. */
  evidence_submitted_at: string | null;
  /** The AI-generated representment document filed with the processor on the seller's behalf, once ready. Null until generation completes, and for disputes not using Whop Dispute Fighter. */
  generated_response_attachment: DisputeAttachment | null;
  /** Dispute ID, prefixed `dspt_`. */
  id: string;
  /** Whether this is a pre-dispute inquiry rather than a formal chargeback. Inquiries follow the same lifecycle but move no funds unless one escalates. */
  inquiry: boolean;
  issuer_comments: DisputeIssuerCommentsList;
  /** The payment being disputed. */
  payment: DisputePayment | null;
  /** The plan the disputed payment was made on, prefixed `plan_`. */
  plan_id: string | null;
  /** The product the disputed payment was for, prefixed `prod_`. */
  product_id: string | null;
  /** Whether Visa Rapid Dispute Resolution settled this automatically. These refund the customer without an evidence round. */
  rapid_dispute_resolution: boolean;
  /** Why the customer says they are disputing, normalized across card networks. `other` covers a code Whop has not categorized yet — read `reason_code` for the raw value. */
  reason: DisputeReason;
  /** The raw card-network or processor reason code, such as `10.4`. */
  reason_code: string | null;
  /** Where the dispute stands. `needs_response` is awaiting evidence, `under_review` is with the processor, `won` returned the funds to the seller, `lost` returned them to the customer, and `closed` ended without a ruling. A dispute past its `evidence_due_at` reports `under_review` — the window to respond has closed. */
  status: DisputeStatus;
  /** When the dispute was last changed, as an ISO 8601 timestamp. */
  updated_at: string;
}
export const Dispute = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.NullOr(S.String),
    amount: S.Number,
    buyer: S.NullOr(DisputeBuyer),
    created_at: S.String,
    currency: S.String,
    evidence: DisputeEvidence,
    evidence_due_at: S.NullOr(S.String),
    evidence_editable: S.Boolean,
    evidence_locked_reason: S.NullOr(DisputeEvidenceLockedReason),
    evidence_submitted_at: S.NullOr(S.String),
    generated_response_attachment: S.NullOr(DisputeAttachment),
    id: S.String,
    inquiry: S.Boolean,
    issuer_comments: DisputeIssuerCommentsList,
    payment: S.NullOr(DisputePayment),
    plan_id: S.NullOr(S.String),
    product_id: S.NullOr(S.String),
    rapid_dispute_resolution: S.Boolean,
    reason: DisputeReason,
    reason_code: S.NullOr(S.String),
    status: DisputeStatus,
    updated_at: S.String,
  }),
).annotate({ identifier: "Dispute" }) as any as S.Schema<Dispute>;

export type ListDisputesResponseDataList = Array<Dispute>;
export const ListDisputesResponseDataList = /*@__PURE__*/ S.Array(
  Dispute,
) as any as S.Schema<ListDisputesResponseDataList>;

export interface ListDisputesResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListDisputesResponsePageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListDisputesResponsePageInfo",
}) as any as S.Schema<ListDisputesResponsePageInfo>;

export interface ListDisputesResponse {
  data: ListDisputesResponseDataList;
  page_info: ListDisputesResponsePageInfo;
}
export const ListDisputesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListDisputesResponseDataList,
    page_info: ListDisputesResponsePageInfo,
  }),
).annotate({
  identifier: "ListDisputesResponse",
}) as any as S.Schema<ListDisputesResponse>;

export interface RetrieveDisputeRequest {
  /** The dispute ID (`dspt_` tag). */
  id: string;
}
export const RetrieveDisputeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/disputes/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveDisputeRequest",
}) as any as S.Schema<RetrieveDisputeRequest>;

export type RetrieveDisputeSummaryRequestGroupsItem = "status" | "currency";
export const RetrieveDisputeSummaryRequestGroupsItem = /*@__PURE__*/ S.String;

export type RetrieveDisputeSummaryRequestGroupsList = Array<
  RetrieveDisputeSummaryRequestGroupsItem | (string & {})
>;
export const RetrieveDisputeSummaryRequestGroupsList = /*@__PURE__*/ S.Array(
  RetrieveDisputeSummaryRequestGroupsItem,
) as any as S.Schema<RetrieveDisputeSummaryRequestGroupsList>;

export type RetrieveDisputeSummaryRequestStatusItem =
  | "needs_response"
  | "under_review"
  | "won"
  | "lost"
  | "closed";
export const RetrieveDisputeSummaryRequestStatusItem = /*@__PURE__*/ S.String;

export type RetrieveDisputeSummaryRequestStatusList = Array<
  RetrieveDisputeSummaryRequestStatusItem | (string & {})
>;
export const RetrieveDisputeSummaryRequestStatusList = /*@__PURE__*/ S.Array(
  RetrieveDisputeSummaryRequestStatusItem,
) as any as S.Schema<RetrieveDisputeSummaryRequestStatusList>;

export interface RetrieveDisputeSummaryRequest {
  /** Which breakdowns to return, keyed by these names under `groups`. Repeat the parameter to ask for several; omit it for all of them. */
  groups?: RetrieveDisputeSummaryRequestGroupsList;
  /** Only disputes filed against this account (`biz_` tag). Omit it to cover every account you can read. */
  account_id?: string;
  /** Only disputes in these statuses. Repeat the parameter to pass several. A `needs_response` dispute whose evidence deadline has passed reports and filters as `under_review` instead. */
  status?: RetrieveDisputeSummaryRequestStatusList;
  /** Only disputes in this three-letter ISO currency. */
  currency?: string;
  /** Only disputes opened before this ISO 8601 timestamp. */
  created_before?: string;
  /** Only disputes opened after this ISO 8601 timestamp. */
  created_after?: string;
}
export const RetrieveDisputeSummaryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    groups: S.optional(RetrieveDisputeSummaryRequestGroupsList.pipe(T.Query())),
    account_id: S.optional(S.String.pipe(T.Query())),
    status: S.optional(RetrieveDisputeSummaryRequestStatusList.pipe(T.Query())),
    currency: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/disputes/summary", code: 200 })),
).annotate({
  identifier: "RetrieveDisputeSummaryRequest",
}) as any as S.Schema<RetrieveDisputeSummaryRequest>;

/** How many of the matching disputes are in each currency, keyed by three-letter ISO code. Only currencies with at least one dispute are present. */
export type RetrieveDisputeSummaryResponseGroupsCurrencyMap = {
  [key: string]: number | undefined;
};
export const RetrieveDisputeSummaryResponseGroupsCurrencyMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Number,
  ) as any as S.Schema<RetrieveDisputeSummaryResponseGroupsCurrencyMap>;

/** How many of the matching disputes are in each status. Every status is present, including those with a count of zero. */
export interface RetrieveDisputeSummaryResponseGroupsStatus {
  closed: number;
  lost: number;
  needs_response: number;
  under_review: number;
  won: number;
}
export const RetrieveDisputeSummaryResponseGroupsStatus =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      closed: S.Number,
      lost: S.Number,
      needs_response: S.Number,
      under_review: S.Number,
      won: S.Number,
    }),
  ).annotate({
    identifier: "RetrieveDisputeSummaryResponseGroupsStatus",
  }) as any as S.Schema<RetrieveDisputeSummaryResponseGroupsStatus>;

/** One entry per requested breakdown, keyed by the field it groups on. A field you did not ask for is absent. */
export interface RetrieveDisputeSummaryResponseGroups {
  /** How many of the matching disputes are in each currency, keyed by three-letter ISO code. Only currencies with at least one dispute are present. */
  currency?: RetrieveDisputeSummaryResponseGroupsCurrencyMap;
  /** How many of the matching disputes are in each status. Every status is present, including those with a count of zero. */
  status?: RetrieveDisputeSummaryResponseGroupsStatus;
}
export const RetrieveDisputeSummaryResponseGroups = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      currency: S.optional(RetrieveDisputeSummaryResponseGroupsCurrencyMap),
      status: S.optional(RetrieveDisputeSummaryResponseGroupsStatus),
    }),
).annotate({
  identifier: "RetrieveDisputeSummaryResponseGroups",
}) as any as S.Schema<RetrieveDisputeSummaryResponseGroups>;

export interface RetrieveDisputeSummaryResponse {
  /** One entry per requested breakdown, keyed by the field it groups on. A field you did not ask for is absent. */
  groups: RetrieveDisputeSummaryResponseGroups;
  /** How many disputes match the filters. */
  total: number;
}
export const RetrieveDisputeSummaryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    groups: RetrieveDisputeSummaryResponseGroups,
    total: S.Number,
  }),
).annotate({
  identifier: "RetrieveDisputeSummaryResponse",
}) as any as S.Schema<RetrieveDisputeSummaryResponse>;

export interface SubmitDisputeRequest {
  /** The dispute ID (`dspt_` tag). */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const SubmitDisputeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/disputes/{id}/submit", code: 200 })),
).annotate({
  identifier: "SubmitDisputeRequest",
}) as any as S.Schema<SubmitDisputeRequest>;

export interface SubmitEvidenceDisputeRequest {
  /** The unique identifier of the dispute to submit to the payment processor for review. */
  id: string;
}
export const SubmitEvidenceDisputeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/disputes/{id}/submit_evidence",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SubmitEvidenceDisputeRequest",
}) as any as S.Schema<SubmitEvidenceDisputeRequest>;

/** The cancellation policy document uploaded as dispute evidence. Null if no cancellation policy has been provided. */
export interface LegacyDisputeCancellationPolicyAttachment {
  /** Uploaded file MIME type, such as image/jpeg, video/mp4, or audio/mpeg. */
  content_type: string | null;
  /** The original filename of the uploaded attachment, including its file extension. */
  filename: string | null;
  /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
  id: string;
  /** A pre-optimized URL for rendering this attachment on the client. This should be used for displaying attachments in apps. */
  url: string | null;
}
export const LegacyDisputeCancellationPolicyAttachment =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      content_type: S.NullOr(S.String),
      filename: S.NullOr(S.String),
      id: S.String,
      url: S.NullOr(S.String),
    }),
  ).annotate({
    identifier: "LegacyDisputeCancellationPolicyAttachment",
  }) as any as S.Schema<LegacyDisputeCancellationPolicyAttachment>;

/** The company that the dispute was filed against. */
export interface LegacyDisputeCompany {
  /** The unique identifier for the company. */
  id: string;
  /** The written name of the company. */
  title: string;
}
export const LegacyDisputeCompany = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({
  identifier: "LegacyDisputeCompany",
}) as any as S.Schema<LegacyDisputeCompany>;

/** The available currencies on the platform */
export type Currencies =
  | "usd"
  | "sgd"
  | "inr"
  | "aud"
  | "brl"
  | "cad"
  | "dkk"
  | "eur"
  | "nok"
  | "gbp"
  | "sek"
  | "chf"
  | "hkd"
  | "huf"
  | "jpy"
  | "mxn"
  | "myr"
  | "pln"
  | "czk"
  | "nzd"
  | "aed"
  | "eth"
  | "ape"
  | "cop"
  | "ron"
  | "thb"
  | "bgn"
  | "idr"
  | "dop"
  | "php"
  | "try"
  | "krw"
  | "twd"
  | "vnd"
  | "pkr"
  | "clp"
  | "uyu"
  | "ars"
  | "zar"
  | "dzd"
  | "tnd"
  | "mad"
  | "kes"
  | "kwd"
  | "jod"
  | "all"
  | "xcd"
  | "amd"
  | "bsd"
  | "bhd"
  | "bob"
  | "bam"
  | "khr"
  | "crc"
  | "xof"
  | "egp"
  | "etb"
  | "gmd"
  | "ghs"
  | "gtq"
  | "gyd"
  | "ils"
  | "jmd"
  | "mop"
  | "mga"
  | "mur"
  | "mdl"
  | "mnt"
  | "nad"
  | "ngn"
  | "mkd"
  | "omr"
  | "pyg"
  | "pen"
  | "qar"
  | "rwf"
  | "sar"
  | "rsd"
  | "lkr"
  | "tzs"
  | "ttd"
  | "uzs"
  | "rub"
  | "btc"
  | "cny"
  | "usdt"
  | "kzt"
  | "awg"
  | "whop_usd"
  | "xau";
export const Currencies = /*@__PURE__*/ S.String;

/** Evidence of customer communication or product usage, uploaded as a dispute attachment. Null if not provided. */
export type LegacyDisputeCustomerCommunicationAttachment =
  LegacyDisputeCancellationPolicyAttachment;
export const LegacyDisputeCustomerCommunicationAttachment =
  LegacyDisputeCancellationPolicyAttachment;

/** The reason why a specific payment was billed */
export type BillingReasons =
  | "subscription_create"
  | "subscription_cycle"
  | "subscription_update"
  | "one_time"
  | "manual"
  | "subscription";
export const BillingReasons = /*@__PURE__*/ S.String;

/** Possible card brands that a payment token can have */
export type CardBrands =
  | "mastercard"
  | "visa"
  | "amex"
  | "discover"
  | "unionpay"
  | "jcb"
  | "diners"
  | "link"
  | "troy"
  | "visadankort"
  | "visabancontact"
  | "china_union_pay"
  | "rupay"
  | "jcbrupay"
  | "elo"
  | "maestro"
  | "tarjeta_naranja"
  | "cirrus"
  | "nspk_mir"
  | "verve"
  | "ebt"
  | "private_label"
  | "local_brand"
  | "uatp"
  | "wexcard"
  | "uzcard"
  | "meeza"
  | "hrg_store_card"
  | "girocard"
  | "fuel_card"
  | "dankort"
  | "carnet"
  | "atm_card"
  | "china_union_payuzcard"
  | "codensa"
  | "cabal"
  | "hipercard"
  | "jcblankapay"
  | "cmi"
  | "aura"
  | "accel"
  | "culiance"
  | "nyce"
  | "pulse"
  | "star"
  | "unknown";
export const CardBrands = /*@__PURE__*/ S.String;

/** The member attached to this payment. */
export interface LegacyDisputePaymentMember {
  /** The unique identifier for the company member. */
  id: string;
  /** The phone number for the member, if available. */
  phone: string | null;
}
export const LegacyDisputePaymentMember = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    phone: S.NullOr(S.String),
  }),
).annotate({
  identifier: "LegacyDisputePaymentMember",
}) as any as S.Schema<LegacyDisputePaymentMember>;

/** The status of a membership */
export type MembershipStatus =
  | "trialing"
  | "active"
  | "past_due"
  | "completed"
  | "canceled"
  | "expired"
  | "unresolved"
  | "drafted"
  | "canceling";
export const MembershipStatus = /*@__PURE__*/ S.String;

/** The membership attached to this payment. */
export interface LegacyDisputePaymentMembership {
  /** The unique identifier for the membership. */
  id: string;
  /** The state of the membership. */
  status: MembershipStatus;
}
export const LegacyDisputePaymentMembership = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    status: MembershipStatus,
  }),
).annotate({
  identifier: "LegacyDisputePaymentMembership",
}) as any as S.Schema<LegacyDisputePaymentMembership>;

/** The colorway for dark surfaces. */
export interface LegacyDisputePaymentPaymentInstrumentIconsSquareDark {
  /** The vector file. Prefer this everywhere SVG renders. */
  svg: string;
}
export const LegacyDisputePaymentPaymentInstrumentIconsSquareDark =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      svg: S.String,
    }),
  ).annotate({
    identifier: "LegacyDisputePaymentPaymentInstrumentIconsSquareDark",
  }) as any as S.Schema<LegacyDisputePaymentPaymentInstrumentIconsSquareDark>;

/** The colorway for light surfaces. */
export type LegacyDisputePaymentPaymentInstrumentIconsSquareLight =
  LegacyDisputePaymentPaymentInstrumentIconsSquareDark;
export const LegacyDisputePaymentPaymentInstrumentIconsSquareLight =
  LegacyDisputePaymentPaymentInstrumentIconsSquareDark;

/** The square tile (32x32). */
export interface LegacyDisputePaymentPaymentInstrumentIconsSquare {
  /** The colorway for dark surfaces. */
  dark: LegacyDisputePaymentPaymentInstrumentIconsSquareDark;
  /** The colorway for light surfaces. */
  light: LegacyDisputePaymentPaymentInstrumentIconsSquareDark;
}
export const LegacyDisputePaymentPaymentInstrumentIconsSquare =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      dark: LegacyDisputePaymentPaymentInstrumentIconsSquareDark,
      light: LegacyDisputePaymentPaymentInstrumentIconsSquareDark,
    }),
  ).annotate({
    identifier: "LegacyDisputePaymentPaymentInstrumentIconsSquare",
  }) as any as S.Schema<LegacyDisputePaymentPaymentInstrumentIconsSquare>;

/** The standard icon set: square and card shapes, each in light and dark colorways. */
export interface LegacyDisputePaymentPaymentInstrumentIcons {
  /** The square tile (32x32). */
  square: LegacyDisputePaymentPaymentInstrumentIconsSquare;
}
export const LegacyDisputePaymentPaymentInstrumentIcons =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      square: LegacyDisputePaymentPaymentInstrumentIconsSquare,
    }),
  ).annotate({
    identifier: "LegacyDisputePaymentPaymentInstrumentIcons",
  }) as any as S.Schema<LegacyDisputePaymentPaymentInstrumentIcons>;

/** The instrument this payment was made with, shaped for display: the method type, a buyer-facing name, the standard icon set, and the card facts when it was a card. Null when the receipt names no payment method. */
export interface LegacyDisputePaymentPaymentInstrument {
  /** Buyer-facing instrument name — "Visa •••• 4242" when the card surfaced, else the method's own name ("Klarna"). */
  display_name: string;
  /** The standard icon set: square and card shapes, each in light and dark colorways. */
  icons: LegacyDisputePaymentPaymentInstrumentIcons;
  /** Installment methods only: how many payments the charge splits into. Data, not copy — compose and translate the label client-side. */
  installment_count: number | null;
  /** The payment method type identifier, e.g. `card`, `klarna`, `apple_pay`. */
  payment_method_type: string;
}
export const LegacyDisputePaymentPaymentInstrument = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      display_name: S.String,
      icons: LegacyDisputePaymentPaymentInstrumentIcons,
      installment_count: S.NullOr(S.Number),
      payment_method_type: S.String,
    }),
).annotate({
  identifier: "LegacyDisputePaymentPaymentInstrument",
}) as any as S.Schema<LegacyDisputePaymentPaymentInstrument>;

/** The different types of payment methods that can be used. */
export type PaymentMethodTypes =
  | "acss_debit"
  | "addi"
  | "affirm"
  | "afterpay_clearpay"
  | "alipay"
  | "alma"
  | "amazon_pay"
  | "apple"
  | "apple_pay"
  | "au_bank_transfer"
  | "au_becs_debit"
  | "bacs_debit"
  | "bancolombia"
  | "bancontact"
  | "bank_wire"
  | "billie"
  | "bizum"
  | "blik"
  | "boleto"
  | "bre_b"
  | "ca_bank_transfer"
  | "capchase_pay"
  | "card"
  | "card_installments_three"
  | "card_installments_six"
  | "card_installments_twelve"
  | "cashapp"
  | "claritypay"
  | "coinbase"
  | "crypto"
  | "custom"
  | "customer_balance"
  | "demo_pay"
  | "efecty"
  | "eps"
  | "eu_bank_transfer"
  | "fpx"
  | "gb_bank_transfer"
  | "giropay"
  | "google_pay"
  | "gopay"
  | "grabpay"
  | "id_bank_transfer"
  | "ideal"
  | "interac"
  | "kakao_pay"
  | "klarna"
  | "klarna_pay_now"
  | "konbini"
  | "kr_card"
  | "kr_market"
  | "kriya"
  | "kueski"
  | "link"
  | "mb_way"
  | "m_pesa"
  | "mercado_pago"
  | "mobilepay"
  | "modo"
  | "mondu"
  | "multibanco"
  | "naver_pay"
  | "nequi"
  | "netbanking"
  | "ng_bank"
  | "ng_bank_transfer"
  | "ng_card"
  | "ng_market"
  | "ng_ussd"
  | "ng_wallet"
  | "nz_bank_account"
  | "oxxo"
  | "p24"
  | "pago_efectivo"
  | "pse"
  | "pay_by_bank"
  | "payco"
  | "paynow"
  | "paypal"
  | "paypay"
  | "payto"
  | "pix"
  | "platform_balance"
  | "promptpay"
  | "qris"
  | "rapipago"
  | "rechnung"
  | "revolut_pay"
  | "samsung_pay"
  | "satispay"
  | "scalapay"
  | "sencillito"
  | "sepa_debit"
  | "sequra"
  | "servipag"
  | "sezzle"
  | "shop_pay"
  | "shopeepay"
  | "sofort"
  | "south_korea_market"
  | "spei"
  | "splitit"
  | "sunbit"
  | "swish"
  | "tamara"
  | "twint"
  | "upi"
  | "us_bank_account"
  | "us_bank_transfer"
  | "venmo"
  | "vipps"
  | "webpay"
  | "wechat_pay"
  | "yape"
  | "zip"
  | "coinflow"
  | "unknown";
export const PaymentMethodTypes = /*@__PURE__*/ S.String;

/** The user that made this payment. */
export interface LegacyDisputePaymentUser {
  /** The user's email address. Requires the member:email:read permission to access. Null if not authorized. */
  email: string | null;
  /** The unique identifier for the user. */
  id: string;
  /** The user's display name shown on their public profile. */
  name: string | null;
  /** The user's unique username shown on their public profile. */
  username: string;
}
export const LegacyDisputePaymentUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email: S.NullOr(S.String),
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({
  identifier: "LegacyDisputePaymentUser",
}) as any as S.Schema<LegacyDisputePaymentUser>;

/** The original payment that was disputed. */
export interface LegacyDisputePayment {
  /** The machine-readable reason this charge was created, such as initial subscription purchase, renewal cycle, or one-time payment. */
  billing_reason: BillingReasons | null;
  /** Card network reported by the processor (e.g., 'visa', 'mastercard', 'amex'). Present only when the payment method type is 'card'. */
  card_brand: CardBrands | null;
  /** The last four digits of the card used to make this payment. Null if the payment was not made with a card. */
  card_last4: string | null;
  /** The datetime the payment was created. */
  created_at: string;
  /** The three-letter ISO currency code for this payment (e.g., 'usd', 'eur'). */
  currency: Currencies;
  /** When an alert came in that this transaction will be disputed */
  dispute_alerted_at: string | null;
  /** The unique identifier for the payment. */
  id: string;
  /** The member attached to this payment. */
  member: LegacyDisputePaymentMember | null;
  /** The membership attached to this payment. */
  membership: LegacyDisputePaymentMembership | null;
  /** The time at which this payment was successfully collected. Null if the payment has not yet succeeded. As a Unix timestamp. */
  paid_at: string | null;
  /** The instrument this payment was made with, shaped for display: the method type, a buyer-facing name, the standard icon set, and the card facts when it was a card. Null when the receipt names no payment method. */
  payment_instrument: LegacyDisputePaymentPaymentInstrument | null;
  /** The type of payment instrument used for this payment (e.g., card, Cash App, iDEAL, Klarna, crypto). Null when the processor does not supply a type. */
  payment_method_type: PaymentMethodTypes | null;
  /** The subtotal to show to the creator (excluding buyer fees). */
  subtotal: number | null;
  /** The total to show to the creator (excluding buyer fees). */
  total: number | null;
  /** The total in USD to show to the creator (excluding buyer fees). */
  usd_total: number | null;
  /** The user that made this payment. */
  user: LegacyDisputePaymentUser | null;
}
export const LegacyDisputePayment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing_reason: S.NullOr(BillingReasons),
    card_brand: S.NullOr(CardBrands),
    card_last4: S.NullOr(S.String),
    created_at: S.String,
    currency: Currencies,
    dispute_alerted_at: S.NullOr(S.String),
    id: S.String,
    member: S.NullOr(LegacyDisputePaymentMember),
    membership: S.NullOr(LegacyDisputePaymentMembership),
    paid_at: S.NullOr(S.String),
    payment_instrument: S.NullOr(LegacyDisputePaymentPaymentInstrument),
    payment_method_type: S.NullOr(PaymentMethodTypes),
    subtotal: S.NullOr(S.Number),
    total: S.NullOr(S.Number),
    usd_total: S.NullOr(S.Number),
    user: S.NullOr(LegacyDisputePaymentUser),
  }),
).annotate({
  identifier: "LegacyDisputePayment",
}) as any as S.Schema<LegacyDisputePayment>;

/** The plan associated with the disputed payment. Null if the dispute is not linked to a specific plan. */
export interface LegacyDisputePlan {
  /** The unique identifier for the plan. */
  id: string;
}
export const LegacyDisputePlan = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "LegacyDisputePlan",
}) as any as S.Schema<LegacyDisputePlan>;

/** The product associated with the disputed payment. Null if the dispute is not linked to a specific product. */
export interface LegacyDisputeProduct {
  /** The unique identifier for the product. */
  id: string;
  /** The display name of the product shown to customers on the product page and in search results. */
  title: string;
}
export const LegacyDisputeProduct = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({
  identifier: "LegacyDisputeProduct",
}) as any as S.Schema<LegacyDisputeProduct>;

/** The refund policy document uploaded as dispute evidence. Null if no refund policy has been provided. */
export type LegacyDisputeRefundPolicyAttachment =
  LegacyDisputeCancellationPolicyAttachment;
export const LegacyDisputeRefundPolicyAttachment =
  LegacyDisputeCancellationPolicyAttachment;

/** The possible statuses of a dispute */
export type DisputeStatuses =
  | "warning_needs_response"
  | "warning_under_review"
  | "warning_closed"
  | "needs_response"
  | "under_review"
  | "won"
  | "lost"
  | "closed"
  | "other";
export const DisputeStatuses = /*@__PURE__*/ S.String;

/** An additional attachment that does not fit into the standard evidence categories. Null if not provided. */
export type LegacyDisputeUncategorizedAttachment =
  LegacyDisputeCancellationPolicyAttachment;
export const LegacyDisputeUncategorizedAttachment =
  LegacyDisputeCancellationPolicyAttachment;

/** A dispute is a chargeback or payment challenge filed against a company, including evidence and response status. */
export interface LegacyDispute {
  /** A log of IP-based access activity for the customer on Whop, submitted as evidence in the dispute. */
  access_activity_log: string | null;
  /** The disputed amount in the specified currency, formatted as a decimal. */
  amount: number;
  /** The customer's billing address from their payment details, submitted as evidence in the dispute. */
  billing_address: string | null;
  /** The cancellation policy document uploaded as dispute evidence. Null if no cancellation policy has been provided. */
  cancellation_policy_attachment: LegacyDisputeCancellationPolicyAttachment | null;
  /** A text disclosure describing the company's cancellation policy, submitted as dispute evidence. */
  cancellation_policy_disclosure: string | null;
  /** The company that the dispute was filed against. */
  company: LegacyDisputeCompany | null;
  /** The datetime the dispute was created. */
  created_at: string | null;
  /** The three-letter ISO currency code for the disputed amount. */
  currency: Currencies;
  /** Evidence of customer communication or product usage, uploaded as a dispute attachment. Null if not provided. */
  customer_communication_attachment: LegacyDisputeCancellationPolicyAttachment | null;
  /** The customer's email address from their payment details, included in the evidence packet sent to the payment processor. Editable before submission. */
  customer_email_address: string | null;
  /** The customer's full name from their payment details, included in the evidence packet sent to the payment processor. Editable before submission. */
  customer_name: string | null;
  /** Whether the dispute evidence can still be edited and submitted. */
  editable: boolean | null;
  /** The unique identifier for the dispute. */
  id: string;
  /** The deadline by which dispute evidence must be submitted. Null if no response deadline is set. */
  needs_response_by: string | null;
  /** Additional freeform notes submitted by the company as part of the dispute evidence. */
  notes: string | null;
  /** The original payment that was disputed. */
  payment: LegacyDisputePayment | null;
  /** The plan associated with the disputed payment. Null if the dispute is not linked to a specific plan. */
  plan: LegacyDisputePlan | null;
  /** The product associated with the disputed payment. Null if the dispute is not linked to a specific product. */
  product: LegacyDisputeProduct | null;
  /** A description of the product or service provided, submitted as dispute evidence. */
  product_description: string | null;
  /** A human-readable reason for the dispute. */
  reason: string | null;
  /** The card network reason code for the dispute. Null when the payment processor did not provide one. */
  reason_code: string | null;
  /** The refund policy document uploaded as dispute evidence. Null if no refund policy has been provided. */
  refund_policy_attachment: LegacyDisputeCancellationPolicyAttachment | null;
  /** A text disclosure describing the company's refund policy, submitted as dispute evidence. */
  refund_policy_disclosure: string | null;
  /** An explanation from the company for why a refund was refused, submitted as dispute evidence. */
  refund_refusal_explanation: string | null;
  /** The date when the product or service was delivered to the customer, submitted as dispute evidence. */
  service_date: string | null;
  /** The current status of the dispute lifecycle, such as needs_response, under_review, won, or lost. */
  status: DisputeStatuses;
  /** An additional attachment that does not fit into the standard evidence categories. Null if not provided. */
  uncategorized_attachment: LegacyDisputeCancellationPolicyAttachment | null;
  /** Whether the dispute was automatically resolved through Visa Rapid Dispute Resolution (RDR). */
  visa_rdr: boolean;
}
export const LegacyDispute = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    access_activity_log: S.NullOr(S.String),
    amount: S.Number,
    billing_address: S.NullOr(S.String),
    cancellation_policy_attachment: S.NullOr(
      LegacyDisputeCancellationPolicyAttachment,
    ),
    cancellation_policy_disclosure: S.NullOr(S.String),
    company: S.NullOr(LegacyDisputeCompany),
    created_at: S.NullOr(S.String),
    currency: Currencies,
    customer_communication_attachment: S.NullOr(
      LegacyDisputeCancellationPolicyAttachment,
    ),
    customer_email_address: S.NullOr(S.String),
    customer_name: S.NullOr(S.String),
    editable: S.NullOr(S.Boolean),
    id: S.String,
    needs_response_by: S.NullOr(S.String),
    notes: S.NullOr(S.String),
    payment: S.NullOr(LegacyDisputePayment),
    plan: S.NullOr(LegacyDisputePlan),
    product: S.NullOr(LegacyDisputeProduct),
    product_description: S.NullOr(S.String),
    reason: S.NullOr(S.String),
    reason_code: S.NullOr(S.String),
    refund_policy_attachment: S.NullOr(
      LegacyDisputeCancellationPolicyAttachment,
    ),
    refund_policy_disclosure: S.NullOr(S.String),
    refund_refusal_explanation: S.NullOr(S.String),
    service_date: S.NullOr(S.String),
    status: DisputeStatuses,
    uncategorized_attachment: S.NullOr(
      LegacyDisputeCancellationPolicyAttachment,
    ),
    visa_rdr: S.Boolean,
  }),
).annotate({ identifier: "LegacyDispute" }) as any as S.Schema<LegacyDispute>;

/** The cancellation policy document. */
export interface UpdateDisputeRequestEvidenceCancellationPolicyAttachment {
  /** The ID returned by a direct upload. */
  direct_upload_id?: string;
  /** The ID of an already-uploaded file. */
  id?: string;
}
export const UpdateDisputeRequestEvidenceCancellationPolicyAttachment =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      direct_upload_id: S.optional(S.String),
      id: S.optional(S.String),
    }),
  ).annotate({
    identifier: "UpdateDisputeRequestEvidenceCancellationPolicyAttachment",
  }) as any as S.Schema<UpdateDisputeRequestEvidenceCancellationPolicyAttachment>;

/** Correspondence with the customer, or proof they used the product. */
export type UpdateDisputeRequestEvidenceCustomerCommunicationAttachment =
  UpdateDisputeRequestEvidenceCancellationPolicyAttachment;
export const UpdateDisputeRequestEvidenceCustomerCommunicationAttachment =
  UpdateDisputeRequestEvidenceCancellationPolicyAttachment;

/** The refund policy document. */
export type UpdateDisputeRequestEvidenceRefundPolicyAttachment =
  UpdateDisputeRequestEvidenceCancellationPolicyAttachment;
export const UpdateDisputeRequestEvidenceRefundPolicyAttachment =
  UpdateDisputeRequestEvidenceCancellationPolicyAttachment;

/** Supporting evidence that does not fit the other categories. */
export type UpdateDisputeRequestEvidenceUncategorizedAttachment =
  UpdateDisputeRequestEvidenceCancellationPolicyAttachment;
export const UpdateDisputeRequestEvidenceUncategorizedAttachment =
  UpdateDisputeRequestEvidenceCancellationPolicyAttachment;

/** The evidence packet to send to the processor. Only the fields you provide are changed. */
export interface UpdateDisputeRequestEvidence {
  /** Log of the customer's access to the product, such as sign-in or download activity. */
  access_activity_log?: string | null;
  /** The billing address the customer provided at checkout. */
  billing_address?: string | null;
  /** The cancellation policy document. */
  cancellation_policy_attachment?: UpdateDisputeRequestEvidenceCancellationPolicyAttachment | null;
  /** How the cancellation policy was shown to the customer before purchase. */
  cancellation_policy_disclosure?: string | null;
  /** Correspondence with the customer, or proof they used the product. */
  customer_communication_attachment?: UpdateDisputeRequestEvidenceCancellationPolicyAttachment | null;
  /** The email address the customer used at checkout. */
  customer_email_address?: string | null;
  /** The customer's name as given at checkout. */
  customer_name?: string | null;
  /** Any additional context for the processor reviewing the dispute. */
  notes?: string | null;
  /** What the customer purchased, in the seller's own words. */
  product_description?: string | null;
  /** The refund policy document. */
  refund_policy_attachment?: UpdateDisputeRequestEvidenceCancellationPolicyAttachment | null;
  /** How the refund policy was shown to the customer before purchase. */
  refund_policy_disclosure?: string | null;
  /** Why a refund was refused, when one was requested and denied. */
  refund_refusal_explanation?: string | null;
  /** When the product or service was delivered. */
  service_date?: string | null;
  /** Supporting evidence that does not fit the other categories. */
  uncategorized_attachment?: UpdateDisputeRequestEvidenceCancellationPolicyAttachment | null;
}
export const UpdateDisputeRequestEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    access_activity_log: S.optional(S.NullOr(S.String)),
    billing_address: S.optional(S.NullOr(S.String)),
    cancellation_policy_attachment: S.optional(
      S.NullOr(UpdateDisputeRequestEvidenceCancellationPolicyAttachment),
    ),
    cancellation_policy_disclosure: S.optional(S.NullOr(S.String)),
    customer_communication_attachment: S.optional(
      S.NullOr(UpdateDisputeRequestEvidenceCancellationPolicyAttachment),
    ),
    customer_email_address: S.optional(S.NullOr(S.String)),
    customer_name: S.optional(S.NullOr(S.String)),
    notes: S.optional(S.NullOr(S.String)),
    product_description: S.optional(S.NullOr(S.String)),
    refund_policy_attachment: S.optional(
      S.NullOr(UpdateDisputeRequestEvidenceCancellationPolicyAttachment),
    ),
    refund_policy_disclosure: S.optional(S.NullOr(S.String)),
    refund_refusal_explanation: S.optional(S.NullOr(S.String)),
    service_date: S.optional(S.NullOr(S.String)),
    uncategorized_attachment: S.optional(
      S.NullOr(UpdateDisputeRequestEvidenceCancellationPolicyAttachment),
    ),
  }),
).annotate({
  identifier: "UpdateDisputeRequestEvidence",
}) as any as S.Schema<UpdateDisputeRequestEvidence>;

export interface UpdateDisputeRequest {
  /** The dispute ID (`dspt_` tag). */
  id: string;
  /** The evidence packet to send to the processor. Only the fields you provide are changed. */
  evidence?: UpdateDisputeRequestEvidence;
}
export const UpdateDisputeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    evidence: S.optional(UpdateDisputeRequestEvidence),
  }).pipe(T.Http({ method: "PATCH", uri: "/disputes/{id}", code: 200 })),
).annotate({
  identifier: "UpdateDisputeRequest",
}) as any as S.Schema<UpdateDisputeRequest>;

/** A file upload containing the company's cancellation policy document. */
export interface UpdateEvidenceDisputeRequestCancellationPolicyAttachment {
  /** The ID of an existing file object. */
  id: string;
}
export const UpdateEvidenceDisputeRequestCancellationPolicyAttachment =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      id: S.String,
    }),
  ).annotate({
    identifier: "UpdateEvidenceDisputeRequestCancellationPolicyAttachment",
  }) as any as S.Schema<UpdateEvidenceDisputeRequestCancellationPolicyAttachment>;

/** A file upload containing evidence of customer communication. Must be a JPEG, PNG, GIF, or PDF. */
export type UpdateEvidenceDisputeRequestCustomerCommunicationAttachment =
  UpdateEvidenceDisputeRequestCancellationPolicyAttachment;
export const UpdateEvidenceDisputeRequestCustomerCommunicationAttachment =
  UpdateEvidenceDisputeRequestCancellationPolicyAttachment;

/** A file upload containing the company's refund policy document. */
export type UpdateEvidenceDisputeRequestRefundPolicyAttachment =
  UpdateEvidenceDisputeRequestCancellationPolicyAttachment;
export const UpdateEvidenceDisputeRequestRefundPolicyAttachment =
  UpdateEvidenceDisputeRequestCancellationPolicyAttachment;

/** A file upload for evidence that does not fit into the other categories. */
export type UpdateEvidenceDisputeRequestUncategorizedAttachment =
  UpdateEvidenceDisputeRequestCancellationPolicyAttachment;
export const UpdateEvidenceDisputeRequestUncategorizedAttachment =
  UpdateEvidenceDisputeRequestCancellationPolicyAttachment;

export interface UpdateEvidenceDisputeRequest {
  /** The unique identifier of the dispute to update. */
  id: string;
  /** An IP access activity log showing the customer used the service. */
  access_activity_log?: string | null;
  /** The billing address associated with the customer's payment method. */
  billing_address?: string | null;
  /** A file upload containing the company's cancellation policy document. */
  cancellation_policy_attachment?: UpdateEvidenceDisputeRequestCancellationPolicyAttachment | null;
  /** The company's cancellation policy text to submit as evidence. */
  cancellation_policy_disclosure?: string | null;
  /** A file upload containing evidence of customer communication. Must be a JPEG, PNG, GIF, or PDF. */
  customer_communication_attachment?: UpdateEvidenceDisputeRequestCancellationPolicyAttachment | null;
  /** The email address of the customer associated with the disputed payment. */
  customer_email_address?: string | null;
  /** The full name of the customer associated with the disputed payment. */
  customer_name?: string | null;
  /** Additional notes or context to submit as part of the dispute evidence. */
  notes?: string | null;
  /** A description of the product or service that was provided to the customer. */
  product_description?: string | null;
  /** A file upload containing the company's refund policy document. */
  refund_policy_attachment?: UpdateEvidenceDisputeRequestCancellationPolicyAttachment | null;
  /** The company's refund policy text to submit as evidence. */
  refund_policy_disclosure?: string | null;
  /** An explanation of why the refund request was refused. */
  refund_refusal_explanation?: string | null;
  /** The date when the product or service was delivered to the customer. */
  service_date?: string | null;
  /** A file upload for evidence that does not fit into the other categories. */
  uncategorized_attachment?: UpdateEvidenceDisputeRequestCancellationPolicyAttachment | null;
}
export const UpdateEvidenceDisputeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    access_activity_log: S.optional(S.NullOr(S.String)),
    billing_address: S.optional(S.NullOr(S.String)),
    cancellation_policy_attachment: S.optional(
      S.NullOr(UpdateEvidenceDisputeRequestCancellationPolicyAttachment),
    ),
    cancellation_policy_disclosure: S.optional(S.NullOr(S.String)),
    customer_communication_attachment: S.optional(
      S.NullOr(UpdateEvidenceDisputeRequestCancellationPolicyAttachment),
    ),
    customer_email_address: S.optional(S.NullOr(S.String)),
    customer_name: S.optional(S.NullOr(S.String)),
    notes: S.optional(S.NullOr(S.String)),
    product_description: S.optional(S.NullOr(S.String)),
    refund_policy_attachment: S.optional(
      S.NullOr(UpdateEvidenceDisputeRequestCancellationPolicyAttachment),
    ),
    refund_policy_disclosure: S.optional(S.NullOr(S.String)),
    refund_refusal_explanation: S.optional(S.NullOr(S.String)),
    service_date: S.optional(S.NullOr(S.String)),
    uncategorized_attachment: S.optional(
      S.NullOr(UpdateEvidenceDisputeRequestCancellationPolicyAttachment),
    ),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/disputes/{id}/update_evidence",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateEvidenceDisputeRequest",
}) as any as S.Schema<UpdateEvidenceDisputeRequest>;

/** What kind of evidence the document is. */
export type UploadDisputeEvidenceRequestDocumentsItemDocumentType =
  | "return_policy"
  | "shipping_policy"
  | "physical_fulfillment"
  | "customer_order_history"
  | "product_image"
  | "prior_transactions"
  | "customer_session"
  | "digital_fulfillment"
  | "subscription";
export const UploadDisputeEvidenceRequestDocumentsItemDocumentType =
  /*@__PURE__*/ S.String;

export interface UploadDisputeEvidenceRequestDocumentsItem {
  /** The ID returned by a direct upload. */
  direct_upload_id?: string;
  /** What kind of evidence the document is. */
  document_type:
    | UploadDisputeEvidenceRequestDocumentsItemDocumentType
    | (string & {});
  /** The file itself. Send it as a file part to upload and attach in one call, or use `id`/`direct_upload_id` for a file that is already stored. */
  file?: string;
  /** The ID of a file already stored on Whop, prefixed `file_`. */
  id?: string;
}
export const UploadDisputeEvidenceRequestDocumentsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      direct_upload_id: S.optional(S.String),
      document_type: UploadDisputeEvidenceRequestDocumentsItemDocumentType,
      file: S.optional(S.String),
      id: S.optional(S.String),
    }),
  ).annotate({
    identifier: "UploadDisputeEvidenceRequestDocumentsItem",
  }) as any as S.Schema<UploadDisputeEvidenceRequestDocumentsItem>;

/** The full set of evidence documents the dispute should carry. Replaces all previously uploaded documents. */
export type UploadDisputeEvidenceRequestDocumentsList =
  Array<UploadDisputeEvidenceRequestDocumentsItem>;
export const UploadDisputeEvidenceRequestDocumentsList = /*@__PURE__*/ S.Array(
  UploadDisputeEvidenceRequestDocumentsItem,
) as any as S.Schema<UploadDisputeEvidenceRequestDocumentsList>;

export interface UploadDisputeEvidenceRequest {
  /** The dispute ID (`dspt_` tag). */
  id: string;
  /** The full set of evidence documents the dispute should carry. Replaces all previously uploaded documents. */
  documents: UploadDisputeEvidenceRequestDocumentsList;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const UploadDisputeEvidenceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    documents: UploadDisputeEvidenceRequestDocumentsList,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/disputes/{id}/upload_evidence",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UploadDisputeEvidenceRequest",
}) as any as S.Schema<UploadDisputeEvidenceRequest>;

export type ListDisputesError = BadRequest | Forbidden | WhopOpError;
/** List Disputes Lists the disputes across the accounts you can read. */
export const listDisputes: API.PaginatedOperationMethod<
  ListDisputesRequest,
  ListDisputesResponse,
  ListDisputesError,
  WhopOpContext,
  Dispute
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListDisputesRequest,
    output: ListDisputesResponse,
    errors: [BadRequest, Forbidden],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveDisputeError = NotFound | WhopOpError;
/** Retrieve Dispute Retrieves a single dispute. */
export const retrieveDispute: API.OperationMethod<
  RetrieveDisputeRequest,
  Dispute,
  RetrieveDisputeError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveDisputeRequest,
  output: Dispute,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveDisputeSummaryError = BadRequest | WhopOpError;
/** Retrieve Dispute Summary Totals up the same disputes the list returns, so you can build status tabs and totals without paging through them. */
export const retrieveDisputeSummary: API.OperationMethod<
  RetrieveDisputeSummaryRequest,
  RetrieveDisputeSummaryResponse,
  RetrieveDisputeSummaryError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveDisputeSummaryRequest,
  output: RetrieveDisputeSummaryResponse,
  errors: [BadRequest],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type SubmitDisputeError = BadRequest | NotFound | Conflict | WhopOpError;
/** Submit Dispute Sends a dispute's evidence to the payment processor. This is final — it cannot be edited or sent again. */
export const submitDispute: API.OperationMethod<
  SubmitDisputeRequest,
  Dispute,
  SubmitDisputeError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubmitDisputeRequest,
  output: Dispute,
  errors: [BadRequest, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type SubmitEvidenceDisputeError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Submit evidence [Legacy API — https://docs.whop.com/api-reference] Submit a payment dispute to the payment processor for review. Once submitted, no further edits can be made. Required permissions: - `payment:dispute` - `plan:basic:read` - `access_pass:basic:read` - `company:basic:read` - `payment:basic:read` - `member:email:read` - `member:basic:read` - `member:phone:read` */
export const submitEvidenceDispute: API.OperationMethod<
  SubmitEvidenceDisputeRequest,
  LegacyDispute,
  SubmitEvidenceDisputeError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubmitEvidenceDisputeRequest,
  output: LegacyDispute,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateDisputeError = BadRequest | NotFound | WhopOpError;
/** Update Dispute Edits a dispute's evidence, while it is still editable. Sending it is a separate call. */
export const updateDispute: API.OperationMethod<
  UpdateDisputeRequest,
  Dispute,
  UpdateDisputeError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateDisputeRequest,
  output: Dispute,
  errors: [BadRequest, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateEvidenceDisputeError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update evidence [Legacy API — https://docs.whop.com/api-reference] Update a dispute with evidence data to attempt to win the dispute. Required permissions: - `payment:dispute` - `plan:basic:read` - `access_pass:basic:read` - `company:basic:read` - `payment:basic:read` - `member:email:read` - `member:basic:read` - `member:phone:read` */
export const updateEvidenceDispute: API.OperationMethod<
  UpdateEvidenceDisputeRequest,
  LegacyDispute,
  UpdateEvidenceDisputeError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateEvidenceDisputeRequest,
  output: LegacyDispute,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UploadDisputeEvidenceError =
  | BadRequest
  | NotFound
  | Conflict
  | WhopOpError;
/** Upload Dispute Evidence Replaces the full set of uploaded evidence documents on a dispute, beyond the four fixed evidence slots. Upload files through `POST /files` and reference them by `id`, or send the files as multipart file parts to upload and attach in one call. Send every document the packet should carry — up to 10, 10MB each and 25MB in total; an empty list removes them all. Accepted content types: application/pdf, application/json, image/jpeg, image/png, image/webp — any other type is rejected. */
export const uploadDisputeEvidence: API.OperationMethod<
  UploadDisputeEvidenceRequest,
  Dispute,
  UploadDisputeEvidenceError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UploadDisputeEvidenceRequest,
  output: Dispute,
  errors: [BadRequest, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
