// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** The window the spend limit applies to. */
export type CreateCardRequestSpendLimitFrequency =
  | "daily"
  | "weekly"
  | "monthly"
  | "one_time";
export const CreateCardRequestSpendLimitFrequency = /*@__PURE__*/ S.String;

export interface CreateCardRequest {
  /** The owning account ID (a biz_ identifier). Provide this or user_id. */
  account_id?: string;
  /** The account member (a user_ identifier) to assign the card to. Required for business card issuing accounts. */
  assigned_user_id?: string;
  /** A display name for the card. */
  name?: string;
  /** Spending limit amount, in dollars. */
  spend_limit?: number;
  /** The window the spend limit applies to. */
  spend_limit_frequency?: CreateCardRequestSpendLimitFrequency | (string & {});
  /** Per-transaction limit amount, in dollars. */
  transaction_limit?: number;
  /** The owning user ID (a user_ identifier). Provide this or account_id. */
  user_id?: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreateCardRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    assigned_user_id: S.optional(S.String),
    name: S.optional(S.String),
    spend_limit: S.optional(S.Number),
    spend_limit_frequency: S.optional(CreateCardRequestSpendLimitFrequency),
    transaction_limit: S.optional(S.Number),
    user_id: S.optional(S.String),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/cards", code: 200 })),
).annotate({
  identifier: "CreateCardRequest",
}) as any as S.Schema<CreateCardRequest>;

/** The billing address. */
export interface CreateCardResponseBilling {
  /** Billing city. */
  city: string | null;
  /** Billing country code. */
  country_code: string | null;
  /** Street address line 1. */
  line1: string | null;
  /** Street address line 2. */
  line2: string | null;
  /** Billing postal code. */
  postal_code: string | null;
  /** Billing region or state. */
  region: string | null;
}
export const CreateCardResponseBilling = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    city: S.NullOr(S.String),
    country_code: S.NullOr(S.String),
    line1: S.NullOr(S.String),
    line2: S.NullOr(S.String),
    postal_code: S.NullOr(S.String),
    region: S.NullOr(S.String),
  }),
).annotate({
  identifier: "CreateCardResponseBilling",
}) as any as S.Schema<CreateCardResponseBilling>;

/** The window the limit amount applies to. `per_transaction` caps each individual authorization and is what a limit set with `transaction_limit` reports. */
export type CreateCardResponseLimitFrequency =
  | "daily"
  | "weekly"
  | "monthly"
  | "one_time"
  | "per_transaction";
export const CreateCardResponseLimitFrequency = /*@__PURE__*/ S.String;

/** The spending limit configuration. */
export interface CreateCardResponseLimit {
  /** The limit amount in dollars. */
  amount: number;
  /** The window the limit amount applies to. `per_transaction` caps each individual authorization and is what a limit set with `transaction_limit` reports. */
  frequency: CreateCardResponseLimitFrequency;
}
export const CreateCardResponseLimit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.Number,
    frequency: CreateCardResponseLimitFrequency,
  }),
).annotate({
  identifier: "CreateCardResponseLimit",
}) as any as S.Schema<CreateCardResponseLimit>;

export type CreateCardResponseObject = "card";
export const CreateCardResponseObject = /*@__PURE__*/ S.String;

/** Sensitive card details. Present only on `GET /cards/:id` for active cards; `null` when the card is inactive or details cannot be retrieved. */
export interface CreateCardResponseSecrets {
  /** Full card number. */
  card_number: string;
  /** Card verification code. */
  cvc: string;
  /** Cardholder name printed on the card. */
  name_on_card: string | null;
  /** The card PIN. Only returned when the request is authenticated as the user the card is assigned to; `null` for all other callers, including account API keys. */
  pin: string | null;
}
export const CreateCardResponseSecrets = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    card_number: S.String,
    cvc: S.String,
    name_on_card: S.NullOr(S.String),
    pin: S.NullOr(S.String),
  }),
).annotate({
  identifier: "CreateCardResponseSecrets",
}) as any as S.Schema<CreateCardResponseSecrets>;

/** The card status. `denied` means the issuer declined the cardholder, so the card will never be issued. */
export type CreateCardResponseStatus =
  | "active"
  | "frozen"
  | "canceled"
  | "invited"
  | "denied";
export const CreateCardResponseStatus = /*@__PURE__*/ S.String;

/** The card type. */
export type CreateCardResponseType = "virtual" | "physical";
export const CreateCardResponseType = /*@__PURE__*/ S.String;

export interface CreateCardResponse {
  /** The billing address. */
  billing: CreateCardResponseBilling | null;
  /** When the card was canceled. */
  canceled_at: string | null;
  /** When the card was created. */
  created_at: string | null;
  /** Card expiration month. */
  expiration_month: string | null;
  /** Card expiration year. */
  expiration_year: string | null;
  /** Card ID, prefixed `icrd_`. */
  id: string;
  /** Last four digits of the card number. `null` for pending invitation cards. */
  last4: string | null;
  /** The spending limit configuration. */
  limit: CreateCardResponseLimit | null;
  /** Card display name. */
  name: string | null;
  object: CreateCardResponseObject;
  /** Sensitive card details. Present only on `GET /cards/:id` for active cards; `null` when the card is inactive or details cannot be retrieved. */
  secrets?: CreateCardResponseSecrets | null;
  /** Total spend in the last 30 days, in cents. */
  spent_last_month: number | null;
  /** The card status. `denied` means the issuer declined the cardholder, so the card will never be issued. */
  status: CreateCardResponseStatus | null;
  /** The card type. */
  type: CreateCardResponseType | null;
  /** Cardholder user ID, prefixed `user_`, when assigned. */
  user_id: string | null;
}
export const CreateCardResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing: S.NullOr(CreateCardResponseBilling),
    canceled_at: S.NullOr(S.String),
    created_at: S.NullOr(S.String),
    expiration_month: S.NullOr(S.String),
    expiration_year: S.NullOr(S.String),
    id: S.String,
    last4: S.NullOr(S.String),
    limit: S.NullOr(CreateCardResponseLimit),
    name: S.NullOr(S.String),
    object: CreateCardResponseObject,
    secrets: S.optional(S.NullOr(CreateCardResponseSecrets)),
    spent_last_month: S.NullOr(S.Number),
    status: S.NullOr(CreateCardResponseStatus),
    type: S.NullOr(CreateCardResponseType),
    user_id: S.NullOr(S.String),
  }),
).annotate({
  identifier: "CreateCardResponse",
}) as any as S.Schema<CreateCardResponse>;

export interface ListCardsRequest {
  /** The owning account ID (a biz_ identifier). Provide this or user_id. */
  account_id?: string;
  /** The owning user ID (a user_ identifier). Provide this or account_id. */
  user_id?: string;
}
export const ListCardsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/cards", code: 200 })),
).annotate({
  identifier: "ListCardsRequest",
}) as any as S.Schema<ListCardsRequest>;

/** The billing address. */
export type ListCardsResponseDataItemBilling = CreateCardResponseBilling;
export const ListCardsResponseDataItemBilling = CreateCardResponseBilling;

/** The window the limit amount applies to. `per_transaction` caps each individual authorization and is what a limit set with `transaction_limit` reports. */
export type ListCardsResponseDataItemLimitFrequency =
  | "daily"
  | "weekly"
  | "monthly"
  | "one_time"
  | "per_transaction";
export const ListCardsResponseDataItemLimitFrequency = /*@__PURE__*/ S.String;

/** The spending limit configuration. */
export interface ListCardsResponseDataItemLimit {
  /** The limit amount in dollars. */
  amount: number;
  /** The window the limit amount applies to. `per_transaction` caps each individual authorization and is what a limit set with `transaction_limit` reports. */
  frequency: ListCardsResponseDataItemLimitFrequency;
}
export const ListCardsResponseDataItemLimit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.Number,
    frequency: ListCardsResponseDataItemLimitFrequency,
  }),
).annotate({
  identifier: "ListCardsResponseDataItemLimit",
}) as any as S.Schema<ListCardsResponseDataItemLimit>;

export type ListCardsResponseDataItemObject = "card";
export const ListCardsResponseDataItemObject = /*@__PURE__*/ S.String;

/** Sensitive card details. Present only on `GET /cards/:id` for active cards; `null` when the card is inactive or details cannot be retrieved. */
export type ListCardsResponseDataItemSecrets = CreateCardResponseSecrets;
export const ListCardsResponseDataItemSecrets = CreateCardResponseSecrets;

/** The card status. `denied` means the issuer declined the cardholder, so the card will never be issued. */
export type ListCardsResponseDataItemStatus =
  | "active"
  | "frozen"
  | "canceled"
  | "invited"
  | "denied";
export const ListCardsResponseDataItemStatus = /*@__PURE__*/ S.String;

/** The card type. */
export type ListCardsResponseDataItemType = "virtual" | "physical";
export const ListCardsResponseDataItemType = /*@__PURE__*/ S.String;

export interface ListCardsResponseDataItem {
  /** The billing address. */
  billing: CreateCardResponseBilling | null;
  /** When the card was canceled. */
  canceled_at: string | null;
  /** When the card was created. */
  created_at: string | null;
  /** Card expiration month. */
  expiration_month: string | null;
  /** Card expiration year. */
  expiration_year: string | null;
  /** Card ID, prefixed `icrd_`. */
  id: string;
  /** Last four digits of the card number. `null` for pending invitation cards. */
  last4: string | null;
  /** The spending limit configuration. */
  limit: ListCardsResponseDataItemLimit | null;
  /** Card display name. */
  name: string | null;
  object: ListCardsResponseDataItemObject;
  /** Sensitive card details. Present only on `GET /cards/:id` for active cards; `null` when the card is inactive or details cannot be retrieved. */
  secrets?: CreateCardResponseSecrets | null;
  /** Total spend in the last 30 days, in cents. */
  spent_last_month: number | null;
  /** The card status. `denied` means the issuer declined the cardholder, so the card will never be issued. */
  status: ListCardsResponseDataItemStatus | null;
  /** The card type. */
  type: ListCardsResponseDataItemType | null;
  /** Cardholder user ID, prefixed `user_`, when assigned. */
  user_id: string | null;
}
export const ListCardsResponseDataItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing: S.NullOr(CreateCardResponseBilling),
    canceled_at: S.NullOr(S.String),
    created_at: S.NullOr(S.String),
    expiration_month: S.NullOr(S.String),
    expiration_year: S.NullOr(S.String),
    id: S.String,
    last4: S.NullOr(S.String),
    limit: S.NullOr(ListCardsResponseDataItemLimit),
    name: S.NullOr(S.String),
    object: ListCardsResponseDataItemObject,
    secrets: S.optional(S.NullOr(CreateCardResponseSecrets)),
    spent_last_month: S.NullOr(S.Number),
    status: S.NullOr(ListCardsResponseDataItemStatus),
    type: S.NullOr(ListCardsResponseDataItemType),
    user_id: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListCardsResponseDataItem",
}) as any as S.Schema<ListCardsResponseDataItem>;

export type ListCardsResponseDataList = Array<ListCardsResponseDataItem>;
export const ListCardsResponseDataList = /*@__PURE__*/ S.Array(
  ListCardsResponseDataItem,
) as any as S.Schema<ListCardsResponseDataList>;

export interface ListCardsResponse {
  data: ListCardsResponseDataList;
}
export const ListCardsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListCardsResponseDataList,
  }),
).annotate({
  identifier: "ListCardsResponse",
}) as any as S.Schema<ListCardsResponse>;

export type ListCardTransactionsRequestTransactionIdsList = Array<string>;
export const ListCardTransactionsRequestTransactionIdsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ListCardTransactionsRequestTransactionIdsList>;

export type ListCardTransactionsRequestCardIdList = Array<string>;
export const ListCardTransactionsRequestCardIdList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListCardTransactionsRequestCardIdList>;

export type ListCardTransactionsRequestCardholderIdList = Array<string>;
export const ListCardTransactionsRequestCardholderIdList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ListCardTransactionsRequestCardholderIdList>;

export type ListCardTransactionsRequestStatus =
  | "pending"
  | "completed"
  | "reversed"
  | "declined";
export const ListCardTransactionsRequestStatus = /*@__PURE__*/ S.String;

export type ListCardTransactionsRequestOrder = "created_at";
export const ListCardTransactionsRequestOrder = /*@__PURE__*/ S.String;

export type ListCardTransactionsRequestDirection = "asc" | "desc";
export const ListCardTransactionsRequestDirection = /*@__PURE__*/ S.String;

export interface ListCardTransactionsRequest {
  /** The account whose card transactions to list, prefixed `biz_`. Defaults to the credential's account. */
  account_id?: string;
  /** Return only these card transactions, each prefixed `citx_`. Repeat the parameter, or pass one comma-separated value. */
  transaction_ids?: ListCardTransactionsRequestTransactionIdsList;
  /** Return only transactions charged to these cards, each prefixed `icrd_`. */
  card_id?: ListCardTransactionsRequestCardIdList;
  /** Return only transactions on cards assigned to these users, each prefixed `user_`. */
  cardholder_id?: ListCardTransactionsRequestCardholderIdList;
  /** Return only transactions with this status. */
  status?: ListCardTransactionsRequestStatus | (string & {});
  /** Return only transactions authorized at or after this ISO 8601 timestamp. */
  created_after?: string;
  /** Return only transactions authorized at or before this ISO 8601 timestamp. */
  created_before?: string;
  /** The field to sort by. Defaults to `created_at`. */
  order?: ListCardTransactionsRequestOrder | (string & {});
  /** The sort direction. Defaults to `desc`. */
  direction?: ListCardTransactionsRequestDirection | (string & {});
  /** The number of card transactions to return. */
  first?: number;
  /** A cursor; returns card transactions after this position. */
  after?: string;
  /** The number of card transactions to return, counting back from the end. */
  last?: number;
  /** A cursor; returns card transactions before this position. */
  before?: string;
}
export const ListCardTransactionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    transaction_ids: S.optional(
      ListCardTransactionsRequestTransactionIdsList.pipe(T.Query()),
    ),
    card_id: S.optional(ListCardTransactionsRequestCardIdList.pipe(T.Query())),
    cardholder_id: S.optional(
      ListCardTransactionsRequestCardholderIdList.pipe(T.Query()),
    ),
    status: S.optional(ListCardTransactionsRequestStatus.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListCardTransactionsRequestOrder.pipe(T.Query())),
    direction: S.optional(ListCardTransactionsRequestDirection.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/card_transactions", code: 200 })),
).annotate({
  identifier: "ListCardTransactionsRequest",
}) as any as S.Schema<ListCardTransactionsRequest>;

/** Current status of the transaction. */
export type CardTransactionStatus =
  | "pending"
  | "completed"
  | "reversed"
  | "declined";
export const CardTransactionStatus = /*@__PURE__*/ S.String;

/** The kind of card transaction. Always `spend` today. */
export type CardTransactionTransactionType = "spend";
export const CardTransactionTransactionType = /*@__PURE__*/ S.String;

export interface CardTransaction {
  /** The card this transaction was charged to, prefixed `icrd_`. */
  card_id: string;
  /** The user the card is assigned to, prefixed `user_`. Null when the card has no assigned cardholder. */
  cardholder_id: string | null;
  /** Cashback earned on this transaction as a USD amount. Zero for declined or ineligible transactions, and null when cashback has not been computed yet. */
  cashback_usd_amount: number | null;
  /** When the transaction was authorized, as an ISO 8601 timestamp. */
  created_at: string;
  /** ISO 4217 currency code the merchant charged in. */
  currency: string | null;
  /** Why the transaction was declined. Null unless `status` is `declined`. */
  declined_reason: string | null;
  /** Card transaction ID, prefixed `citx_`. */
  id: string;
  /** True when the merchant is outside the card's home country. */
  international: boolean;
  /** Amount the merchant charged in their own currency. Pair with `currency`. */
  local_amount: number | null;
  /** Merchant category label, enriched where available and otherwise as the card network reported it. */
  merchant_category: string | null;
  /** Four-digit ISO 18245 merchant category code (MCC). */
  merchant_category_code: string | null;
  /** URL of the enriched merchant logo. Null when no logo was matched. */
  merchant_icon_url: string | null;
  /** Merchant name, enriched where available and otherwise as the card network reported it. */
  merchant_name: string | null;
  /** When the card network settled the transaction, as an ISO 8601 timestamp. Null until it settles. */
  posted_at: string | null;
  /** Current status of the transaction. */
  status: CardTransactionStatus;
  /** The kind of card transaction. Always `spend` today. */
  transaction_type: CardTransactionTransactionType;
  /** Amount charged in USD. Negative when the merchant refunded the card. */
  usd_amount: number | null;
}
export const CardTransaction = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    card_id: S.String,
    cardholder_id: S.NullOr(S.String),
    cashback_usd_amount: S.NullOr(S.Number),
    created_at: S.String,
    currency: S.NullOr(S.String),
    declined_reason: S.NullOr(S.String),
    id: S.String,
    international: S.Boolean,
    local_amount: S.NullOr(S.Number),
    merchant_category: S.NullOr(S.String),
    merchant_category_code: S.NullOr(S.String),
    merchant_icon_url: S.NullOr(S.String),
    merchant_name: S.NullOr(S.String),
    posted_at: S.NullOr(S.String),
    status: CardTransactionStatus,
    transaction_type: CardTransactionTransactionType,
    usd_amount: S.NullOr(S.Number),
  }),
).annotate({
  identifier: "CardTransaction",
}) as any as S.Schema<CardTransaction>;

export type ListCardTransactionsResponseDataList = Array<CardTransaction>;
export const ListCardTransactionsResponseDataList = /*@__PURE__*/ S.Array(
  CardTransaction,
) as any as S.Schema<ListCardTransactionsResponseDataList>;

export interface ListCardTransactionsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListCardTransactionsResponsePageInfo = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      end_cursor: S.NullOr(S.String),
      has_next_page: S.Boolean,
      has_previous_page: S.Boolean,
      start_cursor: S.NullOr(S.String),
    }),
).annotate({
  identifier: "ListCardTransactionsResponsePageInfo",
}) as any as S.Schema<ListCardTransactionsResponsePageInfo>;

export interface ListCardTransactionsResponse {
  data: ListCardTransactionsResponseDataList;
  page_info: ListCardTransactionsResponsePageInfo;
}
export const ListCardTransactionsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListCardTransactionsResponseDataList,
    page_info: ListCardTransactionsResponsePageInfo,
  }),
).annotate({
  identifier: "ListCardTransactionsResponse",
}) as any as S.Schema<ListCardTransactionsResponse>;

export interface RetrieveCardRequest {
  /** Card ID to retrieve, prefixed `icrd_`. */
  id: string;
  /** The owning account ID (a biz_ identifier). Provide this or user_id. */
  account_id?: string;
  /** The owning user ID (a user_ identifier). Provide this or account_id. */
  user_id?: string;
}
export const RetrieveCardRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    account_id: S.optional(S.String.pipe(T.Query())),
    user_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/cards/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveCardRequest",
}) as any as S.Schema<RetrieveCardRequest>;

/** The billing address. */
export type RetrieveCardResponseBilling = CreateCardResponseBilling;
export const RetrieveCardResponseBilling = CreateCardResponseBilling;

/** The window the limit amount applies to. `per_transaction` caps each individual authorization and is what a limit set with `transaction_limit` reports. */
export type RetrieveCardResponseLimitFrequency =
  | "daily"
  | "weekly"
  | "monthly"
  | "one_time"
  | "per_transaction";
export const RetrieveCardResponseLimitFrequency = /*@__PURE__*/ S.String;

/** The spending limit configuration. */
export interface RetrieveCardResponseLimit {
  /** The limit amount in dollars. */
  amount: number;
  /** The window the limit amount applies to. `per_transaction` caps each individual authorization and is what a limit set with `transaction_limit` reports. */
  frequency: RetrieveCardResponseLimitFrequency;
}
export const RetrieveCardResponseLimit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.Number,
    frequency: RetrieveCardResponseLimitFrequency,
  }),
).annotate({
  identifier: "RetrieveCardResponseLimit",
}) as any as S.Schema<RetrieveCardResponseLimit>;

export type RetrieveCardResponseObject = "card";
export const RetrieveCardResponseObject = /*@__PURE__*/ S.String;

/** Sensitive card details. Present only on `GET /cards/:id` for active cards; `null` when the card is inactive or details cannot be retrieved. */
export type RetrieveCardResponseSecrets = CreateCardResponseSecrets;
export const RetrieveCardResponseSecrets = CreateCardResponseSecrets;

/** The card status. `denied` means the issuer declined the cardholder, so the card will never be issued. */
export type RetrieveCardResponseStatus =
  | "active"
  | "frozen"
  | "canceled"
  | "invited"
  | "denied";
export const RetrieveCardResponseStatus = /*@__PURE__*/ S.String;

/** The card type. */
export type RetrieveCardResponseType = "virtual" | "physical";
export const RetrieveCardResponseType = /*@__PURE__*/ S.String;

export interface RetrieveCardResponse {
  /** The billing address. */
  billing: CreateCardResponseBilling | null;
  /** When the card was canceled. */
  canceled_at: string | null;
  /** When the card was created. */
  created_at: string | null;
  /** Card expiration month. */
  expiration_month: string | null;
  /** Card expiration year. */
  expiration_year: string | null;
  /** Card ID, prefixed `icrd_`. */
  id: string;
  /** Last four digits of the card number. `null` for pending invitation cards. */
  last4: string | null;
  /** The spending limit configuration. */
  limit: RetrieveCardResponseLimit | null;
  /** Card display name. */
  name: string | null;
  object: RetrieveCardResponseObject;
  /** Sensitive card details. Present only on `GET /cards/:id` for active cards; `null` when the card is inactive or details cannot be retrieved. */
  secrets?: CreateCardResponseSecrets | null;
  /** Total spend in the last 30 days, in cents. */
  spent_last_month: number | null;
  /** The card status. `denied` means the issuer declined the cardholder, so the card will never be issued. */
  status: RetrieveCardResponseStatus | null;
  /** The card type. */
  type: RetrieveCardResponseType | null;
  /** Cardholder user ID, prefixed `user_`, when assigned. */
  user_id: string | null;
}
export const RetrieveCardResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing: S.NullOr(CreateCardResponseBilling),
    canceled_at: S.NullOr(S.String),
    created_at: S.NullOr(S.String),
    expiration_month: S.NullOr(S.String),
    expiration_year: S.NullOr(S.String),
    id: S.String,
    last4: S.NullOr(S.String),
    limit: S.NullOr(RetrieveCardResponseLimit),
    name: S.NullOr(S.String),
    object: RetrieveCardResponseObject,
    secrets: S.optional(S.NullOr(CreateCardResponseSecrets)),
    spent_last_month: S.NullOr(S.Number),
    status: S.NullOr(RetrieveCardResponseStatus),
    type: S.NullOr(RetrieveCardResponseType),
    user_id: S.NullOr(S.String),
  }),
).annotate({
  identifier: "RetrieveCardResponse",
}) as any as S.Schema<RetrieveCardResponse>;

export interface RetrieveCardTransactionRequest {
  /** The card transaction ID, prefixed `citx_`. */
  id: string;
  /** The account that owns the transaction, prefixed `biz_`. Defaults to the credential's account. */
  account_id?: string;
}
export const RetrieveCardTransactionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    account_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/card_transactions/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveCardTransactionRequest",
}) as any as S.Schema<RetrieveCardTransactionRequest>;

/** New billing address. Requires line1, city, region, postal_code, and country_code. On an invited card, passing billing alone (as the invited user) completes onboarding and starts card provisioning. */
export interface UpdateCardRequestBilling {
  /** Billing city. */
  city: string;
  /** Billing country code, ISO 3166-1 alpha-2. */
  country_code: string;
  /** Street address line 1. */
  line1: string;
  /** Street address line 2. */
  line2?: string;
  /** Billing postal code. */
  postal_code: string;
  /** Billing region or state. */
  region: string;
}
export const UpdateCardRequestBilling = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    city: S.String,
    country_code: S.String,
    line1: S.String,
    line2: S.optional(S.String),
    postal_code: S.String,
    region: S.String,
  }),
).annotate({
  identifier: "UpdateCardRequestBilling",
}) as any as S.Schema<UpdateCardRequestBilling>;

/** The window the spend limit applies to. */
export type UpdateCardRequestSpendLimitFrequency =
  | "daily"
  | "weekly"
  | "monthly"
  | "one_time";
export const UpdateCardRequestSpendLimitFrequency = /*@__PURE__*/ S.String;

export interface UpdateCardRequest {
  /** Card ID to retrieve, prefixed `icrd_`. */
  id: string;
  /** The owning account ID (a biz_ identifier). Provide this or user_id. */
  account_id?: string;
  /** New billing address. Requires line1, city, region, postal_code, and country_code. On an invited card, passing billing alone (as the invited user) completes onboarding and starts card provisioning. */
  billing?: UpdateCardRequestBilling;
  /** Pass `true` to permanently cancel the card. A canceled card cannot be uncanceled. Cannot be combined with other fields. */
  canceled?: boolean;
  /** Pass `true` to freeze the card, `false` to unfreeze it. The assigned cardholder may freeze their own card without the payout:account:update scope. */
  frozen?: boolean;
  /** A display name for the card. */
  name?: string;
  /** New 4-digit PIN. Can only be set on a card assigned to the acting user, who may set it without the payout:account:update scope. */
  pin?: string;
  /** Pass `true` to remove the spending limit (make the card unlimited). */
  remove_limit?: boolean;
  /** Spending limit amount, in dollars. */
  spend_limit?: number;
  /** The window the spend limit applies to. */
  spend_limit_frequency?: UpdateCardRequestSpendLimitFrequency | (string & {});
  /** Per-transaction limit amount, in dollars. */
  transaction_limit?: number;
  /** The owning user ID (a user_ identifier). Provide this or account_id. */
  user_id?: string;
}
export const UpdateCardRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    account_id: S.optional(S.String),
    billing: S.optional(UpdateCardRequestBilling),
    canceled: S.optional(S.Boolean),
    frozen: S.optional(S.Boolean),
    name: S.optional(S.String),
    pin: S.optional(S.String),
    remove_limit: S.optional(S.Boolean),
    spend_limit: S.optional(S.Number),
    spend_limit_frequency: S.optional(UpdateCardRequestSpendLimitFrequency),
    transaction_limit: S.optional(S.Number),
    user_id: S.optional(S.String),
  }).pipe(T.Http({ method: "PATCH", uri: "/cards/{id}", code: 200 })),
).annotate({
  identifier: "UpdateCardRequest",
}) as any as S.Schema<UpdateCardRequest>;

/** The billing address. */
export type UpdateCardResponseBilling = CreateCardResponseBilling;
export const UpdateCardResponseBilling = CreateCardResponseBilling;

/** The window the limit amount applies to. `per_transaction` caps each individual authorization and is what a limit set with `transaction_limit` reports. */
export type UpdateCardResponseLimitFrequency =
  | "daily"
  | "weekly"
  | "monthly"
  | "one_time"
  | "per_transaction";
export const UpdateCardResponseLimitFrequency = /*@__PURE__*/ S.String;

/** The spending limit configuration. */
export interface UpdateCardResponseLimit {
  /** The limit amount in dollars. */
  amount: number;
  /** The window the limit amount applies to. `per_transaction` caps each individual authorization and is what a limit set with `transaction_limit` reports. */
  frequency: UpdateCardResponseLimitFrequency;
}
export const UpdateCardResponseLimit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.Number,
    frequency: UpdateCardResponseLimitFrequency,
  }),
).annotate({
  identifier: "UpdateCardResponseLimit",
}) as any as S.Schema<UpdateCardResponseLimit>;

export type UpdateCardResponseObject = "card";
export const UpdateCardResponseObject = /*@__PURE__*/ S.String;

/** Sensitive card details. Present only on `GET /cards/:id` for active cards; `null` when the card is inactive or details cannot be retrieved. */
export type UpdateCardResponseSecrets = CreateCardResponseSecrets;
export const UpdateCardResponseSecrets = CreateCardResponseSecrets;

/** The card status. `denied` means the issuer declined the cardholder, so the card will never be issued. */
export type UpdateCardResponseStatus =
  | "active"
  | "frozen"
  | "canceled"
  | "invited"
  | "denied";
export const UpdateCardResponseStatus = /*@__PURE__*/ S.String;

/** The card type. */
export type UpdateCardResponseType = "virtual" | "physical";
export const UpdateCardResponseType = /*@__PURE__*/ S.String;

export interface UpdateCardResponse {
  /** The billing address. */
  billing: CreateCardResponseBilling | null;
  /** When the card was canceled. */
  canceled_at: string | null;
  /** When the card was created. */
  created_at: string | null;
  /** Card expiration month. */
  expiration_month: string | null;
  /** Card expiration year. */
  expiration_year: string | null;
  /** Card ID, prefixed `icrd_`. */
  id: string;
  /** Last four digits of the card number. `null` for pending invitation cards. */
  last4: string | null;
  /** The spending limit configuration. */
  limit: UpdateCardResponseLimit | null;
  /** Card display name. */
  name: string | null;
  object: UpdateCardResponseObject;
  /** Sensitive card details. Present only on `GET /cards/:id` for active cards; `null` when the card is inactive or details cannot be retrieved. */
  secrets?: CreateCardResponseSecrets | null;
  /** Total spend in the last 30 days, in cents. */
  spent_last_month: number | null;
  /** The card status. `denied` means the issuer declined the cardholder, so the card will never be issued. */
  status: UpdateCardResponseStatus | null;
  /** The card type. */
  type: UpdateCardResponseType | null;
  /** Cardholder user ID, prefixed `user_`, when assigned. */
  user_id: string | null;
}
export const UpdateCardResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing: S.NullOr(CreateCardResponseBilling),
    canceled_at: S.NullOr(S.String),
    created_at: S.NullOr(S.String),
    expiration_month: S.NullOr(S.String),
    expiration_year: S.NullOr(S.String),
    id: S.String,
    last4: S.NullOr(S.String),
    limit: S.NullOr(UpdateCardResponseLimit),
    name: S.NullOr(S.String),
    object: UpdateCardResponseObject,
    secrets: S.optional(S.NullOr(CreateCardResponseSecrets)),
    spent_last_month: S.NullOr(S.Number),
    status: S.NullOr(UpdateCardResponseStatus),
    type: S.NullOr(UpdateCardResponseType),
    user_id: S.NullOr(S.String),
  }),
).annotate({
  identifier: "UpdateCardResponse",
}) as any as S.Schema<UpdateCardResponse>;

export type CreateCardError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Create Card Issue a virtual card, or apply for card issuing. */
export const createCard: API.OperationMethod<
  CreateCardRequest,
  CreateCardResponse,
  CreateCardError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateCardRequest,
  output: CreateCardResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListCardsError = BadRequest | Forbidden | NotFound | WhopOpError;
/** List Cards Lists the Whop cards of an account or user, including ones still being set up. Team members only see the cards assigned to them. */
export const listCards: API.OperationMethod<
  ListCardsRequest,
  ListCardsResponse,
  ListCardsError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListCardsRequest,
  output: ListCardsResponse,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListCardTransactionsError = BadRequest | Forbidden | WhopOpError;
/** List Card Transactions Lists an account's card transactions, newest first. Defaults to the account the credential belongs to. Covers every card the owner has ever had, including canceled cards and spend that predates a re-application, and team members only see transactions on the cards assigned to them. Pass `transaction_ids` to fetch specific transactions instead of paging for them. */
export const listCardTransactions: API.PaginatedOperationMethod<
  ListCardTransactionsRequest,
  ListCardTransactionsResponse,
  ListCardTransactionsError,
  WhopOpContext,
  CardTransaction
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListCardTransactionsRequest,
    output: ListCardTransactionsResponse,
    errors: [BadRequest, Forbidden],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveCardError = NotFound | WhopOpError;
/** Retrieve Card Retrieve a single card. */
export const retrieveCard: API.OperationMethod<
  RetrieveCardRequest,
  RetrieveCardResponse,
  RetrieveCardError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveCardRequest,
  output: RetrieveCardResponse,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveCardTransactionError = Forbidden | NotFound | WhopOpError;
/** Retrieve Card Transaction Fetches a single card transaction by its `citx_` identifier. The owner defaults to the account the credential belongs to. */
export const retrieveCardTransaction: API.OperationMethod<
  RetrieveCardTransactionRequest,
  CardTransaction,
  RetrieveCardTransactionError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveCardTransactionRequest,
  output: CardTransaction,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateCardError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Update Card Update, freeze, or cancel a card. Updating the card's name, billing address, or limits requires both `payout:account:update` and `company:balance:read`; a card's assigned holder may update their own card's pin and frozen state with any user token. */
export const updateCard: API.OperationMethod<
  UpdateCardRequest,
  UpdateCardResponse,
  UpdateCardError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateCardRequest,
  output: UpdateCardResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
