// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface ApproveEntryRequest {
  /** The unique identifier of the waitlist entry to approve. */
  id: string;
}
export const ApproveEntryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "POST", uri: "/entries/{id}/approve", code: 200 })),
).annotate({
  identifier: "ApproveEntryRequest",
}) as any as S.Schema<ApproveEntryRequest>;

export interface ApproveEntryResponse {
  /** The ID of the job. */
  job_id: string;
}
export const ApproveEntryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    job_id: S.String,
  }),
).annotate({
  identifier: "ApproveEntryResponse",
}) as any as S.Schema<ApproveEntryResponse>;

export interface DenyEntryRequest {
  /** The unique identifier of the waitlist entry to deny. */
  id: string;
}
export const DenyEntryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "POST", uri: "/entries/{id}/deny", code: 200 })),
).annotate({
  identifier: "DenyEntryRequest",
}) as any as S.Schema<DenyEntryRequest>;

/** The response from a custom field on checkout */
export interface EntryCustomFieldResponsesItem {
  /** The response a user gave to the specific question or field. */
  answer: string;
  /** The unique identifier for the custom field response. */
  id: string;
  /** The question asked by the custom field */
  question: string;
}
export const EntryCustomFieldResponsesItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    answer: S.String,
    id: S.String,
    question: S.String,
  }),
).annotate({
  identifier: "EntryCustomFieldResponsesItem",
}) as any as S.Schema<EntryCustomFieldResponsesItem>;

/** The list of responses collected from the user when submitting their waitlist entry. */
export type EntryCustomFieldResponsesList =
  Array<EntryCustomFieldResponsesItem>;
export const EntryCustomFieldResponsesList = /*@__PURE__*/ S.Array(
  EntryCustomFieldResponsesItem,
) as any as S.Schema<EntryCustomFieldResponsesList>;

/** The waitlisted plan that this entry is a signup for. */
export interface EntryPlan {
  /** The unique identifier for the plan. */
  id: string;
}
export const EntryPlan = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({ identifier: "EntryPlan" }) as any as S.Schema<EntryPlan>;

/** The product associated with this entry's waitlisted plan. Null if the plan is not tied to a product. */
export interface EntryProduct {
  /** The unique identifier for the product. */
  id: string;
  /** The display name of the product shown to customers on the product page and in search results. */
  title: string;
}
export const EntryProduct = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.String,
  }),
).annotate({ identifier: "EntryProduct" }) as any as S.Schema<EntryProduct>;

/** The status of an entry to a waitlist. */
export type EntryStatus = "drafted" | "pending" | "approved" | "denied" | "any";
export const EntryStatus = /*@__PURE__*/ S.String;

/** The user who submitted this waitlist entry. */
export interface EntryUser {
  /** The user's email address. Requires the member:email:read permission to access. Null if not authorized. */
  email: string | null;
  /** The unique identifier for the user. */
  id: string;
  /** The user's display name shown on their public profile. */
  name: string | null;
  /** The user's unique username shown on their public profile. */
  username: string;
}
export const EntryUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email: S.NullOr(S.String),
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({ identifier: "EntryUser" }) as any as S.Schema<EntryUser>;

/** An entry represents a user's signup for a waitlisted plan. */
export interface Entry {
  /** The datetime the entry was created. */
  created_at: string | null;
  /** The list of responses collected from the user when submitting their waitlist entry. */
  custom_field_responses: EntryCustomFieldResponsesList | null;
  /** The unique identifier for the entry. */
  id: string;
  /** The waitlisted plan that this entry is a signup for. */
  plan: EntryPlan | null;
  /** The product associated with this entry's waitlisted plan. Null if the plan is not tied to a product. */
  product: EntryProduct | null;
  /** The current status of the waitlist entry (e.g., drafted, pending, approved, denied). */
  status: EntryStatus;
  /** The user who submitted this waitlist entry. */
  user: EntryUser;
}
export const Entry = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    created_at: S.NullOr(S.String),
    custom_field_responses: S.NullOr(EntryCustomFieldResponsesList),
    id: S.String,
    plan: S.NullOr(EntryPlan),
    product: S.NullOr(EntryProduct),
    status: EntryStatus,
    user: EntryUser,
  }),
).annotate({ identifier: "Entry" }) as any as S.Schema<Entry>;

/** The direction of the sort. */
export type Direction = "asc" | "desc";
export const Direction = /*@__PURE__*/ S.String;

/** Which columns can be used to sort. */
export type EntriesSortableColumns = "id" | "created_at";
export const EntriesSortableColumns = /*@__PURE__*/ S.String;

/** Filter entries to only those for specific products. */
export type ListEntryRequestProductIdsList = Array<string>;
export const ListEntryRequestProductIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListEntryRequestProductIdsList>;

/** Filter entries to only those for specific plans. */
export type ListEntryRequestPlanIdsList = Array<string>;
export const ListEntryRequestPlanIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListEntryRequestPlanIdsList>;

/** Filter entries by their current status. */
export type ListEntryRequestStatusesList = Array<EntryStatus | (string & {})>;
export const ListEntryRequestStatusesList = /*@__PURE__*/ S.Array(
  EntryStatus,
) as any as S.Schema<ListEntryRequestStatusesList>;

export interface ListEntryRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  company_id: string;
  direction?: Direction | (string & {});
  order?: EntriesSortableColumns | (string & {});
  product_ids?: ListEntryRequestProductIdsList;
  plan_ids?: ListEntryRequestPlanIdsList;
  statuses?: ListEntryRequestStatusesList;
  created_before?: string;
  created_after?: string;
}
export const ListEntryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    company_id: S.String.pipe(T.Query()),
    direction: S.optional(Direction.pipe(T.Query())),
    order: S.optional(EntriesSortableColumns.pipe(T.Query())),
    product_ids: S.optional(ListEntryRequestProductIdsList.pipe(T.Query())),
    plan_ids: S.optional(ListEntryRequestPlanIdsList.pipe(T.Query())),
    statuses: S.optional(ListEntryRequestStatusesList.pipe(T.Query())),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/entries", code: 200 })),
).annotate({
  identifier: "ListEntryRequest",
}) as any as S.Schema<ListEntryRequest>;

/** The waitlisted plan that this entry is a signup for. */
export type EntryListItemPlan = EntryPlan;
export const EntryListItemPlan = EntryPlan;

/** The product associated with this entry's waitlisted plan. Null if the plan is not tied to a product. */
export type EntryListItemProduct = EntryProduct;
export const EntryListItemProduct = EntryProduct;

/** The user who submitted this waitlist entry. */
export type EntryListItemUser = EntryUser;
export const EntryListItemUser = EntryUser;

/** An entry represents a user's signup for a waitlisted plan. */
export interface EntryListItem {
  /** The datetime the entry was created. */
  created_at: string | null;
  /** The unique identifier for the entry. */
  id: string;
  /** The waitlisted plan that this entry is a signup for. */
  plan: EntryPlan | null;
  /** The product associated with this entry's waitlisted plan. Null if the plan is not tied to a product. */
  product: EntryProduct | null;
  /** The current status of the waitlist entry (e.g., drafted, pending, approved, denied). */
  status: EntryStatus;
  /** The user who submitted this waitlist entry. */
  user: EntryUser;
}
export const EntryListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    created_at: S.NullOr(S.String),
    id: S.String,
    plan: S.NullOr(EntryPlan),
    product: S.NullOr(EntryProduct),
    status: EntryStatus,
    user: EntryUser,
  }),
).annotate({ identifier: "EntryListItem" }) as any as S.Schema<EntryListItem>;

/** A list of nodes. */
export type ListEntryResponseDataList = Array<EntryListItem>;
export const ListEntryResponseDataList = /*@__PURE__*/ S.Array(
  EntryListItem,
) as any as S.Schema<ListEntryResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListEntryResponse {
  /** A list of nodes. */
  data: ListEntryResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListEntryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListEntryResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListEntryResponse",
}) as any as S.Schema<ListEntryResponse>;

export interface RetrieveEntryRequest {
  /** The unique identifier of the waitlist entry to retrieve. */
  id: string;
}
export const RetrieveEntryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/entries/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveEntryRequest",
}) as any as S.Schema<RetrieveEntryRequest>;

export type ApproveEntryError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Approve entry [Legacy API — https://docs.whop.com/api-reference] Approve a pending waitlist entry, triggering the checkout process to grant the user access to the plan. Required permissions: - `plan:waitlist:manage` */
export const approveEntry: API.OperationMethod<
  ApproveEntryRequest,
  ApproveEntryResponse,
  ApproveEntryError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ApproveEntryRequest,
  output: ApproveEntryResponse,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DenyEntryError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Deny entry [Legacy API — https://docs.whop.com/api-reference] Deny a pending waitlist entry, preventing the user from gaining access to the plan. Required permissions: - `plan:waitlist:manage` - `plan:basic:read` - `member:email:read` */
export const denyEntry: API.OperationMethod<
  DenyEntryRequest,
  Entry,
  DenyEntryError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DenyEntryRequest,
  output: Entry,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListEntryError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List entries [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of waitlist entries for a company, with optional filtering by product, plan, status, and creation date. Required permissions: - `plan:waitlist:read` - `member:email:read` */
export const listEntry: API.PaginatedOperationMethod<
  ListEntryRequest,
  ListEntryResponse,
  ListEntryError,
  WhopOpContext,
  EntryListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListEntryRequest,
    output: ListEntryResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveEntryError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve entry [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing waitlist entry. Required permissions: - `plan:waitlist:read` - `member:email:read` */
export const retrieveEntry: API.OperationMethod<
  RetrieveEntryRequest,
  Entry,
  RetrieveEntryError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveEntryRequest,
  output: Entry,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
