// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface CreatePaymentMethodDomainRequest {
  /** Account to register the domain for (`biz_` tag). Defaults to the caller's account. */
  account_id?: string;
  /** Hostname to register (e.g. `checkout.shinetime.example`). */
  hostname: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const CreatePaymentMethodDomainRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    hostname: S.String,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({ method: "POST", uri: "/payment_method_domains", code: 200 }),
  ),
).annotate({
  identifier: "CreatePaymentMethodDomainRequest",
}) as any as S.Schema<CreatePaymentMethodDomainRequest>;

/** Wallet provider the domain is registered with. */
export type PaymentMethodDomainProvider = "apple";
export const PaymentMethodDomainProvider = /*@__PURE__*/ S.String;

/** Verification status. `pending` means the provider could not fetch the domain-association file yet; only `verified` domains show wallet payment methods at checkout. */
export type PaymentMethodDomainStatus = "pending" | "verified";
export const PaymentMethodDomainStatus = /*@__PURE__*/ S.String;

export interface PaymentMethodDomain {
  /** ID of the account the domain is registered for, prefixed `biz_`. */
  account_id: string | null;
  /** When the domain was created, as an ISO 8601 timestamp. */
  created_at: string;
  /** Hostname the checkout is served from (e.g. `checkout.example.com`). */
  hostname: string;
  /** Payment method domain ID, prefixed `pmd_`. */
  id: string;
  /** Wallet provider the domain is registered with. */
  provider: PaymentMethodDomainProvider;
  /** Verification status. `pending` means the provider could not fetch the domain-association file yet; only `verified` domains show wallet payment methods at checkout. */
  status: PaymentMethodDomainStatus;
  /** When the domain was last updated, as an ISO 8601 timestamp. */
  updated_at: string;
}
export const PaymentMethodDomain = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.NullOr(S.String),
    created_at: S.String,
    hostname: S.String,
    id: S.String,
    provider: PaymentMethodDomainProvider,
    status: PaymentMethodDomainStatus,
    updated_at: S.String,
  }),
).annotate({
  identifier: "PaymentMethodDomain",
}) as any as S.Schema<PaymentMethodDomain>;

export interface DeletePaymentMethodDomainRequest {
  /** The unique identifier of the payment method domain, prefixed `pmd_`. */
  id: string;
}
export const DeletePaymentMethodDomainRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/payment_method_domains/{id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeletePaymentMethodDomainRequest",
}) as any as S.Schema<DeletePaymentMethodDomainRequest>;

export interface DeletePaymentMethodDomainResponse {
  deleted: boolean;
  id: string;
}
export const DeletePaymentMethodDomainResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deleted: S.Boolean,
    id: S.String,
  }),
).annotate({
  identifier: "DeletePaymentMethodDomainResponse",
}) as any as S.Schema<DeletePaymentMethodDomainResponse>;

export type ListPaymentMethodDomainsRequestStatus = "pending" | "verified";
export const ListPaymentMethodDomainsRequestStatus = /*@__PURE__*/ S.String;

export type ListPaymentMethodDomainsRequestProvider = "apple";
export const ListPaymentMethodDomainsRequestProvider = /*@__PURE__*/ S.String;

export type ListPaymentMethodDomainsRequestOrder = "created_at";
export const ListPaymentMethodDomainsRequestOrder = /*@__PURE__*/ S.String;

export type ListPaymentMethodDomainsRequestDirection = "asc" | "desc";
export const ListPaymentMethodDomainsRequestDirection = /*@__PURE__*/ S.String;

export interface ListPaymentMethodDomainsRequest {
  /** Only domains registered for this account (`biz_` tag). Defaults to the caller's account plus its connected accounts. */
  account_id?: string;
  /** Only the domain with this exact hostname. */
  hostname?: string;
  /** Only domains with this verification status. */
  status?: ListPaymentMethodDomainsRequestStatus | (string & {});
  /** Only domains registered with this wallet provider. */
  provider?: ListPaymentMethodDomainsRequestProvider | (string & {});
  /** Only domains created before this ISO 8601 timestamp. */
  created_before?: string;
  /** Only domains created after this ISO 8601 timestamp. */
  created_after?: string;
  /** Sort field. */
  order?: ListPaymentMethodDomainsRequestOrder | (string & {});
  /** Sort direction. */
  direction?: ListPaymentMethodDomainsRequestDirection | (string & {});
  /** Number of domains to return from the start of the window. */
  first?: number;
  /** Cursor to paginate forwards from. */
  after?: string;
  /** Number of domains to return from the end of the window. */
  last?: number;
  /** Cursor to paginate backwards from. */
  before?: string;
}
export const ListPaymentMethodDomainsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String.pipe(T.Query())),
    hostname: S.optional(S.String.pipe(T.Query())),
    status: S.optional(ListPaymentMethodDomainsRequestStatus.pipe(T.Query())),
    provider: S.optional(
      ListPaymentMethodDomainsRequestProvider.pipe(T.Query()),
    ),
    created_before: S.optional(S.String.pipe(T.Query())),
    created_after: S.optional(S.String.pipe(T.Query())),
    order: S.optional(ListPaymentMethodDomainsRequestOrder.pipe(T.Query())),
    direction: S.optional(
      ListPaymentMethodDomainsRequestDirection.pipe(T.Query()),
    ),
    first: S.optional(S.Number.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/payment_method_domains", code: 200 })),
).annotate({
  identifier: "ListPaymentMethodDomainsRequest",
}) as any as S.Schema<ListPaymentMethodDomainsRequest>;

export type ListPaymentMethodDomainsResponseDataList =
  Array<PaymentMethodDomain>;
export const ListPaymentMethodDomainsResponseDataList = /*@__PURE__*/ S.Array(
  PaymentMethodDomain,
) as any as S.Schema<ListPaymentMethodDomainsResponseDataList>;

export interface ListPaymentMethodDomainsResponsePageInfo {
  end_cursor: string | null;
  has_next_page: boolean;
  has_previous_page: boolean;
  start_cursor: string | null;
}
export const ListPaymentMethodDomainsResponsePageInfo = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      end_cursor: S.NullOr(S.String),
      has_next_page: S.Boolean,
      has_previous_page: S.Boolean,
      start_cursor: S.NullOr(S.String),
    }),
).annotate({
  identifier: "ListPaymentMethodDomainsResponsePageInfo",
}) as any as S.Schema<ListPaymentMethodDomainsResponsePageInfo>;

export interface ListPaymentMethodDomainsResponse {
  data: ListPaymentMethodDomainsResponseDataList;
  page_info: ListPaymentMethodDomainsResponsePageInfo;
}
export const ListPaymentMethodDomainsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListPaymentMethodDomainsResponseDataList,
    page_info: ListPaymentMethodDomainsResponsePageInfo,
  }),
).annotate({
  identifier: "ListPaymentMethodDomainsResponse",
}) as any as S.Schema<ListPaymentMethodDomainsResponse>;

export interface RetrievePaymentMethodDomainRequest {
  /** The unique identifier of the payment method domain, prefixed `pmd_`. */
  id: string;
}
export const RetrievePaymentMethodDomainRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "GET", uri: "/payment_method_domains/{id}", code: 200 }),
  ),
).annotate({
  identifier: "RetrievePaymentMethodDomainRequest",
}) as any as S.Schema<RetrievePaymentMethodDomainRequest>;

export interface VerifyPaymentMethodDomainRequest {
  /** The unique identifier of the payment method domain, prefixed `pmd_`. */
  id: string;
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const VerifyPaymentMethodDomainRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/payment_method_domains/{id}/verify",
      code: 200,
    }),
  ),
).annotate({
  identifier: "VerifyPaymentMethodDomainRequest",
}) as any as S.Schema<VerifyPaymentMethodDomainRequest>;

export type CreatePaymentMethodDomainError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Create Payment Method Domain Registers a hostname with the wallet provider and attempts verification inline. Returns `verified` when the provider fetched the domain-association file (for Apple Pay, `/.well-known/apple-developer-merchantid-domain-association`), or `pending` when it could not — host the file, then retry with the verify endpoint. */
export const createPaymentMethodDomain: API.OperationMethod<
  CreatePaymentMethodDomainRequest,
  PaymentMethodDomain,
  CreatePaymentMethodDomainError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreatePaymentMethodDomainRequest,
  output: PaymentMethodDomain,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeletePaymentMethodDomainError = Forbidden | NotFound | WhopOpError;
/** Delete Payment Method Domain Unregisters a payment method domain so its wallet payment methods stop rendering there. */
export const deletePaymentMethodDomain: API.OperationMethod<
  DeletePaymentMethodDomainRequest,
  DeletePaymentMethodDomainResponse,
  DeletePaymentMethodDomainError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeletePaymentMethodDomainRequest,
  output: DeletePaymentMethodDomainResponse,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListPaymentMethodDomainsError =
  | BadRequest
  | Forbidden
  | NotFound
  | WhopOpError;
/** List Payment Method Domains Lists payment method domains. Without `account_id`, returns the caller's own domains and those of every connected account. */
export const listPaymentMethodDomains: API.PaginatedOperationMethod<
  ListPaymentMethodDomainsRequest,
  ListPaymentMethodDomainsResponse,
  ListPaymentMethodDomainsError,
  WhopOpContext,
  PaymentMethodDomain
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListPaymentMethodDomainsRequest,
    output: ListPaymentMethodDomainsResponse,
    errors: [BadRequest, Forbidden, NotFound],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrievePaymentMethodDomainError =
  | Forbidden
  | NotFound
  | WhopOpError;
/** Retrieve Payment Method Domain Retrieves a payment method domain to check its verification status. */
export const retrievePaymentMethodDomain: API.OperationMethod<
  RetrievePaymentMethodDomainRequest,
  PaymentMethodDomain,
  RetrievePaymentMethodDomainError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrievePaymentMethodDomainRequest,
  output: PaymentMethodDomain,
  errors: [Forbidden, NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type VerifyPaymentMethodDomainError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | WhopOpError;
/** Verify Payment Method Domain Re-attempts provider verification of a pending domain once the association file is hosted. Fails with a `bad_request` explaining what to fix; verifying an already `verified` domain is a no-op. */
export const verifyPaymentMethodDomain: API.OperationMethod<
  VerifyPaymentMethodDomainRequest,
  PaymentMethodDomain,
  VerifyPaymentMethodDomainError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: VerifyPaymentMethodDomainRequest,
  output: PaymentMethodDomain,
  errors: [BadRequest, Forbidden, NotFound, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
