// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** Input for an attachment */
export interface CreateMessageRequestAttachmentsItem {
  /** The ID of an existing file object. */
  id: string;
}
export const CreateMessageRequestAttachmentsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "CreateMessageRequestAttachmentsItem",
}) as any as S.Schema<CreateMessageRequestAttachmentsItem>;

/** A list of file attachments to include with the message, such as images or videos. */
export type CreateMessageRequestAttachmentsList =
  Array<CreateMessageRequestAttachmentsItem>;
export const CreateMessageRequestAttachmentsList = /*@__PURE__*/ S.Array(
  CreateMessageRequestAttachmentsItem,
) as any as S.Schema<CreateMessageRequestAttachmentsList>;

/** Input type for a single poll option */
export interface CreateMessageRequestPollOptionsItem {
  /** Sequential ID for the poll option (starting from '1') */
  id: string;
  /** The text of the poll option */
  text: string;
}
export const CreateMessageRequestPollOptionsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    text: S.String,
  }),
).annotate({
  identifier: "CreateMessageRequestPollOptionsItem",
}) as any as S.Schema<CreateMessageRequestPollOptionsItem>;

/** The options for the poll. Must have sequential IDs starting from 1 */
export type CreateMessageRequestPollOptionsList =
  Array<CreateMessageRequestPollOptionsItem>;
export const CreateMessageRequestPollOptionsList = /*@__PURE__*/ S.Array(
  CreateMessageRequestPollOptionsItem,
) as any as S.Schema<CreateMessageRequestPollOptionsList>;

/** A poll to attach to this message, allowing recipients to vote on options. */
export interface CreateMessageRequestPoll {
  /** The options for the poll. Must have sequential IDs starting from 1 */
  options: CreateMessageRequestPollOptionsList;
}
export const CreateMessageRequestPoll = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    options: CreateMessageRequestPollOptionsList,
  }),
).annotate({
  identifier: "CreateMessageRequestPoll",
}) as any as S.Schema<CreateMessageRequestPoll>;

export interface CreateMessageRequest {
  /** A list of file attachments to include with the message, such as images or videos. */
  attachments?: CreateMessageRequestAttachmentsList | null;
  /** Automatically detect URLs in the message and generate link previews. */
  auto_detect_links?: boolean | null;
  /** The unique identifier of the channel or experience to send the message in. For example, 'exp_xxxxx' or 'feed_xxxxx'. */
  channel_id: string;
  /** The body of the message in Markdown format. For example, 'Hello **world**'. */
  content: string;
  /** A poll to attach to this message, allowing recipients to vote on options. */
  poll?: CreateMessageRequestPoll | null;
  /** The unique identifier of the message this is replying to, creating a threaded reply. */
  replying_to_message_id?: string | null;
}
export const CreateMessageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    attachments: S.optional(S.NullOr(CreateMessageRequestAttachmentsList)),
    auto_detect_links: S.optional(S.NullOr(S.Boolean)),
    channel_id: S.String,
    content: S.String,
    poll: S.optional(S.NullOr(CreateMessageRequestPoll)),
    replying_to_message_id: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "POST", uri: "/messages", code: 200 })),
).annotate({
  identifier: "CreateMessageRequest",
}) as any as S.Schema<CreateMessageRequest>;

/** A list of user IDs that are explicitly mentioned in this message. */
export type MessageMentionsList = Array<string>;
export const MessageMentionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<MessageMentionsList>;

/** The types of post */
export type DmsPostTypes = "regular" | "system" | "automated";
export const DmsPostTypes = /*@__PURE__*/ S.String;

/** Represents a single poll option */
export interface MessagePollOptionsItem {
  /** The unique identifier for the poll option. */
  id: string;
  /** The text of the poll option */
  text: string;
}
export const MessagePollOptionsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    text: S.String,
  }),
).annotate({
  identifier: "MessagePollOptionsItem",
}) as any as S.Schema<MessagePollOptionsItem>;

/** The options for the poll */
export type MessagePollOptionsList = Array<MessagePollOptionsItem>;
export const MessagePollOptionsList = /*@__PURE__*/ S.Array(
  MessagePollOptionsItem,
) as any as S.Schema<MessagePollOptionsList>;

/** A poll attached to this message. Null if the message does not contain a poll. */
export interface MessagePoll {
  /** The options for the poll */
  options: MessagePollOptionsList | null;
}
export const MessagePoll = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    options: S.NullOr(MessagePollOptionsList),
  }),
).annotate({ identifier: "MessagePoll" }) as any as S.Schema<MessagePoll>;

/** Represents a reaction count for a feed post */
export interface MessagePollVotesItem {
  /** The number of users who reacted */
  count: number;
  /** The reaction that was used */
  option_id: string | null;
}
export const MessagePollVotesItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    count: S.Number,
    option_id: S.NullOr(S.String),
  }),
).annotate({
  identifier: "MessagePollVotesItem",
}) as any as S.Schema<MessagePollVotesItem>;

/** Aggregated reaction counts on this message, filtered to a specific reaction type. */
export type MessagePollVotesList = Array<MessagePollVotesItem>;
export const MessagePollVotesList = /*@__PURE__*/ S.Array(
  MessagePollVotesItem,
) as any as S.Schema<MessagePollVotesList>;

/** Represents a reaction count for a feed post */
export interface MessageReactionCountsItem {
  /** The number of users who reacted */
  count: number;
  /** The emoji that was used in shortcode format (:heart:) */
  emoji: string | null;
}
export const MessageReactionCountsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    count: S.Number,
    emoji: S.NullOr(S.String),
  }),
).annotate({
  identifier: "MessageReactionCountsItem",
}) as any as S.Schema<MessageReactionCountsItem>;

/** Aggregated reaction counts on this message, filtered to a specific reaction type. */
export type MessageReactionCountsList = Array<MessageReactionCountsItem>;
export const MessageReactionCountsList = /*@__PURE__*/ S.Array(
  MessageReactionCountsItem,
) as any as S.Schema<MessageReactionCountsList>;

/** The user who authored this message. */
export interface MessageUser {
  /** The unique identifier for the user. */
  id: string;
  /** The user's display name shown on their public profile. */
  name: string | null;
  /** The user's unique username shown on their public profile. */
  username: string;
}
export const MessageUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    username: S.String,
  }),
).annotate({ identifier: "MessageUser" }) as any as S.Schema<MessageUser>;

/** A message sent within an experience chat, direct message, or group chat. */
export interface Message {
  /** The message content formatted as Markdown. Null if the message has no text content. */
  content: string | null;
  /** The timestamp when this message was originally created. */
  created_at: string;
  /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
  id: string;
  /** Whether the message content has been edited after it was originally sent. */
  is_edited: boolean;
  /** Whether this message is pinned to the top of the channel for easy access. */
  is_pinned: boolean;
  /** A list of user IDs that are explicitly mentioned in this message. */
  mentions: MessageMentionsList;
  /** Whether the message includes an @everyone mention that notifies all channel members. */
  mentions_everyone: boolean;
  /** The classification of this message: regular, system, or automated. */
  message_type: DmsPostTypes;
  /** A poll attached to this message. Null if the message does not contain a poll. */
  poll: MessagePoll | null;
  /** Aggregated reaction counts on this message, filtered to a specific reaction type. */
  poll_votes: MessagePollVotesList;
  /** Aggregated reaction counts on this message, filtered to a specific reaction type. */
  reaction_counts: MessageReactionCountsList;
  /** The unique identifier of the message this post is replying to. Null if this is not a reply. */
  replying_to_message_id: string | null;
  /** The timestamp when this message was last modified. */
  updated_at: string;
  /** The user who authored this message. */
  user: MessageUser;
  /** The number of unique views this message has received. Null if view tracking is not enabled for this channel. */
  view_count: number | null;
}
export const Message = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    content: S.NullOr(S.String),
    created_at: S.String,
    id: S.String,
    is_edited: S.Boolean,
    is_pinned: S.Boolean,
    mentions: MessageMentionsList,
    mentions_everyone: S.Boolean,
    message_type: DmsPostTypes,
    poll: S.NullOr(MessagePoll),
    poll_votes: MessagePollVotesList,
    reaction_counts: MessageReactionCountsList,
    replying_to_message_id: S.NullOr(S.String),
    updated_at: S.String,
    user: MessageUser,
    view_count: S.NullOr(S.Number),
  }),
).annotate({ identifier: "Message" }) as any as S.Schema<Message>;

export interface DeleteMessageRequest {
  /** The unique identifier of the message to delete. */
  id: string;
}
export const DeleteMessageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/messages/{id}", code: 200 })),
).annotate({
  identifier: "DeleteMessageRequest",
}) as any as S.Schema<DeleteMessageRequest>;

export type DeleteMessageResponse = boolean;
export const DeleteMessageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Boolean.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "DeleteMessageResponse",
}) as any as S.Schema<DeleteMessageResponse>;

/** The direction of the sort. */
export type Direction = "asc" | "desc";
export const Direction = /*@__PURE__*/ S.String;

export interface ListMessageRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  channel_id: string;
  direction?: Direction | (string & {});
}
export const ListMessageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    channel_id: S.String.pipe(T.Query()),
    direction: S.optional(Direction.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/messages", code: 200 })),
).annotate({
  identifier: "ListMessageRequest",
}) as any as S.Schema<ListMessageRequest>;

/** A list of user IDs that are explicitly mentioned in this message. */
export type MessageListItemMentionsList = Array<string>;
export const MessageListItemMentionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<MessageListItemMentionsList>;

/** Represents a single poll option */
export type MessageListItemPollOptionsItem = MessagePollOptionsItem;
export const MessageListItemPollOptionsItem = MessagePollOptionsItem;

/** The options for the poll */
export type MessageListItemPollOptionsList = Array<MessagePollOptionsItem>;
export const MessageListItemPollOptionsList = /*@__PURE__*/ S.Array(
  MessagePollOptionsItem,
) as any as S.Schema<MessageListItemPollOptionsList>;

/** A poll attached to this message. Null if the message does not contain a poll. */
export interface MessageListItemPoll {
  /** The options for the poll */
  options: MessageListItemPollOptionsList | null;
}
export const MessageListItemPoll = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    options: S.NullOr(MessageListItemPollOptionsList),
  }),
).annotate({
  identifier: "MessageListItemPoll",
}) as any as S.Schema<MessageListItemPoll>;

/** Represents a reaction count for a feed post */
export type MessageListItemPollVotesItem = MessagePollVotesItem;
export const MessageListItemPollVotesItem = MessagePollVotesItem;

/** Aggregated reaction counts on this message, filtered to a specific reaction type. */
export type MessageListItemPollVotesList = Array<MessagePollVotesItem>;
export const MessageListItemPollVotesList = /*@__PURE__*/ S.Array(
  MessagePollVotesItem,
) as any as S.Schema<MessageListItemPollVotesList>;

/** Represents a reaction count for a feed post */
export type MessageListItemReactionCountsItem = MessageReactionCountsItem;
export const MessageListItemReactionCountsItem = MessageReactionCountsItem;

/** Aggregated reaction counts on this message, filtered to a specific reaction type. */
export type MessageListItemReactionCountsList =
  Array<MessageReactionCountsItem>;
export const MessageListItemReactionCountsList = /*@__PURE__*/ S.Array(
  MessageReactionCountsItem,
) as any as S.Schema<MessageListItemReactionCountsList>;

/** The user who authored this message. */
export type MessageListItemUser = MessageUser;
export const MessageListItemUser = MessageUser;

/** A message sent within an experience chat, direct message, or group chat. */
export interface MessageListItem {
  /** The message content formatted as Markdown. Null if the message has no text content. */
  content: string | null;
  /** The timestamp when this message was originally created. */
  created_at: string;
  /** Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID. */
  id: string;
  /** Whether the message content has been edited after it was originally sent. */
  is_edited: boolean;
  /** Whether this message is pinned to the top of the channel for easy access. */
  is_pinned: boolean;
  /** A list of user IDs that are explicitly mentioned in this message. */
  mentions: MessageListItemMentionsList;
  /** Whether the message includes an @everyone mention that notifies all channel members. */
  mentions_everyone: boolean;
  /** The classification of this message: regular, system, or automated. */
  message_type: DmsPostTypes;
  /** A poll attached to this message. Null if the message does not contain a poll. */
  poll: MessageListItemPoll | null;
  /** Aggregated reaction counts on this message, filtered to a specific reaction type. */
  poll_votes: MessageListItemPollVotesList;
  /** Aggregated reaction counts on this message, filtered to a specific reaction type. */
  reaction_counts: MessageListItemReactionCountsList;
  /** The unique identifier of the message this post is replying to. Null if this is not a reply. */
  replying_to_message_id: string | null;
  /** The timestamp when this message was last modified. */
  updated_at: string;
  /** The user who authored this message. */
  user: MessageUser;
  /** The number of unique views this message has received. Null if view tracking is not enabled for this channel. */
  view_count: number | null;
}
export const MessageListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    content: S.NullOr(S.String),
    created_at: S.String,
    id: S.String,
    is_edited: S.Boolean,
    is_pinned: S.Boolean,
    mentions: MessageListItemMentionsList,
    mentions_everyone: S.Boolean,
    message_type: DmsPostTypes,
    poll: S.NullOr(MessageListItemPoll),
    poll_votes: MessageListItemPollVotesList,
    reaction_counts: MessageListItemReactionCountsList,
    replying_to_message_id: S.NullOr(S.String),
    updated_at: S.String,
    user: MessageUser,
    view_count: S.NullOr(S.Number),
  }),
).annotate({
  identifier: "MessageListItem",
}) as any as S.Schema<MessageListItem>;

/** A list of nodes. */
export type ListMessageResponseDataList = Array<MessageListItem>;
export const ListMessageResponseDataList = /*@__PURE__*/ S.Array(
  MessageListItem,
) as any as S.Schema<ListMessageResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListMessageResponse {
  /** A list of nodes. */
  data: ListMessageResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListMessageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListMessageResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListMessageResponse",
}) as any as S.Schema<ListMessageResponse>;

export interface RetrieveMessageRequest {
  /** The unique identifier of the message to retrieve. */
  id: string;
}
export const RetrieveMessageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/messages/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveMessageRequest",
}) as any as S.Schema<RetrieveMessageRequest>;

/** Input for an attachment */
export type UpdateMessageRequestAttachmentsItem =
  CreateMessageRequestAttachmentsItem;
export const UpdateMessageRequestAttachmentsItem =
  CreateMessageRequestAttachmentsItem;

/** A replacement list of file attachments for this message, such as images or videos. */
export type UpdateMessageRequestAttachmentsList =
  Array<CreateMessageRequestAttachmentsItem>;
export const UpdateMessageRequestAttachmentsList = /*@__PURE__*/ S.Array(
  CreateMessageRequestAttachmentsItem,
) as any as S.Schema<UpdateMessageRequestAttachmentsList>;

export interface UpdateMessageRequest {
  /** The unique identifier of the message to update. */
  id: string;
  /** A replacement list of file attachments for this message, such as images or videos. */
  attachments?: UpdateMessageRequestAttachmentsList | null;
  /** The updated body of the message in Markdown format. For example, 'Hello **world**'. */
  content?: string | null;
  /** Whether this message should be pinned to the top of the channel. */
  is_pinned?: boolean | null;
}
export const UpdateMessageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    attachments: S.optional(S.NullOr(UpdateMessageRequestAttachmentsList)),
    content: S.optional(S.NullOr(S.String)),
    is_pinned: S.optional(S.NullOr(S.Boolean)),
  }).pipe(T.Http({ method: "PATCH", uri: "/messages/{id}", code: 200 })),
).annotate({
  identifier: "UpdateMessageRequest",
}) as any as S.Schema<UpdateMessageRequest>;

export type CreateMessageError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create message [Legacy API — https://docs.whop.com/api-reference] Send a new message in an experience chat, DM, or group chat channel. Supports text content, attachments, polls, and replies. Required permissions (one of): - `chat:message:create` - `dms:message:manage` - `livestream:chat:write` - `support_chat:message:create` */
export const createMessage: API.OperationMethod<
  CreateMessageRequest,
  Message,
  CreateMessageError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateMessageRequest,
  output: Message,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteMessageError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Delete message [Legacy API — https://docs.whop.com/api-reference] Permanently delete a message from an experience chat, DM, or group chat channel. Only the message author or a channel admin can delete a message. Required permissions (one of): - `chat:message:create` and `chat:read` - `dms:message:manage` and `dms:read` - `livestream:chat:write` and `livestream:chat:read` - `support_chat:message:create` and `support_chat:read` */
export const deleteMessage: API.OperationMethod<
  DeleteMessageRequest,
  DeleteMessageResponse,
  DeleteMessageError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteMessageRequest,
  output: DeleteMessageResponse,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListMessageError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List messages [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of messages within a specific experience chat, DM, or group chat channel, sorted by creation time. Required permissions (one of): - `chat:read` - `dms:read` - `support_chat:read` */
export const listMessage: API.PaginatedOperationMethod<
  ListMessageRequest,
  ListMessageResponse,
  ListMessageError,
  WhopOpContext,
  MessageListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListMessageRequest,
    output: ListMessageResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveMessageError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve message [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing message. Required permissions (one of): - `chat:read` - `dms:read` - `livestream:chat:read` - `support_chat:read` */
export const retrieveMessage: API.OperationMethod<
  RetrieveMessageRequest,
  Message,
  RetrieveMessageError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveMessageRequest,
  output: Message,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateMessageError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update message [Legacy API — https://docs.whop.com/api-reference] Edit the content, attachments, or pinned status of an existing message in an experience chat, DM, or group chat channel. Required permissions (one of): - `chat:message:create` - `dms:message:manage` - `livestream:chat:write` - `support_chat:message:create` */
export const updateMessage: API.OperationMethod<
  UpdateMessageRequest,
  Message,
  UpdateMessageError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateMessageRequest,
  output: Message,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
