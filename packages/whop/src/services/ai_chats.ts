// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import { paginateRelay } from "../pagination.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** Input for an attachment */
export interface CreateAiChatRequestMessageAttachmentsItem {
  /** The ID of an existing file object. */
  id: string;
}
export const CreateAiChatRequestMessageAttachmentsItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      id: S.String,
    }),
  ).annotate({
    identifier: "CreateAiChatRequestMessageAttachmentsItem",
  }) as any as S.Schema<CreateAiChatRequestMessageAttachmentsItem>;

/** A list of previously uploaded file attachments to include with the first message. */
export type CreateAiChatRequestMessageAttachmentsList =
  Array<CreateAiChatRequestMessageAttachmentsItem>;
export const CreateAiChatRequestMessageAttachmentsList = /*@__PURE__*/ S.Array(
  CreateAiChatRequestMessageAttachmentsItem,
) as any as S.Schema<CreateAiChatRequestMessageAttachmentsList>;

/** The source of an AI chat message */
export type AiChatMessageSourceTypes = "manual" | "suggestion" | "link";
export const AiChatMessageSourceTypes = /*@__PURE__*/ S.String;

export interface CreateAiChatRequest {
  /** The unique identifier of the company to set as context for the AI chat (e.g., "biz_XXXXX"). */
  current_company_id?: string | null;
  /** A list of previously uploaded file attachments to include with the first message. */
  message_attachments?: CreateAiChatRequestMessageAttachmentsList | null;
  /** The source of the message. */
  message_source?: AiChatMessageSourceTypes | (string & {}) | null;
  /** The text content of the first message to send to the AI agent. */
  message_text: string;
  /** The type of suggestion prompt that was clicked, when message_source is 'suggestion'. */
  suggestion_type?: string | null;
  /** An optional display title for the AI chat thread (e.g., "Help with billing"). */
  title?: string | null;
}
export const CreateAiChatRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    current_company_id: S.optional(S.NullOr(S.String)),
    message_attachments: S.optional(
      S.NullOr(CreateAiChatRequestMessageAttachmentsList),
    ),
    message_source: S.optional(S.NullOr(AiChatMessageSourceTypes)),
    message_text: S.String,
    suggestion_type: S.optional(S.NullOr(S.String)),
    title: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "POST", uri: "/ai_chats", code: 200 })),
).annotate({
  identifier: "CreateAiChatRequest",
}) as any as S.Schema<CreateAiChatRequest>;

/** The notification preference for an AI chat */
export type AiChatNotificationPreferences = "all" | "none";
export const AiChatNotificationPreferences = /*@__PURE__*/ S.String;

/** The user who owns this AI chat conversation. */
export interface AiChatUser {
  /** The unique identifier for the user. */
  id: string;
}
export const AiChatUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({ identifier: "AiChatUser" }) as any as S.Schema<AiChatUser>;

/** An AI-powered chat conversation belonging to a user, with optional scheduled automation. */
export interface AiChat {
  /** The total number of tokens consumed across all messages in this conversation. */
  blended_token_usage: string;
  /** The datetime the ai chat was created. */
  created_at: string;
  /** The unique identifier for the ai chat. */
  id: string;
  /** The timestamp of the most recent message in this conversation. Null if no messages have been sent yet. */
  last_message_at: string | null;
  /** The total number of messages exchanged in this conversation. */
  message_count: number;
  /** The notification preference for this AI chat. `all` delivers AI chat notifications and badges, while `none` mutes them. */
  notification_preference: AiChatNotificationPreferences;
  /** A short descriptive title for this AI chat conversation. Null if no title has been set. */
  title: string | null;
  /** The datetime the ai chat was last updated. */
  updated_at: string;
  /** The user who owns this AI chat conversation. */
  user: AiChatUser;
}
export const AiChat = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    blended_token_usage: S.String,
    created_at: S.String,
    id: S.String,
    last_message_at: S.NullOr(S.String),
    message_count: S.Number,
    notification_preference: AiChatNotificationPreferences,
    title: S.NullOr(S.String),
    updated_at: S.String,
    user: AiChatUser,
  }),
).annotate({ identifier: "AiChat" }) as any as S.Schema<AiChat>;

export interface DeleteAiChatRequest {
  /** The unique identifier of the AI chat to delete (e.g., "ai_chat_XXXXX"). */
  id: string;
}
export const DeleteAiChatRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/ai_chats/{id}", code: 200 })),
).annotate({
  identifier: "DeleteAiChatRequest",
}) as any as S.Schema<DeleteAiChatRequest>;

export type DeleteAiChatResponse = boolean;
export const DeleteAiChatResponse = /*@__PURE__*/ S.suspend(() =>
  S.Boolean.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "DeleteAiChatResponse",
}) as any as S.Schema<DeleteAiChatResponse>;

export interface ListAiChatRequest {
  after?: string;
  before?: string;
  first?: number;
  last?: number;
  only_active_crons?: boolean;
}
export const ListAiChatRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    first: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.Number.pipe(T.Query())),
    only_active_crons: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/ai_chats", code: 200 })),
).annotate({
  identifier: "ListAiChatRequest",
}) as any as S.Schema<ListAiChatRequest>;

/** The user who owns this AI chat conversation. */
export type AiChatListItemUser = AiChatUser;
export const AiChatListItemUser = AiChatUser;

/** An AI-powered chat conversation belonging to a user, with optional scheduled automation. */
export interface AiChatListItem {
  /** The total number of tokens consumed across all messages in this conversation. */
  blended_token_usage: string;
  /** The datetime the ai chat was created. */
  created_at: string;
  /** The unique identifier for the ai chat. */
  id: string;
  /** The timestamp of the most recent message in this conversation. Null if no messages have been sent yet. */
  last_message_at: string | null;
  /** The total number of messages exchanged in this conversation. */
  message_count: number;
  /** The notification preference for this AI chat. `all` delivers AI chat notifications and badges, while `none` mutes them. */
  notification_preference: AiChatNotificationPreferences;
  /** A short descriptive title for this AI chat conversation. Null if no title has been set. */
  title: string | null;
  /** The datetime the ai chat was last updated. */
  updated_at: string;
  /** The user who owns this AI chat conversation. */
  user: AiChatUser;
}
export const AiChatListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    blended_token_usage: S.String,
    created_at: S.String,
    id: S.String,
    last_message_at: S.NullOr(S.String),
    message_count: S.Number,
    notification_preference: AiChatNotificationPreferences,
    title: S.NullOr(S.String),
    updated_at: S.String,
    user: AiChatUser,
  }),
).annotate({ identifier: "AiChatListItem" }) as any as S.Schema<AiChatListItem>;

/** A list of nodes. */
export type ListAiChatResponseDataList = Array<AiChatListItem>;
export const ListAiChatResponseDataList = /*@__PURE__*/ S.Array(
  AiChatListItem,
) as any as S.Schema<ListAiChatResponseDataList>;

/** Information about pagination in a connection. */
export interface PageInfo {
  /** When paginating forwards, the cursor to continue. */
  end_cursor: string | null;
  /** When paginating forwards, are there more items? */
  has_next_page: boolean;
  /** When paginating backwards, are there more items? */
  has_previous_page: boolean;
  /** When paginating backwards, the cursor to continue. */
  start_cursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    end_cursor: S.NullOr(S.String),
    has_next_page: S.Boolean,
    has_previous_page: S.Boolean,
    start_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListAiChatResponse {
  /** A list of nodes. */
  data: ListAiChatResponseDataList;
  /** Information to aid in pagination. */
  page_info: PageInfo;
}
export const ListAiChatResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: ListAiChatResponseDataList,
    page_info: PageInfo,
  }),
).annotate({
  identifier: "ListAiChatResponse",
}) as any as S.Schema<ListAiChatResponse>;

export interface RetrieveAiChatRequest {
  /** The unique identifier of the AI chat to retrieve. */
  id: string;
}
export const RetrieveAiChatRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/ai_chats/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveAiChatRequest",
}) as any as S.Schema<RetrieveAiChatRequest>;

export interface UpdateAiChatRequest {
  /** The unique identifier of the AI chat to update (e.g., "ai_chat_XXXXX"). */
  id: string;
  /** The unique identifier of the company to set as context for the AI chat (e.g., "biz_XXXXX"). */
  current_company_id?: string | null;
  /** The notification preference for the AI chat. */
  notification_preference?:
    | AiChatNotificationPreferences
    | (string & {})
    | null;
  /** The new display title for the AI chat thread (e.g., "Help with billing"). */
  title?: string | null;
}
export const UpdateAiChatRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    current_company_id: S.optional(S.NullOr(S.String)),
    notification_preference: S.optional(
      S.NullOr(AiChatNotificationPreferences),
    ),
    title: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "PATCH", uri: "/ai_chats/{id}", code: 200 })),
).annotate({
  identifier: "UpdateAiChatRequest",
}) as any as S.Schema<UpdateAiChatRequest>;

export type CreateAiChatError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Create ai chat [Legacy API — https://docs.whop.com/api-reference] Create a new AI chat thread and send the first message to the AI agent. Required permissions: - `ai_chat:create` */
export const createAiChat: API.OperationMethod<
  CreateAiChatRequest,
  AiChat,
  CreateAiChatError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAiChatRequest,
  output: AiChat,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type DeleteAiChatError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Delete ai chat [Legacy API — https://docs.whop.com/api-reference] Delete an AI chat thread so it no longer appears in the user's chat list. Required permissions: - `ai_chat:delete` */
export const deleteAiChat: API.OperationMethod<
  DeleteAiChatRequest,
  DeleteAiChatResponse,
  DeleteAiChatError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteAiChatRequest,
  output: DeleteAiChatResponse,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type ListAiChatError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** List ai chats [Legacy API — https://docs.whop.com/api-reference] Returns a paginated list of AI chat threads for the current authenticated user. */
export const listAiChat: API.PaginatedOperationMethod<
  ListAiChatRequest,
  ListAiChatResponse,
  ListAiChatError,
  WhopOpContext,
  AiChatListItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListAiChatRequest,
    output: ListAiChatResponse,
    errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
    protocol: WhopProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "relay",
      inputToken: "after",
      outputToken: "page_info.end_cursor",
      hasNextPage: "page_info.has_next_page",
      items: "data",
      pageSize: "first",
    } as const,
  }),
  paginateRelay,
) as any;

export type RetrieveAiChatError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Retrieve ai chat [Legacy API — https://docs.whop.com/api-reference] Retrieves the details of an existing AI chat. */
export const retrieveAiChat: API.OperationMethod<
  RetrieveAiChatRequest,
  AiChat,
  RetrieveAiChatError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveAiChatRequest,
  output: AiChat,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type UpdateAiChatError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | WhopOpError;
/** Update ai chat [Legacy API — https://docs.whop.com/api-reference] Update an AI chat's title, notification preferences, or associated company context. Required permissions: - `ai_chat:update` */
export const updateAiChat: API.OperationMethod<
  UpdateAiChatRequest,
  AiChat,
  UpdateAiChatError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateAiChatRequest,
  output: AiChat,
  errors: [BadRequest, Forbidden, NotFound, UnprocessableEntity],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
