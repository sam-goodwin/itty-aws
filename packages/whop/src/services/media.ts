// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/api-v1-native.json + api-v1-stable.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  WhopProtocol,
  type WhopOpError,
  type WhopOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { WhopOpError, WhopOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class PaymentRequired
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<PaymentRequired>()("PaymentRequired", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withQuotaError),
    [{ status: 402 }],
  ) {}

/** Video length in seconds. Video only; defaults to 5. */
export type GenerateMediaAssetRequestDurationSeconds = 5 | 10 | 15;
export const GenerateMediaAssetRequestDurationSeconds = /*@__PURE__*/ S.Number;

/** Optional reference image file IDs (`file_` prefixed), up to 4. For video, a single reference seeds the opening frame; multiple references guide subject and style instead. */
export type GenerateMediaAssetRequestReferenceMediaList = Array<string>;
export const GenerateMediaAssetRequestReferenceMediaList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<GenerateMediaAssetRequestReferenceMediaList>;

/** Video resolution. Video only; defaults to `1080p`. `1080p` is not supported by Seedance 2.0 Fast or Mini; `4k` is only supported by Seedance 2.0. */
export type GenerateMediaAssetRequestResolution =
  | "480p"
  | "720p"
  | "1080p"
  | "4k";
export const GenerateMediaAssetRequestResolution = /*@__PURE__*/ S.String;

/** The kind of media to generate. */
export type GenerateMediaAssetRequestType = "video" | "image";
export const GenerateMediaAssetRequestType = /*@__PURE__*/ S.String;

export interface GenerateMediaAssetRequest {
  /** Account ID, prefixed `biz_`. Defaults to the account the API key belongs to. */
  account_id?: string;
  /** Video length in seconds. Video only; defaults to 5. */
  duration_seconds?: GenerateMediaAssetRequestDurationSeconds | (number & {});
  /** What to generate. Up to 2,000 characters. */
  prompt: string;
  /** Optional reference image file IDs (`file_` prefixed), up to 4. For video, a single reference seeds the opening frame; multiple references guide subject and style instead. */
  reference_media?: GenerateMediaAssetRequestReferenceMediaList;
  /** Video resolution. Video only; defaults to `1080p`. `1080p` is not supported by Seedance 2.0 Fast or Mini; `4k` is only supported by Seedance 2.0. */
  resolution?: GenerateMediaAssetRequestResolution | (string & {});
  /** The kind of media to generate. */
  type: GenerateMediaAssetRequestType | (string & {});
  /** A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). */
  idempotency_key?: string;
}
export const GenerateMediaAssetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account_id: S.optional(S.String),
    duration_seconds: S.optional(GenerateMediaAssetRequestDurationSeconds),
    prompt: S.String,
    reference_media: S.optional(GenerateMediaAssetRequestReferenceMediaList),
    resolution: S.optional(GenerateMediaAssetRequestResolution),
    type: GenerateMediaAssetRequestType,
    idempotency_key: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
  }).pipe(T.Http({ method: "POST", uri: "/media/generate", code: 200 })),
).annotate({
  identifier: "GenerateMediaAssetRequest",
}) as any as S.Schema<GenerateMediaAssetRequest>;

export interface MediaAssetFile {
  /** File ID, prefixed `file_`. */
  id: string;
  /** CDN URL for downloading the file. */
  url: string;
}
export const MediaAssetFile = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    url: S.String,
  }),
).annotate({ identifier: "MediaAssetFile" }) as any as S.Schema<MediaAssetFile>;

export type MediaAssetGenerationReferenceMediaList = Array<string>;
export const MediaAssetGenerationReferenceMediaList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<MediaAssetGenerationReferenceMediaList>;

/** Requested video resolution. `null` for images. `1080p` is not supported by Seedance 2.0 Fast or Mini; `4k` is only supported by Seedance 2.0. */
export type MediaAssetGenerationResolution = "480p" | "720p" | "1080p" | "4k";
export const MediaAssetGenerationResolution = /*@__PURE__*/ S.String;

export interface MediaAssetGeneration {
  /** Requested video length in seconds. `null` for images. */
  duration_seconds: number | null;
  /** What the asset was generated from. */
  prompt: string;
  reference_media: MediaAssetGenerationReferenceMediaList;
  /** Requested video resolution. `null` for images. `1080p` is not supported by Seedance 2.0 Fast or Mini; `4k` is only supported by Seedance 2.0. */
  resolution: MediaAssetGenerationResolution | null;
}
export const MediaAssetGeneration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    duration_seconds: S.NullOr(S.Number),
    prompt: S.String,
    reference_media: MediaAssetGenerationReferenceMediaList,
    resolution: S.NullOr(MediaAssetGenerationResolution),
  }),
).annotate({
  identifier: "MediaAssetGeneration",
}) as any as S.Schema<MediaAssetGeneration>;

/** The kind of media this asset holds. */
export type MediaAssetMediaType = "video" | "image";
export const MediaAssetMediaType = /*@__PURE__*/ S.String;

/** How the asset was created. Always `generated`. */
export type MediaAssetSource = "generated";
export const MediaAssetSource = /*@__PURE__*/ S.String;

/** Lifecycle state: `processing` while generation runs, `ready` when the file is available, `failed` when generation failed and the charge was refunded. */
export type MediaAssetStatus = "processing" | "ready" | "failed";
export const MediaAssetStatus = /*@__PURE__*/ S.String;

export interface MediaAsset {
  /** USD amount charged to the account's balance for this generation. `null` if the generation wasn't billed. */
  amount_charged: number | null;
  /** ISO 8601 timestamp when the asset reached a terminal state. `null` while `processing`. */
  completed_at: string | null;
  /** ISO 8601 timestamp when the generation was requested. */
  created_at: string;
  /** Currency of `amount_charged`. Always `usd`. */
  currency: string;
  /** Why generation failed. `null` unless status is `failed`. */
  error_message: string | null;
  /** The produced file, usable anywhere attachments are accepted. `null` until the asset is `ready`. */
  file: MediaAssetFile | null;
  /** The inputs the asset was generated from. */
  generation: MediaAssetGeneration;
  /** Media asset ID, prefixed `media_`. */
  id: string;
  /** The kind of media this asset holds. */
  media_type: MediaAssetMediaType;
  /** How the asset was created. Always `generated`. */
  source: MediaAssetSource;
  /** Lifecycle state: `processing` while generation runs, `ready` when the file is available, `failed` when generation failed and the charge was refunded. */
  status: MediaAssetStatus;
}
export const MediaAsset = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount_charged: S.NullOr(S.Number),
    completed_at: S.NullOr(S.String),
    created_at: S.String,
    currency: S.String,
    error_message: S.NullOr(S.String),
    file: S.NullOr(MediaAssetFile),
    generation: MediaAssetGeneration,
    id: S.String,
    media_type: MediaAssetMediaType,
    source: MediaAssetSource,
    status: MediaAssetStatus,
  }),
).annotate({ identifier: "MediaAsset" }) as any as S.Schema<MediaAsset>;

export interface RetrieveMediaAssetRequest {
  /** Media asset ID, prefixed `media_`. */
  id: string;
}
export const RetrieveMediaAssetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/media/{id}", code: 200 })),
).annotate({
  identifier: "RetrieveMediaAssetRequest",
}) as any as S.Schema<RetrieveMediaAssetRequest>;

export type GenerateMediaAssetError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | Conflict
  | WhopOpError;
/** Generate Media Asset Starts an AI media generation job billed from the account's balance. Generation is asynchronous — poll `GET /media/{id}` until the asset is `ready`, then use `file.id` anywhere attachments are accepted. */
export const generateMediaAsset: API.OperationMethod<
  GenerateMediaAssetRequest,
  MediaAsset,
  GenerateMediaAssetError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GenerateMediaAssetRequest,
  output: MediaAsset,
  errors: [BadRequest, PaymentRequired, Forbidden, Conflict],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));

export type RetrieveMediaAssetError = NotFound | WhopOpError;
/** Retrieve Media Asset Retrieves a media asset by ID. Poll this while the asset is `processing`. */
export const retrieveMediaAsset: API.OperationMethod<
  RetrieveMediaAssetRequest,
  MediaAsset,
  RetrieveMediaAssetError,
  WhopOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RetrieveMediaAssetRequest,
  output: MediaAsset,
  errors: [NotFound],
  protocol: WhopProtocol,
  retry: Retry.Retry,
}));
