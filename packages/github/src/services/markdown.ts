// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/rest-api-description). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  GithubProtocol,
  type GithubOpError,
  type GithubOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { GithubOpError, GithubOpContext };

/** The rendering mode. */
export type RenderRequestMode = "markdown" | "gfm";
export const RenderRequestMode = /*@__PURE__*/ S.String;

export interface RenderRequest {
  /** The Markdown text to render in HTML. */
  text: string;
  /** The rendering mode. */
  mode?: RenderRequestMode | (string & {});
  /** The repository context to use when creating references in `gfm` mode. For example, setting `context` to `octo-org/octo-repo` will change the text `#42` into an HTML link to issue 42 in the `octo-org/octo-repo` repository. */
  context?: string;
}
export const RenderRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    text: S.String,
    mode: S.optional(RenderRequestMode),
    context: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/markdown", code: 200 })),
).annotate({ identifier: "RenderRequest" }) as any as S.Schema<RenderRequest>;

export interface RenderResponse {}
export const RenderResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({ identifier: "RenderResponse" }) as any as S.Schema<RenderResponse>;

export interface RenderRawRequest {}
export const RenderRawRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "POST", uri: "/markdown/raw", code: 200 }),
  ),
).annotate({
  identifier: "RenderRawRequest",
}) as any as S.Schema<RenderRawRequest>;

export interface RenderRawResponse {}
export const RenderRawResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "RenderRawResponse",
}) as any as S.Schema<RenderRawResponse>;

export type RenderError = GithubOpError;
/** Render a Markdown document Depending on what is rendered in the Markdown, you may need to provide additional token scopes for labels, such as `issues:read` or `pull_requests:read`. */
export const render: API.OperationMethod<
  RenderRequest,
  RenderResponse,
  RenderError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RenderRequest,
  output: RenderResponse,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type RenderRawError = GithubOpError;
/** Render a Markdown document in raw mode You must send Markdown as plain text (using a `Content-Type` header of `text/plain` or `text/x-markdown`) to this endpoint, rather than using JSON format. In raw mode, [GitHub Flavored Markdown](https://github.github.com/gfm/) is not supported and Markdown will be rendered in plain format like a README.md file. Markdown content must be 400 KB or less. */
export const renderRaw: API.OperationMethod<
  RenderRawRequest,
  RenderRawResponse,
  RenderRawError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RenderRawRequest,
  output: RenderRawResponse,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));
