// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/rest-api-description). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  GithubProtocol,
  type GithubOpError,
  type GithubOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { GithubOpError, GithubOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** The registry type. */
export type CreateOrgPrivateRegistryRequestRegistryType =
  | "maven_repository"
  | "nuget_feed"
  | "goproxy_server"
  | "npm_registry"
  | "rubygems_server"
  | "cargo_registry"
  | "composer_repository"
  | "docker_registry"
  | "git_source"
  | "helm_registry"
  | "hex_organization"
  | "hex_repository"
  | "pub_repository"
  | "python_index"
  | "terraform_registry";
export const CreateOrgPrivateRegistryRequestRegistryType =
  /*@__PURE__*/ S.String;

/** Which type of organization repositories have access to the private registry. `selected` means only the repositories specified by `selected_repository_ids` can access the private registry. */
export type CreateOrgPrivateRegistryRequestVisibility =
  | "all"
  | "private"
  | "selected";
export const CreateOrgPrivateRegistryRequestVisibility = /*@__PURE__*/ S.String;

/** An array of repository IDs that can access the organization private registry. You can only provide a list of repository IDs when `visibility` is set to `selected`. You can manage the list of selected repositories using the [Update a private registry for an organization](https://docs.github.com/rest/private-registries/organization-configurations#update-a-private-registry-for-an-organization) endpoint. This field should be omitted if `visibility` is set to `all` or `private`. */
export type CreateOrgPrivateRegistryRequestSelectedRepositoryIdsList =
  Array<number>;
export const CreateOrgPrivateRegistryRequestSelectedRepositoryIdsList =
  /*@__PURE__*/ S.Array(
    S.Number,
  ) as any as S.Schema<CreateOrgPrivateRegistryRequestSelectedRepositoryIdsList>;

/** The authentication type for the private registry. Defaults to `token` if not specified. Use `oidc_azure`, `oidc_aws`, `oidc_jfrog`, `oidc_cloudsmith`, or `oidc_gcp` for OIDC authentication. */
export type CreateOrgPrivateRegistryRequestAuthType =
  | "token"
  | "username_password"
  | "oidc_azure"
  | "oidc_aws"
  | "oidc_jfrog"
  | "oidc_cloudsmith"
  | "oidc_gcp";
export const CreateOrgPrivateRegistryRequestAuthType = /*@__PURE__*/ S.String;

export interface CreateOrgPrivateRegistryRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The registry type. */
  registry_type: CreateOrgPrivateRegistryRequestRegistryType | (string & {});
  /** The URL of the private registry. */
  url: string;
  /** The username to use when authenticating with the private registry. This field should be omitted if the private registry does not require a username for authentication. */
  username?: string | null;
  /** Whether this private registry should replace the base registry (e.g., npmjs.org for npm, rubygems.org for rubygems). When set to `true`, Dependabot will only use this registry and will not fall back to the public registry. When set to `false` (default), Dependabot will use this registry for scoped packages but may fall back to the public registry for other packages. */
  replaces_base?: boolean;
  /** The value for your secret, encrypted with [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages) using the public key retrieved from the [Get private registries public key for an organization](https://docs.github.com/rest/private-registries/organization-configurations#get-private-registries-public-key-for-an-organization) endpoint. Required when `auth_type` is `token` or `username_password`. Should be omitted for OIDC auth types. */
  encrypted_value?: string;
  /** The ID of the key you used to encrypt the secret. Required when `auth_type` is `token` or `username_password`. Should be omitted for OIDC auth types. */
  key_id?: string;
  /** Which type of organization repositories have access to the private registry. `selected` means only the repositories specified by `selected_repository_ids` can access the private registry. */
  visibility: CreateOrgPrivateRegistryRequestVisibility | (string & {});
  /** An array of repository IDs that can access the organization private registry. You can only provide a list of repository IDs when `visibility` is set to `selected`. You can manage the list of selected repositories using the [Update a private registry for an organization](https://docs.github.com/rest/private-registries/organization-configurations#update-a-private-registry-for-an-organization) endpoint. This field should be omitted if `visibility` is set to `all` or `private`. */
  selected_repository_ids?: CreateOrgPrivateRegistryRequestSelectedRepositoryIdsList;
  /** The authentication type for the private registry. Defaults to `token` if not specified. Use `oidc_azure`, `oidc_aws`, `oidc_jfrog`, `oidc_cloudsmith`, or `oidc_gcp` for OIDC authentication. */
  auth_type?: CreateOrgPrivateRegistryRequestAuthType | (string & {});
  /** The tenant ID of the Azure AD application. Required when `auth_type` is `oidc_azure`. */
  tenant_id?: string;
  /** The client ID of the Azure AD application. Required when `auth_type` is `oidc_azure`. */
  client_id?: string;
  /** The AWS region. Required when `auth_type` is `oidc_aws`. */
  aws_region?: string;
  /** The AWS account ID. Required when `auth_type` is `oidc_aws`. */
  account_id?: string;
  /** The AWS IAM role name. Required when `auth_type` is `oidc_aws`. */
  role_name?: string;
  /** The CodeArtifact domain. Required when `auth_type` is `oidc_aws`. */
  domain?: string;
  /** The CodeArtifact domain owner (AWS account ID). Required when `auth_type` is `oidc_aws`. */
  domain_owner?: string;
  /** The JFrog OIDC provider name. Required when `auth_type` is `oidc_jfrog`. */
  jfrog_oidc_provider_name?: string;
  /** The OIDC audience. Optional for `oidc_aws`, `oidc_jfrog`, and `oidc_gcp`, and required for `oidc_cloudsmith` auth types. */
  audience?: string;
  /** The JFrog identity mapping name. Optional for `oidc_jfrog` auth type. */
  identity_mapping_name?: string;
  /** The Cloudsmith organization namespace. Required when `auth_type` is `oidc_cloudsmith`. */
  namespace?: string;
  /** The Cloudsmith service account slug. Required when `auth_type` is `oidc_cloudsmith`. */
  service_slug?: string;
  /** The Cloudsmith API host. Optional for `oidc_cloudsmith` auth type. If omitted, `api.cloudsmith.io` is used by default. */
  api_host?: string;
  /** The full resource name of the GCP Workload Identity Provider (e.g. `projects/<NUM>/locations/global/workloadIdentityPools/<POOL>/providers/<PROVIDER>`). Required when `auth_type` is `oidc_gcp`. */
  workload_identity_provider?: string;
  /** The GCP service account email to impersonate. Optional for `oidc_gcp` auth type. If omitted, the federated token is used directly (direct WIF). */
  service_account?: string;
}
export const CreateOrgPrivateRegistryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    registry_type: CreateOrgPrivateRegistryRequestRegistryType,
    url: S.String,
    username: S.optional(S.NullOr(S.String)),
    replaces_base: S.optional(S.Boolean),
    encrypted_value: S.optional(S.String),
    key_id: S.optional(S.String),
    visibility: CreateOrgPrivateRegistryRequestVisibility,
    selected_repository_ids: S.optional(
      CreateOrgPrivateRegistryRequestSelectedRepositoryIdsList,
    ),
    auth_type: S.optional(CreateOrgPrivateRegistryRequestAuthType),
    tenant_id: S.optional(S.String),
    client_id: S.optional(S.String),
    aws_region: S.optional(S.String),
    account_id: S.optional(S.String),
    role_name: S.optional(S.String),
    domain: S.optional(S.String),
    domain_owner: S.optional(S.String),
    jfrog_oidc_provider_name: S.optional(S.String),
    audience: S.optional(S.String),
    identity_mapping_name: S.optional(S.String),
    namespace: S.optional(S.String),
    service_slug: S.optional(S.String),
    api_host: S.optional(S.String),
    workload_identity_provider: S.optional(S.String),
    service_account: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/orgs/{org}/private-registries",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateOrgPrivateRegistryRequest",
}) as any as S.Schema<CreateOrgPrivateRegistryRequest>;

/** The registry type. */
export type OrgPrivateRegistryConfigurationWithSelectedRepositoriesRegistryType =
    | "maven_repository"
    | "nuget_feed"
    | "goproxy_server"
    | "npm_registry"
    | "rubygems_server"
    | "cargo_registry"
    | "composer_repository"
    | "docker_registry"
    | "git_source"
    | "helm_registry"
    | "hex_organization"
    | "hex_repository"
    | "pub_repository"
    | "python_index"
    | "terraform_registry";
export const OrgPrivateRegistryConfigurationWithSelectedRepositoriesRegistryType =
  /*@__PURE__*/ S.String;

/** The authentication type for the private registry. */
export type OrgPrivateRegistryConfigurationWithSelectedRepositoriesAuthType =
  | "token"
  | "username_password"
  | "oidc_azure"
  | "oidc_aws"
  | "oidc_jfrog"
  | "oidc_cloudsmith"
  | "oidc_gcp";
export const OrgPrivateRegistryConfigurationWithSelectedRepositoriesAuthType =
  /*@__PURE__*/ S.String;

/** Which type of organization repositories have access to the private registry. `selected` means only the repositories specified by `selected_repository_ids` can access the private registry. */
export type OrgPrivateRegistryConfigurationWithSelectedRepositoriesVisibility =
  | "all"
  | "private"
  | "selected";
export const OrgPrivateRegistryConfigurationWithSelectedRepositoriesVisibility =
  /*@__PURE__*/ S.String;

/** An array of repository IDs that can access the organization private registry when `visibility` is set to `selected`. */
export type OrgPrivateRegistryConfigurationWithSelectedRepositoriesSelectedRepositoryIdsList =
  Array<number>;
export const OrgPrivateRegistryConfigurationWithSelectedRepositoriesSelectedRepositoryIdsList =
  /*@__PURE__*/ S.Array(
    S.Number,
  ) as any as S.Schema<OrgPrivateRegistryConfigurationWithSelectedRepositoriesSelectedRepositoryIdsList>;

/** Private registry configuration for an organization */
export interface OrgPrivateRegistryConfigurationWithSelectedRepositories {
  /** The name of the private registry configuration. */
  name: string;
  /** The registry type. */
  registry_type: OrgPrivateRegistryConfigurationWithSelectedRepositoriesRegistryType;
  /** The authentication type for the private registry. */
  auth_type?: OrgPrivateRegistryConfigurationWithSelectedRepositoriesAuthType;
  /** The URL of the private registry. */
  url?: string;
  /** The username to use when authenticating with the private registry. */
  username?: string;
  /** Whether this private registry replaces the base registry (e.g., npmjs.org for npm, rubygems.org for rubygems). When `true`, Dependabot will only use this registry and will not fall back to the public registry. When `false` (default), Dependabot will use this registry for scoped packages but may fall back to the public registry for other packages. */
  replaces_base?: boolean;
  /** Which type of organization repositories have access to the private registry. `selected` means only the repositories specified by `selected_repository_ids` can access the private registry. */
  visibility: OrgPrivateRegistryConfigurationWithSelectedRepositoriesVisibility;
  /** An array of repository IDs that can access the organization private registry when `visibility` is set to `selected`. */
  selected_repository_ids?: OrgPrivateRegistryConfigurationWithSelectedRepositoriesSelectedRepositoryIdsList;
  /** The tenant ID of the Azure AD application. */
  tenant_id?: string;
  /** The client ID of the Azure AD application. */
  client_id?: string;
  /** The AWS region. */
  aws_region?: string;
  /** The AWS account ID. */
  account_id?: string;
  /** The AWS IAM role name. */
  role_name?: string;
  /** The CodeArtifact domain. */
  domain?: string;
  /** The CodeArtifact domain owner. */
  domain_owner?: string;
  /** The JFrog OIDC provider name. */
  jfrog_oidc_provider_name?: string;
  /** The OIDC audience. */
  audience?: string;
  /** The JFrog identity mapping name. */
  identity_mapping_name?: string;
  /** The Cloudsmith organization namespace. */
  namespace?: string;
  /** The Cloudsmith service account slug. */
  service_slug?: string;
  /** The Cloudsmith API host. */
  api_host?: string;
  /** The full resource name of the GCP Workload Identity Provider (e.g. `projects/<NUM>/locations/global/workloadIdentityPools/<POOL>/providers/<PROVIDER>`). */
  workload_identity_provider?: string;
  /** The GCP service account email to impersonate. If omitted, the federated token is used directly (direct WIF). */
  service_account?: string;
  created_at: string;
  updated_at: string;
}
export const OrgPrivateRegistryConfigurationWithSelectedRepositories =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      name: S.String,
      registry_type:
        OrgPrivateRegistryConfigurationWithSelectedRepositoriesRegistryType,
      auth_type: S.optional(
        OrgPrivateRegistryConfigurationWithSelectedRepositoriesAuthType,
      ),
      url: S.optional(S.String),
      username: S.optional(S.String),
      replaces_base: S.optional(S.Boolean),
      visibility:
        OrgPrivateRegistryConfigurationWithSelectedRepositoriesVisibility,
      selected_repository_ids: S.optional(
        OrgPrivateRegistryConfigurationWithSelectedRepositoriesSelectedRepositoryIdsList,
      ),
      tenant_id: S.optional(S.String),
      client_id: S.optional(S.String),
      aws_region: S.optional(S.String),
      account_id: S.optional(S.String),
      role_name: S.optional(S.String),
      domain: S.optional(S.String),
      domain_owner: S.optional(S.String),
      jfrog_oidc_provider_name: S.optional(S.String),
      audience: S.optional(S.String),
      identity_mapping_name: S.optional(S.String),
      namespace: S.optional(S.String),
      service_slug: S.optional(S.String),
      api_host: S.optional(S.String),
      workload_identity_provider: S.optional(S.String),
      service_account: S.optional(S.String),
      created_at: S.String,
      updated_at: S.String,
    }),
  ).annotate({
    identifier: "OrgPrivateRegistryConfigurationWithSelectedRepositories",
  }) as any as S.Schema<OrgPrivateRegistryConfigurationWithSelectedRepositories>;

export interface DeleteOrgPrivateRegistryRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The name of the secret. */
  secret_name: string;
}
export const DeleteOrgPrivateRegistryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    secret_name: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/orgs/{org}/private-registries/{secret_name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteOrgPrivateRegistryRequest",
}) as any as S.Schema<DeleteOrgPrivateRegistryRequest>;

export interface DeleteOrgPrivateRegistryResponse {}
export const DeleteOrgPrivateRegistryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteOrgPrivateRegistryResponse",
}) as any as S.Schema<DeleteOrgPrivateRegistryResponse>;

export interface GetOrgPrivateRegistryRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The name of the secret. */
  secret_name: string;
}
export const GetOrgPrivateRegistryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    secret_name: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/orgs/{org}/private-registries/{secret_name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetOrgPrivateRegistryRequest",
}) as any as S.Schema<GetOrgPrivateRegistryRequest>;

/** The registry type. */
export type OrgPrivateRegistryConfigurationRegistryType =
  | "maven_repository"
  | "nuget_feed"
  | "goproxy_server"
  | "npm_registry"
  | "rubygems_server"
  | "cargo_registry"
  | "composer_repository"
  | "docker_registry"
  | "git_source"
  | "helm_registry"
  | "hex_organization"
  | "hex_repository"
  | "pub_repository"
  | "python_index"
  | "terraform_registry";
export const OrgPrivateRegistryConfigurationRegistryType =
  /*@__PURE__*/ S.String;

/** The authentication type for the private registry. */
export type OrgPrivateRegistryConfigurationAuthType =
  | "token"
  | "username_password"
  | "oidc_azure"
  | "oidc_aws"
  | "oidc_jfrog"
  | "oidc_cloudsmith"
  | "oidc_gcp";
export const OrgPrivateRegistryConfigurationAuthType = /*@__PURE__*/ S.String;

/** Which type of organization repositories have access to the private registry. */
export type OrgPrivateRegistryConfigurationVisibility =
  | "all"
  | "private"
  | "selected";
export const OrgPrivateRegistryConfigurationVisibility = /*@__PURE__*/ S.String;

/** Private registry configuration for an organization */
export interface OrgPrivateRegistryConfiguration {
  /** The name of the private registry configuration. */
  name: string;
  /** The registry type. */
  registry_type: OrgPrivateRegistryConfigurationRegistryType;
  /** The authentication type for the private registry. */
  auth_type?: OrgPrivateRegistryConfigurationAuthType;
  /** The URL of the private registry. */
  url?: string;
  /** The username to use when authenticating with the private registry. */
  username?: string | null;
  /** Whether this private registry replaces the base registry (e.g., npmjs.org for npm, rubygems.org for rubygems). When `true`, Dependabot will only use this registry and will not fall back to the public registry. When `false` (default), Dependabot will use this registry for scoped packages but may fall back to the public registry for other packages. */
  replaces_base?: boolean;
  /** Which type of organization repositories have access to the private registry. */
  visibility: OrgPrivateRegistryConfigurationVisibility;
  /** The tenant ID of the Azure AD application. */
  tenant_id?: string;
  /** The client ID of the Azure AD application. */
  client_id?: string;
  /** The AWS region. */
  aws_region?: string;
  /** The AWS account ID. */
  account_id?: string;
  /** The AWS IAM role name. */
  role_name?: string;
  /** The CodeArtifact domain. */
  domain?: string;
  /** The CodeArtifact domain owner. */
  domain_owner?: string;
  /** The JFrog OIDC provider name. */
  jfrog_oidc_provider_name?: string;
  /** The OIDC audience. */
  audience?: string;
  /** The JFrog identity mapping name. */
  identity_mapping_name?: string;
  /** The Cloudsmith organization namespace. */
  namespace?: string;
  /** The Cloudsmith service account slug. */
  service_slug?: string;
  /** The Cloudsmith API host. */
  api_host?: string;
  /** The full resource name of the GCP Workload Identity Provider (e.g. `projects/<NUM>/locations/global/workloadIdentityPools/<POOL>/providers/<PROVIDER>`). */
  workload_identity_provider?: string;
  /** The GCP service account email to impersonate. If omitted, the federated token is used directly (direct WIF). */
  service_account?: string;
  created_at: string;
  updated_at: string;
}
export const OrgPrivateRegistryConfiguration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    registry_type: OrgPrivateRegistryConfigurationRegistryType,
    auth_type: S.optional(OrgPrivateRegistryConfigurationAuthType),
    url: S.optional(S.String),
    username: S.optional(S.NullOr(S.String)),
    replaces_base: S.optional(S.Boolean),
    visibility: OrgPrivateRegistryConfigurationVisibility,
    tenant_id: S.optional(S.String),
    client_id: S.optional(S.String),
    aws_region: S.optional(S.String),
    account_id: S.optional(S.String),
    role_name: S.optional(S.String),
    domain: S.optional(S.String),
    domain_owner: S.optional(S.String),
    jfrog_oidc_provider_name: S.optional(S.String),
    audience: S.optional(S.String),
    identity_mapping_name: S.optional(S.String),
    namespace: S.optional(S.String),
    service_slug: S.optional(S.String),
    api_host: S.optional(S.String),
    workload_identity_provider: S.optional(S.String),
    service_account: S.optional(S.String),
    created_at: S.String,
    updated_at: S.String,
  }),
).annotate({
  identifier: "OrgPrivateRegistryConfiguration",
}) as any as S.Schema<OrgPrivateRegistryConfiguration>;

export interface GetOrgPublicKeyRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
}
export const GetOrgPublicKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/orgs/{org}/private-registries/public-key",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetOrgPublicKeyRequest",
}) as any as S.Schema<GetOrgPublicKeyRequest>;

export interface GetOrgPublicKeyResponse {
  /** The identifier for the key. */
  key_id: string;
  /** The Base64 encoded public key. */
  key: string;
}
export const GetOrgPublicKeyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key_id: S.String,
    key: S.String,
  }),
).annotate({
  identifier: "GetOrgPublicKeyResponse",
}) as any as S.Schema<GetOrgPublicKeyResponse>;

export interface ListOrgPrivateRegistriesRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)." */
  per_page?: number;
  /** The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)." */
  page?: number;
}
export const ListOrgPrivateRegistriesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    per_page: S.optional(S.Number.pipe(T.Query())),
    page: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/orgs/{org}/private-registries", code: 200 }),
  ),
).annotate({
  identifier: "ListOrgPrivateRegistriesRequest",
}) as any as S.Schema<ListOrgPrivateRegistriesRequest>;

export type ListOrgPrivateRegistriesResponseConfigurationsList =
  Array<OrgPrivateRegistryConfiguration>;
export const ListOrgPrivateRegistriesResponseConfigurationsList =
  /*@__PURE__*/ S.Array(
    OrgPrivateRegistryConfiguration,
  ) as any as S.Schema<ListOrgPrivateRegistriesResponseConfigurationsList>;

export interface ListOrgPrivateRegistriesResponse {
  total_count: number;
  configurations: ListOrgPrivateRegistriesResponseConfigurationsList;
}
export const ListOrgPrivateRegistriesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    total_count: S.Number,
    configurations: ListOrgPrivateRegistriesResponseConfigurationsList,
  }),
).annotate({
  identifier: "ListOrgPrivateRegistriesResponse",
}) as any as S.Schema<ListOrgPrivateRegistriesResponse>;

/** The registry type. */
export type UpdateOrgPrivateRegistryRequestRegistryType =
  | "maven_repository"
  | "nuget_feed"
  | "goproxy_server"
  | "npm_registry"
  | "rubygems_server"
  | "cargo_registry"
  | "composer_repository"
  | "docker_registry"
  | "git_source"
  | "helm_registry"
  | "hex_organization"
  | "hex_repository"
  | "pub_repository"
  | "python_index"
  | "terraform_registry";
export const UpdateOrgPrivateRegistryRequestRegistryType =
  /*@__PURE__*/ S.String;

/** Which type of organization repositories have access to the private registry. `selected` means only the repositories specified by `selected_repository_ids` can access the private registry. */
export type UpdateOrgPrivateRegistryRequestVisibility =
  | "all"
  | "private"
  | "selected";
export const UpdateOrgPrivateRegistryRequestVisibility = /*@__PURE__*/ S.String;

/** An array of repository IDs that can access the organization private registry. You can only provide a list of repository IDs when `visibility` is set to `selected`. This field should be omitted if `visibility` is set to `all` or `private`. */
export type UpdateOrgPrivateRegistryRequestSelectedRepositoryIdsList =
  Array<number>;
export const UpdateOrgPrivateRegistryRequestSelectedRepositoryIdsList =
  /*@__PURE__*/ S.Array(
    S.Number,
  ) as any as S.Schema<UpdateOrgPrivateRegistryRequestSelectedRepositoryIdsList>;

/** The authentication type for the private registry. This field cannot be changed after creation. If provided, it must match the existing `auth_type` of the configuration. To change the authentication type, delete and recreate the configuration. */
export type UpdateOrgPrivateRegistryRequestAuthType =
  | "token"
  | "username_password"
  | "oidc_azure"
  | "oidc_aws"
  | "oidc_jfrog"
  | "oidc_cloudsmith"
  | "oidc_gcp";
export const UpdateOrgPrivateRegistryRequestAuthType = /*@__PURE__*/ S.String;

export interface UpdateOrgPrivateRegistryRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The name of the secret. */
  secret_name: string;
  /** The registry type. */
  registry_type?: UpdateOrgPrivateRegistryRequestRegistryType | (string & {});
  /** The URL of the private registry. */
  url?: string;
  /** The username to use when authenticating with the private registry. This field should be omitted if the private registry does not require a username for authentication. */
  username?: string | null;
  /** Whether this private registry should replace the base registry (e.g., npmjs.org for npm, rubygems.org for rubygems). When set to `true`, Dependabot will only use this registry and will not fall back to the public registry. When set to `false` (default), Dependabot will use this registry for scoped packages but may fall back to the public registry for other packages. */
  replaces_base?: boolean;
  /** The value for your secret, encrypted with [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages) using the public key retrieved from the [Get private registries public key for an organization](https://docs.github.com/rest/private-registries/organization-configurations#get-private-registries-public-key-for-an-organization) endpoint. */
  encrypted_value?: string;
  /** The ID of the key you used to encrypt the secret. */
  key_id?: string;
  /** Which type of organization repositories have access to the private registry. `selected` means only the repositories specified by `selected_repository_ids` can access the private registry. */
  visibility?: UpdateOrgPrivateRegistryRequestVisibility | (string & {});
  /** An array of repository IDs that can access the organization private registry. You can only provide a list of repository IDs when `visibility` is set to `selected`. This field should be omitted if `visibility` is set to `all` or `private`. */
  selected_repository_ids?: UpdateOrgPrivateRegistryRequestSelectedRepositoryIdsList;
  /** The authentication type for the private registry. This field cannot be changed after creation. If provided, it must match the existing `auth_type` of the configuration. To change the authentication type, delete and recreate the configuration. */
  auth_type?: UpdateOrgPrivateRegistryRequestAuthType | (string & {});
  /** The tenant ID of the Azure AD application. Required when `auth_type` is `oidc_azure`. */
  tenant_id?: string;
  /** The client ID of the Azure AD application. Required when `auth_type` is `oidc_azure`. */
  client_id?: string;
  /** The AWS region. Required when `auth_type` is `oidc_aws`. */
  aws_region?: string;
  /** The AWS account ID. Required when `auth_type` is `oidc_aws`. */
  account_id?: string;
  /** The AWS IAM role name. Required when `auth_type` is `oidc_aws`. */
  role_name?: string;
  /** The CodeArtifact domain. Required when `auth_type` is `oidc_aws`. */
  domain?: string;
  /** The CodeArtifact domain owner (AWS account ID). Required when `auth_type` is `oidc_aws`. */
  domain_owner?: string;
  /** The JFrog OIDC provider name. Required when `auth_type` is `oidc_jfrog`. */
  jfrog_oidc_provider_name?: string;
  /** The OIDC audience. Optional for `oidc_aws`, `oidc_jfrog`, and `oidc_gcp`, and required for `oidc_cloudsmith` auth types. */
  audience?: string;
  /** The JFrog identity mapping name. Optional for `oidc_jfrog` auth type. */
  identity_mapping_name?: string;
  /** The Cloudsmith organization namespace. Required when `auth_type` is `oidc_cloudsmith`. */
  namespace?: string;
  /** The Cloudsmith service account slug. Required when `auth_type` is `oidc_cloudsmith`. */
  service_slug?: string;
  /** The Cloudsmith API host. Optional for `oidc_cloudsmith` auth type. If omitted, `api.cloudsmith.io` is used by default. */
  api_host?: string;
  /** The full resource name of the GCP Workload Identity Provider (e.g. `projects/<NUM>/locations/global/workloadIdentityPools/<POOL>/providers/<PROVIDER>`). Required when `auth_type` is `oidc_gcp`. */
  workload_identity_provider?: string;
  /** The GCP service account email to impersonate. Optional for `oidc_gcp` auth type. If omitted, the federated token is used directly (direct WIF). */
  service_account?: string;
}
export const UpdateOrgPrivateRegistryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    secret_name: S.String.pipe(T.Label()),
    registry_type: S.optional(UpdateOrgPrivateRegistryRequestRegistryType),
    url: S.optional(S.String),
    username: S.optional(S.NullOr(S.String)),
    replaces_base: S.optional(S.Boolean),
    encrypted_value: S.optional(S.String),
    key_id: S.optional(S.String),
    visibility: S.optional(UpdateOrgPrivateRegistryRequestVisibility),
    selected_repository_ids: S.optional(
      UpdateOrgPrivateRegistryRequestSelectedRepositoryIdsList,
    ),
    auth_type: S.optional(UpdateOrgPrivateRegistryRequestAuthType),
    tenant_id: S.optional(S.String),
    client_id: S.optional(S.String),
    aws_region: S.optional(S.String),
    account_id: S.optional(S.String),
    role_name: S.optional(S.String),
    domain: S.optional(S.String),
    domain_owner: S.optional(S.String),
    jfrog_oidc_provider_name: S.optional(S.String),
    audience: S.optional(S.String),
    identity_mapping_name: S.optional(S.String),
    namespace: S.optional(S.String),
    service_slug: S.optional(S.String),
    api_host: S.optional(S.String),
    workload_identity_provider: S.optional(S.String),
    service_account: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/orgs/{org}/private-registries/{secret_name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateOrgPrivateRegistryRequest",
}) as any as S.Schema<UpdateOrgPrivateRegistryRequest>;

export interface UpdateOrgPrivateRegistryResponse {}
export const UpdateOrgPrivateRegistryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UpdateOrgPrivateRegistryResponse",
}) as any as S.Schema<UpdateOrgPrivateRegistryResponse>;

export type CreateOrgPrivateRegistryError =
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Create a private registry for an organization Creates a private registry configuration with an encrypted value for an organization. Encrypt your secret using [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see "[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)." For OIDC-based registries (`oidc_azure`, `oidc_aws`, `oidc_jfrog`, `oidc_cloudsmith`, or `oidc_gcp`), the `encrypted_value` and `key_id` fields should be omitted. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. */
export const createOrgPrivateRegistry: API.OperationMethod<
  CreateOrgPrivateRegistryRequest,
  OrgPrivateRegistryConfigurationWithSelectedRepositories,
  CreateOrgPrivateRegistryError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateOrgPrivateRegistryRequest,
  output: OrgPrivateRegistryConfigurationWithSelectedRepositories,
  errors: [NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type DeleteOrgPrivateRegistryError =
  | BadRequest
  | NotFound
  | GithubOpError;
/** Delete a private registry for an organization Delete a private registry configuration at the organization-level. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. */
export const deleteOrgPrivateRegistry: API.OperationMethod<
  DeleteOrgPrivateRegistryRequest,
  DeleteOrgPrivateRegistryResponse,
  DeleteOrgPrivateRegistryError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteOrgPrivateRegistryRequest,
  output: DeleteOrgPrivateRegistryResponse,
  errors: [BadRequest, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetOrgPrivateRegistryError = NotFound | GithubOpError;
/** Get a private registry for an organization Get the configuration of a single private registry defined for an organization, omitting its encrypted value. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. */
export const getOrgPrivateRegistry: API.OperationMethod<
  GetOrgPrivateRegistryRequest,
  OrgPrivateRegistryConfiguration,
  GetOrgPrivateRegistryError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetOrgPrivateRegistryRequest,
  output: OrgPrivateRegistryConfiguration,
  errors: [NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetOrgPublicKeyError = NotFound | GithubOpError;
/** Get private registries public key for an organization Gets the org public key, which is needed to encrypt private registry secrets. You need to encrypt a secret before you can create or update secrets. OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. */
export const getOrgPublicKey: API.OperationMethod<
  GetOrgPublicKeyRequest,
  GetOrgPublicKeyResponse,
  GetOrgPublicKeyError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetOrgPublicKeyRequest,
  output: GetOrgPublicKeyResponse,
  errors: [NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListOrgPrivateRegistriesError =
  | BadRequest
  | NotFound
  | GithubOpError;
/** List private registries for an organization Lists all private registry configurations available at the organization-level without revealing their encrypted values. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. */
export const listOrgPrivateRegistries: API.OperationMethod<
  ListOrgPrivateRegistriesRequest,
  ListOrgPrivateRegistriesResponse,
  ListOrgPrivateRegistriesError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListOrgPrivateRegistriesRequest,
  output: ListOrgPrivateRegistriesResponse,
  errors: [BadRequest, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type UpdateOrgPrivateRegistryError =
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Update a private registry for an organization Updates a private registry configuration with an encrypted value for an organization. Encrypt your secret using [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see "[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api)." For OIDC-based registries (`oidc_azure`, `oidc_aws`, `oidc_jfrog`, `oidc_cloudsmith`, or `oidc_gcp`), the `encrypted_value` and `key_id` fields should be omitted. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. */
export const updateOrgPrivateRegistry: API.OperationMethod<
  UpdateOrgPrivateRegistryRequest,
  UpdateOrgPrivateRegistryResponse,
  UpdateOrgPrivateRegistryError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateOrgPrivateRegistryRequest,
  output: UpdateOrgPrivateRegistryResponse,
  errors: [NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));
