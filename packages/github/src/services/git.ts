// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/rest-api-description). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  GithubProtocol,
  type GithubOpError,
  type GithubOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { GithubOpError, GithubOpContext };

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface CreateBlobRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The new blob's content. */
  content: string;
  /** The encoding used for `content`. Currently, `"utf-8"` and `"base64"` are supported. */
  encoding?: string;
}
export const CreateBlobRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    content: S.String,
    encoding: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/repos/{owner}/{repo}/git/blobs",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateBlobRequest",
}) as any as S.Schema<CreateBlobRequest>;

/** Short Blob */
export interface ShortBlob {
  url: string;
  sha: string;
}
export const ShortBlob = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
    sha: S.String,
  }),
).annotate({ identifier: "ShortBlob" }) as any as S.Schema<ShortBlob>;

/** The full SHAs of the commits that were the parents of this commit. If omitted or empty, the commit will be written as a root commit. For a single parent, an array of one SHA should be provided; for a merge commit, an array of more than one should be provided. */
export type CreateCommitRequestParentsList = Array<string>;
export const CreateCommitRequestParentsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateCommitRequestParentsList>;

/** Information about the author of the commit. By default, the `author` will be the authenticated user and the current date. See the `author` and `committer` object below for details. */
export interface CreateCommitRequestAuthor {
  /** The name of the author (or committer) of the commit */
  name: string;
  /** The email of the author (or committer) of the commit */
  email: string;
  /** Indicates when this commit was authored (or committed). This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. */
  date?: string;
}
export const CreateCommitRequestAuthor = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    email: S.String,
    date: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateCommitRequestAuthor",
}) as any as S.Schema<CreateCommitRequestAuthor>;

/** Information about the person who is making the commit. By default, `committer` will use the information set in `author`. See the `author` and `committer` object below for details. */
export interface CreateCommitRequestCommitter {
  /** The name of the author (or committer) of the commit */
  name?: string;
  /** The email of the author (or committer) of the commit */
  email?: string;
  /** Indicates when this commit was authored (or committed). This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. */
  date?: string;
}
export const CreateCommitRequestCommitter = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    email: S.optional(S.String),
    date: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateCommitRequestCommitter",
}) as any as S.Schema<CreateCommitRequestCommitter>;

export interface CreateCommitRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The commit message */
  message: string;
  /** The SHA of the tree object this commit points to */
  tree: string;
  /** The full SHAs of the commits that were the parents of this commit. If omitted or empty, the commit will be written as a root commit. For a single parent, an array of one SHA should be provided; for a merge commit, an array of more than one should be provided. */
  parents?: CreateCommitRequestParentsList;
  /** Information about the author of the commit. By default, the `author` will be the authenticated user and the current date. See the `author` and `committer` object below for details. */
  author?: CreateCommitRequestAuthor;
  /** Information about the person who is making the commit. By default, `committer` will use the information set in `author`. See the `author` and `committer` object below for details. */
  committer?: CreateCommitRequestCommitter;
  /** The [PGP signature](https://en.wikipedia.org/wiki/Pretty_Good_Privacy) of the commit. GitHub adds the signature to the `gpgsig` header of the created commit. For a commit signature to be verifiable by Git or GitHub, it must be an ASCII-armored detached PGP signature over the string commit as it would be written to the object database. To pass a `signature` parameter, you need to first manually create a valid PGP signature, which can be complicated. You may find it easier to [use the command line](https://git-scm.com/book/id/v2/Git-Tools-Signing-Your-Work) to create signed commits. */
  signature?: string;
}
export const CreateCommitRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    message: S.String,
    tree: S.String,
    parents: S.optional(CreateCommitRequestParentsList),
    author: S.optional(CreateCommitRequestAuthor),
    committer: S.optional(CreateCommitRequestCommitter),
    signature: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/repos/{owner}/{repo}/git/commits",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateCommitRequest",
}) as any as S.Schema<CreateCommitRequest>;

/** Identifying information for the git-user */
export interface GitCommitAuthor {
  /** Timestamp of the commit */
  date: string;
  /** Git email address of the user */
  email: string;
  /** Name of the git user */
  name: string;
}
export const GitCommitAuthor = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    date: S.String,
    email: S.String,
    name: S.String,
  }),
).annotate({
  identifier: "GitCommitAuthor",
}) as any as S.Schema<GitCommitAuthor>;

/** Identifying information for the git-user */
export type GitCommitCommitter = GitCommitAuthor;
export const GitCommitCommitter = GitCommitAuthor;

export interface GitCommitTree {
  /** SHA for the commit */
  sha: string;
  url: string;
}
export const GitCommitTree = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sha: S.String,
    url: S.String,
  }),
).annotate({ identifier: "GitCommitTree" }) as any as S.Schema<GitCommitTree>;

export interface GitCommitParentsItem {
  /** SHA for the commit */
  sha: string;
  url: string;
  html_url: string;
}
export const GitCommitParentsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sha: S.String,
    url: S.String,
    html_url: S.String,
  }),
).annotate({
  identifier: "GitCommitParentsItem",
}) as any as S.Schema<GitCommitParentsItem>;

export type GitCommitParentsList = Array<GitCommitParentsItem>;
export const GitCommitParentsList = /*@__PURE__*/ S.Array(
  GitCommitParentsItem,
) as any as S.Schema<GitCommitParentsList>;

export interface GitCommitVerification {
  verified: boolean;
  reason: string;
  signature: string | null;
  payload: string | null;
  verified_at: string | null;
}
export const GitCommitVerification = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    verified: S.Boolean,
    reason: S.String,
    signature: S.NullOr(S.String),
    payload: S.NullOr(S.String),
    verified_at: S.NullOr(S.String),
  }),
).annotate({
  identifier: "GitCommitVerification",
}) as any as S.Schema<GitCommitVerification>;

/** Low-level Git commit operations within a repository */
export interface GitCommit {
  /** SHA for the commit */
  sha: string;
  node_id: string;
  url: string;
  /** Identifying information for the git-user */
  author: GitCommitAuthor;
  /** Identifying information for the git-user */
  committer: GitCommitAuthor;
  /** Message describing the purpose of the commit */
  message: string;
  tree: GitCommitTree;
  parents: GitCommitParentsList;
  verification: GitCommitVerification;
  html_url: string;
}
export const GitCommit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sha: S.String,
    node_id: S.String,
    url: S.String,
    author: GitCommitAuthor,
    committer: GitCommitAuthor,
    message: S.String,
    tree: GitCommitTree,
    parents: GitCommitParentsList,
    verification: GitCommitVerification,
    html_url: S.String,
  }),
).annotate({ identifier: "GitCommit" }) as any as S.Schema<GitCommit>;

export interface CreateRefRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The name of the fully qualified reference (ie: `refs/heads/master`). If it doesn't start with 'refs' and have at least two slashes, it will be rejected. */
  ref: string;
  /** The SHA1 value for this reference. */
  sha: string;
}
export const CreateRefRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    ref: S.String,
    sha: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/repos/{owner}/{repo}/git/refs",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateRefRequest",
}) as any as S.Schema<CreateRefRequest>;

export interface GitRefObject {
  type: string;
  /** SHA for the reference */
  sha: string;
  url: string;
}
export const GitRefObject = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.String,
    sha: S.String,
    url: S.String,
  }),
).annotate({ identifier: "GitRefObject" }) as any as S.Schema<GitRefObject>;

/** Git references within a repository */
export interface GitRef {
  ref: string;
  node_id: string;
  url: string;
  object: GitRefObject;
}
export const GitRef = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ref: S.String,
    node_id: S.String,
    url: S.String,
    object: GitRefObject,
  }),
).annotate({ identifier: "GitRef" }) as any as S.Schema<GitRef>;

/** The type of the object we're tagging. Normally this is a `commit` but it can also be a `tree` or a `blob`. */
export type CreateTagRequestType = "commit" | "tree" | "blob";
export const CreateTagRequestType = /*@__PURE__*/ S.String;

/** An object with information about the individual creating the tag. */
export interface CreateTagRequestTagger {
  /** The name of the author of the tag */
  name: string;
  /** The email of the author of the tag */
  email: string;
  /** When this object was tagged. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. */
  date?: string;
}
export const CreateTagRequestTagger = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    email: S.String,
    date: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateTagRequestTagger",
}) as any as S.Schema<CreateTagRequestTagger>;

export interface CreateTagRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The tag's name. This is typically a version (e.g., "v0.0.1"). */
  tag: string;
  /** The tag message. */
  message: string;
  /** The SHA of the git object this is tagging. */
  object: string;
  /** The type of the object we're tagging. Normally this is a `commit` but it can also be a `tree` or a `blob`. */
  type: CreateTagRequestType | (string & {});
  /** An object with information about the individual creating the tag. */
  tagger?: CreateTagRequestTagger;
}
export const CreateTagRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    tag: S.String,
    message: S.String,
    object: S.String,
    type: CreateTagRequestType,
    tagger: S.optional(CreateTagRequestTagger),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/repos/{owner}/{repo}/git/tags",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateTagRequest",
}) as any as S.Schema<CreateTagRequest>;

export interface GitTagTagger {
  date: string;
  email: string;
  name: string;
}
export const GitTagTagger = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    date: S.String,
    email: S.String,
    name: S.String,
  }),
).annotate({ identifier: "GitTagTagger" }) as any as S.Schema<GitTagTagger>;

export interface GitTagObject {
  sha: string;
  type: string;
  url: string;
}
export const GitTagObject = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sha: S.String,
    type: S.String,
    url: S.String,
  }),
).annotate({ identifier: "GitTagObject" }) as any as S.Schema<GitTagObject>;

export interface Verification {
  verified: boolean;
  reason: string;
  payload: string | null;
  signature: string | null;
  verified_at: string | null;
}
export const Verification = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    verified: S.Boolean,
    reason: S.String,
    payload: S.NullOr(S.String),
    signature: S.NullOr(S.String),
    verified_at: S.NullOr(S.String),
  }),
).annotate({ identifier: "Verification" }) as any as S.Schema<Verification>;

/** Metadata for a Git tag */
export interface GitTag {
  node_id: string;
  /** Name of the tag */
  tag: string;
  sha: string;
  /** URL for the tag */
  url: string;
  /** Message describing the purpose of the tag */
  message: string;
  tagger: GitTagTagger;
  object: GitTagObject;
  verification?: Verification;
}
export const GitTag = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    node_id: S.String,
    tag: S.String,
    sha: S.String,
    url: S.String,
    message: S.String,
    tagger: GitTagTagger,
    object: GitTagObject,
    verification: S.optional(Verification),
  }),
).annotate({ identifier: "GitTag" }) as any as S.Schema<GitTag>;

/** The file mode; one of `100644` for file (blob), `100755` for executable (blob), `040000` for subdirectory (tree), `160000` for submodule (commit), or `120000` for a blob that specifies the path of a symlink. */
export type CreateTreeRequestTreeItemMode =
  | "100644"
  | "100755"
  | "040000"
  | "160000"
  | "120000";
export const CreateTreeRequestTreeItemMode = /*@__PURE__*/ S.String;

/** Either `blob`, `tree`, or `commit`. */
export type CreateTreeRequestTreeItemType = "blob" | "tree" | "commit";
export const CreateTreeRequestTreeItemType = /*@__PURE__*/ S.String;

export interface CreateTreeRequestTreeItem {
  /** The file referenced in the tree. */
  path?: string;
  /** The file mode; one of `100644` for file (blob), `100755` for executable (blob), `040000` for subdirectory (tree), `160000` for submodule (commit), or `120000` for a blob that specifies the path of a symlink. */
  mode?: CreateTreeRequestTreeItemMode | (string & {});
  /** Either `blob`, `tree`, or `commit`. */
  type?: CreateTreeRequestTreeItemType | (string & {});
  /** The SHA1 checksum ID of the object in the tree. Also called `tree.sha`. If the value is `null` then the file will be deleted. **Note:** Use either `tree.sha` or `content` to specify the contents of the entry. Using both `tree.sha` and `content` will return an error. */
  sha?: string | null;
  /** The content you want this file to have. GitHub will write this blob out and use that SHA for this entry. Use either this, or `tree.sha`. **Note:** Use either `tree.sha` or `content` to specify the contents of the entry. Using both `tree.sha` and `content` will return an error. */
  content?: string;
}
export const CreateTreeRequestTreeItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.optional(S.String),
    mode: S.optional(CreateTreeRequestTreeItemMode),
    type: S.optional(CreateTreeRequestTreeItemType),
    sha: S.optional(S.NullOr(S.String)),
    content: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateTreeRequestTreeItem",
}) as any as S.Schema<CreateTreeRequestTreeItem>;

/** Objects (of `path`, `mode`, `type`, and `sha`) specifying a tree structure. */
export type CreateTreeRequestTreeList = Array<CreateTreeRequestTreeItem>;
export const CreateTreeRequestTreeList = /*@__PURE__*/ S.Array(
  CreateTreeRequestTreeItem,
) as any as S.Schema<CreateTreeRequestTreeList>;

export interface CreateTreeRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** Objects (of `path`, `mode`, `type`, and `sha`) specifying a tree structure. */
  tree: CreateTreeRequestTreeList;
  /** The SHA1 of an existing Git tree object which will be used as the base for the new tree. If provided, a new Git tree object will be created from entries in the Git tree object pointed to by `base_tree` and entries defined in the `tree` parameter. Entries defined in the `tree` parameter will overwrite items from `base_tree` with the same `path`. If you're creating new changes on a branch, then normally you'd set `base_tree` to the SHA1 of the Git tree object of the current latest commit on the branch you're working on. If not provided, GitHub will create a new Git tree object from only the entries defined in the `tree` parameter. If you create a new commit pointing to such a tree, then all files which were a part of the parent commit's tree and were not defined in the `tree` parameter will be listed as deleted by the new commit. */
  base_tree?: string;
}
export const CreateTreeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    tree: CreateTreeRequestTreeList,
    base_tree: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/repos/{owner}/{repo}/git/trees",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateTreeRequest",
}) as any as S.Schema<CreateTreeRequest>;

export interface GitTreeTreeItem {
  path: string;
  mode: string;
  type: string;
  sha: string;
  size?: number;
  url?: string;
}
export const GitTreeTreeItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    mode: S.String,
    type: S.String,
    sha: S.String,
    size: S.optional(S.Number),
    url: S.optional(S.String),
  }),
).annotate({
  identifier: "GitTreeTreeItem",
}) as any as S.Schema<GitTreeTreeItem>;

/** Objects specifying a tree structure */
export type GitTreeTreeList = Array<GitTreeTreeItem>;
export const GitTreeTreeList = /*@__PURE__*/ S.Array(
  GitTreeTreeItem,
) as any as S.Schema<GitTreeTreeList>;

/** The hierarchy between files in a Git repository. */
export interface GitTree {
  sha: string;
  url?: string;
  truncated: boolean;
  /** Objects specifying a tree structure */
  tree: GitTreeTreeList;
}
export const GitTree = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sha: S.String,
    url: S.optional(S.String),
    truncated: S.Boolean,
    tree: GitTreeTreeList,
  }),
).annotate({ identifier: "GitTree" }) as any as S.Schema<GitTree>;

export interface DeleteRefRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The Git reference. For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation. */
  ref: string;
}
export const DeleteRefRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    ref: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/repos/{owner}/{repo}/git/refs/{ref}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteRefRequest",
}) as any as S.Schema<DeleteRefRequest>;

export interface DeleteRefResponse {}
export const DeleteRefResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteRefResponse",
}) as any as S.Schema<DeleteRefResponse>;

export interface GetBlobRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  file_sha: string;
}
export const GetBlobRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    file_sha: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/git/blobs/{file_sha}",
      code: 200,
    }),
  ),
).annotate({ identifier: "GetBlobRequest" }) as any as S.Schema<GetBlobRequest>;

/** Blob */
export interface Blob {
  content: string;
  encoding: string;
  url: string;
  sha: string;
  size: number | null;
  node_id: string;
  highlighted_content?: string;
}
export const Blob = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    content: S.String,
    encoding: S.String,
    url: S.String,
    sha: S.String,
    size: S.NullOr(S.Number),
    node_id: S.String,
    highlighted_content: S.optional(S.String),
  }),
).annotate({ identifier: "Blob" }) as any as S.Schema<Blob>;

export interface GetCommitRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The SHA of the commit. */
  commit_sha: string;
}
export const GetCommitRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    commit_sha: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/git/commits/{commit_sha}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetCommitRequest",
}) as any as S.Schema<GetCommitRequest>;

export interface GetRefRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The Git reference. For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation. */
  ref: string;
}
export const GetRefRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    ref: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/git/ref/{ref}",
      code: 200,
    }),
  ),
).annotate({ identifier: "GetRefRequest" }) as any as S.Schema<GetRefRequest>;

export interface GetTagRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  tag_sha: string;
}
export const GetTagRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    tag_sha: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/git/tags/{tag_sha}",
      code: 200,
    }),
  ),
).annotate({ identifier: "GetTagRequest" }) as any as S.Schema<GetTagRequest>;

export interface GetTreeRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The SHA1 value or ref (branch or tag) name of the tree. */
  tree_sha: string;
  /** Setting this parameter to any value returns the objects or subtrees referenced by the tree specified in `:tree_sha`. For example, setting `recursive` to any of the following will enable returning objects or subtrees: `0`, `1`, `"true"`, and `"false"`. Omit this parameter to prevent recursively returning objects or subtrees. */
  recursive?: string;
}
export const GetTreeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    tree_sha: S.String.pipe(T.Label()),
    recursive: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/git/trees/{tree_sha}",
      code: 200,
    }),
  ),
).annotate({ identifier: "GetTreeRequest" }) as any as S.Schema<GetTreeRequest>;

export interface ListMatchingRefsRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The Git reference. For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation. */
  ref: string;
}
export const ListMatchingRefsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    ref: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/git/matching-refs/{ref}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListMatchingRefsRequest",
}) as any as S.Schema<ListMatchingRefsRequest>;

export type ListMatchingRefsResponseBodyList = Array<GitRef>;
export const ListMatchingRefsResponseBodyList = /*@__PURE__*/ S.Array(
  GitRef,
) as any as S.Schema<ListMatchingRefsResponseBodyList>;

export type ListMatchingRefsResponse = ListMatchingRefsResponseBodyList;
export const ListMatchingRefsResponse = /*@__PURE__*/ S.suspend(() =>
  ListMatchingRefsResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListMatchingRefsResponse",
}) as any as S.Schema<ListMatchingRefsResponse>;

export interface UpdateRefRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The Git reference. For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation. */
  ref: string;
  /** The SHA1 value to set this reference to */
  sha: string;
  /** Indicates whether to force the update or to make sure the update is a fast-forward update. Leaving this out or setting it to `false` will make sure you're not overwriting work. */
  force?: boolean;
}
export const UpdateRefRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    ref: S.String.pipe(T.Label()),
    sha: S.String,
    force: S.optional(S.Boolean),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/repos/{owner}/{repo}/git/refs/{ref}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateRefRequest",
}) as any as S.Schema<UpdateRefRequest>;

export type CreateBlobError =
  | Forbidden
  | NotFound
  | Conflict
  | UnprocessableEntity
  | GithubOpError;
/** Create a blob */
export const createBlob: API.OperationMethod<
  CreateBlobRequest,
  ShortBlob,
  CreateBlobError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateBlobRequest,
  output: ShortBlob,
  errors: [Forbidden, NotFound, Conflict, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type CreateCommitError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | GithubOpError;
/** Create a commit Creates a new Git [commit object](https://git-scm.com/book/en/v2/Git-Internals-Git-Objects). **Signature verification object** The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object: These are the possible values for `reason` in the `verification` object: */
export const createCommit: API.OperationMethod<
  CreateCommitRequest,
  GitCommit,
  CreateCommitError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateCommitRequest,
  output: GitCommit,
  errors: [NotFound, Conflict, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type CreateRefError = Conflict | UnprocessableEntity | GithubOpError;
/** Create a reference Creates a reference for your repository. You are unable to create new references for empty repositories, even if the commit SHA-1 hash used exists. Empty repositories are repositories without branches. */
export const createRef: API.OperationMethod<
  CreateRefRequest,
  GitRef,
  CreateRefError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateRefRequest,
  output: GitRef,
  errors: [Conflict, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type CreateTagError = Conflict | UnprocessableEntity | GithubOpError;
/** Create a tag object Note that creating a tag object does not create the reference that makes a tag in Git. If you want to create an annotated tag in Git, you have to do this call to create the tag object, and then [create](https://docs.github.com/rest/git/refs#create-a-reference) the `refs/tags/[tag]` reference. If you want to create a lightweight tag, you only have to [create](https://docs.github.com/rest/git/refs#create-a-reference) the tag reference - this call would be unnecessary. **Signature verification object** The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object: These are the possible values for `reason` in the `verification` object: */
export const createTag: API.OperationMethod<
  CreateTagRequest,
  GitTag,
  CreateTagError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateTagRequest,
  output: GitTag,
  errors: [Conflict, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type CreateTreeError =
  | Forbidden
  | NotFound
  | Conflict
  | UnprocessableEntity
  | GithubOpError;
/** Create a tree The tree creation API accepts nested entries. If you specify both a tree and a nested path modifying that tree, this endpoint will overwrite the contents of the tree with the new path contents, and create a new tree structure. If you use this endpoint to add, delete, or modify the file contents in a tree, you will need to commit the tree and then update a branch to point to the commit. For more information see "[Create a commit](https://docs.github.com/rest/git/commits#create-a-commit)" and "[Update a reference](https://docs.github.com/rest/git/refs#update-a-reference)." Returns an error if you try to delete a file that does not exist. */
export const createTree: API.OperationMethod<
  CreateTreeRequest,
  GitTree,
  CreateTreeError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateTreeRequest,
  output: GitTree,
  errors: [Forbidden, NotFound, Conflict, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type DeleteRefError = Conflict | UnprocessableEntity | GithubOpError;
/** Delete a reference Deletes the provided reference. */
export const deleteRef: API.OperationMethod<
  DeleteRefRequest,
  DeleteRefResponse,
  DeleteRefError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteRefRequest,
  output: DeleteRefResponse,
  errors: [Conflict, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetBlobError =
  | Forbidden
  | NotFound
  | Conflict
  | UnprocessableEntity
  | GithubOpError;
/** Get a blob The `content` in the response will always be Base64 encoded. This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." - **`application/vnd.github.raw+json`**: Returns the raw blob data. - **`application/vnd.github+json`**: Returns a JSON representation of the blob with `content` as a base64 encoded string. This is the default if no media type is specified. **Note** This endpoint supports blobs up to 100 megabytes in size. */
export const getBlob: API.OperationMethod<
  GetBlobRequest,
  Blob,
  GetBlobError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetBlobRequest,
  output: Blob,
  errors: [Forbidden, NotFound, Conflict, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetCommitError = NotFound | Conflict | GithubOpError;
/** Get a commit object Gets a Git [commit object](https://git-scm.com/book/en/v2/Git-Internals-Git-Objects). To get the contents of a commit, see "[Get a commit](/rest/commits/commits#get-a-commit)." **Signature verification object** The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object: These are the possible values for `reason` in the `verification` object: */
export const getCommit: API.OperationMethod<
  GetCommitRequest,
  GitCommit,
  GetCommitError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetCommitRequest,
  output: GitCommit,
  errors: [NotFound, Conflict],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetRefError = NotFound | Conflict | GithubOpError;
/** Get a reference Returns a single reference from your Git database. The `:ref` in the URL must be formatted as `heads/<branch name>` for branches and `tags/<tag name>` for tags. If the `:ref` doesn't match an existing ref, a `404` is returned. > [!NOTE] > You need to explicitly [request a pull request](https://docs.github.com/rest/pulls/pulls#get-a-pull-request) to trigger a test merge commit, which checks the mergeability of pull requests. For more information, see "[Checking mergeability of pull requests](https://docs.github.com/rest/guides/getting-started-with-the-git-database-api#checking-mergeability-of-pull-requests)". */
export const getRef: API.OperationMethod<
  GetRefRequest,
  GitRef,
  GetRefError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetRefRequest,
  output: GitRef,
  errors: [NotFound, Conflict],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetTagError = NotFound | Conflict | GithubOpError;
/** Get a tag **Signature verification object** The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object: These are the possible values for `reason` in the `verification` object: */
export const getTag: API.OperationMethod<
  GetTagRequest,
  GitTag,
  GetTagError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetTagRequest,
  output: GitTag,
  errors: [NotFound, Conflict],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetTreeError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | GithubOpError;
/** Get a tree Returns a single tree using the SHA1 value or ref name for that tree. If `truncated` is `true` in the response then the number of items in the `tree` array exceeded our maximum limit. If you need to fetch more items, use the non-recursive method of fetching trees, and fetch one sub-tree at a time. > [!NOTE] > The limit for the `tree` array is 100,000 entries with a maximum size of 7 MB when using the `recursive` parameter. */
export const getTree: API.OperationMethod<
  GetTreeRequest,
  GitTree,
  GetTreeError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetTreeRequest,
  output: GitTree,
  errors: [NotFound, Conflict, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListMatchingRefsError = Conflict | GithubOpError;
/** List matching references Returns an array of references from your Git database that match the supplied name. The `:ref` in the URL must be formatted as `heads/<branch name>` for branches and `tags/<tag name>` for tags. If the `:ref` doesn't exist in the repository, but existing refs start with `:ref`, they will be returned as an array. When you use this endpoint without providing a `:ref`, it will return an array of all the references from your Git database, including notes and stashes if they exist on the server. Anything in the namespace is returned, not just `heads` and `tags`. > [!NOTE] > You need to explicitly [request a pull request](https://docs.github.com/rest/pulls/pulls#get-a-pull-request) to trigger a test merge commit, which checks the mergeability of pull requests. For more information, see "[Checking mergeability of pull requests](https://docs.github.com/rest/guides/getting-started-with-the-git-database-api#checking-mergeability-of-pull-requests)". If you request matching references for a branch named `feature` but the branch `feature` doesn't exist, the response can still include other matching head refs that start with the word `feature`, such as `featureA` and `featureB`. */
export const listMatchingRefs: API.OperationMethod<
  ListMatchingRefsRequest,
  ListMatchingRefsResponse,
  ListMatchingRefsError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListMatchingRefsRequest,
  output: ListMatchingRefsResponse,
  errors: [Conflict],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type UpdateRefError = Conflict | UnprocessableEntity | GithubOpError;
/** Update a reference Updates the provided reference to point to a new SHA. For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation. */
export const updateRef: API.OperationMethod<
  UpdateRefRequest,
  GitRef,
  UpdateRefError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateRefRequest,
  output: GitRef,
  errors: [Conflict, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));
