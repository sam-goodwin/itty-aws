// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/rest-api-description). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  GithubProtocol,
  type GithubOpError,
  type GithubOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { GithubOpError, GithubOpContext };

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** The type of actor (user or team). */
export type AddCollaboratorForOrgRequestActorType = "User" | "Team";
export const AddCollaboratorForOrgRequestActorType = /*@__PURE__*/ S.String;

/** The role to grant to the collaborator. */
export type AddCollaboratorForOrgRequestRole = "reader" | "writer" | "admin";
export const AddCollaboratorForOrgRequestRole = /*@__PURE__*/ S.String;

export interface AddCollaboratorForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The type of actor (user or team). */
  actor_type: AddCollaboratorForOrgRequestActorType | (string & {});
  /** The username (for users) or team slug (for teams). The numeric ID of a user or team is also accepted. */
  actor_identifier: string;
  /** The role to grant to the collaborator. */
  role: AddCollaboratorForOrgRequestRole | (string & {});
}
export const AddCollaboratorForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    actor_type: AddCollaboratorForOrgRequestActorType,
    actor_identifier: S.String,
    role: AddCollaboratorForOrgRequestRole,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/orgs/{org}/copilot-spaces/{space_number}/collaborators",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AddCollaboratorForOrgRequest",
}) as any as S.Schema<AddCollaboratorForOrgRequest>;

/** The collaborator actor type. */
export type CopilotSpaceCollaboratorCase0ActorType = "User";
export const CopilotSpaceCollaboratorCase0ActorType = /*@__PURE__*/ S.String;

/** The role granted to the collaborator */
export type CopilotSpaceCollaboratorCase0Role = "reader" | "writer" | "admin";
export const CopilotSpaceCollaboratorCase0Role = /*@__PURE__*/ S.String;

export interface CopilotSpaceCollaboratorCase0 {
  name?: string | null;
  email?: string | null;
  login: string;
  id: number;
  node_id: string;
  avatar_url: string;
  gravatar_id: string | null;
  url: string;
  html_url: string;
  followers_url: string;
  following_url: string;
  gists_url: string;
  starred_url: string;
  subscriptions_url: string;
  organizations_url: string;
  repos_url: string;
  events_url: string;
  received_events_url: string;
  type: string;
  site_admin: boolean;
  starred_at?: string;
  user_view_type?: string;
  /** The collaborator actor type. */
  actor_type: CopilotSpaceCollaboratorCase0ActorType;
  /** The role granted to the collaborator */
  role: CopilotSpaceCollaboratorCase0Role;
}
export const CopilotSpaceCollaboratorCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.NullOr(S.String)),
    email: S.optional(S.NullOr(S.String)),
    login: S.String,
    id: S.Number,
    node_id: S.String,
    avatar_url: S.String,
    gravatar_id: S.NullOr(S.String),
    url: S.String,
    html_url: S.String,
    followers_url: S.String,
    following_url: S.String,
    gists_url: S.String,
    starred_url: S.String,
    subscriptions_url: S.String,
    organizations_url: S.String,
    repos_url: S.String,
    events_url: S.String,
    received_events_url: S.String,
    type: S.String,
    site_admin: S.Boolean,
    starred_at: S.optional(S.String),
    user_view_type: S.optional(S.String),
    actor_type: CopilotSpaceCollaboratorCase0ActorType,
    role: CopilotSpaceCollaboratorCase0Role,
  }),
).annotate({
  identifier: "CopilotSpaceCollaboratorCase0",
}) as any as S.Schema<CopilotSpaceCollaboratorCase0>;

/** The collaborator actor type. */
export type CopilotSpaceCollaboratorCase1ActorType = "Team";
export const CopilotSpaceCollaboratorCase1ActorType = /*@__PURE__*/ S.String;

/** The role granted to the collaborator */
export type CopilotSpaceCollaboratorCase1Role = "reader" | "writer" | "admin";
export const CopilotSpaceCollaboratorCase1Role = /*@__PURE__*/ S.String;

export type CopilotSpaceCollaboratorCase1Type = "Team";
export const CopilotSpaceCollaboratorCase1Type = /*@__PURE__*/ S.String;

export interface CopilotSpaceCollaboratorCase1 {
  /** The collaborator actor type. */
  actor_type: CopilotSpaceCollaboratorCase1ActorType;
  /** The role granted to the collaborator */
  role: CopilotSpaceCollaboratorCase1Role;
  id: number;
  node_id: string;
  name: string;
  slug: string;
  type: CopilotSpaceCollaboratorCase1Type;
  description?: string | null;
  privacy?: string;
  notification_setting?: string;
  url?: string;
  html_url?: string;
  members_url?: string;
  repositories_url?: string;
  organization_id?: number;
  parent?: unknown | null;
}
export const CopilotSpaceCollaboratorCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    actor_type: CopilotSpaceCollaboratorCase1ActorType,
    role: CopilotSpaceCollaboratorCase1Role,
    id: S.Number,
    node_id: S.String,
    name: S.String,
    slug: S.String,
    type: CopilotSpaceCollaboratorCase1Type,
    description: S.optional(S.NullOr(S.String)),
    privacy: S.optional(S.String),
    notification_setting: S.optional(S.String),
    url: S.optional(S.String),
    html_url: S.optional(S.String),
    members_url: S.optional(S.String),
    repositories_url: S.optional(S.String),
    organization_id: S.optional(S.Number),
    parent: S.optional(S.NullOr(S.Unknown)),
  }),
).annotate({
  identifier: "CopilotSpaceCollaboratorCase1",
}) as any as S.Schema<CopilotSpaceCollaboratorCase1>;

/** A collaborator (user or team) of a Copilot Space */
export type CopilotSpaceCollaborator =
  | CopilotSpaceCollaboratorCase0
  | CopilotSpaceCollaboratorCase1;
export const CopilotSpaceCollaborator =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CopilotSpaceCollaborator>;

export type AddCollaboratorForOrgResponse = CopilotSpaceCollaborator;
export const AddCollaboratorForOrgResponse = /*@__PURE__*/ S.suspend(() =>
  CopilotSpaceCollaborator.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "AddCollaboratorForOrgResponse",
}) as any as S.Schema<AddCollaboratorForOrgResponse>;

/** The type of actor (must be `User` for user-owned spaces; `Team` will be rejected). */
export type AddCollaboratorForUserRequestActorType = "User" | "Team";
export const AddCollaboratorForUserRequestActorType = /*@__PURE__*/ S.String;

/** The role to grant to the collaborator. */
export type AddCollaboratorForUserRequestRole = "reader" | "writer" | "admin";
export const AddCollaboratorForUserRequestRole = /*@__PURE__*/ S.String;

export interface AddCollaboratorForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The type of actor (must be `User` for user-owned spaces; `Team` will be rejected). */
  actor_type: AddCollaboratorForUserRequestActorType | (string & {});
  /** The username of the collaborator. The numeric user ID is also accepted. */
  actor_identifier: string;
  /** The role to grant to the collaborator. */
  role: AddCollaboratorForUserRequestRole | (string & {});
}
export const AddCollaboratorForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    actor_type: AddCollaboratorForUserRequestActorType,
    actor_identifier: S.String,
    role: AddCollaboratorForUserRequestRole,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/users/{username}/copilot-spaces/{space_number}/collaborators",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AddCollaboratorForUserRequest",
}) as any as S.Schema<AddCollaboratorForUserRequest>;

export type AddCollaboratorForUserResponse = CopilotSpaceCollaborator;
export const AddCollaboratorForUserResponse = /*@__PURE__*/ S.suspend(() =>
  CopilotSpaceCollaborator.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "AddCollaboratorForUserResponse",
}) as any as S.Schema<AddCollaboratorForUserResponse>;

/** The base role that determines default permissions for organization members. - `no_access`: No default access (default) - `reader`: Organization members can read the space - `writer`: Organization members can read and edit the space - `admin`: Organization members have full admin access to the space */
export type CreateForOrgRequestBaseRole =
  | "reader"
  | "writer"
  | "admin"
  | "no_access";
export const CreateForOrgRequestBaseRole = /*@__PURE__*/ S.String;

/** The type of resource. */
export type CreateForOrgRequestResourcesAttributesItemResourceType =
  | "repository"
  | "github_file"
  | "free_text"
  | "github_issue"
  | "github_pull_request"
  | "media_content"
  | "uploaded_text_file";
export const CreateForOrgRequestResourcesAttributesItemResourceType =
  /*@__PURE__*/ S.String;

/** Metadata specific to the resource type. */
export interface CreateForOrgRequestResourcesAttributesItemMetadata {
  /** Repository ID for repository or file resources. */
  repository_id?: number;
  /** File path for file resources. */
  file_path?: string;
  /** Text content for free text resources. */
  text?: string;
  /** Name for the resource. */
  name?: string;
  /** Issue or PR number. */
  number?: number;
}
export const CreateForOrgRequestResourcesAttributesItemMetadata =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      repository_id: S.optional(S.Number),
      file_path: S.optional(S.String),
      text: S.optional(S.String),
      name: S.optional(S.String),
      number: S.optional(S.Number),
    }),
  ).annotate({
    identifier: "CreateForOrgRequestResourcesAttributesItemMetadata",
  }) as any as S.Schema<CreateForOrgRequestResourcesAttributesItemMetadata>;

export interface CreateForOrgRequestResourcesAttributesItem {
  /** The type of resource. */
  resource_type?:
    | CreateForOrgRequestResourcesAttributesItemResourceType
    | (string & {});
  /** Metadata specific to the resource type. */
  metadata?: CreateForOrgRequestResourcesAttributesItemMetadata;
}
export const CreateForOrgRequestResourcesAttributesItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      resource_type: S.optional(
        CreateForOrgRequestResourcesAttributesItemResourceType,
      ),
      metadata: S.optional(CreateForOrgRequestResourcesAttributesItemMetadata),
    }),
  ).annotate({
    identifier: "CreateForOrgRequestResourcesAttributesItem",
  }) as any as S.Schema<CreateForOrgRequestResourcesAttributesItem>;

/** Resources to attach to the space. */
export type CreateForOrgRequestResourcesAttributesList =
  Array<CreateForOrgRequestResourcesAttributesItem>;
export const CreateForOrgRequestResourcesAttributesList = /*@__PURE__*/ S.Array(
  CreateForOrgRequestResourcesAttributesItem,
) as any as S.Schema<CreateForOrgRequestResourcesAttributesList>;

export interface CreateForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The name of the Copilot Space. */
  name: string;
  /** A description of the Copilot Space. */
  description?: string;
  /** General instructions for the Copilot Space. */
  general_instructions?: string;
  /** The base role that determines default permissions for organization members. - `no_access`: No default access (default) - `reader`: Organization members can read the space - `writer`: Organization members can read and edit the space - `admin`: Organization members have full admin access to the space */
  base_role?: CreateForOrgRequestBaseRole | (string & {});
  /** Resources to attach to the space. */
  resources_attributes?: CreateForOrgRequestResourcesAttributesList;
}
export const CreateForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    name: S.String,
    description: S.optional(S.String),
    general_instructions: S.optional(S.String),
    base_role: S.optional(CreateForOrgRequestBaseRole),
    resources_attributes: S.optional(
      CreateForOrgRequestResourcesAttributesList,
    ),
  }).pipe(
    T.Http({ method: "POST", uri: "/orgs/{org}/copilot-spaces", code: 200 }),
  ),
).annotate({
  identifier: "CreateForOrgRequest",
}) as any as S.Schema<CreateForOrgRequest>;

/** The base role that determines default permissions. - `no_access`: No default access - `reader`: Default read permissions - `writer`: Default write permissions (organization spaces only) - `admin`: Default admin permissions (organization spaces only) */
export type CopilotSpaceBaseRole = "reader" | "writer" | "admin" | "no_access";
export const CopilotSpaceBaseRole = /*@__PURE__*/ S.String;

/** A GitHub user. */
export interface SimpleUser {
  name?: string | null;
  email?: string | null;
  login: string;
  id: number;
  node_id: string;
  avatar_url: string;
  gravatar_id: string | null;
  url: string;
  html_url: string;
  followers_url: string;
  following_url: string;
  gists_url: string;
  starred_url: string;
  subscriptions_url: string;
  organizations_url: string;
  repos_url: string;
  events_url: string;
  received_events_url: string;
  type: string;
  site_admin: boolean;
  starred_at?: string;
  user_view_type?: string;
}
export const SimpleUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.NullOr(S.String)),
    email: S.optional(S.NullOr(S.String)),
    login: S.String,
    id: S.Number,
    node_id: S.String,
    avatar_url: S.String,
    gravatar_id: S.NullOr(S.String),
    url: S.String,
    html_url: S.String,
    followers_url: S.String,
    following_url: S.String,
    gists_url: S.String,
    starred_url: S.String,
    subscriptions_url: S.String,
    organizations_url: S.String,
    repos_url: S.String,
    events_url: S.String,
    received_events_url: S.String,
    type: S.String,
    site_admin: S.Boolean,
    starred_at: S.optional(S.String),
    user_view_type: S.optional(S.String),
  }),
).annotate({ identifier: "SimpleUser" }) as any as S.Schema<SimpleUser>;

/** A GitHub organization. */
export interface OrganizationSimple {
  login: string;
  id: number;
  node_id: string;
  url: string;
  repos_url: string;
  events_url: string;
  hooks_url: string;
  issues_url: string;
  members_url: string;
  public_members_url: string;
  avatar_url: string;
  description: string | null;
}
export const OrganizationSimple = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    login: S.String,
    id: S.Number,
    node_id: S.String,
    url: S.String,
    repos_url: S.String,
    events_url: S.String,
    hooks_url: S.String,
    issues_url: S.String,
    members_url: S.String,
    public_members_url: S.String,
    avatar_url: S.String,
    description: S.NullOr(S.String),
  }),
).annotate({
  identifier: "OrganizationSimple",
}) as any as S.Schema<OrganizationSimple>;

/** The user or organization that owns this space. */
export type CopilotSpaceOwner = SimpleUser | OrganizationSimple;
export const CopilotSpaceOwner =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CopilotSpaceOwner>;

/** The type of resource. */
export type CopilotSpaceResourcesAttributesItemResourceType =
  | "repository"
  | "github_file"
  | "free_text"
  | "github_issue"
  | "github_pull_request"
  | "media_content"
  | "uploaded_text_file";
export const CopilotSpaceResourcesAttributesItemResourceType =
  /*@__PURE__*/ S.String;

/** Metadata specific to the resource type. */
export interface CopilotSpaceResourcesAttributesItemMetadata {
  /** Repository ID for repository or file resources. */
  repository_id?: number;
  /** File path for file resources. */
  file_path?: string;
  /** Text content for free text resources. */
  text?: string;
  /** Name for the resource. */
  name?: string;
  /** Issue or PR number. */
  number?: number;
  /** Chat attachment ID for uploaded files or media. */
  copilot_chat_attachment_id?: number;
  /** Media type for media content resources. */
  media_type?: string;
  /** URL for media content resources. */
  url?: string;
  /** Height for media content resources. */
  height?: number;
  /** Width for media content resources. */
  width?: number;
}
export const CopilotSpaceResourcesAttributesItemMetadata =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      repository_id: S.optional(S.Number),
      file_path: S.optional(S.String),
      text: S.optional(S.String),
      name: S.optional(S.String),
      number: S.optional(S.Number),
      copilot_chat_attachment_id: S.optional(S.Number),
      media_type: S.optional(S.String),
      url: S.optional(S.String),
      height: S.optional(S.Number),
      width: S.optional(S.Number),
    }),
  ).annotate({
    identifier: "CopilotSpaceResourcesAttributesItemMetadata",
  }) as any as S.Schema<CopilotSpaceResourcesAttributesItemMetadata>;

export interface CopilotSpaceResourcesAttributesItem {
  /** The unique identifier of the resource. */
  id?: number;
  /** The type of resource. */
  resource_type?: CopilotSpaceResourcesAttributesItemResourceType;
  /** The unique identifier of the chat attachment for uploaded files or media content. */
  copilot_chat_attachment_id?: number | null;
  /** The date and time the resource was created. */
  created_at?: string;
  /** The date and time the resource was last updated. */
  updated_at?: string;
  /** Metadata specific to the resource type. */
  metadata?: CopilotSpaceResourcesAttributesItemMetadata;
}
export const CopilotSpaceResourcesAttributesItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.Number),
    resource_type: S.optional(CopilotSpaceResourcesAttributesItemResourceType),
    copilot_chat_attachment_id: S.optional(S.NullOr(S.Number)),
    created_at: S.optional(S.String),
    updated_at: S.optional(S.String),
    metadata: S.optional(CopilotSpaceResourcesAttributesItemMetadata),
  }),
).annotate({
  identifier: "CopilotSpaceResourcesAttributesItem",
}) as any as S.Schema<CopilotSpaceResourcesAttributesItem>;

/** Resources attached to the space. */
export type CopilotSpaceResourcesAttributesList =
  Array<CopilotSpaceResourcesAttributesItem>;
export const CopilotSpaceResourcesAttributesList = /*@__PURE__*/ S.Array(
  CopilotSpaceResourcesAttributesItem,
) as any as S.Schema<CopilotSpaceResourcesAttributesList>;

/** A GitHub Copilot Space represents an interactive AI workspace where users can ask questions and get assistance. */
export interface CopilotSpace {
  /** The unique identifier of the space. */
  id: number;
  /** The number that identifies the space within its owner. */
  number: number;
  /** The display name of the space. */
  name: string;
  /** A description of the space. */
  description?: string | null;
  /** General instructions for the Copilot Space. */
  general_instructions?: string | null;
  /** The base role that determines default permissions. - `no_access`: No default access - `reader`: Default read permissions - `writer`: Default write permissions (organization spaces only) - `admin`: Default admin permissions (organization spaces only) */
  base_role: CopilotSpaceBaseRole;
  /** The user or organization that owns this space. */
  owner: CopilotSpaceOwner;
  creator: SimpleUser;
  /** The date and time the space was created. */
  created_at: string;
  /** The date and time the space was last updated. */
  updated_at: string;
  /** The HTML URL of the space. */
  html_url: string;
  /** The API URL of the space. */
  api_url: string;
  /** Resources attached to the space. */
  resources_attributes?: CopilotSpaceResourcesAttributesList;
}
export const CopilotSpace = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    number: S.Number,
    name: S.String,
    description: S.optional(S.NullOr(S.String)),
    general_instructions: S.optional(S.NullOr(S.String)),
    base_role: CopilotSpaceBaseRole,
    owner: CopilotSpaceOwner,
    creator: SimpleUser,
    created_at: S.String,
    updated_at: S.String,
    html_url: S.String,
    api_url: S.String,
    resources_attributes: S.optional(CopilotSpaceResourcesAttributesList),
  }),
).annotate({ identifier: "CopilotSpace" }) as any as S.Schema<CopilotSpace>;

/** The base role that determines default permissions for the space. - `no_access`: No default access (default) - `reader`: Makes the space publicly readable Note: User spaces do not support writer or admin base roles. */
export type CreateForUserRequestBaseRole = "reader" | "no_access";
export const CreateForUserRequestBaseRole = /*@__PURE__*/ S.String;

/** The type of resource. */
export type CreateForUserRequestResourcesAttributesItemResourceType =
  | "repository"
  | "github_file"
  | "free_text"
  | "github_issue"
  | "github_pull_request"
  | "media_content"
  | "uploaded_text_file";
export const CreateForUserRequestResourcesAttributesItemResourceType =
  /*@__PURE__*/ S.String;

/** Metadata specific to the resource type. */
export type CreateForUserRequestResourcesAttributesItemMetadata =
  CreateForOrgRequestResourcesAttributesItemMetadata;
export const CreateForUserRequestResourcesAttributesItemMetadata =
  CreateForOrgRequestResourcesAttributesItemMetadata;

export interface CreateForUserRequestResourcesAttributesItem {
  /** The type of resource. */
  resource_type?:
    | CreateForUserRequestResourcesAttributesItemResourceType
    | (string & {});
  /** Metadata specific to the resource type. */
  metadata?: CreateForOrgRequestResourcesAttributesItemMetadata;
}
export const CreateForUserRequestResourcesAttributesItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      resource_type: S.optional(
        CreateForUserRequestResourcesAttributesItemResourceType,
      ),
      metadata: S.optional(CreateForOrgRequestResourcesAttributesItemMetadata),
    }),
  ).annotate({
    identifier: "CreateForUserRequestResourcesAttributesItem",
  }) as any as S.Schema<CreateForUserRequestResourcesAttributesItem>;

/** Resources to attach to the space. */
export type CreateForUserRequestResourcesAttributesList =
  Array<CreateForUserRequestResourcesAttributesItem>;
export const CreateForUserRequestResourcesAttributesList =
  /*@__PURE__*/ S.Array(
    CreateForUserRequestResourcesAttributesItem,
  ) as any as S.Schema<CreateForUserRequestResourcesAttributesList>;

export interface CreateForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The name of the Copilot Space. */
  name: string;
  /** A description of the Copilot Space. */
  description?: string;
  /** General instructions for the Copilot Space. */
  general_instructions?: string;
  /** The base role that determines default permissions for the space. - `no_access`: No default access (default) - `reader`: Makes the space publicly readable Note: User spaces do not support writer or admin base roles. */
  base_role?: CreateForUserRequestBaseRole | (string & {});
  /** Resources to attach to the space. */
  resources_attributes?: CreateForUserRequestResourcesAttributesList;
}
export const CreateForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    name: S.String,
    description: S.optional(S.String),
    general_instructions: S.optional(S.String),
    base_role: S.optional(CreateForUserRequestBaseRole),
    resources_attributes: S.optional(
      CreateForUserRequestResourcesAttributesList,
    ),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/users/{username}/copilot-spaces",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateForUserRequest",
}) as any as S.Schema<CreateForUserRequest>;

/** The type of resource to create. */
export type CreateResourceForOrgRequestResourceType =
  | "repository"
  | "github_file"
  | "free_text"
  | "github_issue"
  | "github_pull_request";
export const CreateResourceForOrgRequestResourceType = /*@__PURE__*/ S.String;

/** Resource-specific metadata. */
export type CreateResourceForOrgRequestMetadataMap = {
  [key: string]: unknown | undefined;
};
export const CreateResourceForOrgRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateResourceForOrgRequestMetadataMap>;

export interface CreateResourceForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The type of resource to create. */
  resource_type: CreateResourceForOrgRequestResourceType | (string & {});
  /** Resource-specific metadata. */
  metadata: CreateResourceForOrgRequestMetadataMap;
}
export const CreateResourceForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    resource_type: CreateResourceForOrgRequestResourceType,
    metadata: CreateResourceForOrgRequestMetadataMap,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/orgs/{org}/copilot-spaces/{space_number}/resources",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateResourceForOrgRequest",
}) as any as S.Schema<CreateResourceForOrgRequest>;

/** The type of the resource. */
export type CopilotSpaceResourceResourceType =
  | "repository"
  | "github_file"
  | "free_text"
  | "github_issue"
  | "github_pull_request"
  | "media_content"
  | "uploaded_text_file";
export const CopilotSpaceResourceResourceType = /*@__PURE__*/ S.String;

/** Resource-specific metadata. The keys and values depend on the resource type. */
export type CopilotSpaceResourceMetadataMap = {
  [key: string]: unknown | undefined;
};
export const CopilotSpaceResourceMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CopilotSpaceResourceMetadataMap>;

/** A resource attached to a Copilot Space. */
export interface CopilotSpaceResource {
  /** The unique identifier of the resource. */
  id: number;
  /** The type of the resource. */
  resource_type: CopilotSpaceResourceResourceType;
  /** The ID of the associated chat attachment, if any. */
  copilot_chat_attachment_id?: number | null;
  /** Resource-specific metadata. The keys and values depend on the resource type. */
  metadata: CopilotSpaceResourceMetadataMap;
  /** The date and time the resource was created. */
  created_at: string;
  /** The date and time the resource was last updated. */
  updated_at: string;
}
export const CopilotSpaceResource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    resource_type: CopilotSpaceResourceResourceType,
    copilot_chat_attachment_id: S.optional(S.NullOr(S.Number)),
    metadata: CopilotSpaceResourceMetadataMap,
    created_at: S.String,
    updated_at: S.String,
  }),
).annotate({
  identifier: "CopilotSpaceResource",
}) as any as S.Schema<CopilotSpaceResource>;

/** The type of resource to create. */
export type CreateResourceForUserRequestResourceType =
  | "repository"
  | "github_file"
  | "free_text"
  | "github_issue"
  | "github_pull_request";
export const CreateResourceForUserRequestResourceType = /*@__PURE__*/ S.String;

/** Resource-specific metadata. */
export type CreateResourceForUserRequestMetadataMap = {
  [key: string]: unknown | undefined;
};
export const CreateResourceForUserRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<CreateResourceForUserRequestMetadataMap>;

export interface CreateResourceForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The type of resource to create. */
  resource_type: CreateResourceForUserRequestResourceType | (string & {});
  /** Resource-specific metadata. */
  metadata: CreateResourceForUserRequestMetadataMap;
}
export const CreateResourceForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    resource_type: CreateResourceForUserRequestResourceType,
    metadata: CreateResourceForUserRequestMetadataMap,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/users/{username}/copilot-spaces/{space_number}/resources",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateResourceForUserRequest",
}) as any as S.Schema<CreateResourceForUserRequest>;

export interface DeleteForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
}
export const DeleteForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/orgs/{org}/copilot-spaces/{space_number}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteForOrgRequest",
}) as any as S.Schema<DeleteForOrgRequest>;

export interface DeleteForOrgResponse {}
export const DeleteForOrgResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteForOrgResponse",
}) as any as S.Schema<DeleteForOrgResponse>;

export interface DeleteForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
}
export const DeleteForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/users/{username}/copilot-spaces/{space_number}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteForUserRequest",
}) as any as S.Schema<DeleteForUserRequest>;

export interface DeleteForUserResponse {}
export const DeleteForUserResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteForUserResponse",
}) as any as S.Schema<DeleteForUserResponse>;

export interface DeleteResourceForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The unique identifier of the resource. */
  space_resource_id: number;
}
export const DeleteResourceForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    space_resource_id: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteResourceForOrgRequest",
}) as any as S.Schema<DeleteResourceForOrgRequest>;

export interface DeleteResourceForOrgResponse {}
export const DeleteResourceForOrgResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteResourceForOrgResponse",
}) as any as S.Schema<DeleteResourceForOrgResponse>;

export interface DeleteResourceForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The unique identifier of the resource. */
  space_resource_id: number;
}
export const DeleteResourceForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    space_resource_id: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteResourceForUserRequest",
}) as any as S.Schema<DeleteResourceForUserRequest>;

export interface DeleteResourceForUserResponse {}
export const DeleteResourceForUserResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteResourceForUserResponse",
}) as any as S.Schema<DeleteResourceForUserResponse>;

export interface GetForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
}
export const GetForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/orgs/{org}/copilot-spaces/{space_number}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetForOrgRequest",
}) as any as S.Schema<GetForOrgRequest>;

export interface GetForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
}
export const GetForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/users/{username}/copilot-spaces/{space_number}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetForUserRequest",
}) as any as S.Schema<GetForUserRequest>;

export interface GetResourceForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The unique identifier of the resource. */
  space_resource_id: number;
}
export const GetResourceForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    space_resource_id: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetResourceForOrgRequest",
}) as any as S.Schema<GetResourceForOrgRequest>;

export interface GetResourceForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The unique identifier of the resource. */
  space_resource_id: number;
}
export const GetResourceForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    space_resource_id: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetResourceForUserRequest",
}) as any as S.Schema<GetResourceForUserRequest>;

export interface ListCollaboratorsForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
}
export const ListCollaboratorsForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/orgs/{org}/copilot-spaces/{space_number}/collaborators",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListCollaboratorsForOrgRequest",
}) as any as S.Schema<ListCollaboratorsForOrgRequest>;

/** The list of collaborators for this Copilot Space. */
export type ListCollaboratorsForOrgResponseCollaboratorsList =
  Array<CopilotSpaceCollaborator>;
export const ListCollaboratorsForOrgResponseCollaboratorsList =
  /*@__PURE__*/ S.Array(
    CopilotSpaceCollaborator,
  ) as any as S.Schema<ListCollaboratorsForOrgResponseCollaboratorsList>;

export interface ListCollaboratorsForOrgResponse {
  /** The list of collaborators for this Copilot Space. */
  collaborators: ListCollaboratorsForOrgResponseCollaboratorsList;
}
export const ListCollaboratorsForOrgResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    collaborators: ListCollaboratorsForOrgResponseCollaboratorsList,
  }),
).annotate({
  identifier: "ListCollaboratorsForOrgResponse",
}) as any as S.Schema<ListCollaboratorsForOrgResponse>;

export interface ListCollaboratorsForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
}
export const ListCollaboratorsForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/users/{username}/copilot-spaces/{space_number}/collaborators",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListCollaboratorsForUserRequest",
}) as any as S.Schema<ListCollaboratorsForUserRequest>;

/** The list of collaborators for this Copilot Space. */
export type ListCollaboratorsForUserResponseCollaboratorsList =
  Array<CopilotSpaceCollaborator>;
export const ListCollaboratorsForUserResponseCollaboratorsList =
  /*@__PURE__*/ S.Array(
    CopilotSpaceCollaborator,
  ) as any as S.Schema<ListCollaboratorsForUserResponseCollaboratorsList>;

export interface ListCollaboratorsForUserResponse {
  /** The list of collaborators for this Copilot Space. */
  collaborators: ListCollaboratorsForUserResponseCollaboratorsList;
}
export const ListCollaboratorsForUserResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    collaborators: ListCollaboratorsForUserResponseCollaboratorsList,
  }),
).annotate({
  identifier: "ListCollaboratorsForUserResponse",
}) as any as S.Schema<ListCollaboratorsForUserResponse>;

export interface ListForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The number of results per page (max 100). */
  per_page?: number;
  /** A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. */
  before?: string;
  /** A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. */
  after?: string;
}
export const ListForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    per_page: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/orgs/{org}/copilot-spaces", code: 200 }),
  ),
).annotate({
  identifier: "ListForOrgRequest",
}) as any as S.Schema<ListForOrgRequest>;

/** The list of Copilot Spaces on this page of results. */
export type ListForOrgResponseSpacesList = Array<CopilotSpace>;
export const ListForOrgResponseSpacesList = /*@__PURE__*/ S.Array(
  CopilotSpace,
) as any as S.Schema<ListForOrgResponseSpacesList>;

export interface ListForOrgResponse {
  /** The list of Copilot Spaces on this page of results. */
  spaces: ListForOrgResponseSpacesList;
}
export const ListForOrgResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    spaces: ListForOrgResponseSpacesList,
  }),
).annotate({
  identifier: "ListForOrgResponse",
}) as any as S.Schema<ListForOrgResponse>;

export interface ListForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The number of results per page (max 100). */
  per_page?: number;
  /** A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. */
  before?: string;
  /** A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. */
  after?: string;
}
export const ListForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    per_page: S.optional(S.Number.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    after: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/users/{username}/copilot-spaces",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListForUserRequest",
}) as any as S.Schema<ListForUserRequest>;

/** The list of Copilot Spaces on this page of results. */
export type ListForUserResponseSpacesList = Array<CopilotSpace>;
export const ListForUserResponseSpacesList = /*@__PURE__*/ S.Array(
  CopilotSpace,
) as any as S.Schema<ListForUserResponseSpacesList>;

export interface ListForUserResponse {
  /** The list of Copilot Spaces on this page of results. */
  spaces: ListForUserResponseSpacesList;
}
export const ListForUserResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    spaces: ListForUserResponseSpacesList,
  }),
).annotate({
  identifier: "ListForUserResponse",
}) as any as S.Schema<ListForUserResponse>;

export interface ListResourcesForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
}
export const ListResourcesForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/orgs/{org}/copilot-spaces/{space_number}/resources",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListResourcesForOrgRequest",
}) as any as S.Schema<ListResourcesForOrgRequest>;

/** The list of resources attached to this Copilot Space. */
export type ListResourcesForOrgResponseResourcesList =
  Array<CopilotSpaceResource>;
export const ListResourcesForOrgResponseResourcesList = /*@__PURE__*/ S.Array(
  CopilotSpaceResource,
) as any as S.Schema<ListResourcesForOrgResponseResourcesList>;

export interface ListResourcesForOrgResponse {
  /** The list of resources attached to this Copilot Space. */
  resources: ListResourcesForOrgResponseResourcesList;
}
export const ListResourcesForOrgResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    resources: ListResourcesForOrgResponseResourcesList,
  }),
).annotate({
  identifier: "ListResourcesForOrgResponse",
}) as any as S.Schema<ListResourcesForOrgResponse>;

export interface ListResourcesForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
}
export const ListResourcesForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/users/{username}/copilot-spaces/{space_number}/resources",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListResourcesForUserRequest",
}) as any as S.Schema<ListResourcesForUserRequest>;

/** The list of resources attached to this Copilot Space. */
export type ListResourcesForUserResponseResourcesList =
  Array<CopilotSpaceResource>;
export const ListResourcesForUserResponseResourcesList = /*@__PURE__*/ S.Array(
  CopilotSpaceResource,
) as any as S.Schema<ListResourcesForUserResponseResourcesList>;

export interface ListResourcesForUserResponse {
  /** The list of resources attached to this Copilot Space. */
  resources: ListResourcesForUserResponseResourcesList;
}
export const ListResourcesForUserResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    resources: ListResourcesForUserResponseResourcesList,
  }),
).annotate({
  identifier: "ListResourcesForUserResponse",
}) as any as S.Schema<ListResourcesForUserResponse>;

export type RemoveCollaboratorForOrgRequestActorType = "User" | "Team";
export const RemoveCollaboratorForOrgRequestActorType = /*@__PURE__*/ S.String;

export interface RemoveCollaboratorForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The type of actor (user or team). */
  actor_type: RemoveCollaboratorForOrgRequestActorType | (string & {});
  /** The username (for users) or team slug (for teams). The numeric ID of a user or team is also accepted. */
  actor_identifier: string;
}
export const RemoveCollaboratorForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    actor_type: RemoveCollaboratorForOrgRequestActorType.pipe(T.Label()),
    actor_identifier: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RemoveCollaboratorForOrgRequest",
}) as any as S.Schema<RemoveCollaboratorForOrgRequest>;

export interface RemoveCollaboratorForOrgResponse {}
export const RemoveCollaboratorForOrgResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "RemoveCollaboratorForOrgResponse",
}) as any as S.Schema<RemoveCollaboratorForOrgResponse>;

export type RemoveCollaboratorForUserRequestActorType = "User" | "Team";
export const RemoveCollaboratorForUserRequestActorType = /*@__PURE__*/ S.String;

export interface RemoveCollaboratorForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The type of actor (must be `User` for user-owned spaces; `Team` will be rejected). */
  actor_type: RemoveCollaboratorForUserRequestActorType | (string & {});
  /** The username of the collaborator. The numeric user ID is also accepted. */
  actor_identifier: string;
}
export const RemoveCollaboratorForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    actor_type: RemoveCollaboratorForUserRequestActorType.pipe(T.Label()),
    actor_identifier: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RemoveCollaboratorForUserRequest",
}) as any as S.Schema<RemoveCollaboratorForUserRequest>;

export interface RemoveCollaboratorForUserResponse {}
export const RemoveCollaboratorForUserResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "RemoveCollaboratorForUserResponse",
}) as any as S.Schema<RemoveCollaboratorForUserResponse>;

export type UpdateCollaboratorForOrgRequestActorType = "User" | "Team";
export const UpdateCollaboratorForOrgRequestActorType = /*@__PURE__*/ S.String;

/** The new role to grant to the collaborator. Use `no_access` to remove the collaborator. */
export type UpdateCollaboratorForOrgRequestRole =
  | "reader"
  | "writer"
  | "admin"
  | "no_access";
export const UpdateCollaboratorForOrgRequestRole = /*@__PURE__*/ S.String;

export interface UpdateCollaboratorForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The type of actor (user or team). */
  actor_type: UpdateCollaboratorForOrgRequestActorType | (string & {});
  /** The username (for users) or team slug (for teams). The numeric ID of a user or team is also accepted. */
  actor_identifier: string;
  /** The new role to grant to the collaborator. Use `no_access` to remove the collaborator. */
  role: UpdateCollaboratorForOrgRequestRole | (string & {});
}
export const UpdateCollaboratorForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    actor_type: UpdateCollaboratorForOrgRequestActorType.pipe(T.Label()),
    actor_identifier: S.String.pipe(T.Label()),
    role: UpdateCollaboratorForOrgRequestRole,
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateCollaboratorForOrgRequest",
}) as any as S.Schema<UpdateCollaboratorForOrgRequest>;

export type UpdateCollaboratorForOrgResponse = CopilotSpaceCollaborator;
export const UpdateCollaboratorForOrgResponse = /*@__PURE__*/ S.suspend(() =>
  CopilotSpaceCollaborator.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "UpdateCollaboratorForOrgResponse",
}) as any as S.Schema<UpdateCollaboratorForOrgResponse>;

export type UpdateCollaboratorForUserRequestActorType = "User" | "Team";
export const UpdateCollaboratorForUserRequestActorType = /*@__PURE__*/ S.String;

/** The new role to grant to the collaborator. Use `no_access` to remove the collaborator. */
export type UpdateCollaboratorForUserRequestRole =
  | "reader"
  | "writer"
  | "admin"
  | "no_access";
export const UpdateCollaboratorForUserRequestRole = /*@__PURE__*/ S.String;

export interface UpdateCollaboratorForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The type of actor (must be `User` for user-owned spaces; `Team` will be rejected). */
  actor_type: UpdateCollaboratorForUserRequestActorType | (string & {});
  /** The username of the collaborator. The numeric user ID is also accepted. */
  actor_identifier: string;
  /** The new role to grant to the collaborator. Use `no_access` to remove the collaborator. */
  role: UpdateCollaboratorForUserRequestRole | (string & {});
}
export const UpdateCollaboratorForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    actor_type: UpdateCollaboratorForUserRequestActorType.pipe(T.Label()),
    actor_identifier: S.String.pipe(T.Label()),
    role: UpdateCollaboratorForUserRequestRole,
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateCollaboratorForUserRequest",
}) as any as S.Schema<UpdateCollaboratorForUserRequest>;

export type UpdateCollaboratorForUserResponse = CopilotSpaceCollaborator;
export const UpdateCollaboratorForUserResponse = /*@__PURE__*/ S.suspend(() =>
  CopilotSpaceCollaborator.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "UpdateCollaboratorForUserResponse",
}) as any as S.Schema<UpdateCollaboratorForUserResponse>;

/** The base role that determines default permissions for organization members. Changing this field requires admin permissions. - `no_access`: No default access (default) - `reader`: Organization members can read the space - `writer`: Organization members can read and edit the space - `admin`: Organization members have full admin access to the space */
export type UpdateForOrgRequestBaseRole =
  | "reader"
  | "writer"
  | "admin"
  | "no_access";
export const UpdateForOrgRequestBaseRole = /*@__PURE__*/ S.String;

/** The type of resource. */
export type UpdateForOrgRequestResourcesAttributesItemResourceType =
  | "repository"
  | "github_file"
  | "free_text"
  | "github_issue"
  | "github_pull_request"
  | "media_content"
  | "uploaded_text_file";
export const UpdateForOrgRequestResourcesAttributesItemResourceType =
  /*@__PURE__*/ S.String;

/** Metadata specific to the resource type. */
export type UpdateForOrgRequestResourcesAttributesItemMetadata =
  CreateForOrgRequestResourcesAttributesItemMetadata;
export const UpdateForOrgRequestResourcesAttributesItemMetadata =
  CreateForOrgRequestResourcesAttributesItemMetadata;

export interface UpdateForOrgRequestResourcesAttributesItem {
  /** The type of resource. */
  resource_type?:
    | UpdateForOrgRequestResourcesAttributesItemResourceType
    | (string & {});
  /** Metadata specific to the resource type. */
  metadata?: CreateForOrgRequestResourcesAttributesItemMetadata;
}
export const UpdateForOrgRequestResourcesAttributesItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      resource_type: S.optional(
        UpdateForOrgRequestResourcesAttributesItemResourceType,
      ),
      metadata: S.optional(CreateForOrgRequestResourcesAttributesItemMetadata),
    }),
  ).annotate({
    identifier: "UpdateForOrgRequestResourcesAttributesItem",
  }) as any as S.Schema<UpdateForOrgRequestResourcesAttributesItem>;

/** Resources to attach to the space. */
export type UpdateForOrgRequestResourcesAttributesList =
  Array<UpdateForOrgRequestResourcesAttributesItem>;
export const UpdateForOrgRequestResourcesAttributesList = /*@__PURE__*/ S.Array(
  UpdateForOrgRequestResourcesAttributesItem,
) as any as S.Schema<UpdateForOrgRequestResourcesAttributesList>;

export interface UpdateForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The name of the Copilot Space. */
  name?: string;
  /** A description of the Copilot Space. */
  description?: string;
  /** General instructions for the Copilot Space. */
  general_instructions?: string;
  /** The base role that determines default permissions for organization members. Changing this field requires admin permissions. - `no_access`: No default access (default) - `reader`: Organization members can read the space - `writer`: Organization members can read and edit the space - `admin`: Organization members have full admin access to the space */
  base_role?: UpdateForOrgRequestBaseRole | (string & {});
  /** Resources to attach to the space. */
  resources_attributes?: UpdateForOrgRequestResourcesAttributesList;
}
export const UpdateForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    name: S.optional(S.String),
    description: S.optional(S.String),
    general_instructions: S.optional(S.String),
    base_role: S.optional(UpdateForOrgRequestBaseRole),
    resources_attributes: S.optional(
      UpdateForOrgRequestResourcesAttributesList,
    ),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/orgs/{org}/copilot-spaces/{space_number}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateForOrgRequest",
}) as any as S.Schema<UpdateForOrgRequest>;

/** The base role that determines default permissions for the space. Changing this field requires admin permissions. - `no_access`: No default access (default) - `reader`: Makes the space publicly readable Note: User spaces do not support writer or admin base roles. */
export type UpdateForUserRequestBaseRole = "reader" | "no_access";
export const UpdateForUserRequestBaseRole = /*@__PURE__*/ S.String;

/** The type of resource. */
export type UpdateForUserRequestResourcesAttributesItemResourceType =
  | "repository"
  | "github_file"
  | "free_text"
  | "github_issue"
  | "github_pull_request"
  | "media_content"
  | "uploaded_text_file";
export const UpdateForUserRequestResourcesAttributesItemResourceType =
  /*@__PURE__*/ S.String;

/** Metadata specific to the resource type. */
export type UpdateForUserRequestResourcesAttributesItemMetadata =
  CreateForOrgRequestResourcesAttributesItemMetadata;
export const UpdateForUserRequestResourcesAttributesItemMetadata =
  CreateForOrgRequestResourcesAttributesItemMetadata;

export interface UpdateForUserRequestResourcesAttributesItem {
  /** The type of resource. */
  resource_type?:
    | UpdateForUserRequestResourcesAttributesItemResourceType
    | (string & {});
  /** Metadata specific to the resource type. */
  metadata?: CreateForOrgRequestResourcesAttributesItemMetadata;
}
export const UpdateForUserRequestResourcesAttributesItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      resource_type: S.optional(
        UpdateForUserRequestResourcesAttributesItemResourceType,
      ),
      metadata: S.optional(CreateForOrgRequestResourcesAttributesItemMetadata),
    }),
  ).annotate({
    identifier: "UpdateForUserRequestResourcesAttributesItem",
  }) as any as S.Schema<UpdateForUserRequestResourcesAttributesItem>;

/** Resources to attach to the space. */
export type UpdateForUserRequestResourcesAttributesList =
  Array<UpdateForUserRequestResourcesAttributesItem>;
export const UpdateForUserRequestResourcesAttributesList =
  /*@__PURE__*/ S.Array(
    UpdateForUserRequestResourcesAttributesItem,
  ) as any as S.Schema<UpdateForUserRequestResourcesAttributesList>;

export interface UpdateForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The name of the Copilot Space. */
  name?: string;
  /** A description of the Copilot Space. */
  description?: string;
  /** General instructions for the Copilot Space. */
  general_instructions?: string;
  /** The base role that determines default permissions for the space. Changing this field requires admin permissions. - `no_access`: No default access (default) - `reader`: Makes the space publicly readable Note: User spaces do not support writer or admin base roles. */
  base_role?: UpdateForUserRequestBaseRole | (string & {});
  /** Resources to attach to the space. */
  resources_attributes?: UpdateForUserRequestResourcesAttributesList;
}
export const UpdateForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    name: S.optional(S.String),
    description: S.optional(S.String),
    general_instructions: S.optional(S.String),
    base_role: S.optional(UpdateForUserRequestBaseRole),
    resources_attributes: S.optional(
      UpdateForUserRequestResourcesAttributesList,
    ),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/users/{username}/copilot-spaces/{space_number}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateForUserRequest",
}) as any as S.Schema<UpdateForUserRequest>;

/** Updated resource-specific metadata. */
export type UpdateResourceForOrgRequestMetadataMap = {
  [key: string]: unknown | undefined;
};
export const UpdateResourceForOrgRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<UpdateResourceForOrgRequestMetadataMap>;

export interface UpdateResourceForOrgRequest {
  /** The organization name. The name is not case sensitive. */
  org: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The unique identifier of the resource. */
  space_resource_id: number;
  /** Updated resource-specific metadata. */
  metadata?: UpdateResourceForOrgRequestMetadataMap;
}
export const UpdateResourceForOrgRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    space_resource_id: S.Number.pipe(T.Label()),
    metadata: S.optional(UpdateResourceForOrgRequestMetadataMap),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateResourceForOrgRequest",
}) as any as S.Schema<UpdateResourceForOrgRequest>;

/** Updated resource-specific metadata. */
export type UpdateResourceForUserRequestMetadataMap = {
  [key: string]: unknown | undefined;
};
export const UpdateResourceForUserRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<UpdateResourceForUserRequestMetadataMap>;

export interface UpdateResourceForUserRequest {
  /** The handle for the GitHub user account. */
  username: string;
  /** The unique identifier of the Copilot Space. */
  space_number: number;
  /** The unique identifier of the resource. */
  space_resource_id: number;
  /** Updated resource-specific metadata. */
  metadata?: UpdateResourceForUserRequestMetadataMap;
}
export const UpdateResourceForUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    username: S.String.pipe(T.Label()),
    space_number: S.Number.pipe(T.Label()),
    space_resource_id: S.Number.pipe(T.Label()),
    metadata: S.optional(UpdateResourceForUserRequestMetadataMap),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateResourceForUserRequest",
}) as any as S.Schema<UpdateResourceForUserRequest>;

export type AddCollaboratorForOrgError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Add a collaborator to an organization Copilot Space Adds a collaborator (user or team) to a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to manage collaborators. **Note:** When adding users as collaborators, they must already be members of the organization. When adding teams as collaborators, they must be defined in the organization. OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space. */
export const addCollaboratorForOrg: API.OperationMethod<
  AddCollaboratorForOrgRequest,
  AddCollaboratorForOrgResponse,
  AddCollaboratorForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AddCollaboratorForOrgRequest,
  output: AddCollaboratorForOrgResponse,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type AddCollaboratorForUserError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Add a collaborator to a Copilot Space for a user Adds a collaborator to a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space. Team collaborators are not supported for user-owned Copilot Spaces. OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint. */
export const addCollaboratorForUser: API.OperationMethod<
  AddCollaboratorForUserRequest,
  AddCollaboratorForUserResponse,
  AddCollaboratorForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AddCollaboratorForUserRequest,
  output: AddCollaboratorForUserResponse,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type CreateForOrgError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Create an organization Copilot Space Creates a new Copilot Space owned by an organization. The authenticated user must have permissions to create spaces in the organization. Organization members with appropriate permissions can create Copilot Spaces to be shared within their organization. OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by the submitted resources. */
export const createForOrg: API.OperationMethod<
  CreateForOrgRequest,
  CopilotSpace,
  CreateForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateForOrgRequest,
  output: CopilotSpace,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type CreateForUserError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Create a Copilot Space for a user Creates a new Copilot Space owned by a user. Only the authenticated user can create spaces for their own account. Users can create personal Copilot Spaces for their individual use. OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint. */
export const createForUser: API.OperationMethod<
  CreateForUserRequest,
  CopilotSpace,
  CreateForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateForUserRequest,
  output: CopilotSpace,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type CreateResourceForOrgError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Create a resource for an organization Copilot Space Creates a new resource in a specific Copilot Space owned by an organization. The authenticated user must have write permissions on the space. The following resource types are supported: `repository`, `github_file`, `free_text`, `github_issue`, `github_pull_request`. The `uploaded_text_file` and `media_content` types are not supported via this endpoint. For `github_file` resources, if a resource with the same repository, file path, and SHA already exists, the existing resource is returned with a `200` status. OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space, including the resource being created. */
export const createResourceForOrg: API.OperationMethod<
  CreateResourceForOrgRequest,
  CopilotSpaceResource,
  CreateResourceForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateResourceForOrgRequest,
  output: CopilotSpaceResource,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type CreateResourceForUserError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Create a resource for a Copilot Space for a user Creates a new resource in a specific Copilot Space owned by a user. The authenticated user must have write permissions on the space. The following resource types are supported: `repository`, `github_file`, `free_text`, `github_issue`, `github_pull_request`. The `uploaded_text_file` and `media_content` types are not supported via this endpoint. For `github_file` resources, if a resource with the same repository, file path, and SHA already exists, the existing resource is returned with a `200` status. OAuth app tokens and personal access tokens (classic) need the `write:user` scope to use this endpoint. */
export const createResourceForUser: API.OperationMethod<
  CreateResourceForUserRequest,
  CopilotSpaceResource,
  CreateResourceForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateResourceForUserRequest,
  output: CopilotSpaceResource,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type DeleteForOrgError = NotFound | UnprocessableEntity | GithubOpError;
/** Delete an organization Copilot Space Deletes a Copilot Space owned by an organization. The authenticated user must have permissions to delete spaces in the organization. **Warning:** This action is permanent and cannot be undone. Deleting a Copilot Space will remove all associated resources and configurations. Organization members with appropriate permissions can delete Copilot Spaces owned by their organization. OAuth app tokens and personal access tokens (classic) need both the `read:org` and `repo` scopes to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space. */
export const deleteForOrg: API.OperationMethod<
  DeleteForOrgRequest,
  DeleteForOrgResponse,
  DeleteForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteForOrgRequest,
  output: DeleteForOrgResponse,
  errors: [NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type DeleteForUserError = NotFound | UnprocessableEntity | GithubOpError;
/** Delete a Copilot Space for a user Deletes a Copilot Space owned by a user. The authenticated user must be the owner of the space. **Warning:** This action is permanent and cannot be undone. Deleting a space will remove all associated resources and configurations. OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint. */
export const deleteForUser: API.OperationMethod<
  DeleteForUserRequest,
  DeleteForUserResponse,
  DeleteForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteForUserRequest,
  output: DeleteForUserResponse,
  errors: [NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type DeleteResourceForOrgError = Forbidden | NotFound | GithubOpError;
/** Delete a resource from an organization Copilot Space Deletes a resource from a specific Copilot Space owned by an organization. The authenticated user must have write permissions on the space. OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space. */
export const deleteResourceForOrg: API.OperationMethod<
  DeleteResourceForOrgRequest,
  DeleteResourceForOrgResponse,
  DeleteResourceForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteResourceForOrgRequest,
  output: DeleteResourceForOrgResponse,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type DeleteResourceForUserError = Forbidden | NotFound | GithubOpError;
/** Delete a resource from a Copilot Space for a user Deletes a resource from a specific Copilot Space owned by a user. The authenticated user must have write permissions on the space. OAuth app tokens and personal access tokens (classic) need the `write:user` scope to use this endpoint. */
export const deleteResourceForUser: API.OperationMethod<
  DeleteResourceForUserRequest,
  DeleteResourceForUserResponse,
  DeleteResourceForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteResourceForUserRequest,
  output: DeleteResourceForUserResponse,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetForOrgError = Forbidden | NotFound | GithubOpError;
/** Get an organization Copilot Space Gets details about a specific Copilot Space owned by an organization. The authenticated user must have read access to the Space. Internal Spaces require the authenticated user to be a member of the organization or have been granted read permissions. OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space. */
export const getForOrg: API.OperationMethod<
  GetForOrgRequest,
  CopilotSpace,
  GetForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetForOrgRequest,
  output: CopilotSpace,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetForUserError = Forbidden | NotFound | GithubOpError;
/** Get a Copilot Space for a user Gets details about a specific Copilot Space owned by a user. The authenticated user must have read access to the Space. Private user spaces require the authenticated user to be the owner of the space. Public user spaces are accessible to any authenticated user. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint. */
export const getForUser: API.OperationMethod<
  GetForUserRequest,
  CopilotSpace,
  GetForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetForUserRequest,
  output: CopilotSpace,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetResourceForOrgError = Forbidden | NotFound | GithubOpError;
/** Get a resource for an organization Copilot Space Gets a specific resource attached to a Copilot Space owned by an organization. The authenticated user must have appropriate permissions to view the space. OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space. */
export const getResourceForOrg: API.OperationMethod<
  GetResourceForOrgRequest,
  CopilotSpaceResource,
  GetResourceForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetResourceForOrgRequest,
  output: CopilotSpaceResource,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetResourceForUserError = Forbidden | NotFound | GithubOpError;
/** Get a resource for a Copilot Space for a user Gets a specific resource attached to a Copilot Space owned by a user. The authenticated user must have appropriate permissions to view the space. OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint. */
export const getResourceForUser: API.OperationMethod<
  GetResourceForUserRequest,
  CopilotSpaceResource,
  GetResourceForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetResourceForUserRequest,
  output: CopilotSpaceResource,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListCollaboratorsForOrgError = Forbidden | NotFound | GithubOpError;
/** List collaborators for an organization Copilot Space Lists all collaborators for a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to view collaborators. Each collaborator entry specifies which user or team has access to the space and at what level (reader, writer, or admin). The space owner (organization) is excluded from this list. **Note:** Team collaborators listed here are teams that are defined in the organization. OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space. */
export const listCollaboratorsForOrg: API.OperationMethod<
  ListCollaboratorsForOrgRequest,
  ListCollaboratorsForOrgResponse,
  ListCollaboratorsForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListCollaboratorsForOrgRequest,
  output: ListCollaboratorsForOrgResponse,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListCollaboratorsForUserError =
  | Forbidden
  | NotFound
  | GithubOpError;
/** List collaborators for a Copilot Space for a user Lists all collaborators for a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space. Each collaborator entry specifies which user has access to the space and at what level (reader, writer, or admin). The space owner is excluded from this list. Team collaborators are not supported for user-owned Copilot Spaces. OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint. */
export const listCollaboratorsForUser: API.OperationMethod<
  ListCollaboratorsForUserRequest,
  ListCollaboratorsForUserResponse,
  ListCollaboratorsForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListCollaboratorsForUserRequest,
  output: ListCollaboratorsForUserResponse,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListForOrgError = Forbidden | NotFound | GithubOpError;
/** List organization Copilot Spaces Lists Copilot Spaces owned by an organization. The authenticated user must have read access to the organization's Copilot Spaces. Only Spaces that are readable by the authenticated user are returned. This includes public Spaces and internal Spaces if the user is a member of the organization. OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in a space; spaces with inaccessible resources are omitted from the response. */
export const listForOrg: API.OperationMethod<
  ListForOrgRequest,
  ListForOrgResponse,
  ListForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListForOrgRequest,
  output: ListForOrgResponse,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListForUserError = Forbidden | NotFound | GithubOpError;
/** List Copilot Spaces for a user Lists Copilot Spaces owned by a user. The authenticated user must have read access to the user's Copilot Spaces. Only Spaces that are readable by the authenticated user are returned. This includes the user's own spaces, and public user spaces when accessing another user's spaces. OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint. */
export const listForUser: API.OperationMethod<
  ListForUserRequest,
  ListForUserResponse,
  ListForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListForUserRequest,
  output: ListForUserResponse,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListResourcesForOrgError = Forbidden | NotFound | GithubOpError;
/** List resources for an organization Copilot Space Lists all resources attached to a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to view the space. OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space. */
export const listResourcesForOrg: API.OperationMethod<
  ListResourcesForOrgRequest,
  ListResourcesForOrgResponse,
  ListResourcesForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListResourcesForOrgRequest,
  output: ListResourcesForOrgResponse,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListResourcesForUserError = Forbidden | NotFound | GithubOpError;
/** List resources for a Copilot Space for a user Lists all resources attached to a specific Copilot Space owned by a user. The authenticated user must have appropriate permissions to view the space. OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint. */
export const listResourcesForUser: API.OperationMethod<
  ListResourcesForUserRequest,
  ListResourcesForUserResponse,
  ListResourcesForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListResourcesForUserRequest,
  output: ListResourcesForUserResponse,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type RemoveCollaboratorForOrgError =
  | Forbidden
  | NotFound
  | GithubOpError;
/** Remove a collaborator from an organization Copilot Space Removes a collaborator from a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to manage collaborators. OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space. */
export const removeCollaboratorForOrg: API.OperationMethod<
  RemoveCollaboratorForOrgRequest,
  RemoveCollaboratorForOrgResponse,
  RemoveCollaboratorForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RemoveCollaboratorForOrgRequest,
  output: RemoveCollaboratorForOrgResponse,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type RemoveCollaboratorForUserError =
  | Forbidden
  | NotFound
  | GithubOpError;
/** Remove a collaborator from a Copilot Space for a user Removes a collaborator from a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space. OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint. */
export const removeCollaboratorForUser: API.OperationMethod<
  RemoveCollaboratorForUserRequest,
  RemoveCollaboratorForUserResponse,
  RemoveCollaboratorForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RemoveCollaboratorForUserRequest,
  output: RemoveCollaboratorForUserResponse,
  errors: [Forbidden, NotFound],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type UpdateCollaboratorForOrgError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Set a collaborator role for an organization Copilot Space Updates the role of a collaborator for a specific Copilot Space owned by an organization. The authenticated user must have appropriate permissions to manage collaborators. OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space. */
export const updateCollaboratorForOrg: API.OperationMethod<
  UpdateCollaboratorForOrgRequest,
  UpdateCollaboratorForOrgResponse,
  UpdateCollaboratorForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateCollaboratorForOrgRequest,
  output: UpdateCollaboratorForOrgResponse,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type UpdateCollaboratorForUserError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Set a collaborator role for a Copilot Space for a user Updates the role of a collaborator for a specific Copilot Space owned by a user. The authenticated user must be the owner of the space or have admin access to the space. OAuth app tokens and personal access tokens (classic) need the `user` scope to use this endpoint. */
export const updateCollaboratorForUser: API.OperationMethod<
  UpdateCollaboratorForUserRequest,
  UpdateCollaboratorForUserResponse,
  UpdateCollaboratorForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateCollaboratorForUserRequest,
  output: UpdateCollaboratorForUserResponse,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type UpdateForOrgError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Set an organization Copilot Space Updates a Copilot Space owned by an organization. The authenticated user must have permissions to update spaces in the organization. Organization members with appropriate permissions can update Copilot Spaces owned by their organization. OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space, including any being added or updated. */
export const updateForOrg: API.OperationMethod<
  UpdateForOrgRequest,
  CopilotSpace,
  UpdateForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateForOrgRequest,
  output: CopilotSpace,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type UpdateForUserError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Set a Copilot Space for a user Updates a Copilot Space owned by a user. Only the authenticated user can update spaces for their own account. Users can update their personal Copilot Spaces. OAuth app tokens and personal access tokens (classic) need the `read:user` scope to use this endpoint. */
export const updateForUser: API.OperationMethod<
  UpdateForUserRequest,
  CopilotSpace,
  UpdateForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateForUserRequest,
  output: CopilotSpace,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type UpdateResourceForOrgError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Set a resource for an organization Copilot Space Updates the metadata of a resource in a specific Copilot Space owned by an organization. The authenticated user must have write permissions on the space. OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint. Fine-grained tokens and GitHub App user access tokens must have been granted access to the organization that owns the space. They must also have been granted access to every repository referenced by resources in the space, including the resource being updated. */
export const updateResourceForOrg: API.OperationMethod<
  UpdateResourceForOrgRequest,
  CopilotSpaceResource,
  UpdateResourceForOrgError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateResourceForOrgRequest,
  output: CopilotSpaceResource,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type UpdateResourceForUserError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Set a resource for a Copilot Space for a user Updates the metadata of a resource in a specific Copilot Space owned by a user. The authenticated user must have write permissions on the space. OAuth app tokens and personal access tokens (classic) need the `write:user` scope to use this endpoint. */
export const updateResourceForUser: API.OperationMethod<
  UpdateResourceForUserRequest,
  CopilotSpaceResource,
  UpdateResourceForUserError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateResourceForUserRequest,
  output: CopilotSpaceResource,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));
