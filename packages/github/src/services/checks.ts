// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/rest-api-description). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  GithubProtocol,
  type GithubOpError,
  type GithubOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { GithubOpError, GithubOpContext };

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** The current status of the check run. Only GitHub Actions can set a status of `waiting`, `pending`, or `requested`. */
export type CreateRequestStatus =
  | "queued"
  | "in_progress"
  | "completed"
  | "waiting"
  | "requested"
  | "pending";
export const CreateRequestStatus = /*@__PURE__*/ S.String;

/** **Required if you provide `completed_at` or a `status` of `completed`**. The final conclusion of the check. **Note:** Providing `conclusion` will automatically set the `status` parameter to `completed`. You cannot change a check run conclusion to `stale`, only GitHub can set this. */
export type CreateRequestConclusion =
  | "action_required"
  | "cancelled"
  | "failure"
  | "neutral"
  | "success"
  | "skipped"
  | "stale"
  | "timed_out";
export const CreateRequestConclusion = /*@__PURE__*/ S.String;

/** The level of the annotation. */
export type CreateRequestOutputAnnotationsItemAnnotationLevel =
  | "notice"
  | "warning"
  | "failure";
export const CreateRequestOutputAnnotationsItemAnnotationLevel =
  /*@__PURE__*/ S.String;

export interface CreateRequestOutputAnnotationsItem {
  /** The path of the file to add an annotation to. For example, `assets/css/main.css`. */
  path: string;
  /** The start line of the annotation. Line numbers start at 1. */
  start_line: number;
  /** The end line of the annotation. */
  end_line: number;
  /** The start column of the annotation. Annotations only support `start_column` and `end_column` on the same line. Omit this parameter if `start_line` and `end_line` have different values. Column numbers start at 1. */
  start_column?: number;
  /** The end column of the annotation. Annotations only support `start_column` and `end_column` on the same line. Omit this parameter if `start_line` and `end_line` have different values. */
  end_column?: number;
  /** The level of the annotation. */
  annotation_level:
    | CreateRequestOutputAnnotationsItemAnnotationLevel
    | (string & {});
  /** A short description of the feedback for these lines of code. The maximum size is 64 KB. */
  message: string;
  /** The title that represents the annotation. The maximum size is 255 characters. */
  title?: string;
  /** Details about this annotation. The maximum size is 64 KB. */
  raw_details?: string;
}
export const CreateRequestOutputAnnotationsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    start_line: S.Number,
    end_line: S.Number,
    start_column: S.optional(S.Number),
    end_column: S.optional(S.Number),
    annotation_level: CreateRequestOutputAnnotationsItemAnnotationLevel,
    message: S.String,
    title: S.optional(S.String),
    raw_details: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateRequestOutputAnnotationsItem",
}) as any as S.Schema<CreateRequestOutputAnnotationsItem>;

/** Adds information from your analysis to specific lines of code. Annotations are visible on GitHub in the **Checks** and **Files changed** tab of the pull request. The Checks API limits the number of annotations to a maximum of 50 per API request. To create more than 50 annotations, you have to make multiple requests to the [Update a check run](https://docs.github.com/rest/checks/runs#update-a-check-run) endpoint. Each time you update the check run, annotations are appended to the list of annotations that already exist for the check run. GitHub Actions are limited to 10 warning annotations and 10 error annotations per step. For details about how you can view annotations on GitHub, see "[About status checks](https://docs.github.com/articles/about-status-checks#checks)". */
export type CreateRequestOutputAnnotationsList =
  Array<CreateRequestOutputAnnotationsItem>;
export const CreateRequestOutputAnnotationsList = /*@__PURE__*/ S.Array(
  CreateRequestOutputAnnotationsItem,
) as any as S.Schema<CreateRequestOutputAnnotationsList>;

export interface CreateRequestOutputImagesItem {
  /** The alternative text for the image. */
  alt: string;
  /** The full URL of the image. */
  image_url: string;
  /** A short image description. */
  caption?: string;
}
export const CreateRequestOutputImagesItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    alt: S.String,
    image_url: S.String,
    caption: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateRequestOutputImagesItem",
}) as any as S.Schema<CreateRequestOutputImagesItem>;

/** Adds images to the output displayed in the GitHub pull request UI. */
export type CreateRequestOutputImagesList =
  Array<CreateRequestOutputImagesItem>;
export const CreateRequestOutputImagesList = /*@__PURE__*/ S.Array(
  CreateRequestOutputImagesItem,
) as any as S.Schema<CreateRequestOutputImagesList>;

/** Check runs can accept a variety of data in the `output` object, including a `title` and `summary` and can optionally provide descriptive details about the run. */
export interface CreateRequestOutput {
  /** The title of the check run. */
  title: string;
  /** The summary of the check run. This parameter supports Markdown. **Maximum length**: 65535 characters. */
  summary: string;
  /** The details of the check run. This parameter supports Markdown. **Maximum length**: 65535 characters. */
  text?: string;
  /** Adds information from your analysis to specific lines of code. Annotations are visible on GitHub in the **Checks** and **Files changed** tab of the pull request. The Checks API limits the number of annotations to a maximum of 50 per API request. To create more than 50 annotations, you have to make multiple requests to the [Update a check run](https://docs.github.com/rest/checks/runs#update-a-check-run) endpoint. Each time you update the check run, annotations are appended to the list of annotations that already exist for the check run. GitHub Actions are limited to 10 warning annotations and 10 error annotations per step. For details about how you can view annotations on GitHub, see "[About status checks](https://docs.github.com/articles/about-status-checks#checks)". */
  annotations?: CreateRequestOutputAnnotationsList;
  /** Adds images to the output displayed in the GitHub pull request UI. */
  images?: CreateRequestOutputImagesList;
}
export const CreateRequestOutput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    title: S.String,
    summary: S.String,
    text: S.optional(S.String),
    annotations: S.optional(CreateRequestOutputAnnotationsList),
    images: S.optional(CreateRequestOutputImagesList),
  }),
).annotate({
  identifier: "CreateRequestOutput",
}) as any as S.Schema<CreateRequestOutput>;

export interface CreateRequestActionsItem {
  /** The text to be displayed on a button in the web UI. The maximum size is 20 characters. */
  label: string;
  /** A short explanation of what this action would do. The maximum size is 40 characters. */
  description: string;
  /** A reference for the action on the integrator's system. The maximum size is 20 characters. */
  identifier: string;
}
export const CreateRequestActionsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    label: S.String,
    description: S.String,
    identifier: S.String,
  }),
).annotate({
  identifier: "CreateRequestActionsItem",
}) as any as S.Schema<CreateRequestActionsItem>;

/** Displays a button on GitHub that can be clicked to alert your app to do additional tasks. For example, a code linting app can display a button that automatically fixes detected errors. The button created in this object is displayed after the check run completes. When a user clicks the button, GitHub sends the [`check_run.requested_action` webhook](https://docs.github.com/webhooks/event-payloads/#check_run) to your app. Each action includes a `label`, `identifier` and `description`. A maximum of three actions are accepted. To learn more about check runs and requested actions, see "[Check runs and requested actions](https://docs.github.com/rest/guides/using-the-rest-api-to-interact-with-checks#check-runs-and-requested-actions)." */
export type CreateRequestActionsList = Array<CreateRequestActionsItem>;
export const CreateRequestActionsList = /*@__PURE__*/ S.Array(
  CreateRequestActionsItem,
) as any as S.Schema<CreateRequestActionsList>;

export interface CreateRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The name of the check. For example, "code-coverage". */
  name: string;
  /** The SHA of the commit. */
  head_sha: string;
  /** The URL of the integrator's site that has the full details of the check. If the integrator does not provide this, then the homepage of the GitHub app is used. */
  details_url?: string;
  /** A reference for the run on the integrator's system. */
  external_id?: string;
  /** The current status of the check run. Only GitHub Actions can set a status of `waiting`, `pending`, or `requested`. */
  status?: CreateRequestStatus | (string & {});
  /** The time that the check run began. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. */
  started_at?: string;
  /** **Required if you provide `completed_at` or a `status` of `completed`**. The final conclusion of the check. **Note:** Providing `conclusion` will automatically set the `status` parameter to `completed`. You cannot change a check run conclusion to `stale`, only GitHub can set this. */
  conclusion?: CreateRequestConclusion | (string & {});
  /** The time the check completed. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. */
  completed_at?: string;
  /** Check runs can accept a variety of data in the `output` object, including a `title` and `summary` and can optionally provide descriptive details about the run. */
  output?: CreateRequestOutput;
  /** Displays a button on GitHub that can be clicked to alert your app to do additional tasks. For example, a code linting app can display a button that automatically fixes detected errors. The button created in this object is displayed after the check run completes. When a user clicks the button, GitHub sends the [`check_run.requested_action` webhook](https://docs.github.com/webhooks/event-payloads/#check_run) to your app. Each action includes a `label`, `identifier` and `description`. A maximum of three actions are accepted. To learn more about check runs and requested actions, see "[Check runs and requested actions](https://docs.github.com/rest/guides/using-the-rest-api-to-interact-with-checks#check-runs-and-requested-actions)." */
  actions?: CreateRequestActionsList;
}
export const CreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    name: S.String,
    head_sha: S.String,
    details_url: S.optional(S.String),
    external_id: S.optional(S.String),
    status: S.optional(CreateRequestStatus),
    started_at: S.optional(S.String),
    conclusion: S.optional(CreateRequestConclusion),
    completed_at: S.optional(S.String),
    output: S.optional(CreateRequestOutput),
    actions: S.optional(CreateRequestActionsList),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/repos/{owner}/{repo}/check-runs",
      code: 200,
    }),
  ),
).annotate({ identifier: "CreateRequest" }) as any as S.Schema<CreateRequest>;

/** The phase of the lifecycle that the check is currently in. Statuses of waiting, requested, and pending are reserved for GitHub Actions check runs. */
export type CheckRunStatus =
  | "queued"
  | "in_progress"
  | "completed"
  | "waiting"
  | "requested"
  | "pending";
export const CheckRunStatus = /*@__PURE__*/ S.String;

export type CheckRunConclusion =
  | "success"
  | "failure"
  | "neutral"
  | "cancelled"
  | "skipped"
  | "timed_out"
  | "action_required";
export const CheckRunConclusion = /*@__PURE__*/ S.String;

export interface CheckRunOutput {
  title: string | null;
  summary: string | null;
  text: string | null;
  annotations_count: number;
  annotations_url: string;
}
export const CheckRunOutput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    title: S.NullOr(S.String),
    summary: S.NullOr(S.String),
    text: S.NullOr(S.String),
    annotations_count: S.Number,
    annotations_url: S.String,
  }),
).annotate({ identifier: "CheckRunOutput" }) as any as S.Schema<CheckRunOutput>;

export interface CheckRunCheckSuite {
  id: number;
}
export const CheckRunCheckSuite = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
  }),
).annotate({
  identifier: "CheckRunCheckSuite",
}) as any as S.Schema<CheckRunCheckSuite>;

/** A GitHub user. */
export interface SimpleUser {
  name?: string | null;
  email?: string | null;
  login: string;
  id: number;
  node_id: string;
  avatar_url: string;
  gravatar_id: string | null;
  url: string;
  html_url: string;
  followers_url: string;
  following_url: string;
  gists_url: string;
  starred_url: string;
  subscriptions_url: string;
  organizations_url: string;
  repos_url: string;
  events_url: string;
  received_events_url: string;
  type: string;
  site_admin: boolean;
  starred_at?: string;
  user_view_type?: string;
}
export const SimpleUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.NullOr(S.String)),
    email: S.optional(S.NullOr(S.String)),
    login: S.String,
    id: S.Number,
    node_id: S.String,
    avatar_url: S.String,
    gravatar_id: S.NullOr(S.String),
    url: S.String,
    html_url: S.String,
    followers_url: S.String,
    following_url: S.String,
    gists_url: S.String,
    starred_url: S.String,
    subscriptions_url: S.String,
    organizations_url: S.String,
    repos_url: S.String,
    events_url: S.String,
    received_events_url: S.String,
    type: S.String,
    site_admin: S.Boolean,
    starred_at: S.optional(S.String),
    user_view_type: S.optional(S.String),
  }),
).annotate({ identifier: "SimpleUser" }) as any as S.Schema<SimpleUser>;

/** An enterprise on GitHub. */
export interface Enterprise {
  /** A short description of the enterprise. */
  description?: string | null;
  html_url: string;
  /** The enterprise's website URL. */
  website_url?: string | null;
  /** Unique identifier of the enterprise */
  id: number;
  node_id: string;
  /** The name of the enterprise. */
  name: string;
  /** The slug url identifier for the enterprise. */
  slug: string;
  created_at: string | null;
  updated_at: string | null;
  avatar_url: string;
}
export const Enterprise = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.optional(S.NullOr(S.String)),
    html_url: S.String,
    website_url: S.optional(S.NullOr(S.String)),
    id: S.Number,
    node_id: S.String,
    name: S.String,
    slug: S.String,
    created_at: S.NullOr(S.String),
    updated_at: S.NullOr(S.String),
    avatar_url: S.String,
  }),
).annotate({ identifier: "Enterprise" }) as any as S.Schema<Enterprise>;

export type NullableIntegrationOwner = SimpleUser | Enterprise;
export const NullableIntegrationOwner =
  /*@__PURE__*/ S.Unknown as any as S.Schema<NullableIntegrationOwner>;

/** The set of permissions for the GitHub app */
export interface NullableIntegrationPermissions {
  issues?: string;
  checks?: string;
  metadata?: string;
  contents?: string;
  deployments?: string;
}
export const NullableIntegrationPermissions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    issues: S.optional(S.String),
    checks: S.optional(S.String),
    metadata: S.optional(S.String),
    contents: S.optional(S.String),
    deployments: S.optional(S.String),
  }),
).annotate({
  identifier: "NullableIntegrationPermissions",
}) as any as S.Schema<NullableIntegrationPermissions>;

/** The list of events for the GitHub app. Note that the `installation_target`, `security_advisory`, and `meta` events are not included because they are global events and not specific to an installation. */
export type NullableIntegrationEventsList = Array<string>;
export const NullableIntegrationEventsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<NullableIntegrationEventsList>;

/** GitHub apps are a new way to extend GitHub. They can be installed directly on organizations and user accounts and granted access to specific repositories. They come with granular permissions and built-in webhooks. GitHub apps are first class actors within GitHub. */
export interface NullableIntegration {
  /** Unique identifier of the GitHub app */
  id: number;
  /** The slug name of the GitHub app */
  slug?: string;
  node_id: string;
  client_id?: string;
  owner: NullableIntegrationOwner;
  /** The name of the GitHub app */
  name: string;
  description: string | null;
  external_url: string;
  html_url: string;
  created_at: string;
  updated_at: string;
  /** The set of permissions for the GitHub app */
  permissions: NullableIntegrationPermissions;
  /** The list of events for the GitHub app. Note that the `installation_target`, `security_advisory`, and `meta` events are not included because they are global events and not specific to an installation. */
  events: NullableIntegrationEventsList;
  /** The number of installations associated with the GitHub app. Only returned when the integration is requesting details about itself. */
  installations_count?: number;
}
export const NullableIntegration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    slug: S.optional(S.String),
    node_id: S.String,
    client_id: S.optional(S.String),
    owner: NullableIntegrationOwner,
    name: S.String,
    description: S.NullOr(S.String),
    external_url: S.String,
    html_url: S.String,
    created_at: S.String,
    updated_at: S.String,
    permissions: NullableIntegrationPermissions,
    events: NullableIntegrationEventsList,
    installations_count: S.optional(S.Number),
  }),
).annotate({
  identifier: "NullableIntegration",
}) as any as S.Schema<NullableIntegration>;

export interface PullRequestMinimalHeadRepo {
  id: number;
  url: string;
  name: string;
}
export const PullRequestMinimalHeadRepo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    url: S.String,
    name: S.String,
  }),
).annotate({
  identifier: "PullRequestMinimalHeadRepo",
}) as any as S.Schema<PullRequestMinimalHeadRepo>;

export interface PullRequestMinimalHead {
  ref: string;
  sha: string;
  repo: PullRequestMinimalHeadRepo;
}
export const PullRequestMinimalHead = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ref: S.String,
    sha: S.String,
    repo: PullRequestMinimalHeadRepo,
  }),
).annotate({
  identifier: "PullRequestMinimalHead",
}) as any as S.Schema<PullRequestMinimalHead>;

export type PullRequestMinimalBaseRepo = PullRequestMinimalHeadRepo;
export const PullRequestMinimalBaseRepo = PullRequestMinimalHeadRepo;

export type PullRequestMinimalBase = PullRequestMinimalHead;
export const PullRequestMinimalBase = PullRequestMinimalHead;

export interface PullRequestMinimal {
  id: number;
  number: number;
  url: string;
  head: PullRequestMinimalHead;
  base: PullRequestMinimalHead;
}
export const PullRequestMinimal = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    number: S.Number,
    url: S.String,
    head: PullRequestMinimalHead,
    base: PullRequestMinimalHead,
  }),
).annotate({
  identifier: "PullRequestMinimal",
}) as any as S.Schema<PullRequestMinimal>;

/** Pull requests that are open with a `head_sha` or `head_branch` that matches the check. The returned pull requests do not necessarily indicate pull requests that triggered the check. */
export type CheckRunPullRequestsList = Array<PullRequestMinimal>;
export const CheckRunPullRequestsList = /*@__PURE__*/ S.Array(
  PullRequestMinimal,
) as any as S.Schema<CheckRunPullRequestsList>;

/** A deployment created as the result of an Actions check run from a workflow that references an environment */
export interface DeploymentSimple {
  url: string;
  /** Unique identifier of the deployment */
  id: number;
  node_id: string;
  /** Parameter to specify a task to execute */
  task: string;
  original_environment?: string;
  /** Name for the target deployment environment. */
  environment: string;
  description: string | null;
  created_at: string;
  updated_at: string;
  statuses_url: string;
  repository_url: string;
  /** Specifies if the given environment is will no longer exist at some point in the future. Default: false. */
  transient_environment?: boolean;
  /** Specifies if the given environment is one that end-users directly interact with. Default: false. */
  production_environment?: boolean;
  performed_via_github_app?: NullableIntegration | null;
}
export const DeploymentSimple = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
    id: S.Number,
    node_id: S.String,
    task: S.String,
    original_environment: S.optional(S.String),
    environment: S.String,
    description: S.NullOr(S.String),
    created_at: S.String,
    updated_at: S.String,
    statuses_url: S.String,
    repository_url: S.String,
    transient_environment: S.optional(S.Boolean),
    production_environment: S.optional(S.Boolean),
    performed_via_github_app: S.optional(S.NullOr(NullableIntegration)),
  }),
).annotate({
  identifier: "DeploymentSimple",
}) as any as S.Schema<DeploymentSimple>;

/** A check performed on the code of a given code change */
export interface CheckRun {
  /** The id of the check. */
  id: number;
  /** The SHA of the commit that is being checked. */
  head_sha: string;
  node_id: string;
  external_id: string | null;
  url: string;
  html_url: string | null;
  details_url: string | null;
  /** The phase of the lifecycle that the check is currently in. Statuses of waiting, requested, and pending are reserved for GitHub Actions check runs. */
  status: CheckRunStatus;
  conclusion: CheckRunConclusion | null;
  started_at: string | null;
  completed_at: string | null;
  output: CheckRunOutput;
  /** The name of the check. */
  name: string;
  check_suite: CheckRunCheckSuite | null;
  app: NullableIntegration | null;
  /** Pull requests that are open with a `head_sha` or `head_branch` that matches the check. The returned pull requests do not necessarily indicate pull requests that triggered the check. */
  pull_requests: CheckRunPullRequestsList;
  deployment?: DeploymentSimple;
}
export const CheckRun = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    head_sha: S.String,
    node_id: S.String,
    external_id: S.NullOr(S.String),
    url: S.String,
    html_url: S.NullOr(S.String),
    details_url: S.NullOr(S.String),
    status: CheckRunStatus,
    conclusion: S.NullOr(CheckRunConclusion),
    started_at: S.NullOr(S.String),
    completed_at: S.NullOr(S.String),
    output: CheckRunOutput,
    name: S.String,
    check_suite: S.NullOr(CheckRunCheckSuite),
    app: S.NullOr(NullableIntegration),
    pull_requests: CheckRunPullRequestsList,
    deployment: S.optional(DeploymentSimple),
  }),
).annotate({ identifier: "CheckRun" }) as any as S.Schema<CheckRun>;

export interface CreateSuiteRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The sha of the head commit. */
  head_sha: string;
}
export const CreateSuiteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    head_sha: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/repos/{owner}/{repo}/check-suites",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateSuiteRequest",
}) as any as S.Schema<CreateSuiteRequest>;

/** The phase of the lifecycle that the check suite is currently in. Statuses of waiting, requested, and pending are reserved for GitHub Actions check suites. */
export type CheckSuiteStatus =
  | "queued"
  | "in_progress"
  | "completed"
  | "waiting"
  | "requested"
  | "pending";
export const CheckSuiteStatus = /*@__PURE__*/ S.String;

export type CheckSuiteConclusion =
  | "success"
  | "failure"
  | "neutral"
  | "cancelled"
  | "skipped"
  | "timed_out"
  | "action_required"
  | "startup_failure"
  | "stale";
export const CheckSuiteConclusion = /*@__PURE__*/ S.String;

export type CheckSuitePullRequestsList = Array<PullRequestMinimal>;
export const CheckSuitePullRequestsList = /*@__PURE__*/ S.Array(
  PullRequestMinimal,
) as any as S.Schema<CheckSuitePullRequestsList>;

export type MinimalRepositoryTopicsList = Array<string>;
export const MinimalRepositoryTopicsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<MinimalRepositoryTopicsList>;

/** The policy controlling who can create pull requests: all or collaborators_only. */
export type MinimalRepositoryPullRequestCreationPolicy =
  | "all"
  | "collaborators_only";
export const MinimalRepositoryPullRequestCreationPolicy =
  /*@__PURE__*/ S.String;

export interface MinimalRepositoryPermissions {
  admin?: boolean;
  maintain?: boolean;
  push?: boolean;
  triage?: boolean;
  pull?: boolean;
}
export const MinimalRepositoryPermissions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    admin: S.optional(S.Boolean),
    maintain: S.optional(S.Boolean),
    push: S.optional(S.Boolean),
    triage: S.optional(S.Boolean),
    pull: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "MinimalRepositoryPermissions",
}) as any as S.Schema<MinimalRepositoryPermissions>;

/** Code Of Conduct */
export interface CodeOfConduct {
  key: string;
  name: string;
  url: string;
  body?: string;
  html_url: string | null;
}
export const CodeOfConduct = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.String,
    name: S.String,
    url: S.String,
    body: S.optional(S.String),
    html_url: S.NullOr(S.String),
  }),
).annotate({ identifier: "CodeOfConduct" }) as any as S.Schema<CodeOfConduct>;

export interface MinimalRepositoryLicense {
  key?: string;
  name?: string;
  spdx_id?: string;
  url?: string | null;
  node_id?: string;
}
export const MinimalRepositoryLicense = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.optional(S.String),
    name: S.optional(S.String),
    spdx_id: S.optional(S.String),
    url: S.optional(S.NullOr(S.String)),
    node_id: S.optional(S.String),
  }),
).annotate({
  identifier: "MinimalRepositoryLicense",
}) as any as S.Schema<MinimalRepositoryLicense>;

export type SecurityAndAnalysisAdvancedSecurityStatus = "enabled" | "disabled";
export const SecurityAndAnalysisAdvancedSecurityStatus = /*@__PURE__*/ S.String;

/** Enable or disable GitHub Advanced Security for the repository. For standalone Code Scanning or Secret Protection products, this parameter cannot be used. */
export interface SecurityAndAnalysisAdvancedSecurity {
  status?: SecurityAndAnalysisAdvancedSecurityStatus;
}
export const SecurityAndAnalysisAdvancedSecurity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    status: S.optional(SecurityAndAnalysisAdvancedSecurityStatus),
  }),
).annotate({
  identifier: "SecurityAndAnalysisAdvancedSecurity",
}) as any as S.Schema<SecurityAndAnalysisAdvancedSecurity>;

export type SecurityAndAnalysisCodeSecurityStatus = "enabled" | "disabled";
export const SecurityAndAnalysisCodeSecurityStatus = /*@__PURE__*/ S.String;

export interface SecurityAndAnalysisCodeSecurity {
  status?: SecurityAndAnalysisCodeSecurityStatus;
}
export const SecurityAndAnalysisCodeSecurity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    status: S.optional(SecurityAndAnalysisCodeSecurityStatus),
  }),
).annotate({
  identifier: "SecurityAndAnalysisCodeSecurity",
}) as any as S.Schema<SecurityAndAnalysisCodeSecurity>;

/** The enablement status of Dependabot security updates for the repository. */
export type SecurityAndAnalysisDependabotSecurityUpdatesStatus =
  | "enabled"
  | "disabled";
export const SecurityAndAnalysisDependabotSecurityUpdatesStatus =
  /*@__PURE__*/ S.String;

/** Enable or disable Dependabot security updates for the repository. */
export interface SecurityAndAnalysisDependabotSecurityUpdates {
  /** The enablement status of Dependabot security updates for the repository. */
  status?: SecurityAndAnalysisDependabotSecurityUpdatesStatus;
}
export const SecurityAndAnalysisDependabotSecurityUpdates =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      status: S.optional(SecurityAndAnalysisDependabotSecurityUpdatesStatus),
    }),
  ).annotate({
    identifier: "SecurityAndAnalysisDependabotSecurityUpdates",
  }) as any as S.Schema<SecurityAndAnalysisDependabotSecurityUpdates>;

export type SecurityAndAnalysisSecretScanningStatus = "enabled" | "disabled";
export const SecurityAndAnalysisSecretScanningStatus = /*@__PURE__*/ S.String;

export interface SecurityAndAnalysisSecretScanning {
  status?: SecurityAndAnalysisSecretScanningStatus;
}
export const SecurityAndAnalysisSecretScanning = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    status: S.optional(SecurityAndAnalysisSecretScanningStatus),
  }),
).annotate({
  identifier: "SecurityAndAnalysisSecretScanning",
}) as any as S.Schema<SecurityAndAnalysisSecretScanning>;

export type SecurityAndAnalysisSecretScanningPushProtectionStatus =
  | "enabled"
  | "disabled";
export const SecurityAndAnalysisSecretScanningPushProtectionStatus =
  /*@__PURE__*/ S.String;

export interface SecurityAndAnalysisSecretScanningPushProtection {
  status?: SecurityAndAnalysisSecretScanningPushProtectionStatus;
}
export const SecurityAndAnalysisSecretScanningPushProtection =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      status: S.optional(SecurityAndAnalysisSecretScanningPushProtectionStatus),
    }),
  ).annotate({
    identifier: "SecurityAndAnalysisSecretScanningPushProtection",
  }) as any as S.Schema<SecurityAndAnalysisSecretScanningPushProtection>;

export type SecurityAndAnalysisSecretScanningNonProviderPatternsStatus =
  | "enabled"
  | "disabled";
export const SecurityAndAnalysisSecretScanningNonProviderPatternsStatus =
  /*@__PURE__*/ S.String;

export interface SecurityAndAnalysisSecretScanningNonProviderPatterns {
  status?: SecurityAndAnalysisSecretScanningNonProviderPatternsStatus;
}
export const SecurityAndAnalysisSecretScanningNonProviderPatterns =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      status: S.optional(
        SecurityAndAnalysisSecretScanningNonProviderPatternsStatus,
      ),
    }),
  ).annotate({
    identifier: "SecurityAndAnalysisSecretScanningNonProviderPatterns",
  }) as any as S.Schema<SecurityAndAnalysisSecretScanningNonProviderPatterns>;

export type SecurityAndAnalysisSecretScanningAiDetectionStatus =
  | "enabled"
  | "disabled";
export const SecurityAndAnalysisSecretScanningAiDetectionStatus =
  /*@__PURE__*/ S.String;

export interface SecurityAndAnalysisSecretScanningAiDetection {
  status?: SecurityAndAnalysisSecretScanningAiDetectionStatus;
}
export const SecurityAndAnalysisSecretScanningAiDetection =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      status: S.optional(SecurityAndAnalysisSecretScanningAiDetectionStatus),
    }),
  ).annotate({
    identifier: "SecurityAndAnalysisSecretScanningAiDetection",
  }) as any as S.Schema<SecurityAndAnalysisSecretScanningAiDetection>;

export type SecurityAndAnalysisSecretScanningDelegatedAlertDismissalStatus =
  | "enabled"
  | "disabled";
export const SecurityAndAnalysisSecretScanningDelegatedAlertDismissalStatus =
  /*@__PURE__*/ S.String;

export interface SecurityAndAnalysisSecretScanningDelegatedAlertDismissal {
  status?: SecurityAndAnalysisSecretScanningDelegatedAlertDismissalStatus;
}
export const SecurityAndAnalysisSecretScanningDelegatedAlertDismissal =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      status: S.optional(
        SecurityAndAnalysisSecretScanningDelegatedAlertDismissalStatus,
      ),
    }),
  ).annotate({
    identifier: "SecurityAndAnalysisSecretScanningDelegatedAlertDismissal",
  }) as any as S.Schema<SecurityAndAnalysisSecretScanningDelegatedAlertDismissal>;

export type SecurityAndAnalysisSecretScanningDelegatedBypassStatus =
  | "enabled"
  | "disabled";
export const SecurityAndAnalysisSecretScanningDelegatedBypassStatus =
  /*@__PURE__*/ S.String;

export interface SecurityAndAnalysisSecretScanningDelegatedBypass {
  status?: SecurityAndAnalysisSecretScanningDelegatedBypassStatus;
}
export const SecurityAndAnalysisSecretScanningDelegatedBypass =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      status: S.optional(
        SecurityAndAnalysisSecretScanningDelegatedBypassStatus,
      ),
    }),
  ).annotate({
    identifier: "SecurityAndAnalysisSecretScanningDelegatedBypass",
  }) as any as S.Schema<SecurityAndAnalysisSecretScanningDelegatedBypass>;

/** The type of the bypass reviewer */
export type SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItemReviewerType =
  "TEAM" | "ROLE";
export const SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItemReviewerType =
  /*@__PURE__*/ S.String;

/** The bypass mode for the reviewer */
export type SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItemMode =
  "ALWAYS" | "EXEMPT";
export const SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItemMode =
  /*@__PURE__*/ S.String;

export interface SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItem {
  /** The ID of the team or role selected as a bypass reviewer */
  reviewer_id: number;
  /** The type of the bypass reviewer */
  reviewer_type: SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItemReviewerType;
  /** The bypass mode for the reviewer */
  mode?: SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItemMode;
}
export const SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      reviewer_id: S.Number,
      reviewer_type:
        SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItemReviewerType,
      mode: S.optional(
        SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItemMode,
      ),
    }),
  ).annotate({
    identifier:
      "SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItem",
  }) as any as S.Schema<SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItem>;

/** The bypass reviewers for secret scanning delegated bypass */
export type SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersList =
  Array<SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItem>;
export const SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersList =
  /*@__PURE__*/ S.Array(
    SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersItem,
  ) as any as S.Schema<SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersList>;

export interface SecurityAndAnalysisSecretScanningDelegatedBypassOptions {
  /** The bypass reviewers for secret scanning delegated bypass */
  reviewers?: SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersList;
}
export const SecurityAndAnalysisSecretScanningDelegatedBypassOptions =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      reviewers: S.optional(
        SecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewersList,
      ),
    }),
  ).annotate({
    identifier: "SecurityAndAnalysisSecretScanningDelegatedBypassOptions",
  }) as any as S.Schema<SecurityAndAnalysisSecretScanningDelegatedBypassOptions>;

export interface SecurityAndAnalysis {
  /** Enable or disable GitHub Advanced Security for the repository. For standalone Code Scanning or Secret Protection products, this parameter cannot be used. */
  advanced_security?: SecurityAndAnalysisAdvancedSecurity;
  code_security?: SecurityAndAnalysisCodeSecurity;
  /** Enable or disable Dependabot security updates for the repository. */
  dependabot_security_updates?: SecurityAndAnalysisDependabotSecurityUpdates;
  secret_scanning?: SecurityAndAnalysisSecretScanning;
  secret_scanning_push_protection?: SecurityAndAnalysisSecretScanningPushProtection;
  secret_scanning_non_provider_patterns?: SecurityAndAnalysisSecretScanningNonProviderPatterns;
  secret_scanning_ai_detection?: SecurityAndAnalysisSecretScanningAiDetection;
  secret_scanning_delegated_alert_dismissal?: SecurityAndAnalysisSecretScanningDelegatedAlertDismissal;
  secret_scanning_delegated_bypass?: SecurityAndAnalysisSecretScanningDelegatedBypass;
  secret_scanning_delegated_bypass_options?: SecurityAndAnalysisSecretScanningDelegatedBypassOptions;
}
export const SecurityAndAnalysis = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    advanced_security: S.optional(SecurityAndAnalysisAdvancedSecurity),
    code_security: S.optional(SecurityAndAnalysisCodeSecurity),
    dependabot_security_updates: S.optional(
      SecurityAndAnalysisDependabotSecurityUpdates,
    ),
    secret_scanning: S.optional(SecurityAndAnalysisSecretScanning),
    secret_scanning_push_protection: S.optional(
      SecurityAndAnalysisSecretScanningPushProtection,
    ),
    secret_scanning_non_provider_patterns: S.optional(
      SecurityAndAnalysisSecretScanningNonProviderPatterns,
    ),
    secret_scanning_ai_detection: S.optional(
      SecurityAndAnalysisSecretScanningAiDetection,
    ),
    secret_scanning_delegated_alert_dismissal: S.optional(
      SecurityAndAnalysisSecretScanningDelegatedAlertDismissal,
    ),
    secret_scanning_delegated_bypass: S.optional(
      SecurityAndAnalysisSecretScanningDelegatedBypass,
    ),
    secret_scanning_delegated_bypass_options: S.optional(
      SecurityAndAnalysisSecretScanningDelegatedBypassOptions,
    ),
  }),
).annotate({
  identifier: "SecurityAndAnalysis",
}) as any as S.Schema<SecurityAndAnalysis>;

/** The custom properties that were defined for the repository. The keys are the custom property names, and the values are the corresponding custom property values. */
export type MinimalRepositoryCustomPropertiesMap = {
  [key: string]: unknown | undefined;
};
export const MinimalRepositoryCustomPropertiesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<MinimalRepositoryCustomPropertiesMap>;

/** Minimal Repository */
export interface MinimalRepository {
  id: number;
  node_id: string;
  name: string;
  full_name: string;
  owner: SimpleUser;
  private: boolean;
  html_url: string;
  description: string | null;
  fork: boolean;
  url: string;
  archive_url: string;
  assignees_url: string;
  blobs_url: string;
  branches_url: string;
  collaborators_url: string;
  comments_url: string;
  commits_url: string;
  compare_url: string;
  contents_url: string;
  contributors_url: string;
  deployments_url: string;
  downloads_url: string;
  events_url: string;
  forks_url: string;
  git_commits_url: string;
  git_refs_url: string;
  git_tags_url: string;
  git_url?: string;
  issue_comment_url: string;
  issue_events_url: string;
  issues_url: string;
  keys_url: string;
  labels_url: string;
  languages_url: string;
  merges_url: string;
  milestones_url: string;
  notifications_url: string;
  pulls_url: string;
  releases_url: string;
  ssh_url?: string;
  stargazers_url: string;
  statuses_url: string;
  subscribers_url: string;
  subscription_url: string;
  tags_url: string;
  teams_url: string;
  trees_url: string;
  clone_url?: string;
  mirror_url?: string | null;
  hooks_url: string;
  svn_url?: string;
  homepage?: string | null;
  language?: string | null;
  forks_count?: number;
  stargazers_count?: number;
  watchers_count?: number;
  /** The size of the repository, in kilobytes. Size is calculated hourly. When a repository is initially created, the size is 0. */
  size?: number;
  default_branch?: string;
  open_issues_count?: number;
  is_template?: boolean;
  topics?: MinimalRepositoryTopicsList;
  has_issues?: boolean;
  has_projects?: boolean;
  has_wiki?: boolean;
  has_pages?: boolean;
  has_downloads?: boolean;
  has_discussions?: boolean;
  has_pull_requests?: boolean;
  /** The policy controlling who can create pull requests: all or collaborators_only. */
  pull_request_creation_policy?: MinimalRepositoryPullRequestCreationPolicy;
  archived?: boolean;
  disabled?: boolean;
  visibility?: string;
  pushed_at?: string | null;
  created_at?: string | null;
  updated_at?: string | null;
  permissions?: MinimalRepositoryPermissions;
  role_name?: string;
  temp_clone_token?: string;
  delete_branch_on_merge?: boolean;
  subscribers_count?: number;
  network_count?: number;
  code_of_conduct?: CodeOfConduct;
  license?: MinimalRepositoryLicense | null;
  forks?: number;
  open_issues?: number;
  watchers?: number;
  allow_forking?: boolean;
  web_commit_signoff_required?: boolean;
  security_and_analysis?: SecurityAndAnalysis | null;
  /** The custom properties that were defined for the repository. The keys are the custom property names, and the values are the corresponding custom property values. */
  custom_properties?: MinimalRepositoryCustomPropertiesMap;
}
export const MinimalRepository = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    node_id: S.String,
    name: S.String,
    full_name: S.String,
    owner: SimpleUser,
    private: S.Boolean,
    html_url: S.String,
    description: S.NullOr(S.String),
    fork: S.Boolean,
    url: S.String,
    archive_url: S.String,
    assignees_url: S.String,
    blobs_url: S.String,
    branches_url: S.String,
    collaborators_url: S.String,
    comments_url: S.String,
    commits_url: S.String,
    compare_url: S.String,
    contents_url: S.String,
    contributors_url: S.String,
    deployments_url: S.String,
    downloads_url: S.String,
    events_url: S.String,
    forks_url: S.String,
    git_commits_url: S.String,
    git_refs_url: S.String,
    git_tags_url: S.String,
    git_url: S.optional(S.String),
    issue_comment_url: S.String,
    issue_events_url: S.String,
    issues_url: S.String,
    keys_url: S.String,
    labels_url: S.String,
    languages_url: S.String,
    merges_url: S.String,
    milestones_url: S.String,
    notifications_url: S.String,
    pulls_url: S.String,
    releases_url: S.String,
    ssh_url: S.optional(S.String),
    stargazers_url: S.String,
    statuses_url: S.String,
    subscribers_url: S.String,
    subscription_url: S.String,
    tags_url: S.String,
    teams_url: S.String,
    trees_url: S.String,
    clone_url: S.optional(S.String),
    mirror_url: S.optional(S.NullOr(S.String)),
    hooks_url: S.String,
    svn_url: S.optional(S.String),
    homepage: S.optional(S.NullOr(S.String)),
    language: S.optional(S.NullOr(S.String)),
    forks_count: S.optional(S.Number),
    stargazers_count: S.optional(S.Number),
    watchers_count: S.optional(S.Number),
    size: S.optional(S.Number),
    default_branch: S.optional(S.String),
    open_issues_count: S.optional(S.Number),
    is_template: S.optional(S.Boolean),
    topics: S.optional(MinimalRepositoryTopicsList),
    has_issues: S.optional(S.Boolean),
    has_projects: S.optional(S.Boolean),
    has_wiki: S.optional(S.Boolean),
    has_pages: S.optional(S.Boolean),
    has_downloads: S.optional(S.Boolean),
    has_discussions: S.optional(S.Boolean),
    has_pull_requests: S.optional(S.Boolean),
    pull_request_creation_policy: S.optional(
      MinimalRepositoryPullRequestCreationPolicy,
    ),
    archived: S.optional(S.Boolean),
    disabled: S.optional(S.Boolean),
    visibility: S.optional(S.String),
    pushed_at: S.optional(S.NullOr(S.String)),
    created_at: S.optional(S.NullOr(S.String)),
    updated_at: S.optional(S.NullOr(S.String)),
    permissions: S.optional(MinimalRepositoryPermissions),
    role_name: S.optional(S.String),
    temp_clone_token: S.optional(S.String),
    delete_branch_on_merge: S.optional(S.Boolean),
    subscribers_count: S.optional(S.Number),
    network_count: S.optional(S.Number),
    code_of_conduct: S.optional(CodeOfConduct),
    license: S.optional(S.NullOr(MinimalRepositoryLicense)),
    forks: S.optional(S.Number),
    open_issues: S.optional(S.Number),
    watchers: S.optional(S.Number),
    allow_forking: S.optional(S.Boolean),
    web_commit_signoff_required: S.optional(S.Boolean),
    security_and_analysis: S.optional(S.NullOr(SecurityAndAnalysis)),
    custom_properties: S.optional(MinimalRepositoryCustomPropertiesMap),
  }),
).annotate({
  identifier: "MinimalRepository",
}) as any as S.Schema<MinimalRepository>;

/** Information about the Git author */
export interface SimpleCommitAuthor {
  /** Name of the commit's author */
  name: string;
  /** Git email address of the commit's author */
  email: string;
}
export const SimpleCommitAuthor = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    email: S.String,
  }),
).annotate({
  identifier: "SimpleCommitAuthor",
}) as any as S.Schema<SimpleCommitAuthor>;

/** Information about the Git committer */
export interface SimpleCommitCommitter {
  /** Name of the commit's committer */
  name: string;
  /** Git email address of the commit's committer */
  email: string;
}
export const SimpleCommitCommitter = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    email: S.String,
  }),
).annotate({
  identifier: "SimpleCommitCommitter",
}) as any as S.Schema<SimpleCommitCommitter>;

/** A commit. */
export interface SimpleCommit {
  /** SHA for the commit */
  id: string;
  /** SHA for the commit's tree */
  tree_id: string;
  /** Message describing the purpose of the commit */
  message: string;
  /** Timestamp of the commit */
  timestamp: string;
  /** Information about the Git author */
  author: SimpleCommitAuthor | null;
  /** Information about the Git committer */
  committer: SimpleCommitCommitter | null;
}
export const SimpleCommit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    tree_id: S.String,
    message: S.String,
    timestamp: S.String,
    author: S.NullOr(SimpleCommitAuthor),
    committer: S.NullOr(SimpleCommitCommitter),
  }),
).annotate({ identifier: "SimpleCommit" }) as any as S.Schema<SimpleCommit>;

/** A suite of checks performed on the code of a given code change */
export interface CheckSuite {
  id: number;
  node_id: string;
  head_branch: string | null;
  /** The SHA of the head commit that is being checked. */
  head_sha: string;
  /** The phase of the lifecycle that the check suite is currently in. Statuses of waiting, requested, and pending are reserved for GitHub Actions check suites. */
  status: CheckSuiteStatus | null;
  conclusion: CheckSuiteConclusion | null;
  url: string | null;
  before: string | null;
  after: string | null;
  pull_requests: CheckSuitePullRequestsList | null;
  app: NullableIntegration | null;
  repository: MinimalRepository;
  created_at: string | null;
  updated_at: string | null;
  head_commit: SimpleCommit;
  latest_check_runs_count: number;
  check_runs_url: string;
  rerequestable?: boolean;
  runs_rerequestable?: boolean;
}
export const CheckSuite = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    node_id: S.String,
    head_branch: S.NullOr(S.String),
    head_sha: S.String,
    status: S.NullOr(CheckSuiteStatus),
    conclusion: S.NullOr(CheckSuiteConclusion),
    url: S.NullOr(S.String),
    before: S.NullOr(S.String),
    after: S.NullOr(S.String),
    pull_requests: S.NullOr(CheckSuitePullRequestsList),
    app: S.NullOr(NullableIntegration),
    repository: MinimalRepository,
    created_at: S.NullOr(S.String),
    updated_at: S.NullOr(S.String),
    head_commit: SimpleCommit,
    latest_check_runs_count: S.Number,
    check_runs_url: S.String,
    rerequestable: S.optional(S.Boolean),
    runs_rerequestable: S.optional(S.Boolean),
  }),
).annotate({ identifier: "CheckSuite" }) as any as S.Schema<CheckSuite>;

export interface GetRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The unique identifier of the check run. */
  check_run_id: number;
}
export const GetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    check_run_id: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/check-runs/{check_run_id}",
      code: 200,
    }),
  ),
).annotate({ identifier: "GetRequest" }) as any as S.Schema<GetRequest>;

export interface GetSuiteRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The unique identifier of the check suite. */
  check_suite_id: number;
}
export const GetSuiteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    check_suite_id: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/check-suites/{check_suite_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetSuiteRequest",
}) as any as S.Schema<GetSuiteRequest>;

export interface ListAnnotationsRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The unique identifier of the check run. */
  check_run_id: number;
  /** The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)." */
  per_page?: number;
  /** The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)." */
  page?: number;
}
export const ListAnnotationsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    check_run_id: S.Number.pipe(T.Label()),
    per_page: S.optional(S.Number.pipe(T.Query())),
    page: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/check-runs/{check_run_id}/annotations",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListAnnotationsRequest",
}) as any as S.Schema<ListAnnotationsRequest>;

/** Check Annotation */
export interface CheckAnnotation {
  path: string;
  start_line: number;
  end_line: number;
  start_column: number | null;
  end_column: number | null;
  annotation_level: string | null;
  title: string | null;
  message: string | null;
  raw_details: string | null;
  blob_href: string;
}
export const CheckAnnotation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    start_line: S.Number,
    end_line: S.Number,
    start_column: S.NullOr(S.Number),
    end_column: S.NullOr(S.Number),
    annotation_level: S.NullOr(S.String),
    title: S.NullOr(S.String),
    message: S.NullOr(S.String),
    raw_details: S.NullOr(S.String),
    blob_href: S.String,
  }),
).annotate({
  identifier: "CheckAnnotation",
}) as any as S.Schema<CheckAnnotation>;

export type ListAnnotationsResponseBodyList = Array<CheckAnnotation>;
export const ListAnnotationsResponseBodyList = /*@__PURE__*/ S.Array(
  CheckAnnotation,
) as any as S.Schema<ListAnnotationsResponseBodyList>;

export type ListAnnotationsResponse = ListAnnotationsResponseBodyList;
export const ListAnnotationsResponse = /*@__PURE__*/ S.suspend(() =>
  ListAnnotationsResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListAnnotationsResponse",
}) as any as S.Schema<ListAnnotationsResponse>;

export type ListForRefRequestStatus = "queued" | "in_progress" | "completed";
export const ListForRefRequestStatus = /*@__PURE__*/ S.String;

export type ListForRefRequestFilter = "latest" | "all";
export const ListForRefRequestFilter = /*@__PURE__*/ S.String;

export interface ListForRefRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation. */
  ref: string;
  /** Returns check runs with the specified `name`. */
  check_name?: string;
  /** Returns check runs with the specified `status`. */
  status?: ListForRefRequestStatus | (string & {});
  /** Filters check runs by their `completed_at` timestamp. `latest` returns the most recent check runs. */
  filter?: ListForRefRequestFilter | (string & {});
  /** The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)." */
  per_page?: number;
  /** The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)." */
  page?: number;
  app_id?: number;
}
export const ListForRefRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    ref: S.String.pipe(T.Label()),
    check_name: S.optional(S.String.pipe(T.Query())),
    status: S.optional(ListForRefRequestStatus.pipe(T.Query())),
    filter: S.optional(ListForRefRequestFilter.pipe(T.Query())),
    per_page: S.optional(S.Number.pipe(T.Query())),
    page: S.optional(S.Number.pipe(T.Query())),
    app_id: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/commits/{ref}/check-runs",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListForRefRequest",
}) as any as S.Schema<ListForRefRequest>;

export type ListForRefResponseCheckRunsList = Array<CheckRun>;
export const ListForRefResponseCheckRunsList = /*@__PURE__*/ S.Array(
  CheckRun,
) as any as S.Schema<ListForRefResponseCheckRunsList>;

export interface ListForRefResponse {
  total_count: number;
  check_runs: ListForRefResponseCheckRunsList;
}
export const ListForRefResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    total_count: S.Number,
    check_runs: ListForRefResponseCheckRunsList,
  }),
).annotate({
  identifier: "ListForRefResponse",
}) as any as S.Schema<ListForRefResponse>;

export type ListForSuiteRequestStatus = "queued" | "in_progress" | "completed";
export const ListForSuiteRequestStatus = /*@__PURE__*/ S.String;

export type ListForSuiteRequestFilter = "latest" | "all";
export const ListForSuiteRequestFilter = /*@__PURE__*/ S.String;

export interface ListForSuiteRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The unique identifier of the check suite. */
  check_suite_id: number;
  /** Returns check runs with the specified `name`. */
  check_name?: string;
  /** Returns check runs with the specified `status`. */
  status?: ListForSuiteRequestStatus | (string & {});
  /** Filters check runs by their `completed_at` timestamp. `latest` returns the most recent check runs. */
  filter?: ListForSuiteRequestFilter | (string & {});
  /** The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)." */
  per_page?: number;
  /** The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)." */
  page?: number;
}
export const ListForSuiteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    check_suite_id: S.Number.pipe(T.Label()),
    check_name: S.optional(S.String.pipe(T.Query())),
    status: S.optional(ListForSuiteRequestStatus.pipe(T.Query())),
    filter: S.optional(ListForSuiteRequestFilter.pipe(T.Query())),
    per_page: S.optional(S.Number.pipe(T.Query())),
    page: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/check-suites/{check_suite_id}/check-runs",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListForSuiteRequest",
}) as any as S.Schema<ListForSuiteRequest>;

export type ListForSuiteResponseCheckRunsList = Array<CheckRun>;
export const ListForSuiteResponseCheckRunsList = /*@__PURE__*/ S.Array(
  CheckRun,
) as any as S.Schema<ListForSuiteResponseCheckRunsList>;

export interface ListForSuiteResponse {
  total_count: number;
  check_runs: ListForSuiteResponseCheckRunsList;
}
export const ListForSuiteResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    total_count: S.Number,
    check_runs: ListForSuiteResponseCheckRunsList,
  }),
).annotate({
  identifier: "ListForSuiteResponse",
}) as any as S.Schema<ListForSuiteResponse>;

export interface ListSuitesForRefRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The commit reference. Can be a commit SHA, branch name (`heads/BRANCH_NAME`), or tag name (`tags/TAG_NAME`). For more information, see "[Git References](https://git-scm.com/book/en/v2/Git-Internals-Git-References)" in the Git documentation. */
  ref: string;
  /** Filters check suites by GitHub App `id`. */
  app_id?: number;
  /** Returns check runs with the specified `name`. */
  check_name?: string;
  /** The number of results per page (max 100). For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)." */
  per_page?: number;
  /** The page number of the results to fetch. For more information, see "[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api)." */
  page?: number;
}
export const ListSuitesForRefRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    ref: S.String.pipe(T.Label()),
    app_id: S.optional(S.Number.pipe(T.Query())),
    check_name: S.optional(S.String.pipe(T.Query())),
    per_page: S.optional(S.Number.pipe(T.Query())),
    page: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/repos/{owner}/{repo}/commits/{ref}/check-suites",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListSuitesForRefRequest",
}) as any as S.Schema<ListSuitesForRefRequest>;

export type ListSuitesForRefResponseCheckSuitesList = Array<CheckSuite>;
export const ListSuitesForRefResponseCheckSuitesList = /*@__PURE__*/ S.Array(
  CheckSuite,
) as any as S.Schema<ListSuitesForRefResponseCheckSuitesList>;

export interface ListSuitesForRefResponse {
  total_count: number;
  check_suites: ListSuitesForRefResponseCheckSuitesList;
}
export const ListSuitesForRefResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    total_count: S.Number,
    check_suites: ListSuitesForRefResponseCheckSuitesList,
  }),
).annotate({
  identifier: "ListSuitesForRefResponse",
}) as any as S.Schema<ListSuitesForRefResponse>;

export interface RerequestRunRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The unique identifier of the check run. */
  check_run_id: number;
}
export const RerequestRunRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    check_run_id: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/repos/{owner}/{repo}/check-runs/{check_run_id}/rerequest",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RerequestRunRequest",
}) as any as S.Schema<RerequestRunRequest>;

export type RerequestRunResponse = unknown;
export const RerequestRunResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "RerequestRunResponse",
}) as any as S.Schema<RerequestRunResponse>;

export interface RerequestSuiteRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The unique identifier of the check suite. */
  check_suite_id: number;
}
export const RerequestSuiteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    check_suite_id: S.Number.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/repos/{owner}/{repo}/check-suites/{check_suite_id}/rerequest",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RerequestSuiteRequest",
}) as any as S.Schema<RerequestSuiteRequest>;

export type RerequestSuiteResponse = unknown;
export const RerequestSuiteResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "RerequestSuiteResponse",
}) as any as S.Schema<RerequestSuiteResponse>;

export interface SetSuitesPreferencesRequestAutoTriggerChecksItem {
  /** The `id` of the GitHub App. */
  app_id: number;
  /** Set to `true` to enable automatic creation of CheckSuite events upon pushes to the repository, or `false` to disable them. */
  setting: boolean;
}
export const SetSuitesPreferencesRequestAutoTriggerChecksItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      app_id: S.Number,
      setting: S.Boolean,
    }),
  ).annotate({
    identifier: "SetSuitesPreferencesRequestAutoTriggerChecksItem",
  }) as any as S.Schema<SetSuitesPreferencesRequestAutoTriggerChecksItem>;

/** Enables or disables automatic creation of CheckSuite events upon pushes to the repository. Enabled by default. */
export type SetSuitesPreferencesRequestAutoTriggerChecksList =
  Array<SetSuitesPreferencesRequestAutoTriggerChecksItem>;
export const SetSuitesPreferencesRequestAutoTriggerChecksList =
  /*@__PURE__*/ S.Array(
    SetSuitesPreferencesRequestAutoTriggerChecksItem,
  ) as any as S.Schema<SetSuitesPreferencesRequestAutoTriggerChecksList>;

export interface SetSuitesPreferencesRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** Enables or disables automatic creation of CheckSuite events upon pushes to the repository. Enabled by default. */
  auto_trigger_checks?: SetSuitesPreferencesRequestAutoTriggerChecksList;
}
export const SetSuitesPreferencesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    auto_trigger_checks: S.optional(
      SetSuitesPreferencesRequestAutoTriggerChecksList,
    ),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/repos/{owner}/{repo}/check-suites/preferences",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SetSuitesPreferencesRequest",
}) as any as S.Schema<SetSuitesPreferencesRequest>;

export interface CheckSuitePreferencePreferencesAutoTriggerChecksItem {
  app_id: number;
  setting: boolean;
}
export const CheckSuitePreferencePreferencesAutoTriggerChecksItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      app_id: S.Number,
      setting: S.Boolean,
    }),
  ).annotate({
    identifier: "CheckSuitePreferencePreferencesAutoTriggerChecksItem",
  }) as any as S.Schema<CheckSuitePreferencePreferencesAutoTriggerChecksItem>;

export type CheckSuitePreferencePreferencesAutoTriggerChecksList =
  Array<CheckSuitePreferencePreferencesAutoTriggerChecksItem>;
export const CheckSuitePreferencePreferencesAutoTriggerChecksList =
  /*@__PURE__*/ S.Array(
    CheckSuitePreferencePreferencesAutoTriggerChecksItem,
  ) as any as S.Schema<CheckSuitePreferencePreferencesAutoTriggerChecksList>;

export interface CheckSuitePreferencePreferences {
  auto_trigger_checks?: CheckSuitePreferencePreferencesAutoTriggerChecksList;
}
export const CheckSuitePreferencePreferences = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auto_trigger_checks: S.optional(
      CheckSuitePreferencePreferencesAutoTriggerChecksList,
    ),
  }),
).annotate({
  identifier: "CheckSuitePreferencePreferences",
}) as any as S.Schema<CheckSuitePreferencePreferences>;

/** Check suite configuration preferences for a repository. */
export interface CheckSuitePreference {
  preferences: CheckSuitePreferencePreferences;
  repository: MinimalRepository;
}
export const CheckSuitePreference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    preferences: CheckSuitePreferencePreferences,
    repository: MinimalRepository,
  }),
).annotate({
  identifier: "CheckSuitePreference",
}) as any as S.Schema<CheckSuitePreference>;

/** The current status of the check run. Only GitHub Actions can set a status of `waiting`, `pending`, or `requested`. */
export type UpdateRequestStatus =
  | "queued"
  | "in_progress"
  | "completed"
  | "waiting"
  | "requested"
  | "pending";
export const UpdateRequestStatus = /*@__PURE__*/ S.String;

/** **Required if you provide `completed_at` or a `status` of `completed`**. The final conclusion of the check. **Note:** Providing `conclusion` will automatically set the `status` parameter to `completed`. You cannot change a check run conclusion to `stale`, only GitHub can set this. */
export type UpdateRequestConclusion =
  | "action_required"
  | "cancelled"
  | "failure"
  | "neutral"
  | "success"
  | "skipped"
  | "stale"
  | "timed_out";
export const UpdateRequestConclusion = /*@__PURE__*/ S.String;

/** The level of the annotation. */
export type UpdateRequestOutputAnnotationsItemAnnotationLevel =
  | "notice"
  | "warning"
  | "failure";
export const UpdateRequestOutputAnnotationsItemAnnotationLevel =
  /*@__PURE__*/ S.String;

export interface UpdateRequestOutputAnnotationsItem {
  /** The path of the file to add an annotation to. For example, `assets/css/main.css`. */
  path: string;
  /** The start line of the annotation. Line numbers start at 1. */
  start_line: number;
  /** The end line of the annotation. */
  end_line: number;
  /** The start column of the annotation. Annotations only support `start_column` and `end_column` on the same line. Omit this parameter if `start_line` and `end_line` have different values. Column numbers start at 1. */
  start_column?: number;
  /** The end column of the annotation. Annotations only support `start_column` and `end_column` on the same line. Omit this parameter if `start_line` and `end_line` have different values. */
  end_column?: number;
  /** The level of the annotation. */
  annotation_level:
    | UpdateRequestOutputAnnotationsItemAnnotationLevel
    | (string & {});
  /** A short description of the feedback for these lines of code. The maximum size is 64 KB. */
  message: string;
  /** The title that represents the annotation. The maximum size is 255 characters. */
  title?: string;
  /** Details about this annotation. The maximum size is 64 KB. */
  raw_details?: string;
}
export const UpdateRequestOutputAnnotationsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    start_line: S.Number,
    end_line: S.Number,
    start_column: S.optional(S.Number),
    end_column: S.optional(S.Number),
    annotation_level: UpdateRequestOutputAnnotationsItemAnnotationLevel,
    message: S.String,
    title: S.optional(S.String),
    raw_details: S.optional(S.String),
  }),
).annotate({
  identifier: "UpdateRequestOutputAnnotationsItem",
}) as any as S.Schema<UpdateRequestOutputAnnotationsItem>;

/** Adds information from your analysis to specific lines of code. Annotations are visible in GitHub's pull request UI. Annotations are visible in GitHub's pull request UI. The Checks API limits the number of annotations to a maximum of 50 per API request. To create more than 50 annotations, you have to make multiple requests to the [Update a check run](https://docs.github.com/rest/checks/runs#update-a-check-run) endpoint. Each time you update the check run, annotations are appended to the list of annotations that already exist for the check run. GitHub Actions are limited to 10 warning annotations and 10 error annotations per step. For details about annotations in the UI, see "[About status checks](https://docs.github.com/articles/about-status-checks#checks)". */
export type UpdateRequestOutputAnnotationsList =
  Array<UpdateRequestOutputAnnotationsItem>;
export const UpdateRequestOutputAnnotationsList = /*@__PURE__*/ S.Array(
  UpdateRequestOutputAnnotationsItem,
) as any as S.Schema<UpdateRequestOutputAnnotationsList>;

export type UpdateRequestOutputImagesItem = CreateRequestOutputImagesItem;
export const UpdateRequestOutputImagesItem = CreateRequestOutputImagesItem;

/** Adds images to the output displayed in the GitHub pull request UI. */
export type UpdateRequestOutputImagesList =
  Array<CreateRequestOutputImagesItem>;
export const UpdateRequestOutputImagesList = /*@__PURE__*/ S.Array(
  CreateRequestOutputImagesItem,
) as any as S.Schema<UpdateRequestOutputImagesList>;

/** Check runs can accept a variety of data in the `output` object, including a `title` and `summary` and can optionally provide descriptive details about the run. */
export interface UpdateRequestOutput {
  /** **Required**. */
  title?: string;
  /** Can contain Markdown. */
  summary: string;
  /** Can contain Markdown. */
  text?: string;
  /** Adds information from your analysis to specific lines of code. Annotations are visible in GitHub's pull request UI. Annotations are visible in GitHub's pull request UI. The Checks API limits the number of annotations to a maximum of 50 per API request. To create more than 50 annotations, you have to make multiple requests to the [Update a check run](https://docs.github.com/rest/checks/runs#update-a-check-run) endpoint. Each time you update the check run, annotations are appended to the list of annotations that already exist for the check run. GitHub Actions are limited to 10 warning annotations and 10 error annotations per step. For details about annotations in the UI, see "[About status checks](https://docs.github.com/articles/about-status-checks#checks)". */
  annotations?: UpdateRequestOutputAnnotationsList;
  /** Adds images to the output displayed in the GitHub pull request UI. */
  images?: UpdateRequestOutputImagesList;
}
export const UpdateRequestOutput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    title: S.optional(S.String),
    summary: S.String,
    text: S.optional(S.String),
    annotations: S.optional(UpdateRequestOutputAnnotationsList),
    images: S.optional(UpdateRequestOutputImagesList),
  }),
).annotate({
  identifier: "UpdateRequestOutput",
}) as any as S.Schema<UpdateRequestOutput>;

export type UpdateRequestActionsItem = CreateRequestActionsItem;
export const UpdateRequestActionsItem = CreateRequestActionsItem;

/** Possible further actions the integrator can perform, which a user may trigger. Each action includes a `label`, `identifier` and `description`. A maximum of three actions are accepted. To learn more about check runs and requested actions, see "[Check runs and requested actions](https://docs.github.com/rest/guides/using-the-rest-api-to-interact-with-checks#check-runs-and-requested-actions)." */
export type UpdateRequestActionsList = Array<CreateRequestActionsItem>;
export const UpdateRequestActionsList = /*@__PURE__*/ S.Array(
  CreateRequestActionsItem,
) as any as S.Schema<UpdateRequestActionsList>;

export interface UpdateRequest {
  /** The account owner of the repository. The name is not case sensitive. */
  owner: string;
  /** The name of the repository without the `.git` extension. The name is not case sensitive. */
  repo: string;
  /** The unique identifier of the check run. */
  check_run_id: number;
  /** The name of the check. For example, "code-coverage". */
  name?: string;
  /** The URL of the integrator's site that has the full details of the check. */
  details_url?: string;
  /** A reference for the run on the integrator's system. */
  external_id?: string;
  /** This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. */
  started_at?: string;
  /** The current status of the check run. Only GitHub Actions can set a status of `waiting`, `pending`, or `requested`. */
  status?: UpdateRequestStatus | (string & {});
  /** **Required if you provide `completed_at` or a `status` of `completed`**. The final conclusion of the check. **Note:** Providing `conclusion` will automatically set the `status` parameter to `completed`. You cannot change a check run conclusion to `stale`, only GitHub can set this. */
  conclusion?: UpdateRequestConclusion | (string & {});
  /** The time the check completed. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. */
  completed_at?: string;
  /** Check runs can accept a variety of data in the `output` object, including a `title` and `summary` and can optionally provide descriptive details about the run. */
  output?: UpdateRequestOutput;
  /** Possible further actions the integrator can perform, which a user may trigger. Each action includes a `label`, `identifier` and `description`. A maximum of three actions are accepted. To learn more about check runs and requested actions, see "[Check runs and requested actions](https://docs.github.com/rest/guides/using-the-rest-api-to-interact-with-checks#check-runs-and-requested-actions)." */
  actions?: UpdateRequestActionsList;
}
export const UpdateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    owner: S.String.pipe(T.Label()),
    repo: S.String.pipe(T.Label()),
    check_run_id: S.Number.pipe(T.Label()),
    name: S.optional(S.String),
    details_url: S.optional(S.String),
    external_id: S.optional(S.String),
    started_at: S.optional(S.String),
    status: S.optional(UpdateRequestStatus),
    conclusion: S.optional(UpdateRequestConclusion),
    completed_at: S.optional(S.String),
    output: S.optional(UpdateRequestOutput),
    actions: S.optional(UpdateRequestActionsList),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/repos/{owner}/{repo}/check-runs/{check_run_id}",
      code: 200,
    }),
  ),
).annotate({ identifier: "UpdateRequest" }) as any as S.Schema<UpdateRequest>;

export type CreateError = GithubOpError;
/** Create a check run Creates a new check run for a specific commit in a repository. To create a check run, you must use a GitHub App. OAuth apps and authenticated users are not able to create a check suite. In a check suite, GitHub limits the number of check runs with the same name to 1000. Once these check runs exceed 1000, GitHub will start to automatically delete older check runs. > [!NOTE] > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array. */
export const create: API.OperationMethod<
  CreateRequest,
  CheckRun,
  CreateError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateRequest,
  output: CheckRun,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type CreateSuiteError = GithubOpError;
/** Create a check suite Creates a check suite manually. By default, check suites are automatically created when you create a [check run](https://docs.github.com/rest/checks/runs). You only need to use this endpoint for manually creating check suites when you've disabled automatic creation using "[Update repository preferences for check suites](https://docs.github.com/rest/checks/suites#update-repository-preferences-for-check-suites)". > [!NOTE] > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array and a `null` value for `head_branch`. OAuth apps and personal access tokens (classic) cannot use this endpoint. */
export const createSuite: API.OperationMethod<
  CreateSuiteRequest,
  CheckSuite,
  CreateSuiteError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSuiteRequest,
  output: CheckSuite,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetError = GithubOpError;
/** Get a check run Gets a single check run using its `id`. > [!NOTE] > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository. */
export const get: API.OperationMethod<
  GetRequest,
  CheckRun,
  GetError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetRequest,
  output: CheckRun,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type GetSuiteError = GithubOpError;
/** Get a check suite Gets a single check suite using its `id`. > [!NOTE] > The Checks API only looks for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array and a `null` value for `head_branch`. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository. */
export const getSuite: API.OperationMethod<
  GetSuiteRequest,
  CheckSuite,
  GetSuiteError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetSuiteRequest,
  output: CheckSuite,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListAnnotationsError = GithubOpError;
/** List check run annotations Lists annotations for a check run using the annotation `id`. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository. */
export const listAnnotations: API.OperationMethod<
  ListAnnotationsRequest,
  ListAnnotationsResponse,
  ListAnnotationsError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListAnnotationsRequest,
  output: ListAnnotationsResponse,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListForRefError = GithubOpError;
/** List check runs for a Git reference Lists check runs for a commit ref. The `ref` can be a SHA, branch name, or a tag name. > [!NOTE] > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array. If there are more than 1000 check suites on a single git reference, this endpoint will limit check runs to the 1000 most recent check suites. To iterate over all possible check runs, use the [List check suites for a Git reference](https://docs.github.com/rest/reference/checks#list-check-suites-for-a-git-reference) endpoint and provide the `check_suite_id` parameter to the [List check runs in a check suite](https://docs.github.com/rest/reference/checks#list-check-runs-in-a-check-suite) endpoint. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository. */
export const listForRef: API.OperationMethod<
  ListForRefRequest,
  ListForRefResponse,
  ListForRefError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListForRefRequest,
  output: ListForRefResponse,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListForSuiteError = GithubOpError;
/** List check runs in a check suite Lists check runs for a check suite using its `id`. > [!NOTE] > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository. */
export const listForSuite: API.OperationMethod<
  ListForSuiteRequest,
  ListForSuiteResponse,
  ListForSuiteError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListForSuiteRequest,
  output: ListForSuiteResponse,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type ListSuitesForRefError = GithubOpError;
/** List check suites for a Git reference Lists check suites for a commit `ref`. The `ref` can be a SHA, branch name, or a tag name. > [!NOTE] > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array and a `null` value for `head_branch`. OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint on a private repository. */
export const listSuitesForRef: API.OperationMethod<
  ListSuitesForRefRequest,
  ListSuitesForRefResponse,
  ListSuitesForRefError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListSuitesForRefRequest,
  output: ListSuitesForRefResponse,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type RerequestRunError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | GithubOpError;
/** Rerequest a check run Triggers GitHub to rerequest an existing check run, without pushing new code to a repository. This endpoint will trigger the [`check_run` webhook](https://docs.github.com/webhooks/event-payloads/#check_run) event with the action `rerequested`. When a check run is `rerequested`, the `status` of the check suite it belongs to is reset to `queued` and the `conclusion` is cleared. The check run itself is not updated. GitHub apps recieving the [`check_run` webhook](https://docs.github.com/webhooks/event-payloads/#check_run) with the `rerequested` action should then decide if the check run should be reset or updated and call the [update `check_run` endpoint](https://docs.github.com/rest/checks/runs#update-a-check-run) to update the check_run if desired. For more information about how to re-run GitHub Actions jobs, see "[Re-run a job from a workflow run](https://docs.github.com/rest/actions/workflow-runs#re-run-a-job-from-a-workflow-run)". */
export const rerequestRun: API.OperationMethod<
  RerequestRunRequest,
  RerequestRunResponse,
  RerequestRunError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RerequestRunRequest,
  output: RerequestRunResponse,
  errors: [Forbidden, NotFound, UnprocessableEntity],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type RerequestSuiteError = GithubOpError;
/** Rerequest a check suite Triggers GitHub to rerequest an existing check suite, without pushing new code to a repository. This endpoint will trigger the [`check_suite` webhook](https://docs.github.com/webhooks/event-payloads/#check_suite) event with the action `rerequested`. When a check suite is `rerequested`, its `status` is reset to `queued` and the `conclusion` is cleared. */
export const rerequestSuite: API.OperationMethod<
  RerequestSuiteRequest,
  RerequestSuiteResponse,
  RerequestSuiteError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RerequestSuiteRequest,
  output: RerequestSuiteResponse,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type SetSuitesPreferencesError = GithubOpError;
/** Update repository preferences for check suites Changes the default automatic flow when creating check suites. By default, a check suite is automatically created each time code is pushed to a repository. When you disable the automatic creation of check suites, you can manually [Create a check suite](https://docs.github.com/rest/checks/suites#create-a-check-suite). You must have admin permissions in the repository to set preferences for check suites. */
export const setSuitesPreferences: API.OperationMethod<
  SetSuitesPreferencesRequest,
  CheckSuitePreference,
  SetSuitesPreferencesError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SetSuitesPreferencesRequest,
  output: CheckSuitePreference,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));

export type UpdateError = GithubOpError;
/** Update a check run Updates a check run for a specific commit in a repository. > [!NOTE] > The endpoints to manage checks only look for pushes in the repository where the check suite or check run were created. Pushes to a branch in a forked repository are not detected and return an empty `pull_requests` array. OAuth apps and personal access tokens (classic) cannot use this endpoint. */
export const update: API.OperationMethod<
  UpdateRequest,
  CheckRun,
  UpdateError,
  GithubOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateRequest,
  output: CheckRun,
  errors: [],
  protocol: GithubProtocol,
  retry: Retry.Retry,
}));
