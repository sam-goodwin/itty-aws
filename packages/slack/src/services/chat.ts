// AUTO-GENERATED by scripts/generate.ts from .generated-specs (docs.slack.dev JSON twins). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  SlackProtocol,
  type SlackOpError,
  type SlackOpContext,
} from "../protocol.ts";
import { slackPaginate } from "../pagination.ts";
import { SlackError, SlackRateLimited } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { SlackOpError, SlackOpContext };

/** Array of streaming chunks. */
export type AppendStreamRequestChunksList = Array<unknown>;
export const AppendStreamRequestChunksList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<AppendStreamRequestChunksList>;

export interface AppendStreamRequest {
  /** An encoded ID that represents a channel, private group, or DM */
  channel: string;
  /** Array of streaming chunks. */
  chunks?: AppendStreamRequestChunksList;
  /** The timestamp of the streaming message. */
  ts: string;
  /** Accepts message text formatted in markdown. Limit this field to 12,000 characters. This text is what will be appended to the message received so far. */
  markdown_text: string;
}
export const AppendStreamRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel: S.String,
    chunks: S.optional(AppendStreamRequestChunksList),
    ts: S.String,
    markdown_text: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/chat.appendStream", code: 200 })),
).annotate({
  identifier: "AppendStreamRequest",
}) as any as S.Schema<AppendStreamRequest>;

export interface AppendStreamResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  channel: string;
  ts: string;
}
export const AppendStreamResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    channel: S.String,
    ts: S.String,
  }),
).annotate({
  identifier: "AppendStreamResponse",
}) as any as S.Schema<AppendStreamResponse>;

export interface DeleteRequest {
  /** (Legacy) Pass true to delete the message as the authed user with `chat:write:user` scope. Bot users in this context are considered authed users. See [legacy `as_user` parameter](#legacy_as_user) below. */
  as_user?: boolean;
  /** Channel containing the message to be deleted. */
  channel: string;
  /** Timestamp of the message to be deleted. */
  ts: string;
}
export const DeleteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    as_user: S.optional(S.Boolean),
    channel: S.String,
    ts: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/chat.delete", code: 200 })),
).annotate({ identifier: "DeleteRequest" }) as any as S.Schema<DeleteRequest>;

export interface DeleteResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  channel: string;
  ts: string;
}
export const DeleteResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    channel: S.String,
    ts: S.String,
  }),
).annotate({ identifier: "DeleteResponse" }) as any as S.Schema<DeleteResponse>;

export interface DeleteScheduledMessageRequest {
  /** Pass true to delete the message as the authed user with `chat:write:user` scope. Bot users in this context are considered authed users. If unused or false, the message will be deleted with `chat:write:bot` scope. */
  as_user?: boolean;
  /** The channel the scheduled_message is posting to */
  channel: string;
  /** `scheduled_message_id` returned from call to chat.scheduleMessage */
  scheduled_message_id: string;
}
export const DeleteScheduledMessageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    as_user: S.optional(S.Boolean),
    channel: S.String,
    scheduled_message_id: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/chat.deleteScheduledMessage", code: 200 }),
  ),
).annotate({
  identifier: "DeleteScheduledMessageRequest",
}) as any as S.Schema<DeleteScheduledMessageRequest>;

export interface DeleteScheduledMessageResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
}
export const DeleteScheduledMessageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
  }),
).annotate({
  identifier: "DeleteScheduledMessageResponse",
}) as any as S.Schema<DeleteScheduledMessageResponse>;

export interface GetPermalinkRequest {
  /** The ID of the conversation or channel containing the message */
  channel: string;
  /** A message's `ts` value, uniquely identifying it within a channel */
  message_ts: string;
}
export const GetPermalinkRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel: S.String.pipe(T.Query()),
    message_ts: S.String.pipe(T.Query()),
  }).pipe(T.Http({ method: "GET", uri: "/chat.getPermalink", code: 200 })),
).annotate({
  identifier: "GetPermalinkRequest",
}) as any as S.Schema<GetPermalinkRequest>;

export interface GetPermalinkResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  permalink: string;
  channel: string;
}
export const GetPermalinkResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    permalink: S.String,
    channel: S.String,
  }),
).annotate({
  identifier: "GetPermalinkResponse",
}) as any as S.Schema<GetPermalinkResponse>;

export interface MeMessageRequest {
  /** Channel to send message to. Can be a public channel, private group or IM channel. Can be an encoded ID, or a name. */
  channel: string;
  /** Text of the message to send. */
  text: string;
}
export const MeMessageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel: S.String,
    text: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/chat.meMessage", code: 200 })),
).annotate({
  identifier: "MeMessageRequest",
}) as any as S.Schema<MeMessageRequest>;

export interface MeMessageResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  ts?: string;
  channel?: string;
}
export const MeMessageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    ts: S.optional(S.String),
    channel: S.optional(S.String),
  }),
).annotate({
  identifier: "MeMessageResponse",
}) as any as S.Schema<MeMessageResponse>;

export type PostEphemeralRequestAttachmentsCase1ItemMap = {
  [key: string]: unknown | undefined;
};
export const PostEphemeralRequestAttachmentsCase1ItemMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<PostEphemeralRequestAttachmentsCase1ItemMap>;

export type PostEphemeralRequestAttachmentsCase1List =
  Array<PostEphemeralRequestAttachmentsCase1ItemMap>;
export const PostEphemeralRequestAttachmentsCase1List = /*@__PURE__*/ S.Array(
  PostEphemeralRequestAttachmentsCase1ItemMap,
) as any as S.Schema<PostEphemeralRequestAttachmentsCase1List>;

/** A JSON-based array of structured attachments, presented as a URL-encoded string. */
export type PostEphemeralRequestAttachments =
  | string
  | PostEphemeralRequestAttachmentsCase1List;
export const PostEphemeralRequestAttachments = /*@__PURE__*/ S.Unknown.pipe(
  T.UnionCases([[], []]),
);

export type PostEphemeralRequestBlocksCase1ItemMap = {
  [key: string]: unknown | undefined;
};
export const PostEphemeralRequestBlocksCase1ItemMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<PostEphemeralRequestBlocksCase1ItemMap>;

export type PostEphemeralRequestBlocksCase1List =
  Array<PostEphemeralRequestBlocksCase1ItemMap>;
export const PostEphemeralRequestBlocksCase1List = /*@__PURE__*/ S.Array(
  PostEphemeralRequestBlocksCase1ItemMap,
) as any as S.Schema<PostEphemeralRequestBlocksCase1List>;

/** A JSON-based array of structured blocks, presented as a URL-encoded string. */
export type PostEphemeralRequestBlocks =
  | string
  | PostEphemeralRequestBlocksCase1List;
export const PostEphemeralRequestBlocks = /*@__PURE__*/ S.Unknown.pipe(
  T.UnionCases([[], []]),
);

export type PostEphemeralRequestParse =
  | ""
  | "none"
  | "full"
  | "mrkdwn"
  | "false";
export const PostEphemeralRequestParse = /*@__PURE__*/ S.String;

export interface PostEphemeralRequest {
  /** (Legacy) Pass true to post the message as the authed user. Defaults to true if the chat:write:bot scope is not included. Otherwise, defaults to false. */
  as_user?: boolean;
  /** A JSON-based array of structured attachments, presented as a URL-encoded string. */
  attachments?: PostEphemeralRequestAttachments;
  /** A JSON-based array of structured blocks, presented as a URL-encoded string. */
  blocks?: PostEphemeralRequestBlocks;
  /** Channel, private group, or IM channel to send message to. Can be an encoded ID, or a name. */
  channel: string;
  /** Emoji to use as the icon for this message. Overrides `icon_url`. */
  icon_emoji?: string;
  /** URL to an image to use as the icon for this message. */
  icon_url?: string;
  /** Find and link channel names and usernames. */
  link_names?: boolean;
  /** Accepts message text formatted in markdown. This argument should not be used in conjunction with `blocks` or `text`. Limit this field to 12,000 characters. */
  markdown_text?: string;
  /** JSON object with an `entities` array of work object entity metadata, presented as a URL-encoded string. Only entity metadata is supported: ephemeral messages are not persisted and never dispatch `message_metadata_*` events, so `event_type`/`event_payload` message metadata is ignored here. Each entity requires `entity_type`, `entity_payload`, `external_ref`, and `url`. */
  metadata?: string | null;
  /** Change how messages are treated. Defaults to `none`. See [below](#formatting). */
  parse?: PostEphemeralRequestParse | (string & {}) | null;
  /** How this field works and whether it is required depends on other fields you use in your API call. [See below](#text_usage) for more detail. */
  text?: string | null;
  /** Provide another message's `ts` value to post this message in a thread. Avoid using a reply's `ts` value; use its parent's value instead. Ephemeral messages in threads are only shown if there is already an active thread. */
  thread_ts?: string | null;
  /** `id` of the user who will receive the ephemeral message. The user should be in the channel specified by the `channel` argument. */
  user: string;
  /** Set your bot's user name. */
  username?: string;
}
export const PostEphemeralRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    as_user: S.optional(S.Boolean),
    attachments: S.optional(PostEphemeralRequestAttachments),
    blocks: S.optional(PostEphemeralRequestBlocks),
    channel: S.String,
    icon_emoji: S.optional(S.String),
    icon_url: S.optional(S.String),
    link_names: S.optional(S.Boolean),
    markdown_text: S.optional(S.String),
    metadata: S.optional(S.NullOr(S.String)),
    parse: S.optional(S.NullOr(PostEphemeralRequestParse)),
    text: S.optional(S.NullOr(S.String)),
    thread_ts: S.optional(S.NullOr(S.String)),
    user: S.String,
    username: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/chat.postEphemeral", code: 200 })),
).annotate({
  identifier: "PostEphemeralRequest",
}) as any as S.Schema<PostEphemeralRequest>;

export interface PostEphemeralResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  message_ts: string;
}
export const PostEphemeralResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    message_ts: S.String,
  }),
).annotate({
  identifier: "PostEphemeralResponse",
}) as any as S.Schema<PostEphemeralResponse>;

export type PostMessageRequestAttachmentsCase1ItemMap = {
  [key: string]: unknown | undefined;
};
export const PostMessageRequestAttachmentsCase1ItemMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<PostMessageRequestAttachmentsCase1ItemMap>;

export type PostMessageRequestAttachmentsCase1List =
  Array<PostMessageRequestAttachmentsCase1ItemMap>;
export const PostMessageRequestAttachmentsCase1List = /*@__PURE__*/ S.Array(
  PostMessageRequestAttachmentsCase1ItemMap,
) as any as S.Schema<PostMessageRequestAttachmentsCase1List>;

/** A JSON-based array of structured attachments, presented as a URL-encoded string. */
export type PostMessageRequestAttachments =
  | string
  | PostMessageRequestAttachmentsCase1List;
export const PostMessageRequestAttachments = /*@__PURE__*/ S.Unknown.pipe(
  T.UnionCases([[], []]),
);

export type PostMessageRequestBlocksCase1ItemMap = {
  [key: string]: unknown | undefined;
};
export const PostMessageRequestBlocksCase1ItemMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<PostMessageRequestBlocksCase1ItemMap>;

export type PostMessageRequestBlocksCase1List =
  Array<PostMessageRequestBlocksCase1ItemMap>;
export const PostMessageRequestBlocksCase1List = /*@__PURE__*/ S.Array(
  PostMessageRequestBlocksCase1ItemMap,
) as any as S.Schema<PostMessageRequestBlocksCase1List>;

/** A JSON-based array of structured blocks, presented as a URL-encoded string. */
export type PostMessageRequestBlocks =
  | string
  | PostMessageRequestBlocksCase1List;
export const PostMessageRequestBlocks = /*@__PURE__*/ S.Unknown.pipe(
  T.UnionCases([[], []]),
);

export interface PostMessageRequest {
  /** (Legacy) Pass true to post the message as the authed user instead of as a bot. Defaults to false. Can only be used by classic apps. See [legacy `as_user` parameter](#legacy_as_user) below. */
  as_user?: boolean;
  /** A JSON-based array of structured attachments, presented as a URL-encoded string. */
  attachments?: PostMessageRequestAttachments;
  /** A JSON-based array of structured blocks, presented as a URL-encoded string. */
  blocks?: PostMessageRequestBlocks;
  /** An encoded ID or channel name that represents a channel, private group, or IM channel to send the message to. See [below](#channels) for more details. */
  channel: string;
  /** This field represents the timestamp of the draft's last update at the time this API is called. If the current message is a draft, this field can be provided to ensure synchronization with the server. */
  current_draft_last_updated_ts?: string;
  /** Emoji to use as the icon for this message. Overrides `icon_url`. */
  icon_emoji?: string | null;
  /** URL to an image to use as the icon for this message. */
  icon_url?: string | null;
  /** Find and link user groups. No longer supports linking individual users; use syntax shown in [Mentioning Users](/messaging/formatting-message-text#mentioning-users) instead. */
  link_names?: boolean;
  /** Accepts message text formatted in markdown. This argument should not be used in conjunction with `blocks` or `text`. Limit this field to 12,000 characters. */
  markdown_text?: string;
  /** JSON object with event_type and event_payload fields, presented as a URL-encoded string. You can also provide Work Object entity metadata using this parameter. Metadata you post to Slack is accessible to any app or user who is a member of that workspace. */
  metadata?: string | null;
  /** Disable Slack markup parsing by setting to `false`. Enabled by default. */
  mrkdwn?: boolean;
  /** Change how messages are treated. See [below](#formatting). */
  parse?: string | null;
  /** Used in conjunction with `thread_ts` and indicates whether reply should be made visible to everyone in the channel or conversation. Defaults to `false`. */
  reply_broadcast?: boolean;
  /** How this field works and whether it is required depends on other fields you use in your API call. [See below](#text_usage) for more detail. */
  text?: string | null;
  /** Provide another message's `ts` value to make this message a reply. Avoid using a reply's `ts` value; use its parent instead. */
  thread_ts?: string | null;
  /** Pass true to enable unfurling of primarily text-based content. */
  unfurl_links?: boolean;
  /** Pass false to disable unfurling of media content. */
  unfurl_media?: boolean;
  /** Set your bot's user name. */
  username?: string | null;
}
export const PostMessageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    as_user: S.optional(S.Boolean),
    attachments: S.optional(PostMessageRequestAttachments),
    blocks: S.optional(PostMessageRequestBlocks),
    channel: S.String,
    current_draft_last_updated_ts: S.optional(S.String),
    icon_emoji: S.optional(S.NullOr(S.String)),
    icon_url: S.optional(S.NullOr(S.String)),
    link_names: S.optional(S.Boolean),
    markdown_text: S.optional(S.String),
    metadata: S.optional(S.NullOr(S.String)),
    mrkdwn: S.optional(S.Boolean),
    parse: S.optional(S.NullOr(S.String)),
    reply_broadcast: S.optional(S.Boolean),
    text: S.optional(S.NullOr(S.String)),
    thread_ts: S.optional(S.NullOr(S.String)),
    unfurl_links: S.optional(S.Boolean),
    unfurl_media: S.optional(S.Boolean),
    username: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "POST", uri: "/chat.postMessage", code: 200 })),
).annotate({
  identifier: "PostMessageRequest",
}) as any as S.Schema<PostMessageRequest>;

export interface PostMessageResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  channel: string;
  ts: string;
  message?: unknown;
  /** Pass true if duplicate message is attempted to be sent. */
  is_duplicate?: boolean;
}
export const PostMessageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    channel: S.String,
    ts: S.String,
    message: S.optional(S.Unknown),
    is_duplicate: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "PostMessageResponse",
}) as any as S.Schema<PostMessageResponse>;

export interface ScheduledMessagesListRequest {
  /** The channel of the scheduled messages */
  channel?: string;
  /** For pagination purposes, this is the `cursor` value returned from a previous call to `chat.scheduledmessages.list` indicating where you want to start this call from. */
  cursor?: string;
  /** A Unix timestamp of the latest value in the time range */
  latest?: string;
  /** Maximum number of original entries to return. */
  limit?: number;
  /** A Unix timestamp of the oldest value in the time range */
  oldest?: string;
  /** encoded team id to list channels in, required if org token is used */
  team_id?: string;
}
export const ScheduledMessagesListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel: S.optional(S.String),
    cursor: S.optional(S.String),
    latest: S.optional(S.String),
    limit: S.optional(S.Number),
    oldest: S.optional(S.String),
    team_id: S.optional(S.String),
  }).pipe(
    T.Http({ method: "POST", uri: "/chat.scheduledMessages.list", code: 200 }),
  ),
).annotate({
  identifier: "ScheduledMessagesListRequest",
}) as any as S.Schema<ScheduledMessagesListRequest>;

export interface ScheduledMessagesListResponseScheduledMessagesItem {
  id: string;
  channel_id: string;
  post_at: number;
  date_created: number;
  text?: string;
}
export const ScheduledMessagesListResponseScheduledMessagesItem =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      id: S.String,
      channel_id: S.String,
      post_at: S.Number,
      date_created: S.Number,
      text: S.optional(S.String),
    }),
  ).annotate({
    identifier: "ScheduledMessagesListResponseScheduledMessagesItem",
  }) as any as S.Schema<ScheduledMessagesListResponseScheduledMessagesItem>;

export type ScheduledMessagesListResponseScheduledMessagesList =
  Array<ScheduledMessagesListResponseScheduledMessagesItem>;
export const ScheduledMessagesListResponseScheduledMessagesList =
  /*@__PURE__*/ S.Array(
    ScheduledMessagesListResponseScheduledMessagesItem,
  ) as any as S.Schema<ScheduledMessagesListResponseScheduledMessagesList>;

/** Pagination metadata. An empty `next_cursor` means the last page. */
export interface ScheduledMessagesListResponseResponseMetadata {
  /** Cursor for the next page — pass as `cursor` on the next call. */
  next_cursor?: string;
}
export const ScheduledMessagesListResponseResponseMetadata =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      next_cursor: S.optional(S.String),
    }),
  ).annotate({
    identifier: "ScheduledMessagesListResponseResponseMetadata",
  }) as any as S.Schema<ScheduledMessagesListResponseResponseMetadata>;

export interface ScheduledMessagesListResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  scheduled_messages: ScheduledMessagesListResponseScheduledMessagesList;
  /** Pagination metadata. An empty `next_cursor` means the last page. */
  response_metadata?: ScheduledMessagesListResponseResponseMetadata;
}
export const ScheduledMessagesListResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    scheduled_messages: ScheduledMessagesListResponseScheduledMessagesList,
    response_metadata: S.optional(
      ScheduledMessagesListResponseResponseMetadata,
    ),
  }),
).annotate({
  identifier: "ScheduledMessagesListResponse",
}) as any as S.Schema<ScheduledMessagesListResponse>;

export type ScheduleMessageRequestAttachmentsCase1ItemMap = {
  [key: string]: unknown | undefined;
};
export const ScheduleMessageRequestAttachmentsCase1ItemMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<ScheduleMessageRequestAttachmentsCase1ItemMap>;

export type ScheduleMessageRequestAttachmentsCase1List =
  Array<ScheduleMessageRequestAttachmentsCase1ItemMap>;
export const ScheduleMessageRequestAttachmentsCase1List = /*@__PURE__*/ S.Array(
  ScheduleMessageRequestAttachmentsCase1ItemMap,
) as any as S.Schema<ScheduleMessageRequestAttachmentsCase1List>;

/** A JSON-based array of structured attachments, presented as a URL-encoded string. */
export type ScheduleMessageRequestAttachments =
  | string
  | ScheduleMessageRequestAttachmentsCase1List;
export const ScheduleMessageRequestAttachments = /*@__PURE__*/ S.Unknown.pipe(
  T.UnionCases([[], []]),
);

export type ScheduleMessageRequestBlocksCase1ItemMap = {
  [key: string]: unknown | undefined;
};
export const ScheduleMessageRequestBlocksCase1ItemMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ScheduleMessageRequestBlocksCase1ItemMap>;

export type ScheduleMessageRequestBlocksCase1List =
  Array<ScheduleMessageRequestBlocksCase1ItemMap>;
export const ScheduleMessageRequestBlocksCase1List = /*@__PURE__*/ S.Array(
  ScheduleMessageRequestBlocksCase1ItemMap,
) as any as S.Schema<ScheduleMessageRequestBlocksCase1List>;

/** A JSON-based array of structured blocks, presented as a URL-encoded string. */
export type ScheduleMessageRequestBlocks =
  | string
  | ScheduleMessageRequestBlocksCase1List;
export const ScheduleMessageRequestBlocks = /*@__PURE__*/ S.Unknown.pipe(
  T.UnionCases([[], []]),
);

/** Change how messages are treated. See [chat.postMessage](chat.postMessage#formatting). */
export type ScheduleMessageRequestParse = "none" | "full";
export const ScheduleMessageRequestParse = /*@__PURE__*/ S.String;

export interface ScheduleMessageRequest {
  /** Set to `true` to post the message as the authed user, instead of as a bot. Defaults to false. Cannot be used by [new Slack apps](/quickstart). See [chat.postMessage](chat.postMessage#authorship). */
  as_user?: boolean;
  /** A JSON-based array of structured attachments, presented as a URL-encoded string. */
  attachments?: ScheduleMessageRequestAttachments;
  /** A JSON-based array of structured blocks, presented as a URL-encoded string. */
  blocks?: ScheduleMessageRequestBlocks;
  /** Channel, private group, or DM channel to send message to. Can be an encoded ID, or a name. See [below](#channels) for more details. */
  channel: string;
  /** Find and link user groups. No longer supports linking individual users; use syntax shown in [Mentioning Users](/messaging/formatting-message-text#mentioning-users) instead. */
  link_names?: boolean;
  /** Accepts message text formatted in markdown. This argument should not be used in conjunction with `blocks` or `text`. Limit this field to 12,000 characters. */
  markdown_text?: string;
  /** Change how messages are treated. See [chat.postMessage](chat.postMessage#formatting). */
  parse?: ScheduleMessageRequestParse | (string & {});
  /** Unix timestamp representing the future time the message should post to Slack. */
  post_at: number;
  /** Used in conjunction with `thread_ts` and indicates whether reply should be made visible to everyone in the channel or conversation. Defaults to `false`. */
  reply_broadcast?: boolean;
  /** How this field works and whether it is required depends on other fields you use in your API call. [See below](#text_usage) for more detail. */
  text?: string;
  /** Provide another message's `ts` value to make this message a reply. Avoid using a reply's `ts` value; use its parent instead. */
  thread_ts?: string;
  /** Pass true to enable unfurling of primarily text-based content. */
  unfurl_links?: boolean;
  /** Pass false to disable unfurling of media content. */
  unfurl_media?: boolean;
  /** JSON object with event_type and event_payload fields, presented as a URL-encoded string. Metadata you post to Slack is accessible to any app or user who is a member of that workspace. */
  metadata?: string | null;
}
export const ScheduleMessageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    as_user: S.optional(S.Boolean),
    attachments: S.optional(ScheduleMessageRequestAttachments),
    blocks: S.optional(ScheduleMessageRequestBlocks),
    channel: S.String,
    link_names: S.optional(S.Boolean),
    markdown_text: S.optional(S.String),
    parse: S.optional(ScheduleMessageRequestParse),
    post_at: S.Number,
    reply_broadcast: S.optional(S.Boolean),
    text: S.optional(S.String),
    thread_ts: S.optional(S.String),
    unfurl_links: S.optional(S.Boolean),
    unfurl_media: S.optional(S.Boolean),
    metadata: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "POST", uri: "/chat.scheduleMessage", code: 200 })),
).annotate({
  identifier: "ScheduleMessageRequest",
}) as any as S.Schema<ScheduleMessageRequest>;

export type ScheduleMessageResponseMessageMap = {
  [key: string]: unknown | undefined;
};
export const ScheduleMessageResponseMessageMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ScheduleMessageResponseMessageMap>;

export interface ScheduleMessageResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  channel: string;
  post_at: number;
  scheduled_message_id: string;
  message: ScheduleMessageResponseMessageMap;
}
export const ScheduleMessageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    channel: S.String,
    post_at: S.Number,
    scheduled_message_id: S.String,
    message: ScheduleMessageResponseMessageMap,
  }),
).annotate({
  identifier: "ScheduleMessageResponse",
}) as any as S.Schema<ScheduleMessageResponse>;

/** Array of streaming chunks. */
export type StartStreamRequestChunksList = Array<unknown>;
export const StartStreamRequestChunksList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<StartStreamRequestChunksList>;

/** Specifies how tasks are displayed in the message. A `timeline` displays individual tasks with text in sequential order, `plan` displays all tasks together, with the first tasks's placement determining the placement of the rest of the tasks, and `dense` collapses consecutive tool calls into a single summarized task card. */
export type StartStreamRequestTaskDisplayMode = "timeline" | "plan" | "dense";
export const StartStreamRequestTaskDisplayMode = /*@__PURE__*/ S.String;

export interface StartStreamRequest {
  /** An encoded ID that represents a channel thread or DM. */
  channel: string;
  /** Array of streaming chunks. */
  chunks?: StartStreamRequestChunksList;
  /** Accepts message text formatted in markdown. Limit this field to 12,000 characters. */
  markdown_text?: string;
  /** Provide another message's `ts` value to reply to. Streamed messages should always be replies to a user request. */
  thread_ts: string;
  /** The encoded ID of the user to receive the streaming text. Required when streaming to channels. */
  recipient_user_id?: string;
  /** The encoded ID of the team the user receiving the streaming text belongs to. Required when streaming to channels. */
  recipient_team_id?: string;
  /** Specifies how tasks are displayed in the message. A `timeline` displays individual tasks with text in sequential order, `plan` displays all tasks together, with the first tasks's placement determining the placement of the rest of the tasks, and `dense` collapses consecutive tool calls into a single summarized task card. */
  task_display_mode?: StartStreamRequestTaskDisplayMode | (string & {});
  /** Emoji to use as the icon for this message. Overrides `icon_url`. */
  icon_emoji?: string;
  /** Image URL to use as the icon for this message. */
  icon_url?: string;
  /** The bot's username to display. */
  username?: string;
}
export const StartStreamRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel: S.String,
    chunks: S.optional(StartStreamRequestChunksList),
    markdown_text: S.optional(S.String),
    thread_ts: S.String,
    recipient_user_id: S.optional(S.String),
    recipient_team_id: S.optional(S.String),
    task_display_mode: S.optional(StartStreamRequestTaskDisplayMode),
    icon_emoji: S.optional(S.String),
    icon_url: S.optional(S.String),
    username: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/chat.startStream", code: 200 })),
).annotate({
  identifier: "StartStreamRequest",
}) as any as S.Schema<StartStreamRequest>;

export interface StartStreamResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  channel: string;
  /** The timestamp of the streaming message. */
  ts: string;
}
export const StartStreamResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    channel: S.String,
    ts: S.String,
  }),
).annotate({
  identifier: "StartStreamResponse",
}) as any as S.Schema<StartStreamResponse>;

/** Array of streaming chunks. */
export type StopStreamRequestChunksList = Array<unknown>;
export const StopStreamRequestChunksList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<StopStreamRequestChunksList>;

export interface StopStreamRequest {
  /** An encoded ID that represents a channel, private group, or DM */
  channel: string;
  /** Array of streaming chunks. */
  chunks?: StopStreamRequestChunksList;
  /** The timestamp of the streaming message. */
  ts: string;
  /** Accepts message text formatted in markdown. Limit this field to 12,000 characters. */
  markdown_text?: string;
  /** A list of blocks that will be rendered at the bottom of the finalized message. */
  blocks?: unknown;
  /** JSON object with event_type and event_payload fields, presented as a URL-encoded string. Metadata you post to Slack is accessible to any app or user who is a member of that workspace. */
  metadata?: string | null;
}
export const StopStreamRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel: S.String,
    chunks: S.optional(StopStreamRequestChunksList),
    ts: S.String,
    markdown_text: S.optional(S.String),
    blocks: S.optional(S.Unknown),
    metadata: S.optional(S.NullOr(S.String)),
  }).pipe(T.Http({ method: "POST", uri: "/chat.stopStream", code: 200 })),
).annotate({
  identifier: "StopStreamRequest",
}) as any as S.Schema<StopStreamRequest>;

export interface StopStreamResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  channel: string;
  ts: string;
  message: unknown;
}
export const StopStreamResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    channel: S.String,
    ts: S.String,
    message: S.Unknown,
  }),
).annotate({
  identifier: "StopStreamResponse",
}) as any as S.Schema<StopStreamResponse>;

/** The source of the link to unfurl. The source may either be `composer`, when the link is inside the message composer, or `conversations_history`, when the link has been posted to a conversation. */
export type UnfurlRequestSource = "composer" | "conversations_history";
export const UnfurlRequestSource = /*@__PURE__*/ S.String;

export interface UnfurlRequest {
  /** Channel ID of the message. Both `channel` and `ts` must be provided together, *or* `unfurl_id` and `source` must be provided together. Required for public channels. */
  channel?: string;
  /** Timestamp of the message to add unfurl behavior to. Required for public channels. */
  ts?: string;
  /** URL-encoded JSON map with keys set to URLs featured in the the message, pointing to their unfurl blocks or message attachments. Required for public channels. */
  unfurls?: string;
  /** Provide a simply-formatted string to send as an ephemeral message to the user as invitation to authenticate further and enable full unfurling behavior. Provides two buttons, `Not now` or `Never ask me again`. */
  user_auth_message?: string | null;
  /** Set to `true` or `1` to indicate the user must install your Slack app to trigger unfurls for this domain */
  user_auth_required?: boolean;
  /** Send users to this custom URL where they will complete authentication in your app to fully trigger unfurling. Value should be properly URL-encoded. */
  user_auth_url?: string | null;
  /** Provide a JSON based array of structured blocks presented as URL-encoded string to send as an ephemeral message to the user as invitation to authenticate further and enable full unfurling behavior */
  user_auth_blocks?: unknown;
  /** The ID of the link to unfurl. Both `unfurl_id` and `source` must be provided together, *or* `channel` and `ts` must be provided together. */
  unfurl_id?: string;
  /** The source of the link to unfurl. The source may either be `composer`, when the link is inside the message composer, or `conversations_history`, when the link has been posted to a conversation. */
  source?: UnfurlRequestSource | (string & {});
}
export const UnfurlRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel: S.optional(S.String),
    ts: S.optional(S.String),
    unfurls: S.optional(S.String),
    user_auth_message: S.optional(S.NullOr(S.String)),
    user_auth_required: S.optional(S.Boolean),
    user_auth_url: S.optional(S.NullOr(S.String)),
    user_auth_blocks: S.optional(S.Unknown),
    unfurl_id: S.optional(S.String),
    source: S.optional(UnfurlRequestSource),
  }).pipe(T.Http({ method: "POST", uri: "/chat.unfurl", code: 200 })),
).annotate({ identifier: "UnfurlRequest" }) as any as S.Schema<UnfurlRequest>;

export interface UnfurlResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
}
export const UnfurlResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
  }),
).annotate({ identifier: "UnfurlResponse" }) as any as S.Schema<UnfurlResponse>;

/** Pass true to update the message as the authed user. Bot users in this context are considered authed users. */
export type UpdateRequestAsUser = boolean | string;
export const UpdateRequestAsUser = /*@__PURE__*/ S.Unknown.pipe(
  T.UnionCases([[], []]),
);

export type UpdateRequestAttachmentsCase1ItemMap = {
  [key: string]: unknown | undefined;
};
export const UpdateRequestAttachmentsCase1ItemMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<UpdateRequestAttachmentsCase1ItemMap>;

export type UpdateRequestAttachmentsCase1List =
  Array<UpdateRequestAttachmentsCase1ItemMap>;
export const UpdateRequestAttachmentsCase1List = /*@__PURE__*/ S.Array(
  UpdateRequestAttachmentsCase1ItemMap,
) as any as S.Schema<UpdateRequestAttachmentsCase1List>;

/** A JSON-based array of structured attachments, presented as a URL-encoded string. */
export type UpdateRequestAttachments =
  | string
  | UpdateRequestAttachmentsCase1List;
export const UpdateRequestAttachments = /*@__PURE__*/ S.Unknown.pipe(
  T.UnionCases([[], []]),
);

export type UpdateRequestBlocksCase1ItemMap = {
  [key: string]: unknown | undefined;
};
export const UpdateRequestBlocksCase1ItemMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<UpdateRequestBlocksCase1ItemMap>;

export type UpdateRequestBlocksCase1List =
  Array<UpdateRequestBlocksCase1ItemMap>;
export const UpdateRequestBlocksCase1List = /*@__PURE__*/ S.Array(
  UpdateRequestBlocksCase1ItemMap,
) as any as S.Schema<UpdateRequestBlocksCase1List>;

/** A JSON-based array of structured blocks, presented as a URL-encoded string. */
export type UpdateRequestBlocks = string | UpdateRequestBlocksCase1List;
export const UpdateRequestBlocks = /*@__PURE__*/ S.Unknown.pipe(
  T.UnionCases([[], []]),
);

/** Find and link channel names and usernames. Defaults to `none`. If you do not specify a value for this field, the original value set for the message will be overwritten with the default, `none`. */
export type UpdateRequestLinkNames = boolean | string;
export const UpdateRequestLinkNames = /*@__PURE__*/ S.Unknown.pipe(
  T.UnionCases([[], []]),
);

/** Array of new file ids that will be sent with this message. */
export type UpdateRequestFileIdsList = Array<string>;
export const UpdateRequestFileIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpdateRequestFileIdsList>;

export interface UpdateRequest {
  /** Pass true to update the message as the authed user. Bot users in this context are considered authed users. */
  as_user?: UpdateRequestAsUser | null;
  /** A JSON-based array of structured attachments, presented as a URL-encoded string. */
  attachments?: UpdateRequestAttachments;
  /** A JSON-based array of structured attachments, presented as a URL-encoded string. */
  unfurled_attachments?: string | null;
  /** A JSON-based array of structured blocks, presented as a URL-encoded string. */
  blocks?: UpdateRequestBlocks;
  /** Accepts message text formatted in markdown. This argument should not be used in conjunction with `blocks` or `text`. Limit this field to 12,000 characters. */
  markdown_text?: string;
  /** JSON object with event_type and event_payload fields, presented as a URL-encoded string. If you don't include this field, the message's previous `metadata` will be retained. To remove previous `metadata`, include an empty object for this field. Metadata you post to Slack is accessible to any app or user who is a member of that workspace. */
  metadata?: string | null;
  /** Channel containing the message to be updated. For direct messages, ensure that this value is a DM ID (starts with `D`) instead of a User ID (starts with either `U` or `W`). */
  channel: string;
  /** Find and link channel names and usernames. Defaults to `none`. If you do not specify a value for this field, the original value set for the message will be overwritten with the default, `none`. */
  link_names?: UpdateRequestLinkNames | null;
  /** Change how messages are treated. Defaults to `client`, unlike `chat.postMessage`. Accepts either `none` or `full`. If you do not specify a value for this field, the original value set for the message will be overwritten with the default, `client`. */
  parse?: string | null;
  /** How this field works and whether it is required depends on other fields you use in your API call. [See below](#text_usage) for more detail. */
  text?: string | null;
  /** Timestamp of the message to be updated. */
  ts: string;
  /** Broadcast an existing thread reply to make it visible to everyone in the channel or conversation. */
  reply_broadcast?: boolean;
  /** Array of new file ids that will be sent with this message. */
  file_ids?: UpdateRequestFileIdsList;
}
export const UpdateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    as_user: S.optional(S.NullOr(UpdateRequestAsUser)),
    attachments: S.optional(UpdateRequestAttachments),
    unfurled_attachments: S.optional(S.NullOr(S.String)),
    blocks: S.optional(UpdateRequestBlocks),
    markdown_text: S.optional(S.String),
    metadata: S.optional(S.NullOr(S.String)),
    channel: S.String,
    link_names: S.optional(S.NullOr(UpdateRequestLinkNames)),
    parse: S.optional(S.NullOr(S.String)),
    text: S.optional(S.NullOr(S.String)),
    ts: S.String,
    reply_broadcast: S.optional(S.Boolean),
    file_ids: S.optional(UpdateRequestFileIdsList),
  }).pipe(T.Http({ method: "POST", uri: "/chat.update", code: 200 })),
).annotate({ identifier: "UpdateRequest" }) as any as S.Schema<UpdateRequest>;

export type UpdateResponseMessageAttachmentsItemMap = {
  [key: string]: unknown | undefined;
};
export const UpdateResponseMessageAttachmentsItemMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<UpdateResponseMessageAttachmentsItemMap>;

export type UpdateResponseMessageAttachmentsList =
  Array<UpdateResponseMessageAttachmentsItemMap>;
export const UpdateResponseMessageAttachmentsList = /*@__PURE__*/ S.Array(
  UpdateResponseMessageAttachmentsItemMap,
) as any as S.Schema<UpdateResponseMessageAttachmentsList>;

export type UpdateResponseMessageMetadataMap = {
  [key: string]: unknown | undefined;
};
export const UpdateResponseMessageMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<UpdateResponseMessageMetadataMap>;

export interface UpdateResponseMessage {
  text: string;
  blocks?: unknown;
  attachments?: UpdateResponseMessageAttachmentsList;
  metadata?: UpdateResponseMessageMetadataMap;
}
export const UpdateResponseMessage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    text: S.String,
    blocks: S.optional(S.Unknown),
    attachments: S.optional(UpdateResponseMessageAttachmentsList),
    metadata: S.optional(UpdateResponseMessageMetadataMap),
  }),
).annotate({
  identifier: "UpdateResponseMessage",
}) as any as S.Schema<UpdateResponseMessage>;

export interface UpdateResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  channel: string;
  ts: string;
  text: string;
  message: UpdateResponseMessage;
}
export const UpdateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    channel: S.String,
    ts: S.String,
    text: S.String,
    message: UpdateResponseMessage,
  }),
).annotate({ identifier: "UpdateResponse" }) as any as S.Schema<UpdateResponse>;

export type AppendStreamError = SlackOpError;
/** Appends text to an existing streaming conversation. Required scopes — bot: `chat:write` Rate limit tier: 4 Method-specific errors (the `error` slug on the SlackError): - `message_not_in_streaming_state` — The message is not in the streaming state. - `messaging_processing_failed` — Failed to process the message. - `as_user_not_supported` — The `as_user` parameter does not function with workspace apps. - `channel_not_found` — Value passed for `channel` was invalid. - `duplicate_channel_not_found` — Channel associated with `client_msg_id` was invalid. - `duplicate_message_not_found` — No duplicate message exists associated with `client_msg_id`. - `ekm_access_denied` — Your message couldn’t be sent because your admins have disabled sending messages to this channel. - `invalid_blocks` — Blocks submitted with this message are not valid. - `invalid_chunks` — Chunks submitted with this message are not valid. - `invalid_metadata_format` — Invalid metadata format provided - `invalid_metadata_schema` — Invalid metadata schema provided - `is_archived` — Channel has been archived. - `message_limit_exceeded` — Members on this team are sending too many messages. For more details, see https://slack.com/help/articles/115002422943-Usage-limits-for-free-workspaces - `messages_tab_disabled` — Messages tab for the app is disabled. - `metadata_must_be_sent_from_app` — Message metadata can only be posted or updated using an app-level token - `msg_blocks_too_long` — Blocks submitted with this message are too long. - `msg_blocks_too_many` — Max blocks exceeded, blocks are limited to 50 per message. - `metadata_too_large` — Metadata exceeds size limit - `no_text` — No message text provided - `not_in_channel` — Cannot post user messages to a channel they are not in. - `rate_limited` — Application has posted too many messages, [read the Rate Limit documentation](/apis/web-api/rate-limits) for more information - `restricted_action` — A workspace preference prevents the authenticated user from posting. - `restricted_action_non_threadable_channel` — Cannot post thread replies into a non_threadable channel. - `restricted_action_read_only_channel` — Cannot post any message into a read-only channel. - `restricted_action_thread_locked` — Cannot post replies to a thread that has been locked by admins. - `restricted_action_thread_only_channel` — Cannot post top-level messages into a thread-only channel. - `slack_connect_canvas_sharing_blocked` — Admin has disabled Canvas File sharing in all Slack Connect communications - `slack_connect_file_link_sharing_blocked` — Admin has disabled Slack File sharing in all Slack Connect communications - `slack_connect_lists_sharing_blocked` — Admin has disabled Lists sharing in all Slack Connect communications - `team_not_found` — This error occurs if, when using an org-wide token, the `channel_name` is passed instead of the `channel_id`. - `too_many_attachments` — Too many attachments were provided with this message. A maximum of 100 attachments are allowed on a message. - `too_many_contact_cards` — Too many contact_cards were provided with this message. A maximum of 10 contact cards are allowed on a message. - `message_not_found` — Message not found - `channel_type_not_supported` — Channel type not supported - `stopped_by_user` — The streaming message was stopped by the user and no further appends are accepted. - `message_not_owned_by_app` — The message is not owned by the app. See https://docs.slack.dev/reference/methods/chat.appendStream */
export const appendStream: API.OperationMethod<
  AppendStreamRequest,
  AppendStreamResponse,
  AppendStreamError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AppendStreamRequest,
  output: AppendStreamResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type DeleteError = SlackOpError;
/** Deletes a message. Required scopes — bot: `chat:write`; user: `chat:write` Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `cant_delete_message` — Authenticated user does not have permission to delete this message. - `channel_not_found` — Value passed for `channel` was invalid. - `message_not_found` — No message exists with the requested timestamp. See https://docs.slack.dev/reference/methods/chat.delete */
export const Delete: API.OperationMethod<
  DeleteRequest,
  DeleteResponse,
  DeleteError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteRequest,
  output: DeleteResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type DeleteScheduledMessageError = SlackOpError;
/** Deletes a pending scheduled message from the queue. Required scopes — bot: `chat:write`; user: `chat:write` Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `bad_token` — The provided `token` was invalid. - `channel_not_found` — The `channel` passed is either an invalid ID or does not exist. - `invalid_scheduled_message_id` — The `scheduled_message_id` passed is either an invalid ID, or the scheduled message it's referencing has already been sent or deleted. See https://docs.slack.dev/reference/methods/chat.deleteScheduledMessage */
export const deleteScheduledMessage: API.OperationMethod<
  DeleteScheduledMessageRequest,
  DeleteScheduledMessageResponse,
  DeleteScheduledMessageError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteScheduledMessageRequest,
  output: DeleteScheduledMessageResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type GetPermalinkError = SlackOpError;
/** Retrieve a permalink URL for a specific extant message Rate limit tier: 5 Method-specific errors (the `error` slug on the SlackError): - `channel_not_found` — Value passed for `channel` was invalid. - `message_not_found` — The message identified by `message_ts` could not be found. See https://docs.slack.dev/reference/methods/chat.getPermalink */
export const getPermalink: API.OperationMethod<
  GetPermalinkRequest,
  GetPermalinkResponse,
  GetPermalinkError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetPermalinkRequest,
  output: GetPermalinkResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type MeMessageError = SlackOpError;
/** Share a me message into a channel. Required scopes — bot: `chat:write`; user: `chat:write` Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `channel_not_found` — Value passed for `channel` was invalid. - `is_archived` — Channel has been archived. - `msg_too_long` — Message text is too long - `no_text` — No message text provided - `not_in_channel` — Cannot post user messages to a channel they are not in. - `rate_limited` — Application has posted too many messages, [read the Rate Limit documentation](/apis/web-api/rate-limits) for more information See https://docs.slack.dev/reference/methods/chat.meMessage */
export const meMessage: API.OperationMethod<
  MeMessageRequest,
  MeMessageResponse,
  MeMessageError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: MeMessageRequest,
  output: MeMessageResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type PostEphemeralError = SlackOpError;
/** Sends an ephemeral message to a user in a channel. Required scopes — bot: `chat:write`; user: `chat:write` Rate limit tier: 4 Method-specific errors (the `error` slug on the SlackError): - `channel_not_found` — Value passed for `channel` was invalid. - `invalid_blocks` — Blocks submitted with this message are not valid - `invalid_blocks_format` — The `blocks` is not a valid JSON object or doesn't match the Block Kit syntax. - `is_archived` — Channel has been archived. - `markdown_text_conflict` — Markdown text cannot be used in conjunction with `blocks` or `text` argument. - `message_limit_exceeded` — Members on this team are sending too many messages. For more details, see https://slack.com/help/articles/115002422943-Usage-limits-for-free-workspaces - `messages_tab_disabled` — Messages tab for the app is disabled. - `msg_too_long` — Message text is too long - `no_text` — No message text provided - `restricted_action` — A workspace preference prevents the authenticated user from posting. - `too_many_attachments` — Too many attachments were provided with this message. A maximum of 100 attachments are allowed on a message. - `user_not_in_channel` — Intended recipient is not in the specified channel. - `not_in_channel` — Cannot post user messages to a channel they are not in. - `invalid_attachments` — Attachments that contain blocks are not valid - `cannot_reply_to_message` — This message type cannot have thread replies. See https://docs.slack.dev/reference/methods/chat.postEphemeral */
export const postEphemeral: API.OperationMethod<
  PostEphemeralRequest,
  PostEphemeralResponse,
  PostEphemeralError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PostEphemeralRequest,
  output: PostEphemeralResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type PostMessageError = SlackOpError;
/** Sends a message to a channel. Required scopes — bot: `chat:write`; user: `chat:write` Rate limit tier: 5 Method-specific errors (the `error` slug on the SlackError): - `app_access_restricted` — The user does not have permission to use this app. - `as_user_not_supported` — The `as_user` parameter does not function with workspace apps. - `channel_not_found` — Value passed for `channel` was invalid. - `draft_already_deleted` — The draft has already been deleted. - `draft_already_sent` — The draft has already been sent. - `draft_has_conflict` — The client draft version is out of sync with the server draft version. - `draft_not_found` — The draft was not found. - `duplicate_channel_not_found` — Channel associated with `client_msg_id` was invalid. - `duplicate_message_not_found` — No duplicate message exists associated with `client_msg_id`. - `ekm_access_denied` — Your message couldn’t be sent because your admins have disabled sending messages to this channel. - `invalid_blocks` — Blocks submitted with this message are not valid. - `invalid_blocks_format` — The `blocks` is not a valid JSON object or doesn't match the Block Kit syntax. - `invalid_metadata_format` — Invalid metadata format provided. - `invalid_metadata_schema` — Invalid metadata schema provided. - `is_archived` — Channel has been archived. - `markdown_text_conflict` — Markdown text cannot be used in conjunction with `blocks` or `text` argument. - `message_limit_exceeded` — Members on this team are sending too many messages. For more details, see https://slack.com/help/articles/115002422943-Usage-limits-for-free-workspaces. - `messages_tab_disabled` — Messages tab for the app is disabled. - `metadata_must_be_sent_from_app` — Message metadata can only be posted or updated using an app-level token. - `metadata_too_large` — Metadata exceeds size limit. - `msg_blocks_too_long` — Blocks submitted with this message are too long. - `no_text` — No message text provided. - `not_in_channel` — Cannot post user messages to a channel they are not in. - `rate_limited` — Application has posted too many messages, [read the Rate Limit documentation](/apis/web-api/rate-limits) for more information. - `restricted_action` — A workspace preference prevents the authenticated user from posting. - `restricted_action_non_threadable_channel` — Cannot post thread replies into a non_threadable channel. - `restricted_action_read_only_channel` — Cannot post any message into a read-only channel. - `restricted_action_thread_locked` — Cannot post replies to a thread that has been locked by admins. - `restricted_action_thread_only_channel` — Cannot post top-level messages into a thread-only channel. - `send_on_behalf_not_allowed` — Cannot send on behalf of the provided user. - `slack_connect_canvas_sharing_blocked` — Admin has disabled Canvas File sharing in all Slack Connect communications. - `slack_connect_file_link_sharing_blocked` — Admin has disabled Slack File sharing in all Slack Connect communications. - `slack_connect_lists_sharing_blocked` — Admin has disabled Lists sharing in all Slack Connect communications. - `team_not_found` — This error occurs if, when using an org-wide token, the `channel_name` is passed instead of the `channel_id`. - `too_many_attachments` — Too many attachments were provided with this message. A maximum of 100 attachments are allowed on a message. - `too_many_contact_cards` — Too many contact_cards were provided with this message. A maximum of 10 contact cards are allowed on a message. - `cannot_reply_to_message` — This message type cannot have thread replies. - `missing_file_data` — Attempted to share a file but some required data was missing. - `attachment_payload_limit_exceeded` — Attachment payload size is too long. See https://docs.slack.dev/reference/methods/chat.postMessage */
export const postMessage: API.OperationMethod<
  PostMessageRequest,
  PostMessageResponse,
  PostMessageError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PostMessageRequest,
  output: PostMessageResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type ScheduledMessagesListError = SlackOpError;
/** Returns a list of scheduled messages. Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `invalid_channel` — The channel passed is invalid - `invalid_cursor` — Provided cursor is invalid - `invalid_token` — Token is invalid See https://docs.slack.dev/reference/methods/chat.scheduledMessages.list */
export const scheduledMessagesList: API.PaginatedOperationMethod<
  ScheduledMessagesListRequest,
  ScheduledMessagesListResponse,
  ScheduledMessagesListError,
  SlackOpContext,
  ScheduledMessagesListResponseScheduledMessagesItem
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ScheduledMessagesListRequest,
    output: ScheduledMessagesListResponse,
    errors: [SlackError, SlackRateLimited],
    protocol: SlackProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "response_metadata.next_cursor",
      items: "scheduled_messages",
      pageSize: "limit",
    } as const,
  }),
  slackPaginate,
) as any;

export type ScheduleMessageError = SlackOpError;
/** Schedules a message to be sent to a channel. Required scopes — bot: `chat:write`; user: `chat:write` Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `channel_not_found` — Value passed for `channel` was invalid. - `is_archived` — Channel has been archived. - `invalid_blocks` — Blocks submitted with this message are not valid - `invalid_blocks_format` — The `blocks` is not a valid JSON object or doesn't match the Block Kit syntax. - `invalid_metadata_format` — Invalid metadata format provided - `invalid_metadata_schema` — Invalid metadata schema provided - `invalid_time` — value passed for `post_time` was invalid. - `markdown_text_conflict` — Markdown text cannot be used in conjunction with `blocks` or `text` argument. - `message_limit_exceeded` — Members on this team are sending too many messages. For more details, see https://slack.com/help/articles/115002422943-Usage-limits-for-free-workspaces - `metadata_must_be_sent_from_app` — Message metadata can only be posted or updated using an app-level token - `metadata_too_large` — Metadata exceeds size limit - `msg_too_long` — Message text is too long - `no_text` — No message text provided - `not_in_channel` — Cannot post user messages to a channel they are not in. - `rate_limited` — Application has posted too many messages, [read the Rate Limit documentation](/apis/web-api/rate-limits) for more information - `restricted_action` — A workspace preference prevents the authenticated user from posting. - `restricted_action_non_threadable_channel` — Cannot post thread replies into a non_threadable channel. - `restricted_action_read_only_channel` — Cannot post any message into a read-only channel. - `restricted_action_thread_only_channel` — Cannot post top-level messages into a thread-only channel. - `restricted_too_many` — Too many messages were scheduled in the channel for a given period. See [usage info](/reference/methods/chat.scheduleMessage#restrictions) for additional details - `slack_connect_file_link_sharing_blocked` — Admin has disabled Slack File sharing in all Slack Connect communications - `time_in_past` — value passed for `post_time` was in the past. - `time_too_far` — value passed for `post_time` was too far into the future. - `too_many_attachments` — Too many attachments were provided with this message. A maximum of 100 attachments are allowed on a message. - `invalid_token` — The passed token is invalid or not supported by this method. See https://docs.slack.dev/reference/methods/chat.scheduleMessage */
export const scheduleMessage: API.OperationMethod<
  ScheduleMessageRequest,
  ScheduleMessageResponse,
  ScheduleMessageError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ScheduleMessageRequest,
  output: ScheduleMessageResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type StartStreamError = SlackOpError;
/** Starts a new streaming conversation. Required scopes — bot: `chat:write` Rate limit tier: 2 Method-specific errors (the `error` slug on the SlackError): - `messaging_processing_failed` — Failed to process the message. - `channel_not_found` — Value passed for `channel` was invalid. - `duplicate_channel_not_found` — Channel associated with `client_msg_id` was invalid. - `duplicate_message_not_found` — No duplicate message exists associated with `client_msg_id`. - `ekm_access_denied` — Your message couldn’t be sent because your admins have disabled sending messages to this channel. - `invalid_blocks` — Blocks submitted with this message are not valid - `invalid_chunks` — Chunks submitted with this message are not valid. - `invalid_blocks_format` — The `blocks` is not a valid JSON object or doesn't match the Block Kit syntax. - `invalid_metadata_format` — Invalid metadata format provided - `invalid_metadata_schema` — Invalid metadata schema provided - `is_archived` — Channel has been archived. - `message_limit_exceeded` — Members on this team are sending too many messages. For more details, see https://slack.com/help/articles/115002422943-Usage-limits-for-free-workspaces - `messages_tab_disabled` — Messages tab for the app is disabled. - `metadata_must_be_sent_from_app` — Message metadata can only be posted or updated using an app-level token - `metadata_too_large` — Metadata exceeds size limit - `msg_blocks_too_long` — Blocks submitted with this message are too long. - `msg_blocks_too_many` — Max blocks exceeded, blocks are limited to 50 per message. - `no_text` — No message text provided - `not_in_channel` — Cannot post user messages to a channel they are not in. - `rate_limited` — Application has posted too many messages, [read the Rate Limit documentation](/apis/web-api/rate-limits) for more information - `restricted_action` — A workspace preference prevents the authenticated user from posting. - `restricted_action_non_threadable_channel` — Cannot post thread replies into a non_threadable channel. - `restricted_action_read_only_channel` — Cannot post any message into a read-only channel. - `restricted_action_thread_locked` — Cannot post replies to a thread that has been locked by admins. - `restricted_action_thread_only_channel` — Cannot post top-level messages into a thread-only channel. - `slack_connect_canvas_sharing_blocked` — Admin has disabled Canvas File sharing in all Slack Connect communications - `slack_connect_file_link_sharing_blocked` — Admin has disabled Slack File sharing in all Slack Connect communications - `slack_connect_lists_sharing_blocked` — Admin has disabled Lists sharing in all Slack Connect communications - `team_not_found` — This error occurs if, when using an org-wide token, the `channel_name` is passed instead of the `channel_id`. - `channel_type_not_supported` — Channel type not supported - `user_not_found` — User not found. - `missing_recipient_team_id` — Missing recipient team ID. - `missing_recipient_user_id` — Missing recipient user ID. - `not_subscribed_to_message_stream_stopped` — The app must be subscribed to the `message_stream_stopped` event to use `is_stoppable`. See https://docs.slack.dev/reference/methods/chat.startStream */
export const startStream: API.OperationMethod<
  StartStreamRequest,
  StartStreamResponse,
  StartStreamError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StartStreamRequest,
  output: StartStreamResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type StopStreamError = SlackOpError;
/** Stops a streaming conversation. Required scopes — bot: `chat:write` Rate limit tier: 2 Method-specific errors (the `error` slug on the SlackError): - `message_not_found` — The message was not found. - `message_not_in_streaming_state` — The message is not in a streaming state. - `as_user_not_supported` — The `as_user` parameter does not function with workspace apps. - `channel_not_found` — Value passed for `channel` was invalid. - `channel_type_not_supported` — Channel type not supported. - `duplicate_channel_not_found` — Channel associated with `client_msg_id` was invalid. - `duplicate_message_not_found` — No duplicate message exists associated with `client_msg_id`. - `ekm_access_denied` — Your message couldn’t be sent because your admins have disabled sending messages to this channel. - `invalid_blocks` — Blocks submitted with this message are not valid. - `invalid_chunks` — Chunks submitted with this message are not valid. - `invalid_metadata_format` — Invalid metadata format provided - `invalid_metadata_schema` — Invalid metadata schema provided - `is_archived` — Channel has been archived. - `message_limit_exceeded` — Members on this team are sending too many messages. For more details, see https://slack.com/help/articles/115002422943-Usage-limits-for-free-workspaces - `message_not_owned_by_app` — The message is not owned by the app. - `messages_tab_disabled` — Messages tab for the app is disabled. - `metadata_must_be_sent_from_app` — Message metadata can only be posted or updated using an app-level token - `metadata_too_large` — Metadata exceeds size limit - `msg_blocks_too_long` — Blocks submitted with this message are too long. - `msg_blocks_too_many` — Max blocks exceeded, blocks are limited to 50 per message. - `no_text` — No message text provided - `not_in_channel` — Cannot post user messages to a channel they are not in. - `rate_limited` — Application has posted too many messages, [read the Rate Limit documentation](/apis/web-api/rate-limits) for more information - `restricted_action` — A workspace preference prevents the authenticated user from posting. - `restricted_action_non_threadable_channel` — Cannot post thread replies into a non_threadable channel. - `restricted_action_read_only_channel` — Cannot post any message into a read-only channel. - `restricted_action_thread_locked` — Cannot post replies to a thread that has been locked by admins. - `restricted_action_thread_only_channel` — Cannot post top-level messages into a thread-only channel. - `slack_connect_canvas_sharing_blocked` — Admin has disabled Canvas File sharing in all Slack Connect communications - `slack_connect_file_link_sharing_blocked` — Admin has disabled Slack File sharing in all Slack Connect communications - `slack_connect_lists_sharing_blocked` — Admin has disabled Lists sharing in all Slack Connect communications - `team_not_found` — This error occurs if, when using an org-wide token, the `channel_name` is passed instead of the `channel_id`. - `too_many_attachments` — Too many attachments were provided with this message. A maximum of 100 attachments are allowed on a message. - `too_many_contact_cards` — Too many contact_cards were provided with this message. A maximum of 10 contact cards are allowed on a message. - `cannot_reply_to_message` — This message type cannot have thread replies. - `missing_file_data` — Attempted to share a file but some required data was missing. - `attachment_payload_limit_exceeded` — Attachment payload size is too long. See https://docs.slack.dev/reference/methods/chat.stopStream */
export const stopStream: API.OperationMethod<
  StopStreamRequest,
  StopStreamResponse,
  StopStreamError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StopStreamRequest,
  output: StopStreamResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type UnfurlError = SlackOpError;
/** Provide custom unfurl behavior for user-posted URLs Required scopes — bot: `links:write`; user: `links:write` Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `cannot_auth_user` — The current user cannot be authenticated. - `cannot_find_channel` — The specified channel could not be located for this token. - `cannot_find_message` — The `ts` value in the request does not match a message. - `cannot_find_service` — A record of your app being allowed to unfurl for this workspace could not be found. - `cannot_parse_attachment` — The provided `unfurls` argument could not be parsed or understood. - `cannot_prompt` — The current user has already interacted with and dismissed a prompt for this application. - `cannot_unfurl_message` — The URL cannot be unfurled because the URL provided does not appear in the message. - `cannot_unfurl_url` — The URL cannot be unfurled. This error may be returned if you haven't acknowledged a `link_shared` event tied to the same URL. It is also returned when the domain appears in a workspace's administrative blocklists. - `invalid_unfurl_id` — The unfurl ID is invalid. - `invalid_unfurls_format` — The `unfurls` parameter cannot be JSON-decoded into a map of URLs to attachments. - `invalid_metadata_format` — The `metadata` parameter cannot be JSON-decoded into the expected format. - `invalid_source` — The unfurl source is invalid. - `missing_channel` — The request is missing the `channel` parameter - `missing_source` — The request is missing the `source` parameter. - `missing_ts` — The request is missing the `ts` parameter - `missing_unfurl_id` — The request is missing the `unfurl_id` parameter. - `missing_unfurls` — The request is missing the `unfurls` parameter. See https://docs.slack.dev/reference/methods/chat.unfurl */
export const unfurl: API.OperationMethod<
  UnfurlRequest,
  UnfurlResponse,
  UnfurlError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UnfurlRequest,
  output: UnfurlResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type UpdateError = SlackOpError;
/** Updates a message. Required scopes — bot: `chat:write`; user: `chat:write` Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `as_user_not_supported` — The `as_user` parameter does not function with workspace apps. - `blocked_file_type` — Admin has disabled uploading this type of file. - `block_mismatch` — Rich-text blocks cannot be replaced with non-rich-text blocks - `cant_broadcast_message` — Unable to broadcast this message. - `cant_update_message` — Authenticated user does not have permission to update this message. - `channel_not_found` — Value passed for `channel` was invalid. - `edit_window_closed` — The message cannot be edited due to the team message edit settings - `external_channel_migrating` — The channel is in the process of migrating and so the message cannot be updated at this time. - `file_deleted` — File to share deleted. - `file_is_deleted` — The file is deleted. - `file_not_found` — One or more of the provided file IDs could not be found. - `file_share_limit_reached` — The file has reached the share limit. - `invalid_attachments` — The attachments were invalid. - `invalid_blocks` — The blocks were invalid for the requesting user. - `invalid_blocks_format` — The `blocks` array is not a valid JSON object or doesn't match the Block Kit syntax. - `invalid_metadata_format` — Invalid metadata format provided - `invalid_metadata_schema` — Invalid metadata schema provided - `is_inactive` — The message cannot be edited within a frozen, archived or deleted channel. - `markdown_text_conflict` — Markdown text cannot be used in conjunction with `blocks` or `text` argument. - `max_file_sharing_exceeded` — Exceeded max allowed files shared. - `message_limit_exceeded` — Members on this team are sending too many messages. For more details, see https://slack.com/help/articles/115002422943-Usage-limits-for-free-workspaces. - `message_not_found` — No message exists with the requested timestamp. - `metadata_must_be_sent_from_app` — Message metadata can only be posted or updated using an app-level token - `metadata_too_large` — Metadata exceeds size limit - `msg_too_long` — Message text is too long. The `text` field cannot exceed 4,000 characters. - `no_dual_broadcast_content_update` — Can't broadcast an old reply and update the content at the same time. - `no_text` — No message text provided - `posting_to_channel_denied` — The user does not have permission to share files in this channel. - `slack_connect_blocked_file_type` — Files with certain extensions are blocked from being uploaded in all Slack Connect messages. - `slack_connect_canvas_sharing_blocked` — Admin has disabled sharing of canvas links in all Slack Connect messages. - `slack_connect_clip_sharing_blocked` — Admin has disabled Clip uploads in Slack Connect channels. - `slack_connect_file_link_sharing_blocked` — Admin has disabled Slack file sharing in all Slack Connect messages. - `slack_connect_file_upload_sharing_blocked` — Admin has disabled file uploads in all Slack Connect messages. - `streaming_state_conflict` — The message is currently streaming text and cannot be edited. - `team_not_found` — Team associated with the message and channel could not be found. - `too_many_attachments` — Too many attachments were provided with this message. A maximum of 100 attachments are allowed on a message. - `unable_to_share_files` — Sharing the files failed. - `update_failed` — Internal update failure. See https://docs.slack.dev/reference/methods/chat.update */
export const update: API.OperationMethod<
  UpdateRequest,
  UpdateResponse,
  UpdateError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateRequest,
  output: UpdateResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));
