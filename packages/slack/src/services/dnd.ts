// AUTO-GENERATED by scripts/generate.ts from .generated-specs (docs.slack.dev JSON twins). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  SlackProtocol,
  type SlackOpError,
  type SlackOpContext,
} from "../protocol.ts";
import { SlackError, SlackRateLimited } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { SlackOpError, SlackOpContext };

export interface EndDndRequest {}
export const EndDndRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "POST", uri: "/dnd.endDnd", code: 200 })),
).annotate({ identifier: "EndDndRequest" }) as any as S.Schema<EndDndRequest>;

export interface EndDndResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
}
export const EndDndResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
  }),
).annotate({ identifier: "EndDndResponse" }) as any as S.Schema<EndDndResponse>;

export interface EndSnoozeRequest {}
export const EndSnoozeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "POST", uri: "/dnd.endSnooze", code: 200 }),
  ),
).annotate({
  identifier: "EndSnoozeRequest",
}) as any as S.Schema<EndSnoozeRequest>;

export interface EndSnoozeResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  snooze_enabled: boolean;
  dnd_enabled: boolean;
  next_dnd_end_ts: number;
  next_dnd_start_ts: number;
  snooze_endtime?: number;
  snooze_remaining?: number;
  dnd_profile_status?: unknown;
}
export const EndSnoozeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    snooze_enabled: S.Boolean,
    dnd_enabled: S.Boolean,
    next_dnd_end_ts: S.Number,
    next_dnd_start_ts: S.Number,
    snooze_endtime: S.optional(S.Number),
    snooze_remaining: S.optional(S.Number),
    dnd_profile_status: S.optional(S.Unknown),
  }),
).annotate({
  identifier: "EndSnoozeResponse",
}) as any as S.Schema<EndSnoozeResponse>;

export interface InfoRequest {
  /** User to fetch status for (defaults to current user) */
  user?: string;
  /** Encoded team id where passed in user param belongs, required if org token is used. If no user param is passed, then a team which has access to the app should be passed */
  team_id?: string;
}
export const InfoRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    user: S.optional(S.String.pipe(T.Query())),
    team_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/dnd.info", code: 200 })),
).annotate({ identifier: "InfoRequest" }) as any as S.Schema<InfoRequest>;

export interface InfoResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  dnd_enabled: boolean;
  next_dnd_start_ts: number;
  next_dnd_end_ts: number;
  snooze_enabled?: boolean;
  snooze_endtime?: number;
  snooze_remaining?: number;
  snooze_is_indefinite?: boolean;
  dnd_profile_status?: unknown;
}
export const InfoResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    dnd_enabled: S.Boolean,
    next_dnd_start_ts: S.Number,
    next_dnd_end_ts: S.Number,
    snooze_enabled: S.optional(S.Boolean),
    snooze_endtime: S.optional(S.Number),
    snooze_remaining: S.optional(S.Number),
    snooze_is_indefinite: S.optional(S.Boolean),
    dnd_profile_status: S.optional(S.Unknown),
  }),
).annotate({ identifier: "InfoResponse" }) as any as S.Schema<InfoResponse>;

export interface SetSnoozeRequest {
  /** This argument is required. Number of minutes, from now, to snooze until. */
  num_minutes: string;
}
export const SetSnoozeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    num_minutes: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/dnd.setSnooze", code: 200 })),
).annotate({
  identifier: "SetSnoozeRequest",
}) as any as S.Schema<SetSnoozeRequest>;

export interface SetSnoozeResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  snooze_endtime?: number;
  snooze_enabled: boolean;
  snooze_remaining?: number;
  snooze_is_indefinite?: boolean;
}
export const SetSnoozeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    snooze_endtime: S.optional(S.Number),
    snooze_enabled: S.Boolean,
    snooze_remaining: S.optional(S.Number),
    snooze_is_indefinite: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "SetSnoozeResponse",
}) as any as S.Schema<SetSnoozeResponse>;

export interface TeamInfoRequest {
  /** Comma-separated list of users to fetch Do Not Disturb status for */
  users: string;
  /** Encoded team id where passed in users belong, required if org token is used */
  team_id?: string;
}
export const TeamInfoRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    users: S.String.pipe(T.Query()),
    team_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/dnd.teamInfo", code: 200 })),
).annotate({
  identifier: "TeamInfoRequest",
}) as any as S.Schema<TeamInfoRequest>;

export type TeamInfoResponseUsersMap = { [key: string]: unknown | undefined };
export const TeamInfoResponseUsersMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<TeamInfoResponseUsersMap>;

export interface TeamInfoResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  users: TeamInfoResponseUsersMap;
}
export const TeamInfoResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    users: TeamInfoResponseUsersMap,
  }),
).annotate({
  identifier: "TeamInfoResponse",
}) as any as S.Schema<TeamInfoResponse>;

export type EndDndError = SlackOpError;
/** Ends the current user's Do Not Disturb session immediately. Required scopes — user: `dnd:write` Rate limit tier: 2 Method-specific errors (the `error` slug on the SlackError): - `not_in_dnd` — The auth'd user isn't in DND mode. See https://docs.slack.dev/reference/methods/dnd.endDnd */
export const endDnd: API.OperationMethod<
  EndDndRequest,
  EndDndResponse,
  EndDndError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EndDndRequest,
  output: EndDndResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type EndSnoozeError = SlackOpError;
/** Ends the current user's snooze mode immediately. Required scopes — user: `dnd:write` Rate limit tier: 2 Method-specific errors (the `error` slug on the SlackError): - `snooze_end_failed` — There was a problem setting the user's Do Not Disturb status See https://docs.slack.dev/reference/methods/dnd.endSnooze */
export const endSnooze: API.OperationMethod<
  EndSnoozeRequest,
  EndSnoozeResponse,
  EndSnoozeError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EndSnoozeRequest,
  output: EndSnoozeResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type InfoError = SlackOpError;
/** Retrieves a user's current Do Not Disturb status. Required scopes — bot: `dnd:read`; user: `dnd:read` Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `user_not_found` — Value passed for `user` was invalid. - `user_not_visible` — User is not visible for this request. See https://docs.slack.dev/reference/methods/dnd.info */
export const info: API.OperationMethod<
  InfoRequest,
  InfoResponse,
  InfoError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InfoRequest,
  output: InfoResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type SetSnoozeError = SlackOpError;
/** Turns on Do Not Disturb mode for the current user, or changes its duration. Required scopes — user: `dnd:write` Rate limit tier: 2 Method-specific errors (the `error` slug on the SlackError): - `missing_duration` — No value provided for `num_minutes` - `snooze_failed` — There was a problem setting the user's Do Not Disturb status - `too_long` — The value for `num_minutes` was too large See https://docs.slack.dev/reference/methods/dnd.setSnooze */
export const setSnooze: API.OperationMethod<
  SetSnoozeRequest,
  SetSnoozeResponse,
  SetSnoozeError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SetSnoozeRequest,
  output: SetSnoozeResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type TeamInfoError = SlackOpError;
/** Retrieves the Do Not Disturb status for up to 50 users on a team. Required scopes — bot: `dnd:read`; user: `dnd:read` Rate limit tier: 2 Method-specific errors (the `error` slug on the SlackError): - `no_users_provided` — Must provide more than one user. - `user_not_visible` — User is not visible for this request. See https://docs.slack.dev/reference/methods/dnd.teamInfo */
export const teamInfo: API.OperationMethod<
  TeamInfoRequest,
  TeamInfoResponse,
  TeamInfoError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: TeamInfoRequest,
  output: TeamInfoResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));
