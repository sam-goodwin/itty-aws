// AUTO-GENERATED by scripts/generate.ts from .generated-specs (docs.slack.dev JSON twins). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  SlackProtocol,
  type SlackOpError,
  type SlackOpContext,
} from "../protocol.ts";
import { SlackError, SlackRateLimited } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { SlackOpError, SlackOpContext };

/** The full comment data. Required for edit and post actions. See the [comment schema](/messaging/work-objects-comments#entity-present-comments-method) for the full list of properties. */
export type AcknowledgeCommentActionRequestCommentMap = {
  [key: string]: unknown | undefined;
};
export const AcknowledgeCommentActionRequestCommentMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<AcknowledgeCommentActionRequestCommentMap>;

export interface AcknowledgeCommentActionRequest {
  /** A reference to the original user action that initiated the comment mutation */
  trigger_id: string;
  /** The full comment data. Required for edit and post actions. See the [comment schema](/messaging/work-objects-comments#entity-present-comments-method) for the full list of properties. */
  comment?: AcknowledgeCommentActionRequestCommentMap;
  /** Error message if the action failed in the app. When present, signals that the mutation could not be completed. */
  error?: string;
}
export const AcknowledgeCommentActionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    trigger_id: S.String,
    comment: S.optional(AcknowledgeCommentActionRequestCommentMap),
    error: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/entity.acknowledgeCommentAction",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AcknowledgeCommentActionRequest",
}) as any as S.Schema<AcknowledgeCommentActionRequest>;

export interface AcknowledgeCommentActionResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
}
export const AcknowledgeCommentActionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
  }),
).annotate({
  identifier: "AcknowledgeCommentActionResponse",
}) as any as S.Schema<AcknowledgeCommentActionResponse>;

export type PresentCommentsRequestCommentsItemMap = {
  [key: string]: unknown | undefined;
};
export const PresentCommentsRequestCommentsItemMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<PresentCommentsRequestCommentsItemMap>;

/** Array of comments to present to the user. See the [comment schema](/messaging/work-objects-comments#entity-present-comments-method) for the full list of properties. */
export type PresentCommentsRequestCommentsList =
  Array<PresentCommentsRequestCommentsItemMap>;
export const PresentCommentsRequestCommentsList = /*@__PURE__*/ S.Array(
  PresentCommentsRequestCommentsItemMap,
) as any as S.Schema<PresentCommentsRequestCommentsList>;

export interface PresentCommentsRequest {
  /** Array of comments to present to the user. See the [comment schema](/messaging/work-objects-comments#entity-present-comments-method) for the full list of properties. */
  comments: PresentCommentsRequestCommentsList;
  /** App supplied cursor used for pagination, will be sent in the next request for comments */
  cursor?: string;
  /** Indicates whether the user has permissions to post comments */
  can_post_comment?: boolean;
  /** A reference to the original user action that initiated the request */
  trigger_id: string;
  /** The block action id that will be sent when a delete request is initiated for a comment. */
  delete_action_id?: string;
  /** Set to true (or 1) to indicate that the user must authenticate to see the comments data. */
  user_auth_required?: boolean;
  /** A custom URL to which users are directed for authentication if required. */
  user_auth_url?: string;
  error?: string;
}
export const PresentCommentsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    comments: PresentCommentsRequestCommentsList,
    cursor: S.optional(S.String),
    can_post_comment: S.optional(S.Boolean),
    trigger_id: S.String,
    delete_action_id: S.optional(S.String),
    user_auth_required: S.optional(S.Boolean),
    user_auth_url: S.optional(S.String),
    error: S.optional(S.String),
  }).pipe(
    T.Http({ method: "POST", uri: "/entity.presentComments", code: 200 }),
  ),
).annotate({
  identifier: "PresentCommentsRequest",
}) as any as S.Schema<PresentCommentsRequest>;

export interface PresentCommentsResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
}
export const PresentCommentsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
  }),
).annotate({
  identifier: "PresentCommentsResponse",
}) as any as S.Schema<PresentCommentsResponse>;

/** URL-encoded JSON object containing flexpane metadata from the app that will be conformed to a Work Object metadata schema, keyed by entity ID. */
export type PresentDetailsRequestMetadataMap = {
  [key: string]: unknown | undefined;
};
export const PresentDetailsRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<PresentDetailsRequestMetadataMap>;

export interface PresentDetailsRequest {
  /** URL-encoded JSON object containing flexpane metadata from the app that will be conformed to a Work Object metadata schema, keyed by entity ID. */
  metadata?: PresentDetailsRequestMetadataMap;
  /** A reference to the original user action that initiated the request. */
  trigger_id: string;
  /** Set to true (or 1) to indicate that the user must authenticate to view full flexpane data. */
  user_auth_required?: boolean;
  /** A custom URL to which users are directed for authentication if required. */
  user_auth_url?: string;
  error?: string;
}
export const PresentDetailsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    metadata: S.optional(PresentDetailsRequestMetadataMap),
    trigger_id: S.String,
    user_auth_required: S.optional(S.Boolean),
    user_auth_url: S.optional(S.String),
    error: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/entity.presentDetails", code: 200 })),
).annotate({
  identifier: "PresentDetailsRequest",
}) as any as S.Schema<PresentDetailsRequest>;

export interface PresentDetailsResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
}
export const PresentDetailsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
  }),
).annotate({
  identifier: "PresentDetailsResponse",
}) as any as S.Schema<PresentDetailsResponse>;

export type AcknowledgeCommentActionError = SlackOpError;
/** Acknowledge a comment mutation (edit, delete, or post) on a work object entity. Apps call this endpoint to confirm they have processed a comment action, and the backend emits a dedicated RTM event to the user. Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `method_not_found` — when the API isn't available to the caller - `invalid_trigger_id` — Trigger id is not valid - `invalid_app_id` — App ID is not a valid format - `invalid_auth` — Invalid token provided - `invalid_action` — The comment data does not match the original trigger context (e.g., comment_id or thread_root_id mismatch) - `missing_comment_data` — Required comment fields are missing for the given action type - `not_allowed` — User is not enrolled in comment syncing - `invalid_sender_schema` — The sender field could not be transformed to a valid user schema See https://docs.slack.dev/reference/methods/entity.acknowledgeCommentAction */
export const acknowledgeCommentAction: API.OperationMethod<
  AcknowledgeCommentActionRequest,
  AcknowledgeCommentActionResponse,
  AcknowledgeCommentActionError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AcknowledgeCommentActionRequest,
  output: AcknowledgeCommentActionResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type PresentCommentsError = SlackOpError;
/** Provide comments for work objects. Apps call this endpoint to send per-user flexpane comment data to the client. Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `method_not_found` — when the API isn't available to the caller - `invalid_trigger_id` — Trigger id is not valid - `invalid_app_id` — App ID is not a valid format - `invalid_auth` — Invalid token provided - `user_auth_url_missing` — User auth url is required if user_auth_required is true - `missing_delete_action_id` — delete_action_id is required when any comment is marked can_delete See https://docs.slack.dev/reference/methods/entity.presentComments */
export const presentComments: API.OperationMethod<
  PresentCommentsRequest,
  PresentCommentsResponse,
  PresentCommentsError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PresentCommentsRequest,
  output: PresentCommentsResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type PresentDetailsError = SlackOpError;
/** Provide custom flexpane behavior for Work Objects. Apps call this endpoint to send per-user flexpane metadata to the client. Rate limit tier: 3 Method-specific errors (the `error` slug on the SlackError): - `invalid_metadata` — The provided `metadata` argument could not be parsed or understood. - `feature_not_enabled` — temporarily gating API - `invalid_app_id` — App ID is not a valid format - `user_auth_url_missing` — User auth url is required if user_auth_required is true - `invalid_trigger_id` — Trigger id is not valid - `entity_metadata_does_not_match_trigger` — The provided entity's ID does not match the ID of the entity used to initiate the request - `missing_interactivity_url` — App does not have an interactivity URL configured - `missing_options_load_url` — App does not have an options load URL configured - `invalid_auth` — Invalid token provided See https://docs.slack.dev/reference/methods/entity.presentDetails */
export const presentDetails: API.OperationMethod<
  PresentDetailsRequest,
  PresentDetailsResponse,
  PresentDetailsError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PresentDetailsRequest,
  output: PresentDetailsResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));
