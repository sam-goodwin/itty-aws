// AUTO-GENERATED by scripts/generate.ts from .generated-specs (docs.slack.dev JSON twins). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  SlackProtocol,
  type SlackOpError,
  type SlackOpContext,
} from "../protocol.ts";
import { SlackError, SlackRateLimited } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { SlackOpError, SlackOpContext };

export type SearchContextRequestChannelTypesItem =
  | "public_channel"
  | "private_channel"
  | "mpim"
  | "im";
export const SearchContextRequestChannelTypesItem = /*@__PURE__*/ S.String;

/** Mix and match channel types by providing a comma-separated list of any combination of `public_channel`, `private_channel`, `mpim`, `im`. */
export type SearchContextRequestChannelTypesList = Array<
  SearchContextRequestChannelTypesItem | (string & {})
>;
export const SearchContextRequestChannelTypesList = /*@__PURE__*/ S.Array(
  SearchContextRequestChannelTypesItem,
) as any as S.Schema<SearchContextRequestChannelTypesList>;

export type SearchContextRequestContentTypesItem =
  | "messages"
  | "files"
  | "enterprise"
  | "channels"
  | "users";
export const SearchContextRequestContentTypesItem = /*@__PURE__*/ S.String;

/** Content types to include, a comma-separated list of any combination of `messages`, `files`, `channels`, `users`. */
export type SearchContextRequestContentTypesList = Array<
  SearchContextRequestContentTypesItem | (string & {})
>;
export const SearchContextRequestContentTypesList = /*@__PURE__*/ S.Array(
  SearchContextRequestContentTypesItem,
) as any as S.Schema<SearchContextRequestContentTypesList>;

/** The field to sort the results by. Defaults to score. Can be one of: score, timestamp. */
export type SearchContextRequestSort = "score" | "timestamp";
export const SearchContextRequestSort = /*@__PURE__*/ S.String;

/** The direction to sort the results by. Defaults to desc. */
export type SearchContextRequestSortDir = "asc" | "desc";
export const SearchContextRequestSortDir = /*@__PURE__*/ S.String;

/** A list of term clauses. A term clause is a string with search terms. Search results returned will match every term clause specified (i.e., conjunctive normal form). */
export type SearchContextRequestTermClausesList = Array<string>;
export const SearchContextRequestTermClausesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SearchContextRequestTermClausesList>;

export interface SearchContextRequest {
  /** User prompt or search query */
  query: string;
  /** Send `action_token` as received in a message event. */
  action_token?: string;
  /** Mix and match channel types by providing a comma-separated list of any combination of `public_channel`, `private_channel`, `mpim`, `im`. */
  channel_types?: SearchContextRequestChannelTypesList;
  /** Content types to include, a comma-separated list of any combination of `messages`, `files`, `channels`, `users`. */
  content_types?: SearchContextRequestContentTypesList;
  /** Whether the results should include bots. */
  include_bots?: boolean;
  /** Whether to include deleted users in the user search results. Defaults to false. */
  include_deleted_users?: boolean;
  /** UNIX timestamp filter. If present, filters for results before this date. */
  before?: number;
  /** UNIX timestamp filter. If present, filters for results after this date. */
  after?: number;
  /** Whether to include context messages surrounding the main message result. Defaults to false if unspecified. */
  include_context_messages?: boolean;
  /** Context channel ID to support scoping the search when applicable. */
  context_channel_id?: string;
  /** The cursor returned by the API. Leave this blank for the first request and use this to get the next page of results. */
  cursor?: string;
  /** Number of results to return, up to a max of 20. Defaults to 20. */
  limit?: number;
  /** The field to sort the results by. Defaults to score. Can be one of: score, timestamp. */
  sort?: SearchContextRequestSort | (string & {});
  /** The direction to sort the results by. Defaults to desc. */
  sort_dir?: SearchContextRequestSortDir | (string & {});
  /** Whether to return the message blocks in the response. */
  include_message_blocks?: boolean;
  /** Whether to highlight the search query in the results. Defaults to false if unspecified. */
  highlight?: boolean;
  /** A list of term clauses. A term clause is a string with search terms. Search results returned will match every term clause specified (i.e., conjunctive normal form). */
  term_clauses?: SearchContextRequestTermClausesList;
  /** A string containing only modifiers in the format of `modifier:value`. Search results returned will match the modifier value. For now modifiers only affect term clauses. */
  modifiers?: string;
  /** Whether to include archived channels in the search results. */
  include_archived_channels?: boolean;
  /** Whether to disable semantic search. When true, only keyword-based search is used. Defaults to false. */
  disable_semantic_search?: boolean;
}
export const SearchContextRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    query: S.String,
    action_token: S.optional(S.String),
    channel_types: S.optional(SearchContextRequestChannelTypesList),
    content_types: S.optional(SearchContextRequestContentTypesList),
    include_bots: S.optional(S.Boolean),
    include_deleted_users: S.optional(S.Boolean),
    before: S.optional(S.Number),
    after: S.optional(S.Number),
    include_context_messages: S.optional(S.Boolean),
    context_channel_id: S.optional(S.String),
    cursor: S.optional(S.String),
    limit: S.optional(S.Number),
    sort: S.optional(SearchContextRequestSort),
    sort_dir: S.optional(SearchContextRequestSortDir),
    include_message_blocks: S.optional(S.Boolean),
    highlight: S.optional(S.Boolean),
    term_clauses: S.optional(SearchContextRequestTermClausesList),
    modifiers: S.optional(S.String),
    include_archived_channels: S.optional(S.Boolean),
    disable_semantic_search: S.optional(S.Boolean),
  }).pipe(
    T.Http({ method: "POST", uri: "/assistant.search.context", code: 200 }),
  ),
).annotate({
  identifier: "SearchContextRequest",
}) as any as S.Schema<SearchContextRequest>;

/** Messages before the current message, if applicable. */
export type SearchContextResponseResultsMessagesItemContextMessagesBeforeList =
  Array<unknown>;
export const SearchContextResponseResultsMessagesItemContextMessagesBeforeList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<SearchContextResponseResultsMessagesItemContextMessagesBeforeList>;

/** Messages after the current message. */
export type SearchContextResponseResultsMessagesItemContextMessagesAfterList =
  Array<unknown>;
export const SearchContextResponseResultsMessagesItemContextMessagesAfterList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<SearchContextResponseResultsMessagesItemContextMessagesAfterList>;

export interface SearchContextResponseResultsMessagesItemContextMessages {
  /** Messages before the current message, if applicable. */
  before?: SearchContextResponseResultsMessagesItemContextMessagesBeforeList;
  /** Messages after the current message. */
  after?: SearchContextResponseResultsMessagesItemContextMessagesAfterList;
}
export const SearchContextResponseResultsMessagesItemContextMessages =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      before: S.optional(
        SearchContextResponseResultsMessagesItemContextMessagesBeforeList,
      ),
      after: S.optional(
        SearchContextResponseResultsMessagesItemContextMessagesAfterList,
      ),
    }),
  ).annotate({
    identifier: "SearchContextResponseResultsMessagesItemContextMessages",
  }) as any as S.Schema<SearchContextResponseResultsMessagesItemContextMessages>;

/** Blocks from the retrieved message. */
export type SearchContextResponseResultsMessagesItemBlocksList = Array<unknown>;
export const SearchContextResponseResultsMessagesItemBlocksList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<SearchContextResponseResultsMessagesItemBlocksList>;

export interface SearchContextResponseResultsMessagesItem {
  /** Full name of the user who authored the message. */
  author_name: string;
  /** Email address of the user who authored the message, if visible to the searching user. */
  author_email?: string;
  /** ID of the user who authored the message. */
  author_user_id: string;
  /** ID of the workspace in which the message was sent. */
  team_id: string;
  /** ID of the channel in which the message was sent. */
  channel_id: string;
  /** Name of the channel in which the message was sent. */
  channel_name: string;
  /** Slack-format timestamp at which the message was sent. */
  message_ts: string;
  /** Content from the retrieved message. */
  content: string;
  /** Whether the message was authored by a bot (app) or not. */
  is_author_bot: boolean;
  permalink: string;
  context_messages?: SearchContextResponseResultsMessagesItemContextMessages;
  /** Blocks from the retrieved message. */
  blocks?: SearchContextResponseResultsMessagesItemBlocksList;
  reply_count?: number;
}
export const SearchContextResponseResultsMessagesItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      author_name: S.String,
      author_email: S.optional(S.String),
      author_user_id: S.String,
      team_id: S.String,
      channel_id: S.String,
      channel_name: S.String,
      message_ts: S.String,
      content: S.String,
      is_author_bot: S.Boolean,
      permalink: S.String,
      context_messages: S.optional(
        SearchContextResponseResultsMessagesItemContextMessages,
      ),
      blocks: S.optional(SearchContextResponseResultsMessagesItemBlocksList),
      reply_count: S.optional(S.Number),
    }),
).annotate({
  identifier: "SearchContextResponseResultsMessagesItem",
}) as any as S.Schema<SearchContextResponseResultsMessagesItem>;

export type SearchContextResponseResultsMessagesList =
  Array<SearchContextResponseResultsMessagesItem>;
export const SearchContextResponseResultsMessagesList = /*@__PURE__*/ S.Array(
  SearchContextResponseResultsMessagesItem,
) as any as S.Schema<SearchContextResponseResultsMessagesList>;

export interface SearchContextResponseResultsFilesItem {
  /** Full name of the user who uploaded or created the file. */
  author_name: string;
  /** Email address of the user who uploaded or created the file, if visible to the searching user */
  author_email?: string;
  /** ID of the user who uploaded or created the file. */
  author_user_id: string;
  /** ID of the user who uploaded the file. */
  uploader_user_id: string;
  /** ID of the workspace in which the file was uploaded. */
  team_id: string;
  /** ID of the file. */
  file_id: string;
  /** Epoch timestamp in microseconds at which the file was created. */
  date_created: number;
  /** Epoch timestamp in microseconds at which the file was uploaded. */
  date_updated: number;
  /** Title of the file */
  title: string;
  /** Type of the file. */
  file_type: string;
  /** Permanent link to the file in Slack. */
  permalink: string;
  /** Content from the retrieved file. */
  content: string;
  /** Size of the file in bytes. */
  size?: number;
}
export const SearchContextResponseResultsFilesItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      author_name: S.String,
      author_email: S.optional(S.String),
      author_user_id: S.String,
      uploader_user_id: S.String,
      team_id: S.String,
      file_id: S.String,
      date_created: S.Number,
      date_updated: S.Number,
      title: S.String,
      file_type: S.String,
      permalink: S.String,
      content: S.String,
      size: S.optional(S.Number),
    }),
).annotate({
  identifier: "SearchContextResponseResultsFilesItem",
}) as any as S.Schema<SearchContextResponseResultsFilesItem>;

export type SearchContextResponseResultsFilesList =
  Array<SearchContextResponseResultsFilesItem>;
export const SearchContextResponseResultsFilesList = /*@__PURE__*/ S.Array(
  SearchContextResponseResultsFilesItem,
) as any as S.Schema<SearchContextResponseResultsFilesList>;

export type SearchContextResponseResultsEnterpriseList = Array<unknown>;
export const SearchContextResponseResultsEnterpriseList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<SearchContextResponseResultsEnterpriseList>;

export interface SearchContextResponseResultsChannelsItem {
  /** ID of the workspace in which the channel was created. */
  team_id: string;
  /** ID of the user who created the channel. */
  creator_user_id: string;
  /** Full name of the user who created the channel. */
  creator_name?: string;
  /** Epoch timestamp in microseconds at which the channel was created. */
  date_created: number;
  /** Epoch timestamp in microseconds at which the channel was updated. */
  date_updated: number;
  /** Name of the channel. */
  name: string;
  /** Topic of the channel. */
  topic?: string;
  /** Purpose of the channel. */
  purpose?: string;
  /** Permanent link to the channel in Slack. */
  permalink: string;
  /** Whether the channel is archived. */
  is_archived: boolean;
}
export const SearchContextResponseResultsChannelsItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      team_id: S.String,
      creator_user_id: S.String,
      creator_name: S.optional(S.String),
      date_created: S.Number,
      date_updated: S.Number,
      name: S.String,
      topic: S.optional(S.String),
      purpose: S.optional(S.String),
      permalink: S.String,
      is_archived: S.Boolean,
    }),
).annotate({
  identifier: "SearchContextResponseResultsChannelsItem",
}) as any as S.Schema<SearchContextResponseResultsChannelsItem>;

export type SearchContextResponseResultsChannelsList =
  Array<SearchContextResponseResultsChannelsItem>;
export const SearchContextResponseResultsChannelsList = /*@__PURE__*/ S.Array(
  SearchContextResponseResultsChannelsItem,
) as any as S.Schema<SearchContextResponseResultsChannelsList>;

export interface SearchContextResponseResultsUsersItem {
  /** The encoded user ID */
  user_id: string;
  /** The permalink to the user in Slack */
  permalink: string;
  /** The permalink to the user's profile photo. */
  profile_pic_permalink?: string;
  /** The user's full name, which might be different from the display name. */
  full_name: string;
  /** The user's job title. */
  title: string;
  /** The user's local timezone. */
  timezone: string;
  /** The user's email address. */
  email: string;
}
export const SearchContextResponseResultsUsersItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      user_id: S.String,
      permalink: S.String,
      profile_pic_permalink: S.optional(S.String),
      full_name: S.String,
      title: S.String,
      timezone: S.String,
      email: S.String,
    }),
).annotate({
  identifier: "SearchContextResponseResultsUsersItem",
}) as any as S.Schema<SearchContextResponseResultsUsersItem>;

export type SearchContextResponseResultsUsersList =
  Array<SearchContextResponseResultsUsersItem>;
export const SearchContextResponseResultsUsersList = /*@__PURE__*/ S.Array(
  SearchContextResponseResultsUsersItem,
) as any as S.Schema<SearchContextResponseResultsUsersList>;

export interface SearchContextResponseResults {
  messages?: SearchContextResponseResultsMessagesList;
  files?: SearchContextResponseResultsFilesList;
  enterprise?: SearchContextResponseResultsEnterpriseList;
  channels?: SearchContextResponseResultsChannelsList;
  users?: SearchContextResponseResultsUsersList;
}
export const SearchContextResponseResults = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    messages: S.optional(SearchContextResponseResultsMessagesList),
    files: S.optional(SearchContextResponseResultsFilesList),
    enterprise: S.optional(SearchContextResponseResultsEnterpriseList),
    channels: S.optional(SearchContextResponseResultsChannelsList),
    users: S.optional(SearchContextResponseResultsUsersList),
  }),
).annotate({
  identifier: "SearchContextResponseResults",
}) as any as S.Schema<SearchContextResponseResults>;

/** Pagination metadata. An empty `next_cursor` means the last page. */
export interface SearchContextResponseResponseMetadata {
  /** Cursor for the next page — pass as `cursor` on the next call. */
  next_cursor?: string;
}
export const SearchContextResponseResponseMetadata = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      next_cursor: S.optional(S.String),
    }),
).annotate({
  identifier: "SearchContextResponseResponseMetadata",
}) as any as S.Schema<SearchContextResponseResponseMetadata>;

export interface SearchContextResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  results: SearchContextResponseResults;
  /** Pagination metadata. An empty `next_cursor` means the last page. */
  response_metadata?: SearchContextResponseResponseMetadata;
}
export const SearchContextResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    results: SearchContextResponseResults,
    response_metadata: S.optional(SearchContextResponseResponseMetadata),
  }),
).annotate({
  identifier: "SearchContextResponse",
}) as any as S.Schema<SearchContextResponse>;

export interface SearchInfoRequest {}
export const SearchInfoRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "POST", uri: "/assistant.search.info", code: 200 }),
  ),
).annotate({
  identifier: "SearchInfoRequest",
}) as any as S.Schema<SearchInfoRequest>;

export interface SearchInfoResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
  /** Whether AI search is enabled for the team */
  is_ai_search_enabled: boolean;
}
export const SearchInfoResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    is_ai_search_enabled: S.Boolean,
  }),
).annotate({
  identifier: "SearchInfoResponse",
}) as any as S.Schema<SearchInfoResponse>;

/** The list of messages to rotate through as a loading indicator. Maximum of 10 messages. */
export type ThreadsSetStatusRequestLoadingMessagesList = Array<string>;
export const ThreadsSetStatusRequestLoadingMessagesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ThreadsSetStatusRequestLoadingMessagesList>;

export interface ThreadsSetStatusRequest {
  /** Channel ID containing the assistant thread. */
  channel_id: string;
  /** Message timestamp of the thread of where to set the status. */
  thread_ts: string;
  /** Status of the specified bot user, e.g., 'is thinking...'. A two minute timeout applies, which will cause the status to be removed if no message has been sent. */
  status: string;
  /** The list of messages to rotate through as a loading indicator. Maximum of 10 messages. */
  loading_messages?: ThreadsSetStatusRequestLoadingMessagesList;
  /** Emoji to use as the icon for this message. Overrides `icon_url`. */
  icon_emoji?: string;
  /** Image URL to use as the icon for this message. */
  icon_url?: string;
  /** The bot's username to display. */
  username?: string;
}
export const ThreadsSetStatusRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel_id: S.String,
    thread_ts: S.String,
    status: S.String,
    loading_messages: S.optional(ThreadsSetStatusRequestLoadingMessagesList),
    icon_emoji: S.optional(S.String),
    icon_url: S.optional(S.String),
    username: S.optional(S.String),
  }).pipe(
    T.Http({ method: "POST", uri: "/assistant.threads.setStatus", code: 200 }),
  ),
).annotate({
  identifier: "ThreadsSetStatusRequest",
}) as any as S.Schema<ThreadsSetStatusRequest>;

export interface ThreadsSetStatusResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
}
export const ThreadsSetStatusResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
  }),
).annotate({
  identifier: "ThreadsSetStatusResponse",
}) as any as S.Schema<ThreadsSetStatusResponse>;

export interface ThreadsSetSuggestedPromptsRequest {
  /** Channel ID containing the assistant thread. */
  channel_id: string;
  /** Message timestamp of the thread to set suggested prompts for. */
  thread_ts?: string;
  /** Each prompt should be supplied with its `title` and `message` attribute. */
  prompts: unknown;
  /** Title for the list of provided prompts. For example: Suggested Prompts, Related Questions. */
  title?: string;
}
export const ThreadsSetSuggestedPromptsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel_id: S.String,
    thread_ts: S.optional(S.String),
    prompts: S.Unknown,
    title: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/assistant.threads.setSuggestedPrompts",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ThreadsSetSuggestedPromptsRequest",
}) as any as S.Schema<ThreadsSetSuggestedPromptsRequest>;

export interface ThreadsSetSuggestedPromptsResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
}
export const ThreadsSetSuggestedPromptsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
  }),
).annotate({
  identifier: "ThreadsSetSuggestedPromptsResponse",
}) as any as S.Schema<ThreadsSetSuggestedPromptsResponse>;

export interface ThreadsSetTitleRequest {
  /** Channel ID containing the assistant thread. */
  channel_id: string;
  /** Message timestamp of the thread to set suggested prompts for. */
  thread_ts: string;
  /** The title to use for the thread. */
  title: string;
}
export const ThreadsSetTitleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel_id: S.String,
    thread_ts: S.String,
    title: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/assistant.threads.setTitle", code: 200 }),
  ),
).annotate({
  identifier: "ThreadsSetTitleRequest",
}) as any as S.Schema<ThreadsSetTitleRequest>;

export interface ThreadsSetTitleResponse {
  /** Always `true` (a failed call raises a typed error instead). */
  ok: boolean;
}
export const ThreadsSetTitleResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
  }),
).annotate({
  identifier: "ThreadsSetTitleResponse",
}) as any as S.Schema<ThreadsSetTitleResponse>;

export type SearchContextError = SlackOpError;
/** Searches messages, files, channels and users across your Slack organization. Required scopes — user: `search:read.public`, `search:read.private`, `search:read.im`, `search:read.mpim`, `search:read.files`, `search:read.users`; bot: `search:read.public`, `search:read.files`, `search:read.users` Rate limit tier: 5 Method-specific errors (the `error` slug on the SlackError): - `internal_error` — Internal error. - `rate_limited` — Rate limited. - `missing_query` — Missing query. - `query_too_long` — Query too long. - `feature_not_enabled` — The feature is not available on the current workspace. - `invalid_action_token` — The `action_token` provided is not valid. - `invalid_cursor` — The cursormark provided is not valid. - `context_channel_not_found` — Specified `context_channel_id` is invalid or the user lacks permission to view it. - `missing_scope` — The requested channel types are not allowed by the provided scopes. - `assistant_search_context_disabled` — We're having issues returning your search results. Please wait and try again. See https://docs.slack.dev/reference/methods/assistant.search.context */
export const searchContext: API.OperationMethod<
  SearchContextRequest,
  SearchContextResponse,
  SearchContextError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SearchContextRequest,
  output: SearchContextResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type SearchInfoError = SlackOpError;
/** Returns search capabilities on a given team. Required scopes — user: `search:read.public`; bot: `search:read.public` Rate limit tier: 2 See https://docs.slack.dev/reference/methods/assistant.search.info */
export const searchInfo: API.OperationMethod<
  SearchInfoRequest,
  SearchInfoResponse,
  SearchInfoError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SearchInfoRequest,
  output: SearchInfoResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type ThreadsSetStatusError = SlackOpError;
/** Set the status for an AI assistant thread. Required scopes — bot: `chat:write` Rate limit tier: 5 Method-specific errors (the `error` slug on the SlackError): - `channel_not_found` — Error returned when given an invalid channel_id - `invalid_thread_ts` — Error returned when given an invalid thread_ts - `reserved_username` — Reserved usernames are not allowed to be used. See https://docs.slack.dev/reference/methods/assistant.threads.setStatus */
export const threadsSetStatus: API.OperationMethod<
  ThreadsSetStatusRequest,
  ThreadsSetStatusResponse,
  ThreadsSetStatusError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ThreadsSetStatusRequest,
  output: ThreadsSetStatusResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type ThreadsSetSuggestedPromptsError = SlackOpError;
/** Set suggested prompts for the given assistant thread Required scopes — bot: `assistant:write` Rate limit tier: 4 Method-specific errors (the `error` slug on the SlackError): - `invalid_thread_ts` — Thread not found - `channel_not_found` — Error returned when given an invalid channel_id - `message_not_found` — Error returned when given an invalid message - `not_agent_app` — This method requires the app to be configured as an agent. Enable the Agent feature in your app configuration at https://api.slack.com/apps. See https://docs.slack.dev/reference/methods/assistant.threads.setSuggestedPrompts */
export const threadsSetSuggestedPrompts: API.OperationMethod<
  ThreadsSetSuggestedPromptsRequest,
  ThreadsSetSuggestedPromptsResponse,
  ThreadsSetSuggestedPromptsError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ThreadsSetSuggestedPromptsRequest,
  output: ThreadsSetSuggestedPromptsResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));

export type ThreadsSetTitleError = SlackOpError;
/** Set the title for the given assistant thread Required scopes — bot: `assistant:write` Rate limit tier: 4 Method-specific errors (the `error` slug on the SlackError): - `invalid_thread_ts` — Thread not found - `channel_not_found` — Error returned when given an invalid channel_id See https://docs.slack.dev/reference/methods/assistant.threads.setTitle */
export const threadsSetTitle: API.OperationMethod<
  ThreadsSetTitleRequest,
  ThreadsSetTitleResponse,
  ThreadsSetTitleError,
  SlackOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ThreadsSetTitleRequest,
  output: ThreadsSetTitleResponse,
  errors: [SlackError, SlackRateLimited],
  protocol: SlackProtocol,
  retry: Retry.Retry,
}));
