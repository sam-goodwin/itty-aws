{
  "description": "GET /v2/droplets/{id} and the action responses always populate their envelope member; the spec leaves them optional because they are readOnly. Marking them required removes phantom-undefined handling in consumers. The create response stays optional — its patched shape carries either `droplet` or `droplets` depending on single vs multi create.",
  "patches": [
    {
      "op": "add",
      "path": "/components/responses/existing_droplet/content/application~1json/schema/required",
      "value": ["droplet"]
    },
    {
      "op": "add",
      "path": "/components/responses/droplet_action/content/application~1json/schema/required",
      "value": ["action"]
    },
    {
      "op": "add",
      "path": "/components/responses/action/content/application~1json/schema/required",
      "value": ["action"]
    },
    {
      "op": "add",
      "path": "/components/schemas/action/required",
      "value": ["id", "status"]
    }
  ]
}
