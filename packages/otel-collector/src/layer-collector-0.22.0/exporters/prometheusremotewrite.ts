// Generated by packages/otel-collector/scripts/generate.ts — DO NOT EDIT.
// Source: specs/layer-collector-0.22.0/exporter_prometheusremotewrite.json (+ patches/layer-collector-0.22.0/exporter_prometheusremotewrite.json)

import type * as Duration from "effect/Duration";
import * as Schema from "@distilled.cloud/core/schema";
import { DurationFromGoString } from "../../duration.ts";

export interface PrometheusRemoteWriteExporterAuth<Str = string> {
  /**
   * Id of the `extensions` entry that authenticates this client, e.g. `sigv4auth` or `basicauth/backend`.
   */
  readonly authenticator?: Str;
}
export const PrometheusRemoteWriteExporterAuth = /*@__PURE__*/ Schema.suspend(
  () =>
    Schema.Struct({
      authenticator: Schema.optional(Schema.String),
    }),
) as unknown as Schema.Codec<PrometheusRemoteWriteExporterAuth>;

export interface PrometheusRemoteWriteExporterCompressionParams {
  readonly level?: number;
}
export const PrometheusRemoteWriteExporterCompressionParams =
  /*@__PURE__*/ Schema.suspend(() =>
    Schema.Struct({
      level: Schema.optional(Schema.Int),
    }),
  ) as unknown as Schema.Codec<PrometheusRemoteWriteExporterCompressionParams>;

export interface PrometheusRemoteWriteExporterCookies {
  readonly enabled?: boolean;
}
export const PrometheusRemoteWriteExporterCookies =
  /*@__PURE__*/ Schema.suspend(() =>
    Schema.Struct({
      enabled: Schema.optional(Schema.Boolean),
    }),
  ) as unknown as Schema.Codec<PrometheusRemoteWriteExporterCookies>;

export interface PrometheusRemoteWriteExporterRemoteWriteQueue {
  /**
   * Enabled if false the queue is not enabled, the export requests are executed synchronously.
   */
  readonly enabled?: boolean;
  /**
   * NumWorkers configures the number of workers used by the collector to fan out remote write requests.
   */
  readonly numConsumers?: number;
  /**
   * QueueSize is the maximum number of OTLP metric batches allowed in the queue at a given time. Ignored if Enabled is false.
   */
  readonly queueSize?: number;
}
export const PrometheusRemoteWriteExporterRemoteWriteQueue =
  /*@__PURE__*/ Schema.suspend(() =>
    Schema.Struct({
      enabled: Schema.optional(Schema.Boolean),
      numConsumers: Schema.optional(Schema.Int),
      queueSize: Schema.optional(Schema.Int),
    }).pipe(
      Schema.encodeKeys({
        enabled: "enabled",
        numConsumers: "num_consumers",
        queueSize: "queue_size",
      }),
    ),
  ) as unknown as Schema.Codec<PrometheusRemoteWriteExporterRemoteWriteQueue>;

export interface PrometheusRemoteWriteExporterResourceToTelemetryConversion {
  /**
   * Enabled indicates whether to convert resource attributes to telemetry attributes. Default is `false`.
   */
  readonly enabled?: boolean;
  /**
   * ExcludeServiceAttributes indicates whether to exclude `service.name`, `service.instance.id` and `service.namespace` resource attributes from being converted to metric attributes. Default is `false`. When set to `true`, these attributes will not be added to metric labels since they are already mapped to Prometheus `job` and `instance` labels respectively.
   */
  readonly excludeServiceAttributes?: boolean;
}
export const PrometheusRemoteWriteExporterResourceToTelemetryConversion =
  /*@__PURE__*/ Schema.suspend(() =>
    Schema.Struct({
      enabled: Schema.optional(Schema.Boolean),
      excludeServiceAttributes: Schema.optional(Schema.Boolean),
    }).pipe(
      Schema.encodeKeys({
        enabled: "enabled",
        excludeServiceAttributes: "exclude_service_attributes",
      }),
    ),
  ) as unknown as Schema.Codec<PrometheusRemoteWriteExporterResourceToTelemetryConversion>;

export interface PrometheusRemoteWriteExporterTargetInfo {
  /** Enabled if false the target_info metric is not generated by the exporter */
  readonly enabled?: boolean;
}
export const PrometheusRemoteWriteExporterTargetInfo =
  /*@__PURE__*/ Schema.suspend(() =>
    Schema.Struct({
      enabled: Schema.optional(Schema.Boolean),
    }),
  ) as unknown as Schema.Codec<PrometheusRemoteWriteExporterTargetInfo>;

export interface PrometheusRemoteWriteExporterTlsTpm<Str = string> {
  readonly auth?: Str;
  readonly enabled?: boolean;
  readonly ownerAuth?: Str;
  /**
   * The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
   */
  readonly path?: Str;
}
export const PrometheusRemoteWriteExporterTlsTpm = /*@__PURE__*/ Schema.suspend(
  () =>
    Schema.Struct({
      auth: Schema.optional(Schema.String),
      enabled: Schema.optional(Schema.Boolean),
      ownerAuth: Schema.optional(Schema.String),
      path: Schema.optional(Schema.String),
    }).pipe(
      Schema.encodeKeys({
        auth: "auth",
        enabled: "enabled",
        ownerAuth: "owner_auth",
        path: "path",
      }),
    ),
) as unknown as Schema.Codec<PrometheusRemoteWriteExporterTlsTpm>;

export interface PrometheusRemoteWriteExporterTls<Str = string> {
  /**
   * Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
   */
  readonly caFile?: Str;
  /** In memory PEM encoded cert. (optional) */
  readonly caPem?: Str;
  /** Path to the TLS cert to use for TLS required connections. (optional) */
  readonly certFile?: Str;
  /**
   * In memory PEM encoded TLS cert to use for TLS required connections. (optional)
   */
  readonly certPem?: Str;
  /**
   * CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
   */
  readonly cipherSuites?: ReadonlyArray<Str>;
  /**
   * contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
   */
  readonly curvePreferences?: ReadonlyArray<Str>;
  /**
   * If true, load system CA certificates pool in addition to the certificates configured in this struct.
   */
  readonly includeSystemCaCertsPool?: boolean;
  /**
   * In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
   */
  readonly insecure?: boolean;
  /** InsecureSkipVerify will enable TLS but not verify the certificate. */
  readonly insecureSkipVerify?: boolean;
  /** Path to the TLS key to use for TLS required connections. (optional) */
  readonly keyFile?: Str;
  /**
   * In memory PEM encoded TLS key to use for TLS required connections. (optional)
   */
  readonly keyPem?: Str;
  /**
   * MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
   */
  readonly maxVersion?: Str;
  /**
   * MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
   */
  readonly minVersion?: Str;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly reloadInterval?: Duration.Duration;
  /**
   * ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
   */
  readonly serverNameOverride?: Str;
  /** Trusted platform module configuration */
  readonly tpm?: PrometheusRemoteWriteExporterTlsTpm<Str>;
}
export const PrometheusRemoteWriteExporterTls = /*@__PURE__*/ Schema.suspend(
  () =>
    Schema.Struct({
      caFile: Schema.optional(Schema.String),
      caPem: Schema.optional(Schema.String),
      certFile: Schema.optional(Schema.String),
      certPem: Schema.optional(Schema.String),
      cipherSuites: Schema.optional(Schema.Array(Schema.String)),
      curvePreferences: Schema.optional(Schema.Array(Schema.String)),
      includeSystemCaCertsPool: Schema.optional(Schema.Boolean),
      insecure: Schema.optional(Schema.Boolean),
      insecureSkipVerify: Schema.optional(Schema.Boolean),
      keyFile: Schema.optional(Schema.String),
      keyPem: Schema.optional(Schema.String),
      maxVersion: Schema.optional(Schema.String),
      minVersion: Schema.optional(Schema.String),
      reloadInterval: Schema.optional(DurationFromGoString),
      serverNameOverride: Schema.optional(Schema.String),
      tpm: Schema.optional(PrometheusRemoteWriteExporterTlsTpm),
    }).pipe(
      Schema.encodeKeys({
        caFile: "ca_file",
        caPem: "ca_pem",
        certFile: "cert_file",
        certPem: "cert_pem",
        cipherSuites: "cipher_suites",
        curvePreferences: "curve_preferences",
        includeSystemCaCertsPool: "include_system_ca_certs_pool",
        insecure: "insecure",
        insecureSkipVerify: "insecure_skip_verify",
        keyFile: "key_file",
        keyPem: "key_pem",
        maxVersion: "max_version",
        minVersion: "min_version",
        reloadInterval: "reload_interval",
        serverNameOverride: "server_name_override",
        tpm: "tpm",
      }),
    ),
) as unknown as Schema.Codec<PrometheusRemoteWriteExporterTls>;

export interface PrometheusRemoteWriteExporterWal<Str = string> {
  readonly bufferSize?: number;
  readonly directory?: Str;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly lagRecordFrequency?: Duration.Duration;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly truncateFrequency?: Duration.Duration;
}
export const PrometheusRemoteWriteExporterWal = /*@__PURE__*/ Schema.suspend(
  () =>
    Schema.Struct({
      bufferSize: Schema.optional(Schema.Int),
      directory: Schema.optional(Schema.String),
      lagRecordFrequency: Schema.optional(DurationFromGoString),
      truncateFrequency: Schema.optional(DurationFromGoString),
    }).pipe(
      Schema.encodeKeys({
        bufferSize: "buffer_size",
        directory: "directory",
        lagRecordFrequency: "lag_record_frequency",
        truncateFrequency: "truncate_frequency",
      }),
    ),
) as unknown as Schema.Codec<PrometheusRemoteWriteExporterWal>;

export interface PrometheusRemoteWriteExporter<Str = string> {
  /**
   * AddMetricSuffixes controls whether unit and type suffixes are added to metrics on export Deprecated: Use TranslationStrategy instead. It will be removed in v0.153.0.
   * @deprecated
   */
  readonly addMetricSuffixes?: boolean;
  readonly auth?: PrometheusRemoteWriteExporterAuth<Str>;
  /** The compression key for supported compression types within collector. */
  readonly compression?:
    | "none"
    | "gzip"
    | "zstd"
    | "snappy"
    | "zlib"
    | "deflate"
    | "lz4";
  /** Advanced configuration options for the Compression */
  readonly compressionParams?: PrometheusRemoteWriteExporterCompressionParams;
  /** Send and accept cookies across requests. */
  readonly cookies?: PrometheusRemoteWriteExporterCookies;
  /**
   * DisableKeepAlives, if true, disables HTTP keep-alives and will only use the connection to the server for a single HTTP request. WARNING: enabling this option can result in significant overhead establishing a new HTTP(S) connection for every request. Before enabling this option please consider whether changes to idle connection settings can achieve your goal.
   */
  readonly disableKeepAlives?: boolean;
  /** DisableScopeInfo allows disabling the export of the scope info labels */
  readonly disableScopeInfo?: boolean;
  /**
   * Enabled indicates whether to not retry sending batches in case of export failure.
   */
  readonly enabled?: boolean;
  /** The target URL to send data to (e.g.: http://some.url:9411/v1/traces). */
  readonly endpoint: Str;
  /**
   * ExternalLabels defines a map of label keys and values that are allowed to start with reserved prefix "__"
   */
  readonly externalLabels?: { readonly [key: string]: Str };
  /**
   * Enabling ForceAttemptHTTP2 forces the HTTP transport to use the HTTP/2 protocol. By default, this is set to true. NOTE: HTTP/2 does not support settings such as MaxConnsPerHost, MaxIdleConnsPerHost and MaxIdleConns.
   */
  readonly forceAttemptHttp2?: boolean;
  /**
   * Additional headers attached to each request sent by the client. Header values are opaque since they may be sensitive.
   */
  readonly headers?: { readonly [key: string]: Str };
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly http2PingTimeout?: Duration.Duration;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly http2ReadIdleTimeout?: Duration.Duration;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly idleConnTimeout?: Duration.Duration;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly initialInterval?: Duration.Duration;
  /** maximum amount of parallel requests to do when handling large batch request */
  readonly maxBatchRequestParallelism?: number;
  /** maximum size in bytes of time series batch sent to remote storage */
  readonly maxBatchSizeBytes?: number;
  /**
   * MaxConnsPerHost limits the total number of connections per host, including connections in the dialing, active, and idle states. Default is 0 (unlimited).
   */
  readonly maxConnsPerHost?: number;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly maxElapsedTime?: Duration.Duration;
  /**
   * MaxIdleConns is used to set a limit to the maximum idle HTTP connections the client can keep open. By default, it is set to 100. Zero means no limit.
   */
  readonly maxIdleConns?: number;
  /**
   * MaxIdleConnsPerHost is used to set a limit to the maximum idle HTTP connections the host can keep open. If zero, [net/http.DefaultMaxIdleConnsPerHost] is used.
   */
  readonly maxIdleConnsPerHost?: number;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly maxInterval?: Duration.Duration;
  /** Multiplier is the value multiplied by the backoff interval bounds */
  readonly multiplier?: number;
  /**
   * prefix attached to each exported metric name See: https://prometheus.io/docs/practices/naming/#metric-names
   */
  readonly namespace?: Str;
  /**
   * RemoteWriteProtoMsg controls whether prometheus remote write v1 or v2 is sent.
   */
  readonly protobufMessage?: Str;
  /** ProxyURL setting for the collector */
  readonly proxyUrl?: Str;
  /**
   * RandomizationFactor is a random factor used to calculate next backoffs Randomized interval = RetryInterval * (1 ± RandomizationFactor)
   */
  readonly randomizationFactor?: number;
  /**
   * ReadBufferSize for HTTP client. See http.Transport.ReadBufferSize. Default is 0.
   */
  readonly readBufferSize?: number;
  /**
   * QueueConfig allows users to fine tune the queues that handle outgoing requests.
   */
  readonly remoteWriteQueue?: PrometheusRemoteWriteExporterRemoteWriteQueue;
  /**
   * ResourceToTelemetrySettings is the option for converting resource attributes to telemetry attributes. "Enabled" - A boolean field to enable/disable this option. Default is `false`. If enabled, all the resource attributes will be converted to metric labels by default. "ExcludeServiceAttributes" - If set to `true`, the `service.name`, `service.instance.id` and `service.namespace` resource attributes, which are already converted to `job` and `instance` labels respectively, will be excluded from the final metrics.
   */
  readonly resourceToTelemetryConversion?: PrometheusRemoteWriteExporterResourceToTelemetryConversion;
  /**
   * SendMetadata controls whether prometheus metadata will be generated and sent, this option is ignored when using PRW 2.0, which always includes metadata.
   */
  readonly sendMetadata?: boolean;
  /** TargetInfo allows customizing the target_info metric */
  readonly targetInfo?: PrometheusRemoteWriteExporterTargetInfo;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly timeout?: Duration.Duration;
  /** TLS struct exposes TLS client configuration. */
  readonly tls?: PrometheusRemoteWriteExporterTls<Str>;
  /**
   * How OTLP metric and attribute names become Prometheus metric and label names.
   */
  readonly translationStrategy?:
    | "UnderscoreEscapingWithSuffixes"
    | "UnderscoreEscapingWithoutSuffixes"
    | "NoUTF8EscapingWithSuffixes"
    | "NoTranslation";
  readonly wal?: PrometheusRemoteWriteExporterWal<Str>;
  /**
   * WriteBufferSize for HTTP client. See http.Transport.WriteBufferSize. Default is 0.
   */
  readonly writeBufferSize?: number;
}
export const PrometheusRemoteWriteExporter = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    addMetricSuffixes: Schema.optional(Schema.Boolean),
    auth: Schema.optional(PrometheusRemoteWriteExporterAuth),
    compression: Schema.optional(
      Schema.Literals([
        "none",
        "gzip",
        "zstd",
        "snappy",
        "zlib",
        "deflate",
        "lz4",
      ]),
    ),
    compressionParams: Schema.optional(
      PrometheusRemoteWriteExporterCompressionParams,
    ),
    cookies: Schema.optional(PrometheusRemoteWriteExporterCookies),
    disableKeepAlives: Schema.optional(Schema.Boolean),
    disableScopeInfo: Schema.optional(Schema.Boolean),
    enabled: Schema.optional(Schema.Boolean),
    endpoint: Schema.String,
    externalLabels: Schema.optional(
      Schema.Record(Schema.String, Schema.String),
    ),
    forceAttemptHttp2: Schema.optional(Schema.Boolean),
    headers: Schema.optional(Schema.Record(Schema.String, Schema.String)),
    http2PingTimeout: Schema.optional(DurationFromGoString),
    http2ReadIdleTimeout: Schema.optional(DurationFromGoString),
    idleConnTimeout: Schema.optional(DurationFromGoString),
    initialInterval: Schema.optional(DurationFromGoString),
    maxBatchRequestParallelism: Schema.optional(Schema.Int),
    maxBatchSizeBytes: Schema.optional(Schema.Int),
    maxConnsPerHost: Schema.optional(Schema.Int),
    maxElapsedTime: Schema.optional(DurationFromGoString),
    maxIdleConns: Schema.optional(Schema.Int),
    maxIdleConnsPerHost: Schema.optional(Schema.Int),
    maxInterval: Schema.optional(DurationFromGoString),
    multiplier: Schema.optional(Schema.Number),
    namespace: Schema.optional(Schema.String),
    protobufMessage: Schema.optional(Schema.String),
    proxyUrl: Schema.optional(Schema.String),
    randomizationFactor: Schema.optional(Schema.Number),
    readBufferSize: Schema.optional(Schema.Int),
    remoteWriteQueue: Schema.optional(
      PrometheusRemoteWriteExporterRemoteWriteQueue,
    ),
    resourceToTelemetryConversion: Schema.optional(
      PrometheusRemoteWriteExporterResourceToTelemetryConversion,
    ),
    sendMetadata: Schema.optional(Schema.Boolean),
    targetInfo: Schema.optional(PrometheusRemoteWriteExporterTargetInfo),
    timeout: Schema.optional(DurationFromGoString),
    tls: Schema.optional(PrometheusRemoteWriteExporterTls),
    translationStrategy: Schema.optional(
      Schema.Literals([
        "UnderscoreEscapingWithSuffixes",
        "UnderscoreEscapingWithoutSuffixes",
        "NoUTF8EscapingWithSuffixes",
        "NoTranslation",
      ]),
    ),
    wal: Schema.optional(PrometheusRemoteWriteExporterWal),
    writeBufferSize: Schema.optional(Schema.Int),
  }).pipe(
    Schema.encodeKeys({
      addMetricSuffixes: "add_metric_suffixes",
      auth: "auth",
      compression: "compression",
      compressionParams: "compression_params",
      cookies: "cookies",
      disableKeepAlives: "disable_keep_alives",
      disableScopeInfo: "disable_scope_info",
      enabled: "enabled",
      endpoint: "endpoint",
      externalLabels: "external_labels",
      forceAttemptHttp2: "force_attempt_http2",
      headers: "headers",
      http2PingTimeout: "http2_ping_timeout",
      http2ReadIdleTimeout: "http2_read_idle_timeout",
      idleConnTimeout: "idle_conn_timeout",
      initialInterval: "initial_interval",
      maxBatchRequestParallelism: "max_batch_request_parallelism",
      maxBatchSizeBytes: "max_batch_size_bytes",
      maxConnsPerHost: "max_conns_per_host",
      maxElapsedTime: "max_elapsed_time",
      maxIdleConns: "max_idle_conns",
      maxIdleConnsPerHost: "max_idle_conns_per_host",
      maxInterval: "max_interval",
      multiplier: "multiplier",
      namespace: "namespace",
      protobufMessage: "protobuf_message",
      proxyUrl: "proxy_url",
      randomizationFactor: "randomization_factor",
      readBufferSize: "read_buffer_size",
      remoteWriteQueue: "remote_write_queue",
      resourceToTelemetryConversion: "resource_to_telemetry_conversion",
      sendMetadata: "send_metadata",
      targetInfo: "target_info",
      timeout: "timeout",
      tls: "tls",
      translationStrategy: "translation_strategy",
      wal: "wal",
      writeBufferSize: "write_buffer_size",
    }),
  ),
) as unknown as Schema.Codec<PrometheusRemoteWriteExporter>;

/** The component's wire type, as written in `exporters:` of the config file. */
export const type = "prometheusremotewrite" as const;

/** The config section this component is declared in. */
export const section = "exporters" as const;
