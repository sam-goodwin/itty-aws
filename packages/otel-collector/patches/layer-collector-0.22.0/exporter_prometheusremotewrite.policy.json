{
  "kind": "build-policy",
  "description": "BUILD POLICY — the closed lambda build registers no middleware extension, so `middlewares` has no value the collector would accept.",
  "patches": [
    {
      "op": "remove",
      "path": "/properties/middlewares"
    }
  ]
}
