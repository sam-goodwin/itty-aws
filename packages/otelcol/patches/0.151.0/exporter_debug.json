{
  "description": "debug exporter: `verbosity` is a `configtelemetry.Level` with a custom unmarshaler — it reflects as an integer but the config file spells it `basic`/`normal`/`detailed`.",
  "patches": [
    {
      "op": "replace",
      "path": "/properties/verbosity",
      "value": {
        "description": "Verbosity defines the debug exporter verbosity.",
        "type": "string",
        "enum": ["basic", "normal", "detailed"]
      }
    },
    {
      "op": "replace",
      "path": "/properties/sending_queue/properties/sizer",
      "value": {
        "description": "Unit the size limits are counted in.",
        "type": "string",
        "enum": ["requests", "items", "bytes"]
      }
    },
    {
      "op": "replace",
      "path": "/properties/sending_queue/properties/batch/properties/sizer",
      "value": {
        "description": "Unit the batch size limits are counted in. If unset, the queue's sizer is used.",
        "type": "string",
        "enum": ["requests", "items", "bytes"]
      }
    },
    {
      "op": "remove",
      "path": "/properties/sending_queue/properties/storage"
    }
  ]
}
