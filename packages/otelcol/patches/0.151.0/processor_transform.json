{
  "description": "transform processor: `error_mode` is an OTTL `ErrorMode`, a closed set that reflects as a bare string.",
  "patches": [
    {
      "op": "replace",
      "path": "/properties/error_mode",
      "value": {
        "description": "How the component reacts to an error while running an OTTL statement.",
        "type": "string",
        "enum": ["ignore", "silent", "propagate"]
      }
    },
    {
      "op": "replace",
      "path": "/properties/trace_statements/items/properties/error_mode",
      "value": {
        "description": "How the component reacts to an error while running an OTTL statement.",
        "type": "string",
        "enum": ["ignore", "silent", "propagate"]
      }
    },
    {
      "op": "replace",
      "path": "/properties/metric_statements/items/properties/error_mode",
      "value": {
        "description": "How the component reacts to an error while running an OTTL statement.",
        "type": "string",
        "enum": ["ignore", "silent", "propagate"]
      }
    },
    {
      "op": "replace",
      "path": "/properties/log_statements/items/properties/error_mode",
      "value": {
        "description": "How the component reacts to an error while running an OTTL statement.",
        "type": "string",
        "enum": ["ignore", "silent", "propagate"]
      }
    }
  ]
}
