{
  "description": "telemetryapi receiver: `types` and `metrics_temporality` are closed sets in Go that reflect as bare strings.",
  "patches": [
    {
      "op": "replace",
      "path": "/properties/types",
      "value": {
        "description": "Lambda Telemetry API event streams to subscribe to.",
        "type": "array",
        "items": {
          "type": "string",
          "enum": ["platform", "function", "extension"]
        }
      }
    },
    {
      "op": "replace",
      "path": "/properties/metrics_temporality",
      "value": {
        "description": "Aggregation temporality for the metrics derived from platform events.",
        "type": "string",
        "enum": ["delta", "cumulative"]
      }
    }
  ]
}
