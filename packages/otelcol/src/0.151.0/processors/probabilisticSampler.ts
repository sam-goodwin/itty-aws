// Generated by packages/otelcol/scripts/generate.ts — DO NOT EDIT.
// Source: specs/0.151.0/processor_probabilistic_sampler.json (+ patches/0.151.0/processor_probabilistic_sampler.json)

import * as Schema from "@distilled.cloud/core/schema";

export interface ProbabilisticSamplerProcessor<Str = string> {
  /**
   * FailClosed indicates to not sample data (the processor will fail "closed") in case of error, such as failure to parse the tracestate field or missing the randomness attribute. By default, failure cases are sampled (the processor is fails "open").  Sampling priority-based decisions are made after FailClosed is processed, making it possible to sample despite errors using priority.
   */
  readonly failClosed?: boolean;
  /**
   * FromAttribute (logs only) The optional name of a log record attribute used for sampling purposes, such as a unique log record ID. The value of the attribute is only used if the trace ID is absent or if `attribute_source` is set to `record`.
   */
  readonly fromAttribute?: Str;
  /**
   * HashSeed allows one to configure the hashing seed. This is important in scenarios where multiple layers of collectors have different sampling rates: if they use the same seed all passing one layer may pass the other even if they have different sampling rates, configuring different seeds avoids that.
   */
  readonly hashSeed?: number;
  /** Sampling mode. */
  readonly mode?: "hash_seed" | "equalizing" | "proportional";
  /**
   * SamplingPercentage is the percentage rate at which traces or logs are going to be sampled. Defaults to zero, i.e.: no sample. Values greater or equal 100 are treated as "sample all traces/logs".
   */
  readonly samplingPercentage: number;
  /**
   * SamplingPrecision is how many hex digits of sampling threshold will be encoded, from 1 up to 14.  Default is 4. 0 is treated as full precision.
   */
  readonly samplingPrecision?: number;
  /**
   * SamplingPriority (logs only) enables using a log record attribute as the sampling priority of the log record.
   */
  readonly samplingPriority?: Str;
}
export const ProbabilisticSamplerProcessor = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    failClosed: Schema.optional(Schema.Boolean),
    fromAttribute: Schema.optional(Schema.String),
    hashSeed: Schema.optional(Schema.Int),
    mode: Schema.optional(
      Schema.Literals(["hash_seed", "equalizing", "proportional"]),
    ),
    samplingPercentage: Schema.Number,
    samplingPrecision: Schema.optional(Schema.Int),
    samplingPriority: Schema.optional(Schema.String),
  }).pipe(
    Schema.encodeKeys({
      failClosed: "fail_closed",
      fromAttribute: "from_attribute",
      hashSeed: "hash_seed",
      mode: "mode",
      samplingPercentage: "sampling_percentage",
      samplingPrecision: "sampling_precision",
      samplingPriority: "sampling_priority",
    }),
  ),
) as unknown as Schema.Codec<ProbabilisticSamplerProcessor>;

/** The component's wire type, as written in `processors:` of the config file. */
export const type = "probabilistic_sampler" as const;

/** The config section this component is declared in. */
export const section = "processors" as const;
