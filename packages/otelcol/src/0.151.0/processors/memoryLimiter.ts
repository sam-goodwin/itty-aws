// Generated by packages/otelcol/scripts/generate.ts — DO NOT EDIT.
// Source: specs/0.151.0/processor_memory_limiter.json (+ patches/0.151.0/processor_memory_limiter.json)

import type * as Duration from "effect/Duration";
import * as Schema from "@distilled.cloud/core/schema";
import { DurationFromGoString } from "../../duration.ts";

export interface MemoryLimiterProcessor {
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly checkInterval?: Duration.Duration;
  /**
   * MemoryLimitMiB is the maximum amount of memory, in MiB, targeted to be allocated by the process.
   */
  readonly limitMib?: number;
  /**
   * MemoryLimitPercentage is the maximum amount of memory, in %, targeted to be allocated by the process. The fixed memory settings MemoryLimitMiB has a higher precedence.
   */
  readonly limitPercentage?: number;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly minGcIntervalWhenHardLimited?: Duration.Duration;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly minGcIntervalWhenSoftLimited?: Duration.Duration;
  /**
   * MemorySpikeLimitMiB is the maximum, in MiB, spike expected between the measurements of memory usage.
   */
  readonly spikeLimitMib?: number;
  /**
   * MemorySpikePercentage is the maximum, in percents against the total memory, spike expected between the measurements of memory usage.
   */
  readonly spikeLimitPercentage?: number;
}
export const MemoryLimiterProcessor = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    checkInterval: Schema.optional(DurationFromGoString),
    limitMib: Schema.optional(Schema.Int),
    limitPercentage: Schema.optional(Schema.Int),
    minGcIntervalWhenHardLimited: Schema.optional(DurationFromGoString),
    minGcIntervalWhenSoftLimited: Schema.optional(DurationFromGoString),
    spikeLimitMib: Schema.optional(Schema.Int),
    spikeLimitPercentage: Schema.optional(Schema.Int),
  }).pipe(
    Schema.encodeKeys({
      checkInterval: "check_interval",
      limitMib: "limit_mib",
      limitPercentage: "limit_percentage",
      minGcIntervalWhenHardLimited: "min_gc_interval_when_hard_limited",
      minGcIntervalWhenSoftLimited: "min_gc_interval_when_soft_limited",
      spikeLimitMib: "spike_limit_mib",
      spikeLimitPercentage: "spike_limit_percentage",
    }),
  ),
) as unknown as Schema.Codec<MemoryLimiterProcessor>;

/** The component's wire type, as written in `processors:` of the config file. */
export const type = "memory_limiter" as const;

/** The config section this component is declared in. */
export const section = "processors" as const;
