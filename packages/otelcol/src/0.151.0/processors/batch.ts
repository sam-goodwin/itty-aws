// Generated by packages/otelcol/scripts/generate.ts — DO NOT EDIT.
// Source: specs/0.151.0/processor_batch.json (+ patches/0.151.0/processor_batch.json)

import type * as Duration from "effect/Duration";
import * as Schema from "@distilled.cloud/core/schema";
import { DurationFromGoString } from "../../duration.ts";

export interface BatchProcessor<Str = string> {
  /**
   * MetadataCardinalityLimit indicates the maximum number of batcher instances that will be created through a distinct combination of MetadataKeys.
   */
  readonly metadataCardinalityLimit?: number;
  /**
   * MetadataKeys is a list of client.Metadata keys that will be used to form distinct batchers.  If this setting is empty, a single batcher instance will be used.  When this setting is not empty, one batcher will be used per distinct combination of values for the listed metadata keys. Empty value and unset metadata are treated as distinct cases. Entries are case-insensitive.  Duplicated entries will trigger a validation error.
   */
  readonly metadataKeys?: ReadonlyArray<Str>;
  /**
   * SendBatchMaxSize is the maximum size of a batch. It must be larger than SendBatchSize. Larger batches are split into smaller units. Default value is 0, that means no maximum size.
   */
  readonly sendBatchMaxSize?: number;
  /**
   * SendBatchSize is the size of a batch which after hit, will trigger it to be sent. When this is set to zero, the batch size is ignored and data will be sent immediately subject to only send_batch_max_size.
   */
  readonly sendBatchSize?: number;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly timeout?: Duration.Duration;
}
export const BatchProcessor = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    metadataCardinalityLimit: Schema.optional(Schema.Int),
    metadataKeys: Schema.optional(Schema.Array(Schema.String)),
    sendBatchMaxSize: Schema.optional(Schema.Int),
    sendBatchSize: Schema.optional(Schema.Int),
    timeout: Schema.optional(DurationFromGoString),
  }).pipe(
    Schema.encodeKeys({
      metadataCardinalityLimit: "metadata_cardinality_limit",
      metadataKeys: "metadata_keys",
      sendBatchMaxSize: "send_batch_max_size",
      sendBatchSize: "send_batch_size",
      timeout: "timeout",
    }),
  ),
) as unknown as Schema.Codec<BatchProcessor>;

/** The component's wire type, as written in `processors:` of the config file. */
export const type = "batch" as const;

/** The config section this component is declared in. */
export const section = "processors" as const;
