// Generated by packages/otelcol/scripts/generate.ts — DO NOT EDIT.
// Source: specs/0.151.0/receiver_otlp.json (+ patches/0.151.0/receiver_otlp.json)

import type * as Duration from "effect/Duration";
import * as Schema from "@distilled.cloud/core/schema";
import { DurationFromGoString } from "../../duration.ts";

export interface OtlpReceiverProtocolsGrpcAuth<Str = string> {
  /** Id of the `extensions` entry that authenticates incoming requests. */
  readonly authenticator?: Str;
}
export const OtlpReceiverProtocolsGrpcAuth = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    authenticator: Schema.optional(Schema.String),
  }),
) as unknown as Schema.Codec<OtlpReceiverProtocolsGrpcAuth>;

export interface OtlpReceiverProtocolsGrpcDialer {
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly timeout?: Duration.Duration;
}
export const OtlpReceiverProtocolsGrpcDialer = /*@__PURE__*/ Schema.suspend(
  () =>
    Schema.Struct({
      timeout: Schema.optional(DurationFromGoString),
    }),
) as unknown as Schema.Codec<OtlpReceiverProtocolsGrpcDialer>;

export interface OtlpReceiverProtocolsGrpcKeepaliveEnforcementPolicy {
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly minTime?: Duration.Duration;
  readonly permitWithoutStream?: boolean;
}
export const OtlpReceiverProtocolsGrpcKeepaliveEnforcementPolicy =
  /*@__PURE__*/ Schema.suspend(() =>
    Schema.Struct({
      minTime: Schema.optional(DurationFromGoString),
      permitWithoutStream: Schema.optional(Schema.Boolean),
    }).pipe(
      Schema.encodeKeys({
        minTime: "min_time",
        permitWithoutStream: "permit_without_stream",
      }),
    ),
  ) as unknown as Schema.Codec<OtlpReceiverProtocolsGrpcKeepaliveEnforcementPolicy>;

export interface OtlpReceiverProtocolsGrpcKeepaliveServerParameters {
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly maxConnectionAge?: Duration.Duration;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly maxConnectionAgeGrace?: Duration.Duration;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly maxConnectionIdle?: Duration.Duration;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly time?: Duration.Duration;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly timeout?: Duration.Duration;
}
export const OtlpReceiverProtocolsGrpcKeepaliveServerParameters =
  /*@__PURE__*/ Schema.suspend(() =>
    Schema.Struct({
      maxConnectionAge: Schema.optional(DurationFromGoString),
      maxConnectionAgeGrace: Schema.optional(DurationFromGoString),
      maxConnectionIdle: Schema.optional(DurationFromGoString),
      time: Schema.optional(DurationFromGoString),
      timeout: Schema.optional(DurationFromGoString),
    }).pipe(
      Schema.encodeKeys({
        maxConnectionAge: "max_connection_age",
        maxConnectionAgeGrace: "max_connection_age_grace",
        maxConnectionIdle: "max_connection_idle",
        time: "time",
        timeout: "timeout",
      }),
    ),
  ) as unknown as Schema.Codec<OtlpReceiverProtocolsGrpcKeepaliveServerParameters>;

export interface OtlpReceiverProtocolsGrpcKeepalive {
  readonly enforcementPolicy?: OtlpReceiverProtocolsGrpcKeepaliveEnforcementPolicy;
  readonly serverParameters?: OtlpReceiverProtocolsGrpcKeepaliveServerParameters;
}
export const OtlpReceiverProtocolsGrpcKeepalive = /*@__PURE__*/ Schema.suspend(
  () =>
    Schema.Struct({
      enforcementPolicy: Schema.optional(
        OtlpReceiverProtocolsGrpcKeepaliveEnforcementPolicy,
      ),
      serverParameters: Schema.optional(
        OtlpReceiverProtocolsGrpcKeepaliveServerParameters,
      ),
    }).pipe(
      Schema.encodeKeys({
        enforcementPolicy: "enforcement_policy",
        serverParameters: "server_parameters",
      }),
    ),
) as unknown as Schema.Codec<OtlpReceiverProtocolsGrpcKeepalive>;

export interface OtlpReceiverProtocolsGrpcTlsTpm<Str = string> {
  readonly auth?: Str;
  readonly enabled?: boolean;
  readonly ownerAuth?: Str;
  /**
   * The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
   */
  readonly path?: Str;
}
export const OtlpReceiverProtocolsGrpcTlsTpm = /*@__PURE__*/ Schema.suspend(
  () =>
    Schema.Struct({
      auth: Schema.optional(Schema.String),
      enabled: Schema.optional(Schema.Boolean),
      ownerAuth: Schema.optional(Schema.String),
      path: Schema.optional(Schema.String),
    }).pipe(
      Schema.encodeKeys({
        auth: "auth",
        enabled: "enabled",
        ownerAuth: "owner_auth",
        path: "path",
      }),
    ),
) as unknown as Schema.Codec<OtlpReceiverProtocolsGrpcTlsTpm>;

export interface OtlpReceiverProtocolsGrpcTls<Str = string> {
  /**
   * Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
   */
  readonly caFile?: Str;
  /** In memory PEM encoded cert. (optional) */
  readonly caPem?: Str;
  /** Path to the TLS cert to use for TLS required connections. (optional) */
  readonly certFile?: Str;
  /**
   * In memory PEM encoded TLS cert to use for TLS required connections. (optional)
   */
  readonly certPem?: Str;
  /**
   * CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
   */
  readonly cipherSuites?: ReadonlyArray<Str>;
  /**
   * Path to the TLS cert to use by the server to verify a client certificate. (optional) This sets the ClientCAs and ClientAuth to RequireAndVerifyClientCert in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
   */
  readonly clientCaFile?: Str;
  /** Reload the ClientCAs file when it is modified (optional, default false) */
  readonly clientCaFileReload?: boolean;
  /**
   * contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
   */
  readonly curvePreferences?: ReadonlyArray<Str>;
  /**
   * If true, load system CA certificates pool in addition to the certificates configured in this struct.
   */
  readonly includeSystemCaCertsPool?: boolean;
  /** Path to the TLS key to use for TLS required connections. (optional) */
  readonly keyFile?: Str;
  /**
   * In memory PEM encoded TLS key to use for TLS required connections. (optional)
   */
  readonly keyPem?: Str;
  /**
   * MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
   */
  readonly maxVersion?: Str;
  /**
   * MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
   */
  readonly minVersion?: Str;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly reloadInterval?: Duration.Duration;
  /** Trusted platform module configuration */
  readonly tpm?: OtlpReceiverProtocolsGrpcTlsTpm<Str>;
}
export const OtlpReceiverProtocolsGrpcTls = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    caFile: Schema.optional(Schema.String),
    caPem: Schema.optional(Schema.String),
    certFile: Schema.optional(Schema.String),
    certPem: Schema.optional(Schema.String),
    cipherSuites: Schema.optional(Schema.Array(Schema.String)),
    clientCaFile: Schema.optional(Schema.String),
    clientCaFileReload: Schema.optional(Schema.Boolean),
    curvePreferences: Schema.optional(Schema.Array(Schema.String)),
    includeSystemCaCertsPool: Schema.optional(Schema.Boolean),
    keyFile: Schema.optional(Schema.String),
    keyPem: Schema.optional(Schema.String),
    maxVersion: Schema.optional(Schema.String),
    minVersion: Schema.optional(Schema.String),
    reloadInterval: Schema.optional(DurationFromGoString),
    tpm: Schema.optional(OtlpReceiverProtocolsGrpcTlsTpm),
  }).pipe(
    Schema.encodeKeys({
      caFile: "ca_file",
      caPem: "ca_pem",
      certFile: "cert_file",
      certPem: "cert_pem",
      cipherSuites: "cipher_suites",
      clientCaFile: "client_ca_file",
      clientCaFileReload: "client_ca_file_reload",
      curvePreferences: "curve_preferences",
      includeSystemCaCertsPool: "include_system_ca_certs_pool",
      keyFile: "key_file",
      keyPem: "key_pem",
      maxVersion: "max_version",
      minVersion: "min_version",
      reloadInterval: "reload_interval",
      tpm: "tpm",
    }),
  ),
) as unknown as Schema.Codec<OtlpReceiverProtocolsGrpcTls>;

export interface OtlpReceiverProtocolsGrpc<Str = string> {
  readonly auth?: OtlpReceiverProtocolsGrpcAuth<Str>;
  /** DialerConfig contains options for connecting to an address. */
  readonly dialer?: OtlpReceiverProtocolsGrpcDialer;
  /**
   * Endpoint configures the address for this network connection. For TCP and UDP networks, the address has the form "host:port". The host must be a literal IP address, or a host name that can be resolved to IP addresses. The port must be a literal port number or a service name. If the host is a literal IPv6 address it must be enclosed in square brackets, as in "[2001:db8::1]:80" or "[fe80::1%zone]:80". The zone specifies the scope of the literal IPv6 address as defined in RFC 4007.
   */
  readonly endpoint?: Str;
  /**
   * Include propagates the incoming connection's metadata to downstream consumers.
   */
  readonly includeMetadata?: boolean;
  readonly keepalive?: OtlpReceiverProtocolsGrpcKeepalive;
  /**
   * MaxConcurrentStreams sets the limit on the number of concurrent streams to each ServerTransport. It has effect only for streaming RPCs.
   */
  readonly maxConcurrentStreams?: number;
  /**
   * MaxRecvMsgSizeMiB sets the maximum size (in MiB) of messages accepted by the server.
   */
  readonly maxRecvMsgSizeMib?: number;
  /**
   * ReadBufferSize for gRPC server. See grpc.ReadBufferSize. (https://godoc.org/google.golang.org/grpc#ReadBufferSize).
   */
  readonly readBufferSize?: number;
  readonly tls?: OtlpReceiverProtocolsGrpcTls<Str>;
  /**
   * Transport to use. Allowed protocols are "tcp", "tcp4" (IPv4-only), "tcp6" (IPv6-only), "udp", "udp4" (IPv4-only), "udp6" (IPv6-only), "ip", "ip4" (IPv4-only), "ip6" (IPv6-only), "unix", "unixgram", "unixpacket" and "npipe" (Windows named pipes, Windows-only).
   */
  readonly transport?: Str;
  /**
   * WriteBufferSize for gRPC server. See grpc.WriteBufferSize. (https://godoc.org/google.golang.org/grpc#WriteBufferSize).
   */
  readonly writeBufferSize?: number;
}
export const OtlpReceiverProtocolsGrpc = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    auth: Schema.optional(OtlpReceiverProtocolsGrpcAuth),
    dialer: Schema.optional(OtlpReceiverProtocolsGrpcDialer),
    endpoint: Schema.optional(Schema.String),
    includeMetadata: Schema.optional(Schema.Boolean),
    keepalive: Schema.optional(OtlpReceiverProtocolsGrpcKeepalive),
    maxConcurrentStreams: Schema.optional(Schema.Int),
    maxRecvMsgSizeMib: Schema.optional(Schema.Int),
    readBufferSize: Schema.optional(Schema.Int),
    tls: Schema.optional(OtlpReceiverProtocolsGrpcTls),
    transport: Schema.optional(Schema.String),
    writeBufferSize: Schema.optional(Schema.Int),
  }).pipe(
    Schema.encodeKeys({
      auth: "auth",
      dialer: "dialer",
      endpoint: "endpoint",
      includeMetadata: "include_metadata",
      keepalive: "keepalive",
      maxConcurrentStreams: "max_concurrent_streams",
      maxRecvMsgSizeMib: "max_recv_msg_size_mib",
      readBufferSize: "read_buffer_size",
      tls: "tls",
      transport: "transport",
      writeBufferSize: "write_buffer_size",
    }),
  ),
) as unknown as Schema.Codec<OtlpReceiverProtocolsGrpc>;

export interface OtlpReceiverProtocolsHttpAuth<Str = string> {
  /** Id of the `extensions` entry that authenticates incoming requests. */
  readonly authenticator?: Str;
  /**
   * RequestParameters is a list of parameters that should be extracted from the request and added to the context. When a parameter is found in both the query string and the header, the value from the query string will be used.
   */
  readonly requestParams?: ReadonlyArray<Str>;
}
export const OtlpReceiverProtocolsHttpAuth = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    authenticator: Schema.optional(Schema.String),
    requestParams: Schema.optional(Schema.Array(Schema.String)),
  }).pipe(
    Schema.encodeKeys({
      authenticator: "authenticator",
      requestParams: "request_params",
    }),
  ),
) as unknown as Schema.Codec<OtlpReceiverProtocolsHttpAuth>;

export interface OtlpReceiverProtocolsHttpCors<Str = string> {
  /**
   * AllowedHeaders sets what headers will be allowed in CORS requests. The Accept, Accept-Language, Content-Type, and Content-Language headers are implicitly allowed. If no headers are listed, X-Requested-With will also be accepted by default. Include "*" to allow any request header.
   */
  readonly allowedHeaders?: ReadonlyArray<Str>;
  /**
   * AllowedOrigins sets the allowed values of the Origin header for HTTP/JSON requests to an OTLP receiver. An origin may contain a wildcard (*) to replace 0 or more characters (e.g., "http://*.domain.com", or "*" to allow any origin).
   */
  readonly allowedOrigins?: ReadonlyArray<Str>;
  /**
   * MaxAge sets the value of the Access-Control-Max-Age response header. Set it to the number of seconds that browsers should cache a CORS preflight response for.
   */
  readonly maxAge?: number;
}
export const OtlpReceiverProtocolsHttpCors = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    allowedHeaders: Schema.optional(Schema.Array(Schema.String)),
    allowedOrigins: Schema.optional(Schema.Array(Schema.String)),
    maxAge: Schema.optional(Schema.Int),
  }).pipe(
    Schema.encodeKeys({
      allowedHeaders: "allowed_headers",
      allowedOrigins: "allowed_origins",
      maxAge: "max_age",
    }),
  ),
) as unknown as Schema.Codec<OtlpReceiverProtocolsHttpCors>;

export interface OtlpReceiverProtocolsHttp<Str = string> {
  readonly auth?: OtlpReceiverProtocolsHttpAuth<Str>;
  /**
   * CompressionAlgorithms configures the list of compression algorithms the server can accept. Default: ["", "gzip", "zstd", "zlib", "snappy", "deflate"]
   */
  readonly compressionAlgorithms?: ReadonlyArray<Str>;
  readonly cors?: OtlpReceiverProtocolsHttpCors<Str>;
  /** DialerConfig contains options for connecting to an address. */
  readonly dialer?: OtlpReceiverProtocolsGrpcDialer;
  /**
   * Endpoint configures the address for this network connection. For TCP and UDP networks, the address has the form "host:port". The host must be a literal IP address, or a host name that can be resolved to IP addresses. The port must be a literal port number or a service name. If the host is a literal IPv6 address it must be enclosed in square brackets, as in "[2001:db8::1]:80" or "[fe80::1%zone]:80". The zone specifies the scope of the literal IPv6 address as defined in RFC 4007.
   */
  readonly endpoint?: Str;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly idleTimeout?: Duration.Duration;
  /**
   * IncludeMetadata propagates the client metadata from the incoming requests to the downstream consumers
   */
  readonly includeMetadata?: boolean;
  /**
   * KeepAlivesEnabled controls whether HTTP keep-alives are enabled. By default, keep-alives are always enabled. Only very resource-constrained environments should disable them.
   */
  readonly keepAlivesEnabled?: boolean;
  /** The URL path to receive logs on. If omitted "/v1/logs" will be used. */
  readonly logsUrlPath?: Str;
  /**
   * MaxRequestBodySize sets the maximum request body size in bytes. Default: 20MiB.
   */
  readonly maxRequestBodySize?: number;
  /** The URL path to receive metrics on. If omitted "/v1/metrics" will be used. */
  readonly metricsUrlPath?: Str;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly readHeaderTimeout?: Duration.Duration;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly readTimeout?: Duration.Duration;
  /** Headers added to every response. */
  readonly responseHeaders?: { readonly [key: string]: Str };
  readonly tls?: OtlpReceiverProtocolsGrpcTls<Str>;
  /** The URL path to receive traces on. If omitted "/v1/traces" will be used. */
  readonly tracesUrlPath?: Str;
  /**
   * Transport to use. Allowed protocols are "tcp", "tcp4" (IPv4-only), "tcp6" (IPv6-only), "udp", "udp4" (IPv4-only), "udp6" (IPv6-only), "ip", "ip4" (IPv4-only), "ip6" (IPv6-only), "unix", "unixgram", "unixpacket" and "npipe" (Windows named pipes, Windows-only).
   */
  readonly transport?: Str;
  /** Duration string (e.g., '1s', '5m', '1h') */
  readonly writeTimeout?: Duration.Duration;
}
export const OtlpReceiverProtocolsHttp = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    auth: Schema.optional(OtlpReceiverProtocolsHttpAuth),
    compressionAlgorithms: Schema.optional(Schema.Array(Schema.String)),
    cors: Schema.optional(OtlpReceiverProtocolsHttpCors),
    dialer: Schema.optional(OtlpReceiverProtocolsGrpcDialer),
    endpoint: Schema.optional(Schema.String),
    idleTimeout: Schema.optional(DurationFromGoString),
    includeMetadata: Schema.optional(Schema.Boolean),
    keepAlivesEnabled: Schema.optional(Schema.Boolean),
    logsUrlPath: Schema.optional(Schema.String),
    maxRequestBodySize: Schema.optional(Schema.Int),
    metricsUrlPath: Schema.optional(Schema.String),
    readHeaderTimeout: Schema.optional(DurationFromGoString),
    readTimeout: Schema.optional(DurationFromGoString),
    responseHeaders: Schema.optional(
      Schema.Record(Schema.String, Schema.String),
    ),
    tls: Schema.optional(OtlpReceiverProtocolsGrpcTls),
    tracesUrlPath: Schema.optional(Schema.String),
    transport: Schema.optional(Schema.String),
    writeTimeout: Schema.optional(DurationFromGoString),
  }).pipe(
    Schema.encodeKeys({
      auth: "auth",
      compressionAlgorithms: "compression_algorithms",
      cors: "cors",
      dialer: "dialer",
      endpoint: "endpoint",
      idleTimeout: "idle_timeout",
      includeMetadata: "include_metadata",
      keepAlivesEnabled: "keep_alives_enabled",
      logsUrlPath: "logs_url_path",
      maxRequestBodySize: "max_request_body_size",
      metricsUrlPath: "metrics_url_path",
      readHeaderTimeout: "read_header_timeout",
      readTimeout: "read_timeout",
      responseHeaders: "response_headers",
      tls: "tls",
      tracesUrlPath: "traces_url_path",
      transport: "transport",
      writeTimeout: "write_timeout",
    }),
  ),
) as unknown as Schema.Codec<OtlpReceiverProtocolsHttp>;

export interface OtlpReceiverProtocols<Str = string> {
  readonly grpc?: OtlpReceiverProtocolsGrpc<Str>;
  readonly http?: OtlpReceiverProtocolsHttp<Str>;
}
export const OtlpReceiverProtocols = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    grpc: Schema.optional(OtlpReceiverProtocolsGrpc),
    http: Schema.optional(OtlpReceiverProtocolsHttp),
  }),
) as unknown as Schema.Codec<OtlpReceiverProtocols>;

export interface OtlpReceiver<Str = string> {
  /**
   * Protocols is the configuration for the supported protocols, currently gRPC and HTTP (Proto and JSON).
   */
  readonly protocols: OtlpReceiverProtocols<Str>;
}
export const OtlpReceiver = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    protocols: OtlpReceiverProtocols,
  }),
) as unknown as Schema.Codec<OtlpReceiver>;

/** The component's wire type, as written in `receivers:` of the config file. */
export const type = "otlp" as const;

/** The config section this component is declared in. */
export const section = "receivers" as const;
