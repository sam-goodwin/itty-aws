// Generated by packages/otelcol/scripts/generate.ts — DO NOT EDIT.
// Source: specs/0.151.0/receiver_telemetryapi.json (+ patches/0.151.0/receiver_telemetryapi.json)

import * as Schema from "@distilled.cloud/core/schema";

export interface TelemetryApiReceiver {
  readonly exportIntervalMs?: number;
  readonly logReport?: boolean;
  /** Aggregation temporality for the metrics derived from platform events. */
  readonly metricsTemporality?: "delta" | "cumulative";
  readonly port?: number;
  /** Lambda Telemetry API event streams to subscribe to. */
  readonly types?: ReadonlyArray<"platform" | "function" | "extension">;
}
export const TelemetryApiReceiver = /*@__PURE__*/ Schema.suspend(() =>
  Schema.Struct({
    exportIntervalMs: Schema.optional(Schema.Int),
    logReport: Schema.optional(Schema.Boolean),
    metricsTemporality: Schema.optional(
      Schema.Literals(["delta", "cumulative"]),
    ),
    port: Schema.optional(Schema.Int),
    types: Schema.optional(
      Schema.Array(Schema.Literals(["platform", "function", "extension"])),
    ),
  }).pipe(
    Schema.encodeKeys({
      exportIntervalMs: "export_interval_ms",
      logReport: "log_report",
      metricsTemporality: "metrics_temporality",
      port: "port",
      types: "types",
    }),
  ),
) as unknown as Schema.Codec<TelemetryApiReceiver>;

/** The component's wire type, as written in `receivers:` of the config file. */
export const type = "telemetryapi" as const;

/** The config section this component is declared in. */
export const section = "receivers" as const;
