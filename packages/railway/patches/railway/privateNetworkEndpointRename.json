{
  "description": "privateNetworkEndpointRename returns Boolean! so the baked document must not select subfields.",
  "patches": [
    {
      "op": "replace",
      "path": "/shapes/com.railway.api#PrivateNetworkEndpointRenameRequest/traits/com.railway.graphql#operation/query",
      "value": "mutation privateNetworkEndpointRename($dnsName: String!, $id: String!, $privateNetworkId: String!) {\n  privateNetworkEndpointRename(dnsName: $dnsName, id: $id, privateNetworkId: $privateNetworkId)\n}"
    }
  ]
}
