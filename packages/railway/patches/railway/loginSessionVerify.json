{
  "description": "loginSessionVerify returns Boolean! so the baked document must not select subfields.",
  "patches": [
    {
      "op": "replace",
      "path": "/shapes/com.railway.api#LoginSessionVerifyRequest/traits/com.railway.graphql#operation/query",
      "value": "mutation loginSessionVerify($code: String!) {\n  loginSessionVerify(code: $code)\n}"
    }
  ]
}
