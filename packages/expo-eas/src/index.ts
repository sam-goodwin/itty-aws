/**
 * @distilled.cloud/expo-eas — Expo Application Services (EAS) SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy model in
 * `.generated-specs` (converted from the EAS GraphQL introspection schema by
 * `scripts/convert.ts`). Everything else in this folder is hand-written.
 *
 * @example
 * ```ts
 * import * as Eas from "@distilled.cloud/expo-eas";
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  ExpoGraphqlProtocol,
  type ExpoEasOpError,
  type ExpoEasOpContext,
} from "./protocol.ts";
export * as Retry from "./retry.ts";
export * as Services from "./services/index.ts";
