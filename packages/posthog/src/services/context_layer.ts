// AUTO-GENERATED by scripts/generate.ts from .generated-specs (see scripts/convert.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  PosthogProtocol,
  type PosthogOpError,
  type PosthogOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { PosthogOpError, PosthogOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface ContextLayerChannelPagesRetrieveRequest {
  /** ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/. */
  organization_id: string;
  channel_id: string;
}
export const ContextLayerChannelPagesRetrieveRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      organization_id: S.String.pipe(T.Label()),
      channel_id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/organizations/{organization_id}/context_layer/channel-pages/{channel_id}/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ContextLayerChannelPagesRetrieveRequest",
}) as any as S.Schema<ContextLayerChannelPagesRetrieveRequest>;

/** Response shape for a channel's page identity in the wiki. */
export interface ChannelWikiPage {
  /** Repo-relative path of the wiki page whose frontmatter names the channel. */
  path: string;
  /** Whether a page exists at this path. False when the path is a proposal for a channel whose page has not been created yet. */
  exists?: boolean;
}
export const ChannelWikiPage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    exists: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "ChannelWikiPage",
}) as any as S.Schema<ChannelWikiPage>;

export interface ContextLayerEnableCreateRequest {
  /** ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/. */
  organization_id: string;
}
export const ContextLayerEnableCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    organization_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/organizations/{organization_id}/context_layer/enable/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ContextLayerEnableCreateRequest",
}) as any as S.Schema<ContextLayerEnableCreateRequest>;

/** Response shape for the wiki's current state. */
export interface ContextLayerStatus {
  /** Commit sha of the wiki's current head. */
  head_sha: string;
}
export const ContextLayerStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    head_sha: S.String,
  }),
).annotate({
  identifier: "ContextLayerStatus",
}) as any as S.Schema<ContextLayerStatus>;

export interface ContextLayerExportRetrieveRequest {
  /** ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/. */
  organization_id: string;
}
export const ContextLayerExportRetrieveRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    organization_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/organizations/{organization_id}/context_layer/export/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ContextLayerExportRetrieveRequest",
}) as any as S.Schema<ContextLayerExportRetrieveRequest>;

/** Response shape for a wiki bundle export. */
export interface WikiExport {
  /** Short-lived download URL for the wiki's current bundle. */
  url: string;
  /** Commit sha of the bundle behind the URL. */
  head_sha: string;
}
export const WikiExport = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
    head_sha: S.String,
  }),
).annotate({ identifier: "WikiExport" }) as any as S.Schema<WikiExport>;

export interface ContextLayerPagesRetrieveRequest {
  /** ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/. */
  organization_id: string;
  /** Repo-relative Markdown path of the page to read. */
  path: string;
}
export const ContextLayerPagesRetrieveRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    organization_id: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/organizations/{organization_id}/context_layer/pages/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ContextLayerPagesRetrieveRequest",
}) as any as S.Schema<ContextLayerPagesRetrieveRequest>;

/** Response shape for one wiki page. */
export interface WikiPage {
  /** Repo-relative path of the page, for example `areas/analytics.md`. */
  path: string;
  /** The page's Markdown content. */
  content: string;
  /** Commit sha the content was read at; pass back as `base_head` on writes. */
  head_sha: string;
  /** When this page was last changed in the wiki history. */
  updated_at: string;
}
export const WikiPage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    content: S.String,
    head_sha: S.String,
    updated_at: S.String,
  }),
).annotate({ identifier: "WikiPage" }) as any as S.Schema<WikiPage>;

export interface ContextLayerStatusRetrieveRequest {
  /** ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/. */
  organization_id: string;
}
export const ContextLayerStatusRetrieveRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    organization_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/organizations/{organization_id}/context_layer/status/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ContextLayerStatusRetrieveRequest",
}) as any as S.Schema<ContextLayerStatusRetrieveRequest>;

export interface ContextLayerTreeRetrieveRequest {
  /** ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/. */
  organization_id: string;
}
export const ContextLayerTreeRetrieveRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    organization_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/organizations/{organization_id}/context_layer/tree/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ContextLayerTreeRetrieveRequest",
}) as any as S.Schema<ContextLayerTreeRetrieveRequest>;

/** Repo-relative path of every Markdown page at the current head. */
export type WikiTreePathsList = Array<string>;
export const WikiTreePathsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<WikiTreePathsList>;

/** Response shape for the wiki's page listing. */
export interface WikiTree {
  /** Commit sha of the wiki's current head. */
  head_sha: string;
  /** Repo-relative path of every Markdown page at the current head. */
  paths: WikiTreePathsList;
}
export const WikiTree = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    head_sha: S.String,
    paths: WikiTreePathsList,
  }),
).annotate({ identifier: "WikiTree" }) as any as S.Schema<WikiTree>;

export interface ContextLayerWikiReportRetrieveRequest {
  /** ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/. */
  organization_id: string;
}
export const ContextLayerWikiReportRetrieveRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      organization_id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/organizations/{organization_id}/context_layer/wiki/report/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ContextLayerWikiReportRetrieveRequest",
}) as any as S.Schema<ContextLayerWikiReportRetrieveRequest>;

export interface WikiHealthFinding {
  /** Stable category used to group this finding. */
  category: string;
  /** Wiki page path associated with this finding. */
  path: string;
  /** Human-readable explanation of the finding. */
  message: string;
}
export const WikiHealthFinding = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    category: S.String,
    path: S.String,
    message: S.String,
  }),
).annotate({
  identifier: "WikiHealthFinding",
}) as any as S.Schema<WikiHealthFinding>;

/** Health findings for the current wiki head. */
export type WikiHealthReportFindingsList = Array<WikiHealthFinding>;
export const WikiHealthReportFindingsList = /*@__PURE__*/ S.Array(
  WikiHealthFinding,
) as any as S.Schema<WikiHealthReportFindingsList>;

export interface WikiHealthReport {
  /** Commit sha inspected by the report. */
  head_sha: string;
  /** Health findings for the current wiki head. */
  findings: WikiHealthReportFindingsList;
}
export const WikiHealthReport = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    head_sha: S.String,
    findings: WikiHealthReportFindingsList,
  }),
).annotate({
  identifier: "WikiHealthReport",
}) as any as S.Schema<WikiHealthReport>;

export interface CreateContextLayerCommitRequest {
  /** ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/. */
  organization_id: string;
  /** A `git bundle` carrying the ref to land, created in the agent's clone (for example `git bundle create out.bundle origin/main..main`). */
  bundle: string;
  /** Optional run summary stored in the landed commit body. */
  summary?: string;
  /** Land a dated dreaming branch (`dream/<YYYY-MM-DD>`) as one merge commit instead of rebasing onto `main`. Omit for ordinary commits on `main`. */
  branch?: string | null;
}
export const CreateContextLayerCommitRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    organization_id: S.String.pipe(T.Label()),
    bundle: S.String,
    summary: S.optional(S.String),
    branch: S.optional(S.NullOr(S.String)),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/organizations/{organization_id}/context_layer/commits/",
      code: 200,
      contentType: "form-urlencoded",
    }),
  ),
).annotate({
  identifier: "CreateContextLayerCommitRequest",
}) as any as S.Schema<CreateContextLayerCommitRequest>;

export interface UpdateContextLayerPageRequest {
  /** ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/. */
  organization_id: string;
  /** Repo-relative Markdown path inside the wiki's structure, for example `projects/12/spaces/general.md`. */
  path: string;
  /** The complete Markdown content for the page. */
  content: string;
  /** Optimistic-concurrency guard: the head sha the edit is based on. A moved head is rejected with 409 and the current head; omit to write unguarded. */
  base_head?: string | null;
}
export const UpdateContextLayerPageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    organization_id: S.String.pipe(T.Label()),
    path: S.String,
    content: S.String,
    base_head: S.optional(S.NullOr(S.String)),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/api/organizations/{organization_id}/context_layer/pages/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateContextLayerPageRequest",
}) as any as S.Schema<UpdateContextLayerPageRequest>;

export type ContextLayerChannelPagesRetrieveError = NotFound | PosthogOpError;
/** Resolve a channel's wiki page The organization's context wiki: a git repo of Markdown pages hosted by PostHog. */
export const contextLayerChannelPagesRetrieve: API.OperationMethod<
  ContextLayerChannelPagesRetrieveRequest,
  ChannelWikiPage,
  ContextLayerChannelPagesRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContextLayerChannelPagesRetrieveRequest,
  output: ChannelWikiPage,
  errors: [NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type ContextLayerEnableCreateError = PosthogOpError;
/** Enable the context layer Create the organization's wiki with the default structure and import existing channel CONTEXT.md documents once. Idempotent. */
export const contextLayerEnableCreate: API.OperationMethod<
  ContextLayerEnableCreateRequest,
  ContextLayerStatus,
  ContextLayerEnableCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContextLayerEnableCreateRequest,
  output: ContextLayerStatus,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type ContextLayerExportRetrieveError = PosthogOpError;
/** Export the wiki A short-lived download URL for the wiki's current bundle: the whole repo and its history, one file, standard git. */
export const contextLayerExportRetrieve: API.OperationMethod<
  ContextLayerExportRetrieveRequest,
  WikiExport,
  ContextLayerExportRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContextLayerExportRetrieveRequest,
  output: WikiExport,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type ContextLayerPagesRetrieveError = NotFound | PosthogOpError;
/** Read a wiki page The organization's context wiki: a git repo of Markdown pages hosted by PostHog. */
export const contextLayerPagesRetrieve: API.OperationMethod<
  ContextLayerPagesRetrieveRequest,
  WikiPage,
  ContextLayerPagesRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContextLayerPagesRetrieveRequest,
  output: WikiPage,
  errors: [NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type ContextLayerStatusRetrieveError = NotFound | PosthogOpError;
/** Get the wiki head The organization's context wiki: a git repo of Markdown pages hosted by PostHog. */
export const contextLayerStatusRetrieve: API.OperationMethod<
  ContextLayerStatusRetrieveRequest,
  ContextLayerStatus,
  ContextLayerStatusRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContextLayerStatusRetrieveRequest,
  output: ContextLayerStatus,
  errors: [NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type ContextLayerTreeRetrieveError = NotFound | PosthogOpError;
/** List wiki pages The organization's context wiki: a git repo of Markdown pages hosted by PostHog. */
export const contextLayerTreeRetrieve: API.OperationMethod<
  ContextLayerTreeRetrieveRequest,
  WikiTree,
  ContextLayerTreeRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContextLayerTreeRetrieveRequest,
  output: WikiTree,
  errors: [NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type ContextLayerWikiReportRetrieveError = PosthogOpError;
/** Report wiki health findings The organization's context wiki: a git repo of Markdown pages hosted by PostHog. */
export const contextLayerWikiReportRetrieve: API.OperationMethod<
  ContextLayerWikiReportRetrieveRequest,
  WikiHealthReport,
  ContextLayerWikiReportRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ContextLayerWikiReportRetrieveRequest,
  output: WikiHealthReport,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type CreateContextLayerCommitError =
  | BadRequest
  | Conflict
  | PosthogOpError;
/** Land agent commits from a git bundle The organization's context wiki: a git repo of Markdown pages hosted by PostHog. */
export const createContextLayerCommit: API.OperationMethod<
  CreateContextLayerCommitRequest,
  ContextLayerStatus,
  CreateContextLayerCommitError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateContextLayerCommitRequest,
  output: ContextLayerStatus,
  errors: [BadRequest, Conflict],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type UpdateContextLayerPageError =
  | BadRequest
  | Forbidden
  | Conflict
  | PosthogOpError;
/** Create or replace a wiki page The organization's context wiki: a git repo of Markdown pages hosted by PostHog. */
export const updateContextLayerPage: API.OperationMethod<
  UpdateContextLayerPageRequest,
  ContextLayerStatus,
  UpdateContextLayerPageError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateContextLayerPageRequest,
  output: ContextLayerStatus,
  errors: [BadRequest, Forbidden, Conflict],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));
