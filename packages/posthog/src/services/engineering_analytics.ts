// AUTO-GENERATED by scripts/generate.ts from .generated-specs (see scripts/convert.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  PosthogProtocol,
  type PosthogOpError,
  type PosthogOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { PosthogOpError, PosthogOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface EngineeringAnalyticsCiCardsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one. */
  source_id?: string;
}
export const EngineeringAnalyticsCiCardsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    source_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/engineering_analytics/ci_cards/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "EngineeringAnalyticsCiCardsRequest",
}) as any as S.Schema<EngineeringAnalyticsCiCardsRequest>;

export interface CICardSummary {
  /** Count of open pull requests. */
  open_prs: number;
  /** Distinct repositories with at least one open pull request. */
  repos: number;
  /** Open, non-draft, non-bot pull requests older than 7 days. */
  stuck: number;
  /** Open pull requests with at least one failing latest CI run. May lag until the workflow_run webhook settles late completions. */
  failing_ci: number;
}
export const CICardSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    open_prs: S.Number,
    repos: S.Number,
    stuck: S.Number,
    failing_ci: S.Number,
  }),
).annotate({ identifier: "CICardSummary" }) as any as S.Schema<CICardSummary>;

export interface EngineeringAnalyticsPrCostRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Pull request number to estimate cost for. */
  pr_number: number;
  /** 'owner/name' repository the pull request belongs to. */
  repo: string;
  /** Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one. */
  source_id?: string;
}
export const EngineeringAnalyticsPrCostRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    pr_number: S.Number.pipe(T.Query()),
    repo: S.String.pipe(T.Query()),
    source_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/engineering_analytics/pr_cost/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "EngineeringAnalyticsPrCostRequest",
}) as any as S.Schema<EngineeringAnalyticsPrCostRequest>;

export interface WorkflowCost {
  /** GitHub Actions workflow name this cost is for. */
  workflow_name: string;
  /** Billable (self-hosted) minutes for this workflow within the scope. */
  billable_minutes: number;
  /** Estimated dollar cost for this workflow, or null when nothing was costable. */
  estimated_cost_usd: number | null;
  /** Costed jobs for this workflow (billable Linux runner, finished). */
  costed_jobs: number;
  /** Billable Linux jobs still queued/running for this workflow. */
  unsettled_jobs: number;
  /** Provider-hosted/non-Linux jobs for this workflow, outside the estimate. */
  excluded_jobs: number;
}
export const WorkflowCost = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    workflow_name: S.String,
    billable_minutes: S.Number,
    estimated_cost_usd: S.NullOr(S.Number),
    costed_jobs: S.Number,
    unsettled_jobs: S.Number,
    excluded_jobs: S.Number,
  }),
).annotate({ identifier: "WorkflowCost" }) as any as S.Schema<WorkflowCost>;

/** Same spend broken down per workflow. */
export type PRCostSummaryByWorkflowList = Array<WorkflowCost>;
export const PRCostSummaryByWorkflowList = /*@__PURE__*/ S.Array(
  WorkflowCost,
) as any as S.Schema<PRCostSummaryByWorkflowList>;

export interface RunCost {
  /** GitHub Actions run id this cost is for. */
  run_id: number;
  /** Re-run attempt number; 1 for the first attempt. */
  run_attempt: number;
  /** Billable (self-hosted) minutes for this run attempt. */
  billable_minutes: number;
  /** Estimated dollar cost for this run attempt, or null when nothing was costable. */
  estimated_cost_usd: number | null;
}
export const RunCost = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    run_id: S.Number,
    run_attempt: S.Number,
    billable_minutes: S.Number,
    estimated_cost_usd: S.NullOr(S.Number),
  }),
).annotate({ identifier: "RunCost" }) as any as S.Schema<RunCost>;

/** Same spend broken down per workflow run, keyed by (run_id, run_attempt). */
export type PRCostSummaryByRunList = Array<RunCost>;
export const PRCostSummaryByRunList = /*@__PURE__*/ S.Array(
  RunCost,
) as any as S.Schema<PRCostSummaryByRunList>;

export interface PRCostSummary {
  /** Same spend broken down per workflow. */
  by_workflow: PRCostSummaryByWorkflowList;
  /** Same spend broken down per workflow run, keyed by (run_id, run_attempt). */
  by_run: PRCostSummaryByRunList;
  /** False when the job-level source (github_workflow_jobs) isn't synced — every figure is then zero/null and the cost cards should be hidden. */
  jobs_available: boolean;
  /** Wall-clock minutes consumed on billable (self-hosted) runners, summed across costed jobs. */
  billable_minutes: number;
  /** Estimated dollar cost (sum of per-job estimates: elapsed x tier multiplier x reference rate). Null when no job was costable. */
  estimated_cost_usd: number | null;
  /** Jobs counted in the estimate (billable Linux runner, finished). */
  costed_jobs: number;
  /** Billable Linux jobs still queued/running (no elapsed) — excluded from the estimate. */
  unsettled_jobs: number;
  /** Jobs on provider-hosted (GitHub-hosted, free) or non-Linux runners — outside the estimate. */
  excluded_jobs: number;
}
export const PRCostSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    by_workflow: PRCostSummaryByWorkflowList,
    by_run: PRCostSummaryByRunList,
    jobs_available: S.Boolean,
    billable_minutes: S.Number,
    estimated_cost_usd: S.NullOr(S.Number),
    costed_jobs: S.Number,
    unsettled_jobs: S.Number,
    excluded_jobs: S.Number,
  }),
).annotate({ identifier: "PRCostSummary" }) as any as S.Schema<PRCostSummary>;

export interface EngineeringAnalyticsPrLifecycleRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Pull request number to inspect. */
  pr_number: number;
  /** 'owner/name' repository the pull request belongs to. */
  repo: string;
  /** Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one. */
  source_id?: string;
}
export const EngineeringAnalyticsPrLifecycleRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      pr_number: S.Number.pipe(T.Query()),
      repo: S.String.pipe(T.Query()),
      source_id: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/engineering_analytics/pr_lifecycle/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "EngineeringAnalyticsPrLifecycleRequest",
}) as any as S.Schema<EngineeringAnalyticsPrLifecycleRequest>;

export interface Author {
  /** Login handle of the pull request author. */
  handle: string;
  /** Human-readable name; equals the handle in v1. */
  display_name: string;
  /** URL of the author's avatar image. */
  avatar_url: string;
  /** True if the author is a bot (handle ends in [bot] or is a known bot). */
  is_bot: boolean;
}
export const Author = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    handle: S.String,
    display_name: S.String,
    avatar_url: S.String,
    is_bot: S.Boolean,
  }),
).annotate({ identifier: "Author" }) as any as S.Schema<Author>;

export interface RepoRef {
  /** Code host provider, e.g. 'github'. */
  provider: string;
  /** Repository owner or organization. */
  owner: string;
  /** Repository name. */
  name: string;
}
export const RepoRef = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    provider: S.String,
    owner: S.String,
    name: S.String,
  }),
).annotate({ identifier: "RepoRef" }) as any as S.Schema<RepoRef>;

/** * `open` - OPEN * `closed` - CLOSED * `merged` - MERGED */
export type EngineeringAnalyticsPRStateEnum = "open" | "closed" | "merged";
export const EngineeringAnalyticsPRStateEnum = /*@__PURE__*/ S.String;

export interface PullRequest {
  /** The pull request author. */
  author: Author;
  /** Repository the pull request belongs to. */
  repo: RepoRef;
  /** GitHub pull request id. */
  id: number;
  /** Pull request number within the repository. */
  number: number;
  /** Pull request title. */
  title: string;
  /** Derived state: 'open', 'closed', or 'merged'. * `open` - OPEN * `closed` - CLOSED * `merged` - MERGED */
  state: EngineeringAnalyticsPRStateEnum;
  /** True if the pull request is a draft. */
  is_draft: boolean;
  /** When the pull request was opened. */
  created_at: string;
  /** When the pull request was merged, or null. */
  merged_at: string | null;
  /** When the pull request was closed, or null. */
  closed_at: string | null;
}
export const PullRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    author: Author,
    repo: RepoRef,
    id: S.Number,
    number: S.Number,
    title: S.String,
    state: EngineeringAnalyticsPRStateEnum,
    is_draft: S.Boolean,
    created_at: S.String,
    merged_at: S.NullOr(S.String),
    closed_at: S.NullOr(S.String),
  }),
).annotate({ identifier: "PullRequest" }) as any as S.Schema<PullRequest>;

/** * `opened` - OPENED * `ci_started` - CI_STARTED * `ci_finished` - CI_FINISHED * `merged` - MERGED * `closed` - CLOSED */
export type PRLifecycleEventKindEnum =
  | "opened"
  | "ci_started"
  | "ci_finished"
  | "merged"
  | "closed";
export const PRLifecycleEventKindEnum = /*@__PURE__*/ S.String;

export interface PRLifecycleEvent {
  /** Event kind: opened, ci_started, ci_finished, merged, or closed. * `opened` - OPENED * `ci_started` - CI_STARTED * `ci_finished` - CI_FINISHED * `merged` - MERGED * `closed` - CLOSED */
  kind: PRLifecycleEventKindEnum;
  /** When the event occurred. */
  at: string;
  /** Optional detail, e.g. workflow name and conclusion for CI events. */
  detail?: string | null;
  /** GitHub Actions run id for ci_started/ci_finished events, null otherwise. */
  run_id?: number | null;
}
export const PRLifecycleEvent = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    kind: PRLifecycleEventKindEnum,
    at: S.String,
    detail: S.optional(S.NullOr(S.String)),
    run_id: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "PRLifecycleEvent",
}) as any as S.Schema<PRLifecycleEvent>;

/** Lifecycle events ordered by time. */
export type PRLifecycleEventsList = Array<PRLifecycleEvent>;
export const PRLifecycleEventsList = /*@__PURE__*/ S.Array(
  PRLifecycleEvent,
) as any as S.Schema<PRLifecycleEventsList>;

/** * `precise` - PRECISE * `coarse` - COARSE * `partial` - PARTIAL */
export type MetricQualityEnum = "precise" | "coarse" | "partial";
export const MetricQualityEnum = /*@__PURE__*/ S.String;

export interface PRLifecycle {
  /** The pull request header. */
  pull_request: PullRequest;
  /** Lifecycle events ordered by time. */
  events: PRLifecycleEventsList;
  /** Always 'partial' — CI events only; reviews and comments are not yet available. * `precise` - PRECISE * `coarse` - COARSE * `partial` - PARTIAL */
  metric_quality?: MetricQualityEnum;
}
export const PRLifecycle = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pull_request: PullRequest,
    events: PRLifecycleEventsList,
    metric_quality: S.optional(MetricQualityEnum),
  }),
).annotate({ identifier: "PRLifecycle" }) as any as S.Schema<PRLifecycle>;

export interface EngineeringAnalyticsPrRunsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Pull request number whose runs to list. */
  pr_number: number;
  /** 'owner/name' repository the pull request belongs to. */
  repo: string;
  /** Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one. */
  source_id?: string;
}
export const EngineeringAnalyticsPrRunsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    pr_number: S.Number.pipe(T.Query()),
    repo: S.String.pipe(T.Query()),
    source_id: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/engineering_analytics/pr_runs/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "EngineeringAnalyticsPrRunsRequest",
}) as any as S.Schema<EngineeringAnalyticsPrRunsRequest>;

export interface WorkflowRunDetail {
  /** Repository the run belongs to. */
  repo: RepoRef;
  /** GitHub Actions run id. */
  id: number;
  /** GitHub Actions workflow name. */
  workflow_name: string;
  /** Commit SHA the run was triggered on. */
  head_sha: string;
  /** Git branch the run was triggered on. */
  head_branch: string;
  /** Raw run status: 'queued', 'in_progress', 'completed', etc. */
  status: string;
  /** Run conclusion ('success', 'failure', 'timed_out', 'cancelled', 'skipped', 'action_required', ...), or null while still in progress. */
  conclusion: string | null;
  /** When the run started, or null for a queued/barely-started run. */
  run_started_at: string | null;
  /** When the run was last updated (its finish time once completed), or null when unstarted. */
  updated_at: string | null;
  /** Wall-clock duration in seconds; null until the run completes. */
  duration_seconds: number | null;
  /** Re-run attempt number; 1 for the first attempt. */
  run_attempt: number;
  /** Attributed pull request number, or 0 when unattributed. */
  pr_number: number;
}
export const WorkflowRunDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    repo: RepoRef,
    id: S.Number,
    workflow_name: S.String,
    head_sha: S.String,
    head_branch: S.String,
    status: S.String,
    conclusion: S.NullOr(S.String),
    run_started_at: S.NullOr(S.String),
    updated_at: S.NullOr(S.String),
    duration_seconds: S.NullOr(S.Number),
    run_attempt: S.Number,
    pr_number: S.Number,
  }),
).annotate({
  identifier: "WorkflowRunDetail",
}) as any as S.Schema<WorkflowRunDetail>;

export type EngineeringAnalyticsPrRunsResponseBodyList =
  Array<WorkflowRunDetail>;
export const EngineeringAnalyticsPrRunsResponseBodyList = /*@__PURE__*/ S.Array(
  WorkflowRunDetail,
) as any as S.Schema<EngineeringAnalyticsPrRunsResponseBodyList>;

export type EngineeringAnalyticsPrRunsResponse =
  EngineeringAnalyticsPrRunsResponseBodyList;
export const EngineeringAnalyticsPrRunsResponse = /*@__PURE__*/ S.suspend(() =>
  EngineeringAnalyticsPrRunsResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "EngineeringAnalyticsPrRunsResponse",
}) as any as S.Schema<EngineeringAnalyticsPrRunsResponse>;

export interface EngineeringAnalyticsPullRequestsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Optional GitHub login to scope the list to one author's pull requests. */
  author?: string;
  /** Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d. */
  date_from?: string;
  /** Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one. */
  source_id?: string;
}
export const EngineeringAnalyticsPullRequestsRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      author: S.optional(S.String.pipe(T.Query())),
      date_from: S.optional(S.String.pipe(T.Query())),
      source_id: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/engineering_analytics/pull_requests/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "EngineeringAnalyticsPullRequestsRequest",
}) as any as S.Schema<EngineeringAnalyticsPullRequestsRequest>;

export interface CIStatusRollup {
  /** Distinct workflows run on the PR's head SHA. */
  runs: number;
  /** Latest runs that completed with conclusion 'success'. */
  passing: number;
  /** Latest runs that completed with conclusion 'failure' or 'timed_out'. */
  failing: number;
  /** Latest runs not yet completed (queued or in progress). */
  pending: number;
}
export const CIStatusRollup = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    runs: S.Number,
    passing: S.Number,
    failing: S.Number,
    pending: S.Number,
  }),
).annotate({ identifier: "CIStatusRollup" }) as any as S.Schema<CIStatusRollup>;

/** GitHub label names on the pull request. */
export type PullRequestListItemLabelsList = Array<string>;
export const PullRequestListItemLabelsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<PullRequestListItemLabelsList>;

export interface PullRequestListItem {
  /** The pull request author. */
  author: Author;
  /** Repository the pull request belongs to. */
  repo: RepoRef;
  /** CI status from the latest workflow runs on the head SHA. */
  ci: CIStatusRollup;
  /** Pull request number within the repository. */
  number: number;
  /** Pull request title. */
  title: string;
  /** Derived state: 'open', 'closed', or 'merged'. * `open` - OPEN * `closed` - CLOSED * `merged` - MERGED */
  state: EngineeringAnalyticsPRStateEnum;
  /** True if the pull request is a draft. */
  is_draft: boolean;
  /** When the pull request was opened. */
  created_at: string;
  /** When the pull request was merged, or null. */
  merged_at: string | null;
  /** Coarse open-to-merge time in seconds (merged_at - created_at; fuses draft and ready-for-review time). Null until merged. */
  open_to_merge_seconds: number | null;
  /** GitHub label names on the pull request. */
  labels: PullRequestListItemLabelsList;
  /** CI triggers attributed to this PR: distinct head SHAs across its workflow runs. Fork-PR runs are unattributed. */
  pushes: number;
  /** Workflow runs attributed to this PR that were a 2nd+ attempt (a re-run). */
  rerun_cycles: number;
  /** Estimated CI cost in USD summed over this PR's jobs (billable runners only). Null when nothing was costable or the job-level source isn't synced. */
  estimated_cost_usd?: number | null;
  /** Billable (self-hosted) minutes summed over this PR's jobs. Null when the job source isn't synced. */
  billable_minutes?: number | null;
}
export const PullRequestListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    author: Author,
    repo: RepoRef,
    ci: CIStatusRollup,
    number: S.Number,
    title: S.String,
    state: EngineeringAnalyticsPRStateEnum,
    is_draft: S.Boolean,
    created_at: S.String,
    merged_at: S.NullOr(S.String),
    open_to_merge_seconds: S.NullOr(S.Number),
    labels: PullRequestListItemLabelsList,
    pushes: S.Number,
    rerun_cycles: S.Number,
    estimated_cost_usd: S.optional(S.NullOr(S.Number)),
    billable_minutes: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "PullRequestListItem",
}) as any as S.Schema<PullRequestListItem>;

/** Pull requests, newest first, capped at `limit`. */
export type PullRequestListItemsList = Array<PullRequestListItem>;
export const PullRequestListItemsList = /*@__PURE__*/ S.Array(
  PullRequestListItem,
) as any as S.Schema<PullRequestListItemsList>;

export interface PullRequestList {
  /** Pull requests, newest first, capped at `limit`. */
  items: PullRequestListItemsList;
  /** True when more pull requests match than the cap; `items` is the newest `limit` rows and the aggregate counts in ci_cards can exceed it. */
  truncated: boolean;
  /** Maximum number of pull requests returned in `items`. */
  limit: number;
}
export const PullRequestList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    items: PullRequestListItemsList,
    truncated: S.Boolean,
    limit: S.Number,
  }),
).annotate({
  identifier: "PullRequestList",
}) as any as S.Schema<PullRequestList>;

export interface EngineeringAnalyticsQuarantineRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Optional 'owner/name' repository to read the quarantine file from. Defaults to the connected GitHub source's most active repo over the last 30 days. */
  repo?: string;
  /** Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one. */
  source_id?: string;
}
export const EngineeringAnalyticsQuarantineRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      repo: S.optional(S.String.pipe(T.Query())),
      source_id: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/engineering_analytics/quarantine/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "EngineeringAnalyticsQuarantineRequest",
}) as any as S.Schema<EngineeringAnalyticsQuarantineRequest>;

/** * `run` - RUN * `skip` - SKIP */
export type QuarantineModeEnum = "run" | "skip";
export const QuarantineModeEnum = /*@__PURE__*/ S.String;

/** * `active` - ACTIVE * `expiring_soon` - EXPIRING_SOON * `in_grace` - IN_GRACE * `overdue` - OVERDUE */
export type LifecycleEnum = "active" | "expiring_soon" | "in_grace" | "overdue";
export const LifecycleEnum = /*@__PURE__*/ S.String;

/** * `product` - PRODUCT * `file` - FILE * `directory` - DIRECTORY * `test` - TEST */
export type SelectorKindEnum = "product" | "file" | "directory" | "test";
export const SelectorKindEnum = /*@__PURE__*/ S.String;

export interface QuarantineEntry {
  /** Test selector: an exact test id, a file, a directory, a class prefix, or 'product:<dashed-name>'. */
  id: string;
  /** Test runner the selector targets, e.g. 'pytest' or 'jest'. */
  runner: string;
  /** Why the test was quarantined. */
  reason: string;
  /** GitHub team or user handle responsible for the fix. */
  owner: string;
  /** Tracking issue URL, or empty when none was filed. */
  issue: string;
  /** ISO date the entry was added. */
  added: string;
  /** ISO date the quarantine expires; past it the test blocks CI normally again. */
  expires: string;
  /** 'run' (the test still executes but cannot fail the suite) or 'skip' (not run at all). * `run` - RUN * `skip` - SKIP */
  mode: QuarantineModeEnum;
  /** Expiry classification: 'active' (>7 days left), 'expiring_soon' (0-7 days left), 'in_grace' (expired up to 7 days ago), 'overdue' (expired beyond the grace period). * `active` - ACTIVE * `expiring_soon` - EXPIRING_SOON * `in_grace` - IN_GRACE * `overdue` - OVERDUE */
  lifecycle: LifecycleEnum;
  /** Days until the entry expires; negative once past expiry. */
  days_until_expiry: number;
  /** What the selector covers: 'test' (contains '::'), 'file', 'directory', or 'product'. * `product` - PRODUCT * `file` - FILE * `directory` - DIRECTORY * `test` - TEST */
  selector_kind: SelectorKindEnum;
}
export const QuarantineEntry = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    runner: S.String,
    reason: S.String,
    owner: S.String,
    issue: S.String,
    added: S.String,
    expires: S.String,
    mode: QuarantineModeEnum,
    lifecycle: LifecycleEnum,
    days_until_expiry: S.Number,
    selector_kind: SelectorKindEnum,
  }),
).annotate({
  identifier: "QuarantineEntry",
}) as any as S.Schema<QuarantineEntry>;

/** Quarantined selectors, most urgent first (overdue, in_grace, expiring_soon, active), then by soonest expiry. */
export type QuarantineFileEntriesList = Array<QuarantineEntry>;
export const QuarantineFileEntriesList = /*@__PURE__*/ S.Array(
  QuarantineEntry,
) as any as S.Schema<QuarantineFileEntriesList>;

/** Contract violations (malformed JSON, bad entries) or fetch failures. Malformed entries are dropped; well-formed ones are kept. */
export type QuarantineFileParseErrorsList = Array<string>;
export const QuarantineFileParseErrorsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<QuarantineFileParseErrorsList>;

/** Forward-compatibility notices, e.g. unknown entry fields. */
export type QuarantineFileParseWarningsList = Array<string>;
export const QuarantineFileParseWarningsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<QuarantineFileParseWarningsList>;

export interface QuarantineFile {
  /** Quarantined selectors, most urgent first (overdue, in_grace, expiring_soon, active), then by soonest expiry. */
  entries: QuarantineFileEntriesList;
  /** Repository the file was read from. Null in local-dev mode, where the server's own checkout is read. */
  repo: RepoRef | null;
  /** False when the repository has no quarantine file (not an error) or it could not be fetched. */
  available: boolean;
  /** Contract violations (malformed JSON, bad entries) or fetch failures. Malformed entries are dropped; well-formed ones are kept. */
  parse_errors: QuarantineFileParseErrorsList;
  /** Forward-compatibility notices, e.g. unknown entry fields. */
  parse_warnings: QuarantineFileParseWarningsList;
  /** GitHub blob URL of the quarantine file, or empty when read locally or unavailable. */
  source_url: string;
  /** When this snapshot was computed (UTC); expiry math uses this clock. */
  generated_at: string;
}
export const QuarantineFile = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    entries: QuarantineFileEntriesList,
    repo: S.NullOr(RepoRef),
    available: S.Boolean,
    parse_errors: QuarantineFileParseErrorsList,
    parse_warnings: QuarantineFileParseWarningsList,
    source_url: S.String,
    generated_at: S.String,
  }),
).annotate({ identifier: "QuarantineFile" }) as any as S.Schema<QuarantineFile>;

export interface EngineeringAnalyticsSourcesRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
}
export const EngineeringAnalyticsSourcesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/engineering_analytics/sources/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "EngineeringAnalyticsSourcesRequest",
}) as any as S.Schema<EngineeringAnalyticsSourcesRequest>;

export interface GitHubSource {
  /** Source id — pass as `source_id` to the other endpoints to read this source. */
  id: string;
  /** Connected repository as 'owner/name', or '' if unknown. */
  repo: string;
  /** User-chosen warehouse table-name prefix for this source, or '' when none. */
  prefix: string;
}
export const GitHubSource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    repo: S.String,
    prefix: S.String,
  }),
).annotate({ identifier: "GitHubSource" }) as any as S.Schema<GitHubSource>;

export type EngineeringAnalyticsSourcesResponseBodyList = Array<GitHubSource>;
export const EngineeringAnalyticsSourcesResponseBodyList =
  /*@__PURE__*/ S.Array(
    GitHubSource,
  ) as any as S.Schema<EngineeringAnalyticsSourcesResponseBodyList>;

export type EngineeringAnalyticsSourcesResponse =
  EngineeringAnalyticsSourcesResponseBodyList;
export const EngineeringAnalyticsSourcesResponse = /*@__PURE__*/ S.suspend(() =>
  EngineeringAnalyticsSourcesResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "EngineeringAnalyticsSourcesResponse",
}) as any as S.Schema<EngineeringAnalyticsSourcesResponse>;

export interface EngineeringAnalyticsWorkflowHealthRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Optional exact git branch (head_branch) to scope workflow health to, e.g. 'main'. Omit or leave blank to aggregate across all branches. */
  branch?: string;
  /** Window start: relative ('-24h', '-7d') or ISO8601. Defaults to -24h. */
  date_from?: string;
  /** Window end: relative or ISO8601. Defaults to now. */
  date_to?: string;
  /** Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one. */
  source_id?: string;
}
export const EngineeringAnalyticsWorkflowHealthRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      branch: S.optional(S.String.pipe(T.Query())),
      date_from: S.optional(S.String.pipe(T.Query())),
      date_to: S.optional(S.String.pipe(T.Query())),
      source_id: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/engineering_analytics/workflow_health/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "EngineeringAnalyticsWorkflowHealthRequest",
  }) as any as S.Schema<EngineeringAnalyticsWorkflowHealthRequest>;

export interface WorkflowHealthBucket {
  /** Bucket start, aligned to the item's granularity (top of hour, midnight, or Monday). */
  bucket_start: string;
  /** Runs started in this bucket. */
  run_count: number;
  /** Runs that completed in this bucket. */
  completed: number;
  /** Completed runs with conclusion 'success' in this bucket. */
  successes: number;
  /** Completed runs that failed in this bucket (conclusion 'failure' or 'timed_out'); excludes skipped, cancelled, and action_required runs. */
  failures: number;
}
export const WorkflowHealthBucket = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bucket_start: S.String,
    run_count: S.Number,
    completed: S.Number,
    successes: S.Number,
    failures: S.Number,
  }),
).annotate({
  identifier: "WorkflowHealthBucket",
}) as any as S.Schema<WorkflowHealthBucket>;

/** Run history across the whole window, oldest first, zero-filled, bucketed by granularity. */
export type WorkflowHealthItemBucketsList = Array<WorkflowHealthBucket>;
export const WorkflowHealthItemBucketsList = /*@__PURE__*/ S.Array(
  WorkflowHealthBucket,
) as any as S.Schema<WorkflowHealthItemBucketsList>;

export interface WorkflowHealthItem {
  /** Repository the workflow runs in. */
  repo: RepoRef;
  /** Run history across the whole window, oldest first, zero-filled, bucketed by granularity. */
  buckets: WorkflowHealthItemBucketsList;
  /** GitHub Actions workflow name. */
  workflow_name: string;
  /** Total runs started in the window. */
  run_count: number;
  /** Fraction of completed runs that succeeded (0-1). Null if no completed runs. */
  success_rate: number | null;
  /** Median duration of completed runs, in seconds. Null if none completed. */
  p50_seconds: number | null;
  /** 95th-percentile duration of completed runs, in seconds. Null if none completed. */
  p95_seconds: number | null;
  /** When the most recent failing run (conclusion 'failure' or 'timed_out') started, or null. */
  last_failure_at: string | null;
  /** Whether the most recent completed run was a decisive failure (conclusion 'failure' or 'timed_out'). Null when no run has completed in the window. Powers the OK/RED status badge. */
  latest_run_failed: boolean | null;
  /** Raw conclusion of the most recent completed run ('success', 'cancelled', 'skipped', ...), so a real pass can be told from a non-failure non-success. Null when none completed. */
  latest_run_conclusion: string | null;
  /** Bucket width of the `buckets` series, chosen to fit the window: 'hour', 'day', or 'week'. */
  granularity: string;
  /** Billable (self-hosted) minutes over this workflow's jobs in the window. Null when the job-level source isn't synced. */
  billable_minutes?: number | null;
  /** Estimated cost in USD over this workflow's jobs in the window. Null when nothing was costable or the job source isn't synced. */
  estimated_cost_usd?: number | null;
}
export const WorkflowHealthItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    repo: RepoRef,
    buckets: WorkflowHealthItemBucketsList,
    workflow_name: S.String,
    run_count: S.Number,
    success_rate: S.NullOr(S.Number),
    p50_seconds: S.NullOr(S.Number),
    p95_seconds: S.NullOr(S.Number),
    last_failure_at: S.NullOr(S.String),
    latest_run_failed: S.NullOr(S.Boolean),
    latest_run_conclusion: S.NullOr(S.String),
    granularity: S.String,
    billable_minutes: S.optional(S.NullOr(S.Number)),
    estimated_cost_usd: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "WorkflowHealthItem",
}) as any as S.Schema<WorkflowHealthItem>;

export type EngineeringAnalyticsWorkflowHealthResponseBodyList =
  Array<WorkflowHealthItem>;
export const EngineeringAnalyticsWorkflowHealthResponseBodyList =
  /*@__PURE__*/ S.Array(
    WorkflowHealthItem,
  ) as any as S.Schema<EngineeringAnalyticsWorkflowHealthResponseBodyList>;

export type EngineeringAnalyticsWorkflowHealthResponse =
  EngineeringAnalyticsWorkflowHealthResponseBodyList;
export const EngineeringAnalyticsWorkflowHealthResponse =
  /*@__PURE__*/ S.suspend(() =>
    EngineeringAnalyticsWorkflowHealthResponseBodyList.pipe(
      T.RawResponseRoot(),
    ),
  ).annotate({
    identifier: "EngineeringAnalyticsWorkflowHealthResponse",
  }) as any as S.Schema<EngineeringAnalyticsWorkflowHealthResponse>;

export interface EngineeringAnalyticsWorkflowJobsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Which re-run attempt to scope jobs to. Omit to use the run's latest attempt; pass an explicit attempt to avoid mixing jobs across a re-run's attempts. */
  run_attempt?: number;
  /** Workflow run id to list jobs for. */
  run_id: number;
  /** Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one. */
  source_id?: string;
}
export const EngineeringAnalyticsWorkflowJobsRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      run_attempt: S.optional(S.Number.pipe(T.Query())),
      run_id: S.Number.pipe(T.Query()),
      source_id: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/engineering_analytics/workflow_jobs/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "EngineeringAnalyticsWorkflowJobsRequest",
}) as any as S.Schema<EngineeringAnalyticsWorkflowJobsRequest>;

export interface WorkflowJob {
  /** GitHub Actions job id. */
  id: number;
  /** The workflow run id this job belongs to. */
  run_id: number;
  /** Job name. */
  name: string;
  /** Raw job status: 'queued', 'in_progress', 'completed', etc. */
  status: string;
  /** Job conclusion ('success', 'failure', 'cancelled', 'skipped', ...), or null while running. */
  conclusion: string | null;
  /** When the job started, or null while still queued. */
  started_at: string | null;
  /** When the job completed, or null while still running. */
  completed_at: string | null;
  /** Wall-clock duration in seconds; null until the job completes. */
  duration_seconds: number | null;
  /** Where the job ran: 'github_hosted' (free for open source), 'self_hosted' (billable), or 'unknown'. */
  runner_provider: string;
  /** Runner tier the job ran on (e.g. '16-core' or 'ubuntu-latest'), or '' when unknown. */
  runner_label: string;
  /** Estimated cost in USD from runner tier + elapsed time; null when the tier is unknown or the job hasn't finished. */
  estimated_cost_usd: number | null;
}
export const WorkflowJob = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.Number,
    run_id: S.Number,
    name: S.String,
    status: S.String,
    conclusion: S.NullOr(S.String),
    started_at: S.NullOr(S.String),
    completed_at: S.NullOr(S.String),
    duration_seconds: S.NullOr(S.Number),
    runner_provider: S.String,
    runner_label: S.String,
    estimated_cost_usd: S.NullOr(S.Number),
  }),
).annotate({ identifier: "WorkflowJob" }) as any as S.Schema<WorkflowJob>;

export type EngineeringAnalyticsWorkflowJobsResponseBodyList =
  Array<WorkflowJob>;
export const EngineeringAnalyticsWorkflowJobsResponseBodyList =
  /*@__PURE__*/ S.Array(
    WorkflowJob,
  ) as any as S.Schema<EngineeringAnalyticsWorkflowJobsResponseBodyList>;

export type EngineeringAnalyticsWorkflowJobsResponse =
  EngineeringAnalyticsWorkflowJobsResponseBodyList;
export const EngineeringAnalyticsWorkflowJobsResponse = /*@__PURE__*/ S.suspend(
  () =>
    EngineeringAnalyticsWorkflowJobsResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "EngineeringAnalyticsWorkflowJobsResponse",
}) as any as S.Schema<EngineeringAnalyticsWorkflowJobsResponse>;

export interface EngineeringAnalyticsWorkflowRunRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** GitHub Actions run id to inspect. */
  run_id: number;
  /** Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one. */
  source_id?: string;
}
export const EngineeringAnalyticsWorkflowRunRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      run_id: S.Number.pipe(T.Query()),
      source_id: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/engineering_analytics/workflow_run/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "EngineeringAnalyticsWorkflowRunRequest",
}) as any as S.Schema<EngineeringAnalyticsWorkflowRunRequest>;

export interface EngineeringAnalyticsWorkflowRunnerCostsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d. */
  date_from?: string;
  /** Window end: relative or ISO8601. Defaults to now. */
  date_to?: string;
  /** 'owner/name' repository the workflow belongs to. */
  repo: string;
  /** Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one. */
  source_id?: string;
  /** Workflow name to break down cost for. */
  workflow_name: string;
}
export const EngineeringAnalyticsWorkflowRunnerCostsRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      date_from: S.optional(S.String.pipe(T.Query())),
      date_to: S.optional(S.String.pipe(T.Query())),
      repo: S.String.pipe(T.Query()),
      source_id: S.optional(S.String.pipe(T.Query())),
      workflow_name: S.String.pipe(T.Query()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/engineering_analytics/workflow_runner_costs/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "EngineeringAnalyticsWorkflowRunnerCostsRequest",
  }) as any as S.Schema<EngineeringAnalyticsWorkflowRunnerCostsRequest>;

export interface WorkflowRunnerCost {
  /** 'self_hosted' (billable), 'github_hosted' (free), or 'unknown'. */
  provider: string;
  /** Runner tier, e.g. '16-core' or 'ubuntu-latest'. */
  runner_label: string;
  /** Jobs that ran on this tier for the workflow. */
  job_count: number;
  /** Billable minutes on this tier. */
  billable_minutes: number;
  /** Estimated cost in USD on this tier; null for non-billable (github-hosted/non-Linux). */
  estimated_cost_usd: number | null;
}
export const WorkflowRunnerCost = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    provider: S.String,
    runner_label: S.String,
    job_count: S.Number,
    billable_minutes: S.Number,
    estimated_cost_usd: S.NullOr(S.Number),
  }),
).annotate({
  identifier: "WorkflowRunnerCost",
}) as any as S.Schema<WorkflowRunnerCost>;

export type EngineeringAnalyticsWorkflowRunnerCostsResponseBodyList =
  Array<WorkflowRunnerCost>;
export const EngineeringAnalyticsWorkflowRunnerCostsResponseBodyList =
  /*@__PURE__*/ S.Array(
    WorkflowRunnerCost,
  ) as any as S.Schema<EngineeringAnalyticsWorkflowRunnerCostsResponseBodyList>;

export type EngineeringAnalyticsWorkflowRunnerCostsResponse =
  EngineeringAnalyticsWorkflowRunnerCostsResponseBodyList;
export const EngineeringAnalyticsWorkflowRunnerCostsResponse =
  /*@__PURE__*/ S.suspend(() =>
    EngineeringAnalyticsWorkflowRunnerCostsResponseBodyList.pipe(
      T.RawResponseRoot(),
    ),
  ).annotate({
    identifier: "EngineeringAnalyticsWorkflowRunnerCostsResponse",
  }) as any as S.Schema<EngineeringAnalyticsWorkflowRunnerCostsResponse>;

export interface EngineeringAnalyticsWorkflowRunsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d. */
  date_from?: string;
  /** Window end: relative or ISO8601. Defaults to now. */
  date_to?: string;
  /** 'owner/name' repository the workflow belongs to. */
  repo: string;
  /** Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one. */
  source_id?: string;
  /** Workflow name to list runs for. */
  workflow_name: string;
}
export const EngineeringAnalyticsWorkflowRunsRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      date_from: S.optional(S.String.pipe(T.Query())),
      date_to: S.optional(S.String.pipe(T.Query())),
      repo: S.String.pipe(T.Query()),
      source_id: S.optional(S.String.pipe(T.Query())),
      workflow_name: S.String.pipe(T.Query()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/engineering_analytics/workflow_runs/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "EngineeringAnalyticsWorkflowRunsRequest",
}) as any as S.Schema<EngineeringAnalyticsWorkflowRunsRequest>;

export type EngineeringAnalyticsWorkflowRunsResponseBodyList =
  Array<WorkflowRunDetail>;
export const EngineeringAnalyticsWorkflowRunsResponseBodyList =
  /*@__PURE__*/ S.Array(
    WorkflowRunDetail,
  ) as any as S.Schema<EngineeringAnalyticsWorkflowRunsResponseBodyList>;

export type EngineeringAnalyticsWorkflowRunsResponse =
  EngineeringAnalyticsWorkflowRunsResponseBodyList;
export const EngineeringAnalyticsWorkflowRunsResponse = /*@__PURE__*/ S.suspend(
  () =>
    EngineeringAnalyticsWorkflowRunsResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "EngineeringAnalyticsWorkflowRunsResponse",
}) as any as S.Schema<EngineeringAnalyticsWorkflowRunsResponse>;

export type EngineeringAnalyticsCiCardsError = BadRequest | PosthogOpError;
/** Headline counts for the open-PR backlog: open PRs, distinct repos, stuck PRs (open, non-draft, non-bot, older than 7 days), and PRs with failing CI. The failing-CI count rests on the head-SHA join and can lag until late CI completions settle. */
export const engineeringAnalyticsCiCards: API.OperationMethod<
  EngineeringAnalyticsCiCardsRequest,
  CICardSummary,
  EngineeringAnalyticsCiCardsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsCiCardsRequest,
  output: CICardSummary,
  errors: [BadRequest],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type EngineeringAnalyticsPrCostError = BadRequest | PosthogOpError;
/** Estimated CI cost for a pull request, summed over the jobs of all its workflow runs. Billable self-hosted Linux runners only — provider-hosted (free GitHub-hosted) and non-Linux jobs are excluded. Every figure is zero/null with `jobs_available` false when the job-level source isn't synced yet. */
export const engineeringAnalyticsPrCost: API.OperationMethod<
  EngineeringAnalyticsPrCostRequest,
  PRCostSummary,
  EngineeringAnalyticsPrCostError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsPrCostRequest,
  output: PRCostSummary,
  errors: [BadRequest],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type EngineeringAnalyticsPrLifecycleError =
  | BadRequest
  | NotFound
  | PosthogOpError;
/** The timeline of a single pull request: header plus ordered events (opened, CI started/finished, merged or closed). Use this to answer 'where is this PR stuck and what happened to it'. This is a partial view: review and comment events are not yet available. */
export const engineeringAnalyticsPrLifecycle: API.OperationMethod<
  EngineeringAnalyticsPrLifecycleRequest,
  PRLifecycle,
  EngineeringAnalyticsPrLifecycleError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsPrLifecycleRequest,
  output: PRLifecycle,
  errors: [BadRequest, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type EngineeringAnalyticsPrRunsError = BadRequest | PosthogOpError;
/** Every workflow run attributed to a pull request, across all its commits (grouped by head SHA client-side), newest first. Run-level only. */
export const engineeringAnalyticsPrRuns: API.OperationMethod<
  EngineeringAnalyticsPrRunsRequest,
  EngineeringAnalyticsPrRunsResponse,
  EngineeringAnalyticsPrRunsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsPrRunsRequest,
  output: EngineeringAnalyticsPrRunsResponse,
  errors: [BadRequest],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type EngineeringAnalyticsPullRequestsError = BadRequest | PosthogOpError;
/** Open pull requests plus any merged or closed since date_from (default -30d), newest first, each with its head-SHA CI rollup. The list is capped; when more match, `truncated` is true and the ci_cards counts can exceed it. open_to_merge_seconds is coarse — it fuses draft and ready-for-review time; CI counts can lag until late completions settle. */
export const engineeringAnalyticsPullRequests: API.OperationMethod<
  EngineeringAnalyticsPullRequestsRequest,
  PullRequestList,
  EngineeringAnalyticsPullRequestsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsPullRequestsRequest,
  output: PullRequestList,
  errors: [BadRequest],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type EngineeringAnalyticsQuarantineError = BadRequest | PosthogOpError;
/** Flaky-test quarantine file The repository's checked-in .test_quarantine.json: flaky tests temporarily quarantined with a hard expiry, classified by urgency (overdue, in grace, expiring soon, active). `available` is false when the repo has no quarantine file — that is not an error. Parsing is fail-open: malformed entries are reported in parse_errors while well-formed ones are kept. */
export const engineeringAnalyticsQuarantine: API.OperationMethod<
  EngineeringAnalyticsQuarantineRequest,
  QuarantineFile,
  EngineeringAnalyticsQuarantineError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsQuarantineRequest,
  output: QuarantineFile,
  errors: [BadRequest],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type EngineeringAnalyticsSourcesError = PosthogOpError;
/** The team's connected GitHub data warehouse sources, oldest first. Populate a source picker from this and pass a chosen `id` back as `source_id` to the other endpoints. A team can connect GitHub more than once (e.g. one source per repository); this lists them all, including any whose tables aren't fully synced yet. */
export const engineeringAnalyticsSources: API.OperationMethod<
  EngineeringAnalyticsSourcesRequest,
  EngineeringAnalyticsSourcesResponse,
  EngineeringAnalyticsSourcesError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsSourcesRequest,
  output: EngineeringAnalyticsSourcesResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type EngineeringAnalyticsWorkflowHealthError =
  | BadRequest
  | PosthogOpError;
/** Per-workflow CI health over a window (default last 24 hours, maximum 366 days): run count, success rate, p50/p95 duration over completed runs, last failure time, latest-run status, and a zero-filled run history bucketed by hour/day/week to fit the window. Optionally scope to a single git branch via `branch`. Use this for 'is CI getting slower' and 'which workflow is the long pole'; compare two windows to get a trend. */
export const engineeringAnalyticsWorkflowHealth: API.OperationMethod<
  EngineeringAnalyticsWorkflowHealthRequest,
  EngineeringAnalyticsWorkflowHealthResponse,
  EngineeringAnalyticsWorkflowHealthError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsWorkflowHealthRequest,
  output: EngineeringAnalyticsWorkflowHealthResponse,
  errors: [BadRequest],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type EngineeringAnalyticsWorkflowJobsError = BadRequest | PosthogOpError;
/** Jobs of a single workflow run attempt, with per-job duration, runner tier, and estimated cost. Scoped to one run_attempt (the latest unless specified) so a re-run's attempts don't merge. Returns an empty list when the job-level source isn't synced yet. */
export const engineeringAnalyticsWorkflowJobs: API.OperationMethod<
  EngineeringAnalyticsWorkflowJobsRequest,
  EngineeringAnalyticsWorkflowJobsResponse,
  EngineeringAnalyticsWorkflowJobsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsWorkflowJobsRequest,
  output: EngineeringAnalyticsWorkflowJobsResponse,
  errors: [BadRequest],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type EngineeringAnalyticsWorkflowRunError =
  | BadRequest
  | NotFound
  | PosthogOpError;
/** A single workflow run: status, conclusion, duration, branch, attempt, and the attributed pull request. Run-level only — per-job and per-step detail are not tracked yet. */
export const engineeringAnalyticsWorkflowRun: API.OperationMethod<
  EngineeringAnalyticsWorkflowRunRequest,
  WorkflowRunDetail,
  EngineeringAnalyticsWorkflowRunError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsWorkflowRunRequest,
  output: WorkflowRunDetail,
  errors: [BadRequest, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type EngineeringAnalyticsWorkflowRunnerCostsError =
  | BadRequest
  | PosthogOpError;
/** A workflow's estimated CI cost broken down by runner tier over a window (date_from default -30d), highest spend first. Returns an empty list when the job-level source isn't synced. */
export const engineeringAnalyticsWorkflowRunnerCosts: API.OperationMethod<
  EngineeringAnalyticsWorkflowRunnerCostsRequest,
  EngineeringAnalyticsWorkflowRunnerCostsResponse,
  EngineeringAnalyticsWorkflowRunnerCostsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsWorkflowRunnerCostsRequest,
  output: EngineeringAnalyticsWorkflowRunnerCostsResponse,
  errors: [BadRequest],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type EngineeringAnalyticsWorkflowRunsError = BadRequest | PosthogOpError;
/** Runs of a single workflow within a repo over a window (date_from default -30d), newest first. Each row is run-level — per-job and per-step detail are not tracked yet. Use this as the GitHub 'workflow' page between the workflow list and a single run. */
export const engineeringAnalyticsWorkflowRuns: API.OperationMethod<
  EngineeringAnalyticsWorkflowRunsRequest,
  EngineeringAnalyticsWorkflowRunsResponse,
  EngineeringAnalyticsWorkflowRunsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EngineeringAnalyticsWorkflowRunsRequest,
  output: EngineeringAnalyticsWorkflowRunsResponse,
  errors: [BadRequest],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));
