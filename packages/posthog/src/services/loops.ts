// AUTO-GENERATED by scripts/generate.ts from .generated-specs (see scripts/convert.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  PosthogProtocol,
  type PosthogOpError,
  type PosthogOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { PosthogOpError, PosthogOpContext };

export class Forbidden extends T.applyErrorMatchers(
  S.TaggedError<Forbidden>()("Forbidden", {
    code: S.Number,
    message: S.String,
  }),
  [{ status: 403 }],
) {}

export class NotFound extends T.applyErrorMatchers(
  S.TaggedError<NotFound>()("NotFound", {
    code: S.Number,
    message: S.String,
  }),
  [{ status: 404 }],
) {}

/** * `personal` - personal * `team` - team */
export type VisibilityEnum = "personal" | "team";
export const VisibilityEnum = /*@__PURE__*/ S.String;

/** * `claude` - claude * `codex` - codex */
export type RuntimeAdapterEnum = "claude" | "codex";
export const RuntimeAdapterEnum = /*@__PURE__*/ S.String;

/** * `low` - low * `medium` - medium * `high` - high * `xhigh` - xhigh * `max` - max */
export type ReasoningEffortEnum = "low" | "medium" | "high" | "xhigh" | "max";
export const ReasoningEffortEnum = /*@__PURE__*/ S.String;

export interface LoopRepositoryEntry {
  /** GitHub integration id this repository is accessed through. */
  github_integration_id: number;
  /** Repository in `organization/repo` format, e.g. `posthog/posthog`. */
  full_name: string;
}
export const LoopRepositoryEntry = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    github_integration_id: S.Number,
    full_name: S.String,
  }),
).annotate({
  identifier: "LoopRepositoryEntry",
}) as any as S.Schema<LoopRepositoryEntry>;

/** Repositories this loop operates on, ordered. Capped at 1 until multi-repo execution ships. May be empty for report-only loops. */
export type LoopsCreateRequestRepositoriesList = Array<LoopRepositoryEntry>;
export const LoopsCreateRequestRepositoriesList = /*@__PURE__*/ S.Array(
  LoopRepositoryEntry,
) as any as S.Schema<LoopsCreateRequestRepositoriesList>;

/** * `skip` - skip * `allow` - allow * `cancel_previous` - cancel_previous */
export type OverlapPolicyEnum = "skip" | "allow" | "cancel_previous";
export const OverlapPolicyEnum = /*@__PURE__*/ S.String;

export interface LoopBehaviors {
  /** Whether the agent may push branches and open PRs. False makes this a report-only loop. */
  create_prs?: boolean;
  /** Whether to watch CI on loop-created PRs and report status. */
  watch_ci?: boolean;
  /** Whether to automatically address review comments on loop-created PRs. */
  fix_review_comments?: boolean;
  /** Ceiling on automatic CI/review-comment fix iterations, capped at 10. */
  max_fix_iterations?: number;
}
export const LoopBehaviors = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    create_prs: S.optional(S.Boolean),
    watch_ci: S.optional(S.Boolean),
    fix_review_comments: S.optional(S.Boolean),
    max_fix_iterations: S.optional(S.Number),
  }),
).annotate({ identifier: "LoopBehaviors" }) as any as S.Schema<LoopBehaviors>;

/** MCP Store installation ids (Slack, Linear, etc.) available to this loop's runs. */
export type LoopConnectorsMcpInstallationIdsList = Array<string>;
export const LoopConnectorsMcpInstallationIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<LoopConnectorsMcpInstallationIdsList>;

/** * `read_only` - read_only * `full` - full */
export type PosthogMcpScopesEnum = "read_only" | "full";
export const PosthogMcpScopesEnum = /*@__PURE__*/ S.String;

export interface LoopConnectors {
  /** MCP Store installation ids (Slack, Linear, etc.) available to this loop's runs. */
  mcp_installation_ids?: LoopConnectorsMcpInstallationIdsList;
  /** Scope of the PostHog MCP access injected into this loop's runs. * `read_only` - read_only * `full` - full */
  posthog_mcp_scopes?: PosthogMcpScopesEnum | (string & {});
}
export const LoopConnectors = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    mcp_installation_ids: S.optional(LoopConnectorsMcpInstallationIdsList),
    posthog_mcp_scopes: S.optional(PosthogMcpScopesEnum),
  }),
).annotate({ identifier: "LoopConnectors" }) as any as S.Schema<LoopConnectors>;

/** * `run_completed` - run_completed * `run_failed` - run_failed * `pr_created` - pr_created * `needs_attention` - needs_attention */
export type EventsEnum =
  | "run_completed"
  | "run_failed"
  | "pr_created"
  | "needs_attention";
export const EventsEnum = /*@__PURE__*/ S.String;

/** Event kinds this channel notifies on. One or more of: run_completed, run_failed, pr_created, needs_attention. */
export type LoopNotificationChannelEventsList = Array<
  EventsEnum | (string & {})
>;
export const LoopNotificationChannelEventsList = /*@__PURE__*/ S.Array(
  EventsEnum,
) as any as S.Schema<LoopNotificationChannelEventsList>;

/** Channel-specific parameters, e.g. Slack's `integration_id` and `channel`. */
export type LoopNotificationChannelParamsMap = {
  [key: string]: unknown | undefined;
};
export const LoopNotificationChannelParamsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<LoopNotificationChannelParamsMap>;

export interface LoopNotificationChannel {
  /** Whether this channel is active. */
  enabled?: boolean;
  /** Event kinds this channel notifies on. One or more of: run_completed, run_failed, pr_created, needs_attention. */
  events?: LoopNotificationChannelEventsList;
  /** Channel-specific parameters, e.g. Slack's `integration_id` and `channel`. */
  params?: LoopNotificationChannelParamsMap;
}
export const LoopNotificationChannel = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    enabled: S.optional(S.Boolean),
    events: S.optional(LoopNotificationChannelEventsList),
    params: S.optional(LoopNotificationChannelParamsMap),
  }),
).annotate({
  identifier: "LoopNotificationChannel",
}) as any as S.Schema<LoopNotificationChannel>;

export interface LoopNotifications {
  /** Push notification settings. */
  push?: LoopNotificationChannel;
  /** Email notification settings. */
  email?: LoopNotificationChannel;
  /** Slack notification settings. */
  slack?: LoopNotificationChannel;
}
export const LoopNotifications = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    push: S.optional(LoopNotificationChannel),
    email: S.optional(LoopNotificationChannel),
    slack: S.optional(LoopNotificationChannel),
  }),
).annotate({
  identifier: "LoopNotifications",
}) as any as S.Schema<LoopNotifications>;

export interface LoopContextOutputsWrite {
  /** Whether each run is filed into the context's feed as a card (sets the run's channel). */
  post_to_feed?: boolean;
  /** Whether each run reads and republishes the context's context.md to reflect the latest state. */
  update_context?: boolean;
  /** Id of a canvas in this context the loop keeps up to date each run, or null to maintain none. */
  canvas_id?: string | null;
}
export const LoopContextOutputsWrite = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    post_to_feed: S.optional(S.Boolean),
    update_context: S.optional(S.Boolean),
    canvas_id: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "LoopContextOutputsWrite",
}) as any as S.Schema<LoopContextOutputsWrite>;

export interface LoopContextTargetWrite {
  /** Desktop folder id of the context this loop is attached to. */
  folder_id: string;
  /** Context (channel) name, used to file runs into its feed. */
  name: string;
  /** What the loop maintains in this context each run. */
  outputs?: LoopContextOutputsWrite;
}
export const LoopContextTargetWrite = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    folder_id: S.String,
    name: S.String,
    outputs: S.optional(LoopContextOutputsWrite),
  }),
).annotate({
  identifier: "LoopContextTargetWrite",
}) as any as S.Schema<LoopContextTargetWrite>;

/** * `schedule` - schedule * `github` - github * `api` - api */
export type LoopTriggerTypeEnum = "schedule" | "github" | "api";
export const LoopTriggerTypeEnum = /*@__PURE__*/ S.String;

export interface LoopTriggerWrite {
  /** Existing trigger id to update in place. Omit to create a new trigger. */
  id?: string;
  /** Trigger type: `schedule` (cron or one-time), `github` (repo webhook events), or `api` (POST to `trigger/`). * `schedule` - schedule * `github` - github * `api` - api */
  type: LoopTriggerTypeEnum | (string & {});
  /** Whether this trigger is active. Disabling pauses only this trigger. */
  enabled?: boolean;
  /** Trigger configuration, shape validated per `type`: schedule takes `{cron_expression, timezone}` or `{run_at}` for a one-time run; github takes `{github_integration_id, repository, events, filters}` where `events` is one or more of `issues`, `issue_comment`, `pull_request`, `push` (`event.action` shorthand like `issues.opened` is folded into an `actions` filter, one event per trigger) and `filters` takes `{actions, branches, labels}`; api takes no config. */
  config?: unknown;
}
export const LoopTriggerWrite = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    type: LoopTriggerTypeEnum,
    enabled: S.optional(S.Boolean),
    config: S.optional(S.Unknown),
  }),
).annotate({
  identifier: "LoopTriggerWrite",
}) as any as S.Schema<LoopTriggerWrite>;

/** Full desired trigger list, id-stable: entries with a matching `id` are updated in place, entries without one are created, and existing triggers absent from this list are deleted. Omit the field entirely to leave triggers untouched. At most 25 triggers per loop. */
export type LoopsCreateRequestTriggersList = Array<LoopTriggerWrite>;
export const LoopsCreateRequestTriggersList = /*@__PURE__*/ S.Array(
  LoopTriggerWrite,
) as any as S.Schema<LoopsCreateRequestTriggersList>;

export interface LoopsCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Display name for the loop. */
  name: string;
  /** Free-form description of what this loop does. */
  description?: string;
  /** On a team loop, claim ownership as part of this update so you can edit identity-bearing config (instructions, model, triggers, ...) that only the owner may change. Ignored on personal loops and on create. */
  take_ownership?: boolean;
  /** `personal` (owner-only) or `team` (visible and fireable by any team member). * `personal` - personal * `team` - team */
  visibility?: VisibilityEnum | (string & {});
  /** The prompt delivered to the agent on every run. */
  instructions: string;
  /** Runtime adapter: 'claude' or 'codex'. * `claude` - claude * `codex` - codex */
  runtime_adapter: RuntimeAdapterEnum | (string & {});
  /** LLM model identifier, validated against `runtime_adapter`'s catalog. Leave blank to let PostHog pick a sensible default at run time. */
  model?: string;
  /** Reasoning effort, validated against `runtime_adapter`/`model`'s supported set. * `low` - low * `medium` - medium * `high` - high * `xhigh` - xhigh * `max` - max */
  reasoning_effort?: ReasoningEffortEnum | (string & {}) | null;
  /** Repositories this loop operates on, ordered. Capped at 1 until multi-repo execution ships. May be empty for report-only loops. */
  repositories?: LoopsCreateRequestRepositoriesList;
  /** Sandbox environment carrying encrypted env vars and the network allowlist into every run. */
  sandbox_environment?: string | null;
  /** Whether the loop's triggers are active. Pausing disables all triggers. */
  enabled?: boolean;
  /** What happens when a trigger fires while a run is already active: 'skip', 'allow', or 'cancel_previous'. * `skip` - skip * `allow` - allow * `cancel_previous` - cancel_previous */
  overlap_policy?: OverlapPolicyEnum | (string & {});
  /** PR / CI-follow-up behavior configuration. */
  behaviors?: LoopBehaviors;
  /** MCP connector configuration for this loop's runs. */
  connectors?: LoopConnectors;
  /** Per-channel notification configuration. */
  notifications?: LoopNotifications;
  /** Context (channel) this loop is attached to, or null to detach. Drives feed placement and the context.md / canvas it keeps up to date. */
  context_target?: LoopContextTargetWrite | null;
  /** Full desired trigger list, id-stable: entries with a matching `id` are updated in place, entries without one are created, and existing triggers absent from this list are deleted. Omit the field entirely to leave triggers untouched. At most 25 triggers per loop. */
  triggers?: LoopsCreateRequestTriggersList;
}
export const LoopsCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    name: S.String,
    description: S.optional(S.String),
    take_ownership: S.optional(S.Boolean),
    visibility: S.optional(VisibilityEnum),
    instructions: S.String,
    runtime_adapter: RuntimeAdapterEnum,
    model: S.optional(S.String),
    reasoning_effort: S.optional(S.NullOr(ReasoningEffortEnum)),
    repositories: S.optional(LoopsCreateRequestRepositoriesList),
    sandbox_environment: S.optional(S.NullOr(S.String)),
    enabled: S.optional(S.Boolean),
    overlap_policy: S.optional(OverlapPolicyEnum),
    behaviors: S.optional(LoopBehaviors),
    connectors: S.optional(LoopConnectors),
    notifications: S.optional(LoopNotifications),
    context_target: S.optional(S.NullOr(LoopContextTargetWrite)),
    triggers: S.optional(LoopsCreateRequestTriggersList),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/projects/{project_id}/loops/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "LoopsCreateRequest",
}) as any as S.Schema<LoopsCreateRequest>;

export interface LoopRepositoryEntryDTO {
  github_integration_id: number;
  full_name: string;
}
export const LoopRepositoryEntryDTO = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    github_integration_id: S.Number,
    full_name: S.String,
  }),
).annotate({
  identifier: "LoopRepositoryEntryDTO",
}) as any as S.Schema<LoopRepositoryEntryDTO>;

/** Repositories this loop operates on. */
export type LoopDTORepositoriesList = Array<LoopRepositoryEntryDTO>;
export const LoopDTORepositoriesList = /*@__PURE__*/ S.Array(
  LoopRepositoryEntryDTO,
) as any as S.Schema<LoopDTORepositoriesList>;

export interface LoopBehaviorsDTO {
  create_prs?: boolean;
  watch_ci?: boolean;
  fix_review_comments?: boolean;
  max_fix_iterations?: number;
}
export const LoopBehaviorsDTO = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    create_prs: S.optional(S.Boolean),
    watch_ci: S.optional(S.Boolean),
    fix_review_comments: S.optional(S.Boolean),
    max_fix_iterations: S.optional(S.Number),
  }),
).annotate({
  identifier: "LoopBehaviorsDTO",
}) as any as S.Schema<LoopBehaviorsDTO>;

export type LoopConnectorsDTOMcpInstallationIdsList = Array<string>;
export const LoopConnectorsDTOMcpInstallationIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<LoopConnectorsDTOMcpInstallationIdsList>;

export interface LoopConnectorsDTO {
  mcp_installation_ids?: LoopConnectorsDTOMcpInstallationIdsList;
  posthog_mcp_scopes?: string;
}
export const LoopConnectorsDTO = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    mcp_installation_ids: S.optional(LoopConnectorsDTOMcpInstallationIdsList),
    posthog_mcp_scopes: S.optional(S.String),
  }),
).annotate({
  identifier: "LoopConnectorsDTO",
}) as any as S.Schema<LoopConnectorsDTO>;

export type LoopNotificationChannelDTOEventsList = Array<string>;
export const LoopNotificationChannelDTOEventsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<LoopNotificationChannelDTOEventsList>;

export type LoopNotificationChannelDTOParamsMap = {
  [key: string]: unknown | undefined;
};
export const LoopNotificationChannelDTOParamsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<LoopNotificationChannelDTOParamsMap>;

export interface LoopNotificationChannelDTO {
  enabled?: boolean;
  events?: LoopNotificationChannelDTOEventsList;
  params?: LoopNotificationChannelDTOParamsMap;
}
export const LoopNotificationChannelDTO = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    enabled: S.optional(S.Boolean),
    events: S.optional(LoopNotificationChannelDTOEventsList),
    params: S.optional(LoopNotificationChannelDTOParamsMap),
  }),
).annotate({
  identifier: "LoopNotificationChannelDTO",
}) as any as S.Schema<LoopNotificationChannelDTO>;

export interface LoopNotificationsDTO {
  push: LoopNotificationChannelDTO;
  email: LoopNotificationChannelDTO;
  slack: LoopNotificationChannelDTO;
}
export const LoopNotificationsDTO = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    push: LoopNotificationChannelDTO,
    email: LoopNotificationChannelDTO,
    slack: LoopNotificationChannelDTO,
  }),
).annotate({
  identifier: "LoopNotificationsDTO",
}) as any as S.Schema<LoopNotificationsDTO>;

export interface LoopContextOutputsDTO {
  post_to_feed?: boolean;
  update_context?: boolean;
  canvas_id?: string | null;
}
export const LoopContextOutputsDTO = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    post_to_feed: S.optional(S.Boolean),
    update_context: S.optional(S.Boolean),
    canvas_id: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "LoopContextOutputsDTO",
}) as any as S.Schema<LoopContextOutputsDTO>;

export interface LoopContextTargetDTO {
  /** What the loop maintains in this context each run. */
  outputs: LoopContextOutputsDTO;
  folder_id: string;
  name: string;
}
export const LoopContextTargetDTO = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    outputs: LoopContextOutputsDTO,
    folder_id: S.String,
    name: S.String,
  }),
).annotate({
  identifier: "LoopContextTargetDTO",
}) as any as S.Schema<LoopContextTargetDTO>;

export type LoopTriggerDTOConfigMap = { [key: string]: unknown | undefined };
export const LoopTriggerDTOConfigMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<LoopTriggerDTOConfigMap>;

/** Read response for a single loop trigger. */
export interface LoopTriggerDTO {
  id: string;
  loop_id: string;
  type: string;
  enabled: boolean;
  config: LoopTriggerDTOConfigMap;
  schedule_sync_status: string | null;
  last_fired_at: string | null;
  created_at: string;
  updated_at: string;
}
export const LoopTriggerDTO = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    loop_id: S.String,
    type: S.String,
    enabled: S.Boolean,
    config: LoopTriggerDTOConfigMap,
    schedule_sync_status: S.NullOr(S.String),
    last_fired_at: S.NullOr(S.String),
    created_at: S.String,
    updated_at: S.String,
  }),
).annotate({ identifier: "LoopTriggerDTO" }) as any as S.Schema<LoopTriggerDTO>;

/** Triggers attached to this loop. */
export type LoopDTOTriggersList = Array<LoopTriggerDTO>;
export const LoopDTOTriggersList = /*@__PURE__*/ S.Array(
  LoopTriggerDTO,
) as any as S.Schema<LoopDTOTriggersList>;

/** Detail/create/update response for a loop, including its triggers. */
export interface LoopDTO {
  id: string;
  team_id: number;
  created_by_id: number | null;
  name: string;
  description: string;
  visibility: string;
  instructions: string;
  runtime_adapter: string;
  model: string;
  reasoning_effort: string | null;
  /** Repositories this loop operates on. */
  repositories: LoopDTORepositoriesList;
  sandbox_environment_id: string | null;
  enabled: boolean;
  disabled_reason: string | null;
  overlap_policy: string;
  /** PR / CI-follow-up behavior configuration. */
  behaviors: LoopBehaviorsDTO;
  /** MCP connector configuration for this loop's runs. */
  connectors: LoopConnectorsDTO;
  /** Per-channel notification configuration. */
  notifications: LoopNotificationsDTO;
  /** Context this loop is attached to, or null when unattached. */
  context_target?: LoopContextTargetDTO | null;
  internal: boolean;
  origin_product: string;
  last_run_at: string | null;
  last_run_status: string | null;
  last_error: string | null;
  consecutive_failures: number;
  created_at: string;
  updated_at: string;
  /** Triggers attached to this loop. */
  triggers: LoopDTOTriggersList;
}
export const LoopDTO = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    team_id: S.Number,
    created_by_id: S.NullOr(S.Number),
    name: S.String,
    description: S.String,
    visibility: S.String,
    instructions: S.String,
    runtime_adapter: S.String,
    model: S.String,
    reasoning_effort: S.NullOr(S.String),
    repositories: LoopDTORepositoriesList,
    sandbox_environment_id: S.NullOr(S.String),
    enabled: S.Boolean,
    disabled_reason: S.NullOr(S.String),
    overlap_policy: S.String,
    behaviors: LoopBehaviorsDTO,
    connectors: LoopConnectorsDTO,
    notifications: LoopNotificationsDTO,
    context_target: S.optional(S.NullOr(LoopContextTargetDTO)),
    internal: S.Boolean,
    origin_product: S.String,
    last_run_at: S.NullOr(S.String),
    last_run_status: S.NullOr(S.String),
    last_error: S.NullOr(S.String),
    consecutive_failures: S.Number,
    created_at: S.String,
    updated_at: S.String,
    triggers: LoopDTOTriggersList,
  }),
).annotate({ identifier: "LoopDTO" }) as any as S.Schema<LoopDTO>;

export interface LoopsDestroyRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  id: string;
}
export const LoopsDestroyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/api/projects/{project_id}/loops/{id}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "LoopsDestroyRequest",
}) as any as S.Schema<LoopsDestroyRequest>;

export interface LoopsDestroyResponse {}
export const LoopsDestroyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "LoopsDestroyResponse",
}) as any as S.Schema<LoopsDestroyResponse>;

export interface LoopsListRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Number of results to return per page. */
  limit?: number;
  /** The initial index from which to return the results. */
  offset?: number;
}
export const LoopsListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    limit: S.optional(S.Number.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/loops/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "LoopsListRequest",
}) as any as S.Schema<LoopsListRequest>;

export type PaginatedLoopDTOListResultsList = Array<LoopDTO>;
export const PaginatedLoopDTOListResultsList = /*@__PURE__*/ S.Array(
  LoopDTO,
) as any as S.Schema<PaginatedLoopDTOListResultsList>;

export interface PaginatedLoopDTOList {
  count: number;
  next?: string | null;
  previous?: string | null;
  results: PaginatedLoopDTOListResultsList;
  /** Hard cap on non-deleted loops per project. Creating a loop beyond this returns a 429 with `error: loop_safety_limit`. Authoritative — read this rather than assuming a value. */
  max_loops_per_team?: number;
  /** Current number of non-deleted, user-facing loops in this project, counted against `max_loops_per_team`. At or above the cap, creation is blocked. */
  total_loop_count?: number;
}
export const PaginatedLoopDTOList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    count: S.Number,
    next: S.optional(S.NullOr(S.String)),
    previous: S.optional(S.NullOr(S.String)),
    results: PaginatedLoopDTOListResultsList,
    max_loops_per_team: S.optional(S.Number),
    total_loop_count: S.optional(S.Number),
  }),
).annotate({
  identifier: "PaginatedLoopDTOList",
}) as any as S.Schema<PaginatedLoopDTOList>;

/** Repositories this loop operates on, ordered. Capped at 1 until multi-repo execution ships. May be empty for report-only loops. */
export type LoopsPartialUpdateRequestRepositoriesList =
  Array<LoopRepositoryEntry>;
export const LoopsPartialUpdateRequestRepositoriesList = /*@__PURE__*/ S.Array(
  LoopRepositoryEntry,
) as any as S.Schema<LoopsPartialUpdateRequestRepositoriesList>;

/** Full desired trigger list, id-stable: entries with a matching `id` are updated in place, entries without one are created, and existing triggers absent from this list are deleted. Omit the field entirely to leave triggers untouched. At most 25 triggers per loop. */
export type LoopsPartialUpdateRequestTriggersList = Array<LoopTriggerWrite>;
export const LoopsPartialUpdateRequestTriggersList = /*@__PURE__*/ S.Array(
  LoopTriggerWrite,
) as any as S.Schema<LoopsPartialUpdateRequestTriggersList>;

export interface LoopsPartialUpdateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  id: string;
  /** Display name for the loop. */
  name?: string;
  /** Free-form description of what this loop does. */
  description?: string;
  /** On a team loop, claim ownership as part of this update so you can edit identity-bearing config (instructions, model, triggers, ...) that only the owner may change. Ignored on personal loops and on create. */
  take_ownership?: boolean;
  /** `personal` (owner-only) or `team` (visible and fireable by any team member). * `personal` - personal * `team` - team */
  visibility?: VisibilityEnum | (string & {});
  /** The prompt delivered to the agent on every run. */
  instructions?: string;
  /** Runtime adapter: 'claude' or 'codex'. * `claude` - claude * `codex` - codex */
  runtime_adapter?: RuntimeAdapterEnum | (string & {});
  /** LLM model identifier, validated against `runtime_adapter`'s catalog. Leave blank to let PostHog pick a sensible default at run time. */
  model?: string;
  /** Reasoning effort, validated against `runtime_adapter`/`model`'s supported set. * `low` - low * `medium` - medium * `high` - high * `xhigh` - xhigh * `max` - max */
  reasoning_effort?: ReasoningEffortEnum | (string & {}) | null;
  /** Repositories this loop operates on, ordered. Capped at 1 until multi-repo execution ships. May be empty for report-only loops. */
  repositories?: LoopsPartialUpdateRequestRepositoriesList;
  /** Sandbox environment carrying encrypted env vars and the network allowlist into every run. */
  sandbox_environment?: string | null;
  /** Whether the loop's triggers are active. Pausing disables all triggers. */
  enabled?: boolean;
  /** What happens when a trigger fires while a run is already active: 'skip', 'allow', or 'cancel_previous'. * `skip` - skip * `allow` - allow * `cancel_previous` - cancel_previous */
  overlap_policy?: OverlapPolicyEnum | (string & {});
  /** PR / CI-follow-up behavior configuration. */
  behaviors?: LoopBehaviors;
  /** MCP connector configuration for this loop's runs. */
  connectors?: LoopConnectors;
  /** Per-channel notification configuration. */
  notifications?: LoopNotifications;
  /** Context (channel) this loop is attached to, or null to detach. Drives feed placement and the context.md / canvas it keeps up to date. */
  context_target?: LoopContextTargetWrite | null;
  /** Full desired trigger list, id-stable: entries with a matching `id` are updated in place, entries without one are created, and existing triggers absent from this list are deleted. Omit the field entirely to leave triggers untouched. At most 25 triggers per loop. */
  triggers?: LoopsPartialUpdateRequestTriggersList;
}
export const LoopsPartialUpdateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    name: S.optional(S.String),
    description: S.optional(S.String),
    take_ownership: S.optional(S.Boolean),
    visibility: S.optional(VisibilityEnum),
    instructions: S.optional(S.String),
    runtime_adapter: S.optional(RuntimeAdapterEnum),
    model: S.optional(S.String),
    reasoning_effort: S.optional(S.NullOr(ReasoningEffortEnum)),
    repositories: S.optional(LoopsPartialUpdateRequestRepositoriesList),
    sandbox_environment: S.optional(S.NullOr(S.String)),
    enabled: S.optional(S.Boolean),
    overlap_policy: S.optional(OverlapPolicyEnum),
    behaviors: S.optional(LoopBehaviors),
    connectors: S.optional(LoopConnectors),
    notifications: S.optional(LoopNotifications),
    context_target: S.optional(S.NullOr(LoopContextTargetWrite)),
    triggers: S.optional(LoopsPartialUpdateRequestTriggersList),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/api/projects/{project_id}/loops/{id}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "LoopsPartialUpdateRequest",
}) as any as S.Schema<LoopsPartialUpdateRequest>;

export interface LoopsPreviewCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  id: string;
  /** Trigger type to simulate. Defaults to a synthetic schedule fire. * `schedule` - schedule * `github` - github * `api` - api */
  trigger_type?: LoopTriggerTypeEnum | (string & {});
  /** Sample trigger payload, e.g. a GitHub webhook body or an API trigger body, to render into context. */
  payload?: unknown;
}
export const LoopsPreviewCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    trigger_type: S.optional(LoopTriggerTypeEnum),
    payload: S.optional(S.Unknown),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/projects/{project_id}/loops/{id}/preview/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "LoopsPreviewCreateRequest",
}) as any as S.Schema<LoopsPreviewCreateRequest>;

export interface LoopPreviewDTO {
  instructions: string;
  trigger_type: string;
  trigger_context: string;
}
export const LoopPreviewDTO = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    instructions: S.String,
    trigger_type: S.String,
    trigger_context: S.String,
  }),
).annotate({ identifier: "LoopPreviewDTO" }) as any as S.Schema<LoopPreviewDTO>;

export interface LoopsRetrieveRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  id: string;
}
export const LoopsRetrieveRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/loops/{id}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "LoopsRetrieveRequest",
}) as any as S.Schema<LoopsRetrieveRequest>;

export interface LoopsRunCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  id: string;
}
export const LoopsRunCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/projects/{project_id}/loops/{id}/run/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "LoopsRunCreateRequest",
}) as any as S.Schema<LoopsRunCreateRequest>;

/** * `created` - created * `deduped` - deduped * `overlap_skipped` - overlap_skipped * `rate_capped` - rate_capped * `team_rate_capped` - team_rate_capped * `disabled` - disabled * `gate_blocked` - gate_blocked * `owner_inactive` - owner_inactive * `owner_changed` - owner_changed */
export type LoopFireResultReasonEnum =
  | "created"
  | "deduped"
  | "overlap_skipped"
  | "rate_capped"
  | "team_rate_capped"
  | "disabled"
  | "gate_blocked"
  | "owner_inactive"
  | "owner_changed";
export const LoopFireResultReasonEnum = /*@__PURE__*/ S.String;

/** Response for a manual (`run/`) or external (`trigger/`) fire. */
export interface LoopFireResult {
  created: boolean;
  /** Outcome of the fire attempt. * `created` - created * `deduped` - deduped * `overlap_skipped` - overlap_skipped * `rate_capped` - rate_capped * `team_rate_capped` - team_rate_capped * `disabled` - disabled * `gate_blocked` - gate_blocked * `owner_inactive` - owner_inactive * `owner_changed` - owner_changed */
  reason: LoopFireResultReasonEnum;
  /** Id of the created task, when `created` is true. */
  task_id: string | null;
  /** Id of the created task run, when `created` is true. */
  task_run_id: string | null;
}
export const LoopFireResult = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    created: S.Boolean,
    reason: LoopFireResultReasonEnum,
    task_id: S.NullOr(S.String),
    task_run_id: S.NullOr(S.String),
  }),
).annotate({ identifier: "LoopFireResult" }) as any as S.Schema<LoopFireResult>;

export interface LoopsRunsRetrieveRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  id: string;
  /** Opaque pagination cursor from a previous response's `next_cursor`. */
  cursor?: string;
  /** Max results per page (default 50, max 100). */
  limit?: number;
}
export const LoopsRunsRetrieveRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    cursor: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/loops/{id}/runs/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "LoopsRunsRetrieveRequest",
}) as any as S.Schema<LoopsRunsRetrieveRequest>;

export type LoopRunDTOOutputMap = { [key: string]: unknown | undefined };
export const LoopRunDTOOutputMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<LoopRunDTOOutputMap>;

/** A single entry in a loop's run history. */
export interface LoopRunDTO {
  id: string;
  task_id: string;
  loop_trigger_id: string | null;
  status: string;
  environment: string;
  branch: string | null;
  error_message: string | null;
  output: LoopRunDTOOutputMap | null;
  created_at: string;
  completed_at: string | null;
}
export const LoopRunDTO = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    task_id: S.String,
    loop_trigger_id: S.NullOr(S.String),
    status: S.String,
    environment: S.String,
    branch: S.NullOr(S.String),
    error_message: S.NullOr(S.String),
    output: S.NullOr(LoopRunDTOOutputMap),
    created_at: S.String,
    completed_at: S.NullOr(S.String),
  }),
).annotate({ identifier: "LoopRunDTO" }) as any as S.Schema<LoopRunDTO>;

/** Run history entries, newest first. */
export type LoopRunPageResultsList = Array<LoopRunDTO>;
export const LoopRunPageResultsList = /*@__PURE__*/ S.Array(
  LoopRunDTO,
) as any as S.Schema<LoopRunPageResultsList>;

export interface LoopRunPage {
  /** Run history entries, newest first. */
  results: LoopRunPageResultsList;
  /** Opaque cursor for the next page, or null when there are no more results. */
  next_cursor: string | null;
}
export const LoopRunPage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: LoopRunPageResultsList,
    next_cursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "LoopRunPage" }) as any as S.Schema<LoopRunPage>;

export type LoopsTriggerCreateRequestBodyMap = {
  [key: string]: unknown | undefined;
};
export const LoopsTriggerCreateRequestBodyMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<LoopsTriggerCreateRequestBodyMap>;

export interface LoopsTriggerCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  id: string;
  body?: LoopsTriggerCreateRequestBodyMap;
}
export const LoopsTriggerCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    body: S.optional(LoopsTriggerCreateRequestBodyMap.pipe(T.HttpBody())),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/projects/{project_id}/loops/{id}/trigger/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "LoopsTriggerCreateRequest",
}) as any as S.Schema<LoopsTriggerCreateRequest>;

export type LoopsCreateError = PosthogOpError;
/** Create a loop API for managing loops — named, cloud-executed agent automations triggered by schedule, GitHub events or authenticated API calls. See `products/tasks/docs/LOOPS.md`. */
export const loopsCreate: API.OperationMethod<
  LoopsCreateRequest,
  LoopDTO,
  LoopsCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LoopsCreateRequest,
  output: LoopDTO,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type LoopsDestroyError = Forbidden | NotFound | PosthogOpError;
/** Delete a loop Soft delete. Pauses every trigger's schedule. Owner or a project admin only. */
export const loopsDestroy: API.OperationMethod<
  LoopsDestroyRequest,
  LoopsDestroyResponse,
  LoopsDestroyError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LoopsDestroyRequest,
  output: LoopsDestroyResponse,
  errors: [Forbidden, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type LoopsListError = PosthogOpError;
/** List loops List loops visible to the caller: personal loops they own, plus every team loop. The response also carries `max_loops_per_team` and `total_loop_count` so a client can show remaining capacity and disable creation at the cap without hardcoding the limit. */
export const loopsList: API.OperationMethod<
  LoopsListRequest,
  PaginatedLoopDTOList,
  LoopsListError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LoopsListRequest,
  output: PaginatedLoopDTOList,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type LoopsPartialUpdateError = Forbidden | NotFound | PosthogOpError;
/** Update a loop Partial update. Identity-bearing fields (instructions, repositories, connectors, behaviors, model config, triggers) are owner-only on team loops; name, description, notifications and enable/pause are editable by any team member. */
export const loopsPartialUpdate: API.OperationMethod<
  LoopsPartialUpdateRequest,
  LoopDTO,
  LoopsPartialUpdateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LoopsPartialUpdateRequest,
  output: LoopDTO,
  errors: [Forbidden, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type LoopsPreviewCreateError = NotFound | PosthogOpError;
/** Preview a loop fire Dry run: renders the assembled instructions and trigger context for a supplied sample payload (or a synthetic schedule fire when omitted), without creating a task, run, or any other side effect. */
export const loopsPreviewCreate: API.OperationMethod<
  LoopsPreviewCreateRequest,
  LoopPreviewDTO,
  LoopsPreviewCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LoopsPreviewCreateRequest,
  output: LoopPreviewDTO,
  errors: [NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type LoopsRetrieveError = NotFound | PosthogOpError;
/** Get a loop API for managing loops — named, cloud-executed agent automations triggered by schedule, GitHub events or authenticated API calls. See `products/tasks/docs/LOOPS.md`. */
export const loopsRetrieve: API.OperationMethod<
  LoopsRetrieveRequest,
  LoopDTO,
  LoopsRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LoopsRetrieveRequest,
  output: LoopDTO,
  errors: [NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type LoopsRunCreateError = Forbidden | NotFound | PosthogOpError;
/** Run a loop manually Manual fire from the UI. Owner-only for personal loops; any team member for team loops. */
export const loopsRunCreate: API.OperationMethod<
  LoopsRunCreateRequest,
  LoopFireResult,
  LoopsRunCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LoopsRunCreateRequest,
  output: LoopFireResult,
  errors: [Forbidden, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type LoopsRunsRetrieveError = NotFound | PosthogOpError;
/** List loop runs Run history for a loop, newest first, cursor-paginated. */
export const loopsRunsRetrieve: API.OperationMethod<
  LoopsRunsRetrieveRequest,
  LoopRunPage,
  LoopsRunsRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LoopsRunsRetrieveRequest,
  output: LoopRunPage,
  errors: [NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type LoopsTriggerCreateError = NotFound | PosthogOpError;
/** Fire a loop externally Authenticated POST trigger for `type=api` triggers. Project secret API key auth (`loop:write` scope), project-wide. Request body (JSON, capped at 64 KB) becomes run context. Send an `Idempotency-Key` header to dedupe retries. */
export const loopsTriggerCreate: API.OperationMethod<
  LoopsTriggerCreateRequest,
  LoopFireResult,
  LoopsTriggerCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LoopsTriggerCreateRequest,
  output: LoopFireResult,
  errors: [NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));
