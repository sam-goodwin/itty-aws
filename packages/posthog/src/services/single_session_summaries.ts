// AUTO-GENERATED by scripts/generate.ts from .generated-specs (see scripts/convert.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  PosthogProtocol,
  type PosthogOpError,
  type PosthogOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { PosthogOpError, PosthogOpContext };

export type SingleSessionSummariesListRequestOrder =
  | "-created_at"
  | "-session_duration"
  | "-session_start_time"
  | "created_at"
  | "session_duration"
  | "session_start_time";
export const SingleSessionSummariesListRequestOrder = /*@__PURE__*/ S.String;

export type SingleSessionSummariesListRequestOutcome =
  | "failure"
  | "success"
  | "unknown";
export const SingleSessionSummariesListRequestOutcome = /*@__PURE__*/ S.String;

export interface SingleSessionSummariesListRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Filter to summaries triggered by a specific user, identified by `User.uuid`. */
  created_by?: string;
  /** Inclusive lower bound on `created_at`, accepts relative shorthand like `-7d`. */
  date_from?: string;
  /** Inclusive upper bound on `created_at`, accepts relative shorthand like `-1d`. */
  date_to?: string;
  /** Filter to summaries for a single user (the session's `distinct_id`). */
  distinct_id?: string;
  /** When true, only summaries that surfaced one or more exception events; when false, only summaries without exceptions. */
  has_exceptions?: boolean;
  /** When true, only summaries produced via the video-based visual-confirmation workflow. */
  has_visual_confirmation?: boolean;
  /** Number of results to return per page. */
  limit?: number;
  /** The initial index from which to return the results. */
  offset?: number;
  /** Ordering field, defaults to `-created_at` (most recent first). Allowed: `created_at`, `session_start_time`, `session_duration` (prefix with `-` for descending). */
  order?: SingleSessionSummariesListRequestOrder | (string & {});
  /** Filter by the summary's recorded `session_outcome.success` field. `success` for true, `failure` for false, `unknown` for summaries without an outcome. */
  outcome?: SingleSessionSummariesListRequestOutcome | (string & {});
  /** Comma-separated list of session IDs to restrict the result to (uses the `(team, session_id)` index). */
  session_ids?: string;
}
export const SingleSessionSummariesListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    created_by: S.optional(S.String.pipe(T.Query())),
    date_from: S.optional(S.String.pipe(T.Query())),
    date_to: S.optional(S.String.pipe(T.Query())),
    distinct_id: S.optional(S.String.pipe(T.Query())),
    has_exceptions: S.optional(S.Boolean.pipe(T.Query())),
    has_visual_confirmation: S.optional(S.Boolean.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
    order: S.optional(SingleSessionSummariesListRequestOrder.pipe(T.Query())),
    outcome: S.optional(
      SingleSessionSummariesListRequestOutcome.pipe(T.Query()),
    ),
    session_ids: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/single_session_summaries/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SingleSessionSummariesListRequest",
}) as any as S.Schema<SingleSessionSummariesListRequest>;

/** Headline outcome from the summary: `{success: bool, description: string}` or null if the summary did not record one. Useful for quickly classifying a session as success/failure. */
export interface SingleSessionSummaryMinimalSessionOutcome {
  success?: boolean;
  description?: string;
}
export const SingleSessionSummaryMinimalSessionOutcome =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      success: S.optional(S.Boolean),
      description: S.optional(S.String),
    }),
  ).annotate({
    identifier: "SingleSessionSummaryMinimalSessionOutcome",
  }) as any as S.Schema<SingleSessionSummaryMinimalSessionOutcome>;

/** Optional context passed to the summary at generation time (e.g. `focus_area`). */
export interface SingleSessionSummaryMinimalExtraSummaryContext {
  focus_area?: string;
}
export const SingleSessionSummaryMinimalExtraSummaryContext =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      focus_area: S.optional(S.String),
    }),
  ).annotate({
    identifier: "SingleSessionSummaryMinimalExtraSummaryContext",
  }) as any as S.Schema<SingleSessionSummaryMinimalExtraSummaryContext>;

export type UserBasicHedgehogConfigMap = { [key: string]: unknown | undefined };
export const UserBasicHedgehogConfigMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<UserBasicHedgehogConfigMap>;

/** * `engineering` - Engineering * `data` - Data * `product` - Product Management * `founder` - Founder * `leadership` - Leadership * `marketing` - Marketing * `sales` - Sales / Success * `other` - Other */
export type RoleAtOrganizationEnum =
  | "engineering"
  | "data"
  | "product"
  | "founder"
  | "leadership"
  | "marketing"
  | "sales"
  | "other";
export const RoleAtOrganizationEnum = /*@__PURE__*/ S.String;

export type BlankEnum = "";
export const BlankEnum = /*@__PURE__*/ S.String;

export type UserBasicRoleAtOrganization = RoleAtOrganizationEnum | BlankEnum;
export const UserBasicRoleAtOrganization =
  /*@__PURE__*/ S.Unknown as any as S.Schema<UserBasicRoleAtOrganization>;

export interface UserBasic {
  id?: number;
  uuid?: string;
  distinct_id?: string | null;
  first_name?: string;
  last_name?: string;
  email?: string;
  is_email_verified?: boolean | null;
  hedgehog_config?: UserBasicHedgehogConfigMap | null;
  role_at_organization?: UserBasicRoleAtOrganization | null;
}
export const UserBasic = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.Number),
    uuid: S.optional(S.String),
    distinct_id: S.optional(S.NullOr(S.String)),
    first_name: S.optional(S.String),
    last_name: S.optional(S.String),
    email: S.optional(S.String),
    is_email_verified: S.optional(S.NullOr(S.Boolean)),
    hedgehog_config: S.optional(S.NullOr(UserBasicHedgehogConfigMap)),
    role_at_organization: S.optional(S.NullOr(UserBasicRoleAtOrganization)),
  }),
).annotate({ identifier: "UserBasic" }) as any as S.Schema<UserBasic>;

/** Lightweight projection for list endpoints — omits the full `summary` JSON (~50 KB per row). */
export interface SingleSessionSummaryMinimal {
  id: string;
  /** Session replay ID */
  session_id: string;
  /** Distinct ID of the session's user */
  distinct_id: string | null;
  /** Session start time */
  session_start_time: string | null;
  /** Session duration in seconds */
  session_duration: number | null;
  /** Headline outcome from the summary: `{success: bool, description: string}` or null if the summary did not record one. Useful for quickly classifying a session as success/failure. */
  session_outcome: SingleSessionSummaryMinimalSessionOutcome | null;
  /** Number of exception event IDs surfaced by this summary (capped at 100). */
  exception_count: number;
  /** True if the summary surfaced any exception events. */
  has_exceptions: boolean;
  /** LLM model identifier that generated this summary, if recorded in run metadata. */
  model_used: string | null;
  /** True if the summary was produced with video-based visual confirmation (the rasterized-recording path). */
  visual_confirmation: boolean;
  /** Optional context passed to the summary at generation time (e.g. `focus_area`). */
  extra_summary_context: SingleSessionSummaryMinimalExtraSummaryContext | null;
  created_at: string;
  created_by: UserBasic | null;
}
export const SingleSessionSummaryMinimal = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    session_id: S.String,
    distinct_id: S.NullOr(S.String),
    session_start_time: S.NullOr(S.String),
    session_duration: S.NullOr(S.Number),
    session_outcome: S.NullOr(SingleSessionSummaryMinimalSessionOutcome),
    exception_count: S.Number,
    has_exceptions: S.Boolean,
    model_used: S.NullOr(S.String),
    visual_confirmation: S.Boolean,
    extra_summary_context: S.NullOr(
      SingleSessionSummaryMinimalExtraSummaryContext,
    ),
    created_at: S.String,
    created_by: S.NullOr(UserBasic),
  }),
).annotate({
  identifier: "SingleSessionSummaryMinimal",
}) as any as S.Schema<SingleSessionSummaryMinimal>;

export type PaginatedSingleSessionSummaryMinimalListResultsList =
  Array<SingleSessionSummaryMinimal>;
export const PaginatedSingleSessionSummaryMinimalListResultsList =
  /*@__PURE__*/ S.Array(
    SingleSessionSummaryMinimal,
  ) as any as S.Schema<PaginatedSingleSessionSummaryMinimalListResultsList>;

export interface PaginatedSingleSessionSummaryMinimalList {
  count: number;
  next?: string | null;
  previous?: string | null;
  results: PaginatedSingleSessionSummaryMinimalListResultsList;
}
export const PaginatedSingleSessionSummaryMinimalList = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      count: S.Number,
      next: S.optional(S.NullOr(S.String)),
      previous: S.optional(S.NullOr(S.String)),
      results: PaginatedSingleSessionSummaryMinimalListResultsList,
    }),
).annotate({
  identifier: "PaginatedSingleSessionSummaryMinimalList",
}) as any as S.Schema<PaginatedSingleSessionSummaryMinimalList>;

export interface SingleSessionSummariesRetrieveRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  session_id: string;
}
export const SingleSessionSummariesRetrieveRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      session_id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/single_session_summaries/{session_id}/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "SingleSessionSummariesRetrieveRequest",
}) as any as S.Schema<SingleSessionSummariesRetrieveRequest>;

/** Full LLM-generated summary JSON. Contains `segments` (chronological journey segments), `key_actions` (per-segment events with `abandonment` / `confusion` / `exception` flags — the structured source of session-level problems), `segment_outcomes`, and `session_outcome`. Video-based runs additionally include a `sentiment` block. */
export type SingleSessionSummarySummaryMap = {
  [key: string]: unknown | undefined;
};
export const SingleSessionSummarySummaryMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<SingleSessionSummarySummaryMap>;

/** Event IDs (capped at 100) where exceptions occurred during the session — extracted from the summary for searchability. */
export type SingleSessionSummaryExceptionEventIdsList = Array<string>;
export const SingleSessionSummaryExceptionEventIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SingleSessionSummaryExceptionEventIdsList>;

/** Optional context passed to the summary at generation time (e.g. `focus_area`). */
export type SingleSessionSummaryExtraSummaryContext =
  SingleSessionSummaryMinimalExtraSummaryContext;
export const SingleSessionSummaryExtraSummaryContext =
  SingleSessionSummaryMinimalExtraSummaryContext;

/** Full session summary, including the generated `summary` JSON content. */
export interface SingleSessionSummary {
  id: string;
  /** Session replay ID */
  session_id: string;
  /** Distinct ID of the session's user */
  distinct_id: string | null;
  /** Session start time */
  session_start_time: string | null;
  /** Session duration in seconds */
  session_duration: number | null;
  /** Full LLM-generated summary JSON. Contains `segments` (chronological journey segments), `key_actions` (per-segment events with `abandonment` / `confusion` / `exception` flags — the structured source of session-level problems), `segment_outcomes`, and `session_outcome`. Video-based runs additionally include a `sentiment` block. */
  summary: SingleSessionSummarySummaryMap;
  /** Event IDs (capped at 100) where exceptions occurred during the session — extracted from the summary for searchability. */
  exception_event_ids: SingleSessionSummaryExceptionEventIdsList;
  /** Optional context passed to the summary at generation time (e.g. `focus_area`). */
  extra_summary_context: SingleSessionSummaryMinimalExtraSummaryContext | null;
  /** `SessionSummaryRunMeta` — model used, whether video-based visual confirmation was applied, and visual-confirmation event-to-asset mappings. */
  run_metadata: unknown | null;
  created_at: string;
  created_by: UserBasic | null;
}
export const SingleSessionSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    session_id: S.String,
    distinct_id: S.NullOr(S.String),
    session_start_time: S.NullOr(S.String),
    session_duration: S.NullOr(S.Number),
    summary: SingleSessionSummarySummaryMap,
    exception_event_ids: SingleSessionSummaryExceptionEventIdsList,
    extra_summary_context: S.NullOr(
      SingleSessionSummaryMinimalExtraSummaryContext,
    ),
    run_metadata: S.NullOr(S.Unknown),
    created_at: S.String,
    created_by: S.NullOr(UserBasic),
  }),
).annotate({
  identifier: "SingleSessionSummary",
}) as any as S.Schema<SingleSessionSummary>;

export type SingleSessionSummariesListError = PosthogOpError;
/** List stored AI-generated session summaries for the team, one row per session (latest summary kept). Use to discover which sessions have been summarized and to filter for sessions with specific problems — `has_exceptions=true`, `outcome=failure`, or a custom `session_ids` narrowing. Returns lightweight rows without the full summary JSON; use the retrieve endpoint for the per-segment / per-action detail. */
export const singleSessionSummariesList: API.OperationMethod<
  SingleSessionSummariesListRequest,
  PaginatedSingleSessionSummaryMinimalList,
  SingleSessionSummariesListError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SingleSessionSummariesListRequest,
  output: PaginatedSingleSessionSummaryMinimalList,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type SingleSessionSummariesRetrieveError = PosthogOpError;
/** Get the latest stored AI summary for a single session by `session_id`. Returns the full `summary` JSON (segments with named timeline, per-action `abandonment` / `confusion` / `exception` flags, segment outcomes, headline `session_outcome`, optional `sentiment`), the `exception_event_ids` array, the `extra_summary_context` (e.g. `focus_area`) used at generation time, and the `run_metadata` (LLM model used, whether visual confirmation was applied). 404 if no summary has been generated for this session yet — to trigger generation, use the existing `session-recording-summarize` flow rather than this endpoint. */
export const singleSessionSummariesRetrieve: API.OperationMethod<
  SingleSessionSummariesRetrieveRequest,
  SingleSessionSummary,
  SingleSessionSummariesRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SingleSessionSummariesRetrieveRequest,
  output: SingleSessionSummary,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));
