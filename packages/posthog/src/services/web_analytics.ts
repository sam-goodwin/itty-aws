// AUTO-GENERATED by scripts/generate.ts from .generated-specs (see scripts/convert.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  PosthogProtocol,
  type PosthogOpError,
  type PosthogOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { PosthogOpError, PosthogOpContext };

export interface WebAnalyticsRecapRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** When true (default), include period-over-period change for each metric comparing against the prior equal-length period. Set to false to skip the comparison query. */
  compare?: boolean;
  /** Lookback window in days (1–90). Defaults to 7. */
  days?: number;
}
export const WebAnalyticsRecapRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    compare: S.optional(S.Boolean.pipe(T.Query())),
    days: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/web_analytics/recap/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "WebAnalyticsRecapRequest",
}) as any as S.Schema<WebAnalyticsRecapRequest>;

/** * `Up` - Up * `Down` - Down */
export type WoWChangeDirectionEnum = "Up" | "Down";
export const WoWChangeDirectionEnum = /*@__PURE__*/ S.String;

export interface WoWChange {
  /** Absolute percentage change, rounded to nearest integer. */
  percent?: number;
  /** Direction of the change relative to the prior period. * `Up` - Up * `Down` - Down */
  direction?: WoWChangeDirectionEnum;
  /** Hex color indicating whether the change is a positive or negative signal. */
  color?: string;
  /** Short label, e.g. 'Up 12%'. */
  text?: string;
  /** Verbose label, e.g. 'Up 12% from prior period'. */
  long_text?: string;
}
export const WoWChange = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    percent: S.optional(S.Number),
    direction: S.optional(WoWChangeDirectionEnum),
    color: S.optional(S.String),
    text: S.optional(S.String),
    long_text: S.optional(S.String),
  }),
).annotate({ identifier: "WoWChange" }) as any as S.Schema<WoWChange>;

export interface NumericMetric {
  /** Value for the most recent period. */
  current?: number;
  /** Value for the prior period, if available. */
  previous?: number | null;
  /** Period-over-period change, null when not meaningful. */
  change?: WoWChange | null;
}
export const NumericMetric = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    current: S.optional(S.Number),
    previous: S.optional(S.NullOr(S.Number)),
    change: S.optional(S.NullOr(WoWChange)),
  }),
).annotate({ identifier: "NumericMetric" }) as any as S.Schema<NumericMetric>;

export interface DurationMetric {
  /** Human-readable duration, e.g. '2m 34s'. */
  current?: string;
  /** Prior-period duration, e.g. '2m 10s'. */
  previous?: string | null;
  /** Period-over-period change, null when not meaningful. */
  change?: WoWChange | null;
}
export const DurationMetric = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    current: S.optional(S.String),
    previous: S.optional(S.NullOr(S.String)),
    change: S.optional(S.NullOr(WoWChange)),
  }),
).annotate({ identifier: "DurationMetric" }) as any as S.Schema<DurationMetric>;

export interface TopPage {
  /** Host for the page, if recorded. */
  host?: string;
  /** URL path. */
  path?: string;
  /** Unique visitors in the period. */
  visitors?: number;
  /** Period-over-period change in visitors, null when not meaningful. */
  change?: WoWChange | null;
}
export const TopPage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    host: S.optional(S.String),
    path: S.optional(S.String),
    visitors: S.optional(S.Number),
    change: S.optional(S.NullOr(WoWChange)),
  }),
).annotate({ identifier: "TopPage" }) as any as S.Schema<TopPage>;

/** Top 5 pages by unique visitors. */
export type WebAnalyticsRecapResponseTopPagesList = Array<TopPage>;
export const WebAnalyticsRecapResponseTopPagesList = /*@__PURE__*/ S.Array(
  TopPage,
) as any as S.Schema<WebAnalyticsRecapResponseTopPagesList>;

export interface TopSource {
  /** Initial referring domain. */
  name?: string;
  /** Unique visitors from this source. */
  visitors?: number;
  /** Period-over-period change in visitors, null when not meaningful. */
  change?: WoWChange | null;
}
export const TopSource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    visitors: S.optional(S.Number),
    change: S.optional(S.NullOr(WoWChange)),
  }),
).annotate({ identifier: "TopSource" }) as any as S.Schema<TopSource>;

/** Top 5 traffic sources by unique visitors. */
export type WebAnalyticsRecapResponseTopSourcesList = Array<TopSource>;
export const WebAnalyticsRecapResponseTopSourcesList = /*@__PURE__*/ S.Array(
  TopSource,
) as any as S.Schema<WebAnalyticsRecapResponseTopSourcesList>;

export interface Goal {
  /** Goal name (action name). */
  name?: string;
  /** Total conversions in the period. */
  conversions?: number;
  /** Period-over-period change in conversions, null when not meaningful. */
  change?: WoWChange | null;
}
export const Goal = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    conversions: S.optional(S.Number),
    change: S.optional(S.NullOr(WoWChange)),
  }),
).annotate({ identifier: "Goal" }) as any as S.Schema<Goal>;

/** Goal conversions. */
export type WebAnalyticsRecapResponseGoalsList = Array<Goal>;
export const WebAnalyticsRecapResponseGoalsList = /*@__PURE__*/ S.Array(
  Goal,
) as any as S.Schema<WebAnalyticsRecapResponseGoalsList>;

export interface RecapPersona {
  /** Stable persona identifier. One of: just_getting_started, conversion_machine, traffic_magnet, crowd_favorite, search_hog, word_of_mouth, loyal_following, rising_star, steady_hog. */
  id: string;
  /** Display name for the persona, e.g. 'Traffic Magnet'. */
  name: string;
  /** Emoji representing the persona. */
  emoji: string;
  /** One-line explanation of why this persona was assigned this week. */
  blurb: string;
  /** Hex accent color for rendering the persona card. */
  color: string;
}
export const RecapPersona = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    emoji: S.String,
    blurb: S.String,
    color: S.String,
  }),
).annotate({ identifier: "RecapPersona" }) as any as S.Schema<RecapPersona>;

export interface RecapHighlight {
  /** Stable highlight identifier, e.g. 'milestone', 'rising_page', 'top_source'. */
  id: string;
  /** Emoji for the highlight. */
  emoji: string;
  /** Short headline for the highlight, e.g. 'Rising star page'. */
  title: string;
  /** The standout value, e.g. a page path or visitor count. */
  value: string;
  /** Supporting sentence for the highlight. */
  detail: string;
}
export const RecapHighlight = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    emoji: S.String,
    title: S.String,
    value: S.String,
    detail: S.String,
  }),
).annotate({ identifier: "RecapHighlight" }) as any as S.Schema<RecapHighlight>;

/** Up to three screenshot-worthy superlatives for the week. */
export type WebAnalyticsRecapResponseHighlightsList = Array<RecapHighlight>;
export const WebAnalyticsRecapResponseHighlightsList = /*@__PURE__*/ S.Array(
  RecapHighlight,
) as any as S.Schema<WebAnalyticsRecapResponseHighlightsList>;

export interface WebAnalyticsRecapResponse {
  /** Unique visitors. */
  visitors: NumericMetric;
  /** Total pageviews. */
  pageviews: NumericMetric;
  /** Total sessions. */
  sessions: NumericMetric;
  /** Bounce rate (0–100). */
  bounce_rate: NumericMetric;
  /** Average session duration. */
  avg_session_duration: DurationMetric;
  /** Top 5 pages by unique visitors. */
  top_pages: WebAnalyticsRecapResponseTopPagesList;
  /** Top 5 traffic sources by unique visitors. */
  top_sources: WebAnalyticsRecapResponseTopSourcesList;
  /** Goal conversions. */
  goals: WebAnalyticsRecapResponseGoalsList;
  /** Link to the Web analytics dashboard for this project. */
  dashboard_url: string;
  /** The single weekly persona assigned from this week's data. */
  persona: RecapPersona;
  /** Up to three screenshot-worthy superlatives for the week. */
  highlights: WebAnalyticsRecapResponseHighlightsList;
  /** Human-readable period label, e.g. 'Last 7 days'. */
  period_label: string;
  /** First date included in the recap period, in the project timezone. */
  period_start: string;
  /** Final date included in the recap period, in the project timezone. */
  period_end: string;
  /** Name of the project this recap is for. */
  project_name: string;
  /** Canonical link to this project's weekly recap. */
  recap_url: string;
}
export const WebAnalyticsRecapResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    visitors: NumericMetric,
    pageviews: NumericMetric,
    sessions: NumericMetric,
    bounce_rate: NumericMetric,
    avg_session_duration: DurationMetric,
    top_pages: WebAnalyticsRecapResponseTopPagesList,
    top_sources: WebAnalyticsRecapResponseTopSourcesList,
    goals: WebAnalyticsRecapResponseGoalsList,
    dashboard_url: S.String,
    persona: RecapPersona,
    highlights: WebAnalyticsRecapResponseHighlightsList,
    period_label: S.String,
    period_start: S.String,
    period_end: S.String,
    project_name: S.String,
    recap_url: S.String,
  }),
).annotate({
  identifier: "WebAnalyticsRecapResponse",
}) as any as S.Schema<WebAnalyticsRecapResponse>;

export interface WebAnalyticsWeeklyDigestRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** When true (default), include period-over-period change for each metric comparing against the prior equal-length period. Set to false to skip the comparison query (faster). */
  compare?: boolean;
  /** Lookback window in days (1–90). Defaults to 7. */
  days?: number;
}
export const WebAnalyticsWeeklyDigestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    compare: S.optional(S.Boolean.pipe(T.Query())),
    days: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/web_analytics/weekly_digest/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "WebAnalyticsWeeklyDigestRequest",
}) as any as S.Schema<WebAnalyticsWeeklyDigestRequest>;

/** Top 5 pages by unique visitors. */
export type WeeklyDigestResponseTopPagesList = Array<TopPage>;
export const WeeklyDigestResponseTopPagesList = /*@__PURE__*/ S.Array(
  TopPage,
) as any as S.Schema<WeeklyDigestResponseTopPagesList>;

/** Top 5 traffic sources by unique visitors. */
export type WeeklyDigestResponseTopSourcesList = Array<TopSource>;
export const WeeklyDigestResponseTopSourcesList = /*@__PURE__*/ S.Array(
  TopSource,
) as any as S.Schema<WeeklyDigestResponseTopSourcesList>;

/** Goal conversions. */
export type WeeklyDigestResponseGoalsList = Array<Goal>;
export const WeeklyDigestResponseGoalsList = /*@__PURE__*/ S.Array(
  Goal,
) as any as S.Schema<WeeklyDigestResponseGoalsList>;

export interface WeeklyDigestResponse {
  /** Unique visitors. */
  visitors?: NumericMetric;
  /** Total pageviews. */
  pageviews?: NumericMetric;
  /** Total sessions. */
  sessions?: NumericMetric;
  /** Bounce rate (0–100). */
  bounce_rate?: NumericMetric;
  /** Average session duration. */
  avg_session_duration?: DurationMetric;
  /** Top 5 pages by unique visitors. */
  top_pages?: WeeklyDigestResponseTopPagesList;
  /** Top 5 traffic sources by unique visitors. */
  top_sources?: WeeklyDigestResponseTopSourcesList;
  /** Goal conversions. */
  goals?: WeeklyDigestResponseGoalsList;
  /** Link to the Web analytics dashboard for this project. */
  dashboard_url?: string;
}
export const WeeklyDigestResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    visitors: S.optional(NumericMetric),
    pageviews: S.optional(NumericMetric),
    sessions: S.optional(NumericMetric),
    bounce_rate: S.optional(NumericMetric),
    avg_session_duration: S.optional(DurationMetric),
    top_pages: S.optional(WeeklyDigestResponseTopPagesList),
    top_sources: S.optional(WeeklyDigestResponseTopSourcesList),
    goals: S.optional(WeeklyDigestResponseGoalsList),
    dashboard_url: S.optional(S.String),
  }),
).annotate({
  identifier: "WeeklyDigestResponse",
}) as any as S.Schema<WeeklyDigestResponse>;

export type WebAnalyticsRecapError = PosthogOpError;
/** Weekly web analytics recap The 'Wrapped'-style weekly recap: everything in the weekly digest (visitors, pageviews, sessions, bounce rate, average session duration with period-over-period comparisons, top pages, top sources, and goals) plus a single derived weekly persona and a short list of screenshot-worthy highlights for the period. */
export const webAnalyticsRecap: API.OperationMethod<
  WebAnalyticsRecapRequest,
  WebAnalyticsRecapResponse,
  WebAnalyticsRecapError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WebAnalyticsRecapRequest,
  output: WebAnalyticsRecapResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type WebAnalyticsWeeklyDigestError = PosthogOpError;
/** Summarize web analytics Summarizes a project's web analytics over a lookback window (default 7 days): unique visitors, pageviews, sessions, bounce rate, and average session duration with period-over-period comparisons, plus the top 5 pages, top 5 traffic sources, and goal conversions. */
export const webAnalyticsWeeklyDigest: API.OperationMethod<
  WebAnalyticsWeeklyDigestRequest,
  WeeklyDigestResponse,
  WebAnalyticsWeeklyDigestError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WebAnalyticsWeeklyDigestRequest,
  output: WeeklyDigestResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));
