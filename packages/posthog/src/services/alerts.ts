// AUTO-GENERATED by scripts/generate.ts from .generated-specs (see scripts/convert.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  PosthogProtocol,
  type PosthogOpError,
  type PosthogOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { PosthogOpError, PosthogOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface AlertsDestroyRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this alert configuration. */
  id: string;
}
export const AlertsDestroyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/api/projects/{project_id}/alerts/{id}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AlertsDestroyRequest",
}) as any as S.Schema<AlertsDestroyRequest>;

export interface AlertsDestroyResponse {}
export const AlertsDestroyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "AlertsDestroyResponse",
}) as any as S.Schema<AlertsDestroyResponse>;

export interface AlertsRetrieveRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this alert configuration. */
  id: string;
  /** Relative date string for the start of the check history window (e.g. '-24h', '-7d', '-14d'). Returns checks created after this time. Max retention is 14 days. */
  checks_date_from?: string;
  /** Relative date string for the end of the check history window (e.g. '-1h', '-1d'). Defaults to now if not specified. */
  checks_date_to?: string;
  /** Maximum number of check results to return (default 5, max 500). Applied after date filtering. */
  checks_limit?: number;
  /** Number of newest checks to skip (0-based). Use with checks_limit for pagination. Default 0. */
  checks_offset?: number;
}
export const AlertsRetrieveRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    checks_date_from: S.optional(S.String.pipe(T.Query())),
    checks_date_to: S.optional(S.String.pipe(T.Query())),
    checks_limit: S.optional(S.Number.pipe(T.Query())),
    checks_offset: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/alerts/{id}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AlertsRetrieveRequest",
}) as any as S.Schema<AlertsRetrieveRequest>;

export type UserBasicHedgehogConfigMap = { [key: string]: unknown | undefined };
export const UserBasicHedgehogConfigMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<UserBasicHedgehogConfigMap>;

/** * `engineering` - Engineering * `data` - Data * `product` - Product Management * `founder` - Founder * `leadership` - Leadership * `marketing` - Marketing * `sales` - Sales / Success * `student` - Student * `other` - Other */
export type RoleAtOrganizationEnum =
  | "engineering"
  | "data"
  | "product"
  | "founder"
  | "leadership"
  | "marketing"
  | "sales"
  | "student"
  | "other";
export const RoleAtOrganizationEnum = /*@__PURE__*/ S.String;

export type BlankEnum = "";
export const BlankEnum = /*@__PURE__*/ S.String;

export type UserBasicRoleAtOrganization = RoleAtOrganizationEnum | BlankEnum;
export const UserBasicRoleAtOrganization =
  /*@__PURE__*/ S.Unknown as any as S.Schema<UserBasicRoleAtOrganization>;

export interface UserBasic {
  id?: number;
  uuid?: string;
  distinct_id?: string | null;
  first_name?: string;
  last_name?: string;
  email?: string;
  is_email_verified?: boolean | null;
  hedgehog_config?: UserBasicHedgehogConfigMap | null;
  role_at_organization?: UserBasicRoleAtOrganization | null;
}
export const UserBasic = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.Number),
    uuid: S.optional(S.String),
    distinct_id: S.optional(S.NullOr(S.String)),
    first_name: S.optional(S.String),
    last_name: S.optional(S.String),
    email: S.optional(S.String),
    is_email_verified: S.optional(S.NullOr(S.Boolean)),
    hedgehog_config: S.optional(S.NullOr(UserBasicHedgehogConfigMap)),
    role_at_organization: S.optional(S.NullOr(UserBasicRoleAtOrganization)),
  }),
).annotate({ identifier: "UserBasic" }) as any as S.Schema<UserBasic>;

/** User IDs to subscribe to this alert. Note: Response returns full UserBasicSerializer object. */
export type AlertSubscribedUsersList = Array<number>;
export const AlertSubscribedUsersList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<AlertSubscribedUsersList>;

export interface InsightsThresholdBounds {
  /** Alert fires when the value drops below this number. */
  lower?: number | null;
  /** Alert fires when the value exceeds this number. */
  upper?: number | null;
}
export const InsightsThresholdBounds = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    lower: S.optional(S.NullOr(S.Number)),
    upper: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "InsightsThresholdBounds",
}) as any as S.Schema<InsightsThresholdBounds>;

export type InsightThresholdType = "absolute" | "percentage";
export const InsightThresholdType = /*@__PURE__*/ S.String;

export interface InsightThreshold {
  bounds?: InsightsThresholdBounds | null;
  /** Whether bounds are compared as absolute values or as percentage change from the previous interval. */
  type?: InsightThresholdType | (string & {});
}
export const InsightThreshold = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bounds: S.optional(S.NullOr(InsightsThresholdBounds)),
    type: S.optional(InsightThresholdType),
  }),
).annotate({
  identifier: "InsightThreshold",
}) as any as S.Schema<InsightThreshold>;

export interface Threshold {
  id?: string;
  created_at?: string;
  /** Optional name for the threshold. */
  name?: string;
  /** Threshold bounds and type. Includes bounds (lower/upper floats) and type (absolute or percentage). For threshold-based alerts (no detector_config), at least one of lower or upper must be set. */
  configuration?: InsightThreshold;
}
export const Threshold = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    created_at: S.optional(S.String),
    name: S.optional(S.String),
    configuration: S.optional(InsightThreshold),
  }),
).annotate({ identifier: "Threshold" }) as any as S.Schema<Threshold>;

export type AlertConditionType =
  | "absolute_value"
  | "relative_increase"
  | "relative_decrease";
export const AlertConditionType = /*@__PURE__*/ S.String;

export interface AlertCondition {
  type?: AlertConditionType | (string & {});
}
export const AlertCondition = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.optional(AlertConditionType),
  }),
).annotate({ identifier: "AlertCondition" }) as any as S.Schema<AlertCondition>;

/** * `Firing` - Firing * `Not firing` - Not firing * `Errored` - Errored * `Snoozed` - Snoozed */
export type AlertCheckStateEnum =
  | "Firing"
  | "Not firing"
  | "Errored"
  | "Snoozed";
export const AlertCheckStateEnum = /*@__PURE__*/ S.String;

export type AlertCheckErrorMap = { [key: string]: string | undefined };
export const AlertCheckErrorMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<AlertCheckErrorMap>;

/** * `pending` - pending * `running` - running * `done` - done * `failed` - failed * `skipped` - skipped */
export type InvestigationStatusEnum =
  | "pending"
  | "running"
  | "done"
  | "failed"
  | "skipped";
export const InvestigationStatusEnum = /*@__PURE__*/ S.String;

/** * `true_positive` - true_positive * `false_positive` - false_positive * `inconclusive` - inconclusive */
export type InvestigationVerdictEnum =
  | "true_positive"
  | "false_positive"
  | "inconclusive";
export const InvestigationVerdictEnum = /*@__PURE__*/ S.String;

export interface AlertDelivery {
  /** Delivery channel: 'email' or 'hog_function' (destinations). */
  channel: string;
  /** Email address, or destination name, that received the notification. */
  target: string;
  /** Hog function ID, for destination deliveries. Null for email. */
  target_id?: string | null;
  /** Destination template: 'slack', 'discord', 'webhook', or 'teams'. Null for email. */
  template?: string | null;
  /** Delivery status. Always 'accepted', for a confirmed send. */
  status: string;
  /** When the delivery was recorded. */
  at: string | null;
  /** Ready-to-display description of the delivery, e.g. 'Email: a@example.com' or 'Slack #eng-alerts'. */
  display_label: string;
}
export const AlertDelivery = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    channel: S.String,
    target: S.String,
    target_id: S.optional(S.NullOr(S.String)),
    template: S.optional(S.NullOr(S.String)),
    status: S.String,
    at: S.NullOr(S.String),
    display_label: S.String,
  }),
).annotate({ identifier: "AlertDelivery" }) as any as S.Schema<AlertDelivery>;

/** Destinations that accepted this check's notification, one record per destination (channel, target, status, at). Null when no delivery receipt was recorded, which covers checks that notified nobody and checks predating delivery receipts. */
export type AlertCheckDeliveriesList = Array<AlertDelivery>;
export const AlertCheckDeliveriesList = /*@__PURE__*/ S.Array(
  AlertDelivery,
) as any as S.Schema<AlertCheckDeliveriesList>;

export interface AlertCheck {
  id?: string;
  created_at?: string;
  calculated_value?: number | null;
  state?: AlertCheckStateEnum;
  error?: AlertCheckErrorMap | null;
  targets_notified?: boolean;
  anomaly_scores?: unknown;
  triggered_points?: unknown;
  triggered_dates?: unknown;
  interval?: string | null;
  triggered_metadata?: unknown;
  investigation_status?: InvestigationStatusEnum | null;
  investigation_verdict?: InvestigationVerdictEnum | null;
  investigation_summary?: string | null;
  /** Short ID of the Notebook produced by the investigation agent, when the agent ran for this check. */
  investigation_notebook_short_id?: string | null;
  notification_sent_at?: string | null;
  notification_suppressed_by_agent?: boolean;
  /** Destinations that accepted this check's notification, one record per destination (channel, target, status, at). Null when no delivery receipt was recorded, which covers checks that notified nobody and checks predating delivery receipts. */
  deliveries?: AlertCheckDeliveriesList | null;
}
export const AlertCheck = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    created_at: S.optional(S.String),
    calculated_value: S.optional(S.NullOr(S.Number)),
    state: S.optional(AlertCheckStateEnum),
    error: S.optional(S.NullOr(AlertCheckErrorMap)),
    targets_notified: S.optional(S.Boolean),
    anomaly_scores: S.optional(S.Unknown),
    triggered_points: S.optional(S.Unknown),
    triggered_dates: S.optional(S.Unknown),
    interval: S.optional(S.NullOr(S.String)),
    triggered_metadata: S.optional(S.Unknown),
    investigation_status: S.optional(S.NullOr(InvestigationStatusEnum)),
    investigation_verdict: S.optional(S.NullOr(InvestigationVerdictEnum)),
    investigation_summary: S.optional(S.NullOr(S.String)),
    investigation_notebook_short_id: S.optional(S.NullOr(S.String)),
    notification_sent_at: S.optional(S.NullOr(S.String)),
    notification_suppressed_by_agent: S.optional(S.Boolean),
    deliveries: S.optional(S.NullOr(AlertCheckDeliveriesList)),
  }),
).annotate({ identifier: "AlertCheck" }) as any as S.Schema<AlertCheck>;

/** Alert check results. By default returns the last 5. Use checks_date_from and checks_date_to (e.g. '-24h', '-7d') to get checks within a time window, checks_limit to cap how many are returned (default 5, max 500), and checks_offset to skip the newest N checks for pagination (0-based). Newest checks first. Only populated on retrieve. */
export type AlertChecksList = Array<AlertCheck>;
export const AlertChecksList = /*@__PURE__*/ S.Array(
  AlertCheck,
) as any as S.Schema<AlertChecksList>;

export interface TrendsAlertConfig {
  /** When true, evaluate the current (still incomplete) time interval in addition to completed ones. */
  check_ongoing_interval?: boolean | null;
  /** Zero-based index of the series in the insight's query to monitor. */
  series_index?: number;
  type?: string;
}
export const TrendsAlertConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    check_ongoing_interval: S.optional(S.NullOr(S.Boolean)),
    series_index: S.optional(S.Number),
    type: S.optional(S.String),
  }),
).annotate({
  identifier: "TrendsAlertConfig",
}) as any as S.Schema<TrendsAlertConfig>;

export type HogQLAlertEvaluation = "last_row" | "first_row" | "any_row";
export const HogQLAlertEvaluation = /*@__PURE__*/ S.String;

export interface HogQLAlertConfig {
  /** Name of the result column to evaluate. When unset, the single numeric column is used (an error if the result has more than one numeric column). */
  column?: string | null;
  /** How to read the result rows — an explicit choice, no implicit default. */
  evaluation: HogQLAlertEvaluation | (string & {});
  /** Column whose value labels the evaluated row(s) in breach messages: every row in `any_row` mode, or the single evaluated row in `last_row`/`first_row`. When unset, the first non-evaluated column is used, falling back to the row number (any_row) or the value column name (last_row/first_row). */
  label_column?: string | null;
  type?: string;
}
export const HogQLAlertConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    column: S.optional(S.NullOr(S.String)),
    evaluation: HogQLAlertEvaluation,
    label_column: S.optional(S.NullOr(S.String)),
    type: S.optional(S.String),
  }),
).annotate({
  identifier: "HogQLAlertConfig",
}) as any as S.Schema<HogQLAlertConfig>;

export type FunnelConversionMetric =
  | "conversion_from_start"
  | "conversion_from_previous";
export const FunnelConversionMetric = /*@__PURE__*/ S.String;

export interface FunnelsAlertConfig {
  /** When true, evaluate the current (still in-progress) period; by default only completed periods are used. */
  check_ongoing_interval?: boolean | null;
  /** Zero-based step index to evaluate. Null = the last step (overall conversion). */
  funnel_step?: number | null;
  metric: FunnelConversionMetric | (string & {});
  type?: string;
}
export const FunnelsAlertConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    check_ongoing_interval: S.optional(S.NullOr(S.Boolean)),
    funnel_step: S.optional(S.NullOr(S.Number)),
    metric: FunnelConversionMetric,
    type: S.optional(S.String),
  }),
).annotate({
  identifier: "FunnelsAlertConfig",
}) as any as S.Schema<FunnelsAlertConfig>;

export interface MetricsAlertConfig {
  /** When true, anchor on the trailing (possibly still accumulating) bucket instead of the last complete one. */
  check_ongoing_interval?: boolean | null;
  type?: string;
}
export const MetricsAlertConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    check_ongoing_interval: S.optional(S.NullOr(S.Boolean)),
    type: S.optional(S.String),
  }),
).annotate({
  identifier: "MetricsAlertConfig",
}) as any as S.Schema<MetricsAlertConfig>;

/** Per-insight-kind alert config, discriminated by ``type`` — keeps the OpenAPI (and the generated frontend types and MCP tool schemas) in sync with every kind alerts support. */
export type AlertConfigUnion =
  | TrendsAlertConfig
  | HogQLAlertConfig
  | FunnelsAlertConfig
  | MetricsAlertConfig;
export const AlertConfigUnion =
  /*@__PURE__*/ S.Unknown as any as S.Schema<AlertConfigUnion>;

export interface PreprocessingConfig {
  /** Order of differencing. 0 = raw values, 1 = first-order diffs (default: 0) */
  diffs_n?: number | null;
  /** Number of lag features. 0 = none, >0 = include n lagged values (default: 0) */
  lags_n?: number | null;
  /** Moving average window size. 0 = no smoothing, >1 = smooth over n points (default: 0) */
  smooth_n?: number | null;
}
export const PreprocessingConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    diffs_n: S.optional(S.NullOr(S.Number)),
    lags_n: S.optional(S.NullOr(S.Number)),
    smooth_n: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "PreprocessingConfig",
}) as any as S.Schema<PreprocessingConfig>;

export interface ZScoreDetectorConfig {
  /** Preprocessing transforms applied before detection */
  preprocessing?: PreprocessingConfig | null;
  /** Anomaly probability threshold [0-1]. Points above this probability are flagged (default: 0.9) */
  threshold?: number | null;
  type?: string;
  /** Rolling window size for calculating mean/std (default: 30) */
  window?: number | null;
}
export const ZScoreDetectorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    preprocessing: S.optional(S.NullOr(PreprocessingConfig)),
    threshold: S.optional(S.NullOr(S.Number)),
    type: S.optional(S.String),
    window: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "ZScoreDetectorConfig",
}) as any as S.Schema<ZScoreDetectorConfig>;

export interface MADDetectorConfig {
  /** Preprocessing transforms applied before detection */
  preprocessing?: PreprocessingConfig | null;
  /** Anomaly probability threshold [0-1]. Points above this probability are flagged (default: 0.9) */
  threshold?: number | null;
  type?: string;
  /** Rolling window size for calculating median/MAD (default: 30) */
  window?: number | null;
}
export const MADDetectorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    preprocessing: S.optional(S.NullOr(PreprocessingConfig)),
    threshold: S.optional(S.NullOr(S.Number)),
    type: S.optional(S.String),
    window: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "MADDetectorConfig",
}) as any as S.Schema<MADDetectorConfig>;

export interface IQRDetectorConfig {
  /** IQR multiplier for fence calculation (default: 1.5, use 3.0 for far outliers) */
  multiplier?: number | null;
  /** Preprocessing transforms applied before detection */
  preprocessing?: PreprocessingConfig | null;
  type?: string;
  /** Rolling window size for calculating quartiles (default: 30) */
  window?: number | null;
}
export const IQRDetectorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    multiplier: S.optional(S.NullOr(S.Number)),
    preprocessing: S.optional(S.NullOr(PreprocessingConfig)),
    type: S.optional(S.String),
    window: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "IQRDetectorConfig",
}) as any as S.Schema<IQRDetectorConfig>;

export interface ThresholdDetectorConfig {
  /** Lower bound - values below this are anomalies */
  lower_bound?: number | null;
  /** Preprocessing transforms applied before detection */
  preprocessing?: PreprocessingConfig | null;
  type?: string;
  /** Upper bound - values above this are anomalies */
  upper_bound?: number | null;
}
export const ThresholdDetectorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    lower_bound: S.optional(S.NullOr(S.Number)),
    preprocessing: S.optional(S.NullOr(PreprocessingConfig)),
    type: S.optional(S.String),
    upper_bound: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "ThresholdDetectorConfig",
}) as any as S.Schema<ThresholdDetectorConfig>;

export interface ECODDetectorConfig {
  /** Preprocessing transforms applied before detection */
  preprocessing?: PreprocessingConfig | null;
  /** Anomaly probability threshold (default: 0.9) */
  threshold?: number | null;
  type?: string;
  /** Rolling window size — how many historical data points to train on (default: based on calculation interval) */
  window?: number | null;
}
export const ECODDetectorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    preprocessing: S.optional(S.NullOr(PreprocessingConfig)),
    threshold: S.optional(S.NullOr(S.Number)),
    type: S.optional(S.String),
    window: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "ECODDetectorConfig",
}) as any as S.Schema<ECODDetectorConfig>;

export type COPODDetectorConfig = ECODDetectorConfig;
export const COPODDetectorConfig = ECODDetectorConfig;

export interface IsolationForestDetectorConfig {
  /** Number of trees in the forest (default: 100) */
  n_estimators?: number | null;
  /** Preprocessing transforms applied before detection */
  preprocessing?: PreprocessingConfig | null;
  /** Anomaly probability threshold (default: 0.9) */
  threshold?: number | null;
  type?: string;
  /** Rolling window size — how many historical data points to train on (default: based on calculation interval) */
  window?: number | null;
}
export const IsolationForestDetectorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    n_estimators: S.optional(S.NullOr(S.Number)),
    preprocessing: S.optional(S.NullOr(PreprocessingConfig)),
    threshold: S.optional(S.NullOr(S.Number)),
    type: S.optional(S.String),
    window: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "IsolationForestDetectorConfig",
}) as any as S.Schema<IsolationForestDetectorConfig>;

export type Method = "largest" | "mean" | "median";
export const Method = /*@__PURE__*/ S.String;

export interface KNNDetectorConfig {
  /** Distance method: 'largest', 'mean', 'median' (default: 'largest') */
  method?: Method | (string & {}) | null;
  /** Number of neighbors to consider (default: 5) */
  n_neighbors?: number | null;
  /** Preprocessing transforms applied before detection */
  preprocessing?: PreprocessingConfig | null;
  /** Anomaly probability threshold (default: 0.9) */
  threshold?: number | null;
  type?: string;
  /** Rolling window size — how many historical data points to train on (default: based on calculation interval) */
  window?: number | null;
}
export const KNNDetectorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    method: S.optional(S.NullOr(Method)),
    n_neighbors: S.optional(S.NullOr(S.Number)),
    preprocessing: S.optional(S.NullOr(PreprocessingConfig)),
    threshold: S.optional(S.NullOr(S.Number)),
    type: S.optional(S.String),
    window: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "KNNDetectorConfig",
}) as any as S.Schema<KNNDetectorConfig>;

export interface HBOSDetectorConfig {
  /** Number of histogram bins (default: 10) */
  n_bins?: number | null;
  /** Preprocessing transforms applied before detection */
  preprocessing?: PreprocessingConfig | null;
  /** Anomaly probability threshold (default: 0.9) */
  threshold?: number | null;
  type?: string;
  /** Rolling window size — how many historical data points to train on (default: based on calculation interval) */
  window?: number | null;
}
export const HBOSDetectorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    n_bins: S.optional(S.NullOr(S.Number)),
    preprocessing: S.optional(S.NullOr(PreprocessingConfig)),
    threshold: S.optional(S.NullOr(S.Number)),
    type: S.optional(S.String),
    window: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "HBOSDetectorConfig",
}) as any as S.Schema<HBOSDetectorConfig>;

export interface LOFDetectorConfig {
  /** Number of neighbors for LOF (default: 20) */
  n_neighbors?: number | null;
  /** Preprocessing transforms applied before detection */
  preprocessing?: PreprocessingConfig | null;
  /** Anomaly probability threshold (default: 0.9) */
  threshold?: number | null;
  type?: string;
  /** Rolling window size — how many historical data points to train on (default: based on calculation interval) */
  window?: number | null;
}
export const LOFDetectorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    n_neighbors: S.optional(S.NullOr(S.Number)),
    preprocessing: S.optional(S.NullOr(PreprocessingConfig)),
    threshold: S.optional(S.NullOr(S.Number)),
    type: S.optional(S.String),
    window: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "LOFDetectorConfig",
}) as any as S.Schema<LOFDetectorConfig>;

export interface OCSVMDetectorConfig {
  /** SVM kernel type (default: "rbf") */
  kernel?: string | null;
  /** Upper bound on training errors fraction (default: 0.1) */
  nu?: number | null;
  /** Preprocessing transforms applied before detection */
  preprocessing?: PreprocessingConfig | null;
  /** Anomaly probability threshold (default: 0.9) */
  threshold?: number | null;
  type?: string;
  /** Rolling window size — how many historical data points to train on (default: based on calculation interval) */
  window?: number | null;
}
export const OCSVMDetectorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    kernel: S.optional(S.NullOr(S.String)),
    nu: S.optional(S.NullOr(S.Number)),
    preprocessing: S.optional(S.NullOr(PreprocessingConfig)),
    threshold: S.optional(S.NullOr(S.Number)),
    type: S.optional(S.String),
    window: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "OCSVMDetectorConfig",
}) as any as S.Schema<OCSVMDetectorConfig>;

export type PCADetectorConfig = ECODDetectorConfig;
export const PCADetectorConfig = ECODDetectorConfig;

export type EnsembleDetectorConfigDetectorsItem =
  | ZScoreDetectorConfig
  | MADDetectorConfig
  | IQRDetectorConfig
  | ThresholdDetectorConfig
  | ECODDetectorConfig
  | ECODDetectorConfig
  | IsolationForestDetectorConfig
  | KNNDetectorConfig
  | HBOSDetectorConfig
  | LOFDetectorConfig
  | OCSVMDetectorConfig
  | ECODDetectorConfig;
export const EnsembleDetectorConfigDetectorsItem =
  /*@__PURE__*/ S.Unknown as any as S.Schema<EnsembleDetectorConfigDetectorsItem>;

/** Sub-detector configurations (minimum 2) */
export type EnsembleDetectorConfigDetectorsList =
  Array<EnsembleDetectorConfigDetectorsItem>;
export const EnsembleDetectorConfigDetectorsList = /*@__PURE__*/ S.Array(
  EnsembleDetectorConfigDetectorsItem,
) as any as S.Schema<EnsembleDetectorConfigDetectorsList>;

export type EnsembleOperator = "and" | "or";
export const EnsembleOperator = /*@__PURE__*/ S.String;

export interface EnsembleDetectorConfig {
  /** Sub-detector configurations (minimum 2) */
  detectors?: EnsembleDetectorConfigDetectorsList;
  /** How to combine sub-detector results */
  operator?: EnsembleOperator | (string & {});
  type?: string;
}
export const EnsembleDetectorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    detectors: S.optional(EnsembleDetectorConfigDetectorsList),
    operator: S.optional(EnsembleOperator),
    type: S.optional(S.String),
  }),
).annotate({
  identifier: "EnsembleDetectorConfig",
}) as any as S.Schema<EnsembleDetectorConfig>;

/** Detector configuration types */
export type DetectorConfig =
  | EnsembleDetectorConfig
  | ZScoreDetectorConfig
  | MADDetectorConfig
  | IQRDetectorConfig
  | ThresholdDetectorConfig
  | ECODDetectorConfig
  | ECODDetectorConfig
  | IsolationForestDetectorConfig
  | KNNDetectorConfig
  | HBOSDetectorConfig
  | LOFDetectorConfig
  | OCSVMDetectorConfig
  | ECODDetectorConfig;
export const DetectorConfig =
  /*@__PURE__*/ S.Unknown as any as S.Schema<DetectorConfig>;

/** * `real_time` - real_time * `every_15_minutes` - every_15_minutes * `hourly` - hourly * `daily` - daily * `weekly` - weekly * `monthly` - monthly */
export type CalculationIntervalEnum =
  | "real_time"
  | "every_15_minutes"
  | "hourly"
  | "daily"
  | "weekly"
  | "monthly";
export const CalculationIntervalEnum = /*@__PURE__*/ S.String;

export interface AlertScheduleRestrictionWindow {
  /** Start time HH:MM (24-hour, project timezone). Inclusive. Each window must span ≥ 30 minutes on the local daily timeline (half-open [start, end)). */
  start?: string;
  /** End time HH:MM (24-hour). Exclusive (half-open interval). Each window must span ≥ 30 minutes locally. */
  end?: string;
}
export const AlertScheduleRestrictionWindow = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    start: S.optional(S.String),
    end: S.optional(S.String),
  }),
).annotate({
  identifier: "AlertScheduleRestrictionWindow",
}) as any as S.Schema<AlertScheduleRestrictionWindow>;

/** Blocked local time windows when the alert must not run. Overlapping or identical windows are merged when saved. At most five windows before normalization; empty array clears quiet hours. */
export type AlertScheduleRestrictionBlockedWindowsList =
  Array<AlertScheduleRestrictionWindow>;
export const AlertScheduleRestrictionBlockedWindowsList = /*@__PURE__*/ S.Array(
  AlertScheduleRestrictionWindow,
) as any as S.Schema<AlertScheduleRestrictionBlockedWindowsList>;

export interface AlertScheduleRestriction {
  /** Blocked local time windows when the alert must not run. Overlapping or identical windows are merged when saved. At most five windows before normalization; empty array clears quiet hours. */
  blocked_windows?: AlertScheduleRestrictionBlockedWindowsList;
}
export const AlertScheduleRestriction = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    blocked_windows: S.optional(AlertScheduleRestrictionBlockedWindowsList),
  }),
).annotate({
  identifier: "AlertScheduleRestriction",
}) as any as S.Schema<AlertScheduleRestriction>;

/** * `notify` - Notify * `suppress` - Suppress */
export type InvestigationInconclusiveActionEnum = "notify" | "suppress";
export const InvestigationInconclusiveActionEnum = /*@__PURE__*/ S.String;

export type SearchMatchTypeEnum = "exact" | "similar";
export const SearchMatchTypeEnum = /*@__PURE__*/ S.String;

export interface Alert {
  id?: string;
  created_by?: UserBasic | null;
  created_at?: string;
  /** Insight ID monitored by this alert. Note: Response returns full InsightBasicSerializer object. */
  insight?: number;
  /** Short ID of the insight monitored by this alert. */
  insight_short_id?: string;
  /** Display name of the insight monitored by this alert. */
  insight_display_name?: string;
  /** Human-readable name for the alert. */
  name?: string;
  /** User IDs to subscribe to this alert. Note: Response returns full UserBasicSerializer object. */
  subscribed_users?: AlertSubscribedUsersList;
  /** Threshold configuration with bounds and type for evaluating the alert. */
  threshold?: Threshold;
  /** Alert condition type. Determines how the value is evaluated: absolute_value, relative_increase, or relative_decrease. */
  condition?: AlertCondition | null;
  /** Current alert state: Firing, Not firing, Errored, or Snoozed. */
  state?: string;
  /** Whether the alert is actively being evaluated. */
  enabled?: boolean;
  last_notified_at?: string | null;
  last_checked_at?: string | null;
  next_check_at?: string | null;
  /** Alert check results. By default returns the last 5. Use checks_date_from and checks_date_to (e.g. '-24h', '-7d') to get checks within a time window, checks_limit to cap how many are returned (default 5, max 500), and checks_offset to skip the newest N checks for pagination (0-based). Newest checks first. Only populated on retrieve. */
  checks?: AlertChecksList;
  /** Total alert checks matching the retrieve filters (date window). Only set on alert retrieve; omitted otherwise. */
  checks_total?: number | null;
  /** Per-insight-kind alert configuration, discriminated by `type`. TrendsAlertConfig: series_index (which series to monitor) and check_ongoing_interval (whether to check the current incomplete interval). HogQLAlertConfig (SQL insights): column (which result column to evaluate, defaults to the single numeric column), evaluation ('last_row' checks the latest value of an oldest->newest query, 'first_row' checks the first value of a newest->oldest query, 'any_row' fires if any row breaches), and label_column (names the evaluated row(s) in breach messages, in every evaluation mode). FunnelsAlertConfig (funnel insights): funnel_step (the step to monitor, null for the overall last step), metric ('conversion_from_start' or 'conversion_from_previous'), and check_ongoing_interval (historical-trend funnels: also evaluate the current in-progress period). Steps funnels support only absolute_value conditions; historical-trend funnels also support relative_increase/relative_decrease (compared against the prior period). */
  config?: AlertConfigUnion | null;
  detector_config?: DetectorConfig | null;
  /** How often the alert is checked: real time (Scale+), every 15 minutes (Boost+), hourly, daily, weekly, or monthly. * `real_time` - real_time * `every_15_minutes` - every_15_minutes * `hourly` - hourly * `daily` - daily * `weekly` - weekly * `monthly` - monthly */
  calculation_interval?: CalculationIntervalEnum;
  /** Snooze the alert until this time. Pass a relative date string (e.g. '2h', '1d') or null to unsnooze. */
  snoozed_until?: string | null;
  /** Skip alert evaluation on weekends (Saturday and Sunday, local to project timezone). */
  skip_weekend?: boolean | null;
  /** Blocked local time windows (HH:MM in the project timezone). Interval is half-open [start, end): start inclusive, end exclusive. Use blocked_windows array of {start, end}. Null disables. */
  schedule_restriction?: AlertScheduleRestriction | null;
  /** The last calculated value from the most recent alert check. */
  last_value?: number | null;
  /** When enabled, an investigation agent runs on the state transition to firing and writes findings to a Notebook linked from the alert check. Only effective for detector-based (anomaly) alerts. */
  investigation_agent_enabled?: boolean;
  /** When enabled (and investigation_agent_enabled is on), notification dispatch is held until the investigation agent produces a verdict. Notifications are suppressed when the verdict is false_positive (and optionally when inconclusive). A safety-net task force-fires after a few minutes if the investigation stalls. */
  investigation_gates_notifications?: boolean;
  /** How to handle an 'inconclusive' verdict: whether gated notifications fire and whether the investigation surfaces in the Signals inbox. 'notify' is the safe default — an agent that can't be sure is itself useful signal. False positives never reach the inbox regardless of this setting. * `notify` - Notify * `suppress` - Suppress */
  investigation_inconclusive_action?: InvestigationInconclusiveActionEnum;
  /** How this row matched the `search` query parameter: `exact` (the term is a case-insensitive substring of a searched field) or `similar` (a fuzzy trigram match, returned only when no exact match exists). Null when the list is not filtered by `search`. */
  search_match_type?: SearchMatchTypeEnum | null;
}
export const Alert = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    created_by: S.optional(S.NullOr(UserBasic)),
    created_at: S.optional(S.String),
    insight: S.optional(S.Number),
    insight_short_id: S.optional(S.String),
    insight_display_name: S.optional(S.String),
    name: S.optional(S.String),
    subscribed_users: S.optional(AlertSubscribedUsersList),
    threshold: S.optional(Threshold),
    condition: S.optional(S.NullOr(AlertCondition)),
    state: S.optional(S.String),
    enabled: S.optional(S.Boolean),
    last_notified_at: S.optional(S.NullOr(S.String)),
    last_checked_at: S.optional(S.NullOr(S.String)),
    next_check_at: S.optional(S.NullOr(S.String)),
    checks: S.optional(AlertChecksList),
    checks_total: S.optional(S.NullOr(S.Number)),
    config: S.optional(S.NullOr(AlertConfigUnion)),
    detector_config: S.optional(S.NullOr(DetectorConfig)),
    calculation_interval: S.optional(CalculationIntervalEnum),
    snoozed_until: S.optional(S.NullOr(S.String)),
    skip_weekend: S.optional(S.NullOr(S.Boolean)),
    schedule_restriction: S.optional(S.NullOr(AlertScheduleRestriction)),
    last_value: S.optional(S.NullOr(S.Number)),
    investigation_agent_enabled: S.optional(S.Boolean),
    investigation_gates_notifications: S.optional(S.Boolean),
    investigation_inconclusive_action: S.optional(
      InvestigationInconclusiveActionEnum,
    ),
    search_match_type: S.optional(S.NullOr(SearchMatchTypeEnum)),
  }),
).annotate({ identifier: "Alert" }) as any as S.Schema<Alert>;

/** User IDs to subscribe to this alert. Note: Response returns full UserBasicSerializer object. */
export type AlertsCreateRequestSubscribedUsersList = Array<number>;
export const AlertsCreateRequestSubscribedUsersList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<AlertsCreateRequestSubscribedUsersList>;

export interface ThresholdInput {
  /** Optional name for the threshold. */
  name?: string;
  /** Threshold bounds and type. Includes bounds (lower/upper floats) and type (absolute or percentage). For threshold-based alerts (no detector_config), at least one of lower or upper must be set. */
  configuration?: InsightThreshold;
}
export const ThresholdInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    configuration: S.optional(InsightThreshold),
  }),
).annotate({ identifier: "ThresholdInput" }) as any as S.Schema<ThresholdInput>;

export interface CreateAlertRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Insight ID monitored by this alert. Note: Response returns full InsightBasicSerializer object. */
  insight?: number;
  /** Human-readable name for the alert. */
  name?: string;
  /** User IDs to subscribe to this alert. Note: Response returns full UserBasicSerializer object. */
  subscribed_users?: AlertsCreateRequestSubscribedUsersList;
  /** Threshold configuration with bounds and type for evaluating the alert. */
  threshold?: ThresholdInput;
  /** Alert condition type. Determines how the value is evaluated: absolute_value, relative_increase, or relative_decrease. */
  condition?: AlertCondition | null;
  /** Whether the alert is actively being evaluated. */
  enabled?: boolean;
  /** Per-insight-kind alert configuration, discriminated by `type`. TrendsAlertConfig: series_index (which series to monitor) and check_ongoing_interval (whether to check the current incomplete interval). HogQLAlertConfig (SQL insights): column (which result column to evaluate, defaults to the single numeric column), evaluation ('last_row' checks the latest value of an oldest->newest query, 'first_row' checks the first value of a newest->oldest query, 'any_row' fires if any row breaches), and label_column (names the evaluated row(s) in breach messages, in every evaluation mode). FunnelsAlertConfig (funnel insights): funnel_step (the step to monitor, null for the overall last step), metric ('conversion_from_start' or 'conversion_from_previous'), and check_ongoing_interval (historical-trend funnels: also evaluate the current in-progress period). Steps funnels support only absolute_value conditions; historical-trend funnels also support relative_increase/relative_decrease (compared against the prior period). */
  config?: AlertConfigUnion | null;
  detector_config?: DetectorConfig | null;
  /** How often the alert is checked: real time (Scale+), every 15 minutes (Boost+), hourly, daily, weekly, or monthly. * `real_time` - real_time * `every_15_minutes` - every_15_minutes * `hourly` - hourly * `daily` - daily * `weekly` - weekly * `monthly` - monthly */
  calculation_interval?: CalculationIntervalEnum | (string & {});
  /** Snooze the alert until this time. Pass a relative date string (e.g. '2h', '1d') or null to unsnooze. */
  snoozed_until?: string | null;
  /** Skip alert evaluation on weekends (Saturday and Sunday, local to project timezone). */
  skip_weekend?: boolean | null;
  /** Blocked local time windows (HH:MM in the project timezone). Interval is half-open [start, end): start inclusive, end exclusive. Use blocked_windows array of {start, end}. Null disables. */
  schedule_restriction?: AlertScheduleRestriction | null;
  /** When enabled, an investigation agent runs on the state transition to firing and writes findings to a Notebook linked from the alert check. Only effective for detector-based (anomaly) alerts. */
  investigation_agent_enabled?: boolean;
  /** When enabled (and investigation_agent_enabled is on), notification dispatch is held until the investigation agent produces a verdict. Notifications are suppressed when the verdict is false_positive (and optionally when inconclusive). A safety-net task force-fires after a few minutes if the investigation stalls. */
  investigation_gates_notifications?: boolean;
  /** How to handle an 'inconclusive' verdict: whether gated notifications fire and whether the investigation surfaces in the Signals inbox. 'notify' is the safe default — an agent that can't be sure is itself useful signal. False positives never reach the inbox regardless of this setting. * `notify` - Notify * `suppress` - Suppress */
  investigation_inconclusive_action?:
    | InvestigationInconclusiveActionEnum
    | (string & {});
}
export const CreateAlertRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    insight: S.optional(S.Number),
    name: S.optional(S.String),
    subscribed_users: S.optional(AlertsCreateRequestSubscribedUsersList),
    threshold: S.optional(ThresholdInput),
    condition: S.optional(S.NullOr(AlertCondition)),
    enabled: S.optional(S.Boolean),
    config: S.optional(S.NullOr(AlertConfigUnion)),
    detector_config: S.optional(S.NullOr(DetectorConfig)),
    calculation_interval: S.optional(CalculationIntervalEnum),
    snoozed_until: S.optional(S.NullOr(S.String)),
    skip_weekend: S.optional(S.NullOr(S.Boolean)),
    schedule_restriction: S.optional(S.NullOr(AlertScheduleRestriction)),
    investigation_agent_enabled: S.optional(S.Boolean),
    investigation_gates_notifications: S.optional(S.Boolean),
    investigation_inconclusive_action: S.optional(
      InvestigationInconclusiveActionEnum,
    ),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/projects/{project_id}/alerts/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateAlertRequest",
}) as any as S.Schema<CreateAlertRequest>;

export interface CreateAlertSimulateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Insight ID to simulate the detector on. */
  insight?: number;
  /** Detector configuration to simulate. */
  detector_config?: DetectorConfig;
  /** Zero-based index of the series to analyze (trends insights only). */
  series_index?: number;
  /** Relative date string for how far back to simulate (e.g. '-24h', '-30d', '-4w'). If not provided, uses the detector's minimum required samples. Trends insights only — a SQL query's own rows are the series. */
  date_from?: string | null;
  /** Per-insight-kind alert config. For SQL insights, selects the evaluated column and read direction (last_row/first_row) so the preview matches the alert; ignored for trends. */
  config?: AlertConfigUnion | null;
}
export const CreateAlertSimulateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    insight: S.optional(S.Number),
    detector_config: S.optional(DetectorConfig),
    series_index: S.optional(S.Number),
    date_from: S.optional(S.NullOr(S.String)),
    config: S.optional(S.NullOr(AlertConfigUnion)),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/projects/{project_id}/alerts/simulate/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateAlertSimulateRequest",
}) as any as S.Schema<CreateAlertSimulateRequest>;

/** Data values for each point. */
export type AlertSimulateResponseDataList = Array<number>;
export const AlertSimulateResponseDataList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<AlertSimulateResponseDataList>;

/** Date labels for each point. */
export type AlertSimulateResponseDatesList = Array<string>;
export const AlertSimulateResponseDatesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AlertSimulateResponseDatesList>;

/** Anomaly score for each point (null if insufficient data). */
export type AlertSimulateResponseScoresList = Array<number>;
export const AlertSimulateResponseScoresList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<AlertSimulateResponseScoresList>;

/** Indices of points flagged as anomalies. */
export type AlertSimulateResponseTriggeredIndicesList = Array<number>;
export const AlertSimulateResponseTriggeredIndicesList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<AlertSimulateResponseTriggeredIndicesList>;

/** Dates of points flagged as anomalies. */
export type AlertSimulateResponseTriggeredDatesList = Array<string>;
export const AlertSimulateResponseTriggeredDatesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AlertSimulateResponseTriggeredDatesList>;

export type AlertSimulateResponseSubDetectorScoresItemMap = {
  [key: string]: unknown | undefined;
};
export const AlertSimulateResponseSubDetectorScoresItemMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<AlertSimulateResponseSubDetectorScoresItemMap>;

/** Per-sub-detector scores for ensemble detectors. Each entry has 'type' and 'scores' fields. */
export type AlertSimulateResponseSubDetectorScoresList =
  Array<AlertSimulateResponseSubDetectorScoresItemMap>;
export const AlertSimulateResponseSubDetectorScoresList = /*@__PURE__*/ S.Array(
  AlertSimulateResponseSubDetectorScoresItemMap,
) as any as S.Schema<AlertSimulateResponseSubDetectorScoresList>;

/** Data values for each point. */
export type BreakdownSimulationResultDataList = Array<number>;
export const BreakdownSimulationResultDataList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<BreakdownSimulationResultDataList>;

/** Date labels for each point. */
export type BreakdownSimulationResultDatesList = Array<string>;
export const BreakdownSimulationResultDatesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<BreakdownSimulationResultDatesList>;

/** Anomaly score for each point. */
export type BreakdownSimulationResultScoresList = Array<number>;
export const BreakdownSimulationResultScoresList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<BreakdownSimulationResultScoresList>;

/** Indices of points flagged as anomalies. */
export type BreakdownSimulationResultTriggeredIndicesList = Array<number>;
export const BreakdownSimulationResultTriggeredIndicesList =
  /*@__PURE__*/ S.Array(
    S.Number,
  ) as any as S.Schema<BreakdownSimulationResultTriggeredIndicesList>;

/** Dates of points flagged as anomalies. */
export type BreakdownSimulationResultTriggeredDatesList = Array<string>;
export const BreakdownSimulationResultTriggeredDatesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<BreakdownSimulationResultTriggeredDatesList>;

export type BreakdownSimulationResultSubDetectorScoresItemMap = {
  [key: string]: unknown | undefined;
};
export const BreakdownSimulationResultSubDetectorScoresItemMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<BreakdownSimulationResultSubDetectorScoresItemMap>;

/** Per-sub-detector scores for ensemble detectors. */
export type BreakdownSimulationResultSubDetectorScoresList =
  Array<BreakdownSimulationResultSubDetectorScoresItemMap>;
export const BreakdownSimulationResultSubDetectorScoresList =
  /*@__PURE__*/ S.Array(
    BreakdownSimulationResultSubDetectorScoresItemMap,
  ) as any as S.Schema<BreakdownSimulationResultSubDetectorScoresList>;

export interface BreakdownSimulationResult {
  /** Breakdown value label. */
  label?: string;
  /** Data values for each point. */
  data?: BreakdownSimulationResultDataList;
  /** Date labels for each point. */
  dates?: BreakdownSimulationResultDatesList;
  /** Anomaly score for each point. */
  scores?: BreakdownSimulationResultScoresList;
  /** Indices of points flagged as anomalies. */
  triggered_indices?: BreakdownSimulationResultTriggeredIndicesList;
  /** Dates of points flagged as anomalies. */
  triggered_dates?: BreakdownSimulationResultTriggeredDatesList;
  /** Total number of data points analyzed. */
  total_points?: number;
  /** Number of anomalies detected. */
  anomaly_count?: number;
  /** Per-sub-detector scores for ensemble detectors. */
  sub_detector_scores?: BreakdownSimulationResultSubDetectorScoresList;
}
export const BreakdownSimulationResult = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    label: S.optional(S.String),
    data: S.optional(BreakdownSimulationResultDataList),
    dates: S.optional(BreakdownSimulationResultDatesList),
    scores: S.optional(BreakdownSimulationResultScoresList),
    triggered_indices: S.optional(
      BreakdownSimulationResultTriggeredIndicesList,
    ),
    triggered_dates: S.optional(BreakdownSimulationResultTriggeredDatesList),
    total_points: S.optional(S.Number),
    anomaly_count: S.optional(S.Number),
    sub_detector_scores: S.optional(
      BreakdownSimulationResultSubDetectorScoresList,
    ),
  }),
).annotate({
  identifier: "BreakdownSimulationResult",
}) as any as S.Schema<BreakdownSimulationResult>;

/** Per-breakdown-value simulation results. Present only when the insight has breakdowns (up to 25 values). */
export type AlertSimulateResponseBreakdownResultsList =
  Array<BreakdownSimulationResult>;
export const AlertSimulateResponseBreakdownResultsList = /*@__PURE__*/ S.Array(
  BreakdownSimulationResult,
) as any as S.Schema<AlertSimulateResponseBreakdownResultsList>;

export interface AlertSimulateResponse {
  /** Data values for each point. */
  data?: AlertSimulateResponseDataList;
  /** Date labels for each point. */
  dates?: AlertSimulateResponseDatesList;
  /** Anomaly score for each point (null if insufficient data). */
  scores?: AlertSimulateResponseScoresList;
  /** Indices of points flagged as anomalies. */
  triggered_indices?: AlertSimulateResponseTriggeredIndicesList;
  /** Dates of points flagged as anomalies. */
  triggered_dates?: AlertSimulateResponseTriggeredDatesList;
  /** Interval of the trends query (hour, day, week, month). */
  interval?: string | null;
  /** Total number of data points analyzed. */
  total_points?: number;
  /** Number of anomalies detected. */
  anomaly_count?: number;
  /** Per-sub-detector scores for ensemble detectors. Each entry has 'type' and 'scores' fields. */
  sub_detector_scores?: AlertSimulateResponseSubDetectorScoresList;
  /** Per-breakdown-value simulation results. Present only when the insight has breakdowns (up to 25 values). */
  breakdown_results?: AlertSimulateResponseBreakdownResultsList;
}
export const AlertSimulateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: S.optional(AlertSimulateResponseDataList),
    dates: S.optional(AlertSimulateResponseDatesList),
    scores: S.optional(AlertSimulateResponseScoresList),
    triggered_indices: S.optional(AlertSimulateResponseTriggeredIndicesList),
    triggered_dates: S.optional(AlertSimulateResponseTriggeredDatesList),
    interval: S.optional(S.NullOr(S.String)),
    total_points: S.optional(S.Number),
    anomaly_count: S.optional(S.Number),
    sub_detector_scores: S.optional(AlertSimulateResponseSubDetectorScoresList),
    breakdown_results: S.optional(AlertSimulateResponseBreakdownResultsList),
  }),
).annotate({
  identifier: "AlertSimulateResponse",
}) as any as S.Schema<AlertSimulateResponse>;

export interface CreateAlertTestDeliveryRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this alert configuration. */
  id: string;
}
export const CreateAlertTestDeliveryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/projects/{project_id}/alerts/{id}/test-delivery/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateAlertTestDeliveryRequest",
}) as any as S.Schema<CreateAlertTestDeliveryRequest>;

export interface CreateAlertTestDeliveryResponse {}
export const CreateAlertTestDeliveryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "CreateAlertTestDeliveryResponse",
}) as any as S.Schema<CreateAlertTestDeliveryResponse>;

export interface ListAlertsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Optional. Restrict results to alerts created by the user with this UUID. */
  created_by?: string;
  /** Optional. Restrict results by whether the alert uses anomaly detection. */
  has_detector?: boolean;
  /** Optional. Restrict results to alerts on this insight ID. */
  insight_id?: number;
  /** Optional. Restrict results to alerts whose insight has this tag. */
  insight_tag?: string;
  /** Number of results to return per page. */
  limit?: number;
  /** The initial index from which to return the results. */
  offset?: number;
  /** Optional. Fuzzy match against alert `name` using Postgres trigram word similarity (handles typos, transpositions, and prefix-as-you-type). Results are ordered by relevance, then creation time. Capped at 200 characters; longer queries return a 400 error. */
  search?: string;
}
export const ListAlertsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    created_by: S.optional(S.String.pipe(T.Query())),
    has_detector: S.optional(S.Boolean.pipe(T.Query())),
    insight_id: S.optional(S.Number.pipe(T.Query())),
    insight_tag: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/alerts/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListAlertsRequest",
}) as any as S.Schema<ListAlertsRequest>;

export type PaginatedAlertListResultsList = Array<Alert>;
export const PaginatedAlertListResultsList = /*@__PURE__*/ S.Array(
  Alert,
) as any as S.Schema<PaginatedAlertListResultsList>;

export interface PaginatedAlertList {
  count?: number;
  next?: string | null;
  previous?: string | null;
  results?: PaginatedAlertListResultsList;
}
export const PaginatedAlertList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    count: S.optional(S.Number),
    next: S.optional(S.NullOr(S.String)),
    previous: S.optional(S.NullOr(S.String)),
    results: S.optional(PaginatedAlertListResultsList),
  }),
).annotate({
  identifier: "PaginatedAlertList",
}) as any as S.Schema<PaginatedAlertList>;

/** User IDs to subscribe to this alert. Note: Response returns full UserBasicSerializer object. */
export type AlertsUpdateRequestSubscribedUsersList = Array<number>;
export const AlertsUpdateRequestSubscribedUsersList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<AlertsUpdateRequestSubscribedUsersList>;

export interface UpdateAlertRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this alert configuration. */
  id: string;
  /** Insight ID monitored by this alert. Note: Response returns full InsightBasicSerializer object. */
  insight?: number;
  /** Human-readable name for the alert. */
  name?: string;
  /** User IDs to subscribe to this alert. Note: Response returns full UserBasicSerializer object. */
  subscribed_users?: AlertsUpdateRequestSubscribedUsersList;
  /** Threshold configuration with bounds and type for evaluating the alert. */
  threshold?: ThresholdInput;
  /** Alert condition type. Determines how the value is evaluated: absolute_value, relative_increase, or relative_decrease. */
  condition?: AlertCondition | null;
  /** Whether the alert is actively being evaluated. */
  enabled?: boolean;
  /** Per-insight-kind alert configuration, discriminated by `type`. TrendsAlertConfig: series_index (which series to monitor) and check_ongoing_interval (whether to check the current incomplete interval). HogQLAlertConfig (SQL insights): column (which result column to evaluate, defaults to the single numeric column), evaluation ('last_row' checks the latest value of an oldest->newest query, 'first_row' checks the first value of a newest->oldest query, 'any_row' fires if any row breaches), and label_column (names the evaluated row(s) in breach messages, in every evaluation mode). FunnelsAlertConfig (funnel insights): funnel_step (the step to monitor, null for the overall last step), metric ('conversion_from_start' or 'conversion_from_previous'), and check_ongoing_interval (historical-trend funnels: also evaluate the current in-progress period). Steps funnels support only absolute_value conditions; historical-trend funnels also support relative_increase/relative_decrease (compared against the prior period). */
  config?: AlertConfigUnion | null;
  detector_config?: DetectorConfig | null;
  /** How often the alert is checked: real time (Scale+), every 15 minutes (Boost+), hourly, daily, weekly, or monthly. * `real_time` - real_time * `every_15_minutes` - every_15_minutes * `hourly` - hourly * `daily` - daily * `weekly` - weekly * `monthly` - monthly */
  calculation_interval?: CalculationIntervalEnum | (string & {});
  /** Snooze the alert until this time. Pass a relative date string (e.g. '2h', '1d') or null to unsnooze. */
  snoozed_until?: string | null;
  /** Skip alert evaluation on weekends (Saturday and Sunday, local to project timezone). */
  skip_weekend?: boolean | null;
  /** Blocked local time windows (HH:MM in the project timezone). Interval is half-open [start, end): start inclusive, end exclusive. Use blocked_windows array of {start, end}. Null disables. */
  schedule_restriction?: AlertScheduleRestriction | null;
  /** When enabled, an investigation agent runs on the state transition to firing and writes findings to a Notebook linked from the alert check. Only effective for detector-based (anomaly) alerts. */
  investigation_agent_enabled?: boolean;
  /** When enabled (and investigation_agent_enabled is on), notification dispatch is held until the investigation agent produces a verdict. Notifications are suppressed when the verdict is false_positive (and optionally when inconclusive). A safety-net task force-fires after a few minutes if the investigation stalls. */
  investigation_gates_notifications?: boolean;
  /** How to handle an 'inconclusive' verdict: whether gated notifications fire and whether the investigation surfaces in the Signals inbox. 'notify' is the safe default — an agent that can't be sure is itself useful signal. False positives never reach the inbox regardless of this setting. * `notify` - Notify * `suppress` - Suppress */
  investigation_inconclusive_action?:
    | InvestigationInconclusiveActionEnum
    | (string & {});
}
export const UpdateAlertRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    insight: S.optional(S.Number),
    name: S.optional(S.String),
    subscribed_users: S.optional(AlertsUpdateRequestSubscribedUsersList),
    threshold: S.optional(ThresholdInput),
    condition: S.optional(S.NullOr(AlertCondition)),
    enabled: S.optional(S.Boolean),
    config: S.optional(S.NullOr(AlertConfigUnion)),
    detector_config: S.optional(S.NullOr(DetectorConfig)),
    calculation_interval: S.optional(CalculationIntervalEnum),
    snoozed_until: S.optional(S.NullOr(S.String)),
    skip_weekend: S.optional(S.NullOr(S.Boolean)),
    schedule_restriction: S.optional(S.NullOr(AlertScheduleRestriction)),
    investigation_agent_enabled: S.optional(S.Boolean),
    investigation_gates_notifications: S.optional(S.Boolean),
    investigation_inconclusive_action: S.optional(
      InvestigationInconclusiveActionEnum,
    ),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/api/projects/{project_id}/alerts/{id}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateAlertRequest",
}) as any as S.Schema<UpdateAlertRequest>;

/** User IDs to subscribe to this alert. Note: Response returns full UserBasicSerializer object. */
export type AlertsPartialUpdateRequestSubscribedUsersList = Array<number>;
export const AlertsPartialUpdateRequestSubscribedUsersList =
  /*@__PURE__*/ S.Array(
    S.Number,
  ) as any as S.Schema<AlertsPartialUpdateRequestSubscribedUsersList>;

export interface UpdateAlertPartialRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this alert configuration. */
  id: string;
  /** Insight ID monitored by this alert. Note: Response returns full InsightBasicSerializer object. */
  insight?: number;
  /** Human-readable name for the alert. */
  name?: string;
  /** User IDs to subscribe to this alert. Note: Response returns full UserBasicSerializer object. */
  subscribed_users?: AlertsPartialUpdateRequestSubscribedUsersList;
  /** Threshold configuration with bounds and type for evaluating the alert. */
  threshold?: ThresholdInput;
  /** Alert condition type. Determines how the value is evaluated: absolute_value, relative_increase, or relative_decrease. */
  condition?: AlertCondition | null;
  /** Whether the alert is actively being evaluated. */
  enabled?: boolean;
  /** Per-insight-kind alert configuration, discriminated by `type`. TrendsAlertConfig: series_index (which series to monitor) and check_ongoing_interval (whether to check the current incomplete interval). HogQLAlertConfig (SQL insights): column (which result column to evaluate, defaults to the single numeric column), evaluation ('last_row' checks the latest value of an oldest->newest query, 'first_row' checks the first value of a newest->oldest query, 'any_row' fires if any row breaches), and label_column (names the evaluated row(s) in breach messages, in every evaluation mode). FunnelsAlertConfig (funnel insights): funnel_step (the step to monitor, null for the overall last step), metric ('conversion_from_start' or 'conversion_from_previous'), and check_ongoing_interval (historical-trend funnels: also evaluate the current in-progress period). Steps funnels support only absolute_value conditions; historical-trend funnels also support relative_increase/relative_decrease (compared against the prior period). */
  config?: AlertConfigUnion | null;
  detector_config?: DetectorConfig | null;
  /** How often the alert is checked: real time (Scale+), every 15 minutes (Boost+), hourly, daily, weekly, or monthly. * `real_time` - real_time * `every_15_minutes` - every_15_minutes * `hourly` - hourly * `daily` - daily * `weekly` - weekly * `monthly` - monthly */
  calculation_interval?: CalculationIntervalEnum | (string & {});
  /** Snooze the alert until this time. Pass a relative date string (e.g. '2h', '1d') or null to unsnooze. */
  snoozed_until?: string | null;
  /** Skip alert evaluation on weekends (Saturday and Sunday, local to project timezone). */
  skip_weekend?: boolean | null;
  /** Blocked local time windows (HH:MM in the project timezone). Interval is half-open [start, end): start inclusive, end exclusive. Use blocked_windows array of {start, end}. Null disables. */
  schedule_restriction?: AlertScheduleRestriction | null;
  /** When enabled, an investigation agent runs on the state transition to firing and writes findings to a Notebook linked from the alert check. Only effective for detector-based (anomaly) alerts. */
  investigation_agent_enabled?: boolean;
  /** When enabled (and investigation_agent_enabled is on), notification dispatch is held until the investigation agent produces a verdict. Notifications are suppressed when the verdict is false_positive (and optionally when inconclusive). A safety-net task force-fires after a few minutes if the investigation stalls. */
  investigation_gates_notifications?: boolean;
  /** How to handle an 'inconclusive' verdict: whether gated notifications fire and whether the investigation surfaces in the Signals inbox. 'notify' is the safe default — an agent that can't be sure is itself useful signal. False positives never reach the inbox regardless of this setting. * `notify` - Notify * `suppress` - Suppress */
  investigation_inconclusive_action?:
    | InvestigationInconclusiveActionEnum
    | (string & {});
}
export const UpdateAlertPartialRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    insight: S.optional(S.Number),
    name: S.optional(S.String),
    subscribed_users: S.optional(AlertsPartialUpdateRequestSubscribedUsersList),
    threshold: S.optional(ThresholdInput),
    condition: S.optional(S.NullOr(AlertCondition)),
    enabled: S.optional(S.Boolean),
    config: S.optional(S.NullOr(AlertConfigUnion)),
    detector_config: S.optional(S.NullOr(DetectorConfig)),
    calculation_interval: S.optional(CalculationIntervalEnum),
    snoozed_until: S.optional(S.NullOr(S.String)),
    skip_weekend: S.optional(S.NullOr(S.Boolean)),
    schedule_restriction: S.optional(S.NullOr(AlertScheduleRestriction)),
    investigation_agent_enabled: S.optional(S.Boolean),
    investigation_gates_notifications: S.optional(S.Boolean),
    investigation_inconclusive_action: S.optional(
      InvestigationInconclusiveActionEnum,
    ),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/api/projects/{project_id}/alerts/{id}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateAlertPartialRequest",
}) as any as S.Schema<UpdateAlertPartialRequest>;

export type AlertsDestroyError = Forbidden | NotFound | PosthogOpError;
export const alertsDestroy: API.OperationMethod<
  AlertsDestroyRequest,
  AlertsDestroyResponse,
  AlertsDestroyError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AlertsDestroyRequest,
  output: AlertsDestroyResponse,
  errors: [Forbidden, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AlertsRetrieveError =
  | BadRequest
  | Forbidden
  | NotFound
  | PosthogOpError;
export const alertsRetrieve: API.OperationMethod<
  AlertsRetrieveRequest,
  Alert,
  AlertsRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AlertsRetrieveRequest,
  output: Alert,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type CreateAlertError =
  | BadRequest
  | Forbidden
  | NotFound
  | PosthogOpError;
export const createAlert: API.OperationMethod<
  CreateAlertRequest,
  Alert,
  CreateAlertError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAlertRequest,
  output: Alert,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type CreateAlertSimulateError =
  | BadRequest
  | Forbidden
  | NotFound
  | PosthogOpError;
/** Simulate a detector on an insight's historical data. Read-only — no AlertCheck records are created. */
export const createAlertSimulate: API.OperationMethod<
  CreateAlertSimulateRequest,
  AlertSimulateResponse,
  CreateAlertSimulateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAlertSimulateRequest,
  output: AlertSimulateResponse,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type CreateAlertTestDeliveryError = PosthogOpError;
/** Send a synthetic test notification to subscribed users and every active destination on this alert. */
export const createAlertTestDelivery: API.OperationMethod<
  CreateAlertTestDeliveryRequest,
  CreateAlertTestDeliveryResponse,
  CreateAlertTestDeliveryError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAlertTestDeliveryRequest,
  output: CreateAlertTestDeliveryResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type ListAlertsError =
  | BadRequest
  | Forbidden
  | NotFound
  | PosthogOpError;
export const listAlerts: API.OperationMethod<
  ListAlertsRequest,
  PaginatedAlertList,
  ListAlertsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListAlertsRequest,
  output: PaginatedAlertList,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type UpdateAlertError =
  | BadRequest
  | Forbidden
  | NotFound
  | PosthogOpError;
export const updateAlert: API.OperationMethod<
  UpdateAlertRequest,
  Alert,
  UpdateAlertError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateAlertRequest,
  output: Alert,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type UpdateAlertPartialError =
  | BadRequest
  | Forbidden
  | NotFound
  | PosthogOpError;
export const updateAlertPartial: API.OperationMethod<
  UpdateAlertPartialRequest,
  Alert,
  UpdateAlertPartialError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateAlertPartialRequest,
  output: Alert,
  errors: [BadRequest, Forbidden, NotFound],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));
