// AUTO-GENERATED by scripts/generate.ts from .generated-specs (see scripts/convert.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  PosthogProtocol,
  type PosthogOpError,
  type PosthogOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { PosthogOpError, PosthogOpContext };

export interface ListCodeSandboxPricingRequest {}
export const ListCodeSandboxPricingRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "GET", uri: "/api/code/sandbox-pricing/", code: 200 }),
  ),
).annotate({
  identifier: "ListCodeSandboxPricingRequest",
}) as any as S.Schema<ListCodeSandboxPricingRequest>;

export interface ComputeRateCard {
  /** Stable identifier for this rate card. */
  version: string;
  /** Time when this rate card became effective. */
  effective_at: string;
  /** Time when this rate card stopped applying, or null while it remains current. */
  expires_at: string | null;
  /** USD charged per CPU core-second as an exact decimal string. */
  cpu_core_second_usd: string;
  /** USD charged per GiB-second of memory as an exact decimal string. */
  memory_gib_second_usd: string;
}
export const ComputeRateCard = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    version: S.String,
    effective_at: S.String,
    expires_at: S.NullOr(S.String),
    cpu_core_second_usd: S.String,
    memory_gib_second_usd: S.String,
  }),
).annotate({
  identifier: "ComputeRateCard",
}) as any as S.Schema<ComputeRateCard>;

/** Expired sandbox compute rate cards, newest first. */
export type SandboxComputePricingHistoryList = Array<ComputeRateCard>;
export const SandboxComputePricingHistoryList = /*@__PURE__*/ S.Array(
  ComputeRateCard,
) as any as S.Schema<SandboxComputePricingHistoryList>;

export interface SandboxComputePricing {
  /** Currently effective sandbox compute rate card, or null before pricing is published. */
  current: ComputeRateCard | null;
  /** Expired sandbox compute rate cards, newest first. */
  history: SandboxComputePricingHistoryList;
}
export const SandboxComputePricing = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    current: S.NullOr(ComputeRateCard),
    history: SandboxComputePricingHistoryList,
  }),
).annotate({
  identifier: "SandboxComputePricing",
}) as any as S.Schema<SandboxComputePricing>;

export type ListCodeSandboxPricingError = PosthogOpError;
/** Get sandbox compute pricing Get the current sandbox compute rate card and expired rate-card history. */
export const listCodeSandboxPricing: API.OperationMethod<
  ListCodeSandboxPricingRequest,
  SandboxComputePricing,
  ListCodeSandboxPricingError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListCodeSandboxPricingRequest,
  output: SandboxComputePricing,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));
