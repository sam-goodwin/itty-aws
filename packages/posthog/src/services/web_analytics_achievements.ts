// AUTO-GENERATED by scripts/generate.ts from .generated-specs (see scripts/convert.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  PosthogProtocol,
  type PosthogOpError,
  type PosthogOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { PosthogOpError, PosthogOpContext };

export interface WebAnalyticsAchievementsAcknowledgeCelebrationRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Track of the celebration being acknowledged. */
  track_key: string;
  /** Stage number being acknowledged, 1-5. */
  stage: number;
}
export const WebAnalyticsAchievementsAcknowledgeCelebrationRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      track_key: S.String,
      stage: S.Number,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/web_analytics_achievements/acknowledge_celebration/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "WebAnalyticsAchievementsAcknowledgeCelebrationRequest",
  }) as any as S.Schema<WebAnalyticsAchievementsAcknowledgeCelebrationRequest>;

export interface AcknowledgeCelebrationResponse {
  /** True if a matching pending celebration was cleared (idempotent). */
  acknowledged: boolean;
}
export const AcknowledgeCelebrationResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    acknowledged: S.Boolean,
  }),
).annotate({
  identifier: "AcknowledgeCelebrationResponse",
}) as any as S.Schema<AcknowledgeCelebrationResponse>;

export interface WebAnalyticsAchievementsOverviewRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
}
export const WebAnalyticsAchievementsOverviewRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/web_analytics_achievements/overview/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "WebAnalyticsAchievementsOverviewRequest",
}) as any as S.Schema<WebAnalyticsAchievementsOverviewRequest>;

/** * `user` - user * `team` - team */
export type AchievementDefinitionScopeEnum = "user" | "team";
export const AchievementDefinitionScopeEnum = /*@__PURE__*/ S.String;

export interface AchievementStage {
  /** Stage number within the track, 1-5. */
  stage: number;
  /** Stage name within the track, e.g. 'On a roll'. */
  name: string;
  /** Progress value needed to unlock this stage, resolved for the user's streak arm. */
  threshold: number;
}
export const AchievementStage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    stage: S.Number,
    name: S.String,
    threshold: S.Number,
  }),
).annotate({
  identifier: "AchievementStage",
}) as any as S.Schema<AchievementStage>;

/** The five stages of this track, in ascending threshold order. */
export type AchievementDefinitionStagesList = Array<AchievementStage>;
export const AchievementDefinitionStagesList = /*@__PURE__*/ S.Array(
  AchievementStage,
) as any as S.Schema<AchievementDefinitionStagesList>;

export interface AchievementDefinition {
  /** Stable track identifier, e.g. 'streak'. */
  key: string;
  /** Human-readable track name. */
  display_name: string;
  /** One-line description of what the track rewards. */
  description: string;
  /** Whether the track is tracked per user or per team. * `user` - user * `team` - team */
  scope: AchievementDefinitionScopeEnum;
  /** True for the streak track, whose thresholds vary by the streak-cadence experiment arm. */
  is_experiment_track: boolean;
  /** The five stages of this track, in ascending threshold order. */
  stages: AchievementDefinitionStagesList;
}
export const AchievementDefinition = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.String,
    display_name: S.String,
    description: S.String,
    scope: AchievementDefinitionScopeEnum,
    is_experiment_track: S.Boolean,
    stages: AchievementDefinitionStagesList,
  }),
).annotate({
  identifier: "AchievementDefinition",
}) as any as S.Schema<AchievementDefinition>;

/** All Wave-1 track definitions, thresholds resolved for the user's streak arm. */
export type AchievementsListResponseDefinitionsList =
  Array<AchievementDefinition>;
export const AchievementsListResponseDefinitionsList = /*@__PURE__*/ S.Array(
  AchievementDefinition,
) as any as S.Schema<AchievementsListResponseDefinitionsList>;

/** Map of unlocked stage number (as a string, '1'-'5') to the ISO timestamp it was unlocked. */
export type AchievementProgressUnlockedAtMap = {
  [key: string]: string | undefined;
};
export const AchievementProgressUnlockedAtMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<AchievementProgressUnlockedAtMap>;

export interface AchievementProgress {
  /** Track this progress row belongs to. */
  track_key: string;
  /** Highest stage unlocked so far, 0-5. */
  current_stage: number;
  /** Most recently computed progress value for the track. */
  progress_value: number;
  /** When the track was last recomputed, or null if it never has been. */
  last_computed_at: string | null;
  /** Map of unlocked stage number (as a string, '1'-'5') to the ISO timestamp it was unlocked. */
  unlocked_at: AchievementProgressUnlockedAtMap;
}
export const AchievementProgress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    track_key: S.String,
    current_stage: S.Number,
    progress_value: S.Number,
    last_computed_at: S.NullOr(S.String),
    unlocked_at: AchievementProgressUnlockedAtMap,
  }),
).annotate({
  identifier: "AchievementProgress",
}) as any as S.Schema<AchievementProgress>;

/** The requesting user's progress on per-user tracks. */
export type AchievementsListResponseUserProgressList =
  Array<AchievementProgress>;
export const AchievementsListResponseUserProgressList = /*@__PURE__*/ S.Array(
  AchievementProgress,
) as any as S.Schema<AchievementsListResponseUserProgressList>;

/** The team's progress on per-team tracks. */
export type AchievementsListResponseTeamProgressList =
  Array<AchievementProgress>;
export const AchievementsListResponseTeamProgressList = /*@__PURE__*/ S.Array(
  AchievementProgress,
) as any as S.Schema<AchievementsListResponseTeamProgressList>;

export interface PendingCelebration {
  /** Track whose stage was newly unlocked. */
  track_key: string;
  /** Newly unlocked stage number, 1-5. */
  stage: number;
  /** Name of the unlocked stage, shown in the celebration UI. */
  stage_name: string;
}
export const PendingCelebration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    track_key: S.String,
    stage: S.Number,
    stage_name: S.String,
  }),
).annotate({
  identifier: "PendingCelebration",
}) as any as S.Schema<PendingCelebration>;

/** Newly unlocked stages awaiting an in-session celebration; acknowledge each to clear it. */
export type AchievementsListResponsePendingCelebrationsList =
  Array<PendingCelebration>;
export const AchievementsListResponsePendingCelebrationsList =
  /*@__PURE__*/ S.Array(
    PendingCelebration,
  ) as any as S.Schema<AchievementsListResponsePendingCelebrationsList>;

export interface AchievementsListResponse {
  /** All Wave-1 track definitions, thresholds resolved for the user's streak arm. */
  definitions: AchievementsListResponseDefinitionsList;
  /** The requesting user's progress on per-user tracks. */
  user_progress: AchievementsListResponseUserProgressList;
  /** The team's progress on per-team tracks. */
  team_progress: AchievementsListResponseTeamProgressList;
  /** Newly unlocked stages awaiting an in-session celebration; acknowledge each to clear it. */
  pending_celebrations: AchievementsListResponsePendingCelebrationsList;
}
export const AchievementsListResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    definitions: AchievementsListResponseDefinitionsList,
    user_progress: AchievementsListResponseUserProgressList,
    team_progress: AchievementsListResponseTeamProgressList,
    pending_celebrations: AchievementsListResponsePendingCelebrationsList,
  }),
).annotate({
  identifier: "AchievementsListResponse",
}) as any as S.Schema<AchievementsListResponse>;

/** * `data` - data * `recording` - recording */
export type InteractionKindEnum = "data" | "recording";
export const InteractionKindEnum = /*@__PURE__*/ S.String;

export interface WebAnalyticsAchievementsRecordInteractionRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Which interaction counter to increment: 'data' (slicing/filtering the dashboard) or 'recording' (opening a session recording). * `data` - data * `recording` - recording */
  interaction_kind: InteractionKindEnum | (string & {});
}
export const WebAnalyticsAchievementsRecordInteractionRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      interaction_kind: InteractionKindEnum,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/web_analytics_achievements/record_interaction/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "WebAnalyticsAchievementsRecordInteractionRequest",
  }) as any as S.Schema<WebAnalyticsAchievementsRecordInteractionRequest>;

export interface RecordInteractionResponse {
  /** True once the interaction has been counted for the user. */
  recorded: boolean;
}
export const RecordInteractionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    recorded: S.Boolean,
  }),
).annotate({
  identifier: "RecordInteractionResponse",
}) as any as S.Schema<RecordInteractionResponse>;

export interface WebAnalyticsAchievementsRecordVisitRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
}
export const WebAnalyticsAchievementsRecordVisitRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/web_analytics_achievements/record_visit/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "WebAnalyticsAchievementsRecordVisitRequest",
  }) as any as S.Schema<WebAnalyticsAchievementsRecordVisitRequest>;

export interface RecordVisitResponse {
  /** True once today's visit row exists for the user. */
  recorded: boolean;
}
export const RecordVisitResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    recorded: S.Boolean,
  }),
).annotate({
  identifier: "RecordVisitResponse",
}) as any as S.Schema<RecordVisitResponse>;

export type WebAnalyticsAchievementsAcknowledgeCelebrationError =
  PosthogOpError;
/** Acknowledge an achievement celebration Clears a pending celebration for the given track and stage once the client has shown it, so it isn't celebrated again. Idempotent. */
export const webAnalyticsAchievementsAcknowledgeCelebration: API.OperationMethod<
  WebAnalyticsAchievementsAcknowledgeCelebrationRequest,
  AcknowledgeCelebrationResponse,
  WebAnalyticsAchievementsAcknowledgeCelebrationError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WebAnalyticsAchievementsAcknowledgeCelebrationRequest,
  output: AcknowledgeCelebrationResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type WebAnalyticsAchievementsOverviewError = PosthogOpError;
/** Get Web analytics achievements overview Returns the achievement track definitions (thresholds resolved for the requesting user's streak-cadence arm), the user's and team's progress, and any newly unlocked stages awaiting an in-session celebration. */
export const webAnalyticsAchievementsOverview: API.OperationMethod<
  WebAnalyticsAchievementsOverviewRequest,
  AchievementsListResponse,
  WebAnalyticsAchievementsOverviewError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WebAnalyticsAchievementsOverviewRequest,
  output: AchievementsListResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type WebAnalyticsAchievementsRecordInteractionError = PosthogOpError;
/** Record a Web analytics interaction Idempotently increments the requesting user's first-party counter for an in-product Web analytics interaction (slicing data, or opening a session recording), which drives the Explorer and Detective achievement tracks. */
export const webAnalyticsAchievementsRecordInteraction: API.OperationMethod<
  WebAnalyticsAchievementsRecordInteractionRequest,
  RecordInteractionResponse,
  WebAnalyticsAchievementsRecordInteractionError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WebAnalyticsAchievementsRecordInteractionRequest,
  output: RecordInteractionResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type WebAnalyticsAchievementsRecordVisitError = PosthogOpError;
/** Record a Web analytics visit Idempotently records that the requesting user opened Web analytics today (team-local date) and schedules a debounced achievement recompute. Intended to be called once per session. */
export const webAnalyticsAchievementsRecordVisit: API.OperationMethod<
  WebAnalyticsAchievementsRecordVisitRequest,
  RecordVisitResponse,
  WebAnalyticsAchievementsRecordVisitError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WebAnalyticsAchievementsRecordVisitRequest,
  output: RecordVisitResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));
