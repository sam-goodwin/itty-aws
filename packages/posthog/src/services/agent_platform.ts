// AUTO-GENERATED by scripts/generate.ts from .generated-specs (see scripts/convert.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  PosthogProtocol,
  type PosthogOpError,
  type PosthogOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { PosthogOpError, PosthogOpContext };

export class Conflict extends T.applyErrorMatchers(
  S.TaggedErrorClass<Conflict>()("Conflict", {
    code: S.Number,
    message: S.String,
  }),
  [{ status: 409 }],
) {}

/** * `approve` - approve * `reject` - reject */
export type DecisionEnum = "approve" | "reject" | (string & {});
export const DecisionEnum = /*@__PURE__*/ S.String;

/** Approver-edited tool arguments. Only honoured when the tool's `approval_policy.allow_edit` is `true`; otherwise the janitor returns 422. */
export type AgentApplicationsApprovalsDecideRequestEditedArgsMap = {
  [key: string]: unknown | undefined;
};
export const AgentApplicationsApprovalsDecideRequestEditedArgsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<AgentApplicationsApprovalsDecideRequestEditedArgsMap>;

export interface AgentApplicationsApprovalsDecideRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** UUID of the approval request to decide. */
  approval_id: string;
  /** The approver's decision. `approve` runs the tool platform-side with the (possibly edited) args; `reject` records a terminal rejection and wakes the session with a synthetic rejected tool_result. * `approve` - approve * `reject` - reject */
  decision: DecisionEnum;
  /** Approver-edited tool arguments. Only honoured when the tool's `approval_policy.allow_edit` is `true`; otherwise the janitor returns 422. */
  edited_args?: AgentApplicationsApprovalsDecideRequestEditedArgsMap;
  /** Free-form approver note. Surfaces in the session's synthetic tool_result so the model can communicate the reason back to the user. */
  reason?: string;
}
export const AgentApplicationsApprovalsDecideRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      approval_id: S.String.pipe(T.Label()),
      decision: DecisionEnum,
      edited_args: S.optional(
        AgentApplicationsApprovalsDecideRequestEditedArgsMap,
      ),
      reason: S.optional(S.String),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{id}/approvals/{approval_id}/decide/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsApprovalsDecideRequest",
}) as any as S.Schema<AgentApplicationsApprovalsDecideRequest>;

export interface AgentApprovalsDecideResponse {
  /** Always `true` on a successful decision. */
  ok: boolean;
  /** The approval row's new state — `approving` for approve, `rejected` for reject. */
  state: string;
}
export const AgentApprovalsDecideResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    state: S.String,
  }),
).annotate({
  identifier: "AgentApprovalsDecideResponse",
}) as any as S.Schema<AgentApprovalsDecideResponse>;

export interface AgentApplicationsApprovalsListRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  limit?: number;
  offset?: number;
  /** Filter by approval state. Comma-separated list accepted. Valid values: queued, approving, dispatched, dispatched_failed, rejected, expired. Defaults to all states. */
  state?: string;
}
export const AgentApplicationsApprovalsListRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      limit: S.optional(S.Number.pipe(T.Query())),
      offset: S.optional(S.Number.pipe(T.Query())),
      state: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{id}/approvals/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsApprovalsListRequest",
}) as any as S.Schema<AgentApplicationsApprovalsListRequest>;

/** Arguments the model proposed. Frozen at intercept time. */
export type AgentApprovalRequestProposedArgsMap = {
  [key: string]: unknown | undefined;
};
export const AgentApprovalRequestProposedArgsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<AgentApprovalRequestProposedArgsMap>;

/** Approver-edited arguments. Present iff `approval_policy.allow_edit` was true and the approver supplied edits. */
export type AgentApprovalRequestDecidedArgsMap = {
  [key: string]: unknown | undefined;
};
export const AgentApprovalRequestDecidedArgsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<AgentApprovalRequestDecidedArgsMap>;

/** Snapshot of the assistant message that emitted the call (text + thinking blocks) — what the approver sees as the model's reasoning. */
export type AgentApprovalRequestAssistantMessageMap = {
  [key: string]: unknown | undefined;
};
export const AgentApprovalRequestAssistantMessageMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<AgentApprovalRequestAssistantMessageMap>;

/** Resolved approval policy (type: principal|agent, allow_edit) at request time. */
export type AgentApprovalRequestApproverScopeMap = {
  [key: string]: unknown | undefined;
};
export const AgentApprovalRequestApproverScopeMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<AgentApprovalRequestApproverScopeMap>;

/** * `queued` - queued * `approving` - approving * `dispatched` - dispatched * `dispatched_failed` - dispatched_failed * `rejected` - rejected * `expired` - expired */
export type AgentApprovalRequestStateEnum =
  | "queued"
  | "approving"
  | "dispatched"
  | "dispatched_failed"
  | "rejected"
  | "expired"
  | (string & {});
export const AgentApprovalRequestStateEnum = /*@__PURE__*/ S.String;

/** `{result: ...}` on a successful approved dispatch, `{error: "..."}` when the tool threw. Null until the runner has finalised. */
export type AgentApprovalRequestDispatchOutcomeMap = {
  [key: string]: unknown | undefined;
};
export const AgentApprovalRequestDispatchOutcomeMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<AgentApprovalRequestDispatchOutcomeMap>;

export interface AgentApprovalRequest {
  /** Approval request UUID — stable, used in /approvals/<id>/decide. */
  id: string;
  /** UUID of the session that proposed the gated call. */
  session_id: string;
  /** UUID of the parent agent application. */
  application_id: string;
  /** Team that owns the agent. */
  team_id: number;
  /** Revision the gated call was proposed against. */
  revision_id: string;
  /** Turn number within the session that emitted the call. */
  turn: number;
  /** pi-ai ToolCall.id from the original assistant message; matched into the synthetic tool_result. */
  tool_call_id: string;
  /** Tool id the model invoked (e.g. `@posthog/team-delete`). */
  tool_name: string;
  /** Arguments the model proposed. Frozen at intercept time. */
  proposed_args: AgentApprovalRequestProposedArgsMap;
  /** Approver-edited arguments. Present iff `approval_policy.allow_edit` was true and the approver supplied edits. */
  decided_args: AgentApprovalRequestDecidedArgsMap | null;
  /** Snapshot of the assistant message that emitted the call (text + thinking blocks) — what the approver sees as the model's reasoning. */
  assistant_message: AgentApprovalRequestAssistantMessageMap;
  /** Resolved approval policy (type: principal|agent, allow_edit) at request time. */
  approver_scope: AgentApprovalRequestApproverScopeMap;
  /** Lifecycle state. `queued` = awaiting an approver; `approving` = decision landed and tool dispatch is in flight; `dispatched`/`dispatched_failed` = approved + tool ran; `rejected` = approver said no; `expired` = TTL elapsed. * `queued` - queued * `approving` - approving * `dispatched` - dispatched * `dispatched_failed` - dispatched_failed * `rejected` - rejected * `expired` - expired */
  state: AgentApprovalRequestStateEnum;
  /** UUID of the user who decided. Null while queued or expired. */
  decision_by: string | null;
  /** ISO timestamp of the decision. Null while queued. */
  decision_at: string | null;
  /** Free-form reason supplied by the approver. Optional. */
  decision_reason: string | null;
  /** `{result: ...}` on a successful approved dispatch, `{error: "..."}` when the tool threw. Null until the runner has finalised. */
  dispatch_outcome: AgentApprovalRequestDispatchOutcomeMap | null;
  /** When the model proposed the gated call. */
  created_at: string;
  /** When the queued request auto-rejects if no decision arrives. */
  expires_at: string;
}
export const AgentApprovalRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    session_id: S.String,
    application_id: S.String,
    team_id: S.Number,
    revision_id: S.String,
    turn: S.Number,
    tool_call_id: S.String,
    tool_name: S.String,
    proposed_args: AgentApprovalRequestProposedArgsMap,
    decided_args: S.NullOr(AgentApprovalRequestDecidedArgsMap),
    assistant_message: AgentApprovalRequestAssistantMessageMap,
    approver_scope: AgentApprovalRequestApproverScopeMap,
    state: AgentApprovalRequestStateEnum,
    decision_by: S.NullOr(S.String),
    decision_at: S.NullOr(S.String),
    decision_reason: S.NullOr(S.String),
    dispatch_outcome: S.NullOr(AgentApprovalRequestDispatchOutcomeMap),
    created_at: S.String,
    expires_at: S.String,
  }),
).annotate({
  identifier: "AgentApprovalRequest",
}) as any as S.Schema<AgentApprovalRequest>;

/** Approval requests for this application, newest first. */
export type AgentApplicationApprovalsListResponseResultsList =
  ReadonlyArray<AgentApprovalRequest>;
export const AgentApplicationApprovalsListResponseResultsList =
  /*@__PURE__*/ S.Array(
    AgentApprovalRequest,
  ) as any as S.Schema<AgentApplicationApprovalsListResponseResultsList>;

export interface AgentApplicationApprovalsListResponse {
  /** Approval requests for this application, newest first. */
  results: AgentApplicationApprovalsListResponseResultsList;
}
export const AgentApplicationApprovalsListResponse = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      results: AgentApplicationApprovalsListResponseResultsList,
    }),
).annotate({
  identifier: "AgentApplicationApprovalsListResponse",
}) as any as S.Schema<AgentApplicationApprovalsListResponse>;

export interface AgentApplicationsApprovalsRetrieveRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** UUID of the approval request (must belong to this application). */
  approval_id: string;
}
export const AgentApplicationsApprovalsRetrieveRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      approval_id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{id}/approvals/{approval_id}/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsApprovalsRetrieveRequest",
  }) as any as S.Schema<AgentApplicationsApprovalsRetrieveRequest>;

export interface AgentApplicationsApprovalsRetrieveResponse {}
export const AgentApplicationsApprovalsRetrieveResponse =
  /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
    identifier: "AgentApplicationsApprovalsRetrieveResponse",
  }) as any as S.Schema<AgentApplicationsApprovalsRetrieveResponse>;

export interface AgentApplicationsCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  name: string;
  /** Globally-unique URL identifier. Server-minted as an opaque random slug on create; only allowlisted first-party teams may set it explicitly. Slugs live in one global namespace (domain-mode ingress routing carries no team). */
  slug?: string;
  description?: string;
  archived?: boolean;
}
export const AgentApplicationsCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    name: S.String,
    slug: S.optional(S.String),
    description: S.optional(S.String),
    archived: S.optional(S.Boolean),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/projects/{project_id}/agent_applications/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsCreateRequest",
}) as any as S.Schema<AgentApplicationsCreateRequest>;

/** Resolved creator (id, first_name, email) from `created_by_id`, or null if unset or the user was deleted. */
export interface AgentApplicationCreatedBy {
  id?: number;
  first_name?: string;
  email?: string;
}
export const AgentApplicationCreatedBy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.Number),
    first_name: S.optional(S.String),
    email: S.optional(S.String),
  }),
).annotate({
  identifier: "AgentApplicationCreatedBy",
}) as any as S.Schema<AgentApplicationCreatedBy>;

export interface AgentApplication {
  id: string;
  team_id: number;
  name: string;
  /** Globally-unique URL identifier. Server-minted as an opaque random slug on create; only allowlisted first-party teams may set it explicitly. Slugs live in one global namespace (domain-mode ingress routing carries no team). */
  slug?: string;
  description?: string;
  live_revision: string | null;
  archived?: boolean;
  archived_at: string | null;
  created_by_id: number | null;
  /** Resolved creator (id, first_name, email) from `created_by_id`, or null if unset or the user was deleted. */
  created_by: AgentApplicationCreatedBy | null;
  created_at: string;
  updated_at: string;
  /** Public URL to paste into the Slack app dashboard under Event Subscriptions → Request URL. Computed from the agent slug and the deployment's ingress routing mode (`AGENT_INGRESS_DOMAIN_SUFFIX` in domain mode, `AGENT_INGRESS_PUBLIC_URL` in path mode). Null when no public agent-ingress URL is configured (e.g. local dev without a tunnel). */
  slack_events_url: string | null;
  /** Public URL to paste into the Slack app dashboard under Interactivity & Shortcuts → Request URL. Same source + null behaviour as `slack_events_url`. */
  slack_interactivity_url: string | null;
  /** Mode-aware base URL the agent's trigger routes hang off — append `/webhook`, `/run`, `/mcp`, etc. Domain mode: `https://<slug><suffix>`; path mode: `<public_url>/agents/<slug>`. Same source + null behaviour as `slack_events_url` (null when no public ingress URL is configured). */
  ingress_base_url: string | null;
}
export const AgentApplication = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    team_id: S.Number,
    name: S.String,
    slug: S.optional(S.String),
    description: S.optional(S.String),
    live_revision: S.NullOr(S.String),
    archived: S.optional(S.Boolean),
    archived_at: S.NullOr(S.String),
    created_by_id: S.NullOr(S.Number),
    created_by: S.NullOr(AgentApplicationCreatedBy),
    created_at: S.String,
    updated_at: S.String,
    slack_events_url: S.NullOr(S.String),
    slack_interactivity_url: S.NullOr(S.String),
    ingress_base_url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AgentApplication",
}) as any as S.Schema<AgentApplication>;

export interface AgentApplicationsDestroyRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
}
export const AgentApplicationsDestroyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/api/projects/{project_id}/agent_applications/{id}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsDestroyRequest",
}) as any as S.Schema<AgentApplicationsDestroyRequest>;

export interface AgentApplicationsDestroyResponse {}
export const AgentApplicationsDestroyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "AgentApplicationsDestroyResponse",
}) as any as S.Schema<AgentApplicationsDestroyResponse>;

export interface AgentApplicationsInvokeRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** The user message that starts the session. Required, non-empty. */
  message: string;
  /** Optional idempotency / threading key. A repeat invoke with the same external_key resumes the existing session instead of starting a new one. */
  external_key?: string;
}
export const AgentApplicationsInvokeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    message: S.String,
    external_key: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/projects/{project_id}/agent_applications/{id}/invoke/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsInvokeRequest",
}) as any as S.Schema<AgentApplicationsInvokeRequest>;

/** * `queued` - queued * `running` - running * `completed` - completed * `closed` - closed * `cancelled` - cancelled * `failed` - failed */
export type AgentSessionStateEnum =
  | "queued"
  | "running"
  | "completed"
  | "closed"
  | "cancelled"
  | "failed"
  | (string & {});
export const AgentSessionStateEnum = /*@__PURE__*/ S.String;

export interface AgentInvokeResponse {
  /** The newly-created (or resumed, if external_key matched) session id. Feed to agent-applications-send / agent-applications-listen. */
  session_id: string;
  /** Session state right after enqueue — `queued`. Poll agent-applications-listen for progress. * `queued` - queued * `running` - running * `completed` - completed * `closed` - closed * `cancelled` - cancelled * `failed` - failed */
  state: AgentSessionStateEnum;
  /** True if an existing session matched `external_key` and was resumed, rather than a new one being created. */
  resumed: boolean;
}
export const AgentInvokeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    session_id: S.String,
    state: AgentSessionStateEnum,
    resumed: S.Boolean,
  }),
).annotate({
  identifier: "AgentInvokeResponse",
}) as any as S.Schema<AgentInvokeResponse>;

export interface AgentApplicationsListRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Number of results to return per page. */
  limit?: number;
  /** The initial index from which to return the results. */
  offset?: number;
}
export const AgentApplicationsListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    limit: S.optional(S.Number.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsListRequest",
}) as any as S.Schema<AgentApplicationsListRequest>;

export type PaginatedAgentApplicationListResultsList =
  ReadonlyArray<AgentApplication>;
export const PaginatedAgentApplicationListResultsList = /*@__PURE__*/ S.Array(
  AgentApplication,
) as any as S.Schema<PaginatedAgentApplicationListResultsList>;

export interface PaginatedAgentApplicationList {
  count: number;
  next?: string | null;
  previous?: string | null;
  results: PaginatedAgentApplicationListResultsList;
}
export const PaginatedAgentApplicationList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    count: S.Number,
    next: S.optional(S.NullOr(S.String)),
    previous: S.optional(S.NullOr(S.String)),
    results: PaginatedAgentApplicationListResultsList,
  }),
).annotate({
  identifier: "PaginatedAgentApplicationList",
}) as any as S.Schema<PaginatedAgentApplicationList>;

export interface AgentApplicationsListenRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** `next_cursor` from the previous call. Omit on the first read; pass it back to summarize only what's new. */
  cursor?: number;
  /** Digest character budget (default 4000, range 1–20000). The digest is clipped to fit and `truncated` is set. */
  max_chars?: number;
  /** Session to read (must belong to this agent). */
  session_id: string;
}
export const AgentApplicationsListenRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    cursor: S.optional(S.Number.pipe(T.Query())),
    max_chars: S.optional(S.Number.pipe(T.Query())),
    session_id: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{id}/listen/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsListenRequest",
}) as any as S.Schema<AgentApplicationsListenRequest>;

export interface AgentListenResponse {
  session_id: string;
  state: AgentSessionStateEnum;
  /** Total messages in the conversation so far. */
  turns: number;
  /** Pass back as `cursor` on the next call to page forward — high-water mark of messages seen. */
  next_cursor: number;
  /** Compact, payload-free progress summary: last assistant text, one-line tool activity, state/usage. */
  digest: string;
  /** True when the digest was clipped to `max_chars` — read the full transcript via `agent-applications-sessions-retrieve`. */
  truncated: boolean;
  /** True once the current turn has finished (`completed`) or the session ended (`closed`/`cancelled`/`failed`) — stop polling. A `completed` session is still open: `agent-applications-send` to continue it. */
  done: boolean;
}
export const AgentListenResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    session_id: S.String,
    state: AgentSessionStateEnum,
    turns: S.Number,
    next_cursor: S.Number,
    digest: S.String,
    truncated: S.Boolean,
    done: S.Boolean,
  }),
).annotate({
  identifier: "AgentListenResponse",
}) as any as S.Schema<AgentListenResponse>;

export interface AgentApplicationsModelsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
}
export const AgentApplicationsModelsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/models/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsModelsRequest",
}) as any as S.Schema<AgentApplicationsModelsRequest>;

export interface AgentApplicationsPartialUpdateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  name?: string;
  /** Globally-unique URL identifier. Server-minted as an opaque random slug on create; only allowlisted first-party teams may set it explicitly. Slugs live in one global namespace (domain-mode ingress routing carries no team). */
  slug?: string;
  description?: string;
  archived?: boolean;
}
export const AgentApplicationsPartialUpdateRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      name: S.optional(S.String),
      slug: S.optional(S.String),
      description: S.optional(S.String),
      archived: S.optional(S.Boolean),
    }).pipe(
      T.Http({
        method: "PATCH",
        uri: "/api/projects/{project_id}/agent_applications/{id}/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsPartialUpdateRequest",
}) as any as S.Schema<AgentApplicationsPartialUpdateRequest>;

export type AgentApplicationsPreviewProxyRequestFormat =
  | "json"
  | "sse"
  | (string & {});
export const AgentApplicationsPreviewProxyRequestFormat =
  /*@__PURE__*/ S.String;

export interface AgentApplicationsPreviewProxyRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** Ingress sub-path under the agent slug. One of: `run`, `send`, `cancel`, `listen`. */
  rest: string;
  format?: AgentApplicationsPreviewProxyRequestFormat;
  /** Target draft revision. Must belong to this application and not be live. */
  revision_id: string;
  /** User message to deliver to the agent. Required for `run` (starts the session) and `send` (appends to it); ignored for `cancel` / `listen`. */
  message?: string;
  /** Target session id for `send` — the running session to append the message to. Omit for `run` (a fresh session is created). */
  session_id?: string;
}
export const AgentApplicationsPreviewProxyRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      rest: S.String.pipe(T.Label()),
      format: S.optional(
        AgentApplicationsPreviewProxyRequestFormat.pipe(T.Query()),
      ),
      revision_id: S.String.pipe(T.Query()),
      message: S.optional(S.String),
      session_id: S.optional(S.String),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{id}/preview-proxy/{rest}/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsPreviewProxyRequest",
}) as any as S.Schema<AgentApplicationsPreviewProxyRequest>;

export interface AgentApplicationsPreviewProxyResponse {}
export const AgentApplicationsPreviewProxyResponse = /*@__PURE__*/ S.suspend(
  () => S.Struct({}),
).annotate({
  identifier: "AgentApplicationsPreviewProxyResponse",
}) as any as S.Schema<AgentApplicationsPreviewProxyResponse>;

export type AgentApplicationsPreviewProxyGetRequestFormat =
  | "json"
  | "sse"
  | (string & {});
export const AgentApplicationsPreviewProxyGetRequestFormat =
  /*@__PURE__*/ S.String;

export interface AgentApplicationsPreviewProxyGetRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** Ingress sub-path under the agent slug. One of: `run`, `send`, `cancel`, `listen`. */
  rest: string;
  format?: AgentApplicationsPreviewProxyGetRequestFormat;
  /** Target draft revision. Must belong to this application and not be live. */
  revision_id: string;
}
export const AgentApplicationsPreviewProxyGetRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      rest: S.String.pipe(T.Label()),
      format: S.optional(
        AgentApplicationsPreviewProxyGetRequestFormat.pipe(T.Query()),
      ),
      revision_id: S.String.pipe(T.Query()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{id}/preview-proxy/{rest}/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsPreviewProxyGetRequest",
}) as any as S.Schema<AgentApplicationsPreviewProxyGetRequest>;

export interface AgentApplicationsPreviewTokenRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** Target draft revision. Must belong to this application and not be live. */
  revision_id: string;
}
export const AgentApplicationsPreviewTokenRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      revision_id: S.String.pipe(T.Query()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{id}/preview-token/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsPreviewTokenRequest",
}) as any as S.Schema<AgentApplicationsPreviewTokenRequest>;

export interface AgentApplicationPreviewTokenResponse {
  /** HS256 JWT bound to (app, rev) with a short TTL. Attach as the `x-agent-preview-token` header (POST/DELETE) or `preview_token` query param (GET, including EventSource) when calling ingress directly. */
  token: string;
  /** Token TTL in seconds from issue. Clients should refresh before this elapses. */
  expires_in: number;
  /** Slug to use in the ingress URL — `<application_slug>-<revision_uuid_hex>`. Identifies the exact revision, placed in the host (domain mode) or path (path mode) routing prefix. */
  ingress_slug: string;
  /** Per-trigger ingress URLs the caller can hit directly, derived from the revision's `spec.triggers[]`. Shape: `{<trigger_type>: {<route_name>: <absolute_url>}}`. Only includes triggers the spec actually declares. Empty when no public agent-ingress URL is configured for the active routing mode. */
  endpoints: unknown;
  /** How to attach credentials to those endpoints: preview-token header/query names, the per-trigger accepted auth modes (`trigger_modes`), and a note about the live vs preview-mode gate split. Lets the caller wire auth without grepping the ingress source. */
  auth: unknown;
  /** Server-side alternative — `/api/projects/<team>/agent_applications/<slug>/preview-proxy/<path>` mints the JWT for you. Strips caller Authorization, so it works for public-auth agents; agents with required auth need the direct endpoints above. */
  preview_proxy: unknown;
}
export const AgentApplicationPreviewTokenResponse = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      token: S.String,
      expires_in: S.Number,
      ingress_slug: S.String,
      endpoints: S.Unknown,
      auth: S.Unknown,
      preview_proxy: S.Unknown,
    }),
).annotate({
  identifier: "AgentApplicationPreviewTokenResponse",
}) as any as S.Schema<AgentApplicationPreviewTokenResponse>;

export interface AgentApplicationsPreviewTokenMintRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** Target draft revision. Must belong to this application and not be live. */
  revision_id: string;
}
export const AgentApplicationsPreviewTokenMintRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      revision_id: S.String.pipe(T.Query()),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{id}/preview-token/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsPreviewTokenMintRequest",
}) as any as S.Schema<AgentApplicationsPreviewTokenMintRequest>;

export interface AgentApplicationsRetrieveRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
}
export const AgentApplicationsRetrieveRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{id}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsRetrieveRequest",
}) as any as S.Schema<AgentApplicationsRetrieveRequest>;

export interface AgentApplicationsRevisionsAgentMdUpdateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  content: string;
}
export const AgentApplicationsRevisionsAgentMdUpdateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      content: S.String,
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/agent_md/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsAgentMdUpdateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsAgentMdUpdateRequest>;

/** * `draft` - draft * `ready` - ready * `live` - live * `archived` - archived */
export type AgentRevisionStateEnum =
  | "draft"
  | "ready"
  | "live"
  | "archived"
  | (string & {});
export const AgentRevisionStateEnum = /*@__PURE__*/ S.String;

/** One reference to a versioned skill in the llma-skill store, pinned into this agent's bundle at freeze. */
export interface SkillRef {
  /** Name of the skill in the llma-skill store to pin into this agent. Resolved at freeze to the chosen `version` and materialized into the bundle. */
  from_template: string;
  /** Folder the resolved skill is materialized under in the bundle (`skills/<alias>/`). Lowercase letters, digits, hyphens or underscores, starting and ending with a letter or digit; must be unique within the revision. */
  alias: string;
  /** Specific published version to pin. Omit to pin the store's latest version at freeze time. */
  version?: number;
}
export const SkillRef = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    from_template: S.String,
    alias: S.String,
    version: S.optional(S.Number),
  }),
).annotate({ identifier: "SkillRef" }) as any as S.Schema<SkillRef>;

/** Store-skill references for this draft, set via the `skill_refs` action and resolved into the bundle at freeze. Preserved as the authoring record on the frozen revision (and carried forward when forking a new draft); resolved provenance is stamped onto `spec.skills[].source_version_id`. */
export type AgentRevisionSkillRefsList = ReadonlyArray<SkillRef>;
export const AgentRevisionSkillRefsList = /*@__PURE__*/ S.Array(
  SkillRef,
) as any as S.Schema<AgentRevisionSkillRefsList>;

/** Resolved creator (id, first_name, email) from `created_by_id`, or null if unset or the user was deleted. */
export interface AgentRevisionCreatedBy {
  id?: number;
  first_name?: string;
  email?: string;
}
export const AgentRevisionCreatedBy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.Number),
    first_name: S.optional(S.String),
    email: S.optional(S.String),
  }),
).annotate({
  identifier: "AgentRevisionCreatedBy",
}) as any as S.Schema<AgentRevisionCreatedBy>;

export interface AgentRevision {
  id: string;
  application: string;
  parent_revision?: string | null;
  state: AgentRevisionStateEnum;
  /** Storage-prefix metadata for the bundle, e.g. `fs://my-agent/`. Optional — leave blank and the server fills `fs://<application-slug>/`. Bundles are addressed by revision id regardless, so this is only a prefix hint. */
  bundle_uri?: string;
  bundle_sha256: string | null;
  spec?: unknown;
  /** Store-skill references for this draft, set via the `skill_refs` action and resolved into the bundle at freeze. Preserved as the authoring record on the frozen revision (and carried forward when forking a new draft); resolved provenance is stamped onto `spec.skills[].source_version_id`. */
  skill_refs: AgentRevisionSkillRefsList;
  created_by_id: number | null;
  /** Resolved creator (id, first_name, email) from `created_by_id`, or null if unset or the user was deleted. */
  created_by: AgentRevisionCreatedBy | null;
  created_at: string;
  updated_at: string;
}
export const AgentRevision = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    application: S.String,
    parent_revision: S.optional(S.NullOr(S.String)),
    state: AgentRevisionStateEnum,
    bundle_uri: S.optional(S.String),
    bundle_sha256: S.NullOr(S.String),
    spec: S.optional(S.Unknown),
    skill_refs: AgentRevisionSkillRefsList,
    created_by_id: S.NullOr(S.Number),
    created_by: S.NullOr(AgentRevisionCreatedBy),
    created_at: S.String,
    updated_at: S.String,
  }),
).annotate({ identifier: "AgentRevision" }) as any as S.Schema<AgentRevision>;

export interface AgentApplicationsRevisionsArchiveCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
}
export const AgentApplicationsRevisionsArchiveCreateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/archive/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsArchiveCreateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsArchiveCreateRequest>;

export interface AgentApplicationsRevisionsBundleFileUpdateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  /** Canonical bundle path. Must be `agent.md` or `skills/<id>/SKILL.md` where `<id>` is a skill-reference alias on this revision. */
  path: string;
  /** The new file contents. For `agent.md`, written verbatim to the draft bundle. For a skill, published as a new version of the referenced store skill — shared with every agent that references it. SKILL.md frontmatter (description, license, allowed-tools, metadata) is honoured when present; body-only content carries those fields forward. */
  content: string;
}
export const AgentApplicationsRevisionsBundleFileUpdateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      path: S.String,
      content: S.String,
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/bundle/file/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsBundleFileUpdateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsBundleFileUpdateRequest>;

/** One skill entry in a bulk-import payload. Skills are store-backed: each entry publishes to (or creates) a skill in the skill store and pins a `skill_refs` entry on the draft. The optional `description` is honoured when supplied; when omitted on an existing skill, the current store description is preserved. Skill `id` must match the canonical resource-id regex used by the janitor. */
export interface ImportBundleSkill {
  /** Skill id. Lowercase letters, digits, hyphens, or underscores; must start and end with `[a-z0-9]`. */
  id: string;
  /** One-line summary shown in the skill index. Required when creating a new skill; optional when updating one. */
  description?: string;
  /** The skill's markdown body, published as a new version of the store skill. */
  body: string;
}
export const ImportBundleSkill = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    description: S.optional(S.String),
    body: S.String,
  }),
).annotate({
  identifier: "ImportBundleSkill",
}) as any as S.Schema<ImportBundleSkill>;

/** Per-skill payloads merged into the skill store by id and pinned onto the draft's skill references. When omitted, no skills are touched. */
export type AgentApplicationsRevisionsBundleImportCreateRequestSkillsList =
  ReadonlyArray<ImportBundleSkill>;
export const AgentApplicationsRevisionsBundleImportCreateRequestSkillsList =
  /*@__PURE__*/ S.Array(
    ImportBundleSkill,
  ) as any as S.Schema<AgentApplicationsRevisionsBundleImportCreateRequestSkillsList>;

export interface AgentApplicationsRevisionsBundleImportCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  /** New `agent.md` contents. When omitted, the existing agent.md is left alone. */
  agent_md?: string;
  /** Per-skill payloads merged into the skill store by id and pinned onto the draft's skill references. When omitted, no skills are touched. */
  skills?: AgentApplicationsRevisionsBundleImportCreateRequestSkillsList;
}
export const AgentApplicationsRevisionsBundleImportCreateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      agent_md: S.optional(S.String),
      skills: S.optional(
        AgentApplicationsRevisionsBundleImportCreateRequestSkillsList,
      ),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/bundle/import/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsBundleImportCreateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsBundleImportCreateRequest>;

export interface AgentApplicationsRevisionsBundleRetrieveRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
}
export const AgentApplicationsRevisionsBundleRetrieveRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/bundle/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsBundleRetrieveRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsBundleRetrieveRequest>;

export type WriteToolRequestArgsSchemaMap = {
  [key: string]: unknown | undefined;
};
export const WriteToolRequestArgsSchemaMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<WriteToolRequestArgsSchemaMap>;

/** Body shape for PUT /revisions/<id>/tools/<tool_id>/. */
export interface WriteToolRequest {
  description: string;
  args_schema: WriteToolRequestArgsSchemaMap;
  source: string;
}
export const WriteToolRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.String,
    args_schema: WriteToolRequestArgsSchemaMap,
    source: S.String,
  }),
).annotate({
  identifier: "WriteToolRequest",
}) as any as S.Schema<WriteToolRequest>;

export type AgentApplicationsRevisionsBundleUpdateRequestToolsList =
  ReadonlyArray<WriteToolRequest>;
export const AgentApplicationsRevisionsBundleUpdateRequestToolsList =
  /*@__PURE__*/ S.Array(
    WriteToolRequest,
  ) as any as S.Schema<AgentApplicationsRevisionsBundleUpdateRequestToolsList>;

export type AgentApplicationsRevisionsBundleUpdateRequestSpecMap = {
  [key: string]: unknown | undefined;
};
export const AgentApplicationsRevisionsBundleUpdateRequestSpecMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<AgentApplicationsRevisionsBundleUpdateRequestSpecMap>;

export interface AgentApplicationsRevisionsBundleUpdateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  agent_md: string;
  tools?: AgentApplicationsRevisionsBundleUpdateRequestToolsList;
  spec: AgentApplicationsRevisionsBundleUpdateRequestSpecMap;
}
export const AgentApplicationsRevisionsBundleUpdateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      agent_md: S.String,
      tools: S.optional(AgentApplicationsRevisionsBundleUpdateRequestToolsList),
      spec: AgentApplicationsRevisionsBundleUpdateRequestSpecMap,
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/bundle/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsBundleUpdateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsBundleUpdateRequest>;

export interface AgentApplicationsRevisionsCloneFromCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  source_revision_id: string;
}
export const AgentApplicationsRevisionsCloneFromCreateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      source_revision_id: S.String,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/clone_from/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsCloneFromCreateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsCloneFromCreateRequest>;

export interface AgentApplicationsRevisionsCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  parent_revision?: string | null;
  /** Storage-prefix metadata for the bundle, e.g. `fs://my-agent/`. Optional — leave blank and the server fills `fs://<application-slug>/`. Bundles are addressed by revision id regardless, so this is only a prefix hint. */
  bundle_uri?: string;
  spec?: unknown;
}
export const AgentApplicationsRevisionsCreateRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      parent_revision: S.optional(S.NullOr(S.String)),
      bundle_uri: S.optional(S.String),
      spec: S.optional(S.Unknown),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsRevisionsCreateRequest",
}) as any as S.Schema<AgentApplicationsRevisionsCreateRequest>;

export interface AgentApplicationsRevisionsCronFireCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  /** `name` of the cron trigger in `spec.triggers[]` to fire. */
  cron_name: string;
  /** Stable client-supplied id so repeated clicks of the same UI 'Fire now' button resolve to the same session id rather than firing twice. The janitor keys dedupe off `cron-manual:<rev>:<name>:<request_id>`. Omit to fire unconditionally — every call generates a fresh UUID. */
  request_id?: string | null;
}
export const AgentApplicationsRevisionsCronFireCreateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      cron_name: S.String,
      request_id: S.optional(S.NullOr(S.String)),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/cron/fire/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsCronFireCreateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsCronFireCreateRequest>;

export interface AgentRevisionCronFireResponse {
  ok: boolean;
  /** ID of the session the cron firing created (or returned, on dedupe). */
  session_id: string;
  /** ISO-8601 timestamp the firing was attributed to. */
  fired_at: string;
  /** Composed dedupe key — `cron-manual:<rev>:<name>:<request_id>`. Returned so the UI can correlate. */
  idempotency_key: string;
  /** The request id the firing used (echoed back, or freshly minted). */
  request_id: string;
}
export const AgentRevisionCronFireResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    session_id: S.String,
    fired_at: S.String,
    idempotency_key: S.String,
    request_id: S.String,
  }),
).annotate({
  identifier: "AgentRevisionCronFireResponse",
}) as any as S.Schema<AgentRevisionCronFireResponse>;

export interface AgentApplicationsRevisionsDestroyRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
}
export const AgentApplicationsRevisionsDestroyRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsRevisionsDestroyRequest",
}) as any as S.Schema<AgentApplicationsRevisionsDestroyRequest>;

export interface AgentApplicationsRevisionsDestroyResponse {}
export const AgentApplicationsRevisionsDestroyResponse =
  /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
    identifier: "AgentApplicationsRevisionsDestroyResponse",
  }) as any as S.Schema<AgentApplicationsRevisionsDestroyResponse>;

export interface AgentApplicationsRevisionsFreezeCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
}
export const AgentApplicationsRevisionsFreezeCreateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/freeze/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsFreezeCreateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsFreezeCreateRequest>;

export interface AgentApplicationsRevisionsListRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** Number of results to return per page. */
  limit?: number;
  /** The initial index from which to return the results. */
  offset?: number;
}
export const AgentApplicationsRevisionsListRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      limit: S.optional(S.Number.pipe(T.Query())),
      offset: S.optional(S.Number.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsRevisionsListRequest",
}) as any as S.Schema<AgentApplicationsRevisionsListRequest>;

export type PaginatedAgentRevisionListResultsList =
  ReadonlyArray<AgentRevision>;
export const PaginatedAgentRevisionListResultsList = /*@__PURE__*/ S.Array(
  AgentRevision,
) as any as S.Schema<PaginatedAgentRevisionListResultsList>;

export interface PaginatedAgentRevisionList {
  count: number;
  next?: string | null;
  previous?: string | null;
  results: PaginatedAgentRevisionListResultsList;
}
export const PaginatedAgentRevisionList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    count: S.Number,
    next: S.optional(S.NullOr(S.String)),
    previous: S.optional(S.NullOr(S.String)),
    results: PaginatedAgentRevisionListResultsList,
  }),
).annotate({
  identifier: "PaginatedAgentRevisionList",
}) as any as S.Schema<PaginatedAgentRevisionList>;

export interface AgentApplicationsRevisionsManifestRetrieveRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
}
export const AgentApplicationsRevisionsManifestRetrieveRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/manifest/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsManifestRetrieveRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsManifestRetrieveRequest>;

export interface AgentApplicationsRevisionsNewDraftCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  source_revision_id: string;
}
export const AgentApplicationsRevisionsNewDraftCreateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      source_revision_id: S.String,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/new_draft/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsNewDraftCreateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsNewDraftCreateRequest>;

export interface AgentApplicationsRevisionsPartialUpdateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  parent_revision?: string | null;
  /** Storage-prefix metadata for the bundle, e.g. `fs://my-agent/`. Optional — leave blank and the server fills `fs://<application-slug>/`. Bundles are addressed by revision id regardless, so this is only a prefix hint. */
  bundle_uri?: string;
  spec?: unknown;
}
export const AgentApplicationsRevisionsPartialUpdateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      parent_revision: S.optional(S.NullOr(S.String)),
      bundle_uri: S.optional(S.String),
      spec: S.optional(S.Unknown),
    }).pipe(
      T.Http({
        method: "PATCH",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsPartialUpdateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsPartialUpdateRequest>;

export interface AgentApplicationsRevisionsPromoteCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
}
export const AgentApplicationsRevisionsPromoteCreateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/promote/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsPromoteCreateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsPromoteCreateRequest>;

export interface AgentApplicationsRevisionsRetrieveRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
}
export const AgentApplicationsRevisionsRetrieveRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsRetrieveRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsRetrieveRequest>;

export type AgentApplicationsRevisionsSetEnvCreateRequestEnvMap = {
  [key: string]: string | undefined;
};
export const AgentApplicationsRevisionsSetEnvCreateRequestEnvMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.String,
  ) as any as S.Schema<AgentApplicationsRevisionsSetEnvCreateRequestEnvMap>;

export interface AgentApplicationsRevisionsSetEnvCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  env: AgentApplicationsRevisionsSetEnvCreateRequestEnvMap;
}
export const AgentApplicationsRevisionsSetEnvCreateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      env: AgentApplicationsRevisionsSetEnvCreateRequestEnvMap,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/set_env/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsSetEnvCreateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsSetEnvCreateRequest>;

/** The complete set of store-skill references for this draft; replaces any existing references. */
export type AgentApplicationsRevisionsSkillRefsUpdateRequestSkillRefsList =
  ReadonlyArray<SkillRef>;
export const AgentApplicationsRevisionsSkillRefsUpdateRequestSkillRefsList =
  /*@__PURE__*/ S.Array(
    SkillRef,
  ) as any as S.Schema<AgentApplicationsRevisionsSkillRefsUpdateRequestSkillRefsList>;

export interface AgentApplicationsRevisionsSkillRefsUpdateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  /** The complete set of store-skill references for this draft; replaces any existing references. */
  skill_refs: AgentApplicationsRevisionsSkillRefsUpdateRequestSkillRefsList;
}
export const AgentApplicationsRevisionsSkillRefsUpdateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      skill_refs: AgentApplicationsRevisionsSkillRefsUpdateRequestSkillRefsList,
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/skill_refs/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsSkillRefsUpdateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsSkillRefsUpdateRequest>;

export interface AgentApplicationsRevisionsSlackManifestRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
}
export const AgentApplicationsRevisionsSlackManifestRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/slack_manifest/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsSlackManifestRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsSlackManifestRequest>;

/** Reminders the manifest can't enforce (e.g. invite the bot to its channels). */
export type AgentRevisionSlackManifestResponseNotesList = ReadonlyArray<string>;
export const AgentRevisionSlackManifestResponseNotesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<AgentRevisionSlackManifestResponseNotesList>;

export interface AgentRevisionSlackManifestResponse {
  revision_id: string;
  /** Slack app manifest (JSON) ready to paste into https://api.slack.com/apps?new_app=1 → 'From an app manifest'. Scopes and event subscriptions are derived from the agent's slack trigger config + tools. */
  manifest: unknown;
  /** Reminders the manifest can't enforce (e.g. invite the bot to its channels). */
  notes: AgentRevisionSlackManifestResponseNotesList;
  /** The Event Subscriptions Request URL baked into the manifest. */
  events_url: string | null;
  /** The Interactivity Request URL (used by approval-gated tools). */
  interactivity_url: string | null;
}
export const AgentRevisionSlackManifestResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    revision_id: S.String,
    manifest: S.Unknown,
    notes: AgentRevisionSlackManifestResponseNotesList,
    events_url: S.NullOr(S.String),
    interactivity_url: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AgentRevisionSlackManifestResponse",
}) as any as S.Schema<AgentRevisionSlackManifestResponse>;

export type AgentApplicationsRevisionsSpecUpdateRequestSpecMap = {
  [key: string]: unknown | undefined;
};
export const AgentApplicationsRevisionsSpecUpdateRequestSpecMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<AgentApplicationsRevisionsSpecUpdateRequestSpecMap>;

export interface AgentApplicationsRevisionsSpecUpdateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  spec: AgentApplicationsRevisionsSpecUpdateRequestSpecMap;
}
export const AgentApplicationsRevisionsSpecUpdateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      spec: AgentApplicationsRevisionsSpecUpdateRequestSpecMap,
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/spec/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsSpecUpdateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsSpecUpdateRequest>;

export interface AgentApplicationsRevisionsSystemPromptRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
}
export const AgentApplicationsRevisionsSystemPromptRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/system_prompt/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsSystemPromptRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsSystemPromptRequest>;

export interface AgentRevisionSystemPromptResponse {
  /** UUID of the revision the prompt was rendered for. */
  revision_id: string;
  /** Active framework preamble version. Bumps when the platform's `# Platform guidance` content changes meaningfully (decision rules, sections renamed, behavioural defaults flipped). Authors can pin to a specific version via `spec.framework_prompt.version_pin`. */
  framework_prompt_version: number;
  /** Fully-assembled system prompt the runner would pass to pi-ai for a session against this revision. Concatenates the platform framework preamble, the bundle's `agent.md`, and the skills index. Inspect before promotion to confirm the model will see what you expect. */
  system_prompt: string;
}
export const AgentRevisionSystemPromptResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    revision_id: S.String,
    framework_prompt_version: S.Number,
    system_prompt: S.String,
  }),
).annotate({
  identifier: "AgentRevisionSystemPromptResponse",
}) as any as S.Schema<AgentRevisionSystemPromptResponse>;

export interface AgentApplicationsRevisionsToolsDestroyRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  tool_id: string;
}
export const AgentApplicationsRevisionsToolsDestroyRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      tool_id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/tools/{tool_id}/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsToolsDestroyRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsToolsDestroyRequest>;

export interface AgentApplicationsRevisionsToolsDestroyResponse {}
export const AgentApplicationsRevisionsToolsDestroyResponse =
  /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
    identifier: "AgentApplicationsRevisionsToolsDestroyResponse",
  }) as any as S.Schema<AgentApplicationsRevisionsToolsDestroyResponse>;

/** Optional `{secret_name → placeholder_string}` map. The string is returned verbatim by `ctx.secrets.ref(name)` inside the tool. The real secret value never enters the sandbox. */
export type AgentApplicationsRevisionsToolsDryRunCreateRequestMockSecretsMap = {
  [key: string]: string | undefined;
};
export const AgentApplicationsRevisionsToolsDryRunCreateRequestMockSecretsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.String,
  ) as any as S.Schema<AgentApplicationsRevisionsToolsDryRunCreateRequestMockSecretsMap>;

export interface AgentApplicationsRevisionsToolsDryRunCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  tool_id: string;
  /** Synthetic args the tool's `actions.default` is called with. Free-form JSON; the sandbox doesn't validate against the tool's `args_schema` — that's the author's responsibility to keep in sync. */
  args: unknown;
  /** Optional `{secret_name → placeholder_string}` map. The string is returned verbatim by `ctx.secrets.ref(name)` inside the tool. The real secret value never enters the sandbox. */
  mock_secrets?: AgentApplicationsRevisionsToolsDryRunCreateRequestMockSecretsMap;
}
export const AgentApplicationsRevisionsToolsDryRunCreateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      tool_id: S.String.pipe(T.Label()),
      args: S.Unknown,
      mock_secrets: S.optional(
        AgentApplicationsRevisionsToolsDryRunCreateRequestMockSecretsMap,
      ),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/tools/{tool_id}/dry_run/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsToolsDryRunCreateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsToolsDryRunCreateRequest>;

export interface AgentRevisionDryRunToolError {
  /** Stable error code. `sandbox_acquire_failed` — the platform could not start a sandbox (infrastructure issue, not tool code). `sandbox_invoke_failed` — the sandbox started but the invoke threw uncaught (problem in the tool body, or a runtime error). Dispatcher-side codes come through on `ok:false` invoke results: `timeout`, `secret_not_provisioned`, `action_not_found`, `tool_not_found`. */
  code: string;
  /** One-line human-readable detail. */
  message: string;
}
export const AgentRevisionDryRunToolError = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    code: S.String,
    message: S.String,
  }),
).annotate({
  identifier: "AgentRevisionDryRunToolError",
}) as any as S.Schema<AgentRevisionDryRunToolError>;

export interface AgentRevisionDryRunToolResponse {
  /** True when the tool's `actions.default` returned without throwing. False when the tool threw or the sandbox rejected the invocation (the structured `error` describes which). */
  ok: boolean;
  /** Echo of the tool id from the URL. */
  tool_id: string;
  /** Present on success — the value the tool's `actions.default` returned. */
  result?: unknown;
  error?: AgentRevisionDryRunToolError;
  /** Wall-clock duration in milliseconds, measured from sandbox acquire to after release. Captured consistently across success, tool-throw, and acquire-failure paths so authors can compare timings between calls. Always present. */
  duration_ms: number;
}
export const AgentRevisionDryRunToolResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    tool_id: S.String,
    result: S.optional(S.Unknown),
    error: S.optional(AgentRevisionDryRunToolError),
    duration_ms: S.Number,
  }),
).annotate({
  identifier: "AgentRevisionDryRunToolResponse",
}) as any as S.Schema<AgentRevisionDryRunToolResponse>;

export type AgentApplicationsRevisionsToolsUpdateRequestArgsSchemaMap = {
  [key: string]: unknown | undefined;
};
export const AgentApplicationsRevisionsToolsUpdateRequestArgsSchemaMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<AgentApplicationsRevisionsToolsUpdateRequestArgsSchemaMap>;

export interface AgentApplicationsRevisionsToolsUpdateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  tool_id: string;
  description: string;
  args_schema: AgentApplicationsRevisionsToolsUpdateRequestArgsSchemaMap;
  source: string;
}
export const AgentApplicationsRevisionsToolsUpdateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      tool_id: S.String.pipe(T.Label()),
      description: S.String,
      args_schema: AgentApplicationsRevisionsToolsUpdateRequestArgsSchemaMap,
      source: S.String,
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/tools/{tool_id}/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsToolsUpdateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsToolsUpdateRequest>;

export interface AgentApplicationsRevisionsUpdateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  parent_revision?: string | null;
  /** Storage-prefix metadata for the bundle, e.g. `fs://my-agent/`. Optional — leave blank and the server fills `fs://<application-slug>/`. Bundles are addressed by revision id regardless, so this is only a prefix hint. */
  bundle_uri?: string;
  spec?: unknown;
}
export const AgentApplicationsRevisionsUpdateRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      parent_revision: S.optional(S.NullOr(S.String)),
      bundle_uri: S.optional(S.String),
      spec: S.optional(S.Unknown),
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsRevisionsUpdateRequest",
}) as any as S.Schema<AgentApplicationsRevisionsUpdateRequest>;

export interface AgentApplicationsRevisionsValidateCreateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
}
export const AgentApplicationsRevisionsValidateCreateRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      application_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/validate/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsRevisionsValidateCreateRequest",
  }) as any as S.Schema<AgentApplicationsRevisionsValidateCreateRequest>;

export interface AgentRevisionValidationError {
  code: string;
  message: string;
  pointer: string;
}
export const AgentRevisionValidationError = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    code: S.String,
    message: S.String,
    pointer: S.String,
  }),
).annotate({
  identifier: "AgentRevisionValidationError",
}) as any as S.Schema<AgentRevisionValidationError>;

export type AgentRevisionValidateResponseErrorsList =
  ReadonlyArray<AgentRevisionValidationError>;
export const AgentRevisionValidateResponseErrorsList = /*@__PURE__*/ S.Array(
  AgentRevisionValidationError,
) as any as S.Schema<AgentRevisionValidateResponseErrorsList>;

export type AgentRevisionValidateResponseResolvedNativesList =
  ReadonlyArray<string>;
export const AgentRevisionValidateResponseResolvedNativesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<AgentRevisionValidateResponseResolvedNativesList>;

export interface AgentRevisionValidateResponse {
  ok: boolean;
  revision_id: string;
  revision_state: string;
  errors: AgentRevisionValidateResponseErrorsList;
  resolved_natives: AgentRevisionValidateResponseResolvedNativesList;
}
export const AgentRevisionValidateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    revision_id: S.String,
    revision_state: S.String,
    errors: AgentRevisionValidateResponseErrorsList,
    resolved_natives: AgentRevisionValidateResponseResolvedNativesList,
  }),
).annotate({
  identifier: "AgentRevisionValidateResponse",
}) as any as S.Schema<AgentRevisionValidateResponse>;

export interface AgentApplicationsSendRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** The session to append to (returned by agent-applications-invoke). Must belong to this agent. */
  session_id: string;
  /** The user message to append. Required, non-empty. */
  message: string;
}
export const AgentApplicationsSendRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    session_id: S.String,
    message: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/projects/{project_id}/agent_applications/{id}/send/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsSendRequest",
}) as any as S.Schema<AgentApplicationsSendRequest>;

export interface AgentSendResponse {
  /** Session state after the message was appended — `queued` (a new turn will run). * `queued` - queued * `running` - running * `completed` - completed * `closed` - closed * `cancelled` - cancelled * `failed` - failed */
  state: AgentSessionStateEnum;
}
export const AgentSendResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    state: AgentSessionStateEnum,
  }),
).annotate({
  identifier: "AgentSendResponse",
}) as any as S.Schema<AgentSendResponse>;

export interface AgentApplicationsSessionLogsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** UUID of the session whose logs to fetch. */
  session_id: string;
  /** Only return entries after this ISO 8601 timestamp. */
  after?: string;
  /** Only return entries before this ISO 8601 timestamp. */
  before?: string;
  /** Filter logs to a specific execution instance. */
  instance_id?: string;
  /** Comma-separated log levels to include, e.g. 'WARN,ERROR'. Valid levels: DEBUG, LOG, INFO, WARN, ERROR. */
  level?: string;
  /** Maximum number of log entries to return (1-500, default 50). */
  limit?: number;
  /** Case-insensitive substring search across log messages. */
  search?: string;
}
export const AgentApplicationsSessionLogsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    session_id: S.String.pipe(T.Label()),
    after: S.optional(S.String.pipe(T.Query())),
    before: S.optional(S.String.pipe(T.Query())),
    instance_id: S.optional(S.String.pipe(T.Query())),
    level: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{id}/sessions/{session_id}/logs/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsSessionLogsRequest",
}) as any as S.Schema<AgentApplicationsSessionLogsRequest>;

export interface LogEntry {
  log_source_id: string;
  instance_id: string;
  timestamp: string;
  level: string;
  message: string;
}
export const LogEntry = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    log_source_id: S.String,
    instance_id: S.String,
    timestamp: S.String,
    level: S.String,
    message: S.String,
  }),
).annotate({ identifier: "LogEntry" }) as any as S.Schema<LogEntry>;

export type AgentApplicationSessionLogsResponseResultsList =
  ReadonlyArray<LogEntry>;
export const AgentApplicationSessionLogsResponseResultsList =
  /*@__PURE__*/ S.Array(
    LogEntry,
  ) as any as S.Schema<AgentApplicationSessionLogsResponseResultsList>;

export interface AgentApplicationSessionLogsResponse {
  results: AgentApplicationSessionLogsResponseResultsList;
}
export const AgentApplicationSessionLogsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: AgentApplicationSessionLogsResponseResultsList,
  }),
).annotate({
  identifier: "AgentApplicationSessionLogsResponse",
}) as any as S.Schema<AgentApplicationSessionLogsResponse>;

export interface AgentApplicationsSessionsListRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** ISO datetime — return sessions with created_at >= this. */
  created_after?: string;
  /** ISO datetime — return sessions with created_at <= this. */
  created_before?: string;
  limit?: number;
  offset?: number;
  /** Only return sessions started against this specific revision. */
  revision_id?: string;
  /** Filter by session state. Comma-separated list accepted (e.g. `completed,failed`). Valid values: queued, running, completed, closed, cancelled, failed. */
  state?: string;
}
export const AgentApplicationsSessionsListRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      created_after: S.optional(S.String.pipe(T.Query())),
      created_before: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      offset: S.optional(S.Number.pipe(T.Query())),
      revision_id: S.optional(S.String.pipe(T.Query())),
      state: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{id}/sessions/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsSessionsListRequest",
}) as any as S.Schema<AgentApplicationsSessionsListRequest>;

export interface AgentSessionUsageTotal {
  tokens_in: number;
  tokens_out: number;
  cache_read: number;
  cache_write: number;
  cost_input: number;
  cost_output: number;
  cost_cache_read: number;
  cost_cache_write: number;
  cost_total: number;
}
export const AgentSessionUsageTotal = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tokens_in: S.Number,
    tokens_out: S.Number,
    cache_read: S.Number,
    cache_write: S.Number,
    cost_input: S.Number,
    cost_output: S.Number,
    cost_cache_read: S.Number,
    cost_cache_write: S.Number,
    cost_total: S.Number,
  }),
).annotate({
  identifier: "AgentSessionUsageTotal",
}) as any as S.Schema<AgentSessionUsageTotal>;

/** * `anonymous` - anonymous * `service` - service * `internal` - internal * `shared_secret` - shared_secret * `slack` - slack */
export type AgentSessionPrincipalKindEnum =
  | "anonymous"
  | "service"
  | "internal"
  | "shared_secret"
  | "slack"
  | (string & {});
export const AgentSessionPrincipalKindEnum = /*@__PURE__*/ S.String;

export interface AgentSessionPrincipal {
  /** What kind of principal authenticated the session start. * `anonymous` - anonymous * `service` - service * `internal` - internal * `shared_secret` - shared_secret * `slack` - slack */
  kind: AgentSessionPrincipalKindEnum;
  /** Stable identifier for the principal (PAT id, slack user id, etc). Absent for anonymous sessions. */
  id?: string;
  /** Team the principal belongs to. Absent for anonymous sessions. */
  team_id?: number;
}
export const AgentSessionPrincipal = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    kind: AgentSessionPrincipalKindEnum,
    id: S.optional(S.String),
    team_id: S.optional(S.Number),
  }),
).annotate({
  identifier: "AgentSessionPrincipal",
}) as any as S.Schema<AgentSessionPrincipal>;

/** Trigger-specific metadata stamped at session creation. Discriminated on `kind`: chat | slack | cron | webhook | mcp. The Zod source of truth is `agent-shared/src/runtime/trigger-metadata.ts`; the node side validates and strips unknown keys at the persistence boundary, so consumers can trust `kind` and per-kind fields. TODO: narrow this DictField to a polymorphic serializer mirroring the union (needs `hogli build:openapi`). */
export type AgentSessionSummaryTriggerMetadataMap = {
  [key: string]: unknown | undefined;
};
export const AgentSessionSummaryTriggerMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<AgentSessionSummaryTriggerMetadataMap>;

export interface AgentSessionSummary {
  usage_total: AgentSessionUsageTotal;
  principal: AgentSessionPrincipal | null;
  id: string;
  application_id: string;
  revision_id: string;
  state: AgentSessionStateEnum;
  external_key: string | null;
  /** Trigger-specific metadata stamped at session creation. Discriminated on `kind`: chat | slack | cron | webhook | mcp. The Zod source of truth is `agent-shared/src/runtime/trigger-metadata.ts`; the node side validates and strips unknown keys at the persistence boundary, so consumers can trust `kind` and per-kind fields. TODO: narrow this DictField to a polymorphic serializer mirroring the union (needs `hogli build:openapi`). */
  trigger_metadata?: AgentSessionSummaryTriggerMetadataMap | null;
  /** Count of messages in the conversation — the full transcript ships on the detail endpoint. */
  turns: number;
  /** Last assistant text (~120 chars). Null for sessions with no assistant turns yet. */
  preview: string | null;
  retry_count: number;
  created_at: string;
  updated_at: string;
}
export const AgentSessionSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    usage_total: AgentSessionUsageTotal,
    principal: S.NullOr(AgentSessionPrincipal),
    id: S.String,
    application_id: S.String,
    revision_id: S.String,
    state: AgentSessionStateEnum,
    external_key: S.NullOr(S.String),
    trigger_metadata: S.optional(
      S.NullOr(AgentSessionSummaryTriggerMetadataMap),
    ),
    turns: S.Number,
    preview: S.NullOr(S.String),
    retry_count: S.Number,
    created_at: S.String,
    updated_at: S.String,
  }),
).annotate({
  identifier: "AgentSessionSummary",
}) as any as S.Schema<AgentSessionSummary>;

export type AgentApplicationSessionsListResponseResultsList =
  ReadonlyArray<AgentSessionSummary>;
export const AgentApplicationSessionsListResponseResultsList =
  /*@__PURE__*/ S.Array(
    AgentSessionSummary,
  ) as any as S.Schema<AgentApplicationSessionsListResponseResultsList>;

export interface AgentApplicationSessionsListResponse {
  results: AgentApplicationSessionsListResponseResultsList;
  /** Total matching sessions before pagination. */
  count: number;
}
export const AgentApplicationSessionsListResponse = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      results: AgentApplicationSessionsListResponseResultsList,
      count: S.Number,
    }),
).annotate({
  identifier: "AgentApplicationSessionsListResponse",
}) as any as S.Schema<AgentApplicationSessionsListResponse>;

export interface AgentApplicationsSessionsRetrieveRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** UUID of the session to fetch (must belong to this application). */
  session_id: string;
  /** If set, return only the most recent N messages from the conversation. `usage_total` is still computed over the full session — only the transcript is trimmed. The response includes `conversation_trimmed: true` and `conversation_total_turns` so the caller knows how much was hidden. */
  last_n?: number;
}
export const AgentApplicationsSessionsRetrieveRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      session_id: S.String.pipe(T.Label()),
      last_n: S.optional(S.Number.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/api/projects/{project_id}/agent_applications/{id}/sessions/{session_id}/",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AgentApplicationsSessionsRetrieveRequest",
}) as any as S.Schema<AgentApplicationsSessionsRetrieveRequest>;

/** Trigger-specific metadata stamped at session creation. Discriminated on `kind`: chat | slack | cron | webhook | mcp. The Zod source of truth is `agent-shared/src/runtime/trigger-metadata.ts`; the node side validates and strips unknown keys at the persistence boundary, so consumers can trust `kind` and per-kind fields. TODO: narrow this DictField to a polymorphic serializer mirroring the union (needs `hogli build:openapi`). */
export type AgentApplicationSessionsRetrieveResponseTriggerMetadataMap = {
  [key: string]: unknown | undefined;
};
export const AgentApplicationSessionsRetrieveResponseTriggerMetadataMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<AgentApplicationSessionsRetrieveResponseTriggerMetadataMap>;

/** * `user` - user */
export type AgentConversationUserMessageRoleEnum = "user" | (string & {});
export const AgentConversationUserMessageRoleEnum = /*@__PURE__*/ S.String;

export interface AgentConversationUserMessage {
  role: AgentConversationUserMessageRoleEnum;
  /** String shorthand, or array of {type:'text'|'image', ...} parts. */
  content: unknown;
  /** Epoch milliseconds. */
  timestamp: number;
}
export const AgentConversationUserMessage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    role: AgentConversationUserMessageRoleEnum,
    content: S.Unknown,
    timestamp: S.Number,
  }),
).annotate({
  identifier: "AgentConversationUserMessage",
}) as any as S.Schema<AgentConversationUserMessage>;

/** * `assistant` - assistant */
export type AgentConversationAssistantMessageRoleEnum =
  | "assistant"
  | (string & {});
export const AgentConversationAssistantMessageRoleEnum = /*@__PURE__*/ S.String;

/** Array of text/thinking/toolCall parts. */
export type AgentConversationAssistantMessageContentList =
  ReadonlyArray<unknown>;
export const AgentConversationAssistantMessageContentList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<AgentConversationAssistantMessageContentList>;

export type AgentConversationAssistantMessageUsageMap = {
  [key: string]: unknown | undefined;
};
export const AgentConversationAssistantMessageUsageMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<AgentConversationAssistantMessageUsageMap>;

/** * `stop` - stop * `length` - length * `toolUse` - toolUse * `error` - error * `aborted` - aborted */
export type StopReasonEnum =
  | "stop"
  | "length"
  | "toolUse"
  | "error"
  | "aborted"
  | (string & {});
export const StopReasonEnum = /*@__PURE__*/ S.String;

export interface AgentConversationAssistantMessage {
  role: AgentConversationAssistantMessageRoleEnum;
  /** Array of text/thinking/toolCall parts. */
  content: AgentConversationAssistantMessageContentList;
  /** Epoch milliseconds. */
  timestamp: number;
  api?: string;
  provider?: string;
  model?: string;
  usage?: AgentConversationAssistantMessageUsageMap;
  stopReason?: StopReasonEnum;
  errorMessage?: string;
}
export const AgentConversationAssistantMessage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    role: AgentConversationAssistantMessageRoleEnum,
    content: AgentConversationAssistantMessageContentList,
    timestamp: S.Number,
    api: S.optional(S.String),
    provider: S.optional(S.String),
    model: S.optional(S.String),
    usage: S.optional(AgentConversationAssistantMessageUsageMap),
    stopReason: S.optional(StopReasonEnum),
    errorMessage: S.optional(S.String),
  }),
).annotate({
  identifier: "AgentConversationAssistantMessage",
}) as any as S.Schema<AgentConversationAssistantMessage>;

/** * `toolResult` - toolResult */
export type AgentConversationToolResultMessageRoleEnum =
  | "toolResult"
  | (string & {});
export const AgentConversationToolResultMessageRoleEnum =
  /*@__PURE__*/ S.String;

/** Array of {type:'text'|'image', ...} parts. */
export type AgentConversationToolResultMessageContentList =
  ReadonlyArray<unknown>;
export const AgentConversationToolResultMessageContentList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<AgentConversationToolResultMessageContentList>;

export interface AgentConversationToolResultMessage {
  role: AgentConversationToolResultMessageRoleEnum;
  toolCallId: string;
  toolName: string;
  /** Array of {type:'text'|'image', ...} parts. */
  content: AgentConversationToolResultMessageContentList;
  isError: boolean;
  /** Epoch milliseconds. */
  timestamp: number;
}
export const AgentConversationToolResultMessage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    role: AgentConversationToolResultMessageRoleEnum,
    toolCallId: S.String,
    toolName: S.String,
    content: AgentConversationToolResultMessageContentList,
    isError: S.Boolean,
    timestamp: S.Number,
  }),
).annotate({
  identifier: "AgentConversationToolResultMessage",
}) as any as S.Schema<AgentConversationToolResultMessage>;

export type AgentConversationMessage =
  | AgentConversationUserMessage
  | AgentConversationAssistantMessage
  | AgentConversationToolResultMessage;
export const AgentConversationMessage =
  /*@__PURE__*/ S.Unknown as any as S.Schema<AgentConversationMessage>;

/** Full transcript, or the trailing `last_n` messages if `?last_n=` was supplied. */
export type AgentApplicationSessionsRetrieveResponseConversationList =
  ReadonlyArray<AgentConversationMessage>;
export const AgentApplicationSessionsRetrieveResponseConversationList =
  /*@__PURE__*/ S.Array(
    AgentConversationMessage,
  ) as any as S.Schema<AgentApplicationSessionsRetrieveResponseConversationList>;

/** Messages that arrived while a turn was in flight; drained into `conversation` at the start of the next turn. */
export type AgentApplicationSessionsRetrieveResponsePendingInputsList =
  ReadonlyArray<AgentConversationMessage>;
export const AgentApplicationSessionsRetrieveResponsePendingInputsList =
  /*@__PURE__*/ S.Array(
    AgentConversationMessage,
  ) as any as S.Schema<AgentApplicationSessionsRetrieveResponsePendingInputsList>;

export interface AgentApplicationSessionsRetrieveResponse {
  usage_total: AgentSessionUsageTotal;
  principal: AgentSessionPrincipal | null;
  id: string;
  application_id: string;
  revision_id: string;
  team_id: number;
  external_key: string | null;
  /** Trigger-specific metadata stamped at session creation. Discriminated on `kind`: chat | slack | cron | webhook | mcp. The Zod source of truth is `agent-shared/src/runtime/trigger-metadata.ts`; the node side validates and strips unknown keys at the persistence boundary, so consumers can trust `kind` and per-kind fields. TODO: narrow this DictField to a polymorphic serializer mirroring the union (needs `hogli build:openapi`). */
  trigger_metadata?: AgentApplicationSessionsRetrieveResponseTriggerMetadataMap | null;
  state: AgentSessionStateEnum;
  /** Full transcript, or the trailing `last_n` messages if `?last_n=` was supplied. */
  conversation: AgentApplicationSessionsRetrieveResponseConversationList;
  /** Messages that arrived while a turn was in flight; drained into `conversation` at the start of the next turn. */
  pending_inputs: AgentApplicationSessionsRetrieveResponsePendingInputsList;
  /** Times the janitor has re-queued this session after a stuck-running detection. */
  retry_count: number;
  created_at: string;
  updated_at: string;
  /** True when `?last_n=` was supplied AND the full conversation exceeded it. */
  conversation_trimmed: boolean;
  /** Total messages in the untrimmed conversation. Present only when `conversation_trimmed=true`. */
  conversation_total_turns?: number;
}
export const AgentApplicationSessionsRetrieveResponse = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      usage_total: AgentSessionUsageTotal,
      principal: S.NullOr(AgentSessionPrincipal),
      id: S.String,
      application_id: S.String,
      revision_id: S.String,
      team_id: S.Number,
      external_key: S.NullOr(S.String),
      trigger_metadata: S.optional(
        S.NullOr(AgentApplicationSessionsRetrieveResponseTriggerMetadataMap),
      ),
      state: AgentSessionStateEnum,
      conversation: AgentApplicationSessionsRetrieveResponseConversationList,
      pending_inputs: AgentApplicationSessionsRetrieveResponsePendingInputsList,
      retry_count: S.Number,
      created_at: S.String,
      updated_at: S.String,
      conversation_trimmed: S.Boolean,
      conversation_total_turns: S.optional(S.Number),
    }),
).annotate({
  identifier: "AgentApplicationSessionsRetrieveResponse",
}) as any as S.Schema<AgentApplicationSessionsRetrieveResponse>;

export interface AgentApplicationsSpecSchemaRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Return only this top-level slice of the spec schema to save tokens — one of `models`, `triggers`, `tools`, `mcps`, `skills`, `identity_providers`, `secrets`, `limits`, `reasoning`, `framework_prompt`, `resume`. Omit for the whole spec schema. */
  section?: string;
}
export const AgentApplicationsSpecSchemaRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    section: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/spec_schema/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsSpecSchemaRequest",
}) as any as S.Schema<AgentApplicationsSpecSchemaRequest>;

export interface AgentApplicationsStatsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  /** ISO datetime — counts spend + session totals from this point forward. Defaults to 24h ago. */
  since?: string;
}
export const AgentApplicationsStatsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    since: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{id}/stats/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsStatsRequest",
}) as any as S.Schema<AgentApplicationsStatsRequest>;

export interface AgentAggregateStats {
  /** Sessions currently in a live state (queued / running). */
  liveCount: number;
  /** Sessions created within the `since` window across all states. */
  sessionsInWindowCount: number;
  /** Sum of `usage_total.cost_total` across sessions in the window. */
  spendInWindowUsd: number;
  /** ISO timestamp of the most recent session update — null when there are no sessions. */
  lastActivityAt: string | null;
  /** Sessions in `failed` state created within the window. */
  failedInWindowCount: number;
  /** Approval-gated tool requests across the team currently awaiting a decision. 0 on the per-application aggregate (which doesn't roll up approvals). */
  pendingApprovalsCount: number;
}
export const AgentAggregateStats = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    liveCount: S.Number,
    sessionsInWindowCount: S.Number,
    spendInWindowUsd: S.Number,
    lastActivityAt: S.NullOr(S.String),
    failedInWindowCount: S.Number,
    pendingApprovalsCount: S.Number,
  }),
).annotate({
  identifier: "AgentAggregateStats",
}) as any as S.Schema<AgentAggregateStats>;

export interface AgentApplicationsUpdateRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  name: string;
  /** Globally-unique URL identifier. Server-minted as an opaque random slug on create; only allowlisted first-party teams may set it explicitly. Slugs live in one global namespace (domain-mode ingress routing carries no team). */
  slug?: string;
  description?: string;
  archived?: boolean;
}
export const AgentApplicationsUpdateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    name: S.String,
    slug: S.optional(S.String),
    description: S.optional(S.String),
    archived: S.optional(S.Boolean),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/api/projects/{project_id}/agent_applications/{id}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsUpdateRequest",
}) as any as S.Schema<AgentApplicationsUpdateRequest>;

export interface AgentApplicationsUsersConnectionDeleteRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
  agent_user_id: string;
  /** Identity provider id (e.g. 'posthog', 'github'). */
  provider: string;
}
export const AgentApplicationsUsersConnectionDeleteRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project_id: S.String.pipe(T.Label()),
      id: S.String.pipe(T.Label()),
      agent_user_id: S.String.pipe(T.Label()),
      provider: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/api/projects/{project_id}/agent_applications/{id}/users/{agent_user_id}/connections/{provider}/",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "AgentApplicationsUsersConnectionDeleteRequest",
  }) as any as S.Schema<AgentApplicationsUsersConnectionDeleteRequest>;

export interface AgentApplicationsUsersConnectionDeleteResponse {}
export const AgentApplicationsUsersConnectionDeleteResponse =
  /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
    identifier: "AgentApplicationsUsersConnectionDeleteResponse",
  }) as any as S.Schema<AgentApplicationsUsersConnectionDeleteResponse>;

export interface AgentApplicationsUsersListRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** A UUID string identifying this agent application. */
  id: string;
}
export const AgentApplicationsUsersListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{id}/users/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentApplicationsUsersListRequest",
}) as any as S.Schema<AgentApplicationsUsersListRequest>;

export type AgentUserConnectionScopesList = ReadonlyArray<string>;
export const AgentUserConnectionScopesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AgentUserConnectionScopesList>;

export interface AgentUserConnection {
  id: string;
  provider: string;
  scopes: AgentUserConnectionScopesList;
  /** active | revoked */
  state: string;
  subject?: string | null;
  access_expires_at?: string | null;
  created_at: string;
  updated_at: string;
  revoked_at?: string | null;
}
export const AgentUserConnection = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    provider: S.String,
    scopes: AgentUserConnectionScopesList,
    state: S.String,
    subject: S.optional(S.NullOr(S.String)),
    access_expires_at: S.optional(S.NullOr(S.String)),
    created_at: S.String,
    updated_at: S.String,
    revoked_at: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "AgentUserConnection",
}) as any as S.Schema<AgentUserConnection>;

export type AgentUserWithConnectionsConnectionsList =
  ReadonlyArray<AgentUserConnection>;
export const AgentUserWithConnectionsConnectionsList = /*@__PURE__*/ S.Array(
  AgentUserConnection,
) as any as S.Schema<AgentUserWithConnectionsConnectionsList>;

export interface AgentUserWithConnections {
  id: string;
  /** Edge-identity kind: slack | jwt | posthog | service | … */
  principal_kind: string;
  principal_id: string;
  metadata?: unknown;
  created_at: string;
  connections: AgentUserWithConnectionsConnectionsList;
}
export const AgentUserWithConnections = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    principal_kind: S.String,
    principal_id: S.String,
    metadata: S.optional(S.Unknown),
    created_at: S.String,
    connections: AgentUserWithConnectionsConnectionsList,
  }),
).annotate({
  identifier: "AgentUserWithConnections",
}) as any as S.Schema<AgentUserWithConnections>;

export type AgentUsersListResultsList = ReadonlyArray<AgentUserWithConnections>;
export const AgentUsersListResultsList = /*@__PURE__*/ S.Array(
  AgentUserWithConnections,
) as any as S.Schema<AgentUsersListResultsList>;

export interface AgentUsersList {
  count: number;
  results: AgentUsersListResultsList;
}
export const AgentUsersList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    count: S.Number,
    results: AgentUsersListResultsList,
  }),
).annotate({ identifier: "AgentUsersList" }) as any as S.Schema<AgentUsersList>;

export interface AgentFleetApprovalsListRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Optional agent UUID — narrows the listing to one application. */
  agent_id?: string;
  limit?: number;
  offset?: number;
  /** Filter by approval state. Comma-separated list accepted. Valid values: queued, approving, dispatched, dispatched_failed, rejected, expired. Defaults to all states. */
  state?: string;
}
export const AgentFleetApprovalsListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    agent_id: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
    state: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_fleet/approvals/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentFleetApprovalsListRequest",
}) as any as S.Schema<AgentFleetApprovalsListRequest>;

export interface AgentFleetApprovalsListResponse {}
export const AgentFleetApprovalsListResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "AgentFleetApprovalsListResponse",
}) as any as S.Schema<AgentFleetApprovalsListResponse>;

export interface AgentFleetLiveSessionsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** Cap on returned sessions (default 100, max 500). */
  limit?: number;
}
export const AgentFleetLiveSessionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    limit: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_fleet/live_sessions/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentFleetLiveSessionsRequest",
}) as any as S.Schema<AgentFleetLiveSessionsRequest>;

/** Trigger-specific metadata stamped at session creation. Discriminated on `kind`: chat | slack | cron | webhook | mcp. The Zod source of truth is `agent-shared/src/runtime/trigger-metadata.ts`; the node side validates and strips unknown keys at the persistence boundary, so consumers can trust `kind` and per-kind fields. TODO: narrow this DictField to a polymorphic serializer mirroring the union (needs `hogli build:openapi`). */
export type AgentFleetLiveSessionSummaryTriggerMetadataMap = {
  [key: string]: unknown | undefined;
};
export const AgentFleetLiveSessionSummaryTriggerMetadataMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<AgentFleetLiveSessionSummaryTriggerMetadataMap>;

export interface AgentFleetLiveSessionSummary {
  usage_total: AgentSessionUsageTotal;
  principal: AgentSessionPrincipal | null;
  id: string;
  application_id: string;
  revision_id: string;
  team_id: number;
  state: AgentSessionStateEnum;
  external_key: string | null;
  /** Trigger-specific metadata stamped at session creation. Discriminated on `kind`: chat | slack | cron | webhook | mcp. The Zod source of truth is `agent-shared/src/runtime/trigger-metadata.ts`; the node side validates and strips unknown keys at the persistence boundary, so consumers can trust `kind` and per-kind fields. TODO: narrow this DictField to a polymorphic serializer mirroring the union (needs `hogli build:openapi`). */
  trigger_metadata?: AgentFleetLiveSessionSummaryTriggerMetadataMap | null;
  /** Messages in the conversation so far. */
  turns: number;
  /** Last assistant text (~120 chars). Null when no assistant turns yet. */
  preview: string | null;
  created_at: string;
  updated_at: string;
}
export const AgentFleetLiveSessionSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    usage_total: AgentSessionUsageTotal,
    principal: S.NullOr(AgentSessionPrincipal),
    id: S.String,
    application_id: S.String,
    revision_id: S.String,
    team_id: S.Number,
    state: AgentSessionStateEnum,
    external_key: S.NullOr(S.String),
    trigger_metadata: S.optional(
      S.NullOr(AgentFleetLiveSessionSummaryTriggerMetadataMap),
    ),
    turns: S.Number,
    preview: S.NullOr(S.String),
    created_at: S.String,
    updated_at: S.String,
  }),
).annotate({
  identifier: "AgentFleetLiveSessionSummary",
}) as any as S.Schema<AgentFleetLiveSessionSummary>;

export type AgentFleetLiveSessionsResponseResultsList =
  ReadonlyArray<AgentFleetLiveSessionSummary>;
export const AgentFleetLiveSessionsResponseResultsList = /*@__PURE__*/ S.Array(
  AgentFleetLiveSessionSummary,
) as any as S.Schema<AgentFleetLiveSessionsResponseResultsList>;

export interface AgentFleetLiveSessionsResponse {
  results: AgentFleetLiveSessionsResponseResultsList;
}
export const AgentFleetLiveSessionsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: AgentFleetLiveSessionsResponseResultsList,
  }),
).annotate({
  identifier: "AgentFleetLiveSessionsResponse",
}) as any as S.Schema<AgentFleetLiveSessionsResponse>;

export interface AgentFleetStatsRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  /** ISO datetime — counts spend + session totals from this point forward. Defaults to 24h ago. */
  since?: string;
}
export const AgentFleetStatsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    since: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_fleet/stats/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentFleetStatsRequest",
}) as any as S.Schema<AgentFleetStatsRequest>;

/** Optional flat tags for search ranking. Lowercase a-z 0-9 _ - only. */
export type AgentMemoryCreateFileRequestTagsList = ReadonlyArray<string>;
export const AgentMemoryCreateFileRequestTagsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AgentMemoryCreateFileRequestTagsList>;

export interface AgentMemoryCreateFileRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** Where to store the file. Lowercase a-z 0-9 _ - / only, must end in .md. */
  path: string;
  /** One-line summary, max 280 chars. Surfaces in list/search results. */
  description: string;
  /** Full markdown body. */
  content: string;
  /** Optional flat tags for search ranking. Lowercase a-z 0-9 _ - only. */
  tags?: AgentMemoryCreateFileRequestTagsList;
}
export const AgentMemoryCreateFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
    path: S.String,
    description: S.String,
    content: S.String,
    tags: S.optional(AgentMemoryCreateFileRequestTagsList),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/memory/files/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentMemoryCreateFileRequest",
}) as any as S.Schema<AgentMemoryCreateFileRequest>;

export interface AgentMemoryFile {
  /** Full markdown body. */
  content: string;
}
export const AgentMemoryFile = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    content: S.String,
  }),
).annotate({
  identifier: "AgentMemoryFile",
}) as any as S.Schema<AgentMemoryFile>;

export interface AgentMemoryDeleteFileRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** Memory path to delete. */
  path: string;
}
export const AgentMemoryDeleteFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/memory/files/by_path/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentMemoryDeleteFileRequest",
}) as any as S.Schema<AgentMemoryDeleteFileRequest>;

export interface AgentMemoryDeleteFileResponse {}
export const AgentMemoryDeleteFileResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "AgentMemoryDeleteFileResponse",
}) as any as S.Schema<AgentMemoryDeleteFileResponse>;

export interface AgentMemoryGetFileRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** Memory path returned by the list endpoint, e.g. 'incidents/db.md'. */
  path: string;
}
export const AgentMemoryGetFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/memory/files/by_path/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentMemoryGetFileRequest",
}) as any as S.Schema<AgentMemoryGetFileRequest>;

export interface AgentMemoryListFilesRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** Optional path prefix to scope the list, e.g. 'incidents/'. */
  prefix?: string;
}
export const AgentMemoryListFilesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
    prefix: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/memory/files/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentMemoryListFilesRequest",
}) as any as S.Schema<AgentMemoryListFilesRequest>;

/** Frontmatter tags (lowercase a-z 0-9 _ - only). */
export type AgentMemoryHeaderTagsList = ReadonlyArray<string>;
export const AgentMemoryHeaderTagsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AgentMemoryHeaderTagsList>;

export interface AgentMemoryHeader {
  /** Relative path within the agent's memory, e.g. 'incidents/db.md'. */
  path: string;
  /** One-line summary from the file's frontmatter. */
  description: string;
  /** Frontmatter tags (lowercase a-z 0-9 _ - only). */
  tags: AgentMemoryHeaderTagsList;
  /** ISO-8601 timestamp stamped on create. Null for files written before this field was introduced. */
  created_at: string | null;
  /** ISO-8601 timestamp stamped on every write. */
  updated_at: string | null;
}
export const AgentMemoryHeader = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    description: S.String,
    tags: AgentMemoryHeaderTagsList,
    created_at: S.NullOr(S.String),
    updated_at: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AgentMemoryHeader",
}) as any as S.Schema<AgentMemoryHeader>;

/** Headers (frontmatter only) — no file bodies. Use the read endpoint for the body. */
export type AgentMemoryListResponseEntriesList =
  ReadonlyArray<AgentMemoryHeader>;
export const AgentMemoryListResponseEntriesList = /*@__PURE__*/ S.Array(
  AgentMemoryHeader,
) as any as S.Schema<AgentMemoryListResponseEntriesList>;

export interface AgentMemoryListResponse {
  /** Number of entries returned. */
  count: number;
  /** Headers (frontmatter only) — no file bodies. Use the read endpoint for the body. */
  entries: AgentMemoryListResponseEntriesList;
}
export const AgentMemoryListResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    count: S.Number,
    entries: AgentMemoryListResponseEntriesList,
  }),
).annotate({
  identifier: "AgentMemoryListResponse",
}) as any as S.Schema<AgentMemoryListResponse>;

export interface AgentMemoryListTablesRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
}
export const AgentMemoryListTablesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/memory/tables/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentMemoryListTablesRequest",
}) as any as S.Schema<AgentMemoryListTablesRequest>;

export interface AgentTableHeader {
  /** Table name. */
  name: string;
  /** Object size in bytes. */
  size: number;
}
export const AgentTableHeader = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    size: S.Number,
  }),
).annotate({
  identifier: "AgentTableHeader",
}) as any as S.Schema<AgentTableHeader>;

/** Tabular-reference tables for this agent (the @posthog/table-* JSONL tables). */
export type AgentTablesListResponseTablesList = ReadonlyArray<AgentTableHeader>;
export const AgentTablesListResponseTablesList = /*@__PURE__*/ S.Array(
  AgentTableHeader,
) as any as S.Schema<AgentTablesListResponseTablesList>;

export interface AgentTablesListResponse {
  /** Number of tables. */
  count: number;
  /** Tabular-reference tables for this agent (the @posthog/table-* JSONL tables). */
  tables: AgentTablesListResponseTablesList;
}
export const AgentTablesListResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    count: S.Number,
    tables: AgentTablesListResponseTablesList,
  }),
).annotate({
  identifier: "AgentTablesListResponse",
}) as any as S.Schema<AgentTablesListResponse>;

export interface AgentMemoryReadTableRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  name: string;
  /** Max rows to return (default 500, max 5000). */
  limit?: number;
}
export const AgentMemoryReadTableRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
    name: S.String.pipe(T.Label()),
    limit: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/memory/tables/{name}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentMemoryReadTableRequest",
}) as any as S.Schema<AgentMemoryReadTableRequest>;

export type AgentTableRowsResponseRowsItemMap = {
  [key: string]: unknown | undefined;
};
export const AgentTableRowsResponseRowsItemMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<AgentTableRowsResponseRowsItemMap>;

/** The rows (arbitrary JSON objects). */
export type AgentTableRowsResponseRowsList =
  ReadonlyArray<AgentTableRowsResponseRowsItemMap>;
export const AgentTableRowsResponseRowsList = /*@__PURE__*/ S.Array(
  AgentTableRowsResponseRowsItemMap,
) as any as S.Schema<AgentTableRowsResponseRowsList>;

export interface AgentTableRowsResponse {
  name: string;
  /** Total rows in the table. */
  total: number;
  /** Rows in this response (capped by limit). */
  returned: number;
  limit: number;
  /** The rows (arbitrary JSON objects). */
  rows: AgentTableRowsResponseRowsList;
}
export const AgentTableRowsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    total: S.Number,
    returned: S.Number,
    limit: S.Number,
    rows: AgentTableRowsResponseRowsList,
  }),
).annotate({
  identifier: "AgentTableRowsResponse",
}) as any as S.Schema<AgentTableRowsResponse>;

export interface AgentMemorySearchRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** Max results (default 10, max 100). */
  limit?: number;
  /** Optional path prefix to scope the search. */
  prefix?: string;
  /** Search cue — plain natural language is fine. */
  q: string;
}
export const AgentMemorySearchRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
    limit: S.optional(S.Number.pipe(T.Query())),
    prefix: S.optional(S.String.pipe(T.Query())),
    q: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/memory/search/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentMemorySearchRequest",
}) as any as S.Schema<AgentMemorySearchRequest>;

export type AgentMemorySearchResultTagsList = ReadonlyArray<string>;
export const AgentMemorySearchResultTagsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AgentMemorySearchResultTagsList>;

export interface AgentMemorySearchResult {
  path: string;
  description: string;
  tags: AgentMemorySearchResultTagsList;
  /** BM25 relevance score. */
  score: number;
  /** Body snippet around the earliest match. Null when only the header matched. */
  snippet: string | null;
}
export const AgentMemorySearchResult = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    description: S.String,
    tags: AgentMemorySearchResultTagsList,
    score: S.Number,
    snippet: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AgentMemorySearchResult",
}) as any as S.Schema<AgentMemorySearchResult>;

export type AgentMemorySearchResponseResultsList =
  ReadonlyArray<AgentMemorySearchResult>;
export const AgentMemorySearchResponseResultsList = /*@__PURE__*/ S.Array(
  AgentMemorySearchResult,
) as any as S.Schema<AgentMemorySearchResponseResultsList>;

export interface AgentMemorySearchResponse {
  /** The original search cue, echoed back. */
  cue: string;
  count: number;
  results: AgentMemorySearchResponseResultsList;
}
export const AgentMemorySearchResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cue: S.String,
    count: S.Number,
    results: AgentMemorySearchResponseResultsList,
  }),
).annotate({
  identifier: "AgentMemorySearchResponse",
}) as any as S.Schema<AgentMemorySearchResponse>;

export interface AgentMemoryTreeRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
}
export const AgentMemoryTreeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/memory/tree/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentMemoryTreeRequest",
}) as any as S.Schema<AgentMemoryTreeRequest>;

/** Folder tree rooted at the agent's memory prefix. Each node is {name, type: 'folder'|'file', path?, description?, tags?, children?}. */
export type AgentMemoryTreeResponseRootMap = {
  [key: string]: unknown | undefined;
};
export const AgentMemoryTreeResponseRootMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<AgentMemoryTreeResponseRootMap>;

export interface AgentMemoryTreeResponse {
  /** Folder tree rooted at the agent's memory prefix. Each node is {name, type: 'folder'|'file', path?, description?, tags?, children?}. */
  root: AgentMemoryTreeResponseRootMap;
}
export const AgentMemoryTreeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    root: AgentMemoryTreeResponseRootMap,
  }),
).annotate({
  identifier: "AgentMemoryTreeResponse",
}) as any as S.Schema<AgentMemoryTreeResponse>;

export type AgentMemoryUpdateFileRequestTagsList = ReadonlyArray<string>;
export const AgentMemoryUpdateFileRequestTagsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AgentMemoryUpdateFileRequestTagsList>;

export interface AgentMemoryUpdateFileRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** Memory path to update. */
  path: string;
  description?: string;
  content?: string;
  tags?: AgentMemoryUpdateFileRequestTagsList;
}
export const AgentMemoryUpdateFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
    description: S.optional(S.String),
    content: S.optional(S.String),
    tags: S.optional(AgentMemoryUpdateFileRequestTagsList),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/memory/files/by_path/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentMemoryUpdateFileRequest",
}) as any as S.Schema<AgentMemoryUpdateFileRequest>;

export interface AgentNativeToolsListRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
}
export const AgentNativeToolsListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_native_tools/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentNativeToolsListRequest",
}) as any as S.Schema<AgentNativeToolsListRequest>;

export type AgentNativeToolEntrySchemaMap = {
  [key: string]: unknown | undefined;
};
export const AgentNativeToolEntrySchemaMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<AgentNativeToolEntrySchemaMap>;

export interface AgentNativeToolEntry {
  id: string;
  schema: AgentNativeToolEntrySchemaMap;
}
export const AgentNativeToolEntry = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    schema: AgentNativeToolEntrySchemaMap,
  }),
).annotate({
  identifier: "AgentNativeToolEntry",
}) as any as S.Schema<AgentNativeToolEntry>;

export type AgentNativeToolsListResponseToolsList =
  ReadonlyArray<AgentNativeToolEntry>;
export const AgentNativeToolsListResponseToolsList = /*@__PURE__*/ S.Array(
  AgentNativeToolEntry,
) as any as S.Schema<AgentNativeToolsListResponseToolsList>;

export interface AgentNativeToolsListResponse {
  tools: AgentNativeToolsListResponseToolsList;
}
export const AgentNativeToolsListResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tools: AgentNativeToolsListResponseToolsList,
  }),
).annotate({
  identifier: "AgentNativeToolsListResponse",
}) as any as S.Schema<AgentNativeToolsListResponse>;

export type AgentNativeToolsListResponseBodyList =
  ReadonlyArray<AgentNativeToolsListResponse>;
export const AgentNativeToolsListResponseBodyList = /*@__PURE__*/ S.Array(
  AgentNativeToolsListResponse,
) as any as S.Schema<AgentNativeToolsListResponseBodyList>;

export type AgentNativeToolsListResponse2 =
  AgentNativeToolsListResponseBodyList;
export const AgentNativeToolsListResponse2 = /*@__PURE__*/ S.suspend(() =>
  AgentNativeToolsListResponseBodyList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "AgentNativeToolsListResponse2",
}) as any as S.Schema<AgentNativeToolsListResponse2>;

export interface AgentRevisionsEnvKeysClearRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  /** The env variable name. Conventionally UPPER_SNAKE_CASE; the API does not enforce a shape. */
  key: string;
}
export const AgentRevisionsEnvKeysClearRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    key: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/env_keys/{key}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentRevisionsEnvKeysClearRequest",
}) as any as S.Schema<AgentRevisionsEnvKeysClearRequest>;

export interface AgentRevisionsEnvKeysClearResponse {}
export const AgentRevisionsEnvKeysClearResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "AgentRevisionsEnvKeysClearResponse",
}) as any as S.Schema<AgentRevisionsEnvKeysClearResponse>;

export interface AgentRevisionsEnvKeysGetRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  /** The env variable name. Conventionally UPPER_SNAKE_CASE; the API does not enforce a shape. */
  key: string;
}
export const AgentRevisionsEnvKeysGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    key: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/env_keys/{key}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentRevisionsEnvKeysGetRequest",
}) as any as S.Schema<AgentRevisionsEnvKeysGetRequest>;

export interface AgentRevisionEnvKeyStatus {
  key: string;
  /** True if the key is present in the env block. The value itself is never returned. */
  is_set: boolean;
}
export const AgentRevisionEnvKeyStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.String,
    is_set: S.Boolean,
  }),
).annotate({
  identifier: "AgentRevisionEnvKeyStatus",
}) as any as S.Schema<AgentRevisionEnvKeyStatus>;

export interface AgentRevisionsEnvKeysListRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
}
export const AgentRevisionsEnvKeysListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/env_keys/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentRevisionsEnvKeysListRequest",
}) as any as S.Schema<AgentRevisionsEnvKeysListRequest>;

/** Names of env variables currently set on the revision. Values are never returned. */
export type AgentRevisionEnvKeysResponseKeysList = ReadonlyArray<string>;
export const AgentRevisionEnvKeysResponseKeysList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AgentRevisionEnvKeysResponseKeysList>;

export interface AgentRevisionEnvKeysResponse {
  /** Names of env variables currently set on the revision. Values are never returned. */
  keys: AgentRevisionEnvKeysResponseKeysList;
}
export const AgentRevisionEnvKeysResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keys: AgentRevisionEnvKeysResponseKeysList,
  }),
).annotate({
  identifier: "AgentRevisionEnvKeysResponse",
}) as any as S.Schema<AgentRevisionEnvKeysResponse>;

export interface AgentRevisionsEnvKeysSetRequest {
  /** Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/. */
  project_id: string;
  application_id: string;
  /** A UUID string identifying this agent revision. */
  id: string;
  /** The env variable name. Conventionally UPPER_SNAKE_CASE; the API does not enforce a shape. */
  key: string;
  value: string;
}
export const AgentRevisionsEnvKeysSetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project_id: S.String.pipe(T.Label()),
    application_id: S.String.pipe(T.Label()),
    id: S.String.pipe(T.Label()),
    key: S.String.pipe(T.Label()),
    value: S.String,
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/api/projects/{project_id}/agent_applications/{application_id}/revisions/{id}/env_keys/{key}/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AgentRevisionsEnvKeysSetRequest",
}) as any as S.Schema<AgentRevisionsEnvKeysSetRequest>;

export type AgentApplicationsApprovalsDecideError = PosthogOpError;
/** Approve or reject a queued `agent`-type tool-approval request. This is the OWNER decision surface — the only PostHog-authoritative one: team admins decide here, in the console. `principal`-type approvals are decided by the session principal at the ingress decision API, not here. The runtime side runs the tool platform-side on approve and wakes the session with a synthetic tool_result either way. */
export const agentApplicationsApprovalsDecide: API.OperationMethod<
  AgentApplicationsApprovalsDecideRequest,
  AgentApprovalsDecideResponse,
  AgentApplicationsApprovalsDecideError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsApprovalsDecideRequest,
  output: AgentApprovalsDecideResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsApprovalsListError = PosthogOpError;
/** List approval-gated tool requests for this application. Team-admin only (per plan §6.1). Default returns all states — pass `?state=queued` for the inbox view. */
export const agentApplicationsApprovalsList: API.OperationMethod<
  AgentApplicationsApprovalsListRequest,
  AgentApplicationApprovalsListResponse,
  AgentApplicationsApprovalsListError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsApprovalsListRequest,
  output: AgentApplicationApprovalsListResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsApprovalsRetrieveError = PosthogOpError;
/** Single approval request — full proposed args, assistant snapshot, decision metadata, dispatch outcome. Team-admin only (plan §6.1). */
export const agentApplicationsApprovalsRetrieve: API.OperationMethod<
  AgentApplicationsApprovalsRetrieveRequest,
  AgentApplicationsApprovalsRetrieveResponse,
  AgentApplicationsApprovalsRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsApprovalsRetrieveRequest,
  output: AgentApplicationsApprovalsRetrieveResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsCreateError = PosthogOpError;
/** Agent applications — the deployable unit of the platform. URLs: GET /api/projects/<team>/agent_applications/ list POST /api/projects/<team>/agent_applications/ create GET /api/projects/<team>/agent_applications/<id|slug>/ retrieve PATCH /api/projects/<team>/agent_applications/<id|slug>/ update DELETE /api/projects/<team>/agent_applications/<id|slug>/ archive POST /api/projects/<team>/agent_applications/<id|slug>/set_env/ bulk replace env GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/ list set keys GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ is one key set? PUT /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ set one key DELETE /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ clear one key */
export const agentApplicationsCreate: API.OperationMethod<
  AgentApplicationsCreateRequest,
  AgentApplication,
  AgentApplicationsCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsCreateRequest,
  output: AgentApplication,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsDestroyError = PosthogOpError;
/** Agent applications — the deployable unit of the platform. URLs: GET /api/projects/<team>/agent_applications/ list POST /api/projects/<team>/agent_applications/ create GET /api/projects/<team>/agent_applications/<id|slug>/ retrieve PATCH /api/projects/<team>/agent_applications/<id|slug>/ update DELETE /api/projects/<team>/agent_applications/<id|slug>/ archive POST /api/projects/<team>/agent_applications/<id|slug>/set_env/ bulk replace env GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/ list set keys GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ is one key set? PUT /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ set one key DELETE /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ clear one key */
export const agentApplicationsDestroy: API.OperationMethod<
  AgentApplicationsDestroyRequest,
  AgentApplicationsDestroyResponse,
  AgentApplicationsDestroyError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsDestroyRequest,
  output: AgentApplicationsDestroyResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsInvokeError = PosthogOpError;
/** Start a new session on this agent's LIVE (promoted) revision. Bridges to ingress `POST /agents/<slug>/run`, forwarding the caller's PAT so the session principal is the real caller. Returns the new `session_id`; drive the conversation with `agent-applications-send` and read progress with `agent-applications-listen`. For non-live / draft revisions use `preview_proxy` instead. */
export const agentApplicationsInvoke: API.OperationMethod<
  AgentApplicationsInvokeRequest,
  AgentInvokeResponse,
  AgentApplicationsInvokeError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsInvokeRequest,
  output: AgentInvokeResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsListError = PosthogOpError;
/** Agent applications — the deployable unit of the platform. URLs: GET /api/projects/<team>/agent_applications/ list POST /api/projects/<team>/agent_applications/ create GET /api/projects/<team>/agent_applications/<id|slug>/ retrieve PATCH /api/projects/<team>/agent_applications/<id|slug>/ update DELETE /api/projects/<team>/agent_applications/<id|slug>/ archive POST /api/projects/<team>/agent_applications/<id|slug>/set_env/ bulk replace env GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/ list set keys GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ is one key set? PUT /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ set one key DELETE /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ clear one key */
export const agentApplicationsList: API.OperationMethod<
  AgentApplicationsListRequest,
  PaginatedAgentApplicationList,
  AgentApplicationsListError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsListRequest,
  output: PaginatedAgentApplicationList,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsListenError = PosthogOpError;
/** Poll a LIVE session's progress as a single JSON digest (non-streaming, MCP-friendly). Bridges to the internal ingress digest RPC — the digest is built node-side (never in Python). Tenancy is enforced by the team-scoped `get_object()` here plus the ingress digest's `application_id` re-scope; there is no janitor pre-check on this polled path (see the comment below). */
export const agentApplicationsListen: API.OperationMethod<
  AgentApplicationsListenRequest,
  AgentListenResponse,
  AgentApplicationsListenError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsListenRequest,
  output: AgentListenResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsModelsError = PosthogOpError;
/** Served-model catalog — each model's id, provider, context window, and USD-per-million-token pricing — plus the curated auto-level → model map. Project-agnostic; sourced from the AI gateway catalog. Powers the config UI model browser and the agent builder's model-choosing skill. */
export const agentApplicationsModels: API.OperationMethod<
  AgentApplicationsModelsRequest,
  AgentApplication,
  AgentApplicationsModelsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsModelsRequest,
  output: AgentApplication,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsPartialUpdateError = PosthogOpError;
/** Agent applications — the deployable unit of the platform. URLs: GET /api/projects/<team>/agent_applications/ list POST /api/projects/<team>/agent_applications/ create GET /api/projects/<team>/agent_applications/<id|slug>/ retrieve PATCH /api/projects/<team>/agent_applications/<id|slug>/ update DELETE /api/projects/<team>/agent_applications/<id|slug>/ archive POST /api/projects/<team>/agent_applications/<id|slug>/set_env/ bulk replace env GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/ list set keys GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ is one key set? PUT /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ set one key DELETE /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ clear one key */
export const agentApplicationsPartialUpdate: API.OperationMethod<
  AgentApplicationsPartialUpdateRequest,
  AgentApplication,
  AgentApplicationsPartialUpdateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsPartialUpdateRequest,
  output: AgentApplication,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsPreviewProxyError = PosthogOpError;
/** Authoring-side proxy for invoking a *draft* (or any non-live) revision. Closes the anonymous-draft-invoke gap: the public ingress URL refuses non-live invokes that don't carry the `x-agent-preview-secret` header; this proxy attaches it after authenticating the Django caller. URL: `/api/projects/<team>/agent_applications/<app>/preview-proxy/<rest>` Auth: standard PAT / session — `agents:write` scope (POST run/send/cancel is a mutating invoke; the read-only `listen` GET is `agents:read`). */
export const agentApplicationsPreviewProxy: API.OperationMethod<
  AgentApplicationsPreviewProxyRequest,
  AgentApplicationsPreviewProxyResponse,
  AgentApplicationsPreviewProxyError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsPreviewProxyRequest,
  output: AgentApplicationsPreviewProxyResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsPreviewProxyGetError = PosthogOpError;
/** GET passthrough for the preview-proxy — used for `/listen` SSE. */
export const agentApplicationsPreviewProxyGet: API.OperationMethod<
  AgentApplicationsPreviewProxyGetRequest,
  AgentApplication,
  AgentApplicationsPreviewProxyGetError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsPreviewProxyGetRequest,
  output: AgentApplication,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsPreviewTokenError = PosthogOpError;
/** GET sibling of `preview_token_mint`. Same body and response shape — exists because `EventSource` can't set headers, so SSE callers fetch the token via GET and then attach `?preview_token=` to the ingress URL. Behind the same URL (`url_path="preview-token"`) thanks to DRF's `@<action>.mapping.get`; DRF resolves it to a distinct `view.action`, but it is in `scope_object_write_actions` alongside the POST sibling — both return a usable credential, so both require `agents:write`. */
export const agentApplicationsPreviewToken: API.OperationMethod<
  AgentApplicationsPreviewTokenRequest,
  AgentApplicationPreviewTokenResponse,
  AgentApplicationsPreviewTokenError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsPreviewTokenRequest,
  output: AgentApplicationPreviewTokenResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsPreviewTokenMintError = PosthogOpError;
/** Mint a short-lived JWT for talking to a non-live revision directly via the public ingress URL. The caller attaches it as the `x-agent-preview-token` header (or `?preview_token=` query param for `EventSource`). See `_mint_preview_jwt` for the payload + claim binding. The response also includes `endpoints`, `auth`, and `preview_proxy` blocks so the caller can wire a preview invocation without grepping the agent-ingress source for which path each trigger exposes or which header name carries the token. This is the "self-describing" half of preview-mode — every piece of info you need to hit ingress is in one response. POST is the canonical verb — minting credentials for downstream `run`/`send`/`cancel` is a write-class capability. A GET sibling exists at the same URL for `EventSource` callers (which can't set headers); it is also write-scoped, since it returns the same token. */
export const agentApplicationsPreviewTokenMint: API.OperationMethod<
  AgentApplicationsPreviewTokenMintRequest,
  AgentApplicationPreviewTokenResponse,
  AgentApplicationsPreviewTokenMintError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsPreviewTokenMintRequest,
  output: AgentApplicationPreviewTokenResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRetrieveError = PosthogOpError;
/** Agent applications — the deployable unit of the platform. URLs: GET /api/projects/<team>/agent_applications/ list POST /api/projects/<team>/agent_applications/ create GET /api/projects/<team>/agent_applications/<id|slug>/ retrieve PATCH /api/projects/<team>/agent_applications/<id|slug>/ update DELETE /api/projects/<team>/agent_applications/<id|slug>/ archive POST /api/projects/<team>/agent_applications/<id|slug>/set_env/ bulk replace env GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/ list set keys GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ is one key set? PUT /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ set one key DELETE /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ clear one key */
export const agentApplicationsRetrieve: API.OperationMethod<
  AgentApplicationsRetrieveRequest,
  AgentApplication,
  AgentApplicationsRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRetrieveRequest,
  output: AgentApplication,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsAgentMdUpdateError = PosthogOpError;
/** Revisions of an agent. Created in `draft`, promoted through `ready → live` once the bundle has been uploaded + frozen. URLs (nested under an application): Model CRUD: GET .../revisions/ list POST .../revisions/ create draft GET .../revisions/<id>/ retrieve PATCH .../revisions/<id>/ update spec (draft only) Lifecycle: POST .../revisions/<id>/promote/ ready → live POST .../revisions/<id>/archive/ → archived POST .../revisions/<id>/freeze/ draft → ready (stamps sha256) POST .../revisions/<id>/clone_from/ copy bundle from another rev POST .../revisions/new_draft/ create draft + clone_from atomically Bundle authoring (proxied to the janitor): GET .../revisions/<id>/manifest/ list paths + sha256 GET .../revisions/<id>/file/?path=… read one file PUT .../revisions/<id>/file/?path=… write one file (draft) DELETE .../revisions/<id>/file/?path=… delete one file (draft) GET .../revisions/<id>/bundle/ bulk pull all files PUT .../revisions/<id>/bundle/ bulk push (replace|merge) */
export const agentApplicationsRevisionsAgentMdUpdate: API.OperationMethod<
  AgentApplicationsRevisionsAgentMdUpdateRequest,
  AgentRevision,
  AgentApplicationsRevisionsAgentMdUpdateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsAgentMdUpdateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsArchiveCreateError = PosthogOpError;
/** Mark a revision archived. If it was the live one, clear the application's live_revision pointer (the app effectively has no deployable version until another revision is promoted). */
export const agentApplicationsRevisionsArchiveCreate: API.OperationMethod<
  AgentApplicationsRevisionsArchiveCreateRequest,
  AgentRevision,
  AgentApplicationsRevisionsArchiveCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsArchiveCreateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsBundleFileUpdateError =
  | Conflict
  | PosthogOpError;
/** Update one `.md` file on a draft revision. `agent.md` writes go to the draft bundle. `skills/<id>/SKILL.md` writes are store-backed — skills are materialized from the skill store at freeze, so the edit publishes a new version of the referenced store skill and re-pins the draft's `skill_refs` entry to it. `<id>` must be a ref alias on this revision; add new skills via `bundle/import/` or `skill_refs`. Tool source / schema editing is out of scope here — use the per-tool endpoints. Returns the updated revision so the caller can refresh in one round-trip. */
export const agentApplicationsRevisionsBundleFileUpdate: API.OperationMethod<
  AgentApplicationsRevisionsBundleFileUpdateRequest,
  AgentRevision,
  AgentApplicationsRevisionsBundleFileUpdateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsBundleFileUpdateRequest,
  output: AgentRevision,
  errors: [Conflict],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsBundleImportCreateError =
  | Conflict
  | PosthogOpError;
/** Bulk-merge a set of `.md` files into a draft revision. Sets `agent_md` on the draft bundle if present. `skills[]` are store-backed and merge by `id`: an id already referenced by the draft publishes a new version of its store skill; an unreferenced id attaches the store skill of that name (publishing the payload's body to it), or creates it when no such skill exists — and each ref is (re-)pinned to the published version. Skills not mentioned are left alone, so the import is safe to retry. Draft-only; non-draft revisions return 409 untouched. */
export const agentApplicationsRevisionsBundleImportCreate: API.OperationMethod<
  AgentApplicationsRevisionsBundleImportCreateRequest,
  AgentRevision,
  AgentApplicationsRevisionsBundleImportCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsBundleImportCreateRequest,
  output: AgentRevision,
  errors: [Conflict],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsBundleRetrieveError = PosthogOpError;
/** Read the full typed bundle: `{ agent_md, skills, tools, spec }`. */
export const agentApplicationsRevisionsBundleRetrieve: API.OperationMethod<
  AgentApplicationsRevisionsBundleRetrieveRequest,
  AgentRevision,
  AgentApplicationsRevisionsBundleRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsBundleRetrieveRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsBundleUpdateError = PosthogOpError;
/** Full-replace the typed bundle. Anything not in the payload is deleted. Tool sources are AST-checked + esbuild-compiled by the janitor before any S3 writes. */
export const agentApplicationsRevisionsBundleUpdate: API.OperationMethod<
  AgentApplicationsRevisionsBundleUpdateRequest,
  AgentRevision,
  AgentApplicationsRevisionsBundleUpdateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsBundleUpdateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsCloneFromCreateError = PosthogOpError;
/** Copy every file from `source_revision_id` into this revision. */
export const agentApplicationsRevisionsCloneFromCreate: API.OperationMethod<
  AgentApplicationsRevisionsCloneFromCreateRequest,
  AgentRevision,
  AgentApplicationsRevisionsCloneFromCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsCloneFromCreateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsCreateError = PosthogOpError;
/** Revisions of an agent. Created in `draft`, promoted through `ready → live` once the bundle has been uploaded + frozen. URLs (nested under an application): Model CRUD: GET .../revisions/ list POST .../revisions/ create draft GET .../revisions/<id>/ retrieve PATCH .../revisions/<id>/ update spec (draft only) Lifecycle: POST .../revisions/<id>/promote/ ready → live POST .../revisions/<id>/archive/ → archived POST .../revisions/<id>/freeze/ draft → ready (stamps sha256) POST .../revisions/<id>/clone_from/ copy bundle from another rev POST .../revisions/new_draft/ create draft + clone_from atomically Bundle authoring (proxied to the janitor): GET .../revisions/<id>/manifest/ list paths + sha256 GET .../revisions/<id>/file/?path=… read one file PUT .../revisions/<id>/file/?path=… write one file (draft) DELETE .../revisions/<id>/file/?path=… delete one file (draft) GET .../revisions/<id>/bundle/ bulk pull all files PUT .../revisions/<id>/bundle/ bulk push (replace|merge) */
export const agentApplicationsRevisionsCreate: API.OperationMethod<
  AgentApplicationsRevisionsCreateRequest,
  AgentRevision,
  AgentApplicationsRevisionsCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsCreateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsCronFireCreateError = PosthogOpError;
/** Fire one cron job out-of-band — the same execution path the scheduler walks, but on demand. Authoring UX: the user iterates on a cron prompt by clicking 'Fire now' rather than waiting for the next scheduled firing. Without this, 'did my prompt do the right thing?' is unanswerable until the cron actually fires. Idempotent via `request_id`: repeat clicks with the same id resolve to the same session id rather than firing N times. */
export const agentApplicationsRevisionsCronFireCreate: API.OperationMethod<
  AgentApplicationsRevisionsCronFireCreateRequest,
  AgentRevisionCronFireResponse,
  AgentApplicationsRevisionsCronFireCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsCronFireCreateRequest,
  output: AgentRevisionCronFireResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsDestroyError = PosthogOpError;
/** Revisions of an agent. Created in `draft`, promoted through `ready → live` once the bundle has been uploaded + frozen. URLs (nested under an application): Model CRUD: GET .../revisions/ list POST .../revisions/ create draft GET .../revisions/<id>/ retrieve PATCH .../revisions/<id>/ update spec (draft only) Lifecycle: POST .../revisions/<id>/promote/ ready → live POST .../revisions/<id>/archive/ → archived POST .../revisions/<id>/freeze/ draft → ready (stamps sha256) POST .../revisions/<id>/clone_from/ copy bundle from another rev POST .../revisions/new_draft/ create draft + clone_from atomically Bundle authoring (proxied to the janitor): GET .../revisions/<id>/manifest/ list paths + sha256 GET .../revisions/<id>/file/?path=… read one file PUT .../revisions/<id>/file/?path=… write one file (draft) DELETE .../revisions/<id>/file/?path=… delete one file (draft) GET .../revisions/<id>/bundle/ bulk pull all files PUT .../revisions/<id>/bundle/ bulk push (replace|merge) */
export const agentApplicationsRevisionsDestroy: API.OperationMethod<
  AgentApplicationsRevisionsDestroyRequest,
  AgentApplicationsRevisionsDestroyResponse,
  AgentApplicationsRevisionsDestroyError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsDestroyRequest,
  output: AgentApplicationsRevisionsDestroyResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsFreezeCreateError = PosthogOpError;
/** Freeze the bundle: draft → ready, stamps sha256 on the row. Django is a thin proxy here: resolve template refs into the bundle, ask the janitor to seal it (the janitor returns the sha + the spec it derived from the typed resources), then stamp the row. No `transaction.atomic()` — the janitor's freeze is idempotent (on retry it re-reads the existing `.frozen` marker + re-derives spec), so a partial failure here is recoverable by re-calling freeze, not by transactional rollback. Holding an atomic block across the janitor HTTP call previously deadlocked the agent_revision row against the janitor's spec write — that's moved off the janitor side as part of the same fix. */
export const agentApplicationsRevisionsFreezeCreate: API.OperationMethod<
  AgentApplicationsRevisionsFreezeCreateRequest,
  AgentRevision,
  AgentApplicationsRevisionsFreezeCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsFreezeCreateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsListError = PosthogOpError;
/** Revisions of an agent. Created in `draft`, promoted through `ready → live` once the bundle has been uploaded + frozen. URLs (nested under an application): Model CRUD: GET .../revisions/ list POST .../revisions/ create draft GET .../revisions/<id>/ retrieve PATCH .../revisions/<id>/ update spec (draft only) Lifecycle: POST .../revisions/<id>/promote/ ready → live POST .../revisions/<id>/archive/ → archived POST .../revisions/<id>/freeze/ draft → ready (stamps sha256) POST .../revisions/<id>/clone_from/ copy bundle from another rev POST .../revisions/new_draft/ create draft + clone_from atomically Bundle authoring (proxied to the janitor): GET .../revisions/<id>/manifest/ list paths + sha256 GET .../revisions/<id>/file/?path=… read one file PUT .../revisions/<id>/file/?path=… write one file (draft) DELETE .../revisions/<id>/file/?path=… delete one file (draft) GET .../revisions/<id>/bundle/ bulk pull all files PUT .../revisions/<id>/bundle/ bulk push (replace|merge) */
export const agentApplicationsRevisionsList: API.OperationMethod<
  AgentApplicationsRevisionsListRequest,
  PaginatedAgentRevisionList,
  AgentApplicationsRevisionsListError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsListRequest,
  output: PaginatedAgentRevisionList,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsManifestRetrieveError = PosthogOpError;
/** List every file in this revision's bundle (path, size, sha256). */
export const agentApplicationsRevisionsManifestRetrieve: API.OperationMethod<
  AgentApplicationsRevisionsManifestRetrieveRequest,
  AgentRevision,
  AgentApplicationsRevisionsManifestRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsManifestRetrieveRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsNewDraftCreateError = PosthogOpError;
/** Create a fresh draft revision under `application_id` and seed it from `source_revision_id`. Saves the MCP one round-trip vs the explicit create + clone_from sequence. */
export const agentApplicationsRevisionsNewDraftCreate: API.OperationMethod<
  AgentApplicationsRevisionsNewDraftCreateRequest,
  AgentRevision,
  AgentApplicationsRevisionsNewDraftCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsNewDraftCreateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsPartialUpdateError = PosthogOpError;
/** Revisions of an agent. Created in `draft`, promoted through `ready → live` once the bundle has been uploaded + frozen. URLs (nested under an application): Model CRUD: GET .../revisions/ list POST .../revisions/ create draft GET .../revisions/<id>/ retrieve PATCH .../revisions/<id>/ update spec (draft only) Lifecycle: POST .../revisions/<id>/promote/ ready → live POST .../revisions/<id>/archive/ → archived POST .../revisions/<id>/freeze/ draft → ready (stamps sha256) POST .../revisions/<id>/clone_from/ copy bundle from another rev POST .../revisions/new_draft/ create draft + clone_from atomically Bundle authoring (proxied to the janitor): GET .../revisions/<id>/manifest/ list paths + sha256 GET .../revisions/<id>/file/?path=… read one file PUT .../revisions/<id>/file/?path=… write one file (draft) DELETE .../revisions/<id>/file/?path=… delete one file (draft) GET .../revisions/<id>/bundle/ bulk pull all files PUT .../revisions/<id>/bundle/ bulk push (replace|merge) */
export const agentApplicationsRevisionsPartialUpdate: API.OperationMethod<
  AgentApplicationsRevisionsPartialUpdateRequest,
  AgentRevision,
  AgentApplicationsRevisionsPartialUpdateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsPartialUpdateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsPromoteCreateError = PosthogOpError;
/** ready → live. Sets the parent application's live_revision. */
export const agentApplicationsRevisionsPromoteCreate: API.OperationMethod<
  AgentApplicationsRevisionsPromoteCreateRequest,
  AgentRevision,
  AgentApplicationsRevisionsPromoteCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsPromoteCreateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsRetrieveError = PosthogOpError;
/** Revisions of an agent. Created in `draft`, promoted through `ready → live` once the bundle has been uploaded + frozen. URLs (nested under an application): Model CRUD: GET .../revisions/ list POST .../revisions/ create draft GET .../revisions/<id>/ retrieve PATCH .../revisions/<id>/ update spec (draft only) Lifecycle: POST .../revisions/<id>/promote/ ready → live POST .../revisions/<id>/archive/ → archived POST .../revisions/<id>/freeze/ draft → ready (stamps sha256) POST .../revisions/<id>/clone_from/ copy bundle from another rev POST .../revisions/new_draft/ create draft + clone_from atomically Bundle authoring (proxied to the janitor): GET .../revisions/<id>/manifest/ list paths + sha256 GET .../revisions/<id>/file/?path=… read one file PUT .../revisions/<id>/file/?path=… write one file (draft) DELETE .../revisions/<id>/file/?path=… delete one file (draft) GET .../revisions/<id>/bundle/ bulk pull all files PUT .../revisions/<id>/bundle/ bulk push (replace|merge) */
export const agentApplicationsRevisionsRetrieve: API.OperationMethod<
  AgentApplicationsRevisionsRetrieveRequest,
  AgentRevision,
  AgentApplicationsRevisionsRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsRetrieveRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsSetEnvCreateError = PosthogOpError;
/** Replace this revision's encrypted env block. The body is `{ "env": { "<KEY>": "<value>", ... } }`. The encrypted text is stored on `AgentRevision.encrypted_env`; the worker decrypts it at session start via the same Fernet schedule (see agent-shared/src/runtime/encryption.ts). */
export const agentApplicationsRevisionsSetEnvCreate: API.OperationMethod<
  AgentApplicationsRevisionsSetEnvCreateRequest,
  AgentRevision,
  AgentApplicationsRevisionsSetEnvCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsSetEnvCreateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsSkillRefsUpdateError = PosthogOpError;
/** Full-replace the draft's store-skill references. They are resolved and materialized into the bundle at freeze, not here — this only records which skills (and pinned versions) the freeze should pull in. */
export const agentApplicationsRevisionsSkillRefsUpdate: API.OperationMethod<
  AgentApplicationsRevisionsSkillRefsUpdateRequest,
  AgentRevision,
  AgentApplicationsRevisionsSkillRefsUpdateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsSkillRefsUpdateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsSlackManifestError = PosthogOpError;
/** Build a Slack app manifest for this revision's slack trigger. Deterministic: the OAuth scopes and bot event subscriptions are derived from the slack trigger config (`mention_only` / `auto_resume_threads` / `ack_reaction`) and the agent's Slack tools, so the manifest already subscribes to exactly the events the config needs. 400 if the revision has no slack trigger. */
export const agentApplicationsRevisionsSlackManifest: API.OperationMethod<
  AgentApplicationsRevisionsSlackManifestRequest,
  AgentRevisionSlackManifestResponse,
  AgentApplicationsRevisionsSlackManifestError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsSlackManifestRequest,
  output: AgentRevisionSlackManifestResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsSpecUpdateError = PosthogOpError;
/** Revisions of an agent. Created in `draft`, promoted through `ready → live` once the bundle has been uploaded + frozen. URLs (nested under an application): Model CRUD: GET .../revisions/ list POST .../revisions/ create draft GET .../revisions/<id>/ retrieve PATCH .../revisions/<id>/ update spec (draft only) Lifecycle: POST .../revisions/<id>/promote/ ready → live POST .../revisions/<id>/archive/ → archived POST .../revisions/<id>/freeze/ draft → ready (stamps sha256) POST .../revisions/<id>/clone_from/ copy bundle from another rev POST .../revisions/new_draft/ create draft + clone_from atomically Bundle authoring (proxied to the janitor): GET .../revisions/<id>/manifest/ list paths + sha256 GET .../revisions/<id>/file/?path=… read one file PUT .../revisions/<id>/file/?path=… write one file (draft) DELETE .../revisions/<id>/file/?path=… delete one file (draft) GET .../revisions/<id>/bundle/ bulk pull all files PUT .../revisions/<id>/bundle/ bulk push (replace|merge) */
export const agentApplicationsRevisionsSpecUpdate: API.OperationMethod<
  AgentApplicationsRevisionsSpecUpdateRequest,
  AgentRevision,
  AgentApplicationsRevisionsSpecUpdateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsSpecUpdateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsSystemPromptError = PosthogOpError;
/** Return the fully-assembled system prompt for this revision. Authoring tools call this to preview what the model will actually see at session start — the platform framework preamble plus the bundle's `agent.md` plus the skills index. Useful for debugging author-vs-framework precedence conflicts and verifying `spec.framework_prompt.omit` overrides took effect. */
export const agentApplicationsRevisionsSystemPrompt: API.OperationMethod<
  AgentApplicationsRevisionsSystemPromptRequest,
  AgentRevisionSystemPromptResponse,
  AgentApplicationsRevisionsSystemPromptError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsSystemPromptRequest,
  output: AgentRevisionSystemPromptResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsToolsDestroyError = PosthogOpError;
/** Revisions of an agent. Created in `draft`, promoted through `ready → live` once the bundle has been uploaded + frozen. URLs (nested under an application): Model CRUD: GET .../revisions/ list POST .../revisions/ create draft GET .../revisions/<id>/ retrieve PATCH .../revisions/<id>/ update spec (draft only) Lifecycle: POST .../revisions/<id>/promote/ ready → live POST .../revisions/<id>/archive/ → archived POST .../revisions/<id>/freeze/ draft → ready (stamps sha256) POST .../revisions/<id>/clone_from/ copy bundle from another rev POST .../revisions/new_draft/ create draft + clone_from atomically Bundle authoring (proxied to the janitor): GET .../revisions/<id>/manifest/ list paths + sha256 GET .../revisions/<id>/file/?path=… read one file PUT .../revisions/<id>/file/?path=… write one file (draft) DELETE .../revisions/<id>/file/?path=… delete one file (draft) GET .../revisions/<id>/bundle/ bulk pull all files PUT .../revisions/<id>/bundle/ bulk push (replace|merge) */
export const agentApplicationsRevisionsToolsDestroy: API.OperationMethod<
  AgentApplicationsRevisionsToolsDestroyRequest,
  AgentApplicationsRevisionsToolsDestroyResponse,
  AgentApplicationsRevisionsToolsDestroyError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsToolsDestroyRequest,
  output: AgentApplicationsRevisionsToolsDestroyResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsToolsDryRunCreateError = PosthogOpError;
/** Execute one persisted custom tool in a single-shot sandbox. Authoring loop's "test this tool" button. The tool's source must already be PUT (compiled.js is what runs); this just invokes it with the caller-supplied args and a stubbed ctx. No real secrets leave Django — `mock_secrets` is a `{name → placeholder}` map. */
export const agentApplicationsRevisionsToolsDryRunCreate: API.OperationMethod<
  AgentApplicationsRevisionsToolsDryRunCreateRequest,
  AgentRevisionDryRunToolResponse,
  AgentApplicationsRevisionsToolsDryRunCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsToolsDryRunCreateRequest,
  output: AgentRevisionDryRunToolResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsToolsUpdateError = PosthogOpError;
/** Revisions of an agent. Created in `draft`, promoted through `ready → live` once the bundle has been uploaded + frozen. URLs (nested under an application): Model CRUD: GET .../revisions/ list POST .../revisions/ create draft GET .../revisions/<id>/ retrieve PATCH .../revisions/<id>/ update spec (draft only) Lifecycle: POST .../revisions/<id>/promote/ ready → live POST .../revisions/<id>/archive/ → archived POST .../revisions/<id>/freeze/ draft → ready (stamps sha256) POST .../revisions/<id>/clone_from/ copy bundle from another rev POST .../revisions/new_draft/ create draft + clone_from atomically Bundle authoring (proxied to the janitor): GET .../revisions/<id>/manifest/ list paths + sha256 GET .../revisions/<id>/file/?path=… read one file PUT .../revisions/<id>/file/?path=… write one file (draft) DELETE .../revisions/<id>/file/?path=… delete one file (draft) GET .../revisions/<id>/bundle/ bulk pull all files PUT .../revisions/<id>/bundle/ bulk push (replace|merge) */
export const agentApplicationsRevisionsToolsUpdate: API.OperationMethod<
  AgentApplicationsRevisionsToolsUpdateRequest,
  AgentRevision,
  AgentApplicationsRevisionsToolsUpdateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsToolsUpdateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsUpdateError = PosthogOpError;
/** Spec edits are only allowed while state='draft'. Once promoted to ready/live the spec is frozen — change requires a new revision. */
export const agentApplicationsRevisionsUpdate: API.OperationMethod<
  AgentApplicationsRevisionsUpdateRequest,
  AgentRevision,
  AgentApplicationsRevisionsUpdateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsUpdateRequest,
  output: AgentRevision,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsRevisionsValidateCreateError = PosthogOpError;
/** Pre-flight checks before freeze + promote: agent.md exists, every native tool id is registered, every custom tool has its compiled.js + schema.json, every skill path exists, every declared secret has a value set in this revision's env block. Returns `{ ok, errors: [...] }`. Works on any revision state. */
export const agentApplicationsRevisionsValidateCreate: API.OperationMethod<
  AgentApplicationsRevisionsValidateCreateRequest,
  AgentRevisionValidateResponse,
  AgentApplicationsRevisionsValidateCreateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsRevisionsValidateCreateRequest,
  output: AgentRevisionValidateResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsSendError = PosthogOpError;
/** Append a message to an existing LIVE session and re-queue it. Bridges to ingress `POST /agents/<slug>/send`, forwarding the caller's PAT so the ACL principal-match passes. A `completed` session is NOT terminal — it's a per-turn idle state for a multi-turn agent, so send re-queues it for another turn; only truly-terminal states (failed / cancelled / closed) 410, which passes through as a 410. A janitor ownership pre-check runs first, but it's redundant defense-in-depth (ingress `/send` already app-scopes the load), kept for a clean early 404. */
export const agentApplicationsSend: API.OperationMethod<
  AgentApplicationsSendRequest,
  AgentSendResponse,
  AgentApplicationsSendError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsSendRequest,
  output: AgentSendResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsSessionLogsError = PosthogOpError;
/** Read the runner's structured event log for one session from ClickHouse. Filters (limit / after / before / level / search) match the shared `LogEntryMixin` helper used by hog_function + hog_flow. */
export const agentApplicationsSessionLogs: API.OperationMethod<
  AgentApplicationsSessionLogsRequest,
  AgentApplicationSessionLogsResponse,
  AgentApplicationsSessionLogsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsSessionLogsRequest,
  output: AgentApplicationSessionLogsResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsSessionsListError = PosthogOpError;
/** List sessions for this application, most recently active first. Strips the conversation transcript from each summary, but includes a `preview` (last assistant text, ~120 chars) and `usage_total` (token + cost aggregate). Use `agent-applications-sessions-retrieve` for the full transcript of a single session. */
export const agentApplicationsSessionsList: API.OperationMethod<
  AgentApplicationsSessionsListRequest,
  AgentApplicationSessionsListResponse,
  AgentApplicationsSessionsListError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsSessionsListRequest,
  output: AgentApplicationSessionsListResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsSessionsRetrieveError = PosthogOpError;
/** Fetch one session's state — full conversation by default, or just the trailing N messages with `?last_n=`. Always returns a `usage_total` block aggregated over the entire session, regardless of trim. The runner-side queue DB is the source of truth. */
export const agentApplicationsSessionsRetrieve: API.OperationMethod<
  AgentApplicationsSessionsRetrieveRequest,
  AgentApplicationSessionsRetrieveResponse,
  AgentApplicationsSessionsRetrieveError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsSessionsRetrieveRequest,
  output: AgentApplicationSessionsRetrieveResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsSpecSchemaError = PosthogOpError;
/** The canonical JSON Schema for an agent `spec` — every field, type, enum, default, and the discriminated unions for `models` / `triggers[]` / `tools[]`, each with an inline description. Emitted from the same source the runner validates against (fields with a default are optional on write), so read it BEFORE composing a spec for create / revisions-spec-update instead of guessing the shape. Pass `section` to fetch just one part. */
export const agentApplicationsSpecSchema: API.OperationMethod<
  AgentApplicationsSpecSchemaRequest,
  AgentApplication,
  AgentApplicationsSpecSchemaError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsSpecSchemaRequest,
  output: AgentApplication,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsStatsError = PosthogOpError;
/** Roll-up stats for the agent — drives the agent-detail overview tiles. */
export const agentApplicationsStats: API.OperationMethod<
  AgentApplicationsStatsRequest,
  AgentAggregateStats,
  AgentApplicationsStatsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsStatsRequest,
  output: AgentAggregateStats,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsUpdateError = PosthogOpError;
/** Agent applications — the deployable unit of the platform. URLs: GET /api/projects/<team>/agent_applications/ list POST /api/projects/<team>/agent_applications/ create GET /api/projects/<team>/agent_applications/<id|slug>/ retrieve PATCH /api/projects/<team>/agent_applications/<id|slug>/ update DELETE /api/projects/<team>/agent_applications/<id|slug>/ archive POST /api/projects/<team>/agent_applications/<id|slug>/set_env/ bulk replace env GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/ list set keys GET /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ is one key set? PUT /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ set one key DELETE /api/projects/<team>/agent_applications/<id|slug>/env_keys/<KEY>/ clear one key */
export const agentApplicationsUpdate: API.OperationMethod<
  AgentApplicationsUpdateRequest,
  AgentApplication,
  AgentApplicationsUpdateError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsUpdateRequest,
  output: AgentApplication,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsUsersConnectionDeleteError = PosthogOpError;
/** Revoke one of an end-user's linked connections. The credential is marked revoked (kept for audit), so the agent can no longer act as that user on the provider. */
export const agentApplicationsUsersConnectionDelete: API.OperationMethod<
  AgentApplicationsUsersConnectionDeleteRequest,
  AgentApplicationsUsersConnectionDeleteResponse,
  AgentApplicationsUsersConnectionDeleteError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsUsersConnectionDeleteRequest,
  output: AgentApplicationsUsersConnectionDeleteResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentApplicationsUsersListError = PosthogOpError;
/** List this agent's end-users (the stable identities behind inbound principals) and each user's linked external connections. Connection metadata only — credential material is never returned. */
export const agentApplicationsUsersList: API.OperationMethod<
  AgentApplicationsUsersListRequest,
  AgentUsersList,
  AgentApplicationsUsersListError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentApplicationsUsersListRequest,
  output: AgentUsersList,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentFleetApprovalsListError = PosthogOpError;
/** Approval-gated tool requests across every agent in this team. Team-admin only. */
export const agentFleetApprovalsList: API.OperationMethod<
  AgentFleetApprovalsListRequest,
  AgentFleetApprovalsListResponse,
  AgentFleetApprovalsListError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentFleetApprovalsListRequest,
  output: AgentFleetApprovalsListResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentFleetLiveSessionsError = PosthogOpError;
/** Live (non-terminal) sessions across every agent owned by this team, newest activity first. */
export const agentFleetLiveSessions: API.OperationMethod<
  AgentFleetLiveSessionsRequest,
  AgentFleetLiveSessionsResponse,
  AgentFleetLiveSessionsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentFleetLiveSessionsRequest,
  output: AgentFleetLiveSessionsResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentFleetStatsError = PosthogOpError;
/** Roll-up stats across every agent owned by this team. */
export const agentFleetStats: API.OperationMethod<
  AgentFleetStatsRequest,
  AgentAggregateStats,
  AgentFleetStatsError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentFleetStatsRequest,
  output: AgentAggregateStats,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentMemoryCreateFileError = PosthogOpError;
/** Create a memory file. Fails if the path already exists — use the update endpoint to overwrite. */
export const agentMemoryCreateFile: API.OperationMethod<
  AgentMemoryCreateFileRequest,
  AgentMemoryFile,
  AgentMemoryCreateFileError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentMemoryCreateFileRequest,
  output: AgentMemoryFile,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentMemoryDeleteFileError = PosthogOpError;
/** Hard-delete a memory file. Activity log captures the action against the agent. */
export const agentMemoryDeleteFile: API.OperationMethod<
  AgentMemoryDeleteFileRequest,
  AgentMemoryDeleteFileResponse,
  AgentMemoryDeleteFileError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentMemoryDeleteFileRequest,
  output: AgentMemoryDeleteFileResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentMemoryGetFileError = PosthogOpError;
/** Read one memory file in full (frontmatter + markdown body). */
export const agentMemoryGetFile: API.OperationMethod<
  AgentMemoryGetFileRequest,
  AgentMemoryFile,
  AgentMemoryGetFileError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentMemoryGetFileRequest,
  output: AgentMemoryFile,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentMemoryListFilesError = PosthogOpError;
/** List memory file headers under the agent's prefix. Headers only — no bodies. */
export const agentMemoryListFiles: API.OperationMethod<
  AgentMemoryListFilesRequest,
  AgentMemoryListResponse,
  AgentMemoryListFilesError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentMemoryListFilesRequest,
  output: AgentMemoryListResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentMemoryListTablesError = PosthogOpError;
/** List the agent's tabular-reference tables (the @posthog/table-* JSONL tables): name + byte size. */
export const agentMemoryListTables: API.OperationMethod<
  AgentMemoryListTablesRequest,
  AgentTablesListResponse,
  AgentMemoryListTablesError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentMemoryListTablesRequest,
  output: AgentTablesListResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentMemoryReadTableError = PosthogOpError;
/** Read rows from one tabular-reference table (capped via ?limit). */
export const agentMemoryReadTable: API.OperationMethod<
  AgentMemoryReadTableRequest,
  AgentTableRowsResponse,
  AgentMemoryReadTableError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentMemoryReadTableRequest,
  output: AgentTableRowsResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentMemorySearchError = PosthogOpError;
/** BM25 search across the agent's memory files. Ranks by description+tags+path+body with field weighting. */
export const agentMemorySearch: API.OperationMethod<
  AgentMemorySearchRequest,
  AgentMemorySearchResponse,
  AgentMemorySearchError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentMemorySearchRequest,
  output: AgentMemorySearchResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentMemoryTreeError = PosthogOpError;
/** Pre-aggregated folder tree of memory files. Saves the frontend re-derivation work. */
export const agentMemoryTree: API.OperationMethod<
  AgentMemoryTreeRequest,
  AgentMemoryTreeResponse,
  AgentMemoryTreeError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentMemoryTreeRequest,
  output: AgentMemoryTreeResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentMemoryUpdateFileError = PosthogOpError;
/** Update a memory file. Any field omitted is preserved from the existing file. */
export const agentMemoryUpdateFile: API.OperationMethod<
  AgentMemoryUpdateFileRequest,
  AgentMemoryFile,
  AgentMemoryUpdateFileError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentMemoryUpdateFileRequest,
  output: AgentMemoryFile,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentNativeToolsListError = PosthogOpError;
/** Read-only catalog of every @posthog/* native tool the runner knows. */
export const agentNativeToolsList: API.OperationMethod<
  AgentNativeToolsListRequest,
  AgentNativeToolsListResponse2,
  AgentNativeToolsListError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentNativeToolsListRequest,
  output: AgentNativeToolsListResponse2,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentRevisionsEnvKeysClearError = PosthogOpError;
/** GET / PUT / DELETE one secret by name on this revision. - `GET` → `{ key, is_set }` (never returns the value). - `PUT` → upserts `{ value }` into the env block. - `DELETE` → removes the key. No-op when it wasn't set. Per-method scope: GET is treated as a write action so the single action name maps to one consistent scope; reading whether a secret is set is restricted to writers in any case. */
export const agentRevisionsEnvKeysClear: API.OperationMethod<
  AgentRevisionsEnvKeysClearRequest,
  AgentRevisionsEnvKeysClearResponse,
  AgentRevisionsEnvKeysClearError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentRevisionsEnvKeysClearRequest,
  output: AgentRevisionsEnvKeysClearResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentRevisionsEnvKeysGetError = PosthogOpError;
/** GET / PUT / DELETE one secret by name on this revision. - `GET` → `{ key, is_set }` (never returns the value). - `PUT` → upserts `{ value }` into the env block. - `DELETE` → removes the key. No-op when it wasn't set. Per-method scope: GET is treated as a write action so the single action name maps to one consistent scope; reading whether a secret is set is restricted to writers in any case. */
export const agentRevisionsEnvKeysGet: API.OperationMethod<
  AgentRevisionsEnvKeysGetRequest,
  AgentRevisionEnvKeyStatus,
  AgentRevisionsEnvKeysGetError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentRevisionsEnvKeysGetRequest,
  output: AgentRevisionEnvKeyStatus,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentRevisionsEnvKeysListError = PosthogOpError;
/** List the names of secrets currently set on this revision. Returns names only — values stay server-side under `EncryptedTextField`. Use this to drive the "set / unset" badge next to a declared secret in the editor UI. */
export const agentRevisionsEnvKeysList: API.OperationMethod<
  AgentRevisionsEnvKeysListRequest,
  AgentRevisionEnvKeysResponse,
  AgentRevisionsEnvKeysListError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentRevisionsEnvKeysListRequest,
  output: AgentRevisionEnvKeysResponse,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));

export type AgentRevisionsEnvKeysSetError = PosthogOpError;
/** GET / PUT / DELETE one secret by name on this revision. - `GET` → `{ key, is_set }` (never returns the value). - `PUT` → upserts `{ value }` into the env block. - `DELETE` → removes the key. No-op when it wasn't set. Per-method scope: GET is treated as a write action so the single action name maps to one consistent scope; reading whether a secret is set is restricted to writers in any case. */
export const agentRevisionsEnvKeysSet: API.OperationMethod<
  AgentRevisionsEnvKeysSetRequest,
  AgentRevisionEnvKeyStatus,
  AgentRevisionsEnvKeysSetError,
  PosthogOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AgentRevisionsEnvKeysSetRequest,
  output: AgentRevisionEnvKeyStatus,
  errors: [],
  protocol: PosthogProtocol,
  retry: Retry.Retry,
}));
