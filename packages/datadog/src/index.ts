/**
 * @distilled.cloud/datadog — Datadog SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy models
 * in `.generated-specs` (written by `scripts/convert.ts` from Datadog's
 * official v1 OpenAPI description submodule, allowlisted tag-by-tag).
 * Everything else in this folder is hand-written.
 *
 * Service modules are also available under their own subpath imports (the
 * per-status error classes each module defines would collide on a root
 * spread):
 *
 * @example
 * ```ts
 * import * as Datadog from "@distilled.cloud/datadog";
 * import * as Monitors from "@distilled.cloud/datadog/monitors";
 *
 * const monitor = yield* Monitors.createMonitor({ ... });
 * const slos = yield* Datadog.Services.serviceLevelObjectives.listSLOs({});
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  DatadogProtocol,
  type DatadogOpError,
  type DatadogOpContext,
} from "./protocol.ts";
export * as Retry from "./retry.ts";
export * as Services from "./services/index.ts";
