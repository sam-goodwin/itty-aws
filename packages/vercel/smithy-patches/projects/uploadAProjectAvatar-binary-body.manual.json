{
  "description": "POST /v1/projects/{idOrName}/avatar takes the image bytes as an application/octet-stream body. The shared OpenAPI converter models application/json request bodies only, so the operation would otherwise send no image. Adds the whole-body member plus the media type the protocol sends it under.",
  "patches": [
    {
      "op": "add",
      "path": "/shapes/com.vercel.projects#UploadAProjectAvatar/traits/smithy.api#http/bodyMediaType",
      "value": "application/octet-stream"
    },
    {
      "op": "add",
      "path": "/shapes/com.vercel.projects#UploadAProjectAvatarRequest/members/body",
      "value": {
        "target": "smithy.api#Blob",
        "traits": {
          "smithy.api#httpPayload": {},
          "smithy.api#required": {},
          "smithy.api#documentation": "The avatar image bytes, sent verbatim as application/octet-stream."
        }
      }
    }
  ]
}
