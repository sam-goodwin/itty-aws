{
  "description": "project_routes.getRouteVersions (GET /v1/projects/{projectId}/routes/versions): the live API returns 404 `not_found` (\"Project not found.\") when the project no longer exists (probed 2026-08-13), but the spec declares no 404 — add it so the reconciler's observe step gets a typed NotFound.",
  "patches": [
    {
      "op": "add",
      "path": "/paths/~1v1~1projects~1{projectId}~1routes~1versions/get/responses/404",
      "value": { "description": "Project not found" }
    }
  ]
}
