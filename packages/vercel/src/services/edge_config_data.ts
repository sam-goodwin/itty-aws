// AUTO-GENERATED by scripts/generate.ts from manual-specs (hand-authored data-plane models). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  EdgeConfigDataProtocol,
  type VercelDataOpError,
  type VercelDataOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { VercelDataOpError, VercelDataOpContext };

/** 404 WITH the `x-edge-config-digest` response header — the config exists but the item does not. (`get` callers typically map this to `undefined`, `has` callers to `false`.) */
export class EdgeConfigItemNotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<EdgeConfigItemNotFound>()(
      "EdgeConfigItemNotFound",
      {
        code: S.Number,
        message: S.String,
      },
    ).pipe(C.withBadRequestError),
    [{ status: 404, header: "x-edge-config-digest" }],
  ) {}

/** Bare 404 (no `x-edge-config-digest` header) — the Edge Config itself does not exist or the token does not resolve to one. */
export class EdgeConfigNotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<EdgeConfigNotFound>()("EdgeConfigNotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** 401 — the read token was rejected. */
export class EdgeConfigUnauthorized
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<EdgeConfigUnauthorized>()(
      "EdgeConfigUnauthorized",
      {
        code: S.Number,
        message: S.String,
      },
    ).pipe(C.withAuthError),
    [{ status: 401 }],
  ) {}

export interface GetEdgeConfigDigestRequest {
  /** Read-plane origin — `https://edge-config.vercel.com` live, or the connection string's origin. */
  origin: string;
  /** The read token — sent as the bearer. */
  token: string | Redacted.Redacted<string>;
  /** The Edge Config id (`ecfg_…`). */
  edgeConfigId: string;
  /** Protocol version — pass "1". */
  version?: string;
}
export const GetEdgeConfigDigestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    origin: S.String.pipe(T.HostLabel()),
    token: S.String.pipe(T.Header("authorization"), T.SensitiveValue({})),
    edgeConfigId: S.String.pipe(T.Label()),
    version: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/{edgeConfigId}/digest", code: 200 })),
).annotate({
  identifier: "GetEdgeConfigDigestRequest",
}) as any as S.Schema<GetEdgeConfigDigestRequest>;

export type GetEdgeConfigDigestResponse = string;
export const GetEdgeConfigDigestResponse = /*@__PURE__*/ S.suspend(() =>
  S.String.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "GetEdgeConfigDigestResponse",
}) as any as S.Schema<GetEdgeConfigDigestResponse>;

export interface GetEdgeConfigItemRequest {
  /** Read-plane origin — `https://edge-config.vercel.com` live, or the connection string's origin (local emulation hands out `http://localhost:<port>`). */
  origin: string;
  /** The read token (the connection string's `token` parameter) — sent as the bearer. */
  token: string | Redacted.Redacted<string>;
  /** The Edge Config id (`ecfg_…`). */
  edgeConfigId: string;
  /** The item key. */
  key: string;
  /** Protocol version — pass "1" (what @vercel/edge-config sends). */
  version?: string;
}
export const GetEdgeConfigItemRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    origin: S.String.pipe(T.HostLabel()),
    token: S.String.pipe(T.Header("authorization"), T.SensitiveValue({})),
    edgeConfigId: S.String.pipe(T.Label()),
    key: S.String.pipe(T.Label()),
    version: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/{edgeConfigId}/item/{key}", code: 200 }),
  ),
).annotate({
  identifier: "GetEdgeConfigItemRequest",
}) as any as S.Schema<GetEdgeConfigItemRequest>;

export type GetEdgeConfigItemResponse = unknown;
export const GetEdgeConfigItemResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "GetEdgeConfigItemResponse",
}) as any as S.Schema<GetEdgeConfigItemResponse>;

export type EdgeConfigKeyList = Array<string>;
export const EdgeConfigKeyList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<EdgeConfigKeyList>;

export interface GetEdgeConfigItemsRequest {
  /** Read-plane origin — `https://edge-config.vercel.com` live, or the connection string's origin. */
  origin: string;
  /** The read token — sent as the bearer. */
  token: string | Redacted.Redacted<string>;
  /** The Edge Config id (`ecfg_…`). */
  edgeConfigId: string;
  /** Optional key filter — each entry repeats as a `key=` query parameter; omit for all items. */
  keys?: EdgeConfigKeyList;
  /** Protocol version — pass "1". */
  version?: string;
}
export const GetEdgeConfigItemsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    origin: S.String.pipe(T.HostLabel()),
    token: S.String.pipe(T.Header("authorization"), T.SensitiveValue({})),
    edgeConfigId: S.String.pipe(T.Label()),
    keys: S.optional(EdgeConfigKeyList.pipe(T.Query("key"))),
    version: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/{edgeConfigId}/items", code: 200 })),
).annotate({
  identifier: "GetEdgeConfigItemsRequest",
}) as any as S.Schema<GetEdgeConfigItemsRequest>;

/** Items as a key → JSON value record. */
export type EdgeConfigItems = { [key: string]: unknown | undefined };
export const EdgeConfigItems = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<EdgeConfigItems>;

export type GetEdgeConfigItemsResponse = EdgeConfigItems;
export const GetEdgeConfigItemsResponse = /*@__PURE__*/ S.suspend(() =>
  EdgeConfigItems.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "GetEdgeConfigItemsResponse",
}) as any as S.Schema<GetEdgeConfigItemsResponse>;

export interface HasEdgeConfigItemRequest {
  /** Read-plane origin — `https://edge-config.vercel.com` live, or the connection string's origin. */
  origin: string;
  /** The read token — sent as the bearer. */
  token: string | Redacted.Redacted<string>;
  /** The Edge Config id (`ecfg_…`). */
  edgeConfigId: string;
  /** The item key. */
  key: string;
  /** Protocol version — pass "1". */
  version?: string;
}
export const HasEdgeConfigItemRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    origin: S.String.pipe(T.HostLabel()),
    token: S.String.pipe(T.Header("authorization"), T.SensitiveValue({})),
    edgeConfigId: S.String.pipe(T.Label()),
    key: S.String.pipe(T.Label()),
    version: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "HEAD", uri: "/{edgeConfigId}/item/{key}", code: 200 }),
  ),
).annotate({
  identifier: "HasEdgeConfigItemRequest",
}) as any as S.Schema<HasEdgeConfigItemRequest>;

/** A 200 means the item exists; a missing item is the typed EdgeConfigItemNotFound (map it to `false`). */
export interface HasEdgeConfigItemResponse {}
export const HasEdgeConfigItemResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "HasEdgeConfigItemResponse",
}) as any as S.Schema<HasEdgeConfigItemResponse>;

export type GetEdgeConfigDigestError =
  | EdgeConfigNotFound
  | EdgeConfigUnauthorized
  | VercelDataOpError;
/** The config's content digest: `GET {origin}/{edgeConfigId}/digest?version=1`. */
export const getEdgeConfigDigest: API.OperationMethod<
  GetEdgeConfigDigestRequest,
  GetEdgeConfigDigestResponse,
  GetEdgeConfigDigestError,
  VercelDataOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetEdgeConfigDigestRequest,
  output: GetEdgeConfigDigestResponse,
  errors: [EdgeConfigNotFound, EdgeConfigUnauthorized],
  protocol: EdgeConfigDataProtocol,
  retry: Retry.Retry,
}));

export type GetEdgeConfigItemError =
  | EdgeConfigItemNotFound
  | EdgeConfigNotFound
  | EdgeConfigUnauthorized
  | VercelDataOpError;
/** Read a single item: `GET {origin}/{edgeConfigId}/item/{key}?version=1`. A missing item is the typed EdgeConfigItemNotFound (map it to `undefined`). */
export const getEdgeConfigItem: API.OperationMethod<
  GetEdgeConfigItemRequest,
  GetEdgeConfigItemResponse,
  GetEdgeConfigItemError,
  VercelDataOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetEdgeConfigItemRequest,
  output: GetEdgeConfigItemResponse,
  errors: [EdgeConfigItemNotFound, EdgeConfigNotFound, EdgeConfigUnauthorized],
  protocol: EdgeConfigDataProtocol,
  retry: Retry.Retry,
}));

export type GetEdgeConfigItemsError =
  | EdgeConfigNotFound
  | EdgeConfigUnauthorized
  | VercelDataOpError;
/** Read all items (or a key subset): `GET {origin}/{edgeConfigId}/items?version=1[&key=…]`. */
export const getEdgeConfigItems: API.OperationMethod<
  GetEdgeConfigItemsRequest,
  GetEdgeConfigItemsResponse,
  GetEdgeConfigItemsError,
  VercelDataOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetEdgeConfigItemsRequest,
  output: GetEdgeConfigItemsResponse,
  errors: [EdgeConfigNotFound, EdgeConfigUnauthorized],
  protocol: EdgeConfigDataProtocol,
  retry: Retry.Retry,
}));

export type HasEdgeConfigItemError =
  | EdgeConfigItemNotFound
  | EdgeConfigNotFound
  | EdgeConfigUnauthorized
  | VercelDataOpError;
/** Existence check: `HEAD {origin}/{edgeConfigId}/item/{key}?version=1` — 200 exists, typed EdgeConfigItemNotFound otherwise. */
export const hasEdgeConfigItem: API.OperationMethod<
  HasEdgeConfigItemRequest,
  HasEdgeConfigItemResponse,
  HasEdgeConfigItemError,
  VercelDataOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: HasEdgeConfigItemRequest,
  output: HasEdgeConfigItemResponse,
  errors: [EdgeConfigItemNotFound, EdgeConfigNotFound, EdgeConfigUnauthorized],
  protocol: EdgeConfigDataProtocol,
  retry: Retry.Retry,
}));
