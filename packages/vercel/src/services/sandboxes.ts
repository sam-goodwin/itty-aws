// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs — the vercel.com/docs/rest-api mirror). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  VercelProtocol,
  type VercelOpError,
  type VercelOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { VercelOpError, VercelOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class Gone
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Gone>()("Gone", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 410 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class PaymentRequired
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<PaymentRequired>()("PaymentRequired", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withQuotaError),
    [{ status: 402 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface CreateADirectoryRequest {
  /** The unique identifier of the session to create the directory in. */
  sessionId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  /** The base directory for resolving relative paths. If not specified, paths are resolved from the sandbox home directory. */
  cwd?: string;
  /** The path of the directory to create. Can be absolute or relative to the working directory. */
  path: string;
  /** If true, creates parent directories as needed (like `mkdir -p`). If false, fails if parent directories do not exist. */
  recursive?: boolean;
}
export const CreateADirectoryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    cwd: S.optional(S.String),
    path: S.String,
    recursive: S.optional(S.Boolean),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/sandboxes/sessions/{sessionId}/fs/mkdir",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateADirectoryRequest",
}) as any as S.Schema<CreateADirectoryRequest>;

export type CreateADirectoryResponse = unknown;
export const CreateADirectoryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "CreateADirectoryResponse",
}) as any as S.Schema<CreateADirectoryResponse>;

/** The network access policy mode. Use \"allow-all\" to permit all outbound traffic. Use \"deny-all\" to block all outbound traffic. Use \"custom\" to specify explicit allow/deny rules. */
export type CreateANamedSandboxRequestNetworkPolicyCase0Mode =
  | "allow-all"
  | "deny-all"
  | "custom"
  | "default-allow"
  | "default-deny";
export const CreateANamedSandboxRequestNetworkPolicyCase0Mode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Only applies when mode is \"custom\". Supports wildcard patterns (e.g., \"*.example.com\" matches all subdomains). */
export type CreateANamedSandboxRequestNetworkPolicyCase0AllowedDomainsList =
  Array<unknown>;
export const CreateANamedSandboxRequestNetworkPolicyCase0AllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxRequestNetworkPolicyCase0AllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. Traffic to these addresses bypasses domain-based restrictions. */
export type CreateANamedSandboxRequestNetworkPolicyCase0AllowedCIDRsList =
  Array<unknown>;
export const CreateANamedSandboxRequestNetworkPolicyCase0AllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxRequestNetworkPolicyCase0AllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type CreateANamedSandboxRequestNetworkPolicyCase0DeniedCIDRsList =
  Array<unknown>;
export const CreateANamedSandboxRequestNetworkPolicyCase0DeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxRequestNetworkPolicyCase0DeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. Traffic to matching domains will be intercepted instead of proxied through encrypted connections. */
export type CreateANamedSandboxRequestNetworkPolicyCase0InjectionRulesList =
  Array<unknown>;
export const CreateANamedSandboxRequestNetworkPolicyCase0InjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxRequestNetworkPolicyCase0InjectionRulesList>;

/** Network access policy for the sandbox.\n Controls which external hosts the sandbox can communicate with.\n Use \"allow-all\" mode to allow all traffic, \"deny-all\" to block all traffic or \"custom\" to provide specific rules. */
export interface CreateANamedSandboxRequestNetworkPolicyCase0 {
  /** The network access policy mode. Use \"allow-all\" to permit all outbound traffic. Use \"deny-all\" to block all outbound traffic. Use \"custom\" to specify explicit allow/deny rules. */
  mode: CreateANamedSandboxRequestNetworkPolicyCase0Mode | (string & {});
  /** List of domain names the sandbox is allowed to connect to. Only applies when mode is \"custom\". Supports wildcard patterns (e.g., \"*.example.com\" matches all subdomains). */
  allowedDomains?: CreateANamedSandboxRequestNetworkPolicyCase0AllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. Traffic to these addresses bypasses domain-based restrictions. */
  allowedCIDRs?: CreateANamedSandboxRequestNetworkPolicyCase0AllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: CreateANamedSandboxRequestNetworkPolicyCase0DeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. Traffic to matching domains will be intercepted instead of proxied through encrypted connections. */
  injectionRules?: CreateANamedSandboxRequestNetworkPolicyCase0InjectionRulesList;
}
export const CreateANamedSandboxRequestNetworkPolicyCase0 =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: CreateANamedSandboxRequestNetworkPolicyCase0Mode,
      allowedDomains: S.optional(
        CreateANamedSandboxRequestNetworkPolicyCase0AllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        CreateANamedSandboxRequestNetworkPolicyCase0AllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        CreateANamedSandboxRequestNetworkPolicyCase0DeniedCIDRsList,
      ),
      injectionRules: S.optional(
        CreateANamedSandboxRequestNetworkPolicyCase0InjectionRulesList,
      ),
    }),
  ).annotate({
    identifier: "CreateANamedSandboxRequestNetworkPolicyCase0",
  }) as any as S.Schema<CreateANamedSandboxRequestNetworkPolicyCase0>;

export interface CreateANamedSandboxRequestNetworkPolicyCase1 {
  allow?: unknown;
  subnets?: unknown;
}
export const CreateANamedSandboxRequestNetworkPolicyCase1 =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      allow: S.optional(S.Unknown),
      subnets: S.optional(S.Unknown),
    }),
  ).annotate({
    identifier: "CreateANamedSandboxRequestNetworkPolicyCase1",
  }) as any as S.Schema<CreateANamedSandboxRequestNetworkPolicyCase1>;

export type CreateANamedSandboxRequestNetworkPolicy =
  | CreateANamedSandboxRequestNetworkPolicyCase0
  | CreateANamedSandboxRequestNetworkPolicyCase1;
export const CreateANamedSandboxRequestNetworkPolicy =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateANamedSandboxRequestNetworkPolicy>;

/** The runtime environment for the sandbox. Determines the pre-installed language runtimes and tools available. */
export type CreateANamedSandboxRequestRuntime =
  | "node22"
  | "node24"
  | "node26"
  | "python3.13";
export const CreateANamedSandboxRequestRuntime = /*@__PURE__*/ S.String;

/** Resources to define the VM */
export interface CreateANamedSandboxRequestResources {
  /** The number of virtual CPUs to allocate to the sandbox. Must be 1, or an even number. */
  vcpus?: number;
  /** The amount of memory in megabytes to allocate to the sandbox. Must equal vcpus * 2048. */
  memory?: number;
}
export const CreateANamedSandboxRequestResources = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vcpus: S.optional(S.Number),
    memory: S.optional(S.Number),
  }),
).annotate({
  identifier: "CreateANamedSandboxRequestResources",
}) as any as S.Schema<CreateANamedSandboxRequestResources>;

/** Clone a Git repository into the sandbox. */
export interface CreateANamedSandboxRequestSourceCase0 {
  /** Indicates the source is a Git repository. */
  type: unknown;
  /** The URL of the Git repository to clone. */
  url: string;
  /** Username for Git authentication. Required together with password for private repositories. */
  username?: string;
  /** Password or personal access token for Git authentication. Required together with username for private repositories. */
  password?: string | Redacted.Redacted<string>;
  /** Create a shallow clone with history truncated to the specified number of commits. Useful for faster cloning of large repositories. */
  depth?: number;
  /** The specific commit SHA, branch name, or tag to checkout after cloning. */
  revision?: string;
}
export const CreateANamedSandboxRequestSourceCase0 = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      type: S.Unknown,
      url: S.String,
      username: S.optional(S.String),
      password: S.optional(S.String.pipe(T.SensitiveValue({}))),
      depth: S.optional(S.Number),
      revision: S.optional(S.String),
    }),
).annotate({
  identifier: "CreateANamedSandboxRequestSourceCase0",
}) as any as S.Schema<CreateANamedSandboxRequestSourceCase0>;

/** Download and extract a gzipped tarball into the sandbox. */
export interface CreateANamedSandboxRequestSourceCase1 {
  /** Indicates the source is a tarball. */
  type: unknown;
  /** URL of a gzipped tarball (.tar.gz) to download and extract. */
  url: string;
}
export const CreateANamedSandboxRequestSourceCase1 = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      type: S.Unknown,
      url: S.String,
    }),
).annotate({
  identifier: "CreateANamedSandboxRequestSourceCase1",
}) as any as S.Schema<CreateANamedSandboxRequestSourceCase1>;

/** Restore the sandbox filesystem from an existing snapshot. */
export interface CreateANamedSandboxRequestSourceCase2 {
  /** Indicates the source is a snapshot. */
  type: unknown;
  /** The unique identifier of the snapshot to restore. */
  snapshotId: string;
}
export const CreateANamedSandboxRequestSourceCase2 = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      type: S.Unknown,
      snapshotId: S.String,
    }),
).annotate({
  identifier: "CreateANamedSandboxRequestSourceCase2",
}) as any as S.Schema<CreateANamedSandboxRequestSourceCase2>;

/** The source from which to initialize the sandbox filesystem. Can be a Git repository, a tarball URL, or an existing snapshot. */
export type CreateANamedSandboxRequestSource =
  | CreateANamedSandboxRequestSourceCase0
  | CreateANamedSandboxRequestSourceCase1
  | CreateANamedSandboxRequestSourceCase2;
export const CreateANamedSandboxRequestSource =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateANamedSandboxRequestSource>;

/** List of ports to expose from the sandbox. Each port will be accessible via a unique URL. Maximum of 15 ports can be exposed. */
export type CreateANamedSandboxRequestPortsList = Array<number>;
export const CreateANamedSandboxRequestPortsList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<CreateANamedSandboxRequestPortsList>;

/** Default environment variables for the sandbox. These are inherited by all commands unless overridden. */
export type CreateANamedSandboxRequestEnvMap = {
  [key: string]: string | undefined;
};
export const CreateANamedSandboxRequestEnvMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateANamedSandboxRequestEnvMap>;

/** Mount the drive read-only or read-write. */
export type CreateANamedSandboxRequestMountsValueMode =
  | "read-only"
  | "read-write";
export const CreateANamedSandboxRequestMountsValueMode = /*@__PURE__*/ S.String;

export interface CreateANamedSandboxRequestMountsValue {
  /** Name of the drive to mount. The drive must already exist. */
  drive: string;
  /** Mount the drive read-only or read-write. */
  mode?: CreateANamedSandboxRequestMountsValueMode | (string & {});
}
export const CreateANamedSandboxRequestMountsValue = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      drive: S.String,
      mode: S.optional(CreateANamedSandboxRequestMountsValueMode),
    }),
).annotate({
  identifier: "CreateANamedSandboxRequestMountsValue",
}) as any as S.Schema<CreateANamedSandboxRequestMountsValue>;

/** List of drives to mount to the sandbox at the provided path. */
export type CreateANamedSandboxRequestMountsMap = {
  [key: string]: CreateANamedSandboxRequestMountsValue | undefined;
};
export const CreateANamedSandboxRequestMountsMap = /*@__PURE__*/ S.Record(
  S.String,
  CreateANamedSandboxRequestMountsValue,
) as any as S.Schema<CreateANamedSandboxRequestMountsMap>;

/** Default snapshot expiration time in milliseconds. Set to 0 to disable expiration. When set, this value is used as the default expiration for all snapshots created for this sandbox. */
export type CreateANamedSandboxRequestSnapshotExpiration = unknown | number;
export const CreateANamedSandboxRequestSnapshotExpiration =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateANamedSandboxRequestSnapshotExpiration>;

/** Expiration time in milliseconds for kept snapshots. Falls back to snapshotExpiration. */
export type CreateANamedSandboxRequestKeepLastSnapshotsExpiration =
  | unknown
  | number;
export const CreateANamedSandboxRequestKeepLastSnapshotsExpiration =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateANamedSandboxRequestKeepLastSnapshotsExpiration>;

/** Protect the N most recent snapshots with different expiration/deletion behavior. */
export interface CreateANamedSandboxRequestKeepLastSnapshots {
  /** Number of most recent snapshots to keep. */
  count: number;
  /** Expiration time in milliseconds for kept snapshots. Falls back to snapshotExpiration. */
  expiration?: CreateANamedSandboxRequestKeepLastSnapshotsExpiration;
  /** Whether to immediately delete evicted snapshots. Defaults to true. */
  deleteEvicted?: boolean;
}
export const CreateANamedSandboxRequestKeepLastSnapshots =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      count: S.Number,
      expiration: S.optional(
        CreateANamedSandboxRequestKeepLastSnapshotsExpiration,
      ),
      deleteEvicted: S.optional(S.Boolean),
    }),
  ).annotate({
    identifier: "CreateANamedSandboxRequestKeepLastSnapshots",
  }) as any as S.Schema<CreateANamedSandboxRequestKeepLastSnapshots>;

/** Key-value tags to associate with the sandbox. Maximum 5 tags. */
export type CreateANamedSandboxRequestTagsMap = {
  [key: string]: string | undefined;
};
export const CreateANamedSandboxRequestTagsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateANamedSandboxRequestTagsMap>;

export interface CreateANamedSandboxRequest {
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  networkPolicy?: CreateANamedSandboxRequestNetworkPolicy;
  /** The runtime environment for the sandbox. Determines the pre-installed language runtimes and tools available. */
  runtime?: CreateANamedSandboxRequestRuntime | (string & {});
  /** Resources to define the VM */
  resources?: CreateANamedSandboxRequestResources;
  /** The source from which to initialize the sandbox filesystem. Can be a Git repository, a tarball URL, or an existing snapshot. */
  source?: CreateANamedSandboxRequestSource;
  /** The target project slug or ID in which the sandbox will be assigned to. */
  projectId?: string;
  /** List of ports to expose from the sandbox. Each port will be accessible via a unique URL. Maximum of 15 ports can be exposed. */
  ports?: CreateANamedSandboxRequestPortsList;
  /** Image to use for the sandbox. */
  image?: string;
  /** Maximum duration in milliseconds that the sandbox can run before being automatically stopped. */
  timeout?: number;
  /** Default environment variables for the sandbox. These are inherited by all commands unless overridden. */
  env?: CreateANamedSandboxRequestEnvMap;
  /** List of drives to mount to the sandbox at the provided path. */
  mounts?: CreateANamedSandboxRequestMountsMap;
  /** Name for the sandbox. Must be unique per project and URL-safe (alphanumeric, hyphens, underscores). */
  name?: string;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. Defaults to true. */
  persistent?: boolean;
  /** Default snapshot expiration time in milliseconds. Set to 0 to disable expiration. When set, this value is used as the default expiration for all snapshots created for this sandbox. */
  snapshotExpiration?: CreateANamedSandboxRequestSnapshotExpiration;
  /** Protect the N most recent snapshots with different expiration/deletion behavior. */
  keepLastSnapshots?: CreateANamedSandboxRequestKeepLastSnapshots;
  /** Key-value tags to associate with the sandbox. Maximum 5 tags. */
  tags?: CreateANamedSandboxRequestTagsMap;
}
export const CreateANamedSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    networkPolicy: S.optional(CreateANamedSandboxRequestNetworkPolicy),
    runtime: S.optional(CreateANamedSandboxRequestRuntime),
    resources: S.optional(CreateANamedSandboxRequestResources),
    source: S.optional(CreateANamedSandboxRequestSource),
    projectId: S.optional(S.String),
    ports: S.optional(CreateANamedSandboxRequestPortsList),
    image: S.optional(S.String),
    timeout: S.optional(S.Number),
    env: S.optional(CreateANamedSandboxRequestEnvMap),
    mounts: S.optional(CreateANamedSandboxRequestMountsMap),
    name: S.optional(S.String),
    persistent: S.optional(S.Boolean),
    snapshotExpiration: S.optional(
      CreateANamedSandboxRequestSnapshotExpiration,
    ),
    keepLastSnapshots: S.optional(CreateANamedSandboxRequestKeepLastSnapshots),
    tags: S.optional(CreateANamedSandboxRequestTagsMap),
  }).pipe(T.Http({ method: "POST", uri: "/v2/sandboxes", code: 200 })),
).annotate({
  identifier: "CreateANamedSandboxRequest",
}) as any as S.Schema<CreateANamedSandboxRequest>;

/** The status of the current sandbox. */
export type CreateANamedSandboxResponseSandboxStatus =
  | "running"
  | "stopped"
  | "stopping";
export const CreateANamedSandboxResponseSandboxStatus = /*@__PURE__*/ S.String;

/** Keep-last snapshot configuration. */
export interface CreateANamedSandboxResponseSandboxKeepLastSnapshots {
  /** Number of most recent snapshots to keep. */
  count: number;
  /** Expiration time in milliseconds for kept snapshots. */
  expiration?: number;
  /** Whether to immediately delete evicted snapshots. */
  deleteEvicted: boolean;
}
export const CreateANamedSandboxResponseSandboxKeepLastSnapshots =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      count: S.Number,
      expiration: S.optional(S.Number),
      deleteEvicted: S.Boolean,
    }),
  ).annotate({
    identifier: "CreateANamedSandboxResponseSandboxKeepLastSnapshots",
  }) as any as S.Schema<CreateANamedSandboxResponseSandboxKeepLastSnapshots>;

export type CreateANamedSandboxResponseSandboxNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "default-allow"
  | "default-deny"
  | "deny-all";
export const CreateANamedSandboxResponseSandboxNetworkPolicyMode =
  /*@__PURE__*/ S.String;

export type CreateANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const CreateANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList>;

export type CreateANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const CreateANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList>;

export type CreateANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const CreateANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList>;

/** Network policy configuration. */
export interface CreateANamedSandboxResponseSandboxNetworkPolicy {
  mode: CreateANamedSandboxResponseSandboxNetworkPolicyMode;
  allowedDomains?: CreateANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList;
  allowedCIDRs?: CreateANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList;
  deniedCIDRs?: CreateANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList;
}
export const CreateANamedSandboxResponseSandboxNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: CreateANamedSandboxResponseSandboxNetworkPolicyMode,
      allowedDomains: S.optional(
        CreateANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        CreateANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        CreateANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList,
      ),
    }),
  ).annotate({
    identifier: "CreateANamedSandboxResponseSandboxNetworkPolicy",
  }) as any as S.Schema<CreateANamedSandboxResponseSandboxNetworkPolicy>;

/** Key-value tags attached to the named sandbox. */
export type CreateANamedSandboxResponseSandboxTagsMap = {
  [key: string]: string | undefined;
};
export const CreateANamedSandboxResponseSandboxTagsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateANamedSandboxResponseSandboxTagsMap>;

/** Key-value pairs of mount path and drive. */
export type CreateANamedSandboxResponseSandboxMountsMap = {
  [key: string]: unknown | undefined;
};
export const CreateANamedSandboxResponseSandboxMountsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxResponseSandboxMountsMap>;

/** This object contains information related to a Vercel NamedSandbox. */
export interface CreateANamedSandboxResponseSandbox {
  /** The unique identifier of the sandbox. */
  name: string;
  /** Current snapshot ID that the named sandbox is pointing to. */
  currentSnapshotId?: string;
  /** Current session ID the sandbox is pointing to. */
  currentSessionId: string;
  /** The status of the current sandbox. */
  status: CreateANamedSandboxResponseSandboxStatus;
  /** The time when the sandbox status was last updated, in milliseconds since the epoch. */
  statusUpdatedAt: number;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. */
  persistent: boolean;
  /** The region the sandbox is configured to run in: the region set on the sandbox, otherwise the project-level default, then the platform default. Where a running session actually landed is reported by `session.region`. */
  region?: string;
  /** Number of virtual CPUs allocated. */
  vcpus?: number;
  /** Memory allocated in MB. */
  memory?: number;
  /** Runtime identifier. */
  runtime?: string;
  /** Digest-pinned reference of the container image the sandbox was created from, when it was created from an image ("{repository}@{manifestDigest}"). */
  image?: string;
  /** Timeout in milliseconds. */
  timeout?: number;
  /** Default snapshot expiration time in milliseconds. 0 means no expiration. */
  snapshotExpiration?: number;
  /** Keep-last snapshot configuration. */
  keepLastSnapshots?: CreateANamedSandboxResponseSandboxKeepLastSnapshots;
  /** Network policy configuration. */
  networkPolicy?: CreateANamedSandboxResponseSandboxNetworkPolicy;
  /** Cumulative egress bytes across all sandbox runs. */
  totalEgressBytes?: number;
  /** Cumulative ingress bytes across all sandbox runs. */
  totalIngressBytes?: number;
  /** Cumulative active CPU duration in milliseconds across all sandbox runs. */
  totalActiveCpuDurationMs?: number;
  /** Cumulative wall-clock duration in milliseconds across all sandbox runs. */
  totalDurationMs?: number;
  /** The working directory of the sandbox. */
  cwd?: string;
  /** Key-value tags attached to the named sandbox. */
  tags?: CreateANamedSandboxResponseSandboxTagsMap;
  /** Key-value pairs of mount path and drive. */
  mounts?: CreateANamedSandboxResponseSandboxMountsMap;
  /** The time when the named sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The time when the named sandbox was last updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The time at which the currently running sandbox will time out, in milliseconds since the epoch. Only present while a session is running. */
  expiresAt?: number;
}
export const CreateANamedSandboxResponseSandbox = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    currentSnapshotId: S.optional(S.String),
    currentSessionId: S.String,
    status: CreateANamedSandboxResponseSandboxStatus,
    statusUpdatedAt: S.Number,
    persistent: S.Boolean,
    region: S.optional(S.String),
    vcpus: S.optional(S.Number),
    memory: S.optional(S.Number),
    runtime: S.optional(S.String),
    image: S.optional(S.String),
    timeout: S.optional(S.Number),
    snapshotExpiration: S.optional(S.Number),
    keepLastSnapshots: S.optional(
      CreateANamedSandboxResponseSandboxKeepLastSnapshots,
    ),
    networkPolicy: S.optional(CreateANamedSandboxResponseSandboxNetworkPolicy),
    totalEgressBytes: S.optional(S.Number),
    totalIngressBytes: S.optional(S.Number),
    totalActiveCpuDurationMs: S.optional(S.Number),
    totalDurationMs: S.optional(S.Number),
    cwd: S.optional(S.String),
    tags: S.optional(CreateANamedSandboxResponseSandboxTagsMap),
    mounts: S.optional(CreateANamedSandboxResponseSandboxMountsMap),
    createdAt: S.Number,
    updatedAt: S.Number,
    expiresAt: S.optional(S.Number),
  }),
).annotate({
  identifier: "CreateANamedSandboxResponseSandbox",
}) as any as S.Schema<CreateANamedSandboxResponseSandbox>;

/** The status of the sandbox. */
export type CreateANamedSandboxResponseSessionStatus =
  | "aborted"
  | "failed"
  | "pending"
  | "running"
  | "snapshotting"
  | "stopped"
  | "stopping";
export const CreateANamedSandboxResponseSessionStatus = /*@__PURE__*/ S.String;

/** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
export type CreateANamedSandboxResponseSessionNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "deny-all";
export const CreateANamedSandboxResponseSessionNetworkPolicyMode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
export type CreateANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const CreateANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
export type CreateANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const CreateANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type CreateANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const CreateANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. */
export type CreateANamedSandboxResponseSessionNetworkPolicyInjectionRulesList =
  Array<unknown>;
export const CreateANamedSandboxResponseSessionNetworkPolicyInjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandboxResponseSessionNetworkPolicyInjectionRulesList>;

/** The network policy applied to this sandbox, if any. */
export interface CreateANamedSandboxResponseSessionNetworkPolicy {
  /** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
  mode: CreateANamedSandboxResponseSessionNetworkPolicyMode;
  /** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
  allowedDomains?: CreateANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
  allowedCIDRs?: CreateANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: CreateANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. */
  injectionRules?: CreateANamedSandboxResponseSessionNetworkPolicyInjectionRulesList;
}
export const CreateANamedSandboxResponseSessionNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: CreateANamedSandboxResponseSessionNetworkPolicyMode,
      allowedDomains: S.optional(
        CreateANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        CreateANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        CreateANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList,
      ),
      injectionRules: S.optional(
        CreateANamedSandboxResponseSessionNetworkPolicyInjectionRulesList,
      ),
    }),
  ).annotate({
    identifier: "CreateANamedSandboxResponseSessionNetworkPolicy",
  }) as any as S.Schema<CreateANamedSandboxResponseSessionNetworkPolicy>;

/** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
export interface CreateANamedSandboxResponseSessionNetworkTransfer {
  ingress: number;
  egress: number;
}
export const CreateANamedSandboxResponseSessionNetworkTransfer =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      ingress: S.Number,
      egress: S.Number,
    }),
  ).annotate({
    identifier: "CreateANamedSandboxResponseSessionNetworkTransfer",
  }) as any as S.Schema<CreateANamedSandboxResponseSessionNetworkTransfer>;

/** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
export interface CreateANamedSandboxResponseSession {
  /** The name of the source sandbox. */
  sourceSandboxName: string;
  /** The unique identifier of the project associated with this session. */
  projectId: string;
  /** The unique identifier of the sandbox. */
  id: string;
  /** Memory allocated to this sandbox in MB. */
  memory: number;
  /** Number of vCPUs allocated to this sandbox. */
  vcpus: number;
  /** The region where the sandbox is hosted. */
  region: string;
  /** The runtime of the sandbox. */
  runtime: string;
  /** The maximum amount of time the sandbox will run for in milliseconds. */
  timeout: number;
  /** The status of the sandbox. */
  status: CreateANamedSandboxResponseSessionStatus;
  /** The time when the sandbox was requested, in milliseconds since the epoch. */
  requestedAt: number;
  /** The time when the sandbox was started, in milliseconds since the epoch. */
  startedAt?: number;
  /** The working directory of the sandbox. */
  cwd: string;
  /** The time when the sandbox was requested to stop, in milliseconds since the epoch. */
  requestedStopAt?: number;
  /** The time when the sandbox was stopped, in milliseconds since the epoch. */
  stoppedAt?: number;
  /** The time when the sandbox was aborted, in milliseconds since the epoch. */
  abortedAt?: number;
  /** The duration of the sandbox in milliseconds. */
  duration?: number;
  /** The unique identifier of the snapshot associated with this sandbox, if any. */
  sourceSnapshotId?: string;
  /** The time when a snapshot was requested, in milliseconds since the epoch. */
  snapshottedAt?: number;
  /** The time when the sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the sandbox was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The network policy applied to this sandbox, if any. */
  networkPolicy?: CreateANamedSandboxResponseSessionNetworkPolicy;
  /** The amount of CPU time the sandbox consumed, if available, in milliseconds. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  activeCpuDurationMs?: number;
  /** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  networkTransfer?: CreateANamedSandboxResponseSessionNetworkTransfer;
}
export const CreateANamedSandboxResponseSession = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sourceSandboxName: S.String,
    projectId: S.String,
    id: S.String,
    memory: S.Number,
    vcpus: S.Number,
    region: S.String,
    runtime: S.String,
    timeout: S.Number,
    status: CreateANamedSandboxResponseSessionStatus,
    requestedAt: S.Number,
    startedAt: S.optional(S.Number),
    cwd: S.String,
    requestedStopAt: S.optional(S.Number),
    stoppedAt: S.optional(S.Number),
    abortedAt: S.optional(S.Number),
    duration: S.optional(S.Number),
    sourceSnapshotId: S.optional(S.String),
    snapshottedAt: S.optional(S.Number),
    createdAt: S.Number,
    updatedAt: S.Number,
    networkPolicy: S.optional(CreateANamedSandboxResponseSessionNetworkPolicy),
    activeCpuDurationMs: S.optional(S.Number),
    networkTransfer: S.optional(
      CreateANamedSandboxResponseSessionNetworkTransfer,
    ),
  }),
).annotate({
  identifier: "CreateANamedSandboxResponseSession",
}) as any as S.Schema<CreateANamedSandboxResponseSession>;

/** This object represents a public route in a Vercel Sandbox. */
export interface CreateANamedSandboxResponseRoutesItem {
  /** A public URL to access the corresponding port in the Sandbox. */
  url: string;
  /** The user port number that the route is mapped to. */
  port: number;
  /** The subdomain assigned to this route. */
  subdomain: string;
  /** Whether the route is reserved by the system (e.g. for internal use). */
  system?: boolean;
}
export const CreateANamedSandboxResponseRoutesItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      url: S.String,
      port: S.Number,
      subdomain: S.String,
      system: S.optional(S.Boolean),
    }),
).annotate({
  identifier: "CreateANamedSandboxResponseRoutesItem",
}) as any as S.Schema<CreateANamedSandboxResponseRoutesItem>;

export type CreateANamedSandboxResponseRoutesList =
  Array<CreateANamedSandboxResponseRoutesItem>;
export const CreateANamedSandboxResponseRoutesList = /*@__PURE__*/ S.Array(
  CreateANamedSandboxResponseRoutesItem,
) as any as S.Schema<CreateANamedSandboxResponseRoutesList>;

export interface CreateANamedSandboxResponse {
  /** This object contains information related to a Vercel NamedSandbox. */
  sandbox: CreateANamedSandboxResponseSandbox;
  /** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
  session: CreateANamedSandboxResponseSession;
  routes: CreateANamedSandboxResponseRoutesList;
}
export const CreateANamedSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandbox: CreateANamedSandboxResponseSandbox,
    session: CreateANamedSandboxResponseSession,
    routes: CreateANamedSandboxResponseRoutesList,
  }),
).annotate({
  identifier: "CreateANamedSandboxResponse",
}) as any as S.Schema<CreateANamedSandboxResponse>;

/** The network access policy mode. Use \"allow-all\" to permit all outbound traffic. Use \"deny-all\" to block all outbound traffic. Use \"custom\" to specify explicit allow/deny rules. */
export type CreateANamedSandbox1RequestNetworkPolicyCase0Mode =
  | "allow-all"
  | "deny-all"
  | "custom"
  | "default-allow"
  | "default-deny";
export const CreateANamedSandbox1RequestNetworkPolicyCase0Mode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Only applies when mode is \"custom\". Supports wildcard patterns (e.g., \"*.example.com\" matches all subdomains). */
export type CreateANamedSandbox1RequestNetworkPolicyCase0AllowedDomainsList =
  Array<unknown>;
export const CreateANamedSandbox1RequestNetworkPolicyCase0AllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1RequestNetworkPolicyCase0AllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. Traffic to these addresses bypasses domain-based restrictions. */
export type CreateANamedSandbox1RequestNetworkPolicyCase0AllowedCIDRsList =
  Array<unknown>;
export const CreateANamedSandbox1RequestNetworkPolicyCase0AllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1RequestNetworkPolicyCase0AllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type CreateANamedSandbox1RequestNetworkPolicyCase0DeniedCIDRsList =
  Array<unknown>;
export const CreateANamedSandbox1RequestNetworkPolicyCase0DeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1RequestNetworkPolicyCase0DeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. Traffic to matching domains will be intercepted instead of proxied through encrypted connections. */
export type CreateANamedSandbox1RequestNetworkPolicyCase0InjectionRulesList =
  Array<unknown>;
export const CreateANamedSandbox1RequestNetworkPolicyCase0InjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1RequestNetworkPolicyCase0InjectionRulesList>;

/** Network access policy for the sandbox.\n Controls which external hosts the sandbox can communicate with.\n Use \"allow-all\" mode to allow all traffic, \"deny-all\" to block all traffic or \"custom\" to provide specific rules. */
export interface CreateANamedSandbox1RequestNetworkPolicyCase0 {
  /** The network access policy mode. Use \"allow-all\" to permit all outbound traffic. Use \"deny-all\" to block all outbound traffic. Use \"custom\" to specify explicit allow/deny rules. */
  mode: CreateANamedSandbox1RequestNetworkPolicyCase0Mode | (string & {});
  /** List of domain names the sandbox is allowed to connect to. Only applies when mode is \"custom\". Supports wildcard patterns (e.g., \"*.example.com\" matches all subdomains). */
  allowedDomains?: CreateANamedSandbox1RequestNetworkPolicyCase0AllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. Traffic to these addresses bypasses domain-based restrictions. */
  allowedCIDRs?: CreateANamedSandbox1RequestNetworkPolicyCase0AllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: CreateANamedSandbox1RequestNetworkPolicyCase0DeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. Traffic to matching domains will be intercepted instead of proxied through encrypted connections. */
  injectionRules?: CreateANamedSandbox1RequestNetworkPolicyCase0InjectionRulesList;
}
export const CreateANamedSandbox1RequestNetworkPolicyCase0 =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: CreateANamedSandbox1RequestNetworkPolicyCase0Mode,
      allowedDomains: S.optional(
        CreateANamedSandbox1RequestNetworkPolicyCase0AllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        CreateANamedSandbox1RequestNetworkPolicyCase0AllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        CreateANamedSandbox1RequestNetworkPolicyCase0DeniedCIDRsList,
      ),
      injectionRules: S.optional(
        CreateANamedSandbox1RequestNetworkPolicyCase0InjectionRulesList,
      ),
    }),
  ).annotate({
    identifier: "CreateANamedSandbox1RequestNetworkPolicyCase0",
  }) as any as S.Schema<CreateANamedSandbox1RequestNetworkPolicyCase0>;

export type CreateANamedSandbox1RequestNetworkPolicyCase1 =
  CreateANamedSandboxRequestNetworkPolicyCase1;
export const CreateANamedSandbox1RequestNetworkPolicyCase1 =
  CreateANamedSandboxRequestNetworkPolicyCase1;

export type CreateANamedSandbox1RequestNetworkPolicy =
  | CreateANamedSandbox1RequestNetworkPolicyCase0
  | CreateANamedSandboxRequestNetworkPolicyCase1;
export const CreateANamedSandbox1RequestNetworkPolicy =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateANamedSandbox1RequestNetworkPolicy>;

/** Resources to define the VM */
export type CreateANamedSandbox1RequestResources =
  CreateANamedSandboxRequestResources;
export const CreateANamedSandbox1RequestResources =
  CreateANamedSandboxRequestResources;

/** Clone a Git repository into the sandbox. */
export type CreateANamedSandbox1RequestSourceCase0 =
  CreateANamedSandboxRequestSourceCase0;
export const CreateANamedSandbox1RequestSourceCase0 =
  CreateANamedSandboxRequestSourceCase0;

/** Download and extract a gzipped tarball into the sandbox. */
export type CreateANamedSandbox1RequestSourceCase1 =
  CreateANamedSandboxRequestSourceCase1;
export const CreateANamedSandbox1RequestSourceCase1 =
  CreateANamedSandboxRequestSourceCase1;

/** Restore the sandbox filesystem from an existing snapshot. */
export type CreateANamedSandbox1RequestSourceCase2 =
  CreateANamedSandboxRequestSourceCase2;
export const CreateANamedSandbox1RequestSourceCase2 =
  CreateANamedSandboxRequestSourceCase2;

/** The source from which to initialize the sandbox filesystem. Can be a Git repository, a tarball URL, or an existing snapshot. */
export type CreateANamedSandbox1RequestSource =
  | CreateANamedSandboxRequestSourceCase0
  | CreateANamedSandboxRequestSourceCase1
  | CreateANamedSandboxRequestSourceCase2;
export const CreateANamedSandbox1RequestSource =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateANamedSandbox1RequestSource>;

/** List of ports to expose from the sandbox. Each port will be accessible via a unique URL. Maximum of 15 ports can be exposed. */
export type CreateANamedSandbox1RequestPortsList = Array<number>;
export const CreateANamedSandbox1RequestPortsList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<CreateANamedSandbox1RequestPortsList>;

/** Default environment variables for the sandbox. These are inherited by all commands unless overridden. */
export type CreateANamedSandbox1RequestEnvMap = {
  [key: string]: string | undefined;
};
export const CreateANamedSandbox1RequestEnvMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateANamedSandbox1RequestEnvMap>;

/** Mount the drive read-only or read-write. */
export type CreateANamedSandbox1RequestMountsValueMode =
  | "read-only"
  | "read-write";
export const CreateANamedSandbox1RequestMountsValueMode =
  /*@__PURE__*/ S.String;

export interface CreateANamedSandbox1RequestMountsValue {
  /** Name of the drive to mount. The drive must already exist. */
  drive: string;
  /** Mount the drive read-only or read-write. */
  mode?: CreateANamedSandbox1RequestMountsValueMode | (string & {});
}
export const CreateANamedSandbox1RequestMountsValue = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      drive: S.String,
      mode: S.optional(CreateANamedSandbox1RequestMountsValueMode),
    }),
).annotate({
  identifier: "CreateANamedSandbox1RequestMountsValue",
}) as any as S.Schema<CreateANamedSandbox1RequestMountsValue>;

/** List of drives to mount to the sandbox at the provided path. */
export type CreateANamedSandbox1RequestMountsMap = {
  [key: string]: CreateANamedSandbox1RequestMountsValue | undefined;
};
export const CreateANamedSandbox1RequestMountsMap = /*@__PURE__*/ S.Record(
  S.String,
  CreateANamedSandbox1RequestMountsValue,
) as any as S.Schema<CreateANamedSandbox1RequestMountsMap>;

/** Default snapshot expiration time in milliseconds. Set to 0 to disable expiration. When set, this value is used as the default expiration for all snapshots created for this sandbox. */
export type CreateANamedSandbox1RequestSnapshotExpiration = unknown | number;
export const CreateANamedSandbox1RequestSnapshotExpiration =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateANamedSandbox1RequestSnapshotExpiration>;

/** Expiration time in milliseconds for kept snapshots. Falls back to snapshotExpiration. */
export type CreateANamedSandbox1RequestKeepLastSnapshotsExpiration =
  | unknown
  | number;
export const CreateANamedSandbox1RequestKeepLastSnapshotsExpiration =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateANamedSandbox1RequestKeepLastSnapshotsExpiration>;

/** Protect the N most recent snapshots with different expiration/deletion behavior. */
export interface CreateANamedSandbox1RequestKeepLastSnapshots {
  /** Number of most recent snapshots to keep. */
  count: number;
  /** Expiration time in milliseconds for kept snapshots. Falls back to snapshotExpiration. */
  expiration?: CreateANamedSandbox1RequestKeepLastSnapshotsExpiration;
  /** Whether to immediately delete evicted snapshots. Defaults to true. */
  deleteEvicted?: boolean;
}
export const CreateANamedSandbox1RequestKeepLastSnapshots =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      count: S.Number,
      expiration: S.optional(
        CreateANamedSandbox1RequestKeepLastSnapshotsExpiration,
      ),
      deleteEvicted: S.optional(S.Boolean),
    }),
  ).annotate({
    identifier: "CreateANamedSandbox1RequestKeepLastSnapshots",
  }) as any as S.Schema<CreateANamedSandbox1RequestKeepLastSnapshots>;

/** Key-value tags to associate with the sandbox. Maximum 5 tags. */
export type CreateANamedSandbox1RequestTagsMap = {
  [key: string]: string | undefined;
};
export const CreateANamedSandbox1RequestTagsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateANamedSandbox1RequestTagsMap>;

export interface CreateANamedSandbox1Request {
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  networkPolicy?: CreateANamedSandbox1RequestNetworkPolicy;
  /** Resources to define the VM */
  resources?: CreateANamedSandboxRequestResources;
  /** The source from which to initialize the sandbox filesystem. Can be a Git repository, a tarball URL, or an existing snapshot. */
  source?: CreateANamedSandbox1RequestSource;
  /** The target project slug or ID in which the sandbox will be assigned to. */
  projectId?: string;
  /** List of ports to expose from the sandbox. Each port will be accessible via a unique URL. Maximum of 15 ports can be exposed. */
  ports?: CreateANamedSandbox1RequestPortsList;
  /** Image to use for the sandbox. */
  image?: string;
  /** Maximum duration in milliseconds that the sandbox can run before being automatically stopped. */
  timeout?: number;
  /** Default environment variables for the sandbox. These are inherited by all commands unless overridden. */
  env?: CreateANamedSandbox1RequestEnvMap;
  /** List of drives to mount to the sandbox at the provided path. */
  mounts?: CreateANamedSandbox1RequestMountsMap;
  /** Name for the sandbox. Must be unique per project and URL-safe (alphanumeric, hyphens, underscores). */
  name?: string;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. Defaults to true. */
  persistent?: boolean;
  /** Default snapshot expiration time in milliseconds. Set to 0 to disable expiration. When set, this value is used as the default expiration for all snapshots created for this sandbox. */
  snapshotExpiration?: CreateANamedSandbox1RequestSnapshotExpiration;
  /** Protect the N most recent snapshots with different expiration/deletion behavior. */
  keepLastSnapshots?: CreateANamedSandbox1RequestKeepLastSnapshots;
  /** Key-value tags to associate with the sandbox. Maximum 5 tags. */
  tags?: CreateANamedSandbox1RequestTagsMap;
}
export const CreateANamedSandbox1Request = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    networkPolicy: S.optional(CreateANamedSandbox1RequestNetworkPolicy),
    resources: S.optional(CreateANamedSandboxRequestResources),
    source: S.optional(CreateANamedSandbox1RequestSource),
    projectId: S.optional(S.String),
    ports: S.optional(CreateANamedSandbox1RequestPortsList),
    image: S.optional(S.String),
    timeout: S.optional(S.Number),
    env: S.optional(CreateANamedSandbox1RequestEnvMap),
    mounts: S.optional(CreateANamedSandbox1RequestMountsMap),
    name: S.optional(S.String),
    persistent: S.optional(S.Boolean),
    snapshotExpiration: S.optional(
      CreateANamedSandbox1RequestSnapshotExpiration,
    ),
    keepLastSnapshots: S.optional(CreateANamedSandbox1RequestKeepLastSnapshots),
    tags: S.optional(CreateANamedSandbox1RequestTagsMap),
  }).pipe(T.Http({ method: "POST", uri: "/v3/sandboxes", code: 200 })),
).annotate({
  identifier: "CreateANamedSandbox1Request",
}) as any as S.Schema<CreateANamedSandbox1Request>;

/** The status of the current sandbox. */
export type CreateANamedSandbox1ResponseSandboxStatus =
  | "running"
  | "stopped"
  | "stopping";
export const CreateANamedSandbox1ResponseSandboxStatus = /*@__PURE__*/ S.String;

/** Keep-last snapshot configuration. */
export type CreateANamedSandbox1ResponseSandboxKeepLastSnapshots =
  CreateANamedSandboxResponseSandboxKeepLastSnapshots;
export const CreateANamedSandbox1ResponseSandboxKeepLastSnapshots =
  CreateANamedSandboxResponseSandboxKeepLastSnapshots;

export type CreateANamedSandbox1ResponseSandboxNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "default-allow"
  | "default-deny"
  | "deny-all";
export const CreateANamedSandbox1ResponseSandboxNetworkPolicyMode =
  /*@__PURE__*/ S.String;

export type CreateANamedSandbox1ResponseSandboxNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const CreateANamedSandbox1ResponseSandboxNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1ResponseSandboxNetworkPolicyAllowedDomainsList>;

export type CreateANamedSandbox1ResponseSandboxNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const CreateANamedSandbox1ResponseSandboxNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1ResponseSandboxNetworkPolicyAllowedCIDRsList>;

export type CreateANamedSandbox1ResponseSandboxNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const CreateANamedSandbox1ResponseSandboxNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1ResponseSandboxNetworkPolicyDeniedCIDRsList>;

/** Network policy configuration. */
export interface CreateANamedSandbox1ResponseSandboxNetworkPolicy {
  mode: CreateANamedSandbox1ResponseSandboxNetworkPolicyMode;
  allowedDomains?: CreateANamedSandbox1ResponseSandboxNetworkPolicyAllowedDomainsList;
  allowedCIDRs?: CreateANamedSandbox1ResponseSandboxNetworkPolicyAllowedCIDRsList;
  deniedCIDRs?: CreateANamedSandbox1ResponseSandboxNetworkPolicyDeniedCIDRsList;
}
export const CreateANamedSandbox1ResponseSandboxNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: CreateANamedSandbox1ResponseSandboxNetworkPolicyMode,
      allowedDomains: S.optional(
        CreateANamedSandbox1ResponseSandboxNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        CreateANamedSandbox1ResponseSandboxNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        CreateANamedSandbox1ResponseSandboxNetworkPolicyDeniedCIDRsList,
      ),
    }),
  ).annotate({
    identifier: "CreateANamedSandbox1ResponseSandboxNetworkPolicy",
  }) as any as S.Schema<CreateANamedSandbox1ResponseSandboxNetworkPolicy>;

/** Key-value tags attached to the named sandbox. */
export type CreateANamedSandbox1ResponseSandboxTagsMap = {
  [key: string]: string | undefined;
};
export const CreateANamedSandbox1ResponseSandboxTagsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.String,
  ) as any as S.Schema<CreateANamedSandbox1ResponseSandboxTagsMap>;

/** Key-value pairs of mount path and drive. */
export type CreateANamedSandbox1ResponseSandboxMountsMap = {
  [key: string]: unknown | undefined;
};
export const CreateANamedSandbox1ResponseSandboxMountsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1ResponseSandboxMountsMap>;

/** This object contains information related to a Vercel NamedSandbox. */
export interface CreateANamedSandbox1ResponseSandbox {
  /** The unique identifier of the sandbox. */
  name: string;
  /** Current snapshot ID that the named sandbox is pointing to. */
  currentSnapshotId?: string;
  /** Current session ID the sandbox is pointing to. */
  currentSessionId: string;
  /** The status of the current sandbox. */
  status: CreateANamedSandbox1ResponseSandboxStatus;
  /** The time when the sandbox status was last updated, in milliseconds since the epoch. */
  statusUpdatedAt: number;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. */
  persistent: boolean;
  /** The region the sandbox is configured to run in: the region set on the sandbox, otherwise the project-level default, then the platform default. Where a running session actually landed is reported by `session.region`. */
  region?: string;
  /** Number of virtual CPUs allocated. */
  vcpus?: number;
  /** Memory allocated in MB. */
  memory?: number;
  /** Runtime identifier. */
  runtime?: string;
  /** Digest-pinned reference of the container image the sandbox was created from, when it was created from an image ("{repository}@{manifestDigest}"). */
  image?: string;
  /** Timeout in milliseconds. */
  timeout?: number;
  /** Default snapshot expiration time in milliseconds. 0 means no expiration. */
  snapshotExpiration?: number;
  /** Keep-last snapshot configuration. */
  keepLastSnapshots?: CreateANamedSandboxResponseSandboxKeepLastSnapshots;
  /** Network policy configuration. */
  networkPolicy?: CreateANamedSandbox1ResponseSandboxNetworkPolicy;
  /** Cumulative egress bytes across all sandbox runs. */
  totalEgressBytes?: number;
  /** Cumulative ingress bytes across all sandbox runs. */
  totalIngressBytes?: number;
  /** Cumulative active CPU duration in milliseconds across all sandbox runs. */
  totalActiveCpuDurationMs?: number;
  /** Cumulative wall-clock duration in milliseconds across all sandbox runs. */
  totalDurationMs?: number;
  /** The working directory of the sandbox. */
  cwd?: string;
  /** Key-value tags attached to the named sandbox. */
  tags?: CreateANamedSandbox1ResponseSandboxTagsMap;
  /** Key-value pairs of mount path and drive. */
  mounts?: CreateANamedSandbox1ResponseSandboxMountsMap;
  /** The time when the named sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The time when the named sandbox was last updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The time at which the currently running sandbox will time out, in milliseconds since the epoch. Only present while a session is running. */
  expiresAt?: number;
}
export const CreateANamedSandbox1ResponseSandbox = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    currentSnapshotId: S.optional(S.String),
    currentSessionId: S.String,
    status: CreateANamedSandbox1ResponseSandboxStatus,
    statusUpdatedAt: S.Number,
    persistent: S.Boolean,
    region: S.optional(S.String),
    vcpus: S.optional(S.Number),
    memory: S.optional(S.Number),
    runtime: S.optional(S.String),
    image: S.optional(S.String),
    timeout: S.optional(S.Number),
    snapshotExpiration: S.optional(S.Number),
    keepLastSnapshots: S.optional(
      CreateANamedSandboxResponseSandboxKeepLastSnapshots,
    ),
    networkPolicy: S.optional(CreateANamedSandbox1ResponseSandboxNetworkPolicy),
    totalEgressBytes: S.optional(S.Number),
    totalIngressBytes: S.optional(S.Number),
    totalActiveCpuDurationMs: S.optional(S.Number),
    totalDurationMs: S.optional(S.Number),
    cwd: S.optional(S.String),
    tags: S.optional(CreateANamedSandbox1ResponseSandboxTagsMap),
    mounts: S.optional(CreateANamedSandbox1ResponseSandboxMountsMap),
    createdAt: S.Number,
    updatedAt: S.Number,
    expiresAt: S.optional(S.Number),
  }),
).annotate({
  identifier: "CreateANamedSandbox1ResponseSandbox",
}) as any as S.Schema<CreateANamedSandbox1ResponseSandbox>;

/** The status of the sandbox. */
export type CreateANamedSandbox1ResponseSessionStatus =
  | "aborted"
  | "failed"
  | "pending"
  | "running"
  | "snapshotting"
  | "stopped"
  | "stopping";
export const CreateANamedSandbox1ResponseSessionStatus = /*@__PURE__*/ S.String;

/** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
export type CreateANamedSandbox1ResponseSessionNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "deny-all";
export const CreateANamedSandbox1ResponseSessionNetworkPolicyMode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
export type CreateANamedSandbox1ResponseSessionNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const CreateANamedSandbox1ResponseSessionNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1ResponseSessionNetworkPolicyAllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
export type CreateANamedSandbox1ResponseSessionNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const CreateANamedSandbox1ResponseSessionNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1ResponseSessionNetworkPolicyAllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type CreateANamedSandbox1ResponseSessionNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const CreateANamedSandbox1ResponseSessionNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1ResponseSessionNetworkPolicyDeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. */
export type CreateANamedSandbox1ResponseSessionNetworkPolicyInjectionRulesList =
  Array<unknown>;
export const CreateANamedSandbox1ResponseSessionNetworkPolicyInjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateANamedSandbox1ResponseSessionNetworkPolicyInjectionRulesList>;

/** The network policy applied to this sandbox, if any. */
export interface CreateANamedSandbox1ResponseSessionNetworkPolicy {
  /** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
  mode: CreateANamedSandbox1ResponseSessionNetworkPolicyMode;
  /** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
  allowedDomains?: CreateANamedSandbox1ResponseSessionNetworkPolicyAllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
  allowedCIDRs?: CreateANamedSandbox1ResponseSessionNetworkPolicyAllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: CreateANamedSandbox1ResponseSessionNetworkPolicyDeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. */
  injectionRules?: CreateANamedSandbox1ResponseSessionNetworkPolicyInjectionRulesList;
}
export const CreateANamedSandbox1ResponseSessionNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: CreateANamedSandbox1ResponseSessionNetworkPolicyMode,
      allowedDomains: S.optional(
        CreateANamedSandbox1ResponseSessionNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        CreateANamedSandbox1ResponseSessionNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        CreateANamedSandbox1ResponseSessionNetworkPolicyDeniedCIDRsList,
      ),
      injectionRules: S.optional(
        CreateANamedSandbox1ResponseSessionNetworkPolicyInjectionRulesList,
      ),
    }),
  ).annotate({
    identifier: "CreateANamedSandbox1ResponseSessionNetworkPolicy",
  }) as any as S.Schema<CreateANamedSandbox1ResponseSessionNetworkPolicy>;

/** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
export type CreateANamedSandbox1ResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;
export const CreateANamedSandbox1ResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;

/** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
export interface CreateANamedSandbox1ResponseSession {
  /** The name of the source sandbox. */
  sourceSandboxName: string;
  /** The unique identifier of the project associated with this session. */
  projectId: string;
  /** The unique identifier of the sandbox. */
  id: string;
  /** Memory allocated to this sandbox in MB. */
  memory: number;
  /** Number of vCPUs allocated to this sandbox. */
  vcpus: number;
  /** The region where the sandbox is hosted. */
  region: string;
  /** The runtime of the sandbox. */
  runtime: string;
  /** The maximum amount of time the sandbox will run for in milliseconds. */
  timeout: number;
  /** The status of the sandbox. */
  status: CreateANamedSandbox1ResponseSessionStatus;
  /** The time when the sandbox was requested, in milliseconds since the epoch. */
  requestedAt: number;
  /** The time when the sandbox was started, in milliseconds since the epoch. */
  startedAt?: number;
  /** The working directory of the sandbox. */
  cwd: string;
  /** The time when the sandbox was requested to stop, in milliseconds since the epoch. */
  requestedStopAt?: number;
  /** The time when the sandbox was stopped, in milliseconds since the epoch. */
  stoppedAt?: number;
  /** The time when the sandbox was aborted, in milliseconds since the epoch. */
  abortedAt?: number;
  /** The duration of the sandbox in milliseconds. */
  duration?: number;
  /** The unique identifier of the snapshot associated with this sandbox, if any. */
  sourceSnapshotId?: string;
  /** The time when a snapshot was requested, in milliseconds since the epoch. */
  snapshottedAt?: number;
  /** The time when the sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the sandbox was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The network policy applied to this sandbox, if any. */
  networkPolicy?: CreateANamedSandbox1ResponseSessionNetworkPolicy;
  /** The amount of CPU time the sandbox consumed, if available, in milliseconds. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  activeCpuDurationMs?: number;
  /** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  networkTransfer?: CreateANamedSandboxResponseSessionNetworkTransfer;
}
export const CreateANamedSandbox1ResponseSession = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sourceSandboxName: S.String,
    projectId: S.String,
    id: S.String,
    memory: S.Number,
    vcpus: S.Number,
    region: S.String,
    runtime: S.String,
    timeout: S.Number,
    status: CreateANamedSandbox1ResponseSessionStatus,
    requestedAt: S.Number,
    startedAt: S.optional(S.Number),
    cwd: S.String,
    requestedStopAt: S.optional(S.Number),
    stoppedAt: S.optional(S.Number),
    abortedAt: S.optional(S.Number),
    duration: S.optional(S.Number),
    sourceSnapshotId: S.optional(S.String),
    snapshottedAt: S.optional(S.Number),
    createdAt: S.Number,
    updatedAt: S.Number,
    networkPolicy: S.optional(CreateANamedSandbox1ResponseSessionNetworkPolicy),
    activeCpuDurationMs: S.optional(S.Number),
    networkTransfer: S.optional(
      CreateANamedSandboxResponseSessionNetworkTransfer,
    ),
  }),
).annotate({
  identifier: "CreateANamedSandbox1ResponseSession",
}) as any as S.Schema<CreateANamedSandbox1ResponseSession>;

/** This object represents a public route in a Vercel Sandbox. */
export type CreateANamedSandbox1ResponseRoutesItem =
  CreateANamedSandboxResponseRoutesItem;
export const CreateANamedSandbox1ResponseRoutesItem =
  CreateANamedSandboxResponseRoutesItem;

export type CreateANamedSandbox1ResponseRoutesList =
  Array<CreateANamedSandboxResponseRoutesItem>;
export const CreateANamedSandbox1ResponseRoutesList = /*@__PURE__*/ S.Array(
  CreateANamedSandboxResponseRoutesItem,
) as any as S.Schema<CreateANamedSandbox1ResponseRoutesList>;

export interface CreateANamedSandbox1Response {
  /** This object contains information related to a Vercel NamedSandbox. */
  sandbox: CreateANamedSandbox1ResponseSandbox;
  /** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
  session: CreateANamedSandbox1ResponseSession;
  routes: CreateANamedSandbox1ResponseRoutesList;
}
export const CreateANamedSandbox1Response = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandbox: CreateANamedSandbox1ResponseSandbox,
    session: CreateANamedSandbox1ResponseSession,
    routes: CreateANamedSandbox1ResponseRoutesList,
  }),
).annotate({
  identifier: "CreateANamedSandbox1Response",
}) as any as S.Schema<CreateANamedSandbox1Response>;

/** The number of milliseconds after which the snapshot will expire and be deleted. Use 0 for no expiration. */
export type CreateASnapshotRequestExpiration = unknown | number;
export const CreateASnapshotRequestExpiration =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CreateASnapshotRequestExpiration>;

export interface CreateASnapshotRequest {
  /** The unique identifier of the session to snapshot. */
  sessionId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  /** The number of milliseconds after which the snapshot will expire and be deleted. Use 0 for no expiration. */
  expiration?: CreateASnapshotRequestExpiration;
}
export const CreateASnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    expiration: S.optional(CreateASnapshotRequestExpiration),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/sandboxes/sessions/{sessionId}/snapshot",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateASnapshotRequest",
}) as any as S.Schema<CreateASnapshotRequest>;

/** The status of the snapshot. */
export type CreateASnapshotResponseSnapshotStatus =
  | "created"
  | "deleted"
  | "failed";
export const CreateASnapshotResponseSnapshotStatus = /*@__PURE__*/ S.String;

/** The method used to create the snapshot. */
export type CreateASnapshotResponseSnapshotCreationMethod =
  | "automatic"
  | "manual";
export const CreateASnapshotResponseSnapshotCreationMethod =
  /*@__PURE__*/ S.String;

/** This object contains information related to a Snapshot of a Vercel Sandbox session (v2 API). */
export interface CreateASnapshotResponseSnapshot {
  /** The unique identifier of the snapshot. */
  id: string;
  /** The unique identifier of the session from which the snapshot was created. */
  sourceSessionId: string;
  /** The region where the snapshot is stored. */
  region?: string;
  /** The status of the snapshot. */
  status: CreateASnapshotResponseSnapshotStatus;
  /** The size of the snapshot in bytes. */
  sizeBytes: number;
  /** The time when the snapshot will expire, in milliseconds since the epoch. If not set, the snapshot does not have any expiration. */
  expiresAt?: number;
  /** The time when the snapshot was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the snapshot was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The last time the snapshot was used (e.g. to resume or create a sandbox), in milliseconds since the epoch. Falls back to `createdAt` for older snapshots that predate this field. */
  lastUsedAt: number;
  /** The method used to create the snapshot. */
  creationMethod?: CreateASnapshotResponseSnapshotCreationMethod;
  /** The unique identifier of the parent snapshot, if this snapshot was created from another snapshot. */
  parentId?: string;
}
export const CreateASnapshotResponseSnapshot = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    sourceSessionId: S.String,
    region: S.optional(S.String),
    status: CreateASnapshotResponseSnapshotStatus,
    sizeBytes: S.Number,
    expiresAt: S.optional(S.Number),
    createdAt: S.Number,
    updatedAt: S.Number,
    lastUsedAt: S.Number,
    creationMethod: S.optional(CreateASnapshotResponseSnapshotCreationMethod),
    parentId: S.optional(S.String),
  }),
).annotate({
  identifier: "CreateASnapshotResponseSnapshot",
}) as any as S.Schema<CreateASnapshotResponseSnapshot>;

/** The status of the sandbox. */
export type CreateASnapshotResponseSessionStatus =
  | "aborted"
  | "failed"
  | "pending"
  | "running"
  | "snapshotting"
  | "stopped"
  | "stopping";
export const CreateASnapshotResponseSessionStatus = /*@__PURE__*/ S.String;

/** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
export type CreateASnapshotResponseSessionNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "deny-all";
export const CreateASnapshotResponseSessionNetworkPolicyMode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
export type CreateASnapshotResponseSessionNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const CreateASnapshotResponseSessionNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateASnapshotResponseSessionNetworkPolicyAllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
export type CreateASnapshotResponseSessionNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const CreateASnapshotResponseSessionNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateASnapshotResponseSessionNetworkPolicyAllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type CreateASnapshotResponseSessionNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const CreateASnapshotResponseSessionNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateASnapshotResponseSessionNetworkPolicyDeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. */
export type CreateASnapshotResponseSessionNetworkPolicyInjectionRulesList =
  Array<unknown>;
export const CreateASnapshotResponseSessionNetworkPolicyInjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<CreateASnapshotResponseSessionNetworkPolicyInjectionRulesList>;

/** The network policy applied to this sandbox, if any. */
export interface CreateASnapshotResponseSessionNetworkPolicy {
  /** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
  mode: CreateASnapshotResponseSessionNetworkPolicyMode;
  /** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
  allowedDomains?: CreateASnapshotResponseSessionNetworkPolicyAllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
  allowedCIDRs?: CreateASnapshotResponseSessionNetworkPolicyAllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: CreateASnapshotResponseSessionNetworkPolicyDeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. */
  injectionRules?: CreateASnapshotResponseSessionNetworkPolicyInjectionRulesList;
}
export const CreateASnapshotResponseSessionNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: CreateASnapshotResponseSessionNetworkPolicyMode,
      allowedDomains: S.optional(
        CreateASnapshotResponseSessionNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        CreateASnapshotResponseSessionNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        CreateASnapshotResponseSessionNetworkPolicyDeniedCIDRsList,
      ),
      injectionRules: S.optional(
        CreateASnapshotResponseSessionNetworkPolicyInjectionRulesList,
      ),
    }),
  ).annotate({
    identifier: "CreateASnapshotResponseSessionNetworkPolicy",
  }) as any as S.Schema<CreateASnapshotResponseSessionNetworkPolicy>;

/** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
export type CreateASnapshotResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;
export const CreateASnapshotResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;

/** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
export interface CreateASnapshotResponseSession {
  /** The name of the source sandbox. */
  sourceSandboxName: string;
  /** The unique identifier of the project associated with this session. */
  projectId: string;
  /** The unique identifier of the sandbox. */
  id: string;
  /** Memory allocated to this sandbox in MB. */
  memory: number;
  /** Number of vCPUs allocated to this sandbox. */
  vcpus: number;
  /** The region where the sandbox is hosted. */
  region: string;
  /** The runtime of the sandbox. */
  runtime: string;
  /** The maximum amount of time the sandbox will run for in milliseconds. */
  timeout: number;
  /** The status of the sandbox. */
  status: CreateASnapshotResponseSessionStatus;
  /** The time when the sandbox was requested, in milliseconds since the epoch. */
  requestedAt: number;
  /** The time when the sandbox was started, in milliseconds since the epoch. */
  startedAt?: number;
  /** The working directory of the sandbox. */
  cwd: string;
  /** The time when the sandbox was requested to stop, in milliseconds since the epoch. */
  requestedStopAt?: number;
  /** The time when the sandbox was stopped, in milliseconds since the epoch. */
  stoppedAt?: number;
  /** The time when the sandbox was aborted, in milliseconds since the epoch. */
  abortedAt?: number;
  /** The duration of the sandbox in milliseconds. */
  duration?: number;
  /** The unique identifier of the snapshot associated with this sandbox, if any. */
  sourceSnapshotId?: string;
  /** The time when a snapshot was requested, in milliseconds since the epoch. */
  snapshottedAt?: number;
  /** The time when the sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the sandbox was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The network policy applied to this sandbox, if any. */
  networkPolicy?: CreateASnapshotResponseSessionNetworkPolicy;
  /** The amount of CPU time the sandbox consumed, if available, in milliseconds. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  activeCpuDurationMs?: number;
  /** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  networkTransfer?: CreateANamedSandboxResponseSessionNetworkTransfer;
}
export const CreateASnapshotResponseSession = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sourceSandboxName: S.String,
    projectId: S.String,
    id: S.String,
    memory: S.Number,
    vcpus: S.Number,
    region: S.String,
    runtime: S.String,
    timeout: S.Number,
    status: CreateASnapshotResponseSessionStatus,
    requestedAt: S.Number,
    startedAt: S.optional(S.Number),
    cwd: S.String,
    requestedStopAt: S.optional(S.Number),
    stoppedAt: S.optional(S.Number),
    abortedAt: S.optional(S.Number),
    duration: S.optional(S.Number),
    sourceSnapshotId: S.optional(S.String),
    snapshottedAt: S.optional(S.Number),
    createdAt: S.Number,
    updatedAt: S.Number,
    networkPolicy: S.optional(CreateASnapshotResponseSessionNetworkPolicy),
    activeCpuDurationMs: S.optional(S.Number),
    networkTransfer: S.optional(
      CreateANamedSandboxResponseSessionNetworkTransfer,
    ),
  }),
).annotate({
  identifier: "CreateASnapshotResponseSession",
}) as any as S.Schema<CreateASnapshotResponseSession>;

export interface CreateASnapshotResponse {
  /** This object contains information related to a Snapshot of a Vercel Sandbox session (v2 API). */
  snapshot: CreateASnapshotResponseSnapshot;
  /** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
  session: CreateASnapshotResponseSession;
}
export const CreateASnapshotResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshot: CreateASnapshotResponseSnapshot,
    session: CreateASnapshotResponseSession,
  }),
).annotate({
  identifier: "CreateASnapshotResponse",
}) as any as S.Schema<CreateASnapshotResponse>;

export interface DeleteADriveRequest {
  /** Name for the drive. Must be unique per project and URL-safe (alphanumeric, hyphens, underscores). */
  name: string;
  /** The project ID or name associated with the drive. Required unless using a Vercel OIDC token scoped to a project. */
  projectId?: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const DeleteADriveRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    projectId: S.optional(S.String.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "DELETE", uri: "/v2/sandboxes/drives/{name}", code: 200 }),
  ),
).annotate({
  identifier: "DeleteADriveRequest",
}) as any as S.Schema<DeleteADriveRequest>;

/** This object contains information related to a Vercel Sandbox Drive. */
export interface DeleteADriveResponseDrive {
  /** The unique drive name within the project. */
  name: string;
  /** The project that owns the drive. */
  projectId: string;
  /** The maximum drive size in bytes. */
  maxSizeBytes: number;
  /** The region where the drive is stored. */
  region: string;
  /** Current session ID the drive is attached to, if any. */
  currentSessionId?: string;
  /** Current sandbox name the drive is attached to, if any. */
  currentSandboxName?: string;
  /** The time when the drive was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the drive was updated, in milliseconds since the epoch. */
  updatedAt: number;
}
export const DeleteADriveResponseDrive = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    projectId: S.String,
    maxSizeBytes: S.Number,
    region: S.String,
    currentSessionId: S.optional(S.String),
    currentSandboxName: S.optional(S.String),
    createdAt: S.Number,
    updatedAt: S.Number,
  }),
).annotate({
  identifier: "DeleteADriveResponseDrive",
}) as any as S.Schema<DeleteADriveResponseDrive>;

export interface DeleteADriveResponse {
  /** This object contains information related to a Vercel Sandbox Drive. */
  drive: DeleteADriveResponseDrive;
}
export const DeleteADriveResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    drive: DeleteADriveResponseDrive,
  }),
).annotate({
  identifier: "DeleteADriveResponse",
}) as any as S.Schema<DeleteADriveResponse>;

export interface DeleteASandboxRequest {
  /** The sandbox name to delete. */
  name: string;
  /** The project ID that owns the named sandbox. When provided, takes precedence over OIDC project context. */
  projectId?: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const DeleteASandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    projectId: S.optional(S.String.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "DELETE", uri: "/v2/sandboxes/{name}", code: 200 })),
).annotate({
  identifier: "DeleteASandboxRequest",
}) as any as S.Schema<DeleteASandboxRequest>;

/** The status of the current sandbox. */
export type DeleteASandboxResponseSandboxStatus =
  | "running"
  | "stopped"
  | "stopping";
export const DeleteASandboxResponseSandboxStatus = /*@__PURE__*/ S.String;

/** Keep-last snapshot configuration. */
export type DeleteASandboxResponseSandboxKeepLastSnapshots =
  CreateANamedSandboxResponseSandboxKeepLastSnapshots;
export const DeleteASandboxResponseSandboxKeepLastSnapshots =
  CreateANamedSandboxResponseSandboxKeepLastSnapshots;

export type DeleteASandboxResponseSandboxNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "default-allow"
  | "default-deny"
  | "deny-all";
export const DeleteASandboxResponseSandboxNetworkPolicyMode =
  /*@__PURE__*/ S.String;

export type DeleteASandboxResponseSandboxNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const DeleteASandboxResponseSandboxNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<DeleteASandboxResponseSandboxNetworkPolicyAllowedDomainsList>;

export type DeleteASandboxResponseSandboxNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const DeleteASandboxResponseSandboxNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<DeleteASandboxResponseSandboxNetworkPolicyAllowedCIDRsList>;

export type DeleteASandboxResponseSandboxNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const DeleteASandboxResponseSandboxNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<DeleteASandboxResponseSandboxNetworkPolicyDeniedCIDRsList>;

/** Network policy configuration. */
export interface DeleteASandboxResponseSandboxNetworkPolicy {
  mode: DeleteASandboxResponseSandboxNetworkPolicyMode;
  allowedDomains?: DeleteASandboxResponseSandboxNetworkPolicyAllowedDomainsList;
  allowedCIDRs?: DeleteASandboxResponseSandboxNetworkPolicyAllowedCIDRsList;
  deniedCIDRs?: DeleteASandboxResponseSandboxNetworkPolicyDeniedCIDRsList;
}
export const DeleteASandboxResponseSandboxNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: DeleteASandboxResponseSandboxNetworkPolicyMode,
      allowedDomains: S.optional(
        DeleteASandboxResponseSandboxNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        DeleteASandboxResponseSandboxNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        DeleteASandboxResponseSandboxNetworkPolicyDeniedCIDRsList,
      ),
    }),
  ).annotate({
    identifier: "DeleteASandboxResponseSandboxNetworkPolicy",
  }) as any as S.Schema<DeleteASandboxResponseSandboxNetworkPolicy>;

/** Key-value tags attached to the named sandbox. */
export type DeleteASandboxResponseSandboxTagsMap = {
  [key: string]: string | undefined;
};
export const DeleteASandboxResponseSandboxTagsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<DeleteASandboxResponseSandboxTagsMap>;

/** Key-value pairs of mount path and drive. */
export type DeleteASandboxResponseSandboxMountsMap = {
  [key: string]: unknown | undefined;
};
export const DeleteASandboxResponseSandboxMountsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<DeleteASandboxResponseSandboxMountsMap>;

/** This object contains information related to a Vercel NamedSandbox. */
export interface DeleteASandboxResponseSandbox {
  /** The unique identifier of the sandbox. */
  name: string;
  /** Current snapshot ID that the named sandbox is pointing to. */
  currentSnapshotId?: string;
  /** Current session ID the sandbox is pointing to. */
  currentSessionId: string;
  /** The status of the current sandbox. */
  status: DeleteASandboxResponseSandboxStatus;
  /** The time when the sandbox status was last updated, in milliseconds since the epoch. */
  statusUpdatedAt: number;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. */
  persistent: boolean;
  /** The region the sandbox is configured to run in: the region set on the sandbox, otherwise the project-level default, then the platform default. Where a running session actually landed is reported by `session.region`. */
  region?: string;
  /** Number of virtual CPUs allocated. */
  vcpus?: number;
  /** Memory allocated in MB. */
  memory?: number;
  /** Runtime identifier. */
  runtime?: string;
  /** Digest-pinned reference of the container image the sandbox was created from, when it was created from an image ("{repository}@{manifestDigest}"). */
  image?: string;
  /** Timeout in milliseconds. */
  timeout?: number;
  /** Default snapshot expiration time in milliseconds. 0 means no expiration. */
  snapshotExpiration?: number;
  /** Keep-last snapshot configuration. */
  keepLastSnapshots?: CreateANamedSandboxResponseSandboxKeepLastSnapshots;
  /** Network policy configuration. */
  networkPolicy?: DeleteASandboxResponseSandboxNetworkPolicy;
  /** Cumulative egress bytes across all sandbox runs. */
  totalEgressBytes?: number;
  /** Cumulative ingress bytes across all sandbox runs. */
  totalIngressBytes?: number;
  /** Cumulative active CPU duration in milliseconds across all sandbox runs. */
  totalActiveCpuDurationMs?: number;
  /** Cumulative wall-clock duration in milliseconds across all sandbox runs. */
  totalDurationMs?: number;
  /** The working directory of the sandbox. */
  cwd?: string;
  /** Key-value tags attached to the named sandbox. */
  tags?: DeleteASandboxResponseSandboxTagsMap;
  /** Key-value pairs of mount path and drive. */
  mounts?: DeleteASandboxResponseSandboxMountsMap;
  /** The time when the named sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The time when the named sandbox was last updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The time at which the currently running sandbox will time out, in milliseconds since the epoch. Only present while a session is running. */
  expiresAt?: number;
}
export const DeleteASandboxResponseSandbox = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    currentSnapshotId: S.optional(S.String),
    currentSessionId: S.String,
    status: DeleteASandboxResponseSandboxStatus,
    statusUpdatedAt: S.Number,
    persistent: S.Boolean,
    region: S.optional(S.String),
    vcpus: S.optional(S.Number),
    memory: S.optional(S.Number),
    runtime: S.optional(S.String),
    image: S.optional(S.String),
    timeout: S.optional(S.Number),
    snapshotExpiration: S.optional(S.Number),
    keepLastSnapshots: S.optional(
      CreateANamedSandboxResponseSandboxKeepLastSnapshots,
    ),
    networkPolicy: S.optional(DeleteASandboxResponseSandboxNetworkPolicy),
    totalEgressBytes: S.optional(S.Number),
    totalIngressBytes: S.optional(S.Number),
    totalActiveCpuDurationMs: S.optional(S.Number),
    totalDurationMs: S.optional(S.Number),
    cwd: S.optional(S.String),
    tags: S.optional(DeleteASandboxResponseSandboxTagsMap),
    mounts: S.optional(DeleteASandboxResponseSandboxMountsMap),
    createdAt: S.Number,
    updatedAt: S.Number,
    expiresAt: S.optional(S.Number),
  }),
).annotate({
  identifier: "DeleteASandboxResponseSandbox",
}) as any as S.Schema<DeleteASandboxResponseSandbox>;

export interface DeleteASandboxResponse {
  /** This object contains information related to a Vercel NamedSandbox. */
  sandbox: DeleteASandboxResponseSandbox;
}
export const DeleteASandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandbox: DeleteASandboxResponseSandbox,
  }),
).annotate({
  identifier: "DeleteASandboxResponse",
}) as any as S.Schema<DeleteASandboxResponse>;

export interface DeleteASnapshotRequest {
  /** The unique identifier of the snapshot to delete. */
  snapshotId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const DeleteASnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/sandboxes/snapshots/{snapshotId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteASnapshotRequest",
}) as any as S.Schema<DeleteASnapshotRequest>;

/** The status of the snapshot. */
export type DeleteASnapshotResponseSnapshotStatus =
  | "created"
  | "deleted"
  | "failed";
export const DeleteASnapshotResponseSnapshotStatus = /*@__PURE__*/ S.String;

/** The method used to create the snapshot. */
export type DeleteASnapshotResponseSnapshotCreationMethod =
  | "automatic"
  | "manual";
export const DeleteASnapshotResponseSnapshotCreationMethod =
  /*@__PURE__*/ S.String;

/** This object contains information related to a Snapshot of a Vercel Sandbox session (v2 API). */
export interface DeleteASnapshotResponseSnapshot {
  /** The unique identifier of the snapshot. */
  id: string;
  /** The unique identifier of the session from which the snapshot was created. */
  sourceSessionId: string;
  /** The region where the snapshot is stored. */
  region?: string;
  /** The status of the snapshot. */
  status: DeleteASnapshotResponseSnapshotStatus;
  /** The size of the snapshot in bytes. */
  sizeBytes: number;
  /** The time when the snapshot will expire, in milliseconds since the epoch. If not set, the snapshot does not have any expiration. */
  expiresAt?: number;
  /** The time when the snapshot was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the snapshot was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The last time the snapshot was used (e.g. to resume or create a sandbox), in milliseconds since the epoch. Falls back to `createdAt` for older snapshots that predate this field. */
  lastUsedAt: number;
  /** The method used to create the snapshot. */
  creationMethod?: DeleteASnapshotResponseSnapshotCreationMethod;
  /** The unique identifier of the parent snapshot, if this snapshot was created from another snapshot. */
  parentId?: string;
}
export const DeleteASnapshotResponseSnapshot = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    sourceSessionId: S.String,
    region: S.optional(S.String),
    status: DeleteASnapshotResponseSnapshotStatus,
    sizeBytes: S.Number,
    expiresAt: S.optional(S.Number),
    createdAt: S.Number,
    updatedAt: S.Number,
    lastUsedAt: S.Number,
    creationMethod: S.optional(DeleteASnapshotResponseSnapshotCreationMethod),
    parentId: S.optional(S.String),
  }),
).annotate({
  identifier: "DeleteASnapshotResponseSnapshot",
}) as any as S.Schema<DeleteASnapshotResponseSnapshot>;

export interface DeleteASnapshotResponse {
  /** This object contains information related to a Snapshot of a Vercel Sandbox session (v2 API). */
  snapshot: DeleteASnapshotResponseSnapshot;
}
export const DeleteASnapshotResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshot: DeleteASnapshotResponseSnapshot,
  }),
).annotate({
  identifier: "DeleteASnapshotResponse",
}) as any as S.Schema<DeleteASnapshotResponse>;

/** Arguments to pass to the command. Each argument should be a separate array element. */
export type ExecuteACommandRequestArgsList = Array<string>;
export const ExecuteACommandRequestArgsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ExecuteACommandRequestArgsList>;

/** Additional environment variables to set for this command. These are merged with the sandbox environment. */
export type ExecuteACommandRequestEnvMap = {
  [key: string]: string | undefined;
};
export const ExecuteACommandRequestEnvMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ExecuteACommandRequestEnvMap>;

export interface ExecuteACommandRequest {
  /** The unique identifier of the session in which to execute the command. */
  sessionId: string;
  /** The unique identifier of the command to stream logs for. */
  cmdId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  /** The executable or shell command to run. This is the program name without arguments. */
  command: string;
  /** Arguments to pass to the command. Each argument should be a separate array element. */
  args?: ExecuteACommandRequestArgsList;
  /** The working directory in which to execute the command. Defaults to the sandbox home directory if not specified. */
  cwd?: string;
  /** Additional environment variables to set for this command. These are merged with the sandbox environment. */
  env?: ExecuteACommandRequestEnvMap;
  /** Execute the command with root (superuser) privileges. */
  sudo?: boolean;
  /** If true, returns an ND-JSON stream that emits the command status when started and again when finished. Useful for synchronously waiting for command completion. */
  wait?: boolean;
  /** If true, stream the logs of the command execution in real-time via ND-JSON. This is only applicable if `wait` is also true. */
  logs?: boolean;
  /** Maximum duration in milliseconds the command may run before it is killed with SIGKILL. Enforced at exec time, independently of `wait`. */
  timeout?: number;
}
export const ExecuteACommandRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    cmdId: S.String.pipe(T.Query()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    command: S.String,
    args: S.optional(ExecuteACommandRequestArgsList),
    cwd: S.optional(S.String),
    env: S.optional(ExecuteACommandRequestEnvMap),
    sudo: S.optional(S.Boolean),
    wait: S.optional(S.Boolean),
    logs: S.optional(S.Boolean),
    timeout: S.optional(S.Number),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/sandboxes/sessions/{sessionId}/cmd",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ExecuteACommandRequest",
}) as any as S.Schema<ExecuteACommandRequest>;

/** The arguments of the command. */
export type ExecuteACommandResponseCommandArgsList = Array<string>;
export const ExecuteACommandResponseCommandArgsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ExecuteACommandResponseCommandArgsList>;

/** This object represents a command run in a Vercel Sandbox session (v2 API). */
export interface ExecuteACommandResponseCommand {
  /** The ID of the command. */
  id: string;
  /** The name of the command. */
  name: string;
  /** The arguments of the command. */
  args: ExecuteACommandResponseCommandArgsList;
  /** The current working directory of the command. */
  cwd: string;
  /** The ID of the session associated with the command. */
  sessionId: string;
  /** If the command did finish, the exit code. */
  exitCode: number | null;
  /** When the command was started, in milliseconds since the epoch. */
  startedAt: number;
  /** Duration of the command execution in milliseconds. */
  durationMs?: number;
}
export const ExecuteACommandResponseCommand = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    args: ExecuteACommandResponseCommandArgsList,
    cwd: S.String,
    sessionId: S.String,
    exitCode: S.NullOr(S.Number),
    startedAt: S.Number,
    durationMs: S.optional(S.Number),
  }),
).annotate({
  identifier: "ExecuteACommandResponseCommand",
}) as any as S.Schema<ExecuteACommandResponseCommand>;

export interface ExecuteACommandResponse {
  /** This object represents a command run in a Vercel Sandbox session (v2 API). */
  command: ExecuteACommandResponseCommand;
}
export const ExecuteACommandResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    command: ExecuteACommandResponseCommand,
  }),
).annotate({
  identifier: "ExecuteACommandResponse",
}) as any as S.Schema<ExecuteACommandResponse>;

export interface ExtendSessionTimeoutRequest {
  /** The unique identifier of the session to extend the timeout for. */
  sessionId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  /** The amount of time in milliseconds to add to the current timeout. Must be at least 1000ms (1 second). */
  duration: number;
}
export const ExtendSessionTimeoutRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    duration: S.Number,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/sandboxes/sessions/{sessionId}/extend-timeout",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ExtendSessionTimeoutRequest",
}) as any as S.Schema<ExtendSessionTimeoutRequest>;

/** The status of the sandbox. */
export type ExtendSessionTimeoutResponseSessionStatus =
  | "aborted"
  | "failed"
  | "pending"
  | "running"
  | "snapshotting"
  | "stopped"
  | "stopping";
export const ExtendSessionTimeoutResponseSessionStatus = /*@__PURE__*/ S.String;

/** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
export type ExtendSessionTimeoutResponseSessionNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "deny-all";
export const ExtendSessionTimeoutResponseSessionNetworkPolicyMode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
export type ExtendSessionTimeoutResponseSessionNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const ExtendSessionTimeoutResponseSessionNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ExtendSessionTimeoutResponseSessionNetworkPolicyAllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
export type ExtendSessionTimeoutResponseSessionNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const ExtendSessionTimeoutResponseSessionNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ExtendSessionTimeoutResponseSessionNetworkPolicyAllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type ExtendSessionTimeoutResponseSessionNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const ExtendSessionTimeoutResponseSessionNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ExtendSessionTimeoutResponseSessionNetworkPolicyDeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. */
export type ExtendSessionTimeoutResponseSessionNetworkPolicyInjectionRulesList =
  Array<unknown>;
export const ExtendSessionTimeoutResponseSessionNetworkPolicyInjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ExtendSessionTimeoutResponseSessionNetworkPolicyInjectionRulesList>;

/** The network policy applied to this sandbox, if any. */
export interface ExtendSessionTimeoutResponseSessionNetworkPolicy {
  /** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
  mode: ExtendSessionTimeoutResponseSessionNetworkPolicyMode;
  /** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
  allowedDomains?: ExtendSessionTimeoutResponseSessionNetworkPolicyAllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
  allowedCIDRs?: ExtendSessionTimeoutResponseSessionNetworkPolicyAllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: ExtendSessionTimeoutResponseSessionNetworkPolicyDeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. */
  injectionRules?: ExtendSessionTimeoutResponseSessionNetworkPolicyInjectionRulesList;
}
export const ExtendSessionTimeoutResponseSessionNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: ExtendSessionTimeoutResponseSessionNetworkPolicyMode,
      allowedDomains: S.optional(
        ExtendSessionTimeoutResponseSessionNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        ExtendSessionTimeoutResponseSessionNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        ExtendSessionTimeoutResponseSessionNetworkPolicyDeniedCIDRsList,
      ),
      injectionRules: S.optional(
        ExtendSessionTimeoutResponseSessionNetworkPolicyInjectionRulesList,
      ),
    }),
  ).annotate({
    identifier: "ExtendSessionTimeoutResponseSessionNetworkPolicy",
  }) as any as S.Schema<ExtendSessionTimeoutResponseSessionNetworkPolicy>;

/** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
export type ExtendSessionTimeoutResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;
export const ExtendSessionTimeoutResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;

/** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
export interface ExtendSessionTimeoutResponseSession {
  /** The name of the source sandbox. */
  sourceSandboxName: string;
  /** The unique identifier of the project associated with this session. */
  projectId: string;
  /** The unique identifier of the sandbox. */
  id: string;
  /** Memory allocated to this sandbox in MB. */
  memory: number;
  /** Number of vCPUs allocated to this sandbox. */
  vcpus: number;
  /** The region where the sandbox is hosted. */
  region: string;
  /** The runtime of the sandbox. */
  runtime: string;
  /** The maximum amount of time the sandbox will run for in milliseconds. */
  timeout: number;
  /** The status of the sandbox. */
  status: ExtendSessionTimeoutResponseSessionStatus;
  /** The time when the sandbox was requested, in milliseconds since the epoch. */
  requestedAt: number;
  /** The time when the sandbox was started, in milliseconds since the epoch. */
  startedAt?: number;
  /** The working directory of the sandbox. */
  cwd: string;
  /** The time when the sandbox was requested to stop, in milliseconds since the epoch. */
  requestedStopAt?: number;
  /** The time when the sandbox was stopped, in milliseconds since the epoch. */
  stoppedAt?: number;
  /** The time when the sandbox was aborted, in milliseconds since the epoch. */
  abortedAt?: number;
  /** The duration of the sandbox in milliseconds. */
  duration?: number;
  /** The unique identifier of the snapshot associated with this sandbox, if any. */
  sourceSnapshotId?: string;
  /** The time when a snapshot was requested, in milliseconds since the epoch. */
  snapshottedAt?: number;
  /** The time when the sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the sandbox was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The network policy applied to this sandbox, if any. */
  networkPolicy?: ExtendSessionTimeoutResponseSessionNetworkPolicy;
  /** The amount of CPU time the sandbox consumed, if available, in milliseconds. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  activeCpuDurationMs?: number;
  /** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  networkTransfer?: CreateANamedSandboxResponseSessionNetworkTransfer;
}
export const ExtendSessionTimeoutResponseSession = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sourceSandboxName: S.String,
    projectId: S.String,
    id: S.String,
    memory: S.Number,
    vcpus: S.Number,
    region: S.String,
    runtime: S.String,
    timeout: S.Number,
    status: ExtendSessionTimeoutResponseSessionStatus,
    requestedAt: S.Number,
    startedAt: S.optional(S.Number),
    cwd: S.String,
    requestedStopAt: S.optional(S.Number),
    stoppedAt: S.optional(S.Number),
    abortedAt: S.optional(S.Number),
    duration: S.optional(S.Number),
    sourceSnapshotId: S.optional(S.String),
    snapshottedAt: S.optional(S.Number),
    createdAt: S.Number,
    updatedAt: S.Number,
    networkPolicy: S.optional(ExtendSessionTimeoutResponseSessionNetworkPolicy),
    activeCpuDurationMs: S.optional(S.Number),
    networkTransfer: S.optional(
      CreateANamedSandboxResponseSessionNetworkTransfer,
    ),
  }),
).annotate({
  identifier: "ExtendSessionTimeoutResponseSession",
}) as any as S.Schema<ExtendSessionTimeoutResponseSession>;

export interface ExtendSessionTimeoutResponse {
  /** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
  session: ExtendSessionTimeoutResponseSession;
}
export const ExtendSessionTimeoutResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    session: ExtendSessionTimeoutResponseSession,
  }),
).annotate({
  identifier: "ExtendSessionTimeoutResponse",
}) as any as S.Schema<ExtendSessionTimeoutResponse>;

/** The network access policy mode. Use \"allow-all\" to permit all outbound traffic. Use \"deny-all\" to block all outbound traffic. Use \"custom\" to specify explicit allow/deny rules. */
export type ForkANamedSandboxRequestNetworkPolicyCase0Mode =
  | "allow-all"
  | "deny-all"
  | "custom"
  | "default-allow"
  | "default-deny";
export const ForkANamedSandboxRequestNetworkPolicyCase0Mode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Only applies when mode is \"custom\". Supports wildcard patterns (e.g., \"*.example.com\" matches all subdomains). */
export type ForkANamedSandboxRequestNetworkPolicyCase0AllowedDomainsList =
  Array<unknown>;
export const ForkANamedSandboxRequestNetworkPolicyCase0AllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ForkANamedSandboxRequestNetworkPolicyCase0AllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. Traffic to these addresses bypasses domain-based restrictions. */
export type ForkANamedSandboxRequestNetworkPolicyCase0AllowedCIDRsList =
  Array<unknown>;
export const ForkANamedSandboxRequestNetworkPolicyCase0AllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ForkANamedSandboxRequestNetworkPolicyCase0AllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type ForkANamedSandboxRequestNetworkPolicyCase0DeniedCIDRsList =
  Array<unknown>;
export const ForkANamedSandboxRequestNetworkPolicyCase0DeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ForkANamedSandboxRequestNetworkPolicyCase0DeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. Traffic to matching domains will be intercepted instead of proxied through encrypted connections. */
export type ForkANamedSandboxRequestNetworkPolicyCase0InjectionRulesList =
  Array<unknown>;
export const ForkANamedSandboxRequestNetworkPolicyCase0InjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ForkANamedSandboxRequestNetworkPolicyCase0InjectionRulesList>;

/** Network access policy for the sandbox.\n Controls which external hosts the sandbox can communicate with.\n Use \"allow-all\" mode to allow all traffic, \"deny-all\" to block all traffic or \"custom\" to provide specific rules. */
export interface ForkANamedSandboxRequestNetworkPolicyCase0 {
  /** The network access policy mode. Use \"allow-all\" to permit all outbound traffic. Use \"deny-all\" to block all outbound traffic. Use \"custom\" to specify explicit allow/deny rules. */
  mode: ForkANamedSandboxRequestNetworkPolicyCase0Mode | (string & {});
  /** List of domain names the sandbox is allowed to connect to. Only applies when mode is \"custom\". Supports wildcard patterns (e.g., \"*.example.com\" matches all subdomains). */
  allowedDomains?: ForkANamedSandboxRequestNetworkPolicyCase0AllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. Traffic to these addresses bypasses domain-based restrictions. */
  allowedCIDRs?: ForkANamedSandboxRequestNetworkPolicyCase0AllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: ForkANamedSandboxRequestNetworkPolicyCase0DeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. Traffic to matching domains will be intercepted instead of proxied through encrypted connections. */
  injectionRules?: ForkANamedSandboxRequestNetworkPolicyCase0InjectionRulesList;
}
export const ForkANamedSandboxRequestNetworkPolicyCase0 =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: ForkANamedSandboxRequestNetworkPolicyCase0Mode,
      allowedDomains: S.optional(
        ForkANamedSandboxRequestNetworkPolicyCase0AllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        ForkANamedSandboxRequestNetworkPolicyCase0AllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        ForkANamedSandboxRequestNetworkPolicyCase0DeniedCIDRsList,
      ),
      injectionRules: S.optional(
        ForkANamedSandboxRequestNetworkPolicyCase0InjectionRulesList,
      ),
    }),
  ).annotate({
    identifier: "ForkANamedSandboxRequestNetworkPolicyCase0",
  }) as any as S.Schema<ForkANamedSandboxRequestNetworkPolicyCase0>;

export type ForkANamedSandboxRequestNetworkPolicyCase1 =
  CreateANamedSandboxRequestNetworkPolicyCase1;
export const ForkANamedSandboxRequestNetworkPolicyCase1 =
  CreateANamedSandboxRequestNetworkPolicyCase1;

export type ForkANamedSandboxRequestNetworkPolicy =
  | ForkANamedSandboxRequestNetworkPolicyCase0
  | CreateANamedSandboxRequestNetworkPolicyCase1;
export const ForkANamedSandboxRequestNetworkPolicy =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ForkANamedSandboxRequestNetworkPolicy>;

/** Resources to define the VM */
export type ForkANamedSandboxRequestResources =
  CreateANamedSandboxRequestResources;
export const ForkANamedSandboxRequestResources =
  CreateANamedSandboxRequestResources;

/** List of ports to expose from the sandbox. Each port will be accessible via a unique URL. Maximum of 15 ports can be exposed. */
export type ForkANamedSandboxRequestPortsList = Array<number>;
export const ForkANamedSandboxRequestPortsList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<ForkANamedSandboxRequestPortsList>;

/** Default environment variables for the sandbox. These are inherited by all commands unless overridden. */
export type ForkANamedSandboxRequestEnvMap = {
  [key: string]: string | undefined;
};
export const ForkANamedSandboxRequestEnvMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ForkANamedSandboxRequestEnvMap>;

/** Mount the drive read-only or read-write. */
export type ForkANamedSandboxRequestMountsValueMode =
  | "read-only"
  | "read-write";
export const ForkANamedSandboxRequestMountsValueMode = /*@__PURE__*/ S.String;

export interface ForkANamedSandboxRequestMountsValue {
  /** Name of the drive to mount. The drive must already exist. */
  drive: string;
  /** Mount the drive read-only or read-write. */
  mode?: ForkANamedSandboxRequestMountsValueMode | (string & {});
}
export const ForkANamedSandboxRequestMountsValue = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    drive: S.String,
    mode: S.optional(ForkANamedSandboxRequestMountsValueMode),
  }),
).annotate({
  identifier: "ForkANamedSandboxRequestMountsValue",
}) as any as S.Schema<ForkANamedSandboxRequestMountsValue>;

/** List of drives to mount to the sandbox at the provided path. */
export type ForkANamedSandboxRequestMountsMap = {
  [key: string]: ForkANamedSandboxRequestMountsValue | undefined;
};
export const ForkANamedSandboxRequestMountsMap = /*@__PURE__*/ S.Record(
  S.String,
  ForkANamedSandboxRequestMountsValue,
) as any as S.Schema<ForkANamedSandboxRequestMountsMap>;

/** Default snapshot expiration time in milliseconds. Set to 0 to disable expiration. When set, this value is used as the default expiration for all snapshots created for this sandbox. */
export type ForkANamedSandboxRequestSnapshotExpiration = unknown | number;
export const ForkANamedSandboxRequestSnapshotExpiration =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ForkANamedSandboxRequestSnapshotExpiration>;

/** Expiration time in milliseconds for kept snapshots. Falls back to snapshotExpiration. */
export type ForkANamedSandboxRequestKeepLastSnapshotsExpiration =
  | unknown
  | number;
export const ForkANamedSandboxRequestKeepLastSnapshotsExpiration =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ForkANamedSandboxRequestKeepLastSnapshotsExpiration>;

/** Protect the N most recent snapshots with different expiration/deletion behavior. */
export interface ForkANamedSandboxRequestKeepLastSnapshots {
  /** Number of most recent snapshots to keep. */
  count: number;
  /** Expiration time in milliseconds for kept snapshots. Falls back to snapshotExpiration. */
  expiration?: ForkANamedSandboxRequestKeepLastSnapshotsExpiration;
  /** Whether to immediately delete evicted snapshots. Defaults to true. */
  deleteEvicted?: boolean;
}
export const ForkANamedSandboxRequestKeepLastSnapshots =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      count: S.Number,
      expiration: S.optional(
        ForkANamedSandboxRequestKeepLastSnapshotsExpiration,
      ),
      deleteEvicted: S.optional(S.Boolean),
    }),
  ).annotate({
    identifier: "ForkANamedSandboxRequestKeepLastSnapshots",
  }) as any as S.Schema<ForkANamedSandboxRequestKeepLastSnapshots>;

/** Key-value tags to associate with the sandbox. Maximum 5 tags. */
export type ForkANamedSandboxRequestTagsMap = {
  [key: string]: string | undefined;
};
export const ForkANamedSandboxRequestTagsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ForkANamedSandboxRequestTagsMap>;

export interface ForkANamedSandboxRequest {
  /** Name of the source sandbox to fork. */
  name: string;
  /** The ID of the project the source sandbox belongs to. Required unless authenticating with an OIDC token. */
  projectId?: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  networkPolicy?: ForkANamedSandboxRequestNetworkPolicy;
  /** Resources to define the VM */
  resources?: CreateANamedSandboxRequestResources;
  /** List of ports to expose from the sandbox. Each port will be accessible via a unique URL. Maximum of 15 ports can be exposed. */
  ports?: ForkANamedSandboxRequestPortsList;
  /** Image to use for the sandbox. */
  image?: string;
  /** Maximum duration in milliseconds that the sandbox can run before being automatically stopped. */
  timeout?: number;
  /** Default environment variables for the sandbox. These are inherited by all commands unless overridden. */
  env?: ForkANamedSandboxRequestEnvMap;
  /** List of drives to mount to the sandbox at the provided path. */
  mounts?: ForkANamedSandboxRequestMountsMap;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. Defaults to the source sandbox setting. */
  persistent?: boolean;
  /** Default snapshot expiration time in milliseconds. Set to 0 to disable expiration. When set, this value is used as the default expiration for all snapshots created for this sandbox. */
  snapshotExpiration?: ForkANamedSandboxRequestSnapshotExpiration;
  /** Protect the N most recent snapshots with different expiration/deletion behavior. */
  keepLastSnapshots?: ForkANamedSandboxRequestKeepLastSnapshots;
  /** Key-value tags to associate with the sandbox. Maximum 5 tags. */
  tags?: ForkANamedSandboxRequestTagsMap;
}
export const ForkANamedSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    projectId: S.optional(S.String.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    networkPolicy: S.optional(ForkANamedSandboxRequestNetworkPolicy),
    resources: S.optional(CreateANamedSandboxRequestResources),
    ports: S.optional(ForkANamedSandboxRequestPortsList),
    image: S.optional(S.String),
    timeout: S.optional(S.Number),
    env: S.optional(ForkANamedSandboxRequestEnvMap),
    mounts: S.optional(ForkANamedSandboxRequestMountsMap),
    persistent: S.optional(S.Boolean),
    snapshotExpiration: S.optional(ForkANamedSandboxRequestSnapshotExpiration),
    keepLastSnapshots: S.optional(ForkANamedSandboxRequestKeepLastSnapshots),
    tags: S.optional(ForkANamedSandboxRequestTagsMap),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/sandboxes/{name}/fork", code: 200 }),
  ),
).annotate({
  identifier: "ForkANamedSandboxRequest",
}) as any as S.Schema<ForkANamedSandboxRequest>;

/** The status of the current sandbox. */
export type ForkANamedSandboxResponseSandboxStatus =
  | "running"
  | "stopped"
  | "stopping";
export const ForkANamedSandboxResponseSandboxStatus = /*@__PURE__*/ S.String;

/** Keep-last snapshot configuration. */
export type ForkANamedSandboxResponseSandboxKeepLastSnapshots =
  CreateANamedSandboxResponseSandboxKeepLastSnapshots;
export const ForkANamedSandboxResponseSandboxKeepLastSnapshots =
  CreateANamedSandboxResponseSandboxKeepLastSnapshots;

export type ForkANamedSandboxResponseSandboxNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "default-allow"
  | "default-deny"
  | "deny-all";
export const ForkANamedSandboxResponseSandboxNetworkPolicyMode =
  /*@__PURE__*/ S.String;

export type ForkANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const ForkANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ForkANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList>;

export type ForkANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const ForkANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ForkANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList>;

export type ForkANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const ForkANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ForkANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList>;

/** Network policy configuration. */
export interface ForkANamedSandboxResponseSandboxNetworkPolicy {
  mode: ForkANamedSandboxResponseSandboxNetworkPolicyMode;
  allowedDomains?: ForkANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList;
  allowedCIDRs?: ForkANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList;
  deniedCIDRs?: ForkANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList;
}
export const ForkANamedSandboxResponseSandboxNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: ForkANamedSandboxResponseSandboxNetworkPolicyMode,
      allowedDomains: S.optional(
        ForkANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        ForkANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        ForkANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList,
      ),
    }),
  ).annotate({
    identifier: "ForkANamedSandboxResponseSandboxNetworkPolicy",
  }) as any as S.Schema<ForkANamedSandboxResponseSandboxNetworkPolicy>;

/** Key-value tags attached to the named sandbox. */
export type ForkANamedSandboxResponseSandboxTagsMap = {
  [key: string]: string | undefined;
};
export const ForkANamedSandboxResponseSandboxTagsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ForkANamedSandboxResponseSandboxTagsMap>;

/** Key-value pairs of mount path and drive. */
export type ForkANamedSandboxResponseSandboxMountsMap = {
  [key: string]: unknown | undefined;
};
export const ForkANamedSandboxResponseSandboxMountsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ForkANamedSandboxResponseSandboxMountsMap>;

/** This object contains information related to a Vercel NamedSandbox. */
export interface ForkANamedSandboxResponseSandbox {
  /** The unique identifier of the sandbox. */
  name: string;
  /** Current snapshot ID that the named sandbox is pointing to. */
  currentSnapshotId?: string;
  /** Current session ID the sandbox is pointing to. */
  currentSessionId: string;
  /** The status of the current sandbox. */
  status: ForkANamedSandboxResponseSandboxStatus;
  /** The time when the sandbox status was last updated, in milliseconds since the epoch. */
  statusUpdatedAt: number;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. */
  persistent: boolean;
  /** The region the sandbox is configured to run in: the region set on the sandbox, otherwise the project-level default, then the platform default. Where a running session actually landed is reported by `session.region`. */
  region?: string;
  /** Number of virtual CPUs allocated. */
  vcpus?: number;
  /** Memory allocated in MB. */
  memory?: number;
  /** Runtime identifier. */
  runtime?: string;
  /** Digest-pinned reference of the container image the sandbox was created from, when it was created from an image ("{repository}@{manifestDigest}"). */
  image?: string;
  /** Timeout in milliseconds. */
  timeout?: number;
  /** Default snapshot expiration time in milliseconds. 0 means no expiration. */
  snapshotExpiration?: number;
  /** Keep-last snapshot configuration. */
  keepLastSnapshots?: CreateANamedSandboxResponseSandboxKeepLastSnapshots;
  /** Network policy configuration. */
  networkPolicy?: ForkANamedSandboxResponseSandboxNetworkPolicy;
  /** Cumulative egress bytes across all sandbox runs. */
  totalEgressBytes?: number;
  /** Cumulative ingress bytes across all sandbox runs. */
  totalIngressBytes?: number;
  /** Cumulative active CPU duration in milliseconds across all sandbox runs. */
  totalActiveCpuDurationMs?: number;
  /** Cumulative wall-clock duration in milliseconds across all sandbox runs. */
  totalDurationMs?: number;
  /** The working directory of the sandbox. */
  cwd?: string;
  /** Key-value tags attached to the named sandbox. */
  tags?: ForkANamedSandboxResponseSandboxTagsMap;
  /** Key-value pairs of mount path and drive. */
  mounts?: ForkANamedSandboxResponseSandboxMountsMap;
  /** The time when the named sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The time when the named sandbox was last updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The time at which the currently running sandbox will time out, in milliseconds since the epoch. Only present while a session is running. */
  expiresAt?: number;
}
export const ForkANamedSandboxResponseSandbox = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    currentSnapshotId: S.optional(S.String),
    currentSessionId: S.String,
    status: ForkANamedSandboxResponseSandboxStatus,
    statusUpdatedAt: S.Number,
    persistent: S.Boolean,
    region: S.optional(S.String),
    vcpus: S.optional(S.Number),
    memory: S.optional(S.Number),
    runtime: S.optional(S.String),
    image: S.optional(S.String),
    timeout: S.optional(S.Number),
    snapshotExpiration: S.optional(S.Number),
    keepLastSnapshots: S.optional(
      CreateANamedSandboxResponseSandboxKeepLastSnapshots,
    ),
    networkPolicy: S.optional(ForkANamedSandboxResponseSandboxNetworkPolicy),
    totalEgressBytes: S.optional(S.Number),
    totalIngressBytes: S.optional(S.Number),
    totalActiveCpuDurationMs: S.optional(S.Number),
    totalDurationMs: S.optional(S.Number),
    cwd: S.optional(S.String),
    tags: S.optional(ForkANamedSandboxResponseSandboxTagsMap),
    mounts: S.optional(ForkANamedSandboxResponseSandboxMountsMap),
    createdAt: S.Number,
    updatedAt: S.Number,
    expiresAt: S.optional(S.Number),
  }),
).annotate({
  identifier: "ForkANamedSandboxResponseSandbox",
}) as any as S.Schema<ForkANamedSandboxResponseSandbox>;

/** The status of the sandbox. */
export type ForkANamedSandboxResponseSessionStatus =
  | "aborted"
  | "failed"
  | "pending"
  | "running"
  | "snapshotting"
  | "stopped"
  | "stopping";
export const ForkANamedSandboxResponseSessionStatus = /*@__PURE__*/ S.String;

/** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
export type ForkANamedSandboxResponseSessionNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "deny-all";
export const ForkANamedSandboxResponseSessionNetworkPolicyMode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
export type ForkANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const ForkANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ForkANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
export type ForkANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const ForkANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ForkANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type ForkANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const ForkANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ForkANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. */
export type ForkANamedSandboxResponseSessionNetworkPolicyInjectionRulesList =
  Array<unknown>;
export const ForkANamedSandboxResponseSessionNetworkPolicyInjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ForkANamedSandboxResponseSessionNetworkPolicyInjectionRulesList>;

/** The network policy applied to this sandbox, if any. */
export interface ForkANamedSandboxResponseSessionNetworkPolicy {
  /** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
  mode: ForkANamedSandboxResponseSessionNetworkPolicyMode;
  /** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
  allowedDomains?: ForkANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
  allowedCIDRs?: ForkANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: ForkANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. */
  injectionRules?: ForkANamedSandboxResponseSessionNetworkPolicyInjectionRulesList;
}
export const ForkANamedSandboxResponseSessionNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: ForkANamedSandboxResponseSessionNetworkPolicyMode,
      allowedDomains: S.optional(
        ForkANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        ForkANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        ForkANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList,
      ),
      injectionRules: S.optional(
        ForkANamedSandboxResponseSessionNetworkPolicyInjectionRulesList,
      ),
    }),
  ).annotate({
    identifier: "ForkANamedSandboxResponseSessionNetworkPolicy",
  }) as any as S.Schema<ForkANamedSandboxResponseSessionNetworkPolicy>;

/** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
export type ForkANamedSandboxResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;
export const ForkANamedSandboxResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;

/** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
export interface ForkANamedSandboxResponseSession {
  /** The name of the source sandbox. */
  sourceSandboxName: string;
  /** The unique identifier of the project associated with this session. */
  projectId: string;
  /** The unique identifier of the sandbox. */
  id: string;
  /** Memory allocated to this sandbox in MB. */
  memory: number;
  /** Number of vCPUs allocated to this sandbox. */
  vcpus: number;
  /** The region where the sandbox is hosted. */
  region: string;
  /** The runtime of the sandbox. */
  runtime: string;
  /** The maximum amount of time the sandbox will run for in milliseconds. */
  timeout: number;
  /** The status of the sandbox. */
  status: ForkANamedSandboxResponseSessionStatus;
  /** The time when the sandbox was requested, in milliseconds since the epoch. */
  requestedAt: number;
  /** The time when the sandbox was started, in milliseconds since the epoch. */
  startedAt?: number;
  /** The working directory of the sandbox. */
  cwd: string;
  /** The time when the sandbox was requested to stop, in milliseconds since the epoch. */
  requestedStopAt?: number;
  /** The time when the sandbox was stopped, in milliseconds since the epoch. */
  stoppedAt?: number;
  /** The time when the sandbox was aborted, in milliseconds since the epoch. */
  abortedAt?: number;
  /** The duration of the sandbox in milliseconds. */
  duration?: number;
  /** The unique identifier of the snapshot associated with this sandbox, if any. */
  sourceSnapshotId?: string;
  /** The time when a snapshot was requested, in milliseconds since the epoch. */
  snapshottedAt?: number;
  /** The time when the sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the sandbox was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The network policy applied to this sandbox, if any. */
  networkPolicy?: ForkANamedSandboxResponseSessionNetworkPolicy;
  /** The amount of CPU time the sandbox consumed, if available, in milliseconds. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  activeCpuDurationMs?: number;
  /** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  networkTransfer?: CreateANamedSandboxResponseSessionNetworkTransfer;
}
export const ForkANamedSandboxResponseSession = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sourceSandboxName: S.String,
    projectId: S.String,
    id: S.String,
    memory: S.Number,
    vcpus: S.Number,
    region: S.String,
    runtime: S.String,
    timeout: S.Number,
    status: ForkANamedSandboxResponseSessionStatus,
    requestedAt: S.Number,
    startedAt: S.optional(S.Number),
    cwd: S.String,
    requestedStopAt: S.optional(S.Number),
    stoppedAt: S.optional(S.Number),
    abortedAt: S.optional(S.Number),
    duration: S.optional(S.Number),
    sourceSnapshotId: S.optional(S.String),
    snapshottedAt: S.optional(S.Number),
    createdAt: S.Number,
    updatedAt: S.Number,
    networkPolicy: S.optional(ForkANamedSandboxResponseSessionNetworkPolicy),
    activeCpuDurationMs: S.optional(S.Number),
    networkTransfer: S.optional(
      CreateANamedSandboxResponseSessionNetworkTransfer,
    ),
  }),
).annotate({
  identifier: "ForkANamedSandboxResponseSession",
}) as any as S.Schema<ForkANamedSandboxResponseSession>;

/** This object represents a public route in a Vercel Sandbox. */
export type ForkANamedSandboxResponseRoutesItem =
  CreateANamedSandboxResponseRoutesItem;
export const ForkANamedSandboxResponseRoutesItem =
  CreateANamedSandboxResponseRoutesItem;

export type ForkANamedSandboxResponseRoutesList =
  Array<CreateANamedSandboxResponseRoutesItem>;
export const ForkANamedSandboxResponseRoutesList = /*@__PURE__*/ S.Array(
  CreateANamedSandboxResponseRoutesItem,
) as any as S.Schema<ForkANamedSandboxResponseRoutesList>;

export interface ForkANamedSandboxResponse {
  /** This object contains information related to a Vercel NamedSandbox. */
  sandbox: ForkANamedSandboxResponseSandbox;
  /** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
  session: ForkANamedSandboxResponseSession;
  routes: ForkANamedSandboxResponseRoutesList;
}
export const ForkANamedSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandbox: ForkANamedSandboxResponseSandbox,
    session: ForkANamedSandboxResponseSession,
    routes: ForkANamedSandboxResponseRoutesList,
  }),
).annotate({
  identifier: "ForkANamedSandboxResponse",
}) as any as S.Schema<ForkANamedSandboxResponse>;

export type GetACommandRequestWait = "true" | "false";
export const GetACommandRequestWait = /*@__PURE__*/ S.String;

export interface GetACommandRequest {
  /** The unique identifier of the session containing the command. */
  sessionId: string;
  /** The unique identifier of the command to retrieve. */
  cmdId: string;
  /** If set to "true", the request will block until the command finishes execution. Useful for synchronously waiting for command completion. */
  wait?: GetACommandRequestWait | (string & {});
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const GetACommandRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    cmdId: S.String.pipe(T.Label()),
    wait: S.optional(GetACommandRequestWait.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/sandboxes/sessions/{sessionId}/cmd/{cmdId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetACommandRequest",
}) as any as S.Schema<GetACommandRequest>;

/** The arguments of the command. */
export type GetACommandResponseCommandArgsList = Array<string>;
export const GetACommandResponseCommandArgsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<GetACommandResponseCommandArgsList>;

/** This object represents a command run in a Vercel Sandbox session (v2 API). */
export interface GetACommandResponseCommand {
  /** The ID of the command. */
  id: string;
  /** The name of the command. */
  name: string;
  /** The arguments of the command. */
  args: GetACommandResponseCommandArgsList;
  /** The current working directory of the command. */
  cwd: string;
  /** The ID of the session associated with the command. */
  sessionId: string;
  /** If the command did finish, the exit code. */
  exitCode: number | null;
  /** When the command was started, in milliseconds since the epoch. */
  startedAt: number;
  /** Duration of the command execution in milliseconds. */
  durationMs?: number;
}
export const GetACommandResponseCommand = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    args: GetACommandResponseCommandArgsList,
    cwd: S.String,
    sessionId: S.String,
    exitCode: S.NullOr(S.Number),
    startedAt: S.Number,
    durationMs: S.optional(S.Number),
  }),
).annotate({
  identifier: "GetACommandResponseCommand",
}) as any as S.Schema<GetACommandResponseCommand>;

export interface GetACommandResponse {
  /** This object represents a command run in a Vercel Sandbox session (v2 API). */
  command: GetACommandResponseCommand;
}
export const GetACommandResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    command: GetACommandResponseCommand,
  }),
).annotate({
  identifier: "GetACommandResponse",
}) as any as S.Schema<GetACommandResponse>;

export interface GetANamedSandboxRequest {
  /** Name for the sandbox. Must be unique per project and URL-safe (alphanumeric, hyphens, underscores). */
  name: string;
  /** The project ID or name (required when not using OIDC token). */
  projectId?: string;
  /** Whether to automatically resume a stopped named sandbox by creating a new instance from its snapshot. Defaults to false. */
  resume?: boolean;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const GetANamedSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    projectId: S.optional(S.String.pipe(T.Query())),
    resume: S.optional(S.Boolean.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v2/sandboxes/{name}", code: 200 })),
).annotate({
  identifier: "GetANamedSandboxRequest",
}) as any as S.Schema<GetANamedSandboxRequest>;

/** The status of the current sandbox. */
export type GetANamedSandboxResponseSandboxStatus =
  | "running"
  | "stopped"
  | "stopping";
export const GetANamedSandboxResponseSandboxStatus = /*@__PURE__*/ S.String;

/** Keep-last snapshot configuration. */
export type GetANamedSandboxResponseSandboxKeepLastSnapshots =
  CreateANamedSandboxResponseSandboxKeepLastSnapshots;
export const GetANamedSandboxResponseSandboxKeepLastSnapshots =
  CreateANamedSandboxResponseSandboxKeepLastSnapshots;

export type GetANamedSandboxResponseSandboxNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "default-allow"
  | "default-deny"
  | "deny-all";
export const GetANamedSandboxResponseSandboxNetworkPolicyMode =
  /*@__PURE__*/ S.String;

export type GetANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const GetANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<GetANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList>;

export type GetANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const GetANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<GetANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList>;

export type GetANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const GetANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<GetANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList>;

/** Network policy configuration. */
export interface GetANamedSandboxResponseSandboxNetworkPolicy {
  mode: GetANamedSandboxResponseSandboxNetworkPolicyMode;
  allowedDomains?: GetANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList;
  allowedCIDRs?: GetANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList;
  deniedCIDRs?: GetANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList;
}
export const GetANamedSandboxResponseSandboxNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: GetANamedSandboxResponseSandboxNetworkPolicyMode,
      allowedDomains: S.optional(
        GetANamedSandboxResponseSandboxNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        GetANamedSandboxResponseSandboxNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        GetANamedSandboxResponseSandboxNetworkPolicyDeniedCIDRsList,
      ),
    }),
  ).annotate({
    identifier: "GetANamedSandboxResponseSandboxNetworkPolicy",
  }) as any as S.Schema<GetANamedSandboxResponseSandboxNetworkPolicy>;

/** Key-value tags attached to the named sandbox. */
export type GetANamedSandboxResponseSandboxTagsMap = {
  [key: string]: string | undefined;
};
export const GetANamedSandboxResponseSandboxTagsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<GetANamedSandboxResponseSandboxTagsMap>;

/** Key-value pairs of mount path and drive. */
export type GetANamedSandboxResponseSandboxMountsMap = {
  [key: string]: unknown | undefined;
};
export const GetANamedSandboxResponseSandboxMountsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<GetANamedSandboxResponseSandboxMountsMap>;

/** This object contains information related to a Vercel NamedSandbox. */
export interface GetANamedSandboxResponseSandbox {
  /** The unique identifier of the sandbox. */
  name: string;
  /** Current snapshot ID that the named sandbox is pointing to. */
  currentSnapshotId?: string;
  /** Current session ID the sandbox is pointing to. */
  currentSessionId: string;
  /** The status of the current sandbox. */
  status: GetANamedSandboxResponseSandboxStatus;
  /** The time when the sandbox status was last updated, in milliseconds since the epoch. */
  statusUpdatedAt: number;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. */
  persistent: boolean;
  /** The region the sandbox is configured to run in: the region set on the sandbox, otherwise the project-level default, then the platform default. Where a running session actually landed is reported by `session.region`. */
  region?: string;
  /** Number of virtual CPUs allocated. */
  vcpus?: number;
  /** Memory allocated in MB. */
  memory?: number;
  /** Runtime identifier. */
  runtime?: string;
  /** Digest-pinned reference of the container image the sandbox was created from, when it was created from an image ("{repository}@{manifestDigest}"). */
  image?: string;
  /** Timeout in milliseconds. */
  timeout?: number;
  /** Default snapshot expiration time in milliseconds. 0 means no expiration. */
  snapshotExpiration?: number;
  /** Keep-last snapshot configuration. */
  keepLastSnapshots?: CreateANamedSandboxResponseSandboxKeepLastSnapshots;
  /** Network policy configuration. */
  networkPolicy?: GetANamedSandboxResponseSandboxNetworkPolicy;
  /** Cumulative egress bytes across all sandbox runs. */
  totalEgressBytes?: number;
  /** Cumulative ingress bytes across all sandbox runs. */
  totalIngressBytes?: number;
  /** Cumulative active CPU duration in milliseconds across all sandbox runs. */
  totalActiveCpuDurationMs?: number;
  /** Cumulative wall-clock duration in milliseconds across all sandbox runs. */
  totalDurationMs?: number;
  /** The working directory of the sandbox. */
  cwd?: string;
  /** Key-value tags attached to the named sandbox. */
  tags?: GetANamedSandboxResponseSandboxTagsMap;
  /** Key-value pairs of mount path and drive. */
  mounts?: GetANamedSandboxResponseSandboxMountsMap;
  /** The time when the named sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The time when the named sandbox was last updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The time at which the currently running sandbox will time out, in milliseconds since the epoch. Only present while a session is running. */
  expiresAt?: number;
}
export const GetANamedSandboxResponseSandbox = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    currentSnapshotId: S.optional(S.String),
    currentSessionId: S.String,
    status: GetANamedSandboxResponseSandboxStatus,
    statusUpdatedAt: S.Number,
    persistent: S.Boolean,
    region: S.optional(S.String),
    vcpus: S.optional(S.Number),
    memory: S.optional(S.Number),
    runtime: S.optional(S.String),
    image: S.optional(S.String),
    timeout: S.optional(S.Number),
    snapshotExpiration: S.optional(S.Number),
    keepLastSnapshots: S.optional(
      CreateANamedSandboxResponseSandboxKeepLastSnapshots,
    ),
    networkPolicy: S.optional(GetANamedSandboxResponseSandboxNetworkPolicy),
    totalEgressBytes: S.optional(S.Number),
    totalIngressBytes: S.optional(S.Number),
    totalActiveCpuDurationMs: S.optional(S.Number),
    totalDurationMs: S.optional(S.Number),
    cwd: S.optional(S.String),
    tags: S.optional(GetANamedSandboxResponseSandboxTagsMap),
    mounts: S.optional(GetANamedSandboxResponseSandboxMountsMap),
    createdAt: S.Number,
    updatedAt: S.Number,
    expiresAt: S.optional(S.Number),
  }),
).annotate({
  identifier: "GetANamedSandboxResponseSandbox",
}) as any as S.Schema<GetANamedSandboxResponseSandbox>;

/** The status of the sandbox. */
export type GetANamedSandboxResponseSessionStatus =
  | "aborted"
  | "failed"
  | "pending"
  | "running"
  | "snapshotting"
  | "stopped"
  | "stopping";
export const GetANamedSandboxResponseSessionStatus = /*@__PURE__*/ S.String;

/** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
export type GetANamedSandboxResponseSessionNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "deny-all";
export const GetANamedSandboxResponseSessionNetworkPolicyMode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
export type GetANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const GetANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<GetANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
export type GetANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const GetANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<GetANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type GetANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const GetANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<GetANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. */
export type GetANamedSandboxResponseSessionNetworkPolicyInjectionRulesList =
  Array<unknown>;
export const GetANamedSandboxResponseSessionNetworkPolicyInjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<GetANamedSandboxResponseSessionNetworkPolicyInjectionRulesList>;

/** The network policy applied to this sandbox, if any. */
export interface GetANamedSandboxResponseSessionNetworkPolicy {
  /** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
  mode: GetANamedSandboxResponseSessionNetworkPolicyMode;
  /** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
  allowedDomains?: GetANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
  allowedCIDRs?: GetANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: GetANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. */
  injectionRules?: GetANamedSandboxResponseSessionNetworkPolicyInjectionRulesList;
}
export const GetANamedSandboxResponseSessionNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: GetANamedSandboxResponseSessionNetworkPolicyMode,
      allowedDomains: S.optional(
        GetANamedSandboxResponseSessionNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        GetANamedSandboxResponseSessionNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        GetANamedSandboxResponseSessionNetworkPolicyDeniedCIDRsList,
      ),
      injectionRules: S.optional(
        GetANamedSandboxResponseSessionNetworkPolicyInjectionRulesList,
      ),
    }),
  ).annotate({
    identifier: "GetANamedSandboxResponseSessionNetworkPolicy",
  }) as any as S.Schema<GetANamedSandboxResponseSessionNetworkPolicy>;

/** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
export type GetANamedSandboxResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;
export const GetANamedSandboxResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;

/** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
export interface GetANamedSandboxResponseSession {
  /** The name of the source sandbox. */
  sourceSandboxName: string;
  /** The unique identifier of the project associated with this session. */
  projectId: string;
  /** The unique identifier of the sandbox. */
  id: string;
  /** Memory allocated to this sandbox in MB. */
  memory: number;
  /** Number of vCPUs allocated to this sandbox. */
  vcpus: number;
  /** The region where the sandbox is hosted. */
  region: string;
  /** The runtime of the sandbox. */
  runtime: string;
  /** The maximum amount of time the sandbox will run for in milliseconds. */
  timeout: number;
  /** The status of the sandbox. */
  status: GetANamedSandboxResponseSessionStatus;
  /** The time when the sandbox was requested, in milliseconds since the epoch. */
  requestedAt: number;
  /** The time when the sandbox was started, in milliseconds since the epoch. */
  startedAt?: number;
  /** The working directory of the sandbox. */
  cwd: string;
  /** The time when the sandbox was requested to stop, in milliseconds since the epoch. */
  requestedStopAt?: number;
  /** The time when the sandbox was stopped, in milliseconds since the epoch. */
  stoppedAt?: number;
  /** The time when the sandbox was aborted, in milliseconds since the epoch. */
  abortedAt?: number;
  /** The duration of the sandbox in milliseconds. */
  duration?: number;
  /** The unique identifier of the snapshot associated with this sandbox, if any. */
  sourceSnapshotId?: string;
  /** The time when a snapshot was requested, in milliseconds since the epoch. */
  snapshottedAt?: number;
  /** The time when the sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the sandbox was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The network policy applied to this sandbox, if any. */
  networkPolicy?: GetANamedSandboxResponseSessionNetworkPolicy;
  /** The amount of CPU time the sandbox consumed, if available, in milliseconds. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  activeCpuDurationMs?: number;
  /** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  networkTransfer?: CreateANamedSandboxResponseSessionNetworkTransfer;
}
export const GetANamedSandboxResponseSession = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sourceSandboxName: S.String,
    projectId: S.String,
    id: S.String,
    memory: S.Number,
    vcpus: S.Number,
    region: S.String,
    runtime: S.String,
    timeout: S.Number,
    status: GetANamedSandboxResponseSessionStatus,
    requestedAt: S.Number,
    startedAt: S.optional(S.Number),
    cwd: S.String,
    requestedStopAt: S.optional(S.Number),
    stoppedAt: S.optional(S.Number),
    abortedAt: S.optional(S.Number),
    duration: S.optional(S.Number),
    sourceSnapshotId: S.optional(S.String),
    snapshottedAt: S.optional(S.Number),
    createdAt: S.Number,
    updatedAt: S.Number,
    networkPolicy: S.optional(GetANamedSandboxResponseSessionNetworkPolicy),
    activeCpuDurationMs: S.optional(S.Number),
    networkTransfer: S.optional(
      CreateANamedSandboxResponseSessionNetworkTransfer,
    ),
  }),
).annotate({
  identifier: "GetANamedSandboxResponseSession",
}) as any as S.Schema<GetANamedSandboxResponseSession>;

/** This object represents a public route in a Vercel Sandbox. */
export type GetANamedSandboxResponseRoutesItem =
  CreateANamedSandboxResponseRoutesItem;
export const GetANamedSandboxResponseRoutesItem =
  CreateANamedSandboxResponseRoutesItem;

export type GetANamedSandboxResponseRoutesList =
  Array<CreateANamedSandboxResponseRoutesItem>;
export const GetANamedSandboxResponseRoutesList = /*@__PURE__*/ S.Array(
  CreateANamedSandboxResponseRoutesItem,
) as any as S.Schema<GetANamedSandboxResponseRoutesList>;

export interface GetANamedSandboxResponse {
  /** This object contains information related to a Vercel NamedSandbox. */
  sandbox: GetANamedSandboxResponseSandbox;
  /** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
  session: GetANamedSandboxResponseSession;
  routes: GetANamedSandboxResponseRoutesList;
  resumed: boolean;
}
export const GetANamedSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandbox: GetANamedSandboxResponseSandbox,
    session: GetANamedSandboxResponseSession,
    routes: GetANamedSandboxResponseRoutesList,
    resumed: S.Boolean,
  }),
).annotate({
  identifier: "GetANamedSandboxResponse",
}) as any as S.Schema<GetANamedSandboxResponse>;

export interface GetASessionRequest {
  /** The unique identifier of the session to retrieve. */
  sessionId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const GetASessionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/sandboxes/sessions/{sessionId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetASessionRequest",
}) as any as S.Schema<GetASessionRequest>;

/** The status of the sandbox. */
export type GetASessionResponseSessionStatus =
  | "aborted"
  | "failed"
  | "pending"
  | "running"
  | "snapshotting"
  | "stopped"
  | "stopping";
export const GetASessionResponseSessionStatus = /*@__PURE__*/ S.String;

/** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
export type GetASessionResponseSessionNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "deny-all";
export const GetASessionResponseSessionNetworkPolicyMode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
export type GetASessionResponseSessionNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const GetASessionResponseSessionNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<GetASessionResponseSessionNetworkPolicyAllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
export type GetASessionResponseSessionNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const GetASessionResponseSessionNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<GetASessionResponseSessionNetworkPolicyAllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type GetASessionResponseSessionNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const GetASessionResponseSessionNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<GetASessionResponseSessionNetworkPolicyDeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. */
export type GetASessionResponseSessionNetworkPolicyInjectionRulesList =
  Array<unknown>;
export const GetASessionResponseSessionNetworkPolicyInjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<GetASessionResponseSessionNetworkPolicyInjectionRulesList>;

/** The network policy applied to this sandbox, if any. */
export interface GetASessionResponseSessionNetworkPolicy {
  /** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
  mode: GetASessionResponseSessionNetworkPolicyMode;
  /** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
  allowedDomains?: GetASessionResponseSessionNetworkPolicyAllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
  allowedCIDRs?: GetASessionResponseSessionNetworkPolicyAllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: GetASessionResponseSessionNetworkPolicyDeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. */
  injectionRules?: GetASessionResponseSessionNetworkPolicyInjectionRulesList;
}
export const GetASessionResponseSessionNetworkPolicy = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      mode: GetASessionResponseSessionNetworkPolicyMode,
      allowedDomains: S.optional(
        GetASessionResponseSessionNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        GetASessionResponseSessionNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        GetASessionResponseSessionNetworkPolicyDeniedCIDRsList,
      ),
      injectionRules: S.optional(
        GetASessionResponseSessionNetworkPolicyInjectionRulesList,
      ),
    }),
).annotate({
  identifier: "GetASessionResponseSessionNetworkPolicy",
}) as any as S.Schema<GetASessionResponseSessionNetworkPolicy>;

/** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
export type GetASessionResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;
export const GetASessionResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;

/** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
export interface GetASessionResponseSession {
  /** The name of the source sandbox. */
  sourceSandboxName: string;
  /** The unique identifier of the project associated with this session. */
  projectId: string;
  /** The unique identifier of the sandbox. */
  id: string;
  /** Memory allocated to this sandbox in MB. */
  memory: number;
  /** Number of vCPUs allocated to this sandbox. */
  vcpus: number;
  /** The region where the sandbox is hosted. */
  region: string;
  /** The runtime of the sandbox. */
  runtime: string;
  /** The maximum amount of time the sandbox will run for in milliseconds. */
  timeout: number;
  /** The status of the sandbox. */
  status: GetASessionResponseSessionStatus;
  /** The time when the sandbox was requested, in milliseconds since the epoch. */
  requestedAt: number;
  /** The time when the sandbox was started, in milliseconds since the epoch. */
  startedAt?: number;
  /** The working directory of the sandbox. */
  cwd: string;
  /** The time when the sandbox was requested to stop, in milliseconds since the epoch. */
  requestedStopAt?: number;
  /** The time when the sandbox was stopped, in milliseconds since the epoch. */
  stoppedAt?: number;
  /** The time when the sandbox was aborted, in milliseconds since the epoch. */
  abortedAt?: number;
  /** The duration of the sandbox in milliseconds. */
  duration?: number;
  /** The unique identifier of the snapshot associated with this sandbox, if any. */
  sourceSnapshotId?: string;
  /** The time when a snapshot was requested, in milliseconds since the epoch. */
  snapshottedAt?: number;
  /** The time when the sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the sandbox was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The network policy applied to this sandbox, if any. */
  networkPolicy?: GetASessionResponseSessionNetworkPolicy;
  /** The amount of CPU time the sandbox consumed, if available, in milliseconds. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  activeCpuDurationMs?: number;
  /** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  networkTransfer?: CreateANamedSandboxResponseSessionNetworkTransfer;
}
export const GetASessionResponseSession = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sourceSandboxName: S.String,
    projectId: S.String,
    id: S.String,
    memory: S.Number,
    vcpus: S.Number,
    region: S.String,
    runtime: S.String,
    timeout: S.Number,
    status: GetASessionResponseSessionStatus,
    requestedAt: S.Number,
    startedAt: S.optional(S.Number),
    cwd: S.String,
    requestedStopAt: S.optional(S.Number),
    stoppedAt: S.optional(S.Number),
    abortedAt: S.optional(S.Number),
    duration: S.optional(S.Number),
    sourceSnapshotId: S.optional(S.String),
    snapshottedAt: S.optional(S.Number),
    createdAt: S.Number,
    updatedAt: S.Number,
    networkPolicy: S.optional(GetASessionResponseSessionNetworkPolicy),
    activeCpuDurationMs: S.optional(S.Number),
    networkTransfer: S.optional(
      CreateANamedSandboxResponseSessionNetworkTransfer,
    ),
  }),
).annotate({
  identifier: "GetASessionResponseSession",
}) as any as S.Schema<GetASessionResponseSession>;

/** This object represents a public route in a Vercel Sandbox. */
export type GetASessionResponseRoutesItem =
  CreateANamedSandboxResponseRoutesItem;
export const GetASessionResponseRoutesItem =
  CreateANamedSandboxResponseRoutesItem;

export type GetASessionResponseRoutesList =
  Array<CreateANamedSandboxResponseRoutesItem>;
export const GetASessionResponseRoutesList = /*@__PURE__*/ S.Array(
  CreateANamedSandboxResponseRoutesItem,
) as any as S.Schema<GetASessionResponseRoutesList>;

export interface GetASessionResponse {
  /** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
  session: GetASessionResponseSession;
  routes: GetASessionResponseRoutesList;
}
export const GetASessionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    session: GetASessionResponseSession,
    routes: GetASessionResponseRoutesList,
  }),
).annotate({
  identifier: "GetASessionResponse",
}) as any as S.Schema<GetASessionResponse>;

export interface GetASnapshotRequest {
  /** The unique identifier of the snapshot to retrieve. */
  snapshotId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const GetASnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/sandboxes/snapshots/{snapshotId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetASnapshotRequest",
}) as any as S.Schema<GetASnapshotRequest>;

/** The status of the snapshot. */
export type GetASnapshotResponseSnapshotStatus =
  | "created"
  | "deleted"
  | "failed";
export const GetASnapshotResponseSnapshotStatus = /*@__PURE__*/ S.String;

/** The method used to create the snapshot. */
export type GetASnapshotResponseSnapshotCreationMethod = "automatic" | "manual";
export const GetASnapshotResponseSnapshotCreationMethod =
  /*@__PURE__*/ S.String;

/** This object contains information related to a Snapshot of a Vercel Sandbox session (v2 API). */
export interface GetASnapshotResponseSnapshot {
  /** The unique identifier of the snapshot. */
  id: string;
  /** The unique identifier of the session from which the snapshot was created. */
  sourceSessionId: string;
  /** The region where the snapshot is stored. */
  region?: string;
  /** The status of the snapshot. */
  status: GetASnapshotResponseSnapshotStatus;
  /** The size of the snapshot in bytes. */
  sizeBytes: number;
  /** The time when the snapshot will expire, in milliseconds since the epoch. If not set, the snapshot does not have any expiration. */
  expiresAt?: number;
  /** The time when the snapshot was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the snapshot was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The last time the snapshot was used (e.g. to resume or create a sandbox), in milliseconds since the epoch. Falls back to `createdAt` for older snapshots that predate this field. */
  lastUsedAt: number;
  /** The method used to create the snapshot. */
  creationMethod?: GetASnapshotResponseSnapshotCreationMethod;
  /** The unique identifier of the parent snapshot, if this snapshot was created from another snapshot. */
  parentId?: string;
}
export const GetASnapshotResponseSnapshot = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    sourceSessionId: S.String,
    region: S.optional(S.String),
    status: GetASnapshotResponseSnapshotStatus,
    sizeBytes: S.Number,
    expiresAt: S.optional(S.Number),
    createdAt: S.Number,
    updatedAt: S.Number,
    lastUsedAt: S.Number,
    creationMethod: S.optional(GetASnapshotResponseSnapshotCreationMethod),
    parentId: S.optional(S.String),
  }),
).annotate({
  identifier: "GetASnapshotResponseSnapshot",
}) as any as S.Schema<GetASnapshotResponseSnapshot>;

export interface GetASnapshotResponse {
  /** This object contains information related to a Snapshot of a Vercel Sandbox session (v2 API). */
  snapshot: GetASnapshotResponseSnapshot;
}
export const GetASnapshotResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshot: GetASnapshotResponseSnapshot,
  }),
).annotate({
  identifier: "GetASnapshotResponse",
}) as any as S.Schema<GetASnapshotResponse>;

/** Region where the drive is stored. Defaults to iad1. */
export type GetOrCreateADriveRequestRegion = "iad1" | "sfo1" | "cle1";
export const GetOrCreateADriveRequestRegion = /*@__PURE__*/ S.String;

export interface GetOrCreateADriveRequest {
  /** Name for the drive. Must be unique per project and URL-safe (alphanumeric, hyphens, underscores). */
  name: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  /** The project ID or name to associate the drive with. Required unless using a Vercel OIDC token scoped to a project. */
  projectId?: string;
  /** Maximum drive size in bytes. Defaults to 100 GiB when omitted. */
  maxSizeBytes?: number;
  /** Region where the drive is stored. Defaults to iad1. */
  region?: GetOrCreateADriveRequestRegion | (string & {});
}
export const GetOrCreateADriveRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    projectId: S.optional(S.String),
    maxSizeBytes: S.optional(S.Number),
    region: S.optional(GetOrCreateADriveRequestRegion),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/sandboxes/drives/{name}", code: 200 }),
  ),
).annotate({
  identifier: "GetOrCreateADriveRequest",
}) as any as S.Schema<GetOrCreateADriveRequest>;

/** This object contains information related to a Vercel Sandbox Drive. */
export type GetOrCreateADriveResponseDrive = DeleteADriveResponseDrive;
export const GetOrCreateADriveResponseDrive = DeleteADriveResponseDrive;

export interface GetOrCreateADriveResponse {
  /** This object contains information related to a Vercel Sandbox Drive. */
  drive: DeleteADriveResponseDrive;
}
export const GetOrCreateADriveResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    drive: DeleteADriveResponseDrive,
  }),
).annotate({
  identifier: "GetOrCreateADriveResponse",
}) as any as S.Schema<GetOrCreateADriveResponse>;

export interface KillACommandRequest {
  /** The unique identifier of the session containing the command. */
  sessionId: string;
  /** The unique identifier of the command to terminate. */
  cmdId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  /** The POSIX signal number to send to the process. Common values: 15 (SIGTERM) for graceful termination, 9 (SIGKILL) for forced termination. */
  signal: number;
}
export const KillACommandRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    cmdId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    signal: S.Number,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/sandboxes/sessions/{sessionId}/cmd/{cmdId}/kill",
      code: 200,
    }),
  ),
).annotate({
  identifier: "KillACommandRequest",
}) as any as S.Schema<KillACommandRequest>;

/** The arguments of the command. */
export type KillACommandResponseCommandArgsList = Array<string>;
export const KillACommandResponseCommandArgsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KillACommandResponseCommandArgsList>;

/** This object represents a command run in a Vercel Sandbox session (v2 API). */
export interface KillACommandResponseCommand {
  /** The ID of the command. */
  id: string;
  /** The name of the command. */
  name: string;
  /** The arguments of the command. */
  args: KillACommandResponseCommandArgsList;
  /** The current working directory of the command. */
  cwd: string;
  /** The ID of the session associated with the command. */
  sessionId: string;
  /** If the command did finish, the exit code. */
  exitCode: number | null;
  /** When the command was started, in milliseconds since the epoch. */
  startedAt: number;
  /** Duration of the command execution in milliseconds. */
  durationMs?: number;
}
export const KillACommandResponseCommand = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    args: KillACommandResponseCommandArgsList,
    cwd: S.String,
    sessionId: S.String,
    exitCode: S.NullOr(S.Number),
    startedAt: S.Number,
    durationMs: S.optional(S.Number),
  }),
).annotate({
  identifier: "KillACommandResponseCommand",
}) as any as S.Schema<KillACommandResponseCommand>;

export interface KillACommandResponse {
  /** This object represents a command run in a Vercel Sandbox session (v2 API). */
  command: KillACommandResponseCommand;
}
export const KillACommandResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    command: KillACommandResponseCommand,
  }),
).annotate({
  identifier: "KillACommandResponse",
}) as any as S.Schema<KillACommandResponse>;

export interface ListCommandsRequest {
  /** The unique identifier of the session to list commands for. */
  sessionId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const ListCommandsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/sandboxes/sessions/{sessionId}/cmd",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListCommandsRequest",
}) as any as S.Schema<ListCommandsRequest>;

/** The arguments of the command. */
export type ListCommandsResponseCommandsItemArgsList = Array<unknown>;
export const ListCommandsResponseCommandsItemArgsList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<ListCommandsResponseCommandsItemArgsList>;

/** This object represents a command run in a Vercel Sandbox session (v2 API). */
export interface ListCommandsResponseCommandsItem {
  /** The ID of the command. */
  id: string;
  /** The name of the command. */
  name: string;
  /** The arguments of the command. */
  args: ListCommandsResponseCommandsItemArgsList;
  /** The current working directory of the command. */
  cwd: string;
  /** The ID of the session associated with the command. */
  sessionId: string;
  /** If the command did finish, the exit code. */
  exitCode: number | null;
  /** When the command was started, in milliseconds since the epoch. */
  startedAt: number;
  /** Duration of the command execution in milliseconds. */
  durationMs?: number;
}
export const ListCommandsResponseCommandsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    args: ListCommandsResponseCommandsItemArgsList,
    cwd: S.String,
    sessionId: S.String,
    exitCode: S.NullOr(S.Number),
    startedAt: S.Number,
    durationMs: S.optional(S.Number),
  }),
).annotate({
  identifier: "ListCommandsResponseCommandsItem",
}) as any as S.Schema<ListCommandsResponseCommandsItem>;

export type ListCommandsResponseCommandsList =
  Array<ListCommandsResponseCommandsItem>;
export const ListCommandsResponseCommandsList = /*@__PURE__*/ S.Array(
  ListCommandsResponseCommandsItem,
) as any as S.Schema<ListCommandsResponseCommandsList>;

export interface ListCommandsResponse {
  commands: ListCommandsResponseCommandsList;
}
export const ListCommandsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    commands: ListCommandsResponseCommandsList,
  }),
).annotate({
  identifier: "ListCommandsResponse",
}) as any as S.Schema<ListCommandsResponse>;

export type ListDrivesRequestSortBy = "createdAt" | "updatedAt" | "name";
export const ListDrivesRequestSortBy = /*@__PURE__*/ S.String;

export type ListDrivesRequestSortOrder = "asc" | "desc";
export const ListDrivesRequestSortOrder = /*@__PURE__*/ S.String;

export interface ListDrivesRequest {
  /** The project ID or name associated with the drives. Required unless using a Vercel OIDC token scoped to a project. */
  projectId?: string;
  /** Maximum number of drives to return in the response. Used for pagination. */
  limit?: number;
  /** Opaque pagination cursor from a previous response. */
  cursor?: string;
  /** Field to sort drives by. */
  sortBy?: ListDrivesRequestSortBy | (string & {});
  /** Filter drives whose name starts with this prefix. Only valid when sortBy=name. */
  namePrefix?: string;
  /** Sort direction for results. */
  sortOrder?: ListDrivesRequestSortOrder | (string & {});
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const ListDrivesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    projectId: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    sortBy: S.optional(ListDrivesRequestSortBy.pipe(T.Query())),
    namePrefix: S.optional(S.String.pipe(T.Query())),
    sortOrder: S.optional(ListDrivesRequestSortOrder.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v2/sandboxes/drives", code: 200 })),
).annotate({
  identifier: "ListDrivesRequest",
}) as any as S.Schema<ListDrivesRequest>;

/** This object contains information related to a Vercel Sandbox Drive. */
export type ListDrivesResponseDrivesItem = DeleteADriveResponseDrive;
export const ListDrivesResponseDrivesItem = DeleteADriveResponseDrive;

export type ListDrivesResponseDrivesList = Array<DeleteADriveResponseDrive>;
export const ListDrivesResponseDrivesList = /*@__PURE__*/ S.Array(
  DeleteADriveResponseDrive,
) as any as S.Schema<ListDrivesResponseDrivesList>;

export interface ListDrivesResponsePagination {
  count: number;
  next: string | null;
}
export const ListDrivesResponsePagination = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    count: S.Number,
    next: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ListDrivesResponsePagination",
}) as any as S.Schema<ListDrivesResponsePagination>;

export interface ListDrivesResponse {
  drives: ListDrivesResponseDrivesList;
  pagination: ListDrivesResponsePagination;
}
export const ListDrivesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    drives: ListDrivesResponseDrivesList,
    pagination: ListDrivesResponsePagination,
  }),
).annotate({
  identifier: "ListDrivesResponse",
}) as any as S.Schema<ListDrivesResponse>;

export type ListSandboxesRequestSortBy =
  | "createdAt"
  | "name"
  | "statusUpdatedAt"
  | "currentSnapshotId";
export const ListSandboxesRequestSortBy = /*@__PURE__*/ S.String;

export type ListSandboxesRequestSortOrder = "asc" | "desc";
export const ListSandboxesRequestSortOrder = /*@__PURE__*/ S.String;

export type ListSandboxesRequestStatus = "running" | "stopping" | "stopped";
export const ListSandboxesRequestStatus = /*@__PURE__*/ S.String;

export interface ListSandboxesRequest {
  /** The unique identifier or name of the project to list named sandboxes for. */
  project?: string;
  /** Maximum number of named sandboxes to return in the response. Used for pagination. */
  limit?: number;
  /** Field to sort by. */
  sortBy?: ListSandboxesRequestSortBy | (string & {});
  /** Filter named sandboxes whose name starts with this prefix. Only valid when sortBy=name. */
  namePrefix?: string;
  /** Opaque pagination cursor from a previous response. */
  cursor?: string;
  /** Sort direction. Defaults to desc. */
  sortOrder?: ListSandboxesRequestSortOrder | (string & {});
  /** Filter named sandboxes by status. Only valid when sortBy is createdAt. */
  status?: ListSandboxesRequestStatus | (string & {});
  /** Filter sandboxes by tag. Format: \"key:value\". Only one tag filter is supported at a time. */
  tags?: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const ListSandboxesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    sortBy: S.optional(ListSandboxesRequestSortBy.pipe(T.Query())),
    namePrefix: S.optional(S.String.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    sortOrder: S.optional(ListSandboxesRequestSortOrder.pipe(T.Query())),
    status: S.optional(ListSandboxesRequestStatus.pipe(T.Query())),
    tags: S.optional(S.String.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v2/sandboxes", code: 200 })),
).annotate({
  identifier: "ListSandboxesRequest",
}) as any as S.Schema<ListSandboxesRequest>;

/** The status of the current sandbox. */
export type ListSandboxesResponseSandboxesItemStatus =
  | "running"
  | "stopped"
  | "stopping";
export const ListSandboxesResponseSandboxesItemStatus = /*@__PURE__*/ S.String;

/** This object contains information related to a Vercel NamedSandbox. */
export interface ListSandboxesResponseSandboxesItem {
  /** The unique identifier of the sandbox. */
  name: string;
  /** Current snapshot ID that the named sandbox is pointing to. */
  currentSnapshotId?: string;
  /** Current session ID the sandbox is pointing to. */
  currentSessionId: string;
  /** The status of the current sandbox. */
  status: ListSandboxesResponseSandboxesItemStatus;
  /** The time when the sandbox status was last updated, in milliseconds since the epoch. */
  statusUpdatedAt: number;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. */
  persistent: boolean;
  /** The region the sandbox is configured to run in: the region set on the sandbox, otherwise the project-level default, then the platform default. Where a running session actually landed is reported by `session.region`. */
  region?: string;
  /** Number of virtual CPUs allocated. */
  vcpus?: number;
  /** Memory allocated in MB. */
  memory?: number;
  /** Runtime identifier. */
  runtime?: string;
  /** Digest-pinned reference of the container image the sandbox was created from, when it was created from an image ("{repository}@{manifestDigest}"). */
  image?: string;
  /** Timeout in milliseconds. */
  timeout?: number;
  /** Default snapshot expiration time in milliseconds. 0 means no expiration. */
  snapshotExpiration?: number;
  /** Keep-last snapshot configuration. */
  keepLastSnapshots?: unknown;
  /** Network policy configuration. */
  networkPolicy?: unknown;
  /** Cumulative egress bytes across all sandbox runs. */
  totalEgressBytes?: number;
  /** Cumulative ingress bytes across all sandbox runs. */
  totalIngressBytes?: number;
  /** Cumulative active CPU duration in milliseconds across all sandbox runs. */
  totalActiveCpuDurationMs?: number;
  /** Cumulative wall-clock duration in milliseconds across all sandbox runs. */
  totalDurationMs?: number;
  /** The working directory of the sandbox. */
  cwd?: string;
  /** Key-value tags attached to the named sandbox. */
  tags?: unknown;
  /** Key-value pairs of mount path and drive. */
  mounts?: unknown;
  /** The time when the named sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The time when the named sandbox was last updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The time at which the currently running sandbox will time out, in milliseconds since the epoch. Only present while a session is running. */
  expiresAt?: number;
}
export const ListSandboxesResponseSandboxesItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    currentSnapshotId: S.optional(S.String),
    currentSessionId: S.String,
    status: ListSandboxesResponseSandboxesItemStatus,
    statusUpdatedAt: S.Number,
    persistent: S.Boolean,
    region: S.optional(S.String),
    vcpus: S.optional(S.Number),
    memory: S.optional(S.Number),
    runtime: S.optional(S.String),
    image: S.optional(S.String),
    timeout: S.optional(S.Number),
    snapshotExpiration: S.optional(S.Number),
    keepLastSnapshots: S.optional(S.Unknown),
    networkPolicy: S.optional(S.Unknown),
    totalEgressBytes: S.optional(S.Number),
    totalIngressBytes: S.optional(S.Number),
    totalActiveCpuDurationMs: S.optional(S.Number),
    totalDurationMs: S.optional(S.Number),
    cwd: S.optional(S.String),
    tags: S.optional(S.Unknown),
    mounts: S.optional(S.Unknown),
    createdAt: S.Number,
    updatedAt: S.Number,
    expiresAt: S.optional(S.Number),
  }),
).annotate({
  identifier: "ListSandboxesResponseSandboxesItem",
}) as any as S.Schema<ListSandboxesResponseSandboxesItem>;

export type ListSandboxesResponseSandboxesList =
  Array<ListSandboxesResponseSandboxesItem>;
export const ListSandboxesResponseSandboxesList = /*@__PURE__*/ S.Array(
  ListSandboxesResponseSandboxesItem,
) as any as S.Schema<ListSandboxesResponseSandboxesList>;

export type ListSandboxesResponsePagination = ListDrivesResponsePagination;
export const ListSandboxesResponsePagination = ListDrivesResponsePagination;

export interface ListSandboxesResponse {
  sandboxes: ListSandboxesResponseSandboxesList;
  pagination: ListDrivesResponsePagination;
}
export const ListSandboxesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxes: ListSandboxesResponseSandboxesList,
    pagination: ListDrivesResponsePagination,
  }),
).annotate({
  identifier: "ListSandboxesResponse",
}) as any as S.Schema<ListSandboxesResponse>;

export type ListSessionsRequestSortOrder = "asc" | "desc";
export const ListSessionsRequestSortOrder = /*@__PURE__*/ S.String;

export interface ListSessionsRequest {
  /** The unique identifier or name of the project to list sessions for. */
  project?: string;
  /** Filter sessions by sandbox name. Only sessions belonging to the specified sandbox are returned. */
  name?: string;
  /** Maximum number of sessions to return in the response. Used for pagination. */
  limit?: number;
  /** Opaque pagination cursor from a previous response. */
  cursor?: string;
  /** Sort direction for results by creation time. */
  sortOrder?: ListSessionsRequestSortOrder | (string & {});
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const ListSessionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.optional(S.String.pipe(T.Query())),
    name: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    sortOrder: S.optional(ListSessionsRequestSortOrder.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v2/sandboxes/sessions", code: 200 })),
).annotate({
  identifier: "ListSessionsRequest",
}) as any as S.Schema<ListSessionsRequest>;

export type ListSessionsResponseBodyCase1SessionsList = Array<unknown>;
export const ListSessionsResponseBodyCase1SessionsList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<ListSessionsResponseBodyCase1SessionsList>;

export type ListSessionsResponseBodyCase1Pagination =
  ListDrivesResponsePagination;
export const ListSessionsResponseBodyCase1Pagination =
  ListDrivesResponsePagination;

export interface ListSessionsResponseBodyCase1 {
  sessions: ListSessionsResponseBodyCase1SessionsList;
  pagination: ListDrivesResponsePagination;
}
export const ListSessionsResponseBodyCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessions: ListSessionsResponseBodyCase1SessionsList,
    pagination: ListDrivesResponsePagination,
  }),
).annotate({
  identifier: "ListSessionsResponseBodyCase1",
}) as any as S.Schema<ListSessionsResponseBodyCase1>;

export type ListSessionsResponseBody = unknown | ListSessionsResponseBodyCase1;
export const ListSessionsResponseBody =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ListSessionsResponseBody>;

export type ListSessionsResponse = ListSessionsResponseBody;
export const ListSessionsResponse = /*@__PURE__*/ S.suspend(() =>
  ListSessionsResponseBody.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListSessionsResponse",
}) as any as S.Schema<ListSessionsResponse>;

export type ListSnapshotsRequestSortOrder = "asc" | "desc";
export const ListSnapshotsRequestSortOrder = /*@__PURE__*/ S.String;

export interface ListSnapshotsRequest {
  /** The unique identifier or name of the project to list snapshots for. */
  project?: string;
  /** Name for the sandbox. Must be unique per project and URL-safe (alphanumeric, hyphens, underscores). */
  name?: string;
  /** Maximum number of snapshots to return in the response. Used for pagination. */
  limit?: number;
  /** Opaque pagination cursor from a previous response. */
  cursor?: string;
  /** Sort direction for results by creation time. */
  sortOrder?: ListSnapshotsRequestSortOrder | (string & {});
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const ListSnapshotsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.optional(S.String.pipe(T.Query())),
    name: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    sortOrder: S.optional(ListSnapshotsRequestSortOrder.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v2/sandboxes/snapshots", code: 200 })),
).annotate({
  identifier: "ListSnapshotsRequest",
}) as any as S.Schema<ListSnapshotsRequest>;

export type ListSnapshotsResponseBodyCase1SnapshotsList = Array<unknown>;
export const ListSnapshotsResponseBodyCase1SnapshotsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ListSnapshotsResponseBodyCase1SnapshotsList>;

export type ListSnapshotsResponseBodyCase1Pagination =
  ListDrivesResponsePagination;
export const ListSnapshotsResponseBodyCase1Pagination =
  ListDrivesResponsePagination;

export interface ListSnapshotsResponseBodyCase1 {
  snapshots: ListSnapshotsResponseBodyCase1SnapshotsList;
  pagination: ListDrivesResponsePagination;
}
export const ListSnapshotsResponseBodyCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshots: ListSnapshotsResponseBodyCase1SnapshotsList,
    pagination: ListDrivesResponsePagination,
  }),
).annotate({
  identifier: "ListSnapshotsResponseBodyCase1",
}) as any as S.Schema<ListSnapshotsResponseBodyCase1>;

export type ListSnapshotsResponseBody =
  | unknown
  | ListSnapshotsResponseBodyCase1;
export const ListSnapshotsResponseBody =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ListSnapshotsResponseBody>;

export type ListSnapshotsResponse = ListSnapshotsResponseBody;
export const ListSnapshotsResponse = /*@__PURE__*/ S.suspend(() =>
  ListSnapshotsResponseBody.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ListSnapshotsResponse",
}) as any as S.Schema<ListSnapshotsResponse>;

export interface ReadAFileRequest {
  /** The unique identifier of the session to read the file from. */
  sessionId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  /** The base directory for resolving relative paths. If not specified, paths are resolved from the sandbox home directory. */
  cwd?: string;
  /** The path of the file to read. Can be absolute or relative to the working directory. */
  path: string;
}
export const ReadAFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    cwd: S.optional(S.String),
    path: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/sandboxes/sessions/{sessionId}/fs/read",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ReadAFileRequest",
}) as any as S.Schema<ReadAFileRequest>;

export interface ReadAFileResponse {}
export const ReadAFileResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ReadAFileResponse",
}) as any as S.Schema<ReadAFileResponse>;

export interface StopASessionRequest {
  /** The unique identifier of the session to stop. */
  sessionId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const StopASessionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/sandboxes/sessions/{sessionId}/stop",
      code: 200,
    }),
  ),
).annotate({
  identifier: "StopASessionRequest",
}) as any as S.Schema<StopASessionRequest>;

/** The status of the sandbox. */
export type StopASessionResponseBodyCase0SessionStatus =
  | "aborted"
  | "failed"
  | "pending"
  | "running"
  | "snapshotting"
  | "stopped"
  | "stopping";
export const StopASessionResponseBodyCase0SessionStatus =
  /*@__PURE__*/ S.String;

/** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
export interface StopASessionResponseBodyCase0Session {
  /** The name of the source sandbox. */
  sourceSandboxName: string;
  /** The unique identifier of the project associated with this session. */
  projectId: string;
  /** The unique identifier of the sandbox. */
  id: string;
  /** Memory allocated to this sandbox in MB. */
  memory: number;
  /** Number of vCPUs allocated to this sandbox. */
  vcpus: number;
  /** The region where the sandbox is hosted. */
  region: string;
  /** The runtime of the sandbox. */
  runtime: string;
  /** The maximum amount of time the sandbox will run for in milliseconds. */
  timeout: number;
  /** The status of the sandbox. */
  status: StopASessionResponseBodyCase0SessionStatus;
  /** The time when the sandbox was requested, in milliseconds since the epoch. */
  requestedAt: number;
  /** The time when the sandbox was started, in milliseconds since the epoch. */
  startedAt?: number;
  /** The working directory of the sandbox. */
  cwd: string;
  /** The time when the sandbox was requested to stop, in milliseconds since the epoch. */
  requestedStopAt?: number;
  /** The time when the sandbox was stopped, in milliseconds since the epoch. */
  stoppedAt?: number;
  /** The time when the sandbox was aborted, in milliseconds since the epoch. */
  abortedAt?: number;
  /** The duration of the sandbox in milliseconds. */
  duration?: number;
  /** The unique identifier of the snapshot associated with this sandbox, if any. */
  sourceSnapshotId?: string;
  /** The time when a snapshot was requested, in milliseconds since the epoch. */
  snapshottedAt?: number;
  /** The time when the sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the sandbox was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The network policy applied to this sandbox, if any. */
  networkPolicy?: unknown;
  /** The amount of CPU time the sandbox consumed, if available, in milliseconds. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  activeCpuDurationMs?: number;
  /** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  networkTransfer?: unknown;
}
export const StopASessionResponseBodyCase0Session = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      sourceSandboxName: S.String,
      projectId: S.String,
      id: S.String,
      memory: S.Number,
      vcpus: S.Number,
      region: S.String,
      runtime: S.String,
      timeout: S.Number,
      status: StopASessionResponseBodyCase0SessionStatus,
      requestedAt: S.Number,
      startedAt: S.optional(S.Number),
      cwd: S.String,
      requestedStopAt: S.optional(S.Number),
      stoppedAt: S.optional(S.Number),
      abortedAt: S.optional(S.Number),
      duration: S.optional(S.Number),
      sourceSnapshotId: S.optional(S.String),
      snapshottedAt: S.optional(S.Number),
      createdAt: S.Number,
      updatedAt: S.Number,
      networkPolicy: S.optional(S.Unknown),
      activeCpuDurationMs: S.optional(S.Number),
      networkTransfer: S.optional(S.Unknown),
    }),
).annotate({
  identifier: "StopASessionResponseBodyCase0Session",
}) as any as S.Schema<StopASessionResponseBodyCase0Session>;

export interface StopASessionResponseBodyCase0 {
  /** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
  session: StopASessionResponseBodyCase0Session;
}
export const StopASessionResponseBodyCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    session: StopASessionResponseBodyCase0Session,
  }),
).annotate({
  identifier: "StopASessionResponseBodyCase0",
}) as any as S.Schema<StopASessionResponseBodyCase0>;

/** The status of the snapshot. */
export type StopASessionResponseBodyCase1SnapshotStatus =
  | "created"
  | "deleted"
  | "failed";
export const StopASessionResponseBodyCase1SnapshotStatus =
  /*@__PURE__*/ S.String;

/** The method used to create the snapshot. */
export type StopASessionResponseBodyCase1SnapshotCreationMethod =
  | "automatic"
  | "manual";
export const StopASessionResponseBodyCase1SnapshotCreationMethod =
  /*@__PURE__*/ S.String;

/** This object contains information related to a Snapshot of a Vercel Sandbox session (v2 API). */
export interface StopASessionResponseBodyCase1Snapshot {
  /** The unique identifier of the snapshot. */
  id: string;
  /** The unique identifier of the session from which the snapshot was created. */
  sourceSessionId: string;
  /** The region where the snapshot is stored. */
  region?: string;
  /** The status of the snapshot. */
  status: StopASessionResponseBodyCase1SnapshotStatus;
  /** The size of the snapshot in bytes. */
  sizeBytes: number;
  /** The time when the snapshot will expire, in milliseconds since the epoch. If not set, the snapshot does not have any expiration. */
  expiresAt?: number;
  /** The time when the snapshot was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the snapshot was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The last time the snapshot was used (e.g. to resume or create a sandbox), in milliseconds since the epoch. Falls back to `createdAt` for older snapshots that predate this field. */
  lastUsedAt: number;
  /** The method used to create the snapshot. */
  creationMethod?: StopASessionResponseBodyCase1SnapshotCreationMethod;
  /** The unique identifier of the parent snapshot, if this snapshot was created from another snapshot. */
  parentId?: string;
}
export const StopASessionResponseBodyCase1Snapshot = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      id: S.String,
      sourceSessionId: S.String,
      region: S.optional(S.String),
      status: StopASessionResponseBodyCase1SnapshotStatus,
      sizeBytes: S.Number,
      expiresAt: S.optional(S.Number),
      createdAt: S.Number,
      updatedAt: S.Number,
      lastUsedAt: S.Number,
      creationMethod: S.optional(
        StopASessionResponseBodyCase1SnapshotCreationMethod,
      ),
      parentId: S.optional(S.String),
    }),
).annotate({
  identifier: "StopASessionResponseBodyCase1Snapshot",
}) as any as S.Schema<StopASessionResponseBodyCase1Snapshot>;

/** The status of the current sandbox. */
export type StopASessionResponseBodyCase1SandboxStatus =
  | "running"
  | "stopped"
  | "stopping";
export const StopASessionResponseBodyCase1SandboxStatus =
  /*@__PURE__*/ S.String;

/** This object contains information related to a Vercel NamedSandbox. */
export interface StopASessionResponseBodyCase1Sandbox {
  /** The unique identifier of the sandbox. */
  name: string;
  /** Current snapshot ID that the named sandbox is pointing to. */
  currentSnapshotId?: string;
  /** Current session ID the sandbox is pointing to. */
  currentSessionId: string;
  /** The status of the current sandbox. */
  status: StopASessionResponseBodyCase1SandboxStatus;
  /** The time when the sandbox status was last updated, in milliseconds since the epoch. */
  statusUpdatedAt: number;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. */
  persistent: boolean;
  /** The region the sandbox is configured to run in: the region set on the sandbox, otherwise the project-level default, then the platform default. Where a running session actually landed is reported by `session.region`. */
  region?: string;
  /** Number of virtual CPUs allocated. */
  vcpus?: number;
  /** Memory allocated in MB. */
  memory?: number;
  /** Runtime identifier. */
  runtime?: string;
  /** Digest-pinned reference of the container image the sandbox was created from, when it was created from an image ("{repository}@{manifestDigest}"). */
  image?: string;
  /** Timeout in milliseconds. */
  timeout?: number;
  /** Default snapshot expiration time in milliseconds. 0 means no expiration. */
  snapshotExpiration?: number;
  /** Keep-last snapshot configuration. */
  keepLastSnapshots?: unknown;
  /** Network policy configuration. */
  networkPolicy?: unknown;
  /** Cumulative egress bytes across all sandbox runs. */
  totalEgressBytes?: number;
  /** Cumulative ingress bytes across all sandbox runs. */
  totalIngressBytes?: number;
  /** Cumulative active CPU duration in milliseconds across all sandbox runs. */
  totalActiveCpuDurationMs?: number;
  /** Cumulative wall-clock duration in milliseconds across all sandbox runs. */
  totalDurationMs?: number;
  /** The working directory of the sandbox. */
  cwd?: string;
  /** Key-value tags attached to the named sandbox. */
  tags?: unknown;
  /** Key-value pairs of mount path and drive. */
  mounts?: unknown;
  /** The time when the named sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The time when the named sandbox was last updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The time at which the currently running sandbox will time out, in milliseconds since the epoch. Only present while a session is running. */
  expiresAt?: number;
}
export const StopASessionResponseBodyCase1Sandbox = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      name: S.String,
      currentSnapshotId: S.optional(S.String),
      currentSessionId: S.String,
      status: StopASessionResponseBodyCase1SandboxStatus,
      statusUpdatedAt: S.Number,
      persistent: S.Boolean,
      region: S.optional(S.String),
      vcpus: S.optional(S.Number),
      memory: S.optional(S.Number),
      runtime: S.optional(S.String),
      image: S.optional(S.String),
      timeout: S.optional(S.Number),
      snapshotExpiration: S.optional(S.Number),
      keepLastSnapshots: S.optional(S.Unknown),
      networkPolicy: S.optional(S.Unknown),
      totalEgressBytes: S.optional(S.Number),
      totalIngressBytes: S.optional(S.Number),
      totalActiveCpuDurationMs: S.optional(S.Number),
      totalDurationMs: S.optional(S.Number),
      cwd: S.optional(S.String),
      tags: S.optional(S.Unknown),
      mounts: S.optional(S.Unknown),
      createdAt: S.Number,
      updatedAt: S.Number,
      expiresAt: S.optional(S.Number),
    }),
).annotate({
  identifier: "StopASessionResponseBodyCase1Sandbox",
}) as any as S.Schema<StopASessionResponseBodyCase1Sandbox>;

/** The status of the sandbox. */
export type StopASessionResponseBodyCase1SessionStatus =
  | "aborted"
  | "failed"
  | "pending"
  | "running"
  | "snapshotting"
  | "stopped"
  | "stopping";
export const StopASessionResponseBodyCase1SessionStatus =
  /*@__PURE__*/ S.String;

/** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
export interface StopASessionResponseBodyCase1Session {
  /** The name of the source sandbox. */
  sourceSandboxName: string;
  /** The unique identifier of the project associated with this session. */
  projectId: string;
  /** The unique identifier of the sandbox. */
  id: string;
  /** Memory allocated to this sandbox in MB. */
  memory: number;
  /** Number of vCPUs allocated to this sandbox. */
  vcpus: number;
  /** The region where the sandbox is hosted. */
  region: string;
  /** The runtime of the sandbox. */
  runtime: string;
  /** The maximum amount of time the sandbox will run for in milliseconds. */
  timeout: number;
  /** The status of the sandbox. */
  status: StopASessionResponseBodyCase1SessionStatus;
  /** The time when the sandbox was requested, in milliseconds since the epoch. */
  requestedAt: number;
  /** The time when the sandbox was started, in milliseconds since the epoch. */
  startedAt?: number;
  /** The working directory of the sandbox. */
  cwd: string;
  /** The time when the sandbox was requested to stop, in milliseconds since the epoch. */
  requestedStopAt?: number;
  /** The time when the sandbox was stopped, in milliseconds since the epoch. */
  stoppedAt?: number;
  /** The time when the sandbox was aborted, in milliseconds since the epoch. */
  abortedAt?: number;
  /** The duration of the sandbox in milliseconds. */
  duration?: number;
  /** The unique identifier of the snapshot associated with this sandbox, if any. */
  sourceSnapshotId?: string;
  /** The time when a snapshot was requested, in milliseconds since the epoch. */
  snapshottedAt?: number;
  /** The time when the sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the sandbox was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The network policy applied to this sandbox, if any. */
  networkPolicy?: unknown;
  /** The amount of CPU time the sandbox consumed, if available, in milliseconds. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  activeCpuDurationMs?: number;
  /** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  networkTransfer?: unknown;
}
export const StopASessionResponseBodyCase1Session = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      sourceSandboxName: S.String,
      projectId: S.String,
      id: S.String,
      memory: S.Number,
      vcpus: S.Number,
      region: S.String,
      runtime: S.String,
      timeout: S.Number,
      status: StopASessionResponseBodyCase1SessionStatus,
      requestedAt: S.Number,
      startedAt: S.optional(S.Number),
      cwd: S.String,
      requestedStopAt: S.optional(S.Number),
      stoppedAt: S.optional(S.Number),
      abortedAt: S.optional(S.Number),
      duration: S.optional(S.Number),
      sourceSnapshotId: S.optional(S.String),
      snapshottedAt: S.optional(S.Number),
      createdAt: S.Number,
      updatedAt: S.Number,
      networkPolicy: S.optional(S.Unknown),
      activeCpuDurationMs: S.optional(S.Number),
      networkTransfer: S.optional(S.Unknown),
    }),
).annotate({
  identifier: "StopASessionResponseBodyCase1Session",
}) as any as S.Schema<StopASessionResponseBodyCase1Session>;

export interface StopASessionResponseBodyCase1 {
  /** This object contains information related to a Snapshot of a Vercel Sandbox session (v2 API). */
  snapshot: StopASessionResponseBodyCase1Snapshot;
  /** This object contains information related to a Vercel NamedSandbox. */
  sandbox: StopASessionResponseBodyCase1Sandbox;
  /** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
  session: StopASessionResponseBodyCase1Session;
}
export const StopASessionResponseBodyCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshot: StopASessionResponseBodyCase1Snapshot,
    sandbox: StopASessionResponseBodyCase1Sandbox,
    session: StopASessionResponseBodyCase1Session,
  }),
).annotate({
  identifier: "StopASessionResponseBodyCase1",
}) as any as S.Schema<StopASessionResponseBodyCase1>;

export type StopASessionResponseBody =
  | StopASessionResponseBodyCase0
  | StopASessionResponseBodyCase1;
export const StopASessionResponseBody =
  /*@__PURE__*/ S.Unknown as any as S.Schema<StopASessionResponseBody>;

export type StopASessionResponse = StopASessionResponseBody;
export const StopASessionResponse = /*@__PURE__*/ S.suspend(() =>
  StopASessionResponseBody.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "StopASessionResponse",
}) as any as S.Schema<StopASessionResponse>;

export interface StreamCommandLogsRequest {
  /** The unique identifier of the session containing the command. */
  sessionId: string;
  /** The unique identifier of the command to stream logs for. */
  cmdId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const StreamCommandLogsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    cmdId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/sandboxes/sessions/{sessionId}/cmd/{cmdId}/logs",
      code: 200,
    }),
  ),
).annotate({
  identifier: "StreamCommandLogsRequest",
}) as any as S.Schema<StreamCommandLogsRequest>;

export interface StreamCommandLogsResponse {}
export const StreamCommandLogsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "StreamCommandLogsResponse",
}) as any as S.Schema<StreamCommandLogsResponse>;

/** Resources to define the VM */
export type UpdateASandboxRequestResources =
  CreateANamedSandboxRequestResources;
export const UpdateASandboxRequestResources =
  CreateANamedSandboxRequestResources;

/** The runtime environment for the sandbox. Determines the pre-installed language runtimes and tools available. */
export type UpdateASandboxRequestRuntime =
  | "node22"
  | "node24"
  | "node26"
  | "python3.13";
export const UpdateASandboxRequestRuntime = /*@__PURE__*/ S.String;

/** Default snapshot expiration time in milliseconds. Set to 0 to disable expiration. When set, this value is used as the default expiration for all snapshots created for this sandbox. */
export type UpdateASandboxRequestSnapshotExpiration = unknown | number;
export const UpdateASandboxRequestSnapshotExpiration =
  /*@__PURE__*/ S.Unknown as any as S.Schema<UpdateASandboxRequestSnapshotExpiration>;

export interface UpdateASandboxRequestKeepLastSnapshotsCase1 {
  /** Number of most recent snapshots to keep. */
  count: number;
  /** Expiration time in milliseconds for kept snapshots. Falls back to snapshotExpiration. */
  expiration?: unknown;
  /** Whether to immediately delete evicted snapshots. Defaults to true. */
  deleteEvicted?: boolean;
}
export const UpdateASandboxRequestKeepLastSnapshotsCase1 =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      count: S.Number,
      expiration: S.optional(S.Unknown),
      deleteEvicted: S.optional(S.Boolean),
    }),
  ).annotate({
    identifier: "UpdateASandboxRequestKeepLastSnapshotsCase1",
  }) as any as S.Schema<UpdateASandboxRequestKeepLastSnapshotsCase1>;

/** Protect the N most recent snapshots with different expiration/deletion behavior. Set to null to clear. */
export type UpdateASandboxRequestKeepLastSnapshots =
  | string
  | UpdateASandboxRequestKeepLastSnapshotsCase1;
export const UpdateASandboxRequestKeepLastSnapshots =
  /*@__PURE__*/ S.Unknown as any as S.Schema<UpdateASandboxRequestKeepLastSnapshots>;

/** The network access policy mode. Use \"allow-all\" to permit all outbound traffic. Use \"deny-all\" to block all outbound traffic. Use \"custom\" to specify explicit allow/deny rules. */
export type UpdateASandboxRequestNetworkPolicyCase0Mode =
  | "allow-all"
  | "deny-all"
  | "custom"
  | "default-allow"
  | "default-deny";
export const UpdateASandboxRequestNetworkPolicyCase0Mode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Only applies when mode is \"custom\". Supports wildcard patterns (e.g., \"*.example.com\" matches all subdomains). */
export type UpdateASandboxRequestNetworkPolicyCase0AllowedDomainsList =
  Array<unknown>;
export const UpdateASandboxRequestNetworkPolicyCase0AllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateASandboxRequestNetworkPolicyCase0AllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. Traffic to these addresses bypasses domain-based restrictions. */
export type UpdateASandboxRequestNetworkPolicyCase0AllowedCIDRsList =
  Array<unknown>;
export const UpdateASandboxRequestNetworkPolicyCase0AllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateASandboxRequestNetworkPolicyCase0AllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type UpdateASandboxRequestNetworkPolicyCase0DeniedCIDRsList =
  Array<unknown>;
export const UpdateASandboxRequestNetworkPolicyCase0DeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateASandboxRequestNetworkPolicyCase0DeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. Traffic to matching domains will be intercepted instead of proxied through encrypted connections. */
export type UpdateASandboxRequestNetworkPolicyCase0InjectionRulesList =
  Array<unknown>;
export const UpdateASandboxRequestNetworkPolicyCase0InjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateASandboxRequestNetworkPolicyCase0InjectionRulesList>;

/** Network access policy for the sandbox.\n Controls which external hosts the sandbox can communicate with.\n Use \"allow-all\" mode to allow all traffic, \"deny-all\" to block all traffic or \"custom\" to provide specific rules. */
export interface UpdateASandboxRequestNetworkPolicyCase0 {
  /** The network access policy mode. Use \"allow-all\" to permit all outbound traffic. Use \"deny-all\" to block all outbound traffic. Use \"custom\" to specify explicit allow/deny rules. */
  mode: UpdateASandboxRequestNetworkPolicyCase0Mode | (string & {});
  /** List of domain names the sandbox is allowed to connect to. Only applies when mode is \"custom\". Supports wildcard patterns (e.g., \"*.example.com\" matches all subdomains). */
  allowedDomains?: UpdateASandboxRequestNetworkPolicyCase0AllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. Traffic to these addresses bypasses domain-based restrictions. */
  allowedCIDRs?: UpdateASandboxRequestNetworkPolicyCase0AllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: UpdateASandboxRequestNetworkPolicyCase0DeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. Traffic to matching domains will be intercepted instead of proxied through encrypted connections. */
  injectionRules?: UpdateASandboxRequestNetworkPolicyCase0InjectionRulesList;
}
export const UpdateASandboxRequestNetworkPolicyCase0 = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      mode: UpdateASandboxRequestNetworkPolicyCase0Mode,
      allowedDomains: S.optional(
        UpdateASandboxRequestNetworkPolicyCase0AllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        UpdateASandboxRequestNetworkPolicyCase0AllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        UpdateASandboxRequestNetworkPolicyCase0DeniedCIDRsList,
      ),
      injectionRules: S.optional(
        UpdateASandboxRequestNetworkPolicyCase0InjectionRulesList,
      ),
    }),
).annotate({
  identifier: "UpdateASandboxRequestNetworkPolicyCase0",
}) as any as S.Schema<UpdateASandboxRequestNetworkPolicyCase0>;

export type UpdateASandboxRequestNetworkPolicyCase1 =
  CreateANamedSandboxRequestNetworkPolicyCase1;
export const UpdateASandboxRequestNetworkPolicyCase1 =
  CreateANamedSandboxRequestNetworkPolicyCase1;

export type UpdateASandboxRequestNetworkPolicy =
  | UpdateASandboxRequestNetworkPolicyCase0
  | CreateANamedSandboxRequestNetworkPolicyCase1;
export const UpdateASandboxRequestNetworkPolicy =
  /*@__PURE__*/ S.Unknown as any as S.Schema<UpdateASandboxRequestNetworkPolicy>;

/** Default environment variables for the sandbox. Set to empty object to clear. */
export type UpdateASandboxRequestEnvMap = { [key: string]: string | undefined };
export const UpdateASandboxRequestEnvMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<UpdateASandboxRequestEnvMap>;

/** List of ports to expose from the sandbox. Each port will be accessible via a unique URL. Maximum of 15 ports can be exposed. */
export type UpdateASandboxRequestPortsList = Array<number>;
export const UpdateASandboxRequestPortsList = /*@__PURE__*/ S.Array(
  S.Number,
) as any as S.Schema<UpdateASandboxRequestPortsList>;

/** Key-value tags to associate with the sandbox. Replaces existing tags. Set to empty object to clear. Maximum 5 tags. */
export type UpdateASandboxRequestTagsMap = {
  [key: string]: string | undefined;
};
export const UpdateASandboxRequestTagsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<UpdateASandboxRequestTagsMap>;

export interface UpdateASandboxRequest {
  /** The sandbox to update. */
  name: string;
  /** The project ID that owns the named sandbox. When provided, takes precedence over OIDC project context. */
  projectId?: string;
  /** Whether to automatically resume a stopped named sandbox by creating a new instance from its snapshot. Defaults to false. */
  resume?: boolean;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  /** Resources to define the VM */
  resources?: CreateANamedSandboxRequestResources;
  /** The runtime environment for the sandbox. Determines the pre-installed language runtimes and tools available. */
  runtime?: UpdateASandboxRequestRuntime | (string & {});
  /** Maximum duration in milliseconds that the sandbox can run before being automatically stopped. */
  timeout?: number;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. */
  persistent?: boolean;
  /** Default snapshot expiration time in milliseconds. Set to 0 to disable expiration. When set, this value is used as the default expiration for all snapshots created for this sandbox. */
  snapshotExpiration?: UpdateASandboxRequestSnapshotExpiration;
  /** Protect the N most recent snapshots with different expiration/deletion behavior. Set to null to clear. */
  keepLastSnapshots?: UpdateASandboxRequestKeepLastSnapshots;
  networkPolicy?: UpdateASandboxRequestNetworkPolicy;
  /** Default environment variables for the sandbox. Set to empty object to clear. */
  env?: UpdateASandboxRequestEnvMap;
  /** List of ports to expose from the sandbox. Each port will be accessible via a unique URL. Maximum of 15 ports can be exposed. */
  ports?: UpdateASandboxRequestPortsList;
  /** The snapshot ID to set as the current snapshot. Must be active and belong to the same project. */
  currentSnapshotId?: string;
  /** Key-value tags to associate with the sandbox. Replaces existing tags. Set to empty object to clear. Maximum 5 tags. */
  tags?: UpdateASandboxRequestTagsMap;
}
export const UpdateASandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    projectId: S.optional(S.String.pipe(T.Query())),
    resume: S.optional(S.Boolean.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    resources: S.optional(CreateANamedSandboxRequestResources),
    runtime: S.optional(UpdateASandboxRequestRuntime),
    timeout: S.optional(S.Number),
    persistent: S.optional(S.Boolean),
    snapshotExpiration: S.optional(UpdateASandboxRequestSnapshotExpiration),
    keepLastSnapshots: S.optional(UpdateASandboxRequestKeepLastSnapshots),
    networkPolicy: S.optional(UpdateASandboxRequestNetworkPolicy),
    env: S.optional(UpdateASandboxRequestEnvMap),
    ports: S.optional(UpdateASandboxRequestPortsList),
    currentSnapshotId: S.optional(S.String),
    tags: S.optional(UpdateASandboxRequestTagsMap),
  }).pipe(T.Http({ method: "PATCH", uri: "/v2/sandboxes/{name}", code: 200 })),
).annotate({
  identifier: "UpdateASandboxRequest",
}) as any as S.Schema<UpdateASandboxRequest>;

export type UpdateASandboxResponseBodyCase0RoutesList = Array<unknown>;
export const UpdateASandboxResponseBodyCase0RoutesList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<UpdateASandboxResponseBodyCase0RoutesList>;

/** The status of the current sandbox. */
export type UpdateASandboxResponseBodyCase0SandboxStatus =
  | "running"
  | "stopped"
  | "stopping";
export const UpdateASandboxResponseBodyCase0SandboxStatus =
  /*@__PURE__*/ S.String;

/** This object contains information related to a Vercel NamedSandbox. */
export interface UpdateASandboxResponseBodyCase0Sandbox {
  /** The unique identifier of the sandbox. */
  name: string;
  /** Current snapshot ID that the named sandbox is pointing to. */
  currentSnapshotId?: string;
  /** Current session ID the sandbox is pointing to. */
  currentSessionId: string;
  /** The status of the current sandbox. */
  status: UpdateASandboxResponseBodyCase0SandboxStatus;
  /** The time when the sandbox status was last updated, in milliseconds since the epoch. */
  statusUpdatedAt: number;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. */
  persistent: boolean;
  /** The region the sandbox is configured to run in: the region set on the sandbox, otherwise the project-level default, then the platform default. Where a running session actually landed is reported by `session.region`. */
  region?: string;
  /** Number of virtual CPUs allocated. */
  vcpus?: number;
  /** Memory allocated in MB. */
  memory?: number;
  /** Runtime identifier. */
  runtime?: string;
  /** Digest-pinned reference of the container image the sandbox was created from, when it was created from an image ("{repository}@{manifestDigest}"). */
  image?: string;
  /** Timeout in milliseconds. */
  timeout?: number;
  /** Default snapshot expiration time in milliseconds. 0 means no expiration. */
  snapshotExpiration?: number;
  /** Keep-last snapshot configuration. */
  keepLastSnapshots?: unknown;
  /** Network policy configuration. */
  networkPolicy?: unknown;
  /** Cumulative egress bytes across all sandbox runs. */
  totalEgressBytes?: number;
  /** Cumulative ingress bytes across all sandbox runs. */
  totalIngressBytes?: number;
  /** Cumulative active CPU duration in milliseconds across all sandbox runs. */
  totalActiveCpuDurationMs?: number;
  /** Cumulative wall-clock duration in milliseconds across all sandbox runs. */
  totalDurationMs?: number;
  /** The working directory of the sandbox. */
  cwd?: string;
  /** Key-value tags attached to the named sandbox. */
  tags?: unknown;
  /** Key-value pairs of mount path and drive. */
  mounts?: unknown;
  /** The time when the named sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The time when the named sandbox was last updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The time at which the currently running sandbox will time out, in milliseconds since the epoch. Only present while a session is running. */
  expiresAt?: number;
}
export const UpdateASandboxResponseBodyCase0Sandbox = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      name: S.String,
      currentSnapshotId: S.optional(S.String),
      currentSessionId: S.String,
      status: UpdateASandboxResponseBodyCase0SandboxStatus,
      statusUpdatedAt: S.Number,
      persistent: S.Boolean,
      region: S.optional(S.String),
      vcpus: S.optional(S.Number),
      memory: S.optional(S.Number),
      runtime: S.optional(S.String),
      image: S.optional(S.String),
      timeout: S.optional(S.Number),
      snapshotExpiration: S.optional(S.Number),
      keepLastSnapshots: S.optional(S.Unknown),
      networkPolicy: S.optional(S.Unknown),
      totalEgressBytes: S.optional(S.Number),
      totalIngressBytes: S.optional(S.Number),
      totalActiveCpuDurationMs: S.optional(S.Number),
      totalDurationMs: S.optional(S.Number),
      cwd: S.optional(S.String),
      tags: S.optional(S.Unknown),
      mounts: S.optional(S.Unknown),
      createdAt: S.Number,
      updatedAt: S.Number,
      expiresAt: S.optional(S.Number),
    }),
).annotate({
  identifier: "UpdateASandboxResponseBodyCase0Sandbox",
}) as any as S.Schema<UpdateASandboxResponseBodyCase0Sandbox>;

export interface UpdateASandboxResponseBodyCase0 {
  routes: UpdateASandboxResponseBodyCase0RoutesList;
  /** This object contains information related to a Vercel NamedSandbox. */
  sandbox: UpdateASandboxResponseBodyCase0Sandbox;
}
export const UpdateASandboxResponseBodyCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    routes: UpdateASandboxResponseBodyCase0RoutesList,
    sandbox: UpdateASandboxResponseBodyCase0Sandbox,
  }),
).annotate({
  identifier: "UpdateASandboxResponseBodyCase0",
}) as any as S.Schema<UpdateASandboxResponseBodyCase0>;

/** The status of the current sandbox. */
export type UpdateASandboxResponseBodyCase1SandboxStatus =
  | "running"
  | "stopped"
  | "stopping";
export const UpdateASandboxResponseBodyCase1SandboxStatus =
  /*@__PURE__*/ S.String;

/** This object contains information related to a Vercel NamedSandbox. */
export interface UpdateASandboxResponseBodyCase1Sandbox {
  /** The unique identifier of the sandbox. */
  name: string;
  /** Current snapshot ID that the named sandbox is pointing to. */
  currentSnapshotId?: string;
  /** Current session ID the sandbox is pointing to. */
  currentSessionId: string;
  /** The status of the current sandbox. */
  status: UpdateASandboxResponseBodyCase1SandboxStatus;
  /** The time when the sandbox status was last updated, in milliseconds since the epoch. */
  statusUpdatedAt: number;
  /** Whether the sandbox persists its state across restarts via automatic snapshots. */
  persistent: boolean;
  /** The region the sandbox is configured to run in: the region set on the sandbox, otherwise the project-level default, then the platform default. Where a running session actually landed is reported by `session.region`. */
  region?: string;
  /** Number of virtual CPUs allocated. */
  vcpus?: number;
  /** Memory allocated in MB. */
  memory?: number;
  /** Runtime identifier. */
  runtime?: string;
  /** Digest-pinned reference of the container image the sandbox was created from, when it was created from an image ("{repository}@{manifestDigest}"). */
  image?: string;
  /** Timeout in milliseconds. */
  timeout?: number;
  /** Default snapshot expiration time in milliseconds. 0 means no expiration. */
  snapshotExpiration?: number;
  /** Keep-last snapshot configuration. */
  keepLastSnapshots?: unknown;
  /** Network policy configuration. */
  networkPolicy?: unknown;
  /** Cumulative egress bytes across all sandbox runs. */
  totalEgressBytes?: number;
  /** Cumulative ingress bytes across all sandbox runs. */
  totalIngressBytes?: number;
  /** Cumulative active CPU duration in milliseconds across all sandbox runs. */
  totalActiveCpuDurationMs?: number;
  /** Cumulative wall-clock duration in milliseconds across all sandbox runs. */
  totalDurationMs?: number;
  /** The working directory of the sandbox. */
  cwd?: string;
  /** Key-value tags attached to the named sandbox. */
  tags?: unknown;
  /** Key-value pairs of mount path and drive. */
  mounts?: unknown;
  /** The time when the named sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The time when the named sandbox was last updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The time at which the currently running sandbox will time out, in milliseconds since the epoch. Only present while a session is running. */
  expiresAt?: number;
}
export const UpdateASandboxResponseBodyCase1Sandbox = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      name: S.String,
      currentSnapshotId: S.optional(S.String),
      currentSessionId: S.String,
      status: UpdateASandboxResponseBodyCase1SandboxStatus,
      statusUpdatedAt: S.Number,
      persistent: S.Boolean,
      region: S.optional(S.String),
      vcpus: S.optional(S.Number),
      memory: S.optional(S.Number),
      runtime: S.optional(S.String),
      image: S.optional(S.String),
      timeout: S.optional(S.Number),
      snapshotExpiration: S.optional(S.Number),
      keepLastSnapshots: S.optional(S.Unknown),
      networkPolicy: S.optional(S.Unknown),
      totalEgressBytes: S.optional(S.Number),
      totalIngressBytes: S.optional(S.Number),
      totalActiveCpuDurationMs: S.optional(S.Number),
      totalDurationMs: S.optional(S.Number),
      cwd: S.optional(S.String),
      tags: S.optional(S.Unknown),
      mounts: S.optional(S.Unknown),
      createdAt: S.Number,
      updatedAt: S.Number,
      expiresAt: S.optional(S.Number),
    }),
).annotate({
  identifier: "UpdateASandboxResponseBodyCase1Sandbox",
}) as any as S.Schema<UpdateASandboxResponseBodyCase1Sandbox>;

/** The status of the sandbox. */
export type UpdateASandboxResponseBodyCase1SessionStatus =
  | "aborted"
  | "failed"
  | "pending"
  | "running"
  | "snapshotting"
  | "stopped"
  | "stopping";
export const UpdateASandboxResponseBodyCase1SessionStatus =
  /*@__PURE__*/ S.String;

/** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
export interface UpdateASandboxResponseBodyCase1Session {
  /** The name of the source sandbox. */
  sourceSandboxName: string;
  /** The unique identifier of the project associated with this session. */
  projectId: string;
  /** The unique identifier of the sandbox. */
  id: string;
  /** Memory allocated to this sandbox in MB. */
  memory: number;
  /** Number of vCPUs allocated to this sandbox. */
  vcpus: number;
  /** The region where the sandbox is hosted. */
  region: string;
  /** The runtime of the sandbox. */
  runtime: string;
  /** The maximum amount of time the sandbox will run for in milliseconds. */
  timeout: number;
  /** The status of the sandbox. */
  status: UpdateASandboxResponseBodyCase1SessionStatus;
  /** The time when the sandbox was requested, in milliseconds since the epoch. */
  requestedAt: number;
  /** The time when the sandbox was started, in milliseconds since the epoch. */
  startedAt?: number;
  /** The working directory of the sandbox. */
  cwd: string;
  /** The time when the sandbox was requested to stop, in milliseconds since the epoch. */
  requestedStopAt?: number;
  /** The time when the sandbox was stopped, in milliseconds since the epoch. */
  stoppedAt?: number;
  /** The time when the sandbox was aborted, in milliseconds since the epoch. */
  abortedAt?: number;
  /** The duration of the sandbox in milliseconds. */
  duration?: number;
  /** The unique identifier of the snapshot associated with this sandbox, if any. */
  sourceSnapshotId?: string;
  /** The time when a snapshot was requested, in milliseconds since the epoch. */
  snapshottedAt?: number;
  /** The time when the sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the sandbox was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The network policy applied to this sandbox, if any. */
  networkPolicy?: unknown;
  /** The amount of CPU time the sandbox consumed, if available, in milliseconds. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  activeCpuDurationMs?: number;
  /** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  networkTransfer?: unknown;
}
export const UpdateASandboxResponseBodyCase1Session = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      sourceSandboxName: S.String,
      projectId: S.String,
      id: S.String,
      memory: S.Number,
      vcpus: S.Number,
      region: S.String,
      runtime: S.String,
      timeout: S.Number,
      status: UpdateASandboxResponseBodyCase1SessionStatus,
      requestedAt: S.Number,
      startedAt: S.optional(S.Number),
      cwd: S.String,
      requestedStopAt: S.optional(S.Number),
      stoppedAt: S.optional(S.Number),
      abortedAt: S.optional(S.Number),
      duration: S.optional(S.Number),
      sourceSnapshotId: S.optional(S.String),
      snapshottedAt: S.optional(S.Number),
      createdAt: S.Number,
      updatedAt: S.Number,
      networkPolicy: S.optional(S.Unknown),
      activeCpuDurationMs: S.optional(S.Number),
      networkTransfer: S.optional(S.Unknown),
    }),
).annotate({
  identifier: "UpdateASandboxResponseBodyCase1Session",
}) as any as S.Schema<UpdateASandboxResponseBodyCase1Session>;

export type UpdateASandboxResponseBodyCase1RoutesList = Array<unknown>;
export const UpdateASandboxResponseBodyCase1RoutesList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<UpdateASandboxResponseBodyCase1RoutesList>;

export interface UpdateASandboxResponseBodyCase1 {
  /** This object contains information related to a Vercel NamedSandbox. */
  sandbox: UpdateASandboxResponseBodyCase1Sandbox;
  /** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
  session: UpdateASandboxResponseBodyCase1Session;
  routes: UpdateASandboxResponseBodyCase1RoutesList;
  resumed: boolean;
}
export const UpdateASandboxResponseBodyCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandbox: UpdateASandboxResponseBodyCase1Sandbox,
    session: UpdateASandboxResponseBodyCase1Session,
    routes: UpdateASandboxResponseBodyCase1RoutesList,
    resumed: S.Boolean,
  }),
).annotate({
  identifier: "UpdateASandboxResponseBodyCase1",
}) as any as S.Schema<UpdateASandboxResponseBodyCase1>;

export type UpdateASandboxResponseBody =
  | UpdateASandboxResponseBodyCase0
  | UpdateASandboxResponseBodyCase1;
export const UpdateASandboxResponseBody =
  /*@__PURE__*/ S.Unknown as any as S.Schema<UpdateASandboxResponseBody>;

export type UpdateASandboxResponse = UpdateASandboxResponseBody;
export const UpdateASandboxResponse = /*@__PURE__*/ S.suspend(() =>
  UpdateASandboxResponseBody.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "UpdateASandboxResponse",
}) as any as S.Schema<UpdateASandboxResponse>;

/** The network access policy mode. Use \"allow-all\" to permit all outbound traffic. Use \"deny-all\" to block all outbound traffic. Use \"custom\" to specify explicit allow/deny rules. */
export type UpdateNetworkPolicyRequestBodyCase0Mode =
  | "allow-all"
  | "deny-all"
  | "custom"
  | "default-allow"
  | "default-deny";
export const UpdateNetworkPolicyRequestBodyCase0Mode = /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Only applies when mode is \"custom\". Supports wildcard patterns (e.g., \"*.example.com\" matches all subdomains). */
export type UpdateNetworkPolicyRequestBodyCase0AllowedDomainsList =
  Array<string>;
export const UpdateNetworkPolicyRequestBodyCase0AllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateNetworkPolicyRequestBodyCase0AllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. Traffic to these addresses bypasses domain-based restrictions. */
export type UpdateNetworkPolicyRequestBodyCase0AllowedCIDRsList = Array<string>;
export const UpdateNetworkPolicyRequestBodyCase0AllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateNetworkPolicyRequestBodyCase0AllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type UpdateNetworkPolicyRequestBodyCase0DeniedCIDRsList = Array<string>;
export const UpdateNetworkPolicyRequestBodyCase0DeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateNetworkPolicyRequestBodyCase0DeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. Traffic to matching domains will be intercepted instead of proxied through encrypted connections. */
export type UpdateNetworkPolicyRequestBodyCase0InjectionRulesList =
  Array<unknown>;
export const UpdateNetworkPolicyRequestBodyCase0InjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateNetworkPolicyRequestBodyCase0InjectionRulesList>;

/** Network access policy for the sandbox.\n Controls which external hosts the sandbox can communicate with.\n Use \"allow-all\" mode to allow all traffic, \"deny-all\" to block all traffic or \"custom\" to provide specific rules. */
export interface UpdateNetworkPolicyRequestBodyCase0 {
  /** The network access policy mode. Use \"allow-all\" to permit all outbound traffic. Use \"deny-all\" to block all outbound traffic. Use \"custom\" to specify explicit allow/deny rules. */
  mode: UpdateNetworkPolicyRequestBodyCase0Mode | (string & {});
  /** List of domain names the sandbox is allowed to connect to. Only applies when mode is \"custom\". Supports wildcard patterns (e.g., \"*.example.com\" matches all subdomains). */
  allowedDomains?: UpdateNetworkPolicyRequestBodyCase0AllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. Traffic to these addresses bypasses domain-based restrictions. */
  allowedCIDRs?: UpdateNetworkPolicyRequestBodyCase0AllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: UpdateNetworkPolicyRequestBodyCase0DeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. Traffic to matching domains will be intercepted instead of proxied through encrypted connections. */
  injectionRules?: UpdateNetworkPolicyRequestBodyCase0InjectionRulesList;
}
export const UpdateNetworkPolicyRequestBodyCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    mode: UpdateNetworkPolicyRequestBodyCase0Mode,
    allowedDomains: S.optional(
      UpdateNetworkPolicyRequestBodyCase0AllowedDomainsList,
    ),
    allowedCIDRs: S.optional(
      UpdateNetworkPolicyRequestBodyCase0AllowedCIDRsList,
    ),
    deniedCIDRs: S.optional(UpdateNetworkPolicyRequestBodyCase0DeniedCIDRsList),
    injectionRules: S.optional(
      UpdateNetworkPolicyRequestBodyCase0InjectionRulesList,
    ),
  }),
).annotate({
  identifier: "UpdateNetworkPolicyRequestBodyCase0",
}) as any as S.Schema<UpdateNetworkPolicyRequestBodyCase0>;

export type UpdateNetworkPolicyRequestBodyCase1AllowCase0List = Array<unknown>;
export const UpdateNetworkPolicyRequestBodyCase1AllowCase0List =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateNetworkPolicyRequestBodyCase1AllowCase0List>;

export type UpdateNetworkPolicyRequestBodyCase1Allow =
  | UpdateNetworkPolicyRequestBodyCase1AllowCase0List
  | unknown;
export const UpdateNetworkPolicyRequestBodyCase1Allow =
  /*@__PURE__*/ S.Unknown as any as S.Schema<UpdateNetworkPolicyRequestBodyCase1Allow>;

export type UpdateNetworkPolicyRequestBodyCase1SubnetsAllowList =
  Array<unknown>;
export const UpdateNetworkPolicyRequestBodyCase1SubnetsAllowList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateNetworkPolicyRequestBodyCase1SubnetsAllowList>;

export type UpdateNetworkPolicyRequestBodyCase1SubnetsDenyList = Array<unknown>;
export const UpdateNetworkPolicyRequestBodyCase1SubnetsDenyList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateNetworkPolicyRequestBodyCase1SubnetsDenyList>;

export interface UpdateNetworkPolicyRequestBodyCase1Subnets {
  allow?: UpdateNetworkPolicyRequestBodyCase1SubnetsAllowList;
  deny?: UpdateNetworkPolicyRequestBodyCase1SubnetsDenyList;
}
export const UpdateNetworkPolicyRequestBodyCase1Subnets =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      allow: S.optional(UpdateNetworkPolicyRequestBodyCase1SubnetsAllowList),
      deny: S.optional(UpdateNetworkPolicyRequestBodyCase1SubnetsDenyList),
    }),
  ).annotate({
    identifier: "UpdateNetworkPolicyRequestBodyCase1Subnets",
  }) as any as S.Schema<UpdateNetworkPolicyRequestBodyCase1Subnets>;

export interface UpdateNetworkPolicyRequestBodyCase1 {
  allow?: UpdateNetworkPolicyRequestBodyCase1Allow;
  subnets?: UpdateNetworkPolicyRequestBodyCase1Subnets;
}
export const UpdateNetworkPolicyRequestBodyCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    allow: S.optional(UpdateNetworkPolicyRequestBodyCase1Allow),
    subnets: S.optional(UpdateNetworkPolicyRequestBodyCase1Subnets),
  }),
).annotate({
  identifier: "UpdateNetworkPolicyRequestBodyCase1",
}) as any as S.Schema<UpdateNetworkPolicyRequestBodyCase1>;

export type UpdateNetworkPolicyRequestBody =
  | UpdateNetworkPolicyRequestBodyCase0
  | UpdateNetworkPolicyRequestBodyCase1;
export const UpdateNetworkPolicyRequestBody =
  /*@__PURE__*/ S.Unknown as any as S.Schema<UpdateNetworkPolicyRequestBody>;

export interface UpdateNetworkPolicyRequest {
  /** The unique identifier of the session to update the network policy for. */
  sessionId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  body?: UpdateNetworkPolicyRequestBody;
}
export const UpdateNetworkPolicyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    body: S.optional(UpdateNetworkPolicyRequestBody.pipe(T.HttpBody())),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/sandboxes/sessions/{sessionId}/network-policy",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateNetworkPolicyRequest",
}) as any as S.Schema<UpdateNetworkPolicyRequest>;

/** The status of the sandbox. */
export type UpdateNetworkPolicyResponseSessionStatus =
  | "aborted"
  | "failed"
  | "pending"
  | "running"
  | "snapshotting"
  | "stopped"
  | "stopping";
export const UpdateNetworkPolicyResponseSessionStatus = /*@__PURE__*/ S.String;

/** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
export type UpdateNetworkPolicyResponseSessionNetworkPolicyMode =
  | "allow-all"
  | "custom"
  | "deny-all";
export const UpdateNetworkPolicyResponseSessionNetworkPolicyMode =
  /*@__PURE__*/ S.String;

/** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
export type UpdateNetworkPolicyResponseSessionNetworkPolicyAllowedDomainsList =
  Array<unknown>;
export const UpdateNetworkPolicyResponseSessionNetworkPolicyAllowedDomainsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateNetworkPolicyResponseSessionNetworkPolicyAllowedDomainsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
export type UpdateNetworkPolicyResponseSessionNetworkPolicyAllowedCIDRsList =
  Array<unknown>;
export const UpdateNetworkPolicyResponseSessionNetworkPolicyAllowedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateNetworkPolicyResponseSessionNetworkPolicyAllowedCIDRsList>;

/** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
export type UpdateNetworkPolicyResponseSessionNetworkPolicyDeniedCIDRsList =
  Array<unknown>;
export const UpdateNetworkPolicyResponseSessionNetworkPolicyDeniedCIDRsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateNetworkPolicyResponseSessionNetworkPolicyDeniedCIDRsList>;

/** HTTP header injection rules for outgoing requests matching specific domains. */
export type UpdateNetworkPolicyResponseSessionNetworkPolicyInjectionRulesList =
  Array<unknown>;
export const UpdateNetworkPolicyResponseSessionNetworkPolicyInjectionRulesList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<UpdateNetworkPolicyResponseSessionNetworkPolicyInjectionRulesList>;

/** The network policy applied to this sandbox, if any. */
export interface UpdateNetworkPolicyResponseSessionNetworkPolicy {
  /** The network policy mode. - 'allow-all': All traffic is allowed. - 'deny-all': All traffic is blocked. - 'custom': Traffic is controlled by explicit allow/deny rules. */
  mode: UpdateNetworkPolicyResponseSessionNetworkPolicyMode;
  /** List of domain names the sandbox is allowed to connect to. Supports wildcard patterns (e.g., "*.vercel.com" matches all subdomains). */
  allowedDomains?: UpdateNetworkPolicyResponseSessionNetworkPolicyAllowedDomainsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is allowed to connect to. */
  allowedCIDRs?: UpdateNetworkPolicyResponseSessionNetworkPolicyAllowedCIDRsList;
  /** List of IP address ranges (in CIDR notation) the sandbox is blocked from connecting to. These rules take precedence over all allowed rules. */
  deniedCIDRs?: UpdateNetworkPolicyResponseSessionNetworkPolicyDeniedCIDRsList;
  /** HTTP header injection rules for outgoing requests matching specific domains. */
  injectionRules?: UpdateNetworkPolicyResponseSessionNetworkPolicyInjectionRulesList;
}
export const UpdateNetworkPolicyResponseSessionNetworkPolicy =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      mode: UpdateNetworkPolicyResponseSessionNetworkPolicyMode,
      allowedDomains: S.optional(
        UpdateNetworkPolicyResponseSessionNetworkPolicyAllowedDomainsList,
      ),
      allowedCIDRs: S.optional(
        UpdateNetworkPolicyResponseSessionNetworkPolicyAllowedCIDRsList,
      ),
      deniedCIDRs: S.optional(
        UpdateNetworkPolicyResponseSessionNetworkPolicyDeniedCIDRsList,
      ),
      injectionRules: S.optional(
        UpdateNetworkPolicyResponseSessionNetworkPolicyInjectionRulesList,
      ),
    }),
  ).annotate({
    identifier: "UpdateNetworkPolicyResponseSessionNetworkPolicy",
  }) as any as S.Schema<UpdateNetworkPolicyResponseSessionNetworkPolicy>;

/** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
export type UpdateNetworkPolicyResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;
export const UpdateNetworkPolicyResponseSessionNetworkTransfer =
  CreateANamedSandboxResponseSessionNetworkTransfer;

/** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
export interface UpdateNetworkPolicyResponseSession {
  /** The name of the source sandbox. */
  sourceSandboxName: string;
  /** The unique identifier of the project associated with this session. */
  projectId: string;
  /** The unique identifier of the sandbox. */
  id: string;
  /** Memory allocated to this sandbox in MB. */
  memory: number;
  /** Number of vCPUs allocated to this sandbox. */
  vcpus: number;
  /** The region where the sandbox is hosted. */
  region: string;
  /** The runtime of the sandbox. */
  runtime: string;
  /** The maximum amount of time the sandbox will run for in milliseconds. */
  timeout: number;
  /** The status of the sandbox. */
  status: UpdateNetworkPolicyResponseSessionStatus;
  /** The time when the sandbox was requested, in milliseconds since the epoch. */
  requestedAt: number;
  /** The time when the sandbox was started, in milliseconds since the epoch. */
  startedAt?: number;
  /** The working directory of the sandbox. */
  cwd: string;
  /** The time when the sandbox was requested to stop, in milliseconds since the epoch. */
  requestedStopAt?: number;
  /** The time when the sandbox was stopped, in milliseconds since the epoch. */
  stoppedAt?: number;
  /** The time when the sandbox was aborted, in milliseconds since the epoch. */
  abortedAt?: number;
  /** The duration of the sandbox in milliseconds. */
  duration?: number;
  /** The unique identifier of the snapshot associated with this sandbox, if any. */
  sourceSnapshotId?: string;
  /** The time when a snapshot was requested, in milliseconds since the epoch. */
  snapshottedAt?: number;
  /** The time when the sandbox was created, in milliseconds since the epoch. */
  createdAt: number;
  /** The last time the sandbox was updated, in milliseconds since the epoch. */
  updatedAt: number;
  /** The network policy applied to this sandbox, if any. */
  networkPolicy?: UpdateNetworkPolicyResponseSessionNetworkPolicy;
  /** The amount of CPU time the sandbox consumed, if available, in milliseconds. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  activeCpuDurationMs?: number;
  /** The quantity of data transfered to and from the sandbox, in bytes. This value is only available once the sandbox is stopped, and only if it stopped successfully. */
  networkTransfer?: CreateANamedSandboxResponseSessionNetworkTransfer;
}
export const UpdateNetworkPolicyResponseSession = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sourceSandboxName: S.String,
    projectId: S.String,
    id: S.String,
    memory: S.Number,
    vcpus: S.Number,
    region: S.String,
    runtime: S.String,
    timeout: S.Number,
    status: UpdateNetworkPolicyResponseSessionStatus,
    requestedAt: S.Number,
    startedAt: S.optional(S.Number),
    cwd: S.String,
    requestedStopAt: S.optional(S.Number),
    stoppedAt: S.optional(S.Number),
    abortedAt: S.optional(S.Number),
    duration: S.optional(S.Number),
    sourceSnapshotId: S.optional(S.String),
    snapshottedAt: S.optional(S.Number),
    createdAt: S.Number,
    updatedAt: S.Number,
    networkPolicy: S.optional(UpdateNetworkPolicyResponseSessionNetworkPolicy),
    activeCpuDurationMs: S.optional(S.Number),
    networkTransfer: S.optional(
      CreateANamedSandboxResponseSessionNetworkTransfer,
    ),
  }),
).annotate({
  identifier: "UpdateNetworkPolicyResponseSession",
}) as any as S.Schema<UpdateNetworkPolicyResponseSession>;

export interface UpdateNetworkPolicyResponse {
  /** This object contains information related to a Vercel Sandbox Session. v2 endpoints return "session" instead of "sandbox" as the response wrapper key. */
  session: UpdateNetworkPolicyResponseSession;
}
export const UpdateNetworkPolicyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    session: UpdateNetworkPolicyResponseSession,
  }),
).annotate({
  identifier: "UpdateNetworkPolicyResponse",
}) as any as S.Schema<UpdateNetworkPolicyResponse>;

export interface WriteFilesRequest {
  /** The unique identifier of the session to write files to. */
  sessionId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  /** The target directory where the tarball contents will be extracted. If not specified, files are extracted to the sandbox home directory. */
  xCwd?: string;
}
export const WriteFilesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessionId: S.String.pipe(T.Label()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    xCwd: S.optional(S.String.pipe(T.Header("x-Cwd"))),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/sandboxes/sessions/{sessionId}/fs/write",
      code: 200,
    }),
  ),
).annotate({
  identifier: "WriteFilesRequest",
}) as any as S.Schema<WriteFilesRequest>;

export type WriteFilesResponse = unknown;
export const WriteFilesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "WriteFilesResponse",
}) as any as S.Schema<WriteFilesResponse>;

export type CreateADirectoryError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Create a directory Creates a new directory in a session's filesystem. By default, parent directories are created recursively if they don't exist (similar to `mkdir -p`). */
export const createADirectory: API.OperationMethod<
  CreateADirectoryRequest,
  CreateADirectoryResponse,
  CreateADirectoryError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateADirectoryRequest,
  output: CreateADirectoryResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone, UnprocessableEntity],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type CreateANamedSandboxError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Conflict
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Create a named sandbox Creates a named sandbox environment. Named sandboxes have a unique name within a project and support automatic snapshotting on shutdown. */
export const createANamedSandbox: API.OperationMethod<
  CreateANamedSandboxRequest,
  CreateANamedSandboxResponse,
  CreateANamedSandboxError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateANamedSandboxRequest,
  output: CreateANamedSandboxResponse,
  errors: [
    BadRequest,
    PaymentRequired,
    Forbidden,
    NotFound,
    Conflict,
    Gone,
    UnprocessableEntity,
  ],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type CreateANamedSandbox1Error =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Conflict
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Create a named sandbox Creates a named sandbox environment. Named sandboxes have a unique name within a project and support automatic snapshotting on shutdown. Unlike v2, this version has no `runtime` parameter: when no `image` is provided (and the sandbox is not restored from a snapshot), the sandbox is created from the default universal image. */
export const createANamedSandbox1: API.OperationMethod<
  CreateANamedSandbox1Request,
  CreateANamedSandbox1Response,
  CreateANamedSandbox1Error,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateANamedSandbox1Request,
  output: CreateANamedSandbox1Response,
  errors: [
    BadRequest,
    PaymentRequired,
    Forbidden,
    NotFound,
    Conflict,
    Gone,
    UnprocessableEntity,
  ],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type CreateASnapshotError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Create a snapshot Creates a point-in-time snapshot of a running session's filesystem. Snapshots can be used to quickly restore a session to a previous state or to create new sessions with pre-configured environments. The session must be running and able to accept commands for a snapshot to be created. The session will be terminated after the snapshot is created. */
export const createASnapshot: API.OperationMethod<
  CreateASnapshotRequest,
  CreateASnapshotResponse,
  CreateASnapshotError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateASnapshotRequest,
  output: CreateASnapshotResponse,
  errors: [
    BadRequest,
    PaymentRequired,
    Forbidden,
    NotFound,
    Gone,
    UnprocessableEntity,
  ],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type DeleteADriveError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | Gone
  | VercelOpError;
/** Delete a drive Deletes a drive by project and name. Attached drives cannot be deleted. Stop or replace the session currently using the drive before retrying deletion. Drives are in private beta. Register your interest to get access: https://vercel.com/changelog/drives-for-vercel-sandbox-in-private-beta */
export const deleteADrive: API.OperationMethod<
  DeleteADriveRequest,
  DeleteADriveResponse,
  DeleteADriveError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteADriveRequest,
  output: DeleteADriveResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type DeleteASandboxError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Delete a sandbox Deletes a sandbox by name. If sandboxes are currently running, they will be stopped first. This operation deletes all sandbox entities with the given name and the named sandbox metadata. */
export const deleteASandbox: API.OperationMethod<
  DeleteASandboxRequest,
  DeleteASandboxResponse,
  DeleteASandboxError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteASandboxRequest,
  output: DeleteASandboxResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type DeleteASnapshotError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Delete a snapshot Permanently deletes a snapshot and frees its associated storage. This action cannot be undone. After deletion, the snapshot can no longer be used to create new sessions. */
export const deleteASnapshot: API.OperationMethod<
  DeleteASnapshotRequest,
  DeleteASnapshotResponse,
  DeleteASnapshotError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteASnapshotRequest,
  output: DeleteASnapshotResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ExecuteACommandError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Execute a command Executes a shell command inside a running session. The command runs asynchronously and returns immediately with a command ID that can be used to track its progress and retrieve its output. Optionally, use the `wait` parameter to stream the command status until completion. */
export const executeACommand: API.OperationMethod<
  ExecuteACommandRequest,
  ExecuteACommandResponse,
  ExecuteACommandError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExecuteACommandRequest,
  output: ExecuteACommandResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone, UnprocessableEntity],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ExtendSessionTimeoutError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Extend session timeout Extends the maximum execution time of a running session. The session must be active and able to accept commands. The total timeout cannot exceed the maximum allowed limit for your account. */
export const extendSessionTimeout: API.OperationMethod<
  ExtendSessionTimeoutRequest,
  ExtendSessionTimeoutResponse,
  ExtendSessionTimeoutError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExtendSessionTimeoutRequest,
  output: ExtendSessionTimeoutResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone, UnprocessableEntity],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ForkANamedSandboxError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Conflict
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Fork a named sandbox Forks a named sandbox, creating a new named sandbox from the source's configuration. Resources, timeout, ports, tags, network policy, mounts, Connect network, image, persistence, snapshot settings and — unlike the SDK-side fork — environment variables are copied from the source automatically (`interactive` is not). When the source has a snapshot the fork starts from it; otherwise it starts from the source's runtime/image. Any field provided in the request body overrides the value copied from the source. */
export const forkANamedSandbox: API.OperationMethod<
  ForkANamedSandboxRequest,
  ForkANamedSandboxResponse,
  ForkANamedSandboxError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ForkANamedSandboxRequest,
  output: ForkANamedSandboxResponse,
  errors: [
    BadRequest,
    PaymentRequired,
    Forbidden,
    NotFound,
    Conflict,
    Gone,
    UnprocessableEntity,
  ],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type GetACommandError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Get a command Retrieves the current status and details of a command executed in a session. Use the `wait` parameter to block until the command finishes execution. */
export const getACommand: API.OperationMethod<
  GetACommandRequest,
  GetACommandResponse,
  GetACommandError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetACommandRequest,
  output: GetACommandResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone, UnprocessableEntity],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type GetANamedSandboxError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Conflict
  | Gone
  | VercelOpError;
/** Get a named sandbox Retrieves a named sandbox by name, including its current sandbox and routes. If the sandbox is stopped and resume is true, a new sandbox will be created from the most recent snapshot. */
export const getANamedSandbox: API.OperationMethod<
  GetANamedSandboxRequest,
  GetANamedSandboxResponse,
  GetANamedSandboxError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetANamedSandboxRequest,
  output: GetANamedSandboxResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Conflict, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type GetASessionError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Get a session Retrieves detailed information about a specific session, including its current status, resource configuration, and exposed routes. */
export const getASession: API.OperationMethod<
  GetASessionRequest,
  GetASessionResponse,
  GetASessionError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetASessionRequest,
  output: GetASessionResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type GetASnapshotError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Get a snapshot Retrieves detailed information about a specific snapshot, including its creation time, size, expiration date, and the source session it was created from. */
export const getASnapshot: API.OperationMethod<
  GetASnapshotRequest,
  GetASnapshotResponse,
  GetASnapshotError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetASnapshotRequest,
  output: GetASnapshotResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type GetOrCreateADriveError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Conflict
  | Gone
  | VercelOpError;
/** Get or create a drive Gets an existing drive by project and name, or creates it when it does not exist. Drives are in private beta. Register your interest to get access: https://vercel.com/changelog/drives-for-vercel-sandbox-in-private-beta */
export const getOrCreateADrive: API.OperationMethod<
  GetOrCreateADriveRequest,
  GetOrCreateADriveResponse,
  GetOrCreateADriveError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetOrCreateADriveRequest,
  output: GetOrCreateADriveResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Conflict, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type KillACommandError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Kill a command Sends a signal to terminate a running command in a session. The signal can be used to gracefully stop (SIGTERM) or forcefully kill (SIGKILL) the process. The command must still be running for this operation to succeed. */
export const killACommand: API.OperationMethod<
  KillACommandRequest,
  KillACommandResponse,
  KillACommandError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KillACommandRequest,
  output: KillACommandResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone, UnprocessableEntity],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ListCommandsError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** List commands Retrieves a list of all commands that have been executed in a session, including their current status, exit codes, and execution times, ordered from the most recent to the oldest. */
export const listCommands: API.OperationMethod<
  ListCommandsRequest,
  ListCommandsResponse,
  ListCommandsError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListCommandsRequest,
  output: ListCommandsResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ListDrivesError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** List drives Retrieves a paginated list of drives belonging to a specific project. Drives are in private beta. Register your interest to get access: https://vercel.com/changelog/drives-for-vercel-sandbox-in-private-beta */
export const listDrives: API.OperationMethod<
  ListDrivesRequest,
  ListDrivesResponse,
  ListDrivesError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListDrivesRequest,
  output: ListDrivesResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ListSandboxesError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** List sandboxes Retrieves a paginated list of named sandboxes belonging to a specific project. Results can be sorted by creation time or name, and optionally filtered by name prefix or status. */
export const listSandboxes: API.OperationMethod<
  ListSandboxesRequest,
  ListSandboxesResponse,
  ListSandboxesError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListSandboxesRequest,
  output: ListSandboxesResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ListSessionsError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** List sessions Retrieves a paginated list of sessions belonging to a specific sandbox. Results are sorted by creation time and paginated using an opaque cursor. */
export const listSessions: API.OperationMethod<
  ListSessionsRequest,
  ListSessionsResponse,
  ListSessionsError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListSessionsRequest,
  output: ListSessionsResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ListSnapshotsError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** List snapshots Retrieves a paginated list of snapshots for a specific project. */
export const listSnapshots: API.OperationMethod<
  ListSnapshotsRequest,
  ListSnapshotsResponse,
  ListSnapshotsError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListSnapshotsRequest,
  output: ListSnapshotsResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ReadAFileError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Read a file Downloads the contents of a file from a session's filesystem. The file content is returned as a binary stream with appropriate Content-Disposition headers for file download. */
export const readAFile: API.OperationMethod<
  ReadAFileRequest,
  ReadAFileResponse,
  ReadAFileError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReadAFileRequest,
  output: ReadAFileResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone, UnprocessableEntity],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type StopASessionError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Stop a session Stops a running session and releases its allocated resources. All running processes within the session will be terminated. This action cannot be undone. A stopped session cannot be restarted. */
export const stopASession: API.OperationMethod<
  StopASessionRequest,
  StopASessionResponse,
  StopASessionError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StopASessionRequest,
  output: StopASessionResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone, UnprocessableEntity],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type StreamCommandLogsError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Stream command logs Streams the output of a command in real-time using newline-delimited JSON (ND-JSON). Each entry includes the output data and stream type. Stream types include `stdout`, `stderr`, and `error` (for stream failures). */
export const streamCommandLogs: API.OperationMethod<
  StreamCommandLogsRequest,
  StreamCommandLogsResponse,
  StreamCommandLogsError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StreamCommandLogsRequest,
  output: StreamCommandLogsResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone, UnprocessableEntity],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type UpdateASandboxError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Conflict
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Update a sandbox Updates the configuration of a sandbox. Only the provided fields will be modified; omitted fields remain unchanged. */
export const updateASandbox: API.OperationMethod<
  UpdateASandboxRequest,
  UpdateASandboxResponse,
  UpdateASandboxError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateASandboxRequest,
  output: UpdateASandboxResponse,
  errors: [
    BadRequest,
    PaymentRequired,
    Forbidden,
    NotFound,
    Conflict,
    Gone,
    UnprocessableEntity,
  ],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type UpdateNetworkPolicyError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Update network policy Replaces the network access policy of a running session. Use this to control which external hosts the session can communicate with. This is a full replacement. Any previously configured network rules will be overwritten. */
export const updateNetworkPolicy: API.OperationMethod<
  UpdateNetworkPolicyRequest,
  UpdateNetworkPolicyResponse,
  UpdateNetworkPolicyError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateNetworkPolicyRequest,
  output: UpdateNetworkPolicyResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone, UnprocessableEntity],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type WriteFilesError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | UnprocessableEntity
  | VercelOpError;
/** Write files Uploads and extracts files to a session's filesystem. Files must be uploaded as a gzipped tarball (`.tar.gz`) with the `Content-Type` header set to `application/gzip`. The tarball contents are extracted to the session's working directory, or to a custom directory specified via the `x-cwd` header. */
export const writeFiles: API.OperationMethod<
  WriteFilesRequest,
  WriteFilesResponse,
  WriteFilesError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WriteFilesRequest,
  output: WriteFilesResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone, UnprocessableEntity],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));
