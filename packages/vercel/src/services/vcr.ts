// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs — the vercel.com/docs/rest-api mirror). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  VercelProtocol,
  type VercelOpError,
  type VercelOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { VercelOpError, VercelOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class Gone
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Gone>()("Gone", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 410 }],
  ) {}

export class MethodNotAllowed
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<MethodNotAllowed>()("MethodNotAllowed", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 405 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class PayloadTooLarge
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<PayloadTooLarge>()("PayloadTooLarge", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 413 }],
  ) {}

export class PaymentRequired
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<PaymentRequired>()("PaymentRequired", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withQuotaError),
    [{ status: 402 }],
  ) {}

export class RangeNotSatisfiable
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<RangeNotSatisfiable>()("RangeNotSatisfiable", {
      code: S.Number,
      message: S.String,
    }),
    [{ status: 416 }],
  ) {}

export interface AddARepositoryPermissionRequest {
  idOrName: string;
  projectId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  /** Slug of a team that is granted access to a repository. */
  teamSlug?: string;
}
export const AddARepositoryPermissionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    idOrName: S.String.pipe(T.Label()),
    projectId: S.String.pipe(T.Query()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    teamSlug: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v1/vcr/repository/{idOrName}/permissions",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AddARepositoryPermissionRequest",
}) as any as S.Schema<AddARepositoryPermissionRequest>;

/** A team's access grant to a Vercel Container Registry repository. */
export interface AddARepositoryPermissionResponsePermission {
  /** Identifier of the repository the permission grants access to. */
  repositoryId: string;
  /** Identifier of the team that is granted access to the repository. */
  teamId: string;
  /** Slug of the team that is granted access to the repository. */
  teamSlug: string;
  /** ISO 8601 timestamp of when the permission was created. */
  createdAt: string;
}
export const AddARepositoryPermissionResponsePermission =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      repositoryId: S.String,
      teamId: S.String,
      teamSlug: S.String,
      createdAt: S.String,
    }),
  ).annotate({
    identifier: "AddARepositoryPermissionResponsePermission",
  }) as any as S.Schema<AddARepositoryPermissionResponsePermission>;

export interface AddARepositoryPermissionResponse {
  /** A team's access grant to a Vercel Container Registry repository. */
  permission: AddARepositoryPermissionResponsePermission;
}
export const AddARepositoryPermissionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    permission: AddARepositoryPermissionResponsePermission,
  }),
).annotate({
  identifier: "AddARepositoryPermissionResponse",
}) as any as S.Schema<AddARepositoryPermissionResponse>;

export interface CancelABlobUploadRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Blob upload session identifier. */
  uuid: string;
}
export const CancelABlobUploadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    uuid: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/blobs/uploads/{uuid}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CancelABlobUploadRequest",
}) as any as S.Schema<CancelABlobUploadRequest>;

export type CancelABlobUploadResponse = unknown;
export const CancelABlobUploadResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "CancelABlobUploadResponse",
}) as any as S.Schema<CancelABlobUploadResponse>;

export interface CheckIfABlobExistsRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Content-addressable digest (algorithm:hex). */
  digest: string;
}
export const CheckIfABlobExistsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    digest: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "HEAD",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/blobs/{digest}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CheckIfABlobExistsRequest",
}) as any as S.Schema<CheckIfABlobExistsRequest>;

export type CheckIfABlobExistsResponse = unknown;
export const CheckIfABlobExistsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "CheckIfABlobExistsResponse",
}) as any as S.Schema<CheckIfABlobExistsResponse>;

export interface CheckIfAManifestExistsRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Manifest reference: a tag or digest. */
  reference: string;
}
export const CheckIfAManifestExistsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    reference: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "HEAD",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/manifests/{reference}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CheckIfAManifestExistsRequest",
}) as any as S.Schema<CheckIfAManifestExistsRequest>;

export type CheckIfAManifestExistsResponse = unknown;
export const CheckIfAManifestExistsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "CheckIfAManifestExistsResponse",
}) as any as S.Schema<CheckIfAManifestExistsResponse>;

export interface CheckRegistryApiVersionSupportRequest {}
export const CheckRegistryApiVersionSupportRequest = /*@__PURE__*/ S.suspend(
  () => S.Struct({}).pipe(T.Http({ method: "GET", uri: "/v2/", code: 200 })),
).annotate({
  identifier: "CheckRegistryApiVersionSupportRequest",
}) as any as S.Schema<CheckRegistryApiVersionSupportRequest>;

export type CheckRegistryApiVersionSupportResponse = unknown;
export const CheckRegistryApiVersionSupportResponse = /*@__PURE__*/ S.suspend(
  () => S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "CheckRegistryApiVersionSupportResponse",
}) as any as S.Schema<CheckRegistryApiVersionSupportResponse>;

export interface ClearAllRepositoryPermissionsRequest {
  idOrName: string;
  projectId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const ClearAllRepositoryPermissionsRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      idOrName: S.String.pipe(T.Label()),
      projectId: S.String.pipe(T.Query()),
      teamId: S.optional(S.String.pipe(T.Query())),
      slug: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/v1/vcr/repository/{idOrName}/permissions/all",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ClearAllRepositoryPermissionsRequest",
}) as any as S.Schema<ClearAllRepositoryPermissionsRequest>;

export type ClearAllRepositoryPermissionsResponse = unknown;
export const ClearAllRepositoryPermissionsResponse = /*@__PURE__*/ S.suspend(
  () => S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ClearAllRepositoryPermissionsResponse",
}) as any as S.Schema<ClearAllRepositoryPermissionsResponse>;

export interface CompleteABlobUploadRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Blob upload session identifier. */
  uuid: string;
  /** Content-addressable digest (algorithm:hex). */
  digest: string;
}
export const CompleteABlobUploadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    uuid: S.String.pipe(T.Label()),
    digest: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/blobs/uploads/{uuid}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CompleteABlobUploadRequest",
}) as any as S.Schema<CompleteABlobUploadRequest>;

export type CompleteABlobUploadResponse = unknown;
export const CompleteABlobUploadResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "CompleteABlobUploadResponse",
}) as any as S.Schema<CompleteABlobUploadResponse>;

export interface CreateARepositoryRequest {
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  projectId: string;
  /** Single Docker repository name component. */
  name: string;
}
export const CreateARepositoryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    projectId: S.String,
    name: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/v1/vcr/repository", code: 200 })),
).annotate({
  identifier: "CreateARepositoryRequest",
}) as any as S.Schema<CreateARepositoryRequest>;

/** A Vercel Container Registry repository. */
export interface CreateARepositoryResponseRepository {
  /** Unique identifier of the repository. */
  id: string;
  /** Identifier of the project the repository belongs to. */
  projectId: string;
  /** Name of the repository. */
  name: string;
  /** Whether the repository is public. Images in public repositories can be pulled by anyone. Defaults to `false` (private). */
  public: boolean;
  /** ISO 8601 timestamp of when the repository was created. */
  createdAt: string;
  /** ISO 8601 timestamp of when the repository was last updated. */
  updatedAt: string;
}
export const CreateARepositoryResponseRepository = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    projectId: S.String,
    name: S.String,
    public: S.Boolean,
    createdAt: S.String,
    updatedAt: S.String,
  }),
).annotate({
  identifier: "CreateARepositoryResponseRepository",
}) as any as S.Schema<CreateARepositoryResponseRepository>;

export interface CreateARepositoryResponse {
  /** A Vercel Container Registry repository. */
  repository: CreateARepositoryResponseRepository;
}
export const CreateARepositoryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    repository: CreateARepositoryResponseRepository,
  }),
).annotate({
  identifier: "CreateARepositoryResponse",
}) as any as S.Schema<CreateARepositoryResponse>;

export interface DeleteABlobRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Content-addressable digest (algorithm:hex). */
  digest: string;
}
export const DeleteABlobRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    digest: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/blobs/{digest}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteABlobRequest",
}) as any as S.Schema<DeleteABlobRequest>;

export interface DeleteABlobResponse {}
export const DeleteABlobResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteABlobResponse",
}) as any as S.Schema<DeleteABlobResponse>;

export interface DeleteAnImageManifestRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Content-addressable digest (algorithm:hex). */
  reference: string;
}
export const DeleteAnImageManifestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    reference: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/manifests/{reference}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteAnImageManifestRequest",
}) as any as S.Schema<DeleteAnImageManifestRequest>;

export type DeleteAnImageManifestResponse = unknown;
export const DeleteAnImageManifestResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "DeleteAnImageManifestResponse",
}) as any as S.Schema<DeleteAnImageManifestResponse>;

export interface DeleteARepositoryRequest {
  idOrName: string;
  projectId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const DeleteARepositoryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    idOrName: S.String.pipe(T.Label()),
    projectId: S.String.pipe(T.Query()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v1/vcr/repository/{idOrName}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteARepositoryRequest",
}) as any as S.Schema<DeleteARepositoryRequest>;

export type DeleteARepositoryResponse = unknown;
export const DeleteARepositoryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "DeleteARepositoryResponse",
}) as any as S.Schema<DeleteARepositoryResponse>;

export interface DeleteARepositoryImageRequest {
  idOrName: string;
  imageId: string;
  projectId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const DeleteARepositoryImageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    idOrName: S.String.pipe(T.Label()),
    imageId: S.String.pipe(T.Label()),
    projectId: S.String.pipe(T.Query()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v1/vcr/repository/{idOrName}/images/{imageId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteARepositoryImageRequest",
}) as any as S.Schema<DeleteARepositoryImageRequest>;

export type DeleteARepositoryImageResponse = unknown;
export const DeleteARepositoryImageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "DeleteARepositoryImageResponse",
}) as any as S.Schema<DeleteARepositoryImageResponse>;

export interface DownloadABlobRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Content-addressable digest (algorithm:hex). */
  digest: string;
}
export const DownloadABlobRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    digest: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/blobs/{digest}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DownloadABlobRequest",
}) as any as S.Schema<DownloadABlobRequest>;

export interface DownloadABlobResponse {}
export const DownloadABlobResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DownloadABlobResponse",
}) as any as S.Schema<DownloadABlobResponse>;

export interface GetARepositoryRequest {
  idOrName: string;
  projectId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const GetARepositoryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    idOrName: S.String.pipe(T.Label()),
    projectId: S.String.pipe(T.Query()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/v1/vcr/repository/{idOrName}", code: 200 }),
  ),
).annotate({
  identifier: "GetARepositoryRequest",
}) as any as S.Schema<GetARepositoryRequest>;

/** A Vercel Container Registry repository. */
export type GetARepositoryResponseRepository =
  CreateARepositoryResponseRepository;
export const GetARepositoryResponseRepository =
  CreateARepositoryResponseRepository;

export interface GetARepositoryResponse {
  /** A Vercel Container Registry repository. */
  repository: CreateARepositoryResponseRepository;
}
export const GetARepositoryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    repository: CreateARepositoryResponseRepository,
  }),
).annotate({
  identifier: "GetARepositoryResponse",
}) as any as S.Schema<GetARepositoryResponse>;

export interface GetARepositoryImageRequest {
  idOrName: string;
  /** The internal image id (`image_...`) or the image manifest digest (`sha256:...`). */
  imageIdOrDigest: string;
  projectId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const GetARepositoryImageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    idOrName: S.String.pipe(T.Label()),
    imageIdOrDigest: S.String.pipe(T.Label()),
    projectId: S.String.pipe(T.Query()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v1/vcr/repository/{idOrName}/images/{imageIdOrDigest}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetARepositoryImageRequest",
}) as any as S.Schema<GetARepositoryImageRequest>;

export type GetARepositoryImageResponseImageLayersList = Array<unknown>;
export const GetARepositoryImageResponseImageLayersList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<GetARepositoryImageResponseImageLayersList>;

/** VHS-readiness status, or `null` for a multi-platform index. */
export type GetARepositoryImageResponseImageStatus =
  | "preparing"
  | "ready"
  | "unoptimized";
export const GetARepositoryImageResponseImageStatus = /*@__PURE__*/ S.String;

/** Tags pointing at this image's manifest. */
export type GetARepositoryImageResponseImageTagsList = Array<string>;
export const GetARepositoryImageResponseImageTagsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<GetARepositoryImageResponseImageTagsList>;

/** Whether the manifest is a multi-platform image index, a single-platform image manifest or an attestation. */
export type GetARepositoryImageResponseImageKind =
  | "attestation"
  | "index"
  | "manifest";
export const GetARepositoryImageResponseImageKind = /*@__PURE__*/ S.String;

/** Converted VHS drive data, present once an image has been optimized for sandbox launch. */
export interface GetARepositoryImageResponseImageVhs {
  path: string;
  digest: string;
  /** Optional VHS drive configuration captured for an optimized image. */
  config?: unknown;
}
export const GetARepositoryImageResponseImageVhs = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    digest: S.String,
    config: S.optional(S.Unknown),
  }),
).annotate({
  identifier: "GetARepositoryImageResponseImageVhs",
}) as any as S.Schema<GetARepositoryImageResponseImageVhs>;

/** A single image with its tags, status and resolved Dockerfile layer history. */
export interface GetARepositoryImageResponseImage {
  layers: GetARepositoryImageResponseImageLayersList;
  /** VHS-readiness status, or `null` for a multi-platform index. */
  status: GetARepositoryImageResponseImageStatus | null;
  /** Tags pointing at this image's manifest. */
  tags: GetARepositoryImageResponseImageTagsList;
  /** Internal identifier of the image. */
  id: string;
  /** Identifier of the repository the image belongs to. */
  repositoryId: string;
  /** SHA-256 digest of the image manifest. */
  manifestDigest: string;
  /** Whether the manifest is a multi-platform image index, a single-platform image manifest or an attestation. */
  kind: GetARepositoryImageResponseImageKind;
  /** Operating system the manifest targets. Only present for single-platform manifests. */
  platform?: string;
  /** CPU architecture the manifest targets. Only present for single-platform manifests. */
  arch?: string;
  /** Identifier of the actor that pushed the image. */
  pushedBy?: string;
  /** Total size in bytes of the image's resources (manifest, config and layer blobs) stored by the registry. */
  sizeInBytes: number;
  /** Converted VHS drive data, present once an image has been optimized for sandbox launch. */
  vhs?: GetARepositoryImageResponseImageVhs;
  /** ISO 8601 timestamp of when the image was created. */
  createdAt: string;
}
export const GetARepositoryImageResponseImage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    layers: GetARepositoryImageResponseImageLayersList,
    status: S.NullOr(GetARepositoryImageResponseImageStatus),
    tags: GetARepositoryImageResponseImageTagsList,
    id: S.String,
    repositoryId: S.String,
    manifestDigest: S.String,
    kind: GetARepositoryImageResponseImageKind,
    platform: S.optional(S.String),
    arch: S.optional(S.String),
    pushedBy: S.optional(S.String),
    sizeInBytes: S.Number,
    vhs: S.optional(GetARepositoryImageResponseImageVhs),
    createdAt: S.String,
  }),
).annotate({
  identifier: "GetARepositoryImageResponseImage",
}) as any as S.Schema<GetARepositoryImageResponseImage>;

export interface GetARepositoryImageResponse {
  /** A single image with its tags, status and resolved Dockerfile layer history. */
  image: GetARepositoryImageResponseImage;
}
export const GetARepositoryImageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    image: GetARepositoryImageResponseImage,
  }),
).annotate({
  identifier: "GetARepositoryImageResponse",
}) as any as S.Schema<GetARepositoryImageResponse>;

export interface GetARepositoryTagRequest {
  idOrName: string;
  tag: string;
  projectId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const GetARepositoryTagRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    idOrName: S.String.pipe(T.Label()),
    tag: S.String.pipe(T.Label()),
    projectId: S.String.pipe(T.Query()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v1/vcr/repository/{idOrName}/tags/{tag}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetARepositoryTagRequest",
}) as any as S.Schema<GetARepositoryTagRequest>;

/** Whether the manifest is a multi-platform image index, a single-platform image manifest or an attestation. */
export type GetARepositoryTagResponseTagKind =
  | "attestation"
  | "index"
  | "manifest";
export const GetARepositoryTagResponseTagKind = /*@__PURE__*/ S.String;

/** VHS-readiness status, or `null` for a multi-platform index. */
export type GetARepositoryTagResponseTagStatus =
  | "preparing"
  | "ready"
  | "unoptimized";
export const GetARepositoryTagResponseTagStatus = /*@__PURE__*/ S.String;

/** A tag pointing at an image in a Vercel Container Registry repository, enriched with the backing image's metadata and VHS-readiness status. */
export interface GetARepositoryTagResponseTag {
  /** The tag name. */
  tag: string;
  /** SHA-256 digest of the image manifest the tag points at. */
  manifestDigest: string;
  /** Internal identifier of the image the tag points at. */
  imageId: string;
  /** Whether the manifest is a multi-platform image index, a single-platform image manifest or an attestation. */
  kind: GetARepositoryTagResponseTagKind;
  /** Operating system the manifest targets. Only present for single-platform manifests. */
  platform?: string;
  /** CPU architecture the manifest targets. Only present for single-platform manifests. */
  arch?: string;
  /** Identifier of the actor that pushed the image. */
  pushedBy?: string;
  /** VHS-readiness status, or `null` for a multi-platform index. */
  status: GetARepositoryTagResponseTagStatus | null;
  /** Total size in bytes of the image's resources (manifest, config and layer blobs) stored by the registry. */
  sizeInBytes: number;
  /** ISO 8601 timestamp of when the tag was created. */
  createdAt: string;
  /** ISO 8601 timestamp of when the tag was last updated. */
  updatedAt: string;
}
export const GetARepositoryTagResponseTag = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tag: S.String,
    manifestDigest: S.String,
    imageId: S.String,
    kind: GetARepositoryTagResponseTagKind,
    platform: S.optional(S.String),
    arch: S.optional(S.String),
    pushedBy: S.optional(S.String),
    status: S.NullOr(GetARepositoryTagResponseTagStatus),
    sizeInBytes: S.Number,
    createdAt: S.String,
    updatedAt: S.String,
  }),
).annotate({
  identifier: "GetARepositoryTagResponseTag",
}) as any as S.Schema<GetARepositoryTagResponseTag>;

export interface GetARepositoryTagResponse {
  /** A tag pointing at an image in a Vercel Container Registry repository, enriched with the backing image's metadata and VHS-readiness status. */
  tag: GetARepositoryTagResponseTag;
}
export const GetARepositoryTagResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tag: GetARepositoryTagResponseTag,
  }),
).annotate({
  identifier: "GetARepositoryTagResponse",
}) as any as S.Schema<GetARepositoryTagResponse>;

export interface GetBlobUploadStatusRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Blob upload session identifier. */
  uuid: string;
}
export const GetBlobUploadStatusRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    uuid: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/blobs/uploads/{uuid}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetBlobUploadStatusRequest",
}) as any as S.Schema<GetBlobUploadStatusRequest>;

export type GetBlobUploadStatusResponse = unknown;
export const GetBlobUploadStatusResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "GetBlobUploadStatusResponse",
}) as any as S.Schema<GetBlobUploadStatusResponse>;

export interface ListImageTagsRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  n?: number;
  /** Opaque pagination cursor returned by a previous list response. */
  last?: string;
}
export const ListImageTagsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    n: S.optional(S.Number.pipe(T.Query())),
    last: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/tags/list",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListImageTagsRequest",
}) as any as S.Schema<ListImageTagsRequest>;

export type ListImageTagsResponseTagsList = Array<string>;
export const ListImageTagsResponseTagsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListImageTagsResponseTagsList>;

export interface ListImageTagsResponse {
  name: string;
  tags: ListImageTagsResponseTagsList;
}
export const ListImageTagsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    tags: ListImageTagsResponseTagsList,
  }),
).annotate({
  identifier: "ListImageTagsResponse",
}) as any as S.Schema<ListImageTagsResponse>;

export interface ListRepositoriesRequest {
  projectId: string;
  limit?: number;
  /** Opaque pagination cursor returned by a previous list response. */
  cursor?: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const ListRepositoriesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    projectId: S.String.pipe(T.Query()),
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v1/vcr/repository", code: 200 })),
).annotate({
  identifier: "ListRepositoriesRequest",
}) as any as S.Schema<ListRepositoriesRequest>;

/** A Vercel Container Registry repository. */
export type ListRepositoriesResponseRepositoriesItem =
  CreateARepositoryResponseRepository;
export const ListRepositoriesResponseRepositoriesItem =
  CreateARepositoryResponseRepository;

export type ListRepositoriesResponseRepositoriesList =
  Array<CreateARepositoryResponseRepository>;
export const ListRepositoriesResponseRepositoriesList = /*@__PURE__*/ S.Array(
  CreateARepositoryResponseRepository,
) as any as S.Schema<ListRepositoriesResponseRepositoriesList>;

export interface ListRepositoriesResponse {
  repositories: ListRepositoriesResponseRepositoriesList;
  /** Cursor to fetch the next page of results, when more are available. */
  nextCursor?: string;
}
export const ListRepositoriesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    repositories: ListRepositoriesResponseRepositoriesList,
    nextCursor: S.optional(S.String),
  }),
).annotate({
  identifier: "ListRepositoriesResponse",
}) as any as S.Schema<ListRepositoriesResponse>;

export interface ListRepositoryImagesRequest {
  idOrName: string;
  projectId: string;
  limit?: number;
  /** Opaque pagination cursor returned by a previous list response. */
  cursor?: string;
  untagged?: boolean;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const ListRepositoryImagesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    idOrName: S.String.pipe(T.Label()),
    projectId: S.String.pipe(T.Query()),
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    untagged: S.optional(S.Boolean.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v1/vcr/repository/{idOrName}/images",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListRepositoryImagesRequest",
}) as any as S.Schema<ListRepositoryImagesRequest>;

/** VHS-readiness status, or `null` for a multi-platform index. */
export type ListRepositoryImagesResponseImagesItemStatus =
  | "preparing"
  | "ready"
  | "unoptimized";
export const ListRepositoryImagesResponseImagesItemStatus =
  /*@__PURE__*/ S.String;

/** Tags pointing at this image's manifest. */
export type ListRepositoryImagesResponseImagesItemTagsList = Array<unknown>;
export const ListRepositoryImagesResponseImagesItemTagsList =
  /*@__PURE__*/ S.Array(
    S.Unknown,
  ) as any as S.Schema<ListRepositoryImagesResponseImagesItemTagsList>;

/** Whether the manifest is a multi-platform image index, a single-platform image manifest or an attestation. */
export type ListRepositoryImagesResponseImagesItemKind =
  | "attestation"
  | "index"
  | "manifest";
export const ListRepositoryImagesResponseImagesItemKind =
  /*@__PURE__*/ S.String;

/** An image enriched with its tags and VHS-readiness status, as returned when listing a repository's images. */
export interface ListRepositoryImagesResponseImagesItem {
  /** VHS-readiness status, or `null` for a multi-platform index. */
  status: ListRepositoryImagesResponseImagesItemStatus | null;
  /** Tags pointing at this image's manifest. */
  tags: ListRepositoryImagesResponseImagesItemTagsList;
  /** Internal identifier of the image. */
  id: string;
  /** Identifier of the repository the image belongs to. */
  repositoryId: string;
  /** SHA-256 digest of the image manifest. */
  manifestDigest: string;
  /** Whether the manifest is a multi-platform image index, a single-platform image manifest or an attestation. */
  kind: ListRepositoryImagesResponseImagesItemKind;
  /** Operating system the manifest targets. Only present for single-platform manifests. */
  platform?: string;
  /** CPU architecture the manifest targets. Only present for single-platform manifests. */
  arch?: string;
  /** Identifier of the actor that pushed the image. */
  pushedBy?: string;
  /** Total size in bytes of the image's resources (manifest, config and layer blobs) stored by the registry. */
  sizeInBytes: number;
  /** Converted VHS drive data, present once an image has been optimized for sandbox launch. */
  vhs?: unknown;
  /** ISO 8601 timestamp of when the image was created. */
  createdAt: string;
}
export const ListRepositoryImagesResponseImagesItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      status: S.NullOr(ListRepositoryImagesResponseImagesItemStatus),
      tags: ListRepositoryImagesResponseImagesItemTagsList,
      id: S.String,
      repositoryId: S.String,
      manifestDigest: S.String,
      kind: ListRepositoryImagesResponseImagesItemKind,
      platform: S.optional(S.String),
      arch: S.optional(S.String),
      pushedBy: S.optional(S.String),
      sizeInBytes: S.Number,
      vhs: S.optional(S.Unknown),
      createdAt: S.String,
    }),
).annotate({
  identifier: "ListRepositoryImagesResponseImagesItem",
}) as any as S.Schema<ListRepositoryImagesResponseImagesItem>;

export type ListRepositoryImagesResponseImagesList =
  Array<ListRepositoryImagesResponseImagesItem>;
export const ListRepositoryImagesResponseImagesList = /*@__PURE__*/ S.Array(
  ListRepositoryImagesResponseImagesItem,
) as any as S.Schema<ListRepositoryImagesResponseImagesList>;

export interface ListRepositoryImagesResponse {
  images: ListRepositoryImagesResponseImagesList;
  /** Cursor to fetch the next page of results, when more are available. */
  nextCursor?: string;
}
export const ListRepositoryImagesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    images: ListRepositoryImagesResponseImagesList,
    nextCursor: S.optional(S.String),
  }),
).annotate({
  identifier: "ListRepositoryImagesResponse",
}) as any as S.Schema<ListRepositoryImagesResponse>;

export interface ListRepositoryPermissionsRequest {
  idOrName: string;
  projectId: string;
  limit?: number;
  /** Opaque pagination cursor returned by a previous list response. */
  cursor?: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const ListRepositoryPermissionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    idOrName: S.String.pipe(T.Label()),
    projectId: S.String.pipe(T.Query()),
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v1/vcr/repository/{idOrName}/permissions",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListRepositoryPermissionsRequest",
}) as any as S.Schema<ListRepositoryPermissionsRequest>;

/** A team's access grant to a Vercel Container Registry repository. */
export type ListRepositoryPermissionsResponsePermissionsItem =
  AddARepositoryPermissionResponsePermission;
export const ListRepositoryPermissionsResponsePermissionsItem =
  AddARepositoryPermissionResponsePermission;

export type ListRepositoryPermissionsResponsePermissionsList =
  Array<AddARepositoryPermissionResponsePermission>;
export const ListRepositoryPermissionsResponsePermissionsList =
  /*@__PURE__*/ S.Array(
    AddARepositoryPermissionResponsePermission,
  ) as any as S.Schema<ListRepositoryPermissionsResponsePermissionsList>;

export interface ListRepositoryPermissionsResponse {
  permissions: ListRepositoryPermissionsResponsePermissionsList;
  /** Cursor to fetch the next page of results, when more are available. */
  nextCursor?: string;
}
export const ListRepositoryPermissionsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    permissions: ListRepositoryPermissionsResponsePermissionsList,
    nextCursor: S.optional(S.String),
  }),
).annotate({
  identifier: "ListRepositoryPermissionsResponse",
}) as any as S.Schema<ListRepositoryPermissionsResponse>;

export type ListRepositoryTagsRequestSortBy = "updatedAt" | "tag";
export const ListRepositoryTagsRequestSortBy = /*@__PURE__*/ S.String;

export type ListRepositoryTagsRequestSortOrder = "asc" | "desc";
export const ListRepositoryTagsRequestSortOrder = /*@__PURE__*/ S.String;

export interface ListRepositoryTagsRequest {
  idOrName: string;
  projectId: string;
  limit?: number;
  cursor?: string;
  /** Field to sort the non-pinned tags by. */
  sortBy?: ListRepositoryTagsRequestSortBy | (string & {});
  /** Sort direction. Defaults to desc. */
  sortOrder?: ListRepositoryTagsRequestSortOrder | (string & {});
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
}
export const ListRepositoryTagsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    idOrName: S.String.pipe(T.Label()),
    projectId: S.String.pipe(T.Query()),
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    sortBy: S.optional(ListRepositoryTagsRequestSortBy.pipe(T.Query())),
    sortOrder: S.optional(ListRepositoryTagsRequestSortOrder.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v1/vcr/repository/{idOrName}/tags",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListRepositoryTagsRequest",
}) as any as S.Schema<ListRepositoryTagsRequest>;

export type ListRepositoryTagsResponseTagsItemKind =
  | "attestation"
  | "index"
  | "manifest";
export const ListRepositoryTagsResponseTagsItemKind = /*@__PURE__*/ S.String;

export type ListRepositoryTagsResponseTagsItemStatus =
  | "preparing"
  | "ready"
  | "unoptimized";
export const ListRepositoryTagsResponseTagsItemStatus = /*@__PURE__*/ S.String;

export interface ListRepositoryTagsResponseTagsItem {
  tag: string;
  manifestDigest: string;
  imageId: string;
  kind: ListRepositoryTagsResponseTagsItemKind;
  platform?: string;
  arch?: string;
  pushedBy?: string;
  status: ListRepositoryTagsResponseTagsItemStatus | null;
  sizeInBytes: number;
  createdAt: string;
  updatedAt: string;
}
export const ListRepositoryTagsResponseTagsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tag: S.String,
    manifestDigest: S.String,
    imageId: S.String,
    kind: ListRepositoryTagsResponseTagsItemKind,
    platform: S.optional(S.String),
    arch: S.optional(S.String),
    pushedBy: S.optional(S.String),
    status: S.NullOr(ListRepositoryTagsResponseTagsItemStatus),
    sizeInBytes: S.Number,
    createdAt: S.String,
    updatedAt: S.String,
  }),
).annotate({
  identifier: "ListRepositoryTagsResponseTagsItem",
}) as any as S.Schema<ListRepositoryTagsResponseTagsItem>;

export type ListRepositoryTagsResponseTagsList =
  Array<ListRepositoryTagsResponseTagsItem>;
export const ListRepositoryTagsResponseTagsList = /*@__PURE__*/ S.Array(
  ListRepositoryTagsResponseTagsItem,
) as any as S.Schema<ListRepositoryTagsResponseTagsList>;

export interface ListRepositoryTagsResponse {
  tags: ListRepositoryTagsResponseTagsList;
  nextCursor?: string;
}
export const ListRepositoryTagsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tags: ListRepositoryTagsResponseTagsList,
    nextCursor: S.optional(S.String),
  }),
).annotate({
  identifier: "ListRepositoryTagsResponse",
}) as any as S.Schema<ListRepositoryTagsResponse>;

export interface PullAnImageManifestRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Manifest reference: a tag or digest. */
  reference: string;
}
export const PullAnImageManifestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    reference: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/manifests/{reference}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PullAnImageManifestRequest",
}) as any as S.Schema<PullAnImageManifestRequest>;

export interface PullAnImageManifestResponse {}
export const PullAnImageManifestResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PullAnImageManifestResponse",
}) as any as S.Schema<PullAnImageManifestResponse>;

export interface PushAnImageManifestRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Manifest reference: a tag or digest. */
  reference: string;
}
export const PushAnImageManifestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    reference: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/manifests/{reference}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PushAnImageManifestRequest",
}) as any as S.Schema<PushAnImageManifestRequest>;

export type PushAnImageManifestResponse = unknown;
export const PushAnImageManifestResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "PushAnImageManifestResponse",
}) as any as S.Schema<PushAnImageManifestResponse>;

export interface RemoveARepositoryPermissionRequest {
  idOrName: string;
  projectId: string;
  /** The Team identifier to perform the request on behalf of. */
  teamId?: string;
  /** The Team slug to perform the request on behalf of. */
  slug?: string;
  /** Slug of a team that is granted access to a repository. */
  teamSlug?: string;
}
export const RemoveARepositoryPermissionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    idOrName: S.String.pipe(T.Label()),
    projectId: S.String.pipe(T.Query()),
    teamId: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    teamSlug: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v1/vcr/repository/{idOrName}/permissions",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RemoveARepositoryPermissionRequest",
}) as any as S.Schema<RemoveARepositoryPermissionRequest>;

export type RemoveARepositoryPermissionResponse = unknown;
export const RemoveARepositoryPermissionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "RemoveARepositoryPermissionResponse",
}) as any as S.Schema<RemoveARepositoryPermissionResponse>;

export interface StartABlobUploadRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Digest of the blob to mount from another repository. */
  mount?: string;
  /** Source repository to mount the blob from. */
  from?: string;
}
export const StartABlobUploadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    mount: S.optional(S.String.pipe(T.Query())),
    from: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/blobs/uploads/",
      code: 200,
    }),
  ),
).annotate({
  identifier: "StartABlobUploadRequest",
}) as any as S.Schema<StartABlobUploadRequest>;

export type StartABlobUploadResponse = unknown;
export const StartABlobUploadResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "StartABlobUploadResponse",
}) as any as S.Schema<StartABlobUploadResponse>;

export interface UploadABlobChunkRequest {
  /** Single Docker repository team slug component. */
  teamSlug: string;
  /** Single Docker repository project slug component. */
  projectSlug: string;
  /** Single Docker repository name component. */
  repositoryName: string;
  /** Blob upload session identifier. */
  uuid: string;
}
export const UploadABlobChunkRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    teamSlug: S.String.pipe(T.Label()),
    projectSlug: S.String.pipe(T.Label()),
    repositoryName: S.String.pipe(T.Label()),
    uuid: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/v2/{teamSlug}/{projectSlug}/{repositoryName}/blobs/uploads/{uuid}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UploadABlobChunkRequest",
}) as any as S.Schema<UploadABlobChunkRequest>;

export type UploadABlobChunkResponse = unknown;
export const UploadABlobChunkResponse = /*@__PURE__*/ S.suspend(() =>
  S.Unknown.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "UploadABlobChunkResponse",
}) as any as S.Schema<UploadABlobChunkResponse>;

export type AddARepositoryPermissionError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Add a repository permission Grant a team access to a VCR repository. Sharing applies to the whole repository. */
export const addARepositoryPermission: API.OperationMethod<
  AddARepositoryPermissionRequest,
  AddARepositoryPermissionResponse,
  AddARepositoryPermissionError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AddARepositoryPermissionRequest,
  output: AddARepositoryPermissionResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type CancelABlobUploadError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Cancel a blob upload DELETE /v2/:teamSlug/:projectSlug/:repositoryName/blobs/uploads/:uuid Cancel an in-flight blob upload. Aborts the underlying S3 multipart upload (if one was started) and discards the session. */
export const cancelABlobUpload: API.OperationMethod<
  CancelABlobUploadRequest,
  CancelABlobUploadResponse,
  CancelABlobUploadError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CancelABlobUploadRequest,
  output: CancelABlobUploadResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type CheckIfABlobExistsError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Check if a blob exists HEAD /v2/:teamSlug/:projectSlug/:repositoryName/blobs/:digest Check whether a blob exists. Used by the Docker client before pushing a layer to avoid re-uploading content that already exists. */
export const checkIfABlobExists: API.OperationMethod<
  CheckIfABlobExistsRequest,
  CheckIfABlobExistsResponse,
  CheckIfABlobExistsError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CheckIfABlobExistsRequest,
  output: CheckIfABlobExistsResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type CheckIfAManifestExistsError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Check if a manifest exists HEAD /v2/:teamSlug/:projectSlug/:repositoryName/manifests/:reference Check whether a manifest exists. Used by Docker client during push to determine if a manifest (or config blob referenced by digest) is already present. */
export const checkIfAManifestExists: API.OperationMethod<
  CheckIfAManifestExistsRequest,
  CheckIfAManifestExistsResponse,
  CheckIfAManifestExistsError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CheckIfAManifestExistsRequest,
  output: CheckIfAManifestExistsResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type CheckRegistryApiVersionSupportError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Check registry API version support GET /v2/ Docker Registry v2 version check. Returns a 401 challenge when no credentials are provided, prompting the Docker client to send auth. With valid credentials, returns 200 so the client can proceed. */
export const checkRegistryApiVersionSupport: API.OperationMethod<
  CheckRegistryApiVersionSupportRequest,
  CheckRegistryApiVersionSupportResponse,
  CheckRegistryApiVersionSupportError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CheckRegistryApiVersionSupportRequest,
  output: CheckRegistryApiVersionSupportResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ClearAllRepositoryPermissionsError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Clear all repository permissions Revoke every team's access to a VCR repository. Clearing an unshared repository is a no-op. */
export const clearAllRepositoryPermissions: API.OperationMethod<
  ClearAllRepositoryPermissionsRequest,
  ClearAllRepositoryPermissionsResponse,
  ClearAllRepositoryPermissionsError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ClearAllRepositoryPermissionsRequest,
  output: ClearAllRepositoryPermissionsResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type CompleteABlobUploadError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | PayloadTooLarge
  | VercelOpError;
/** Complete a blob upload PUT /v2/:teamSlug/:projectSlug/:repositoryName/blobs/uploads/:uuid?digest=<digest> Complete the blob upload. This may include a final chunk of data in the request body (monolithic upload) or just finalize a previous chunked upload. */
export const completeABlobUpload: API.OperationMethod<
  CompleteABlobUploadRequest,
  CompleteABlobUploadResponse,
  CompleteABlobUploadError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CompleteABlobUploadRequest,
  output: CompleteABlobUploadResponse,
  errors: [
    BadRequest,
    PaymentRequired,
    Forbidden,
    NotFound,
    Gone,
    PayloadTooLarge,
  ],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type CreateARepositoryError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Conflict
  | Gone
  | VercelOpError;
/** Create a repository Create a container registry repository for a project. */
export const createARepository: API.OperationMethod<
  CreateARepositoryRequest,
  CreateARepositoryResponse,
  CreateARepositoryError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateARepositoryRequest,
  output: CreateARepositoryResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Conflict, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type DeleteABlobError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | MethodNotAllowed
  | Gone
  | VercelOpError;
/** Delete a blob DELETE /v2/:teamSlug/:projectSlug/:repositoryName/blobs/:digest Blob deletion is intentionally not supported. Matches the behaviour of most public registries. */
export const deleteABlob: API.OperationMethod<
  DeleteABlobRequest,
  DeleteABlobResponse,
  DeleteABlobError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteABlobRequest,
  output: DeleteABlobResponse,
  errors: [
    BadRequest,
    PaymentRequired,
    Forbidden,
    NotFound,
    MethodNotAllowed,
    Gone,
  ],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type DeleteAnImageManifestError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Delete an image manifest DELETE /v2/:teamSlug/:projectSlug/:repositoryName/manifests/:reference Reference must be a digest. */
export const deleteAnImageManifest: API.OperationMethod<
  DeleteAnImageManifestRequest,
  DeleteAnImageManifestResponse,
  DeleteAnImageManifestError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteAnImageManifestRequest,
  output: DeleteAnImageManifestResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type DeleteARepositoryError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Delete a repository Schedule a repository for deletion. The repository is marked so it disappears from list/get immediately; subscriber-vcr reclaims every image (manifests, blobs, tags and rows) and finally deletes the repository row asynchronously via the VcrRepositoryRemoved event. */
export const deleteARepository: API.OperationMethod<
  DeleteARepositoryRequest,
  DeleteARepositoryResponse,
  DeleteARepositoryError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteARepositoryRequest,
  output: DeleteARepositoryResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type DeleteARepositoryImageError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Delete a repository image Schedule an image for deletion. The image is marked so it disappears from list/get immediately; subscriber-vcr reclaims the manifest, blobs, tags and row asynchronously via the VcrManifestRemoved event. */
export const deleteARepositoryImage: API.OperationMethod<
  DeleteARepositoryImageRequest,
  DeleteARepositoryImageResponse,
  DeleteARepositoryImageError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteARepositoryImageRequest,
  output: DeleteARepositoryImageResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type DownloadABlobError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | RangeNotSatisfiable
  | VercelOpError;
/** Download a blob GET /v2/:teamSlug/:projectSlug/:repositoryName/blobs/:digest Fetch a blob by digest. */
export const downloadABlob: API.OperationMethod<
  DownloadABlobRequest,
  DownloadABlobResponse,
  DownloadABlobError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DownloadABlobRequest,
  output: DownloadABlobResponse,
  errors: [
    BadRequest,
    PaymentRequired,
    Forbidden,
    NotFound,
    Gone,
    RangeNotSatisfiable,
  ],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type GetARepositoryError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Get a repository Fetch a container registry repository for a project by ID or name. */
export const getARepository: API.OperationMethod<
  GetARepositoryRequest,
  GetARepositoryResponse,
  GetARepositoryError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetARepositoryRequest,
  output: GetARepositoryResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type GetARepositoryImageError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Get a repository image Fetch an individual image from a repository, including its tags and Dockerfile history entries with discriminated layer details for UI rendering. The image may be addressed by its internal id (`image_...`) or by its manifest digest (`sha256:...`). */
export const getARepositoryImage: API.OperationMethod<
  GetARepositoryImageRequest,
  GetARepositoryImageResponse,
  GetARepositoryImageError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetARepositoryImageRequest,
  output: GetARepositoryImageResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type GetARepositoryTagError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Get a repository tag Fetch a single tag from a repository, including the backing image's metadata and VHS-readiness status. */
export const getARepositoryTag: API.OperationMethod<
  GetARepositoryTagRequest,
  GetARepositoryTagResponse,
  GetARepositoryTagError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetARepositoryTagRequest,
  output: GetARepositoryTagResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type GetBlobUploadStatusError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Get blob upload status GET /v2/:teamSlug/:projectSlug/:repositoryName/blobs/uploads/:uuid Query the status of an in-progress blob upload. Used by clients to resume a partial upload after an interruption. */
export const getBlobUploadStatus: API.OperationMethod<
  GetBlobUploadStatusRequest,
  GetBlobUploadStatusResponse,
  GetBlobUploadStatusError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetBlobUploadStatusRequest,
  output: GetBlobUploadStatusResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ListImageTagsError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** List image tags GET /v2/:teamSlug/:projectSlug/:repositoryName/tags/list List the tags in a repository. */
export const listImageTags: API.OperationMethod<
  ListImageTagsRequest,
  ListImageTagsResponse,
  ListImageTagsError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListImageTagsRequest,
  output: ListImageTagsResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ListRepositoriesError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** List repositories List container registry repositories for a project. */
export const listRepositories: API.OperationMethod<
  ListRepositoriesRequest,
  ListRepositoriesResponse,
  ListRepositoriesError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListRepositoriesRequest,
  output: ListRepositoriesResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ListRepositoryImagesError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** List repository images List images for a container registry repository, including their tags. */
export const listRepositoryImages: API.OperationMethod<
  ListRepositoryImagesRequest,
  ListRepositoryImagesResponse,
  ListRepositoryImagesError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListRepositoryImagesRequest,
  output: ListRepositoryImagesResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ListRepositoryPermissionsError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** List repository permissions List the teams a VCR repository is shared with. */
export const listRepositoryPermissions: API.OperationMethod<
  ListRepositoryPermissionsRequest,
  ListRepositoryPermissionsResponse,
  ListRepositoryPermissionsError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListRepositoryPermissionsRequest,
  output: ListRepositoryPermissionsResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type ListRepositoryTagsError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** List repository tags List a repository's tags. */
export const listRepositoryTags: API.OperationMethod<
  ListRepositoryTagsRequest,
  ListRepositoryTagsResponse,
  ListRepositoryTagsError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListRepositoryTagsRequest,
  output: ListRepositoryTagsResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type PullAnImageManifestError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Pull an image manifest GET /v2/:teamSlug/:projectSlug/:repositoryName/manifests/:reference Fetch a manifest by tag or digest. */
export const pullAnImageManifest: API.OperationMethod<
  PullAnImageManifestRequest,
  PullAnImageManifestResponse,
  PullAnImageManifestError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PullAnImageManifestRequest,
  output: PullAnImageManifestResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type PushAnImageManifestError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | PayloadTooLarge
  | VercelOpError;
/** Push an image manifest PUT /v2/:teamSlug/:projectSlug/:repositoryName/manifests/:reference Upload an image manifest. The digest is computed from the body and returned in the Docker-Content-Digest header. */
export const pushAnImageManifest: API.OperationMethod<
  PushAnImageManifestRequest,
  PushAnImageManifestResponse,
  PushAnImageManifestError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PushAnImageManifestRequest,
  output: PushAnImageManifestResponse,
  errors: [
    BadRequest,
    PaymentRequired,
    Forbidden,
    NotFound,
    Gone,
    PayloadTooLarge,
  ],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type RemoveARepositoryPermissionError =
  | BadRequest
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Remove a repository permission Revoke a team's access to a VCR repository. */
export const removeARepositoryPermission: API.OperationMethod<
  RemoveARepositoryPermissionRequest,
  RemoveARepositoryPermissionResponse,
  RemoveARepositoryPermissionError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RemoveARepositoryPermissionRequest,
  output: RemoveARepositoryPermissionResponse,
  errors: [BadRequest, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type StartABlobUploadError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | VercelOpError;
/** Start a blob upload POST /v2/:teamSlug/:projectSlug/:repositoryName/blobs/uploads/[?mount=<digest>&from=<repo>] Initiate a blob upload. Returns a UUID in the Location header that the client uses for subsequent PATCH (chunk) and PUT (complete) requests. */
export const startABlobUpload: API.OperationMethod<
  StartABlobUploadRequest,
  StartABlobUploadResponse,
  StartABlobUploadError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StartABlobUploadRequest,
  output: StartABlobUploadResponse,
  errors: [BadRequest, PaymentRequired, Forbidden, NotFound, Gone],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));

export type UploadABlobChunkError =
  | BadRequest
  | PaymentRequired
  | Forbidden
  | NotFound
  | Gone
  | PayloadTooLarge
  | VercelOpError;
/** Upload a blob chunk PATCH /v2/:teamSlug/:projectSlug/:repositoryName/blobs/uploads/:uuid Upload a chunk of blob data. The request body is streamed directly to S3 as a multipart upload part while hashing incrementally. The client may call this multiple times for chunked uploads. */
export const uploadABlobChunk: API.OperationMethod<
  UploadABlobChunkRequest,
  UploadABlobChunkResponse,
  UploadABlobChunkError,
  VercelOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UploadABlobChunkRequest,
  output: UploadABlobChunkResponse,
  errors: [
    BadRequest,
    PaymentRequired,
    Forbidden,
    NotFound,
    Gone,
    PayloadTooLarge,
  ],
  protocol: VercelProtocol,
  retry: Retry.Retry,
}));
