// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-unkey/specs/openapi.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  UnkeyProtocol,
  type UnkeyOpError,
  type UnkeyOpContext,
} from "../protocol.ts";
import { paginateCursor } from "../pagination.ts";
import { UnknownUnkeyError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { UnkeyOpError, UnkeyOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface AnalyticsGetGatewayRequestsRequest {
  /** The SQL query to run on your gateway request data. A query can use only the public alias `gateway_requests_v1`. The physical `default.*` table names are not permitted. Only SELECT queries are permitted. CTEs, subqueries, UNION, and EXCEPT are also permitted. Unkey limits each query to the workspace of the root key. To get the data for one project, app, or environment, add a filter on `project_id`, `app_id`, or `environment_id`. The workspace retention period and the workspace query limits apply. */
  query: string;
}
export const AnalyticsGetGatewayRequestsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    query: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/analytics.getGatewayRequests",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AnalyticsGetGatewayRequestsRequest",
}) as any as S.Schema<AnalyticsGetGatewayRequestsRequest>;

/** Metadata object included in every API response. This provides context about the request and is essential for debugging, audit trails, and support inquiries. The `requestId` is particularly important when troubleshooting issues with the Unkey support team. */
export interface Meta {
  /** A unique id for this request. Always include this ID when contacting support about a specific API request. This identifier allows Unkey's support team to trace the exact request through logs and diagnostic systems to provide faster assistance. */
  requestId: string;
}
export const Meta = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    requestId: S.String,
  }),
).annotate({ identifier: "Meta" }) as any as S.Schema<Meta>;

/** One result row. The query controls its fields. */
export type V2AnalyticsGetGatewayRequestsResponseDataItemMap = {
  [key: string]: unknown | undefined;
};
export const V2AnalyticsGetGatewayRequestsResponseDataItemMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<V2AnalyticsGetGatewayRequestsResponseDataItemMap>;

/** The gateway request rows that the query returned. The SELECT clause of the query controls the fields in each row. */
export type V2AnalyticsGetGatewayRequestsResponseData =
  Array<V2AnalyticsGetGatewayRequestsResponseDataItemMap>;
export const V2AnalyticsGetGatewayRequestsResponseData = /*@__PURE__*/ S.Array(
  V2AnalyticsGetGatewayRequestsResponseDataItemMap,
) as any as S.Schema<V2AnalyticsGetGatewayRequestsResponseData>;

export interface V2AnalyticsGetGatewayRequestsResponseBody {
  meta: Meta;
  data: V2AnalyticsGetGatewayRequestsResponseData;
}
export const V2AnalyticsGetGatewayRequestsResponseBody =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      meta: Meta,
      data: V2AnalyticsGetGatewayRequestsResponseData,
    }),
  ).annotate({
    identifier: "V2AnalyticsGetGatewayRequestsResponseBody",
  }) as any as S.Schema<V2AnalyticsGetGatewayRequestsResponseBody>;

export interface AnalyticsGetRatelimitsRequest {
  /** SQL query to execute against your rate limit analytics data. Queries may reference only the five public aliases: `ratelimits_v1`, `ratelimits_per_minute_v1`, `ratelimits_per_hour_v1`, `ratelimits_per_day_v1`, or `ratelimits_per_month_v1`. Physical `default.*` table names are unsupported. CTEs, subqueries, UNION, and EXCEPT are supported. Queries are always restricted to the authenticated workspace. Wildcard analytics permission can read every namespace in that workspace; namespace-scoped permissions automatically restrict results to the permitted namespace IDs. Workspace retention and query limits apply. */
  query: string;
}
export const AnalyticsGetRatelimitsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    query: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/analytics.getRatelimits", code: 200 }),
  ),
).annotate({
  identifier: "AnalyticsGetRatelimitsRequest",
}) as any as S.Schema<AnalyticsGetRatelimitsRequest>;

/** Dynamic row with fields determined by the query. */
export type V2AnalyticsGetRatelimitsResponseDataItemMap = {
  [key: string]: unknown | undefined;
};
export const V2AnalyticsGetRatelimitsResponseDataItemMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<V2AnalyticsGetRatelimitsResponseDataItemMap>;

/** Array of rate limit rows returned by the query. Fields vary based on the SQL SELECT clause. */
export type V2AnalyticsGetRatelimitsResponseData =
  Array<V2AnalyticsGetRatelimitsResponseDataItemMap>;
export const V2AnalyticsGetRatelimitsResponseData = /*@__PURE__*/ S.Array(
  V2AnalyticsGetRatelimitsResponseDataItemMap,
) as any as S.Schema<V2AnalyticsGetRatelimitsResponseData>;

export interface V2AnalyticsGetRatelimitsResponseBody {
  meta: Meta;
  data: V2AnalyticsGetRatelimitsResponseData;
}
export const V2AnalyticsGetRatelimitsResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: V2AnalyticsGetRatelimitsResponseData,
    }),
).annotate({
  identifier: "V2AnalyticsGetRatelimitsResponseBody",
}) as any as S.Schema<V2AnalyticsGetRatelimitsResponseBody>;

export interface AnalyticsGetRuntimeLogsRequest {
  /** The SQL query for your runtime log data. A query can use only the public alias `runtime_logs_v1`. The physical `default.*` table name is not permitted. CTEs, subqueries, UNION, and EXCEPT are permitted. Only SELECT queries are permitted. Unkey limits each query to the workspace of the root key. To get the logs of one project, app, environment, or deployment, add a filter on `project_id`, `app_id`, `environment_id`, or `deployment_id`. The workspace retention period and the workspace query limits apply. */
  query: string;
}
export const AnalyticsGetRuntimeLogsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    query: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/analytics.getRuntimeLogs", code: 200 }),
  ),
).annotate({
  identifier: "AnalyticsGetRuntimeLogsRequest",
}) as any as S.Schema<AnalyticsGetRuntimeLogsRequest>;

/** One result row. The query sets its fields. */
export type V2AnalyticsGetRuntimeLogsResponseDataItemMap = {
  [key: string]: unknown | undefined;
};
export const V2AnalyticsGetRuntimeLogsResponseDataItemMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<V2AnalyticsGetRuntimeLogsResponseDataItemMap>;

/** The runtime log rows that the query returned. The SELECT clause of the query sets the fields of each row. */
export type V2AnalyticsGetRuntimeLogsResponseData =
  Array<V2AnalyticsGetRuntimeLogsResponseDataItemMap>;
export const V2AnalyticsGetRuntimeLogsResponseData = /*@__PURE__*/ S.Array(
  V2AnalyticsGetRuntimeLogsResponseDataItemMap,
) as any as S.Schema<V2AnalyticsGetRuntimeLogsResponseData>;

export interface V2AnalyticsGetRuntimeLogsResponseBody {
  meta: Meta;
  data: V2AnalyticsGetRuntimeLogsResponseData;
}
export const V2AnalyticsGetRuntimeLogsResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: V2AnalyticsGetRuntimeLogsResponseData,
    }),
).annotate({
  identifier: "V2AnalyticsGetRuntimeLogsResponseBody",
}) as any as S.Schema<V2AnalyticsGetRuntimeLogsResponseBody>;

export interface AnalyticsGetVerificationsRequest {
  /** SQL query to execute against your analytics data. Queries may reference only the five public aliases: `key_verifications_v1`, `key_verifications_per_minute_v1`, `key_verifications_per_hour_v1`, `key_verifications_per_day_v1`, or `key_verifications_per_month_v1`. Physical `default.*` table names are unsupported. Only SELECT queries are allowed. CTEs, subqueries, UNION, and EXCEPT are supported. */
  query: string;
}
export const AnalyticsGetVerificationsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    query: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/analytics.getVerifications",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AnalyticsGetVerificationsRequest",
}) as any as S.Schema<AnalyticsGetVerificationsRequest>;

/** Dynamic row with fields determined by the query. Can include any combination of fields like time, outcome, count, key_id, etc. */
export type V2AnalyticsGetVerificationsResponseDataItemMap = {
  [key: string]: unknown | undefined;
};
export const V2AnalyticsGetVerificationsResponseDataItemMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<V2AnalyticsGetVerificationsResponseDataItemMap>;

/** Array of verification rows returned by the query. Fields vary based on the SQL SELECT clause. */
export type V2AnalyticsGetVerificationsResponseData =
  Array<V2AnalyticsGetVerificationsResponseDataItemMap>;
export const V2AnalyticsGetVerificationsResponseData = /*@__PURE__*/ S.Array(
  V2AnalyticsGetVerificationsResponseDataItemMap,
) as any as S.Schema<V2AnalyticsGetVerificationsResponseData>;

export interface V2AnalyticsGetVerificationsResponseBody {
  meta: Meta;
  data: V2AnalyticsGetVerificationsResponseData;
}
export const V2AnalyticsGetVerificationsResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: V2AnalyticsGetVerificationsResponseData,
    }),
).annotate({
  identifier: "V2AnalyticsGetVerificationsResponseBody",
}) as any as S.Schema<V2AnalyticsGetVerificationsResponseBody>;

export interface ApisCreateApiRequest {
  /** Human-readable name for this API within your workspace. Use descriptive names like 'Payment Service (prod)' or 'user-api-dev' to clearly identify purpose and environment. */
  name: string;
}
export const ApisCreateApiRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/v2/apis.createApi", code: 200 })),
).annotate({
  identifier: "ApisCreateApiRequest",
}) as any as S.Schema<ApisCreateApiRequest>;

export interface V2ApisCreateApiResponseData {
  /** The unique identifier assigned to the newly created API. Use this ID for all subsequent operations including key creation, verification, and API management. Always begins with 'api_' followed by a unique alphanumeric sequence. Store this ID securely as it's required when: - Creating API keys within this namespace - Verifying keys associated with this API - Managing API settings and metadata - Listing keys belonging to this API This identifier is permanent and cannot be changed after creation. */
  apiId: string;
}
export const V2ApisCreateApiResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiId: S.String,
  }),
).annotate({
  identifier: "V2ApisCreateApiResponseData",
}) as any as S.Schema<V2ApisCreateApiResponseData>;

export interface V2ApisCreateApiResponseBody {
  meta: Meta;
  data: V2ApisCreateApiResponseData;
}
export const V2ApisCreateApiResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2ApisCreateApiResponseData,
  }),
).annotate({
  identifier: "V2ApisCreateApiResponseBody",
}) as any as S.Schema<V2ApisCreateApiResponseBody>;

export interface ApisDeleteApiRequest {
  /** Specifies which API namespace to permanently delete from your workspace. Must be a valid API ID that begins with 'api_' and exists within your workspace. Before proceeding, ensure you have the correct API ID and understand that this action cannot be undone. If you need to migrate functionality, create replacement keys in a new API namespace and update client applications before deletion. */
  apiId: string;
}
export const ApisDeleteApiRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiId: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/v2/apis.deleteApi", code: 200 })),
).annotate({
  identifier: "ApisDeleteApiRequest",
}) as any as S.Schema<ApisDeleteApiRequest>;

export interface V2ApisDeleteApiResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2ApisDeleteApiResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: S.Unknown,
  }),
).annotate({
  identifier: "V2ApisDeleteApiResponseBody",
}) as any as S.Schema<V2ApisDeleteApiResponseBody>;

export interface ApisGetApiRequest {
  /** Specifies which API to retrieve by its unique identifier. Must be a valid API ID that begins with 'api_' and exists within your workspace. */
  apiId: string;
}
export const ApisGetApiRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiId: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/v2/apis.getApi", code: 200 })),
).annotate({
  identifier: "ApisGetApiRequest",
}) as any as S.Schema<ApisGetApiRequest>;

export interface V2ApisGetApiResponseData {
  /** The unique identifier of this API within Unkey's system. Used in all operations related to this API including key creation, verification, and management. Always begins with 'api_' followed by alphanumeric characters and underscores. This identifier is permanent and never changes after API creation. */
  id: string;
  /** The internal name of this API as specified during creation. Used for organization and identification within your workspace. Helps distinguish between different environments, services, or access tiers. Not visible to end users - this is purely for administrative purposes. */
  name: string;
}
export const V2ApisGetApiResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
  }),
).annotate({
  identifier: "V2ApisGetApiResponseData",
}) as any as S.Schema<V2ApisGetApiResponseData>;

export interface V2ApisGetApiResponseBody {
  meta: Meta;
  data: V2ApisGetApiResponseData;
}
export const V2ApisGetApiResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2ApisGetApiResponseData,
  }),
).annotate({
  identifier: "V2ApisGetApiResponseBody",
}) as any as S.Schema<V2ApisGetApiResponseBody>;

export interface ApisListKeysRequest {
  /** The API namespace whose keys you want to list. Returns all keys in this API, subject to pagination and filters. */
  apiId: string;
  /** Maximum number of keys to return per request. Balance between response size and number of pagination calls needed. */
  limit?: number;
  /** Pagination cursor from previous response to fetch next page. Use when `hasMore: true` in previous response. */
  cursor?: string;
  /** Filter keys by external ID to find keys for a specific user or entity. Must exactly match the externalId set during key creation. */
  externalId?: string;
  /** When true, attempts to include the plaintext key value in the response. SECURITY WARNING: - This requires special permissions on the calling root key - Only works for keys created with 'recoverable: true' - Exposes sensitive key material in the response - Should only be used in secure administrative contexts - Never enable this in user-facing applications */
  decrypt?: boolean;
  /** EXPERIMENTAL: Skip the cache and fetch the keys directly from the database. This ensures you see the most recent state, including keys created moments ago. Use this when: - You've just created a key and need to display it immediately - You need absolute certainty about the current key state - You're debugging cache consistency issues This parameter comes with a performance cost and should be used sparingly. */
  revalidateKeysCache?: boolean;
}
export const ApisListKeysRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiId: S.String,
    limit: S.optional(S.Number),
    cursor: S.optional(S.String),
    externalId: S.optional(S.String),
    decrypt: S.optional(S.Boolean),
    revalidateKeysCache: S.optional(S.Boolean),
  }).pipe(T.Http({ method: "POST", uri: "/v2/apis.listKeys", code: 200 })),
).annotate({
  identifier: "ApisListKeysRequest",
}) as any as S.Schema<ApisListKeysRequest>;

/** Custom metadata associated with this key. */
export type KeyResponseDataMetaMap = { [key: string]: unknown | undefined };
export const KeyResponseDataMetaMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<KeyResponseDataMetaMap>;

export type KeyResponseDataPermissionsList = Array<string>;
export const KeyResponseDataPermissionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeyResponseDataPermissionsList>;

export type KeyResponseDataRolesList = Array<string>;
export const KeyResponseDataRolesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeyResponseDataRolesList>;

/** How often credits are automatically refilled. */
export type KeyCreditsRefillInterval = "daily" | "monthly";
export const KeyCreditsRefillInterval = /*@__PURE__*/ S.String;

/** Configuration for automatic credit refill behavior. */
export interface KeyCreditsRefill {
  /** How often credits are automatically refilled. */
  interval: KeyCreditsRefillInterval | (string & {});
  /** Number of credits to add during each refill cycle. */
  amount: number;
  /** Day of the month for monthly refills (1-31). Only required when interval is 'monthly'. For days beyond the month's length, refill occurs on the last day of the month. */
  refillDay?: number;
}
export const KeyCreditsRefill = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    interval: KeyCreditsRefillInterval,
    amount: S.Number,
    refillDay: S.optional(S.Number),
  }),
).annotate({
  identifier: "KeyCreditsRefill",
}) as any as S.Schema<KeyCreditsRefill>;

/** Credit configuration and remaining balance for this key. */
export interface KeyCreditsData {
  /** Number of credits remaining (null for unlimited). */
  remaining: number | null;
  refill?: KeyCreditsRefill;
}
export const KeyCreditsData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    remaining: S.NullOr(S.Number),
    refill: S.optional(KeyCreditsRefill),
  }),
).annotate({ identifier: "KeyCreditsData" }) as any as S.Schema<KeyCreditsData>;

/** Identity metadata */
export type IdentityMetaMap = { [key: string]: unknown | undefined };
export const IdentityMetaMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<IdentityMetaMap>;

export interface RatelimitResponse {
  /** Unique identifier for this rate limit configuration. */
  id: string;
  /** Human-readable name for this rate limit. */
  name: string;
  /** Maximum requests allowed within the time window. */
  limit: number;
  /** Rate limit window duration in milliseconds. */
  duration: number;
  /** Whether this rate limit was automatically applied when verifying the key. */
  autoApply: boolean;
}
export const RatelimitResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    limit: S.Number,
    duration: S.Number,
    autoApply: S.Boolean,
  }),
).annotate({
  identifier: "RatelimitResponse",
}) as any as S.Schema<RatelimitResponse>;

/** Identity ratelimits */
export type IdentityRatelimitsList = Array<RatelimitResponse>;
export const IdentityRatelimitsList = /*@__PURE__*/ S.Array(
  RatelimitResponse,
) as any as S.Schema<IdentityRatelimitsList>;

export interface Identity {
  /** Identity ID */
  id: string;
  /** External identity ID */
  externalId: string;
  /** Identity metadata */
  meta?: IdentityMetaMap;
  /** Identity ratelimits */
  ratelimits?: IdentityRatelimitsList;
}
export const Identity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    externalId: S.String,
    meta: S.optional(IdentityMetaMap),
    ratelimits: S.optional(IdentityRatelimitsList),
  }),
).annotate({ identifier: "Identity" }) as any as S.Schema<Identity>;

export type KeyResponseDataRatelimitsList = Array<RatelimitResponse>;
export const KeyResponseDataRatelimitsList = /*@__PURE__*/ S.Array(
  RatelimitResponse,
) as any as S.Schema<KeyResponseDataRatelimitsList>;

export interface KeyResponseData {
  /** Unique identifier for this key. */
  keyId: string;
  /** First few characters of the key for identification. */
  start: string;
  /** Whether the key is enabled or disabled. */
  enabled: boolean;
  /** Human-readable name for this key. */
  name?: string;
  /** Custom metadata associated with this key. */
  meta?: KeyResponseDataMetaMap;
  /** Unix timestamp in milliseconds when key was created. */
  createdAt: number;
  /** Unix timestamp in milliseconds when key was last updated. */
  updatedAt?: number;
  /** Unix timestamp in milliseconds when key was last used for verification. This is an approximated value, accurate to within 5 minutes. */
  lastUsedAt?: number;
  /** Unix timestamp in milliseconds when key expires (if set). */
  expires?: number;
  permissions?: KeyResponseDataPermissionsList;
  roles?: KeyResponseDataRolesList;
  credits?: KeyCreditsData;
  identity?: Identity;
  /** Decrypted key value (only when decrypt=true). */
  plaintext?: string | Redacted.Redacted<string>;
  ratelimits?: KeyResponseDataRatelimitsList;
}
export const KeyResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    start: S.String,
    enabled: S.Boolean,
    name: S.optional(S.String),
    meta: S.optional(KeyResponseDataMetaMap),
    createdAt: S.Number,
    updatedAt: S.optional(S.Number),
    lastUsedAt: S.optional(S.Number),
    expires: S.optional(S.Number),
    permissions: S.optional(KeyResponseDataPermissionsList),
    roles: S.optional(KeyResponseDataRolesList),
    credits: S.optional(KeyCreditsData),
    identity: S.optional(Identity),
    plaintext: S.optional(S.String.pipe(T.SensitiveValue({}))),
    ratelimits: S.optional(KeyResponseDataRatelimitsList),
  }),
).annotate({
  identifier: "KeyResponseData",
}) as any as S.Schema<KeyResponseData>;

/** Array of API keys with complete configuration and metadata. */
export type V2ApisListKeysResponseData = Array<KeyResponseData>;
export const V2ApisListKeysResponseData = /*@__PURE__*/ S.Array(
  KeyResponseData,
) as any as S.Schema<V2ApisListKeysResponseData>;

/** Pagination metadata for list endpoints. Provides information necessary to traverse through large result sets efficiently using cursor-based pagination. */
export interface Pagination {
  /** Opaque pagination token for retrieving the next page of results. Include this exact value in the cursor field of subsequent requests. Cursors are temporary and may expire after extended periods. */
  cursor?: string;
  /** Indicates whether additional results exist beyond this page. When true, use the cursor to fetch the next page. When false, you have reached the end of the result set. */
  hasMore: boolean;
}
export const Pagination = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cursor: S.optional(S.String),
    hasMore: S.Boolean,
  }),
).annotate({ identifier: "Pagination" }) as any as S.Schema<Pagination>;

export interface V2ApisListKeysResponseBody {
  meta: Meta;
  data: V2ApisListKeysResponseData;
  pagination: Pagination;
}
export const V2ApisListKeysResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2ApisListKeysResponseData,
    pagination: Pagination,
  }),
).annotate({
  identifier: "V2ApisListKeysResponseBody",
}) as any as S.Schema<V2ApisListKeysResponseBody>;

/** Connect a GitHub repository to the app on creation. Omit to create the app without a repository and connect one later with apps.updateApp. */
export interface AppGitCreateInput {
  /** The GitHub repository to connect, as "owner/repo". The workspace must have the Unkey GitHub App installed with access to it. */
  repository: string;
  /** The branch this app's deployments track. Omit to adopt the repository's default branch on GitHub. */
  defaultBranch?: string;
}
export const AppGitCreateInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    repository: S.String,
    defaultBranch: S.optional(S.String),
  }),
).annotate({
  identifier: "AppGitCreateInput",
}) as any as S.Schema<AppGitCreateInput>;

export interface AppsCreateAppRequest {
  project: string;
  /** Human-readable name for this app. Use a descriptive name like 'Payments API' to identify its purpose. */
  name: string;
  slug: string;
  git?: AppGitCreateInput;
}
export const AppsCreateAppRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    name: S.String,
    slug: S.String,
    git: S.optional(AppGitCreateInput),
  }).pipe(T.Http({ method: "POST", uri: "/v2/apps.createApp", code: 200 })),
).annotate({
  identifier: "AppsCreateAppRequest",
}) as any as S.Schema<AppsCreateAppRequest>;

export interface V2AppsCreateAppResponseData {
  /** The unique identifier of the newly created app, generated by Unkey. Always begins with 'app_' followed by a unique alphanumeric sequence. */
  appId: string;
}
export const V2AppsCreateAppResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    appId: S.String,
  }),
).annotate({
  identifier: "V2AppsCreateAppResponseData",
}) as any as S.Schema<V2AppsCreateAppResponseData>;

export interface V2AppsCreateAppResponseBody {
  meta: Meta;
  data: V2AppsCreateAppResponseData;
}
export const V2AppsCreateAppResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2AppsCreateAppResponseData,
  }),
).annotate({
  identifier: "V2AppsCreateAppResponseBody",
}) as any as S.Schema<V2AppsCreateAppResponseBody>;

export interface AppsDeleteAppRequest {
  project: string;
  app: string;
}
export const AppsDeleteAppRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/v2/apps.deleteApp", code: 200 })),
).annotate({
  identifier: "AppsDeleteAppRequest",
}) as any as S.Schema<AppsDeleteAppRequest>;

export interface AppsDeleteAppResponse {}
export const AppsDeleteAppResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "AppsDeleteAppResponse",
}) as any as S.Schema<AppsDeleteAppResponse>;

export interface AppsGetAppRequest {
  project: string;
  app: string;
}
export const AppsGetAppRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/v2/apps.getApp", code: 200 })),
).annotate({
  identifier: "AppsGetAppRequest",
}) as any as S.Schema<AppsGetAppRequest>;

export interface AppGit {
  /** The connected GitHub repository, as "owner/repo". */
  repository: string;
  /** The branch this app's deployments track. */
  defaultBranch?: string;
}
export const AppGit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    repository: S.String,
    defaultBranch: S.optional(S.String),
  }),
).annotate({ identifier: "AppGit" }) as any as S.Schema<AppGit>;

export interface App {
  /** The unique identifier of the app, generated by Unkey. */
  id: string;
  /** Human-readable name for this app. */
  name: string;
  /** URL-safe handle for this app, unique within its project. Chosen at creation time. */
  slug: string;
  /** The connected GitHub repository and the branch its deployments track. Omitted when the app has no repository connected (for example a Docker-based app). */
  git?: AppGit;
  /** The identifier of the deployment currently serving this app. Omitted if the app has no active deployment yet. */
  currentDeploymentId?: string;
  /** Whether the app is currently serving a rolled-back deployment rather than its latest one. */
  isRolledBack: boolean;
  /** Whether delete protection is enabled for this app. When true, the app cannot be deleted until protection is disabled. */
  deleteProtection: boolean;
  /** Unix timestamp in milliseconds when the app was created. */
  createdAt: number;
  /** Unix timestamp in milliseconds when the app was last updated. Omitted if the app has never been updated. */
  updatedAt?: number;
}
export const App = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    slug: S.String,
    git: S.optional(AppGit),
    currentDeploymentId: S.optional(S.String),
    isRolledBack: S.Boolean,
    deleteProtection: S.Boolean,
    createdAt: S.Number,
    updatedAt: S.optional(S.Number),
  }),
).annotate({ identifier: "App" }) as any as S.Schema<App>;

export interface V2AppsGetAppResponseBody {
  meta: Meta;
  data: App;
}
export const V2AppsGetAppResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: App,
  }),
).annotate({
  identifier: "V2AppsGetAppResponseBody",
}) as any as S.Schema<V2AppsGetAppResponseBody>;

export interface AppsListAppsRequest {
  project: string;
  /** Maximum number of apps to return per request. Balance between response size and number of pagination calls needed. */
  limit?: number;
  /** Pagination cursor from a previous response to fetch the next page. Use when `hasMore: true` in the previous response. */
  cursor?: string;
  /** Free-form text to filter apps. Returns apps whose ID, name, or slug contains the search string. Matching is case-insensitive. */
  search?: string;
}
export const AppsListAppsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    limit: S.optional(S.Number),
    cursor: S.optional(S.String),
    search: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/v2/apps.listApps", code: 200 })),
).annotate({
  identifier: "AppsListAppsRequest",
}) as any as S.Schema<AppsListAppsRequest>;

/** Array of apps in the project, ordered by app id. */
export type V2AppsListAppsResponseBodyDataList = Array<App>;
export const V2AppsListAppsResponseBodyDataList = /*@__PURE__*/ S.Array(
  App,
) as any as S.Schema<V2AppsListAppsResponseBodyDataList>;

export interface V2AppsListAppsResponseBody {
  meta: Meta;
  /** Array of apps in the project, ordered by app id. */
  data: V2AppsListAppsResponseBodyDataList;
  pagination: Pagination;
}
export const V2AppsListAppsResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2AppsListAppsResponseBodyDataList,
    pagination: Pagination,
  }),
).annotate({
  identifier: "V2AppsListAppsResponseBody",
}) as any as S.Schema<V2AppsListAppsResponseBody>;

export interface AppGitUpdateInput {
  /** The GitHub repository to connect, as "owner/repo". Omit to change only the tracked branch of the already-connected repository. The workspace must have the Unkey GitHub App installed with access to it. */
  repository?: string;
  /** The branch this app's deployments track. Omit to adopt the repository's GitHub default branch on a new connection, keep the current branch when replacing an already-connected repository, or set it alone to retarget the branch of the connected repository. */
  defaultBranch?: string;
}
export const AppGitUpdateInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    repository: S.optional(S.String),
    defaultBranch: S.optional(S.String),
  }),
).annotate({
  identifier: "AppGitUpdateInput",
}) as any as S.Schema<AppGitUpdateInput>;

export interface AppsUpdateAppRequest {
  project: string;
  app: string;
  /** New human-readable name for the app. Omit this field to leave the current name unchanged. */
  name?: string;
  slug?: string;
  /** Connect, reconfigure, or disconnect this app's GitHub repository. Omit to leave unchanged, set null to disconnect, or set an object with a "repository" to connect or replace it and/or a "defaultBranch" to set which branch it tracks. Fields are independent, so send only the one you change. */
  git?: AppGitUpdateInput | null;
  /** Enable or disable delete protection for the app. Omit this field to leave the current setting unchanged. */
  deleteProtection?: boolean;
}
export const AppsUpdateAppRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
    name: S.optional(S.String),
    slug: S.optional(S.String),
    git: S.optional(S.NullOr(AppGitUpdateInput)),
    deleteProtection: S.optional(S.Boolean),
  }).pipe(T.Http({ method: "POST", uri: "/v2/apps.updateApp", code: 200 })),
).annotate({
  identifier: "AppsUpdateAppRequest",
}) as any as S.Schema<AppsUpdateAppRequest>;

export interface V2AppsUpdateAppResponseBody {
  meta: Meta;
  data: App;
}
export const V2AppsUpdateAppResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: App,
  }),
).annotate({
  identifier: "V2AppsUpdateAppResponseBody",
}) as any as S.Schema<V2AppsUpdateAppResponseBody>;

/** Build from the app's connected GitHub repository. */
export interface DeploymentSourceGit {
  /** Branch to build (its HEAD). Omit branch and commitSha to use the app's default branch. */
  branch?: string;
  /** Commit to build (full or abbreviated SHA). Takes precedence over branch. */
  commitSha?: string;
  /** Build from a fork instead of the app's connected repository, as "owner/repo". Requires commitSha. */
  repository?: string;
}
export const DeploymentSourceGit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    branch: S.optional(S.String),
    commitSha: S.optional(S.String),
    repository: S.optional(S.String),
  }),
).annotate({
  identifier: "DeploymentSourceGit",
}) as any as S.Schema<DeploymentSourceGit>;

/** Deploy a prebuilt Docker image as-is. */
export interface DeploymentSourceImage {
  /** Docker image to deploy as-is. */
  dockerImage: string;
}
export const DeploymentSourceImage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    dockerImage: S.String,
  }),
).annotate({
  identifier: "DeploymentSourceImage",
}) as any as S.Schema<DeploymentSourceImage>;

/** Re-run an existing deployment. */
export interface DeploymentSourceDeployment {
  deploymentId: string;
}
export const DeploymentSourceDeployment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deploymentId: S.String,
  }),
).annotate({
  identifier: "DeploymentSourceDeployment",
}) as any as S.Schema<DeploymentSourceDeployment>;

export interface DeploymentsCreateDeploymentRequest {
  project: string;
  app: string;
  environment: string;
  git?: DeploymentSourceGit;
  image?: DeploymentSourceImage;
  deployment?: DeploymentSourceDeployment;
}
export const DeploymentsCreateDeploymentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
    environment: S.String,
    git: S.optional(DeploymentSourceGit),
    image: S.optional(DeploymentSourceImage),
    deployment: S.optional(DeploymentSourceDeployment),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/deployments.createDeployment",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeploymentsCreateDeploymentRequest",
}) as any as S.Schema<DeploymentsCreateDeploymentRequest>;

export interface V2DeploymentsCreateDeploymentResponseData {
  /** Unique deployment identifier. Poll deployments.getDeployment with this id to watch status. */
  deploymentId: string;
}
export const V2DeploymentsCreateDeploymentResponseData =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      deploymentId: S.String,
    }),
  ).annotate({
    identifier: "V2DeploymentsCreateDeploymentResponseData",
  }) as any as S.Schema<V2DeploymentsCreateDeploymentResponseData>;

export interface V2DeploymentsCreateDeploymentResponseBody {
  meta: Meta;
  data: V2DeploymentsCreateDeploymentResponseData;
}
export const V2DeploymentsCreateDeploymentResponseBody =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      meta: Meta,
      data: V2DeploymentsCreateDeploymentResponseData,
    }),
  ).annotate({
    identifier: "V2DeploymentsCreateDeploymentResponseBody",
  }) as any as S.Schema<V2DeploymentsCreateDeploymentResponseBody>;

export interface DeploymentsGetDeploymentRequest {
  deploymentId: string;
}
export const DeploymentsGetDeploymentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deploymentId: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/deployments.getDeployment", code: 200 }),
  ),
).annotate({
  identifier: "DeploymentsGetDeploymentRequest",
}) as any as S.Schema<DeploymentsGetDeploymentRequest>;

/** Current lifecycle status of the deployment. Poll until it reaches a terminal state: ready (serving), failed, skipped, superseded, stopped, or cancelled. */
export type DeploymentStatus =
  | "pending"
  | "starting"
  | "building"
  | "deploying"
  | "network"
  | "finalizing"
  | "ready"
  | "failed"
  | "skipped"
  | "awaiting_approval"
  | "stopped"
  | "superseded"
  | "cancelled";
export const DeploymentStatus = /*@__PURE__*/ S.String;

export interface DeploymentGit {
  /** The git commit SHA this deployment was built from. */
  commitSha: string;
  /** The git branch this deployment was built from. Omitted when unknown. */
  branch?: string;
}
export const DeploymentGit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    commitSha: S.String,
    branch: S.optional(S.String),
  }),
).annotate({ identifier: "DeploymentGit" }) as any as S.Schema<DeploymentGit>;

export interface DeploymentDocker {
  /** The Docker image this deployment runs. */
  image: string;
}
export const DeploymentDocker = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    image: S.String,
  }),
).annotate({
  identifier: "DeploymentDocker",
}) as any as S.Schema<DeploymentDocker>;

/** A lifecycle operation that can be performed on the deployment in its current state, given its status, environment, and whether it is the current deployment. */
export type DeploymentAction = "promote" | "rollback" | "stop" | "start";
export const DeploymentAction = /*@__PURE__*/ S.String;

/** Lifecycle operations you are allowed to call on this deployment right now. Empty when none apply (e.g. while building or in a terminal state). */
export type DeploymentAvailableActionsList = Array<DeploymentAction>;
export const DeploymentAvailableActionsList = /*@__PURE__*/ S.Array(
  DeploymentAction,
) as any as S.Schema<DeploymentAvailableActionsList>;

/** Regions this deployment is configured to run in. Empty while the deployment has no scheduled regions yet. */
export type DeploymentRegionsList = Array<string>;
export const DeploymentRegionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<DeploymentRegionsList>;

/** The reason a deployment failed. `unknown` means Unkey could not classify the failure; see `message` for details. */
export type DeploymentErrorCode =
  | "no_schedulable_regions"
  | "invalid_runtime_settings"
  | "cpu_quota_exceeded"
  | "memory_quota_exceeded"
  | "storage_quota_exceeded"
  | "build_failed"
  | "unknown";
export const DeploymentErrorCode = /*@__PURE__*/ S.String;

export interface DeploymentError {
  code: DeploymentErrorCode;
  /** The pipeline step that failed (e.g. `building`, `deploying`, `starting`). */
  step: string;
  /** Human-readable description of why the deployment failed. For programmatic handling, use `code`. */
  message: string;
}
export const DeploymentError = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    code: DeploymentErrorCode,
    step: S.String,
    message: S.String,
  }),
).annotate({
  identifier: "DeploymentError",
}) as any as S.Schema<DeploymentError>;

/** Public hostnames this deployment is reachable at. */
export type DeploymentDomainsList = Array<string>;
export const DeploymentDomainsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<DeploymentDomainsList>;

/** Container entrypoint command override. Empty when none is set. */
export type DeploymentRuntimeCommandList = Array<string>;
export const DeploymentRuntimeCommandList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<DeploymentRuntimeCommandList>;

/** Signal sent to the container on shutdown. */
export type EnvironmentShutdownSignal =
  | "SIGTERM"
  | "SIGINT"
  | "SIGQUIT"
  | "SIGKILL";
export const EnvironmentShutdownSignal = /*@__PURE__*/ S.String;

/** Protocol used to reach the container. */
export type EnvironmentUpstreamProtocol = "http1" | "h2c";
export const EnvironmentUpstreamProtocol = /*@__PURE__*/ S.String;

/** HTTP method used to probe the container. */
export type EnvironmentHealthcheckMethod = "GET" | "POST";
export const EnvironmentHealthcheckMethod = /*@__PURE__*/ S.String;

export interface EnvironmentHealthcheck {
  /** HTTP method used to probe the container. */
  method: EnvironmentHealthcheckMethod | (string & {});
  /** HTTP path probed on the container. Must start with a slash. */
  path: string;
  /** How often the probe runs, in seconds. Defaults to 10 when omitted. */
  intervalSeconds?: number;
  /** Per-probe timeout, in seconds. Defaults to 5 when omitted. */
  timeoutSeconds?: number;
  /** Consecutive failures before the container is restarted. Defaults to 3 when omitted. */
  failureThreshold?: number;
  /** Delay before the first probe runs, in seconds. Defaults to 0 when omitted. */
  initialDelaySeconds?: number;
}
export const EnvironmentHealthcheck = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    method: EnvironmentHealthcheckMethod,
    path: S.String,
    intervalSeconds: S.optional(S.Number),
    timeoutSeconds: S.optional(S.Number),
    failureThreshold: S.optional(S.Number),
    initialDelaySeconds: S.optional(S.Number),
  }),
).annotate({
  identifier: "EnvironmentHealthcheck",
}) as any as S.Schema<EnvironmentHealthcheck>;

export interface DeploymentRuntime {
  /** CPU allocation in vCPUs (1 = one vCPU, 0.5 = half a vCPU). */
  vCpus: number;
  /** Memory allocation in mebibytes. */
  memoryMib: number;
  /** Ephemeral storage allocation in mebibytes. */
  storageMib: number;
  /** Port the container listens on. */
  port: number;
  /** Container entrypoint command override. Empty when none is set. */
  command: DeploymentRuntimeCommandList;
  shutdownSignal: EnvironmentShutdownSignal;
  upstreamProtocol: EnvironmentUpstreamProtocol;
  /** HTTP health probe configuration. Omitted when no healthcheck is configured. */
  healthcheck?: EnvironmentHealthcheck;
}
export const DeploymentRuntime = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vCpus: S.Number,
    memoryMib: S.Number,
    storageMib: S.Number,
    port: S.Number,
    command: DeploymentRuntimeCommandList,
    shutdownSignal: EnvironmentShutdownSignal,
    upstreamProtocol: EnvironmentUpstreamProtocol,
    healthcheck: S.optional(EnvironmentHealthcheck),
  }),
).annotate({
  identifier: "DeploymentRuntime",
}) as any as S.Schema<DeploymentRuntime>;

export interface Deployment {
  /** The unique identifier of the deployment, generated by Unkey. */
  id: string;
  status: DeploymentStatus;
  /** True when this is the production deployment currently serving traffic, i.e. on api.acme.com. Only production deployments can be current, and at most one deployment is current. Rollbacks and promotions change which deployment is current and serves requests to api.acme.com. */
  isCurrent: boolean;
  /** Slug of the environment this deployment belongs to. */
  environment: string;
  /** Slug of the app this deployment belongs to. */
  app: string;
  /** Slug of the project this deployment belongs to. */
  project: string;
  /** Present for git-sourced deployments. Mutually exclusive with `docker`. */
  git?: DeploymentGit;
  /** Present for image-sourced deployments. Mutually exclusive with `git`. */
  docker?: DeploymentDocker;
  /** Lifecycle operations you are allowed to call on this deployment right now. Empty when none apply (e.g. while building or in a terminal state). */
  availableActions: DeploymentAvailableActionsList;
  /** Regions this deployment is configured to run in. Empty while the deployment has no scheduled regions yet. */
  regions: DeploymentRegionsList;
  /** Why the deployment failed. Present only when `status` is `failed`. */
  error?: DeploymentError;
  /** Public hostnames this deployment is reachable at. */
  domains?: DeploymentDomainsList;
  runtime: DeploymentRuntime;
  /** Unix timestamp in milliseconds when the deployment was created. */
  createdAt: number;
  /** Unix timestamp in milliseconds when the deployment was last updated. Omitted if the deployment has never been updated. */
  updatedAt?: number;
}
export const Deployment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    status: DeploymentStatus,
    isCurrent: S.Boolean,
    environment: S.String,
    app: S.String,
    project: S.String,
    git: S.optional(DeploymentGit),
    docker: S.optional(DeploymentDocker),
    availableActions: DeploymentAvailableActionsList,
    regions: DeploymentRegionsList,
    error: S.optional(DeploymentError),
    domains: S.optional(DeploymentDomainsList),
    runtime: DeploymentRuntime,
    createdAt: S.Number,
    updatedAt: S.optional(S.Number),
  }),
).annotate({ identifier: "Deployment" }) as any as S.Schema<Deployment>;

export interface V2DeploymentsGetDeploymentResponseBody {
  meta: Meta;
  data: Deployment;
}
export const V2DeploymentsGetDeploymentResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: Deployment,
    }),
).annotate({
  identifier: "V2DeploymentsGetDeploymentResponseBody",
}) as any as S.Schema<V2DeploymentsGetDeploymentResponseBody>;

/** Restrict results to deployments in any of the given lifecycle statuses. Omit to return deployments in every status. */
export type DeploymentsListDeploymentsRequestStatusList = Array<
  DeploymentStatus | (string & {})
>;
export const DeploymentsListDeploymentsRequestStatusList =
  /*@__PURE__*/ S.Array(
    DeploymentStatus,
  ) as any as S.Schema<DeploymentsListDeploymentsRequestStatusList>;

export interface DeploymentsListDeploymentsRequest {
  /** Restrict results to a single project, identified by its ID or slug. Required when filtering by `app` or `environment`. */
  project?: string;
  /** Restrict results to a single app, identified by its ID or slug. Requires `project` to also be set. */
  app?: string;
  /** Restrict results to a single environment, identified by its ID or slug. Requires `project` and `app` to also be set. */
  environment?: string;
  /** Restrict results to deployments in any of the given lifecycle statuses. Omit to return deployments in every status. */
  status?: DeploymentsListDeploymentsRequestStatusList;
  /** Maximum number of deployments to return per request. Balance between response size and number of pagination calls needed. */
  limit?: number;
  /** Pagination cursor from a previous response to fetch the next page. Use when `hasMore: true` in the previous response. */
  cursor?: string;
}
export const DeploymentsListDeploymentsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.optional(S.String),
    app: S.optional(S.String),
    environment: S.optional(S.String),
    status: S.optional(DeploymentsListDeploymentsRequestStatusList),
    limit: S.optional(S.Number),
    cursor: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/deployments.listDeployments",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeploymentsListDeploymentsRequest",
}) as any as S.Schema<DeploymentsListDeploymentsRequest>;

/** Array of deployments, ordered newest first. */
export type V2DeploymentsListDeploymentsResponseBodyDataList =
  Array<Deployment>;
export const V2DeploymentsListDeploymentsResponseBodyDataList =
  /*@__PURE__*/ S.Array(
    Deployment,
  ) as any as S.Schema<V2DeploymentsListDeploymentsResponseBodyDataList>;

export interface V2DeploymentsListDeploymentsResponseBody {
  meta: Meta;
  /** Array of deployments, ordered newest first. */
  data: V2DeploymentsListDeploymentsResponseBodyDataList;
  pagination: Pagination;
}
export const V2DeploymentsListDeploymentsResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: V2DeploymentsListDeploymentsResponseBodyDataList,
      pagination: Pagination,
    }),
).annotate({
  identifier: "V2DeploymentsListDeploymentsResponseBody",
}) as any as S.Schema<V2DeploymentsListDeploymentsResponseBody>;

export interface DeploymentsPromoteDeploymentRequest {
  deploymentId: string;
}
export const DeploymentsPromoteDeploymentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deploymentId: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/deployments.promoteDeployment",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeploymentsPromoteDeploymentRequest",
}) as any as S.Schema<DeploymentsPromoteDeploymentRequest>;

export interface DeploymentsPromoteDeploymentResponse {}
export const DeploymentsPromoteDeploymentResponse = /*@__PURE__*/ S.suspend(
  () => S.Struct({}),
).annotate({
  identifier: "DeploymentsPromoteDeploymentResponse",
}) as any as S.Schema<DeploymentsPromoteDeploymentResponse>;

export interface DeploymentsRollbackDeploymentRequest {
  /** The deployment to roll back TO. Its app's current deployment is used as the rollback source automatically. */
  deploymentId: string;
}
export const DeploymentsRollbackDeploymentRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      deploymentId: S.String,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/v2/deployments.rollbackDeployment",
        code: 200,
      }),
    ),
).annotate({
  identifier: "DeploymentsRollbackDeploymentRequest",
}) as any as S.Schema<DeploymentsRollbackDeploymentRequest>;

export interface DeploymentsRollbackDeploymentResponse {}
export const DeploymentsRollbackDeploymentResponse = /*@__PURE__*/ S.suspend(
  () => S.Struct({}),
).annotate({
  identifier: "DeploymentsRollbackDeploymentResponse",
}) as any as S.Schema<DeploymentsRollbackDeploymentResponse>;

export interface DeploymentsStartDeploymentRequest {
  /** The stopped deployment to start. */
  deploymentId: string;
}
export const DeploymentsStartDeploymentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deploymentId: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/deployments.startDeployment",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeploymentsStartDeploymentRequest",
}) as any as S.Schema<DeploymentsStartDeploymentRequest>;

export interface DeploymentsStartDeploymentResponse {}
export const DeploymentsStartDeploymentResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeploymentsStartDeploymentResponse",
}) as any as S.Schema<DeploymentsStartDeploymentResponse>;

export interface DeploymentsStopDeploymentRequest {
  deploymentId: string;
}
export const DeploymentsStopDeploymentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    deploymentId: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/deployments.stopDeployment",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeploymentsStopDeploymentRequest",
}) as any as S.Schema<DeploymentsStopDeploymentRequest>;

export interface DeploymentsStopDeploymentResponse {}
export const DeploymentsStopDeploymentResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeploymentsStopDeploymentResponse",
}) as any as S.Schema<DeploymentsStopDeploymentResponse>;

export interface DomainsCreateDomainRequest {
  project: string;
  app: string;
  environment: string;
  /** Fully qualified domain name to attach to the environment, without a scheme, port, or path. Must be unique across your entire workspace: the same name cannot be attached to two environments. The name must sit under a registrable domain: 'api.acme.co.uk' is accepted, the public suffix 'co.uk' itself is not. Internationalized names may be sent in Unicode or Punycode form; either way the domain is stored and returned in its canonical form, lowercase ASCII with Unicode labels Punycode encoded, and the DNS records in the response use that form. */
  domain: string;
}
export const DomainsCreateDomainRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
    environment: S.String,
    domain: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/domains.createDomain", code: 200 }),
  ),
).annotate({
  identifier: "DomainsCreateDomainRequest",
}) as any as S.Schema<DomainsCreateDomainRequest>;

/** Record type to create. `ALIAS` is not a real DNS record type: it means an apex-compatible alias, which providers expose as ALIAS, ANAME, or a flattened CNAME. Apex domains cannot hold a plain CNAME, so they receive `ALIAS` where a subdomain receives `CNAME`. */
export type DnsRecordType = "CNAME" | "ALIAS" | "TXT";
export const DnsRecordType = /*@__PURE__*/ S.String;

export interface DnsRecord {
  /** Record type to create. `ALIAS` is not a real DNS record type: it means an apex-compatible alias, which providers expose as ALIAS, ANAME, or a flattened CNAME. Apex domains cannot hold a plain CNAME, so they receive `ALIAS` where a subdomain receives `CNAME`. */
  type: DnsRecordType;
  /** Fully qualified name of the record, ready to use as-is. Some providers want a name relative to the zone instead. Drop the zone and its trailing dot: in zone `acme.com`, `api.acme.com` becomes `api` and `_unkey.api.acme.com` becomes `_unkey.api`. A name equal to the zone itself is usually entered as `@`. */
  name: string;
  /** The value to set on the record, exactly as given, including any prefix. Do not trim or reformat it: verification compares the published record against this string. Use the lowest TTL your provider allows until the domain is verified. Verification polls DNS, so a long TTL keeps a stale value cached and can burn the verification window on a value you have already corrected. Raise it afterwards if you want. */
  value: string;
  /** Seconds a resolver may cache this record. Set it in your provider alongside the record's name and value. */
  ttl: number;
  /** Whether Unkey has read this record back with the expected value. Use it to see which records are still outstanding. False does not always mean the record is missing. A provider that does not expose the published value to a DNS lookup, such as a proxied or flattened routing record, leaves this false for as long as it serves traffic; such a domain verifies through its TXT record instead. Always false on a domain no check has run against yet. */
  verified: boolean;
  /** What this record is for and any provider-specific caveat that applies to it. Worth surfacing to whoever edits the DNS zone. Treat it as optional: it carries no data the record itself needs, so a future record type may omit it. */
  note?: string;
}
export const DnsRecord = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: DnsRecordType,
    name: S.String,
    value: S.String,
    ttl: S.Number,
    verified: S.Boolean,
    note: S.optional(S.String),
  }),
).annotate({ identifier: "DnsRecord" }) as any as S.Schema<DnsRecord>;

/** Every DNS record needed to finish setting up this domain, ready to create at your provider. The list already accounts for whether the domain is an apex or a subdomain, so no further branching is needed: create each entry as given. One record establishes routing and one proves ownership. Create all of them: whether ownership can be inferred from the routing record depends on how your provider publishes it, and a name another workspace has already verified can only be claimed through the ownership record. Neither is knowable before the records exist. */
export type V2DomainsCreateDomainResponseDataDnsRecordsList = Array<DnsRecord>;
export const V2DomainsCreateDomainResponseDataDnsRecordsList =
  /*@__PURE__*/ S.Array(
    DnsRecord,
  ) as any as S.Schema<V2DomainsCreateDomainResponseDataDnsRecordsList>;

/** One-click setup at the domain's DNS provider. Omitted entirely when the provider does not support Domain Connect or discovery failed, so the object's presence is the signal that the shortcut is available and both of its fields are filled. */
export interface DomainConnect {
  /** Display name of the DNS provider the domain is delegated to, such as 'Cloudflare'. Discovered from the domain's nameservers, so it reflects where DNS is actually hosted rather than where the domain was registered. */
  provider: string;
  /** Signed Domain Connect URL that pre-fills the records in `dnsRecords` at the provider. Open it in a browser and the domain owner approves them in one step instead of entering them by hand. The URL is signed with an Unkey key, so it cannot be constructed or altered by the caller. Intended for a browser, not a script: after approval the provider sends the browser to this workspace's app settings page in the Unkey dashboard, so it suits a caller who administers this workspace. Anyone who does not have access to it approves the records successfully but lands on a page they cannot open. Approving is what writes the records; verification then proceeds on its own, so nothing depends on completing that return trip. */
  url: string;
}
export const DomainConnect = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    provider: S.String,
    url: S.String,
  }),
).annotate({ identifier: "DomainConnect" }) as any as S.Schema<DomainConnect>;

export interface V2DomainsCreateDomainResponseData {
  domainId: string;
  /** Every DNS record needed to finish setting up this domain, ready to create at your provider. The list already accounts for whether the domain is an apex or a subdomain, so no further branching is needed: create each entry as given. One record establishes routing and one proves ownership. Create all of them: whether ownership can be inferred from the routing record depends on how your provider publishes it, and a name another workspace has already verified can only be claimed through the ownership record. Neither is knowable before the records exist. */
  dnsRecords: V2DomainsCreateDomainResponseDataDnsRecordsList;
  domainConnect?: DomainConnect;
}
export const V2DomainsCreateDomainResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    domainId: S.String,
    dnsRecords: V2DomainsCreateDomainResponseDataDnsRecordsList,
    domainConnect: S.optional(DomainConnect),
  }),
).annotate({
  identifier: "V2DomainsCreateDomainResponseData",
}) as any as S.Schema<V2DomainsCreateDomainResponseData>;

export interface V2DomainsCreateDomainResponseBody {
  meta: Meta;
  data: V2DomainsCreateDomainResponseData;
}
export const V2DomainsCreateDomainResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2DomainsCreateDomainResponseData,
  }),
).annotate({
  identifier: "V2DomainsCreateDomainResponseBody",
}) as any as S.Schema<V2DomainsCreateDomainResponseBody>;

export interface DomainsDeleteDomainRequest {
  /** Identifies a domain by its Unkey ID or by its name. Pass a 'dom_'-prefixed ID, or a fully qualified domain name such as 'api.acme.com' without a scheme, port, or path. You can give an internationalized name in Unicode or Punycode form. Both forms address the same domain. Domain names are unique per workspace, so the name alone addresses the domain. You do not need to supply a project, app, or environment. */
  domain: string;
}
export const DomainsDeleteDomainRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    domain: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/domains.deleteDomain", code: 200 }),
  ),
).annotate({
  identifier: "DomainsDeleteDomainRequest",
}) as any as S.Schema<DomainsDeleteDomainRequest>;

export interface V2DomainsDeleteDomainResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2DomainsDeleteDomainResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: S.Unknown,
  }),
).annotate({
  identifier: "V2DomainsDeleteDomainResponseBody",
}) as any as S.Schema<V2DomainsDeleteDomainResponseBody>;

export interface DomainsGetDomainRequest {
  /** Identifies a domain by its Unkey ID or by its name. Pass a 'dom_'-prefixed ID, or a fully qualified domain name such as 'api.acme.com' without a scheme, port, or path. You can give an internationalized name in Unicode or Punycode form. Both forms address the same domain. Domain names are unique per workspace, so the name alone addresses the domain. You do not need to supply a project, app, or environment. */
  domain: string;
}
export const DomainsGetDomainRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    domain: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/v2/domains.getDomain", code: 200 })),
).annotate({
  identifier: "DomainsGetDomainRequest",
}) as any as S.Schema<DomainsGetDomainRequest>;

/** The verification status of the domain. - `pending`: the domain is created. No DNS check has completed yet. - `verifying`: Unkey checks the DNS records approximately each minute. - `verified`: the domain is verified. Unkey has configured routing and requested a certificate. - `failed`: the required DNS records did not appear within 24 hours. Fix the records, then retry verification. */
export type DomainStatus = "pending" | "verifying" | "verified" | "failed";
export const DomainStatus = /*@__PURE__*/ S.String;

/** The DNS records this domain needs. Create each record at your DNS provider. Each record has a `verified` flag. The flag shows whether Unkey has read that record back, so it tells you which records are still missing. */
export type DomainDnsRecordsList = Array<DnsRecord>;
export const DomainDnsRecordsList = /*@__PURE__*/ S.Array(
  DnsRecord,
) as any as S.Schema<DomainDnsRecordsList>;

export interface Domain {
  id: string;
  /** Fully qualified domain name attached to the environment. */
  domain: string;
  /** The project the domain's environment belongs to. */
  projectId: string;
  /** The app the domain's environment belongs to. */
  appId: string;
  /** The environment this domain serves. Traffic to the domain reaches whatever is currently deployed to this environment. */
  environmentId: string;
  /** The verification status of the domain. - `pending`: the domain is created. No DNS check has completed yet. - `verifying`: Unkey checks the DNS records approximately each minute. - `verified`: the domain is verified. Unkey has configured routing and requested a certificate. - `failed`: the required DNS records did not appear within 24 hours. Fix the records, then retry verification. */
  status: DomainStatus;
  /** Why the most recent verification attempt did not succeed, in plain language. Omitted while verification is progressing normally. */
  verificationError?: string;
  /** The DNS records this domain needs. Create each record at your DNS provider. Each record has a `verified` flag. The flag shows whether Unkey has read that record back, so it tells you which records are still missing. */
  dnsRecords: DomainDnsRecordsList;
  domainConnect?: DomainConnect;
  /** Unix timestamp in milliseconds when the domain was created. The 24 hour verification window runs from here. */
  createdAt: number;
  /** Unix timestamp in milliseconds of the last change to this domain. Omitted if it has never changed. */
  updatedAt?: number;
}
export const Domain = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    domain: S.String,
    projectId: S.String,
    appId: S.String,
    environmentId: S.String,
    status: DomainStatus,
    verificationError: S.optional(S.String),
    dnsRecords: DomainDnsRecordsList,
    domainConnect: S.optional(DomainConnect),
    createdAt: S.Number,
    updatedAt: S.optional(S.Number),
  }),
).annotate({ identifier: "Domain" }) as any as S.Schema<Domain>;

export interface V2DomainsGetDomainResponseBody {
  meta: Meta;
  data: Domain;
}
export const V2DomainsGetDomainResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: Domain,
  }),
).annotate({
  identifier: "V2DomainsGetDomainResponseBody",
}) as any as S.Schema<V2DomainsGetDomainResponseBody>;

export interface DomainsListDomainsRequest {
  project: string;
  app: string;
  environment: string;
  /** The maximum number of domains one response contains. A small limit makes the response smaller, but makes more requests necessary. */
  limit?: number;
  /** The pagination cursor from the response that came before. Send it to get the next page when that response has `hasMore: true`. */
  cursor?: string;
  /** Free-form text to filter domains. Returns domains whose ID or name contains the search string. Matching is case-insensitive. */
  search?: string;
}
export const DomainsListDomainsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
    environment: S.String,
    limit: S.optional(S.Number),
    cursor: S.optional(S.String),
    search: S.optional(S.String),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/domains.listDomains", code: 200 }),
  ),
).annotate({
  identifier: "DomainsListDomainsRequest",
}) as any as S.Schema<DomainsListDomainsRequest>;

/** The domains attached to the environment, sorted by their id. The array is empty when the environment has no domains. This is not an error. */
export type V2DomainsListDomainsResponseBodyDataList = Array<Domain>;
export const V2DomainsListDomainsResponseBodyDataList = /*@__PURE__*/ S.Array(
  Domain,
) as any as S.Schema<V2DomainsListDomainsResponseBodyDataList>;

export interface V2DomainsListDomainsResponseBody {
  meta: Meta;
  /** The domains attached to the environment, sorted by their id. The array is empty when the environment has no domains. This is not an error. */
  data: V2DomainsListDomainsResponseBodyDataList;
  pagination: Pagination;
}
export const V2DomainsListDomainsResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2DomainsListDomainsResponseBodyDataList,
    pagination: Pagination,
  }),
).annotate({
  identifier: "V2DomainsListDomainsResponseBody",
}) as any as S.Schema<V2DomainsListDomainsResponseBody>;

export interface DomainsVerifyDomainRequest {
  /** Identifies a domain by its name or by its ID. Send the fully qualified domain name, such as 'api.acme.com', without a scheme, port, or path, or send the domain ID that domains.createDomain returns. You can send an internationalized name in Unicode or in Punycode form. Both forms address the same domain. */
  domain: string;
}
export const DomainsVerifyDomainRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    domain: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/domains.verifyDomain", code: 200 }),
  ),
).annotate({
  identifier: "DomainsVerifyDomainRequest",
}) as any as S.Schema<DomainsVerifyDomainRequest>;

export interface DomainsVerifyDomainResponse {}
export const DomainsVerifyDomainResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DomainsVerifyDomainResponse",
}) as any as S.Schema<DomainsVerifyDomainResponse>;

export interface EnvironmentsGetEnvironmentRequest {
  project: string;
  app: string;
  environment: string;
}
export const EnvironmentsGetEnvironmentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
    environment: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/environments.getEnvironment",
      code: 200,
    }),
  ),
).annotate({
  identifier: "EnvironmentsGetEnvironmentRequest",
}) as any as S.Schema<EnvironmentsGetEnvironmentRequest>;

/** The deployment lifecycle role of an environment. - `production`: Deployments serve production traffic, support promotion and rollback, and cannot be stopped. - `preview`: Deployments can be stopped and started and are eligible for preview lifecycle automation. */
export type EnvironmentKind = "production" | "preview";
export const EnvironmentKind = /*@__PURE__*/ S.String;

/** Container entrypoint command override. */
export type EnvironmentRuntimeCommandList = Array<string>;
export const EnvironmentRuntimeCommandList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<EnvironmentRuntimeCommandList>;

/** Runtime settings that control how the container runs. Omitted until the environment has runtime settings. */
export interface EnvironmentRuntime {
  /** Port the container listens on. */
  port: number;
  /** CPU allocation in vCPUs (1 = one vCPU, 0.5 = half a vCPU). */
  vCpus: number;
  /** Memory allocation in mebibytes. */
  memoryMib: number;
  /** Ephemeral storage allocation in mebibytes. */
  storageMib: number;
  /** Container entrypoint command override. */
  command: EnvironmentRuntimeCommandList;
  healthcheck?: EnvironmentHealthcheck;
  shutdownSignal: EnvironmentShutdownSignal;
  upstreamProtocol: EnvironmentUpstreamProtocol;
  /** Path to the OpenAPI spec served by the container, if any. */
  openapiSpecPath?: string;
}
export const EnvironmentRuntime = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    port: S.Number,
    vCpus: S.Number,
    memoryMib: S.Number,
    storageMib: S.Number,
    command: EnvironmentRuntimeCommandList,
    healthcheck: S.optional(EnvironmentHealthcheck),
    shutdownSignal: EnvironmentShutdownSignal,
    upstreamProtocol: EnvironmentUpstreamProtocol,
    openapiSpecPath: S.optional(S.String),
  }),
).annotate({
  identifier: "EnvironmentRuntime",
}) as any as S.Schema<EnvironmentRuntime>;

/** Paths that trigger a rebuild when changed. */
export type EnvironmentBuildWatchPathsList = Array<string>;
export const EnvironmentBuildWatchPathsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<EnvironmentBuildWatchPathsList>;

/** Build settings that control how the app is built. Omitted until the environment has build settings. */
export interface EnvironmentBuild {
  /** Path to the Dockerfile used to build the app, if any. */
  dockerfile?: string;
  /** The directory the app is built from. "." for the repository root. */
  rootDirectory: string;
  /** Overrides the build command auto-detected by Railpack, so monorepos can scope the build to a single app. Omitted when left to auto-detection or for Dockerfile builds. */
  buildCommand?: string;
  /** Paths that trigger a rebuild when changed. */
  watchPaths: EnvironmentBuildWatchPathsList;
  /** Whether pushes automatically trigger a deployment. */
  autoDeploy: boolean;
}
export const EnvironmentBuild = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    dockerfile: S.optional(S.String),
    rootDirectory: S.String,
    buildCommand: S.optional(S.String),
    watchPaths: EnvironmentBuildWatchPathsList,
    autoDeploy: S.Boolean,
  }),
).annotate({
  identifier: "EnvironmentBuild",
}) as any as S.Schema<EnvironmentBuild>;

/** Min and max replica bounds for autoscaling in a region. */
export interface Replicas {
  /** Minimum number of replicas. */
  min: number;
  /** Maximum number of replicas. */
  max: number;
}
export const Replicas = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    min: S.Number,
    max: S.Number,
  }),
).annotate({ identifier: "Replicas" }) as any as S.Schema<Replicas>;

/** Replica bounds for a single region the environment deploys to. */
export interface EnvironmentRegion {
  /** Region name, such as us-east-1. */
  name: string;
  replicas: Replicas;
}
export const EnvironmentRegion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    replicas: Replicas,
  }),
).annotate({
  identifier: "EnvironmentRegion",
}) as any as S.Schema<EnvironmentRegion>;

/** Per-region deployment settings for this environment. Empty until regional settings are configured. */
export type EnvironmentRegionsList = Array<EnvironmentRegion>;
export const EnvironmentRegionsList = /*@__PURE__*/ S.Array(
  EnvironmentRegion,
) as any as S.Schema<EnvironmentRegionsList>;

export interface Environment {
  /** The unique identifier of the environment, generated by Unkey. */
  id: string;
  /** Human-readable slug of the environment, unique within its app. */
  slug: string;
  /** Human-readable description of this environment. Empty string if none was provided. */
  description: string;
  /** How deployments in this environment participate in the deployment lifecycle. */
  kind: EnvironmentKind;
  /** Whether delete protection is enabled for this environment. When true, the environment cannot be deleted until protection is disabled. */
  deleteProtection: boolean;
  /** Unix timestamp in milliseconds when the environment was created. */
  createdAt: number;
  /** Unix timestamp in milliseconds when the environment was last updated. Omitted if the environment has never been updated. */
  updatedAt?: number;
  runtime?: EnvironmentRuntime;
  build?: EnvironmentBuild;
  /** Per-region deployment settings for this environment. Empty until regional settings are configured. */
  regions?: EnvironmentRegionsList;
}
export const Environment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    slug: S.String,
    description: S.String,
    kind: EnvironmentKind,
    deleteProtection: S.Boolean,
    createdAt: S.Number,
    updatedAt: S.optional(S.Number),
    runtime: S.optional(EnvironmentRuntime),
    build: S.optional(EnvironmentBuild),
    regions: S.optional(EnvironmentRegionsList),
  }),
).annotate({ identifier: "Environment" }) as any as S.Schema<Environment>;

export interface V2EnvironmentsGetEnvironmentResponseBody {
  meta: Meta;
  data: Environment;
}
export const V2EnvironmentsGetEnvironmentResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: Environment,
    }),
).annotate({
  identifier: "V2EnvironmentsGetEnvironmentResponseBody",
}) as any as S.Schema<V2EnvironmentsGetEnvironmentResponseBody>;

export interface EnvironmentsListEnvironmentsRequest {
  project: string;
  app: string;
}
export const EnvironmentsListEnvironmentsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/environments.listEnvironments",
      code: 200,
    }),
  ),
).annotate({
  identifier: "EnvironmentsListEnvironmentsRequest",
}) as any as S.Schema<EnvironmentsListEnvironmentsRequest>;

/** Array of environments in the app, ordered by environment id. */
export type V2EnvironmentsListEnvironmentsResponseBodyDataList =
  Array<Environment>;
export const V2EnvironmentsListEnvironmentsResponseBodyDataList =
  /*@__PURE__*/ S.Array(
    Environment,
  ) as any as S.Schema<V2EnvironmentsListEnvironmentsResponseBodyDataList>;

export interface V2EnvironmentsListEnvironmentsResponseBody {
  meta: Meta;
  /** Array of environments in the app, ordered by environment id. */
  data: V2EnvironmentsListEnvironmentsResponseBodyDataList;
}
export const V2EnvironmentsListEnvironmentsResponseBody =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      meta: Meta,
      data: V2EnvironmentsListEnvironmentsResponseBodyDataList,
    }),
  ).annotate({
    identifier: "V2EnvironmentsListEnvironmentsResponseBody",
  }) as any as S.Schema<V2EnvironmentsListEnvironmentsResponseBody>;

export interface EnvironmentsListEnvironmentVariablesRequest {
  project: string;
  app: string;
  environment: string;
  /** Maximum number of variables to return per request. */
  limit?: number;
  /** Pagination cursor from a previous response to fetch the next page. Use when `hasMore: true` in the previous response. */
  cursor?: string;
}
export const EnvironmentsListEnvironmentVariablesRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project: S.String,
      app: S.String,
      environment: S.String,
      limit: S.optional(S.Number),
      cursor: S.optional(S.String),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/v2/environments.listEnvironmentVariables",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "EnvironmentsListEnvironmentVariablesRequest",
  }) as any as S.Schema<EnvironmentsListEnvironmentVariablesRequest>;

/** How the value may be read back. `writeonly` values can never be read back through the API; `recoverable` values can be decrypted. Values are encrypted at rest either way. */
export type EnvironmentVariableKind = "recoverable" | "writeonly";
export const EnvironmentVariableKind = /*@__PURE__*/ S.String;

export interface EnvironmentVariable {
  /** The variable name. */
  key: string;
  kind: EnvironmentVariableKind;
  /** The decrypted plaintext value. Present only for `recoverable` variables. Omitted for `writeonly` variables, which can never be read back. */
  value?: string;
  /** Human-readable description. Omitted if none was provided. */
  description?: string;
  /** Unix timestamp in milliseconds when the variable was created. */
  createdAt: number;
}
export const EnvironmentVariable = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.String,
    kind: EnvironmentVariableKind,
    value: S.optional(S.String),
    description: S.optional(S.String),
    createdAt: S.Number,
  }),
).annotate({
  identifier: "EnvironmentVariable",
}) as any as S.Schema<EnvironmentVariable>;

export type V2EnvironmentsListEnvironmentVariablesResponseBodyDataList =
  Array<EnvironmentVariable>;
export const V2EnvironmentsListEnvironmentVariablesResponseBodyDataList =
  /*@__PURE__*/ S.Array(
    EnvironmentVariable,
  ) as any as S.Schema<V2EnvironmentsListEnvironmentVariablesResponseBodyDataList>;

export interface V2EnvironmentsListEnvironmentVariablesResponseBody {
  meta: Meta;
  data: V2EnvironmentsListEnvironmentVariablesResponseBodyDataList;
  pagination: Pagination;
}
export const V2EnvironmentsListEnvironmentVariablesResponseBody =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      meta: Meta,
      data: V2EnvironmentsListEnvironmentVariablesResponseBodyDataList,
      pagination: Pagination,
    }),
  ).annotate({
    identifier: "V2EnvironmentsListEnvironmentVariablesResponseBody",
  }) as any as S.Schema<V2EnvironmentsListEnvironmentVariablesResponseBody>;

/** The names of the variables to remove. Keys that exist are deleted; keys that are not present are ignored, since their absence already matches the requested state. Duplicate keys collapse to a single removal. The whole operation is atomic: if any part fails the environment is left unchanged. Limited to 50 variables per request. */
export type EnvironmentsRemoveEnvironmentVariablesRequestVariablesList =
  Array<string>;
export const EnvironmentsRemoveEnvironmentVariablesRequestVariablesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<EnvironmentsRemoveEnvironmentVariablesRequestVariablesList>;

export interface EnvironmentsRemoveEnvironmentVariablesRequest {
  project: string;
  app: string;
  environment: string;
  /** The names of the variables to remove. Keys that exist are deleted; keys that are not present are ignored, since their absence already matches the requested state. Duplicate keys collapse to a single removal. The whole operation is atomic: if any part fails the environment is left unchanged. Limited to 50 variables per request. */
  variables: EnvironmentsRemoveEnvironmentVariablesRequestVariablesList;
}
export const EnvironmentsRemoveEnvironmentVariablesRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project: S.String,
      app: S.String,
      environment: S.String,
      variables: EnvironmentsRemoveEnvironmentVariablesRequestVariablesList,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/v2/environments.removeEnvironmentVariables",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "EnvironmentsRemoveEnvironmentVariablesRequest",
  }) as any as S.Schema<EnvironmentsRemoveEnvironmentVariablesRequest>;

export interface V2EnvironmentsRemoveEnvironmentVariablesResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2EnvironmentsRemoveEnvironmentVariablesResponseBody =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      meta: Meta,
      data: S.Unknown,
    }),
  ).annotate({
    identifier: "V2EnvironmentsRemoveEnvironmentVariablesResponseBody",
  }) as any as S.Schema<V2EnvironmentsRemoveEnvironmentVariablesResponseBody>;

/** A single environment variable to set. */
export interface EnvironmentVariableInput {
  /** The variable name. Must be a POSIX shell name: letters, digits, and underscores only, and must not start with a digit. Other names are unreachable from shells and most runtimes. */
  key: string;
  /** The variable value. Always encrypted at rest. The limit is enforced server-side in UTF-8 bytes, so a multibyte value may be rejected before it reaches this code-point maximum. */
  value: string;
  /** How the value may be read back. Defaults to `writeonly`. */
  kind?: EnvironmentVariableKind | (string & {});
  /** Human-readable description of the variable. */
  description?: string;
}
export const EnvironmentVariableInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.String,
    value: S.String,
    kind: S.optional(EnvironmentVariableKind),
    description: S.optional(S.String),
  }),
).annotate({
  identifier: "EnvironmentVariableInput",
}) as any as S.Schema<EnvironmentVariableInput>;

/** The variables to upsert. Each entry is created if its key is new or fully overwritten if the key already exists. Existing variables whose keys are not in this list are left untouched, unless `prune` is true. Each entry is written exactly as sent, never merged with the current state. Only `value` is required; omitted optional fields (`kind`, `description`) fall back to their defaults rather than any previous value, so overwriting a variable without a `description` clears it. Each key may appear at most once; a duplicate key is rejected with a 400. The whole operation is atomic: if any part fails the environment is left unchanged. All values are encrypted at rest. Limited to 50 variables per request. */
export type EnvironmentsSetEnvironmentVariablesRequestVariablesList =
  Array<EnvironmentVariableInput>;
export const EnvironmentsSetEnvironmentVariablesRequestVariablesList =
  /*@__PURE__*/ S.Array(
    EnvironmentVariableInput,
  ) as any as S.Schema<EnvironmentsSetEnvironmentVariablesRequestVariablesList>;

export interface EnvironmentsSetEnvironmentVariablesRequest {
  project: string;
  app: string;
  environment: string;
  /** The variables to upsert. Each entry is created if its key is new or fully overwritten if the key already exists. Existing variables whose keys are not in this list are left untouched, unless `prune` is true. Each entry is written exactly as sent, never merged with the current state. Only `value` is required; omitted optional fields (`kind`, `description`) fall back to their defaults rather than any previous value, so overwriting a variable without a `description` clears it. Each key may appear at most once; a duplicate key is rejected with a 400. The whole operation is atomic: if any part fails the environment is left unchanged. All values are encrypted at rest. Limited to 50 variables per request. */
  variables: EnvironmentsSetEnvironmentVariablesRequestVariablesList;
  /** Optional. Defaults to false. When false, the variables above are upserted and any existing variable not in the list is kept. When true, this becomes a full replace: after upserting, every variable not in the list is deleted. Combined with an empty `variables` list, `prune: true` resets the entire environment by deleting every variable. */
  prune?: boolean;
}
export const EnvironmentsSetEnvironmentVariablesRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      project: S.String,
      app: S.String,
      environment: S.String,
      variables: EnvironmentsSetEnvironmentVariablesRequestVariablesList,
      prune: S.optional(S.Boolean),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/v2/environments.setEnvironmentVariables",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "EnvironmentsSetEnvironmentVariablesRequest",
  }) as any as S.Schema<EnvironmentsSetEnvironmentVariablesRequest>;

export interface V2EnvironmentsSetEnvironmentVariablesResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2EnvironmentsSetEnvironmentVariablesResponseBody =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      meta: Meta,
      data: S.Unknown,
    }),
  ).annotate({
    identifier: "V2EnvironmentsSetEnvironmentVariablesResponseBody",
  }) as any as S.Schema<V2EnvironmentsSetEnvironmentVariablesResponseBody>;

/** Glob paths that trigger auto-deploys when changed. Omit to leave unchanged. */
export type EnvironmentsUpdateSettingsRequestWatchPathsList = Array<string>;
export const EnvironmentsUpdateSettingsRequestWatchPathsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<EnvironmentsUpdateSettingsRequestWatchPathsList>;

/** Override container entrypoint command. Omit to leave unchanged. */
export type EnvironmentsUpdateSettingsRequestCommandList = Array<string>;
export const EnvironmentsUpdateSettingsRequestCommandList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<EnvironmentsUpdateSettingsRequestCommandList>;

/** Desired set of regions with per-region replica bounds. Omit to leave regions unchanged; when present, this replaces the full set (regions absent from the list are removed). At least one region is required; an empty list is rejected because an environment cannot have zero regions. */
export type EnvironmentsUpdateSettingsRequestRegionsList =
  Array<EnvironmentRegion>;
export const EnvironmentsUpdateSettingsRequestRegionsList =
  /*@__PURE__*/ S.Array(
    EnvironmentRegion,
  ) as any as S.Schema<EnvironmentsUpdateSettingsRequestRegionsList>;

export interface EnvironmentsUpdateSettingsRequest {
  project: string;
  app: string;
  environment: string;
  /** Path to the Dockerfile used for builds. Omit to leave unchanged; set null to clear and fall back to Railpack. */
  dockerfile?: string | null;
  /** The directory your app lives in. Unkey builds from here. Use "." for the repository root, or set a subdirectory when your app is nested (e.g., services/api). Omit to leave unchanged. */
  rootDirectory?: string;
  /** Overrides the build command auto-detected by Railpack. Omit to leave unchanged; set null to clear and fall back to auto-detection. */
  buildCommand?: string | null;
  /** Glob paths that trigger auto-deploys when changed. Omit to leave unchanged. */
  watchPaths?: EnvironmentsUpdateSettingsRequestWatchPathsList;
  /** Whether pushes auto-deploy. Omit to leave unchanged. */
  autoDeploy?: boolean;
  /** Container port the app listens on. Omit to leave unchanged. */
  port?: number;
  /** CPU allocation in vCPUs. Minimum 0.25 (1/4 vCPU), in steps of 0.25. The upper bound is your workspace's per-instance quota; exceeding it returns 400. Omit to leave unchanged. */
  vCpus?: number;
  /** Memory allocation in MiB. Minimum 256, in steps of 256. The upper bound is your workspace's per-instance quota; exceeding it returns 400. Omit to leave unchanged. */
  memoryMib?: number;
  /** Ephemeral storage allocation in MiB, in steps of 512 (0 for none). The upper bound is your workspace's per-instance quota; exceeding it returns 400. Omit to leave unchanged. */
  storageMib?: number;
  /** Override container entrypoint command. Omit to leave unchanged. */
  command?: EnvironmentsUpdateSettingsRequestCommandList;
  /** HTTP healthcheck configuration. Omit to leave unchanged; set null to remove. */
  healthcheck?: EnvironmentHealthcheck | null;
  /** Signal sent to the container on shutdown. Omit to leave unchanged. */
  shutdownSignal?: EnvironmentShutdownSignal | (string & {});
  /** Protocol used to reach the upstream container. Omit to leave unchanged. */
  upstreamProtocol?: EnvironmentUpstreamProtocol | (string & {});
  /** Path to the OpenAPI spec file within the build. Must start with a slash. Omit to leave unchanged; set null to clear. */
  openapiSpecPath?: string | null;
  /** Desired set of regions with per-region replica bounds. Omit to leave regions unchanged; when present, this replaces the full set (regions absent from the list are removed). At least one region is required; an empty list is rejected because an environment cannot have zero regions. */
  regions?: EnvironmentsUpdateSettingsRequestRegionsList;
}
export const EnvironmentsUpdateSettingsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
    environment: S.String,
    dockerfile: S.optional(S.NullOr(S.String)),
    rootDirectory: S.optional(S.String),
    buildCommand: S.optional(S.NullOr(S.String)),
    watchPaths: S.optional(EnvironmentsUpdateSettingsRequestWatchPathsList),
    autoDeploy: S.optional(S.Boolean),
    port: S.optional(S.Number),
    vCpus: S.optional(S.Number),
    memoryMib: S.optional(S.Number),
    storageMib: S.optional(S.Number),
    command: S.optional(EnvironmentsUpdateSettingsRequestCommandList),
    healthcheck: S.optional(S.NullOr(EnvironmentHealthcheck)),
    shutdownSignal: S.optional(EnvironmentShutdownSignal),
    upstreamProtocol: S.optional(EnvironmentUpstreamProtocol),
    openapiSpecPath: S.optional(S.NullOr(S.String)),
    regions: S.optional(EnvironmentsUpdateSettingsRequestRegionsList),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/environments.updateSettings",
      code: 200,
    }),
  ),
).annotate({
  identifier: "EnvironmentsUpdateSettingsRequest",
}) as any as S.Schema<EnvironmentsUpdateSettingsRequest>;

export interface V2EnvironmentsUpdateSettingsResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2EnvironmentsUpdateSettingsResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: S.Unknown,
    }),
).annotate({
  identifier: "V2EnvironmentsUpdateSettingsResponseBody",
}) as any as S.Schema<V2EnvironmentsUpdateSettingsResponseBody>;

export interface GatewayListPoliciesRequest {
  project: string;
  app: string;
  environment: string;
}
export const GatewayListPoliciesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
    environment: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/gateway.listPolicies", code: 200 }),
  ),
).annotate({
  identifier: "GatewayListPoliciesRequest",
}) as any as S.Schema<GatewayListPoliciesRequest>;

/** String matcher. Exactly one of `exact`, `prefix` or `regex` must be set. */
export interface StringMatch {
  /** Matches when the input equals this value. */
  exact?: string;
  /** Matches when the input starts with this value. */
  prefix?: string;
  /** Matches when the input satisfies this RE2 regular expression. Invalid patterns are rejected when the policy is created. */
  regex?: string;
  /** Compare case-insensitively. May accompany any match mode. */
  ignoreCase?: boolean;
}
export const StringMatch = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exact: S.optional(S.String),
    prefix: S.optional(S.String),
    regex: S.optional(S.String),
    ignoreCase: S.optional(S.Boolean),
  }),
).annotate({ identifier: "StringMatch" }) as any as S.Schema<StringMatch>;

/** Matches on the request path. */
export interface PathMatch {
  path: StringMatch;
}
export const PathMatch = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: StringMatch,
  }),
).annotate({ identifier: "PathMatch" }) as any as S.Schema<PathMatch>;

export type MethodMatchMethodsItem =
  | "GET"
  | "POST"
  | "PUT"
  | "PATCH"
  | "DELETE"
  | "HEAD"
  | "OPTIONS";
export const MethodMatchMethodsItem = /*@__PURE__*/ S.String;

export type MethodMatchMethodsList = Array<
  MethodMatchMethodsItem | (string & {})
>;
export const MethodMatchMethodsList = /*@__PURE__*/ S.Array(
  MethodMatchMethodsItem,
) as any as S.Schema<MethodMatchMethodsList>;

/** Matches when the request method is one of the listed methods. */
export interface MethodMatch {
  methods: MethodMatchMethodsList;
}
export const MethodMatch = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    methods: MethodMatchMethodsList,
  }),
).annotate({ identifier: "MethodMatch" }) as any as S.Schema<MethodMatch>;

/** Matches a named request field (header or query parameter). Exactly one of `present` or `value` must be set. */
export interface FieldMatch {
  name: string;
  /** Matches when the field is present, regardless of value. */
  present?: boolean;
  value?: StringMatch;
}
export const FieldMatch = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    present: S.optional(S.Boolean),
    value: S.optional(StringMatch),
  }),
).annotate({ identifier: "FieldMatch" }) as any as S.Schema<FieldMatch>;

/** A single request match expression. Exactly one of `path`, `method`, `header` or `queryParam` must be set. */
export interface MatchExpr {
  path?: PathMatch;
  method?: MethodMatch;
  header?: FieldMatch;
  queryParam?: FieldMatch;
}
export const MatchExpr = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.optional(PathMatch),
    method: S.optional(MethodMatch),
    header: S.optional(FieldMatch),
    queryParam: S.optional(FieldMatch),
  }),
).annotate({ identifier: "MatchExpr" }) as any as S.Schema<MatchExpr>;

/** Optional request matchers. The policy applies only to requests matching all expressions; omitted when the policy applies to every request. */
export type PolicyResponseMatchList = Array<MatchExpr>;
export const PolicyResponseMatchList = /*@__PURE__*/ S.Array(
  MatchExpr,
) as any as S.Schema<PolicyResponseMatchList>;

/** Keyspaces to verify keys against, referenced by id. All keyspaces must belong to your workspace. */
export type KeyauthPolicyKeyspacesList = Array<string>;
export const KeyauthPolicyKeyspacesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeyauthPolicyKeyspacesList>;

/** Extract the key from a custom header. */
export interface HeaderKeyLocation {
  name: string;
  /** Optional prefix removed from the header value before verification. */
  stripPrefix?: string;
}
export const HeaderKeyLocation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    stripPrefix: S.optional(S.String),
  }),
).annotate({
  identifier: "HeaderKeyLocation",
}) as any as S.Schema<HeaderKeyLocation>;

/** Extract the key from a query parameter. */
export interface QueryParamKeyLocation {
  name: string;
}
export const QueryParamKeyLocation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
  }),
).annotate({
  identifier: "QueryParamKeyLocation",
}) as any as S.Schema<QueryParamKeyLocation>;

/** Where to look for the API key on incoming requests. Exactly one of `bearer`, `header` or `queryParam` must be set. */
export interface KeyLocation {
  bearer?: unknown;
  header?: HeaderKeyLocation;
  queryParam?: QueryParamKeyLocation;
}
export const KeyLocation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bearer: S.optional(S.Unknown),
    header: S.optional(HeaderKeyLocation),
    queryParam: S.optional(QueryParamKeyLocation),
  }),
).annotate({ identifier: "KeyLocation" }) as any as S.Schema<KeyLocation>;

/** Where to look for the key on incoming requests, tried in order. Defaults to the `Authorization Bearer` header when omitted. */
export type KeyauthPolicyLocationsList = Array<KeyLocation>;
export const KeyauthPolicyLocationsList = /*@__PURE__*/ S.Array(
  KeyLocation,
) as any as S.Schema<KeyauthPolicyLocationsList>;

/** A rate limit applied during key verification. `limit` and `duration` must be set together or both omitted; a partial pair is rejected. */
export interface KeyRatelimit {
  /** Name of a rate limit configured on the key or its identity, or the name of the inline override defined by `limit` and `duration`. */
  name: string;
  /** Inline override: maximum number of operations per window. Must be set together with `duration`. */
  limit?: number;
  /** Inline override: window duration in milliseconds. Must be set together with `limit`. */
  duration?: number;
  /** Cost charged against the limit per request. Defaults to 1. */
  cost?: number;
}
export const KeyRatelimit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    limit: S.optional(S.Number),
    duration: S.optional(S.Number),
    cost: S.optional(S.Number),
  }),
).annotate({ identifier: "KeyRatelimit" }) as any as S.Schema<KeyRatelimit>;

/** Rate limits applied during key verification. */
export type KeyauthPolicyRatelimitsList = Array<KeyRatelimit>;
export const KeyauthPolicyRatelimitsList = /*@__PURE__*/ S.Array(
  KeyRatelimit,
) as any as S.Schema<KeyauthPolicyRatelimitsList>;

/** Verifies Unkey API keys on matching requests. */
export interface KeyauthPolicy {
  /** Keyspaces to verify keys against, referenced by id. All keyspaces must belong to your workspace. */
  keyspaces: KeyauthPolicyKeyspacesList;
  /** Where to look for the key on incoming requests, tried in order. Defaults to the `Authorization Bearer` header when omitted. */
  locations?: KeyauthPolicyLocationsList;
  /** Optional permission query the verified key must satisfy, e.g. `documents.read AND documents.write`. */
  permissionQuery?: string;
  /** Rate limits applied during key verification. */
  ratelimits?: KeyauthPolicyRatelimitsList;
  /** Usage credits a matching request deducts from the verified key. Defaults to 1. Set to 0 to verify the key without spending credits, or to a higher value to charge more per request. Keys with unlimited usage are unaffected. */
  credits?: number;
}
export const KeyauthPolicy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyspaces: KeyauthPolicyKeyspacesList,
    locations: S.optional(KeyauthPolicyLocationsList),
    permissionQuery: S.optional(S.String),
    ratelimits: S.optional(KeyauthPolicyRatelimitsList),
    credits: S.optional(S.Number),
  }),
).annotate({ identifier: "KeyauthPolicy" }) as any as S.Schema<KeyauthPolicy>;

/** Rate limit by the value of a request header. */
export type HeaderKey = QueryParamKeyLocation;
export const HeaderKey = QueryParamKeyLocation;

/** Rate limit by a field extracted from the authenticated principal. */
export interface PrincipalFieldKey {
  path: string;
}
export const PrincipalFieldKey = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
  }),
).annotate({
  identifier: "PrincipalFieldKey",
}) as any as S.Schema<PrincipalFieldKey>;

/** How requests are grouped for rate limiting. Exactly one of `remoteIp`, `header`, `authenticatedSubject`, `path` or `principalField` must be set. */
export interface RatelimitIdentifier {
  remoteIp?: unknown;
  header?: QueryParamKeyLocation;
  authenticatedSubject?: unknown;
  path?: unknown;
  principalField?: PrincipalFieldKey;
}
export const RatelimitIdentifier = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    remoteIp: S.optional(S.Unknown),
    header: S.optional(QueryParamKeyLocation),
    authenticatedSubject: S.optional(S.Unknown),
    path: S.optional(S.Unknown),
    principalField: S.optional(PrincipalFieldKey),
  }),
).annotate({
  identifier: "RatelimitIdentifier",
}) as any as S.Schema<RatelimitIdentifier>;

/** Ordered list of sources that form a compound rate limit key. The gateway resolves each source for each request. Each unique combination of resolved values has its own counter. All counters use the same limit and window. Example: `[authenticatedSubject, path]` limits each subject separately on each path. */
export type RatelimitPolicyIdentifiersList = Array<RatelimitIdentifier>;
export const RatelimitPolicyIdentifiersList = /*@__PURE__*/ S.Array(
  RatelimitIdentifier,
) as any as S.Schema<RatelimitPolicyIdentifiersList>;

/** Rate limits matching requests. Set `identifiers` with 1 to 5 sources. The deprecated `identifier` field is accepted in place of a one-entry `identifiers` list; set exactly one of the two. */
export interface RatelimitPolicy {
  /** Maximum number of requests per window. */
  limit: number;
  /** Window duration in milliseconds. */
  windowMs: number;
  /** Deprecated. Accepted for compatibility with old clients. Use `identifiers` with one entry. Responses always return `identifiers`. */
  identifier?: RatelimitIdentifier;
  /** Ordered list of sources that form a compound rate limit key. The gateway resolves each source for each request. Each unique combination of resolved values has its own counter. All counters use the same limit and window. Example: `[authenticatedSubject, path]` limits each subject separately on each path. */
  identifiers?: RatelimitPolicyIdentifiersList;
}
export const RatelimitPolicy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    limit: S.Number,
    windowMs: S.Number,
    identifier: S.optional(RatelimitIdentifier),
    identifiers: S.optional(RatelimitPolicyIdentifiersList),
  }),
).annotate({
  identifier: "RatelimitPolicy",
}) as any as S.Schema<RatelimitPolicy>;

/** What to do with matching requests. */
export type FirewallPolicyAction = "ACTION_DENY";
export const FirewallPolicyAction = /*@__PURE__*/ S.String;

/** Blocks matching requests. */
export interface FirewallPolicy {
  /** What to do with matching requests. */
  action: FirewallPolicyAction | (string & {});
}
export const FirewallPolicy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: FirewallPolicyAction,
  }),
).annotate({ identifier: "FirewallPolicy" }) as any as S.Schema<FirewallPolicy>;

/** Adds request data to the log entries of matching requests. The gateway always records a basic log entry for every request: method, host, path, status, and latency. Each capture setting is a separate opt-in: request headers, response headers, request body, response body, and query data. The policy's `match` expressions select the requests. A policy without `match` expressions matches every request. If more than one enabled logging policy matches a request, the gateway combines their settings. The gateway always redacts the `Authorization` header and configured key locations before it stores headers or query data. */
export interface LoggingPolicy {
  /** Capture request headers, the user agent, and the client IP. The user agent and client IP are included because they identify the client. */
  requestHeaders?: boolean;
  /** Capture response headers. */
  responseHeaders?: boolean;
  /** Capture the request body, up to the capture limit. */
  requestBody?: boolean;
  /** Capture the response body, up to the capture limit. */
  responseBody?: boolean;
  /** Capture the query string and query parameters. Query data is a separate opt-in because URLs can contain secrets, for example `?api_key=...`. */
  query?: boolean;
}
export const LoggingPolicy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    requestHeaders: S.optional(S.Boolean),
    responseHeaders: S.optional(S.Boolean),
    requestBody: S.optional(S.Boolean),
    responseBody: S.optional(S.Boolean),
    query: S.optional(S.Boolean),
  }),
).annotate({ identifier: "LoggingPolicy" }) as any as S.Schema<LoggingPolicy>;

/** A stored gateway policy as returned by list endpoints. Exactly one of `keyauth`, `ratelimit`, `firewall`, `openapi` or `logging` is set. */
export interface PolicyResponse {
  /** Server-generated policy id. Regenerated on every `gateway.setPolicies` call, so treat it as stable only until the environment's policies are next replaced. */
  id: string;
  /** Human-readable name shown in the dashboard. */
  name: string;
  /** Disabled policies are stored but skipped during evaluation. */
  enabled: boolean;
  /** Optional request matchers. The policy applies only to requests matching all expressions; omitted when the policy applies to every request. */
  match?: PolicyResponseMatchList;
  keyauth?: KeyauthPolicy;
  ratelimit?: RatelimitPolicy;
  firewall?: FirewallPolicy;
  openapi?: unknown;
  logging?: LoggingPolicy;
}
export const PolicyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    enabled: S.Boolean,
    match: S.optional(PolicyResponseMatchList),
    keyauth: S.optional(KeyauthPolicy),
    ratelimit: S.optional(RatelimitPolicy),
    firewall: S.optional(FirewallPolicy),
    openapi: S.optional(S.Unknown),
    logging: S.optional(LoggingPolicy),
  }),
).annotate({ identifier: "PolicyResponse" }) as any as S.Schema<PolicyResponse>;

/** The environment's gateway policies in evaluation order. */
export type V2GatewayListPoliciesResponseData = Array<PolicyResponse>;
export const V2GatewayListPoliciesResponseData = /*@__PURE__*/ S.Array(
  PolicyResponse,
) as any as S.Schema<V2GatewayListPoliciesResponseData>;

export interface V2GatewayListPoliciesResponseBody {
  meta: Meta;
  data: V2GatewayListPoliciesResponseData;
}
export const V2GatewayListPoliciesResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2GatewayListPoliciesResponseData,
  }),
).annotate({
  identifier: "V2GatewayListPoliciesResponseBody",
}) as any as S.Schema<V2GatewayListPoliciesResponseBody>;

/** Optional request matchers. The policy applies only to requests matching all expressions; omit to apply to every request. */
export type PolicyMatchList = Array<MatchExpr>;
export const PolicyMatchList = /*@__PURE__*/ S.Array(
  MatchExpr,
) as any as S.Schema<PolicyMatchList>;

/** A gateway policy. Exactly one of `keyauth`, `ratelimit`, `firewall`, `openapi` or `logging` must be set. The server generates an id for every policy it stores. */
export interface Policy {
  /** Human-readable name shown in the dashboard. */
  name: string;
  /** Disabled policies are stored but skipped during evaluation. */
  enabled: boolean;
  /** Optional request matchers. The policy applies only to requests matching all expressions; omit to apply to every request. */
  match?: PolicyMatchList;
  keyauth?: KeyauthPolicy;
  ratelimit?: RatelimitPolicy;
  firewall?: FirewallPolicy;
  openapi?: unknown;
  logging?: LoggingPolicy;
}
export const Policy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    enabled: S.Boolean,
    match: S.optional(PolicyMatchList),
    keyauth: S.optional(KeyauthPolicy),
    ratelimit: S.optional(RatelimitPolicy),
    firewall: S.optional(FirewallPolicy),
    openapi: S.optional(S.Unknown),
    logging: S.optional(LoggingPolicy),
  }),
).annotate({ identifier: "Policy" }) as any as S.Schema<Policy>;

/** The environment's complete policy list, in evaluation order. Every call replaces all stored policies with exactly this list; an empty list removes every policy. The operation is atomic: if any policy is invalid, nothing is written. An environment can hold at most 50 policies. */
export type GatewaySetPoliciesRequestPoliciesList = Array<Policy>;
export const GatewaySetPoliciesRequestPoliciesList = /*@__PURE__*/ S.Array(
  Policy,
) as any as S.Schema<GatewaySetPoliciesRequestPoliciesList>;

export interface GatewaySetPoliciesRequest {
  project: string;
  app: string;
  environment: string;
  /** The environment's complete policy list, in evaluation order. Every call replaces all stored policies with exactly this list; an empty list removes every policy. The operation is atomic: if any policy is invalid, nothing is written. An environment can hold at most 50 policies. */
  policies: GatewaySetPoliciesRequestPoliciesList;
}
export const GatewaySetPoliciesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
    environment: S.String,
    policies: GatewaySetPoliciesRequestPoliciesList,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/gateway.setPolicies", code: 200 }),
  ),
).annotate({
  identifier: "GatewaySetPoliciesRequest",
}) as any as S.Schema<GatewaySetPoliciesRequest>;

export interface V2GatewaySetPoliciesResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2GatewaySetPoliciesResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: S.Unknown,
  }),
).annotate({
  identifier: "V2GatewaySetPoliciesResponseBody",
}) as any as S.Schema<V2GatewaySetPoliciesResponseBody>;

/** Replaces all match expressions. Set null to remove them so the policy applies to every request. Omit to keep the current expressions. */
export type GatewayUpdatePolicyRequestMatchList = Array<MatchExpr>;
export const GatewayUpdatePolicyRequestMatchList = /*@__PURE__*/ S.Array(
  MatchExpr,
) as any as S.Schema<GatewayUpdatePolicyRequestMatchList>;

export interface GatewayUpdatePolicyRequest {
  project: string;
  app: string;
  environment: string;
  /** Id of the policy to update, as returned by `gateway.listPolicies`. Ids are regenerated whenever `gateway.setPolicies` replaces the list, so list the policies first if you are unsure the id is current. */
  policyId: string;
  /** New human-readable name. Omit to keep the current name. */
  name?: string;
  /** Enable or disable the policy. Disabled policies are stored but skipped during evaluation. Omit to keep the current setting. */
  enabled?: boolean;
  /** Replaces all match expressions. Set null to remove them so the policy applies to every request. Omit to keep the current expressions. */
  match?: GatewayUpdatePolicyRequestMatchList | null;
  keyauth?: KeyauthPolicy;
  ratelimit?: RatelimitPolicy;
  firewall?: FirewallPolicy;
  openapi?: unknown;
  logging?: LoggingPolicy;
}
export const GatewayUpdatePolicyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    app: S.String,
    environment: S.String,
    policyId: S.String,
    name: S.optional(S.String),
    enabled: S.optional(S.Boolean),
    match: S.optional(S.NullOr(GatewayUpdatePolicyRequestMatchList)),
    keyauth: S.optional(KeyauthPolicy),
    ratelimit: S.optional(RatelimitPolicy),
    firewall: S.optional(FirewallPolicy),
    openapi: S.optional(S.Unknown),
    logging: S.optional(LoggingPolicy),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/gateway.updatePolicy", code: 200 }),
  ),
).annotate({
  identifier: "GatewayUpdatePolicyRequest",
}) as any as S.Schema<GatewayUpdatePolicyRequest>;

export interface V2GatewayUpdatePolicyResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2GatewayUpdatePolicyResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: S.Unknown,
  }),
).annotate({
  identifier: "V2GatewayUpdatePolicyResponseBody",
}) as any as S.Schema<V2GatewayUpdatePolicyResponseBody>;

export interface GithubInstallAppRequest {}
export const GithubInstallAppRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "POST", uri: "/v2/github.installApp", code: 200 }),
  ),
).annotate({
  identifier: "GithubInstallAppRequest",
}) as any as S.Schema<GithubInstallAppRequest>;

export interface V2GithubInstallAppResponseData {
  /** The GitHub App install URL. Open it in a browser to install the app and grant repository access. After installation GitHub returns to Unkey, which binds the installation and lands you in the workspace settings. */
  url: string;
  /** Unix timestamp in milliseconds after which the URL's signed state expires and the link must be requested again. */
  expiresAt: number;
}
export const V2GithubInstallAppResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
    expiresAt: S.Number,
  }),
).annotate({
  identifier: "V2GithubInstallAppResponseData",
}) as any as S.Schema<V2GithubInstallAppResponseData>;

export interface V2GithubInstallAppResponseBody {
  meta: Meta;
  data: V2GithubInstallAppResponseData;
}
export const V2GithubInstallAppResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2GithubInstallAppResponseData,
  }),
).annotate({
  identifier: "V2GithubInstallAppResponseBody",
}) as any as S.Schema<V2GithubInstallAppResponseBody>;

/** Stores arbitrary JSON metadata returned during key verification for contextual information. Eliminates additional database lookups during verification, improving performance for stateless services. Avoid storing sensitive data here as it's returned in verification responses. Large metadata objects increase verification latency and should stay under 10KB total size. Use this for subscription details, feature flags, user preferences, and organization information. Metadata is returned as-is whenever keys associated with this identity are verified. */
export type IdentitiesCreateIdentityRequestMetaMap = {
  [key: string]: unknown | undefined;
};
export const IdentitiesCreateIdentityRequestMetaMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<IdentitiesCreateIdentityRequestMetaMap>;

export interface RatelimitRequest {
  /** The name of this rate limit. This name is used to identify which limit to check during key verification. Best practices for limit names: - Use descriptive, semantic names like 'api_requests', 'heavy_operations', or 'downloads' - Be consistent with naming conventions across your application - Create separate limits for different resource types or operation costs - Consider using namespaced names for better organization (e.g., 'files.downloads', 'compute.training') You will reference this exact name when verifying keys to check against this specific limit. */
  name: string;
  /** The maximum number of operations allowed within the specified time window. When this limit is reached, verification requests will fail with `code=RATE_LIMITED` until the window resets. The limit should reflect: - Your infrastructure capacity and scaling limitations - Fair usage expectations for your service - Different tier levels for various user types - The relative cost of the operations being limited Higher values allow more frequent access but may impact service performance. */
  limit: number;
  /** The duration for each ratelimit window in milliseconds. This controls how long the rate limit counter accumulates before resetting. Common values include: - 1000 (1 second): For strict per-second limits on high-frequency operations - 60000 (1 minute): For moderate API usage control - 3600000 (1 hour): For less frequent but costly operations - 86400000 (24 hours): For daily quotas Shorter windows provide more frequent resets but may allow large burst usage. Longer windows provide more consistent usage patterns but take longer to reset after limit exhaustion. */
  duration: number;
  /** Whether this ratelimit should be automatically applied when verifying a key. */
  autoApply: boolean;
}
export const RatelimitRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    limit: S.Number,
    duration: S.Number,
    autoApply: S.Boolean,
  }),
).annotate({
  identifier: "RatelimitRequest",
}) as any as S.Schema<RatelimitRequest>;

/** Defines shared rate limits that apply to all keys belonging to this identity. Prevents abuse by users with multiple keys by enforcing consistent limits across their entire key portfolio. Essential for implementing fair usage policies and tiered access levels in multi-tenant applications. Rate limit counters are shared across all keys with this identity, regardless of how many keys the user creates. During verification, specify which named limits to check for enforcement. Identity rate limits supplement any key-specific rate limits that may also be configured. - Each named limit can have different thresholds and windows When verifying keys, you can specify which limits you want to use and all keys attached to this identity will share the limits, regardless of which specific key is used. */
export type IdentitiesCreateIdentityRequestRatelimitsList =
  Array<RatelimitRequest>;
export const IdentitiesCreateIdentityRequestRatelimitsList =
  /*@__PURE__*/ S.Array(
    RatelimitRequest,
  ) as any as S.Schema<IdentitiesCreateIdentityRequestRatelimitsList>;

export interface IdentitiesCreateIdentityRequest {
  /** Creates an identity using your system's unique identifier for a user, organization, or entity. Must be stable and unique across your workspace - duplicate externalIds return CONFLICT errors. This identifier links Unkey identities to your authentication system, database records, or tenant structure. Avoid changing externalIds after creation as this breaks the link between your systems. Use consistent identifier patterns across your application for easier management and debugging. Accepts letters, numbers, underscores, dots, and hyphens for flexible identifier formats. Essential for implementing proper multi-tenant isolation and user-specific rate limiting. */
  externalId: string;
  /** Stores arbitrary JSON metadata returned during key verification for contextual information. Eliminates additional database lookups during verification, improving performance for stateless services. Avoid storing sensitive data here as it's returned in verification responses. Large metadata objects increase verification latency and should stay under 10KB total size. Use this for subscription details, feature flags, user preferences, and organization information. Metadata is returned as-is whenever keys associated with this identity are verified. */
  meta?: IdentitiesCreateIdentityRequestMetaMap;
  /** Defines shared rate limits that apply to all keys belonging to this identity. Prevents abuse by users with multiple keys by enforcing consistent limits across their entire key portfolio. Essential for implementing fair usage policies and tiered access levels in multi-tenant applications. Rate limit counters are shared across all keys with this identity, regardless of how many keys the user creates. During verification, specify which named limits to check for enforcement. Identity rate limits supplement any key-specific rate limits that may also be configured. - Each named limit can have different thresholds and windows When verifying keys, you can specify which limits you want to use and all keys attached to this identity will share the limits, regardless of which specific key is used. */
  ratelimits?: IdentitiesCreateIdentityRequestRatelimitsList;
}
export const IdentitiesCreateIdentityRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    externalId: S.String,
    meta: S.optional(IdentitiesCreateIdentityRequestMetaMap),
    ratelimits: S.optional(IdentitiesCreateIdentityRequestRatelimitsList),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/identities.createIdentity", code: 200 }),
  ),
).annotate({
  identifier: "IdentitiesCreateIdentityRequest",
}) as any as S.Schema<IdentitiesCreateIdentityRequest>;

export interface V2IdentitiesCreateIdentityResponseData {
  /** The unique identifier of the created identity. */
  identityId: string;
}
export const V2IdentitiesCreateIdentityResponseData = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      identityId: S.String,
    }),
).annotate({
  identifier: "V2IdentitiesCreateIdentityResponseData",
}) as any as S.Schema<V2IdentitiesCreateIdentityResponseData>;

export interface V2IdentitiesCreateIdentityResponseBody {
  meta: Meta;
  data: V2IdentitiesCreateIdentityResponseData;
}
export const V2IdentitiesCreateIdentityResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: V2IdentitiesCreateIdentityResponseData,
    }),
).annotate({
  identifier: "V2IdentitiesCreateIdentityResponseBody",
}) as any as S.Schema<V2IdentitiesCreateIdentityResponseBody>;

export interface IdentitiesDeleteIdentityRequest {
  /** The ID of the identity to delete. This can be either the externalId (from your own system that was used during identity creation) or the identityId (the internal ID returned by the identity service). */
  identity: string;
}
export const IdentitiesDeleteIdentityRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    identity: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/identities.deleteIdentity", code: 200 }),
  ),
).annotate({
  identifier: "IdentitiesDeleteIdentityRequest",
}) as any as S.Schema<IdentitiesDeleteIdentityRequest>;

/** Empty response object. A successful response indicates the identity was deleted successfully. */
export interface V2IdentitiesDeleteIdentityResponseBody {
  meta: Meta;
}
export const V2IdentitiesDeleteIdentityResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
    }),
).annotate({
  identifier: "V2IdentitiesDeleteIdentityResponseBody",
}) as any as S.Schema<V2IdentitiesDeleteIdentityResponseBody>;

export interface IdentitiesGetIdentityRequest {
  /** The ID of the identity to retrieve. This can be either the externalId (from your own system that was used during identity creation) or the identityId (the internal ID returned by the identity service). */
  identity: string;
}
export const IdentitiesGetIdentityRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    identity: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/identities.getIdentity", code: 200 }),
  ),
).annotate({
  identifier: "IdentitiesGetIdentityRequest",
}) as any as S.Schema<IdentitiesGetIdentityRequest>;

export interface V2IdentitiesGetIdentityResponseBody {
  meta: Meta;
  data: Identity;
}
export const V2IdentitiesGetIdentityResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: Identity,
  }),
).annotate({
  identifier: "V2IdentitiesGetIdentityResponseBody",
}) as any as S.Schema<V2IdentitiesGetIdentityResponseBody>;

export interface IdentitiesListIdentitiesRequest {
  /** The maximum number of identities to return in a single request. Use this to control response size and loading performance. */
  limit?: number;
  /** Pagination cursor from a previous response. Use this to fetch subsequent pages of results when the response contains a cursor value. */
  cursor?: string;
  /** Free-form text to filter identities. Returns identities whose ID or external ID contains the search string. Matching is case-insensitive. */
  search?: string;
}
export const IdentitiesListIdentitiesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    limit: S.optional(S.Number),
    cursor: S.optional(S.String),
    search: S.optional(S.String),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/identities.listIdentities", code: 200 }),
  ),
).annotate({
  identifier: "IdentitiesListIdentitiesRequest",
}) as any as S.Schema<IdentitiesListIdentitiesRequest>;

/** List of identities matching the specified criteria. */
export type V2IdentitiesListIdentitiesResponseData = Array<Identity>;
export const V2IdentitiesListIdentitiesResponseData = /*@__PURE__*/ S.Array(
  Identity,
) as any as S.Schema<V2IdentitiesListIdentitiesResponseData>;

export interface V2IdentitiesListIdentitiesResponseBody {
  meta: Meta;
  data: V2IdentitiesListIdentitiesResponseData;
  pagination: Pagination;
}
export const V2IdentitiesListIdentitiesResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: V2IdentitiesListIdentitiesResponseData,
      pagination: Pagination,
    }),
).annotate({
  identifier: "V2IdentitiesListIdentitiesResponseBody",
}) as any as S.Schema<V2IdentitiesListIdentitiesResponseBody>;

/** Replaces all existing metadata with this new metadata object. Omitting this field preserves existing metadata, while providing an empty object clears all metadata. Avoid storing sensitive data here as it's returned in verification responses. Large metadata objects increase verification latency and should stay under 10KB total size. */
export type IdentitiesUpdateIdentityRequestMetaMap = {
  [key: string]: unknown | undefined;
};
export const IdentitiesUpdateIdentityRequestMetaMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<IdentitiesUpdateIdentityRequestMetaMap>;

/** Replaces all existing identity rate limits with this complete list of rate limits. Omitting this field preserves existing rate limits, while providing an empty array removes all rate limits. These limits are shared across all keys belonging to this identity, preventing abuse through multiple keys. Rate limit changes take effect immediately but may take up to 30 seconds to propagate across all regions. */
export type IdentitiesUpdateIdentityRequestRatelimitsList =
  Array<RatelimitRequest>;
export const IdentitiesUpdateIdentityRequestRatelimitsList =
  /*@__PURE__*/ S.Array(
    RatelimitRequest,
  ) as any as S.Schema<IdentitiesUpdateIdentityRequestRatelimitsList>;

export interface IdentitiesUpdateIdentityRequest {
  /** The ID of the identity to update. Accepts either the externalId (your system-generated identifier) or the identityId (internal identifier returned by the identity service). */
  identity: string;
  /** Replaces all existing metadata with this new metadata object. Omitting this field preserves existing metadata, while providing an empty object clears all metadata. Avoid storing sensitive data here as it's returned in verification responses. Large metadata objects increase verification latency and should stay under 10KB total size. */
  meta?: IdentitiesUpdateIdentityRequestMetaMap;
  /** Replaces all existing identity rate limits with this complete list of rate limits. Omitting this field preserves existing rate limits, while providing an empty array removes all rate limits. These limits are shared across all keys belonging to this identity, preventing abuse through multiple keys. Rate limit changes take effect immediately but may take up to 30 seconds to propagate across all regions. */
  ratelimits?: IdentitiesUpdateIdentityRequestRatelimitsList;
}
export const IdentitiesUpdateIdentityRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    identity: S.String,
    meta: S.optional(IdentitiesUpdateIdentityRequestMetaMap),
    ratelimits: S.optional(IdentitiesUpdateIdentityRequestRatelimitsList),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/identities.updateIdentity", code: 200 }),
  ),
).annotate({
  identifier: "IdentitiesUpdateIdentityRequest",
}) as any as S.Schema<IdentitiesUpdateIdentityRequest>;

export interface V2IdentitiesUpdateIdentityResponseBody {
  data: Identity;
  meta: Meta;
}
export const V2IdentitiesUpdateIdentityResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      data: Identity,
      meta: Meta,
    }),
).annotate({
  identifier: "V2IdentitiesUpdateIdentityResponseBody",
}) as any as S.Schema<V2IdentitiesUpdateIdentityResponseBody>;

/** Grants additional permissions to the key through direct assignment or automatic creation. Duplicate permissions are ignored automatically, making this operation idempotent. Adding permissions never removes existing permissions or role-based permissions. Any permissions that do not exist will be auto created if the root key has permissions, otherwise this operation will fail with a 403 error. */
export type KeysAddPermissionsRequestPermissionsList = Array<string>;
export const KeysAddPermissionsRequestPermissionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeysAddPermissionsRequestPermissionsList>;

export interface KeysAddPermissionsRequest {
  /** Specifies which key receives the additional permissions using the database identifier returned from `keys.createKey`. Do not confuse this with the actual API key string that users include in requests. */
  keyId: string;
  /** Grants additional permissions to the key through direct assignment or automatic creation. Duplicate permissions are ignored automatically, making this operation idempotent. Adding permissions never removes existing permissions or role-based permissions. Any permissions that do not exist will be auto created if the root key has permissions, otherwise this operation will fail with a 403 error. */
  permissions: KeysAddPermissionsRequestPermissionsList;
}
export const KeysAddPermissionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    permissions: KeysAddPermissionsRequestPermissionsList,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/keys.addPermissions", code: 200 }),
  ),
).annotate({
  identifier: "KeysAddPermissionsRequest",
}) as any as S.Schema<KeysAddPermissionsRequest>;

export interface Permission {
  /** The unique identifier for this permission within Unkey's system. Generated automatically when the permission is created and used to reference this permission in API operations. Always begins with 'perm_' followed by alphanumeric characters and underscores. */
  id: string;
  /** The human-readable name for this permission that describes its purpose. Should be descriptive enough for developers to understand what access it grants. Use clear, semantic names that reflect the resources or actions being permitted. Names must be unique within your workspace to avoid confusion and conflicts. */
  name: string;
  /** The unique URL-safe identifier for this permission. */
  slug: string;
  /** Optional detailed explanation of what this permission grants access to. Helps team members understand the scope and implications of granting this permission. Include information about what resources can be accessed and what actions can be performed. Not visible to end users - this is for internal documentation and team clarity. */
  description?: string;
}
export const Permission = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    slug: S.String,
    description: S.optional(S.String),
  }),
).annotate({ identifier: "Permission" }) as any as S.Schema<Permission>;

/** Complete list of all permissions directly assigned to the key (including both newly added permissions and those that were already assigned). This response includes: - All direct permissions assigned to the key (both pre-existing and newly added) - Both the permission ID and name for each permission Important notes: - This list does NOT include permissions granted through roles - For a complete permission picture, use `/v2/keys.getKey` instead - An empty array indicates the key has no direct permissions assigned */
export type V2KeysAddPermissionsResponseData = Array<Permission>;
export const V2KeysAddPermissionsResponseData = /*@__PURE__*/ S.Array(
  Permission,
) as any as S.Schema<V2KeysAddPermissionsResponseData>;

export interface V2KeysAddPermissionsResponseBody {
  meta: Meta;
  data: V2KeysAddPermissionsResponseData;
}
export const V2KeysAddPermissionsResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2KeysAddPermissionsResponseData,
  }),
).annotate({
  identifier: "V2KeysAddPermissionsResponseBody",
}) as any as S.Schema<V2KeysAddPermissionsResponseBody>;

/** Assigns additional roles to the key through direct assignment to existing workspace roles. Operations are idempotent - adding existing roles has no effect and causes no errors. All roles must already exist in the workspace - roles cannot be created automatically. Invalid roles cause the entire operation to fail atomically, ensuring consistent state. */
export type KeysAddRolesRequestRolesList = Array<string>;
export const KeysAddRolesRequestRolesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeysAddRolesRequestRolesList>;

export interface KeysAddRolesRequest {
  /** Specifies which key receives the additional roles using the database identifier returned from `createKey`. Do not confuse this with the actual API key string that users include in requests. Added roles supplement existing roles and permissions without replacing them. Role assignments take effect immediately but may take up to 30 seconds to propagate across all regions. */
  keyId: string;
  /** Assigns additional roles to the key through direct assignment to existing workspace roles. Operations are idempotent - adding existing roles has no effect and causes no errors. All roles must already exist in the workspace - roles cannot be created automatically. Invalid roles cause the entire operation to fail atomically, ensuring consistent state. */
  roles: KeysAddRolesRequestRolesList;
}
export const KeysAddRolesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    roles: KeysAddRolesRequestRolesList,
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.addRoles", code: 200 })),
).annotate({
  identifier: "KeysAddRolesRequest",
}) as any as S.Schema<KeysAddRolesRequest>;

/** Complete list of permissions currently assigned to this role. Each permission grants specific access rights that will be inherited by any keys or users assigned this role. Use this list to understand the full scope of access provided by this role. Permissions can be added or removed from roles without affecting the role's identity or other properties. Empty array indicates a role with no permissions currently assigned. */
export type RolePermissionsList = Array<Permission>;
export const RolePermissionsList = /*@__PURE__*/ S.Array(
  Permission,
) as any as S.Schema<RolePermissionsList>;

export interface Role {
  /** The unique identifier for this role within Unkey's system. Generated automatically when the role is created and used to reference this role in API operations. Always begins with 'role_' followed by alphanumeric characters and underscores. */
  id: string;
  /** The human-readable name for this role that describes its function. Should be descriptive enough for administrators to understand what access this role provides. Use clear, semantic names that reflect the job function or responsibility level. Names must be unique within your workspace to avoid confusion during role assignment. */
  name: string;
  /** Optional detailed explanation of what this role encompasses and what access it provides. Helps team members understand the role's scope, intended use cases, and security implications. Include information about what types of users should receive this role and what they can accomplish. Not visible to end users - this is for internal documentation and access control audits. */
  description?: string;
  /** Complete list of permissions currently assigned to this role. Each permission grants specific access rights that will be inherited by any keys or users assigned this role. Use this list to understand the full scope of access provided by this role. Permissions can be added or removed from roles without affecting the role's identity or other properties. Empty array indicates a role with no permissions currently assigned. */
  permissions?: RolePermissionsList;
}
export const Role = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    description: S.optional(S.String),
    permissions: S.optional(RolePermissionsList),
  }),
).annotate({ identifier: "Role" }) as any as S.Schema<Role>;

/** Complete list of all roles directly assigned to the key after the operation completes. The response includes: - All roles now assigned to the key (both pre-existing and newly added) - Both ID and name of each role for easy reference Important notes: - The response shows the complete current state after the addition - An empty array means the key has no roles assigned (unlikely after an add operation) - This only shows direct role assignments, not inherited or nested roles - Role permissions are not expanded in this response - use keys.getKey for full details */
export type V2KeysAddRolesResponseData = Array<Role>;
export const V2KeysAddRolesResponseData = /*@__PURE__*/ S.Array(
  Role,
) as any as S.Schema<V2KeysAddRolesResponseData>;

export interface V2KeysAddRolesResponseBody {
  meta: Meta;
  data: V2KeysAddRolesResponseData;
}
export const V2KeysAddRolesResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2KeysAddRolesResponseData,
  }),
).annotate({
  identifier: "V2KeysAddRolesResponseBody",
}) as any as S.Schema<V2KeysAddRolesResponseBody>;

/** Stores arbitrary JSON metadata returned during key verification for contextual information. Eliminates additional database lookups during verification, improving performance for stateless services. Avoid storing sensitive data here as it's returned in verification responses. Large metadata objects increase verification latency and should stay under 10KB total size. */
export type KeysCreateKeyRequestMetaMap = {
  [key: string]: unknown | undefined;
};
export const KeysCreateKeyRequestMetaMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<KeysCreateKeyRequestMetaMap>;

/** Assigns existing roles to this key for permission management through role-based access control. Roles must already exist in your workspace before assignment. During verification, all permissions from assigned roles are checked against requested permissions. Roles provide a convenient way to group permissions and apply consistent access patterns across multiple keys. */
export type KeysCreateKeyRequestRolesList = Array<string>;
export const KeysCreateKeyRequestRolesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeysCreateKeyRequestRolesList>;

/** Grants specific permissions directly to this key without requiring role membership. Wildcard permissions like `documents.*` grant access to all sub-permissions including `documents.read` and `documents.write`. Direct permissions supplement any permissions inherited from assigned roles. */
export type KeysCreateKeyRequestPermissionsList = Array<string>;
export const KeysCreateKeyRequestPermissionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeysCreateKeyRequestPermissionsList>;

/** Defines time-based rate limits that protect against abuse by controlling request frequency. Unlike credits which track total usage, rate limits reset automatically after each window expires. Multiple rate limits can control different operation types with separate thresholds and windows. Essential for preventing API abuse while maintaining good performance for legitimate usage. */
export type KeysCreateKeyRequestRatelimitsList = Array<RatelimitRequest>;
export const KeysCreateKeyRequestRatelimitsList = /*@__PURE__*/ S.Array(
  RatelimitRequest,
) as any as S.Schema<KeysCreateKeyRequestRatelimitsList>;

export interface KeysCreateKeyRequest {
  /** The API namespace this key belongs to. Keys from different APIs cannot access each other. */
  apiId: string;
  /** Adds a visual identifier to the beginning of the generated key for easier recognition in logs and dashboards. The prefix becomes part of the actual key string (e.g., `prod_xxxxxxxxx`). Avoid using sensitive information in prefixes as they may appear in logs and error messages. */
  prefix?: string;
  /** Sets a human-readable identifier for internal organization and dashboard display. Never exposed to end users, only visible in management interfaces and API responses. Avoid generic names like "API Key" when managing multiple keys for the same user or service. */
  name?: string;
  /** Controls the cryptographic strength of the generated key in bytes. Higher values increase security but result in longer keys that may be more annoying to handle. The default 16 bytes provides 2^128 possible combinations, sufficient for most applications. Consider 32 bytes for highly sensitive APIs, but avoid values above 64 bytes unless specifically required. */
  byteLength?: number;
  /** Links this key to a user or entity in your system using your own identifier. Returned during verification to identify the key owner without additional database lookups. Essential for user-specific analytics, billing, and multi-tenant key management. Use your primary user ID, organization ID, or tenant ID for best results. Accepts letters, numbers, underscores, dots, and hyphens for flexible identifier formats. */
  externalId?: string;
  /** Stores arbitrary JSON metadata returned during key verification for contextual information. Eliminates additional database lookups during verification, improving performance for stateless services. Avoid storing sensitive data here as it's returned in verification responses. Large metadata objects increase verification latency and should stay under 10KB total size. */
  meta?: KeysCreateKeyRequestMetaMap;
  /** Assigns existing roles to this key for permission management through role-based access control. Roles must already exist in your workspace before assignment. During verification, all permissions from assigned roles are checked against requested permissions. Roles provide a convenient way to group permissions and apply consistent access patterns across multiple keys. */
  roles?: KeysCreateKeyRequestRolesList;
  /** Grants specific permissions directly to this key without requiring role membership. Wildcard permissions like `documents.*` grant access to all sub-permissions including `documents.read` and `documents.write`. Direct permissions supplement any permissions inherited from assigned roles. */
  permissions?: KeysCreateKeyRequestPermissionsList;
  /** Sets when this key automatically expires as a Unix timestamp in milliseconds. Verification fails with code=EXPIRED immediately after this time passes. Omitting this field creates a permanent key that never expires. Avoid setting timestamps in the past as they immediately invalidate the key. Keys expire based on server time, not client time, which prevents timezone-related issues. Essential for trial periods, temporary access, and security compliance requiring key rotation. */
  expires?: number;
  /** Controls usage-based limits through credit consumption with optional automatic refills. Unlike rate limits which control frequency, credits control total usage with global consistency. Essential for implementing usage-based pricing, subscription tiers, and hard usage quotas. Omitting this field creates unlimited usage, while setting null is not allowed during creation. */
  credits?: KeyCreditsData;
  /** Defines time-based rate limits that protect against abuse by controlling request frequency. Unlike credits which track total usage, rate limits reset automatically after each window expires. Multiple rate limits can control different operation types with separate thresholds and windows. Essential for preventing API abuse while maintaining good performance for legitimate usage. */
  ratelimits?: KeysCreateKeyRequestRatelimitsList;
  /** Controls whether the key is active immediately upon creation. When set to `false`, the key exists but all verification attempts fail with `code=DISABLED`. Useful for pre-creating keys that will be activated later or for keys requiring manual approval. Most keys should be created with `enabled=true` for immediate use. */
  enabled?: boolean;
  /** Controls whether the plaintext key is stored in an encrypted vault for later retrieval. When true, allows recovering the actual key value using keys.getKey with decrypt=true. When false, the key value cannot be retrieved after creation for maximum security. Only enable for development keys or when key recovery is absolutely necessary. */
  recoverable?: boolean;
}
export const KeysCreateKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiId: S.String,
    prefix: S.optional(S.String),
    name: S.optional(S.String),
    byteLength: S.optional(S.Number),
    externalId: S.optional(S.String),
    meta: S.optional(KeysCreateKeyRequestMetaMap),
    roles: S.optional(KeysCreateKeyRequestRolesList),
    permissions: S.optional(KeysCreateKeyRequestPermissionsList),
    expires: S.optional(S.Number),
    credits: S.optional(KeyCreditsData),
    ratelimits: S.optional(KeysCreateKeyRequestRatelimitsList),
    enabled: S.optional(S.Boolean),
    recoverable: S.optional(S.Boolean),
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.createKey", code: 200 })),
).annotate({
  identifier: "KeysCreateKeyRequest",
}) as any as S.Schema<KeysCreateKeyRequest>;

export interface V2KeysCreateKeyResponseData {
  /** The unique identifier for this key in Unkey's system. This is NOT the actual API key, but a reference ID used for management operations like updating or deleting the key. Store this ID in your database to reference the key later. This ID is not sensitive and can be logged or displayed in dashboards. */
  keyId: string;
  /** The full generated API key that should be securely provided to your user. SECURITY WARNING: This is the only time you'll receive the complete key - Unkey only stores a securely hashed version. Never log or store this value in your own systems; provide it directly to your end user via secure channels. After this API call completes, this value cannot be retrieved again (unless created with `recoverable=true`). */
  key: string;
}
export const V2KeysCreateKeyResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    key: S.String,
  }),
).annotate({
  identifier: "V2KeysCreateKeyResponseData",
}) as any as S.Schema<V2KeysCreateKeyResponseData>;

export interface V2KeysCreateKeyResponseBody {
  meta: Meta;
  data: V2KeysCreateKeyResponseData;
}
export const V2KeysCreateKeyResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2KeysCreateKeyResponseData,
  }),
).annotate({
  identifier: "V2KeysCreateKeyResponseBody",
}) as any as S.Schema<V2KeysCreateKeyResponseBody>;

export interface KeysDeleteKeyRequest {
  /** Specifies which key to delete using the database identifier returned from `createKey`. Do not confuse this with the actual API key string that users include in requests. Deletion immediately invalidates the key, causing all future verification attempts to fail with `code=NOT_FOUND`. Key deletion triggers cache invalidation across all regions but may take up to 30 seconds to fully propagate. */
  keyId: string;
  /** Controls deletion behavior between recoverable soft-deletion and irreversible permanent erasure. Soft deletion (default) preserves key data for potential recovery through direct database operations. Permanent deletion completely removes all traces including hash values and metadata with no recovery option. Use permanent deletion only for regulatory compliance (GDPR), resolving hash collisions, or when reusing identical key strings. Permanent deletion cannot be undone and may affect analytics data that references the deleted key. Most applications should use soft deletion to maintain audit trails and prevent accidental data loss. */
  permanent?: boolean;
}
export const KeysDeleteKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    permanent: S.optional(S.Boolean),
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.deleteKey", code: 200 })),
).annotate({
  identifier: "KeysDeleteKeyRequest",
}) as any as S.Schema<KeysDeleteKeyRequest>;

export interface V2KeysDeleteKeyResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2KeysDeleteKeyResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: S.Unknown,
  }),
).annotate({
  identifier: "V2KeysDeleteKeyResponseBody",
}) as any as S.Schema<V2KeysDeleteKeyResponseBody>;

export interface KeysGetKeyRequest {
  /** Specifies which key to retrieve using the database identifier returned from `keys.createKey`. Do not confuse this with the actual API key string that users include in requests. Key data includes metadata, permissions, usage statistics, and configuration but never the plaintext key value unless `decrypt=true`. Find this ID in creation responses, key listings, dashboard, or verification responses. */
  keyId: string;
  /** Controls whether to include the plaintext key value in the response for recovery purposes. Only works for keys created with `recoverable=true` and requires the `decrypt_key` permission. Returned keys must be handled securely, never logged, cached, or stored insecurely. Use only for legitimate recovery scenarios like user password resets or emergency access. Most applications should keep this false to maintain security best practices and avoid accidental key exposure. Decryption requests are audited and may trigger security alerts in enterprise environments. */
  decrypt?: boolean;
}
export const KeysGetKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    decrypt: S.optional(S.Boolean),
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.getKey", code: 200 })),
).annotate({
  identifier: "KeysGetKeyRequest",
}) as any as S.Schema<KeysGetKeyRequest>;

export interface V2KeysGetKeyResponseBody {
  meta: Meta;
  data: KeyResponseData;
}
export const V2KeysGetKeyResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: KeyResponseData,
  }),
).annotate({
  identifier: "V2KeysGetKeyResponseBody",
}) as any as S.Schema<V2KeysGetKeyResponseBody>;

/** Stores arbitrary JSON metadata returned during key verification for contextual information. Eliminates additional database lookups during verification, improving performance for stateless services. Avoid storing sensitive data here as it's returned in verification responses. Large metadata objects increase verification latency and should stay under 10KB total size. */
export type V2KeysMigrateKeyDataMetaMap = {
  [key: string]: unknown | undefined;
};
export const V2KeysMigrateKeyDataMetaMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<V2KeysMigrateKeyDataMetaMap>;

/** Assigns existing roles to this key for permission management through role-based access control. Roles must already exist in your workspace before assignment. During verification, all permissions from assigned roles are checked against requested permissions. Roles provide a convenient way to group permissions and apply consistent access patterns across multiple keys. */
export type V2KeysMigrateKeyDataRolesList = Array<string>;
export const V2KeysMigrateKeyDataRolesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<V2KeysMigrateKeyDataRolesList>;

/** Grants specific permissions directly to this key without requiring role membership. Wildcard permissions like `documents.*` grant access to all sub-permissions including `documents.read` and `documents.write`. Direct permissions supplement any permissions inherited from assigned roles. */
export type V2KeysMigrateKeyDataPermissionsList = Array<string>;
export const V2KeysMigrateKeyDataPermissionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<V2KeysMigrateKeyDataPermissionsList>;

/** Defines time-based rate limits that protect against abuse by controlling request frequency. Unlike credits which track total usage, rate limits reset automatically after each window expires. Multiple rate limits can control different operation types with separate thresholds and windows. Essential for preventing API abuse while maintaining good performance for legitimate usage. */
export type V2KeysMigrateKeyDataRatelimitsList = Array<RatelimitRequest>;
export const V2KeysMigrateKeyDataRatelimitsList = /*@__PURE__*/ S.Array(
  RatelimitRequest,
) as any as S.Schema<V2KeysMigrateKeyDataRatelimitsList>;

export interface V2KeysMigrateKeyData {
  /** The current hash of the key on your side */
  hash: string;
  /** Sets a human-readable identifier for internal organization and dashboard display. Never exposed to end users, only visible in management interfaces and API responses. Avoid generic names like "API Key" when managing multiple keys for the same user or service. */
  name?: string;
  /** Links this key to a user or entity in your system using your own identifier. Returned during verification to identify the key owner without additional database lookups. Essential for user-specific analytics, billing, and multi-tenant key management. Use your primary user ID, organization ID, or tenant ID for best results. Accepts letters, numbers, underscores, dots, and hyphens for flexible identifier formats. */
  externalId?: string;
  /** Stores arbitrary JSON metadata returned during key verification for contextual information. Eliminates additional database lookups during verification, improving performance for stateless services. Avoid storing sensitive data here as it's returned in verification responses. Large metadata objects increase verification latency and should stay under 10KB total size. */
  meta?: V2KeysMigrateKeyDataMetaMap;
  /** Assigns existing roles to this key for permission management through role-based access control. Roles must already exist in your workspace before assignment. During verification, all permissions from assigned roles are checked against requested permissions. Roles provide a convenient way to group permissions and apply consistent access patterns across multiple keys. */
  roles?: V2KeysMigrateKeyDataRolesList;
  /** Grants specific permissions directly to this key without requiring role membership. Wildcard permissions like `documents.*` grant access to all sub-permissions including `documents.read` and `documents.write`. Direct permissions supplement any permissions inherited from assigned roles. */
  permissions?: V2KeysMigrateKeyDataPermissionsList;
  /** Sets when this key automatically expires as a Unix timestamp in milliseconds. Verification fails with code=EXPIRED immediately after this time passes. Omitting this field creates a permanent key that never expires. Avoid setting timestamps in the past as they immediately invalidate the key. Keys expire based on server time, not client time, which prevents timezone-related issues. Essential for trial periods, temporary access, and security compliance requiring key rotation. */
  expires?: number;
  /** Controls whether the key is active immediately upon creation. When set to `false`, the key exists but all verification attempts fail with `code=DISABLED`. Useful for pre-creating keys that will be activated later or for keys requiring manual approval. Most keys should be created with `enabled=true` for immediate use. */
  enabled?: boolean;
  /** Controls usage-based limits through credit consumption with optional automatic refills. Unlike rate limits which control frequency, credits control total usage with global consistency. Essential for implementing usage-based pricing, subscription tiers, and hard usage quotas. Omitting this field creates unlimited usage, while setting null is not allowed during creation. */
  credits?: KeyCreditsData;
  /** Defines time-based rate limits that protect against abuse by controlling request frequency. Unlike credits which track total usage, rate limits reset automatically after each window expires. Multiple rate limits can control different operation types with separate thresholds and windows. Essential for preventing API abuse while maintaining good performance for legitimate usage. */
  ratelimits?: V2KeysMigrateKeyDataRatelimitsList;
}
export const V2KeysMigrateKeyData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    hash: S.String,
    name: S.optional(S.String),
    externalId: S.optional(S.String),
    meta: S.optional(V2KeysMigrateKeyDataMetaMap),
    roles: S.optional(V2KeysMigrateKeyDataRolesList),
    permissions: S.optional(V2KeysMigrateKeyDataPermissionsList),
    expires: S.optional(S.Number),
    enabled: S.optional(S.Boolean),
    credits: S.optional(KeyCreditsData),
    ratelimits: S.optional(V2KeysMigrateKeyDataRatelimitsList),
  }),
).annotate({
  identifier: "V2KeysMigrateKeyData",
}) as any as S.Schema<V2KeysMigrateKeyData>;

export type KeysMigrateKeysRequestKeysList = Array<V2KeysMigrateKeyData>;
export const KeysMigrateKeysRequestKeysList = /*@__PURE__*/ S.Array(
  V2KeysMigrateKeyData,
) as any as S.Schema<KeysMigrateKeysRequestKeysList>;

export interface KeysMigrateKeysRequest {
  /** Identifier of the configured migration provider/strategy to use (e.g., "your_company"). You will receive this from Unkey's support staff. */
  migrationId: string;
  /** The ID of the API that the keys should be inserted into */
  apiId: string;
  keys: KeysMigrateKeysRequestKeysList;
}
export const KeysMigrateKeysRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    migrationId: S.String,
    apiId: S.String,
    keys: KeysMigrateKeysRequestKeysList,
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.migrateKeys", code: 200 })),
).annotate({
  identifier: "KeysMigrateKeysRequest",
}) as any as S.Schema<KeysMigrateKeysRequest>;

export interface V2KeysMigrateKeysMigration {
  /** The hash provided in the migration request */
  hash: string;
  /** The unique identifier for this key in Unkey's system. This is NOT the actual API key, but a reference ID used for management operations like updating or deleting the key. Store this ID in your database to reference the key later. This ID is not sensitive and can be logged or displayed in dashboards. */
  keyId: string;
}
export const V2KeysMigrateKeysMigration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    hash: S.String,
    keyId: S.String,
  }),
).annotate({
  identifier: "V2KeysMigrateKeysMigration",
}) as any as S.Schema<V2KeysMigrateKeysMigration>;

/** Successfully migrated keys with their hash and generated keyId */
export type V2KeysMigrateKeysResponseDataMigratedList =
  Array<V2KeysMigrateKeysMigration>;
export const V2KeysMigrateKeysResponseDataMigratedList = /*@__PURE__*/ S.Array(
  V2KeysMigrateKeysMigration,
) as any as S.Schema<V2KeysMigrateKeysResponseDataMigratedList>;

/** Hashes that could not be migrated (e.g., already exist in the system) */
export type V2KeysMigrateKeysResponseDataFailedList = Array<string>;
export const V2KeysMigrateKeysResponseDataFailedList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<V2KeysMigrateKeysResponseDataFailedList>;

export interface V2KeysMigrateKeysResponseData {
  /** Successfully migrated keys with their hash and generated keyId */
  migrated: V2KeysMigrateKeysResponseDataMigratedList;
  /** Hashes that could not be migrated (e.g., already exist in the system) */
  failed: V2KeysMigrateKeysResponseDataFailedList;
}
export const V2KeysMigrateKeysResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    migrated: V2KeysMigrateKeysResponseDataMigratedList,
    failed: V2KeysMigrateKeysResponseDataFailedList,
  }),
).annotate({
  identifier: "V2KeysMigrateKeysResponseData",
}) as any as S.Schema<V2KeysMigrateKeysResponseData>;

export interface V2KeysMigrateKeysResponseBody {
  meta: Meta;
  data: V2KeysMigrateKeysResponseData;
}
export const V2KeysMigrateKeysResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2KeysMigrateKeysResponseData,
  }),
).annotate({
  identifier: "V2KeysMigrateKeysResponseBody",
}) as any as S.Schema<V2KeysMigrateKeysResponseBody>;

/** Removes direct permissions from the key without affecting role-based permissions. You can either use a permission slug, or the permission ID. After removal, verification checks for these permissions will fail unless granted through roles. */
export type KeysRemovePermissionsRequestPermissionsList = Array<string>;
export const KeysRemovePermissionsRequestPermissionsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<KeysRemovePermissionsRequestPermissionsList>;

export interface KeysRemovePermissionsRequest {
  /** Specifies which key to remove permissions from using the database identifier returned from `keys.createKey`. Do not confuse this with the actual API key string that users include in requests. */
  keyId: string;
  /** Removes direct permissions from the key without affecting role-based permissions. You can either use a permission slug, or the permission ID. After removal, verification checks for these permissions will fail unless granted through roles. */
  permissions: KeysRemovePermissionsRequestPermissionsList;
}
export const KeysRemovePermissionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    permissions: KeysRemovePermissionsRequestPermissionsList,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/keys.removePermissions", code: 200 }),
  ),
).annotate({
  identifier: "KeysRemovePermissionsRequest",
}) as any as S.Schema<KeysRemovePermissionsRequest>;

/** Complete list of all permissions directly assigned to the key after the removal operation (remaining permissions only). Notes: - This list does NOT include permissions granted through roles - For a complete permission picture, use `/v2/keys.getKey` instead - An empty array indicates the key has no direct permissions assigned - Any cached versions of the key are immediately invalidated to ensure consistency - Changes to permissions take effect within seconds for new verifications */
export type V2KeysRemovePermissionsResponseData = Array<Permission>;
export const V2KeysRemovePermissionsResponseData = /*@__PURE__*/ S.Array(
  Permission,
) as any as S.Schema<V2KeysRemovePermissionsResponseData>;

export interface V2KeysRemovePermissionsResponseBody {
  meta: Meta;
  data: V2KeysRemovePermissionsResponseData;
}
export const V2KeysRemovePermissionsResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2KeysRemovePermissionsResponseData,
  }),
).annotate({
  identifier: "V2KeysRemovePermissionsResponseBody",
}) as any as S.Schema<V2KeysRemovePermissionsResponseBody>;

/** Removes direct role assignments from the key without affecting other role sources or permissions. Operations are idempotent - removing non-assigned roles has no effect and causes no errors. After removal, the key loses access to permissions that were only granted through these roles. Invalid role references cause the entire operation to fail atomically, ensuring consistent state. */
export type KeysRemoveRolesRequestRolesList = Array<string>;
export const KeysRemoveRolesRequestRolesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeysRemoveRolesRequestRolesList>;

export interface KeysRemoveRolesRequest {
  /** Specifies which key loses the roles using the database identifier returned from createKey. Do not confuse this with the actual API key string that users include in requests. Removing roles only affects direct assignments, not permissions inherited from other sources. Role changes take effect immediately but may take up to 30 seconds to propagate across all regions. */
  keyId: string;
  /** Removes direct role assignments from the key without affecting other role sources or permissions. Operations are idempotent - removing non-assigned roles has no effect and causes no errors. After removal, the key loses access to permissions that were only granted through these roles. Invalid role references cause the entire operation to fail atomically, ensuring consistent state. */
  roles: KeysRemoveRolesRequestRolesList;
}
export const KeysRemoveRolesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    roles: KeysRemoveRolesRequestRolesList,
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.removeRoles", code: 200 })),
).annotate({
  identifier: "KeysRemoveRolesRequest",
}) as any as S.Schema<KeysRemoveRolesRequest>;

/** Complete list of all roles directly assigned to the key after the removal operation completes. The response includes: - The remaining roles still assigned to the key (after removing the specified roles) - Both ID and name for each role for easy reference Important notes: - The response reflects the current state after the removal operation - An empty array indicates the key now has no roles assigned - This only shows direct role assignments - Role permissions are not expanded in this response - use keys.getKey for full details - Changes take effect immediately for new verifications but cached sessions may retain old permissions briefly */
export type V2KeysRemoveRolesResponseData = Array<Role>;
export const V2KeysRemoveRolesResponseData = /*@__PURE__*/ S.Array(
  Role,
) as any as S.Schema<V2KeysRemoveRolesResponseData>;

export interface V2KeysRemoveRolesResponseBody {
  meta: Meta;
  data: V2KeysRemoveRolesResponseData;
}
export const V2KeysRemoveRolesResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2KeysRemoveRolesResponseData,
  }),
).annotate({
  identifier: "V2KeysRemoveRolesResponseBody",
}) as any as S.Schema<V2KeysRemoveRolesResponseBody>;

export interface KeysRerollKeyRequest {
  /** The database identifier of the key to reroll. This is the unique ID returned when creating or listing keys, NOT the actual API key token. You can find this ID in: - The response from `keys.createKey` - Key verification responses - The Unkey dashboard - API key listing endpoints */
  keyId: string;
  /** Duration in milliseconds until the ORIGINAL key is revoked, starting from now. This parameter controls the overlap period for key rotation: - Set to `0` to revoke the original key immediately - Positive values keep the original key active for the specified duration - Allows graceful migration by giving users time to update their credentials Common overlap periods: - Immediate revocation: 0 - 1 hour grace period: 3600000 - 24 hours grace period: 86400000 - 7 days grace period: 604800000 - 30 days grace period: 2592000000 */
  expiration: number;
}
export const KeysRerollKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    expiration: S.Number,
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.rerollKey", code: 200 })),
).annotate({
  identifier: "KeysRerollKeyRequest",
}) as any as S.Schema<KeysRerollKeyRequest>;

export interface V2KeysRerollKeyResponseData {
  /** The unique identifier for the newly created key. This is NOT the actual API key token, but a reference ID for management operations. Store this ID to: - Update or revoke the key later - Track the key in your database - Display in admin dashboards (safe to log) Note: This is a new ID - the original key retains its own ID. */
  keyId: string;
  /** The newly generated API key token (the actual secret that authenticates requests). **SECURITY CRITICAL:** - This is the only time you'll receive the complete key - Unkey stores only a hashed version (unless the original key was created with `recoverable=true`) - Never log, store, or expose this value in your systems - Transmit directly to the end user via secure channels only - If lost and not recoverable, you must reroll or create a new key The key format follows: `[prefix]_[random_bytes]` - Prefix is extracted from the original key or uses API default - Random bytes follow API configuration (default: 16 bytes) This is NOT the keyId - it's the actual secret token used for authentication. */
  key: string;
}
export const V2KeysRerollKeyResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    key: S.String,
  }),
).annotate({
  identifier: "V2KeysRerollKeyResponseData",
}) as any as S.Schema<V2KeysRerollKeyResponseData>;

export interface V2KeysRerollKeyResponseBody {
  meta: Meta;
  data: V2KeysRerollKeyResponseData;
}
export const V2KeysRerollKeyResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2KeysRerollKeyResponseData,
  }),
).annotate({
  identifier: "V2KeysRerollKeyResponseBody",
}) as any as S.Schema<V2KeysRerollKeyResponseBody>;

/** The permissions to set for this key. This is a complete replacement operation - it overwrites all existing direct permissions with this new set. Key behaviors: - Providing an empty array removes all direct permissions from the key - This only affects direct permissions - permissions granted through roles are not affected - All existing direct permissions not included in this list will be removed Any permissions that do not exist will be auto created if the root key has permissions, otherwise this operation will fail with a 403 error. */
export type KeysSetPermissionsRequestPermissionsList = Array<string>;
export const KeysSetPermissionsRequestPermissionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeysSetPermissionsRequestPermissionsList>;

export interface KeysSetPermissionsRequest {
  /** Specifies which key receives the additional permissions using the database identifier returned from `keys.createKey`. Do not confuse this with the actual API key string that users include in requests. */
  keyId: string;
  /** The permissions to set for this key. This is a complete replacement operation - it overwrites all existing direct permissions with this new set. Key behaviors: - Providing an empty array removes all direct permissions from the key - This only affects direct permissions - permissions granted through roles are not affected - All existing direct permissions not included in this list will be removed Any permissions that do not exist will be auto created if the root key has permissions, otherwise this operation will fail with a 403 error. */
  permissions: KeysSetPermissionsRequestPermissionsList;
}
export const KeysSetPermissionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    permissions: KeysSetPermissionsRequestPermissionsList,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/keys.setPermissions", code: 200 }),
  ),
).annotate({
  identifier: "KeysSetPermissionsRequest",
}) as any as S.Schema<KeysSetPermissionsRequest>;

/** Complete list of all permissions now directly assigned to the key after the set operation has completed. The response includes: - The comprehensive, updated set of direct permissions (reflecting the complete replacement) - Both ID and name for each permission for easy reference Important notes: - This only shows direct permissions, not those granted through roles - An empty array means the key has no direct permissions assigned - For a complete permission picture including roles, use keys.getKey instead */
export type V2KeysSetPermissionsResponseData = Array<Permission>;
export const V2KeysSetPermissionsResponseData = /*@__PURE__*/ S.Array(
  Permission,
) as any as S.Schema<V2KeysSetPermissionsResponseData>;

export interface V2KeysSetPermissionsResponseBody {
  meta: Meta;
  data: V2KeysSetPermissionsResponseData;
}
export const V2KeysSetPermissionsResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2KeysSetPermissionsResponseData,
  }),
).annotate({
  identifier: "V2KeysSetPermissionsResponseBody",
}) as any as S.Schema<V2KeysSetPermissionsResponseBody>;

/** Replaces all existing role assignments with this complete list of roles. This is a wholesale replacement operation, not an incremental update like add/remove operations. Providing an empty array removes all direct role assignments from the key. All roles must already exist in the workspace - roles cannot be created automatically. Invalid role references cause the entire operation to fail atomically, ensuring consistent state. */
export type KeysSetRolesRequestRolesList = Array<string>;
export const KeysSetRolesRequestRolesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeysSetRolesRequestRolesList>;

export interface KeysSetRolesRequest {
  /** Specifies which key gets the complete role replacement using the database identifier returned from createKey. Do not confuse this with the actual API key string that users include in requests. This is a wholesale replacement operation that removes all existing roles not included in the request. Role changes take effect immediately but may take up to 30 seconds to propagate across all regions. */
  keyId: string;
  /** Replaces all existing role assignments with this complete list of roles. This is a wholesale replacement operation, not an incremental update like add/remove operations. Providing an empty array removes all direct role assignments from the key. All roles must already exist in the workspace - roles cannot be created automatically. Invalid role references cause the entire operation to fail atomically, ensuring consistent state. */
  roles: KeysSetRolesRequestRolesList;
}
export const KeysSetRolesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    roles: KeysSetRolesRequestRolesList,
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.setRoles", code: 200 })),
).annotate({
  identifier: "KeysSetRolesRequest",
}) as any as S.Schema<KeysSetRolesRequest>;

/** Complete list of all roles now directly assigned to the key after the set operation has completed. The response includes: - The comprehensive, updated set of roles (reflecting the complete replacement) - Both ID and name for each role for easy reference Important notes: - This response shows the final state after the complete replacement - If you provided an empty array in the request, this will also be empty - This only shows direct role assignments on the key - Role permissions are not expanded in this response - use keys.getKey for complete details - An empty array indicates the key now has no roles assigned at all */
export type V2KeysSetRolesResponseData = Array<Role>;
export const V2KeysSetRolesResponseData = /*@__PURE__*/ S.Array(
  Role,
) as any as S.Schema<V2KeysSetRolesResponseData>;

export interface V2KeysSetRolesResponseBody {
  meta: Meta;
  data: V2KeysSetRolesResponseData;
}
export const V2KeysSetRolesResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2KeysSetRolesResponseData,
  }),
).annotate({
  identifier: "V2KeysSetRolesResponseBody",
}) as any as S.Schema<V2KeysSetRolesResponseBody>;

/** Defines how to modify the key's remaining credits. Use 'set' to replace current credits with a specific value or unlimited usage, 'increment' to add credits for plan upgrades or credit purchases, and 'decrement' to reduce credits for refunds or policy violations. */
export type KeysUpdateCreditsRequestOperation =
  | "set"
  | "increment"
  | "decrement";
export const KeysUpdateCreditsRequestOperation = /*@__PURE__*/ S.String;

export interface KeysUpdateCreditsRequest {
  /** The ID of the key to update (begins with `key_`). This is the database reference ID for the key, not the actual API key string that users authenticate with. This ID uniquely identifies which key's credits will be updated. */
  keyId: string;
  /** The credit value to use with the specified operation. The meaning depends on the operation: for 'set', this becomes the new remaining credits value; for 'increment', this amount is added to current credits; for 'decrement', this amount is subtracted from current credits. Set to null when using 'set' operation to make the key unlimited (removes usage restrictions entirely). When decrementing, if the result would be negative, remaining credits are automatically set to zero. Credits are consumed during successful key verification, and when credits reach zero, verification fails with `code=USAGE_EXCEEDED`. Required when using 'increment' or 'decrement' operations. Optional for 'set' operation (null creates unlimited usage). */
  value?: number | null;
  /** Defines how to modify the key's remaining credits. Use 'set' to replace current credits with a specific value or unlimited usage, 'increment' to add credits for plan upgrades or credit purchases, and 'decrement' to reduce credits for refunds or policy violations. */
  operation: KeysUpdateCreditsRequestOperation | (string & {});
}
export const KeysUpdateCreditsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    value: S.optional(S.NullOr(S.Number)),
    operation: KeysUpdateCreditsRequestOperation,
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.updateCredits", code: 200 })),
).annotate({
  identifier: "KeysUpdateCreditsRequest",
}) as any as S.Schema<KeysUpdateCreditsRequest>;

export interface V2KeysUpdateCreditsResponseBody {
  meta: Meta;
  data: KeyCreditsData;
}
export const V2KeysUpdateCreditsResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: KeyCreditsData,
  }),
).annotate({
  identifier: "V2KeysUpdateCreditsResponseBody",
}) as any as S.Schema<V2KeysUpdateCreditsResponseBody>;

/** Stores arbitrary JSON metadata returned during key verification. Omitting this field preserves existing metadata, while setting null removes all metadata entirely. Avoid storing sensitive data here as it's returned in verification responses. Large metadata objects increase verification latency and should stay under 10KB total size. */
export type KeysUpdateKeyRequestMetaMap = {
  [key: string]: unknown | undefined;
};
export const KeysUpdateKeyRequestMetaMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<KeysUpdateKeyRequestMetaMap>;

/** How often credits are automatically refilled. */
export type UpdateKeyCreditsRefillInterval = "daily" | "monthly";
export const UpdateKeyCreditsRefillInterval = /*@__PURE__*/ S.String;

/** Configuration for automatic credit refill behavior. */
export interface UpdateKeyCreditsRefill {
  /** How often credits are automatically refilled. */
  interval: UpdateKeyCreditsRefillInterval | (string & {});
  /** Number of credits to add during each refill cycle. */
  amount: number;
  /** Day of the month for monthly refills (1-31). Only required when interval is 'monthly'. For days beyond the month's length, refill occurs on the last day of the month. */
  refillDay?: number;
}
export const UpdateKeyCreditsRefill = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    interval: UpdateKeyCreditsRefillInterval,
    amount: S.Number,
    refillDay: S.optional(S.Number),
  }),
).annotate({
  identifier: "UpdateKeyCreditsRefill",
}) as any as S.Schema<UpdateKeyCreditsRefill>;

/** Credit configuration and remaining balance for this key. */
export interface UpdateKeyCreditsData {
  /** Number of credits remaining (null for unlimited). This also clears the refilling schedule. */
  remaining?: number | null;
  refill?: UpdateKeyCreditsRefill | null;
}
export const UpdateKeyCreditsData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    remaining: S.optional(S.NullOr(S.Number)),
    refill: S.optional(S.NullOr(UpdateKeyCreditsRefill)),
  }),
).annotate({
  identifier: "UpdateKeyCreditsData",
}) as any as S.Schema<UpdateKeyCreditsData>;

/** Defines time-based rate limits that protect against abuse by controlling request frequency. Omitting this field preserves existing rate limits, while setting null removes all rate limits. Unlike credits which track total usage, rate limits reset automatically after each window expires. Multiple rate limits can control different operation types with separate thresholds and windows. */
export type KeysUpdateKeyRequestRatelimitsList = Array<RatelimitRequest>;
export const KeysUpdateKeyRequestRatelimitsList = /*@__PURE__*/ S.Array(
  RatelimitRequest,
) as any as S.Schema<KeysUpdateKeyRequestRatelimitsList>;

export type KeysUpdateKeyRequestRolesList = Array<string>;
export const KeysUpdateKeyRequestRolesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeysUpdateKeyRequestRolesList>;

export type KeysUpdateKeyRequestPermissionsList = Array<string>;
export const KeysUpdateKeyRequestPermissionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeysUpdateKeyRequestPermissionsList>;

export interface KeysUpdateKeyRequest {
  /** Specifies which key to update using the database identifier returned from `createKey`. Do not confuse this with the actual API key string that users include in requests. */
  keyId: string;
  /** Sets a human-readable name for internal organization and identification. Omitting this field leaves the current name unchanged, while setting null removes it entirely. Avoid generic names like "API Key" when managing multiple keys per user or service. */
  name?: string | null;
  /** Links this key to a user or entity in your system for ownership tracking during verification. Omitting this field preserves the current association, while setting null disconnects the key from any identity. Essential for user-specific analytics, billing, and key management across multiple users. Supports letters, numbers, underscores, dots, and hyphens for flexible identifier formats. */
  externalId?: string | null;
  /** Stores arbitrary JSON metadata returned during key verification. Omitting this field preserves existing metadata, while setting null removes all metadata entirely. Avoid storing sensitive data here as it's returned in verification responses. Large metadata objects increase verification latency and should stay under 10KB total size. */
  meta?: KeysUpdateKeyRequestMetaMap | null;
  /** Sets when this key automatically expires as a Unix timestamp in milliseconds. Verification fails with code=EXPIRED immediately after this time passes. Omitting this field preserves the current expiration, while setting null makes the key permanent. Avoid setting timestamps in the past as they immediately invalidate the key. Keys expire based on server time, not client time, which prevents timezone-related issues. Active sessions continue until their next verification attempt after expiry. */
  expires?: number | null;
  /** Controls usage-based limits for this key through credit consumption. Omitting this field preserves current credit settings, while setting null enables unlimited usage. Cannot configure refill settings when credits is null, and refillDay requires monthly interval. Essential for implementing usage-based pricing and subscription quotas. */
  credits?: UpdateKeyCreditsData | null;
  /** Defines time-based rate limits that protect against abuse by controlling request frequency. Omitting this field preserves existing rate limits, while setting null removes all rate limits. Unlike credits which track total usage, rate limits reset automatically after each window expires. Multiple rate limits can control different operation types with separate thresholds and windows. */
  ratelimits?: KeysUpdateKeyRequestRatelimitsList | null;
  /** Controls whether the key is currently active for verification requests. When set to `false`, all verification attempts fail with `code=DISABLED` regardless of other settings. Omitting this field preserves the current enabled status. Useful for temporarily suspending access during billing issues, security incidents, or maintenance windows without losing key configuration. */
  enabled?: boolean;
  roles?: KeysUpdateKeyRequestRolesList;
  permissions?: KeysUpdateKeyRequestPermissionsList;
}
export const KeysUpdateKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    name: S.optional(S.NullOr(S.String)),
    externalId: S.optional(S.NullOr(S.String)),
    meta: S.optional(S.NullOr(KeysUpdateKeyRequestMetaMap)),
    expires: S.optional(S.NullOr(S.Number)),
    credits: S.optional(S.NullOr(UpdateKeyCreditsData)),
    ratelimits: S.optional(S.NullOr(KeysUpdateKeyRequestRatelimitsList)),
    enabled: S.optional(S.Boolean),
    roles: S.optional(KeysUpdateKeyRequestRolesList),
    permissions: S.optional(KeysUpdateKeyRequestPermissionsList),
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.updateKey", code: 200 })),
).annotate({
  identifier: "KeysUpdateKeyRequest",
}) as any as S.Schema<KeysUpdateKeyRequest>;

export interface V2KeysUpdateKeyResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2KeysUpdateKeyResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: S.Unknown,
  }),
).annotate({
  identifier: "V2KeysUpdateKeyResponseBody",
}) as any as S.Schema<V2KeysUpdateKeyResponseBody>;

/** Attaches metadata tags for analytics and monitoring without affecting verification outcomes. Enables segmentation of API usage in dashboards by endpoint, client version, region, or custom dimensions. Use 'key=value' format for compatibility with most analytics tools and clear categorization. Avoid including sensitive data in tags as they may appear in logs and analytics reports. */
export type KeysVerifyKeyRequestTagsList = Array<string>;
export const KeysVerifyKeyRequestTagsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<KeysVerifyKeyRequestTagsList>;

/** Controls credit consumption for usage-based billing and quota enforcement. Omitting this field uses the default cost of 1 credit per verification. Credits provide globally consistent usage tracking, essential for paid APIs with strict quotas. */
export interface KeysVerifyKeyCredits {
  /** Sets how many credits to deduct for this verification request. Use 0 for read-only operations or free tier access, higher values for premium features. Credits are deducted after all security checks pass. Essential for implementing usage-based pricing with different operation costs. */
  cost: number;
}
export const KeysVerifyKeyCredits = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cost: S.Number,
  }),
).annotate({
  identifier: "KeysVerifyKeyCredits",
}) as any as S.Schema<KeysVerifyKeyCredits>;

export interface KeysVerifyKeyRatelimit {
  /** References an existing ratelimit by its name. Key Ratelimits will take precedence over identifier-based limits. */
  name: string;
  /** Optionally override how expensive this operation is and how many tokens are deducted from the current limit. */
  cost?: number;
  /** Optionally override the maximum number of requests allowed within the specified interval. */
  limit?: number;
  /** Optionally override the duration of the rate limit window duration. */
  duration?: number;
}
export const KeysVerifyKeyRatelimit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    cost: S.optional(S.Number),
    limit: S.optional(S.Number),
    duration: S.optional(S.Number),
  }),
).annotate({
  identifier: "KeysVerifyKeyRatelimit",
}) as any as S.Schema<KeysVerifyKeyRatelimit>;

/** Enforces time-based rate limiting during verification to prevent abuse and ensure fair usage. Omitting this field skips rate limit checks entirely, relying only on configured key rate limits. Multiple rate limits can be checked simultaneously, each with different costs and temporary overrides. Rate limit checks are optimized for performance but may allow brief bursts during high concurrency. */
export type KeysVerifyKeyRequestRatelimitsList = Array<KeysVerifyKeyRatelimit>;
export const KeysVerifyKeyRequestRatelimitsList = /*@__PURE__*/ S.Array(
  KeysVerifyKeyRatelimit,
) as any as S.Schema<KeysVerifyKeyRequestRatelimitsList>;

export interface KeysVerifyKeyRequest {
  /** The API key to verify, exactly as provided by your user. Include any prefix - even small changes will cause verification to fail. */
  key: string;
  /** Attaches metadata tags for analytics and monitoring without affecting verification outcomes. Enables segmentation of API usage in dashboards by endpoint, client version, region, or custom dimensions. Use 'key=value' format for compatibility with most analytics tools and clear categorization. Avoid including sensitive data in tags as they may appear in logs and analytics reports. */
  tags?: KeysVerifyKeyRequestTagsList;
  /** Checks if the key has the specified permission(s) using a query syntax. Supports single permissions, logical operators (AND, OR), and parentheses for grouping. Examples: - Single permission: "documents.read" - Multiple permissions: "documents.read AND documents.write" - Complex queries: "(documents.read OR documents.write) AND users.view" Verification fails if the key lacks the required permissions through direct assignment or role inheritance. */
  permissions?: string;
  credits?: KeysVerifyKeyCredits;
  /** Enforces time-based rate limiting during verification to prevent abuse and ensure fair usage. Omitting this field skips rate limit checks entirely, relying only on configured key rate limits. Multiple rate limits can be checked simultaneously, each with different costs and temporary overrides. Rate limit checks are optimized for performance but may allow brief bursts during high concurrency. */
  ratelimits?: KeysVerifyKeyRequestRatelimitsList;
  /** Migrate keys on demand from your previous system. Reach out for migration support at support@unkey.dev */
  migrationId?: string;
}
export const KeysVerifyKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.String,
    tags: S.optional(KeysVerifyKeyRequestTagsList),
    permissions: S.optional(S.String),
    credits: S.optional(KeysVerifyKeyCredits),
    ratelimits: S.optional(KeysVerifyKeyRequestRatelimitsList),
    migrationId: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.verifyKey", code: 200 })),
).annotate({
  identifier: "KeysVerifyKeyRequest",
}) as any as S.Schema<KeysVerifyKeyRequest>;

/** A machine-readable code indicating the verification status or failure reason. Values: `VALID` (key is valid and passed all checks), `NOT_FOUND` (key doesn't exist or belongs to wrong API), `FORBIDDEN` (key lacks required permissions), `INSUFFICIENT_PERMISSIONS` (key lacks specific required permissions for this request), `USAGE_EXCEEDED` (key has no remaining credits), `RATE_LIMITED` (key exceeded rate limits), `DISABLED` (key was explicitly disabled), `EXPIRED` (key has passed its expiration date). */
export type V2KeysVerifyKeyResponseDataCode =
  | "VALID"
  | "NOT_FOUND"
  | "FORBIDDEN"
  | "INSUFFICIENT_PERMISSIONS"
  | "USAGE_EXCEEDED"
  | "RATE_LIMITED"
  | "DISABLED"
  | "EXPIRED";
export const V2KeysVerifyKeyResponseDataCode = /*@__PURE__*/ S.String;

/** Custom metadata associated with the key. This can include any JSON-serializable data you stored with the key during creation or updates, such as plan information, feature flags, or user details. Use this to avoid additional database lookups for contextual information needed during API calls. */
export type V2KeysVerifyKeyResponseDataMetaMap = {
  [key: string]: unknown | undefined;
};
export const V2KeysVerifyKeyResponseDataMetaMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<V2KeysVerifyKeyResponseDataMetaMap>;

/** A list of all permission names assigned to this key, either directly or through roles. These permissions determine what actions the key can perform. Only returned when permissions were checked during verification or when the key fails with `code=FORBIDDEN`. */
export type V2KeysVerifyKeyResponseDataPermissionsList = Array<string>;
export const V2KeysVerifyKeyResponseDataPermissionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<V2KeysVerifyKeyResponseDataPermissionsList>;

/** A list of all role names assigned to this key. Roles are collections of permissions that grant access to specific functionality. Only returned when permissions were checked during verification. */
export type V2KeysVerifyKeyResponseDataRolesList = Array<string>;
export const V2KeysVerifyKeyResponseDataRolesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<V2KeysVerifyKeyResponseDataRolesList>;

export interface VerifyKeyRatelimitData {
  /** Whether the rate limit was exceeded. */
  exceeded: boolean;
  /** Unique identifier for this rate limit configuration. */
  id: string;
  /** Human-readable name for this rate limit. */
  name: string;
  /** Maximum requests allowed within the time window. */
  limit: number;
  /** Rate limit window duration in milliseconds. */
  duration: number;
  /** Rate limit reset duration in milliseconds. */
  reset: number;
  /** Rate limit remaining requests within the time window. */
  remaining: number;
  /** Whether this rate limit should be automatically applied when verifying keys. When true, we will automatically apply this limit during verification without it being explicitly listed. */
  autoApply: boolean;
}
export const VerifyKeyRatelimitData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exceeded: S.Boolean,
    id: S.String,
    name: S.String,
    limit: S.Number,
    duration: S.Number,
    reset: S.Number,
    remaining: S.Number,
    autoApply: S.Boolean,
  }),
).annotate({
  identifier: "VerifyKeyRatelimitData",
}) as any as S.Schema<VerifyKeyRatelimitData>;

export type V2KeysVerifyKeyResponseDataRatelimitsList =
  Array<VerifyKeyRatelimitData>;
export const V2KeysVerifyKeyResponseDataRatelimitsList = /*@__PURE__*/ S.Array(
  VerifyKeyRatelimitData,
) as any as S.Schema<V2KeysVerifyKeyResponseDataRatelimitsList>;

export interface V2KeysVerifyKeyResponseData {
  /** The primary verification result. If true, the key is valid and can be used. If false, check the 'code' field to understand why verification failed. Your application should always check this field first before proceeding. */
  valid: boolean;
  /** A machine-readable code indicating the verification status or failure reason. Values: `VALID` (key is valid and passed all checks), `NOT_FOUND` (key doesn't exist or belongs to wrong API), `FORBIDDEN` (key lacks required permissions), `INSUFFICIENT_PERMISSIONS` (key lacks specific required permissions for this request), `USAGE_EXCEEDED` (key has no remaining credits), `RATE_LIMITED` (key exceeded rate limits), `DISABLED` (key was explicitly disabled), `EXPIRED` (key has passed its expiration date). */
  code: V2KeysVerifyKeyResponseDataCode;
  /** The unique identifier of the verified key in Unkey's system. Use this ID for operations like updating or revoking the key. This field is returned for both valid and invalid keys (except when `code=NOT_FOUND`). */
  keyId?: string;
  /** The human-readable name assigned to this key during creation. This is useful for displaying in logs or admin interfaces to identify the key's purpose. */
  name?: string;
  /** Custom metadata associated with the key. This can include any JSON-serializable data you stored with the key during creation or updates, such as plan information, feature flags, or user details. Use this to avoid additional database lookups for contextual information needed during API calls. */
  meta?: V2KeysVerifyKeyResponseDataMetaMap;
  /** Unix timestamp (in milliseconds) when the key will expire. If omitted, the key has no expiration. You can use this to warn users about upcoming expirations or to understand the validity period. */
  expires?: number;
  /** The number of requests/credits remaining for this key. If omitted, the key has unlimited usage. This value decreases with each verification (based on the 'cost' parameter) unless explicit credit refills are configured. */
  credits?: number;
  /** Indicates if the key is currently enabled. Disabled keys will always fail verification with `code=DISABLED`. This is useful for implementing temporary suspensions without deleting the key. */
  enabled?: boolean;
  /** A list of all permission names assigned to this key, either directly or through roles. These permissions determine what actions the key can perform. Only returned when permissions were checked during verification or when the key fails with `code=FORBIDDEN`. */
  permissions?: V2KeysVerifyKeyResponseDataPermissionsList;
  /** A list of all role names assigned to this key. Roles are collections of permissions that grant access to specific functionality. Only returned when permissions were checked during verification. */
  roles?: V2KeysVerifyKeyResponseDataRolesList;
  /** Information about the identity associated with this key. Identities allow multiple keys to share resources (like rate limits) and represent the same user or entity across different applications or devices. */
  identity?: Identity;
  ratelimits?: V2KeysVerifyKeyResponseDataRatelimitsList;
}
export const V2KeysVerifyKeyResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    valid: S.Boolean,
    code: V2KeysVerifyKeyResponseDataCode,
    keyId: S.optional(S.String),
    name: S.optional(S.String),
    meta: S.optional(V2KeysVerifyKeyResponseDataMetaMap),
    expires: S.optional(S.Number),
    credits: S.optional(S.Number),
    enabled: S.optional(S.Boolean),
    permissions: S.optional(V2KeysVerifyKeyResponseDataPermissionsList),
    roles: S.optional(V2KeysVerifyKeyResponseDataRolesList),
    identity: S.optional(Identity),
    ratelimits: S.optional(V2KeysVerifyKeyResponseDataRatelimitsList),
  }),
).annotate({
  identifier: "V2KeysVerifyKeyResponseData",
}) as any as S.Schema<V2KeysVerifyKeyResponseData>;

export interface V2KeysVerifyKeyResponseBody {
  meta: Meta;
  data: V2KeysVerifyKeyResponseData;
}
export const V2KeysVerifyKeyResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2KeysVerifyKeyResponseData,
  }),
).annotate({
  identifier: "V2KeysVerifyKeyResponseBody",
}) as any as S.Schema<V2KeysVerifyKeyResponseBody>;

export interface KeysWhoamiRequest {
  /** The complete API key string provided by you, including any prefix. Never log, cache, or store API keys in your system as they provide full access to user resources. Include the full key exactly as provided - even minor modifications will cause a not found error. */
  key: string;
}
export const KeysWhoamiRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/v2/keys.whoami", code: 200 })),
).annotate({
  identifier: "KeysWhoamiRequest",
}) as any as S.Schema<KeysWhoamiRequest>;

export interface V2KeysWhoamiResponseBody {
  meta: Meta;
  data: KeyResponseData;
}
export const V2KeysWhoamiResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: KeyResponseData,
  }),
).annotate({
  identifier: "V2KeysWhoamiResponseBody",
}) as any as S.Schema<V2KeysWhoamiResponseBody>;

export interface LivenessRequest {}
export const LivenessRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/v2/liveness", code: 200 })),
).annotate({
  identifier: "LivenessRequest",
}) as any as S.Schema<LivenessRequest>;

/** Response data for the liveness check endpoint. This provides a simple indication of whether the Unkey API service is running and able to process requests. Monitoring systems can use this endpoint to track service availability and trigger alerts if the service becomes unhealthy. */
export interface V2LivenessResponseData {
  /** Status message indicating the health of the service. A value of 'OK' indicates that the service is functioning properly and ready to accept requests. Any other value indicates a potential issue with the service health. */
  message: string;
}
export const V2LivenessResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    message: S.String,
  }),
).annotate({
  identifier: "V2LivenessResponseData",
}) as any as S.Schema<V2LivenessResponseData>;

export interface V2LivenessResponseBody {
  meta: Meta;
  data: V2LivenessResponseData;
}
export const V2LivenessResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2LivenessResponseData,
  }),
).annotate({
  identifier: "V2LivenessResponseBody",
}) as any as S.Schema<V2LivenessResponseBody>;

export interface PermissionsCreatePermissionRequest {
  /** Creates a permission with this human-readable name that describes its purpose. Names must be unique within your workspace to prevent conflicts during assignment. Use clear, semantic names that developers can easily understand when building authorization logic. Consider using hierarchical naming conventions like 'resource.action' for better organization. Examples: 'users.read', 'billing.write', 'analytics.view', 'admin.manage' */
  name: string;
  /** Creates a URL-safe identifier for this permission that can be used in APIs and integrations. Must start with a letter and contain only letters, numbers, periods, underscores, and hyphens. Slugs are often used in REST endpoints, configuration files, and external integrations. Should closely match the name but in a format suitable for technical usage. Must be unique within your workspace to ensure reliable permission lookups. Keep slugs concise but descriptive for better developer experience. */
  slug: string;
  /** Provides detailed documentation of what this permission grants access to. Include information about affected resources, allowed actions, and any important limitations. This internal documentation helps team members understand permission scope and security implications. Not visible to end users - designed for development teams and security audits. Consider documenting: - What resources can be accessed - What operations are permitted - Any conditions or limitations - Related permissions that might be needed */
  description?: string;
}
export const PermissionsCreatePermissionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    slug: S.String,
    description: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/permissions.createPermission",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PermissionsCreatePermissionRequest",
}) as any as S.Schema<PermissionsCreatePermissionRequest>;

export interface V2PermissionsCreatePermissionResponseData {
  /** The unique identifier assigned to the newly created permission. Use this ID to reference the permission in role assignments, key operations, and other API calls. Always begins with 'perm_' followed by a unique alphanumeric sequence. Store this ID if you need to manage or reference this permission in future operations. */
  permissionId: string;
}
export const V2PermissionsCreatePermissionResponseData =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      permissionId: S.String,
    }),
  ).annotate({
    identifier: "V2PermissionsCreatePermissionResponseData",
  }) as any as S.Schema<V2PermissionsCreatePermissionResponseData>;

export interface V2PermissionsCreatePermissionResponseBody {
  meta: Meta;
  data: V2PermissionsCreatePermissionResponseData;
}
export const V2PermissionsCreatePermissionResponseBody =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      meta: Meta,
      data: V2PermissionsCreatePermissionResponseData,
    }),
  ).annotate({
    identifier: "V2PermissionsCreatePermissionResponseBody",
  }) as any as S.Schema<V2PermissionsCreatePermissionResponseBody>;

/** Permission slugs to attach to the role. Existing permissions are reused. Missing permissions are created automatically when the root key has `rbac.*.create_permission`. Omit this field or provide an empty array to create the role without permissions. */
export type PermissionsCreateRoleRequestPermissionsList = Array<string>;
export const PermissionsCreateRoleRequestPermissionsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<PermissionsCreateRoleRequestPermissionsList>;

export interface PermissionsCreateRoleRequest {
  /** The unique name for this role. Must be unique within your workspace and clearly indicate the role's purpose. Use descriptive names like 'admin', 'editor', or 'Billing Manager'. Examples: 'admin.billing', 'support.readonly', 'developer.api', 'Billing Manager' */
  name: string;
  /** Provides comprehensive documentation of what this role encompasses and what access it grants. Include information about the intended use case, what permissions should be assigned, and any important considerations. This internal documentation helps team members understand role boundaries and security implications. Not visible to end users - designed for administration teams and access control audits. Consider documenting: - The role's intended purpose and scope - What types of users should receive this role - What permissions are typically associated with it - Any security considerations or limitations - Related roles that might be used together */
  description?: string;
  /** Permission slugs to attach to the role. Existing permissions are reused. Missing permissions are created automatically when the root key has `rbac.*.create_permission`. Omit this field or provide an empty array to create the role without permissions. */
  permissions?: PermissionsCreateRoleRequestPermissionsList;
}
export const PermissionsCreateRoleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    description: S.optional(S.String),
    permissions: S.optional(PermissionsCreateRoleRequestPermissionsList),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/permissions.createRole", code: 200 }),
  ),
).annotate({
  identifier: "PermissionsCreateRoleRequest",
}) as any as S.Schema<PermissionsCreateRoleRequest>;

export interface V2PermissionsCreateRoleResponseData {
  /** The unique identifier assigned to the newly created role. Use this ID to reference the role in permission assignments, key operations, and role management calls. Always begins with 'role_' followed by a unique alphanumeric sequence. Store this ID if you need to manage, modify, or assign this role in future operations. */
  roleId: string;
}
export const V2PermissionsCreateRoleResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    roleId: S.String,
  }),
).annotate({
  identifier: "V2PermissionsCreateRoleResponseData",
}) as any as S.Schema<V2PermissionsCreateRoleResponseData>;

export interface V2PermissionsCreateRoleResponseBody {
  meta: Meta;
  data: V2PermissionsCreateRoleResponseData;
}
export const V2PermissionsCreateRoleResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2PermissionsCreateRoleResponseData,
  }),
).annotate({
  identifier: "V2PermissionsCreateRoleResponseBody",
}) as any as S.Schema<V2PermissionsCreateRoleResponseBody>;

export interface PermissionsDeletePermissionRequest {
  /** Specifies which permission to permanently delete from your workspace. This can be a permission ID or a permission slug. WARNING: Deleting a permission has immediate and irreversible consequences: - All API keys with this permission will lose that access immediately - All roles containing this permission will have it removed - Any verification requests checking for this permission will fail - This action cannot be undone Before deletion, ensure you: - Have updated any keys or roles that depend on this permission - Have migrated to alternative permissions if needed - Have notified affected users about the access changes */
  permission: string;
}
export const PermissionsDeletePermissionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    permission: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/permissions.deletePermission",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PermissionsDeletePermissionRequest",
}) as any as S.Schema<PermissionsDeletePermissionRequest>;

export interface V2PermissionsDeletePermissionResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2PermissionsDeletePermissionResponseBody =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      meta: Meta,
      data: S.Unknown,
    }),
  ).annotate({
    identifier: "V2PermissionsDeletePermissionResponseBody",
  }) as any as S.Schema<V2PermissionsDeletePermissionResponseBody>;

export interface PermissionsDeleteRoleRequest {
  /** Unique identifier of the role to permanently delete from your workspace. Must either be a valid role ID that begins with 'role_' or the given role name and exists within your workspace. WARNING: Deletion is immediate and irreversible with significant consequences: - All API keys assigned this role will lose the associated permissions - Access to resources protected by this role's permissions will be denied - Any authorization logic depending on this role will start failing - Historical analytics referencing this role remain intact Before deletion, ensure: - You've updated any dependent authorization logic or code - You've migrated any keys to use alternative roles or direct permissions - You've notified relevant team members of the access changes */
  role: string;
}
export const PermissionsDeleteRoleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    role: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/permissions.deleteRole", code: 200 }),
  ),
).annotate({
  identifier: "PermissionsDeleteRoleRequest",
}) as any as S.Schema<PermissionsDeleteRoleRequest>;

export interface V2PermissionsDeleteRoleResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2PermissionsDeleteRoleResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: S.Unknown,
  }),
).annotate({
  identifier: "V2PermissionsDeleteRoleResponseBody",
}) as any as S.Schema<V2PermissionsDeleteRoleResponseBody>;

export interface PermissionsGetPermissionRequest {
  /** The unique identifier of the permission to retrieve. Must be a valid permission ID that begins with 'perm_' and exists within your workspace. */
  permission: string;
}
export const PermissionsGetPermissionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    permission: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/permissions.getPermission", code: 200 }),
  ),
).annotate({
  identifier: "PermissionsGetPermissionRequest",
}) as any as S.Schema<PermissionsGetPermissionRequest>;

export interface V2PermissionsGetPermissionResponseBody {
  meta: Meta;
  data: Permission;
}
export const V2PermissionsGetPermissionResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: Permission,
    }),
).annotate({
  identifier: "V2PermissionsGetPermissionResponseBody",
}) as any as S.Schema<V2PermissionsGetPermissionResponseBody>;

export interface PermissionsGetRoleRequest {
  /** Unique identifier of the role to permanently delete from your workspace. Must either be a valid role ID that begins with 'role_' or the given role name and exists within your workspace. Use this endpoint to verify role details, check its current permissions, or retrieve metadata. Returns complete role information including all assigned permissions for comprehensive access review. */
  role: string;
}
export const PermissionsGetRoleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    role: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/permissions.getRole", code: 200 }),
  ),
).annotate({
  identifier: "PermissionsGetRoleRequest",
}) as any as S.Schema<PermissionsGetRoleRequest>;

export interface V2PermissionsGetRoleResponseBody {
  meta: Meta;
  data: Role;
}
export const V2PermissionsGetRoleResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: Role,
  }),
).annotate({
  identifier: "V2PermissionsGetRoleResponseBody",
}) as any as S.Schema<V2PermissionsGetRoleResponseBody>;

export interface PermissionsListPermissionsRequest {
  /** Pagination cursor from a previous response to fetch the next page of permissions. Include this value when you need to retrieve additional permissions beyond the initial response. Each response containing more results than the requested limit includes a cursor for subsequent pages. Leave empty or omit this field to start from the beginning of the permission list. Cursors are temporary and may expire - always handle cases where a cursor becomes invalid. */
  cursor?: string;
  /** Maximum number of permissions to return in a single response. */
  limit?: number;
  /** Free-form text to filter permissions. Returns permissions whose ID, name, slug, or description contains the search string. Matching is case-insensitive. */
  search?: string;
}
export const PermissionsListPermissionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cursor: S.optional(S.String),
    limit: S.optional(S.Number),
    search: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/permissions.listPermissions",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PermissionsListPermissionsRequest",
}) as any as S.Schema<PermissionsListPermissionsRequest>;

/** Array of permission objects with complete configuration details. */
export type V2PermissionsListPermissionsResponseData = Array<Permission>;
export const V2PermissionsListPermissionsResponseData = /*@__PURE__*/ S.Array(
  Permission,
) as any as S.Schema<V2PermissionsListPermissionsResponseData>;

export interface V2PermissionsListPermissionsResponseBody {
  meta: Meta;
  data: V2PermissionsListPermissionsResponseData;
  pagination: Pagination;
}
export const V2PermissionsListPermissionsResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: V2PermissionsListPermissionsResponseData,
      pagination: Pagination,
    }),
).annotate({
  identifier: "V2PermissionsListPermissionsResponseBody",
}) as any as S.Schema<V2PermissionsListPermissionsResponseBody>;

export interface PermissionsListRolesRequest {
  /** Maximum number of roles to return in a single response. Use smaller values for faster response times and better UI performance. Use larger values when you need to process many roles efficiently. Results exceeding this limit will be paginated with a cursor for continuation. */
  limit?: number;
  /** Pagination cursor from a previous response to fetch the next page of roles. Include this when you need to retrieve additional roles beyond the first page. Each response containing more results will include a cursor value that can be used here. Leave empty or omit this field to start from the beginning of the role list. */
  cursor?: string;
  /** Free-form text to filter roles. Returns roles whose ID, name, or description contains the search string. Matching is case-insensitive. */
  search?: string;
}
export const PermissionsListRolesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    limit: S.optional(S.Number),
    cursor: S.optional(S.String),
    search: S.optional(S.String),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/permissions.listRoles", code: 200 }),
  ),
).annotate({
  identifier: "PermissionsListRolesRequest",
}) as any as S.Schema<PermissionsListRolesRequest>;

/** Array of roles with their assigned permissions. */
export type V2PermissionsListRolesResponseData = Array<Role>;
export const V2PermissionsListRolesResponseData = /*@__PURE__*/ S.Array(
  Role,
) as any as S.Schema<V2PermissionsListRolesResponseData>;

export interface V2PermissionsListRolesResponseBody {
  meta: Meta;
  data: V2PermissionsListRolesResponseData;
  pagination: Pagination;
}
export const V2PermissionsListRolesResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2PermissionsListRolesResponseData,
    pagination: Pagination,
  }),
).annotate({
  identifier: "V2PermissionsListRolesResponseBody",
}) as any as S.Schema<V2PermissionsListRolesResponseBody>;

/** The complete set of permission slugs to assign directly to the role. Missing permissions are created when authorized. An empty array clears all direct permissions. */
export type PermissionsSetRolePermissionsRequestPermissionsList = Array<string>;
export const PermissionsSetRolePermissionsRequestPermissionsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<PermissionsSetRolePermissionsRequestPermissionsList>;

export interface PermissionsSetRolePermissionsRequest {
  /** The ID of the role whose directly assigned permissions will be replaced. */
  roleId: string;
  /** The complete set of permission slugs to assign directly to the role. Missing permissions are created when authorized. An empty array clears all direct permissions. */
  permissions: PermissionsSetRolePermissionsRequestPermissionsList;
}
export const PermissionsSetRolePermissionsRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      roleId: S.String,
      permissions: PermissionsSetRolePermissionsRequestPermissionsList,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/v2/permissions.setRolePermissions",
        code: 200,
      }),
    ),
).annotate({
  identifier: "PermissionsSetRolePermissionsRequest",
}) as any as S.Schema<PermissionsSetRolePermissionsRequest>;

/** Complete list of permissions now directly assigned to the role. */
export type V2PermissionsSetRolePermissionsResponseData = Array<Permission>;
export const V2PermissionsSetRolePermissionsResponseData =
  /*@__PURE__*/ S.Array(
    Permission,
  ) as any as S.Schema<V2PermissionsSetRolePermissionsResponseData>;

export interface V2PermissionsSetRolePermissionsResponseBody {
  meta: Meta;
  data: V2PermissionsSetRolePermissionsResponseData;
}
export const V2PermissionsSetRolePermissionsResponseBody =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      meta: Meta,
      data: V2PermissionsSetRolePermissionsResponseData,
    }),
  ).annotate({
    identifier: "V2PermissionsSetRolePermissionsResponseBody",
  }) as any as S.Schema<V2PermissionsSetRolePermissionsResponseBody>;

export interface PortalCreatePortalRequest {
  /** URL-safe handle for this portal, unique within your workspace. Lowercase alphanumerics and single hyphens, 3-64 characters, not starting or ending with a hyphen. */
  slug: string;
  /** Human-readable name your end users see in the portal header and page titles. Unlike `slug` it is not part of any URL, so it can be changed freely. */
  displayName: string;
  keyspaceId?: string;
  appId?: string;
  /** Whether sessions can be minted for this portal immediately. Defaults to true; send false to create it dormant and enable it later. */
  enabled?: boolean;
  /** Absolute `https://` URL of the logo shown in the portal header. Loaded by your end users' browsers, so the host you name receives their IP and user agent on every page view. */
  logoUrl?: string;
  /** Six-digit hex colour used for primary actions and accents in the portal. */
  primaryColor?: string;
}
export const PortalCreatePortalRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    slug: S.String,
    displayName: S.String,
    keyspaceId: S.optional(S.String),
    appId: S.optional(S.String),
    enabled: S.optional(S.Boolean),
    logoUrl: S.optional(S.String),
    primaryColor: S.optional(S.String),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/portal.createPortal", code: 200 }),
  ),
).annotate({
  identifier: "PortalCreatePortalRequest",
}) as any as S.Schema<PortalCreatePortalRequest>;

export interface V2PortalCreatePortalResponseData {
  /** The unique identifier of the newly created portal, generated by Unkey. Always begins with 'pc_' followed by a unique alphanumeric sequence. */
  portalId: string;
}
export const V2PortalCreatePortalResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    portalId: S.String,
  }),
).annotate({
  identifier: "V2PortalCreatePortalResponseData",
}) as any as S.Schema<V2PortalCreatePortalResponseData>;

export interface V2PortalCreatePortalResponseBody {
  meta: Meta;
  data: V2PortalCreatePortalResponseData;
}
export const V2PortalCreatePortalResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2PortalCreatePortalResponseData,
  }),
).annotate({
  identifier: "V2PortalCreatePortalResponseBody",
}) as any as S.Schema<V2PortalCreatePortalResponseBody>;

export type PortalCreateSessionRequestScopesItem =
  | "keys:read"
  | "keys:create"
  | "keys:reroll"
  | "analytics:read";
export const PortalCreateSessionRequestScopesItem = /*@__PURE__*/ S.String;

/** The capabilities granted to the end user in the Portal, from a fixed vocabulary. All capabilities are scoped to this end user: key capabilities (`keys:*`) apply only to keys the end user owns within the keyspace configured on the portal, and `analytics:read` returns only the end user's own verification events. An end user can never see another identity's keys or analytics. Tab visibility is derived from the scopes: - Keys tab: any `keys:*` scope - Analytics tab: `analytics:read` - Docs tab: visible when any scope is present `keys:create` is accepted but has no portal route behind it yet. It is still authorized like the others, so a session minted with it required `create_key` on the keyspace at mint time, and a future portal create-key route inherits an enforced ceiling rather than trusting sessions minted while the capability was inert. Each scope requires the equivalent permission on your own root key. See Required Permissions on this operation. */
export type PortalCreateSessionRequestScopesList = Array<
  PortalCreateSessionRequestScopesItem | (string & {})
>;
export const PortalCreateSessionRequestScopesList = /*@__PURE__*/ S.Array(
  PortalCreateSessionRequestScopesItem,
) as any as S.Schema<PortalCreateSessionRequestScopesList>;

export interface PortalCreateSessionRequest {
  portal: string;
  /** The end user's identifier in the customer's system. Accepts arbitrary string values (user IDs, emails, UUIDs, etc.). */
  externalId: string;
  /** The capabilities granted to the end user in the Portal, from a fixed vocabulary. All capabilities are scoped to this end user: key capabilities (`keys:*`) apply only to keys the end user owns within the keyspace configured on the portal, and `analytics:read` returns only the end user's own verification events. An end user can never see another identity's keys or analytics. Tab visibility is derived from the scopes: - Keys tab: any `keys:*` scope - Analytics tab: `analytics:read` - Docs tab: visible when any scope is present `keys:create` is accepted but has no portal route behind it yet. It is still authorized like the others, so a session minted with it required `create_key` on the keyspace at mint time, and a future portal create-key route inherits an enforced ceiling rather than trusting sessions minted while the capability was inert. Each scope requires the equivalent permission on your own root key. See Required Permissions on this operation. */
  scopes: PortalCreateSessionRequestScopesList;
  /** When true, creates a preview session for testing the portal experience. */
  preview?: boolean;
  /** Absolute URL the end user is sent back to when they leave the portal, or when their session expires mid-visit. Set per session rather than per portal, so one portal can serve several entry points and return each user to the page they came from. When omitted, the portal shows no return link. */
  returnUrl?: string;
}
export const PortalCreateSessionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    portal: S.String,
    externalId: S.String,
    scopes: PortalCreateSessionRequestScopesList,
    preview: S.optional(S.Boolean),
    returnUrl: S.optional(S.String),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/portal.createSession", code: 200 }),
  ),
).annotate({
  identifier: "PortalCreateSessionRequest",
}) as any as S.Schema<PortalCreateSessionRequest>;

export interface V2PortalCreateSessionResponseData {
  /** The portal session's identifier. Not a credential: it is safe to log and to store against your own records of the end user's visit. */
  id: string;
  /** The full portal URL to redirect the end user to. Carries a single-use exchange code that is valid for 15 minutes. */
  url: string;
}
export const V2PortalCreateSessionResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    url: S.String,
  }),
).annotate({
  identifier: "V2PortalCreateSessionResponseData",
}) as any as S.Schema<V2PortalCreateSessionResponseData>;

export interface V2PortalCreateSessionResponseBody {
  meta: Meta;
  data: V2PortalCreateSessionResponseData;
}
export const V2PortalCreateSessionResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2PortalCreateSessionResponseData,
  }),
).annotate({
  identifier: "V2PortalCreateSessionResponseBody",
}) as any as S.Schema<V2PortalCreateSessionResponseBody>;

export interface PortalDeletePortalRequest {
  portal: string;
}
export const PortalDeletePortalRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    portal: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/portal.deletePortal", code: 200 }),
  ),
).annotate({
  identifier: "PortalDeletePortalRequest",
}) as any as S.Schema<PortalDeletePortalRequest>;

export interface V2PortalDeletePortalResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2PortalDeletePortalResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: S.Unknown,
  }),
).annotate({
  identifier: "V2PortalDeletePortalResponseBody",
}) as any as S.Schema<V2PortalDeletePortalResponseBody>;

export interface PortalExchangeCodeRequest {
  /** The exchange code carried by the portal URL from `portal.createSession`. Must be valid, unexpired, and not previously redeemed. */
  code: string;
}
export const PortalExchangeCodeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    code: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/portal.exchangeCode", code: 200 }),
  ),
).annotate({
  identifier: "PortalExchangeCodeRequest",
}) as any as S.Schema<PortalExchangeCodeRequest>;

export interface V2PortalExchangeCodeResponseData {
  /** The portal access token. Store this as an httpOnly cookie for subsequent portal requests. */
  accessToken: string | Redacted.Redacted<string>;
  /** Unix timestamp in milliseconds when the access token expires (24 hours from creation). */
  expiresAt: number;
}
export const V2PortalExchangeCodeResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accessToken: S.String.pipe(T.SensitiveValue({})),
    expiresAt: S.Number,
  }),
).annotate({
  identifier: "V2PortalExchangeCodeResponseData",
}) as any as S.Schema<V2PortalExchangeCodeResponseData>;

export interface V2PortalExchangeCodeResponseBody {
  meta: Meta;
  data: V2PortalExchangeCodeResponseData;
}
export const V2PortalExchangeCodeResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2PortalExchangeCodeResponseData,
  }),
).annotate({
  identifier: "V2PortalExchangeCodeResponseBody",
}) as any as S.Schema<V2PortalExchangeCodeResponseBody>;

export interface PortalGetPortalRequest {
  portal?: string;
  keyspaceId?: string;
  appId?: string;
}
export const PortalGetPortalRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    portal: S.optional(S.String),
    keyspaceId: S.optional(S.String),
    appId: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/v2/portal.getPortal", code: 200 })),
).annotate({
  identifier: "PortalGetPortalRequest",
}) as any as S.Schema<PortalGetPortalRequest>;

/** How the portal looks to your end users. Both fields are optional; a portal with neither set renders with default styling. */
export interface PortalBranding {
  /** Absolute `https://` URL of the logo shown in the portal header. Loaded by your end users' browsers, so the host you name receives their IP and user agent on every page view. */
  logoUrl?: string;
  /** Six-digit hex colour used for primary actions and accents in the portal. */
  primaryColor?: string;
}
export const PortalBranding = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    logoUrl: S.optional(S.String),
    primaryColor: S.optional(S.String),
  }),
).annotate({ identifier: "PortalBranding" }) as any as S.Schema<PortalBranding>;

/** A portal you expose to your end users so they can manage their own keys. Exactly one of `keyspaceId` or `appId` is present, naming the single resource the portal serves. Neither is in the required list because which one appears depends on the portal, so a reader checks for the one it cares about. */
export interface Portal {
  /** The unique identifier of the portal, generated by Unkey. */
  id: string;
  /** URL-safe handle for this portal, unique within your workspace. */
  slug: string;
  /** Human-readable name your end users see in the portal header and page titles. Unlike `slug` it is not part of any URL, so it can be changed freely. */
  displayName: string;
  /** Whether new sessions can be minted for this portal. Disabling a portal stops `portal.createSession` from minting new sessions. It does not end sessions that are already live. */
  enabled: boolean;
  keyspaceId?: string;
  appId?: string;
  /** How the portal looks to your end users. Omitted when no branding is set. */
  branding?: PortalBranding;
  /** Unix timestamp in milliseconds when the portal was created. */
  createdAt: number;
  /** Unix timestamp in milliseconds when the portal was last changed. Omitted if it has never been changed since creation. */
  updatedAt?: number;
}
export const Portal = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    slug: S.String,
    displayName: S.String,
    enabled: S.Boolean,
    keyspaceId: S.optional(S.String),
    appId: S.optional(S.String),
    branding: S.optional(PortalBranding),
    createdAt: S.Number,
    updatedAt: S.optional(S.Number),
  }),
).annotate({ identifier: "Portal" }) as any as S.Schema<Portal>;

export interface V2PortalGetPortalResponseBody {
  meta: Meta;
  data: Portal;
}
export const V2PortalGetPortalResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: Portal,
  }),
).annotate({
  identifier: "V2PortalGetPortalResponseBody",
}) as any as S.Schema<V2PortalGetPortalResponseBody>;

export interface PortalGetVerificationsRequest {
  /** Start of the query window as a unix timestamp in milliseconds (inclusive). */
  startTime: number;
  /** End of the query window as a unix timestamp in milliseconds (exclusive). Bucket granularity (minute, hour, or day) is chosen automatically from the window size. */
  endTime: number;
  /** Optional. Restrict results to a single key. The key must belong to the authenticated end user; results are always scoped to the session identity regardless of this value. */
  keyId?: string;
}
export const PortalGetVerificationsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    startTime: S.Number,
    endTime: S.Number,
    keyId: S.optional(S.String),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/portal.getVerifications", code: 200 }),
  ),
).annotate({
  identifier: "PortalGetVerificationsRequest",
}) as any as S.Schema<PortalGetVerificationsRequest>;

export interface V2PortalGetVerificationsDataPoint {
  /** Bucket start as a unix timestamp in milliseconds. */
  time: number;
  /** Total verifications in this bucket, across all outcomes. */
  total: number;
  /** Verifications with a VALID outcome. */
  valid: number;
  /** Verifications rejected because a rate limit was exceeded. */
  rateLimited: number;
  /** Verifications rejected for insufficient permissions. */
  insufficientPermissions: number;
  /** Verifications rejected as forbidden. */
  forbidden: number;
  /** Verifications rejected because the key was disabled. */
  disabled: number;
  /** Verifications rejected because the key was expired. */
  expired: number;
  /** Verifications rejected because remaining usage was exhausted. */
  usageExceeded: number;
}
export const V2PortalGetVerificationsDataPoint = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    time: S.Number,
    total: S.Number,
    valid: S.Number,
    rateLimited: S.Number,
    insufficientPermissions: S.Number,
    forbidden: S.Number,
    disabled: S.Number,
    expired: S.Number,
    usageExceeded: S.Number,
  }),
).annotate({
  identifier: "V2PortalGetVerificationsDataPoint",
}) as any as S.Schema<V2PortalGetVerificationsDataPoint>;

/** Zero-filled verification timeseries for the authenticated end user, ordered by time ascending. Buckets with no verifications are present with zero counts so the series is contiguous across the requested window. */
export type V2PortalGetVerificationsResponseBodyDataList =
  Array<V2PortalGetVerificationsDataPoint>;
export const V2PortalGetVerificationsResponseBodyDataList =
  /*@__PURE__*/ S.Array(
    V2PortalGetVerificationsDataPoint,
  ) as any as S.Schema<V2PortalGetVerificationsResponseBodyDataList>;

export interface V2PortalGetVerificationsResponseBody {
  meta: Meta;
  /** Zero-filled verification timeseries for the authenticated end user, ordered by time ascending. Buckets with no verifications are present with zero counts so the series is contiguous across the requested window. */
  data: V2PortalGetVerificationsResponseBodyDataList;
}
export const V2PortalGetVerificationsResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: V2PortalGetVerificationsResponseBodyDataList,
    }),
).annotate({
  identifier: "V2PortalGetVerificationsResponseBody",
}) as any as S.Schema<V2PortalGetVerificationsResponseBody>;

export interface PortalListKeysRequest {
  /** Maximum number of keys to return per request. Balance between response size and number of pagination calls needed. */
  limit?: number;
  /** Pagination cursor from a previous response to fetch the next page. Use when `hasMore: true` in the previous response. */
  cursor?: string;
}
export const PortalListKeysRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    limit: S.optional(S.Number),
    cursor: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/v2/portal.listKeys", code: 200 })),
).annotate({
  identifier: "PortalListKeysRequest",
}) as any as S.Schema<PortalListKeysRequest>;

/** Array of the portal end user's API keys. */
export type V2PortalListKeysResponseData = Array<KeyResponseData>;
export const V2PortalListKeysResponseData = /*@__PURE__*/ S.Array(
  KeyResponseData,
) as any as S.Schema<V2PortalListKeysResponseData>;

export interface V2PortalListKeysResponseBody {
  meta: Meta;
  data: V2PortalListKeysResponseData;
  pagination: Pagination;
}
export const V2PortalListKeysResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2PortalListKeysResponseData,
    pagination: Pagination,
  }),
).annotate({
  identifier: "V2PortalListKeysResponseBody",
}) as any as S.Schema<V2PortalListKeysResponseBody>;

export interface PortalRerollKeyRequest {
  /** The database identifier of the key to reroll. This is the unique ID returned when creating or listing keys, NOT the actual API key token. You can find this ID in: - The response from `keys.createKey` - Key verification responses - The Unkey dashboard - API key listing endpoints */
  keyId: string;
  /** Duration in milliseconds until the ORIGINAL key is revoked, starting from now. This parameter controls the overlap period for key rotation: - Set to `0` to revoke the original key immediately - Positive values keep the original key active for the specified duration - Allows graceful migration by giving users time to update their credentials Common overlap periods: - Immediate revocation: 0 - 1 hour grace period: 3600000 - 24 hours grace period: 86400000 - 7 days grace period: 604800000 - 30 days grace period: 2592000000 */
  expiration: number;
}
export const PortalRerollKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    keyId: S.String,
    expiration: S.Number,
  }).pipe(T.Http({ method: "POST", uri: "/v2/portal.rerollKey", code: 200 })),
).annotate({
  identifier: "PortalRerollKeyRequest",
}) as any as S.Schema<PortalRerollKeyRequest>;

export interface PortalUpdatePortalRequest {
  portal: string;
  /** New handle for this portal. Omit to leave unchanged. */
  slug?: string;
  /** New human-readable name shown to your end users. Omit to leave unchanged. */
  displayName?: string;
  /** Re-point the portal at a different keyspace. Omit to leave the resource it serves alone. Re-pointing revokes the portal's live sessions, because a session carries the keyspace scope it was minted with and would otherwise keep reaching the resource the portal no longer serves. */
  keyspaceId?: string;
  /** Re-point the portal at a different app. Omit to leave the resource it serves alone. Re-pointing revokes the portal's live sessions, for the same reason as `keyspaceId`. */
  appId?: string;
  /** Whether new sessions can be minted. Omit to leave unchanged. Disabling does not end sessions that are already live. */
  enabled?: boolean;
  /** Absolute `https://` URL of the portal logo. Omit to leave unchanged, or set null to remove the logo. */
  logoUrl?: string | null;
  /** Six-digit hex colour for primary actions. Omit to leave unchanged, or set null to fall back to default styling. */
  primaryColor?: string | null;
}
export const PortalUpdatePortalRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    portal: S.String,
    slug: S.optional(S.String),
    displayName: S.optional(S.String),
    keyspaceId: S.optional(S.String),
    appId: S.optional(S.String),
    enabled: S.optional(S.Boolean),
    logoUrl: S.optional(S.NullOr(S.String)),
    primaryColor: S.optional(S.NullOr(S.String)),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/portal.updatePortal", code: 200 }),
  ),
).annotate({
  identifier: "PortalUpdatePortalRequest",
}) as any as S.Schema<PortalUpdatePortalRequest>;

export interface V2PortalUpdatePortalResponseBody {
  meta: Meta;
  data: Portal;
}
export const V2PortalUpdatePortalResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: Portal,
  }),
).annotate({
  identifier: "V2PortalUpdatePortalResponseBody",
}) as any as S.Schema<V2PortalUpdatePortalResponseBody>;

export interface ProjectsCreateProjectRequest {
  /** Human-readable name for this project. Use a descriptive name like 'Payments Service' to identify its purpose. */
  name: string;
  slug: string;
}
export const ProjectsCreateProjectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    slug: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/projects.createProject", code: 200 }),
  ),
).annotate({
  identifier: "ProjectsCreateProjectRequest",
}) as any as S.Schema<ProjectsCreateProjectRequest>;

export interface V2ProjectsCreateProjectResponseData {
  /** The unique identifier of the newly created project, generated by Unkey. Always begins with 'proj_' followed by a unique alphanumeric sequence. */
  id: string;
}
export const V2ProjectsCreateProjectResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "V2ProjectsCreateProjectResponseData",
}) as any as S.Schema<V2ProjectsCreateProjectResponseData>;

export interface V2ProjectsCreateProjectResponseBody {
  meta: Meta;
  data: V2ProjectsCreateProjectResponseData;
}
export const V2ProjectsCreateProjectResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2ProjectsCreateProjectResponseData,
  }),
).annotate({
  identifier: "V2ProjectsCreateProjectResponseBody",
}) as any as S.Schema<V2ProjectsCreateProjectResponseBody>;

export interface ProjectsDeleteProjectRequest {
  project: string;
}
export const ProjectsDeleteProjectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/projects.deleteProject", code: 200 }),
  ),
).annotate({
  identifier: "ProjectsDeleteProjectRequest",
}) as any as S.Schema<ProjectsDeleteProjectRequest>;

export interface ProjectsDeleteProjectResponse {}
export const ProjectsDeleteProjectResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ProjectsDeleteProjectResponse",
}) as any as S.Schema<ProjectsDeleteProjectResponse>;

export interface ProjectsGetProjectRequest {
  project: string;
}
export const ProjectsGetProjectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/projects.getProject", code: 200 }),
  ),
).annotate({
  identifier: "ProjectsGetProjectRequest",
}) as any as S.Schema<ProjectsGetProjectRequest>;

export interface Project {
  /** The unique identifier of the project, generated by Unkey. */
  id: string;
  /** Human-readable name for this project. */
  name: string;
  /** URL-safe handle for this project, unique within your workspace. This is the caller-defined identifier used to reference the project in get, update, and delete operations. */
  slug: string;
  /** Unix timestamp in milliseconds when the project was created. */
  createdAt: number;
  /** Unix timestamp in milliseconds when the project was last updated. Omitted if the project has never been updated. */
  updatedAt?: number;
  /** Whether delete protection is enabled for this project. When true, the project cannot be deleted until protection is disabled. */
  deleteProtection: boolean;
}
export const Project = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    slug: S.String,
    createdAt: S.Number,
    updatedAt: S.optional(S.Number),
    deleteProtection: S.Boolean,
  }),
).annotate({ identifier: "Project" }) as any as S.Schema<Project>;

export interface V2ProjectsGetProjectResponseBody {
  meta: Meta;
  data: Project;
}
export const V2ProjectsGetProjectResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: Project,
  }),
).annotate({
  identifier: "V2ProjectsGetProjectResponseBody",
}) as any as S.Schema<V2ProjectsGetProjectResponseBody>;

export interface ProjectsListProjectsRequest {
  /** Maximum number of projects to return per request. Balance between response size and number of pagination calls needed. */
  limit?: number;
  /** Pagination cursor from a previous response to fetch the next page. Use when `hasMore: true` in the previous response. */
  cursor?: string;
  /** Free-form text to filter projects. Returns projects whose ID, name, or slug contains the search string. Matching is case-insensitive. */
  search?: string;
}
export const ProjectsListProjectsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    limit: S.optional(S.Number),
    cursor: S.optional(S.String),
    search: S.optional(S.String),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/projects.listProjects", code: 200 }),
  ),
).annotate({
  identifier: "ProjectsListProjectsRequest",
}) as any as S.Schema<ProjectsListProjectsRequest>;

/** Array of projects in the workspace, ordered by project id. */
export type V2ProjectsListProjectsResponseBodyDataList = Array<Project>;
export const V2ProjectsListProjectsResponseBodyDataList = /*@__PURE__*/ S.Array(
  Project,
) as any as S.Schema<V2ProjectsListProjectsResponseBodyDataList>;

export interface V2ProjectsListProjectsResponseBody {
  meta: Meta;
  /** Array of projects in the workspace, ordered by project id. */
  data: V2ProjectsListProjectsResponseBodyDataList;
  pagination: Pagination;
}
export const V2ProjectsListProjectsResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2ProjectsListProjectsResponseBodyDataList,
    pagination: Pagination,
  }),
).annotate({
  identifier: "V2ProjectsListProjectsResponseBody",
}) as any as S.Schema<V2ProjectsListProjectsResponseBody>;

export interface ProjectsUpdateProjectRequest {
  project: string;
  slug?: string;
  /** New human-readable name for the project. Omit this field to leave the current name unchanged. */
  name?: string;
  /** Enable or disable delete protection for the project. Omit this field to leave the current setting unchanged. */
  deleteProtection?: boolean;
}
export const ProjectsUpdateProjectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    project: S.String,
    slug: S.optional(S.String),
    name: S.optional(S.String),
    deleteProtection: S.optional(S.Boolean),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/projects.updateProject", code: 200 }),
  ),
).annotate({
  identifier: "ProjectsUpdateProjectRequest",
}) as any as S.Schema<ProjectsUpdateProjectRequest>;

export interface V2ProjectsUpdateProjectResponseBody {
  meta: Meta;
  data: Project;
}
export const V2ProjectsUpdateProjectResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: Project,
  }),
).annotate({
  identifier: "V2ProjectsUpdateProjectResponseBody",
}) as any as S.Schema<V2ProjectsUpdateProjectResponseBody>;

export interface RatelimitDeleteOverrideRequest {
  /** The id or name of the namespace containing the override. */
  namespace: string;
  /** The exact identifier pattern of the override to delete. This must match exactly as it was specified when creating the override. Important notes: - This is case-sensitive and must match exactly - Include any wildcards (*) that were part of the original pattern - For example, if the override was created for 'premium_*', you must use 'premium_*' here, not a specific ID After deletion, any identifiers previously affected by this override will immediately revert to using the default rate limit for the namespace. */
  identifier: string;
}
export const RatelimitDeleteOverrideRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String,
    identifier: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/ratelimit.deleteOverride", code: 200 }),
  ),
).annotate({
  identifier: "RatelimitDeleteOverrideRequest",
}) as any as S.Schema<RatelimitDeleteOverrideRequest>;

export interface V2RatelimitDeleteOverrideResponseBody {
  meta: Meta;
  data: unknown;
}
export const V2RatelimitDeleteOverrideResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: S.Unknown,
    }),
).annotate({
  identifier: "V2RatelimitDeleteOverrideResponseBody",
}) as any as S.Schema<V2RatelimitDeleteOverrideResponseBody>;

export interface RatelimitGetOverrideRequest {
  /** The id or name of the namespace containing the override. */
  namespace: string;
  /** The exact identifier pattern for the override you want to retrieve. This must match exactly as it was specified when creating the override. Important notes: - This is case-sensitive and must match exactly - Include any wildcards (*) that were part of the original pattern - For example, if the override was created for 'premium_*', you must use 'premium_*' here, not a specific ID like 'premium_user1' This field is used to look up the specific override configuration for this pattern. */
  identifier: string;
}
export const RatelimitGetOverrideRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String,
    identifier: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/ratelimit.getOverride", code: 200 }),
  ),
).annotate({
  identifier: "RatelimitGetOverrideRequest",
}) as any as S.Schema<RatelimitGetOverrideRequest>;

export interface RatelimitOverride {
  /** The unique identifier of this specific rate limit override. This ID is generated when the override is created and can be used for management operations like updating or deleting the override. */
  overrideId: string;
  /** The duration in milliseconds for this override's rate limit window. This may differ from the default duration for the namespace, allowing custom time windows for specific entities. After this duration elapses, the rate limit counter for affected identifiers resets to zero. */
  duration: number;
  /** The identifier pattern this override applies to. This determines which entities receive the custom rate limit. This can be: - An exact identifier for a specific entity - A pattern with wildcards for matching multiple entities Wildcard examples: - 'admin_*' matches any identifier starting with 'admin_' - '*_test' matches any identifier ending with '_test' - '*premium*' matches any identifier containing 'premium' More complex patterns can combine multiple wildcards. Detailed documentation on pattern matching rules is available at https://www.unkey.com/docs/ratelimiting/overrides#wildcard-rules */
  identifier: string;
  /** The maximum number of requests allowed for entities matching this override. This replaces the default limit for the namespace when applied. Common use cases: - Higher limits for premium customers - Reduced limits for abusive or suspicious entities - Zero limit to completely block specific patterns - Custom tier-based limits for different customer segments */
  limit: number;
}
export const RatelimitOverride = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    overrideId: S.String,
    duration: S.Number,
    identifier: S.String,
    limit: S.Number,
  }),
).annotate({
  identifier: "RatelimitOverride",
}) as any as S.Schema<RatelimitOverride>;

export interface V2RatelimitGetOverrideResponseBody {
  meta: Meta;
  data: RatelimitOverride;
}
export const V2RatelimitGetOverrideResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: RatelimitOverride,
  }),
).annotate({
  identifier: "V2RatelimitGetOverrideResponseBody",
}) as any as S.Schema<V2RatelimitGetOverrideResponseBody>;

export interface RatelimitLimitRequest {
  /** The id or name of the namespace. */
  namespace: string;
  /** Sets how much of the rate limit quota this request consumes, enabling weighted rate limiting. Use higher values for resource-intensive operations and 0 for tracking without limiting. When accumulated cost exceeds the limit within the duration window, subsequent requests are rejected. Essential for implementing fair usage policies and preventing resource abuse through expensive operations. */
  cost?: number;
  /** Sets the rate limit window duration in milliseconds after which the counter resets. Shorter durations enable faster recovery but may be less effective against sustained abuse. Common values include 60000 (1 minute), 3600000 (1 hour), and 86400000 (24 hours). Balance user experience with protection needs when choosing window sizes. */
  duration: number;
  /** Defines the scope of rate limiting by identifying the entity being limited. Use user IDs for per-user limits, IP addresses for anonymous limiting, or API key IDs for per-key limits. Accepts letters, numbers, underscores, dots, colons, slashes, and hyphens for flexible identifier formats. The same identifier can be used across different namespaces to apply multiple rate limit types. Choose identifiers that provide appropriate granularity for your rate limiting strategy. */
  identifier: string;
  /** Sets the maximum operations allowed within the duration window before requests are rejected. When this limit is reached, subsequent requests fail with `RATE_LIMITED` until the window resets. Balance user experience with resource protection when setting limits for different user tiers. Consider system capacity, business requirements, and fair usage policies in limit determination. */
  limit: number;
}
export const RatelimitLimitRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String,
    cost: S.optional(S.Number),
    duration: S.Number,
    identifier: S.String,
    limit: S.Number,
  }).pipe(T.Http({ method: "POST", uri: "/v2/ratelimit.limit", code: 200 })),
).annotate({
  identifier: "RatelimitLimitRequest",
}) as any as S.Schema<RatelimitLimitRequest>;

export interface V2RatelimitLimitResponseData {
  /** The maximum number of operations allowed within the time window. This reflects either the default limit specified in the request or an override limit if one exists for this identifier. This value helps clients understand their total quota for the current window. */
  limit: number;
  /** The number of operations remaining in the current window before the rate limit is exceeded. Applications should use this value to: - Implement client-side throttling before hitting limits - Display usage information to end users - Trigger alerts when approaching limits - Adjust request patterns based on available capacity When this reaches zero, requests will be rejected until the window resets. */
  remaining: number;
  /** The Unix timestamp in milliseconds when the rate limit window will reset and 'remaining' will return to 'limit'. This timestamp enables clients to: - Calculate and display wait times to users - Implement intelligent retry mechanisms - Schedule requests to resume after the reset - Implement exponential backoff when needed The reset time is based on a sliding window from the first request in the current window. */
  reset: number;
  /** Whether the request passed the rate limit check. If true, the request is allowed to proceed. If false, the request has exceeded the rate limit and should be blocked or rejected. You MUST check this field to determine if the request should proceed, as the endpoint always returns `HTTP 200` even when rate limited. */
  success: boolean;
  /** If a rate limit override was applied for this identifier, this field contains the ID of the override that was used. Empty when no override is in effect. This can be useful for: - Debugging which override rule was matched - Tracking the effects of specific overrides - Understanding why limits differ from default values */
  overrideId?: string;
}
export const V2RatelimitLimitResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    limit: S.Number,
    remaining: S.Number,
    reset: S.Number,
    success: S.Boolean,
    overrideId: S.optional(S.String),
  }),
).annotate({
  identifier: "V2RatelimitLimitResponseData",
}) as any as S.Schema<V2RatelimitLimitResponseData>;

export interface V2RatelimitLimitResponseBody {
  meta: Meta;
  data: V2RatelimitLimitResponseData;
}
export const V2RatelimitLimitResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2RatelimitLimitResponseData,
  }),
).annotate({
  identifier: "V2RatelimitLimitResponseBody",
}) as any as S.Schema<V2RatelimitLimitResponseBody>;

export interface RatelimitListOverridesRequest {
  /** The id or name of the rate limit namespace to list overrides for. */
  namespace: string;
  /** Pagination cursor from a previous response. Include this when fetching subsequent pages of results. Each response containing more results than the requested limit will include a cursor value in the pagination object that can be used here. */
  cursor?: string;
  /** Maximum number of override entries to return in a single response. Use this to control response size and loading performance. - Lower values (10-20): Better for UI displays and faster response times - Higher values (50-100): Better for data exports or bulk operations - Default (10): Suitable for most dashboard views Results exceeding this limit will be paginated, with a cursor provided for fetching subsequent pages. */
  limit?: number;
}
export const RatelimitListOverridesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String,
    cursor: S.optional(S.String),
    limit: S.optional(S.Number),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/ratelimit.listOverrides", code: 200 }),
  ),
).annotate({
  identifier: "RatelimitListOverridesRequest",
}) as any as S.Schema<RatelimitListOverridesRequest>;

export type V2RatelimitListOverridesResponseData = Array<RatelimitOverride>;
export const V2RatelimitListOverridesResponseData = /*@__PURE__*/ S.Array(
  RatelimitOverride,
) as any as S.Schema<V2RatelimitListOverridesResponseData>;

export interface V2RatelimitListOverridesResponseBody {
  meta: Meta;
  data: V2RatelimitListOverridesResponseData;
  pagination: Pagination;
}
export const V2RatelimitListOverridesResponseBody = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      meta: Meta,
      data: V2RatelimitListOverridesResponseData,
      pagination: Pagination,
    }),
).annotate({
  identifier: "V2RatelimitListOverridesResponseBody",
}) as any as S.Schema<V2RatelimitListOverridesResponseBody>;

export interface V2RatelimitLimitRequestBody {
  /** The id or name of the namespace. */
  namespace: string;
  /** Sets how much of the rate limit quota this request consumes, enabling weighted rate limiting. Use higher values for resource-intensive operations and 0 for tracking without limiting. When accumulated cost exceeds the limit within the duration window, subsequent requests are rejected. Essential for implementing fair usage policies and preventing resource abuse through expensive operations. */
  cost?: number;
  /** Sets the rate limit window duration in milliseconds after which the counter resets. Shorter durations enable faster recovery but may be less effective against sustained abuse. Common values include 60000 (1 minute), 3600000 (1 hour), and 86400000 (24 hours). Balance user experience with protection needs when choosing window sizes. */
  duration: number;
  /** Defines the scope of rate limiting by identifying the entity being limited. Use user IDs for per-user limits, IP addresses for anonymous limiting, or API key IDs for per-key limits. Accepts letters, numbers, underscores, dots, colons, slashes, and hyphens for flexible identifier formats. The same identifier can be used across different namespaces to apply multiple rate limit types. Choose identifiers that provide appropriate granularity for your rate limiting strategy. */
  identifier: string;
  /** Sets the maximum operations allowed within the duration window before requests are rejected. When this limit is reached, subsequent requests fail with `RATE_LIMITED` until the window resets. Balance user experience with resource protection when setting limits for different user tiers. Consider system capacity, business requirements, and fair usage policies in limit determination. */
  limit: number;
}
export const V2RatelimitLimitRequestBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String,
    cost: S.optional(S.Number),
    duration: S.Number,
    identifier: S.String,
    limit: S.Number,
  }),
).annotate({
  identifier: "V2RatelimitLimitRequestBody",
}) as any as S.Schema<V2RatelimitLimitRequestBody>;

/** Array of rate limit checks to perform */
export type V2RatelimitMultiLimitRequestBody =
  Array<V2RatelimitLimitRequestBody>;
export const V2RatelimitMultiLimitRequestBody = /*@__PURE__*/ S.Array(
  V2RatelimitLimitRequestBody,
) as any as S.Schema<V2RatelimitMultiLimitRequestBody>;

export interface RatelimitMultiLimitRequest {
  body: V2RatelimitMultiLimitRequestBody;
}
export const RatelimitMultiLimitRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: V2RatelimitMultiLimitRequestBody.pipe(T.HttpBody()),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/ratelimit.multiLimit", code: 200 }),
  ),
).annotate({
  identifier: "RatelimitMultiLimitRequest",
}) as any as S.Schema<RatelimitMultiLimitRequest>;

export interface V2RatelimitMultiLimitCheck {
  /** The namespace this rate limit result corresponds to. Use this field to correlate the response with the request when checking multiple rate limits. */
  namespace: string;
  /** The identifier this rate limit result corresponds to. Use this field to correlate the response with the request when checking multiple rate limits. */
  identifier: string;
  /** The maximum number of operations allowed within the time window. This reflects either the default limit specified in the request or an override limit if one exists for this identifier. This value helps clients understand their total quota for the current window. */
  limit: number;
  /** The number of operations remaining in the current window before the rate limit is exceeded. Applications should use this value to: - Implement client-side throttling before hitting limits - Display usage information to end users - Trigger alerts when approaching limits - Adjust request patterns based on available capacity When this reaches zero, requests will be rejected until the window resets. */
  remaining: number;
  /** The Unix timestamp in milliseconds when the rate limit window will reset and 'remaining' will return to 'limit'. This timestamp enables clients to: - Calculate and display wait times to users - Implement intelligent retry mechanisms - Schedule requests to resume after the reset - Implement exponential backoff when needed The reset time is based on a sliding window from the first request in the current window. */
  reset: number;
  /** Whether the request passed the rate limit check. If true, the request is allowed to proceed. If false, the request has exceeded the rate limit and should be blocked or rejected. You MUST check this field to determine if the request should proceed, as the endpoint always returns `HTTP 200` even when rate limited. */
  passed: boolean;
  /** If a rate limit override was applied for this identifier, this field contains the ID of the override that was used. Empty when no override is in effect. This can be useful for: - Debugging which override rule was matched - Tracking the effects of specific overrides - Understanding why limits differ from default values */
  overrideId?: string;
}
export const V2RatelimitMultiLimitCheck = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String,
    identifier: S.String,
    limit: S.Number,
    remaining: S.Number,
    reset: S.Number,
    passed: S.Boolean,
    overrideId: S.optional(S.String),
  }),
).annotate({
  identifier: "V2RatelimitMultiLimitCheck",
}) as any as S.Schema<V2RatelimitMultiLimitCheck>;

/** Array of individual rate limit check results, one for each rate limit check in the request */
export type V2RatelimitMultiLimitResponseDataLimitsList =
  Array<V2RatelimitMultiLimitCheck>;
export const V2RatelimitMultiLimitResponseDataLimitsList =
  /*@__PURE__*/ S.Array(
    V2RatelimitMultiLimitCheck,
  ) as any as S.Schema<V2RatelimitMultiLimitResponseDataLimitsList>;

/** Container for multi-limit rate limit check results */
export interface V2RatelimitMultiLimitResponseData {
  /** Overall success indicator for all rate limit checks. This is true if ALL individual rate limit checks passed (all have success: true), and false if ANY check failed. Use this as a quick indicator to determine if the request should proceed. */
  passed: boolean;
  /** Array of individual rate limit check results, one for each rate limit check in the request */
  limits: V2RatelimitMultiLimitResponseDataLimitsList;
}
export const V2RatelimitMultiLimitResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    passed: S.Boolean,
    limits: V2RatelimitMultiLimitResponseDataLimitsList,
  }),
).annotate({
  identifier: "V2RatelimitMultiLimitResponseData",
}) as any as S.Schema<V2RatelimitMultiLimitResponseData>;

export interface V2RatelimitMultiLimitResponseBody {
  meta: Meta;
  data: V2RatelimitMultiLimitResponseData;
}
export const V2RatelimitMultiLimitResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2RatelimitMultiLimitResponseData,
  }),
).annotate({
  identifier: "V2RatelimitMultiLimitResponseBody",
}) as any as S.Schema<V2RatelimitMultiLimitResponseBody>;

export interface RatelimitSetOverrideRequest {
  /** The ID or name of the rate limit namespace. */
  namespace: string;
  /** The duration in milliseconds for the rate limit window. This defines how long the rate limit counter accumulates before resetting to zero. Considerations: - This can differ from the default duration for the namespace - Longer durations create stricter limits that take longer to reset - Shorter durations allow more frequent bursts of activity - Common values: 60000 (1 minute), 3600000 (1 hour), 86400000 (1 day) */
  duration: number;
  /** Identifier of the entity receiving this custom rate limit. This can be: - A specific user ID for individual custom limits - An IP address for location-based rules - An email domain for organization-wide policies - Any other string that identifies the target entity Wildcards (*) can be used to create pattern-matching rules that apply to multiple identifiers. For example: - 'premium_*' would match all identifiers starting with 'premium_' - '*_admin' would match all identifiers ending with '_admin' - '*suspicious*' would match any identifier containing 'suspicious' More detailed information on wildcard pattern rules is available at https://www.unkey.com/docs/ratelimiting/overrides#wildcard-rules */
  identifier: string;
  /** The maximum number of requests allowed for this override. This defines the custom quota for the specified identifier(s). Special values: - Higher than default: For premium or trusted entities - Lower than default: For suspicious or abusive entities - 0: To completely block access (useful for ban implementation) This limit entirely replaces the default limit for matching identifiers. */
  limit: number;
}
export const RatelimitSetOverrideRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String,
    duration: S.Number,
    identifier: S.String,
    limit: S.Number,
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/ratelimit.setOverride", code: 200 }),
  ),
).annotate({
  identifier: "RatelimitSetOverrideRequest",
}) as any as S.Schema<RatelimitSetOverrideRequest>;

export interface V2RatelimitSetOverrideResponseData {
  /** The unique identifier for the newly created or updated rate limit override. This ID can be used to: - Reference this specific override in subsequent API calls - Delete or modify this override later - Track which override is being applied in rate limit responses - Associate override effects with specific rules in analytics Store this ID if you need to manage the override in the future. */
  overrideId: string;
}
export const V2RatelimitSetOverrideResponseData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    overrideId: S.String,
  }),
).annotate({
  identifier: "V2RatelimitSetOverrideResponseData",
}) as any as S.Schema<V2RatelimitSetOverrideResponseData>;

export interface V2RatelimitSetOverrideResponseBody {
  meta: Meta;
  data: V2RatelimitSetOverrideResponseData;
}
export const V2RatelimitSetOverrideResponseBody = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    meta: Meta,
    data: V2RatelimitSetOverrideResponseData,
  }),
).annotate({
  identifier: "V2RatelimitSetOverrideResponseBody",
}) as any as S.Schema<V2RatelimitSetOverrideResponseBody>;

export type AnalyticsGetGatewayRequestsError =
  | BadRequest
  | Forbidden
  | UnprocessableEntity
  | UnkeyOpError;
/** Query gateway request data A query can use only the public alias `gateway_requests_v1`. CTEs, subqueries, UNION, and EXCEPT are permitted. The root key must have the `project.*.read_gateway_requests` permission. Unkey limits each query to the workspace of the root key. To get the data for one project, app, or environment, add a filter on `project_id`, `app_id`, or `environment_id`. The workspace retention period and the workspace query limits apply. For the columns and more query examples, see the gateway request analytics documentation. */
export const analyticsGetGatewayRequests: API.OperationMethod<
  AnalyticsGetGatewayRequestsRequest,
  V2AnalyticsGetGatewayRequestsResponseBody,
  AnalyticsGetGatewayRequestsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AnalyticsGetGatewayRequestsRequest,
  output: V2AnalyticsGetGatewayRequestsResponseBody,
  errors: [BadRequest, Forbidden, UnprocessableEntity, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type AnalyticsGetRatelimitsError =
  | BadRequest
  | Forbidden
  | UnprocessableEntity
  | UnkeyOpError;
/** Query rate limit data Queries may reference only the five public rate limit analytics aliases: `ratelimits_v1`, `ratelimits_per_minute_v1`, `ratelimits_per_hour_v1`, `ratelimits_per_day_v1`, or `ratelimits_per_month_v1`. CTEs, subqueries, UNION, and EXCEPT are supported. Queries are always restricted to the authenticated workspace. Wildcard analytics permission can read every namespace in that workspace; namespace-scoped permissions automatically restrict results to the permitted namespace IDs. Workspace retention and query limits apply. */
export const analyticsGetRatelimits: API.OperationMethod<
  AnalyticsGetRatelimitsRequest,
  V2AnalyticsGetRatelimitsResponseBody,
  AnalyticsGetRatelimitsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AnalyticsGetRatelimitsRequest,
  output: V2AnalyticsGetRatelimitsResponseBody,
  errors: [BadRequest, Forbidden, UnprocessableEntity, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type AnalyticsGetRuntimeLogsError =
  | BadRequest
  | Forbidden
  | UnprocessableEntity
  | UnkeyOpError;
/** Query runtime log data A query can use only the public alias `runtime_logs_v1`. CTEs, subqueries, UNION, and EXCEPT are permitted. The root key must have the `project.*.read_runtime_logs` permission. Unkey limits each query to the workspace of the root key. To get the logs of one project, app, environment, or deployment, add a filter on `project_id`, `app_id`, `environment_id`, or `deployment_id`. The workspace retention period and the workspace query limits apply. For the table, the columns, and more query examples, see [Query runtime logs](/platform/analytics/get-runtime-logs). */
export const analyticsGetRuntimeLogs: API.OperationMethod<
  AnalyticsGetRuntimeLogsRequest,
  V2AnalyticsGetRuntimeLogsResponseBody,
  AnalyticsGetRuntimeLogsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AnalyticsGetRuntimeLogsRequest,
  output: V2AnalyticsGetRuntimeLogsResponseBody,
  errors: [BadRequest, Forbidden, UnprocessableEntity, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type AnalyticsGetVerificationsError =
  | BadRequest
  | Forbidden
  | UnprocessableEntity
  | UnkeyOpError;
/** Query key verification data Execute custom SQL queries against your key verification analytics. CTEs, subqueries, UNION, and EXCEPT are supported. Queries must use one of the five public aliases: `key_verifications_v1`, `key_verifications_per_minute_v1`, `key_verifications_per_hour_v1`, `key_verifications_per_day_v1`, or `key_verifications_per_month_v1`. Physical `default.*` table names are unsupported. Queries are always restricted to the authenticated workspace. Wildcard analytics permission can read every API in that workspace; API-scoped permissions automatically restrict results to the permitted APIs. For complete documentation including available tables, columns, data types, query examples, see the schema reference in the API documentation. */
export const analyticsGetVerifications: API.OperationMethod<
  AnalyticsGetVerificationsRequest,
  V2AnalyticsGetVerificationsResponseBody,
  AnalyticsGetVerificationsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AnalyticsGetVerificationsRequest,
  output: V2AnalyticsGetVerificationsResponseBody,
  errors: [BadRequest, Forbidden, UnprocessableEntity, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type ApisCreateApiError = BadRequest | Forbidden | UnkeyOpError;
/** Create API namespace Create an API namespace for organizing keys by environment, service, or product. Use this to separate production from development keys, isolate different services, or manage multiple products. Each API gets a unique identifier and dedicated infrastructure for secure key operations. **Important**: API names must be unique within your workspace and cannot be changed after creation. **Required Permissions** Your root key must have one of the following permissions: - `api.*.create_api` (to create APIs in any workspace) */
export const apisCreateApi: API.OperationMethod<
  ApisCreateApiRequest,
  V2ApisCreateApiResponseBody,
  ApisCreateApiError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ApisCreateApiRequest,
  output: V2ApisCreateApiResponseBody,
  errors: [BadRequest, Forbidden, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type ApisDeleteApiError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Delete API namespace Permanently delete an API namespace and immediately invalidate all associated keys. Use this for cleaning up development environments, retiring deprecated services, or removing unused resources. All keys in the namespace are immediately marked as deleted and will fail verification with `code=NOT_FOUND`. **Important**: This operation is immediate and permanent. Verify you have the correct API ID before deletion. If delete protection is enabled, disable it first through the dashboard or API configuration. **Required Permissions** Your root key must have one of the following permissions: - `api.*.delete_api` (to delete any API) - `api.<api_id>.delete_api` (to delete a specific API) */
export const apisDeleteApi: API.OperationMethod<
  ApisDeleteApiRequest,
  V2ApisDeleteApiResponseBody,
  ApisDeleteApiError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ApisDeleteApiRequest,
  output: V2ApisDeleteApiResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type ApisGetApiError = BadRequest | Forbidden | NotFound | UnkeyOpError;
/** Get API namespace Retrieve basic information about an API namespace including its ID and name. Use this to verify an API exists before performing operations, get the human-readable name when you only have the API ID, or confirm access to a specific namespace. For detailed key information, use the `listKeys` endpoint instead. **Required Permissions** Your root key must have one of the following permissions: - `api.*.read_api` (to read any API) - `api.<api_id>.read_api` (to read a specific API) */
export const apisGetApi: API.OperationMethod<
  ApisGetApiRequest,
  V2ApisGetApiResponseBody,
  ApisGetApiError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ApisGetApiRequest,
  output: V2ApisGetApiResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type ApisListKeysError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** List API keys Retrieve a paginated list of API keys for dashboard and administrative interfaces. Use this to build key management dashboards, filter keys by user with `externalId`, or retrieve key details for administrative purposes. Each key includes status, metadata, permissions, and usage limits. **Important**: Set `decrypt: true` only in secure contexts to retrieve plaintext key values from recoverable keys. **Required Permissions** Your root key must have one of the following permissions for basic key listing: - `api.*.read_key` (to read keys from any API) - `api.<api_id>.read_key` (to read keys from a specific API) Additionally, you need read access to the API itself: - `api.*.read_api` or `api.<api_id>.read_api` Additional permission required for decrypt functionality: - `api.*.decrypt_key` or `api.<api_id>.decrypt_key` */
export const apisListKeys: API.PaginatedOperationMethod<
  ApisListKeysRequest,
  V2ApisListKeysResponseBody,
  ApisListKeysError,
  UnkeyOpContext,
  KeyResponseData
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ApisListKeysRequest,
    output: V2ApisListKeysResponseBody,
    errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
    protocol: UnkeyProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pagination.cursor",
      items: "data",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type AppsCreateAppError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | UnkeyOpError;
/** Create app Create an app within a project. The app is created with default `production` and `preview` environments. The slug you provide is the stable, caller-defined handle used to reference this app. It must be unique within the project. **Important**: The slug cannot collide with an existing app in the same project. A duplicate slug returns a 409 conflict. **Required Permissions** Your root key must have one of the following permissions: - `project.*.create_app` (to create apps in any project) - `project.<project_id>.create_app` (to create apps in a specific project) */
export const appsCreateApp: API.OperationMethod<
  AppsCreateAppRequest,
  V2AppsCreateAppResponseBody,
  AppsCreateAppError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AppsCreateAppRequest,
  output: V2AppsCreateAppResponseBody,
  errors: [BadRequest, Forbidden, NotFound, Conflict, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type AppsDeleteAppError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Delete app Delete an existing app, identified by its id. Deletion is asynchronous and eventually consistent. The app and all of its associated resources (environments, deployments, custom domains) are torn down by a background workflow. A successful response indicates the deletion was enqueued, not that every resource has already been removed. Apps with delete protection enabled cannot be deleted until protection is disabled. **Required Permissions** Your root key must have one of the following permissions: - `app.*.delete_app` (to delete any app) - `app.<app_id>.delete_app` (to delete a specific app) */
export const appsDeleteApp: API.OperationMethod<
  AppsDeleteAppRequest,
  AppsDeleteAppResponse,
  AppsDeleteAppError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AppsDeleteAppRequest,
  output: AppsDeleteAppResponse,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type AppsGetAppError = BadRequest | Forbidden | NotFound | UnkeyOpError;
/** Get app Retrieve a single app by its id or slug within a project. Use this to fetch app details after creation or to verify an app exists before performing operations. **Required Permissions** Your root key must have one of the following permissions: - `app.*.read_app` (to read any app) - `app.<app_id>.read_app` (to read a specific app) */
export const appsGetApp: API.OperationMethod<
  AppsGetAppRequest,
  V2AppsGetAppResponseBody,
  AppsGetAppError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AppsGetAppRequest,
  output: V2AppsGetAppResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type AppsListAppsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** List apps Retrieve a paginated list of apps within a project. Use this to enumerate every app in a project. Results are ordered by app id and paginated; when `hasMore` is true, pass the returned `cursor` to fetch the next page. **Required Permissions** Your root key must have the following permission: - `app.*.read_app` (to read apps in any project) */
export const appsListApps: API.PaginatedOperationMethod<
  AppsListAppsRequest,
  V2AppsListAppsResponseBody,
  AppsListAppsError,
  UnkeyOpContext,
  App
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: AppsListAppsRequest,
    output: V2AppsListAppsResponseBody,
    errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
    protocol: UnkeyProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pagination.cursor",
      items: "data",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type AppsUpdateAppError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | UnkeyOpError;
/** Update app Update an existing app, identified by its id. The app name, slug, default branch, and delete protection setting can be changed. Omitted fields are left unchanged. Changing the slug affects the deployment domains generated for this app. **Important**: The slug cannot collide with an existing app in the same project. A duplicate slug returns a 409 conflict. **Required Permissions** Your root key must have one of the following permissions: - `app.*.update_app` (to update any app) - `app.<app_id>.update_app` (to update a specific app) */
export const appsUpdateApp: API.OperationMethod<
  AppsUpdateAppRequest,
  V2AppsUpdateAppResponseBody,
  AppsUpdateAppError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AppsUpdateAppRequest,
  output: V2AppsUpdateAppResponseBody,
  errors: [BadRequest, Forbidden, NotFound, Conflict, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type DeploymentsCreateDeploymentError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Create deployment Create a deployment for an app in a project. Provide exactly one source: - `image`: deploy a prebuilt Docker image as-is (no build). - `git`: build and deploy from the app's connected GitHub repository, a branch, a specific commit, or a fork commit. Requires the app to have a repository connected. - `deployment`: re-run an existing deployment by its id. Git-connected apps rebuild from the recorded commit; other apps reuse the recorded image. Returns immediately with a `deploymentId`. The build and rollout run asynchronously — poll `deployments.getDeployment` to watch status until it is ready. **Authentication**: requires a root key with permission to create deployments. */
export const deploymentsCreateDeployment: API.OperationMethod<
  DeploymentsCreateDeploymentRequest,
  V2DeploymentsCreateDeploymentResponseBody,
  DeploymentsCreateDeploymentError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeploymentsCreateDeploymentRequest,
  output: V2DeploymentsCreateDeploymentResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type DeploymentsGetDeploymentError =
  | BadRequest
  | NotFound
  | UnkeyOpError;
/** Get deployment Retrieve a single deployment by its id. Use this to check a deployment's status after creating it, or to inspect the runtime configuration of an existing deployment. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.read_deployment` (to read deployments in any environment) - `environment.<environment_id>.read_deployment` (to read deployments in a specific environment) */
export const deploymentsGetDeployment: API.OperationMethod<
  DeploymentsGetDeploymentRequest,
  V2DeploymentsGetDeploymentResponseBody,
  DeploymentsGetDeploymentError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeploymentsGetDeploymentRequest,
  output: V2DeploymentsGetDeploymentResponseBody,
  errors: [BadRequest, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type DeploymentsListDeploymentsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** List deployments Retrieve a paginated list of deployments within a workspace, newest first. Filter by project, app, environment, and lifecycle status. All filters are optional; with none set, every deployment in the workspace is returned. Filters nest: `app` requires `project`, and `environment` requires both `project` and `app`. Results are paginated; when `hasMore` is true, pass the returned `cursor` to fetch the next page. **Required Permissions** Your root key must have the `environment.*.read_deployment` permission. Listing spans environments, so a grant on a single environment is not sufficient. */
export const deploymentsListDeployments: API.PaginatedOperationMethod<
  DeploymentsListDeploymentsRequest,
  V2DeploymentsListDeploymentsResponseBody,
  DeploymentsListDeploymentsError,
  UnkeyOpContext,
  Deployment
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: DeploymentsListDeploymentsRequest,
    output: V2DeploymentsListDeploymentsResponseBody,
    errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
    protocol: UnkeyProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pagination.cursor",
      items: "data",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type DeploymentsPromoteDeploymentError =
  | BadRequest
  | NotFound
  | UnkeyOpError;
/** Promote deployment Promote a deployment to become the current deployment for its environment. All sticky domains are reassigned from the current deployment to the promoted one, and the previous deployment is scheduled for standby. The deployment must be ready, not already shutting down, belong to the production environment, and its app must already have a current deployment. Promoting the deployment that is already current fails, unless the app is in a rolled-back state, in which case promoting the current deployment confirms the rollback and re-enables automatic promotion of future deployments. Promotion runs as a durable workflow: this endpoint returns once the promotion is accepted. Poll `getDeployment` or `listDeployments` to observe the result. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.promote_deployment` (to promote deployments in any environment) - `environment.<environment_id>.promote_deployment` (to promote deployments in a specific environment) */
export const deploymentsPromoteDeployment: API.OperationMethod<
  DeploymentsPromoteDeploymentRequest,
  DeploymentsPromoteDeploymentResponse,
  DeploymentsPromoteDeploymentError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeploymentsPromoteDeploymentRequest,
  output: DeploymentsPromoteDeploymentResponse,
  errors: [BadRequest, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type DeploymentsRollbackDeploymentError =
  | BadRequest
  | NotFound
  | UnkeyOpError;
/** Rollback deployment Roll live traffic back to a previous deployment. `deploymentId` is the deployment to roll back TO; the app's current deployment is used as the rollback source automatically. The target deployment must be ready, not already shutting down, belong to the production environment, and must not itself be the current deployment. After a rollback the app is marked as rolled back, which prevents new deployments from automatically taking over live traffic. Promote the rolled-back deployment (or a newer one) to clear this state. Rollback runs as a durable workflow: this endpoint returns once the rollback is accepted. Poll `getDeployment` or `listDeployments` to observe the result. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.rollback_deployment` (to roll back deployments in any environment) - `environment.<environment_id>.rollback_deployment` (to roll back deployments in a specific environment) */
export const deploymentsRollbackDeployment: API.OperationMethod<
  DeploymentsRollbackDeploymentRequest,
  DeploymentsRollbackDeploymentResponse,
  DeploymentsRollbackDeploymentError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeploymentsRollbackDeploymentRequest,
  output: DeploymentsRollbackDeploymentResponse,
  errors: [BadRequest, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type DeploymentsStartDeploymentError =
  | BadRequest
  | NotFound
  | UnkeyOpError;
/** Start deployment Start a deployment that was previously stopped with `stopDeployment`, so it serves traffic again. The deployment keeps the configuration it had when it was stopped; nothing is rebuilt or redeployed. The deployment must currently be stopped, and must belong to a non-production environment. Production deployments are never stopped, so they cannot be started. Starting is asynchronous: this endpoint only enqueues the start and returns immediately. Poll `getDeployment` until the status reaches `ready`. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.start_deployment` (to start deployments in any environment) - `environment.<environment_id>.start_deployment` (to start deployments in a specific environment) */
export const deploymentsStartDeployment: API.OperationMethod<
  DeploymentsStartDeploymentRequest,
  DeploymentsStartDeploymentResponse,
  DeploymentsStartDeploymentError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeploymentsStartDeploymentRequest,
  output: DeploymentsStartDeploymentResponse,
  errors: [BadRequest, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type DeploymentsStopDeploymentError =
  | BadRequest
  | NotFound
  | UnkeyOpError;
/** Stop deployment Stop a running preview deployment. Stopped deployments keep their configuration and can be resumed later with `startDeployment`. The deployment must be ready and running, and must belong to a non-production environment; production deployments cannot be stopped. A deployment that is already draining from a previous stop is rejected with a precondition error. Stopping is asynchronous: this endpoint only enqueues the stop and returns immediately. Poll `getDeployment` until the status reaches `stopped`. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.stop_deployment` (to stop deployments in any environment) - `environment.<environment_id>.stop_deployment` (to stop deployments in a specific environment) */
export const deploymentsStopDeployment: API.OperationMethod<
  DeploymentsStopDeploymentRequest,
  DeploymentsStopDeploymentResponse,
  DeploymentsStopDeploymentError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeploymentsStopDeploymentRequest,
  output: DeploymentsStopDeploymentResponse,
  errors: [BadRequest, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type DomainsCreateDomainError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | UnkeyOpError;
/** Create domain Attach a custom domain to an environment and start verifying it. The domain is created in the `pending` state and does not serve traffic until verification succeeds. Verification runs in the background and polls DNS, so it is eventually consistent. The response returns `dnsRecords`: every record needed to finish setup, already resolved for whether this domain is an apex or a subdomain. Create every entry exactly as given. One record establishes routing and one proves ownership, and both are needed: whether ownership can be inferred from the routing record depends on how your provider publishes it, and a name another workspace has already verified can only be claimed through the ownership record. Neither is knowable before the records exist. When your DNS provider supports Domain Connect, the response also carries a `domainConnect` object; opening its `url` applies the same records at the provider in one step. The object is absent when the shortcut is unavailable. Domains are unique per workspace, so the same name cannot be attached to two environments. Attaching a domain that already exists in your workspace returns a 409 conflict. How many domains you may attach is set by your plan. Attaching one beyond that allowance returns a 403; upgrade the plan or remove a domain you no longer need. **Important**: verification stops after 24 hours without the required DNS records, and the domain moves to `failed`. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.create_domain` (to attach domains to any environment) - `environment.<environment_id>.create_domain` (to attach domains to a specific environment) */
export const domainsCreateDomain: API.OperationMethod<
  DomainsCreateDomainRequest,
  V2DomainsCreateDomainResponseBody,
  DomainsCreateDomainError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DomainsCreateDomainRequest,
  output: V2DomainsCreateDomainResponseBody,
  errors: [BadRequest, Forbidden, NotFound, Conflict, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type DomainsDeleteDomainError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Delete domain Delete a custom domain from your workspace. Address the domain by its id or by its name. Names are unique per workspace, so `api.acme.com` is enough. Unkey stops serving the domain. Later requests fail with a certificate error. The DNS records at your provider stay in place. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.delete_domain` (to delete domains in any environment) - `environment.<environment_id>.delete_domain` (to delete domains in a specific environment) */
export const domainsDeleteDomain: API.OperationMethod<
  DomainsDeleteDomainRequest,
  V2DomainsDeleteDomainResponseBody,
  DomainsDeleteDomainError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DomainsDeleteDomainRequest,
  output: V2DomainsDeleteDomainResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type DomainsGetDomainError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Get domain Retrieve a custom domain and its verification status. Address the domain by its id or by its name. Names are unique per workspace, so `api.acme.com` is sufficient. You do not need to supply a project, app, or environment. Use this endpoint to poll after `domains.createDomain`. Verification runs in the background and checks DNS approximately each minute. `status: verified` means the domain is verified. Unkey has configured routing and requested a certificate. Each entry in `dnsRecords` has a `verified` flag. The flag shows which records Unkey has read back, so you can see which records are still missing. Some providers hide a record from DNS lookups, for example a proxied or flattened routing record. Such a record stays `false` while it serves traffic. `verificationError` gives the reason for the last failed attempt. `dnsRecords` contains the same values that `domains.createDomain` returned. Use it to recover the values without creating the domain again. **Important**: verification stops 24 hours after the domain was created, and the status becomes `failed`. The window starts at `createdAt`, not at the last attempt. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.read_domain` (to read domains in any environment) - `environment.<environment_id>.read_domain` (to read domains in a specific environment) */
export const domainsGetDomain: API.OperationMethod<
  DomainsGetDomainRequest,
  V2DomainsGetDomainResponseBody,
  DomainsGetDomainError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DomainsGetDomainRequest,
  output: V2DomainsGetDomainResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type DomainsListDomainsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** List domains List the custom domains attached to an environment and their verification status. Results are paginated and sorted by their id. When `hasMore` is true, send the returned `cursor` to get the next page. An environment with no domains returns an empty array, not a 404. `status: verified` means the domain is verified. Unkey has configured routing and requested a certificate. Each domain includes its full `dnsRecords`. Each record has a `verified` flag. The flag shows which records Unkey has read back, so you can see which records are still missing without a second call. Some providers hide a record from DNS lookups, for example a proxied or flattened routing record. Such a record stays `false` while it serves traffic. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.read_domain` (to read domains in any environment) - `environment.<environment_id>.read_domain` (to read domains in a specific environment) */
export const domainsListDomains: API.PaginatedOperationMethod<
  DomainsListDomainsRequest,
  V2DomainsListDomainsResponseBody,
  DomainsListDomainsError,
  UnkeyOpContext,
  Domain
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: DomainsListDomainsRequest,
    output: V2DomainsListDomainsResponseBody,
    errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
    protocol: UnkeyProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pagination.cursor",
      items: "data",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type DomainsVerifyDomainError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Verify domain Restart verification for a custom domain. Address the domain by its ID or by its name. Names are unique per workspace, so `api.acme.com` is enough. Call this after you correct the DNS records of a domain that shows `failed`, or to give a `pending` domain a new 24-hour verification period. The domain goes back to `pending` and the 24-hour period starts again. The endpoint returns when Unkey accepts the retry. Poll `domains.getDomain` for the result. A domain that is already `verified` returns a 412. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.verify_domain` (to verify domains in any environment) - `environment.<environment_id>.verify_domain` (to verify domains in a specific environment) */
export const domainsVerifyDomain: API.OperationMethod<
  DomainsVerifyDomainRequest,
  DomainsVerifyDomainResponse,
  DomainsVerifyDomainError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DomainsVerifyDomainRequest,
  output: DomainsVerifyDomainResponse,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type EnvironmentsGetEnvironmentError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Get environment Retrieve a single environment by its id. Use this to fetch environment details after creation or to verify an environment exists before performing operations. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.read_environment` (to read any environment) - `environment.<environment_id>.read_environment` (to read a specific environment) */
export const environmentsGetEnvironment: API.OperationMethod<
  EnvironmentsGetEnvironmentRequest,
  V2EnvironmentsGetEnvironmentResponseBody,
  EnvironmentsGetEnvironmentError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EnvironmentsGetEnvironmentRequest,
  output: V2EnvironmentsGetEnvironmentResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type EnvironmentsListEnvironmentsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** List environments Retrieve the environments within an app. Use this to enumerate every environment in an app. Identify the app by its project slug and app slug. Results are ordered by environment id. An app has only a handful of environments, so all of them are returned in a single response. **Required Permissions** Your root key must have the following permission: - `environment.*.read_environment` (to read environments in any app) */
export const environmentsListEnvironments: API.OperationMethod<
  EnvironmentsListEnvironmentsRequest,
  V2EnvironmentsListEnvironmentsResponseBody,
  EnvironmentsListEnvironmentsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EnvironmentsListEnvironmentsRequest,
  output: V2EnvironmentsListEnvironmentsResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type EnvironmentsListEnvironmentVariablesError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** List environment variables Retrieve the environment variables for an environment. Identify the environment by its project, app, and environment identifiers. Results are ordered by variable id and paginated; when `hasMore` is true, pass the returned `cursor` to fetch the next page. `recoverable` variables are returned with their decrypted plaintext value. `writeonly` variables never expose a value: only the key, kind, and description are returned. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.read_environment_variables` (for any environment) - `environment.<environment_id>.read_environment_variables` (for a specific environment) */
export const environmentsListEnvironmentVariables: API.PaginatedOperationMethod<
  EnvironmentsListEnvironmentVariablesRequest,
  V2EnvironmentsListEnvironmentVariablesResponseBody,
  EnvironmentsListEnvironmentVariablesError,
  UnkeyOpContext,
  EnvironmentVariable
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: EnvironmentsListEnvironmentVariablesRequest,
    output: V2EnvironmentsListEnvironmentVariablesResponseBody,
    errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
    protocol: UnkeyProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pagination.cursor",
      items: "data",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type EnvironmentsRemoveEnvironmentVariablesError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Remove environment variables Remove environment variables from an environment in a single atomic request. This operation only deletes keys by name. Keys in the payload that exist are removed; keys that are not present are ignored, since their absence already matches the requested state. To replace or update values, use `setEnvironmentVariables` instead. The whole operation is atomic: if any part fails the environment is left unchanged. Duplicate keys in the payload collapse to a single removal. Values are never read or returned. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.remove_environment_variables` (for any environment) - `environment.<environment_id>.remove_environment_variables` (for a specific environment) */
export const environmentsRemoveEnvironmentVariables: API.OperationMethod<
  EnvironmentsRemoveEnvironmentVariablesRequest,
  V2EnvironmentsRemoveEnvironmentVariablesResponseBody,
  EnvironmentsRemoveEnvironmentVariablesError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EnvironmentsRemoveEnvironmentVariablesRequest,
  output: V2EnvironmentsRemoveEnvironmentVariablesResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type EnvironmentsSetEnvironmentVariablesError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Set environment variables Create or update environment variables for an environment in a single atomic request. By default this is an upsert: each variable in the payload is created if new or fully overwritten if the key already exists, and any variable not in the payload is left untouched. This lets you change one variable without re-sending the others, which matters for write-only secrets you can no longer read back. Set `prune: true` to make it a full replace instead: after upserting, every variable not in the payload is deleted. Sending `prune: true` with an empty `variables` list resets the environment by deleting every variable. Each variable is written exactly as sent, never merged, so omitted optional fields fall back to their defaults rather than the previous value. Values are always encrypted at rest. Set `kind: recoverable` to allow a value to be read back; it defaults to `writeonly`, which can never be read back through the API. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.set_environment_variables` (for any environment) - `environment.<environment_id>.set_environment_variables` (for a specific environment) */
export const environmentsSetEnvironmentVariables: API.OperationMethod<
  EnvironmentsSetEnvironmentVariablesRequest,
  V2EnvironmentsSetEnvironmentVariablesResponseBody,
  EnvironmentsSetEnvironmentVariablesError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EnvironmentsSetEnvironmentVariablesRequest,
  output: V2EnvironmentsSetEnvironmentVariablesResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type EnvironmentsUpdateSettingsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Update environment settings Update the build, runtime, and regional settings for an environment. All settings fields are optional. Omit a field to leave it unchanged. For nullable fields (`dockerfile`, `healthcheck`, `openapiSpecPath`), send null to clear the value. When `regions` is present it replaces the full set of regions for the environment. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.update_environment` (to update any environment) - `environment.<environment_id>.update_environment` (to update a specific environment) */
export const environmentsUpdateSettings: API.OperationMethod<
  EnvironmentsUpdateSettingsRequest,
  V2EnvironmentsUpdateSettingsResponseBody,
  EnvironmentsUpdateSettingsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: EnvironmentsUpdateSettingsRequest,
  output: V2EnvironmentsUpdateSettingsResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type GatewayListPoliciesError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** List policies Retrieve an environment's gateway policies in evaluation order: the gateway evaluates them top to bottom and the first rejection short-circuits the request. The full policy list is returned in a single response. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.read_policies` (for any environment) - `environment.<environment_id>.read_policies` (for a specific environment) */
export const gatewayListPolicies: API.OperationMethod<
  GatewayListPoliciesRequest,
  V2GatewayListPoliciesResponseBody,
  GatewayListPoliciesError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GatewayListPoliciesRequest,
  output: V2GatewayListPoliciesResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type GatewaySetPoliciesError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Set policies Replace an environment's gateway policies in a single atomic request. Policies run at the edge before requests reach your app: verify API keys, rate limit, block requests outright, or validate them against your OpenAPI spec. Policies are an ordered list: the gateway evaluates them top to bottom and the first rejection short-circuits the request. Each policy sets exactly one of `keyauth`, `ratelimit`, `firewall` or `openapi`, plus optional `match` expressions restricting which requests it applies to. Every call is a full replace: the environment's policies become exactly the request list in the given order, and the server generates a fresh id for each one. An empty list removes all policies. The operation is atomic: if any policy is invalid, nothing is written. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.set_policies` (for any environment) - `environment.<environment_id>.set_policies` (for a specific environment) */
export const gatewaySetPolicies: API.OperationMethod<
  GatewaySetPoliciesRequest,
  V2GatewaySetPoliciesResponseBody,
  GatewaySetPoliciesError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GatewaySetPoliciesRequest,
  output: V2GatewaySetPoliciesResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type GatewayUpdatePolicyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Update policy Update a single policy in place without resending the environment's full policy list. The policy keeps its id and its position in the evaluation order, and all other policies are untouched. Omitted fields keep their stored values; at least one updatable field must be provided. Setting `match` to null removes all match expressions so the policy applies to every request. Providing one of `keyauth`, `ratelimit`, `firewall` or `openapi` replaces the policy's rule entirely, including switching its type; at most one may be set. Policy ids are regenerated whenever `gateway.setPolicies` replaces the list, so fetch current ids via `gateway.listPolicies` first. **Required Permissions** Your root key must have one of the following permissions: - `environment.*.update_policy` (for any environment) - `environment.<environment_id>.update_policy` (for a specific environment) */
export const gatewayUpdatePolicy: API.OperationMethod<
  GatewayUpdatePolicyRequest,
  V2GatewayUpdatePolicyResponseBody,
  GatewayUpdatePolicyError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GatewayUpdatePolicyRequest,
  output: V2GatewayUpdatePolicyResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type GithubInstallAppError = BadRequest | Forbidden | UnkeyOpError;
/** Install GitHub App Start installing the Unkey GitHub App for your workspace. Returns a GitHub App install URL: open it in a browser to install the app and grant repository access. After installation GitHub returns to Unkey, which binds the installation to your workspace and lands you in the workspace settings. Installation is workspace-wide and takes no parameters. Once installed, link repositories to individual apps with the `git` field on `apps.createApp` and `apps.updateApp`. **Required Permissions** Your root key must have the following permission: - `workspace.*.install_github` */
export const githubInstallApp: API.OperationMethod<
  GithubInstallAppRequest,
  V2GithubInstallAppResponseBody,
  GithubInstallAppError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GithubInstallAppRequest,
  output: V2GithubInstallAppResponseBody,
  errors: [BadRequest, Forbidden, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type IdentitiesCreateIdentityError =
  | BadRequest
  | Forbidden
  | Conflict
  | UnkeyOpError;
/** Create Identity Create an identity to group multiple API keys under a single entity. Identities enable shared rate limits and metadata across all associated keys. Perfect for users with multiple devices, organizations with multiple API keys, or when you need unified rate limiting across different services. **Important** Requires `identity.*.create_identity` permission */
export const identitiesCreateIdentity: API.OperationMethod<
  IdentitiesCreateIdentityRequest,
  V2IdentitiesCreateIdentityResponseBody,
  IdentitiesCreateIdentityError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: IdentitiesCreateIdentityRequest,
  output: V2IdentitiesCreateIdentityResponseBody,
  errors: [BadRequest, Forbidden, Conflict, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type IdentitiesDeleteIdentityError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Delete Identity Permanently delete an identity. This operation cannot be undone. Use this for data cleanup, compliance requirements, or when removing entities from your system. > **Important** > Requires `identity.*.delete_identity` permission > Associated API keys remain functional but lose shared resources > External ID becomes available for reuse immediately */
export const identitiesDeleteIdentity: API.OperationMethod<
  IdentitiesDeleteIdentityRequest,
  V2IdentitiesDeleteIdentityResponseBody,
  IdentitiesDeleteIdentityError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: IdentitiesDeleteIdentityRequest,
  output: V2IdentitiesDeleteIdentityResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type IdentitiesGetIdentityError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Get Identity Retrieve an identity by external ID. Returns metadata, rate limits, and other associated data. Use this to check if an identity exists, view configurations, or build management dashboards. > **Important** > Requires `identity.*.read_identity` permission */
export const identitiesGetIdentity: API.OperationMethod<
  IdentitiesGetIdentityRequest,
  V2IdentitiesGetIdentityResponseBody,
  IdentitiesGetIdentityError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: IdentitiesGetIdentityRequest,
  output: V2IdentitiesGetIdentityResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type IdentitiesListIdentitiesError =
  | BadRequest
  | Forbidden
  | UnkeyOpError;
/** List Identities Get a paginated list of all identities in your workspace. Returns metadata and rate limit configurations. Perfect for building management dashboards, auditing configurations, or browsing your identities. > **Important** > Requires `identity.*.read_identity` permission */
export const identitiesListIdentities: API.PaginatedOperationMethod<
  IdentitiesListIdentitiesRequest,
  V2IdentitiesListIdentitiesResponseBody,
  IdentitiesListIdentitiesError,
  UnkeyOpContext,
  Identity
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: IdentitiesListIdentitiesRequest,
    output: V2IdentitiesListIdentitiesResponseBody,
    errors: [BadRequest, Forbidden, UnknownUnkeyError],
    protocol: UnkeyProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pagination.cursor",
      items: "data",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type IdentitiesUpdateIdentityError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Update Identity Update an identity's metadata and rate limits. Only specified fields are modified - others remain unchanged. Perfect for subscription changes, plan upgrades, or updating user information. Changes take effect immediately. > **Important** > Requires `identity.*.update_identity` permission > Rate limit changes propagate within 30 seconds */
export const identitiesUpdateIdentity: API.OperationMethod<
  IdentitiesUpdateIdentityRequest,
  V2IdentitiesUpdateIdentityResponseBody,
  IdentitiesUpdateIdentityError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: IdentitiesUpdateIdentityRequest,
  output: V2IdentitiesUpdateIdentityResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysAddPermissionsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Add key permissions Add permissions to a key without affecting existing permissions. Use this for privilege upgrades, enabling new features, or plan changes that grant additional capabilities. Permissions granted through roles remain unchanged. **Important**: Changes take effect immediately with up to 30-second edge propagation. **Required Permissions** Your root key must have one of the following permissions: - `api.*.update_key` (to update keys in any API) - `api.<api_id>.update_key` (to update keys in a specific API) **Side Effects** Invalidates the key cache for immediate effect, and makes permissions available for verification within 30 seconds across all regions. */
export const keysAddPermissions: API.OperationMethod<
  KeysAddPermissionsRequest,
  V2KeysAddPermissionsResponseBody,
  KeysAddPermissionsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysAddPermissionsRequest,
  output: V2KeysAddPermissionsResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysAddRolesError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Add key roles Add roles to a key without affecting existing roles or permissions. Use this for privilege upgrades, enabling new feature sets, or subscription changes that grant additional role-based capabilities. Direct permissions remain unchanged. **Important**: Changes take effect immediately with up to 30-second edge propagation. **Required Permissions** Your root key must have one of the following permissions: - `api.*.update_key` (to update keys in any API) - `api.<api_id>.update_key` (to update keys in a specific API) **Side Effects** Invalidates the key cache for immediate effect, and makes role assignments available for verification within 30 seconds across all regions. */
export const keysAddRoles: API.OperationMethod<
  KeysAddRolesRequest,
  V2KeysAddRolesResponseBody,
  KeysAddRolesError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysAddRolesRequest,
  output: V2KeysAddRolesResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysCreateKeyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Create API key Create a new API key for user authentication and authorization. Use this endpoint when users sign up, upgrade subscription tiers, or need additional keys. Keys are cryptographically secure and unique to the specified API namespace. **Important**: The key is returned only once. Store it immediately and provide it to your user, as it cannot be retrieved later. **Common use cases:** - Generate keys for new user registrations - Create additional keys for different applications - Issue keys with specific permissions or limits **Required Permissions** Your credential needs one of: - `api.*.create_key` (create keys in any API) - `api.<api_id>.create_key` (create keys in specific API) - `unkey:v1:<workspace_id>:keyspaces/*#create_key` (create keys in any keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>#create_key` (create keys in a specific keyspace) */
export const keysCreateKey: API.OperationMethod<
  KeysCreateKeyRequest,
  V2KeysCreateKeyResponseBody,
  KeysCreateKeyError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysCreateKeyRequest,
  output: V2KeysCreateKeyResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysDeleteKeyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Delete API keys Delete API keys from user accounts or for cleanup purposes. Use this for user-requested key revocation, account deletion workflows, or cleaning up unused keys. Keys are immediately invalidated. Two modes: soft delete (default, preserves audit records) and permanent delete. **Important**: For temporary access control, use `updateKey` with `enabled: false` instead of deletion. **Required Permissions** Your credential must have one of the following permissions: - `api.*.delete_key` (to delete keys in any API) - `api.<api_id>.delete_key` (to delete keys in a specific API) - `unkey:v1:<workspace_id>:keyspaces/*\/keys/*#delete_key` (to delete keys in any keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/*#delete_key` (to delete keys in a specific keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/<key_id>#delete_key` (to delete a specific key) */
export const keysDeleteKey: API.OperationMethod<
  KeysDeleteKeyRequest,
  V2KeysDeleteKeyResponseBody,
  KeysDeleteKeyError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysDeleteKeyRequest,
  output: V2KeysDeleteKeyResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysGetKeyError = BadRequest | Forbidden | NotFound | UnkeyOpError;
/** Get API key Retrieve detailed key information for dashboard interfaces and administrative purposes. Use this to build key management dashboards showing users their key details, status, permissions, and usage data. You can identify keys by `keyId` or the actual key string. **Important**: Set `decrypt: true` only in secure contexts to retrieve plaintext key values from recoverable keys. **Required Permissions** Your credential must have one of the following permissions for basic key information: - `api.*.read_key` (to read keys from any API) - `api.<api_id>.read_key` (to read keys from a specific API) - `unkey:v1:<workspace_id>:keyspaces/*\/keys/*#read_key` (to read keys in any keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/*#read_key` (to read keys in a specific keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/<key_id>#read_key` (to read a specific key) Additional permission required for decrypt functionality: - `api.*.decrypt_key` or `api.<api_id>.decrypt_key` - `unkey:v1:<workspace_id>:keyspaces/*\/keys/*#decrypt_key` - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/*#decrypt_key` - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/<key_id>#decrypt_key` */
export const keysGetKey: API.OperationMethod<
  KeysGetKeyRequest,
  V2KeysGetKeyResponseBody,
  KeysGetKeyError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysGetKeyRequest,
  output: V2KeysGetKeyResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysMigrateKeysError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Migrate API key(s) Returns HTTP 200 even on partial success; hashes that could not be migrated are listed under `data.failed`. **Required Permissions** Your root key must have one of the following permissions for basic key information: - `api.*.create_key` (to migrate keys to any API) - `api.<api_id>.create_key` (to migrate keys to a specific API) */
export const keysMigrateKeys: API.OperationMethod<
  KeysMigrateKeysRequest,
  V2KeysMigrateKeysResponseBody,
  KeysMigrateKeysError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysMigrateKeysRequest,
  output: V2KeysMigrateKeysResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysRemovePermissionsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Remove key permissions Remove permissions from a key without affecting existing roles or other permissions. Use this for privilege downgrades, removing temporary access, or plan changes that revoke specific capabilities. Permissions granted through roles remain unchanged. **Important**: Changes take effect immediately with up to 30-second edge propagation. **Required Permissions** Your root key must have one of the following permissions: - `api.*.update_key` (to update keys in any API) - `api.<api_id>.update_key` (to update keys in a specific API) **Side Effects** Invalidates the key cache for immediate effect, and makes permission changes available for verification within 30 seconds across all regions. */
export const keysRemovePermissions: API.OperationMethod<
  KeysRemovePermissionsRequest,
  V2KeysRemovePermissionsResponseBody,
  KeysRemovePermissionsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysRemovePermissionsRequest,
  output: V2KeysRemovePermissionsResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysRemoveRolesError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Remove key roles Remove roles from a key without affecting direct permissions or other roles. Use this for privilege downgrades, removing temporary access, or subscription changes that revoke specific role-based capabilities. Direct permissions remain unchanged. **Important**: Changes take effect immediately with up to 30-second edge propagation. **Required Permissions** Your root key must have one of the following permissions: - `api.*.update_key` (to update keys in any API) - `api.<api_id>.update_key` (to update keys in a specific API) **Side Effects** Invalidates the key cache for immediate effect, and makes role changes available for verification within 30 seconds across all regions. */
export const keysRemoveRoles: API.OperationMethod<
  KeysRemoveRolesRequest,
  V2KeysRemoveRolesResponseBody,
  KeysRemoveRolesError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysRemoveRolesRequest,
  output: V2KeysRemoveRolesResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysRerollKeyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Reroll Key Generate a new API key while preserving the configuration from an existing key. This operation creates a fresh key with a new token while maintaining all settings from the original key: - Permissions and roles - Custom metadata - Rate limit configurations - Identity associations - Remaining credits - Recovery settings **Key Generation:** - The system attempts to extract the prefix from the original key - If prefix extraction fails, the default API prefix is used - Key length follows the API's default byte configuration (or 16 bytes if not specified) **Original Key Handling:** - The original key will be revoked after the duration specified in `expiration` - Set `expiration` to 0 to revoke immediately - This allows for graceful key rotation with an overlap period Common use cases include: - Rotating keys for security compliance - Issuing replacement keys for compromised credentials - Creating backup keys with identical permissions **Important:** Analytics and usage metrics are tracked at both the key level AND identity level. If the original key has an identity, the new key will inherit it, allowing you to track usage across both individual keys and the overall identity. **Required Permissions** Your credential must have: - `api.*.create_key` or `api.<api_id>.create_key` - `unkey:v1:<workspace_id>:keyspaces/*#create_key` or `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>#create_key` - `api.*.encrypt_key` or `api.<api_id>.encrypt_key` (only when the original key is recoverable) - `unkey:v1:<workspace_id>:keyspaces/*\/keys/*#encrypt_key` or `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/*#encrypt_key` (only when the original key is recoverable) */
export const keysRerollKey: API.OperationMethod<
  KeysRerollKeyRequest,
  V2KeysRerollKeyResponseBody,
  KeysRerollKeyError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysRerollKeyRequest,
  output: V2KeysRerollKeyResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysSetPermissionsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Set key permissions Replace all permissions on a key with the specified set in a single atomic operation. Use this to synchronize with external systems, reset permissions to a known state, or apply standardized permission templates. Permissions granted through roles remain unchanged. **Important**: Changes take effect immediately with up to 30-second edge propagation. **Required Permissions** Your root key must have one of the following permissions: - `api.*.update_key` (to update keys in any API) - `api.<api_id>.update_key` (to update keys in a specific API) **Side Effects** Invalidates the key cache for immediate effect, and makes permission changes available for verification within 30 seconds across all regions. */
export const keysSetPermissions: API.OperationMethod<
  KeysSetPermissionsRequest,
  V2KeysSetPermissionsResponseBody,
  KeysSetPermissionsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysSetPermissionsRequest,
  output: V2KeysSetPermissionsResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysSetRolesError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Set key roles Replace all roles on a key with the specified set in a single atomic operation. Use this to synchronize with external systems, reset roles to a known state, or apply standardized role templates. Direct permissions are never affected. **Important**: Changes take effect immediately with up to 30-second edge propagation. **Required Permissions** Your root key must have one of the following permissions: - `api.*.update_key` (to update keys in any API) - `api.<api_id>.update_key` (to update keys in a specific API) **Side Effects** Invalidates the key cache for immediate effect, and makes role changes available for verification within 30 seconds across all regions. */
export const keysSetRoles: API.OperationMethod<
  KeysSetRolesRequest,
  V2KeysSetRolesResponseBody,
  KeysSetRolesError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysSetRolesRequest,
  output: V2KeysSetRolesResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysUpdateCreditsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Update key credits Update credit quotas in response to plan changes, billing cycles, or usage purchases. Use this for user upgrades/downgrades, monthly quota resets, credit purchases, or promotional bonuses. Supports three operations: set, increment, or decrement credits. Set to null for unlimited usage. **Important**: Setting unlimited credits automatically clears existing refill configurations. **Required Permissions** Your credential must have one of the following permissions: - `api.*.update_key` (to update keys in any API) - `api.<api_id>.update_key` (to update keys in a specific API) - `unkey:v1:<workspace_id>:keyspaces/*\/keys/*#update_key` (to update keys in any keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/*#update_key` (to update keys in a specific keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/<key_id>#update_key` (to update a specific key) **Side Effects** Credit updates remove the key from cache immediately. Setting credits to unlimited automatically clears any existing refill settings. Changes take effect instantly but may take up to 30 seconds to propagate to all edge regions. */
export const keysUpdateCredits: API.OperationMethod<
  KeysUpdateCreditsRequest,
  V2KeysUpdateCreditsResponseBody,
  KeysUpdateCreditsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysUpdateCreditsRequest,
  output: V2KeysUpdateCreditsResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysUpdateKeyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Update key settings Update key properties in response to plan changes, subscription updates, or account status changes. Use this for user upgrades/downgrades, role modifications, or administrative changes. Supports partial updates - only specify fields you want to change. Set fields to null to clear them. **Important**: Permissions and roles are replaced entirely. Use dedicated add/remove endpoints for incremental changes. **Required Permissions** Your credential must have one of the following permissions: - `api.*.update_key` (to update keys in any API) - `api.<api_id>.update_key` (to update keys in a specific API) - `unkey:v1:<workspace_id>:keyspaces/*\/keys/*#update_key` (to update keys in any keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/*#update_key` (to update keys in a specific keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/<key_id>#update_key` (to update a specific key) **Side Effects** If you specify an `externalId` that doesn't exist, a new identity will be automatically created and linked to the key. Permission updates will auto-create any permissions that don't exist in your workspace. Changes take effect immediately but may take up to 30 seconds to propagate to all edge regions due to cache invalidation. */
export const keysUpdateKey: API.OperationMethod<
  KeysUpdateKeyRequest,
  V2KeysUpdateKeyResponseBody,
  KeysUpdateKeyError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysUpdateKeyRequest,
  output: V2KeysUpdateKeyResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysVerifyKeyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Verify API key Verify an API key's validity and permissions for request authentication. Use this endpoint on every incoming request to your protected resources. It checks key validity, permissions, rate limits, and usage quotas in a single call. **Important**: Returns HTTP 200 for all verification outcomes — check the `valid` field in response data to determine if the key is authorized. A 429 may be returned if the workspace exceeds its API rate limit. **Common use cases:** - Authenticate API requests before processing - Enforce permission-based access control - Track usage and apply rate limits **Required Permissions** Your credential needs one of: - `api.*.verify_key` (verify keys in any API) - `api.<api_id>.verify_key` (verify keys in specific API) - `unkey:v1:<workspace_id>:keyspaces/*\/keys/*#verify_key` (verify keys in any keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/*#verify_key` (verify keys in a specific keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/<key_id>#verify_key` (verify a specific key) **Note**: If your credential has no verify permissions at all, you will receive a `403 Forbidden` error. If your credential has verify permissions for a different API or keyspace than the key you're verifying, you will receive a `200` response with `code: NOT_FOUND` to avoid leaking key existence. */
export const keysVerifyKey: API.OperationMethod<
  KeysVerifyKeyRequest,
  V2KeysVerifyKeyResponseBody,
  KeysVerifyKeyError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysVerifyKeyRequest,
  output: V2KeysVerifyKeyResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type KeysWhoamiError = BadRequest | Forbidden | NotFound | UnkeyOpError;
/** Get API key by hash Find out what key this is. **Required Permissions** Your credential must have one of the following permissions for basic key information: - `api.*.read_key` (to read keys from any API) - `api.<api_id>.read_key` (to read keys from a specific API) - `unkey:v1:<workspace_id>:keyspaces/*\/keys/*#read_key` (to read keys in any keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/*#read_key` (to read keys in a specific keyspace) - `unkey:v1:<workspace_id>:keyspaces/<keyspace_id>/keys/<key_id>#read_key` (to read a specific key) If your credential lacks permissions but the key exists, we may return a 404 status here to prevent leaking the existence of a key to unauthorized clients. If you believe that a key should exist, but receive a 404, please double check your credential has the correct permissions. */
export const keysWhoami: API.OperationMethod<
  KeysWhoamiRequest,
  V2KeysWhoamiResponseBody,
  KeysWhoamiError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: KeysWhoamiRequest,
  output: V2KeysWhoamiResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type LivenessError = UnkeyOpError;
/** Health check Check if the Unkey API service is healthy and ready to handle requests. Use this for load balancer health checks, monitoring systems, and orchestration platforms. No authentication required with minimal processing overhead. **Required Permissions** None - this endpoint requires no authentication. **Side Effects** None - this is a read-only health check that does not modify any data or state. */
export const liveness: API.OperationMethod<
  LivenessRequest,
  V2LivenessResponseBody,
  LivenessError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LivenessRequest,
  output: V2LivenessResponseBody,
  errors: [UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PermissionsCreatePermissionError =
  | BadRequest
  | Forbidden
  | Conflict
  | UnkeyOpError;
/** Create permission Create a new permission to define specific actions or capabilities in your RBAC system. Permissions can be assigned directly to API keys or included in roles. Use hierarchical naming patterns like `documents.read`, `admin.users.delete`, or `billing.invoices.create` for clear organization. **Important:** Permission names must be unique within the workspace. Once created, permissions are immediately available for assignment. **Required Permissions** Your root key must have the following permission: - `rbac.*.create_permission` */
export const permissionsCreatePermission: API.OperationMethod<
  PermissionsCreatePermissionRequest,
  V2PermissionsCreatePermissionResponseBody,
  PermissionsCreatePermissionError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PermissionsCreatePermissionRequest,
  output: V2PermissionsCreatePermissionResponseBody,
  errors: [BadRequest, Forbidden, Conflict, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PermissionsCreateRoleError =
  | BadRequest
  | Forbidden
  | Conflict
  | UnkeyOpError;
/** Create role Create a new role to group related permissions for easier management. Roles enable consistent permission assignment across multiple API keys. Permission slugs supplied in `permissions` are attached during creation. Missing permissions are created automatically. **Important:** Role names must be unique within the workspace. Once created, roles are immediately available for assignment. **Required Permissions** Your root key must always have: - `rbac.*.create_role` When `permissions` is not empty, it must also have: - `rbac.*.add_permission_to_role` When any requested permission slug does not exist, it must also have: - `rbac.*.create_permission` */
export const permissionsCreateRole: API.OperationMethod<
  PermissionsCreateRoleRequest,
  V2PermissionsCreateRoleResponseBody,
  PermissionsCreateRoleError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PermissionsCreateRoleRequest,
  output: V2PermissionsCreateRoleResponseBody,
  errors: [BadRequest, Forbidden, Conflict, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PermissionsDeletePermissionError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Delete permission Remove a permission from your workspace. This also removes the permission from all API keys and roles. **Important:** This operation cannot be undone and immediately affects all API keys and roles that had this permission assigned. **Required Permissions** Your root key must have the following permission: - `rbac.*.delete_permission` */
export const permissionsDeletePermission: API.OperationMethod<
  PermissionsDeletePermissionRequest,
  V2PermissionsDeletePermissionResponseBody,
  PermissionsDeletePermissionError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PermissionsDeletePermissionRequest,
  output: V2PermissionsDeletePermissionResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PermissionsDeleteRoleError = BadRequest | Forbidden | UnkeyOpError;
/** Delete role Remove a role from your workspace. This also removes the role from all assigned API keys. **Important:** This operation cannot be undone and immediately affects all API keys that had this role assigned. **Required Permissions** Your root key must have the following permission: - `rbac.*.delete_role` */
export const permissionsDeleteRole: API.OperationMethod<
  PermissionsDeleteRoleRequest,
  V2PermissionsDeleteRoleResponseBody,
  PermissionsDeleteRoleError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PermissionsDeleteRoleRequest,
  output: V2PermissionsDeleteRoleResponseBody,
  errors: [BadRequest, Forbidden, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PermissionsGetPermissionError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Get permission Retrieve details about a specific permission including its name, description, and metadata. **Required Permissions** Your root key must have the following permission: - `rbac.*.read_permission` */
export const permissionsGetPermission: API.OperationMethod<
  PermissionsGetPermissionRequest,
  V2PermissionsGetPermissionResponseBody,
  PermissionsGetPermissionError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PermissionsGetPermissionRequest,
  output: V2PermissionsGetPermissionResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PermissionsGetRoleError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Get role Retrieve details about a specific role including its assigned permissions. **Required Permissions** Your root key must have the following permission: - `rbac.*.read_role` */
export const permissionsGetRole: API.OperationMethod<
  PermissionsGetRoleRequest,
  V2PermissionsGetRoleResponseBody,
  PermissionsGetRoleError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PermissionsGetRoleRequest,
  output: V2PermissionsGetRoleResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PermissionsListPermissionsError =
  | BadRequest
  | Forbidden
  | UnkeyOpError;
/** List permissions Retrieve all permissions in your workspace. Results are paginated and sorted by their id. **Required Permissions** Your root key must have the following permission: - `rbac.*.read_permission` */
export const permissionsListPermissions: API.PaginatedOperationMethod<
  PermissionsListPermissionsRequest,
  V2PermissionsListPermissionsResponseBody,
  PermissionsListPermissionsError,
  UnkeyOpContext,
  Permission
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: PermissionsListPermissionsRequest,
    output: V2PermissionsListPermissionsResponseBody,
    errors: [BadRequest, Forbidden, UnknownUnkeyError],
    protocol: UnkeyProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pagination.cursor",
      items: "data",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type PermissionsListRolesError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** List roles Retrieve all roles in your workspace including their assigned permissions. Results are paginated and sorted by their id. **Required Permissions** Your root key must have the following permission: - `rbac.*.read_role` */
export const permissionsListRoles: API.PaginatedOperationMethod<
  PermissionsListRolesRequest,
  V2PermissionsListRolesResponseBody,
  PermissionsListRolesError,
  UnkeyOpContext,
  Role
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: PermissionsListRolesRequest,
    output: V2PermissionsListRolesResponseBody,
    errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
    protocol: UnkeyProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pagination.cursor",
      items: "data",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type PermissionsSetRolePermissionsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Set role permissions Atomically replaces all permissions directly assigned to a role. An empty `permissions` array removes every permission from the role. Permissions that do not exist are created when the caller has permission to create them. **Required Permissions** Your root key must have: - `rbac.*.add_permission_to_role` - `rbac.*.remove_permission_from_role` When any requested permission slug does not exist, it must also have: - `rbac.*.create_permission` */
export const permissionsSetRolePermissions: API.OperationMethod<
  PermissionsSetRolePermissionsRequest,
  V2PermissionsSetRolePermissionsResponseBody,
  PermissionsSetRolePermissionsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PermissionsSetRolePermissionsRequest,
  output: V2PermissionsSetRolePermissionsResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PortalCreatePortalError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | UnkeyOpError;
/** Create portal Create a portal for one app or keyspace in your workspace. Unreleased and subject to change without notice. Send exactly one of `keyspaceId` or `appId`. That resource must belong to your workspace, and it can back only one portal, so a second portal for the same resource is a **409**. `displayName` is what your end users see. It is yours to set and change independently of the resource the portal serves. **Required Permissions** Your root key must have `portal.*.create_portal`. A grant scoped to a specific portal id does not authorize creation, because the id does not exist yet. */
export const portalCreatePortal: API.OperationMethod<
  PortalCreatePortalRequest,
  V2PortalCreatePortalResponseBody,
  PortalCreatePortalError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PortalCreatePortalRequest,
  output: V2PortalCreatePortalResponseBody,
  errors: [BadRequest, Forbidden, NotFound, Conflict, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PortalCreateSessionError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Create portal session Create a portal session for an end user and get the URL to redirect them to. The URL carries a single-use exchange code valid for 15 minutes, which the portal redeems exactly once for a 24-hour access token via `portal.exchangeCode`. **Required Permissions** Authorization runs in two stages, and both must pass. First, your root key must have one of the following permissions: - `portal.*.create_portal_session` (to mint sessions for any portal in the workspace) - `portal.<portal_id>.create_portal_session` (to mint sessions for a specific portal) Second, a session can never carry a capability your root key does not itself hold. Each requested scope additionally requires the equivalent permission on every keyspace the portal resolves to: - `keys:read` requires `api.<api_id>.read_key` **and** `api.<api_id>.read_api` - `keys:reroll` and `keys:create` require `api.<api_id>.create_key`, plus `api.<api_id>.encrypt_key` when the keyspace stores encrypted keys - `analytics:read` requires `api.<api_id>.read_analytics` The `*` form of each is also accepted. Requesting a scope you do not hold returns 403 for the whole request rather than minting a reduced session, so a missing grant is visible instead of surfacing later as a broken portal. Missing the portal permission itself returns **404**, not 403: a caller who cannot mint for a portal is not told whether it exists. Your root key must also be associated with a workspace that has an enabled portal. */
export const portalCreateSession: API.OperationMethod<
  PortalCreateSessionRequest,
  V2PortalCreateSessionResponseBody,
  PortalCreateSessionError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PortalCreateSessionRequest,
  output: V2PortalCreateSessionResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PortalDeletePortalError = BadRequest | NotFound | UnkeyOpError;
/** Delete portal Delete a portal and revoke the sessions it minted. Unreleased and subject to change without notice. Its end users lose access rather than keeping it until their tokens expire. Revocation is not instantaneous: session lookups are cached briefly, so a request already in flight may still succeed. The app or keyspace it served is untouched, and its slug becomes free for a new portal. **Required Permissions** Your root key must have one of: - `portal.*.delete_portal` (to delete any portal in the workspace) - `portal.<portal_id>.delete_portal` (to delete a specific portal) Without the permission this returns **404**, not 403. */
export const portalDeletePortal: API.OperationMethod<
  PortalDeletePortalRequest,
  V2PortalDeletePortalResponseBody,
  PortalDeletePortalError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PortalDeletePortalRequest,
  output: V2PortalDeletePortalResponseBody,
  errors: [BadRequest, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PortalExchangeCodeError = BadRequest | UnkeyOpError;
/** Exchange portal code Exchange a short-lived code for a long-lived portal access token. This endpoint is unauthenticated. The code itself serves as proof of authorization. Each code can only be redeemed once; subsequent attempts return 401. The returned access token is valid for 24 hours and should be stored as an httpOnly cookie or used in the Authorization header for subsequent API calls. */
export const portalExchangeCode: API.OperationMethod<
  PortalExchangeCodeRequest,
  V2PortalExchangeCodeResponseBody,
  PortalExchangeCodeError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PortalExchangeCodeRequest,
  output: V2PortalExchangeCodeResponseBody,
  errors: [BadRequest, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PortalGetPortalError = BadRequest | NotFound | UnkeyOpError;
/** Get portal Read one portal, by its id or slug, or by the resource it serves. Unreleased and subject to change without notice. Send exactly one of `portal`, `keyspaceId`, or `appId`. Sending more than one is a **400**. **Required Permissions** Your root key must have one of: - `portal.*.read_portal` (to read any portal in the workspace) - `portal.<portal_id>.read_portal` (to read a specific portal) Without the permission this returns **404**, not 403. */
export const portalGetPortal: API.OperationMethod<
  PortalGetPortalRequest,
  V2PortalGetPortalResponseBody,
  PortalGetPortalError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PortalGetPortalRequest,
  output: V2PortalGetPortalResponseBody,
  errors: [BadRequest, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PortalGetVerificationsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Get portal verifications Return a verification analytics timeseries for the authenticated portal session's end user. Authenticates only with a portal session cookie and always restricts results to verification events attributed to the session's external identity. Unlike `analytics.getVerifications`, this endpoint takes a fixed time window (no query language) and returns a zero-filled, outcome-broken-out timeseries. Bucket granularity is chosen automatically from the window size. */
export const portalGetVerifications: API.OperationMethod<
  PortalGetVerificationsRequest,
  V2PortalGetVerificationsResponseBody,
  PortalGetVerificationsError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PortalGetVerificationsRequest,
  output: V2PortalGetVerificationsResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PortalListKeysError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** List portal keys Retrieve a paginated list of API keys owned by the authenticated portal session's end user. This is the portal-scoped variant of `apis.listKeys`. It authenticates only with a portal session cookie and always restricts results to the keys owned by the session's external identity, within the keyspaces configured on the portal configuration. Both the identity and the keyspaces come from the session, so the request body has no `externalId` or `apiId` field. */
export const portalListKeys: API.PaginatedOperationMethod<
  PortalListKeysRequest,
  V2PortalListKeysResponseBody,
  PortalListKeysError,
  UnkeyOpContext,
  KeyResponseData
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: PortalListKeysRequest,
    output: V2PortalListKeysResponseBody,
    errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
    protocol: UnkeyProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pagination.cursor",
      items: "data",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type PortalRerollKeyError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Reroll portal key Reroll an API key owned by the authenticated portal session's end user, issuing a new key while preserving its configuration. This is the portal-scoped variant of `keys.rerollKey`. It authenticates only with a portal session cookie and may only reroll keys owned by the session's external identity; any other key returns 404. */
export const portalRerollKey: API.OperationMethod<
  PortalRerollKeyRequest,
  V2KeysRerollKeyResponseBody,
  PortalRerollKeyError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PortalRerollKeyRequest,
  output: V2KeysRerollKeyResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type PortalUpdatePortalError =
  | BadRequest
  | NotFound
  | Conflict
  | UnkeyOpError;
/** Update portal Change a portal's slug, display name, the resource it serves, its enabled state, or its branding. Unreleased and subject to change without notice. Only the fields you send change. Omitting a field leaves it as it is, and for branding, sending null clears it. Send at most one of `keyspaceId` or `appId`. Two changes affect your end users immediately: - Re-pointing at a different resource revokes the portal's live sessions, because a session carries the scope it was minted with. - Disabling stops new sessions but leaves live ones running until they expire. **Required Permissions** Your root key must have one of: - `portal.*.update_portal` (to update any portal in the workspace) - `portal.<portal_id>.update_portal` (to update a specific portal) Without the permission this returns **404**, not 403. */
export const portalUpdatePortal: API.OperationMethod<
  PortalUpdatePortalRequest,
  V2PortalUpdatePortalResponseBody,
  PortalUpdatePortalError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PortalUpdatePortalRequest,
  output: V2PortalUpdatePortalResponseBody,
  errors: [BadRequest, NotFound, Conflict, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type ProjectsCreateProjectError =
  | BadRequest
  | Forbidden
  | Conflict
  | UnkeyOpError;
/** Create project Create a project to group deployments and applications under a workspace-scoped slug. The slug you provide is the stable, caller-defined handle used to reference this project in subsequent operations (get, update, delete). It must be unique within your workspace. **Important**: The slug cannot collide with an existing project in your workspace. A duplicate slug returns a 409 conflict. **Required Permissions** Your root key must have the following permission: - `project.*.create_project` (to create projects in your workspace) */
export const projectsCreateProject: API.OperationMethod<
  ProjectsCreateProjectRequest,
  V2ProjectsCreateProjectResponseBody,
  ProjectsCreateProjectError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ProjectsCreateProjectRequest,
  output: V2ProjectsCreateProjectResponseBody,
  errors: [BadRequest, Forbidden, Conflict, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type ProjectsDeleteProjectError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Delete project Delete an existing project in your workspace, identified by its id. Deletion is asynchronous and eventually consistent. The project and all of its associated resources (apps, environments, deployments, custom domains) are torn down by a background workflow. A successful response indicates the deletion was enqueued, not that every resource has already been removed. Projects with delete protection enabled cannot be deleted until protection is disabled. **Required Permissions** Your root key must have one of the following permissions: - `project.*.delete_project` (to delete any project) - `project.<project_id>.delete_project` (to delete a specific project) */
export const projectsDeleteProject: API.OperationMethod<
  ProjectsDeleteProjectRequest,
  ProjectsDeleteProjectResponse,
  ProjectsDeleteProjectError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ProjectsDeleteProjectRequest,
  output: ProjectsDeleteProjectResponse,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type ProjectsGetProjectError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Get project Retrieve a single project in your workspace by its id. Use this to fetch project details after creation, verify a project exists before performing operations, or resolve a project's metadata from its id. **Required Permissions** Your root key must have one of the following permissions: - `project.*.read_project` (to read any project) - `project.<project_id>.read_project` (to read a specific project) */
export const projectsGetProject: API.OperationMethod<
  ProjectsGetProjectRequest,
  V2ProjectsGetProjectResponseBody,
  ProjectsGetProjectError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ProjectsGetProjectRequest,
  output: V2ProjectsGetProjectResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type ProjectsListProjectsError = BadRequest | Forbidden | UnkeyOpError;
/** List projects Retrieve a paginated list of projects in your workspace. Use this to build project management dashboards or to enumerate projects for administrative purposes. Results are ordered by project id and returned in pages. When `hasMore` is true, pass the returned `cursor` to fetch the next page. **Required Permissions** Your root key must have the following permission: - `project.*.read_project` (to read projects in your workspace) */
export const projectsListProjects: API.PaginatedOperationMethod<
  ProjectsListProjectsRequest,
  V2ProjectsListProjectsResponseBody,
  ProjectsListProjectsError,
  UnkeyOpContext,
  Project
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ProjectsListProjectsRequest,
    output: V2ProjectsListProjectsResponseBody,
    errors: [BadRequest, Forbidden, UnknownUnkeyError],
    protocol: UnkeyProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pagination.cursor",
      items: "data",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type ProjectsUpdateProjectError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | UnkeyOpError;
/** Update project Update an existing project in your workspace, identified by its id. The project name, slug, and delete protection setting can be changed. Omitted fields are left unchanged. Changing the slug affects the deployment domains generated for this project. **Required Permissions** Your root key must have one of the following permissions: - `project.*.update_project` (to update any project) - `project.<project_id>.update_project` (to update a specific project) */
export const projectsUpdateProject: API.OperationMethod<
  ProjectsUpdateProjectRequest,
  V2ProjectsUpdateProjectResponseBody,
  ProjectsUpdateProjectError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ProjectsUpdateProjectRequest,
  output: V2ProjectsUpdateProjectResponseBody,
  errors: [BadRequest, Forbidden, NotFound, Conflict, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type RatelimitDeleteOverrideError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Delete ratelimit override Permanently remove a rate limit override. Affected identifiers immediately revert to the namespace default. Use this to remove temporary overrides, reset identifiers to standard limits, or clean up outdated rules. **Important:** Deletion is immediate and permanent. The override cannot be recovered and must be recreated if needed again. **Permissions:** Requires `ratelimit.*.delete_override` or `ratelimit.<namespace_id>.delete_override` */
export const ratelimitDeleteOverride: API.OperationMethod<
  RatelimitDeleteOverrideRequest,
  V2RatelimitDeleteOverrideResponseBody,
  RatelimitDeleteOverrideError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RatelimitDeleteOverrideRequest,
  output: V2RatelimitDeleteOverrideResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type RatelimitGetOverrideError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Get ratelimit override Retrieve the configuration of a specific rate limit override by its identifier. Use this to inspect override configurations, audit rate limiting policies, or debug rate limiting behavior. **Important:** The identifier must match exactly as specified when creating the override, including wildcard patterns. **Permissions:** Requires `ratelimit.*.read_override` or `ratelimit.<namespace_id>.read_override` */
export const ratelimitGetOverride: API.OperationMethod<
  RatelimitGetOverrideRequest,
  V2RatelimitGetOverrideResponseBody,
  RatelimitGetOverrideError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RatelimitGetOverrideRequest,
  output: V2RatelimitGetOverrideResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type RatelimitLimitError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Apply rate limiting Check and enforce rate limits for any identifier (user ID, IP address, API client, etc.). Use this for rate limiting beyond API keys - limit users by ID, IPs by address, or any custom identifier. Supports namespace organization, variable costs, and custom overrides. **Response Codes**: Rate limit checks return HTTP 200 regardless of whether the limit is exceeded — check the `success` field in the response to determine if the request should be allowed. A 429 may be returned if the workspace exceeds its API rate limit. Other 4xx responses indicate auth, namespace existence/deletion, or validation errors (e.g., 410 Gone for deleted namespaces). 5xx responses indicate server errors. **Required Permissions** Your root key must have one of the following permissions: - `ratelimit.*.limit` (to check limits in any namespace) - `ratelimit.<namespace_id>.limit` (to check limits in a specific namespace) */
export const ratelimitLimit: API.OperationMethod<
  RatelimitLimitRequest,
  V2RatelimitLimitResponseBody,
  RatelimitLimitError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RatelimitLimitRequest,
  output: V2RatelimitLimitResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type RatelimitListOverridesError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** List ratelimit overrides Retrieve a paginated list of all rate limit overrides in a namespace. Use this to audit rate limiting policies, build admin dashboards, or manage override configurations. **Important:** Results are paginated. Use the cursor parameter to retrieve additional pages when more results are available. **Permissions:** Requires `ratelimit.*.read_override` or `ratelimit.<namespace_id>.read_override` */
export const ratelimitListOverrides: API.PaginatedOperationMethod<
  RatelimitListOverridesRequest,
  V2RatelimitListOverridesResponseBody,
  RatelimitListOverridesError,
  UnkeyOpContext,
  RatelimitOverride
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: RatelimitListOverridesRequest,
    output: V2RatelimitListOverridesResponseBody,
    errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
    protocol: UnkeyProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pagination.cursor",
      items: "data",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type RatelimitMultiLimitError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Apply multiple rate limit checks Check and enforce multiple rate limits in a single request for any identifiers (user IDs, IP addresses, API clients, etc.). Use this to efficiently check multiple rate limits at once. Each rate limit check is independent and returns its own result with a top-level `passed` indicator showing if all checks succeeded. **Response Codes**: Rate limit checks return HTTP 200 regardless of whether limits are exceeded — check the `passed` field to see if all limits passed, or the `success` field in each individual result. A 429 may be returned if the workspace exceeds its API rate limit. Other 4xx responses indicate auth, namespace existence/deletion, or validation errors (e.g., 410 Gone for deleted namespaces). 5xx responses indicate server errors. **Required Permissions** Your root key must have one of the following permissions: - `ratelimit.*.limit` (to check limits in any namespace) - `ratelimit.<namespace_id>.limit` (to check limits in all specific namespaces being checked) */
export const ratelimitMultiLimit: API.OperationMethod<
  RatelimitMultiLimitRequest,
  V2RatelimitMultiLimitResponseBody,
  RatelimitMultiLimitError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RatelimitMultiLimitRequest,
  output: V2RatelimitMultiLimitResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));

export type RatelimitSetOverrideError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnkeyOpError;
/** Set ratelimit override Create or update a custom rate limit for specific identifiers, bypassing the namespace default. Use this to create premium tiers with higher limits, apply stricter limits to specific users, or implement emergency throttling. **Important:** Overrides take effect immediately and completely replace the default limit for matching identifiers. Use wildcard patterns (e.g., `premium_*`) to match multiple identifiers. **Permissions:** Requires `ratelimit.*.set_override` or `ratelimit.<namespace_id>.set_override` */
export const ratelimitSetOverride: API.OperationMethod<
  RatelimitSetOverrideRequest,
  V2RatelimitSetOverrideResponseBody,
  RatelimitSetOverrideError,
  UnkeyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RatelimitSetOverrideRequest,
  output: V2RatelimitSetOverrideResponseBody,
  errors: [BadRequest, Forbidden, NotFound, UnknownUnkeyError],
  protocol: UnkeyProtocol,
  retry: Retry.Retry,
}));
