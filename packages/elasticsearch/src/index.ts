/**
 * @distilled.cloud/elasticsearch — Elasticsearch SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy model
 * in `.generated-specs` (written by `scripts/convert.ts` from the OpenAPI
 * spec). Everything else in this folder is hand-written.
 *
 * @example
 * ```ts
 * import * as Elasticsearch from "@distilled.cloud/elasticsearch";
 *
 * const info = yield* Elasticsearch.Services.elasticsearch.info({});
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  ElasticsearchProtocol,
  type ElasticsearchOpError,
  type ElasticsearchOpContext,
} from "./protocol.ts";
export { paginateCursor } from "./pagination.ts";
export * as Retry from "./retry.ts";
export * as Services from "./services/index.ts";
// Operations (and their request/response types) are importable straight off
// the package root. Shared HTTP error classes come from ./errors.ts — the
// OpenAPI document does not declare per-status responses, so the generated
// module does not emit matching matcher classes.
export * from "./services/elasticsearch.ts";
