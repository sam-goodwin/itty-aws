/**
 * @distilled.cloud/azure — Azure SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the per-service
 * Smithy models in `.generated-specs` (written by `scripts/convert.ts` from
 * the azure-rest-api-specs submodule: latest stable api-version per
 * `Microsoft.*` resource provider, resource-manager plane only). Everything
 * else in this folder is hand-written.
 *
 * @example
 * ```ts
 * import * as Azure from "@distilled.cloud/azure";
 *
 * const groups = yield* Azure.Services.resources.ResourceGroupsList({});
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  AzureProtocol,
  type AzureOpError,
  type AzureOpContext,
} from "./protocol.ts";
export * as Retry from "./retry.ts";
export * as Services from "./services/index.ts";
