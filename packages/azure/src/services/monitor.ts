// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/azure-rest-api-specs). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  AzureProtocol,
  type AzureOpError,
  type AzureOpContext,
} from "../protocol.ts";
import { UnknownAzureError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { AzureOpError, AzureOpContext };

export interface DataCollectionRuleConfigurationMetadataFetchRequest {
  /** The ID of the target subscription. The value must be an UUID. */
  subscriptionId: string;
  /** The Azure region for the metadata request. */
  location: string;
  /** DCR Kind for which metadata is being requested (e.g., PlatformTelemetry). */
  dcrKind?: string;
  /** Optional resource type filter (e.g., Microsoft.Compute/virtualMachines). If specified, returns configuration metadata for that resource type. */
  resourceType?: string;
  /** When true, returns detailed stream metadata in the response. Defaults to false. */
  withStreamMetadata?: boolean;
}
export const DataCollectionRuleConfigurationMetadataFetchRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      subscriptionId: S.String.pipe(T.Label()),
      location: S.String.pipe(T.Label()),
      dcrKind: S.optional(S.String),
      resourceType: S.optional(S.String),
      withStreamMetadata: S.optional(S.Boolean),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/subscriptions/{subscriptionId}/providers/Microsoft.Insights/locations/{location}/fetchDataCollectionRuleConfigurationMetadata",
        code: 200,
        apiVersion: "2025-01-01",
      }),
    ),
  ).annotate({
    identifier: "DataCollectionRuleConfigurationMetadataFetchRequest",
  }) as any as S.Schema<DataCollectionRuleConfigurationMetadataFetchRequest>;

/** List of supported destination resource types. */
export type TelemetryTypeMetadataSupportedDestinationsList = Array<string>;
export const TelemetryTypeMetadataSupportedDestinationsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<TelemetryTypeMetadataSupportedDestinationsList>;

/** List of supported source resource types. */
export type TelemetryTypeMetadataSupportedResourceTypesList = Array<string>;
export const TelemetryTypeMetadataSupportedResourceTypesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<TelemetryTypeMetadataSupportedResourceTypesList>;

/** Categorization groups for the logs stream. */
export type LogsSpecificationGroupsList = Array<string>;
export const LogsSpecificationGroupsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<LogsSpecificationGroupsList>;

/** Specification for a logs stream. */
export interface LogsSpecification {
  /** The name of the logs stream. */
  name?: string;
  /** The display name of the logs stream. */
  displayName?: string;
  /** Categorization groups for the logs stream. */
  groups?: LogsSpecificationGroupsList;
}
export const LogsSpecification = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    displayName: S.optional(S.String),
    groups: S.optional(LogsSpecificationGroupsList),
  }),
).annotate({
  identifier: "LogsSpecification",
}) as any as S.Schema<LogsSpecification>;

/** Categorization groups for the metric. */
export type MetricsSpecificationGroupsList = Array<string>;
export const MetricsSpecificationGroupsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<MetricsSpecificationGroupsList>;

/** Specification for a metrics stream. Properties are driven by the service and are manifest driven. */
export interface MetricsSpecification {
  /** The name of the metric. */
  name?: string;
  /** The display name of the metric. */
  displayName?: string;
  /** Description of the metric. */
  description?: string;
  /** The unit of measurement for the metric. This value is provided by the service and is manifest-driven; the set of possible values may evolve over time. Example: "Count". */
  unit?: string;
  /** The aggregation type for the metric. This value is provided by the service and is manifest-driven; the set of possible values may evolve over time. Example: "Total". */
  aggregationType?: string;
  /** Categorization groups for the metric. */
  groups?: MetricsSpecificationGroupsList;
}
export const MetricsSpecification = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    displayName: S.optional(S.String),
    description: S.optional(S.String),
    unit: S.optional(S.String),
    aggregationType: S.optional(S.String),
    groups: S.optional(MetricsSpecificationGroupsList),
  }),
).annotate({
  identifier: "MetricsSpecification",
}) as any as S.Schema<MetricsSpecification>;

/** Detailed metadata for a stream (logs or metrics specification). */
export interface StreamDetailMetadata {
  /** Specification for logs stream. */
  logsSpecification?: LogsSpecification;
  /** Specification for metrics stream. */
  metricsSpecification?: MetricsSpecification;
}
export const StreamDetailMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    logsSpecification: S.optional(LogsSpecification),
    metricsSpecification: S.optional(MetricsSpecification),
  }),
).annotate({
  identifier: "StreamDetailMetadata",
}) as any as S.Schema<StreamDetailMetadata>;

/** Metadata for a specific data stream. */
export interface StreamMetadata {
  /** The unique identifier for the stream. */
  streamId?: string;
  /** Additional metadata about the stream. */
  metadata?: StreamDetailMetadata;
}
export const StreamMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    streamId: S.optional(S.String),
    metadata: S.optional(StreamDetailMetadata),
  }),
).annotate({ identifier: "StreamMetadata" }) as any as S.Schema<StreamMetadata>;

/** Detailed stream information. Only returned when withStreamMetadata is true in the request. */
export type TelemetryTypeMetadataSupportedStreamsList = Array<StreamMetadata>;
export const TelemetryTypeMetadataSupportedStreamsList = /*@__PURE__*/ S.Array(
  StreamMetadata,
) as any as S.Schema<TelemetryTypeMetadataSupportedStreamsList>;

/** Metadata for a specific telemetry type (logs or metrics). */
export interface TelemetryTypeMetadata {
  /** List of supported destination resource types. */
  supportedDestinations?: TelemetryTypeMetadataSupportedDestinationsList;
  /** List of supported source resource types. */
  supportedResourceTypes?: TelemetryTypeMetadataSupportedResourceTypesList;
  /** Detailed stream information. Only returned when withStreamMetadata is true in the request. */
  supportedStreams?: TelemetryTypeMetadataSupportedStreamsList;
}
export const TelemetryTypeMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    supportedDestinations: S.optional(
      TelemetryTypeMetadataSupportedDestinationsList,
    ),
    supportedResourceTypes: S.optional(
      TelemetryTypeMetadataSupportedResourceTypesList,
    ),
    supportedStreams: S.optional(TelemetryTypeMetadataSupportedStreamsList),
  }),
).annotate({
  identifier: "TelemetryTypeMetadata",
}) as any as S.Schema<TelemetryTypeMetadata>;

/** Metadata for platform telemetry (logs and metrics). */
export interface PlatformTelemetryMetadata {
  /** Configuration metadata for platform logs. */
  platformLogs?: TelemetryTypeMetadata;
  /** Configuration metadata for platform metrics. */
  platformMetrics?: TelemetryTypeMetadata;
}
export const PlatformTelemetryMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    platformLogs: S.optional(TelemetryTypeMetadata),
    platformMetrics: S.optional(TelemetryTypeMetadata),
  }),
).annotate({
  identifier: "PlatformTelemetryMetadata",
}) as any as S.Schema<PlatformTelemetryMetadata>;

/** Configuration metadata for data collection. */
export interface ConfigurationMetadata {
  /** Platform telemetry configuration metadata. */
  platformTelemetry?: PlatformTelemetryMetadata;
}
export const ConfigurationMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    platformTelemetry: S.optional(PlatformTelemetryMetadata),
  }),
).annotate({
  identifier: "ConfigurationMetadata",
}) as any as S.Schema<ConfigurationMetadata>;

/** Response containing configuration metadata for data collection rules. */
export interface DataCollectionRuleConfigurationMetadataResponse {
  /** The configuration metadata for platform telemetry. */
  configurationMetadata?: ConfigurationMetadata;
}
export const DataCollectionRuleConfigurationMetadataResponse =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      configurationMetadata: S.optional(ConfigurationMetadata),
    }),
  ).annotate({
    identifier: "DataCollectionRuleConfigurationMetadataResponse",
  }) as any as S.Schema<DataCollectionRuleConfigurationMetadataResponse>;

export interface OperationsListRequest {}
export const OperationsListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({
      method: "GET",
      uri: "/providers/Microsoft.Insights/operations",
      code: 200,
      apiVersion: "2025-01-01",
    }),
  ),
).annotate({
  identifier: "OperationsListRequest",
}) as any as S.Schema<OperationsListRequest>;

/** Localized display information for this particular operation. */
export interface OperationDisplay {
  /** The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute". */
  provider?: string;
  /** The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections". */
  resource?: string;
  /** The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine". */
  operation?: string;
  /** The short, localized friendly description of the operation; suitable for tool tips and detailed views. */
  description?: string;
}
export const OperationDisplay = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    provider: S.optional(S.String),
    resource: S.optional(S.String),
    operation: S.optional(S.String),
    description: S.optional(S.String),
  }),
).annotate({
  identifier: "OperationDisplay",
}) as any as S.Schema<OperationDisplay>;

/** The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system" */
export type OperationOrigin = "user" | "system" | "user,system";
export const OperationOrigin = /*@__PURE__*/ S.String;

/** Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. */
export type OperationActionType = "Internal";
export const OperationActionType = /*@__PURE__*/ S.String;

/** Details of a REST API operation, returned from the Resource Provider Operations API */
export interface Operation {
  /** The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action" */
  name?: string;
  /** Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations. */
  isDataAction?: boolean;
  /** Localized display information for this particular operation. */
  display?: OperationDisplay;
  /** The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system" */
  origin?: OperationOrigin;
  /** Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs. */
  actionType?: OperationActionType;
}
export const Operation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    isDataAction: S.optional(S.Boolean),
    display: S.optional(OperationDisplay),
    origin: S.optional(OperationOrigin),
    actionType: S.optional(OperationActionType),
  }),
).annotate({ identifier: "Operation" }) as any as S.Schema<Operation>;

/** List of operations supported by the resource provider */
export type OperationsListResponseValueList = Array<Operation>;
export const OperationsListResponseValueList = /*@__PURE__*/ S.Array(
  Operation,
) as any as S.Schema<OperationsListResponseValueList>;

export interface OperationsListResponse {
  /** List of operations supported by the resource provider */
  value?: OperationsListResponseValueList;
  /** URL to get the next set of operation list results (if there are any). */
  nextLink?: string;
}
export const OperationsListResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    value: S.optional(OperationsListResponseValueList),
    nextLink: S.optional(S.String),
  }),
).annotate({
  identifier: "OperationsListResponse",
}) as any as S.Schema<OperationsListResponse>;

/** Type of managed service identity. */
export type IdentityInputType = "SystemAssigned" | "UserAssigned" | "None";
export const IdentityInputType = /*@__PURE__*/ S.String;

/** User assigned identity properties. */
export interface UserIdentityPropertiesInput {}
export const UserIdentityPropertiesInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UserIdentityPropertiesInput",
}) as any as S.Schema<UserIdentityPropertiesInput>;

/** The list of user identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}'. */
export type IdentityInputUserAssignedIdentitiesMap = {
  [key: string]: UserIdentityPropertiesInput | undefined;
};
export const IdentityInputUserAssignedIdentitiesMap = /*@__PURE__*/ S.Record(
  S.String,
  UserIdentityPropertiesInput,
) as any as S.Schema<IdentityInputUserAssignedIdentitiesMap>;

/** Identity for the resource. */
export interface IdentityInput {
  /** Type of managed service identity. */
  type: IdentityInputType | (string & {});
  /** The list of user identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}'. */
  userAssignedIdentities?: IdentityInputUserAssignedIdentitiesMap;
}
export const IdentityInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: IdentityInputType,
    userAssignedIdentities: S.optional(IdentityInputUserAssignedIdentitiesMap),
  }),
).annotate({ identifier: "IdentityInput" }) as any as S.Schema<IdentityInput>;

/** Resource tags. */
export type ScheduledQueryRulesCreateOrUpdateRequestTagsMap = {
  [key: string]: string | undefined;
};
export const ScheduledQueryRulesCreateOrUpdateRequestTagsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.String,
  ) as any as S.Schema<ScheduledQueryRulesCreateOrUpdateRequestTagsMap>;

/** Indicates the type of scheduled query rule. The default is LogAlert. */
export type ScheduledQueryRulesCreateOrUpdateRequestKind =
  | "LogAlert"
  | "SimpleLogAlert"
  | "LogToMetric";
export const ScheduledQueryRulesCreateOrUpdateRequestKind =
  /*@__PURE__*/ S.String;

/** Severity of the alert. Should be an integer between [0-4]. Value of 0 is severest. Relevant and required only for rules of the kind LogAlert. */
export type ScheduledQueryRulePropertiesInputSeverity = 0 | 1 | 2 | 3 | 4;
export const ScheduledQueryRulePropertiesInputSeverity = /*@__PURE__*/ S.Number;

/** The list of resource id's that this scheduled query rule is scoped to. */
export type ScheduledQueryRulePropertiesInputScopesList = Array<string>;
export const ScheduledQueryRulePropertiesInputScopesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ScheduledQueryRulePropertiesInputScopesList>;

/** List of resource type of the target resource(s) on which the alert is created/updated. For example if the scope is a resource group and targetResourceTypes is Microsoft.Compute/virtualMachines, then a different alert will be fired for each virtual machine in the resource group which meet the alert criteria. Relevant only for rules of the kind LogAlert */
export type ScheduledQueryRulePropertiesInputTargetResourceTypesList =
  Array<string>;
export const ScheduledQueryRulePropertiesInputTargetResourceTypesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ScheduledQueryRulePropertiesInputTargetResourceTypesList>;

/** Specifies the type of threshold criteria */
export type ConditionCriterionType =
  | "StaticThresholdCriterion"
  | "DynamicThresholdCriterion";
export const ConditionCriterionType = /*@__PURE__*/ S.String;

/** Aggregation type. Relevant and required only for rules of the kind LogAlert. */
export type ConditionTimeAggregation =
  | "Count"
  | "Average"
  | "Minimum"
  | "Maximum"
  | "Total";
export const ConditionTimeAggregation = /*@__PURE__*/ S.String;

/** Operator for dimension values */
export type DimensionOperator = "Include" | "Exclude";
export const DimensionOperator = /*@__PURE__*/ S.String;

/** List of dimension values */
export type DimensionValuesList = Array<string>;
export const DimensionValuesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<DimensionValuesList>;

/** Dimension splitting and filtering definition */
export interface Dimension {
  /** Name of the dimension */
  name: string;
  /** Operator for dimension values */
  operator: DimensionOperator | (string & {});
  /** List of dimension values */
  values: DimensionValuesList;
}
export const Dimension = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    operator: DimensionOperator,
    values: DimensionValuesList,
  }),
).annotate({ identifier: "Dimension" }) as any as S.Schema<Dimension>;

/** List of Dimensions conditions. Relevant only for rules of the kind LogAlert and LogToMetric. */
export type ConditionDimensionsList = Array<Dimension>;
export const ConditionDimensionsList = /*@__PURE__*/ S.Array(
  Dimension,
) as any as S.Schema<ConditionDimensionsList>;

/** The criteria operator. Relevant and required only for rules of the kind LogAlert. */
export type ConditionOperator =
  | "Equals"
  | "GreaterThan"
  | "GreaterThanOrEqual"
  | "LessThan"
  | "LessThanOrEqual"
  | "GreaterOrLessThan";
export const ConditionOperator = /*@__PURE__*/ S.String;

/** The minimum number of violations required within the selected lookback time window required to raise an alert. Relevant only for rules of the kind LogAlert. */
export interface ConditionFailingPeriods {
  /** The number of aggregated lookback points. The lookback time window is calculated based on the aggregation granularity (windowSize) and the selected number of aggregated points. Default value is 1 */
  numberOfEvaluationPeriods?: number;
  /** The number of violations to trigger an alert. Should be smaller or equal to numberOfEvaluationPeriods. Default value is 1 */
  minFailingPeriodsToAlert?: number;
}
export const ConditionFailingPeriods = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    numberOfEvaluationPeriods: S.optional(S.Number),
    minFailingPeriodsToAlert: S.optional(S.Number),
  }),
).annotate({
  identifier: "ConditionFailingPeriods",
}) as any as S.Schema<ConditionFailingPeriods>;

/** A condition of the scheduled query rule. */
export interface Condition {
  /** Specifies the type of threshold criteria */
  criterionType?: ConditionCriterionType | (string & {});
  /** Log query alert */
  query?: string;
  /** Aggregation type. Relevant and required only for rules of the kind LogAlert. */
  timeAggregation?: ConditionTimeAggregation | (string & {});
  /** The column containing the metric measure number. Relevant only for rules of the kind LogAlert. */
  metricMeasureColumn?: string;
  /** The column containing the resource id. The content of the column must be a uri formatted as resource id. Relevant only for rules of the kind LogAlert. */
  resourceIdColumn?: string;
  /** List of Dimensions conditions. Relevant only for rules of the kind LogAlert and LogToMetric. */
  dimensions?: ConditionDimensionsList;
  /** The criteria operator. Relevant and required only for rules of the kind LogAlert. */
  operator?: ConditionOperator | (string & {});
  /** the criteria threshold value that activates the alert. Relevant and required only for static threshold rules of the kind LogAlert. */
  threshold?: number;
  /** The extent of deviation required to trigger an alert. Allowed values are 'Low', 'Medium' and 'High'. This will affect how tight the threshold is to the metric series pattern. Relevant only for dynamic threshold rules of the kind LogAlert. */
  alertSensitivity?: string;
  /** Use this option to set the date from which to start learning the metric historical data and calculate the dynamic thresholds (in ISO8601 format). Relevant only for dynamic threshold rules of the kind LogAlert. */
  ignoreDataBefore?: string;
  /** The minimum number of violations required within the selected lookback time window required to raise an alert. Relevant only for rules of the kind LogAlert. */
  failingPeriods?: ConditionFailingPeriods;
  /** The name of the metric to be sent. Relevant and required only for rules of the kind LogToMetric. */
  metricName?: string;
  /** The minimum results count that should be found for triggering an alert. Relevant only for rules of the kind SimpleLogAlert. */
  minRecurrenceCount?: number;
}
export const Condition = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    criterionType: S.optional(ConditionCriterionType),
    query: S.optional(S.String),
    timeAggregation: S.optional(ConditionTimeAggregation),
    metricMeasureColumn: S.optional(S.String),
    resourceIdColumn: S.optional(S.String),
    dimensions: S.optional(ConditionDimensionsList),
    operator: S.optional(ConditionOperator),
    threshold: S.optional(S.Number),
    alertSensitivity: S.optional(S.String),
    ignoreDataBefore: S.optional(S.String),
    failingPeriods: S.optional(ConditionFailingPeriods),
    metricName: S.optional(S.String),
    minRecurrenceCount: S.optional(S.Number),
  }),
).annotate({ identifier: "Condition" }) as any as S.Schema<Condition>;

/** A list of conditions to evaluate against the specified scopes */
export type ScheduledQueryRuleCriteriaAllOfList = Array<Condition>;
export const ScheduledQueryRuleCriteriaAllOfList = /*@__PURE__*/ S.Array(
  Condition,
) as any as S.Schema<ScheduledQueryRuleCriteriaAllOfList>;

/** The rule criteria that defines the conditions of the scheduled query rule. */
export interface ScheduledQueryRuleCriteria {
  /** A list of conditions to evaluate against the specified scopes */
  allOf?: ScheduledQueryRuleCriteriaAllOfList;
}
export const ScheduledQueryRuleCriteria = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    allOf: S.optional(ScheduledQueryRuleCriteriaAllOfList),
  }),
).annotate({
  identifier: "ScheduledQueryRuleCriteria",
}) as any as S.Schema<ScheduledQueryRuleCriteria>;

/** Action Group resource Ids to invoke when the alert fires. */
export type ActionsActionGroupsList = Array<string>;
export const ActionsActionGroupsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ActionsActionGroupsList>;

/** The properties of an alert payload. */
export type ActionsCustomPropertiesMap = { [key: string]: string | undefined };
export const ActionsCustomPropertiesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ActionsCustomPropertiesMap>;

/** The properties of an action properties. */
export type ActionsActionPropertiesMap = { [key: string]: string | undefined };
export const ActionsActionPropertiesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ActionsActionPropertiesMap>;

/** Actions to invoke when the alert fires. */
export interface Actions {
  /** Action Group resource Ids to invoke when the alert fires. */
  actionGroups?: ActionsActionGroupsList;
  /** The properties of an alert payload. */
  customProperties?: ActionsCustomPropertiesMap;
  /** The properties of an action properties. */
  actionProperties?: ActionsActionPropertiesMap;
}
export const Actions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    actionGroups: S.optional(ActionsActionGroupsList),
    customProperties: S.optional(ActionsCustomPropertiesMap),
    actionProperties: S.optional(ActionsActionPropertiesMap),
  }),
).annotate({ identifier: "Actions" }) as any as S.Schema<Actions>;

/** TBD. Relevant only for rules of the kind LogAlert. */
export interface RuleResolveConfiguration {
  /** The flag that indicates whether or not to auto resolve a fired alert. */
  autoResolved?: boolean;
  /** The duration a rule must evaluate as healthy before the fired alert is automatically resolved represented in ISO 8601 duration format. */
  timeToResolve?: string;
}
export const RuleResolveConfiguration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    autoResolved: S.optional(S.Boolean),
    timeToResolve: S.optional(S.String),
  }),
).annotate({
  identifier: "RuleResolveConfiguration",
}) as any as S.Schema<RuleResolveConfiguration>;

/** scheduled query rule Definition */
export interface ScheduledQueryRulePropertiesInput {
  /** The description of the scheduled query rule. */
  description?: string;
  /** The display name of the alert rule */
  displayName?: string;
  /** Severity of the alert. Should be an integer between [0-4]. Value of 0 is severest. Relevant and required only for rules of the kind LogAlert. */
  severity?: ScheduledQueryRulePropertiesInputSeverity | (number & {});
  /** The flag which indicates whether this scheduled query rule is enabled. Value should be true or false */
  enabled?: boolean;
  /** The list of resource id's that this scheduled query rule is scoped to. */
  scopes?: ScheduledQueryRulePropertiesInputScopesList;
  /** How often the scheduled query rule is evaluated represented in ISO 8601 duration format. Relevant and required only for rules of the kind LogAlert. */
  evaluationFrequency?: string;
  /** The period of time (in ISO 8601 duration format) on which the Alert query will be executed (bin size). Relevant and required only for rules of the kind LogAlert. */
  windowSize?: string;
  /** If specified then overrides the query time range (default is WindowSize*NumberOfEvaluationPeriods). Relevant only for rules of the kind LogAlert. */
  overrideQueryTimeRange?: string;
  /** List of resource type of the target resource(s) on which the alert is created/updated. For example if the scope is a resource group and targetResourceTypes is Microsoft.Compute/virtualMachines, then a different alert will be fired for each virtual machine in the resource group which meet the alert criteria. Relevant only for rules of the kind LogAlert */
  targetResourceTypes?: ScheduledQueryRulePropertiesInputTargetResourceTypesList;
  /** The rule criteria that defines the conditions of the scheduled query rule. */
  criteria?: ScheduledQueryRuleCriteria;
  /** Mute actions for the chosen period of time (in ISO 8601 duration format) after the alert is fired. Relevant only for rules of the kind LogAlert. */
  muteActionsDuration?: string;
  /** Actions to invoke when the alert fires. */
  actions?: Actions;
  /** The flag which indicates whether this scheduled query rule should be stored in the customer's storage. The default is false. Relevant only for rules of the kind LogAlert. */
  checkWorkspaceAlertsStorageConfigured?: boolean;
  /** The flag which indicates whether the provided query should be validated or not. The default is false. Relevant only for rules of the kind LogAlert. */
  skipQueryValidation?: boolean;
  /** The flag that indicates whether the alert should be automatically resolved or not. The default is true. Relevant only for rules of kinds LogAlert and SimpleLogAlert. */
  autoMitigate?: boolean;
  /** Defines the configuration for resolving fired alerts. Relevant only for rules of kinds LogAlert and SimpleLogAlert. */
  resolveConfiguration?: RuleResolveConfiguration;
}
export const ScheduledQueryRulePropertiesInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.optional(S.String),
    displayName: S.optional(S.String),
    severity: S.optional(ScheduledQueryRulePropertiesInputSeverity),
    enabled: S.optional(S.Boolean),
    scopes: S.optional(ScheduledQueryRulePropertiesInputScopesList),
    evaluationFrequency: S.optional(S.String),
    windowSize: S.optional(S.String),
    overrideQueryTimeRange: S.optional(S.String),
    targetResourceTypes: S.optional(
      ScheduledQueryRulePropertiesInputTargetResourceTypesList,
    ),
    criteria: S.optional(ScheduledQueryRuleCriteria),
    muteActionsDuration: S.optional(S.String),
    actions: S.optional(Actions),
    checkWorkspaceAlertsStorageConfigured: S.optional(S.Boolean),
    skipQueryValidation: S.optional(S.Boolean),
    autoMitigate: S.optional(S.Boolean),
    resolveConfiguration: S.optional(RuleResolveConfiguration),
  }),
).annotate({
  identifier: "ScheduledQueryRulePropertiesInput",
}) as any as S.Schema<ScheduledQueryRulePropertiesInput>;

export interface ScheduledQueryRulesCreateOrUpdateRequest {
  /** The ID of the target subscription. */
  subscriptionId: string;
  /** The name of the resource group. The name is case insensitive. */
  resourceGroupName: string;
  /** The name of the rule. */
  ruleName: string;
  /** The identity of the resource. */
  identity?: IdentityInput;
  /** Resource tags. */
  tags?: ScheduledQueryRulesCreateOrUpdateRequestTagsMap;
  /** The geo-location where the resource lives */
  location: string;
  /** Indicates the type of scheduled query rule. The default is LogAlert. */
  kind?: ScheduledQueryRulesCreateOrUpdateRequestKind | (string & {});
  /** The rule properties of the resource. */
  properties: ScheduledQueryRulePropertiesInput;
}
export const ScheduledQueryRulesCreateOrUpdateRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      subscriptionId: S.String.pipe(T.Label()),
      resourceGroupName: S.String.pipe(T.Label()),
      ruleName: S.String.pipe(T.Label()),
      identity: S.optional(IdentityInput),
      tags: S.optional(ScheduledQueryRulesCreateOrUpdateRequestTagsMap),
      location: S.String,
      kind: S.optional(ScheduledQueryRulesCreateOrUpdateRequestKind),
      properties: ScheduledQueryRulePropertiesInput,
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Insights/scheduledQueryRules/{ruleName}",
        code: 200,
        apiVersion: "2026-03-01",
      }),
    ),
).annotate({
  identifier: "ScheduledQueryRulesCreateOrUpdateRequest",
}) as any as S.Schema<ScheduledQueryRulesCreateOrUpdateRequest>;

/** Type of managed service identity. */
export type IdentityType = "SystemAssigned" | "UserAssigned" | "None";
export const IdentityType = /*@__PURE__*/ S.String;

/** User assigned identity properties. */
export interface UserIdentityProperties {
  /** The principal id of user assigned identity. */
  principalId?: string;
  /** The client id of user assigned identity. */
  clientId?: string;
}
export const UserIdentityProperties = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    principalId: S.optional(S.String),
    clientId: S.optional(S.String),
  }),
).annotate({
  identifier: "UserIdentityProperties",
}) as any as S.Schema<UserIdentityProperties>;

/** The list of user identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}'. */
export type IdentityUserAssignedIdentitiesMap = {
  [key: string]: UserIdentityProperties | undefined;
};
export const IdentityUserAssignedIdentitiesMap = /*@__PURE__*/ S.Record(
  S.String,
  UserIdentityProperties,
) as any as S.Schema<IdentityUserAssignedIdentitiesMap>;

/** Identity for the resource. */
export interface Identity {
  /** The principal ID of resource identity. */
  principalId?: string;
  /** The tenant ID of resource. */
  tenantId?: string;
  /** Type of managed service identity. */
  type: IdentityType;
  /** The list of user identities associated with the resource. The user identity dictionary key references will be ARM resource ids in the form: '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}'. */
  userAssignedIdentities?: IdentityUserAssignedIdentitiesMap;
}
export const Identity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    principalId: S.optional(S.String),
    tenantId: S.optional(S.String),
    type: IdentityType,
    userAssignedIdentities: S.optional(IdentityUserAssignedIdentitiesMap),
  }),
).annotate({ identifier: "Identity" }) as any as S.Schema<Identity>;

/** Resource tags. */
export type ScheduledQueryRuleResourceTagsMap = {
  [key: string]: string | undefined;
};
export const ScheduledQueryRuleResourceTagsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ScheduledQueryRuleResourceTagsMap>;

/** Indicates the type of scheduled query rule. The default is LogAlert. */
export type ScheduledQueryRuleResourceKind =
  | "LogAlert"
  | "SimpleLogAlert"
  | "LogToMetric";
export const ScheduledQueryRuleResourceKind = /*@__PURE__*/ S.String;

/** The type of identity that created the resource. */
export type ScheduledQueryRuleResourceSystemDataCreatedByType =
  | "User"
  | "Application"
  | "ManagedIdentity"
  | "Key";
export const ScheduledQueryRuleResourceSystemDataCreatedByType =
  /*@__PURE__*/ S.String;

/** The type of identity that last modified the resource. */
export type ScheduledQueryRuleResourceSystemDataLastModifiedByType =
  | "User"
  | "Application"
  | "ManagedIdentity"
  | "Key";
export const ScheduledQueryRuleResourceSystemDataLastModifiedByType =
  /*@__PURE__*/ S.String;

/** Metadata pertaining to creation and last modification of the resource. */
export interface ScheduledQueryRuleResourceSystemData {
  /** The identity that created the resource. */
  createdBy?: string;
  /** The type of identity that created the resource. */
  createdByType?: ScheduledQueryRuleResourceSystemDataCreatedByType;
  /** The timestamp of resource creation (UTC). */
  createdAt?: string;
  /** The identity that last modified the resource. */
  lastModifiedBy?: string;
  /** The type of identity that last modified the resource. */
  lastModifiedByType?: ScheduledQueryRuleResourceSystemDataLastModifiedByType;
  /** The timestamp of resource last modification (UTC) */
  lastModifiedAt?: string;
}
export const ScheduledQueryRuleResourceSystemData = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      createdBy: S.optional(S.String),
      createdByType: S.optional(
        ScheduledQueryRuleResourceSystemDataCreatedByType,
      ),
      createdAt: S.optional(S.String),
      lastModifiedBy: S.optional(S.String),
      lastModifiedByType: S.optional(
        ScheduledQueryRuleResourceSystemDataLastModifiedByType,
      ),
      lastModifiedAt: S.optional(S.String),
    }),
).annotate({
  identifier: "ScheduledQueryRuleResourceSystemData",
}) as any as S.Schema<ScheduledQueryRuleResourceSystemData>;

/** Severity of the alert. Should be an integer between [0-4]. Value of 0 is severest. Relevant and required only for rules of the kind LogAlert. */
export type ScheduledQueryRulePropertiesSeverity = 0 | 1 | 2 | 3 | 4;
export const ScheduledQueryRulePropertiesSeverity = /*@__PURE__*/ S.Number;

/** The list of resource id's that this scheduled query rule is scoped to. */
export type ScheduledQueryRulePropertiesScopesList = Array<string>;
export const ScheduledQueryRulePropertiesScopesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ScheduledQueryRulePropertiesScopesList>;

/** List of resource type of the target resource(s) on which the alert is created/updated. For example if the scope is a resource group and targetResourceTypes is Microsoft.Compute/virtualMachines, then a different alert will be fired for each virtual machine in the resource group which meet the alert criteria. Relevant only for rules of the kind LogAlert */
export type ScheduledQueryRulePropertiesTargetResourceTypesList = Array<string>;
export const ScheduledQueryRulePropertiesTargetResourceTypesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ScheduledQueryRulePropertiesTargetResourceTypesList>;

/** scheduled query rule Definition */
export interface ScheduledQueryRuleProperties {
  /** The api-version used when creating this alert rule */
  createdWithApiVersion?: string;
  /** True if alert rule is legacy Log Analytic rule */
  isLegacyLogAnalyticsRule?: boolean;
  /** The description of the scheduled query rule. */
  description?: string;
  /** The display name of the alert rule */
  displayName?: string;
  /** Severity of the alert. Should be an integer between [0-4]. Value of 0 is severest. Relevant and required only for rules of the kind LogAlert. */
  severity?: ScheduledQueryRulePropertiesSeverity;
  /** The flag which indicates whether this scheduled query rule is enabled. Value should be true or false */
  enabled?: boolean;
  /** The list of resource id's that this scheduled query rule is scoped to. */
  scopes?: ScheduledQueryRulePropertiesScopesList;
  /** How often the scheduled query rule is evaluated represented in ISO 8601 duration format. Relevant and required only for rules of the kind LogAlert. */
  evaluationFrequency?: string;
  /** The period of time (in ISO 8601 duration format) on which the Alert query will be executed (bin size). Relevant and required only for rules of the kind LogAlert. */
  windowSize?: string;
  /** If specified then overrides the query time range (default is WindowSize*NumberOfEvaluationPeriods). Relevant only for rules of the kind LogAlert. */
  overrideQueryTimeRange?: string;
  /** List of resource type of the target resource(s) on which the alert is created/updated. For example if the scope is a resource group and targetResourceTypes is Microsoft.Compute/virtualMachines, then a different alert will be fired for each virtual machine in the resource group which meet the alert criteria. Relevant only for rules of the kind LogAlert */
  targetResourceTypes?: ScheduledQueryRulePropertiesTargetResourceTypesList;
  /** The rule criteria that defines the conditions of the scheduled query rule. */
  criteria?: ScheduledQueryRuleCriteria;
  /** Mute actions for the chosen period of time (in ISO 8601 duration format) after the alert is fired. Relevant only for rules of the kind LogAlert. */
  muteActionsDuration?: string;
  /** Actions to invoke when the alert fires. */
  actions?: Actions;
  /** The flag which indicates whether this scheduled query rule has been configured to be stored in the customer's storage. The default is false. */
  isWorkspaceAlertsStorageConfigured?: boolean;
  /** The flag which indicates whether this scheduled query rule should be stored in the customer's storage. The default is false. Relevant only for rules of the kind LogAlert. */
  checkWorkspaceAlertsStorageConfigured?: boolean;
  /** The flag which indicates whether the provided query should be validated or not. The default is false. Relevant only for rules of the kind LogAlert. */
  skipQueryValidation?: boolean;
  /** The flag that indicates whether the alert should be automatically resolved or not. The default is true. Relevant only for rules of kinds LogAlert and SimpleLogAlert. */
  autoMitigate?: boolean;
  /** Defines the configuration for resolving fired alerts. Relevant only for rules of kinds LogAlert and SimpleLogAlert. */
  resolveConfiguration?: RuleResolveConfiguration;
}
export const ScheduledQueryRuleProperties = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    createdWithApiVersion: S.optional(S.String),
    isLegacyLogAnalyticsRule: S.optional(S.Boolean),
    description: S.optional(S.String),
    displayName: S.optional(S.String),
    severity: S.optional(ScheduledQueryRulePropertiesSeverity),
    enabled: S.optional(S.Boolean),
    scopes: S.optional(ScheduledQueryRulePropertiesScopesList),
    evaluationFrequency: S.optional(S.String),
    windowSize: S.optional(S.String),
    overrideQueryTimeRange: S.optional(S.String),
    targetResourceTypes: S.optional(
      ScheduledQueryRulePropertiesTargetResourceTypesList,
    ),
    criteria: S.optional(ScheduledQueryRuleCriteria),
    muteActionsDuration: S.optional(S.String),
    actions: S.optional(Actions),
    isWorkspaceAlertsStorageConfigured: S.optional(S.Boolean),
    checkWorkspaceAlertsStorageConfigured: S.optional(S.Boolean),
    skipQueryValidation: S.optional(S.Boolean),
    autoMitigate: S.optional(S.Boolean),
    resolveConfiguration: S.optional(RuleResolveConfiguration),
  }),
).annotate({
  identifier: "ScheduledQueryRuleProperties",
}) as any as S.Schema<ScheduledQueryRuleProperties>;

/** The scheduled query rule resource. */
export interface ScheduledQueryRuleResource {
  /** Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName} */
  id?: string;
  /** The name of the resource */
  name?: string;
  /** The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts" */
  type?: string;
  /** The identity of the resource. */
  identity?: Identity;
  /** Resource tags. */
  tags?: ScheduledQueryRuleResourceTagsMap;
  /** The geo-location where the resource lives */
  location: string;
  /** Indicates the type of scheduled query rule. The default is LogAlert. */
  kind?: ScheduledQueryRuleResourceKind;
  /** The etag field is *not* required. If it is provided in the response body, it must also be provided as a header per the normal etag convention. Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header fields. */
  etag?: string;
  /** Metadata pertaining to creation and last modification of the resource. */
  systemData?: ScheduledQueryRuleResourceSystemData;
  /** The rule properties of the resource. */
  properties: ScheduledQueryRuleProperties;
}
export const ScheduledQueryRuleResource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    name: S.optional(S.String),
    type: S.optional(S.String),
    identity: S.optional(Identity),
    tags: S.optional(ScheduledQueryRuleResourceTagsMap),
    location: S.String,
    kind: S.optional(ScheduledQueryRuleResourceKind),
    etag: S.optional(S.String),
    systemData: S.optional(ScheduledQueryRuleResourceSystemData),
    properties: ScheduledQueryRuleProperties,
  }),
).annotate({
  identifier: "ScheduledQueryRuleResource",
}) as any as S.Schema<ScheduledQueryRuleResource>;

export interface ScheduledQueryRulesDeleteRequest {
  /** The ID of the target subscription. */
  subscriptionId: string;
  /** The name of the resource group. The name is case insensitive. */
  resourceGroupName: string;
  /** The name of the rule. */
  ruleName: string;
}
export const ScheduledQueryRulesDeleteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    subscriptionId: S.String.pipe(T.Label()),
    resourceGroupName: S.String.pipe(T.Label()),
    ruleName: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Insights/scheduledQueryRules/{ruleName}",
      code: 200,
      apiVersion: "2026-03-01",
    }),
  ),
).annotate({
  identifier: "ScheduledQueryRulesDeleteRequest",
}) as any as S.Schema<ScheduledQueryRulesDeleteRequest>;

export interface ScheduledQueryRulesDeleteResponse {}
export const ScheduledQueryRulesDeleteResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ScheduledQueryRulesDeleteResponse",
}) as any as S.Schema<ScheduledQueryRulesDeleteResponse>;

export interface ScheduledQueryRulesGetRequest {
  /** The ID of the target subscription. */
  subscriptionId: string;
  /** The name of the resource group. The name is case insensitive. */
  resourceGroupName: string;
  /** The name of the rule. */
  ruleName: string;
}
export const ScheduledQueryRulesGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    subscriptionId: S.String.pipe(T.Label()),
    resourceGroupName: S.String.pipe(T.Label()),
    ruleName: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Insights/scheduledQueryRules/{ruleName}",
      code: 200,
      apiVersion: "2026-03-01",
    }),
  ),
).annotate({
  identifier: "ScheduledQueryRulesGetRequest",
}) as any as S.Schema<ScheduledQueryRulesGetRequest>;

export interface ScheduledQueryRulesListByResourceGroupRequest {
  /** The ID of the target subscription. */
  subscriptionId: string;
  /** The name of the resource group. The name is case insensitive. */
  resourceGroupName: string;
}
export const ScheduledQueryRulesListByResourceGroupRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      subscriptionId: S.String.pipe(T.Label()),
      resourceGroupName: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Insights/scheduledQueryRules",
        code: 200,
        apiVersion: "2026-03-01",
      }),
    ),
  ).annotate({
    identifier: "ScheduledQueryRulesListByResourceGroupRequest",
  }) as any as S.Schema<ScheduledQueryRulesListByResourceGroupRequest>;

/** The values for the scheduled query rule resources. */
export type ScheduledQueryRuleResourceCollectionValueList =
  Array<ScheduledQueryRuleResource>;
export const ScheduledQueryRuleResourceCollectionValueList =
  /*@__PURE__*/ S.Array(
    ScheduledQueryRuleResource,
  ) as any as S.Schema<ScheduledQueryRuleResourceCollectionValueList>;

/** Represents a collection of scheduled query rule resources. */
export interface ScheduledQueryRuleResourceCollection {
  /** The values for the scheduled query rule resources. */
  value?: ScheduledQueryRuleResourceCollectionValueList;
  /** Provides the link to retrieve the next set of elements. */
  nextLink?: string;
}
export const ScheduledQueryRuleResourceCollection = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      value: S.optional(ScheduledQueryRuleResourceCollectionValueList),
      nextLink: S.optional(S.String),
    }),
).annotate({
  identifier: "ScheduledQueryRuleResourceCollection",
}) as any as S.Schema<ScheduledQueryRuleResourceCollection>;

export interface ScheduledQueryRulesListBySubscriptionRequest {
  /** The ID of the target subscription. */
  subscriptionId: string;
}
export const ScheduledQueryRulesListBySubscriptionRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      subscriptionId: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/subscriptions/{subscriptionId}/providers/Microsoft.Insights/scheduledQueryRules",
        code: 200,
        apiVersion: "2026-03-01",
      }),
    ),
  ).annotate({
    identifier: "ScheduledQueryRulesListBySubscriptionRequest",
  }) as any as S.Schema<ScheduledQueryRulesListBySubscriptionRequest>;

/** Resource tags */
export type ScheduledQueryRulesUpdateRequestTagsMap = {
  [key: string]: string | undefined;
};
export const ScheduledQueryRulesUpdateRequestTagsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ScheduledQueryRulesUpdateRequestTagsMap>;

export interface ScheduledQueryRulesUpdateRequest {
  /** The ID of the target subscription. */
  subscriptionId: string;
  /** The name of the resource group. The name is case insensitive. */
  resourceGroupName: string;
  /** The name of the rule. */
  ruleName: string;
  /** The identity of the resource. */
  identity?: IdentityInput;
  /** Resource tags */
  tags?: ScheduledQueryRulesUpdateRequestTagsMap;
  /** The scheduled query rule properties of the resource. */
  properties?: ScheduledQueryRulePropertiesInput;
}
export const ScheduledQueryRulesUpdateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    subscriptionId: S.String.pipe(T.Label()),
    resourceGroupName: S.String.pipe(T.Label()),
    ruleName: S.String.pipe(T.Label()),
    identity: S.optional(IdentityInput),
    tags: S.optional(ScheduledQueryRulesUpdateRequestTagsMap),
    properties: S.optional(ScheduledQueryRulePropertiesInput),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Insights/scheduledQueryRules/{ruleName}",
      code: 200,
      apiVersion: "2026-03-01",
    }),
  ),
).annotate({
  identifier: "ScheduledQueryRulesUpdateRequest",
}) as any as S.Schema<ScheduledQueryRulesUpdateRequest>;

export type DataCollectionRuleConfigurationMetadataFetchError = AzureOpError;
/** Fetches configuration metadata for data collection rules. When withStreamMetadata is true, returns detailed stream information. */
export const DataCollectionRuleConfigurationMetadataFetch: API.OperationMethod<
  DataCollectionRuleConfigurationMetadataFetchRequest,
  DataCollectionRuleConfigurationMetadataResponse,
  DataCollectionRuleConfigurationMetadataFetchError,
  AzureOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DataCollectionRuleConfigurationMetadataFetchRequest,
  output: DataCollectionRuleConfigurationMetadataResponse,
  errors: [UnknownAzureError],
  protocol: AzureProtocol,
  retry: Retry.Retry,
}));

export type OperationsListError = AzureOpError;
/** List the operations for the provider */
export const OperationsList: API.OperationMethod<
  OperationsListRequest,
  OperationsListResponse,
  OperationsListError,
  AzureOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: OperationsListRequest,
  output: OperationsListResponse,
  errors: [UnknownAzureError],
  protocol: AzureProtocol,
  retry: Retry.Retry,
}));

export type ScheduledQueryRulesCreateOrUpdateError = AzureOpError;
/** Creates or updates a scheduled query rule. */
export const ScheduledQueryRulesCreateOrUpdate: API.OperationMethod<
  ScheduledQueryRulesCreateOrUpdateRequest,
  ScheduledQueryRuleResource,
  ScheduledQueryRulesCreateOrUpdateError,
  AzureOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ScheduledQueryRulesCreateOrUpdateRequest,
  output: ScheduledQueryRuleResource,
  errors: [UnknownAzureError],
  protocol: AzureProtocol,
  retry: Retry.Retry,
}));

export type ScheduledQueryRulesDeleteError = AzureOpError;
/** Deletes a scheduled query rule. */
export const ScheduledQueryRulesDelete: API.OperationMethod<
  ScheduledQueryRulesDeleteRequest,
  ScheduledQueryRulesDeleteResponse,
  ScheduledQueryRulesDeleteError,
  AzureOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ScheduledQueryRulesDeleteRequest,
  output: ScheduledQueryRulesDeleteResponse,
  errors: [UnknownAzureError],
  protocol: AzureProtocol,
  retry: Retry.Retry,
}));

export type ScheduledQueryRulesGetError = AzureOpError;
/** Retrieve an scheduled query rule definition. */
export const ScheduledQueryRulesGet: API.OperationMethod<
  ScheduledQueryRulesGetRequest,
  ScheduledQueryRuleResource,
  ScheduledQueryRulesGetError,
  AzureOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ScheduledQueryRulesGetRequest,
  output: ScheduledQueryRuleResource,
  errors: [UnknownAzureError],
  protocol: AzureProtocol,
  retry: Retry.Retry,
}));

export type ScheduledQueryRulesListByResourceGroupError = AzureOpError;
/** Retrieve scheduled query rule definitions in a resource group. */
export const ScheduledQueryRulesListByResourceGroup: API.OperationMethod<
  ScheduledQueryRulesListByResourceGroupRequest,
  ScheduledQueryRuleResourceCollection,
  ScheduledQueryRulesListByResourceGroupError,
  AzureOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ScheduledQueryRulesListByResourceGroupRequest,
  output: ScheduledQueryRuleResourceCollection,
  errors: [UnknownAzureError],
  protocol: AzureProtocol,
  retry: Retry.Retry,
}));

export type ScheduledQueryRulesListBySubscriptionError = AzureOpError;
/** Retrieve a scheduled query rule definitions in a subscription. */
export const ScheduledQueryRulesListBySubscription: API.OperationMethod<
  ScheduledQueryRulesListBySubscriptionRequest,
  ScheduledQueryRuleResourceCollection,
  ScheduledQueryRulesListBySubscriptionError,
  AzureOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ScheduledQueryRulesListBySubscriptionRequest,
  output: ScheduledQueryRuleResourceCollection,
  errors: [UnknownAzureError],
  protocol: AzureProtocol,
  retry: Retry.Retry,
}));

export type ScheduledQueryRulesUpdateError = AzureOpError;
/** Update a scheduled query rule. */
export const ScheduledQueryRulesUpdate: API.OperationMethod<
  ScheduledQueryRulesUpdateRequest,
  ScheduledQueryRuleResource,
  ScheduledQueryRulesUpdateError,
  AzureOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ScheduledQueryRulesUpdateRequest,
  output: ScheduledQueryRuleResource,
  errors: [UnknownAzureError],
  protocol: AzureProtocol,
  retry: Retry.Retry,
}));
