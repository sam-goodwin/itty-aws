// AUTO-GENERATED by scripts/generate.ts from .generated-specs (hand-authored ZeroSSL OpenAPI → smithy). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  ZeroSslProtocol,
  type ZeroSslOpError,
  type ZeroSslOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { ZeroSslOpError, ZeroSslOpContext };

/** The access key is missing or wrong (`invalid_access_key`, code 101). */
export class InvalidAccessKey
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<InvalidAccessKey>()("InvalidAccessKey", {
      code: S.Number,
      message: S.String,
    }),
    [{ message: "invalid_access_key" }, { code: 101 }],
  ) {}

export class RateLimitExceeded
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<RateLimitExceeded>()("RateLimitExceeded", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withRetryableError, C.withThrottlingError),
    [{ message: { includes: "rate_limit" } }],
  ) {}

export interface GenerateEabCredentialsRequest {}
export const GenerateEabCredentialsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "POST", uri: "/acme/eab-credentials", code: 200 }),
  ),
).annotate({
  identifier: "GenerateEabCredentialsRequest",
}) as any as S.Schema<GenerateEabCredentialsRequest>;

export interface EabCredentials {
  success: boolean;
  /** The EAB key id. */
  eab_kid: string;
  /** The base64url HMAC key. */
  eab_hmac_key: string;
}
export const EabCredentials = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    success: S.Boolean,
    eab_kid: S.String,
    eab_hmac_key: S.String.pipe(T.SensitiveValue({})),
  }),
).annotate({ identifier: "EabCredentials" }) as any as S.Schema<EabCredentials>;

export type GenerateEabCredentialsError =
  | InvalidAccessKey
  | RateLimitExceeded
  | ZeroSslOpError;
/** Mint ACME External Account Binding credentials for this account Returns an EAB key id and HMAC key to bind a new ACME account to this ZeroSSL account (`newAccount` with `externalAccountBinding`). Each pair binds exactly one ACME account. */
export const generateEabCredentials: API.OperationMethod<
  GenerateEabCredentialsRequest,
  EabCredentials,
  GenerateEabCredentialsError,
  ZeroSslOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GenerateEabCredentialsRequest,
  output: EabCredentials,
  errors: [InvalidAccessKey, RateLimitExceeded],
  protocol: ZeroSslProtocol,
  retry: Retry.Retry,
}));
