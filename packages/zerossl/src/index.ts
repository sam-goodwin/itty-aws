/**
 * @distilled.cloud/zerossl — ZeroSSL REST SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the hand-authored
 * Smithy model in `specs/zerossl.json`. Everything else in this folder is
 * hand-written.
 *
 * Certificates themselves are issued over ACME (see `@distilled.cloud/acme`);
 * this package covers ZeroSSL's own REST surface, starting with the
 * External Account Binding credentials an ACME account needs.
 *
 * @example
 * ```ts
 * import * as ZeroSsl from "@distilled.cloud/zerossl";
 *
 * const { eab_kid, eab_hmac_key } = yield* ZeroSsl.zerossl.generateEabCredentials({});
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export * as Retry from "./retry.ts";
export {
  ZeroSslProtocol,
  type ZeroSslOpError,
  type ZeroSslOpContext,
} from "./protocol.ts";
export * from "./services/index.ts";
