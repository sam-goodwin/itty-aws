/**
 * @distilled.cloud/chronosphere — Chronosphere Config API SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy model
 * in `.generated-specs` (written by `scripts/convert.ts` from the OpenAPI
 * spec). Everything else in this folder is hand-written.
 *
 * @example
 * ```ts
 * import * as Chronosphere from "@distilled.cloud/chronosphere";
 *
 * const collections = yield* Chronosphere.Services.chronosphere.listCollections({});
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  ChronosphereProtocol,
  type ChronosphereOpError,
  type ChronosphereOpContext,
} from "./protocol.ts";
export { paginateCursor } from "./pagination.ts";
export * as Retry from "./retry.ts";
export * as Services from "./services/index.ts";
// Operations (and their request/response types) are importable straight
// off the package root. The service-local typed error classes shadow the
// same-named shared ones from ./errors.ts when they exist.
export * from "./services/chronosphere.ts";
export { BadRequest, Conflict, NotFound } from "./services/chronosphere.ts";
