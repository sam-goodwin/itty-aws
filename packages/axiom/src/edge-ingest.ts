/**
 * `@distilled.cloud/axiom/edge-ingest` — the v1 edge ingest API
 * (`ingestToDataset`), served from the same canonical host
 * (https://api.axiom.co) under the `/v1` prefix.
 */
export * from "./services/edge_ingest.ts";
