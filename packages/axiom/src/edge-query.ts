/**
 * `@distilled.cloud/axiom/edge-query` — the v1 edge query API (`queryApl`),
 * served from the same canonical host (https://api.axiom.co) under the `/v1`
 * prefix.
 */
export * from "./services/edge_query.ts";
