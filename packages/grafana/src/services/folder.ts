// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-grafana). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  GrafanaProtocol,
  type GrafanaOpError,
  type GrafanaOpContext,
} from "../protocol.ts";
import { UnknownGrafanaError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { GrafanaOpError, GrafanaOpContext };

/** Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations */
export type ObjectMetaAnnotationsMap = { [key: string]: string | undefined };
export const ObjectMetaAnnotationsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ObjectMetaAnnotationsMap>;

/** Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. */
export type ObjectMetaFinalizersList = Array<string>;
export const ObjectMetaFinalizersList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ObjectMetaFinalizersList>;

/** Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels */
export type ObjectMetaLabelsMap = { [key: string]: string | undefined };
export const ObjectMetaLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ObjectMetaLabelsMap>;

/** ManagedFieldsEntry is a workflow-id, a FieldSet and the group version of the resource that the fieldset applies to. */
export interface ManagedFieldsEntry {
  /** APIVersion defines the version of this resource that this field set applies to. The format is "group/version" just like the top-level APIVersion field. It is necessary to track the version of a field set because it cannot be automatically converted. */
  apiVersion?: string;
  /** FieldsType is the discriminator for the different fields format and version. There is currently only one possible value: "FieldsV1" */
  fieldsType?: string;
  /** FieldsV1 holds the first JSON version format as described in the "FieldsV1" type. */
  fieldsV1?: unknown;
  /** Manager is an identifier of the workflow managing these fields. */
  manager?: string;
  /** Operation is the type of operation which lead to this ManagedFieldsEntry being created. The only valid values for this field are 'Apply' and 'Update'. */
  operation?: string;
  /** Subresource is the name of the subresource used to update that object, or empty string if the object was updated through the main resource. The value of this field is used to distinguish between managers, even if they share the same name. For example, a status update will be distinct from a regular update using the same manager name. Note that the APIVersion field is not related to the Subresource field and it always corresponds to the version of the main resource. */
  subresource?: string;
  /** Time is the timestamp of when the ManagedFields entry was added. The timestamp will also be updated if a field is added, the manager changes any of the owned fields value or removes a field. The timestamp does not update when a field is removed from the entry because another manager took it over. */
  time?: string;
}
export const ManagedFieldsEntry = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    fieldsType: S.optional(S.String),
    fieldsV1: S.optional(S.Unknown),
    manager: S.optional(S.String),
    operation: S.optional(S.String),
    subresource: S.optional(S.String),
    time: S.optional(S.String),
  }),
).annotate({
  identifier: "ManagedFieldsEntry",
}) as any as S.Schema<ManagedFieldsEntry>;

/** ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. */
export type ObjectMetaManagedFieldsList = Array<ManagedFieldsEntry>;
export const ObjectMetaManagedFieldsList = /*@__PURE__*/ S.Array(
  ManagedFieldsEntry,
) as any as S.Schema<ObjectMetaManagedFieldsList>;

/** OwnerReference contains enough information to let you identify an owning object. An owning object must be in the same namespace as the dependent, or be cluster-scoped, so there is no namespace field. */
export interface OwnerReference {
  /** API version of the referent. */
  apiVersion: string;
  /** If true, AND if the owner has the "foregroundDeletion" finalizer, then the owner cannot be deleted from the key-value store until this reference is removed. See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion for how the garbage collector interacts with this field and enforces the foreground deletion. Defaults to false. To set this field, a user needs "delete" permission of the owner, otherwise 422 (Unprocessable Entity) will be returned. */
  blockOwnerDeletion?: boolean;
  /** If true, this reference points to the managing controller. */
  controller?: boolean;
  /** Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind: string;
  /** Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names */
  name: string;
  /** UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids */
  uid: string;
}
export const OwnerReference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.String,
    blockOwnerDeletion: S.optional(S.Boolean),
    controller: S.optional(S.Boolean),
    kind: S.String,
    name: S.String,
    uid: S.String,
  }),
).annotate({ identifier: "OwnerReference" }) as any as S.Schema<OwnerReference>;

/** List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. */
export type ObjectMetaOwnerReferencesList = Array<OwnerReference>;
export const ObjectMetaOwnerReferencesList = /*@__PURE__*/ S.Array(
  OwnerReference,
) as any as S.Schema<ObjectMetaOwnerReferencesList>;

/** ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create. */
export interface ObjectMeta {
  /** Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations */
  annotations?: ObjectMetaAnnotationsMap;
  /** CreationTimestamp is a timestamp representing the server time when this object was created. It is not guaranteed to be set in happens-before order across separate operations. Clients may not set this value. It is represented in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for lists. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  creationTimestamp?: string;
  /** Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only. */
  deletionGracePeriodSeconds?: number;
  /** DeletionTimestamp is RFC 3339 date and time at which this resource will be deleted. This field is set by the server when a graceful deletion is requested by the user, and is not directly settable by a client. The resource is expected to be deleted (no longer visible from resource lists, and not reachable by name) after the time in this field, once the finalizers list is empty. As long as the finalizers list contains items, deletion is blocked. Once the deletionTimestamp is set, this value may not be unset or be set further into the future, although it may be shortened or the resource may be deleted prior to this time. For example, a user may request that a pod is deleted in 30 seconds. The Kubelet will react by sending a graceful termination signal to the containers in the pod. After that 30 seconds, the Kubelet will send a hard termination signal (SIGKILL) to the container and after cleanup, remove the pod from the API. In the presence of network partitions, this object may still exist after this timestamp, until an administrator or automated process can determine the resource is fully terminated. If not set, graceful deletion of the object has not been requested. Populated by the system when a graceful deletion is requested. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  deletionTimestamp?: string;
  /** Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. */
  finalizers?: ObjectMetaFinalizersList;
  /** GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server. If this field is specified and the generated name exists, the server will return a 409. Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency */
  generateName?: string;
  /** A sequence number representing a specific generation of the desired state. Populated by the system. Read-only. */
  generation?: number;
  /** Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels */
  labels?: ObjectMetaLabelsMap;
  /** ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. */
  managedFields?: ObjectMetaManagedFieldsList;
  /** Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names */
  name?: string;
  /** Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty. Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces */
  namespace?: string;
  /** List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. */
  ownerReferences?: ObjectMetaOwnerReferencesList;
  /** An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources. Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency */
  resourceVersion?: string;
  /** Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. */
  selfLink?: string;
  /** UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids */
  uid?: string;
}
export const ObjectMeta = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    annotations: S.optional(ObjectMetaAnnotationsMap),
    creationTimestamp: S.optional(S.String),
    deletionGracePeriodSeconds: S.optional(S.Number),
    deletionTimestamp: S.optional(S.String),
    finalizers: S.optional(ObjectMetaFinalizersList),
    generateName: S.optional(S.String),
    generation: S.optional(S.Number),
    labels: S.optional(ObjectMetaLabelsMap),
    managedFields: S.optional(ObjectMetaManagedFieldsList),
    name: S.optional(S.String),
    namespace: S.optional(S.String),
    ownerReferences: S.optional(ObjectMetaOwnerReferencesList),
    resourceVersion: S.optional(S.String),
    selfLink: S.optional(S.String),
    uid: S.optional(S.String),
  }),
).annotate({ identifier: "ObjectMeta" }) as any as S.Schema<ObjectMeta>;

export interface FolderSpec {
  description?: string;
  title: string;
}
export const FolderSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.optional(S.String),
    title: S.String,
  }),
).annotate({ identifier: "FolderSpec" }) as any as S.Schema<FolderSpec>;

export interface CreateFolderRequest {
  namespace: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  metadata: ObjectMeta;
  /** Spec is the spec of the Folder */
  spec: FolderSpec;
}
export const CreateFolderRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
    dryRun: S.optional(S.String.pipe(T.Query())),
    fieldManager: S.optional(S.String.pipe(T.Query())),
    fieldValidation: S.optional(S.String.pipe(T.Query())),
    apiVersion: S.optional(S.String),
    kind: S.optional(S.String),
    metadata: ObjectMeta,
    spec: FolderSpec,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/apis/folder.grafana.app/v1/namespaces/{namespace}/folders",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateFolderRequest",
}) as any as S.Schema<CreateFolderRequest>;

export interface Folder2 {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  metadata: ObjectMeta;
  /** Spec is the spec of the Folder */
  spec: FolderSpec;
}
export const Folder2 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    kind: S.optional(S.String),
    metadata: ObjectMeta,
    spec: FolderSpec,
  }),
).annotate({ identifier: "Folder2" }) as any as S.Schema<Folder2>;

export interface DeleteFolderRequest {
  namespace: string;
  /** name of the Folder */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. */
  gracePeriodSeconds?: number;
  /** if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it */
  ignoreStoreReadErrorWithClusterBreakingPotential?: boolean;
  /** Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. */
  orphanDependents?: boolean;
  /** Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground. */
  propagationPolicy?: string;
}
export const DeleteFolderRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String.pipe(T.Label()),
    name: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
    dryRun: S.optional(S.String.pipe(T.Query())),
    gracePeriodSeconds: S.optional(S.Number.pipe(T.Query())),
    ignoreStoreReadErrorWithClusterBreakingPotential: S.optional(
      S.Boolean.pipe(T.Query()),
    ),
    orphanDependents: S.optional(S.Boolean.pipe(T.Query())),
    propagationPolicy: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/apis/folder.grafana.app/v1/namespaces/{namespace}/folders/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteFolderRequest",
}) as any as S.Schema<DeleteFolderRequest>;

/** StatusCause provides more information about an api.Status failure, including cases when multiple errors are encountered. */
export interface StatusCause {
  /** The field of the resource that has caused this error, as named by its JSON serialization. May include dot and postfix notation for nested attributes. Arrays are zero-indexed. Fields may appear more than once in an array of causes due to fields having multiple errors. Optional. Examples: "name" - the field "name" on the current resource "items[0].name" - the field "name" on the first array entry in "items" */
  field?: string;
  /** A human-readable description of the cause of the error. This field may be presented as-is to a reader. */
  message?: string;
  /** A machine-readable description of the cause of the error. If this value is empty there is no information available. */
  reason?: string;
}
export const StatusCause = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    field: S.optional(S.String),
    message: S.optional(S.String),
    reason: S.optional(S.String),
  }),
).annotate({ identifier: "StatusCause" }) as any as S.Schema<StatusCause>;

/** The Causes array includes more details associated with the StatusReason failure. Not all StatusReasons may provide detailed causes. */
export type StatusDetailsCausesList = Array<StatusCause>;
export const StatusDetailsCausesList = /*@__PURE__*/ S.Array(
  StatusCause,
) as any as S.Schema<StatusDetailsCausesList>;

/** StatusDetails is a set of additional properties that MAY be set by the server to provide additional information about a response. The Reason field of a Status object defines what attributes will be set. Clients must ignore fields that do not match the defined type of each attribute, and should assume that any attribute may be empty, invalid, or under defined. */
export interface StatusDetails {
  /** The Causes array includes more details associated with the StatusReason failure. Not all StatusReasons may provide detailed causes. */
  causes?: StatusDetailsCausesList;
  /** The group attribute of the resource associated with the status StatusReason. */
  group?: string;
  /** The kind attribute of the resource associated with the status StatusReason. On some operations may differ from the requested resource Kind. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** The name attribute of the resource associated with the status StatusReason (when there is a single name which can be described). */
  name?: string;
  /** If specified, the time in seconds before the operation should be retried. Some errors may indicate the client must take an alternate action - for those errors this field may indicate how long to wait before taking the alternate action. */
  retryAfterSeconds?: number;
  /** UID of the resource. (when there is a single resource which can be described). More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids */
  uid?: string;
}
export const StatusDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    causes: S.optional(StatusDetailsCausesList),
    group: S.optional(S.String),
    kind: S.optional(S.String),
    name: S.optional(S.String),
    retryAfterSeconds: S.optional(S.Number),
    uid: S.optional(S.String),
  }),
).annotate({ identifier: "StatusDetails" }) as any as S.Schema<StatusDetails>;

/** ListMeta describes metadata that synthetic resources must have, including lists and various status objects. A resource may have only one of {ObjectMeta, ListMeta}. */
export interface ListMeta {
  /** continue may be set if the user set a limit on the number of items returned, and indicates that the server has more data available. The value is opaque and may be used to issue another request to the endpoint that served this list to retrieve the next set of available objects. Continuing a consistent list may not be possible if the server configuration has changed or more than a few minutes have passed. The resourceVersion field returned when using this continue value will be identical to the value in the first response, unless you have received this token from an error message. */
  continue?: string;
  /** remainingItemCount is the number of subsequent items in the list which are not included in this list response. If the list request contained label or field selectors, then the number of remaining items is unknown and the field will be left unset and omitted during serialization. If the list is complete (either because it is not chunking or because this is the last chunk), then there are no more remaining items and this field will be left unset and omitted during serialization. Servers older than v1.15 do not set this field. The intended use of the remainingItemCount is *estimating* the size of a collection. Clients should not rely on the remainingItemCount to be set or to be exact. */
  remainingItemCount?: number;
  /** String that identifies the server's internal version of this object that can be used by clients to determine when objects have changed. Value must be treated as opaque by clients and passed unmodified back to the server. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency */
  resourceVersion?: string;
  /** Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. */
  selfLink?: string;
}
export const ListMeta = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    continue: S.optional(S.String),
    remainingItemCount: S.optional(S.Number),
    resourceVersion: S.optional(S.String),
    selfLink: S.optional(S.String),
  }),
).annotate({ identifier: "ListMeta" }) as any as S.Schema<ListMeta>;

/** Status is a return value for calls that don't return other objects. */
export interface Status {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Suggested HTTP return code for this status, 0 if not set. */
  code?: number;
  /** Extended data associated with the reason. Each reason may define its own extended details. This field is optional and the data returned is not guaranteed to conform to any schema except that defined by the reason type. */
  details?: StatusDetails;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** A human-readable description of the status of this operation. */
  message?: string;
  /** Standard list metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  metadata?: ListMeta;
  /** A machine-readable description of why this operation is in the "Failure" status. If this value is empty there is no information available. A Reason clarifies an HTTP status code but does not override it. */
  reason?: string;
  /** Status of the operation. One of: "Success" or "Failure". More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  status?: string;
}
export const Status = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    code: S.optional(S.Number),
    details: S.optional(StatusDetails),
    kind: S.optional(S.String),
    message: S.optional(S.String),
    metadata: S.optional(ListMeta),
    reason: S.optional(S.String),
    status: S.optional(S.String),
  }),
).annotate({ identifier: "Status" }) as any as S.Schema<Status>;

export interface GetAPIResourcesRequest {}
export const GetAPIResourcesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "GET", uri: "/apis/folder.grafana.app/v1", code: 200 }),
  ),
).annotate({
  identifier: "GetAPIResourcesRequest",
}) as any as S.Schema<GetAPIResourcesRequest>;

/** categories is a list of the grouped resources this resource belongs to (e.g. 'all') */
export type APIResourceCategoriesList = Array<string>;
export const APIResourceCategoriesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<APIResourceCategoriesList>;

/** shortNames is a list of suggested short names of the resource. */
export type APIResourceShortNamesList = Array<string>;
export const APIResourceShortNamesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<APIResourceShortNamesList>;

/** verbs is a list of supported kube verbs (this includes get, list, watch, create, update, patch, delete, deletecollection, and proxy) */
export type APIResourceVerbsList = Array<string>;
export const APIResourceVerbsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<APIResourceVerbsList>;

/** APIResource specifies the name of a resource and whether it is namespaced. */
export interface APIResource {
  /** categories is a list of the grouped resources this resource belongs to (e.g. 'all') */
  categories?: APIResourceCategoriesList;
  /** group is the preferred group of the resource. Empty implies the group of the containing resource list. For subresources, this may have a different value, for example: Scale". */
  group?: string;
  /** kind is the kind for the resource (e.g. 'Foo' is the kind for a resource 'foo') */
  kind: string;
  /** name is the plural name of the resource. */
  name: string;
  /** namespaced indicates if a resource is namespaced or not. */
  namespaced: boolean;
  /** shortNames is a list of suggested short names of the resource. */
  shortNames?: APIResourceShortNamesList;
  /** singularName is the singular name of the resource. This allows clients to handle plural and singular opaquely. The singularName is more correct for reporting status on a single item and both singular and plural are allowed from the kubectl CLI interface. */
  singularName: string;
  /** The hash value of the storage version, the version this resource is converted to when written to the data store. Value must be treated as opaque by clients. Only equality comparison on the value is valid. This is an alpha feature and may change or be removed in the future. The field is populated by the apiserver only if the StorageVersionHash feature gate is enabled. This field will remain optional even if it graduates. */
  storageVersionHash?: string;
  /** verbs is a list of supported kube verbs (this includes get, list, watch, create, update, patch, delete, deletecollection, and proxy) */
  verbs: APIResourceVerbsList;
  /** version is the preferred version of the resource. Empty implies the version of the containing resource list For subresources, this may have a different value, for example: v1 (while inside a v1beta1 version of the core resource's group)". */
  version?: string;
}
export const APIResource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    categories: S.optional(APIResourceCategoriesList),
    group: S.optional(S.String),
    kind: S.String,
    name: S.String,
    namespaced: S.Boolean,
    shortNames: S.optional(APIResourceShortNamesList),
    singularName: S.String,
    storageVersionHash: S.optional(S.String),
    verbs: APIResourceVerbsList,
    version: S.optional(S.String),
  }),
).annotate({ identifier: "APIResource" }) as any as S.Schema<APIResource>;

/** resources contains the name of the resources and if they are namespaced. */
export type APIResourceListResourcesList = Array<APIResource>;
export const APIResourceListResourcesList = /*@__PURE__*/ S.Array(
  APIResource,
) as any as S.Schema<APIResourceListResourcesList>;

/** APIResourceList is a list of APIResource, it is used to expose the name of the resources supported in a specific group and version, and if the resource is namespaced. */
export interface APIResourceList {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** groupVersion is the group and version this APIResourceList is for. */
  groupVersion: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** resources contains the name of the resources and if they are namespaced. */
  resources: APIResourceListResourcesList;
}
export const APIResourceList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    groupVersion: S.String,
    kind: S.optional(S.String),
    resources: APIResourceListResourcesList,
  }),
).annotate({
  identifier: "APIResourceList",
}) as any as S.Schema<APIResourceList>;

export interface GetFolderRequest {
  namespace: string;
  /** name of the Folder */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
}
export const GetFolderRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String.pipe(T.Label()),
    name: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/folder.grafana.app/v1/namespaces/{namespace}/folders/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetFolderRequest",
}) as any as S.Schema<GetFolderRequest>;

export interface GetFolderAccessRequest {
  namespace: string;
  /** name of the FolderAccessInfo */
  name: string;
}
export const GetFolderAccessRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String.pipe(T.Label()),
    name: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/folder.grafana.app/v1/namespaces/{namespace}/folders/{name}/access",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetFolderAccessRequest",
}) as any as S.Schema<GetFolderAccessRequest>;

export interface GetFolderAccessResponse {}
export const GetFolderAccessResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "GetFolderAccessResponse",
}) as any as S.Schema<GetFolderAccessResponse>;

export interface GetFolderChildrenRequest {
  namespace: string;
  /** name of the FolderList */
  name: string;
}
export const GetFolderChildrenRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String.pipe(T.Label()),
    name: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/folder.grafana.app/v1/namespaces/{namespace}/folders/{name}/children",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetFolderChildrenRequest",
}) as any as S.Schema<GetFolderChildrenRequest>;

export interface GetFolderChildrenResponse {}
export const GetFolderChildrenResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "GetFolderChildrenResponse",
}) as any as S.Schema<GetFolderChildrenResponse>;

export interface GetFolderCountsRequest {
  namespace: string;
  /** name of the DescendantCounts */
  name: string;
}
export const GetFolderCountsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String.pipe(T.Label()),
    name: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/folder.grafana.app/v1/namespaces/{namespace}/folders/{name}/counts",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetFolderCountsRequest",
}) as any as S.Schema<GetFolderCountsRequest>;

export interface GetFolderCountsResponse {}
export const GetFolderCountsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "GetFolderCountsResponse",
}) as any as S.Schema<GetFolderCountsResponse>;

export interface GetFolderParentsRequest {
  namespace: string;
  /** name of the FolderInfoList */
  name: string;
}
export const GetFolderParentsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String.pipe(T.Label()),
    name: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/folder.grafana.app/v1/namespaces/{namespace}/folders/{name}/parents",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetFolderParentsRequest",
}) as any as S.Schema<GetFolderParentsRequest>;

export interface GetFolderParentsResponse {}
export const GetFolderParentsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "GetFolderParentsResponse",
}) as any as S.Schema<GetFolderParentsResponse>;

export interface ListFolderRequest {
  namespace: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const ListFolderRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
    continue: S.optional(S.String.pipe(T.Query())),
    fieldSelector: S.optional(S.String.pipe(T.Query())),
    labelSelector: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    resourceVersion: S.optional(S.String.pipe(T.Query())),
    timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
    watch: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/folder.grafana.app/v1/namespaces/{namespace}/folders",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListFolderRequest",
}) as any as S.Schema<ListFolderRequest>;

export type FolderListItemsList = Array<Folder2>;
export const FolderListItemsList = /*@__PURE__*/ S.Array(
  Folder2,
) as any as S.Schema<FolderListItemsList>;

export interface FolderList {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  items: FolderListItemsList;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  metadata: ListMeta;
}
export const FolderList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    items: FolderListItemsList,
    kind: S.optional(S.String),
    metadata: ListMeta,
  }),
).annotate({ identifier: "FolderList" }) as any as S.Schema<FolderList>;

export type ListFolderSearchV1RequestFacetsList = Array<string>;
export const ListFolderSearchV1RequestFacetsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListFolderSearchV1RequestFacetsList>;

export type ListFolderSearchV1RequestFieldsList = Array<string>;
export const ListFolderSearchV1RequestFieldsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListFolderSearchV1RequestFieldsList>;

/** values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch. */
export type LabelSelectorRequirementValuesList = Array<string>;
export const LabelSelectorRequirementValuesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<LabelSelectorRequirementValuesList>;

/** A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values. */
export interface LabelSelectorRequirement {
  /** key is the label key that the selector applies to. */
  key: string;
  /** operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist. */
  operator: string;
  /** values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch. */
  values?: LabelSelectorRequirementValuesList;
}
export const LabelSelectorRequirement = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key: S.String,
    operator: S.String,
    values: S.optional(LabelSelectorRequirementValuesList),
  }),
).annotate({
  identifier: "LabelSelectorRequirement",
}) as any as S.Schema<LabelSelectorRequirement>;

/** matchExpressions is a list of label selector requirements. The requirements are ANDed. */
export type LabelSelectorMatchExpressionsList = Array<LabelSelectorRequirement>;
export const LabelSelectorMatchExpressionsList = /*@__PURE__*/ S.Array(
  LabelSelectorRequirement,
) as any as S.Schema<LabelSelectorMatchExpressionsList>;

/** matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. */
export type LabelSelectorMatchLabelsMap = { [key: string]: string | undefined };
export const LabelSelectorMatchLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<LabelSelectorMatchLabelsMap>;

/** A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects. */
export interface LabelSelector {
  /** matchExpressions is a list of label selector requirements. The requirements are ANDed. */
  matchExpressions?: LabelSelectorMatchExpressionsList;
  /** matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. */
  matchLabels?: LabelSelectorMatchLabelsMap;
}
export const LabelSelector = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    matchExpressions: S.optional(LabelSelectorMatchExpressionsList),
    matchLabels: S.optional(LabelSelectorMatchLabelsMap),
  }),
).annotate({ identifier: "LabelSelector" }) as any as S.Schema<LabelSelector>;

/** SortField names a field to sort by and a direction ("asc" or "desc"). V1 allows sorting only on scalar string and numeric fields that declare the sort capability. */
export interface SortField {
  direction?: string;
  field: string;
}
export const SortField = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    direction: S.optional(S.String),
    field: S.String,
  }),
).annotate({ identifier: "SortField" }) as any as S.Schema<SortField>;

export type ListFolderSearchV1RequestSortList = Array<SortField>;
export const ListFolderSearchV1RequestSortList = /*@__PURE__*/ S.Array(
  SortField,
) as any as S.Schema<ListFolderSearchV1RequestSortList>;

/** Combinators. */
export type WhereNodeAndList = Array<WhereNode>;
export const WhereNodeAndList = /*@__PURE__*/ S.Array(
  S.suspend(() => WhereNode),
) as any as S.Schema<WhereNodeAndList>;

/** ExistsPredicate is a future field-existence predicate. Modelled for schema stability; always rejected in v1. */
export interface ExistsPredicate {
  field: string;
}
export const ExistsPredicate = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    field: S.String,
  }),
).annotate({
  identifier: "ExistsPredicate",
}) as any as S.Schema<ExistsPredicate>;

export type FilterPredicateValuesList = Array<string>;
export const FilterPredicateValuesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<FilterPredicateValuesList>;

/** FilterPredicate is an exact / set-based predicate against a single field. Values are strings whatever the field's declared type, so a boolean field takes "true" or "false" and a numeric field takes the number written out. Numbers are held as float64, so only integers up to 2^53 are compared exactly. Past that, values a whole number apart share one representation, and a filter can both match a neighbour and miss the value asked for. */
export interface FilterPredicate {
  field: string;
  /** Operator is "In", "NotIn" or "All" in v1. "In" matches any of the values, "NotIn" excludes all of them, and "All" requires the field to hold every value, which only a field holding a list of values can do. */
  operator: string;
  values: FilterPredicateValuesList;
}
export const FilterPredicate = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    field: S.String,
    operator: S.String,
    values: FilterPredicateValuesList,
  }),
).annotate({
  identifier: "FilterPredicate",
}) as any as S.Schema<FilterPredicate>;

export type WhereNodeOrList = Array<WhereNode>;
export const WhereNodeOrList = /*@__PURE__*/ S.Array(
  S.suspend(() => WhereNode),
) as any as S.Schema<WhereNodeOrList>;

/** RangePredicate compares a numeric field against one or two bounds. At least one bound is required, gt cannot be combined with gte, and lt cannot be combined with lte. Boolean and string fields are rejected: only numbers have the order a range asks about. On an integer field a bound must be whole. Bounds are held as float64, so only integers up to 2^53 are compared exactly. Past that, values a whole number apart share one representation, and a bound can both admit a neighbour and exclude the value asked for. */
export interface RangePredicate {
  field: string;
  gt?: number;
  gte?: number;
  lt?: number;
  lte?: number;
}
export const RangePredicate = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    field: S.String,
    gt: S.optional(S.Number),
    gte: S.optional(S.Number),
    lt: S.optional(S.Number),
    lte: S.optional(S.Number),
  }),
).annotate({ identifier: "RangePredicate" }) as any as S.Schema<RangePredicate>;

export type TextPredicateFieldsList = Array<string>;
export const TextPredicateFieldsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TextPredicateFieldsList>;

/** TextPredicate is a free-text predicate against one or more text-capable fields. When Fields is empty it defaults to the kind's searchTextFields (currently ["title"]). */
export interface TextPredicate {
  /** Boost is a future per-leaf score multiplier. Setting it is rejected in v1. */
  boost?: number;
  fields?: TextPredicateFieldsList;
  value: string;
}
export const TextPredicate = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    boost: S.optional(S.Number),
    fields: S.optional(TextPredicateFieldsList),
    value: S.String,
  }),
).annotate({ identifier: "TextPredicate" }) as any as S.Schema<TextPredicate>;

/** WhereNode is a single node of the where tree. Exactly one field must be set; the set field names the node's type. Combinators (and/or/not) compose other nodes, leaves (text/filter/range/exists) are terminal predicates. All node types are modelled so the schema is future-proof, but v1 only accepts a narrow subset (top-level single leaf or a single and of leaves; text, filter and range leaves; In/NotIn/All filter operators). Everything else is rejected with 422 Unprocessable Entity by the validation layer. exists is sketched for a future version and always rejected today. */
export interface WhereNode {
  /** Combinators. */
  and?: WhereNodeAndList;
  exists?: ExistsPredicate;
  filter?: FilterPredicate;
  not?: WhereNode;
  or?: WhereNodeOrList;
  range?: RangePredicate;
  /** Leaves. */
  text?: TextPredicate;
}
export const WhereNode = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    and: S.optional(WhereNodeAndList),
    exists: S.optional(ExistsPredicate),
    filter: S.optional(FilterPredicate),
    not: S.optional(WhereNode),
    or: S.optional(WhereNodeOrList),
    range: S.optional(RangePredicate),
    text: S.optional(TextPredicate),
  }),
).annotate({ identifier: "WhereNode" }) as any as S.Schema<WhereNode>;

export interface ListFolderSearchV1Request {
  namespace: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Continue is an opaque pagination token from a previous page. */
  continue?: string;
  /** FacetLimit caps the number of terms returned per facet. It applies to every entry in Facets. Zero uses the server default; values above the server cap are clamped. */
  facetLimit?: number;
  facets?: ListFolderSearchV1RequestFacetsList;
  fields?: ListFolderSearchV1RequestFieldsList;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** LabelSelector filters on metadata.labels, ANDed with Where. */
  labelSelector?: LabelSelector;
  /** Limit is the page size. Zero uses the default; values above the maximum are clamped. */
  limit?: number;
  sort?: ListFolderSearchV1RequestSortList;
  /** Where is the search predicate tree. Omitting it matches all resources of the kind (subject to labelSelector and per-item authz). */
  where?: WhereNode;
}
export const ListFolderSearchV1Request = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String.pipe(T.Label()),
    apiVersion: S.optional(S.String),
    continue: S.optional(S.String),
    facetLimit: S.optional(S.Number),
    facets: S.optional(ListFolderSearchV1RequestFacetsList),
    fields: S.optional(ListFolderSearchV1RequestFieldsList),
    kind: S.optional(S.String),
    labelSelector: S.optional(LabelSelector),
    limit: S.optional(S.Number),
    sort: S.optional(ListFolderSearchV1RequestSortList),
    where: S.optional(WhereNode),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/apis/folder.grafana.app/v1/namespaces/{namespace}/folders/search",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListFolderSearchV1Request",
}) as any as S.Schema<ListFolderSearchV1Request>;

/** FacetTerm is a single facet term and its count. */
export interface FacetTerm {
  count: number;
  value: string;
}
export const FacetTerm = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    count: S.Number,
    value: S.String,
  }),
).annotate({ identifier: "FacetTerm" }) as any as S.Schema<FacetTerm>;

export type SearchResultsFacetsValueList = Array<FacetTerm>;
export const SearchResultsFacetsValueList = /*@__PURE__*/ S.Array(
  FacetTerm,
) as any as S.Schema<SearchResultsFacetsValueList>;

/** Facets holds term counts per requested facet field. Counts are computed over a bounded sample window, so they are best-effort/approximate. */
export type SearchResultsFacetsMap = {
  [key: string]: SearchResultsFacetsValueList | undefined;
};
export const SearchResultsFacetsMap = /*@__PURE__*/ S.Record(
  S.String,
  SearchResultsFacetsValueList,
) as any as S.Schema<SearchResultsFacetsMap>;

export type Unstructured = { [key: string]: unknown | undefined };
export const Unstructured = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<Unstructured>;

/** ResourceRef is the full identity of a returned item. Namespace is implicit from the URL and omitted. */
export interface ResourceRef {
  group: string;
  kind: string;
  name: string;
  resource: string;
}
export const ResourceRef = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    group: S.String,
    kind: S.String,
    name: S.String,
    resource: S.String,
  }),
).annotate({ identifier: "ResourceRef" }) as any as S.Schema<ResourceRef>;

/** ResultItem is one search or trash hit. The item shape is identical on both endpoints; only which fields the Fields map can carry differs. */
export interface ResultItem {
  /** Fields holds the JSON values for the requested (or default) fields. Array-typed fields are returned as JSON arrays. Absent fields are omitted. */
  fields?: Unstructured;
  resource: ResourceRef;
  /** Score is present only when a text query was evaluated. A pointer so a real score of 0 is still distinguishable from "no text query" (absent). */
  score?: number;
}
export const ResultItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    fields: S.optional(Unstructured),
    resource: ResourceRef,
    score: S.optional(S.Number),
  }),
).annotate({ identifier: "ResultItem" }) as any as S.Schema<ResultItem>;

export type SearchResultsItemsList = Array<ResultItem>;
export const SearchResultsItemsList = /*@__PURE__*/ S.Array(
  ResultItem,
) as any as S.Schema<SearchResultsItemsList>;

/** ResultsMetadata carries the pagination token and the total hit count. */
export interface ResultsMetadata {
  continue?: string;
  /** TotalHits counts the resources matching the query. Always read it together with TotalHitsRelation, which says whether the count is exact. */
  totalHits: number;
  /** TotalHitsRelation is "eq" when TotalHits is exact and "lte" when it is an upper bound. */
  totalHitsRelation: string;
}
export const ResultsMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    continue: S.optional(S.String),
    totalHits: S.Number,
    totalHitsRelation: S.String,
  }),
).annotate({
  identifier: "ResultsMetadata",
}) as any as S.Schema<ResultsMetadata>;

/** SearchResults is the response body for POST .../{resource}/search. */
export interface SearchResults {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Facets holds term counts per requested facet field. Counts are computed over a bounded sample window, so they are best-effort/approximate. */
  facets?: SearchResultsFacetsMap;
  items: SearchResultsItemsList;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  metadata: ResultsMetadata;
}
export const SearchResults = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    facets: S.optional(SearchResultsFacetsMap),
    items: SearchResultsItemsList,
    kind: S.optional(S.String),
    metadata: ResultsMetadata,
  }),
).annotate({ identifier: "SearchResults" }) as any as S.Schema<SearchResults>;

export interface ReplaceFolderRequest {
  namespace: string;
  /** name of the Folder */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  metadata: ObjectMeta;
  /** Spec is the spec of the Folder */
  spec: FolderSpec;
}
export const ReplaceFolderRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String.pipe(T.Label()),
    name: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
    dryRun: S.optional(S.String.pipe(T.Query())),
    fieldManager: S.optional(S.String.pipe(T.Query())),
    fieldValidation: S.optional(S.String.pipe(T.Query())),
    apiVersion: S.optional(S.String),
    kind: S.optional(S.String),
    metadata: ObjectMeta,
    spec: FolderSpec,
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/apis/folder.grafana.app/v1/namespaces/{namespace}/folders/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ReplaceFolderRequest",
}) as any as S.Schema<ReplaceFolderRequest>;

export interface UpdateFolderRequest {
  namespace: string;
  /** name of the Folder */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch). */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests. */
  force?: boolean;
}
export const UpdateFolderRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    namespace: S.String.pipe(T.Label()),
    name: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
    dryRun: S.optional(S.String.pipe(T.Query())),
    fieldManager: S.optional(S.String.pipe(T.Query())),
    fieldValidation: S.optional(S.String.pipe(T.Query())),
    force: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/apis/folder.grafana.app/v1/namespaces/{namespace}/folders/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateFolderRequest",
}) as any as S.Schema<UpdateFolderRequest>;

export type CreateFolderError = GrafanaOpError;
/** create a Folder */
export const createFolder: API.OperationMethod<
  CreateFolderRequest,
  Folder2,
  CreateFolderError,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateFolderRequest,
  output: Folder2,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));

export type DeleteFolderError = GrafanaOpError;
/** delete a Folder */
export const deleteFolder: API.OperationMethod<
  DeleteFolderRequest,
  Status,
  DeleteFolderError,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteFolderRequest,
  output: Status,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));

export type GetAPIResourcesError = GrafanaOpError;
/** Describe the available kubernetes resources */
export const getAPIResources: API.OperationMethod<
  GetAPIResourcesRequest,
  APIResourceList,
  GetAPIResourcesError,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetAPIResourcesRequest,
  output: APIResourceList,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));

export type GetFolderError = GrafanaOpError;
/** read the specified Folder */
export const getFolder: API.OperationMethod<
  GetFolderRequest,
  Folder2,
  GetFolderError,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetFolderRequest,
  output: Folder2,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));

export type GetFolderAccessError = GrafanaOpError;
/** connect GET requests to access of Folder */
export const getFolderAccess: API.OperationMethod<
  GetFolderAccessRequest,
  GetFolderAccessResponse,
  GetFolderAccessError,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetFolderAccessRequest,
  output: GetFolderAccessResponse,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));

export type GetFolderChildrenError = GrafanaOpError;
/** connect GET requests to children of Folder */
export const getFolderChildren: API.OperationMethod<
  GetFolderChildrenRequest,
  GetFolderChildrenResponse,
  GetFolderChildrenError,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetFolderChildrenRequest,
  output: GetFolderChildrenResponse,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));

export type GetFolderCountsError = GrafanaOpError;
/** connect GET requests to counts of Folder */
export const getFolderCounts: API.OperationMethod<
  GetFolderCountsRequest,
  GetFolderCountsResponse,
  GetFolderCountsError,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetFolderCountsRequest,
  output: GetFolderCountsResponse,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));

export type GetFolderParentsError = GrafanaOpError;
/** connect GET requests to parents of Folder */
export const getFolderParents: API.OperationMethod<
  GetFolderParentsRequest,
  GetFolderParentsResponse,
  GetFolderParentsError,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetFolderParentsRequest,
  output: GetFolderParentsResponse,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));

export type ListFolderError = GrafanaOpError;
/** list objects of kind Folder */
export const listFolder: API.OperationMethod<
  ListFolderRequest,
  FolderList,
  ListFolderError,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListFolderRequest,
  output: FolderList,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));

export type ListFolderSearchV1Error = GrafanaOpError;
/** Search Folder resources in a namespace. */
export const listFolderSearchV1: API.OperationMethod<
  ListFolderSearchV1Request,
  SearchResults,
  ListFolderSearchV1Error,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListFolderSearchV1Request,
  output: SearchResults,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));

export type ReplaceFolderError = GrafanaOpError;
/** replace the specified Folder */
export const replaceFolder: API.OperationMethod<
  ReplaceFolderRequest,
  Folder2,
  ReplaceFolderError,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReplaceFolderRequest,
  output: Folder2,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));

export type UpdateFolderError = GrafanaOpError;
/** partially update the specified Folder */
export const updateFolder: API.OperationMethod<
  UpdateFolderRequest,
  Folder2,
  UpdateFolderError,
  GrafanaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateFolderRequest,
  output: Folder2,
  errors: [UnknownGrafanaError],
  protocol: GrafanaProtocol,
  retry: Retry.Retry,
}));
