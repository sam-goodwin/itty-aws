/**
 * @distilled.cloud/forgejo — Forgejo REST API SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy models in
 * `.generated-specs` (written by `scripts/convert.ts` from the Swagger 2.0
 * document in the `spec-mirror-forgejo` submodule under `specs/`). Everything
 * else in this folder is hand-written.
 *
 * One module per Forgejo API tag — `repository`, `organization`, `user`,
 * `issue`, `admin`, … — the same split the API reference uses.
 *
 * @example
 * ```ts
 * import * as Forgejo from "@distilled.cloud/forgejo";
 *
 * const repo = yield* Forgejo.Services.repository.repoGet({
 *   owner: "acme",
 *   repo: "api",
 * });
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  ForgejoProtocol,
  type ForgejoOpError,
  type ForgejoOpContext,
} from "./protocol.ts";
export * as Retry from "./retry.ts";
export * as Services from "./services/index.ts";
