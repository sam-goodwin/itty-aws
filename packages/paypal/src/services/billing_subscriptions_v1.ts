// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-paypal). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  PaypalProtocol,
  type PaypalOpError,
  type PaypalOpContext,
} from "../protocol.ts";
import { UnknownPaypalError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { PaypalOpError, PaypalOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface PlansActivateRequest {
  /** The ID of the subscription. */
  id: string;
}
export const PlansActivateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v1/billing/plans/{id}/activate",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PlansActivateRequest",
}) as any as S.Schema<PlansActivateRequest>;

export interface PlansActivateResponse {}
export const PlansActivateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PlansActivateResponse",
}) as any as S.Schema<PlansActivateResponse>;

/** The initial state of the plan. Allowed input values are CREATED and ACTIVE. */
export type PlansCreateRequestStatus = "CREATED" | "INACTIVE" | "ACTIVE";
export const PlansCreateRequestStatus = /*@__PURE__*/ S.String;

/** The currency and amount for a financial transaction, such as a balance or payment due. */
export interface Money {
  currency_code: string;
  /** The value, which might be:<ul><li>An integer for currencies like `JPY` that are not typically fractional.</li><li>A decimal fraction for currencies like `TND` that are subdivided into thousandths.</li></ul>For the required number of decimal places for a currency code, see [Currency Codes](/api/rest/reference/currency-codes/). */
  value: string;
}
export const Money = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    currency_code: S.String,
    value: S.String,
  }),
).annotate({ identifier: "Money" }) as any as S.Schema<Money>;

/** The pricing model for tiered plan. The `tiers` parameter is required. */
export type PricingSchemeInputPricingModel = "VOLUME" | "TIERED";
export const PricingSchemeInputPricingModel = /*@__PURE__*/ S.String;

/** The pricing tier details. */
export interface PricingTier {
  /** The starting quantity for the tier. */
  starting_quantity: string;
  /** The ending quantity for the tier. Optional for the last tier. */
  ending_quantity?: string;
  /** The pricing amount for the tier. */
  amount: Money;
}
export const PricingTier = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    starting_quantity: S.String,
    ending_quantity: S.optional(S.String),
    amount: Money,
  }),
).annotate({ identifier: "PricingTier" }) as any as S.Schema<PricingTier>;

/** An array of pricing tiers which are used for billing volume/tiered plans. pricing_model field has to be specified. */
export type PricingTierList = Array<PricingTier>;
export const PricingTierList = /*@__PURE__*/ S.Array(
  PricingTier,
) as any as S.Schema<PricingTierList>;

/** The pricing scheme details. */
export interface PricingSchemeInput {
  /** The fixed amount to charge for the subscription. The changes to fixed amount are applicable to both existing and future subscriptions. For existing subscriptions, payments within 10 days of price change are not affected. */
  fixed_price?: Money;
  /** The pricing model for tiered plan. The `tiers` parameter is required. */
  pricing_model?: PricingSchemeInputPricingModel | (string & {});
  tiers?: PricingTierList;
}
export const PricingSchemeInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    fixed_price: S.optional(Money),
    pricing_model: S.optional(PricingSchemeInputPricingModel),
    tiers: S.optional(PricingTierList),
  }),
).annotate({
  identifier: "PricingSchemeInput",
}) as any as S.Schema<PricingSchemeInput>;

/** The interval at which the subscription is charged or billed. */
export type FrequencyIntervalUnit = "DAY" | "WEEK" | "MONTH" | "YEAR";
export const FrequencyIntervalUnit = /*@__PURE__*/ S.String;

/** The frequency of the billing cycle. */
export interface Frequency {
  /** The interval at which the subscription is charged or billed. */
  interval_unit: FrequencyIntervalUnit | (string & {});
  /** The number of intervals after which a subscriber is billed. For example, if the `interval_unit` is `DAY` with an `interval_count` of `2`, the subscription is billed once every two days. The following table lists the maximum allowed values for the `interval_count` for each `interval_unit`:<table><thead><tr><th><code>Interval unit</code></th><th>Maximum interval count</th></tr></thead><tbody><tr><td><code>DAY</code></td><td align="right">365</td></tr><tr><td><code>WEEK</code></td><td align="right">52</td></tr><tr><td><code>MONTH</code></td><td align="right">12</td></tr><tr><td><code>YEAR</code></td><td align="right">1</td></tr></tbody></table> */
  interval_count?: number;
}
export const Frequency = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    interval_unit: FrequencyIntervalUnit,
    interval_count: S.optional(S.Number),
  }),
).annotate({ identifier: "Frequency" }) as any as S.Schema<Frequency>;

/** The tenure type of the billing cycle. In case of a plan having trial cycle, only 2 trial cycles are allowed per plan. */
export type BillingCycleInputTenureType = "REGULAR" | "TRIAL";
export const BillingCycleInputTenureType = /*@__PURE__*/ S.String;

/** The billing cycle details. */
export interface BillingCycleInput {
  /** The active pricing scheme for this billing cycle. A free trial billing cycle does not require a pricing scheme. */
  pricing_scheme?: PricingSchemeInput;
  /** The frequency details for this billing cycle. */
  frequency: Frequency;
  /** The tenure type of the billing cycle. In case of a plan having trial cycle, only 2 trial cycles are allowed per plan. */
  tenure_type: BillingCycleInputTenureType | (string & {});
  /** The order in which this cycle is to run among other billing cycles. For example, a trial billing cycle has a `sequence` of `1` while a regular billing cycle has a `sequence` of `2`, so that trial cycle runs before the regular cycle. */
  sequence: number;
  /** The number of times this billing cycle gets executed. Trial billing cycles can only be executed a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). Regular billing cycles can be executed infinite times (value of <code>0</code> for <code>total_cycles</code>) or a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). */
  total_cycles?: number;
}
export const BillingCycleInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pricing_scheme: S.optional(PricingSchemeInput),
    frequency: Frequency,
    tenure_type: BillingCycleInputTenureType,
    sequence: S.Number,
    total_cycles: S.optional(S.Number),
  }),
).annotate({
  identifier: "BillingCycleInput",
}) as any as S.Schema<BillingCycleInput>;

/** An array of billing cycles for trial billing and regular billing. A plan can have at most two trial cycles and only one regular cycle. */
export type BillingCycleListInput = Array<BillingCycleInput>;
export const BillingCycleListInput = /*@__PURE__*/ S.Array(
  BillingCycleInput,
) as any as S.Schema<BillingCycleListInput>;

/** The action to take on the subscription if the initial payment for the setup fails. */
export type PaymentPreferencesSetupFeeFailureAction = "CONTINUE" | "CANCEL";
export const PaymentPreferencesSetupFeeFailureAction = /*@__PURE__*/ S.String;

/** The payment preferences for a subscription. */
export interface PaymentPreferences {
  /** Indicates whether to automatically bill the outstanding amount in the next billing cycle. */
  auto_bill_outstanding?: boolean;
  /** The initial set-up fee for the service. */
  setup_fee?: Money;
  /** The action to take on the subscription if the initial payment for the setup fails. */
  setup_fee_failure_action?:
    | PaymentPreferencesSetupFeeFailureAction
    | (string & {});
  /** The maximum number of payment failures before a subscription is suspended. For example, if `payment_failure_threshold` is `2`, the subscription automatically updates to the `SUSPEND` state if two consecutive payments fail. */
  payment_failure_threshold?: number;
}
export const PaymentPreferences = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auto_bill_outstanding: S.optional(S.Boolean),
    setup_fee: S.optional(Money),
    setup_fee_failure_action: S.optional(
      PaymentPreferencesSetupFeeFailureAction,
    ),
    payment_failure_threshold: S.optional(S.Number),
  }),
).annotate({
  identifier: "PaymentPreferences",
}) as any as S.Schema<PaymentPreferences>;

/** The tax details. */
export interface Taxes {
  /** The tax percentage on the billing amount. */
  percentage: string;
  /** Indicates whether the tax was already included in the billing amount. */
  inclusive?: boolean;
}
export const Taxes = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    percentage: S.String,
    inclusive: S.optional(S.Boolean),
  }),
).annotate({ identifier: "Taxes" }) as any as S.Schema<Taxes>;

export interface PlansCreateRequest {
  /** The preferred server response upon successful completion of the request. Value is:<ul><li><code>return=minimal</code>. The server returns a minimal response to optimize communication between the API caller and the server. A minimal response includes the <code>id</code>, <code>status</code> and HATEOAS links.</li><li><code>return=representation</code>. The server returns a complete resource representation, including the current state of the resource.</li></ul> */
  prefer?: string;
  /** The server stores keys for 72 hours. */
  payPalRequestId?: string;
  /** The ID of the product created through Catalog Products API. */
  product_id: string;
  /** The plan name. */
  name: string;
  /** The initial state of the plan. Allowed input values are CREATED and ACTIVE. */
  status?: PlansCreateRequestStatus | (string & {});
  /** The detailed description of the plan. */
  description?: string;
  billing_cycles: BillingCycleListInput;
  payment_preferences: PaymentPreferences;
  taxes?: Taxes;
  /** Indicates whether you can subscribe to this plan by providing a quantity for the goods or service. */
  quantity_supported?: boolean;
}
export const PlansCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    prefer: S.optional(S.String.pipe(T.Header("Prefer"))),
    payPalRequestId: S.optional(S.String.pipe(T.Header("PayPal-Request-Id"))),
    product_id: S.String,
    name: S.String,
    status: S.optional(PlansCreateRequestStatus),
    description: S.optional(S.String),
    billing_cycles: BillingCycleListInput,
    payment_preferences: PaymentPreferences,
    taxes: S.optional(Taxes),
    quantity_supported: S.optional(S.Boolean),
  }).pipe(T.Http({ method: "POST", uri: "/v1/billing/plans", code: 200 })),
).annotate({
  identifier: "PlansCreateRequest",
}) as any as S.Schema<PlansCreateRequest>;

/** The plan status. */
export type PlanStatus = "CREATED" | "INACTIVE" | "ACTIVE";
export const PlanStatus = /*@__PURE__*/ S.String;

/** The pricing model for tiered plan. The `tiers` parameter is required. */
export type PricingSchemePricingModel = "VOLUME" | "TIERED";
export const PricingSchemePricingModel = /*@__PURE__*/ S.String;

/** The pricing scheme details. */
export interface PricingScheme {
  /** The version of the pricing scheme. */
  version?: number;
  /** The fixed amount to charge for the subscription. The changes to fixed amount are applicable to both existing and future subscriptions. For existing subscriptions, payments within 10 days of price change are not affected. */
  fixed_price?: Money;
  /** The pricing model for tiered plan. The `tiers` parameter is required. */
  pricing_model?: PricingSchemePricingModel;
  tiers?: PricingTierList;
  /** The date and time when this pricing scheme was created, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  create_time?: string;
  /** The date and time when this pricing scheme was last updated, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  update_time?: string;
}
export const PricingScheme = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    version: S.optional(S.Number),
    fixed_price: S.optional(Money),
    pricing_model: S.optional(PricingSchemePricingModel),
    tiers: S.optional(PricingTierList),
    create_time: S.optional(S.String),
    update_time: S.optional(S.String),
  }),
).annotate({ identifier: "PricingScheme" }) as any as S.Schema<PricingScheme>;

/** The tenure type of the billing cycle. In case of a plan having trial cycle, only 2 trial cycles are allowed per plan. */
export type BillingCycleTenureType = "REGULAR" | "TRIAL";
export const BillingCycleTenureType = /*@__PURE__*/ S.String;

/** The billing cycle details. */
export interface BillingCycle {
  /** The active pricing scheme for this billing cycle. A free trial billing cycle does not require a pricing scheme. */
  pricing_scheme?: PricingScheme;
  /** The frequency details for this billing cycle. */
  frequency: Frequency;
  /** The tenure type of the billing cycle. In case of a plan having trial cycle, only 2 trial cycles are allowed per plan. */
  tenure_type: BillingCycleTenureType;
  /** The order in which this cycle is to run among other billing cycles. For example, a trial billing cycle has a `sequence` of `1` while a regular billing cycle has a `sequence` of `2`, so that trial cycle runs before the regular cycle. */
  sequence: number;
  /** The number of times this billing cycle gets executed. Trial billing cycles can only be executed a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). Regular billing cycles can be executed infinite times (value of <code>0</code> for <code>total_cycles</code>) or a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). */
  total_cycles?: number;
}
export const BillingCycle = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pricing_scheme: S.optional(PricingScheme),
    frequency: Frequency,
    tenure_type: BillingCycleTenureType,
    sequence: S.Number,
    total_cycles: S.optional(S.Number),
  }),
).annotate({ identifier: "BillingCycle" }) as any as S.Schema<BillingCycle>;

/** An array of billing cycles for trial billing and regular billing. A plan can have at most two trial cycles and only one regular cycle. */
export type BillingCycleList = Array<BillingCycle>;
export const BillingCycleList = /*@__PURE__*/ S.Array(
  BillingCycle,
) as any as S.Schema<BillingCycleList>;

/** The HTTP method required to make the related call. */
export type LinkDescriptionMethod =
  | "GET"
  | "POST"
  | "PUT"
  | "DELETE"
  | "HEAD"
  | "CONNECT"
  | "OPTIONS"
  | "PATCH";
export const LinkDescriptionMethod = /*@__PURE__*/ S.String;

/** The request-related [HATEOAS link](/api/rest/responses/#hateoas-links) information. */
export interface LinkDescription {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  /** The HTTP method required to make the related call. */
  method?: LinkDescriptionMethod;
}
export const LinkDescription = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    href: S.String,
    rel: S.String,
    method: S.optional(LinkDescriptionMethod),
  }),
).annotate({
  identifier: "LinkDescription",
}) as any as S.Schema<LinkDescription>;

/** An array of request-related [HATEOAS links](/docs/api/reference/api-responses/#hateoas-links). */
export type LinkDescriptionList = Array<LinkDescription>;
export const LinkDescriptionList = /*@__PURE__*/ S.Array(
  LinkDescription,
) as any as S.Schema<LinkDescriptionList>;

/** The plan details. */
export interface Plan {
  /** The unique PayPal-generated ID for the plan. */
  id?: string;
  /** The ID for the product. */
  product_id?: string;
  /** The plan name. */
  name?: string;
  /** The plan status. */
  status?: PlanStatus;
  /** The detailed description of the plan. */
  description?: string;
  billing_cycles?: BillingCycleList;
  payment_preferences?: PaymentPreferences;
  taxes?: Taxes;
  /** Indicates whether you can subscribe to this plan by providing a quantity for the goods or service. */
  quantity_supported?: boolean;
  /** The date and time when the plan was created, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  create_time?: string;
  /** The date and time when the plan was last updated, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  update_time?: string;
  links?: LinkDescriptionList;
}
export const Plan = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    product_id: S.optional(S.String),
    name: S.optional(S.String),
    status: S.optional(PlanStatus),
    description: S.optional(S.String),
    billing_cycles: S.optional(BillingCycleList),
    payment_preferences: S.optional(PaymentPreferences),
    taxes: S.optional(Taxes),
    quantity_supported: S.optional(S.Boolean),
    create_time: S.optional(S.String),
    update_time: S.optional(S.String),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({ identifier: "Plan" }) as any as S.Schema<Plan>;

export interface PlansDeactivateRequest {
  /** The ID of the subscription. */
  id: string;
}
export const PlansDeactivateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v1/billing/plans/{id}/deactivate",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PlansDeactivateRequest",
}) as any as S.Schema<PlansDeactivateRequest>;

export interface PlansDeactivateResponse {}
export const PlansDeactivateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PlansDeactivateResponse",
}) as any as S.Schema<PlansDeactivateResponse>;

export interface PlansGetRequest {
  /** The ID of the subscription. */
  id: string;
}
export const PlansGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/v1/billing/plans/{id}", code: 200 })),
).annotate({
  identifier: "PlansGetRequest",
}) as any as S.Schema<PlansGetRequest>;

export interface PlansListRequest {
  /** Filters the response by a Product ID. */
  product_id?: string;
  /** The number of items to return in the response. */
  page_size?: number;
  /** A non-zero integer which is the start index of the entire list of items to return in the response. The combination of `page=1` and `page_size=20` returns the first 20 items. The combination of `page=2` and `page_size=20` returns the next 20 items. */
  page?: number;
  /** Indicates whether to show the total count in the response. */
  total_required?: boolean;
  /** The preferred server response upon successful completion of the request. Value is:<ul><li><code>return=minimal</code>. The server returns a minimal response to optimize communication between the API caller and the server. A minimal response includes the <code>id</code>, <code>status</code> and HATEOAS links.</li><li><code>return=representation</code>. The server returns a complete resource representation, including the current state of the resource.</li></ul> */
  prefer?: string;
}
export const PlansListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    product_id: S.optional(S.String.pipe(T.Query())),
    page_size: S.optional(S.Number.pipe(T.Query())),
    page: S.optional(S.Number.pipe(T.Query())),
    total_required: S.optional(S.Boolean.pipe(T.Query())),
    prefer: S.optional(S.String.pipe(T.Header("Prefer"))),
  }).pipe(T.Http({ method: "GET", uri: "/v1/billing/plans", code: 200 })),
).annotate({
  identifier: "PlansListRequest",
}) as any as S.Schema<PlansListRequest>;

/** An array of plans. */
export type PlanList = Array<Plan>;
export const PlanList = /*@__PURE__*/ S.Array(
  Plan,
) as any as S.Schema<PlanList>;

/** The list of plans with details. */
export interface PlanCollection {
  plans?: PlanList;
  /** The total number of items. */
  total_items?: number;
  /** The total number of pages. */
  total_pages?: number;
  links?: LinkDescriptionList;
}
export const PlanCollection = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    plans: S.optional(PlanList),
    total_items: S.optional(S.Number),
    total_pages: S.optional(S.Number),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({ identifier: "PlanCollection" }) as any as S.Schema<PlanCollection>;

/** The operation. */
export type PatchOp = "add" | "remove" | "replace" | "move" | "copy" | "test";
export const PatchOp = /*@__PURE__*/ S.String;

/** The JSON patch object to apply partial updates to resources. */
export interface Patch {
  /** The operation. */
  op: PatchOp | (string & {});
  /** The <a href="https://tools.ietf.org/html/rfc6901">JSON Pointer</a> to the target document location at which to complete the operation. */
  path?: string;
  /** The value to apply. The <code>remove</code>, <code>copy</code>, and <code>move</code> operations do not require a value. Since <a href="https://www.rfc-editor.org/rfc/rfc69021">JSON Patch</a> allows any type for <code>value</code>, the <code>type</code> property is not specified. */
  value?: unknown;
  /** The <a href="https://tools.ietf.org/html/rfc6901">JSON Pointer</a> to the target document location from which to move the value. Required for the <code>move</code> operation. */
  from?: string;
}
export const Patch = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    op: PatchOp,
    path: S.optional(S.String),
    value: S.optional(S.Unknown),
    from: S.optional(S.String),
  }),
).annotate({ identifier: "Patch" }) as any as S.Schema<Patch>;

/** An array of JSON patch objects to apply partial updates to resources. */
export type PatchRequest = Array<Patch>;
export const PatchRequest = /*@__PURE__*/ S.Array(
  Patch,
) as any as S.Schema<PatchRequest>;

export interface PlansPatchRequest {
  /** The ID of the subscription. */
  id: string;
  body?: PatchRequest;
}
export const PlansPatchRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    body: S.optional(PatchRequest.pipe(T.HttpBody())),
  }).pipe(
    T.Http({ method: "PATCH", uri: "/v1/billing/plans/{id}", code: 200 }),
  ),
).annotate({
  identifier: "PlansPatchRequest",
}) as any as S.Schema<PlansPatchRequest>;

export interface PlansPatchResponse {}
export const PlansPatchResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PlansPatchResponse",
}) as any as S.Schema<PlansPatchResponse>;

/** The update pricing scheme request details. */
export interface UpdatePricingSchemeRequestInput {
  /** The billing cycle sequence. */
  billing_cycle_sequence: number;
  pricing_scheme: PricingSchemeInput;
}
export const UpdatePricingSchemeRequestInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing_cycle_sequence: S.Number,
    pricing_scheme: PricingSchemeInput,
  }),
).annotate({
  identifier: "UpdatePricingSchemeRequestInput",
}) as any as S.Schema<UpdatePricingSchemeRequestInput>;

/** An array of pricing schemes. */
export type UpdatePricingSchemeRequestListInput =
  Array<UpdatePricingSchemeRequestInput>;
export const UpdatePricingSchemeRequestListInput = /*@__PURE__*/ S.Array(
  UpdatePricingSchemeRequestInput,
) as any as S.Schema<UpdatePricingSchemeRequestListInput>;

export interface PlansUpdatePricingSchemesRequest {
  /** The ID of the subscription. */
  id: string;
  pricing_schemes: UpdatePricingSchemeRequestListInput;
}
export const PlansUpdatePricingSchemesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    pricing_schemes: UpdatePricingSchemeRequestListInput,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v1/billing/plans/{id}/update-pricing-schemes",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PlansUpdatePricingSchemesRequest",
}) as any as S.Schema<PlansUpdatePricingSchemesRequest>;

export interface PlansUpdatePricingSchemesResponse {}
export const PlansUpdatePricingSchemesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PlansUpdatePricingSchemesResponse",
}) as any as S.Schema<PlansUpdatePricingSchemesResponse>;

export interface SubscriptionsActivateRequest {
  /** The ID of the subscription. */
  id: string;
  /** The reason for activation of a subscription. Required to reactivate the subscription. */
  reason?: string;
}
export const SubscriptionsActivateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    reason: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v1/billing/subscriptions/{id}/activate",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SubscriptionsActivateRequest",
}) as any as S.Schema<SubscriptionsActivateRequest>;

export interface SubscriptionsActivateResponse {}
export const SubscriptionsActivateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "SubscriptionsActivateResponse",
}) as any as S.Schema<SubscriptionsActivateResponse>;

export interface SubscriptionsCancelRequest {
  /** The ID of the subscription. */
  id: string;
  /** The reason for the cancellation of a subscription. */
  reason: string;
}
export const SubscriptionsCancelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    reason: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v1/billing/subscriptions/{id}/cancel",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SubscriptionsCancelRequest",
}) as any as S.Schema<SubscriptionsCancelRequest>;

export interface SubscriptionsCancelResponse {}
export const SubscriptionsCancelResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "SubscriptionsCancelResponse",
}) as any as S.Schema<SubscriptionsCancelResponse>;

/** The type of capture. */
export type SubscriptionsCaptureRequestCaptureType = "OUTSTANDING_BALANCE";
export const SubscriptionsCaptureRequestCaptureType = /*@__PURE__*/ S.String;

export interface SubscriptionsCaptureRequest {
  /** The ID of the subscription. */
  id: string;
  /** The server stores keys for 72 hours. */
  payPalRequestId?: string;
  /** The reason or note for the subscription charge. */
  note: string;
  /** The type of capture. */
  capture_type: SubscriptionsCaptureRequestCaptureType | (string & {});
  /** The amount of the outstanding balance. This value cannot be greater than the current outstanding balance amount. */
  amount: Money;
}
export const SubscriptionsCaptureRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    payPalRequestId: S.optional(S.String.pipe(T.Header("PayPal-Request-Id"))),
    note: S.String,
    capture_type: SubscriptionsCaptureRequestCaptureType,
    amount: Money,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v1/billing/subscriptions/{id}/capture",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SubscriptionsCaptureRequest",
}) as any as S.Schema<SubscriptionsCaptureRequest>;

/** The status of the captured payment. */
export type TransactionStatus =
  | "COMPLETED"
  | "DECLINED"
  | "PARTIALLY_REFUNDED"
  | "PENDING"
  | "REFUNDED"
  | "FAILED";
export const TransactionStatus = /*@__PURE__*/ S.String;

/** The breakdown details for the amount. Includes the gross, tax, fee, and shipping amounts. */
export interface AmountWithBreakdown {
  /** The amount for this transaction. */
  gross_amount: Money;
  /** The item total for the transaction. */
  total_item_amount?: Money;
  /** The fee details for the transaction. */
  fee_amount?: Money;
  /** The shipping amount for the transaction. */
  shipping_amount?: Money;
  /** The tax amount for the transaction. */
  tax_amount?: Money;
  /** The net amount that the payee receives for this transaction in their PayPal account. The net amount is computed as <code>gross_amount</code> minus the <code>paypal_fee</code>. */
  net_amount?: Money;
}
export const AmountWithBreakdown = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    gross_amount: Money,
    total_item_amount: S.optional(Money),
    fee_amount: S.optional(Money),
    shipping_amount: S.optional(Money),
    tax_amount: S.optional(Money),
    net_amount: S.optional(Money),
  }),
).annotate({
  identifier: "AmountWithBreakdown",
}) as any as S.Schema<AmountWithBreakdown>;

/** The name of the party. */
export interface Name {
  /** The prefix, or title, to the party's name. */
  prefix?: string;
  /** When the party is a person, the party's given, or first, name. */
  given_name?: string;
  /** When the party is a person, the party's surname or family name. Also known as the last name. Required when the party is a person. Use also to store multiple surnames including the matronymic, or mother's, surname. */
  surname?: string;
  /** When the party is a person, the party's middle name. Use also to store multiple middle names including the patronymic, or father's, middle name. */
  middle_name?: string;
  /** The suffix for the party's name. */
  suffix?: string;
  /** When the party is a person, the party's full name. */
  full_name?: string;
}
export const Name = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    prefix: S.optional(S.String),
    given_name: S.optional(S.String),
    surname: S.optional(S.String),
    middle_name: S.optional(S.String),
    suffix: S.optional(S.String),
    full_name: S.optional(S.String),
  }),
).annotate({ identifier: "Name" }) as any as S.Schema<Name>;

/** The transaction details. */
export interface Transaction {
  /** The status of the captured payment. */
  status?: TransactionStatus;
  /** The PayPal-generated transaction ID. */
  id: string;
  amount_with_breakdown: AmountWithBreakdown;
  /** The name of the customer. */
  payer_name?: Name;
  /** The email ID of the customer. */
  payer_email?: string;
  /** The date and time when the transaction was processed, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  time: string;
}
export const Transaction = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    status: S.optional(TransactionStatus),
    id: S.String,
    amount_with_breakdown: AmountWithBreakdown,
    payer_name: S.optional(Name),
    payer_email: S.optional(S.String),
    time: S.String,
  }),
).annotate({ identifier: "Transaction" }) as any as S.Schema<Transaction>;

/** The name of the party. */
export interface SubscriberRequestName {
  /** When the party is a person, the party's given, or first, name. */
  given_name?: string;
  /** When the party is a person, the party's surname or family name. Also known as the last name. Required when the party is a person. Use also to store multiple surnames including the matronymic, or mother's, surname. */
  surname?: string;
}
export const SubscriberRequestName = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    given_name: S.optional(S.String),
    surname: S.optional(S.String),
  }),
).annotate({
  identifier: "SubscriberRequestName",
}) as any as S.Schema<SubscriberRequestName>;

/** The phone type. */
export type PhoneType = "FAX" | "HOME" | "MOBILE" | "OTHER" | "PAGER";
export const PhoneType = /*@__PURE__*/ S.String;

/** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
export interface PhoneWithTypePhoneNumber {
  /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  national_number: string;
}
export const PhoneWithTypePhoneNumber = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    national_number: S.String,
  }),
).annotate({
  identifier: "PhoneWithTypePhoneNumber",
}) as any as S.Schema<PhoneWithTypePhoneNumber>;

/** The phone information. */
export interface PhoneWithType {
  phone_type?: PhoneType | (string & {});
  /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
  phone_number: PhoneWithTypePhoneNumber;
}
export const PhoneWithType = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    phone_type: S.optional(PhoneType),
    phone_number: PhoneWithTypePhoneNumber,
  }),
).annotate({ identifier: "PhoneWithType" }) as any as S.Schema<PhoneWithType>;

/** The name of the party. */
export interface ShippingDetailName {
  /** When the party is a person, the party's full name. */
  full_name?: string;
}
export const ShippingDetailName = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    full_name: S.optional(S.String),
  }),
).annotate({
  identifier: "ShippingDetailName",
}) as any as S.Schema<ShippingDetailName>;

/** A classification for the method of purchase fulfillment (e.g shipping, in-store pickup, etc). Either `type` or `options` may be present, but not both. */
export type ShippingDetailType =
  | "SHIPPING"
  | "PICKUP_IN_PERSON"
  | "PICKUP_IN_STORE"
  | "PICKUP_FROM_PERSON";
export const ShippingDetailType = /*@__PURE__*/ S.String;

/** A classification for the method of purchase fulfillment. */
export type ShippingType =
  | "SHIPPING"
  | "PICKUP"
  | "PICKUP_IN_STORE"
  | "PICKUP_FROM_PERSON";
export const ShippingType = /*@__PURE__*/ S.String;

/** The options that the payee or merchant offers to the payer to ship or pick up their items. */
export interface ShippingOption {
  /** A unique ID that identifies a payer-selected shipping option. */
  id: string;
  /** A description that the payer sees, which helps them choose an appropriate shipping option. For example, `Free Shipping`, `USPS Priority Shipping`, `Expédition prioritaire USPS`, or `USPS yōuxiān fā huò`. Localize this description to the payer's locale. */
  label: string;
  /** A classification for the method of purchase fulfillment. */
  type?: ShippingType | (string & {});
  /** The shipping cost for the selected option. */
  amount?: Money;
  /** If the API request sets `selected = true`, it represents the shipping option that the payee or merchant expects to be pre-selected for the payer when they first view the `shipping.options` in the PayPal Checkout experience. As part of the response if a `shipping.option` contains `selected=true`, it represents the shipping option that the payer selected during the course of checkout with PayPal. Only one `shipping.option` can be set to `selected=true`. */
  selected: boolean;
}
export const ShippingOption = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    label: S.String,
    type: S.optional(ShippingType),
    amount: S.optional(Money),
    selected: S.Boolean,
  }),
).annotate({ identifier: "ShippingOption" }) as any as S.Schema<ShippingOption>;

/** An array of shipping options that the payee or merchant offers to the payer to ship or pick up their items. */
export type ShippingOptionList = Array<ShippingOption>;
export const ShippingOptionList = /*@__PURE__*/ S.Array(
  ShippingOption,
) as any as S.Schema<ShippingOptionList>;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export interface ShippingDetailAddress {
  /** The first line of the address, such as number and street, for example, `173 Drury Lane`. Needed for data entry, and Compliance and Risk checks. This field needs to pass the full address. */
  address_line_1?: string;
  /** The second line of the address, for example, a suite or apartment number. */
  address_line_2?: string;
  /** A city, town, or village. Smaller than `admin_area_level_1`. */
  admin_area_2?: string;
  /** The highest-level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. This data is formatted for postal delivery, for example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A *kanton*.</li></ul> */
  admin_area_1?: string;
  /** The postal code, which is the ZIP code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
  postal_code?: string;
  country_code: string;
}
export const ShippingDetailAddress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address_line_1: S.optional(S.String),
    address_line_2: S.optional(S.String),
    admin_area_2: S.optional(S.String),
    admin_area_1: S.optional(S.String),
    postal_code: S.optional(S.String),
    country_code: S.String,
  }),
).annotate({
  identifier: "ShippingDetailAddress",
}) as any as S.Schema<ShippingDetailAddress>;

/** The shipping details. */
export interface ShippingDetail {
  /** The name of the party. */
  name?: ShippingDetailName;
  /** A classification for the method of purchase fulfillment (e.g shipping, in-store pickup, etc). Either `type` or `options` may be present, but not both. */
  type?: ShippingDetailType | (string & {});
  options?: ShippingOptionList;
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  address?: ShippingDetailAddress;
}
export const ShippingDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(ShippingDetailName),
    type: S.optional(ShippingDetailType),
    options: S.optional(ShippingOptionList),
    address: S.optional(ShippingDetailAddress),
  }),
).annotate({ identifier: "ShippingDetail" }) as any as S.Schema<ShippingDetail>;

/** Type of card. i.e Credit, Debit and so on. */
export type CardType = "CREDIT" | "DEBIT" | "PREPAID" | "STORE" | "UNKNOWN";
export const CardType = /*@__PURE__*/ S.String;

/** The card network or brand. Applies to credit, debit, gift, and payment cards. */
export type CardBrand =
  | "VISA"
  | "MASTERCARD"
  | "DISCOVER"
  | "AMEX"
  | "SOLO"
  | "JCB"
  | "STAR"
  | "DELTA"
  | "SWITCH"
  | "MAESTRO"
  | "CB_NATIONALE"
  | "CONFIGOGA"
  | "CONFIDIS"
  | "ELECTRON"
  | "CETELEM"
  | "CHINA_UNION_PAY"
  | "DINERS"
  | "ELO"
  | "HIPER"
  | "HIPERCARD"
  | "RUPAY"
  | "GE"
  | "SYNCHRONY"
  | "UNKNOWN";
export const CardBrand = /*@__PURE__*/ S.String;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export type PaymentSourceCardBillingAddress = ShippingDetailAddress;
export const PaymentSourceCardBillingAddress = ShippingDetailAddress;

/** The details about a customer in PayPal's system of record. */
export interface Customer {
  id?: string;
  /** Email address of the buyer as provided to the merchant or on file with the merchant. Email Address is required if you are processing the transaction using PayPal Guest Processing which is offered to select partners and merchants. For all other use cases we do not expect partners/merchant to send email_address of their customer. */
  email_address?: string;
  /** The phone number of the buyer as provided to the merchant or on file with the merchant. The `phone.phone_number` supports only `national_number`. */
  phone?: PhoneWithType;
}
export const Customer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    email_address: S.optional(S.String),
    phone: S.optional(PhoneWithType),
  }),
).annotate({ identifier: "Customer" }) as any as S.Schema<Customer>;

/** Defines how and when the payment source gets vaulted. */
export type StoreInVaultInstruction = "ON_SUCCESS";
export const StoreInVaultInstruction = /*@__PURE__*/ S.String;

/** Basic vault instruction specification that can be extended by specific payment sources that supports vaulting. */
export interface VaultInstructionBase {
  store_in_vault?: StoreInVaultInstruction | (string & {});
}
export const VaultInstructionBase = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    store_in_vault: S.optional(StoreInVaultInstruction),
  }),
).annotate({
  identifier: "VaultInstructionBase",
}) as any as S.Schema<VaultInstructionBase>;

/** The method used for card verification. */
export type CardVerificationMethod =
  | "SCA_ALWAYS"
  | "SCA_WHEN_REQUIRED"
  | "3D_SECURE"
  | "AVS_CVV";
export const CardVerificationMethod = /*@__PURE__*/ S.String;

/** The API caller can opt in to verify the card through PayPal offered verification services (e.g. Smart Dollar Auth, 3DS). */
export interface CardVerification {
  /** The method used for card verification. */
  method?: CardVerificationMethod | (string & {});
}
export const CardVerification = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    method: S.optional(CardVerificationMethod),
  }),
).annotate({
  identifier: "CardVerification",
}) as any as S.Schema<CardVerification>;

/** Additional attributes associated with the use of this card. */
export interface CardAttributes {
  customer?: Customer;
  /** Instruction to vault the card based on the specified strategy. */
  vault?: VaultInstructionBase;
  /** Instruction to optionally verify the card based on the specified strategy. */
  verification?: CardVerification;
}
export const CardAttributes = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    customer: S.optional(Customer),
    vault: S.optional(VaultInstructionBase),
    verification: S.optional(CardVerification),
  }),
).annotate({ identifier: "CardAttributes" }) as any as S.Schema<CardAttributes>;

/** The payment card to use to fund a payment. Can be a credit or debit card. */
export interface PaymentSourceCard {
  /** The card holder's name as it appears on the card. */
  name?: string;
  /** The primary account number (PAN) for the payment card. */
  number?: string;
  /** The card expiration year and month, in [Internet date format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  expiry?: string;
  /** The three- or four-digit security code of the card. Also known as the CVV, CVC, CVN, CVE, or CID. This parameter cannot be present in the request when `payment_initiator=MERCHANT`. */
  security_code?: string;
  /** The payment card type. */
  type?: CardType | (string & {});
  /** The card brand or network. Typically used in the response. */
  brand?: CardBrand | (string & {});
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  billing_address?: ShippingDetailAddress;
  /** Additional attributes associated with the use of this card. */
  attributes?: CardAttributes;
}
export const PaymentSourceCard = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    number: S.optional(S.String),
    expiry: S.optional(S.String),
    security_code: S.optional(S.String),
    type: S.optional(CardType),
    brand: S.optional(CardBrand),
    billing_address: S.optional(ShippingDetailAddress),
    attributes: S.optional(CardAttributes),
  }),
).annotate({
  identifier: "PaymentSourceCard",
}) as any as S.Schema<PaymentSourceCard>;

/** The payment source definition. To be eligible to create subscription using debit or credit card, you will need to sign up here (https://www.paypal.com/bizsignup/entry/product/ppcp). Please note, its available only for non-3DS cards and for merchants in US and AU regions. */
export interface PaymentSource {
  /** The payment card to use to fund a payment. Can be a credit or debit card. */
  card?: PaymentSourceCard;
}
export const PaymentSource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    card: S.optional(PaymentSourceCard),
  }),
).annotate({ identifier: "PaymentSource" }) as any as S.Schema<PaymentSource>;

export interface SubscriberRequest {
  /** The email address of the payer. */
  email_address?: string;
  /** The name of the party. */
  name?: SubscriberRequestName;
  /** The phone number of the customer. Available only when you enable the **Contact Telephone Number** option in the <a href="https://www.paypal.com/cgi-bin/customerprofileweb?cmd=_profile-website-payments">**Profile & Settings**</a> for the merchant's PayPal account. The `phone.phone_number` supports only `national_number`. */
  phone?: PhoneWithType;
  shipping_address?: ShippingDetail;
  payment_source?: PaymentSource;
}
export const SubscriberRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email_address: S.optional(S.String),
    name: S.optional(SubscriberRequestName),
    phone: S.optional(PhoneWithType),
    shipping_address: S.optional(ShippingDetail),
    payment_source: S.optional(PaymentSource),
  }),
).annotate({
  identifier: "SubscriberRequest",
}) as any as S.Schema<SubscriberRequest>;

/** The location from which the shipping address is derived. */
export type ApplicationContextShippingPreference =
  | "GET_FROM_FILE"
  | "NO_SHIPPING"
  | "SET_PROVIDED_ADDRESS";
export const ApplicationContextShippingPreference = /*@__PURE__*/ S.String;

/** Configures the label name to `Continue` or `Subscribe Now` for subscription consent experience. */
export type ApplicationContextUserAction = "CONTINUE" | "SUBSCRIBE_NOW";
export const ApplicationContextUserAction = /*@__PURE__*/ S.String;

/** The merchant-preferred payment methods. */
export type PayeePaymentMethodPreference =
  | "UNRESTRICTED"
  | "IMMEDIATE_PAYMENT_REQUIRED";
export const PayeePaymentMethodPreference = /*@__PURE__*/ S.String;

/** The customer and merchant payment preferences. */
export interface ApplicationContextPaymentMethod {
  payee_preferred?: PayeePaymentMethodPreference | (string & {});
}
export const ApplicationContextPaymentMethod = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    payee_preferred: S.optional(PayeePaymentMethodPreference),
  }),
).annotate({
  identifier: "ApplicationContextPaymentMethod",
}) as any as S.Schema<ApplicationContextPaymentMethod>;

/** The application context, which customizes the payer experience during the subscription approval process with PayPal. */
export interface ApplicationContext {
  /** The label that overrides the business name in the PayPal account on the PayPal site. */
  brand_name?: string;
  /** The BCP 47-formatted locale of pages that the PayPal payment experience shows. PayPal supports a five-character code. For example, `da-DK`, `he-IL`, `id-ID`, `ja-JP`, `no-NO`, `pt-BR`, `ru-RU`, `sv-SE`, `th-TH`, `zh-CN`, `zh-HK`, or `zh-TW`. */
  locale?: string;
  /** The location from which the shipping address is derived. */
  shipping_preference?: ApplicationContextShippingPreference | (string & {});
  /** Configures the label name to `Continue` or `Subscribe Now` for subscription consent experience. */
  user_action?: ApplicationContextUserAction | (string & {});
  /** The customer and merchant payment preferences. */
  payment_method?: ApplicationContextPaymentMethod;
  /** The URL where the customer is redirected after the customer approves the payment. */
  return_url: string;
  /** The URL where the customer is redirected after the customer cancels the payment. */
  cancel_url: string;
}
export const ApplicationContext = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    brand_name: S.optional(S.String),
    locale: S.optional(S.String),
    shipping_preference: S.optional(ApplicationContextShippingPreference),
    user_action: S.optional(ApplicationContextUserAction),
    payment_method: S.optional(ApplicationContextPaymentMethod),
    return_url: S.String,
    cancel_url: S.String,
  }),
).annotate({
  identifier: "ApplicationContext",
}) as any as S.Schema<ApplicationContext>;

/** The billing cycle details to override at subscription level. The subscription billing cycle definition has to adhere to the plan billing cycle definition. */
export interface BillingCycleOverrideInput {
  /** The active pricing scheme for this billing cycle. A free trial billing cycle does not require a pricing scheme. */
  pricing_scheme?: PricingSchemeInput;
  /** The order in which this cycle is to run among other billing cycles. For example, a trial billing cycle has a `sequence` of `1` while a regular billing cycle has a `sequence` of `2`, so that trial cycle runs before the regular cycle. */
  sequence: number;
  /** The number of times this billing cycle gets executed. Trial billing cycles can only be executed a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). Regular billing cycles can be executed infinite times (value of <code>0</code> for <code>total_cycles</code>) or a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). */
  total_cycles?: number;
}
export const BillingCycleOverrideInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pricing_scheme: S.optional(PricingSchemeInput),
    sequence: S.Number,
    total_cycles: S.optional(S.Number),
  }),
).annotate({
  identifier: "BillingCycleOverrideInput",
}) as any as S.Schema<BillingCycleOverrideInput>;

/** An array of billing cycles for trial billing and regular billing. The subscription billing cycle definition has to adhere to the plan billing cycle definition. */
export type BillingCycleOverrideListInput = Array<BillingCycleOverrideInput>;
export const BillingCycleOverrideListInput = /*@__PURE__*/ S.Array(
  BillingCycleOverrideInput,
) as any as S.Schema<BillingCycleOverrideListInput>;

/** The action to take on the subscription if the initial payment for the setup fails. */
export type PaymentPreferencesOverrideSetupFeeFailureAction =
  | "CONTINUE"
  | "CANCEL";
export const PaymentPreferencesOverrideSetupFeeFailureAction =
  /*@__PURE__*/ S.String;

/** The payment preferences to override at subscription level. */
export interface PaymentPreferencesOverride {
  /** Indicates whether to automatically bill the outstanding amount in the next billing cycle. */
  auto_bill_outstanding?: boolean;
  /** The initial set-up fee for the service. */
  setup_fee?: Money;
  /** The action to take on the subscription if the initial payment for the setup fails. */
  setup_fee_failure_action?:
    | PaymentPreferencesOverrideSetupFeeFailureAction
    | (string & {});
  /** The maximum number of payment failures before a subscription is suspended. For example, if `payment_failure_threshold` is `2`, the subscription automatically updates to the `SUSPEND` state if two consecutive payments fail. */
  payment_failure_threshold?: number;
}
export const PaymentPreferencesOverride = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auto_bill_outstanding: S.optional(S.Boolean),
    setup_fee: S.optional(Money),
    setup_fee_failure_action: S.optional(
      PaymentPreferencesOverrideSetupFeeFailureAction,
    ),
    payment_failure_threshold: S.optional(S.Number),
  }),
).annotate({
  identifier: "PaymentPreferencesOverride",
}) as any as S.Schema<PaymentPreferencesOverride>;

/** The tax details. */
export interface TaxesOverride {
  /** The tax percentage on the billing amount. */
  percentage?: string;
  /** Indicates whether the tax was already included in the billing amount. */
  inclusive?: boolean;
}
export const TaxesOverride = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    percentage: S.optional(S.String),
    inclusive: S.optional(S.Boolean),
  }),
).annotate({ identifier: "TaxesOverride" }) as any as S.Schema<TaxesOverride>;

/** An inline plan object to customise the subscription. You can override plan level default attributes by providing customised values for the subscription in this object. */
export interface PlanOverrideInput {
  billing_cycles?: BillingCycleOverrideListInput;
  payment_preferences?: PaymentPreferencesOverride;
  taxes?: TaxesOverride;
}
export const PlanOverrideInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing_cycles: S.optional(BillingCycleOverrideListInput),
    payment_preferences: S.optional(PaymentPreferencesOverride),
    taxes: S.optional(TaxesOverride),
  }),
).annotate({
  identifier: "PlanOverrideInput",
}) as any as S.Schema<PlanOverrideInput>;

export interface SubscriptionsCreateRequest {
  /** The preferred server response upon successful completion of the request. Value is:<ul><li><code>return=minimal</code>. The server returns a minimal response to optimize communication between the API caller and the server. A minimal response includes the <code>id</code>, <code>status</code> and HATEOAS links.</li><li><code>return=representation</code>. The server returns a complete resource representation, including the current state of the resource.</li></ul> */
  prefer?: string;
  /** The server stores keys for 72 hours. */
  payPalRequestId?: string;
  /** The ID of the plan. */
  plan_id: string;
  /** The date and time when the subscription started, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  start_time?: string;
  /** The quantity of the product in the subscription. */
  quantity?: string;
  /** The shipping charges. */
  shipping_amount?: Money;
  subscriber?: SubscriberRequest;
  /** DEPRECATED. Indicates whether the subscription auto-renews after the billing cycles complete. */
  auto_renewal?: boolean;
  application_context?: ApplicationContext;
  /** The custom id for the subscription. Can be invoice id. */
  custom_id?: string;
  /** An inline plan object to customise the subscription. You can override plan level default attributes by providing customised values for the subscription in this object. */
  plan?: PlanOverrideInput;
  /** Merchant's inventory identifier details for the WPS hosted button. */
  merchant_inventory?: unknown;
}
export const SubscriptionsCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    prefer: S.optional(S.String.pipe(T.Header("Prefer"))),
    payPalRequestId: S.optional(S.String.pipe(T.Header("PayPal-Request-Id"))),
    plan_id: S.String,
    start_time: S.optional(S.String),
    quantity: S.optional(S.String),
    shipping_amount: S.optional(Money),
    subscriber: S.optional(SubscriberRequest),
    auto_renewal: S.optional(S.Boolean),
    application_context: S.optional(ApplicationContext),
    custom_id: S.optional(S.String),
    plan: S.optional(PlanOverrideInput),
    merchant_inventory: S.optional(S.Unknown),
  }).pipe(
    T.Http({ method: "POST", uri: "/v1/billing/subscriptions", code: 200 }),
  ),
).annotate({
  identifier: "SubscriptionsCreateRequest",
}) as any as S.Schema<SubscriptionsCreateRequest>;

/** The status of the subscription. */
export type SubscriptionsCreateResponseStatus =
  | "APPROVAL_PENDING"
  | "APPROVED"
  | "ACTIVE"
  | "SUSPENDED"
  | "CANCELLED"
  | "EXPIRED";
export const SubscriptionsCreateResponseStatus = /*@__PURE__*/ S.String;

/** The name of the party. */
export type SubscriberName = SubscriberRequestName;
export const SubscriberName = SubscriberRequestName;

/** Array of brands or networks associated with the card. */
export type CardBrandList = Array<CardBrand>;
export const CardBrandList = /*@__PURE__*/ S.Array(
  CardBrand,
) as any as S.Schema<CardBrandList>;

/** Liability shift indicator. The outcome of the issuer's authentication. */
export type LiabilityShift = "NO" | "POSSIBLE" | "UNKNOWN";
export const LiabilityShift = /*@__PURE__*/ S.String;

/** Transactions status result identifier. The outcome of the issuer's authentication. */
export type ParesStatus = "Y" | "N" | "U" | "A" | "C" | "R" | "D" | "I";
export const ParesStatus = /*@__PURE__*/ S.String;

/** Status of Authentication eligibility. */
export type Enrolled = "Y" | "N" | "U" | "B";
export const Enrolled = /*@__PURE__*/ S.String;

/** Results of 3D Secure Authentication. */
export interface ThreeDSecureAuthenticationResponse {
  /** The outcome of the issuer's authentication. */
  authentication_status?: ParesStatus;
  /** Status of authentication eligibility. */
  enrollment_status?: Enrolled;
}
export const ThreeDSecureAuthenticationResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    authentication_status: S.optional(ParesStatus),
    enrollment_status: S.optional(Enrolled),
  }),
).annotate({
  identifier: "ThreeDSecureAuthenticationResponse",
}) as any as S.Schema<ThreeDSecureAuthenticationResponse>;

/** Results of Authentication such as 3D Secure. */
export interface AuthenticationResponse {
  liability_shift?: LiabilityShift;
  three_d_secure?: ThreeDSecureAuthenticationResponse;
}
export const AuthenticationResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    liability_shift: S.optional(LiabilityShift),
    three_d_secure: S.optional(ThreeDSecureAuthenticationResponse),
  }),
).annotate({
  identifier: "AuthenticationResponse",
}) as any as S.Schema<AuthenticationResponse>;

/** The vault status. */
export type VaultResponseStatus = "VAULTED" | "CREATED" | "APPROVED";
export const VaultResponseStatus = /*@__PURE__*/ S.String;

/** The details about a customer in PayPal's system of record. */
export interface VaultResponseCustomer {
  id?: string;
}
export const VaultResponseCustomer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
  }),
).annotate({
  identifier: "VaultResponseCustomer",
}) as any as S.Schema<VaultResponseCustomer>;

/** An array of request-related HATEOAS links. */
export type DefinitionsLinkDescriptionList = Array<LinkDescription>;
export const DefinitionsLinkDescriptionList = /*@__PURE__*/ S.Array(
  LinkDescription,
) as any as S.Schema<DefinitionsLinkDescriptionList>;

/** The details about a saved payment source. */
export interface VaultResponse {
  /** The PayPal-generated ID for the saved payment source. */
  id?: string;
  /** The vault status. */
  status?: VaultResponseStatus;
  /** The details about a customer in PayPal's system of record. */
  customer?: VaultResponseCustomer;
  links?: DefinitionsLinkDescriptionList;
}
export const VaultResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    status: S.optional(VaultResponseStatus),
    customer: S.optional(VaultResponseCustomer),
    links: S.optional(DefinitionsLinkDescriptionList),
  }),
).annotate({ identifier: "VaultResponse" }) as any as S.Schema<VaultResponse>;

/** Additional attributes associated with the use of this card. */
export interface CardAttributesResponse {
  vault?: VaultResponse;
}
export const CardAttributesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vault: S.optional(VaultResponse),
  }),
).annotate({
  identifier: "CardAttributesResponse",
}) as any as S.Schema<CardAttributesResponse>;

/** Representation of card details as received in the request. */
export interface CardFromRequest {
  /** The card expiration year and month, in [Internet date format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  expiry?: string;
  /** The last digits of the payment card. */
  last_digits?: string;
}
export const CardFromRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    expiry: S.optional(S.String),
    last_digits: S.optional(S.String),
  }),
).annotate({
  identifier: "CardFromRequest",
}) as any as S.Schema<CardFromRequest>;

/** The type of card product assigned to the BIN by the issuer. These values are defined by the issuer and may change over time. Some examples include: PREPAID_GIFT, CONSUMER, CORPORATE. */
export type ProductsList = Array<string>;
export const ProductsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ProductsList>;

/** Bank Identification Number (BIN) details used to fund a payment. */
export interface BinDetails {
  /** The Bank Identification Number (BIN) signifies the number that is being used to identify the granular level details (except the PII information) of the card. */
  bin?: string;
  /** The issuer of the card instrument. */
  issuing_bank?: string;
  /** The [two-character ISO-3166-1 country code](/docs/integration/direct/rest/country-codes/) of the bank. */
  bin_country_code?: string;
  products?: ProductsList;
}
export const BinDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bin: S.optional(S.String),
    issuing_bank: S.optional(S.String),
    bin_country_code: S.optional(S.String),
    products: S.optional(ProductsList),
  }),
).annotate({ identifier: "BinDetails" }) as any as S.Schema<BinDetails>;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export type CardResponseWithBillingAddressBillingAddress =
  ShippingDetailAddress;
export const CardResponseWithBillingAddressBillingAddress =
  ShippingDetailAddress;

export interface CardResponseWithBillingAddress {
  /** The card holder's name as it appears on the card. */
  name?: string;
  /** The last digits of the payment card. */
  last_digits?: string;
  /** The card brand or network. Typically used in the response. */
  brand?: CardBrand;
  available_networks?: CardBrandList;
  /** The payment card type. */
  type?: CardType;
  authentication_result?: AuthenticationResponse;
  attributes?: CardAttributesResponse;
  from_request?: CardFromRequest;
  /** The card expiration year and month, in [Internet date format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  expiry?: string;
  /** Bank Identification Number (BIN) details used to fund a payment. */
  bin_details?: BinDetails;
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  billing_address?: ShippingDetailAddress;
  /** Currency code of the given instrument */
  currency_code?: string;
}
export const CardResponseWithBillingAddress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    last_digits: S.optional(S.String),
    brand: S.optional(CardBrand),
    available_networks: S.optional(CardBrandList),
    type: S.optional(CardType),
    authentication_result: S.optional(AuthenticationResponse),
    attributes: S.optional(CardAttributesResponse),
    from_request: S.optional(CardFromRequest),
    expiry: S.optional(S.String),
    bin_details: S.optional(BinDetails),
    billing_address: S.optional(ShippingDetailAddress),
    currency_code: S.optional(S.String),
  }),
).annotate({
  identifier: "CardResponseWithBillingAddress",
}) as any as S.Schema<CardResponseWithBillingAddress>;

/** The payment source used to fund the payment. */
export interface PaymentSourceResponse {
  card?: CardResponseWithBillingAddress;
}
export const PaymentSourceResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    card: S.optional(CardResponseWithBillingAddress),
  }),
).annotate({
  identifier: "PaymentSourceResponse",
}) as any as S.Schema<PaymentSourceResponse>;

export interface Subscriber {
  /** The email address of the payer. */
  email_address?: string;
  /** The PayPal-assigned ID for the payer. */
  payer_id?: string;
  /** The name of the party. */
  name?: SubscriberRequestName;
  /** The phone number of the customer. Available only when you enable the **Contact Telephone Number** option in the <a href="https://www.paypal.com/cgi-bin/customerprofileweb?cmd=_profile-website-payments">**Profile & Settings**</a> for the merchant's PayPal account. The `phone.phone_number` supports only `national_number`. */
  phone?: PhoneWithType;
  shipping_address?: ShippingDetail;
  payment_source?: PaymentSourceResponse;
}
export const Subscriber = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email_address: S.optional(S.String),
    payer_id: S.optional(S.String),
    name: S.optional(SubscriberRequestName),
    phone: S.optional(PhoneWithType),
    shipping_address: S.optional(ShippingDetail),
    payment_source: S.optional(PaymentSourceResponse),
  }),
).annotate({ identifier: "Subscriber" }) as any as S.Schema<Subscriber>;

/** The type of the billing cycle. */
export type CycleExecutionTenureType = "REGULAR" | "TRIAL";
export const CycleExecutionTenureType = /*@__PURE__*/ S.String;

/** The regular and trial execution details for a billing cycle. */
export interface CycleExecution {
  /** The type of the billing cycle. */
  tenure_type: CycleExecutionTenureType;
  /** The order in which to run this cycle among other billing cycles. */
  sequence: number;
  /** The number of billing cycles that have completed. */
  cycles_completed: number;
  /** For a finite billing cycle, cycles_remaining is the number of remaining cycles. For an infinite billing cycle, cycles_remaining is set as 0. */
  cycles_remaining?: number;
  /** The active pricing scheme version for the billing cycle. */
  current_pricing_scheme_version?: number;
  /** The number of times this billing cycle gets executed. Trial billing cycles can only be executed a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). Regular billing cycles can be executed infinite times (value of <code>0</code> for <code>total_cycles</code>) or a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). */
  total_cycles?: number;
}
export const CycleExecution = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tenure_type: CycleExecutionTenureType,
    sequence: S.Number,
    cycles_completed: S.Number,
    cycles_remaining: S.optional(S.Number),
    current_pricing_scheme_version: S.optional(S.Number),
    total_cycles: S.optional(S.Number),
  }),
).annotate({ identifier: "CycleExecution" }) as any as S.Schema<CycleExecution>;

/** The trial and regular billing executions. */
export type CycleExecutionList = Array<CycleExecution>;
export const CycleExecutionList = /*@__PURE__*/ S.Array(
  CycleExecution,
) as any as S.Schema<CycleExecutionList>;

export interface LastPaymentDetails {
  /** The last payment amount. */
  amount?: Money;
  /** The date and time when the last payment was made, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  time?: string;
}
export const LastPaymentDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.optional(Money),
    time: S.optional(S.String),
  }),
).annotate({
  identifier: "LastPaymentDetails",
}) as any as S.Schema<LastPaymentDetails>;

/** The reason code for the payment failure. */
export type FailedPaymentDetailsReasonCode =
  | "PAYMENT_DENIED"
  | "INTERNAL_SERVER_ERROR"
  | "PAYEE_ACCOUNT_RESTRICTED"
  | "PAYER_ACCOUNT_RESTRICTED"
  | "PAYER_CANNOT_PAY"
  | "SENDING_LIMIT_EXCEEDED"
  | "TRANSACTION_RECEIVING_LIMIT_EXCEEDED"
  | "CURRENCY_MISMATCH";
export const FailedPaymentDetailsReasonCode = /*@__PURE__*/ S.String;

/** The details for the failed payment of the subscription. */
export interface FailedPaymentDetails {
  /** The failed payment amount. */
  amount: Money;
  /** The date and time when the failed payment was made, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  time: string;
  /** The reason code for the payment failure. */
  reason_code?: FailedPaymentDetailsReasonCode;
  /** The time when the retry attempt for the failed payment occurs, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  next_payment_retry_time?: string;
}
export const FailedPaymentDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: Money,
    time: S.String,
    reason_code: S.optional(FailedPaymentDetailsReasonCode),
    next_payment_retry_time: S.optional(S.String),
  }),
).annotate({
  identifier: "FailedPaymentDetails",
}) as any as S.Schema<FailedPaymentDetails>;

/** The billing details for the subscription. If the subscription was or is active, these fields are populated. */
export interface SubscriptionBillingInfo {
  /** The total pending bill amount, to be paid by the subscriber. */
  outstanding_balance: Money;
  cycle_executions?: CycleExecutionList;
  /** The details for the last payment of the subscription. */
  last_payment?: LastPaymentDetails;
  /** The next date and time for billing this subscription, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  next_billing_time?: string;
  /** The date and time when the final billing cycle occurs, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  final_payment_time?: string;
  /** The number of consecutive payment failures. Resets to `0` after a successful payment. If this reaches the `payment_failure_threshold` value, the subscription updates to the `SUSPENDED` state. */
  failed_payments_count: number;
  /** The details for the last failed payment of the subscription. */
  last_failed_payment?: FailedPaymentDetails;
}
export const SubscriptionBillingInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    outstanding_balance: Money,
    cycle_executions: S.optional(CycleExecutionList),
    last_payment: S.optional(LastPaymentDetails),
    next_billing_time: S.optional(S.String),
    final_payment_time: S.optional(S.String),
    failed_payments_count: S.Number,
    last_failed_payment: S.optional(FailedPaymentDetails),
  }),
).annotate({
  identifier: "SubscriptionBillingInfo",
}) as any as S.Schema<SubscriptionBillingInfo>;

/** The plan details. */
export interface SubscriptionsCreateResponsePlan {
  /** The ID for the product. */
  product_id?: string;
  /** The plan name. */
  name?: string;
  /** The detailed description of the plan. */
  description?: string;
  billing_cycles?: BillingCycleList;
  payment_preferences?: PaymentPreferences;
  taxes?: Taxes;
  /** Indicates whether you can subscribe to this plan by providing a quantity for the goods or service. */
  quantity_supported?: boolean;
}
export const SubscriptionsCreateResponsePlan = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    product_id: S.optional(S.String),
    name: S.optional(S.String),
    description: S.optional(S.String),
    billing_cycles: S.optional(BillingCycleList),
    payment_preferences: S.optional(PaymentPreferences),
    taxes: S.optional(Taxes),
    quantity_supported: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "SubscriptionsCreateResponsePlan",
}) as any as S.Schema<SubscriptionsCreateResponsePlan>;

export interface SubscriptionsCreateResponse {
  /** The status of the subscription. */
  status?: SubscriptionsCreateResponseStatus;
  /** The reason or notes for the status of the subscription. */
  status_change_note?: string;
  status_update_time?: string;
  /** The PayPal-generated ID for the subscription. */
  id?: string;
  /** The ID of the plan. */
  plan_id?: string;
  start_time?: string;
  /** The quantity of the product in the subscription. */
  quantity?: string;
  shipping_amount?: Money;
  subscriber?: Subscriber;
  billing_info?: SubscriptionBillingInfo;
  create_time?: string;
  update_time?: string;
  /** The custom id for the subscription. Can be invoice id. */
  custom_id?: string;
  /** Indicates whether the subscription has overridden any plan attributes. */
  plan_overridden?: boolean;
  /** The plan details. */
  plan?: SubscriptionsCreateResponsePlan;
  links?: LinkDescriptionList;
}
export const SubscriptionsCreateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    status: S.optional(SubscriptionsCreateResponseStatus),
    status_change_note: S.optional(S.String),
    status_update_time: S.optional(S.String),
    id: S.optional(S.String),
    plan_id: S.optional(S.String),
    start_time: S.optional(S.String),
    quantity: S.optional(S.String),
    shipping_amount: S.optional(Money),
    subscriber: S.optional(Subscriber),
    billing_info: S.optional(SubscriptionBillingInfo),
    create_time: S.optional(S.String),
    update_time: S.optional(S.String),
    custom_id: S.optional(S.String),
    plan_overridden: S.optional(S.Boolean),
    plan: S.optional(SubscriptionsCreateResponsePlan),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({
  identifier: "SubscriptionsCreateResponse",
}) as any as S.Schema<SubscriptionsCreateResponse>;

export interface SubscriptionsGetRequest {
  /** The ID of the subscription. */
  id: string;
  /** List of fields that are to be returned in the response. Possible value for fields are last_failed_payment and plan. */
  fields?: string;
}
export const SubscriptionsGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    fields: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/v1/billing/subscriptions/{id}", code: 200 }),
  ),
).annotate({
  identifier: "SubscriptionsGetRequest",
}) as any as S.Schema<SubscriptionsGetRequest>;

/** The status of the subscription. */
export type SubscriptionsGetResponseStatus =
  | "APPROVAL_PENDING"
  | "APPROVED"
  | "ACTIVE"
  | "SUSPENDED"
  | "CANCELLED"
  | "EXPIRED";
export const SubscriptionsGetResponseStatus = /*@__PURE__*/ S.String;

/** The plan details. */
export type SubscriptionsGetResponsePlan = SubscriptionsCreateResponsePlan;
export const SubscriptionsGetResponsePlan = SubscriptionsCreateResponsePlan;

export interface SubscriptionsGetResponse {
  /** The status of the subscription. */
  status?: SubscriptionsGetResponseStatus;
  /** The reason or notes for the status of the subscription. */
  status_change_note?: string;
  status_update_time?: string;
  /** The PayPal-generated ID for the subscription. */
  id?: string;
  /** The ID of the plan. */
  plan_id?: string;
  start_time?: string;
  /** The quantity of the product in the subscription. */
  quantity?: string;
  shipping_amount?: Money;
  subscriber?: Subscriber;
  billing_info?: SubscriptionBillingInfo;
  create_time?: string;
  update_time?: string;
  /** The custom id for the subscription. Can be invoice id. */
  custom_id?: string;
  /** Indicates whether the subscription has overridden any plan attributes. */
  plan_overridden?: boolean;
  /** The plan details. */
  plan?: SubscriptionsCreateResponsePlan;
  links?: LinkDescriptionList;
}
export const SubscriptionsGetResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    status: S.optional(SubscriptionsGetResponseStatus),
    status_change_note: S.optional(S.String),
    status_update_time: S.optional(S.String),
    id: S.optional(S.String),
    plan_id: S.optional(S.String),
    start_time: S.optional(S.String),
    quantity: S.optional(S.String),
    shipping_amount: S.optional(Money),
    subscriber: S.optional(Subscriber),
    billing_info: S.optional(SubscriptionBillingInfo),
    create_time: S.optional(S.String),
    update_time: S.optional(S.String),
    custom_id: S.optional(S.String),
    plan_overridden: S.optional(S.Boolean),
    plan: S.optional(SubscriptionsCreateResponsePlan),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({
  identifier: "SubscriptionsGetResponse",
}) as any as S.Schema<SubscriptionsGetResponse>;

export interface SubscriptionsPatchRequest {
  /** The ID of the subscription. */
  id: string;
  body?: PatchRequest;
}
export const SubscriptionsPatchRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    body: S.optional(PatchRequest.pipe(T.HttpBody())),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/v1/billing/subscriptions/{id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SubscriptionsPatchRequest",
}) as any as S.Schema<SubscriptionsPatchRequest>;

export interface SubscriptionsPatchResponse {}
export const SubscriptionsPatchResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "SubscriptionsPatchResponse",
}) as any as S.Schema<SubscriptionsPatchResponse>;

/** The location from which the shipping address is derived. */
export type SubscriptionsReviseRequestApplicationContextShippingPreference =
  | "GET_FROM_FILE"
  | "NO_SHIPPING"
  | "SET_PROVIDED_ADDRESS";
export const SubscriptionsReviseRequestApplicationContextShippingPreference =
  /*@__PURE__*/ S.String;

/** The customer and merchant payment preferences. */
export type SubscriptionsReviseRequestApplicationContextPaymentMethod =
  ApplicationContextPaymentMethod;
export const SubscriptionsReviseRequestApplicationContextPaymentMethod =
  ApplicationContextPaymentMethod;

/** The application context, which customizes the payer experience during the subscription approval process with PayPal. */
export interface SubscriptionsReviseRequestApplicationContext {
  /** The label that overrides the business name in the PayPal account on the PayPal site. */
  brand_name?: string;
  /** The BCP 47-formatted locale of pages that the PayPal payment experience shows. PayPal supports a five-character code. For example, `da-DK`, `he-IL`, `id-ID`, `ja-JP`, `no-NO`, `pt-BR`, `ru-RU`, `sv-SE`, `th-TH`, `zh-CN`, `zh-HK`, or `zh-TW`. */
  locale?: string;
  /** The location from which the shipping address is derived. */
  shipping_preference?:
    | SubscriptionsReviseRequestApplicationContextShippingPreference
    | (string & {});
  /** The customer and merchant payment preferences. */
  payment_method?: ApplicationContextPaymentMethod;
  /** The URL where the customer is redirected after the customer approves the payment. */
  return_url: string;
  /** The URL where the customer is redirected after the customer cancels the payment. */
  cancel_url: string;
}
export const SubscriptionsReviseRequestApplicationContext =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      brand_name: S.optional(S.String),
      locale: S.optional(S.String),
      shipping_preference: S.optional(
        SubscriptionsReviseRequestApplicationContextShippingPreference,
      ),
      payment_method: S.optional(ApplicationContextPaymentMethod),
      return_url: S.String,
      cancel_url: S.String,
    }),
  ).annotate({
    identifier: "SubscriptionsReviseRequestApplicationContext",
  }) as any as S.Schema<SubscriptionsReviseRequestApplicationContext>;

export interface SubscriptionsReviseRequest {
  /** The ID of the subscription. */
  id: string;
  /** The unique PayPal-generated ID for the plan. */
  plan_id?: string;
  /** The quantity of the product or service in the subscription. */
  quantity?: string;
  /** The shipping charges. */
  shipping_amount?: Money;
  /** The shipping address of the subscriber. */
  shipping_address?: ShippingDetail;
  /** The application context, which customizes the payer experience during the subscription approval process with PayPal. */
  application_context?: SubscriptionsReviseRequestApplicationContext;
  /** An inline plan object to customise the subscription. You can override plan level default attributes by providing customised values for the subscription in this object. Any existing overrides will not be carried forward during subscription revise. */
  plan?: PlanOverrideInput;
}
export const SubscriptionsReviseRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    plan_id: S.optional(S.String),
    quantity: S.optional(S.String),
    shipping_amount: S.optional(Money),
    shipping_address: S.optional(ShippingDetail),
    application_context: S.optional(
      SubscriptionsReviseRequestApplicationContext,
    ),
    plan: S.optional(PlanOverrideInput),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v1/billing/subscriptions/{id}/revise",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SubscriptionsReviseRequest",
}) as any as S.Schema<SubscriptionsReviseRequest>;

/** The billing cycle details to override at subscription level. The subscription billing cycle definition has to adhere to the plan billing cycle definition. */
export interface BillingCycleOverride {
  /** The active pricing scheme for this billing cycle. A free trial billing cycle does not require a pricing scheme. */
  pricing_scheme?: PricingScheme;
  /** The order in which this cycle is to run among other billing cycles. For example, a trial billing cycle has a `sequence` of `1` while a regular billing cycle has a `sequence` of `2`, so that trial cycle runs before the regular cycle. */
  sequence: number;
  /** The number of times this billing cycle gets executed. Trial billing cycles can only be executed a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). Regular billing cycles can be executed infinite times (value of <code>0</code> for <code>total_cycles</code>) or a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). */
  total_cycles?: number;
}
export const BillingCycleOverride = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pricing_scheme: S.optional(PricingScheme),
    sequence: S.Number,
    total_cycles: S.optional(S.Number),
  }),
).annotate({
  identifier: "BillingCycleOverride",
}) as any as S.Schema<BillingCycleOverride>;

/** An array of billing cycles for trial billing and regular billing. The subscription billing cycle definition has to adhere to the plan billing cycle definition. */
export type BillingCycleOverrideList = Array<BillingCycleOverride>;
export const BillingCycleOverrideList = /*@__PURE__*/ S.Array(
  BillingCycleOverride,
) as any as S.Schema<BillingCycleOverrideList>;

/** An inline plan object to customise the subscription. You can override plan level default attributes by providing customised values for the subscription in this object. */
export interface PlanOverride {
  billing_cycles?: BillingCycleOverrideList;
  payment_preferences?: PaymentPreferencesOverride;
  taxes?: TaxesOverride;
}
export const PlanOverride = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing_cycles: S.optional(BillingCycleOverrideList),
    payment_preferences: S.optional(PaymentPreferencesOverride),
    taxes: S.optional(TaxesOverride),
  }),
).annotate({ identifier: "PlanOverride" }) as any as S.Schema<PlanOverride>;

/** An array of request-related [HATEOAS links](/docs/api/reference/api-responses/#hateoas-links). */
export type SubscriptionReviseResponseDefinitionsLinkDescriptionList =
  Array<LinkDescription>;
export const SubscriptionReviseResponseDefinitionsLinkDescriptionList =
  /*@__PURE__*/ S.Array(
    LinkDescription,
  ) as any as S.Schema<SubscriptionReviseResponseDefinitionsLinkDescriptionList>;

/** The response to a request to update the quantity of the product or service in a subscription. You can also use this method to switch the plan and update the `shipping_amount` and `shipping_address` values for the subscription. This type of update requires the buyer's consent. */
export interface SubscriptionReviseResponse {
  /** The unique PayPal-generated ID for the plan. */
  plan_id?: string;
  /** The quantity of the product or service in the subscription. */
  quantity?: string;
  /** The shipping charges. */
  shipping_amount?: Money;
  /** The shipping address of the subscriber. */
  shipping_address?: ShippingDetail;
  /** An inline plan object to customise the subscription. You can override plan level default attributes by providing customised values for the subscription in this object. Any existing overrides will not be carried forward during subscription revise. */
  plan?: PlanOverride;
  /** Indicates whether the subscription has overridden any plan attributes. */
  plan_overridden?: boolean;
  links?: SubscriptionReviseResponseDefinitionsLinkDescriptionList;
}
export const SubscriptionReviseResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    plan_id: S.optional(S.String),
    quantity: S.optional(S.String),
    shipping_amount: S.optional(Money),
    shipping_address: S.optional(ShippingDetail),
    plan: S.optional(PlanOverride),
    plan_overridden: S.optional(S.Boolean),
    links: S.optional(SubscriptionReviseResponseDefinitionsLinkDescriptionList),
  }),
).annotate({
  identifier: "SubscriptionReviseResponse",
}) as any as S.Schema<SubscriptionReviseResponse>;

export interface SubscriptionsSuspendRequest {
  /** The ID of the subscription. */
  id: string;
  /** The reason for suspension of the Subscription. */
  reason: string;
}
export const SubscriptionsSuspendRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    reason: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v1/billing/subscriptions/{id}/suspend",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SubscriptionsSuspendRequest",
}) as any as S.Schema<SubscriptionsSuspendRequest>;

export interface SubscriptionsSuspendResponse {}
export const SubscriptionsSuspendResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "SubscriptionsSuspendResponse",
}) as any as S.Schema<SubscriptionsSuspendResponse>;

export interface SubscriptionsTransactionsRequest {
  /** The ID of the subscription. */
  id: string;
  /** The start time of the range of transactions to list. */
  start_time: string;
  /** The end time of the range of transactions to list. */
  end_time: string;
}
export const SubscriptionsTransactionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    start_time: S.String.pipe(T.Query()),
    end_time: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v1/billing/subscriptions/{id}/transactions",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SubscriptionsTransactionsRequest",
}) as any as S.Schema<SubscriptionsTransactionsRequest>;

/** An array of transactions. */
export type TransactionList = Array<Transaction>;
export const TransactionList = /*@__PURE__*/ S.Array(
  Transaction,
) as any as S.Schema<TransactionList>;

/** The list transactions for a subscription request details. */
export interface TransactionsList {
  transactions?: TransactionList;
  /** The total number of items. */
  total_items?: number;
  /** The total number of pages. */
  total_pages?: number;
  links?: LinkDescriptionList;
}
export const TransactionsList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    transactions: S.optional(TransactionList),
    total_items: S.optional(S.Number),
    total_pages: S.optional(S.Number),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({
  identifier: "TransactionsList",
}) as any as S.Schema<TransactionsList>;

export type PlansActivateError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Activate plan Activates a plan, by ID. */
export const plansActivate: API.OperationMethod<
  PlansActivateRequest,
  PlansActivateResponse,
  PlansActivateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PlansActivateRequest,
  output: PlansActivateResponse,
  errors: [Forbidden, NotFound, UnprocessableEntity, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type PlansCreateError =
  | BadRequest
  | Forbidden
  | UnprocessableEntity
  | PaypalOpError;
/** Create plan Creates a plan that defines pricing and billing cycle details for subscriptions. */
export const plansCreate: API.OperationMethod<
  PlansCreateRequest,
  Plan,
  PlansCreateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PlansCreateRequest,
  output: Plan,
  errors: [BadRequest, Forbidden, UnprocessableEntity, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type PlansDeactivateError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Deactivate plan Deactivates a plan, by ID. */
export const plansDeactivate: API.OperationMethod<
  PlansDeactivateRequest,
  PlansDeactivateResponse,
  PlansDeactivateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PlansDeactivateRequest,
  output: PlansDeactivateResponse,
  errors: [Forbidden, NotFound, UnprocessableEntity, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type PlansGetError = Forbidden | NotFound | PaypalOpError;
/** Show plan details Shows details for a plan, by ID. */
export const plansGet: API.OperationMethod<
  PlansGetRequest,
  Plan,
  PlansGetError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PlansGetRequest,
  output: Plan,
  errors: [Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type PlansListError = BadRequest | Forbidden | NotFound | PaypalOpError;
/** List plans Lists billing plans. */
export const plansList: API.OperationMethod<
  PlansListRequest,
  PlanCollection,
  PlansListError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PlansListRequest,
  output: PlanCollection,
  errors: [BadRequest, Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type PlansPatchError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Update plan Updates a plan with the `CREATED` or `ACTIVE` status. For an `INACTIVE` plan, you can make only status updates.<br/>You can patch these attributes and objects:<table><thead><tr><th>Attribute or object</th><th>Operations</th></tr></thead><tbody><tr><td><code>description</code></td><td>replace</td></tr><tr><td><code>payment_preferences.auto_bill_outstanding</code></td><td>replace</td></tr><tr><td><code>taxes.percentage</code></td><td>replace</td></tr><tr><td><code>payment_preferences.payment_failure_threshold</code></td><td>replace</td></tr><tr><td><code>payment_preferences.setup_fee</code></td><td>replace</td></tr><tr><td><code>payment_preferences.setup_fee_failure_action</code></td><td>replace</td></tr><tr><td><code>name</code></td><td>replace</td></tr></tbody></table> */
export const plansPatch: API.OperationMethod<
  PlansPatchRequest,
  PlansPatchResponse,
  PlansPatchError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PlansPatchRequest,
  output: PlansPatchResponse,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type PlansUpdatePricingSchemesError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Update pricing Updates pricing for a plan. For example, you can update a regular billing cycle from $5 per month to $7 per month. */
export const plansUpdatePricingSchemes: API.OperationMethod<
  PlansUpdatePricingSchemesRequest,
  PlansUpdatePricingSchemesResponse,
  PlansUpdatePricingSchemesError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PlansUpdatePricingSchemesRequest,
  output: PlansUpdatePricingSchemesResponse,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type SubscriptionsActivateError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Activate subscription Activates the subscription. */
export const subscriptionsActivate: API.OperationMethod<
  SubscriptionsActivateRequest,
  SubscriptionsActivateResponse,
  SubscriptionsActivateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubscriptionsActivateRequest,
  output: SubscriptionsActivateResponse,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type SubscriptionsCancelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Cancel subscription Cancels the subscription. */
export const subscriptionsCancel: API.OperationMethod<
  SubscriptionsCancelRequest,
  SubscriptionsCancelResponse,
  SubscriptionsCancelError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubscriptionsCancelRequest,
  output: SubscriptionsCancelResponse,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type SubscriptionsCaptureError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Capture authorized payment on subscription Captures an authorized payment from the subscriber on the subscription. */
export const subscriptionsCapture: API.OperationMethod<
  SubscriptionsCaptureRequest,
  Transaction,
  SubscriptionsCaptureError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubscriptionsCaptureRequest,
  output: Transaction,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type SubscriptionsCreateError =
  | BadRequest
  | Forbidden
  | UnprocessableEntity
  | PaypalOpError;
/** Create subscription Creates a subscription. */
export const subscriptionsCreate: API.OperationMethod<
  SubscriptionsCreateRequest,
  SubscriptionsCreateResponse,
  SubscriptionsCreateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubscriptionsCreateRequest,
  output: SubscriptionsCreateResponse,
  errors: [BadRequest, Forbidden, UnprocessableEntity, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type SubscriptionsGetError = Forbidden | NotFound | PaypalOpError;
/** Show subscription details Shows details for a subscription, by ID. */
export const subscriptionsGet: API.OperationMethod<
  SubscriptionsGetRequest,
  SubscriptionsGetResponse,
  SubscriptionsGetError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubscriptionsGetRequest,
  output: SubscriptionsGetResponse,
  errors: [Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type SubscriptionsPatchError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Update subscription Updates a subscription which could be in <code>ACTIVE</code> or <code>SUSPENDED</code> status. You can override plan level default attributes by providing customised values for plan path in the patch request.<br /> <ul> <li>You cannot update attributes that have already completed (Example - trial cycles can’t be updated if completed).</li> <li>Once overridden, changes to plan resource will not impact subscription.</li> <li>Any price update will not impact billing cycles within next 10 days (Applicable only for subscriptions funded by PayPal account).</li> </ul> Following are the fields eligible for patch.<table><thead><tr><th>Attribute or object</th><th>Operations</th></tr></thead><tbody><tr><td><code>billing_info.outstanding_balance</code></td><td>replace</td></tr><tr><td><code>custom_id</code></td><td>add,replace</td></tr><tr><td><code>plan.billing_cycles[@sequence==n].<br/>pricing_scheme.fixed_price</code></td><td>add,replace</td></tr><tr><td><code>plan.billing_cycles[@sequence==n].<br/>pricing_scheme.tiers</code></td><td>replace</td></tr><tr><td><code>plan.billing_cycles[@sequence==n].<br/>total_cycles</code></td><td>replace</td></tr><tr><td><code>plan.payment_preferences.<br/>auto_bill_outstanding</code></td><td>replace</td></tr><tr><td><code>plan.payment_preferences.<br/>payment_failure_threshold</code></td><td>replace</td></tr><tr><td><code>plan.taxes.inclusive</code></td><td>add,replace</td></tr><tr><td><code>plan.taxes.percentage</code></td><td>add,replace</td></tr><tr><td><code>shipping_amount</code></td><td>add,replace</td></tr><tr><td><code>start_time</code></td><td>replace</td></tr><tr><td><code>subscriber.shipping_address</code></td><td>add,replace</td></tr><tr><td><code>subscriber.payment_source (for subscriptions funded<br/>by card payments)</code></td><td>replace</td></tr></tbody></table> */
export const subscriptionsPatch: API.OperationMethod<
  SubscriptionsPatchRequest,
  SubscriptionsPatchResponse,
  SubscriptionsPatchError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubscriptionsPatchRequest,
  output: SubscriptionsPatchResponse,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type SubscriptionsReviseError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Revise plan or quantity of subscription Updates the quantity of the product or service in a subscription. You can also use this method to switch the plan and update the `shipping_amount`, `shipping_address` values for the subscription. This type of update requires the buyer's consent. */
export const subscriptionsRevise: API.OperationMethod<
  SubscriptionsReviseRequest,
  SubscriptionReviseResponse,
  SubscriptionsReviseError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubscriptionsReviseRequest,
  output: SubscriptionReviseResponse,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type SubscriptionsSuspendError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Suspend subscription Suspends the subscription. */
export const subscriptionsSuspend: API.OperationMethod<
  SubscriptionsSuspendRequest,
  SubscriptionsSuspendResponse,
  SubscriptionsSuspendError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubscriptionsSuspendRequest,
  output: SubscriptionsSuspendResponse,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type SubscriptionsTransactionsError =
  | BadRequest
  | Forbidden
  | NotFound
  | PaypalOpError;
/** List transactions for subscription Lists transactions for a subscription. */
export const subscriptionsTransactions: API.OperationMethod<
  SubscriptionsTransactionsRequest,
  TransactionsList,
  SubscriptionsTransactionsError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SubscriptionsTransactionsRequest,
  output: TransactionsList,
  errors: [BadRequest, Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));
