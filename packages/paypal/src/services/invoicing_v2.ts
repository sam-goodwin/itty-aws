// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-paypal). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  PaypalProtocol,
  type PaypalOpError,
  type PaypalOpContext,
} from "../protocol.ts";
import { UnknownPaypalError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { PaypalOpError, PaypalOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface ConnectionsGetRequest {}
export const ConnectionsGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/invoicing/accounting-sync/merchant/connections",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ConnectionsGetRequest",
}) as any as S.Schema<ConnectionsGetRequest>;

/** The status of the last sync. This property supports Unicode. */
export type ConnectionLastSyncStatus = "IN_PROGRESS" | "SUCCESS" | "FAILED";
export const ConnectionLastSyncStatus = /*@__PURE__*/ S.String;

/** This lists last sync status and connection platform name. */
export interface Connection {
  /** The name of the platform. This property supports Unicode. The pattern is not provided because the value is defined by an external party. */
  platform_name?: string;
  /** The date and time when the resource is last synced with the accounting platform. This property supports Unicode. */
  last_sync_time?: string;
  /** The status of the last sync. This property supports Unicode. */
  last_sync_status?: ConnectionLastSyncStatus;
}
export const Connection = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    platform_name: S.optional(S.String),
    last_sync_time: S.optional(S.String),
    last_sync_status: S.optional(ConnectionLastSyncStatus),
  }),
).annotate({ identifier: "Connection" }) as any as S.Schema<Connection>;

/** An array of connection-level details. */
export type ConnectionList = Array<Connection>;
export const ConnectionList = /*@__PURE__*/ S.Array(
  Connection,
) as any as S.Schema<ConnectionList>;

/** This object contains an array of connection details. It is used to load sync status for a user. */
export interface Connections {
  connections?: ConnectionList;
}
export const Connections = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    connections: S.optional(ConnectionList),
  }),
).annotate({ identifier: "Connections" }) as any as S.Schema<Connections>;

export interface InvoiceConnectionDetailsGetRequest {
  /** The invoice id of the account. */
  id: string;
}
export const InvoiceConnectionDetailsGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/invoicing/accounting-sync/invoices/{id}/connections",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoiceConnectionDetailsGetRequest",
}) as any as S.Schema<InvoiceConnectionDetailsGetRequest>;

/** An array of connection-level details. */
export type ConnectionsList = Array<Connections>;
export const ConnectionsList = /*@__PURE__*/ S.Array(
  Connections,
) as any as S.Schema<ConnectionsList>;

/** Returns invoice connection status with timestamp per invoice. */
export interface InvoiceConnectionDetails {
  /** The ID of the invoice. This property supports Unicode. */
  id?: string;
  connection_status?: ConnectionsList;
}
export const InvoiceConnectionDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    connection_status: S.optional(ConnectionsList),
  }),
).annotate({
  identifier: "InvoiceConnectionDetails",
}) as any as S.Schema<InvoiceConnectionDetails>;

/** An array of one or more CC: emails to which notifications are sent. If you omit this parameter, a notification is sent to all CC: email addresses that are part of the invoice.<blockquote><strong>Note:</strong> Valid values are email addresses in the `additional_recipients` value associated with the invoice.</blockquote> */
export type EmailAddressList = Array<string>;
export const EmailAddressList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<EmailAddressList>;

export interface InvoicesCancelRequest {
  /** The ID of the draft invoice to delete. */
  invoice_id: string;
  /** The subject of the email that is sent as a notification to the recipient.<blockquote><strong>Note:</strong> User-provided values for this field will not be honored and the subject will always be defaulted to a system-defined value.</blockquote> */
  subject?: string;
  /** A note to the payer.<blockquote><strong>Note:</strong> User-provided values for this field will not be honored and the note will always be defaulted to a system-defined value.</blockquote> */
  note?: string;
  /** Indicates whether to send a copy of the email to the merchant. */
  send_to_invoicer?: boolean;
  /** Indicates whether to send a copy of the email to the recipient. */
  send_to_recipient?: boolean;
  additional_recipients?: EmailAddressList;
}
export const InvoicesCancelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_id: S.String.pipe(T.Label()),
    subject: S.optional(S.String),
    note: S.optional(S.String),
    send_to_invoicer: S.optional(S.Boolean),
    send_to_recipient: S.optional(S.Boolean),
    additional_recipients: S.optional(EmailAddressList),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/invoicing/invoices/{invoice_id}/cancel",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoicesCancelRequest",
}) as any as S.Schema<InvoicesCancelRequest>;

export interface InvoicesCancelResponse {}
export const InvoicesCancelResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "InvoicesCancelResponse",
}) as any as S.Schema<InvoicesCancelResponse>;

/** The file reference. Can be a file in PayPal MediaServ, PayPal DMS, or other custom store. */
export interface FileReference {
  /** The ID of the referenced file. */
  id?: string;
  /** The reference URL for the file. */
  reference_url?: string;
  /** The [Internet Assigned Numbers Authority (IANA) media type of the file](https://www.iana.org/assignments/media-types/media-types.xhtml). */
  content_type?: string;
  /** The date and time when the file was created, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  create_time?: string;
  /** The size of the file, in bytes. */
  size?: string;
}
export const FileReference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    reference_url: S.optional(S.String),
    content_type: S.optional(S.String),
    create_time: S.optional(S.String),
    size: S.optional(S.String),
  }),
).annotate({ identifier: "FileReference" }) as any as S.Schema<FileReference>;

/** An array of PayPal IDs for the files that are attached to an invoice. */
export type FileReferenceList = Array<FileReference>;
export const FileReferenceList = /*@__PURE__*/ S.Array(
  FileReference,
) as any as S.Schema<FileReferenceList>;

/** The payment term. Payment can be due upon receipt, a specified date, or in a set number of days. */
export type PaymentTermType =
  | "DUE_ON_RECEIPT"
  | "DUE_ON_DATE_SPECIFIED"
  | "NET_10"
  | "NET_15"
  | "NET_30"
  | "NET_45"
  | "NET_60"
  | "NET_90"
  | "NO_DUE_DATE";
export const PaymentTermType = /*@__PURE__*/ S.String;

/** The payment term of the invoice. Payment can be due upon receipt, a specified date, or in a set number of days. */
export interface InvoicePaymentTerm {
  /** The payment term. Payment can be due upon receipt, a specified date, or in a set number of days. */
  term_type?: PaymentTermType | (string & {});
  /** The date when the invoice payment is due, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). For example, *yyyy*-*MM*-*dd*T*z*. */
  due_date?: string;
}
export const InvoicePaymentTerm = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    term_type: S.optional(PaymentTermType),
    due_date: S.optional(S.String),
  }),
).annotate({
  identifier: "InvoicePaymentTerm",
}) as any as S.Schema<InvoicePaymentTerm>;

/** The details of the invoice. Includes invoice number, date, payment terms, and audit metadata. */
export interface InvoiceDetailInput {
  /** The reference data. Includes a Purchase Order (PO) number. */
  reference?: string;
  /** The [three-character ISO-4217 currency code](/docs/integration/direct/rest/currency-codes/) that identifies the currency. */
  currency_code: string;
  /** A note to the invoice recipient. Also appears on the invoice notification email. */
  note?: string;
  /** The general terms of the invoice. Can include return or cancellation policy and other terms and conditions. */
  terms_and_conditions?: string;
  /** A private bookkeeping memo for the user. */
  memo?: string;
  attachments?: FileReferenceList;
  /** The invoice number. Default is the number that is auto-incremented number from the last number. */
  invoice_number?: string;
  /** The invoice date as specificed by the sender, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). Only UTC is supported in response. For example, *yyyy*-*MM*-*dd*T*z*. */
  invoice_date?: string;
  /** The payment due date for the invoice. Value is either but not both <code>term_type</code> or <code>due_date</code>. */
  payment_term?: InvoicePaymentTerm;
}
export const InvoiceDetailInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    reference: S.optional(S.String),
    currency_code: S.String,
    note: S.optional(S.String),
    terms_and_conditions: S.optional(S.String),
    memo: S.optional(S.String),
    attachments: S.optional(FileReferenceList),
    invoice_number: S.optional(S.String),
    invoice_date: S.optional(S.String),
    payment_term: S.optional(InvoicePaymentTerm),
  }),
).annotate({
  identifier: "InvoiceDetailInput",
}) as any as S.Schema<InvoiceDetailInput>;

/** The name of the party. */
export interface Name {
  /** The prefix, or title, to the party's name. */
  prefix?: string;
  /** When the party is a person, the party's given, or first, name. */
  given_name?: string;
  /** When the party is a person, the party's surname or family name. Also known as the last name. Required when the party is a person. Use also to store multiple surnames including the matronymic, or mother's, surname. */
  surname?: string;
  /** When the party is a person, the party's middle name. Use also to store multiple middle names including the patronymic, or father's, middle name. */
  middle_name?: string;
  /** The suffix for the party's name. */
  suffix?: string;
  /** DEPRECATED. The party's alternate name. Can be a business name, nickname, or any other name that cannot be split into first, last name. Required when the party is a business. */
  alternate_full_name?: string;
  /** When the party is a person, the party's full name. */
  full_name?: string;
}
export const Name = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    prefix: S.optional(S.String),
    given_name: S.optional(S.String),
    surname: S.optional(S.String),
    middle_name: S.optional(S.String),
    suffix: S.optional(S.String),
    alternate_full_name: S.optional(S.String),
    full_name: S.optional(S.String),
  }),
).annotate({ identifier: "Name" }) as any as S.Schema<Name>;

/** The non-portable additional address details that are sometimes needed for compliance, risk, or other scenarios where fine-grain address information might be needed. Not portable with common third party and open source. Redundant with core fields.<br/>For example, `address_portable.address_line_1` is usually a combination of `address_details.street_number`, `street_name`, and `street_type`. */
export interface AddressDetails {
  /** The street number. */
  street_number?: string;
  /** The street name. Just `Drury` in `Drury Lane`. */
  street_name?: string;
  /** The street type. For example, avenue, boulevard, road, or expressway. */
  street_type?: string;
  /** The delivery service. Post office box, bag number, or post office name. */
  delivery_service?: string;
  /** A named locations that represents the premise. Usually a building name or number or collection of buildings with a common name or number. For example, <code>Craven House</code>. */
  building_name?: string;
  /** The first-order entity below a named building or location that represents the sub-premises. Usually a single building within a collection of buildings with a common name. Can be a flat, story, floor, room, or apartment. */
  sub_building?: string;
}
export const AddressDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    street_number: S.optional(S.String),
    street_name: S.optional(S.String),
    street_type: S.optional(S.String),
    delivery_service: S.optional(S.String),
    building_name: S.optional(S.String),
    sub_building: S.optional(S.String),
  }),
).annotate({ identifier: "AddressDetails" }) as any as S.Schema<AddressDetails>;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export interface AddressPortable {
  /** The first line of the address. For example, number or street. For example, `173 Drury Lane`. Required for data entry and compliance and risk checks. Must contain the full address. */
  address_line_1?: string;
  /** The second line of the address. For example, suite or apartment number. */
  address_line_2?: string;
  /** The third line of the address, if needed. For example, a street complement for Brazil, direction text, such as `next to Walmart`, or a landmark in an Indian address. */
  address_line_3?: string;
  /** The neighborhood, ward, or district. Smaller than `admin_area_level_3` or `sub_locality`. Value is:<ul><li>The postal sorting code for Guernsey and many French territories, such as French Guiana.</li><li>The fine-grained administrative levels in China.</li></ul> */
  admin_area_4?: string;
  /** A sub-locality, suburb, neighborhood, or district. Smaller than `admin_area_level_2`. Value is:<ul><li>Brazil. Suburb, bairro, or neighborhood.</li><li>India. Sub-locality or district. Street name information is not always available but a sub-locality or district can be a very small area.</li></ul> */
  admin_area_3?: string;
  /** A city, town, or village. Smaller than `admin_area_level_1`. */
  admin_area_2?: string;
  /** The highest level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. Format for postal delivery. For example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A kanton.</li></ul> */
  admin_area_1?: string;
  /** The postal code, which is the zip code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
  postal_code?: string;
  country_code: string;
  address_details?: AddressDetails;
}
export const AddressPortable = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address_line_1: S.optional(S.String),
    address_line_2: S.optional(S.String),
    address_line_3: S.optional(S.String),
    admin_area_4: S.optional(S.String),
    admin_area_3: S.optional(S.String),
    admin_area_2: S.optional(S.String),
    admin_area_1: S.optional(S.String),
    postal_code: S.optional(S.String),
    country_code: S.String,
    address_details: S.optional(AddressDetails),
  }),
).annotate({
  identifier: "AddressPortable",
}) as any as S.Schema<AddressPortable>;

/** The phone type. */
export type PhoneType = "FAX" | "HOME" | "MOBILE" | "OTHER" | "PAGER";
export const PhoneType = /*@__PURE__*/ S.String;

/** The phone details. Includes the phone number and type. */
export interface PhoneDetail {
  /** The country calling code (CC), in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the CC and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  country_code: string;
  /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  national_number: string;
  /** The extension number. */
  extension_number?: string;
  phone_type: PhoneType | (string & {});
}
export const PhoneDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    country_code: S.String,
    national_number: S.String,
    extension_number: S.optional(S.String),
    phone_type: PhoneType,
  }),
).annotate({ identifier: "PhoneDetail" }) as any as S.Schema<PhoneDetail>;

/** An array of invoicer's phone numbers. The invoicer can choose to hide the phone number on the invoice. */
export type PhoneDetailList = Array<PhoneDetail>;
export const PhoneDetailList = /*@__PURE__*/ S.Array(
  PhoneDetail,
) as any as S.Schema<PhoneDetailList>;

/** The invoicer business information that appears on the invoice. */
export interface InvoicerInfo {
  /** Required. The business name of the party. */
  business_name?: string;
  /** The first and Last name of the recipient. */
  name?: Name;
  /** The address of the recipient. */
  address?: AddressPortable;
  /** The invoicer email address, which must be listed in the user's PayPal profile. If you omit this value, notifications are sent from and to the primary email address but do not appear on the invoice. */
  email_address?: string;
  phones?: PhoneDetailList;
  /** The invoicer's website. */
  website?: string;
  /** The invoicer's tax ID. */
  tax_id?: string;
  /** Any additional information. Includes business hours. */
  additional_notes?: string;
  /** The full URL to an external logo image. The logo image must not be larger than 250 pixels wide by 90 pixels high. */
  logo_url?: string;
}
export const InvoicerInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    business_name: S.optional(S.String),
    name: S.optional(Name),
    address: S.optional(AddressPortable),
    email_address: S.optional(S.String),
    phones: S.optional(PhoneDetailList),
    website: S.optional(S.String),
    tax_id: S.optional(S.String),
    additional_notes: S.optional(S.String),
    logo_url: S.optional(S.String),
  }),
).annotate({ identifier: "InvoicerInfo" }) as any as S.Schema<InvoicerInfo>;

/** The invoice recipient's phone numbers. Extension number is not supported. */
export type DefinitionsPhoneDetailList = Array<PhoneDetail>;
export const DefinitionsPhoneDetailList = /*@__PURE__*/ S.Array(
  PhoneDetail,
) as any as S.Schema<DefinitionsPhoneDetailList>;

/** The billing information of the invoice recipient. Includes name, address, email, phone, and language. */
export interface BillingInfo {
  /** Required. The business name of the party. */
  business_name?: string;
  /** The first and Last name of the recipient. */
  name?: Name;
  /** The address of the recipient. */
  address?: AddressPortable;
  /** The invoice recipient email address. If you omit this value, the invoice is payable and a notification email is not sent. */
  email_address?: string;
  phones?: DefinitionsPhoneDetailList;
  /** Any additional information about the recipient. */
  additional_info?: string;
  /** The language in which to show the invoice recipient's email message. Used only when the recipient does not have a PayPal account. If you omit the language and the recipient does not have a PayPal account, the email message is sent in the language of the merchant's PayPal account. */
  language?: string;
}
export const BillingInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    business_name: S.optional(S.String),
    name: S.optional(Name),
    address: S.optional(AddressPortable),
    email_address: S.optional(S.String),
    phones: S.optional(DefinitionsPhoneDetailList),
    additional_info: S.optional(S.String),
    language: S.optional(S.String),
  }),
).annotate({ identifier: "BillingInfo" }) as any as S.Schema<BillingInfo>;

/** The contact information of the user. Includes name and address. */
export interface ContactNameAddress {
  /** Required. The business name of the party. */
  business_name?: string;
  /** The first and Last name of the recipient. */
  name?: Name;
  /** The address of the recipient. */
  address?: AddressPortable;
}
export const ContactNameAddress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    business_name: S.optional(S.String),
    name: S.optional(Name),
    address: S.optional(AddressPortable),
  }),
).annotate({
  identifier: "ContactNameAddress",
}) as any as S.Schema<ContactNameAddress>;

/** The billing and shipping information. Includes name, email, address, phone, and language. */
export interface RecipientInfo {
  /** The billing information for the invoice recipient. Includes name, address, email, phone, and language. */
  billing_info?: BillingInfo;
  /** The recipient's shipping information. Includes the user's contact information, which includes name and address. */
  shipping_info?: ContactNameAddress;
}
export const RecipientInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing_info: S.optional(BillingInfo),
    shipping_info: S.optional(ContactNameAddress),
  }),
).annotate({ identifier: "RecipientInfo" }) as any as S.Schema<RecipientInfo>;

/** The billing and shipping information. Includes name, email, address, phone and language. */
export type RecipientInfoList = Array<RecipientInfo>;
export const RecipientInfoList = /*@__PURE__*/ S.Array(
  RecipientInfo,
) as any as S.Schema<RecipientInfoList>;

/** The currency and amount for a financial transaction, such as a balance or payment due. */
export interface Money {
  currency_code: string;
  /** The value, which might be:<ul><li>An integer for currencies like `JPY` that are not typically fractional.</li><li>A decimal fraction for currencies like `TND` that are subdivided into thousandths.</li></ul>For the required number of decimal places for a currency code, see [Currency Codes](/api/rest/reference/currency-codes/). */
  value: string;
}
export const Money = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    currency_code: S.String,
    value: S.String,
  }),
).annotate({ identifier: "Money" }) as any as S.Schema<Money>;

/** The tax information. Includes the tax name and tax rate of invoice items. The tax amount is added to the item total. */
export interface Tax {
  /** The name of the tax applied on the invoice items. */
  name: string;
  /** The tax rate. Value is from `0` to `100`. Supports up to five decimal places. */
  percent: string;
  /** The calculated tax amount. The tax amount is added to the item total. */
  amount?: Money;
  /** The tax note used to track the tax related data. */
  tax_note?: string;
}
export const Tax = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    percent: S.String,
    amount: S.optional(Money),
    tax_note: S.optional(S.String),
  }),
).annotate({ identifier: "Tax" }) as any as S.Schema<Tax>;

/** The discount as a percent or amount at invoice level. The invoice discount amount is subtracted from the item total. */
export interface Discount {
  /** The discount as a percentage value. Value is from `0` to `100`. Supports up to five decimal places. */
  percent?: string;
  /** The invoice level discount amount. Value is from `0` to `1000000`. Supports up to two decimal places. */
  amount?: Money;
}
export const Discount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    percent: S.optional(S.String),
    amount: S.optional(Money),
  }),
).annotate({ identifier: "Discount" }) as any as S.Schema<Discount>;

/** The unit of measure for the invoiced item. */
export type UnitOfMeasure = "QUANTITY" | "HOURS" | "AMOUNT";
export const UnitOfMeasure = /*@__PURE__*/ S.String;

/** An array of invoice line item information. The maximum items for an invoice is `100`. */
export interface ItemInput {
  /** The item name for the invoice line item. */
  name: string;
  /** The item description for the invoice line item. */
  description?: string;
  /** The quantity of the item that the invoicer provides to the payer. Value is from `-1000000` to `1000000`. Supports up to five decimal places. */
  quantity: string;
  /** The unit price of the item. This does not include tax and discount. Value is from `-1000000` to `1000000`. Supports up to two decimal places. */
  unit_amount: Money;
  /** The tax associated with the item. The tax amount is added to the item total. Value is from `0` to `100`. Supports up to five decimal places. */
  tax?: Tax;
  /** The date when the item or service was provided, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). For example, *yyyy*-*MM*-*dd*T*z*. */
  item_date?: string;
  /** Discount as a percent or amount at invoice level. The invoice discount amount is subtracted from the item total. */
  discount?: Discount;
  /** The unit of measure for the invoiced item. For `AMOUNT` the `unit_amount` and `quantity` are not shown on the invoice.<blockquote><strong>Note:</strong> If your specify different `unit_of_measure` values for the same invoice, the invoice uses the first value.</blockquote> */
  unit_of_measure?: UnitOfMeasure | (string & {});
}
export const ItemInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    description: S.optional(S.String),
    quantity: S.String,
    unit_amount: Money,
    tax: S.optional(Tax),
    item_date: S.optional(S.String),
    discount: S.optional(Discount),
    unit_of_measure: S.optional(UnitOfMeasure),
  }),
).annotate({ identifier: "ItemInput" }) as any as S.Schema<ItemInput>;

/** An array of invoice line item information. */
export type ItemListInput = Array<ItemInput>;
export const ItemListInput = /*@__PURE__*/ S.Array(
  ItemInput,
) as any as S.Schema<ItemListInput>;

/** The partial payment details. Includes the minimum amount that the invoicer expects from the payer. */
export interface PartialPayment {
  /** Indicates whether the invoice allows a partial payment. If `false`, the invoice must be paid in full. If `true`, the invoice allows partial payments.<blockquote><strong>Note:</strong> This feature is not available for users in `India`, `Brazil`, or `Israel`.</blockquote> */
  allow_partial_payment?: boolean;
  /** The minimum amount allowed for a partial payment. Valid only when `allow_partial_payment` is `true`. */
  minimum_amount_due?: Money;
}
export const PartialPayment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    allow_partial_payment: S.optional(S.Boolean),
    minimum_amount_due: S.optional(Money),
  }),
).annotate({ identifier: "PartialPayment" }) as any as S.Schema<PartialPayment>;

/** The invoice configuration details. Includes partial payment, tip, and tax calculated after discount. */
export interface Configuration {
  /** Indicates whether the tax is calculated before or after a discount. If `false`, the tax is calculated before a discount. If `true`, the tax is calculated after a discount. */
  tax_calculated_after_discount?: boolean;
  /** Indicates whether the unit price includes tax. */
  tax_inclusive?: boolean;
  /** Indicates whether the invoice enables the customer to enter a tip amount during payment. If `true`, the invoice shows a tip amount field so that the customer can enter a tip amount. If `false`, the invoice does not show a tip amount field.<blockquote><strong>Note:</strong> This feature is not available for users in `Hong Kong`, `Taiwan`, `India`, or `Japan`.</blockquote> */
  allow_tip?: boolean;
  /** The partial payment details. Includes the minimum amount that the invoicer wants the payer to pay. */
  partial_payment?: PartialPayment;
  /** Indicates whether conditional pricing rules are applied to the invoice. If `true`, pricing rules (such as discounts or surcharges based on specific conditions) are applied. If `false`, no conditional pricing rules are applied. */
  has_conditional_rule?: boolean;
  /** The template ID. The template determines the layout of the invoice. Includes which fields to show and hide.<blockquote><strong>Note:</strong> This is an optional field. If you wish to customize the invoice layout using a specific template, provide a valid template ID here. You can either use an existing template ID or create a new template via the create template API and then use the newly created template's ID.</blockquote> */
  template_id?: string;
}
export const Configuration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tax_calculated_after_discount: S.optional(S.Boolean),
    tax_inclusive: S.optional(S.Boolean),
    allow_tip: S.optional(S.Boolean),
    partial_payment: S.optional(PartialPayment),
    has_conditional_rule: S.optional(S.Boolean),
    template_id: S.optional(S.String),
  }),
).annotate({ identifier: "Configuration" }) as any as S.Schema<Configuration>;

/** The discount. Can be an item- or invoice-level discount, or both. Can be applied as a percent or amount. If you provide both amount and percent, amount takes precedent. */
export interface AggregatedDiscount {
  /** The discount as a percent or amount at invoice level. The invoice discount amount is subtracted from the item total. */
  invoice_discount?: Discount;
  /** The discount as a percent or amount at item level. The item discount amount is subtracted from each item amount. */
  item_discount?: Money;
}
export const AggregatedDiscount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_discount: S.optional(Discount),
    item_discount: S.optional(Money),
  }),
).annotate({
  identifier: "AggregatedDiscount",
}) as any as S.Schema<AggregatedDiscount>;

/** The shipping fee for all items. Includes tax on shipping. */
export interface ShippingCost {
  /** The shipping amount. Value is from `0` to `1000000`. Supports up to two decimal places. */
  amount?: Money;
  /** The tax associated with the shipping. */
  tax?: Tax;
}
export const ShippingCost = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    amount: S.optional(Money),
    tax: S.optional(Tax),
  }),
).annotate({ identifier: "ShippingCost" }) as any as S.Schema<ShippingCost>;

/** The custom amount to apply to an invoice. If you include a label, you must include a custom amount. */
export interface CustomAmount {
  /** The label to the custom amount of the invoice. */
  label: string;
  /** The custom amount value. Value is from `-1000000` to `1000000`. Supports up to two decimal places. */
  amount?: Money;
}
export const CustomAmount = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    label: S.String,
    amount: S.optional(Money),
  }),
).annotate({ identifier: "CustomAmount" }) as any as S.Schema<CustomAmount>;

/** The breakdown of the amount. Includes total item amount, total tax amount, custom amount, and shipping and discounts, if any. */
export interface AmountWithBreakdown {
  /** The subtotal for all items. Must equal the sum of (`items[].unit_amount` * `items[].quantity`) for all items. */
  item_total?: Money;
  /** The discount can be at the item or invoice level, or both. Can be applied as a percent or amount. If you provide both amount and percent, amount takes precedent. */
  discount?: AggregatedDiscount;
  /** The aggregated amount of the item and shipping taxes. */
  tax_total?: Money;
  /** The shipping fee for all items. Includes tax on shipping. */
  shipping?: ShippingCost;
  /** The custom amount to apply to an invoice. If you include a label, you must include the custom amount. */
  custom?: CustomAmount;
}
export const AmountWithBreakdown = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    item_total: S.optional(Money),
    discount: S.optional(AggregatedDiscount),
    tax_total: S.optional(Money),
    shipping: S.optional(ShippingCost),
    custom: S.optional(CustomAmount),
  }),
).annotate({
  identifier: "AmountWithBreakdown",
}) as any as S.Schema<AmountWithBreakdown>;

/** The invoice amount summary of item total, discount, tax total, and shipping. */
export interface AmountSummaryDetail {
  currency_code?: string;
  /** The value, which might be:<ul><li>An integer for currencies like `JPY` that are not typically fractional.</li><li>A decimal fraction for currencies like `TND` that are subdivided into thousandths.</li></ul>For the required number of decimal places for a currency code, see [Currency Codes](/docs/integration/direct/rest/currency-codes/). */
  value?: string;
  /** The breakdown of the amount. Breakdown provides details such as total item amount, total tax amount, custom amount, shipping and discounts, if any. */
  breakdown?: AmountWithBreakdown;
}
export const AmountSummaryDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    currency_code: S.optional(S.String),
    value: S.optional(S.String),
    breakdown: S.optional(AmountWithBreakdown),
  }),
).annotate({
  identifier: "AmountSummaryDetail",
}) as any as S.Schema<AmountSummaryDetail>;

/** An array of payments registered against the invoice. */
export interface PaymentsInput {}
export const PaymentsInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({ identifier: "PaymentsInput" }) as any as S.Schema<PaymentsInput>;

/** The invoicing refund details. Includes the refund type, date, amount, and method. */
export type RefundsInput = PaymentsInput;
export const RefundsInput = PaymentsInput;

export interface InvoicesCreateRequest {
  /** The details of the invoice. Includes the invoice number, date, payment terms, and audit metadata. */
  detail: InvoiceDetailInput;
  /** The invoicer information. Includes the business name, email, address, phone, fax, tax ID, additional notes, and logo URL. */
  invoicer?: InvoicerInfo;
  primary_recipients?: RecipientInfoList;
  additional_recipients?: EmailAddressList;
  items?: ItemListInput;
  /** The invoice configuration details. Includes partial payment, tip, and tax calculated after discount. */
  configuration?: Configuration;
  /** The invoice amount summary of item total, discount, tax total and shipping.. */
  amount?: AmountSummaryDetail;
  /** List of payments registered against the invoice.. */
  payments?: PaymentsInput;
  /** List of refunds against this invoice. The invoicing refund details includes refund type, date, amount, and method. */
  refunds?: PaymentsInput;
}
export const InvoicesCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    detail: InvoiceDetailInput,
    invoicer: S.optional(InvoicerInfo),
    primary_recipients: S.optional(RecipientInfoList),
    additional_recipients: S.optional(EmailAddressList),
    items: S.optional(ItemListInput),
    configuration: S.optional(Configuration),
    amount: S.optional(AmountSummaryDetail),
    payments: S.optional(PaymentsInput),
    refunds: S.optional(PaymentsInput),
  }).pipe(T.Http({ method: "POST", uri: "/v2/invoicing/invoices", code: 200 })),
).annotate({
  identifier: "InvoicesCreateRequest",
}) as any as S.Schema<InvoicesCreateRequest>;

/** The status of the invoice. */
export type InvoiceStatus =
  | "DRAFT"
  | "SENT"
  | "SCHEDULED"
  | "PAID"
  | "MARKED_AS_PAID"
  | "CANCELLED"
  | "REFUNDED"
  | "PARTIALLY_PAID"
  | "PARTIALLY_REFUNDED"
  | "MARKED_AS_REFUNDED"
  | "UNPAID"
  | "PAYMENT_PENDING"
  | "AUTO_CANCELLED"
  | "PAID_EXTERNAL"
  | "REFUNDED_EXTERNAL"
  | "SHARED";
export const InvoiceStatus = /*@__PURE__*/ S.String;

/** The frequency at which the invoice is sent:<ul><li>Multiple recipient. Sent to multiple recipients.</li><li>Batch. Sent in a batch.</li><li>Regular single. Sent one time to a single recipient.</li></ul> */
export type InvoiceCreationFlow =
  | "MULTIPLE_RECIPIENTS_GROUP"
  | "BATCH"
  | "REGULAR_SINGLE";
export const InvoiceCreationFlow = /*@__PURE__*/ S.String;

/** The audit metadata. Captures all invoicing actions on create, send, update, and cancel. */
export interface Metadata {
  /** The date and time when the resource was created, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  create_time?: string;
  /** The email address of the account that created the resource. */
  created_by?: string;
  /** The date and time when the resource was last edited, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  last_update_time?: string;
  /** The email address of the account that last edited the resource. */
  last_updated_by?: string;
  /** The date and time when the resource was canceled, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  cancel_time?: string;
  /** The actor who canceled the resource. */
  cancelled_by?: string;
  /** The date and time when the resource was first sent, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  first_sent_time?: string;
  /** The date and time when the resource was last sent, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  last_sent_time?: string;
  /** The email address of the account that last sent the resource. */
  last_sent_by?: string;
  /** The flow variation that created this invoice. */
  created_by_flow?: InvoiceCreationFlow;
  /** The URL for the invoice payer view hosted on paypal.com. */
  recipient_view_url?: string;
  /** The URL for the invoice merchant view hosted on paypal.com. */
  invoicer_view_url?: string;
}
export const Metadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    create_time: S.optional(S.String),
    created_by: S.optional(S.String),
    last_update_time: S.optional(S.String),
    last_updated_by: S.optional(S.String),
    cancel_time: S.optional(S.String),
    cancelled_by: S.optional(S.String),
    first_sent_time: S.optional(S.String),
    last_sent_time: S.optional(S.String),
    last_sent_by: S.optional(S.String),
    created_by_flow: S.optional(InvoiceCreationFlow),
    recipient_view_url: S.optional(S.String),
    invoicer_view_url: S.optional(S.String),
  }),
).annotate({ identifier: "Metadata" }) as any as S.Schema<Metadata>;

/** The details of the invoice. Includes invoice number, date, payment terms, and audit metadata. */
export interface InvoiceDetail {
  /** The reference data. Includes a Purchase Order (PO) number. */
  reference?: string;
  /** The [three-character ISO-4217 currency code](/docs/integration/direct/rest/currency-codes/) that identifies the currency. */
  currency_code: string;
  /** A note to the invoice recipient. Also appears on the invoice notification email. */
  note?: string;
  /** The general terms of the invoice. Can include return or cancellation policy and other terms and conditions. */
  terms_and_conditions?: string;
  /** A private bookkeeping memo for the user. */
  memo?: string;
  attachments?: FileReferenceList;
  /** The invoice number. Default is the number that is auto-incremented number from the last number. */
  invoice_number?: string;
  /** The invoice date as specificed by the sender, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). Only UTC is supported in response. For example, *yyyy*-*MM*-*dd*T*z*. */
  invoice_date?: string;
  /** The payment due date for the invoice. Value is either but not both <code>term_type</code> or <code>due_date</code>. */
  payment_term?: InvoicePaymentTerm;
  /** The audit metadata. Captures all invoicing actions on create, send, update, and cancel. */
  metadata?: Metadata;
}
export const InvoiceDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    reference: S.optional(S.String),
    currency_code: S.String,
    note: S.optional(S.String),
    terms_and_conditions: S.optional(S.String),
    memo: S.optional(S.String),
    attachments: S.optional(FileReferenceList),
    invoice_number: S.optional(S.String),
    invoice_date: S.optional(S.String),
    payment_term: S.optional(InvoicePaymentTerm),
    metadata: S.optional(Metadata),
  }),
).annotate({ identifier: "InvoiceDetail" }) as any as S.Schema<InvoiceDetail>;

/** An array of invoice line item information. The maximum items for an invoice is `100`. */
export interface Item {
  /** The ID of the invoice line item. */
  id?: string;
  /** The item name for the invoice line item. */
  name: string;
  /** The item description for the invoice line item. */
  description?: string;
  /** The quantity of the item that the invoicer provides to the payer. Value is from `-1000000` to `1000000`. Supports up to five decimal places. */
  quantity: string;
  /** The unit price of the item. This does not include tax and discount. Value is from `-1000000` to `1000000`. Supports up to two decimal places. */
  unit_amount: Money;
  /** The tax associated with the item. The tax amount is added to the item total. Value is from `0` to `100`. Supports up to five decimal places. */
  tax?: Tax;
  /** The date when the item or service was provided, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). For example, *yyyy*-*MM*-*dd*T*z*. */
  item_date?: string;
  /** Discount as a percent or amount at invoice level. The invoice discount amount is subtracted from the item total. */
  discount?: Discount;
  /** The unit of measure for the invoiced item. For `AMOUNT` the `unit_amount` and `quantity` are not shown on the invoice.<blockquote><strong>Note:</strong> If your specify different `unit_of_measure` values for the same invoice, the invoice uses the first value.</blockquote> */
  unit_of_measure?: UnitOfMeasure;
}
export const Item = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    name: S.String,
    description: S.optional(S.String),
    quantity: S.String,
    unit_amount: Money,
    tax: S.optional(Tax),
    item_date: S.optional(S.String),
    discount: S.optional(Discount),
    unit_of_measure: S.optional(UnitOfMeasure),
  }),
).annotate({ identifier: "Item" }) as any as S.Schema<Item>;

/** An array of invoice line item information. */
export type ItemList = Array<Item>;
export const ItemList = /*@__PURE__*/ S.Array(
  Item,
) as any as S.Schema<ItemList>;

/** The payment type. Can be PayPal or an external payment. Includes cash or a check. */
export type PaymentType = "PAYPAL" | "EXTERNAL";
export const PaymentType = /*@__PURE__*/ S.String;

/** The payment mode or method through which the invoicer can accept the payments. */
export type PaymentMethod =
  | "BANK_TRANSFER"
  | "CASH"
  | "CHECK"
  | "CREDIT_CARD"
  | "DEBIT_CARD"
  | "PAYPAL"
  | "WIRE_TRANSFER"
  | "OTHER";
export const PaymentMethod = /*@__PURE__*/ S.String;

/** The payment details of the invoice. Includes payment type, method, date, discount, and transaction type. */
export interface PaymentDetail {
  /** The payment type in an invoicing flow which can be PayPal or an external cash or check payment. */
  type?: PaymentType;
  /** The ID for a PayPal payment transaction. Required for the `PAYPAL` payment type. */
  payment_id?: string;
  /** The date when the invoice was paid, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). For example, *yyyy*-*MM*-*dd* *z*. */
  payment_date?: string;
  /** The payment mode or method through which the invoicer can accept the payment. */
  method: PaymentMethod;
  /** A note associated with an external cash or check payment. */
  note?: string;
  /** The payment amount to record against the invoice. If you omit this parameter, the total invoice amount is marked as paid. This amount cannot exceed the amount due. */
  amount?: Money;
  /** The recipient's shipping information. Includes the user's contact information, which includes name and address. */
  shipping_info?: ContactNameAddress;
}
export const PaymentDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.optional(PaymentType),
    payment_id: S.optional(S.String),
    payment_date: S.optional(S.String),
    method: PaymentMethod,
    note: S.optional(S.String),
    amount: S.optional(Money),
    shipping_info: S.optional(ContactNameAddress),
  }),
).annotate({ identifier: "PaymentDetail" }) as any as S.Schema<PaymentDetail>;

/** An array of payment details for the invoice. The payment details of the invoice like payment type, method, date, discount and transaction type. */
export type PaymentDetailList = Array<PaymentDetail>;
export const PaymentDetailList = /*@__PURE__*/ S.Array(
  PaymentDetail,
) as any as S.Schema<PaymentDetailList>;

/** An array of payments registered against the invoice. */
export interface Payments {
  /** The aggregated payment amounts against this invoice. */
  paid_amount?: Money;
  transactions?: PaymentDetailList;
}
export const Payments = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    paid_amount: S.optional(Money),
    transactions: S.optional(PaymentDetailList),
  }),
).annotate({ identifier: "Payments" }) as any as S.Schema<Payments>;

/** The refund details of the invoice. Includes the refund type, date, amount, and method. */
export interface RefundDetail {
  /** The PayPal refund type. Indicates whether the refund was paid through PayPal or externally in the invoicing flow. The record refund method supports the `EXTERNAL` refund type. The `PAYPAL` refund type is supported for backward compatibility. */
  type?: PaymentType;
  /** The ID for a PayPal payment transaction. Required for the `PAYPAL` payment type. */
  refund_id?: string;
  /** The date when the invoice was refunded, in [Internet date format](https://tools.ietf.org/html/rfc3339#section-5.6). For example, `2014-02-27`. */
  refund_date?: string;
  /** The amount to record as refunded. If you omit the amount, the total invoice paid amount is recorded as refunded. */
  amount?: Money;
  /** The payment mode or method through which the invoicer can accept the payments. */
  method: PaymentMethod;
}
export const RefundDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.optional(PaymentType),
    refund_id: S.optional(S.String),
    refund_date: S.optional(S.String),
    amount: S.optional(Money),
    method: PaymentMethod,
  }),
).annotate({ identifier: "RefundDetail" }) as any as S.Schema<RefundDetail>;

/** An array of refund details for the invoice. Includes the refund type, date, amount, and method. */
export type RefundDetailList = Array<RefundDetail>;
export const RefundDetailList = /*@__PURE__*/ S.Array(
  RefundDetail,
) as any as S.Schema<RefundDetailList>;

/** The invoicing refund details. Includes the refund type, date, amount, and method. */
export interface Refunds {
  /** The aggregated refund amounts. */
  refund_amount?: Money;
  transactions?: RefundDetailList;
}
export const Refunds = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    refund_amount: S.optional(Money),
    transactions: S.optional(RefundDetailList),
  }),
).annotate({ identifier: "Refunds" }) as any as S.Schema<Refunds>;

/** The HTTP method required to make the related call. */
export type LinkDescriptionMethod =
  | "GET"
  | "POST"
  | "PUT"
  | "DELETE"
  | "HEAD"
  | "CONNECT"
  | "OPTIONS"
  | "PATCH";
export const LinkDescriptionMethod = /*@__PURE__*/ S.String;

/** The request-related [HATEOAS link](/api/rest/responses/#hateoas-links) information. */
export interface LinkDescription {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  /** The HTTP method required to make the related call. */
  method?: LinkDescriptionMethod;
}
export const LinkDescription = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    href: S.String,
    rel: S.String,
    method: S.optional(LinkDescriptionMethod),
  }),
).annotate({
  identifier: "LinkDescription",
}) as any as S.Schema<LinkDescription>;

/** An array of request-related [HATEOAS links](/docs/api/reference/api-responses/#hateoas-links). */
export type LinkDescriptionList = Array<LinkDescription>;
export const LinkDescriptionList = /*@__PURE__*/ S.Array(
  LinkDescription,
) as any as S.Schema<LinkDescriptionList>;

/** The invoice details which includes all information of the invoice like items, billing information. */
export interface Invoice {
  /** The ID of the invoice. */
  id?: string;
  /** The parent ID to an invoice that defines the group invoice to which the invoice is related. */
  parent_id?: string;
  status?: InvoiceStatus;
  /** The details of the invoice. Includes the invoice number, date, payment terms, and audit metadata. */
  detail: InvoiceDetail;
  /** The invoicer information. Includes the business name, email, address, phone, fax, tax ID, additional notes, and logo URL. */
  invoicer?: InvoicerInfo;
  primary_recipients?: RecipientInfoList;
  additional_recipients?: EmailAddressList;
  items?: ItemList;
  /** The invoice configuration details. Includes partial payment, tip, and tax calculated after discount. */
  configuration?: Configuration;
  /** The invoice amount summary of item total, discount, tax total and shipping.. */
  amount?: AmountSummaryDetail;
  /** The due amount, which is the balance amount outstanding after payments. */
  due_amount?: Money;
  /** The amount paid by the payer as gratuity to the invoicer. */
  gratuity?: Money;
  /** List of payments registered against the invoice.. */
  payments?: Payments;
  /** List of refunds against this invoice. The invoicing refund details includes refund type, date, amount, and method. */
  refunds?: Refunds;
  links?: LinkDescriptionList;
}
export const Invoice = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    parent_id: S.optional(S.String),
    status: S.optional(InvoiceStatus),
    detail: InvoiceDetail,
    invoicer: S.optional(InvoicerInfo),
    primary_recipients: S.optional(RecipientInfoList),
    additional_recipients: S.optional(EmailAddressList),
    items: S.optional(ItemList),
    configuration: S.optional(Configuration),
    amount: S.optional(AmountSummaryDetail),
    due_amount: S.optional(Money),
    gratuity: S.optional(Money),
    payments: S.optional(Payments),
    refunds: S.optional(Refunds),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({ identifier: "Invoice" }) as any as S.Schema<Invoice>;

export interface InvoicesDeleteRequest {
  /** The ID of the draft invoice to delete. */
  invoice_id: string;
}
export const InvoicesDeleteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/invoicing/invoices/{invoice_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoicesDeleteRequest",
}) as any as S.Schema<InvoicesDeleteRequest>;

export interface InvoicesDeleteResponse {}
export const InvoicesDeleteResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "InvoicesDeleteResponse",
}) as any as S.Schema<InvoicesDeleteResponse>;

export interface InvoicesGenerateQrCodeRequest {
  /** The ID of the draft invoice to delete. */
  invoice_id: string;
  /** The width, in pixels, of the QR code image. Value is from `150` to `500`. */
  width?: number;
  /** The height, in pixels, of the QR code image. Value is from `150` to `500`. */
  height?: number;
  /** The type of URL for which to generate a QR code. Valid values are `pay` and `details`. */
  action?: string;
}
export const InvoicesGenerateQrCodeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_id: S.String.pipe(T.Label()),
    width: S.optional(S.Number),
    height: S.optional(S.Number),
    action: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/invoicing/invoices/{invoice_id}/generate-qr-code",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoicesGenerateQrCodeRequest",
}) as any as S.Schema<InvoicesGenerateQrCodeRequest>;

export interface InvoicesGenerateQrCodeResponse {}
export const InvoicesGenerateQrCodeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "InvoicesGenerateQrCodeResponse",
}) as any as S.Schema<InvoicesGenerateQrCodeResponse>;

export interface InvoicesGetRequest {
  /** The ID of the draft invoice to delete. */
  invoice_id: string;
}
export const InvoicesGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/invoicing/invoices/{invoice_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoicesGetRequest",
}) as any as S.Schema<InvoicesGetRequest>;

export interface InvoicesListRequest {
  /** The page number to be retrieved, for the list of templates. So, a combination of `page=1` and `page_size=20` returns the first 20 templates. A combination of `page=2` and `page_size=20` returns the next 20 templates. */
  page?: number;
  /** The maximum number of templates to return in the response. */
  page_size?: number;
  /** Indicates whether the to show <code>total_pages</code> and <code>total_items</code> in the response. */
  total_required?: boolean;
  /** The fields to return in the response. Value is `all` or `none`. To return only the template name, ID, and default attributes, specify `none`. */
  fields?: string;
}
export const InvoicesListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    page: S.optional(S.Number.pipe(T.Query())),
    page_size: S.optional(S.Number.pipe(T.Query())),
    total_required: S.optional(S.Boolean.pipe(T.Query())),
    fields: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v2/invoicing/invoices", code: 200 })),
).annotate({
  identifier: "InvoicesListRequest",
}) as any as S.Schema<InvoicesListRequest>;

/** The list of invoices that match the search criteria. */
export type InvoiceList = Array<Invoice>;
export const InvoiceList = /*@__PURE__*/ S.Array(
  Invoice,
) as any as S.Schema<InvoiceList>;

/** An array of merchant invoices. Includes the total invoices count and [HATEOAS links](/docs/api/reference/api-responses/#hateoas-links) for navigation. */
export interface Invoices {
  /** The total number of pages that are available for the search criteria. <blockquote><strong>Note:</strong> Clients MUST NOT assume that the value of total_pages is constant. The value MAY change from one request to the next</blockquote> */
  total_pages?: number;
  /** The total number of invoices that match the search criteria.<blockquote><strong>Note:</strong> Clients MUST NOT assume that the value of <code>total_items</code> is constant. The value MAY change from one request to the next.</blockquote> */
  total_items?: number;
  items?: InvoiceList;
  links?: LinkDescriptionList;
}
export const Invoices = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    total_pages: S.optional(S.Number),
    total_items: S.optional(S.Number),
    items: S.optional(InvoiceList),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({ identifier: "Invoices" }) as any as S.Schema<Invoices>;

export interface InvoicesPaymentsRequest {
  /** The ID of the draft invoice to delete. */
  invoice_id: string;
  /** The ID for a PayPal payment transaction. Required for the `PAYPAL` payment type. */
  payment_id?: string;
  /** The date when the invoice was paid, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). For example, *yyyy*-*MM*-*dd* *z*. */
  payment_date?: string;
  /** The payment mode or method through which the invoicer can accept the payment. */
  method: PaymentMethod | (string & {});
  /** A note associated with an external cash or check payment. */
  note?: string;
  /** The payment amount to record against the invoice. If you omit this parameter, the total invoice amount is marked as paid. This amount cannot exceed the amount due. */
  amount?: Money;
  /** The recipient's shipping information. Includes the user's contact information, which includes name and address. */
  shipping_info?: ContactNameAddress;
}
export const InvoicesPaymentsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_id: S.String.pipe(T.Label()),
    payment_id: S.optional(S.String),
    payment_date: S.optional(S.String),
    method: PaymentMethod,
    note: S.optional(S.String),
    amount: S.optional(Money),
    shipping_info: S.optional(ContactNameAddress),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/invoicing/invoices/{invoice_id}/payments",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoicesPaymentsRequest",
}) as any as S.Schema<InvoicesPaymentsRequest>;

/** The reference to the payment detail. */
export interface PaymentReference {
  /** The ID for the invoice payment. */
  payment_id?: string;
}
export const PaymentReference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    payment_id: S.optional(S.String),
  }),
).annotate({
  identifier: "PaymentReference",
}) as any as S.Schema<PaymentReference>;

export interface InvoicesPaymentsDeleteRequest {
  /** The ID of the draft invoice to delete. */
  invoice_id: string;
  /** The ID of the external refund transaction to delete. */
  transaction_id: string;
}
export const InvoicesPaymentsDeleteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_id: S.String.pipe(T.Label()),
    transaction_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/invoicing/invoices/{invoice_id}/payments/{transaction_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoicesPaymentsDeleteRequest",
}) as any as S.Schema<InvoicesPaymentsDeleteRequest>;

export interface InvoicesPaymentsDeleteResponse {}
export const InvoicesPaymentsDeleteResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "InvoicesPaymentsDeleteResponse",
}) as any as S.Schema<InvoicesPaymentsDeleteResponse>;

export interface InvoicesRefundsRequest {
  /** The ID of the draft invoice to delete. */
  invoice_id: string;
  /** The date when the invoice was refunded, in [Internet date format](https://tools.ietf.org/html/rfc3339#section-5.6). For example, `2014-02-27`. */
  refund_date?: string;
  /** The amount to record as refunded. If you omit the amount, the total invoice paid amount is recorded as refunded. */
  amount?: Money;
  /** The payment mode or method through which the invoicer can accept the payments. */
  method: PaymentMethod | (string & {});
}
export const InvoicesRefundsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_id: S.String.pipe(T.Label()),
    refund_date: S.optional(S.String),
    amount: S.optional(Money),
    method: PaymentMethod,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/invoicing/invoices/{invoice_id}/refunds",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoicesRefundsRequest",
}) as any as S.Schema<InvoicesRefundsRequest>;

/** The reference to the refund payment detail. */
export interface RefundReference {
  /** The ID of the refund of an invoice payment. */
  refund_id?: string;
}
export const RefundReference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    refund_id: S.optional(S.String),
  }),
).annotate({
  identifier: "RefundReference",
}) as any as S.Schema<RefundReference>;

export interface InvoicesRefundsDeleteRequest {
  /** The ID of the draft invoice to delete. */
  invoice_id: string;
  /** The ID of the external refund transaction to delete. */
  transaction_id: string;
}
export const InvoicesRefundsDeleteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_id: S.String.pipe(T.Label()),
    transaction_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/invoicing/invoices/{invoice_id}/refunds/{transaction_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoicesRefundsDeleteRequest",
}) as any as S.Schema<InvoicesRefundsDeleteRequest>;

export interface InvoicesRefundsDeleteResponse {}
export const InvoicesRefundsDeleteResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "InvoicesRefundsDeleteResponse",
}) as any as S.Schema<InvoicesRefundsDeleteResponse>;

export interface InvoicesRemindRequest {
  /** The ID of the draft invoice to delete. */
  invoice_id: string;
  /** The subject of the email that is sent as a notification to the recipient.<blockquote><strong>Note:</strong> User-provided values for this field will not be honored and the subject will always be defaulted to a system-defined value.</blockquote> */
  subject?: string;
  /** A note to the payer.<blockquote><strong>Note:</strong> User-provided values for this field will not be honored and the note will always be defaulted to a system-defined value.</blockquote> */
  note?: string;
  /** Indicates whether to send a copy of the email to the merchant. */
  send_to_invoicer?: boolean;
  /** Indicates whether to send a copy of the email to the recipient. */
  send_to_recipient?: boolean;
  additional_recipients?: EmailAddressList;
}
export const InvoicesRemindRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_id: S.String.pipe(T.Label()),
    subject: S.optional(S.String),
    note: S.optional(S.String),
    send_to_invoicer: S.optional(S.Boolean),
    send_to_recipient: S.optional(S.Boolean),
    additional_recipients: S.optional(EmailAddressList),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/invoicing/invoices/{invoice_id}/remind",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoicesRemindRequest",
}) as any as S.Schema<InvoicesRemindRequest>;

export interface InvoicesRemindResponse {}
export const InvoicesRemindResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "InvoicesRemindResponse",
}) as any as S.Schema<InvoicesRemindResponse>;

/** An array of status values. */
export type InvoiceStatusList = Array<InvoiceStatus | (string & {})>;
export const InvoiceStatusList = /*@__PURE__*/ S.Array(
  InvoiceStatus,
) as any as S.Schema<InvoiceStatusList>;

/** The amount range. */
export interface AmountRange {
  /** The lower limit of the amount range. */
  lower_amount: Money;
  /** The upper limit of the amount range. */
  upper_amount: Money;
}
export const AmountRange = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    lower_amount: Money,
    upper_amount: Money,
  }),
).annotate({ identifier: "AmountRange" }) as any as S.Schema<AmountRange>;

/** The date range. Filters invoices by creation date, invoice date, due date, and payment date. */
export interface DateRange {
  /** The start date of the range. Filters invoices by creation date, invoice date, due date, and payment date. */
  start: string;
  /** The end date of the range. Filters invoices by creation date, invoice date, due date, and payment date. */
  end: string;
}
export const DateRange = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    start: S.String,
    end: S.String,
  }),
).annotate({ identifier: "DateRange" }) as any as S.Schema<DateRange>;

/** The date and time range. Filters invoices by creation date, invoice date, due date, and payment date. */
export interface DateTimeRange {
  /** The start date of the range. Filters invoices by creation date, invoice date, due date, and payment date. */
  start: string;
  /** The end date of the range. Filters invoices by creation date, invoice date, due date, and payment date. <blockquote><strong>Note: The regular expression provides guidance but does not reject all invalid dates.</strong></blockquote><br/>Minimum length: 20.<br/>Maximum length: 64<br/>Pattern: <code>^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[1-2][0-9]|3[0-1])[T,t]([0-1][0-9]|2[0-3]):[0-5][0-9]:([0-5][0-9]|60)([.][0-9]+)?([Zz]|[+-][0-9]{2}:[0-9]{2})$</code> */
  end: string;
}
export const DateTimeRange = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    start: S.String,
    end: S.String,
  }),
).annotate({ identifier: "DateTimeRange" }) as any as S.Schema<DateTimeRange>;

/** A CSV file of fields to return for the user, if available. Because the invoice object can be very large, field filtering is required. Valid collection fields are <code>items</code>, <code>payments</code>, <code>refunds</code>, <code>additional_recipients_info</code>, and <code>attachments</code>. */
export type FieldsList = Array<string>;
export const FieldsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<FieldsList>;

export interface InvoicesSearchInvoicesRequest {
  /** The page number to be retrieved, for the list of templates. So, a combination of `page=1` and `page_size=20` returns the first 20 templates. A combination of `page=2` and `page_size=20` returns the next 20 templates. */
  page?: number;
  /** The maximum number of templates to return in the response. */
  page_size?: number;
  /** Indicates whether the to show <code>total_pages</code> and <code>total_items</code> in the response. */
  total_required?: boolean;
  /** Filters the search by the email address. */
  recipient_email?: string;
  /** Filters the search by the recipient first name. */
  recipient_first_name?: string;
  /** Filters the search by the recipient last name. */
  recipient_last_name?: string;
  /** Filters the search by the recipient business name. */
  recipient_business_name?: string;
  /** Filters the search by the invoice number. */
  invoice_number?: string;
  status?: InvoiceStatusList;
  /** The reference data. Includes a Purchase Order (PO) number. */
  reference?: string;
  /** The [three-character ISO-4217 currency code](/docs/integration/direct/rest/currency-codes/) that identifies the currency. */
  currency_code?: string;
  /** A private bookkeeping memo for the user. */
  memo?: string;
  /** Filters the search by the total amount. */
  total_amount_range?: AmountRange;
  /** Filters the search by a date range for the invoice, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  invoice_date_range?: DateRange;
  /** Filters the search by a due date range for the invoice, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  due_date_range?: DateRange;
  payment_date_range?: DateTimeRange;
  /** Filters the search by a creation date range for the invoice, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  creation_date_range?: DateTimeRange;
  /** Indicates whether to list merchant-archived invoices in the response. Value is:<ul><li><code>true</code>. Response lists only merchant-archived invoices.</li><li><code>false</code>. Response lists only unarchived invoices.</li><li><code>null</code>. Response lists all invoices.</li></ul> */
  archived?: boolean;
  fields?: FieldsList;
}
export const InvoicesSearchInvoicesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    page: S.optional(S.Number.pipe(T.Query())),
    page_size: S.optional(S.Number.pipe(T.Query())),
    total_required: S.optional(S.Boolean.pipe(T.Query())),
    recipient_email: S.optional(S.String),
    recipient_first_name: S.optional(S.String),
    recipient_last_name: S.optional(S.String),
    recipient_business_name: S.optional(S.String),
    invoice_number: S.optional(S.String),
    status: S.optional(InvoiceStatusList),
    reference: S.optional(S.String),
    currency_code: S.optional(S.String),
    memo: S.optional(S.String),
    total_amount_range: S.optional(AmountRange),
    invoice_date_range: S.optional(DateRange),
    due_date_range: S.optional(DateRange),
    payment_date_range: S.optional(DateTimeRange),
    creation_date_range: S.optional(DateTimeRange),
    archived: S.optional(S.Boolean),
    fields: S.optional(FieldsList),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/invoicing/search-invoices", code: 200 }),
  ),
).annotate({
  identifier: "InvoicesSearchInvoicesRequest",
}) as any as S.Schema<InvoicesSearchInvoicesRequest>;

export interface InvoicesSendRequest {
  /** The ID of the draft invoice to delete. */
  invoice_id: string;
  /** The subject of the email that is sent as a notification to the recipient.<blockquote><strong>Note:</strong> User-provided values for this field will not be honored and the subject will always be defaulted to a system-defined value.</blockquote> */
  subject?: string;
  /** A note to the payer.<blockquote><strong>Note:</strong> User-provided values for this field will not be honored and the note will always be defaulted to a system-defined value.</blockquote> */
  note?: string;
  /** Indicates whether to send a copy of the email to the merchant. */
  send_to_invoicer?: boolean;
  /** Indicates whether to send a copy of the email to the recipient. */
  send_to_recipient?: boolean;
  additional_recipients?: EmailAddressList;
}
export const InvoicesSendRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_id: S.String.pipe(T.Label()),
    subject: S.optional(S.String),
    note: S.optional(S.String),
    send_to_invoicer: S.optional(S.Boolean),
    send_to_recipient: S.optional(S.Boolean),
    additional_recipients: S.optional(EmailAddressList),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/invoicing/invoices/{invoice_id}/send",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoicesSendRequest",
}) as any as S.Schema<InvoicesSendRequest>;

export interface InvoicesUpdateRequest {
  /** The ID of the draft invoice to delete. */
  invoice_id: string;
  /** Indicates whether to send the invoice update notification to the recipient. */
  send_to_recipient?: boolean;
  /** Indicates whether to send the invoice update notification to the merchant. */
  send_to_invoicer?: boolean;
  /** The details of the invoice. Includes the invoice number, date, payment terms, and audit metadata. */
  detail: InvoiceDetailInput;
  /** The invoicer information. Includes the business name, email, address, phone, fax, tax ID, additional notes, and logo URL. */
  invoicer?: InvoicerInfo;
  primary_recipients?: RecipientInfoList;
  additional_recipients?: EmailAddressList;
  items?: ItemListInput;
  /** The invoice configuration details. Includes partial payment, tip, and tax calculated after discount. */
  configuration?: Configuration;
  /** The invoice amount summary of item total, discount, tax total and shipping.. */
  amount?: AmountSummaryDetail;
  /** List of payments registered against the invoice.. */
  payments?: PaymentsInput;
  /** List of refunds against this invoice. The invoicing refund details includes refund type, date, amount, and method. */
  refunds?: PaymentsInput;
}
export const InvoicesUpdateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_id: S.String.pipe(T.Label()),
    send_to_recipient: S.optional(S.Boolean.pipe(T.Query())),
    send_to_invoicer: S.optional(S.Boolean.pipe(T.Query())),
    detail: InvoiceDetailInput,
    invoicer: S.optional(InvoicerInfo),
    primary_recipients: S.optional(RecipientInfoList),
    additional_recipients: S.optional(EmailAddressList),
    items: S.optional(ItemListInput),
    configuration: S.optional(Configuration),
    amount: S.optional(AmountSummaryDetail),
    payments: S.optional(PaymentsInput),
    refunds: S.optional(PaymentsInput),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/v2/invoicing/invoices/{invoice_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InvoicesUpdateRequest",
}) as any as S.Schema<InvoicesUpdateRequest>;

export interface InvoicingGenerateNextInvoiceNumberRequest {
  /** Optional to decide the number or ID. */
  fetch_id?: boolean;
}
export const InvoicingGenerateNextInvoiceNumberRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      fetch_id: S.optional(S.Boolean),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/v2/invoicing/generate-next-invoice-number",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "InvoicingGenerateNextInvoiceNumberRequest",
  }) as any as S.Schema<InvoicingGenerateNextInvoiceNumberRequest>;

/** The invoice number. */
export interface InvoiceNumber {
  /** The invoice number. If you omit this value, the default is the auto-incremented number from the last number. */
  invoice_number?: string;
  /** Resource Id. */
  invoice_id?: string;
}
export const InvoiceNumber = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    invoice_number: S.optional(S.String),
    invoice_id: S.optional(S.String),
  }),
).annotate({ identifier: "InvoiceNumber" }) as any as S.Schema<InvoiceNumber>;

/** The payment term of the invoice. Payment can be due upon receipt, a specified date, or in a set number of days. */
export interface PaymentTerm {
  /** The payment term. Payment can be due upon receipt, a specified date, or in a set number of days. */
  term_type?: PaymentTermType | (string & {});
}
export const PaymentTerm = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    term_type: S.optional(PaymentTermType),
  }),
).annotate({ identifier: "PaymentTerm" }) as any as S.Schema<PaymentTerm>;

/** The audit metadata. Captures all template actions on create and update. */
export type TemplateMetadataInput = PaymentsInput;
export const TemplateMetadataInput = PaymentsInput;

/** The template-related details. Includes notes, terms and conditions, memo, and attachments. */
export interface TemplateDetailInput {
  /** The reference data. Includes a Purchase Order (PO) number. */
  reference?: string;
  /** The [three-character ISO-4217 currency code](/docs/integration/direct/rest/currency-codes/) that identifies the currency. */
  currency_code: string;
  /** A note to the invoice recipient. Also appears on the invoice notification email. */
  note?: string;
  /** The general terms of the invoice. Can include return or cancellation policy and other terms and conditions. */
  terms_and_conditions?: string;
  /** A private bookkeeping memo for the user. */
  memo?: string;
  attachments?: FileReferenceList;
  /** The payment due date for the invoice. Value is either but not both <code>term_type</code> or <code>due_date</code>. */
  payment_term?: PaymentTerm;
  /** The audit metadata. Captures all template actions on create and update. */
  metadata?: PaymentsInput;
}
export const TemplateDetailInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    reference: S.optional(S.String),
    currency_code: S.String,
    note: S.optional(S.String),
    terms_and_conditions: S.optional(S.String),
    memo: S.optional(S.String),
    attachments: S.optional(FileReferenceList),
    payment_term: S.optional(PaymentTerm),
    metadata: S.optional(PaymentsInput),
  }),
).annotate({
  identifier: "TemplateDetailInput",
}) as any as S.Schema<TemplateDetailInput>;

/** The billing and shipping information. Includes name, email, address, phone, and language. */
export type DefinitionsRecipientInfoList = Array<RecipientInfo>;
export const DefinitionsRecipientInfoList = /*@__PURE__*/ S.Array(
  RecipientInfo,
) as any as S.Schema<DefinitionsRecipientInfoList>;

/** An array of invoice line-item information. */
export type DefinitionsItemListInput = Array<ItemInput>;
export const DefinitionsItemListInput = /*@__PURE__*/ S.Array(
  ItemInput,
) as any as S.Schema<DefinitionsItemListInput>;

/** The template configuration details. Includes tax information, tip, and partial payment. */
export interface TemplateConfiguration {
  /** Indicates whether the tax is calculated before or after a discount. If `false`, the tax is calculated before a discount. If `true`, the tax is calculated after a discount. */
  tax_calculated_after_discount?: boolean;
  /** Indicates whether the unit price includes tax. */
  tax_inclusive?: boolean;
  /** Indicates whether the invoice enables the customer to enter a tip amount during payment. If `true`, the invoice shows a tip amount field so that the customer can enter a tip amount. If `false`, the invoice does not show a tip amount field.<blockquote><strong>Note:</strong> This feature is not available for users in `Hong Kong`, `Taiwan`, `India`, or `Japan`.</blockquote> */
  allow_tip?: boolean;
  /** The partial payment details. Includes the minimum amount that the invoicer wants the payer to pay. */
  partial_payment?: PartialPayment;
  /** Indicates whether conditional pricing rules are applied to the invoice. If `true`, pricing rules (such as discounts or surcharges based on specific conditions) are applied. If `false`, no conditional pricing rules are applied. */
  has_conditional_rule?: boolean;
}
export const TemplateConfiguration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tax_calculated_after_discount: S.optional(S.Boolean),
    tax_inclusive: S.optional(S.Boolean),
    allow_tip: S.optional(S.Boolean),
    partial_payment: S.optional(PartialPayment),
    has_conditional_rule: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "TemplateConfiguration",
}) as any as S.Schema<TemplateConfiguration>;

/** The template details. Includes invoicer business information, invoice recipients, items, and configuration. */
export interface TemplateInfoInput {
  /** The template-related details. Includes notes, terms and conditions, memo, and attachments. */
  detail?: TemplateDetailInput;
  /** The invoicer information. Includes business name, email, address, phone, fax, tax ID, additional notes, and logo URL. */
  invoicer?: InvoicerInfo;
  primary_recipients?: DefinitionsRecipientInfoList;
  additional_recipients?: EmailAddressList;
  items?: DefinitionsItemListInput;
  /** The template configuration details. Includes tax information, tip, and partial payment. */
  configuration?: TemplateConfiguration;
  /** The invoice amount summary of the item total, discount, tax total, and shipping. */
  amount?: AmountSummaryDetail;
}
export const TemplateInfoInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    detail: S.optional(TemplateDetailInput),
    invoicer: S.optional(InvoicerInfo),
    primary_recipients: S.optional(DefinitionsRecipientInfoList),
    additional_recipients: S.optional(EmailAddressList),
    items: S.optional(DefinitionsItemListInput),
    configuration: S.optional(TemplateConfiguration),
    amount: S.optional(AmountSummaryDetail),
  }),
).annotate({
  identifier: "TemplateInfoInput",
}) as any as S.Schema<TemplateInfoInput>;

/** The field names for the invoice line items in the template. */
export type TemplateItemField =
  | "ITEMS_QUANTITY"
  | "ITEMS_DESCRIPTION"
  | "ITEMS_DATE"
  | "ITEMS_DISCOUNT"
  | "ITEMS_TAX";
export const TemplateItemField = /*@__PURE__*/ S.String;

/** The template display preference. */
export interface TemplateDisplayPreference {
  /** Indicates whether to show or hide this field. */
  hidden?: boolean;
}
export const TemplateDisplayPreference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    hidden: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "TemplateDisplayPreference",
}) as any as S.Schema<TemplateDisplayPreference>;

/** The template item setting. Sets a template as the default template or edit template. */
export interface TemplateItemSetting {
  /** The field name in `template_data` for which to map corresponding display preferences. */
  field_name?: TemplateItemField | (string & {});
  /** The display preference. */
  display_preference?: TemplateDisplayPreference;
}
export const TemplateItemSetting = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    field_name: S.optional(TemplateItemField),
    display_preference: S.optional(TemplateDisplayPreference),
  }),
).annotate({
  identifier: "TemplateItemSetting",
}) as any as S.Schema<TemplateItemSetting>;

/** The template item headers display preference. */
export type TemplateItemSettingList = Array<TemplateItemSetting>;
export const TemplateItemSettingList = /*@__PURE__*/ S.Array(
  TemplateItemSetting,
) as any as S.Schema<TemplateItemSettingList>;

/** The field names in the template for discount, shipping, and custom amounts. */
export type TemplateSubtotalField = "DISCOUNT" | "SHIPPING" | "CUSTOM";
export const TemplateSubtotalField = /*@__PURE__*/ S.String;

/** The template subtotal setting. Includes the field name and display preference. */
export interface TemplateSubtotalSetting {
  /** The field name in `template_data` for which to map corresponding display preferences. */
  field_name?: TemplateSubtotalField | (string & {});
  /** The display preference. */
  display_preference?: TemplateDisplayPreference;
}
export const TemplateSubtotalSetting = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    field_name: S.optional(TemplateSubtotalField),
    display_preference: S.optional(TemplateDisplayPreference),
  }),
).annotate({
  identifier: "TemplateSubtotalSetting",
}) as any as S.Schema<TemplateSubtotalSetting>;

/** The template subtotal headers display preference. */
export type TemplateSubtotalSettingList = Array<TemplateSubtotalSetting>;
export const TemplateSubtotalSettingList = /*@__PURE__*/ S.Array(
  TemplateSubtotalSetting,
) as any as S.Schema<TemplateSubtotalSettingList>;

/** The template settings. Sets a template as the default template or edit template. */
export interface TemplateSettings {
  template_item_settings?: TemplateItemSettingList;
  template_subtotal_settings?: TemplateSubtotalSettingList;
}
export const TemplateSettings = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    template_item_settings: S.optional(TemplateItemSettingList),
    template_subtotal_settings: S.optional(TemplateSubtotalSettingList),
  }),
).annotate({
  identifier: "TemplateSettings",
}) as any as S.Schema<TemplateSettings>;

export interface TemplatesCreateRequest {
  /** The template name.<blockquote><strong>Note:</strong> The template name must be unique.</blockquote> */
  name?: string;
  /** Indicates whether this template is the default template. A invoicer can have one default template. */
  default_template?: boolean;
  /** The template details. Includes invoicer business information, invoice recipients, items, and configuration. */
  template_info?: TemplateInfoInput;
  /** The template settings. Describes which fields to show or hide when you create an invoice. */
  settings?: TemplateSettings;
  /** The unit of measure for the template. Value is quantity, hours, or amount. */
  unit_of_measure?: UnitOfMeasure | (string & {});
}
export const TemplatesCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    default_template: S.optional(S.Boolean),
    template_info: S.optional(TemplateInfoInput),
    settings: S.optional(TemplateSettings),
    unit_of_measure: S.optional(UnitOfMeasure),
  }).pipe(
    T.Http({ method: "POST", uri: "/v2/invoicing/templates", code: 200 }),
  ),
).annotate({
  identifier: "TemplatesCreateRequest",
}) as any as S.Schema<TemplatesCreateRequest>;

/** The audit metadata. Captures all template actions on create and update. */
export interface TemplateMetadata {
  /** The date and time when the resource was created, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  create_time?: string;
  /** The email address of the account that created the resource. */
  created_by?: string;
  /** The date and time when the resource was last edited, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  last_update_time?: string;
  /** The email address of the account that last edited the resource. */
  last_updated_by?: string;
}
export const TemplateMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    create_time: S.optional(S.String),
    created_by: S.optional(S.String),
    last_update_time: S.optional(S.String),
    last_updated_by: S.optional(S.String),
  }),
).annotate({
  identifier: "TemplateMetadata",
}) as any as S.Schema<TemplateMetadata>;

/** The template-related details. Includes notes, terms and conditions, memo, and attachments. */
export interface TemplateDetail {
  /** The reference data. Includes a Purchase Order (PO) number. */
  reference?: string;
  /** The [three-character ISO-4217 currency code](/docs/integration/direct/rest/currency-codes/) that identifies the currency. */
  currency_code: string;
  /** A note to the invoice recipient. Also appears on the invoice notification email. */
  note?: string;
  /** The general terms of the invoice. Can include return or cancellation policy and other terms and conditions. */
  terms_and_conditions?: string;
  /** A private bookkeeping memo for the user. */
  memo?: string;
  attachments?: FileReferenceList;
  /** The payment due date for the invoice. Value is either but not both <code>term_type</code> or <code>due_date</code>. */
  payment_term?: PaymentTerm;
  /** The audit metadata. Captures all template actions on create and update. */
  metadata?: TemplateMetadata;
}
export const TemplateDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    reference: S.optional(S.String),
    currency_code: S.String,
    note: S.optional(S.String),
    terms_and_conditions: S.optional(S.String),
    memo: S.optional(S.String),
    attachments: S.optional(FileReferenceList),
    payment_term: S.optional(PaymentTerm),
    metadata: S.optional(TemplateMetadata),
  }),
).annotate({ identifier: "TemplateDetail" }) as any as S.Schema<TemplateDetail>;

/** An array of invoice line-item information. */
export type DefinitionsItemList = Array<Item>;
export const DefinitionsItemList = /*@__PURE__*/ S.Array(
  Item,
) as any as S.Schema<DefinitionsItemList>;

/** The template details. Includes invoicer business information, invoice recipients, items, and configuration. */
export interface TemplateInfo {
  /** The template-related details. Includes notes, terms and conditions, memo, and attachments. */
  detail?: TemplateDetail;
  /** The invoicer information. Includes business name, email, address, phone, fax, tax ID, additional notes, and logo URL. */
  invoicer?: InvoicerInfo;
  primary_recipients?: DefinitionsRecipientInfoList;
  additional_recipients?: EmailAddressList;
  items?: DefinitionsItemList;
  /** The template configuration details. Includes tax information, tip, and partial payment. */
  configuration?: TemplateConfiguration;
  /** The invoice amount summary of the item total, discount, tax total, and shipping. */
  amount?: AmountSummaryDetail;
  /** The amount due for the invoice. */
  due_amount?: Money;
}
export const TemplateInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    detail: S.optional(TemplateDetail),
    invoicer: S.optional(InvoicerInfo),
    primary_recipients: S.optional(DefinitionsRecipientInfoList),
    additional_recipients: S.optional(EmailAddressList),
    items: S.optional(DefinitionsItemList),
    configuration: S.optional(TemplateConfiguration),
    amount: S.optional(AmountSummaryDetail),
    due_amount: S.optional(Money),
  }),
).annotate({ identifier: "TemplateInfo" }) as any as S.Schema<TemplateInfo>;

/** The template with invoice details to load with all captured fields. */
export interface Template {
  /** The ID of the template. */
  id?: string;
  /** The template name.<blockquote><strong>Note:</strong> The template name must be unique.</blockquote> */
  name?: string;
  /** Indicates whether this template is the default template. A invoicer can have one default template. */
  default_template?: boolean;
  /** The template details. Includes invoicer business information, invoice recipients, items, and configuration. */
  template_info?: TemplateInfo;
  /** The template settings. Describes which fields to show or hide when you create an invoice. */
  settings?: TemplateSettings;
  /** The unit of measure for the template. Value is quantity, hours, or amount. */
  unit_of_measure?: UnitOfMeasure;
  /** Indicates whether this template is a invoicer-created custom template. The system generates non-custom templates. */
  standard_template?: boolean;
  links?: LinkDescriptionList;
}
export const Template = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    name: S.optional(S.String),
    default_template: S.optional(S.Boolean),
    template_info: S.optional(TemplateInfo),
    settings: S.optional(TemplateSettings),
    unit_of_measure: S.optional(UnitOfMeasure),
    standard_template: S.optional(S.Boolean),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({ identifier: "Template" }) as any as S.Schema<Template>;

export interface TemplatesDeleteRequest {
  /** The ID of the template to delete. */
  template_id: string;
}
export const TemplatesDeleteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    template_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/invoicing/templates/{template_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "TemplatesDeleteRequest",
}) as any as S.Schema<TemplatesDeleteRequest>;

export interface TemplatesDeleteResponse {}
export const TemplatesDeleteResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "TemplatesDeleteResponse",
}) as any as S.Schema<TemplatesDeleteResponse>;

export interface TemplatesGetRequest {
  /** The ID of the template to delete. */
  template_id: string;
}
export const TemplatesGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    template_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/invoicing/templates/{template_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "TemplatesGetRequest",
}) as any as S.Schema<TemplatesGetRequest>;

export interface TemplatesListRequest {
  /** The fields to return in the response. Value is `all` or `none`. To return only the template name, ID, and default attributes, specify `none`. */
  fields?: string;
  /** The page number to be retrieved, for the list of templates. So, a combination of `page=1` and `page_size=20` returns the first 20 templates. A combination of `page=2` and `page_size=20` returns the next 20 templates. */
  page?: number;
  /** The maximum number of templates to return in the response. */
  page_size?: number;
}
export const TemplatesListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    fields: S.optional(S.String.pipe(T.Query())),
    page: S.optional(S.Number.pipe(T.Query())),
    page_size: S.optional(S.Number.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v2/invoicing/templates", code: 200 })),
).annotate({
  identifier: "TemplatesListRequest",
}) as any as S.Schema<TemplatesListRequest>;

/** An array of addresses in the user's PayPal profile. */
export type AddressPortableList = Array<AddressPortable>;
export const AddressPortableList = /*@__PURE__*/ S.Array(
  AddressPortable,
) as any as S.Schema<AddressPortableList>;

/** An array of phone numbers in the user's PayPal profile. */
export type TemplatesDefinitionsPhoneDetailList = Array<PhoneDetail>;
export const TemplatesDefinitionsPhoneDetailList = /*@__PURE__*/ S.Array(
  PhoneDetail,
) as any as S.Schema<TemplatesDefinitionsPhoneDetailList>;

/** An array of details for each template. If `fields` is `none`, returns only the template name, ID, and default status. */
export type TemplateList = Array<Template>;
export const TemplateList = /*@__PURE__*/ S.Array(
  Template,
) as any as S.Schema<TemplateList>;

/** An array of merchant-created templates with associated details that include the emails, addresses, and phone numbers from the user's PayPal profile. */
export interface Templates {
  addresses?: AddressPortableList;
  /** An array of emails in the user's PayPal profile. */
  emails?: string;
  phones?: TemplatesDefinitionsPhoneDetailList;
  templates?: TemplateList;
  links?: LinkDescriptionList;
}
export const Templates = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    addresses: S.optional(AddressPortableList),
    emails: S.optional(S.String),
    phones: S.optional(TemplatesDefinitionsPhoneDetailList),
    templates: S.optional(TemplateList),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({ identifier: "Templates" }) as any as S.Schema<Templates>;

export interface TemplatesUpdateRequest {
  /** The ID of the template to delete. */
  template_id: string;
  /** The template name.<blockquote><strong>Note:</strong> The template name must be unique.</blockquote> */
  name?: string;
  /** Indicates whether this template is the default template. A invoicer can have one default template. */
  default_template?: boolean;
  /** The template details. Includes invoicer business information, invoice recipients, items, and configuration. */
  template_info?: TemplateInfoInput;
  /** The template settings. Describes which fields to show or hide when you create an invoice. */
  settings?: TemplateSettings;
  /** The unit of measure for the template. Value is quantity, hours, or amount. */
  unit_of_measure?: UnitOfMeasure | (string & {});
}
export const TemplatesUpdateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    template_id: S.String.pipe(T.Label()),
    name: S.optional(S.String),
    default_template: S.optional(S.Boolean),
    template_info: S.optional(TemplateInfoInput),
    settings: S.optional(TemplateSettings),
    unit_of_measure: S.optional(UnitOfMeasure),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/v2/invoicing/templates/{template_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "TemplatesUpdateRequest",
}) as any as S.Schema<TemplatesUpdateRequest>;

export type ConnectionsGetError =
  | BadRequest
  | Forbidden
  | NotFound
  | PaypalOpError;
/** List Connections. Lists connections to accounting platforms per merchant. */
export const connectionsGet: API.OperationMethod<
  ConnectionsGetRequest,
  Connections,
  ConnectionsGetError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ConnectionsGetRequest,
  output: Connections,
  errors: [BadRequest, Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoiceConnectionDetailsGetError =
  | BadRequest
  | Forbidden
  | NotFound
  | PaypalOpError;
/** List Connection Status for an Invoice. Lists connections to accounting platforms per invoice. */
export const invoiceConnectionDetailsGet: API.OperationMethod<
  InvoiceConnectionDetailsGetRequest,
  InvoiceConnectionDetails,
  InvoiceConnectionDetailsGetError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoiceConnectionDetailsGetRequest,
  output: InvoiceConnectionDetails,
  errors: [BadRequest, Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesCancelError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Cancel sent invoice Cancels a sent invoice, by ID, and, optionally, sends a notification about the cancellation to the payer, merchant, and CC: emails. */
export const invoicesCancel: API.OperationMethod<
  InvoicesCancelRequest,
  InvoicesCancelResponse,
  InvoicesCancelError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesCancelRequest,
  output: InvoicesCancelResponse,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesCreateError =
  | BadRequest
  | Forbidden
  | UnprocessableEntity
  | PaypalOpError;
/** Create draft invoice Creates a draft invoice. To move the invoice from a draft to payable state, you must <a href="#invoices_send">send the invoice</a>.<br/><br/>In the JSON request body, include invoice details including merchant information. The <code>invoice</code> object must include an <code>items</code> array.<blockquote><strong>Note:</strong> The merchant that you specify in an invoice must have a PayPal account in good standing.</blockquote>. */
export const invoicesCreate: API.OperationMethod<
  InvoicesCreateRequest,
  Invoice,
  InvoicesCreateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesCreateRequest,
  output: Invoice,
  errors: [BadRequest, Forbidden, UnprocessableEntity, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesDeleteError =
  | BadRequest
  | Forbidden
  | NotFound
  | PaypalOpError;
/** Delete invoice Deletes a draft or scheduled invoice, by ID. Deletes invoices in the draft or scheduled state only. For invoices that have already been sent, you can <a href="/docs/api/invoicing/v2/#invoices_cancel">cancel the invoice</a>. After you delete a draft or scheduled invoice, you can no longer use it or show its details. However, you can reuse its invoice number. */
export const invoicesDelete: API.OperationMethod<
  InvoicesDeleteRequest,
  InvoicesDeleteResponse,
  InvoicesDeleteError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesDeleteRequest,
  output: InvoicesDeleteResponse,
  errors: [BadRequest, Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesGenerateQrCodeError =
  | BadRequest
  | Forbidden
  | NotFound
  | PaypalOpError;
/** Generate QR code Generates a QR code for an invoice, by ID. The QR code is a PNG image in <a href="https://www.base64encode.org/">Base64-encoded</a> format that corresponds to the invoice ID. You can generate a QR code for an invoice and add it to a paper or PDF invoice. When customers use their mobile devices to scan the QR code, they are redirected to the PayPal mobile payment flow where they can view the invoice and pay online with PayPal or a credit card. Before you get a QR code, you must <a href="#invoices_create">create an invoice</a> and <a href="#invoices_send">send an invoice</a> to move the invoice from a draft to payable state. Do not include an email address if you do not want the invoice emailed. */
export const invoicesGenerateQrCode: API.OperationMethod<
  InvoicesGenerateQrCodeRequest,
  InvoicesGenerateQrCodeResponse,
  InvoicesGenerateQrCodeError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesGenerateQrCodeRequest,
  output: InvoicesGenerateQrCodeResponse,
  errors: [BadRequest, Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesGetError =
  | BadRequest
  | Forbidden
  | NotFound
  | PaypalOpError;
/** Show invoice details Shows details for an invoice, by ID. */
export const invoicesGet: API.OperationMethod<
  InvoicesGetRequest,
  Invoice,
  InvoicesGetError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesGetRequest,
  output: Invoice,
  errors: [BadRequest, Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesListError = BadRequest | Forbidden | PaypalOpError;
/** List invoices Lists invoices. To filter the invoices that appear in the response, you can specify one or more optional query parameters. */
export const invoicesList: API.OperationMethod<
  InvoicesListRequest,
  Invoices,
  InvoicesListError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesListRequest,
  output: Invoices,
  errors: [BadRequest, Forbidden, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesPaymentsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Record payment for invoice Records a payment for the invoice. If no payment is due, the invoice is marked as `PAID`. Otherwise, the invoice is marked as `PARTIALLY PAID`. */
export const invoicesPayments: API.OperationMethod<
  InvoicesPaymentsRequest,
  PaymentReference,
  InvoicesPaymentsError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesPaymentsRequest,
  output: PaymentReference,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesPaymentsDeleteError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Delete external payment Deletes an external payment, by invoice ID and transaction ID. */
export const invoicesPaymentsDelete: API.OperationMethod<
  InvoicesPaymentsDeleteRequest,
  InvoicesPaymentsDeleteResponse,
  InvoicesPaymentsDeleteError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesPaymentsDeleteRequest,
  output: InvoicesPaymentsDeleteResponse,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesRefundsError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Record refund for invoice Records a refund for the invoice. If all payments are refunded, the invoice is marked as `REFUNDED`. Otherwise, the invoice is marked as `PARTIALLY REFUNDED`. */
export const invoicesRefunds: API.OperationMethod<
  InvoicesRefundsRequest,
  RefundReference,
  InvoicesRefundsError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesRefundsRequest,
  output: RefundReference,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesRefundsDeleteError =
  | BadRequest
  | Forbidden
  | NotFound
  | PaypalOpError;
/** Delete external refund Deletes an external refund, by invoice ID and transaction ID. */
export const invoicesRefundsDelete: API.OperationMethod<
  InvoicesRefundsDeleteRequest,
  InvoicesRefundsDeleteResponse,
  InvoicesRefundsDeleteError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesRefundsDeleteRequest,
  output: InvoicesRefundsDeleteResponse,
  errors: [BadRequest, Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesRemindError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Send invoice reminder Sends a reminder to the payer about an invoice, by ID. In the JSON request body, include a `notification` object that defines the subject of the reminder and other details. <blockquote><strong>Notes:</strong><ul><li>API caller can send only 2 reminders in a day.</li></ul></blockquote>. */
export const invoicesRemind: API.OperationMethod<
  InvoicesRemindRequest,
  InvoicesRemindResponse,
  InvoicesRemindError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesRemindRequest,
  output: InvoicesRemindResponse,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesSearchInvoicesError =
  | BadRequest
  | Forbidden
  | PaypalOpError;
/** Search for invoices Searches for and lists invoices that match search criteria. If you pass multiple criteria, the response lists invoices that match all criteria. */
export const invoicesSearchInvoices: API.OperationMethod<
  InvoicesSearchInvoicesRequest,
  Invoices,
  InvoicesSearchInvoicesError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesSearchInvoicesRequest,
  output: Invoices,
  errors: [BadRequest, Forbidden, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesSendError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Send invoice Sends or schedules an invoice, by ID, to be sent to a customer. The action depends on the invoice issue date:<ul><li>If the invoice issue date is current or in the past, sends the invoice immediately.</li><li>If the invoice issue date is in the future, schedules the invoice to be sent on that date.</li></ul>To suppress the merchant's email notification, set the `send_to_invoicer` body parameter to `false`. To send the invoice through a share link and not through PayPal, set the <code>send_to_recipient</code> parameter to <code>false</code> in the <code>notification</code> object. The <code>send_to_recipient</code> parameter does not apply to a future issue date because the invoice is scheduled to be sent through PayPal on that date.<blockquote><strong>Notes:</strong><ul><li>After you send an invoice, resending it has no effect.</li><li>To send a notification for updates, <a href="#invoices_update">update the invoice</a> and set the <code>send_to_recipient</code> body parameter to <code>true</code>.</li></ul></blockquote> */
export const invoicesSend: API.OperationMethod<
  InvoicesSendRequest,
  LinkDescription,
  InvoicesSendError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesSendRequest,
  output: LinkDescription,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicesUpdateError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Fully update invoice Fully updates an invoice, by ID. In the JSON request body, include a complete `invoice` object. This call does not support partial updates. <blockquote><strong>Notes:</strong><ul><li>API caller can change/modify recipient only 2 times in 72 hours.</li></ul></blockquote>. */
export const invoicesUpdate: API.OperationMethod<
  InvoicesUpdateRequest,
  Invoice,
  InvoicesUpdateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicesUpdateRequest,
  output: Invoice,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type InvoicingGenerateNextInvoiceNumberError =
  | BadRequest
  | Forbidden
  | PaypalOpError;
/** Generate invoice number Generates the next invoice number that is available to the merchant. The next invoice number uses the prefix and suffix from the last invoice number and increments the number by one. For example, the next invoice number after `INVOICE-1234` is `INVOICE-1235`. */
export const invoicingGenerateNextInvoiceNumber: API.OperationMethod<
  InvoicingGenerateNextInvoiceNumberRequest,
  InvoiceNumber,
  InvoicingGenerateNextInvoiceNumberError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InvoicingGenerateNextInvoiceNumberRequest,
  output: InvoiceNumber,
  errors: [BadRequest, Forbidden, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type TemplatesCreateError =
  | BadRequest
  | Forbidden
  | UnprocessableEntity
  | PaypalOpError;
/** Create template Creates an invoice template. You can use details from this template to create an invoice. You can create up to 50 templates.<blockquote><strong>Note:</strong> Every merchant starts with three PayPal system templates that are optimized for the unit type billed. The template includes `Quantity`, `Hours`, and `Amount`.</blockquote> */
export const templatesCreate: API.OperationMethod<
  TemplatesCreateRequest,
  Template,
  TemplatesCreateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: TemplatesCreateRequest,
  output: Template,
  errors: [BadRequest, Forbidden, UnprocessableEntity, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type TemplatesDeleteError = Forbidden | NotFound | PaypalOpError;
/** Delete template Deletes a template, by ID. */
export const templatesDelete: API.OperationMethod<
  TemplatesDeleteRequest,
  TemplatesDeleteResponse,
  TemplatesDeleteError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: TemplatesDeleteRequest,
  output: TemplatesDeleteResponse,
  errors: [Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type TemplatesGetError = Forbidden | NotFound | PaypalOpError;
/** Show template details Shows details for a template, by ID. */
export const templatesGet: API.OperationMethod<
  TemplatesGetRequest,
  Template,
  TemplatesGetError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: TemplatesGetRequest,
  output: Template,
  errors: [Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type TemplatesListError = BadRequest | Forbidden | PaypalOpError;
/** List templates Lists merchant-created templates with associated details. The associated details include the emails, addresses, and phone numbers from the user's PayPal profile.<br/>The user can select which values to show in the business information section of their template. */
export const templatesList: API.OperationMethod<
  TemplatesListRequest,
  Templates,
  TemplatesListError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: TemplatesListRequest,
  output: Templates,
  errors: [BadRequest, Forbidden, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type TemplatesUpdateError =
  | BadRequest
  | Forbidden
  | NotFound
  | PaypalOpError;
/** Fully update template Fully updates a template, by ID. In the JSON request body, include a complete `template` object. This call does not support partial updates. */
export const templatesUpdate: API.OperationMethod<
  TemplatesUpdateRequest,
  Template,
  TemplatesUpdateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: TemplatesUpdateRequest,
  output: Template,
  errors: [BadRequest, Forbidden, NotFound, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));
