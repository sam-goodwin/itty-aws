// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-paypal). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  PaypalProtocol,
  type PaypalOpError,
  type PaypalOpContext,
} from "../protocol.ts";
import { UnknownPaypalError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { PaypalOpError, PaypalOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface CustomerPaymentTokensGetRequest {
  /** A unique identifier representing a specific customer in merchant's/partner's system or records. */
  customer_id: string;
  /** A non-negative, non-zero integer indicating the maximum number of results to return at one time. */
  page_size?: number;
  /** A non-negative, non-zero integer representing the page of the results. */
  page?: number;
  /** A boolean indicating total number of items (total_items) and pages (total_pages) are expected to be returned in the response. */
  total_required?: boolean;
}
export const CustomerPaymentTokensGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    customer_id: S.String.pipe(T.Query()),
    page_size: S.optional(S.Number.pipe(T.Query())),
    page: S.optional(S.Number.pipe(T.Query())),
    total_required: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/v3/vault/payment-tokens", code: 200 }),
  ),
).annotate({
  identifier: "CustomerPaymentTokensGetRequest",
}) as any as S.Schema<CustomerPaymentTokensGetRequest>;

/** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
export interface CustomerVaultPaymentTokensResponseCustomer {
  /** The unique ID for a customer in merchant's or partner's system of records. */
  id?: string;
  /** Merchants and partners may already have a data-store where their customer information is persisted. Use merchant_customer_id to associate the PayPal-generated customer.id to your representation of a customer. */
  merchant_customer_id?: string;
}
export const CustomerVaultPaymentTokensResponseCustomer =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      id: S.optional(S.String),
      merchant_customer_id: S.optional(S.String),
    }),
  ).annotate({
    identifier: "CustomerVaultPaymentTokensResponseCustomer",
  }) as any as S.Schema<CustomerVaultPaymentTokensResponseCustomer>;

/** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
export type Customer = CustomerVaultPaymentTokensResponseCustomer;
export const Customer = CustomerVaultPaymentTokensResponseCustomer;

/** The card network or brand. Applies to credit, debit, gift, and payment cards. */
export type CardBrand =
  | "VISA"
  | "MASTERCARD"
  | "DISCOVER"
  | "AMEX"
  | "SOLO"
  | "JCB"
  | "STAR"
  | "DELTA"
  | "SWITCH"
  | "MAESTRO"
  | "CB_NATIONALE"
  | "CONFIGOGA"
  | "CONFIDIS"
  | "ELECTRON"
  | "CETELEM"
  | "CHINA_UNION_PAY"
  | "DINERS"
  | "ELO"
  | "HIPER"
  | "HIPERCARD"
  | "RUPAY"
  | "GE"
  | "SYNCHRONY"
  | "EFTPOS"
  | "UNKNOWN";
export const CardBrand = /*@__PURE__*/ S.String;

/** Address request details. */
export interface CardResponseBillingAddress {
  /** The first line of the address, such as number and street, for example, `173 Drury Lane`. Needed for data entry, and Compliance and Risk checks. This field needs to pass the full address. */
  address_line_1?: string;
  /** The second line of the address, for example, a suite or apartment number. */
  address_line_2?: string;
  /** A city, town, or village. Smaller than `admin_area_level_1`. */
  admin_area_2?: string;
  /** The highest-level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. This data is formatted for postal delivery, for example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A *kanton*.</li></ul> */
  admin_area_1?: string;
  /** The postal code, which is the ZIP code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
  postal_code?: string;
  country_code: string;
  /** The resource ID of the address. */
  id?: string;
}
export const CardResponseBillingAddress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address_line_1: S.optional(S.String),
    address_line_2: S.optional(S.String),
    admin_area_2: S.optional(S.String),
    admin_area_1: S.optional(S.String),
    postal_code: S.optional(S.String),
    country_code: S.String,
    id: S.optional(S.String),
  }),
).annotate({
  identifier: "CardResponseBillingAddress",
}) as any as S.Schema<CardResponseBillingAddress>;

/** Verification status of Card. */
export type CardVerificationStatus = "VERIFIED" | "FAILED";
export const CardVerificationStatus = /*@__PURE__*/ S.String;

/** The currency and amount for a financial transaction, such as a balance or payment due. */
export interface Money {
  currency_code: string;
  /** The value, which might be:<ul><li>An integer for currencies like `JPY` that are not typically fractional.</li><li>A decimal fraction for currencies like `TND` that are subdivided into thousandths.</li></ul>For the required number of decimal places for a currency code, see [Currency Codes](/api/rest/reference/currency-codes/). */
  value: string;
}
export const Money = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    currency_code: S.String,
    value: S.String,
  }),
).annotate({ identifier: "Money" }) as any as S.Schema<Money>;

/** The address verification code for Visa, Discover, Mastercard, or American Express transactions. */
export type CardVerificationDetailsProcessorResponseAvsCode =
  | "A"
  | "B"
  | "C"
  | "D"
  | "E"
  | "F"
  | "G"
  | "I"
  | "M"
  | "N"
  | "P"
  | "R"
  | "S"
  | "U"
  | "W"
  | "X"
  | "Y"
  | "Z"
  | "Null"
  | "0"
  | "1"
  | "2"
  | "3"
  | "4";
export const CardVerificationDetailsProcessorResponseAvsCode =
  /*@__PURE__*/ S.String;

/** The card verification value code for for Visa, Discover, Mastercard, or American Express. */
export type CardVerificationDetailsProcessorResponseCvvCode =
  | "E"
  | "I"
  | "M"
  | "N"
  | "P"
  | "S"
  | "U"
  | "X"
  | "All others"
  | "0"
  | "1"
  | "2"
  | "3"
  | "4";
export const CardVerificationDetailsProcessorResponseCvvCode =
  /*@__PURE__*/ S.String;

/** The processor response information for payment requests, such as direct credit card transactions. */
export interface CardVerificationDetailsProcessorResponse {
  /** The address verification code for Visa, Discover, Mastercard, or American Express transactions. */
  avs_code?: CardVerificationDetailsProcessorResponseAvsCode;
  /** The card verification value code for for Visa, Discover, Mastercard, or American Express. */
  cvv_code?: CardVerificationDetailsProcessorResponseCvvCode;
}
export const CardVerificationDetailsProcessorResponse = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      avs_code: S.optional(CardVerificationDetailsProcessorResponseAvsCode),
      cvv_code: S.optional(CardVerificationDetailsProcessorResponseCvvCode),
    }),
).annotate({
  identifier: "CardVerificationDetailsProcessorResponse",
}) as any as S.Schema<CardVerificationDetailsProcessorResponse>;

/** Card Verification details including the authorization details and 3D SECURE details. */
export interface CardVerificationDetails {
  /** DEPRECATED. This field is DEPRECATED. Please find the network transaction id data in the 'id' field under the 'network_transaction_reference' object instead of the 'verification' object. */
  network_transaction_id?: string;
  /** DEPRECATED. This field is DEPRECATED. Please find the date data in the 'date' field under the 'network_transaction_reference' object instead of the 'verification' object. */
  date?: string;
  /** DEPRECATED. This field is DEPRECATED. Please find the network data in the 'network' field under the 'network_transaction_reference' object instead of the 'verification' object. */
  network?: CardBrand;
  /** DEPRECATED. This field is DEPRECATED. Please find the time data in the 'time' field under the 'network_transaction_reference' object instead of the 'verification' object. */
  time?: string;
  /** The amount used for card verification. */
  amount?: Money;
  /** The processor response information for payment requests, such as direct credit card transactions. */
  processor_response?: CardVerificationDetailsProcessorResponse;
  /** DEPRECATED. This field is DEPRECATED. Please find the 3D secure authentication data in the 'three_d_secure' object under the 'authentication_result' object instead of the 'verification' object. */
  three_d_secure?: unknown;
}
export const CardVerificationDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    network_transaction_id: S.optional(S.String),
    date: S.optional(S.String),
    network: S.optional(CardBrand),
    time: S.optional(S.String),
    amount: S.optional(Money),
    processor_response: S.optional(CardVerificationDetailsProcessorResponse),
    three_d_secure: S.optional(S.Unknown),
  }),
).annotate({
  identifier: "CardVerificationDetails",
}) as any as S.Schema<CardVerificationDetails>;

/** Previous network transaction reference including id and network. */
export interface NetworkTransactionReferenceEntity {
  /** Transaction reference id returned by the scheme. For Visa and Amex, this is the "Tran id" field in response. For MasterCard, this is the "BankNet reference id" field in response. For Discover, this is the "NRID" field in response. The pattern we expect for this field from Visa/Amex/CB/Discover is numeric, Mastercard/BNPP is alphanumeric and Paysecure is alphanumeric with special character -. */
  id: string;
  /** The date that the transaction was authorized by the scheme. This field may not be returned for all networks. MasterCard refers to this field as "BankNet reference date. */
  date?: string;
  /** Name of the card network through which the transaction was routed. */
  network?: CardBrand;
  /** The date and time when the instrument was verified. */
  time?: string;
}
export const NetworkTransactionReferenceEntity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    date: S.optional(S.String),
    network: S.optional(CardBrand),
    time: S.optional(S.String),
  }),
).annotate({
  identifier: "NetworkTransactionReferenceEntity",
}) as any as S.Schema<NetworkTransactionReferenceEntity>;

/** Transactions status result identifier. The outcome of the issuer's authentication. */
export type ParesStatus = "Y" | "N" | "U" | "A" | "C" | "R" | "D" | "I";
export const ParesStatus = /*@__PURE__*/ S.String;

/** Status of Authentication eligibility. */
export type Enrolled = "Y" | "N" | "U" | "B";
export const Enrolled = /*@__PURE__*/ S.String;

/** Results of 3D Secure Authentication. */
export interface ThreeDSecureAuthenticationResponse2 {
  /** The outcome of the issuer's authentication. */
  authentication_status?: ParesStatus;
  /** Status of authentication eligibility. */
  enrollment_status?: Enrolled;
  /** The externally received 3ds authentication id, to be returned in card detokenization response. */
  authentication_id?: string;
}
export const ThreeDSecureAuthenticationResponse2 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    authentication_status: S.optional(ParesStatus),
    enrollment_status: S.optional(Enrolled),
    authentication_id: S.optional(S.String),
  }),
).annotate({
  identifier: "ThreeDSecureAuthenticationResponse2",
}) as any as S.Schema<ThreeDSecureAuthenticationResponse2>;

/** Results of Authentication such as 3D Secure. */
export interface CardResponseAuthenticationResult {
  three_d_secure?: ThreeDSecureAuthenticationResponse2;
}
export const CardResponseAuthenticationResult = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    three_d_secure: S.optional(ThreeDSecureAuthenticationResponse2),
  }),
).annotate({
  identifier: "CardResponseAuthenticationResult",
}) as any as S.Schema<CardResponseAuthenticationResult>;

/** The type of card product assigned to the BIN by the issuer. These values are defined by the issuer and may change over time. Some examples include: PREPAID_GIFT, CONSUMER, CORPORATE. */
export type ProductsList = Array<string>;
export const ProductsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ProductsList>;

/** Bank Identification Number (BIN) details used to fund a payment. */
export interface BinDetails {
  /** The Bank Identification Number (BIN) signifies the number that is being used to identify the granular level details (except the PII information) of the card. */
  bin?: string;
  /** The issuer of the card instrument. */
  issuing_bank?: string;
  /** The [two-character ISO-3166-1 country code](/docs/integration/direct/rest/country-codes/) of the bank. */
  bin_country_code?: string;
  products?: ProductsList;
}
export const BinDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    bin: S.optional(S.String),
    issuing_bank: S.optional(S.String),
    bin_country_code: S.optional(S.String),
    products: S.optional(ProductsList),
  }),
).annotate({ identifier: "BinDetails" }) as any as S.Schema<BinDetails>;

/** Type of card. i.e Credit, Debit and so on. */
export type CardType = "CREDIT" | "DEBIT" | "PREPAID" | "STORE" | "UNKNOWN";
export const CardType = /*@__PURE__*/ S.String;

/** Full representation of a Card Payment Token including network token. */
export interface CardResponse {
  /** The card holder's name as it appears on the card. */
  name?: string;
  /** The last digits of the payment card. */
  last_digits?: string;
  /** The card brand or network. Typically used in the response. */
  brand?: CardBrand;
  /** The card expiration year and month, in [Internet date format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  expiry?: string;
  /** Address request details. */
  billing_address?: CardResponseBillingAddress;
  /** Card Verification status. */
  verification_status?: CardVerificationStatus;
  verification?: CardVerificationDetails;
  network_transaction_reference?: NetworkTransactionReferenceEntity;
  /** Results of Authentication such as 3D Secure. */
  authentication_result?: CardResponseAuthenticationResult;
  /** Bank Identification Number (BIN) details used to fund a payment. */
  bin_details?: BinDetails;
  /** The payment card type. */
  type?: CardType;
  network_token?: unknown;
}
export const CardResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    last_digits: S.optional(S.String),
    brand: S.optional(CardBrand),
    expiry: S.optional(S.String),
    billing_address: S.optional(CardResponseBillingAddress),
    verification_status: S.optional(CardVerificationStatus),
    verification: S.optional(CardVerificationDetails),
    network_transaction_reference: S.optional(
      NetworkTransactionReferenceEntity,
    ),
    authentication_result: S.optional(CardResponseAuthenticationResult),
    bin_details: S.optional(BinDetails),
    type: S.optional(CardType),
    network_token: S.optional(S.Unknown),
  }),
).annotate({ identifier: "CardResponse" }) as any as S.Schema<CardResponse>;

/** Expected business/charge model for the billing agreement. */
export type PaypalWalletResponseUsagePattern =
  | "IMMEDIATE"
  | "DEFERRED"
  | "RECURRING_PREPAID"
  | "RECURRING_POSTPAID"
  | "THRESHOLD_PREPAID"
  | "THRESHOLD_POSTPAID"
  | "UNSCHEDULED_POSTPAID"
  | "UNSCHEDULED_PREPAID"
  | "SUBSCRIPTION_PREPAID"
  | "SUBSCRIPTION_POSTPAID"
  | "INSTALLMENT_POSTPAID"
  | "INSTALLMENT_PREPAID";
export const PaypalWalletResponseUsagePattern = /*@__PURE__*/ S.String;

/** The name of the party. */
export interface PaypalWalletResponseShippingName {
  /** When the party is a person, the party's full name. */
  full_name?: string;
}
export const PaypalWalletResponseShippingName = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    full_name: S.optional(S.String),
  }),
).annotate({
  identifier: "PaypalWalletResponseShippingName",
}) as any as S.Schema<PaypalWalletResponseShippingName>;

/** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
export interface PaypalWalletResponseShippingPhoneNumber {
  /** The country calling code (CC), in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the CC and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  country_code: string;
  /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  national_number: string;
}
export const PaypalWalletResponseShippingPhoneNumber = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      country_code: S.String,
      national_number: S.String,
    }),
).annotate({
  identifier: "PaypalWalletResponseShippingPhoneNumber",
}) as any as S.Schema<PaypalWalletResponseShippingPhoneNumber>;

/** A classification for the method of purchase fulfillment (e.g shipping, in-store pickup, etc). Either `type` or `options` may be present, but not both. */
export type PaypalWalletResponseShippingType =
  | "SHIPPING"
  | "PICKUP_IN_PERSON"
  | "PICKUP_IN_STORE"
  | "PICKUP_FROM_PERSON";
export const PaypalWalletResponseShippingType = /*@__PURE__*/ S.String;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export interface PaypalWalletResponseShippingAddress {
  /** The first line of the address, such as number and street, for example, `173 Drury Lane`. Needed for data entry, and Compliance and Risk checks. This field needs to pass the full address. */
  address_line_1?: string;
  /** The second line of the address, for example, a suite or apartment number. */
  address_line_2?: string;
  /** A city, town, or village. Smaller than `admin_area_level_1`. */
  admin_area_2?: string;
  /** The highest-level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. This data is formatted for postal delivery, for example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A *kanton*.</li></ul> */
  admin_area_1?: string;
  /** The postal code, which is the ZIP code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
  postal_code?: string;
  country_code: string;
}
export const PaypalWalletResponseShippingAddress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address_line_1: S.optional(S.String),
    address_line_2: S.optional(S.String),
    admin_area_2: S.optional(S.String),
    admin_area_1: S.optional(S.String),
    postal_code: S.optional(S.String),
    country_code: S.String,
  }),
).annotate({
  identifier: "PaypalWalletResponseShippingAddress",
}) as any as S.Schema<PaypalWalletResponseShippingAddress>;

/** The shipping details. */
export interface PaypalWalletResponseShipping {
  /** The name of the party. */
  name?: PaypalWalletResponseShippingName;
  /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
  phone_number?: PaypalWalletResponseShippingPhoneNumber;
  /** A classification for the method of purchase fulfillment (e.g shipping, in-store pickup, etc). Either `type` or `options` may be present, but not both. */
  type?: PaypalWalletResponseShippingType;
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  address?: PaypalWalletResponseShippingAddress;
}
export const PaypalWalletResponseShipping = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(PaypalWalletResponseShippingName),
    phone_number: S.optional(PaypalWalletResponseShippingPhoneNumber),
    type: S.optional(PaypalWalletResponseShippingType),
    address: S.optional(PaypalWalletResponseShippingAddress),
  }),
).annotate({
  identifier: "PaypalWalletResponseShipping",
}) as any as S.Schema<PaypalWalletResponseShipping>;

/** The usage type associated with a digital wallet payment token. */
export type PaypalWalletResponseUsageType = "MERCHANT" | "PLATFORM";
export const PaypalWalletResponseUsageType = /*@__PURE__*/ S.String;

/** The customer type associated with a digital wallet payment token. This is to indicate whether the customer acting on the merchant / platform is either a business or a consumer. */
export type PaypalWalletResponseCustomerType = "CONSUMER" | "BUSINESS";
export const PaypalWalletResponseCustomerType = /*@__PURE__*/ S.String;

/** The name of the party. */
export interface PaypalWalletResponseName {
  /** When the party is a person, the party's given, or first, name. */
  given_name?: string;
  /** When the party is a person, the party's surname or family name. Also known as the last name. Required when the party is a person. Use also to store multiple surnames including the matronymic, or mother's, surname. */
  surname?: string;
}
export const PaypalWalletResponseName = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    given_name: S.optional(S.String),
    surname: S.optional(S.String),
  }),
).annotate({
  identifier: "PaypalWalletResponseName",
}) as any as S.Schema<PaypalWalletResponseName>;

/** The phone type. */
export type PhoneType = "FAX" | "HOME" | "MOBILE" | "OTHER" | "PAGER";
export const PhoneType = /*@__PURE__*/ S.String;

/** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
export interface PhoneWithTypePhoneNumber {
  /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  national_number: string;
}
export const PhoneWithTypePhoneNumber = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    national_number: S.String,
  }),
).annotate({
  identifier: "PhoneWithTypePhoneNumber",
}) as any as S.Schema<PhoneWithTypePhoneNumber>;

/** The phone information. */
export interface PhoneWithType {
  phone_type?: PhoneType;
  /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
  phone_number: PhoneWithTypePhoneNumber;
}
export const PhoneWithType = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    phone_type: S.optional(PhoneType),
    phone_number: PhoneWithTypePhoneNumber,
  }),
).annotate({ identifier: "PhoneWithType" }) as any as S.Schema<PhoneWithType>;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export type PaypalWalletResponseAddress = PaypalWalletResponseShippingAddress;
export const PaypalWalletResponseAddress = PaypalWalletResponseShippingAddress;

/** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
export interface Phone {
  /** The country calling code (CC), in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the CC and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  country_code: string;
  /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  national_number: string;
  /** The extension number. */
  extension_number?: string;
}
export const Phone = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    country_code: S.String,
    national_number: S.String,
    extension_number: S.optional(S.String),
  }),
).annotate({ identifier: "Phone" }) as any as S.Schema<Phone>;

export interface PaypalWalletResponse {
  /** The description displayed to the consumer on the approval flow for a digital wallet, as well as on the merchant view of the payment token management experience. exp: PayPal.com. */
  description?: string;
  /** Expected business/charge model for the billing agreement. */
  usage_pattern?: PaypalWalletResponseUsagePattern;
  /** The shipping details. */
  shipping?: PaypalWalletResponseShipping;
  /** Create multiple payment tokens for the same payer, merchant/platform combination. Use this when the customer has not logged in at merchant/platform. The payment token thus generated, can then also be used to create the customer account at merchant/platform. Use this also when multiple payment tokens are required for the same payer, different customer at merchant/platform. This helps to identify customers distinctly even though they may share the same PayPal account. This only applies to PayPal payment source. */
  permit_multiple_payment_tokens?: boolean;
  /** The usage type associated with a digital wallet payment token. */
  usage_type?: PaypalWalletResponseUsageType;
  /** The customer type associated with a digital wallet payment token. This is to indicate whether the customer acting on the merchant / platform is either a business or a consumer. */
  customer_type?: PaypalWalletResponseCustomerType;
  /** The email address of the payer. */
  email_address?: string;
  /** The PayPal-assigned ID for the payer. */
  payer_id?: string;
  /** The name of the party. */
  name?: PaypalWalletResponseName;
  /** The phone number of the customer. Available only when you enable the **Contact Telephone Number** option in the <a href="https://www.paypal.com/cgi-bin/customerprofileweb?cmd=_profile-website-payments">**Profile & Settings**</a> for the merchant's PayPal account. The `phone.phone_number` supports only `national_number`. */
  phone?: PhoneWithType;
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  address?: PaypalWalletResponseShippingAddress;
  /** The account identifier for a PayPal account. */
  account_id?: string;
  /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
  phone_number?: Phone;
}
export const PaypalWalletResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.optional(S.String),
    usage_pattern: S.optional(PaypalWalletResponseUsagePattern),
    shipping: S.optional(PaypalWalletResponseShipping),
    permit_multiple_payment_tokens: S.optional(S.Boolean),
    usage_type: S.optional(PaypalWalletResponseUsageType),
    customer_type: S.optional(PaypalWalletResponseCustomerType),
    email_address: S.optional(S.String),
    payer_id: S.optional(S.String),
    name: S.optional(PaypalWalletResponseName),
    phone: S.optional(PhoneWithType),
    address: S.optional(PaypalWalletResponseShippingAddress),
    account_id: S.optional(S.String),
    phone_number: S.optional(Phone),
  }),
).annotate({
  identifier: "PaypalWalletResponse",
}) as any as S.Schema<PaypalWalletResponse>;

/** Expected business/charge model for the billing agreement. */
export type VenmoResponseUsagePattern =
  | "IMMEDIATE"
  | "DEFERRED"
  | "RECURRING_PREPAID"
  | "RECURRING_POSTPAID"
  | "THRESHOLD_PREPAID"
  | "THRESHOLD_POSTPAID"
  | "UNSCHEDULED_POSTPAID"
  | "UNSCHEDULED_PREPAID"
  | "SUBSCRIPTION_PREPAID"
  | "SUBSCRIPTION_POSTPAID"
  | "INSTALLMENT_POSTPAID"
  | "INSTALLMENT_PREPAID";
export const VenmoResponseUsagePattern = /*@__PURE__*/ S.String;

/** The name of the party. */
export type VenmoResponseShippingName = PaypalWalletResponseShippingName;
export const VenmoResponseShippingName = PaypalWalletResponseShippingName;

/** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
export type VenmoResponseShippingPhoneNumber =
  PaypalWalletResponseShippingPhoneNumber;
export const VenmoResponseShippingPhoneNumber =
  PaypalWalletResponseShippingPhoneNumber;

/** A classification for the method of purchase fulfillment (e.g shipping, in-store pickup, etc). Either `type` or `options` may be present, but not both. */
export type VenmoResponseShippingType =
  | "SHIPPING"
  | "PICKUP_IN_PERSON"
  | "PICKUP_IN_STORE"
  | "PICKUP_FROM_PERSON";
export const VenmoResponseShippingType = /*@__PURE__*/ S.String;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export type VenmoResponseShippingAddress = PaypalWalletResponseShippingAddress;
export const VenmoResponseShippingAddress = PaypalWalletResponseShippingAddress;

/** The shipping details. */
export interface VenmoResponseShipping {
  /** The name of the party. */
  name?: PaypalWalletResponseShippingName;
  /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
  phone_number?: PaypalWalletResponseShippingPhoneNumber;
  /** A classification for the method of purchase fulfillment (e.g shipping, in-store pickup, etc). Either `type` or `options` may be present, but not both. */
  type?: VenmoResponseShippingType;
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  address?: PaypalWalletResponseShippingAddress;
}
export const VenmoResponseShipping = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(PaypalWalletResponseShippingName),
    phone_number: S.optional(PaypalWalletResponseShippingPhoneNumber),
    type: S.optional(VenmoResponseShippingType),
    address: S.optional(PaypalWalletResponseShippingAddress),
  }),
).annotate({
  identifier: "VenmoResponseShipping",
}) as any as S.Schema<VenmoResponseShipping>;

/** The usage type associated with a digital wallet payment token. */
export type VenmoResponseUsageType = "MERCHANT" | "PLATFORM";
export const VenmoResponseUsageType = /*@__PURE__*/ S.String;

/** The customer type associated with a digital wallet payment token. This is to indicate whether the customer acting on the merchant / platform is either a business or a consumer. */
export type VenmoResponseCustomerType = "CONSUMER" | "BUSINESS";
export const VenmoResponseCustomerType = /*@__PURE__*/ S.String;

/** The name of the party. */
export type VenmoResponseName = PaypalWalletResponseName;
export const VenmoResponseName = PaypalWalletResponseName;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export type VenmoResponseAddress = PaypalWalletResponseShippingAddress;
export const VenmoResponseAddress = PaypalWalletResponseShippingAddress;

export interface VenmoResponse {
  /** The description displayed to the consumer on the approval flow for a digital wallet, as well as on the merchant view of the payment token management experience. exp: PayPal.com. */
  description?: string;
  /** Expected business/charge model for the billing agreement. */
  usage_pattern?: VenmoResponseUsagePattern;
  /** The shipping details. */
  shipping?: VenmoResponseShipping;
  /** Create multiple payment tokens for the same payer, merchant/platform combination. Use this when the customer has not logged in at merchant/platform. The payment token thus generated, can then also be used to create the customer account at merchant/platform. Use this also when multiple payment tokens are required for the same payer, different customer at merchant/platform. This helps to identify customers distinctly even though they may share the same PayPal account. This only applies to PayPal payment source. */
  permit_multiple_payment_tokens?: boolean;
  /** The usage type associated with a digital wallet payment token. */
  usage_type?: VenmoResponseUsageType;
  /** The customer type associated with a digital wallet payment token. This is to indicate whether the customer acting on the merchant / platform is either a business or a consumer. */
  customer_type?: VenmoResponseCustomerType;
  /** The email address of the payer. */
  email_address?: string;
  /** The PayPal-assigned ID for the payer. */
  payer_id?: string;
  /** The name of the party. */
  name?: PaypalWalletResponseName;
  /** The phone number of the customer. Available only when you enable the **Contact Telephone Number** option in the <a href="https://www.paypal.com/cgi-bin/customerprofileweb?cmd=_profile-website-payments">**Profile & Settings**</a> for the merchant's PayPal account. The `phone.phone_number` supports only `national_number`. */
  phone?: PhoneWithType;
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  address?: PaypalWalletResponseShippingAddress;
  /** The Venmo username, as chosen by the user. */
  user_name?: string;
}
export const VenmoResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.optional(S.String),
    usage_pattern: S.optional(VenmoResponseUsagePattern),
    shipping: S.optional(VenmoResponseShipping),
    permit_multiple_payment_tokens: S.optional(S.Boolean),
    usage_type: S.optional(VenmoResponseUsageType),
    customer_type: S.optional(VenmoResponseCustomerType),
    email_address: S.optional(S.String),
    payer_id: S.optional(S.String),
    name: S.optional(PaypalWalletResponseName),
    phone: S.optional(PhoneWithType),
    address: S.optional(PaypalWalletResponseShippingAddress),
    user_name: S.optional(S.String),
  }),
).annotate({ identifier: "VenmoResponse" }) as any as S.Schema<VenmoResponse>;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export type ApplePayPaymentTokenResponseCardBillingAddress =
  PaypalWalletResponseShippingAddress;
export const ApplePayPaymentTokenResponseCardBillingAddress =
  PaypalWalletResponseShippingAddress;

/** The payment card to be used to fund a payment. Can be a credit or debit card. */
export interface ApplePayPaymentTokenResponseCard {
  /** The card holder's name as it appears on the card. */
  name?: string;
  /** The last digits of the payment card. */
  last_digits?: string;
  /** The payment card type. */
  type?: CardType;
  /** The card brand or network. Typically used in the response. */
  brand?: CardBrand;
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  billing_address?: PaypalWalletResponseShippingAddress;
}
export const ApplePayPaymentTokenResponseCard = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    last_digits: S.optional(S.String),
    type: S.optional(CardType),
    brand: S.optional(CardBrand),
    billing_address: S.optional(PaypalWalletResponseShippingAddress),
  }),
).annotate({
  identifier: "ApplePayPaymentTokenResponseCard",
}) as any as S.Schema<ApplePayPaymentTokenResponseCard>;

/** A resource representing a response for Apple Pay. */
export interface ApplePayPaymentTokenResponse {
  /** The payment card to be used to fund a payment. Can be a credit or debit card. */
  card?: ApplePayPaymentTokenResponseCard;
}
export const ApplePayPaymentTokenResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    card: S.optional(ApplePayPaymentTokenResponseCard),
  }),
).annotate({
  identifier: "ApplePayPaymentTokenResponse",
}) as any as S.Schema<ApplePayPaymentTokenResponse>;

/** The vaulted payment method details. */
export interface PaymentSource {
  card?: CardResponse;
  paypal?: PaypalWalletResponse;
  venmo?: VenmoResponse;
  apple_pay?: ApplePayPaymentTokenResponse;
}
export const PaymentSource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    card: S.optional(CardResponse),
    paypal: S.optional(PaypalWalletResponse),
    venmo: S.optional(VenmoResponse),
    apple_pay: S.optional(ApplePayPaymentTokenResponse),
  }),
).annotate({ identifier: "PaymentSource" }) as any as S.Schema<PaymentSource>;

/** The HTTP method required to make the related call. */
export type LinkDescriptionMethod =
  | "GET"
  | "POST"
  | "PUT"
  | "DELETE"
  | "HEAD"
  | "CONNECT"
  | "OPTIONS"
  | "PATCH";
export const LinkDescriptionMethod = /*@__PURE__*/ S.String;

/** The request-related [HATEOAS link](/api/rest/responses/#hateoas-links) information. */
export interface LinkDescription {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  /** The HTTP method required to make the related call. */
  method?: LinkDescriptionMethod;
}
export const LinkDescription = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    href: S.String,
    rel: S.String,
    method: S.optional(LinkDescriptionMethod),
  }),
).annotate({
  identifier: "LinkDescription",
}) as any as S.Schema<LinkDescription>;

/** An array of related [HATEOAS links](/api/rest/responses/#hateoas). */
export type LinkDescriptionList = Array<LinkDescription>;
export const LinkDescriptionList = /*@__PURE__*/ S.Array(
  LinkDescription,
) as any as S.Schema<LinkDescriptionList>;

/** Full representation of a saved payment token. */
export interface PaymentTokenResponse {
  /** The PayPal-generated ID for the vault token. */
  id?: string;
  /** Customer in merchant's or partner's system of records. */
  customer?: CustomerVaultPaymentTokensResponseCustomer;
  payment_source?: PaymentSource;
  links?: LinkDescriptionList;
}
export const PaymentTokenResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    customer: S.optional(CustomerVaultPaymentTokensResponseCustomer),
    payment_source: S.optional(PaymentSource),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({
  identifier: "PaymentTokenResponse",
}) as any as S.Schema<PaymentTokenResponse>;

export type PaymentTokenResponseList = Array<PaymentTokenResponse>;
export const PaymentTokenResponseList = /*@__PURE__*/ S.Array(
  PaymentTokenResponse,
) as any as S.Schema<PaymentTokenResponseList>;

/** Collection of payment tokens saved for a given customer. */
export interface CustomerVaultPaymentTokensResponse {
  /** Total number of items. */
  total_items?: number;
  /** Total number of pages. */
  total_pages?: number;
  /** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
  customer?: CustomerVaultPaymentTokensResponseCustomer;
  payment_tokens?: PaymentTokenResponseList;
  links?: LinkDescriptionList;
}
export const CustomerVaultPaymentTokensResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    total_items: S.optional(S.Number),
    total_pages: S.optional(S.Number),
    customer: S.optional(CustomerVaultPaymentTokensResponseCustomer),
    payment_tokens: S.optional(PaymentTokenResponseList),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({
  identifier: "CustomerVaultPaymentTokensResponse",
}) as any as S.Schema<CustomerVaultPaymentTokensResponse>;

/** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
export type PaymentTokensCreateRequestCustomer =
  CustomerVaultPaymentTokensResponseCustomer;
export const PaymentTokensCreateRequestCustomer =
  CustomerVaultPaymentTokensResponseCustomer;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export type DefinitionsPaymentSourceCardBillingAddress =
  PaypalWalletResponseShippingAddress;
export const DefinitionsPaymentSourceCardBillingAddress =
  PaypalWalletResponseShippingAddress;

/** A Resource representing a request to vault a Card. */
export interface DefinitionsPaymentSourceCard {
  /** The card holder's name as it appears on the card. */
  name?: string;
  /** The primary account number (PAN) for the payment card. */
  number?: string;
  /** The card expiration year and month, in [Internet date format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  expiry?: string;
  /** The three- or four-digit security code of the card. Also known as the CVV, CVC, CVN, CVE, or CID. This parameter cannot be present in the request when `payment_initiator=MERCHANT`. */
  security_code?: string;
  /** The card brand or network. Typically used in the response. */
  brand?: CardBrand | (string & {});
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  billing_address?: PaypalWalletResponseShippingAddress;
  network_transaction_reference?: unknown;
}
export const DefinitionsPaymentSourceCard = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    number: S.optional(S.String),
    expiry: S.optional(S.String),
    security_code: S.optional(S.String),
    brand: S.optional(CardBrand),
    billing_address: S.optional(PaypalWalletResponseShippingAddress),
    network_transaction_reference: S.optional(S.Unknown),
  }),
).annotate({
  identifier: "DefinitionsPaymentSourceCard",
}) as any as S.Schema<DefinitionsPaymentSourceCard>;

/** The tokenization method that generated the ID. */
export type TokenIdRequestType = "SETUP_TOKEN";
export const TokenIdRequestType = /*@__PURE__*/ S.String;

/** The Tokenized Payment Source representing a Request to Vault a Token. */
export interface TokenIdRequest {
  /** The PayPal-generated ID for the token. */
  id: string;
  /** The tokenization method that generated the ID. */
  type: TokenIdRequestType | (string & {});
}
export const TokenIdRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    type: TokenIdRequestType,
  }),
).annotate({ identifier: "TokenIdRequest" }) as any as S.Schema<TokenIdRequest>;

/** The payment method to vault with the instrument details. */
export interface DefinitionsPaymentSource {
  /** A Resource representing a request to vault a Card. */
  card?: DefinitionsPaymentSourceCard;
  token?: TokenIdRequest;
}
export const DefinitionsPaymentSource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    card: S.optional(DefinitionsPaymentSourceCard),
    token: S.optional(TokenIdRequest),
  }),
).annotate({
  identifier: "DefinitionsPaymentSource",
}) as any as S.Schema<DefinitionsPaymentSource>;

export interface PaymentTokensCreateRequest {
  /** The server stores keys for 3 hours. */
  payPalRequestId?: string;
  /** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
  customer?: CustomerVaultPaymentTokensResponseCustomer;
  payment_source: DefinitionsPaymentSource;
}
export const PaymentTokensCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    payPalRequestId: S.optional(S.String.pipe(T.Header("PayPal-Request-Id"))),
    customer: S.optional(CustomerVaultPaymentTokensResponseCustomer),
    payment_source: DefinitionsPaymentSource,
  }).pipe(
    T.Http({ method: "POST", uri: "/v3/vault/payment-tokens", code: 200 }),
  ),
).annotate({
  identifier: "PaymentTokensCreateRequest",
}) as any as S.Schema<PaymentTokensCreateRequest>;

export interface PaymentTokensDeleteRequest {
  /** ID of the setup token. */
  id: string;
}
export const PaymentTokensDeleteRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v3/vault/payment-tokens/{id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PaymentTokensDeleteRequest",
}) as any as S.Schema<PaymentTokensDeleteRequest>;

export interface PaymentTokensDeleteResponse {}
export const PaymentTokensDeleteResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "PaymentTokensDeleteResponse",
}) as any as S.Schema<PaymentTokensDeleteResponse>;

export interface PaymentTokensGetRequest {
  /** ID of the setup token. */
  id: string;
}
export const PaymentTokensGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "GET", uri: "/v3/vault/payment-tokens/{id}", code: 200 }),
  ),
).annotate({
  identifier: "PaymentTokensGetRequest",
}) as any as S.Schema<PaymentTokensGetRequest>;

/** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
export type SetupTokensCreateRequestCustomer =
  CustomerVaultPaymentTokensResponseCustomer;
export const SetupTokensCreateRequestCustomer =
  CustomerVaultPaymentTokensResponseCustomer;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export type SetupTokensCreateRequestPaymentSourceCardBillingAddress =
  PaypalWalletResponseShippingAddress;
export const SetupTokensCreateRequestPaymentSourceCardBillingAddress =
  PaypalWalletResponseShippingAddress;

/** The verification method of the card. */
export type CardVerificationMethod = "SCA_WHEN_REQUIRED" | "SCA_ALWAYS";
export const CardVerificationMethod = /*@__PURE__*/ S.String;

/** Vault Instruction on action to be performed after a successful payer approval. */
export type VaultInstruction = "ON_CREATE_PAYMENT_TOKENS" | "ON_PAYER_APPROVAL";
export const VaultInstruction = /*@__PURE__*/ S.String;

/** Customizes the Vault creation flow experience for your customers. */
export interface SetupTokensCreateRequestPaymentSourceCardExperienceContext {
  /** The label that overrides the business name in the PayPal account on the PayPal site. The pattern is defined by an external party and supports Unicode. */
  brand_name?: string;
  /** The BCP 47-formatted locale of pages that the PayPal vaulting experience shows. PayPal supports a five-character code. For example, `DA-DK`, `HE-IL`, `ID-ID`, `JA-JP`, `NO-NO`, `PT-BR`, `RU-RU`, `SV-SE`, `TH-TH`, `ZH-CN`, `ZH-HK`, or `ZH-TW`. */
  locale?: string;
  /** The URL where the customer is redirected after customer approves leaves the flow. It is a required field for contingency flows like PayPal wallet, 3DS. */
  return_url?: string;
  /** The URL where the customer is redirected after customer cancels or leaves the flow. It is a required field for contingency flows like PayPal wallet, 3DS. */
  cancel_url?: string;
  /** Vault Instruction on action to be performed after a successful payer approval. */
  vault_instruction?: VaultInstruction | (string & {});
}
export const SetupTokensCreateRequestPaymentSourceCardExperienceContext =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      brand_name: S.optional(S.String),
      locale: S.optional(S.String),
      return_url: S.optional(S.String),
      cancel_url: S.optional(S.String),
      vault_instruction: S.optional(VaultInstruction),
    }),
  ).annotate({
    identifier: "SetupTokensCreateRequestPaymentSourceCardExperienceContext",
  }) as any as S.Schema<SetupTokensCreateRequestPaymentSourceCardExperienceContext>;

/** A Resource representing a request to vault a Card. */
export interface SetupTokensCreateRequestPaymentSourceCard {
  /** The card holder's name as it appears on the card. */
  name?: string;
  /** The primary account number (PAN) for the payment card. */
  number?: string;
  /** The card expiration year and month, in [Internet date format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  expiry?: string;
  /** The three- or four-digit security code of the card. Also known as the CVV, CVC, CVN, CVE, or CID. This parameter cannot be present in the request when `payment_initiator=MERCHANT`. */
  security_code?: string;
  /** The card brand or network. Typically used in the response. */
  brand?: CardBrand | (string & {});
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  billing_address?: PaypalWalletResponseShippingAddress;
  /** The API caller can opt in to verify the payment token through PayPal offered verification services (e.g. Smart Dollar Auth, 3DS). */
  verification_method?: CardVerificationMethod | (string & {});
  /** Customizes the Vault creation flow experience for your customers. */
  experience_context?: SetupTokensCreateRequestPaymentSourceCardExperienceContext;
  network_transaction_reference?: unknown;
}
export const SetupTokensCreateRequestPaymentSourceCard =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      name: S.optional(S.String),
      number: S.optional(S.String),
      expiry: S.optional(S.String),
      security_code: S.optional(S.String),
      brand: S.optional(CardBrand),
      billing_address: S.optional(PaypalWalletResponseShippingAddress),
      verification_method: S.optional(CardVerificationMethod),
      experience_context: S.optional(
        SetupTokensCreateRequestPaymentSourceCardExperienceContext,
      ),
      network_transaction_reference: S.optional(S.Unknown),
    }),
  ).annotate({
    identifier: "SetupTokensCreateRequestPaymentSourceCard",
  }) as any as S.Schema<SetupTokensCreateRequestPaymentSourceCard>;

/** Expected business/charge model for the billing agreement. */
export type PaypalWalletRequestUsagePattern =
  | "IMMEDIATE"
  | "DEFERRED"
  | "RECURRING_PREPAID"
  | "RECURRING_POSTPAID"
  | "THRESHOLD_PREPAID"
  | "THRESHOLD_POSTPAID"
  | "UNSCHEDULED_POSTPAID"
  | "UNSCHEDULED_PREPAID"
  | "SUBSCRIPTION_PREPAID"
  | "SUBSCRIPTION_POSTPAID"
  | "INSTALLMENT_POSTPAID"
  | "INSTALLMENT_PREPAID";
export const PaypalWalletRequestUsagePattern = /*@__PURE__*/ S.String;

/** The name of the party. */
export type PaypalWalletRequestShippingName = PaypalWalletResponseShippingName;
export const PaypalWalletRequestShippingName = PaypalWalletResponseShippingName;

/** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
export type PaypalWalletRequestShippingPhoneNumber =
  PaypalWalletResponseShippingPhoneNumber;
export const PaypalWalletRequestShippingPhoneNumber =
  PaypalWalletResponseShippingPhoneNumber;

/** A classification for the method of purchase fulfillment (e.g shipping, in-store pickup, etc). Either `type` or `options` may be present, but not both. */
export type PaypalWalletRequestShippingType =
  | "SHIPPING"
  | "PICKUP_IN_PERSON"
  | "PICKUP_IN_STORE"
  | "PICKUP_FROM_PERSON";
export const PaypalWalletRequestShippingType = /*@__PURE__*/ S.String;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export type PaypalWalletRequestShippingAddress =
  PaypalWalletResponseShippingAddress;
export const PaypalWalletRequestShippingAddress =
  PaypalWalletResponseShippingAddress;

/** The shipping details. */
export interface PaypalWalletRequestShipping {
  /** The name of the party. */
  name?: PaypalWalletResponseShippingName;
  /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
  phone_number?: PaypalWalletResponseShippingPhoneNumber;
  /** A classification for the method of purchase fulfillment (e.g shipping, in-store pickup, etc). Either `type` or `options` may be present, but not both. */
  type?: PaypalWalletRequestShippingType | (string & {});
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  address?: PaypalWalletResponseShippingAddress;
}
export const PaypalWalletRequestShipping = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(PaypalWalletResponseShippingName),
    phone_number: S.optional(PaypalWalletResponseShippingPhoneNumber),
    type: S.optional(PaypalWalletRequestShippingType),
    address: S.optional(PaypalWalletResponseShippingAddress),
  }),
).annotate({
  identifier: "PaypalWalletRequestShipping",
}) as any as S.Schema<PaypalWalletRequestShipping>;

/** The usage type associated with a digital wallet payment token. */
export type PaypalWalletRequestUsageType = "MERCHANT" | "PLATFORM";
export const PaypalWalletRequestUsageType = /*@__PURE__*/ S.String;

/** The customer type associated with a digital wallet payment token. This is to indicate whether the customer acting on the merchant / platform is either a business or a consumer. */
export type PaypalWalletRequestCustomerType = "CONSUMER" | "BUSINESS";
export const PaypalWalletRequestCustomerType = /*@__PURE__*/ S.String;

/** The tenure type of the billing cycle identifies if the billing cycle is a trial(free or discounted) or regular billing cycle. */
export type BillingCycleTenureType = "REGULAR" | "TRIAL";
export const BillingCycleTenureType = /*@__PURE__*/ S.String;

/** The pricing model for the billing cycle. */
export type PricingSchemePricingModel = "FIXED" | "VARIABLE" | "AUTO_RELOAD";
export const PricingSchemePricingModel = /*@__PURE__*/ S.String;

/** The pricing scheme details. */
export interface PricingScheme {
  /** The price the customer will be charged based on the pricing model */
  price?: Money;
  /** The pricing model for the billing cycle. */
  pricing_model: PricingSchemePricingModel | (string & {});
  /** The threshold amount on which the reload charge would be triggered. This will be associated with the account-balance where if the account-balance goes below this amount then customer would incur reload charge. */
  reload_threshold_amount?: Money;
}
export const PricingScheme = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    price: S.optional(Money),
    pricing_model: PricingSchemePricingModel,
    reload_threshold_amount: S.optional(Money),
  }),
).annotate({ identifier: "PricingScheme" }) as any as S.Schema<PricingScheme>;

/** The billing cycle providing details of the billing frequency, amount, duration and if the billing cycle is a free, discounted or regular billing cycle. The sequence of the billing cycle will be in the following order - free trial billing cycle(s), discounted trial billing cycle(s), regular billing cycle(s). */
export interface BillingCycle {
  /** The tenure type of the billing cycle identifies if the billing cycle is a trial(free or discounted) or regular billing cycle. */
  tenure_type: BillingCycleTenureType | (string & {});
  /** The active pricing scheme for this billing cycle. A free trial billing cycle does not require a pricing scheme. */
  pricing_scheme?: PricingScheme;
  /** The frequency details for this billing cycle. */
  frequency?: unknown;
  /** The number of times this billing cycle gets executed. Trial billing cycles can only be executed a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). Regular billing cycles can be executed infinite times (value of <code>0</code> for <code>total_cycles</code>) or a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). */
  total_cycles?: number;
  /** The order in which this cycle is to run among other billing cycles. For example, a trial billing cycle has a `sequence` of `1` while a regular billing cycle has a `sequence` of `2`, so that trial cycle runs before the regular cycle. */
  sequence?: number;
  /** The start date for the billing cycle, in YYYY-MM-DD. This field should be not be provided if the billing cycle starts at the time of checkout. When this field is not provided, the billing cycle amount will be included in any data validations confirming that the total provided by the merchant match the sum of individual items due at the time of checkout. Only one billing cycle (with sequence equal to 1) can have a no start date. */
  start_date?: string;
}
export const BillingCycle = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tenure_type: BillingCycleTenureType,
    pricing_scheme: S.optional(PricingScheme),
    frequency: S.optional(S.Unknown),
    total_cycles: S.optional(S.Number),
    sequence: S.optional(S.Number),
    start_date: S.optional(S.String),
  }),
).annotate({ identifier: "BillingCycle" }) as any as S.Schema<BillingCycle>;

/** An array of billing cycles for trial billing and regular billing. A plan can have at most two trial cycles and only one regular cycle. */
export type BillingCycleList = Array<BillingCycle>;
export const BillingCycleList = /*@__PURE__*/ S.Array(
  BillingCycle,
) as any as S.Schema<BillingCycleList>;

/** The one-time charge info at the time of checkout. */
export interface OneTimeCharges {
  /** The setup fee for the recurring plan. */
  setup_fee?: Money;
  /** The shipping amount due at the time of checkout. */
  shipping_amount?: Money;
  /** The taxes due at the time of checkout. */
  taxes?: Money;
  /** The product price for any one-time product purchased at the time of checkout. */
  product_price?: Money;
  /** The total amount at the time of checkout. This is calculated by using the formula x + y + z where x is subtotal, y is shipping-amount and z is taxes. */
  total_amount: Money;
}
export const OneTimeCharges = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    setup_fee: S.optional(Money),
    shipping_amount: S.optional(Money),
    taxes: S.optional(Money),
    product_price: S.optional(Money),
    total_amount: Money,
  }),
).annotate({ identifier: "OneTimeCharges" }) as any as S.Schema<OneTimeCharges>;

/** The merchant level Recurring Billing plan metadata for the Billing Agreement. */
export interface Plan {
  billing_cycles: BillingCycleList;
  /** Product details associated with any one-time product purchase. */
  product?: unknown;
  /** Price and currency for any one-time charges due at plan signup. */
  one_time_charges: OneTimeCharges;
  /** Name of the recurring plan. */
  name?: string;
}
export const Plan = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    billing_cycles: BillingCycleList,
    product: S.optional(S.Unknown),
    one_time_charges: OneTimeCharges,
    name: S.optional(S.String),
  }),
).annotate({ identifier: "Plan" }) as any as S.Schema<Plan>;

/** The shipping preference. This only applies to PayPal payment source. */
export type ExperienceContextShippingPreference =
  | "GET_FROM_FILE"
  | "NO_SHIPPING"
  | "SET_PROVIDED_ADDRESS";
export const ExperienceContextShippingPreference = /*@__PURE__*/ S.String;

/** Customizes the Vault creation flow experience for your customers. */
export interface ExperienceContext {
  /** The label that overrides the business name in the PayPal account on the PayPal site. The pattern is defined by an external party and supports Unicode. */
  brand_name?: string;
  /** The BCP 47-formatted locale of pages that the PayPal vaulting experience shows. PayPal supports a five-character code. For example, `DA-DK`, `HE-IL`, `ID-ID`, `JA-JP`, `NO-NO`, `PT-BR`, `RU-RU`, `SV-SE`, `TH-TH`, `ZH-CN`, `ZH-HK`, or `ZH-TW`. */
  locale?: string;
  /** The URL where the customer is redirected after customer approves leaves the flow. It is a required field for contingency flows like PayPal wallet, 3DS. */
  return_url?: string;
  /** The URL where the customer is redirected after customer cancels or leaves the flow. It is a required field for contingency flows like PayPal wallet, 3DS. */
  cancel_url?: string;
  /** The shipping preference. This only applies to PayPal payment source. */
  shipping_preference?: ExperienceContextShippingPreference | (string & {});
  /** Vault Instruction on action to be performed after a successful payer approval. */
  vault_instruction?: VaultInstruction | (string & {});
}
export const ExperienceContext = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    brand_name: S.optional(S.String),
    locale: S.optional(S.String),
    return_url: S.optional(S.String),
    cancel_url: S.optional(S.String),
    shipping_preference: S.optional(ExperienceContextShippingPreference),
    vault_instruction: S.optional(VaultInstruction),
  }),
).annotate({
  identifier: "ExperienceContext",
}) as any as S.Schema<ExperienceContext>;

/** A resource representing a request to vault PayPal Wallet. */
export interface PaypalWalletRequest {
  /** The description displayed to the consumer on the approval flow for a digital wallet, as well as on the merchant view of the payment token management experience. exp: PayPal.com. */
  description?: string;
  /** Expected business/charge model for the billing agreement. */
  usage_pattern?: PaypalWalletRequestUsagePattern | (string & {});
  /** The shipping details. */
  shipping?: PaypalWalletRequestShipping;
  /** Create multiple payment tokens for the same payer, merchant/platform combination. Use this when the customer has not logged in at merchant/platform. The payment token thus generated, can then also be used to create the customer account at merchant/platform. Use this also when multiple payment tokens are required for the same payer, different customer at merchant/platform. This helps to identify customers distinctly even though they may share the same PayPal account. This only applies to PayPal payment source. */
  permit_multiple_payment_tokens?: boolean;
  /** The usage type associated with a digital wallet payment token. */
  usage_type?: PaypalWalletRequestUsageType | (string & {});
  /** The customer type associated with a digital wallet payment token. This is to indicate whether the customer acting on the merchant / platform is either a business or a consumer. */
  customer_type?: PaypalWalletRequestCustomerType | (string & {});
  billing_plan?: Plan;
  experience_context?: ExperienceContext;
}
export const PaypalWalletRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.optional(S.String),
    usage_pattern: S.optional(PaypalWalletRequestUsagePattern),
    shipping: S.optional(PaypalWalletRequestShipping),
    permit_multiple_payment_tokens: S.optional(S.Boolean),
    usage_type: S.optional(PaypalWalletRequestUsageType),
    customer_type: S.optional(PaypalWalletRequestCustomerType),
    billing_plan: S.optional(Plan),
    experience_context: S.optional(ExperienceContext),
  }),
).annotate({
  identifier: "PaypalWalletRequest",
}) as any as S.Schema<PaypalWalletRequest>;

/** Expected business/charge model for the billing agreement. */
export type VenmoRequestUsagePattern =
  | "IMMEDIATE"
  | "DEFERRED"
  | "RECURRING_PREPAID"
  | "RECURRING_POSTPAID"
  | "THRESHOLD_PREPAID"
  | "THRESHOLD_POSTPAID"
  | "UNSCHEDULED_POSTPAID"
  | "UNSCHEDULED_PREPAID"
  | "SUBSCRIPTION_PREPAID"
  | "SUBSCRIPTION_POSTPAID"
  | "INSTALLMENT_POSTPAID"
  | "INSTALLMENT_PREPAID";
export const VenmoRequestUsagePattern = /*@__PURE__*/ S.String;

/** The name of the party. */
export type VenmoRequestShippingName = PaypalWalletResponseShippingName;
export const VenmoRequestShippingName = PaypalWalletResponseShippingName;

/** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
export type VenmoRequestShippingPhoneNumber =
  PaypalWalletResponseShippingPhoneNumber;
export const VenmoRequestShippingPhoneNumber =
  PaypalWalletResponseShippingPhoneNumber;

/** A classification for the method of purchase fulfillment (e.g shipping, in-store pickup, etc). Either `type` or `options` may be present, but not both. */
export type VenmoRequestShippingType =
  | "SHIPPING"
  | "PICKUP_IN_PERSON"
  | "PICKUP_IN_STORE"
  | "PICKUP_FROM_PERSON";
export const VenmoRequestShippingType = /*@__PURE__*/ S.String;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export type VenmoRequestShippingAddress = PaypalWalletResponseShippingAddress;
export const VenmoRequestShippingAddress = PaypalWalletResponseShippingAddress;

/** The shipping details. */
export interface VenmoRequestShipping {
  /** The name of the party. */
  name?: PaypalWalletResponseShippingName;
  /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
  phone_number?: PaypalWalletResponseShippingPhoneNumber;
  /** A classification for the method of purchase fulfillment (e.g shipping, in-store pickup, etc). Either `type` or `options` may be present, but not both. */
  type?: VenmoRequestShippingType | (string & {});
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  address?: PaypalWalletResponseShippingAddress;
}
export const VenmoRequestShipping = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(PaypalWalletResponseShippingName),
    phone_number: S.optional(PaypalWalletResponseShippingPhoneNumber),
    type: S.optional(VenmoRequestShippingType),
    address: S.optional(PaypalWalletResponseShippingAddress),
  }),
).annotate({
  identifier: "VenmoRequestShipping",
}) as any as S.Schema<VenmoRequestShipping>;

/** The usage type associated with a digital wallet payment token. */
export type VenmoRequestUsageType = "MERCHANT" | "PLATFORM";
export const VenmoRequestUsageType = /*@__PURE__*/ S.String;

/** The customer type associated with a digital wallet payment token. This is to indicate whether the customer acting on the merchant / platform is either a business or a consumer. */
export type VenmoRequestCustomerType = "CONSUMER" | "BUSINESS";
export const VenmoRequestCustomerType = /*@__PURE__*/ S.String;

/** The shipping preference. This only applies to PayPal payment source. */
export type VenmoRequestExperienceContextShippingPreference =
  | "GET_FROM_FILE"
  | "NO_SHIPPING"
  | "SET_PROVIDED_ADDRESS";
export const VenmoRequestExperienceContextShippingPreference =
  /*@__PURE__*/ S.String;

/** Customizes the Vault creation flow experience for your customers. */
export interface VenmoRequestExperienceContext {
  /** The label that overrides the business name in the PayPal account on the PayPal site. The pattern is defined by an external party and supports Unicode. */
  brand_name?: string;
  /** The shipping preference. This only applies to PayPal payment source. */
  shipping_preference?:
    | VenmoRequestExperienceContextShippingPreference
    | (string & {});
  /** Vault Instruction on action to be performed after a successful payer approval. */
  vault_instruction?: VaultInstruction | (string & {});
}
export const VenmoRequestExperienceContext = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    brand_name: S.optional(S.String),
    shipping_preference: S.optional(
      VenmoRequestExperienceContextShippingPreference,
    ),
    vault_instruction: S.optional(VaultInstruction),
  }),
).annotate({
  identifier: "VenmoRequestExperienceContext",
}) as any as S.Schema<VenmoRequestExperienceContext>;

export interface VenmoRequest {
  /** The description displayed to the consumer on the approval flow for a digital wallet, as well as on the merchant view of the payment token management experience. exp: PayPal.com. */
  description?: string;
  /** Expected business/charge model for the billing agreement. */
  usage_pattern?: VenmoRequestUsagePattern | (string & {});
  /** The shipping details. */
  shipping?: VenmoRequestShipping;
  /** Create multiple payment tokens for the same payer, merchant/platform combination. Use this when the customer has not logged in at merchant/platform. The payment token thus generated, can then also be used to create the customer account at merchant/platform. Use this also when multiple payment tokens are required for the same payer, different customer at merchant/platform. This helps to identify customers distinctly even though they may share the same PayPal account. This only applies to PayPal payment source. */
  permit_multiple_payment_tokens?: boolean;
  /** The usage type associated with a digital wallet payment token. */
  usage_type?: VenmoRequestUsageType | (string & {});
  /** The customer type associated with a digital wallet payment token. This is to indicate whether the customer acting on the merchant / platform is either a business or a consumer. */
  customer_type?: VenmoRequestCustomerType | (string & {});
  /** Customizes the Vault creation flow experience for your customers. */
  experience_context?: VenmoRequestExperienceContext;
}
export const VenmoRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.optional(S.String),
    usage_pattern: S.optional(VenmoRequestUsagePattern),
    shipping: S.optional(VenmoRequestShipping),
    permit_multiple_payment_tokens: S.optional(S.Boolean),
    usage_type: S.optional(VenmoRequestUsageType),
    customer_type: S.optional(VenmoRequestCustomerType),
    experience_context: S.optional(VenmoRequestExperienceContext),
  }),
).annotate({ identifier: "VenmoRequest" }) as any as S.Schema<VenmoRequest>;

/** The payment method to vault with the instrument details. */
export interface SetupTokensCreateRequestPaymentSource {
  /** A Resource representing a request to vault a Card. */
  card?: SetupTokensCreateRequestPaymentSourceCard;
  paypal?: PaypalWalletRequest;
  venmo?: VenmoRequest;
  token?: TokenIdRequest;
}
export const SetupTokensCreateRequestPaymentSource = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      card: S.optional(SetupTokensCreateRequestPaymentSourceCard),
      paypal: S.optional(PaypalWalletRequest),
      venmo: S.optional(VenmoRequest),
      token: S.optional(TokenIdRequest),
    }),
).annotate({
  identifier: "SetupTokensCreateRequestPaymentSource",
}) as any as S.Schema<SetupTokensCreateRequestPaymentSource>;

export interface SetupTokensCreateRequest {
  /** The server stores keys for 3 hours. */
  payPalRequestId?: string;
  /** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
  customer?: CustomerVaultPaymentTokensResponseCustomer;
  /** The payment method to vault with the instrument details. */
  payment_source: SetupTokensCreateRequestPaymentSource;
}
export const SetupTokensCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    payPalRequestId: S.optional(S.String.pipe(T.Header("PayPal-Request-Id"))),
    customer: S.optional(CustomerVaultPaymentTokensResponseCustomer),
    payment_source: SetupTokensCreateRequestPaymentSource,
  }).pipe(T.Http({ method: "POST", uri: "/v3/vault/setup-tokens", code: 200 })),
).annotate({
  identifier: "SetupTokensCreateRequest",
}) as any as S.Schema<SetupTokensCreateRequest>;

/** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
export type SetupTokenResponseCustomer =
  CustomerVaultPaymentTokensResponseCustomer;
export const SetupTokenResponseCustomer =
  CustomerVaultPaymentTokensResponseCustomer;

/** The status of the payment token. */
export type PaymentTokenStatus =
  | "CREATED"
  | "PAYER_ACTION_REQUIRED"
  | "APPROVED"
  | "VAULTED"
  | "TOKENIZED";
export const PaymentTokenStatus = /*@__PURE__*/ S.String;

/** Address request details. */
export type SetupTokenResponsePaymentSourceCardBillingAddress =
  CardResponseBillingAddress;
export const SetupTokenResponsePaymentSourceCardBillingAddress =
  CardResponseBillingAddress;

/** Results of Authentication such as 3D Secure. */
export type SetupTokenResponsePaymentSourceCardAuthenticationResult =
  CardResponseAuthenticationResult;
export const SetupTokenResponsePaymentSourceCardAuthenticationResult =
  CardResponseAuthenticationResult;

export interface SetupTokenResponsePaymentSourceCard {
  /** The card holder's name as it appears on the card. */
  name?: string;
  /** The last digits of the payment card. */
  last_digits?: string;
  /** The card brand or network. Typically used in the response. */
  brand?: CardBrand;
  /** The card expiration year and month, in [Internet date format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  expiry?: string;
  /** Address request details. */
  billing_address?: CardResponseBillingAddress;
  /** Card Verification status. */
  verification_status?: CardVerificationStatus;
  verification?: CardVerificationDetails;
  network_transaction_reference?: NetworkTransactionReferenceEntity;
  /** Results of Authentication such as 3D Secure. */
  authentication_result?: CardResponseAuthenticationResult;
  /** Bank Identification Number (BIN) details used to fund a payment. */
  bin_details?: BinDetails;
  /** The payment card type. */
  type?: CardType;
}
export const SetupTokenResponsePaymentSourceCard = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    last_digits: S.optional(S.String),
    brand: S.optional(CardBrand),
    expiry: S.optional(S.String),
    billing_address: S.optional(CardResponseBillingAddress),
    verification_status: S.optional(CardVerificationStatus),
    verification: S.optional(CardVerificationDetails),
    network_transaction_reference: S.optional(
      NetworkTransactionReferenceEntity,
    ),
    authentication_result: S.optional(CardResponseAuthenticationResult),
    bin_details: S.optional(BinDetails),
    type: S.optional(CardType),
  }),
).annotate({
  identifier: "SetupTokenResponsePaymentSourceCard",
}) as any as S.Schema<SetupTokenResponsePaymentSourceCard>;

/** The setup payment method details. */
export interface SetupTokenResponsePaymentSource {
  card?: SetupTokenResponsePaymentSourceCard;
  paypal?: PaypalWalletResponse;
  venmo?: VenmoResponse;
}
export const SetupTokenResponsePaymentSource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    card: S.optional(SetupTokenResponsePaymentSourceCard),
    paypal: S.optional(PaypalWalletResponse),
    venmo: S.optional(VenmoResponse),
  }),
).annotate({
  identifier: "SetupTokenResponsePaymentSource",
}) as any as S.Schema<SetupTokenResponsePaymentSource>;

/** Minimal representation of a cached setup token. */
export interface SetupTokenResponse {
  /** The PayPal-generated ID for the vault token. */
  id?: string;
  /** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
  customer?: CustomerVaultPaymentTokensResponseCustomer;
  /** The status of the payment token. */
  status?: PaymentTokenStatus;
  /** The setup payment method details. */
  payment_source?: SetupTokenResponsePaymentSource;
  links?: LinkDescriptionList;
}
export const SetupTokenResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    customer: S.optional(CustomerVaultPaymentTokensResponseCustomer),
    status: S.optional(PaymentTokenStatus),
    payment_source: S.optional(SetupTokenResponsePaymentSource),
    links: S.optional(LinkDescriptionList),
  }),
).annotate({
  identifier: "SetupTokenResponse",
}) as any as S.Schema<SetupTokenResponse>;

export interface SetupTokensGetRequest {
  /** ID of the setup token. */
  id: string;
}
export const SetupTokensGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "GET", uri: "/v3/vault/setup-tokens/{id}", code: 200 }),
  ),
).annotate({
  identifier: "SetupTokensGetRequest",
}) as any as S.Schema<SetupTokensGetRequest>;

export type CustomerPaymentTokensGetError =
  | BadRequest
  | Forbidden
  | PaypalOpError;
/** List all payment tokens Returns all payment tokens for a customer. */
export const customerPaymentTokensGet: API.OperationMethod<
  CustomerPaymentTokensGetRequest,
  CustomerVaultPaymentTokensResponse,
  CustomerPaymentTokensGetError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CustomerPaymentTokensGetRequest,
  output: CustomerVaultPaymentTokensResponse,
  errors: [BadRequest, Forbidden, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type PaymentTokensCreateError =
  | BadRequest
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Create payment token for a given payment source Creates a Payment Token from the given payment source and adds it to the Vault of the associated customer. */
export const paymentTokensCreate: API.OperationMethod<
  PaymentTokensCreateRequest,
  PaymentTokenResponse,
  PaymentTokensCreateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PaymentTokensCreateRequest,
  output: PaymentTokenResponse,
  errors: [
    BadRequest,
    Forbidden,
    NotFound,
    UnprocessableEntity,
    UnknownPaypalError,
  ],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type PaymentTokensDeleteError = BadRequest | Forbidden | PaypalOpError;
/** Delete payment token Delete the payment token associated with the payment token id. */
export const paymentTokensDelete: API.OperationMethod<
  PaymentTokensDeleteRequest,
  PaymentTokensDeleteResponse,
  PaymentTokensDeleteError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PaymentTokensDeleteRequest,
  output: PaymentTokensDeleteResponse,
  errors: [BadRequest, Forbidden, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type PaymentTokensGetError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Retrieve a payment token Returns a readable representation of vaulted payment source associated with the payment token id. */
export const paymentTokensGet: API.OperationMethod<
  PaymentTokensGetRequest,
  PaymentTokenResponse,
  PaymentTokensGetError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PaymentTokensGetRequest,
  output: PaymentTokenResponse,
  errors: [Forbidden, NotFound, UnprocessableEntity, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type SetupTokensCreateError =
  | BadRequest
  | Forbidden
  | UnprocessableEntity
  | PaypalOpError;
/** Create a setup token Creates a Setup Token from the given payment source and adds it to the Vault of the associated customer. */
export const setupTokensCreate: API.OperationMethod<
  SetupTokensCreateRequest,
  SetupTokenResponse,
  SetupTokensCreateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SetupTokensCreateRequest,
  output: SetupTokenResponse,
  errors: [BadRequest, Forbidden, UnprocessableEntity, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type SetupTokensGetError =
  | Forbidden
  | NotFound
  | UnprocessableEntity
  | PaypalOpError;
/** Retrieve a setup token Returns a readable representation of temporarily vaulted payment source associated with the setup token id. */
export const setupTokensGet: API.OperationMethod<
  SetupTokensGetRequest,
  SetupTokenResponse,
  SetupTokensGetError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SetupTokensGetRequest,
  output: SetupTokenResponse,
  errors: [Forbidden, NotFound, UnprocessableEntity, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));
