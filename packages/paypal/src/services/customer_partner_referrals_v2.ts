// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-paypal). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  PaypalProtocol,
  type PaypalOpError,
  type PaypalOpContext,
} from "../protocol.ts";
import { UnknownPaypalError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { PaypalOpError, PaypalOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** The person's name type. */
export type PersonNameType = "LEGAL";
export const PersonNameType = /*@__PURE__*/ S.String;

export interface PersonName {
  /** The prefix, or title, to the party's name. */
  prefix?: string;
  /** When the party is a person, the party's given, or first, name. */
  given_name?: string;
  /** When the party is a person, the party's surname or family name. Also known as the last name. Required when the party is a person. Use also to store multiple surnames including the matronymic, or mother's, surname. */
  surname?: string;
  /** When the party is a person, the party's middle name. Use also to store multiple middle names including the patronymic, or father's, middle name. */
  middle_name?: string;
  /** The suffix for the party's name. */
  suffix?: string;
  /** When the party is a person, the party's full name. */
  full_name?: string;
  /** The type of name. Currently supported values are: `LEGAL`. */
  type: PersonNameType | (string & {});
}
export const PersonName = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    prefix: S.optional(S.String),
    given_name: S.optional(S.String),
    surname: S.optional(S.String),
    middle_name: S.optional(S.String),
    suffix: S.optional(S.String),
    full_name: S.optional(S.String),
    type: PersonNameType,
  }),
).annotate({ identifier: "PersonName" }) as any as S.Schema<PersonName>;

/** The name of the person. */
export type PersonNameList = Array<PersonName>;
export const PersonNameList = /*@__PURE__*/ S.Array(
  PersonName,
) as any as S.Schema<PersonNameList>;

/** The non-portable additional address details that are sometimes needed for compliance, risk, or other scenarios where fine-grain address information might be needed. Not portable with common third party and open source. Redundant with core fields.<br/>For example, `address_portable.address_line_1` is usually a combination of `address_details.street_number`, `street_name`, and `street_type`. */
export interface AddressDetails {
  /** The street number. */
  street_number?: string;
  /** The street name. Just `Drury` in `Drury Lane`. */
  street_name?: string;
  /** The street type. For example, avenue, boulevard, road, or expressway. */
  street_type?: string;
  /** The delivery service. Post office box, bag number, or post office name. */
  delivery_service?: string;
  /** A named locations that represents the premise. Usually a building name or number or collection of buildings with a common name or number. For example, <code>Craven House</code>. */
  building_name?: string;
  /** The first-order entity below a named building or location that represents the sub-premise. Usually a single building within a collection of buildings with a common name. Can be a flat, story, floor, room, or apartment. */
  sub_building?: string;
}
export const AddressDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    street_number: S.optional(S.String),
    street_name: S.optional(S.String),
    street_type: S.optional(S.String),
    delivery_service: S.optional(S.String),
    building_name: S.optional(S.String),
    sub_building: S.optional(S.String),
  }),
).annotate({ identifier: "AddressDetails" }) as any as S.Schema<AddressDetails>;

/** The address type under which the provided address is tagged. */
export type PersonAddressType = "HOME";
export const PersonAddressType = /*@__PURE__*/ S.String;

export interface PersonAddressDetail {
  /** The first line of the address. For example, number or street. For example, `173 Drury Lane`. Required for data entry and compliance and risk checks. Must contain the full address. */
  address_line_1?: string;
  /** The second line of the address. For example, suite or apartment number. */
  address_line_2?: string;
  /** The third line of the address, if needed. For example, a street complement for Brazil, direction text, such as `next to Walmart`, or a landmark in an Indian address. */
  address_line_3?: string;
  /** The neighborhood, ward, or district. Smaller than `admin_area_level_3` or `sub_locality`. Value is:<ul><li>The postal sorting code for Guernsey and many French territories, such as French Guiana.</li><li>The fine-grained administrative levels in China.</li></ul> */
  admin_area_4?: string;
  /** A sub-locality, suburb, neighborhood, or district. Smaller than `admin_area_level_2`. Value is:<ul><li>Brazil. Suburb, bairro, or neighborhood.</li><li>India. Sub-locality or district. Street name information is not always available but a sub-locality or district can be a very small area.</li></ul> */
  admin_area_3?: string;
  /** A city, town, or village. Smaller than `admin_area_level_1`. */
  admin_area_2?: string;
  /** The highest level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. Format for postal delivery. For example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A kanton.</li></ul> */
  admin_area_1?: string;
  /** The postal code, which is the zip code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
  postal_code?: string;
  country_code: string;
  address_details?: AddressDetails;
  /** The address type under which this is classified. */
  type: PersonAddressType | (string & {});
}
export const PersonAddressDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address_line_1: S.optional(S.String),
    address_line_2: S.optional(S.String),
    address_line_3: S.optional(S.String),
    admin_area_4: S.optional(S.String),
    admin_area_3: S.optional(S.String),
    admin_area_2: S.optional(S.String),
    admin_area_1: S.optional(S.String),
    postal_code: S.optional(S.String),
    country_code: S.String,
    address_details: S.optional(AddressDetails),
    type: PersonAddressType,
  }),
).annotate({
  identifier: "PersonAddressDetail",
}) as any as S.Schema<PersonAddressDetail>;

/** The list of addresses associated with the person. */
export type PersonAddressDetailList = Array<PersonAddressDetail>;
export const PersonAddressDetailList = /*@__PURE__*/ S.Array(
  PersonAddressDetail,
) as any as S.Schema<PersonAddressDetailList>;

/** The phone type. */
export type PhoneType = "FAX" | "HOME" | "MOBILE" | "OTHER" | "PAGER";
export const PhoneType = /*@__PURE__*/ S.String;

export interface PersonPhoneDetail {
  /** The country calling code (CC), in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the CC and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  country_code: string;
  /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  national_number: string;
  /** The extension number. */
  extension_number?: string;
  /** The type of phone number provided. For example, home, work, or mobile. */
  type: PhoneType | (string & {});
}
export const PersonPhoneDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    country_code: S.String,
    national_number: S.String,
    extension_number: S.optional(S.String),
    type: PhoneType,
  }),
).annotate({
  identifier: "PersonPhoneDetail",
}) as any as S.Schema<PersonPhoneDetail>;

/** The list of phone numbers associated with the person. */
export type PersonPhoneDetailList = Array<PersonPhoneDetail>;
export const PersonPhoneDetailList = /*@__PURE__*/ S.Array(
  PersonPhoneDetail,
) as any as S.Schema<PersonPhoneDetailList>;

/** Date of birth data provided by the user */
export interface BirthDetails {
  /** date of birth, fomrat [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  date_of_birth: string;
}
export const BirthDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    date_of_birth: S.String,
  }),
).annotate({ identifier: "BirthDetails" }) as any as S.Schema<BirthDetails>;

/** The document labels. A document could be classfied to multiple categories. For example, a bill document can be classfified as `BILL DOCUMENT` and `UTILITY DOCUMENT`. */
export type LabelsList = Array<string>;
export const LabelsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<LabelsList>;

/** The file reference. Can be a file in PayPal MediaServ, PayPal DMS, or another custom store. */
export interface FileReference {
  /** The ID of the referenced file. */
  id?: string;
  /** The reference URL for the file. */
  reference_url?: string;
  /** The [Internet Assigned Numbers Authority (IANA) media type of the file](https://www.iana.org/assignments/media-types/media-types.xhtml). */
  content_type?: string;
  /** The date and time when the file was created, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  create_time?: string;
  /** The size of the file, in bytes. */
  size?: string;
}
export const FileReference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    reference_url: S.optional(S.String),
    content_type: S.optional(S.String),
    create_time: S.optional(S.String),
    size: S.optional(S.String),
  }),
).annotate({ identifier: "FileReference" }) as any as S.Schema<FileReference>;

/** The files contained in the document. For example, a document could be represented by a front page file and a back page file, etc. */
export type FileReferenceList = Array<FileReference>;
export const FileReferenceList = /*@__PURE__*/ S.Array(
  FileReference,
) as any as S.Schema<FileReferenceList>;

/** The type of documents. */
export type PersonDocumentType =
  | "SOCIAL_SECURITY_NUMBER"
  | "EMPLOYMENT_IDENTIFICATION_NUMBER"
  | "TAX_IDENTIFICATION_NUMBER"
  | "PASSPORT_NUMBER"
  | "PENSION_FUND_ID"
  | "MEDICAL_INSURANCE_ID"
  | "CNPJ"
  | "CPF"
  | "PAN"
  | "NATIONAL_ID_CARD";
export const PersonDocumentType = /*@__PURE__*/ S.String;

export interface PersonDocumentInput {
  labels?: LabelsList;
  /** The file name. */
  name?: string;
  /** The number for the document. It is the ID number if the document is `ID CARD`, the passport number if the document is `PASSPORT`, etc. */
  identification_number?: string;
  /** The issue date of the document. */
  issue_date?: string;
  /** The expire data of the document. */
  expiry_date?: string;
  /** The country code of the document. */
  issuing_country_code?: string;
  files?: FileReferenceList;
  /** The actual type of the document. It could be `ID_CARD`, `PASSPORT`, etc. */
  type?: PersonDocumentType | (string & {});
}
export const PersonDocumentInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    labels: S.optional(LabelsList),
    name: S.optional(S.String),
    identification_number: S.optional(S.String),
    issue_date: S.optional(S.String),
    expiry_date: S.optional(S.String),
    issuing_country_code: S.optional(S.String),
    files: S.optional(FileReferenceList),
    type: S.optional(PersonDocumentType),
  }),
).annotate({
  identifier: "PersonDocumentInput",
}) as any as S.Schema<PersonDocumentInput>;

/** A person's or party's related document data collected from the customer. For example SSN, ITIN, or business registration number collected from the user. <blockquote><strong>Note:</strong> This field is not applicable for POST [/v2/customer/partner-referrals](/docs/api/partner-referrals/v2/#partner-referrals_create) API calls.</blockquote> */
export type PersonDocumentListInput = Array<PersonDocumentInput>;
export const PersonDocumentListInput = /*@__PURE__*/ S.Array(
  PersonDocumentInput,
) as any as S.Schema<PersonDocumentListInput>;

/** Role of the person party played in the account. */
export type IndividualOwnerType = "PRIMARY";
export const IndividualOwnerType = /*@__PURE__*/ S.String;

export interface IndividualOwnerInput {
  names?: PersonNameList;
  /** The citizenship country code of the person. */
  citizenship?: string;
  addresses?: PersonAddressDetailList;
  phones?: PersonPhoneDetailList;
  /** The person's birth details. */
  birth_details?: BirthDetails;
  documents?: PersonDocumentListInput;
  /** The actual role of this user on the account, PRIMARY/SECONDARY. */
  type?: IndividualOwnerType | (string & {});
}
export const IndividualOwnerInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    names: S.optional(PersonNameList),
    citizenship: S.optional(S.String),
    addresses: S.optional(PersonAddressDetailList),
    phones: S.optional(PersonPhoneDetailList),
    birth_details: S.optional(BirthDetails),
    documents: S.optional(PersonDocumentListInput),
    type: S.optional(IndividualOwnerType),
  }),
).annotate({
  identifier: "IndividualOwnerInput",
}) as any as S.Schema<IndividualOwnerInput>;

/** List of owners in the account. There should be only one primary account owner which is mentioned in their role_type. */
export type IndividualOwnerListInput = Array<IndividualOwnerInput>;
export const IndividualOwnerListInput = /*@__PURE__*/ S.Array(
  IndividualOwnerInput,
) as any as S.Schema<IndividualOwnerListInput>;

/** The business types classified */
export type BusinessType =
  | "ANY_OTHER_BUSINESS_ENTITY"
  | "ASSOCIATION"
  | "CORPORATION"
  | "GENERAL_PARTNERSHIP"
  | "GOVERNMENT"
  | "INDIVIDUAL"
  | "LIMITED_LIABILITY_PARTNERSHIP"
  | "LIMITED_LIABILITY_PROPRIETORS"
  | "LIMITED_LIABILITY_PRIVATE_CORPORATION"
  | "LIMITED_PARTNERSHIP"
  | "LIMITED_PARTNERSHIP_PRIVATE_CORPORATION"
  | "NONPROFIT"
  | "ONLY_BUY_OR_SEND_MONEY"
  | "OTHER_CORPORATE_BODY"
  | "PARTNERSHIP"
  | "PRIVATE_PARTNERSHIP"
  | "PROPRIETORSHIP"
  | "PROPRIETORSHIP_CRAFTSMAN"
  | "PROPRIETORY_COMPANY"
  | "PRIVATE_CORPORATION"
  | "PUBLIC_COMPANY"
  | "PUBLIC_CORPORATION"
  | "PUBLIC_PARTNERSHIP"
  | "REGISTERED_COOPERATIVE";
export const BusinessType = /*@__PURE__*/ S.String;

/** Sub classification of the business type */
export type BusinessSubType =
  | "ASSO_TYPE_INCORPORATED"
  | "ASSO_TYPE_NON_INCORPORATED"
  | "GOVT_TYPE_ENTITY"
  | "GOVT_TYPE_EMANATION"
  | "GOVT_TYPE_ESTD_COMM"
  | "GOVT_TYPE_ESTD_FC"
  | "GOVT_TYPE_ESTD_ST_TR";
export const BusinessSubType = /*@__PURE__*/ S.String;

/** The type and subtype of the business. */
export interface BusinessTypeInfo {
  /** Type of business entity like corporation, sole prop, governmental. */
  type?: BusinessType | (string & {});
  /** The sub classification of the business type. */
  subtype?: BusinessSubType | (string & {});
}
export const BusinessTypeInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.optional(BusinessType),
    subtype: S.optional(BusinessSubType),
  }),
).annotate({
  identifier: "BusinessTypeInfo",
}) as any as S.Schema<BusinessTypeInfo>;

/** The category, subcategory and MCC code of the business. */
export interface BusinessIndustry {
  /** The customer's business category code. PayPal uses industry standard seller category codes. */
  category: string;
  /** The customer's business seller category code. PayPal uses industry standard seller category codes. */
  mcc_code: string;
  /** The customer's business subcategory code. PayPal uses industry standard seller subcategory codes. */
  subcategory: string;
}
export const BusinessIndustry = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    category: S.String,
    mcc_code: S.String,
    subcategory: S.String,
  }),
).annotate({
  identifier: "BusinessIndustry",
}) as any as S.Schema<BusinessIndustry>;

/** Business incorporation information. */
export interface BusinessEntityInputBusinessIncorporation {
  /** The incorporation country code. */
  incorporation_country_code?: string;
  /** date of incorporation */
  incorporation_date?: string;
}
export const BusinessEntityInputBusinessIncorporation = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      incorporation_country_code: S.optional(S.String),
      incorporation_date: S.optional(S.String),
    }),
).annotate({
  identifier: "BusinessEntityInputBusinessIncorporation",
}) as any as S.Schema<BusinessEntityInputBusinessIncorporation>;

/** Business name type */
export type BusinessNameType = "DOING_BUSINESS_AS" | "LEGAL_NAME";
export const BusinessNameType = /*@__PURE__*/ S.String;

export interface BusinessNameDetail {
  /** Required. The business name of the party. */
  business_name?: string;
  /** The type of business name. For example, trading name. */
  type: BusinessNameType | (string & {});
}
export const BusinessNameDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    business_name: S.optional(S.String),
    type: BusinessNameType,
  }),
).annotate({
  identifier: "BusinessNameDetail",
}) as any as S.Schema<BusinessNameDetail>;

/** Name of the business. */
export type BusinessNameDetailList = Array<BusinessNameDetail>;
export const BusinessNameDetailList = /*@__PURE__*/ S.Array(
  BusinessNameDetail,
) as any as S.Schema<BusinessNameDetailList>;

/** The role of the email address. */
export type EmailType = "CUSTOMER_SERVICE";
export const EmailType = /*@__PURE__*/ S.String;

/** An email address at which the person or business can be contacted. */
export interface Email {
  /** The role of the email address. */
  type: EmailType | (string & {});
  email: string;
}
export const Email = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: EmailType,
    email: S.String,
  }),
).annotate({ identifier: "Email" }) as any as S.Schema<Email>;

/** Email addresses of the business. */
export type EmailList = Array<Email>;
export const EmailList = /*@__PURE__*/ S.Array(
  Email,
) as any as S.Schema<EmailList>;

/** Address type under which the provided address is tagged */
export type BusinessAddressType = "WORK";
export const BusinessAddressType = /*@__PURE__*/ S.String;

export interface BusinessAddressDetail {
  /** The first line of the address. For example, number or street. For example, `173 Drury Lane`. Required for data entry and compliance and risk checks. Must contain the full address. */
  address_line_1?: string;
  /** The second line of the address. For example, suite or apartment number. */
  address_line_2?: string;
  /** The third line of the address, if needed. For example, a street complement for Brazil, direction text, such as `next to Walmart`, or a landmark in an Indian address. */
  address_line_3?: string;
  /** The neighborhood, ward, or district. Smaller than `admin_area_level_3` or `sub_locality`. Value is:<ul><li>The postal sorting code for Guernsey and many French territories, such as French Guiana.</li><li>The fine-grained administrative levels in China.</li></ul> */
  admin_area_4?: string;
  /** A sub-locality, suburb, neighborhood, or district. Smaller than `admin_area_level_2`. Value is:<ul><li>Brazil. Suburb, bairro, or neighborhood.</li><li>India. Sub-locality or district. Street name information is not always available but a sub-locality or district can be a very small area.</li></ul> */
  admin_area_3?: string;
  /** A city, town, or village. Smaller than `admin_area_level_1`. */
  admin_area_2?: string;
  /** The highest level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. Format for postal delivery. For example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A kanton.</li></ul> */
  admin_area_1?: string;
  /** The postal code, which is the zip code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
  postal_code?: string;
  country_code: string;
  address_details?: AddressDetails;
  /** The address type under which this is classified. For example, shipping or dropoff. */
  type: BusinessAddressType | (string & {});
}
export const BusinessAddressDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address_line_1: S.optional(S.String),
    address_line_2: S.optional(S.String),
    address_line_3: S.optional(S.String),
    admin_area_4: S.optional(S.String),
    admin_area_3: S.optional(S.String),
    admin_area_2: S.optional(S.String),
    admin_area_1: S.optional(S.String),
    postal_code: S.optional(S.String),
    country_code: S.String,
    address_details: S.optional(AddressDetails),
    type: BusinessAddressType,
  }),
).annotate({
  identifier: "BusinessAddressDetail",
}) as any as S.Schema<BusinessAddressDetail>;

/** List of addresses associated with the business entity. */
export type BusinessAddressDetailList = Array<BusinessAddressDetail>;
export const BusinessAddressDetailList = /*@__PURE__*/ S.Array(
  BusinessAddressDetail,
) as any as S.Schema<BusinessAddressDetailList>;

/** The type of phone number provided. For example, home, work, or mobile. */
export type BusinessPhoneType = "CUSTOMER_SERVICE" | "BUSINESS";
export const BusinessPhoneType = /*@__PURE__*/ S.String;

export interface BusinessPhoneDetail {
  /** The country calling code (CC), in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the CC and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  country_code: string;
  /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  national_number: string;
  /** The extension number. */
  extension_number?: string;
  /** The type of phone number provided. For example, home, work, or mobile. */
  type: BusinessPhoneType | (string & {});
}
export const BusinessPhoneDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    country_code: S.String,
    national_number: S.String,
    extension_number: S.optional(S.String),
    type: BusinessPhoneType,
  }),
).annotate({
  identifier: "BusinessPhoneDetail",
}) as any as S.Schema<BusinessPhoneDetail>;

/** List of phone number associated with the business. */
export type BusinessPhoneDetailList = Array<BusinessPhoneDetail>;
export const BusinessPhoneDetailList = /*@__PURE__*/ S.Array(
  BusinessPhoneDetail,
) as any as S.Schema<BusinessPhoneDetailList>;

/** The type of documents. */
export type BusinessDocumentType =
  | "SOCIAL_SECURITY_NUMBER"
  | "EMPLOYMENT_IDENTIFICATION_NUMBER"
  | "TAX_IDENTIFICATION_NUMBER"
  | "PASSPORT_NUMBER"
  | "PENSION_FUND_ID"
  | "MEDICAL_INSURANCE_ID"
  | "CNPJ"
  | "CPF"
  | "PAN"
  | "BUSINESS_REGISTRATION";
export const BusinessDocumentType = /*@__PURE__*/ S.String;

export interface BusinessDocumentInput {
  labels?: LabelsList;
  /** The file name. */
  name?: string;
  /** The number for the document. It is the ID number if the document is `ID CARD`, the passport number if the document is `PASSPORT`, etc. */
  identification_number?: string;
  /** The issue date of the document. */
  issue_date?: string;
  /** The expire data of the document. */
  expiry_date?: string;
  /** The country code of the document. */
  issuing_country_code?: string;
  files?: FileReferenceList;
  /** The actual type of the document. */
  type?: BusinessDocumentType | (string & {});
}
export const BusinessDocumentInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    labels: S.optional(LabelsList),
    name: S.optional(S.String),
    identification_number: S.optional(S.String),
    issue_date: S.optional(S.String),
    expiry_date: S.optional(S.String),
    issuing_country_code: S.optional(S.String),
    files: S.optional(FileReferenceList),
    type: S.optional(BusinessDocumentType),
  }),
).annotate({
  identifier: "BusinessDocumentInput",
}) as any as S.Schema<BusinessDocumentInput>;

/** Business Party related Document data collected from the customer.. For example SSN, ITIN, Business registration number that were collected from the user. */
export type BusinessDocumentListInput = Array<BusinessDocumentInput>;
export const BusinessDocumentListInput = /*@__PURE__*/ S.Array(
  BusinessDocumentInput,
) as any as S.Schema<BusinessDocumentListInput>;

export interface IndividualBeneficialOwnerInput {
  names?: PersonNameList;
  /** The citizenship country code of the person. */
  citizenship?: string;
  addresses?: PersonAddressDetailList;
  phones?: PersonPhoneDetailList;
  /** The person's birth details. */
  birth_details?: BirthDetails;
  documents?: PersonDocumentListInput;
  /** The percentage of shares this person owns in the company. */
  percentage_of_ownership?: string;
}
export const IndividualBeneficialOwnerInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    names: S.optional(PersonNameList),
    citizenship: S.optional(S.String),
    addresses: S.optional(PersonAddressDetailList),
    phones: S.optional(PersonPhoneDetailList),
    birth_details: S.optional(BirthDetails),
    documents: S.optional(PersonDocumentListInput),
    percentage_of_ownership: S.optional(S.String),
  }),
).annotate({
  identifier: "IndividualBeneficialOwnerInput",
}) as any as S.Schema<IndividualBeneficialOwnerInput>;

/** Individual beneficial owners. */
export type IndividualBeneficialOwnerListInput =
  Array<IndividualBeneficialOwnerInput>;
export const IndividualBeneficialOwnerListInput = /*@__PURE__*/ S.Array(
  IndividualBeneficialOwnerInput,
) as any as S.Schema<IndividualBeneficialOwnerListInput>;

/** Business incorporation information. */
export type BusinessBeneficialOwnerInputBusinessIncorporation =
  BusinessEntityInputBusinessIncorporation;
export const BusinessBeneficialOwnerInputBusinessIncorporation =
  BusinessEntityInputBusinessIncorporation;

export interface BusinessBeneficialOwnerInput {
  /** Information related to the business like the nature of business, started date etc. */
  business_type?: BusinessTypeInfo;
  /** Information related to the business like the nature of business, started date etc. */
  business_industry?: BusinessIndustry;
  /** Business incorporation information. */
  business_incorporation?: BusinessEntityInputBusinessIncorporation;
  names?: BusinessNameDetailList;
  emails?: EmailList;
  /** Website of the business. */
  website?: string;
  addresses?: BusinessAddressDetailList;
  phones?: BusinessPhoneDetailList;
  documents?: BusinessDocumentListInput;
  /** The percentage of shares this person owns in the company. */
  percentage_of_ownership?: string;
}
export const BusinessBeneficialOwnerInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    business_type: S.optional(BusinessTypeInfo),
    business_industry: S.optional(BusinessIndustry),
    business_incorporation: S.optional(
      BusinessEntityInputBusinessIncorporation,
    ),
    names: S.optional(BusinessNameDetailList),
    emails: S.optional(EmailList),
    website: S.optional(S.String),
    addresses: S.optional(BusinessAddressDetailList),
    phones: S.optional(BusinessPhoneDetailList),
    documents: S.optional(BusinessDocumentListInput),
    percentage_of_ownership: S.optional(S.String),
  }),
).annotate({
  identifier: "BusinessBeneficialOwnerInput",
}) as any as S.Schema<BusinessBeneficialOwnerInput>;

/** Business beneficial owners. */
export type BusinessBeneficialOwnerListInput =
  Array<BusinessBeneficialOwnerInput>;
export const BusinessBeneficialOwnerListInput = /*@__PURE__*/ S.Array(
  BusinessBeneficialOwnerInput,
) as any as S.Schema<BusinessBeneficialOwnerListInput>;

/** Beneficial owners of the entity. */
export interface BeneficialOwnersInput {
  individual_beneficial_owners?: IndividualBeneficialOwnerListInput;
  business_beneficial_owners?: BusinessBeneficialOwnerListInput;
}
export const BeneficialOwnersInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    individual_beneficial_owners: S.optional(
      IndividualBeneficialOwnerListInput,
    ),
    business_beneficial_owners: S.optional(BusinessBeneficialOwnerListInput),
  }),
).annotate({
  identifier: "BeneficialOwnersInput",
}) as any as S.Schema<BeneficialOwnersInput>;

/** The currency and amount for a financial transaction, such as a balance or payment due. */
export interface Money {
  currency_code: string;
  /** The value, which might be:<ul><li>An integer for currencies like `JPY` that are not typically fractional.</li><li>A decimal fraction for currencies like `TND` that are subdivided into thousandths.</li></ul>For the required number of decimal places for a currency code, see [Currency Codes](/docs/integration/direct/rest/currency-codes/). */
  value: string;
}
export const Money = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    currency_code: S.String,
    value: S.String,
  }),
).annotate({ identifier: "Money" }) as any as S.Schema<Money>;

/** The currency range, from the minimum inclusive amount to the maximum inclusive amount. */
export interface CurrencyRange {
  /** The minimum inclusive amount for the range. */
  minimum_amount?: Money;
  /** The maximum inclusive amount for the range. */
  maximum_amount?: Money;
}
export const CurrencyRange = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    minimum_amount: S.optional(Money),
    maximum_amount: S.optional(Money),
  }),
).annotate({ identifier: "CurrencyRange" }) as any as S.Schema<CurrencyRange>;

export interface BusinessEntityInput {
  /** Information related to the business like the nature of business, started date etc. */
  business_type?: BusinessTypeInfo;
  /** Information related to the business like the nature of business, started date etc. */
  business_industry?: BusinessIndustry;
  /** Business incorporation information. */
  business_incorporation?: BusinessEntityInputBusinessIncorporation;
  names?: BusinessNameDetailList;
  emails?: EmailList;
  /** Website of the business. */
  website?: string;
  addresses?: BusinessAddressDetailList;
  phones?: BusinessPhoneDetailList;
  documents?: BusinessDocumentListInput;
  /** List of beneficial owners part of the entity. They can be either a Person or a business entity. */
  beneficial_owners?: BeneficialOwnersInput;
  /** The range for the total annual sales volume of the business. */
  annual_sales_volume_range?: CurrencyRange;
  /** The range for the average monthly volume of the business. */
  average_monthly_volume_range?: CurrencyRange;
  /** The account's purpose code. */
  purpose_code?: unknown;
  /** The business goals description. For example, a mission statement. */
  business_description?: string;
}
export const BusinessEntityInput = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    business_type: S.optional(BusinessTypeInfo),
    business_industry: S.optional(BusinessIndustry),
    business_incorporation: S.optional(
      BusinessEntityInputBusinessIncorporation,
    ),
    names: S.optional(BusinessNameDetailList),
    emails: S.optional(EmailList),
    website: S.optional(S.String),
    addresses: S.optional(BusinessAddressDetailList),
    phones: S.optional(BusinessPhoneDetailList),
    documents: S.optional(BusinessDocumentListInput),
    beneficial_owners: S.optional(BeneficialOwnersInput),
    annual_sales_volume_range: S.optional(CurrencyRange),
    average_monthly_volume_range: S.optional(CurrencyRange),
    purpose_code: S.optional(S.Unknown),
    business_description: S.optional(S.String),
  }),
).annotate({
  identifier: "BusinessEntityInput",
}) as any as S.Schema<BusinessEntityInput>;

/** The preference to customize the web experience of the customer by overriding that is set at the Partner's Account. */
export interface PartnerConfigOverride {
  /** The URL to which to redirect the customer upon completion of the onboarding process. */
  return_url?: string;
  /** The description of the return URL. */
  return_url_description?: string;
  /** Indicates whether to show an add credit card page. */
  show_add_credit_card?: boolean;
}
export const PartnerConfigOverride = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    return_url: S.optional(S.String),
    return_url_description: S.optional(S.String),
    show_add_credit_card: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "PartnerConfigOverride",
}) as any as S.Schema<PartnerConfigOverride>;

/** The type of bank account. */
export type BankAccountType = "CHECKING" | "SAVINGS";
export const BankAccountType = /*@__PURE__*/ S.String;

/** The bank account ID type. */
export type IdentifierType =
  | "BANK_CODE"
  | "BI_CODE"
  | "BRANCH_CODE"
  | "ROUTING_NUMBER_1"
  | "ROUTING_NUMBER_2"
  | "ROUTING_NUMBER_3"
  | "SWIFT_CODE"
  | "BRANCH_CODE"
  | "INTERMEDIARY_SWIFT_CODE"
  | "BBAN"
  | "BBAN_ENCRYPTED"
  | "BBAN_HMAC"
  | "AGGREGATOR_YODLEE";
export const IdentifierType = /*@__PURE__*/ S.String;

/** The bank account ID. An ID with `ROUTING_NUMBER_1` is required. */
export interface Identifier {
  /** The bank account ID type. */
  type?: IdentifierType | (string & {});
  /** The value of account identifier. */
  value?: string;
}
export const Identifier = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.optional(IdentifierType),
    value: S.optional(S.String),
  }),
).annotate({ identifier: "Identifier" }) as any as S.Schema<Identifier>;

/** An array of instrument institute attributes. Used with the account number to uniquely identify the instrument. Value is:<ul><li>For banks with IBAN information, the IBAN number.</li><li>For banks with BBAN information, the BBAN number.</li><li>For banks with both IBAN and BBAN information, the IBAN number.</li></ul> */
export type IdentifierList = Array<Identifier>;
export const IdentifierList = /*@__PURE__*/ S.Array(
  Identifier,
) as any as S.Schema<IdentifierList>;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export interface BankBranchLocation {
  /** The first line of the address. For example, number or street. For example, `173 Drury Lane`. Required for data entry and compliance and risk checks. Must contain the full address. */
  address_line_1?: string;
  /** The second line of the address. For example, suite or apartment number. */
  address_line_2?: string;
  /** A city, town, or village. Smaller than `admin_area_level_1`. */
  admin_area_2?: string;
  /** The highest level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. Format for postal delivery. For example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A kanton.</li></ul> */
  admin_area_1?: string;
  /** The postal code, which is the zip code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
  postal_code?: string;
  country_code: string;
}
export const BankBranchLocation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address_line_1: S.optional(S.String),
    address_line_2: S.optional(S.String),
    admin_area_2: S.optional(S.String),
    admin_area_1: S.optional(S.String),
    postal_code: S.optional(S.String),
    country_code: S.String,
  }),
).annotate({
  identifier: "BankBranchLocation",
}) as any as S.Schema<BankBranchLocation>;

/** Seller’s consent to operate on this financial instrument. */
export interface Mandate {
  /** Whether mandate was accepted or not. */
  accepted: boolean;
}
export const Mandate = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accepted: S.Boolean,
  }),
).annotate({ identifier: "Mandate" }) as any as S.Schema<Mandate>;

/** The bank account information. */
export interface Bank {
  /** The user-provided short name for the user's bank account. */
  nick_name?: string;
  /** The bank account number. */
  account_number: string;
  /** The type of bank account. */
  account_type: BankAccountType | (string & {});
  /** The primary currency code of the bank account. */
  currency_code?: string;
  identifiers?: IdentifierList;
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  branch_location?: BankBranchLocation;
  /** Mandate for this bank account. */
  mandate?: Mandate;
}
export const Bank = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    nick_name: S.optional(S.String),
    account_number: S.String,
    account_type: BankAccountType,
    currency_code: S.optional(S.String),
    identifiers: S.optional(IdentifierList),
    branch_location: S.optional(BankBranchLocation),
    mandate: S.optional(Mandate),
  }),
).annotate({ identifier: "Bank" }) as any as S.Schema<Bank>;

/** An array of banks attached to this managed account. */
export type BankList = Array<Bank>;
export const BankList = /*@__PURE__*/ S.Array(
  Bank,
) as any as S.Schema<BankList>;

/** Financial instruments attached to this account. */
export interface FinancialInstruments {
  banks?: BankList;
}
export const FinancialInstruments = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    banks: S.optional(BankList),
  }),
).annotate({
  identifier: "FinancialInstruments",
}) as any as S.Schema<FinancialInstruments>;

/** The operation to enable for the customer. To enable the collection of the API permissions that you require to integrate with the customer, specify `API_INTEGRATION`. `BANK_ADDITION` is supported only for the US. */
export type OperationOperation =
  | "API_INTEGRATION"
  | "BANK_ADDITION"
  | "BILLING_AGREEMENT"
  | "CONTEXTUAL_MARKETING_CONSENT"
  | "OFFLINE_ONBOARDING"
  | "SALESFORCE_REFERRAL";
export const OperationOperation = /*@__PURE__*/ S.String;

/** The REST-credential integration method. */
export type RestApiIntegrationIntegrationMethod =
  | "BRAINTREE"
  | "PAYPAL"
  | "SDK";
export const RestApiIntegrationIntegrationMethod = /*@__PURE__*/ S.String;

/** The type of REST-endpoint integration. To integrate with Braintree v.zero for PayPal REST endpoints, specify `third_party_details`. */
export type RestApiIntegrationIntegrationType = "FIRST_PARTY" | "THIRD_PARTY";
export const RestApiIntegrationIntegrationType = /*@__PURE__*/ S.String;

/** The REST endpoint. */
export type RestEndpointFeaturesEnum =
  | "PAYOUTS"
  | "PAYMENT"
  | "REFUND"
  | "FUTURE_PAYMENT"
  | "DIRECT_PAYMENT"
  | "PARTNER_FEE"
  | "DELAY_FUNDS_DISBURSEMENT"
  | "READ_SELLER_DISPUTE"
  | "UPDATE_SELLER_DISPUTE"
  | "ADVANCED_TRANSACTIONS_SEARCH"
  | "SWEEP_FUNDS_EXTERNAL_SINK"
  | "ACCESS_MERCHANT_INFORMATION"
  | "TRACKING_SHIPMENT_READWRITE"
  | "INVOICE_READ_WRITE"
  | "DISPUTE_READ_BUYER"
  | "UPDATE_CUSTOMER_DISPUTES"
  | "VAULT"
  | "BILLING_AGREEMENT"
  | "WITHDRAWALS"
  | "LINKED_FINANCIAL_INSTRUMENTS"
  | "USER_PROFILE"
  | "TRANSACTION_RISK_DATA"
  | "PAYPAL_BALANCE"
  | "EXCHANGE_CURRENCY";
export const RestEndpointFeaturesEnum = /*@__PURE__*/ S.String;

/** An array of features that partner can access, or use, in PayPal on behalf of the seller. The seller grants permission for these features to the partner. */
export type RestEndpointFeaturesEnumList = Array<
  RestEndpointFeaturesEnum | (string & {})
>;
export const RestEndpointFeaturesEnumList = /*@__PURE__*/ S.Array(
  RestEndpointFeaturesEnum,
) as any as S.Schema<RestEndpointFeaturesEnumList>;

/** The integration details for PayPal first party REST endpoints. */
export interface FirstPartyDetails {
  features: RestEndpointFeaturesEnumList;
  /** S256 - The code verifier must be high-entropy cryptographic random string with a byte length of 43-128 range. */
  seller_nonce: string;
}
export const FirstPartyDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    features: RestEndpointFeaturesEnumList,
    seller_nonce: S.String,
  }),
).annotate({
  identifier: "FirstPartyDetails",
}) as any as S.Schema<FirstPartyDetails>;

/** An array of features that partner can access, or use, in PayPal on behalf of the seller. The seller grants permission for these features to the partner. */
export type RestApiIntegrationRestEndpointFeaturesEnumList = Array<
  RestEndpointFeaturesEnum | (string & {})
>;
export const RestApiIntegrationRestEndpointFeaturesEnumList =
  /*@__PURE__*/ S.Array(
    RestEndpointFeaturesEnum,
  ) as any as S.Schema<RestApiIntegrationRestEndpointFeaturesEnumList>;

/** Signup Mode to be used for sellers for third party integration. */
export type SignupMode = "VERIFY_WITH_PAYPAL";
export const SignupMode = /*@__PURE__*/ S.String;

/** The integration details for PayPal REST endpoints. */
export interface ThirdPartyDetails {
  features?: RestApiIntegrationRestEndpointFeaturesEnumList;
  signup_mode?: SignupMode | (string & {});
  /** The organization to which the account will belong in the partner’s hierarchy, as a path. */
  organization?: string;
}
export const ThirdPartyDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    features: S.optional(RestApiIntegrationRestEndpointFeaturesEnumList),
    signup_mode: S.optional(SignupMode),
    organization: S.optional(S.String),
  }),
).annotate({
  identifier: "ThirdPartyDetails",
}) as any as S.Schema<ThirdPartyDetails>;

/** The integration details for PayPal REST endpoints. */
export interface RestApiIntegration {
  /** The REST-credential integration method. */
  integration_method?: RestApiIntegrationIntegrationMethod | (string & {});
  /** The type of REST-endpoint integration. To integrate with Braintree v.zero for PayPal REST endpoints, specify `third_party_details`. */
  integration_type: RestApiIntegrationIntegrationType | (string & {});
  first_party_details?: FirstPartyDetails;
  third_party_details?: ThirdPartyDetails;
}
export const RestApiIntegration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    integration_method: S.optional(RestApiIntegrationIntegrationMethod),
    integration_type: RestApiIntegrationIntegrationType,
    first_party_details: S.optional(FirstPartyDetails),
    third_party_details: S.optional(ThirdPartyDetails),
  }),
).annotate({
  identifier: "RestApiIntegration",
}) as any as S.Schema<RestApiIntegration>;

/** The integration details for the partner and customer relationship. Required if `operation` is `API_INTEGRATION`. */
export interface IntegrationDetails {
  classic_api_integration?: unknown;
  rest_api_integration?: RestApiIntegration;
}
export const IntegrationDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    classic_api_integration: S.optional(S.Unknown),
    rest_api_integration: S.optional(RestApiIntegration),
  }),
).annotate({
  identifier: "IntegrationDetails",
}) as any as S.Schema<IntegrationDetails>;

/** The preference that customizes the billing experience of the customer. */
export interface BillingExperiencePreference {
  /** The ID of the payment web experience profile. */
  experience_id?: string;
  /** Indicates whether the partner has already displayed the billing context to the seller. */
  billing_context_set?: boolean;
}
export const BillingExperiencePreference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    experience_id: S.optional(S.String),
    billing_context_set: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "BillingExperiencePreference",
}) as any as S.Schema<BillingExperiencePreference>;

/** The details of the billing agreement between the partner and a seller. */
export interface BillingAgreement {
  /** The billing agreement description. */
  description?: string;
  billing_experience_preference?: BillingExperiencePreference;
  /** The custom data for the billing agreement. */
  merchant_custom_data?: string;
  /** The URL to which to redirect seller to accept the billing agreement. */
  approval_url?: string;
  /** The billing agreement token for the agreement. */
  ec_token?: string;
}
export const BillingAgreement = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    description: S.optional(S.String),
    billing_experience_preference: S.optional(BillingExperiencePreference),
    merchant_custom_data: S.optional(S.String),
    approval_url: S.optional(S.String),
    ec_token: S.optional(S.String),
  }),
).annotate({
  identifier: "BillingAgreement",
}) as any as S.Schema<BillingAgreement>;

/** The required operation to share data. */
export interface Operation {
  /** The operation to enable for the customer. To enable the collection of the API permissions that you require to integrate with the customer, specify `API_INTEGRATION`. `BANK_ADDITION` is supported only for the US. */
  operation: OperationOperation | (string & {});
  api_integration_preference?: IntegrationDetails;
  offline_onboarding_preference?: unknown;
  billing_agreement?: BillingAgreement;
}
export const Operation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    operation: OperationOperation,
    api_integration_preference: S.optional(IntegrationDetails),
    offline_onboarding_preference: S.optional(S.Unknown),
    billing_agreement: S.optional(BillingAgreement),
  }),
).annotate({ identifier: "Operation" }) as any as S.Schema<Operation>;

/** An array of operations to perform for the customer while they share their data. */
export type OperationList = Array<Operation>;
export const OperationList = /*@__PURE__*/ S.Array(
  Operation,
) as any as S.Schema<OperationList>;

/** The PayPal product for which the customer is onboarded. */
export type Product =
  | "ALIPAY"
  | "BANCONTACT"
  | "BLIK"
  | "EPS"
  | "PPCP"
  | "EXPRESS_CHECKOUT"
  | "PAYMENT_METHODS"
  | "ADVANCED_VAULTING"
  | "IDEAL"
  | "MB_WAY"
  | "MULTIBANCO"
  | "PAYPAL_COMMERCE_PLATFORM_BUSINESS"
  | "PPPLUS"
  | "PRZELEWY24"
  | "SATISPAY"
  | "TRUSTLY"
  | "WECHAT_PAY"
  | "WEBSITE_PAYMENT_PRO"
  | "ZETTLE"
  | "PAYPAL_COMMERCE_PLATFORM_BUSINESS"
  | "HYPERWALLET_PAYOUTS";
export const Product = /*@__PURE__*/ S.String;

/** An array of PayPal products to which the partner wants to onboard the customer. */
export type ProductList = Array<Product | (string & {})>;
export const ProductList = /*@__PURE__*/ S.Array(
  Product,
) as any as S.Schema<ProductList>;

/** Optional capabilities of the selected PayPal product for which the customer is being onboarded. */
export type Capabilities =
  | "PAYPAL_WALLET_VAULTING_ADVANCED"
  | "PAY_UPON_INVOICE"
  | "APPLE_PAY"
  | "GOOGLE_PAY";
export const Capabilities = /*@__PURE__*/ S.String;

/** An array of capabilities which the partner wants to enable for the selected products. Supported only when products are specified. */
export type CapabilitiesList = Array<Capabilities | (string & {})>;
export const CapabilitiesList = /*@__PURE__*/ S.Array(
  Capabilities,
) as any as S.Schema<CapabilitiesList>;

/** An array of dependent processes. */
export type DependentProcessList = Array<unknown>;
export const DependentProcessList = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<DependentProcessList>;

/** The type of consent. `SHARE_DATA_CONSENT` gives consent to you to share your customer's data with PayPal. */
export type LegalConsentType = "SHARE_DATA_CONSENT";
export const LegalConsentType = /*@__PURE__*/ S.String;

/** The customer-provided consent. */
export interface LegalConsent {
  /** The type of consent. `SHARE_DATA_CONSENT` gives consent to you to share your customer's data with PayPal. */
  type: LegalConsentType | (string & {});
  /** Indicates whether the customer agreed to share this type of data. To give consent, specify `true`. To withhold consent, specify `false`. */
  granted: boolean;
}
export const LegalConsent = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: LegalConsentType,
    granted: S.Boolean,
  }),
).annotate({ identifier: "LegalConsent" }) as any as S.Schema<LegalConsent>;

/** An array of all consents that the partner has received from this seller. If `SHARE_DATA_CONSENT` is not granted, PayPal does not store customer data. */
export type LegalConsentList = Array<LegalConsent>;
export const LegalConsentList = /*@__PURE__*/ S.Array(
  LegalConsent,
) as any as S.Schema<LegalConsentList>;

/** Transfer Method type. */
export type TransferMethodTransferMethodType =
  | "BANK_ACCOUNT"
  | "PAYPAL"
  | "VENMO"
  | "WIRE_ACCOUNT";
export const TransferMethodTransferMethodType = /*@__PURE__*/ S.String;

/** Requested Currencies for a Transfer Method. */
export type CurrencyCodeList = Array<string>;
export const CurrencyCodeList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CurrencyCodeList>;

/** Requested transfer method and currency for a country. */
export interface TransferMethod {
  /** Transfer Method type. */
  transfer_method_type?: TransferMethodTransferMethodType | (string & {});
  currencies?: CurrencyCodeList;
}
export const TransferMethod = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    transfer_method_type: S.optional(TransferMethodTransferMethodType),
    currencies: S.optional(CurrencyCodeList),
  }),
).annotate({ identifier: "TransferMethod" }) as any as S.Schema<TransferMethod>;

/** Requested transfer method and currency for a country. */
export type TransferMethodList = Array<TransferMethod>;
export const TransferMethodList = /*@__PURE__*/ S.Array(
  TransferMethod,
) as any as S.Schema<TransferMethodList>;

/** Requested country, transfer method and currency. */
export interface CountryTransferMethodCurrencySelection {
  /** Country. */
  country?: string;
  transfer_methods?: TransferMethodList;
}
export const CountryTransferMethodCurrencySelection = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      country: S.optional(S.String),
      transfer_methods: S.optional(TransferMethodList),
    }),
).annotate({
  identifier: "CountryTransferMethodCurrencySelection",
}) as any as S.Schema<CountryTransferMethodCurrencySelection>;

/** Requested country, transfer method and currency. */
export type CountryTransferMethodCurrencySelectionList =
  Array<CountryTransferMethodCurrencySelection>;
export const CountryTransferMethodCurrencySelectionList = /*@__PURE__*/ S.Array(
  CountryTransferMethodCurrencySelection,
) as any as S.Schema<CountryTransferMethodCurrencySelectionList>;

/** Payout specific attributes. */
export interface PayoutAttributes {
  /** If `true`, specifies that the merchant or platform is offering goods or services on behalf of 3rd party sellers. */
  marketplace?: boolean;
  /** If `true`, specifies that the Kyc is required for the merchant. */
  kyc_required?: boolean;
  country_transfer_method_currency_selection?: CountryTransferMethodCurrencySelectionList;
}
export const PayoutAttributes = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    marketplace: S.optional(S.Boolean),
    kyc_required: S.optional(S.Boolean),
    country_transfer_method_currency_selection: S.optional(
      CountryTransferMethodCurrencySelectionList,
    ),
  }),
).annotate({
  identifier: "PayoutAttributes",
}) as any as S.Schema<PayoutAttributes>;

export interface PartnerReferralsCreateRequest {
  individual_owners?: IndividualOwnerListInput;
  /** Business entity of the account. */
  business_entity?: BusinessEntityInput;
  /** Email address of the customer used to create the account. */
  email?: string;
  /** The preferred [locale code](/docs/api/reference/locale-codes/) to use in the onboarding flow for the customer. */
  preferred_language_code?: string;
  /** The partner's unique identifier for this customer in their system which can be used to track user in PayPal. */
  tracking_id?: string;
  /** The configuration property that the partner intends to override for this onboarding request. */
  partner_config_override?: PartnerConfigOverride;
  /** Array of financial instruments attached to the customer's account. */
  financial_instruments?: FinancialInstruments;
  operations: OperationList;
  products?: ProductList;
  capabilities?: CapabilitiesList;
  outside_process_dependencies?: DependentProcessList;
  legal_consents: LegalConsentList;
  /** Payout specific attributes. */
  payout_attributes?: PayoutAttributes;
  /** Legal Country Code. */
  legal_country_code?: string;
}
export const PartnerReferralsCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    individual_owners: S.optional(IndividualOwnerListInput),
    business_entity: S.optional(BusinessEntityInput),
    email: S.optional(S.String),
    preferred_language_code: S.optional(S.String),
    tracking_id: S.optional(S.String),
    partner_config_override: S.optional(PartnerConfigOverride),
    financial_instruments: S.optional(FinancialInstruments),
    operations: OperationList,
    products: S.optional(ProductList),
    capabilities: S.optional(CapabilitiesList),
    outside_process_dependencies: S.optional(DependentProcessList),
    legal_consents: LegalConsentList,
    payout_attributes: S.optional(PayoutAttributes),
    legal_country_code: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/customer/partner-referrals",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PartnerReferralsCreateRequest",
}) as any as S.Schema<PartnerReferralsCreateRequest>;

/** The HTTP method required to make the related call. */
export type LinkDescriptionMethod =
  | "GET"
  | "POST"
  | "PUT"
  | "DELETE"
  | "HEAD"
  | "CONNECT"
  | "OPTIONS"
  | "PATCH";
export const LinkDescriptionMethod = /*@__PURE__*/ S.String;

/** The request-related [HATEOAS link](/docs/api/reference/api-responses/#hateoas-links) information. */
export interface LinkDescription {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  /** The HTTP method required to make the related call. */
  method?: LinkDescriptionMethod;
}
export const LinkDescription = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    href: S.String,
    rel: S.String,
    method: S.optional(LinkDescriptionMethod),
  }),
).annotate({
  identifier: "LinkDescription",
}) as any as S.Schema<LinkDescription>;

/** An array of request-related [HATEOAS links](/docs/api/overview/#hateoas-links). */
export type DefinitionsLinkDescriptionList = Array<LinkDescription>;
export const DefinitionsLinkDescriptionList = /*@__PURE__*/ S.Array(
  LinkDescription,
) as any as S.Schema<DefinitionsLinkDescriptionList>;

/** The shared referral data. */
export interface CreateReferralDataResponse {
  links?: DefinitionsLinkDescriptionList;
}
export const CreateReferralDataResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    links: S.optional(DefinitionsLinkDescriptionList),
  }),
).annotate({
  identifier: "CreateReferralDataResponse",
}) as any as S.Schema<CreateReferralDataResponse>;

export interface PartnerReferralsReadRequest {
  /** The ID of the partner-referrals data for which to show details. */
  partner_referral_id: string;
}
export const PartnerReferralsReadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    partner_referral_id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/customer/partner-referrals/{partner_referral_id}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PartnerReferralsReadRequest",
}) as any as S.Schema<PartnerReferralsReadRequest>;

/** The HATEOAS links. */
export type LinkDescriptionList = Array<LinkDescription>;
export const LinkDescriptionList = /*@__PURE__*/ S.Array(
  LinkDescription,
) as any as S.Schema<LinkDescriptionList>;

export interface PersonDocument {
  /** The encrypted identifier for the document. */
  id?: string;
  labels?: LabelsList;
  /** The file name. */
  name?: string;
  /** The number for the document. It is the ID number if the document is `ID CARD`, the passport number if the document is `PASSPORT`, etc. */
  identification_number?: string;
  /** The issue date of the document. */
  issue_date?: string;
  /** The expire data of the document. */
  expiry_date?: string;
  /** The country code of the document. */
  issuing_country_code?: string;
  files?: FileReferenceList;
  links?: LinkDescriptionList;
  /** The actual type of the document. It could be `ID_CARD`, `PASSPORT`, etc. */
  type?: PersonDocumentType;
}
export const PersonDocument = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    labels: S.optional(LabelsList),
    name: S.optional(S.String),
    identification_number: S.optional(S.String),
    issue_date: S.optional(S.String),
    expiry_date: S.optional(S.String),
    issuing_country_code: S.optional(S.String),
    files: S.optional(FileReferenceList),
    links: S.optional(LinkDescriptionList),
    type: S.optional(PersonDocumentType),
  }),
).annotate({ identifier: "PersonDocument" }) as any as S.Schema<PersonDocument>;

/** A person's or party's related document data collected from the customer. For example SSN, ITIN, or business registration number collected from the user. <blockquote><strong>Note:</strong> This field is not applicable for POST [/v2/customer/partner-referrals](/docs/api/partner-referrals/v2/#partner-referrals_create) API calls.</blockquote> */
export type PersonDocumentList = Array<PersonDocument>;
export const PersonDocumentList = /*@__PURE__*/ S.Array(
  PersonDocument,
) as any as S.Schema<PersonDocumentList>;

export interface IndividualOwner {
  /** The encrypted party ID. */
  id?: string;
  names?: PersonNameList;
  /** The citizenship country code of the person. */
  citizenship?: string;
  addresses?: PersonAddressDetailList;
  phones?: PersonPhoneDetailList;
  /** The person's birth details. */
  birth_details?: BirthDetails;
  documents?: PersonDocumentList;
  /** The actual role of this user on the account, PRIMARY/SECONDARY. */
  type?: IndividualOwnerType;
}
export const IndividualOwner = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    names: S.optional(PersonNameList),
    citizenship: S.optional(S.String),
    addresses: S.optional(PersonAddressDetailList),
    phones: S.optional(PersonPhoneDetailList),
    birth_details: S.optional(BirthDetails),
    documents: S.optional(PersonDocumentList),
    type: S.optional(IndividualOwnerType),
  }),
).annotate({
  identifier: "IndividualOwner",
}) as any as S.Schema<IndividualOwner>;

/** List of owners in the account. There should be only one primary account owner which is mentioned in their role_type. */
export type IndividualOwnerList = Array<IndividualOwner>;
export const IndividualOwnerList = /*@__PURE__*/ S.Array(
  IndividualOwner,
) as any as S.Schema<IndividualOwnerList>;

/** Business incorporation information. */
export type BusinessEntityBusinessIncorporation =
  BusinessEntityInputBusinessIncorporation;
export const BusinessEntityBusinessIncorporation =
  BusinessEntityInputBusinessIncorporation;

export interface BusinessDocument {
  /** The encrypted identifier for the document. */
  id?: string;
  labels?: LabelsList;
  /** The file name. */
  name?: string;
  /** The number for the document. It is the ID number if the document is `ID CARD`, the passport number if the document is `PASSPORT`, etc. */
  identification_number?: string;
  /** The issue date of the document. */
  issue_date?: string;
  /** The expire data of the document. */
  expiry_date?: string;
  /** The country code of the document. */
  issuing_country_code?: string;
  files?: FileReferenceList;
  links?: LinkDescriptionList;
  /** The actual type of the document. */
  type?: BusinessDocumentType;
}
export const BusinessDocument = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    labels: S.optional(LabelsList),
    name: S.optional(S.String),
    identification_number: S.optional(S.String),
    issue_date: S.optional(S.String),
    expiry_date: S.optional(S.String),
    issuing_country_code: S.optional(S.String),
    files: S.optional(FileReferenceList),
    links: S.optional(LinkDescriptionList),
    type: S.optional(BusinessDocumentType),
  }),
).annotate({
  identifier: "BusinessDocument",
}) as any as S.Schema<BusinessDocument>;

/** Business Party related Document data collected from the customer.. For example SSN, ITIN, Business registration number that were collected from the user. */
export type BusinessDocumentList = Array<BusinessDocument>;
export const BusinessDocumentList = /*@__PURE__*/ S.Array(
  BusinessDocument,
) as any as S.Schema<BusinessDocumentList>;

export interface IndividualBeneficialOwner {
  /** The encrypted party ID. */
  id?: string;
  names?: PersonNameList;
  /** The citizenship country code of the person. */
  citizenship?: string;
  addresses?: PersonAddressDetailList;
  phones?: PersonPhoneDetailList;
  /** The person's birth details. */
  birth_details?: BirthDetails;
  documents?: PersonDocumentList;
  /** The percentage of shares this person owns in the company. */
  percentage_of_ownership?: string;
}
export const IndividualBeneficialOwner = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    names: S.optional(PersonNameList),
    citizenship: S.optional(S.String),
    addresses: S.optional(PersonAddressDetailList),
    phones: S.optional(PersonPhoneDetailList),
    birth_details: S.optional(BirthDetails),
    documents: S.optional(PersonDocumentList),
    percentage_of_ownership: S.optional(S.String),
  }),
).annotate({
  identifier: "IndividualBeneficialOwner",
}) as any as S.Schema<IndividualBeneficialOwner>;

/** Individual beneficial owners. */
export type IndividualBeneficialOwnerList = Array<IndividualBeneficialOwner>;
export const IndividualBeneficialOwnerList = /*@__PURE__*/ S.Array(
  IndividualBeneficialOwner,
) as any as S.Schema<IndividualBeneficialOwnerList>;

/** Business incorporation information. */
export type BusinessBeneficialOwnerBusinessIncorporation =
  BusinessEntityInputBusinessIncorporation;
export const BusinessBeneficialOwnerBusinessIncorporation =
  BusinessEntityInputBusinessIncorporation;

export interface BusinessBeneficialOwner {
  /** Information related to the business like the nature of business, started date etc. */
  business_type?: BusinessTypeInfo;
  /** Information related to the business like the nature of business, started date etc. */
  business_industry?: BusinessIndustry;
  /** Business incorporation information. */
  business_incorporation?: BusinessEntityInputBusinessIncorporation;
  names?: BusinessNameDetailList;
  emails?: EmailList;
  /** Website of the business. */
  website?: string;
  addresses?: BusinessAddressDetailList;
  phones?: BusinessPhoneDetailList;
  documents?: BusinessDocumentList;
  /** The percentage of shares this person owns in the company. */
  percentage_of_ownership?: string;
}
export const BusinessBeneficialOwner = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    business_type: S.optional(BusinessTypeInfo),
    business_industry: S.optional(BusinessIndustry),
    business_incorporation: S.optional(
      BusinessEntityInputBusinessIncorporation,
    ),
    names: S.optional(BusinessNameDetailList),
    emails: S.optional(EmailList),
    website: S.optional(S.String),
    addresses: S.optional(BusinessAddressDetailList),
    phones: S.optional(BusinessPhoneDetailList),
    documents: S.optional(BusinessDocumentList),
    percentage_of_ownership: S.optional(S.String),
  }),
).annotate({
  identifier: "BusinessBeneficialOwner",
}) as any as S.Schema<BusinessBeneficialOwner>;

/** Business beneficial owners. */
export type BusinessBeneficialOwnerList = Array<BusinessBeneficialOwner>;
export const BusinessBeneficialOwnerList = /*@__PURE__*/ S.Array(
  BusinessBeneficialOwner,
) as any as S.Schema<BusinessBeneficialOwnerList>;

/** Beneficial owners of the entity. */
export interface BeneficialOwners {
  individual_beneficial_owners?: IndividualBeneficialOwnerList;
  business_beneficial_owners?: BusinessBeneficialOwnerList;
}
export const BeneficialOwners = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    individual_beneficial_owners: S.optional(IndividualBeneficialOwnerList),
    business_beneficial_owners: S.optional(BusinessBeneficialOwnerList),
  }),
).annotate({
  identifier: "BeneficialOwners",
}) as any as S.Schema<BeneficialOwners>;

export interface BusinessEntity {
  /** Information related to the business like the nature of business, started date etc. */
  business_type?: BusinessTypeInfo;
  /** Information related to the business like the nature of business, started date etc. */
  business_industry?: BusinessIndustry;
  /** Business incorporation information. */
  business_incorporation?: BusinessEntityInputBusinessIncorporation;
  names?: BusinessNameDetailList;
  emails?: EmailList;
  /** Website of the business. */
  website?: string;
  addresses?: BusinessAddressDetailList;
  phones?: BusinessPhoneDetailList;
  documents?: BusinessDocumentList;
  /** List of beneficial owners part of the entity. They can be either a Person or a business entity. */
  beneficial_owners?: BeneficialOwners;
  /** The range for the total annual sales volume of the business. */
  annual_sales_volume_range?: CurrencyRange;
  /** The range for the average monthly volume of the business. */
  average_monthly_volume_range?: CurrencyRange;
  /** The account's purpose code. */
  purpose_code?: unknown;
  /** The business goals description. For example, a mission statement. */
  business_description?: string;
}
export const BusinessEntity = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    business_type: S.optional(BusinessTypeInfo),
    business_industry: S.optional(BusinessIndustry),
    business_incorporation: S.optional(
      BusinessEntityInputBusinessIncorporation,
    ),
    names: S.optional(BusinessNameDetailList),
    emails: S.optional(EmailList),
    website: S.optional(S.String),
    addresses: S.optional(BusinessAddressDetailList),
    phones: S.optional(BusinessPhoneDetailList),
    documents: S.optional(BusinessDocumentList),
    beneficial_owners: S.optional(BeneficialOwners),
    annual_sales_volume_range: S.optional(CurrencyRange),
    average_monthly_volume_range: S.optional(CurrencyRange),
    purpose_code: S.optional(S.Unknown),
    business_description: S.optional(S.String),
  }),
).annotate({ identifier: "BusinessEntity" }) as any as S.Schema<BusinessEntity>;

/** The customer's referral data that partners share with PayPal. */
export interface ReferralData {
  individual_owners?: IndividualOwnerList;
  /** Business entity of the account. */
  business_entity?: BusinessEntity;
  /** Email address of the customer used to create the account. */
  email?: string;
  /** The preferred [locale code](/docs/api/reference/locale-codes/) to use in the onboarding flow for the customer. */
  preferred_language_code?: string;
  /** The partner's unique identifier for this customer in their system which can be used to track user in PayPal. */
  tracking_id?: string;
  /** The configuration property that the partner intends to override for this onboarding request. */
  partner_config_override?: PartnerConfigOverride;
  /** Array of financial instruments attached to the customer's account. */
  financial_instruments?: FinancialInstruments;
  operations: OperationList;
  products?: ProductList;
  capabilities?: CapabilitiesList;
  outside_process_dependencies?: DependentProcessList;
  legal_consents: LegalConsentList;
  /** Payout specific attributes. */
  payout_attributes?: PayoutAttributes;
  /** Legal Country Code. */
  legal_country_code?: string;
}
export const ReferralData = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    individual_owners: S.optional(IndividualOwnerList),
    business_entity: S.optional(BusinessEntity),
    email: S.optional(S.String),
    preferred_language_code: S.optional(S.String),
    tracking_id: S.optional(S.String),
    partner_config_override: S.optional(PartnerConfigOverride),
    financial_instruments: S.optional(FinancialInstruments),
    operations: OperationList,
    products: S.optional(ProductList),
    capabilities: S.optional(CapabilitiesList),
    outside_process_dependencies: S.optional(DependentProcessList),
    legal_consents: LegalConsentList,
    payout_attributes: S.optional(PayoutAttributes),
    legal_country_code: S.optional(S.String),
  }),
).annotate({ identifier: "ReferralData" }) as any as S.Schema<ReferralData>;

/** An array of request-related [HATEOAS links](/docs/api/overview/#hateoas-links). */
export type ReferralDataResponseDefinitionsLinkDescriptionList =
  Array<LinkDescription>;
export const ReferralDataResponseDefinitionsLinkDescriptionList =
  /*@__PURE__*/ S.Array(
    LinkDescription,
  ) as any as S.Schema<ReferralDataResponseDefinitionsLinkDescriptionList>;

/** The share referral data response. */
export interface ReferralDataResponse {
  /** The ID to access the customer's data shared by the partner with PayPal. */
  partner_referral_id?: string;
  /** The payer ID of the partner who shared the referral data. */
  submitter_payer_id?: string;
  /** The client ID of the partner who shared the referral data. This cliend ID will be returned only when the caller is determined as an internal partner, which means the scope of the caller's security context only have "https://uri.paypal.com/services/customer/partner-referrals", For more information, see <a href="https://github.paypal.com/Customers-R/partnselleronbnodeserv/blob/b0a631c43e80a4dd43d34c01f479c272b3287796/src/helpers/securityContextHelpers.js#L143">Api Actor type in Typhoon service</a>. */
  submitter_client_id?: string;
  referral_data?: ReferralData;
  links?: ReferralDataResponseDefinitionsLinkDescriptionList;
}
export const ReferralDataResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    partner_referral_id: S.optional(S.String),
    submitter_payer_id: S.optional(S.String),
    submitter_client_id: S.optional(S.String),
    referral_data: S.optional(ReferralData),
    links: S.optional(ReferralDataResponseDefinitionsLinkDescriptionList),
  }),
).annotate({
  identifier: "ReferralDataResponse",
}) as any as S.Schema<ReferralDataResponse>;

export type PartnerReferralsCreateError =
  | BadRequest
  | Forbidden
  | UnprocessableEntity
  | PaypalOpError;
/** Create partner referral Creates a partner referral that is shared by the partner or API caller. The partner referral is used to onboard the seller, and contains the seller's personal, business, financial and operations. */
export const partnerReferralsCreate: API.OperationMethod<
  PartnerReferralsCreateRequest,
  CreateReferralDataResponse,
  PartnerReferralsCreateError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PartnerReferralsCreateRequest,
  output: CreateReferralDataResponse,
  errors: [BadRequest, Forbidden, UnprocessableEntity, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));

export type PartnerReferralsReadError = Forbidden | PaypalOpError;
/** Show referral data Shows details by ID for referral data that was shared by the partner or API caller. */
export const partnerReferralsRead: API.OperationMethod<
  PartnerReferralsReadRequest,
  ReferralDataResponse,
  PartnerReferralsReadError,
  PaypalOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PartnerReferralsReadRequest,
  output: ReferralDataResponse,
  errors: [Forbidden, UnknownPaypalError],
  protocol: PaypalProtocol,
  retry: Retry.Retry,
}));
