/**
 * Trigger.dev pagination — hand-written.
 *
 * Paginated list operations that the converter marks as cursor-mode are
 * traversed by core's {@link paginateCursor}.
 */
export { paginateCursor } from "@distilled.cloud/core/pagination";
