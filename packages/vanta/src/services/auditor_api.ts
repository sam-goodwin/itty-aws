// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-vanta). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  VantaProtocol,
  type VantaOpError,
  type VantaOpContext,
} from "../protocol.ts";
import { UnknownVantaError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { VantaOpError, VantaOpContext };

export interface AcceptInformationRequestEvidenceRequest {
  auditId: string;
  requestId: string;
  /** Email of the auditor accepting the evidence. Must match an existing Vanta user who belongs to the audit firm making the API request. */
  auditorEmail: string;
}
export const AcceptInformationRequestEvidenceRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      requestId: S.String.pipe(T.Label()),
      auditorEmail: S.String,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/audits/{auditId}/information-requests/{requestId}/accept-evidence",
        code: 200,
      }),
    ),
).annotate({
  identifier: "AcceptInformationRequestEvidenceRequest",
}) as any as S.Schema<AcceptInformationRequestEvidenceRequest>;

/** Additional control IDs beyond those automatically mapped from framework codes. Allows manual association with specific controls when automatic mapping is insufficient. Each ID should reference a valid control in your audit framework. */
export type InformationRequestAdditionalControlIdsList = Array<string>;
export const InformationRequestAdditionalControlIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<InformationRequestAdditionalControlIdsList>;

/** Current approval status of the information request, tracking its lifecycle through the audit process. The status progresses through the workflow: initial state → awaiting review → approved or flagged. Status can move between awaiting review and flagged states as evidence is reviewed and resubmitted. */
export type InformationRequestApprovalStatus =
  | "NEEDS_EVIDENCE"
  | "READY_FOR_AUDIT"
  | "AUDITOR_APPROVED"
  | "AUDITOR_FLAGGED";
export const InformationRequestApprovalStatus = /*@__PURE__*/ S.String;

/** Frequency cadence for the information request, indicating how often it recurs. */
export type InformationRequestCadence =
  | "ANNUALLY"
  | "BIANNUALLY"
  | "MONTHLY"
  | "QUARTERLY";
export const InformationRequestCadence = /*@__PURE__*/ S.String;

/** The framework codes this request addresses. Links the request to specific compliance requirements. Can be an empty array if no framework codes are associated. These codes correspond to standards like SOC 2, ISO 27001, etc. */
export type InformationRequestFrameworkCodesList = Array<string>;
export const InformationRequestFrameworkCodesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<InformationRequestFrameworkCodesList>;

/** Type of information request, defining what scope of evidence is needed. - POINT_IN_TIME: Evidence for a specific moment (e.g., current state of a policy) - POPULATION: Evidence covering all items in a category (e.g., all employees) - SAMPLE: Evidence for a representative sample (e.g., 10 random customer records) */
export type InformationRequestType = "POINT_IN_TIME" | "POPULATION" | "SAMPLE";
export const InformationRequestType = /*@__PURE__*/ S.String;

export type ResourceOwnerCase0Type = "user";
export const ResourceOwnerCase0Type = /*@__PURE__*/ S.String;

export interface ResourceOwnerCase0 {
  displayName: string;
  id: string;
  type: ResourceOwnerCase0Type;
}
export const ResourceOwnerCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    displayName: S.String,
    id: S.String,
    type: ResourceOwnerCase0Type,
  }),
).annotate({
  identifier: "ResourceOwnerCase0",
}) as any as S.Schema<ResourceOwnerCase0>;

export type ResourceOwnerCase1Type = "team";
export const ResourceOwnerCase1Type = /*@__PURE__*/ S.String;

export interface ResourceOwnerCase1 {
  displayName: string;
  id: string;
  type: ResourceOwnerCase1Type;
}
export const ResourceOwnerCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    displayName: S.String,
    id: S.String,
    type: ResourceOwnerCase1Type,
  }),
).annotate({
  identifier: "ResourceOwnerCase1",
}) as any as S.Schema<ResourceOwnerCase1>;

/** Represents either a user or a team owning a resource. */
export type ResourceOwner = ResourceOwnerCase0 | ResourceOwnerCase1;
export const ResourceOwner =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ResourceOwner>;

/** Information Request resource representing a single request for audit evidence from a customer. An information request is created by an auditor and shared with the customer organization. The customer then uploads evidence, which the auditor reviews and either approves or flags for issues. */
export interface InformationRequest {
  /** The unique identifier for the information request within Vanta's system. This is the primary identifier used in all API endpoints. Format: ObjectId as a string (e.g., "6890e473dce1da5d8406f5e7") */
  id: string;
  /** External unique ID to prevent duplicates across different audit systems. Used for idempotency when syncing data between external audit management systems and Vanta. Unlike `id`, this value is provided by the external system. */
  uniqueId: string;
  /** Additional control IDs beyond those automatically mapped from framework codes. Allows manual association with specific controls when automatic mapping is insufficient. Each ID should reference a valid control in your audit framework. */
  additionalControlIds: InformationRequestAdditionalControlIdsList;
  /** Current approval status tracking the request's lifecycle through evidence submission and auditor review. */
  approvalStatus: InformationRequestApprovalStatus;
  /** How frequently this information request recurs (e.g., annual password policy reviews). Null for one-time requests. */
  cadence: InformationRequestCadence | null;
  /** The framework codes this request addresses. Links the request to specific compliance requirements. Can be an empty array if no framework codes are associated. These codes correspond to standards like SOC 2, ISO 27001, etc. */
  frameworkCodes: InformationRequestFrameworkCodesList;
  /** Detailed description explaining what evidence is needed and why. Should provide clear instructions to help the customer understand what to submit. */
  description: string | null;
  /** The deadline by which the customer must fulfill this request. Null if no specific deadline is set. Format: ISO 8601 UTC timestamp. */
  dueDate: string | null;
  /** The earliest date for which evidence should be captured. Evidence dated before this date may not be accepted. Null if not restricted. Format: ISO 8601 UTC timestamp. */
  evidenceCaptureDate: string | null;
  /** Non-unique external reference ID for this request. Unlike `uniqueId` which must be unique, `requestId` is for display/reference purposes only (e.g., "REQ-123"). Null if not provided. */
  requestId: string | null;
  /** Defines the scope of evidence required. */
  requestType: InformationRequestType;
  /** Short, descriptive title summarizing what is being requested. */
  title: string;
  /** Timestamp when the request was created in the system. Format: ISO 8601 UTC timestamp. */
  creationDate: string;
  /** Timestamp when the request was last modified. Format: ISO 8601 UTC timestamp. */
  modificationDate: string;
  /** Timestamp when the request was soft-deleted. Null if the request has not been deleted. Soft deletes allow retaining history while hiding the request from normal operations. Format: ISO 8601 UTC timestamp. */
  deletionDate: string | null;
  /** Resource owner (user or team) assigned to this information request. Returns null if no resource owner is assigned. */
  ownerAssignment: ResourceOwner | null;
}
export const InformationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    uniqueId: S.String,
    additionalControlIds: InformationRequestAdditionalControlIdsList,
    approvalStatus: InformationRequestApprovalStatus,
    cadence: S.NullOr(InformationRequestCadence),
    frameworkCodes: InformationRequestFrameworkCodesList,
    description: S.NullOr(S.String),
    dueDate: S.NullOr(S.String),
    evidenceCaptureDate: S.NullOr(S.String),
    requestId: S.NullOr(S.String),
    requestType: InformationRequestType,
    title: S.String,
    creationDate: S.String,
    modificationDate: S.String,
    deletionDate: S.NullOr(S.String),
    ownerAssignment: S.NullOr(ResourceOwner),
  }),
).annotate({
  identifier: "InformationRequest",
}) as any as S.Schema<InformationRequest>;

export interface CreateAuditorRequest {
  /** Email of the new user. */
  email: string;
  /** First name of the new user. */
  givenName: string;
  /** Last name of the new user. */
  familyName: string;
}
export const CreateAuditorRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email: S.String,
    givenName: S.String,
    familyName: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/auditors", code: 200 })),
).annotate({
  identifier: "CreateAuditorRequest",
}) as any as S.Schema<CreateAuditorRequest>;

export interface Auditor {
  id: string;
  /** The unique identifier for the organization. */
  organizationId: string;
  /** The email address of the auditor. */
  email: string;
  /** The given name (first name) of the auditor. */
  givenName: string;
  /** The family name (last name) of the auditor. */
  familyName: string;
}
export const Auditor = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    organizationId: S.String,
    email: S.String,
    givenName: S.String,
    familyName: S.String,
  }),
).annotate({ identifier: "Auditor" }) as any as S.Schema<Auditor>;

export interface CreateCommentForAuditEvidenceRequest {
  auditId: string;
  auditEvidenceId: string;
  /** Text value of the comment */
  text: string;
  /** Email of author. Must match an existing Vanta user and the user must exist under the Audit Firm who is making the API request */
  email: string;
  /** When the comment was created in the external system */
  creationDate: string;
}
export const CreateCommentForAuditEvidenceRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      auditEvidenceId: S.String.pipe(T.Label()),
      text: S.String,
      email: S.String,
      creationDate: S.String,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/audits/{auditId}/evidence/{auditEvidenceId}/comments",
        code: 200,
      }),
    ),
).annotate({
  identifier: "CreateCommentForAuditEvidenceRequest",
}) as any as S.Schema<CreateCommentForAuditEvidenceRequest>;

export interface Comment {
  /** The unique identifier for the comment */
  id: string;
  /** The unique identifier for the audit evidence related to the comment. */
  auditEvidenceId: string;
  /** The comment message */
  text: string;
  /** When the comment was created */
  creationDate: string;
  /** When the comment was updated */
  modificationDate: string | null;
  /** When the comment was deleted */
  deletionDate: string | null;
  /** The email of the comment author. This acts as a unique identifier to map users between Vanta and external systems. */
  email: string | null;
  /** Human-readable display name of the comment author. Null if the author's name is not available (e.g., user was deleted). This enables correct author attribution in integrations where users cannot be reliably matched across systems by email alone. */
  authorName: string | null;
}
export const Comment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    auditEvidenceId: S.String,
    text: S.String,
    creationDate: S.String,
    modificationDate: S.NullOr(S.String),
    deletionDate: S.NullOr(S.String),
    email: S.NullOr(S.String),
    authorName: S.NullOr(S.String),
  }),
).annotate({ identifier: "Comment" }) as any as S.Schema<Comment>;

export interface CreateCommentForControlRequest {
  auditId: string;
  controlId: string;
  /** The text content of the comment. Must be at least 1 character. Can include questions, clarifications, or explanations related to the control. */
  text: string;
  /** Email address of the comment author. Must match an existing Vanta user who belongs to the audit firm making the API request. This email uniquely identifies the author across systems. */
  email: string;
  /** Timestamp when the comment was created in the external audit management system. This allows synchronizing comment timestamps from external systems. Format: ISO 8601 UTC timestamp. */
  creationDate: string;
}
export const CreateCommentForControlRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    controlId: S.String.pipe(T.Label()),
    text: S.String,
    email: S.String,
    creationDate: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/audits/{auditId}/controls/{controlId}/comments",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateCommentForControlRequest",
}) as any as S.Schema<CreateCommentForControlRequest>;

/** A comment on a control within an audit. These threaded discussions let auditors and customers collaborate on a specific control — asking questions, documenting reasoning, or recording follow-ups — directly against the control being assessed. Audit control comments are scoped to a single audit engagement and are distinct from any organization-internal control comments. */
export interface AuditControlComment {
  /** The unique identifier for the comment within Vanta's system. Format: ObjectId as a string (e.g., "6890e473dce1da5d8406f5e7"). */
  id: string;
  /** The comment message content. Can include explanations, questions, or clarifications about the control. */
  text: string;
  /** Timestamp when the comment was created. Format: ISO 8601 UTC timestamp. */
  creationDate: string;
  /** Timestamp when the comment was last edited. Null if the comment has never been modified. Format: ISO 8601 UTC timestamp. */
  modificationDate: string | null;
  /** Timestamp when the comment was soft-deleted. Null if the comment has not been deleted. Soft deletes retain the comment for audit history while hiding it from normal operations. Format: ISO 8601 UTC timestamp. */
  deletionDate: string | null;
  /** Email address of the comment author. This email uniquely identifies users between Vanta and external audit systems. Null when the comment author can't be matched to a Vanta user. */
  email: string | null;
  /** Human-readable display name of the comment author. Null if the author's name is not available (e.g., user was deleted). This enables correct author attribution in integrations where users cannot be reliably matched across systems by email alone. */
  authorName: string | null;
}
export const AuditControlComment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    text: S.String,
    creationDate: S.String,
    modificationDate: S.NullOr(S.String),
    deletionDate: S.NullOr(S.String),
    email: S.NullOr(S.String),
    authorName: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AuditControlComment",
}) as any as S.Schema<AuditControlComment>;

export interface CreateCommentForInformationRequestRequest {
  auditId: string;
  requestId: string;
  /** The text content of the comment. Must be at least 1 character. Can include questions, clarifications, or explanations related to the information request. */
  text: string;
  /** Email address of the comment author. Must match an existing Vanta user who belongs to the audit firm making the API request. This email uniquely identifies the author across systems. */
  email: string;
  /** Timestamp when the comment was created in the external audit management system. This allows synchronizing comment timestamps from external systems. Format: ISO 8601 UTC timestamp. */
  creationDate: string;
}
export const CreateCommentForInformationRequestRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      requestId: S.String.pipe(T.Label()),
      text: S.String,
      email: S.String,
      creationDate: S.String,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/audits/{auditId}/information-requests/{requestId}/comments",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "CreateCommentForInformationRequestRequest",
  }) as any as S.Schema<CreateCommentForInformationRequestRequest>;

/** A comment on an information request enables communication between auditors and customers regarding evidence requirements and submissions. These threaded discussions help clarify requests, explain or resolve questions about evidence, and are always visible to both parties once created. */
export interface InformationRequestComment {
  /** The unique identifier for the comment within Vanta's system. Format: ObjectId as a string (e.g., "6890e473dce1da5d8406f5e7"). */
  id: string;
  /** The comment message content. Can include explanations, questions, or clarifications about the information request. */
  text: string;
  /** Timestamp when the comment was created. Format: ISO 8601 UTC timestamp. */
  creationDate: string;
  /** Timestamp when the comment was last edited. Null if the comment has never been modified. Format: ISO 8601 UTC timestamp. */
  modificationDate: string | null;
  /** Timestamp when the comment was soft-deleted. Null if the comment has not been deleted. Soft deletes retain the comment for audit history while hiding it from normal operations. Format: ISO 8601 UTC timestamp. */
  deletionDate: string | null;
  /** Email address of the comment author. This email uniquely identifies users between Vanta and external audit systems. Null when the comment author can't be matched to a Vanta user. */
  email: string | null;
  /** Human-readable display name of the comment author. Null if the author's name is not available (e.g., user was deleted). This enables correct author attribution in integrations where users cannot be reliably matched across systems by email alone. */
  authorName: string | null;
}
export const InformationRequestComment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    text: S.String,
    creationDate: S.String,
    modificationDate: S.NullOr(S.String),
    deletionDate: S.NullOr(S.String),
    email: S.NullOr(S.String),
    authorName: S.NullOr(S.String),
  }),
).annotate({
  identifier: "InformationRequestComment",
}) as any as S.Schema<InformationRequestComment>;

export type ControlDomain =
  | "ARTIFICIAL_&_AUTONOMOUS_TECHNOLOGY"
  | "ASSET_MANAGEMENT"
  | "BUSINESS_CONTINUITY_&_DISASTER_RECOVERY"
  | "CAPACITY_&_PERFORMANCE_PLANNING"
  | "CHANGE_MANAGEMENT"
  | "CLOUD_SECURITY"
  | "COMPLIANCE"
  | "CONFIGURATION_MANAGEMENT"
  | "CONTINUOUS_MONITORING"
  | "CRYPTOGRAPHIC_PROTECTIONS"
  | "DATA_CLASSIFICATION_&_HANDLING"
  | "EMBEDDED_TECHNOLOGY"
  | "ENDPOINT_SECURITY"
  | "HUMAN_RESOURCES_SECURITY"
  | "IDENTIFICATION_&_AUTHENTICATION"
  | "INCIDENT_RESPONSE"
  | "INFORMATION_ASSURANCE"
  | "MAINTENANCE"
  | "MOBILE_DEVICE_MANAGEMENT"
  | "NETWORK SECURITY"
  | "PHYSICAL_&_ENVIRONMENTAL_SECURITY"
  | "PRIVACY"
  | "PROJECT_&_RESOURCE MANAGEMENT"
  | "RISK_MANAGEMENT"
  | "SECURE_ENGINEERING_&_ARCHITECTURE"
  | "SECURITY_AWARENESS_&_TRAINING"
  | "SECURITY_OPERATIONS"
  | "SECURITY_&_PRIVACY_GOVERNANCE"
  | "TECHNOLOGY_DEVELOPMENT_&_ACQUISITION"
  | "THIRD-PARTY_MANAGEMENT"
  | "THREAT_MANAGEMENT"
  | "VULNERABILITY_&_PATCH_MANAGEMENT"
  | "WEB_SECURITY"
  | "ADMINISTRATIVE"
  | "PHYSICAL"
  | "TECHNICAL"
  | "BASIC"
  | "DERIVED";
export const ControlDomain = /*@__PURE__*/ S.String;

export type FrameworkId =
  | "AU_E_8"
  | "AWS_FTR"
  | "CCPA"
  | "CIS_V8"
  | "CPS_234"
  | "DORA"
  | "FEDRAMP"
  | "GDPR"
  | "HIPAA"
  | "HITRUST_E1"
  | "ISO_27001"
  | "ISO_27001_2022"
  | "ISO_27017"
  | "ISO_27018"
  | "ISO_27701"
  | "ISO_42001"
  | "ISO_9001"
  | "MSFT_SSPA"
  | "MVSP"
  | "NIS_2D"
  | "NIST_171"
  | "NIST_171_R3"
  | "NIST_53"
  | "NIST_AI_RMF"
  | "NIST_CSF"
  | "NIST_CSF_2"
  | "OFDSS"
  | "PCI_SAQ_A"
  | "PCI_SAQ_A_EP"
  | "PCI_SAQ_D_MERCHANT"
  | "PCI_SAQ_D_SP"
  | "PCI_DDS_4"
  | "SOC_2"
  | "SOX_ITGC"
  | "UK_CYBER_ESSENTIALS"
  | "US_DATA_PRIVACY";
export const FrameworkId = /*@__PURE__*/ S.String;

export type FrameworkSectionFrameworkId = FrameworkId | string;
export const FrameworkSectionFrameworkId =
  /*@__PURE__*/ S.Unknown as any as S.Schema<FrameworkSectionFrameworkId>;

export interface FrameworkSection {
  frameworkId: FrameworkSectionFrameworkId;
  sectionId: string;
}
export const FrameworkSection = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    frameworkId: FrameworkSectionFrameworkId,
    sectionId: S.String,
  }),
).annotate({
  identifier: "FrameworkSection",
}) as any as S.Schema<FrameworkSection>;

/** Framework sections that the control should be mapped to. */
export type CreateCustomControlRequestSectionsList = Array<FrameworkSection>;
export const CreateCustomControlRequestSectionsList = /*@__PURE__*/ S.Array(
  FrameworkSection,
) as any as S.Schema<CreateCustomControlRequestSectionsList>;

export type GdprRole = "BOTH" | "CONTROLLER" | "PROCESSOR";
export const GdprRole = /*@__PURE__*/ S.String;

export interface CreateCustomControlRequest {
  auditId: string;
  /** The external id of the control. */
  externalId: string;
  /** The name of the control. */
  name: string | null;
  /** The description of the control. */
  description: string;
  /** The effective date of the control. */
  effectiveDate: string;
  /** The category of the control. See the ControlDomain enum for possible values. */
  category: ControlDomain | (string & {});
  /** Framework sections that the control should be mapped to. */
  sections?: CreateCustomControlRequestSectionsList | null;
  /** The GDPR role of the control, which specifies whether the data is being "collected" or "processed". See the GdprRole enum for possible values. This field should only be included for controls that are to be mapped to the GDPR framework. */
  role?: GdprRole | (string & {}) | null;
}
export const CreateCustomControlRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    externalId: S.String,
    name: S.NullOr(S.String),
    description: S.String,
    effectiveDate: S.String,
    category: ControlDomain,
    sections: S.optional(S.NullOr(CreateCustomControlRequestSectionsList)),
    role: S.optional(S.NullOr(GdprRole)),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/audits/{auditId}/controls/custom-controls",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateCustomControlRequest",
}) as any as S.Schema<CreateCustomControlRequest>;

export type ControlSource = "Vanta" | "Custom";
export const ControlSource = /*@__PURE__*/ S.String;

/** The security domains that the control belongs to. */
export type ControlDomainsList = Array<string>;
export const ControlDomainsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ControlDomainsList>;

export interface Owner {
  /** Unique identifier for the person. */
  id: string;
  /** Name of the person that is shown in product. */
  displayName: string;
  /** Email address of the person. */
  emailAddress: string;
}
export const Owner = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    displayName: S.String,
    emailAddress: S.String,
  }),
).annotate({ identifier: "Owner" }) as any as S.Schema<Owner>;

export type CustomFieldValueCase1List = Array<string>;
export const CustomFieldValueCase1List = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CustomFieldValueCase1List>;

export type CustomFieldValue = string | CustomFieldValueCase1List;
export const CustomFieldValue =
  /*@__PURE__*/ S.Unknown as any as S.Schema<CustomFieldValue>;

export interface CustomField {
  label: string;
  value: CustomFieldValue;
}
export const CustomField = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    label: S.String,
    value: CustomFieldValue,
  }),
).annotate({ identifier: "CustomField" }) as any as S.Schema<CustomField>;

/** The control's custom field values, if control custom fields is included in your Vanta instance. */
export type ControlCustomFieldsList = Array<CustomField>;
export const ControlCustomFieldsList = /*@__PURE__*/ S.Array(
  CustomField,
) as any as S.Schema<ControlCustomFieldsList>;

export interface Control {
  /** The control's unique ID. */
  id: string;
  /** The control's external ID. */
  externalId: string | null;
  /** The control's name. */
  name: string;
  /** The control's description. */
  description: string;
  /** The control's source, either "VANTA" or "CUSTOM". */
  source: ControlSource;
  /** The security domains that the control belongs to. */
  domains: ControlDomainsList;
  /** The control's owner. */
  owner: Owner | null;
  /** The control's GDPR role, if the control is a GDPR control. */
  role?: string | null;
  /** The control's custom field values, if control custom fields is included in your Vanta instance. */
  customFields: ControlCustomFieldsList;
  /** When the control was created. Returns null for Vanta library controls. */
  creationDate: string | null;
  /** When the control was last modified. Returns null for Vanta library controls. */
  modificationDate: string | null;
}
export const Control = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    externalId: S.NullOr(S.String),
    name: S.String,
    description: S.String,
    source: ControlSource,
    domains: ControlDomainsList,
    owner: S.NullOr(Owner),
    role: S.optional(S.NullOr(S.String)),
    customFields: ControlCustomFieldsList,
    creationDate: S.NullOr(S.String),
    modificationDate: S.NullOr(S.String),
  }),
).annotate({ identifier: "Control" }) as any as S.Schema<Control>;

/** A set of controls, referenced by id, to map the evidence to */
export type CreateCustomEvidenceRequestRequestControlIdsList = Array<string>;
export const CreateCustomEvidenceRequestRequestControlIdsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateCustomEvidenceRequestRequestControlIdsList>;

export type RecurrenceDuration =
  | "P0D"
  | "P1D"
  | "P1W"
  | "P1M"
  | "P3M"
  | "P6M"
  | "P1Y"
  | "P2Y";
export const RecurrenceDuration = /*@__PURE__*/ S.String;

export interface CreateCustomEvidenceRequestRequest {
  auditId: string;
  /** A set of controls, referenced by id, to map the evidence to */
  controlIds: CreateCustomEvidenceRequestRequestControlIdsList;
  /** Title for the evidence request */
  title: string;
  /** Description for the evidence request */
  description: string;
  /** Renewal cadence */
  cadence: RecurrenceDuration | (string & {});
  /** Duration representing when to send notifications, relative to renewal date */
  reminderWindow: RecurrenceDuration | (string & {});
  /** Whether this document contains sensitive data and needs more restrictive read access */
  isRestricted: boolean;
  /** Email of the auditor who created the custom evidence request. */
  auditorEmail: string;
}
export const CreateCustomEvidenceRequestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    controlIds: CreateCustomEvidenceRequestRequestControlIdsList,
    title: S.String,
    description: S.String,
    cadence: RecurrenceDuration,
    reminderWindow: RecurrenceDuration,
    isRestricted: S.Boolean,
    auditorEmail: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/audits/{auditId}/evidence/custom-evidence-requests",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateCustomEvidenceRequestRequest",
}) as any as S.Schema<CreateCustomEvidenceRequestRequest>;

/** A set of controls, referenced by id, to map the evidence to */
export type CustomEvidenceRequestControlIdsList = Array<string>;
export const CustomEvidenceRequestControlIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CustomEvidenceRequestControlIdsList>;

export interface CustomEvidenceRequest {
  /** Internal id of the custom evidence request within Vanta */
  id: string;
  /** A set of controls, referenced by id, to map the evidence to */
  controlIds: CustomEvidenceRequestControlIdsList;
  /** Title for the evidence request */
  title: string;
  /** Description for the evidence request */
  description: string;
  /** Renewal cadence */
  cadence: RecurrenceDuration;
  /** Duration representing when to send notifications, relative to renewal date */
  reminderWindow: RecurrenceDuration;
  /** Whether this document contains sensitive data and needs more restrictive read access */
  isRestricted: boolean;
}
export const CustomEvidenceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    controlIds: CustomEvidenceRequestControlIdsList,
    title: S.String,
    description: S.String,
    cadence: RecurrenceDuration,
    reminderWindow: RecurrenceDuration,
    isRestricted: S.Boolean,
  }),
).annotate({
  identifier: "CustomEvidenceRequest",
}) as any as S.Schema<CustomEvidenceRequest>;

/** Framework codes this request addresses. An empty array if no framework codes are associated. */
export type CreateInformationRequestRequestFrameworkCodesList = Array<string>;
export const CreateInformationRequestRequestFrameworkCodesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateInformationRequestRequestFrameworkCodesList>;

/** Control IDs to link directly to this request, beyond those automatically mapped from framework codes. Each must be the `id` of an existing control in the customer's organization (the identifier returned by the controls endpoints). The request is rejected if any ID does not match a control. Omit or pass an empty array for no direct control links. */
export type CreateInformationRequestRequestAdditionalControlIdsList =
  Array<string>;
export const CreateInformationRequestRequestAdditionalControlIdsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateInformationRequestRequestAdditionalControlIdsList>;

export interface CreateInformationRequestRequest {
  auditId: string;
  /** External unique ID to prevent duplicate requests across audit systems. Must be unique within the audit. Used for idempotency. */
  uniqueId: string;
  /** Short, descriptive title summarizing what is being requested. Must be at least 1 character. */
  title: string;
  /** Scope of evidence required. */
  requestType: InformationRequestType | (string & {});
  /** Detailed description explaining what evidence is needed. Helps the customer understand what to submit. Optional. */
  description?: string | null;
  /** Framework codes this request addresses. An empty array if no framework codes are associated. */
  frameworkCodes: CreateInformationRequestRequestFrameworkCodesList;
  /** How frequently this request recurs. Null or omitted for one-time requests. */
  cadence?: InformationRequestCadence | (string & {}) | null;
  /** Deadline for fulfilling this request. Null or omitted if no deadline. Format: ISO 8601 UTC timestamp. */
  dueDate?: string | null;
  /** Start date of the audit period. Evidence before this date may not be accepted. Null or omitted if not restricted. Format: ISO 8601 UTC timestamp. */
  evidenceCaptureDate?: string | null;
  /** Control IDs to link directly to this request, beyond those automatically mapped from framework codes. Each must be the `id` of an existing control in the customer's organization (the identifier returned by the controls endpoints). The request is rejected if any ID does not match a control. Omit or pass an empty array for no direct control links. */
  additionalControlIds?: CreateInformationRequestRequestAdditionalControlIdsList;
}
export const CreateInformationRequestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    uniqueId: S.String,
    title: S.String,
    requestType: InformationRequestType,
    description: S.optional(S.NullOr(S.String)),
    frameworkCodes: CreateInformationRequestRequestFrameworkCodesList,
    cadence: S.optional(S.NullOr(InformationRequestCadence)),
    dueDate: S.optional(S.NullOr(S.String)),
    evidenceCaptureDate: S.optional(S.NullOr(S.String)),
    additionalControlIds: S.optional(
      CreateInformationRequestRequestAdditionalControlIdsList,
    ),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/audits/{auditId}/information-requests",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateInformationRequestRequest",
}) as any as S.Schema<CreateInformationRequestRequest>;

export interface DeleteCommentForControlRequest {
  auditId: string;
  controlId: string;
  commentId: string;
  /** Email address of the comment author. Must match an existing Vanta user who belongs to the audit firm making the API request. This email uniquely identifies the author across systems. */
  email: string;
}
export const DeleteCommentForControlRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    controlId: S.String.pipe(T.Label()),
    commentId: S.String.pipe(T.Label()),
    email: S.String,
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/audits/{auditId}/controls/{controlId}/comments/{commentId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteCommentForControlRequest",
}) as any as S.Schema<DeleteCommentForControlRequest>;

export interface DeleteCommentForControlResponse {}
export const DeleteCommentForControlResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteCommentForControlResponse",
}) as any as S.Schema<DeleteCommentForControlResponse>;

export interface DeleteCommentForInformationRequestRequest {
  auditId: string;
  requestId: string;
  commentId: string;
  /** Email address of the comment author. Must match an existing Vanta user who belongs to the audit firm making the API request. This email uniquely identifies the author across systems. */
  email: string;
}
export const DeleteCommentForInformationRequestRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      requestId: S.String.pipe(T.Label()),
      commentId: S.String.pipe(T.Label()),
      email: S.String,
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/audits/{auditId}/information-requests/{requestId}/comments/{commentId}",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "DeleteCommentForInformationRequestRequest",
  }) as any as S.Schema<DeleteCommentForInformationRequestRequest>;

export interface DeleteCommentForInformationRequestResponse {}
export const DeleteCommentForInformationRequestResponse =
  /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
    identifier: "DeleteCommentForInformationRequestResponse",
  }) as any as S.Schema<DeleteCommentForInformationRequestResponse>;

export interface DeleteInformationRequestRequest {
  auditId: string;
  requestId: string;
}
export const DeleteInformationRequestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    requestId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/audits/{auditId}/information-requests/{requestId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteInformationRequestRequest",
}) as any as S.Schema<DeleteInformationRequestRequest>;

export interface DeleteInformationRequestResponse {}
export const DeleteInformationRequestResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteInformationRequestResponse",
}) as any as S.Schema<DeleteInformationRequestResponse>;

/** Emails of auditors who may access the new audit. Minimum one entry required. Each email must match an active user in the authenticated audit firm's domain. */
export type DuplicateRequestAllowAuditorEmailsList = Array<string>;
export const DuplicateRequestAllowAuditorEmailsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<DuplicateRequestAllowAuditorEmailsList>;

export interface DuplicateRequest {
  /** ID of the source IRL audit to duplicate. */
  sourceAuditId: string;
  /** Display name for the new audit engagement. */
  displayName: string;
  /** Start of the audit window for the new audit. */
  auditStartDate: string;
  /** End of the audit window for the new audit. */
  auditEndDate: string;
  /** When auditors gain access to the new audit. */
  earlyAccessStartsAt: string;
  /** Emails of auditors who may access the new audit. Minimum one entry required. Each email must match an active user in the authenticated audit firm's domain. */
  allowAuditorEmails: DuplicateRequestAllowAuditorEmailsList;
}
export const DuplicateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sourceAuditId: S.String,
    displayName: S.String,
    auditStartDate: S.String,
    auditEndDate: S.String,
    earlyAccessStartsAt: S.String,
    allowAuditorEmails: DuplicateRequestAllowAuditorEmailsList,
  }).pipe(T.Http({ method: "POST", uri: "/audits/duplicate", code: 200 })),
).annotate({
  identifier: "DuplicateRequest",
}) as any as S.Schema<DuplicateRequest>;

/** Emails of auditors with access to audit */
export type AuditAllowAuditorEmailsList = Array<string>;
export const AuditAllowAuditorEmailsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AuditAllowAuditorEmailsList>;

export type AuditFocus = "EXTERNAL" | "INTERNAL";
export const AuditFocus = /*@__PURE__*/ S.String;

/** Metadata about the auditor request list. This field is only present for IRL (Information Request List) based audits and will be undefined for standard audits. Use the presence of this field to differentiate between IRL and non-IRL audits. */
export interface AuditAuditorRequestListMetadata {
  /** Timestamp when information requests were shared with the customer. Null if not shared. */
  requestsSharedWithCustomer: string | null;
}
export const AuditAuditorRequestListMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    requestsSharedWithCustomer: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AuditAuditorRequestListMetadata",
}) as any as S.Schema<AuditAuditorRequestListMetadata>;

export type AuditSegmentKind = "PROGRAM" | "SYSTEM";
export const AuditSegmentKind = /*@__PURE__*/ S.String;

/** The framework reached through a segment. */
export interface AuditSegmentFramework {
  /** The stable identifier of the framework (for example `soc2`). */
  id: string;
  /** The human-readable framework name. */
  displayName: string;
  /** The short framework name used in compact UI. */
  shortName: string;
}
export const AuditSegmentFramework = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    displayName: S.String,
    shortName: S.String,
  }),
).annotate({
  identifier: "AuditSegmentFramework",
}) as any as S.Schema<AuditSegmentFramework>;

/** A business unit or information-system association on an audit segment. */
export interface AuditSegmentAssociation {
  /** The unique identifier of the associated business unit or information system. */
  id: string;
  /** The human-readable name of the associated business unit or information system. */
  displayName: string;
}
export const AuditSegmentAssociation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    displayName: S.String,
  }),
).annotate({
  identifier: "AuditSegmentAssociation",
}) as any as S.Schema<AuditSegmentAssociation>;

export type AuditSoc2ReportType = "soc2type1" | "soc2type2";
export const AuditSoc2ReportType = /*@__PURE__*/ S.String;

export type AuditSoc2Tsc =
  | "SECURITY"
  | "AVAILABILITY"
  | "CONFIDENTIALITY"
  | "PRIVACY"
  | "PROCESSING_INTEGRITY";
export const AuditSoc2Tsc = /*@__PURE__*/ S.String;

/** Trust Services Criteria this segment covers. */
export type AuditSoc2DeliverableTscsList = Array<AuditSoc2Tsc>;
export const AuditSoc2DeliverableTscsList = /*@__PURE__*/ S.Array(
  AuditSoc2Tsc,
) as any as S.Schema<AuditSoc2DeliverableTscsList>;

/** Per-segment SOC 2 report configuration. Null when the segment is not SOC 2 or has no configured deliverable. */
export interface AuditSoc2Deliverable {
  /** SOC 2 report type for this segment. */
  reportType: AuditSoc2ReportType;
  /** Trust Services Criteria this segment covers. */
  tscs: AuditSoc2DeliverableTscsList;
}
export const AuditSoc2Deliverable = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    reportType: AuditSoc2ReportType,
    tscs: AuditSoc2DeliverableTscsList,
  }),
).annotate({
  identifier: "AuditSoc2Deliverable",
}) as any as S.Schema<AuditSoc2Deliverable>;

/** One segment of an audit's scope. A live single-framework audit has exactly one entry; a live multi-framework audit has one per in-scope framework (and business unit or system, when applicable). Soft-deleted audits return an empty list. `displayName` is the segment label owned by audit scope (for example "{Framework}" or "{Framework} — {Business Unit}"). `soc2Deliverable` is the per-segment SOC 2 config, or null. `businessUnit` is set for program segments that are BU-scoped and null otherwise. `system` is set for system segments and null otherwise. */
export interface AuditSegment {
  /** The unique identifier for the segment. */
  id: string;
  /** Whether this segment is a program (framework / business unit) or a system. */
  kind: AuditSegmentKind;
  /** The segment's display label. Do not reconstruct this from framework + BU. */
  displayName: string;
  /** The framework reached through this segment. */
  framework: AuditSegmentFramework;
  /** The business unit this program segment is scoped to, or null when the segment is not BU-scoped or the BU cannot be resolved. */
  businessUnit: AuditSegmentAssociation | null;
  /** The information system this system segment is scoped to, or null on program segments. */
  system: AuditSegmentAssociation | null;
  /** SOC 2 report type and Trust Services Criteria for this segment, or null. */
  soc2Deliverable: AuditSoc2Deliverable | null;
}
export const AuditSegment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    kind: AuditSegmentKind,
    displayName: S.String,
    framework: AuditSegmentFramework,
    businessUnit: S.NullOr(AuditSegmentAssociation),
    system: S.NullOr(AuditSegmentAssociation),
    soc2Deliverable: S.NullOr(AuditSoc2Deliverable),
  }),
).annotate({ identifier: "AuditSegment" }) as any as S.Schema<AuditSegment>;

/** The audit's scope as a list of segments. Always present. A live single-framework audit has one entry; a multi-framework audit has one entry per in-scope framework (and business unit or system, when applicable). Soft-deleted audits return an empty list. Prefer this over the deprecated top-level `framework` field. */
export type AuditSegmentsList = Array<AuditSegment>;
export const AuditSegmentsList = /*@__PURE__*/ S.Array(
  AuditSegment,
) as any as S.Schema<AuditSegmentsList>;

export interface Audit {
  /** The unique identifier for the audit. */
  id: string;
  /** The domain name of the customer organization being audited (e.g. vanta.com) */
  customerOrganizationName: string;
  /** The human readable name of the customer organization being audited (e.g. Vanta) */
  customerDisplayName: string | null;
  /** The uuid of the customer organization being audited */
  customerOrganizationId: string;
  /** The start of the audit window. This is also when data collection for audit starts. */
  auditStartDate: string;
  /** The end of the audit window. */
  auditEndDate: string;
  /** Timestamp at which auditors gain access to the audit. Occurs before the audit window begins */
  earlyAccessStartsAt: string | null;
  /** Legacy singular framework display name from the audit type. Incomplete for a multi-framework audit — use `segments` for framework identity. */
  framework: string;
  /** The display name for the audit. Returns the custom audit name if set, otherwise returns the framework name. */
  displayName: string;
  /** Emails of auditors with access to audit */
  allowAuditorEmails: AuditAllowAuditorEmailsList;
  /** Set to true if all auditors in audit firm have access */
  allowAllAuditors: boolean;
  /** Timestamp when the audit was deleted */
  deletionDate: string | null;
  /** Timestamp when the audit was created */
  creationDate: string;
  /** Timestamp when the audit was updated */
  modificationDate: string | null;
  /** Timestamp when the audit was marked completed, and report was uploaded */
  completionDate: string | null;
  /** Audit focus determines if the audit is internal or external facing */
  auditFocus: AuditFocus;
  /** Metadata about the auditor request list. This field is only present for IRL (Information Request List) based audits and will be undefined for standard audits. Use the presence of this field to differentiate between IRL and non-IRL audits. */
  auditorRequestListMetadata?: AuditAuditorRequestListMetadata;
  /** The audit's scope as a list of segments. Always present. A live single-framework audit has one entry; a multi-framework audit has one entry per in-scope framework (and business unit or system, when applicable). Soft-deleted audits return an empty list. Prefer this over the deprecated top-level `framework` field. */
  segments: AuditSegmentsList;
}
export const Audit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    customerOrganizationName: S.String,
    customerDisplayName: S.NullOr(S.String),
    customerOrganizationId: S.String,
    auditStartDate: S.String,
    auditEndDate: S.String,
    earlyAccessStartsAt: S.NullOr(S.String),
    framework: S.String,
    displayName: S.String,
    allowAuditorEmails: AuditAllowAuditorEmailsList,
    allowAllAuditors: S.Boolean,
    deletionDate: S.NullOr(S.String),
    creationDate: S.String,
    modificationDate: S.NullOr(S.String),
    completionDate: S.NullOr(S.String),
    auditFocus: AuditFocus,
    auditorRequestListMetadata: S.optional(AuditAuditorRequestListMetadata),
    segments: AuditSegmentsList,
  }),
).annotate({ identifier: "Audit" }) as any as S.Schema<Audit>;

export interface FlagInformationRequestEvidenceRequest {
  auditId: string;
  requestId: string;
  /** Email of the auditor flagging the evidence. Must match an existing Vanta user who belongs to the audit firm making the API request. */
  auditorEmail: string;
  /** Detailed explanation of what issues were found with the evidence. This reason is visible to the customer and guides them on what to fix. Must be at least 1 character. */
  reason: string;
}
export const FlagInformationRequestEvidenceRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      requestId: S.String.pipe(T.Label()),
      auditorEmail: S.String,
      reason: S.String,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/audits/{auditId}/information-requests/{requestId}/flag-evidence",
        code: 200,
      }),
    ),
).annotate({
  identifier: "FlagInformationRequestEvidenceRequest",
}) as any as S.Schema<FlagInformationRequestEvidenceRequest>;

export interface GetAuditRequest {
  auditId: string;
}
export const GetAuditRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/audits/{auditId}", code: 200 })),
).annotate({
  identifier: "GetAuditRequest",
}) as any as S.Schema<GetAuditRequest>;

export interface GetAuditEvidenceRequest {
  auditId: string;
  auditEvidenceId: string;
}
export const GetAuditEvidenceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    auditEvidenceId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/evidence/{auditEvidenceId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetAuditEvidenceRequest",
}) as any as S.Schema<GetAuditEvidenceRequest>;

export type AuditEvidenceState =
  | "Accepted"
  | "Flagged"
  | "Initialized"
  | "NA"
  | "Not ready for audit"
  | "Ready for audit";
export const AuditEvidenceState = /*@__PURE__*/ S.String;

export type AuditEvidenceType = "Evidence Request" | "Policy" | "Test";
export const AuditEvidenceType = /*@__PURE__*/ S.String;

/** A list sections associated to the control */
export type EvidenceControlSectionNamesList = Array<string>;
export const EvidenceControlSectionNamesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<EvidenceControlSectionNamesList>;

export interface EvidenceControl {
  /** Name of control associated to this evidence */
  name: string;
  /** A list sections associated to the control */
  sectionNames: EvidenceControlSectionNamesList;
}
export const EvidenceControl = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    sectionNames: EvidenceControlSectionNamesList,
  }),
).annotate({
  identifier: "EvidenceControl",
}) as any as S.Schema<EvidenceControl>;

/** The controls associated to this evidence */
export type EvidenceRelatedControlsList = Array<EvidenceControl>;
export const EvidenceRelatedControlsList = /*@__PURE__*/ S.Array(
  EvidenceControl,
) as any as S.Schema<EvidenceRelatedControlsList>;

export interface Evidence {
  /** Vanta internal reference to evidence */
  id: string;
  /** This is a static UUID to map Audit Firm controls to Vanta controls */
  externalId: string;
  /** Vanta internal statuses for audit evidence */
  status: AuditEvidenceState;
  /** Mutable name for evidence. Not guaranteed to be unique. */
  name: string;
  /** The date this Audit Evidence was deleted */
  deletionDate: string | null;
  /** The date this Audit Evidence was created */
  creationDate: string;
  /** Point in time that status was last updated */
  statusUpdatedDate: string;
  /** The outcome of the automated test run, for Test-type evidence */
  testStatus: string | null;
  /** The type of Audit Evidence */
  evidenceType: AuditEvidenceType;
  /** Unique identifier for evidence */
  evidenceId: string;
  /** The controls associated to this evidence */
  relatedControls: EvidenceRelatedControlsList;
  /** The description for the evidence. It will be set to null if the evidence is deleted */
  description: string | null;
}
export const Evidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    externalId: S.String,
    status: AuditEvidenceState,
    name: S.String,
    deletionDate: S.NullOr(S.String),
    creationDate: S.String,
    statusUpdatedDate: S.String,
    testStatus: S.NullOr(S.String),
    evidenceType: AuditEvidenceType,
    evidenceId: S.String,
    relatedControls: EvidenceRelatedControlsList,
    description: S.NullOr(S.String),
  }),
).annotate({ identifier: "Evidence" }) as any as S.Schema<Evidence>;

export interface GetAuditEvidenceCommentRequest {
  auditId: string;
  auditEvidenceId: string;
  commentId: string;
}
export const GetAuditEvidenceCommentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    auditEvidenceId: S.String.pipe(T.Label()),
    commentId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/evidence/{auditEvidenceId}/comments/{commentId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetAuditEvidenceCommentRequest",
}) as any as S.Schema<GetAuditEvidenceCommentRequest>;

export interface GetCommentForInformationRequestRequest {
  auditId: string;
  requestId: string;
  commentId: string;
}
export const GetCommentForInformationRequestRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      requestId: S.String.pipe(T.Label()),
      commentId: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/audits/{auditId}/information-requests/{requestId}/comments/{commentId}",
        code: 200,
      }),
    ),
).annotate({
  identifier: "GetCommentForInformationRequestRequest",
}) as any as S.Schema<GetCommentForInformationRequestRequest>;

export interface GetFrameworkCodesRequest {
  auditId: string;
}
export const GetFrameworkCodesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/framework-codes",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetFrameworkCodesRequest",
}) as any as S.Schema<GetFrameworkCodesRequest>;

/** Array of valid framework codes for the audit's framework (e.g., "CC6.1", "CC6.2"). These represent the different framework sections available for creating information requests. */
export type FrameworkCodesFrameworkCodesList = Array<string>;
export const FrameworkCodesFrameworkCodesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<FrameworkCodesFrameworkCodesList>;

/** Framework codes response resource */
export interface FrameworkCodes {
  /** Array of valid framework codes for the audit's framework (e.g., "CC6.1", "CC6.2"). These represent the different framework sections available for creating information requests. */
  frameworkCodes: FrameworkCodesFrameworkCodesList;
}
export const FrameworkCodes = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    frameworkCodes: FrameworkCodesFrameworkCodesList,
  }),
).annotate({ identifier: "FrameworkCodes" }) as any as S.Schema<FrameworkCodes>;

export interface GetInformationRequestRequest {
  auditId: string;
  requestId: string;
}
export const GetInformationRequestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    requestId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/information-requests/{requestId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetInformationRequestRequest",
}) as any as S.Schema<GetInformationRequestRequest>;

export interface GetInformationRequestEvidenceRequest {
  auditId: string;
  requestId: string;
  evidenceId: string;
}
export const GetInformationRequestEvidenceRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      requestId: S.String.pipe(T.Label()),
      evidenceId: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/audits/{auditId}/information-requests/{requestId}/evidence/{evidenceId}",
        code: 200,
      }),
    ),
).annotate({
  identifier: "GetInformationRequestEvidenceRequest",
}) as any as S.Schema<GetInformationRequestEvidenceRequest>;

export type InformationRequestSupportedEvidenceType =
  | "UPLOADED_DOCUMENT"
  | "OBSERVATION"
  | "LINK"
  | "VANTA_DOCUMENT"
  | "VANTA_POLICY"
  | "VANTA_TEST"
  | "VANTA_TEST_SNAPSHOT";
export const InformationRequestSupportedEvidenceType = /*@__PURE__*/ S.String;

/** Evidence in the form of an uploaded document file. The URL is a temporary presigned URL that expires after a certain period. */
export interface UploadedDocumentEvidence {
  /** The unique identifier for the uploaded document in Vanta's system. */
  id: string;
  /** The original filename of the uploaded document. Undefined if the filename was not captured during upload. */
  filename?: string;
  /** The MIME type of the document indicating its file format. Common types: "application/pdf", "image/png", "image/jpeg", "text/plain" */
  mimeType: string;
  /** A presigned URL for downloading the document. This URL is temporary and expires after a limited time. Request a new evidence list to get a fresh URL if expired. */
  url: string;
}
export const UploadedDocumentEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    filename: S.optional(S.String),
    mimeType: S.String,
    url: S.String,
  }),
).annotate({
  identifier: "UploadedDocumentEvidence",
}) as any as S.Schema<UploadedDocumentEvidence>;

/** Evidence in the form of a link to external documentation or resources. */
export interface LinkEvidence {
  /** The title or descriptive name for the link. */
  title: string;
  /** Optional description providing additional context about the link. */
  description?: string;
  /** The URL pointing to the external documentation or resource. */
  url: string;
}
export const LinkEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    title: S.String,
    description: S.optional(S.String),
    url: S.String,
  }),
).annotate({ identifier: "LinkEvidence" }) as any as S.Schema<LinkEvidence>;

/** Evidence in the form of a text observation or note. */
export interface ObservationEvidence {
  /** The text content of the observation. */
  observationDetails: string;
}
export const ObservationEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    observationDetails: S.String,
  }),
).annotate({
  identifier: "ObservationEvidence",
}) as any as S.Schema<ObservationEvidence>;

/** Information about the user who added this file version to the document. Undefined if the user information is not available. */
export interface VantaDocumentEvidenceAddedBy {
  email: string;
  name: string;
}
export const VantaDocumentEvidenceAddedBy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    email: S.String,
    name: S.String,
  }),
).annotate({
  identifier: "VantaDocumentEvidenceAddedBy",
}) as any as S.Schema<VantaDocumentEvidenceAddedBy>;

/** Discriminator to identify this as an uploaded file. */
export type VantaDocumentUploadedEvidenceType = "uploaded";
export const VantaDocumentUploadedEvidenceType = /*@__PURE__*/ S.String;

/** Represents an uploaded file in a Vanta Document. The file is stored in Vanta's system and accessed via a presigned URL. */
export interface VantaDocumentUploadedEvidence {
  /** Discriminator to identify this as an uploaded file. */
  type: VantaDocumentUploadedEvidenceType;
  /** The unique identifier for the file version in Vanta's system. */
  id: string;
  /** The original filename of the uploaded document. Undefined if the filename was not captured during upload. */
  filename?: string;
  /** The MIME type of the document indicating its file format. Common types: "application/pdf", "image/png", "image/jpeg", "text/plain" */
  mimeType: string;
  /** A presigned URL for downloading the document. This URL is temporary and expires after a limited time. Request a new evidence list to get a fresh URL if expired. */
  url: string;
}
export const VantaDocumentUploadedEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: VantaDocumentUploadedEvidenceType,
    id: S.String,
    filename: S.optional(S.String),
    mimeType: S.String,
    url: S.String,
  }),
).annotate({
  identifier: "VantaDocumentUploadedEvidence",
}) as any as S.Schema<VantaDocumentUploadedEvidence>;

/** Discriminator to identify this as an external link. */
export type VantaDocumentLinkEvidenceType = "link";
export const VantaDocumentLinkEvidenceType = /*@__PURE__*/ S.String;

/** Represents an external link in a Vanta Document. The link points to documentation hosted outside of Vanta's system. */
export interface VantaDocumentLinkEvidence {
  /** Discriminator to identify this as an external link. */
  type: VantaDocumentLinkEvidenceType;
  /** The unique identifier for the file version in Vanta's system. */
  id: string;
  /** The URL pointing to the external documentation or resource. */
  url: string;
}
export const VantaDocumentLinkEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: VantaDocumentLinkEvidenceType,
    id: S.String,
    url: S.String,
  }),
).annotate({
  identifier: "VantaDocumentLinkEvidence",
}) as any as S.Schema<VantaDocumentLinkEvidence>;

/** The file or link associated with this document version. Use the `type` discriminator to determine whether it's an uploaded file or an external link. */
export type VantaDocumentEvidenceFile =
  | VantaDocumentUploadedEvidence
  | VantaDocumentLinkEvidence;
export const VantaDocumentEvidenceFile =
  /*@__PURE__*/ S.Unknown as any as S.Schema<VantaDocumentEvidenceFile>;

/** Evidence in the form of a Vanta Document. Vanta Documents are managed documents with metadata like expiration dates and versioning. They can either be uploaded files stored in Vanta or links to external documentation. */
export interface VantaDocumentEvidence {
  /** The title of the document as displayed in Vanta. */
  title: string;
  /** Optional description providing additional context about the document. */
  description?: string;
  /** The date when the document version expires and should be renewed. Undefined if no expiration is set. Format: ISO 8601 UTC timestamp. */
  expirationDate?: string;
  /** Information about the user who added this file version to the document. Undefined if the user information is not available. */
  addedBy?: VantaDocumentEvidenceAddedBy;
  /** The unique identifier of the document version submitted as evidence. */
  documentVersionId: string;
  /** The file or link associated with this document version. Use the `type` discriminator to determine whether it's an uploaded file or an external link. */
  file: VantaDocumentEvidenceFile;
}
export const VantaDocumentEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    title: S.String,
    description: S.optional(S.String),
    expirationDate: S.optional(S.String),
    addedBy: S.optional(VantaDocumentEvidenceAddedBy),
    documentVersionId: S.String,
    file: VantaDocumentEvidenceFile,
  }),
).annotate({
  identifier: "VantaDocumentEvidence",
}) as any as S.Schema<VantaDocumentEvidence>;

/** Information about the user who last edited this policy version. Undefined if the user information is not available. */
export type VantaPolicyEvidenceLastEditedBy = VantaDocumentEvidenceAddedBy;
export const VantaPolicyEvidenceLastEditedBy = VantaDocumentEvidenceAddedBy;

/** The uploaded policy file for this version. */
export interface VantaPolicyEvidenceFile {
  /** The date when this policy version became effective, or when it was created. Format: ISO 8601 UTC timestamp. */
  effectiveOrCreationDate: string;
  /** The locale/language of the policy document. Examples: "EN" for English, "ES" for Spanish, "FR" for French */
  locale: string;
  /** A presigned URL for downloading the policy document. This URL is temporary and expires after a limited time. Request a new evidence list to get a fresh URL if expired. */
  url: string;
  /** The MIME type of the policy document indicating its file format. Common types: "application/pdf", "application/msword" */
  mimeType: string;
  /** The original filename of the uploaded policy document. Undefined if the filename was not captured during upload. */
  filename?: string;
  /** The unique identifier for the uploaded policy file in Vanta's system. */
  id: string;
}
export const VantaPolicyEvidenceFile = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    effectiveOrCreationDate: S.String,
    locale: S.String,
    url: S.String,
    mimeType: S.String,
    filename: S.optional(S.String),
    id: S.String,
  }),
).annotate({
  identifier: "VantaPolicyEvidenceFile",
}) as any as S.Schema<VantaPolicyEvidenceFile>;

/** Evidence in the form of a Vanta Policy. Vanta Policies are compliance policies with metadata like locale, effective dates, and versioning. Policies are always uploaded files stored in Vanta's system. */
export interface VantaPolicyEvidence {
  /** The title of the policy as displayed in Vanta. */
  title: string;
  /** Optional description providing additional context about the policy. */
  description?: string;
  /** The date when the policy version expires and should be renewed. Undefined if no expiration is set. Format: ISO 8601 UTC timestamp. */
  expirationDate?: string;
  /** Information about the user who last edited this policy version. Undefined if the user information is not available. */
  lastEditedBy?: VantaDocumentEvidenceAddedBy;
  /** The policy type this evidence belongs to (e.g. "information-security-policy-bsi"). Policies prefixed with "custom-" are customer-defined; others are Vanta-provided templates. */
  policyId: string;
  /** The identifier of the approved policy version that was submitted as evidence. A new version is created each time a policy is updated and approved. */
  policyVersionId: string;
  /** The identifier of the locale-specific file within the policy version. Each policy version may contain multiple files, one per supported language. */
  policyVersionFileId: string;
  /** The uploaded policy file for this version. */
  file: VantaPolicyEvidenceFile;
}
export const VantaPolicyEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    title: S.String,
    description: S.optional(S.String),
    expirationDate: S.optional(S.String),
    lastEditedBy: S.optional(VantaDocumentEvidenceAddedBy),
    policyId: S.String,
    policyVersionId: S.String,
    policyVersionFileId: S.String,
    file: VantaPolicyEvidenceFile,
  }),
).annotate({
  identifier: "VantaPolicyEvidence",
}) as any as S.Schema<VantaPolicyEvidence>;

export type VantaTestRunStatus =
  | "NA"
  | "IN_PROGRESS"
  | "FAIL"
  | "PASS"
  | "INVALID"
  | "DISABLED";
export const VantaTestRunStatus = /*@__PURE__*/ S.String;

export type VantaTestFileType =
  | "TEST_EVIDENCE_WORKPAPER"
  | "TEST_RAW_DATA"
  | "OUT_OF_SCOPE_RESOURCES"
  | "API_REQUESTS";
export const VantaTestFileType = /*@__PURE__*/ S.String;

/** The evidence file for this test run. */
export interface VantaTestEvidenceFile {
  /** The MIME type of the evidence file. Common types: "application/pdf" for workpapers, "text/csv" for raw data */
  mimeType: string;
  /** A presigned URL for downloading the evidence file. This URL is temporary and expires after a limited time. Request a new evidence list to get a fresh URL if expired. */
  url: string;
}
export const VantaTestEvidenceFile = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    mimeType: S.String,
    url: S.String,
  }),
).annotate({
  identifier: "VantaTestEvidenceFile",
}) as any as S.Schema<VantaTestEvidenceFile>;

/** Evidence in the form of a Vanta automated test run. Vanta Tests are automated compliance tests with metadata like test status, execution time, and file type. Tests generate evidence files that can be workpapers, raw data, or API request logs. */
export interface VantaTestEvidence {
  /** The title of the test run as displayed in Vanta. */
  title: string;
  /** The unique identifier for the test in Vanta's system. */
  testId: string;
  /** The unique identifier for the point-in-time capture of this test run's evidence. A snapshot is an immutable record of all files (workpapers, raw data, etc.) generated during a single test execution. */
  snapshotId: string;
  /** The outcome status of the test run. */
  testRunStatus: VantaTestRunStatus;
  /** The date and time when the test was executed. Format: ISO 8601 UTC timestamp. */
  executedAt: string;
  /** The type of evidence file included in this evidence. */
  fileType: VantaTestFileType;
  /** The evidence file for this test run. */
  file: VantaTestEvidenceFile;
}
export const VantaTestEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    title: S.String,
    testId: S.String,
    snapshotId: S.String,
    testRunStatus: VantaTestRunStatus,
    executedAt: S.String,
    fileType: VantaTestFileType,
    file: VantaTestEvidenceFile,
  }),
).annotate({
  identifier: "VantaTestEvidence",
}) as any as S.Schema<VantaTestEvidence>;

/** The outcome status of the test run. */
export type VantaTestSnapshotEvidenceTestRunStatus =
  | "NA"
  | "IN_PROGRESS"
  | "FAIL"
  | "PASS"
  | "INVALID"
  | "DISABLED";
export const VantaTestSnapshotEvidenceTestRunStatus = /*@__PURE__*/ S.String;

/** Evidence in the form of a Vanta automated test run snapshot. Unlike VantaTestEvidence (which represents a single file within a test run's evidence packet), a snapshot represents the entire test-run bundle. Use the dedicated snapshot detail endpoint to retrieve the files associated with a snapshot. */
export interface VantaTestSnapshotEvidence {
  /** The unique identifier for the point-in-time capture of this test run's evidence. A snapshot is an immutable record of all files (workpapers, raw data, etc.) generated during a single test execution. */
  snapshotId: string;
  /** The unique identifier for the test in Vanta's system. */
  testId: string;
  /** The unique identifier for the test run this snapshot was captured from. */
  testRunId: string;
  /** The title of the test run as displayed in Vanta. */
  testRunTitle: string;
  /** The outcome status of the test run. */
  testRunStatus: VantaTestSnapshotEvidenceTestRunStatus;
  /** The date and time when the test was executed. Format: ISO 8601 UTC timestamp. */
  testRunExecutedAt: string;
}
export const VantaTestSnapshotEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotId: S.String,
    testId: S.String,
    testRunId: S.String,
    testRunTitle: S.String,
    testRunStatus: VantaTestSnapshotEvidenceTestRunStatus,
    testRunExecutedAt: S.String,
  }),
).annotate({
  identifier: "VantaTestSnapshotEvidence",
}) as any as S.Schema<VantaTestSnapshotEvidence>;

/** Union type representing the different forms of evidence content. The specific type is determined by the `evidenceType` field in InformationRequestEvidence. */
export type InformationRequestEvidenceUnion =
  | UploadedDocumentEvidence
  | LinkEvidence
  | ObservationEvidence
  | VantaDocumentEvidence
  | VantaPolicyEvidence
  | VantaTestEvidence
  | VantaTestSnapshotEvidence;
export const InformationRequestEvidenceUnion =
  /*@__PURE__*/ S.Unknown as any as S.Schema<InformationRequestEvidenceUnion>;

export interface InformationRequestEvidence {
  /** The unique identifier for the evidence within Vanta's system. Format: ObjectId as a string (e.g., "6890e473dce1da5d8406f5e7"). */
  id: string;
  /** Type of evidence attached to this information request. Determines the structure of the `evidence` field. */
  evidenceType: InformationRequestSupportedEvidenceType;
  /** Timestamp when the evidence was made visible to the auditor. Null if the customer has not yet shared this evidence with the auditor. Evidence must be shared before the auditor can review it. Format: ISO 8601 UTC timestamp. */
  visibleToAuditorDate?: string | null;
  /** Timestamp when the evidence was created/uploaded. Format: ISO 8601 UTC timestamp. */
  creationDate: string;
  /** Timestamp when the evidence was last modified. Format: ISO 8601 UTC timestamp. */
  modificationDate: string;
  /** Timestamp when the evidence was soft-deleted. Null if the evidence has not been deleted. Soft deletes retain evidence in the system for audit history while hiding it from normal operations. Format: ISO 8601 UTC timestamp. */
  deletionDate?: string | null;
  /** The evidence content. Structure varies based on evidenceType. */
  evidence: InformationRequestEvidenceUnion;
}
export const InformationRequestEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    evidenceType: InformationRequestSupportedEvidenceType,
    visibleToAuditorDate: S.optional(S.NullOr(S.String)),
    creationDate: S.String,
    modificationDate: S.String,
    deletionDate: S.optional(S.NullOr(S.String)),
    evidence: InformationRequestEvidenceUnion,
  }),
).annotate({
  identifier: "InformationRequestEvidence",
}) as any as S.Schema<InformationRequestEvidence>;

export interface GetInformationRequestTestSnapshotEvidenceDetailRequest {
  auditId: string;
  requestId: string;
  evidenceId: string;
}
export const GetInformationRequestTestSnapshotEvidenceDetailRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      requestId: S.String.pipe(T.Label()),
      evidenceId: S.String.pipe(T.Label()),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/audits/{auditId}/information-requests/{requestId}/evidence/{evidenceId}/test-snapshot",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "GetInformationRequestTestSnapshotEvidenceDetailRequest",
  }) as any as S.Schema<GetInformationRequestTestSnapshotEvidenceDetailRequest>;

/** Integration metadata for a test, mirroring the Vanta web app. */
export interface VantaTestSnapshotIntegration {
  integrationId: string;
  displayName: string;
}
export const VantaTestSnapshotIntegration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    integrationId: S.String,
    displayName: S.String,
  }),
).annotate({
  identifier: "VantaTestSnapshotIntegration",
}) as any as S.Schema<VantaTestSnapshotIntegration>;

/** Integrations whose data feeds the test. */
export type VantaTestSnapshotEvidenceDetailIntegrationsList =
  Array<VantaTestSnapshotIntegration>;
export const VantaTestSnapshotEvidenceDetailIntegrationsList =
  /*@__PURE__*/ S.Array(
    VantaTestSnapshotIntegration,
  ) as any as S.Schema<VantaTestSnapshotEvidenceDetailIntegrationsList>;

export interface VantaTestSnapshotSlaRemediationSettings {
  numericValue: number;
  unit: string | null;
}
export const VantaTestSnapshotSlaRemediationSettings = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      numericValue: S.Number,
      unit: S.NullOr(S.String),
    }),
).annotate({
  identifier: "VantaTestSnapshotSlaRemediationSettings",
}) as any as S.Schema<VantaTestSnapshotSlaRemediationSettings>;

export interface VantaTestSnapshotSlaRemediation {
  id: string;
  settings: VantaTestSnapshotSlaRemediationSettings | null;
}
export const VantaTestSnapshotSlaRemediation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    settings: S.NullOr(VantaTestSnapshotSlaRemediationSettings),
  }),
).annotate({
  identifier: "VantaTestSnapshotSlaRemediation",
}) as any as S.Schema<VantaTestSnapshotSlaRemediation>;

/** One row of raw test data attached to a snapshot. For structured snapshots, every row carries the resource refs and the raw JSON for that resource. For unstructured snapshots, a single row is returned with `resourceId` and `resourceType` set to `null` and `rawJson` containing the entire test-run JSON blob. */
export interface VantaTestSnapshotRawTestDataRow {
  /** Identifier of the underlying resource the row was captured from. Null for unstructured snapshots, which do not partition by resource. */
  resourceId: string | null;
  /** Specific resource kind for the row (e.g. `AwsIamUser`). Null for unstructured snapshots. */
  resourceType: string | null;
  /** Raw JSON payload as emitted by the test runner. Shape varies by `resourceType` for structured snapshots and matches the full test-run blob for unstructured snapshots. */
  rawJson: unknown;
}
export const VantaTestSnapshotRawTestDataRow = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    resourceId: S.NullOr(S.String),
    resourceType: S.NullOr(S.String),
    rawJson: S.Unknown,
  }),
).annotate({
  identifier: "VantaTestSnapshotRawTestDataRow",
}) as any as S.Schema<VantaTestSnapshotRawTestDataRow>;

/** Raw test data captured at snapshot time. */
export type VantaTestSnapshotEvidenceDetailRawTestDataList =
  Array<VantaTestSnapshotRawTestDataRow>;
export const VantaTestSnapshotEvidenceDetailRawTestDataList =
  /*@__PURE__*/ S.Array(
    VantaTestSnapshotRawTestDataRow,
  ) as any as S.Schema<VantaTestSnapshotEvidenceDetailRawTestDataList>;

export interface VantaTestSnapshotExcludedItem {
  id: string;
  displayName: string | null;
}
export const VantaTestSnapshotExcludedItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    displayName: S.NullOr(S.String),
  }),
).annotate({
  identifier: "VantaTestSnapshotExcludedItem",
}) as any as S.Schema<VantaTestSnapshotExcludedItem>;

export type VantaTestSnapshotExcludedItemGroupEntitiesList =
  Array<VantaTestSnapshotExcludedItem>;
export const VantaTestSnapshotExcludedItemGroupEntitiesList =
  /*@__PURE__*/ S.Array(
    VantaTestSnapshotExcludedItem,
  ) as any as S.Schema<VantaTestSnapshotExcludedItemGroupEntitiesList>;

export interface VantaTestSnapshotExcludedItemGroup {
  reason: string;
  entities: VantaTestSnapshotExcludedItemGroupEntitiesList;
}
export const VantaTestSnapshotExcludedItemGroup = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    reason: S.String,
    entities: VantaTestSnapshotExcludedItemGroupEntitiesList,
  }),
).annotate({
  identifier: "VantaTestSnapshotExcludedItemGroup",
}) as any as S.Schema<VantaTestSnapshotExcludedItemGroup>;

export type VantaTestSnapshotOutOfScopeResourcesTestLevelList =
  Array<VantaTestSnapshotExcludedItemGroup>;
export const VantaTestSnapshotOutOfScopeResourcesTestLevelList =
  /*@__PURE__*/ S.Array(
    VantaTestSnapshotExcludedItemGroup,
  ) as any as S.Schema<VantaTestSnapshotOutOfScopeResourcesTestLevelList>;

export type VantaTestSnapshotFrameworkExclusionGroupedByReasonList =
  Array<VantaTestSnapshotExcludedItemGroup>;
export const VantaTestSnapshotFrameworkExclusionGroupedByReasonList =
  /*@__PURE__*/ S.Array(
    VantaTestSnapshotExcludedItemGroup,
  ) as any as S.Schema<VantaTestSnapshotFrameworkExclusionGroupedByReasonList>;

export interface VantaTestSnapshotFrameworkExclusion {
  frameworkId: string;
  frameworkName: string;
  groupedByReason: VantaTestSnapshotFrameworkExclusionGroupedByReasonList;
}
export const VantaTestSnapshotFrameworkExclusion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    frameworkId: S.String,
    frameworkName: S.String,
    groupedByReason: VantaTestSnapshotFrameworkExclusionGroupedByReasonList,
  }),
).annotate({
  identifier: "VantaTestSnapshotFrameworkExclusion",
}) as any as S.Schema<VantaTestSnapshotFrameworkExclusion>;

export type VantaTestSnapshotOutOfScopeResourcesFrameworkLevelList =
  Array<VantaTestSnapshotFrameworkExclusion>;
export const VantaTestSnapshotOutOfScopeResourcesFrameworkLevelList =
  /*@__PURE__*/ S.Array(
    VantaTestSnapshotFrameworkExclusion,
  ) as any as S.Schema<VantaTestSnapshotOutOfScopeResourcesFrameworkLevelList>;

/** Resources excluded from the test at snapshot time, grouped by exclusion reason. `testLevel` contains resources the customer disabled for the test; `frameworkLevel` contains resources scoped out by the framework's segment configuration. */
export interface VantaTestSnapshotOutOfScopeResources {
  testLevel: VantaTestSnapshotOutOfScopeResourcesTestLevelList;
  frameworkLevel: VantaTestSnapshotOutOfScopeResourcesFrameworkLevelList;
}
export const VantaTestSnapshotOutOfScopeResources = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      testLevel: VantaTestSnapshotOutOfScopeResourcesTestLevelList,
      frameworkLevel: VantaTestSnapshotOutOfScopeResourcesFrameworkLevelList,
    }),
).annotate({
  identifier: "VantaTestSnapshotOutOfScopeResources",
}) as any as S.Schema<VantaTestSnapshotOutOfScopeResources>;

/** A single API request captured during an API introspection test run. */
export interface VantaTestSnapshotApiRequest {
  id: string;
  resourceType: string;
  resourceTypeDisplayName: string;
  requestMethod: string;
  url: string;
  responseCode: number;
  timestamp: string;
  dataTooLarge: boolean;
}
export const VantaTestSnapshotApiRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    resourceType: S.String,
    resourceTypeDisplayName: S.String,
    requestMethod: S.String,
    url: S.String,
    responseCode: S.Number,
    timestamp: S.String,
    dataTooLarge: S.Boolean,
  }),
).annotate({
  identifier: "VantaTestSnapshotApiRequest",
}) as any as S.Schema<VantaTestSnapshotApiRequest>;

/** API requests captured during the test run. Absent when the snapshot data could not be loaded. Empty array when the test does not perform API introspection. */
export type VantaTestSnapshotEvidenceDetailApiRequestsList =
  Array<VantaTestSnapshotApiRequest>;
export const VantaTestSnapshotEvidenceDetailApiRequestsList =
  /*@__PURE__*/ S.Array(
    VantaTestSnapshotApiRequest,
  ) as any as S.Schema<VantaTestSnapshotEvidenceDetailApiRequestsList>;

/** Detail response for a captured automated-test snapshot evidence row. Combines the test-level metadata (description, integrations, SLA remediation) with a uniform raw-data array so external clients see the same shape regardless of whether the snapshot was structured or unstructured. */
export interface VantaTestSnapshotEvidenceDetail {
  /** The unique identifier for the test in Vanta's system. */
  testId: string;
  /** The display name of the test. */
  testName: string;
  /** A short description of what the test checks. */
  description: string;
  /** Longer-form, formatted description of the test. Null when unset. */
  evaluationCriteria: string | null;
  /** Integrations whose data feeds the test. */
  integrations: VantaTestSnapshotEvidenceDetailIntegrationsList;
  /** SLA remediation policy attached to the test, or null when no SLA is set. */
  slaRemediation: VantaTestSnapshotSlaRemediation | null;
  /** Raw test data captured at snapshot time. */
  rawTestData: VantaTestSnapshotEvidenceDetailRawTestDataList;
  /** Resources excluded from the test at snapshot time. Absent when the snapshot data could not be loaded; clients should treat a missing field as "unknown" rather than "nothing excluded". */
  outOfScopeResources?: VantaTestSnapshotOutOfScopeResources;
  /** API requests captured during the test run. Absent when the snapshot data could not be loaded. Empty array when the test does not perform API introspection. */
  apiRequests?: VantaTestSnapshotEvidenceDetailApiRequestsList;
}
export const VantaTestSnapshotEvidenceDetail = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    testId: S.String,
    testName: S.String,
    description: S.String,
    evaluationCriteria: S.NullOr(S.String),
    integrations: VantaTestSnapshotEvidenceDetailIntegrationsList,
    slaRemediation: S.NullOr(VantaTestSnapshotSlaRemediation),
    rawTestData: VantaTestSnapshotEvidenceDetailRawTestDataList,
    outOfScopeResources: S.optional(VantaTestSnapshotOutOfScopeResources),
    apiRequests: S.optional(VantaTestSnapshotEvidenceDetailApiRequestsList),
  }),
).annotate({
  identifier: "VantaTestSnapshotEvidenceDetail",
}) as any as S.Schema<VantaTestSnapshotEvidenceDetail>;

export interface GetOrganizationInformationRequest {
  /** The audit ID */
  auditId: string;
}
export const GetOrganizationInformationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/organization/information",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetOrganizationInformationRequest",
}) as any as S.Schema<GetOrganizationInformationRequest>;

/** Company logo reference for an organization. */
export interface AuditOrganizationCompanyLogo {
  /** URL to download the company logo. */
  url: string;
}
export const AuditOrganizationCompanyLogo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    url: S.String,
  }),
).annotate({
  identifier: "AuditOrganizationCompanyLogo",
}) as any as S.Schema<AuditOrganizationCompanyLogo>;

/** Organization information record returned by the auditor organization API. `id` and `displayName` are always present (primary fields). Other fields are optional to support controlled audits where only approved columns are returned. */
export interface AuditOrganizationInformation {
  /** Business info document identifier. */
  id: string;
  /** Display name of the organization. */
  displayName: string;
  /** Legal name of the organization. */
  legalName?: string | null;
  /** Incorporation status. Returns "Incorporated in the United States" when the organization is a US company, or null otherwise. */
  incorporation?: string | null;
  /** Organization URL. */
  url?: string | null;
  /** Mailing address of the organization. */
  mailingAddress?: string | null;
  /** Telephone number of the organization. */
  telephone?: string | null;
  /** Company logo reference (download URL), or null if no logo is uploaded. */
  companyLogo?: AuditOrganizationCompanyLogo | null;
}
export const AuditOrganizationInformation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    displayName: S.String,
    legalName: S.optional(S.NullOr(S.String)),
    incorporation: S.optional(S.NullOr(S.String)),
    url: S.optional(S.NullOr(S.String)),
    mailingAddress: S.optional(S.NullOr(S.String)),
    telephone: S.optional(S.NullOr(S.String)),
    companyLogo: S.optional(S.NullOr(AuditOrganizationCompanyLogo)),
  }),
).annotate({
  identifier: "AuditOrganizationInformation",
}) as any as S.Schema<AuditOrganizationInformation>;

export interface GetOrganizationNotificationsRequest {
  /** The audit ID */
  auditId: string;
}
export const GetOrganizationNotificationsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/organization/notifications",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetOrganizationNotificationsRequest",
}) as any as S.Schema<GetOrganizationNotificationsRequest>;

/** Channels on which the personnel reminder digest is delivered (e.g. `["EMAIL"]` or `["EMAIL", "SLACK"]`). */
export type AuditOrganizationNotificationsSubscribedChannelTypesList =
  Array<string>;
export const AuditOrganizationNotificationsSubscribedChannelTypesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<AuditOrganizationNotificationsSubscribedChannelTypesList>;

/** A single external-notification subscription row returned by the auditor organization notifications API. Represents one mailing-list address + notification-category pairing. */
export interface AuditOrganizationNotificationSubscription {
  /** Subscription record identifier. */
  id: string;
  /** Email address that receives notifications for this category. */
  address: string;
  /** Cadence of summary notifications for this subscription, or null for non-summary (immediate / reminder) subscriptions. */
  cadence: string | null;
  /** Notification category the subscription belongs to (e.g. `SUMMARY_EMPLOYEE`, `NEW_VENDORS_DISCOVERED_EMAIL`, `TRUST_REPORT_ACCESS_REQUEST_EMAIL`). */
  unsubSettingsKey: string;
  /** Whether the address is currently subscribed to receive this category's notifications. */
  toggleIsSubscribed: boolean;
}
export const AuditOrganizationNotificationSubscription =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      id: S.String,
      address: S.String,
      cadence: S.NullOr(S.String),
      unsubSettingsKey: S.String,
      toggleIsSubscribed: S.Boolean,
    }),
  ).annotate({
    identifier: "AuditOrganizationNotificationSubscription",
  }) as any as S.Schema<AuditOrganizationNotificationSubscription>;

/** External notification subscriptions — one row per mailing-list address plus category pairing. Covers Compliance, Vendors, Access Reviews, and Trust Center categories. */
export type AuditOrganizationNotificationsExternalNotificationsList =
  Array<AuditOrganizationNotificationSubscription>;
export const AuditOrganizationNotificationsExternalNotificationsList =
  /*@__PURE__*/ S.Array(
    AuditOrganizationNotificationSubscription,
  ) as any as S.Schema<AuditOrganizationNotificationsExternalNotificationsList>;

/** Organization notifications record returned by the auditor API. Ad-hoc single-row payload per domain mirroring the shape of the Organization → Notifications page. Only `id` is guaranteed; every other field is optional because controlled audit views can strip any column from the response. */
export interface AuditOrganizationNotifications {
  /** Domain identifier of the audited organization. */
  id: string;
  /** IANA timezone identifier that determines when scheduled reminder notifications are sent. `null` means the organization has no configured preference ("Anytime" in the UI). */
  preferredTimeZone?: string | null;
  /** Frequency at which personnel reminder digests are sent (e.g. `DAILY`, `WEEKLY`, or `NEVER`). `null` when no notification settings have been configured for the organization. */
  employeeDigestFrequency?: string | null;
  /** Channels on which the personnel reminder digest is delivered (e.g. `["EMAIL"]` or `["EMAIL", "SLACK"]`). */
  subscribedChannelTypes?: AuditOrganizationNotificationsSubscribedChannelTypesList;
  /** External notification subscriptions — one row per mailing-list address plus category pairing. Covers Compliance, Vendors, Access Reviews, and Trust Center categories. */
  externalNotifications?: AuditOrganizationNotificationsExternalNotificationsList;
}
export const AuditOrganizationNotifications = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    preferredTimeZone: S.optional(S.NullOr(S.String)),
    employeeDigestFrequency: S.optional(S.NullOr(S.String)),
    subscribedChannelTypes: S.optional(
      AuditOrganizationNotificationsSubscribedChannelTypesList,
    ),
    externalNotifications: S.optional(
      AuditOrganizationNotificationsExternalNotificationsList,
    ),
  }),
).annotate({
  identifier: "AuditOrganizationNotifications",
}) as any as S.Schema<AuditOrganizationNotifications>;

export interface ListAccountAccessServicesRequest {
  /** The audit ID */
  auditId: string;
  /** Maximum number of results per page (1-100, default 10) */
  pageSize?: number;
  /** Pagination cursor from previous response */
  pageCursor?: string;
}
export const ListAccountAccessServicesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/personnel/account-access/services",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListAccountAccessServicesRequest",
}) as any as S.Schema<ListAccountAccessServicesRequest>;

/** An account access service (identity provider, access credential, or external application) connected to the organization. These represent credentials for integrations like Okta, Azure AD, Google Workspace, etc. that provide identity and access management data for personnel, as well as external applications that provide received user accounts. */
export interface AccountAccessService {
  /** Unique identifier for the service. */
  id: string;
  /** Display name of the service shown to users. */
  credentialDisplayName: string;
  /** The service name or integration ID. */
  service: string;
  /** Sub-account identifier for multi-account integrations, or null if not applicable. */
  subAccountId: string | null;
}
export const AccountAccessService = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    credentialDisplayName: S.String,
    service: S.String,
    subAccountId: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AccountAccessService",
}) as any as S.Schema<AccountAccessService>;

export type PaginatedResponseAccountAccessServiceResultsDataList =
  Array<AccountAccessService>;
export const PaginatedResponseAccountAccessServiceResultsDataList =
  /*@__PURE__*/ S.Array(
    AccountAccessService,
  ) as any as S.Schema<PaginatedResponseAccountAccessServiceResultsDataList>;

/** Provides information about the pagination of a dataset. */
export interface PageInfo {
  /** The cursor that points to the end of the current page, or null if there is no such cursor. */
  endCursor: string | null;
  /** Indicates if there is another page after the current page. */
  hasNextPage: boolean;
  /** Indicates if there is a page before the current page. */
  hasPreviousPage: boolean;
  /** The cursor that points to the start of the current page, or null if there is no such cursor. */
  startCursor: string | null;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    endCursor: S.NullOr(S.String),
    hasNextPage: S.Boolean,
    hasPreviousPage: S.Boolean,
    startCursor: S.NullOr(S.String),
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface PaginatedResponseAccountAccessServiceResults {
  data: PaginatedResponseAccountAccessServiceResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseAccountAccessServiceResults =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      data: PaginatedResponseAccountAccessServiceResultsDataList,
      pageInfo: PageInfo,
    }),
  ).annotate({
    identifier: "PaginatedResponseAccountAccessServiceResults",
  }) as any as S.Schema<PaginatedResponseAccountAccessServiceResults>;

export interface PaginatedResponseAccountAccessService {
  results: PaginatedResponseAccountAccessServiceResults;
}
export const PaginatedResponseAccountAccessService = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      results: PaginatedResponseAccountAccessServiceResults,
    }),
).annotate({
  identifier: "PaginatedResponseAccountAccessService",
}) as any as S.Schema<PaginatedResponseAccountAccessService>;

export interface ListAuditCommentsRequest {
  auditId: string;
  pageSize?: number;
  pageCursor?: string;
  /** Includes all comments that have changed since changedSinceDate. */
  changedSinceDate?: string;
}
export const ListAuditCommentsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    changedSinceDate: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/audits/{auditId}/comments", code: 200 }),
  ),
).annotate({
  identifier: "ListAuditCommentsRequest",
}) as any as S.Schema<ListAuditCommentsRequest>;

export type PaginatedResponseCommentResultsDataList = Array<Comment>;
export const PaginatedResponseCommentResultsDataList = /*@__PURE__*/ S.Array(
  Comment,
) as any as S.Schema<PaginatedResponseCommentResultsDataList>;

export interface PaginatedResponseCommentResults {
  data: PaginatedResponseCommentResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseCommentResults = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: PaginatedResponseCommentResultsDataList,
    pageInfo: PageInfo,
  }),
).annotate({
  identifier: "PaginatedResponseCommentResults",
}) as any as S.Schema<PaginatedResponseCommentResults>;

export interface PaginatedResponseComment {
  results: PaginatedResponseCommentResults;
}
export const PaginatedResponseComment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseCommentResults,
  }),
).annotate({
  identifier: "PaginatedResponseComment",
}) as any as S.Schema<PaginatedResponseComment>;

export type ListAuditControlsRequestExternalIdMatchesAnyList = Array<string>;
export const ListAuditControlsRequestExternalIdMatchesAnyList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ListAuditControlsRequestExternalIdMatchesAnyList>;

export interface ListAuditControlsRequest {
  auditId: string;
  pageSize?: number;
  pageCursor?: string;
  /** Filter controls whose externalId matches any of the provided values (exact, case-sensitive match). */
  externalIdMatchesAny?: ListAuditControlsRequestExternalIdMatchesAnyList;
}
export const ListAuditControlsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    externalIdMatchesAny: S.optional(
      ListAuditControlsRequestExternalIdMatchesAnyList.pipe(T.Query()),
    ),
  }).pipe(
    T.Http({ method: "GET", uri: "/audits/{auditId}/controls", code: 200 }),
  ),
).annotate({
  identifier: "ListAuditControlsRequest",
}) as any as S.Schema<ListAuditControlsRequest>;

/** The security domains that the control belongs to. */
export type AuditorControlDomainsList = Array<string>;
export const AuditorControlDomainsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AuditorControlDomainsList>;

/** The control's custom field values, if control custom fields is included in your Vanta instance. */
export type AuditorControlCustomFieldsList = Array<CustomField>;
export const AuditorControlCustomFieldsList = /*@__PURE__*/ S.Array(
  CustomField,
) as any as S.Schema<AuditorControlCustomFieldsList>;

export interface SectionPrinciple {
  /** The principle's unique ID. */
  id: string;
  /** The principle's name. */
  name: string;
}
export const SectionPrinciple = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
  }),
).annotate({
  identifier: "SectionPrinciple",
}) as any as S.Schema<SectionPrinciple>;

export interface Section {
  /** The section name */
  name: string;
  /** The section framework */
  framework: string;
  /** The principle that groups this section, if any. */
  principle: SectionPrinciple | null;
}
export const Section = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    framework: S.String,
    principle: S.NullOr(SectionPrinciple),
  }),
).annotate({ identifier: "Section" }) as any as S.Schema<Section>;

/** Sections of a framework that this control satisfies */
export type AuditorControlSectionsList = Array<Section>;
export const AuditorControlSectionsList = /*@__PURE__*/ S.Array(
  Section,
) as any as S.Schema<AuditorControlSectionsList>;

/** Audit segments this control is in scope for. Empty when the control is attached only by a direct link and has no catalog mapping on a multi-program audit. */
export type AuditorControlInScopeSegmentIdsList = Array<string>;
export const AuditorControlInScopeSegmentIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AuditorControlInScopeSegmentIdsList>;

/** An auditor's assessment of a control within an audit. This is the full flat union of every framework's assessment states (the superset); a given audit's framework only uses its own subset. `NOT_ASSESSED` is shared by all frameworks and is the default for a control that has not yet been assessed. Which states apply to which framework: - Most frameworks (e.g. SOC 2): `IN_PLACE`, `NOT_IN_PLACE`, `PARTIAL`, `NOT_ASSESSED` - ISO 27001: `CONFORMING`, `MINOR_NON_CONFORMITY`, `MAJOR_NON_CONFORMITY`, `NOT_ASSESSED` - FedRAMP: `SATISFIED`, `NOT_SATISFIED`, `OTHER_THAN_SATISFIED`, `NOT_ASSESSED` - FedRAMP Key Security Indicators (KSI): `TRUE`, `FALSE`, `PARTIAL`, `NOT_ASSESSED` Distinct from `ControlStatus`, which is the computed tests/documents-passing status — a different concept. */
export type AuditControlAssessmentState =
  | "IN_PLACE"
  | "NOT_IN_PLACE"
  | "PARTIAL"
  | "NOT_ASSESSED"
  | "CONFORMING"
  | "MINOR_NON_CONFORMITY"
  | "MAJOR_NON_CONFORMITY"
  | "SATISFIED"
  | "NOT_SATISFIED"
  | "OTHER_THAN_SATISFIED"
  | "TRUE"
  | "FALSE";
export const AuditControlAssessmentState = /*@__PURE__*/ S.String;

/** An auditor's assessment of a control within one segment of an audit. Assessments are keyed per segment, so a control in scope for more than one segment (a multi-framework audit) can carry a distinct assessment per segment. A segment with no recorded assessment coerces to `NOT_ASSESSED` with a `null` justification. */
export interface AuditControlAssessment {
  /** The audit program segment this assessment belongs to. */
  segmentId: string;
  /** The auditor's assessment of the control for this segment; `NOT_ASSESSED` when no assessment has been recorded. */
  assessmentState: AuditControlAssessmentState;
  /** Free-text reasoning for the assessment; `null` when none has been recorded. */
  justification: string | null;
}
export const AuditControlAssessment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    segmentId: S.String,
    assessmentState: AuditControlAssessmentState,
    justification: S.NullOr(S.String),
  }),
).annotate({
  identifier: "AuditControlAssessment",
}) as any as S.Schema<AuditControlAssessment>;

/** The auditor's assessments of this control, one per audit program segment the control is in scope for (a multi-framework audit can have more than one). Populated only for IRL audits when the assessment feature is enabled; empty otherwise. Within an in-scope segment a control with no recorded assessment coerces to `NOT_ASSESSED`; a segment the control is not in scope for contributes no entry. */
export type AuditorControlAssessmentsList = Array<AuditControlAssessment>;
export const AuditorControlAssessmentsList = /*@__PURE__*/ S.Array(
  AuditControlAssessment,
) as any as S.Schema<AuditorControlAssessmentsList>;

export interface AuditorControl {
  /** The control's unique ID. */
  id: string;
  /** The control's external ID. */
  externalId: string | null;
  /** The control's name. */
  name: string;
  /** The control's description. */
  description: string;
  /** The control's source, either "VANTA" or "CUSTOM". */
  source: ControlSource;
  /** The security domains that the control belongs to. */
  domains: AuditorControlDomainsList;
  /** The control's owner. */
  owner: Owner | null;
  /** The control's GDPR role, if the control is a GDPR control. */
  role?: string | null;
  /** The control's custom field values, if control custom fields is included in your Vanta instance. */
  customFields: AuditorControlCustomFieldsList;
  /** When the control was created. Returns null for Vanta library controls. */
  creationDate: string | null;
  /** When the control was last modified. Returns null for Vanta library controls. */
  modificationDate: string | null;
  /** The report standard framework fulfilled by the control. Incomplete once a control is in scope for more than one framework. */
  framework: string;
  /** Sections of a framework that this control satisfies */
  sections: AuditorControlSectionsList;
  /** Audit segments this control is in scope for. Empty when the control is attached only by a direct link and has no catalog mapping on a multi-program audit. */
  inScopeSegmentIds: AuditorControlInScopeSegmentIdsList;
  /** The auditor's assessments of this control, one per audit program segment the control is in scope for (a multi-framework audit can have more than one). Populated only for IRL audits when the assessment feature is enabled; empty otherwise. Within an in-scope segment a control with no recorded assessment coerces to `NOT_ASSESSED`; a segment the control is not in scope for contributes no entry. */
  assessments: AuditorControlAssessmentsList;
}
export const AuditorControl = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    externalId: S.NullOr(S.String),
    name: S.String,
    description: S.String,
    source: ControlSource,
    domains: AuditorControlDomainsList,
    owner: S.NullOr(Owner),
    role: S.optional(S.NullOr(S.String)),
    customFields: AuditorControlCustomFieldsList,
    creationDate: S.NullOr(S.String),
    modificationDate: S.NullOr(S.String),
    framework: S.String,
    sections: AuditorControlSectionsList,
    inScopeSegmentIds: AuditorControlInScopeSegmentIdsList,
    assessments: AuditorControlAssessmentsList,
  }),
).annotate({ identifier: "AuditorControl" }) as any as S.Schema<AuditorControl>;

export type PaginatedResponseAuditorControlResultsDataList =
  Array<AuditorControl>;
export const PaginatedResponseAuditorControlResultsDataList =
  /*@__PURE__*/ S.Array(
    AuditorControl,
  ) as any as S.Schema<PaginatedResponseAuditorControlResultsDataList>;

export interface PaginatedResponseAuditorControlResults {
  data: PaginatedResponseAuditorControlResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseAuditorControlResults = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      data: PaginatedResponseAuditorControlResultsDataList,
      pageInfo: PageInfo,
    }),
).annotate({
  identifier: "PaginatedResponseAuditorControlResults",
}) as any as S.Schema<PaginatedResponseAuditorControlResults>;

export interface PaginatedResponseAuditorControl {
  results: PaginatedResponseAuditorControlResults;
}
export const PaginatedResponseAuditorControl = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseAuditorControlResults,
  }),
).annotate({
  identifier: "PaginatedResponseAuditorControl",
}) as any as S.Schema<PaginatedResponseAuditorControl>;

export interface ListAuditEvidenceRequest {
  auditId: string;
  pageSize?: number;
  pageCursor?: string;
  /** Includes all audit evidence that have changed since changedSinceDate. */
  changedSinceDate?: string;
}
export const ListAuditEvidenceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    changedSinceDate: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/audits/{auditId}/evidence", code: 200 }),
  ),
).annotate({
  identifier: "ListAuditEvidenceRequest",
}) as any as S.Schema<ListAuditEvidenceRequest>;

export type PaginatedResponseEvidenceResultsDataList = Array<Evidence>;
export const PaginatedResponseEvidenceResultsDataList = /*@__PURE__*/ S.Array(
  Evidence,
) as any as S.Schema<PaginatedResponseEvidenceResultsDataList>;

export interface PaginatedResponseEvidenceResults {
  data: PaginatedResponseEvidenceResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseEvidenceResults = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: PaginatedResponseEvidenceResultsDataList,
    pageInfo: PageInfo,
  }),
).annotate({
  identifier: "PaginatedResponseEvidenceResults",
}) as any as S.Schema<PaginatedResponseEvidenceResults>;

export interface PaginatedResponseEvidence {
  results: PaginatedResponseEvidenceResults;
}
export const PaginatedResponseEvidence = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseEvidenceResults,
  }),
).annotate({
  identifier: "PaginatedResponseEvidence",
}) as any as S.Schema<PaginatedResponseEvidence>;

export interface ListAuditEvidenceUrlsRequest {
  auditId: string;
  auditEvidenceId: string;
  pageSize?: number;
  pageCursor?: string;
}
export const ListAuditEvidenceUrlsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    auditEvidenceId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/evidence/{auditEvidenceId}/urls",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListAuditEvidenceUrlsRequest",
}) as any as S.Schema<ListAuditEvidenceUrlsRequest>;

export interface EvidenceUrl {
  /** Vanta internal reference to evidence */
  id: string;
  /** Pre-signed S3 URL for evidence */
  url: string;
  /** File name of evidence */
  filename: string;
  /** Set to true if this is a presigned s3 url. Set to false if this is a customer uploaded link */
  isDownloadable: boolean;
  /** MIME type of the evidence file (e.g., "application/pdf", "image/png") */
  mimeType: string | null;
}
export const EvidenceUrl = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    url: S.String,
    filename: S.String,
    isDownloadable: S.Boolean,
    mimeType: S.NullOr(S.String),
  }),
).annotate({ identifier: "EvidenceUrl" }) as any as S.Schema<EvidenceUrl>;

export type PaginatedResponseEvidenceUrlResultsDataList = Array<EvidenceUrl>;
export const PaginatedResponseEvidenceUrlResultsDataList =
  /*@__PURE__*/ S.Array(
    EvidenceUrl,
  ) as any as S.Schema<PaginatedResponseEvidenceUrlResultsDataList>;

export interface PaginatedResponseEvidenceUrlResults {
  data: PaginatedResponseEvidenceUrlResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseEvidenceUrlResults = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: PaginatedResponseEvidenceUrlResultsDataList,
    pageInfo: PageInfo,
  }),
).annotate({
  identifier: "PaginatedResponseEvidenceUrlResults",
}) as any as S.Schema<PaginatedResponseEvidenceUrlResults>;

export interface PaginatedResponseEvidenceUrl {
  results: PaginatedResponseEvidenceUrlResults;
}
export const PaginatedResponseEvidenceUrl = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseEvidenceUrlResults,
  }),
).annotate({
  identifier: "PaginatedResponseEvidenceUrl",
}) as any as S.Schema<PaginatedResponseEvidenceUrl>;

export type ListAuditIssuesRequestSnapshotIdMatchesAnyList = Array<string>;
export const ListAuditIssuesRequestSnapshotIdMatchesAnyList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ListAuditIssuesRequestSnapshotIdMatchesAnyList>;

/** Allowed values for the `orderBy` query parameter on issue snapshot item endpoints. */
export type IssueSnapshotItemOrderBy =
  | "createdAt"
  | "lastModifiedAt"
  | "detectedAt";
export const IssueSnapshotItemOrderBy = /*@__PURE__*/ S.String;

/** `"asc"` for ascending, `"desc"` for descending. */
export type OrderDirection = "asc" | "desc";
export const OrderDirection = /*@__PURE__*/ S.String;

export interface ListAuditIssuesRequest {
  /** The audit ID */
  auditId: string;
  /** Maximum number of results per page (1-100, default 10) */
  pageSize?: number;
  /** Pagination cursor from previous response */
  pageCursor?: string;
  /** Search term for filtering by issue title and description */
  search?: string;
  /** Filter issues to specific snapshots by snapshot ID */
  snapshotIdMatchesAny?: ListAuditIssuesRequestSnapshotIdMatchesAnyList;
  /** Filter to issues created on or after this date (ISO 8601) */
  createdAfterDate?: string;
  /** Filter to issues created on or before this date (ISO 8601) */
  createdBeforeDate?: string;
  /** Filter to issues detected on or after this date (ISO 8601) */
  detectedAfterDate?: string;
  /** Filter to issues detected on or before this date (ISO 8601) */
  detectedBeforeDate?: string;
  /** Field to sort results by. Allowed: "createdAt", "lastModifiedAt", "detectedAt". Default: "createdAt" */
  orderBy?: IssueSnapshotItemOrderBy | (string & {});
  /** Sort direction: "asc" or "desc". Default: "desc" */
  orderDirection?: OrderDirection | (string & {});
}
export const ListAuditIssuesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
    snapshotIdMatchesAny: S.optional(
      ListAuditIssuesRequestSnapshotIdMatchesAnyList.pipe(T.Query()),
    ),
    createdAfterDate: S.optional(S.String.pipe(T.Query())),
    createdBeforeDate: S.optional(S.String.pipe(T.Query())),
    detectedAfterDate: S.optional(S.String.pipe(T.Query())),
    detectedBeforeDate: S.optional(S.String.pipe(T.Query())),
    orderBy: S.optional(IssueSnapshotItemOrderBy.pipe(T.Query())),
    orderDirection: S.optional(OrderDirection.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/audits/{auditId}/issues/items", code: 200 }),
  ),
).annotate({
  identifier: "ListAuditIssuesRequest",
}) as any as S.Schema<ListAuditIssuesRequest>;

export type IssueStatusTypeForSnapshot =
  | "IN_PROGRESS"
  | "NOT_STARTED"
  | "PENDING_REVIEW"
  | "CLOSED";
export const IssueStatusTypeForSnapshot = /*@__PURE__*/ S.String;

/** Approval progress for an issue at the time the snapshot was captured. */
export interface IssueSnapshotItemApprovalProgress {
  /** The number of approvers who have approved the issue. */
  approvedCount: number;
  /** The total number of approvers in the issue's approval chain. */
  totalApprovers: number;
}
export const IssueSnapshotItemApprovalProgress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    approvedCount: S.Number,
    totalApprovers: S.Number,
  }),
).annotate({
  identifier: "IssueSnapshotItemApprovalProgress",
}) as any as S.Schema<IssueSnapshotItemApprovalProgress>;

export interface IssueSnapshotItem {
  /** The unique identifier for the snapshot of an issue. This is distinct from the issueId, which identifies the underlying issue across snapshots. The snapshot item ID is unique to this specific snapshot of the issue. */
  id: string;
  /** The issue title */
  title?: string;
  /** The issue description */
  description?: string;
  /** The human-readable identifier for the issue. This field is unique per-domain, and can be used to correlate this issue snapshot item to the underlying issue across different snapshots. */
  readableIssueId: string;
  /** The globally unique identifier for the underlying issue across snapshots. This field can be used to correlate this snapshot item to the underlying issue across different snapshots. */
  issueId: string;
  /** The identifier for the snapshot this issue item belongs to. */
  snapshotId: string;
  /** The due date for the issue. */
  dueDate?: string | null;
  /** The status of the issue. */
  status?: IssueStatusTypeForSnapshot;
  /** The issue's approval progress at the time the snapshot was captured, or null if no approval was in progress or issue approvals are not enabled. */
  approvalProgress?: IssueSnapshotItemApprovalProgress | null;
  /** The date and time when the issue was last modified. */
  lastModifiedAt?: string;
  /** The date and time when the issue was created. */
  createdAt?: string;
  /** The date and time when the issue was detected. */
  detectedAt?: string;
}
export const IssueSnapshotItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.optional(S.String),
    description: S.optional(S.String),
    readableIssueId: S.String,
    issueId: S.String,
    snapshotId: S.String,
    dueDate: S.optional(S.NullOr(S.String)),
    status: S.optional(IssueStatusTypeForSnapshot),
    approvalProgress: S.optional(S.NullOr(IssueSnapshotItemApprovalProgress)),
    lastModifiedAt: S.optional(S.String),
    createdAt: S.optional(S.String),
    detectedAt: S.optional(S.String),
  }),
).annotate({
  identifier: "IssueSnapshotItem",
}) as any as S.Schema<IssueSnapshotItem>;

export type PaginatedResponseIssueSnapshotItemResultsDataList =
  Array<IssueSnapshotItem>;
export const PaginatedResponseIssueSnapshotItemResultsDataList =
  /*@__PURE__*/ S.Array(
    IssueSnapshotItem,
  ) as any as S.Schema<PaginatedResponseIssueSnapshotItemResultsDataList>;

export interface PaginatedResponseIssueSnapshotItemResults {
  data: PaginatedResponseIssueSnapshotItemResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseIssueSnapshotItemResults =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      data: PaginatedResponseIssueSnapshotItemResultsDataList,
      pageInfo: PageInfo,
    }),
  ).annotate({
    identifier: "PaginatedResponseIssueSnapshotItemResults",
  }) as any as S.Schema<PaginatedResponseIssueSnapshotItemResults>;

export interface PaginatedResponseIssueSnapshotItem {
  results: PaginatedResponseIssueSnapshotItemResults;
}
export const PaginatedResponseIssueSnapshotItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseIssueSnapshotItemResults,
  }),
).annotate({
  identifier: "PaginatedResponseIssueSnapshotItem",
}) as any as S.Schema<PaginatedResponseIssueSnapshotItem>;

export type RiskOrderBy =
  | "inherentRisk"
  | "riskId"
  | "riskScenario"
  | "treatment"
  | "residualRisk"
  | "reviewStatus"
  | "owner"
  | "categories"
  | "ciaCategories"
  | "identified";
export const RiskOrderBy = /*@__PURE__*/ S.String;

export interface ListAuditRisksRequest {
  /** The audit ID */
  auditId: string;
  /** The risk assessment snapshot ID */
  snapshotId: string;
  /** Maximum number of results per page (1-100, default 10) */
  pageSize?: number;
  /** Pagination cursor from previous response */
  pageCursor?: string;
  /** Search term for filtering by risk scenario description */
  search?: string;
  /** Field to sort results by. Allowed: "riskId", "riskScenario", "inherentRisk", "treatment", "residualRisk", "reviewStatus", "owner", "categories", "ciaCategories", "identified". Default: "identified" */
  orderBy?: RiskOrderBy | (string & {});
  /** Sort direction: "asc" or "desc". Default: "desc" */
  orderDirection?: OrderDirection | (string & {});
}
export const ListAuditRisksRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    snapshotId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
    orderBy: S.optional(RiskOrderBy.pipe(T.Query())),
    orderDirection: S.optional(OrderDirection.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/risks/{snapshotId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListAuditRisksRequest",
}) as any as S.Schema<ListAuditRisksRequest>;

/** Risk treatment strategy returned by the auditor API. */
export type AuditRiskTreatment = "ACCEPT" | "AVOID" | "MITIGATE" | "TRANSFER";
export const AuditRiskTreatment = /*@__PURE__*/ S.String;

/** Treatment implementation status returned by the auditor API. */
export type AuditRiskTreatmentStatus = "IN_PROGRESS" | "DONE";
export const AuditRiskTreatmentStatus = /*@__PURE__*/ S.String;

/** Risk scenario review status returned by the auditor API. */
export type AuditRiskReviewStatus =
  | "APPROVED"
  | "DRAFT"
  | "NOT_REVIEWED"
  | "AWAITING_SUBMISSION"
  | "PENDING_APPROVAL"
  | "REQUESTED_CHANGES";
export const AuditRiskReviewStatus = /*@__PURE__*/ S.String;

/** Owner information for a risk. */
export interface AuditRiskOwner {
  /** Display name of the risk owner. */
  displayName: string;
  /** URL of the owner's profile image, or null if not available. */
  imageUrl: string | null;
}
export const AuditRiskOwner = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    displayName: S.String,
    imageUrl: S.NullOr(S.String),
  }),
).annotate({ identifier: "AuditRiskOwner" }) as any as S.Schema<AuditRiskOwner>;

/** Risk category names, or empty array if not categorized. */
export type AuditRiskCategoriesList = Array<string>;
export const AuditRiskCategoriesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AuditRiskCategoriesList>;

/** CIA triad category returned by the auditor API. */
export type AuditRiskCia = "AVAILABILITY" | "CONFIDENTIALITY" | "INTEGRITY";
export const AuditRiskCia = /*@__PURE__*/ S.String;

/** CIA triad categories, or empty array if not categorized. */
export type AuditRiskCiaCategoriesList = Array<AuditRiskCia>;
export const AuditRiskCiaCategoriesList = /*@__PURE__*/ S.Array(
  AuditRiskCia,
) as any as S.Schema<AuditRiskCiaCategoriesList>;

/** IDs of controls linked to this risk scenario, or empty array if none. Full Audit View only - omitted in Controlled Audit View. */
export type AuditRiskLinkedControlIdsList = Array<string>;
export const AuditRiskLinkedControlIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AuditRiskLinkedControlIdsList>;

/** A risk record from the risk population. `id` and `riskId` are required. All other fields are optional to support customizable field visibility. Omitted keys mean the column is not in the response; `null` means the column is present but empty. */
export interface AuditRisk {
  /** Unique identifier for the risk scenario version. */
  id: string;
  /** Custom risk ID (e.g., "RISK-001"), or null if not available. */
  riskId: string | null;
  /** Description of the risk scenario, or null if not available. */
  riskScenario?: string | null;
  /** Likelihood score of the risk, or null if not scored. Full Audit View only - omitted in Controlled Audit View. */
  likelihood?: number | null;
  /** Impact score of the risk, or null if not scored. Full Audit View only - omitted in Controlled Audit View. */
  impact?: number | null;
  /** Inherent risk score, or null if not scored. Defaults to the likelihood * impact product; when the organization scores risks by factors, this is the rolled-up factor score rounded to two decimals. */
  inherentRisk?: number | null;
  /** Risk treatment strategy, or null if not set. */
  treatment?: AuditRiskTreatment | null;
  /** Treatment implementation status, or null if not set. */
  treatmentStatus?: AuditRiskTreatmentStatus | null;
  /** Residual risk score after treatment, or null if not calculable. */
  residualRisk?: number | null;
  /** Review status of the risk scenario, or null if not set. */
  reviewStatus?: AuditRiskReviewStatus | null;
  /** Risk owner information, or null if no owner assigned. */
  owner?: AuditRiskOwner | null;
  /** Risk category names, or empty array if not categorized. */
  categories?: AuditRiskCategoriesList;
  /** CIA triad categories, or empty array if not categorized. */
  ciaCategories?: AuditRiskCiaCategoriesList;
  /** IDs of controls linked to this risk scenario, or empty array if none. Full Audit View only - omitted in Controlled Audit View. */
  linkedControlIds?: AuditRiskLinkedControlIdsList;
  /** When the risk was identified, or null if not recorded. ISO 8601 format. */
  identified?: string | null;
}
export const AuditRisk = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    riskId: S.NullOr(S.String),
    riskScenario: S.optional(S.NullOr(S.String)),
    likelihood: S.optional(S.NullOr(S.Number)),
    impact: S.optional(S.NullOr(S.Number)),
    inherentRisk: S.optional(S.NullOr(S.Number)),
    treatment: S.optional(S.NullOr(AuditRiskTreatment)),
    treatmentStatus: S.optional(S.NullOr(AuditRiskTreatmentStatus)),
    residualRisk: S.optional(S.NullOr(S.Number)),
    reviewStatus: S.optional(S.NullOr(AuditRiskReviewStatus)),
    owner: S.optional(S.NullOr(AuditRiskOwner)),
    categories: S.optional(AuditRiskCategoriesList),
    ciaCategories: S.optional(AuditRiskCiaCategoriesList),
    linkedControlIds: S.optional(AuditRiskLinkedControlIdsList),
    identified: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "AuditRisk" }) as any as S.Schema<AuditRisk>;

export type PaginatedResponseAuditRiskResultsDataList = Array<AuditRisk>;
export const PaginatedResponseAuditRiskResultsDataList = /*@__PURE__*/ S.Array(
  AuditRisk,
) as any as S.Schema<PaginatedResponseAuditRiskResultsDataList>;

export interface PaginatedResponseAuditRiskResults {
  data: PaginatedResponseAuditRiskResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseAuditRiskResults = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: PaginatedResponseAuditRiskResultsDataList,
    pageInfo: PageInfo,
  }),
).annotate({
  identifier: "PaginatedResponseAuditRiskResults",
}) as any as S.Schema<PaginatedResponseAuditRiskResults>;

export interface PaginatedResponseAuditRisk {
  results: PaginatedResponseAuditRiskResults;
}
export const PaginatedResponseAuditRisk = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseAuditRiskResults,
  }),
).annotate({
  identifier: "PaginatedResponseAuditRisk",
}) as any as S.Schema<PaginatedResponseAuditRisk>;

export interface ListAuditsRequest {
  pageSize?: number;
  pageCursor?: string;
  /** Includes all audits that have changed since changedSinceDate. */
  changedSinceDate?: string;
  /** Includes only audits with no audit report uploaded */
  isActiveAudit?: boolean;
}
export const ListAuditsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    changedSinceDate: S.optional(S.String.pipe(T.Query())),
    isActiveAudit: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/audits", code: 200 })),
).annotate({
  identifier: "ListAuditsRequest",
}) as any as S.Schema<ListAuditsRequest>;

export type PaginatedResponseAuditResultsDataList = Array<Audit>;
export const PaginatedResponseAuditResultsDataList = /*@__PURE__*/ S.Array(
  Audit,
) as any as S.Schema<PaginatedResponseAuditResultsDataList>;

export interface PaginatedResponseAuditResults {
  data: PaginatedResponseAuditResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseAuditResults = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: PaginatedResponseAuditResultsDataList,
    pageInfo: PageInfo,
  }),
).annotate({
  identifier: "PaginatedResponseAuditResults",
}) as any as S.Schema<PaginatedResponseAuditResults>;

export interface PaginatedResponseAudit {
  results: PaginatedResponseAuditResults;
}
export const PaginatedResponseAudit = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseAuditResults,
  }),
).annotate({
  identifier: "PaginatedResponseAudit",
}) as any as S.Schema<PaginatedResponseAudit>;

export interface ListAuditSnapshotsRequest {
  /** The audit ID */
  auditId: string;
  /** Maximum number of results per page (1-100, default 10) */
  pageSize?: number;
  /** Pagination cursor from previous response */
  pageCursor?: string;
  /** Search term for filtering by snapshot title and description */
  search?: string;
}
export const ListAuditSnapshotsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/issues/snapshots",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListAuditSnapshotsRequest",
}) as any as S.Schema<ListAuditSnapshotsRequest>;

export interface IssueSnapshotMetadata {
  /** The unique identifier for the issue snapshot. */
  id: string;
  /** The title of the snapshot */
  title?: string;
  /** The description of the snapshot */
  description?: string;
  /** The date and time when the snapshot was created. */
  createdAt?: string;
}
export const IssueSnapshotMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    title: S.optional(S.String),
    description: S.optional(S.String),
    createdAt: S.optional(S.String),
  }),
).annotate({
  identifier: "IssueSnapshotMetadata",
}) as any as S.Schema<IssueSnapshotMetadata>;

export type PaginatedResponseIssueSnapshotMetadataResultsDataList =
  Array<IssueSnapshotMetadata>;
export const PaginatedResponseIssueSnapshotMetadataResultsDataList =
  /*@__PURE__*/ S.Array(
    IssueSnapshotMetadata,
  ) as any as S.Schema<PaginatedResponseIssueSnapshotMetadataResultsDataList>;

export interface PaginatedResponseIssueSnapshotMetadataResults {
  data: PaginatedResponseIssueSnapshotMetadataResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseIssueSnapshotMetadataResults =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      data: PaginatedResponseIssueSnapshotMetadataResultsDataList,
      pageInfo: PageInfo,
    }),
  ).annotate({
    identifier: "PaginatedResponseIssueSnapshotMetadataResults",
  }) as any as S.Schema<PaginatedResponseIssueSnapshotMetadataResults>;

export interface PaginatedResponseIssueSnapshotMetadata {
  results: PaginatedResponseIssueSnapshotMetadataResults;
}
export const PaginatedResponseIssueSnapshotMetadata = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      results: PaginatedResponseIssueSnapshotMetadataResults,
    }),
).annotate({
  identifier: "PaginatedResponseIssueSnapshotMetadata",
}) as any as S.Schema<PaginatedResponseIssueSnapshotMetadata>;

/** Version control source service name for code changes. */
export type ApiCodeChangeSource =
  | "github"
  | "gitlab"
  | "bitbucket"
  | "azuredevops";
export const ApiCodeChangeSource = /*@__PURE__*/ S.String;

export type ListCodeChangesRequestSourcesMatchesAnyList = Array<
  ApiCodeChangeSource | (string & {})
>;
export const ListCodeChangesRequestSourcesMatchesAnyList =
  /*@__PURE__*/ S.Array(
    ApiCodeChangeSource,
  ) as any as S.Schema<ListCodeChangesRequestSourcesMatchesAnyList>;

export interface ListCodeChangesRequest {
  /** The audit ID */
  auditId: string;
  /** Maximum number of results per page (1-100, default 10) */
  pageSize?: number;
  /** Pagination cursor from previous response */
  pageCursor?: string;
  /** Search term for filtering by code change title or repository name */
  search?: string;
  /** Filter code changes by version control source (accepted values: github, gitlab, bitbucket, azuredevops) */
  sourcesMatchesAny?: ListCodeChangesRequestSourcesMatchesAnyList;
  /** Filter code changes closed on or after this date (ISO 8601) */
  closedAfterDate?: string;
  /** Filter code changes closed on or before this date (ISO 8601) */
  closedBeforeDate?: string;
}
export const ListCodeChangesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
    sourcesMatchesAny: S.optional(
      ListCodeChangesRequestSourcesMatchesAnyList.pipe(T.Query()),
    ),
    closedAfterDate: S.optional(S.String.pipe(T.Query())),
    closedBeforeDate: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/assets/code-changes",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListCodeChangesRequest",
}) as any as S.Schema<ListCodeChangesRequest>;

/** Code change data. `id` and `codeChange` are required. All other fields are optional to support customizable field visibility. Omitted keys mean the column is not in the response; `null` means the column is present but empty. */
export interface CodeChange {
  /** Code change record ID - always present. */
  id: string;
  /** Code change title (e.g. PR title) - always present. */
  codeChange: string;
  /** Code change identifier (e.g. PR number). */
  identifier?: string;
  /** Version control source where the change originated. Known values are listed in {@link ApiCodeChangeSource}; other strings may appear if a new source is onboarded before this enum is updated. */
  service?: string;
  /** Repository name. */
  repository?: string;
  /** Date the code change was opened. */
  openedAt?: string;
  /** Date the code change was merged/closed. */
  closedAt?: string;
}
export const CodeChange = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    codeChange: S.String,
    identifier: S.optional(S.String),
    service: S.optional(S.String),
    repository: S.optional(S.String),
    openedAt: S.optional(S.String),
    closedAt: S.optional(S.String),
  }),
).annotate({ identifier: "CodeChange" }) as any as S.Schema<CodeChange>;

export type PaginatedResponseCodeChangeResultsDataList = Array<CodeChange>;
export const PaginatedResponseCodeChangeResultsDataList = /*@__PURE__*/ S.Array(
  CodeChange,
) as any as S.Schema<PaginatedResponseCodeChangeResultsDataList>;

export interface PaginatedResponseCodeChangeResults {
  data: PaginatedResponseCodeChangeResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseCodeChangeResults = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: PaginatedResponseCodeChangeResultsDataList,
    pageInfo: PageInfo,
  }),
).annotate({
  identifier: "PaginatedResponseCodeChangeResults",
}) as any as S.Schema<PaginatedResponseCodeChangeResults>;

export interface PaginatedResponseCodeChange {
  results: PaginatedResponseCodeChangeResults;
}
export const PaginatedResponseCodeChange = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseCodeChangeResults,
  }),
).annotate({
  identifier: "PaginatedResponseCodeChange",
}) as any as S.Schema<PaginatedResponseCodeChange>;

export interface ListCommentsForControlRequest {
  auditId: string;
  controlId: string;
  /** Maximum number of comments to return per page. */
  pageSize?: number;
  /** Pagination cursor from a previous response. Provide to fetch the next page of comments. */
  pageCursor?: string;
  /** Includes all comments that have changed since changedSinceDate. Considers creationDate, modificationDate, and deletionDate timestamps when determining changes. */
  changedSinceDate?: string;
}
export const ListCommentsForControlRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    controlId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    changedSinceDate: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/controls/{controlId}/comments",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListCommentsForControlRequest",
}) as any as S.Schema<ListCommentsForControlRequest>;

export type PaginatedResponseAuditControlCommentResultsDataList =
  Array<AuditControlComment>;
export const PaginatedResponseAuditControlCommentResultsDataList =
  /*@__PURE__*/ S.Array(
    AuditControlComment,
  ) as any as S.Schema<PaginatedResponseAuditControlCommentResultsDataList>;

export interface PaginatedResponseAuditControlCommentResults {
  data: PaginatedResponseAuditControlCommentResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseAuditControlCommentResults =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      data: PaginatedResponseAuditControlCommentResultsDataList,
      pageInfo: PageInfo,
    }),
  ).annotate({
    identifier: "PaginatedResponseAuditControlCommentResults",
  }) as any as S.Schema<PaginatedResponseAuditControlCommentResults>;

export interface PaginatedResponseAuditControlComment {
  results: PaginatedResponseAuditControlCommentResults;
}
export const PaginatedResponseAuditControlComment = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      results: PaginatedResponseAuditControlCommentResults,
    }),
).annotate({
  identifier: "PaginatedResponseAuditControlComment",
}) as any as S.Schema<PaginatedResponseAuditControlComment>;

export interface ListCommentsForInformationRequestRequest {
  auditId: string;
  requestId: string;
  /** Maximum number of comments to return per page. */
  pageSize?: number;
  /** Pagination cursor from a previous response. Provide to fetch the next page of comments. */
  pageCursor?: string;
  /** Includes all comments that have changed since changedSinceDate. Considers creationDate, modificationDate, and deletionDate timestamps when determining changes. */
  changedSinceDate?: string;
}
export const ListCommentsForInformationRequestRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      requestId: S.String.pipe(T.Label()),
      pageSize: S.optional(S.Number.pipe(T.Query())),
      pageCursor: S.optional(S.String.pipe(T.Query())),
      changedSinceDate: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/audits/{auditId}/information-requests/{requestId}/comments",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ListCommentsForInformationRequestRequest",
}) as any as S.Schema<ListCommentsForInformationRequestRequest>;

export type PaginatedResponseInformationRequestCommentResultsDataList =
  Array<InformationRequestComment>;
export const PaginatedResponseInformationRequestCommentResultsDataList =
  /*@__PURE__*/ S.Array(
    InformationRequestComment,
  ) as any as S.Schema<PaginatedResponseInformationRequestCommentResultsDataList>;

export interface PaginatedResponseInformationRequestCommentResults {
  data: PaginatedResponseInformationRequestCommentResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseInformationRequestCommentResults =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      data: PaginatedResponseInformationRequestCommentResultsDataList,
      pageInfo: PageInfo,
    }),
  ).annotate({
    identifier: "PaginatedResponseInformationRequestCommentResults",
  }) as any as S.Schema<PaginatedResponseInformationRequestCommentResults>;

export interface PaginatedResponseInformationRequestComment {
  results: PaginatedResponseInformationRequestCommentResults;
}
export const PaginatedResponseInformationRequestComment =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      results: PaginatedResponseInformationRequestCommentResults,
    }),
  ).annotate({
    identifier: "PaginatedResponseInformationRequestComment",
  }) as any as S.Schema<PaginatedResponseInformationRequestComment>;

export interface ListInformationRequestActivityRequest {
  auditId: string;
  requestId: string;
  /** Maximum number of activity entries to return per page. */
  pageSize?: number;
  /** Pagination cursor from a previous response. Provide to fetch the next page of activity logs. */
  pageCursor?: string;
  /** Includes activity logs that have changed since changedSinceDate. */
  changedSinceDate?: string;
}
export const ListInformationRequestActivityRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      requestId: S.String.pipe(T.Label()),
      pageSize: S.optional(S.Number.pipe(T.Query())),
      pageCursor: S.optional(S.String.pipe(T.Query())),
      changedSinceDate: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/audits/{auditId}/information-requests/{requestId}/activity",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ListInformationRequestActivityRequest",
}) as any as S.Schema<ListInformationRequestActivityRequest>;

export type InformationRequestActivityType =
  | "AUDIT_DUPLICATION_AUDIT_TRAIL"
  | "AUDIT_INFORMATION_REQUEST_EVIDENCE_FILL"
  | "CHANGE_STATUS"
  | "EDIT_REQUEST"
  | "REMOVE_EVIDENCE"
  | "SHARE_EVIDENCE_WITH_AUDITOR"
  | "UPLOAD_EVIDENCE";
export const InformationRequestActivityType = /*@__PURE__*/ S.String;

/** Previous approval status before the status change. Only populated for status change activities. Null for all other activity types. */
export type InformationRequestActivityLogOldStatus =
  | "READY_FOR_AUDIT"
  | "NEEDS_EVIDENCE"
  | "AUDITOR_APPROVED"
  | "AUDITOR_FLAGGED"
  | "READY_FOR_INTERNAL_REVIEW";
export const InformationRequestActivityLogOldStatus = /*@__PURE__*/ S.String;

/** New approval status after the status change. Only populated for status change activities. Null for all other activity types. */
export type InformationRequestActivityLogNewStatus =
  | "READY_FOR_AUDIT"
  | "NEEDS_EVIDENCE"
  | "AUDITOR_APPROVED"
  | "AUDITOR_FLAGGED"
  | "READY_FOR_INTERNAL_REVIEW";
export const InformationRequestActivityLogNewStatus = /*@__PURE__*/ S.String;

export type EvidenceFillOutcome = "SUCCESS" | "PARTIAL" | "FAILED";
export const EvidenceFillOutcome = /*@__PURE__*/ S.String;

/** Activity log entry tracking changes and actions on an information request. The activity log provides a complete audit trail of all operations performed on an information request, including status changes, evidence uploads, edits, and evidence sharing. This enables tracking compliance activities and understanding the request's history. */
export interface InformationRequestActivityLog {
  /** The unique identifier for the activity log entry within Vanta's system. Format: ObjectId as a string (e.g., "6890e473dce1da5d8406f5e7"). */
  id: string;
  /** Type of activity that occurred on the information request. Determines which additional fields are populated. */
  activityType: InformationRequestActivityType;
  /** Timestamp when the activity occurred. Format: ISO 8601 UTC timestamp. */
  timestamp: string;
  /** Email address of the user who performed the activity. Null for system-generated activities (rare, such as automated status changes). This email uniquely identifies users across systems. */
  userEmail: string | null;
  /** Previous approval status before the status change. Only populated for status change activities. Null for all other activity types. */
  oldStatus: InformationRequestActivityLogOldStatus | null;
  /** New approval status after the status change. Only populated for status change activities. Null for all other activity types. */
  newStatus: InformationRequestActivityLogNewStatus | null;
  /** Optional explanation for the status change. Only populated for status change activities when a reason is provided (e.g., when flagging evidence). Null for all other cases. */
  reason: string | null;
  /** Result of an automated evidence fill. Only populated for evidence fill activities. Null for all other activity types. */
  fillOutcome: EvidenceFillOutcome | null;
  /** Identifier of the information request this one was copied from when its audit was duplicated. Only populated for audit-duplication trail activities. Null for all other activity types. Format: ObjectId as a string. */
  sourceInformationRequestId: string | null;
  /** Identifier of the audit this request's audit was duplicated from. Only populated for audit-duplication trail activities. Null for all other activity types. Format: ObjectId as a string. */
  sourceAuditId: string | null;
}
export const InformationRequestActivityLog = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    activityType: InformationRequestActivityType,
    timestamp: S.String,
    userEmail: S.NullOr(S.String),
    oldStatus: S.NullOr(InformationRequestActivityLogOldStatus),
    newStatus: S.NullOr(InformationRequestActivityLogNewStatus),
    reason: S.NullOr(S.String),
    fillOutcome: S.NullOr(EvidenceFillOutcome),
    sourceInformationRequestId: S.NullOr(S.String),
    sourceAuditId: S.NullOr(S.String),
  }),
).annotate({
  identifier: "InformationRequestActivityLog",
}) as any as S.Schema<InformationRequestActivityLog>;

export type PaginatedResponseInformationRequestActivityLogResultsDataList =
  Array<InformationRequestActivityLog>;
export const PaginatedResponseInformationRequestActivityLogResultsDataList =
  /*@__PURE__*/ S.Array(
    InformationRequestActivityLog,
  ) as any as S.Schema<PaginatedResponseInformationRequestActivityLogResultsDataList>;

export interface PaginatedResponseInformationRequestActivityLogResults {
  data: PaginatedResponseInformationRequestActivityLogResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseInformationRequestActivityLogResults =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      data: PaginatedResponseInformationRequestActivityLogResultsDataList,
      pageInfo: PageInfo,
    }),
  ).annotate({
    identifier: "PaginatedResponseInformationRequestActivityLogResults",
  }) as any as S.Schema<PaginatedResponseInformationRequestActivityLogResults>;

export interface PaginatedResponseInformationRequestActivityLog {
  results: PaginatedResponseInformationRequestActivityLogResults;
}
export const PaginatedResponseInformationRequestActivityLog =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      results: PaginatedResponseInformationRequestActivityLogResults,
    }),
  ).annotate({
    identifier: "PaginatedResponseInformationRequestActivityLog",
  }) as any as S.Schema<PaginatedResponseInformationRequestActivityLog>;

export type ListInformationRequestEvidenceRequestEvidenceTypeMatchesAnyList =
  Array<InformationRequestSupportedEvidenceType | (string & {})>;
export const ListInformationRequestEvidenceRequestEvidenceTypeMatchesAnyList =
  /*@__PURE__*/ S.Array(
    InformationRequestSupportedEvidenceType,
  ) as any as S.Schema<ListInformationRequestEvidenceRequestEvidenceTypeMatchesAnyList>;

export interface ListInformationRequestEvidenceRequest {
  auditId: string;
  requestId: string;
  /** Maximum number of evidence entries to return per page. */
  pageSize?: number;
  /** Pagination cursor from a previous response. Provide to fetch the next page of evidence. */
  pageCursor?: string;
  /** Includes all evidence that have changed since changedSinceDate. Considers creationDate, modificationDate, deletionDate, and visibleToAuditorDate timestamps when determining changes. */
  changedSinceDate?: string;
  /** Limits results to the provided evidence types. Must include at least one of: UPLOADED_DOCUMENT, OBSERVATION, LINK, VANTA_DOCUMENT, VANTA_POLICY, VANTA_TEST, VANTA_TEST_SNAPSHOT. */
  evidenceTypeMatchesAny?: ListInformationRequestEvidenceRequestEvidenceTypeMatchesAnyList;
}
export const ListInformationRequestEvidenceRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      requestId: S.String.pipe(T.Label()),
      pageSize: S.optional(S.Number.pipe(T.Query())),
      pageCursor: S.optional(S.String.pipe(T.Query())),
      changedSinceDate: S.optional(S.String.pipe(T.Query())),
      evidenceTypeMatchesAny: S.optional(
        ListInformationRequestEvidenceRequestEvidenceTypeMatchesAnyList.pipe(
          T.Query(),
        ),
      ),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/audits/{auditId}/information-requests/{requestId}/evidence",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ListInformationRequestEvidenceRequest",
}) as any as S.Schema<ListInformationRequestEvidenceRequest>;

export type PaginatedResponseInformationRequestEvidenceResultsDataList =
  Array<InformationRequestEvidence>;
export const PaginatedResponseInformationRequestEvidenceResultsDataList =
  /*@__PURE__*/ S.Array(
    InformationRequestEvidence,
  ) as any as S.Schema<PaginatedResponseInformationRequestEvidenceResultsDataList>;

export interface PaginatedResponseInformationRequestEvidenceResults {
  data: PaginatedResponseInformationRequestEvidenceResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseInformationRequestEvidenceResults =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      data: PaginatedResponseInformationRequestEvidenceResultsDataList,
      pageInfo: PageInfo,
    }),
  ).annotate({
    identifier: "PaginatedResponseInformationRequestEvidenceResults",
  }) as any as S.Schema<PaginatedResponseInformationRequestEvidenceResults>;

export interface PaginatedResponseInformationRequestEvidence {
  results: PaginatedResponseInformationRequestEvidenceResults;
}
export const PaginatedResponseInformationRequestEvidence =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      results: PaginatedResponseInformationRequestEvidenceResults,
    }),
  ).annotate({
    identifier: "PaginatedResponseInformationRequestEvidence",
  }) as any as S.Schema<PaginatedResponseInformationRequestEvidence>;

export interface ListInformationRequestsRequest {
  auditId: string;
  /** Maximum number of information requests to return per page. */
  pageSize?: number;
  /** Pagination cursor from a previous response. Provide to fetch the next page of results. */
  pageCursor?: string;
  /** Includes all information requests that have changed since changedSinceDate. Considers creationDate, modificationDate, and deletionDate timestamps when determining changes. */
  changedSinceDate?: string;
}
export const ListInformationRequestsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    changedSinceDate: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/information-requests",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListInformationRequestsRequest",
}) as any as S.Schema<ListInformationRequestsRequest>;

export type PaginatedResponseInformationRequestResultsDataList =
  Array<InformationRequest>;
export const PaginatedResponseInformationRequestResultsDataList =
  /*@__PURE__*/ S.Array(
    InformationRequest,
  ) as any as S.Schema<PaginatedResponseInformationRequestResultsDataList>;

export interface PaginatedResponseInformationRequestResults {
  data: PaginatedResponseInformationRequestResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseInformationRequestResults =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      data: PaginatedResponseInformationRequestResultsDataList,
      pageInfo: PageInfo,
    }),
  ).annotate({
    identifier: "PaginatedResponseInformationRequestResults",
  }) as any as S.Schema<PaginatedResponseInformationRequestResults>;

export interface PaginatedResponseInformationRequest {
  results: PaginatedResponseInformationRequestResults;
}
export const PaginatedResponseInformationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseInformationRequestResults,
  }),
).annotate({
  identifier: "PaginatedResponseInformationRequest",
}) as any as S.Schema<PaginatedResponseInformationRequest>;

export interface ListInformationRequestsForControlRequest {
  auditId: string;
  controlId: string;
  /** Maximum number of information requests to return per page. */
  pageSize?: number;
  /** Pagination cursor from a previous response. Provide to fetch the next page of results. */
  pageCursor?: string;
}
export const ListInformationRequestsForControlRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      controlId: S.String.pipe(T.Label()),
      pageSize: S.optional(S.Number.pipe(T.Query())),
      pageCursor: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/audits/{auditId}/controls/{controlId}/information-requests",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ListInformationRequestsForControlRequest",
}) as any as S.Schema<ListInformationRequestsForControlRequest>;

export type AuditIntegrationTag =
  | "ACCESS"
  | "COMPUTERS"
  | "CUSTOM_RESOURCE"
  | "DATA_LOSS_PREVENTION"
  | "DOCUMENTS"
  | "DOCUMENT_UPLOAD"
  | "FRAMEWORK"
  | "GROUPS"
  | "INVENTORY"
  | "NOTIFICATIONS"
  | "PEOPLE"
  | "POLICIES"
  | "SECURITY_AWARENESS_TRAINING"
  | "SECURITY_SCANNER_ALERTS"
  | "SSO"
  | "TASK_CREATION"
  | "TASK_TRACKING"
  | "TRUST_CENTER"
  | "VENDOR_DISCOVERY"
  | "VENDOR_PROCUREMENT"
  | "VULNERABILITIES";
export const AuditIntegrationTag = /*@__PURE__*/ S.String;

export type ListIntegrationsRequestTagsMatchesAnyList = Array<
  AuditIntegrationTag | (string & {})
>;
export const ListIntegrationsRequestTagsMatchesAnyList = /*@__PURE__*/ S.Array(
  AuditIntegrationTag,
) as any as S.Schema<ListIntegrationsRequestTagsMatchesAnyList>;

export type AuditIntegrationCategory =
  | "ATS_MERGE_ACCOUNT"
  | "BACKGROUND_CHECK_SERVICE"
  | "CLOUD_PROVIDER"
  | "COMMUNICATION_PLATFORM"
  | "CRM_MERGE_ACCOUNT"
  | "CRM_PLATFORM"
  | "DATASTORE_PROVIDER"
  | "DATA_LOSS_PREVENTION"
  | "DATA_WAREHOUSE_PROVIDER"
  | "DOCUMENT_MANAGEMENT_TOOL"
  | "ENDPOINT_SECURITY_TOOL"
  | "HR_PROVIDER"
  | "IDENTITY_PROVIDER"
  | "INCIDENT_MANAGEMENT_TOOL"
  | "MOBILE_DEVICE_MANAGEMENT_TOOL"
  | "MONITORING_SERVICE"
  | "OTHER_INTEGRATION"
  | "SECURITY_AWARENESS_TRAINING_PROVIDER"
  | "SECURITY_SCANNER_ALERTS"
  | "TASK_TRACKER"
  | "VERSION_CONTROL_SYSTEM"
  | "VULNERABILITY_SCANNER";
export const AuditIntegrationCategory = /*@__PURE__*/ S.String;

export type ListIntegrationsRequestCategoriesMatchesAnyList = Array<
  AuditIntegrationCategory | (string & {})
>;
export const ListIntegrationsRequestCategoriesMatchesAnyList =
  /*@__PURE__*/ S.Array(
    AuditIntegrationCategory,
  ) as any as S.Schema<ListIntegrationsRequestCategoriesMatchesAnyList>;

export interface ListIntegrationsRequest {
  /** The audit ID */
  auditId: string;
  /** Maximum number of results per page (1-100, default 10) */
  pageSize?: number;
  /** Pagination cursor from previous response */
  pageCursor?: string;
  /** Search term for filtering by integration name */
  search?: string;
  /** Filter integrations by tag values */
  tagsMatchesAny?: ListIntegrationsRequestTagsMatchesAnyList;
  /** Filter integrations by category values */
  categoriesMatchesAny?: ListIntegrationsRequestCategoriesMatchesAnyList;
}
export const ListIntegrationsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
    tagsMatchesAny: S.optional(
      ListIntegrationsRequestTagsMatchesAnyList.pipe(T.Query()),
    ),
    categoriesMatchesAny: S.optional(
      ListIntegrationsRequestCategoriesMatchesAnyList.pipe(T.Query()),
    ),
  }).pipe(
    T.Http({ method: "GET", uri: "/audits/{auditId}/integrations", code: 200 }),
  ),
).annotate({
  identifier: "ListIntegrationsRequest",
}) as any as S.Schema<ListIntegrationsRequest>;

/** Integration tag display names describing what data this integration provides. */
export type AuditIntegrationTagsList = Array<string>;
export const AuditIntegrationTagsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AuditIntegrationTagsList>;

/** Category display names describing what the integration is used for. */
export type AuditIntegrationCategoriesList = Array<string>;
export const AuditIntegrationCategoriesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AuditIntegrationCategoriesList>;

/** Integration row returned by the auditor integrations API. `id` and `name` are always present. Other fields are optional to support controlled audits where only approved columns are returned. */
export interface AuditIntegration {
  /** Integration identifier. */
  id: string;
  /** Display name of the integration. */
  name: string;
  /** Integration tag display names describing what data this integration provides. */
  tags?: AuditIntegrationTagsList;
  /** Category display names describing what the integration is used for. */
  categories?: AuditIntegrationCategoriesList;
}
export const AuditIntegration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    tags: S.optional(AuditIntegrationTagsList),
    categories: S.optional(AuditIntegrationCategoriesList),
  }),
).annotate({
  identifier: "AuditIntegration",
}) as any as S.Schema<AuditIntegration>;

export type PaginatedResponseAuditIntegrationResultsDataList =
  Array<AuditIntegration>;
export const PaginatedResponseAuditIntegrationResultsDataList =
  /*@__PURE__*/ S.Array(
    AuditIntegration,
  ) as any as S.Schema<PaginatedResponseAuditIntegrationResultsDataList>;

export interface PaginatedResponseAuditIntegrationResults {
  data: PaginatedResponseAuditIntegrationResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseAuditIntegrationResults = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      data: PaginatedResponseAuditIntegrationResultsDataList,
      pageInfo: PageInfo,
    }),
).annotate({
  identifier: "PaginatedResponseAuditIntegrationResults",
}) as any as S.Schema<PaginatedResponseAuditIntegrationResults>;

export interface PaginatedResponseAuditIntegration {
  results: PaginatedResponseAuditIntegrationResults;
}
export const PaginatedResponseAuditIntegration = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseAuditIntegrationResults,
  }),
).annotate({
  identifier: "PaginatedResponseAuditIntegration",
}) as any as S.Schema<PaginatedResponseAuditIntegration>;

/** Account status values for account access records. Unified status enum that covers all service types. */
export type AccountAccessStatus = "ACTIVE" | "DEACTIVATED" | "UNKNOWN";
export const AccountAccessStatus = /*@__PURE__*/ S.String;

export interface ListPersonnelAccountAccessRequest {
  /** The audit ID */
  auditId: string;
  /** The service ID from the /services endpoint */
  serviceId: string;
  /** Maximum number of results per page (1-100, default 10) */
  pageSize?: number;
  /** Pagination cursor from previous response */
  pageCursor?: string;
  /** Search term for filtering by account name or email */
  search?: string;
  /** Filter by account status */
  status?: AccountAccessStatus | (string & {});
}
export const ListPersonnelAccountAccessRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    serviceId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
    status: S.optional(AccountAccessStatus.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/personnel/account-access/{serviceId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListPersonnelAccountAccessRequest",
}) as any as S.Schema<ListPersonnelAccountAccessRequest>;

/** Account roles; empty array when the column is visible but has no roles. */
export type AccountAccessRoleList = Array<string>;
export const AccountAccessRoleList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AccountAccessRoleList>;

/** Account access record from personnel population. This unified interface represents account access data from all service types: - IDP services (Okta, Azure AD, Google Workspace, etc.) - First-party account services (AWS, Azure, GCP, Oracle Cloud, etc.) - Role grants services (GCP, Azure role grants) - Received/third-party applications Only `id` and `accountName` are always present. Other fields are optional so disallowed columns can be omitted (for example under controlled audit view). When an optional field is present, `null` means the column is visible but empty (only fields that are nullable in population data use `null`). */
export interface AccountAccess {
  /** Unique identifier for the account. */
  id: string;
  /** Account name or email address (primary identifier). */
  accountName: string;
  /** Display name of the account owner, or null when the column is visible but unknown. */
  owner?: string | null;
  /** Account roles; empty array when the column is visible but has no roles. */
  role?: AccountAccessRoleList;
  /** Account status when present; omitted when the column is not in the response. */
  status?: AccountAccessStatus;
  /** Multi-factor authentication (MFA) status when present. `true` = MFA enabled, `false` = MFA not enabled, `null` = visible but unknown. */
  mfa?: boolean | null;
  /** Account creation date when present, or null when visible but absent. */
  createdDate?: string | null;
  /** Account deactivation date when present, or null when active. */
  deactivatedDate?: string | null;
}
export const AccountAccess = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    accountName: S.String,
    owner: S.optional(S.NullOr(S.String)),
    role: S.optional(AccountAccessRoleList),
    status: S.optional(AccountAccessStatus),
    mfa: S.optional(S.NullOr(S.Boolean)),
    createdDate: S.optional(S.NullOr(S.String)),
    deactivatedDate: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "AccountAccess" }) as any as S.Schema<AccountAccess>;

export type PaginatedResponseAccountAccessResultsDataList =
  Array<AccountAccess>;
export const PaginatedResponseAccountAccessResultsDataList =
  /*@__PURE__*/ S.Array(
    AccountAccess,
  ) as any as S.Schema<PaginatedResponseAccountAccessResultsDataList>;

export interface PaginatedResponseAccountAccessResults {
  data: PaginatedResponseAccountAccessResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseAccountAccessResults = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      data: PaginatedResponseAccountAccessResultsDataList,
      pageInfo: PageInfo,
    }),
).annotate({
  identifier: "PaginatedResponseAccountAccessResults",
}) as any as S.Schema<PaginatedResponseAccountAccessResults>;

export interface PaginatedResponseAccountAccess {
  results: PaginatedResponseAccountAccessResults;
}
export const PaginatedResponseAccountAccess = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseAccountAccessResults,
  }),
).annotate({
  identifier: "PaginatedResponseAccountAccess",
}) as any as S.Schema<PaginatedResponseAccountAccess>;

export type ListPersonnelGroupsRequestSourcesMatchesAnyList = Array<string>;
export const ListPersonnelGroupsRequestSourcesMatchesAnyList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ListPersonnelGroupsRequestSourcesMatchesAnyList>;

export type GroupOrderBy =
  | "name"
  | "members"
  | "source"
  | "tasksLastUpdated"
  | "pointOfContact";
export const GroupOrderBy = /*@__PURE__*/ S.String;

export interface ListPersonnelGroupsRequest {
  /** The audit ID */
  auditId: string;
  /** Maximum number of results per page (1-100, default 10) */
  pageSize?: number;
  /** Pagination cursor from previous response */
  pageCursor?: string;
  /** Search term for filtering by group name */
  search?: string;
  /** Filter groups by IDP source service names */
  sourcesMatchesAny?: ListPersonnelGroupsRequestSourcesMatchesAnyList;
  /** Field to sort results by. Allowed: "name", "members", "source", "tasksLastUpdated", "pointOfContact". Default: "name" */
  orderBy?: GroupOrderBy | (string & {});
  /** Sort direction: "asc" or "desc". Default: "asc" */
  orderDirection?: OrderDirection | (string & {});
}
export const ListPersonnelGroupsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
    sourcesMatchesAny: S.optional(
      ListPersonnelGroupsRequestSourcesMatchesAnyList.pipe(T.Query()),
    ),
    orderBy: S.optional(GroupOrderBy.pipe(T.Query())),
    orderDirection: S.optional(OrderDirection.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/personnel/groups",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListPersonnelGroupsRequest",
}) as any as S.Schema<ListPersonnelGroupsRequest>;

/** A task assigned to a personnel group. */
export interface PersonnelGroupTask {
  /** Task name */
  name: string;
}
export const PersonnelGroupTask = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
  }),
).annotate({
  identifier: "PersonnelGroupTask",
}) as any as S.Schema<PersonnelGroupTask>;

/** Security tasks enabled for this group, or empty array if not available. */
export type PersonnelGroupTasksList = Array<PersonnelGroupTask>;
export const PersonnelGroupTasksList = /*@__PURE__*/ S.Array(
  PersonnelGroupTask,
) as any as S.Schema<PersonnelGroupTasksList>;

/** A group record from the personnel population. `id` and `name` are required. All other fields are optional to support customizable field visibility. Omitted keys mean the column is not in the response; `null` means the column is present but empty. */
export interface PersonnelGroup {
  /** Unique identifier for the group. */
  id: string;
  /** Display name of the group. */
  name: string;
  /** Number of people in this group within the audit scope. */
  members?: number;
  /** The source of this group. For IDP-synced groups, this is the provider name (e.g., "Okta", "Azure AD"). For manually created groups, this is "Vanta". */
  source?: string;
  /** Security tasks enabled for this group, or empty array if not available. */
  tasks?: PersonnelGroupTasksList;
  /** When the group's task configuration was last updated, or null if never updated. ISO 8601 format. */
  tasksLastUpdated?: string | null;
  /** Display name of the group's designated point of contact, or null if not set. */
  pointOfContact?: string | null;
}
export const PersonnelGroup = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    members: S.optional(S.Number),
    source: S.optional(S.String),
    tasks: S.optional(PersonnelGroupTasksList),
    tasksLastUpdated: S.optional(S.NullOr(S.String)),
    pointOfContact: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "PersonnelGroup" }) as any as S.Schema<PersonnelGroup>;

export type PaginatedResponsePersonnelGroupResultsDataList =
  Array<PersonnelGroup>;
export const PaginatedResponsePersonnelGroupResultsDataList =
  /*@__PURE__*/ S.Array(
    PersonnelGroup,
  ) as any as S.Schema<PaginatedResponsePersonnelGroupResultsDataList>;

export interface PaginatedResponsePersonnelGroupResults {
  data: PaginatedResponsePersonnelGroupResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponsePersonnelGroupResults = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      data: PaginatedResponsePersonnelGroupResultsDataList,
      pageInfo: PageInfo,
    }),
).annotate({
  identifier: "PaginatedResponsePersonnelGroupResults",
}) as any as S.Schema<PaginatedResponsePersonnelGroupResults>;

export interface PaginatedResponsePersonnelGroup {
  results: PaginatedResponsePersonnelGroupResults;
}
export const PaginatedResponsePersonnelGroup = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponsePersonnelGroupResults,
  }),
).annotate({
  identifier: "PaginatedResponsePersonnelGroup",
}) as any as S.Schema<PaginatedResponsePersonnelGroup>;

/** Employment status filter for people queries. */
export type PeopleEmploymentStatus =
  | "CURRENTLY_EMPLOYED"
  | "INACTIVE_EMPLOYEE"
  | "NOT_PEOPLE"
  | "ON_LEAVE"
  | "PREVIOUSLY_EMPLOYED"
  | "UPCOMING_EMPLOYEE";
export const PeopleEmploymentStatus = /*@__PURE__*/ S.String;

export type ListPersonnelPeopleRequestGroupsMatchesAnyList = Array<string>;
export const ListPersonnelPeopleRequestGroupsMatchesAnyList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ListPersonnelPeopleRequestGroupsMatchesAnyList>;

export type PeopleOrderBy = "name" | "employmentStatus";
export const PeopleOrderBy = /*@__PURE__*/ S.String;

export interface ListPersonnelPeopleRequest {
  /** The audit ID */
  auditId: string;
  /** Maximum number of results per page (1-100, default 10) */
  pageSize?: number;
  /** Pagination cursor from previous response */
  pageCursor?: string;
  /** Search term for filtering by name or email */
  search?: string;
  /** Employment status filter */
  status?: PeopleEmploymentStatus | (string & {});
  /** Filter people by group IDs */
  groupsMatchesAny?: ListPersonnelPeopleRequestGroupsMatchesAnyList;
  /** Field to sort results by. Allowed: "name", "employmentStatus". Default: "name" */
  orderBy?: PeopleOrderBy | (string & {});
  /** Sort direction: "asc" or "desc". Default: "asc" */
  orderDirection?: OrderDirection | (string & {});
}
export const ListPersonnelPeopleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
    status: S.optional(PeopleEmploymentStatus.pipe(T.Query())),
    groupsMatchesAny: S.optional(
      ListPersonnelPeopleRequestGroupsMatchesAnyList.pipe(T.Query()),
    ),
    orderBy: S.optional(PeopleOrderBy.pipe(T.Query())),
    orderDirection: S.optional(OrderDirection.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/personnel/people",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListPersonnelPeopleRequest",
}) as any as S.Schema<ListPersonnelPeopleRequest>;

export interface PersonnelPersonGroupsItem {
  name: string;
}
export const PersonnelPersonGroupsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
  }),
).annotate({
  identifier: "PersonnelPersonGroupsItem",
}) as any as S.Schema<PersonnelPersonGroupsItem>;

/** Groups (roles) the person belongs to. Present only when the column is in the response; use an empty array when there are no groups. */
export type PersonnelPersonGroupsList = Array<PersonnelPersonGroupsItem>;
export const PersonnelPersonGroupsList = /*@__PURE__*/ S.Array(
  PersonnelPersonGroupsItem,
) as any as S.Schema<PersonnelPersonGroupsList>;

/** Source information for a person's data. Full Audit View only - omitted in Controlled Audit View. */
export interface PeopleSourceInfo {
  /** Main source of user data. */
  mainSource: string;
  /** Supplementary source, or null if none. */
  supplementarySource: string | null;
}
export const PeopleSourceInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    mainSource: S.String,
    supplementarySource: S.NullOr(S.String),
  }),
).annotate({
  identifier: "PeopleSourceInfo",
}) as any as S.Schema<PeopleSourceInfo>;

/** Overall user task status. */
export type PeopleTaskStatus =
  | "NO_SECURITY_TASKS"
  | "OFFBOARDING_COMPLETE"
  | "OFFBOARDING_DUE_SOON"
  | "OFFBOARDING_OVERDUE"
  | "PAUSED"
  | "SECURITY_TASKS_COMPLETE"
  | "SECURITY_TASKS_DUE_SOON"
  | "SECURITY_TASKS_OVERDUE";
export const PeopleTaskStatus = /*@__PURE__*/ S.String;

/** Overall task status information for a person. Full Audit View only - omitted in Controlled Audit View. */
export interface PeopleTaskStatusInfo {
  /** Overall task status. */
  status: PeopleTaskStatus;
  /** Due date for the earliest incomplete task in ISO 8601 format, or null if none. */
  dueDate: string | null;
  /** Completion date when all tasks are complete in ISO 8601 format, or null if incomplete. */
  completionDate: string | null;
  /** Count of completed tasks, or null if not available. */
  numTasksCompleted: number | null;
  /** Count of overdue tasks, or null if not available. */
  numTasksOverdue: number | null;
  /** Count of tasks due soon, or null if not available. */
  numTasksDueSoon: number | null;
}
export const PeopleTaskStatusInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    status: PeopleTaskStatus,
    dueDate: S.NullOr(S.String),
    completionDate: S.NullOr(S.String),
    numTasksCompleted: S.NullOr(S.Number),
    numTasksOverdue: S.NullOr(S.Number),
    numTasksDueSoon: S.NullOr(S.Number),
  }),
).annotate({
  identifier: "PeopleTaskStatusInfo",
}) as any as S.Schema<PeopleTaskStatusInfo>;

/** Security task status for individual task categories. */
export type PeopleSecurityTaskStatus =
  | "NO_SECURITY_TASKS"
  | "SECURITY_TASKS_COMPLETE"
  | "SECURITY_TASKS_DUE_SOON"
  | "SECURITY_TASKS_OVERDUE";
export const PeopleSecurityTaskStatus = /*@__PURE__*/ S.String;

/** Security task details for a specific task category. Full Audit View only - omitted in Controlled Audit View. */
export interface PeopleSecurityTaskDetails {
  /** Task status for this category. */
  status: PeopleSecurityTaskStatus;
  /** Due date for incomplete task in ISO 8601 format, or null if none. */
  dueDate: string | null;
  /** Completion date for completed task in ISO 8601 format, or null if incomplete. */
  completionDate: string | null;
}
export const PeopleSecurityTaskDetails = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    status: PeopleSecurityTaskStatus,
    dueDate: S.NullOr(S.String),
    completionDate: S.NullOr(S.String),
  }),
).annotate({
  identifier: "PeopleSecurityTaskDetails",
}) as any as S.Schema<PeopleSecurityTaskDetails>;

/** Personnel person data. `id`, `name`, and `email` are required. All other fields are optional to support customizable field visibility. Omitted keys mean the column is not in the response; `null` means the column is present but empty. */
export interface PersonnelPerson {
  /** User ID - always present. */
  id: string;
  /** Display name of the person - always present. */
  name: string;
  /** Email address of the person - always present. */
  email: string;
  /** Job title from HR system, or null if not available. */
  jobTitle?: string | null;
  /** Employment status, or null if not available. */
  employmentStatus?: PeopleEmploymentStatus | null;
  /** Start date in ISO 8601 format (YYYY-MM-DD), or null if not available. Timezone depends on source: UTC if from HR system, customer timezone if manually entered. */
  startDate?: string | null;
  /** End date in ISO 8601 format (YYYY-MM-DD), or null if currently employed. */
  endDate?: string | null;
  /** Groups (roles) the person belongs to. Present only when the column is in the response; use an empty array when there are no groups. */
  groups?: PersonnelPersonGroupsList;
  /** Source of user data, or null if not available. Full Audit View only - omitted in Controlled Audit View. */
  source?: PeopleSourceInfo | null;
  /** Overall task status information, or null if not available. Full Audit View only - omitted in Controlled Audit View. */
  taskStatus?: PeopleTaskStatusInfo | null;
  /** Last date user was sent an onboarding reminder email in ISO 8601 format, or null if never reminded. Full Audit View only - omitted in Controlled Audit View. */
  lastReminded?: string | null;
  /** Policy acceptance task details, or null if not available. Full Audit View only - omitted in Controlled Audit View. */
  policies?: PeopleSecurityTaskDetails | null;
  /** Security training task details, or null if not available. Full Audit View only - omitted in Controlled Audit View. */
  trainings?: PeopleSecurityTaskDetails | null;
  /** Background check task details, or null if not available. Full Audit View only - omitted in Controlled Audit View. */
  backgroundChecks?: PeopleSecurityTaskDetails | null;
  /** Device monitoring task details, or null if not available. Full Audit View only - omitted in Controlled Audit View. */
  deviceMonitoring?: PeopleSecurityTaskDetails | null;
  /** Custom onboarding task details, or null if not available. Full Audit View only - omitted in Controlled Audit View. */
  customOnboarding?: PeopleSecurityTaskDetails | null;
  /** Custom offboarding task details, or null if not available. Full Audit View only - omitted in Controlled Audit View. */
  customOffboarding?: PeopleSecurityTaskDetails | null;
}
export const PersonnelPerson = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    email: S.String,
    jobTitle: S.optional(S.NullOr(S.String)),
    employmentStatus: S.optional(S.NullOr(PeopleEmploymentStatus)),
    startDate: S.optional(S.NullOr(S.String)),
    endDate: S.optional(S.NullOr(S.String)),
    groups: S.optional(PersonnelPersonGroupsList),
    source: S.optional(S.NullOr(PeopleSourceInfo)),
    taskStatus: S.optional(S.NullOr(PeopleTaskStatusInfo)),
    lastReminded: S.optional(S.NullOr(S.String)),
    policies: S.optional(S.NullOr(PeopleSecurityTaskDetails)),
    trainings: S.optional(S.NullOr(PeopleSecurityTaskDetails)),
    backgroundChecks: S.optional(S.NullOr(PeopleSecurityTaskDetails)),
    deviceMonitoring: S.optional(S.NullOr(PeopleSecurityTaskDetails)),
    customOnboarding: S.optional(S.NullOr(PeopleSecurityTaskDetails)),
    customOffboarding: S.optional(S.NullOr(PeopleSecurityTaskDetails)),
  }),
).annotate({
  identifier: "PersonnelPerson",
}) as any as S.Schema<PersonnelPerson>;

export type PaginatedResponsePersonnelPersonResultsDataList =
  Array<PersonnelPerson>;
export const PaginatedResponsePersonnelPersonResultsDataList =
  /*@__PURE__*/ S.Array(
    PersonnelPerson,
  ) as any as S.Schema<PaginatedResponsePersonnelPersonResultsDataList>;

export interface PaginatedResponsePersonnelPersonResults {
  data: PaginatedResponsePersonnelPersonResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponsePersonnelPersonResults = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      data: PaginatedResponsePersonnelPersonResultsDataList,
      pageInfo: PageInfo,
    }),
).annotate({
  identifier: "PaginatedResponsePersonnelPersonResults",
}) as any as S.Schema<PaginatedResponsePersonnelPersonResults>;

export interface PaginatedResponsePersonnelPerson {
  results: PaginatedResponsePersonnelPersonResults;
}
export const PaginatedResponsePersonnelPerson = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponsePersonnelPersonResults,
  }),
).annotate({
  identifier: "PaginatedResponsePersonnelPerson",
}) as any as S.Schema<PaginatedResponsePersonnelPerson>;

export interface ListRiskSnapshotsRequest {
  /** The audit ID */
  auditId: string;
  /** Maximum number of results per page (1-100, default 10) */
  pageSize?: number;
  /** Pagination cursor from previous response */
  pageCursor?: string;
}
export const ListRiskSnapshotsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/risks/snapshots",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListRiskSnapshotsRequest",
}) as any as S.Schema<ListRiskSnapshotsRequest>;

/** A risk assessment snapshot available for an audit. Risk snapshots capture the state of an organization's risk register at a point in time. Use the snapshot `id` with the `/audits/{auditId}/risks/{snapshotId}` endpoint to retrieve the risk scenarios within a snapshot. */
export interface RiskSnapshot {
  /** Unique identifier for the risk snapshot. */
  id: string;
  /** The date and time when this risk snapshot was created. */
  createdAt: string;
}
export const RiskSnapshot = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    createdAt: S.String,
  }),
).annotate({ identifier: "RiskSnapshot" }) as any as S.Schema<RiskSnapshot>;

export type PaginatedResponseRiskSnapshotResultsDataList = Array<RiskSnapshot>;
export const PaginatedResponseRiskSnapshotResultsDataList =
  /*@__PURE__*/ S.Array(
    RiskSnapshot,
  ) as any as S.Schema<PaginatedResponseRiskSnapshotResultsDataList>;

export interface PaginatedResponseRiskSnapshotResults {
  data: PaginatedResponseRiskSnapshotResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseRiskSnapshotResults = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      data: PaginatedResponseRiskSnapshotResultsDataList,
      pageInfo: PageInfo,
    }),
).annotate({
  identifier: "PaginatedResponseRiskSnapshotResults",
}) as any as S.Schema<PaginatedResponseRiskSnapshotResults>;

export interface PaginatedResponseRiskSnapshot {
  results: PaginatedResponseRiskSnapshotResults;
}
export const PaginatedResponseRiskSnapshot = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseRiskSnapshotResults,
  }),
).annotate({
  identifier: "PaginatedResponseRiskSnapshot",
}) as any as S.Schema<PaginatedResponseRiskSnapshot>;

export type AuditVendorStatus = "ACTIVE" | "ARCHIVED" | "IN_PROCUREMENT";
export const AuditVendorStatus = /*@__PURE__*/ S.String;

export type ListVendorsRequestVendorStatusesMatchesAnyList = Array<
  AuditVendorStatus | (string & {})
>;
export const ListVendorsRequestVendorStatusesMatchesAnyList =
  /*@__PURE__*/ S.Array(
    AuditVendorStatus,
  ) as any as S.Schema<ListVendorsRequestVendorStatusesMatchesAnyList>;

export type AuditVendorRiskLevel =
  | "CRITICAL"
  | "HIGH"
  | "MEDIUM"
  | "LOW"
  | "UNSCORED";
export const AuditVendorRiskLevel = /*@__PURE__*/ S.String;

export type ListVendorsRequestInherentRiskMatchesAnyList = Array<
  AuditVendorRiskLevel | (string & {})
>;
export const ListVendorsRequestInherentRiskMatchesAnyList =
  /*@__PURE__*/ S.Array(
    AuditVendorRiskLevel,
  ) as any as S.Schema<ListVendorsRequestInherentRiskMatchesAnyList>;

export type VendorOrderBy = "name" | "inherentRisk";
export const VendorOrderBy = /*@__PURE__*/ S.String;

export interface ListVendorsRequest {
  /** The audit ID */
  auditId: string;
  /** Maximum number of results per page (1-100, default 10) */
  pageSize?: number;
  /** Pagination cursor from previous response */
  pageCursor?: string;
  /** Search term for filtering by vendor name */
  search?: string;
  /** Filter vendors by status values */
  vendorStatusesMatchesAny?: ListVendorsRequestVendorStatusesMatchesAnyList;
  /** Filter vendors by inherent risk level values */
  inherentRiskMatchesAny?: ListVendorsRequestInherentRiskMatchesAnyList;
  /** Field to sort results by. Allowed: "name", "inherentRisk". Default: "name" */
  orderBy?: VendorOrderBy | (string & {});
  /** Sort direction: "asc" or "desc". Default: "asc" */
  orderDirection?: OrderDirection | (string & {});
}
export const ListVendorsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    pageSize: S.optional(S.Number.pipe(T.Query())),
    pageCursor: S.optional(S.String.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
    vendorStatusesMatchesAny: S.optional(
      ListVendorsRequestVendorStatusesMatchesAnyList.pipe(T.Query()),
    ),
    inherentRiskMatchesAny: S.optional(
      ListVendorsRequestInherentRiskMatchesAnyList.pipe(T.Query()),
    ),
    orderBy: S.optional(VendorOrderBy.pipe(T.Query())),
    orderDirection: S.optional(OrderDirection.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/audits/{auditId}/managed-vendors",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListVendorsRequest",
}) as any as S.Schema<ListVendorsRequest>;

/** BAA/DPA document completion counts, or null if no such documents are required. Full Audit View only - omitted in Controlled Audit View. */
export interface AuditVendorDataAgreements {
  total: number;
  complete: number;
}
export const AuditVendorDataAgreements = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    total: S.Number,
    complete: S.Number,
  }),
).annotate({
  identifier: "AuditVendorDataAgreements",
}) as any as S.Schema<AuditVendorDataAgreements>;

/** Managed vendor row returned by the auditor managed-vendors API. Only `id` and `name` are required. All other fields are optional to support controlled audits where only approved columns are returned and customizable field visibility. */
export interface AuditVendor {
  /** Vendor record ID. */
  id: string;
  /** Vendor name. */
  name: string;
  /** Inherent risk level of the vendor. */
  inherentRisk?: AuditVendorRiskLevel;
  /** Vendor status. */
  status?: AuditVendorStatus;
  /** Number of findings associated with the vendor. */
  findings?: number;
  /** Date when the last security review was completed, or null if none. */
  lastReviewedCompleted?: string | null;
  /** Date when the next security review is due, or null if not scheduled. */
  nextSecurityReviewDueDate?: string | null;
  /** Vendor category (e.g. cloud provider, SaaS). */
  vendorCategory?: string | null;
  /** Residual risk level of the vendor. Full Audit View only - omitted in Controlled Audit View. */
  residualRisk?: AuditVendorRiskLevel | null;
  /** BAA/DPA document completion counts, or null if no such documents are required. Full Audit View only - omitted in Controlled Audit View. */
  dataAgreements?: AuditVendorDataAgreements | null;
}
export const AuditVendor = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    inherentRisk: S.optional(AuditVendorRiskLevel),
    status: S.optional(AuditVendorStatus),
    findings: S.optional(S.Number),
    lastReviewedCompleted: S.optional(S.NullOr(S.String)),
    nextSecurityReviewDueDate: S.optional(S.NullOr(S.String)),
    vendorCategory: S.optional(S.NullOr(S.String)),
    residualRisk: S.optional(S.NullOr(AuditVendorRiskLevel)),
    dataAgreements: S.optional(S.NullOr(AuditVendorDataAgreements)),
  }),
).annotate({ identifier: "AuditVendor" }) as any as S.Schema<AuditVendor>;

export type PaginatedResponseAuditVendorResultsDataList = Array<AuditVendor>;
export const PaginatedResponseAuditVendorResultsDataList =
  /*@__PURE__*/ S.Array(
    AuditVendor,
  ) as any as S.Schema<PaginatedResponseAuditVendorResultsDataList>;

export interface PaginatedResponseAuditVendorResults {
  data: PaginatedResponseAuditVendorResultsDataList;
  pageInfo: PageInfo;
}
export const PaginatedResponseAuditVendorResults = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    data: PaginatedResponseAuditVendorResultsDataList,
    pageInfo: PageInfo,
  }),
).annotate({
  identifier: "PaginatedResponseAuditVendorResults",
}) as any as S.Schema<PaginatedResponseAuditVendorResults>;

export interface PaginatedResponseAuditVendor {
  results: PaginatedResponseAuditVendorResults;
}
export const PaginatedResponseAuditVendor = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    results: PaginatedResponseAuditVendorResults,
  }),
).annotate({
  identifier: "PaginatedResponseAuditVendor",
}) as any as S.Schema<PaginatedResponseAuditVendor>;

export interface ShareInformationRequestListRequest {
  auditId: string;
}
export const ShareInformationRequestListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/audits/{auditId}/share-information-request-list",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ShareInformationRequestListRequest",
}) as any as S.Schema<ShareInformationRequestListRequest>;

export type AuditorEnabledStateTransition =
  | "ACCEPT"
  | "MARK_APPLICABLE"
  | "FLAG"
  | "MARK_NA"
  | "READY_FOR_AUDIT";
export const AuditorEnabledStateTransition = /*@__PURE__*/ S.String;

export interface UpdateAuditEvidenceRequestStatusUpdate {
  /** Email of the auditor who changed the state */
  auditorEmail: string;
  /** State change for audit evidence */
  stateTransition: AuditorEnabledStateTransition | (string & {});
}
export const UpdateAuditEvidenceRequestStatusUpdate = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      auditorEmail: S.String,
      stateTransition: AuditorEnabledStateTransition,
    }),
).annotate({
  identifier: "UpdateAuditEvidenceRequestStatusUpdate",
}) as any as S.Schema<UpdateAuditEvidenceRequestStatusUpdate>;

export interface UpdateAuditEvidenceRequest {
  auditId: string;
  auditEvidenceId: string;
  statusUpdate?: UpdateAuditEvidenceRequestStatusUpdate;
}
export const UpdateAuditEvidenceRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    auditEvidenceId: S.String.pipe(T.Label()),
    statusUpdate: S.optional(UpdateAuditEvidenceRequestStatusUpdate),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/audits/{auditId}/evidence/{auditEvidenceId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateAuditEvidenceRequest",
}) as any as S.Schema<UpdateAuditEvidenceRequest>;

export interface UpdateCommentForControlRequest {
  auditId: string;
  controlId: string;
  commentId: string;
  /** The text content of the comment. Must be at least 1 character. Can include questions, clarifications, or explanations related to the control. */
  text: string;
  /** Email address of the comment author. Must match an existing Vanta user who belongs to the audit firm making the API request. This email uniquely identifies the author across systems. */
  email: string;
}
export const UpdateCommentForControlRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    controlId: S.String.pipe(T.Label()),
    commentId: S.String.pipe(T.Label()),
    text: S.String,
    email: S.String,
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/audits/{auditId}/controls/{controlId}/comments/{commentId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateCommentForControlRequest",
}) as any as S.Schema<UpdateCommentForControlRequest>;

export interface UpdateCommentForInformationRequestRequest {
  auditId: string;
  requestId: string;
  commentId: string;
  /** The text content of the comment. Must be at least 1 character. Can include questions, clarifications, or explanations related to the information request. */
  text: string;
  /** Email address of the comment author. Must match an existing Vanta user who belongs to the audit firm making the API request. This email uniquely identifies the author across systems. */
  email: string;
}
export const UpdateCommentForInformationRequestRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      auditId: S.String.pipe(T.Label()),
      requestId: S.String.pipe(T.Label()),
      commentId: S.String.pipe(T.Label()),
      text: S.String,
      email: S.String,
    }).pipe(
      T.Http({
        method: "PATCH",
        uri: "/audits/{auditId}/information-requests/{requestId}/comments/{commentId}",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "UpdateCommentForInformationRequestRequest",
  }) as any as S.Schema<UpdateCommentForInformationRequestRequest>;

/** The framework codes this request addresses. An empty array if no framework codes are associated. */
export type UpdateInformationRequestRequestFrameworkCodesList = Array<string>;
export const UpdateInformationRequestRequestFrameworkCodesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateInformationRequestRequestFrameworkCodesList>;

/** Control IDs to link directly to this request, beyond those automatically mapped from framework codes. Replaces the existing set: pass the complete desired list, an empty array to clear all direct control links, or omit to leave them unchanged. Each must be the `id` of an existing control in the customer's organization (the identifier returned by the controls endpoints). The request is rejected if any ID does not match a control. */
export type UpdateInformationRequestRequestAdditionalControlIdsList =
  Array<string>;
export const UpdateInformationRequestRequestAdditionalControlIdsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateInformationRequestRequestAdditionalControlIdsList>;

export interface UpdateInformationRequestRequest {
  auditId: string;
  requestId: string;
  /** The framework codes this request addresses. An empty array if no framework codes are associated. */
  frameworkCodes?: UpdateInformationRequestRequestFrameworkCodesList;
  /** Detailed description of what evidence is needed. */
  description?: string | null;
  /** Deadline for fulfilling this request. Null if no deadline. Format: ISO 8601 UTC timestamp. */
  dueDate?: string | null;
  /** Start date of the audit period. Evidence before this date may not be accepted. Null if not restricted. Format: ISO 8601 UTC timestamp. */
  evidenceCaptureDate?: string | null;
  /** Scope of evidence required. */
  requestType?: InformationRequestType | (string & {});
  /** Short, descriptive title. */
  title?: string;
  /** How frequently this request recurs. */
  cadence?: InformationRequestCadence | (string & {});
  /** Control IDs to link directly to this request, beyond those automatically mapped from framework codes. Replaces the existing set: pass the complete desired list, an empty array to clear all direct control links, or omit to leave them unchanged. Each must be the `id` of an existing control in the customer's organization (the identifier returned by the controls endpoints). The request is rejected if any ID does not match a control. */
  additionalControlIds?: UpdateInformationRequestRequestAdditionalControlIdsList;
}
export const UpdateInformationRequestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    requestId: S.String.pipe(T.Label()),
    frameworkCodes: S.optional(
      UpdateInformationRequestRequestFrameworkCodesList,
    ),
    description: S.optional(S.NullOr(S.String)),
    dueDate: S.optional(S.NullOr(S.String)),
    evidenceCaptureDate: S.optional(S.NullOr(S.String)),
    requestType: S.optional(InformationRequestType),
    title: S.optional(S.String),
    cadence: S.optional(InformationRequestCadence),
    additionalControlIds: S.optional(
      UpdateInformationRequestRequestAdditionalControlIdsList,
    ),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/audits/{auditId}/information-requests/{requestId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateInformationRequestRequest",
}) as any as S.Schema<UpdateInformationRequestRequest>;

export interface UpsertAssessmentForControlRequest {
  auditId: string;
  controlId: string;
  /** The program segment to assess. Required when the audit has more than one program segment. Optional on a single-program audit (the only program is used). Must be a program segment on the audit; system segments are rejected. */
  segmentId?: string;
  /** The assessment state to record. Must be one of the states valid for the segment's framework; the request is rejected otherwise. */
  assessmentState: AuditControlAssessmentState | (string & {});
  /** Free-text reasoning for the assessment. Required, and at most 5000 characters. */
  justification: string;
  /** Email of the auditor performing the assessment. Must match an existing Vanta user who belongs to the audit firm making the API request. */
  auditorEmail: string;
}
export const UpsertAssessmentForControlRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auditId: S.String.pipe(T.Label()),
    controlId: S.String.pipe(T.Label()),
    segmentId: S.optional(S.String),
    assessmentState: AuditControlAssessmentState,
    justification: S.String,
    auditorEmail: S.String,
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/audits/{auditId}/controls/{controlId}/assessment",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpsertAssessmentForControlRequest",
}) as any as S.Schema<UpsertAssessmentForControlRequest>;

/** A control's auditor assessment, as persisted. Returned by the assessment write endpoint so the caller sees exactly what was recorded. */
export interface AuditorControlAssessment {
  /** The control this assessment belongs to. */
  controlId: string;
  /** The program segment this assessment was written to. */
  segmentId: string;
  /** The recorded assessment state. */
  assessmentState: AuditControlAssessmentState;
  /** The recorded justification. */
  justification: string;
}
export const AuditorControlAssessment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    controlId: S.String,
    segmentId: S.String,
    assessmentState: AuditControlAssessmentState,
    justification: S.String,
  }),
).annotate({
  identifier: "AuditorControlAssessment",
}) as any as S.Schema<AuditorControlAssessment>;

export type AcceptInformationRequestEvidenceError = VantaOpError;
/** Accept evidence for an information request Accepts evidence for an information request, confirming that all submitted evidence meets audit requirements. This action changes the request's approvalStatus to an approved state and creates an activity log entry. Acceptance workflow: 1. Auditor reviews submitted evidence 2. If evidence is satisfactory, auditor calls this endpoint 3. Request status changes to approved state and is considered complete for this audit cycle Use this endpoint when: - All required evidence has been submitted - Evidence quality meets audit standards - Evidence addresses all specified framework codes - No additional information is needed Rate limit: 50 requests / minute. */
export const acceptInformationRequestEvidence: API.OperationMethod<
  AcceptInformationRequestEvidenceRequest,
  InformationRequest,
  AcceptInformationRequestEvidenceError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AcceptInformationRequestEvidenceRequest,
  output: InformationRequest,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type CreateAuditorError = VantaOpError;
/** Create an auditor Create an auditor in Vanta. Rate limit: 10 requests / minute. */
export const createAuditor: API.OperationMethod<
  CreateAuditorRequest,
  Auditor,
  CreateAuditorError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateAuditorRequest,
  output: Auditor,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type CreateCommentForAuditEvidenceError = VantaOpError;
/** Create a comment for audit evidence Create a comment in Vanta for a piece of evidence. Rate limit: 25 requests / minute. */
export const createCommentForAuditEvidence: API.OperationMethod<
  CreateCommentForAuditEvidenceRequest,
  Comment,
  CreateCommentForAuditEvidenceError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateCommentForAuditEvidenceRequest,
  output: Comment,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type CreateCommentForControlError = VantaOpError;
/** Create a comment for a control within an audit Creates a new comment on a control within an IRL audit. The comment author must be an auditor in the audit firm making the request. The comment will be associated with the control and visible to all authorized users. Returns 404 when the control is not part of the audit. Rate limit: 50 requests / minute. */
export const createCommentForControl: API.OperationMethod<
  CreateCommentForControlRequest,
  AuditControlComment,
  CreateCommentForControlError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateCommentForControlRequest,
  output: AuditControlComment,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type CreateCommentForInformationRequestError = VantaOpError;
/** Create a comment for an information request Creates a new comment for an information request. The comment author must be an auditor in the audit firm making the request. The comment will be associated with the information request and visible to all authorized users. Rate limit: 50 requests / minute. */
export const createCommentForInformationRequest: API.OperationMethod<
  CreateCommentForInformationRequestRequest,
  InformationRequestComment,
  CreateCommentForInformationRequestError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateCommentForInformationRequestRequest,
  output: InformationRequestComment,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type CreateCustomControlError = VantaOpError;
/** Create a custom control for an audit Create a custom control for an audit. This endpoint supports classic audits only. Audits that use information request lists (IRL) are not supported and return a 422 error. Rate limit: 10 requests / minute. */
export const createCustomControl: API.OperationMethod<
  CreateCustomControlRequest,
  Control,
  CreateCustomControlError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateCustomControlRequest,
  output: Control,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type CreateCustomEvidenceRequestError = VantaOpError;
/** Create a custom evidence request for an audit Create a custom evidence request for an audit. Rate limit: 10 requests / minute. */
export const createCustomEvidenceRequest: API.OperationMethod<
  CreateCustomEvidenceRequestRequest,
  CustomEvidenceRequest,
  CreateCustomEvidenceRequestError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateCustomEvidenceRequestRequest,
  output: CustomEvidenceRequest,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type CreateInformationRequestError = VantaOpError;
/** Create a new information request Creates a new information request for an audit during audit setup or as requirements evolve. After creating all information requests, use POST /audits/{auditId}/share-information-request-list to make them visible to the customer organization. Until shared, requests remain in draft state visible only to auditors. New requests are created in an initial state indicating evidence is needed. The status progresses through the workflow: initial state → awaiting review → approved or flagged. Rate limit: 600 requests / minute. */
export const createInformationRequest: API.OperationMethod<
  CreateInformationRequestRequest,
  InformationRequest,
  CreateInformationRequestError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateInformationRequestRequest,
  output: InformationRequest,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type DeleteCommentForControlError = VantaOpError;
/** Delete a comment for a control within an audit Deletes an existing comment on a control. Only the original author of the comment can delete it. The author is identified by their email address, which must match the email of the user who created the comment. Rate limit: 10 requests / minute. */
export const deleteCommentForControl: API.OperationMethod<
  DeleteCommentForControlRequest,
  DeleteCommentForControlResponse,
  DeleteCommentForControlError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteCommentForControlRequest,
  output: DeleteCommentForControlResponse,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type DeleteCommentForInformationRequestError = VantaOpError;
/** Delete a comment for an information request Deletes an existing comment for an information request. Only the original author of the comment can delete it. The author is identified by their email address, which must match the email of the user who created the comment. Rate limit: 25 requests / minute. */
export const deleteCommentForInformationRequest: API.OperationMethod<
  DeleteCommentForInformationRequestRequest,
  DeleteCommentForInformationRequestResponse,
  DeleteCommentForInformationRequestError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteCommentForInformationRequestRequest,
  output: DeleteCommentForInformationRequestResponse,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type DeleteInformationRequestError = VantaOpError;
/** Delete an information request for an audit Deletes an information request for an audit. This performs a soft delete, marking the request as deleted (setting `deletionDate`) while preserving it in the system for audit history and compliance tracking. Soft deletion allows: - Maintaining complete audit trail of all requests ever created - Retrieving deleted requests via `changedSinceDate` for synchronization After deletion: - The request will not appear in normal list responses (without `changedSinceDate`) - The request's `deletionDate` field will be populated Rate limit: 50 requests / minute. */
export const deleteInformationRequest: API.OperationMethod<
  DeleteInformationRequestRequest,
  DeleteInformationRequestResponse,
  DeleteInformationRequestError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteInformationRequestRequest,
  output: DeleteInformationRequestResponse,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type DuplicateError = VantaOpError;
/** Duplicate an IRL audit Duplicates an existing IRL audit into a new audit engagement with the supplied displayName, audit dates, early access date, and auditor roster. Company, audit type, and framework are copied from the source audit and cannot be changed. Each email in `allowAuditorEmails` must match an active user in the authenticated audit firm's domain. Provision auditors via `POST /auditors` before referencing them here, or copy emails from `GET /audits/{sourceAuditId}` → `allowAuditorEmails` when duplicating with the same roster. Information requests are copied from the source audit. After duplication: - Requests with Vanta evidence will be pre-filled and marked as internal review. Review them before sharing with your customer. - Requests where evidence was not available or was uploaded externally will need evidence added manually. - Evidence capture dates and due dates can be modified after duplication. Audits with generated information requests can be duplicated only after their information requests have been created successfully. Rate limit: 10 requests / minute. */
export const duplicate: API.OperationMethod<
  DuplicateRequest,
  Audit,
  DuplicateError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DuplicateRequest,
  output: Audit,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type FlagInformationRequestEvidenceError = VantaOpError;
/** Flag evidence for an information request Flags evidence for an information request when it doesn't meet audit requirements, marking issues that need to be addressed before approval. This action changes the request's approvalStatus to a flagged state and creates an activity log entry. Flagging workflow: 1. Auditor reviews submitted evidence 2. If issues are found, auditor calls this endpoint with detailed reason 3. Request status changes to flagged state 4. Customer is notified and can see the reason in activity logs 5. Customer addresses issues and updates evidence 6. When ready, customer changes status back to awaiting review 7. Auditor reviews again and either flags again or accepts The `reason` field should clearly explain what's missing or incorrect so the customer knows exactly what to fix. This reason is visible to the customer and appears in the activity log. Rate limit: 50 requests / minute. */
export const flagInformationRequestEvidence: API.OperationMethod<
  FlagInformationRequestEvidenceRequest,
  InformationRequest,
  FlagInformationRequestEvidenceError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: FlagInformationRequestEvidenceRequest,
  output: InformationRequest,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type GetAuditError = VantaOpError;
/** Get audit by ID Returns a single audit by ID, scoped to the audit firm. To identify IRL (Information Request List) audits, check for the presence of the `auditorRequestListMetadata` field. This field is only present for IRL-based audits and will be `undefined` for standard audits. The response includes `segments`, the audit's scope. A live single-framework audit has one entry; a live multi-framework audit has one entry per in-scope framework (and business unit or system, when applicable). Soft-deleted audits return an empty list. The top-level `framework` field is deprecated; use `segments` for framework identity. Rate limit: 250 requests / minute. */
export const getAudit: API.OperationMethod<
  GetAuditRequest,
  Audit,
  GetAuditError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetAuditRequest,
  output: Audit,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type GetAuditEvidenceError = VantaOpError;
/** Get an audit evidence item by ID Retrieves a single classic audit evidence item by its ID, scoped to its audit. The response matches the entry `GET /audits/{auditId}/evidence` returns for the same item, so an evidence ID surfaced by a webhook can be resolved directly instead of paging the audit's full evidence list. Soft-deleted evidence (where `deletionDate !== null`) is included in the response. Clients should check `deletionDate` to determine whether the item has been deleted. This matches `GET /audits/{auditId}/evidence`, which supports `changedSinceDate` and returns soft-deleted evidence for delta sync. As on the list endpoint, `description` is null for deleted items. Rate limit: 250 requests / minute. */
export const getAuditEvidence: API.OperationMethod<
  GetAuditEvidenceRequest,
  Evidence,
  GetAuditEvidenceError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetAuditEvidenceRequest,
  output: Evidence,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type GetAuditEvidenceCommentError = VantaOpError;
/** Get an audit evidence comment by ID Retrieves a single comment on a classic audit evidence item by its ID. Soft-deleted comments (where `deletionDate !== null`) are included in the response. Clients should check `deletionDate` to determine whether the comment has been deleted. This matches `GET /audits/{auditId}/comments`, which supports `changedSinceDate` and returns soft-deleted comments for delta sync. Comments remain fetchable when the parent evidence item has been soft-deleted, so delayed webhook consumers can still resolve a comment ID after evidence deletion. Rate limit: 50 requests / minute. */
export const getAuditEvidenceComment: API.OperationMethod<
  GetAuditEvidenceCommentRequest,
  Comment,
  GetAuditEvidenceCommentError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetAuditEvidenceCommentRequest,
  output: Comment,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type GetCommentForInformationRequestError = VantaOpError;
/** Get an information request comment by ID Retrieves a single comment on an information request by its ID. Soft-deleted comments (where `deletionDate !== null`) are included in the response. Clients should check `deletionDate` to determine whether the comment has been deleted. This matches `GET /audits/{auditId}/information-requests/{requestId}/comments`, which supports `changedSinceDate` and returns soft-deleted comments for delta sync. Comments remain fetchable when the parent information request has been soft-deleted, so delayed webhook consumers can still resolve a comment ID after the request is deleted. Rate limit: 50 requests / minute. */
export const getCommentForInformationRequest: API.OperationMethod<
  GetCommentForInformationRequestRequest,
  InformationRequestComment,
  GetCommentForInformationRequestError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetCommentForInformationRequestRequest,
  output: InformationRequestComment,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type GetFrameworkCodesError = VantaOpError;
/** Get framework codes for an audit Retrieves all valid framework codes for the specified audit. This endpoint helps users discover which framework codes are available for creating and updating information requests for this audit. Use this endpoint to: - Discover available framework codes before creating information requests - Validate framework codes against the audit's framework - Get context about what framework codes are available for the audit type Rate limit: 50 requests / minute. */
export const getFrameworkCodes: API.OperationMethod<
  GetFrameworkCodesRequest,
  FrameworkCodes,
  GetFrameworkCodesError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetFrameworkCodesRequest,
  output: FrameworkCodes,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type GetInformationRequestError = VantaOpError;
/** Get an information request by ID Retrieves a single information request by its ID for an audit, allowing external audit management systems to fetch the latest state of a specific request without paginating through the full list. Soft-deleted records (where `deletionDate !== null`) are included in the response. Clients should check `deletionDate` to determine whether the request has been deleted. Rate limit: 50 requests / minute. */
export const getInformationRequest: API.OperationMethod<
  GetInformationRequestRequest,
  InformationRequest,
  GetInformationRequestError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetInformationRequestRequest,
  output: InformationRequest,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type GetInformationRequestEvidenceError = VantaOpError;
/** Get information request evidence by ID Retrieves a single evidence item attached to an information request by its ID. This endpoint always includes soft-deleted evidence (where `deletionDate !== null`), so an evidence ID surfaced by a `changedSinceDate` delta sync stays fetchable after the evidence is deleted. Clients should check the `deletionDate` field to identify and handle deleted records appropriately in their systems. Evidence is only resolvable while its information request exists. Once the request itself is deleted, this endpoint reports the request as not found — matching `GET /audits/{auditId}/information-requests/{requestId}/evidence`. Clients reconciling a deleted request should treat its evidence as gone with it. Evidence that the customer has not shared with the auditor is reported as not found, rather than distinguishing it from an ID that does not exist. Rate limit: 250 requests / minute. */
export const getInformationRequestEvidence: API.OperationMethod<
  GetInformationRequestEvidenceRequest,
  InformationRequestEvidence,
  GetInformationRequestEvidenceError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetInformationRequestEvidenceRequest,
  output: InformationRequestEvidence,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type GetInformationRequestTestSnapshotEvidenceDetailError = VantaOpError;
/** Get test snapshot detail for an evidence row Retrieves the rich detail for a single VANTA_TEST_SNAPSHOT evidence row attached to an information request. The response includes test-level metadata (description, integrations, SLA), the raw test data captured at snapshot time, and the resources that were excluded from the test (out-of-scope resources). For structured snapshots, the `rawTestData` array contains one row per resource the test ran against; each row carries `resourceId`, `resourceType`, and the raw JSON for that resource. For unstructured snapshots, the `rawTestData` array contains a single row with `resourceId` and `resourceType` set to `null` and `rawJson` containing the entire test-run JSON blob. The `outOfScopeResources` field lists resources excluded at the test level (customer-disabled) and the framework level (segment configuration). Empty exclusion groups are filtered out. The `apiRequests` array contains the HTTP requests captured during API introspection tests. Empty when the test does not perform API introspection. Rate limit: 10 requests / minute. */
export const getInformationRequestTestSnapshotEvidenceDetail: API.OperationMethod<
  GetInformationRequestTestSnapshotEvidenceDetailRequest,
  VantaTestSnapshotEvidenceDetail,
  GetInformationRequestTestSnapshotEvidenceDetailError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetInformationRequestTestSnapshotEvidenceDetailRequest,
  output: VantaTestSnapshotEvidenceDetail,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type GetOrganizationInformationError = VantaOpError;
/** Get organization information for an audit Retrieves organization information for an audit. This endpoint returns a single record containing the organization's business information visible to auditors during an audit engagement. Sorting and pagination are not applicable. Rate limit: 10 requests / minute. */
export const getOrganizationInformation: API.OperationMethod<
  GetOrganizationInformationRequest,
  AuditOrganizationInformation,
  GetOrganizationInformationError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetOrganizationInformationRequest,
  output: AuditOrganizationInformation,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type GetOrganizationNotificationsError = VantaOpError;
/** Get organization notification settings for an audit Retrieves organization notification settings for an audit. This endpoint returns a single record containing the auditee organization's notification configuration — schedule, personnel reminder settings, and external notification subscriptions (Compliance, Vendors, Access Reviews, Trust Center). The response is a single aggregate object per domain. Sorting and pagination are not applicable. Under a controlled audit view (TRIMMED_DOWN), only CAV-approved fields are included. Rate limit: 10 requests / minute. */
export const getOrganizationNotifications: API.OperationMethod<
  GetOrganizationNotificationsRequest,
  AuditOrganizationNotifications,
  GetOrganizationNotificationsError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetOrganizationNotificationsRequest,
  output: AuditOrganizationNotifications,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListAccountAccessServicesError = VantaOpError;
/** List account access services for an audit Retrieves connected account access services for an audit. Returns the list of identity providers and access integrations (such as Okta, Azure AD, Google Workspace, AWS IAM) that are connected to the organization and provide account access data for personnel. These integrations are used to verify user access and identity management during an audit engagement. Uses cursor-based pagination. To paginate: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` 3. Use `results.pageInfo.endCursor` as `pageCursor` for next request Results are returned in connection order. Sort order is not guaranteed and cannot be customized via query parameters. Returns 422 when the audit does not have exactly one program segment (multi-framework audits are not supported on this endpoint). Rate limit: 10 requests / minute. */
export const listAccountAccessServices: API.OperationMethod<
  ListAccountAccessServicesRequest,
  PaginatedResponseAccountAccessService,
  ListAccountAccessServicesError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListAccountAccessServicesRequest,
  output: PaginatedResponseAccountAccessService,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListAuditCommentsError = VantaOpError;
/** List audit comments Returns a paginated list of comments for an audit. Rate limit: 250 requests / minute. */
export const listAuditComments: API.OperationMethod<
  ListAuditCommentsRequest,
  PaginatedResponseComment,
  ListAuditCommentsError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListAuditCommentsRequest,
  output: PaginatedResponseComment,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListAuditControlsError = VantaOpError;
/** List audit controls Returns a paginated list of controls for an audit. Rate limit: 250 requests / minute. */
export const listAuditControls: API.OperationMethod<
  ListAuditControlsRequest,
  PaginatedResponseAuditorControl,
  ListAuditControlsError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListAuditControlsRequest,
  output: PaginatedResponseAuditorControl,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListAuditEvidenceError = VantaOpError;
/** List audit evidence Returns a paginated list of evidence for an audit. Rate limit: 250 requests / minute. */
export const listAuditEvidence: API.OperationMethod<
  ListAuditEvidenceRequest,
  PaginatedResponseEvidence,
  ListAuditEvidenceError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListAuditEvidenceRequest,
  output: PaginatedResponseEvidence,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListAuditEvidenceUrlsError = VantaOpError;
/** List audit evidence url Returns a paginated list of evidence urls for an audit. This endpoint should be called whenever an evidence is created or has a statusUpdatedAt field that is more recent than the most recent polling event. Evidence must be in one of the following states to retrieve URLs: "Ready for audit", "Accepted", "Flagged", or "NA". Returns 422 when the audit does not have exactly one program segment (multi-framework audits are not supported on this endpoint). Rate limit: 600 requests / minute. */
export const listAuditEvidenceUrls: API.OperationMethod<
  ListAuditEvidenceUrlsRequest,
  PaginatedResponseEvidenceUrl,
  ListAuditEvidenceUrlsError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListAuditEvidenceUrlsRequest,
  output: PaginatedResponseEvidenceUrl,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListAuditIssuesError = VantaOpError;
/** List snapshotted issues for an audit Retrieves a list of all issues that have been shared with an audit. The issues returned are immutable, point-in-time snapshots; there may be duplicates of issues that have been snapshotted at different times. The GET /audits/{auditId}/issues/snapshots endpoint can be used to retrieve metadata about the snapshots that issues belong to. Issues represent compliance findings from a variety of sources that need to be tracked and remediated. Supports filtering by: - `search`: full text search across issue title and description - `snapshotId`: filtering to a specific snapshot or snapshots, which represent point-in-time captures of issues. Use the GET /audits/{auditId}/issues/snapshots endpoint to retrieve snapshot IDs and metadata. - `createdAfterDate` / `createdBeforeDate`: filter to issues created within a date range (inclusive) - `detectedAfterDate` / `detectedBeforeDate`: filter to issues detected within a date range (inclusive) Results are sorted by issue creation date in descending order (newest first) by default. Use `orderBy` and `orderDirection` to customize sorting. Sort parameters must remain consistent across paginated requests. Uses cursor-based pagination. To paginate: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` 3. Use `results.pageInfo.endCursor` as `pageCursor` for next request Rate limit: 10 requests / minute. */
export const listAuditIssues: API.OperationMethod<
  ListAuditIssuesRequest,
  PaginatedResponseIssueSnapshotItem,
  ListAuditIssuesError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListAuditIssuesRequest,
  output: PaginatedResponseIssueSnapshotItem,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListAuditRisksError = VantaOpError;
/** List risks for an audit Retrieves risk population data for an audit. This endpoint provides access to the risk records visible to auditors during an audit engagement. Risk data is scoped to a specific risk assessment snapshot identified by the `snapshotId` parameter. Only Controlled Audit View (CAV) audits are supported. Full Audit View audits are rejected with 403. Supports filtering by: - `search`: Searches risk scenario descriptions (case-insensitive) Results are sorted by identified date (newest first) by default. Use `orderBy` and `orderDirection` to customize sorting. Sort parameters must remain consistent across paginated requests. Uses cursor-based pagination. To paginate: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` 3. Use `results.pageInfo.endCursor` as `pageCursor` for next request Rate limit: 10 requests / minute. */
export const listAuditRisks: API.OperationMethod<
  ListAuditRisksRequest,
  PaginatedResponseAuditRisk,
  ListAuditRisksError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListAuditRisksRequest,
  output: PaginatedResponseAuditRisk,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListAuditsError = VantaOpError;
/** List audits Returns a paginated list of audits scoped to the audit firm. To identify IRL (Information Request List) audits, check for the presence of the `auditorRequestListMetadata` field. This field is only present for IRL-based audits and will be `undefined` for standard audits. Each audit includes `segments`, the audit's scope. A live single-framework audit has one entry; a live multi-framework audit has one entry per in-scope framework (and business unit or system, when applicable). Soft-deleted audits return an empty list. The top-level `framework` field is deprecated; use `segments` for framework identity. Rate limit: 250 requests / minute. */
export const listAudits: API.OperationMethod<
  ListAuditsRequest,
  PaginatedResponseAudit,
  ListAuditsError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListAuditsRequest,
  output: PaginatedResponseAudit,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListAuditSnapshotsError = VantaOpError;
/** List snapshotted issues for an audit Retrieves a list of snapshots that have been shared with an audit. The snapshots returned contain metadata about point-in-time captures of issues for an audit. This data can be used to filter down the list of issues to specific snapshots when querying the GET /audits/{auditId}/issues/items endpoint. Supports filtering by: - `search`: full text search across snapshot title and description Results are sorted by snapshot creation date in descending order (newest first). Uses cursor-based pagination. To paginate: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` 3. Use `results.pageInfo.endCursor` as `pageCursor` for next request Rate limit: 10 requests / minute. */
export const listAuditSnapshots: API.OperationMethod<
  ListAuditSnapshotsRequest,
  PaginatedResponseIssueSnapshotMetadata,
  ListAuditSnapshotsError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListAuditSnapshotsRequest,
  output: PaginatedResponseIssueSnapshotMetadata,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListCodeChangesError = VantaOpError;
/** List code changes for an audit Retrieves code changes population data for an audit. This endpoint provides access to code change records (pull requests) visible to auditors during an audit engagement. Supports filtering by: - `search`: Searches code change titles and repository names (case-insensitive) - `sourcesMatchesAny`: Filters by version control source (accepted values: github, gitlab, bitbucket, azuredevops) - `closedAfterDate` / `closedBeforeDate`: Filters by the closed date range Uses cursor-based pagination. To paginate: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` 3. Use `results.pageInfo.endCursor` as `pageCursor` for next request Results are sorted by closed date (newest first). This sort order is fixed and cannot be customized via query parameters. Returns 422 when the audit does not have exactly one program segment (multi-framework audits are not supported on this endpoint). Rate limit: 10 requests / minute. */
export const listCodeChanges: API.OperationMethod<
  ListCodeChangesRequest,
  PaginatedResponseCodeChange,
  ListCodeChangesError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListCodeChangesRequest,
  output: PaginatedResponseCodeChange,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListCommentsForControlError = VantaOpError;
/** List comments for a control within an audit Retrieves a paginated list of comments on a control within an IRL audit, enabling auditors to view collaboration history on the control. This endpoint always includes soft-deleted records (where `deletionDate !== null`). Clients should check the `deletionDate` field to identify and handle deleted records appropriately in their systems. This endpoint supports delta synchronization via the `changedSinceDate` parameter, allowing efficient polling for changes without retrieving the entire dataset. Returns 404 when the control is not part of the audit. Pagination usage: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` to see if more data exists 3. If true, use `results.pageInfo.endCursor` as `pageCursor` in next request 4. Repeat until `hasNextPage` is false Delta sync usage: 1. Store the timestamp of your last sync 2. Pass that timestamp as `changedSinceDate` 3. Only comments created, modified, or deleted since that timestamp are returned 4. Process updates, including soft-deletes (deletionDate !== null) 5. Update your last sync timestamp to the current time Rate limit: 50 requests / minute. */
export const listCommentsForControl: API.OperationMethod<
  ListCommentsForControlRequest,
  PaginatedResponseAuditControlComment,
  ListCommentsForControlError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListCommentsForControlRequest,
  output: PaginatedResponseAuditControlComment,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListCommentsForInformationRequestError = VantaOpError;
/** List comments for an information request Retrieves a paginated list of comments for an information request, enabling auditors to view communication history and collaborate with customers. This endpoint always includes soft-deleted records (where `deletionDate !== null`). Clients should check the `deletionDate` field to identify and handle deleted records appropriately in their systems. This endpoint supports delta synchronization via the `changedSinceDate` parameter, allowing efficient polling for changes without retrieving the entire dataset. Pagination usage: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` to see if more data exists 3. If true, use `results.pageInfo.endCursor` as `pageCursor` in next request 4. Repeat until `hasNextPage` is false Delta sync usage: 1. Store the timestamp of your last sync 2. Pass that timestamp as `changedSinceDate` 3. Only comments created, modified, or deleted since that timestamp are returned 4. Process updates, including soft-deletes (deletionDate !== null) 5. Update your last sync timestamp to the current time Rate limit: 50 requests / minute. */
export const listCommentsForInformationRequest: API.OperationMethod<
  ListCommentsForInformationRequestRequest,
  PaginatedResponseInformationRequestComment,
  ListCommentsForInformationRequestError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListCommentsForInformationRequestRequest,
  output: PaginatedResponseInformationRequestComment,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListInformationRequestActivityError = VantaOpError;
/** List information request activity Retrieves a paginated list of activity logs for an information request, providing a complete audit trail of all changes and actions. This endpoint supports delta synchronization via the `changedSinceDate` parameter, allowing efficient polling for changes without retrieving the entire dataset. Pagination usage: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` to see if more data exists 3. If true, use `results.pageInfo.endCursor` as `pageCursor` in next request 4. Repeat until `hasNextPage` is false Delta sync usage: 1. Store the timestamp of your last sync 2. Pass that timestamp as `changedSinceDate` 3. Only activity created since that timestamp is returned 4. Process updates to track all changes to the information request 5. Update your last sync timestamp to the current time Rate limit: 50 requests / minute. */
export const listInformationRequestActivity: API.OperationMethod<
  ListInformationRequestActivityRequest,
  PaginatedResponseInformationRequestActivityLog,
  ListInformationRequestActivityError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListInformationRequestActivityRequest,
  output: PaginatedResponseInformationRequestActivityLog,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListInformationRequestEvidenceError = VantaOpError;
/** List evidence for an information request Retrieves a paginated list of all evidence attached to an information request, enabling auditors to review evidence submitted by customers. This endpoint always includes soft-deleted records (where `deletionDate !== null`). Clients should check the `deletionDate` field to identify and handle deleted records appropriately in their systems. This endpoint supports delta synchronization via the `changedSinceDate` parameter, allowing efficient polling for changes without retrieving the entire dataset. Pagination usage: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` to see if more data exists 3. If true, use `results.pageInfo.endCursor` as `pageCursor` in next request 4. Repeat until `hasNextPage` is false Delta sync usage: 1. Store the timestamp of your last sync 2. Pass that timestamp as `changedSinceDate` 3. Only evidence created, modified, shared, or deleted since that timestamp is returned 4. Process updates, including soft-deletes (deletionDate !== null) 5. Update your last sync timestamp to the current time Rate limit: 50 requests / minute. */
export const listInformationRequestEvidence: API.OperationMethod<
  ListInformationRequestEvidenceRequest,
  PaginatedResponseInformationRequestEvidence,
  ListInformationRequestEvidenceError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListInformationRequestEvidenceRequest,
  output: PaginatedResponseInformationRequestEvidence,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListInformationRequestsError = VantaOpError;
/** List information requests for an audit Retrieves a paginated list of all information requests for an audit, enabling external audit management systems to display and track evidence requests. This endpoint always includes soft-deleted records (where `deletionDate !== null`). Clients should check the `deletionDate` field to identify and handle deleted records appropriately in their systems. This endpoint supports delta synchronization via the `changedSinceDate` parameter, allowing efficient polling for changes without retrieving the entire dataset. Pagination usage: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` to see if more data exists 3. If true, use `results.pageInfo.endCursor` as `pageCursor` in next request 4. Repeat until `hasNextPage` is false Delta sync usage: 1. Store the timestamp of your last sync 2. Pass that timestamp as `changedSinceDate` 3. Only requests created, modified, or deleted since that timestamp are returned 4. Process updates and soft-deletes by checking the `deletionDate` field 5. Update your last sync timestamp to the current time Rate limit: 50 requests / minute. */
export const listInformationRequests: API.OperationMethod<
  ListInformationRequestsRequest,
  PaginatedResponseInformationRequest,
  ListInformationRequestsError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListInformationRequestsRequest,
  output: PaginatedResponseInformationRequest,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListInformationRequestsForControlError = VantaOpError;
/** List information requests linked to a control within an audit Returns a paginated list of active information requests linked to a specific control within an IRL audit. An information request is linked to a control either via its framework codes (`criteriaIds`) or via a direct association (`additionalControlIds`). Soft-deleted information requests are not included in the response. To synchronize deletions, use `GET /audits/{auditId}/information-requests`, which supports `changedSinceDate` and includes soft-deleted records. Returns 404 when the control is not part of the audit. Returns an empty page when the control is part of the audit but has no active IRLs linked to it. Pagination usage: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` to see if more data exists 3. If true, use `results.pageInfo.endCursor` as `pageCursor` in next request 4. Repeat until `hasNextPage` is false Rate limit: 50 requests / minute. */
export const listInformationRequestsForControl: API.OperationMethod<
  ListInformationRequestsForControlRequest,
  PaginatedResponseInformationRequest,
  ListInformationRequestsForControlError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListInformationRequestsForControlRequest,
  output: PaginatedResponseInformationRequest,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListIntegrationsError = VantaOpError;
/** List integrations for an audit Retrieves integration population data for an audit. This endpoint provides access to integration records visible to auditors during an audit engagement. Integrations represent connected services (e.g., GitHub, AWS, Slack) that provide data for the audit. Supports filtering by: - `search`: Searches integration names (case-insensitive) - `tagsMatchesAny`: Filters by integration tag (ACCESS, COMPUTERS, etc.) - `categoriesMatchesAny`: Filters by category (CLOUD_PROVIDER, HR_PROVIDER, etc.) Uses cursor-based pagination. To paginate: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` 3. Use `results.pageInfo.endCursor` as `pageCursor` for next request Results are sorted by integration display name (ascending). This sort order is fixed and cannot be customized via query parameters. Rate limit: 10 requests / minute. */
export const listIntegrations: API.OperationMethod<
  ListIntegrationsRequest,
  PaginatedResponseAuditIntegration,
  ListIntegrationsError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListIntegrationsRequest,
  output: PaginatedResponseAuditIntegration,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListPersonnelAccountAccessError = VantaOpError;
/** List account access records for an audit Retrieves account access population data for an audit. This endpoint provides access to account access records visible to auditors during an audit engagement. Account access data comes from various sources: - **IDP Services** (Identity Providers): Okta, Azure AD, Google Workspace, OneLogin, PingOne - Returns user accounts from identity providers - Supports filtering by search and status - **Role Grants Services**: GCP, Azure (when role grants are enabled) - Returns accounts with role-based access grants - Supports filtering by search and status - **First-Party Account Services**: AWS, Oracle Cloud, Azure (when not using role grants), etc. - Returns cloud provider account access records - Supports filtering by search and status - **Received Account Services**: External applications (Jira, GitHub, Slack, etc.) - Returns user accounts from third-party integrations - Supports filtering by search and status Supports filtering by: - `search`: Searches account names/emails (case-insensitive) - `status`: Filters by account status Uses cursor-based pagination. To paginate: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` 3. Use `results.pageInfo.endCursor` as `pageCursor` for next request The default sort order depends on the service type: - Identity provider services (e.g. Okta, Azure AD): sorted by email, ascending - Cloud provider services (e.g. AWS, GCP): sorted by account name, ascending - Role grant services: sorted by account name, ascending - Third-party application services (e.g. GitHub, Jira): sorted by account name, ascending Sort order cannot be customized via query parameters. Returns 422 when the audit does not have exactly one program segment (multi-framework audits are not supported on this endpoint). Rate limit: 10 requests / minute. */
export const listPersonnelAccountAccess: API.OperationMethod<
  ListPersonnelAccountAccessRequest,
  PaginatedResponseAccountAccess,
  ListPersonnelAccountAccessError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListPersonnelAccountAccessRequest,
  output: PaginatedResponseAccountAccess,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListPersonnelGroupsError = VantaOpError;
/** List groups for an audit Retrieves groups population data for an audit. This endpoint provides access to the group records visible to auditors during an audit engagement. Groups represent organizational units that contain people, either imported from an identity provider (IDP) or created manually in Vanta. Only Controlled Audit View (CAV) audits are supported. Full Audit View audits are rejected with 403. Supports filtering by: - `search`: Searches group names (case-insensitive) - `sourcesMatchesAny`: Filters by IDP source service names Results are sorted by name (ascending) by default. Use `orderBy` and `orderDirection` to customize sorting. Sort parameters must remain consistent across paginated requests. Uses cursor-based pagination. To paginate: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` 3. Use `results.pageInfo.endCursor` as `pageCursor` for next request Rate limit: 10 requests / minute. */
export const listPersonnelGroups: API.OperationMethod<
  ListPersonnelGroupsRequest,
  PaginatedResponsePersonnelGroup,
  ListPersonnelGroupsError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListPersonnelGroupsRequest,
  output: PaginatedResponsePersonnelGroup,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListPersonnelPeopleError = VantaOpError;
/** List people for an audit Retrieves people population data for an audit. This endpoint provides access to the people records visible to auditors during an audit engagement. Only Controlled Audit View (CAV) audits are supported. Full Audit View audits are rejected with 403. Supports filtering by: - `search`: Searches names and email addresses - `status`: Filters by employment status - `groupsMatchesAny`: Filter people by group/role IDs Results are sorted by name (ascending) by default. Use `orderBy` and `orderDirection` to customize sorting. Sort parameters must remain consistent across paginated requests. Uses cursor-based pagination. To paginate: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` 3. Use `results.pageInfo.endCursor` as `pageCursor` for next request Rate limit: 10 requests / minute. */
export const listPersonnelPeople: API.OperationMethod<
  ListPersonnelPeopleRequest,
  PaginatedResponsePersonnelPerson,
  ListPersonnelPeopleError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListPersonnelPeopleRequest,
  output: PaginatedResponsePersonnelPerson,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListRiskSnapshotsError = VantaOpError;
/** List risk snapshots for an audit Returns a paginated list of risk assessment snapshots available for an audit. Risk snapshots capture the state of an organization's risk register at a point in time. Each snapshot has an `id` that can be used with the `/audits/{auditId}/risks` endpoint to retrieve the individual risk scenarios within that snapshot. Results are sorted by creation date (newest first). This sort order is fixed and cannot be customized via query parameters. Only snapshots that are shared with auditors are included. Uses cursor-based pagination. To paginate: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` 3. Use `results.pageInfo.endCursor` as `pageCursor` for next request Rate limit: 10 requests / minute. */
export const listRiskSnapshots: API.OperationMethod<
  ListRiskSnapshotsRequest,
  PaginatedResponseRiskSnapshot,
  ListRiskSnapshotsError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListRiskSnapshotsRequest,
  output: PaginatedResponseRiskSnapshot,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ListVendorsError = VantaOpError;
/** List vendors for an audit Retrieves vendor population data for an audit. This endpoint provides access to vendor records visible to auditors during an audit engagement. Supports filtering by: - `search`: Searches vendor names (case-insensitive) - `vendorStatusesMatchesAny`: Filters by vendor status (ACTIVE, ARCHIVED, IN_PROCUREMENT) - `inherentRiskMatchesAny`: Filters by inherent risk level Results are sorted by name (ascending) by default. Use `orderBy` and `orderDirection` to customize sorting. Sort parameters must remain consistent across paginated requests. Uses cursor-based pagination. To paginate: 1. Make initial request with desired `pageSize` 2. Check `results.pageInfo.hasNextPage` 3. Use `results.pageInfo.endCursor` as `pageCursor` for next request Rate limit: 10 requests / minute. */
export const listVendors: API.OperationMethod<
  ListVendorsRequest,
  PaginatedResponseAuditVendor,
  ListVendorsError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListVendorsRequest,
  output: PaginatedResponseAuditVendor,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type ShareInformationRequestListError = VantaOpError;
/** Share information request list with customer Shares the current information request list for an audit with the customer organization, making it visible in their portal. This action allows the customer to see all information requests that have been created for their audit. Only IRL audits are supported. Rate limit: 50 requests / minute. */
export const shareInformationRequestList: API.OperationMethod<
  ShareInformationRequestListRequest,
  Audit,
  ShareInformationRequestListError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ShareInformationRequestListRequest,
  output: Audit,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type UpdateAuditEvidenceError = VantaOpError;
/** Update audit evidence Update audit evidence. Rate limit: 50 requests / minute. */
export const updateAuditEvidence: API.OperationMethod<
  UpdateAuditEvidenceRequest,
  Evidence,
  UpdateAuditEvidenceError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateAuditEvidenceRequest,
  output: Evidence,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type UpdateCommentForControlError = VantaOpError;
/** Update a comment for a control within an audit Updates an existing comment on a control. Only the original author of the comment can update it. The author is identified by their email address, which must match the email of the user who created the comment. Rate limit: 10 requests / minute. */
export const updateCommentForControl: API.OperationMethod<
  UpdateCommentForControlRequest,
  AuditControlComment,
  UpdateCommentForControlError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateCommentForControlRequest,
  output: AuditControlComment,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type UpdateCommentForInformationRequestError = VantaOpError;
/** Update a comment for an information request Updates an existing comment for an information request. Only the original author of the comment can update it. The author is identified by their email address, which must match the email of the user who created the comment. Rate limit: 25 requests / minute. */
export const updateCommentForInformationRequest: API.OperationMethod<
  UpdateCommentForInformationRequestRequest,
  InformationRequestComment,
  UpdateCommentForInformationRequestError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateCommentForInformationRequestRequest,
  output: InformationRequestComment,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type UpdateInformationRequestError = VantaOpError;
/** Update an information request for an audit Updates an existing information request for an audit, allowing modification of request details as audit requirements evolve. Supports partial updates where only specified fields are changed; omitted fields remain unchanged. Common use cases: - Updating due dates as audit timelines shift - Refining descriptions to clarify requirements - Adjusting request type Note: The `modificationDate` is automatically updated to the current timestamp when any field is changed. Rate limit: 50 requests / minute. */
export const updateInformationRequest: API.OperationMethod<
  UpdateInformationRequestRequest,
  InformationRequest,
  UpdateInformationRequestError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateInformationRequestRequest,
  output: InformationRequest,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));

export type UpsertAssessmentForControlError = VantaOpError;
/** Upsert a control's assessment within an audit Records (upserts) an auditor's assessment state and justification for a control within an IRL audit — the API equivalent of assessing a control in the web app. Overwrites the single assessment for this control in the audit's program segment. The `assessmentState` must be valid for the segment's framework (the request is rejected otherwise). The acting auditor is identified by `auditorEmail`, which must belong to the audit firm making the request. Returns 404 when the control is not part of the audit or the auditor email does not resolve to a firm user. Returns 422 when the audit does not have exactly one program segment (multi-framework audits are not supported on this endpoint). Applies to both Full and Controlled Audit View audits. Rate limit: 10 requests / minute. */
export const upsertAssessmentForControl: API.OperationMethod<
  UpsertAssessmentForControlRequest,
  AuditorControlAssessment,
  UpsertAssessmentForControlError,
  VantaOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpsertAssessmentForControlRequest,
  output: AuditorControlAssessment,
  errors: [UnknownVantaError],
  protocol: VantaProtocol,
  retry: Retry.Retry,
}));
