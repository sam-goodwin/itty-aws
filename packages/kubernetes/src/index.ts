/**
 * @distilled.cloud/kubernetes — Kubernetes SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy models
 * in `.generated-specs` (written by `scripts/convert.ts` from the aggregated
 * Kubernetes swagger.json, one model per API group). Everything else in this
 * folder is hand-written.
 *
 * @example
 * ```ts
 * import * as Kubernetes from "@distilled.cloud/kubernetes";
 *
 * const pods = yield* Kubernetes.Services.core.listCoreV1NamespacedPod({
 *   namespace: "default",
 * });
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  KubernetesProtocol,
  type KubernetesOpError,
  type KubernetesOpContext,
} from "./protocol.ts";
export * as Retry from "./retry.ts";
export * as Services from "./services/index.ts";
