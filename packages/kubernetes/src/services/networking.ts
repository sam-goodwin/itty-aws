// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/kubernetes swagger.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  KubernetesProtocol,
  type KubernetesOpError,
  type KubernetesOpContext,
} from "../protocol.ts";
import { UnknownKubernetesError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { KubernetesOpError, KubernetesOpContext };

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations */
export type IoK8sApimachineryPkgApisMetaV1ObjectMetaAnnotationsMap = {
  [key: string]: string | undefined;
};
export const IoK8sApimachineryPkgApisMetaV1ObjectMetaAnnotationsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.String,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1ObjectMetaAnnotationsMap>;

/** Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. */
export type IoK8sApimachineryPkgApisMetaV1ObjectMetaFinalizersList =
  Array<string>;
export const IoK8sApimachineryPkgApisMetaV1ObjectMetaFinalizersList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1ObjectMetaFinalizersList>;

/** Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels */
export type IoK8sApimachineryPkgApisMetaV1ObjectMetaLabelsMap = {
  [key: string]: string | undefined;
};
export const IoK8sApimachineryPkgApisMetaV1ObjectMetaLabelsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.String,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1ObjectMetaLabelsMap>;

/** ManagedFieldsEntry is a workflow-id, a FieldSet and the group version of the resource that the fieldset applies to. */
export interface IoK8sApimachineryPkgApisMetaV1ManagedFieldsEntry {
  /** APIVersion defines the version of this resource that this field set applies to. The format is "group/version" just like the top-level APIVersion field. It is necessary to track the version of a field set because it cannot be automatically converted. */
  apiVersion?: string;
  /** FieldsType is the discriminator for the different fields format and version. There is currently only one possible value: "FieldsV1" */
  fieldsType?: string;
  /** FieldsV1 holds the first JSON version format as described in the "FieldsV1" type. */
  fieldsV1?: unknown;
  /** Manager is an identifier of the workflow managing these fields. */
  manager?: string;
  /** Operation is the type of operation which lead to this ManagedFieldsEntry being created. The only valid values for this field are 'Apply' and 'Update'. */
  operation?: string;
  /** Subresource is the name of the subresource used to update that object, or empty string if the object was updated through the main resource. The value of this field is used to distinguish between managers, even if they share the same name. For example, a status update will be distinct from a regular update using the same manager name. Note that the APIVersion field is not related to the Subresource field and it always corresponds to the version of the main resource. */
  subresource?: string;
  /** Time is the timestamp of when the ManagedFields entry was added. The timestamp will also be updated if a field is added, the manager changes any of the owned fields value or removes a field. The timestamp does not update when a field is removed from the entry because another manager took it over. */
  time?: string;
}
export const IoK8sApimachineryPkgApisMetaV1ManagedFieldsEntry =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      apiVersion: S.optional(S.String),
      fieldsType: S.optional(S.String),
      fieldsV1: S.optional(S.Unknown),
      manager: S.optional(S.String),
      operation: S.optional(S.String),
      subresource: S.optional(S.String),
      time: S.optional(S.String),
    }),
  ).annotate({
    identifier: "IoK8sApimachineryPkgApisMetaV1ManagedFieldsEntry",
  }) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1ManagedFieldsEntry>;

/** ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. */
export type IoK8sApimachineryPkgApisMetaV1ObjectMetaManagedFieldsList =
  Array<IoK8sApimachineryPkgApisMetaV1ManagedFieldsEntry>;
export const IoK8sApimachineryPkgApisMetaV1ObjectMetaManagedFieldsList =
  /*@__PURE__*/ S.Array(
    IoK8sApimachineryPkgApisMetaV1ManagedFieldsEntry,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1ObjectMetaManagedFieldsList>;

/** OwnerReference contains enough information to let you identify an owning object. An owning object must be in the same namespace as the dependent, or be cluster-scoped, so there is no namespace field. */
export interface IoK8sApimachineryPkgApisMetaV1OwnerReference {
  /** API version of the referent. */
  apiVersion: string;
  /** If true, AND if the owner has the "foregroundDeletion" finalizer, then the owner cannot be deleted from the key-value store until this reference is removed. See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion for how the garbage collector interacts with this field and enforces the foreground deletion. Defaults to false. To set this field, a user needs "delete" permission of the owner, otherwise 422 (Unprocessable Entity) will be returned. */
  blockOwnerDeletion?: boolean;
  /** If true, this reference points to the managing controller. */
  controller?: boolean;
  /** Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind: string;
  /** Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names */
  name: string;
  /** UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids */
  uid: string;
}
export const IoK8sApimachineryPkgApisMetaV1OwnerReference =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      apiVersion: S.String,
      blockOwnerDeletion: S.optional(S.Boolean),
      controller: S.optional(S.Boolean),
      kind: S.String,
      name: S.String,
      uid: S.String,
    }),
  ).annotate({
    identifier: "IoK8sApimachineryPkgApisMetaV1OwnerReference",
  }) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1OwnerReference>;

/** List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. */
export type IoK8sApimachineryPkgApisMetaV1ObjectMetaOwnerReferencesList =
  Array<IoK8sApimachineryPkgApisMetaV1OwnerReference>;
export const IoK8sApimachineryPkgApisMetaV1ObjectMetaOwnerReferencesList =
  /*@__PURE__*/ S.Array(
    IoK8sApimachineryPkgApisMetaV1OwnerReference,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1ObjectMetaOwnerReferencesList>;

/** ObjectMeta is metadata that all persisted resources must have, which includes all objects users must create. */
export interface IoK8sApimachineryPkgApisMetaV1ObjectMeta {
  /** Annotations is an unstructured key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. They are not queryable and should be preserved when modifying objects. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations */
  annotations?: IoK8sApimachineryPkgApisMetaV1ObjectMetaAnnotationsMap;
  /** CreationTimestamp is a timestamp representing the server time when this object was created. It is not guaranteed to be set in happens-before order across separate operations. Clients may not set this value. It is represented in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for lists. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  creationTimestamp?: string;
  /** Number of seconds allowed for this object to gracefully terminate before it will be removed from the system. Only set when deletionTimestamp is also set. May only be shortened. Read-only. */
  deletionGracePeriodSeconds?: number;
  /** DeletionTimestamp is RFC 3339 date and time at which this resource will be deleted. This field is set by the server when a graceful deletion is requested by the user, and is not directly settable by a client. The resource is expected to be deleted (no longer visible from resource lists, and not reachable by name) after the time in this field, once the finalizers list is empty. As long as the finalizers list contains items, deletion is blocked. Once the deletionTimestamp is set, this value may not be unset or be set further into the future, although it may be shortened or the resource may be deleted prior to this time. For example, a user may request that a pod is deleted in 30 seconds. The Kubelet will react by sending a graceful termination signal to the containers in the pod. After that 30 seconds, the Kubelet will send a hard termination signal (SIGKILL) to the container and after cleanup, remove the pod from the API. In the presence of network partitions, this object may still exist after this timestamp, until an administrator or automated process can determine the resource is fully terminated. If not set, graceful deletion of the object has not been requested. Populated by the system when a graceful deletion is requested. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  deletionTimestamp?: string;
  /** Must be empty before the object is deleted from the registry. Each entry is an identifier for the responsible component that will remove the entry from the list. If the deletionTimestamp of the object is non-nil, entries in this list can only be removed. Finalizers may be processed and removed in any order. Order is NOT enforced because it introduces significant risk of stuck finalizers. finalizers is a shared field, any actor with permission can reorder it. If the finalizer list is processed in order, then this can lead to a situation in which the component responsible for the first finalizer in the list is waiting for a signal (field value, external system, or other) produced by a component responsible for a finalizer later in the list, resulting in a deadlock. Without enforced ordering finalizers are free to order amongst themselves and are not vulnerable to ordering changes in the list. */
  finalizers?: IoK8sApimachineryPkgApisMetaV1ObjectMetaFinalizersList;
  /** GenerateName is an optional prefix, used by the server, to generate a unique name ONLY IF the Name field has not been provided. If this field is used, the name returned to the client will be different than the name passed. This value will also be combined with a unique suffix. The provided value has the same validation rules as the Name field, and may be truncated by the length of the suffix required to make the value unique on the server. If this field is specified and the generated name exists, the server will return a 409. Applied only if Name is not specified. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency */
  generateName?: string;
  /** A sequence number representing a specific generation of the desired state. Populated by the system. Read-only. */
  generation?: number;
  /** Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and services. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels */
  labels?: IoK8sApimachineryPkgApisMetaV1ObjectMetaLabelsMap;
  /** ManagedFields maps workflow-id and version to the set of fields that are managed by that workflow. This is mostly for internal housekeeping, and users typically shouldn't need to set or understand this field. A workflow can be the user's name, a controller's name, or the name of a specific apply path like "ci-cd". The set of fields is always in the version that the workflow used when modifying the object. */
  managedFields?: IoK8sApimachineryPkgApisMetaV1ObjectMetaManagedFieldsList;
  /** Name must be unique within a namespace. Is required when creating resources, although some resources may allow a client to request the generation of an appropriate name automatically. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names */
  name?: string;
  /** Namespace defines the space within which each name must be unique. An empty namespace is equivalent to the "default" namespace, but "default" is the canonical representation. Not all objects are required to be scoped to a namespace - the value of this field for those objects will be empty. Must be a DNS_LABEL. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces */
  namespace?: string;
  /** List of objects depended by this object. If ALL objects in the list have been deleted, this object will be garbage collected. If this object is managed by a controller, then an entry in this list will point to this controller, with the controller field set to true. There cannot be more than one managing controller. */
  ownerReferences?: IoK8sApimachineryPkgApisMetaV1ObjectMetaOwnerReferencesList;
  /** An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. Clients must treat these values as opaque and passed unmodified back to the server. They may only be valid for a particular resource or set of resources. Populated by the system. Read-only. Value must be treated as opaque by clients and . More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency */
  resourceVersion?: string;
  /** Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. */
  selfLink?: string;
  /** UID is the unique in time and space value for this object. It is typically generated by the server on successful creation of a resource and is not allowed to change on PUT operations. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids */
  uid?: string;
}
export const IoK8sApimachineryPkgApisMetaV1ObjectMeta = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      annotations: S.optional(
        IoK8sApimachineryPkgApisMetaV1ObjectMetaAnnotationsMap,
      ),
      creationTimestamp: S.optional(S.String),
      deletionGracePeriodSeconds: S.optional(S.Number),
      deletionTimestamp: S.optional(S.String),
      finalizers: S.optional(
        IoK8sApimachineryPkgApisMetaV1ObjectMetaFinalizersList,
      ),
      generateName: S.optional(S.String),
      generation: S.optional(S.Number),
      labels: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMetaLabelsMap),
      managedFields: S.optional(
        IoK8sApimachineryPkgApisMetaV1ObjectMetaManagedFieldsList,
      ),
      name: S.optional(S.String),
      namespace: S.optional(S.String),
      ownerReferences: S.optional(
        IoK8sApimachineryPkgApisMetaV1ObjectMetaOwnerReferencesList,
      ),
      resourceVersion: S.optional(S.String),
      selfLink: S.optional(S.String),
      uid: S.optional(S.String),
    }),
).annotate({
  identifier: "IoK8sApimachineryPkgApisMetaV1ObjectMeta",
}) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1ObjectMeta>;

/** IngressClassParametersReference identifies an API object. This can be used to specify a cluster or namespace-scoped resource. */
export interface IoK8sApiNetworkingV1IngressClassParametersReference {
  /** apiGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required. */
  apiGroup?: string;
  /** kind is the type of resource being referenced. */
  kind: string;
  /** name is the name of resource being referenced. */
  name: string;
  /** namespace is the namespace of the resource being referenced. This field is required when scope is set to "Namespace" and must be unset when scope is set to "Cluster". */
  namespace?: string;
  /** scope represents if this refers to a cluster or namespace scoped resource. This may be set to "Cluster" (default) or "Namespace". */
  scope?: string;
}
export const IoK8sApiNetworkingV1IngressClassParametersReference =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      apiGroup: S.optional(S.String),
      kind: S.String,
      name: S.String,
      namespace: S.optional(S.String),
      scope: S.optional(S.String),
    }),
  ).annotate({
    identifier: "IoK8sApiNetworkingV1IngressClassParametersReference",
  }) as any as S.Schema<IoK8sApiNetworkingV1IngressClassParametersReference>;

/** IngressClassSpec provides information about the class of an Ingress. */
export interface IoK8sApiNetworkingV1IngressClassSpec {
  /** controller refers to the name of the controller that should handle this class. This allows for different "flavors" that are controlled by the same controller. For example, you may have different parameters for the same implementing controller. This should be specified as a domain-prefixed path no more than 250 characters in length, e.g. "acme.io/ingress-controller". This field is immutable. */
  controller?: string;
  /** parameters is a link to a custom resource containing additional configuration for the controller. This is optional if the controller does not require extra parameters. */
  parameters?: IoK8sApiNetworkingV1IngressClassParametersReference;
}
export const IoK8sApiNetworkingV1IngressClassSpec = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      controller: S.optional(S.String),
      parameters: S.optional(
        IoK8sApiNetworkingV1IngressClassParametersReference,
      ),
    }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IngressClassSpec",
}) as any as S.Schema<IoK8sApiNetworkingV1IngressClassSpec>;

export interface CreateNetworkingV1IngressClassRequest {
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the IngressClass. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec?: IoK8sApiNetworkingV1IngressClassSpec;
}
export const CreateNetworkingV1IngressClassRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
      spec: S.optional(IoK8sApiNetworkingV1IngressClassSpec),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/apis/networking.k8s.io/v1/ingressclasses",
        code: 200,
      }),
    ),
).annotate({
  identifier: "CreateNetworkingV1IngressClassRequest",
}) as any as S.Schema<CreateNetworkingV1IngressClassRequest>;

/** IngressClass represents the class of the Ingress, referenced by the Ingress Spec. The `ingressclass.kubernetes.io/is-default-class` annotation can be used to indicate that an IngressClass should be considered default. When a single IngressClass resource has this annotation set to true, new Ingress resources without a class specified will be assigned this default class. */
export interface IoK8sApiNetworkingV1IngressClass {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the IngressClass. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec?: IoK8sApiNetworkingV1IngressClassSpec;
}
export const IoK8sApiNetworkingV1IngressClass = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    kind: S.optional(S.String),
    metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
    spec: S.optional(IoK8sApiNetworkingV1IngressClassSpec),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IngressClass",
}) as any as S.Schema<IoK8sApiNetworkingV1IngressClass>;

/** ParentReference describes a reference to a parent object. */
export interface IoK8sApiNetworkingV1ParentReference {
  /** Group is the group of the object being referenced. */
  group?: string;
  /** Name is the name of the object being referenced. */
  name: string;
  /** Namespace is the namespace of the object being referenced. */
  namespace?: string;
  /** Resource is the resource of the object being referenced. */
  resource: string;
}
export const IoK8sApiNetworkingV1ParentReference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    group: S.optional(S.String),
    name: S.String,
    namespace: S.optional(S.String),
    resource: S.String,
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1ParentReference",
}) as any as S.Schema<IoK8sApiNetworkingV1ParentReference>;

/** IPAddressSpec describe the attributes in an IP Address. */
export interface IoK8sApiNetworkingV1IPAddressSpec {
  /** ParentRef references the resource that an IPAddress is attached to. An IPAddress must reference a parent object. */
  parentRef: IoK8sApiNetworkingV1ParentReference;
}
export const IoK8sApiNetworkingV1IPAddressSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    parentRef: IoK8sApiNetworkingV1ParentReference,
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IPAddressSpec",
}) as any as S.Schema<IoK8sApiNetworkingV1IPAddressSpec>;

export interface CreateNetworkingV1IPAddressRequest {
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the IPAddress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec: IoK8sApiNetworkingV1IPAddressSpec;
}
export const CreateNetworkingV1IPAddressRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pretty: S.optional(S.String.pipe(T.Query())),
    dryRun: S.optional(S.String.pipe(T.Query())),
    fieldManager: S.optional(S.String.pipe(T.Query())),
    fieldValidation: S.optional(S.String.pipe(T.Query())),
    apiVersion: S.optional(S.String),
    kind: S.optional(S.String),
    metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
    spec: IoK8sApiNetworkingV1IPAddressSpec,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/apis/networking.k8s.io/v1/ipaddresses",
      code: 200,
    }),
  ),
).annotate({
  identifier: "CreateNetworkingV1IPAddressRequest",
}) as any as S.Schema<CreateNetworkingV1IPAddressRequest>;

/** IPAddress represents a single IP of a single IP Family. The object is designed to be used by APIs that operate on IP addresses. The object is used by the Service core API for allocation of IP addresses. An IP address can be represented in different formats, to guarantee the uniqueness of the IP, the name of the object is the IP address in canonical format, four decimal digits separated by dots suppressing leading zeros for IPv4 and the representation defined by RFC 5952 for IPv6. Valid: 192.168.1.5 or 2001:db8::1 or 2001:db8:aaaa:bbbb:cccc:dddd:eeee:1 Invalid: 10.01.2.3 or 2001:db8:0:0:0::1 */
export interface IoK8sApiNetworkingV1IPAddress {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the IPAddress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec: IoK8sApiNetworkingV1IPAddressSpec;
}
export const IoK8sApiNetworkingV1IPAddress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    kind: S.optional(S.String),
    metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
    spec: IoK8sApiNetworkingV1IPAddressSpec,
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IPAddress",
}) as any as S.Schema<IoK8sApiNetworkingV1IPAddress>;

/** TypedLocalObjectReference contains enough information to let you locate the typed referenced object inside the same namespace. */
export interface IoK8sApiCoreV1TypedLocalObjectReference {
  /** APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required. */
  apiGroup?: string;
  /** Kind is the type of resource being referenced */
  kind: string;
  /** Name is the name of resource being referenced */
  name: string;
}
export const IoK8sApiCoreV1TypedLocalObjectReference = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      apiGroup: S.optional(S.String),
      kind: S.String,
      name: S.String,
    }),
).annotate({
  identifier: "IoK8sApiCoreV1TypedLocalObjectReference",
}) as any as S.Schema<IoK8sApiCoreV1TypedLocalObjectReference>;

/** ServiceBackendPort is the service port being referenced. */
export interface IoK8sApiNetworkingV1ServiceBackendPort {
  /** name is the name of the port on the Service. This is a mutually exclusive setting with "Number". */
  name?: string;
  /** number is the numerical port number (e.g. 80) on the Service. This is a mutually exclusive setting with "Name". */
  number?: number;
}
export const IoK8sApiNetworkingV1ServiceBackendPort = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      name: S.optional(S.String),
      number: S.optional(S.Number),
    }),
).annotate({
  identifier: "IoK8sApiNetworkingV1ServiceBackendPort",
}) as any as S.Schema<IoK8sApiNetworkingV1ServiceBackendPort>;

/** IngressServiceBackend references a Kubernetes Service as a Backend. */
export interface IoK8sApiNetworkingV1IngressServiceBackend {
  /** name is the referenced service. The service must exist in the same namespace as the Ingress object. */
  name: string;
  /** port of the referenced service. A port name or port number is required for a IngressServiceBackend. */
  port?: IoK8sApiNetworkingV1ServiceBackendPort;
}
export const IoK8sApiNetworkingV1IngressServiceBackend =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      name: S.String,
      port: S.optional(IoK8sApiNetworkingV1ServiceBackendPort),
    }),
  ).annotate({
    identifier: "IoK8sApiNetworkingV1IngressServiceBackend",
  }) as any as S.Schema<IoK8sApiNetworkingV1IngressServiceBackend>;

/** IngressBackend describes all endpoints for a given service and port. */
export interface IoK8sApiNetworkingV1IngressBackend {
  /** resource is an ObjectRef to another Kubernetes resource in the namespace of the Ingress object. If resource is specified, a service.Name and service.Port must not be specified. This is a mutually exclusive setting with "Service". */
  resource?: IoK8sApiCoreV1TypedLocalObjectReference;
  /** service references a service as a backend. This is a mutually exclusive setting with "Resource". */
  service?: IoK8sApiNetworkingV1IngressServiceBackend;
}
export const IoK8sApiNetworkingV1IngressBackend = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    resource: S.optional(IoK8sApiCoreV1TypedLocalObjectReference),
    service: S.optional(IoK8sApiNetworkingV1IngressServiceBackend),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IngressBackend",
}) as any as S.Schema<IoK8sApiNetworkingV1IngressBackend>;

/** HTTPIngressPath associates a path with a backend. Incoming urls matching the path are forwarded to the backend. */
export interface IoK8sApiNetworkingV1HTTPIngressPath {
  /** backend defines the referenced service endpoint to which the traffic will be forwarded to. */
  backend: IoK8sApiNetworkingV1IngressBackend;
  /** path is matched against the path of an incoming request. Currently it can contain characters disallowed from the conventional "path" part of a URL as defined by RFC 3986. Paths must begin with a '/' and must be present when using PathType with value "Exact" or "Prefix". */
  path?: string;
  /** pathType determines the interpretation of the path matching. PathType can be one of the following values: * Exact: Matches the URL path exactly. * Prefix: Matches based on a URL path prefix split by '/'. Matching is done on a path element by element basis. A path element refers is the list of labels in the path split by the '/' separator. A request is a match for path p if every p is an element-wise prefix of p of the request path. Note that if the last element of the path is a substring of the last element in request path, it is not a match (e.g. /foo/bar matches /foo/bar/baz, but does not match /foo/barbaz). * ImplementationSpecific: Interpretation of the Path matching is up to the IngressClass. Implementations can treat this as a separate PathType or treat it identically to Prefix or Exact path types. Implementations are required to support all path types. */
  pathType: string;
}
export const IoK8sApiNetworkingV1HTTPIngressPath = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    backend: IoK8sApiNetworkingV1IngressBackend,
    path: S.optional(S.String),
    pathType: S.String,
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1HTTPIngressPath",
}) as any as S.Schema<IoK8sApiNetworkingV1HTTPIngressPath>;

/** paths is a collection of paths that map requests to backends. */
export type IoK8sApiNetworkingV1HTTPIngressRuleValuePathsList =
  Array<IoK8sApiNetworkingV1HTTPIngressPath>;
export const IoK8sApiNetworkingV1HTTPIngressRuleValuePathsList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1HTTPIngressPath,
  ) as any as S.Schema<IoK8sApiNetworkingV1HTTPIngressRuleValuePathsList>;

/** HTTPIngressRuleValue is a list of http selectors pointing to backends. In the example: http://<host>/<path>?<searchpart> -> backend where where parts of the url correspond to RFC 3986, this resource will be used to match against everything after the last '/' and before the first '?' or '#'. */
export interface IoK8sApiNetworkingV1HTTPIngressRuleValue {
  /** paths is a collection of paths that map requests to backends. */
  paths: IoK8sApiNetworkingV1HTTPIngressRuleValuePathsList;
}
export const IoK8sApiNetworkingV1HTTPIngressRuleValue = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      paths: IoK8sApiNetworkingV1HTTPIngressRuleValuePathsList,
    }),
).annotate({
  identifier: "IoK8sApiNetworkingV1HTTPIngressRuleValue",
}) as any as S.Schema<IoK8sApiNetworkingV1HTTPIngressRuleValue>;

/** IngressRule represents the rules mapping the paths under a specified host to the related backend services. Incoming requests are first evaluated for a host match, then routed to the backend associated with the matching IngressRuleValue. */
export interface IoK8sApiNetworkingV1IngressRule {
  /** host is the fully qualified domain name of a network host, as defined by RFC 3986. Note the following deviations from the "host" part of the URI as defined in RFC 3986: 1. IPs are not allowed. Currently an IngressRuleValue can only apply to the IP in the Spec of the parent Ingress. 2. The `:` delimiter is not respected because ports are not allowed. Currently the port of an Ingress is implicitly :80 for http and :443 for https. Both these may change in the future. Incoming requests are matched against the host before the IngressRuleValue. If the host is unspecified, the Ingress routes all traffic based on the specified IngressRuleValue. host can be "precise" which is a domain name without the terminating dot of a network host (e.g. "foo.bar.com") or "wildcard", which is a domain name prefixed with a single wildcard label (e.g. "*.foo.com"). The wildcard character '*' must appear by itself as the first DNS label and matches only a single label. You cannot have a wildcard label by itself (e.g. Host == "*"). Requests will be matched against the Host field in the following way: 1. If host is precise, the request matches this rule if the http host header is equal to Host. 2. If host is a wildcard, then the request matches this rule if the http host header is to equal to the suffix (removing the first label) of the wildcard rule. */
  host?: string;
  http?: IoK8sApiNetworkingV1HTTPIngressRuleValue;
}
export const IoK8sApiNetworkingV1IngressRule = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    host: S.optional(S.String),
    http: S.optional(IoK8sApiNetworkingV1HTTPIngressRuleValue),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IngressRule",
}) as any as S.Schema<IoK8sApiNetworkingV1IngressRule>;

/** rules is a list of host rules used to configure the Ingress. If unspecified, or no rule matches, all traffic is sent to the default backend. */
export type IoK8sApiNetworkingV1IngressSpecRulesList =
  Array<IoK8sApiNetworkingV1IngressRule>;
export const IoK8sApiNetworkingV1IngressSpecRulesList = /*@__PURE__*/ S.Array(
  IoK8sApiNetworkingV1IngressRule,
) as any as S.Schema<IoK8sApiNetworkingV1IngressSpecRulesList>;

/** hosts is a list of hosts included in the TLS certificate. The values in this list must match the name/s used in the tlsSecret. Defaults to the wildcard host setting for the loadbalancer controller fulfilling this Ingress, if left unspecified. */
export type IoK8sApiNetworkingV1IngressTLSHostsList = Array<string>;
export const IoK8sApiNetworkingV1IngressTLSHostsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<IoK8sApiNetworkingV1IngressTLSHostsList>;

/** IngressTLS describes the transport layer security associated with an ingress. */
export interface IoK8sApiNetworkingV1IngressTLS {
  /** hosts is a list of hosts included in the TLS certificate. The values in this list must match the name/s used in the tlsSecret. Defaults to the wildcard host setting for the loadbalancer controller fulfilling this Ingress, if left unspecified. */
  hosts?: IoK8sApiNetworkingV1IngressTLSHostsList;
  /** secretName is the name of the secret used to terminate TLS traffic on port 443. Field is left optional to allow TLS routing based on SNI hostname alone. If the SNI host in a listener conflicts with the "Host" header field used by an IngressRule, the SNI host is used for termination and value of the "Host" header is used for routing. */
  secretName?: string;
}
export const IoK8sApiNetworkingV1IngressTLS = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    hosts: S.optional(IoK8sApiNetworkingV1IngressTLSHostsList),
    secretName: S.optional(S.String),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IngressTLS",
}) as any as S.Schema<IoK8sApiNetworkingV1IngressTLS>;

/** tls represents the TLS configuration. Currently the Ingress only supports a single TLS port, 443. If multiple members of this list specify different hosts, they will be multiplexed on the same port according to the hostname specified through the SNI TLS extension, if the ingress controller fulfilling the ingress supports SNI. */
export type IoK8sApiNetworkingV1IngressSpecTlsList =
  Array<IoK8sApiNetworkingV1IngressTLS>;
export const IoK8sApiNetworkingV1IngressSpecTlsList = /*@__PURE__*/ S.Array(
  IoK8sApiNetworkingV1IngressTLS,
) as any as S.Schema<IoK8sApiNetworkingV1IngressSpecTlsList>;

/** IngressSpec describes the Ingress the user wishes to exist. */
export interface IoK8sApiNetworkingV1IngressSpec {
  /** defaultBackend is the backend that should handle requests that don't match any rule. If Rules are not specified, DefaultBackend must be specified. If DefaultBackend is not set, the handling of requests that do not match any of the rules will be up to the Ingress controller. */
  defaultBackend?: IoK8sApiNetworkingV1IngressBackend;
  /** ingressClassName is the name of an IngressClass cluster resource. Ingress controller implementations use this field to know whether they should be serving this Ingress resource, by a transitive connection (controller -> IngressClass -> Ingress resource). Although the `kubernetes.io/ingress.class` annotation (simple constant name) was never formally defined, it was widely supported by Ingress controllers to create a direct binding between Ingress controller and Ingress resources. Newly created Ingress resources should prefer using the field. However, even though the annotation is officially deprecated, for backwards compatibility reasons, ingress controllers should still honor that annotation if present. */
  ingressClassName?: string;
  /** rules is a list of host rules used to configure the Ingress. If unspecified, or no rule matches, all traffic is sent to the default backend. */
  rules?: IoK8sApiNetworkingV1IngressSpecRulesList;
  /** tls represents the TLS configuration. Currently the Ingress only supports a single TLS port, 443. If multiple members of this list specify different hosts, they will be multiplexed on the same port according to the hostname specified through the SNI TLS extension, if the ingress controller fulfilling the ingress supports SNI. */
  tls?: IoK8sApiNetworkingV1IngressSpecTlsList;
}
export const IoK8sApiNetworkingV1IngressSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    defaultBackend: S.optional(IoK8sApiNetworkingV1IngressBackend),
    ingressClassName: S.optional(S.String),
    rules: S.optional(IoK8sApiNetworkingV1IngressSpecRulesList),
    tls: S.optional(IoK8sApiNetworkingV1IngressSpecTlsList),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IngressSpec",
}) as any as S.Schema<IoK8sApiNetworkingV1IngressSpec>;

/** IngressPortStatus represents the error condition of a service port */
export interface IoK8sApiNetworkingV1IngressPortStatus {
  /** error is to record the problem with the service port The format of the error shall comply with the following rules: - built-in error values shall be specified in this file and those shall use CamelCase names - cloud provider specific error values must have names that comply with the format foo.example.com/CamelCase. */
  error?: string;
  /** port is the port number of the ingress port. */
  port: number;
  /** protocol is the protocol of the ingress port. The supported values are: "TCP", "UDP", "SCTP" */
  protocol: string;
}
export const IoK8sApiNetworkingV1IngressPortStatus = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      error: S.optional(S.String),
      port: S.Number,
      protocol: S.String,
    }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IngressPortStatus",
}) as any as S.Schema<IoK8sApiNetworkingV1IngressPortStatus>;

/** ports provides information about the ports exposed by this LoadBalancer. */
export type IoK8sApiNetworkingV1IngressLoadBalancerIngressPortsList =
  Array<IoK8sApiNetworkingV1IngressPortStatus>;
export const IoK8sApiNetworkingV1IngressLoadBalancerIngressPortsList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1IngressPortStatus,
  ) as any as S.Schema<IoK8sApiNetworkingV1IngressLoadBalancerIngressPortsList>;

/** IngressLoadBalancerIngress represents the status of a load-balancer ingress point. */
export interface IoK8sApiNetworkingV1IngressLoadBalancerIngress {
  /** hostname is set for load-balancer ingress points that are DNS based. */
  hostname?: string;
  /** ip is set for load-balancer ingress points that are IP based. */
  ip?: string;
  /** ports provides information about the ports exposed by this LoadBalancer. */
  ports?: IoK8sApiNetworkingV1IngressLoadBalancerIngressPortsList;
}
export const IoK8sApiNetworkingV1IngressLoadBalancerIngress =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      hostname: S.optional(S.String),
      ip: S.optional(S.String),
      ports: S.optional(
        IoK8sApiNetworkingV1IngressLoadBalancerIngressPortsList,
      ),
    }),
  ).annotate({
    identifier: "IoK8sApiNetworkingV1IngressLoadBalancerIngress",
  }) as any as S.Schema<IoK8sApiNetworkingV1IngressLoadBalancerIngress>;

/** ingress is a list containing ingress points for the load-balancer. */
export type IoK8sApiNetworkingV1IngressLoadBalancerStatusIngressList =
  Array<IoK8sApiNetworkingV1IngressLoadBalancerIngress>;
export const IoK8sApiNetworkingV1IngressLoadBalancerStatusIngressList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1IngressLoadBalancerIngress,
  ) as any as S.Schema<IoK8sApiNetworkingV1IngressLoadBalancerStatusIngressList>;

/** IngressLoadBalancerStatus represents the status of a load-balancer. */
export interface IoK8sApiNetworkingV1IngressLoadBalancerStatus {
  /** ingress is a list containing ingress points for the load-balancer. */
  ingress?: IoK8sApiNetworkingV1IngressLoadBalancerStatusIngressList;
}
export const IoK8sApiNetworkingV1IngressLoadBalancerStatus =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      ingress: S.optional(
        IoK8sApiNetworkingV1IngressLoadBalancerStatusIngressList,
      ),
    }),
  ).annotate({
    identifier: "IoK8sApiNetworkingV1IngressLoadBalancerStatus",
  }) as any as S.Schema<IoK8sApiNetworkingV1IngressLoadBalancerStatus>;

/** IngressStatus describe the current state of the Ingress. */
export interface IoK8sApiNetworkingV1IngressStatus {
  /** loadBalancer contains the current status of the load-balancer. */
  loadBalancer?: IoK8sApiNetworkingV1IngressLoadBalancerStatus;
}
export const IoK8sApiNetworkingV1IngressStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    loadBalancer: S.optional(IoK8sApiNetworkingV1IngressLoadBalancerStatus),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IngressStatus",
}) as any as S.Schema<IoK8sApiNetworkingV1IngressStatus>;

export interface CreateNetworkingV1NamespacedIngressRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the Ingress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec?: IoK8sApiNetworkingV1IngressSpec;
  /** status is the current state of the Ingress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  status?: IoK8sApiNetworkingV1IngressStatus;
}
export const CreateNetworkingV1NamespacedIngressRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
      spec: S.optional(IoK8sApiNetworkingV1IngressSpec),
      status: S.optional(IoK8sApiNetworkingV1IngressStatus),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/ingresses",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "CreateNetworkingV1NamespacedIngressRequest",
  }) as any as S.Schema<CreateNetworkingV1NamespacedIngressRequest>;

/** Ingress is a collection of rules that allow inbound connections to reach the endpoints defined by a backend. An Ingress can be configured to give services externally-reachable urls, load balance traffic, terminate SSL, offer name based virtual hosting etc. */
export interface IoK8sApiNetworkingV1Ingress {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the Ingress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec?: IoK8sApiNetworkingV1IngressSpec;
  /** status is the current state of the Ingress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  status?: IoK8sApiNetworkingV1IngressStatus;
}
export const IoK8sApiNetworkingV1Ingress = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    kind: S.optional(S.String),
    metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
    spec: S.optional(IoK8sApiNetworkingV1IngressSpec),
    status: S.optional(IoK8sApiNetworkingV1IngressStatus),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1Ingress",
}) as any as S.Schema<IoK8sApiNetworkingV1Ingress>;

/** NetworkPolicyPort describes a port to allow traffic on */
export interface IoK8sApiNetworkingV1NetworkPolicyPort {
  /** endPort indicates that the range of ports from port to endPort if set, inclusive, should be allowed by the policy. This field cannot be defined if the port field is not defined or if the port field is defined as a named (string) port. The endPort must be equal or greater than port. */
  endPort?: number;
  /** port represents the port on the given protocol. This can either be a numerical or named port on a pod. If this field is not provided, this matches all port names and numbers. If present, only traffic on the specified protocol AND port will be matched. */
  port?: string;
  /** protocol represents the protocol (TCP, UDP, or SCTP) which traffic must match. If not specified, this field defaults to TCP. */
  protocol?: string;
}
export const IoK8sApiNetworkingV1NetworkPolicyPort = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      endPort: S.optional(S.Number),
      port: S.optional(S.String),
      protocol: S.optional(S.String),
    }),
).annotate({
  identifier: "IoK8sApiNetworkingV1NetworkPolicyPort",
}) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicyPort>;

/** ports is a list of destination ports for outgoing traffic. Each item in this list is combined using a logical OR. If this field is empty or missing, this rule matches all ports (traffic not restricted by port). If this field is present and contains at least one item, then this rule allows traffic only if the traffic matches at least one port in the list. */
export type IoK8sApiNetworkingV1NetworkPolicyEgressRulePortsList =
  Array<IoK8sApiNetworkingV1NetworkPolicyPort>;
export const IoK8sApiNetworkingV1NetworkPolicyEgressRulePortsList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1NetworkPolicyPort,
  ) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicyEgressRulePortsList>;

/** except is a slice of CIDRs that should not be included within an IPBlock Valid examples are "192.168.1.0/24" or "2001:db8::/64" Except values will be rejected if they are outside the cidr range */
export type IoK8sApiNetworkingV1IPBlockExceptList = Array<string>;
export const IoK8sApiNetworkingV1IPBlockExceptList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<IoK8sApiNetworkingV1IPBlockExceptList>;

/** IPBlock describes a particular CIDR (Ex. "192.168.1.0/24","2001:db8::/64") that is allowed to the pods matched by a NetworkPolicySpec's podSelector. The except entry describes CIDRs that should not be included within this rule. */
export interface IoK8sApiNetworkingV1IPBlock {
  /** cidr is a string representing the IPBlock Valid examples are "192.168.1.0/24" or "2001:db8::/64" */
  cidr: string;
  /** except is a slice of CIDRs that should not be included within an IPBlock Valid examples are "192.168.1.0/24" or "2001:db8::/64" Except values will be rejected if they are outside the cidr range */
  except?: IoK8sApiNetworkingV1IPBlockExceptList;
}
export const IoK8sApiNetworkingV1IPBlock = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cidr: S.String,
    except: S.optional(IoK8sApiNetworkingV1IPBlockExceptList),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IPBlock",
}) as any as S.Schema<IoK8sApiNetworkingV1IPBlock>;

/** values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch. */
export type IoK8sApimachineryPkgApisMetaV1LabelSelectorRequirementValuesList =
  Array<string>;
export const IoK8sApimachineryPkgApisMetaV1LabelSelectorRequirementValuesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1LabelSelectorRequirementValuesList>;

/** A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values. */
export interface IoK8sApimachineryPkgApisMetaV1LabelSelectorRequirement {
  /** key is the label key that the selector applies to. */
  key: string;
  /** operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist. */
  operator: string;
  /** values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch. */
  values?: IoK8sApimachineryPkgApisMetaV1LabelSelectorRequirementValuesList;
}
export const IoK8sApimachineryPkgApisMetaV1LabelSelectorRequirement =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      key: S.String,
      operator: S.String,
      values: S.optional(
        IoK8sApimachineryPkgApisMetaV1LabelSelectorRequirementValuesList,
      ),
    }),
  ).annotate({
    identifier: "IoK8sApimachineryPkgApisMetaV1LabelSelectorRequirement",
  }) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1LabelSelectorRequirement>;

/** matchExpressions is a list of label selector requirements. The requirements are ANDed. */
export type IoK8sApimachineryPkgApisMetaV1LabelSelectorMatchExpressionsList =
  Array<IoK8sApimachineryPkgApisMetaV1LabelSelectorRequirement>;
export const IoK8sApimachineryPkgApisMetaV1LabelSelectorMatchExpressionsList =
  /*@__PURE__*/ S.Array(
    IoK8sApimachineryPkgApisMetaV1LabelSelectorRequirement,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1LabelSelectorMatchExpressionsList>;

/** matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. */
export type IoK8sApimachineryPkgApisMetaV1LabelSelectorMatchLabelsMap = {
  [key: string]: string | undefined;
};
export const IoK8sApimachineryPkgApisMetaV1LabelSelectorMatchLabelsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.String,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1LabelSelectorMatchLabelsMap>;

/** A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects. */
export interface IoK8sApimachineryPkgApisMetaV1LabelSelector {
  /** matchExpressions is a list of label selector requirements. The requirements are ANDed. */
  matchExpressions?: IoK8sApimachineryPkgApisMetaV1LabelSelectorMatchExpressionsList;
  /** matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed. */
  matchLabels?: IoK8sApimachineryPkgApisMetaV1LabelSelectorMatchLabelsMap;
}
export const IoK8sApimachineryPkgApisMetaV1LabelSelector =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      matchExpressions: S.optional(
        IoK8sApimachineryPkgApisMetaV1LabelSelectorMatchExpressionsList,
      ),
      matchLabels: S.optional(
        IoK8sApimachineryPkgApisMetaV1LabelSelectorMatchLabelsMap,
      ),
    }),
  ).annotate({
    identifier: "IoK8sApimachineryPkgApisMetaV1LabelSelector",
  }) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1LabelSelector>;

/** NetworkPolicyPeer describes a peer to allow traffic to/from. Only certain combinations of fields are allowed */
export interface IoK8sApiNetworkingV1NetworkPolicyPeer {
  /** ipBlock defines policy on a particular IPBlock. If this field is set then neither of the other fields can be. */
  ipBlock?: IoK8sApiNetworkingV1IPBlock;
  /** namespaceSelector selects namespaces using cluster-scoped labels. This field follows standard label selector semantics; if present but empty, it selects all namespaces. If podSelector is also set, then the NetworkPolicyPeer as a whole selects the pods matching podSelector in the namespaces selected by namespaceSelector. Otherwise it selects all pods in the namespaces selected by namespaceSelector. */
  namespaceSelector?: IoK8sApimachineryPkgApisMetaV1LabelSelector;
  /** podSelector is a label selector which selects pods. This field follows standard label selector semantics; if present but empty, it selects all pods. If namespaceSelector is also set, then the NetworkPolicyPeer as a whole selects the pods matching podSelector in the Namespaces selected by NamespaceSelector. Otherwise it selects the pods matching podSelector in the policy's own namespace. */
  podSelector?: IoK8sApimachineryPkgApisMetaV1LabelSelector;
}
export const IoK8sApiNetworkingV1NetworkPolicyPeer = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      ipBlock: S.optional(IoK8sApiNetworkingV1IPBlock),
      namespaceSelector: S.optional(
        IoK8sApimachineryPkgApisMetaV1LabelSelector,
      ),
      podSelector: S.optional(IoK8sApimachineryPkgApisMetaV1LabelSelector),
    }),
).annotate({
  identifier: "IoK8sApiNetworkingV1NetworkPolicyPeer",
}) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicyPeer>;

/** to is a list of destinations for outgoing traffic of pods selected for this rule. Items in this list are combined using a logical OR operation. If this field is empty or missing, this rule matches all destinations (traffic not restricted by destination). If this field is present and contains at least one item, this rule allows traffic only if the traffic matches at least one item in the to list. */
export type IoK8sApiNetworkingV1NetworkPolicyEgressRuleToList =
  Array<IoK8sApiNetworkingV1NetworkPolicyPeer>;
export const IoK8sApiNetworkingV1NetworkPolicyEgressRuleToList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1NetworkPolicyPeer,
  ) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicyEgressRuleToList>;

/** NetworkPolicyEgressRule describes a particular set of traffic that is allowed out of pods matched by a NetworkPolicySpec's podSelector. The traffic must match both ports and to. This type is beta-level in 1.8 */
export interface IoK8sApiNetworkingV1NetworkPolicyEgressRule {
  /** ports is a list of destination ports for outgoing traffic. Each item in this list is combined using a logical OR. If this field is empty or missing, this rule matches all ports (traffic not restricted by port). If this field is present and contains at least one item, then this rule allows traffic only if the traffic matches at least one port in the list. */
  ports?: IoK8sApiNetworkingV1NetworkPolicyEgressRulePortsList;
  /** to is a list of destinations for outgoing traffic of pods selected for this rule. Items in this list are combined using a logical OR operation. If this field is empty or missing, this rule matches all destinations (traffic not restricted by destination). If this field is present and contains at least one item, this rule allows traffic only if the traffic matches at least one item in the to list. */
  to?: IoK8sApiNetworkingV1NetworkPolicyEgressRuleToList;
}
export const IoK8sApiNetworkingV1NetworkPolicyEgressRule =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      ports: S.optional(IoK8sApiNetworkingV1NetworkPolicyEgressRulePortsList),
      to: S.optional(IoK8sApiNetworkingV1NetworkPolicyEgressRuleToList),
    }),
  ).annotate({
    identifier: "IoK8sApiNetworkingV1NetworkPolicyEgressRule",
  }) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicyEgressRule>;

/** egress is a list of egress rules to be applied to the selected pods. Outgoing traffic is allowed if there are no NetworkPolicies selecting the pod (and cluster policy otherwise allows the traffic), OR if the traffic matches at least one egress rule across all of the NetworkPolicy objects whose podSelector matches the pod. If this field is empty then this NetworkPolicy limits all outgoing traffic (and serves solely to ensure that the pods it selects are isolated by default). This field is beta-level in 1.8 */
export type IoK8sApiNetworkingV1NetworkPolicySpecEgressList =
  Array<IoK8sApiNetworkingV1NetworkPolicyEgressRule>;
export const IoK8sApiNetworkingV1NetworkPolicySpecEgressList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1NetworkPolicyEgressRule,
  ) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicySpecEgressList>;

/** from is a list of sources which should be able to access the pods selected for this rule. Items in this list are combined using a logical OR operation. If this field is empty or missing, this rule matches all sources (traffic not restricted by source). If this field is present and contains at least one item, this rule allows traffic only if the traffic matches at least one item in the from list. */
export type IoK8sApiNetworkingV1NetworkPolicyIngressRuleFromList =
  Array<IoK8sApiNetworkingV1NetworkPolicyPeer>;
export const IoK8sApiNetworkingV1NetworkPolicyIngressRuleFromList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1NetworkPolicyPeer,
  ) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicyIngressRuleFromList>;

/** ports is a list of ports which should be made accessible on the pods selected for this rule. Each item in this list is combined using a logical OR. If this field is empty or missing, this rule matches all ports (traffic not restricted by port). If this field is present and contains at least one item, then this rule allows traffic only if the traffic matches at least one port in the list. */
export type IoK8sApiNetworkingV1NetworkPolicyIngressRulePortsList =
  Array<IoK8sApiNetworkingV1NetworkPolicyPort>;
export const IoK8sApiNetworkingV1NetworkPolicyIngressRulePortsList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1NetworkPolicyPort,
  ) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicyIngressRulePortsList>;

/** NetworkPolicyIngressRule describes a particular set of traffic that is allowed to the pods matched by a NetworkPolicySpec's podSelector. The traffic must match both ports and from. */
export interface IoK8sApiNetworkingV1NetworkPolicyIngressRule {
  /** from is a list of sources which should be able to access the pods selected for this rule. Items in this list are combined using a logical OR operation. If this field is empty or missing, this rule matches all sources (traffic not restricted by source). If this field is present and contains at least one item, this rule allows traffic only if the traffic matches at least one item in the from list. */
  from?: IoK8sApiNetworkingV1NetworkPolicyIngressRuleFromList;
  /** ports is a list of ports which should be made accessible on the pods selected for this rule. Each item in this list is combined using a logical OR. If this field is empty or missing, this rule matches all ports (traffic not restricted by port). If this field is present and contains at least one item, then this rule allows traffic only if the traffic matches at least one port in the list. */
  ports?: IoK8sApiNetworkingV1NetworkPolicyIngressRulePortsList;
}
export const IoK8sApiNetworkingV1NetworkPolicyIngressRule =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      from: S.optional(IoK8sApiNetworkingV1NetworkPolicyIngressRuleFromList),
      ports: S.optional(IoK8sApiNetworkingV1NetworkPolicyIngressRulePortsList),
    }),
  ).annotate({
    identifier: "IoK8sApiNetworkingV1NetworkPolicyIngressRule",
  }) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicyIngressRule>;

/** ingress is a list of ingress rules to be applied to the selected pods. Traffic is allowed to a pod if there are no NetworkPolicies selecting the pod (and cluster policy otherwise allows the traffic), OR if the traffic source is the pod's local node, OR if the traffic matches at least one ingress rule across all of the NetworkPolicy objects whose podSelector matches the pod. If this field is empty then this NetworkPolicy does not allow any traffic (and serves solely to ensure that the pods it selects are isolated by default) */
export type IoK8sApiNetworkingV1NetworkPolicySpecIngressList =
  Array<IoK8sApiNetworkingV1NetworkPolicyIngressRule>;
export const IoK8sApiNetworkingV1NetworkPolicySpecIngressList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1NetworkPolicyIngressRule,
  ) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicySpecIngressList>;

/** policyTypes is a list of rule types that the NetworkPolicy relates to. Valid options are ["Ingress"], ["Egress"], or ["Ingress", "Egress"]. If this field is not specified, it will default based on the existence of ingress or egress rules; policies that contain an egress section are assumed to affect egress, and all policies (whether or not they contain an ingress section) are assumed to affect ingress. If you want to write an egress-only policy, you must explicitly specify policyTypes [ "Egress" ]. Likewise, if you want to write a policy that specifies that no egress is allowed, you must specify a policyTypes value that include "Egress" (since such a policy would not include an egress section and would otherwise default to just [ "Ingress" ]). This field is beta-level in 1.8 */
export type IoK8sApiNetworkingV1NetworkPolicySpecPolicyTypesList =
  Array<string>;
export const IoK8sApiNetworkingV1NetworkPolicySpecPolicyTypesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicySpecPolicyTypesList>;

/** NetworkPolicySpec provides the specification of a NetworkPolicy */
export interface IoK8sApiNetworkingV1NetworkPolicySpec {
  /** egress is a list of egress rules to be applied to the selected pods. Outgoing traffic is allowed if there are no NetworkPolicies selecting the pod (and cluster policy otherwise allows the traffic), OR if the traffic matches at least one egress rule across all of the NetworkPolicy objects whose podSelector matches the pod. If this field is empty then this NetworkPolicy limits all outgoing traffic (and serves solely to ensure that the pods it selects are isolated by default). This field is beta-level in 1.8 */
  egress?: IoK8sApiNetworkingV1NetworkPolicySpecEgressList;
  /** ingress is a list of ingress rules to be applied to the selected pods. Traffic is allowed to a pod if there are no NetworkPolicies selecting the pod (and cluster policy otherwise allows the traffic), OR if the traffic source is the pod's local node, OR if the traffic matches at least one ingress rule across all of the NetworkPolicy objects whose podSelector matches the pod. If this field is empty then this NetworkPolicy does not allow any traffic (and serves solely to ensure that the pods it selects are isolated by default) */
  ingress?: IoK8sApiNetworkingV1NetworkPolicySpecIngressList;
  /** podSelector selects the pods to which this NetworkPolicy object applies. The array of rules is applied to any pods selected by this field. An empty selector matches all pods in the policy's namespace. Multiple network policies can select the same set of pods. In this case, the ingress rules for each are combined additively. This field is optional. If it is not specified, it defaults to an empty selector. */
  podSelector?: IoK8sApimachineryPkgApisMetaV1LabelSelector;
  /** policyTypes is a list of rule types that the NetworkPolicy relates to. Valid options are ["Ingress"], ["Egress"], or ["Ingress", "Egress"]. If this field is not specified, it will default based on the existence of ingress or egress rules; policies that contain an egress section are assumed to affect egress, and all policies (whether or not they contain an ingress section) are assumed to affect ingress. If you want to write an egress-only policy, you must explicitly specify policyTypes [ "Egress" ]. Likewise, if you want to write a policy that specifies that no egress is allowed, you must specify a policyTypes value that include "Egress" (since such a policy would not include an egress section and would otherwise default to just [ "Ingress" ]). This field is beta-level in 1.8 */
  policyTypes?: IoK8sApiNetworkingV1NetworkPolicySpecPolicyTypesList;
}
export const IoK8sApiNetworkingV1NetworkPolicySpec = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      egress: S.optional(IoK8sApiNetworkingV1NetworkPolicySpecEgressList),
      ingress: S.optional(IoK8sApiNetworkingV1NetworkPolicySpecIngressList),
      podSelector: S.optional(IoK8sApimachineryPkgApisMetaV1LabelSelector),
      policyTypes: S.optional(
        IoK8sApiNetworkingV1NetworkPolicySpecPolicyTypesList,
      ),
    }),
).annotate({
  identifier: "IoK8sApiNetworkingV1NetworkPolicySpec",
}) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicySpec>;

export interface CreateNetworkingV1NamespacedNetworkPolicyRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec represents the specification of the desired behavior for this NetworkPolicy. */
  spec?: IoK8sApiNetworkingV1NetworkPolicySpec;
}
export const CreateNetworkingV1NamespacedNetworkPolicyRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
      spec: S.optional(IoK8sApiNetworkingV1NetworkPolicySpec),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/networkpolicies",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "CreateNetworkingV1NamespacedNetworkPolicyRequest",
  }) as any as S.Schema<CreateNetworkingV1NamespacedNetworkPolicyRequest>;

/** NetworkPolicy describes what network traffic is allowed for a set of Pods */
export interface IoK8sApiNetworkingV1NetworkPolicy {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec represents the specification of the desired behavior for this NetworkPolicy. */
  spec?: IoK8sApiNetworkingV1NetworkPolicySpec;
}
export const IoK8sApiNetworkingV1NetworkPolicy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    kind: S.optional(S.String),
    metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
    spec: S.optional(IoK8sApiNetworkingV1NetworkPolicySpec),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1NetworkPolicy",
}) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicy>;

/** CIDRs defines the IP blocks in CIDR notation (e.g. "192.168.0.0/24" or "2001:db8::/64") from which to assign service cluster IPs. Max of two CIDRs is allowed, one of each IP family. This field is immutable. */
export type IoK8sApiNetworkingV1ServiceCIDRSpecCidrsList = Array<string>;
export const IoK8sApiNetworkingV1ServiceCIDRSpecCidrsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<IoK8sApiNetworkingV1ServiceCIDRSpecCidrsList>;

/** ServiceCIDRSpec define the CIDRs the user wants to use for allocating ClusterIPs for Services. */
export interface IoK8sApiNetworkingV1ServiceCIDRSpec {
  /** CIDRs defines the IP blocks in CIDR notation (e.g. "192.168.0.0/24" or "2001:db8::/64") from which to assign service cluster IPs. Max of two CIDRs is allowed, one of each IP family. This field is immutable. */
  cidrs?: IoK8sApiNetworkingV1ServiceCIDRSpecCidrsList;
}
export const IoK8sApiNetworkingV1ServiceCIDRSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cidrs: S.optional(IoK8sApiNetworkingV1ServiceCIDRSpecCidrsList),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1ServiceCIDRSpec",
}) as any as S.Schema<IoK8sApiNetworkingV1ServiceCIDRSpec>;

/** Condition contains details for one aspect of the current state of this API Resource. */
export interface IoK8sApimachineryPkgApisMetaV1Condition {
  /** lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed. If that is not known, then using the time when the API field changed is acceptable. */
  lastTransitionTime: string;
  /** message is a human readable message indicating details about the transition. This may be an empty string. */
  message: string;
  /** observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance. */
  observedGeneration?: number;
  /** reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty. */
  reason: string;
  /** status of the condition, one of True, False, Unknown. */
  status: string;
  /** type of condition in CamelCase or in foo.example.com/CamelCase. */
  type: string;
}
export const IoK8sApimachineryPkgApisMetaV1Condition = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      lastTransitionTime: S.String,
      message: S.String,
      observedGeneration: S.optional(S.Number),
      reason: S.String,
      status: S.String,
      type: S.String,
    }),
).annotate({
  identifier: "IoK8sApimachineryPkgApisMetaV1Condition",
}) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1Condition>;

/** conditions holds an array of metav1.Condition that describe the state of the ServiceCIDR. Current service state */
export type IoK8sApiNetworkingV1ServiceCIDRStatusConditionsList =
  Array<IoK8sApimachineryPkgApisMetaV1Condition>;
export const IoK8sApiNetworkingV1ServiceCIDRStatusConditionsList =
  /*@__PURE__*/ S.Array(
    IoK8sApimachineryPkgApisMetaV1Condition,
  ) as any as S.Schema<IoK8sApiNetworkingV1ServiceCIDRStatusConditionsList>;

/** ServiceCIDRStatus describes the current state of the ServiceCIDR. */
export interface IoK8sApiNetworkingV1ServiceCIDRStatus {
  /** conditions holds an array of metav1.Condition that describe the state of the ServiceCIDR. Current service state */
  conditions?: IoK8sApiNetworkingV1ServiceCIDRStatusConditionsList;
}
export const IoK8sApiNetworkingV1ServiceCIDRStatus = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      conditions: S.optional(
        IoK8sApiNetworkingV1ServiceCIDRStatusConditionsList,
      ),
    }),
).annotate({
  identifier: "IoK8sApiNetworkingV1ServiceCIDRStatus",
}) as any as S.Schema<IoK8sApiNetworkingV1ServiceCIDRStatus>;

export interface CreateNetworkingV1ServiceCIDRRequest {
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the ServiceCIDR. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec?: IoK8sApiNetworkingV1ServiceCIDRSpec;
  /** status represents the current state of the ServiceCIDR. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  status?: IoK8sApiNetworkingV1ServiceCIDRStatus;
}
export const CreateNetworkingV1ServiceCIDRRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
      spec: S.optional(IoK8sApiNetworkingV1ServiceCIDRSpec),
      status: S.optional(IoK8sApiNetworkingV1ServiceCIDRStatus),
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/apis/networking.k8s.io/v1/servicecidrs",
        code: 200,
      }),
    ),
).annotate({
  identifier: "CreateNetworkingV1ServiceCIDRRequest",
}) as any as S.Schema<CreateNetworkingV1ServiceCIDRRequest>;

/** ServiceCIDR defines a range of IP addresses using CIDR format (e.g. 192.168.0.0/24 or 2001:db2::/64). This range is used to allocate ClusterIPs to Service objects. */
export interface IoK8sApiNetworkingV1ServiceCIDR {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the ServiceCIDR. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec?: IoK8sApiNetworkingV1ServiceCIDRSpec;
  /** status represents the current state of the ServiceCIDR. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  status?: IoK8sApiNetworkingV1ServiceCIDRStatus;
}
export const IoK8sApiNetworkingV1ServiceCIDR = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    kind: S.optional(S.String),
    metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
    spec: S.optional(IoK8sApiNetworkingV1ServiceCIDRSpec),
    status: S.optional(IoK8sApiNetworkingV1ServiceCIDRStatus),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1ServiceCIDR",
}) as any as S.Schema<IoK8sApiNetworkingV1ServiceCIDR>;

/** Preconditions must be fulfilled before an operation (update, delete, etc.) is carried out. */
export interface IoK8sApimachineryPkgApisMetaV1Preconditions {
  /** Specifies the target ResourceVersion */
  resourceVersion?: string;
  /** Specifies the target UID. */
  uid?: string;
}
export const IoK8sApimachineryPkgApisMetaV1Preconditions =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      resourceVersion: S.optional(S.String),
      uid: S.optional(S.String),
    }),
  ).annotate({
    identifier: "IoK8sApimachineryPkgApisMetaV1Preconditions",
  }) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1Preconditions>;

export interface DeleteNetworkingV1CollectionIngressClassRequest {
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. */
  gracePeriodSeconds?: number;
  /** if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it */
  ignoreStoreReadErrorWithClusterBreakingPotential?: boolean;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. */
  orphanDependents?: boolean;
  /** Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground. */
  propagationPolicy?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Must be fulfilled before a deletion is carried out. If not possible, a 409 Conflict status will be returned. */
  preconditions?: IoK8sApimachineryPkgApisMetaV1Preconditions;
}
export const DeleteNetworkingV1CollectionIngressClassRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      pretty: S.optional(S.String.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      gracePeriodSeconds: S.optional(S.Number.pipe(T.Query())),
      ignoreStoreReadErrorWithClusterBreakingPotential: S.optional(
        S.Boolean.pipe(T.Query()),
      ),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      orphanDependents: S.optional(S.Boolean.pipe(T.Query())),
      propagationPolicy: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      preconditions: S.optional(IoK8sApimachineryPkgApisMetaV1Preconditions),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/apis/networking.k8s.io/v1/ingressclasses",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "DeleteNetworkingV1CollectionIngressClassRequest",
  }) as any as S.Schema<DeleteNetworkingV1CollectionIngressClassRequest>;

/** StatusCause provides more information about an api.Status failure, including cases when multiple errors are encountered. */
export interface IoK8sApimachineryPkgApisMetaV1StatusCause {
  /** The field of the resource that has caused this error, as named by its JSON serialization. May include dot and postfix notation for nested attributes. Arrays are zero-indexed. Fields may appear more than once in an array of causes due to fields having multiple errors. Optional. Examples: "name" - the field "name" on the current resource "items[0].name" - the field "name" on the first array entry in "items" */
  field?: string;
  /** A human-readable description of the cause of the error. This field may be presented as-is to a reader. */
  message?: string;
  /** A machine-readable description of the cause of the error. If this value is empty there is no information available. */
  reason?: string;
}
export const IoK8sApimachineryPkgApisMetaV1StatusCause =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      field: S.optional(S.String),
      message: S.optional(S.String),
      reason: S.optional(S.String),
    }),
  ).annotate({
    identifier: "IoK8sApimachineryPkgApisMetaV1StatusCause",
  }) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1StatusCause>;

/** The Causes array includes more details associated with the StatusReason failure. Not all StatusReasons may provide detailed causes. */
export type IoK8sApimachineryPkgApisMetaV1StatusDetailsCausesList =
  Array<IoK8sApimachineryPkgApisMetaV1StatusCause>;
export const IoK8sApimachineryPkgApisMetaV1StatusDetailsCausesList =
  /*@__PURE__*/ S.Array(
    IoK8sApimachineryPkgApisMetaV1StatusCause,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1StatusDetailsCausesList>;

/** StatusDetails is a set of additional properties that MAY be set by the server to provide additional information about a response. The Reason field of a Status object defines what attributes will be set. Clients must ignore fields that do not match the defined type of each attribute, and should assume that any attribute may be empty, invalid, or under defined. */
export interface IoK8sApimachineryPkgApisMetaV1StatusDetails {
  /** The Causes array includes more details associated with the StatusReason failure. Not all StatusReasons may provide detailed causes. */
  causes?: IoK8sApimachineryPkgApisMetaV1StatusDetailsCausesList;
  /** The group attribute of the resource associated with the status StatusReason. */
  group?: string;
  /** The kind attribute of the resource associated with the status StatusReason. On some operations may differ from the requested resource Kind. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** The name attribute of the resource associated with the status StatusReason (when there is a single name which can be described). */
  name?: string;
  /** If specified, the time in seconds before the operation should be retried. Some errors may indicate the client must take an alternate action - for those errors this field may indicate how long to wait before taking the alternate action. */
  retryAfterSeconds?: number;
  /** UID of the resource. (when there is a single resource which can be described). More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids */
  uid?: string;
}
export const IoK8sApimachineryPkgApisMetaV1StatusDetails =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      causes: S.optional(IoK8sApimachineryPkgApisMetaV1StatusDetailsCausesList),
      group: S.optional(S.String),
      kind: S.optional(S.String),
      name: S.optional(S.String),
      retryAfterSeconds: S.optional(S.Number),
      uid: S.optional(S.String),
    }),
  ).annotate({
    identifier: "IoK8sApimachineryPkgApisMetaV1StatusDetails",
  }) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1StatusDetails>;

/** ShardInfo describes the shard selector that was applied to produce a list response. Its presence on a list response indicates the list is a filtered subset. */
export interface IoK8sApimachineryPkgApisMetaV1ShardInfo {
  /** selector is the shard selector string from the request, echoed back so clients can verify which shard they received and merge responses from multiple shards. */
  selector: string;
}
export const IoK8sApimachineryPkgApisMetaV1ShardInfo = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      selector: S.String,
    }),
).annotate({
  identifier: "IoK8sApimachineryPkgApisMetaV1ShardInfo",
}) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1ShardInfo>;

/** ListMeta describes metadata that synthetic resources must have, including lists and various status objects. A resource may have only one of {ObjectMeta, ListMeta}. */
export interface IoK8sApimachineryPkgApisMetaV1ListMeta {
  /** continue may be set if the user set a limit on the number of items returned, and indicates that the server has more data available. The value is opaque and may be used to issue another request to the endpoint that served this list to retrieve the next set of available objects. Continuing a consistent list may not be possible if the server configuration has changed or more than a few minutes have passed. The resourceVersion field returned when using this continue value will be identical to the value in the first response, unless you have received this token from an error message. */
  continue?: string;
  /** remainingItemCount is the number of subsequent items in the list which are not included in this list response. If the list request contained label or field selectors, then the number of remaining items is unknown and the field will be left unset and omitted during serialization. If the list is complete (either because it is not chunking or because this is the last chunk), then there are no more remaining items and this field will be left unset and omitted during serialization. Servers older than v1.15 do not set this field. The intended use of the remainingItemCount is *estimating* the size of a collection. Clients should not rely on the remainingItemCount to be set or to be exact. */
  remainingItemCount?: number;
  /** String that identifies the server's internal version of this object that can be used by clients to determine when objects have changed. Value must be treated as opaque by clients and passed unmodified back to the server. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency */
  resourceVersion?: string;
  /** Deprecated: selfLink is a legacy read-only field that is no longer populated by the system. */
  selfLink?: string;
  /** shardInfo is set when the list is a filtered subset of the full collection, as selected by a shard selector on the request. It echoes back the selector so clients can verify which shard they received and merge sharded responses. Clients should not cache sharded list responses as a full representation of the collection. This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardInfo?: IoK8sApimachineryPkgApisMetaV1ShardInfo;
}
export const IoK8sApimachineryPkgApisMetaV1ListMeta = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      continue: S.optional(S.String),
      remainingItemCount: S.optional(S.Number),
      resourceVersion: S.optional(S.String),
      selfLink: S.optional(S.String),
      shardInfo: S.optional(IoK8sApimachineryPkgApisMetaV1ShardInfo),
    }),
).annotate({
  identifier: "IoK8sApimachineryPkgApisMetaV1ListMeta",
}) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1ListMeta>;

/** Status is a return value for calls that don't return other objects. */
export interface IoK8sApimachineryPkgApisMetaV1Status {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Suggested HTTP return code for this status, 0 if not set. */
  code?: number;
  /** Extended data associated with the reason. Each reason may define its own extended details. This field is optional and the data returned is not guaranteed to conform to any schema except that defined by the reason type. */
  details?: IoK8sApimachineryPkgApisMetaV1StatusDetails;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** A human-readable description of the status of this operation. */
  message?: string;
  /** Standard list metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  metadata?: IoK8sApimachineryPkgApisMetaV1ListMeta;
  /** A machine-readable description of why this operation is in the "Failure" status. If this value is empty there is no information available. A Reason clarifies an HTTP status code but does not override it. */
  reason?: string;
  /** Status of the operation. One of: "Success" or "Failure". More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  status?: string;
}
export const IoK8sApimachineryPkgApisMetaV1Status = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      apiVersion: S.optional(S.String),
      code: S.optional(S.Number),
      details: S.optional(IoK8sApimachineryPkgApisMetaV1StatusDetails),
      kind: S.optional(S.String),
      message: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ListMeta),
      reason: S.optional(S.String),
      status: S.optional(S.String),
    }),
).annotate({
  identifier: "IoK8sApimachineryPkgApisMetaV1Status",
}) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1Status>;

export interface DeleteNetworkingV1CollectionIPAddressRequest {
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. */
  gracePeriodSeconds?: number;
  /** if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it */
  ignoreStoreReadErrorWithClusterBreakingPotential?: boolean;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. */
  orphanDependents?: boolean;
  /** Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground. */
  propagationPolicy?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Must be fulfilled before a deletion is carried out. If not possible, a 409 Conflict status will be returned. */
  preconditions?: IoK8sApimachineryPkgApisMetaV1Preconditions;
}
export const DeleteNetworkingV1CollectionIPAddressRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      pretty: S.optional(S.String.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      gracePeriodSeconds: S.optional(S.Number.pipe(T.Query())),
      ignoreStoreReadErrorWithClusterBreakingPotential: S.optional(
        S.Boolean.pipe(T.Query()),
      ),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      orphanDependents: S.optional(S.Boolean.pipe(T.Query())),
      propagationPolicy: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      preconditions: S.optional(IoK8sApimachineryPkgApisMetaV1Preconditions),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/apis/networking.k8s.io/v1/ipaddresses",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "DeleteNetworkingV1CollectionIPAddressRequest",
  }) as any as S.Schema<DeleteNetworkingV1CollectionIPAddressRequest>;

export interface DeleteNetworkingV1CollectionNamespacedIngressRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. */
  gracePeriodSeconds?: number;
  /** if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it */
  ignoreStoreReadErrorWithClusterBreakingPotential?: boolean;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. */
  orphanDependents?: boolean;
  /** Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground. */
  propagationPolicy?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Must be fulfilled before a deletion is carried out. If not possible, a 409 Conflict status will be returned. */
  preconditions?: IoK8sApimachineryPkgApisMetaV1Preconditions;
}
export const DeleteNetworkingV1CollectionNamespacedIngressRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      gracePeriodSeconds: S.optional(S.Number.pipe(T.Query())),
      ignoreStoreReadErrorWithClusterBreakingPotential: S.optional(
        S.Boolean.pipe(T.Query()),
      ),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      orphanDependents: S.optional(S.Boolean.pipe(T.Query())),
      propagationPolicy: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      preconditions: S.optional(IoK8sApimachineryPkgApisMetaV1Preconditions),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/ingresses",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "DeleteNetworkingV1CollectionNamespacedIngressRequest",
  }) as any as S.Schema<DeleteNetworkingV1CollectionNamespacedIngressRequest>;

export interface DeleteNetworkingV1CollectionNamespacedNetworkPolicyRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. */
  gracePeriodSeconds?: number;
  /** if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it */
  ignoreStoreReadErrorWithClusterBreakingPotential?: boolean;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. */
  orphanDependents?: boolean;
  /** Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground. */
  propagationPolicy?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Must be fulfilled before a deletion is carried out. If not possible, a 409 Conflict status will be returned. */
  preconditions?: IoK8sApimachineryPkgApisMetaV1Preconditions;
}
export const DeleteNetworkingV1CollectionNamespacedNetworkPolicyRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      gracePeriodSeconds: S.optional(S.Number.pipe(T.Query())),
      ignoreStoreReadErrorWithClusterBreakingPotential: S.optional(
        S.Boolean.pipe(T.Query()),
      ),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      orphanDependents: S.optional(S.Boolean.pipe(T.Query())),
      propagationPolicy: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      preconditions: S.optional(IoK8sApimachineryPkgApisMetaV1Preconditions),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/networkpolicies",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "DeleteNetworkingV1CollectionNamespacedNetworkPolicyRequest",
  }) as any as S.Schema<DeleteNetworkingV1CollectionNamespacedNetworkPolicyRequest>;

export interface DeleteNetworkingV1CollectionServiceCIDRRequest {
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. */
  gracePeriodSeconds?: number;
  /** if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it */
  ignoreStoreReadErrorWithClusterBreakingPotential?: boolean;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. */
  orphanDependents?: boolean;
  /** Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground. */
  propagationPolicy?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Must be fulfilled before a deletion is carried out. If not possible, a 409 Conflict status will be returned. */
  preconditions?: IoK8sApimachineryPkgApisMetaV1Preconditions;
}
export const DeleteNetworkingV1CollectionServiceCIDRRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      pretty: S.optional(S.String.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      gracePeriodSeconds: S.optional(S.Number.pipe(T.Query())),
      ignoreStoreReadErrorWithClusterBreakingPotential: S.optional(
        S.Boolean.pipe(T.Query()),
      ),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      orphanDependents: S.optional(S.Boolean.pipe(T.Query())),
      propagationPolicy: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      preconditions: S.optional(IoK8sApimachineryPkgApisMetaV1Preconditions),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/apis/networking.k8s.io/v1/servicecidrs",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "DeleteNetworkingV1CollectionServiceCIDRRequest",
  }) as any as S.Schema<DeleteNetworkingV1CollectionServiceCIDRRequest>;

export interface DeleteNetworkingV1IngressClassRequest {
  /** name of the IngressClass */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. */
  gracePeriodSeconds?: number;
  /** if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it */
  ignoreStoreReadErrorWithClusterBreakingPotential?: boolean;
  /** Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. */
  orphanDependents?: boolean;
  /** Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground. */
  propagationPolicy?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Must be fulfilled before a deletion is carried out. If not possible, a 409 Conflict status will be returned. */
  preconditions?: IoK8sApimachineryPkgApisMetaV1Preconditions;
}
export const DeleteNetworkingV1IngressClassRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      gracePeriodSeconds: S.optional(S.Number.pipe(T.Query())),
      ignoreStoreReadErrorWithClusterBreakingPotential: S.optional(
        S.Boolean.pipe(T.Query()),
      ),
      orphanDependents: S.optional(S.Boolean.pipe(T.Query())),
      propagationPolicy: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      preconditions: S.optional(IoK8sApimachineryPkgApisMetaV1Preconditions),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/apis/networking.k8s.io/v1/ingressclasses/{name}",
        code: 200,
      }),
    ),
).annotate({
  identifier: "DeleteNetworkingV1IngressClassRequest",
}) as any as S.Schema<DeleteNetworkingV1IngressClassRequest>;

export interface DeleteNetworkingV1IPAddressRequest {
  /** name of the IPAddress */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. */
  gracePeriodSeconds?: number;
  /** if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it */
  ignoreStoreReadErrorWithClusterBreakingPotential?: boolean;
  /** Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. */
  orphanDependents?: boolean;
  /** Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground. */
  propagationPolicy?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Must be fulfilled before a deletion is carried out. If not possible, a 409 Conflict status will be returned. */
  preconditions?: IoK8sApimachineryPkgApisMetaV1Preconditions;
}
export const DeleteNetworkingV1IPAddressRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
    dryRun: S.optional(S.String.pipe(T.Query())),
    gracePeriodSeconds: S.optional(S.Number.pipe(T.Query())),
    ignoreStoreReadErrorWithClusterBreakingPotential: S.optional(
      S.Boolean.pipe(T.Query()),
    ),
    orphanDependents: S.optional(S.Boolean.pipe(T.Query())),
    propagationPolicy: S.optional(S.String.pipe(T.Query())),
    apiVersion: S.optional(S.String),
    kind: S.optional(S.String),
    preconditions: S.optional(IoK8sApimachineryPkgApisMetaV1Preconditions),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/apis/networking.k8s.io/v1/ipaddresses/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteNetworkingV1IPAddressRequest",
}) as any as S.Schema<DeleteNetworkingV1IPAddressRequest>;

export interface DeleteNetworkingV1NamespacedIngressRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the Ingress */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. */
  gracePeriodSeconds?: number;
  /** if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it */
  ignoreStoreReadErrorWithClusterBreakingPotential?: boolean;
  /** Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. */
  orphanDependents?: boolean;
  /** Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground. */
  propagationPolicy?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Must be fulfilled before a deletion is carried out. If not possible, a 409 Conflict status will be returned. */
  preconditions?: IoK8sApimachineryPkgApisMetaV1Preconditions;
}
export const DeleteNetworkingV1NamespacedIngressRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      gracePeriodSeconds: S.optional(S.Number.pipe(T.Query())),
      ignoreStoreReadErrorWithClusterBreakingPotential: S.optional(
        S.Boolean.pipe(T.Query()),
      ),
      orphanDependents: S.optional(S.Boolean.pipe(T.Query())),
      propagationPolicy: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      preconditions: S.optional(IoK8sApimachineryPkgApisMetaV1Preconditions),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/ingresses/{name}",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "DeleteNetworkingV1NamespacedIngressRequest",
  }) as any as S.Schema<DeleteNetworkingV1NamespacedIngressRequest>;

export interface DeleteNetworkingV1NamespacedNetworkPolicyRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the NetworkPolicy */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. */
  gracePeriodSeconds?: number;
  /** if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it */
  ignoreStoreReadErrorWithClusterBreakingPotential?: boolean;
  /** Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. */
  orphanDependents?: boolean;
  /** Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground. */
  propagationPolicy?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Must be fulfilled before a deletion is carried out. If not possible, a 409 Conflict status will be returned. */
  preconditions?: IoK8sApimachineryPkgApisMetaV1Preconditions;
}
export const DeleteNetworkingV1NamespacedNetworkPolicyRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      gracePeriodSeconds: S.optional(S.Number.pipe(T.Query())),
      ignoreStoreReadErrorWithClusterBreakingPotential: S.optional(
        S.Boolean.pipe(T.Query()),
      ),
      orphanDependents: S.optional(S.Boolean.pipe(T.Query())),
      propagationPolicy: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      preconditions: S.optional(IoK8sApimachineryPkgApisMetaV1Preconditions),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/networkpolicies/{name}",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "DeleteNetworkingV1NamespacedNetworkPolicyRequest",
  }) as any as S.Schema<DeleteNetworkingV1NamespacedNetworkPolicyRequest>;

export interface DeleteNetworkingV1ServiceCIDRRequest {
  /** name of the ServiceCIDR */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately. */
  gracePeriodSeconds?: number;
  /** if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it */
  ignoreStoreReadErrorWithClusterBreakingPotential?: boolean;
  /** Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both. */
  orphanDependents?: boolean;
  /** Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground. */
  propagationPolicy?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Must be fulfilled before a deletion is carried out. If not possible, a 409 Conflict status will be returned. */
  preconditions?: IoK8sApimachineryPkgApisMetaV1Preconditions;
}
export const DeleteNetworkingV1ServiceCIDRRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      gracePeriodSeconds: S.optional(S.Number.pipe(T.Query())),
      ignoreStoreReadErrorWithClusterBreakingPotential: S.optional(
        S.Boolean.pipe(T.Query()),
      ),
      orphanDependents: S.optional(S.Boolean.pipe(T.Query())),
      propagationPolicy: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      preconditions: S.optional(IoK8sApimachineryPkgApisMetaV1Preconditions),
    }).pipe(
      T.Http({
        method: "DELETE",
        uri: "/apis/networking.k8s.io/v1/servicecidrs/{name}",
        code: 200,
      }),
    ),
).annotate({
  identifier: "DeleteNetworkingV1ServiceCIDRRequest",
}) as any as S.Schema<DeleteNetworkingV1ServiceCIDRRequest>;

export interface GetNetworkingAPIGroupRequest {}
export const GetNetworkingAPIGroupRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "GET", uri: "/apis/networking.k8s.io/", code: 200 }),
  ),
).annotate({
  identifier: "GetNetworkingAPIGroupRequest",
}) as any as S.Schema<GetNetworkingAPIGroupRequest>;

/** GroupVersion contains the "group/version" and "version" string of a version. It is made a struct to keep extensibility. */
export interface IoK8sApimachineryPkgApisMetaV1GroupVersionForDiscovery {
  /** groupVersion specifies the API group and version in the form "group/version" */
  groupVersion: string;
  /** version specifies the version in the form of "version". This is to save the clients the trouble of splitting the GroupVersion. */
  version: string;
}
export const IoK8sApimachineryPkgApisMetaV1GroupVersionForDiscovery =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      groupVersion: S.String,
      version: S.String,
    }),
  ).annotate({
    identifier: "IoK8sApimachineryPkgApisMetaV1GroupVersionForDiscovery",
  }) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1GroupVersionForDiscovery>;

/** ServerAddressByClientCIDR helps the client to determine the server address that they should use, depending on the clientCIDR that they match. */
export interface IoK8sApimachineryPkgApisMetaV1ServerAddressByClientCIDR {
  /** The CIDR with which clients can match their IP to figure out the server address that they should use. */
  clientCIDR: string;
  /** Address of this server, suitable for a client that matches the above CIDR. This can be a hostname, hostname:port, IP or IP:port. */
  serverAddress: string;
}
export const IoK8sApimachineryPkgApisMetaV1ServerAddressByClientCIDR =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      clientCIDR: S.String,
      serverAddress: S.String,
    }),
  ).annotate({
    identifier: "IoK8sApimachineryPkgApisMetaV1ServerAddressByClientCIDR",
  }) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1ServerAddressByClientCIDR>;

/** a map of client CIDR to server address that is serving this group. This is to help clients reach servers in the most network-efficient way possible. Clients can use the appropriate server address as per the CIDR that they match. In case of multiple matches, clients should use the longest matching CIDR. The server returns only those CIDRs that it thinks that the client can match. For example: the master will return an internal IP CIDR only, if the client reaches the server using an internal IP. Server looks at X-Forwarded-For header or X-Real-Ip header or request.RemoteAddr (in that order) to get the client IP. */
export type IoK8sApimachineryPkgApisMetaV1APIGroupServerAddressByClientCIDRsList =
  Array<IoK8sApimachineryPkgApisMetaV1ServerAddressByClientCIDR>;
export const IoK8sApimachineryPkgApisMetaV1APIGroupServerAddressByClientCIDRsList =
  /*@__PURE__*/ S.Array(
    IoK8sApimachineryPkgApisMetaV1ServerAddressByClientCIDR,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1APIGroupServerAddressByClientCIDRsList>;

/** versions are the versions supported in this group. */
export type IoK8sApimachineryPkgApisMetaV1APIGroupVersionsList =
  Array<IoK8sApimachineryPkgApisMetaV1GroupVersionForDiscovery>;
export const IoK8sApimachineryPkgApisMetaV1APIGroupVersionsList =
  /*@__PURE__*/ S.Array(
    IoK8sApimachineryPkgApisMetaV1GroupVersionForDiscovery,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1APIGroupVersionsList>;

/** APIGroup contains the name, the supported versions, and the preferred version of a group. */
export interface IoK8sApimachineryPkgApisMetaV1APIGroup {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** name is the name of the group. */
  name: string;
  /** preferredVersion is the version preferred by the API server, which probably is the storage version. */
  preferredVersion?: IoK8sApimachineryPkgApisMetaV1GroupVersionForDiscovery;
  /** a map of client CIDR to server address that is serving this group. This is to help clients reach servers in the most network-efficient way possible. Clients can use the appropriate server address as per the CIDR that they match. In case of multiple matches, clients should use the longest matching CIDR. The server returns only those CIDRs that it thinks that the client can match. For example: the master will return an internal IP CIDR only, if the client reaches the server using an internal IP. Server looks at X-Forwarded-For header or X-Real-Ip header or request.RemoteAddr (in that order) to get the client IP. */
  serverAddressByClientCIDRs?: IoK8sApimachineryPkgApisMetaV1APIGroupServerAddressByClientCIDRsList;
  /** versions are the versions supported in this group. */
  versions: IoK8sApimachineryPkgApisMetaV1APIGroupVersionsList;
}
export const IoK8sApimachineryPkgApisMetaV1APIGroup = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      name: S.String,
      preferredVersion: S.optional(
        IoK8sApimachineryPkgApisMetaV1GroupVersionForDiscovery,
      ),
      serverAddressByClientCIDRs: S.optional(
        IoK8sApimachineryPkgApisMetaV1APIGroupServerAddressByClientCIDRsList,
      ),
      versions: IoK8sApimachineryPkgApisMetaV1APIGroupVersionsList,
    }),
).annotate({
  identifier: "IoK8sApimachineryPkgApisMetaV1APIGroup",
}) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1APIGroup>;

export interface GetNetworkingV1APIResourcesRequest {}
export const GetNetworkingV1APIResourcesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "GET", uri: "/apis/networking.k8s.io/v1/", code: 200 }),
  ),
).annotate({
  identifier: "GetNetworkingV1APIResourcesRequest",
}) as any as S.Schema<GetNetworkingV1APIResourcesRequest>;

/** categories is a list of the grouped resources this resource belongs to (e.g. 'all') */
export type IoK8sApimachineryPkgApisMetaV1APIResourceCategoriesList =
  Array<string>;
export const IoK8sApimachineryPkgApisMetaV1APIResourceCategoriesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1APIResourceCategoriesList>;

/** shortNames is a list of suggested short names of the resource. */
export type IoK8sApimachineryPkgApisMetaV1APIResourceShortNamesList =
  Array<string>;
export const IoK8sApimachineryPkgApisMetaV1APIResourceShortNamesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1APIResourceShortNamesList>;

/** verbs is a list of supported kube verbs (this includes get, list, watch, create, update, patch, delete, deletecollection, and proxy) */
export type IoK8sApimachineryPkgApisMetaV1APIResourceVerbsList = Array<string>;
export const IoK8sApimachineryPkgApisMetaV1APIResourceVerbsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1APIResourceVerbsList>;

/** APIResource specifies the name of a resource and whether it is namespaced. */
export interface IoK8sApimachineryPkgApisMetaV1APIResource {
  /** categories is a list of the grouped resources this resource belongs to (e.g. 'all') */
  categories?: IoK8sApimachineryPkgApisMetaV1APIResourceCategoriesList;
  /** group is the preferred group of the resource. Empty implies the group of the containing resource list. For subresources, this may have a different value, for example: Scale". */
  group?: string;
  /** kind is the kind for the resource (e.g. 'Foo' is the kind for a resource 'foo') */
  kind: string;
  /** name is the plural name of the resource. */
  name: string;
  /** namespaced indicates if a resource is namespaced or not. */
  namespaced: boolean;
  /** shortNames is a list of suggested short names of the resource. */
  shortNames?: IoK8sApimachineryPkgApisMetaV1APIResourceShortNamesList;
  /** singularName is the singular name of the resource. This allows clients to handle plural and singular opaquely. The singularName is more correct for reporting status on a single item and both singular and plural are allowed from the kubectl CLI interface. */
  singularName: string;
  /** The hash value of the storage version, the version this resource is converted to when written to the data store. Value must be treated as opaque by clients. Only equality comparison on the value is valid. This is an alpha feature and may change or be removed in the future. The field is populated by the apiserver only if the StorageVersionHash feature gate is enabled. This field will remain optional even if it graduates. */
  storageVersionHash?: string;
  /** verbs is a list of supported kube verbs (this includes get, list, watch, create, update, patch, delete, deletecollection, and proxy) */
  verbs: IoK8sApimachineryPkgApisMetaV1APIResourceVerbsList;
  /** version is the preferred version of the resource. Empty implies the version of the containing resource list For subresources, this may have a different value, for example: v1 (while inside a v1beta1 version of the core resource's group)". */
  version?: string;
}
export const IoK8sApimachineryPkgApisMetaV1APIResource =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      categories: S.optional(
        IoK8sApimachineryPkgApisMetaV1APIResourceCategoriesList,
      ),
      group: S.optional(S.String),
      kind: S.String,
      name: S.String,
      namespaced: S.Boolean,
      shortNames: S.optional(
        IoK8sApimachineryPkgApisMetaV1APIResourceShortNamesList,
      ),
      singularName: S.String,
      storageVersionHash: S.optional(S.String),
      verbs: IoK8sApimachineryPkgApisMetaV1APIResourceVerbsList,
      version: S.optional(S.String),
    }),
  ).annotate({
    identifier: "IoK8sApimachineryPkgApisMetaV1APIResource",
  }) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1APIResource>;

/** resources contains the name of the resources and if they are namespaced. */
export type IoK8sApimachineryPkgApisMetaV1APIResourceListResourcesList =
  Array<IoK8sApimachineryPkgApisMetaV1APIResource>;
export const IoK8sApimachineryPkgApisMetaV1APIResourceListResourcesList =
  /*@__PURE__*/ S.Array(
    IoK8sApimachineryPkgApisMetaV1APIResource,
  ) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1APIResourceListResourcesList>;

/** APIResourceList is a list of APIResource, it is used to expose the name of the resources supported in a specific group and version, and if the resource is namespaced. */
export interface IoK8sApimachineryPkgApisMetaV1APIResourceList {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** groupVersion is the group and version this APIResourceList is for. */
  groupVersion: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** resources contains the name of the resources and if they are namespaced. */
  resources: IoK8sApimachineryPkgApisMetaV1APIResourceListResourcesList;
}
export const IoK8sApimachineryPkgApisMetaV1APIResourceList =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      apiVersion: S.optional(S.String),
      groupVersion: S.String,
      kind: S.optional(S.String),
      resources: IoK8sApimachineryPkgApisMetaV1APIResourceListResourcesList,
    }),
  ).annotate({
    identifier: "IoK8sApimachineryPkgApisMetaV1APIResourceList",
  }) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1APIResourceList>;

export interface ListNetworkingV1IngressClassRequest {
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const ListNetworkingV1IngressClassRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pretty: S.optional(S.String.pipe(T.Query())),
    allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
    continue: S.optional(S.String.pipe(T.Query())),
    fieldSelector: S.optional(S.String.pipe(T.Query())),
    labelSelector: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    resourceVersion: S.optional(S.String.pipe(T.Query())),
    resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
    sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
    shardSelector: S.optional(S.String.pipe(T.Query())),
    timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
    watch: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/networking.k8s.io/v1/ingressclasses",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListNetworkingV1IngressClassRequest",
}) as any as S.Schema<ListNetworkingV1IngressClassRequest>;

/** items is the list of IngressClasses. */
export type IoK8sApiNetworkingV1IngressClassListItemsList =
  Array<IoK8sApiNetworkingV1IngressClass>;
export const IoK8sApiNetworkingV1IngressClassListItemsList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1IngressClass,
  ) as any as S.Schema<IoK8sApiNetworkingV1IngressClassListItemsList>;

/** IngressClassList is a collection of IngressClasses. */
export interface IoK8sApiNetworkingV1IngressClassList {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** items is the list of IngressClasses. */
  items: IoK8sApiNetworkingV1IngressClassListItemsList;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard list metadata. */
  metadata?: IoK8sApimachineryPkgApisMetaV1ListMeta;
}
export const IoK8sApiNetworkingV1IngressClassList = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      apiVersion: S.optional(S.String),
      items: IoK8sApiNetworkingV1IngressClassListItemsList,
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ListMeta),
    }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IngressClassList",
}) as any as S.Schema<IoK8sApiNetworkingV1IngressClassList>;

export interface ListNetworkingV1IngressForAllNamespacesRequest {
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const ListNetworkingV1IngressForAllNamespacesRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/ingresses",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "ListNetworkingV1IngressForAllNamespacesRequest",
  }) as any as S.Schema<ListNetworkingV1IngressForAllNamespacesRequest>;

/** items is the list of Ingress. */
export type IoK8sApiNetworkingV1IngressListItemsList =
  Array<IoK8sApiNetworkingV1Ingress>;
export const IoK8sApiNetworkingV1IngressListItemsList = /*@__PURE__*/ S.Array(
  IoK8sApiNetworkingV1Ingress,
) as any as S.Schema<IoK8sApiNetworkingV1IngressListItemsList>;

/** IngressList is a collection of Ingress. */
export interface IoK8sApiNetworkingV1IngressList {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** items is the list of Ingress. */
  items: IoK8sApiNetworkingV1IngressListItemsList;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ListMeta;
}
export const IoK8sApiNetworkingV1IngressList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    items: IoK8sApiNetworkingV1IngressListItemsList,
    kind: S.optional(S.String),
    metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ListMeta),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IngressList",
}) as any as S.Schema<IoK8sApiNetworkingV1IngressList>;

export interface ListNetworkingV1IPAddressRequest {
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const ListNetworkingV1IPAddressRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pretty: S.optional(S.String.pipe(T.Query())),
    allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
    continue: S.optional(S.String.pipe(T.Query())),
    fieldSelector: S.optional(S.String.pipe(T.Query())),
    labelSelector: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    resourceVersion: S.optional(S.String.pipe(T.Query())),
    resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
    sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
    shardSelector: S.optional(S.String.pipe(T.Query())),
    timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
    watch: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/networking.k8s.io/v1/ipaddresses",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListNetworkingV1IPAddressRequest",
}) as any as S.Schema<ListNetworkingV1IPAddressRequest>;

/** items is the list of IPAddresses. */
export type IoK8sApiNetworkingV1IPAddressListItemsList =
  Array<IoK8sApiNetworkingV1IPAddress>;
export const IoK8sApiNetworkingV1IPAddressListItemsList = /*@__PURE__*/ S.Array(
  IoK8sApiNetworkingV1IPAddress,
) as any as S.Schema<IoK8sApiNetworkingV1IPAddressListItemsList>;

/** IPAddressList contains a list of IPAddress. */
export interface IoK8sApiNetworkingV1IPAddressList {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** items is the list of IPAddresses. */
  items: IoK8sApiNetworkingV1IPAddressListItemsList;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ListMeta;
}
export const IoK8sApiNetworkingV1IPAddressList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    items: IoK8sApiNetworkingV1IPAddressListItemsList,
    kind: S.optional(S.String),
    metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ListMeta),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1IPAddressList",
}) as any as S.Schema<IoK8sApiNetworkingV1IPAddressList>;

export interface ListNetworkingV1NamespacedIngressRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const ListNetworkingV1NamespacedIngressRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/ingresses",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ListNetworkingV1NamespacedIngressRequest",
}) as any as S.Schema<ListNetworkingV1NamespacedIngressRequest>;

export interface ListNetworkingV1NamespacedNetworkPolicyRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const ListNetworkingV1NamespacedNetworkPolicyRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/networkpolicies",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "ListNetworkingV1NamespacedNetworkPolicyRequest",
  }) as any as S.Schema<ListNetworkingV1NamespacedNetworkPolicyRequest>;

/** items is a list of schema objects. */
export type IoK8sApiNetworkingV1NetworkPolicyListItemsList =
  Array<IoK8sApiNetworkingV1NetworkPolicy>;
export const IoK8sApiNetworkingV1NetworkPolicyListItemsList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1NetworkPolicy,
  ) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicyListItemsList>;

/** NetworkPolicyList is a list of NetworkPolicy objects. */
export interface IoK8sApiNetworkingV1NetworkPolicyList {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** items is a list of schema objects. */
  items: IoK8sApiNetworkingV1NetworkPolicyListItemsList;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard list metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ListMeta;
}
export const IoK8sApiNetworkingV1NetworkPolicyList = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      apiVersion: S.optional(S.String),
      items: IoK8sApiNetworkingV1NetworkPolicyListItemsList,
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ListMeta),
    }),
).annotate({
  identifier: "IoK8sApiNetworkingV1NetworkPolicyList",
}) as any as S.Schema<IoK8sApiNetworkingV1NetworkPolicyList>;

export interface ListNetworkingV1NetworkPolicyForAllNamespacesRequest {
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const ListNetworkingV1NetworkPolicyForAllNamespacesRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/networkpolicies",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "ListNetworkingV1NetworkPolicyForAllNamespacesRequest",
  }) as any as S.Schema<ListNetworkingV1NetworkPolicyForAllNamespacesRequest>;

export interface ListNetworkingV1ServiceCIDRRequest {
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const ListNetworkingV1ServiceCIDRRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pretty: S.optional(S.String.pipe(T.Query())),
    allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
    continue: S.optional(S.String.pipe(T.Query())),
    fieldSelector: S.optional(S.String.pipe(T.Query())),
    labelSelector: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    resourceVersion: S.optional(S.String.pipe(T.Query())),
    resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
    sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
    shardSelector: S.optional(S.String.pipe(T.Query())),
    timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
    watch: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/networking.k8s.io/v1/servicecidrs",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListNetworkingV1ServiceCIDRRequest",
}) as any as S.Schema<ListNetworkingV1ServiceCIDRRequest>;

/** items is the list of ServiceCIDRs. */
export type IoK8sApiNetworkingV1ServiceCIDRListItemsList =
  Array<IoK8sApiNetworkingV1ServiceCIDR>;
export const IoK8sApiNetworkingV1ServiceCIDRListItemsList =
  /*@__PURE__*/ S.Array(
    IoK8sApiNetworkingV1ServiceCIDR,
  ) as any as S.Schema<IoK8sApiNetworkingV1ServiceCIDRListItemsList>;

/** ServiceCIDRList contains a list of ServiceCIDR objects. */
export interface IoK8sApiNetworkingV1ServiceCIDRList {
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** items is the list of ServiceCIDRs. */
  items: IoK8sApiNetworkingV1ServiceCIDRListItemsList;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ListMeta;
}
export const IoK8sApiNetworkingV1ServiceCIDRList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiVersion: S.optional(S.String),
    items: IoK8sApiNetworkingV1ServiceCIDRListItemsList,
    kind: S.optional(S.String),
    metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ListMeta),
  }),
).annotate({
  identifier: "IoK8sApiNetworkingV1ServiceCIDRList",
}) as any as S.Schema<IoK8sApiNetworkingV1ServiceCIDRList>;

export interface PatchNetworkingV1IngressClassRequest {
  /** name of the IngressClass */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch). */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests. */
  force?: boolean;
}
export const PatchNetworkingV1IngressClassRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      force: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "PATCH",
        uri: "/apis/networking.k8s.io/v1/ingressclasses/{name}",
        code: 200,
      }),
    ),
).annotate({
  identifier: "PatchNetworkingV1IngressClassRequest",
}) as any as S.Schema<PatchNetworkingV1IngressClassRequest>;

export interface PatchNetworkingV1IPAddressRequest {
  /** name of the IPAddress */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch). */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests. */
  force?: boolean;
}
export const PatchNetworkingV1IPAddressRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
    dryRun: S.optional(S.String.pipe(T.Query())),
    fieldManager: S.optional(S.String.pipe(T.Query())),
    fieldValidation: S.optional(S.String.pipe(T.Query())),
    force: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/apis/networking.k8s.io/v1/ipaddresses/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PatchNetworkingV1IPAddressRequest",
}) as any as S.Schema<PatchNetworkingV1IPAddressRequest>;

export interface PatchNetworkingV1NamespacedIngressRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the Ingress */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch). */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests. */
  force?: boolean;
}
export const PatchNetworkingV1NamespacedIngressRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      force: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "PATCH",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/ingresses/{name}",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "PatchNetworkingV1NamespacedIngressRequest",
  }) as any as S.Schema<PatchNetworkingV1NamespacedIngressRequest>;

export interface PatchNetworkingV1NamespacedIngressStatusRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the Ingress */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch). */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests. */
  force?: boolean;
}
export const PatchNetworkingV1NamespacedIngressStatusRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      force: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "PATCH",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/ingresses/{name}/status",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "PatchNetworkingV1NamespacedIngressStatusRequest",
  }) as any as S.Schema<PatchNetworkingV1NamespacedIngressStatusRequest>;

export interface PatchNetworkingV1NamespacedNetworkPolicyRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the NetworkPolicy */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch). */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests. */
  force?: boolean;
}
export const PatchNetworkingV1NamespacedNetworkPolicyRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      force: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "PATCH",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/networkpolicies/{name}",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "PatchNetworkingV1NamespacedNetworkPolicyRequest",
  }) as any as S.Schema<PatchNetworkingV1NamespacedNetworkPolicyRequest>;

export interface PatchNetworkingV1ServiceCIDRRequest {
  /** name of the ServiceCIDR */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch). */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests. */
  force?: boolean;
}
export const PatchNetworkingV1ServiceCIDRRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
    dryRun: S.optional(S.String.pipe(T.Query())),
    fieldManager: S.optional(S.String.pipe(T.Query())),
    fieldValidation: S.optional(S.String.pipe(T.Query())),
    force: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/apis/networking.k8s.io/v1/servicecidrs/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "PatchNetworkingV1ServiceCIDRRequest",
}) as any as S.Schema<PatchNetworkingV1ServiceCIDRRequest>;

export interface PatchNetworkingV1ServiceCIDRStatusRequest {
  /** name of the ServiceCIDR */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch). */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests. */
  force?: boolean;
}
export const PatchNetworkingV1ServiceCIDRStatusRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      force: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "PATCH",
        uri: "/apis/networking.k8s.io/v1/servicecidrs/{name}/status",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "PatchNetworkingV1ServiceCIDRStatusRequest",
  }) as any as S.Schema<PatchNetworkingV1ServiceCIDRStatusRequest>;

export interface ReadNetworkingV1IngressClassRequest {
  /** name of the IngressClass */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
}
export const ReadNetworkingV1IngressClassRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/networking.k8s.io/v1/ingressclasses/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ReadNetworkingV1IngressClassRequest",
}) as any as S.Schema<ReadNetworkingV1IngressClassRequest>;

export interface ReadNetworkingV1IPAddressRequest {
  /** name of the IPAddress */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
}
export const ReadNetworkingV1IPAddressRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/networking.k8s.io/v1/ipaddresses/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ReadNetworkingV1IPAddressRequest",
}) as any as S.Schema<ReadNetworkingV1IPAddressRequest>;

export interface ReadNetworkingV1NamespacedIngressRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the Ingress */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
}
export const ReadNetworkingV1NamespacedIngressRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/ingresses/{name}",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ReadNetworkingV1NamespacedIngressRequest",
}) as any as S.Schema<ReadNetworkingV1NamespacedIngressRequest>;

export interface ReadNetworkingV1NamespacedIngressStatusRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the Ingress */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
}
export const ReadNetworkingV1NamespacedIngressStatusRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/ingresses/{name}/status",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "ReadNetworkingV1NamespacedIngressStatusRequest",
  }) as any as S.Schema<ReadNetworkingV1NamespacedIngressStatusRequest>;

export interface ReadNetworkingV1NamespacedNetworkPolicyRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the NetworkPolicy */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
}
export const ReadNetworkingV1NamespacedNetworkPolicyRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/networkpolicies/{name}",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "ReadNetworkingV1NamespacedNetworkPolicyRequest",
  }) as any as S.Schema<ReadNetworkingV1NamespacedNetworkPolicyRequest>;

export interface ReadNetworkingV1ServiceCIDRRequest {
  /** name of the ServiceCIDR */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
}
export const ReadNetworkingV1ServiceCIDRRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/networking.k8s.io/v1/servicecidrs/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ReadNetworkingV1ServiceCIDRRequest",
}) as any as S.Schema<ReadNetworkingV1ServiceCIDRRequest>;

export interface ReadNetworkingV1ServiceCIDRStatusRequest {
  /** name of the ServiceCIDR */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
}
export const ReadNetworkingV1ServiceCIDRStatusRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/servicecidrs/{name}/status",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ReadNetworkingV1ServiceCIDRStatusRequest",
}) as any as S.Schema<ReadNetworkingV1ServiceCIDRStatusRequest>;

export interface ReplaceNetworkingV1IngressClassRequest {
  /** name of the IngressClass */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the IngressClass. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec?: IoK8sApiNetworkingV1IngressClassSpec;
}
export const ReplaceNetworkingV1IngressClassRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
      spec: S.optional(IoK8sApiNetworkingV1IngressClassSpec),
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/apis/networking.k8s.io/v1/ingressclasses/{name}",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ReplaceNetworkingV1IngressClassRequest",
}) as any as S.Schema<ReplaceNetworkingV1IngressClassRequest>;

export interface ReplaceNetworkingV1IPAddressRequest {
  /** name of the IPAddress */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the IPAddress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec: IoK8sApiNetworkingV1IPAddressSpec;
}
export const ReplaceNetworkingV1IPAddressRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    pretty: S.optional(S.String.pipe(T.Query())),
    dryRun: S.optional(S.String.pipe(T.Query())),
    fieldManager: S.optional(S.String.pipe(T.Query())),
    fieldValidation: S.optional(S.String.pipe(T.Query())),
    apiVersion: S.optional(S.String),
    kind: S.optional(S.String),
    metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
    spec: IoK8sApiNetworkingV1IPAddressSpec,
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/apis/networking.k8s.io/v1/ipaddresses/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ReplaceNetworkingV1IPAddressRequest",
}) as any as S.Schema<ReplaceNetworkingV1IPAddressRequest>;

export interface ReplaceNetworkingV1NamespacedIngressRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the Ingress */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the Ingress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec?: IoK8sApiNetworkingV1IngressSpec;
  /** status is the current state of the Ingress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  status?: IoK8sApiNetworkingV1IngressStatus;
}
export const ReplaceNetworkingV1NamespacedIngressRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
      spec: S.optional(IoK8sApiNetworkingV1IngressSpec),
      status: S.optional(IoK8sApiNetworkingV1IngressStatus),
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/ingresses/{name}",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "ReplaceNetworkingV1NamespacedIngressRequest",
  }) as any as S.Schema<ReplaceNetworkingV1NamespacedIngressRequest>;

export interface ReplaceNetworkingV1NamespacedIngressStatusRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the Ingress */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the Ingress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec?: IoK8sApiNetworkingV1IngressSpec;
  /** status is the current state of the Ingress. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  status?: IoK8sApiNetworkingV1IngressStatus;
}
export const ReplaceNetworkingV1NamespacedIngressStatusRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
      spec: S.optional(IoK8sApiNetworkingV1IngressSpec),
      status: S.optional(IoK8sApiNetworkingV1IngressStatus),
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/ingresses/{name}/status",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "ReplaceNetworkingV1NamespacedIngressStatusRequest",
  }) as any as S.Schema<ReplaceNetworkingV1NamespacedIngressStatusRequest>;

export interface ReplaceNetworkingV1NamespacedNetworkPolicyRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the NetworkPolicy */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec represents the specification of the desired behavior for this NetworkPolicy. */
  spec?: IoK8sApiNetworkingV1NetworkPolicySpec;
}
export const ReplaceNetworkingV1NamespacedNetworkPolicyRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
      spec: S.optional(IoK8sApiNetworkingV1NetworkPolicySpec),
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/apis/networking.k8s.io/v1/namespaces/{namespace}/networkpolicies/{name}",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "ReplaceNetworkingV1NamespacedNetworkPolicyRequest",
  }) as any as S.Schema<ReplaceNetworkingV1NamespacedNetworkPolicyRequest>;

export interface ReplaceNetworkingV1ServiceCIDRRequest {
  /** name of the ServiceCIDR */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the ServiceCIDR. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec?: IoK8sApiNetworkingV1ServiceCIDRSpec;
  /** status represents the current state of the ServiceCIDR. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  status?: IoK8sApiNetworkingV1ServiceCIDRStatus;
}
export const ReplaceNetworkingV1ServiceCIDRRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
      spec: S.optional(IoK8sApiNetworkingV1ServiceCIDRSpec),
      status: S.optional(IoK8sApiNetworkingV1ServiceCIDRStatus),
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/apis/networking.k8s.io/v1/servicecidrs/{name}",
        code: 200,
      }),
    ),
).annotate({
  identifier: "ReplaceNetworkingV1ServiceCIDRRequest",
}) as any as S.Schema<ReplaceNetworkingV1ServiceCIDRRequest>;

export interface ReplaceNetworkingV1ServiceCIDRStatusRequest {
  /** name of the ServiceCIDR */
  name: string;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed */
  dryRun?: string;
  /** fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. */
  fieldManager?: string;
  /** fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered. */
  fieldValidation?: string;
  /** APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources */
  apiVersion?: string;
  /** Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds */
  kind?: string;
  /** Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata */
  metadata?: IoK8sApimachineryPkgApisMetaV1ObjectMeta;
  /** spec is the desired state of the ServiceCIDR. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  spec?: IoK8sApiNetworkingV1ServiceCIDRSpec;
  /** status represents the current state of the ServiceCIDR. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status */
  status?: IoK8sApiNetworkingV1ServiceCIDRStatus;
}
export const ReplaceNetworkingV1ServiceCIDRStatusRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      name: S.String.pipe(T.Label()),
      pretty: S.optional(S.String.pipe(T.Query())),
      dryRun: S.optional(S.String.pipe(T.Query())),
      fieldManager: S.optional(S.String.pipe(T.Query())),
      fieldValidation: S.optional(S.String.pipe(T.Query())),
      apiVersion: S.optional(S.String),
      kind: S.optional(S.String),
      metadata: S.optional(IoK8sApimachineryPkgApisMetaV1ObjectMeta),
      spec: S.optional(IoK8sApiNetworkingV1ServiceCIDRSpec),
      status: S.optional(IoK8sApiNetworkingV1ServiceCIDRStatus),
    }).pipe(
      T.Http({
        method: "PUT",
        uri: "/apis/networking.k8s.io/v1/servicecidrs/{name}/status",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "ReplaceNetworkingV1ServiceCIDRStatusRequest",
  }) as any as S.Schema<ReplaceNetworkingV1ServiceCIDRStatusRequest>;

export interface WatchNetworkingV1IngressClassRequest {
  /** name of the IngressClass */
  name: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1IngressClassRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      name: S.String.pipe(T.Label()),
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/watch/ingressclasses/{name}",
        code: 200,
      }),
    ),
).annotate({
  identifier: "WatchNetworkingV1IngressClassRequest",
}) as any as S.Schema<WatchNetworkingV1IngressClassRequest>;

/** Event represents a single event to a watched resource. */
export interface IoK8sApimachineryPkgApisMetaV1WatchEvent {
  /** Object is: * If Type is Added or Modified: the new state of the object. * If Type is Deleted: the state of the object immediately before deletion. * If Type is Error: *Status is recommended; other types may make sense depending on context. */
  object: unknown;
  type: string;
}
export const IoK8sApimachineryPkgApisMetaV1WatchEvent = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      object: S.Unknown,
      type: S.String,
    }),
).annotate({
  identifier: "IoK8sApimachineryPkgApisMetaV1WatchEvent",
}) as any as S.Schema<IoK8sApimachineryPkgApisMetaV1WatchEvent>;

export interface WatchNetworkingV1IngressClassListRequest {
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1IngressClassListRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/watch/ingressclasses",
        code: 200,
      }),
    ),
).annotate({
  identifier: "WatchNetworkingV1IngressClassListRequest",
}) as any as S.Schema<WatchNetworkingV1IngressClassListRequest>;

export interface WatchNetworkingV1IngressListForAllNamespacesRequest {
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1IngressListForAllNamespacesRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/watch/ingresses",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "WatchNetworkingV1IngressListForAllNamespacesRequest",
  }) as any as S.Schema<WatchNetworkingV1IngressListForAllNamespacesRequest>;

export interface WatchNetworkingV1IPAddressRequest {
  /** name of the IPAddress */
  name: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1IPAddressRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
    continue: S.optional(S.String.pipe(T.Query())),
    fieldSelector: S.optional(S.String.pipe(T.Query())),
    labelSelector: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    pretty: S.optional(S.String.pipe(T.Query())),
    resourceVersion: S.optional(S.String.pipe(T.Query())),
    resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
    sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
    shardSelector: S.optional(S.String.pipe(T.Query())),
    timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
    watch: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/networking.k8s.io/v1/watch/ipaddresses/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "WatchNetworkingV1IPAddressRequest",
}) as any as S.Schema<WatchNetworkingV1IPAddressRequest>;

export interface WatchNetworkingV1IPAddressListRequest {
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1IPAddressListRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/watch/ipaddresses",
        code: 200,
      }),
    ),
).annotate({
  identifier: "WatchNetworkingV1IPAddressListRequest",
}) as any as S.Schema<WatchNetworkingV1IPAddressListRequest>;

export interface WatchNetworkingV1NamespacedIngressRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the Ingress */
  name: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1NamespacedIngressRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/watch/namespaces/{namespace}/ingresses/{name}",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "WatchNetworkingV1NamespacedIngressRequest",
  }) as any as S.Schema<WatchNetworkingV1NamespacedIngressRequest>;

export interface WatchNetworkingV1NamespacedIngressListRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1NamespacedIngressListRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/watch/namespaces/{namespace}/ingresses",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "WatchNetworkingV1NamespacedIngressListRequest",
  }) as any as S.Schema<WatchNetworkingV1NamespacedIngressListRequest>;

export interface WatchNetworkingV1NamespacedNetworkPolicyRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** name of the NetworkPolicy */
  name: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1NamespacedNetworkPolicyRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      name: S.String.pipe(T.Label()),
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/watch/namespaces/{namespace}/networkpolicies/{name}",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "WatchNetworkingV1NamespacedNetworkPolicyRequest",
  }) as any as S.Schema<WatchNetworkingV1NamespacedNetworkPolicyRequest>;

export interface WatchNetworkingV1NamespacedNetworkPolicyListRequest {
  /** object name and auth scope, such as for teams and projects */
  namespace: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1NamespacedNetworkPolicyListRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      namespace: S.String.pipe(T.Label()),
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/watch/namespaces/{namespace}/networkpolicies",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "WatchNetworkingV1NamespacedNetworkPolicyListRequest",
  }) as any as S.Schema<WatchNetworkingV1NamespacedNetworkPolicyListRequest>;

export interface WatchNetworkingV1NetworkPolicyListForAllNamespacesRequest {
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1NetworkPolicyListForAllNamespacesRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/watch/networkpolicies",
        code: 200,
      }),
    ),
  ).annotate({
    identifier: "WatchNetworkingV1NetworkPolicyListForAllNamespacesRequest",
  }) as any as S.Schema<WatchNetworkingV1NetworkPolicyListForAllNamespacesRequest>;

export interface WatchNetworkingV1ServiceCIDRRequest {
  /** name of the ServiceCIDR */
  name: string;
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1ServiceCIDRRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
    allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
    continue: S.optional(S.String.pipe(T.Query())),
    fieldSelector: S.optional(S.String.pipe(T.Query())),
    labelSelector: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    pretty: S.optional(S.String.pipe(T.Query())),
    resourceVersion: S.optional(S.String.pipe(T.Query())),
    resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
    sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
    shardSelector: S.optional(S.String.pipe(T.Query())),
    timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
    watch: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/apis/networking.k8s.io/v1/watch/servicecidrs/{name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "WatchNetworkingV1ServiceCIDRRequest",
}) as any as S.Schema<WatchNetworkingV1ServiceCIDRRequest>;

export interface WatchNetworkingV1ServiceCIDRListRequest {
  /** allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored. */
  allowWatchBookmarks?: boolean;
  /** The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key". This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications. */
  continue?: string;
  /** A selector to restrict the list of returned objects by their fields. Defaults to everything. */
  fieldSelector?: string;
  /** A selector to restrict the list of returned objects by their labels. Defaults to everything. */
  labelSelector?: string;
  /** limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true. The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned. */
  limit?: number;
  /** If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget). */
  pretty?: string;
  /** resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersion?: string;
  /** resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details. Defaults to unset */
  resourceVersionMatch?: string;
  /** `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched. When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan is interpreted as "data at least as new as the provided `resourceVersion`" and the bookmark event is send when the state is synced to a `resourceVersion` at least as fresh as the one provided by the ListOptions. If `resourceVersion` is unset, this is interpreted as "consistent read" and the bookmark event is send when the state is synced at least to the moment when request started being processed. - `resourceVersionMatch` set to any other value or unset Invalid error is returned. Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise. */
  sendInitialEvents?: boolean;
  /** shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges: shardRange(object.metadata.uid, '0x0', '0x8000000000000000') shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths: - object.metadata.uid - object.metadata.namespace hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64. Examples: 2-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000') shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000') 4-shard split: shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000') shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000') shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000') shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000') This is an alpha field and requires enabling the ShardedListAndWatch feature gate. */
  shardSelector?: string;
  /** Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity. */
  timeoutSeconds?: number;
  /** Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion. */
  watch?: boolean;
}
export const WatchNetworkingV1ServiceCIDRListRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      allowWatchBookmarks: S.optional(S.Boolean.pipe(T.Query())),
      continue: S.optional(S.String.pipe(T.Query())),
      fieldSelector: S.optional(S.String.pipe(T.Query())),
      labelSelector: S.optional(S.String.pipe(T.Query())),
      limit: S.optional(S.Number.pipe(T.Query())),
      pretty: S.optional(S.String.pipe(T.Query())),
      resourceVersion: S.optional(S.String.pipe(T.Query())),
      resourceVersionMatch: S.optional(S.String.pipe(T.Query())),
      sendInitialEvents: S.optional(S.Boolean.pipe(T.Query())),
      shardSelector: S.optional(S.String.pipe(T.Query())),
      timeoutSeconds: S.optional(S.Number.pipe(T.Query())),
      watch: S.optional(S.Boolean.pipe(T.Query())),
    }).pipe(
      T.Http({
        method: "GET",
        uri: "/apis/networking.k8s.io/v1/watch/servicecidrs",
        code: 200,
      }),
    ),
).annotate({
  identifier: "WatchNetworkingV1ServiceCIDRListRequest",
}) as any as S.Schema<WatchNetworkingV1ServiceCIDRListRequest>;

export type CreateNetworkingV1IngressClassError =
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** create an IngressClass */
export const createNetworkingV1IngressClass: API.OperationMethod<
  CreateNetworkingV1IngressClassRequest,
  IoK8sApiNetworkingV1IngressClass,
  CreateNetworkingV1IngressClassError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateNetworkingV1IngressClassRequest,
  output: IoK8sApiNetworkingV1IngressClass,
  errors: [Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type CreateNetworkingV1IPAddressError =
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** create an IPAddress */
export const createNetworkingV1IPAddress: API.OperationMethod<
  CreateNetworkingV1IPAddressRequest,
  IoK8sApiNetworkingV1IPAddress,
  CreateNetworkingV1IPAddressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateNetworkingV1IPAddressRequest,
  output: IoK8sApiNetworkingV1IPAddress,
  errors: [Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type CreateNetworkingV1NamespacedIngressError =
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** create an Ingress */
export const createNetworkingV1NamespacedIngress: API.OperationMethod<
  CreateNetworkingV1NamespacedIngressRequest,
  IoK8sApiNetworkingV1Ingress,
  CreateNetworkingV1NamespacedIngressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateNetworkingV1NamespacedIngressRequest,
  output: IoK8sApiNetworkingV1Ingress,
  errors: [Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type CreateNetworkingV1NamespacedNetworkPolicyError =
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** create a NetworkPolicy */
export const createNetworkingV1NamespacedNetworkPolicy: API.OperationMethod<
  CreateNetworkingV1NamespacedNetworkPolicyRequest,
  IoK8sApiNetworkingV1NetworkPolicy,
  CreateNetworkingV1NamespacedNetworkPolicyError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateNetworkingV1NamespacedNetworkPolicyRequest,
  output: IoK8sApiNetworkingV1NetworkPolicy,
  errors: [Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type CreateNetworkingV1ServiceCIDRError =
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** create a ServiceCIDR */
export const createNetworkingV1ServiceCIDR: API.OperationMethod<
  CreateNetworkingV1ServiceCIDRRequest,
  IoK8sApiNetworkingV1ServiceCIDR,
  CreateNetworkingV1ServiceCIDRError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateNetworkingV1ServiceCIDRRequest,
  output: IoK8sApiNetworkingV1ServiceCIDR,
  errors: [Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type DeleteNetworkingV1CollectionIngressClassError = KubernetesOpError;
/** delete collection of IngressClass */
export const deleteNetworkingV1CollectionIngressClass: API.OperationMethod<
  DeleteNetworkingV1CollectionIngressClassRequest,
  IoK8sApimachineryPkgApisMetaV1Status,
  DeleteNetworkingV1CollectionIngressClassError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNetworkingV1CollectionIngressClassRequest,
  output: IoK8sApimachineryPkgApisMetaV1Status,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type DeleteNetworkingV1CollectionIPAddressError = KubernetesOpError;
/** delete collection of IPAddress */
export const deleteNetworkingV1CollectionIPAddress: API.OperationMethod<
  DeleteNetworkingV1CollectionIPAddressRequest,
  IoK8sApimachineryPkgApisMetaV1Status,
  DeleteNetworkingV1CollectionIPAddressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNetworkingV1CollectionIPAddressRequest,
  output: IoK8sApimachineryPkgApisMetaV1Status,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type DeleteNetworkingV1CollectionNamespacedIngressError =
  KubernetesOpError;
/** delete collection of Ingress */
export const deleteNetworkingV1CollectionNamespacedIngress: API.OperationMethod<
  DeleteNetworkingV1CollectionNamespacedIngressRequest,
  IoK8sApimachineryPkgApisMetaV1Status,
  DeleteNetworkingV1CollectionNamespacedIngressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNetworkingV1CollectionNamespacedIngressRequest,
  output: IoK8sApimachineryPkgApisMetaV1Status,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type DeleteNetworkingV1CollectionNamespacedNetworkPolicyError =
  KubernetesOpError;
/** delete collection of NetworkPolicy */
export const deleteNetworkingV1CollectionNamespacedNetworkPolicy: API.OperationMethod<
  DeleteNetworkingV1CollectionNamespacedNetworkPolicyRequest,
  IoK8sApimachineryPkgApisMetaV1Status,
  DeleteNetworkingV1CollectionNamespacedNetworkPolicyError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNetworkingV1CollectionNamespacedNetworkPolicyRequest,
  output: IoK8sApimachineryPkgApisMetaV1Status,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type DeleteNetworkingV1CollectionServiceCIDRError = KubernetesOpError;
/** delete collection of ServiceCIDR */
export const deleteNetworkingV1CollectionServiceCIDR: API.OperationMethod<
  DeleteNetworkingV1CollectionServiceCIDRRequest,
  IoK8sApimachineryPkgApisMetaV1Status,
  DeleteNetworkingV1CollectionServiceCIDRError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNetworkingV1CollectionServiceCIDRRequest,
  output: IoK8sApimachineryPkgApisMetaV1Status,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type DeleteNetworkingV1IngressClassError =
  | NotFound
  | Conflict
  | KubernetesOpError;
/** delete an IngressClass */
export const deleteNetworkingV1IngressClass: API.OperationMethod<
  DeleteNetworkingV1IngressClassRequest,
  IoK8sApimachineryPkgApisMetaV1Status,
  DeleteNetworkingV1IngressClassError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNetworkingV1IngressClassRequest,
  output: IoK8sApimachineryPkgApisMetaV1Status,
  errors: [NotFound, Conflict, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type DeleteNetworkingV1IPAddressError =
  | NotFound
  | Conflict
  | KubernetesOpError;
/** delete an IPAddress */
export const deleteNetworkingV1IPAddress: API.OperationMethod<
  DeleteNetworkingV1IPAddressRequest,
  IoK8sApimachineryPkgApisMetaV1Status,
  DeleteNetworkingV1IPAddressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNetworkingV1IPAddressRequest,
  output: IoK8sApimachineryPkgApisMetaV1Status,
  errors: [NotFound, Conflict, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type DeleteNetworkingV1NamespacedIngressError =
  | NotFound
  | Conflict
  | KubernetesOpError;
/** delete an Ingress */
export const deleteNetworkingV1NamespacedIngress: API.OperationMethod<
  DeleteNetworkingV1NamespacedIngressRequest,
  IoK8sApimachineryPkgApisMetaV1Status,
  DeleteNetworkingV1NamespacedIngressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNetworkingV1NamespacedIngressRequest,
  output: IoK8sApimachineryPkgApisMetaV1Status,
  errors: [NotFound, Conflict, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type DeleteNetworkingV1NamespacedNetworkPolicyError =
  | NotFound
  | Conflict
  | KubernetesOpError;
/** delete a NetworkPolicy */
export const deleteNetworkingV1NamespacedNetworkPolicy: API.OperationMethod<
  DeleteNetworkingV1NamespacedNetworkPolicyRequest,
  IoK8sApimachineryPkgApisMetaV1Status,
  DeleteNetworkingV1NamespacedNetworkPolicyError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNetworkingV1NamespacedNetworkPolicyRequest,
  output: IoK8sApimachineryPkgApisMetaV1Status,
  errors: [NotFound, Conflict, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type DeleteNetworkingV1ServiceCIDRError =
  | NotFound
  | Conflict
  | KubernetesOpError;
/** delete a ServiceCIDR */
export const deleteNetworkingV1ServiceCIDR: API.OperationMethod<
  DeleteNetworkingV1ServiceCIDRRequest,
  IoK8sApimachineryPkgApisMetaV1Status,
  DeleteNetworkingV1ServiceCIDRError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNetworkingV1ServiceCIDRRequest,
  output: IoK8sApimachineryPkgApisMetaV1Status,
  errors: [NotFound, Conflict, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type GetNetworkingAPIGroupError = KubernetesOpError;
/** get information of a group */
export const getNetworkingAPIGroup: API.OperationMethod<
  GetNetworkingAPIGroupRequest,
  IoK8sApimachineryPkgApisMetaV1APIGroup,
  GetNetworkingAPIGroupError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetNetworkingAPIGroupRequest,
  output: IoK8sApimachineryPkgApisMetaV1APIGroup,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type GetNetworkingV1APIResourcesError = KubernetesOpError;
/** get available resources */
export const getNetworkingV1APIResources: API.OperationMethod<
  GetNetworkingV1APIResourcesRequest,
  IoK8sApimachineryPkgApisMetaV1APIResourceList,
  GetNetworkingV1APIResourcesError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetNetworkingV1APIResourcesRequest,
  output: IoK8sApimachineryPkgApisMetaV1APIResourceList,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ListNetworkingV1IngressClassError = KubernetesOpError;
/** list or watch objects of kind IngressClass */
export const listNetworkingV1IngressClass: API.OperationMethod<
  ListNetworkingV1IngressClassRequest,
  IoK8sApiNetworkingV1IngressClassList,
  ListNetworkingV1IngressClassError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListNetworkingV1IngressClassRequest,
  output: IoK8sApiNetworkingV1IngressClassList,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ListNetworkingV1IngressForAllNamespacesError = KubernetesOpError;
/** list or watch objects of kind Ingress */
export const listNetworkingV1IngressForAllNamespaces: API.OperationMethod<
  ListNetworkingV1IngressForAllNamespacesRequest,
  IoK8sApiNetworkingV1IngressList,
  ListNetworkingV1IngressForAllNamespacesError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListNetworkingV1IngressForAllNamespacesRequest,
  output: IoK8sApiNetworkingV1IngressList,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ListNetworkingV1IPAddressError = KubernetesOpError;
/** list or watch objects of kind IPAddress */
export const listNetworkingV1IPAddress: API.OperationMethod<
  ListNetworkingV1IPAddressRequest,
  IoK8sApiNetworkingV1IPAddressList,
  ListNetworkingV1IPAddressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListNetworkingV1IPAddressRequest,
  output: IoK8sApiNetworkingV1IPAddressList,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ListNetworkingV1NamespacedIngressError = KubernetesOpError;
/** list or watch objects of kind Ingress */
export const listNetworkingV1NamespacedIngress: API.OperationMethod<
  ListNetworkingV1NamespacedIngressRequest,
  IoK8sApiNetworkingV1IngressList,
  ListNetworkingV1NamespacedIngressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListNetworkingV1NamespacedIngressRequest,
  output: IoK8sApiNetworkingV1IngressList,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ListNetworkingV1NamespacedNetworkPolicyError = KubernetesOpError;
/** list or watch objects of kind NetworkPolicy */
export const listNetworkingV1NamespacedNetworkPolicy: API.OperationMethod<
  ListNetworkingV1NamespacedNetworkPolicyRequest,
  IoK8sApiNetworkingV1NetworkPolicyList,
  ListNetworkingV1NamespacedNetworkPolicyError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListNetworkingV1NamespacedNetworkPolicyRequest,
  output: IoK8sApiNetworkingV1NetworkPolicyList,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ListNetworkingV1NetworkPolicyForAllNamespacesError =
  KubernetesOpError;
/** list or watch objects of kind NetworkPolicy */
export const listNetworkingV1NetworkPolicyForAllNamespaces: API.OperationMethod<
  ListNetworkingV1NetworkPolicyForAllNamespacesRequest,
  IoK8sApiNetworkingV1NetworkPolicyList,
  ListNetworkingV1NetworkPolicyForAllNamespacesError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListNetworkingV1NetworkPolicyForAllNamespacesRequest,
  output: IoK8sApiNetworkingV1NetworkPolicyList,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ListNetworkingV1ServiceCIDRError = KubernetesOpError;
/** list or watch objects of kind ServiceCIDR */
export const listNetworkingV1ServiceCIDR: API.OperationMethod<
  ListNetworkingV1ServiceCIDRRequest,
  IoK8sApiNetworkingV1ServiceCIDRList,
  ListNetworkingV1ServiceCIDRError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListNetworkingV1ServiceCIDRRequest,
  output: IoK8sApiNetworkingV1ServiceCIDRList,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type PatchNetworkingV1IngressClassError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** partially update the specified IngressClass */
export const patchNetworkingV1IngressClass: API.OperationMethod<
  PatchNetworkingV1IngressClassRequest,
  IoK8sApiNetworkingV1IngressClass,
  PatchNetworkingV1IngressClassError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PatchNetworkingV1IngressClassRequest,
  output: IoK8sApiNetworkingV1IngressClass,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type PatchNetworkingV1IPAddressError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** partially update the specified IPAddress */
export const patchNetworkingV1IPAddress: API.OperationMethod<
  PatchNetworkingV1IPAddressRequest,
  IoK8sApiNetworkingV1IPAddress,
  PatchNetworkingV1IPAddressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PatchNetworkingV1IPAddressRequest,
  output: IoK8sApiNetworkingV1IPAddress,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type PatchNetworkingV1NamespacedIngressError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** partially update the specified Ingress */
export const patchNetworkingV1NamespacedIngress: API.OperationMethod<
  PatchNetworkingV1NamespacedIngressRequest,
  IoK8sApiNetworkingV1Ingress,
  PatchNetworkingV1NamespacedIngressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PatchNetworkingV1NamespacedIngressRequest,
  output: IoK8sApiNetworkingV1Ingress,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type PatchNetworkingV1NamespacedIngressStatusError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** partially update status of the specified Ingress */
export const patchNetworkingV1NamespacedIngressStatus: API.OperationMethod<
  PatchNetworkingV1NamespacedIngressStatusRequest,
  IoK8sApiNetworkingV1Ingress,
  PatchNetworkingV1NamespacedIngressStatusError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PatchNetworkingV1NamespacedIngressStatusRequest,
  output: IoK8sApiNetworkingV1Ingress,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type PatchNetworkingV1NamespacedNetworkPolicyError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** partially update the specified NetworkPolicy */
export const patchNetworkingV1NamespacedNetworkPolicy: API.OperationMethod<
  PatchNetworkingV1NamespacedNetworkPolicyRequest,
  IoK8sApiNetworkingV1NetworkPolicy,
  PatchNetworkingV1NamespacedNetworkPolicyError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PatchNetworkingV1NamespacedNetworkPolicyRequest,
  output: IoK8sApiNetworkingV1NetworkPolicy,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type PatchNetworkingV1ServiceCIDRError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** partially update the specified ServiceCIDR */
export const patchNetworkingV1ServiceCIDR: API.OperationMethod<
  PatchNetworkingV1ServiceCIDRRequest,
  IoK8sApiNetworkingV1ServiceCIDR,
  PatchNetworkingV1ServiceCIDRError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PatchNetworkingV1ServiceCIDRRequest,
  output: IoK8sApiNetworkingV1ServiceCIDR,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type PatchNetworkingV1ServiceCIDRStatusError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** partially update status of the specified ServiceCIDR */
export const patchNetworkingV1ServiceCIDRStatus: API.OperationMethod<
  PatchNetworkingV1ServiceCIDRStatusRequest,
  IoK8sApiNetworkingV1ServiceCIDR,
  PatchNetworkingV1ServiceCIDRStatusError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PatchNetworkingV1ServiceCIDRStatusRequest,
  output: IoK8sApiNetworkingV1ServiceCIDR,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReadNetworkingV1IngressClassError = NotFound | KubernetesOpError;
/** read the specified IngressClass */
export const readNetworkingV1IngressClass: API.OperationMethod<
  ReadNetworkingV1IngressClassRequest,
  IoK8sApiNetworkingV1IngressClass,
  ReadNetworkingV1IngressClassError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReadNetworkingV1IngressClassRequest,
  output: IoK8sApiNetworkingV1IngressClass,
  errors: [NotFound, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReadNetworkingV1IPAddressError = NotFound | KubernetesOpError;
/** read the specified IPAddress */
export const readNetworkingV1IPAddress: API.OperationMethod<
  ReadNetworkingV1IPAddressRequest,
  IoK8sApiNetworkingV1IPAddress,
  ReadNetworkingV1IPAddressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReadNetworkingV1IPAddressRequest,
  output: IoK8sApiNetworkingV1IPAddress,
  errors: [NotFound, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReadNetworkingV1NamespacedIngressError =
  | NotFound
  | KubernetesOpError;
/** read the specified Ingress */
export const readNetworkingV1NamespacedIngress: API.OperationMethod<
  ReadNetworkingV1NamespacedIngressRequest,
  IoK8sApiNetworkingV1Ingress,
  ReadNetworkingV1NamespacedIngressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReadNetworkingV1NamespacedIngressRequest,
  output: IoK8sApiNetworkingV1Ingress,
  errors: [NotFound, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReadNetworkingV1NamespacedIngressStatusError =
  | NotFound
  | KubernetesOpError;
/** read status of the specified Ingress */
export const readNetworkingV1NamespacedIngressStatus: API.OperationMethod<
  ReadNetworkingV1NamespacedIngressStatusRequest,
  IoK8sApiNetworkingV1Ingress,
  ReadNetworkingV1NamespacedIngressStatusError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReadNetworkingV1NamespacedIngressStatusRequest,
  output: IoK8sApiNetworkingV1Ingress,
  errors: [NotFound, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReadNetworkingV1NamespacedNetworkPolicyError =
  | NotFound
  | KubernetesOpError;
/** read the specified NetworkPolicy */
export const readNetworkingV1NamespacedNetworkPolicy: API.OperationMethod<
  ReadNetworkingV1NamespacedNetworkPolicyRequest,
  IoK8sApiNetworkingV1NetworkPolicy,
  ReadNetworkingV1NamespacedNetworkPolicyError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReadNetworkingV1NamespacedNetworkPolicyRequest,
  output: IoK8sApiNetworkingV1NetworkPolicy,
  errors: [NotFound, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReadNetworkingV1ServiceCIDRError = NotFound | KubernetesOpError;
/** read the specified ServiceCIDR */
export const readNetworkingV1ServiceCIDR: API.OperationMethod<
  ReadNetworkingV1ServiceCIDRRequest,
  IoK8sApiNetworkingV1ServiceCIDR,
  ReadNetworkingV1ServiceCIDRError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReadNetworkingV1ServiceCIDRRequest,
  output: IoK8sApiNetworkingV1ServiceCIDR,
  errors: [NotFound, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReadNetworkingV1ServiceCIDRStatusError =
  | NotFound
  | KubernetesOpError;
/** read status of the specified ServiceCIDR */
export const readNetworkingV1ServiceCIDRStatus: API.OperationMethod<
  ReadNetworkingV1ServiceCIDRStatusRequest,
  IoK8sApiNetworkingV1ServiceCIDR,
  ReadNetworkingV1ServiceCIDRStatusError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReadNetworkingV1ServiceCIDRStatusRequest,
  output: IoK8sApiNetworkingV1ServiceCIDR,
  errors: [NotFound, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReplaceNetworkingV1IngressClassError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** replace the specified IngressClass */
export const replaceNetworkingV1IngressClass: API.OperationMethod<
  ReplaceNetworkingV1IngressClassRequest,
  IoK8sApiNetworkingV1IngressClass,
  ReplaceNetworkingV1IngressClassError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReplaceNetworkingV1IngressClassRequest,
  output: IoK8sApiNetworkingV1IngressClass,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReplaceNetworkingV1IPAddressError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** replace the specified IPAddress */
export const replaceNetworkingV1IPAddress: API.OperationMethod<
  ReplaceNetworkingV1IPAddressRequest,
  IoK8sApiNetworkingV1IPAddress,
  ReplaceNetworkingV1IPAddressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReplaceNetworkingV1IPAddressRequest,
  output: IoK8sApiNetworkingV1IPAddress,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReplaceNetworkingV1NamespacedIngressError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** replace the specified Ingress */
export const replaceNetworkingV1NamespacedIngress: API.OperationMethod<
  ReplaceNetworkingV1NamespacedIngressRequest,
  IoK8sApiNetworkingV1Ingress,
  ReplaceNetworkingV1NamespacedIngressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReplaceNetworkingV1NamespacedIngressRequest,
  output: IoK8sApiNetworkingV1Ingress,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReplaceNetworkingV1NamespacedIngressStatusError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** replace status of the specified Ingress */
export const replaceNetworkingV1NamespacedIngressStatus: API.OperationMethod<
  ReplaceNetworkingV1NamespacedIngressStatusRequest,
  IoK8sApiNetworkingV1Ingress,
  ReplaceNetworkingV1NamespacedIngressStatusError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReplaceNetworkingV1NamespacedIngressStatusRequest,
  output: IoK8sApiNetworkingV1Ingress,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReplaceNetworkingV1NamespacedNetworkPolicyError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** replace the specified NetworkPolicy */
export const replaceNetworkingV1NamespacedNetworkPolicy: API.OperationMethod<
  ReplaceNetworkingV1NamespacedNetworkPolicyRequest,
  IoK8sApiNetworkingV1NetworkPolicy,
  ReplaceNetworkingV1NamespacedNetworkPolicyError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReplaceNetworkingV1NamespacedNetworkPolicyRequest,
  output: IoK8sApiNetworkingV1NetworkPolicy,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReplaceNetworkingV1ServiceCIDRError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** replace the specified ServiceCIDR */
export const replaceNetworkingV1ServiceCIDR: API.OperationMethod<
  ReplaceNetworkingV1ServiceCIDRRequest,
  IoK8sApiNetworkingV1ServiceCIDR,
  ReplaceNetworkingV1ServiceCIDRError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReplaceNetworkingV1ServiceCIDRRequest,
  output: IoK8sApiNetworkingV1ServiceCIDR,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type ReplaceNetworkingV1ServiceCIDRStatusError =
  | NotFound
  | Conflict
  | UnprocessableEntity
  | KubernetesOpError;
/** replace status of the specified ServiceCIDR */
export const replaceNetworkingV1ServiceCIDRStatus: API.OperationMethod<
  ReplaceNetworkingV1ServiceCIDRStatusRequest,
  IoK8sApiNetworkingV1ServiceCIDR,
  ReplaceNetworkingV1ServiceCIDRStatusError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReplaceNetworkingV1ServiceCIDRStatusRequest,
  output: IoK8sApiNetworkingV1ServiceCIDR,
  errors: [NotFound, Conflict, UnprocessableEntity, UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1IngressClassError = KubernetesOpError;
/** watch changes to an object of kind IngressClass. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter. */
export const watchNetworkingV1IngressClass: API.OperationMethod<
  WatchNetworkingV1IngressClassRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1IngressClassError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1IngressClassRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1IngressClassListError = KubernetesOpError;
/** watch individual changes to a list of IngressClass. deprecated: use the 'watch' parameter with a list operation instead. */
export const watchNetworkingV1IngressClassList: API.OperationMethod<
  WatchNetworkingV1IngressClassListRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1IngressClassListError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1IngressClassListRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1IngressListForAllNamespacesError =
  KubernetesOpError;
/** watch individual changes to a list of Ingress. deprecated: use the 'watch' parameter with a list operation instead. */
export const watchNetworkingV1IngressListForAllNamespaces: API.OperationMethod<
  WatchNetworkingV1IngressListForAllNamespacesRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1IngressListForAllNamespacesError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1IngressListForAllNamespacesRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1IPAddressError = KubernetesOpError;
/** watch changes to an object of kind IPAddress. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter. */
export const watchNetworkingV1IPAddress: API.OperationMethod<
  WatchNetworkingV1IPAddressRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1IPAddressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1IPAddressRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1IPAddressListError = KubernetesOpError;
/** watch individual changes to a list of IPAddress. deprecated: use the 'watch' parameter with a list operation instead. */
export const watchNetworkingV1IPAddressList: API.OperationMethod<
  WatchNetworkingV1IPAddressListRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1IPAddressListError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1IPAddressListRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1NamespacedIngressError = KubernetesOpError;
/** watch changes to an object of kind Ingress. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter. */
export const watchNetworkingV1NamespacedIngress: API.OperationMethod<
  WatchNetworkingV1NamespacedIngressRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1NamespacedIngressError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1NamespacedIngressRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1NamespacedIngressListError = KubernetesOpError;
/** watch individual changes to a list of Ingress. deprecated: use the 'watch' parameter with a list operation instead. */
export const watchNetworkingV1NamespacedIngressList: API.OperationMethod<
  WatchNetworkingV1NamespacedIngressListRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1NamespacedIngressListError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1NamespacedIngressListRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1NamespacedNetworkPolicyError = KubernetesOpError;
/** watch changes to an object of kind NetworkPolicy. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter. */
export const watchNetworkingV1NamespacedNetworkPolicy: API.OperationMethod<
  WatchNetworkingV1NamespacedNetworkPolicyRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1NamespacedNetworkPolicyError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1NamespacedNetworkPolicyRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1NamespacedNetworkPolicyListError =
  KubernetesOpError;
/** watch individual changes to a list of NetworkPolicy. deprecated: use the 'watch' parameter with a list operation instead. */
export const watchNetworkingV1NamespacedNetworkPolicyList: API.OperationMethod<
  WatchNetworkingV1NamespacedNetworkPolicyListRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1NamespacedNetworkPolicyListError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1NamespacedNetworkPolicyListRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1NetworkPolicyListForAllNamespacesError =
  KubernetesOpError;
/** watch individual changes to a list of NetworkPolicy. deprecated: use the 'watch' parameter with a list operation instead. */
export const watchNetworkingV1NetworkPolicyListForAllNamespaces: API.OperationMethod<
  WatchNetworkingV1NetworkPolicyListForAllNamespacesRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1NetworkPolicyListForAllNamespacesError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1NetworkPolicyListForAllNamespacesRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1ServiceCIDRError = KubernetesOpError;
/** watch changes to an object of kind ServiceCIDR. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter. */
export const watchNetworkingV1ServiceCIDR: API.OperationMethod<
  WatchNetworkingV1ServiceCIDRRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1ServiceCIDRError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1ServiceCIDRRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));

export type WatchNetworkingV1ServiceCIDRListError = KubernetesOpError;
/** watch individual changes to a list of ServiceCIDR. deprecated: use the 'watch' parameter with a list operation instead. */
export const watchNetworkingV1ServiceCIDRList: API.OperationMethod<
  WatchNetworkingV1ServiceCIDRListRequest,
  IoK8sApimachineryPkgApisMetaV1WatchEvent,
  WatchNetworkingV1ServiceCIDRListError,
  KubernetesOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WatchNetworkingV1ServiceCIDRListRequest,
  output: IoK8sApimachineryPkgApisMetaV1WatchEvent,
  errors: [UnknownKubernetesError],
  protocol: KubernetesProtocol,
  retry: Retry.Retry,
}));
