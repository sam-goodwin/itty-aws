// AUTO-GENERATED by scripts/generate.ts from .generated-specs. Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  CloudflareProtocol,
  type CloudflareOpError,
  type CloudflareOpContext,
} from "../protocol.ts";
import { CloudflareError, CloudflareRateLimited } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { CloudflareOpError, CloudflareOpContext };

export type CheckRequestDomainsList = Array<string>;
export const CheckRequestDomainsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CheckRequestDomainsList>;

export interface CheckRequest {
  /** Identifier */
  accountId: string;
  /** List of fully qualified domain names (FQDNs) to check for availability. Each domain must include the extension. */
  domains: CheckRequestDomainsList;
}
export const CheckRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accountId: S.String.pipe(T.Label("account_id")),
    domains: CheckRequestDomainsList,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/accounts/{account_id}/registrar-sandbox/domain-check",
      code: 200,
    }),
  ),
).annotate({ identifier: "CheckRequest" }) as any as S.Schema<CheckRequest>;

export interface CheckResponseDomainsItemPricing {
  /** ISO-4217 currency code for the prices (e.g., “USD”, “EUR”, “GBP”). */
  currency: string;
  /** The first-year cost to register this domain. For premium domains (`tier: premium`), this price is set by the registry and may be significantly higher than standard pricing. For multi-year registrations, this cost applies to the first year only; subsequent years are charged at `renewal_cost`. */
  registrationCost: string;
  /** Per-year renewal cost for this domain. Applied to each year beyond the first year of a multi-year registration, and to each annual auto-renewal thereafter. May differ from `registration_cost`, especially for premium domains where initial registration often costs more than renewals. */
  renewalCost: string;
}
export const CheckResponseDomainsItemPricing = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    currency: S.String,
    registrationCost: S.String.pipe(T.Body("registration_cost")),
    renewalCost: S.String.pipe(T.Body("renewal_cost")),
  }),
).annotate({
  identifier: "CheckResponseDomainsItemPricing",
}) as any as S.Schema<CheckResponseDomainsItemPricing>;

export type CheckResponseDomainsItemReason =
  | "extension_not_supported_via_api"
  | "extension_not_supported"
  | "extension_disallows_registration"
  | "domain_premium"
  | "domain_unavailable";
export const CheckResponseDomainsItemReason = /*@__PURE__*/ S.String;

export type CheckResponseDomainsItemTier = "standard" | "premium";
export const CheckResponseDomainsItemTier = /*@__PURE__*/ S.String;

export interface CheckResponseDomainsItem {
  /** The fully qualified domain name (FQDN) in punycode format for internationalized domain names (IDNs). */
  name: string;
  /** Indicates whether this domain can be registered programmatically through this API based on a real-time registry check. */
  registrable: boolean;
  /** Annual pricing information for a registrable domain. This object is only present when `registrable` is `true`. All prices are per year and returned as strings to preserve decimal precision. */
  pricing?: CheckResponseDomainsItemPricing | null;
  /** Present only when `registrable` is `false`. Explains why the domain cannot be registered via this API. */
  reason?: CheckResponseDomainsItemReason | null;
  /** The pricing tier for this domain. Always present when `registrable` is `true`; defaults to `standard` for most domains. May be absent when `registrable` is `false`. */
  tier?: CheckResponseDomainsItemTier | null;
}
export const CheckResponseDomainsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    registrable: S.Boolean,
    pricing: S.optional(S.NullOr(CheckResponseDomainsItemPricing)),
    reason: S.optional(S.NullOr(CheckResponseDomainsItemReason)),
    tier: S.optional(S.NullOr(CheckResponseDomainsItemTier)),
  }),
).annotate({
  identifier: "CheckResponseDomainsItem",
}) as any as S.Schema<CheckResponseDomainsItem>;

export type CheckResponseDomainsList = Array<CheckResponseDomainsItem>;
export const CheckResponseDomainsList = /*@__PURE__*/ S.Array(
  CheckResponseDomainsItem,
) as any as S.Schema<CheckResponseDomainsList>;

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface CheckResponse {
  /** Array of domain availability results. Domains on unsupported extensions are included with `registrable: false` and a `reason`field. Malformed domain names may be omitted. */
  domains: CheckResponseDomainsList;
}
export const CheckResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    domains: CheckResponseDomainsList,
  }),
).annotate({ identifier: "CheckResponse" }) as any as S.Schema<CheckResponse>;

export interface ExtensionsGetRequest {
  /** Identifier */
  accountId: string;
  extension: string;
}
export const ExtensionsGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accountId: S.String.pipe(T.Label("account_id")),
    extension: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/accounts/{account_id}/registrar-sandbox/extensions/{extension}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ExtensionsGetRequest",
}) as any as S.Schema<ExtensionsGetRequest>;

export interface ExtensionsGetResponseMetadata {
  /** The full name of the extension. For example, “co.uk”, or “uk” */
  name: string;
  /** The tld of the extension. For example, for “co.uk”, it’s “uk”. For “uk”, it’s “uk” */
  tld: string;
}
export const ExtensionsGetResponseMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    tld: S.String,
  }),
).annotate({
  identifier: "ExtensionsGetResponseMetadata",
}) as any as S.Schema<ExtensionsGetResponseMetadata>;

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface ExtensionsGetResponse {
  /** Extension metadata */
  metadata: ExtensionsGetResponseMetadata;
  /** JSON Schema describing the expected input structure for registration operations on this extension. */
  registrationSchema: unknown;
}
export const ExtensionsGetResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    metadata: ExtensionsGetResponseMetadata,
    registrationSchema: S.Unknown.pipe(T.Body("registration_schema")),
  }),
).annotate({
  identifier: "ExtensionsGetResponse",
}) as any as S.Schema<ExtensionsGetResponse>;

export type ExtensionsListRequestDirection = "asc" | "desc";
export const ExtensionsListRequestDirection = /*@__PURE__*/ S.String;

export type ExtensionsListRequestSortBy = "name" | "created_at" | "updated_at";
export const ExtensionsListRequestSortBy = /*@__PURE__*/ S.String;

export interface ExtensionsListRequest {
  /** Identifier */
  accountId: string;
  /** Opaque token from a previous response’s `result_info.cursor`. Pass this value to fetch the next page of results. Omit (or pass an empty string) for the first page. */
  cursor?: string;
  /** Sort direction for results. Defaults to ascending order. */
  direction?: ExtensionsListRequestDirection | (string & {});
  /** Filter extensions by exact name match. For example, `name=com` returns only the `com` extension. */
  name?: string;
  /** Number of items to return per page. */
  perPage?: number;
  /** Column to sort results by. Defaults to `name` when omitted. */
  sortBy?: ExtensionsListRequestSortBy | (string & {});
}
export const ExtensionsListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accountId: S.String.pipe(T.Label("account_id")),
    cursor: S.optional(S.String.pipe(T.Query())),
    direction: S.optional(ExtensionsListRequestDirection.pipe(T.Query())),
    name: S.optional(S.String.pipe(T.Query())),
    perPage: S.optional(S.Number.pipe(T.Query("per_page"))),
    sortBy: S.optional(ExtensionsListRequestSortBy.pipe(T.Query("sort_by"))),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/accounts/{account_id}/registrar-sandbox/extensions",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ExtensionsListRequest",
}) as any as S.Schema<ExtensionsListRequest>;

export type ExtensionsListResultItemMetadata = ExtensionsGetResponseMetadata;
export const ExtensionsListResultItemMetadata = ExtensionsGetResponseMetadata;

export interface ExtensionsListResultItem {
  /** Extension metadata */
  metadata: ExtensionsGetResponseMetadata;
  /** JSON Schema describing the expected input structure for registration operations on this extension. */
  registrationSchema: unknown;
}
export const ExtensionsListResultItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    metadata: ExtensionsGetResponseMetadata,
    registrationSchema: S.Unknown.pipe(T.Body("registration_schema")),
  }),
).annotate({
  identifier: "ExtensionsListResultItem",
}) as any as S.Schema<ExtensionsListResultItem>;

export type ExtensionsListResultList = Array<ExtensionsListResultItem>;
export const ExtensionsListResultList = /*@__PURE__*/ S.Array(
  ExtensionsListResultItem,
) as any as S.Schema<ExtensionsListResultList>;

export type ExtensionsListResponse = ExtensionsListResultList;
export const ExtensionsListResponse = /*@__PURE__*/ S.suspend(() =>
  ExtensionsListResultList.pipe(T.EnvelopePayloadRoot()),
).annotate({
  identifier: "ExtensionsListResponse",
}) as any as S.Schema<ExtensionsListResponse>;

export type RegistrationsCreateRequestAcknowledgementsMap = {
  [key: string]: unknown | undefined;
};
export const RegistrationsCreateRequestAcknowledgementsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<RegistrationsCreateRequestAcknowledgementsMap>;

export type RegistrationsCreateRequestContactExtensionsMap = {
  [key: string]: unknown | undefined;
};
export const RegistrationsCreateRequestContactExtensionsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<RegistrationsCreateRequestContactExtensionsMap>;

export type RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfoAddressPrivacyMode =
  | "off"
  | "redaction";
export const RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfoAddressPrivacyMode =
  /*@__PURE__*/ S.String;

export interface RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfoAddress {
  /** City or locality name. */
  city: string;
  /** Two-letter country code per ISO 3166-1 alpha-2 (e.g., `US`, `GB`, `CA`, `DE`). */
  countryCode: string;
  /** Postal or ZIP code. */
  postalCode: string;
  /** State, province, or region. Use the standard abbreviation where applicable (e.g., `TX` for Texas, `ON` for Ontario). */
  state: string;
  /** Street address including building/suite number. */
  street: string;
  /** Full legal name of the contact, including all required name components for an individual or authorized representative. Some registries require a complete personal name that includes a family or last name where applicable. Provide the complete name in this single field, for example `Ada Lovelace`; do not send separate first-name or last-name fields. */
  name: string;
  /** Organization or company name. Optional for individual registrants. */
  organization?: string;
  /** Fax number in E.164 format (e.g., `+1.5555555555`). Optional. Most registrations do not require a fax number. */
  fax?: string;
  /** WHOIS privacy mode for the registration. Defaults to `redaction`. */
  privacyMode?:
    | RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfoAddressPrivacyMode
    | (string & {});
  /** Number of years to register (1–10). If omitted, defaults to the minimum registration period required by the registry for this extension. For most extensions this is 1 year, but some extensions require longer minimum terms (e.g., `.ai` requires a minimum of 2 years). */
  years?: number;
}
export const RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfoAddress =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      city: S.String,
      countryCode: S.String.pipe(T.Body("country_code")),
      postalCode: S.String.pipe(T.Body("postal_code")),
      state: S.String,
      street: S.String,
      name: S.String,
      organization: S.optional(S.String),
      fax: S.optional(S.String),
      privacyMode: S.optional(
        RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfoAddressPrivacyMode.pipe(
          T.Body("privacy_mode"),
        ),
      ),
      years: S.optional(S.Number),
    }),
  ).annotate({
    identifier:
      "RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfoAddress",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfoAddress>;

export interface RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfo {
  /** Physical mailing address for the registrant contact. */
  address: RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfoAddress;
}
export const RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfo =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      address:
        RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfoAddress,
    }),
  ).annotate({
    identifier:
      "RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfo",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfo>;

export interface RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnical {
  /** Email address for the registrant. Used for domain-related communications from the registry, including ownership verification and renewal notices. */
  email: string;
  /** Phone number in E.164 format: `+{country_code}.{number}` with no spaces or dashes. Examples: `+1.5555555555` (US), `+44.2071234567`(UK), `+81.312345678` (Japan). */
  phone: string;
  /** Postal/mailing information for the contact. The `name` field is the complete contact name in one string. Some registries require a complete personal name, including a family or last name where applicable, but this API does not accept separate first-name and last-name fields for registration contacts. */
  postalInfo: RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfo;
}
export const RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnical =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      email: S.String,
      phone: S.String,
      postalInfo:
        RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnicalPostalInfo.pipe(
          T.Body("postal_info"),
        ),
    }),
  ).annotate({
    identifier:
      "RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnical",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnical>;

export interface RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddress {
  /** City or locality name. */
  city: string;
  /** Two-letter country code per ISO 3166-1 alpha-2 (e.g., `US`, `GB`, `CA`, `DE`). */
  countryCode: string;
  /** Postal or ZIP code. */
  postalCode: string;
  /** State, province, or region. Use the standard abbreviation where applicable (e.g., `TX` for Texas, `ON` for Ontario). */
  state: string;
  /** Street address including building/suite number. */
  street: string;
  /** Full legal name of the contact, including all required name components for an individual or authorized representative. Some registries require a complete personal name that includes a family or last name where applicable. Provide the complete name in this single field, for example `Ada Lovelace`; do not send separate first-name or last-name fields. */
  name: string;
  /** Organization or company name. Optional for individual registrants. */
  organization?: string;
  /** Fax number in E.164 format (e.g., `+1.5555555555`). Optional. Most registrations do not require a fax number. */
  fax?: string;
  /** Contact data for the domain registration. This information is submitted to the domain registry and, depending on extension and privacy settings, may appear in public WHOIS records. */
  technical?: RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnical;
}
export const RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddress =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      city: S.String,
      countryCode: S.String.pipe(T.Body("country_code")),
      postalCode: S.String.pipe(T.Body("postal_code")),
      state: S.String,
      street: S.String,
      name: S.String,
      organization: S.optional(S.String),
      fax: S.optional(S.String),
      technical: S.optional(
        RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddressTechnical,
      ),
    }),
  ).annotate({
    identifier:
      "RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddress",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddress>;

export interface RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfo {
  /** Physical mailing address for the registrant contact. */
  address: RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddress;
}
export const RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfo =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      address:
        RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfoAddress,
    }),
  ).annotate({
    identifier:
      "RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfo",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfo>;

export interface RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrant {
  /** Email address for the registrant. Used for domain-related communications from the registry, including ownership verification and renewal notices. */
  email: string;
  /** Phone number in E.164 format: `+{country_code}.{number}` with no spaces or dashes. Examples: `+1.5555555555` (US), `+44.2071234567`(UK), `+81.312345678` (Japan). */
  phone: string;
  /** Postal/mailing information for the contact. The `name` field is the complete contact name in one string. Some registries require a complete personal name, including a family or last name where applicable, but this API does not accept separate first-name and last-name fields for registration contacts. */
  postalInfo: RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfo;
}
export const RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrant =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      email: S.String,
      phone: S.String,
      postalInfo:
        RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrantPostalInfo.pipe(
          T.Body("postal_info"),
        ),
    }),
  ).annotate({
    identifier:
      "RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrant",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrant>;

export interface RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddress {
  /** City or locality name. */
  city: string;
  /** Two-letter country code per ISO 3166-1 alpha-2 (e.g., `US`, `GB`, `CA`, `DE`). */
  countryCode: string;
  /** Postal or ZIP code. */
  postalCode: string;
  /** State, province, or region. Use the standard abbreviation where applicable (e.g., `TX` for Texas, `ON` for Ontario). */
  state: string;
  /** Street address including building/suite number. */
  street: string;
  /** Full legal name of the contact, including all required name components for an individual or authorized representative. Some registries require a complete personal name that includes a family or last name where applicable. Provide the complete name in this single field, for example `Ada Lovelace`; do not send separate first-name or last-name fields. */
  name: string;
  /** Organization or company name. Optional for individual registrants. */
  organization?: string;
  /** Fax number in E.164 format (e.g., `+1.5555555555`). Optional. Most registrations do not require a fax number. */
  fax?: string;
  /** Contact data for the domain registration. This information is submitted to the domain registry and, depending on extension and privacy settings, may appear in public WHOIS records. */
  registrant?: RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrant;
}
export const RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddress =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      city: S.String,
      countryCode: S.String.pipe(T.Body("country_code")),
      postalCode: S.String.pipe(T.Body("postal_code")),
      state: S.String,
      street: S.String,
      name: S.String,
      organization: S.optional(S.String),
      fax: S.optional(S.String),
      registrant: S.optional(
        RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddressRegistrant,
      ),
    }),
  ).annotate({
    identifier:
      "RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddress",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddress>;

export interface RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfo {
  /** Physical mailing address for the registrant contact. */
  address: RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddress;
}
export const RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfo =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      address:
        RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfoAddress,
    }),
  ).annotate({
    identifier:
      "RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfo",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfo>;

export interface RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBilling {
  /** Email address for the registrant. Used for domain-related communications from the registry, including ownership verification and renewal notices. */
  email: string;
  /** Phone number in E.164 format: `+{country_code}.{number}` with no spaces or dashes. Examples: `+1.5555555555` (US), `+44.2071234567`(UK), `+81.312345678` (Japan). */
  phone: string;
  /** Postal/mailing information for the contact. The `name` field is the complete contact name in one string. Some registries require a complete personal name, including a family or last name where applicable, but this API does not accept separate first-name and last-name fields for registration contacts. */
  postalInfo: RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfo;
}
export const RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBilling =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      email: S.String,
      phone: S.String,
      postalInfo:
        RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBillingPostalInfo.pipe(
          T.Body("postal_info"),
        ),
    }),
  ).annotate({
    identifier:
      "RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBilling",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBilling>;

export interface RegistrationsCreateRequestContactsAdministratorPostalInfoAddress {
  /** City or locality name. */
  city: string;
  /** Two-letter country code per ISO 3166-1 alpha-2 (e.g., `US`, `GB`, `CA`, `DE`). */
  countryCode: string;
  /** Postal or ZIP code. */
  postalCode: string;
  /** State, province, or region. Use the standard abbreviation where applicable (e.g., `TX` for Texas, `ON` for Ontario). */
  state: string;
  /** Street address including building/suite number. */
  street: string;
  /** Full legal name of the contact, including all required name components for an individual or authorized representative. Some registries require a complete personal name that includes a family or last name where applicable. Provide the complete name in this single field, for example `Ada Lovelace`; do not send separate first-name or last-name fields. */
  name: string;
  /** Organization or company name. Optional for individual registrants. */
  organization?: string;
  /** Fax number in E.164 format (e.g., `+1.5555555555`). Optional. Most registrations do not require a fax number. */
  fax?: string;
  /** Contact data for the domain registration. This information is submitted to the domain registry and, depending on extension and privacy settings, may appear in public WHOIS records. */
  billing?: RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBilling;
}
export const RegistrationsCreateRequestContactsAdministratorPostalInfoAddress =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      city: S.String,
      countryCode: S.String.pipe(T.Body("country_code")),
      postalCode: S.String.pipe(T.Body("postal_code")),
      state: S.String,
      street: S.String,
      name: S.String,
      organization: S.optional(S.String),
      fax: S.optional(S.String),
      billing: S.optional(
        RegistrationsCreateRequestContactsAdministratorPostalInfoAddressBilling,
      ),
    }),
  ).annotate({
    identifier:
      "RegistrationsCreateRequestContactsAdministratorPostalInfoAddress",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministratorPostalInfoAddress>;

export interface RegistrationsCreateRequestContactsAdministratorPostalInfo {
  /** Physical mailing address for the registrant contact. */
  address: RegistrationsCreateRequestContactsAdministratorPostalInfoAddress;
}
export const RegistrationsCreateRequestContactsAdministratorPostalInfo =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      address: RegistrationsCreateRequestContactsAdministratorPostalInfoAddress,
    }),
  ).annotate({
    identifier: "RegistrationsCreateRequestContactsAdministratorPostalInfo",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministratorPostalInfo>;

export interface RegistrationsCreateRequestContactsAdministrator {
  /** Email address for the registrant. Used for domain-related communications from the registry, including ownership verification and renewal notices. */
  email: string;
  /** Phone number in E.164 format: `+{country_code}.{number}` with no spaces or dashes. Examples: `+1.5555555555` (US), `+44.2071234567`(UK), `+81.312345678` (Japan). */
  phone: string;
  /** Postal/mailing information for the contact. The `name` field is the complete contact name in one string. Some registries require a complete personal name, including a family or last name where applicable, but this API does not accept separate first-name and last-name fields for registration contacts. */
  postalInfo: RegistrationsCreateRequestContactsAdministratorPostalInfo;
}
export const RegistrationsCreateRequestContactsAdministrator =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      email: S.String,
      phone: S.String,
      postalInfo:
        RegistrationsCreateRequestContactsAdministratorPostalInfo.pipe(
          T.Body("postal_info"),
        ),
    }),
  ).annotate({
    identifier: "RegistrationsCreateRequestContactsAdministrator",
  }) as any as S.Schema<RegistrationsCreateRequestContactsAdministrator>;

export interface RegistrationsCreateRequestContacts {
  /** Contact data for the domain registration. This information is submitted to the domain registry and, depending on extension and privacy settings, may appear in public WHOIS records. */
  administrator?: RegistrationsCreateRequestContactsAdministrator;
}
export const RegistrationsCreateRequestContacts = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    administrator: S.optional(RegistrationsCreateRequestContactsAdministrator),
  }),
).annotate({
  identifier: "RegistrationsCreateRequestContacts",
}) as any as S.Schema<RegistrationsCreateRequestContacts>;

export interface RegistrationsCreateRequest {
  /** Identifier */
  accountId: string;
  prefer?: string;
  /** Fully qualified domain name (FQDN) including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration — the same domain cannot be registered twice, making it a natural idempotency key for registration requests. */
  domainName: string;
  /** User acknowledgements required by a specific extension or premium registration flow. The expected keys are described by the extension registration schema returned by the extension discovery endpoint. */
  acknowledgements?: RegistrationsCreateRequestAcknowledgementsMap;
  /** Enable or disable automatic renewal. Defaults to `false` if omitted. Setting this field to `true` is an explicit opt-in authorizing Cloudflare to charge the account’s default payment method up to 30 days before domain expiry to renew the domain automatically. Renewal pricing may change over time based on registry pricing. */
  autoRenew?: boolean;
  /** Registry-specific contact extension values for the registrant. The required keys and allowed values vary by extension and are described by `GET /accounts/{account_id}/registrar/extensions/{extension}` in the `registration_schema.properties.contact_extensions` object. */
  contactExtensions?: RegistrationsCreateRequestContactExtensionsMap;
  /** Contact data for the registration request. */
  contacts?: RegistrationsCreateRequestContacts;
}
export const RegistrationsCreateRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accountId: S.String.pipe(T.Label("account_id")),
    prefer: S.optional(S.String.pipe(T.Header("Prefer"))),
    domainName: S.String.pipe(T.Body("domain_name")),
    acknowledgements: S.optional(RegistrationsCreateRequestAcknowledgementsMap),
    autoRenew: S.optional(S.Boolean.pipe(T.Body("auto_renew"))),
    contactExtensions: S.optional(
      RegistrationsCreateRequestContactExtensionsMap.pipe(
        T.Body("contact_extensions"),
      ),
    ),
    contacts: S.optional(RegistrationsCreateRequestContacts),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/accounts/{account_id}/registrar-sandbox/registrations",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RegistrationsCreateRequest",
}) as any as S.Schema<RegistrationsCreateRequest>;

export interface RegistrationsCreateResponseLinks {
  /** URL to this status resource. */
  self: string;
  /** URL to the domain resource. */
  resource?: string | null;
}
export const RegistrationsCreateResponseLinks = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    self: S.String,
    resource: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "RegistrationsCreateResponseLinks",
}) as any as S.Schema<RegistrationsCreateResponseLinks>;

export type RegistrationsCreateResponseState =
  | "pending"
  | "in_progress"
  | "action_required"
  | "blocked"
  | "succeeded"
  | "failed";
export const RegistrationsCreateResponseState = /*@__PURE__*/ S.String;

export type RegistrationsCreateResponseContextMap = {
  [key: string]: unknown | undefined;
};
export const RegistrationsCreateResponseContextMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<RegistrationsCreateResponseContextMap>;

export interface RegistrationsCreateResponseError {
  /** Machine-readable error code identifying the failure reason. */
  code: string;
  /** Human-readable explanation of the failure. May include registry-specific details. */
  message: string;
}
export const RegistrationsCreateResponseError = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    code: S.String,
    message: S.String,
  }),
).annotate({
  identifier: "RegistrationsCreateResponseError",
}) as any as S.Schema<RegistrationsCreateResponseError>;

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface RegistrationsCreateResponse {
  /** Whether the workflow has reached a terminal state. `true` when `state` is `succeeded` or `failed`. `false` for `pending`, `in_progress`, `action_required`, and `blocked`. */
  completed: boolean;
  /** formatdate-time */
  createdAt: string;
  links: RegistrationsCreateResponseLinks;
  /** Workflow lifecycle state. */
  state: RegistrationsCreateResponseState;
  /** formatdate-time */
  updatedAt: string;
  /** Workflow-specific data for this workflow. */
  context?: RegistrationsCreateResponseContextMap | null;
  /** Error details when a workflow reaches the `failed` state. The specific error codes and messages depend on the workflow type (registration, update, etc.) and the underlying registry response. These workflow error codes are separate from immediate HTTP error `errors[].code`values returned by non-2xx responses. Surface `error.message` to the user for context. */
  error?: RegistrationsCreateResponseError | null;
}
export const RegistrationsCreateResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    completed: S.Boolean,
    createdAt: S.String.pipe(T.Body("created_at")),
    links: RegistrationsCreateResponseLinks,
    state: RegistrationsCreateResponseState,
    updatedAt: S.String.pipe(T.Body("updated_at")),
    context: S.optional(S.NullOr(RegistrationsCreateResponseContextMap)),
    error: S.optional(S.NullOr(RegistrationsCreateResponseError)),
  }),
).annotate({
  identifier: "RegistrationsCreateResponse",
}) as any as S.Schema<RegistrationsCreateResponse>;

export type RegistrationsEditRequestPrefer = "respond-async";
export const RegistrationsEditRequestPrefer = /*@__PURE__*/ S.String;

export interface RegistrationsEditRequest {
  /** Identifier */
  accountId: string;
  /** Fully qualified domain name (FQDN) including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration — the same domain cannot be registered twice, making it a natural idempotency key for registration requests. */
  domainName: string;
  prefer?: RegistrationsEditRequestPrefer | (string & {});
  /** Enable or disable automatic renewal. Setting this field to `true` authorizes Cloudflare to charge the account’s default payment method up to 30 days before domain expiry to renew the domain automatically. Renewal pricing may change over time based on registry pricing. */
  autoRenew?: boolean;
}
export const RegistrationsEditRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accountId: S.String.pipe(T.Label("account_id")),
    domainName: S.String.pipe(T.Label("domain_name")),
    prefer: S.optional(RegistrationsEditRequestPrefer.pipe(T.Header("Prefer"))),
    autoRenew: S.optional(S.Boolean.pipe(T.Body("auto_renew"))),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/accounts/{account_id}/registrar-sandbox/registrations/{domain_name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RegistrationsEditRequest",
}) as any as S.Schema<RegistrationsEditRequest>;

export type RegistrationsEditResponseLinks = RegistrationsCreateResponseLinks;
export const RegistrationsEditResponseLinks = RegistrationsCreateResponseLinks;

export type RegistrationsEditResponseState =
  | "pending"
  | "in_progress"
  | "action_required"
  | "blocked"
  | "succeeded"
  | "failed";
export const RegistrationsEditResponseState = /*@__PURE__*/ S.String;

export type RegistrationsEditResponseContextMap = {
  [key: string]: unknown | undefined;
};
export const RegistrationsEditResponseContextMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<RegistrationsEditResponseContextMap>;

export type RegistrationsEditResponseError = RegistrationsCreateResponseError;
export const RegistrationsEditResponseError = RegistrationsCreateResponseError;

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface RegistrationsEditResponse {
  /** Whether the workflow has reached a terminal state. `true` when `state` is `succeeded` or `failed`. `false` for `pending`, `in_progress`, `action_required`, and `blocked`. */
  completed: boolean;
  /** formatdate-time */
  createdAt: string;
  links: RegistrationsCreateResponseLinks;
  /** Workflow lifecycle state. */
  state: RegistrationsEditResponseState;
  /** formatdate-time */
  updatedAt: string;
  /** Workflow-specific data for this workflow. */
  context?: RegistrationsEditResponseContextMap | null;
  /** Error details when a workflow reaches the `failed` state. The specific error codes and messages depend on the workflow type (registration, update, etc.) and the underlying registry response. These workflow error codes are separate from immediate HTTP error `errors[].code`values returned by non-2xx responses. Surface `error.message` to the user for context. */
  error?: RegistrationsCreateResponseError | null;
}
export const RegistrationsEditResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    completed: S.Boolean,
    createdAt: S.String.pipe(T.Body("created_at")),
    links: RegistrationsCreateResponseLinks,
    state: RegistrationsEditResponseState,
    updatedAt: S.String.pipe(T.Body("updated_at")),
    context: S.optional(S.NullOr(RegistrationsEditResponseContextMap)),
    error: S.optional(S.NullOr(RegistrationsCreateResponseError)),
  }),
).annotate({
  identifier: "RegistrationsEditResponse",
}) as any as S.Schema<RegistrationsEditResponse>;

export interface RegistrationsGetRequest {
  /** Identifier */
  accountId: string;
  /** Fully qualified domain name (FQDN) including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration — the same domain cannot be registered twice, making it a natural idempotency key for registration requests. */
  domainName: string;
}
export const RegistrationsGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accountId: S.String.pipe(T.Label("account_id")),
    domainName: S.String.pipe(T.Label("domain_name")),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/accounts/{account_id}/registrar-sandbox/registrations/{domain_name}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RegistrationsGetRequest",
}) as any as S.Schema<RegistrationsGetRequest>;

export type RegistrationsGetResponsePrivacyMode = "off" | "redaction";
export const RegistrationsGetResponsePrivacyMode = /*@__PURE__*/ S.String;

export type RegistrationsGetResponseStatus =
  | "active"
  | "registration_pending"
  | "expired"
  | "suspended"
  | "redemption_period"
  | "pending_delete";
export const RegistrationsGetResponseStatus = /*@__PURE__*/ S.String;

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface RegistrationsGetResponse {
  /** Whether the domain will be automatically renewed before expiration. */
  autoRenew: boolean;
  /** When the domain was registered. Present when the registration resource exists. */
  createdAt: string;
  /** Fully qualified domain name (FQDN) including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration — the same domain cannot be registered twice, making it a natural idempotency key for registration requests. */
  domainName: string;
  /** When the domain registration expires. Present when the registration is ready; may be null only while `status` is `registration_pending`. */
  expiresAt: string;
  /** Whether the domain is locked for transfer. */
  locked: boolean;
  /** Current WHOIS privacy mode for the registration. */
  privacyMode: RegistrationsGetResponsePrivacyMode;
  /** Current registration status. */
  status: RegistrationsGetResponseStatus;
}
export const RegistrationsGetResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    autoRenew: S.Boolean.pipe(T.Body("auto_renew")),
    createdAt: S.String.pipe(T.Body("created_at")),
    domainName: S.String.pipe(T.Body("domain_name")),
    expiresAt: S.String.pipe(T.Body("expires_at")),
    locked: S.Boolean,
    privacyMode: RegistrationsGetResponsePrivacyMode.pipe(
      T.Body("privacy_mode"),
    ),
    status: RegistrationsGetResponseStatus,
  }),
).annotate({
  identifier: "RegistrationsGetResponse",
}) as any as S.Schema<RegistrationsGetResponse>;

export type RegistrationsListRequestDirection = "asc" | "desc";
export const RegistrationsListRequestDirection = /*@__PURE__*/ S.String;

export type RegistrationsListRequestSortBy =
  | "registry_created_at"
  | "registry_expires_at"
  | "name";
export const RegistrationsListRequestSortBy = /*@__PURE__*/ S.String;

export interface RegistrationsListRequest {
  /** Identifier */
  accountId: string;
  /** Opaque token from a previous response’s `result_info.cursor`. Pass this value to fetch the next page of results. Omit (or pass an empty string) for the first page. */
  cursor?: string;
  /** Sort direction for results. Defaults to ascending order. */
  direction?: RegistrationsListRequestDirection | (string & {});
  /** Number of items to return per page. */
  perPage?: number;
  /** Column to sort results by. Defaults to registration date (`registry_created_at`) when omitted. */
  sortBy?: RegistrationsListRequestSortBy | (string & {});
}
export const RegistrationsListRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accountId: S.String.pipe(T.Label("account_id")),
    cursor: S.optional(S.String.pipe(T.Query())),
    direction: S.optional(RegistrationsListRequestDirection.pipe(T.Query())),
    perPage: S.optional(S.Number.pipe(T.Query("per_page"))),
    sortBy: S.optional(RegistrationsListRequestSortBy.pipe(T.Query("sort_by"))),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/accounts/{account_id}/registrar-sandbox/registrations",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RegistrationsListRequest",
}) as any as S.Schema<RegistrationsListRequest>;

export type RegistrationsListResultItemPrivacyMode = "off" | "redaction";
export const RegistrationsListResultItemPrivacyMode = /*@__PURE__*/ S.String;

export type RegistrationsListResultItemStatus =
  | "active"
  | "registration_pending"
  | "expired"
  | "suspended"
  | "redemption_period"
  | "pending_delete";
export const RegistrationsListResultItemStatus = /*@__PURE__*/ S.String;

export interface RegistrationsListResultItem {
  /** Whether the domain will be automatically renewed before expiration. */
  autoRenew: boolean;
  /** When the domain was registered. Present when the registration resource exists. */
  createdAt: string;
  /** Fully qualified domain name (FQDN) including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration — the same domain cannot be registered twice, making it a natural idempotency key for registration requests. */
  domainName: string;
  /** When the domain registration expires. Present when the registration is ready; may be null only while `status` is `registration_pending`. */
  expiresAt: string;
  /** Whether the domain is locked for transfer. */
  locked: boolean;
  /** Current WHOIS privacy mode for the registration. */
  privacyMode: RegistrationsListResultItemPrivacyMode;
  /** Current registration status. */
  status: RegistrationsListResultItemStatus;
}
export const RegistrationsListResultItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    autoRenew: S.Boolean.pipe(T.Body("auto_renew")),
    createdAt: S.String.pipe(T.Body("created_at")),
    domainName: S.String.pipe(T.Body("domain_name")),
    expiresAt: S.String.pipe(T.Body("expires_at")),
    locked: S.Boolean,
    privacyMode: RegistrationsListResultItemPrivacyMode.pipe(
      T.Body("privacy_mode"),
    ),
    status: RegistrationsListResultItemStatus,
  }),
).annotate({
  identifier: "RegistrationsListResultItem",
}) as any as S.Schema<RegistrationsListResultItem>;

export type RegistrationsListResultList = Array<RegistrationsListResultItem>;
export const RegistrationsListResultList = /*@__PURE__*/ S.Array(
  RegistrationsListResultItem,
) as any as S.Schema<RegistrationsListResultList>;

export type RegistrationsListResponse = RegistrationsListResultList;
export const RegistrationsListResponse = /*@__PURE__*/ S.suspend(() =>
  RegistrationsListResultList.pipe(T.EnvelopePayloadRoot()),
).annotate({
  identifier: "RegistrationsListResponse",
}) as any as S.Schema<RegistrationsListResponse>;

export interface RegistrationStatusGetRequest {
  /** Identifier */
  accountId: string;
  /** Fully qualified domain name (FQDN) including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration — the same domain cannot be registered twice, making it a natural idempotency key for registration requests. */
  domainName: string;
}
export const RegistrationStatusGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accountId: S.String.pipe(T.Label("account_id")),
    domainName: S.String.pipe(T.Label("domain_name")),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/accounts/{account_id}/registrar-sandbox/registrations/{domain_name}/registration-status",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RegistrationStatusGetRequest",
}) as any as S.Schema<RegistrationStatusGetRequest>;

export type RegistrationStatusGetResponseLinks =
  RegistrationsCreateResponseLinks;
export const RegistrationStatusGetResponseLinks =
  RegistrationsCreateResponseLinks;

export type RegistrationStatusGetResponseState =
  | "pending"
  | "in_progress"
  | "action_required"
  | "blocked"
  | "succeeded"
  | "failed";
export const RegistrationStatusGetResponseState = /*@__PURE__*/ S.String;

export type RegistrationStatusGetResponseContextMap = {
  [key: string]: unknown | undefined;
};
export const RegistrationStatusGetResponseContextMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<RegistrationStatusGetResponseContextMap>;

export type RegistrationStatusGetResponseError =
  RegistrationsCreateResponseError;
export const RegistrationStatusGetResponseError =
  RegistrationsCreateResponseError;

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface RegistrationStatusGetResponse {
  /** Whether the workflow has reached a terminal state. `true` when `state` is `succeeded` or `failed`. `false` for `pending`, `in_progress`, `action_required`, and `blocked`. */
  completed: boolean;
  /** formatdate-time */
  createdAt: string;
  links: RegistrationsCreateResponseLinks;
  /** Workflow lifecycle state. */
  state: RegistrationStatusGetResponseState;
  /** formatdate-time */
  updatedAt: string;
  /** Workflow-specific data for this workflow. */
  context?: RegistrationStatusGetResponseContextMap | null;
  /** Error details when a workflow reaches the `failed` state. The specific error codes and messages depend on the workflow type (registration, update, etc.) and the underlying registry response. These workflow error codes are separate from immediate HTTP error `errors[].code`values returned by non-2xx responses. Surface `error.message` to the user for context. */
  error?: RegistrationsCreateResponseError | null;
}
export const RegistrationStatusGetResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    completed: S.Boolean,
    createdAt: S.String.pipe(T.Body("created_at")),
    links: RegistrationsCreateResponseLinks,
    state: RegistrationStatusGetResponseState,
    updatedAt: S.String.pipe(T.Body("updated_at")),
    context: S.optional(S.NullOr(RegistrationStatusGetResponseContextMap)),
    error: S.optional(S.NullOr(RegistrationsCreateResponseError)),
  }),
).annotate({
  identifier: "RegistrationStatusGetResponse",
}) as any as S.Schema<RegistrationStatusGetResponse>;

export type SearchRequestExtensionsList = Array<string>;
export const SearchRequestExtensionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<SearchRequestExtensionsList>;

export interface SearchRequest {
  /** Identifier */
  accountId: string;
  /** The search term to find domain suggestions. Accepts keywords, phrases, or full domain names. */
  q: string;
  /** Limits results to specific domain extensions from the supported set. If not specified, returns results across all supported extensions. Extensions not in the supported set are silently ignored. */
  extensions?: SearchRequestExtensionsList;
  /** Maximum number of domain suggestions to return. Defaults to 20 if not specified. */
  limit?: number;
}
export const SearchRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accountId: S.String.pipe(T.Label("account_id")),
    q: S.String.pipe(T.Query()),
    extensions: S.optional(SearchRequestExtensionsList.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/accounts/{account_id}/registrar-sandbox/domain-search",
      code: 200,
    }),
  ),
).annotate({ identifier: "SearchRequest" }) as any as S.Schema<SearchRequest>;

export type SearchResponseDomainsItemPricing = CheckResponseDomainsItemPricing;
export const SearchResponseDomainsItemPricing = CheckResponseDomainsItemPricing;

export type SearchResponseDomainsItemReason =
  | "extension_not_supported_via_api"
  | "extension_not_supported"
  | "extension_disallows_registration"
  | "domain_premium"
  | "domain_unavailable";
export const SearchResponseDomainsItemReason = /*@__PURE__*/ S.String;

export type SearchResponseDomainsItemTier = "standard" | "premium";
export const SearchResponseDomainsItemTier = /*@__PURE__*/ S.String;

export interface SearchResponseDomainsItem {
  /** The fully qualified domain name (FQDN) in punycode format for internationalized domain names (IDNs). */
  name: string;
  /** Indicates whether this domain appears available based on search data. Search results are non-authoritative and may be stale. - `true`: The domain appears available. Use POST /domain-check to confirm before registration. */
  registrable: boolean;
  /** Annual pricing information for a registrable domain. This object is only present when `registrable` is `true`. All prices are per year and returned as strings to preserve decimal precision. */
  pricing?: CheckResponseDomainsItemPricing | null;
  /** Present only when `registrable` is `false` on search results. Explains why the domain does not appear registrable through this API. These values are advisory; use POST /domain-check for authoritative status. */
  reason?: SearchResponseDomainsItemReason | null;
  /** The pricing tier for this domain. Always present when `registrable` is `true`; defaults to `standard` for most domains. May be absent when `registrable`is `false`. */
  tier?: SearchResponseDomainsItemTier | null;
}
export const SearchResponseDomainsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    registrable: S.Boolean,
    pricing: S.optional(S.NullOr(CheckResponseDomainsItemPricing)),
    reason: S.optional(S.NullOr(SearchResponseDomainsItemReason)),
    tier: S.optional(S.NullOr(SearchResponseDomainsItemTier)),
  }),
).annotate({
  identifier: "SearchResponseDomainsItem",
}) as any as S.Schema<SearchResponseDomainsItem>;

export type SearchResponseDomainsList = Array<SearchResponseDomainsItem>;
export const SearchResponseDomainsList = /*@__PURE__*/ S.Array(
  SearchResponseDomainsItem,
) as any as S.Schema<SearchResponseDomainsList>;

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface SearchResponse {
  /** Array of domain suggestions sorted by relevance. May be empty if no domains match the search criteria. */
  domains: SearchResponseDomainsList;
}
export const SearchResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    domains: SearchResponseDomainsList,
  }),
).annotate({ identifier: "SearchResponse" }) as any as S.Schema<SearchResponse>;

export interface UpdateStatusGetRequest {
  /** Identifier */
  accountId: string;
  /** Fully qualified domain name (FQDN) including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration — the same domain cannot be registered twice, making it a natural idempotency key for registration requests. */
  domainName: string;
}
export const UpdateStatusGetRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accountId: S.String.pipe(T.Label("account_id")),
    domainName: S.String.pipe(T.Label("domain_name")),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/accounts/{account_id}/registrar-sandbox/registrations/{domain_name}/update-status",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateStatusGetRequest",
}) as any as S.Schema<UpdateStatusGetRequest>;

export type UpdateStatusGetResponseLinks = RegistrationsCreateResponseLinks;
export const UpdateStatusGetResponseLinks = RegistrationsCreateResponseLinks;

export type UpdateStatusGetResponseState =
  | "pending"
  | "in_progress"
  | "action_required"
  | "blocked"
  | "succeeded"
  | "failed";
export const UpdateStatusGetResponseState = /*@__PURE__*/ S.String;

export type UpdateStatusGetResponseContextMap = {
  [key: string]: unknown | undefined;
};
export const UpdateStatusGetResponseContextMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<UpdateStatusGetResponseContextMap>;

export type UpdateStatusGetResponseError = RegistrationsCreateResponseError;
export const UpdateStatusGetResponseError = RegistrationsCreateResponseError;

/** Unwrapped `result` payload of the Cloudflare v4 response envelope. */
export interface UpdateStatusGetResponse {
  /** Whether the workflow has reached a terminal state. `true` when `state` is `succeeded` or `failed`. `false` for `pending`, `in_progress`, `action_required`, and `blocked`. */
  completed: boolean;
  /** formatdate-time */
  createdAt: string;
  links: RegistrationsCreateResponseLinks;
  /** Workflow lifecycle state. */
  state: UpdateStatusGetResponseState;
  /** formatdate-time */
  updatedAt: string;
  /** Workflow-specific data for this workflow. */
  context?: UpdateStatusGetResponseContextMap | null;
  /** Error details when a workflow reaches the `failed` state. The specific error codes and messages depend on the workflow type (registration, update, etc.) and the underlying registry response. These workflow error codes are separate from immediate HTTP error `errors[].code`values returned by non-2xx responses. Surface `error.message` to the user for context. */
  error?: RegistrationsCreateResponseError | null;
}
export const UpdateStatusGetResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    completed: S.Boolean,
    createdAt: S.String.pipe(T.Body("created_at")),
    links: RegistrationsCreateResponseLinks,
    state: UpdateStatusGetResponseState,
    updatedAt: S.String.pipe(T.Body("updated_at")),
    context: S.optional(S.NullOr(UpdateStatusGetResponseContextMap)),
    error: S.optional(S.NullOr(RegistrationsCreateResponseError)),
  }),
).annotate({
  identifier: "UpdateStatusGetResponse",
}) as any as S.Schema<UpdateStatusGetResponse>;

export type CheckError = CloudflareOpError;
/** Performs real-time, authoritative availability checks directly against domain registries. Use this endpoint to verify a domain is available before attempting registration via `POST /registrations`. */
export const check: API.OperationMethod<
  CheckRequest,
  CheckResponse,
  CheckError,
  CloudflareOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CheckRequest,
  output: CheckResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
  retry: Retry.Retry,
}));

export type ExtensionsGetError = CloudflareOpError;
/** Returns metadata and JSON Schema documents describing the expected input structure for registration operations on a specific extension (TLD). Supports HTTP conditional GET via `ETag`. Include the `ETag` value from a previous response in an `If-None-Match` header to receive a `304 Not Modified` when the data has not changed. */
export const extensionsGet: API.OperationMethod<
  ExtensionsGetRequest,
  ExtensionsGetResponse,
  ExtensionsGetError,
  CloudflareOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExtensionsGetRequest,
  output: ExtensionsGetResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
  retry: Retry.Retry,
}));

export type ExtensionsListError = CloudflareOpError;
/** Returns metadata and JSON Schema documents describing the expected input structure for registration operations on each supported extension (TLD). This endpoint uses cursor-based pagination. Results are ordered by extension name by default. To fetch the next page, pass the `cursor`value from the `result_info` object in the response as the `cursor`query parameter in your next request. An empty `cursor` string indicates there are no more pages. Supports HTTP conditional GET via `ETag`. Include the `ETag` value from a previous response in an `If-None-Match` header to receive a `304 Not Modified` when the data has not changed. */
export const extensionsList: API.OperationMethod<
  ExtensionsListRequest,
  ExtensionsListResponse,
  ExtensionsListError,
  CloudflareOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExtensionsListRequest,
  output: ExtensionsListResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
  retry: Retry.Retry,
}));

export type RegistrationsCreateError = CloudflareOpError;
/** Starts a domain registration workflow. */
export const registrationsCreate: API.OperationMethod<
  RegistrationsCreateRequest,
  RegistrationsCreateResponse,
  RegistrationsCreateError,
  CloudflareOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RegistrationsCreateRequest,
  output: RegistrationsCreateResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
  retry: Retry.Retry,
}));

export type RegistrationsEditError = CloudflareOpError;
/** Updates an existing domain registration. By default, the server holds the connection for a bounded, server-defined amount of time while the update completes. Most updates finish within this window and return `200 OK` with a completed workflow status. If the update is still processing after this synchronous wait window, the server returns `202 Accepted`. Poll the URL in `links.self` to track progress. To skip the wait and receive an immediate `202`, send `Prefer: respond-async`. This endpoint currently supports updating `auto_renew` only. */
export const registrationsEdit: API.OperationMethod<
  RegistrationsEditRequest,
  RegistrationsEditResponse,
  RegistrationsEditError,
  CloudflareOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RegistrationsEditRequest,
  output: RegistrationsEditResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
  retry: Retry.Retry,
}));

export type RegistrationsGetError = CloudflareOpError;
/** Returns the current state of a domain registration. This is the canonical read endpoint for a domain you own. It returns the full registration resource including current settings and expiration. When the registration resource is ready, both `created_at` and `expires_at`are present in the response. */
export const registrationsGet: API.OperationMethod<
  RegistrationsGetRequest,
  RegistrationsGetResponse,
  RegistrationsGetError,
  CloudflareOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RegistrationsGetRequest,
  output: RegistrationsGetResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
  retry: Retry.Retry,
}));

export type RegistrationsListError = CloudflareOpError;
/** Returns a paginated list of domain registrations owned by the account. This endpoint uses cursor-based pagination. Results are ordered by registration date by default. To fetch the next page, pass the `cursor` value from the `result_info` object in the response as the `cursor` query parameter in your next request. An empty `cursor` string indicates there are no more pages. */
export const registrationsList: API.OperationMethod<
  RegistrationsListRequest,
  RegistrationsListResponse,
  RegistrationsListError,
  CloudflareOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RegistrationsListRequest,
  output: RegistrationsListResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
  retry: Retry.Retry,
}));

export type RegistrationStatusGetError = CloudflareOpError;
/** Returns the current status of a domain registration workflow. Use this endpoint to poll for completion when the POST response returned `202 Accepted`. The URL is provided in the `links.self`field of the workflow status response. Poll this endpoint until the workflow reaches a terminal state or a state that requires user attention. * `action_required` has `completed: false` and will not resolve on its own. The workflow is paused pending user intervention. * `blocked` has `completed: false` and indicates the workflow is waiting on a third party such as the extension registry or losing registrar. Continue polling while informing the user of the delay. Use increasing backoff between polls. When `state: blocked`, use a longer polling interval and do not poll indefinitely. A naive polling loop that only checks `completed` can run indefinitely when `state: action_required`. Break explicitly on `action_required`: ```js let status; do { await new Promise(r => setTimeout(r, 2000)); status = await cloudflare.request({ method: 'GET', path: reg.result.links.self, }); } while ( !status.result.completed && status.result.state !== 'action_required' ); if (status.result.state === 'action_required') { // Surface context.action and context.confirmation_sent_to to the user. // Do not re-submit the registration request. } ``` */
export const registrationStatusGet: API.OperationMethod<
  RegistrationStatusGetRequest,
  RegistrationStatusGetResponse,
  RegistrationStatusGetError,
  CloudflareOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RegistrationStatusGetRequest,
  output: RegistrationStatusGetResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
  retry: Retry.Retry,
}));

export type SearchError = CloudflareOpError;
/** Searches for domain name suggestions based on a keyword, phrase, or partial domain name. Returns a list of potentially available domains with pricing information. Suggestions are scoped to extensions supported for programmatic registration via this API (`POST /registrations`). Domains on unsupported extensions will not appear in results, even if they are available at the registry level. */
export const search: API.OperationMethod<
  SearchRequest,
  SearchResponse,
  SearchError,
  CloudflareOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SearchRequest,
  output: SearchResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
  retry: Retry.Retry,
}));

export type UpdateStatusGetError = CloudflareOpError;
/** Returns the current status of a domain update workflow. Use this endpoint to poll for completion when the PATCH response returned `202 Accepted`. The URL is provided in the `links.self`field of the workflow status response. Poll this endpoint until the workflow reaches a terminal state or a state that requires user attention. Use increasing backoff between polls. When the workflow remains blocked on a third party, use a longer polling interval and do not poll indefinitely. */
export const updateStatusGet: API.OperationMethod<
  UpdateStatusGetRequest,
  UpdateStatusGetResponse,
  UpdateStatusGetError,
  CloudflareOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateStatusGetRequest,
  output: UpdateStatusGetResponse,
  errors: [CloudflareRateLimited, CloudflareError],
  protocol: CloudflareProtocol,
  retry: Retry.Retry,
}));
