openapi: 3.1.0
info:
  title: Cloudflare CUSTOM-CERTIFICATES API
  version: 0.0.0
servers:
  - url: https://api.cloudflare.com/client/v4
x-distilled-service: custom-certificates
paths:
  /zones/{zone_id}/custom_certificates/{customCertificateId}:
    get:
      operationId: getCustomCertificate
      description: "Retrieves details for a specific custom SSL certificate, including
        certificate metadata, bundle method, geographic restrictions, and
        associated keyless server configuration.  @example ```ts const
        customCertificate = await client.customCertificates.get(
        '023e105f4ecef8ad9ca31a8372d0c353', { zone_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: customCertificateId
          in: path
          required: true
          schema:
            type: string
        - name: zone_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier.
                  zone_id:
                    type: string
                    description: Identifier.
                  bundle_method:
                    type: string
                    enum:
                      - ubiquitous
                      - optimal
                      - force
                    description: A ubiquitous bundle has the highest probability of being verified
                      everywhere, even by clients using outdated or unusual
                      trust stores. An optimal bundle uses the shortest chain
                      and newest intermediates. And the force bundle verifies
                      the chain, but does not otherwise modify it.
                  custom_csr_id:
                    type: string
                    description: The identifier for the Custom CSR that was used.
                  expires_on:
                    type: string
                    description: When the certificate from the authority expires.
                  geo_restrictions:
                    type: object
                    properties:
                      label:
                        type: string
                        enum:
                          - us
                          - eu
                          - highest_security
                    description: Specify the region where your private key can be held locally for
                      optimal TLS performance. HTTPS connections to any excluded
                      data center will still be fully encrypted, but will incur
                      some latency while Keyless SSL is used to complete the
                      handshake with the nearest allowed data center. Options
                      allow distribution to only to U.S. data centers, only to
                      E.U. data centers, or only to highest security data
                      centers. Default distribution is to all Cloudflare
                      datacenters, for optimal performance.
                  hosts:
                    type: array
                    items:
                      type: string
                  issuer:
                    type: string
                    description: The certificate authority that issued the certificate.
                  keyless_server:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Keyless certificate identifier tag.
                      created_on:
                        type: string
                        description: When the Keyless SSL was created.
                      enabled:
                        type: boolean
                        description: Whether or not the Keyless SSL is on or off.
                      host:
                        type: string
                        description: The keyless SSL name.
                      modified_on:
                        type: string
                        description: When the Keyless SSL was last modified.
                      name:
                        type: string
                        description: The keyless SSL name.
                      permissions:
                        type: array
                        items:
                          type: string
                        description: Available permissions for the Keyless SSL for the current user
                          requesting the item.
                      port:
                        type: number
                        description: The keyless SSL port used to communicate between Cloudflare and the
                          client's Keyless SSL server.
                      status:
                        type: string
                        enum:
                          - active
                          - deleted
                        description: Status of the Keyless SSL.
                      tunnel:
                        type: object
                        properties:
                          private_ip:
                            type: string
                            description: Private IP of the Key Server Host
                          vnet_id:
                            type: string
                            description: Cloudflare Tunnel Virtual Network ID
                        required:
                          - private_ip
                          - vnet_id
                        description: Configuration for using Keyless SSL through a Cloudflare Tunnel
                    required:
                      - id
                      - created_on
                      - enabled
                      - host
                      - modified_on
                      - name
                      - permissions
                      - port
                      - status
                  modified_on:
                    type: string
                    description: When the certificate was last modified.
                  policy_restrictions:
                    type: string
                    description: 'The policy restrictions returned by the API. This field is
                      returned in responses when a policy has been set. The API
                      accepts the "policy" field in requests but returns this
                      field as "policy_restrictions" in responses.  Specifies
                      the region(s) where your private key can be held locally
                      for optimal TLS performance. Format is a boolean
                      expression, for example: "(country: US) or (region: EU)"'
                  priority:
                    type: number
                    description: The order/priority in which the certificate will be used in a
                      request. The higher priority will break ties across
                      overlapping 'legacy_custom' certificates, but
                      'legacy_custom' certificates will always supercede
                      'sni_custom' certificates.
                  signature:
                    type: string
                    description: The type of hash used for the certificate.
                  status:
                    type: string
                    enum:
                      - active
                      - expired
                      - deleted
                      - pending
                      - initializing
                    description: Status of the zone's custom SSL.
                  uploaded_on:
                    type: string
                    description: When the certificate was uploaded to Cloudflare.
                required:
                  - id
                  - zone_id
      x-distilled-response-path: result
      x-distilled-errors:
        CustomCertificateNotFound:
          - status: 404
        PlanLevelNotAllowed:
          - code: 1011
        Forbidden:
          - status: 403
    patch:
      operationId: patchCustomCertificate
      description: "Upload a new private key and/or PEM/CRT for the SSL certificate.
        Note: PATCHing a configuration for sni_custom certificates will result
        in a new resource id being returned, and the previous one being
        deleted.  @example ```ts const customCertificate = await
        client.customCertificates.edit( '023e105f4ecef8ad9ca31a8372d0c353', {
        zone_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: customCertificateId
          in: path
          required: true
          schema:
            type: string
        - name: zone_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                bundle_method:
                  type: string
                  enum:
                    - ubiquitous
                    - optimal
                    - force
                  description: "Body param: A ubiquitous bundle has the highest probability of
                    being verified everywhere, even by clients using outdated or
                    unusual trust stores. An optimal bundle uses the shortest
                    chain and newest intermediates. And the force bundle
                    verifies the chain, but does not otherwise modify it."
                certificate:
                  type: string
                  description: "Body param: The zone's SSL certificate or certificate and the
                    intermediate(s)."
                custom_csr_id:
                  type: string
                  description: "Body param: The identifier for the Custom CSR that was used."
                deploy:
                  type: string
                  enum:
                    - staging
                    - production
                  description: "Body param: The environment to deploy the certificate to, defaults
                    to production"
                geo_restrictions:
                  type: object
                  properties:
                    label:
                      type: string
                      enum:
                        - us
                        - eu
                        - highest_security
                  description: "Body param: Specify the region where your private key can be held
                    locally for optimal TLS performance. HTTPS connections to
                    any excluded data center will still be fully encrypted, but
                    will incur some latency while Keyless SSL is used to
                    complete the handshake with the nearest allowed data center.
                    Options allow distribution to only to U.S. data centers,
                    only to E.U. data centers, or only to highest security data
                    centers. Default distribution is to all Cloudflare
                    datacenters, for optimal performance."
                policy:
                  type: string
                  description: "Body param: Specify the policy that determines the region where
                    your private key will be held locally. HTTPS connections to
                    any excluded data center will still be fully encrypted, but
                    will incur some latency while Keyless SSL is used to
                    complete the handshake with the nearest allowed data center.
                    Any combination of countries, specified by their two letter
                    country code
                    (https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2#Officiall\
                    y_assigned_code_elements) can be chosen, such as 'country:
                    IN', as well as 'region: EU' which refers to the EU region.
                    If there are too few data centers satisfying the policy, it
                    will be rejected. Note: The API accepts this field as either
                    \"policy\" or \"policy_restrictions\" in requests. Responses
                    return this field as \"policy_restrictions\"."
                private_key:
                  type: string
                  description: "Body param: The zone's private key. Not required if custom_csr_id
                    is provided, in which case the private key is retrieved from
                    the CSR record held by Cloudflare."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier.
                  zone_id:
                    type: string
                    description: Identifier.
                  bundle_method:
                    type: string
                    enum:
                      - ubiquitous
                      - optimal
                      - force
                    description: A ubiquitous bundle has the highest probability of being verified
                      everywhere, even by clients using outdated or unusual
                      trust stores. An optimal bundle uses the shortest chain
                      and newest intermediates. And the force bundle verifies
                      the chain, but does not otherwise modify it.
                  custom_csr_id:
                    type: string
                    description: The identifier for the Custom CSR that was used.
                  expires_on:
                    type: string
                    description: When the certificate from the authority expires.
                  geo_restrictions:
                    type: object
                    properties:
                      label:
                        type: string
                        enum:
                          - us
                          - eu
                          - highest_security
                    description: Specify the region where your private key can be held locally for
                      optimal TLS performance. HTTPS connections to any excluded
                      data center will still be fully encrypted, but will incur
                      some latency while Keyless SSL is used to complete the
                      handshake with the nearest allowed data center. Options
                      allow distribution to only to U.S. data centers, only to
                      E.U. data centers, or only to highest security data
                      centers. Default distribution is to all Cloudflare
                      datacenters, for optimal performance.
                  hosts:
                    type: array
                    items:
                      type: string
                  issuer:
                    type: string
                    description: The certificate authority that issued the certificate.
                  keyless_server:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Keyless certificate identifier tag.
                      created_on:
                        type: string
                        description: When the Keyless SSL was created.
                      enabled:
                        type: boolean
                        description: Whether or not the Keyless SSL is on or off.
                      host:
                        type: string
                        description: The keyless SSL name.
                      modified_on:
                        type: string
                        description: When the Keyless SSL was last modified.
                      name:
                        type: string
                        description: The keyless SSL name.
                      permissions:
                        type: array
                        items:
                          type: string
                        description: Available permissions for the Keyless SSL for the current user
                          requesting the item.
                      port:
                        type: number
                        description: The keyless SSL port used to communicate between Cloudflare and the
                          client's Keyless SSL server.
                      status:
                        type: string
                        enum:
                          - active
                          - deleted
                        description: Status of the Keyless SSL.
                      tunnel:
                        type: object
                        properties:
                          private_ip:
                            type: string
                            description: Private IP of the Key Server Host
                          vnet_id:
                            type: string
                            description: Cloudflare Tunnel Virtual Network ID
                        required:
                          - private_ip
                          - vnet_id
                        description: Configuration for using Keyless SSL through a Cloudflare Tunnel
                    required:
                      - id
                      - created_on
                      - enabled
                      - host
                      - modified_on
                      - name
                      - permissions
                      - port
                      - status
                  modified_on:
                    type: string
                    description: When the certificate was last modified.
                  policy_restrictions:
                    type: string
                    description: 'The policy restrictions returned by the API. This field is
                      returned in responses when a policy has been set. The API
                      accepts the "policy" field in requests but returns this
                      field as "policy_restrictions" in responses.  Specifies
                      the region(s) where your private key can be held locally
                      for optimal TLS performance. Format is a boolean
                      expression, for example: "(country: US) or (region: EU)"'
                  priority:
                    type: number
                    description: The order/priority in which the certificate will be used in a
                      request. The higher priority will break ties across
                      overlapping 'legacy_custom' certificates, but
                      'legacy_custom' certificates will always supercede
                      'sni_custom' certificates.
                  signature:
                    type: string
                    description: The type of hash used for the certificate.
                  status:
                    type: string
                    enum:
                      - active
                      - expired
                      - deleted
                      - pending
                      - initializing
                    description: Status of the zone's custom SSL.
                  uploaded_on:
                    type: string
                    description: When the certificate was uploaded to Cloudflare.
                required:
                  - id
                  - zone_id
      x-distilled-response-path: result
      x-distilled-errors:
        CustomCertificateNotFound:
          - status: 404
        PlanLevelNotAllowed:
          - code: 1011
        Forbidden:
          - status: 403
    delete:
      operationId: deleteCustomCertificate
      description: "Remove a SSL certificate from a zone.  @example ```ts const
        customCertificate = await client.customCertificates.delete(
        '023e105f4ecef8ad9ca31a8372d0c353', { zone_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: customCertificateId
          in: path
          required: true
          schema:
            type: string
        - name: zone_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier.
      x-distilled-response-path: result
      x-distilled-errors:
        CustomCertificateNotFound:
          - status: 404
          - code: 1002
            message:
              includes: Invalid certificate
        PlanLevelNotAllowed:
          - code: 1011
        Forbidden:
          - status: 403
  /zones/{zone_id}/custom_certificates:
    get:
      operationId: listCustomCertificates
      description: "List, search, and filter all of your custom SSL certificates. The
        higher priority will break ties across overlapping 'legacy_custom'
        certificates, but 'legacy_custom' certificates will always supercede
        'sni_custom' certificates.  @example ```ts // Automatically fetches more
        pages as needed. for await (const customCertificate of
        client.customCertificates.list( { zone_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: zone_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
        - name: page
          in: query
          required: false
          schema:
            type: number
        - name: per_page
          in: query
          required: false
          schema:
            type: number
        - name: match
          in: query
          required: false
          description: "Query param: Whether to match all search requirements or at least
            one (any)."
          schema:
            type: string
            enum:
              - any
              - all
        - name: status
          in: query
          required: false
          description: "Query param: Status of the zone's custom SSL."
          schema:
            type: string
            enum:
              - active
              - expired
              - deleted
              - pending
              - initializing
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier.
                  zone_id:
                    type: string
                    description: Identifier.
                  bundle_method:
                    type: string
                    enum:
                      - ubiquitous
                      - optimal
                      - force
                    description: A ubiquitous bundle has the highest probability of being verified
                      everywhere, even by clients using outdated or unusual
                      trust stores. An optimal bundle uses the shortest chain
                      and newest intermediates. And the force bundle verifies
                      the chain, but does not otherwise modify it.
                  custom_csr_id:
                    type: string
                    description: The identifier for the Custom CSR that was used.
                  expires_on:
                    type: string
                    description: When the certificate from the authority expires.
                  geo_restrictions:
                    type: object
                    properties:
                      label:
                        type: string
                        enum:
                          - us
                          - eu
                          - highest_security
                    description: Specify the region where your private key can be held locally for
                      optimal TLS performance. HTTPS connections to any excluded
                      data center will still be fully encrypted, but will incur
                      some latency while Keyless SSL is used to complete the
                      handshake with the nearest allowed data center. Options
                      allow distribution to only to U.S. data centers, only to
                      E.U. data centers, or only to highest security data
                      centers. Default distribution is to all Cloudflare
                      datacenters, for optimal performance.
                  hosts:
                    type: array
                    items:
                      type: string
                  issuer:
                    type: string
                    description: The certificate authority that issued the certificate.
                  keyless_server:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Keyless certificate identifier tag.
                      created_on:
                        type: string
                        description: When the Keyless SSL was created.
                      enabled:
                        type: boolean
                        description: Whether or not the Keyless SSL is on or off.
                      host:
                        type: string
                        description: The keyless SSL name.
                      modified_on:
                        type: string
                        description: When the Keyless SSL was last modified.
                      name:
                        type: string
                        description: The keyless SSL name.
                      permissions:
                        type: array
                        items:
                          type: string
                        description: Available permissions for the Keyless SSL for the current user
                          requesting the item.
                      port:
                        type: number
                        description: The keyless SSL port used to communicate between Cloudflare and the
                          client's Keyless SSL server.
                      status:
                        type: string
                        enum:
                          - active
                          - deleted
                        description: Status of the Keyless SSL.
                      tunnel:
                        type: object
                        properties:
                          private_ip:
                            type: string
                            description: Private IP of the Key Server Host
                          vnet_id:
                            type: string
                            description: Cloudflare Tunnel Virtual Network ID
                        required:
                          - private_ip
                          - vnet_id
                        description: Configuration for using Keyless SSL through a Cloudflare Tunnel
                    required:
                      - id
                      - created_on
                      - enabled
                      - host
                      - modified_on
                      - name
                      - permissions
                      - port
                      - status
                  modified_on:
                    type: string
                    description: When the certificate was last modified.
                  policy_restrictions:
                    type: string
                    description: 'The policy restrictions returned by the API. This field is
                      returned in responses when a policy has been set. The API
                      accepts the "policy" field in requests but returns this
                      field as "policy_restrictions" in responses.  Specifies
                      the region(s) where your private key can be held locally
                      for optimal TLS performance. Format is a boolean
                      expression, for example: "(country: US) or (region: EU)"'
                  priority:
                    type: number
                    description: The order/priority in which the certificate will be used in a
                      request. The higher priority will break ties across
                      overlapping 'legacy_custom' certificates, but
                      'legacy_custom' certificates will always supercede
                      'sni_custom' certificates.
                  signature:
                    type: string
                    description: The type of hash used for the certificate.
                  status:
                    type: string
                    enum:
                      - active
                      - expired
                      - deleted
                      - pending
                      - initializing
                    description: Status of the zone's custom SSL.
                  uploaded_on:
                    type: string
                    description: When the certificate was uploaded to Cloudflare.
                required:
                  - id
                  - zone_id
      x-distilled-pagination-class: CustomCertificatesV4PagePaginationArray
      x-distilled-errors:
        PlanLevelNotAllowed:
          - code: 1011
        Forbidden:
          - status: 403
    post:
      operationId: createCustomCertificate
      description: "Upload a new SSL certificate for a zone.  @example ```ts const
        customCertificate = await client.customCertificates.create({ zone_id:
        '023e105f4ecef8ad9ca31a8372d0c353', certificate: '-----BEGIN
        CERTIFICATE-----\\nMIIDtTCCAp2gAwIBAgIJAMHAwfXZ5/PWMA0GCSqGSIb3DQEBCwUA\
        MEUxCzAJBgNV\\nBAYTAkFVMRMwEQYDVQQIEwpTb21lLVN0YXRlMSEwHwYDVQQKExhJbnRl\
        cm5ldCBX\\naWRnaXRzIFB0eSBMdGQwHhcNMTYwODI0MTY0MzAxWhcNMTYxMTIyMTY0MzAx\
        WjBF\\nMQswCQYDVQQGEwJBVTETMBEGA1UECBMKU29tZS1TdGF0ZTEhMB8GA1UEChMYSW50\
        \\nZXJuZXQgV2lkZ2l0cyBQdHkgTHRkMIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIB\\nC\
        gKCAQEAwQHoetcl9+5ikGzV6cMzWtWPJHqXT3wpbEkRU9Yz7lgvddmGdtcGbg/1\\nCGZu0\
        jJGkMoppoUo4c3dts3iwqRYmBikUP77wwY2QGmDZw2FvkJCJlKnabIRuGvB\\nKwzESIXgK\
        k2016aTP6/dAjEHyo6SeoK8lkIySUvK0fyOVlsiEsCmOpidtnKX/a+5\\n0GjB79CJH4ER2\
        lLVZnhePFR/zUOyPxZQQ4naHf7yu/b5jhO0f8fwt+pyFxIXjbEI\\ndZliWRkRMtzrHOJIh\
        rmJ2A1J7iOrirbbwillwjjNVUWPf3IJ3M12S9pEewooaeO2\\nizNTERcG9HzAacbVRn2Y2\
        SWIyT/18QIDAQABo4GnMIGkMB0GA1UdDgQWBBT/LbE4\\n9rWf288N6sJA5BRb6FJIGDB1B\
        gNVHSMEbjBsgBT/LbE49rWf288N6sJA5BRb6FJI\\nGKFJpEcwRTELMAkGA1UEBhMCQVUxE\
        zARBgNVBAgTClNvbWUtU3RhdGUxITAfBgNV\\nBAoTGEludGVybmV0IFdpZGdpdHMgUHR5I\
        Ex0ZIIJAMHAwfXZ5/PWMAwGA1UdEwQF\\nMAMBAf8wDQYJKoZIhvcNAQELBQADggEBAHHFw\
        l0tH0quUYZYO0dZYt4R7SJ0pCm2\\n2satiyzHl4OnXcHDpekAo7/a09c6Lz6AU83cKy/+x\
        3/djYHXWba7HpEu0dR3ugQP\\nMlr4zrhd9xKZ0KZKiYmtJH+ak4OM4L3FbT0owUZPyjLSl\
        hMtJVcoRp5CJsjAMBUG\\nSvD8RX+T01wzox/Qb+lnnNnOlaWpqu8eoOenybxKp1a9ULzIV\
        vN/LAcc+14vioFq\\n2swRWtmocBAs8QR9n4uvbpiYvS8eYueDCWMM4fvFfBhaDZ3N9Ibty\
        Sh3SpFdQDhw\\nYbjM2rxXiyLGxB4Bol7QTv4zHif7Zt89FReT/NBy4rzaskDJY5L6xmY=\
        \\n-----END CERTIFICATE-----\\n', }); ```"
      parameters:
        - name: zone_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                certificate:
                  type: string
                  description: "Body param: The zone's SSL certificate or certificate and the
                    intermediate(s)."
                bundle_method:
                  type: string
                  enum:
                    - ubiquitous
                    - optimal
                    - force
                  description: "Body param: A ubiquitous bundle has the highest probability of
                    being verified everywhere, even by clients using outdated or
                    unusual trust stores. An optimal bundle uses the shortest
                    chain and newest intermediates. And the force bundle
                    verifies the chain, but does not otherwise modify it."
                custom_csr_id:
                  type: string
                  description: "Body param: The identifier for the Custom CSR that was used."
                deploy:
                  type: string
                  enum:
                    - staging
                    - production
                  description: "Body param: The environment to deploy the certificate to, defaults
                    to production"
                geo_restrictions:
                  type: object
                  properties:
                    label:
                      type: string
                      enum:
                        - us
                        - eu
                        - highest_security
                  description: "Body param: Specify the region where your private key can be held
                    locally for optimal TLS performance. HTTPS connections to
                    any excluded data center will still be fully encrypted, but
                    will incur some latency while Keyless SSL is used to
                    complete the handshake with the nearest allowed data center.
                    Options allow distribution to only to U.S. data centers,
                    only to E.U. data centers, or only to highest security data
                    centers. Default distribution is to all Cloudflare
                    datacenters, for optimal performance."
                policy:
                  type: string
                  description: "Body param: Specify the policy that determines the region where
                    your private key will be held locally. HTTPS connections to
                    any excluded data center will still be fully encrypted, but
                    will incur some latency while Keyless SSL is used to
                    complete the handshake with the nearest allowed data center.
                    Any combination of countries, specified by their two letter
                    country code
                    (https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2#Officiall\
                    y_assigned_code_elements) can be chosen, such as 'country:
                    IN', as well as 'region: EU' which refers to the EU region.
                    If there are too few data centers satisfying the policy, it
                    will be rejected. Note: The API accepts this field as either
                    \"policy\" or \"policy_restrictions\" in requests. Responses
                    return this field as \"policy_restrictions\"."
                private_key:
                  type: string
                  description: "Body param: The zone's private key. Not required if custom_csr_id
                    is provided, in which case the private key is retrieved from
                    the CSR record held by Cloudflare."
                type:
                  type: string
                  enum:
                    - legacy_custom
                    - sni_custom
                  description: "Body param: The type 'legacy_custom' enables support for legacy
                    clients which do not include SNI in the TLS handshake."
              required:
                - certificate
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier.
                  zone_id:
                    type: string
                    description: Identifier.
                  bundle_method:
                    type: string
                    enum:
                      - ubiquitous
                      - optimal
                      - force
                    description: A ubiquitous bundle has the highest probability of being verified
                      everywhere, even by clients using outdated or unusual
                      trust stores. An optimal bundle uses the shortest chain
                      and newest intermediates. And the force bundle verifies
                      the chain, but does not otherwise modify it.
                  custom_csr_id:
                    type: string
                    description: The identifier for the Custom CSR that was used.
                  expires_on:
                    type: string
                    description: When the certificate from the authority expires.
                  geo_restrictions:
                    type: object
                    properties:
                      label:
                        type: string
                        enum:
                          - us
                          - eu
                          - highest_security
                    description: Specify the region where your private key can be held locally for
                      optimal TLS performance. HTTPS connections to any excluded
                      data center will still be fully encrypted, but will incur
                      some latency while Keyless SSL is used to complete the
                      handshake with the nearest allowed data center. Options
                      allow distribution to only to U.S. data centers, only to
                      E.U. data centers, or only to highest security data
                      centers. Default distribution is to all Cloudflare
                      datacenters, for optimal performance.
                  hosts:
                    type: array
                    items:
                      type: string
                  issuer:
                    type: string
                    description: The certificate authority that issued the certificate.
                  keyless_server:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Keyless certificate identifier tag.
                      created_on:
                        type: string
                        description: When the Keyless SSL was created.
                      enabled:
                        type: boolean
                        description: Whether or not the Keyless SSL is on or off.
                      host:
                        type: string
                        description: The keyless SSL name.
                      modified_on:
                        type: string
                        description: When the Keyless SSL was last modified.
                      name:
                        type: string
                        description: The keyless SSL name.
                      permissions:
                        type: array
                        items:
                          type: string
                        description: Available permissions for the Keyless SSL for the current user
                          requesting the item.
                      port:
                        type: number
                        description: The keyless SSL port used to communicate between Cloudflare and the
                          client's Keyless SSL server.
                      status:
                        type: string
                        enum:
                          - active
                          - deleted
                        description: Status of the Keyless SSL.
                      tunnel:
                        type: object
                        properties:
                          private_ip:
                            type: string
                            description: Private IP of the Key Server Host
                          vnet_id:
                            type: string
                            description: Cloudflare Tunnel Virtual Network ID
                        required:
                          - private_ip
                          - vnet_id
                        description: Configuration for using Keyless SSL through a Cloudflare Tunnel
                    required:
                      - id
                      - created_on
                      - enabled
                      - host
                      - modified_on
                      - name
                      - permissions
                      - port
                      - status
                  modified_on:
                    type: string
                    description: When the certificate was last modified.
                  policy_restrictions:
                    type: string
                    description: 'The policy restrictions returned by the API. This field is
                      returned in responses when a policy has been set. The API
                      accepts the "policy" field in requests but returns this
                      field as "policy_restrictions" in responses.  Specifies
                      the region(s) where your private key can be held locally
                      for optimal TLS performance. Format is a boolean
                      expression, for example: "(country: US) or (region: EU)"'
                  priority:
                    type: number
                    description: The order/priority in which the certificate will be used in a
                      request. The higher priority will break ties across
                      overlapping 'legacy_custom' certificates, but
                      'legacy_custom' certificates will always supercede
                      'sni_custom' certificates.
                  signature:
                    type: string
                    description: The type of hash used for the certificate.
                  status:
                    type: string
                    enum:
                      - active
                      - expired
                      - deleted
                      - pending
                      - initializing
                    description: Status of the zone's custom SSL.
                  uploaded_on:
                    type: string
                    description: When the certificate was uploaded to Cloudflare.
                required:
                  - id
                  - zone_id
      x-distilled-response-path: result
      x-distilled-errors:
        PlanLevelNotAllowed:
          - code: 1011
        Forbidden:
          - status: 403
  /zones/{zone_id}/custom_certificates/prioritize:
    put:
      operationId: putPrioritize
      description: "If a zone has multiple SSL certificates, you can set the order in
        which they should be used during a request. The higher priority will
        break ties across overlapping 'legacy_custom' certificates.  @example
        ```ts // Automatically fetches more pages as needed. for await (const
        customCertificate of client.customCertificates.prioritize.update( {
        zone_id: '023e105f4ecef8ad9ca31a8372d0c353', certificates: [{}, {}], },
        )) { // ... } ```"
      parameters:
        - name: zone_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                certificates:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier.
                      priority:
                        type: number
                        description: The order/priority in which the certificate will be used in a
                          request. The higher priority will break ties across
                          overlapping 'legacy_custom' certificates, but
                          'legacy_custom' certificates will always supercede
                          'sni_custom' certificates.
                  description: "Body param: Array of ordered certificates."
              required:
                - certificates
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier.
                  zone_id:
                    type: string
                    description: Identifier.
                  bundle_method:
                    type: string
                    enum:
                      - ubiquitous
                      - optimal
                      - force
                    description: A ubiquitous bundle has the highest probability of being verified
                      everywhere, even by clients using outdated or unusual
                      trust stores. An optimal bundle uses the shortest chain
                      and newest intermediates. And the force bundle verifies
                      the chain, but does not otherwise modify it.
                  custom_csr_id:
                    type: string
                    description: The identifier for the Custom CSR that was used.
                  expires_on:
                    type: string
                    description: When the certificate from the authority expires.
                  geo_restrictions:
                    type: object
                    properties:
                      label:
                        type: string
                        enum:
                          - us
                          - eu
                          - highest_security
                    description: Specify the region where your private key can be held locally for
                      optimal TLS performance. HTTPS connections to any excluded
                      data center will still be fully encrypted, but will incur
                      some latency while Keyless SSL is used to complete the
                      handshake with the nearest allowed data center. Options
                      allow distribution to only to U.S. data centers, only to
                      E.U. data centers, or only to highest security data
                      centers. Default distribution is to all Cloudflare
                      datacenters, for optimal performance.
                  hosts:
                    type: array
                    items:
                      type: string
                  issuer:
                    type: string
                    description: The certificate authority that issued the certificate.
                  keyless_server:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Keyless certificate identifier tag.
                      created_on:
                        type: string
                        description: When the Keyless SSL was created.
                      enabled:
                        type: boolean
                        description: Whether or not the Keyless SSL is on or off.
                      host:
                        type: string
                        description: The keyless SSL name.
                      modified_on:
                        type: string
                        description: When the Keyless SSL was last modified.
                      name:
                        type: string
                        description: The keyless SSL name.
                      permissions:
                        type: array
                        items:
                          type: string
                        description: Available permissions for the Keyless SSL for the current user
                          requesting the item.
                      port:
                        type: number
                        description: The keyless SSL port used to communicate between Cloudflare and the
                          client's Keyless SSL server.
                      status:
                        type: string
                        enum:
                          - active
                          - deleted
                        description: Status of the Keyless SSL.
                      tunnel:
                        type: object
                        properties:
                          private_ip:
                            type: string
                            description: Private IP of the Key Server Host
                          vnet_id:
                            type: string
                            description: Cloudflare Tunnel Virtual Network ID
                        required:
                          - private_ip
                          - vnet_id
                        description: Configuration for using Keyless SSL through a Cloudflare Tunnel
                    required:
                      - id
                      - created_on
                      - enabled
                      - host
                      - modified_on
                      - name
                      - permissions
                      - port
                      - status
                  modified_on:
                    type: string
                    description: When the certificate was last modified.
                  policy_restrictions:
                    type: string
                    description: 'The policy restrictions returned by the API. This field is
                      returned in responses when a policy has been set. The API
                      accepts the "policy" field in requests but returns this
                      field as "policy_restrictions" in responses.  Specifies
                      the region(s) where your private key can be held locally
                      for optimal TLS performance. Format is a boolean
                      expression, for example: "(country: US) or (region: EU)"'
                  priority:
                    type: number
                    description: The order/priority in which the certificate will be used in a
                      request. The higher priority will break ties across
                      overlapping 'legacy_custom' certificates, but
                      'legacy_custom' certificates will always supercede
                      'sni_custom' certificates.
                  signature:
                    type: string
                    description: The type of hash used for the certificate.
                  status:
                    type: string
                    enum:
                      - active
                      - expired
                      - deleted
                      - pending
                      - initializing
                    description: Status of the zone's custom SSL.
                  uploaded_on:
                    type: string
                    description: When the certificate was uploaded to Cloudflare.
                required:
                  - id
                  - zone_id
      x-distilled-pagination-class: CustomCertificatesSinglePage
      x-distilled-errors:
        PlanLevelNotAllowed:
          - code: 1011
        Forbidden:
          - status: 403
