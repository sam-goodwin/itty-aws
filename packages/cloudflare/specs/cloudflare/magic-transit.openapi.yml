openapi: 3.1.0
info:
  title: Cloudflare MAGIC-TRANSIT API
  version: 0.0.0
servers:
  - url: https://api.cloudflare.com/client/v4
x-distilled-service: magic-transit
paths:
  /accounts/{account_id}/magic/apps:
    get:
      operationId: listApps
      description: "Lists Apps associated with an account.  @example ```ts //
        Automatically fetches more pages as needed. for await (const
        appListResponse of client.magicTransit.apps.list( { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    properties:
                      account_app_id:
                        type: string
                        description: Magic account app ID.
                      hostnames:
                        type: array
                        items:
                          type: string
                        description: FQDNs to associate with traffic decisions.
                      ip_subnets:
                        type: array
                        items:
                          type: string
                        description: IPv4 CIDRs to associate with traffic decisions. (IPv6 CIDRs are
                          currently unsupported)
                      name:
                        type: string
                        description: Display name for the app.
                      type:
                        type: string
                        description: Category of the app.
                    required:
                      - account_app_id
                  - type: object
                    properties:
                      managed_app_id:
                        type: string
                        description: Managed app ID.
                      hostnames:
                        type: array
                        items:
                          type: string
                        description: FQDNs to associate with traffic decisions.
                      ip_subnets:
                        type: array
                        items:
                          type: string
                        description: IPv4 CIDRs to associate with traffic decisions. (IPv6 CIDRs are
                          currently unsupported)
                      name:
                        type: string
                        description: Display name for the app.
                      type:
                        type: string
                        description: Category of the app.
                    required:
                      - managed_app_id
      x-distilled-pagination-class: AppListResponsesSinglePage
    post:
      operationId: createApp
      description: "Creates a new App for an account  @example ```ts const app = await
        client.magicTransit.apps.create({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', name: 'Cloudflare Dashboard', type:
        'Development', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: "Body param: Display name for the app."
                type:
                  type: string
                  description: "Body param: Category of the app."
                hostnames:
                  type: array
                  items:
                    type: string
                  description: "Body param: FQDNs to associate with traffic decisions."
                ip_subnets:
                  type: array
                  items:
                    type: string
                  description: "Body param: IPv4 CIDRs to associate with traffic decisions. (IPv6
                    CIDRs are currently unsupported)"
              required:
                - name
                - type
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  account_app_id:
                    type: string
                    description: Magic account app ID.
                  hostnames:
                    type: array
                    items:
                      type: string
                    description: FQDNs to associate with traffic decisions.
                  ip_subnets:
                    type: array
                    items:
                      type: string
                    description: IPv4 CIDRs to associate with traffic decisions. (IPv6 CIDRs are
                      currently unsupported)
                  name:
                    type: string
                    description: Display name for the app.
                  type:
                    type: string
                    description: Category of the app.
                required:
                  - account_app_id
      x-distilled-response-path: result
  /accounts/{account_id}/magic/apps/{accountAppId}:
    put:
      operationId: updateApp
      description: "Updates an Account App  @example ```ts const app = await
        client.magicTransit.apps.update( '023e105f4ecef8ad9ca31a8372d0c353', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: accountAppId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                hostnames:
                  type: array
                  items:
                    type: string
                  description: "Body param: FQDNs to associate with traffic decisions."
                ip_subnets:
                  type: array
                  items:
                    type: string
                  description: "Body param: IPv4 CIDRs to associate with traffic decisions. (IPv6
                    CIDRs are currently unsupported)"
                name:
                  type: string
                  description: "Body param: Display name for the app."
                type:
                  type: string
                  description: "Body param: Category of the app."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  account_app_id:
                    type: string
                    description: Magic account app ID.
                  hostnames:
                    type: array
                    items:
                      type: string
                    description: FQDNs to associate with traffic decisions.
                  ip_subnets:
                    type: array
                    items:
                      type: string
                    description: IPv4 CIDRs to associate with traffic decisions. (IPv6 CIDRs are
                      currently unsupported)
                  name:
                    type: string
                    description: Display name for the app.
                  type:
                    type: string
                    description: Category of the app.
                required:
                  - account_app_id
      x-distilled-response-path: result
    patch:
      operationId: patchApp
      description: "Updates an Account App  @example ```ts const response = await
        client.magicTransit.apps.edit( '023e105f4ecef8ad9ca31a8372d0c353', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: accountAppId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                hostnames:
                  type: array
                  items:
                    type: string
                  description: "Body param: FQDNs to associate with traffic decisions."
                ip_subnets:
                  type: array
                  items:
                    type: string
                  description: "Body param: IPv4 CIDRs to associate with traffic decisions. (IPv6
                    CIDRs are currently unsupported)"
                name:
                  type: string
                  description: "Body param: Display name for the app."
                type:
                  type: string
                  description: "Body param: Category of the app."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  account_app_id:
                    type: string
                    description: Magic account app ID.
                  hostnames:
                    type: array
                    items:
                      type: string
                    description: FQDNs to associate with traffic decisions.
                  ip_subnets:
                    type: array
                    items:
                      type: string
                    description: IPv4 CIDRs to associate with traffic decisions. (IPv6 CIDRs are
                      currently unsupported)
                  name:
                    type: string
                    description: Display name for the app.
                  type:
                    type: string
                    description: Category of the app.
                required:
                  - account_app_id
      x-distilled-response-path: result
    delete:
      operationId: deleteApp
      description: "Deletes specific Account App.  @example ```ts const app = await
        client.magicTransit.apps.delete( '023e105f4ecef8ad9ca31a8372d0c353', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: accountAppId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  account_app_id:
                    type: string
                    description: Magic account app ID.
                  hostnames:
                    type: array
                    items:
                      type: string
                    description: FQDNs to associate with traffic decisions.
                  ip_subnets:
                    type: array
                    items:
                      type: string
                    description: IPv4 CIDRs to associate with traffic decisions. (IPv6 CIDRs are
                      currently unsupported)
                  name:
                    type: string
                    description: Display name for the app.
                  type:
                    type: string
                    description: Category of the app.
                required:
                  - account_app_id
      x-distilled-response-path: result
  /accounts/{account_id}/magic/cf_interconnects/{cfInterconnectId}:
    get:
      operationId: getCfInterconnect
      description: "Lists details for a specific interconnect.  @example ```ts const
        cfInterconnect = await client.magicTransit.cfInterconnects.get(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: cfInterconnectId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the response
            body will be presented using the new object format. Defaults to
            false."
          schema:
            type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  interconnect:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      automatic_return_routing:
                        type: boolean
                        description: True if automatic stateful return routing should be enabled for a
                          tunnel, false otherwise.
                      colo_name:
                        type: string
                        description: The name of the interconnect. The name cannot share a name with
                          other tunnels.
                      created_on:
                        type: string
                        description: The date and time the tunnel was created.
                      description:
                        type: string
                        description: An optional description of the interconnect.
                      gre:
                        type: object
                        properties:
                          cloudflare_endpoint:
                            type: string
                            description: The IP address assigned to the Cloudflare side of the GRE tunnel
                              created as part of the Interconnect.
                        description: The configuration specific to GRE interconnects.
                      health_check:
                        type: object
                        properties:
                          enabled:
                            type: boolean
                            description: Determines whether to run healthchecks for a tunnel.
                          rate:
                            type: string
                            enum:
                              - low
                              - mid
                              - high
                            description: How frequent the health check is run. The default value is `mid`.
                          target:
                            oneOf:
                              - type: object
                                properties:
                                  effective:
                                    type: string
                                    description: The effective health check target. If 'saved' is empty, then this
                                      field will be populated with the
                                      calculated default value on GET requests.
                                      Ignored in POST, PUT, and PATCH requests.
                                  saved:
                                    type: string
                                    description: The saved health check target. Setting the value to the empty
                                      string indicates that the calculated
                                      default value will be used.
                              - type: string
                            description: The destination address in a request type health check. After the
                              healthcheck is decapsulated at the customer end of
                              the tunnel, the ICMP echo will be forwarded to
                              this address. This field defaults to
                              `customer_gre_endpoint address`. This field is
                              ignored for bidirectional healthchecks as the
                              interface_address (not assigned to the Cloudflare
                              side of the tunnel) is used as the target. Must be
                              in object form if the x-magic-new-hc-target header
                              is set to true and string form if
                              x-magic-new-hc-target is absent or set to false.
                          type:
                            type: string
                            enum:
                              - reply
                              - request
                            description: The type of healthcheck to run, reply or request. The default value
                              is `reply`.
                      interface_address:
                        type: string
                        description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                          for each side of the tunnel. Select the subnet from
                          the following private IP space:
                          10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                          192.168.0.0–192.168.255.255."
                      interface_address6:
                        type: string
                        description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                          with the address being the first IP of the subnet and
                          not same as the address of virtual_subnet6. Eg if
                          virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                          interface_address6 could be
                          2606:54c1:7:0:a9fe:12d2:1:200/127
                      modified_on:
                        type: string
                        description: The date and time the tunnel was last modified.
                      mtu:
                        type: number
                        description: The Maximum Transmission Unit (MTU) in bytes for the interconnect.
                          The minimum value is 576.
                      name:
                        type: string
                        description: The name of the interconnect. The name cannot share a name with
                          other tunnels.
      x-distilled-response-path: result
    put:
      operationId: putCfInterconnect
      description: "Updates a specific interconnect associated with an account. Use
        `?validate_only=true` as an optional query parameter to only run
        validation without persisting changes.  @example ```ts const
        cfInterconnect = await client.magicTransit.cfInterconnects.update(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: cfInterconnectId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the request and
            response bodies will be presented using the new object format.
            Defaults to false."
          schema:
            type: boolean
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                automatic_return_routing:
                  type: boolean
                  description: "Body param: True if automatic stateful return routing should be
                    enabled for a tunnel, false otherwise."
                description:
                  type: string
                  description: "Body param: An optional description of the interconnect."
                gre:
                  type: object
                  properties:
                    cloudflare_endpoint:
                      type: string
                      description: The IP address assigned to the Cloudflare side of the GRE tunnel
                        created as part of the Interconnect.
                  description: "Body param: The configuration specific to GRE interconnects."
                health_check:
                  type: object
                  properties:
                    enabled:
                      type: boolean
                      description: Determines whether to run healthchecks for a tunnel.
                    rate:
                      type: string
                      enum:
                        - low
                        - mid
                        - high
                      description: How frequent the health check is run. The default value is `mid`.
                    target:
                      oneOf:
                        - type: object
                          properties:
                            saved:
                              type: string
                              description: The saved health check target. Setting the value to the empty
                                string indicates that the calculated default
                                value will be used.
                        - type: string
                      description: The destination address in a request type health check. After the
                        healthcheck is decapsulated at the customer end of the
                        tunnel, the ICMP echo will be forwarded to this address.
                        This field defaults to `customer_gre_endpoint address`.
                        This field is ignored for bidirectional healthchecks as
                        the interface_address (not assigned to the Cloudflare
                        side of the tunnel) is used as the target. Must be in
                        object form if the x-magic-new-hc-target header is set
                        to true and string form if x-magic-new-hc-target is
                        absent or set to false.
                    type:
                      type: string
                      enum:
                        - reply
                        - request
                      description: The type of healthcheck to run, reply or request. The default value
                        is `reply`.
                  description: "Body param:"
                interface_address:
                  type: string
                  description: "Body param: A 31-bit prefix (/31 in CIDR notation) supporting two
                    hosts, one for each side of the tunnel. Select the subnet
                    from the following private IP space:
                    10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                    192.168.0.0–192.168.255.255."
                interface_address6:
                  type: string
                  description: "Body param: A 127 bit IPV6 prefix from within the virtual_subnet6
                    prefix space with the address being the first IP of the
                    subnet and not same as the address of virtual_subnet6. Eg if
                    virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                    interface_address6 could be
                    2606:54c1:7:0:a9fe:12d2:1:200/127"
                mtu:
                  type: number
                  description: "Body param: The Maximum Transmission Unit (MTU) in bytes for the
                    interconnect. The minimum value is 576."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  modified:
                    type: boolean
                  modified_interconnect:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      automatic_return_routing:
                        type: boolean
                        description: True if automatic stateful return routing should be enabled for a
                          tunnel, false otherwise.
                      colo_name:
                        type: string
                        description: The name of the interconnect. The name cannot share a name with
                          other tunnels.
                      created_on:
                        type: string
                        description: The date and time the tunnel was created.
                      description:
                        type: string
                        description: An optional description of the interconnect.
                      gre:
                        type: object
                        properties:
                          cloudflare_endpoint:
                            type: string
                            description: The IP address assigned to the Cloudflare side of the GRE tunnel
                              created as part of the Interconnect.
                        description: The configuration specific to GRE interconnects.
                      health_check:
                        type: object
                        properties:
                          enabled:
                            type: boolean
                            description: Determines whether to run healthchecks for a tunnel.
                          rate:
                            type: string
                            enum:
                              - low
                              - mid
                              - high
                            description: How frequent the health check is run. The default value is `mid`.
                          target:
                            oneOf:
                              - type: object
                                properties:
                                  effective:
                                    type: string
                                    description: The effective health check target. If 'saved' is empty, then this
                                      field will be populated with the
                                      calculated default value on GET requests.
                                      Ignored in POST, PUT, and PATCH requests.
                                  saved:
                                    type: string
                                    description: The saved health check target. Setting the value to the empty
                                      string indicates that the calculated
                                      default value will be used.
                              - type: string
                            description: The destination address in a request type health check. After the
                              healthcheck is decapsulated at the customer end of
                              the tunnel, the ICMP echo will be forwarded to
                              this address. This field defaults to
                              `customer_gre_endpoint address`. This field is
                              ignored for bidirectional healthchecks as the
                              interface_address (not assigned to the Cloudflare
                              side of the tunnel) is used as the target. Must be
                              in object form if the x-magic-new-hc-target header
                              is set to true and string form if
                              x-magic-new-hc-target is absent or set to false.
                          type:
                            type: string
                            enum:
                              - reply
                              - request
                            description: The type of healthcheck to run, reply or request. The default value
                              is `reply`.
                      interface_address:
                        type: string
                        description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                          for each side of the tunnel. Select the subnet from
                          the following private IP space:
                          10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                          192.168.0.0–192.168.255.255."
                      interface_address6:
                        type: string
                        description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                          with the address being the first IP of the subnet and
                          not same as the address of virtual_subnet6. Eg if
                          virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                          interface_address6 could be
                          2606:54c1:7:0:a9fe:12d2:1:200/127
                      modified_on:
                        type: string
                        description: The date and time the tunnel was last modified.
                      mtu:
                        type: number
                        description: The Maximum Transmission Unit (MTU) in bytes for the interconnect.
                          The minimum value is 576.
                      name:
                        type: string
                        description: The name of the interconnect. The name cannot share a name with
                          other tunnels.
      x-distilled-response-path: result
  /accounts/{account_id}/magic/cf_interconnects:
    get:
      operationId: listCfInterconnects
      description: "Lists interconnects associated with an account.  @example ```ts
        const cfInterconnects = await client.magicTransit.cfInterconnects.list({
        account_id: '023e105f4ecef8ad9ca31a8372d0c353', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the response
            body will be presented using the new object format. Defaults to
            false."
          schema:
            type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  interconnects:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: Identifier
                        automatic_return_routing:
                          type: boolean
                          description: True if automatic stateful return routing should be enabled for a
                            tunnel, false otherwise.
                        colo_name:
                          type: string
                          description: The name of the interconnect. The name cannot share a name with
                            other tunnels.
                        created_on:
                          type: string
                          description: The date and time the tunnel was created.
                        description:
                          type: string
                          description: An optional description of the interconnect.
                        gre:
                          type: object
                          properties:
                            cloudflare_endpoint:
                              type: string
                              description: The IP address assigned to the Cloudflare side of the GRE tunnel
                                created as part of the Interconnect.
                          description: The configuration specific to GRE interconnects.
                        health_check:
                          type: object
                          properties:
                            enabled:
                              type: boolean
                              description: Determines whether to run healthchecks for a tunnel.
                            rate:
                              type: string
                              enum:
                                - low
                                - mid
                                - high
                              description: How frequent the health check is run. The default value is `mid`.
                            target:
                              oneOf:
                                - type: object
                                  properties:
                                    effective:
                                      type: string
                                      description: The effective health check target. If 'saved' is empty, then this
                                        field will be populated with the
                                        calculated default value on GET
                                        requests. Ignored in POST, PUT, and
                                        PATCH requests.
                                    saved:
                                      type: string
                                      description: The saved health check target. Setting the value to the empty
                                        string indicates that the calculated
                                        default value will be used.
                                - type: string
                              description: The destination address in a request type health check. After the
                                healthcheck is decapsulated at the customer end
                                of the tunnel, the ICMP echo will be forwarded
                                to this address. This field defaults to
                                `customer_gre_endpoint address`. This field is
                                ignored for bidirectional healthchecks as the
                                interface_address (not assigned to the
                                Cloudflare side of the tunnel) is used as the
                                target. Must be in object form if the
                                x-magic-new-hc-target header is set to true and
                                string form if x-magic-new-hc-target is absent
                                or set to false.
                            type:
                              type: string
                              enum:
                                - reply
                                - request
                              description: The type of healthcheck to run, reply or request. The default value
                                is `reply`.
                        interface_address:
                          type: string
                          description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                            for each side of the tunnel. Select the subnet from
                            the following private IP space:
                            10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                            192.168.0.0–192.168.255.255."
                        interface_address6:
                          type: string
                          description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                            with the address being the first IP of the subnet
                            and not same as the address of virtual_subnet6. Eg
                            if virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127
                            , interface_address6 could be
                            2606:54c1:7:0:a9fe:12d2:1:200/127
                        modified_on:
                          type: string
                          description: The date and time the tunnel was last modified.
                        mtu:
                          type: number
                          description: The Maximum Transmission Unit (MTU) in bytes for the interconnect.
                            The minimum value is 576.
                        name:
                          type: string
                          description: The name of the interconnect. The name cannot share a name with
                            other tunnels.
      x-distilled-response-path: result
    put:
      operationId: bulkPutCfInterconnects
      description: "Updates multiple interconnects associated with an account. Use
        `?validate_only=true` as an optional query parameter to only run
        validation without persisting changes.  @example ```ts const response =
        await client.magicTransit.cfInterconnects.bulkUpdate({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', body: {}, }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the request and
            response bodies will be presented using the new object format.
            Defaults to false."
          schema:
            type: boolean
      requestBody:
        required: true
        content:
          application/json:
            schema: {}
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  modified:
                    type: boolean
                  modified_interconnects:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: Identifier
                        automatic_return_routing:
                          type: boolean
                          description: True if automatic stateful return routing should be enabled for a
                            tunnel, false otherwise.
                        colo_name:
                          type: string
                          description: The name of the interconnect. The name cannot share a name with
                            other tunnels.
                        created_on:
                          type: string
                          description: The date and time the tunnel was created.
                        description:
                          type: string
                          description: An optional description of the interconnect.
                        gre:
                          type: object
                          properties:
                            cloudflare_endpoint:
                              type: string
                              description: The IP address assigned to the Cloudflare side of the GRE tunnel
                                created as part of the Interconnect.
                          description: The configuration specific to GRE interconnects.
                        health_check:
                          type: object
                          properties:
                            enabled:
                              type: boolean
                              description: Determines whether to run healthchecks for a tunnel.
                            rate:
                              type: string
                              enum:
                                - low
                                - mid
                                - high
                              description: How frequent the health check is run. The default value is `mid`.
                            target:
                              oneOf:
                                - type: object
                                  properties:
                                    effective:
                                      type: string
                                      description: The effective health check target. If 'saved' is empty, then this
                                        field will be populated with the
                                        calculated default value on GET
                                        requests. Ignored in POST, PUT, and
                                        PATCH requests.
                                    saved:
                                      type: string
                                      description: The saved health check target. Setting the value to the empty
                                        string indicates that the calculated
                                        default value will be used.
                                - type: string
                              description: The destination address in a request type health check. After the
                                healthcheck is decapsulated at the customer end
                                of the tunnel, the ICMP echo will be forwarded
                                to this address. This field defaults to
                                `customer_gre_endpoint address`. This field is
                                ignored for bidirectional healthchecks as the
                                interface_address (not assigned to the
                                Cloudflare side of the tunnel) is used as the
                                target. Must be in object form if the
                                x-magic-new-hc-target header is set to true and
                                string form if x-magic-new-hc-target is absent
                                or set to false.
                            type:
                              type: string
                              enum:
                                - reply
                                - request
                              description: The type of healthcheck to run, reply or request. The default value
                                is `reply`.
                        interface_address:
                          type: string
                          description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                            for each side of the tunnel. Select the subnet from
                            the following private IP space:
                            10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                            192.168.0.0–192.168.255.255."
                        interface_address6:
                          type: string
                          description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                            with the address being the first IP of the subnet
                            and not same as the address of virtual_subnet6. Eg
                            if virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127
                            , interface_address6 could be
                            2606:54c1:7:0:a9fe:12d2:1:200/127
                        modified_on:
                          type: string
                          description: The date and time the tunnel was last modified.
                        mtu:
                          type: number
                          description: The Maximum Transmission Unit (MTU) in bytes for the interconnect.
                            The minimum value is 576.
                        name:
                          type: string
                          description: The name of the interconnect. The name cannot share a name with
                            other tunnels.
      x-distilled-response-path: result
  /accounts/{account_id}/magic/connectors/{connectorId}:
    get:
      operationId: getConnector
      description: "Fetch Connector  @example ```ts const connector = await
        client.magicTransit.connectors.get( 'connector_id', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: connectorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Account identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  activated:
                    type: boolean
                  interrupt_window_duration_hours:
                    type: number
                  interrupt_window_hour_of_day:
                    type: number
                  last_updated:
                    type: string
                  notes:
                    type: string
                  timezone:
                    type: string
                  device:
                    type: object
                    properties:
                      id:
                        type: string
                      serial_number:
                        type: string
                    required:
                      - id
                  last_heartbeat:
                    type: string
                  last_seen_version:
                    type: string
                  license_key:
                    type: string
                required:
                  - id
                  - activated
                  - interrupt_window_duration_hours
                  - interrupt_window_hour_of_day
                  - last_updated
                  - notes
                  - timezone
      x-distilled-response-path: result
    put:
      operationId: updateConnector
      description: "Replace Connector or Re-provision License Key  @example ```ts
        const connector = await client.magicTransit.connectors.update(
        'connector_id', { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, );
        ```"
      parameters:
        - name: connectorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Account identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                activated:
                  type: boolean
                  description: "Body param:"
                interrupt_window_duration_hours:
                  type: number
                  description: "Body param:"
                interrupt_window_hour_of_day:
                  type: number
                  description: "Body param:"
                notes:
                  type: string
                  description: "Body param:"
                provision_license:
                  type: boolean
                  description: "Body param: When true, regenerate license key for the connector."
                timezone:
                  type: string
                  description: "Body param:"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  activated:
                    type: boolean
                  interrupt_window_duration_hours:
                    type: number
                  interrupt_window_hour_of_day:
                    type: number
                  last_updated:
                    type: string
                  notes:
                    type: string
                  timezone:
                    type: string
                  device:
                    type: object
                    properties:
                      id:
                        type: string
                      serial_number:
                        type: string
                    required:
                      - id
                  last_heartbeat:
                    type: string
                  last_seen_version:
                    type: string
                  license_key:
                    type: string
                required:
                  - id
                  - activated
                  - interrupt_window_duration_hours
                  - interrupt_window_hour_of_day
                  - last_updated
                  - notes
                  - timezone
      x-distilled-response-path: result
    patch:
      operationId: patchConnector
      description: "Edit Connector to update specific properties or Re-provision
        License Key  @example ```ts const response = await
        client.magicTransit.connectors.edit( 'connector_id', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: connectorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Account identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                activated:
                  type: boolean
                  description: "Body param:"
                interrupt_window_duration_hours:
                  type: number
                  description: "Body param:"
                interrupt_window_hour_of_day:
                  type: number
                  description: "Body param:"
                notes:
                  type: string
                  description: "Body param:"
                provision_license:
                  type: boolean
                  description: "Body param: When true, regenerate license key for the connector."
                timezone:
                  type: string
                  description: "Body param:"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  activated:
                    type: boolean
                  interrupt_window_duration_hours:
                    type: number
                  interrupt_window_hour_of_day:
                    type: number
                  last_updated:
                    type: string
                  notes:
                    type: string
                  timezone:
                    type: string
                  device:
                    type: object
                    properties:
                      id:
                        type: string
                      serial_number:
                        type: string
                    required:
                      - id
                  last_heartbeat:
                    type: string
                  last_seen_version:
                    type: string
                  license_key:
                    type: string
                required:
                  - id
                  - activated
                  - interrupt_window_duration_hours
                  - interrupt_window_hour_of_day
                  - last_updated
                  - notes
                  - timezone
      x-distilled-response-path: result
    delete:
      operationId: deleteConnector
      description: "Remove a connector from your account  @example ```ts const
        connector = await client.magicTransit.connectors.delete( 'connector_id',
        { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: connectorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Account identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  activated:
                    type: boolean
                  interrupt_window_duration_hours:
                    type: number
                  interrupt_window_hour_of_day:
                    type: number
                  last_updated:
                    type: string
                  notes:
                    type: string
                  timezone:
                    type: string
                  device:
                    type: object
                    properties:
                      id:
                        type: string
                      serial_number:
                        type: string
                    required:
                      - id
                  last_heartbeat:
                    type: string
                  last_seen_version:
                    type: string
                  license_key:
                    type: string
                required:
                  - id
                  - activated
                  - interrupt_window_duration_hours
                  - interrupt_window_hour_of_day
                  - last_updated
                  - notes
                  - timezone
      x-distilled-response-path: result
  /accounts/{account_id}/magic/connectors:
    get:
      operationId: listConnectors
      description: "List Connectors  @example ```ts // Automatically fetches more
        pages as needed. for await (const connectorListResponse of
        client.magicTransit.connectors.list( { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: Account identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  activated:
                    type: boolean
                  interrupt_window_duration_hours:
                    type: number
                  interrupt_window_hour_of_day:
                    type: number
                  last_updated:
                    type: string
                  notes:
                    type: string
                  timezone:
                    type: string
                  device:
                    type: object
                    properties:
                      id:
                        type: string
                      serial_number:
                        type: string
                    required:
                      - id
                  last_heartbeat:
                    type: string
                  last_seen_version:
                    type: string
                  license_key:
                    type: string
                required:
                  - id
                  - activated
                  - interrupt_window_duration_hours
                  - interrupt_window_hour_of_day
                  - last_updated
                  - notes
                  - timezone
      x-distilled-pagination-class: ConnectorListResponsesSinglePage
    post:
      operationId: createConnector
      description: "Add a connector to your account  @example ```ts const connector =
        await client.magicTransit.connectors.create({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', device: {}, }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Account identifier"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                device:
                  type: object
                  properties:
                    id:
                      type: string
                    provision_license:
                      type: boolean
                      description: When true, create and provision a new licence key for the
                        connector.
                    serial_number:
                      type: string
                  description: "Body param: Exactly one of id, serial_number, or provision_license
                    must be provided."
                activated:
                  type: boolean
                  description: "Body param:"
                interrupt_window_duration_hours:
                  type: number
                  description: "Body param:"
                interrupt_window_hour_of_day:
                  type: number
                  description: "Body param:"
                notes:
                  type: string
                  description: "Body param:"
                timezone:
                  type: string
                  description: "Body param:"
              required:
                - device
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  activated:
                    type: boolean
                  interrupt_window_duration_hours:
                    type: number
                  interrupt_window_hour_of_day:
                    type: number
                  last_updated:
                    type: string
                  notes:
                    type: string
                  timezone:
                    type: string
                  device:
                    type: object
                    properties:
                      id:
                        type: string
                      serial_number:
                        type: string
                    required:
                      - id
                  last_heartbeat:
                    type: string
                  last_seen_version:
                    type: string
                  license_key:
                    type: string
                required:
                  - id
                  - activated
                  - interrupt_window_duration_hours
                  - interrupt_window_hour_of_day
                  - last_updated
                  - notes
                  - timezone
      x-distilled-response-path: result
  /accounts/{account_id}/magic/connectors/{connectorId}/telemetry/events/{eventT}.{eventN}:
    get:
      operationId: getConnectorEvent
      description: "Get Event  @example ```ts const event = await
        client.magicTransit.connectors.events.get( 'connector_id', 0, 0, {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: connectorId
          in: path
          required: true
          schema:
            type: string
        - name: eventT
          in: path
          required: true
          schema:
            type: number
        - name: eventN
          in: path
          required: true
          schema:
            type: number
        - name: account_id
          in: path
          required: true
          description: Account identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  e:
                    oneOf:
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - Init
                            description: Initialized process
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - Leave
                            description: Stopped process
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - StartAttestation
                            description: Started attestation
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - FinishAttestationSuccess
                            description: Finished attestation
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - FinishAttestationFailure
                            description: Failed attestation
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - StartRotateCryptKey
                            description: Started crypt key rotation
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - FinishRotateCryptKeySuccess
                            description: Finished crypt key rotation
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - FinishRotateCryptKeyFailure
                            description: Failed crypt key rotation
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - StartRotatePki
                            description: Started PKI rotation
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - FinishRotatePkiSuccess
                            description: Finished PKI rotation
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - FinishRotatePkiFailure
                            description: Failed PKI rotation
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - StartUpgrade
                            description: Started upgrade
                          url:
                            type: string
                            description: Location of upgrade bundle
                        required:
                          - k
                          - url
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - FinishUpgradeSuccess
                            description: Finished upgrade
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - FinishUpgradeFailure
                            description: Failed upgrade
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - Reconcile
                            description: Reconciled
                        required:
                          - k
                      - type: object
                        properties:
                          k:
                            type: string
                            enum:
                              - ConfigureCloudflaredTunnel
                            description: Configured Cloudflared tunnel
                        required:
                          - k
                  n:
                    type: number
                    description: Sequence number, used to order events with the same timestamp
                  t:
                    type: number
                    description: Time the Event was recorded (seconds since the Unix epoch)
                required:
                  - e
                  - n
                  - t
      x-distilled-response-path: result
  /accounts/{account_id}/magic/connectors/{connectorId}/telemetry/events:
    get:
      operationId: listConnectorEvents
      description: "List Events  @example ```ts const events = await
        client.magicTransit.connectors.events.list( 'connector_id', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353', from: 0, to: 0, }, );
        ```"
      parameters:
        - name: connectorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Account identifier"
          schema:
            type: string
        - name: from
          in: query
          required: true
          description: "Query param:"
          schema:
            type: number
        - name: to
          in: query
          required: true
          description: "Query param:"
          schema:
            type: number
        - name: cursor
          in: query
          required: false
          description: "Query param:"
          schema:
            type: string
        - name: k
          in: query
          required: false
          description: "Query param: Filter by event kind"
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: "Query param:"
          schema:
            type: number
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: number
                  items:
                    type: array
                    items:
                      type: object
                      properties:
                        a:
                          type: number
                          description: Time the Event was collected (seconds since the Unix epoch)
                        k:
                          type: string
                          description: Kind
                        n:
                          type: number
                          description: Sequence number, used to order events with the same timestamp
                        t:
                          type: number
                          description: Time the Event was recorded (seconds since the Unix epoch)
                      required:
                        - a
                        - k
                        - n
                        - t
                  cursor:
                    type: string
                required:
                  - count
                  - items
      x-distilled-response-path: result
  /accounts/{account_id}/magic/connectors/{connectorId}/telemetry/events/latest:
    get:
      operationId: listConnectorEventLatests
      description: "Get latest Events  @example ```ts const latests = await
        client.magicTransit.connectors.events.latest.list( 'connector_id', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: connectorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Account identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: number
                  items:
                    type: array
                    items:
                      type: object
                      properties:
                        e:
                          oneOf:
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - Init
                                  description: Initialized process
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - Leave
                                  description: Stopped process
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - StartAttestation
                                  description: Started attestation
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - FinishAttestationSuccess
                                  description: Finished attestation
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - FinishAttestationFailure
                                  description: Failed attestation
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - StartRotateCryptKey
                                  description: Started crypt key rotation
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - FinishRotateCryptKeySuccess
                                  description: Finished crypt key rotation
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - FinishRotateCryptKeyFailure
                                  description: Failed crypt key rotation
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - StartRotatePki
                                  description: Started PKI rotation
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - FinishRotatePkiSuccess
                                  description: Finished PKI rotation
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - FinishRotatePkiFailure
                                  description: Failed PKI rotation
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - StartUpgrade
                                  description: Started upgrade
                                url:
                                  type: string
                                  description: Location of upgrade bundle
                              required:
                                - k
                                - url
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - FinishUpgradeSuccess
                                  description: Finished upgrade
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - FinishUpgradeFailure
                                  description: Failed upgrade
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - Reconcile
                                  description: Reconciled
                              required:
                                - k
                            - type: object
                              properties:
                                k:
                                  type: string
                                  enum:
                                    - ConfigureCloudflaredTunnel
                                  description: Configured Cloudflared tunnel
                              required:
                                - k
                        n:
                          type: number
                          description: Sequence number, used to order events with the same timestamp
                        t:
                          type: number
                          description: Time the Event was recorded (seconds since the Unix epoch)
                      required:
                        - e
                        - n
                        - t
                required:
                  - count
                  - items
      x-distilled-response-path: result
  /accounts/{account_id}/magic/connectors/{connectorId}/telemetry/snapshots/{snapshotT}:
    get:
      operationId: getConnectorSnapshot
      description: "Get Snapshot  @example ```ts const snapshot = await
        client.magicTransit.connectors.snapshots.get( 'connector_id', 0, {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: connectorId
          in: path
          required: true
          schema:
            type: string
        - name: snapshotT
          in: path
          required: true
          schema:
            type: number
        - name: account_id
          in: path
          required: true
          description: Account identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  count_reclaim_failures:
                    type: number
                    description: Count of failures to reclaim space
                  count_reclaimed_paths:
                    type: number
                    description: Count of reclaimed paths
                  count_record_failed:
                    type: number
                    description: Count of failed snapshot recordings
                  count_transmit_failures:
                    type: number
                    description: Count of failed snapshot transmissions
                  t:
                    type: number
                    description: Time the Snapshot was recorded (seconds since the Unix epoch)
                  v:
                    type: string
                    description: Version
                  bonds:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                          description: Name of the network interface
                        status:
                          type: string
                          description: Current status of the network interface
                      required:
                        - name
                        - status
                  cpu_count:
                    type: number
                    description: Count of processors/cores
                  cpu_pressure_10s:
                    type: number
                    description: Percentage of time over a 10 second window that tasks were stalled
                  cpu_pressure_300s:
                    type: number
                    description: Percentage of time over a 5 minute window that tasks were stalled
                  cpu_pressure_60s:
                    type: number
                    description: Percentage of time over a 1 minute window that tasks were stalled
                  cpu_pressure_total_us:
                    type: number
                    description: Total stall time (microseconds)
                  cpu_time_guest_ms:
                    type: number
                    description: Time spent running a virtual CPU or guest OS (milliseconds)
                  cpu_time_guest_nice_ms:
                    type: number
                    description: Time spent running a niced guest (milliseconds)
                  cpu_time_idle_ms:
                    type: number
                    description: Time spent in idle state (milliseconds)
                  cpu_time_iowait_ms:
                    type: number
                    description: Time spent wait for I/O to complete (milliseconds)
                  cpu_time_irq_ms:
                    type: number
                    description: Time spent servicing interrupts (milliseconds)
                  cpu_time_nice_ms:
                    type: number
                    description: Time spent in low-priority user mode (milliseconds)
                  cpu_time_softirq_ms:
                    type: number
                    description: Time spent servicing softirqs (milliseconds)
                  cpu_time_steal_ms:
                    type: number
                    description: Time stolen (milliseconds)
                  cpu_time_system_ms:
                    type: number
                    description: Time spent in system mode (milliseconds)
                  cpu_time_user_ms:
                    type: number
                    description: Time spent in user mode (milliseconds)
                  dhcp_leases:
                    type: array
                    items:
                      type: object
                      properties:
                        client_id:
                          type: string
                          description: Client ID of the device the IP Address was leased to
                        expiry_time:
                          type: number
                          description: Expiry time of the DHCP lease (seconds since the Unix epoch)
                        hostname:
                          type: string
                          description: Hostname of the device the IP Address was leased to
                        interface_name:
                          type: string
                          description: Name of the network interface
                        ip_address:
                          type: string
                          description: IP Address that was leased
                        mac_address:
                          type: string
                          description: MAC Address of the device the IP Address was leased to
                        connector_id:
                          type: string
                          description: Connector identifier
                      required:
                        - client_id
                        - expiry_time
                        - hostname
                        - interface_name
                        - ip_address
                        - mac_address
                  disks:
                    type: array
                    items:
                      type: object
                      properties:
                        in_progress:
                          type: number
                          description: I/Os currently in progress
                        major:
                          type: number
                          description: Device major number
                        merged:
                          type: number
                          description: Reads merged
                        minor:
                          type: number
                          description: Device minor number
                        name:
                          type: string
                          description: Device name
                        reads:
                          type: number
                          description: Reads completed successfully
                        sectors_read:
                          type: number
                          description: Sectors read successfully
                        sectors_written:
                          type: number
                          description: Sectors written successfully
                        time_in_progress_ms:
                          type: number
                          description: Time spent doing I/Os (milliseconds)
                        time_reading_ms:
                          type: number
                          description: Time spent reading (milliseconds)
                        time_writing_ms:
                          type: number
                          description: Time spent writing (milliseconds)
                        weighted_time_in_progress_ms:
                          type: number
                          description: Weighted time spent doing I/Os (milliseconds)
                        writes:
                          type: number
                          description: Writes completed
                        writes_merged:
                          type: number
                          description: Writes merged
                        connector_id:
                          type: string
                          description: Connector identifier
                        discards:
                          type: number
                          description: Discards completed successfully
                        discards_merged:
                          type: number
                          description: Discards merged
                        flushes:
                          type: number
                          description: Flushes completed successfully
                        sectors_discarded:
                          type: number
                          description: Sectors discarded
                        time_discarding_ms:
                          type: number
                          description: Time spent discarding (milliseconds)
                        time_flushing_ms:
                          type: number
                          description: Time spent flushing (milliseconds)
                      required:
                        - in_progress
                        - major
                        - merged
                        - minor
                        - name
                        - reads
                        - sectors_read
                        - sectors_written
                        - time_in_progress_ms
                        - time_reading_ms
                        - time_writing_ms
                        - weighted_time_in_progress_ms
                        - writes
                        - writes_merged
                  ha_state:
                    type: string
                    description: Name of high availability state
                  ha_value:
                    type: number
                    description: Numeric value associated with high availability state (0 =
                      disabled, 1 = active, 2 = standby, 3 = stopped, 4 = fault)
                  interfaces:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                          description: Name of the network interface
                        operstate:
                          type: string
                          description: UP/DOWN state of the network interface
                        connector_id:
                          type: string
                          description: Connector identifier
                        ip_addresses:
                          type: array
                          items:
                            type: object
                            properties:
                              interface_name:
                                type: string
                                description: Name of the network interface
                              ip_address:
                                type: string
                                description: IP address of the network interface
                              connector_id:
                                type: string
                                description: Connector identifier
                            required:
                              - interface_name
                              - ip_address
                        speed:
                          type: number
                          description: Speed of the network interface (bits per second)
                      required:
                        - name
                        - operstate
                  io_pressure_full_10s:
                    type: number
                    description: Percentage of time over a 10 second window that all tasks were
                      stalled
                  io_pressure_full_300s:
                    type: number
                    description: Percentage of time over a 5 minute window that all tasks were
                      stalled
                  io_pressure_full_60s:
                    type: number
                    description: Percentage of time over a 1 minute window that all tasks were
                      stalled
                  io_pressure_full_total_us:
                    type: number
                    description: Total stall time (microseconds)
                  io_pressure_some_10s:
                    type: number
                    description: Percentage of time over a 10 second window that some tasks were
                      stalled
                  io_pressure_some_300s:
                    type: number
                    description: Percentage of time over a 3 minute window that some tasks were
                      stalled
                  io_pressure_some_60s:
                    type: number
                    description: Percentage of time over a 1 minute window that some tasks were
                      stalled
                  io_pressure_some_total_us:
                    type: number
                    description: Total stall time (microseconds)
                  kernel_btime:
                    type: number
                    description: Boot time (seconds since Unix epoch)
                  kernel_ctxt:
                    type: number
                    description: Number of context switches that the system underwent
                  kernel_processes:
                    type: number
                    description: Number of forks since boot
                  kernel_processes_blocked:
                    type: number
                    description: Number of processes blocked waiting for I/O
                  kernel_processes_running:
                    type: number
                    description: Number of processes in runnable state
                  load_average_15m:
                    type: number
                    description: The fifteen-minute load average
                  load_average_1m:
                    type: number
                    description: The one-minute load average
                  load_average_5m:
                    type: number
                    description: The five-minute load average
                  load_average_cur:
                    type: number
                    description: Number of currently runnable kernel scheduling entities
                  load_average_max:
                    type: number
                    description: Number of kernel scheduling entities that currently exist on the
                      system
                  memory_active_bytes:
                    type: number
                    description: Memory that has been used more recently
                  memory_anon_hugepages_bytes:
                    type: number
                    description: Non-file backed huge pages mapped into user-space page tables
                  memory_anon_pages_bytes:
                    type: number
                    description: Non-file backed pages mapped into user-space page tables
                  memory_available_bytes:
                    type: number
                    description: Estimate of how much memory is available for starting new
                      applications
                  memory_bounce_bytes:
                    type: number
                    description: Memory used for block device bounce buffers
                  memory_buffers_bytes:
                    type: number
                    description: Relatively temporary storage for raw disk blocks
                  memory_cached_bytes:
                    type: number
                    description: In-memory cache for files read from the disk
                  memory_cma_free_bytes:
                    type: number
                    description: Free CMA (Contiguous Memory Allocator) pages
                  memory_cma_total_bytes:
                    type: number
                    description: Total CMA (Contiguous Memory Allocator) pages
                  memory_commit_limit_bytes:
                    type: number
                    description: Total amount of memory currently available to be allocated on the
                      system
                  memory_committed_as_bytes:
                    type: number
                    description: Amount of memory presently allocated on the system
                  memory_dirty_bytes:
                    type: number
                    description: Memory which is waiting to get written back to the disk
                  memory_free_bytes:
                    type: number
                    description: The sum of LowFree and HighFree
                  memory_high_free_bytes:
                    type: number
                    description: Amount of free highmem
                  memory_high_total_bytes:
                    type: number
                    description: Total amount of highmem
                  memory_hugepages_free:
                    type: number
                    description: The number of huge pages in the pool that are not yet allocated
                  memory_hugepages_rsvd:
                    type: number
                    description: Number of huge pages for which a commitment has been made, but no
                      allocation has yet been made
                  memory_hugepages_surp:
                    type: number
                    description: Number of huge pages in the pool above the threshold
                  memory_hugepages_total:
                    type: number
                    description: The size of the pool of huge pages
                  memory_hugepagesize_bytes:
                    type: number
                    description: The size of huge pages
                  memory_inactive_bytes:
                    type: number
                    description: Memory which has been less recently used
                  memory_k_reclaimable_bytes:
                    type: number
                    description: Kernel allocations that the kernel will attempt to reclaim under
                      memory pressure
                  memory_kernel_stack_bytes:
                    type: number
                    description: Amount of memory allocated to kernel stacks
                  memory_low_free_bytes:
                    type: number
                    description: Amount of free lowmem
                  memory_low_total_bytes:
                    type: number
                    description: Total amount of lowmem
                  memory_mapped_bytes:
                    type: number
                    description: Files which have been mapped into memory
                  memory_page_tables_bytes:
                    type: number
                    description: Amount of memory dedicated to the lowest level of page tables
                  memory_per_cpu_bytes:
                    type: number
                    description: Memory allocated to the per-cpu alloctor used to back per-cpu
                      allocations
                  memory_pressure_full_10s:
                    type: number
                    description: Percentage of time over a 10 second window that all tasks were
                      stalled
                  memory_pressure_full_300s:
                    type: number
                    description: Percentage of time over a 5 minute window that all tasks were
                      stalled
                  memory_pressure_full_60s:
                    type: number
                    description: Percentage of time over a 1 minute window that all tasks were
                      stalled
                  memory_pressure_full_total_us:
                    type: number
                    description: Total stall time (microseconds)
                  memory_pressure_some_10s:
                    type: number
                    description: Percentage of time over a 10 second window that some tasks were
                      stalled
                  memory_pressure_some_300s:
                    type: number
                    description: Percentage of time over a 5 minute window that some tasks were
                      stalled
                  memory_pressure_some_60s:
                    type: number
                    description: Percentage of time over a 1 minute window that some tasks were
                      stalled
                  memory_pressure_some_total_us:
                    type: number
                    description: Total stall time (microseconds)
                  memory_s_reclaimable_bytes:
                    type: number
                    description: Part of slab that can be reclaimed on memory pressure
                  memory_s_unreclaim_bytes:
                    type: number
                    description: Part of slab that cannot be reclaimed on memory pressure
                  memory_secondary_page_tables_bytes:
                    type: number
                    description: Amount of memory dedicated to the lowest level of page tables
                  memory_shmem_bytes:
                    type: number
                    description: Amount of memory consumed by tmpfs
                  memory_shmem_hugepages_bytes:
                    type: number
                    description: Memory used by shmem and tmpfs, allocated with huge pages
                  memory_shmem_pmd_mapped_bytes:
                    type: number
                    description: Shared memory mapped into user space with huge pages
                  memory_slab_bytes:
                    type: number
                    description: In-kernel data structures cache
                  memory_swap_cached_bytes:
                    type: number
                    description: Memory swapped out and back in while still in swap file
                  memory_swap_free_bytes:
                    type: number
                    description: Amount of swap space that is currently unused
                  memory_swap_total_bytes:
                    type: number
                    description: Total amount of swap space available
                  memory_total_bytes:
                    type: number
                    description: Total usable RAM
                  memory_vmalloc_chunk_bytes:
                    type: number
                    description: Largest contiguous block of vmalloc area which is free
                  memory_vmalloc_total_bytes:
                    type: number
                    description: Total size of vmalloc memory area
                  memory_vmalloc_used_bytes:
                    type: number
                    description: Amount of vmalloc area which is used
                  memory_writeback_bytes:
                    type: number
                    description: Memory which is actively being written back to the disk
                  memory_writeback_tmp_bytes:
                    type: number
                    description: Memory used by FUSE for temporary writeback buffers
                  memory_z_swap_bytes:
                    type: number
                    description: Memory consumed by the zswap backend, compressed
                  memory_z_swapped_bytes:
                    type: number
                    description: Amount of anonymous memory stored in zswap, uncompressed
                  mounts:
                    type: array
                    items:
                      type: object
                      properties:
                        file_system:
                          type: string
                          description: File system on disk (EXT4, NTFS, etc.)
                        kind:
                          type: string
                          description: Kind of disk (HDD, SSD, etc.)
                        mount_point:
                          type: string
                          description: Path where disk is mounted
                        name:
                          type: string
                          description: Name of the disk mount
                        available_bytes:
                          type: number
                          description: Available disk size (bytes)
                        connector_id:
                          type: string
                          description: Connector identifier
                        is_read_only:
                          type: boolean
                          description: Determines whether the disk is read-only
                        is_removable:
                          type: boolean
                          description: Determines whether the disk is removable
                        total_bytes:
                          type: number
                          description: Total disk size (bytes)
                      required:
                        - file_system
                        - kind
                        - mount_point
                        - name
                  netdevs:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                          description: Name of the network device
                        recv_bytes:
                          type: number
                          description: Total bytes received
                        recv_compressed:
                          type: number
                          description: Compressed packets received
                        recv_drop:
                          type: number
                          description: Packets dropped
                        recv_errs:
                          type: number
                          description: Bad packets received
                        recv_fifo:
                          type: number
                          description: FIFO overruns
                        recv_frame:
                          type: number
                          description: Frame alignment errors
                        recv_multicast:
                          type: number
                          description: Multicast packets received
                        recv_packets:
                          type: number
                          description: Total packets received
                        sent_bytes:
                          type: number
                          description: Total bytes transmitted
                        sent_carrier:
                          type: number
                          description: Number of packets not sent due to carrier errors
                        sent_colls:
                          type: number
                          description: Number of collisions
                        sent_compressed:
                          type: number
                          description: Number of compressed packets transmitted
                        sent_drop:
                          type: number
                          description: Number of packets dropped during transmission
                        sent_errs:
                          type: number
                          description: Number of transmission errors
                        sent_fifo:
                          type: number
                          description: FIFO overruns
                        sent_packets:
                          type: number
                          description: Total packets transmitted
                        connector_id:
                          type: string
                          description: Connector identifier
                      required:
                        - name
                        - recv_bytes
                        - recv_compressed
                        - recv_drop
                        - recv_errs
                        - recv_fifo
                        - recv_frame
                        - recv_multicast
                        - recv_packets
                        - sent_bytes
                        - sent_carrier
                        - sent_colls
                        - sent_compressed
                        - sent_drop
                        - sent_errs
                        - sent_fifo
                        - sent_packets
                  snmp_icmp_in_addr_mask_reps:
                    type: number
                    description: Number of ICMP Address Mask Reply messages received
                  snmp_icmp_in_addr_masks:
                    type: number
                    description: Number of ICMP Address Mask Request messages received
                  snmp_icmp_in_csum_errors:
                    type: number
                    description: Number of ICMP messages received with bad checksums
                  snmp_icmp_in_dest_unreachs:
                    type: number
                    description: Number of ICMP Destination Unreachable messages received
                  snmp_icmp_in_echo_reps:
                    type: number
                    description: Number of ICMP Echo Reply messages received
                  snmp_icmp_in_echos:
                    type: number
                    description: Number of ICMP Echo (request) messages received
                  snmp_icmp_in_errors:
                    type: number
                    description: Number of ICMP messages received with ICMP-specific errors
                  snmp_icmp_in_msgs:
                    type: number
                    description: Number of ICMP messages received
                  snmp_icmp_in_parm_probs:
                    type: number
                    description: Number of ICMP Parameter Problem messages received
                  snmp_icmp_in_redirects:
                    type: number
                    description: Number of ICMP Redirect messages received
                  snmp_icmp_in_src_quenchs:
                    type: number
                    description: Number of ICMP Source Quench messages received
                  snmp_icmp_in_time_excds:
                    type: number
                    description: Number of ICMP Time Exceeded messages received
                  snmp_icmp_in_timestamp_reps:
                    type: number
                    description: Number of ICMP Address Mask Request messages received
                  snmp_icmp_in_timestamps:
                    type: number
                    description: Number of ICMP Timestamp (request) messages received
                  snmp_icmp_out_addr_mask_reps:
                    type: number
                    description: Number of ICMP Address Mask Reply messages sent
                  snmp_icmp_out_addr_masks:
                    type: number
                    description: Number of ICMP Address Mask Request messages sent
                  snmp_icmp_out_dest_unreachs:
                    type: number
                    description: Number of ICMP Destination Unreachable messages sent
                  snmp_icmp_out_echo_reps:
                    type: number
                    description: Number of ICMP Echo Reply messages sent
                  snmp_icmp_out_echos:
                    type: number
                    description: Number of ICMP Echo (request) messages sent
                  snmp_icmp_out_errors:
                    type: number
                    description: Number of ICMP messages which this entity did not send due to
                      ICMP-specific errors
                  snmp_icmp_out_msgs:
                    type: number
                    description: Number of ICMP messages attempted to send
                  snmp_icmp_out_parm_probs:
                    type: number
                    description: Number of ICMP Parameter Problem messages sent
                  snmp_icmp_out_redirects:
                    type: number
                    description: Number of ICMP Redirect messages sent
                  snmp_icmp_out_src_quenchs:
                    type: number
                    description: Number of ICMP Source Quench messages sent
                  snmp_icmp_out_time_excds:
                    type: number
                    description: Number of ICMP Time Exceeded messages sent
                  snmp_icmp_out_timestamp_reps:
                    type: number
                    description: Number of ICMP Timestamp Reply messages sent
                  snmp_icmp_out_timestamps:
                    type: number
                    description: Number of ICMP Timestamp (request) messages sent
                  snmp_ip_default_ttl:
                    type: number
                    description: Default value of the Time-To-Live field of the IP header
                  snmp_ip_forw_datagrams:
                    type: number
                    description: Number of datagrams forwarded to their final destination
                  snmp_ip_forwarding_enabled:
                    type: boolean
                    description: Set when acting as an IP gateway
                  snmp_ip_frag_creates:
                    type: number
                    description: Number of datagrams generated by fragmentation
                  snmp_ip_frag_fails:
                    type: number
                    description: Number of datagrams discarded because fragmentation failed
                  snmp_ip_frag_oks:
                    type: number
                    description: Number of datagrams successfully fragmented
                  snmp_ip_in_addr_errors:
                    type: number
                    description: Number of input datagrams discarded due to errors in the IP address
                  snmp_ip_in_delivers:
                    type: number
                    description: Number of input datagrams successfully delivered to IP
                      user-protocols
                  snmp_ip_in_discards:
                    type: number
                    description: Number of input datagrams otherwise discarded
                  snmp_ip_in_hdr_errors:
                    type: number
                    description: Number of input datagrams discarded due to errors in the IP header
                  snmp_ip_in_receives:
                    type: number
                    description: Number of input datagrams received from interfaces
                  snmp_ip_in_unknown_protos:
                    type: number
                    description: Number of input datagrams discarded due unknown or unsupported
                      protocol
                  snmp_ip_out_discards:
                    type: number
                    description: Number of output datagrams otherwise discarded
                  snmp_ip_out_no_routes:
                    type: number
                    description: Number of output datagrams discarded because no route matched
                  snmp_ip_out_requests:
                    type: number
                    description: Number of datagrams supplied for transmission
                  snmp_ip_reasm_fails:
                    type: number
                    description: Number of failures detected by the reassembly algorithm
                  snmp_ip_reasm_oks:
                    type: number
                    description: Number of datagrams successfully reassembled
                  snmp_ip_reasm_reqds:
                    type: number
                    description: Number of fragments received which needed to be reassembled
                  snmp_ip_reasm_timeout:
                    type: number
                    description: Number of seconds fragments are held while awaiting reassembly
                  snmp_tcp_active_opens:
                    type: number
                    description: Number of times TCP transitions to SYN-SENT from CLOSED
                  snmp_tcp_attempt_fails:
                    type: number
                    description: Number of times TCP transitions to CLOSED from SYN-SENT or
                      SYN-RCVD, plus transitions to LISTEN from SYN-RCVD
                  snmp_tcp_curr_estab:
                    type: number
                    description: Number of TCP connections in ESTABLISHED or CLOSE-WAIT
                  snmp_tcp_estab_resets:
                    type: number
                    description: Number of times TCP transitions to CLOSED from ESTABLISHED or
                      CLOSE-WAIT
                  snmp_tcp_in_csum_errors:
                    type: number
                    description: Number of TCP segments received with checksum errors
                  snmp_tcp_in_errs:
                    type: number
                    description: Number of TCP segments received in error
                  snmp_tcp_in_segs:
                    type: number
                    description: Number of TCP segments received
                  snmp_tcp_max_conn:
                    type: number
                    description: Limit on the total number of TCP connections
                  snmp_tcp_out_rsts:
                    type: number
                    description: Number of TCP segments sent with RST flag
                  snmp_tcp_out_segs:
                    type: number
                    description: Number of TCP segments sent
                  snmp_tcp_passive_opens:
                    type: number
                    description: Number of times TCP transitions to SYN-RCVD from LISTEN
                  snmp_tcp_retrans_segs:
                    type: number
                    description: Number of TCP segments retransmitted
                  snmp_tcp_rto_max:
                    type: number
                    description: Maximum value permitted by a TCP implementation for the
                      retransmission timeout (milliseconds)
                  snmp_tcp_rto_min:
                    type: number
                    description: Minimum value permitted by a TCP implementation for the
                      retransmission timeout (milliseconds)
                  snmp_udp_in_datagrams:
                    type: number
                    description: Number of UDP datagrams delivered to UDP applications
                  snmp_udp_in_errors:
                    type: number
                    description: Number of UDP datagrams failed to be delivered for reasons other
                      than lack of application at the destination port
                  snmp_udp_no_ports:
                    type: number
                    description: Number of UDP datagrams received for which there was not
                      application at the destination port
                  snmp_udp_out_datagrams:
                    type: number
                    description: Number of UDP datagrams sent
                  system_boot_time_s:
                    type: number
                    description: Boottime of the system (seconds since the Unix epoch)
                  thermals:
                    type: array
                    items:
                      type: object
                      properties:
                        label:
                          type: string
                          description: Sensor identifier for the component
                        connector_id:
                          type: string
                          description: Connector identifier
                        critical_celcius:
                          type: number
                          description: Critical failure temperature of the component (degrees Celsius)
                        current_celcius:
                          type: number
                          description: Current temperature of the component (degrees Celsius)
                        max_celcius:
                          type: number
                          description: Maximum temperature of the component (degrees Celsius)
                      required:
                        - label
                  tunnels:
                    type: array
                    items:
                      type: object
                      properties:
                        health_state:
                          type: string
                          description: Name of tunnel health state (unknown, healthy, degraded, down)
                        health_value:
                          type: number
                          description: Numeric value associated with tunnel state (0 = unknown, 1 =
                            healthy, 2 = degraded, 3 = down)
                        interface_name:
                          type: string
                          description: The tunnel interface name (i.e. xfrm1, xfrm3.99, etc.)
                        tunnel_id:
                          type: string
                          description: Tunnel identifier
                        connector_id:
                          type: string
                          description: Connector identifier
                        probed_mtu:
                          type: number
                          description: MTU as measured between the two ends of the tunnel
                      required:
                        - health_state
                        - health_value
                        - interface_name
                        - tunnel_id
                  uptime_idle_ms:
                    type: number
                    description: Sum of how much time each core has spent idle
                  uptime_total_ms:
                    type: number
                    description: Uptime of the system, including time spent in suspend
                required:
                  - count_reclaim_failures
                  - count_reclaimed_paths
                  - count_record_failed
                  - count_transmit_failures
                  - t
                  - v
      x-distilled-response-path: result
  /accounts/{account_id}/magic/connectors/{connectorId}/telemetry/snapshots:
    get:
      operationId: listConnectorSnapshots
      description: "List Snapshots  @example ```ts const snapshots = await
        client.magicTransit.connectors.snapshots.list( 'connector_id', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353', from: 0, to: 0, }, );
        ```"
      parameters:
        - name: connectorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Account identifier"
          schema:
            type: string
        - name: from
          in: query
          required: true
          description: "Query param:"
          schema:
            type: number
        - name: to
          in: query
          required: true
          description: "Query param:"
          schema:
            type: number
        - name: cursor
          in: query
          required: false
          description: "Query param:"
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: "Query param:"
          schema:
            type: number
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: number
                  items:
                    type: array
                    items:
                      type: object
                      properties:
                        a:
                          type: number
                          description: Time the Snapshot was collected (seconds since the Unix epoch)
                        t:
                          type: number
                          description: Time the Snapshot was recorded (seconds since the Unix epoch)
                      required:
                        - a
                        - t
                  cursor:
                    type: string
                required:
                  - count
                  - items
      x-distilled-response-path: result
  /accounts/{account_id}/magic/connectors/{connectorId}/telemetry/snapshots/latest:
    get:
      operationId: listConnectorSnapshotLatests
      description: "Get latest Snapshots  @example ```ts const latests = await
        client.magicTransit.connectors.snapshots.latest.list( 'connector_id', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: connectorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Account identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: number
                  items:
                    type: array
                    items:
                      type: object
                      properties:
                        count_reclaim_failures:
                          type: number
                          description: Count of failures to reclaim space
                        count_reclaimed_paths:
                          type: number
                          description: Count of reclaimed paths
                        count_record_failed:
                          type: number
                          description: Count of failed snapshot recordings
                        count_transmit_failures:
                          type: number
                          description: Count of failed snapshot transmissions
                        t:
                          type: number
                          description: Time the Snapshot was recorded (seconds since the Unix epoch)
                        v:
                          type: string
                          description: Version
                        bonds:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: Name of the network interface
                              status:
                                type: string
                                description: Current status of the network interface
                            required:
                              - name
                              - status
                        cpu_count:
                          type: number
                          description: Count of processors/cores
                        cpu_pressure_10s:
                          type: number
                          description: Percentage of time over a 10 second window that tasks were stalled
                        cpu_pressure_300s:
                          type: number
                          description: Percentage of time over a 5 minute window that tasks were stalled
                        cpu_pressure_60s:
                          type: number
                          description: Percentage of time over a 1 minute window that tasks were stalled
                        cpu_pressure_total_us:
                          type: number
                          description: Total stall time (microseconds)
                        cpu_time_guest_ms:
                          type: number
                          description: Time spent running a virtual CPU or guest OS (milliseconds)
                        cpu_time_guest_nice_ms:
                          type: number
                          description: Time spent running a niced guest (milliseconds)
                        cpu_time_idle_ms:
                          type: number
                          description: Time spent in idle state (milliseconds)
                        cpu_time_iowait_ms:
                          type: number
                          description: Time spent wait for I/O to complete (milliseconds)
                        cpu_time_irq_ms:
                          type: number
                          description: Time spent servicing interrupts (milliseconds)
                        cpu_time_nice_ms:
                          type: number
                          description: Time spent in low-priority user mode (milliseconds)
                        cpu_time_softirq_ms:
                          type: number
                          description: Time spent servicing softirqs (milliseconds)
                        cpu_time_steal_ms:
                          type: number
                          description: Time stolen (milliseconds)
                        cpu_time_system_ms:
                          type: number
                          description: Time spent in system mode (milliseconds)
                        cpu_time_user_ms:
                          type: number
                          description: Time spent in user mode (milliseconds)
                        dhcp_leases:
                          type: array
                          items:
                            type: object
                            properties:
                              client_id:
                                type: string
                                description: Client ID of the device the IP Address was leased to
                              expiry_time:
                                type: number
                                description: Expiry time of the DHCP lease (seconds since the Unix epoch)
                              hostname:
                                type: string
                                description: Hostname of the device the IP Address was leased to
                              interface_name:
                                type: string
                                description: Name of the network interface
                              ip_address:
                                type: string
                                description: IP Address that was leased
                              mac_address:
                                type: string
                                description: MAC Address of the device the IP Address was leased to
                              connector_id:
                                type: string
                                description: Connector identifier
                            required:
                              - client_id
                              - expiry_time
                              - hostname
                              - interface_name
                              - ip_address
                              - mac_address
                        disks:
                          type: array
                          items:
                            type: object
                            properties:
                              in_progress:
                                type: number
                                description: I/Os currently in progress
                              major:
                                type: number
                                description: Device major number
                              merged:
                                type: number
                                description: Reads merged
                              minor:
                                type: number
                                description: Device minor number
                              name:
                                type: string
                                description: Device name
                              reads:
                                type: number
                                description: Reads completed successfully
                              sectors_read:
                                type: number
                                description: Sectors read successfully
                              sectors_written:
                                type: number
                                description: Sectors written successfully
                              time_in_progress_ms:
                                type: number
                                description: Time spent doing I/Os (milliseconds)
                              time_reading_ms:
                                type: number
                                description: Time spent reading (milliseconds)
                              time_writing_ms:
                                type: number
                                description: Time spent writing (milliseconds)
                              weighted_time_in_progress_ms:
                                type: number
                                description: Weighted time spent doing I/Os (milliseconds)
                              writes:
                                type: number
                                description: Writes completed
                              writes_merged:
                                type: number
                                description: Writes merged
                              connector_id:
                                type: string
                                description: Connector identifier
                              discards:
                                type: number
                                description: Discards completed successfully
                              discards_merged:
                                type: number
                                description: Discards merged
                              flushes:
                                type: number
                                description: Flushes completed successfully
                              sectors_discarded:
                                type: number
                                description: Sectors discarded
                              time_discarding_ms:
                                type: number
                                description: Time spent discarding (milliseconds)
                              time_flushing_ms:
                                type: number
                                description: Time spent flushing (milliseconds)
                            required:
                              - in_progress
                              - major
                              - merged
                              - minor
                              - name
                              - reads
                              - sectors_read
                              - sectors_written
                              - time_in_progress_ms
                              - time_reading_ms
                              - time_writing_ms
                              - weighted_time_in_progress_ms
                              - writes
                              - writes_merged
                        ha_state:
                          type: string
                          description: Name of high availability state
                        ha_value:
                          type: number
                          description: Numeric value associated with high availability state (0 =
                            disabled, 1 = active, 2 = standby, 3 = stopped, 4 =
                            fault)
                        interfaces:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: Name of the network interface
                              operstate:
                                type: string
                                description: UP/DOWN state of the network interface
                              connector_id:
                                type: string
                                description: Connector identifier
                              ip_addresses:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    interface_name:
                                      type: string
                                      description: Name of the network interface
                                    ip_address:
                                      type: string
                                      description: IP address of the network interface
                                    connector_id:
                                      type: string
                                      description: Connector identifier
                                  required:
                                    - interface_name
                                    - ip_address
                              speed:
                                type: number
                                description: Speed of the network interface (bits per second)
                            required:
                              - name
                              - operstate
                        io_pressure_full_10s:
                          type: number
                          description: Percentage of time over a 10 second window that all tasks were
                            stalled
                        io_pressure_full_300s:
                          type: number
                          description: Percentage of time over a 5 minute window that all tasks were
                            stalled
                        io_pressure_full_60s:
                          type: number
                          description: Percentage of time over a 1 minute window that all tasks were
                            stalled
                        io_pressure_full_total_us:
                          type: number
                          description: Total stall time (microseconds)
                        io_pressure_some_10s:
                          type: number
                          description: Percentage of time over a 10 second window that some tasks were
                            stalled
                        io_pressure_some_300s:
                          type: number
                          description: Percentage of time over a 3 minute window that some tasks were
                            stalled
                        io_pressure_some_60s:
                          type: number
                          description: Percentage of time over a 1 minute window that some tasks were
                            stalled
                        io_pressure_some_total_us:
                          type: number
                          description: Total stall time (microseconds)
                        kernel_btime:
                          type: number
                          description: Boot time (seconds since Unix epoch)
                        kernel_ctxt:
                          type: number
                          description: Number of context switches that the system underwent
                        kernel_processes:
                          type: number
                          description: Number of forks since boot
                        kernel_processes_blocked:
                          type: number
                          description: Number of processes blocked waiting for I/O
                        kernel_processes_running:
                          type: number
                          description: Number of processes in runnable state
                        load_average_15m:
                          type: number
                          description: The fifteen-minute load average
                        load_average_1m:
                          type: number
                          description: The one-minute load average
                        load_average_5m:
                          type: number
                          description: The five-minute load average
                        load_average_cur:
                          type: number
                          description: Number of currently runnable kernel scheduling entities
                        load_average_max:
                          type: number
                          description: Number of kernel scheduling entities that currently exist on the
                            system
                        memory_active_bytes:
                          type: number
                          description: Memory that has been used more recently
                        memory_anon_hugepages_bytes:
                          type: number
                          description: Non-file backed huge pages mapped into user-space page tables
                        memory_anon_pages_bytes:
                          type: number
                          description: Non-file backed pages mapped into user-space page tables
                        memory_available_bytes:
                          type: number
                          description: Estimate of how much memory is available for starting new
                            applications
                        memory_bounce_bytes:
                          type: number
                          description: Memory used for block device bounce buffers
                        memory_buffers_bytes:
                          type: number
                          description: Relatively temporary storage for raw disk blocks
                        memory_cached_bytes:
                          type: number
                          description: In-memory cache for files read from the disk
                        memory_cma_free_bytes:
                          type: number
                          description: Free CMA (Contiguous Memory Allocator) pages
                        memory_cma_total_bytes:
                          type: number
                          description: Total CMA (Contiguous Memory Allocator) pages
                        memory_commit_limit_bytes:
                          type: number
                          description: Total amount of memory currently available to be allocated on the
                            system
                        memory_committed_as_bytes:
                          type: number
                          description: Amount of memory presently allocated on the system
                        memory_dirty_bytes:
                          type: number
                          description: Memory which is waiting to get written back to the disk
                        memory_free_bytes:
                          type: number
                          description: The sum of LowFree and HighFree
                        memory_high_free_bytes:
                          type: number
                          description: Amount of free highmem
                        memory_high_total_bytes:
                          type: number
                          description: Total amount of highmem
                        memory_hugepages_free:
                          type: number
                          description: The number of huge pages in the pool that are not yet allocated
                        memory_hugepages_rsvd:
                          type: number
                          description: Number of huge pages for which a commitment has been made, but no
                            allocation has yet been made
                        memory_hugepages_surp:
                          type: number
                          description: Number of huge pages in the pool above the threshold
                        memory_hugepages_total:
                          type: number
                          description: The size of the pool of huge pages
                        memory_hugepagesize_bytes:
                          type: number
                          description: The size of huge pages
                        memory_inactive_bytes:
                          type: number
                          description: Memory which has been less recently used
                        memory_k_reclaimable_bytes:
                          type: number
                          description: Kernel allocations that the kernel will attempt to reclaim under
                            memory pressure
                        memory_kernel_stack_bytes:
                          type: number
                          description: Amount of memory allocated to kernel stacks
                        memory_low_free_bytes:
                          type: number
                          description: Amount of free lowmem
                        memory_low_total_bytes:
                          type: number
                          description: Total amount of lowmem
                        memory_mapped_bytes:
                          type: number
                          description: Files which have been mapped into memory
                        memory_page_tables_bytes:
                          type: number
                          description: Amount of memory dedicated to the lowest level of page tables
                        memory_per_cpu_bytes:
                          type: number
                          description: Memory allocated to the per-cpu alloctor used to back per-cpu
                            allocations
                        memory_pressure_full_10s:
                          type: number
                          description: Percentage of time over a 10 second window that all tasks were
                            stalled
                        memory_pressure_full_300s:
                          type: number
                          description: Percentage of time over a 5 minute window that all tasks were
                            stalled
                        memory_pressure_full_60s:
                          type: number
                          description: Percentage of time over a 1 minute window that all tasks were
                            stalled
                        memory_pressure_full_total_us:
                          type: number
                          description: Total stall time (microseconds)
                        memory_pressure_some_10s:
                          type: number
                          description: Percentage of time over a 10 second window that some tasks were
                            stalled
                        memory_pressure_some_300s:
                          type: number
                          description: Percentage of time over a 5 minute window that some tasks were
                            stalled
                        memory_pressure_some_60s:
                          type: number
                          description: Percentage of time over a 1 minute window that some tasks were
                            stalled
                        memory_pressure_some_total_us:
                          type: number
                          description: Total stall time (microseconds)
                        memory_s_reclaimable_bytes:
                          type: number
                          description: Part of slab that can be reclaimed on memory pressure
                        memory_s_unreclaim_bytes:
                          type: number
                          description: Part of slab that cannot be reclaimed on memory pressure
                        memory_secondary_page_tables_bytes:
                          type: number
                          description: Amount of memory dedicated to the lowest level of page tables
                        memory_shmem_bytes:
                          type: number
                          description: Amount of memory consumed by tmpfs
                        memory_shmem_hugepages_bytes:
                          type: number
                          description: Memory used by shmem and tmpfs, allocated with huge pages
                        memory_shmem_pmd_mapped_bytes:
                          type: number
                          description: Shared memory mapped into user space with huge pages
                        memory_slab_bytes:
                          type: number
                          description: In-kernel data structures cache
                        memory_swap_cached_bytes:
                          type: number
                          description: Memory swapped out and back in while still in swap file
                        memory_swap_free_bytes:
                          type: number
                          description: Amount of swap space that is currently unused
                        memory_swap_total_bytes:
                          type: number
                          description: Total amount of swap space available
                        memory_total_bytes:
                          type: number
                          description: Total usable RAM
                        memory_vmalloc_chunk_bytes:
                          type: number
                          description: Largest contiguous block of vmalloc area which is free
                        memory_vmalloc_total_bytes:
                          type: number
                          description: Total size of vmalloc memory area
                        memory_vmalloc_used_bytes:
                          type: number
                          description: Amount of vmalloc area which is used
                        memory_writeback_bytes:
                          type: number
                          description: Memory which is actively being written back to the disk
                        memory_writeback_tmp_bytes:
                          type: number
                          description: Memory used by FUSE for temporary writeback buffers
                        memory_z_swap_bytes:
                          type: number
                          description: Memory consumed by the zswap backend, compressed
                        memory_z_swapped_bytes:
                          type: number
                          description: Amount of anonymous memory stored in zswap, uncompressed
                        mounts:
                          type: array
                          items:
                            type: object
                            properties:
                              file_system:
                                type: string
                                description: File system on disk (EXT4, NTFS, etc.)
                              kind:
                                type: string
                                description: Kind of disk (HDD, SSD, etc.)
                              mount_point:
                                type: string
                                description: Path where disk is mounted
                              name:
                                type: string
                                description: Name of the disk mount
                              available_bytes:
                                type: number
                                description: Available disk size (bytes)
                              connector_id:
                                type: string
                                description: Connector identifier
                              is_read_only:
                                type: boolean
                                description: Determines whether the disk is read-only
                              is_removable:
                                type: boolean
                                description: Determines whether the disk is removable
                              total_bytes:
                                type: number
                                description: Total disk size (bytes)
                            required:
                              - file_system
                              - kind
                              - mount_point
                              - name
                        netdevs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: Name of the network device
                              recv_bytes:
                                type: number
                                description: Total bytes received
                              recv_compressed:
                                type: number
                                description: Compressed packets received
                              recv_drop:
                                type: number
                                description: Packets dropped
                              recv_errs:
                                type: number
                                description: Bad packets received
                              recv_fifo:
                                type: number
                                description: FIFO overruns
                              recv_frame:
                                type: number
                                description: Frame alignment errors
                              recv_multicast:
                                type: number
                                description: Multicast packets received
                              recv_packets:
                                type: number
                                description: Total packets received
                              sent_bytes:
                                type: number
                                description: Total bytes transmitted
                              sent_carrier:
                                type: number
                                description: Number of packets not sent due to carrier errors
                              sent_colls:
                                type: number
                                description: Number of collisions
                              sent_compressed:
                                type: number
                                description: Number of compressed packets transmitted
                              sent_drop:
                                type: number
                                description: Number of packets dropped during transmission
                              sent_errs:
                                type: number
                                description: Number of transmission errors
                              sent_fifo:
                                type: number
                                description: FIFO overruns
                              sent_packets:
                                type: number
                                description: Total packets transmitted
                              connector_id:
                                type: string
                                description: Connector identifier
                            required:
                              - name
                              - recv_bytes
                              - recv_compressed
                              - recv_drop
                              - recv_errs
                              - recv_fifo
                              - recv_frame
                              - recv_multicast
                              - recv_packets
                              - sent_bytes
                              - sent_carrier
                              - sent_colls
                              - sent_compressed
                              - sent_drop
                              - sent_errs
                              - sent_fifo
                              - sent_packets
                        snmp_icmp_in_addr_mask_reps:
                          type: number
                          description: Number of ICMP Address Mask Reply messages received
                        snmp_icmp_in_addr_masks:
                          type: number
                          description: Number of ICMP Address Mask Request messages received
                        snmp_icmp_in_csum_errors:
                          type: number
                          description: Number of ICMP messages received with bad checksums
                        snmp_icmp_in_dest_unreachs:
                          type: number
                          description: Number of ICMP Destination Unreachable messages received
                        snmp_icmp_in_echo_reps:
                          type: number
                          description: Number of ICMP Echo Reply messages received
                        snmp_icmp_in_echos:
                          type: number
                          description: Number of ICMP Echo (request) messages received
                        snmp_icmp_in_errors:
                          type: number
                          description: Number of ICMP messages received with ICMP-specific errors
                        snmp_icmp_in_msgs:
                          type: number
                          description: Number of ICMP messages received
                        snmp_icmp_in_parm_probs:
                          type: number
                          description: Number of ICMP Parameter Problem messages received
                        snmp_icmp_in_redirects:
                          type: number
                          description: Number of ICMP Redirect messages received
                        snmp_icmp_in_src_quenchs:
                          type: number
                          description: Number of ICMP Source Quench messages received
                        snmp_icmp_in_time_excds:
                          type: number
                          description: Number of ICMP Time Exceeded messages received
                        snmp_icmp_in_timestamp_reps:
                          type: number
                          description: Number of ICMP Address Mask Request messages received
                        snmp_icmp_in_timestamps:
                          type: number
                          description: Number of ICMP Timestamp (request) messages received
                        snmp_icmp_out_addr_mask_reps:
                          type: number
                          description: Number of ICMP Address Mask Reply messages sent
                        snmp_icmp_out_addr_masks:
                          type: number
                          description: Number of ICMP Address Mask Request messages sent
                        snmp_icmp_out_dest_unreachs:
                          type: number
                          description: Number of ICMP Destination Unreachable messages sent
                        snmp_icmp_out_echo_reps:
                          type: number
                          description: Number of ICMP Echo Reply messages sent
                        snmp_icmp_out_echos:
                          type: number
                          description: Number of ICMP Echo (request) messages sent
                        snmp_icmp_out_errors:
                          type: number
                          description: Number of ICMP messages which this entity did not send due to
                            ICMP-specific errors
                        snmp_icmp_out_msgs:
                          type: number
                          description: Number of ICMP messages attempted to send
                        snmp_icmp_out_parm_probs:
                          type: number
                          description: Number of ICMP Parameter Problem messages sent
                        snmp_icmp_out_redirects:
                          type: number
                          description: Number of ICMP Redirect messages sent
                        snmp_icmp_out_src_quenchs:
                          type: number
                          description: Number of ICMP Source Quench messages sent
                        snmp_icmp_out_time_excds:
                          type: number
                          description: Number of ICMP Time Exceeded messages sent
                        snmp_icmp_out_timestamp_reps:
                          type: number
                          description: Number of ICMP Timestamp Reply messages sent
                        snmp_icmp_out_timestamps:
                          type: number
                          description: Number of ICMP Timestamp (request) messages sent
                        snmp_ip_default_ttl:
                          type: number
                          description: Default value of the Time-To-Live field of the IP header
                        snmp_ip_forw_datagrams:
                          type: number
                          description: Number of datagrams forwarded to their final destination
                        snmp_ip_forwarding_enabled:
                          type: boolean
                          description: Set when acting as an IP gateway
                        snmp_ip_frag_creates:
                          type: number
                          description: Number of datagrams generated by fragmentation
                        snmp_ip_frag_fails:
                          type: number
                          description: Number of datagrams discarded because fragmentation failed
                        snmp_ip_frag_oks:
                          type: number
                          description: Number of datagrams successfully fragmented
                        snmp_ip_in_addr_errors:
                          type: number
                          description: Number of input datagrams discarded due to errors in the IP address
                        snmp_ip_in_delivers:
                          type: number
                          description: Number of input datagrams successfully delivered to IP
                            user-protocols
                        snmp_ip_in_discards:
                          type: number
                          description: Number of input datagrams otherwise discarded
                        snmp_ip_in_hdr_errors:
                          type: number
                          description: Number of input datagrams discarded due to errors in the IP header
                        snmp_ip_in_receives:
                          type: number
                          description: Number of input datagrams received from interfaces
                        snmp_ip_in_unknown_protos:
                          type: number
                          description: Number of input datagrams discarded due unknown or unsupported
                            protocol
                        snmp_ip_out_discards:
                          type: number
                          description: Number of output datagrams otherwise discarded
                        snmp_ip_out_no_routes:
                          type: number
                          description: Number of output datagrams discarded because no route matched
                        snmp_ip_out_requests:
                          type: number
                          description: Number of datagrams supplied for transmission
                        snmp_ip_reasm_fails:
                          type: number
                          description: Number of failures detected by the reassembly algorithm
                        snmp_ip_reasm_oks:
                          type: number
                          description: Number of datagrams successfully reassembled
                        snmp_ip_reasm_reqds:
                          type: number
                          description: Number of fragments received which needed to be reassembled
                        snmp_ip_reasm_timeout:
                          type: number
                          description: Number of seconds fragments are held while awaiting reassembly
                        snmp_tcp_active_opens:
                          type: number
                          description: Number of times TCP transitions to SYN-SENT from CLOSED
                        snmp_tcp_attempt_fails:
                          type: number
                          description: Number of times TCP transitions to CLOSED from SYN-SENT or
                            SYN-RCVD, plus transitions to LISTEN from SYN-RCVD
                        snmp_tcp_curr_estab:
                          type: number
                          description: Number of TCP connections in ESTABLISHED or CLOSE-WAIT
                        snmp_tcp_estab_resets:
                          type: number
                          description: Number of times TCP transitions to CLOSED from ESTABLISHED or
                            CLOSE-WAIT
                        snmp_tcp_in_csum_errors:
                          type: number
                          description: Number of TCP segments received with checksum errors
                        snmp_tcp_in_errs:
                          type: number
                          description: Number of TCP segments received in error
                        snmp_tcp_in_segs:
                          type: number
                          description: Number of TCP segments received
                        snmp_tcp_max_conn:
                          type: number
                          description: Limit on the total number of TCP connections
                        snmp_tcp_out_rsts:
                          type: number
                          description: Number of TCP segments sent with RST flag
                        snmp_tcp_out_segs:
                          type: number
                          description: Number of TCP segments sent
                        snmp_tcp_passive_opens:
                          type: number
                          description: Number of times TCP transitions to SYN-RCVD from LISTEN
                        snmp_tcp_retrans_segs:
                          type: number
                          description: Number of TCP segments retransmitted
                        snmp_tcp_rto_max:
                          type: number
                          description: Maximum value permitted by a TCP implementation for the
                            retransmission timeout (milliseconds)
                        snmp_tcp_rto_min:
                          type: number
                          description: Minimum value permitted by a TCP implementation for the
                            retransmission timeout (milliseconds)
                        snmp_udp_in_datagrams:
                          type: number
                          description: Number of UDP datagrams delivered to UDP applications
                        snmp_udp_in_errors:
                          type: number
                          description: Number of UDP datagrams failed to be delivered for reasons other
                            than lack of application at the destination port
                        snmp_udp_no_ports:
                          type: number
                          description: Number of UDP datagrams received for which there was not
                            application at the destination port
                        snmp_udp_out_datagrams:
                          type: number
                          description: Number of UDP datagrams sent
                        system_boot_time_s:
                          type: number
                          description: Boottime of the system (seconds since the Unix epoch)
                        thermals:
                          type: array
                          items:
                            type: object
                            properties:
                              label:
                                type: string
                                description: Sensor identifier for the component
                              connector_id:
                                type: string
                                description: Connector identifier
                              critical_celcius:
                                type: number
                                description: Critical failure temperature of the component (degrees Celsius)
                              current_celcius:
                                type: number
                                description: Current temperature of the component (degrees Celsius)
                              max_celcius:
                                type: number
                                description: Maximum temperature of the component (degrees Celsius)
                            required:
                              - label
                        tunnels:
                          type: array
                          items:
                            type: object
                            properties:
                              health_state:
                                type: string
                                description: Name of tunnel health state (unknown, healthy, degraded, down)
                              health_value:
                                type: number
                                description: Numeric value associated with tunnel state (0 = unknown, 1 =
                                  healthy, 2 = degraded, 3 = down)
                              interface_name:
                                type: string
                                description: The tunnel interface name (i.e. xfrm1, xfrm3.99, etc.)
                              tunnel_id:
                                type: string
                                description: Tunnel identifier
                              connector_id:
                                type: string
                                description: Connector identifier
                              probed_mtu:
                                type: number
                                description: MTU as measured between the two ends of the tunnel
                            required:
                              - health_state
                              - health_value
                              - interface_name
                              - tunnel_id
                        uptime_idle_ms:
                          type: number
                          description: Sum of how much time each core has spent idle
                        uptime_total_ms:
                          type: number
                          description: Uptime of the system, including time spent in suspend
                      required:
                        - count_reclaim_failures
                        - count_reclaimed_paths
                        - count_record_failed
                        - count_transmit_failures
                        - t
                        - v
                required:
                  - count
                  - items
      x-distilled-response-path: result
  /accounts/{account_id}/magic/ipsec_tunnels/{ipsecTunnelId}/psk_generate:
    post:
      operationId: pskGenerateIpsecTunnel
      description: "Generates a Pre Shared Key for a specific IPsec tunnel used in the
        IKE session. Use `?validate_only=true` as an optional query parameter to
        only run validation without persisting changes. After a PSK is
        generated, the PSK is immediately persisted to Cloudflare's edge and
        cannot be retrieved later. Note the PSK in a safe place.  @example ```ts
        const response = await client.magicTransit.ipsecTunnels.pskGenerate(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', body: {}, }, ); ```"
      parameters:
        - name: ipsecTunnelId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema: {}
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  ipsec_tunnel_id:
                    type: string
                    description: Identifier
                  psk:
                    type: string
                    description: A randomly generated or provided string for use in the IPsec
                      tunnel.
                  psk_metadata:
                    type: object
                    properties:
                      last_generated_on:
                        type: string
                        description: The date and time the tunnel was last modified.
                    description: The PSK metadata that includes when the PSK was generated.
      x-distilled-response-path: result
  /accounts/{account_id}/magic/gre_tunnels/{greTunnelId}:
    get:
      operationId: getGreTunnel
      description: "Lists informtion for a specific GRE tunnel.  @example ```ts const
        greTunnel = await client.magicTransit.greTunnels.get(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: greTunnelId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the response
            body will be presented using the new object format. Defaults to
            false."
          schema:
            type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  gre_tunnel:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      cloudflare_gre_endpoint:
                        type: string
                        description: The IP address assigned to the Cloudflare side of the GRE tunnel.
                      customer_gre_endpoint:
                        type: string
                        description: The IP address assigned to the customer side of the GRE tunnel.
                      interface_address:
                        type: string
                        description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                          for each side of the tunnel. Select the subnet from
                          the following private IP space:
                          10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                          192.168.0.0–192.168.255.255."
                      name:
                        type: string
                        description: The name of the tunnel. The name cannot contain spaces or special
                          characters, must be 15 characters or less, and cannot
                          share a name with another GRE tunnel.
                      automatic_return_routing:
                        type: boolean
                        description: True if automatic stateful return routing should be enabled for a
                          tunnel, false otherwise.
                      bgp:
                        type: object
                        properties:
                          customer_asn:
                            type: number
                            description: ASN used on the customer end of the BGP session
                          extra_prefixes:
                            type: array
                            items:
                              type: string
                            description: Prefixes in this list will be advertised to the customer device, in
                              addition to the routes in the Magic routing table.
                          md5_key:
                            type: string
                            description: "MD5 key to use for session authentication.  Note that _this is not
                              a security measure_. MD5 is not a valid security
                              mechanism, and the key is not treated as a secret
                              value. This is _only_ supported for preventing
                              misconfiguration, not for defending against
                              malicious attacks.  The MD5 key, if set, must be
                              of non-zero length and consist only of the
                              following types of character:  - ASCII
                              alphanumerics: `[a-zA-Z0-9]` - Special characters
                              in the set `'!@#$%^& ()+[]{}<>/.,;:_-~`= \\|`  In
                              other words, MD5 keys may contain any printable
                              ASCII character aside from newline (0x0A),
                              quotation mark (`\"`), vertical tab (0x0B),
                              carriage return (0x0D), tab (0x09), form feed
                              (0x0C), and the question mark (`?`). Requests
                              specifying an MD5 key with one or more of these
                              disallowed characters will be rejected."
                        required:
                          - customer_asn
                      bgp_status:
                        type: object
                        properties:
                          state:
                            type: string
                            enum:
                              - BGP_DOWN
                              - BGP_UP
                              - BGP_ESTABLISHING
                          tcp_established:
                            type: boolean
                          updated_at:
                            type: string
                          bgp_state:
                            type: string
                          cf_speaker_ip:
                            type: string
                          cf_speaker_port:
                            type: number
                          customer_speaker_ip:
                            type: string
                          customer_speaker_port:
                            type: number
                        required:
                          - state
                          - tcp_established
                          - updated_at
                      created_on:
                        type: string
                        description: The date and time the tunnel was created.
                      description:
                        type: string
                        description: An optional description of the GRE tunnel.
                      health_check:
                        type: object
                        properties:
                          direction:
                            type: string
                            enum:
                              - unidirectional
                              - bidirectional
                            description: The direction of the flow of the healthcheck. Either
                              unidirectional, where the probe comes to you via
                              the tunnel and the result comes back to Cloudflare
                              via the open Internet, or bidirectional where both
                              the probe and result come and go via the tunnel.
                          enabled:
                            type: boolean
                            description: Determines whether to run healthchecks for a tunnel.
                          rate:
                            type: string
                            enum:
                              - low
                              - mid
                              - high
                            description: How frequent the health check is run. The default value is `mid`.
                          target:
                            oneOf:
                              - type: object
                                properties:
                                  effective:
                                    type: string
                                    description: The effective health check target. If 'saved' is empty, then this
                                      field will be populated with the
                                      calculated default value on GET requests.
                                      Ignored in POST, PUT, and PATCH requests.
                                  saved:
                                    type: string
                                    description: The saved health check target. Setting the value to the empty
                                      string indicates that the calculated
                                      default value will be used.
                              - type: string
                            description: The destination address in a request type health check. After the
                              healthcheck is decapsulated at the customer end of
                              the tunnel, the ICMP echo will be forwarded to
                              this address. This field defaults to
                              `customer_gre_endpoint address`. This field is
                              ignored for bidirectional healthchecks as the
                              interface_address (not assigned to the Cloudflare
                              side of the tunnel) is used as the target. Must be
                              in object form if the x-magic-new-hc-target header
                              is set to true and string form if
                              x-magic-new-hc-target is absent or set to false.
                          type:
                            type: string
                            enum:
                              - reply
                              - request
                            description: The type of healthcheck to run, reply or request. The default value
                              is `reply`.
                      interface_address6:
                        type: string
                        description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                          with the address being the first IP of the subnet and
                          not same as the address of virtual_subnet6. Eg if
                          virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                          interface_address6 could be
                          2606:54c1:7:0:a9fe:12d2:1:200/127
                      modified_on:
                        type: string
                        description: The date and time the tunnel was last modified.
                      mtu:
                        type: number
                        description: Maximum Transmission Unit (MTU) in bytes for the GRE tunnel. The
                          minimum value is 576.
                      ttl:
                        type: number
                        description: Time To Live (TTL) in number of hops of the GRE tunnel.
                    required:
                      - id
                      - cloudflare_gre_endpoint
                      - customer_gre_endpoint
                      - interface_address
                      - name
      x-distilled-response-path: result
    put:
      operationId: updateGreTunnel
      description: "Updates a specific GRE tunnel. Use `?validate_only=true` as an
        optional query parameter to only run validation without persisting
        changes.  @example ```ts const greTunnel = await
        client.magicTransit.greTunnels.update(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', cloudflare_gre_endpoint:
        '203.0.113.1', customer_gre_endpoint: '203.0.113.1', interface_address:
        '192.0.2.0/31', name: 'GRE_1', }, ); ```"
      parameters:
        - name: greTunnelId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the request and
            response bodies will be presented using the new object format.
            Defaults to false."
          schema:
            type: boolean
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                cloudflare_gre_endpoint:
                  type: string
                  description: "Body param: The IP address assigned to the Cloudflare side of the
                    GRE tunnel."
                customer_gre_endpoint:
                  type: string
                  description: "Body param: The IP address assigned to the customer side of the
                    GRE tunnel."
                interface_address:
                  type: string
                  description: "Body param: A 31-bit prefix (/31 in CIDR notation) supporting two
                    hosts, one for each side of the tunnel. Select the subnet
                    from the following private IP space:
                    10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                    192.168.0.0–192.168.255.255."
                name:
                  type: string
                  description: "Body param: The name of the tunnel. The name cannot contain spaces
                    or special characters, must be 15 characters or less, and
                    cannot share a name with another GRE tunnel."
                automatic_return_routing:
                  type: boolean
                  description: "Body param: True if automatic stateful return routing should be
                    enabled for a tunnel, false otherwise."
                description:
                  type: string
                  description: "Body param: An optional description of the GRE tunnel."
                health_check:
                  type: object
                  properties:
                    direction:
                      type: string
                      enum:
                        - unidirectional
                        - bidirectional
                      description: The direction of the flow of the healthcheck. Either
                        unidirectional, where the probe comes to you via the
                        tunnel and the result comes back to Cloudflare via the
                        open Internet, or bidirectional where both the probe and
                        result come and go via the tunnel.
                    enabled:
                      type: boolean
                      description: Determines whether to run healthchecks for a tunnel.
                    rate:
                      type: string
                      enum:
                        - low
                        - mid
                        - high
                      description: How frequent the health check is run. The default value is `mid`.
                    target:
                      oneOf:
                        - type: object
                          properties:
                            saved:
                              type: string
                              description: The saved health check target. Setting the value to the empty
                                string indicates that the calculated default
                                value will be used.
                        - type: string
                      description: The destination address in a request type health check. After the
                        healthcheck is decapsulated at the customer end of the
                        tunnel, the ICMP echo will be forwarded to this address.
                        This field defaults to `customer_gre_endpoint address`.
                        This field is ignored for bidirectional healthchecks as
                        the interface_address (not assigned to the Cloudflare
                        side of the tunnel) is used as the target. Must be in
                        object form if the x-magic-new-hc-target header is set
                        to true and string form if x-magic-new-hc-target is
                        absent or set to false.
                    type:
                      type: string
                      enum:
                        - reply
                        - request
                      description: The type of healthcheck to run, reply or request. The default value
                        is `reply`.
                  description: "Body param:"
                interface_address6:
                  type: string
                  description: "Body param: A 127 bit IPV6 prefix from within the virtual_subnet6
                    prefix space with the address being the first IP of the
                    subnet and not same as the address of virtual_subnet6. Eg if
                    virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                    interface_address6 could be
                    2606:54c1:7:0:a9fe:12d2:1:200/127"
                mtu:
                  type: number
                  description: "Body param: Maximum Transmission Unit (MTU) in bytes for the GRE
                    tunnel. The minimum value is 576."
                ttl:
                  type: number
                  description: "Body param: Time To Live (TTL) in number of hops of the GRE
                    tunnel."
              required:
                - cloudflare_gre_endpoint
                - customer_gre_endpoint
                - interface_address
                - name
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  modified:
                    type: boolean
                  modified_gre_tunnel:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      cloudflare_gre_endpoint:
                        type: string
                        description: The IP address assigned to the Cloudflare side of the GRE tunnel.
                      customer_gre_endpoint:
                        type: string
                        description: The IP address assigned to the customer side of the GRE tunnel.
                      interface_address:
                        type: string
                        description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                          for each side of the tunnel. Select the subnet from
                          the following private IP space:
                          10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                          192.168.0.0–192.168.255.255."
                      name:
                        type: string
                        description: The name of the tunnel. The name cannot contain spaces or special
                          characters, must be 15 characters or less, and cannot
                          share a name with another GRE tunnel.
                      automatic_return_routing:
                        type: boolean
                        description: True if automatic stateful return routing should be enabled for a
                          tunnel, false otherwise.
                      bgp:
                        type: object
                        properties:
                          customer_asn:
                            type: number
                            description: ASN used on the customer end of the BGP session
                          extra_prefixes:
                            type: array
                            items:
                              type: string
                            description: Prefixes in this list will be advertised to the customer device, in
                              addition to the routes in the Magic routing table.
                          md5_key:
                            type: string
                            description: "MD5 key to use for session authentication.  Note that _this is not
                              a security measure_. MD5 is not a valid security
                              mechanism, and the key is not treated as a secret
                              value. This is _only_ supported for preventing
                              misconfiguration, not for defending against
                              malicious attacks.  The MD5 key, if set, must be
                              of non-zero length and consist only of the
                              following types of character:  - ASCII
                              alphanumerics: `[a-zA-Z0-9]` - Special characters
                              in the set `'!@#$%^& ()+[]{}<>/.,;:_-~`= \\|`  In
                              other words, MD5 keys may contain any printable
                              ASCII character aside from newline (0x0A),
                              quotation mark (`\"`), vertical tab (0x0B),
                              carriage return (0x0D), tab (0x09), form feed
                              (0x0C), and the question mark (`?`). Requests
                              specifying an MD5 key with one or more of these
                              disallowed characters will be rejected."
                        required:
                          - customer_asn
                      bgp_status:
                        type: object
                        properties:
                          state:
                            type: string
                            enum:
                              - BGP_DOWN
                              - BGP_UP
                              - BGP_ESTABLISHING
                          tcp_established:
                            type: boolean
                          updated_at:
                            type: string
                          bgp_state:
                            type: string
                          cf_speaker_ip:
                            type: string
                          cf_speaker_port:
                            type: number
                          customer_speaker_ip:
                            type: string
                          customer_speaker_port:
                            type: number
                        required:
                          - state
                          - tcp_established
                          - updated_at
                      created_on:
                        type: string
                        description: The date and time the tunnel was created.
                      description:
                        type: string
                        description: An optional description of the GRE tunnel.
                      health_check:
                        type: object
                        properties:
                          direction:
                            type: string
                            enum:
                              - unidirectional
                              - bidirectional
                            description: The direction of the flow of the healthcheck. Either
                              unidirectional, where the probe comes to you via
                              the tunnel and the result comes back to Cloudflare
                              via the open Internet, or bidirectional where both
                              the probe and result come and go via the tunnel.
                          enabled:
                            type: boolean
                            description: Determines whether to run healthchecks for a tunnel.
                          rate:
                            type: string
                            enum:
                              - low
                              - mid
                              - high
                            description: How frequent the health check is run. The default value is `mid`.
                          target:
                            oneOf:
                              - type: object
                                properties:
                                  effective:
                                    type: string
                                    description: The effective health check target. If 'saved' is empty, then this
                                      field will be populated with the
                                      calculated default value on GET requests.
                                      Ignored in POST, PUT, and PATCH requests.
                                  saved:
                                    type: string
                                    description: The saved health check target. Setting the value to the empty
                                      string indicates that the calculated
                                      default value will be used.
                              - type: string
                            description: The destination address in a request type health check. After the
                              healthcheck is decapsulated at the customer end of
                              the tunnel, the ICMP echo will be forwarded to
                              this address. This field defaults to
                              `customer_gre_endpoint address`. This field is
                              ignored for bidirectional healthchecks as the
                              interface_address (not assigned to the Cloudflare
                              side of the tunnel) is used as the target. Must be
                              in object form if the x-magic-new-hc-target header
                              is set to true and string form if
                              x-magic-new-hc-target is absent or set to false.
                          type:
                            type: string
                            enum:
                              - reply
                              - request
                            description: The type of healthcheck to run, reply or request. The default value
                              is `reply`.
                      interface_address6:
                        type: string
                        description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                          with the address being the first IP of the subnet and
                          not same as the address of virtual_subnet6. Eg if
                          virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                          interface_address6 could be
                          2606:54c1:7:0:a9fe:12d2:1:200/127
                      modified_on:
                        type: string
                        description: The date and time the tunnel was last modified.
                      mtu:
                        type: number
                        description: Maximum Transmission Unit (MTU) in bytes for the GRE tunnel. The
                          minimum value is 576.
                      ttl:
                        type: number
                        description: Time To Live (TTL) in number of hops of the GRE tunnel.
                    required:
                      - id
                      - cloudflare_gre_endpoint
                      - customer_gre_endpoint
                      - interface_address
                      - name
      x-distilled-response-path: result
    delete:
      operationId: deleteGreTunnel
      description: "Disables and removes a specific static GRE tunnel. Use
        `?validate_only=true` as an optional query parameter to only run
        validation without persisting changes.  @example ```ts const greTunnel =
        await client.magicTransit.greTunnels.delete(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: greTunnelId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the response
            body will be presented using the new object format. Defaults to
            false."
          schema:
            type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: boolean
                  deleted_gre_tunnel:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      cloudflare_gre_endpoint:
                        type: string
                        description: The IP address assigned to the Cloudflare side of the GRE tunnel.
                      customer_gre_endpoint:
                        type: string
                        description: The IP address assigned to the customer side of the GRE tunnel.
                      interface_address:
                        type: string
                        description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                          for each side of the tunnel. Select the subnet from
                          the following private IP space:
                          10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                          192.168.0.0–192.168.255.255."
                      name:
                        type: string
                        description: The name of the tunnel. The name cannot contain spaces or special
                          characters, must be 15 characters or less, and cannot
                          share a name with another GRE tunnel.
                      automatic_return_routing:
                        type: boolean
                        description: True if automatic stateful return routing should be enabled for a
                          tunnel, false otherwise.
                      bgp:
                        type: object
                        properties:
                          customer_asn:
                            type: number
                            description: ASN used on the customer end of the BGP session
                          extra_prefixes:
                            type: array
                            items:
                              type: string
                            description: Prefixes in this list will be advertised to the customer device, in
                              addition to the routes in the Magic routing table.
                          md5_key:
                            type: string
                            description: "MD5 key to use for session authentication.  Note that _this is not
                              a security measure_. MD5 is not a valid security
                              mechanism, and the key is not treated as a secret
                              value. This is _only_ supported for preventing
                              misconfiguration, not for defending against
                              malicious attacks.  The MD5 key, if set, must be
                              of non-zero length and consist only of the
                              following types of character:  - ASCII
                              alphanumerics: `[a-zA-Z0-9]` - Special characters
                              in the set `'!@#$%^& ()+[]{}<>/.,;:_-~`= \\|`  In
                              other words, MD5 keys may contain any printable
                              ASCII character aside from newline (0x0A),
                              quotation mark (`\"`), vertical tab (0x0B),
                              carriage return (0x0D), tab (0x09), form feed
                              (0x0C), and the question mark (`?`). Requests
                              specifying an MD5 key with one or more of these
                              disallowed characters will be rejected."
                        required:
                          - customer_asn
                      bgp_status:
                        type: object
                        properties:
                          state:
                            type: string
                            enum:
                              - BGP_DOWN
                              - BGP_UP
                              - BGP_ESTABLISHING
                          tcp_established:
                            type: boolean
                          updated_at:
                            type: string
                          bgp_state:
                            type: string
                          cf_speaker_ip:
                            type: string
                          cf_speaker_port:
                            type: number
                          customer_speaker_ip:
                            type: string
                          customer_speaker_port:
                            type: number
                        required:
                          - state
                          - tcp_established
                          - updated_at
                      created_on:
                        type: string
                        description: The date and time the tunnel was created.
                      description:
                        type: string
                        description: An optional description of the GRE tunnel.
                      health_check:
                        type: object
                        properties:
                          direction:
                            type: string
                            enum:
                              - unidirectional
                              - bidirectional
                            description: The direction of the flow of the healthcheck. Either
                              unidirectional, where the probe comes to you via
                              the tunnel and the result comes back to Cloudflare
                              via the open Internet, or bidirectional where both
                              the probe and result come and go via the tunnel.
                          enabled:
                            type: boolean
                            description: Determines whether to run healthchecks for a tunnel.
                          rate:
                            type: string
                            enum:
                              - low
                              - mid
                              - high
                            description: How frequent the health check is run. The default value is `mid`.
                          target:
                            oneOf:
                              - type: object
                                properties:
                                  effective:
                                    type: string
                                    description: The effective health check target. If 'saved' is empty, then this
                                      field will be populated with the
                                      calculated default value on GET requests.
                                      Ignored in POST, PUT, and PATCH requests.
                                  saved:
                                    type: string
                                    description: The saved health check target. Setting the value to the empty
                                      string indicates that the calculated
                                      default value will be used.
                              - type: string
                            description: The destination address in a request type health check. After the
                              healthcheck is decapsulated at the customer end of
                              the tunnel, the ICMP echo will be forwarded to
                              this address. This field defaults to
                              `customer_gre_endpoint address`. This field is
                              ignored for bidirectional healthchecks as the
                              interface_address (not assigned to the Cloudflare
                              side of the tunnel) is used as the target. Must be
                              in object form if the x-magic-new-hc-target header
                              is set to true and string form if
                              x-magic-new-hc-target is absent or set to false.
                          type:
                            type: string
                            enum:
                              - reply
                              - request
                            description: The type of healthcheck to run, reply or request. The default value
                              is `reply`.
                      interface_address6:
                        type: string
                        description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                          with the address being the first IP of the subnet and
                          not same as the address of virtual_subnet6. Eg if
                          virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                          interface_address6 could be
                          2606:54c1:7:0:a9fe:12d2:1:200/127
                      modified_on:
                        type: string
                        description: The date and time the tunnel was last modified.
                      mtu:
                        type: number
                        description: Maximum Transmission Unit (MTU) in bytes for the GRE tunnel. The
                          minimum value is 576.
                      ttl:
                        type: number
                        description: Time To Live (TTL) in number of hops of the GRE tunnel.
                    required:
                      - id
                      - cloudflare_gre_endpoint
                      - customer_gre_endpoint
                      - interface_address
                      - name
      x-distilled-response-path: result
  /accounts/{account_id}/magic/gre_tunnels:
    get:
      operationId: listGreTunnels
      description: "Lists GRE tunnels associated with an account.  @example ```ts
        const greTunnels = await client.magicTransit.greTunnels.list({
        account_id: '023e105f4ecef8ad9ca31a8372d0c353', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the response
            body will be presented using the new object format. Defaults to
            false."
          schema:
            type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  gre_tunnels:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: Identifier
                        cloudflare_gre_endpoint:
                          type: string
                          description: The IP address assigned to the Cloudflare side of the GRE tunnel.
                        customer_gre_endpoint:
                          type: string
                          description: The IP address assigned to the customer side of the GRE tunnel.
                        interface_address:
                          type: string
                          description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                            for each side of the tunnel. Select the subnet from
                            the following private IP space:
                            10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                            192.168.0.0–192.168.255.255."
                        name:
                          type: string
                          description: The name of the tunnel. The name cannot contain spaces or special
                            characters, must be 15 characters or less, and
                            cannot share a name with another GRE tunnel.
                        automatic_return_routing:
                          type: boolean
                          description: True if automatic stateful return routing should be enabled for a
                            tunnel, false otherwise.
                        bgp:
                          type: object
                          properties:
                            customer_asn:
                              type: number
                              description: ASN used on the customer end of the BGP session
                            extra_prefixes:
                              type: array
                              items:
                                type: string
                              description: Prefixes in this list will be advertised to the customer device, in
                                addition to the routes in the Magic routing
                                table.
                            md5_key:
                              type: string
                              description: "MD5 key to use for session authentication.  Note that _this is not
                                a security measure_. MD5 is not a valid security
                                mechanism, and the key is not treated as a
                                secret value. This is _only_ supported for
                                preventing misconfiguration, not for defending
                                against malicious attacks.  The MD5 key, if set,
                                must be of non-zero length and consist only of
                                the following types of character:  - ASCII
                                alphanumerics: `[a-zA-Z0-9]` - Special
                                characters in the set `'!@#$%^&
                                ()+[]{}<>/.,;:_-~`= \\|`  In other words, MD5
                                keys may contain any printable ASCII character
                                aside from newline (0x0A), quotation mark
                                (`\"`), vertical tab (0x0B), carriage return
                                (0x0D), tab (0x09), form feed (0x0C), and the
                                question mark (`?`). Requests specifying an MD5
                                key with one or more of these disallowed
                                characters will be rejected."
                          required:
                            - customer_asn
                        bgp_status:
                          type: object
                          properties:
                            state:
                              type: string
                              enum:
                                - BGP_DOWN
                                - BGP_UP
                                - BGP_ESTABLISHING
                            tcp_established:
                              type: boolean
                            updated_at:
                              type: string
                            bgp_state:
                              type: string
                            cf_speaker_ip:
                              type: string
                            cf_speaker_port:
                              type: number
                            customer_speaker_ip:
                              type: string
                            customer_speaker_port:
                              type: number
                          required:
                            - state
                            - tcp_established
                            - updated_at
                        created_on:
                          type: string
                          description: The date and time the tunnel was created.
                        description:
                          type: string
                          description: An optional description of the GRE tunnel.
                        health_check:
                          type: object
                          properties:
                            direction:
                              type: string
                              enum:
                                - unidirectional
                                - bidirectional
                              description: The direction of the flow of the healthcheck. Either
                                unidirectional, where the probe comes to you via
                                the tunnel and the result comes back to
                                Cloudflare via the open Internet, or
                                bidirectional where both the probe and result
                                come and go via the tunnel.
                            enabled:
                              type: boolean
                              description: Determines whether to run healthchecks for a tunnel.
                            rate:
                              type: string
                              enum:
                                - low
                                - mid
                                - high
                              description: How frequent the health check is run. The default value is `mid`.
                            target:
                              oneOf:
                                - type: object
                                  properties:
                                    effective:
                                      type: string
                                      description: The effective health check target. If 'saved' is empty, then this
                                        field will be populated with the
                                        calculated default value on GET
                                        requests. Ignored in POST, PUT, and
                                        PATCH requests.
                                    saved:
                                      type: string
                                      description: The saved health check target. Setting the value to the empty
                                        string indicates that the calculated
                                        default value will be used.
                                - type: string
                              description: The destination address in a request type health check. After the
                                healthcheck is decapsulated at the customer end
                                of the tunnel, the ICMP echo will be forwarded
                                to this address. This field defaults to
                                `customer_gre_endpoint address`. This field is
                                ignored for bidirectional healthchecks as the
                                interface_address (not assigned to the
                                Cloudflare side of the tunnel) is used as the
                                target. Must be in object form if the
                                x-magic-new-hc-target header is set to true and
                                string form if x-magic-new-hc-target is absent
                                or set to false.
                            type:
                              type: string
                              enum:
                                - reply
                                - request
                              description: The type of healthcheck to run, reply or request. The default value
                                is `reply`.
                        interface_address6:
                          type: string
                          description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                            with the address being the first IP of the subnet
                            and not same as the address of virtual_subnet6. Eg
                            if virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127
                            , interface_address6 could be
                            2606:54c1:7:0:a9fe:12d2:1:200/127
                        modified_on:
                          type: string
                          description: The date and time the tunnel was last modified.
                        mtu:
                          type: number
                          description: Maximum Transmission Unit (MTU) in bytes for the GRE tunnel. The
                            minimum value is 576.
                        ttl:
                          type: number
                          description: Time To Live (TTL) in number of hops of the GRE tunnel.
                      required:
                        - id
                        - cloudflare_gre_endpoint
                        - customer_gre_endpoint
                        - interface_address
                        - name
      x-distilled-response-path: result
    post:
      operationId: createGreTunnel
      description: "Creates a new GRE tunnel. Use `?validate_only=true` as an optional
        query parameter to only run validation without persisting
        changes.  @example ```ts const greTunnel = await
        client.magicTransit.greTunnels.create({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', cloudflare_gre_endpoint:
        '203.0.113.1', customer_gre_endpoint: '203.0.113.1', interface_address:
        '192.0.2.0/31', name: 'GRE_1', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the request and
            response bodies will be presented using the new object format.
            Defaults to false."
          schema:
            type: boolean
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                cloudflare_gre_endpoint:
                  type: string
                  description: "Body param: The IP address assigned to the Cloudflare side of the
                    GRE tunnel."
                customer_gre_endpoint:
                  type: string
                  description: "Body param: The IP address assigned to the customer side of the
                    GRE tunnel."
                interface_address:
                  type: string
                  description: "Body param: A 31-bit prefix (/31 in CIDR notation) supporting two
                    hosts, one for each side of the tunnel. Select the subnet
                    from the following private IP space:
                    10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                    192.168.0.0–192.168.255.255."
                name:
                  type: string
                  description: "Body param: The name of the tunnel. The name cannot contain spaces
                    or special characters, must be 15 characters or less, and
                    cannot share a name with another GRE tunnel."
                automatic_return_routing:
                  type: boolean
                  description: "Body param: True if automatic stateful return routing should be
                    enabled for a tunnel, false otherwise."
                bgp:
                  type: object
                  properties:
                    customer_asn:
                      type: number
                      description: ASN used on the customer end of the BGP session
                    extra_prefixes:
                      type: array
                      items:
                        type: string
                      description: Prefixes in this list will be advertised to the customer device, in
                        addition to the routes in the Magic routing table.
                    md5_key:
                      type: string
                      description: "MD5 key to use for session authentication.  Note that _this is not
                        a security measure_. MD5 is not a valid security
                        mechanism, and the key is not treated as a secret value.
                        This is _only_ supported for preventing
                        misconfiguration, not for defending against malicious
                        attacks.  The MD5 key, if set, must be of non-zero
                        length and consist only of the following types of
                        character:  - ASCII alphanumerics: `[a-zA-Z0-9]` -
                        Special characters in the set `'!@#$%^&
                        ()+[]{}<>/.,;:_-~`= \\|`  In other words, MD5 keys may
                        contain any printable ASCII character aside from newline
                        (0x0A), quotation mark (`\"`), vertical tab (0x0B),
                        carriage return (0x0D), tab (0x09), form feed (0x0C),
                        and the question mark (`?`). Requests specifying an MD5
                        key with one or more of these disallowed characters will
                        be rejected."
                  required:
                    - customer_asn
                  description: "Body param:"
                description:
                  type: string
                  description: "Body param: An optional description of the GRE tunnel."
                health_check:
                  type: object
                  properties:
                    direction:
                      type: string
                      enum:
                        - unidirectional
                        - bidirectional
                      description: The direction of the flow of the healthcheck. Either
                        unidirectional, where the probe comes to you via the
                        tunnel and the result comes back to Cloudflare via the
                        open Internet, or bidirectional where both the probe and
                        result come and go via the tunnel.
                    enabled:
                      type: boolean
                      description: Determines whether to run healthchecks for a tunnel.
                    rate:
                      type: string
                      enum:
                        - low
                        - mid
                        - high
                      description: How frequent the health check is run. The default value is `mid`.
                    target:
                      oneOf:
                        - type: object
                          properties:
                            saved:
                              type: string
                              description: The saved health check target. Setting the value to the empty
                                string indicates that the calculated default
                                value will be used.
                        - type: string
                      description: The destination address in a request type health check. After the
                        healthcheck is decapsulated at the customer end of the
                        tunnel, the ICMP echo will be forwarded to this address.
                        This field defaults to `customer_gre_endpoint address`.
                        This field is ignored for bidirectional healthchecks as
                        the interface_address (not assigned to the Cloudflare
                        side of the tunnel) is used as the target. Must be in
                        object form if the x-magic-new-hc-target header is set
                        to true and string form if x-magic-new-hc-target is
                        absent or set to false.
                    type:
                      type: string
                      enum:
                        - reply
                        - request
                      description: The type of healthcheck to run, reply or request. The default value
                        is `reply`.
                  description: "Body param:"
                interface_address6:
                  type: string
                  description: "Body param: A 127 bit IPV6 prefix from within the virtual_subnet6
                    prefix space with the address being the first IP of the
                    subnet and not same as the address of virtual_subnet6. Eg if
                    virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                    interface_address6 could be
                    2606:54c1:7:0:a9fe:12d2:1:200/127"
                mtu:
                  type: number
                  description: "Body param: Maximum Transmission Unit (MTU) in bytes for the GRE
                    tunnel. The minimum value is 576."
                ttl:
                  type: number
                  description: "Body param: Time To Live (TTL) in number of hops of the GRE
                    tunnel."
              required:
                - cloudflare_gre_endpoint
                - customer_gre_endpoint
                - interface_address
                - name
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  cloudflare_gre_endpoint:
                    type: string
                    description: The IP address assigned to the Cloudflare side of the GRE tunnel.
                  customer_gre_endpoint:
                    type: string
                    description: The IP address assigned to the customer side of the GRE tunnel.
                  interface_address:
                    type: string
                    description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                      for each side of the tunnel. Select the subnet from the
                      following private IP space: 10.0.0.0–10.255.255.255,
                      172.16.0.0–172.31.255.255, 192.168.0.0–192.168.255.255."
                  name:
                    type: string
                    description: The name of the tunnel. The name cannot contain spaces or special
                      characters, must be 15 characters or less, and cannot
                      share a name with another GRE tunnel.
                  automatic_return_routing:
                    type: boolean
                    description: True if automatic stateful return routing should be enabled for a
                      tunnel, false otherwise.
                  bgp:
                    type: object
                    properties:
                      customer_asn:
                        type: number
                        description: ASN used on the customer end of the BGP session
                      extra_prefixes:
                        type: array
                        items:
                          type: string
                        description: Prefixes in this list will be advertised to the customer device, in
                          addition to the routes in the Magic routing table.
                      md5_key:
                        type: string
                        description: "MD5 key to use for session authentication.  Note that _this is not
                          a security measure_. MD5 is not a valid security
                          mechanism, and the key is not treated as a secret
                          value. This is _only_ supported for preventing
                          misconfiguration, not for defending against malicious
                          attacks.  The MD5 key, if set, must be of non-zero
                          length and consist only of the following types of
                          character:  - ASCII alphanumerics: `[a-zA-Z0-9]` -
                          Special characters in the set `'!@#$%^&
                          ()+[]{}<>/.,;:_-~`= \\|`  In other words, MD5 keys may
                          contain any printable ASCII character aside from
                          newline (0x0A), quotation mark (`\"`), vertical tab
                          (0x0B), carriage return (0x0D), tab (0x09), form feed
                          (0x0C), and the question mark (`?`). Requests
                          specifying an MD5 key with one or more of these
                          disallowed characters will be rejected."
                    required:
                      - customer_asn
                  bgp_status:
                    type: object
                    properties:
                      state:
                        type: string
                        enum:
                          - BGP_DOWN
                          - BGP_UP
                          - BGP_ESTABLISHING
                      tcp_established:
                        type: boolean
                      updated_at:
                        type: string
                      bgp_state:
                        type: string
                      cf_speaker_ip:
                        type: string
                      cf_speaker_port:
                        type: number
                      customer_speaker_ip:
                        type: string
                      customer_speaker_port:
                        type: number
                    required:
                      - state
                      - tcp_established
                      - updated_at
                  created_on:
                    type: string
                    description: The date and time the tunnel was created.
                  description:
                    type: string
                    description: An optional description of the GRE tunnel.
                  health_check:
                    type: object
                    properties:
                      direction:
                        type: string
                        enum:
                          - unidirectional
                          - bidirectional
                        description: The direction of the flow of the healthcheck. Either
                          unidirectional, where the probe comes to you via the
                          tunnel and the result comes back to Cloudflare via the
                          open Internet, or bidirectional where both the probe
                          and result come and go via the tunnel.
                      enabled:
                        type: boolean
                        description: Determines whether to run healthchecks for a tunnel.
                      rate:
                        type: string
                        enum:
                          - low
                          - mid
                          - high
                        description: How frequent the health check is run. The default value is `mid`.
                      target:
                        oneOf:
                          - type: object
                            properties:
                              effective:
                                type: string
                                description: The effective health check target. If 'saved' is empty, then this
                                  field will be populated with the calculated
                                  default value on GET requests. Ignored in
                                  POST, PUT, and PATCH requests.
                              saved:
                                type: string
                                description: The saved health check target. Setting the value to the empty
                                  string indicates that the calculated default
                                  value will be used.
                          - type: string
                        description: The destination address in a request type health check. After the
                          healthcheck is decapsulated at the customer end of the
                          tunnel, the ICMP echo will be forwarded to this
                          address. This field defaults to `customer_gre_endpoint
                          address`. This field is ignored for bidirectional
                          healthchecks as the interface_address (not assigned to
                          the Cloudflare side of the tunnel) is used as the
                          target. Must be in object form if the
                          x-magic-new-hc-target header is set to true and string
                          form if x-magic-new-hc-target is absent or set to
                          false.
                      type:
                        type: string
                        enum:
                          - reply
                          - request
                        description: The type of healthcheck to run, reply or request. The default value
                          is `reply`.
                  interface_address6:
                    type: string
                    description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                      with the address being the first IP of the subnet and not
                      same as the address of virtual_subnet6. Eg if
                      virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                      interface_address6 could be
                      2606:54c1:7:0:a9fe:12d2:1:200/127
                  modified_on:
                    type: string
                    description: The date and time the tunnel was last modified.
                  mtu:
                    type: number
                    description: Maximum Transmission Unit (MTU) in bytes for the GRE tunnel. The
                      minimum value is 576.
                  ttl:
                    type: number
                    description: Time To Live (TTL) in number of hops of the GRE tunnel.
                required:
                  - id
                  - cloudflare_gre_endpoint
                  - customer_gre_endpoint
                  - interface_address
                  - name
      x-distilled-response-path: result
    put:
      operationId: bulkPutGreTunnels
      description: "Updates multiple GRE tunnels. Use `?validate_only=true` as an
        optional query parameter to only run validation without persisting
        changes.  @example ```ts const response = await
        client.magicTransit.greTunnels.bulkUpdate({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', body: {}, }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the request and
            response bodies will be presented using the new object format.
            Defaults to false."
          schema:
            type: boolean
      requestBody:
        required: true
        content:
          application/json:
            schema: {}
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  modified:
                    type: boolean
                  modified_gre_tunnels:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: Identifier
                        cloudflare_gre_endpoint:
                          type: string
                          description: The IP address assigned to the Cloudflare side of the GRE tunnel.
                        customer_gre_endpoint:
                          type: string
                          description: The IP address assigned to the customer side of the GRE tunnel.
                        interface_address:
                          type: string
                          description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                            for each side of the tunnel. Select the subnet from
                            the following private IP space:
                            10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                            192.168.0.0–192.168.255.255."
                        name:
                          type: string
                          description: The name of the tunnel. The name cannot contain spaces or special
                            characters, must be 15 characters or less, and
                            cannot share a name with another GRE tunnel.
                        automatic_return_routing:
                          type: boolean
                          description: True if automatic stateful return routing should be enabled for a
                            tunnel, false otherwise.
                        bgp:
                          type: object
                          properties:
                            customer_asn:
                              type: number
                              description: ASN used on the customer end of the BGP session
                            extra_prefixes:
                              type: array
                              items:
                                type: string
                              description: Prefixes in this list will be advertised to the customer device, in
                                addition to the routes in the Magic routing
                                table.
                            md5_key:
                              type: string
                              description: "MD5 key to use for session authentication.  Note that _this is not
                                a security measure_. MD5 is not a valid security
                                mechanism, and the key is not treated as a
                                secret value. This is _only_ supported for
                                preventing misconfiguration, not for defending
                                against malicious attacks.  The MD5 key, if set,
                                must be of non-zero length and consist only of
                                the following types of character:  - ASCII
                                alphanumerics: `[a-zA-Z0-9]` - Special
                                characters in the set `'!@#$%^&
                                ()+[]{}<>/.,;:_-~`= \\|`  In other words, MD5
                                keys may contain any printable ASCII character
                                aside from newline (0x0A), quotation mark
                                (`\"`), vertical tab (0x0B), carriage return
                                (0x0D), tab (0x09), form feed (0x0C), and the
                                question mark (`?`). Requests specifying an MD5
                                key with one or more of these disallowed
                                characters will be rejected."
                          required:
                            - customer_asn
                        bgp_status:
                          type: object
                          properties:
                            state:
                              type: string
                              enum:
                                - BGP_DOWN
                                - BGP_UP
                                - BGP_ESTABLISHING
                            tcp_established:
                              type: boolean
                            updated_at:
                              type: string
                            bgp_state:
                              type: string
                            cf_speaker_ip:
                              type: string
                            cf_speaker_port:
                              type: number
                            customer_speaker_ip:
                              type: string
                            customer_speaker_port:
                              type: number
                          required:
                            - state
                            - tcp_established
                            - updated_at
                        created_on:
                          type: string
                          description: The date and time the tunnel was created.
                        description:
                          type: string
                          description: An optional description of the GRE tunnel.
                        health_check:
                          type: object
                          properties:
                            direction:
                              type: string
                              enum:
                                - unidirectional
                                - bidirectional
                              description: The direction of the flow of the healthcheck. Either
                                unidirectional, where the probe comes to you via
                                the tunnel and the result comes back to
                                Cloudflare via the open Internet, or
                                bidirectional where both the probe and result
                                come and go via the tunnel.
                            enabled:
                              type: boolean
                              description: Determines whether to run healthchecks for a tunnel.
                            rate:
                              type: string
                              enum:
                                - low
                                - mid
                                - high
                              description: How frequent the health check is run. The default value is `mid`.
                            target:
                              oneOf:
                                - type: object
                                  properties:
                                    effective:
                                      type: string
                                      description: The effective health check target. If 'saved' is empty, then this
                                        field will be populated with the
                                        calculated default value on GET
                                        requests. Ignored in POST, PUT, and
                                        PATCH requests.
                                    saved:
                                      type: string
                                      description: The saved health check target. Setting the value to the empty
                                        string indicates that the calculated
                                        default value will be used.
                                - type: string
                              description: The destination address in a request type health check. After the
                                healthcheck is decapsulated at the customer end
                                of the tunnel, the ICMP echo will be forwarded
                                to this address. This field defaults to
                                `customer_gre_endpoint address`. This field is
                                ignored for bidirectional healthchecks as the
                                interface_address (not assigned to the
                                Cloudflare side of the tunnel) is used as the
                                target. Must be in object form if the
                                x-magic-new-hc-target header is set to true and
                                string form if x-magic-new-hc-target is absent
                                or set to false.
                            type:
                              type: string
                              enum:
                                - reply
                                - request
                              description: The type of healthcheck to run, reply or request. The default value
                                is `reply`.
                        interface_address6:
                          type: string
                          description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                            with the address being the first IP of the subnet
                            and not same as the address of virtual_subnet6. Eg
                            if virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127
                            , interface_address6 could be
                            2606:54c1:7:0:a9fe:12d2:1:200/127
                        modified_on:
                          type: string
                          description: The date and time the tunnel was last modified.
                        mtu:
                          type: number
                          description: Maximum Transmission Unit (MTU) in bytes for the GRE tunnel. The
                            minimum value is 576.
                        ttl:
                          type: number
                          description: Time To Live (TTL) in number of hops of the GRE tunnel.
                      required:
                        - id
                        - cloudflare_gre_endpoint
                        - customer_gre_endpoint
                        - interface_address
                        - name
      x-distilled-response-path: result
  /accounts/{account_id}/magic/ipsec_tunnels/{ipsecTunnelId}:
    get:
      operationId: getIpsecTunnel
      description: "Lists details for a specific IPsec tunnel.  @example ```ts const
        ipsecTunnel = await client.magicTransit.ipsecTunnels.get(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: ipsecTunnelId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the response
            body will be presented using the new object format. Defaults to
            false."
          schema:
            type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  ipsec_tunnel:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      cloudflare_endpoint:
                        type: string
                        description: The IP address assigned to the Cloudflare side of the IPsec tunnel.
                      interface_address:
                        type: string
                        description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                          for each side of the tunnel. Select the subnet from
                          the following private IP space:
                          10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                          192.168.0.0–192.168.255.255."
                      name:
                        type: string
                        description: The name of the IPsec tunnel. The name cannot share a name with
                          other tunnels.
                      allow_null_cipher:
                        type: boolean
                        description: When `true`, the tunnel can use a null-cipher (`ENCR_NULL`) in the
                          ESP tunnel (Phase 2).
                      automatic_return_routing:
                        type: boolean
                        description: True if automatic stateful return routing should be enabled for a
                          tunnel, false otherwise.
                      bgp:
                        type: object
                        properties:
                          customer_asn:
                            type: number
                            description: ASN used on the customer end of the BGP session
                          extra_prefixes:
                            type: array
                            items:
                              type: string
                            description: Prefixes in this list will be advertised to the customer device, in
                              addition to the routes in the Magic routing table.
                          md5_key:
                            type: string
                            description: "MD5 key to use for session authentication.  Note that _this is not
                              a security measure_. MD5 is not a valid security
                              mechanism, and the key is not treated as a secret
                              value. This is _only_ supported for preventing
                              misconfiguration, not for defending against
                              malicious attacks.  The MD5 key, if set, must be
                              of non-zero length and consist only of the
                              following types of character:  - ASCII
                              alphanumerics: `[a-zA-Z0-9]` - Special characters
                              in the set `'!@#$%^& ()+[]{}<>/.,;:_-~`= \\|`  In
                              other words, MD5 keys may contain any printable
                              ASCII character aside from newline (0x0A),
                              quotation mark (`\"`), vertical tab (0x0B),
                              carriage return (0x0D), tab (0x09), form feed
                              (0x0C), and the question mark (`?`). Requests
                              specifying an MD5 key with one or more of these
                              disallowed characters will be rejected."
                        required:
                          - customer_asn
                      bgp_status:
                        type: object
                        properties:
                          state:
                            type: string
                            enum:
                              - BGP_DOWN
                              - BGP_UP
                              - BGP_ESTABLISHING
                          tcp_established:
                            type: boolean
                          updated_at:
                            type: string
                          bgp_state:
                            type: string
                          cf_speaker_ip:
                            type: string
                          cf_speaker_port:
                            type: number
                          customer_speaker_ip:
                            type: string
                          customer_speaker_port:
                            type: number
                        required:
                          - state
                          - tcp_established
                          - updated_at
                      created_on:
                        type: string
                        description: The date and time the tunnel was created.
                      custom_remote_identities:
                        type: object
                        properties:
                          fqdn_id:
                            type: string
                            description: A custom IKE ID of type FQDN that may be used to identity the IPsec
                              tunnel. The generated IKE IDs can still be used
                              even if this custom value is specified.  Must be
                              of the form `<custom label>.<account
                              ID>.custom.ipsec.cloudflare.com`.  This custom ID
                              does not need to be unique. Two IPsec tunnels may
                              have the same custom fqdn_id. However, if another
                              IPsec tunnel has the same value then the two
                              tunnels cannot have the same cloudflare_endpoint.
                      customer_endpoint:
                        type: string
                        description: The IP address assigned to the customer side of the IPsec tunnel.
                          Not required, but must be set for proactive
                          traceroutes to work.
                      description:
                        type: string
                        description: An optional description forthe IPsec tunnel.
                      health_check:
                        type: object
                        properties:
                          direction:
                            type: string
                            enum:
                              - unidirectional
                              - bidirectional
                            description: The direction of the flow of the healthcheck. Either
                              unidirectional, where the probe comes to you via
                              the tunnel and the result comes back to Cloudflare
                              via the open Internet, or bidirectional where both
                              the probe and result come and go via the tunnel.
                          enabled:
                            type: boolean
                            description: Determines whether to run healthchecks for a tunnel.
                          rate:
                            type: string
                            enum:
                              - low
                              - mid
                              - high
                            description: How frequent the health check is run. The default value is `mid`.
                          target:
                            oneOf:
                              - type: object
                                properties:
                                  effective:
                                    type: string
                                    description: The effective health check target. If 'saved' is empty, then this
                                      field will be populated with the
                                      calculated default value on GET requests.
                                      Ignored in POST, PUT, and PATCH requests.
                                  saved:
                                    type: string
                                    description: The saved health check target. Setting the value to the empty
                                      string indicates that the calculated
                                      default value will be used.
                              - type: string
                            description: The destination address in a request type health check. After the
                              healthcheck is decapsulated at the customer end of
                              the tunnel, the ICMP echo will be forwarded to
                              this address. This field defaults to
                              `customer_gre_endpoint address`. This field is
                              ignored for bidirectional healthchecks as the
                              interface_address (not assigned to the Cloudflare
                              side of the tunnel) is used as the target. Must be
                              in object form if the x-magic-new-hc-target header
                              is set to true and string form if
                              x-magic-new-hc-target is absent or set to false.
                          type:
                            type: string
                            enum:
                              - reply
                              - request
                            description: The type of healthcheck to run, reply or request. The default value
                              is `reply`.
                      interface_address6:
                        type: string
                        description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                          with the address being the first IP of the subnet and
                          not same as the address of virtual_subnet6. Eg if
                          virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                          interface_address6 could be
                          2606:54c1:7:0:a9fe:12d2:1:200/127
                      modified_on:
                        type: string
                        description: The date and time the tunnel was last modified.
                      psk_metadata:
                        type: object
                        properties:
                          last_generated_on:
                            type: string
                            description: The date and time the tunnel was last modified.
                        description: The PSK metadata that includes when the PSK was generated.
                      replay_protection:
                        type: boolean
                        description: If `true`, then IPsec replay protection will be supported in the
                          Cloudflare-to-customer direction.
                    required:
                      - id
                      - cloudflare_endpoint
                      - interface_address
                      - name
      x-distilled-response-path: result
    put:
      operationId: updateIpsecTunnel
      description: "Updates a specific IPsec tunnel associated with an account. Use
        `?validate_only=true` as an optional query parameter to only run
        validation without persisting changes.  @example ```ts const ipsecTunnel
        = await client.magicTransit.ipsecTunnels.update(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', cloudflare_endpoint: '203.0.113.1',
        interface_address: '192.0.2.0/31', name: 'IPsec_1', }, ); ```"
      parameters:
        - name: ipsecTunnelId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the request and
            response bodies will be presented using the new object format.
            Defaults to false."
          schema:
            type: boolean
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                cloudflare_endpoint:
                  type: string
                  description: "Body param: The IP address assigned to the Cloudflare side of the
                    IPsec tunnel."
                interface_address:
                  type: string
                  description: "Body param: A 31-bit prefix (/31 in CIDR notation) supporting two
                    hosts, one for each side of the tunnel. Select the subnet
                    from the following private IP space:
                    10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                    192.168.0.0–192.168.255.255."
                name:
                  type: string
                  description: "Body param: The name of the IPsec tunnel. The name cannot share a
                    name with other tunnels."
                automatic_return_routing:
                  type: boolean
                  description: "Body param: True if automatic stateful return routing should be
                    enabled for a tunnel, false otherwise."
                bgp:
                  type: object
                  properties:
                    customer_asn:
                      type: number
                      description: ASN used on the customer end of the BGP session
                    extra_prefixes:
                      type: array
                      items:
                        type: string
                      description: Prefixes in this list will be advertised to the customer device, in
                        addition to the routes in the Magic routing table.
                    md5_key:
                      type: string
                      description: "MD5 key to use for session authentication.  Note that _this is not
                        a security measure_. MD5 is not a valid security
                        mechanism, and the key is not treated as a secret value.
                        This is _only_ supported for preventing
                        misconfiguration, not for defending against malicious
                        attacks.  The MD5 key, if set, must be of non-zero
                        length and consist only of the following types of
                        character:  - ASCII alphanumerics: `[a-zA-Z0-9]` -
                        Special characters in the set `'!@#$%^&
                        ()+[]{}<>/.,;:_-~`= \\|`  In other words, MD5 keys may
                        contain any printable ASCII character aside from newline
                        (0x0A), quotation mark (`\"`), vertical tab (0x0B),
                        carriage return (0x0D), tab (0x09), form feed (0x0C),
                        and the question mark (`?`). Requests specifying an MD5
                        key with one or more of these disallowed characters will
                        be rejected."
                  required:
                    - customer_asn
                  description: "Body param:"
                custom_remote_identities:
                  type: object
                  properties:
                    fqdn_id:
                      type: string
                      description: A custom IKE ID of type FQDN that may be used to identity the IPsec
                        tunnel. The generated IKE IDs can still be used even if
                        this custom value is specified.  Must be of the form
                        `<custom label>.<account
                        ID>.custom.ipsec.cloudflare.com`.  This custom ID does
                        not need to be unique. Two IPsec tunnels may have the
                        same custom fqdn_id. However, if another IPsec tunnel
                        has the same value then the two tunnels cannot have the
                        same cloudflare_endpoint.
                  description: "Body param:"
                customer_endpoint:
                  type: string
                  description: "Body param: The IP address assigned to the customer side of the
                    IPsec tunnel. Not required, but must be set for proactive
                    traceroutes to work."
                description:
                  type: string
                  description: "Body param: An optional description forthe IPsec tunnel."
                health_check:
                  type: object
                  properties:
                    direction:
                      type: string
                      enum:
                        - unidirectional
                        - bidirectional
                      description: The direction of the flow of the healthcheck. Either
                        unidirectional, where the probe comes to you via the
                        tunnel and the result comes back to Cloudflare via the
                        open Internet, or bidirectional where both the probe and
                        result come and go via the tunnel.
                    enabled:
                      type: boolean
                      description: Determines whether to run healthchecks for a tunnel.
                    rate:
                      type: string
                      enum:
                        - low
                        - mid
                        - high
                      description: How frequent the health check is run. The default value is `mid`.
                    target:
                      oneOf:
                        - type: object
                          properties:
                            saved:
                              type: string
                              description: The saved health check target. Setting the value to the empty
                                string indicates that the calculated default
                                value will be used.
                        - type: string
                      description: The destination address in a request type health check. After the
                        healthcheck is decapsulated at the customer end of the
                        tunnel, the ICMP echo will be forwarded to this address.
                        This field defaults to `customer_gre_endpoint address`.
                        This field is ignored for bidirectional healthchecks as
                        the interface_address (not assigned to the Cloudflare
                        side of the tunnel) is used as the target. Must be in
                        object form if the x-magic-new-hc-target header is set
                        to true and string form if x-magic-new-hc-target is
                        absent or set to false.
                    type:
                      type: string
                      enum:
                        - reply
                        - request
                      description: The type of healthcheck to run, reply or request. The default value
                        is `reply`.
                  description: "Body param:"
                interface_address6:
                  type: string
                  description: "Body param: A 127 bit IPV6 prefix from within the virtual_subnet6
                    prefix space with the address being the first IP of the
                    subnet and not same as the address of virtual_subnet6. Eg if
                    virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                    interface_address6 could be
                    2606:54c1:7:0:a9fe:12d2:1:200/127"
                psk:
                  type: string
                  description: "Body param: A randomly generated or provided string for use in the
                    IPsec tunnel."
                replay_protection:
                  type: boolean
                  description: "Body param: If `true`, then IPsec replay protection will be
                    supported in the Cloudflare-to-customer direction."
              required:
                - cloudflare_endpoint
                - interface_address
                - name
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  modified:
                    type: boolean
                  modified_ipsec_tunnel:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      cloudflare_endpoint:
                        type: string
                        description: The IP address assigned to the Cloudflare side of the IPsec tunnel.
                      interface_address:
                        type: string
                        description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                          for each side of the tunnel. Select the subnet from
                          the following private IP space:
                          10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                          192.168.0.0–192.168.255.255."
                      name:
                        type: string
                        description: The name of the IPsec tunnel. The name cannot share a name with
                          other tunnels.
                      allow_null_cipher:
                        type: boolean
                        description: When `true`, the tunnel can use a null-cipher (`ENCR_NULL`) in the
                          ESP tunnel (Phase 2).
                      automatic_return_routing:
                        type: boolean
                        description: True if automatic stateful return routing should be enabled for a
                          tunnel, false otherwise.
                      bgp:
                        type: object
                        properties:
                          customer_asn:
                            type: number
                            description: ASN used on the customer end of the BGP session
                          extra_prefixes:
                            type: array
                            items:
                              type: string
                            description: Prefixes in this list will be advertised to the customer device, in
                              addition to the routes in the Magic routing table.
                          md5_key:
                            type: string
                            description: "MD5 key to use for session authentication.  Note that _this is not
                              a security measure_. MD5 is not a valid security
                              mechanism, and the key is not treated as a secret
                              value. This is _only_ supported for preventing
                              misconfiguration, not for defending against
                              malicious attacks.  The MD5 key, if set, must be
                              of non-zero length and consist only of the
                              following types of character:  - ASCII
                              alphanumerics: `[a-zA-Z0-9]` - Special characters
                              in the set `'!@#$%^& ()+[]{}<>/.,;:_-~`= \\|`  In
                              other words, MD5 keys may contain any printable
                              ASCII character aside from newline (0x0A),
                              quotation mark (`\"`), vertical tab (0x0B),
                              carriage return (0x0D), tab (0x09), form feed
                              (0x0C), and the question mark (`?`). Requests
                              specifying an MD5 key with one or more of these
                              disallowed characters will be rejected."
                        required:
                          - customer_asn
                      bgp_status:
                        type: object
                        properties:
                          state:
                            type: string
                            enum:
                              - BGP_DOWN
                              - BGP_UP
                              - BGP_ESTABLISHING
                          tcp_established:
                            type: boolean
                          updated_at:
                            type: string
                          bgp_state:
                            type: string
                          cf_speaker_ip:
                            type: string
                          cf_speaker_port:
                            type: number
                          customer_speaker_ip:
                            type: string
                          customer_speaker_port:
                            type: number
                        required:
                          - state
                          - tcp_established
                          - updated_at
                      created_on:
                        type: string
                        description: The date and time the tunnel was created.
                      custom_remote_identities:
                        type: object
                        properties:
                          fqdn_id:
                            type: string
                            description: A custom IKE ID of type FQDN that may be used to identity the IPsec
                              tunnel. The generated IKE IDs can still be used
                              even if this custom value is specified.  Must be
                              of the form `<custom label>.<account
                              ID>.custom.ipsec.cloudflare.com`.  This custom ID
                              does not need to be unique. Two IPsec tunnels may
                              have the same custom fqdn_id. However, if another
                              IPsec tunnel has the same value then the two
                              tunnels cannot have the same cloudflare_endpoint.
                      customer_endpoint:
                        type: string
                        description: The IP address assigned to the customer side of the IPsec tunnel.
                          Not required, but must be set for proactive
                          traceroutes to work.
                      description:
                        type: string
                        description: An optional description forthe IPsec tunnel.
                      health_check:
                        type: object
                        properties:
                          direction:
                            type: string
                            enum:
                              - unidirectional
                              - bidirectional
                            description: The direction of the flow of the healthcheck. Either
                              unidirectional, where the probe comes to you via
                              the tunnel and the result comes back to Cloudflare
                              via the open Internet, or bidirectional where both
                              the probe and result come and go via the tunnel.
                          enabled:
                            type: boolean
                            description: Determines whether to run healthchecks for a tunnel.
                          rate:
                            type: string
                            enum:
                              - low
                              - mid
                              - high
                            description: How frequent the health check is run. The default value is `mid`.
                          target:
                            oneOf:
                              - type: object
                                properties:
                                  effective:
                                    type: string
                                    description: The effective health check target. If 'saved' is empty, then this
                                      field will be populated with the
                                      calculated default value on GET requests.
                                      Ignored in POST, PUT, and PATCH requests.
                                  saved:
                                    type: string
                                    description: The saved health check target. Setting the value to the empty
                                      string indicates that the calculated
                                      default value will be used.
                              - type: string
                            description: The destination address in a request type health check. After the
                              healthcheck is decapsulated at the customer end of
                              the tunnel, the ICMP echo will be forwarded to
                              this address. This field defaults to
                              `customer_gre_endpoint address`. This field is
                              ignored for bidirectional healthchecks as the
                              interface_address (not assigned to the Cloudflare
                              side of the tunnel) is used as the target. Must be
                              in object form if the x-magic-new-hc-target header
                              is set to true and string form if
                              x-magic-new-hc-target is absent or set to false.
                          type:
                            type: string
                            enum:
                              - reply
                              - request
                            description: The type of healthcheck to run, reply or request. The default value
                              is `reply`.
                      interface_address6:
                        type: string
                        description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                          with the address being the first IP of the subnet and
                          not same as the address of virtual_subnet6. Eg if
                          virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                          interface_address6 could be
                          2606:54c1:7:0:a9fe:12d2:1:200/127
                      modified_on:
                        type: string
                        description: The date and time the tunnel was last modified.
                      psk_metadata:
                        type: object
                        properties:
                          last_generated_on:
                            type: string
                            description: The date and time the tunnel was last modified.
                        description: The PSK metadata that includes when the PSK was generated.
                      replay_protection:
                        type: boolean
                        description: If `true`, then IPsec replay protection will be supported in the
                          Cloudflare-to-customer direction.
                    required:
                      - id
                      - cloudflare_endpoint
                      - interface_address
                      - name
      x-distilled-response-path: result
    delete:
      operationId: deleteIpsecTunnel
      description: "Disables and removes a specific static IPsec Tunnel associated
        with an account. Use `?validate_only=true` as an optional query
        parameter to only run validation without persisting changes.  @example
        ```ts const ipsecTunnel = await client.magicTransit.ipsecTunnels.delete(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: ipsecTunnelId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the response
            body will be presented using the new object format. Defaults to
            false."
          schema:
            type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: boolean
                  deleted_ipsec_tunnel:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      cloudflare_endpoint:
                        type: string
                        description: The IP address assigned to the Cloudflare side of the IPsec tunnel.
                      interface_address:
                        type: string
                        description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                          for each side of the tunnel. Select the subnet from
                          the following private IP space:
                          10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                          192.168.0.0–192.168.255.255."
                      name:
                        type: string
                        description: The name of the IPsec tunnel. The name cannot share a name with
                          other tunnels.
                      allow_null_cipher:
                        type: boolean
                        description: When `true`, the tunnel can use a null-cipher (`ENCR_NULL`) in the
                          ESP tunnel (Phase 2).
                      automatic_return_routing:
                        type: boolean
                        description: True if automatic stateful return routing should be enabled for a
                          tunnel, false otherwise.
                      bgp:
                        type: object
                        properties:
                          customer_asn:
                            type: number
                            description: ASN used on the customer end of the BGP session
                          extra_prefixes:
                            type: array
                            items:
                              type: string
                            description: Prefixes in this list will be advertised to the customer device, in
                              addition to the routes in the Magic routing table.
                          md5_key:
                            type: string
                            description: "MD5 key to use for session authentication.  Note that _this is not
                              a security measure_. MD5 is not a valid security
                              mechanism, and the key is not treated as a secret
                              value. This is _only_ supported for preventing
                              misconfiguration, not for defending against
                              malicious attacks.  The MD5 key, if set, must be
                              of non-zero length and consist only of the
                              following types of character:  - ASCII
                              alphanumerics: `[a-zA-Z0-9]` - Special characters
                              in the set `'!@#$%^& ()+[]{}<>/.,;:_-~`= \\|`  In
                              other words, MD5 keys may contain any printable
                              ASCII character aside from newline (0x0A),
                              quotation mark (`\"`), vertical tab (0x0B),
                              carriage return (0x0D), tab (0x09), form feed
                              (0x0C), and the question mark (`?`). Requests
                              specifying an MD5 key with one or more of these
                              disallowed characters will be rejected."
                        required:
                          - customer_asn
                      bgp_status:
                        type: object
                        properties:
                          state:
                            type: string
                            enum:
                              - BGP_DOWN
                              - BGP_UP
                              - BGP_ESTABLISHING
                          tcp_established:
                            type: boolean
                          updated_at:
                            type: string
                          bgp_state:
                            type: string
                          cf_speaker_ip:
                            type: string
                          cf_speaker_port:
                            type: number
                          customer_speaker_ip:
                            type: string
                          customer_speaker_port:
                            type: number
                        required:
                          - state
                          - tcp_established
                          - updated_at
                      created_on:
                        type: string
                        description: The date and time the tunnel was created.
                      custom_remote_identities:
                        type: object
                        properties:
                          fqdn_id:
                            type: string
                            description: A custom IKE ID of type FQDN that may be used to identity the IPsec
                              tunnel. The generated IKE IDs can still be used
                              even if this custom value is specified.  Must be
                              of the form `<custom label>.<account
                              ID>.custom.ipsec.cloudflare.com`.  This custom ID
                              does not need to be unique. Two IPsec tunnels may
                              have the same custom fqdn_id. However, if another
                              IPsec tunnel has the same value then the two
                              tunnels cannot have the same cloudflare_endpoint.
                      customer_endpoint:
                        type: string
                        description: The IP address assigned to the customer side of the IPsec tunnel.
                          Not required, but must be set for proactive
                          traceroutes to work.
                      description:
                        type: string
                        description: An optional description forthe IPsec tunnel.
                      health_check:
                        type: object
                        properties:
                          direction:
                            type: string
                            enum:
                              - unidirectional
                              - bidirectional
                            description: The direction of the flow of the healthcheck. Either
                              unidirectional, where the probe comes to you via
                              the tunnel and the result comes back to Cloudflare
                              via the open Internet, or bidirectional where both
                              the probe and result come and go via the tunnel.
                          enabled:
                            type: boolean
                            description: Determines whether to run healthchecks for a tunnel.
                          rate:
                            type: string
                            enum:
                              - low
                              - mid
                              - high
                            description: How frequent the health check is run. The default value is `mid`.
                          target:
                            oneOf:
                              - type: object
                                properties:
                                  effective:
                                    type: string
                                    description: The effective health check target. If 'saved' is empty, then this
                                      field will be populated with the
                                      calculated default value on GET requests.
                                      Ignored in POST, PUT, and PATCH requests.
                                  saved:
                                    type: string
                                    description: The saved health check target. Setting the value to the empty
                                      string indicates that the calculated
                                      default value will be used.
                              - type: string
                            description: The destination address in a request type health check. After the
                              healthcheck is decapsulated at the customer end of
                              the tunnel, the ICMP echo will be forwarded to
                              this address. This field defaults to
                              `customer_gre_endpoint address`. This field is
                              ignored for bidirectional healthchecks as the
                              interface_address (not assigned to the Cloudflare
                              side of the tunnel) is used as the target. Must be
                              in object form if the x-magic-new-hc-target header
                              is set to true and string form if
                              x-magic-new-hc-target is absent or set to false.
                          type:
                            type: string
                            enum:
                              - reply
                              - request
                            description: The type of healthcheck to run, reply or request. The default value
                              is `reply`.
                      interface_address6:
                        type: string
                        description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                          with the address being the first IP of the subnet and
                          not same as the address of virtual_subnet6. Eg if
                          virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                          interface_address6 could be
                          2606:54c1:7:0:a9fe:12d2:1:200/127
                      modified_on:
                        type: string
                        description: The date and time the tunnel was last modified.
                      psk_metadata:
                        type: object
                        properties:
                          last_generated_on:
                            type: string
                            description: The date and time the tunnel was last modified.
                        description: The PSK metadata that includes when the PSK was generated.
                      replay_protection:
                        type: boolean
                        description: If `true`, then IPsec replay protection will be supported in the
                          Cloudflare-to-customer direction.
                    required:
                      - id
                      - cloudflare_endpoint
                      - interface_address
                      - name
      x-distilled-response-path: result
  /accounts/{account_id}/magic/ipsec_tunnels:
    get:
      operationId: listIpsecTunnels
      description: "Lists IPsec tunnels associated with an account.  @example ```ts
        const ipsecTunnels = await client.magicTransit.ipsecTunnels.list({
        account_id: '023e105f4ecef8ad9ca31a8372d0c353', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the response
            body will be presented using the new object format. Defaults to
            false."
          schema:
            type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  ipsec_tunnels:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: Identifier
                        cloudflare_endpoint:
                          type: string
                          description: The IP address assigned to the Cloudflare side of the IPsec tunnel.
                        interface_address:
                          type: string
                          description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                            for each side of the tunnel. Select the subnet from
                            the following private IP space:
                            10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                            192.168.0.0–192.168.255.255."
                        name:
                          type: string
                          description: The name of the IPsec tunnel. The name cannot share a name with
                            other tunnels.
                        allow_null_cipher:
                          type: boolean
                          description: When `true`, the tunnel can use a null-cipher (`ENCR_NULL`) in the
                            ESP tunnel (Phase 2).
                        automatic_return_routing:
                          type: boolean
                          description: True if automatic stateful return routing should be enabled for a
                            tunnel, false otherwise.
                        bgp:
                          type: object
                          properties:
                            customer_asn:
                              type: number
                              description: ASN used on the customer end of the BGP session
                            extra_prefixes:
                              type: array
                              items:
                                type: string
                              description: Prefixes in this list will be advertised to the customer device, in
                                addition to the routes in the Magic routing
                                table.
                            md5_key:
                              type: string
                              description: "MD5 key to use for session authentication.  Note that _this is not
                                a security measure_. MD5 is not a valid security
                                mechanism, and the key is not treated as a
                                secret value. This is _only_ supported for
                                preventing misconfiguration, not for defending
                                against malicious attacks.  The MD5 key, if set,
                                must be of non-zero length and consist only of
                                the following types of character:  - ASCII
                                alphanumerics: `[a-zA-Z0-9]` - Special
                                characters in the set `'!@#$%^&
                                ()+[]{}<>/.,;:_-~`= \\|`  In other words, MD5
                                keys may contain any printable ASCII character
                                aside from newline (0x0A), quotation mark
                                (`\"`), vertical tab (0x0B), carriage return
                                (0x0D), tab (0x09), form feed (0x0C), and the
                                question mark (`?`). Requests specifying an MD5
                                key with one or more of these disallowed
                                characters will be rejected."
                          required:
                            - customer_asn
                        bgp_status:
                          type: object
                          properties:
                            state:
                              type: string
                              enum:
                                - BGP_DOWN
                                - BGP_UP
                                - BGP_ESTABLISHING
                            tcp_established:
                              type: boolean
                            updated_at:
                              type: string
                            bgp_state:
                              type: string
                            cf_speaker_ip:
                              type: string
                            cf_speaker_port:
                              type: number
                            customer_speaker_ip:
                              type: string
                            customer_speaker_port:
                              type: number
                          required:
                            - state
                            - tcp_established
                            - updated_at
                        created_on:
                          type: string
                          description: The date and time the tunnel was created.
                        custom_remote_identities:
                          type: object
                          properties:
                            fqdn_id:
                              type: string
                              description: A custom IKE ID of type FQDN that may be used to identity the IPsec
                                tunnel. The generated IKE IDs can still be used
                                even if this custom value is specified.  Must be
                                of the form `<custom label>.<account
                                ID>.custom.ipsec.cloudflare.com`.  This custom
                                ID does not need to be unique. Two IPsec tunnels
                                may have the same custom fqdn_id. However, if
                                another IPsec tunnel has the same value then the
                                two tunnels cannot have the same
                                cloudflare_endpoint.
                        customer_endpoint:
                          type: string
                          description: The IP address assigned to the customer side of the IPsec tunnel.
                            Not required, but must be set for proactive
                            traceroutes to work.
                        description:
                          type: string
                          description: An optional description forthe IPsec tunnel.
                        health_check:
                          type: object
                          properties:
                            direction:
                              type: string
                              enum:
                                - unidirectional
                                - bidirectional
                              description: The direction of the flow of the healthcheck. Either
                                unidirectional, where the probe comes to you via
                                the tunnel and the result comes back to
                                Cloudflare via the open Internet, or
                                bidirectional where both the probe and result
                                come and go via the tunnel.
                            enabled:
                              type: boolean
                              description: Determines whether to run healthchecks for a tunnel.
                            rate:
                              type: string
                              enum:
                                - low
                                - mid
                                - high
                              description: How frequent the health check is run. The default value is `mid`.
                            target:
                              oneOf:
                                - type: object
                                  properties:
                                    effective:
                                      type: string
                                      description: The effective health check target. If 'saved' is empty, then this
                                        field will be populated with the
                                        calculated default value on GET
                                        requests. Ignored in POST, PUT, and
                                        PATCH requests.
                                    saved:
                                      type: string
                                      description: The saved health check target. Setting the value to the empty
                                        string indicates that the calculated
                                        default value will be used.
                                - type: string
                              description: The destination address in a request type health check. After the
                                healthcheck is decapsulated at the customer end
                                of the tunnel, the ICMP echo will be forwarded
                                to this address. This field defaults to
                                `customer_gre_endpoint address`. This field is
                                ignored for bidirectional healthchecks as the
                                interface_address (not assigned to the
                                Cloudflare side of the tunnel) is used as the
                                target. Must be in object form if the
                                x-magic-new-hc-target header is set to true and
                                string form if x-magic-new-hc-target is absent
                                or set to false.
                            type:
                              type: string
                              enum:
                                - reply
                                - request
                              description: The type of healthcheck to run, reply or request. The default value
                                is `reply`.
                        interface_address6:
                          type: string
                          description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                            with the address being the first IP of the subnet
                            and not same as the address of virtual_subnet6. Eg
                            if virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127
                            , interface_address6 could be
                            2606:54c1:7:0:a9fe:12d2:1:200/127
                        modified_on:
                          type: string
                          description: The date and time the tunnel was last modified.
                        psk_metadata:
                          type: object
                          properties:
                            last_generated_on:
                              type: string
                              description: The date and time the tunnel was last modified.
                          description: The PSK metadata that includes when the PSK was generated.
                        replay_protection:
                          type: boolean
                          description: If `true`, then IPsec replay protection will be supported in the
                            Cloudflare-to-customer direction.
                      required:
                        - id
                        - cloudflare_endpoint
                        - interface_address
                        - name
      x-distilled-response-path: result
    post:
      operationId: createIpsecTunnel
      description: "Creates a new IPsec tunnel associated with an account. Use
        `?validate_only=true` as an optional query parameter to only run
        validation without persisting changes.  @example ```ts const ipsecTunnel
        = await client.magicTransit.ipsecTunnels.create({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', cloudflare_endpoint: '203.0.113.1',
        interface_address: '192.0.2.0/31', name: 'IPsec_1', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the request and
            response bodies will be presented using the new object format.
            Defaults to false."
          schema:
            type: boolean
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                cloudflare_endpoint:
                  type: string
                  description: "Body param: The IP address assigned to the Cloudflare side of the
                    IPsec tunnel."
                interface_address:
                  type: string
                  description: "Body param: A 31-bit prefix (/31 in CIDR notation) supporting two
                    hosts, one for each side of the tunnel. Select the subnet
                    from the following private IP space:
                    10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                    192.168.0.0–192.168.255.255."
                name:
                  type: string
                  description: "Body param: The name of the IPsec tunnel. The name cannot share a
                    name with other tunnels."
                automatic_return_routing:
                  type: boolean
                  description: "Body param: True if automatic stateful return routing should be
                    enabled for a tunnel, false otherwise."
                bgp:
                  type: object
                  properties:
                    customer_asn:
                      type: number
                      description: ASN used on the customer end of the BGP session
                    extra_prefixes:
                      type: array
                      items:
                        type: string
                      description: Prefixes in this list will be advertised to the customer device, in
                        addition to the routes in the Magic routing table.
                    md5_key:
                      type: string
                      description: "MD5 key to use for session authentication.  Note that _this is not
                        a security measure_. MD5 is not a valid security
                        mechanism, and the key is not treated as a secret value.
                        This is _only_ supported for preventing
                        misconfiguration, not for defending against malicious
                        attacks.  The MD5 key, if set, must be of non-zero
                        length and consist only of the following types of
                        character:  - ASCII alphanumerics: `[a-zA-Z0-9]` -
                        Special characters in the set `'!@#$%^&
                        ()+[]{}<>/.,;:_-~`= \\|`  In other words, MD5 keys may
                        contain any printable ASCII character aside from newline
                        (0x0A), quotation mark (`\"`), vertical tab (0x0B),
                        carriage return (0x0D), tab (0x09), form feed (0x0C),
                        and the question mark (`?`). Requests specifying an MD5
                        key with one or more of these disallowed characters will
                        be rejected."
                  required:
                    - customer_asn
                  description: "Body param:"
                custom_remote_identities:
                  type: object
                  properties:
                    fqdn_id:
                      type: string
                      description: A custom IKE ID of type FQDN that may be used to identity the IPsec
                        tunnel. The generated IKE IDs can still be used even if
                        this custom value is specified.  Must be of the form
                        `<custom label>.<account
                        ID>.custom.ipsec.cloudflare.com`.  This custom ID does
                        not need to be unique. Two IPsec tunnels may have the
                        same custom fqdn_id. However, if another IPsec tunnel
                        has the same value then the two tunnels cannot have the
                        same cloudflare_endpoint.
                  description: "Body param:"
                customer_endpoint:
                  type: string
                  description: "Body param: The IP address assigned to the customer side of the
                    IPsec tunnel. Not required, but must be set for proactive
                    traceroutes to work."
                description:
                  type: string
                  description: "Body param: An optional description forthe IPsec tunnel."
                health_check:
                  type: object
                  properties:
                    direction:
                      type: string
                      enum:
                        - unidirectional
                        - bidirectional
                      description: The direction of the flow of the healthcheck. Either
                        unidirectional, where the probe comes to you via the
                        tunnel and the result comes back to Cloudflare via the
                        open Internet, or bidirectional where both the probe and
                        result come and go via the tunnel.
                    enabled:
                      type: boolean
                      description: Determines whether to run healthchecks for a tunnel.
                    rate:
                      type: string
                      enum:
                        - low
                        - mid
                        - high
                      description: How frequent the health check is run. The default value is `mid`.
                    target:
                      oneOf:
                        - type: object
                          properties:
                            saved:
                              type: string
                              description: The saved health check target. Setting the value to the empty
                                string indicates that the calculated default
                                value will be used.
                        - type: string
                      description: The destination address in a request type health check. After the
                        healthcheck is decapsulated at the customer end of the
                        tunnel, the ICMP echo will be forwarded to this address.
                        This field defaults to `customer_gre_endpoint address`.
                        This field is ignored for bidirectional healthchecks as
                        the interface_address (not assigned to the Cloudflare
                        side of the tunnel) is used as the target. Must be in
                        object form if the x-magic-new-hc-target header is set
                        to true and string form if x-magic-new-hc-target is
                        absent or set to false.
                    type:
                      type: string
                      enum:
                        - reply
                        - request
                      description: The type of healthcheck to run, reply or request. The default value
                        is `reply`.
                  description: "Body param:"
                interface_address6:
                  type: string
                  description: "Body param: A 127 bit IPV6 prefix from within the virtual_subnet6
                    prefix space with the address being the first IP of the
                    subnet and not same as the address of virtual_subnet6. Eg if
                    virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                    interface_address6 could be
                    2606:54c1:7:0:a9fe:12d2:1:200/127"
                psk:
                  type: string
                  description: "Body param: A randomly generated or provided string for use in the
                    IPsec tunnel."
                replay_protection:
                  type: boolean
                  description: "Body param: If `true`, then IPsec replay protection will be
                    supported in the Cloudflare-to-customer direction."
              required:
                - cloudflare_endpoint
                - interface_address
                - name
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  cloudflare_endpoint:
                    type: string
                    description: The IP address assigned to the Cloudflare side of the IPsec tunnel.
                  interface_address:
                    type: string
                    description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                      for each side of the tunnel. Select the subnet from the
                      following private IP space: 10.0.0.0–10.255.255.255,
                      172.16.0.0–172.31.255.255, 192.168.0.0–192.168.255.255."
                  name:
                    type: string
                    description: The name of the IPsec tunnel. The name cannot share a name with
                      other tunnels.
                  allow_null_cipher:
                    type: boolean
                    description: When `true`, the tunnel can use a null-cipher (`ENCR_NULL`) in the
                      ESP tunnel (Phase 2).
                  automatic_return_routing:
                    type: boolean
                    description: True if automatic stateful return routing should be enabled for a
                      tunnel, false otherwise.
                  bgp:
                    type: object
                    properties:
                      customer_asn:
                        type: number
                        description: ASN used on the customer end of the BGP session
                      extra_prefixes:
                        type: array
                        items:
                          type: string
                        description: Prefixes in this list will be advertised to the customer device, in
                          addition to the routes in the Magic routing table.
                      md5_key:
                        type: string
                        description: "MD5 key to use for session authentication.  Note that _this is not
                          a security measure_. MD5 is not a valid security
                          mechanism, and the key is not treated as a secret
                          value. This is _only_ supported for preventing
                          misconfiguration, not for defending against malicious
                          attacks.  The MD5 key, if set, must be of non-zero
                          length and consist only of the following types of
                          character:  - ASCII alphanumerics: `[a-zA-Z0-9]` -
                          Special characters in the set `'!@#$%^&
                          ()+[]{}<>/.,;:_-~`= \\|`  In other words, MD5 keys may
                          contain any printable ASCII character aside from
                          newline (0x0A), quotation mark (`\"`), vertical tab
                          (0x0B), carriage return (0x0D), tab (0x09), form feed
                          (0x0C), and the question mark (`?`). Requests
                          specifying an MD5 key with one or more of these
                          disallowed characters will be rejected."
                    required:
                      - customer_asn
                  bgp_status:
                    type: object
                    properties:
                      state:
                        type: string
                        enum:
                          - BGP_DOWN
                          - BGP_UP
                          - BGP_ESTABLISHING
                      tcp_established:
                        type: boolean
                      updated_at:
                        type: string
                      bgp_state:
                        type: string
                      cf_speaker_ip:
                        type: string
                      cf_speaker_port:
                        type: number
                      customer_speaker_ip:
                        type: string
                      customer_speaker_port:
                        type: number
                    required:
                      - state
                      - tcp_established
                      - updated_at
                  created_on:
                    type: string
                    description: The date and time the tunnel was created.
                  custom_remote_identities:
                    type: object
                    properties:
                      fqdn_id:
                        type: string
                        description: A custom IKE ID of type FQDN that may be used to identity the IPsec
                          tunnel. The generated IKE IDs can still be used even
                          if this custom value is specified.  Must be of the
                          form `<custom label>.<account
                          ID>.custom.ipsec.cloudflare.com`.  This custom ID does
                          not need to be unique. Two IPsec tunnels may have the
                          same custom fqdn_id. However, if another IPsec tunnel
                          has the same value then the two tunnels cannot have
                          the same cloudflare_endpoint.
                  customer_endpoint:
                    type: string
                    description: The IP address assigned to the customer side of the IPsec tunnel.
                      Not required, but must be set for proactive traceroutes to
                      work.
                  description:
                    type: string
                    description: An optional description forthe IPsec tunnel.
                  health_check:
                    type: object
                    properties:
                      direction:
                        type: string
                        enum:
                          - unidirectional
                          - bidirectional
                        description: The direction of the flow of the healthcheck. Either
                          unidirectional, where the probe comes to you via the
                          tunnel and the result comes back to Cloudflare via the
                          open Internet, or bidirectional where both the probe
                          and result come and go via the tunnel.
                      enabled:
                        type: boolean
                        description: Determines whether to run healthchecks for a tunnel.
                      rate:
                        type: string
                        enum:
                          - low
                          - mid
                          - high
                        description: How frequent the health check is run. The default value is `mid`.
                      target:
                        oneOf:
                          - type: object
                            properties:
                              effective:
                                type: string
                                description: The effective health check target. If 'saved' is empty, then this
                                  field will be populated with the calculated
                                  default value on GET requests. Ignored in
                                  POST, PUT, and PATCH requests.
                              saved:
                                type: string
                                description: The saved health check target. Setting the value to the empty
                                  string indicates that the calculated default
                                  value will be used.
                          - type: string
                        description: The destination address in a request type health check. After the
                          healthcheck is decapsulated at the customer end of the
                          tunnel, the ICMP echo will be forwarded to this
                          address. This field defaults to `customer_gre_endpoint
                          address`. This field is ignored for bidirectional
                          healthchecks as the interface_address (not assigned to
                          the Cloudflare side of the tunnel) is used as the
                          target. Must be in object form if the
                          x-magic-new-hc-target header is set to true and string
                          form if x-magic-new-hc-target is absent or set to
                          false.
                      type:
                        type: string
                        enum:
                          - reply
                          - request
                        description: The type of healthcheck to run, reply or request. The default value
                          is `reply`.
                  interface_address6:
                    type: string
                    description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                      with the address being the first IP of the subnet and not
                      same as the address of virtual_subnet6. Eg if
                      virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127 ,
                      interface_address6 could be
                      2606:54c1:7:0:a9fe:12d2:1:200/127
                  modified_on:
                    type: string
                    description: The date and time the tunnel was last modified.
                  psk_metadata:
                    type: object
                    properties:
                      last_generated_on:
                        type: string
                        description: The date and time the tunnel was last modified.
                    description: The PSK metadata that includes when the PSK was generated.
                  replay_protection:
                    type: boolean
                    description: If `true`, then IPsec replay protection will be supported in the
                      Cloudflare-to-customer direction.
                required:
                  - id
                  - cloudflare_endpoint
                  - interface_address
                  - name
      x-distilled-response-path: result
    put:
      operationId: bulkPutIpsecTunnels
      description: "Update multiple IPsec tunnels associated with an account. Use
        `?validate_only=true` as an optional query parameter to only run
        validation without persisting changes.  @example ```ts const response =
        await client.magicTransit.ipsecTunnels.bulkUpdate({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', body: {}, }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the request and
            response bodies will be presented using the new object format.
            Defaults to false."
          schema:
            type: boolean
      requestBody:
        required: true
        content:
          application/json:
            schema: {}
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  modified:
                    type: boolean
                  modified_ipsec_tunnels:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: Identifier
                        cloudflare_endpoint:
                          type: string
                          description: The IP address assigned to the Cloudflare side of the IPsec tunnel.
                        interface_address:
                          type: string
                          description: "A 31-bit prefix (/31 in CIDR notation) supporting two hosts, one
                            for each side of the tunnel. Select the subnet from
                            the following private IP space:
                            10.0.0.0–10.255.255.255, 172.16.0.0–172.31.255.255,
                            192.168.0.0–192.168.255.255."
                        name:
                          type: string
                          description: The name of the IPsec tunnel. The name cannot share a name with
                            other tunnels.
                        allow_null_cipher:
                          type: boolean
                          description: When `true`, the tunnel can use a null-cipher (`ENCR_NULL`) in the
                            ESP tunnel (Phase 2).
                        automatic_return_routing:
                          type: boolean
                          description: True if automatic stateful return routing should be enabled for a
                            tunnel, false otherwise.
                        bgp:
                          type: object
                          properties:
                            customer_asn:
                              type: number
                              description: ASN used on the customer end of the BGP session
                            extra_prefixes:
                              type: array
                              items:
                                type: string
                              description: Prefixes in this list will be advertised to the customer device, in
                                addition to the routes in the Magic routing
                                table.
                            md5_key:
                              type: string
                              description: "MD5 key to use for session authentication.  Note that _this is not
                                a security measure_. MD5 is not a valid security
                                mechanism, and the key is not treated as a
                                secret value. This is _only_ supported for
                                preventing misconfiguration, not for defending
                                against malicious attacks.  The MD5 key, if set,
                                must be of non-zero length and consist only of
                                the following types of character:  - ASCII
                                alphanumerics: `[a-zA-Z0-9]` - Special
                                characters in the set `'!@#$%^&
                                ()+[]{}<>/.,;:_-~`= \\|`  In other words, MD5
                                keys may contain any printable ASCII character
                                aside from newline (0x0A), quotation mark
                                (`\"`), vertical tab (0x0B), carriage return
                                (0x0D), tab (0x09), form feed (0x0C), and the
                                question mark (`?`). Requests specifying an MD5
                                key with one or more of these disallowed
                                characters will be rejected."
                          required:
                            - customer_asn
                        bgp_status:
                          type: object
                          properties:
                            state:
                              type: string
                              enum:
                                - BGP_DOWN
                                - BGP_UP
                                - BGP_ESTABLISHING
                            tcp_established:
                              type: boolean
                            updated_at:
                              type: string
                            bgp_state:
                              type: string
                            cf_speaker_ip:
                              type: string
                            cf_speaker_port:
                              type: number
                            customer_speaker_ip:
                              type: string
                            customer_speaker_port:
                              type: number
                          required:
                            - state
                            - tcp_established
                            - updated_at
                        created_on:
                          type: string
                          description: The date and time the tunnel was created.
                        custom_remote_identities:
                          type: object
                          properties:
                            fqdn_id:
                              type: string
                              description: A custom IKE ID of type FQDN that may be used to identity the IPsec
                                tunnel. The generated IKE IDs can still be used
                                even if this custom value is specified.  Must be
                                of the form `<custom label>.<account
                                ID>.custom.ipsec.cloudflare.com`.  This custom
                                ID does not need to be unique. Two IPsec tunnels
                                may have the same custom fqdn_id. However, if
                                another IPsec tunnel has the same value then the
                                two tunnels cannot have the same
                                cloudflare_endpoint.
                        customer_endpoint:
                          type: string
                          description: The IP address assigned to the customer side of the IPsec tunnel.
                            Not required, but must be set for proactive
                            traceroutes to work.
                        description:
                          type: string
                          description: An optional description forthe IPsec tunnel.
                        health_check:
                          type: object
                          properties:
                            direction:
                              type: string
                              enum:
                                - unidirectional
                                - bidirectional
                              description: The direction of the flow of the healthcheck. Either
                                unidirectional, where the probe comes to you via
                                the tunnel and the result comes back to
                                Cloudflare via the open Internet, or
                                bidirectional where both the probe and result
                                come and go via the tunnel.
                            enabled:
                              type: boolean
                              description: Determines whether to run healthchecks for a tunnel.
                            rate:
                              type: string
                              enum:
                                - low
                                - mid
                                - high
                              description: How frequent the health check is run. The default value is `mid`.
                            target:
                              oneOf:
                                - type: object
                                  properties:
                                    effective:
                                      type: string
                                      description: The effective health check target. If 'saved' is empty, then this
                                        field will be populated with the
                                        calculated default value on GET
                                        requests. Ignored in POST, PUT, and
                                        PATCH requests.
                                    saved:
                                      type: string
                                      description: The saved health check target. Setting the value to the empty
                                        string indicates that the calculated
                                        default value will be used.
                                - type: string
                              description: The destination address in a request type health check. After the
                                healthcheck is decapsulated at the customer end
                                of the tunnel, the ICMP echo will be forwarded
                                to this address. This field defaults to
                                `customer_gre_endpoint address`. This field is
                                ignored for bidirectional healthchecks as the
                                interface_address (not assigned to the
                                Cloudflare side of the tunnel) is used as the
                                target. Must be in object form if the
                                x-magic-new-hc-target header is set to true and
                                string form if x-magic-new-hc-target is absent
                                or set to false.
                            type:
                              type: string
                              enum:
                                - reply
                                - request
                              description: The type of healthcheck to run, reply or request. The default value
                                is `reply`.
                        interface_address6:
                          type: string
                          description: A 127 bit IPV6 prefix from within the virtual_subnet6 prefix space
                            with the address being the first IP of the subnet
                            and not same as the address of virtual_subnet6. Eg
                            if virtual_subnet6 is 2606:54c1:7:0:a9fe:12d2::/127
                            , interface_address6 could be
                            2606:54c1:7:0:a9fe:12d2:1:200/127
                        modified_on:
                          type: string
                          description: The date and time the tunnel was last modified.
                        psk_metadata:
                          type: object
                          properties:
                            last_generated_on:
                              type: string
                              description: The date and time the tunnel was last modified.
                          description: The PSK metadata that includes when the PSK was generated.
                        replay_protection:
                          type: boolean
                          description: If `true`, then IPsec replay protection will be supported in the
                            Cloudflare-to-customer direction.
                      required:
                        - id
                        - cloudflare_endpoint
                        - interface_address
                        - name
      x-distilled-response-path: result
  /accounts/{account_id}/pcaps/{pcapId}:
    get:
      operationId: getPcap
      description: "Get information for a PCAP request by id.  @example ```ts const
        pcap = await client.magicTransit.pcaps.get(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: pcapId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    properties:
                      id:
                        type: string
                        description: The ID for the packet capture.
                      filter_v1:
                        type: object
                        properties:
                          destination_address:
                            type: string
                            description: The destination IP address of the packet.
                          destination_port:
                            type: number
                            description: The destination port of the packet.
                          protocol:
                            type: number
                            description: The protocol number of the packet.
                          source_address:
                            type: string
                            description: The source IP address of the packet.
                          source_port:
                            type: number
                            description: The source port of the packet.
                        description: The packet capture filter. When this field is empty, all packets
                          are captured.
                      offset_time:
                        type: string
                        description: The RFC 3339 offset timestamp from which to query backwards for
                          packets. Must be within the last 24h. When this field
                          is empty, defaults to time of request.
                      status:
                        type: string
                        enum:
                          - unknown
                          - success
                          - pending
                          - running
                          - conversion_pending
                          - conversion_running
                          - complete
                          - failed
                        description: The status of the packet capture request.
                      submitted:
                        type: string
                        description: The RFC 3339 timestamp when the packet capture was created.
                      system:
                        type: string
                        enum:
                          - magic-transit
                        description: The system used to collect packet captures.
                      time_limit:
                        type: number
                        description: The packet capture duration in seconds.
                      type:
                        type: string
                        enum:
                          - simple
                          - full
                        description: The type of packet capture. `Simple` captures sampled packets, and
                          `full` captures entire payloads and non-sampled
                          packets.
                  - type: object
                    properties:
                      id:
                        type: string
                        description: The ID for the packet capture.
                      byte_limit:
                        type: number
                        description: The maximum number of bytes to capture. This field only applies to
                          `full` packet captures.
                      colo_name:
                        type: string
                        description: The name of the data center used for the packet capture. This can
                          be a specific colo (ord02) or a multi-colo name (ORD).
                          This field only applies to `full` packet captures.
                      destination_conf:
                        type: string
                        description: The full URI for the bucket. This field only applies to `full`
                          packet captures.
                      error_message:
                        type: string
                        description: An error message that describes why the packet capture failed. This
                          field only applies to `full` packet captures.
                      filter_v1:
                        type: object
                        properties:
                          destination_address:
                            type: string
                            description: The destination IP address of the packet.
                          destination_port:
                            type: number
                            description: The destination port of the packet.
                          protocol:
                            type: number
                            description: The protocol number of the packet.
                          source_address:
                            type: string
                            description: The source IP address of the packet.
                          source_port:
                            type: number
                            description: The source port of the packet.
                        description: The packet capture filter. When this field is empty, all packets
                          are captured.
                      packets_captured:
                        type: number
                        description: The number of packets captured.
                      status:
                        type: string
                        enum:
                          - unknown
                          - success
                          - pending
                          - running
                          - conversion_pending
                          - conversion_running
                          - complete
                          - failed
                        description: The status of the packet capture request.
                      stop_requested:
                        type: string
                        description: The RFC 3339 timestamp when stopping the packet capture was
                          requested. This field only applies to `full` packet
                          captures.
                      submitted:
                        type: string
                        description: The RFC 3339 timestamp when the packet capture was created.
                      system:
                        type: string
                        enum:
                          - magic-transit
                        description: The system used to collect packet captures.
                      time_limit:
                        type: number
                        description: The packet capture duration in seconds.
                      type:
                        type: string
                        enum:
                          - simple
                          - full
                        description: The type of packet capture. `Simple` captures sampled packets, and
                          `full` captures entire payloads and non-sampled
                          packets.
      x-distilled-response-path: result
  /accounts/{account_id}/pcaps:
    get:
      operationId: listPcaps
      description: "Lists all packet capture requests for an account.  @example ```ts
        // Automatically fetches more pages as needed. for await (const
        pcapListResponse of client.magicTransit.pcaps.list( { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    properties:
                      id:
                        type: string
                        description: The ID for the packet capture.
                      filter_v1:
                        type: object
                        properties:
                          destination_address:
                            type: string
                            description: The destination IP address of the packet.
                          destination_port:
                            type: number
                            description: The destination port of the packet.
                          protocol:
                            type: number
                            description: The protocol number of the packet.
                          source_address:
                            type: string
                            description: The source IP address of the packet.
                          source_port:
                            type: number
                            description: The source port of the packet.
                        description: The packet capture filter. When this field is empty, all packets
                          are captured.
                      offset_time:
                        type: string
                        description: The RFC 3339 offset timestamp from which to query backwards for
                          packets. Must be within the last 24h. When this field
                          is empty, defaults to time of request.
                      status:
                        type: string
                        enum:
                          - unknown
                          - success
                          - pending
                          - running
                          - conversion_pending
                          - conversion_running
                          - complete
                          - failed
                        description: The status of the packet capture request.
                      submitted:
                        type: string
                        description: The RFC 3339 timestamp when the packet capture was created.
                      system:
                        type: string
                        enum:
                          - magic-transit
                        description: The system used to collect packet captures.
                      time_limit:
                        type: number
                        description: The packet capture duration in seconds.
                      type:
                        type: string
                        enum:
                          - simple
                          - full
                        description: The type of packet capture. `Simple` captures sampled packets, and
                          `full` captures entire payloads and non-sampled
                          packets.
                  - type: object
                    properties:
                      id:
                        type: string
                        description: The ID for the packet capture.
                      byte_limit:
                        type: number
                        description: The maximum number of bytes to capture. This field only applies to
                          `full` packet captures.
                      colo_name:
                        type: string
                        description: The name of the data center used for the packet capture. This can
                          be a specific colo (ord02) or a multi-colo name (ORD).
                          This field only applies to `full` packet captures.
                      destination_conf:
                        type: string
                        description: The full URI for the bucket. This field only applies to `full`
                          packet captures.
                      error_message:
                        type: string
                        description: An error message that describes why the packet capture failed. This
                          field only applies to `full` packet captures.
                      filter_v1:
                        type: object
                        properties:
                          destination_address:
                            type: string
                            description: The destination IP address of the packet.
                          destination_port:
                            type: number
                            description: The destination port of the packet.
                          protocol:
                            type: number
                            description: The protocol number of the packet.
                          source_address:
                            type: string
                            description: The source IP address of the packet.
                          source_port:
                            type: number
                            description: The source port of the packet.
                        description: The packet capture filter. When this field is empty, all packets
                          are captured.
                      packets_captured:
                        type: number
                        description: The number of packets captured.
                      status:
                        type: string
                        enum:
                          - unknown
                          - success
                          - pending
                          - running
                          - conversion_pending
                          - conversion_running
                          - complete
                          - failed
                        description: The status of the packet capture request.
                      stop_requested:
                        type: string
                        description: The RFC 3339 timestamp when stopping the packet capture was
                          requested. This field only applies to `full` packet
                          captures.
                      submitted:
                        type: string
                        description: The RFC 3339 timestamp when the packet capture was created.
                      system:
                        type: string
                        enum:
                          - magic-transit
                        description: The system used to collect packet captures.
                      time_limit:
                        type: number
                        description: The packet capture duration in seconds.
                      type:
                        type: string
                        enum:
                          - simple
                          - full
                        description: The type of packet capture. `Simple` captures sampled packets, and
                          `full` captures entire payloads and non-sampled
                          packets.
      x-distilled-pagination-class: PCAPListResponsesSinglePage
    post:
      operationId: createPcap
      description: "Create new PCAP request for account.  @example ```ts const pcap =
        await client.magicTransit.pcaps.create({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', packet_limit: 10000, system:
        'magic-transit', time_limit: 300, type: 'simple', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                packet_limit:
                  type: number
                  description: "Body param: The limit of packets contained in a packet capture."
                system:
                  type: string
                  enum:
                    - magic-transit
                  description: "Body param: The system used to collect packet captures."
                time_limit:
                  type: number
                  description: "Body param: The packet capture duration in seconds."
                type:
                  type: string
                  enum:
                    - simple
                    - full
                  description: "Body param: The type of packet capture. `Simple` captures sampled
                    packets, and `full` captures entire payloads and non-sampled
                    packets."
                filter_v1:
                  type: object
                  properties:
                    destination_address:
                      type: string
                      description: The destination IP address of the packet.
                    destination_port:
                      type: number
                      description: The destination port of the packet.
                    protocol:
                      type: number
                      description: The protocol number of the packet.
                    source_address:
                      type: string
                      description: The source IP address of the packet.
                    source_port:
                      type: number
                      description: The source port of the packet.
                  description: "Body param: The packet capture filter. When this field is empty,
                    all packets are captured."
                offset_time:
                  type: string
                  description: "Body param: The RFC 3339 offset timestamp from which to query
                    backwards for packets. Must be within the last 24h. When
                    this field is empty, defaults to time of request."
              required:
                - packet_limit
                - system
                - time_limit
                - type
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    properties:
                      id:
                        type: string
                        description: The ID for the packet capture.
                      filter_v1:
                        type: object
                        properties:
                          destination_address:
                            type: string
                            description: The destination IP address of the packet.
                          destination_port:
                            type: number
                            description: The destination port of the packet.
                          protocol:
                            type: number
                            description: The protocol number of the packet.
                          source_address:
                            type: string
                            description: The source IP address of the packet.
                          source_port:
                            type: number
                            description: The source port of the packet.
                        description: The packet capture filter. When this field is empty, all packets
                          are captured.
                      offset_time:
                        type: string
                        description: The RFC 3339 offset timestamp from which to query backwards for
                          packets. Must be within the last 24h. When this field
                          is empty, defaults to time of request.
                      status:
                        type: string
                        enum:
                          - unknown
                          - success
                          - pending
                          - running
                          - conversion_pending
                          - conversion_running
                          - complete
                          - failed
                        description: The status of the packet capture request.
                      submitted:
                        type: string
                        description: The RFC 3339 timestamp when the packet capture was created.
                      system:
                        type: string
                        enum:
                          - magic-transit
                        description: The system used to collect packet captures.
                      time_limit:
                        type: number
                        description: The packet capture duration in seconds.
                      type:
                        type: string
                        enum:
                          - simple
                          - full
                        description: The type of packet capture. `Simple` captures sampled packets, and
                          `full` captures entire payloads and non-sampled
                          packets.
                  - type: object
                    properties:
                      id:
                        type: string
                        description: The ID for the packet capture.
                      byte_limit:
                        type: number
                        description: The maximum number of bytes to capture. This field only applies to
                          `full` packet captures.
                      colo_name:
                        type: string
                        description: The name of the data center used for the packet capture. This can
                          be a specific colo (ord02) or a multi-colo name (ORD).
                          This field only applies to `full` packet captures.
                      destination_conf:
                        type: string
                        description: The full URI for the bucket. This field only applies to `full`
                          packet captures.
                      error_message:
                        type: string
                        description: An error message that describes why the packet capture failed. This
                          field only applies to `full` packet captures.
                      filter_v1:
                        type: object
                        properties:
                          destination_address:
                            type: string
                            description: The destination IP address of the packet.
                          destination_port:
                            type: number
                            description: The destination port of the packet.
                          protocol:
                            type: number
                            description: The protocol number of the packet.
                          source_address:
                            type: string
                            description: The source IP address of the packet.
                          source_port:
                            type: number
                            description: The source port of the packet.
                        description: The packet capture filter. When this field is empty, all packets
                          are captured.
                      packets_captured:
                        type: number
                        description: The number of packets captured.
                      status:
                        type: string
                        enum:
                          - unknown
                          - success
                          - pending
                          - running
                          - conversion_pending
                          - conversion_running
                          - complete
                          - failed
                        description: The status of the packet capture request.
                      stop_requested:
                        type: string
                        description: The RFC 3339 timestamp when stopping the packet capture was
                          requested. This field only applies to `full` packet
                          captures.
                      submitted:
                        type: string
                        description: The RFC 3339 timestamp when the packet capture was created.
                      system:
                        type: string
                        enum:
                          - magic-transit
                        description: The system used to collect packet captures.
                      time_limit:
                        type: number
                        description: The packet capture duration in seconds.
                      type:
                        type: string
                        enum:
                          - simple
                          - full
                        description: The type of packet capture. `Simple` captures sampled packets, and
                          `full` captures entire payloads and non-sampled
                          packets.
      x-distilled-response-path: result
  /accounts/{account_id}/pcaps/{pcapId}/stop:
    put:
      operationId: stopPcap
      description: "Stop full PCAP.  @example ```ts await
        client.magicTransit.pcaps.stop( '023e105f4ecef8ad9ca31a8372d0c353', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: pcapId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
  /accounts/{account_id}/pcaps/{pcapId}/download:
    get:
      operationId: getPcapDownload
      description: "Download PCAP information into a file. Response is a binary PCAP
        file.  @example ```ts const download = await
        client.magicTransit.pcaps.download.get(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, );  const content = await
        download.blob(); console.log(content); ```"
      parameters:
        - name: pcapId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
  /accounts/{account_id}/pcaps/ownership:
    get:
      operationId: getPcapOwnership
      description: "List all buckets configured for use with PCAPs API.  @example
        ```ts // Automatically fetches more pages as needed. for await (const
        ownership of client.magicTransit.pcaps.ownership.get( { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: The bucket ID associated with the packet captures API.
                  destination_conf:
                    type: string
                    description: The full URI for the bucket. This field only applies to `full`
                      packet captures.
                  filename:
                    type: string
                    description: The ownership challenge filename stored in the bucket.
                  status:
                    type: string
                    enum:
                      - pending
                      - success
                      - failed
                    description: The status of the ownership challenge. Can be pending, success or
                      failed.
                  submitted:
                    type: string
                    description: The RFC 3339 timestamp when the bucket was added to packet captures
                      API.
                  validated:
                    type: string
                    description: The RFC 3339 timestamp when the bucket was validated.
                required:
                  - id
                  - destination_conf
                  - filename
                  - status
                  - submitted
      x-distilled-pagination-class: OwnershipsSinglePage
    post:
      operationId: createPcapOwnership
      description: "Adds an AWS or GCP bucket to use with full packet
        captures.  @example ```ts const ownership = await
        client.magicTransit.pcaps.ownership.create({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', destination_conf:
        's3://pcaps-bucket?region=us-east-1', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                destination_conf:
                  type: string
                  description: "Body param: The full URI for the bucket. This field only applies
                    to `full` packet captures."
              required:
                - destination_conf
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: The bucket ID associated with the packet captures API.
                  destination_conf:
                    type: string
                    description: The full URI for the bucket. This field only applies to `full`
                      packet captures.
                  filename:
                    type: string
                    description: The ownership challenge filename stored in the bucket.
                  status:
                    type: string
                    enum:
                      - pending
                      - success
                      - failed
                    description: The status of the ownership challenge. Can be pending, success or
                      failed.
                  submitted:
                    type: string
                    description: The RFC 3339 timestamp when the bucket was added to packet captures
                      API.
                  validated:
                    type: string
                    description: The RFC 3339 timestamp when the bucket was validated.
                required:
                  - id
                  - destination_conf
                  - filename
                  - status
                  - submitted
      x-distilled-response-path: result
  /accounts/{account_id}/pcaps/ownership/{ownershipId}:
    delete:
      operationId: deletePcapOwnership
      description: "Deletes buckets added to the packet captures API.  @example ```ts
        await client.magicTransit.pcaps.ownership.delete(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: ownershipId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
  /accounts/{account_id}/pcaps/ownership/validate:
    post:
      operationId: validatePcapOwnership
      description: "Validates buckets added to the packet captures API.  @example
        ```ts const ownership = await
        client.magicTransit.pcaps.ownership.validate({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', destination_conf:
        's3://pcaps-bucket?region=us-east-1', ownership_challenge:
        'ownership-challenge-9883874ecac311ec8475433579a6bf5f.txt', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                destination_conf:
                  type: string
                  description: "Body param: The full URI for the bucket. This field only applies
                    to `full` packet captures."
                ownership_challenge:
                  type: string
                  description: "Body param: The ownership challenge filename stored in the
                    bucket."
              required:
                - destination_conf
                - ownership_challenge
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: The bucket ID associated with the packet captures API.
                  destination_conf:
                    type: string
                    description: The full URI for the bucket. This field only applies to `full`
                      packet captures.
                  filename:
                    type: string
                    description: The ownership challenge filename stored in the bucket.
                  status:
                    type: string
                    enum:
                      - pending
                      - success
                      - failed
                    description: The status of the ownership challenge. Can be pending, success or
                      failed.
                  submitted:
                    type: string
                    description: The RFC 3339 timestamp when the bucket was added to packet captures
                      API.
                  validated:
                    type: string
                    description: The RFC 3339 timestamp when the bucket was validated.
                required:
                  - id
                  - destination_conf
                  - filename
                  - status
                  - submitted
      x-distilled-response-path: result
  /accounts/{account_id}/magic/routes:
    put:
      operationId: bulkPutRoutes
      description: "Update multiple Magic static routes. Use `?validate_only=true` as
        an optional query parameter to run validation only without persisting
        changes. Only fields for a route that need to be changed need be
        provided.  @example ```ts const response = await
        client.magicTransit.routes.bulkUpdate({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', routes: [ { id:
        '023e105f4ecef8ad9ca31a8372d0c353', nexthop: '203.0.113.1', prefix:
        '192.0.2.0/24', priority: 0, }, ], }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                routes:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      nexthop:
                        type: string
                        description: The next-hop IP Address for the static route.
                      prefix:
                        type: string
                        description: IP Prefix in Classless Inter-Domain Routing format.
                      priority:
                        type: number
                        description: Priority of the static route.
                      description:
                        type: string
                        description: An optional human provided description of the static route.
                      scope:
                        type: object
                        properties:
                          colo_names:
                            type: array
                            items:
                              type: string
                            description: List of colo names for the ECMP scope.
                          colo_regions:
                            type: array
                            items:
                              type: string
                            description: List of colo regions for the ECMP scope.
                        description: Used only for ECMP routes.
                      weight:
                        type: number
                        description: Optional weight of the ECMP scope - if provided.
                    required:
                      - id
                      - nexthop
                      - prefix
                      - priority
                  description: "Body param:"
              required:
                - routes
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  modified:
                    type: boolean
                  modified_routes:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: Identifier
                        nexthop:
                          type: string
                          description: The next-hop IP Address for the static route.
                        prefix:
                          type: string
                          description: IP Prefix in Classless Inter-Domain Routing format.
                        priority:
                          type: number
                          description: Priority of the static route.
                        created_on:
                          type: string
                          description: When the route was created.
                        description:
                          type: string
                          description: An optional human provided description of the static route.
                        modified_on:
                          type: string
                          description: When the route was last modified.
                        scope:
                          type: object
                          properties:
                            colo_names:
                              type: array
                              items:
                                type: string
                              description: List of colo names for the ECMP scope.
                            colo_regions:
                              type: array
                              items:
                                type: string
                              description: List of colo regions for the ECMP scope.
                          description: Used only for ECMP routes.
                        weight:
                          type: number
                          description: Optional weight of the ECMP scope - if provided.
                      required:
                        - id
                        - nexthop
                        - prefix
                        - priority
      x-distilled-response-path: result
    get:
      operationId: listRoutes
      description: "List all Magic static routes.  @example ```ts const routes = await
        client.magicTransit.routes.list({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  routes:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: Identifier
                        nexthop:
                          type: string
                          description: The next-hop IP Address for the static route.
                        prefix:
                          type: string
                          description: IP Prefix in Classless Inter-Domain Routing format.
                        priority:
                          type: number
                          description: Priority of the static route.
                        created_on:
                          type: string
                          description: When the route was created.
                        description:
                          type: string
                          description: An optional human provided description of the static route.
                        modified_on:
                          type: string
                          description: When the route was last modified.
                        scope:
                          type: object
                          properties:
                            colo_names:
                              type: array
                              items:
                                type: string
                              description: List of colo names for the ECMP scope.
                            colo_regions:
                              type: array
                              items:
                                type: string
                              description: List of colo regions for the ECMP scope.
                          description: Used only for ECMP routes.
                        weight:
                          type: number
                          description: Optional weight of the ECMP scope - if provided.
                      required:
                        - id
                        - nexthop
                        - prefix
                        - priority
      x-distilled-response-path: result
    post:
      operationId: createRoute
      description: "Creates a new Magic static route. Use `?validate_only=true` as an
        optional query parameter to run validation only without persisting
        changes.  @example ```ts const route = await
        client.magicTransit.routes.create({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', nexthop: '203.0.113.1', prefix:
        '192.0.2.0/24', priority: 0, }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                nexthop:
                  type: string
                  description: "Body param: The next-hop IP Address for the static route."
                prefix:
                  type: string
                  description: "Body param: IP Prefix in Classless Inter-Domain Routing format."
                priority:
                  type: number
                  description: "Body param: Priority of the static route."
                description:
                  type: string
                  description: "Body param: An optional human provided description of the static
                    route."
                scope:
                  type: object
                  properties:
                    colo_names:
                      type: array
                      items:
                        type: string
                      description: List of colo names for the ECMP scope.
                    colo_regions:
                      type: array
                      items:
                        type: string
                      description: List of colo regions for the ECMP scope.
                  description: "Body param: Used only for ECMP routes."
                weight:
                  type: number
                  description: "Body param: Optional weight of the ECMP scope - if provided."
              required:
                - nexthop
                - prefix
                - priority
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  nexthop:
                    type: string
                    description: The next-hop IP Address for the static route.
                  prefix:
                    type: string
                    description: IP Prefix in Classless Inter-Domain Routing format.
                  priority:
                    type: number
                    description: Priority of the static route.
                  created_on:
                    type: string
                    description: When the route was created.
                  description:
                    type: string
                    description: An optional human provided description of the static route.
                  modified_on:
                    type: string
                    description: When the route was last modified.
                  scope:
                    type: object
                    properties:
                      colo_names:
                        type: array
                        items:
                          type: string
                        description: List of colo names for the ECMP scope.
                      colo_regions:
                        type: array
                        items:
                          type: string
                        description: List of colo regions for the ECMP scope.
                    description: Used only for ECMP routes.
                  weight:
                    type: number
                    description: Optional weight of the ECMP scope - if provided.
                required:
                  - id
                  - nexthop
                  - prefix
                  - priority
      x-distilled-response-path: result
    delete:
      operationId: emptyRoute
      description: "Delete multiple Magic static routes.  @example ```ts const
        response = await client.magicTransit.routes.empty({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: boolean
                  deleted_routes:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: Identifier
                        nexthop:
                          type: string
                          description: The next-hop IP Address for the static route.
                        prefix:
                          type: string
                          description: IP Prefix in Classless Inter-Domain Routing format.
                        priority:
                          type: number
                          description: Priority of the static route.
                        created_on:
                          type: string
                          description: When the route was created.
                        description:
                          type: string
                          description: An optional human provided description of the static route.
                        modified_on:
                          type: string
                          description: When the route was last modified.
                        scope:
                          type: object
                          properties:
                            colo_names:
                              type: array
                              items:
                                type: string
                              description: List of colo names for the ECMP scope.
                            colo_regions:
                              type: array
                              items:
                                type: string
                              description: List of colo regions for the ECMP scope.
                          description: Used only for ECMP routes.
                        weight:
                          type: number
                          description: Optional weight of the ECMP scope - if provided.
                      required:
                        - id
                        - nexthop
                        - prefix
                        - priority
      x-distilled-response-path: result
  /accounts/{account_id}/magic/routes/{routeId}:
    get:
      operationId: getRoute
      description: "Get a specific Magic static route.  @example ```ts const route =
        await client.magicTransit.routes.get(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: routeId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  route:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      nexthop:
                        type: string
                        description: The next-hop IP Address for the static route.
                      prefix:
                        type: string
                        description: IP Prefix in Classless Inter-Domain Routing format.
                      priority:
                        type: number
                        description: Priority of the static route.
                      created_on:
                        type: string
                        description: When the route was created.
                      description:
                        type: string
                        description: An optional human provided description of the static route.
                      modified_on:
                        type: string
                        description: When the route was last modified.
                      scope:
                        type: object
                        properties:
                          colo_names:
                            type: array
                            items:
                              type: string
                            description: List of colo names for the ECMP scope.
                          colo_regions:
                            type: array
                            items:
                              type: string
                            description: List of colo regions for the ECMP scope.
                        description: Used only for ECMP routes.
                      weight:
                        type: number
                        description: Optional weight of the ECMP scope - if provided.
                    required:
                      - id
                      - nexthop
                      - prefix
                      - priority
      x-distilled-response-path: result
    put:
      operationId: updateRoute
      description: "Update a specific Magic static route. Use `?validate_only=true` as
        an optional query parameter to run validation only without persisting
        changes.  @example ```ts const route = await
        client.magicTransit.routes.update( '023e105f4ecef8ad9ca31a8372d0c353', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353', nexthop: '203.0.113.1',
        prefix: '192.0.2.0/24', priority: 0, }, ); ```"
      parameters:
        - name: routeId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                nexthop:
                  type: string
                  description: "Body param: The next-hop IP Address for the static route."
                prefix:
                  type: string
                  description: "Body param: IP Prefix in Classless Inter-Domain Routing format."
                priority:
                  type: number
                  description: "Body param: Priority of the static route."
                description:
                  type: string
                  description: "Body param: An optional human provided description of the static
                    route."
                scope:
                  type: object
                  properties:
                    colo_names:
                      type: array
                      items:
                        type: string
                      description: List of colo names for the ECMP scope.
                    colo_regions:
                      type: array
                      items:
                        type: string
                      description: List of colo regions for the ECMP scope.
                  description: "Body param: Used only for ECMP routes."
                weight:
                  type: number
                  description: "Body param: Optional weight of the ECMP scope - if provided."
              required:
                - nexthop
                - prefix
                - priority
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  modified:
                    type: boolean
                  modified_route:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      nexthop:
                        type: string
                        description: The next-hop IP Address for the static route.
                      prefix:
                        type: string
                        description: IP Prefix in Classless Inter-Domain Routing format.
                      priority:
                        type: number
                        description: Priority of the static route.
                      created_on:
                        type: string
                        description: When the route was created.
                      description:
                        type: string
                        description: An optional human provided description of the static route.
                      modified_on:
                        type: string
                        description: When the route was last modified.
                      scope:
                        type: object
                        properties:
                          colo_names:
                            type: array
                            items:
                              type: string
                            description: List of colo names for the ECMP scope.
                          colo_regions:
                            type: array
                            items:
                              type: string
                            description: List of colo regions for the ECMP scope.
                        description: Used only for ECMP routes.
                      weight:
                        type: number
                        description: Optional weight of the ECMP scope - if provided.
                    required:
                      - id
                      - nexthop
                      - prefix
                      - priority
      x-distilled-response-path: result
    delete:
      operationId: deleteRoute
      description: "Disable and remove a specific Magic static route.  @example ```ts
        const route = await client.magicTransit.routes.delete(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: routeId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: boolean
                  deleted_route:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Identifier
                      nexthop:
                        type: string
                        description: The next-hop IP Address for the static route.
                      prefix:
                        type: string
                        description: IP Prefix in Classless Inter-Domain Routing format.
                      priority:
                        type: number
                        description: Priority of the static route.
                      created_on:
                        type: string
                        description: When the route was created.
                      description:
                        type: string
                        description: An optional human provided description of the static route.
                      modified_on:
                        type: string
                        description: When the route was last modified.
                      scope:
                        type: object
                        properties:
                          colo_names:
                            type: array
                            items:
                              type: string
                            description: List of colo names for the ECMP scope.
                          colo_regions:
                            type: array
                            items:
                              type: string
                            description: List of colo regions for the ECMP scope.
                        description: Used only for ECMP routes.
                      weight:
                        type: number
                        description: Optional weight of the ECMP scope - if provided.
                    required:
                      - id
                      - nexthop
                      - prefix
                      - priority
      x-distilled-response-path: result
  /accounts/{account_id}/magic/sites/{siteId}:
    get:
      operationId: getSite
      description: "Get a specific Site.  @example ```ts const site = await
        client.magicTransit.sites.get( '023e105f4ecef8ad9ca31a8372d0c353', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: x-magic-new-hc-target
          in: header
          required: false
          description: "Header param: If true, the health check target in the response
            body will be presented using the new object format. Defaults to
            false."
          schema:
            type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  connector_id:
                    type: string
                    description: Magic Connector identifier tag.
                  description:
                    type: string
                  ha_mode:
                    type: boolean
                    description: Site high availability mode. If set to true, the site can have two
                      connectors and runs in high availability mode.
                  location:
                    type: object
                    properties:
                      lat:
                        type: string
                        description: Latitude
                      lon:
                        type: string
                        description: Longitude
                    description: Location of site in latitude and longitude.
                  name:
                    type: string
                    description: The name of the site.
                  secondary_connector_id:
                    type: string
                    description: Magic Connector identifier tag. Used when high availability mode is
                      on.
      x-distilled-response-path: result
    put:
      operationId: updateSite
      description: "Update a specific Site.  @example ```ts const site = await
        client.magicTransit.sites.update( '023e105f4ecef8ad9ca31a8372d0c353', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                connector_id:
                  type: string
                  description: "Body param: Magic Connector identifier tag."
                description:
                  type: string
                  description: "Body param:"
                location:
                  type: object
                  properties:
                    lat:
                      type: string
                      description: Latitude
                    lon:
                      type: string
                      description: Longitude
                  description: "Body param: Location of site in latitude and longitude."
                name:
                  type: string
                  description: "Body param: The name of the site."
                secondary_connector_id:
                  type: string
                  description: "Body param: Magic Connector identifier tag. Used when high
                    availability mode is on."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  connector_id:
                    type: string
                    description: Magic Connector identifier tag.
                  description:
                    type: string
                  ha_mode:
                    type: boolean
                    description: Site high availability mode. If set to true, the site can have two
                      connectors and runs in high availability mode.
                  location:
                    type: object
                    properties:
                      lat:
                        type: string
                        description: Latitude
                      lon:
                        type: string
                        description: Longitude
                    description: Location of site in latitude and longitude.
                  name:
                    type: string
                    description: The name of the site.
                  secondary_connector_id:
                    type: string
                    description: Magic Connector identifier tag. Used when high availability mode is
                      on.
      x-distilled-response-path: result
    patch:
      operationId: patchSite
      description: "Patch a specific Site.  @example ```ts const site = await
        client.magicTransit.sites.edit( '023e105f4ecef8ad9ca31a8372d0c353', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                connector_id:
                  type: string
                  description: "Body param: Magic Connector identifier tag."
                description:
                  type: string
                  description: "Body param:"
                location:
                  type: object
                  properties:
                    lat:
                      type: string
                      description: Latitude
                    lon:
                      type: string
                      description: Longitude
                  description: "Body param: Location of site in latitude and longitude."
                name:
                  type: string
                  description: "Body param: The name of the site."
                secondary_connector_id:
                  type: string
                  description: "Body param: Magic Connector identifier tag. Used when high
                    availability mode is on."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  connector_id:
                    type: string
                    description: Magic Connector identifier tag.
                  description:
                    type: string
                  ha_mode:
                    type: boolean
                    description: Site high availability mode. If set to true, the site can have two
                      connectors and runs in high availability mode.
                  location:
                    type: object
                    properties:
                      lat:
                        type: string
                        description: Latitude
                      lon:
                        type: string
                        description: Longitude
                    description: Location of site in latitude and longitude.
                  name:
                    type: string
                    description: The name of the site.
                  secondary_connector_id:
                    type: string
                    description: Magic Connector identifier tag. Used when high availability mode is
                      on.
      x-distilled-response-path: result
    delete:
      operationId: deleteSite
      description: "Remove a specific Site.  @example ```ts const site = await
        client.magicTransit.sites.delete( '023e105f4ecef8ad9ca31a8372d0c353', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  connector_id:
                    type: string
                    description: Magic Connector identifier tag.
                  description:
                    type: string
                  ha_mode:
                    type: boolean
                    description: Site high availability mode. If set to true, the site can have two
                      connectors and runs in high availability mode.
                  location:
                    type: object
                    properties:
                      lat:
                        type: string
                        description: Latitude
                      lon:
                        type: string
                        description: Longitude
                    description: Location of site in latitude and longitude.
                  name:
                    type: string
                    description: The name of the site.
                  secondary_connector_id:
                    type: string
                    description: Magic Connector identifier tag. Used when high availability mode is
                      on.
      x-distilled-response-path: result
  /accounts/{account_id}/magic/sites:
    get:
      operationId: listSites
      description: "Lists Sites associated with an account. Use connectorid query
        param to return sites where connectorid matches either site.ConnectorID
        or site.SecondaryConnectorID.  @example ```ts // Automatically fetches
        more pages as needed. for await (const site of
        client.magicTransit.sites.list({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', })) { // ... } ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
        - name: connectorid
          in: query
          required: false
          description: "Query param: Identifier"
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  connector_id:
                    type: string
                    description: Magic Connector identifier tag.
                  description:
                    type: string
                  ha_mode:
                    type: boolean
                    description: Site high availability mode. If set to true, the site can have two
                      connectors and runs in high availability mode.
                  location:
                    type: object
                    properties:
                      lat:
                        type: string
                        description: Latitude
                      lon:
                        type: string
                        description: Longitude
                    description: Location of site in latitude and longitude.
                  name:
                    type: string
                    description: The name of the site.
                  secondary_connector_id:
                    type: string
                    description: Magic Connector identifier tag. Used when high availability mode is
                      on.
      x-distilled-pagination-class: SitesSinglePage
    post:
      operationId: createSite
      description: "Creates a new Site  @example ```ts const site = await
        client.magicTransit.sites.create({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', name: 'site_1', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: "Body param: The name of the site."
                connector_id:
                  type: string
                  description: "Body param: Magic Connector identifier tag."
                description:
                  type: string
                  description: "Body param:"
                ha_mode:
                  type: boolean
                  description: "Body param: Site high availability mode. If set to true, the site
                    can have two connectors and runs in high availability mode."
                location:
                  type: object
                  properties:
                    lat:
                      type: string
                      description: Latitude
                    lon:
                      type: string
                      description: Longitude
                  description: "Body param: Location of site in latitude and longitude."
                secondary_connector_id:
                  type: string
                  description: "Body param: Magic Connector identifier tag. Used when high
                    availability mode is on."
              required:
                - name
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  connector_id:
                    type: string
                    description: Magic Connector identifier tag.
                  description:
                    type: string
                  ha_mode:
                    type: boolean
                    description: Site high availability mode. If set to true, the site can have two
                      connectors and runs in high availability mode.
                  location:
                    type: object
                    properties:
                      lat:
                        type: string
                        description: Latitude
                      lon:
                        type: string
                        description: Longitude
                    description: Location of site in latitude and longitude.
                  name:
                    type: string
                    description: The name of the site.
                  secondary_connector_id:
                    type: string
                    description: Magic Connector identifier tag. Used when high availability mode is
                      on.
      x-distilled-response-path: result
  /accounts/{account_id}/magic/sites/{siteId}/acls/{aclId}:
    get:
      operationId: getSiteAcl
      description: "Get a specific Site ACL.  @example ```ts const acl = await
        client.magicTransit.sites.acls.get( '023e105f4ecef8ad9ca31a8372d0c353',
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: aclId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  description:
                    type: string
                    description: Description for the ACL.
                  forward_locally:
                    type: boolean
                    description: The desired forwarding action for this ACL policy. If set to
                      "false", the policy will forward traffic to Cloudflare. If
                      set to "true", the policy will forward traffic locally on
                      the Magic Connector. If not included in request, will
                      default to false.
                  lan_1:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  lan_2:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  name:
                    type: string
                    description: The name of the ACL.
                  protocols:
                    type: array
                    items:
                      type: string
                      enum:
                        - tcp
                        - udp
                        - icmp
                  unidirectional:
                    type: boolean
                    description: The desired traffic direction for this ACL policy. If set to
                      "false", the policy will allow bidirectional traffic. If
                      set to "true", the policy will only allow traffic in one
                      direction. If not included in request, will default to
                      false.
      x-distilled-response-path: result
    put:
      operationId: updateSiteAcl
      description: "Update a specific Site ACL.  @example ```ts const acl = await
        client.magicTransit.sites.acls.update(
        '023e105f4ecef8ad9ca31a8372d0c353', '023e105f4ecef8ad9ca31a8372d0c353',
        { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: aclId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                description:
                  type: string
                  description: "Body param: Description for the ACL."
                forward_locally:
                  type: boolean
                  description: 'Body param: The desired forwarding action for this ACL policy. If
                    set to "false", the policy will forward traffic to
                    Cloudflare. If set to "true", the policy will forward
                    traffic locally on the Magic Connector. If not included in
                    request, will default to false.'
                lan_1:
                  type: object
                  properties:
                    lan_id:
                      type: string
                      description: The identifier for the LAN you want to create an ACL policy with.
                    lan_name:
                      type: string
                      description: The name of the LAN based on the provided lan_id.
                    port_ranges:
                      type: array
                      items:
                        type: string
                      description: Array of port ranges on the provided LAN that will be included in
                        the ACL. If no ports or port rangess are provided,
                        communication on any port on this LAN is allowed.
                    ports:
                      type: array
                      items:
                        type: number
                      description: Array of ports on the provided LAN that will be included in the
                        ACL. If no ports or port ranges are provided,
                        communication on any port on this LAN is allowed.
                    subnets:
                      type: array
                      items:
                        type: string
                      description: Array of subnet IPs within the LAN that will be included in the
                        ACL. If no subnets are provided, communication on any
                        subnets on this LAN are allowed.
                  required:
                    - lan_id
                  description: "Body param:"
                lan_2:
                  type: object
                  properties:
                    lan_id:
                      type: string
                      description: The identifier for the LAN you want to create an ACL policy with.
                    lan_name:
                      type: string
                      description: The name of the LAN based on the provided lan_id.
                    port_ranges:
                      type: array
                      items:
                        type: string
                      description: Array of port ranges on the provided LAN that will be included in
                        the ACL. If no ports or port rangess are provided,
                        communication on any port on this LAN is allowed.
                    ports:
                      type: array
                      items:
                        type: number
                      description: Array of ports on the provided LAN that will be included in the
                        ACL. If no ports or port ranges are provided,
                        communication on any port on this LAN is allowed.
                    subnets:
                      type: array
                      items:
                        type: string
                      description: Array of subnet IPs within the LAN that will be included in the
                        ACL. If no subnets are provided, communication on any
                        subnets on this LAN are allowed.
                  required:
                    - lan_id
                  description: "Body param:"
                name:
                  type: string
                  description: "Body param: The name of the ACL."
                protocols:
                  type: array
                  items:
                    type: string
                    enum:
                      - tcp
                      - udp
                      - icmp
                  description: "Body param:"
                unidirectional:
                  type: boolean
                  description: 'Body param: The desired traffic direction for this ACL policy. If
                    set to "false", the policy will allow bidirectional traffic.
                    If set to "true", the policy will only allow traffic in one
                    direction. If not included in request, will default to
                    false.'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  description:
                    type: string
                    description: Description for the ACL.
                  forward_locally:
                    type: boolean
                    description: The desired forwarding action for this ACL policy. If set to
                      "false", the policy will forward traffic to Cloudflare. If
                      set to "true", the policy will forward traffic locally on
                      the Magic Connector. If not included in request, will
                      default to false.
                  lan_1:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  lan_2:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  name:
                    type: string
                    description: The name of the ACL.
                  protocols:
                    type: array
                    items:
                      type: string
                      enum:
                        - tcp
                        - udp
                        - icmp
                  unidirectional:
                    type: boolean
                    description: The desired traffic direction for this ACL policy. If set to
                      "false", the policy will allow bidirectional traffic. If
                      set to "true", the policy will only allow traffic in one
                      direction. If not included in request, will default to
                      false.
      x-distilled-response-path: result
    patch:
      operationId: patchSiteAcl
      description: "Patch a specific Site ACL.  @example ```ts const acl = await
        client.magicTransit.sites.acls.edit( '023e105f4ecef8ad9ca31a8372d0c353',
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: aclId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                description:
                  type: string
                  description: "Body param: Description for the ACL."
                forward_locally:
                  type: boolean
                  description: 'Body param: The desired forwarding action for this ACL policy. If
                    set to "false", the policy will forward traffic to
                    Cloudflare. If set to "true", the policy will forward
                    traffic locally on the Magic Connector. If not included in
                    request, will default to false.'
                lan_1:
                  type: object
                  properties:
                    lan_id:
                      type: string
                      description: The identifier for the LAN you want to create an ACL policy with.
                    lan_name:
                      type: string
                      description: The name of the LAN based on the provided lan_id.
                    port_ranges:
                      type: array
                      items:
                        type: string
                      description: Array of port ranges on the provided LAN that will be included in
                        the ACL. If no ports or port rangess are provided,
                        communication on any port on this LAN is allowed.
                    ports:
                      type: array
                      items:
                        type: number
                      description: Array of ports on the provided LAN that will be included in the
                        ACL. If no ports or port ranges are provided,
                        communication on any port on this LAN is allowed.
                    subnets:
                      type: array
                      items:
                        type: string
                      description: Array of subnet IPs within the LAN that will be included in the
                        ACL. If no subnets are provided, communication on any
                        subnets on this LAN are allowed.
                  required:
                    - lan_id
                  description: "Body param:"
                lan_2:
                  type: object
                  properties:
                    lan_id:
                      type: string
                      description: The identifier for the LAN you want to create an ACL policy with.
                    lan_name:
                      type: string
                      description: The name of the LAN based on the provided lan_id.
                    port_ranges:
                      type: array
                      items:
                        type: string
                      description: Array of port ranges on the provided LAN that will be included in
                        the ACL. If no ports or port rangess are provided,
                        communication on any port on this LAN is allowed.
                    ports:
                      type: array
                      items:
                        type: number
                      description: Array of ports on the provided LAN that will be included in the
                        ACL. If no ports or port ranges are provided,
                        communication on any port on this LAN is allowed.
                    subnets:
                      type: array
                      items:
                        type: string
                      description: Array of subnet IPs within the LAN that will be included in the
                        ACL. If no subnets are provided, communication on any
                        subnets on this LAN are allowed.
                  required:
                    - lan_id
                  description: "Body param:"
                name:
                  type: string
                  description: "Body param: The name of the ACL."
                protocols:
                  type: array
                  items:
                    type: string
                    enum:
                      - tcp
                      - udp
                      - icmp
                  description: "Body param:"
                unidirectional:
                  type: boolean
                  description: 'Body param: The desired traffic direction for this ACL policy. If
                    set to "false", the policy will allow bidirectional traffic.
                    If set to "true", the policy will only allow traffic in one
                    direction. If not included in request, will default to
                    false.'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  description:
                    type: string
                    description: Description for the ACL.
                  forward_locally:
                    type: boolean
                    description: The desired forwarding action for this ACL policy. If set to
                      "false", the policy will forward traffic to Cloudflare. If
                      set to "true", the policy will forward traffic locally on
                      the Magic Connector. If not included in request, will
                      default to false.
                  lan_1:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  lan_2:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  name:
                    type: string
                    description: The name of the ACL.
                  protocols:
                    type: array
                    items:
                      type: string
                      enum:
                        - tcp
                        - udp
                        - icmp
                  unidirectional:
                    type: boolean
                    description: The desired traffic direction for this ACL policy. If set to
                      "false", the policy will allow bidirectional traffic. If
                      set to "true", the policy will only allow traffic in one
                      direction. If not included in request, will default to
                      false.
      x-distilled-response-path: result
    delete:
      operationId: deleteSiteAcl
      description: "Remove a specific Site ACL.  @example ```ts const acl = await
        client.magicTransit.sites.acls.delete(
        '023e105f4ecef8ad9ca31a8372d0c353', '023e105f4ecef8ad9ca31a8372d0c353',
        { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: aclId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  description:
                    type: string
                    description: Description for the ACL.
                  forward_locally:
                    type: boolean
                    description: The desired forwarding action for this ACL policy. If set to
                      "false", the policy will forward traffic to Cloudflare. If
                      set to "true", the policy will forward traffic locally on
                      the Magic Connector. If not included in request, will
                      default to false.
                  lan_1:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  lan_2:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  name:
                    type: string
                    description: The name of the ACL.
                  protocols:
                    type: array
                    items:
                      type: string
                      enum:
                        - tcp
                        - udp
                        - icmp
                  unidirectional:
                    type: boolean
                    description: The desired traffic direction for this ACL policy. If set to
                      "false", the policy will allow bidirectional traffic. If
                      set to "true", the policy will only allow traffic in one
                      direction. If not included in request, will default to
                      false.
      x-distilled-response-path: result
  /accounts/{account_id}/magic/sites/{siteId}/acls:
    get:
      operationId: listSiteAcls
      description: "Lists Site ACLs associated with an account.  @example ```ts //
        Automatically fetches more pages as needed. for await (const acl of
        client.magicTransit.sites.acls.list( '023e105f4ecef8ad9ca31a8372d0c353',
        { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  description:
                    type: string
                    description: Description for the ACL.
                  forward_locally:
                    type: boolean
                    description: The desired forwarding action for this ACL policy. If set to
                      "false", the policy will forward traffic to Cloudflare. If
                      set to "true", the policy will forward traffic locally on
                      the Magic Connector. If not included in request, will
                      default to false.
                  lan_1:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  lan_2:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  name:
                    type: string
                    description: The name of the ACL.
                  protocols:
                    type: array
                    items:
                      type: string
                      enum:
                        - tcp
                        - udp
                        - icmp
                  unidirectional:
                    type: boolean
                    description: The desired traffic direction for this ACL policy. If set to
                      "false", the policy will allow bidirectional traffic. If
                      set to "true", the policy will only allow traffic in one
                      direction. If not included in request, will default to
                      false.
      x-distilled-pagination-class: ACLsSinglePage
    post:
      operationId: createSiteAcl
      description: "Creates a new Site ACL.  @example ```ts const acl = await
        client.magicTransit.sites.acls.create(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', lan_1: { lan_id: 'lan_id' }, lan_2:
        { lan_id: 'lan_id' }, name: 'PIN Pad - Cash Register', }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                lan_1:
                  type: object
                  properties:
                    lan_id:
                      type: string
                      description: The identifier for the LAN you want to create an ACL policy with.
                    lan_name:
                      type: string
                      description: The name of the LAN based on the provided lan_id.
                    port_ranges:
                      type: array
                      items:
                        type: string
                      description: Array of port ranges on the provided LAN that will be included in
                        the ACL. If no ports or port rangess are provided,
                        communication on any port on this LAN is allowed.
                    ports:
                      type: array
                      items:
                        type: number
                      description: Array of ports on the provided LAN that will be included in the
                        ACL. If no ports or port ranges are provided,
                        communication on any port on this LAN is allowed.
                    subnets:
                      type: array
                      items:
                        type: string
                      description: Array of subnet IPs within the LAN that will be included in the
                        ACL. If no subnets are provided, communication on any
                        subnets on this LAN are allowed.
                  required:
                    - lan_id
                  description: "Body param:"
                lan_2:
                  type: object
                  properties:
                    lan_id:
                      type: string
                      description: The identifier for the LAN you want to create an ACL policy with.
                    lan_name:
                      type: string
                      description: The name of the LAN based on the provided lan_id.
                    port_ranges:
                      type: array
                      items:
                        type: string
                      description: Array of port ranges on the provided LAN that will be included in
                        the ACL. If no ports or port rangess are provided,
                        communication on any port on this LAN is allowed.
                    ports:
                      type: array
                      items:
                        type: number
                      description: Array of ports on the provided LAN that will be included in the
                        ACL. If no ports or port ranges are provided,
                        communication on any port on this LAN is allowed.
                    subnets:
                      type: array
                      items:
                        type: string
                      description: Array of subnet IPs within the LAN that will be included in the
                        ACL. If no subnets are provided, communication on any
                        subnets on this LAN are allowed.
                  required:
                    - lan_id
                  description: "Body param:"
                name:
                  type: string
                  description: "Body param: The name of the ACL."
                description:
                  type: string
                  description: "Body param: Description for the ACL."
                forward_locally:
                  type: boolean
                  description: 'Body param: The desired forwarding action for this ACL policy. If
                    set to "false", the policy will forward traffic to
                    Cloudflare. If set to "true", the policy will forward
                    traffic locally on the Magic Connector. If not included in
                    request, will default to false.'
                protocols:
                  type: array
                  items:
                    type: string
                    enum:
                      - tcp
                      - udp
                      - icmp
                  description: "Body param:"
                unidirectional:
                  type: boolean
                  description: 'Body param: The desired traffic direction for this ACL policy. If
                    set to "false", the policy will allow bidirectional traffic.
                    If set to "true", the policy will only allow traffic in one
                    direction. If not included in request, will default to
                    false.'
              required:
                - lan_1
                - lan_2
                - name
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  description:
                    type: string
                    description: Description for the ACL.
                  forward_locally:
                    type: boolean
                    description: The desired forwarding action for this ACL policy. If set to
                      "false", the policy will forward traffic to Cloudflare. If
                      set to "true", the policy will forward traffic locally on
                      the Magic Connector. If not included in request, will
                      default to false.
                  lan_1:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  lan_2:
                    type: object
                    properties:
                      lan_id:
                        type: string
                        description: The identifier for the LAN you want to create an ACL policy with.
                      lan_name:
                        type: string
                        description: The name of the LAN based on the provided lan_id.
                      port_ranges:
                        type: array
                        items:
                          type: string
                        description: Array of port ranges on the provided LAN that will be included in
                          the ACL. If no ports or port rangess are provided,
                          communication on any port on this LAN is allowed.
                      ports:
                        type: array
                        items:
                          type: number
                        description: Array of ports on the provided LAN that will be included in the
                          ACL. If no ports or port ranges are provided,
                          communication on any port on this LAN is allowed.
                      subnets:
                        type: array
                        items:
                          type: string
                        description: Array of subnet IPs within the LAN that will be included in the
                          ACL. If no subnets are provided, communication on any
                          subnets on this LAN are allowed.
                    required:
                      - lan_id
                  name:
                    type: string
                    description: The name of the ACL.
                  protocols:
                    type: array
                    items:
                      type: string
                      enum:
                        - tcp
                        - udp
                        - icmp
                  unidirectional:
                    type: boolean
                    description: The desired traffic direction for this ACL policy. If set to
                      "false", the policy will allow bidirectional traffic. If
                      set to "true", the policy will only allow traffic in one
                      direction. If not included in request, will default to
                      false.
      x-distilled-response-path: result
  /accounts/{account_id}/magic/sites/{siteId}/lans/{lanId}:
    get:
      operationId: getSiteLan
      description: "Get a specific Site LAN.  @example ```ts const lan = await
        client.magicTransit.sites.lans.get( '023e105f4ecef8ad9ca31a8372d0c353',
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: lanId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  ha_link:
                    type: boolean
                    description: mark true to use this LAN for HA probing. only works for site with
                      HA turned on. only one LAN can be set as the ha_link.
                  name:
                    type: string
                  nat:
                    type: object
                    properties:
                      static_prefix:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                  physport:
                    type: number
                  routed_subnets:
                    type: array
                    items:
                      type: object
                      properties:
                        next_hop:
                          type: string
                          description: A valid IPv4 address.
                        prefix:
                          type: string
                          description: A valid CIDR notation representing an IP range.
                        nat:
                          type: object
                          properties:
                            static_prefix:
                              type: string
                              description: A valid CIDR notation representing an IP range.
                      required:
                        - next_hop
                        - prefix
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      dhcp_relay:
                        type: object
                        properties:
                          server_addresses:
                            type: array
                            items:
                              type: string
                            description: List of DHCP server IPs.
                      dhcp_server:
                        type: object
                        properties:
                          dhcp_pool_end:
                            type: string
                            description: A valid IPv4 address.
                          dhcp_pool_start:
                            type: string
                            description: A valid IPv4 address.
                          dns_server:
                            type: string
                            description: A valid IPv4 address.
                          dns_servers:
                            type: array
                            items:
                              type: string
                          reservations:
                            type: object
                            additionalProperties: true
                            description: Mapping of MAC addresses to IP addresses
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      virtual_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                    description: If the site is not configured in high availability mode, this
                      configuration is optional (if omitted, use DHCP). However,
                      if in high availability mode, static_address is required
                      along with secondary and virtual address.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-response-path: result
    put:
      operationId: updateSiteLan
      description: "Update a specific Site LAN.  @example ```ts const lan = await
        client.magicTransit.sites.lans.update(
        '023e105f4ecef8ad9ca31a8372d0c353', '023e105f4ecef8ad9ca31a8372d0c353',
        { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: lanId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: "Body param:"
                nat:
                  type: object
                  properties:
                    static_prefix:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                  description: "Body param:"
                physport:
                  type: number
                  description: "Body param:"
                routed_subnets:
                  type: array
                  items:
                    type: object
                    properties:
                      next_hop:
                        type: string
                        description: A valid IPv4 address.
                      prefix:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      nat:
                        type: object
                        properties:
                          static_prefix:
                            type: string
                            description: A valid CIDR notation representing an IP range.
                    required:
                      - next_hop
                      - prefix
                  description: "Body param:"
                static_addressing:
                  type: object
                  properties:
                    address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                    dhcp_relay:
                      type: object
                      properties:
                        server_addresses:
                          type: array
                          items:
                            type: string
                          description: List of DHCP server IPs.
                    dhcp_server:
                      type: object
                      properties:
                        dhcp_pool_end:
                          type: string
                          description: A valid IPv4 address.
                        dhcp_pool_start:
                          type: string
                          description: A valid IPv4 address.
                        dns_server:
                          type: string
                          description: A valid IPv4 address.
                        dns_servers:
                          type: array
                          items:
                            type: string
                        reservations:
                          type: object
                          additionalProperties: true
                          description: Mapping of MAC addresses to IP addresses
                    secondary_address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                    virtual_address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                  required:
                    - address
                  description: "Body param: If the site is not configured in high availability
                    mode, this configuration is optional (if omitted, use DHCP).
                    However, if in high availability mode, static_address is
                    required along with secondary and virtual address."
                vlan_tag:
                  type: number
                  description: "Body param: VLAN ID. Use zero for untagged."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  ha_link:
                    type: boolean
                    description: mark true to use this LAN for HA probing. only works for site with
                      HA turned on. only one LAN can be set as the ha_link.
                  name:
                    type: string
                  nat:
                    type: object
                    properties:
                      static_prefix:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                  physport:
                    type: number
                  routed_subnets:
                    type: array
                    items:
                      type: object
                      properties:
                        next_hop:
                          type: string
                          description: A valid IPv4 address.
                        prefix:
                          type: string
                          description: A valid CIDR notation representing an IP range.
                        nat:
                          type: object
                          properties:
                            static_prefix:
                              type: string
                              description: A valid CIDR notation representing an IP range.
                      required:
                        - next_hop
                        - prefix
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      dhcp_relay:
                        type: object
                        properties:
                          server_addresses:
                            type: array
                            items:
                              type: string
                            description: List of DHCP server IPs.
                      dhcp_server:
                        type: object
                        properties:
                          dhcp_pool_end:
                            type: string
                            description: A valid IPv4 address.
                          dhcp_pool_start:
                            type: string
                            description: A valid IPv4 address.
                          dns_server:
                            type: string
                            description: A valid IPv4 address.
                          dns_servers:
                            type: array
                            items:
                              type: string
                          reservations:
                            type: object
                            additionalProperties: true
                            description: Mapping of MAC addresses to IP addresses
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      virtual_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                    description: If the site is not configured in high availability mode, this
                      configuration is optional (if omitted, use DHCP). However,
                      if in high availability mode, static_address is required
                      along with secondary and virtual address.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-response-path: result
    patch:
      operationId: patchSiteLan
      description: "Patch a specific Site LAN.  @example ```ts const lan = await
        client.magicTransit.sites.lans.edit( '023e105f4ecef8ad9ca31a8372d0c353',
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: lanId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: "Body param:"
                nat:
                  type: object
                  properties:
                    static_prefix:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                  description: "Body param:"
                physport:
                  type: number
                  description: "Body param:"
                routed_subnets:
                  type: array
                  items:
                    type: object
                    properties:
                      next_hop:
                        type: string
                        description: A valid IPv4 address.
                      prefix:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      nat:
                        type: object
                        properties:
                          static_prefix:
                            type: string
                            description: A valid CIDR notation representing an IP range.
                    required:
                      - next_hop
                      - prefix
                  description: "Body param:"
                static_addressing:
                  type: object
                  properties:
                    address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                    dhcp_relay:
                      type: object
                      properties:
                        server_addresses:
                          type: array
                          items:
                            type: string
                          description: List of DHCP server IPs.
                    dhcp_server:
                      type: object
                      properties:
                        dhcp_pool_end:
                          type: string
                          description: A valid IPv4 address.
                        dhcp_pool_start:
                          type: string
                          description: A valid IPv4 address.
                        dns_server:
                          type: string
                          description: A valid IPv4 address.
                        dns_servers:
                          type: array
                          items:
                            type: string
                        reservations:
                          type: object
                          additionalProperties: true
                          description: Mapping of MAC addresses to IP addresses
                    secondary_address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                    virtual_address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                  required:
                    - address
                  description: "Body param: If the site is not configured in high availability
                    mode, this configuration is optional (if omitted, use DHCP).
                    However, if in high availability mode, static_address is
                    required along with secondary and virtual address."
                vlan_tag:
                  type: number
                  description: "Body param: VLAN ID. Use zero for untagged."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  ha_link:
                    type: boolean
                    description: mark true to use this LAN for HA probing. only works for site with
                      HA turned on. only one LAN can be set as the ha_link.
                  name:
                    type: string
                  nat:
                    type: object
                    properties:
                      static_prefix:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                  physport:
                    type: number
                  routed_subnets:
                    type: array
                    items:
                      type: object
                      properties:
                        next_hop:
                          type: string
                          description: A valid IPv4 address.
                        prefix:
                          type: string
                          description: A valid CIDR notation representing an IP range.
                        nat:
                          type: object
                          properties:
                            static_prefix:
                              type: string
                              description: A valid CIDR notation representing an IP range.
                      required:
                        - next_hop
                        - prefix
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      dhcp_relay:
                        type: object
                        properties:
                          server_addresses:
                            type: array
                            items:
                              type: string
                            description: List of DHCP server IPs.
                      dhcp_server:
                        type: object
                        properties:
                          dhcp_pool_end:
                            type: string
                            description: A valid IPv4 address.
                          dhcp_pool_start:
                            type: string
                            description: A valid IPv4 address.
                          dns_server:
                            type: string
                            description: A valid IPv4 address.
                          dns_servers:
                            type: array
                            items:
                              type: string
                          reservations:
                            type: object
                            additionalProperties: true
                            description: Mapping of MAC addresses to IP addresses
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      virtual_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                    description: If the site is not configured in high availability mode, this
                      configuration is optional (if omitted, use DHCP). However,
                      if in high availability mode, static_address is required
                      along with secondary and virtual address.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-response-path: result
    delete:
      operationId: deleteSiteLan
      description: "Remove a specific Site LAN.  @example ```ts const lan = await
        client.magicTransit.sites.lans.delete(
        '023e105f4ecef8ad9ca31a8372d0c353', '023e105f4ecef8ad9ca31a8372d0c353',
        { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: lanId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  ha_link:
                    type: boolean
                    description: mark true to use this LAN for HA probing. only works for site with
                      HA turned on. only one LAN can be set as the ha_link.
                  name:
                    type: string
                  nat:
                    type: object
                    properties:
                      static_prefix:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                  physport:
                    type: number
                  routed_subnets:
                    type: array
                    items:
                      type: object
                      properties:
                        next_hop:
                          type: string
                          description: A valid IPv4 address.
                        prefix:
                          type: string
                          description: A valid CIDR notation representing an IP range.
                        nat:
                          type: object
                          properties:
                            static_prefix:
                              type: string
                              description: A valid CIDR notation representing an IP range.
                      required:
                        - next_hop
                        - prefix
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      dhcp_relay:
                        type: object
                        properties:
                          server_addresses:
                            type: array
                            items:
                              type: string
                            description: List of DHCP server IPs.
                      dhcp_server:
                        type: object
                        properties:
                          dhcp_pool_end:
                            type: string
                            description: A valid IPv4 address.
                          dhcp_pool_start:
                            type: string
                            description: A valid IPv4 address.
                          dns_server:
                            type: string
                            description: A valid IPv4 address.
                          dns_servers:
                            type: array
                            items:
                              type: string
                          reservations:
                            type: object
                            additionalProperties: true
                            description: Mapping of MAC addresses to IP addresses
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      virtual_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                    description: If the site is not configured in high availability mode, this
                      configuration is optional (if omitted, use DHCP). However,
                      if in high availability mode, static_address is required
                      along with secondary and virtual address.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-response-path: result
  /accounts/{account_id}/magic/sites/{siteId}/lans:
    get:
      operationId: listSiteLans
      description: "Lists Site LANs associated with an account.  @example ```ts //
        Automatically fetches more pages as needed. for await (const lan of
        client.magicTransit.sites.lans.list( '023e105f4ecef8ad9ca31a8372d0c353',
        { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  ha_link:
                    type: boolean
                    description: mark true to use this LAN for HA probing. only works for site with
                      HA turned on. only one LAN can be set as the ha_link.
                  name:
                    type: string
                  nat:
                    type: object
                    properties:
                      static_prefix:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                  physport:
                    type: number
                  routed_subnets:
                    type: array
                    items:
                      type: object
                      properties:
                        next_hop:
                          type: string
                          description: A valid IPv4 address.
                        prefix:
                          type: string
                          description: A valid CIDR notation representing an IP range.
                        nat:
                          type: object
                          properties:
                            static_prefix:
                              type: string
                              description: A valid CIDR notation representing an IP range.
                      required:
                        - next_hop
                        - prefix
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      dhcp_relay:
                        type: object
                        properties:
                          server_addresses:
                            type: array
                            items:
                              type: string
                            description: List of DHCP server IPs.
                      dhcp_server:
                        type: object
                        properties:
                          dhcp_pool_end:
                            type: string
                            description: A valid IPv4 address.
                          dhcp_pool_start:
                            type: string
                            description: A valid IPv4 address.
                          dns_server:
                            type: string
                            description: A valid IPv4 address.
                          dns_servers:
                            type: array
                            items:
                              type: string
                          reservations:
                            type: object
                            additionalProperties: true
                            description: Mapping of MAC addresses to IP addresses
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      virtual_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                    description: If the site is not configured in high availability mode, this
                      configuration is optional (if omitted, use DHCP). However,
                      if in high availability mode, static_address is required
                      along with secondary and virtual address.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-pagination-class: LANsSinglePage
    post:
      operationId: createSiteLan
      description: "Creates a new Site LAN. If the site is in high availability mode,
        static_addressing is required along with secondary and virtual
        address.  @example ```ts // Automatically fetches more pages as needed.
        for await (const lan of client.magicTransit.sites.lans.create(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', physport: 1, }, )) { // ... } ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                physport:
                  type: number
                  description: "Body param:"
                ha_link:
                  type: boolean
                  description: "Body param: mark true to use this LAN for HA probing. only works
                    for site with HA turned on. only one LAN can be set as the
                    ha_link."
                name:
                  type: string
                  description: "Body param:"
                nat:
                  type: object
                  properties:
                    static_prefix:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                  description: "Body param:"
                routed_subnets:
                  type: array
                  items:
                    type: object
                    properties:
                      next_hop:
                        type: string
                        description: A valid IPv4 address.
                      prefix:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      nat:
                        type: object
                        properties:
                          static_prefix:
                            type: string
                            description: A valid CIDR notation representing an IP range.
                    required:
                      - next_hop
                      - prefix
                  description: "Body param:"
                static_addressing:
                  type: object
                  properties:
                    address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                    dhcp_relay:
                      type: object
                      properties:
                        server_addresses:
                          type: array
                          items:
                            type: string
                          description: List of DHCP server IPs.
                    dhcp_server:
                      type: object
                      properties:
                        dhcp_pool_end:
                          type: string
                          description: A valid IPv4 address.
                        dhcp_pool_start:
                          type: string
                          description: A valid IPv4 address.
                        dns_server:
                          type: string
                          description: A valid IPv4 address.
                        dns_servers:
                          type: array
                          items:
                            type: string
                        reservations:
                          type: object
                          additionalProperties: true
                          description: Mapping of MAC addresses to IP addresses
                    secondary_address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                    virtual_address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                  required:
                    - address
                  description: "Body param: If the site is not configured in high availability
                    mode, this configuration is optional (if omitted, use DHCP).
                    However, if in high availability mode, static_address is
                    required along with secondary and virtual address."
                vlan_tag:
                  type: number
                  description: "Body param: VLAN ID. Use zero for untagged."
              required:
                - physport
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  ha_link:
                    type: boolean
                    description: mark true to use this LAN for HA probing. only works for site with
                      HA turned on. only one LAN can be set as the ha_link.
                  name:
                    type: string
                  nat:
                    type: object
                    properties:
                      static_prefix:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                  physport:
                    type: number
                  routed_subnets:
                    type: array
                    items:
                      type: object
                      properties:
                        next_hop:
                          type: string
                          description: A valid IPv4 address.
                        prefix:
                          type: string
                          description: A valid CIDR notation representing an IP range.
                        nat:
                          type: object
                          properties:
                            static_prefix:
                              type: string
                              description: A valid CIDR notation representing an IP range.
                      required:
                        - next_hop
                        - prefix
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      dhcp_relay:
                        type: object
                        properties:
                          server_addresses:
                            type: array
                            items:
                              type: string
                            description: List of DHCP server IPs.
                      dhcp_server:
                        type: object
                        properties:
                          dhcp_pool_end:
                            type: string
                            description: A valid IPv4 address.
                          dhcp_pool_start:
                            type: string
                            description: A valid IPv4 address.
                          dns_server:
                            type: string
                            description: A valid IPv4 address.
                          dns_servers:
                            type: array
                            items:
                              type: string
                          reservations:
                            type: object
                            additionalProperties: true
                            description: Mapping of MAC addresses to IP addresses
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      virtual_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                    description: If the site is not configured in high availability mode, this
                      configuration is optional (if omitted, use DHCP). However,
                      if in high availability mode, static_address is required
                      along with secondary and virtual address.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-pagination-class: LANsSinglePage
  /accounts/{account_id}/magic/sites/{siteId}/wans/{wanId}:
    get:
      operationId: getSiteWan
      description: "Get a specific Site WAN.  @example ```ts const wan = await
        client.magicTransit.sites.wans.get( '023e105f4ecef8ad9ca31a8372d0c353',
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: wanId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  health_check_rate:
                    type: string
                    enum:
                      - low
                      - mid
                      - high
                    description: Magic WAN health check rate for tunnels created on this link. The
                      default value is `mid`.
                  name:
                    type: string
                  physport:
                    type: number
                  priority:
                    type: number
                    description: Priority of WAN for traffic loadbalancing.
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      gateway_address:
                        type: string
                        description: A valid IPv4 address.
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                      - gateway_address
                    description: (optional) if omitted, use DHCP. Submit secondary_address when site
                      is in high availability mode.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-response-path: result
    put:
      operationId: updateSiteWan
      description: "Update a specific Site WAN.  @example ```ts const wan = await
        client.magicTransit.sites.wans.update(
        '023e105f4ecef8ad9ca31a8372d0c353', '023e105f4ecef8ad9ca31a8372d0c353',
        { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: wanId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: "Body param:"
                physport:
                  type: number
                  description: "Body param:"
                priority:
                  type: number
                  description: "Body param:"
                static_addressing:
                  type: object
                  properties:
                    address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                    gateway_address:
                      type: string
                      description: A valid IPv4 address.
                    secondary_address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                  required:
                    - address
                    - gateway_address
                  description: "Body param: (optional) if omitted, use DHCP. Submit
                    secondary_address when site is in high availability mode."
                vlan_tag:
                  type: number
                  description: "Body param: VLAN ID. Use zero for untagged."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  health_check_rate:
                    type: string
                    enum:
                      - low
                      - mid
                      - high
                    description: Magic WAN health check rate for tunnels created on this link. The
                      default value is `mid`.
                  name:
                    type: string
                  physport:
                    type: number
                  priority:
                    type: number
                    description: Priority of WAN for traffic loadbalancing.
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      gateway_address:
                        type: string
                        description: A valid IPv4 address.
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                      - gateway_address
                    description: (optional) if omitted, use DHCP. Submit secondary_address when site
                      is in high availability mode.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-response-path: result
    patch:
      operationId: patchSiteWan
      description: "Patch a specific Site WAN.  @example ```ts const wan = await
        client.magicTransit.sites.wans.edit( '023e105f4ecef8ad9ca31a8372d0c353',
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: wanId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: "Body param:"
                physport:
                  type: number
                  description: "Body param:"
                priority:
                  type: number
                  description: "Body param:"
                static_addressing:
                  type: object
                  properties:
                    address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                    gateway_address:
                      type: string
                      description: A valid IPv4 address.
                    secondary_address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                  required:
                    - address
                    - gateway_address
                  description: "Body param: (optional) if omitted, use DHCP. Submit
                    secondary_address when site is in high availability mode."
                vlan_tag:
                  type: number
                  description: "Body param: VLAN ID. Use zero for untagged."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  health_check_rate:
                    type: string
                    enum:
                      - low
                      - mid
                      - high
                    description: Magic WAN health check rate for tunnels created on this link. The
                      default value is `mid`.
                  name:
                    type: string
                  physport:
                    type: number
                  priority:
                    type: number
                    description: Priority of WAN for traffic loadbalancing.
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      gateway_address:
                        type: string
                        description: A valid IPv4 address.
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                      - gateway_address
                    description: (optional) if omitted, use DHCP. Submit secondary_address when site
                      is in high availability mode.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-response-path: result
    delete:
      operationId: deleteSiteWan
      description: "Remove a specific Site WAN.  @example ```ts const wan = await
        client.magicTransit.sites.wans.delete(
        '023e105f4ecef8ad9ca31a8372d0c353', '023e105f4ecef8ad9ca31a8372d0c353',
        { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: wanId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  health_check_rate:
                    type: string
                    enum:
                      - low
                      - mid
                      - high
                    description: Magic WAN health check rate for tunnels created on this link. The
                      default value is `mid`.
                  name:
                    type: string
                  physport:
                    type: number
                  priority:
                    type: number
                    description: Priority of WAN for traffic loadbalancing.
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      gateway_address:
                        type: string
                        description: A valid IPv4 address.
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                      - gateway_address
                    description: (optional) if omitted, use DHCP. Submit secondary_address when site
                      is in high availability mode.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-response-path: result
  /accounts/{account_id}/magic/sites/{siteId}/wans:
    get:
      operationId: listSiteWans
      description: "Lists Site WANs associated with an account.  @example ```ts //
        Automatically fetches more pages as needed. for await (const wan of
        client.magicTransit.sites.wans.list( '023e105f4ecef8ad9ca31a8372d0c353',
        { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  health_check_rate:
                    type: string
                    enum:
                      - low
                      - mid
                      - high
                    description: Magic WAN health check rate for tunnels created on this link. The
                      default value is `mid`.
                  name:
                    type: string
                  physport:
                    type: number
                  priority:
                    type: number
                    description: Priority of WAN for traffic loadbalancing.
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      gateway_address:
                        type: string
                        description: A valid IPv4 address.
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                      - gateway_address
                    description: (optional) if omitted, use DHCP. Submit secondary_address when site
                      is in high availability mode.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-pagination-class: WANsSinglePage
    post:
      operationId: createSiteWan
      description: "Creates a new Site WAN.  @example ```ts // Automatically fetches
        more pages as needed. for await (const wan of
        client.magicTransit.sites.wans.create(
        '023e105f4ecef8ad9ca31a8372d0c353', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', physport: 1, }, )) { // ... } ```"
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                physport:
                  type: number
                  description: "Body param:"
                name:
                  type: string
                  description: "Body param:"
                priority:
                  type: number
                  description: "Body param:"
                static_addressing:
                  type: object
                  properties:
                    address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                    gateway_address:
                      type: string
                      description: A valid IPv4 address.
                    secondary_address:
                      type: string
                      description: A valid CIDR notation representing an IP range.
                  required:
                    - address
                    - gateway_address
                  description: "Body param: (optional) if omitted, use DHCP. Submit
                    secondary_address when site is in high availability mode."
                vlan_tag:
                  type: number
                  description: "Body param: VLAN ID. Use zero for untagged."
              required:
                - physport
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: Identifier
                  health_check_rate:
                    type: string
                    enum:
                      - low
                      - mid
                      - high
                    description: Magic WAN health check rate for tunnels created on this link. The
                      default value is `mid`.
                  name:
                    type: string
                  physport:
                    type: number
                  priority:
                    type: number
                    description: Priority of WAN for traffic loadbalancing.
                  site_id:
                    type: string
                    description: Identifier
                  static_addressing:
                    type: object
                    properties:
                      address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                      gateway_address:
                        type: string
                        description: A valid IPv4 address.
                      secondary_address:
                        type: string
                        description: A valid CIDR notation representing an IP range.
                    required:
                      - address
                      - gateway_address
                    description: (optional) if omitted, use DHCP. Submit secondary_address when site
                      is in high availability mode.
                  vlan_tag:
                    type: number
                    description: VLAN ID. Use zero for untagged.
      x-distilled-pagination-class: WANsSinglePage
