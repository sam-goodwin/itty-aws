openapi: 3.1.0
info:
  title: Cloudflare CLOUD-CONNECTOR API
  version: 0.0.0
servers:
  - url: https://api.cloudflare.com/client/v4
x-distilled-service: cloud-connector
paths:
  /zones/{zone_id}/cloud_connector/rules:
    get:
      operationId: listRules
      description: Rules
      parameters:
        - name: zone_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  description:
                    type: string
                  enabled:
                    type: boolean
                  expression:
                    type: string
                  parameters:
                    type: object
                    properties:
                      host:
                        type: string
                        description: Host to perform Cloud Connection to
                    description: Parameters of Cloud Connector Rule
                  provider:
                    type: string
                    enum:
                      - aws_s3
                      - cloudflare_r2
                      - gcp_storage
                      - azure_storage
                    description: Cloud Provider type
      x-distilled-pagination-class: RuleListResponsesSinglePage
      x-distilled-errors:
        Forbidden:
          - status: 403
        CloudConnectorRulesNotFound:
          - code: 10003
            message:
              includes: could not find entrypoint ruleset
    put:
      operationId: putRule
      description: Put Rules
      parameters:
        - name: zone_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: array
              items:
                type: object
                properties:
                  id:
                    type: string
                  description:
                    type: string
                  enabled:
                    type: boolean
                  expression:
                    type: string
                  parameters:
                    type: object
                    properties:
                      host:
                        type: string
                        description: Host to perform Cloud Connection to
                    description: Parameters of Cloud Connector Rule
                  provider:
                    type: string
                    enum:
                      - aws_s3
                      - cloudflare_r2
                      - gcp_storage
                      - azure_storage
                    description: Cloud Provider type
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  description:
                    type: string
                  enabled:
                    type: boolean
                  expression:
                    type: string
                  parameters:
                    type: object
                    properties:
                      host:
                        type: string
                        description: Host to perform Cloud Connection to
                    description: Parameters of Cloud Connector Rule
                  provider:
                    type: string
                    enum:
                      - aws_s3
                      - cloudflare_r2
                      - gcp_storage
                      - azure_storage
                    description: Cloud Provider type
      x-distilled-pagination-class: RuleUpdateResponsesSinglePage
      x-distilled-errors:
        Forbidden:
          - status: 403
