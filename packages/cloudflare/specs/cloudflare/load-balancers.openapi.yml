openapi: 3.1.0
info:
  title: Cloudflare LOAD-BALANCERS API
  version: 0.0.0
servers:
  - url: https://api.cloudflare.com/client/v4
x-distilled-service: load-balancers
paths:
  /zones/{zone_id}/load_balancers/{loadBalancerId}:
    get:
      operationId: getLoadBalancer
      description: "Fetch a single configured load balancer.  @example ```ts const
        loadBalancer = await client.loadBalancers.get(
        '699d98642c564d2e855e9661899b7252', { zone_id:
        '699d98642c564d2e855e9661899b7252' }, ); ```"
      parameters:
        - name: loadBalancerId
          in: path
          required: true
          schema:
            type: string
        - name: zone_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  adaptive_routing:
                    type: object
                    properties:
                      failover_across_pools:
                        type: boolean
                        description: Extends zero-downtime failover of requests to healthy origins from
                          alternate pools, when no healthy alternate exists in
                          the same pool, according to the failover order defined
                          by traffic and origin steering. When set false (the
                          default) zero-downtime failover will only occur
                          between origins within the same pool. See
                          `session_affinity_attributes` for control over when
                          sessions are broken or reassigned.
                    description: Controls features that modify the routing of requests to pools and
                      origins in response to dynamic conditions, such as during
                      the interval between active health monitoring requests.
                      For example, zero-downtime failover occurs immediately
                      when an origin becomes unavailable due to HTTP 521, 522,
                      or 523 response codes. If there is another healthy origin
                      in the same pool, the request is retried once against this
                      alternate origin.
                  country_pools:
                    type: object
                    additionalProperties: true
                    description: A mapping of country codes to a list of pool IDs (ordered by their
                      failover priority) for the given country. Any country not
                      explicitly defined will fall back to using the
                      corresponding region_pool mapping if it exists else to
                      default_pools.
                  created_on:
                    type: string
                  default_pools:
                    type: array
                    items:
                      type: string
                    description: A list of pool IDs ordered by their failover priority. Pools
                      defined here are used by default, or when region_pools are
                      not configured for a given region.
                  description:
                    type: string
                    description: Object description.
                  enabled:
                    type: boolean
                    description: Whether to enable (the default) this load balancer.
                  fallback_pool:
                    type: string
                    description: The pool ID to use when all other pools are detected as unhealthy.
                  location_strategy:
                    type: object
                    properties:
                      mode:
                        type: string
                        enum:
                          - pop
                          - resolver_ip
                        description: 'Determines the authoritative location when ECS is not preferred,
                          does not exist in the request, or its GeoIP lookup is
                          unsuccessful.  - `"pop"`: Use the Cloudflare PoP
                          location. - `"resolver_ip"`: Use the DNS resolver
                          GeoIP location. If the GeoIP lookup is unsuccessful,
                          use the Cloudflare PoP location.'
                      prefer_ecs:
                        type: string
                        enum:
                          - always
                          - never
                          - proximity
                          - geo
                        description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                          the authoritative location.  - `"always"`: Always
                          prefer ECS. - `"never"`: Never prefer ECS. -
                          `"proximity"`: Prefer ECS only when
                          `steering_policy="proximity"`. - `"geo"`: Prefer ECS
                          only when `steering_policy="geo"`.'
                    description: Controls location-based steering for non-proxied requests. See
                      `steering_policy` to learn how steering is affected.
                  modified_on:
                    type: string
                  name:
                    type: string
                    description: The DNS hostname to associate with your Load Balancer. If this
                      hostname already exists as a DNS record in Cloudflare's
                      DNS, the Load Balancer will take precedence and the DNS
                      record will not be used.
                  networks:
                    type: array
                    items:
                      type: string
                    description: List of networks where Load Balancer or Pool is enabled.
                  pop_pools:
                    type: object
                    additionalProperties: true
                    description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                      of pool IDs (ordered by their failover priority) for the
                      PoP (datacenter). Any PoPs not explicitly defined will
                      fall back to using the corresponding country_pool, then
                      region_pool mapping if it exists else to default_pools."
                  proxied:
                    type: boolean
                    description: Whether the hostname should be gray clouded (false) or orange
                      clouded (true).
                  random_steering:
                    type: object
                    properties:
                      default_weight:
                        type: number
                        description: The default weight for pools in the load balancer that are not
                          specified in the pool_weights map.
                      pool_weights:
                        type: object
                        additionalProperties: true
                        description: A mapping of pool IDs to custom weights. The weight is relative to
                          other pools in the load balancer.
                    description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                      pool is selected with probability proportional to pool
                      weights. -
                      `steering_policy=\"least_outstanding_requests\"`: Use pool
                      weights to scale each pool's outstanding requests. -
                      `steering_policy=\"least_connections\"`: Use pool weights
                      to scale each pool's open connections."
                  region_pools:
                    type: object
                    additionalProperties: true
                    description: A mapping of region codes to a list of pool IDs (ordered by their
                      failover priority) for the given region. Any regions not
                      explicitly defined will fall back to using default_pools.
                  rules:
                    type: array
                    items:
                      type: object
                      properties:
                        condition:
                          type: string
                          description: The condition expressions to evaluate. If the condition evaluates
                            to true, the overrides or fixed_response in this
                            rule will be applied. An empty condition is always
                            true. For more details on condition expressions,
                            please see
                            https://developers.cloudflare.com/load-balancing/understand-basics/load-balancing-rules/expressions.
                        disabled:
                          type: boolean
                          description: Disable this specific rule. It will no longer be evaluated by this
                            load balancer.
                        fixed_response:
                          type: object
                          properties:
                            content_type:
                              type: string
                              description: The http 'Content-Type' header to include in the response.
                            location:
                              type: string
                              description: The http 'Location' header to include in the response.
                            message_body:
                              type: string
                              description: Text to include as the http body.
                            status_code:
                              type: number
                              description: The http status code to respond with.
                          description: A collection of fields used to directly respond to the eyeball
                            instead of routing to a pool. If a fixed_response is
                            supplied the rule will be marked as terminates.
                        name:
                          type: string
                          description: Name of this rule. Only used for human readability.
                        overrides:
                          type: object
                          properties:
                            adaptive_routing:
                              type: object
                              properties:
                                failover_across_pools:
                                  type: boolean
                                  description: Extends zero-downtime failover of requests to healthy origins from
                                    alternate pools, when no healthy alternate
                                    exists in the same pool, according to the
                                    failover order defined by traffic and origin
                                    steering. When set false (the default)
                                    zero-downtime failover will only occur
                                    between origins within the same pool. See
                                    `session_affinity_attributes` for control
                                    over when sessions are broken or reassigned.
                              description: Controls features that modify the routing of requests to pools and
                                origins in response to dynamic conditions, such
                                as during the interval between active health
                                monitoring requests. For example, zero-downtime
                                failover occurs immediately when an origin
                                becomes unavailable due to HTTP 521, 522, or 523
                                response codes. If there is another healthy
                                origin in the same pool, the request is retried
                                once against this alternate origin.
                            country_pools:
                              type: object
                              additionalProperties: true
                              description: A mapping of country codes to a list of pool IDs (ordered by their
                                failover priority) for the given country. Any
                                country not explicitly defined will fall back to
                                using the corresponding region_pool mapping if
                                it exists else to default_pools.
                            default_pools:
                              type: array
                              items:
                                type: string
                              description: A list of pool IDs ordered by their failover priority. Pools
                                defined here are used by default, or when
                                region_pools are not configured for a given
                                region.
                            fallback_pool:
                              type: string
                              description: The pool ID to use when all other pools are detected as unhealthy.
                            location_strategy:
                              type: object
                              properties:
                                mode:
                                  type: string
                                  enum:
                                    - pop
                                    - resolver_ip
                                  description: 'Determines the authoritative location when ECS is not preferred,
                                    does not exist in the request, or its GeoIP
                                    lookup is unsuccessful.  - `"pop"`: Use the
                                    Cloudflare PoP location. - `"resolver_ip"`:
                                    Use the DNS resolver GeoIP location. If the
                                    GeoIP lookup is unsuccessful, use the
                                    Cloudflare PoP location.'
                                prefer_ecs:
                                  type: string
                                  enum:
                                    - always
                                    - never
                                    - proximity
                                    - geo
                                  description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                                    the authoritative location.  - `"always"`:
                                    Always prefer ECS. - `"never"`: Never prefer
                                    ECS. - `"proximity"`: Prefer ECS only when
                                    `steering_policy="proximity"`. - `"geo"`:
                                    Prefer ECS only when
                                    `steering_policy="geo"`.'
                              description: Controls location-based steering for non-proxied requests. See
                                `steering_policy` to learn how steering is
                                affected.
                            pop_pools:
                              type: object
                              additionalProperties: true
                              description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                                of pool IDs (ordered by their failover priority)
                                for the PoP (datacenter). Any PoPs not
                                explicitly defined will fall back to using the
                                corresponding country_pool, then region_pool
                                mapping if it exists else to default_pools."
                            random_steering:
                              type: object
                              properties:
                                default_weight:
                                  type: number
                                  description: The default weight for pools in the load balancer that are not
                                    specified in the pool_weights map.
                                pool_weights:
                                  type: object
                                  additionalProperties: true
                                  description: A mapping of pool IDs to custom weights. The weight is relative to
                                    other pools in the load balancer.
                              description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                                pool is selected with probability proportional
                                to pool weights. -
                                `steering_policy=\"least_outstanding_requests\"\
                                `: Use pool weights to scale each pool's
                                outstanding requests. -
                                `steering_policy=\"least_connections\"`: Use
                                pool weights to scale each pool's open
                                connections."
                            region_pools:
                              type: object
                              additionalProperties: true
                              description: A mapping of region codes to a list of pool IDs (ordered by their
                                failover priority) for the given region. Any
                                regions not explicitly defined will fall back to
                                using default_pools.
                            session_affinity:
                              type: string
                              enum:
                                - none
                                - cookie
                                - ip_cookie
                                - header
                              description: "Specifies the type of session affinity the load balancer should
                                use unless specified as `\"none\"`. The
                                supported types are: - `\"cookie\"`: On the
                                first request to a proxied load balancer, a
                                cookie is generated, encoding information of
                                which origin the request will be forwarded to.
                                Subsequent requests, by the same client to the
                                same load balancer, will be sent to the origin
                                server the cookie encodes, for the duration of
                                the cookie and as long as the origin server
                                remains healthy. If the cookie has expired or
                                the origin server is unhealthy, then a new
                                origin server is calculated and used. -
                                `\"ip_cookie\"`: Behaves the same as
                                `\"cookie\"` except the initial origin selection
                                is stable and based on the client's ip address.
                                - `\"header\"`: On the first request to a
                                proxied load balancer, a session key based on
                                the configured HTTP headers (see
                                `session_affinity_attributes.headers`) is
                                generated, encoding the request headers used for
                                storing in the load balancer session state which
                                origin the request will be forwarded to.
                                Subsequent requests to the load balancer with
                                the same headers will be sent to the same origin
                                server, for the duration of the session and as
                                long as the origin server remains healthy. If
                                the session has been idle for the duration of
                                `session_affinity_ttl` seconds or the origin
                                server is unhealthy, then a new origin server is
                                calculated and used. See `headers` in
                                `session_affinity_attributes` for additional
                                required configuration."
                            session_affinity_attributes:
                              type: object
                              properties:
                                drain_duration:
                                  type: number
                                  description: Configures the drain duration in seconds. This field is only used
                                    when session affinity is enabled on the load
                                    balancer.
                                headers:
                                  type: array
                                  items:
                                    type: string
                                  description: 'Configures the names of HTTP headers to base session affinity on
                                    when header `session_affinity` is enabled.
                                    At least one HTTP header name must be
                                    provided. To specify the exact cookies to be
                                    used, include an item in the following
                                    format:
                                    `"cookie:<cookie-name-1>,<cookie-name-2>"`
                                    (example) where everything after the colon
                                    is a comma-separated list of cookie names.
                                    Providing only `"cookie"` will result in all
                                    cookies being used. The default max number
                                    of HTTP header names that can be provided
                                    depends on your plan: 5 for Enterprise, 1
                                    for all other plans.'
                                require_all_headers:
                                  type: boolean
                                  description: "When header `session_affinity` is enabled, this option can be used
                                    to specify how HTTP headers on load
                                    balancing requests will be used. The
                                    supported values are: - `\"true\"`: Load
                                    balancing requests must contain _all_ of the
                                    HTTP headers specified by the `headers`
                                    session affinity attribute, otherwise
                                    sessions aren't created. - `\"false\"`: Load
                                    balancing requests must contain _at least
                                    one_ of the HTTP headers specified by the
                                    `headers` session affinity attribute,
                                    otherwise sessions aren't created."
                                samesite:
                                  type: string
                                  enum:
                                    - Auto
                                    - Lax
                                    - None
                                    - Strict
                                  description: 'Configures the SameSite attribute on session affinity cookie.
                                    Value "Auto" will be translated to "Lax" or
                                    "None" depending if Always Use HTTPS is
                                    enabled. Note: when using value "None", the
                                    secure attribute can not be set to "Never".'
                                secure:
                                  type: string
                                  enum:
                                    - Auto
                                    - Always
                                    - Never
                                  description: Configures the Secure attribute on session affinity cookie. Value
                                    "Always" indicates the Secure attribute will
                                    be set in the Set-Cookie header, "Never"
                                    indicates the Secure attribute will not be
                                    set, and "Auto" will set the Secure
                                    attribute depending if Always Use HTTPS is
                                    enabled.
                                zero_downtime_failover:
                                  type: string
                                  enum:
                                    - none
                                    - temporary
                                    - sticky
                                  description: 'Configures the zero-downtime failover between origins within a
                                    pool when session affinity is enabled. This
                                    feature is currently incompatible with Argo,
                                    Tiered Cache, and Bandwidth Alliance. The
                                    supported values are: - `"none"`: No
                                    failover takes place for sessions pinned to
                                    the origin (default). - `"temporary"`:
                                    Traffic will be sent to another other
                                    healthy origin until the originally pinned
                                    origin is available; note that this can
                                    potentially result in heavy origin flapping.
                                    - `"sticky"`: The session affinity cookie is
                                    updated and subsequent requests are sent to
                                    the new origin. Note: Zero-downtime failover
                                    with sticky sessions is currently not
                                    supported for session affinity by header.'
                              description: Configures attributes for session affinity.
                            session_affinity_ttl:
                              type: number
                              description: "Time, in seconds, until a client's session expires after being
                                created. Once the expiry time has been reached,
                                subsequent requests may get sent to a different
                                origin server. The accepted ranges per
                                `session_affinity` policy are: - `\"cookie\"` /
                                `\"ip_cookie\"`: The current default of 23 hours
                                will be used unless explicitly set. The accepted
                                range of values is between [1800, 604800]. -
                                `\"header\"`: The current default of 1800
                                seconds will be used unless explicitly set. The
                                accepted range of values is between [30, 3600].
                                Note: With session affinity by header, sessions
                                only expire after they haven't been used for the
                                number of seconds specified."
                            steering_policy:
                              type: string
                              enum:
                                - off
                                - geo
                                - random
                                - dynamic_latency
                                - proximity
                                - least_outstanding_requests
                                - least_connections
                                - ""
                              description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                                `default_pools`. - `\"geo\"`: Use
                                `region_pools`/`country_pools`/`pop_pools`. For
                                non-proxied requests, the country for
                                `country_pools` is determined by
                                `location_strategy`. - `\"random\"`: Select a
                                pool randomly. - `\"dynamic_latency\"`: Use
                                round trip time to select the closest pool in
                                default_pools (requires pool health checks). -
                                `\"proximity\"`: Use the pools' latitude and
                                longitude to select the closest pool using the
                                Cloudflare PoP location for proxied requests or
                                the location determined by `location_strategy`
                                for non-proxied requests. -
                                `\"least_outstanding_requests\"`: Select a pool
                                by taking into consideration `random_steering`
                                weights, as well as each pool's number of
                                outstanding requests. Pools with more pending
                                requests are weighted proportionately less
                                relative to others. - `\"least_connections\"`:
                                Select a pool by taking into consideration
                                `random_steering` weights, as well as each
                                pool's number of open connections. Pools with
                                more open connections are weighted
                                proportionately less relative to others.
                                Supported for HTTP/1 and HTTP/2 connections. -
                                `\"\"`: Will map to `\"geo\"` if you use
                                `region_pools`/`country_pools`/`pop_pools`
                                otherwise `\"off\"`."
                            ttl:
                              type: number
                              description: Time to live (TTL) of the DNS entry for the IP address returned by
                                this load balancer. This only applies to
                                gray-clouded (unproxied) load balancers.
                          description: A collection of overrides to apply to the load balancer when this
                            rule's condition is true. All fields are optional.
                        priority:
                          type: number
                          description: The order in which rules should be executed in relation to each
                            other. Lower values are executed first. Values do
                            not need to be sequential. If no value is provided
                            for any rule the array order of the rules field will
                            be used to assign a priority.
                        terminates:
                          type: boolean
                          description: If this rule's condition is true, this causes rule evaluation to
                            stop after processing this rule.
                    description: "BETA Field Not General Access: A list of rules for this load
                      balancer to execute."
                  session_affinity:
                    type: string
                    enum:
                      - none
                      - cookie
                      - ip_cookie
                      - header
                    description: "Specifies the type of session affinity the load balancer should
                      use unless specified as `\"none\"`. The supported types
                      are: - `\"cookie\"`: On the first request to a proxied
                      load balancer, a cookie is generated, encoding information
                      of which origin the request will be forwarded to.
                      Subsequent requests, by the same client to the same load
                      balancer, will be sent to the origin server the cookie
                      encodes, for the duration of the cookie and as long as the
                      origin server remains healthy. If the cookie has expired
                      or the origin server is unhealthy, then a new origin
                      server is calculated and used. - `\"ip_cookie\"`: Behaves
                      the same as `\"cookie\"` except the initial origin
                      selection is stable and based on the client's ip address.
                      - `\"header\"`: On the first request to a proxied load
                      balancer, a session key based on the configured HTTP
                      headers (see `session_affinity_attributes.headers`) is
                      generated, encoding the request headers used for storing
                      in the load balancer session state which origin the
                      request will be forwarded to. Subsequent requests to the
                      load balancer with the same headers will be sent to the
                      same origin server, for the duration of the session and as
                      long as the origin server remains healthy. If the session
                      has been idle for the duration of `session_affinity_ttl`
                      seconds or the origin server is unhealthy, then a new
                      origin server is calculated and used. See `headers` in
                      `session_affinity_attributes` for additional required
                      configuration."
                  session_affinity_attributes:
                    type: object
                    properties:
                      drain_duration:
                        type: number
                        description: Configures the drain duration in seconds. This field is only used
                          when session affinity is enabled on the load balancer.
                      headers:
                        type: array
                        items:
                          type: string
                        description: 'Configures the names of HTTP headers to base session affinity on
                          when header `session_affinity` is enabled. At least
                          one HTTP header name must be provided. To specify the
                          exact cookies to be used, include an item in the
                          following format:
                          `"cookie:<cookie-name-1>,<cookie-name-2>"` (example)
                          where everything after the colon is a comma-separated
                          list of cookie names. Providing only `"cookie"` will
                          result in all cookies being used. The default max
                          number of HTTP header names that can be provided
                          depends on your plan: 5 for Enterprise, 1 for all
                          other plans.'
                      require_all_headers:
                        type: boolean
                        description: "When header `session_affinity` is enabled, this option can be used
                          to specify how HTTP headers on load balancing requests
                          will be used. The supported values are: - `\"true\"`:
                          Load balancing requests must contain _all_ of the HTTP
                          headers specified by the `headers` session affinity
                          attribute, otherwise sessions aren't created. -
                          `\"false\"`: Load balancing requests must contain _at
                          least one_ of the HTTP headers specified by the
                          `headers` session affinity attribute, otherwise
                          sessions aren't created."
                      samesite:
                        type: string
                        enum:
                          - Auto
                          - Lax
                          - None
                          - Strict
                        description: 'Configures the SameSite attribute on session affinity cookie.
                          Value "Auto" will be translated to "Lax" or "None"
                          depending if Always Use HTTPS is enabled. Note: when
                          using value "None", the secure attribute can not be
                          set to "Never".'
                      secure:
                        type: string
                        enum:
                          - Auto
                          - Always
                          - Never
                        description: Configures the Secure attribute on session affinity cookie. Value
                          "Always" indicates the Secure attribute will be set in
                          the Set-Cookie header, "Never" indicates the Secure
                          attribute will not be set, and "Auto" will set the
                          Secure attribute depending if Always Use HTTPS is
                          enabled.
                      zero_downtime_failover:
                        type: string
                        enum:
                          - none
                          - temporary
                          - sticky
                        description: 'Configures the zero-downtime failover between origins within a
                          pool when session affinity is enabled. This feature is
                          currently incompatible with Argo, Tiered Cache, and
                          Bandwidth Alliance. The supported values are: -
                          `"none"`: No failover takes place for sessions pinned
                          to the origin (default). - `"temporary"`: Traffic will
                          be sent to another other healthy origin until the
                          originally pinned origin is available; note that this
                          can potentially result in heavy origin flapping. -
                          `"sticky"`: The session affinity cookie is updated and
                          subsequent requests are sent to the new origin. Note:
                          Zero-downtime failover with sticky sessions is
                          currently not supported for session affinity by
                          header.'
                    description: Configures attributes for session affinity.
                  session_affinity_ttl:
                    type: number
                    description: "Time, in seconds, until a client's session expires after being
                      created. Once the expiry time has been reached, subsequent
                      requests may get sent to a different origin server. The
                      accepted ranges per `session_affinity` policy are: -
                      `\"cookie\"` / `\"ip_cookie\"`: The current default of 23
                      hours will be used unless explicitly set. The accepted
                      range of values is between [1800, 604800]. - `\"header\"`:
                      The current default of 1800 seconds will be used unless
                      explicitly set. The accepted range of values is between
                      [30, 3600]. Note: With session affinity by header,
                      sessions only expire after they haven't been used for the
                      number of seconds specified."
                  steering_policy:
                    type: string
                    enum:
                      - off
                      - geo
                      - random
                      - dynamic_latency
                      - proximity
                      - least_outstanding_requests
                      - least_connections
                      - ""
                    description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                      `default_pools`. - `\"geo\"`: Use
                      `region_pools`/`country_pools`/`pop_pools`. For
                      non-proxied requests, the country for `country_pools` is
                      determined by `location_strategy`. - `\"random\"`: Select
                      a pool randomly. - `\"dynamic_latency\"`: Use round trip
                      time to select the closest pool in default_pools (requires
                      pool health checks). - `\"proximity\"`: Use the pools'
                      latitude and longitude to select the closest pool using
                      the Cloudflare PoP location for proxied requests or the
                      location determined by `location_strategy` for non-proxied
                      requests. - `\"least_outstanding_requests\"`: Select a
                      pool by taking into consideration `random_steering`
                      weights, as well as each pool's number of outstanding
                      requests. Pools with more pending requests are weighted
                      proportionately less relative to others. -
                      `\"least_connections\"`: Select a pool by taking into
                      consideration `random_steering` weights, as well as each
                      pool's number of open connections. Pools with more open
                      connections are weighted proportionately less relative to
                      others. Supported for HTTP/1 and HTTP/2 connections. -
                      `\"\"`: Will map to `\"geo\"` if you use
                      `region_pools`/`country_pools`/`pop_pools` otherwise
                      `\"off\"`."
                  ttl:
                    type: number
                    description: Time to live (TTL) of the DNS entry for the IP address returned by
                      this load balancer. This only applies to gray-clouded
                      (unproxied) load balancers.
                  zone_name:
                    type: string
      x-distilled-response-path: result
    put:
      operationId: updateLoadBalancer
      description: "Update a configured load balancer.  @example ```ts const
        loadBalancer = await client.loadBalancers.update(
        '699d98642c564d2e855e9661899b7252', { zone_id:
        '699d98642c564d2e855e9661899b7252', default_pools: [
        '17b5962d775c646f3f9725cbc7a53df4', '9290f38c5d07c2e2f4df57b1f61d4196',
        '00920f38ce07c2e2f4df50b1f61d4194', ], fallback_pool: 'fallback_pool',
        name: 'www.example.com', }, ); ```"
      parameters:
        - name: loadBalancerId
          in: path
          required: true
          schema:
            type: string
        - name: zone_id
          in: path
          required: true
          description: "Path param:"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                default_pools:
                  type: array
                  items:
                    type: string
                  description: "Body param: A list of pool IDs ordered by their failover priority.
                    Pools defined here are used by default, or when region_pools
                    are not configured for a given region."
                fallback_pool:
                  type: string
                  description: "Body param: The pool ID to use when all other pools are detected
                    as unhealthy."
                name:
                  type: string
                  description: "Body param: The DNS hostname to associate with your Load Balancer.
                    If this hostname already exists as a DNS record in
                    Cloudflare's DNS, the Load Balancer will take precedence and
                    the DNS record will not be used."
                adaptive_routing:
                  type: object
                  properties:
                    failover_across_pools:
                      type: boolean
                      description: Extends zero-downtime failover of requests to healthy origins from
                        alternate pools, when no healthy alternate exists in the
                        same pool, according to the failover order defined by
                        traffic and origin steering. When set false (the
                        default) zero-downtime failover will only occur between
                        origins within the same pool. See
                        `session_affinity_attributes` for control over when
                        sessions are broken or reassigned.
                  description: "Body param: Controls features that modify the routing of requests
                    to pools and origins in response to dynamic conditions, such
                    as during the interval between active health monitoring
                    requests. For example, zero-downtime failover occurs
                    immediately when an origin becomes unavailable due to HTTP
                    521, 522, or 523 response codes. If there is another healthy
                    origin in the same pool, the request is retried once against
                    this alternate origin."
                country_pools:
                  type: object
                  additionalProperties: true
                  description: "Body param: A mapping of country codes to a list of pool IDs
                    (ordered by their failover priority) for the given country.
                    Any country not explicitly defined will fall back to using
                    the corresponding region_pool mapping if it exists else to
                    default_pools."
                description:
                  type: string
                  description: "Body param: Object description."
                enabled:
                  type: boolean
                  description: "Body param: Whether to enable (the default) this load balancer."
                location_strategy:
                  type: object
                  properties:
                    mode:
                      type: string
                      enum:
                        - pop
                        - resolver_ip
                      description: 'Determines the authoritative location when ECS is not preferred,
                        does not exist in the request, or its GeoIP lookup is
                        unsuccessful.  - `"pop"`: Use the Cloudflare PoP
                        location. - `"resolver_ip"`: Use the DNS resolver GeoIP
                        location. If the GeoIP lookup is unsuccessful, use the
                        Cloudflare PoP location.'
                    prefer_ecs:
                      type: string
                      enum:
                        - always
                        - never
                        - proximity
                        - geo
                      description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                        the authoritative location.  - `"always"`: Always prefer
                        ECS. - `"never"`: Never prefer ECS. - `"proximity"`:
                        Prefer ECS only when `steering_policy="proximity"`. -
                        `"geo"`: Prefer ECS only when `steering_policy="geo"`.'
                  description: "Body param: Controls location-based steering for non-proxied
                    requests. See `steering_policy` to learn how steering is
                    affected."
                networks:
                  type: array
                  items:
                    type: string
                  description: "Body param: List of networks where Load Balancer or Pool is
                    enabled."
                pop_pools:
                  type: object
                  additionalProperties: true
                  description: "Body param: Enterprise only: A mapping of Cloudflare PoP
                    identifiers to a list of pool IDs (ordered by their failover
                    priority) for the PoP (datacenter). Any PoPs not explicitly
                    defined will fall back to using the corresponding
                    country_pool, then region_pool mapping if it exists else to
                    default_pools."
                proxied:
                  type: boolean
                  description: "Body param: Whether the hostname should be gray clouded (false) or
                    orange clouded (true)."
                random_steering:
                  type: object
                  properties:
                    default_weight:
                      type: number
                      description: The default weight for pools in the load balancer that are not
                        specified in the pool_weights map.
                    pool_weights:
                      type: object
                      additionalProperties: true
                      description: A mapping of pool IDs to custom weights. The weight is relative to
                        other pools in the load balancer.
                  description: "Body param: Configures pool weights.  -
                    `steering_policy=\"random\"`: A random pool is selected with
                    probability proportional to pool weights. -
                    `steering_policy=\"least_outstanding_requests\"`: Use pool
                    weights to scale each pool's outstanding requests. -
                    `steering_policy=\"least_connections\"`: Use pool weights to
                    scale each pool's open connections."
                region_pools:
                  type: object
                  additionalProperties: true
                  description: "Body param: A mapping of region codes to a list of pool IDs
                    (ordered by their failover priority) for the given region.
                    Any regions not explicitly defined will fall back to using
                    default_pools."
                rules:
                  type: array
                  items:
                    type: object
                    properties:
                      condition:
                        type: string
                        description: The condition expressions to evaluate. If the condition evaluates
                          to true, the overrides or fixed_response in this rule
                          will be applied. An empty condition is always true.
                          For more details on condition expressions, please see
                          https://developers.cloudflare.com/load-balancing/understand-basics/load-balancing-rules/expressions.
                      disabled:
                        type: boolean
                        description: Disable this specific rule. It will no longer be evaluated by this
                          load balancer.
                      fixed_response:
                        type: object
                        properties:
                          content_type:
                            type: string
                            description: The http 'Content-Type' header to include in the response.
                          location:
                            type: string
                            description: The http 'Location' header to include in the response.
                          message_body:
                            type: string
                            description: Text to include as the http body.
                          status_code:
                            type: number
                            description: The http status code to respond with.
                        description: A collection of fields used to directly respond to the eyeball
                          instead of routing to a pool. If a fixed_response is
                          supplied the rule will be marked as terminates.
                      name:
                        type: string
                        description: Name of this rule. Only used for human readability.
                      overrides:
                        type: object
                        properties:
                          adaptive_routing:
                            type: object
                            properties:
                              failover_across_pools:
                                type: boolean
                                description: Extends zero-downtime failover of requests to healthy origins from
                                  alternate pools, when no healthy alternate
                                  exists in the same pool, according to the
                                  failover order defined by traffic and origin
                                  steering. When set false (the default)
                                  zero-downtime failover will only occur between
                                  origins within the same pool. See
                                  `session_affinity_attributes` for control over
                                  when sessions are broken or reassigned.
                            description: Controls features that modify the routing of requests to pools and
                              origins in response to dynamic conditions, such as
                              during the interval between active health
                              monitoring requests. For example, zero-downtime
                              failover occurs immediately when an origin becomes
                              unavailable due to HTTP 521, 522, or 523 response
                              codes. If there is another healthy origin in the
                              same pool, the request is retried once against
                              this alternate origin.
                          country_pools:
                            type: object
                            additionalProperties: true
                            description: A mapping of country codes to a list of pool IDs (ordered by their
                              failover priority) for the given country. Any
                              country not explicitly defined will fall back to
                              using the corresponding region_pool mapping if it
                              exists else to default_pools.
                          default_pools:
                            type: array
                            items:
                              type: string
                            description: A list of pool IDs ordered by their failover priority. Pools
                              defined here are used by default, or when
                              region_pools are not configured for a given
                              region.
                          fallback_pool:
                            type: string
                            description: The pool ID to use when all other pools are detected as unhealthy.
                          location_strategy:
                            type: object
                            properties:
                              mode:
                                type: string
                                enum:
                                  - pop
                                  - resolver_ip
                                description: 'Determines the authoritative location when ECS is not preferred,
                                  does not exist in the request, or its GeoIP
                                  lookup is unsuccessful.  - `"pop"`: Use the
                                  Cloudflare PoP location. - `"resolver_ip"`:
                                  Use the DNS resolver GeoIP location. If the
                                  GeoIP lookup is unsuccessful, use the
                                  Cloudflare PoP location.'
                              prefer_ecs:
                                type: string
                                enum:
                                  - always
                                  - never
                                  - proximity
                                  - geo
                                description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                                  the authoritative location.  - `"always"`:
                                  Always prefer ECS. - `"never"`: Never prefer
                                  ECS. - `"proximity"`: Prefer ECS only when
                                  `steering_policy="proximity"`. - `"geo"`:
                                  Prefer ECS only when `steering_policy="geo"`.'
                            description: Controls location-based steering for non-proxied requests. See
                              `steering_policy` to learn how steering is
                              affected.
                          pop_pools:
                            type: object
                            additionalProperties: true
                            description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                              of pool IDs (ordered by their failover priority)
                              for the PoP (datacenter). Any PoPs not explicitly
                              defined will fall back to using the corresponding
                              country_pool, then region_pool mapping if it
                              exists else to default_pools."
                          random_steering:
                            type: object
                            properties:
                              default_weight:
                                type: number
                                description: The default weight for pools in the load balancer that are not
                                  specified in the pool_weights map.
                              pool_weights:
                                type: object
                                additionalProperties: true
                                description: A mapping of pool IDs to custom weights. The weight is relative to
                                  other pools in the load balancer.
                            description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                              pool is selected with probability proportional to
                              pool weights. -
                              `steering_policy=\"least_outstanding_requests\"`:
                              Use pool weights to scale each pool's outstanding
                              requests. -
                              `steering_policy=\"least_connections\"`: Use pool
                              weights to scale each pool's open connections."
                          region_pools:
                            type: object
                            additionalProperties: true
                            description: A mapping of region codes to a list of pool IDs (ordered by their
                              failover priority) for the given region. Any
                              regions not explicitly defined will fall back to
                              using default_pools.
                          session_affinity:
                            type: string
                            enum:
                              - none
                              - cookie
                              - ip_cookie
                              - header
                            description: "Specifies the type of session affinity the load balancer should
                              use unless specified as `\"none\"`. The supported
                              types are: - `\"cookie\"`: On the first request to
                              a proxied load balancer, a cookie is generated,
                              encoding information of which origin the request
                              will be forwarded to. Subsequent requests, by the
                              same client to the same load balancer, will be
                              sent to the origin server the cookie encodes, for
                              the duration of the cookie and as long as the
                              origin server remains healthy. If the cookie has
                              expired or the origin server is unhealthy, then a
                              new origin server is calculated and used. -
                              `\"ip_cookie\"`: Behaves the same as `\"cookie\"`
                              except the initial origin selection is stable and
                              based on the client's ip address. - `\"header\"`:
                              On the first request to a proxied load balancer, a
                              session key based on the configured HTTP headers
                              (see `session_affinity_attributes.headers`) is
                              generated, encoding the request headers used for
                              storing in the load balancer session state which
                              origin the request will be forwarded to.
                              Subsequent requests to the load balancer with the
                              same headers will be sent to the same origin
                              server, for the duration of the session and as
                              long as the origin server remains healthy. If the
                              session has been idle for the duration of
                              `session_affinity_ttl` seconds or the origin
                              server is unhealthy, then a new origin server is
                              calculated and used. See `headers` in
                              `session_affinity_attributes` for additional
                              required configuration."
                          session_affinity_attributes:
                            type: object
                            properties:
                              drain_duration:
                                type: number
                                description: Configures the drain duration in seconds. This field is only used
                                  when session affinity is enabled on the load
                                  balancer.
                              headers:
                                type: array
                                items:
                                  type: string
                                description: 'Configures the names of HTTP headers to base session affinity on
                                  when header `session_affinity` is enabled. At
                                  least one HTTP header name must be provided.
                                  To specify the exact cookies to be used,
                                  include an item in the following format:
                                  `"cookie:<cookie-name-1>,<cookie-name-2>"`
                                  (example) where everything after the colon is
                                  a comma-separated list of cookie names.
                                  Providing only `"cookie"` will result in all
                                  cookies being used. The default max number of
                                  HTTP header names that can be provided depends
                                  on your plan: 5 for Enterprise, 1 for all
                                  other plans.'
                              require_all_headers:
                                type: boolean
                                description: "When header `session_affinity` is enabled, this option can be used
                                  to specify how HTTP headers on load balancing
                                  requests will be used. The supported values
                                  are: - `\"true\"`: Load balancing requests
                                  must contain _all_ of the HTTP headers
                                  specified by the `headers` session affinity
                                  attribute, otherwise sessions aren't created.
                                  - `\"false\"`: Load balancing requests must
                                  contain _at least one_ of the HTTP headers
                                  specified by the `headers` session affinity
                                  attribute, otherwise sessions aren't created."
                              samesite:
                                type: string
                                enum:
                                  - Auto
                                  - Lax
                                  - None
                                  - Strict
                                description: 'Configures the SameSite attribute on session affinity cookie.
                                  Value "Auto" will be translated to "Lax" or
                                  "None" depending if Always Use HTTPS is
                                  enabled. Note: when using value "None", the
                                  secure attribute can not be set to "Never".'
                              secure:
                                type: string
                                enum:
                                  - Auto
                                  - Always
                                  - Never
                                description: Configures the Secure attribute on session affinity cookie. Value
                                  "Always" indicates the Secure attribute will
                                  be set in the Set-Cookie header, "Never"
                                  indicates the Secure attribute will not be
                                  set, and "Auto" will set the Secure attribute
                                  depending if Always Use HTTPS is enabled.
                              zero_downtime_failover:
                                type: string
                                enum:
                                  - none
                                  - temporary
                                  - sticky
                                description: 'Configures the zero-downtime failover between origins within a
                                  pool when session affinity is enabled. This
                                  feature is currently incompatible with Argo,
                                  Tiered Cache, and Bandwidth Alliance. The
                                  supported values are: - `"none"`: No failover
                                  takes place for sessions pinned to the origin
                                  (default). - `"temporary"`: Traffic will be
                                  sent to another other healthy origin until the
                                  originally pinned origin is available; note
                                  that this can potentially result in heavy
                                  origin flapping. - `"sticky"`: The session
                                  affinity cookie is updated and subsequent
                                  requests are sent to the new origin. Note:
                                  Zero-downtime failover with sticky sessions is
                                  currently not supported for session affinity
                                  by header.'
                            description: Configures attributes for session affinity.
                          session_affinity_ttl:
                            type: number
                            description: "Time, in seconds, until a client's session expires after being
                              created. Once the expiry time has been reached,
                              subsequent requests may get sent to a different
                              origin server. The accepted ranges per
                              `session_affinity` policy are: - `\"cookie\"` /
                              `\"ip_cookie\"`: The current default of 23 hours
                              will be used unless explicitly set. The accepted
                              range of values is between [1800, 604800]. -
                              `\"header\"`: The current default of 1800 seconds
                              will be used unless explicitly set. The accepted
                              range of values is between [30, 3600]. Note: With
                              session affinity by header, sessions only expire
                              after they haven't been used for the number of
                              seconds specified."
                          steering_policy:
                            type: string
                            enum:
                              - off
                              - geo
                              - random
                              - dynamic_latency
                              - proximity
                              - least_outstanding_requests
                              - least_connections
                              - ""
                            description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                              `default_pools`. - `\"geo\"`: Use
                              `region_pools`/`country_pools`/`pop_pools`. For
                              non-proxied requests, the country for
                              `country_pools` is determined by
                              `location_strategy`. - `\"random\"`: Select a pool
                              randomly. - `\"dynamic_latency\"`: Use round trip
                              time to select the closest pool in default_pools
                              (requires pool health checks). - `\"proximity\"`:
                              Use the pools' latitude and longitude to select
                              the closest pool using the Cloudflare PoP location
                              for proxied requests or the location determined by
                              `location_strategy` for non-proxied requests. -
                              `\"least_outstanding_requests\"`: Select a pool by
                              taking into consideration `random_steering`
                              weights, as well as each pool's number of
                              outstanding requests. Pools with more pending
                              requests are weighted proportionately less
                              relative to others. - `\"least_connections\"`:
                              Select a pool by taking into consideration
                              `random_steering` weights, as well as each pool's
                              number of open connections. Pools with more open
                              connections are weighted proportionately less
                              relative to others. Supported for HTTP/1 and
                              HTTP/2 connections. - `\"\"`: Will map to
                              `\"geo\"` if you use
                              `region_pools`/`country_pools`/`pop_pools`
                              otherwise `\"off\"`."
                          ttl:
                            type: number
                            description: Time to live (TTL) of the DNS entry for the IP address returned by
                              this load balancer. This only applies to
                              gray-clouded (unproxied) load balancers.
                        description: A collection of overrides to apply to the load balancer when this
                          rule's condition is true. All fields are optional.
                      priority:
                        type: number
                        description: The order in which rules should be executed in relation to each
                          other. Lower values are executed first. Values do not
                          need to be sequential. If no value is provided for any
                          rule the array order of the rules field will be used
                          to assign a priority.
                      terminates:
                        type: boolean
                        description: If this rule's condition is true, this causes rule evaluation to
                          stop after processing this rule.
                  description: "Body param: BETA Field Not General Access: A list of rules for
                    this load balancer to execute."
                session_affinity:
                  type: string
                  enum:
                    - none
                    - cookie
                    - ip_cookie
                    - header
                  description: "Body param: Specifies the type of session affinity the load
                    balancer should use unless specified as `\"none\"`. The
                    supported types are: - `\"cookie\"`: On the first request to
                    a proxied load balancer, a cookie is generated, encoding
                    information of which origin the request will be forwarded
                    to. Subsequent requests, by the same client to the same load
                    balancer, will be sent to the origin server the cookie
                    encodes, for the duration of the cookie and as long as the
                    origin server remains healthy. If the cookie has expired or
                    the origin server is unhealthy, then a new origin server is
                    calculated and used. - `\"ip_cookie\"`: Behaves the same as
                    `\"cookie\"` except the initial origin selection is stable
                    and based on the client's ip address. - `\"header\"`: On the
                    first request to a proxied load balancer, a session key
                    based on the configured HTTP headers (see
                    `session_affinity_attributes.headers`) is generated,
                    encoding the request headers used for storing in the load
                    balancer session state which origin the request will be
                    forwarded to. Subsequent requests to the load balancer with
                    the same headers will be sent to the same origin server, for
                    the duration of the session and as long as the origin server
                    remains healthy. If the session has been idle for the
                    duration of `session_affinity_ttl` seconds or the origin
                    server is unhealthy, then a new origin server is calculated
                    and used. See `headers` in `session_affinity_attributes` for
                    additional required configuration."
                session_affinity_attributes:
                  type: object
                  properties:
                    drain_duration:
                      type: number
                      description: Configures the drain duration in seconds. This field is only used
                        when session affinity is enabled on the load balancer.
                    headers:
                      type: array
                      items:
                        type: string
                      description: 'Configures the names of HTTP headers to base session affinity on
                        when header `session_affinity` is enabled. At least one
                        HTTP header name must be provided. To specify the exact
                        cookies to be used, include an item in the following
                        format: `"cookie:<cookie-name-1>,<cookie-name-2>"`
                        (example) where everything after the colon is a
                        comma-separated list of cookie names. Providing only
                        `"cookie"` will result in all cookies being used. The
                        default max number of HTTP header names that can be
                        provided depends on your plan: 5 for Enterprise, 1 for
                        all other plans.'
                    require_all_headers:
                      type: boolean
                      description: "When header `session_affinity` is enabled, this option can be used
                        to specify how HTTP headers on load balancing requests
                        will be used. The supported values are: - `\"true\"`:
                        Load balancing requests must contain _all_ of the HTTP
                        headers specified by the `headers` session affinity
                        attribute, otherwise sessions aren't created. -
                        `\"false\"`: Load balancing requests must contain _at
                        least one_ of the HTTP headers specified by the
                        `headers` session affinity attribute, otherwise sessions
                        aren't created."
                    samesite:
                      type: string
                      enum:
                        - Auto
                        - Lax
                        - None
                        - Strict
                      description: 'Configures the SameSite attribute on session affinity cookie.
                        Value "Auto" will be translated to "Lax" or "None"
                        depending if Always Use HTTPS is enabled. Note: when
                        using value "None", the secure attribute can not be set
                        to "Never".'
                    secure:
                      type: string
                      enum:
                        - Auto
                        - Always
                        - Never
                      description: Configures the Secure attribute on session affinity cookie. Value
                        "Always" indicates the Secure attribute will be set in
                        the Set-Cookie header, "Never" indicates the Secure
                        attribute will not be set, and "Auto" will set the
                        Secure attribute depending if Always Use HTTPS is
                        enabled.
                    zero_downtime_failover:
                      type: string
                      enum:
                        - none
                        - temporary
                        - sticky
                      description: 'Configures the zero-downtime failover between origins within a
                        pool when session affinity is enabled. This feature is
                        currently incompatible with Argo, Tiered Cache, and
                        Bandwidth Alliance. The supported values are: -
                        `"none"`: No failover takes place for sessions pinned to
                        the origin (default). - `"temporary"`: Traffic will be
                        sent to another other healthy origin until the
                        originally pinned origin is available; note that this
                        can potentially result in heavy origin flapping. -
                        `"sticky"`: The session affinity cookie is updated and
                        subsequent requests are sent to the new origin. Note:
                        Zero-downtime failover with sticky sessions is currently
                        not supported for session affinity by header.'
                  description: "Body param: Configures attributes for session affinity."
                session_affinity_ttl:
                  type: number
                  description: "Body param: Time, in seconds, until a client's session expires
                    after being created. Once the expiry time has been reached,
                    subsequent requests may get sent to a different origin
                    server. The accepted ranges per `session_affinity` policy
                    are: - `\"cookie\"` / `\"ip_cookie\"`: The current default
                    of 23 hours will be used unless explicitly set. The accepted
                    range of values is between [1800, 604800]. - `\"header\"`:
                    The current default of 1800 seconds will be used unless
                    explicitly set. The accepted range of values is between [30,
                    3600]. Note: With session affinity by header, sessions only
                    expire after they haven't been used for the number of
                    seconds specified."
                steering_policy:
                  type: string
                  enum:
                    - off
                    - geo
                    - random
                    - dynamic_latency
                    - proximity
                    - least_outstanding_requests
                    - least_connections
                    - ""
                  description: "Body param: Steering Policy for this load balancer.  - `\"off\"`:
                    Use `default_pools`. - `\"geo\"`: Use
                    `region_pools`/`country_pools`/`pop_pools`. For non-proxied
                    requests, the country for `country_pools` is determined by
                    `location_strategy`. - `\"random\"`: Select a pool randomly.
                    - `\"dynamic_latency\"`: Use round trip time to select the
                    closest pool in default_pools (requires pool health checks).
                    - `\"proximity\"`: Use the pools' latitude and longitude to
                    select the closest pool using the Cloudflare PoP location
                    for proxied requests or the location determined by
                    `location_strategy` for non-proxied requests. -
                    `\"least_outstanding_requests\"`: Select a pool by taking
                    into consideration `random_steering` weights, as well as
                    each pool's number of outstanding requests. Pools with more
                    pending requests are weighted proportionately less relative
                    to others. - `\"least_connections\"`: Select a pool by
                    taking into consideration `random_steering` weights, as well
                    as each pool's number of open connections. Pools with more
                    open connections are weighted proportionately less relative
                    to others. Supported for HTTP/1 and HTTP/2 connections. -
                    `\"\"`: Will map to `\"geo\"` if you use
                    `region_pools`/`country_pools`/`pop_pools` otherwise
                    `\"off\"`."
                ttl:
                  type: number
                  description: "Body param: Time to live (TTL) of the DNS entry for the IP address
                    returned by this load balancer. This only applies to
                    gray-clouded (unproxied) load balancers."
              required:
                - default_pools
                - fallback_pool
                - name
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  adaptive_routing:
                    type: object
                    properties:
                      failover_across_pools:
                        type: boolean
                        description: Extends zero-downtime failover of requests to healthy origins from
                          alternate pools, when no healthy alternate exists in
                          the same pool, according to the failover order defined
                          by traffic and origin steering. When set false (the
                          default) zero-downtime failover will only occur
                          between origins within the same pool. See
                          `session_affinity_attributes` for control over when
                          sessions are broken or reassigned.
                    description: Controls features that modify the routing of requests to pools and
                      origins in response to dynamic conditions, such as during
                      the interval between active health monitoring requests.
                      For example, zero-downtime failover occurs immediately
                      when an origin becomes unavailable due to HTTP 521, 522,
                      or 523 response codes. If there is another healthy origin
                      in the same pool, the request is retried once against this
                      alternate origin.
                  country_pools:
                    type: object
                    additionalProperties: true
                    description: A mapping of country codes to a list of pool IDs (ordered by their
                      failover priority) for the given country. Any country not
                      explicitly defined will fall back to using the
                      corresponding region_pool mapping if it exists else to
                      default_pools.
                  created_on:
                    type: string
                  default_pools:
                    type: array
                    items:
                      type: string
                    description: A list of pool IDs ordered by their failover priority. Pools
                      defined here are used by default, or when region_pools are
                      not configured for a given region.
                  description:
                    type: string
                    description: Object description.
                  enabled:
                    type: boolean
                    description: Whether to enable (the default) this load balancer.
                  fallback_pool:
                    type: string
                    description: The pool ID to use when all other pools are detected as unhealthy.
                  location_strategy:
                    type: object
                    properties:
                      mode:
                        type: string
                        enum:
                          - pop
                          - resolver_ip
                        description: 'Determines the authoritative location when ECS is not preferred,
                          does not exist in the request, or its GeoIP lookup is
                          unsuccessful.  - `"pop"`: Use the Cloudflare PoP
                          location. - `"resolver_ip"`: Use the DNS resolver
                          GeoIP location. If the GeoIP lookup is unsuccessful,
                          use the Cloudflare PoP location.'
                      prefer_ecs:
                        type: string
                        enum:
                          - always
                          - never
                          - proximity
                          - geo
                        description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                          the authoritative location.  - `"always"`: Always
                          prefer ECS. - `"never"`: Never prefer ECS. -
                          `"proximity"`: Prefer ECS only when
                          `steering_policy="proximity"`. - `"geo"`: Prefer ECS
                          only when `steering_policy="geo"`.'
                    description: Controls location-based steering for non-proxied requests. See
                      `steering_policy` to learn how steering is affected.
                  modified_on:
                    type: string
                  name:
                    type: string
                    description: The DNS hostname to associate with your Load Balancer. If this
                      hostname already exists as a DNS record in Cloudflare's
                      DNS, the Load Balancer will take precedence and the DNS
                      record will not be used.
                  networks:
                    type: array
                    items:
                      type: string
                    description: List of networks where Load Balancer or Pool is enabled.
                  pop_pools:
                    type: object
                    additionalProperties: true
                    description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                      of pool IDs (ordered by their failover priority) for the
                      PoP (datacenter). Any PoPs not explicitly defined will
                      fall back to using the corresponding country_pool, then
                      region_pool mapping if it exists else to default_pools."
                  proxied:
                    type: boolean
                    description: Whether the hostname should be gray clouded (false) or orange
                      clouded (true).
                  random_steering:
                    type: object
                    properties:
                      default_weight:
                        type: number
                        description: The default weight for pools in the load balancer that are not
                          specified in the pool_weights map.
                      pool_weights:
                        type: object
                        additionalProperties: true
                        description: A mapping of pool IDs to custom weights. The weight is relative to
                          other pools in the load balancer.
                    description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                      pool is selected with probability proportional to pool
                      weights. -
                      `steering_policy=\"least_outstanding_requests\"`: Use pool
                      weights to scale each pool's outstanding requests. -
                      `steering_policy=\"least_connections\"`: Use pool weights
                      to scale each pool's open connections."
                  region_pools:
                    type: object
                    additionalProperties: true
                    description: A mapping of region codes to a list of pool IDs (ordered by their
                      failover priority) for the given region. Any regions not
                      explicitly defined will fall back to using default_pools.
                  rules:
                    type: array
                    items:
                      type: object
                      properties:
                        condition:
                          type: string
                          description: The condition expressions to evaluate. If the condition evaluates
                            to true, the overrides or fixed_response in this
                            rule will be applied. An empty condition is always
                            true. For more details on condition expressions,
                            please see
                            https://developers.cloudflare.com/load-balancing/understand-basics/load-balancing-rules/expressions.
                        disabled:
                          type: boolean
                          description: Disable this specific rule. It will no longer be evaluated by this
                            load balancer.
                        fixed_response:
                          type: object
                          properties:
                            content_type:
                              type: string
                              description: The http 'Content-Type' header to include in the response.
                            location:
                              type: string
                              description: The http 'Location' header to include in the response.
                            message_body:
                              type: string
                              description: Text to include as the http body.
                            status_code:
                              type: number
                              description: The http status code to respond with.
                          description: A collection of fields used to directly respond to the eyeball
                            instead of routing to a pool. If a fixed_response is
                            supplied the rule will be marked as terminates.
                        name:
                          type: string
                          description: Name of this rule. Only used for human readability.
                        overrides:
                          type: object
                          properties:
                            adaptive_routing:
                              type: object
                              properties:
                                failover_across_pools:
                                  type: boolean
                                  description: Extends zero-downtime failover of requests to healthy origins from
                                    alternate pools, when no healthy alternate
                                    exists in the same pool, according to the
                                    failover order defined by traffic and origin
                                    steering. When set false (the default)
                                    zero-downtime failover will only occur
                                    between origins within the same pool. See
                                    `session_affinity_attributes` for control
                                    over when sessions are broken or reassigned.
                              description: Controls features that modify the routing of requests to pools and
                                origins in response to dynamic conditions, such
                                as during the interval between active health
                                monitoring requests. For example, zero-downtime
                                failover occurs immediately when an origin
                                becomes unavailable due to HTTP 521, 522, or 523
                                response codes. If there is another healthy
                                origin in the same pool, the request is retried
                                once against this alternate origin.
                            country_pools:
                              type: object
                              additionalProperties: true
                              description: A mapping of country codes to a list of pool IDs (ordered by their
                                failover priority) for the given country. Any
                                country not explicitly defined will fall back to
                                using the corresponding region_pool mapping if
                                it exists else to default_pools.
                            default_pools:
                              type: array
                              items:
                                type: string
                              description: A list of pool IDs ordered by their failover priority. Pools
                                defined here are used by default, or when
                                region_pools are not configured for a given
                                region.
                            fallback_pool:
                              type: string
                              description: The pool ID to use when all other pools are detected as unhealthy.
                            location_strategy:
                              type: object
                              properties:
                                mode:
                                  type: string
                                  enum:
                                    - pop
                                    - resolver_ip
                                  description: 'Determines the authoritative location when ECS is not preferred,
                                    does not exist in the request, or its GeoIP
                                    lookup is unsuccessful.  - `"pop"`: Use the
                                    Cloudflare PoP location. - `"resolver_ip"`:
                                    Use the DNS resolver GeoIP location. If the
                                    GeoIP lookup is unsuccessful, use the
                                    Cloudflare PoP location.'
                                prefer_ecs:
                                  type: string
                                  enum:
                                    - always
                                    - never
                                    - proximity
                                    - geo
                                  description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                                    the authoritative location.  - `"always"`:
                                    Always prefer ECS. - `"never"`: Never prefer
                                    ECS. - `"proximity"`: Prefer ECS only when
                                    `steering_policy="proximity"`. - `"geo"`:
                                    Prefer ECS only when
                                    `steering_policy="geo"`.'
                              description: Controls location-based steering for non-proxied requests. See
                                `steering_policy` to learn how steering is
                                affected.
                            pop_pools:
                              type: object
                              additionalProperties: true
                              description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                                of pool IDs (ordered by their failover priority)
                                for the PoP (datacenter). Any PoPs not
                                explicitly defined will fall back to using the
                                corresponding country_pool, then region_pool
                                mapping if it exists else to default_pools."
                            random_steering:
                              type: object
                              properties:
                                default_weight:
                                  type: number
                                  description: The default weight for pools in the load balancer that are not
                                    specified in the pool_weights map.
                                pool_weights:
                                  type: object
                                  additionalProperties: true
                                  description: A mapping of pool IDs to custom weights. The weight is relative to
                                    other pools in the load balancer.
                              description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                                pool is selected with probability proportional
                                to pool weights. -
                                `steering_policy=\"least_outstanding_requests\"\
                                `: Use pool weights to scale each pool's
                                outstanding requests. -
                                `steering_policy=\"least_connections\"`: Use
                                pool weights to scale each pool's open
                                connections."
                            region_pools:
                              type: object
                              additionalProperties: true
                              description: A mapping of region codes to a list of pool IDs (ordered by their
                                failover priority) for the given region. Any
                                regions not explicitly defined will fall back to
                                using default_pools.
                            session_affinity:
                              type: string
                              enum:
                                - none
                                - cookie
                                - ip_cookie
                                - header
                              description: "Specifies the type of session affinity the load balancer should
                                use unless specified as `\"none\"`. The
                                supported types are: - `\"cookie\"`: On the
                                first request to a proxied load balancer, a
                                cookie is generated, encoding information of
                                which origin the request will be forwarded to.
                                Subsequent requests, by the same client to the
                                same load balancer, will be sent to the origin
                                server the cookie encodes, for the duration of
                                the cookie and as long as the origin server
                                remains healthy. If the cookie has expired or
                                the origin server is unhealthy, then a new
                                origin server is calculated and used. -
                                `\"ip_cookie\"`: Behaves the same as
                                `\"cookie\"` except the initial origin selection
                                is stable and based on the client's ip address.
                                - `\"header\"`: On the first request to a
                                proxied load balancer, a session key based on
                                the configured HTTP headers (see
                                `session_affinity_attributes.headers`) is
                                generated, encoding the request headers used for
                                storing in the load balancer session state which
                                origin the request will be forwarded to.
                                Subsequent requests to the load balancer with
                                the same headers will be sent to the same origin
                                server, for the duration of the session and as
                                long as the origin server remains healthy. If
                                the session has been idle for the duration of
                                `session_affinity_ttl` seconds or the origin
                                server is unhealthy, then a new origin server is
                                calculated and used. See `headers` in
                                `session_affinity_attributes` for additional
                                required configuration."
                            session_affinity_attributes:
                              type: object
                              properties:
                                drain_duration:
                                  type: number
                                  description: Configures the drain duration in seconds. This field is only used
                                    when session affinity is enabled on the load
                                    balancer.
                                headers:
                                  type: array
                                  items:
                                    type: string
                                  description: 'Configures the names of HTTP headers to base session affinity on
                                    when header `session_affinity` is enabled.
                                    At least one HTTP header name must be
                                    provided. To specify the exact cookies to be
                                    used, include an item in the following
                                    format:
                                    `"cookie:<cookie-name-1>,<cookie-name-2>"`
                                    (example) where everything after the colon
                                    is a comma-separated list of cookie names.
                                    Providing only `"cookie"` will result in all
                                    cookies being used. The default max number
                                    of HTTP header names that can be provided
                                    depends on your plan: 5 for Enterprise, 1
                                    for all other plans.'
                                require_all_headers:
                                  type: boolean
                                  description: "When header `session_affinity` is enabled, this option can be used
                                    to specify how HTTP headers on load
                                    balancing requests will be used. The
                                    supported values are: - `\"true\"`: Load
                                    balancing requests must contain _all_ of the
                                    HTTP headers specified by the `headers`
                                    session affinity attribute, otherwise
                                    sessions aren't created. - `\"false\"`: Load
                                    balancing requests must contain _at least
                                    one_ of the HTTP headers specified by the
                                    `headers` session affinity attribute,
                                    otherwise sessions aren't created."
                                samesite:
                                  type: string
                                  enum:
                                    - Auto
                                    - Lax
                                    - None
                                    - Strict
                                  description: 'Configures the SameSite attribute on session affinity cookie.
                                    Value "Auto" will be translated to "Lax" or
                                    "None" depending if Always Use HTTPS is
                                    enabled. Note: when using value "None", the
                                    secure attribute can not be set to "Never".'
                                secure:
                                  type: string
                                  enum:
                                    - Auto
                                    - Always
                                    - Never
                                  description: Configures the Secure attribute on session affinity cookie. Value
                                    "Always" indicates the Secure attribute will
                                    be set in the Set-Cookie header, "Never"
                                    indicates the Secure attribute will not be
                                    set, and "Auto" will set the Secure
                                    attribute depending if Always Use HTTPS is
                                    enabled.
                                zero_downtime_failover:
                                  type: string
                                  enum:
                                    - none
                                    - temporary
                                    - sticky
                                  description: 'Configures the zero-downtime failover between origins within a
                                    pool when session affinity is enabled. This
                                    feature is currently incompatible with Argo,
                                    Tiered Cache, and Bandwidth Alliance. The
                                    supported values are: - `"none"`: No
                                    failover takes place for sessions pinned to
                                    the origin (default). - `"temporary"`:
                                    Traffic will be sent to another other
                                    healthy origin until the originally pinned
                                    origin is available; note that this can
                                    potentially result in heavy origin flapping.
                                    - `"sticky"`: The session affinity cookie is
                                    updated and subsequent requests are sent to
                                    the new origin. Note: Zero-downtime failover
                                    with sticky sessions is currently not
                                    supported for session affinity by header.'
                              description: Configures attributes for session affinity.
                            session_affinity_ttl:
                              type: number
                              description: "Time, in seconds, until a client's session expires after being
                                created. Once the expiry time has been reached,
                                subsequent requests may get sent to a different
                                origin server. The accepted ranges per
                                `session_affinity` policy are: - `\"cookie\"` /
                                `\"ip_cookie\"`: The current default of 23 hours
                                will be used unless explicitly set. The accepted
                                range of values is between [1800, 604800]. -
                                `\"header\"`: The current default of 1800
                                seconds will be used unless explicitly set. The
                                accepted range of values is between [30, 3600].
                                Note: With session affinity by header, sessions
                                only expire after they haven't been used for the
                                number of seconds specified."
                            steering_policy:
                              type: string
                              enum:
                                - off
                                - geo
                                - random
                                - dynamic_latency
                                - proximity
                                - least_outstanding_requests
                                - least_connections
                                - ""
                              description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                                `default_pools`. - `\"geo\"`: Use
                                `region_pools`/`country_pools`/`pop_pools`. For
                                non-proxied requests, the country for
                                `country_pools` is determined by
                                `location_strategy`. - `\"random\"`: Select a
                                pool randomly. - `\"dynamic_latency\"`: Use
                                round trip time to select the closest pool in
                                default_pools (requires pool health checks). -
                                `\"proximity\"`: Use the pools' latitude and
                                longitude to select the closest pool using the
                                Cloudflare PoP location for proxied requests or
                                the location determined by `location_strategy`
                                for non-proxied requests. -
                                `\"least_outstanding_requests\"`: Select a pool
                                by taking into consideration `random_steering`
                                weights, as well as each pool's number of
                                outstanding requests. Pools with more pending
                                requests are weighted proportionately less
                                relative to others. - `\"least_connections\"`:
                                Select a pool by taking into consideration
                                `random_steering` weights, as well as each
                                pool's number of open connections. Pools with
                                more open connections are weighted
                                proportionately less relative to others.
                                Supported for HTTP/1 and HTTP/2 connections. -
                                `\"\"`: Will map to `\"geo\"` if you use
                                `region_pools`/`country_pools`/`pop_pools`
                                otherwise `\"off\"`."
                            ttl:
                              type: number
                              description: Time to live (TTL) of the DNS entry for the IP address returned by
                                this load balancer. This only applies to
                                gray-clouded (unproxied) load balancers.
                          description: A collection of overrides to apply to the load balancer when this
                            rule's condition is true. All fields are optional.
                        priority:
                          type: number
                          description: The order in which rules should be executed in relation to each
                            other. Lower values are executed first. Values do
                            not need to be sequential. If no value is provided
                            for any rule the array order of the rules field will
                            be used to assign a priority.
                        terminates:
                          type: boolean
                          description: If this rule's condition is true, this causes rule evaluation to
                            stop after processing this rule.
                    description: "BETA Field Not General Access: A list of rules for this load
                      balancer to execute."
                  session_affinity:
                    type: string
                    enum:
                      - none
                      - cookie
                      - ip_cookie
                      - header
                    description: "Specifies the type of session affinity the load balancer should
                      use unless specified as `\"none\"`. The supported types
                      are: - `\"cookie\"`: On the first request to a proxied
                      load balancer, a cookie is generated, encoding information
                      of which origin the request will be forwarded to.
                      Subsequent requests, by the same client to the same load
                      balancer, will be sent to the origin server the cookie
                      encodes, for the duration of the cookie and as long as the
                      origin server remains healthy. If the cookie has expired
                      or the origin server is unhealthy, then a new origin
                      server is calculated and used. - `\"ip_cookie\"`: Behaves
                      the same as `\"cookie\"` except the initial origin
                      selection is stable and based on the client's ip address.
                      - `\"header\"`: On the first request to a proxied load
                      balancer, a session key based on the configured HTTP
                      headers (see `session_affinity_attributes.headers`) is
                      generated, encoding the request headers used for storing
                      in the load balancer session state which origin the
                      request will be forwarded to. Subsequent requests to the
                      load balancer with the same headers will be sent to the
                      same origin server, for the duration of the session and as
                      long as the origin server remains healthy. If the session
                      has been idle for the duration of `session_affinity_ttl`
                      seconds or the origin server is unhealthy, then a new
                      origin server is calculated and used. See `headers` in
                      `session_affinity_attributes` for additional required
                      configuration."
                  session_affinity_attributes:
                    type: object
                    properties:
                      drain_duration:
                        type: number
                        description: Configures the drain duration in seconds. This field is only used
                          when session affinity is enabled on the load balancer.
                      headers:
                        type: array
                        items:
                          type: string
                        description: 'Configures the names of HTTP headers to base session affinity on
                          when header `session_affinity` is enabled. At least
                          one HTTP header name must be provided. To specify the
                          exact cookies to be used, include an item in the
                          following format:
                          `"cookie:<cookie-name-1>,<cookie-name-2>"` (example)
                          where everything after the colon is a comma-separated
                          list of cookie names. Providing only `"cookie"` will
                          result in all cookies being used. The default max
                          number of HTTP header names that can be provided
                          depends on your plan: 5 for Enterprise, 1 for all
                          other plans.'
                      require_all_headers:
                        type: boolean
                        description: "When header `session_affinity` is enabled, this option can be used
                          to specify how HTTP headers on load balancing requests
                          will be used. The supported values are: - `\"true\"`:
                          Load balancing requests must contain _all_ of the HTTP
                          headers specified by the `headers` session affinity
                          attribute, otherwise sessions aren't created. -
                          `\"false\"`: Load balancing requests must contain _at
                          least one_ of the HTTP headers specified by the
                          `headers` session affinity attribute, otherwise
                          sessions aren't created."
                      samesite:
                        type: string
                        enum:
                          - Auto
                          - Lax
                          - None
                          - Strict
                        description: 'Configures the SameSite attribute on session affinity cookie.
                          Value "Auto" will be translated to "Lax" or "None"
                          depending if Always Use HTTPS is enabled. Note: when
                          using value "None", the secure attribute can not be
                          set to "Never".'
                      secure:
                        type: string
                        enum:
                          - Auto
                          - Always
                          - Never
                        description: Configures the Secure attribute on session affinity cookie. Value
                          "Always" indicates the Secure attribute will be set in
                          the Set-Cookie header, "Never" indicates the Secure
                          attribute will not be set, and "Auto" will set the
                          Secure attribute depending if Always Use HTTPS is
                          enabled.
                      zero_downtime_failover:
                        type: string
                        enum:
                          - none
                          - temporary
                          - sticky
                        description: 'Configures the zero-downtime failover between origins within a
                          pool when session affinity is enabled. This feature is
                          currently incompatible with Argo, Tiered Cache, and
                          Bandwidth Alliance. The supported values are: -
                          `"none"`: No failover takes place for sessions pinned
                          to the origin (default). - `"temporary"`: Traffic will
                          be sent to another other healthy origin until the
                          originally pinned origin is available; note that this
                          can potentially result in heavy origin flapping. -
                          `"sticky"`: The session affinity cookie is updated and
                          subsequent requests are sent to the new origin. Note:
                          Zero-downtime failover with sticky sessions is
                          currently not supported for session affinity by
                          header.'
                    description: Configures attributes for session affinity.
                  session_affinity_ttl:
                    type: number
                    description: "Time, in seconds, until a client's session expires after being
                      created. Once the expiry time has been reached, subsequent
                      requests may get sent to a different origin server. The
                      accepted ranges per `session_affinity` policy are: -
                      `\"cookie\"` / `\"ip_cookie\"`: The current default of 23
                      hours will be used unless explicitly set. The accepted
                      range of values is between [1800, 604800]. - `\"header\"`:
                      The current default of 1800 seconds will be used unless
                      explicitly set. The accepted range of values is between
                      [30, 3600]. Note: With session affinity by header,
                      sessions only expire after they haven't been used for the
                      number of seconds specified."
                  steering_policy:
                    type: string
                    enum:
                      - off
                      - geo
                      - random
                      - dynamic_latency
                      - proximity
                      - least_outstanding_requests
                      - least_connections
                      - ""
                    description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                      `default_pools`. - `\"geo\"`: Use
                      `region_pools`/`country_pools`/`pop_pools`. For
                      non-proxied requests, the country for `country_pools` is
                      determined by `location_strategy`. - `\"random\"`: Select
                      a pool randomly. - `\"dynamic_latency\"`: Use round trip
                      time to select the closest pool in default_pools (requires
                      pool health checks). - `\"proximity\"`: Use the pools'
                      latitude and longitude to select the closest pool using
                      the Cloudflare PoP location for proxied requests or the
                      location determined by `location_strategy` for non-proxied
                      requests. - `\"least_outstanding_requests\"`: Select a
                      pool by taking into consideration `random_steering`
                      weights, as well as each pool's number of outstanding
                      requests. Pools with more pending requests are weighted
                      proportionately less relative to others. -
                      `\"least_connections\"`: Select a pool by taking into
                      consideration `random_steering` weights, as well as each
                      pool's number of open connections. Pools with more open
                      connections are weighted proportionately less relative to
                      others. Supported for HTTP/1 and HTTP/2 connections. -
                      `\"\"`: Will map to `\"geo\"` if you use
                      `region_pools`/`country_pools`/`pop_pools` otherwise
                      `\"off\"`."
                  ttl:
                    type: number
                    description: Time to live (TTL) of the DNS entry for the IP address returned by
                      this load balancer. This only applies to gray-clouded
                      (unproxied) load balancers.
                  zone_name:
                    type: string
      x-distilled-response-path: result
    patch:
      operationId: patchLoadBalancer
      description: "Apply changes to an existing load balancer, overwriting the
        supplied properties.  @example ```ts const loadBalancer = await
        client.loadBalancers.edit( '699d98642c564d2e855e9661899b7252', {
        zone_id: '699d98642c564d2e855e9661899b7252' }, ); ```"
      parameters:
        - name: loadBalancerId
          in: path
          required: true
          schema:
            type: string
        - name: zone_id
          in: path
          required: true
          description: "Path param:"
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                adaptive_routing:
                  type: object
                  properties:
                    failover_across_pools:
                      type: boolean
                      description: Extends zero-downtime failover of requests to healthy origins from
                        alternate pools, when no healthy alternate exists in the
                        same pool, according to the failover order defined by
                        traffic and origin steering. When set false (the
                        default) zero-downtime failover will only occur between
                        origins within the same pool. See
                        `session_affinity_attributes` for control over when
                        sessions are broken or reassigned.
                  description: "Body param: Controls features that modify the routing of requests
                    to pools and origins in response to dynamic conditions, such
                    as during the interval between active health monitoring
                    requests. For example, zero-downtime failover occurs
                    immediately when an origin becomes unavailable due to HTTP
                    521, 522, or 523 response codes. If there is another healthy
                    origin in the same pool, the request is retried once against
                    this alternate origin."
                country_pools:
                  type: object
                  additionalProperties: true
                  description: "Body param: A mapping of country codes to a list of pool IDs
                    (ordered by their failover priority) for the given country.
                    Any country not explicitly defined will fall back to using
                    the corresponding region_pool mapping if it exists else to
                    default_pools."
                default_pools:
                  type: array
                  items:
                    type: string
                  description: "Body param: A list of pool IDs ordered by their failover priority.
                    Pools defined here are used by default, or when region_pools
                    are not configured for a given region."
                description:
                  type: string
                  description: "Body param: Object description."
                enabled:
                  type: boolean
                  description: "Body param: Whether to enable (the default) this load balancer."
                fallback_pool:
                  type: string
                  description: "Body param: The pool ID to use when all other pools are detected
                    as unhealthy."
                location_strategy:
                  type: object
                  properties:
                    mode:
                      type: string
                      enum:
                        - pop
                        - resolver_ip
                      description: 'Determines the authoritative location when ECS is not preferred,
                        does not exist in the request, or its GeoIP lookup is
                        unsuccessful.  - `"pop"`: Use the Cloudflare PoP
                        location. - `"resolver_ip"`: Use the DNS resolver GeoIP
                        location. If the GeoIP lookup is unsuccessful, use the
                        Cloudflare PoP location.'
                    prefer_ecs:
                      type: string
                      enum:
                        - always
                        - never
                        - proximity
                        - geo
                      description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                        the authoritative location.  - `"always"`: Always prefer
                        ECS. - `"never"`: Never prefer ECS. - `"proximity"`:
                        Prefer ECS only when `steering_policy="proximity"`. -
                        `"geo"`: Prefer ECS only when `steering_policy="geo"`.'
                  description: "Body param: Controls location-based steering for non-proxied
                    requests. See `steering_policy` to learn how steering is
                    affected."
                name:
                  type: string
                  description: "Body param: The DNS hostname to associate with your Load Balancer.
                    If this hostname already exists as a DNS record in
                    Cloudflare's DNS, the Load Balancer will take precedence and
                    the DNS record will not be used."
                pop_pools:
                  type: object
                  additionalProperties: true
                  description: "Body param: Enterprise only: A mapping of Cloudflare PoP
                    identifiers to a list of pool IDs (ordered by their failover
                    priority) for the PoP (datacenter). Any PoPs not explicitly
                    defined will fall back to using the corresponding
                    country_pool, then region_pool mapping if it exists else to
                    default_pools."
                proxied:
                  type: boolean
                  description: "Body param: Whether the hostname should be gray clouded (false) or
                    orange clouded (true)."
                random_steering:
                  type: object
                  properties:
                    default_weight:
                      type: number
                      description: The default weight for pools in the load balancer that are not
                        specified in the pool_weights map.
                    pool_weights:
                      type: object
                      additionalProperties: true
                      description: A mapping of pool IDs to custom weights. The weight is relative to
                        other pools in the load balancer.
                  description: "Body param: Configures pool weights.  -
                    `steering_policy=\"random\"`: A random pool is selected with
                    probability proportional to pool weights. -
                    `steering_policy=\"least_outstanding_requests\"`: Use pool
                    weights to scale each pool's outstanding requests. -
                    `steering_policy=\"least_connections\"`: Use pool weights to
                    scale each pool's open connections."
                region_pools:
                  type: object
                  additionalProperties: true
                  description: "Body param: A mapping of region codes to a list of pool IDs
                    (ordered by their failover priority) for the given region.
                    Any regions not explicitly defined will fall back to using
                    default_pools."
                rules:
                  type: array
                  items:
                    type: object
                    properties:
                      condition:
                        type: string
                        description: The condition expressions to evaluate. If the condition evaluates
                          to true, the overrides or fixed_response in this rule
                          will be applied. An empty condition is always true.
                          For more details on condition expressions, please see
                          https://developers.cloudflare.com/load-balancing/understand-basics/load-balancing-rules/expressions.
                      disabled:
                        type: boolean
                        description: Disable this specific rule. It will no longer be evaluated by this
                          load balancer.
                      fixed_response:
                        type: object
                        properties:
                          content_type:
                            type: string
                            description: The http 'Content-Type' header to include in the response.
                          location:
                            type: string
                            description: The http 'Location' header to include in the response.
                          message_body:
                            type: string
                            description: Text to include as the http body.
                          status_code:
                            type: number
                            description: The http status code to respond with.
                        description: A collection of fields used to directly respond to the eyeball
                          instead of routing to a pool. If a fixed_response is
                          supplied the rule will be marked as terminates.
                      name:
                        type: string
                        description: Name of this rule. Only used for human readability.
                      overrides:
                        type: object
                        properties:
                          adaptive_routing:
                            type: object
                            properties:
                              failover_across_pools:
                                type: boolean
                                description: Extends zero-downtime failover of requests to healthy origins from
                                  alternate pools, when no healthy alternate
                                  exists in the same pool, according to the
                                  failover order defined by traffic and origin
                                  steering. When set false (the default)
                                  zero-downtime failover will only occur between
                                  origins within the same pool. See
                                  `session_affinity_attributes` for control over
                                  when sessions are broken or reassigned.
                            description: Controls features that modify the routing of requests to pools and
                              origins in response to dynamic conditions, such as
                              during the interval between active health
                              monitoring requests. For example, zero-downtime
                              failover occurs immediately when an origin becomes
                              unavailable due to HTTP 521, 522, or 523 response
                              codes. If there is another healthy origin in the
                              same pool, the request is retried once against
                              this alternate origin.
                          country_pools:
                            type: object
                            additionalProperties: true
                            description: A mapping of country codes to a list of pool IDs (ordered by their
                              failover priority) for the given country. Any
                              country not explicitly defined will fall back to
                              using the corresponding region_pool mapping if it
                              exists else to default_pools.
                          default_pools:
                            type: array
                            items:
                              type: string
                            description: A list of pool IDs ordered by their failover priority. Pools
                              defined here are used by default, or when
                              region_pools are not configured for a given
                              region.
                          fallback_pool:
                            type: string
                            description: The pool ID to use when all other pools are detected as unhealthy.
                          location_strategy:
                            type: object
                            properties:
                              mode:
                                type: string
                                enum:
                                  - pop
                                  - resolver_ip
                                description: 'Determines the authoritative location when ECS is not preferred,
                                  does not exist in the request, or its GeoIP
                                  lookup is unsuccessful.  - `"pop"`: Use the
                                  Cloudflare PoP location. - `"resolver_ip"`:
                                  Use the DNS resolver GeoIP location. If the
                                  GeoIP lookup is unsuccessful, use the
                                  Cloudflare PoP location.'
                              prefer_ecs:
                                type: string
                                enum:
                                  - always
                                  - never
                                  - proximity
                                  - geo
                                description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                                  the authoritative location.  - `"always"`:
                                  Always prefer ECS. - `"never"`: Never prefer
                                  ECS. - `"proximity"`: Prefer ECS only when
                                  `steering_policy="proximity"`. - `"geo"`:
                                  Prefer ECS only when `steering_policy="geo"`.'
                            description: Controls location-based steering for non-proxied requests. See
                              `steering_policy` to learn how steering is
                              affected.
                          pop_pools:
                            type: object
                            additionalProperties: true
                            description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                              of pool IDs (ordered by their failover priority)
                              for the PoP (datacenter). Any PoPs not explicitly
                              defined will fall back to using the corresponding
                              country_pool, then region_pool mapping if it
                              exists else to default_pools."
                          random_steering:
                            type: object
                            properties:
                              default_weight:
                                type: number
                                description: The default weight for pools in the load balancer that are not
                                  specified in the pool_weights map.
                              pool_weights:
                                type: object
                                additionalProperties: true
                                description: A mapping of pool IDs to custom weights. The weight is relative to
                                  other pools in the load balancer.
                            description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                              pool is selected with probability proportional to
                              pool weights. -
                              `steering_policy=\"least_outstanding_requests\"`:
                              Use pool weights to scale each pool's outstanding
                              requests. -
                              `steering_policy=\"least_connections\"`: Use pool
                              weights to scale each pool's open connections."
                          region_pools:
                            type: object
                            additionalProperties: true
                            description: A mapping of region codes to a list of pool IDs (ordered by their
                              failover priority) for the given region. Any
                              regions not explicitly defined will fall back to
                              using default_pools.
                          session_affinity:
                            type: string
                            enum:
                              - none
                              - cookie
                              - ip_cookie
                              - header
                            description: "Specifies the type of session affinity the load balancer should
                              use unless specified as `\"none\"`. The supported
                              types are: - `\"cookie\"`: On the first request to
                              a proxied load balancer, a cookie is generated,
                              encoding information of which origin the request
                              will be forwarded to. Subsequent requests, by the
                              same client to the same load balancer, will be
                              sent to the origin server the cookie encodes, for
                              the duration of the cookie and as long as the
                              origin server remains healthy. If the cookie has
                              expired or the origin server is unhealthy, then a
                              new origin server is calculated and used. -
                              `\"ip_cookie\"`: Behaves the same as `\"cookie\"`
                              except the initial origin selection is stable and
                              based on the client's ip address. - `\"header\"`:
                              On the first request to a proxied load balancer, a
                              session key based on the configured HTTP headers
                              (see `session_affinity_attributes.headers`) is
                              generated, encoding the request headers used for
                              storing in the load balancer session state which
                              origin the request will be forwarded to.
                              Subsequent requests to the load balancer with the
                              same headers will be sent to the same origin
                              server, for the duration of the session and as
                              long as the origin server remains healthy. If the
                              session has been idle for the duration of
                              `session_affinity_ttl` seconds or the origin
                              server is unhealthy, then a new origin server is
                              calculated and used. See `headers` in
                              `session_affinity_attributes` for additional
                              required configuration."
                          session_affinity_attributes:
                            type: object
                            properties:
                              drain_duration:
                                type: number
                                description: Configures the drain duration in seconds. This field is only used
                                  when session affinity is enabled on the load
                                  balancer.
                              headers:
                                type: array
                                items:
                                  type: string
                                description: 'Configures the names of HTTP headers to base session affinity on
                                  when header `session_affinity` is enabled. At
                                  least one HTTP header name must be provided.
                                  To specify the exact cookies to be used,
                                  include an item in the following format:
                                  `"cookie:<cookie-name-1>,<cookie-name-2>"`
                                  (example) where everything after the colon is
                                  a comma-separated list of cookie names.
                                  Providing only `"cookie"` will result in all
                                  cookies being used. The default max number of
                                  HTTP header names that can be provided depends
                                  on your plan: 5 for Enterprise, 1 for all
                                  other plans.'
                              require_all_headers:
                                type: boolean
                                description: "When header `session_affinity` is enabled, this option can be used
                                  to specify how HTTP headers on load balancing
                                  requests will be used. The supported values
                                  are: - `\"true\"`: Load balancing requests
                                  must contain _all_ of the HTTP headers
                                  specified by the `headers` session affinity
                                  attribute, otherwise sessions aren't created.
                                  - `\"false\"`: Load balancing requests must
                                  contain _at least one_ of the HTTP headers
                                  specified by the `headers` session affinity
                                  attribute, otherwise sessions aren't created."
                              samesite:
                                type: string
                                enum:
                                  - Auto
                                  - Lax
                                  - None
                                  - Strict
                                description: 'Configures the SameSite attribute on session affinity cookie.
                                  Value "Auto" will be translated to "Lax" or
                                  "None" depending if Always Use HTTPS is
                                  enabled. Note: when using value "None", the
                                  secure attribute can not be set to "Never".'
                              secure:
                                type: string
                                enum:
                                  - Auto
                                  - Always
                                  - Never
                                description: Configures the Secure attribute on session affinity cookie. Value
                                  "Always" indicates the Secure attribute will
                                  be set in the Set-Cookie header, "Never"
                                  indicates the Secure attribute will not be
                                  set, and "Auto" will set the Secure attribute
                                  depending if Always Use HTTPS is enabled.
                              zero_downtime_failover:
                                type: string
                                enum:
                                  - none
                                  - temporary
                                  - sticky
                                description: 'Configures the zero-downtime failover between origins within a
                                  pool when session affinity is enabled. This
                                  feature is currently incompatible with Argo,
                                  Tiered Cache, and Bandwidth Alliance. The
                                  supported values are: - `"none"`: No failover
                                  takes place for sessions pinned to the origin
                                  (default). - `"temporary"`: Traffic will be
                                  sent to another other healthy origin until the
                                  originally pinned origin is available; note
                                  that this can potentially result in heavy
                                  origin flapping. - `"sticky"`: The session
                                  affinity cookie is updated and subsequent
                                  requests are sent to the new origin. Note:
                                  Zero-downtime failover with sticky sessions is
                                  currently not supported for session affinity
                                  by header.'
                            description: Configures attributes for session affinity.
                          session_affinity_ttl:
                            type: number
                            description: "Time, in seconds, until a client's session expires after being
                              created. Once the expiry time has been reached,
                              subsequent requests may get sent to a different
                              origin server. The accepted ranges per
                              `session_affinity` policy are: - `\"cookie\"` /
                              `\"ip_cookie\"`: The current default of 23 hours
                              will be used unless explicitly set. The accepted
                              range of values is between [1800, 604800]. -
                              `\"header\"`: The current default of 1800 seconds
                              will be used unless explicitly set. The accepted
                              range of values is between [30, 3600]. Note: With
                              session affinity by header, sessions only expire
                              after they haven't been used for the number of
                              seconds specified."
                          steering_policy:
                            type: string
                            enum:
                              - off
                              - geo
                              - random
                              - dynamic_latency
                              - proximity
                              - least_outstanding_requests
                              - least_connections
                              - ""
                            description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                              `default_pools`. - `\"geo\"`: Use
                              `region_pools`/`country_pools`/`pop_pools`. For
                              non-proxied requests, the country for
                              `country_pools` is determined by
                              `location_strategy`. - `\"random\"`: Select a pool
                              randomly. - `\"dynamic_latency\"`: Use round trip
                              time to select the closest pool in default_pools
                              (requires pool health checks). - `\"proximity\"`:
                              Use the pools' latitude and longitude to select
                              the closest pool using the Cloudflare PoP location
                              for proxied requests or the location determined by
                              `location_strategy` for non-proxied requests. -
                              `\"least_outstanding_requests\"`: Select a pool by
                              taking into consideration `random_steering`
                              weights, as well as each pool's number of
                              outstanding requests. Pools with more pending
                              requests are weighted proportionately less
                              relative to others. - `\"least_connections\"`:
                              Select a pool by taking into consideration
                              `random_steering` weights, as well as each pool's
                              number of open connections. Pools with more open
                              connections are weighted proportionately less
                              relative to others. Supported for HTTP/1 and
                              HTTP/2 connections. - `\"\"`: Will map to
                              `\"geo\"` if you use
                              `region_pools`/`country_pools`/`pop_pools`
                              otherwise `\"off\"`."
                          ttl:
                            type: number
                            description: Time to live (TTL) of the DNS entry for the IP address returned by
                              this load balancer. This only applies to
                              gray-clouded (unproxied) load balancers.
                        description: A collection of overrides to apply to the load balancer when this
                          rule's condition is true. All fields are optional.
                      priority:
                        type: number
                        description: The order in which rules should be executed in relation to each
                          other. Lower values are executed first. Values do not
                          need to be sequential. If no value is provided for any
                          rule the array order of the rules field will be used
                          to assign a priority.
                      terminates:
                        type: boolean
                        description: If this rule's condition is true, this causes rule evaluation to
                          stop after processing this rule.
                  description: "Body param: BETA Field Not General Access: A list of rules for
                    this load balancer to execute."
                session_affinity:
                  type: string
                  enum:
                    - none
                    - cookie
                    - ip_cookie
                    - header
                  description: "Body param: Specifies the type of session affinity the load
                    balancer should use unless specified as `\"none\"`. The
                    supported types are: - `\"cookie\"`: On the first request to
                    a proxied load balancer, a cookie is generated, encoding
                    information of which origin the request will be forwarded
                    to. Subsequent requests, by the same client to the same load
                    balancer, will be sent to the origin server the cookie
                    encodes, for the duration of the cookie and as long as the
                    origin server remains healthy. If the cookie has expired or
                    the origin server is unhealthy, then a new origin server is
                    calculated and used. - `\"ip_cookie\"`: Behaves the same as
                    `\"cookie\"` except the initial origin selection is stable
                    and based on the client's ip address. - `\"header\"`: On the
                    first request to a proxied load balancer, a session key
                    based on the configured HTTP headers (see
                    `session_affinity_attributes.headers`) is generated,
                    encoding the request headers used for storing in the load
                    balancer session state which origin the request will be
                    forwarded to. Subsequent requests to the load balancer with
                    the same headers will be sent to the same origin server, for
                    the duration of the session and as long as the origin server
                    remains healthy. If the session has been idle for the
                    duration of `session_affinity_ttl` seconds or the origin
                    server is unhealthy, then a new origin server is calculated
                    and used. See `headers` in `session_affinity_attributes` for
                    additional required configuration."
                session_affinity_attributes:
                  type: object
                  properties:
                    drain_duration:
                      type: number
                      description: Configures the drain duration in seconds. This field is only used
                        when session affinity is enabled on the load balancer.
                    headers:
                      type: array
                      items:
                        type: string
                      description: 'Configures the names of HTTP headers to base session affinity on
                        when header `session_affinity` is enabled. At least one
                        HTTP header name must be provided. To specify the exact
                        cookies to be used, include an item in the following
                        format: `"cookie:<cookie-name-1>,<cookie-name-2>"`
                        (example) where everything after the colon is a
                        comma-separated list of cookie names. Providing only
                        `"cookie"` will result in all cookies being used. The
                        default max number of HTTP header names that can be
                        provided depends on your plan: 5 for Enterprise, 1 for
                        all other plans.'
                    require_all_headers:
                      type: boolean
                      description: "When header `session_affinity` is enabled, this option can be used
                        to specify how HTTP headers on load balancing requests
                        will be used. The supported values are: - `\"true\"`:
                        Load balancing requests must contain _all_ of the HTTP
                        headers specified by the `headers` session affinity
                        attribute, otherwise sessions aren't created. -
                        `\"false\"`: Load balancing requests must contain _at
                        least one_ of the HTTP headers specified by the
                        `headers` session affinity attribute, otherwise sessions
                        aren't created."
                    samesite:
                      type: string
                      enum:
                        - Auto
                        - Lax
                        - None
                        - Strict
                      description: 'Configures the SameSite attribute on session affinity cookie.
                        Value "Auto" will be translated to "Lax" or "None"
                        depending if Always Use HTTPS is enabled. Note: when
                        using value "None", the secure attribute can not be set
                        to "Never".'
                    secure:
                      type: string
                      enum:
                        - Auto
                        - Always
                        - Never
                      description: Configures the Secure attribute on session affinity cookie. Value
                        "Always" indicates the Secure attribute will be set in
                        the Set-Cookie header, "Never" indicates the Secure
                        attribute will not be set, and "Auto" will set the
                        Secure attribute depending if Always Use HTTPS is
                        enabled.
                    zero_downtime_failover:
                      type: string
                      enum:
                        - none
                        - temporary
                        - sticky
                      description: 'Configures the zero-downtime failover between origins within a
                        pool when session affinity is enabled. This feature is
                        currently incompatible with Argo, Tiered Cache, and
                        Bandwidth Alliance. The supported values are: -
                        `"none"`: No failover takes place for sessions pinned to
                        the origin (default). - `"temporary"`: Traffic will be
                        sent to another other healthy origin until the
                        originally pinned origin is available; note that this
                        can potentially result in heavy origin flapping. -
                        `"sticky"`: The session affinity cookie is updated and
                        subsequent requests are sent to the new origin. Note:
                        Zero-downtime failover with sticky sessions is currently
                        not supported for session affinity by header.'
                  description: "Body param: Configures attributes for session affinity."
                session_affinity_ttl:
                  type: number
                  description: "Body param: Time, in seconds, until a client's session expires
                    after being created. Once the expiry time has been reached,
                    subsequent requests may get sent to a different origin
                    server. The accepted ranges per `session_affinity` policy
                    are: - `\"cookie\"` / `\"ip_cookie\"`: The current default
                    of 23 hours will be used unless explicitly set. The accepted
                    range of values is between [1800, 604800]. - `\"header\"`:
                    The current default of 1800 seconds will be used unless
                    explicitly set. The accepted range of values is between [30,
                    3600]. Note: With session affinity by header, sessions only
                    expire after they haven't been used for the number of
                    seconds specified."
                steering_policy:
                  type: string
                  enum:
                    - off
                    - geo
                    - random
                    - dynamic_latency
                    - proximity
                    - least_outstanding_requests
                    - least_connections
                    - ""
                  description: "Body param: Steering Policy for this load balancer.  - `\"off\"`:
                    Use `default_pools`. - `\"geo\"`: Use
                    `region_pools`/`country_pools`/`pop_pools`. For non-proxied
                    requests, the country for `country_pools` is determined by
                    `location_strategy`. - `\"random\"`: Select a pool randomly.
                    - `\"dynamic_latency\"`: Use round trip time to select the
                    closest pool in default_pools (requires pool health checks).
                    - `\"proximity\"`: Use the pools' latitude and longitude to
                    select the closest pool using the Cloudflare PoP location
                    for proxied requests or the location determined by
                    `location_strategy` for non-proxied requests. -
                    `\"least_outstanding_requests\"`: Select a pool by taking
                    into consideration `random_steering` weights, as well as
                    each pool's number of outstanding requests. Pools with more
                    pending requests are weighted proportionately less relative
                    to others. - `\"least_connections\"`: Select a pool by
                    taking into consideration `random_steering` weights, as well
                    as each pool's number of open connections. Pools with more
                    open connections are weighted proportionately less relative
                    to others. Supported for HTTP/1 and HTTP/2 connections. -
                    `\"\"`: Will map to `\"geo\"` if you use
                    `region_pools`/`country_pools`/`pop_pools` otherwise
                    `\"off\"`."
                ttl:
                  type: number
                  description: "Body param: Time to live (TTL) of the DNS entry for the IP address
                    returned by this load balancer. This only applies to
                    gray-clouded (unproxied) load balancers."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  adaptive_routing:
                    type: object
                    properties:
                      failover_across_pools:
                        type: boolean
                        description: Extends zero-downtime failover of requests to healthy origins from
                          alternate pools, when no healthy alternate exists in
                          the same pool, according to the failover order defined
                          by traffic and origin steering. When set false (the
                          default) zero-downtime failover will only occur
                          between origins within the same pool. See
                          `session_affinity_attributes` for control over when
                          sessions are broken or reassigned.
                    description: Controls features that modify the routing of requests to pools and
                      origins in response to dynamic conditions, such as during
                      the interval between active health monitoring requests.
                      For example, zero-downtime failover occurs immediately
                      when an origin becomes unavailable due to HTTP 521, 522,
                      or 523 response codes. If there is another healthy origin
                      in the same pool, the request is retried once against this
                      alternate origin.
                  country_pools:
                    type: object
                    additionalProperties: true
                    description: A mapping of country codes to a list of pool IDs (ordered by their
                      failover priority) for the given country. Any country not
                      explicitly defined will fall back to using the
                      corresponding region_pool mapping if it exists else to
                      default_pools.
                  created_on:
                    type: string
                  default_pools:
                    type: array
                    items:
                      type: string
                    description: A list of pool IDs ordered by their failover priority. Pools
                      defined here are used by default, or when region_pools are
                      not configured for a given region.
                  description:
                    type: string
                    description: Object description.
                  enabled:
                    type: boolean
                    description: Whether to enable (the default) this load balancer.
                  fallback_pool:
                    type: string
                    description: The pool ID to use when all other pools are detected as unhealthy.
                  location_strategy:
                    type: object
                    properties:
                      mode:
                        type: string
                        enum:
                          - pop
                          - resolver_ip
                        description: 'Determines the authoritative location when ECS is not preferred,
                          does not exist in the request, or its GeoIP lookup is
                          unsuccessful.  - `"pop"`: Use the Cloudflare PoP
                          location. - `"resolver_ip"`: Use the DNS resolver
                          GeoIP location. If the GeoIP lookup is unsuccessful,
                          use the Cloudflare PoP location.'
                      prefer_ecs:
                        type: string
                        enum:
                          - always
                          - never
                          - proximity
                          - geo
                        description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                          the authoritative location.  - `"always"`: Always
                          prefer ECS. - `"never"`: Never prefer ECS. -
                          `"proximity"`: Prefer ECS only when
                          `steering_policy="proximity"`. - `"geo"`: Prefer ECS
                          only when `steering_policy="geo"`.'
                    description: Controls location-based steering for non-proxied requests. See
                      `steering_policy` to learn how steering is affected.
                  modified_on:
                    type: string
                  name:
                    type: string
                    description: The DNS hostname to associate with your Load Balancer. If this
                      hostname already exists as a DNS record in Cloudflare's
                      DNS, the Load Balancer will take precedence and the DNS
                      record will not be used.
                  networks:
                    type: array
                    items:
                      type: string
                    description: List of networks where Load Balancer or Pool is enabled.
                  pop_pools:
                    type: object
                    additionalProperties: true
                    description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                      of pool IDs (ordered by their failover priority) for the
                      PoP (datacenter). Any PoPs not explicitly defined will
                      fall back to using the corresponding country_pool, then
                      region_pool mapping if it exists else to default_pools."
                  proxied:
                    type: boolean
                    description: Whether the hostname should be gray clouded (false) or orange
                      clouded (true).
                  random_steering:
                    type: object
                    properties:
                      default_weight:
                        type: number
                        description: The default weight for pools in the load balancer that are not
                          specified in the pool_weights map.
                      pool_weights:
                        type: object
                        additionalProperties: true
                        description: A mapping of pool IDs to custom weights. The weight is relative to
                          other pools in the load balancer.
                    description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                      pool is selected with probability proportional to pool
                      weights. -
                      `steering_policy=\"least_outstanding_requests\"`: Use pool
                      weights to scale each pool's outstanding requests. -
                      `steering_policy=\"least_connections\"`: Use pool weights
                      to scale each pool's open connections."
                  region_pools:
                    type: object
                    additionalProperties: true
                    description: A mapping of region codes to a list of pool IDs (ordered by their
                      failover priority) for the given region. Any regions not
                      explicitly defined will fall back to using default_pools.
                  rules:
                    type: array
                    items:
                      type: object
                      properties:
                        condition:
                          type: string
                          description: The condition expressions to evaluate. If the condition evaluates
                            to true, the overrides or fixed_response in this
                            rule will be applied. An empty condition is always
                            true. For more details on condition expressions,
                            please see
                            https://developers.cloudflare.com/load-balancing/understand-basics/load-balancing-rules/expressions.
                        disabled:
                          type: boolean
                          description: Disable this specific rule. It will no longer be evaluated by this
                            load balancer.
                        fixed_response:
                          type: object
                          properties:
                            content_type:
                              type: string
                              description: The http 'Content-Type' header to include in the response.
                            location:
                              type: string
                              description: The http 'Location' header to include in the response.
                            message_body:
                              type: string
                              description: Text to include as the http body.
                            status_code:
                              type: number
                              description: The http status code to respond with.
                          description: A collection of fields used to directly respond to the eyeball
                            instead of routing to a pool. If a fixed_response is
                            supplied the rule will be marked as terminates.
                        name:
                          type: string
                          description: Name of this rule. Only used for human readability.
                        overrides:
                          type: object
                          properties:
                            adaptive_routing:
                              type: object
                              properties:
                                failover_across_pools:
                                  type: boolean
                                  description: Extends zero-downtime failover of requests to healthy origins from
                                    alternate pools, when no healthy alternate
                                    exists in the same pool, according to the
                                    failover order defined by traffic and origin
                                    steering. When set false (the default)
                                    zero-downtime failover will only occur
                                    between origins within the same pool. See
                                    `session_affinity_attributes` for control
                                    over when sessions are broken or reassigned.
                              description: Controls features that modify the routing of requests to pools and
                                origins in response to dynamic conditions, such
                                as during the interval between active health
                                monitoring requests. For example, zero-downtime
                                failover occurs immediately when an origin
                                becomes unavailable due to HTTP 521, 522, or 523
                                response codes. If there is another healthy
                                origin in the same pool, the request is retried
                                once against this alternate origin.
                            country_pools:
                              type: object
                              additionalProperties: true
                              description: A mapping of country codes to a list of pool IDs (ordered by their
                                failover priority) for the given country. Any
                                country not explicitly defined will fall back to
                                using the corresponding region_pool mapping if
                                it exists else to default_pools.
                            default_pools:
                              type: array
                              items:
                                type: string
                              description: A list of pool IDs ordered by their failover priority. Pools
                                defined here are used by default, or when
                                region_pools are not configured for a given
                                region.
                            fallback_pool:
                              type: string
                              description: The pool ID to use when all other pools are detected as unhealthy.
                            location_strategy:
                              type: object
                              properties:
                                mode:
                                  type: string
                                  enum:
                                    - pop
                                    - resolver_ip
                                  description: 'Determines the authoritative location when ECS is not preferred,
                                    does not exist in the request, or its GeoIP
                                    lookup is unsuccessful.  - `"pop"`: Use the
                                    Cloudflare PoP location. - `"resolver_ip"`:
                                    Use the DNS resolver GeoIP location. If the
                                    GeoIP lookup is unsuccessful, use the
                                    Cloudflare PoP location.'
                                prefer_ecs:
                                  type: string
                                  enum:
                                    - always
                                    - never
                                    - proximity
                                    - geo
                                  description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                                    the authoritative location.  - `"always"`:
                                    Always prefer ECS. - `"never"`: Never prefer
                                    ECS. - `"proximity"`: Prefer ECS only when
                                    `steering_policy="proximity"`. - `"geo"`:
                                    Prefer ECS only when
                                    `steering_policy="geo"`.'
                              description: Controls location-based steering for non-proxied requests. See
                                `steering_policy` to learn how steering is
                                affected.
                            pop_pools:
                              type: object
                              additionalProperties: true
                              description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                                of pool IDs (ordered by their failover priority)
                                for the PoP (datacenter). Any PoPs not
                                explicitly defined will fall back to using the
                                corresponding country_pool, then region_pool
                                mapping if it exists else to default_pools."
                            random_steering:
                              type: object
                              properties:
                                default_weight:
                                  type: number
                                  description: The default weight for pools in the load balancer that are not
                                    specified in the pool_weights map.
                                pool_weights:
                                  type: object
                                  additionalProperties: true
                                  description: A mapping of pool IDs to custom weights. The weight is relative to
                                    other pools in the load balancer.
                              description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                                pool is selected with probability proportional
                                to pool weights. -
                                `steering_policy=\"least_outstanding_requests\"\
                                `: Use pool weights to scale each pool's
                                outstanding requests. -
                                `steering_policy=\"least_connections\"`: Use
                                pool weights to scale each pool's open
                                connections."
                            region_pools:
                              type: object
                              additionalProperties: true
                              description: A mapping of region codes to a list of pool IDs (ordered by their
                                failover priority) for the given region. Any
                                regions not explicitly defined will fall back to
                                using default_pools.
                            session_affinity:
                              type: string
                              enum:
                                - none
                                - cookie
                                - ip_cookie
                                - header
                              description: "Specifies the type of session affinity the load balancer should
                                use unless specified as `\"none\"`. The
                                supported types are: - `\"cookie\"`: On the
                                first request to a proxied load balancer, a
                                cookie is generated, encoding information of
                                which origin the request will be forwarded to.
                                Subsequent requests, by the same client to the
                                same load balancer, will be sent to the origin
                                server the cookie encodes, for the duration of
                                the cookie and as long as the origin server
                                remains healthy. If the cookie has expired or
                                the origin server is unhealthy, then a new
                                origin server is calculated and used. -
                                `\"ip_cookie\"`: Behaves the same as
                                `\"cookie\"` except the initial origin selection
                                is stable and based on the client's ip address.
                                - `\"header\"`: On the first request to a
                                proxied load balancer, a session key based on
                                the configured HTTP headers (see
                                `session_affinity_attributes.headers`) is
                                generated, encoding the request headers used for
                                storing in the load balancer session state which
                                origin the request will be forwarded to.
                                Subsequent requests to the load balancer with
                                the same headers will be sent to the same origin
                                server, for the duration of the session and as
                                long as the origin server remains healthy. If
                                the session has been idle for the duration of
                                `session_affinity_ttl` seconds or the origin
                                server is unhealthy, then a new origin server is
                                calculated and used. See `headers` in
                                `session_affinity_attributes` for additional
                                required configuration."
                            session_affinity_attributes:
                              type: object
                              properties:
                                drain_duration:
                                  type: number
                                  description: Configures the drain duration in seconds. This field is only used
                                    when session affinity is enabled on the load
                                    balancer.
                                headers:
                                  type: array
                                  items:
                                    type: string
                                  description: 'Configures the names of HTTP headers to base session affinity on
                                    when header `session_affinity` is enabled.
                                    At least one HTTP header name must be
                                    provided. To specify the exact cookies to be
                                    used, include an item in the following
                                    format:
                                    `"cookie:<cookie-name-1>,<cookie-name-2>"`
                                    (example) where everything after the colon
                                    is a comma-separated list of cookie names.
                                    Providing only `"cookie"` will result in all
                                    cookies being used. The default max number
                                    of HTTP header names that can be provided
                                    depends on your plan: 5 for Enterprise, 1
                                    for all other plans.'
                                require_all_headers:
                                  type: boolean
                                  description: "When header `session_affinity` is enabled, this option can be used
                                    to specify how HTTP headers on load
                                    balancing requests will be used. The
                                    supported values are: - `\"true\"`: Load
                                    balancing requests must contain _all_ of the
                                    HTTP headers specified by the `headers`
                                    session affinity attribute, otherwise
                                    sessions aren't created. - `\"false\"`: Load
                                    balancing requests must contain _at least
                                    one_ of the HTTP headers specified by the
                                    `headers` session affinity attribute,
                                    otherwise sessions aren't created."
                                samesite:
                                  type: string
                                  enum:
                                    - Auto
                                    - Lax
                                    - None
                                    - Strict
                                  description: 'Configures the SameSite attribute on session affinity cookie.
                                    Value "Auto" will be translated to "Lax" or
                                    "None" depending if Always Use HTTPS is
                                    enabled. Note: when using value "None", the
                                    secure attribute can not be set to "Never".'
                                secure:
                                  type: string
                                  enum:
                                    - Auto
                                    - Always
                                    - Never
                                  description: Configures the Secure attribute on session affinity cookie. Value
                                    "Always" indicates the Secure attribute will
                                    be set in the Set-Cookie header, "Never"
                                    indicates the Secure attribute will not be
                                    set, and "Auto" will set the Secure
                                    attribute depending if Always Use HTTPS is
                                    enabled.
                                zero_downtime_failover:
                                  type: string
                                  enum:
                                    - none
                                    - temporary
                                    - sticky
                                  description: 'Configures the zero-downtime failover between origins within a
                                    pool when session affinity is enabled. This
                                    feature is currently incompatible with Argo,
                                    Tiered Cache, and Bandwidth Alliance. The
                                    supported values are: - `"none"`: No
                                    failover takes place for sessions pinned to
                                    the origin (default). - `"temporary"`:
                                    Traffic will be sent to another other
                                    healthy origin until the originally pinned
                                    origin is available; note that this can
                                    potentially result in heavy origin flapping.
                                    - `"sticky"`: The session affinity cookie is
                                    updated and subsequent requests are sent to
                                    the new origin. Note: Zero-downtime failover
                                    with sticky sessions is currently not
                                    supported for session affinity by header.'
                              description: Configures attributes for session affinity.
                            session_affinity_ttl:
                              type: number
                              description: "Time, in seconds, until a client's session expires after being
                                created. Once the expiry time has been reached,
                                subsequent requests may get sent to a different
                                origin server. The accepted ranges per
                                `session_affinity` policy are: - `\"cookie\"` /
                                `\"ip_cookie\"`: The current default of 23 hours
                                will be used unless explicitly set. The accepted
                                range of values is between [1800, 604800]. -
                                `\"header\"`: The current default of 1800
                                seconds will be used unless explicitly set. The
                                accepted range of values is between [30, 3600].
                                Note: With session affinity by header, sessions
                                only expire after they haven't been used for the
                                number of seconds specified."
                            steering_policy:
                              type: string
                              enum:
                                - off
                                - geo
                                - random
                                - dynamic_latency
                                - proximity
                                - least_outstanding_requests
                                - least_connections
                                - ""
                              description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                                `default_pools`. - `\"geo\"`: Use
                                `region_pools`/`country_pools`/`pop_pools`. For
                                non-proxied requests, the country for
                                `country_pools` is determined by
                                `location_strategy`. - `\"random\"`: Select a
                                pool randomly. - `\"dynamic_latency\"`: Use
                                round trip time to select the closest pool in
                                default_pools (requires pool health checks). -
                                `\"proximity\"`: Use the pools' latitude and
                                longitude to select the closest pool using the
                                Cloudflare PoP location for proxied requests or
                                the location determined by `location_strategy`
                                for non-proxied requests. -
                                `\"least_outstanding_requests\"`: Select a pool
                                by taking into consideration `random_steering`
                                weights, as well as each pool's number of
                                outstanding requests. Pools with more pending
                                requests are weighted proportionately less
                                relative to others. - `\"least_connections\"`:
                                Select a pool by taking into consideration
                                `random_steering` weights, as well as each
                                pool's number of open connections. Pools with
                                more open connections are weighted
                                proportionately less relative to others.
                                Supported for HTTP/1 and HTTP/2 connections. -
                                `\"\"`: Will map to `\"geo\"` if you use
                                `region_pools`/`country_pools`/`pop_pools`
                                otherwise `\"off\"`."
                            ttl:
                              type: number
                              description: Time to live (TTL) of the DNS entry for the IP address returned by
                                this load balancer. This only applies to
                                gray-clouded (unproxied) load balancers.
                          description: A collection of overrides to apply to the load balancer when this
                            rule's condition is true. All fields are optional.
                        priority:
                          type: number
                          description: The order in which rules should be executed in relation to each
                            other. Lower values are executed first. Values do
                            not need to be sequential. If no value is provided
                            for any rule the array order of the rules field will
                            be used to assign a priority.
                        terminates:
                          type: boolean
                          description: If this rule's condition is true, this causes rule evaluation to
                            stop after processing this rule.
                    description: "BETA Field Not General Access: A list of rules for this load
                      balancer to execute."
                  session_affinity:
                    type: string
                    enum:
                      - none
                      - cookie
                      - ip_cookie
                      - header
                    description: "Specifies the type of session affinity the load balancer should
                      use unless specified as `\"none\"`. The supported types
                      are: - `\"cookie\"`: On the first request to a proxied
                      load balancer, a cookie is generated, encoding information
                      of which origin the request will be forwarded to.
                      Subsequent requests, by the same client to the same load
                      balancer, will be sent to the origin server the cookie
                      encodes, for the duration of the cookie and as long as the
                      origin server remains healthy. If the cookie has expired
                      or the origin server is unhealthy, then a new origin
                      server is calculated and used. - `\"ip_cookie\"`: Behaves
                      the same as `\"cookie\"` except the initial origin
                      selection is stable and based on the client's ip address.
                      - `\"header\"`: On the first request to a proxied load
                      balancer, a session key based on the configured HTTP
                      headers (see `session_affinity_attributes.headers`) is
                      generated, encoding the request headers used for storing
                      in the load balancer session state which origin the
                      request will be forwarded to. Subsequent requests to the
                      load balancer with the same headers will be sent to the
                      same origin server, for the duration of the session and as
                      long as the origin server remains healthy. If the session
                      has been idle for the duration of `session_affinity_ttl`
                      seconds or the origin server is unhealthy, then a new
                      origin server is calculated and used. See `headers` in
                      `session_affinity_attributes` for additional required
                      configuration."
                  session_affinity_attributes:
                    type: object
                    properties:
                      drain_duration:
                        type: number
                        description: Configures the drain duration in seconds. This field is only used
                          when session affinity is enabled on the load balancer.
                      headers:
                        type: array
                        items:
                          type: string
                        description: 'Configures the names of HTTP headers to base session affinity on
                          when header `session_affinity` is enabled. At least
                          one HTTP header name must be provided. To specify the
                          exact cookies to be used, include an item in the
                          following format:
                          `"cookie:<cookie-name-1>,<cookie-name-2>"` (example)
                          where everything after the colon is a comma-separated
                          list of cookie names. Providing only `"cookie"` will
                          result in all cookies being used. The default max
                          number of HTTP header names that can be provided
                          depends on your plan: 5 for Enterprise, 1 for all
                          other plans.'
                      require_all_headers:
                        type: boolean
                        description: "When header `session_affinity` is enabled, this option can be used
                          to specify how HTTP headers on load balancing requests
                          will be used. The supported values are: - `\"true\"`:
                          Load balancing requests must contain _all_ of the HTTP
                          headers specified by the `headers` session affinity
                          attribute, otherwise sessions aren't created. -
                          `\"false\"`: Load balancing requests must contain _at
                          least one_ of the HTTP headers specified by the
                          `headers` session affinity attribute, otherwise
                          sessions aren't created."
                      samesite:
                        type: string
                        enum:
                          - Auto
                          - Lax
                          - None
                          - Strict
                        description: 'Configures the SameSite attribute on session affinity cookie.
                          Value "Auto" will be translated to "Lax" or "None"
                          depending if Always Use HTTPS is enabled. Note: when
                          using value "None", the secure attribute can not be
                          set to "Never".'
                      secure:
                        type: string
                        enum:
                          - Auto
                          - Always
                          - Never
                        description: Configures the Secure attribute on session affinity cookie. Value
                          "Always" indicates the Secure attribute will be set in
                          the Set-Cookie header, "Never" indicates the Secure
                          attribute will not be set, and "Auto" will set the
                          Secure attribute depending if Always Use HTTPS is
                          enabled.
                      zero_downtime_failover:
                        type: string
                        enum:
                          - none
                          - temporary
                          - sticky
                        description: 'Configures the zero-downtime failover between origins within a
                          pool when session affinity is enabled. This feature is
                          currently incompatible with Argo, Tiered Cache, and
                          Bandwidth Alliance. The supported values are: -
                          `"none"`: No failover takes place for sessions pinned
                          to the origin (default). - `"temporary"`: Traffic will
                          be sent to another other healthy origin until the
                          originally pinned origin is available; note that this
                          can potentially result in heavy origin flapping. -
                          `"sticky"`: The session affinity cookie is updated and
                          subsequent requests are sent to the new origin. Note:
                          Zero-downtime failover with sticky sessions is
                          currently not supported for session affinity by
                          header.'
                    description: Configures attributes for session affinity.
                  session_affinity_ttl:
                    type: number
                    description: "Time, in seconds, until a client's session expires after being
                      created. Once the expiry time has been reached, subsequent
                      requests may get sent to a different origin server. The
                      accepted ranges per `session_affinity` policy are: -
                      `\"cookie\"` / `\"ip_cookie\"`: The current default of 23
                      hours will be used unless explicitly set. The accepted
                      range of values is between [1800, 604800]. - `\"header\"`:
                      The current default of 1800 seconds will be used unless
                      explicitly set. The accepted range of values is between
                      [30, 3600]. Note: With session affinity by header,
                      sessions only expire after they haven't been used for the
                      number of seconds specified."
                  steering_policy:
                    type: string
                    enum:
                      - off
                      - geo
                      - random
                      - dynamic_latency
                      - proximity
                      - least_outstanding_requests
                      - least_connections
                      - ""
                    description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                      `default_pools`. - `\"geo\"`: Use
                      `region_pools`/`country_pools`/`pop_pools`. For
                      non-proxied requests, the country for `country_pools` is
                      determined by `location_strategy`. - `\"random\"`: Select
                      a pool randomly. - `\"dynamic_latency\"`: Use round trip
                      time to select the closest pool in default_pools (requires
                      pool health checks). - `\"proximity\"`: Use the pools'
                      latitude and longitude to select the closest pool using
                      the Cloudflare PoP location for proxied requests or the
                      location determined by `location_strategy` for non-proxied
                      requests. - `\"least_outstanding_requests\"`: Select a
                      pool by taking into consideration `random_steering`
                      weights, as well as each pool's number of outstanding
                      requests. Pools with more pending requests are weighted
                      proportionately less relative to others. -
                      `\"least_connections\"`: Select a pool by taking into
                      consideration `random_steering` weights, as well as each
                      pool's number of open connections. Pools with more open
                      connections are weighted proportionately less relative to
                      others. Supported for HTTP/1 and HTTP/2 connections. -
                      `\"\"`: Will map to `\"geo\"` if you use
                      `region_pools`/`country_pools`/`pop_pools` otherwise
                      `\"off\"`."
                  ttl:
                    type: number
                    description: Time to live (TTL) of the DNS entry for the IP address returned by
                      this load balancer. This only applies to gray-clouded
                      (unproxied) load balancers.
                  zone_name:
                    type: string
      x-distilled-response-path: result
    delete:
      operationId: deleteLoadBalancer
      description: "Delete a configured load balancer.  @example ```ts const
        loadBalancer = await client.loadBalancers.delete(
        '699d98642c564d2e855e9661899b7252', { zone_id:
        '699d98642c564d2e855e9661899b7252' }, ); ```"
      parameters:
        - name: loadBalancerId
          in: path
          required: true
          schema:
            type: string
        - name: zone_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
      x-distilled-response-path: result
  /zones/{zone_id}/load_balancers:
    get:
      operationId: listLoadBalancers
      description: "List configured load balancers.  @example ```ts // Automatically
        fetches more pages as needed. for await (const loadBalancer of
        client.loadBalancers.list({ zone_id: '699d98642c564d2e855e9661899b7252',
        })) { // ... } ```"
      parameters:
        - name: zone_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  adaptive_routing:
                    type: object
                    properties:
                      failover_across_pools:
                        type: boolean
                        description: Extends zero-downtime failover of requests to healthy origins from
                          alternate pools, when no healthy alternate exists in
                          the same pool, according to the failover order defined
                          by traffic and origin steering. When set false (the
                          default) zero-downtime failover will only occur
                          between origins within the same pool. See
                          `session_affinity_attributes` for control over when
                          sessions are broken or reassigned.
                    description: Controls features that modify the routing of requests to pools and
                      origins in response to dynamic conditions, such as during
                      the interval between active health monitoring requests.
                      For example, zero-downtime failover occurs immediately
                      when an origin becomes unavailable due to HTTP 521, 522,
                      or 523 response codes. If there is another healthy origin
                      in the same pool, the request is retried once against this
                      alternate origin.
                  country_pools:
                    type: object
                    additionalProperties: true
                    description: A mapping of country codes to a list of pool IDs (ordered by their
                      failover priority) for the given country. Any country not
                      explicitly defined will fall back to using the
                      corresponding region_pool mapping if it exists else to
                      default_pools.
                  created_on:
                    type: string
                  default_pools:
                    type: array
                    items:
                      type: string
                    description: A list of pool IDs ordered by their failover priority. Pools
                      defined here are used by default, or when region_pools are
                      not configured for a given region.
                  description:
                    type: string
                    description: Object description.
                  enabled:
                    type: boolean
                    description: Whether to enable (the default) this load balancer.
                  fallback_pool:
                    type: string
                    description: The pool ID to use when all other pools are detected as unhealthy.
                  location_strategy:
                    type: object
                    properties:
                      mode:
                        type: string
                        enum:
                          - pop
                          - resolver_ip
                        description: 'Determines the authoritative location when ECS is not preferred,
                          does not exist in the request, or its GeoIP lookup is
                          unsuccessful.  - `"pop"`: Use the Cloudflare PoP
                          location. - `"resolver_ip"`: Use the DNS resolver
                          GeoIP location. If the GeoIP lookup is unsuccessful,
                          use the Cloudflare PoP location.'
                      prefer_ecs:
                        type: string
                        enum:
                          - always
                          - never
                          - proximity
                          - geo
                        description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                          the authoritative location.  - `"always"`: Always
                          prefer ECS. - `"never"`: Never prefer ECS. -
                          `"proximity"`: Prefer ECS only when
                          `steering_policy="proximity"`. - `"geo"`: Prefer ECS
                          only when `steering_policy="geo"`.'
                    description: Controls location-based steering for non-proxied requests. See
                      `steering_policy` to learn how steering is affected.
                  modified_on:
                    type: string
                  name:
                    type: string
                    description: The DNS hostname to associate with your Load Balancer. If this
                      hostname already exists as a DNS record in Cloudflare's
                      DNS, the Load Balancer will take precedence and the DNS
                      record will not be used.
                  networks:
                    type: array
                    items:
                      type: string
                    description: List of networks where Load Balancer or Pool is enabled.
                  pop_pools:
                    type: object
                    additionalProperties: true
                    description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                      of pool IDs (ordered by their failover priority) for the
                      PoP (datacenter). Any PoPs not explicitly defined will
                      fall back to using the corresponding country_pool, then
                      region_pool mapping if it exists else to default_pools."
                  proxied:
                    type: boolean
                    description: Whether the hostname should be gray clouded (false) or orange
                      clouded (true).
                  random_steering:
                    type: object
                    properties:
                      default_weight:
                        type: number
                        description: The default weight for pools in the load balancer that are not
                          specified in the pool_weights map.
                      pool_weights:
                        type: object
                        additionalProperties: true
                        description: A mapping of pool IDs to custom weights. The weight is relative to
                          other pools in the load balancer.
                    description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                      pool is selected with probability proportional to pool
                      weights. -
                      `steering_policy=\"least_outstanding_requests\"`: Use pool
                      weights to scale each pool's outstanding requests. -
                      `steering_policy=\"least_connections\"`: Use pool weights
                      to scale each pool's open connections."
                  region_pools:
                    type: object
                    additionalProperties: true
                    description: A mapping of region codes to a list of pool IDs (ordered by their
                      failover priority) for the given region. Any regions not
                      explicitly defined will fall back to using default_pools.
                  rules:
                    type: array
                    items:
                      type: object
                      properties:
                        condition:
                          type: string
                          description: The condition expressions to evaluate. If the condition evaluates
                            to true, the overrides or fixed_response in this
                            rule will be applied. An empty condition is always
                            true. For more details on condition expressions,
                            please see
                            https://developers.cloudflare.com/load-balancing/understand-basics/load-balancing-rules/expressions.
                        disabled:
                          type: boolean
                          description: Disable this specific rule. It will no longer be evaluated by this
                            load balancer.
                        fixed_response:
                          type: object
                          properties:
                            content_type:
                              type: string
                              description: The http 'Content-Type' header to include in the response.
                            location:
                              type: string
                              description: The http 'Location' header to include in the response.
                            message_body:
                              type: string
                              description: Text to include as the http body.
                            status_code:
                              type: number
                              description: The http status code to respond with.
                          description: A collection of fields used to directly respond to the eyeball
                            instead of routing to a pool. If a fixed_response is
                            supplied the rule will be marked as terminates.
                        name:
                          type: string
                          description: Name of this rule. Only used for human readability.
                        overrides:
                          type: object
                          properties:
                            adaptive_routing:
                              type: object
                              properties:
                                failover_across_pools:
                                  type: boolean
                                  description: Extends zero-downtime failover of requests to healthy origins from
                                    alternate pools, when no healthy alternate
                                    exists in the same pool, according to the
                                    failover order defined by traffic and origin
                                    steering. When set false (the default)
                                    zero-downtime failover will only occur
                                    between origins within the same pool. See
                                    `session_affinity_attributes` for control
                                    over when sessions are broken or reassigned.
                              description: Controls features that modify the routing of requests to pools and
                                origins in response to dynamic conditions, such
                                as during the interval between active health
                                monitoring requests. For example, zero-downtime
                                failover occurs immediately when an origin
                                becomes unavailable due to HTTP 521, 522, or 523
                                response codes. If there is another healthy
                                origin in the same pool, the request is retried
                                once against this alternate origin.
                            country_pools:
                              type: object
                              additionalProperties: true
                              description: A mapping of country codes to a list of pool IDs (ordered by their
                                failover priority) for the given country. Any
                                country not explicitly defined will fall back to
                                using the corresponding region_pool mapping if
                                it exists else to default_pools.
                            default_pools:
                              type: array
                              items:
                                type: string
                              description: A list of pool IDs ordered by their failover priority. Pools
                                defined here are used by default, or when
                                region_pools are not configured for a given
                                region.
                            fallback_pool:
                              type: string
                              description: The pool ID to use when all other pools are detected as unhealthy.
                            location_strategy:
                              type: object
                              properties:
                                mode:
                                  type: string
                                  enum:
                                    - pop
                                    - resolver_ip
                                  description: 'Determines the authoritative location when ECS is not preferred,
                                    does not exist in the request, or its GeoIP
                                    lookup is unsuccessful.  - `"pop"`: Use the
                                    Cloudflare PoP location. - `"resolver_ip"`:
                                    Use the DNS resolver GeoIP location. If the
                                    GeoIP lookup is unsuccessful, use the
                                    Cloudflare PoP location.'
                                prefer_ecs:
                                  type: string
                                  enum:
                                    - always
                                    - never
                                    - proximity
                                    - geo
                                  description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                                    the authoritative location.  - `"always"`:
                                    Always prefer ECS. - `"never"`: Never prefer
                                    ECS. - `"proximity"`: Prefer ECS only when
                                    `steering_policy="proximity"`. - `"geo"`:
                                    Prefer ECS only when
                                    `steering_policy="geo"`.'
                              description: Controls location-based steering for non-proxied requests. See
                                `steering_policy` to learn how steering is
                                affected.
                            pop_pools:
                              type: object
                              additionalProperties: true
                              description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                                of pool IDs (ordered by their failover priority)
                                for the PoP (datacenter). Any PoPs not
                                explicitly defined will fall back to using the
                                corresponding country_pool, then region_pool
                                mapping if it exists else to default_pools."
                            random_steering:
                              type: object
                              properties:
                                default_weight:
                                  type: number
                                  description: The default weight for pools in the load balancer that are not
                                    specified in the pool_weights map.
                                pool_weights:
                                  type: object
                                  additionalProperties: true
                                  description: A mapping of pool IDs to custom weights. The weight is relative to
                                    other pools in the load balancer.
                              description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                                pool is selected with probability proportional
                                to pool weights. -
                                `steering_policy=\"least_outstanding_requests\"\
                                `: Use pool weights to scale each pool's
                                outstanding requests. -
                                `steering_policy=\"least_connections\"`: Use
                                pool weights to scale each pool's open
                                connections."
                            region_pools:
                              type: object
                              additionalProperties: true
                              description: A mapping of region codes to a list of pool IDs (ordered by their
                                failover priority) for the given region. Any
                                regions not explicitly defined will fall back to
                                using default_pools.
                            session_affinity:
                              type: string
                              enum:
                                - none
                                - cookie
                                - ip_cookie
                                - header
                              description: "Specifies the type of session affinity the load balancer should
                                use unless specified as `\"none\"`. The
                                supported types are: - `\"cookie\"`: On the
                                first request to a proxied load balancer, a
                                cookie is generated, encoding information of
                                which origin the request will be forwarded to.
                                Subsequent requests, by the same client to the
                                same load balancer, will be sent to the origin
                                server the cookie encodes, for the duration of
                                the cookie and as long as the origin server
                                remains healthy. If the cookie has expired or
                                the origin server is unhealthy, then a new
                                origin server is calculated and used. -
                                `\"ip_cookie\"`: Behaves the same as
                                `\"cookie\"` except the initial origin selection
                                is stable and based on the client's ip address.
                                - `\"header\"`: On the first request to a
                                proxied load balancer, a session key based on
                                the configured HTTP headers (see
                                `session_affinity_attributes.headers`) is
                                generated, encoding the request headers used for
                                storing in the load balancer session state which
                                origin the request will be forwarded to.
                                Subsequent requests to the load balancer with
                                the same headers will be sent to the same origin
                                server, for the duration of the session and as
                                long as the origin server remains healthy. If
                                the session has been idle for the duration of
                                `session_affinity_ttl` seconds or the origin
                                server is unhealthy, then a new origin server is
                                calculated and used. See `headers` in
                                `session_affinity_attributes` for additional
                                required configuration."
                            session_affinity_attributes:
                              type: object
                              properties:
                                drain_duration:
                                  type: number
                                  description: Configures the drain duration in seconds. This field is only used
                                    when session affinity is enabled on the load
                                    balancer.
                                headers:
                                  type: array
                                  items:
                                    type: string
                                  description: 'Configures the names of HTTP headers to base session affinity on
                                    when header `session_affinity` is enabled.
                                    At least one HTTP header name must be
                                    provided. To specify the exact cookies to be
                                    used, include an item in the following
                                    format:
                                    `"cookie:<cookie-name-1>,<cookie-name-2>"`
                                    (example) where everything after the colon
                                    is a comma-separated list of cookie names.
                                    Providing only `"cookie"` will result in all
                                    cookies being used. The default max number
                                    of HTTP header names that can be provided
                                    depends on your plan: 5 for Enterprise, 1
                                    for all other plans.'
                                require_all_headers:
                                  type: boolean
                                  description: "When header `session_affinity` is enabled, this option can be used
                                    to specify how HTTP headers on load
                                    balancing requests will be used. The
                                    supported values are: - `\"true\"`: Load
                                    balancing requests must contain _all_ of the
                                    HTTP headers specified by the `headers`
                                    session affinity attribute, otherwise
                                    sessions aren't created. - `\"false\"`: Load
                                    balancing requests must contain _at least
                                    one_ of the HTTP headers specified by the
                                    `headers` session affinity attribute,
                                    otherwise sessions aren't created."
                                samesite:
                                  type: string
                                  enum:
                                    - Auto
                                    - Lax
                                    - None
                                    - Strict
                                  description: 'Configures the SameSite attribute on session affinity cookie.
                                    Value "Auto" will be translated to "Lax" or
                                    "None" depending if Always Use HTTPS is
                                    enabled. Note: when using value "None", the
                                    secure attribute can not be set to "Never".'
                                secure:
                                  type: string
                                  enum:
                                    - Auto
                                    - Always
                                    - Never
                                  description: Configures the Secure attribute on session affinity cookie. Value
                                    "Always" indicates the Secure attribute will
                                    be set in the Set-Cookie header, "Never"
                                    indicates the Secure attribute will not be
                                    set, and "Auto" will set the Secure
                                    attribute depending if Always Use HTTPS is
                                    enabled.
                                zero_downtime_failover:
                                  type: string
                                  enum:
                                    - none
                                    - temporary
                                    - sticky
                                  description: 'Configures the zero-downtime failover between origins within a
                                    pool when session affinity is enabled. This
                                    feature is currently incompatible with Argo,
                                    Tiered Cache, and Bandwidth Alliance. The
                                    supported values are: - `"none"`: No
                                    failover takes place for sessions pinned to
                                    the origin (default). - `"temporary"`:
                                    Traffic will be sent to another other
                                    healthy origin until the originally pinned
                                    origin is available; note that this can
                                    potentially result in heavy origin flapping.
                                    - `"sticky"`: The session affinity cookie is
                                    updated and subsequent requests are sent to
                                    the new origin. Note: Zero-downtime failover
                                    with sticky sessions is currently not
                                    supported for session affinity by header.'
                              description: Configures attributes for session affinity.
                            session_affinity_ttl:
                              type: number
                              description: "Time, in seconds, until a client's session expires after being
                                created. Once the expiry time has been reached,
                                subsequent requests may get sent to a different
                                origin server. The accepted ranges per
                                `session_affinity` policy are: - `\"cookie\"` /
                                `\"ip_cookie\"`: The current default of 23 hours
                                will be used unless explicitly set. The accepted
                                range of values is between [1800, 604800]. -
                                `\"header\"`: The current default of 1800
                                seconds will be used unless explicitly set. The
                                accepted range of values is between [30, 3600].
                                Note: With session affinity by header, sessions
                                only expire after they haven't been used for the
                                number of seconds specified."
                            steering_policy:
                              type: string
                              enum:
                                - off
                                - geo
                                - random
                                - dynamic_latency
                                - proximity
                                - least_outstanding_requests
                                - least_connections
                                - ""
                              description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                                `default_pools`. - `\"geo\"`: Use
                                `region_pools`/`country_pools`/`pop_pools`. For
                                non-proxied requests, the country for
                                `country_pools` is determined by
                                `location_strategy`. - `\"random\"`: Select a
                                pool randomly. - `\"dynamic_latency\"`: Use
                                round trip time to select the closest pool in
                                default_pools (requires pool health checks). -
                                `\"proximity\"`: Use the pools' latitude and
                                longitude to select the closest pool using the
                                Cloudflare PoP location for proxied requests or
                                the location determined by `location_strategy`
                                for non-proxied requests. -
                                `\"least_outstanding_requests\"`: Select a pool
                                by taking into consideration `random_steering`
                                weights, as well as each pool's number of
                                outstanding requests. Pools with more pending
                                requests are weighted proportionately less
                                relative to others. - `\"least_connections\"`:
                                Select a pool by taking into consideration
                                `random_steering` weights, as well as each
                                pool's number of open connections. Pools with
                                more open connections are weighted
                                proportionately less relative to others.
                                Supported for HTTP/1 and HTTP/2 connections. -
                                `\"\"`: Will map to `\"geo\"` if you use
                                `region_pools`/`country_pools`/`pop_pools`
                                otherwise `\"off\"`."
                            ttl:
                              type: number
                              description: Time to live (TTL) of the DNS entry for the IP address returned by
                                this load balancer. This only applies to
                                gray-clouded (unproxied) load balancers.
                          description: A collection of overrides to apply to the load balancer when this
                            rule's condition is true. All fields are optional.
                        priority:
                          type: number
                          description: The order in which rules should be executed in relation to each
                            other. Lower values are executed first. Values do
                            not need to be sequential. If no value is provided
                            for any rule the array order of the rules field will
                            be used to assign a priority.
                        terminates:
                          type: boolean
                          description: If this rule's condition is true, this causes rule evaluation to
                            stop after processing this rule.
                    description: "BETA Field Not General Access: A list of rules for this load
                      balancer to execute."
                  session_affinity:
                    type: string
                    enum:
                      - none
                      - cookie
                      - ip_cookie
                      - header
                    description: "Specifies the type of session affinity the load balancer should
                      use unless specified as `\"none\"`. The supported types
                      are: - `\"cookie\"`: On the first request to a proxied
                      load balancer, a cookie is generated, encoding information
                      of which origin the request will be forwarded to.
                      Subsequent requests, by the same client to the same load
                      balancer, will be sent to the origin server the cookie
                      encodes, for the duration of the cookie and as long as the
                      origin server remains healthy. If the cookie has expired
                      or the origin server is unhealthy, then a new origin
                      server is calculated and used. - `\"ip_cookie\"`: Behaves
                      the same as `\"cookie\"` except the initial origin
                      selection is stable and based on the client's ip address.
                      - `\"header\"`: On the first request to a proxied load
                      balancer, a session key based on the configured HTTP
                      headers (see `session_affinity_attributes.headers`) is
                      generated, encoding the request headers used for storing
                      in the load balancer session state which origin the
                      request will be forwarded to. Subsequent requests to the
                      load balancer with the same headers will be sent to the
                      same origin server, for the duration of the session and as
                      long as the origin server remains healthy. If the session
                      has been idle for the duration of `session_affinity_ttl`
                      seconds or the origin server is unhealthy, then a new
                      origin server is calculated and used. See `headers` in
                      `session_affinity_attributes` for additional required
                      configuration."
                  session_affinity_attributes:
                    type: object
                    properties:
                      drain_duration:
                        type: number
                        description: Configures the drain duration in seconds. This field is only used
                          when session affinity is enabled on the load balancer.
                      headers:
                        type: array
                        items:
                          type: string
                        description: 'Configures the names of HTTP headers to base session affinity on
                          when header `session_affinity` is enabled. At least
                          one HTTP header name must be provided. To specify the
                          exact cookies to be used, include an item in the
                          following format:
                          `"cookie:<cookie-name-1>,<cookie-name-2>"` (example)
                          where everything after the colon is a comma-separated
                          list of cookie names. Providing only `"cookie"` will
                          result in all cookies being used. The default max
                          number of HTTP header names that can be provided
                          depends on your plan: 5 for Enterprise, 1 for all
                          other plans.'
                      require_all_headers:
                        type: boolean
                        description: "When header `session_affinity` is enabled, this option can be used
                          to specify how HTTP headers on load balancing requests
                          will be used. The supported values are: - `\"true\"`:
                          Load balancing requests must contain _all_ of the HTTP
                          headers specified by the `headers` session affinity
                          attribute, otherwise sessions aren't created. -
                          `\"false\"`: Load balancing requests must contain _at
                          least one_ of the HTTP headers specified by the
                          `headers` session affinity attribute, otherwise
                          sessions aren't created."
                      samesite:
                        type: string
                        enum:
                          - Auto
                          - Lax
                          - None
                          - Strict
                        description: 'Configures the SameSite attribute on session affinity cookie.
                          Value "Auto" will be translated to "Lax" or "None"
                          depending if Always Use HTTPS is enabled. Note: when
                          using value "None", the secure attribute can not be
                          set to "Never".'
                      secure:
                        type: string
                        enum:
                          - Auto
                          - Always
                          - Never
                        description: Configures the Secure attribute on session affinity cookie. Value
                          "Always" indicates the Secure attribute will be set in
                          the Set-Cookie header, "Never" indicates the Secure
                          attribute will not be set, and "Auto" will set the
                          Secure attribute depending if Always Use HTTPS is
                          enabled.
                      zero_downtime_failover:
                        type: string
                        enum:
                          - none
                          - temporary
                          - sticky
                        description: 'Configures the zero-downtime failover between origins within a
                          pool when session affinity is enabled. This feature is
                          currently incompatible with Argo, Tiered Cache, and
                          Bandwidth Alliance. The supported values are: -
                          `"none"`: No failover takes place for sessions pinned
                          to the origin (default). - `"temporary"`: Traffic will
                          be sent to another other healthy origin until the
                          originally pinned origin is available; note that this
                          can potentially result in heavy origin flapping. -
                          `"sticky"`: The session affinity cookie is updated and
                          subsequent requests are sent to the new origin. Note:
                          Zero-downtime failover with sticky sessions is
                          currently not supported for session affinity by
                          header.'
                    description: Configures attributes for session affinity.
                  session_affinity_ttl:
                    type: number
                    description: "Time, in seconds, until a client's session expires after being
                      created. Once the expiry time has been reached, subsequent
                      requests may get sent to a different origin server. The
                      accepted ranges per `session_affinity` policy are: -
                      `\"cookie\"` / `\"ip_cookie\"`: The current default of 23
                      hours will be used unless explicitly set. The accepted
                      range of values is between [1800, 604800]. - `\"header\"`:
                      The current default of 1800 seconds will be used unless
                      explicitly set. The accepted range of values is between
                      [30, 3600]. Note: With session affinity by header,
                      sessions only expire after they haven't been used for the
                      number of seconds specified."
                  steering_policy:
                    type: string
                    enum:
                      - off
                      - geo
                      - random
                      - dynamic_latency
                      - proximity
                      - least_outstanding_requests
                      - least_connections
                      - ""
                    description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                      `default_pools`. - `\"geo\"`: Use
                      `region_pools`/`country_pools`/`pop_pools`. For
                      non-proxied requests, the country for `country_pools` is
                      determined by `location_strategy`. - `\"random\"`: Select
                      a pool randomly. - `\"dynamic_latency\"`: Use round trip
                      time to select the closest pool in default_pools (requires
                      pool health checks). - `\"proximity\"`: Use the pools'
                      latitude and longitude to select the closest pool using
                      the Cloudflare PoP location for proxied requests or the
                      location determined by `location_strategy` for non-proxied
                      requests. - `\"least_outstanding_requests\"`: Select a
                      pool by taking into consideration `random_steering`
                      weights, as well as each pool's number of outstanding
                      requests. Pools with more pending requests are weighted
                      proportionately less relative to others. -
                      `\"least_connections\"`: Select a pool by taking into
                      consideration `random_steering` weights, as well as each
                      pool's number of open connections. Pools with more open
                      connections are weighted proportionately less relative to
                      others. Supported for HTTP/1 and HTTP/2 connections. -
                      `\"\"`: Will map to `\"geo\"` if you use
                      `region_pools`/`country_pools`/`pop_pools` otherwise
                      `\"off\"`."
                  ttl:
                    type: number
                    description: Time to live (TTL) of the DNS entry for the IP address returned by
                      this load balancer. This only applies to gray-clouded
                      (unproxied) load balancers.
                  zone_name:
                    type: string
      x-distilled-pagination-class: LoadBalancersSinglePage
    post:
      operationId: createLoadBalancer
      description: "Create a new load balancer.  @example ```ts const loadBalancer =
        await client.loadBalancers.create({ zone_id:
        '699d98642c564d2e855e9661899b7252', default_pools: [
        '17b5962d775c646f3f9725cbc7a53df4', '9290f38c5d07c2e2f4df57b1f61d4196',
        '00920f38ce07c2e2f4df50b1f61d4194', ], fallback_pool: 'fallback_pool',
        name: 'www.example.com', }); ```"
      parameters:
        - name: zone_id
          in: path
          required: true
          description: "Path param:"
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                default_pools:
                  type: array
                  items:
                    type: string
                  description: "Body param: A list of pool IDs ordered by their failover priority.
                    Pools defined here are used by default, or when region_pools
                    are not configured for a given region."
                fallback_pool:
                  type: string
                  description: "Body param: The pool ID to use when all other pools are detected
                    as unhealthy."
                name:
                  type: string
                  description: "Body param: The DNS hostname to associate with your Load Balancer.
                    If this hostname already exists as a DNS record in
                    Cloudflare's DNS, the Load Balancer will take precedence and
                    the DNS record will not be used."
                adaptive_routing:
                  type: object
                  properties:
                    failover_across_pools:
                      type: boolean
                      description: Extends zero-downtime failover of requests to healthy origins from
                        alternate pools, when no healthy alternate exists in the
                        same pool, according to the failover order defined by
                        traffic and origin steering. When set false (the
                        default) zero-downtime failover will only occur between
                        origins within the same pool. See
                        `session_affinity_attributes` for control over when
                        sessions are broken or reassigned.
                  description: "Body param: Controls features that modify the routing of requests
                    to pools and origins in response to dynamic conditions, such
                    as during the interval between active health monitoring
                    requests. For example, zero-downtime failover occurs
                    immediately when an origin becomes unavailable due to HTTP
                    521, 522, or 523 response codes. If there is another healthy
                    origin in the same pool, the request is retried once against
                    this alternate origin."
                country_pools:
                  type: object
                  additionalProperties: true
                  description: "Body param: A mapping of country codes to a list of pool IDs
                    (ordered by their failover priority) for the given country.
                    Any country not explicitly defined will fall back to using
                    the corresponding region_pool mapping if it exists else to
                    default_pools."
                description:
                  type: string
                  description: "Body param: Object description."
                location_strategy:
                  type: object
                  properties:
                    mode:
                      type: string
                      enum:
                        - pop
                        - resolver_ip
                      description: 'Determines the authoritative location when ECS is not preferred,
                        does not exist in the request, or its GeoIP lookup is
                        unsuccessful.  - `"pop"`: Use the Cloudflare PoP
                        location. - `"resolver_ip"`: Use the DNS resolver GeoIP
                        location. If the GeoIP lookup is unsuccessful, use the
                        Cloudflare PoP location.'
                    prefer_ecs:
                      type: string
                      enum:
                        - always
                        - never
                        - proximity
                        - geo
                      description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                        the authoritative location.  - `"always"`: Always prefer
                        ECS. - `"never"`: Never prefer ECS. - `"proximity"`:
                        Prefer ECS only when `steering_policy="proximity"`. -
                        `"geo"`: Prefer ECS only when `steering_policy="geo"`.'
                  description: "Body param: Controls location-based steering for non-proxied
                    requests. See `steering_policy` to learn how steering is
                    affected."
                networks:
                  type: array
                  items:
                    type: string
                  description: "Body param: List of networks where Load Balancer or Pool is
                    enabled."
                pop_pools:
                  type: object
                  additionalProperties: true
                  description: "Body param: Enterprise only: A mapping of Cloudflare PoP
                    identifiers to a list of pool IDs (ordered by their failover
                    priority) for the PoP (datacenter). Any PoPs not explicitly
                    defined will fall back to using the corresponding
                    country_pool, then region_pool mapping if it exists else to
                    default_pools."
                proxied:
                  type: boolean
                  description: "Body param: Whether the hostname should be gray clouded (false) or
                    orange clouded (true)."
                random_steering:
                  type: object
                  properties:
                    default_weight:
                      type: number
                      description: The default weight for pools in the load balancer that are not
                        specified in the pool_weights map.
                    pool_weights:
                      type: object
                      additionalProperties: true
                      description: A mapping of pool IDs to custom weights. The weight is relative to
                        other pools in the load balancer.
                  description: "Body param: Configures pool weights.  -
                    `steering_policy=\"random\"`: A random pool is selected with
                    probability proportional to pool weights. -
                    `steering_policy=\"least_outstanding_requests\"`: Use pool
                    weights to scale each pool's outstanding requests. -
                    `steering_policy=\"least_connections\"`: Use pool weights to
                    scale each pool's open connections."
                region_pools:
                  type: object
                  additionalProperties: true
                  description: "Body param: A mapping of region codes to a list of pool IDs
                    (ordered by their failover priority) for the given region.
                    Any regions not explicitly defined will fall back to using
                    default_pools."
                rules:
                  type: array
                  items:
                    type: object
                    properties:
                      condition:
                        type: string
                        description: The condition expressions to evaluate. If the condition evaluates
                          to true, the overrides or fixed_response in this rule
                          will be applied. An empty condition is always true.
                          For more details on condition expressions, please see
                          https://developers.cloudflare.com/load-balancing/understand-basics/load-balancing-rules/expressions.
                      disabled:
                        type: boolean
                        description: Disable this specific rule. It will no longer be evaluated by this
                          load balancer.
                      fixed_response:
                        type: object
                        properties:
                          content_type:
                            type: string
                            description: The http 'Content-Type' header to include in the response.
                          location:
                            type: string
                            description: The http 'Location' header to include in the response.
                          message_body:
                            type: string
                            description: Text to include as the http body.
                          status_code:
                            type: number
                            description: The http status code to respond with.
                        description: A collection of fields used to directly respond to the eyeball
                          instead of routing to a pool. If a fixed_response is
                          supplied the rule will be marked as terminates.
                      name:
                        type: string
                        description: Name of this rule. Only used for human readability.
                      overrides:
                        type: object
                        properties:
                          adaptive_routing:
                            type: object
                            properties:
                              failover_across_pools:
                                type: boolean
                                description: Extends zero-downtime failover of requests to healthy origins from
                                  alternate pools, when no healthy alternate
                                  exists in the same pool, according to the
                                  failover order defined by traffic and origin
                                  steering. When set false (the default)
                                  zero-downtime failover will only occur between
                                  origins within the same pool. See
                                  `session_affinity_attributes` for control over
                                  when sessions are broken or reassigned.
                            description: Controls features that modify the routing of requests to pools and
                              origins in response to dynamic conditions, such as
                              during the interval between active health
                              monitoring requests. For example, zero-downtime
                              failover occurs immediately when an origin becomes
                              unavailable due to HTTP 521, 522, or 523 response
                              codes. If there is another healthy origin in the
                              same pool, the request is retried once against
                              this alternate origin.
                          country_pools:
                            type: object
                            additionalProperties: true
                            description: A mapping of country codes to a list of pool IDs (ordered by their
                              failover priority) for the given country. Any
                              country not explicitly defined will fall back to
                              using the corresponding region_pool mapping if it
                              exists else to default_pools.
                          default_pools:
                            type: array
                            items:
                              type: string
                            description: A list of pool IDs ordered by their failover priority. Pools
                              defined here are used by default, or when
                              region_pools are not configured for a given
                              region.
                          fallback_pool:
                            type: string
                            description: The pool ID to use when all other pools are detected as unhealthy.
                          location_strategy:
                            type: object
                            properties:
                              mode:
                                type: string
                                enum:
                                  - pop
                                  - resolver_ip
                                description: 'Determines the authoritative location when ECS is not preferred,
                                  does not exist in the request, or its GeoIP
                                  lookup is unsuccessful.  - `"pop"`: Use the
                                  Cloudflare PoP location. - `"resolver_ip"`:
                                  Use the DNS resolver GeoIP location. If the
                                  GeoIP lookup is unsuccessful, use the
                                  Cloudflare PoP location.'
                              prefer_ecs:
                                type: string
                                enum:
                                  - always
                                  - never
                                  - proximity
                                  - geo
                                description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                                  the authoritative location.  - `"always"`:
                                  Always prefer ECS. - `"never"`: Never prefer
                                  ECS. - `"proximity"`: Prefer ECS only when
                                  `steering_policy="proximity"`. - `"geo"`:
                                  Prefer ECS only when `steering_policy="geo"`.'
                            description: Controls location-based steering for non-proxied requests. See
                              `steering_policy` to learn how steering is
                              affected.
                          pop_pools:
                            type: object
                            additionalProperties: true
                            description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                              of pool IDs (ordered by their failover priority)
                              for the PoP (datacenter). Any PoPs not explicitly
                              defined will fall back to using the corresponding
                              country_pool, then region_pool mapping if it
                              exists else to default_pools."
                          random_steering:
                            type: object
                            properties:
                              default_weight:
                                type: number
                                description: The default weight for pools in the load balancer that are not
                                  specified in the pool_weights map.
                              pool_weights:
                                type: object
                                additionalProperties: true
                                description: A mapping of pool IDs to custom weights. The weight is relative to
                                  other pools in the load balancer.
                            description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                              pool is selected with probability proportional to
                              pool weights. -
                              `steering_policy=\"least_outstanding_requests\"`:
                              Use pool weights to scale each pool's outstanding
                              requests. -
                              `steering_policy=\"least_connections\"`: Use pool
                              weights to scale each pool's open connections."
                          region_pools:
                            type: object
                            additionalProperties: true
                            description: A mapping of region codes to a list of pool IDs (ordered by their
                              failover priority) for the given region. Any
                              regions not explicitly defined will fall back to
                              using default_pools.
                          session_affinity:
                            type: string
                            enum:
                              - none
                              - cookie
                              - ip_cookie
                              - header
                            description: "Specifies the type of session affinity the load balancer should
                              use unless specified as `\"none\"`. The supported
                              types are: - `\"cookie\"`: On the first request to
                              a proxied load balancer, a cookie is generated,
                              encoding information of which origin the request
                              will be forwarded to. Subsequent requests, by the
                              same client to the same load balancer, will be
                              sent to the origin server the cookie encodes, for
                              the duration of the cookie and as long as the
                              origin server remains healthy. If the cookie has
                              expired or the origin server is unhealthy, then a
                              new origin server is calculated and used. -
                              `\"ip_cookie\"`: Behaves the same as `\"cookie\"`
                              except the initial origin selection is stable and
                              based on the client's ip address. - `\"header\"`:
                              On the first request to a proxied load balancer, a
                              session key based on the configured HTTP headers
                              (see `session_affinity_attributes.headers`) is
                              generated, encoding the request headers used for
                              storing in the load balancer session state which
                              origin the request will be forwarded to.
                              Subsequent requests to the load balancer with the
                              same headers will be sent to the same origin
                              server, for the duration of the session and as
                              long as the origin server remains healthy. If the
                              session has been idle for the duration of
                              `session_affinity_ttl` seconds or the origin
                              server is unhealthy, then a new origin server is
                              calculated and used. See `headers` in
                              `session_affinity_attributes` for additional
                              required configuration."
                          session_affinity_attributes:
                            type: object
                            properties:
                              drain_duration:
                                type: number
                                description: Configures the drain duration in seconds. This field is only used
                                  when session affinity is enabled on the load
                                  balancer.
                              headers:
                                type: array
                                items:
                                  type: string
                                description: 'Configures the names of HTTP headers to base session affinity on
                                  when header `session_affinity` is enabled. At
                                  least one HTTP header name must be provided.
                                  To specify the exact cookies to be used,
                                  include an item in the following format:
                                  `"cookie:<cookie-name-1>,<cookie-name-2>"`
                                  (example) where everything after the colon is
                                  a comma-separated list of cookie names.
                                  Providing only `"cookie"` will result in all
                                  cookies being used. The default max number of
                                  HTTP header names that can be provided depends
                                  on your plan: 5 for Enterprise, 1 for all
                                  other plans.'
                              require_all_headers:
                                type: boolean
                                description: "When header `session_affinity` is enabled, this option can be used
                                  to specify how HTTP headers on load balancing
                                  requests will be used. The supported values
                                  are: - `\"true\"`: Load balancing requests
                                  must contain _all_ of the HTTP headers
                                  specified by the `headers` session affinity
                                  attribute, otherwise sessions aren't created.
                                  - `\"false\"`: Load balancing requests must
                                  contain _at least one_ of the HTTP headers
                                  specified by the `headers` session affinity
                                  attribute, otherwise sessions aren't created."
                              samesite:
                                type: string
                                enum:
                                  - Auto
                                  - Lax
                                  - None
                                  - Strict
                                description: 'Configures the SameSite attribute on session affinity cookie.
                                  Value "Auto" will be translated to "Lax" or
                                  "None" depending if Always Use HTTPS is
                                  enabled. Note: when using value "None", the
                                  secure attribute can not be set to "Never".'
                              secure:
                                type: string
                                enum:
                                  - Auto
                                  - Always
                                  - Never
                                description: Configures the Secure attribute on session affinity cookie. Value
                                  "Always" indicates the Secure attribute will
                                  be set in the Set-Cookie header, "Never"
                                  indicates the Secure attribute will not be
                                  set, and "Auto" will set the Secure attribute
                                  depending if Always Use HTTPS is enabled.
                              zero_downtime_failover:
                                type: string
                                enum:
                                  - none
                                  - temporary
                                  - sticky
                                description: 'Configures the zero-downtime failover between origins within a
                                  pool when session affinity is enabled. This
                                  feature is currently incompatible with Argo,
                                  Tiered Cache, and Bandwidth Alliance. The
                                  supported values are: - `"none"`: No failover
                                  takes place for sessions pinned to the origin
                                  (default). - `"temporary"`: Traffic will be
                                  sent to another other healthy origin until the
                                  originally pinned origin is available; note
                                  that this can potentially result in heavy
                                  origin flapping. - `"sticky"`: The session
                                  affinity cookie is updated and subsequent
                                  requests are sent to the new origin. Note:
                                  Zero-downtime failover with sticky sessions is
                                  currently not supported for session affinity
                                  by header.'
                            description: Configures attributes for session affinity.
                          session_affinity_ttl:
                            type: number
                            description: "Time, in seconds, until a client's session expires after being
                              created. Once the expiry time has been reached,
                              subsequent requests may get sent to a different
                              origin server. The accepted ranges per
                              `session_affinity` policy are: - `\"cookie\"` /
                              `\"ip_cookie\"`: The current default of 23 hours
                              will be used unless explicitly set. The accepted
                              range of values is between [1800, 604800]. -
                              `\"header\"`: The current default of 1800 seconds
                              will be used unless explicitly set. The accepted
                              range of values is between [30, 3600]. Note: With
                              session affinity by header, sessions only expire
                              after they haven't been used for the number of
                              seconds specified."
                          steering_policy:
                            type: string
                            enum:
                              - off
                              - geo
                              - random
                              - dynamic_latency
                              - proximity
                              - least_outstanding_requests
                              - least_connections
                              - ""
                            description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                              `default_pools`. - `\"geo\"`: Use
                              `region_pools`/`country_pools`/`pop_pools`. For
                              non-proxied requests, the country for
                              `country_pools` is determined by
                              `location_strategy`. - `\"random\"`: Select a pool
                              randomly. - `\"dynamic_latency\"`: Use round trip
                              time to select the closest pool in default_pools
                              (requires pool health checks). - `\"proximity\"`:
                              Use the pools' latitude and longitude to select
                              the closest pool using the Cloudflare PoP location
                              for proxied requests or the location determined by
                              `location_strategy` for non-proxied requests. -
                              `\"least_outstanding_requests\"`: Select a pool by
                              taking into consideration `random_steering`
                              weights, as well as each pool's number of
                              outstanding requests. Pools with more pending
                              requests are weighted proportionately less
                              relative to others. - `\"least_connections\"`:
                              Select a pool by taking into consideration
                              `random_steering` weights, as well as each pool's
                              number of open connections. Pools with more open
                              connections are weighted proportionately less
                              relative to others. Supported for HTTP/1 and
                              HTTP/2 connections. - `\"\"`: Will map to
                              `\"geo\"` if you use
                              `region_pools`/`country_pools`/`pop_pools`
                              otherwise `\"off\"`."
                          ttl:
                            type: number
                            description: Time to live (TTL) of the DNS entry for the IP address returned by
                              this load balancer. This only applies to
                              gray-clouded (unproxied) load balancers.
                        description: A collection of overrides to apply to the load balancer when this
                          rule's condition is true. All fields are optional.
                      priority:
                        type: number
                        description: The order in which rules should be executed in relation to each
                          other. Lower values are executed first. Values do not
                          need to be sequential. If no value is provided for any
                          rule the array order of the rules field will be used
                          to assign a priority.
                      terminates:
                        type: boolean
                        description: If this rule's condition is true, this causes rule evaluation to
                          stop after processing this rule.
                  description: "Body param: BETA Field Not General Access: A list of rules for
                    this load balancer to execute."
                session_affinity:
                  type: string
                  enum:
                    - none
                    - cookie
                    - ip_cookie
                    - header
                  description: "Body param: Specifies the type of session affinity the load
                    balancer should use unless specified as `\"none\"`. The
                    supported types are: - `\"cookie\"`: On the first request to
                    a proxied load balancer, a cookie is generated, encoding
                    information of which origin the request will be forwarded
                    to. Subsequent requests, by the same client to the same load
                    balancer, will be sent to the origin server the cookie
                    encodes, for the duration of the cookie and as long as the
                    origin server remains healthy. If the cookie has expired or
                    the origin server is unhealthy, then a new origin server is
                    calculated and used. - `\"ip_cookie\"`: Behaves the same as
                    `\"cookie\"` except the initial origin selection is stable
                    and based on the client's ip address. - `\"header\"`: On the
                    first request to a proxied load balancer, a session key
                    based on the configured HTTP headers (see
                    `session_affinity_attributes.headers`) is generated,
                    encoding the request headers used for storing in the load
                    balancer session state which origin the request will be
                    forwarded to. Subsequent requests to the load balancer with
                    the same headers will be sent to the same origin server, for
                    the duration of the session and as long as the origin server
                    remains healthy. If the session has been idle for the
                    duration of `session_affinity_ttl` seconds or the origin
                    server is unhealthy, then a new origin server is calculated
                    and used. See `headers` in `session_affinity_attributes` for
                    additional required configuration."
                session_affinity_attributes:
                  type: object
                  properties:
                    drain_duration:
                      type: number
                      description: Configures the drain duration in seconds. This field is only used
                        when session affinity is enabled on the load balancer.
                    headers:
                      type: array
                      items:
                        type: string
                      description: 'Configures the names of HTTP headers to base session affinity on
                        when header `session_affinity` is enabled. At least one
                        HTTP header name must be provided. To specify the exact
                        cookies to be used, include an item in the following
                        format: `"cookie:<cookie-name-1>,<cookie-name-2>"`
                        (example) where everything after the colon is a
                        comma-separated list of cookie names. Providing only
                        `"cookie"` will result in all cookies being used. The
                        default max number of HTTP header names that can be
                        provided depends on your plan: 5 for Enterprise, 1 for
                        all other plans.'
                    require_all_headers:
                      type: boolean
                      description: "When header `session_affinity` is enabled, this option can be used
                        to specify how HTTP headers on load balancing requests
                        will be used. The supported values are: - `\"true\"`:
                        Load balancing requests must contain _all_ of the HTTP
                        headers specified by the `headers` session affinity
                        attribute, otherwise sessions aren't created. -
                        `\"false\"`: Load balancing requests must contain _at
                        least one_ of the HTTP headers specified by the
                        `headers` session affinity attribute, otherwise sessions
                        aren't created."
                    samesite:
                      type: string
                      enum:
                        - Auto
                        - Lax
                        - None
                        - Strict
                      description: 'Configures the SameSite attribute on session affinity cookie.
                        Value "Auto" will be translated to "Lax" or "None"
                        depending if Always Use HTTPS is enabled. Note: when
                        using value "None", the secure attribute can not be set
                        to "Never".'
                    secure:
                      type: string
                      enum:
                        - Auto
                        - Always
                        - Never
                      description: Configures the Secure attribute on session affinity cookie. Value
                        "Always" indicates the Secure attribute will be set in
                        the Set-Cookie header, "Never" indicates the Secure
                        attribute will not be set, and "Auto" will set the
                        Secure attribute depending if Always Use HTTPS is
                        enabled.
                    zero_downtime_failover:
                      type: string
                      enum:
                        - none
                        - temporary
                        - sticky
                      description: 'Configures the zero-downtime failover between origins within a
                        pool when session affinity is enabled. This feature is
                        currently incompatible with Argo, Tiered Cache, and
                        Bandwidth Alliance. The supported values are: -
                        `"none"`: No failover takes place for sessions pinned to
                        the origin (default). - `"temporary"`: Traffic will be
                        sent to another other healthy origin until the
                        originally pinned origin is available; note that this
                        can potentially result in heavy origin flapping. -
                        `"sticky"`: The session affinity cookie is updated and
                        subsequent requests are sent to the new origin. Note:
                        Zero-downtime failover with sticky sessions is currently
                        not supported for session affinity by header.'
                  description: "Body param: Configures attributes for session affinity."
                session_affinity_ttl:
                  type: number
                  description: "Body param: Time, in seconds, until a client's session expires
                    after being created. Once the expiry time has been reached,
                    subsequent requests may get sent to a different origin
                    server. The accepted ranges per `session_affinity` policy
                    are: - `\"cookie\"` / `\"ip_cookie\"`: The current default
                    of 23 hours will be used unless explicitly set. The accepted
                    range of values is between [1800, 604800]. - `\"header\"`:
                    The current default of 1800 seconds will be used unless
                    explicitly set. The accepted range of values is between [30,
                    3600]. Note: With session affinity by header, sessions only
                    expire after they haven't been used for the number of
                    seconds specified."
                steering_policy:
                  type: string
                  enum:
                    - off
                    - geo
                    - random
                    - dynamic_latency
                    - proximity
                    - least_outstanding_requests
                    - least_connections
                    - ""
                  description: "Body param: Steering Policy for this load balancer.  - `\"off\"`:
                    Use `default_pools`. - `\"geo\"`: Use
                    `region_pools`/`country_pools`/`pop_pools`. For non-proxied
                    requests, the country for `country_pools` is determined by
                    `location_strategy`. - `\"random\"`: Select a pool randomly.
                    - `\"dynamic_latency\"`: Use round trip time to select the
                    closest pool in default_pools (requires pool health checks).
                    - `\"proximity\"`: Use the pools' latitude and longitude to
                    select the closest pool using the Cloudflare PoP location
                    for proxied requests or the location determined by
                    `location_strategy` for non-proxied requests. -
                    `\"least_outstanding_requests\"`: Select a pool by taking
                    into consideration `random_steering` weights, as well as
                    each pool's number of outstanding requests. Pools with more
                    pending requests are weighted proportionately less relative
                    to others. - `\"least_connections\"`: Select a pool by
                    taking into consideration `random_steering` weights, as well
                    as each pool's number of open connections. Pools with more
                    open connections are weighted proportionately less relative
                    to others. Supported for HTTP/1 and HTTP/2 connections. -
                    `\"\"`: Will map to `\"geo\"` if you use
                    `region_pools`/`country_pools`/`pop_pools` otherwise
                    `\"off\"`."
                ttl:
                  type: number
                  description: "Body param: Time to live (TTL) of the DNS entry for the IP address
                    returned by this load balancer. This only applies to
                    gray-clouded (unproxied) load balancers."
              required:
                - default_pools
                - fallback_pool
                - name
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  adaptive_routing:
                    type: object
                    properties:
                      failover_across_pools:
                        type: boolean
                        description: Extends zero-downtime failover of requests to healthy origins from
                          alternate pools, when no healthy alternate exists in
                          the same pool, according to the failover order defined
                          by traffic and origin steering. When set false (the
                          default) zero-downtime failover will only occur
                          between origins within the same pool. See
                          `session_affinity_attributes` for control over when
                          sessions are broken or reassigned.
                    description: Controls features that modify the routing of requests to pools and
                      origins in response to dynamic conditions, such as during
                      the interval between active health monitoring requests.
                      For example, zero-downtime failover occurs immediately
                      when an origin becomes unavailable due to HTTP 521, 522,
                      or 523 response codes. If there is another healthy origin
                      in the same pool, the request is retried once against this
                      alternate origin.
                  country_pools:
                    type: object
                    additionalProperties: true
                    description: A mapping of country codes to a list of pool IDs (ordered by their
                      failover priority) for the given country. Any country not
                      explicitly defined will fall back to using the
                      corresponding region_pool mapping if it exists else to
                      default_pools.
                  created_on:
                    type: string
                  default_pools:
                    type: array
                    items:
                      type: string
                    description: A list of pool IDs ordered by their failover priority. Pools
                      defined here are used by default, or when region_pools are
                      not configured for a given region.
                  description:
                    type: string
                    description: Object description.
                  enabled:
                    type: boolean
                    description: Whether to enable (the default) this load balancer.
                  fallback_pool:
                    type: string
                    description: The pool ID to use when all other pools are detected as unhealthy.
                  location_strategy:
                    type: object
                    properties:
                      mode:
                        type: string
                        enum:
                          - pop
                          - resolver_ip
                        description: 'Determines the authoritative location when ECS is not preferred,
                          does not exist in the request, or its GeoIP lookup is
                          unsuccessful.  - `"pop"`: Use the Cloudflare PoP
                          location. - `"resolver_ip"`: Use the DNS resolver
                          GeoIP location. If the GeoIP lookup is unsuccessful,
                          use the Cloudflare PoP location.'
                      prefer_ecs:
                        type: string
                        enum:
                          - always
                          - never
                          - proximity
                          - geo
                        description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                          the authoritative location.  - `"always"`: Always
                          prefer ECS. - `"never"`: Never prefer ECS. -
                          `"proximity"`: Prefer ECS only when
                          `steering_policy="proximity"`. - `"geo"`: Prefer ECS
                          only when `steering_policy="geo"`.'
                    description: Controls location-based steering for non-proxied requests. See
                      `steering_policy` to learn how steering is affected.
                  modified_on:
                    type: string
                  name:
                    type: string
                    description: The DNS hostname to associate with your Load Balancer. If this
                      hostname already exists as a DNS record in Cloudflare's
                      DNS, the Load Balancer will take precedence and the DNS
                      record will not be used.
                  networks:
                    type: array
                    items:
                      type: string
                    description: List of networks where Load Balancer or Pool is enabled.
                  pop_pools:
                    type: object
                    additionalProperties: true
                    description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                      of pool IDs (ordered by their failover priority) for the
                      PoP (datacenter). Any PoPs not explicitly defined will
                      fall back to using the corresponding country_pool, then
                      region_pool mapping if it exists else to default_pools."
                  proxied:
                    type: boolean
                    description: Whether the hostname should be gray clouded (false) or orange
                      clouded (true).
                  random_steering:
                    type: object
                    properties:
                      default_weight:
                        type: number
                        description: The default weight for pools in the load balancer that are not
                          specified in the pool_weights map.
                      pool_weights:
                        type: object
                        additionalProperties: true
                        description: A mapping of pool IDs to custom weights. The weight is relative to
                          other pools in the load balancer.
                    description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                      pool is selected with probability proportional to pool
                      weights. -
                      `steering_policy=\"least_outstanding_requests\"`: Use pool
                      weights to scale each pool's outstanding requests. -
                      `steering_policy=\"least_connections\"`: Use pool weights
                      to scale each pool's open connections."
                  region_pools:
                    type: object
                    additionalProperties: true
                    description: A mapping of region codes to a list of pool IDs (ordered by their
                      failover priority) for the given region. Any regions not
                      explicitly defined will fall back to using default_pools.
                  rules:
                    type: array
                    items:
                      type: object
                      properties:
                        condition:
                          type: string
                          description: The condition expressions to evaluate. If the condition evaluates
                            to true, the overrides or fixed_response in this
                            rule will be applied. An empty condition is always
                            true. For more details on condition expressions,
                            please see
                            https://developers.cloudflare.com/load-balancing/understand-basics/load-balancing-rules/expressions.
                        disabled:
                          type: boolean
                          description: Disable this specific rule. It will no longer be evaluated by this
                            load balancer.
                        fixed_response:
                          type: object
                          properties:
                            content_type:
                              type: string
                              description: The http 'Content-Type' header to include in the response.
                            location:
                              type: string
                              description: The http 'Location' header to include in the response.
                            message_body:
                              type: string
                              description: Text to include as the http body.
                            status_code:
                              type: number
                              description: The http status code to respond with.
                          description: A collection of fields used to directly respond to the eyeball
                            instead of routing to a pool. If a fixed_response is
                            supplied the rule will be marked as terminates.
                        name:
                          type: string
                          description: Name of this rule. Only used for human readability.
                        overrides:
                          type: object
                          properties:
                            adaptive_routing:
                              type: object
                              properties:
                                failover_across_pools:
                                  type: boolean
                                  description: Extends zero-downtime failover of requests to healthy origins from
                                    alternate pools, when no healthy alternate
                                    exists in the same pool, according to the
                                    failover order defined by traffic and origin
                                    steering. When set false (the default)
                                    zero-downtime failover will only occur
                                    between origins within the same pool. See
                                    `session_affinity_attributes` for control
                                    over when sessions are broken or reassigned.
                              description: Controls features that modify the routing of requests to pools and
                                origins in response to dynamic conditions, such
                                as during the interval between active health
                                monitoring requests. For example, zero-downtime
                                failover occurs immediately when an origin
                                becomes unavailable due to HTTP 521, 522, or 523
                                response codes. If there is another healthy
                                origin in the same pool, the request is retried
                                once against this alternate origin.
                            country_pools:
                              type: object
                              additionalProperties: true
                              description: A mapping of country codes to a list of pool IDs (ordered by their
                                failover priority) for the given country. Any
                                country not explicitly defined will fall back to
                                using the corresponding region_pool mapping if
                                it exists else to default_pools.
                            default_pools:
                              type: array
                              items:
                                type: string
                              description: A list of pool IDs ordered by their failover priority. Pools
                                defined here are used by default, or when
                                region_pools are not configured for a given
                                region.
                            fallback_pool:
                              type: string
                              description: The pool ID to use when all other pools are detected as unhealthy.
                            location_strategy:
                              type: object
                              properties:
                                mode:
                                  type: string
                                  enum:
                                    - pop
                                    - resolver_ip
                                  description: 'Determines the authoritative location when ECS is not preferred,
                                    does not exist in the request, or its GeoIP
                                    lookup is unsuccessful.  - `"pop"`: Use the
                                    Cloudflare PoP location. - `"resolver_ip"`:
                                    Use the DNS resolver GeoIP location. If the
                                    GeoIP lookup is unsuccessful, use the
                                    Cloudflare PoP location.'
                                prefer_ecs:
                                  type: string
                                  enum:
                                    - always
                                    - never
                                    - proximity
                                    - geo
                                  description: 'Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as
                                    the authoritative location.  - `"always"`:
                                    Always prefer ECS. - `"never"`: Never prefer
                                    ECS. - `"proximity"`: Prefer ECS only when
                                    `steering_policy="proximity"`. - `"geo"`:
                                    Prefer ECS only when
                                    `steering_policy="geo"`.'
                              description: Controls location-based steering for non-proxied requests. See
                                `steering_policy` to learn how steering is
                                affected.
                            pop_pools:
                              type: object
                              additionalProperties: true
                              description: "Enterprise only: A mapping of Cloudflare PoP identifiers to a list
                                of pool IDs (ordered by their failover priority)
                                for the PoP (datacenter). Any PoPs not
                                explicitly defined will fall back to using the
                                corresponding country_pool, then region_pool
                                mapping if it exists else to default_pools."
                            random_steering:
                              type: object
                              properties:
                                default_weight:
                                  type: number
                                  description: The default weight for pools in the load balancer that are not
                                    specified in the pool_weights map.
                                pool_weights:
                                  type: object
                                  additionalProperties: true
                                  description: A mapping of pool IDs to custom weights. The weight is relative to
                                    other pools in the load balancer.
                              description: "Configures pool weights.  - `steering_policy=\"random\"`: A random
                                pool is selected with probability proportional
                                to pool weights. -
                                `steering_policy=\"least_outstanding_requests\"\
                                `: Use pool weights to scale each pool's
                                outstanding requests. -
                                `steering_policy=\"least_connections\"`: Use
                                pool weights to scale each pool's open
                                connections."
                            region_pools:
                              type: object
                              additionalProperties: true
                              description: A mapping of region codes to a list of pool IDs (ordered by their
                                failover priority) for the given region. Any
                                regions not explicitly defined will fall back to
                                using default_pools.
                            session_affinity:
                              type: string
                              enum:
                                - none
                                - cookie
                                - ip_cookie
                                - header
                              description: "Specifies the type of session affinity the load balancer should
                                use unless specified as `\"none\"`. The
                                supported types are: - `\"cookie\"`: On the
                                first request to a proxied load balancer, a
                                cookie is generated, encoding information of
                                which origin the request will be forwarded to.
                                Subsequent requests, by the same client to the
                                same load balancer, will be sent to the origin
                                server the cookie encodes, for the duration of
                                the cookie and as long as the origin server
                                remains healthy. If the cookie has expired or
                                the origin server is unhealthy, then a new
                                origin server is calculated and used. -
                                `\"ip_cookie\"`: Behaves the same as
                                `\"cookie\"` except the initial origin selection
                                is stable and based on the client's ip address.
                                - `\"header\"`: On the first request to a
                                proxied load balancer, a session key based on
                                the configured HTTP headers (see
                                `session_affinity_attributes.headers`) is
                                generated, encoding the request headers used for
                                storing in the load balancer session state which
                                origin the request will be forwarded to.
                                Subsequent requests to the load balancer with
                                the same headers will be sent to the same origin
                                server, for the duration of the session and as
                                long as the origin server remains healthy. If
                                the session has been idle for the duration of
                                `session_affinity_ttl` seconds or the origin
                                server is unhealthy, then a new origin server is
                                calculated and used. See `headers` in
                                `session_affinity_attributes` for additional
                                required configuration."
                            session_affinity_attributes:
                              type: object
                              properties:
                                drain_duration:
                                  type: number
                                  description: Configures the drain duration in seconds. This field is only used
                                    when session affinity is enabled on the load
                                    balancer.
                                headers:
                                  type: array
                                  items:
                                    type: string
                                  description: 'Configures the names of HTTP headers to base session affinity on
                                    when header `session_affinity` is enabled.
                                    At least one HTTP header name must be
                                    provided. To specify the exact cookies to be
                                    used, include an item in the following
                                    format:
                                    `"cookie:<cookie-name-1>,<cookie-name-2>"`
                                    (example) where everything after the colon
                                    is a comma-separated list of cookie names.
                                    Providing only `"cookie"` will result in all
                                    cookies being used. The default max number
                                    of HTTP header names that can be provided
                                    depends on your plan: 5 for Enterprise, 1
                                    for all other plans.'
                                require_all_headers:
                                  type: boolean
                                  description: "When header `session_affinity` is enabled, this option can be used
                                    to specify how HTTP headers on load
                                    balancing requests will be used. The
                                    supported values are: - `\"true\"`: Load
                                    balancing requests must contain _all_ of the
                                    HTTP headers specified by the `headers`
                                    session affinity attribute, otherwise
                                    sessions aren't created. - `\"false\"`: Load
                                    balancing requests must contain _at least
                                    one_ of the HTTP headers specified by the
                                    `headers` session affinity attribute,
                                    otherwise sessions aren't created."
                                samesite:
                                  type: string
                                  enum:
                                    - Auto
                                    - Lax
                                    - None
                                    - Strict
                                  description: 'Configures the SameSite attribute on session affinity cookie.
                                    Value "Auto" will be translated to "Lax" or
                                    "None" depending if Always Use HTTPS is
                                    enabled. Note: when using value "None", the
                                    secure attribute can not be set to "Never".'
                                secure:
                                  type: string
                                  enum:
                                    - Auto
                                    - Always
                                    - Never
                                  description: Configures the Secure attribute on session affinity cookie. Value
                                    "Always" indicates the Secure attribute will
                                    be set in the Set-Cookie header, "Never"
                                    indicates the Secure attribute will not be
                                    set, and "Auto" will set the Secure
                                    attribute depending if Always Use HTTPS is
                                    enabled.
                                zero_downtime_failover:
                                  type: string
                                  enum:
                                    - none
                                    - temporary
                                    - sticky
                                  description: 'Configures the zero-downtime failover between origins within a
                                    pool when session affinity is enabled. This
                                    feature is currently incompatible with Argo,
                                    Tiered Cache, and Bandwidth Alliance. The
                                    supported values are: - `"none"`: No
                                    failover takes place for sessions pinned to
                                    the origin (default). - `"temporary"`:
                                    Traffic will be sent to another other
                                    healthy origin until the originally pinned
                                    origin is available; note that this can
                                    potentially result in heavy origin flapping.
                                    - `"sticky"`: The session affinity cookie is
                                    updated and subsequent requests are sent to
                                    the new origin. Note: Zero-downtime failover
                                    with sticky sessions is currently not
                                    supported for session affinity by header.'
                              description: Configures attributes for session affinity.
                            session_affinity_ttl:
                              type: number
                              description: "Time, in seconds, until a client's session expires after being
                                created. Once the expiry time has been reached,
                                subsequent requests may get sent to a different
                                origin server. The accepted ranges per
                                `session_affinity` policy are: - `\"cookie\"` /
                                `\"ip_cookie\"`: The current default of 23 hours
                                will be used unless explicitly set. The accepted
                                range of values is between [1800, 604800]. -
                                `\"header\"`: The current default of 1800
                                seconds will be used unless explicitly set. The
                                accepted range of values is between [30, 3600].
                                Note: With session affinity by header, sessions
                                only expire after they haven't been used for the
                                number of seconds specified."
                            steering_policy:
                              type: string
                              enum:
                                - off
                                - geo
                                - random
                                - dynamic_latency
                                - proximity
                                - least_outstanding_requests
                                - least_connections
                                - ""
                              description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                                `default_pools`. - `\"geo\"`: Use
                                `region_pools`/`country_pools`/`pop_pools`. For
                                non-proxied requests, the country for
                                `country_pools` is determined by
                                `location_strategy`. - `\"random\"`: Select a
                                pool randomly. - `\"dynamic_latency\"`: Use
                                round trip time to select the closest pool in
                                default_pools (requires pool health checks). -
                                `\"proximity\"`: Use the pools' latitude and
                                longitude to select the closest pool using the
                                Cloudflare PoP location for proxied requests or
                                the location determined by `location_strategy`
                                for non-proxied requests. -
                                `\"least_outstanding_requests\"`: Select a pool
                                by taking into consideration `random_steering`
                                weights, as well as each pool's number of
                                outstanding requests. Pools with more pending
                                requests are weighted proportionately less
                                relative to others. - `\"least_connections\"`:
                                Select a pool by taking into consideration
                                `random_steering` weights, as well as each
                                pool's number of open connections. Pools with
                                more open connections are weighted
                                proportionately less relative to others.
                                Supported for HTTP/1 and HTTP/2 connections. -
                                `\"\"`: Will map to `\"geo\"` if you use
                                `region_pools`/`country_pools`/`pop_pools`
                                otherwise `\"off\"`."
                            ttl:
                              type: number
                              description: Time to live (TTL) of the DNS entry for the IP address returned by
                                this load balancer. This only applies to
                                gray-clouded (unproxied) load balancers.
                          description: A collection of overrides to apply to the load balancer when this
                            rule's condition is true. All fields are optional.
                        priority:
                          type: number
                          description: The order in which rules should be executed in relation to each
                            other. Lower values are executed first. Values do
                            not need to be sequential. If no value is provided
                            for any rule the array order of the rules field will
                            be used to assign a priority.
                        terminates:
                          type: boolean
                          description: If this rule's condition is true, this causes rule evaluation to
                            stop after processing this rule.
                    description: "BETA Field Not General Access: A list of rules for this load
                      balancer to execute."
                  session_affinity:
                    type: string
                    enum:
                      - none
                      - cookie
                      - ip_cookie
                      - header
                    description: "Specifies the type of session affinity the load balancer should
                      use unless specified as `\"none\"`. The supported types
                      are: - `\"cookie\"`: On the first request to a proxied
                      load balancer, a cookie is generated, encoding information
                      of which origin the request will be forwarded to.
                      Subsequent requests, by the same client to the same load
                      balancer, will be sent to the origin server the cookie
                      encodes, for the duration of the cookie and as long as the
                      origin server remains healthy. If the cookie has expired
                      or the origin server is unhealthy, then a new origin
                      server is calculated and used. - `\"ip_cookie\"`: Behaves
                      the same as `\"cookie\"` except the initial origin
                      selection is stable and based on the client's ip address.
                      - `\"header\"`: On the first request to a proxied load
                      balancer, a session key based on the configured HTTP
                      headers (see `session_affinity_attributes.headers`) is
                      generated, encoding the request headers used for storing
                      in the load balancer session state which origin the
                      request will be forwarded to. Subsequent requests to the
                      load balancer with the same headers will be sent to the
                      same origin server, for the duration of the session and as
                      long as the origin server remains healthy. If the session
                      has been idle for the duration of `session_affinity_ttl`
                      seconds or the origin server is unhealthy, then a new
                      origin server is calculated and used. See `headers` in
                      `session_affinity_attributes` for additional required
                      configuration."
                  session_affinity_attributes:
                    type: object
                    properties:
                      drain_duration:
                        type: number
                        description: Configures the drain duration in seconds. This field is only used
                          when session affinity is enabled on the load balancer.
                      headers:
                        type: array
                        items:
                          type: string
                        description: 'Configures the names of HTTP headers to base session affinity on
                          when header `session_affinity` is enabled. At least
                          one HTTP header name must be provided. To specify the
                          exact cookies to be used, include an item in the
                          following format:
                          `"cookie:<cookie-name-1>,<cookie-name-2>"` (example)
                          where everything after the colon is a comma-separated
                          list of cookie names. Providing only `"cookie"` will
                          result in all cookies being used. The default max
                          number of HTTP header names that can be provided
                          depends on your plan: 5 for Enterprise, 1 for all
                          other plans.'
                      require_all_headers:
                        type: boolean
                        description: "When header `session_affinity` is enabled, this option can be used
                          to specify how HTTP headers on load balancing requests
                          will be used. The supported values are: - `\"true\"`:
                          Load balancing requests must contain _all_ of the HTTP
                          headers specified by the `headers` session affinity
                          attribute, otherwise sessions aren't created. -
                          `\"false\"`: Load balancing requests must contain _at
                          least one_ of the HTTP headers specified by the
                          `headers` session affinity attribute, otherwise
                          sessions aren't created."
                      samesite:
                        type: string
                        enum:
                          - Auto
                          - Lax
                          - None
                          - Strict
                        description: 'Configures the SameSite attribute on session affinity cookie.
                          Value "Auto" will be translated to "Lax" or "None"
                          depending if Always Use HTTPS is enabled. Note: when
                          using value "None", the secure attribute can not be
                          set to "Never".'
                      secure:
                        type: string
                        enum:
                          - Auto
                          - Always
                          - Never
                        description: Configures the Secure attribute on session affinity cookie. Value
                          "Always" indicates the Secure attribute will be set in
                          the Set-Cookie header, "Never" indicates the Secure
                          attribute will not be set, and "Auto" will set the
                          Secure attribute depending if Always Use HTTPS is
                          enabled.
                      zero_downtime_failover:
                        type: string
                        enum:
                          - none
                          - temporary
                          - sticky
                        description: 'Configures the zero-downtime failover between origins within a
                          pool when session affinity is enabled. This feature is
                          currently incompatible with Argo, Tiered Cache, and
                          Bandwidth Alliance. The supported values are: -
                          `"none"`: No failover takes place for sessions pinned
                          to the origin (default). - `"temporary"`: Traffic will
                          be sent to another other healthy origin until the
                          originally pinned origin is available; note that this
                          can potentially result in heavy origin flapping. -
                          `"sticky"`: The session affinity cookie is updated and
                          subsequent requests are sent to the new origin. Note:
                          Zero-downtime failover with sticky sessions is
                          currently not supported for session affinity by
                          header.'
                    description: Configures attributes for session affinity.
                  session_affinity_ttl:
                    type: number
                    description: "Time, in seconds, until a client's session expires after being
                      created. Once the expiry time has been reached, subsequent
                      requests may get sent to a different origin server. The
                      accepted ranges per `session_affinity` policy are: -
                      `\"cookie\"` / `\"ip_cookie\"`: The current default of 23
                      hours will be used unless explicitly set. The accepted
                      range of values is between [1800, 604800]. - `\"header\"`:
                      The current default of 1800 seconds will be used unless
                      explicitly set. The accepted range of values is between
                      [30, 3600]. Note: With session affinity by header,
                      sessions only expire after they haven't been used for the
                      number of seconds specified."
                  steering_policy:
                    type: string
                    enum:
                      - off
                      - geo
                      - random
                      - dynamic_latency
                      - proximity
                      - least_outstanding_requests
                      - least_connections
                      - ""
                    description: "Steering Policy for this load balancer.  - `\"off\"`: Use
                      `default_pools`. - `\"geo\"`: Use
                      `region_pools`/`country_pools`/`pop_pools`. For
                      non-proxied requests, the country for `country_pools` is
                      determined by `location_strategy`. - `\"random\"`: Select
                      a pool randomly. - `\"dynamic_latency\"`: Use round trip
                      time to select the closest pool in default_pools (requires
                      pool health checks). - `\"proximity\"`: Use the pools'
                      latitude and longitude to select the closest pool using
                      the Cloudflare PoP location for proxied requests or the
                      location determined by `location_strategy` for non-proxied
                      requests. - `\"least_outstanding_requests\"`: Select a
                      pool by taking into consideration `random_steering`
                      weights, as well as each pool's number of outstanding
                      requests. Pools with more pending requests are weighted
                      proportionately less relative to others. -
                      `\"least_connections\"`: Select a pool by taking into
                      consideration `random_steering` weights, as well as each
                      pool's number of open connections. Pools with more open
                      connections are weighted proportionately less relative to
                      others. Supported for HTTP/1 and HTTP/2 connections. -
                      `\"\"`: Will map to `\"geo\"` if you use
                      `region_pools`/`country_pools`/`pop_pools` otherwise
                      `\"off\"`."
                  ttl:
                    type: number
                    description: Time to live (TTL) of the DNS entry for the IP address returned by
                      this load balancer. This only applies to gray-clouded
                      (unproxied) load balancers.
                  zone_name:
                    type: string
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/monitors/{monitorId}:
    get:
      operationId: getMonitor
      description: "List a single configured monitor for an account.  @example ```ts
        const monitor = await client.loadBalancers.monitors.get(
        'f1aba936b94213e5b8dca0c0dbf1f9cc', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: monitorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  allow_insecure:
                    type: boolean
                    description: Do not validate the certificate when monitor use HTTPS. This
                      parameter is currently only valid for HTTP and HTTPS
                      monitors.
                  consecutive_down:
                    type: number
                    description: To be marked unhealthy the monitored origin must fail this
                      healthcheck N consecutive times.
                  consecutive_up:
                    type: number
                    description: To be marked healthy the monitored origin must pass this
                      healthcheck N consecutive times.
                  created_on:
                    type: string
                  description:
                    type: string
                    description: Object description.
                  expected_body:
                    type: string
                    description: A case-insensitive sub-string to look for in the response body. If
                      this string is not found, the origin will be marked as
                      unhealthy. This parameter is only valid for HTTP and HTTPS
                      monitors.
                  expected_codes:
                    type: string
                    description: The expected HTTP response code or code range of the health check.
                      This parameter is only valid for HTTP and HTTPS monitors.
                  follow_redirects:
                    type: boolean
                    description: Follow redirects if returned by the origin. This parameter is only
                      valid for HTTP and HTTPS monitors.
                  header:
                    type: object
                    additionalProperties: true
                    description: The HTTP request headers to send in the health check. It is
                      recommended you set a Host header by default. The
                      User-Agent header cannot be overridden. This parameter is
                      only valid for HTTP and HTTPS monitors.
                  interval:
                    type: number
                    description: The interval between each health check. Shorter intervals may
                      improve failover time, but will increase load on the
                      origins as we check from multiple locations.
                  method:
                    type: string
                    description: The method to use for the health check. This defaults to 'GET' for
                      HTTP/HTTPS based checks and 'connection_established' for
                      TCP based health checks.
                  modified_on:
                    type: string
                  path:
                    type: string
                    description: The endpoint path you want to conduct a health check against. This
                      parameter is only valid for HTTP and HTTPS monitors.
                  port:
                    oneOf:
                      - type: number
                    nullable: true
                    description: "The port number to connect to for the health check. Required for
                      TCP, UDP, and SMTP checks. HTTP and HTTPS checks should
                      only define the port when using a non-standard port (HTTP:
                      default 80, HTTPS: default 443)."
                  probe_zone:
                    type: string
                    description: Assign this monitor to emulate the specified zone while probing.
                      This parameter is only valid for HTTP and HTTPS monitors.
                  retries:
                    type: number
                    description: The number of retries to attempt in case of a timeout before
                      marking the origin as unhealthy. Retries are attempted
                      immediately.
                  timeout:
                    type: number
                    description: The timeout (in seconds) before marking the health check as failed.
                  type:
                    type: string
                    enum:
                      - http
                      - https
                      - tcp
                      - udp_icmp
                      - icmp_ping
                      - smtp
                    description: The protocol to use for the health check. Currently supported
                      protocols are 'HTTP','HTTPS', 'TCP', 'ICMP-PING',
                      'UDP-ICMP', and 'SMTP'.
      x-distilled-response-path: result
    put:
      operationId: updateMonitor
      description: "Modify a configured monitor.  @example ```ts const monitor = await
        client.loadBalancers.monitors.update(
        'f1aba936b94213e5b8dca0c0dbf1f9cc', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: monitorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                allow_insecure:
                  type: boolean
                  description: "Body param: Do not validate the certificate when monitor use
                    HTTPS. This parameter is currently only valid for HTTP and
                    HTTPS monitors."
                consecutive_down:
                  type: number
                  description: "Body param: To be marked unhealthy the monitored origin must fail
                    this healthcheck N consecutive times."
                consecutive_up:
                  type: number
                  description: "Body param: To be marked healthy the monitored origin must pass
                    this healthcheck N consecutive times."
                description:
                  type: string
                  description: "Body param: Object description."
                expected_body:
                  type: string
                  description: "Body param: A case-insensitive sub-string to look for in the
                    response body. If this string is not found, the origin will
                    be marked as unhealthy. This parameter is only valid for
                    HTTP and HTTPS monitors."
                expected_codes:
                  type: string
                  description: "Body param: The expected HTTP response code or code range of the
                    health check. This parameter is only valid for HTTP and
                    HTTPS monitors."
                follow_redirects:
                  type: boolean
                  description: "Body param: Follow redirects if returned by the origin. This
                    parameter is only valid for HTTP and HTTPS monitors."
                header:
                  type: object
                  additionalProperties: true
                  description: "Body param: The HTTP request headers to send in the health check.
                    It is recommended you set a Host header by default. The
                    User-Agent header cannot be overridden. This parameter is
                    only valid for HTTP and HTTPS monitors."
                interval:
                  type: number
                  description: "Body param: The interval between each health check. Shorter
                    intervals may improve failover time, but will increase load
                    on the origins as we check from multiple locations."
                method:
                  type: string
                  description: "Body param: The method to use for the health check. This defaults
                    to 'GET' for HTTP/HTTPS based checks and
                    'connection_established' for TCP based health checks."
                path:
                  type: string
                  description: "Body param: The endpoint path you want to conduct a health check
                    against. This parameter is only valid for HTTP and HTTPS
                    monitors."
                port:
                  oneOf:
                    - type: number
                  nullable: true
                  description: "Body param: The port number to connect to for the health check.
                    Required for TCP, UDP, and SMTP checks. HTTP and HTTPS
                    checks should only define the port when using a non-standard
                    port (HTTP: default 80, HTTPS: default 443)."
                probe_zone:
                  type: string
                  description: "Body param: Assign this monitor to emulate the specified zone
                    while probing. This parameter is only valid for HTTP and
                    HTTPS monitors."
                retries:
                  type: number
                  description: "Body param: The number of retries to attempt in case of a timeout
                    before marking the origin as unhealthy. Retries are
                    attempted immediately."
                timeout:
                  type: number
                  description: "Body param: The timeout (in seconds) before marking the health
                    check as failed."
                type:
                  type: string
                  enum:
                    - http
                    - https
                    - tcp
                    - udp_icmp
                    - icmp_ping
                    - smtp
                  description: "Body param: The protocol to use for the health check. Currently
                    supported protocols are 'HTTP','HTTPS', 'TCP', 'ICMP-PING',
                    'UDP-ICMP', and 'SMTP'."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  allow_insecure:
                    type: boolean
                    description: Do not validate the certificate when monitor use HTTPS. This
                      parameter is currently only valid for HTTP and HTTPS
                      monitors.
                  consecutive_down:
                    type: number
                    description: To be marked unhealthy the monitored origin must fail this
                      healthcheck N consecutive times.
                  consecutive_up:
                    type: number
                    description: To be marked healthy the monitored origin must pass this
                      healthcheck N consecutive times.
                  created_on:
                    type: string
                  description:
                    type: string
                    description: Object description.
                  expected_body:
                    type: string
                    description: A case-insensitive sub-string to look for in the response body. If
                      this string is not found, the origin will be marked as
                      unhealthy. This parameter is only valid for HTTP and HTTPS
                      monitors.
                  expected_codes:
                    type: string
                    description: The expected HTTP response code or code range of the health check.
                      This parameter is only valid for HTTP and HTTPS monitors.
                  follow_redirects:
                    type: boolean
                    description: Follow redirects if returned by the origin. This parameter is only
                      valid for HTTP and HTTPS monitors.
                  header:
                    type: object
                    additionalProperties: true
                    description: The HTTP request headers to send in the health check. It is
                      recommended you set a Host header by default. The
                      User-Agent header cannot be overridden. This parameter is
                      only valid for HTTP and HTTPS monitors.
                  interval:
                    type: number
                    description: The interval between each health check. Shorter intervals may
                      improve failover time, but will increase load on the
                      origins as we check from multiple locations.
                  method:
                    type: string
                    description: The method to use for the health check. This defaults to 'GET' for
                      HTTP/HTTPS based checks and 'connection_established' for
                      TCP based health checks.
                  modified_on:
                    type: string
                  path:
                    type: string
                    description: The endpoint path you want to conduct a health check against. This
                      parameter is only valid for HTTP and HTTPS monitors.
                  port:
                    oneOf:
                      - type: number
                    nullable: true
                    description: "The port number to connect to for the health check. Required for
                      TCP, UDP, and SMTP checks. HTTP and HTTPS checks should
                      only define the port when using a non-standard port (HTTP:
                      default 80, HTTPS: default 443)."
                  probe_zone:
                    type: string
                    description: Assign this monitor to emulate the specified zone while probing.
                      This parameter is only valid for HTTP and HTTPS monitors.
                  retries:
                    type: number
                    description: The number of retries to attempt in case of a timeout before
                      marking the origin as unhealthy. Retries are attempted
                      immediately.
                  timeout:
                    type: number
                    description: The timeout (in seconds) before marking the health check as failed.
                  type:
                    type: string
                    enum:
                      - http
                      - https
                      - tcp
                      - udp_icmp
                      - icmp_ping
                      - smtp
                    description: The protocol to use for the health check. Currently supported
                      protocols are 'HTTP','HTTPS', 'TCP', 'ICMP-PING',
                      'UDP-ICMP', and 'SMTP'.
      x-distilled-response-path: result
    patch:
      operationId: patchMonitor
      description: "Apply changes to an existing monitor, overwriting the supplied
        properties.  @example ```ts const monitor = await
        client.loadBalancers.monitors.edit( 'f1aba936b94213e5b8dca0c0dbf1f9cc',
        { account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: monitorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                allow_insecure:
                  type: boolean
                  description: "Body param: Do not validate the certificate when monitor use
                    HTTPS. This parameter is currently only valid for HTTP and
                    HTTPS monitors."
                consecutive_down:
                  type: number
                  description: "Body param: To be marked unhealthy the monitored origin must fail
                    this healthcheck N consecutive times."
                consecutive_up:
                  type: number
                  description: "Body param: To be marked healthy the monitored origin must pass
                    this healthcheck N consecutive times."
                description:
                  type: string
                  description: "Body param: Object description."
                expected_body:
                  type: string
                  description: "Body param: A case-insensitive sub-string to look for in the
                    response body. If this string is not found, the origin will
                    be marked as unhealthy. This parameter is only valid for
                    HTTP and HTTPS monitors."
                expected_codes:
                  type: string
                  description: "Body param: The expected HTTP response code or code range of the
                    health check. This parameter is only valid for HTTP and
                    HTTPS monitors."
                follow_redirects:
                  type: boolean
                  description: "Body param: Follow redirects if returned by the origin. This
                    parameter is only valid for HTTP and HTTPS monitors."
                header:
                  type: object
                  additionalProperties: true
                  description: "Body param: The HTTP request headers to send in the health check.
                    It is recommended you set a Host header by default. The
                    User-Agent header cannot be overridden. This parameter is
                    only valid for HTTP and HTTPS monitors."
                interval:
                  type: number
                  description: "Body param: The interval between each health check. Shorter
                    intervals may improve failover time, but will increase load
                    on the origins as we check from multiple locations."
                method:
                  type: string
                  description: "Body param: The method to use for the health check. This defaults
                    to 'GET' for HTTP/HTTPS based checks and
                    'connection_established' for TCP based health checks."
                path:
                  type: string
                  description: "Body param: The endpoint path you want to conduct a health check
                    against. This parameter is only valid for HTTP and HTTPS
                    monitors."
                port:
                  oneOf:
                    - type: number
                  nullable: true
                  description: "Body param: The port number to connect to for the health check.
                    Required for TCP, UDP, and SMTP checks. HTTP and HTTPS
                    checks should only define the port when using a non-standard
                    port (HTTP: default 80, HTTPS: default 443)."
                probe_zone:
                  type: string
                  description: "Body param: Assign this monitor to emulate the specified zone
                    while probing. This parameter is only valid for HTTP and
                    HTTPS monitors."
                retries:
                  type: number
                  description: "Body param: The number of retries to attempt in case of a timeout
                    before marking the origin as unhealthy. Retries are
                    attempted immediately."
                timeout:
                  type: number
                  description: "Body param: The timeout (in seconds) before marking the health
                    check as failed."
                type:
                  type: string
                  enum:
                    - http
                    - https
                    - tcp
                    - udp_icmp
                    - icmp_ping
                    - smtp
                  description: "Body param: The protocol to use for the health check. Currently
                    supported protocols are 'HTTP','HTTPS', 'TCP', 'ICMP-PING',
                    'UDP-ICMP', and 'SMTP'."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  allow_insecure:
                    type: boolean
                    description: Do not validate the certificate when monitor use HTTPS. This
                      parameter is currently only valid for HTTP and HTTPS
                      monitors.
                  consecutive_down:
                    type: number
                    description: To be marked unhealthy the monitored origin must fail this
                      healthcheck N consecutive times.
                  consecutive_up:
                    type: number
                    description: To be marked healthy the monitored origin must pass this
                      healthcheck N consecutive times.
                  created_on:
                    type: string
                  description:
                    type: string
                    description: Object description.
                  expected_body:
                    type: string
                    description: A case-insensitive sub-string to look for in the response body. If
                      this string is not found, the origin will be marked as
                      unhealthy. This parameter is only valid for HTTP and HTTPS
                      monitors.
                  expected_codes:
                    type: string
                    description: The expected HTTP response code or code range of the health check.
                      This parameter is only valid for HTTP and HTTPS monitors.
                  follow_redirects:
                    type: boolean
                    description: Follow redirects if returned by the origin. This parameter is only
                      valid for HTTP and HTTPS monitors.
                  header:
                    type: object
                    additionalProperties: true
                    description: The HTTP request headers to send in the health check. It is
                      recommended you set a Host header by default. The
                      User-Agent header cannot be overridden. This parameter is
                      only valid for HTTP and HTTPS monitors.
                  interval:
                    type: number
                    description: The interval between each health check. Shorter intervals may
                      improve failover time, but will increase load on the
                      origins as we check from multiple locations.
                  method:
                    type: string
                    description: The method to use for the health check. This defaults to 'GET' for
                      HTTP/HTTPS based checks and 'connection_established' for
                      TCP based health checks.
                  modified_on:
                    type: string
                  path:
                    type: string
                    description: The endpoint path you want to conduct a health check against. This
                      parameter is only valid for HTTP and HTTPS monitors.
                  port:
                    oneOf:
                      - type: number
                    nullable: true
                    description: "The port number to connect to for the health check. Required for
                      TCP, UDP, and SMTP checks. HTTP and HTTPS checks should
                      only define the port when using a non-standard port (HTTP:
                      default 80, HTTPS: default 443)."
                  probe_zone:
                    type: string
                    description: Assign this monitor to emulate the specified zone while probing.
                      This parameter is only valid for HTTP and HTTPS monitors.
                  retries:
                    type: number
                    description: The number of retries to attempt in case of a timeout before
                      marking the origin as unhealthy. Retries are attempted
                      immediately.
                  timeout:
                    type: number
                    description: The timeout (in seconds) before marking the health check as failed.
                  type:
                    type: string
                    enum:
                      - http
                      - https
                      - tcp
                      - udp_icmp
                      - icmp_ping
                      - smtp
                    description: The protocol to use for the health check. Currently supported
                      protocols are 'HTTP','HTTPS', 'TCP', 'ICMP-PING',
                      'UDP-ICMP', and 'SMTP'.
      x-distilled-response-path: result
    delete:
      operationId: deleteMonitor
      description: "Delete a configured monitor.  @example ```ts const monitor = await
        client.loadBalancers.monitors.delete(
        'f1aba936b94213e5b8dca0c0dbf1f9cc', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: monitorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/monitors:
    get:
      operationId: listMonitors
      description: "List configured monitors for an account.  @example ```ts //
        Automatically fetches more pages as needed. for await (const monitor of
        client.loadBalancers.monitors.list( { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  allow_insecure:
                    type: boolean
                    description: Do not validate the certificate when monitor use HTTPS. This
                      parameter is currently only valid for HTTP and HTTPS
                      monitors.
                  consecutive_down:
                    type: number
                    description: To be marked unhealthy the monitored origin must fail this
                      healthcheck N consecutive times.
                  consecutive_up:
                    type: number
                    description: To be marked healthy the monitored origin must pass this
                      healthcheck N consecutive times.
                  created_on:
                    type: string
                  description:
                    type: string
                    description: Object description.
                  expected_body:
                    type: string
                    description: A case-insensitive sub-string to look for in the response body. If
                      this string is not found, the origin will be marked as
                      unhealthy. This parameter is only valid for HTTP and HTTPS
                      monitors.
                  expected_codes:
                    type: string
                    description: The expected HTTP response code or code range of the health check.
                      This parameter is only valid for HTTP and HTTPS monitors.
                  follow_redirects:
                    type: boolean
                    description: Follow redirects if returned by the origin. This parameter is only
                      valid for HTTP and HTTPS monitors.
                  header:
                    type: object
                    additionalProperties: true
                    description: The HTTP request headers to send in the health check. It is
                      recommended you set a Host header by default. The
                      User-Agent header cannot be overridden. This parameter is
                      only valid for HTTP and HTTPS monitors.
                  interval:
                    type: number
                    description: The interval between each health check. Shorter intervals may
                      improve failover time, but will increase load on the
                      origins as we check from multiple locations.
                  method:
                    type: string
                    description: The method to use for the health check. This defaults to 'GET' for
                      HTTP/HTTPS based checks and 'connection_established' for
                      TCP based health checks.
                  modified_on:
                    type: string
                  path:
                    type: string
                    description: The endpoint path you want to conduct a health check against. This
                      parameter is only valid for HTTP and HTTPS monitors.
                  port:
                    oneOf:
                      - type: number
                    nullable: true
                    description: "The port number to connect to for the health check. Required for
                      TCP, UDP, and SMTP checks. HTTP and HTTPS checks should
                      only define the port when using a non-standard port (HTTP:
                      default 80, HTTPS: default 443)."
                  probe_zone:
                    type: string
                    description: Assign this monitor to emulate the specified zone while probing.
                      This parameter is only valid for HTTP and HTTPS monitors.
                  retries:
                    type: number
                    description: The number of retries to attempt in case of a timeout before
                      marking the origin as unhealthy. Retries are attempted
                      immediately.
                  timeout:
                    type: number
                    description: The timeout (in seconds) before marking the health check as failed.
                  type:
                    type: string
                    enum:
                      - http
                      - https
                      - tcp
                      - udp_icmp
                      - icmp_ping
                      - smtp
                    description: The protocol to use for the health check. Currently supported
                      protocols are 'HTTP','HTTPS', 'TCP', 'ICMP-PING',
                      'UDP-ICMP', and 'SMTP'.
      x-distilled-pagination-class: MonitorsSinglePage
    post:
      operationId: createMonitor
      description: "Create a configured monitor.  @example ```ts const monitor = await
        client.loadBalancers.monitors.create({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                allow_insecure:
                  type: boolean
                  description: "Body param: Do not validate the certificate when monitor use
                    HTTPS. This parameter is currently only valid for HTTP and
                    HTTPS monitors."
                consecutive_down:
                  type: number
                  description: "Body param: To be marked unhealthy the monitored origin must fail
                    this healthcheck N consecutive times."
                consecutive_up:
                  type: number
                  description: "Body param: To be marked healthy the monitored origin must pass
                    this healthcheck N consecutive times."
                description:
                  type: string
                  description: "Body param: Object description."
                expected_body:
                  type: string
                  description: "Body param: A case-insensitive sub-string to look for in the
                    response body. If this string is not found, the origin will
                    be marked as unhealthy. This parameter is only valid for
                    HTTP and HTTPS monitors."
                expected_codes:
                  type: string
                  description: "Body param: The expected HTTP response code or code range of the
                    health check. This parameter is only valid for HTTP and
                    HTTPS monitors."
                follow_redirects:
                  type: boolean
                  description: "Body param: Follow redirects if returned by the origin. This
                    parameter is only valid for HTTP and HTTPS monitors."
                header:
                  type: object
                  additionalProperties: true
                  description: "Body param: The HTTP request headers to send in the health check.
                    It is recommended you set a Host header by default. The
                    User-Agent header cannot be overridden. This parameter is
                    only valid for HTTP and HTTPS monitors."
                interval:
                  type: number
                  description: "Body param: The interval between each health check. Shorter
                    intervals may improve failover time, but will increase load
                    on the origins as we check from multiple locations."
                method:
                  type: string
                  description: "Body param: The method to use for the health check. This defaults
                    to 'GET' for HTTP/HTTPS based checks and
                    'connection_established' for TCP based health checks."
                path:
                  type: string
                  description: "Body param: The endpoint path you want to conduct a health check
                    against. This parameter is only valid for HTTP and HTTPS
                    monitors."
                port:
                  oneOf:
                    - type: number
                  nullable: true
                  description: "Body param: The port number to connect to for the health check.
                    Required for TCP, UDP, and SMTP checks. HTTP and HTTPS
                    checks should only define the port when using a non-standard
                    port (HTTP: default 80, HTTPS: default 443)."
                probe_zone:
                  type: string
                  description: "Body param: Assign this monitor to emulate the specified zone
                    while probing. This parameter is only valid for HTTP and
                    HTTPS monitors."
                retries:
                  type: number
                  description: "Body param: The number of retries to attempt in case of a timeout
                    before marking the origin as unhealthy. Retries are
                    attempted immediately."
                timeout:
                  type: number
                  description: "Body param: The timeout (in seconds) before marking the health
                    check as failed."
                type:
                  type: string
                  enum:
                    - http
                    - https
                    - tcp
                    - udp_icmp
                    - icmp_ping
                    - smtp
                  description: "Body param: The protocol to use for the health check. Currently
                    supported protocols are 'HTTP','HTTPS', 'TCP', 'ICMP-PING',
                    'UDP-ICMP', and 'SMTP'."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  allow_insecure:
                    type: boolean
                    description: Do not validate the certificate when monitor use HTTPS. This
                      parameter is currently only valid for HTTP and HTTPS
                      monitors.
                  consecutive_down:
                    type: number
                    description: To be marked unhealthy the monitored origin must fail this
                      healthcheck N consecutive times.
                  consecutive_up:
                    type: number
                    description: To be marked healthy the monitored origin must pass this
                      healthcheck N consecutive times.
                  created_on:
                    type: string
                  description:
                    type: string
                    description: Object description.
                  expected_body:
                    type: string
                    description: A case-insensitive sub-string to look for in the response body. If
                      this string is not found, the origin will be marked as
                      unhealthy. This parameter is only valid for HTTP and HTTPS
                      monitors.
                  expected_codes:
                    type: string
                    description: The expected HTTP response code or code range of the health check.
                      This parameter is only valid for HTTP and HTTPS monitors.
                  follow_redirects:
                    type: boolean
                    description: Follow redirects if returned by the origin. This parameter is only
                      valid for HTTP and HTTPS monitors.
                  header:
                    type: object
                    additionalProperties: true
                    description: The HTTP request headers to send in the health check. It is
                      recommended you set a Host header by default. The
                      User-Agent header cannot be overridden. This parameter is
                      only valid for HTTP and HTTPS monitors.
                  interval:
                    type: number
                    description: The interval between each health check. Shorter intervals may
                      improve failover time, but will increase load on the
                      origins as we check from multiple locations.
                  method:
                    type: string
                    description: The method to use for the health check. This defaults to 'GET' for
                      HTTP/HTTPS based checks and 'connection_established' for
                      TCP based health checks.
                  modified_on:
                    type: string
                  path:
                    type: string
                    description: The endpoint path you want to conduct a health check against. This
                      parameter is only valid for HTTP and HTTPS monitors.
                  port:
                    oneOf:
                      - type: number
                    nullable: true
                    description: "The port number to connect to for the health check. Required for
                      TCP, UDP, and SMTP checks. HTTP and HTTPS checks should
                      only define the port when using a non-standard port (HTTP:
                      default 80, HTTPS: default 443)."
                  probe_zone:
                    type: string
                    description: Assign this monitor to emulate the specified zone while probing.
                      This parameter is only valid for HTTP and HTTPS monitors.
                  retries:
                    type: number
                    description: The number of retries to attempt in case of a timeout before
                      marking the origin as unhealthy. Retries are attempted
                      immediately.
                  timeout:
                    type: number
                    description: The timeout (in seconds) before marking the health check as failed.
                  type:
                    type: string
                    enum:
                      - http
                      - https
                      - tcp
                      - udp_icmp
                      - icmp_ping
                      - smtp
                    description: The protocol to use for the health check. Currently supported
                      protocols are 'HTTP','HTTPS', 'TCP', 'ICMP-PING',
                      'UDP-ICMP', and 'SMTP'.
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/monitor_groups/{monitorGroupId}:
    get:
      operationId: getMonitorGroup
      description: "Fetch a single configured monitor group.  @example ```ts const
        monitorGroup = await client.loadBalancers.monitorGroups.get(
        '17b5962d775c646f3f9725cbc7a53df4', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: monitorGroupId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  description:
                    type: string
                    description: A short description of the monitor group
                  members:
                    type: array
                    items:
                      type: object
                      properties:
                        enabled:
                          type: boolean
                          description: Whether this monitor is enabled in the group
                        monitor_id:
                          type: string
                          description: The ID of the Monitor to use for checking the health of origins
                            within this pool.
                        monitoring_only:
                          type: boolean
                          description: Whether this monitor is used for monitoring only (does not affect
                            pool health)
                        must_be_healthy:
                          type: boolean
                          description: Whether this monitor must be healthy for the pool to be considered
                            healthy
                        created_at:
                          type: string
                          description: The timestamp of when the monitor was added to the group
                        updated_at:
                          type: string
                          description: The timestamp of when the monitor group member was last updated
                      required:
                        - enabled
                        - monitor_id
                        - monitoring_only
                        - must_be_healthy
                    description: List of monitors in this group
                  created_at:
                    type: string
                    description: The timestamp of when the monitor group was created
                  updated_at:
                    type: string
                    description: The timestamp of when the monitor group was last updated
                required:
                  - id
                  - description
                  - members
      x-distilled-response-path: result
    put:
      operationId: updateMonitorGroup
      description: "Modify a configured monitor group.  @example ```ts const
        monitorGroup = await client.loadBalancers.monitorGroups.update(
        '17b5962d775c646f3f9725cbc7a53df4', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', id: 'id', description: 'Primary
        datacenter monitors', members: [ { enabled: true, monitor_id:
        'monitor_id', monitoring_only: false, must_be_healthy: true, }, ], }, );
        ```"
      parameters:
        - name: monitorGroupId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
                  description: "Body param: The ID of the Monitor Group to use for checking the
                    health of origins within this pool."
                description:
                  type: string
                  description: "Body param: A short description of the monitor group"
                members:
                  type: array
                  items:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                        description: Whether this monitor is enabled in the group
                      monitor_id:
                        type: string
                        description: The ID of the Monitor to use for checking the health of origins
                          within this pool.
                      monitoring_only:
                        type: boolean
                        description: Whether this monitor is used for monitoring only (does not affect
                          pool health)
                      must_be_healthy:
                        type: boolean
                        description: Whether this monitor must be healthy for the pool to be considered
                          healthy
                    required:
                      - enabled
                      - monitor_id
                      - monitoring_only
                      - must_be_healthy
                  description: "Body param: List of monitors in this group"
              required:
                - id
                - description
                - members
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  description:
                    type: string
                    description: A short description of the monitor group
                  members:
                    type: array
                    items:
                      type: object
                      properties:
                        enabled:
                          type: boolean
                          description: Whether this monitor is enabled in the group
                        monitor_id:
                          type: string
                          description: The ID of the Monitor to use for checking the health of origins
                            within this pool.
                        monitoring_only:
                          type: boolean
                          description: Whether this monitor is used for monitoring only (does not affect
                            pool health)
                        must_be_healthy:
                          type: boolean
                          description: Whether this monitor must be healthy for the pool to be considered
                            healthy
                        created_at:
                          type: string
                          description: The timestamp of when the monitor was added to the group
                        updated_at:
                          type: string
                          description: The timestamp of when the monitor group member was last updated
                      required:
                        - enabled
                        - monitor_id
                        - monitoring_only
                        - must_be_healthy
                    description: List of monitors in this group
                  created_at:
                    type: string
                    description: The timestamp of when the monitor group was created
                  updated_at:
                    type: string
                    description: The timestamp of when the monitor group was last updated
                required:
                  - id
                  - description
                  - members
      x-distilled-response-path: result
    patch:
      operationId: patchMonitorGroup
      description: "Apply changes to an existing monitor group, overwriting the
        supplied properties.  @example ```ts const monitorGroup = await
        client.loadBalancers.monitorGroups.edit(
        '17b5962d775c646f3f9725cbc7a53df4', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', id: 'id', description: 'Primary
        datacenter monitors', members: [ { enabled: true, monitor_id:
        'monitor_id', monitoring_only: false, must_be_healthy: true, }, ], }, );
        ```"
      parameters:
        - name: monitorGroupId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
                  description: "Body param: The ID of the Monitor Group to use for checking the
                    health of origins within this pool."
                description:
                  type: string
                  description: "Body param: A short description of the monitor group"
                members:
                  type: array
                  items:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                        description: Whether this monitor is enabled in the group
                      monitor_id:
                        type: string
                        description: The ID of the Monitor to use for checking the health of origins
                          within this pool.
                      monitoring_only:
                        type: boolean
                        description: Whether this monitor is used for monitoring only (does not affect
                          pool health)
                      must_be_healthy:
                        type: boolean
                        description: Whether this monitor must be healthy for the pool to be considered
                          healthy
                    required:
                      - enabled
                      - monitor_id
                      - monitoring_only
                      - must_be_healthy
                  description: "Body param: List of monitors in this group"
              required:
                - id
                - description
                - members
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  description:
                    type: string
                    description: A short description of the monitor group
                  members:
                    type: array
                    items:
                      type: object
                      properties:
                        enabled:
                          type: boolean
                          description: Whether this monitor is enabled in the group
                        monitor_id:
                          type: string
                          description: The ID of the Monitor to use for checking the health of origins
                            within this pool.
                        monitoring_only:
                          type: boolean
                          description: Whether this monitor is used for monitoring only (does not affect
                            pool health)
                        must_be_healthy:
                          type: boolean
                          description: Whether this monitor must be healthy for the pool to be considered
                            healthy
                        created_at:
                          type: string
                          description: The timestamp of when the monitor was added to the group
                        updated_at:
                          type: string
                          description: The timestamp of when the monitor group member was last updated
                      required:
                        - enabled
                        - monitor_id
                        - monitoring_only
                        - must_be_healthy
                    description: List of monitors in this group
                  created_at:
                    type: string
                    description: The timestamp of when the monitor group was created
                  updated_at:
                    type: string
                    description: The timestamp of when the monitor group was last updated
                required:
                  - id
                  - description
                  - members
      x-distilled-response-path: result
    delete:
      operationId: deleteMonitorGroup
      description: "Delete a configured monitor group.  @example ```ts const
        monitorGroup = await client.loadBalancers.monitorGroups.delete(
        '17b5962d775c646f3f9725cbc7a53df4', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: monitorGroupId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  description:
                    type: string
                    description: A short description of the monitor group
                  members:
                    type: array
                    items:
                      type: object
                      properties:
                        enabled:
                          type: boolean
                          description: Whether this monitor is enabled in the group
                        monitor_id:
                          type: string
                          description: The ID of the Monitor to use for checking the health of origins
                            within this pool.
                        monitoring_only:
                          type: boolean
                          description: Whether this monitor is used for monitoring only (does not affect
                            pool health)
                        must_be_healthy:
                          type: boolean
                          description: Whether this monitor must be healthy for the pool to be considered
                            healthy
                        created_at:
                          type: string
                          description: The timestamp of when the monitor was added to the group
                        updated_at:
                          type: string
                          description: The timestamp of when the monitor group member was last updated
                      required:
                        - enabled
                        - monitor_id
                        - monitoring_only
                        - must_be_healthy
                    description: List of monitors in this group
                  created_at:
                    type: string
                    description: The timestamp of when the monitor group was created
                  updated_at:
                    type: string
                    description: The timestamp of when the monitor group was last updated
                required:
                  - id
                  - description
                  - members
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/monitor_groups:
    get:
      operationId: listMonitorGroups
      description: "List configured monitor groups.  @example ```ts // Automatically
        fetches more pages as needed. for await (const monitorGroup of
        client.loadBalancers.monitorGroups.list( { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  description:
                    type: string
                    description: A short description of the monitor group
                  members:
                    type: array
                    items:
                      type: object
                      properties:
                        enabled:
                          type: boolean
                          description: Whether this monitor is enabled in the group
                        monitor_id:
                          type: string
                          description: The ID of the Monitor to use for checking the health of origins
                            within this pool.
                        monitoring_only:
                          type: boolean
                          description: Whether this monitor is used for monitoring only (does not affect
                            pool health)
                        must_be_healthy:
                          type: boolean
                          description: Whether this monitor must be healthy for the pool to be considered
                            healthy
                        created_at:
                          type: string
                          description: The timestamp of when the monitor was added to the group
                        updated_at:
                          type: string
                          description: The timestamp of when the monitor group member was last updated
                      required:
                        - enabled
                        - monitor_id
                        - monitoring_only
                        - must_be_healthy
                    description: List of monitors in this group
                  created_at:
                    type: string
                    description: The timestamp of when the monitor group was created
                  updated_at:
                    type: string
                    description: The timestamp of when the monitor group was last updated
                required:
                  - id
                  - description
                  - members
      x-distilled-pagination-class: MonitorGroupsSinglePage
    post:
      operationId: createMonitorGroup
      description: "Create a new monitor group.  @example ```ts const monitorGroup =
        await client.loadBalancers.monitorGroups.create({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', id: 'id', description: 'Primary
        datacenter monitors', members: [ { enabled: true, monitor_id:
        'monitor_id', monitoring_only: false, must_be_healthy: true, }, ], });
        ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
                  description: "Body param: The ID of the Monitor Group to use for checking the
                    health of origins within this pool."
                description:
                  type: string
                  description: "Body param: A short description of the monitor group"
                members:
                  type: array
                  items:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                        description: Whether this monitor is enabled in the group
                      monitor_id:
                        type: string
                        description: The ID of the Monitor to use for checking the health of origins
                          within this pool.
                      monitoring_only:
                        type: boolean
                        description: Whether this monitor is used for monitoring only (does not affect
                          pool health)
                      must_be_healthy:
                        type: boolean
                        description: Whether this monitor must be healthy for the pool to be considered
                          healthy
                    required:
                      - enabled
                      - monitor_id
                      - monitoring_only
                      - must_be_healthy
                  description: "Body param: List of monitors in this group"
              required:
                - id
                - description
                - members
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  description:
                    type: string
                    description: A short description of the monitor group
                  members:
                    type: array
                    items:
                      type: object
                      properties:
                        enabled:
                          type: boolean
                          description: Whether this monitor is enabled in the group
                        monitor_id:
                          type: string
                          description: The ID of the Monitor to use for checking the health of origins
                            within this pool.
                        monitoring_only:
                          type: boolean
                          description: Whether this monitor is used for monitoring only (does not affect
                            pool health)
                        must_be_healthy:
                          type: boolean
                          description: Whether this monitor must be healthy for the pool to be considered
                            healthy
                        created_at:
                          type: string
                          description: The timestamp of when the monitor was added to the group
                        updated_at:
                          type: string
                          description: The timestamp of when the monitor group member was last updated
                      required:
                        - enabled
                        - monitor_id
                        - monitoring_only
                        - must_be_healthy
                    description: List of monitors in this group
                  created_at:
                    type: string
                    description: The timestamp of when the monitor group was created
                  updated_at:
                    type: string
                    description: The timestamp of when the monitor group was last updated
                required:
                  - id
                  - description
                  - members
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/monitors/{monitorId}/preview:
    post:
      operationId: createMonitorPreview
      description: "Preview pools using the specified monitor with provided monitor
        details. The returned preview_id can be used in the preview endpoint to
        retrieve the results.  @example ```ts const preview = await
        client.loadBalancers.monitors.previews.create(
        'f1aba936b94213e5b8dca0c0dbf1f9cc', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: monitorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                allow_insecure:
                  type: boolean
                  description: "Body param: Do not validate the certificate when monitor use
                    HTTPS. This parameter is currently only valid for HTTP and
                    HTTPS monitors."
                consecutive_down:
                  type: number
                  description: "Body param: To be marked unhealthy the monitored origin must fail
                    this healthcheck N consecutive times."
                consecutive_up:
                  type: number
                  description: "Body param: To be marked healthy the monitored origin must pass
                    this healthcheck N consecutive times."
                description:
                  type: string
                  description: "Body param: Object description."
                expected_body:
                  type: string
                  description: "Body param: A case-insensitive sub-string to look for in the
                    response body. If this string is not found, the origin will
                    be marked as unhealthy. This parameter is only valid for
                    HTTP and HTTPS monitors."
                expected_codes:
                  type: string
                  description: "Body param: The expected HTTP response code or code range of the
                    health check. This parameter is only valid for HTTP and
                    HTTPS monitors."
                follow_redirects:
                  type: boolean
                  description: "Body param: Follow redirects if returned by the origin. This
                    parameter is only valid for HTTP and HTTPS monitors."
                header:
                  type: object
                  additionalProperties: true
                  description: "Body param: The HTTP request headers to send in the health check.
                    It is recommended you set a Host header by default. The
                    User-Agent header cannot be overridden. This parameter is
                    only valid for HTTP and HTTPS monitors."
                interval:
                  type: number
                  description: "Body param: The interval between each health check. Shorter
                    intervals may improve failover time, but will increase load
                    on the origins as we check from multiple locations."
                method:
                  type: string
                  description: "Body param: The method to use for the health check. This defaults
                    to 'GET' for HTTP/HTTPS based checks and
                    'connection_established' for TCP based health checks."
                path:
                  type: string
                  description: "Body param: The endpoint path you want to conduct a health check
                    against. This parameter is only valid for HTTP and HTTPS
                    monitors."
                port:
                  oneOf:
                    - type: number
                  nullable: true
                  description: "Body param: The port number to connect to for the health check.
                    Required for TCP, UDP, and SMTP checks. HTTP and HTTPS
                    checks should only define the port when using a non-standard
                    port (HTTP: default 80, HTTPS: default 443)."
                probe_zone:
                  type: string
                  description: "Body param: Assign this monitor to emulate the specified zone
                    while probing. This parameter is only valid for HTTP and
                    HTTPS monitors."
                retries:
                  type: number
                  description: "Body param: The number of retries to attempt in case of a timeout
                    before marking the origin as unhealthy. Retries are
                    attempted immediately."
                timeout:
                  type: number
                  description: "Body param: The timeout (in seconds) before marking the health
                    check as failed."
                type:
                  type: string
                  enum:
                    - http
                    - https
                    - tcp
                    - udp_icmp
                    - icmp_ping
                    - smtp
                  description: "Body param: The protocol to use for the health check. Currently
                    supported protocols are 'HTTP','HTTPS', 'TCP', 'ICMP-PING',
                    'UDP-ICMP', and 'SMTP'."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  pools:
                    type: object
                    additionalProperties: true
                    description: Monitored pool IDs mapped to their respective names.
                  preview_id:
                    type: string
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/monitors/{monitorId}/references:
    get:
      operationId: getMonitorReference
      description: "Get the list of resources that reference the provided
        monitor.  @example ```ts // Automatically fetches more pages as needed.
        for await (const referenceGetResponse of
        client.loadBalancers.monitors.references.get(
        'f1aba936b94213e5b8dca0c0dbf1f9cc', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: monitorId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  reference_type:
                    type: string
                    enum:
                      - "*"
                      - referral
                      - referrer
                  resource_id:
                    type: string
                  resource_name:
                    type: string
                  resource_type:
                    type: string
      x-distilled-pagination-class: ReferenceGetResponsesSinglePage
  /accounts/{account_id}/load_balancers/pools/{poolId}:
    get:
      operationId: getPool
      description: "Fetch a single configured pool.  @example ```ts const pool = await
        client.loadBalancers.pools.get( '17b5962d775c646f3f9725cbc7a53df4', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: poolId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  check_regions:
                    oneOf:
                      - type: array
                        items:
                          type: string
                          enum:
                            - WNAM
                            - ENAM
                            - WEU
                            - EEU
                            - NSAM
                            - SSAM
                            - OC
                            - ME
                            - NAF
                            - SAF
                            - SAS
                            - SEAS
                            - NEAS
                            - ALL_REGIONS
                    nullable: true
                    description: A list of regions from which to run health checks. Null means every
                      Cloudflare data center.
                  created_on:
                    type: string
                  description:
                    type: string
                    description: A human-readable description of the pool.
                  disabled_at:
                    type: string
                    description: This field shows up only if the pool is disabled. This field is set
                      with the time the pool was disabled at.
                  enabled:
                    type: boolean
                    description: Whether to enable (the default) or disable this pool. Disabled
                      pools will not receive traffic and are excluded from
                      health checks. Disabling a pool will cause any load
                      balancers using it to failover to the next pool (if any).
                  latitude:
                    type: number
                    description: The latitude of the data center containing the origins used in this
                      pool in decimal degrees. If this is set, longitude must
                      also be set.
                  load_shedding:
                    oneOf:
                      - type: object
                        properties:
                          default_percent:
                            type: number
                            description: The percent of traffic to shed from the pool, according to the
                              default policy. Applies to new sessions and
                              traffic without session affinity.
                          default_policy:
                            type: string
                            enum:
                              - random
                              - hash
                            description: The default policy to use when load shedding. A random policy
                              randomly sheds a given percent of requests. A hash
                              policy computes a hash over the CF-Connecting-IP
                              address and sheds all requests originating from a
                              percent of IPs.
                          session_percent:
                            type: number
                            description: The percent of existing sessions to shed from the pool, according
                              to the session policy.
                          session_policy:
                            type: string
                            enum:
                              - hash
                            description: Only the hash policy is supported for existing sessions (to avoid
                              exponential decay).
                    nullable: true
                    description: Configures load shedding policies and percentages for the pool.
                  longitude:
                    type: number
                    description: The longitude of the data center containing the origins used in
                      this pool in decimal degrees. If this is set, latitude
                      must also be set.
                  minimum_origins:
                    type: number
                    description: The minimum number of origins that must be healthy for this pool to
                      serve traffic. If the number of healthy origins falls
                      below this number, the pool will be marked unhealthy and
                      will failover to the next available pool.
                  modified_on:
                    type: string
                  monitor:
                    type: string
                    description: The ID of the Monitor to use for checking the health of origins
                      within this pool.
                  monitor_group:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  name:
                    type: string
                    description: A short name (tag) for the pool. Only alphanumeric characters,
                      hyphens, and underscores are allowed.
                  networks:
                    type: array
                    items:
                      type: string
                    description: List of networks where Load Balancer or Pool is enabled.
                  notification_email:
                    type: string
                    description: This field is now deprecated. It has been moved to Cloudflare's
                      Centralized Notification service
                      https://developers.cloudflare.com/fundamentals/notifications/.
                      The email address to send health status notifications to.
                      This can be an individual mailbox or a mailing list.
                      Multiple emails can be supplied as a comma delimited list.
                  notification_filter:
                    oneOf:
                      - type: object
                        properties:
                          origin:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                          pool:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                    nullable: true
                    description: Filter pool and origin health notifications by resource type or
                      health status. Use null to reset.
                  origin_steering:
                    oneOf:
                      - type: object
                        properties:
                          policy:
                            type: string
                            enum:
                              - random
                              - hash
                              - least_outstanding_requests
                              - least_connections
                            description: "The type of origin steering policy to use.  - `\"random\"`: Select
                              an origin randomly. - `\"hash\"`: Select an origin
                              by computing a hash over the CF-Connecting-IP
                              address. - `\"least_outstanding_requests\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              outstanding requests. Origins with more pending
                              requests are weighted proportionately less
                              relative to others. - `\"least_connections\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              open connections. Origins with more open
                              connections are weighted proportionately less
                              relative to others. Supported for HTTP/1 and
                              HTTP/2 connections."
                    nullable: true
                    description: Configures origin steering for the pool. Controls how origins are
                      selected for new sessions and traffic without session
                      affinity.
                  origins:
                    type: array
                    items:
                      type: object
                      properties:
                        address:
                          type: string
                          description: The IP address (IPv4 or IPv6) of the origin, or its publicly
                            addressable hostname. Hostnames entered here should
                            resolve directly to the origin, and not be a
                            hostname proxied by Cloudflare. To set an
                            internal/reserved address, virtual_network_id must
                            also be set.
                        disabled_at:
                          type: string
                          description: This field shows up only if the origin is disabled. This field is
                            set with the time the origin was disabled.
                        enabled:
                          type: boolean
                          description: Whether to enable (the default) this origin within the pool.
                            Disabled origins will not receive traffic and are
                            excluded from health checks. The origin will only be
                            disabled for the current pool.
                        header:
                          type: object
                          properties:
                            Host:
                              type: array
                              items:
                                type: string
                              description: The 'Host' header allows to override the hostname set in the HTTP
                                request. Current support is 1 'Host' header
                                override per origin.
                          description: The request header is used to pass additional information with an
                            HTTP request. Currently supported header is 'Host'.
                        name:
                          type: string
                          description: A human-identifiable name for the origin.
                        port:
                          type: number
                          description: The port for upstream connections. A value of 0 means the default
                            port for the protocol will be used.
                        virtual_network_id:
                          type: string
                          description: The virtual network subnet ID the origin belongs in. Virtual
                            network must also belong to the account.
                        weight:
                          type: number
                          description: "The weight of this origin relative to other origins in the pool.
                            Based on the configured weight the total traffic is
                            distributed among origins within the pool.  -
                            `origin_steering.policy=\"least_outstanding_requests\
                            \"`: Use weight to scale the origin's outstanding
                            requests. -
                            `origin_steering.policy=\"least_connections\"`: Use
                            weight to scale the origin's open connections."
                    description: The list of origins within this pool. Traffic directed at this pool
                      is balanced across all currently healthy origins, provided
                      the pool itself is healthy.
      x-distilled-response-path: result
    put:
      operationId: updatePool
      description: "Modify a configured pool.  @example ```ts const pool = await
        client.loadBalancers.pools.update( '17b5962d775c646f3f9725cbc7a53df4', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353', name: 'primary-dc-1',
        origins: [{}], }, ); ```"
      parameters:
        - name: poolId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: "Body param: A short name (tag) for the pool. Only alphanumeric
                    characters, hyphens, and underscores are allowed."
                origins:
                  type: array
                  items:
                    type: object
                    properties:
                      address:
                        type: string
                        description: The IP address (IPv4 or IPv6) of the origin, or its publicly
                          addressable hostname. Hostnames entered here should
                          resolve directly to the origin, and not be a hostname
                          proxied by Cloudflare. To set an internal/reserved
                          address, virtual_network_id must also be set.
                      enabled:
                        type: boolean
                        description: Whether to enable (the default) this origin within the pool.
                          Disabled origins will not receive traffic and are
                          excluded from health checks. The origin will only be
                          disabled for the current pool.
                      header:
                        type: object
                        properties:
                          Host:
                            type: array
                            items:
                              type: string
                            description: The 'Host' header allows to override the hostname set in the HTTP
                              request. Current support is 1 'Host' header
                              override per origin.
                        description: The request header is used to pass additional information with an
                          HTTP request. Currently supported header is 'Host'.
                      name:
                        type: string
                        description: A human-identifiable name for the origin.
                      port:
                        type: number
                        description: The port for upstream connections. A value of 0 means the default
                          port for the protocol will be used.
                      virtual_network_id:
                        type: string
                        description: The virtual network subnet ID the origin belongs in. Virtual
                          network must also belong to the account.
                      weight:
                        type: number
                        description: "The weight of this origin relative to other origins in the pool.
                          Based on the configured weight the total traffic is
                          distributed among origins within the pool.  -
                          `origin_steering.policy=\"least_outstanding_requests\
                          \"`: Use weight to scale the origin's outstanding
                          requests. -
                          `origin_steering.policy=\"least_connections\"`: Use
                          weight to scale the origin's open connections."
                  description: "Body param: The list of origins within this pool. Traffic directed
                    at this pool is balanced across all currently healthy
                    origins, provided the pool itself is healthy."
                check_regions:
                  oneOf:
                    - type: array
                      items:
                        type: string
                        enum:
                          - WNAM
                          - ENAM
                          - WEU
                          - EEU
                          - NSAM
                          - SSAM
                          - OC
                          - ME
                          - NAF
                          - SAF
                          - SAS
                          - SEAS
                          - NEAS
                          - ALL_REGIONS
                  nullable: true
                  description: "Body param: A list of regions from which to run health checks.
                    Null means every Cloudflare data center."
                description:
                  type: string
                  description: "Body param: A human-readable description of the pool."
                enabled:
                  type: boolean
                  description: "Body param: Whether to enable (the default) or disable this pool.
                    Disabled pools will not receive traffic and are excluded
                    from health checks. Disabling a pool will cause any load
                    balancers using it to failover to the next pool (if any)."
                latitude:
                  type: number
                  description: "Body param: The latitude of the data center containing the origins
                    used in this pool in decimal degrees. If this is set,
                    longitude must also be set."
                load_shedding:
                  oneOf:
                    - type: object
                      properties:
                        default_percent:
                          type: number
                          description: The percent of traffic to shed from the pool, according to the
                            default policy. Applies to new sessions and traffic
                            without session affinity.
                        default_policy:
                          type: string
                          enum:
                            - random
                            - hash
                          description: The default policy to use when load shedding. A random policy
                            randomly sheds a given percent of requests. A hash
                            policy computes a hash over the CF-Connecting-IP
                            address and sheds all requests originating from a
                            percent of IPs.
                        session_percent:
                          type: number
                          description: The percent of existing sessions to shed from the pool, according
                            to the session policy.
                        session_policy:
                          type: string
                          enum:
                            - hash
                          description: Only the hash policy is supported for existing sessions (to avoid
                            exponential decay).
                  nullable: true
                  description: "Body param: Configures load shedding policies and percentages for
                    the pool."
                longitude:
                  type: number
                  description: "Body param: The longitude of the data center containing the
                    origins used in this pool in decimal degrees. If this is
                    set, latitude must also be set."
                minimum_origins:
                  type: number
                  description: "Body param: The minimum number of origins that must be healthy for
                    this pool to serve traffic. If the number of healthy origins
                    falls below this number, the pool will be marked unhealthy
                    and will failover to the next available pool."
                monitor:
                  type: string
                  description: "Body param: The ID of the Monitor to use for checking the health
                    of origins within this pool."
                monitor_group:
                  type: string
                  description: "Body param: The ID of the Monitor Group to use for checking the
                    health of origins within this pool."
                notification_email:
                  type: string
                  description: "Body param: This field is now deprecated. It has been moved to
                    Cloudflare's Centralized Notification service
                    https://developers.cloudflare.com/fundamentals/notification\
                    s/. The email address to send health status notifications
                    to. This can be an individual mailbox or a mailing list.
                    Multiple emails can be supplied as a comma delimited list."
                notification_filter:
                  oneOf:
                    - type: object
                      properties:
                        origin:
                          oneOf:
                            - type: object
                              properties:
                                disable:
                                  type: boolean
                                  description: If set true, disable notifications for this type of resource (pool
                                    or origin).
                                healthy:
                                  oneOf:
                                    - type: boolean
                                  nullable: true
                                  description: If present, send notifications only for this health status (e.g.
                                    false for only DOWN events). Use null to
                                    reset (all events).
                          nullable: true
                          description: Filter options for a particular resource type (pool or origin). Use
                            null to reset.
                        pool:
                          oneOf:
                            - type: object
                              properties:
                                disable:
                                  type: boolean
                                  description: If set true, disable notifications for this type of resource (pool
                                    or origin).
                                healthy:
                                  oneOf:
                                    - type: boolean
                                  nullable: true
                                  description: If present, send notifications only for this health status (e.g.
                                    false for only DOWN events). Use null to
                                    reset (all events).
                          nullable: true
                          description: Filter options for a particular resource type (pool or origin). Use
                            null to reset.
                  nullable: true
                  description: "Body param: Filter pool and origin health notifications by
                    resource type or health status. Use null to reset."
                origin_steering:
                  oneOf:
                    - type: object
                      properties:
                        policy:
                          type: string
                          enum:
                            - random
                            - hash
                            - least_outstanding_requests
                            - least_connections
                          description: "The type of origin steering policy to use.  - `\"random\"`: Select
                            an origin randomly. - `\"hash\"`: Select an origin
                            by computing a hash over the CF-Connecting-IP
                            address. - `\"least_outstanding_requests\"`: Select
                            an origin by taking into consideration origin
                            weights, as well as each origin's number of
                            outstanding requests. Origins with more pending
                            requests are weighted proportionately less relative
                            to others. - `\"least_connections\"`: Select an
                            origin by taking into consideration origin weights,
                            as well as each origin's number of open connections.
                            Origins with more open connections are weighted
                            proportionately less relative to others. Supported
                            for HTTP/1 and HTTP/2 connections."
                  nullable: true
                  description: "Body param: Configures origin steering for the pool. Controls how
                    origins are selected for new sessions and traffic without
                    session affinity."
              required:
                - name
                - origins
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  check_regions:
                    oneOf:
                      - type: array
                        items:
                          type: string
                          enum:
                            - WNAM
                            - ENAM
                            - WEU
                            - EEU
                            - NSAM
                            - SSAM
                            - OC
                            - ME
                            - NAF
                            - SAF
                            - SAS
                            - SEAS
                            - NEAS
                            - ALL_REGIONS
                    nullable: true
                    description: A list of regions from which to run health checks. Null means every
                      Cloudflare data center.
                  created_on:
                    type: string
                  description:
                    type: string
                    description: A human-readable description of the pool.
                  disabled_at:
                    type: string
                    description: This field shows up only if the pool is disabled. This field is set
                      with the time the pool was disabled at.
                  enabled:
                    type: boolean
                    description: Whether to enable (the default) or disable this pool. Disabled
                      pools will not receive traffic and are excluded from
                      health checks. Disabling a pool will cause any load
                      balancers using it to failover to the next pool (if any).
                  latitude:
                    type: number
                    description: The latitude of the data center containing the origins used in this
                      pool in decimal degrees. If this is set, longitude must
                      also be set.
                  load_shedding:
                    oneOf:
                      - type: object
                        properties:
                          default_percent:
                            type: number
                            description: The percent of traffic to shed from the pool, according to the
                              default policy. Applies to new sessions and
                              traffic without session affinity.
                          default_policy:
                            type: string
                            enum:
                              - random
                              - hash
                            description: The default policy to use when load shedding. A random policy
                              randomly sheds a given percent of requests. A hash
                              policy computes a hash over the CF-Connecting-IP
                              address and sheds all requests originating from a
                              percent of IPs.
                          session_percent:
                            type: number
                            description: The percent of existing sessions to shed from the pool, according
                              to the session policy.
                          session_policy:
                            type: string
                            enum:
                              - hash
                            description: Only the hash policy is supported for existing sessions (to avoid
                              exponential decay).
                    nullable: true
                    description: Configures load shedding policies and percentages for the pool.
                  longitude:
                    type: number
                    description: The longitude of the data center containing the origins used in
                      this pool in decimal degrees. If this is set, latitude
                      must also be set.
                  minimum_origins:
                    type: number
                    description: The minimum number of origins that must be healthy for this pool to
                      serve traffic. If the number of healthy origins falls
                      below this number, the pool will be marked unhealthy and
                      will failover to the next available pool.
                  modified_on:
                    type: string
                  monitor:
                    type: string
                    description: The ID of the Monitor to use for checking the health of origins
                      within this pool.
                  monitor_group:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  name:
                    type: string
                    description: A short name (tag) for the pool. Only alphanumeric characters,
                      hyphens, and underscores are allowed.
                  networks:
                    type: array
                    items:
                      type: string
                    description: List of networks where Load Balancer or Pool is enabled.
                  notification_email:
                    type: string
                    description: This field is now deprecated. It has been moved to Cloudflare's
                      Centralized Notification service
                      https://developers.cloudflare.com/fundamentals/notifications/.
                      The email address to send health status notifications to.
                      This can be an individual mailbox or a mailing list.
                      Multiple emails can be supplied as a comma delimited list.
                  notification_filter:
                    oneOf:
                      - type: object
                        properties:
                          origin:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                          pool:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                    nullable: true
                    description: Filter pool and origin health notifications by resource type or
                      health status. Use null to reset.
                  origin_steering:
                    oneOf:
                      - type: object
                        properties:
                          policy:
                            type: string
                            enum:
                              - random
                              - hash
                              - least_outstanding_requests
                              - least_connections
                            description: "The type of origin steering policy to use.  - `\"random\"`: Select
                              an origin randomly. - `\"hash\"`: Select an origin
                              by computing a hash over the CF-Connecting-IP
                              address. - `\"least_outstanding_requests\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              outstanding requests. Origins with more pending
                              requests are weighted proportionately less
                              relative to others. - `\"least_connections\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              open connections. Origins with more open
                              connections are weighted proportionately less
                              relative to others. Supported for HTTP/1 and
                              HTTP/2 connections."
                    nullable: true
                    description: Configures origin steering for the pool. Controls how origins are
                      selected for new sessions and traffic without session
                      affinity.
                  origins:
                    type: array
                    items:
                      type: object
                      properties:
                        address:
                          type: string
                          description: The IP address (IPv4 or IPv6) of the origin, or its publicly
                            addressable hostname. Hostnames entered here should
                            resolve directly to the origin, and not be a
                            hostname proxied by Cloudflare. To set an
                            internal/reserved address, virtual_network_id must
                            also be set.
                        disabled_at:
                          type: string
                          description: This field shows up only if the origin is disabled. This field is
                            set with the time the origin was disabled.
                        enabled:
                          type: boolean
                          description: Whether to enable (the default) this origin within the pool.
                            Disabled origins will not receive traffic and are
                            excluded from health checks. The origin will only be
                            disabled for the current pool.
                        header:
                          type: object
                          properties:
                            Host:
                              type: array
                              items:
                                type: string
                              description: The 'Host' header allows to override the hostname set in the HTTP
                                request. Current support is 1 'Host' header
                                override per origin.
                          description: The request header is used to pass additional information with an
                            HTTP request. Currently supported header is 'Host'.
                        name:
                          type: string
                          description: A human-identifiable name for the origin.
                        port:
                          type: number
                          description: The port for upstream connections. A value of 0 means the default
                            port for the protocol will be used.
                        virtual_network_id:
                          type: string
                          description: The virtual network subnet ID the origin belongs in. Virtual
                            network must also belong to the account.
                        weight:
                          type: number
                          description: "The weight of this origin relative to other origins in the pool.
                            Based on the configured weight the total traffic is
                            distributed among origins within the pool.  -
                            `origin_steering.policy=\"least_outstanding_requests\
                            \"`: Use weight to scale the origin's outstanding
                            requests. -
                            `origin_steering.policy=\"least_connections\"`: Use
                            weight to scale the origin's open connections."
                    description: The list of origins within this pool. Traffic directed at this pool
                      is balanced across all currently healthy origins, provided
                      the pool itself is healthy.
      x-distilled-response-path: result
    patch:
      operationId: patchPool
      description: "Apply changes to an existing pool, overwriting the supplied
        properties.  @example ```ts const pool = await
        client.loadBalancers.pools.edit( '17b5962d775c646f3f9725cbc7a53df4', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: poolId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                check_regions:
                  oneOf:
                    - type: array
                      items:
                        type: string
                        enum:
                          - WNAM
                          - ENAM
                          - WEU
                          - EEU
                          - NSAM
                          - SSAM
                          - OC
                          - ME
                          - NAF
                          - SAF
                          - SAS
                          - SEAS
                          - NEAS
                          - ALL_REGIONS
                  nullable: true
                  description: "Body param: A list of regions from which to run health checks.
                    Null means every Cloudflare data center."
                description:
                  type: string
                  description: "Body param: A human-readable description of the pool."
                enabled:
                  type: boolean
                  description: "Body param: Whether to enable (the default) or disable this pool.
                    Disabled pools will not receive traffic and are excluded
                    from health checks. Disabling a pool will cause any load
                    balancers using it to failover to the next pool (if any)."
                latitude:
                  type: number
                  description: "Body param: The latitude of the data center containing the origins
                    used in this pool in decimal degrees. If this is set,
                    longitude must also be set."
                load_shedding:
                  oneOf:
                    - type: object
                      properties:
                        default_percent:
                          type: number
                          description: The percent of traffic to shed from the pool, according to the
                            default policy. Applies to new sessions and traffic
                            without session affinity.
                        default_policy:
                          type: string
                          enum:
                            - random
                            - hash
                          description: The default policy to use when load shedding. A random policy
                            randomly sheds a given percent of requests. A hash
                            policy computes a hash over the CF-Connecting-IP
                            address and sheds all requests originating from a
                            percent of IPs.
                        session_percent:
                          type: number
                          description: The percent of existing sessions to shed from the pool, according
                            to the session policy.
                        session_policy:
                          type: string
                          enum:
                            - hash
                          description: Only the hash policy is supported for existing sessions (to avoid
                            exponential decay).
                  nullable: true
                  description: "Body param: Configures load shedding policies and percentages for
                    the pool."
                longitude:
                  type: number
                  description: "Body param: The longitude of the data center containing the
                    origins used in this pool in decimal degrees. If this is
                    set, latitude must also be set."
                minimum_origins:
                  type: number
                  description: "Body param: The minimum number of origins that must be healthy for
                    this pool to serve traffic. If the number of healthy origins
                    falls below this number, the pool will be marked unhealthy
                    and will failover to the next available pool."
                monitor:
                  type: string
                  description: "Body param: The ID of the Monitor to use for checking the health
                    of origins within this pool."
                monitor_group:
                  type: string
                  description: "Body param: The ID of the Monitor Group to use for checking the
                    health of origins within this pool."
                name:
                  type: string
                  description: "Body param: A short name (tag) for the pool. Only alphanumeric
                    characters, hyphens, and underscores are allowed."
                notification_email:
                  type: string
                  description: "Body param: This field is now deprecated. It has been moved to
                    Cloudflare's Centralized Notification service
                    https://developers.cloudflare.com/fundamentals/notification\
                    s/. The email address to send health status notifications
                    to. This can be an individual mailbox or a mailing list.
                    Multiple emails can be supplied as a comma delimited list."
                notification_filter:
                  oneOf:
                    - type: object
                      properties:
                        origin:
                          oneOf:
                            - type: object
                              properties:
                                disable:
                                  type: boolean
                                  description: If set true, disable notifications for this type of resource (pool
                                    or origin).
                                healthy:
                                  oneOf:
                                    - type: boolean
                                  nullable: true
                                  description: If present, send notifications only for this health status (e.g.
                                    false for only DOWN events). Use null to
                                    reset (all events).
                          nullable: true
                          description: Filter options for a particular resource type (pool or origin). Use
                            null to reset.
                        pool:
                          oneOf:
                            - type: object
                              properties:
                                disable:
                                  type: boolean
                                  description: If set true, disable notifications for this type of resource (pool
                                    or origin).
                                healthy:
                                  oneOf:
                                    - type: boolean
                                  nullable: true
                                  description: If present, send notifications only for this health status (e.g.
                                    false for only DOWN events). Use null to
                                    reset (all events).
                          nullable: true
                          description: Filter options for a particular resource type (pool or origin). Use
                            null to reset.
                  nullable: true
                  description: "Body param: Filter pool and origin health notifications by
                    resource type or health status. Use null to reset."
                origin_steering:
                  oneOf:
                    - type: object
                      properties:
                        policy:
                          type: string
                          enum:
                            - random
                            - hash
                            - least_outstanding_requests
                            - least_connections
                          description: "The type of origin steering policy to use.  - `\"random\"`: Select
                            an origin randomly. - `\"hash\"`: Select an origin
                            by computing a hash over the CF-Connecting-IP
                            address. - `\"least_outstanding_requests\"`: Select
                            an origin by taking into consideration origin
                            weights, as well as each origin's number of
                            outstanding requests. Origins with more pending
                            requests are weighted proportionately less relative
                            to others. - `\"least_connections\"`: Select an
                            origin by taking into consideration origin weights,
                            as well as each origin's number of open connections.
                            Origins with more open connections are weighted
                            proportionately less relative to others. Supported
                            for HTTP/1 and HTTP/2 connections."
                  nullable: true
                  description: "Body param: Configures origin steering for the pool. Controls how
                    origins are selected for new sessions and traffic without
                    session affinity."
                origins:
                  type: array
                  items:
                    type: object
                    properties:
                      address:
                        type: string
                        description: The IP address (IPv4 or IPv6) of the origin, or its publicly
                          addressable hostname. Hostnames entered here should
                          resolve directly to the origin, and not be a hostname
                          proxied by Cloudflare. To set an internal/reserved
                          address, virtual_network_id must also be set.
                      enabled:
                        type: boolean
                        description: Whether to enable (the default) this origin within the pool.
                          Disabled origins will not receive traffic and are
                          excluded from health checks. The origin will only be
                          disabled for the current pool.
                      header:
                        type: object
                        properties:
                          Host:
                            type: array
                            items:
                              type: string
                            description: The 'Host' header allows to override the hostname set in the HTTP
                              request. Current support is 1 'Host' header
                              override per origin.
                        description: The request header is used to pass additional information with an
                          HTTP request. Currently supported header is 'Host'.
                      name:
                        type: string
                        description: A human-identifiable name for the origin.
                      port:
                        type: number
                        description: The port for upstream connections. A value of 0 means the default
                          port for the protocol will be used.
                      virtual_network_id:
                        type: string
                        description: The virtual network subnet ID the origin belongs in. Virtual
                          network must also belong to the account.
                      weight:
                        type: number
                        description: "The weight of this origin relative to other origins in the pool.
                          Based on the configured weight the total traffic is
                          distributed among origins within the pool.  -
                          `origin_steering.policy=\"least_outstanding_requests\
                          \"`: Use weight to scale the origin's outstanding
                          requests. -
                          `origin_steering.policy=\"least_connections\"`: Use
                          weight to scale the origin's open connections."
                  description: "Body param: The list of origins within this pool. Traffic directed
                    at this pool is balanced across all currently healthy
                    origins, provided the pool itself is healthy."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  check_regions:
                    oneOf:
                      - type: array
                        items:
                          type: string
                          enum:
                            - WNAM
                            - ENAM
                            - WEU
                            - EEU
                            - NSAM
                            - SSAM
                            - OC
                            - ME
                            - NAF
                            - SAF
                            - SAS
                            - SEAS
                            - NEAS
                            - ALL_REGIONS
                    nullable: true
                    description: A list of regions from which to run health checks. Null means every
                      Cloudflare data center.
                  created_on:
                    type: string
                  description:
                    type: string
                    description: A human-readable description of the pool.
                  disabled_at:
                    type: string
                    description: This field shows up only if the pool is disabled. This field is set
                      with the time the pool was disabled at.
                  enabled:
                    type: boolean
                    description: Whether to enable (the default) or disable this pool. Disabled
                      pools will not receive traffic and are excluded from
                      health checks. Disabling a pool will cause any load
                      balancers using it to failover to the next pool (if any).
                  latitude:
                    type: number
                    description: The latitude of the data center containing the origins used in this
                      pool in decimal degrees. If this is set, longitude must
                      also be set.
                  load_shedding:
                    oneOf:
                      - type: object
                        properties:
                          default_percent:
                            type: number
                            description: The percent of traffic to shed from the pool, according to the
                              default policy. Applies to new sessions and
                              traffic without session affinity.
                          default_policy:
                            type: string
                            enum:
                              - random
                              - hash
                            description: The default policy to use when load shedding. A random policy
                              randomly sheds a given percent of requests. A hash
                              policy computes a hash over the CF-Connecting-IP
                              address and sheds all requests originating from a
                              percent of IPs.
                          session_percent:
                            type: number
                            description: The percent of existing sessions to shed from the pool, according
                              to the session policy.
                          session_policy:
                            type: string
                            enum:
                              - hash
                            description: Only the hash policy is supported for existing sessions (to avoid
                              exponential decay).
                    nullable: true
                    description: Configures load shedding policies and percentages for the pool.
                  longitude:
                    type: number
                    description: The longitude of the data center containing the origins used in
                      this pool in decimal degrees. If this is set, latitude
                      must also be set.
                  minimum_origins:
                    type: number
                    description: The minimum number of origins that must be healthy for this pool to
                      serve traffic. If the number of healthy origins falls
                      below this number, the pool will be marked unhealthy and
                      will failover to the next available pool.
                  modified_on:
                    type: string
                  monitor:
                    type: string
                    description: The ID of the Monitor to use for checking the health of origins
                      within this pool.
                  monitor_group:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  name:
                    type: string
                    description: A short name (tag) for the pool. Only alphanumeric characters,
                      hyphens, and underscores are allowed.
                  networks:
                    type: array
                    items:
                      type: string
                    description: List of networks where Load Balancer or Pool is enabled.
                  notification_email:
                    type: string
                    description: This field is now deprecated. It has been moved to Cloudflare's
                      Centralized Notification service
                      https://developers.cloudflare.com/fundamentals/notifications/.
                      The email address to send health status notifications to.
                      This can be an individual mailbox or a mailing list.
                      Multiple emails can be supplied as a comma delimited list.
                  notification_filter:
                    oneOf:
                      - type: object
                        properties:
                          origin:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                          pool:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                    nullable: true
                    description: Filter pool and origin health notifications by resource type or
                      health status. Use null to reset.
                  origin_steering:
                    oneOf:
                      - type: object
                        properties:
                          policy:
                            type: string
                            enum:
                              - random
                              - hash
                              - least_outstanding_requests
                              - least_connections
                            description: "The type of origin steering policy to use.  - `\"random\"`: Select
                              an origin randomly. - `\"hash\"`: Select an origin
                              by computing a hash over the CF-Connecting-IP
                              address. - `\"least_outstanding_requests\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              outstanding requests. Origins with more pending
                              requests are weighted proportionately less
                              relative to others. - `\"least_connections\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              open connections. Origins with more open
                              connections are weighted proportionately less
                              relative to others. Supported for HTTP/1 and
                              HTTP/2 connections."
                    nullable: true
                    description: Configures origin steering for the pool. Controls how origins are
                      selected for new sessions and traffic without session
                      affinity.
                  origins:
                    type: array
                    items:
                      type: object
                      properties:
                        address:
                          type: string
                          description: The IP address (IPv4 or IPv6) of the origin, or its publicly
                            addressable hostname. Hostnames entered here should
                            resolve directly to the origin, and not be a
                            hostname proxied by Cloudflare. To set an
                            internal/reserved address, virtual_network_id must
                            also be set.
                        disabled_at:
                          type: string
                          description: This field shows up only if the origin is disabled. This field is
                            set with the time the origin was disabled.
                        enabled:
                          type: boolean
                          description: Whether to enable (the default) this origin within the pool.
                            Disabled origins will not receive traffic and are
                            excluded from health checks. The origin will only be
                            disabled for the current pool.
                        header:
                          type: object
                          properties:
                            Host:
                              type: array
                              items:
                                type: string
                              description: The 'Host' header allows to override the hostname set in the HTTP
                                request. Current support is 1 'Host' header
                                override per origin.
                          description: The request header is used to pass additional information with an
                            HTTP request. Currently supported header is 'Host'.
                        name:
                          type: string
                          description: A human-identifiable name for the origin.
                        port:
                          type: number
                          description: The port for upstream connections. A value of 0 means the default
                            port for the protocol will be used.
                        virtual_network_id:
                          type: string
                          description: The virtual network subnet ID the origin belongs in. Virtual
                            network must also belong to the account.
                        weight:
                          type: number
                          description: "The weight of this origin relative to other origins in the pool.
                            Based on the configured weight the total traffic is
                            distributed among origins within the pool.  -
                            `origin_steering.policy=\"least_outstanding_requests\
                            \"`: Use weight to scale the origin's outstanding
                            requests. -
                            `origin_steering.policy=\"least_connections\"`: Use
                            weight to scale the origin's open connections."
                    description: The list of origins within this pool. Traffic directed at this pool
                      is balanced across all currently healthy origins, provided
                      the pool itself is healthy.
      x-distilled-response-path: result
    delete:
      operationId: deletePool
      description: "Delete a configured pool.  @example ```ts const pool = await
        client.loadBalancers.pools.delete( '17b5962d775c646f3f9725cbc7a53df4', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: poolId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/pools:
    get:
      operationId: listPools
      description: "List configured pools.  @example ```ts // Automatically fetches
        more pages as needed. for await (const pool of
        client.loadBalancers.pools.list({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', })) { // ... } ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
        - name: monitor
          in: query
          required: false
          description: "Query param: The ID of the Monitor to use for checking the health
            of origins within this pool."
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  check_regions:
                    oneOf:
                      - type: array
                        items:
                          type: string
                          enum:
                            - WNAM
                            - ENAM
                            - WEU
                            - EEU
                            - NSAM
                            - SSAM
                            - OC
                            - ME
                            - NAF
                            - SAF
                            - SAS
                            - SEAS
                            - NEAS
                            - ALL_REGIONS
                    nullable: true
                    description: A list of regions from which to run health checks. Null means every
                      Cloudflare data center.
                  created_on:
                    type: string
                  description:
                    type: string
                    description: A human-readable description of the pool.
                  disabled_at:
                    type: string
                    description: This field shows up only if the pool is disabled. This field is set
                      with the time the pool was disabled at.
                  enabled:
                    type: boolean
                    description: Whether to enable (the default) or disable this pool. Disabled
                      pools will not receive traffic and are excluded from
                      health checks. Disabling a pool will cause any load
                      balancers using it to failover to the next pool (if any).
                  latitude:
                    type: number
                    description: The latitude of the data center containing the origins used in this
                      pool in decimal degrees. If this is set, longitude must
                      also be set.
                  load_shedding:
                    oneOf:
                      - type: object
                        properties:
                          default_percent:
                            type: number
                            description: The percent of traffic to shed from the pool, according to the
                              default policy. Applies to new sessions and
                              traffic without session affinity.
                          default_policy:
                            type: string
                            enum:
                              - random
                              - hash
                            description: The default policy to use when load shedding. A random policy
                              randomly sheds a given percent of requests. A hash
                              policy computes a hash over the CF-Connecting-IP
                              address and sheds all requests originating from a
                              percent of IPs.
                          session_percent:
                            type: number
                            description: The percent of existing sessions to shed from the pool, according
                              to the session policy.
                          session_policy:
                            type: string
                            enum:
                              - hash
                            description: Only the hash policy is supported for existing sessions (to avoid
                              exponential decay).
                    nullable: true
                    description: Configures load shedding policies and percentages for the pool.
                  longitude:
                    type: number
                    description: The longitude of the data center containing the origins used in
                      this pool in decimal degrees. If this is set, latitude
                      must also be set.
                  minimum_origins:
                    type: number
                    description: The minimum number of origins that must be healthy for this pool to
                      serve traffic. If the number of healthy origins falls
                      below this number, the pool will be marked unhealthy and
                      will failover to the next available pool.
                  modified_on:
                    type: string
                  monitor:
                    type: string
                    description: The ID of the Monitor to use for checking the health of origins
                      within this pool.
                  monitor_group:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  name:
                    type: string
                    description: A short name (tag) for the pool. Only alphanumeric characters,
                      hyphens, and underscores are allowed.
                  networks:
                    type: array
                    items:
                      type: string
                    description: List of networks where Load Balancer or Pool is enabled.
                  notification_email:
                    type: string
                    description: This field is now deprecated. It has been moved to Cloudflare's
                      Centralized Notification service
                      https://developers.cloudflare.com/fundamentals/notifications/.
                      The email address to send health status notifications to.
                      This can be an individual mailbox or a mailing list.
                      Multiple emails can be supplied as a comma delimited list.
                  notification_filter:
                    oneOf:
                      - type: object
                        properties:
                          origin:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                          pool:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                    nullable: true
                    description: Filter pool and origin health notifications by resource type or
                      health status. Use null to reset.
                  origin_steering:
                    oneOf:
                      - type: object
                        properties:
                          policy:
                            type: string
                            enum:
                              - random
                              - hash
                              - least_outstanding_requests
                              - least_connections
                            description: "The type of origin steering policy to use.  - `\"random\"`: Select
                              an origin randomly. - `\"hash\"`: Select an origin
                              by computing a hash over the CF-Connecting-IP
                              address. - `\"least_outstanding_requests\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              outstanding requests. Origins with more pending
                              requests are weighted proportionately less
                              relative to others. - `\"least_connections\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              open connections. Origins with more open
                              connections are weighted proportionately less
                              relative to others. Supported for HTTP/1 and
                              HTTP/2 connections."
                    nullable: true
                    description: Configures origin steering for the pool. Controls how origins are
                      selected for new sessions and traffic without session
                      affinity.
                  origins:
                    type: array
                    items:
                      type: object
                      properties:
                        address:
                          type: string
                          description: The IP address (IPv4 or IPv6) of the origin, or its publicly
                            addressable hostname. Hostnames entered here should
                            resolve directly to the origin, and not be a
                            hostname proxied by Cloudflare. To set an
                            internal/reserved address, virtual_network_id must
                            also be set.
                        disabled_at:
                          type: string
                          description: This field shows up only if the origin is disabled. This field is
                            set with the time the origin was disabled.
                        enabled:
                          type: boolean
                          description: Whether to enable (the default) this origin within the pool.
                            Disabled origins will not receive traffic and are
                            excluded from health checks. The origin will only be
                            disabled for the current pool.
                        header:
                          type: object
                          properties:
                            Host:
                              type: array
                              items:
                                type: string
                              description: The 'Host' header allows to override the hostname set in the HTTP
                                request. Current support is 1 'Host' header
                                override per origin.
                          description: The request header is used to pass additional information with an
                            HTTP request. Currently supported header is 'Host'.
                        name:
                          type: string
                          description: A human-identifiable name for the origin.
                        port:
                          type: number
                          description: The port for upstream connections. A value of 0 means the default
                            port for the protocol will be used.
                        virtual_network_id:
                          type: string
                          description: The virtual network subnet ID the origin belongs in. Virtual
                            network must also belong to the account.
                        weight:
                          type: number
                          description: "The weight of this origin relative to other origins in the pool.
                            Based on the configured weight the total traffic is
                            distributed among origins within the pool.  -
                            `origin_steering.policy=\"least_outstanding_requests\
                            \"`: Use weight to scale the origin's outstanding
                            requests. -
                            `origin_steering.policy=\"least_connections\"`: Use
                            weight to scale the origin's open connections."
                    description: The list of origins within this pool. Traffic directed at this pool
                      is balanced across all currently healthy origins, provided
                      the pool itself is healthy.
      x-distilled-pagination-class: PoolsSinglePage
    post:
      operationId: createPool
      description: "Create a new pool.  @example ```ts const pool = await
        client.loadBalancers.pools.create({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', name: 'primary-dc-1', origins: [{}],
        }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: "Body param: A short name (tag) for the pool. Only alphanumeric
                    characters, hyphens, and underscores are allowed."
                origins:
                  type: array
                  items:
                    type: object
                    properties:
                      address:
                        type: string
                        description: The IP address (IPv4 or IPv6) of the origin, or its publicly
                          addressable hostname. Hostnames entered here should
                          resolve directly to the origin, and not be a hostname
                          proxied by Cloudflare. To set an internal/reserved
                          address, virtual_network_id must also be set.
                      enabled:
                        type: boolean
                        description: Whether to enable (the default) this origin within the pool.
                          Disabled origins will not receive traffic and are
                          excluded from health checks. The origin will only be
                          disabled for the current pool.
                      header:
                        type: object
                        properties:
                          Host:
                            type: array
                            items:
                              type: string
                            description: The 'Host' header allows to override the hostname set in the HTTP
                              request. Current support is 1 'Host' header
                              override per origin.
                        description: The request header is used to pass additional information with an
                          HTTP request. Currently supported header is 'Host'.
                      name:
                        type: string
                        description: A human-identifiable name for the origin.
                      port:
                        type: number
                        description: The port for upstream connections. A value of 0 means the default
                          port for the protocol will be used.
                      virtual_network_id:
                        type: string
                        description: The virtual network subnet ID the origin belongs in. Virtual
                          network must also belong to the account.
                      weight:
                        type: number
                        description: "The weight of this origin relative to other origins in the pool.
                          Based on the configured weight the total traffic is
                          distributed among origins within the pool.  -
                          `origin_steering.policy=\"least_outstanding_requests\
                          \"`: Use weight to scale the origin's outstanding
                          requests. -
                          `origin_steering.policy=\"least_connections\"`: Use
                          weight to scale the origin's open connections."
                  description: "Body param: The list of origins within this pool. Traffic directed
                    at this pool is balanced across all currently healthy
                    origins, provided the pool itself is healthy."
                description:
                  type: string
                  description: "Body param: A human-readable description of the pool."
                enabled:
                  type: boolean
                  description: "Body param: Whether to enable (the default) or disable this pool.
                    Disabled pools will not receive traffic and are excluded
                    from health checks. Disabling a pool will cause any load
                    balancers using it to failover to the next pool (if any)."
                latitude:
                  type: number
                  description: "Body param: The latitude of the data center containing the origins
                    used in this pool in decimal degrees. If this is set,
                    longitude must also be set."
                load_shedding:
                  oneOf:
                    - type: object
                      properties:
                        default_percent:
                          type: number
                          description: The percent of traffic to shed from the pool, according to the
                            default policy. Applies to new sessions and traffic
                            without session affinity.
                        default_policy:
                          type: string
                          enum:
                            - random
                            - hash
                          description: The default policy to use when load shedding. A random policy
                            randomly sheds a given percent of requests. A hash
                            policy computes a hash over the CF-Connecting-IP
                            address and sheds all requests originating from a
                            percent of IPs.
                        session_percent:
                          type: number
                          description: The percent of existing sessions to shed from the pool, according
                            to the session policy.
                        session_policy:
                          type: string
                          enum:
                            - hash
                          description: Only the hash policy is supported for existing sessions (to avoid
                            exponential decay).
                  nullable: true
                  description: "Body param: Configures load shedding policies and percentages for
                    the pool."
                longitude:
                  type: number
                  description: "Body param: The longitude of the data center containing the
                    origins used in this pool in decimal degrees. If this is
                    set, latitude must also be set."
                minimum_origins:
                  type: number
                  description: "Body param: The minimum number of origins that must be healthy for
                    this pool to serve traffic. If the number of healthy origins
                    falls below this number, the pool will be marked unhealthy
                    and will failover to the next available pool."
                monitor:
                  type: string
                  description: "Body param: The ID of the Monitor to use for checking the health
                    of origins within this pool."
                monitor_group:
                  type: string
                  description: "Body param: The ID of the Monitor Group to use for checking the
                    health of origins within this pool."
                notification_email:
                  type: string
                  description: "Body param: This field is now deprecated. It has been moved to
                    Cloudflare's Centralized Notification service
                    https://developers.cloudflare.com/fundamentals/notification\
                    s/. The email address to send health status notifications
                    to. This can be an individual mailbox or a mailing list.
                    Multiple emails can be supplied as a comma delimited list."
                notification_filter:
                  oneOf:
                    - type: object
                      properties:
                        origin:
                          oneOf:
                            - type: object
                              properties:
                                disable:
                                  type: boolean
                                  description: If set true, disable notifications for this type of resource (pool
                                    or origin).
                                healthy:
                                  oneOf:
                                    - type: boolean
                                  nullable: true
                                  description: If present, send notifications only for this health status (e.g.
                                    false for only DOWN events). Use null to
                                    reset (all events).
                          nullable: true
                          description: Filter options for a particular resource type (pool or origin). Use
                            null to reset.
                        pool:
                          oneOf:
                            - type: object
                              properties:
                                disable:
                                  type: boolean
                                  description: If set true, disable notifications for this type of resource (pool
                                    or origin).
                                healthy:
                                  oneOf:
                                    - type: boolean
                                  nullable: true
                                  description: If present, send notifications only for this health status (e.g.
                                    false for only DOWN events). Use null to
                                    reset (all events).
                          nullable: true
                          description: Filter options for a particular resource type (pool or origin). Use
                            null to reset.
                  nullable: true
                  description: "Body param: Filter pool and origin health notifications by
                    resource type or health status. Use null to reset."
                origin_steering:
                  oneOf:
                    - type: object
                      properties:
                        policy:
                          type: string
                          enum:
                            - random
                            - hash
                            - least_outstanding_requests
                            - least_connections
                          description: "The type of origin steering policy to use.  - `\"random\"`: Select
                            an origin randomly. - `\"hash\"`: Select an origin
                            by computing a hash over the CF-Connecting-IP
                            address. - `\"least_outstanding_requests\"`: Select
                            an origin by taking into consideration origin
                            weights, as well as each origin's number of
                            outstanding requests. Origins with more pending
                            requests are weighted proportionately less relative
                            to others. - `\"least_connections\"`: Select an
                            origin by taking into consideration origin weights,
                            as well as each origin's number of open connections.
                            Origins with more open connections are weighted
                            proportionately less relative to others. Supported
                            for HTTP/1 and HTTP/2 connections."
                  nullable: true
                  description: "Body param: Configures origin steering for the pool. Controls how
                    origins are selected for new sessions and traffic without
                    session affinity."
              required:
                - name
                - origins
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  check_regions:
                    oneOf:
                      - type: array
                        items:
                          type: string
                          enum:
                            - WNAM
                            - ENAM
                            - WEU
                            - EEU
                            - NSAM
                            - SSAM
                            - OC
                            - ME
                            - NAF
                            - SAF
                            - SAS
                            - SEAS
                            - NEAS
                            - ALL_REGIONS
                    nullable: true
                    description: A list of regions from which to run health checks. Null means every
                      Cloudflare data center.
                  created_on:
                    type: string
                  description:
                    type: string
                    description: A human-readable description of the pool.
                  disabled_at:
                    type: string
                    description: This field shows up only if the pool is disabled. This field is set
                      with the time the pool was disabled at.
                  enabled:
                    type: boolean
                    description: Whether to enable (the default) or disable this pool. Disabled
                      pools will not receive traffic and are excluded from
                      health checks. Disabling a pool will cause any load
                      balancers using it to failover to the next pool (if any).
                  latitude:
                    type: number
                    description: The latitude of the data center containing the origins used in this
                      pool in decimal degrees. If this is set, longitude must
                      also be set.
                  load_shedding:
                    oneOf:
                      - type: object
                        properties:
                          default_percent:
                            type: number
                            description: The percent of traffic to shed from the pool, according to the
                              default policy. Applies to new sessions and
                              traffic without session affinity.
                          default_policy:
                            type: string
                            enum:
                              - random
                              - hash
                            description: The default policy to use when load shedding. A random policy
                              randomly sheds a given percent of requests. A hash
                              policy computes a hash over the CF-Connecting-IP
                              address and sheds all requests originating from a
                              percent of IPs.
                          session_percent:
                            type: number
                            description: The percent of existing sessions to shed from the pool, according
                              to the session policy.
                          session_policy:
                            type: string
                            enum:
                              - hash
                            description: Only the hash policy is supported for existing sessions (to avoid
                              exponential decay).
                    nullable: true
                    description: Configures load shedding policies and percentages for the pool.
                  longitude:
                    type: number
                    description: The longitude of the data center containing the origins used in
                      this pool in decimal degrees. If this is set, latitude
                      must also be set.
                  minimum_origins:
                    type: number
                    description: The minimum number of origins that must be healthy for this pool to
                      serve traffic. If the number of healthy origins falls
                      below this number, the pool will be marked unhealthy and
                      will failover to the next available pool.
                  modified_on:
                    type: string
                  monitor:
                    type: string
                    description: The ID of the Monitor to use for checking the health of origins
                      within this pool.
                  monitor_group:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  name:
                    type: string
                    description: A short name (tag) for the pool. Only alphanumeric characters,
                      hyphens, and underscores are allowed.
                  networks:
                    type: array
                    items:
                      type: string
                    description: List of networks where Load Balancer or Pool is enabled.
                  notification_email:
                    type: string
                    description: This field is now deprecated. It has been moved to Cloudflare's
                      Centralized Notification service
                      https://developers.cloudflare.com/fundamentals/notifications/.
                      The email address to send health status notifications to.
                      This can be an individual mailbox or a mailing list.
                      Multiple emails can be supplied as a comma delimited list.
                  notification_filter:
                    oneOf:
                      - type: object
                        properties:
                          origin:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                          pool:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                    nullable: true
                    description: Filter pool and origin health notifications by resource type or
                      health status. Use null to reset.
                  origin_steering:
                    oneOf:
                      - type: object
                        properties:
                          policy:
                            type: string
                            enum:
                              - random
                              - hash
                              - least_outstanding_requests
                              - least_connections
                            description: "The type of origin steering policy to use.  - `\"random\"`: Select
                              an origin randomly. - `\"hash\"`: Select an origin
                              by computing a hash over the CF-Connecting-IP
                              address. - `\"least_outstanding_requests\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              outstanding requests. Origins with more pending
                              requests are weighted proportionately less
                              relative to others. - `\"least_connections\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              open connections. Origins with more open
                              connections are weighted proportionately less
                              relative to others. Supported for HTTP/1 and
                              HTTP/2 connections."
                    nullable: true
                    description: Configures origin steering for the pool. Controls how origins are
                      selected for new sessions and traffic without session
                      affinity.
                  origins:
                    type: array
                    items:
                      type: object
                      properties:
                        address:
                          type: string
                          description: The IP address (IPv4 or IPv6) of the origin, or its publicly
                            addressable hostname. Hostnames entered here should
                            resolve directly to the origin, and not be a
                            hostname proxied by Cloudflare. To set an
                            internal/reserved address, virtual_network_id must
                            also be set.
                        disabled_at:
                          type: string
                          description: This field shows up only if the origin is disabled. This field is
                            set with the time the origin was disabled.
                        enabled:
                          type: boolean
                          description: Whether to enable (the default) this origin within the pool.
                            Disabled origins will not receive traffic and are
                            excluded from health checks. The origin will only be
                            disabled for the current pool.
                        header:
                          type: object
                          properties:
                            Host:
                              type: array
                              items:
                                type: string
                              description: The 'Host' header allows to override the hostname set in the HTTP
                                request. Current support is 1 'Host' header
                                override per origin.
                          description: The request header is used to pass additional information with an
                            HTTP request. Currently supported header is 'Host'.
                        name:
                          type: string
                          description: A human-identifiable name for the origin.
                        port:
                          type: number
                          description: The port for upstream connections. A value of 0 means the default
                            port for the protocol will be used.
                        virtual_network_id:
                          type: string
                          description: The virtual network subnet ID the origin belongs in. Virtual
                            network must also belong to the account.
                        weight:
                          type: number
                          description: "The weight of this origin relative to other origins in the pool.
                            Based on the configured weight the total traffic is
                            distributed among origins within the pool.  -
                            `origin_steering.policy=\"least_outstanding_requests\
                            \"`: Use weight to scale the origin's outstanding
                            requests. -
                            `origin_steering.policy=\"least_connections\"`: Use
                            weight to scale the origin's open connections."
                    description: The list of origins within this pool. Traffic directed at this pool
                      is balanced across all currently healthy origins, provided
                      the pool itself is healthy.
      x-distilled-response-path: result
    patch:
      operationId: bulkPatchPools
      description: "Apply changes to a number of existing pools, overwriting the
        supplied properties. Pools are ordered by ascending `name`. Returns the
        list of affected pools. Supports the standard pagination query
        parameters, either `limit`/`offset` or `per_page`/`page`.  @example
        ```ts // Automatically fetches more pages as needed. for await (const
        pool of client.loadBalancers.pools.bulkEdit( { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                notification_email:
                  type: string
                  enum:
                    - ""
                  description: 'Body param: The email address to send health status notifications
                    to. This field is now deprecated in favor of Cloudflare
                    Notifications for Load Balancing, so only resetting this
                    field with an empty string `""` is accepted.'
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  check_regions:
                    oneOf:
                      - type: array
                        items:
                          type: string
                          enum:
                            - WNAM
                            - ENAM
                            - WEU
                            - EEU
                            - NSAM
                            - SSAM
                            - OC
                            - ME
                            - NAF
                            - SAF
                            - SAS
                            - SEAS
                            - NEAS
                            - ALL_REGIONS
                    nullable: true
                    description: A list of regions from which to run health checks. Null means every
                      Cloudflare data center.
                  created_on:
                    type: string
                  description:
                    type: string
                    description: A human-readable description of the pool.
                  disabled_at:
                    type: string
                    description: This field shows up only if the pool is disabled. This field is set
                      with the time the pool was disabled at.
                  enabled:
                    type: boolean
                    description: Whether to enable (the default) or disable this pool. Disabled
                      pools will not receive traffic and are excluded from
                      health checks. Disabling a pool will cause any load
                      balancers using it to failover to the next pool (if any).
                  latitude:
                    type: number
                    description: The latitude of the data center containing the origins used in this
                      pool in decimal degrees. If this is set, longitude must
                      also be set.
                  load_shedding:
                    oneOf:
                      - type: object
                        properties:
                          default_percent:
                            type: number
                            description: The percent of traffic to shed from the pool, according to the
                              default policy. Applies to new sessions and
                              traffic without session affinity.
                          default_policy:
                            type: string
                            enum:
                              - random
                              - hash
                            description: The default policy to use when load shedding. A random policy
                              randomly sheds a given percent of requests. A hash
                              policy computes a hash over the CF-Connecting-IP
                              address and sheds all requests originating from a
                              percent of IPs.
                          session_percent:
                            type: number
                            description: The percent of existing sessions to shed from the pool, according
                              to the session policy.
                          session_policy:
                            type: string
                            enum:
                              - hash
                            description: Only the hash policy is supported for existing sessions (to avoid
                              exponential decay).
                    nullable: true
                    description: Configures load shedding policies and percentages for the pool.
                  longitude:
                    type: number
                    description: The longitude of the data center containing the origins used in
                      this pool in decimal degrees. If this is set, latitude
                      must also be set.
                  minimum_origins:
                    type: number
                    description: The minimum number of origins that must be healthy for this pool to
                      serve traffic. If the number of healthy origins falls
                      below this number, the pool will be marked unhealthy and
                      will failover to the next available pool.
                  modified_on:
                    type: string
                  monitor:
                    type: string
                    description: The ID of the Monitor to use for checking the health of origins
                      within this pool.
                  monitor_group:
                    type: string
                    description: The ID of the Monitor Group to use for checking the health of
                      origins within this pool.
                  name:
                    type: string
                    description: A short name (tag) for the pool. Only alphanumeric characters,
                      hyphens, and underscores are allowed.
                  networks:
                    type: array
                    items:
                      type: string
                    description: List of networks where Load Balancer or Pool is enabled.
                  notification_email:
                    type: string
                    description: This field is now deprecated. It has been moved to Cloudflare's
                      Centralized Notification service
                      https://developers.cloudflare.com/fundamentals/notifications/.
                      The email address to send health status notifications to.
                      This can be an individual mailbox or a mailing list.
                      Multiple emails can be supplied as a comma delimited list.
                  notification_filter:
                    oneOf:
                      - type: object
                        properties:
                          origin:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                          pool:
                            oneOf:
                              - type: object
                                properties:
                                  disable:
                                    type: boolean
                                    description: If set true, disable notifications for this type of resource (pool
                                      or origin).
                                  healthy:
                                    oneOf:
                                      - type: boolean
                                    nullable: true
                                    description: If present, send notifications only for this health status (e.g.
                                      false for only DOWN events). Use null to
                                      reset (all events).
                            nullable: true
                            description: Filter options for a particular resource type (pool or origin). Use
                              null to reset.
                    nullable: true
                    description: Filter pool and origin health notifications by resource type or
                      health status. Use null to reset.
                  origin_steering:
                    oneOf:
                      - type: object
                        properties:
                          policy:
                            type: string
                            enum:
                              - random
                              - hash
                              - least_outstanding_requests
                              - least_connections
                            description: "The type of origin steering policy to use.  - `\"random\"`: Select
                              an origin randomly. - `\"hash\"`: Select an origin
                              by computing a hash over the CF-Connecting-IP
                              address. - `\"least_outstanding_requests\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              outstanding requests. Origins with more pending
                              requests are weighted proportionately less
                              relative to others. - `\"least_connections\"`:
                              Select an origin by taking into consideration
                              origin weights, as well as each origin's number of
                              open connections. Origins with more open
                              connections are weighted proportionately less
                              relative to others. Supported for HTTP/1 and
                              HTTP/2 connections."
                    nullable: true
                    description: Configures origin steering for the pool. Controls how origins are
                      selected for new sessions and traffic without session
                      affinity.
                  origins:
                    type: array
                    items:
                      type: object
                      properties:
                        address:
                          type: string
                          description: The IP address (IPv4 or IPv6) of the origin, or its publicly
                            addressable hostname. Hostnames entered here should
                            resolve directly to the origin, and not be a
                            hostname proxied by Cloudflare. To set an
                            internal/reserved address, virtual_network_id must
                            also be set.
                        disabled_at:
                          type: string
                          description: This field shows up only if the origin is disabled. This field is
                            set with the time the origin was disabled.
                        enabled:
                          type: boolean
                          description: Whether to enable (the default) this origin within the pool.
                            Disabled origins will not receive traffic and are
                            excluded from health checks. The origin will only be
                            disabled for the current pool.
                        header:
                          type: object
                          properties:
                            Host:
                              type: array
                              items:
                                type: string
                              description: The 'Host' header allows to override the hostname set in the HTTP
                                request. Current support is 1 'Host' header
                                override per origin.
                          description: The request header is used to pass additional information with an
                            HTTP request. Currently supported header is 'Host'.
                        name:
                          type: string
                          description: A human-identifiable name for the origin.
                        port:
                          type: number
                          description: The port for upstream connections. A value of 0 means the default
                            port for the protocol will be used.
                        virtual_network_id:
                          type: string
                          description: The virtual network subnet ID the origin belongs in. Virtual
                            network must also belong to the account.
                        weight:
                          type: number
                          description: "The weight of this origin relative to other origins in the pool.
                            Based on the configured weight the total traffic is
                            distributed among origins within the pool.  -
                            `origin_steering.policy=\"least_outstanding_requests\
                            \"`: Use weight to scale the origin's outstanding
                            requests. -
                            `origin_steering.policy=\"least_connections\"`: Use
                            weight to scale the origin's open connections."
                    description: The list of origins within this pool. Traffic directed at this pool
                      is balanced across all currently healthy origins, provided
                      the pool itself is healthy.
      x-distilled-pagination-class: PoolsSinglePage
  /accounts/{account_id}/load_balancers/pools/{poolId}/health:
    get:
      operationId: getPoolHealth
      description: "Fetch the latest pool health status for a single pool.  @example
        ```ts const health = await client.loadBalancers.pools.health.get(
        '17b5962d775c646f3f9725cbc7a53df4', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: poolId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  pool_id:
                    type: string
                    description: Pool ID.
                  pop_health:
                    type: object
                    properties:
                      healthy:
                        type: boolean
                        description: Whether health check in region is healthy.
                      origins:
                        type: array
                        items:
                          type: object
                          properties:
                            ip:
                              type: object
                              properties:
                                failure_reason:
                                  type: string
                                  description: Failure reason.
                                healthy:
                                  type: boolean
                                  description: Origin health status.
                                response_code:
                                  type: number
                                  description: Response code from origin health check.
                                rtt:
                                  type: string
                                  description: Origin RTT (Round Trip Time) response.
                    description: List of regions and associated health status.
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/pools/{poolId}/preview:
    post:
      operationId: createPoolHealth
      description: "Preview pool health using provided monitor details. The returned
        preview_id can be used in the preview endpoint to retrieve the
        results.  @example ```ts const health = await
        client.loadBalancers.pools.health.create(
        '17b5962d775c646f3f9725cbc7a53df4', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: poolId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                allow_insecure:
                  type: boolean
                  description: "Body param: Do not validate the certificate when monitor use
                    HTTPS. This parameter is currently only valid for HTTP and
                    HTTPS monitors."
                consecutive_down:
                  type: number
                  description: "Body param: To be marked unhealthy the monitored origin must fail
                    this healthcheck N consecutive times."
                consecutive_up:
                  type: number
                  description: "Body param: To be marked healthy the monitored origin must pass
                    this healthcheck N consecutive times."
                description:
                  type: string
                  description: "Body param: Object description."
                expected_body:
                  type: string
                  description: "Body param: A case-insensitive sub-string to look for in the
                    response body. If this string is not found, the origin will
                    be marked as unhealthy. This parameter is only valid for
                    HTTP and HTTPS monitors."
                expected_codes:
                  type: string
                  description: "Body param: The expected HTTP response code or code range of the
                    health check. This parameter is only valid for HTTP and
                    HTTPS monitors."
                follow_redirects:
                  type: boolean
                  description: "Body param: Follow redirects if returned by the origin. This
                    parameter is only valid for HTTP and HTTPS monitors."
                header:
                  type: object
                  additionalProperties: true
                  description: "Body param: The HTTP request headers to send in the health check.
                    It is recommended you set a Host header by default. The
                    User-Agent header cannot be overridden. This parameter is
                    only valid for HTTP and HTTPS monitors."
                interval:
                  type: number
                  description: "Body param: The interval between each health check. Shorter
                    intervals may improve failover time, but will increase load
                    on the origins as we check from multiple locations."
                method:
                  type: string
                  description: "Body param: The method to use for the health check. This defaults
                    to 'GET' for HTTP/HTTPS based checks and
                    'connection_established' for TCP based health checks."
                path:
                  type: string
                  description: "Body param: The endpoint path you want to conduct a health check
                    against. This parameter is only valid for HTTP and HTTPS
                    monitors."
                port:
                  oneOf:
                    - type: number
                  nullable: true
                  description: "Body param: The port number to connect to for the health check.
                    Required for TCP, UDP, and SMTP checks. HTTP and HTTPS
                    checks should only define the port when using a non-standard
                    port (HTTP: default 80, HTTPS: default 443)."
                probe_zone:
                  type: string
                  description: "Body param: Assign this monitor to emulate the specified zone
                    while probing. This parameter is only valid for HTTP and
                    HTTPS monitors."
                retries:
                  type: number
                  description: "Body param: The number of retries to attempt in case of a timeout
                    before marking the origin as unhealthy. Retries are
                    attempted immediately."
                timeout:
                  type: number
                  description: "Body param: The timeout (in seconds) before marking the health
                    check as failed."
                type:
                  type: string
                  enum:
                    - http
                    - https
                    - tcp
                    - udp_icmp
                    - icmp_ping
                    - smtp
                  description: "Body param: The protocol to use for the health check. Currently
                    supported protocols are 'HTTP','HTTPS', 'TCP', 'ICMP-PING',
                    'UDP-ICMP', and 'SMTP'."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  pools:
                    type: object
                    additionalProperties: true
                    description: Monitored pool IDs mapped to their respective names.
                  preview_id:
                    type: string
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/pools/{poolId}/references:
    get:
      operationId: getPoolReference
      description: "Get the list of resources that reference the provided
        pool.  @example ```ts // Automatically fetches more pages as needed. for
        await (const referenceGetResponse of
        client.loadBalancers.pools.references.get(
        '17b5962d775c646f3f9725cbc7a53df4', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: poolId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  reference_type:
                    type: string
                    enum:
                      - "*"
                      - referral
                      - referrer
                  resource_id:
                    type: string
                  resource_name:
                    type: string
                  resource_type:
                    type: string
      x-distilled-pagination-class: ReferenceGetResponsesSinglePage
  /accounts/{account_id}/load_balancers/preview/{previewId}:
    get:
      operationId: getPreview
      description: "Get the result of a previous preview operation using the provided
        preview_id.  @example ```ts const preview = await
        client.loadBalancers.previews.get( 'p1aba936b94213e5b8dca0c0dbf1f9cc', {
        account_id: '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: previewId
          in: path
          required: true
          schema:
            type: string
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/regions/{regionId}:
    get:
      operationId: getRegion
      description: "Get a single region mapping.  @example ```ts const region = await
        client.loadBalancers.regions.get( 'WNAM', { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, ); ```"
      parameters:
        - name: regionId
          in: path
          required: true
          schema:
            type: string
            enum:
              - WNAM
              - ENAM
              - WEU
              - EEU
              - NSAM
              - SSAM
              - OC
              - ME
              - NAF
              - SAF
              - SAS
              - SEAS
              - NEAS
        - name: account_id
          in: path
          required: true
          description: Identifier.
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                oneOf:
                  - type: string
                nullable: true
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/regions:
    get:
      operationId: listRegions
      description: "List all region mappings.  @example ```ts const regions = await
        client.loadBalancers.regions.list({ account_id:
        '023e105f4ecef8ad9ca31a8372d0c353', }); ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
        - name: country_code_a2
          in: query
          required: false
          description: "Query param: Two-letter alpha-2 country code followed in ISO
            3166-1."
          schema:
            type: string
        - name: subdivision_code
          in: query
          required: false
          description: "Query param: Two-letter subdivision code followed in ISO 3166-2."
          schema:
            type: string
        - name: subdivision_code_a2
          in: query
          required: false
          description: "Query param: Two-letter subdivision code followed in ISO 3166-2."
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                oneOf:
                  - type: string
                nullable: true
      x-distilled-response-path: result
  /accounts/{account_id}/load_balancers/search:
    get:
      operationId: listSearches
      description: "Search for Load Balancing resources.  @example ```ts //
        Automatically fetches more pages as needed. for await (const
        searchListResponse of client.loadBalancers.searches.list( { account_id:
        '023e105f4ecef8ad9ca31a8372d0c353' }, )) { // ... } ```"
      parameters:
        - name: account_id
          in: path
          required: true
          description: "Path param: Identifier."
          schema:
            type: string
        - name: query
          in: query
          required: false
          description: "Query param: Search query term."
          schema:
            type: string
        - name: references
          in: query
          required: false
          description: 'Query param: The type of references to include. "\ " to include
            both referral and referrer references. "" to not include any
            reference information.'
          schema:
            type: string
            enum:
              - ""
              - "*"
              - referral
              - referrer
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  resources:
                    type: array
                    items:
                      type: object
                      properties:
                        reference_type:
                          type: string
                          enum:
                            - referral
                            - referrer
                          description: When listed as a reference, the type (direction) of the reference.
                        references:
                          type: array
                          items: {}
                          description: A list of references to (referrer) or from (referral) this
                            resource.
                        resource_id:
                          type: string
                        resource_name:
                          type: string
                          description: The human-identifiable name of the resource.
                        resource_type:
                          type: string
                          enum:
                            - load_balancer
                            - monitor
                            - pool
                          description: The type of the resource.
                    description: A list of resources matching the search query.
      x-distilled-pagination-class: SearchListResponsesV4PagePagination
