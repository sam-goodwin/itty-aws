/**
 * @distilled.cloud/spacetimedb — SpacetimeDB HTTP management SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy model
 * in `.generated-specs` (written by `scripts/convert.ts` from the OpenAPI
 * spec assembled from first-party HTTP docs). Everything else in this folder
 * is hand-written.
 *
 * @example
 * ```ts
 * import * as SpacetimeDB from "@distilled.cloud/spacetimedb";
 *
 * const info = yield* SpacetimeDB.Services.spacetimedb.getDatabase({
 *   name_or_identity: "my-db",
 * });
 * ```
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  SpacetimeDBProtocol,
  type SpacetimeDBOpError,
  type SpacetimeDBOpContext,
} from "./protocol.ts";
export { paginateCursor } from "./pagination.ts";
export * as Retry from "./retry.ts";
export * as Services from "./services/index.ts";
export * from "./services/spacetimedb.ts";
export { BadRequest, NotFound } from "./services/spacetimedb.ts";
