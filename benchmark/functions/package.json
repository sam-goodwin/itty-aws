{
  "name": "@benchmark/functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/*.ts --format esm --target esnext"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.245.0",
    "itty-aws": "workspace:^"
  },
  "devDependencies": {
    "tsup": "^8.0.1"
  }
}
