name: Nuke Test Resources

on:
  merge_group:
    types: [destroyed]
  # Manual trigger
  workflow_dispatch:
    inputs:
      all:
        description: "Nuke ALL SDKs"
        type: boolean
        default: false
      aws:
        description: "AWS"
        type: boolean
        default: false
      cloudflare:
        description: "Cloudflare"
        type: boolean
        default: false
      coinbase:
        description: "Coinbase"
        type: boolean
        default: false
      fly-io:
        description: "Fly.io"
        type: boolean
        default: false
      gcp:
        description: "GCP"
        type: boolean
        default: false
      kubernetes:
        description: "Kubernetes"
        type: boolean
        default: false
      mongodb-atlas:
        description: "MongoDB Atlas"
        type: boolean
        default: false
      neon:
        description: "Neon"
        type: boolean
        default: false
      planetscale:
        description: "PlanetScale"
        type: boolean
        default: false
      prisma-postgres:
        description: "Prisma Postgres"
        type: boolean
        default: false
      stripe:
        description: "Stripe"
        type: boolean
        default: false
      supabase:
        description: "Supabase"
        type: boolean
        default: false
      turso:
        description: "Turso"
        type: boolean
        default: false
      workos:
        description: "WorkOS"
        type: boolean
        default: false
      expo-eas:
        description: "Eas"
        type: boolean
        default: false
      modrinth:
        description: "Modrinth"
        type: boolean
        default: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  # ── Gate: decide whether to proceed ──
  gate:
    runs-on: ubuntu-latest
    outputs:
      run: ${{ steps.decide.outputs.run }}
    steps:
      - name: Validate inputs
        if: github.event_name == 'workflow_dispatch'
        run: |
          if [[ "${{ inputs.all }}" != "true" \
             && "${{ inputs.aws }}" != "true" \
             && "${{ inputs.cloudflare }}" != "true" \
             && "${{ inputs.coinbase }}" != "true" \
             && "${{ inputs.fly-io }}" != "true" \
             && "${{ inputs.gcp }}" != "true" \
             && "${{ inputs.kubernetes }}" != "true" \
             && "${{ inputs.mongodb-atlas }}" != "true" \
             && "${{ inputs.neon }}" != "true" \
             && "${{ inputs.planetscale }}" != "true" \
             && "${{ inputs.prisma-postgres }}" != "true" \
             && "${{ inputs.stripe }}" != "true" \
             && "${{ inputs.supabase }}" != "true" \
             && "${{ inputs.turso }}" != "true" \
             && "${{ inputs.workos }}" != "true" ]]; then
            echo "::error::You must select at least one SDK to nuke (or choose 'all')"
            exit 1
          fi

      - name: Decide whether to run
        id: decide
        run: echo "run=true" >> $GITHUB_OUTPUT

  # ── AWS ──
  nuke-aws:
    needs: gate
    if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.aws)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v6

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::391965393224:role/distilled-github-oidc-role
          aws-region: us-east-1

      - name: Install aws-nuke
        run: |
          curl -fsSL "https://github.com/ekristen/aws-nuke/releases/latest/download/aws-nuke-v3-linux-amd64.tar.gz" -o aws-nuke.tar.gz
          tar -xzf aws-nuke.tar.gz
          chmod +x aws-nuke
          sudo mv aws-nuke /usr/local/bin/

      - name: Run aws-nuke
        run: aws-nuke run --config packages/aws/nuke-config.yml --no-alias-check --no-dry-run

  # ── Fly.io ──
  nuke-fly-io:
    needs: gate
    if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.fly-io)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run build
        working-directory: packages/core
      - name: Run nuke script
        run: bun scripts/nuke.ts
        working-directory: packages/fly-io
        env:
          FLY_IO_API_KEY: ${{ secrets.FLY_IO_API_KEY }}

  # ── Kubernetes ──
  nuke-kubernetes:
    needs: gate
    if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.kubernetes)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run build
        working-directory: packages/core
      - name: Run nuke script
        run: bun scripts/nuke.ts
        working-directory: packages/kubernetes
        env:
          KUBERNETES_TOKEN: ${{ secrets.KUBERNETES_TOKEN }}
          KUBERNETES_API_URL: ${{ secrets.KUBERNETES_API_URL }}

  # ── MongoDB Atlas ──
  nuke-mongodb-atlas:
    needs: gate
    if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.mongodb-atlas)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run build
        working-directory: packages/core
      - name: Run nuke script
        run: bun scripts/nuke.ts
        working-directory: packages/mongodb-atlas
        env:
          MONGODB_ATLAS_CLIENT_ID: ${{ secrets.MONGODB_ATLAS_CLIENT_ID }}
          MONGODB_ATLAS_CLIENT_SECRET: ${{ secrets.MONGODB_ATLAS_CLIENT_SECRET }}

  # ── Neon ──
  nuke-neon:
    needs: gate
    if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.neon)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run build
        working-directory: packages/core
      - name: Run nuke script
        run: bun scripts/nuke.ts
        working-directory: packages/neon
        env:
          NEON_API_KEY: ${{ secrets.NEON_API_KEY }}

  # ── Prisma Postgres ──
  nuke-prisma-postgres:
    needs: gate
    if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.prisma-postgres)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run build
        working-directory: packages/core
      - name: Run nuke script
        run: bun scripts/nuke.ts
        working-directory: packages/prisma-postgres
        env:
          PRISMA_POSTGRES_API_TOKEN: ${{ secrets.PRISMA_POSTGRES_API_TOKEN }}

  # ── Stripe ──
  nuke-stripe:
    needs: gate
    if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.stripe)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run build
        working-directory: packages/core
      - name: Run nuke script
        run: bun scripts/nuke.ts
        working-directory: packages/stripe
        env:
          STRIPE_API_KEY: ${{ secrets.STRIPE_API_KEY }}

  # ── Supabase ──
  nuke-supabase:
    needs: gate
    if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.supabase)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run build
        working-directory: packages/core
      - name: Run nuke script
        run: bun scripts/nuke.ts
        working-directory: packages/supabase
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

  # ── Cloudflare ──
  # nuke-cloudflare:
  #   needs: gate
  #   if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.cloudflare)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v6
  #     - uses: oven-sh/setup-bun@v2
  #       with:
  #         bun-version: latest
  #     - run: bun install --frozen-lockfile
  #     - run: bun run build
  #       working-directory: packages/core
  #     - name: Run nuke script
  #       run: bun scripts/nuke.ts
  #       working-directory: packages/cloudflare
  #       env:
  #         CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  #         CLOUDFLARE_API_KEY: ${{ secrets.CLOUDFLARE_API_KEY }}
  #         CLOUDFLARE_API_EMAIL: ${{ secrets.CLOUDFLARE_API_EMAIL }}
  #         CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

  # ── Coinbase ──
  # nuke-coinbase:
  #   needs: gate
  #   if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.coinbase)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - TODO: no nuke script yet — run `bun run generate-nuke coinbase`

  # ── GCP ──
  # nuke-gcp:
  #   needs: gate
  #   if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.gcp)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - TODO: no nuke script yet — run `bun run generate-nuke gcp`

  # ── PlanetScale ──
  # nuke-planetscale:
  #   needs: gate
  #   if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.planetscale)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - TODO: no nuke script yet — run `bun run generate-nuke planetscale`

  # ── Turso ──
  # nuke-turso:
  #   needs: gate
  #   if: needs.gate.outputs.run == 'true' && (github.event_name == 'merge_group' || inputs.all || inputs.turso)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - TODO: no nuke script yet — run `bun run generate-nuke turso`
